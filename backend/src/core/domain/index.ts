/**
 * Domain Services Index
 *
 * Re-exports all domain service implementations.
 * Domain services contain business logic and coordinate between
 * repository and infrastructure ports.
 */

export { TaskDomainService } from './task.domain-service'
