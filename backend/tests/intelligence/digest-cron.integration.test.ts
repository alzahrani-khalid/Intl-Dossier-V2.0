// Wave 0 stub - implementation fills tests in later waves.

import { describe, it } from 'vitest'

describe('DIGEST-02: clearance-gated digest cron', () => {
  it.todo('generates per-subscriber digests filtered by each subscriber clearance level')
})
