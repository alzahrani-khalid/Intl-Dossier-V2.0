// Wave 0 stub - implementation fills tests in later waves.

import { describe, it } from 'vitest'

describe('DIGEST-01: digest subscriptions', () => {
  it.todo('subscribes and unsubscribes an authenticated user to a dossier digest')
})
