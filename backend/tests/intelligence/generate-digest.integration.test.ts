// Wave 0 stub - implementation fills tests in later waves.

import { describe, it } from 'vitest'

describe('DIGEST-04: generate and publish digest RPC flow', () => {
  it.todo('generates a preview digest and publishes the approved digest row')
})
