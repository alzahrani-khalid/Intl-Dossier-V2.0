# Data-Entry Quality Sweep — Merged & Ranked Backlog

**Branch:** `fix/prod-quality-sweep-260627` · **Planner pass:** 2026-06-27
**Source:** `findings-{A,B,C,D,E}-*.md` de-duped + re-verified against live code at cited
`file:line` (every CRITICAL/HIGH re-read this pass; key anchors quoted in `_LANES.md`).

## Counts

**Raw findings (per slice header):** A `C0 H4 M3 L2` · B `C1 H8 M20 L10` ·
C `C1 H4 M6 L8` · D `C3 H11 M17 L9` · E `C2 H7 M16 L8` → **C7 H34 M62 L37 = 140 rows**.

**After de-dup → distinct backlog items:**

| Severity  | FIX-NOW | NEEDS-DECISION |  DEFER |   Total |
| --------- | ------: | -------------: | -----: | ------: |
| CRITICAL  |       6 |              0 |      0 |   **6** |
| HIGH      |      24 |              6 |      0 |  **30** |
| MEDIUM    |      42 |              4 |     2¹ |  **48** |
| LOW       |       0 |              0 |     33 |  **33** |
| **Total** |  **72** |         **10** | **35** | **117** |

¹ Two MEDIUM items (E-20 locale digits, E-21 i18n ternaries) are **systemic mechanical
sweeps** that touch 33–40+ files spanning _every_ lane → they cannot be a disjoint lane and
are deferred to a post-merge sequential pass (see DEFER).

**FIX-NOW lane distribution:** L0(2) · L1(11) · L2(13) · L3(11) · L4(11) · L5(13) · L6(4) ·
L7(20) · L8(2). Nine lanes, file-disjoint (proof in `_LANES.md`).

## De-duplications applied (confirmed against code)

| Merged ID       | Members                                                                              | Why same                                                                                                                     |
| --------------- | ------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------- |
| **B-1 ≡ C-1**   | after-actions-create omits NOT NULL `aa_commitments.title`                           | identical insert at `after-actions-create/index.ts:207-221` (verified: no `title` key)                                       |
| **D-4 ≡ E-1**   | SettingsPage upserts ~23 non-existent `public.users` columns                         | same upsert `SettingsPage.tsx:172-217` (verified). Take higher sev → **CRITICAL**                                            |
| **D-6 ≡ E-4**   | "Set up two-factor" `<Button>` has no `onClick` + persists `mfa_enabled` w/o secret  | same `SecuritySettingsSection.tsx:163-166` (verified no onClick)                                                             |
| **C-5 ≡ E-9**   | PositionDossierLinker create+delete catch → `console.error` only                     | same `PositionDossierLinker.tsx:64-66,75-77` (verified)                                                                      |
| **A-3 ≡ E-5**   | wizard never validates; `canComplete` gates only name length                         | same `useCreateDossierWizard.ts:146` + `form-wizard.tsx` (verified)                                                          |
| **B-17 ≡ E-22** | `created_from_route/entity` sent by form, dropped by service insert                  | `commitments.service.ts:188` insert omits them (verified)                                                                    |
| **B-13 ≡ E-13** | IntakeForm renders raw `validation:required` via plain `<p>`                         | same `IntakeForm.tsx:40,47,267,309`                                                                                          |
| **B-22 ≡ E-11** | TaskEditDialog dot-form `t('tasks.*')` → English-in-Arabic (`tasks` ns unregistered) | same `TaskEditDialog.tsx`; B-22 also covers `DeleteTaskDialog.tsx`                                                           |
| **B-19 ≡ E-18** | CommitmentForm owner is raw free-text `<Input>` for a UUID (TODO picker)             | same `CommitmentForm.tsx:302-325`                                                                                            |
| **D-22 ≡ D-25** | admin role-set disagreement across client guard ↔ validators ↔ DB enum               | converge one role list                                                                                                       |
| **E-6 (split)** | due_date `toISOString()` 1-day-early in GST                                          | lives in **two** files: `CommitmentForm.tsx:120,144` (L3) + `CommitmentQuickForm.tsx:138` (L5) — fixed per-file in each lane |

**Rejected on verification (NOT chased)** — already disproved by slice C/E against live schema:
`aa_commitments` table-missing (false; table exists, real bug is omitted `title`); `ai_brief_status`
enum-missing (false); MoU `pending_approval/suspended/terminated` enum-drift (false; valid `mou_state`,
only i18n labels missing → C-8); `afterActions` namespace unregistered (false; nested under `common`).

---

## FIX-NOW — CRITICAL (6)

| ID        | Sev      | Bucket  | Entity / Form                  | Type                       | Key file(s)                                                                                                  | One-line fix                                                                                                                                                                                                                                                                                |
| --------- | -------- | ------- | ------------------------------ | -------------------------- | ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B-1 ≡ C-1 | CRITICAL | FIX-NOW | After-action → commitment      | wrong-column               | `after-actions-create/index.ts:207-221`; `commitment-editor/CommitmentEditor.tsx`; `hooks/useAfterAction.ts` | Add NOT-NULL `title` to the `aa_commitments` insert (`commitment.title ?? description.slice(0,200)`) + a title input in CommitmentEditor + the type → **L3**                                                                                                                                |
| D-1       | CRITICAL | FIX-NOW | Role self-elevation            | rls-block (too-permissive) | DB migration (staging)                                                                                       | `BEFORE UPDATE` trigger on `public.users` blocking `role` change unless `auth.role()='service_role'` → **L0**                                                                                                                                                                               |
| D-2       | CRITICAL | FIX-NOW | Clearance self-elevation       | rls-block (too-permissive) | DB migration (staging)                                                                                       | `BEFORE UPDATE` trigger on `public.profiles` blocking `clearance_level` change unless service_role → **L0**                                                                                                                                                                                 |
| D-3       | CRITICAL | FIX-NOW | Express MFA verify             | fake-success / auth-bypass | `backend/src/services/auth.service.ts:863`                                                                   | Replace `verifyMFACode(){return true}` with real TOTP (otpauth ±1) vs `public.users.mfa_secret`, **or** delete the Express MFA path → **L1**                                                                                                                                                |
| D-4 ≡ E-1 | CRITICAL | FIX-NOW | Settings "Save" (all sections) | wrong-column               | `pages/settings/SettingsPage.tsx:172-217` (+ sections)                                                       | Stop upserting ~23 phantom cols; write only real `users` cols (`full_name`,`job_title_en/ar`,`department`,`phone`,`avatar_url`,`language_preference`,`timezone`); route notifications→`notification_category_preferences`; appearance→DesignProvider; drop/honest-disable the rest → **L2** |
| E-2       | CRITICAL | FIX-NOW | Positions / AttachmentUploader | i18n                       | `positions/AttachmentUploader.tsx`; `i18n/{en,ar}/positions.json:248/422`                                    | `positions.json` defines `attachments` as a **string**; author a nested `attachments_uploader.*` key set EN+AR and repoint the component → **L7**                                                                                                                                           |

## FIX-NOW — HIGH (24)

| ID        | Sev  | Bucket  | Entity / Form                      | Type           | Key file(s)                                                                                           | One-line fix                                                                                                                                                |
| --------- | ---- | ------- | ---------------------------------- | -------------- | ----------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A-3 ≡ E-5 | HIGH | FIX-NOW | Dossier wizard submit              | validation-gap | `wizard/hooks/useCreateDossierWizard.ts:78,146`; `ui/form-wizard.tsx:191-210`                         | `await form.trigger()` in handleComplete + per-step `validate`; gate Complete on `formState.isValid` → **L6**                                               |
| A-4       | HIGH | FIX-NOW | engagement participants            | fake-success   | `wizard/config/engagement.config.ts:73,89`; `useCreateDossierWizard.ts:101-113`                       | Rethrow from `postCreate` so the compensating warning toast fires (stop silently dropping participants) → **L6**                                            |
| B-2       | HIGH | FIX-NOW | tasks-create (all tasks)           | wrong-column   | `tasks-create/index.ts:176-182`                                                                       | `.select('organization_id').eq('user_id', user.id)`; `tenantId = profile?.organization_id ?? user.id` → **L4**                                              |
| B-3       | HIGH | FIX-NOW | TaskEditDialog + tasks-update      | contract-drift | `tasks/TaskEditDialog.tsx:96-109`; `tasks-update/index.ts:158-176`                                    | Derive `status` from `workflow_stage` (done→completed, cancelled→cancelled, else in_progress) so SLA stops firing on done → **L4**                          |
| B-4       | HIGH | FIX-NOW | useTasks toasts                    | i18n-gap       | `hooks/useTasks.ts:128-139,207,242,265`                                                               | Move task toast keys into the **registered** `tasks-page` ns (colon form) + add EN/AR keys → **L4**                                                         |
| B-5       | HIGH | FIX-NOW | CommitmentQuickForm external owner | validation-gap | `work-creation/forms/CommitmentQuickForm.tsx:59,140-148`                                              | Remove the `external` owner option from the quick form (no contact field → `valid_owner` 500) → **L5**                                                      |
| B-6       | HIGH | FIX-NOW | AfterActionForm internal owner     | validation-gap | `routes/.../after-action.tsx:135-140`; `after-action-form/AfterActionForm.tsx`                        | Pass a real `availableUsers` list; block submit when an internal commitment has no `owner_user_id` → **L3**                                                 |
| B-7       | HIGH | FIX-NOW | updateCommitment                   | contract-drift | `services/commitments.service.ts:221-239`                                                             | Recompute `tracking_mode` from `owner_type` (internal⇒automatic / external⇒manual) in update, mirroring create → **L3**                                     |
| B-8       | HIGH | FIX-NOW | TaskQuickForm dossier link         | error-handling | `work-creation/forms/TaskQuickForm.tsx:122-176`                                                       | Surface link-insert failure (tasks have **no** backstop trigger → silent orphan + success toast) → **L5**                                                   |
| B-9       | HIGH | FIX-NOW | detect-overdue-commitments         | wrong-column   | `detect-overdue-commitments/index.ts:86`                                                              | Select `owner_user_id`/`owner_contact_id` not `owner_id` (42703 → fn 500s; overdue flagging never runs) → **L3**                                            |
| C-2       | HIGH | FIX-NOW | Calendar create participants       | fake-success   | `calendar/CalendarEntryForm.tsx:354`; `calendar-create/index.ts:133`                                  | Persist participants (junction/map) **or** remove the participants control (today: `attendee_ids:[]` hardcoded) → **L7**                                    |
| C-4       | HIGH | FIX-NOW | AttachPositionDialog               | error-handling | `positions/AttachPositionDialog.tsx:124-125`                                                          | `toast.error(t('positions:attach.attachError'))` (add key) + keep dialog open on failure → **L7**                                                           |
| C-5 ≡ E-9 | HIGH | FIX-NOW | PositionDossierLinker              | error-handling | `positions/PositionDossierLinker.tsx:64-66,75-77`                                                     | Surface `toast.error` in both create+delete catch blocks; add confirm on delete → **L7**                                                                    |
| D-5       | HIGH | FIX-NOW | Settings load                      | error-handling | `settings/SettingsPage.tsx:114-148`                                                                   | Read the real columns; show empty/unset state instead of fabricating name=email-prefix + all-toggles-ON → **L2**                                            |
| D-6 ≡ E-4 | HIGH | FIX-NOW | MFA setup (Settings)               | fake-success   | `settings/sections/SecuritySettingsSection.tsx:147,163-166`                                           | Wire the button to `setup-mfa`→QR→`verify-mfa-setup`, **or** honest-disable ("coming soon"); never persist `mfa_enabled` without a verified secret → **L2** |
| D-7       | HIGH | FIX-NOW | "Export my data"                   | fake-success   | `settings/sections/DataPrivacySettingsSection.tsx:112-142`                                            | Query real tables (`user_preferences`, real activity source); check `error`; fail honestly if a source is missing → **L2**                                  |
| D-8       | HIGH | FIX-NOW | "Delete account"                   | wrong-column   | `settings/sections/DataPrivacySettingsSection.tsx:187-196`                                            | Stop the client-side `users.update({status:'deleted'})` (cols don't exist; destructive from browser); honest-disable + route to a server fn → **L2**        |
| D-11      | HIGH | FIX-NOW | Deactivate / reactivate user       | wrong-column   | `deactivate-user/index.ts:89,202`; `reactivate-user/index.ts:73,106`                                  | Use `is_active boolean` (not non-existent `status`) — today every user 404s → **L1**                                                                        |
| D-12      | HIGH | FIX-NOW | Step-up MFA                        | contract-drift | `step-up-mfa/StepUpMFA.tsx:227-241`                                                                   | Send `{challenge_id, factor_id, totp_code}` and read `step_up_token` (match `auth-step-up-complete`) → **L1**                                               |
| D-13      | HIGH | FIX-NOW | Login form (MFA)                   | fake-success   | `store/authStore.ts:44,93-99`; `auth/LoginPage.tsx:42-52,147`                                         | Make `login()` re-throw on failure so nav gates on success; remove/disable the unreachable MFA input (decorative) → **L1**                                  |
| D-14      | HIGH | FIX-NOW | Create user (role write)           | error-handling | `create-user/index.ts:341-356`                                                                        | Fail/compensate the request if the `public.users.role` write doesn't land (authz reads that column) → **L1**                                                |
| E-3       | HIGH | FIX-NOW | All API-backed forms (~22 repos)   | error-handling | `lib/api-client.ts:60-61,88-89`                                                                       | Parse JSON body on `!response.ok`; throw an error carrying `{message, details}` so localized/field server errors reach the UI → **L8**                      |
| E-6       | HIGH | FIX-NOW | Commitment due_date (GST)          | contract-drift | `commitments/CommitmentForm.tsx:120,144` (L3); `work-creation/forms/CommitmentQuickForm.tsx:138` (L5) | Format local date without UTC: `format(values.due_date,'yyyy-MM-dd')` — `toISOString()` rolls back 1 day in UTC+4 → **L3+L5**                               |
| E-7       | HIGH | FIX-NOW | RelationshipForm                   | validation-gap | `relationships/RelationshipForm.tsx:66-77,114,129-133`                                                | Add `zodResolver` (required `to_contact_id`,`relationship_type`); gate submit; route messages via `FormMessage` → **L7**                                    |

## FIX-NOW — MEDIUM (42)

| ID          | Sev | Bucket  | Entity / Form                     | Type                       | Key file(s)                                                                                                          | One-line fix                                                                                                                                                                        |
| ----------- | --- | ------- | --------------------------------- | -------------------------- | -------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | ----------------------------------------------------------------------------------- |
| A-5         | MED | FIX-NOW | person/elected NULLs              | validation-gap             | (covered by A-3)                                                                                                     | Folded into A-3 trigger (`form.trigger()`) — no extra file → **L6**                                                                                                                 |
| A-6         | MED | FIX-NOW | elected_official office_type      | data-entry gap             | `wizard/steps/OfficeTermStep.tsx`; `wizard/config/person.config.ts:187-240`                                          | Add an `office_type` Select + map it in `filterExtensionData` → **L6**                                                                                                              |
| B-10        | MED | FIX-NOW | intake-tickets-create             | validation-gap             | `intake-tickets-create/index.ts:80,163-165`                                                                          | Require `title_ar`/`description_ar` in the validator (400 on missing); never coerce `                                                                                               |     | null` into NOT NULL cols → **L5**                                                   |
| B-11        | MED | FIX-NOW | IntakeQuickForm junction          | contract-drift             | `work-creation/forms/IntakeQuickForm.tsx:122-133`                                                                    | Insert a `work_item_dossiers` row (`work_item_type:'intake'`, `inheritance_source:'direct'`) after create → **L5**                                                                  |
| B-12        | MED | FIX-NOW | IntakeQuickForm EN→AR mirror      | i18n-gap                   | `work-creation/forms/IntakeQuickForm.tsx:128,130`                                                                    | Add Arabic title/desc inputs (stop mirroring English into NOT-NULL `*_ar`) → **L5**                                                                                                 |
| B-13 ≡ E-13 | MED | FIX-NOW | IntakeForm raw key                | i18n-gap                   | `intake-form/IntakeForm.tsx:40,47,267,309`                                                                           | Render AR messages via `FormMessage` (auto-translates) or wrap in `t()` → **L5**                                                                                                    |
| B-14        | MED | FIX-NOW | intake-tickets-create             | validation-gap             | `intake-tickets-create/index.ts:80`                                                                                  | Validate `request_type`/`urgency` against allowed-value arrays (400 on mismatch) → **L5**                                                                                           |
| B-15        | MED | FIX-NOW | intake create/update/assign       | error-handling             | `intake-tickets-create/index.ts:194`; `-update:246`; `-assign:185`                                                   | Log raw PG error server-side only; return a generic message (mirror `convert/index.ts`) → **L5**                                                                                    |
| B-16        | MED | FIX-NOW | IntakeForm labels                 | a11y                       | `intake-form/IntakeForm.tsx:187-317`                                                                                 | Add `htmlFor`/`id` pairs (or use FormLabel+FormControl) → **L5**                                                                                                                    |
| B-17 ≡ E-22 | MED | FIX-NOW | createCommitment audit cols       | contract-drift             | `services/commitments.service.ts:188-203`                                                                            | Add `created_from_route`/`created_from_entity` to the insert (form sends them; insert drops them) → **L3**                                                                          |
| B-19 ≡ E-18 | MED | FIX-NOW | CommitmentForm owner              | validation-gap             | `commitments/CommitmentForm.tsx:302-325`                                                                             | Floor: validate the id is a UUID + require a selection (reject free-text → no FK/`valid_owner` 500); reuse the `availableUsers` Select for internal → **L3**                        |
| B-20        | MED | FIX-NOW | CommitmentForm helper text        | i18n-gap                   | `commitments/CommitmentForm.tsx:317-321`; `i18n/{en,ar}/commitments.json`                                            | Move the hardcoded English helper strings into the `commitments` ns + `t()` → **L3**                                                                                                |
| B-21        | MED | FIX-NOW | WorkBoard drag-to-Done            | ux-correctness             | `hooks/useUnifiedKanban.ts:337-343`                                                                                  | Set `completed_at`/`completed_by` on completion (so SLA can compute on-time/late) → **L4**                                                                                          |
| B-22 ≡ E-11 | MED | FIX-NOW | TaskEditDialog/DeleteTaskDialog   | i18n-gap                   | `tasks/TaskEditDialog.tsx:160`; `tasks/DeleteTaskDialog.tsx:56,60`                                                   | Use the `tasks-page` ns with distinct keys (avoid `common.tasks.title="Tasks"` collision) → **L4**                                                                                  |
| B-23        | MED | FIX-NOW | ConflictDialog                    | i18n-gap                   | `tasks/ConflictDialog.tsx:78-128`                                                                                    | Add `conflict.*` keys (EN+AR) in `tasks-page` ns → **L4**                                                                                                                           |
| B-24        | MED | FIX-NOW | WorkBoard +Add                    | ux-correctness             | `pages/WorkBoard/WorkBoard.tsx:251-260`                                                                              | Open the work-creation palette prefilled with the stage, **or** remove the misleading +Add → **L4**                                                                                 |
| B-25        | MED | FIX-NOW | tasks-create/-update              | validation-gap             | `tasks-create/index.ts:75,217`; `tasks-update/index.ts:95,190`                                                       | Validate `priority`/`workflow_stage` enums in the edge fn; return generic error not `insertError.message` → **L4**                                                                  |
| B-26        | MED | FIX-NOW | stage→status map                  | contract-drift             | `pages/WorkBoard/WorkBoard.tsx:58-64`; `unified-kanban/utils/column-definitions.ts:261`; `status-transitions.ts:329` | Map 1:1 where the enum allows (todo→pending, review→review) so status filters don't miss → **L4**                                                                                   |
| B-27        | MED | FIX-NOW | WorkCreationPalette stale dossier | ux-correctness             | `work-creation/WorkCreationPalette.tsx:153`                                                                          | Handle null: `if(!dossierId) setSelectedDossier(undefined)` (X clear leaves stale selection) → **L5**                                                                               |
| B-28        | MED | FIX-NOW | Double success toast              | ux-correctness             | `work-creation/WorkCreationProvider.tsx:54`; `forms/TaskQuickForm.tsx:174`; `forms/IntakeQuickForm.tsx:138`          | Remove per-form `toast.success`; keep the provider's single toast → **L5**                                                                                                          |
| B-29        | MED | FIX-NOW | DossierPicker                     | a11y                       | `work-creation/DossierPicker.tsx:308,338-367`                                                                        | Add `aria-haspopup="listbox"` + wrap loading/empty in `aria-live="polite"` → **L5**                                                                                                 |
| C-6         | MED | FIX-NOW | Calendar title not required       | validation-gap             | `calendar/CalendarEntryForm.tsx:334-343`; `calendar-create/index.ts`                                                 | Require `title_en                                                                                                                                                                   |     | title_ar` client-side + 400 in edge (`calendar_entries.title_en` NOT NULL) → **L7** |
| C-7         | MED | FIX-NOW | Calendar reminder_minutes         | data-loss                  | `calendar/CalendarEntryForm.tsx:353`; `calendar-create/index.ts:45-57`                                               | Accept+persist `reminder_minutes`, **or** remove the control (today silently dropped) → **L7**                                                                                      |
| C-8         | MED | FIX-NOW | MoU status labels                 | i18n-gap                   | `i18n/{en,ar}/common.json` (`mous.statuses`)                                                                         | Add `pending_approval`/`suspended`/`terminated` (EN+AR); drop dead `internal_review`/`external_review`/`renewed` → **L7**                                                           |
| C-9         | MED | FIX-NOW | MoU transition button             | ux-correctness             | `pages/MoUs/MousPage.tsx:73-75`                                                                                      | Hide/disable the dead `onClick={()=>{/* handle transition */}}` placeholder (honest) → **L7**                                                                                       |
| C-10        | MED | FIX-NOW | Calendar recurring detect         | wrong-table                | `domains/calendar/hooks/useRecurringEvents.ts:202`                                                                   | Query `calendar_entries` recurrence cols, not empty `calendar_events.series_id` → **L7**                                                                                            |
| C-11        | MED | FIX-NOW | PositionDossierLinker label       | a11y                       | `positions/PositionDossierLinker.tsx:118-120`                                                                        | Bind `htmlFor`→control `id`; add `aria-required` → **L7**                                                                                                                           |
| D-16        | MED | FIX-NOW | AI settings first-save            | ux-correctness             | `routes/_protected/admin/ai-settings.tsx:196-237`                                                                    | `setFormState(null)` on success; payload `{...(policy??defaults),...formState}`; `.upsert(...,{onConflict:'organization_id'})` (UI fallback must match DB `openai/gpt-4o`) → **L2** |
| D-17        | MED | FIX-NOW | AI usage Top Users                | wrong-column               | `routes/_protected/admin/ai-usage.tsx:144-149`                                                                       | Query `users(id,email)` not `profiles(id,email)`; check `error` → **L2**                                                                                                            |
| D-18        | MED | FIX-NOW | notifications-center digest       | error-handling             | `notifications-center/index.ts:302-310`                                                                              | `.maybeSingle()` + handle `emailError` (0-row `email_notification_preferences` → PGRST116 swallowed) → **L2**                                                                       |
| D-20        | MED | FIX-NOW | Profile/General forms             | a11y                       | `settings/sections/ProfileSettingsSection.tsx:182-259`                                                               | Render field errors with `aria-invalid`+`aria-describedby` (Zod failure currently blocks submit silently) → **L2**                                                                  |
| D-21        | MED | FIX-NOW | Notification toggles              | a11y                       | `settings/sections/NotificationsSettingsSection.tsx:95-101`                                                          | Give each label an `id` + pass `aria-labelledby` to the `Switch` (8 unnamed switches) → **L2**                                                                                      |
| D-22 ≡ D-25 | MED | FIX-NOW | Admin role set                    | contract-drift             | `lib/auth/require-admin.ts:15`; `create-user/index.ts:76`; `assign-role/index.ts:148`                                | Converge ONE admin/role list across client guard ↔ validators (DB `role` is free `text`); UI picker rides D-10 → **L1**                                                             |
| D-23        | MED | FIX-NOW | Create user clearance             | validation-gap             | `create-user/index.ts:21-29,341-350`                                                                                 | Accept optional `clearance` (1-4 validated) and write `profiles.clearance_level` via service-role (UI field rides D-10) → **L1**                                                    |
| D-29        | MED | FIX-NOW | user-permissions read             | wrong-column               | `user-permissions/index.ts:204,231,255`                                                                              | Fix the `auth.users` embed + the `id,email,role`-only select that makes `allowed_resources` undefined; check `error` → **L1**                                                       |
| D-30        | MED | FIX-NOW | Notification prefs source         | wrong-table                | `settings/sections/NotificationsSettingsSection.tsx`; `hooks/useNotificationCenter.ts:349-384`                       | Point Settings notifications at `notification_category_preferences` (one source of truth; aligns with D-4) → **L2**                                                                 |
| D-31        | MED | FIX-NOW | Deactivate cleanup                | wrong-column               | `deactivate-user/index.ts:127-164`                                                                                   | Fix `pending_role_approvals` col names (`requested_by`/`first_approver_id`/`second_approver_id`); guard for real tables; check `error` → **L1**                                     |
| E-10        | MED | FIX-NOW | All RHF forms (shared)            | a11y                       | `components/ui/form.tsx:158-167`                                                                                     | Add `role="alert"` (or `aria-live`) to `FormMessage` — one-line systemic fix, errors never announced today → **L8**                                                                 |
| E-14        | MED | FIX-NOW | CalendarEntryForm alerts          | ux-correctness             | `calendar/CalendarEntryForm.tsx:334-391`                                                                             | Replace blocking `alert()` with toast/inline `role="alert"`; validate title client-side → **L7**                                                                                    |
| E-15        | MED | FIX-NOW | CalendarEntryForm saving label    | i18n-gap                   | `calendar/CalendarEntryForm.tsx:892`; `i18n/{en,ar}/common.json`                                                     | `common.saving` doesn't exist → renders literal; add the key or point at an existing one → **L7**                                                                                   |
| E-16        | MED | FIX-NOW | LegislationForm edit              | ux-correctness (data-loss) | `legislation/LegislationForm.tsx:166-168`                                                                            | In edit mode send `null` for cleared fields instead of stripping them (can't blank optional fields) → **L7**                                                                        |
| E-17        | MED | FIX-NOW | InteractionNoteForm               | error-handling             | `contacts/InteractionNoteForm.tsx:148-183`                                                                           | Track created note id; on partial (attachment) failure don't re-create the note on resubmit → **L7**                                                                                |
| E-19        | MED | FIX-NOW | AddDeliverableDialog              | i18n-gap                   | `commitments/deliverables/AddDeliverableDialog.tsx:57,70,276,314`                                                    | Use a `validation:` key + `FormMessage` (raw English "Required" in AR today) → **L3**                                                                                               |
| E-23        | MED | FIX-NOW | AttachmentUploader error keys     | i18n / error-handling      | `positions/AttachmentUploader.tsx:107,188,394,419`                                                                   | Use leaf keys (`common:errors.generic`) — `common:error` is an object → diagnostic string shown → **L7**                                                                            |
| E-24        | MED | FIX-NOW | PositionEditor                    | i18n / ux / a11y           | `position-editor/PositionEditor.tsx:543,369-372,409-418`                                                             | Colon-form `t('common:...')`; `role="alert"` on save-error; exclude `autoSaving` from manual-save guard → **L7**                                                                    |
| E-25        | MED | FIX-NOW | PositionDossierLinker labels      | i18n / a11y                | `positions/PositionDossierLinker.tsx:202,118,148,174`                                                                | Use an existing pending key; add `htmlFor`/`id` pairs → **L7**                                                                                                                      |

---

## NEEDS-DECISION (10)

Each needs a product/UX call, a new sizeable feature/route, or new infra. The **honest-disable
fallback** is the cheap, safe default if the decision is "not now" — most fallbacks are tiny and
ride an existing FIX-NOW lane (noted). Full detail + decision questions in `_LANES.md`.

| ID   | Sev  | Entity                                | Decision question                                                                                                          | Honest-disable fallback (cheap)                                                                                                      |
| ---- | ---- | ------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| A-1  | HIGH | Dossier **edit** (all 8 types)        | Build an edit surface — reuse wizard in edit mode, or inline-edit on detail pages?                                         | None needed: app is create-only today, no control falsely claims edit. Leave as-is.                                                  |
| A-2  | HIGH | `dossiers-update` edge fn             | Rewrite to match live `dossiers` schema + POST `{id,...}` contract — but to which edit UI's contract?                      | Leave dead (0 callers). Implement **with** A-1 once the edit-UI shape is chosen.                                                     |
| C-3  | HIGH | MoU create                            | Build a MoU create form/route writing `mous` (type/category/dates/parties/lifecycle_state)?                                | Disable the dead "Add MoU" button (`MousPage.tsx:210`) — **FIX-NOW task in L7**.                                                     |
| D-9  | HIGH | Avatar upload                         | Create the `avatars` storage bucket + RLS?                                                                                 | Hide the avatar upload control until the bucket exists — **FIX-NOW task in L2** (`ProfileSettingsSection.tsx`).                      |
| D-10 | HIGH | User-management create/detail         | Build `/users/create` + `/users/:id` routes and implement `userManagementApi` against the edge fns?                        | Remove the dead "Create User"/row-nav buttons (`UsersListPage.tsx:189,193`). Edge fns exist + are hardened by L1.                    |
| E-8  | HIGH | ConsistencyPanel                      | Wire a real consistency query + action handlers?                                                                           | Hide the inert panel at `routes/_protected/positions/$id.tsx:218` (always-empty, inert buttons).                                     |
| A-7  | MED  | Country flag/population/area          | Should `flag_url`/`population`/`area_sq_km`/`subregion` be user-editable at create?                                        | Document as derived/seed-only (flags already derived from ISO). Zero code.                                                           |
| B-18 | MED  | after-actions-update nested           | Implement nested commitment/decision/risk persistence (diff/upsert) in edit mode?                                          | Remove nested entities from `UpdateAfterActionRequest` + disable nested editors in AA edit mode so the 200 doesn't imply they saved. |
| D-15 | MED  | AI-settings org scoping (multi-admin) | Seed `organization_members`(admin/owner)+`default_organization_id` for every admin, or derive write-org from `users.role`? | Works for today's sole admin; document the limitation until a 2nd admin is added.                                                    |
| D-19 | MED  | MFA secret at rest                    | Adopt at-rest encryption (pgsodium/KMS) for `users.mfa_secret` (currently plaintext base32)?                               | Accept plaintext for now (already flagged in commit); no UI lie. New-infra decision.                                                 |

---

## DEFER (33 LOW + 2 systemic MED — not laned)

LOW / cosmetic per the triage rule ("don't lane them"). Where a LOW lives in a file a FIX-NOW
lane already owns, the lane's brief lists it as **opportunistic** (the worker _may_ fix it in
passing); it is NOT required and does not affect the disjointness proof.

**Systemic mechanical sweeps (MED, deferred — cannot be a disjoint lane, touch files in every lane):**

- **E-20** — bare `toLocaleString()`/`Intl.NumberFormat` → Latin digits in AR (~40+ files). Route through `lib/format-locale.ts`. **Run as one sequential pass AFTER lanes merge.**
- **E-21** — 1,173 `isRTL ? 'EN' : 'AR'` hard-coded ternaries (33+ form files). Migrate to namespace keys. **Sequential post-merge sweep.**

**LOW (defer):**
A-8 (Select `defaultValue`→`value`), A-9 (OfficeTermStep `aria-required` — L6-opportunistic),
B-30 (TaskDetail label keys — L4-opp), B-31 ≡ C-13 (`'critical'`→`'urgent'` AA type — L3-opp),
B-32 (IntakeForm dead edit mode — L5-opp), B-33 (intake assign/update app-role — L5-opp),
B-34 (`after_action_record`→`after_action`), B-35 (DossierPicker `ms-2` double-flip — L5-opp),
B-36 (TaskDetail `flex-row-reverse` — L4-opp), B-37 (column-definitions mutate-in-place — L4-opp),
B-38 (clear `completed_at` on leave-completed — L3-opp), B-39 (resolve owner/dossier UUIDs),
C-12 (calendar `alert()`→toast — folded into E-14/L7), C-14 (calendar end>start — L7-opp),
C-15/C-16/C-17/C-19 (relationship barrel/repo/health-score cleanups), C-18 (bulk attach `link_type`),
D-32 (auth-error `role="alert"`), D-33 (intake app-role — dup B-33), D-34 (test backdoors→fixtures),
D-35 (ai-settings label `htmlFor`), D-36 (`alert_threshold_percent` unwired), D-37 (prune dead i18n keys),
D-39 (appearance default→Bureau — upsert-drop covered by D-4/L2), D-40 (delete dead `services/auth.ts`),
E-26 (PersonCreatePage placeholders), E-27 (ContactForm `name_ar` copies EN), E-28 (InteractionNoteForm
date-fns locale — L7-opp), E-29 (IntakeForm label assoc — dup B-16/L5), E-31 (AttachmentUploader fake
progress + tokens — L7-opp), E-32 (PositionDossierLinker `ms-2` — L7-opp), E-33 (NewPositionDialog
lookup-error `role="alert"`).
