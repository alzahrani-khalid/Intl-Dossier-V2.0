---
phase: 05-responsive-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/hooks/useResponsive.ts
  - frontend/src/hooks/useMobile.tsx
  - frontend/src/routes/_protected.tsx
  - frontend/src/components/layout/MainLayout.tsx
  - frontend/src/components/ui/adaptive-dialog.tsx
autonomous: true
requirements: [RESP-04, RESP-05]

must_haves:
  truths:
    - 'Navigation collapses to hamburger drawer on viewports under 768px'
    - 'Navigation shows icon rail only on 768-1023px viewports'
    - 'Navigation shows full sidebar on 1024px+ viewports'
    - 'Bottom sheets render on mobile instead of centered dialogs'
    - 'A single useResponsive() hook is the only responsive detection hook in the codebase'
  artifacts:
    - path: 'frontend/src/routes/_protected.tsx'
      provides: 'NavigationShell wired as production nav'
      contains: 'NavigationShell'
    - path: 'frontend/src/components/ui/adaptive-dialog.tsx'
      provides: 'Adaptive dialog that switches between Modal and BottomSheet'
      exports: ['AdaptiveDialog']
    - path: 'frontend/src/hooks/useResponsive.ts'
      provides: 'Single responsive detection hook'
      exports: ['useResponsive']
  key_links:
    - from: 'frontend/src/routes/_protected.tsx'
      to: 'frontend/src/components/modern-nav/NavigationShell/NavigationShell.tsx'
      via: 'import and render'
      pattern: 'import.*NavigationShell'
    - from: 'frontend/src/components/ui/adaptive-dialog.tsx'
      to: 'frontend/src/components/ui/bottom-sheet.tsx'
      via: 'conditional render on mobile'
      pattern: 'BottomSheet'
---

<objective>
Wire responsive infrastructure: install vaul, consolidate responsive hooks, replace legacy navigation with NavigationShell, create AdaptiveDialog component.

Purpose: Establish the foundational responsive infrastructure that all subsequent plans depend on — navigation shell, responsive hook, and adaptive dialog pattern.
Output: Production navigation working at all breakpoints, single responsive hook, adaptive modal/bottom-sheet component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-responsive-design/05-CONTEXT.md
@.planning/phases/05-responsive-design/05-RESEARCH.md
@.planning/phases/05-responsive-design/05-UI-SPEC.md
@frontend/src/routes/_protected.tsx
@frontend/src/components/modern-nav/NavigationShell/NavigationShell.tsx
@frontend/src/hooks/useResponsive.ts
@frontend/src/hooks/useMobile.tsx
@frontend/src/components/ui/bottom-sheet.tsx
@frontend/src/components/ui/heroui-modal.tsx
@frontend/src/components/layout/MainLayout.tsx
@frontend/src/components/layout/AppSidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vaul, consolidate responsive hooks, create AdaptiveDialog</name>
  <files>
    frontend/package.json,
    frontend/src/hooks/useResponsive.ts,
    frontend/src/hooks/useMobile.tsx,
    frontend/src/components/ui/adaptive-dialog.tsx
  </files>
  <read_first>
    frontend/package.json,
    frontend/src/hooks/useResponsive.ts,
    frontend/src/hooks/useMobile.tsx,
    frontend/src/components/ui/bottom-sheet.tsx,
    frontend/src/components/ui/heroui-modal.tsx
  </read_first>
  <action>
1. Install vaul: `cd frontend && pnpm add vaul`

2. Consolidate responsive hooks:
   - Read `useMobile.tsx` to find all consumers (grep for `useIsMobile` and `useMobile` across the frontend).
   - Replace every import of `useIsMobile` from `useMobile.tsx` (or `@/hooks/useMobile`) with `import { useResponsive } from '@/hooks/useResponsive'` and replace `useIsMobile()` calls with `useResponsive().isMobile`.
   - After all consumers are migrated, add a deprecation comment to the top of `useMobile.tsx`:
     ```typescript
     /** @deprecated Use useResponsive() from '@/hooks/useResponsive' instead. Will be removed in Phase 6. */
     ```
   - Do NOT delete `useMobile.tsx` yet (keep as safety net for this phase).

3. Create `frontend/src/components/ui/adaptive-dialog.tsx`:
   - Import `useResponsive` from `@/hooks/useResponsive`
   - Import `BottomSheet` from `@/components/ui/bottom-sheet`
   - Import Modal from `@/components/ui/heroui-modal`
   - Export `AdaptiveDialog` component with props: `{ open: boolean, onOpenChange: (open: boolean) => void, title?: string, children: React.ReactNode, snapPreset?: 'small' | 'medium' | 'large' | 'full' }`
   - When `useResponsive().isMobile` is true, render `BottomSheet` with snap preset (default 'medium'), drag handle, and children inside `BottomSheet.Content`
   - When not mobile, render `HeroUIModal` (the existing heroui-modal wrapper) with title and children
   - Export explicit return type: `JSX.Element`
   - Use `useDirection()` for RTL-aware rendering if needed
   - All spacing must use logical properties (ms-_, me-_, ps-_, pe-_)
     </action>
     <verify>
     <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0/frontend && pnpm list vaul && grep -r "useIsMobile" src/ --include="_.tsx" --include="_.ts" -l | grep -v useMobile.tsx | head -5; echo "---"; test -f src/components/ui/adaptive-dialog.tsx && echo "adaptive-dialog exists"</automated>
     </verify>
     <acceptance_criteria>
   - `frontend/package.json` contains `"vaul"` in dependencies
   - `frontend/src/components/ui/adaptive-dialog.tsx` exists and contains `export function AdaptiveDialog`
   - `adaptive-dialog.tsx` contains `import { useResponsive }` and `import.*BottomSheet` and `import.*Modal`
   - `adaptive-dialog.tsx` contains `isMobile` conditional rendering
   - No files other than `useMobile.tsx` itself import `useIsMobile` (all migrated to `useResponsive`)
   - `useMobile.tsx` contains `@deprecated` comment
     </acceptance_criteria>
     <done>vaul installed, all useIsMobile consumers migrated to useResponsive, AdaptiveDialog component created with mobile bottom-sheet / desktop modal switching</done>
     </task>

<task type="auto">
  <name>Task 2: Wire NavigationShell into production layout, replace AppSidebar + Header</name>
  <files>
    frontend/src/routes/_protected.tsx,
    frontend/src/components/layout/MainLayout.tsx
  </files>
  <read_first>
    frontend/src/routes/_protected.tsx,
    frontend/src/components/layout/MainLayout.tsx,
    frontend/src/components/layout/AppSidebar.tsx,
    frontend/src/components/layout/Header.tsx,
    frontend/src/components/modern-nav/NavigationShell/NavigationShell.tsx
  </read_first>
  <action>
Per D-01: Adopt NavigationShell as production navigation, replacing AppSidebar + Header.

1. Read `_protected.tsx` to understand current layout structure (likely renders MainLayout which renders AppSidebar + Header + children).

2. Read `NavigationShell.tsx` to understand its API: props it accepts, how children are rendered (content area), any context providers it wraps.

3. Modify `_protected.tsx`:
   - Replace the MainLayout/AppSidebar/Header usage with NavigationShell
   - NavigationShell should wrap the `<Outlet />` (TanStack Router outlet for nested routes)
   - Preserve any existing providers or wrappers (AuthProvider, ThemeProvider, etc.) that are in \_protected.tsx
   - Preserve ChatDock and OnboardingTourTrigger if they exist — verify they work inside NavigationShell's DOM structure (they should be positioned fixed/absolute so parent DOM doesn't matter)
   - Remove imports of AppSidebar, Header, SiteHeader if they're no longer used

4. Per D-02 (Claude's discretion): Remove AppSidebar and Header imports from \_protected.tsx. Do NOT delete the component files themselves yet — they can be removed in a future cleanup pass. This minimizes risk.

5. Update `MainLayout.tsx`:
   - If MainLayout is still used by \_protected.tsx after the NavigationShell swap, simplify it to a thin pass-through wrapper
   - If MainLayout is no longer referenced, add a deprecation comment: `/** @deprecated Replaced by NavigationShell in Phase 5. Remove in Phase 6. */`

6. Verify the layout renders: `cd frontend && pnpm build` must succeed with no import errors.
   </action>
   <verify>
   <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0/frontend && pnpm build 2>&1 | tail -5</automated>
   </verify>
   <acceptance_criteria> - `_protected.tsx` contains `import.*NavigationShell` from the modern-nav directory - `_protected.tsx` does NOT contain `import.*AppSidebar` or `import.*SiteHeader` - `_protected.tsx` renders `<NavigationShell>` wrapping route content (Outlet) - `pnpm build` succeeds with exit code 0 - ChatDock and OnboardingTourTrigger (if present) are preserved in the layout
   </acceptance_criteria>
   <done>NavigationShell is the production navigation in \_protected.tsx, AppSidebar and Header are no longer imported, build passes cleanly</done>
   </task>

</tasks>

<verification>
- `cd frontend && pnpm build` passes
- `pnpm list vaul` shows vaul installed
- `grep -r "useIsMobile" src/ --include="*.tsx" --include="*.ts" -l | grep -v useMobile.tsx` returns empty (all migrated)
- `grep "NavigationShell" src/routes/_protected.tsx` shows NavigationShell is wired
- `adaptive-dialog.tsx` exists with BottomSheet + Modal conditional rendering
</verification>

<success_criteria>

- NavigationShell is the production nav at all 3 breakpoint states (hamburger/icon-rail/full-sidebar)
- Single useResponsive() hook is the standard for responsive detection
- AdaptiveDialog component available for Plans 03-05 to use in modal conversions
- vaul package installed enabling bottom-sheet functionality
- Build passes with zero errors
  </success_criteria>

<output>
After completion, create `.planning/phases/05-responsive-design/05-01-SUMMARY.md`
</output>
