---
phase: 18-e2e-test-suite
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - tests/e2e/01-login.spec.ts
  - tests/e2e/03-dossier-navigation.spec.ts
  - tests/e2e/04-command-palette.spec.ts
  - tests/e2e/02-engagement-lifecycle.spec.ts
  - tests/e2e/07-calendar-events.spec.ts
  - tests/e2e/support/pages/LoginPage.ts
  - tests/e2e/support/pages/DossierListPage.ts
  - tests/e2e/support/pages/DossierDetailPage.ts
  - tests/e2e/support/pages/CommandPalettePage.ts
  - tests/e2e/support/pages/EngagementPage.ts
  - tests/e2e/support/pages/CalendarPage.ts
  - tests/e2e/policy-officer-multi-dossier.spec.ts
  - tests/e2e/country-analyst-relationships.spec.ts
  - tests/e2e/calendar-event-creation.spec.ts
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-07]
must_haves:
  truths:
    - "User can log in with email/password, session persists across page reload, user can log out"
    - "User can navigate dossier list → detail → tabs → RelationshipSidebar"
    - "User can open Cmd+K, search, navigate to a result, see recent items"
    - "User can create an engagement and transition it through lifecycle stages"
    - "User can create a calendar event and view it"
  artifacts:
    - path: "tests/e2e/01-login.spec.ts"
      provides: "TEST-01 coverage"
    - path: "tests/e2e/03-dossier-navigation.spec.ts"
      provides: "TEST-03 coverage"
    - path: "tests/e2e/04-command-palette.spec.ts"
      provides: "TEST-04 coverage"
    - path: "tests/e2e/02-engagement-lifecycle.spec.ts"
      provides: "TEST-02 coverage (refactors policy-officer-multi-dossier)"
    - path: "tests/e2e/07-calendar-events.spec.ts"
      provides: "TEST-07 coverage (refactors calendar-event-creation)"
  key_links:
    - from: "tests/e2e/01-login.spec.ts"
      to: "tests/e2e/support/fixtures"
      via: "import { test, expect }"
      pattern: "from.*support/fixtures"
    - from: "tests/e2e/03-dossier-navigation.spec.ts"
      to: "tests/e2e/support/pages/DossierListPage"
      via: "POM instantiation"
      pattern: "new DossierListPage"
---

<objective>
Wave 2 — Core navigation journeys. Implement TEST-01 (login), TEST-03 (dossier navigation), TEST-04 (Cmd+K), TEST-02 (engagement lifecycle), TEST-07 (calendar). These are the highest-value, lowest-flake flows. Refactor 3 existing specs into the new POM/fixture pattern (D-18) instead of writing from scratch.

Purpose: Land the 5 specs that depend only on Wave 1 scaffolding. No drag-drop, no AI stubs, no realtime — those are Wave 3.
Output: 5 working spec files + filled-in POM method bodies for the surfaces they exercise.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-e2e-test-suite/18-CONTEXT.md
@.planning/phases/18-e2e-test-suite/18-RESEARCH.md
@.planning/phases/18-e2e-test-suite/18-01-SUMMARY.md
@tests/e2e/policy-officer-multi-dossier.spec.ts
@tests/e2e/country-analyst-relationships.spec.ts
@tests/e2e/calendar-event-creation.spec.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TEST-01 login + TEST-04 Cmd+K + TEST-03 dossier navigation</name>
  <files>tests/e2e/01-login.spec.ts, tests/e2e/03-dossier-navigation.spec.ts, tests/e2e/04-command-palette.spec.ts, tests/e2e/support/pages/LoginPage.ts, tests/e2e/support/pages/DossierListPage.ts, tests/e2e/support/pages/DossierDetailPage.ts, tests/e2e/support/pages/CommandPalettePage.ts, tests/e2e/country-analyst-relationships.spec.ts</files>
  <action>
Implements TEST-01, TEST-03, TEST-04. All specs import `{ test, expect }` from `../support/fixtures`. Use the `adminPage` fixture from Wave 1 unless a test explicitly needs a fresh login.

**TEST-01 — `01-login.spec.ts`**:
- `test('signs in with email/password and reaches dashboard')` — uses raw `page` (no storageState), goes to `/login`, fills creds from `process.env.E2E_ADMIN_*`, expects dashboard URL.
- `test('persists session across page reload')` — uses `adminPage`, reloads, expects no redirect to login.
- `test('signs out and returns to login')` — uses `adminPage`, calls `LoginPage.signOut()`, expects `/login`.
Fill in `LoginPage.signIn()` and `signOut()` bodies (use `getByLabel(/email/i)`, `getByRole('button', { name: /sign in/i })`, then user menu → sign out — fall back to `data-testid="user-menu"` if role-based search is ambiguous).

**TEST-03 — `03-dossier-navigation.spec.ts`**:
- `test('navigates list → detail → tabs → RelationshipSidebar')` — uses `analystPage`, opens `/dossiers`, asserts seed entity visible (e.g., `Saudi Arabia` from Phase 17 seed), clicks it, asserts URL pattern `/dossiers/[id]`, opens each tab in turn (`overview`, `docs`, `work-items`, `calendar`, `relationships`), opens RelationshipSidebar and asserts at least one relation row, closes sidebar.
Fill in `DossierListPage.searchByName`, `openDossier`, and `DossierDetailPage.openTab`, `openRelationshipSidebar`, `closeRelationshipSidebar` bodies.

**TEST-04 — `04-command-palette.spec.ts`**:
- `test('opens Cmd+K, searches, navigates to result')` — uses `adminPage`, calls `CommandPalettePage.open()` (handles mac vs linux modifier), types a seed dossier name, presses Enter, asserts navigation to that dossier's detail URL.
- `test('Cmd+K shows recent items after navigation')` — visit two dossiers in sequence, reopen palette, assert recent items section shows them.
Fill in `CommandPalettePage.open`, `search`, `selectResult`, `close` bodies. Use `process.platform === 'darwin' ? 'Meta+K' : 'Control+K'`.

**Refactor `country-analyst-relationships.spec.ts`** (D-18):
- Replace its bespoke navigation with calls to the new POMs. Keep the test name. Map it to TEST-03 by renaming or adding a `// @covers TEST-03` comment. If it duplicates the new `03-dossier-navigation.spec.ts` test entirely, delete it and note in commit message.

All assertions live in spec files. POM methods do navigation/interaction, not assertion.
  </action>
  <verify>
    <automated>pnpm exec playwright test 01-login.spec.ts 03-dossier-navigation.spec.ts 04-command-palette.spec.ts --project=chromium-en --reporter=list</automated>
  </verify>
  <done>
- All three new spec files exist and pass against staging.
- LoginPage, DossierListPage, DossierDetailPage, CommandPalettePage methods used in these specs are fully implemented.
- `country-analyst-relationships.spec.ts` either refactored to use POMs or removed (with note).
- `tsc --noEmit` clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: TEST-02 engagement lifecycle + TEST-07 calendar</name>
  <files>tests/e2e/02-engagement-lifecycle.spec.ts, tests/e2e/07-calendar-events.spec.ts, tests/e2e/support/pages/EngagementPage.ts, tests/e2e/support/pages/CalendarPage.ts, tests/e2e/policy-officer-multi-dossier.spec.ts, tests/e2e/calendar-event-creation.spec.ts</files>
  <action>
Implements TEST-02, TEST-07.

**TEST-02 — `02-engagement-lifecycle.spec.ts`** (refactor of `policy-officer-multi-dossier.spec.ts` per D-18):
- Use `analystPage` fixture and `uniqueId` fixture.
- `test('creates engagement and transitions through lifecycle')`:
  1. Generate `const title = uniqueId('engagement')`.
  2. Call `EngagementPage.create({ title, dossierId: <seed dossier>, type: 'consultation' })`.
  3. Assert engagement appears in list with status `planning`.
  4. Call `EngagementPage.transitionTo('active')`, assert status badge updates.
  5. Call `EngagementPage.transitionTo('completed')`, assert.
  6. `afterEach` cleanup deletes by title prefix via `cleanupE2eEntities`.
Fill in `EngagementPage.create` and `transitionTo` (use `getByRole('button', { name: /create engagement/i })`, form fields by label, status transition controls — fall back to `data-testid="engagement-status-button"` if needed).

**TEST-07 — `07-calendar-events.spec.ts`** (refactor of `calendar-event-creation.spec.ts` per D-18):
- Use `analystPage`, `uniqueId`.
- `test('creates a calendar event and views it')`:
  1. `const title = uniqueId('cal')`.
  2. `CalendarPage.createEvent({ title, start: '2026-05-01T10:00', end: '2026-05-01T11:00' })`.
  3. Assert event card visible in calendar grid.
  4. Click event, assert detail panel opens with title.
- `test('shows lifecycle dates on engagement-linked event')` — create engagement (or use seed), assert linked calendar entry exists with correct lifecycle date badges.
Fill in `CalendarPage.createEvent` and `viewEvent` bodies.

After refactoring, the original `policy-officer-multi-dossier.spec.ts` and `calendar-event-creation.spec.ts` files: either delete (preferred — cleaner) or convert to thin re-exports of the new specs. Decide per file based on whether they covered anything not in the new spec; document choice in commit message.

Both spec files must include `@mobile` tag on at least one assertion path so the `chromium-mobile` project picks up at least one test per requirement (mobile-first coverage requirement).
  </action>
  <verify>
    <automated>pnpm exec playwright test 02-engagement-lifecycle.spec.ts 07-calendar-events.spec.ts --project=chromium-en --reporter=list</automated>
  </verify>
  <done>
- Both new spec files exist and pass.
- EngagementPage and CalendarPage have working method bodies for the actions used.
- Original 2 specs refactored or deleted.
- Cleanup runs in `afterEach` (no orphan e2e- entities after run).
- At least one mobile-tagged test exists.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test browser → app login form | Untrusted credentials submitted to auth endpoint |
| Test → Supabase cleanup | Service-role deletes scoped to e2e- prefixed entities |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Spoofing | Login spec hardcodes credentials | mitigate | All credentials read from `process.env.E2E_*`; CI injects via GitHub secrets |
| T-18-07 | Tampering | Engagement transition spec mutates seed data | mitigate | Test creates own engagement via `uniqueId`; never mutates seed entities |
| T-18-08 | Denial of Service | Test entities accumulate if cleanup fails | mitigate | `afterEach` cleanup + nightly seed reset (Phase 17 dependency) |
</threat_model>

<verification>
- `pnpm exec playwright test 01-login 02-engagement 03-dossier 04-command 07-calendar --project=chromium-en` passes
- No e2e- entities remain in DB after run (manual spot check)
- `tsc --noEmit` clean
</verification>

<success_criteria>
- 5 spec files green covering TEST-01, TEST-02, TEST-03, TEST-04, TEST-07
- 3 existing specs refactored or deleted per D-18
- POMs for these surfaces fully implemented
- At least one mobile-tagged test
</success_criteria>

<output>
After completion, create `.planning/phases/18-e2e-test-suite/18-02-SUMMARY.md`
</output>
