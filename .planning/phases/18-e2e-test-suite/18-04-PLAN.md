---
phase: 18-e2e-test-suite
plan: 04
type: execute
wave: 4
depends_on: [18-01, 18-02, 18-03]
files_modified:
  - .github/workflows/e2e.yml
  - .github/workflows/ci.yml
  - playwright.config.ts
  - package.json
  - tests/e2e/support/fixtures.ts
  - .planning/phases/18-e2e-test-suite/18-FLAKE-LOG.md
autonomous: true
requirements: [TEST-11]
must_haves:
  truths:
    - "E2E suite runs on every PR and every push to main"
    - "CI uses 2 parallel shards via GitHub Actions matrix; results merged via blob reporter"
    - "On test failure, screenshots and Playwright traces upload as CI artifacts"
    - "Failed CI run blocks PR merge"
    - "Retried tests are logged for visibility"
  artifacts:
    - path: ".github/workflows/e2e.yml"
      provides: "Playwright CI job with shard matrix and merge-reports"
    - path: ".planning/phases/18-e2e-test-suite/18-FLAKE-LOG.md"
      provides: "Living document tracking retried/flaky tests for follow-up"
  key_links:
    - from: ".github/workflows/e2e.yml"
      to: "playwright.config.ts blob reporter"
      via: "CI env triggers blob reporter; merge job consumes blob outputs"
      pattern: "blob"
    - from: ".github/workflows/e2e.yml"
      to: "tests/e2e/support/storage"
      via: "auth.setup runs as project dependency before specs"
      pattern: "playwright test"
---

<objective>
Wave 4 — CI integration. Implements TEST-11. Wires the E2E suite into GitHub Actions with sharding (D-13), retry policy logging (D-14), failure artifact upload (D-16), and merge-blocking branch protection (D-15). Hardens flake handling by logging retried tests.

Purpose: Make the E2E suite a first-class CI gate. After this plan, every PR runs the suite in ~2x parallel and surfaces failures with full traces.
Output: GitHub Actions workflow + flake log scaffold + small fixture additions for retry logging.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-e2e-test-suite/18-CONTEXT.md
@.planning/phases/18-e2e-test-suite/18-RESEARCH.md
@.planning/phases/18-e2e-test-suite/18-01-SUMMARY.md
@.planning/phases/18-e2e-test-suite/18-02-SUMMARY.md
@.planning/phases/18-e2e-test-suite/18-03-SUMMARY.md
@.github/workflows/ci.yml
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitHub Actions workflow with shard matrix + merge-reports</name>
  <files>.github/workflows/e2e.yml, .github/workflows/ci.yml</files>
  <action>
Implements TEST-11 + D-12..D-17.

Create `.github/workflows/e2e.yml`:

```yaml
name: E2E Tests
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  e2e:
    name: E2E (shard ${{ matrix.shard }}/${{ matrix.total }})
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2]
        total: [2]
    env:
      CI: true
      E2E_BASE_URL: ${{ secrets.E2E_BASE_URL }}
      E2E_ADMIN_EMAIL: ${{ secrets.E2E_ADMIN_EMAIL }}
      E2E_ADMIN_PASSWORD: ${{ secrets.E2E_ADMIN_PASSWORD }}
      E2E_ANALYST_EMAIL: ${{ secrets.E2E_ANALYST_EMAIL }}
      E2E_ANALYST_PASSWORD: ${{ secrets.E2E_ANALYST_PASSWORD }}
      E2E_INTAKE_EMAIL: ${{ secrets.E2E_INTAKE_EMAIL }}
      E2E_INTAKE_PASSWORD: ${{ secrets.E2E_INTAKE_PASSWORD }}
      SUPABASE_URL: ${{ secrets.E2E_SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.E2E_SUPABASE_SERVICE_ROLE_KEY }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: 10 }
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec playwright install --with-deps chromium
      - name: Run Playwright tests (shard ${{ matrix.shard }})
        run: pnpm exec playwright test --shard=${{ matrix.shard }}/${{ matrix.total }} --project=chromium-en --project=chromium-ar-smoke
      - name: Upload blob report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: blob-report-${{ matrix.shard }}
          path: blob-report
          retention-days: 90
      - name: Upload traces & screenshots on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-failure-${{ matrix.shard }}
          path: |
            test-results/**/*.zip
            test-results/**/*.png
            test-results/**/*.webm
          retention-days: 90

  merge-reports:
    name: Merge Playwright reports
    if: always()
    needs: [e2e]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: 10 }
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: 'pnpm' }
      - run: pnpm install --frozen-lockfile
      - uses: actions/download-artifact@v4
        with:
          path: all-blob-reports
          pattern: blob-report-*
          merge-multiple: true
      - run: pnpm exec playwright merge-reports --reporter=html ./all-blob-reports
      - uses: actions/upload-artifact@v4
        with:
          name: playwright-html-report
          path: playwright-report
          retention-days: 90
```

Then update `.github/workflows/ci.yml` (Phase 14 pipeline) to add a `needs: [e2e]` gate on the merge-blocking job, OR document in commit message that branch protection rules in repo settings should require the `E2E (shard 1/2)`, `E2E (shard 2/2)`, and `Merge Playwright reports` checks. (Branch protection is configured in GitHub UI, not in YAML — note this in the SUMMARY.)

DECISION: GitHub Actions matrix sharding (not Playwright workers) — gives independent job logs, faster feedback per shard, easier to retry one shard.
DECISION: Both `chromium-en` and `chromium-ar-smoke` projects run in the same shard pass — they're cheap enough together. `chromium-mobile` runs only on push to main (add separate job if mobile takes too long; defer that decision until baseline runtime is measured).

Document required GitHub secrets in commit message:
- `E2E_BASE_URL`, `E2E_ADMIN_*`, `E2E_ANALYST_*`, `E2E_INTAKE_*`, `E2E_SUPABASE_URL`, `E2E_SUPABASE_SERVICE_ROLE_KEY`
  </action>
  <verify>
    <automated>pnpm exec playwright test --shard=1/2 --project=chromium-en --reporter=list --list</automated>
  </verify>
  <done>
- `.github/workflows/e2e.yml` exists
- Workflow YAML validates (`gh workflow view e2e.yml` after push, or `actionlint` if installed)
- Workflow uses 2-shard matrix
- Failure artifacts upload step exists with `if: failure()`
- Blob reports merged in separate job
  </done>
</task>

<task type="auto">
  <name>Task 2: Flake logging + retry visibility + final hardening</name>
  <files>tests/e2e/support/fixtures.ts, .planning/phases/18-e2e-test-suite/18-FLAKE-LOG.md, package.json, playwright.config.ts</files>
  <action>
Implements D-14 retry-logging tail.

1. Create `.planning/phases/18-e2e-test-suite/18-FLAKE-LOG.md` with template:
```markdown
# Phase 18 E2E Flake Log

> Per D-14: 2 retries are a flake bandaid, NOT a license to write flaky tests.
> Persistent flakes (>2 retries in 7 days) MUST be fixed at the source.

## Tracked Flakes

| Test ID | First Seen | Last Retried | Retry Count | Owner | Fix Status |
|---------|-----------|--------------|-------------|-------|------------|
| _none yet_ | | | | | |

## Resolution Log

| Test ID | Date Fixed | Root Cause | Fix |
|---------|-----------|------------|-----|
| _none yet_ | | | |
```

2. Add a Playwright reporter hook that prints retried tests at end of run. Either:
   - Add a custom reporter file `tests/e2e/support/flake-reporter.ts` implementing `Reporter` interface, or
   - Use the built-in `--reporter=list` which already shows retries; instead add a small post-run script `pnpm test:e2e:flake-report` that parses the JSON report.
   
   PREFER: Custom reporter, simpler:
```ts
// tests/e2e/support/flake-reporter.ts
import type { Reporter, TestCase, TestResult } from '@playwright/test/reporter'
export default class FlakeReporter implements Reporter {
  private retried: { title: string; retry: number }[] = []
  onTestEnd(test: TestCase, result: TestResult): void {
    if (result.retry > 0) {
      this.retried.push({ title: test.titlePath().join(' > '), retry: result.retry })
    }
  }
  onEnd(): void {
    if (this.retried.length === 0) return
    console.warn('\n=== RETRIED TESTS (potential flakes) ===')
    for (const t of this.retried) console.warn(`  retry=${t.retry}  ${t.title}`)
    console.warn(`Total retried: ${this.retried.length}`)
    console.warn('=> Log in .planning/phases/18-e2e-test-suite/18-FLAKE-LOG.md')
  }
}
```
Add to `playwright.config.ts` reporter array: `[['blob'], ['github'], ['./tests/e2e/support/flake-reporter.ts']]` in CI mode.

3. Add npm script `"test:e2e:ci": "playwright test --project=chromium-en --project=chromium-ar-smoke"` so CI runs match local intent.

4. Final hardening pass — review all 13 specs (10 EN + 3 AR) for these flake patterns and fix any found:
   - `waitForTimeout` other than the 200ms post-drag → replace with `expect.poll` or explicit locator wait
   - Hardcoded sleeps
   - Reliance on race-y network requests without `waitForResponse`
   - Missing `afterEach` cleanup
   - Tests that mutate seed data instead of creating own entities
  </action>
  <verify>
    <automated>pnpm exec playwright test --project=chromium-en --reporter=./tests/e2e/support/flake-reporter.ts</automated>
  </verify>
  <done>
- Flake reporter prints retried tests at end of run
- 18-FLAKE-LOG.md template committed
- No `waitForTimeout` calls outside the documented dnd-kit drop wait
- All specs use `afterEach` cleanup where they create entities
- `test:e2e:ci` script defined
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → Supabase | CI uses repo secrets for service-role key |
| GitHub artifacts → public PR viewers | Trace files may include screenshots of test data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-13 | Information Disclosure | Failure artifacts uploaded to GitHub | mitigate | E2E runs against staging Supabase with synthetic seed data; no real PII; private repo restricts artifact access |
| T-18-14 | Spoofing | GitHub secrets injected into workflow | mitigate | Secrets scoped to repo, never echoed; `set-env` not used; secrets masked in logs |
| T-18-15 | Elevation of Privilege | E2E_SUPABASE_SERVICE_ROLE_KEY in CI env | mitigate | Key scoped to E2E project (deferred provisioning — risk noted in 18-01); rotated quarterly |
| T-18-16 | Denial of Service | Flaky tests waste CI minutes | mitigate | 2-retry cap + 30-min job timeout + flake log forces fixes |
</threat_model>

<verification>
- `gh workflow run e2e.yml` (or push to a test branch) — both shards complete
- Failure artifact upload verified by intentionally failing one test and confirming the trace zip appears
- Branch protection rule documented (manual GitHub UI step)
</verification>

<success_criteria>
- E2E workflow runs on PR + push to main
- 2-shard matrix with merge-reports
- Failure artifacts upload only on failure (90-day retention)
- Flake log scaffolded
- TEST-11 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/18-e2e-test-suite/18-04-SUMMARY.md`
</output>
