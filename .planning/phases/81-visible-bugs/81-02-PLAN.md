---
phase: 81-visible-bugs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/settings/SettingsSectionCard.tsx
  - frontend/src/components/settings/sections/ProfileSettingsSection.tsx
  - frontend/src/routes/_protected/calendar.tsx
  - frontend/src/components/calendar/UnifiedCalendar.tsx
autonomous: true
requirements: [BUG-02, BUG-03]

must_haves:
  truths:
    - "Satisfies D-81-02: the settings page renders exactly one 'Profile Settings' title + description — the inner SettingsSectionCard header duplicate is removed, the SettingsLayout page-level title is kept"
    - "Satisfies D-81-03: the calendar view exposes exactly one primary create-event action — the PageHeader 'Create Event' action is kept, the redundant UnifiedCalendar toolbar button is dropped"
    - 'Satisfies D-81-06: no new colors, shadows, or physical properties introduced; removals only, tokens untouched'
    - 'Section cards elsewhere on the settings page do not repeat the page-level section title verbatim'
  artifacts:
    - path: 'frontend/src/components/settings/SettingsSectionCard.tsx'
      provides: 'CardHeader rendered only when a title is actually provided (title/description optional)'
    - path: 'frontend/src/components/settings/sections/ProfileSettingsSection.tsx'
      provides: 'Profile section card without the duplicated title/description header'
    - path: 'frontend/src/components/calendar/UnifiedCalendar.tsx'
      provides: 'Calendar toolbar without the redundant create-event button (Today + type filter kept)'
  key_links:
    - from: 'frontend/src/components/settings/SettingsLayout.tsx'
      to: 'frontend/src/i18n/en/settings.json'
      via: "page-level header t('profile.title') — the single surviving render of the title"
      pattern: "navLabelKey\\(activeSection\\)"
    - from: 'frontend/src/routes/_protected/calendar.tsx'
      to: '/calendar/new'
      via: 'PageHeader actions Link — the single surviving create-event action'
      pattern: 'calendar/new'
---

<objective>
Fix BUG-02 (F2, HIGH) and BUG-03 (F3, MEDIUM) — two "remove the duplicate element" fixes on
disjoint files.

BUG-02: "Profile Settings" + its description render twice — once as the page-level header in
`SettingsLayout.tsx` (which shows `t('profile.title')` for the active section) and again
verbatim inside `ProfileSettingsSection.tsx` via `SettingsSectionCard`'s CardHeader.

BUG-03: two "Create Event" buttons render on /calendar — the PageHeader action in
`routes/_protected/calendar.tsx` (Link to /calendar/new) and the toolbar button in
`UnifiedCalendar.tsx` (opens the inline create form).

Purpose: exactly one title and exactly one primary create action, per the signed-off audit.
Output: surgical removals; page-level header and PageHeader action kept.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-visible-bugs/81-CONTEXT.md
@CLAUDE.md
@frontend/src/components/settings/SettingsLayout.tsx
@frontend/src/components/settings/SettingsSectionCard.tsx
@frontend/src/components/settings/sections/ProfileSettingsSection.tsx
@frontend/src/routes/_protected/calendar.tsx
@frontend/src/components/calendar/UnifiedCalendar.tsx

<interfaces>
Verified duplicate sources (2026-07-04):
- SettingsLayout.tsx ~L80-90 renders the page-level header:
  `t('${navLabelKey(activeSection)}.title', { defaultValue: t('nav.…') })` + the matching
  `.description` — for the profile section this resolves to `t('profile.title')` =
  "Profile Settings" + `t('profile.description')`. THIS ONE STAYS.
- ProfileSettingsSection.tsx ~L121-124 passes the SAME `t('profile.title')` +
  `t('profile.description')` into `SettingsSectionCard`, whose CardHeader renders them again
  (SettingsSectionCard.tsx ~L41-49). THIS DUPLICATE GOES.
- SettingsSectionCardProps currently requires `title: string`; `description` optional.
- calendar.tsx ~L32-39: PageHeader `actions` slot renders Link to /calendar/new with a Button
  labeled `t('form.create_event')`. THIS ONE STAYS (matches the app-wide PageHeader-action
  pattern; the /calendar/new standalone form is live and verified — quick tasks 260603-tuq /
  260604-lmy).
- UnifiedCalendar.tsx ~L224-232: toolbar Button `variant="default"` with
  `onClick={() => setShowCreateForm(true)}` and the same `t('form.create_event')` label.
  THIS DUPLICATE GOES. Note: `showCreateForm` may also be set by the CalendarEmptyWizard
  path (`handleCreateFromTemplate`) — the inline-form machinery itself may still be live.
- UnifiedCalendar has exactly one consumer: routes/_protected/calendar.tsx.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the duplicated settings card header (keep the page-level title)</name>
  <files>frontend/src/components/settings/SettingsSectionCard.tsx, frontend/src/components/settings/sections/ProfileSettingsSection.tsx</files>
  <read_first>
    - .planning/phases/81-visible-bugs/81-CONTEXT.md (D-81-02, D-81-06)
    - frontend/src/components/settings/SettingsLayout.tsx (the page-level header block around navLabelKey — must stay untouched)
    - frontend/src/components/settings/SettingsSectionCard.tsx (CardHeader/CardTitle rendering)
    - frontend/src/components/settings/sections/ProfileSettingsSection.tsx (the SettingsSectionCard usage at ~L121)
    - frontend/src/components/settings/sections/ (list the sibling *SettingsSection.tsx files and check their SettingsSectionCard titles)
  </read_first>
  <action>
    Per D-81-02 — keep exactly one title; remove the inner card header:

    1. In SettingsSectionCard.tsx, make `title` (and keep `description`) optional in
       SettingsSectionCardProps, and render the CardHeader block only when a non-empty title
       is provided. When no title is given the Card renders CardContent only. Keep the
       explicit return type / ESLint rules intact (no `any`, explicit-function-return-type).
    2. In ProfileSettingsSection.tsx, stop passing `title={t('profile.title')}`,
       `description={t('profile.description')}`, and the `icon={User}` header icon into
       SettingsSectionCard (drop the now-orphaned `User` import if nothing else uses it —
       orphans created by this change must be cleaned, per repo lint no-unused-vars).
    3. Root-cause sweep (decision text: "Section cards must not repeat the page title"):
       grep the sibling section components under frontend/src/components/settings/sections/
       for SettingsSectionCard usages whose `title` is the SAME `<section>.title` i18n key
       the SettingsLayout page-level header shows for that section (compare against
       navLabelKey in SettingsLayout.tsx). Apply the identical removal to each verbatim
       duplicate. Cards whose title is a DIFFERENT sub-group string (not the page-level
       section title) are NOT duplicates — leave them alone. Do not touch SettingsLayout.tsx.

  </action>
  <verify>
    <automated>grep -c "profile.title" frontend/src/components/settings/sections/ProfileSettingsSection.tsx | grep -qx 0 && grep -c "navLabelKey" frontend/src/components/settings/SettingsLayout.tsx | grep -qvx 0 && cd frontend && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - ProfileSettingsSection.tsx no longer references `profile.title` or `profile.description`
    - SettingsLayout.tsx page-level header is unmodified (still renders the section title + description)
    - SettingsSectionCard renders no empty CardHeader when title is omitted
    - No sibling section card renders a title string identical to its page-level section title
    - Frontend type-check passes; no unused imports left behind
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Remove the redundant calendar toolbar create button (keep the PageHeader action)</name>
  <files>frontend/src/components/calendar/UnifiedCalendar.tsx, frontend/src/routes/_protected/calendar.tsx</files>
  <read_first>
    - .planning/phases/81-visible-bugs/81-CONTEXT.md (D-81-03 + Claude's Discretion: which button to keep)
    - frontend/src/routes/_protected/calendar.tsx (PageHeader action — the keeper)
    - frontend/src/components/calendar/UnifiedCalendar.tsx (whole file — toolbar button, showCreateForm state, CalendarEmptyWizard/handleCreateFromTemplate path, the inline create form render)
  </read_first>
  <action>
    Per D-81-03 — single primary create-event action. Decision within discretion: KEEP the
    PageHeader action in routes/_protected/calendar.tsx (Link to /calendar/new — matches the
    app-wide PageHeader-primary-action pattern and the verified standalone form), REMOVE the
    toolbar Button in UnifiedCalendar.tsx (~L224-232, the `variant="default"` button calling
    setShowCreateForm(true) with label t('form.create_event')).

    Before deleting, trace every setter/reader of `showCreateForm` in UnifiedCalendar.tsx:
    - If CalendarEmptyWizard's handleCreateFromTemplate (or any other path) still sets it,
      keep the state + inline form render — remove ONLY the toolbar button.
    - If the toolbar button was the sole setter, also remove the now-dead state, the inline
      form render block it gated, and any imports orphaned by the removal (e.g. the Plus
      icon) — but only orphans created by THIS change.
    Leave the Today button, month navigation, and the type-filter Select untouched.
    routes/_protected/calendar.tsx should need no edit (verify the keeper is still the only
    create action on the route).

  </action>
  <verify>
    <automated>grep -c "form.create_event" frontend/src/components/calendar/UnifiedCalendar.tsx | grep -qx 0 && grep -c "form.create_event" frontend/src/routes/_protected/calendar.tsx | grep -qx 1 && cd frontend && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - UnifiedCalendar.tsx contains zero `form.create_event` renders; calendar.tsx contains exactly one
    - The CalendarEmptyWizard empty-state flow still compiles and works (its create path preserved if it consumed showCreateForm)
    - Today button, month prev/next, and type filter unchanged
    - Frontend type-check passes; no unused imports/state left behind
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Live visual verification of /settings and /calendar (1400 + 1024, EN+AR, dark+light)</name>
  <files>frontend/src/components/settings/sections/ProfileSettingsSection.tsx</files>
  <read_first>
    - CLAUDE.md "Definition of Done — UI checklist" + "Browser Automation"
    - .planning/phases/81-visible-bugs/81-CONTEXT.md (Specifics: verify at real analyst widths)
  </read_first>
  <action>
    With the dev server running, use the browser harness / agent-browser at 1400×900 and
    1024×768, EN/LTR + AR/RTL, dark + light:
    - /settings (Profile section active): count elements containing the "Profile Settings"
      title text (and its AR equivalent) — must be exactly 1; the description line appears
      exactly once; the card body (avatar, display name, etc.) renders intact directly under
      the page header. Click through 2-3 other sections (General, Appearance) and confirm no
      section shows its title twice.
    - /calendar: count visible create-event buttons — exactly 1 (the PageHeader action);
      clicking it lands on /calendar/new; the calendar toolbar still shows Today + month nav +
      type filter; the empty-state wizard (if the calendar is empty) still offers its CTA.
    Capture screenshots as evidence. Run the settings component tests if present.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/components/settings --reporter=basic 2>&1 | tail -3</automated>
    <human-check>Screenshots: exactly one Profile Settings title on /settings, exactly one create-event button on /calendar, EN+AR</human-check>
  </verify>
  <acceptance_criteria>
    - Settings page renders exactly one element with the Profile Settings title (EN and AR)
    - Calendar view renders exactly one primary create-event button (EN and AR)
    - No layout regression in the settings card body or calendar toolbar at 1400 and 1024
    - Existing settings component tests pass
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                |
| -------- | -------------------------------------------------------------------------- |
| none new | Element removals only; no input handling, data flow, or dependency changes |

## STRIDE Threat Register

| Threat ID | Category    | Component            | Disposition | Mitigation Plan                                                                                 |
| --------- | ----------- | -------------------- | ----------- | ----------------------------------------------------------------------------------------------- |
| T-81-03   | Elevation   | settings save path   | accept      | no auth/persistence logic touched — header render removal only                                  |
| T-81-04   | Repudiation | calendar create flow | accept      | surviving create path is the already-verified /calendar/new form; no package installs this plan |

</threat_model>

<verification>
- /settings shows exactly one "Profile Settings" title + one description (EN + AR, dark + light, 1400 + 1024)
- /calendar shows exactly one primary create-event action; it reaches the working /calendar/new form
- `pnpm exec tsc --noEmit` clean; settings vitest suite green
</verification>

<success_criteria>

- BUG-02 closed per D-81-02 (inner card header removed, page-level title kept)
- BUG-03 closed per D-81-03 (single primary create-event action)
- Zero regressions to save flow, empty-state wizard, or section navigation
  </success_criteria>

<output>
Create `.planning/phases/81-visible-bugs/81-02-SUMMARY.md` when done
</output>
