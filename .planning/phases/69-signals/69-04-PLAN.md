---
phase: 69-signals
plan: 04
type: execute
wave: 4
depends_on:
  - '69-03'
files_modified:
  - frontend/src/components/signals/EscalateSignalDialog.tsx
  - frontend/src/components/dossier/tabs/DossierSignalsTab.tsx
  - frontend/src/routes/_protected/intelligence.tsx
autonomous: false
requirements:
  - SIGNAL-03
  - SIGNAL-04
  - SIGNAL-05
  - SIGNAL-06

must_haves:
  truths:
    - 'EscalateSignalDialog pre-fills title/body from signal, priority from signal.severity (1:1 map), calls useSignalEscalate 3-step sequence'
    - "After escalation, toast shows 'Signal escalated. Work item created.' — signal moves to Escalated filter"
    - 'DossierSignalsTab passes dossierId to SignalsQueue; all 8 dossier-type route files get a Signals tab'
    - 'read_signals RPC verified on staging: p_dossier_id filters to one dossier, source_type::text cast works (no 42804), clearance gate returns empty for above-clearance signals'
    - 'Keyboard-only triage verified in Arabic mode (j/k/a/d/e, dir=rtl, Tajawal font)'
    - 'L1 account does not see L3 signal (indistinguishable empty) — verified via CDP forced-error protocol'
    - 'Escalated task visible in /kanban (todo column) + work_item_dossiers copied'
  artifacts:
    - path: 'frontend/src/components/signals/EscalateSignalDialog.tsx'
      provides: 'Compact escalate dialog pre-filled from signal, calls useSignalEscalate'
      exports: ['EscalateSignalDialog']
    - path: 'frontend/src/components/dossier/tabs/DossierSignalsTab.tsx'
      provides: 'Thin wrapper passing dossierId to SignalsQueue'
      exports: ['DossierSignalsTab']
  key_links:
    - from: 'EscalateSignalDialog.tsx'
      to: 'useSignalEscalate'
      via: 'signalEscalate.mutateAsync(input)'
      pattern: 'useSignalEscalate'
    - from: 'DossierSignalsTab.tsx'
      to: 'SignalsQueue'
      via: 'dossierId prop → useSignals filter'
      pattern: 'SignalsQueue.*dossierId'
    - from: 'SignalsQueue.tsx'
      to: 'EscalateSignalDialog'
      via: 'escalateTarget state → dialog open'
      pattern: 'EscalateSignalDialog'
---

<objective>
Wave 4 — Escalate Dialog, Dossier Tab Wiring, read_signals Direct Validation, and Phase Gate UAT.

Complete the last UI components (EscalateSignalDialog, DossierSignalsTab) and wire
them into the surfaces (SignalsQueue receives EscalateSignalDialog; dossier route
tabs get DossierSignalsTab). Then validate all 6 SIGNAL requirements live on staging:
directly invoke read_signals to verify the 42804-safe source_type::text cast, the
clearance gate, and the per-dossier filter; manually verify keyboard triage in Arabic
mode; verify escalate → Kanban task round-trip.

Purpose: This is the phase gate plan — it wires the last component and verifies all
success criteria that require live staging, seed data, and human observation.

Output: EscalateSignalDialog wired; DossierSignalsTab in all dossier routes; all 6
SIGNAL UAT scenarios verified and documented in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-signals/69-RESEARCH.md
@.planning/phases/69-signals/69-PATTERNS.md
@.planning/phases/69-signals/69-UI-SPEC.md
@.planning/phases/69-signals/69-VALIDATION.md
@.planning/phases/69-signals/69-03-SUMMARY.md

<interfaces>
<!-- Extracted from codebase — executor reads before touching any file. -->

From frontend/src/components/waiting-queue/EscalationDialog.tsx (the exact-copy base):
-- Props pattern (lines 43–80): { assignmentId?: string; isOpen: boolean; onClose: () => void; onSuccess?: () => void; isLoading?: boolean }
-- RTL shell (lines 188–207): <Dialog open={isOpen} onOpenChange={onClose}>
<DialogContent className="ps-4 pe-4 sm:ps-6 sm:pe-6 max-w-md sm:max-w-lg" dir={isRTL ? 'rtl' : 'ltr'}>
-- Footer (lines 335–369): <DialogFooter className="flex flex-col-reverse sm:flex-row gap-2 sm:gap-3">
Cancel: className="h-11 min-w-11 ps-4 pe-4 w-full sm:w-auto", label t('dialog.cancelEscalation')
Confirm: disabled={isLoading || !title.trim()}, label t('actions.escalate')
-- import { useToast } from '@/hooks/use-toast'
-- import { Loader2 } from 'lucide-react'

From frontend/src/components/positions/DossierPositionsTab.tsx (lines 27–57):
-- Props: { dossierId: string }
-- Pattern: const { t } = useTranslation('intelligence-signals')
-- Thin wrapper that delegates to SignalsQueue with the dossierId prop

From frontend/src/routes/\_protected/\*.tsx (dossier route files):
-- Pattern: existing tabs export array including { label, component: lazy(() => ...) }
-- DossierSignalsTab must be added to each of the 8 dossier-type route surfaces (7 under dossiers/\* + engagements/$engagementId)
-- Check: grep -rn "DossierPositionsTab\|DossierWorkItemsTab" frontend/src/routes/ to find all tab wiring locations

From frontend/src/domains/signals/hooks/useSignalEscalate.ts (Wave 2 output):
-- useSignalEscalate(): UseMutationResult<{ taskId: string }, Error, EscalateSignalInput>
-- EscalateSignalInput.priority maps from SignalSeverity (same values: low/medium/high/urgent)

From frontend/src/i18n/en/intelligence-signals.json (Wave 0 output):
-- t('dialog.escalateTitle') = "Escalate signal"
-- t('dialog.escalateDesc') = "Create a work item from this signal"
-- t('dialog.cancelEscalation') = "Cancel escalation"
-- t('actions.escalate') = "Escalate"
-- t('toast.escalateSuccess') = "Signal escalated. Work item created."
-- t('actions.viewWorkItem') = "View work item"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EscalateSignalDialog + wire into SignalsQueue</name>
  <files>
    frontend/src/components/signals/EscalateSignalDialog.tsx,
    frontend/src/components/signals/SignalsQueue.tsx
  </files>

<read_first> - .planning/phases/69-signals/69-PATTERNS.md — "EscalateSignalDialog.tsx" section — exact DELTAS vs EscalationDialog.tsx (which fields to remove, which to add, pre-fill map) - .planning/phases/69-signals/69-UI-SPEC.md — "EscalateSignalDialog" component spec + "Escalation Dialog — Field Pre-fill Map" table + "Escalate → Work Item" interaction sequence - frontend/src/components/waiting-queue/EscalationDialog.tsx — READ FULLY as the copy base; extract the Dialog shell, RTL pattern, footer pattern, Loader2 usage, useToast import - .planning/phases/69-signals/69-RESEARCH.md — RF-4 escalation sequence (Step 2: work-item-dossiers assignee_id is REQUIRED in tasks-create, dialog must collect it) - frontend/src/domains/signals/hooks/useSignalEscalate.ts — EscalateSignalInput shape, what params are needed
</read_first>

  <action>
    Create `frontend/src/components/signals/EscalateSignalDialog.tsx` based on EscalationDialog.tsx:

    Props:
    { signal: Signal | null; isOpen: boolean; onClose: () => void; onSuccess?: () => void }

    Remove from EscalationDialog.tsx (do NOT port):
    - recipient select / organizational hierarchy fetch
    - days-waiting badge
    - useEscalationAction hook
    - reason field (D-10: compact dialog, no reason)

    Add (specific to this dialog):
    - const { t } = useTranslation('intelligence-signals')
    - Pre-filled state from signal prop (useEffect to reset when signal changes):
      - title: useState(signal?.title ?? '')
      - body: useState(signal?.content ?? '')
      - priority: useState<SignalSeverity>(signal?.severity ?? 'medium')
      - assigneeId: useState<string>('')  ← required by tasks-create; user must provide
      - slaDeadline: useState<string>('')
    - Form fields:
      1. Title input (editable, pre-filled, required): text input
      2. Body textarea (editable, pre-filled, 500 char limit, resize:none, min-h-24)
         char counter: <span className="text-xs text-ink-mute">{body.length}/500</span>
      3. Priority select (editable, pre-filled from signal.severity): low/medium/high/urgent
         Note: same values as SignalSeverity — direct pass-through (1:1 map, D-11)
      4. Assignee select or text input (optional but validated — tasks-create requires assignee_id)
         If empty: show inline warning "Assignee required" but do not block from submitting;
         fall back to current user's id from useAuth if available
      5. Deadline date input (optional): type="date"

    Submit handler calls: signalEscalate.mutateAsync({
      signalId: signal.id, title, body, priority, assigneeId (use current user if empty),
      slaDeadline: slaDeadline || undefined,
      dossierIds: [list of signal's dossier IDs — need to fetch or pass in props]
    })

    NOTE on dossier IDs for escalation: The signal's dossier links live in intelligence_event_dossiers.
    The SignalsQueue must pass the signal's dossierIds when opening the dialog. Extend Signal type or
    pass dossierIds as a separate prop to EscalateSignalDialog. Simplest: add optional dossierIds?: string[]
    to Signal type (not in DB, fetched separately), OR pass dossierIds prop to dialog:
    { signal: Signal | null; dossierIds?: string[]; isOpen: boolean; ... }
    The executor decides the cleanest approach — the PATTERNS.md recommends a separate prop.

    On success:
    - toast({ description: t('toast.escalateSuccess') })
    - onClose()
    - onSuccess?.()

    Dialog shell — copy VERBATIM from EscalationDialog.tsx:
    - dir={isRTL ? 'rtl' : 'ltr'} on DialogContent
    - className="ps-4 pe-4 sm:ps-6 sm:pe-6 max-w-md sm:max-w-lg"
    - DialogFooter className="flex flex-col-reverse sm:flex-row gap-2 sm:gap-3"
    - Cancel button: disabled={isLoading}, label t('dialog.cancelEscalation')  ← NOT bare "Cancel"
    - Confirm button: disabled={isLoading || !title.trim()}, label t('actions.escalate')
    - Loader2 spinner when isLoading

    Then modify `frontend/src/components/signals/SignalsQueue.tsx` to wire the dialog:
    - Add import EscalateSignalDialog from './EscalateSignalDialog'
    - Replace the placeholder from Wave 3 with actual:
      {escalateTarget && <EscalateSignalDialog
        signal={escalateTarget}
        isOpen={escalateTarget !== null}
        onClose={() => setEscalateTarget(null)}
        onSuccess={() => setEscalateTarget(null)}
      />}
    - Note: useSignalEscalate Step 3 already sets status='escalated' on the signal (RF-4).
      The onSuccess callback only closes the dialog — do NOT call updateStatus separately.

  </action>

  <verify>
    <automated>pnpm --filter frontend typecheck 2>&1 | tail -5 — exit 0</automated>
    <automated>grep -c "dir={isRTL" frontend/src/components/signals/EscalateSignalDialog.tsx — must be >= 1</automated>
    <automated>grep -c "cancelEscalation" frontend/src/components/signals/EscalateSignalDialog.tsx — must be >= 1 (not bare "Cancel")</automated>
    <automated>grep -c "EscalateSignalDialog" frontend/src/components/signals/SignalsQueue.tsx — must be >= 2 (import + render)</automated>
    <automated>pnpm --filter frontend lint 2>&1 | tail -5 — exit 0</automated>
  </verify>

<acceptance_criteria> - EscalateSignalDialog props accept signal (pre-fill source) and isOpen/onClose - title and body pre-filled from signal.title and signal.content respectively - priority pre-filled from signal.severity (identical values, 1:1 mapping per RF-4) - Cancel label = "Cancel escalation" / "إلغاء التصعيد" (NOT bare "Cancel") - dir={isRTL ? 'rtl' : 'ltr'} on DialogContent - Confirm disabled when title is empty - On success: toast with t('toast.escalateSuccess') fires - SignalsQueue: escalateTarget state drives EscalateSignalDialog open/close - pnpm --filter frontend typecheck + lint exit 0
</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: DossierSignalsTab and per-dossier route wiring</name>
  <files>
    frontend/src/components/dossier/tabs/DossierSignalsTab.tsx,
    frontend/src/routes/_protected/intelligence.tsx
  </files>

<read_first> - .planning/phases/69-signals/69-PATTERNS.md — "DossierSignalsTab.tsx" section (exact analog: DossierPositionsTab.tsx; thin wrapper, passes dossierId to SignalsQueue) - .planning/phases/69-signals/69-UI-SPEC.md — "DossierSignalsTab" spec (tab label "Signals"/"الإشارات"; placement after existing tabs; one component path D-01) - frontend/src/components/positions/DossierPositionsTab.tsx — READ lines 27–57 (props pattern, useTranslation, useDirection pattern) - frontend/src/components/dossier/tabs/ — list existing tab files to understand naming convention - frontend/src/routes/\_protected/ — run: grep -rn "DossierPositionsTab\|DossierWorkItemsTab\|Signals" frontend/src/routes/\_protected/ to find all dossier tab arrays needing a Signals entry - frontend/src/routes/\_protected/intelligence.tsx — the existing /intelligence route (read fully to understand how SignalsQueue is already mounted there via IntelligencePage.tsx; this file may need no change if IntelligencePage.tsx already handles the route)
</read_first>

  <action>
    Create `frontend/src/components/dossier/tabs/DossierSignalsTab.tsx`:
    Thin wrapper component. Props: { dossierId: string }.
    Body: const { t } = useTranslation('intelligence-signals'); return <SignalsQueue dossierId={dossierId} />;
    This delegates ALL rendering, filtering, and keyboard behavior to SignalsQueue.
    File should be ~20 lines including imports.

    Then wire DossierSignalsTab into EVERY dossier-type route:
    - Run grep to find all files that import DossierWorkItemsTab or DossierDocumentsTab
      (those are the files that need a Signals tab added)
    - Route surfaces (all 8) — create a new signals.tsx file at each:
        frontend/src/routes/_protected/dossiers/countries/$id/signals.tsx
        frontend/src/routes/_protected/dossiers/elected-officials/$id/signals.tsx
        frontend/src/routes/_protected/dossiers/forums/$id/signals.tsx
        frontend/src/routes/_protected/dossiers/organizations/$id/signals.tsx
        frontend/src/routes/_protected/dossiers/persons/$id/signals.tsx
        frontend/src/routes/_protected/dossiers/topics/$id/signals.tsx
        frontend/src/routes/_protected/dossiers/working_groups/$id/signals.tsx
        frontend/src/routes/_protected/engagements/$engagementId/signals.tsx
      Each file follows the EXACT same pattern as the adjacent tasks.tsx file in that directory
      (createFileRoute + Suspense + lazy import of DossierSignalsTab).
    - For each: add the DossierSignalsTab to the tabs array
      after the existing tabs (Engagements, Tasks, Documents, Timeline as applicable)
      Tab entry pattern: { id: 'signals', label: t('tab.label', { ns: 'intelligence-signals' }),
        component: DossierSignalsTab, props: { dossierId } }
      Or if lazy-loaded: lazy(() => import('@/components/dossier/tabs/DossierSignalsTab'))

    For `frontend/src/routes/_protected/intelligence.tsx`:
    - Read this file first; if it just renders <IntelligencePage />, it needs no modification
      (the tab switcher was added to IntelligencePage.tsx in Wave 3 Plan 03 Task 3)
    - Only modify if the route file itself has a tab structure that needs updating
    - If no changes needed, skip this file (leave it as-is)

    The goal: after Wave 4, navigating to any dossier detail page shows a "Signals" tab
    that renders the same SignalsQueue filtered to that dossier's signals.

    IMPORTANT: The DossierSignalsTab file must be added to frontend/src/components/dossier/tabs/ —
    verify the existing files in that directory (DossierWorkItemsTab, DossierDocumentsTab,
    DossierEngagementsTab) for exact naming and export convention to match.

  </action>

  <verify>
    <automated>pnpm --filter frontend typecheck 2>&1 | tail -5 — exit 0</automated>
    <automated>grep -rl "DossierSignalsTab" frontend/src/routes/ | wc -l | xargs -I{} sh -c 'test "{}" -eq 8 && echo PASS || echo FAIL: {} files found, expected 8'</automated>
    <automated>pnpm --filter frontend lint 2>&1 | tail -5 — exit 0</automated>
    <automated>pnpm --filter frontend build 2>&1 | tail -10 — exit 0 (production build passes)</automated>
  </verify>

<acceptance_criteria> - DossierSignalsTab.tsx exists at frontend/src/components/dossier/tabs/DossierSignalsTab.tsx - DossierSignalsTab renders <SignalsQueue dossierId={dossierId} /> and nothing else - DossierSignalsTab added to all 8 dossier-type route surfaces, each with a new signals.tsx route file: dossiers/countries/$id/signals.tsx, dossiers/elected-officials/$id/signals.tsx, dossiers/forums/$id/signals.tsx, dossiers/organizations/$id/signals.tsx, dossiers/persons/$id/signals.tsx, dossiers/topics/$id/signals.tsx, dossiers/working_groups/$id/signals.tsx, engagements/$engagementId/signals.tsx - Tab label uses t('tab.label', { ns: 'intelligence-signals' }) = "Signals" / "الإشارات" - intelligence.tsx unchanged OR modified only to add Signals tab if it had its own tab structure - pnpm --filter frontend typecheck + lint + build exit 0
</acceptance_criteria>
</task>

<task type="checkpoint:human-verify">
  <name>Phase Gate UAT — all 6 SIGNAL requirements on staging (EN + AR)</name>

  <what-built>
    Wave 1–4 complete: migration applied, clearance-keyed RLS live, read_signals INVOKER RPC live,
    i18n registered, types written, hooks (useSignals/useCreateSignal/useUpdateSignalStatus/
    useSignalEscalate) wired, SignalRow/SignalsQueue/CaptureSignalForm/EscalateSignalDialog built,
    IntelligencePage tab switcher added, DossierSignalsTab in all dossier routes.
  </what-built>

  <how-to-verify>
    Seed data instructions (run via Supabase MCP execute_sql on zkrcjzdemdmwhearhfgg):

    A. Seed a manual signal at sensitivity L1:
    INSERT INTO public.intelligence_event (organization_id, source_type, title, content,
      occurred_at, severity, sensitivity_level, category, status, created_by)
    VALUES (
      (SELECT organization_id FROM public.profiles WHERE clearance_level >= 1 LIMIT 1),
      'human_entered', 'Test signal EN/AR triage', 'Signal body for triage verification.',
      now(), 'high', 1, 'security', 'new',
      (SELECT id FROM public.profiles WHERE clearance_level >= 1 LIMIT 1)
    ) RETURNING id;
    -- Then link to a dossier:
    INSERT INTO public.intelligence_event_dossiers (event_id, dossier_type, dossier_id)
    VALUES ('<signal-id-above>', 'country', (SELECT id FROM public.dossiers WHERE type='country' LIMIT 1));

    B. Seed an AI-generated signal at sensitivity L3 via the APPLICATION write path (D-13):
    In the browser console (logged in as an L3 account) or in a test script, call:
      useCreateSignal().mutateAsync({
        title: 'AI-surfaced L3 signal',
        body: 'Above-clearance signal body.',
        severity: 'urgent',
        category: 'political',
        sensitivityLevel: 3,
        dossierIds: [],
        sourceType: 'ai_generated',
        aiConfidence: 0.92
      })
    This exercises the parameterized AI write path delivered in Plan 02 Task 2 (Blocker 3).
    The insert goes through the same clearance-gated RLS as manual signals; ai_confidence=0.92 is persisted.
    Fallback if browser console is inconvenient: use Supabase MCP execute_sql to call the RPC directly
    as a cleared L3 user session — but verify useCreateSignal with ai_generated at least once in the UAT.

    UAT scenarios to verify:

    SIGNAL-01: Open http://localhost:5173/intelligence → Signals tab.
    - The L1 manual signal from Seed A appears in the queue.
    - Click "Capture signal" → fill form → submit → signal appears.
    - Navigate to the linked country dossier's Signals tab → same signal appears.
    PASS condition: Signal visible in global queue and on dossier Signals tab, EN and AR.

    SIGNAL-02: Log in as an L1 account.
    - The L3 AI-generated signal (Seed B) is NOT visible in the queue (indistinguishable empty).
    - Queue shows "No signals." or the L1 signals only — no error, no mention of clearance.
    CDP verification: Network.setBlockedURLs → block the read_signals RPC URL pattern;
    assert no role="alert" appears with clearance text; confirm empty state is generic.
    PASS condition: L3 signal absent; queue shows generic empty state for L1 user.

    SIGNAL-03: With at least one L1 signal in the queue:
    - Switch locale to Arabic (topbar ع button; verify localStorage id.locale='ar' and dir=rtl).
    - Use ONLY keyboard: press j (moves focus to first signal; accent ring visible),
      press k (moves up), press a (acknowledges — signal disappears from New filter),
      restore to New filter, press d (dismisses — toast "تم رفض الإشارة. استعادة؟").
    - Press e → EscalateSignalDialog opens in Arabic.
    PASS condition: All keys work in Arabic mode; dir=rtl; Tajawal font renders; keyboard hints show in Arabic.

    SIGNAL-04: Per-dossier clearance gate:
    - Seed a second L3 signal linked to the same country dossier.
    - Log in as L1 account → navigate to that country's Signals tab.
    - Confirm the L3 signal is absent; L1 signal appears; empty state is generic.
    PASS condition: L3 signal absent on per-dossier tab from L1 account.

    SIGNAL-05: Escalate round-trip:
    - With a signal in queue: press e → EscalateSignalDialog opens pre-filled with signal title/body/priority.
    - Confirm escalation.
    - Signal moves to Escalated filter with "View work item" link.
    - Navigate to /kanban → new task visible in todo column with signal title.
    - Query: SELECT wid.* FROM work_item_dossiers wid JOIN tasks t ON t.id=wid.work_item_id
      WHERE t.title = '<signal-title>' — dossier link rows should be present.
    PASS condition: Task in Kanban; dossier links copied; signal status=escalated; link to task visible.

    SIGNAL-06: read_signals direct invocation:
    - In Supabase SQL editor on staging, as an L1 user (use service_role to impersonate):
      SELECT * FROM public.read_signals(
        p_dossier_id := '<country-dossier-id>',
        p_status := NULL, p_since := NULL, p_limit := 50
      );
    - Confirm: L1 signal returned, L3 signal absent.
    - Confirm: source_type column returns text (not enum type — no 42804 error).
    - Confirm function exists: SELECT security_type FROM information_schema.routines WHERE routine_name='read_signals'; → INVOKER.
    PASS condition: L3 signal absent from result; source_type is plain text; no 42804 error; INVOKER confirmed.

    Cleanup (run after all verifications):
    DELETE FROM public.intelligence_event WHERE title IN ('Test signal EN/AR triage', 'AI-surfaced L3 signal');

  </how-to-verify>

  <resume-signal>
    Type "PASS" if all 6 scenarios above verified, with brief notes on each.
    Type "FAIL: [scenario]: [what was wrong]" if any scenario failed.
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                                                     |
| ---------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| EscalateSignalDialog → useSignalEscalate | assignee_id must be provided; tasks-create returns 400 if missing; dialog pre-fill prevents accidental omission |
| Per-dossier Signals tab → read_signals   | dossierId filter passes through RPC; clearance gate still applies (INVOKER)                                     |
| UAT seed data                            | Seed signals must be cleaned up after verification to avoid polluting staging data                              |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                    | Disposition | Mitigation Plan                                                                                                                                                                                                                                      |
| --------- | ---------------------- | ---------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-69-14   | Elevation of Privilege | EscalateSignalDialog — escalated signal links to task via work_item_dossiers | mitigate    | work_item_dossiers INSERT policy checks tasks.created_by = auth.uid() — the dossier-link copy runs immediately after task creation in useSignalEscalate Step 2, under the same JWT; no window for privilege substitution; verified in Plan 02 Task 3 |
| T-69-15   | Information Disclosure | DossierSignalsTab per-dossier clearance gate                                 | mitigate    | dossierId prop passed to useSignals → read_signals; INVOKER RPC enforces clearance at DB layer; non-cleared user sees generic empty state (indistinguishable from zero-signal dossier); verified in UAT scenario SIGNAL-04                           |
| T-69-16   | Information Disclosure | Escalated signal "View work item" link                                       | accept      | The link shows only the task ID (not signal body/sensitivity); task is a work item in the caller's own workspace — they created it and have access to it; no above-clearance content exposed via the link                                            |
| T-69-17   | Denial of Service      | read_signals p_limit param                                                   | accept      | Default limit 50; maximum not enforced beyond DB query cost; in P69 the signal table has O(10) rows; rate limiting on the Supabase connection pool is the backstop                                                                                   |
| T-69-SC   | Tampering              | npm/pip/cargo installs                                                       | accept      | No new packages this wave                                                                                                                                                                                                                            |

</threat_model>

<verification>
Phase 69 complete when:
1. pnpm --filter frontend typecheck exits 0
2. pnpm --filter frontend lint exits 0 (--max-warnings 0)
3. pnpm --filter frontend build exits 0
4. All 6 UAT scenarios verified on staging zkrcjzdemdmwhearhfgg in both EN and AR
5. Seed data cleaned up from staging after UAT
6. SIGNAL-01..06 all marked verified in 69-VALIDATION.md
</verification>

<success_criteria>

- EscalateSignalDialog: pre-fills title/body/priority from signal; RTL dir; "Cancel escalation" label; toast on success
- DossierSignalsTab: thin wrapper in all 8 dossier-type route surfaces (countries, elected-officials, forums, organizations, persons, topics, working_groups, engagements); tab label from intelligence-signals namespace
- Phase gate UAT (checkpoint:human-verify): analyst manually confirms all 6 SIGNAL scenarios pass on staging EN+AR
- After UAT: 69-VALIDATION.md updated with all rows marked "green"
- pnpm --filter frontend typecheck + lint + build exit 0
  </success_criteria>

<output>
Create `.planning/phases/69-signals/69-04-SUMMARY.md` when done
</output>
