---
phase: 69-signals
plan: 02
type: execute
wave: 2
depends_on:
  - '69-01'
files_modified:
  - frontend/src/domains/signals/hooks/useSignals.ts
  - frontend/src/domains/signals/hooks/useSignalMutations.ts
  - frontend/src/domains/signals/hooks/useSignalEscalate.ts
autonomous: true
requirements:
  - SIGNAL-01
  - SIGNAL-02
  - SIGNAL-05
  - SIGNAL-06

must_haves:
  truths:
    - 'useSignals calls read_signals RPC via supabase.rpc() with all four params, returns Signal[]'
    - 'useCreateSignal inserts into intelligence_event with source_type defaulting to human_entered; accepts optional sourceType=ai_generated + aiConfidence for the D-13 AI write path (same insert path, same clearance RLS)'
    - 'useUpdateSignalStatus patches status directly on intelligence_event under caller JWT'
    - 'useSignalEscalate orchestrates 3 steps sequentially: tasks-create → work-item-dossiers → intelligence_event UPDATE'
    - "work_item_type is always 'task' (const assertion) when calling work-item-dossiers — silently-failing RLS pitfall mitigated"
    - 'Escalation pre-fills priority from signal.severity (direct 1:1 map)'
  artifacts:
    - path: 'frontend/src/domains/signals/hooks/useSignals.ts'
      provides: 'TanStack Query wrapper for read_signals RPC'
      exports: ['useSignals', 'signalKeys']
    - path: 'frontend/src/domains/signals/hooks/useSignalMutations.ts'
      provides: 'useCreateSignal, useUpdateSignalStatus mutations'
      exports: ['useCreateSignal', 'useUpdateSignalStatus']
    - path: 'frontend/src/domains/signals/hooks/useSignalEscalate.ts'
      provides: '3-step escalation orchestrator'
      exports: ['useSignalEscalate']
  key_links:
    - from: 'useSignals.ts'
      to: "supabase.rpc('read_signals')"
      via: 'p_dossier_id / p_status / p_since / p_limit params'
      pattern: 'rpc.*read_signals'
    - from: 'useSignalEscalate.ts'
      to: "supabase.functions.invoke('tasks-create')"
      via: 'caller JWT — supabase client not supabaseAdmin'
      pattern: 'functions.invoke.*tasks-create'
    - from: 'useSignalEscalate.ts'
      to: "supabase.functions.invoke('work-item-dossiers')"
      via: "work_item_type: 'task' (const)"
      pattern: 'work-item-dossiers'
---

<objective>
Wave 2 — Data Hooks: read, create, triage, escalate.

Build the four TanStack Query hooks that provide the complete data API for the
signals surfaces: useSignals (RPC read), useCreateSignal + useUpdateSignalStatus
(CRUD mutations), and useSignalEscalate (3-step escalation orchestrator). All
hooks use the Supabase JS client under the caller's JWT — no supabaseAdmin.

Purpose: The Wave 3 UI components depend on these hooks. Having data-layer hooks
complete and typechecked before UI prevents cascading type errors in Wave 3.

Output: Three hook files in domains/signals/hooks/; all exported from domains/signals/index.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-signals/69-RESEARCH.md
@.planning/phases/69-signals/69-PATTERNS.md
@.planning/phases/69-signals/69-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs — extracted from codebase. -->

From frontend/src/hooks/useCreateWorkItemDossierLinks.ts (lines 44–60, 115–146):
-- supabase.functions.invoke<T>('work-item-dossiers', { body: request }) pattern
-- const { data, error } = await supabase.functions.invoke<CreateWorkItemDossierLinksResponse>('work-item-dossiers', { body: request })
-- if (error) throw new Error(error.message || 'Failed to create dossier links')
-- if (!data) throw new Error('No data returned')
-- workItemType must be 'task' | 'commitment' | 'intake' (RLS gate on work_item_dossiers_insert)

From frontend/src/domains/intake/hooks/useWaitingQueueActions.ts (lines 59–70, 132–145):
-- useMutation pattern: mutationFn, onSuccess with queryClient.invalidateQueries
-- void queryClient.invalidateQueries({ queryKey: [...] }) <- void prefix required by no-floating-promises
-- onError: (error: Error) => { console.error(...); onError?.(error) }

From frontend/src/services/tasks-api.ts (CreateTaskRequest shape, lines 26–50):
-- { title: string; description?: string; priority: 'low'|'medium'|'high'|'urgent';
workflow_stage: 'todo'|'in_progress'|'review'|'done'|'cancelled';
assignee_id: string; <- REQUIRED by tasks-create (not nullable)
sla_deadline?: string;
tenant_id: string; source?: jsonb }
-- tasks-create edge fn validates assignee_id at line 88 — must be provided

From frontend/src/domains/signals/types/signal.types.ts (from Plan 01):
-- Signal, SignalFilters, CreateSignalInput, EscalateSignalInput, UpdateSignalStatusInput
-- signalKeys = { all, lists, list(filters) }
-- SignalSeverity maps 1:1 to task priority: 'low'|'medium'|'high'|'urgent'

From frontend/src/lib/supabase.ts (or wherever supabase client is exported):
-- import { supabase } from '@/lib/supabase'
-- the client automatically passes the user's active JWT session
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useSignals — read_signals RPC wrapper</name>
  <files>frontend/src/domains/signals/hooks/useSignals.ts</files>

<read_first> - .planning/phases/69-signals/69-PATTERNS.md — "useSignals.ts" section (lines showing queryKey factory + useQuery pattern) - .planning/phases/69-signals/69-RESEARCH.md — "Pattern 3: read_signals hook" and RF-5 for the RPC param names - frontend/src/hooks/useCreateWorkItemDossierLinks.ts — lines 67–73 (queryKeys factory pattern) - frontend/src/pages/IntakeQueue.tsx — lines 128–161 (useQuery with filters in queryKey) - frontend/src/domains/signals/types/signal.types.ts — Signal, SignalFilters, signalKeys
</read_first>

  <behavior>
    - useSignals({ dossierId: 'uuid', status: 'new' }) calls supabase.rpc('read_signals', { p_dossier_id: 'uuid', p_status: 'new', p_since: null, p_limit: 50 })
    - useSignals({}) calls with all params null except p_limit: 50
    - useSignals({ limit: 10 }) sends p_limit: 10
    - staleTime is 60_000ms (mirrors IntakeQueue.tsx)
    - On RPC error, throws the error (TanStack Query isError state)
    - Returns UseQueryResult with data typed as Signal[]
  </behavior>

  <action>
    Create `frontend/src/domains/signals/hooks/useSignals.ts`.

    Import from: @tanstack/react-query (useQuery, UseQueryResult), @/lib/supabase (supabase),
    and ../types/signal.types (Signal, SignalFilters, signalKeys).

    Export a single function `useSignals(filters: SignalFilters = {}): UseQueryResult<Signal[]>`.

    Inside: use useQuery with:
    - queryKey: signalKeys.list(filters)
    - queryFn: async (): Promise<Signal[]> => { const { data, error } = await supabase.rpc('read_signals', {
        p_dossier_id: filters.dossierId ?? null,
        p_status: filters.status ?? null,
        p_since: filters.since ?? null,
        p_limit: filters.limit ?? 50
      }); if (error) throw error; return (data as Signal[]) ?? [] }
    - staleTime: 60_000

    The explicit return type `Promise<Signal[]>` on queryFn is required by
    @typescript-eslint/explicit-function-return-type.

    Do NOT call this hook with supabaseAdmin. The supabase client from @/lib/supabase
    automatically passes the user's JWT session — RLS clearance gating is automatic.

  </action>

  <verify>
    <automated>pnpm --filter frontend typecheck 2>&1 | tail -5 — exit 0</automated>
    <automated>pnpm --filter frontend lint 2>&1 | tail -5 — exit 0</automated>
  </verify>

<acceptance_criteria> - useSignals exports with correct UseQueryResult<Signal[]> return type - queryKey uses signalKeys.list(filters) — queries with different filters are cached separately - p_dossier_id is null when dossierId is undefined (global queue mode) - pnpm --filter frontend typecheck exits 0 - No supabaseAdmin import
</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useSignalMutations — create and status-update</name>
  <files>frontend/src/domains/signals/hooks/useSignalMutations.ts</files>

<read_first> - .planning/phases/69-signals/69-PATTERNS.md — "useSignalMutations.ts" section (lines showing useMutation pattern for 3 mutations) - .planning/phases/69-signals/69-RESEARCH.md — "Pattern 2: Signal Capture" section and "Pitfall 5: Junction dossier_type" - frontend/src/domains/intake/hooks/useWaitingQueueActions.ts — lines 59–70 (useMutation factory with queryClient.invalidateQueries pattern) - frontend/src/domains/signals/types/signal.types.ts — CreateSignalInput, UpdateSignalStatusInput, Signal - frontend/src/hooks/useCreateWorkItemDossierLinks.ts — error handling pattern (console.error + throw)
</read_first>

  <behavior>
    - useCreateSignal().mutateAsync({ title, body, severity, category, sensitivityLevel, dossierIds, dossierTypes })
      → inserts into intelligence_event with source_type='human_entered', ai_confidence=null, status='new', created_by=userId
      → then inserts intelligence_event_dossiers rows (one per dossier_id, each with correct dossier_type from dossierTypes map)
      → invalidates signalKeys.lists()
    - useCreateSignal().mutateAsync({ title, body, severity, category, sensitivityLevel, dossierIds, sourceType: 'ai_generated', aiConfidence: 0.87 })
      → inserts with source_type='ai_generated', ai_confidence=0.87 (D-13 AI write path — same RLS, same insert path)
    - useUpdateSignalStatus().mutateAsync({ id: 'signal-uuid', status: 'acknowledged' })
      → supabase.from('intelligence_event').update({ status: 'acknowledged' }).eq('id', 'signal-uuid')
      → invalidates signalKeys.lists()
    - useUpdateSignalStatus().mutateAsync({ id: 'dismissed-uuid', status: 'acknowledged' }) — restore path works (no hard delete, just status change)
    - Error is console.error'd and re-thrown (not swallowed)
  </behavior>

  <action>
    Create `frontend/src/domains/signals/hooks/useSignalMutations.ts`.

    Import from: @tanstack/react-query (useMutation, useQueryClient, UseMutationResult),
    @/lib/supabase (supabase), ../types/signal.types (CreateSignalInput, UpdateSignalStatusInput, Signal, signalKeys).
    Also import useAuth or equivalent to get the current userId and organization_id — check how
    IntakeQueue.tsx or useWaitingQueueActions.ts obtains the org/user context.

    Export `useCreateSignal(): UseMutationResult<Signal, Error, CreateSignalInput>`:
    - mutationFn inserts to intelligence_event:
      { organization_id, source_type: input.sourceType ?? 'human_entered',
        ai_confidence: input.sourceType === 'ai_generated' ? (input.aiConfidence ?? null) : null,
        title: input.title, content: input.body, occurred_at: new Date().toISOString(),
        severity: input.severity, sensitivity_level: input.sensitivityLevel,
        category: input.category, status: 'new', created_by: userId }
      The source_type defaults to 'human_entered' when sourceType is omitted (CaptureSignalForm path).
      Passing sourceType='ai_generated' with aiConfidence enables the D-13 AI write path — same
      RLS policy (clearance-gated INSERT), no separate code branch needed.
    - .select('id').single() to get the new id
    - If dossierIds.length > 0: insert junction rows into intelligence_event_dossiers:
      dossierIds.map(did => ({ event_id: ie.id, dossier_type: input.dossierTypes?.[did] ?? 'topic',
      dossier_id: did }))
      NOTE: dossier_type MUST match the live dossiers.type value for each dossier — executor must
      document that the caller is responsible for passing correct dossierTypes. The junction CHECK
      covers: country,organization,forum,engagement,topic,working_group,person only.
    - On error: throw (never swallow)
    - onSuccess: void queryClient.invalidateQueries({ queryKey: signalKeys.lists() })

    Export `useUpdateSignalStatus(): UseMutationResult<void, Error, UpdateSignalStatusInput>`:
    - mutationFn: supabase.from('intelligence_event').update({ status: input.status }).eq('id', input.id)
    - Handle error (throw if error present)
    - onSuccess: void queryClient.invalidateQueries({ queryKey: signalKeys.lists() })

    This covers: acknowledge (status→acknowledged), dismiss (status→dismissed), restore (status→acknowledged),
    and the third step of escalation (status→escalated) — all via the same mutation.
    The UPDATE RLS policy allows this under the caller's JWT if the signal is within their clearance.

  </action>

  <verify>
    <automated>pnpm --filter frontend typecheck 2>&1 | tail -5 — exit 0</automated>
    <automated>pnpm --filter frontend lint 2>&1 | tail -5 — exit 0</automated>
    <automated>grep -c "input\.sourceType" frontend/src/domains/signals/hooks/useSignalMutations.ts — must be >= 1 (parameterized source_type present)</automated>
    <automated>grep -c "ai_confidence" frontend/src/domains/signals/hooks/useSignalMutations.ts — must be >= 1 (ai_confidence field wired)</automated>
  </verify>

<acceptance_criteria> - useCreateSignal and useUpdateSignalStatus exported with correct generics - source_type defaults to 'human_entered' when sourceType is omitted (manual capture path unchanged); caller may pass sourceType='ai_generated' + aiConfidence for the D-13 AI write path - ai_confidence is null when source_type is 'human_entered'; non-null when 'ai_generated' - Same insert path, same RLS enforcement for both source types — no separate code branch - status update mutation handles 'new'/'acknowledged'/'dismissed'/'escalated' (the full SignalStatus union) - No supabaseAdmin import anywhere in the file - void prefix on all queryClient.invalidateQueries calls - pnpm --filter frontend typecheck + lint exit 0
</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: useSignalEscalate — 3-step escalation orchestrator</name>
  <files>
    frontend/src/domains/signals/hooks/useSignalEscalate.ts,
    frontend/src/domains/signals/index.ts
  </files>

<read_first> - .planning/phases/69-signals/69-RESEARCH.md — "RF-4: Escalate Write Path" section — the full 3-step sequence and the critical tasks-create assignee_id requirement (line 88 in edge fn) - .planning/phases/69-signals/69-PATTERNS.md — "useSignalEscalate.ts" section — the multi-step orchestration pattern - frontend/src/hooks/useCreateWorkItemDossierLinks.ts — lines 44–60 (supabase.functions.invoke pattern) and lines 115–146 (onSuccess multi-step with queryKey invalidations) - frontend/src/services/tasks-api.ts — CreateTaskRequest shape (assignee_id is REQUIRED, workflow_stage must be 'todo') - frontend/src/domains/signals/types/signal.types.ts — EscalateSignalInput
</read_first>

  <behavior>
    - useSignalEscalate().mutateAsync({ signalId, title, body, priority, assigneeId, slaDeadline?, dossierIds, dossierTypes? })
      Step 1: supabase.functions.invoke('tasks-create', { body: { title, description: body,
        priority, workflow_stage: 'todo', assignee_id: assigneeId, sla_deadline: slaDeadline } })
        → returns { task: { id: string, ... } }
      Step 2: supabase.functions.invoke('work-item-dossiers', { body: { work_item_type: 'task',
        work_item_id: task.id, dossier_ids: dossierIds, inheritance_source: 'direct' } })
        → if dossierIds.length > 0
      Step 3: supabase.from('intelligence_event').update({ status: 'escalated', escalated_task_id: task.id })
        .eq('id', signalId)
    - If Step 1 fails: throw (do not proceed to steps 2/3)
    - If Step 2 fails: log warning but do NOT rollback — task already created; continue to Step 3
    - If Step 3 fails: throw — escalated_task_id linkage is part of the contract
    - onSuccess: invalidate signalKeys.lists()
    - work_item_type MUST be the literal 'task' (use `const workItemType = 'task' as const`)
      — RLS on work_item_dossiers gates on IN('task','commitment','intake'); other values silently fail
  </behavior>

  <action>
    Create `frontend/src/domains/signals/hooks/useSignalEscalate.ts`.

    Import from: @tanstack/react-query (useMutation, useQueryClient, UseMutationResult),
    @/lib/supabase (supabase), ../types/signal.types (EscalateSignalInput, signalKeys).

    Export `useSignalEscalate(): UseMutationResult<{ taskId: string }, Error, EscalateSignalInput>`.

    The mutationFn must be typed with explicit return type: Promise<{ taskId: string }>.

    Implementation:
    1. const workItemType = 'task' as const  ← compile-time assertion; never pass a variable here
    2. Step 1 — task create:
       const { data: taskData, error: taskError } = await supabase.functions.invoke<{ task: { id: string } }>(
         'tasks-create', { body: { title: input.title, description: input.body,
           priority: input.priority, workflow_stage: 'todo', assignee_id: input.assigneeId,
           ...(input.slaDeadline ? { sla_deadline: input.slaDeadline } : {}) } })
       if (taskError) throw new Error(taskError.message || 'Task creation failed')
       if (!taskData?.task?.id) throw new Error('No task ID returned')
    3. Step 2 — dossier links (only if dossierIds.length > 0):
       const { error: linkError } = await supabase.functions.invoke(
         'work-item-dossiers', { body: { work_item_type: workItemType,
           work_item_id: taskData.task.id, dossier_ids: input.dossierIds,
           inheritance_source: 'direct' } })
       if (linkError) console.warn('Dossier link copy failed (task created):', linkError.message)
    4. Step 3 — update signal status:
       const { error: updateError } = await supabase
         .from('intelligence_event')
         .update({ status: 'escalated', escalated_task_id: taskData.task.id })
         .eq('id', input.signalId)
       if (updateError) throw new Error(updateError.message || 'Signal status update failed')
    5. return { taskId: taskData.task.id }

    onSuccess: void queryClient.invalidateQueries({ queryKey: signalKeys.lists() })
    onError: (error: Error) => console.error('Escalation failed:', error.message)

    Then update `frontend/src/domains/signals/index.ts` to also re-export from all three hook files:
    export * from './hooks/useSignals'
    export * from './hooks/useSignalMutations'
    export * from './hooks/useSignalEscalate'
    export * from './types/signal.types'

  </action>

  <verify>
    <automated>pnpm --filter frontend typecheck 2>&1 | tail -5 — exit 0</automated>
    <automated>grep -c "as const" frontend/src/domains/signals/hooks/useSignalEscalate.ts — must be >= 1 (workItemType assertion)</automated>
    <automated>grep -c "tasks-create" frontend/src/domains/signals/hooks/useSignalEscalate.ts — must be >= 1</automated>
    <automated>grep -c "work-item-dossiers" frontend/src/domains/signals/hooks/useSignalEscalate.ts — must be >= 1</automated>
  </verify>

<acceptance_criteria> - 3-step sequence present: tasks-create → work-item-dossiers → intelligence_event UPDATE - workItemType = 'task' as const (not a variable string; compile-time asserted) - assignee_id passed to tasks-create (REQUIRED field per RF-4 edge fn line 88) - workflow_stage hard-coded to 'todo' (D-11 — escalated signals become Kanban todos) - Step 2 failure is a warning not a throw (task already exists; partial dossier link failure is recoverable) - Step 3 failure throws (the escalated_task_id bidirectional link is the contract) - signalKeys.lists() invalidated on success - domains/signals/index.ts re-exports all three hooks + types - pnpm --filter frontend typecheck + lint exit 0
</acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                               |
| --------------------------------------- | ------------------------------------------------------------------------- |
| Frontend hook → Supabase client         | Must use supabase (caller JWT) never supabaseAdmin for signal operations  |
| useSignalEscalate step 2 work_item_type | Hardcoded 'task' prevents silent RLS failure on work_item_dossiers INSERT |
| tasks-create assignee_id                | Required by edge fn; missing value causes 400 — dialog must pre-fill it   |

## STRIDE Threat Register

| Threat ID | Category               | Component                               | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                |
| --------- | ---------------------- | --------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-69-06   | Elevation of Privilege | useCreateSignal mutation                | mitigate    | source_type defaults to 'human_entered' for CaptureSignalForm; ai_generated path callable only by passing sourceType explicitly (internal/agent use); sensitivityLevel validated by DB CHECK (1-4); DB INSERT policy enforces clearance gate on both paths — implemented in Task 2                                             |
| T-69-07   | Elevation of Privilege | useSignalEscalate step 1 (tasks-create) | mitigate    | tasks-create edge fn runs under caller JWT; tasks INSERT RLS allows any authenticated user (verified RF-4); no privilege escalation. dossier-link copy under same JWT — work_item_dossiers INSERT policy gates on tasks.created_by = auth.uid() (must run immediately after create, which this hook does) — implemented Task 3 |
| T-69-08   | Information Disclosure | useSignals (read_signals RPC)           | mitigate    | read_signals is SECURITY INVOKER — clearance gate inherited from Plan 01 intelligence_event_select_clearance policy; non-cleared callers receive empty result indistinguishable from zero-signal state — per D-09                                                                                                              |
| T-69-09   | Injection              | useCreateSignal body/title inputs       | mitigate    | Supabase JS client uses parameterized queries; no raw SQL concatenation; title/body stored as-is (D-09 free text); DB CHECKs on category/status/sensitivity_level enforce valid values                                                                                                                                         |
| T-69-SC   | Tampering              | npm/pip/cargo installs                  | accept      | No new packages installed this wave; all imports are pre-existing in repo                                                                                                                                                                                                                                                      |

</threat_model>

<verification>
Wave 2 complete when:
1. pnpm --filter frontend typecheck exits 0 (all three hook files type-correct)
2. pnpm --filter frontend lint exits 0
3. grep -c "supabaseAdmin" frontend/src/domains/signals/hooks/*.ts returns 0 (no admin client use)
4. grep -c "as const" frontend/src/domains/signals/hooks/useSignalEscalate.ts >= 1 (workItemType assertion present)
5. domains/signals/index.ts re-exports all hooks and types
</verification>

<success_criteria>

- useSignals wraps read_signals RPC with correct TanStack Query pattern and staleTime 60_000
- useCreateSignal inserts to intelligence_event then junction — invalidates signalKeys.lists()
- useUpdateSignalStatus patches status under caller JWT — covers all 4 status values including restore
- useSignalEscalate runs 3 sequential steps under caller JWT; workItemType = 'task' as const; Step 3 failure throws
- No supabaseAdmin used in any hook
- All TypeScript explicit return types present (explicit-function-return-type rule)
- pnpm --filter frontend typecheck + lint exit 0
  </success_criteria>

<output>
Create `.planning/phases/69-signals/69-02-SUMMARY.md` when done
</output>
