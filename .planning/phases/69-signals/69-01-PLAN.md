---
phase: 69-signals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260614_phase69_signals_extend.sql
  - frontend/src/i18n/en/intelligence-signals.json
  - frontend/src/i18n/ar/intelligence-signals.json
  - frontend/src/i18n/index.ts
  - frontend/src/domains/signals/types/signal.types.ts
autonomous: true
requirements:
  - SIGNAL-01
  - SIGNAL-02
  - SIGNAL-03
  - SIGNAL-04
  - SIGNAL-05
  - SIGNAL-06

must_haves:
  truths:
    - 'Migration file exists and is applied to staging (new columns, clearance-keyed RLS, read_signals RPC all live)'
    - 'intelligence-signals i18n namespace is registered in both resources.en and resources.ar in i18n/index.ts'
    - 'signal.types.ts exports Signal, SignalStatus, SignalCategory, SignalSeverity, SignalSourceType, SignalFilters, CreateSignalInput'
    - 'Arabic translations for all status values are present: جديدة / مُستلمة / مُهملة / مُصعَّدة'
  artifacts:
    - path: 'supabase/migrations/20260614_phase69_signals_extend.sql'
      provides: 'Intelligence event extension + RLS rewrite + read_signals RPC'
      contains: 'ADD COLUMN IF NOT EXISTS title'
    - path: 'frontend/src/i18n/en/intelligence-signals.json'
      provides: 'English i18n keys for all signals UI strings'
      min_lines: 40
    - path: 'frontend/src/i18n/ar/intelligence-signals.json'
      provides: 'Arabic i18n keys for all signals UI strings'
      min_lines: 40
    - path: 'frontend/src/domains/signals/types/signal.types.ts'
      provides: 'TypeScript types for Signal, filters, mutations'
      exports:
        - Signal
        - SignalStatus
        - SignalFilters
        - CreateSignalInput
  key_links:
    - from: 'frontend/src/i18n/index.ts'
      to: 'frontend/src/i18n/en/intelligence-signals.json'
      via: "import enIntelligenceSignals from './en/intelligence-signals.json'"
      pattern: 'intelligence-signals.*enIntelligenceSignals'
    - from: 'supabase/migrations/20260614_phase69_signals_extend.sql'
      to: 'public.intelligence_event'
      via: 'ADD COLUMN IF NOT EXISTS title'
      pattern: 'ADD COLUMN IF NOT EXISTS title'
    - from: 'public.read_signals'
      to: 'public.intelligence_event'
      via: 'SECURITY INVOKER — caller RLS gate'
      pattern: 'SECURITY INVOKER'
---

<objective>
Wave 0 — Data Layer, i18n Foundation, and Base Types.

Write the forward migration for the intelligence_event extension (6 new columns,
3 indexes, full RLS rewrite for intelligence_event + intelligence_event_dossiers,
read_signals INVOKER RPC), apply it to staging via Supabase MCP, create the
intelligence-signals i18n namespace files (EN + AR), register the namespace in
i18n/index.ts, and write the base TypeScript types that all later hooks and
components depend on.

Purpose: All subsequent waves depend on the columns, policies, and RPC existing
on live staging, and on the type contract being defined before any hook imports it.

Output: Applied migration on staging; two i18n JSON files; registered namespace;
signal.types.ts with all exported types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-signals/69-RESEARCH.md
@.planning/phases/69-signals/69-PATTERNS.md
@.planning/phases/69-signals/69-UI-SPEC.md
@.planning/phases/69-signals/69-VALIDATION.md
@.planning/phases/69-signals/69-CONTEXT.md

<interfaces>
<!-- Extracted from live codebase — executor reads these before touching any file. -->

From supabase/migrations/20260516000002_phase54_intelligence_event_and_digest.sql:
-- intelligence_event existing columns: id, organization_id, source_type (signal_source_type enum),
-- source_ref, content, occurred_at, ingested_at, severity (text CHECK low/medium/high/urgent),
-- created_by, created_at
-- signal_source_type enum values: 'human_entered', 'ai_generated', 'publication', 'feed'

From supabase/migrations/20251022000009_update_polymorphic_refs.sql (lines 102/119):
-- Canonical clearance expression (copy VERBATIM, never vary):
-- sensitivity_level <= (SELECT clearance_level FROM profiles WHERE id = auth.uid())

From frontend/src/i18n/index.ts (registration pattern):
-- import enBriefsPage from './en/briefs-page.json'
-- import arBriefsPage from './ar/briefs-page.json'
-- In resources.en: 'briefs-page': enBriefsPage
-- In resources.ar: 'briefs-page': arBriefsPage
-- CRITICAL: add to BOTH en and ar blocks or AR falls back to English silently

From frontend/src/hooks/useCreateWorkItemDossierLinks.ts (lines 22–36):
-- Type pattern for signals:
-- export type SignalStatus = 'new' | 'acknowledged' | 'dismissed' | 'escalated'
-- export type SignalCategory = 'political' | 'economic' | 'security' | 'diplomatic' | 'other'
-- export type SignalSeverity = 'low' | 'medium' | 'high' | 'urgent'
-- export type SignalSourceType = 'human_entered' | 'ai_generated' | 'publication' | 'feed'
-- export const signalKeys = { all, lists, list } factory pattern
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and apply the Phase 69 migration</name>
  <files>supabase/migrations/20260614_phase69_signals_extend.sql</files>

<read_first> - .planning/phases/69-signals/69-RESEARCH.md — "Data Layer: Complete Migration Spec" section (the full SQL is there — copy it verbatim, do not re-derive) - supabase/migrations/20260516000002_phase54_intelligence_event_and_digest.sql — existing intelligence_event columns and signal_source_type enum to confirm baseline - supabase/migrations/20251022000009_update_polymorphic_refs.sql — lines 102 and 119 for the canonical clearance expression (must be byte-identical) - supabase/migrations/20260516000003_phase54_intelligence_event_dossiers.sql — existing junction policies to confirm DROP targets - CLAUDE.md — "Deployment Configuration" section for project ID zkrcjzdemdmwhearhfgg
</read_first>

  <action>
    Write `supabase/migrations/20260614_phase69_signals_extend.sql` with the exact content from RESEARCH.md "Data Layer: Complete Migration Spec". The migration must contain in order:

    Section 1 — ADD COLUMN IF NOT EXISTS on public.intelligence_event:
    - title TEXT NOT NULL DEFAULT ''
    - sensitivity_level INTEGER NOT NULL DEFAULT 1 CHECK (sensitivity_level BETWEEN 1 AND 4)
    - status TEXT NOT NULL DEFAULT 'new' CHECK (status IN ('new','acknowledged','dismissed','escalated'))
    - category TEXT CHECK (category IN ('political','economic','security','diplomatic','other'))
    - ai_confidence NUMERIC(3,2) CHECK (ai_confidence IS NULL OR (ai_confidence >= 0.00 AND ai_confidence <= 1.00))
    - escalated_task_id UUID REFERENCES public.tasks(id) ON DELETE SET NULL

    Section 2 — CREATE INDEX IF NOT EXISTS (3 indexes):
    - idx_intelligence_event_status ON (organization_id, status, occurred_at DESC)
    - idx_intelligence_event_sensitivity ON (organization_id, sensitivity_level)
    - idx_intelligence_event_category ON (organization_id, category) WHERE category IS NOT NULL

    Section 3 — RLS policy replacement on intelligence_event (per RF-2):
    DROP these 4 policies first (IF EXISTS):
    - intelligence_event_select_org
    - intelligence_event_insert_editor
    - intelligence_event_update_editor
    - intelligence_event_delete_admin

    CREATE 4 new policies with the EXACT canonical expression
    `sensitivity_level <= (SELECT clearance_level FROM profiles WHERE id = auth.uid())`:
    - intelligence_event_select_clearance (FOR SELECT, USING tenant_isolation.rls_select_policy AND clearance)
    - intelligence_event_insert_cleared (FOR INSERT, WITH CHECK tenant+clearance+created_by=auth.uid())
    - intelligence_event_update_cleared (FOR UPDATE, both USING and WITH CHECK: tenant+clearance)
    - intelligence_event_delete_admin (FOR DELETE, USING tenant_isolation.rls_delete_policy only)

    Section 4 — RLS policy replacement on intelligence_event_dossiers:
    DROP these 3 policies first (IF EXISTS):
    - intelligence_event_dossiers_select
    - intelligence_event_dossiers_insert
    - intelligence_event_dossiers_update

    CREATE 3 new policies each using EXISTS (SELECT 1 FROM public.intelligence_event ie WHERE ie.id = event_id AND tenant_isolation.rls_*_policy AND clearance):
    - intelligence_event_dossiers_select (FOR SELECT, USING EXISTS with select+clearance)
    - intelligence_event_dossiers_insert (FOR INSERT, WITH CHECK EXISTS with insert+clearance)
    - intelligence_event_dossiers_update (FOR UPDATE, both USING and WITH CHECK EXISTS with update+clearance)

    Section 5 — read_signals SECURITY INVOKER RPC:
    CREATE OR REPLACE FUNCTION public.read_signals(
      p_dossier_id UUID DEFAULT NULL,
      p_status TEXT DEFAULT NULL,
      p_since TIMESTAMPTZ DEFAULT NULL,
      p_limit INTEGER DEFAULT 50
    )
    RETURNS TABLE (id UUID, title TEXT, content TEXT, sensitivity_level INTEGER, severity TEXT,
      category TEXT, source_type TEXT, source_ref TEXT, ai_confidence NUMERIC, status TEXT,
      occurred_at TIMESTAMPTZ, created_at TIMESTAMPTZ, created_by UUID, escalated_task_id UUID,
      organization_id UUID)
    LANGUAGE SQL SECURITY INVOKER STABLE
    — body: SELECT ie.id, ie.title, ie.content, ie.sensitivity_level, ie.severity, ie.category,
      ie.source_type::text, ie.source_ref, ie.ai_confidence, ie.status, ie.occurred_at,
      ie.created_at, ie.created_by, ie.escalated_task_id, ie.organization_id
      FROM public.intelligence_event ie
      WHERE (p_dossier_id IS NULL OR EXISTS (SELECT 1 FROM public.intelligence_event_dossiers ied WHERE ied.event_id=ie.id AND ied.dossier_id=p_dossier_id))
        AND (p_status IS NULL OR ie.status=p_status)
        AND (p_since IS NULL OR ie.occurred_at>=p_since)
      ORDER BY ie.occurred_at DESC LIMIT p_limit;

    GRANT EXECUTE ON FUNCTION public.read_signals TO authenticated;
    COMMENT on the function describing SECURITY INVOKER rationale and indistinguishable-empty behavior.

    CRITICAL RULES:
    - The source_type::text cast is MANDATORY in the SELECT body — omitting it causes 42804 at runtime
    - The clearance expression must be copied VERBATIM from 20251022000009 (no reformulation)
    - Use ADD COLUMN IF NOT EXISTS everywhere (idempotent)
    - Use DROP POLICY IF EXISTS before each CREATE POLICY (idempotent)

    After writing the file, apply it to staging via Supabase MCP `mcp__supabase__apply_migration`
    targeting project `zkrcjzdemdmwhearhfgg`.

    After apply, verify via Supabase MCP `mcp__supabase__execute_sql` on project zkrcjzdemdmwhearhfgg:
    - Confirm columns exist: SELECT column_name FROM information_schema.columns WHERE table_name='intelligence_event' AND column_name IN ('title','sensitivity_level','status','category','ai_confidence','escalated_task_id');
    - Confirm policies: SELECT policyname FROM pg_policies WHERE tablename='intelligence_event';
    - Confirm RPC: SELECT routine_name FROM information_schema.routines WHERE routine_name='read_signals';

  </action>

  <verify>
    <automated>Supabase MCP execute_sql: SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='intelligence_event' AND column_name IN ('title','sensitivity_level','status','category','ai_confidence','escalated_task_id') ORDER BY column_name; — must return 6 rows</automated>
    <automated>Supabase MCP execute_sql: SELECT policyname FROM pg_policies WHERE tablename='intelligence_event' ORDER BY policyname; — must include intelligence_event_select_clearance, intelligence_event_insert_cleared, intelligence_event_update_cleared, intelligence_event_delete_admin (new names, no old names)</automated>
    <automated>Supabase MCP execute_sql: SELECT routine_name, security_type FROM information_schema.routines WHERE routine_schema='public' AND routine_name='read_signals'; — must return 1 row with security_type='INVOKER'</automated>
  </verify>

<acceptance_criteria> - 6 new columns present on intelligence_event (verified via information_schema) - Old role-locked policies (intelligence_event_select_org, intelligence_event_insert_editor, intelligence_event_update_editor) are ABSENT from pg_policies - New clearance-keyed policies (intelligence_event_select_clearance, intelligence_event_insert_cleared) ARE present - All 3 new intelligence_event_dossiers policies present in pg_policies - read_signals function exists with security_type=INVOKER - pnpm --filter frontend typecheck passes (no frontend code changed yet — just confirming baseline)
</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create i18n namespace files and register in index.ts</name>
  <files>
    frontend/src/i18n/en/intelligence-signals.json,
    frontend/src/i18n/ar/intelligence-signals.json,
    frontend/src/i18n/index.ts
  </files>

<read_first> - .planning/phases/69-signals/69-UI-SPEC.md — "Copywriting Contract" table (all EN and AR strings) - .planning/phases/69-signals/69-PATTERNS.md — "intelligence-signals.json" section with exact key structure - frontend/src/i18n/index.ts — lines 216–222 (Phase 42 import pattern) and lines 364–376 (Quick 260605-u2z block) for where to insert the new import + registration; lines ~383–400 for the en/ar resources blocks - frontend/src/i18n/en/tasks-page.json — exact key/value format to match (flat structure, dot-separated nested keys)
</read_first>

  <action>
    Create `frontend/src/i18n/en/intelligence-signals.json` with the full key structure from PATTERNS.md
    (section "en/intelligence-signals.json and ar/intelligence-signals.json"), populating all English
    values from the UI-SPEC Copywriting Contract table. Required keys include:
    - tab.label: "Signals"
    - queue.emptyState.heading: "No signals."
    - queue.emptyState.body: "Capture a signal or wait for AI-surfaced items."
    - queue.emptyDossier: "No signals for this dossier."
    - queue.emptyDismissed: "No dismissed signals."
    - queue.emptyEscalated: "No escalated signals."
    - queue.errorState: "Signals could not be loaded. Refresh to try again."
    - queue.loading: "Loading signals…"
    - queue.count_one: "{{count}} signal"
    - queue.count_other: "{{count}} signals"
    - actions.capture: "Capture signal"
    - actions.acknowledge: "Acknowledge"
    - actions.dismiss: "Dismiss"
    - actions.escalate: "Escalate"
    - actions.restore: "Restore"
    - actions.viewWorkItem: "View work item"
    - status.new: "New"
    - status.acknowledged: "Acknowledged"
    - status.dismissed: "Dismissed"
    - status.escalated: "Escalated"
    - category.political, .economic, .security, .diplomatic, .other (full English labels)
    - toast.dismissed: "Signal dismissed."
    - toast.restoreAction: "Signal dismissed. Restore?"
    - toast.restored: "Signal restored."
    - toast.escalateSuccess: "Signal escalated. Work item created."
    - dialog.escalateTitle: "Escalate signal"
    - dialog.escalateDesc: "Create a work item from this signal"
    - dialog.cancelEscalation: "Cancel escalation"
    - dialog.captureTitle: "Capture signal"
    - dialog.discardSignal: "Discard signal"
    - form.titleLabel, titlePlaceholder, bodyLabel, bodyPlaceholder, severityLabel, categoryLabel, sensitivityLabel, dossiersLabel
    - badge.aiConfidence: "AI · {{n}}%"
    - badge.sensitivity: "L{{n}}"
    - keyboard.hint: "J/K navigate · A acknowledge · D dismiss · E escalate"
    - columns.severity: "SEVERITY", .category: "CATEGORY", .status: "STATUS", .date: "DATE"

    Create `frontend/src/i18n/ar/intelligence-signals.json` with the same key structure, all values
    in Arabic from the UI-SPEC Copywriting Contract. Key Arabic values:
    - tab.label: "الإشارات"
    - queue.emptyState.heading: "لا توجد إشارات."
    - queue.emptyState.body: "سجّل إشارة أو انتظر العناصر المُكتشفة بالذكاء الاصطناعي."
    - queue.emptyDossier: "لا توجد إشارات لهذا الملف."
    - queue.emptyDismissed: "لا توجد إشارات مرفوضة."
    - queue.emptyEscalated: "لا توجد إشارات تم تصعيدها."
    - queue.errorState: "تعذّر تحميل الإشارات. أعد التحميل للمحاولة مرة أخرى."
    - queue.loading: "جارٍ تحميل الإشارات…"
    - status.new: "جديدة", .acknowledged: "مُستلمة", .dismissed: "مُهملة", .escalated: "مُصعَّدة"
    - actions.capture: "تسجيل الإشارة", .acknowledge: "إقرار", .dismiss: "رفض", .escalate: "تصعيد", .restore: "استعادة", .viewWorkItem: "عرض بند العمل"
    - toast.dismissed: "تم رفض الإشارة.", .restoreAction: "تم رفض الإشارة. استعادة؟", .restored: "تمت استعادة الإشارة.", .escalateSuccess: "تمت إشارة التصعيد. تم إنشاء بند العمل."
    - dialog.escalateTitle: "تصعيد الإشارة", .cancelEscalation: "إلغاء التصعيد", .captureTitle: "تسجيل الإشارة", .discardSignal: "تجاهل الإشارة"
    - badge.aiConfidence: "ذكاء اصطناعي · {{n}}%", .sensitivity: "م{{n}}"
    - keyboard.hint: "ي/ك للتنقل · أ للإقرار · م للرفض · ت للتصعيد"
    - columns.severity: "الخطورة", .category: "الفئة", .status: "الحالة", .date: "التاريخ"
    - category.political: "سياسي", .economic: "اقتصادي", .security: "أمني", .diplomatic: "دبلوماسي", .other: "أخرى"
    - form.titleLabel: Arabic equivalent from UI-SPEC

    Modify `frontend/src/i18n/index.ts`:
    1. Add these two import lines in the Phase 42 import block (or immediately after the last import block before const supportedLanguages):
       import enIntelligenceSignals from './en/intelligence-signals.json'
       import arIntelligenceSignals from './ar/intelligence-signals.json'
    2. In the resources.en object, add:
       'intelligence-signals': enIntelligenceSignals,
    3. In the resources.ar object, add:
       'intelligence-signals': arIntelligenceSignals,

    CRITICAL: Add to BOTH en and ar resources. Missing from ar causes silent English fallback
    in Arabic mode. Use ONLY the hyphenated string key 'intelligence-signals' (never dot-form
    'intelligence.signals'). Components will call useTranslation('intelligence-signals') with colon form.

  </action>

  <verify>
    <automated>pnpm --filter frontend typecheck 2>&1 | tail -5 — must exit 0 (no TS errors from new imports)</automated>
    <automated>grep -c "intelligence-signals" frontend/src/i18n/index.ts — must be >= 4 (import ×2 + resource entry ×2)</automated>
    <automated>node -e "const en=require('./frontend/src/i18n/en/intelligence-signals.json'); console.log(Object.keys(en).join(','))" 2>/dev/null | grep -c "status" || echo "run from project root"</automated>
  </verify>

<acceptance_criteria> - `frontend/src/i18n/en/intelligence-signals.json` exists with all required keys (tab, queue, actions, status, category, toast, dialog, form, badge, keyboard, columns) - `frontend/src/i18n/ar/intelligence-signals.json` exists with the same key structure and Arabic values - status.new = "جديدة", status.acknowledged = "مُستلمة", status.dismissed = "مُهملة", status.escalated = "مُصعَّدة" in AR file - i18n/index.ts contains `'intelligence-signals': enIntelligenceSignals` in resources.en and `'intelligence-signals': arIntelligenceSignals` in resources.ar - pnpm --filter frontend typecheck exits 0 - pnpm --filter frontend lint exits 0
</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Write base TypeScript types for signals domain</name>
  <files>
    frontend/src/domains/signals/types/signal.types.ts,
    frontend/src/domains/signals/index.ts
  </files>

<read_first> - .planning/phases/69-signals/69-PATTERNS.md — "signal.types.ts" section — the complete type interface definitions - .planning/phases/69-signals/69-RESEARCH.md — RF-1 data layer decision (column types) and RF-5 read_signals return shape - frontend/src/hooks/useCreateWorkItemDossierLinks.ts — lines 22–36 for the interface + union type pattern (query key factory) - frontend/src/domains/intake/index.ts — barrel export pattern to mirror - CLAUDE.md — "Work Management Terminology" for priority/status enum alignment
</read_first>

  <action>
    Create `frontend/src/domains/signals/types/signal.types.ts` with these NAMED exports (no default exports per CLAUDE.md convention):

    Types/unions:
    - SignalStatus = 'new' | 'acknowledged' | 'dismissed' | 'escalated'
    - SignalCategory = 'political' | 'economic' | 'security' | 'diplomatic' | 'other'
    - SignalSeverity = 'low' | 'medium' | 'high' | 'urgent'
    - SignalSourceType = 'human_entered' | 'ai_generated' | 'publication' | 'feed'

    Interfaces:
    - Signal: { id: string; title: string; content: string; sensitivity_level: number;
      severity: SignalSeverity; category: SignalCategory | null; source_type: SignalSourceType;
      source_ref: string | null; ai_confidence: number | null; status: SignalStatus;
      occurred_at: string; created_at: string; created_by: string;
      escalated_task_id: string | null; organization_id: string }
    - SignalFilters: { dossierId?: string; status?: SignalStatus; since?: string; limit?: number }
    - CreateSignalInput: { title: string; body: string; severity: SignalSeverity;
      category: SignalCategory; sensitivityLevel: number; dossierIds: string[];
      dossierTypes?: Record<string, string>;
      sourceType?: SignalSourceType;   // defaults to 'human_entered'; pass 'ai_generated' for AI write path (D-13)
      aiConfidence?: number | null }   // required when sourceType='ai_generated'; null for human_entered
      (dossierTypes maps dossier_id → dossier.type for the junction INSERT; optional to keep types minimal)
    - UpdateSignalStatusInput: { id: string; status: SignalStatus }
    - EscalateSignalInput: { signalId: string; title: string; body: string;
      priority: SignalSeverity; assigneeId?: string; slaDeadline?: string;
      dossierIds: string[]; dossierTypes?: Record<string, string> }

    Query key factory:
    - export const signalKeys = { all: ['signals'] as const; lists: () => [...]; list: (filters: SignalFilters) => [...] }

    All types are pure (no logic). Explicit explicit-function-return-type applies to functions only.
    File must be < 80 lines total.

    Create `frontend/src/domains/signals/index.ts` as a barrel re-export of everything from ./types/signal.types.
    Follow frontend/src/domains/intake/index.ts pattern (re-export all named exports).

  </action>

  <verify>
    <automated>pnpm --filter frontend typecheck 2>&1 | tail -5 — must exit 0</automated>
    <automated>grep -c "export" frontend/src/domains/signals/types/signal.types.ts — must be >= 8 (each type/interface/const exported)</automated>
  </verify>

<acceptance_criteria> - signal.types.ts exports: Signal, SignalStatus, SignalCategory, SignalSeverity, SignalSourceType, SignalFilters, CreateSignalInput, UpdateSignalStatusInput, EscalateSignalInput, signalKeys - All SignalSeverity values match tasks.priority enum: low, medium, high, urgent (1:1 map for escalation per RF-4) - signalKeys factory matches TanStack Query pattern used in useCreateWorkItemDossierLinks.ts - domains/signals/index.ts barrel-exports all types - pnpm --filter frontend typecheck exits 0 - pnpm --filter frontend lint exits 0
</acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                           |
| ------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| Supabase RLS → application            | New clearance-keyed policies must gate every read/write on intelligence_event and junction            |
| read_signals RPC INVOKER → caller JWT | Clearance enforcement depends on caller JWT flowing through INVOKER — any DEFINER leak collapses this |
| Migration idempotency                 | DROP POLICY IF EXISTS + ADD COLUMN IF NOT EXISTS prevents dual-apply corruption                       |

## STRIDE Threat Register

| Threat ID | Category               | Component                                        | Disposition | Mitigation Plan                                                                                                                                                                                     |
| --------- | ---------------------- | ------------------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-69-01   | Information Disclosure | intelligence_event SELECT RLS                    | mitigate    | New intelligence_event_select_clearance policy enforces `sensitivity_level <= clearance_level`; indistinguishable empty on denial — implemented in Task 1                                           |
| T-69-02   | Elevation of Privilege | intelligence_event INSERT RLS (loosened in D-05) | mitigate    | WITH CHECK requires `sensitivity_level <= clearance_level AND created_by = auth.uid()` — user cannot write above their own clearance level; implemented in Task 1                                   |
| T-69-03   | Information Disclosure | read_signals RPC SECURITY INVOKER                | mitigate    | SECURITY INVOKER (never DEFINER) ensures caller JWT propagates into all downstream RLS checks; Task 1 applies GRANT EXECUTE TO authenticated; verified via information_schema security_type=INVOKER |
| T-69-04   | Information Disclosure | intelligence_event_dossiers SELECT (junction)    | mitigate    | New junction SELECT policy re-checks clearance via EXISTS JOIN on parent ie.sensitivity_level; cannot bypass clearance by querying junction directly; Task 1                                        |
| T-69-05   | Tampering              | intelligence_event UPDATE RLS                    | mitigate    | UPDATE policy checks clearance in both USING and WITH CHECK — prevents escalating a signal's sensitivity_level above the caller's clearance post-insert; Task 1                                     |
| T-69-SC   | Tampering              | npm installs                                     | accept      | No new packages in this phase (all dependencies pre-existing; RESEARCH.md Package Legitimacy Audit confirms zero new installs)                                                                      |

</threat_model>

<verification>
Wave 1 complete when:
1. `SELECT column_name FROM information_schema.columns WHERE table_name='intelligence_event' AND column_name IN ('title','sensitivity_level','status','category','ai_confidence','escalated_task_id')` returns 6 rows on staging
2. `SELECT policyname FROM pg_policies WHERE tablename='intelligence_event'` shows new clearance policies, not old role-locked ones
3. `SELECT routine_name, security_type FROM information_schema.routines WHERE routine_schema='public' AND routine_name='read_signals'` returns 1 row with INVOKER
4. `grep -c "intelligence-signals" frontend/src/i18n/index.ts` >= 4
5. `pnpm --filter frontend typecheck` exits 0
6. `pnpm --filter frontend lint` exits 0
</verification>

<success_criteria>

- Migration applied: 6 columns added, 3 indexes created, 7 RLS policies replaced (4 on intelligence_event + 3 on junction), read_signals INVOKER RPC live on staging zkrcjzdemdmwhearhfgg
- i18n: en/intelligence-signals.json + ar/intelligence-signals.json exist with all keys; registered in BOTH resources.en and resources.ar in i18n/index.ts
- Types: signal.types.ts exports Signal, SignalStatus, SignalFilters, CreateSignalInput, EscalateSignalInput, signalKeys
- No TypeScript errors; no lint errors
  </success_criteria>

<output>
Create `.planning/phases/69-signals/69-01-SUMMARY.md` when done
</output>
