---
phase: 90-cors-edge-migration
plan: 32
type: checkpoint
wave: 2
depends_on:
  [
    90-01,
    90-02,
    90-03,
    90-04,
    90-05,
    90-06,
    90-07,
    90-08,
    90-09,
    90-10,
    90-11,
    90-12,
    90-13,
    90-14,
    90-15,
    90-16,
    90-17,
    90-18,
  ]
files_modified:
  - .planning/phases/90-cors-edge-migration/90-32-SUMMARY.md
autonomous: false
requirements: [CORS-02]
routing:
  floor: frontier

must_haves:
  truths:
    - 'All 170 Group-A functions are deployed to STAGING after their code merged'
    - 'A smoke request from an ALLOWED origin succeeds unchanged (2xx, correct Access-Control-Allow-Origin echoing the origin)'
    - 'A request from a DISALLOWED origin receives Access-Control-Allow-Origin: null (origin validation active)'
    - 'CORS-01 (90-01) is green — ALLOWED_ORIGINS verified — before this deploy runs'
---

<objective>
Deploy + smoke the migrated **Group-A** edge functions (170 functions, deploy batch A,
CORS-02) to STAGING. Runs AFTER every Group-A migration plan (02..18)
has merged, and only after CORS-01 (90-01) is green.

**ORCHESTRATOR-ONLY step.** Worker worktrees cannot deploy (no Supabase creds/network); this is run
by the orchestrator via the Supabase MCP / CLI. STAGING ONLY — no prod deploy in this phase.
</objective>

<context>
@.planning/phases/90-cors-edge-migration/90-RESEARCH.md
</context>

<tasks>

<task type="manual">
  <name>Task 1: Deploy Group-A to staging and smoke-check CORS</name>
  <action>
Precondition: 90-01 (CORS-01) SUMMARY confirms ALLOWED_ORIGINS present+correct. If not, STOP.
  1. Deploy every Group-A function to the staging project (zkrcjzdemdmwhearhfgg) via the Supabase
     MCP `deploy_edge_function` / `supabase functions deploy` — the list is the union of
     `files_modified` across plans 02..18.
  2. Smoke ALLOWED origin: send an OPTIONS preflight and a normal request with
     `Origin: <an allow-listed origin>` to 2–3 representative functions; assert 2xx/204 and
     `Access-Control-Allow-Origin` echoes that origin (NOT `*`, NOT `null`).
  3. Smoke DISALLOWED origin: same request with `Origin: https://evil.example`; assert
     `Access-Control-Allow-Origin: null`.
  Record deployed function count + smoke results in the SUMMARY.
  </action>
  <acceptance_criteria>
    - All 170 Group-A functions deployed to staging (deploy log captured)
    - Allowed-origin smoke: 2xx/204 with the origin echoed back (no regression)
    - Disallowed-origin smoke: `Access-Control-Allow-Origin: null`
  </acceptance_criteria>
  <done>Batch A live on staging with origin validation confirmed working.</done>
</task>

</tasks>

<verification>
- Supabase MCP deploy log lists the Group-A functions.
- Manual curl/smoke shows allowed-origin echo + disallowed-origin `null`.

</verification>

<success_criteria>

- CORS-02 deploy leg satisfied for batch A: migrated, deployed to staging, smoke-clean.

</success_criteria>

<output>
Create `.planning/phases/90-cors-edge-migration/90-32-SUMMARY.md` when done
</output>
