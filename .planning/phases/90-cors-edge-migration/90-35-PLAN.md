---
phase: 90-cors-edge-migration
plan: 35
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/activate-account/index.ts
  - supabase/functions/initiate-password-reset/index.ts
  - supabase/functions/reset-password/index.ts
  - supabase/functions/push-device-register/index.ts
  - .planning/phases/90-cors-edge-migration/90-35-SUMMARY.md
autonomous: true
requirements: [CORS-03]
routing:
  floor: frontier

must_haves:
  truths:
    - 'All 4 edge functions in this chunk import and use getCorsHeaders(req) from _shared/cors.ts; the inline hard-coded wildcard header object is gone'
    - 'No file in this chunk contains the deprecated wildcard Access-Control-Allow-Origin star (checked by grep)'
    - 'Response bodies and status codes are otherwise unchanged — only the CORS header source changed (no functional regression from allowed origins)'
  key_links:
    - from: 'supabase/functions/activate-account/index.ts'
      to: 'supabase/functions/_shared/cors.ts'
      via: 'getCorsHeaders(req) origin-validated CORS'
      pattern: 'getCorsHeaders'
    - from: 'supabase/functions/reset-password/index.ts'
      to: 'supabase/functions/_shared/cors.ts'
      via: 'getCorsHeaders(req) origin-validated CORS'
      pattern: 'getCorsHeaders'
---

<objective>
Migrate 4 Group-D (inline-wildcard, handler-scope) edge functions off the deprecated wildcard CORS
onto the origin-validated `_shared/cors.ts` helper. Deploy batch **C** (CORS-03), chunk 1/2.

Group D is the fourth CORS shape (missed by the initial variable-based partition, added after
plan-check): these files hard-code `'Access-Control-Allow-Origin': '*'` **inline** in a response
header object, with no `corsHeaders` variable and no `_shared/cors` import. Without this plan the
final repo-wide grep gate (90-34) fails.

CODE-MIGRATION ONLY — no deploy (worker sandbox has no Supabase creds). Deploy + smoke runs at the
batch-C orchestrator checkpoint (90-34) after this code merges.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-cors-edge-migration/90-RESEARCH.md
@supabase/functions/activate-account/index.ts
@supabase/functions/_shared/cors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 4 Group-D inline-wildcard functions to getCorsHeaders</name>
  <files>supabase/functions/activate-account/index.ts, supabase/functions/initiate-password-reset/index.ts, supabase/functions/reset-password/index.ts, supabase/functions/push-device-register/index.ts</files>
  <read_first>supabase/functions/activate-account/index.ts, supabase/functions/_shared/cors.ts, supabase/functions/reactivate-user/index.ts (canonical migrated reference), .planning/phases/90-cors-edge-migration/90-RESEARCH.md (section 2.D)</read_first>
  <action>
Each of these 4 files hard-codes `'Access-Control-Allow-Origin': '*'` inline near the top of its
request handler (a local `const cors/corsHeaders = { 'Access-Control-Allow-Origin': '*',
'Access-Control-Allow-Methods': ..., ... }` object built inside `serve(async (req) => {`). All
usage is handler-scope. For EACH file:
  1. Add `import { getCorsHeaders, handleCorsPreflightRequest } from '../_shared/cors.ts'` (adjust
     the `../` depth to the file's location; these are one level deep so `../_shared/cors.ts`).
  2. Replace the inline wildcard object with `const corsHeaders = getCorsHeaders(req)` as the first
     statement inside the handler (use the handler's ACTUAL request param name — `req`).
  3. Route the OPTIONS preflight through `return handleCorsPreflightRequest(req)`.
  4. Update every response that spread/used the old inline object to spread `...corsHeaders`
     (preserve any non-CORS headers like `Content-Type` that were in the same object).
Do NOT touch `_shared/`. Change nothing else (surgical).
  </action>
  <verify>
    <automated>for f in supabase/functions/activate-account/index.ts supabase/functions/initiate-password-reset/index.ts supabase/functions/reset-password/index.ts supabase/functions/push-device-register/index.ts; do grep -L "getCorsHeaders" "$f"; done</automated>
  </verify>
  <acceptance_criteria>
    - `for f in supabase/functions/activate-account/index.ts supabase/functions/initiate-password-reset/index.ts supabase/functions/reset-password/index.ts supabase/functions/push-device-register/index.ts; do grep -L "getCorsHeaders" "$f"; done` prints NOTHING
    - `grep -rn "Allow-Origin.*[*]" supabase/functions/activate-account/index.ts supabase/functions/initiate-password-reset/index.ts supabase/functions/reset-password/index.ts supabase/functions/push-device-register/index.ts | wc -l` returns 0 (no inline wildcard remains)
    - each file matching `\.\.\.corsHeaders` also matches `const corsHeaders = getCorsHeaders(`
  </acceptance_criteria>
  <done>All 4 files use `getCorsHeaders(req)`; zero inline wildcard remains.</done>
</task>

</tasks>

<verification>
- `git grep -n "Access-Control-Allow-Origin" -- supabase/functions/activate-account/index.ts supabase/functions/initiate-password-reset/index.ts supabase/functions/reset-password/index.ts supabase/functions/push-device-register/index.ts` shows no `*` wildcard value.
- Runtime smoke verified at the batch-C deploy checkpoint (90-34), NOT here.
</verification>

<success_criteria>

- Contributes to CORS-03 and the final grep gate: 4 inline-wildcard functions migrated.
  </success_criteria>

<output>
Create `.planning/phases/90-cors-edge-migration/90-35-SUMMARY.md` when done
</output>
