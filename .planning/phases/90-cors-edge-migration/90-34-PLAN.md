---
phase: 90-cors-edge-migration
plan: 34
type: checkpoint
wave: 2
depends_on: [90-01, 90-27, 90-28, 90-29, 90-30, 90-31, 90-35, 90-36]
files_modified:
  - .planning/phases/90-cors-edge-migration/90-34-SUMMARY.md
autonomous: false
requirements: [CORS-03]
routing:
  floor: frontier

must_haves:
  truths:
    - 'All 26 Group-C functions AND the 8 Group-D inline-wildcard functions (90-35/36) are deployed to STAGING after their code merged'
    - 'A smoke request from an ALLOWED origin succeeds unchanged (2xx, correct Access-Control-Allow-Origin echoing the origin)'
    - 'A request from a DISALLOWED origin receives Access-Control-Allow-Origin: null (origin validation active)'
    - 'CORS-01 (90-01) is green — ALLOWED_ORIGINS verified — before this deploy runs'
---

<objective>
Deploy + smoke the migrated **Group-C** (26) + **Group-D** (8 inline-wildcard, plans 90-35/36) edge
functions to STAGING — deploy batch C, CORS-03. Runs AFTER every Group-C migration plan (27..31) AND
the Group-D plans (35, 36) have merged, and only after CORS-01 (90-01) is green. This is the LAST
checkpoint of the phase, so it also runs the terminal repo-wide grep gate.

**ORCHESTRATOR-ONLY step.** Worker worktrees cannot deploy (no Supabase creds/network); this is run
by the orchestrator via the Supabase MCP / CLI. STAGING ONLY — no prod deploy in this phase.
</objective>

<context>
@.planning/phases/90-cors-edge-migration/90-RESEARCH.md
</context>

<tasks>

<task type="manual">
  <name>Task 1: Deploy Group-C to staging and smoke-check CORS</name>
  <action>
Precondition: 90-01 (CORS-01) SUMMARY confirms ALLOWED_ORIGINS present+correct. If not, STOP.
  1. Deploy every Group-C AND Group-D function to the staging project (zkrcjzdemdmwhearhfgg) via the
     Supabase MCP `deploy_edge_function` / `supabase functions deploy` — the list is the union of
     `files_modified` across plans 27..31, 35, 36 (34 functions total).
  2. Smoke ALLOWED origin: send an OPTIONS preflight and a normal request with
     `Origin: <an allow-listed origin>` to 2–3 representative functions; assert 2xx/204 and
     `Access-Control-Allow-Origin` echoes that origin (NOT `*`, NOT `null`).
  3. Smoke DISALLOWED origin: same request with `Origin: https://evil.example`; assert
     `Access-Control-Allow-Origin: null`.
  4. FINAL GATE — run the repo-wide grep and confirm ZERO matches:
     `grep -rn "Access-Control-Allow-Origin': '\*'" supabase/functions --include='*.ts' | grep -v _shared | wc -l` == 0
     and `grep -rln "corsHeaders" supabase/functions --include='*.ts' | grep -v _shared | xargs grep -L getCorsHeaders` prints nothing.
     Only then is Phase 90 complete.
  Record deployed function count + smoke results in the SUMMARY.
  </action>
  <acceptance_criteria>
    - All 26 Group-C + 8 Group-D functions deployed to staging (deploy log captured)
    - Allowed-origin smoke: 2xx/204 with the origin echoed back (no regression)
    - Disallowed-origin smoke: `Access-Control-Allow-Origin: null`
    - FINAL: repo-wide grep for the deprecated wildcard returns 0 matches across all edge functions (excl _shared)
  </acceptance_criteria>
  <done>Batch C live on staging with origin validation confirmed working and the repo-wide wildcard grep at 0 (phase complete).</done>
</task>

</tasks>

<verification>
- Supabase MCP deploy log lists the Group-C functions.
- Manual curl/smoke shows allowed-origin echo + disallowed-origin `null`.
- git grep for the deprecated wildcard across supabase/functions (excl _shared) returns 0.
</verification>

<success_criteria>

- CORS-03 deploy leg satisfied for batch C: migrated, deployed to staging, smoke-clean.
- Phase 90 success criterion 4 satisfied: repo-wide grep for the deprecated wildcard returns 0.
  </success_criteria>

<output>
Create `.planning/phases/90-cors-edge-migration/90-34-SUMMARY.md` when done
</output>
