---
phase: 90-cors-edge-migration
plan: 36
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/ocr-extract/index.ts
  - supabase/functions/push-notification-send/index.ts
  - supabase/functions/scenario-sandbox/index.ts
  - supabase/functions/waiting-queue-escalation/index.ts
  - .planning/phases/90-cors-edge-migration/90-36-SUMMARY.md
autonomous: true
requirements: [CORS-03]
routing:
  floor: frontier

must_haves:
  truths:
    - 'All 4 edge functions in this chunk import and use getCorsHeaders(req) from _shared/cors.ts; every inline hard-coded wildcard header object is gone'
    - 'ocr-extract has all 3 inline wildcard sites fixed; scenario-sandbox module-scope helpers (createErrorResponse/createSuccessResponse) receive origin-validated headers rather than a dangling reference'
    - 'No file in this chunk contains the deprecated wildcard Access-Control-Allow-Origin star (checked by grep); responses otherwise unchanged'
  key_links:
    - from: 'supabase/functions/ocr-extract/index.ts'
      to: 'supabase/functions/_shared/cors.ts'
      via: 'getCorsHeaders(req) origin-validated CORS'
      pattern: 'getCorsHeaders'
    - from: 'supabase/functions/scenario-sandbox/index.ts'
      to: 'supabase/functions/_shared/cors.ts'
      via: 'getCorsHeaders(req) threaded into module-scope response helpers'
      pattern: 'getCorsHeaders'
---

<objective>
Migrate 4 Group-D (inline-wildcard) edge functions — the harder subset (multi-site + module-scope
helpers) — off the deprecated wildcard CORS onto the origin-validated `_shared/cors.ts` helper.
Deploy batch **C** (CORS-03), chunk 2/2.

These hard-code `'Access-Control-Allow-Origin': '*'` inline: `ocr-extract` at 3 response sites;
`scenario-sandbox` inside MODULE-SCOPE helpers `createErrorResponse` / `createSuccessResponse` (needs
threading, like Group C). Without this plan the final repo-wide grep gate (90-34) fails.

CODE-MIGRATION ONLY — no deploy. Deploy + smoke runs at the batch-C orchestrator checkpoint (90-34).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-cors-edge-migration/90-RESEARCH.md
@supabase/functions/scenario-sandbox/index.ts
@supabase/functions/_shared/cors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 4 Group-D multi-site / module-scope functions to getCorsHeaders</name>
  <files>supabase/functions/ocr-extract/index.ts, supabase/functions/push-notification-send/index.ts, supabase/functions/scenario-sandbox/index.ts, supabase/functions/waiting-queue-escalation/index.ts</files>
  <read_first>supabase/functions/scenario-sandbox/index.ts, supabase/functions/ocr-extract/index.ts, supabase/functions/_shared/cors.ts, supabase/functions/reactivate-user/index.ts, .planning/phases/90-cors-edge-migration/90-RESEARCH.md (section 2.C + 2.D)</read_first>
  <action>
For EACH file, add `import { getCorsHeaders, handleCorsPreflightRequest } from '../_shared/cors.ts'`
(adjust `../` depth), then remove the inline `'Access-Control-Allow-Origin': '*'` wildcard:

- **ocr-extract** — 3 inline wildcard sites (one inside the OPTIONS branch at ~166, two in later
  responses ~325/~341). Add `const corsHeaders = getCorsHeaders(req)` at the top of the handler,
  route OPTIONS through `handleCorsPreflightRequest(req)`, and replace ALL THREE inline objects with
  `...corsHeaders` (preserving any non-CORS headers merged in the same object). Verify no 4th site.
- **push-notification-send** / **waiting-queue-escalation** — handler-scope inline object; same as
  Group D chunk 1: `const corsHeaders = getCorsHeaders(req)` at handler top, OPTIONS via
  `handleCorsPreflightRequest(req)`, spread `...corsHeaders` at response sites.
- **scenario-sandbox** — the wildcard lives in MODULE-SCOPE helpers `createErrorResponse` (line ~79)
  and `createSuccessResponse` (line ~98), called from the handler. Do NOT rely on a handler-local
  rebind — that leaves the helpers referencing an undefined/ wildcard value (deno compile error).
  Thread the origin-validated headers in: add a `cors: Record<string,string>` parameter to each
  helper (and to any intermediate functions like listScenarios/getScenario that call them), compute
  `const corsHeaders = getCorsHeaders(req)` once in the handler, and pass it down; OR move the
  helpers inside the handler. Route OPTIONS through `handleCorsPreflightRequest(req)`.

Do NOT touch `_shared/`. Use the handler's ACTUAL request param name. Change nothing else (surgical).
</action>
<verify>
<automated>for f in supabase/functions/ocr-extract/index.ts supabase/functions/push-notification-send/index.ts supabase/functions/scenario-sandbox/index.ts supabase/functions/waiting-queue-escalation/index.ts; do grep -L "getCorsHeaders" "$f"; done</automated>
  </verify>
  <acceptance_criteria>
    - `for f in supabase/functions/ocr-extract/index.ts supabase/functions/push-notification-send/index.ts supabase/functions/scenario-sandbox/index.ts supabase/functions/waiting-queue-escalation/index.ts; do grep -L "getCorsHeaders" "$f"; done`prints NOTHING
    -`grep -rn "Allow-Origin._[_]" supabase/functions/ocr-extract/index.ts supabase/functions/push-notification-send/index.ts supabase/functions/scenario-sandbox/index.ts supabase/functions/waiting-queue-escalation/index.ts | wc -l`returns 0 (all inline wildcards, incl ocr-extract's 3, gone)
    -`grep -c "getCorsHeaders" supabase/functions/scenario-sandbox/index.ts`>= 1 AND scenario-sandbox has no`...cors`/wildcard reference unbound to a threaded `cors`param (module-scope helpers fixed)
  </acceptance_criteria>
  <done>All 4 files use`getCorsHeaders(req)`; zero inline wildcard remains; scenario-sandbox helpers receive validated headers.</done>
</task>

</tasks>

<verification>
- `git grep -n "Access-Control-Allow-Origin" -- supabase/functions/ocr-extract/index.ts supabase/functions/push-notification-send/index.ts supabase/functions/scenario-sandbox/index.ts supabase/functions/waiting-queue-escalation/index.ts` shows no `*` wildcard value.
- scenario-sandbox threading correctness is enforced by the deno compile step at the 90-34 deploy checkpoint (a dangling `cors` ref is a hard compile error).
</verification>

<success_criteria>

- Contributes to CORS-03 and the final grep gate: the 4 harder inline-wildcard functions migrated.
  </success_criteria>

<output>
Create `.planning/phases/90-cors-edge-migration/90-36-SUMMARY.md` when done
</output>
