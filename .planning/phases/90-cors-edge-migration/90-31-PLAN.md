---
phase: 90-cors-edge-migration
plan: 31
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/workflow-rules/index.ts
  - supabase/functions/working-groups/index.ts
  - .planning/phases/90-cors-edge-migration/90-31-SUMMARY.md
autonomous: true
requirements: [CORS-03]
routing:
  floor: frontier

must_haves:
  truths:
    - 'All 2 edge functions in this chunk import and use getCorsHeaders(req) from _shared/cors.ts; none imports the deprecated static corsHeaders'
    - 'No file in this chunk contains the deprecated wildcard Access-Control-Allow-Origin star (checked by grep)'
    - 'Response bodies and status codes are otherwise byte-identical — only the CORS header source changed (no functional regression from allowed origins)'
  key_links:
    - from: 'supabase/functions/workflow-rules/index.ts'
      to: 'supabase/functions/_shared/cors.ts'
      via: 'getCorsHeaders(req) origin-validated CORS'
      pattern: 'getCorsHeaders'
    - from: 'supabase/functions/working-groups/index.ts'
      to: 'supabase/functions/_shared/cors.ts'
      via: 'getCorsHeaders(req) origin-validated CORS'
      pattern: 'getCorsHeaders'
---

<objective>
Migrate 2 Group-C (module-scope (careful)) edge functions off the deprecated wildcard CORS onto the
origin-validated `_shared/cors.ts` helper. Deploy batch **C** (CORS-03), chunk 5/5.

CODE-MIGRATION ONLY — no deploy (worker sandbox has no Supabase creds). Deploy + smoke for batch
C runs at the orchestrator checkpoint plan after this batch's code merges.

The transformation is mechanical and identical across all files (canonical reference:
`supabase/functions/reactivate-user/index.ts`). Keep each edit surgical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-cors-edge-migration/90-RESEARCH.md
@supabase/functions/workflow-rules/index.ts
@supabase/functions/_shared/cors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 2 Group-C functions to getCorsHeaders</name>
  <files>supabase/functions/workflow-rules/index.ts, supabase/functions/working-groups/index.ts</files>
  <read_first>supabase/functions/workflow-rules/index.ts, supabase/functions/_shared/cors.ts, supabase/functions/reactivate-user/index.ts (canonical migrated reference), .planning/phases/90-cors-edge-migration/90-RESEARCH.md (section 2.C)</read_first>
  <action>
For EACH file listed in the files tag, apply the identical section-2.C transformation:
Add/replace the import with `{ getCorsHeaders, handleCorsPreflightRequest }` from `_shared/cors.ts`; delete any local wildcard `const corsHeaders = {'*'}`; compute `const corsHeaders = getCorsHeaders(<reqParam>)` in the handler AND thread the origin-validated headers into every MODULE-SCOPE helper that referenced the old `corsHeaders` — add a `cors: Record<string,string>` param to the helper (pass `corsHeaders` at each call) or move the helper inside the handler. For a `function corsHeaders(extra)` factory, rewrite its body to spread `getCorsHeaders(<reqParam>)` (thread `req`). No `...corsHeaders` reference may remain bound to the removed static/wildcard source.

Use the handler's ACTUAL request parameter name (usually `req`, sometimes `request`/`_req`).
Watch for multi-line import blocks that list corsHeaders among other names — edit the block, not a
one-line assumption. Do NOT touch `_shared/`. Change nothing else in each file (surgical).
</action>
<verify>
<automated>for f in supabase/functions/workflow-rules/index.ts supabase/functions/working-groups/index.ts; do grep -L "getCorsHeaders" "$f"; done   # must print nothing</automated>
  </verify>
  <acceptance_criteria>
    - `for f in supabase/functions/workflow-rules/index.ts supabase/functions/working-groups/index.ts; do grep -L "getCorsHeaders" "$f"; done`prints NOTHING (every file uses the validated helper)
    -`grep -rn "Allow-Origin._[_]" supabase/functions/workflow-rules/index.ts supabase/functions/working-groups/index.ts | wc -l`returns 0 (no wildcard remains; also try single-quote form)
    -`grep -rn "corsHeaders" supabase/functions/workflow-rules/index.ts supabase/functions/working-groups/index.ts | grep "\_shared/cors" | wc -l`returns 0 (deprecated lowercase static import gone — getCorsHeaders has a capital C so it never matches)
    - every file matching`\.\.\.corsHeaders`also matches`const corsHeaders = getCorsHeaders(`(spreads now bound to the validated object)
  </acceptance_criteria>
  <done>All 2 files import/use`getCorsHeaders(req)`; zero wildcard and zero deprecated static import remain in the chunk.</done>
</task>

</tasks>

<verification>
- `for f in supabase/functions/workflow-rules/index.ts supabase/functions/working-groups/index.ts; do grep -Lq "getCorsHeaders" "$f" && echo "MISSING $f"; done` prints nothing
- `git grep -n "Access-Control-Allow-Origin" -- supabase/functions/workflow-rules/index.ts supabase/functions/working-groups/index.ts` shows no `*` wildcard value
- Runtime smoke (allowed-origin request unchanged; disallowed origin → `null`) is verified at the batch-C orchestrator deploy checkpoint, NOT here.
</verification>

<success_criteria>

- Contributes to CORS-03: 2 functions migrated off the deprecated wildcard
- No functional regression: only the CORS header source changed
  </success_criteria>

<output>
Create `.planning/phases/90-cors-edge-migration/90-31-SUMMARY.md` when done
</output>
