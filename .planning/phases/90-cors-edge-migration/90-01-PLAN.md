---
phase: 90-cors-edge-migration
plan: 01
type: checkpoint
wave: 0
depends_on: []
files_modified:
  - .planning/phases/90-cors-edge-migration/90-01-SUMMARY.md
autonomous: false
requirements: [CORS-01]
routing:
  floor: frontier

must_haves:
  truths:
    - 'The ALLOWED_ORIGINS secret is confirmed PRESENT in both staging (zkrcjzdemdmwhearhfgg) and prod before any batch is deployed'
    - 'Its value is confirmed CORRECT — it contains the real app origins (droplet + any web hosts), comma-separated, no trailing wildcard'
    - 'This is a READ-ONLY verification: no secret is written, no function is deployed'
---

<objective>
CORS-01 — the gate for the whole phase. Verify the `ALLOWED_ORIGINS` Supabase secret is present and
correct in **staging AND prod** BEFORE any migrated batch is deployed. If a batch ships while
`ALLOWED_ORIGINS` is unset/wrong, every real browser origin gets `Access-Control-Allow-Origin: null`
and the app breaks — so this must be green before deploy checkpoints 90-32/33/34 run.

**ORCHESTRATOR / MANUAL step — not a worker task.** Read-only. No code change, no secret write, no
deploy. Prod is only READ here (prod ship is a later operator step, out of this phase).
</objective>

<context>
@.planning/phases/90-cors-edge-migration/90-RESEARCH.md
@supabase/functions/_shared/cors.ts
</context>

<tasks>

<task type="manual">
  <name>Task 1: Verify ALLOWED_ORIGINS present + correct in staging and prod (read-only)</name>
  <action>
Via the Supabase MCP (or dashboard → Edge Functions → Secrets), for BOTH the staging project
(Intl-Dossier, id `zkrcjzdemdmwhearhfgg`) and the prod project:
  1. Confirm a secret named `ALLOWED_ORIGINS` exists.
  2. Confirm its value is a comma-separated origin allow-list containing the real app origins
     (production droplet `http://138.197.195.242` and any HTTPS/web host in use), with NO `*`
     wildcard entry and no stray whitespace beyond the `, ` the helper trims.
  3. Record both values (redacted host list is fine) in the SUMMARY.
Do NOT set or rotate the secret. Do NOT deploy anything. If EITHER environment is missing or wrong,
STOP and flag to the operator — the deploy checkpoints (90-32/33/34) must not run until this is green.
  </action>
  <acceptance_criteria>
    - SUMMARY records `ALLOWED_ORIGINS` = PRESENT for staging AND prod, with the confirmed origin list (redacted OK)
    - SUMMARY confirms no `*` wildcard entry in either value
    - No secret write and no function deploy occurred (verification only)
  </acceptance_criteria>
  <done>Both environments confirmed to hold a correct `ALLOWED_ORIGINS`; the deploy gate is open. If not, phase is BLOCKED pending operator fix.</done>
</task>

</tasks>

<verification>
- Supabase MCP shows `ALLOWED_ORIGINS` in staging and prod secret lists.
- The `_shared/cors.ts` `getAllowedOrigins()` will therefore return the real allow-list (not the
  localhost dev fallback) once functions are deployed.
</verification>

<success_criteria>

- CORS-01 satisfied: secret verified in staging + prod; deploy gate open for CORS-02/CORS-03.
  </success_criteria>

<output>
Create `.planning/phases/90-cors-edge-migration/90-01-SUMMARY.md` when done
</output>
