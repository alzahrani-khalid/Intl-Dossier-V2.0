---
phase: 59-cosmetic-ci-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/tweaks/TweaksDrawer.test.tsx
  - .planning/phases/51-design-token-compliance-gate/51-VALIDATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [POLISH-02, POLISH-03, POLISH-04]
user_setup: []

must_haves:
  truths:
    - 'TweaksDrawer.test.tsx no longer carries the false claim that the global setup omits initReactI18next'
    - '51-VALIDATION.md frontmatter reads status: passed with nyquist_compliant: true preserved'
    - 'Local flip-test proves each eslint fixture genuinely fails today and would let CI fail loudly if neutralized'
    - 'ROADMAP criterion-4 and REQUIREMENTS POLISH-04 describe a lint/ESLint failure (not a test failure) for bad-vi-mock.ts'
  artifacts:
    - path: 'frontend/src/components/tweaks/TweaksDrawer.test.tsx'
      provides: 'TweaksDrawer live tests with stale mock-factory comment removed'
      contains: "vi.mock('react-i18next'"
    - path: '.planning/phases/51-design-token-compliance-gate/51-VALIDATION.md'
      provides: 'Phase 51 validation strategy with passed status'
      contains: 'status: passed'
    - path: '.planning/phases/59-cosmetic-ci-gap-closure/59-01-SUMMARY.md'
      provides: 'POLISH-04 flip-test evidence (before/after exit codes) + POLISH-02/03 record'
      contains: 'flip-test'
  key_links:
    - from: '.planning/ROADMAP.md'
      to: '.planning/REQUIREMENTS.md'
      via: 'matching POLISH-04 wording (lint/ESLint failure)'
      pattern: 'ESLint error|lint failure'
---

<objective>
Close the three non-gated POLISH items in a single autonomous plan: delete the stale
TweaksDrawer mock-factory comment (POLISH-02), flip the 51-VALIDATION frontmatter status
(POLISH-03), and verify-and-reconcile the two eslint positive-failure fixtures (POLISH-04).

Purpose: Correct documentation drift, finalize a draft validation doc, and prove the
existing Phase 55 CI fixture assertions still break loudly if a fixture stops failing —
without touching the CI jobs or the fixtures themselves.

Output: Two corrected docs, one corrected test comment, two reconciled oracle files, and a
SUMMARY capturing the POLISH-04 flip-test evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-cosmetic-ci-gap-closure/59-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Authoritative facts the executor needs — do not re-derive by exploration. -->

POLISH-02 source of truth (frontend/docs/test-setup.md §2 "The react-i18next mock contract", line 56):
"The global `react-i18next` mock must surface `initReactI18next` and every other real
package export" — i.e. the setup DOES register initReactI18next. The TweaksDrawer comment
that says the stub "omits `initReactI18next`" is therefore FALSE.

POLISH-02 stale comment (frontend/src/components/tweaks/TweaksDrawer.test.tsx lines 1-11):
A JSDoc block whose "NOTE ON MOCKING" body (lines 4-10) claims the global setup
"omits `initReactI18next`, `I18nextProvider`, and `Trans`". The vi.mock(...) call at
lines 16-19 is real test logic and MUST be preserved byte-for-byte.

POLISH-03 target (51-VALIDATION.md frontmatter, lines 1-8):
status: draft (line 4) → passed
nyquist_compliant: true (line 5) MUST remain unchanged. All other frontmatter lines
(phase, slug, wave_0_complete, created) preserved byte-for-byte.

POLISH-04 fixtures (UNCHANGED — flip-test edits are local-and-reverted only):
tools/eslint-fixtures/bad-design-token.tsx — raw hex '#3B82F6' (line 10), palette literal
className="bg-red-500" (line 11), template literal `text-red-500 ...` (line 12).
tools/eslint-fixtures/bad-vi-mock.ts — vi.mock factory at lines 8-10 omits the
`...(await vi.importActual('react-i18next'))` spread.

POLISH-04 CI jobs (.github/workflows/ci.yml lines 66-116 — UNCHANGED, Phase 55 commit faa8a710):
design-token-check → `! pnpm exec eslint -c eslint.config.mjs --max-warnings 0 tools/eslint-fixtures/bad-design-token.tsx`
i18next-factory-check → `! pnpm exec eslint -c eslint.config.mjs --max-warnings 0 tools/eslint-fixtures/bad-vi-mock.ts`
The leading `!` means: the CI step PASSES when eslint exits non-zero, and FAILS when
eslint exits 0. Phase 59 makes NO edits to these jobs (D-01, D-05).

POLISH-04 reconciliation targets:
ROADMAP.md §"Phase 59" Success Criterion 4 (line 243): "...and `bad-vi-mock.ts` produces a
test failure" → "...produces an ESLint error / lint failure".
REQUIREMENTS.md POLISH-04 (line 41): "...its expected lint/test failure" wording — align
the bad-vi-mock.ts portion to "lint failure" / "ESLint error".
Edit ONLY the POLISH-04 wording via direct prose edit. Do NOT touch any gsd:progress block,
checkbox markers, or the traceability table. Do NOT use any gsd-sdk roadmap mutation handler
(D-04 safety).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: POLISH-02 — delete the stale TweaksDrawer mock-factory comment</name>
  <read_first>
    - frontend/docs/test-setup.md §2 "The react-i18next mock contract" (line 56) — confirms the global mock surfaces initReactI18next, proving the comment false
    - frontend/src/components/tweaks/TweaksDrawer.test.tsx (full file) — the comment to delete and the vi.mock logic to preserve
  </read_first>
  <action>
    In frontend/src/components/tweaks/TweaksDrawer.test.tsx, remove the false "NOTE ON MOCKING"
    paragraph (lines 4-10) that claims the global stub "omits `initReactI18next`,
    `I18nextProvider`, and `Trans`". Keep the file-purpose line 2 ("TweaksDrawer live tests
    (THEME-01, Plan 34-04).") and the surrounding JSDoc fences if they still read coherently;
    if removing the body leaves an empty/dangling JSDoc block, remove the whole block (lines
    1-11) cleanly. Do NOT alter any line from `import { beforeEach, ... } from 'vitest'`
    onward, and do NOT change the vi.mock('react-i18next', ...) call at lines 16-19 — it is
    real test logic, not the stale comment. Confirm the false claim is gone against
    test-setup.md line 56 before saving.
  </action>
  <acceptance_criteria>
    - grep -n "omits" frontend/src/components/tweaks/TweaksDrawer.test.tsx returns nothing
    - grep -n "initReactI18next" frontend/src/components/tweaks/TweaksDrawer.test.tsx returns nothing
    - grep -c "vi.mock('react-i18next'" frontend/src/components/tweaks/TweaksDrawer.test.tsx returns 1 (mock logic preserved)
    - pnpm --filter frontend exec vitest run src/components/tweaks/TweaksDrawer.test.tsx exits 0 (test still compiles and runs)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/components/tweaks/TweaksDrawer.test.tsx</automated>
  </verify>
  <done>The false mock-factory comment is removed, vi.mock logic is untouched, and the test file still runs green.</done>
</task>

<task type="auto">
  <name>Task 2: POLISH-03 — flip 51-VALIDATION frontmatter status draft → passed</name>
  <read_first>
    - .planning/phases/51-design-token-compliance-gate/51-VALIDATION.md (lines 1-10) — the frontmatter block
  </read_first>
  <action>
    In .planning/phases/51-design-token-compliance-gate/51-VALIDATION.md, change line 4 from
    `status: draft` to `status: passed`. Preserve every other frontmatter field byte-for-byte:
    `phase: 51` (line 2), `slug: design-token-compliance-gate` (line 3),
    `nyquist_compliant: true` (line 5), `wave_0_complete: true` (line 6),
    `created: 2026-05-15` (line 7). Make NO other edits to the file body.
  </action>
  <acceptance_criteria>
    - grep -c "^status: passed$" .planning/phases/51-design-token-compliance-gate/51-VALIDATION.md returns 1
    - grep -c "^status: draft$" .planning/phases/51-design-token-compliance-gate/51-VALIDATION.md returns 0
    - grep -c "^nyquist_compliant: true$" .planning/phases/51-design-token-compliance-gate/51-VALIDATION.md returns 1
    - git diff --stat shows exactly 1 file changed with 1 insertion and 1 deletion
  </acceptance_criteria>
  <verify>
    <automated>grep -q "^status: passed$" .planning/phases/51-design-token-compliance-gate/51-VALIDATION.md && grep -q "^nyquist_compliant: true$" .planning/phases/51-design-token-compliance-gate/51-VALIDATION.md && ! grep -q "^status: draft$" .planning/phases/51-design-token-compliance-gate/51-VALIDATION.md</automated>
  </verify>
  <done>Frontmatter reads status: passed with nyquist_compliant: true and all other fields intact.</done>
</task>

<task type="auto">
  <name>Task 3: POLISH-04 — flip-test the two fixtures (local + reverted) and reconcile criterion wording</name>
  <read_first>
    - tools/eslint-fixtures/bad-design-token.tsx (full file) — the design-token violations to neutralize
    - tools/eslint-fixtures/bad-vi-mock.ts (full file) — the vi.mock factory missing the importActual spread
    - .github/workflows/ci.yml lines 66-116 — the two `! pnpm exec eslint` positive-failure jobs (DO NOT EDIT)
    - .planning/ROADMAP.md §"Phase 59" Success Criterion 4 (line 243) — reconciliation target
    - .planning/REQUIREMENTS.md POLISH-04 (line 41) — reconciliation target
  </read_first>
  <action>
    Part A — flip-test (local-and-reverted; D-02). For each fixture, first capture the
    baseline: run `pnpm exec eslint -c eslint.config.mjs --max-warnings 0 tools/eslint-fixtures/bad-design-token.tsx`
    and `... tools/eslint-fixtures/bad-vi-mock.ts` and record the exit codes (expected
    non-zero for both — proving the `!`-guarded CI step currently passes). Then NEUTRALIZE
    each fixture in place: in bad-design-token.tsx replace the raw hex `#3B82F6`, the
    `bg-red-500` palette literal, and the `text-red-500` template literal with token-compliant
    equivalents (e.g. var(--accent) / `bg-surface` / a literal with no palette class) so the
    D-05 selectors no longer fire; in bad-vi-mock.ts add the missing spread
    `...(await vi.importActual('react-i18next'))` into the factory return object (and make the
    factory async). Re-run the same two eslint commands and record the exit codes (expected 0
    for both — proving the `!`-guarded CI step would then FAIL, i.e. CI breaks loudly).
    Finally REVERT both fixtures with `git checkout -- tools/eslint-fixtures/bad-design-token.tsx tools/eslint-fixtures/bad-vi-mock.ts`
    and confirm both eslint commands exit non-zero again. NEVER commit the neutralized
    fixtures. Capture the four before/after exit codes in the plan SUMMARY as the POLISH-04
    deliverable evidence. Do NOT edit .github/workflows/ci.yml and do NOT strengthen the
    `! eslint` guard to be rule-specific (D-01, D-05).

    Part B — wording reconciliation (D-04; direct prose edit only). In ROADMAP.md
    Success Criterion 4 (line 243), change the phrase describing bad-vi-mock.ts from
    "produces a test failure" to "produces an ESLint error / lint failure". In
    REQUIREMENTS.md POLISH-04 (line 41), align the bad-vi-mock.ts portion of
    "expected lint/test failure" to read "lint failure" / "ESLint error" so both oracles match
    the lint-based reality. Edit ONLY the POLISH-04 wording. Do NOT touch the gsd:progress
    block, any checkbox marker, the traceability table, or use any gsd-sdk roadmap mutation
    handler.

  </action>
  <acceptance_criteria>
    - Baseline: pnpm exec eslint -c eslint.config.mjs --max-warnings 0 tools/eslint-fixtures/bad-design-token.tsx exits non-zero AND ... bad-vi-mock.ts exits non-zero
    - After neutralizing: both eslint commands exit 0 (evidence captured in SUMMARY)
    - After git checkout --: both eslint commands exit non-zero again
    - git status shows tools/eslint-fixtures/bad-design-token.tsx and bad-vi-mock.ts as unmodified (no staged/unstaged changes; neutralized versions NOT committed)
    - git diff .github/workflows/ci.yml is empty (CI jobs untouched)
    - grep -n "test failure" applied to ROADMAP.md line 243 region returns nothing for the bad-vi-mock.ts criterion; "ESLint error" or "lint failure" present
    - REQUIREMENTS.md POLISH-04 line no longer attributes a "test failure" to bad-vi-mock.ts
    - git diff .planning/ROADMAP.md touches only the criterion-4 prose line — no gsd:progress / checkbox / traceability-table lines changed
  </acceptance_criteria>
  <verify>
    <automated>! pnpm exec eslint -c eslint.config.mjs --max-warnings 0 tools/eslint-fixtures/bad-design-token.tsx && ! pnpm exec eslint -c eslint.config.mjs --max-warnings 0 tools/eslint-fixtures/bad-vi-mock.ts && git diff --quiet .github/workflows/ci.yml tools/eslint-fixtures/bad-design-token.tsx tools/eslint-fixtures/bad-vi-mock.ts</automated>
  </verify>
  <done>Flip-test evidence (4 exit codes) captured in SUMMARY; fixtures and CI jobs unmodified; ROADMAP + REQUIREMENTS POLISH-04 wording reconciled to lint/ESLint failure with no progress-marker churn.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary          | Description                                                                                                                                                                |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none introduced) | This plan is documentation edits, a test-comment deletion, and a local-only eslint flip-test. No auth, user input, network, or data-handling surface is added or modified. |

## STRIDE Threat Register

| Threat ID | Category                    | Component                                      | Disposition | Mitigation Plan                                                                                                                                                                     |
| --------- | --------------------------- | ---------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-59-01   | Tampering                   | tools/eslint-fixtures/\* during flip-test      | mitigate    | Flip-test edits are local-and-reverted (`git checkout --`); acceptance criteria assert `git status` shows fixtures unmodified — neutralized fixtures can never be committed (D-02). |
| T-59-02   | Integrity                   | ROADMAP.md gsd:progress / traceability markers | mitigate    | D-04 prose-only edit confined to criterion-4 wording; acceptance criteria assert no progress-marker / checkbox / traceability-table lines change; no roadmap mutation handler used. |
| T-59-03   | Denial of Service (CI gate) | .github/workflows/ci.yml Phase 55 jobs         | accept      | Jobs are explicitly NOT edited (D-01, D-05); `git diff` of ci.yml asserted empty. No risk to the 8 live `main` required contexts from this plan.                                    |
| T-59-SC   | Tampering                   | npm/pip/cargo installs                         | accept      | No package installs in this plan. No slopcheck checkpoint needed.                                                                                                                   |

</threat_model>

<verification>
- POLISH-02: TweaksDrawer.test.tsx no longer claims initReactI18next is omitted; vi.mock logic intact; test runs green.
- POLISH-03: 51-VALIDATION.md frontmatter status: passed, nyquist_compliant: true preserved.
- POLISH-04: both fixtures fail lint today; flip-test proved CI would break if neutralized; fixtures + CI jobs unchanged; ROADMAP + REQUIREMENTS wording reconciled to lint/ESLint failure.
</verification>

<success_criteria>

- ROADMAP Success Criterion 2 satisfied (TweaksDrawer comment corrected).
- ROADMAP Success Criterion 3 satisfied (51-VALIDATION status: passed).
- ROADMAP Success Criterion 4 satisfied (fixture positive-failure assertions verified via flip-test; criterion wording reconciled to lint/ESLint).
- No edits to CI jobs, fixtures (committed state), or roadmap progress markers.
  </success_criteria>

<output>
Create `.planning/phases/59-cosmetic-ci-gap-closure/59-01-SUMMARY.md` when done.
Record the POLISH-04 flip-test evidence inline: the four eslint exit codes
(baseline non-zero ×2, neutralized 0 ×2) and confirmation that fixtures were reverted
and never committed.
</output>
