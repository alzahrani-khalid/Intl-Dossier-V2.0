---
phase: 102-staging-data-debt-tail
plan: 15
type: execute
wave: 2
depends_on: []
files_modified:
  - supabase/seed/072-p102-today-relative-dashboard-fixtures.sql
  - frontend/tests/e2e/dashboard-widgets-visual.spec.ts
  - .planning/phases/102-staging-data-debt-tail/102-15-SUMMARY.md
autonomous: true
requirements: [CARRY-06]
success_criterion: "The dashboard visual snapshots survive a date change: the three b0000002 engagements and five b0000006 calendar fixtures fall inside [CURRENT_DATE, CURRENT_DATE+14] after the idempotent re-anchor seed runs, the spec re-anchors them in beforeAll and computes FROZEN_TIME as today 12:00Z, and a test titled `dashboard snapshots survive a date change` captures week-ahead under today's clock and today+1 with date labels masked and both match the committed baseline."
routing:
  floor: frontier
  pin: {via: claude-code, model: opus} # OVERSEER ruling P102-Q2 2026-09-11 (same class as P101 a554c0a4d): the codex sandbox refuses the wrapped Playwright run (pre-spawn port census unavailable -> 90); this task must RUN Playwright
truths:
  - "Decisions covered - D-20: option B - an idempotent supabase/seed SQL re-anchors the b0000002 engagement and b0000006 calendar fixture dates to CURRENT_DATE-relative offsets (the same offsets 060-dashboard-demo.sql uses), the spec computes FROZEN_TIME at run time and masks date-label regions, the FIXTURE_BLOCKED guard for week-ahead is removed, and option A (p_now on two SECURITY DEFINER RPCs) is rejected; regenerating baselines is not a fix; D-03: the browser oracle is not drilled tonight and runs through the reaped wrapper as a command oracle; D-02: the drillable control asserts 3 of 3 and 5 of 5 fixtures inside the window."
must_haves:
  truths:
    - oracle: command
      command: |-
        PATH="/opt/homebrew/bin:$PATH"; set -a; . ./.env.test 2>/dev/null; set +a; command -v psql >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: psql absent"; exit 3; }; [ -n "${SUPABASE_DB_URL:-}" ] || { echo "INSTRUMENT-CANNOT-RUN: SUPABASE_DB_URL unset - .env.test not materialised in this worktree"; exit 3; }; PQ() { PSQL_OUT=$(psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 -c "$1" 2>&1); PSQL_RC=$?; [ "$PSQL_RC" = "0" ] || { echo "INSTRUMENT-CANNOT-RUN: psql exited $PSQL_RC for $2 :: $PSQL_OUT"; exit 3; }; }; PQ "select (select count(*) from engagement_dossiers where id::text like 'b0000002-%' and start_date::date between current_date and current_date + 14 and end_date > start_date)||' '||(select count(*) from calendar_entries where id::text like 'b0000006-%' and event_date between current_date and current_date + 14)||' '||(select count(*) from engagement_dossiers where id::text like 'b0000002-%')||' '||(select count(*) from calendar_entries where id::text like 'b0000006-%')" "fixture date window"; set -- $PSQL_OUT; echo "P102-15-WINDOW engagements_in_window=$1 calendar_in_window=$2 engagements_present=$3 calendar_present=$4 expected 3 5 3 5 (window = [CURRENT_DATE, CURRENT_DATE+14])"; [ "$3" = "3" ] && [ "$4" = "5" ] || { echo "INSTRUMENT-CANNOT-RUN: the b0000002/b0000006 fixture family is not 3+5 rows ($3+$4) - the seed population moved"; exit 3; }; [ "$1" = "3" ] && [ "$2" = "5" ] || { echo "FAIL: $1 of 3 fixture engagements and $2 of 5 fixture calendar entries fall inside the next 14 days - the dashboard fixtures are anchored to a past capture date"; exit 1; }; echo "PASS window"
      text: "the drillable control: 3 of 3 b0000002 engagements have start_date inside [CURRENT_DATE, CURRENT_DATE+14] with end_date > start_date and 5 of 5 b0000006 calendar entries have event_date inside the window, with the 3+5 population as the control. RED at HEAD: 0 0 3 5 (dates anchored to 2026-07-03 and 2026-04-30). Exit 3 if the fixture family is not 3+5."
    - oracle: command
      command: |-
        PATH="/opt/homebrew/bin:$HOME/bin:$PATH"; ROOT=$(git rev-parse --show-toplevel 2>/dev/null) || { echo "INSTRUMENT-CANNOT-RUN: not in a git tree"; exit 3; }; cd "$ROOT" || exit 3; for t in node pnpm python3; do command -v $t >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: $t absent"; exit 3; }; done; set -a; . ./.env.test 2>/dev/null; set +a; [ -n "${TEST_USER_EMAIL:-}" ] && [ -n "${TEST_USER_PASSWORD:-}" ] || { echo "INSTRUMENT-CANNOT-RUN: TEST_USER_EMAIL/PASSWORD unset - .env.test not materialised"; exit 3; }; [ "${PW_REUSE:-}" != "1" ] || { echo "INSTRUMENT-CANNOT-RUN: PW_REUSE=1 would reuse a stray server"; exit 3; }; PWRUN() { PWDIR="$1"; shift; PWMARK=$(mktemp); PWOUT=$(mktemp); if [ -f scripts/pw-run-reaped.mjs ]; then ( cd "$PWDIR" && node "$( [ "$PWDIR" = frontend ] && echo ../scripts/pw-run-reaped.mjs || echo scripts/pw-run-reaped.mjs )" -- "$@" ) >"$PWOUT" 2>&1; PWRC=$?; PWREP=$(find "$PWDIR/test-results" -maxdepth 1 -name "pw-reaped-*.json" -newer "$PWMARK" 2>/dev/null | head -1); else echo "  WARN: scripts/pw-run-reaped.mjs absent - running playwright unleased"; ( cd "$PWDIR" && pnpm exec playwright test --reporter=json "$@" ) >"$PWOUT" 2>&1; PWRC=$?; PWREP="$PWOUT"; fi; [ -n "$PWREP" ] && [ -s "$PWREP" ] || { echo "INSTRUMENT-CANNOT-RUN: no playwright report was published (wrapper exit $PWRC) :: $(tail -c 300 "$PWOUT")"; exit 3; }; PWSTATS=$(python3 -c "import sys,json,re; s=open(sys.argv[1]).read(); i=s.find(\"{\"); d=json.loads(s[i:]); st=d.get(\"stats\",{}); print(\"expected=%s unexpected=%s skipped=%s flaky=%s\" % (st.get(\"expected\",\"?\"),st.get(\"unexpected\",\"?\"),st.get(\"skipped\",\"?\"),st.get(\"flaky\",\"?\")))
        def walk(s):
            for sp in s.get(\"specs\",[]):
                for t in sp.get(\"tests\",[]):
                    for r in t.get(\"results\",[]):
                        if r.get(\"status\")!=\"passed\": print(\"  PW\", r.get(\"status\"), \"|\", sp.get(\"title\"), \"|\", ((r.get(\"error\") or {}).get(\"message\",\"\"))[:200].replace(\"\\n\",\" \"))
            for c in s.get(\"suites\",[]): walk(c)
        for s in d.get(\"suites\",[]): walk(s)" "$PWREP" 2>&1); rm -f "$PWMARK"; PWPASS=$(printf "%s\n" "$PWSTATS" | head -1 | grep -oE "expected=[0-9]+" | cut -d= -f2); PWFAIL=$(printf "%s\n" "$PWSTATS" | head -1 | grep -oE "unexpected=[0-9]+" | cut -d= -f2); PWSKIP=$(printf "%s\n" "$PWSTATS" | head -1 | grep -oE "skipped=[0-9]+" | cut -d= -f2); printf "%s\n" "$PWSTATS" | tail -n +2 | head -12; [ -n "$PWPASS" ] && [ -n "$PWFAIL" ] || { echo "INSTRUMENT-CANNOT-RUN: the playwright report carried no stats :: $(head -c 200 "$PWREP")"; exit 3; }; }; SPEC=frontend/tests/e2e/dashboard-widgets-visual.spec.ts; [ -f "$SPEC" ] || { echo "INSTRUMENT-CANNOT-RUN: $SPEC absent"; exit 3; }; grep -q "dashboard snapshots survive a date change" "$SPEC" || { echo "FAIL: the spec carries no test titled 'dashboard snapshots survive a date change' - the two-clock invariance test was not authored"; exit 1; }; PWRUN frontend e2e/dashboard-widgets-visual.spec.ts --project=chromium-dashboard-widgets -g "dashboard snapshots survive a date change|visual week-ahead"; echo "P102-15-VISUAL wrapper_rc=$PWRC passed=$PWPASS failed=$PWFAIL skipped=${PWSKIP:-0} expected passed=2 failed=0 skipped=0 (week-ahead matches its committed baseline under today's clock AND under today+1 with date labels masked, both from a seed the spec re-anchors in beforeAll)"; [ "$PWPASS" = "2" ] && [ "$PWFAIL" = "0" ] && [ "${PWSKIP:-0}" = "0" ] || { echo "FAIL: the week-ahead visual is not invariant to the calendar date - a skip means the FIXTURE_BLOCKED guard still fires, a failure means the two clocks disagree"; exit 1; }; echo "PASS visual-invariant"
      text: "the two-clock invariance run through the reaped wrapper (project chromium-dashboard-widgets, -g on the new test and `visual week-ahead`): expected=2 unexpected=0 skipped=0 - the existing week-ahead visual no longer skips on FIXTURE_BLOCKED, and the new test's two captures (today 12:00Z and today+1 12:00Z, date labels masked) both match the committed baseline. RED at HEAD: the spec has no test with that title (exit 1); the browser leg was not exercised tonight (brief rule 4)."
    - oracle: judge
      text: "The DIFF cites: the seed file's UPDATE statements for the eight ids using date_trunc/NOW()/CURRENT_DATE offsets identical to 060-dashboard-demo.sql :205-216 and :251-262 (no INSERTs, no other rows, and no free text that matches 102-06's class regex); in the spec, FROZEN_TIME derived from `new Date()` truncated to 12:00Z, a beforeAll that applies the same eight updates through a service-role supabase client (SUPABASE_SERVICE_ROLE_KEY from .env.test) and fails loudly without it, the FIXTURE_BLOCKED entry for week-ahead removed with its comment replaced by the new mechanism's explanation, and a new test titled exactly `dashboard snapshots survive a date change` that installs the clock twice (today 12:00Z, today+1 12:00Z) and before EACH `toHaveScreenshot('week-ahead.png', {{ mask }})` asserts `await expect(widget.getByText('Bilateral consultation — ESCWA')).toBeVisible()`, `await expect(widget.getByText('Prep session — G20 Data Gaps Initiative')).toBeVisible()` and `await expect(widget.getByText('Delegation visit — Indonesia BPS')).toBeVisible()` (the three b0000002 engagement titles as read from staging on 2026-09-10), so the capture cannot be an empty or masked-over widget. The `mask:` option is EXACTLY `[widget.locator('.week-date')]` - the date/weekday/time column of each `.week-row` (WeekAhead.tsx :66-73) and nothing else; masking `.week-row`, `.week-title`, `.week-body`, the widget root, or any locator broader than `.week-date` is a failure of this criterion. The committed week-ahead baseline is NOT regenerated in this plan under any condition (roadmap CARRY-06: regenerating baselines is not a fix); if the two captures do not match the committed baseline, the SUMMARY records the pixel-diff artefact path and the plan ends ok:false."
---

<objective>
Remove the frozen-clock vs server-NOW() divergence so the dashboard visual snapshots stop rotting daily, without regenerating baselines as the fix.

The seed dates are static once seeded, the browser clock is frozen to a constant, and `get_upcoming_events` filters by real `NOW()` (research §8.1). Ruled option B (102-CONTEXT.md D-20): move the seed to today-relative on every run and let the frozen clock follow today, masking the date labels the snapshot would otherwise diff on.
</objective>

<context>
@.planning/phases/102-staging-data-debt-tail/102-CONTEXT.md
@.planning/phases/102-staging-data-debt-tail/102-RESEARCH.md
@frontend/tests/e2e/dashboard-widgets-visual.spec.ts
@frontend/playwright.config.ts

Ref notes: 102-CONTEXT.md D-20, D-03, D-02; 102-RESEARCH.md §8.1 (the mechanism named in the spec itself, the RPC filters, today's seed dates), §8.2 (baseline dirs and the chromium-dashboard-widgets snapshot path template), §8.3 (options A/B). 060-dashboard-demo.sql :205-216 and :251-262 carry the offsets to reuse (read it from the worktree; it is owned by 102-06 and is deliberately not a context ref here): engagements +2h/+4h from date_trunc('hour', NOW()), +1 day 10:00/12:00, +2 days 14:00/+4 days 16:00; calendar CURRENT_DATE +0, +1, +2, +3, +5.

<interfaces>

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>today-relative re-anchor seed, run-time FROZEN_TIME, masked date labels, two-clock invariance test</name>
  <files>the paths in files_modified</files>
  <read_first>102-CONTEXT.md D-20; research §8 in full; the spec's FROZEN_TIME comment (:5-11) and FIXTURE_BLOCKED block (:97-105); the 060 seed offsets; frontend/playwright.config.ts :80-92 (snapshot path template).</read_first>
  <action>
1. Author `supabase/seed/072-p102-today-relative-dashboard-fixtures.sql`: UPDATE engagement_dossiers SET start_date/end_date for the three b0000002 ids and UPDATE calendar_entries SET event_date for the five b0000006 ids (dates only - titles, descriptions and every other text column untouched; any free text you do write must not match 102-06's class regex: no `E2E`, `UAT`, `Phase <n>`, `staging verification`, `fixture`), offsets copied from 060 (+2h/+4h today; +1d 10:00/12:00; +2d 14:00/+4d 16:00; CURRENT_DATE +0/+1/+2/+3/+5). Apply with `PATH="/opt/homebrew/bin:$PATH"; set -a; . ./.env.test; set +a; psql "$SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f <file>` twice.
2. Edit the spec as the judge item specifies: mask ONLY `widget.locator('.week-date')`, assert the three fixture titles visible before each capture, never regenerate the baseline. The beforeAll performs the same eight updates via `@supabase/supabase-js` with the service-role key (the spec cannot run psql), so the invariance holds on any day the engine runs it.
3. Run both command oracles from the repo root; record verbatim (a stray 5173 listener makes the wrapper oracle exit 3 - clear it first).
  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim in the SUMMARY, and no path outside files_modified changes.</acceptance_criteria>
  <done>The re-anchored fixtures sit inside the 14-day window, the week-ahead visual no longer skips, and the two-clock invariance test passes against the committed baseline.</done>
</task>

</tasks>

<verification>
POPULATION: the seed, the spec, the eight fixture rows, the week-ahead baseline. OUTSIDE IT: the other seven dashboard widgets' baselines (unchanged - their FIXTURE_BLOCKED state, if any, is recorded), the two SECURITY DEFINER RPCs (option A rejected), calendar-rtl.spec.ts's own clock (102-06 touches its names only), and dossier-drawer specs' 2026-04-26 clocks (not in CARRY-06).
</verification>

<success_criteria>
The dashboard visual snapshots survive a date change: the three b0000002 engagements and five b0000006 calendar fixtures fall inside [CURRENT_DATE, CURRENT_DATE+14] after the idempotent re-anchor seed runs, the spec re-anchors them in beforeAll and computes FROZEN_TIME as today 12:00Z, and a test titled `dashboard snapshots survive a date change` captures week-ahead under today's clock and today+1 with date labels masked and both match the committed baseline.
</success_criteria>

<output>
Create `.planning/phases/102-staging-data-debt-tail/102-15-SUMMARY.md` when done. It MUST record every command this task ran with its
VERBATIM output in execution order, the populations it re-derived with the commands that produced them, every
zero beside the control that proves the instrument could have seen a non-zero, the verbatim output of every
command oracle above run AFTER the work, and anything left for a named later task.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete`.
Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as
**pending** no matter how many gates passed (measured on P99-32 and again on P99-61).
</output>
