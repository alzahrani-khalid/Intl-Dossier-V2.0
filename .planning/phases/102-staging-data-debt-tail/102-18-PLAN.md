---
phase: 102-staging-data-debt-tail
plan: 18
type: execute
wave: 3
depends_on: [102-03, 102-06, 102-07, 102-08, 102-09, 102-10, 102-11, 102-12, 102-13, 102-14, 102-15, 102-16, 102-17] # OVERSEER ruling P102-Q1 2026-09-11: D-04 says the purge is the LAST wave, but graph.json carries no wave field and the engine schedules by deps only - so every non-closing task is a dep; only 102-19 follows
files_modified:
  - scripts/p102-purge-fixture-accounts.mjs
  - .planning/phases/102-staging-data-debt-tail/102-18-SUMMARY.md
autonomous: true
requirements: [DATA-01]
success_criterion: "auth.users, public.users and public.profiles each hold exactly the 13 enumerated real accounts and all 13 keep-list emails are present, after a script that exported every row it would delete, re-ran the blocking-FK census and aborted on any non-zero, and deleted the @example.com / @gastat.test / @example.test population through GoTrue admin deleteUser printing kept/deleted/cascade magnitudes."
routing:
  floor: frontier
truths:
  - "Decisions covered - D-04: the purge runs AFTER Phase 101 closes - read_first checks the 101-*-SUMMARY.md set and the P101 acceptance note before executing, and autonomous:false is NOT used for this; D-05: population = the two named suffixes plus @example.test, keep-list = the 13 named emails asserted PRESENT; D-06: GoTrue admin deleteUser per row via the service-role key, preceded by the §17 CSV export to a dated directory outside the worktree, with the blocking-FK census re-run and any non-zero aborting; D-26: export first, magnitudes not verdicts; D-28: no summary pre-exists."
must_haves:
  truths:
    - oracle: command
      command: |-
        PATH="/opt/homebrew/bin:$PATH"; set -a; . ./.env.test 2>/dev/null; set +a; command -v psql >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: psql absent"; exit 3; }; [ -n "${SUPABASE_DB_URL:-}" ] || { echo "INSTRUMENT-CANNOT-RUN: SUPABASE_DB_URL unset - .env.test not materialised in this worktree"; exit 3; }; PQ() { PSQL_OUT=$(psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 -c "$1" 2>&1); PSQL_RC=$?; [ "$PSQL_RC" = "0" ] || { echo "INSTRUMENT-CANNOT-RUN: psql exited $PSQL_RC for $2 :: $PSQL_OUT"; exit 3; }; }; PQ "select (select count(*) from auth.users)||' '||(select count(*) from auth.users where email in ('kazahrani@stats.gov.sa','test.user@gmail.com','admin@gastat.gov.sa','test@gastat-intake.local','mfmuhanna@gstats.gov.sa','jfafnan@stats.gov.sa','akhorayef@stats.gov.sa','hmghulaiga@stats.gov.sa','aabalobaid@stats.gov.sa','ashaibani@stats.gov.sa','analyst@e2e.test','intake@e2e.test','admin@e2e.test'))||' '||(select count(*) from public.users)||' '||(select count(*) from public.profiles)" "purge end state"; set -- $PSQL_OUT; echo "P102-18-PURGE auth_users=$1 keep_list_present=$2 public_users=$3 profiles=$4 expected 13 13 13 13"; [ "$2" = "13" ] || { echo "FAIL: only $2 of the 13 enumerated real accounts survive - the purge touched the keep-list"; exit 1; }; [ "$1" = "13" ] && [ "$3" = "13" ] && [ "$4" = "13" ] || { echo "FAIL: auth.users=$1 public.users=$3 profiles=$4 - fixture accounts (or their mirror rows) remain beyond the 13 real accounts"; exit 1; }; echo "PASS purge"
      text: "the positive end state: auth.users == 13, all 13 keep-list emails present, public.users == 13, profiles == 13. RED at HEAD: 415 13 415 415. The keep-list count is the discriminator that a purge which over-deleted would fail; the 415s are what an un-run purge shows."
    - oracle: command
      command: |-
        PATH="/opt/homebrew/bin:$PATH"; set -a; . ./.env.test 2>/dev/null; set +a; command -v psql >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: psql absent"; exit 3; }; [ -n "${SUPABASE_DB_URL:-}" ] || { echo "INSTRUMENT-CANNOT-RUN: SUPABASE_DB_URL unset - .env.test not materialised in this worktree"; exit 3; }; OV=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0/.tickmarkr/overseer; [ -d "$OV" ] || { echo "INSTRUMENT-CANNOT-RUN: $OV absent - not on the planning machine"; exit 3; }; D=$(ls -d "$OV"/p102-prepurge-*/ 2>/dev/null | sort | tail -1); [ -n "$D" ] || { echo "P102-18-EXPORT export_dir=absent"; echo "FAIL: no p102-prepurge-* export directory exists under $OV - the purge script never exported before deleting (or never ran)"; exit 1; }; CSV="$D/auth_users_fixtures.csv"; MAG="$D/magnitudes.txt"; [ -f "$CSV" ] && [ -f "$MAG" ] || { echo "P102-18-EXPORT export_dir=$D csv=$([ -f "$CSV" ] && echo present || echo absent) magnitudes=$([ -f "$MAG" ] && echo present || echo absent)"; echo "FAIL: the export directory lacks auth_users_fixtures.csv or magnitudes.txt"; exit 1; }; ROWS=$(($(wc -l < "$CSV") - 1)); DEL=$(grep -oE '^deleted=[0-9]+' "$MAG" | cut -d= -f2); FD=$(grep -oE '^first_delete_epoch=[0-9]+' "$MAG" | cut -d= -f2); CM=$(stat -f %m "$CSV" 2>/dev/null || stat -c %Y "$CSV"); PQ() { PSQL_OUT=$(psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 -c "$1" 2>&1); PSQL_RC=$?; [ "$PSQL_RC" = "0" ] || { echo "INSTRUMENT-CANNOT-RUN: psql exited $PSQL_RC for $2 :: $PSQL_OUT"; exit 3; }; }; PQ "select count(*) from auth.users where email ilike '%@example.com' or email ilike '%@gastat.test' or email ilike '%@example.test'" "population left"; LEFT=$PSQL_OUT; echo "P102-18-EXPORT export_dir=$D csv_rows=$ROWS deleted=[$DEL] csv_mtime=$CM first_delete_epoch=[$FD] population_left=$LEFT expected csv_rows==deleted>=1 csv_mtime<=first_delete_epoch population_left=0"; [ -n "$DEL" ] && [ -n "$FD" ] || { echo "FAIL: magnitudes.txt carries no deleted=/first_delete_epoch= lines - the script did not print its magnitudes"; exit 1; }; [ "$ROWS" -ge 1 ] && [ "$ROWS" = "$DEL" ] || { echo "FAIL: the CSV holds $ROWS data rows but the script deleted $DEL - the export does not cover the delete set"; exit 1; }; [ "$CM" -le "$FD" ] || { echo "FAIL: the CSV was written at $CM, after the first delete at $FD"; exit 1; }; [ "$LEFT" = "0" ] || { echo "FAIL: $LEFT fixture accounts remain in the purge population"; exit 1; }; echo "PASS export-before-delete"
      text: "the export-before-delete proof, independent of the script's own report: the newest /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0/.tickmarkr/overseer/p102-prepurge-<utc>/ directory holds auth_users_fixtures.csv whose data-row count equals the `deleted=` magnitude in magnitudes.txt (>=1; expected 402 today), the CSV's mtime is not after `first_delete_epoch=` from the same file (`-le`, rev-2 G-05: whole-second granularity - the CSV is closed and fsynced before the first delete, so a tie is still not-after), and the purge population left in auth.users is 0. The row count is `wc -l` minus the header, so the script MUST terminate the CSV with a trailing newline (G-05b). RED at HEAD (re-drilled rev-1 2026-09-10): export_dir=absent -> exit 1. Exit 3 without .env.test or off the planning machine (the overseer directory is outside every worktree by design, D-26)."
    - oracle: judge
      text: "The DIFF cites the script: it reads .env.test (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_DB_URL), refuses to run without all three, exports auth.users rows matching the three suffixes to `/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0/.tickmarkr/overseer/p102-prepurge-<YYYYmmddTHHMMSSZ>/auth_users_fixtures.csv` (header + one data row per account to be deleted, the file terminated by a trailing newline so `wc -l` minus one is the data-row count) plus their public.users / profiles / mou_notification_preferences / user_notification_preferences / staff_profiles / user_roles rows to sibling CSVs in the same directory (path printed), on the UNCONDITIONAL run path before any delete (no flag, no swallowed catch - an export failure aborts the run), re-runs the research §1.6 generated blocking-FK census (built from pg_constraint, never typed) and exits non-zero with the offending fk names if any count is above 0, asserts every keep-list email is OUTSIDE the delete set before the first delete, deletes through auth.admin.deleteUser one id at a time with a running count, and prints `kept=<n> deleted=<n> cascade=<n> failed=<n>` at the end, writing the same line plus `first_delete_epoch=<unix seconds of the first deleteUser call>` and `export_completed_epoch=<unix seconds>` to `<export dir>/magnitudes.txt` (one `key=value` per line - the export oracle reads it). The SUMMARY records the P101 precondition check output, the export path and row counts, the FK census output (0 blocking), the final magnitudes line, and the oracle output."
---

<objective>
Purge the fixture accounts from staging through GoTrue after exporting them, leaving exactly the 13 enumerated real accounts.

This is criterion 1's first clause. It runs in the last repair wave, after Phase 101's suites are green (roadmap: purging first would fight the stabilization - 102-CONTEXT.md D-04), after 102-06 and 102-07 have removed the residue and stopped its growth, and after 102-03 seeded rows whose grantees are on the keep-list. The population is 402 today plus any `@example.test` rows (research §1.1, §1.4); the delete is irreversible, so the export precedes it (D-26).
</objective>

<context>
@.planning/phases/102-staging-data-debt-tail/102-CONTEXT.md
@.planning/phases/102-staging-data-debt-tail/102-RESEARCH.md
@.planning/phases/100-security-posture/100-01-PLAN.md

Ref notes: 102-CONTEXT.md D-04, D-05, D-06, D-26, D-28; 102-RESEARCH.md §1.1-§1.7 (population, the 13 named accounts, references, creators, FK fan-out and the generated census with its control, mechanisms), §17 (the export command). PRECONDITION (OVERSEER-corrected 2026-09-12: the original named a wrong path and a stale count): Phase 101 closed - `ls .planning/phases/101-ci-gates-green/101-*-SUMMARY.md` returns AT LEAST 7 files (10 on 2026-09-12) each with status: complete, and the overseer's P101 acceptance note is the TRACKED file `.planning/phases/101-ci-gates-green/101-ACCEPTANCE.md`, which must exist and end with the line `ACCEPTANCE-END` (its verdict is PARTIAL, accepted for sequencing; 101-06/07/11 are the operator's). Record the ls count and the tail line in the SUMMARY. If either is missing, STOP, record it, end ok:false.

<interfaces>

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>export, census, purge through GoTrue, assert the 13</name>
  <files>the paths in files_modified</files>
  <read_first>The P101 precondition (above) FIRST; 102-CONTEXT.md D-04/D-05/D-06/D-26; research §1 in full and §17; the keep-list of 13 emails in D-05 verbatim.</read_first>
  <action>
1. Author `scripts/p102-purge-fixture-accounts.mjs` (node, @supabase/supabase-js from the root node_modules, pg via psql child process for the census and export) exactly as the judge item specifies: export directory `/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0/.tickmarkr/overseer/p102-prepurge-$(date -u +%Y%m%dT%H%M%SZ)/` (absolute - it lives outside the worktree and is git-ignored), `auth_users_fixtures.csv` written (every row including the last terminated by `\n`) and fsynced BEFORE the first deleteUser, `magnitudes.txt` written at the end with `deleted=`, `first_delete_epoch=`, `export_completed_epoch=`. Keep-list (13): kazahrani@stats.gov.sa, test.user@gmail.com, admin@gastat.gov.sa, test@gastat-intake.local, mfmuhanna@gstats.gov.sa, jfafnan@stats.gov.sa, akhorayef@stats.gov.sa, hmghulaiga@stats.gov.sa, aabalobaid@stats.gov.sa, ashaibani@stats.gov.sa, analyst@e2e.test, intake@e2e.test, admin@e2e.test. Delete set: `email ILIKE '%@example.com' OR '%@gastat.test' OR '%@example.test'`.
2. Run it: `PATH="/opt/homebrew/bin:$PATH" node scripts/p102-purge-fixture-accounts.mjs` with cwd at the repo root; paste every line. Expected magnitudes today: kept=13 deleted=402 cascade=834 (402+402+402+22+4+2 - re-derive, the run may differ).
3. Run the command oracle; record verbatim. Re-run research §1.1's count block and paste it.
  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim in the SUMMARY, and no path outside files_modified changes.</acceptance_criteria>
  <done>auth.users, public.users and profiles hold exactly the 13 enumerated accounts, and the deleted rows exist as CSVs outside the worktree.</done>
</task>

</tasks>

<verification>
POPULATION: auth.users rows carrying one of the three suffixes and their six CASCADE child tables. OUTSIDE IT: columns holding user ids without a declared FK (storage.objects.owner, jsonb payloads - research §19), production, the E2E_* GitHub secrets, and any account created by a backend test run between the census and the delete (the script's FK census aborts on a blocking row; a new CASCADE-only row is deleted with the rest).
</verification>

<success_criteria>
auth.users, public.users and public.profiles each hold exactly the 13 enumerated real accounts and all 13 keep-list emails are present, after a script that exported every row it would delete, re-ran the blocking-FK census and aborted on any non-zero, and deleted the @example.com / @gastat.test / @example.test population through GoTrue admin deleteUser printing kept/deleted/cascade magnitudes.
</success_criteria>

<output>
Create `.planning/phases/102-staging-data-debt-tail/102-18-SUMMARY.md` when done. It MUST record every command this task ran with its
VERBATIM output in execution order, the populations it re-derived with the commands that produced them, every
zero beside the control that proves the instrument could have seen a non-zero, the verbatim output of every
command oracle above run AFTER the work, and anything left for a named later task.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete`.
Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as
**pending** no matter how many gates passed (measured on P99-32 and again on P99-61).
</output>
