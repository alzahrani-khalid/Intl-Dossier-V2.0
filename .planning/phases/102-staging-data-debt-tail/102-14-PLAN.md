---
phase: 102-staging-data-debt-tail
plan: 14
type: execute
wave: 2
depends_on: []
files_modified:
  - frontend/src/routes/_protected/admin/preview-layouts.tsx
  - frontend/src/hooks/usePreviewLayouts.ts
  - frontend/src/types/preview-layout.types.ts
  - frontend/src/i18n/{en,ar}/preview-layouts.json
  - frontend/src/i18n/index.ts
  - frontend/src/routeTree.gen.ts
  - supabase/migrations/20260911000002_p102_drop_preview_layouts.sql
  - .planning/phases/102-staging-data-debt-tail/102-14-SUMMARY.md
autonomous: true
requirements: [PREVIEW-HOLLOW-01]
success_criterion: "The preview-layouts feature is gone end to end: the route, hook, types and i18n namespace are deleted from the frontend and the route tree regenerates without it, and on staging the 3 tables, 5 functions and 3 enum types from 20260115100001 are absent while the control relation public.dossiers is present."
routing:
  floor: frontier
truths:
  - "Decisions covered - D-16: delete, not finish - route, hook, i18n namespace (both locales + i18n/index.ts registration), types, and a drop migration for the 3 tables, 3 enum types, 5 functions, 4 triggers and 8 policies in dependency order, applied to staging; the catalog oracle asserts absence bounded by a named present control; D-24: the migration is 20260911000002, idempotent (IF EXISTS everywhere), applied twice; D-02: the oracle counts remaining objects against a present control."
must_haves:
  truths:
    - oracle: command
      command: |-
        PATH="/opt/homebrew/bin:$PATH"; set -a; . ./.env.test 2>/dev/null; set +a; command -v psql >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: psql absent"; exit 3; }; [ -n "${SUPABASE_DB_URL:-}" ] || { echo "INSTRUMENT-CANNOT-RUN: SUPABASE_DB_URL unset - .env.test not materialised in this worktree"; exit 3; }; PQ() { PSQL_OUT=$(psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 -c "$1" 2>&1); PSQL_RC=$?; [ "$PSQL_RC" = "0" ] || { echo "INSTRUMENT-CANNOT-RUN: psql exited $PSQL_RC for $2 :: $PSQL_OUT"; exit 3; }; }; PQ "select (select count(*) from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relkind='r' and c.relname in ('entity_preview_layouts','preview_layout_fields','user_preview_preferences'))||' '||(select count(*) from pg_proc p join pg_namespace n on n.oid=p.pronamespace where n.nspname='public' and p.proname in ('update_preview_layout_timestamp','enforce_single_default_layout','get_preview_layout','get_entity_layouts','set_default_layout'))||' '||(select count(*) from pg_type t join pg_namespace n on n.oid=t.typnamespace where n.nspname='public' and t.typname in ('preview_entity_type','preview_context','preview_field_type'))||' '||(select count(*) from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relkind='r' and c.relname='dossiers')" "preview catalog"; set -- $PSQL_OUT; echo "P102-14-CATALOG tables_remaining=$1 functions_remaining=$2 types_remaining=$3 control_dossiers=$4 expected 0 0 0 1"; [ "$4" = "1" ] || { echo "INSTRUMENT-CANNOT-RUN: the control relation public.dossiers is not visible - the catalog probe is blind"; exit 3; }; [ "$1" = "0" ] && [ "$2" = "0" ] && [ "$3" = "0" ] || { echo "FAIL: preview-layout objects survive on staging (tables=$1/3 functions=$2/5 types=$3/3) - the drop migration was not authored or not applied"; exit 1; }; echo "PASS preview-dropped"
      text: "the catalog end state: 0 of the 3 tables, 0 of the 5 functions and 0 of the 3 enum types remain, and public.dossiers is present (control). RED at HEAD: 3 5 3 1. Exit 3 if the control is not visible."
    - oracle: judge
      text: "The DIFF deletes preview-layouts.tsx, usePreviewLayouts.ts, preview-layout.types.ts and both preview-layouts.json files, removes the two imports and the two namespace registrations from i18n/index.ts, regenerates routeTree.gen.ts without the /admin/preview-layouts route, and the migration drops - in this order - the 4 triggers, the 8 policies (four preview_layouts_*, four *_org_isolation_* from 20260627000002), the 5 functions, the 3 tables, then the 3 enum types, every statement IF EXISTS. The SUMMARY records `git grep -n preview-layouts\\|usePreviewLayouts\\|entity_preview_layouts -- frontend/src backend/src supabase/functions` = 0 hits beside a positive control grep, both migration applies, and the 12 seed rows exported to a CSV path before the drop."
---

<objective>
Delete the unreachable, hollow preview-layouts feature end to end - route, hook, types, i18n namespace, and a drop migration for its database objects.

The route has zero inbound links and the table is read only by a hook whose sole importer is the route (REQUIREMENTS PREVIEW-HOLLOW-01, research §14). A route-only deletion would orphan the table, so the migration ships in the same plan (102-CONTEXT.md D-16).
</objective>

<context>
@.planning/phases/102-staging-data-debt-tail/102-CONTEXT.md
@.planning/phases/102-staging-data-debt-tail/102-RESEARCH.md
@supabase/migrations/20260115100001_entity_preview_layouts.sql
@supabase/migrations/20260627000002_sec_be_01_admin_rls_db_role.sql
@frontend/src/i18n/index.ts
@frontend/vite.config.ts

Ref notes: 102-CONTEXT.md D-16, D-24; 102-RESEARCH.md §14 (importers, DB objects, policies, the 12-row seed). The route tree is generated by @tanstack/router-plugin at build (vite.config.ts :32) - run `pnpm -C frontend build` (or the dev server once) to regenerate routeTree.gen.ts after deleting the route file.

<interfaces>

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>delete the feature and drop its objects</name>
  <files>the paths in files_modified</files>
  <read_first>102-CONTEXT.md D-16; research §14; the creating migration end to end (every object name); the org-isolation migration's four preview policies; i18n/index.ts :242-243, :394, :530.</read_first>
  <action>
1. Export the 12 `entity_preview_layouts` rows (`\copy ... to '/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0/.tickmarkr/overseer/p102-preview-layouts-<utc>.csv' csv header`) before anything else.
2. Delete the four frontend files and the i18n registrations; regenerate routeTree.gen.ts by running the frontend build; `git grep` the three names over frontend/src, backend/src, supabase/functions (expect 0) with a control grep (`git grep -c usePreviewLayouts` before the deletion).
3. Author the migration with every object named in dependency order, `IF EXISTS` on each, and apply with `PATH="/opt/homebrew/bin:$PATH"; set -a; . ./.env.test; set +a; psql "$SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f <file>` twice.
4. Run the command oracle; record verbatim.
  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim in the SUMMARY, and no path outside files_modified changes.</acceptance_criteria>
  <done>No preview-layouts route, hook, type or namespace remains in the frontend, and staging's catalog holds none of the feature's tables, functions or types while public.dossiers is present.</done>
</task>

</tasks>

<verification>
POPULATION: the eight files and the 3+5+3 (+4 triggers, 8 policies) database objects. OUTSIDE IT: `20260627000001_sec_helper_is_platform_admin.sql` (touches the table only by grant - the drop makes that moot), user_preview_preferences rows (0), and every other admin route.
</verification>

<success_criteria>
The preview-layouts feature is gone end to end: the route, hook, types and i18n namespace are deleted from the frontend and the route tree regenerates without it, and on staging the 3 tables, 5 functions and 3 enum types from 20260115100001 are absent while the control relation public.dossiers is present.
</success_criteria>

<output>
Create `.planning/phases/102-staging-data-debt-tail/102-14-SUMMARY.md` when done. It MUST record every command this task ran with its
VERBATIM output in execution order, the populations it re-derived with the commands that produced them, every
zero beside the control that proves the instrument could have seen a non-zero, the verbatim output of every
command oracle above run AFTER the work, and anything left for a named later task.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete`.
Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as
**pending** no matter how many gates passed (measured on P99-32 and again on P99-61).
</output>
