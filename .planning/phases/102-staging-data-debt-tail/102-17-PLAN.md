---
phase: 102-staging-data-debt-tail
plan: 17
type: execute
wave: 3
depends_on: [102-03, 102-05, 102-08, 102-09, 102-10, 102-11, 102-12, 102-13, 102-14]
files_modified:
  - frontend/vite.config.ts
  - frontend/.size-limit.json
  - frontend/src/**
  - .planning/phases/102-staging-data-debt-tail/102-17-SUMMARY.md
autonomous: true
requirements: [CARRY-07]
success_criterion: "The entry chunk is under budget and the budget is lowered: after a fresh build, pnpm -C frontend size-limit exits 0 and the configured `Initial JS (entry point)` limit is a measured value below 500 KB (the number the reduction actually achieved, rounded up to the next 4 kB), never 476 on faith."
routing:
  floor: frontier
truths:
  - "Decisions covered - D-21: build once at HEAD and record the number (516.25 kB gzipped on the 2026-09-10 dist, OVER the 500 KB budget - the roadmap's 493.71 does not reproduce), move growth out of the entry chunk by lazy routes / manualChunks on APP code not vendor, rebuild, and set the limit to the measured value rounded up to 4 kB; if it cannot get below 500 the plan says so and the limit stays 500 with the number recorded; D-27: this plan runs after every wave-2 plan that touches frontend/src or is read beside it (03, 05, 08-14) so the measurement includes their bundles and the frontend/src/** write glob never races a sibling."
must_haves:
  truths:
    - oracle: command
      command: |-
        PATH="/opt/homebrew/bin:$PATH"; [ -f frontend/.size-limit.json ] || { echo "INSTRUMENT-CANNOT-RUN: frontend/.size-limit.json absent - not at the repo root"; exit 3; }; for t in node pnpm; do command -v $t >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: $t absent"; exit 3; }; done; ls frontend/dist/assets/app-*.js >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: frontend/dist/assets/app-*.js absent - run pnpm -C frontend build first"; exit 3; }; E=$(node -e 'const j=require("./frontend/.size-limit.json");const e=j.find(x=>x.name==="Initial JS (entry point)");console.log(e?[e.limit,e.path,e.gzip===true].join("|"):"absent||")'); L=${E%%|*}; EP=${E#*|}; EPATH=${EP%%|*}; EGZ=${EP#*|}; LN=$(echo "$L" | grep -oE '^[0-9]+(\.[0-9]+)?'); AS=$(node frontend/scripts/assert-size-limit-matches.mjs 2>&1); ASRC=$?; echo "P102-17-SHAPE entry_path=[$EPATH] gzip=[$EGZ] assert_matches_exit=$ASRC expected [dist/assets/app-*.js] [true] 0"; [ "$EPATH" = "dist/assets/app-*.js" ] && [ "$EGZ" = "true" ] || { echo "FAIL: the entry-point check was re-pointed (path=$EPATH gzip=$EGZ) - the budget is not measuring the entry chunk gzipped"; exit 1; }; [ "$ASRC" = "0" ] || { printf "%s\n" "$AS" | sed "s/^/  AS /"; echo "FAIL: assert-size-limit-matches exited $ASRC - a budget pattern matches no built chunk"; exit 1; }; OUT=$(cd frontend && pnpm -s size-limit 2>&1); RC=$?; SZ=$(printf '%s\n' "$OUT" | grep -A3 'Initial JS' | grep -oE 'Size:? *[0-9.]+ kB' | head -1); echo "P102-17-BUDGET configured_limit=[$L] size_limit_exit=$RC entry=[$SZ] expected limit<500 KB and exit 0"; [ -n "$LN" ] || { echo "INSTRUMENT-CANNOT-RUN: entry-point limit unparseable ($L)"; exit 3; }; [ "$RC" = "0" ] || { printf "%s\n" "$OUT" | grep -B1 -A3 "Initial JS" | sed "s/^/  SL /"; echo "FAIL: size-limit exit $RC - the entry chunk exceeds the configured budget"; exit 1; }; node -e 'process.exit(parseFloat(process.argv[1])<500?0:1)' "$LN" || { echo "FAIL: entry-point limit is $L - the budget was not lowered below 500 KB"; exit 1; }; echo "PASS budget"
      text: "after a build exists in the worktree: the entry-point check still has path === dist/assets/app-*.js and gzip === true (re-pointing the budget at a smaller chunk or switching to brotli fails), `node frontend/scripts/assert-size-limit-matches.mjs` exits 0 (every budget pattern matches exactly one built chunk), the configured entry-point limit parses below 500 KB AND `pnpm -C frontend size-limit` exits 0 (all literals rendered). RED at HEAD on the on-disk dist (re-drilled rev-1 2026-09-10): shape ok (path/gzip/assert 0), limit 500 KB, size-limit exit 1, entry 516.25 kB. Exit 3 without a dist (the build gate provides it; a worker that never built cannot pass)."
    - oracle: judge
      text: "The DIFF cites the vite.config.ts change (route-level lazy imports or a manualChunks entry naming APP modules moved out of the entry - not a vendor regroup, not a raise of chunkSizeWarningLimit) and the .size-limit.json entry-point limit equal to the SUMMARY's measured post-build number rounded up to the next 4 kB; the SUMMARY records the HEAD build's size-limit table verbatim, the post-change table verbatim, the delta per named chunk, and `node frontend/scripts/assert-size-limit-matches.mjs` output. If the entry could not get under 500 KB the SUMMARY says so in its first line and the limit is left at 500 KB."
---

<objective>
Bring the entry chunk under its budget by moving app code out of it, then lower the budget to the number the build actually measures.

The on-disk dist measures 516.25 kB gzipped against a 500 KB limit, so the required CI check is red today and the roadmap's 493.71 does not reproduce (research §7). The target is measured, not assumed (102-CONTEXT.md D-21); this plan runs last among the frontend plans so the measurement is of the phase's final bundle.
</objective>

<context>
@.planning/phases/102-staging-data-debt-tail/102-CONTEXT.md
@.planning/phases/102-staging-data-debt-tail/102-RESEARCH.md
@frontend/vite.config.ts
@frontend/.size-limit.json
@frontend/scripts/assert-size-limit-matches.mjs

Ref notes: 102-CONTEXT.md D-21; 102-RESEARCH.md §7 (budget file, CI job, the measured 516.25, largest chunks); project memory: manualChunks on react-vendor once white-screened the app (scope regex to runtime) - do not regroup vendor.

<interfaces>

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>measure, move app code out of the entry, re-measure, lower the budget</name>
  <files>the paths in files_modified</files>
  <read_first>102-CONTEXT.md D-21; research §7; vite.config.ts manualChunks (:100-160 region) and chunkSizeWarningLimit; frontend/scripts/assert-size-limit-matches.mjs (what CI asserts about the budget file).</read_first>
  <action>
1. `pnpm -C frontend build && (cd frontend && pnpm size-limit)`; paste the table - this is the HEAD number for the phase's tree.
2. Identify the largest app-code contributors to `app-*.js` (`npx vite-bundle-visualizer` is NOT installed - use `--sourcemap` plus `source-map-explorer` if present, else read the rollup manifest at dist/.vite/manifest.json for the entry's imports) and lazy-load route-level pages or add a manualChunks group for the named app modules. Rebuild; re-run size-limit.
3. Set `.size-limit.json` entry limit to ceil(measured/4)*4 kB; run `node frontend/scripts/assert-size-limit-matches.mjs`.
4. Run the command oracle; record verbatim.
  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim in the SUMMARY, and no path outside files_modified changes.</acceptance_criteria>
  <done>size-limit exits 0 on a fresh build with the entry-point limit configured below 500 KB at the measured value.</done>
</task>

</tasks>

<verification>
POPULATION: the entry chunk, vite.config.ts, .size-limit.json. OUTSIDE IT: vendor chunk limits (unchanged), `chunkSizeWarningLimit` (a warning, not the gate), and the CI job definition.
</verification>

<success_criteria>
The entry chunk is under budget and the budget is lowered: after a fresh build, pnpm -C frontend size-limit exits 0 and the configured `Initial JS (entry point)` limit is a measured value below 500 KB (the number the reduction actually achieved, rounded up to the next 4 kB), never 476 on faith.
</success_criteria>

<output>
Create `.planning/phases/102-staging-data-debt-tail/102-17-SUMMARY.md` when done. It MUST record every command this task ran with its
VERBATIM output in execution order, the populations it re-derived with the commands that produced them, every
zero beside the control that proves the instrument could have seen a non-zero, the verbatim output of every
command oracle above run AFTER the work, and anything left for a named later task.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete`.
Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as
**pending** no matter how many gates passed (measured on P99-32 and again on P99-61).
</output>
