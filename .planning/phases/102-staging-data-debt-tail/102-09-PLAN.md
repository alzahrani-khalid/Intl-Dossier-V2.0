---
phase: 102-staging-data-debt-tail
plan: 9
type: execute
wave: 2
depends_on: [102-02, 102-08] # OVERSEER ruling P102-09: 08 and 09 both write the AppShell/Sidebar/*ListPage tests; ordered so neither's committed work is dropped (OBS-212)
files_modified:
  - frontend/src/i18n/{en,ar}/{common,assignments,dossiers,briefing-books}.json
  - frontend/src/pages/help/HelpPage.tsx
  - frontend/src/hooks/useBriefingBooks.ts
  - frontend/src/**/{AppShell.a11y.test.tsx,AppShell.test.tsx,Sidebar.test.tsx,CountriesListPage.test.tsx,ForumsListPage.test.tsx,OrganizationsListPage.test.tsx,TopicsListPage.test.tsx,WorkingGroupsListPage.test.tsx}   # OVERSEER ruling P102-09 2026-09-11 (run 0083 att0/att2): these tests hard-code the Title Case copy this task sentence-cases; the copy change owns re-casing the literals they assert (files[]-vs-oracle pin, RULING-222-36). The i18n patterns above were consolidated to stay within the compiler's 8-pattern / 24-surface caps
  - frontend/tests/component/{AfterActionForm,CommitmentList,DecisionList,EscalationDialog,FilterPanel,ReminderButton}.test.tsx
  - frontend/tests/accessibility/waiting-queue-a11y.test.tsx
  - .planning/phases/102-staging-data-debt-tail/102-09-SUMMARY.md
autonomous: true
requirements: [COPY-09]
success_criterion: "The namespaces common, assignments, dossiers hold Title Case only where 102-COPY09-CARVEOUTS.md says so (candidates == carved per namespace), every en key has an ar key, and the ar mirror was re-checked for every edited key."
routing:
  floor: frontier
truths:
  - "Decisions covered - D-17: this is COPY-09 lane 1 over common, assignments, dossiers (en+ar pairs), carve-outs enumerated before the pass by 102-02, oracle = the shipped instrument reporting candidates==carve-out count and an ar key per en key; D-27: these namespace files have exactly one writer in this wave."
must_haves:
  truths:
    - oracle: command
      command: |-
        PATH="/opt/homebrew/bin:$PATH"; [ -d frontend/src/i18n/en ] && [ -d .planning ] || { echo "INSTRUMENT-CANNOT-RUN: frontend/src/i18n/en absent - not at the repo root"; exit 3; }; command -v node >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: node absent"; exit 3; }; CV=.planning/phases/102-staging-data-debt-tail/102-COPY09-CARVEOUTS.md; [ -f scripts/titlecase-census.mjs ] && [ -f "$CV" ] || { echo "FAIL: scripts/titlecase-census.mjs or 102-COPY09-CARVEOUTS.md absent - P102-02 has not landed in this tree"; exit 1; }; OUT=$(node scripts/titlecase-census.mjs --carveouts "$CV" common assignments dossiers 2>&1); RC=$?; [ "$RC" = "0" ] || { echo "INSTRUMENT-CANNOT-RUN: census exited $RC :: $OUT"; exit 3; }; printf '%s\n' "$OUT" | grep -E '^NS ' | sed 's/^/  /'; N=$(printf '%s\n' "$OUT" | grep -cE '^NS '); G=$(printf '%s\n' "$OUT" | grep -E '^NS ' | awk '{ for(i=1;i<=NF;i++){ split($i,kv,"="); v[kv[1]]=kv[2] } if (v["candidates"]==v["carved"] && v["carved"]==v["carve_rows"] && v["ar_mirror"]==v["candidates"] && v["ar_missing_keys"]=="0") n++ } END { print n+0 }'); echo "P102-LANE namespaces=$N at_end_state=$G expected 3 3 (candidates==carved, carved==carve_rows, ar_mirror==candidates, ar_missing_keys=0)"; [ "$N" = "3" ] || { echo "INSTRUMENT-CANNOT-RUN: $N of 3 namespaces reported"; exit 3; }; [ "$G" = "3" ] || { echo "FAIL: $G of 3 namespaces reach the end state - a Title Case string survives outside the carve-out table, a carve-out row was lowercased (carved<carve_rows), or an en key has no ar key"; exit 1; }; echo "PASS lane"
      text: "the census over common, assignments, dossiers through scripts/titlecase-census.mjs with the carve-out table: every namespace reaches candidates==carved AND carved==carve_rows (every carve-out row is still a candidate - a lane that lowercases a ribbon or column header to reach the end state is caught), ar_mirror==candidates, ar_missing_keys=0. RED at HEAD: the instrument is absent (102-02 lands first); the planner's scratch census at HEAD reported common=225, assignments=128, dossiers=127 candidates with 0 carved. Re-drilled rev-1 2026-09-10: exit 1, same line; the carve_rows clause drilled on synthetic NS lines (carved=2 carve_rows=3 is NOT counted)."
    - oracle: judge
      text: "The DIFF touches only string VALUES in the named en files and the same keys' ar values where the Arabic needed a matching change (never a key rename, never a deletion of an ar plural suffix key); every string that stayed Title Case in en appears as a carve-out row; the SUMMARY lists the per-namespace before/after candidate counts from the instrument and the count of ar keys re-checked. For lane 1 the diff also shows HelpPage.tsx and useBriefingBooks.ts reading their copy through t() with the new keys present in both locales and no exclamation mark in the replacement strings. Test-side (ruling P102-09): every test hunk changes ONLY a string literal that this task's copy change superseded, to the new sentence-case value byte-for-byte - no test title changes, no assertion removed, weakened, skipped or made case-insensitive, no new fallback; a test hunk whose literal is not among this task's changed values is out of scope."
---

<objective>
Sentence-case the Title Case long tail in the common, assignments, dossiers namespaces, checking the Arabic mirror for every edited key.

COPY-09 lane 1 of four (102-CONTEXT.md D-17). The carve-out table from 102-02 is the population definition; the instrument reports candidates per namespace and this lane closes when every survivor is a carve-out row.
</objective>

<context>
@.planning/phases/102-staging-data-debt-tail/102-CONTEXT.md
@.planning/phases/102-staging-data-debt-tail/102-RESEARCH.md
@CLAUDE.md

Ref notes: 102-CONTEXT.md D-17, D-27; 102-RESEARCH.md §12; the carve-out table 102-COPY09-CARVEOUTS.md is PRODUCED by 102-02 (a dependency, so it is in this worktree - a created file cannot be a context ref); CLAUDE.md design rules (sentence case for titles and buttons; UPPERCASE only for classification ribbons, mono labels and table-column headers; no marketing voice).

<interfaces>

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>sentence-case pass over common, assignments, dossiers with ar mirror check</name>
  <files>the paths in files_modified</files>
  <read_first>102-COPY09-CARVEOUTS.md rows for these namespaces; the instrument's NS lines for them at HEAD (`node scripts/titlecase-census.mjs --carveouts <table> common assignments dossiers`); CLAUDE.md voice rules; where each edited key renders (grep the key) so a ribbon or column header is not lowercased by mistake.</read_first>
  <action>
1. Run the instrument for these namespaces and paste the NS lines (before).
2. For every candidate key not in the carve-out table: rewrite the en value in sentence case (first word capitalised, proper nouns kept), and read the ar value - change it only if the English change alters meaning; confirm the ar key exists. Never rename keys; never delete ar-only keys (plural suffixes).
3. If a candidate turns out to be a legitimate carve-out that 102-02 missed, STOP and record it in the SUMMARY as a carve-out gap for the orchestrator - do not edit 102-COPY09-CARVEOUTS.md from this plan (102-02 owns it).
   3b. Hardcoded literal sites (REQUIREMENTS COPY-09 dated note): `HelpPage.tsx` :166 and :168 (`How can we help you?` / subtitle, both locales inline) route through `useTranslation('common')` keys `help.title` / `help.subtitle` added to common.json en+ar; `useBriefingBooks.ts` :156-157 and :164-165 (`Starting briefing book generation...`, `Briefing book generated successfully!` and their ar) route through `t()` on the `briefing-books` namespace the hook already holds, with the exclamation mark dropped (CLAUDE.md bans it).
4. Run the command oracle and paste the NS lines (after).
  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim in the SUMMARY, and no path outside files_modified changes.</acceptance_criteria>
  <done>Every namespace in common, assignments, dossiers reaches candidates==carved with ar_missing_keys=0 through the shipped instrument.</done>
</task>

</tasks>

<verification>
POPULATION: the 3 namespace pairs and the two hardcoded-literal sites. OUTSIDE IT: every other namespace, the carve-out table (102-02), and components that render these keys.
</verification>

<success_criteria>
The namespaces common, assignments, dossiers hold Title Case only where 102-COPY09-CARVEOUTS.md says so (candidates == carved per namespace), every en key has an ar key, and the ar mirror was re-checked for every edited key.
</success_criteria>

<output>
Create `.planning/phases/102-staging-data-debt-tail/102-09-SUMMARY.md` when done. It MUST record every command this task ran with its
VERBATIM output in execution order, the populations it re-derived with the commands that produced them, every
zero beside the control that proves the instrument could have seen a non-zero, the verbatim output of every
command oracle above run AFTER the work, and anything left for a named later task.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete`.
Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as
**pending** no matter how many gates passed (measured on P99-32 and again on P99-61).
</output>
