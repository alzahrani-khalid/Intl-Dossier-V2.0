---
phase: 102-staging-data-debt-tail
plan: 16
type: execute
wave: 2
depends_on: []
files_modified:
  - .planning/quick/260530-{w2-data-entry-shared-primitives-dedupe,w3-data-entry-per-surface-ux,w4-data-entry-polish}/SUMMARY.md
  - .planning/phases/102-staging-data-debt-tail/102-ROUTE-DISPOSITIONS.md
  - .planning/phases/102-staging-data-debt-tail/102-16-SUMMARY.md
autonomous: true
requirements: [CARRY-08, ROUTE-ORPHAN-01]
success_criterion: "The three data-entry quick tasks carry a SUMMARY.md each with a Tasks table of 9, 9 and 7 rows (derived from their PLAN.md headings) and a verdict per row plus status front-matter, and 102-ROUTE-DISPOSITIONS.md records all 14 orphan routes with a class and a KEEP/DELETE ruling."
routing:
  floor: frontier
truths:
  - "Decisions covered - D-22: three SUMMARY.md files citing research §9's per-task checks, re-run at HEAD - with the task counts RE-DERIVED from the PLAN.md headings as 9/9/7 (25 tasks; research §9 said 24 and D-22 said 8/10/7 - neither reproduces) and W4-E5 recorded as DONE because the intake form and the intake quick form both resolve intake:actions.submitRequest for the idle label (research §9 read the pending-branch key), so no form is edited; D-18: ROUTE-ORPHAN-01 closes on a disposition record - 6 redirect shells KEEP, 7 data-rendering pages KEEP with EMPTY-ON-STAGING and the table each reads, /geographic-visualization KEEP, /help/commitments KEEP - with no route file changed."
must_haves:
  truths:
    - oracle: command
      command: |-
        PATH="/opt/homebrew/bin:$PATH"; [ -d .planning/quick ] || { echo "INSTRUMENT-CANNOT-RUN: .planning/quick absent - not at the repo root"; exit 3; }; R=""; OK=0; for pair in w2-data-entry-shared-primitives-dedupe:9 w3-data-entry-per-surface-ux:9 w4-data-entry-polish:7; do d=${pair%%:*}; want=${pair##*:}; f=".planning/quick/260530-$d/SUMMARY.md"; if [ -f "$f" ]; then rows=$(awk '/^## Tasks/{f=1;next} f&&/^## /{f=0} f&&/^\| *W[234]-[A-Z][0-9]+ *\|/' "$f" | grep -cE '\| *(DONE|PARTIAL|SUPERSEDED|RETIRED|UNIFIED)[A-Z-]* *\|'); st=$(grep -cE '^status: (complete|retired)' "$f"); else rows=absent; st=0; fi; R="$R $d=rows:$rows/$want,status:$st"; [ "$rows" = "$want" ] && [ "$st" = "1" ] && OK=$((OK+1)); done; echo "P102-16-QUICK$R expected rows 9/9/7 each with a verdict and status: complete|retired; at_end_state=$OK/3"; [ "$OK" = "3" ] || { echo "FAIL: $OK of 3 quick-task SUMMARY.md files exist with a ## Tasks table carrying every task (9/9/7, derived from the PLAN.md headings) with a verdict per row"; exit 1; }; echo "PASS quick-summaries"
      text: "the three SUMMARY.md files exist with `status: complete|retired` front-matter and a `## Tasks` table whose W2-/W3-/W4- rows number 9, 9 and 7 with a verdict token each. RED at HEAD: all three absent. Exit 3 outside the repo."
    - oracle: command
      command: |-
        PATH="/opt/homebrew/bin:$PATH"; [ -d .planning/phases/102-staging-data-debt-tail ] || { echo "INSTRUMENT-CANNOT-RUN: phase dir absent - not at the repo root"; exit 3; }; F=.planning/phases/102-staging-data-debt-tail/102-ROUTE-DISPOSITIONS.md; [ -f "$F" ] || { echo "FAIL: 102-ROUTE-DISPOSITIONS.md does not exist - the 14 orphan routes have no recorded disposition"; exit 1; }; N=0; for r in /contacts /countries /organizations /persons /working-groups /data-library /geographic-visualization /stakeholder-influence /workflow-automation /tasks/escalations /my-work/waiting /reports/scheduled /help/commitments /intake/queue; do grep -qE "^\| *\`$r\` *\| *(redirect-shell|renders-data-empty|renders-data|static) *\| *(KEEP|DELETE)[^|]*\|" "$F" && N=$((N+1)); done; echo "P102-16-ROUTES rows_with_class_and_ruling=$N expected 14"; [ "$N" = "14" ] || { echo "FAIL: $N of 14 routes carry a class (redirect-shell|renders-data-empty|renders-data|static) and a ruling (KEEP|DELETE) in the disposition record"; exit 1; }; echo "PASS dispositions"
      text: "102-ROUTE-DISPOSITIONS.md holds a table row for each of the 14 named routes with a class in {redirect-shell, renders-data-empty, renders-data, static} and a ruling in {KEEP, DELETE}. RED at HEAD: the file does not exist."
    - oracle: judge
      text: "Each quick SUMMARY's Tasks table cites, per row, the HEAD check from research §9 RE-RUN by this task with its output (a git grep count, a file existence), the verdict DONE / PARTIAL / SUPERSEDED with the superseding PR for W2-B5, and W4-E5's DONE verdict with the two `t()` sites quoted (the intake form at components/intake-form :490 `t('actions.submitRequest')` under useTranslation('intake'); the intake quick form at work-creation/forms :385 `t('intake:actions.submitRequest')`) and the pending-branch key named as the research misread; the disposition record carries, per route, the file, what it renders, the tables it reads with their staging row counts re-derived, the class, and the ruling with one sentence of reason (the alias contract is unknowable, so deletion is a product act - KEEP)."
---

<objective>
Write the three data-entry quick-task SUMMARY files and the 14-route disposition record, both by re-deriving the checks at HEAD.

CARRY-08 is satisfiable by summaries that cite the checks (research §9: 22 done, 1 partial, 1 superseded over what it counted as 24 tasks - the PLAN.md headings actually number 25: 9/9/7). ROUTE-ORPHAN-01 closes on a record, not deletions (102-CONTEXT.md D-18).
</objective>

<context>
@.planning/phases/102-staging-data-debt-tail/102-CONTEXT.md
@.planning/phases/102-staging-data-debt-tail/102-RESEARCH.md
@.planning/quick/260530-w2-data-entry-shared-primitives-dedupe/PLAN.md
@.planning/quick/260530-w3-data-entry-per-surface-ux/PLAN.md
@.planning/quick/260530-w4-data-entry-polish/PLAN.md
@scripts/inbound-link-classify.mjs

Ref notes: 102-CONTEXT.md D-22, D-18; 102-RESEARCH.md §9 (the per-task checks), §15 (the 14 routes classified with tables and row counts). W4-E5 correction: research §9 compared IntakeForm's idle key with IntakeQuickForm's PENDING key (`form.creating`); the idle keys are the same `intake:actions.submitRequest`.

<interfaces>

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>three quick summaries and the route disposition record</name>
  <files>the paths in files_modified</files>
  <read_first>The three PLAN.md files (count the `### `/`## ` task headings yourself: W2 A1-A4 + B1-B5 = 9, W3 I1-I3, D1, A1-A3, B1-B2 = 9, W4 E1-E7 = 7); research §9 and §15; frontend/src/components/intake-form/IntakeForm.tsx :486-492 and work-creation/forms/IntakeQuickForm.tsx :376-388.</read_first>
  <action>
1. For each quick dir author SUMMARY.md with front-matter `status: complete` (or `retired` if every row is SUPERSEDED), a `## Tasks` table `| task | check run at HEAD | output | verdict |` with one row per task id (`W2-A1` ... `W4-E7`), the check re-run (not copied) and the verdict token; W2-B5 SUPERSEDED (PR #37), W4-E5 DONE with the two sites quoted.
2. Author `102-ROUTE-DISPOSITIONS.md` in the phase dir: a table `| route | file | renders | reads (table = rows) | class | ruling | reason |` for the 14 routes, re-running `node scripts/inbound-link-classify.mjs` (unrestricted) to confirm 95/186 and the 14, and the staging row counts from research §15 re-derived with psql.
3. Run both command oracles; record verbatim.
  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim in the SUMMARY, and no path outside files_modified changes.</acceptance_criteria>
  <done>Three quick-task SUMMARY files with 9/9/7 verdict rows and status front-matter exist, and the disposition record carries all 14 routes with class and ruling.</done>
</task>

</tasks>

<verification>
POPULATION: the three summaries and the disposition record. OUTSIDE IT: any route file (none changes - D-18), the 2 P0s from PR #35 (not in these plans - research §9), and the other 81 zero-inbound routes already dispositioned elsewhere.
</verification>

<success_criteria>
The three data-entry quick tasks carry a SUMMARY.md each with a Tasks table of 9, 9 and 7 rows (derived from their PLAN.md headings) and a verdict per row plus status front-matter, and 102-ROUTE-DISPOSITIONS.md records all 14 orphan routes with a class and a KEEP/DELETE ruling.
</success_criteria>

<output>
Create `.planning/phases/102-staging-data-debt-tail/102-16-SUMMARY.md` when done. It MUST record every command this task ran with its
VERBATIM output in execution order, the populations it re-derived with the commands that produced them, every
zero beside the control that proves the instrument could have seen a non-zero, the verbatim output of every
command oracle above run AFTER the work, and anything left for a named later task.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete`.
Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as
**pending** no matter how many gates passed (measured on P99-32 and again on P99-61).
</output>
