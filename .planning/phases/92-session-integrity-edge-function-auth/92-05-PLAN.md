---
phase: 92-session-integrity-edge-function-auth
plan: 05
type: execute
wave: 2
depends_on: [92-01]
files_modified:
  - supabase/functions/access-review-detail/index.ts
  - supabase/functions/ai-interaction-logs/index.ts
  - supabase/functions/ai-summary-generate/index.ts
  - supabase/functions/approve-role-change/index.ts
  - supabase/functions/assign-role/index.ts
  - supabase/functions/assignments-checklist-create-item/index.ts
  - supabase/functions/assignments-checklist-import-template/index.ts
  - supabase/functions/assignments-checklist-toggle-item/index.ts
  - supabase/functions/assignments-comments-create/index.ts
  - supabase/functions/assignments-comments-reactions-toggle/index.ts
  - supabase/functions/assignments-complete/index.ts
  - supabase/functions/assignments-escalate/index.ts
  - supabase/functions/assignments-observer-action/index.ts
  - supabase/functions/assignments-related-get/index.ts
  - supabase/functions/auth-step-up-complete/index.ts
  - supabase/functions/auth-step-up-initiate/index.ts
  - supabase/functions/auth-verify-step-up/index.ts
  - supabase/functions/bot-notification-dispatcher/index.ts
  - supabase/functions/briefing-packs-list/index.ts
  - supabase/functions/calculate-health-score/index.ts
  - supabase/functions/calendar-conflicts/index.ts
  - supabase/functions/certify-user-access/index.ts
  - supabase/functions/complete-access-review/index.ts
  - supabase/functions/content-expiration/index.ts
  - supabase/functions/content-expiration-processor/index.ts
  - supabase/functions/countries/index.ts
  - supabase/functions/create-user/index.ts
  - supabase/functions/data-export/index.ts
  - supabase/functions/data-import/index.ts
  - supabase/functions/data-library/index.ts
  - supabase/functions/delegate-permissions/index.ts
  - supabase/functions/detect-overdue-commitments/index.ts
  - supabase/functions/document-versions/index.ts
autonomous: true
requirements: [AUTH-02]
truths:
  - 'Decisions covered — D-07: the migration population is all 133 index.ts files pinning supabase-js@2.3x, operator-decided (RULING-P92-02 PARK-1 → A); this plan sweeps the alphabetical slice access-review-detail → document-versions; the 110 bare-getUser functions already on @2 are out of scope'
  - "Decisions covered — D-08: acceptance is the derivation command reaching zero over this slice — never a frozen count, because the population is a count over files this phase's own work mutates"
must_haves:
  truths:
    - 'No file in this slice pins @supabase/supabase-js@2.3x (slice-scoped derivation grep → empty)'
    - 'Every Class-1 edit in this slice retains its header-injected client (RLS scoping survives)'
    - 'No CORS import, config.toml entry, error idiom, or ≥2.43.0 pin (2.45.0/2.49.2) was touched'
  artifacts:
    - path: 'supabase/functions/access-review-detail/index.ts'
      provides: 'Class-1 specimen migrated: jsr @2 + getUser(token), injected client intact'
      contains: 'jsr:@supabase/supabase-js@2'
  key_links:
    - from: 'each migrated index.ts'
      to: 'GoTrue'
      via: 'getUser(token) or already-token-passing call on the @2 wrapper'
      pattern: 'jsr:@supabase/supabase-js@2'
---

<objective>
AUTH-02 sweep, slice A of 4 (alphabetical access-review-detail → document-versions, 33 files):
retire the 2.3x pin in every file, converting bare getUser() to getUser(token) in the same edit
where present (Class 1) or bumping the specifier only where the token is already passed (Class 2).
Code-migration only — deploy for the sweep happens once, in plan 92-09, after all slices merge
(no partial-deploy churn; the worker context may lack Supabase creds).

Purpose: version hygiene converting suspect to resolved (D-19: no per-function failure was ever
demonstrated for the sweep — acceptance is the derivation command, not a failure proof). The pin
retirement fixes a nondeterminism: a 2.3x pin floats its gotrue-js caret to 2.112.3 at every
deploy, so any fresh deploy of a bare-getUser file is broken today.
Output: 33 files on jsr:@supabase/supabase-js@2 with correct auth shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-session-integrity-edge-function-auth/92-PATTERNS.md  (Class 1/2 edit specs — the load-bearing reference)

<interfaces>
Edit classes (from PATTERNS, verified):
- CLASS 1 (pinned + bare getUser() + injected header): bump every 2.3x specifier occurrence to
  `jsr:@supabase/supabase-js@2`, add `const token = authHeader.replace('Bearer ', '')`, pass it to
  `getUser(token)`, KEEP `{ global: { headers: { Authorization: authHeader } } }` untouched.
  BEFORE specimen: access-review-detail/index.ts:15,83-97. AFTER shape: access-requests/index.ts:7,57-61.
- CLASS 2 (pinned + already getUser(token)): specifier bump ONLY. Specimen: dossiers-update/index.ts:2,92-97.
- Class is decided PER FILE by reading its auth call, not assumed from any list.
- If a file has bare getUser() and NO Authorization header anywhere (should not occur in this
  population), do not invent a fix — record it in the SUMMARY as an anomaly and leave the getUser
  call unchanged (bump the pin only).
Never leave the old specifier in a comment (it re-trips the derivation grep). Do not touch:
_shared/cors.ts imports, config.toml, error-response idioms, service-role clients, or any file
pinned 2.45.0/2.49.2 (≥2.43.0 = fine by shape, outside the operator-granted population).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate slice A first half (access-review-detail → auth-verify-step-up, 17 files)</name>
  <files>supabase/functions/{access-review-detail,ai-interaction-logs,ai-summary-generate,approve-role-change,assign-role,assignments-checklist-create-item,assignments-checklist-import-template,assignments-checklist-toggle-item,assignments-comments-create,assignments-comments-reactions-toggle,assignments-complete,assignments-escalate,assignments-observer-action,assignments-related-get,auth-step-up-complete,auth-step-up-initiate,auth-verify-step-up}/index.ts</files>
  <read_first>Per file: `grep -n '@supabase/supabase-js@2\.3\|getUser(' <file>` first, then read only the import line and the auth block — do not read whole files</read_first>
  <action>
    For each of the 17 files: determine its class from the getUser call, apply the Class-1 or
    Class-2 edit per the interfaces block. Nothing else in the file changes.
  </action>
  <verify>
    <automated>grep -rlE '@supabase/supabase-js@2\.3[0-9]' supabase/functions/access-review-detail supabase/functions/ai-interaction-logs supabase/functions/ai-summary-generate supabase/functions/approve-role-change supabase/functions/assign-role supabase/functions/assignments-checklist-create-item supabase/functions/assignments-checklist-import-template supabase/functions/assignments-checklist-toggle-item supabase/functions/assignments-comments-create supabase/functions/assignments-comments-reactions-toggle supabase/functions/assignments-complete supabase/functions/assignments-escalate supabase/functions/assignments-observer-action supabase/functions/assignments-related-get supabase/functions/auth-step-up-complete supabase/functions/auth-step-up-initiate supabase/functions/auth-verify-step-up 2>/dev/null | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>All 17 off the 2.3x pin; every file that had an injected client still has it (spot-check the diff: no `global: { headers` line removed); no bare getUser() remains in a file that forwards an Authorization header.</acceptance_criteria>
  <done>First half migrated, per-file class recorded in the commit message or SUMMARY table.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate slice A second half (bot-notification-dispatcher → document-versions, 16 files)</name>
  <files>supabase/functions/{bot-notification-dispatcher,briefing-packs-list,calculate-health-score,calendar-conflicts,certify-user-access,complete-access-review,content-expiration,content-expiration-processor,countries,create-user,data-export,data-import,data-library,delegate-permissions,detect-overdue-commitments,document-versions}/index.ts</files>
  <read_first>Same per-file grep-then-targeted-read protocol as Task 1</read_first>
  <action>
    Same edit protocol for the remaining 16 files. Then run the slice-scoped derivation over ALL 33
    directories of this plan to confirm the slice is clean.
  </action>
  <verify>
    <automated>grep -rlE '@supabase/supabase-js@2\.3[0-9]' supabase/functions/bot-notification-dispatcher supabase/functions/briefing-packs-list supabase/functions/calculate-health-score supabase/functions/calendar-conflicts supabase/functions/certify-user-access supabase/functions/complete-access-review supabase/functions/content-expiration supabase/functions/content-expiration-processor supabase/functions/countries supabase/functions/create-user supabase/functions/data-export supabase/functions/data-import supabase/functions/data-library supabase/functions/delegate-permissions supabase/functions/detect-overdue-commitments supabase/functions/document-versions 2>/dev/null | wc -l | grep -qx 0 && git rev-parse -q --verify refs/tags/phase-92-base >/dev/null && git diff --name-only phase-92-base -- supabase/functions/_shared/cors.ts supabase/config.toml | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>Slice A fully clean under the derivation grep; _shared/cors.ts and config.toml untouched since phase-92-base (auth.ts is 92-04’s legitimate same-wave edit, so the guard excludes it); SUMMARY carries the per-file Class 1/Class 2 tally.</acceptance_criteria>
  <done>33/33 slice-A files migrated, code-only, ready for the 92-09 batch deploy.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description              |
| ------------------------- | ------------------------ |
| client → edge function    | caller JWT; getUser gate |
| edge function → PostgREST | injected JWT scopes RLS  |

## STRIDE Threat Register

| Threat ID | Category                          | Component            | Disposition | Mitigation Plan                                                                        |
| --------- | --------------------------------- | -------------------- | ----------- | -------------------------------------------------------------------------------------- |
| T-92-02   | Spoofing                          | getUser gate         | mitigate    | explicit token (or preserved token-passing call) against GoTrue on the current wrapper |
| T-92-09   | Information Disclosure (inverted) | RLS scoping          | mitigate    | injected clients preserved verbatim; the edit spec forbids client-construction changes |
| T-92-SC   | Tampering                         | dependency specifier | mitigate    | official jsr registry specifier already used by 45 repo functions; zero installs       |

</threat_model>

<verification>
Slice-scoped `grep -rlE '@supabase/supabase-js@2\.3[0-9]'` over all 33 directories → empty;
no diff outside the 33 index.ts files.
</verification>

<success_criteria>Slice A source-clean; injected clients and CORS untouched; classes tallied.</success_criteria>

<output>
Create `.planning/phases/92-session-integrity-edge-function-auth/92-05-SUMMARY.md` when done.
</output>
