---
phase: 92-session-integrity-edge-function-auth
plan: 03
type: execute
wave: 2
depends_on: [92-01]
files_modified:
  - frontend/src/pages/delegations/DelegationManagementPage.tsx
  - frontend/src/i18n/en/delegation.json
  - frontend/src/i18n/ar/delegation.json
autonomous: true
requirements: [AUTH-04]
truths:
  - 'Decisions covered — D-13: /delegations is fixed locally in this phase, UI half only — the rejected query renders an error state instead of "You haven''t granted any delegations"; the general repository-layer failure fix is TRUST-01 (Phase 93) and is not pre-empted here; the auth half of my-delegations lands in plan 92-04'
must_haves:
  truths:
    - 'A rejected my-delegations query renders a role="alert" error state with a retry action — visually and semantically distinct from the empty state'
    - 'The three stat cards render an em-dash — never a confident 0 — while the query is in error'
    - 'A successful query still renders real delegations and the unchanged empty state when genuinely empty'
  artifacts:
    - path: 'frontend/src/pages/delegations/DelegationManagementPage.tsx'
      provides: "isError branch replacing both tabs' list area; em-dash stats on error"
      contains: 'isError'
    - path: 'frontend/src/i18n/en/delegation.json'
      provides: 'list.error.title / list.error.description / list.error.retry'
      contains: 'error'
  key_links:
    - from: 'frontend/src/pages/delegations/DelegationManagementPage.tsx'
      to: 'frontend/src/hooks/useDelegation.ts:175'
      via: 'isError/error/refetch destructured from useMyDelegations (already surfaced, never read today)'
      pattern: 'useMyDelegations'
---

<objective>
Make /delegations report failure as failure (AUTH-04, UI half): branch on the `isError` that
`useMyDelegations` already surfaces, render an error state per the approved UI-SPEC (distinct from
the empty state on every axis), and stop the stat cards reporting a confident zero over a failed
request.

Purpose: today a rejected query is pixel-identical to "you have no delegations" — the exact
failure-rendered-as-emptiness defect this milestone exists to kill.
Output: honest /delegations; 92-delegations-error.spec.ts green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-session-integrity-edge-function-auth/92-UI-SPEC.md  (Surface 3 — the ERROR vs EMPTY axis table is binding)
@.planning/phases/92-session-integrity-edge-function-auth/92-PATTERNS.md  (DelegationManagementPage + error-state analogs)
@frontend/src/pages/delegations/DelegationManagementPage.tsx

<interfaces>
Verified this session:
- The query layer is already honest: `user-management-api.ts:481-493` throws on invoke error;
  `useDelegation.ts:175-182` is a plain useQuery — `isError`/`error` available, page never reads them.
- Page today: `:50-57` destructures only data/isLoading/refetch; stats memo `:90-99` returns zeros
  for missing data; Tabs content `:199-250`; `AlertTriangle` already imported (`:36`).
- Geometry analog: `components/empty-states/EmptyState.tsx:114-137` (centered column, py-12,
  circular icon wrapper) + its `.btn-primary` CTA recipe `:151-160` with `isRTL` logical icon spacing.
- Empty-state sibling to invert: `DelegationList.tsx:71-88`.
- Branch-shape analog (shape ONLY, not body): `WorkingGroupsPage.tsx:237-247` — it renders
  `error?.message` raw, which is FORBIDDEN here (FunctionsHttpError.message is a supabase-js
  internal string; house rule: user-facing errors never leak internals).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: isError branch + inline error state + em-dash stats</name>
  <files>frontend/src/pages/delegations/DelegationManagementPage.tsx</files>
  <read_first>DelegationManagementPage.tsx in full; EmptyState.tsx:114-160 (geometry + CTA recipe); UI-SPEC Surface 3 axis table</read_first>
  <action>
    Destructure `isError` (and keep `refetch`) from useMyDelegations. When isError:
    - Replace the list area of BOTH tabs with ONE error block (one failure, one message — render it
      once above/instead of the Tabs content at :199-250).
    - The block mirrors EmptyState md geometry inline in this page (do NOT extend the shared
      EmptyState component for a single consumer): centered column, py-12, circular icon wrapper —
      but `AlertTriangle` in `text-danger` on a `bg-danger/10` wrapper (opacity modifier, never a
      -soft utility). Container gets `role="alert"`. Heading t('list.error.title'), body
      t('list.error.description') — NEVER the raw error string. `.btn-primary` "Try again" CTA
      wired to `refetch`, icon spacing via logical properties.
    - Stat cards render the em-dash '—' instead of numbers while isError — bypass the stats memo's
      confident zeros (a failed query must not report 0).
    Empty state, loading state, and the happy path stay byte-identical in behavior.
    Tokens only (no raw hex, no Tailwind palette literals); sentence case; no emoji.
  </action>
  <verify>
    <automated>cd frontend && pnpm typecheck && grep -q 'role="alert"' src/pages/delegations/DelegationManagementPage.tsx && ! grep -q 'error?.message\|error\.message' src/pages/delegations/DelegationManagementPage.tsx</automated>
  </verify>
  <acceptance_criteria>Error and empty are distinct on every UI-SPEC axis (icon semantics, heading, body, action, role="alert", em-dash stats, both tabs covered); no internal string rendered; happy/empty paths unchanged.</acceptance_criteria>
  <done>A rejected query is visibly a failure with a retry, never an empty onboarding screen.</done>
</task>

<task type="auto">
  <name>Task 2: Error strings (en + ar) and forced-error verification</name>
  <files>frontend/src/i18n/en/delegation.json, frontend/src/i18n/ar/delegation.json</files>
  <read_first>delegation.json list.empty.* / list.emptyDescription.* key shapes (place error keys beside them); UI-SPEC copy contract table</read_first>
  <action>
    Add under `list.error.*` in BOTH locales in the same edit (static i18n bundle — a missing key
    silently renders English in both languages):
    en: title: "Couldn't load delegations", description: "Your delegations couldn't be retrieved.
    Try again — if the problem continues, sign in again.", retry: "Try again"
    ar: title: "تعذر تحميل التفويضات", description: "تعذر استرجاع تفويضاتك. حاول مرة أخرى، وإذا
    استمرت المشكلة فسجّل الدخول من جديد.", retry: "حاول مرة أخرى"
    Arabic reuses the bundle's established noun (تفويضات). No new namespaces — `delegation` is
    already registered in src/i18n/index.ts. Never touch public/locales/ (dead).
    Then run the forced-error spec to prove the branch (the spec blocks my-delegations via CDP and
    asserts the DOM — an auth/RLS denial can present as an empty 200, so network status is never
    the oracle).
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/92-delegations-error.spec.ts --project=chromium-en --no-deps</automated>
  </verify>
  <acceptance_criteria>Spec green: role="alert" visible with the error heading, empty-state copy absent, stats show '—', retry button present. Both locales carry all three keys. The spec's 15s assertion timeout is load-bearing: a CDP-blocked invoke has no numeric status, so the query-client 4xx short-circuit never fires and TanStack's 4-attempt 1s+2s+4s backoff delivers isError at ~7s — do not "fix" a slow assertion by loosening it or shortening the timeout.</acceptance_criteria>
  <done>Forced-error e2e green; strings resolve in both languages.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                |
| ------------------------------------ | ------------------------------------------ |
| edge function response → page render | failure/emptiness distinction crosses here |

## STRIDE Threat Register

| Threat ID | Category               | Component                | Disposition | Mitigation Plan                                                                                                                       |
| --------- | ---------------------- | ------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| T-92-04   | Repudiation            | DelegationManagementPage | mitigate    | rejected query renders as failure (role="alert"), never as emptiness; em-dash stats prevent confident-zero repudiation of the failure |
| T-92-08   | Information Disclosure | error rendering          | mitigate    | i18n copy only — the raw FunctionsHttpError/supabase-js message never reaches the DOM (grep-gated in verify)                          |
| T-92-SC   | Tampering              | package installs         | accept      | no installs                                                                                                                           |

</threat_model>

<verification>
- `pnpm exec playwright test tests/e2e/92-delegations-error.spec.ts --project=chromium-en --no-deps` green.
- Typecheck + ESLint clean on touched files.
- RTL render of the error state at 1024/1400px is deferred to the phase-end human check.
</verification>

<success_criteria>
Criterion 4's UI half holds: /delegations renders an error state when my-delegations calls are
rejected and real delegations when they are not (the auth half closes in 92-04).
</success_criteria>

<output>
Create `.planning/phases/92-session-integrity-edge-function-auth/92-03-SUMMARY.md` when done.
</output>
