---
phase: 92-session-integrity-edge-function-auth
plan: 08
type: execute
wave: 2
depends_on: [92-01]
files_modified:
  - supabase/functions/push-notification-send/index.ts
  - supabase/functions/refresh-commitment-stats/index.ts
  - supabase/functions/relationship-health/index.ts
  - supabase/functions/relationships-manage/index.ts
  - supabase/functions/reports/index.ts
  - supabase/functions/resolve-dossier-context/index.ts
  - supabase/functions/retention-processor/index.ts
  - supabase/functions/revoke-delegation/index.ts
  - supabase/functions/sample-data/index.ts
  - supabase/functions/schedule-access-review/index.ts
  - supabase/functions/slack-bot/index.ts
  - supabase/functions/smart-import-suggestions/index.ts
  - supabase/functions/stakeholder-influence/index.ts
  - supabase/functions/sync-incremental/index.ts
  - supabase/functions/sync-pull/index.ts
  - supabase/functions/tags-manage/index.ts
  - supabase/functions/team-collaboration/index.ts
  - supabase/functions/teams-bot/index.ts
  - supabase/functions/themes/index.ts
  - supabase/functions/topics/index.ts
  - supabase/functions/translate-content/index.ts
  - supabase/functions/trigger-health-recalculation/index.ts
  - supabase/functions/user-permissions/index.ts
  - supabase/functions/validate-delegation/index.ts
  - supabase/functions/waiting-queue-escalation/index.ts
  - supabase/functions/waiting-queue-filters/index.ts
  - supabase/functions/webhook-delivery/index.ts
  - supabase/functions/webhooks/index.ts
  - supabase/functions/word-assistant/index.ts
  - supabase/functions/working-groups/index.ts
autonomous: true
requirements: [AUTH-02]
truths:
  - 'Decisions covered — D-07: population is all 133 2.3x-pinned index.ts files (operator-decided); this plan sweeps the final alphabetical slice push-notification-send → working-groups'
  - 'Decisions covered — D-08: acceptance is the slice-scoped derivation command reaching zero, never a frozen count'
must_haves:
  truths:
    - 'No file in this slice pins @supabase/supabase-js@2.3x'
    - 'Every Class-1 edit retains its header-injected client'
    - 'CORS imports, config.toml, error idioms, and ≥2.43.0 pins untouched'
  artifacts:
    - path: 'supabase/functions/smart-import-suggestions/index.ts'
      provides: 'Class-2 member migrated (already token-passing per PATTERNS)'
      contains: 'jsr:@supabase/supabase-js@2'
  key_links:
    - from: 'each migrated index.ts'
      to: 'GoTrue'
      via: 'token-passing getUser on the @2 wrapper'
      pattern: 'jsr:@supabase/supabase-js@2'
---

<objective>
AUTH-02 sweep, slice D of 4 (alphabetical push-notification-send → working-groups, 30 files):
identical protocol to slices A/B/C — retire every 2.3x pin, Class-1 or Class-2 edit per file,
code-only (deploy in 92-09).
Output: 30 files on jsr:@supabase/supabase-js@2 with correct auth shape; the whole 133-population
derivation reaches 0 once this slice merges alongside its siblings and 92-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-session-integrity-edge-function-auth/92-PATTERNS.md  (Class 1/2 edit specs)

<interfaces>
Same edit classes as 92-05 (PATTERNS is the source):
- CLASS 1: specifier bump to `jsr:@supabase/supabase-js@2` (every occurrence) + token extraction +
  `getUser(token)`, injected client KEPT verbatim.
- CLASS 2: specifier bump only (known member in this slice per PATTERNS: smart-import-suggestions).
- Class decided per file by reading its auth call. Anomaly (bare getUser, no header) → record in
  SUMMARY, bump pin only. No old specifiers in comments. Do not touch _shared/, config.toml, CORS
  imports, error idioms, service-role clients, or 2.45.0/2.49.2 pins.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate slice D first half (push-notification-send → sample-data, 15 files)</name>
  <files>supabase/functions/{push-notification-send,refresh-commitment-stats,relationship-health,relationships-manage,reports,resolve-dossier-context,retention-processor,revoke-delegation,sample-data,schedule-access-review,slack-bot,smart-import-suggestions,stakeholder-influence,sync-incremental,sync-pull}/index.ts</files>
  <read_first>Per file: `grep -n '@supabase/supabase-js@2\.3\|getUser(' <file>` first, then read only the import line and the auth block</read_first>
  <action>Apply the Class-1/Class-2 edit per file per the interfaces block. Nothing else changes.</action>
  <verify>
    <automated>grep -rlE '@supabase/supabase-js@2\.3[0-9]' supabase/functions/push-notification-send supabase/functions/refresh-commitment-stats supabase/functions/relationship-health supabase/functions/relationships-manage supabase/functions/reports supabase/functions/resolve-dossier-context supabase/functions/retention-processor supabase/functions/revoke-delegation supabase/functions/sample-data supabase/functions/schedule-access-review supabase/functions/slack-bot supabase/functions/smart-import-suggestions supabase/functions/stakeholder-influence supabase/functions/sync-incremental supabase/functions/sync-pull 2>/dev/null | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>15 files off the pin; injected clients preserved.</acceptance_criteria>
  <done>First half migrated with per-file class recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate slice D second half (tags-manage → working-groups, 15 files)</name>
  <files>supabase/functions/{tags-manage,team-collaboration,teams-bot,themes,topics,translate-content,trigger-health-recalculation,user-permissions,validate-delegation,waiting-queue-escalation,waiting-queue-filters,webhook-delivery,webhooks,word-assistant,working-groups}/index.ts</files>
  <read_first>Same per-file grep-then-targeted-read protocol</read_first>
  <action>Apply the edit to the remaining 15 files, then confirm the whole slice is clean.</action>
  <verify>
    <automated>grep -rlE '@supabase/supabase-js@2\.3[0-9]' supabase/functions/tags-manage supabase/functions/team-collaboration supabase/functions/teams-bot supabase/functions/themes supabase/functions/topics supabase/functions/translate-content supabase/functions/trigger-health-recalculation supabase/functions/user-permissions supabase/functions/validate-delegation supabase/functions/waiting-queue-escalation supabase/functions/waiting-queue-filters supabase/functions/webhook-delivery supabase/functions/webhooks supabase/functions/word-assistant supabase/functions/working-groups 2>/dev/null | wc -l | grep -qx 0 && git rev-parse -q --verify refs/tags/phase-92-base >/dev/null && git diff --name-only phase-92-base -- supabase/functions/_shared/cors.ts supabase/config.toml | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>Slice D fully clean; _shared/cors.ts and config.toml untouched since phase-92-base (auth.ts is 92-04’s legitimate same-wave edit, so the guard excludes it); SUMMARY carries the Class 1/2 tally.</acceptance_criteria>
  <done>30/30 slice-D files migrated, ready for the 92-09 batch deploy.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description              |
| ------------------------- | ------------------------ |
| client → edge function    | caller JWT; getUser gate |
| edge function → PostgREST | injected JWT scopes RLS  |

## STRIDE Threat Register

| Threat ID | Category                          | Component            | Disposition | Mitigation Plan                              |
| --------- | --------------------------------- | -------------------- | ----------- | -------------------------------------------- |
| T-92-02   | Spoofing                          | getUser gate         | mitigate    | token-passing getUser on the current wrapper |
| T-92-09   | Information Disclosure (inverted) | RLS scoping          | mitigate    | injected clients preserved verbatim          |
| T-92-SC   | Tampering                         | dependency specifier | mitigate    | official jsr specifier; zero installs        |

</threat_model>

<verification>Slice-scoped derivation grep → empty; no diff outside the 30 index.ts files.</verification>

<success_criteria>Slice D source-clean; injected clients and CORS untouched; classes tallied.</success_criteria>

<output>
Create `.planning/phases/92-session-integrity-edge-function-auth/92-08-SUMMARY.md` when done.
</output>
