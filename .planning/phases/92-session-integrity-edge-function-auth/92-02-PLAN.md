---
phase: 92-session-integrity-edge-function-auth
plan: 02
type: execute
wave: 2
depends_on: [92-01]
files_modified:
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/components/layout/nav-user.tsx
  - frontend/src/components/layout/header/UserMenu.tsx
  - frontend/src/store/authStore.ts
  - frontend/src/store/authStore.signout.test.ts
  - frontend/src/components/settings/sections/SecuritySettingsSection.tsx
  - frontend/src/components/settings/sections/DataPrivacySettingsSection.tsx
  - frontend/src/i18n/en/common.json
  - frontend/src/i18n/en/settings.json
  - frontend/src/i18n/ar/settings.json
autonomous: true
requirements: [AUTH-01, AUTH-03, AUTH-05]
truths:
  - 'Decisions covered — D-01: the live sign-out control is the existing NavUser mounted as the sidebar user card, not a new build; D-23: NavUser has an unsatisfied useSidebar provider dependency — adapt the consumer to useResponsive, never wrap the shell in SidebarProvider'
  - "Decisions covered — D-02: /settings exposes a second, independent sign-out control; D-03: AUTH-05 reachability is satisfied by NavUser's existing /settings links — the /settings/* subtree nav defect is NAV-02, a later phase, and is not touched here"
  - "Decisions covered — D-22: exactly ONE live seam is edited — authStore.handleAuthStateChange's SIGNED_OUT branch — with the five-site live/no-op/dead classification carried as evidence; D-10: no sixth onAuthStateChange subscription is added; D-11: the dead services/auth.ts module is NOT edited"
  - "Decisions covered — D-25: the seam's navigation MUST use the lazy import('@/router') form — a module-level import newly introduces the verified cycle authStore → router → routeTree.gen → routes → authStore, in a repo that has already shipped a white-screen from a module cycle; the task states why so the next reader does not simplify it back"
  - 'Decisions covered — D-29: the SIGNED_OUT seam is the single owner of post-sign-out teardown AND navigation — it calls queryClient.clear() before navigating, closing the query-cache leak the three new soft-navigating sign-out paths would otherwise introduce, and DataPrivacySettingsSection drops its setTimeout + window.location.href reload (its only load-bearing function is now owned centrally)'
  - 'Decisions covered — D-30: the cache acceptance criterion is bounded to the IN-MEMORY query cache — persisted localStorage residue (auth-storage, entity-history-storage, ui-storage, pinned-entities-storage, dossier-store, search/recent-item writers) is a pre-existing leak filed as CLIENTSEC-01 and owned by Phase 100, deliberately NOT swept here'
  - "Decisions covered — D-12: the forced redirect is a plain /login navigation with no redirectTo and /login stays out of files_modified; D-18: _protected.tsx's beforeLoad guard already works and is a non-goal"
  - "Decisions covered — D-24: the sign-out label becomes t('common.logout') — dot form, second argument DROPPED — repointing one existing key, no new keys, no class sweep (that is AR-04a / Phase 99); D-27: the rendered label is load-bearing for the live 01-login spec's accessible-name regex, so both label values must keep matching /sign out|logout|تسجيل الخروج|خروج/i"
  - 'Decisions covered — D-28: /settings gets BOTH a new plain sign-out in an account context (Security section) AND a relabel of the existing DataPrivacy control that falsely claims "other sessions" (both locales, sentence case, scope unchanged); both controls carry data-testids because the shared name regex now matches 2+ elements on that page'
  - 'Decisions covered — D-26: the fix is proven by re-running THE SAME COMMAND 92-01 measured its RED with — `pnpm exec playwright test tests/e2e/92-signout.spec.ts --project=chromium-en --no-deps` — and capturing the GREEN beside that RED; the post-fix path is a different path (the user-menu branch turns true for the first time), so it is re-run, never reasoned about. The oracle is 92-signout, NOT 01-login: RULING-36 moved the RED there because 01-login routes through the `setup` project, which cannot run on this machine (E2ECRED-01). Pairing the red of one oracle with the green of another is exactly the substitution RULING-37 forbids'
must_haves:
  truths:
    - 'A signed-in user can sign out from the sidebar user card and lands on /login with the supabase auth storage key cleared'
    - 'A signed-in user can sign out from /settings via an independent, discoverable control with the same result'
    - 'Invalidating the session out-of-band bounces the already-open tab to /login within one 30s gotrue tick — no reload, no ghost "Member/Member" shell'
    - 'After any sign-out path, the in-memory TanStack Query cache is empty (getQueryCache().getAll().length === 0) — bounded claim: in-memory only, persisted localStorage is out of scope'
    - 'The /settings Data & Privacy control no longer claims it signs out only "other" sessions'
    - 'The Arabic UI renders تسجيل الخروج for the sign-out item (no inline-English fallback masking)'
  artifacts:
    - path: 'frontend/src/components/layout/Sidebar.tsx'
      provides: 'NavUser mounted in place of the static user card'
      contains: 'NavUser'
    - path: 'frontend/src/components/layout/nav-user.tsx'
      provides: "Adapted consumer: useResponsive instead of useSidebar; t('common.logout'); data-testid user-menu"
      contains: 'useResponsive'
    - path: 'frontend/src/store/authStore.ts'
      provides: 'SIGNED_OUT branch: queryClient.clear() then lazy-router navigation to /login'
      contains: "import('@/router')"
    - path: 'frontend/src/store/authStore.signout.test.ts'
      provides: 'Unit proof the SIGNED_OUT teardown empties the query cache'
      contains: 'getQueryCache'
  key_links:
    - from: 'frontend/src/store/authStore.ts'
      to: 'frontend/src/router/index.tsx'
      via: 'dynamic import inside the SIGNED_OUT branch (module-level import newly creates a real cycle — D-25)'
      pattern: "import\\('@/router'\\)"
    - from: 'frontend/src/store/authStore.ts'
      to: 'frontend/src/lib/query-client.ts'
      via: 'plain module import of the queryClient singleton (verified acyclic: query-client.ts imports only @tanstack/react-query, sonner, ./query-tiers)'
      pattern: 'queryClient.clear'
    - from: 'frontend/src/components/settings/sections/SecuritySettingsSection.tsx'
      to: 'useAuth().logout()'
      via: 'same seam NavUser uses — neither surface navigates itself'
      pattern: 'logout\('
---

<objective>
Make session end honest on all three fronts (AUTH-01, AUTH-03, AUTH-05): mount the existing
NavUser dropdown as the sidebar user card, give /settings a discoverable plain sign-out AND fix
the existing Data & Privacy control's lying label (D-28), and make ONE seam —
authStore.handleAuthStateChange's SIGNED_OUT branch — the single owner of post-sign-out teardown
and navigation: clear the query cache (D-29), then lazily navigate to /login (D-25).

Purpose: today nothing discoverable can sign out, an invalidated session decays into a
"Member/Member" ghost page, and the three sign-out paths this phase adds would each leak the
previous user's query cache for up to 10 minutes on a shared workstation if the seam did not own
teardown.
Output: two working sign-out surfaces + reactive bounce + cache-safe teardown; 92-signout.spec.ts
flips RED→GREEN with both outputs on record (same command both times — see D-26).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-session-integrity-edge-function-auth/92-UI-SPEC.md  (Surface contracts 1-2 — binding)
@.planning/phases/92-session-integrity-edge-function-auth/92-PATTERNS.md  (authStore, Sidebar, nav-user, SecuritySettingsSection assignments)
@frontend/src/components/layout/Sidebar.tsx
@frontend/src/components/layout/nav-user.tsx
@frontend/src/store/authStore.ts
@frontend/src/hooks/useResponsive.ts
@frontend/src/components/settings/sections/SecuritySettingsSection.tsx
@frontend/src/components/settings/sections/DataPrivacySettingsSection.tsx

<interfaces>
Verified this session:
- Mount seam: `Sidebar.tsx:101-114` static user card (accent initials disc, two-line name+role,
  `--radius-sm` wash). Locals in scope: `getInitials` :48-56, `displayName`/`roleLabel`/`isRTL` :67-71.
- `nav-user.tsx:15,27` imports `useSidebar` — `SidebarProvider` renders NOWHERE in the app;
  `useResponsive()` (frontend/src/hooks/useResponsive.ts) exposes the SAME `isMobile` field name
  (:17 declares, :116 computes) — a one-line swap, not a refactor. Keep the `side` expression at
  nav-user.tsx:60: the same `<Sidebar />` renders in the desktop column (AppShell.tsx:190) AND the
  mobile drawer (AppShell.tsx:238-253), so the mobile branch is genuinely reachable.
- `nav-user.tsx:94`: `t('navigation.logout', 'Logout')` — key missing in both locales.
  `frontend/src/i18n/en/common.json:68` has `"logout": "Logout"`; `ar/common.json:68` has
  `"logout": "تسجيل الخروج"`. `common.logout` has ZERO live consumers (grep verified) — changing
  the en value is safe.
- LIVE FAILING TEST (D-26): `tests/e2e/01-login.spec.ts:30-41` — not quarantined; finds
  `getByTestId('user-menu')` then a role-regex button `/sign out|logout|تسجيل الخروج|خروج/i`.
  92-01 recorded its RED. This plan's mount makes it pass — and `data-testid="user-menu"` is
  load-bearing for it.
- Live seam: `authStore.ts:201-212` `handleAuthStateChange` SIGNED_OUT branch; wired via
  `subscribeToAuthChanges()` :264-271 → `AuthListenerManager.tsx:11` → `App.tsx:32`.
  `authStore.logout()` :105-130 clears state, does NOT navigate — correct as-is.
- Router cycle (D-25): `router/index.tsx:2` imports routeTree.gen → routes → `_protected.tsx:4` /
  `routes/index.tsx:2` import authStore. authStore does NOT import router today — a static import
  would newly introduce the cycle. LAZY FORM REQUIRED.
- Query cache (D-29): `lib/query-client.ts` exports the singleton `queryClient` (App.tsx:30);
  staleTime 5min / gcTime 10min / refetchOnMount:false. Nothing clears it on sign-out. Its import
  graph is @tanstack/react-query + sonner + ./query-tiers ONLY — importing it into authStore is
  acyclic (verified).
- /settings sections mount order (SettingsPage.tsx:22-28): Profile, General, Appearance,
  Notifications, Security, Accessibility, DataPrivacy.
- Existing DataPrivacy control (D-28): `DataPrivacySettingsSection.tsx` `handleSignOutAll`
  (:164-179) — `supabase.auth.signOut({ scope: 'global' })` then
  `setTimeout(() => { logout(); window.location.href = '/login' }, 1000)`. Label
  `dataPrivacy.signOutAll` = settings.json:200 en "Sign Out All Other Sessions" / ar
  "تسجيل الخروج من جميع الجلسات الأخرى" — but scope:'global' ends THIS session too. Trigger :286
  and dialog confirm :301 both render the same key.
- TESTID CONTRACT (shared with 92-01's spec): new Security control `data-testid="settings-signout"`;
  DataPrivacy trigger `data-testid="signout-all-sessions"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount NavUser as the sidebar user card; delete the second orphan (D-01, D-23, D-24, D-27)</name>
  <files>frontend/src/components/layout/Sidebar.tsx, frontend/src/components/layout/nav-user.tsx, frontend/src/components/layout/header/UserMenu.tsx, frontend/src/i18n/en/common.json</files>
  <read_first>Sidebar.tsx:28-40 (import style) and :48-114 (locals + card block); nav-user.tsx in full (~110 lines); 92-UI-SPEC.md Surface 1 states table</read_first>
  <action>
    Adapt nav-user.tsx per D-23/D-24, then mount it per D-01:
    (a) Replace the `useSidebar` import/call with `useResponsive` from '@/hooks/useResponsive' —
        identical `isMobile` field, a one-line swap, not a refactor. KEEP the `side` expression at
        :60 (`isMobile ? 'bottom' : ...`): the mobile branch is reachable via the AppShell drawer
        mount. Do NOT wrap anything in SidebarProvider.
    (b) Change line 94's call to exactly `t('common.logout')` — dot form, SECOND ARGUMENT DROPPED
        (D-24: the English default only masks a future key regression; the key exists in both
        locales). Repoint this ONE key only — no new keys, no sweep of other `t('key','default')`
        sites (Phase 99).
    (c) Add `data-testid="user-menu"` to the dropdown trigger. This attribute is LOAD-BEARING, not
        decoration: the live-failing 01-login.spec.ts:30 test opens `getByTestId('user-menu')`
        (D-26) — it belongs in this task's acceptance.
        ROLE COUPLING (checker B-1, REVERSED on measurement): the sign-out item stays a
        `DropdownMenuItem` with its native `role="menuitem"` — that is CORRECT a11y for a menu
        item, and Radix's Slot merge keeps `role="menuitem"` even under `asChild` over a
        `<button>` (mergeProps iterates childProps keys only; a button has no `role` prop), so an
        asChild "fix" changes nothing. Do NOT force role="button" on a menu item — this project
        does not trade a11y for test convenience. The 01-login oracle is repaired on the LOCATOR
        side in plan 92-01 (LoginPage.ts widened to button|menuitem); no role change here.
    (d) In en/common.json:68 change the value `"Logout"` → `"Sign out"` (UI-SPEC copy contract;
        sentence case). LEAVE ar/common.json untouched. LABEL COUPLING (D-27): the rendered value —
        in BOTH locales — must keep matching the 01-login accessible-name regex
        /sign out|logout|تسجيل الخروج|خروج/i; "Sign out" and "تسجيل الخروج" both do. Any future
        rewording outside that regex breaks the spec in a way that reads as an auth regression.
    (e) In Sidebar.tsx, import NavUser (relative, beside './navigation-config') and replace the
        static card block at :101-114 with the mounted control. The rendered default state must keep
        the card's visual identity per UI-SPEC: accent initials disc, two-line name+role, the
        `--radius-sm` card wash on `--sidebar-bg` — adapt NavUser's trigger to that markup rather
        than shipping shadcn sidebar-menu chrome that looks foreign. Open state: menu on
        `--surface-3`, `1px solid var(--line)`, `--radius`; Sign out is plain ink, NOT danger.
        Remove any Sidebar locals your replacement orphans (only ones YOUR edit makes unused).
    (f) Delete frontend/src/components/layout/header/UserMenu.tsx (zero importers, verified —
        re-verify with `grep -rn "header/UserMenu" frontend/src --include='*.ts*'` before deleting).
        Two logout menus is a worse outcome than one orphan; mounting exactly one is D-01.
    Logical properties only (ms-*/ps-*/text-start); no raw hex; flip directional icons under RTL.
  </action>
  <verify>
    <automated>cd frontend && pnpm type-check && ! grep -q "useSidebar" src/components/layout/nav-user.tsx && grep -q "data-testid=\"user-menu\"" src/components/layout/nav-user.tsx && ! test -f src/components/layout/header/UserMenu.tsx</automated>
  </verify>
  <acceptance_criteria>NavUser renders as the sidebar user card (desktop column AND mobile drawer); dropdown opens with Profile//settings, Settings//settings, Sign out items; `data-testid="user-menu"` present on the trigger (load-bearing for 01-login.spec.ts:30); the sign-out item keeps its native role="menuitem" (correct a11y; the widened 92-01 locator matches it) — ROLE coupling beside D-27's LABEL coupling; `t('common.logout')` has no second argument; en value "Sign out", ar untouched, both matching the D-27 regex; UserMenu.tsx deleted; typecheck clean.</acceptance_criteria>
  <done>Sidebar user card is an interactive dropdown preserving today's visual identity; exactly one logout menu exists in the tree.</done>
</task>

<task type="auto">
  <name>Task 2: The SIGNED_OUT seam — cache teardown + one lazy navigation (D-22, D-25, D-29, D-30, D-12, D-18)</name>
  <files>frontend/src/store/authStore.ts, frontend/src/store/authStore.signout.test.ts</files>
  <read_first>authStore.ts:105-130 (logout — do not touch), :201-212 (SIGNED_OUT branch — the seam), :264-271 (subscription wiring); lib/query-client.ts:1-20 (the singleton export)</read_first>
  <action>
    Inside the existing SIGNED_OUT/USER_DELETED branch of handleAuthStateChange, after the state
    clear, add teardown-then-navigation — the seam is the SINGLE OWNER of both (D-29):
    1. `queryClient.clear()` — import `{ queryClient }` from '@/lib/query-client' as a plain
       module-level import (verified acyclic: query-client.ts imports only @tanstack/react-query,
       sonner, ./query-tiers — this import is NOT part of the D-25 router cycle). Without it, every
       soft-navigating sign-out leaves the previous user's rows resident for up to 10 minutes and
       served without refetch for 5 — on a clearance-gated product.
    2. `void import('@/router').then(({ router }) => router.navigate({ to: '/login' }))` — the
       LAZY form is REQUIRED (D-25): a module-level `import { router }` would NEWLY introduce the
       cycle authStore → router → routeTree.gen → routes/_protected.tsx → authStore (every edge
       verified twice; this repo has already shipped a production white-screen from a module cycle).
       Add a one-line comment stating exactly that, because the next reader's instinct is to
       simplify it back to a static import. Plain `/login`, no redirectTo (D-12); /login files
       untouched.
    NEGATIVE SCOPE (D-30) — state it in the SUMMARY: `queryClient.clear()` empties the IN-MEMORY
    cache only. Persisted localStorage residue (auth-storage, entity-history-storage, ui-storage,
    pinned-entities-storage, dossier-store, advanced-search-history, quickswitcher_recent_items) is
    a pre-existing leak filed to the backlog — a green cache criterion must not be read as
    "client-side residue cleared". Do not sweep it here.

    Then add authStore.signout.test.ts (vitest, colocated): seed the queryClient singleton with a
    dummy query entry, invoke handleAuthStateChange('SIGNED_OUT', null) (mock '@/router' with
    vi.mock so the dynamic import resolves to a spy), and assert
    `queryClient.getQueryCache().getAll().length === 0` AND the navigate spy was called with
    { to: '/login' }. This is the automated proof of the D-29 leak criterion — asserting the cache
    is empty, not merely that the route changed.

    HARD CONSTRAINTS (each is a REJECT if violated):
    - Do NOT edit frontend/src/services/auth.ts — dead module, zero importers (NAV-04 owns disposal).
    - Do NOT add a new onAuthStateChange subscription anywhere — five already exist.
    - Do NOT modify routes/_protected.tsx's beforeLoad — the reload path already works (D-18).
    - Do NOT add navigation or cache-clearing inside logout() — signOut() fires SIGNED_OUT, which
      now tears down and navigates; one seam covers deliberate logout (AUTH-01), the /settings
      surfaces, and forced invalidation (AUTH-03).

    FIVE-SITE EVIDENCE (D-22/D-10/D-11 — why exactly one site is touched; without this table
    "we only changed one line" is indistinguishable from an incomplete fix):
    | Site | Today on invalidation | Disposition |
    |------|----------------------|-------------|
    | store/authStore.ts:267 → handleAuthStateChange | clears state, navigates nowhere → ghost shell | THE FIX SITE — teardown + navigation |
    | services/auth.ts:635 | never runs — zero importers; also persists a 2nd zustand store under the same 'auth-storage' key | DEAD — do not edit, do not revive (NAV-04) |
    | hooks/useUnifiedWorkRealtime.ts:166 | re-subscribes its realtime concern | benign no-op — confirm only |
    | hooks/useActivityFeed.ts:44 | re-subscribes its feed | benign no-op — confirm only |
    | auth/ResetPasswordPage.tsx:25 | PASSWORD_RECOVERY only | benign no-op — confirm only |

  </action>
  <verify>
    <automated>cd frontend && pnpm type-check && pnpm exec vitest run src/store/authStore.signout.test.ts && test "$(grep -v '^[[:space:]]*//' src/store/authStore.ts | grep -c "import('@/router')")" -eq 1 && git rev-parse -q --verify refs/tags/phase-92-base >/dev/null && test "$(git diff --name-only phase-92-base -- src/services/auth.ts src/routes/_protected.tsx | wc -l)" -eq 0</automated>
  </verify>
  <acceptance_criteria>SIGNED_OUT branch clears the query cache BEFORE navigating; navigation uses the lazy import with the why-comment; unit test proves getQueryCache().getAll().length === 0 post-teardown (in-memory bound stated per D-30); services/auth.ts, _protected.tsx, and logout() untouched; no new subscription; five-site table in the SUMMARY.</acceptance_criteria>
  <done>SIGNED_OUT — from any trigger — tears down the cache and drives the open tab to /login; every sign-out path this phase adds is safe by construction.</done>
</task>

<task type="auto">
  <name>Task 3: /settings — add the plain control in Security, fix the lying DataPrivacy label (D-02, D-03, D-28)</name>
  <files>frontend/src/components/settings/sections/SecuritySettingsSection.tsx, frontend/src/components/settings/sections/DataPrivacySettingsSection.tsx, frontend/src/i18n/en/settings.json, frontend/src/i18n/ar/settings.json</files>
  <read_first>SecuritySettingsSection.tsx:1-40 (imports/bindings) and :132-152, :193 (row + group geometry); DataPrivacySettingsSection.tsx:164-179 (handleSignOutAll) and :282-304 (trigger + dialog); 92-UI-SPEC.md Surface 2 + copy table</read_first>
  <action>
    ADD (D-28.1): a "Session" SettingsGroup in SecuritySettingsSection with one SettingsItem row:
    label "Sign out", Meta-size description "End your session on this device.", and a `.btn-ghost`
    button on the end side with `data-testid="settings-signout"`. Section choice rationale (say it
    in the SUMMARY): Security is the account/session context — it already owns signOutConfirm and
    session-timeout copy — whereas Data & Privacy is exactly the burial D-28 names. NOT .btn-primary
    (reserved for saves), NOT danger. One click, no confirmation. The button calls `logout()` from
    `useAuth()` — the same seam NavUser uses — and performs NO navigation or cache-clearing itself
    (Task 2's seam owns both). Independent of the sidebar mount (D-02).
    i18n (en+ar in the same edit, settings namespace under security.*):
    en: session: "Session", signOut: "Sign out", signOutDesc: "End your session on this device."
    ar: session: "الجلسة", signOut: "تسجيل الخروج", signOutDesc: "إنهاء جلستك على هذا الجهاز."

    RELABEL (D-28.2): at settings.json:200 (both locales) `dataPrivacy.signOutAll` currently claims
    "Other" while scope:'global' ends THIS session too — it works and lies. Change to sentence-case
    copy that stops claiming "other": en "Sign Out All Other Sessions" → "Sign out of all sessions";
    ar "تسجيل الخروج من جميع الجلسات الأخرى" → "تسجيل الخروج من جميع الجلسات". One edit fixes copy
    truth and the house sentence-case rule together. Do NOT change `scope: 'global'` — what the
    button does to other devices is a separate product decision; the relabel removes the lie. The
    trigger (:286) and dialog confirm (:301) render the same key, so both update automatically.
    Add `data-testid="signout-all-sessions"` to the trigger button (D-28.3 — /settings now holds
    2+ elements matching the shared name regex; tests must target testids).

    DE-NAVIGATE (D-29): in handleSignOutAll, remove the `setTimeout(() => { logout();
    window.location.href = '/login' }, 1000)` block entirely and keep only the success toast.
    `signOut({ scope: 'global' })` already emits SIGNED_OUT, and Task 2's seam now owns teardown
    (queryClient.clear) and navigation — the reload's only load-bearing function is covered
    centrally, and the redundant logout() call re-invoked signOut a second time. Leave a one-line
    comment: "navigation + cache teardown owned by authStore's SIGNED_OUT handler (D-29) — do not
    restore the reload." There is no double-navigation to accept: one owner navigates.

    Scope fence (D-03): do NOT touch AppShell.tsx:125's startsWith('/settings') check or
    routes/_protected/settings.tsx's exact-match check — the /settings/* children-without-nav
    defect is NAV-02. Reachability here is NavUser's two existing /settings links.
    Row geometry: 1px solid var(--line) separations, logical properties throughout.

  </action>
  <verify>
    <automated>cd frontend && pnpm type-check && ! grep -q 'window.location.href' src/components/settings/sections/DataPrivacySettingsSection.tsx && grep -q 'data-testid="settings-signout"' src/components/settings/sections/SecuritySettingsSection.tsx && grep -q 'data-testid="signout-all-sessions"' src/components/settings/sections/DataPrivacySettingsSection.tsx && node -e "const en=require('./src/i18n/en/settings.json'),ar=require('./src/i18n/ar/settings.json');process.exit(en.dataPrivacy.signOutAll.includes('Other')||ar.dataPrivacy.signOutAll.includes('الأخرى')||!(en.security.session&&en.security.signOut&&ar.security.session&&ar.security.signOut)?1:0)"</automated>
  </verify>
  <acceptance_criteria>Session group renders in Security with the testid'd sign-out row; clicking it ends the session, empties the cache, and lands on /login via the Task-2 seam; DataPrivacy label no longer claims "other" in either locale and is sentence case; scope:'global' unchanged; the reload block is gone with the why-comment in its place; AppShell.tsx and routes/_protected/settings.tsx untouched.</acceptance_criteria>
  <done>/settings has a discoverable, truthful sign-out story: one plain control in Security, one honestly-labelled global control in Data & Privacy, both test-addressable.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                               |
| ------------------------------ | ----------------------------------------- |
| browser session → auth state   | SIGNED_OUT event drives UI trust state    |
| sign-out redirect              | navigation target chosen by app code      |
| shared workstation → next user | client-side residue crosses user sessions |

## STRIDE Threat Register

| Threat ID | Category               | Component                   | Disposition | Mitigation Plan                                                                                                                                                                                            |
| --------- | ---------------------- | --------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-92-01   | Spoofing               | sign-out flow               | mitigate    | acceptance asserts the supabase storage key is null after sign-out (D-04) — session cleared, not merely navigated away from                                                                                |
| T-92-03   | Spoofing (UI state)    | authStore SIGNED_OUT branch | mitigate    | reactive navigation ends the ghost-authenticated shell; e2e test keeps it fixed                                                                                                                            |
| T-92-14   | Information Disclosure | TanStack Query cache        | mitigate    | queryClient.clear() at the single-owner seam before navigation; unit test asserts empty cache (D-29). Bounded: in-memory only — persisted localStorage residue is pre-existing, filed, out of scope (D-30) |
| T-92-06   | Tampering              | redirect target             | mitigate    | plain hardcoded '/login' (D-12) — no redirectTo param, no open-redirect surface                                                                                                                            |
| T-92-13   | Tampering              | /settings test locators     | mitigate    | testids on both controls — the shared name regex could click the destructive global sign-out (D-28.3)                                                                                                      |
| T-92-07   | Elevation              | role rendering              | accept      | SIGNED_IN branch (role from public.users.role) untouched by this plan                                                                                                                                      |
| T-92-SC   | Tampering              | package installs            | accept      | no installs                                                                                                                                                                                                |

</threat_model>

<verification>
- D-26 GREEN capture: `pnpm exec playwright test tests/e2e/92-signout.spec.ts --project=chromium-en --no-deps` — THE SAME COMMAND 92-01 measured the RED with (never a different oracle: pairing one oracle's red with another's green is the substitution RULING-37 forbids, and `01-login.spec.ts` cannot run here at all — no `--no-deps`, `setup` dependency, E2ECRED-01). Re-run it (never reasoned about: the user-menu branch turns true for the first time, a different path than the RED run) and record the actual output in the SUMMARY beside 92-01's RED.
- VALIDITY RULE, carried from 92-01 (RULING-37): the GREEN counts ONLY if the run reached the sign-out assertions and passed THERE. A failure or pass decided before that point — the `setup` project, the inline sign-in, rotated credentials, staging down, navigation, or a login-form selector — is recorded as `UNABLE TO MEASURE — <the actual error>` and is NEVER recorded as the green. An honest `UNABLE TO MEASURE` flagged to the orchestrator is worth more than a green you cannot attribute.
- `pnpm exec playwright test tests/e2e/92-signout.spec.ts --project=chromium-en --no-deps` — all 3 tests green (dev server on :5173 or E2E_BASE_URL set).
- `cd frontend && pnpm exec vitest run src/store/authStore.signout.test.ts` green.
- `cd frontend && pnpm type-check` clean; ESLint clean on touched files.
- Storage-state staleness (reverse of D-15): --no-deps skips the Playwright setup project and reuses tests/e2e/support/storage/admin.json. If CARRY-01 (92-10) rotated credentials mid-phase, re-run the setup project first — otherwise the run executes unauthenticated and reads as an auth regression.
- RTL/Tajawal render of the dropdown and settings rows is deferred to the phase-end human check (92-VALIDATION Manual-Only table).
</verification>

<success_criteria>
Sign-out works from both surfaces and lands on /login with storage cleared AND the query cache
empty; the forced-invalidation repro bounces the open tab within 45s; 92-signout.spec.ts's
RED→GREEN flip is on record as the PAIR of outputs from the one `--no-deps` command (or an
`UNABLE TO MEASURE — <error>` if the run never reached the sign-out assertion — RULING-37); the
DataPrivacy label tells the truth; Arabic renders تسجيل الخروج.
</success_criteria>

<output>
Create `.planning/phases/92-session-integrity-edge-function-auth/92-02-SUMMARY.md` when done,
including: the five-site classification table verbatim, the RED and GREEN outputs of
`92-signout.spec.ts --no-deps` (the same command both times — or `UNABLE TO MEASURE — <error>` per
RULING-37), the Security-section rationale, and the D-30 negative-scope statement.
</output>
