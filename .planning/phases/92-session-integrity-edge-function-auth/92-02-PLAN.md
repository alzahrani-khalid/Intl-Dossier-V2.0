---
phase: 92-session-integrity-edge-function-auth
plan: 02
type: execute
wave: 2
depends_on: [92-01]
files_modified:
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/components/layout/nav-user.tsx
  - frontend/src/components/layout/header/UserMenu.tsx
  - frontend/src/store/authStore.ts
  - frontend/src/components/settings/sections/SecuritySettingsSection.tsx
  - frontend/src/i18n/en/common.json
  - frontend/src/i18n/en/settings.json
  - frontend/src/i18n/ar/settings.json
autonomous: true
requirements: [AUTH-01, AUTH-03, AUTH-05]
truths:
  - 'Decisions covered — D-01: the live sign-out control is the existing NavUser mounted as the sidebar user card, not a new build; D-23: NavUser has an unsatisfied useSidebar provider dependency — adapt the consumer to useResponsive, never wrap the shell in SidebarProvider'
  - "Decisions covered — D-02: /settings exposes a second, independent sign-out control; D-03: AUTH-05 reachability is satisfied by NavUser's existing /settings links — the /settings/* subtree nav defect is NAV-02, a later phase, and is not touched here"
  - "Decisions covered — D-22: exactly ONE live seam is edited — authStore.handleAuthStateChange's SIGNED_OUT branch — with the five-site live/no-op/dead classification carried as evidence; D-10: no sixth onAuthStateChange subscription is added; D-11: the dead services/auth.ts module is NOT edited"
  - "Decisions covered — D-12: the forced redirect is a plain /login navigation with no redirectTo and /login stays out of files_modified; D-18: _protected.tsx's beforeLoad guard already works and is a non-goal"
  - "Decisions covered — D-24: the sign-out label becomes t('common.logout') — dot form, second argument DROPPED — repointing one existing key, no new keys, no class sweep (that is AR-04a / Phase 99)"
must_haves:
  truths:
    - 'A signed-in user can sign out from the sidebar user card and lands on /login with the supabase auth storage key cleared'
    - 'A signed-in user can sign out from /settings via an independent control with the same result'
    - 'Invalidating the session out-of-band bounces the already-open tab to /login within one 30s gotrue tick — no reload, no ghost "Member/Member" shell'
    - 'The Arabic UI renders تسجيل الخروج for the sign-out item (no inline-English fallback masking)'
  artifacts:
    - path: 'frontend/src/components/layout/Sidebar.tsx'
      provides: 'NavUser mounted in place of the static user card'
      contains: 'NavUser'
    - path: 'frontend/src/components/layout/nav-user.tsx'
      provides: "Adapted consumer: useResponsive instead of useSidebar; t('common.logout'); data-testid user-menu"
      contains: 'useResponsive'
    - path: 'frontend/src/store/authStore.ts'
      provides: 'SIGNED_OUT branch navigates to /login via lazy router import'
      contains: "import('@/router')"
  key_links:
    - from: 'frontend/src/store/authStore.ts'
      to: 'frontend/src/router/index.tsx'
      via: 'dynamic import inside the SIGNED_OUT branch (module-level import creates a real cycle)'
      pattern: "import\\('@/router'\\)"
    - from: 'frontend/src/components/settings/sections/SecuritySettingsSection.tsx'
      to: 'useAuth().logout()'
      via: 'same seam NavUser uses — neither surface navigates itself'
      pattern: 'logout\('
---

<objective>
Make session end honest on all three fronts (AUTH-01, AUTH-03, AUTH-05): mount the existing
NavUser dropdown as the sidebar user card, add a second independent sign-out row to /settings,
and add ONE navigation line at the SIGNED_OUT seam in authStore so both deliberate logout and
forced invalidation land the tab on /login with the session cleared.

Purpose: today nothing in the live shell can sign out, and an invalidated session decays into a
"Member/Member" ghost page. One seam edit + two mounts close both.
Output: working sign-out from two surfaces; reactive bounce on invalidation; 92-signout.spec.ts green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-session-integrity-edge-function-auth/92-UI-SPEC.md  (Surface contracts 1-2 — binding)
@.planning/phases/92-session-integrity-edge-function-auth/92-PATTERNS.md  (authStore, Sidebar, nav-user, SecuritySettingsSection assignments)
@frontend/src/components/layout/Sidebar.tsx
@frontend/src/components/layout/nav-user.tsx
@frontend/src/store/authStore.ts
@frontend/src/hooks/useResponsive.ts
@frontend/src/components/settings/sections/SecuritySettingsSection.tsx

<interfaces>
Verified this session:
- Mount seam: `Sidebar.tsx:101-114` static user card (accent initials disc, two-line name+role,
  `--radius-sm` wash). Locals in scope: `getInitials` :48-56, `displayName`/`roleLabel`/`isRTL` :67-71.
- `nav-user.tsx:15,27` imports `useSidebar` — `SidebarProvider` renders NOWHERE in the app;
  `useResponsive()` (frontend/src/hooks/useResponsive.ts) exposes the SAME `isMobile` field name.
- `nav-user.tsx:94`: `t('navigation.logout', 'Logout')` — key missing in both locales.
  `frontend/src/i18n/en/common.json:68` has `"logout": "Logout"`; `ar/common.json:68` has
  `"logout": "تسجيل الخروج"`. Dead-but-correct usage: `Header.tsx:102` `{t('common.logout')}`.
  `common.logout` has ZERO live consumers (grep verified) — changing the en value is safe.
- Live seam: `authStore.ts:201-212` `handleAuthStateChange` SIGNED_OUT branch; wired via
  `subscribeToAuthChanges()` :264-271 → `AuthListenerManager.tsx:11` → `App.tsx:32`.
  `authStore.logout()` :105-130 clears state, does NOT navigate — correct as-is.
- Router: `frontend/src/router/index.tsx:62` `export const router = createRouter(...)`.
  IMPORT CYCLE IS REAL: router → routeTree.gen → _protected.tsx → authStore. Use the lazy form.
- Settings row analog: `SecuritySettingsSection.tsx:132-152` (SettingsGroup/SettingsItem geometry),
  group title prop at :193; file already imports Button (:5) and binds `useTranslation('settings')` (:34).
- e2e wiring: `tests/e2e/support/pages/LoginPage.ts:18-19` finds sign-out by role-regex;
  `01-login.spec.ts:34` tries `getByTestId('user-menu')` first — put that testid on the trigger.
- `header/UserMenu.tsx` has zero importers (grep verified) — safe to delete.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount NavUser as the sidebar user card; delete the second orphan (D-01, D-23, D-24)</name>
  <files>frontend/src/components/layout/Sidebar.tsx, frontend/src/components/layout/nav-user.tsx, frontend/src/components/layout/header/UserMenu.tsx, frontend/src/i18n/en/common.json</files>
  <read_first>Sidebar.tsx:28-40 (import style) and :48-114 (locals + card block); nav-user.tsx in full (~110 lines); 92-UI-SPEC.md Surface 1 states table</read_first>
  <action>
    Adapt nav-user.tsx per D-23/D-24, then mount it per D-01:
    (a) Replace the `useSidebar` import/call with `useResponsive` from '@/hooks/useResponsive' —
        same `isMobile` field, used once at :60 for the dropdown `side`; keep that expression.
        Do NOT wrap anything in SidebarProvider.
    (b) Change line 94's call to exactly `t('common.logout')` — dot form, SECOND ARGUMENT DROPPED
        (D-24: the English default only masks a future key regression; the key exists in both locales).
        Repoint this ONE key only — no new keys, no sweep of other `t('key','default')` sites (Phase 99).
    (c) Add `data-testid="user-menu"` to the dropdown trigger so `01-login.spec.ts:34` and the new
        spec find it.
    (d) In en/common.json:68 change the value `"Logout"` → `"Sign out"` (UI-SPEC copy contract;
        sentence case). LEAVE ar/common.json untouched — تسجيل الخروج is already correct.
    (e) In Sidebar.tsx, import NavUser (relative, beside './navigation-config') and replace the
        static card block at :101-114 with the mounted control. The rendered default state must keep
        the card's visual identity per UI-SPEC: accent initials disc, two-line name+role, the
        `--radius-sm` card wash on `--sidebar-bg` — adapt NavUser's trigger to that markup rather
        than shipping shadcn sidebar-menu chrome that looks foreign. Open state: menu on
        `--surface-3`, `1px solid var(--line)`, `--radius`; Sign out is plain ink, NOT danger.
        Remove any Sidebar locals your replacement orphans (only ones YOUR edit makes unused).
    (f) Delete frontend/src/components/layout/header/UserMenu.tsx (zero importers, verified —
        re-verify with `grep -rn "header/UserMenu" frontend/src --include='*.ts*'` before deleting).
        Two logout menus is a worse outcome than one orphan; mounting exactly one is D-01.
    Logical properties only (ms-*/ps-*/text-start); no raw hex; flip directional icons under RTL.
  </action>
  <verify>
    <automated>cd frontend && pnpm typecheck && ! grep -q "useSidebar" src/components/layout/nav-user.tsx && grep -q "data-testid=\"user-menu\"" src/components/layout/nav-user.tsx && ! test -f src/components/layout/header/UserMenu.tsx</automated>
  </verify>
  <acceptance_criteria>NavUser renders as the sidebar user card (desktop column AND the mobile drawer — both mount the same Sidebar); dropdown opens with Profile//settings, Settings//settings, Sign out items; `t('common.logout')` has no second argument; en value is "Sign out", ar untouched; UserMenu.tsx deleted; typecheck clean.</acceptance_criteria>
  <done>Sidebar user card is an interactive dropdown preserving today's visual identity; exactly one logout menu exists in the tree.</done>
</task>

<task type="auto">
  <name>Task 2: One navigation line at the SIGNED_OUT seam (D-22, D-12, D-18)</name>
  <files>frontend/src/store/authStore.ts</files>
  <read_first>authStore.ts:105-130 (logout — do not touch), :201-212 (SIGNED_OUT branch — the seam), :264-271 (subscription wiring); PATTERNS "authStore" section (verified cycle + lazy form)</read_first>
  <action>
    Inside the existing SIGNED_OUT/USER_DELETED branch of handleAuthStateChange, after the state
    clear, add exactly one navigation:
    `void import('@/router').then(({ router }) => router.navigate({ to: '/login' }))`
    Lazy import is REQUIRED — a module-level `import { router }` creates the verified cycle
    authStore → router → routeTree.gen → _protected.tsx → authStore. Plain `/login`, no redirectTo
    param, no allowlist (D-12, operator-decided); `/login` route files are not touched.

    HARD CONSTRAINTS (each is a REJECT if violated):
    - Do NOT edit frontend/src/services/auth.ts — its onAuthStateChange never registers (zero
      importers); editing it ships nothing while looking thorough. Its disposal is filed to NAV-04.
    - Do NOT add a new onAuthStateChange subscription anywhere — five already exist.
    - Do NOT modify routes/_protected.tsx's beforeLoad — the reload path already bounces correctly (D-18).
    - Do NOT add navigation inside logout() — signOut() fires SIGNED_OUT, which now navigates;
      one line covers deliberate logout (AUTH-01) and forced invalidation (AUTH-03) together.

    FIVE-SITE EVIDENCE (D-22/D-10/D-11 — why exactly one site is touched; without this table
    "we only changed one line" is indistinguishable from an incomplete fix):
    | Site | Today on invalidation | Disposition |
    |------|----------------------|-------------|
    | store/authStore.ts:267 → handleAuthStateChange | clears state, navigates nowhere → ghost shell | THE FIX SITE — add navigation |
    | services/auth.ts:635 | never runs — module has zero importers; also persists a 2nd zustand store under the same 'auth-storage' key | DEAD — do not edit, do not revive (NAV-04) |
    | hooks/useUnifiedWorkRealtime.ts:166 | re-subscribes its realtime concern | benign no-op — confirm only |
    | hooks/useActivityFeed.ts:44 | re-subscribes its feed | benign no-op — confirm only |
    | auth/ResetPasswordPage.tsx:25 | PASSWORD_RECOVERY only | benign no-op — confirm only |

  </action>
  <verify>
    <automated>cd frontend && pnpm typecheck && grep -c "import('@/router')" src/store/authStore.ts | grep -qx 1 && git diff --name-only HEAD -- src/services/auth.ts src/routes/_protected.tsx | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>Exactly one navigation line added, inside the existing SIGNED_OUT branch, lazy-import form; services/auth.ts, _protected.tsx, and logout() untouched; no new subscription; the five-site table appears in the SUMMARY as evidence.</acceptance_criteria>
  <done>SIGNED_OUT — from any trigger — drives the open tab to /login; the diff is one line plus nothing.</done>
</task>

<task type="auto">
  <name>Task 3: /settings Session group with sign-out row (D-02, D-03)</name>
  <files>frontend/src/components/settings/sections/SecuritySettingsSection.tsx, frontend/src/i18n/en/settings.json, frontend/src/i18n/ar/settings.json</files>
  <read_first>SecuritySettingsSection.tsx:1-40 (imports/bindings) and :132-152, :193 (row + group geometry); 92-UI-SPEC.md Surface 2 + copy table</read_first>
  <action>
    Add a "Session" SettingsGroup to SecuritySettingsSection with one SettingsItem row: label
    "Sign out", Meta-size description "End your session on this device.", and a `.btn-ghost` button
    on the end side (NOT .btn-primary — reserved for save actions; NOT danger — signing out is a
    session end, not data destruction). One click, no confirmation dialog. The button calls
    `logout()` from `useAuth()` — the same seam NavUser uses — and performs NO navigation itself
    (Task 2's seam handles landing on /login). This surface must work even if the sidebar mount
    regresses (D-02: independent).

    i18n: add to the `settings` namespace under `security.*` in BOTH en and ar in this same edit
    (static bundle — a missing key silently falls back to English in both languages):
    en: session: "Session", signOut: "Sign out", signOutDesc: "End your session on this device."
    ar: session: "الجلسة", signOut: "تسجيل الخروج", signOutDesc: "إنهاء جلستك على هذا الجهاز."

    Scope fence (D-03): do NOT touch AppShell.tsx:125's startsWith('/settings') check or
    routes/_protected/settings.tsx's exact-match check — the /settings/* children-without-nav
    defect is NAV-02, a later phase. Reachability here is NavUser's two existing /settings links.
    Row geometry: 1px solid var(--line) separations, logical properties throughout.

  </action>
  <verify>
    <automated>cd frontend && pnpm typecheck && node -e "const en=require('./src/i18n/en/settings.json'),ar=require('./src/i18n/ar/settings.json');const g=o=>o.security&&o.security.session&&o.security.signOut&&o.security.signOutDesc;process.exit(g(en)&&g(ar)?0:1)"</automated>
  </verify>
  <acceptance_criteria>Session group renders in the Security section with the sign-out row; clicking it ends the session and lands on /login (via the Task-2 seam, not local navigation); both locales carry all three keys; AppShell.tsx and routes/_protected/settings.tsx untouched.</acceptance_criteria>
  <done>Second independent sign-out surface live on /settings.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                            |
| ---------------------------- | -------------------------------------- |
| browser session → auth state | SIGNED_OUT event drives UI trust state |
| sign-out redirect            | navigation target chosen by app code   |

## STRIDE Threat Register

| Threat ID | Category            | Component                   | Disposition | Mitigation Plan                                                                                                             |
| --------- | ------------------- | --------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------- |
| T-92-01   | Spoofing            | sign-out flow               | mitigate    | acceptance asserts the supabase storage key is null after sign-out (D-04) — session cleared, not merely navigated away from |
| T-92-03   | Spoofing (UI state) | authStore SIGNED_OUT branch | mitigate    | reactive navigation ends the ghost-authenticated shell; e2e test 3 keeps it fixed                                           |
| T-92-06   | Tampering           | redirect target             | mitigate    | plain hardcoded '/login' (D-12) — no redirectTo param, no open-redirect surface introduced in a security phase              |
| T-92-07   | Elevation           | role rendering              | accept      | SIGNED_IN branch (role from public.users.role) untouched by this plan                                                       |
| T-92-SC   | Tampering           | package installs            | accept      | no installs                                                                                                                 |

</threat_model>

<verification>
- `pnpm exec playwright test tests/e2e/92-signout.spec.ts --project=chromium-en --no-deps` — all 3 tests green (dev server on :5173 or E2E_BASE_URL set).
- `cd frontend && pnpm typecheck` clean; ESLint clean on touched files.
- RTL/Tajawal render of the dropdown and settings row is deferred to the phase-end human check (92-VALIDATION Manual-Only table).
</verification>

<success_criteria>
Sign-out works from both surfaces and lands on /login with storage cleared; the forced-invalidation
repro bounces the open tab within 45s; Arabic renders تسجيل الخروج; exactly one file changed for AUTH-03.
</success_criteria>

<output>
Create `.planning/phases/92-session-integrity-edge-function-auth/92-02-SUMMARY.md` when done,
including the five-site classification table verbatim.
</output>
