---
phase: 92-session-integrity-edge-function-auth
plan: 10
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [CARRY-01]
truths:
  - 'Decisions covered — D-14: CARRY-01 is an operator act, not code — a checklist with an explicit verification step (GitHub Actions secrets updated, .env.test updated, login smoke passing), owned by the operator'
  - 'Decisions covered — D-15: no other criterion in this phase depends on CARRY-01 — the phase closes its four code criteria with this plan still outstanding; nothing depends_on this plan'
must_haves:
  truths:
    - 'The P88-02 credentials are rotated; the old values no longer authenticate'
    - 'GitHub Actions secrets and local .env.test carry the new values — including all six E2E_{ADMIN,ANALYST,INTAKE}_{EMAIL,PASSWORD} keys in .env.test, without which the Playwright setup project throws and this plan gate cannot reach its subject'
    - 'The existing login smoke passes with the rotated credentials'
    - 'No credential value appears in any commit, log, or summary'
  artifacts: []
  key_links:
    - from: 'GitHub Actions secrets E2E_ADMIN_*/E2E_ANALYST_*/E2E_INTAKE_* (all six — auth.setup.ts:10-13)'
      to: '.github/workflows/ci.yml + e2e.yml'
      via: 'secret consumption at ci.yml:244-245,300-301,340-341,387-388 and e2e.yml:67-68'
      pattern: 'E2E_(ADMIN|ANALYST|INTAKE)_'
---

<objective>
CARRY-01: the operator rotates the P88-02 credentials (an operator act, not code — D-14) and
verification proves the rotation: secrets updated in all three stores, login smoke green.

This plan blocks nothing (D-15): it sits in wave 1 with no dependents, and Phase 92's four code
criteria close with it still outstanding — it held Phase 88 open once; it must not hold 92.
It is scheduled here, ten phases ahead of the CARRY-02/CARRY-05 gates it feeds in Phase 101.

Purpose: retire the exposed P88-02 credentials without letting the rotation gate the phase.
Output: rotated credentials verified by the existing login smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-session-integrity-edge-function-auth/92-RESEARCH.md  (CARRY-01 checklist skeleton + secret-name inventory — verified against the workflows)
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator rotates the P88-02 credentials</name>
  <action>Pause for the operator: rotation of live credentials cannot and must not be automated from this session. Present the checklist below and wait for the resume signal.</action>
  <what-built>Nothing automated — rotating a live secret is operator-only: the GitHub Actions secret store is not writable from this session, and the house rule forbids echoing credential values anywhere Claude can see them.</what-built>
  <how-to-verify>
    Operator checklist (never paste any value into this session):
    1. Rotate the staging test users' passwords — Supabase dashboard (project zkrcjzdemdmwhearhfgg
       → Authentication → Users) or auth.admin.updateUserById.
    2. Update the GitHub Actions repository secrets. SIX are required, not four:
       E2E_ADMIN_EMAIL, E2E_ADMIN_PASSWORD, E2E_ANALYST_EMAIL, E2E_ANALYST_PASSWORD,
       **E2E_INTAKE_EMAIL, E2E_INTAKE_PASSWORD** — `tests/e2e/support/auth.setup.ts:10-13` reads
       all three roles and `:18-22` throws unless every one is a string. `e2e.yml` already consumes
       all six; `ci.yml` consumes only four (0 occurrences of `E2E_INTAKE` — measured), which is a
       CI defect owned by E2ECRED-01 / Phase 101 and is NOT fixed here.
    3. Update local .env.test with BOTH sets: TEST_USER_EMAIL / TEST_USER_PASSWORD (template
       .env.test.example:18-19) AND the same six E2E_* keys. This is load-bearing, not tidiness:
       `.env.test` carries ZERO E2E_ keys today (measured), so without them step 4 below and this
       plan's own gate cannot run at all — the `setup` project throws
       "Missing E2E_ADMIN_EMAIL or _PASSWORD" and all three 01-login tests report "did not run".
       File is git-ignored; never paste any value into this session. Confirm the old values no
       longer log in.
    4. Re-run the Playwright `setup` project to regenerate the saved storage state
       (tests/e2e/support/storage/admin.json) — downstream e2e runs use --no-deps, which SKIPS
       setup and reuses that file; after rotation it holds a dead session and every such run
       executes unauthenticated, reading as an auth regression (reverse of D-15). Step 3 is this
       step's precondition.
  </how-to-verify>
  <resume-signal>Type "rotated" when all three stores are updated (or "defer" to close the phase's code criteria with CARRY-01 still open — D-15 permits this)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Login smoke with rotated credentials</name>
  <files>(none — verification only)</files>
  <read_first>tests/e2e/01-login.spec.ts (env-guarded; reads .env.test)</read_first>
  <action>
    Run the existing login smoke against the app with the rotated .env.test values. Locally:
    `pnpm exec playwright test tests/e2e/01-login.spec.ts --project=chromium-en` (dev server on
    :5173 or E2E_BASE_URL). Note: CI's e2e.yml runs against the DEPLOYED app — a green CI run
    additionally requires the deploy to be current; the local smoke is the acceptance here.
    Record pass/fail in the SUMMARY. NEVER echo the credential values.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/01-login.spec.ts --project=chromium-en</automated>
  </verify>
  <acceptance_criteria>Login smoke green with the rotated credentials; no credential value in any output committed to the repo or summaries. VALIDITY RULE (RULING-37, carried from 92-01): the run counts as the acceptance ONLY if it reached the 01-login assertions. A failure in the `setup` project — i.e. `auth.setup.ts` throwing "Missing E2E_&lt;ROLE&gt;_EMAIL or _PASSWORD", which is what happens whenever checklist step 3's six E2E_* keys are absent from .env.test — is recorded as `UNABLE TO MEASURE — <the actual error>` and NEVER as a failed rotation; it means the checklist was not completed, not that the credentials are bad.</acceptance_criteria>
  <done>CARRY-01 verified: rotation complete, smoke green — the Phase 101 gate (CARRY-02/CARRY-05) is unblocked.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                  |
| ------------------------ | -------------------------------------------- |
| operator ↔ secret stores | live credentials handled outside the session |

## STRIDE Threat Register

| Threat ID | Category               | Component           | Disposition | Mitigation Plan                                                                                                                        |
| --------- | ---------------------- | ------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| T-92-05   | Information Disclosure | credential handling | mitigate    | values never echoed, never committed; .env.test stays git-ignored; checklist instructs the operator to keep values out of this session |
| T-92-12   | Spoofing               | stale credentials   | mitigate    | rotation invalidates the exposed P88-02 values; smoke proves the new ones work and the checklist confirms the old ones do not          |
| T-92-SC   | Tampering              | package installs    | accept      | no installs                                                                                                                            |

</threat_model>

<verification>
Login smoke green post-rotation; operator confirmation on record; zero secret values in the repo.
</verification>

<success_criteria>
Criterion 5 closed by the operator with verification — without any other criterion having waited on it.
</success_criteria>

<output>
Create `.planning/phases/92-session-integrity-edge-function-auth/92-10-SUMMARY.md` when done
(or when deferred, recording the deferral per D-15).
</output>
