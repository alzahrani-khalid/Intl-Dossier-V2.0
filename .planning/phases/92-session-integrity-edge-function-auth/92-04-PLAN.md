---
phase: 92-session-integrity-edge-function-auth
plan: 04
type: execute
wave: 2
depends_on: [92-01]
files_modified:
  - supabase/functions/audit-logs-viewer/index.ts
  - supabase/functions/data-retention/index.ts
  - supabase/functions/field-permissions/index.ts
  - supabase/functions/my-delegations/index.ts
  - supabase/functions/_shared/auth.ts
autonomous: true
requirements: [AUTH-02, AUTH-04]
truths:
  - 'Decisions covered — D-19: the 133 is not one population; the three confirmed-broken functions (audit-logs-viewer, data-retention, field-permissions) are the must-fix core with their own acceptance, closed against the nine observed 401s — the other 130 are the sweep with grep-derivation acceptance in plans 92-05..92-08'
  - 'Decisions covered — D-05: the end state per migrated function is @supabase/supabase-js@2 + getUser(token), the house edge-function-add pattern — a helper refactor is not a precondition for the version bump'
  - 'Decisions covered — D-06: _shared/auth.ts is itself in scope — its line-2 @2.39.3 pin is the exact deprecated pin this requirement retires; validateJWT already calls getUser(token), so this is a pin bump, not a rewrite'
  - "Decisions covered — D-20: data-retention and field-permissions are exactly the surfaces Phase 93's criterion 2 turns on — this plan names that dependency so Phase 93's planner inherits them as a known-closed precondition instead of re-deriving them"
must_haves:
  truths:
    - 'The three confirmed-broken functions and my-delegations accept a valid session JWT on deployed staging (probe flips from the baseline 401s to non-401)'
    - 'Every migrated function keeps its header-injected client, so RLS scoping survives the migration (no 200-with-[] where data existed)'
    - '_shared/auth.ts no longer pins 2.39.3, and its ten importers are redeployed so their bundled artifacts pick up the bump'
  artifacts:
    - path: 'supabase/functions/my-delegations/index.ts'
      provides: 'AUTH-04 auth half: jsr @2 specifier + getUser(token), injected client intact'
      contains: 'getUser(token)'
    - path: 'supabase/functions/_shared/auth.ts'
      provides: 'Canonical validateJWT helper on the current wrapper'
      contains: 'jsr:@supabase/supabase-js@2'
  key_links:
    - from: 'supabase/functions/*/index.ts'
      to: 'GoTrue /auth/v1/user'
      via: 'explicit-token getUser(token) — version-independent path'
      pattern: 'getUser\(token\)'
    - from: 'migrated functions'
      to: 'PostgREST with RLS'
      via: 'the UNTOUCHED header-injected client'
      pattern: 'global: \{ headers: \{ Authorization'
---

<objective>
Fix the confirmed-broken AUTH-02 core (D-19): migrate the three functions with observed 401s —
audit-logs-viewer, data-retention, field-permissions — plus my-delegations (the AUTH-04 auth half,
same Class-1 shape, its 401s observed in the audit's engagements lane F4), bump the
_shared/auth.ts pin (D-06), deploy them plus the helper's importers to staging, and prove the flip
with the probe.

Cross-phase dependency named per D-20: data-retention and field-permissions are the surfaces Phase
93's criterion 2 turns on (/admin/data-retention, /admin/field-permissions showing real rows or
honest errors). Phase 93 inherits them as known-closed once this plan's deploy lands.

Purpose: these are the only functions with demonstrated per-function failures; they close against
that evidence, separately from the version-hygiene sweep.
Output: 4 migrated + deployed functions, bumped shared helper, redeployed importers, probe flip on record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-session-integrity-edge-function-auth/92-PATTERNS.md  (Class 1/3 edit specs + per-file evidence anchors)
@.planning/phases/92-session-integrity-edge-function-auth/92-RESEARCH.md  (version-skew mechanism; Pitfall 1)
@supabase/functions/access-requests/index.ts  (target end state: :7 jsr specifier, :57-61 getUser(token) — copy the CALL shape, NOT its service-role client construction)

<interfaces>
Per-file evidence anchors (verified by the pattern mapper):
| Function | 2.3x import | Injected client | Bare getUser() | Error idiom (KEEP as-is) |
|----------|------------|-----------------|----------------|--------------------------|
| audit-logs-viewer/index.ts | :21 | :371-379 | :382-385 | errorResponse(...) via _shared/utils.ts |
| data-retention/index.ts | :2 | :77-85 | :88-91 | inline bilingual { code, message_en, message_ar } |
| field-permissions/index.ts | :2 | :165-173 | :176-179 | createErrorResponse local helper |
| my-delegations/index.ts | :16 | :92-100 | :103-106 | inline { error, code: "UNAUTHORIZED" } |
_shared/auth.ts: line 2 is the only edit; validateJWT already extracts and passes the token.
The 10 importers of _shared/auth.ts (each needs ITS OWN deploy to re-bundle the helper — editing
the helper redeploys nothing by itself): auth-biometric-setup, auth-refresh-token,
embeddings-generate, intelligence-batch-update, intelligence-get, intelligence-refresh,
intelligence-refresh-v2, notifications-register-device, push-notification, sync-push.
Of these, the four intelligence-* functions are ALSO in the 2.3x sweep population — they are
deployed in plan 92-09 AFTER their own sweep edits, so THIS plan deploys only the six that are
outside the population: auth-biometric-setup, auth-refresh-token, embeddings-generate,
notifications-register-device, push-notification, sync-push.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Class-1 edits on the 4 functions + Class-3 bump on _shared/auth.ts</name>
  <files>supabase/functions/audit-logs-viewer/index.ts, supabase/functions/data-retention/index.ts, supabase/functions/field-permissions/index.ts, supabase/functions/my-delegations/index.ts, supabase/functions/_shared/auth.ts</files>
  <read_first>The anchor line ranges in the interfaces table above (import line + auth block per file); access-requests/index.ts:7,57-61 (end-state call shape)</read_first>
  <action>
    For each of the four functions, apply the Class-1 edit EXACTLY (per D-05, the house pattern) —
    this is NOT a find-and-replace (RESEARCH Pitfall 1):
    (a) replace every occurrence of the 2.3x specifier in the file (including any type imports from
        the same URL) with `jsr:@supabase/supabase-js@2`; never leave the old specifier behind in a
        comment (it would re-trip the derivation grep);
    (b) add `const token = authHeader.replace('Bearer ', '')` before the auth call;
    (c) change bare `.auth.getUser()` to `.auth.getUser(token)`;
    (d) KEEP `{ global: { headers: { Authorization: authHeader } } }` untouched — the injected
        client serves TWO purposes: the auth check AND RLS scoping of every later query. A
        "cleanup" to a plain client makes auth pass while every query silently runs as anon and
        returns [] — the exact failure-as-emptiness this milestone kills;
    (e) keep each file's own error-response idiom and its `_shared/cors.ts` imports byte-identical.
    In _shared/auth.ts change ONLY line 2's specifier to `jsr:@supabase/supabase-js@2` (D-06 —
    validateJWT already calls getUser(token)).
  </action>
  <verify>
    <automated>grep -rlE '@supabase/supabase-js@2\.3[0-9]' supabase/functions/audit-logs-viewer supabase/functions/data-retention supabase/functions/field-permissions supabase/functions/my-delegations supabase/functions/_shared/auth.ts | wc -l | grep -qx 0 && grep -l 'getUser(token)' supabase/functions/audit-logs-viewer/index.ts supabase/functions/data-retention/index.ts supabase/functions/field-permissions/index.ts supabase/functions/my-delegations/index.ts | wc -l | grep -qx 4 && grep -rl 'Authorization: authHeader' supabase/functions/audit-logs-viewer/index.ts supabase/functions/data-retention/index.ts supabase/functions/field-permissions/index.ts supabase/functions/my-delegations/index.ts | wc -l | grep -qx 4</automated>
  </verify>
  <acceptance_criteria>All five files off the 2.3x pin; all four functions pass the token explicitly AND retain the header-injected client; CORS imports, config.toml, and error idioms untouched.</acceptance_criteria>
  <done>Source state correct for the core; nothing live has changed yet (deploy is Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Deploy core + non-population helper importers; prove the flip (D-09 partial, D-19 acceptance)</name>
  <files>.planning/phases/92-session-integrity-edge-function-auth/92-DEPLOY-LEDGER.md</files>
  <read_first>.planning/phases/92-session-integrity-edge-function-auth/92-PROBE-BASELINE.md (the pre-migration 401s this closes against)</read_first>
  <action>
    Deploy each of these ten functions to staging with
    `supabase functions deploy <name> --project-ref zkrcjzdemdmwhearhfgg` (fallback: MCP
    deploy_edge_function): audit-logs-viewer, data-retention, field-permissions, my-delegations,
    auth-biometric-setup, auth-refresh-token, embeddings-generate, notifications-register-device,
    push-notification, sync-push. Start 92-DEPLOY-LEDGER.md recording one line per function:
    name, deploy timestamp, OK/FAIL — retry failures; the ledger is the resumable record (partial
    deploys are the failure mode, not deploy syntax).
    Then run `scripts/probe-edge-auth.sh audit-logs-viewer data-retention field-permissions
    my-delegations` and append the ACTUAL post-deploy status codes to the ledger next to the
    baseline codes.
    Do NOT run bare `supabase functions deploy` with no name — that deploys all 303 functions,
    re-resolving carets on functions this phase does not own.
  </action>
  <verify>
    <automated>OUT=$(bash scripts/probe-edge-auth.sh audit-logs-viewer data-retention field-permissions my-delegations); [ "$(echo "$OUT" | grep -c ' -> ')" -eq 4 ] && ! echo "$OUT" | grep -q ' -> 401'</automated>
  </verify>
  <acceptance_criteria>All ten deploys recorded OK in the ledger; the three cores close against their evidence — the audit's nine observed 401s (adminops F2) now probe non-401 — and my-delegations closes against engagements F4''s two observed 401s, also non-401. At least one core function returns a non-empty, well-formed body for the seeded test user (RLS still scoped — Pitfall 1 warning sign is 200-with-[] where data existed). SUMMARY names the D-20 handoff: data-retention + field-permissions are now known-closed preconditions for Phase 93 criterion 2.</acceptance_criteria>
  <done>Four previously-401ing deployed artifacts accept a valid JWT; ledger started; Phase 93 dependency named in the SUMMARY.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                    |
| ------------------------- | ---------------------------------------------- |
| client → edge function    | caller JWT crosses; getUser gate decides trust |
| edge function → PostgREST | injected JWT scopes RLS                        |
| repo → deployed artifact  | deploy-time dependency resolution (eszip)      |

## STRIDE Threat Register

| Threat ID | Category                                            | Component            | Disposition | Mitigation Plan                                                                                                                                                          |
| --------- | --------------------------------------------------- | -------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-92-02   | Spoofing                                            | getUser gate         | mitigate    | explicit getUser(token) against GoTrue — signature/expiry/aud checked server-side; never hand-rolled JWT parsing                                                         |
| T-92-09   | Information Disclosure (inverted: silent data loss) | RLS scoping          | mitigate    | injected client retained verbatim (grep-gated); probe asserts non-empty data for a seeded user                                                                           |
| T-92-10   | Tampering                                           | deploy drift         | mitigate    | named-function deploys only; ledger records every deploy; probe runs against the deployed artifact, not source                                                           |
| T-92-SC   | Tampering                                           | dependency specifier | mitigate    | jsr:@supabase/supabase-js@2 — official registry, official package, already vendored in 45 functions; no new packages installed (RESEARCH Package Legitimacy Audit: none) |

</threat_model>

<verification>
- Probe: all four functions non-401 on deployed staging, codes recorded beside the baseline.
- `grep -rlE '@supabase/supabase-js@2\.3[0-9]'` over the five files → empty.
- Ledger holds 10 OK lines.
</verification>

<success_criteria>
The demonstrated-broken core of AUTH-02 is closed against its own evidence; AUTH-04's auth half is
live; Phase 93's criterion-2 surfaces are named as known-closed in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/92-session-integrity-edge-function-auth/92-04-SUMMARY.md` when done.
</output>
