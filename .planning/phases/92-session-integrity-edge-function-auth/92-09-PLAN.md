---
phase: 92-session-integrity-edge-function-auth
plan: 09
type: execute
wave: 3
depends_on: [92-04, 92-05, 92-06, 92-07, 92-08]
files_modified:
  - .planning/phases/92-session-integrity-edge-function-auth/92-DEPLOY-LEDGER.md
  - .planning/phases/92-session-integrity-edge-function-auth/92-PROBE-FINAL.md
autonomous: true
requirements: [AUTH-02]
truths:
  - 'Decisions covered — D-09: migrated functions are deployed to staging within this phase — an edited-but-undeployed function does not satisfy criterion 2, so every sweep function is deployed here with a per-function ledger'
  - 'Decisions covered — D-08: phase-gate acceptance is re-running the derivation command to 0 repo-wide, never re-quoting a count; D-21: verification is repo grep PLUS live probe of deployed artifacts, never grep alone, because deploy-time caret resolution makes deployed behavior independent of source'
  - 'Decisions covered — D-17: the criterion''s false "133 with bare getUser()" wording was corrected in the governing docs during planning (ROADMAP.md:285, REQUIREMENTS.md AUTH-02, STATE.md, PROJECT.md, audit INDEX.md) — this plan verifies the corrected form is what it closes against and edits no doc'
must_haves:
  truths:
    - 'Every function migrated by 92-05..92-08 is deployed to staging with an OK line in the ledger'
    - 'The derivation command over supabase/functions index.ts files returns 0 repo-wide'
    - 'The post-migration probe records actual non-401 status codes for representatives of all three D-16 sets, including my-delegations, against deployed staging'
    - 'Any @2 bare-getUser function that probes 401 is filed to Phase 93 in the SUMMARY, not pulled into this phase'
  artifacts:
    - path: '.planning/phases/92-session-integrity-edge-function-auth/92-DEPLOY-LEDGER.md'
      provides: 'Resumable per-function deploy record covering the full population plus the 10 helper importers'
    - path: '.planning/phases/92-session-integrity-edge-function-auth/92-PROBE-FINAL.md'
      provides: "Post-deploy status codes closing criterion 2's live half"
  key_links:
    - from: '92-DEPLOY-LEDGER.md'
      to: 'staging zkrcjzdemdmwhearhfgg'
      via: 'supabase functions deploy <name> per ledger line'
      pattern: 'deploy'
---

<objective>
Close AUTH-02's live half (D-09): batch-deploy the 129 sweep functions to staging with a
resumable per-function ledger, then verify both sides — the D-08 derivation command at 0
repo-wide (source) AND the probe returning non-401 on deployed representatives of every set
(live) — because a green grep with an undeployed function is a false pass, and a redeploy
without an edit can silently flip behavior (D-21).

Purpose: criterion 2 is a claim about deployed staging, not about the repo.
Output: complete deploy ledger, final probe record, criterion 2 closeable by verify-phase via
re-derivation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-session-integrity-edge-function-auth/92-PROBE-BASELINE.md
@.planning/phases/92-session-integrity-edge-function-auth/92-DEPLOY-LEDGER.md  (started by 92-04)

<interfaces>
Deploy list = the union of files_modified across 92-05..92-08 (129 functions; the 4 core functions
and 6 non-population helper importers were deployed by 92-04 and are already in the ledger).
The four intelligence-* functions deploy HERE (after their slice edits), re-bundling both their own
index.ts and the bumped _shared/auth.ts.
Deploy command: `supabase functions deploy <name> --project-ref zkrcjzdemdmwhearhfgg`
(CLI 2.106.0 at /opt/homebrew/bin/supabase; fallback MCP deploy_edge_function).
NEVER run bare `supabase functions deploy` — it deploys all 303 functions, re-resolving carets on
functions this phase does not own (that is D-21's silent-flip hazard, weaponized).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Batch deploy the sweep with a resumable ledger (D-09)</name>
  <files>.planning/phases/92-session-integrity-edge-function-auth/92-DEPLOY-LEDGER.md</files>
  <read_first>92-DEPLOY-LEDGER.md (skip functions already OK); the files_modified lists of 92-05..92-08 (the deploy population)</read_first>
  <action>
    Reconcile the deploy list first: run
    `grep -rlE '@supabase/supabase-js@2\.3[0-9]' supabase/functions --include='index.ts' | wc -l`
    — it MUST be 0 before any deploy (all slices merged); if not, stop and report which slice is
    incomplete. Then derive the list of migrated functions from git
    (`git log`/`git diff` of the merged sweep plans, cross-checked against the four plans'
    files_modified) and deploy each with the named-function command, appending
    `name | timestamp | OK/FAIL` to the ledger as you go. On FAIL: retry once, then continue the
    batch and return to failures at the end — partial deploys are the failure mode; the ledger is
    the recovery record. Resumable: skip any function already marked OK.
  </action>
  <verify>
    <automated>test $(grep -c '| OK' .planning/phases/92-session-integrity-edge-function-auth/92-DEPLOY-LEDGER.md) -ge 139 && grep -c '| FAIL' .planning/phases/92-session-integrity-edge-function-auth/92-DEPLOY-LEDGER.md | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>Ledger holds an OK line for all 129 sweep functions plus the 10 from 92-04 (≥139 OK, 0 unresolved FAIL); no unnamed bulk deploy was run.</acceptance_criteria>
  <done>Every migrated source file has a corresponding deployed artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Two-sided phase verification + carry-forward notes (D-08, D-21, D-17)</name>
  <files>.planning/phases/92-session-integrity-edge-function-auth/92-PROBE-FINAL.md</files>
  <read_first>92-PROBE-BASELINE.md (the codes to compare against); .planning/audits/live-audit-2026-08-15/adminops.md F2 (audited route → function mapping)</read_first>
  <action>
    (a) Source side: run the D-08 derivation command repo-wide; record the output (must be 0).
    (b) Live side: run `scripts/probe-edge-auth.sh` against deployed staging for: the three cores +
        my-delegations (re-confirm), a Class-2 representative (dossiers-update), a slice
        representative from each of the four slices, and tasks-get (B\A set). Write
        92-PROBE-FINAL.md with the ACTUAL status codes beside the baseline codes.
    (c) Apply the D-16 branches to the B\A reading: if tasks-get (or any @2 bare-getUser function)
        probes 401, FILE it to Phase 93 in the SUMMARY — Phase 93 already declares Depends on:
        Phase 92; do not widen this phase.
    (d) Audited-route check (criterion 2's "no audited route renders empty because of a 401"):
        cross-check adminops F2's route→function mapping against the ledger — every function behind
        an audited 401 finding must be in the ledger with OK; probe any that are not among (b)'s
        representatives.
    (e) Carry-forward notes in the SUMMARY (recorded, not acted on):
        - the two bare-getUser functions that forward NO Authorization header (outside every PARK-1
          scope, the only two that provably cannot authenticate) — identify by
          `grep -L 'Authorization' $(grep -rlE 'auth\.getUser\(\)' supabase/functions --include='index.ts')`
          and name them for Phase 93;
        - `supabase/functions/_shared/ai-interaction-logger.ts:12` still pins esm.sh 2.39.0 —
          outside D-06 (which names only _shared/auth.ts) and outside the D-08 index.ts population;
          named here so it is a recorded decision, not a silent omission;
        - confirm (read-only) that ROADMAP.md:285 / REQUIREMENTS.md AUTH-02 carry the D-17
          corrected wording this verification closes against.
  </action>
  <verify>
    <automated>grep -rlE '@supabase/supabase-js@2\.3[0-9]' supabase/functions --include='index.ts' | wc -l | grep -qx 0 && bash scripts/probe-edge-auth.sh audit-logs-viewer data-retention field-permissions my-delegations dossiers-update tasks-get | grep -c ' -> 401' | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>Derivation command → 0 repo-wide; probe non-401 for every representative including my-delegations, with actual codes recorded; any B\A 401 filed to Phase 93 (branch 2 of D-16), never absorbed here; the two no-header outliers and the ai-interaction-logger pin recorded in the SUMMARY; no governing doc edited by this plan.</acceptance_criteria>
  <done>Criterion 2 is closeable by verify-phase re-derivation: source clean, artifacts live, evidence on file.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                            |
| ------------------------ | ------------------------------------------------------ |
| repo → deployed artifact | eszip bundling at deploy time resolves floating carets |
| probe → staging          | real creds mint the probe JWT                          |

## STRIDE Threat Register

| Threat ID | Category                         | Component        | Disposition | Mitigation Plan                                                                                                 |
| --------- | -------------------------------- | ---------------- | ----------- | --------------------------------------------------------------------------------------------------------------- |
| T-92-10   | Tampering                        | deploy batch     | mitigate    | named-function deploys only from the ledger; bulk unnamed deploy forbidden                                      |
| T-92-11   | Denial of Service (availability) | partial deploy   | mitigate    | resumable ledger with per-function OK/FAIL; failures retried and never silently dropped                         |
| T-92-02   | Spoofing                         | verification     | mitigate    | two-sided: D-08 grep (source) + probe of deployed artifacts (live) — grep alone is a documented false-pass mode |
| T-92-05   | Information Disclosure           | probe run        | mitigate    | probe script never echoes creds/JWT                                                                             |
| T-92-SC   | Tampering                        | package installs | accept      | no installs; deploys re-bundle the official @2 specifier                                                        |

</threat_model>

<verification>
- Ledger: ≥139 OK, 0 FAIL outstanding.
- `grep -rlE '@supabase/supabase-js@2\.3[0-9]' supabase/functions --include='index.ts' | wc -l` → 0.
- 92-PROBE-FINAL.md holds actual codes for ≥10 representatives, all non-401.
</verification>

<success_criteria>
Criterion 2 holds on deployed staging and is re-derivable; nothing outside the operator-granted
population was deployed or edited; carry-forwards for Phase 93 are named.
</success_criteria>

<output>
Create `.planning/phases/92-session-integrity-edge-function-auth/92-09-SUMMARY.md` when done,
including the D-16 branch verdict and the carry-forward list.
</output>
