---
phase: 92-session-integrity-edge-function-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/probe-edge-auth.sh
  - tests/e2e/92-signout.spec.ts
  - tests/e2e/92-delegations-error.spec.ts
  - tests/e2e/support/pages/LoginPage.ts
  - .planning/phases/92-session-integrity-edge-function-auth/92-PROBE-BASELINE.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05]
truths:
  - 'Decisions covered — D-16: the probe states its three-branch verdict rule in acceptance criteria BEFORE collecting any data, and records actual status codes as evidence'
  - 'Decisions covered — D-21: the probe runs against deployed staging artifacts, never repo source, because a redeploy with no edit can silently flip a function between working and broken'
  - 'Decisions covered — D-04: the sign-out spec asserts the session is gone from storage (supabase auth key null), not just that the route changed'
  - "Decisions covered — D-26: tests/e2e/01-login.spec.ts:30 is a LIVE FAILING TEST, not existing coverage — this plan MEASURES its red (records actual output, never asserts redness; a pass means AUTH-01's premise is wrong and is surfaced as a finding), and the Wave-0 spec narrows to what that test does not cover: the AUTH-03 bounce, the /settings surface, and the D-04 cleared-session assertion"
must_haves:
  truths:
    - 'A baseline probe record exists with actual HTTP status codes from one deployed representative of each of the three sets (A∩B, B\A, A\B), taken before any migration edit'
    - 'The actual output of the existing 01-login sign-out test is recorded pre-fix as the AUTH-01 RED baseline'
    - 'A sign-out e2e spec exists covering the /settings surface (testid locators only), forced storage-invalidation bounce, and the cleared-session assertion — no page reload, no duplication of the 01-login URL assertion'
    - 'A delegations forced-error e2e spec exists that blocks my-delegations via CDP and asserts role="alert" in the DOM, never inferring failure from the network'
  artifacts:
    - path: 'scripts/probe-edge-auth.sh'
      provides: 'Reusable staging probe: mints a JWT from .env.test creds, curls named functions, prints fn -> status'
    - path: 'tests/e2e/92-signout.spec.ts'
      provides: 'AUTH-03/05 + D-04 e2e: cleared-session, /settings surface via testids, storage-invalidation bounce'
    - path: 'tests/e2e/92-delegations-error.spec.ts'
      provides: 'AUTH-04 forced-error e2e via CDP Network.setBlockedURLs'
    - path: '.planning/phases/92-session-integrity-edge-function-auth/92-PROBE-BASELINE.md'
      provides: 'Pre-migration status codes (D-16 evidence) + the 01-login sign-out RED output (D-26 evidence)'
  key_links:
    - from: 'scripts/probe-edge-auth.sh'
      to: 'https://zkrcjzdemdmwhearhfgg.supabase.co/functions/v1/*'
      via: 'curl with minted user JWT'
      pattern: 'functions/v1'
    - from: 'tests/e2e/92-delegations-error.spec.ts'
      to: 'CDP Network.setBlockedURLs'
      via: 'context.newCDPSession'
      pattern: 'setBlockedURLs'
---

<objective>
Wave 0 for Phase 92: create the missing test artifacts named by 92-VALIDATION.md — the staging auth
probe (with its D-16 verdict rule stated before any data is collected) and two e2e specs — and
capture TWO pre-fix baselines: the edge-function probe against deployed staging (D-21), and the
measured RED of the already-existing, live-failing 01-login sign-out test (D-26).

Purpose: every later plan verifies against these. The probe baseline must exist BEFORE any AUTH-02
edit; the RED measurement must exist BEFORE the NavUser mount, or the before/after evidence is
unrecoverable.
Output: probe script + two e2e specs + 92-PROBE-BASELINE.md holding both baselines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-session-integrity-edge-function-auth/92-VALIDATION.md
@.planning/phases/92-session-integrity-edge-function-auth/92-RESEARCH.md  (Pattern 3 = probe source; Pattern 4 = invalidation repro; Pitfall 2)
@tests/e2e/01-login.spec.ts  (fixture/page-object conventions; the LIVE FAILING sign-out test at :30-41)
@tests/e2e/support/pages/LoginPage.ts

<interfaces>
Env vars verified present in `.env.test` (git-ignored): `SUPABASE_URL`, `SUPABASE_ANON_KEY`,
`TEST_USER_EMAIL`, `TEST_USER_PASSWORD`. Staging ref: `zkrcjzdemdmwhearhfgg`.
Supabase auth localStorage key (Assumption A1, verify in-test): `sb-zkrcjzdemdmwhearhfgg-auth-token`.
Playwright: root `playwright.config.ts`, projects incl. `chromium-en`, fixtures at
`tests/e2e/support/fixtures` (adminPage), baseURL `E2E_BASE_URL` or `http://localhost:5173`.
TESTID CONTRACT with plan 92-02 (D-28.3 — both /settings controls get testids; the shared
name-regex is FORBIDDEN on /settings because it will match 2+ elements and could click the
destructive global sign-out): new plain control = `data-testid="settings-signout"`; existing
Data & Privacy control = `data-testid="signout-all-sessions"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe script + pre-migration baseline run (D-16, D-21)</name>
  <files>scripts/probe-edge-auth.sh, .planning/phases/92-session-integrity-edge-function-auth/92-PROBE-BASELINE.md</files>
  <read_first>92-RESEARCH.md "Pattern 3: The PARK-1 runtime probe" (the complete source); .env.test var names above (do not print values)</read_first>
  <action>
    FIRST, create the phase base tag (chosen over a merge-base expression because it is the house
    convention per CLAUDE.md § Tag signing and gives every later plan one stable, named anchor):
    idempotently — the tag may ALREADY exist because planning created it at the true pre-execution
    HEAD (RULING-P92-24). The step's contract is "a verified signed phase-92-base exists at the
    phase base", not "this task created a tag" — a bare `git tag -s` is not idempotent and fails on
    a clean re-run; the verify is the acceptance. Do not simplify this back:
    `git rev-parse -q --verify refs/tags/phase-92-base >/dev/null || git tag -s phase-92-base -m "Phase 92 base: session integrity & edge-function auth"`
    then report the signature: `git tag -v phase-92-base || echo "WARN: signature unverifiable on this machine — signing config is user-local (CLAUDE.md § Tag signing) and its absence is an environment fact, not a work defect"` (on the planner's machine it prints
    "Good \"git\" signature" — signing config is machine-local per CLAUDE.md). Later plans' scope
    guards diff against `phase-92-base` instead of HEAD, because a HEAD-relative diff goes empty
    the moment a forbidden edit is committed — the exact moment the guard exists to fire.

    Write scripts/probe-edge-auth.sh per RESEARCH Pattern 3: source .env.test (set -a), mint a JWT
    via POST $SUPABASE_URL/auth/v1/token?grant_type=password with TEST_USER_EMAIL/TEST_USER_PASSWORD
    and apikey $SUPABASE_ANON_KEY, then for each function name passed as an argument curl
    $SUPABASE_URL/functions/v1/$fn with "Authorization: Bearer $JWT" + apikey header and print
    "fn -> HTTP status" (curl -s -o /dev/null -w '%{http_code}'). Default argument list when none
    given: audit-logs-viewer data-retention field-permissions my-delegations dossiers-update tasks-get.
    NEVER echo the JWT, the password, or any env value (house rule). chmod +x.

    Then RUN it against deployed staging and write 92-PROBE-BASELINE.md recording: date, the exact
    command, and the actual status code per function, grouped by set — A∩B pinned+bare
    (audit-logs-viewer, data-retention, field-permissions, my-delegations), A\B pinned+token-passing
    (dossiers-update), B\A @2+bare (tasks-get). Include the D-16 verdict-rule text (from
    acceptance_criteria below) verbatim in the file ABOVE the recorded data.

  </action>
  <verify>
    <automated>git rev-parse -q --verify refs/tags/phase-92-base >/dev/null && test -x scripts/probe-edge-auth.sh && [ "$(grep -c ' -> ' .planning/phases/92-session-integrity-edge-function-auth/92-PROBE-BASELINE.md)" -ge 6 ]</automated>
  </verify>
  <acceptance_criteria>
    VERDICT RULE — stated here, before data collection (D-16). Probe one deployed representative per
    set with a valid JWT; a 401 means auth rejected, any other status (200/400/405/500) means the
    request passed the getUser gate. Branches:
    (1) all three sets non-401 → the 110 @2+bare functions are healthy; AUTH-02 closes at 133;
        nothing carries forward.
    (2) B\A (tasks-get) returns 401 → bare getUser() fails independently of the pin; the 110 are
        real work and are FILED TO PHASE 93 (which declares Depends on: Phase 92) — not pulled in here.
    (3) only A∩B returns 401 → the pin is the sole failure mode; the premise is confirmed by
        measurement rather than assumption.
    92-PROBE-BASELINE.md records ACTUAL status codes (not a summary of them), the probe ran against
    deployed staging (D-21), and no credential value appears in the script output or the baseline file.
  </acceptance_criteria>
  <done>Executable probe script committed; baseline file holds real per-function status codes for all six representatives with the verdict rule above them.</done>
</task>

<task type="auto">
  <name>Task 2: Measure the AUTH-01 RED (D-26) + write 92-signout.spec.ts (AUTH-03/05, D-04)</name>
  <files>tests/e2e/92-signout.spec.ts, .planning/phases/92-session-integrity-edge-function-auth/92-PROBE-BASELINE.md</files>
  <read_first>tests/e2e/01-login.spec.ts (:9-14 credential-guarded first describe, :23+ second describe, :30-41 the live sign-out test), tests/e2e/support/pages/LoginPage.ts (:18-19 role-regex locator), 92-RESEARCH.md Pattern 4 + "Sign-out acceptance check"</read_first>
  <action>
    FIRST, measure the RED (D-26): run
    `pnpm exec playwright test tests/e2e/01-login.spec.ts --project=chromium-en --grep "signs out"`
    against the current app and append its ACTUAL output (pass/fail + failure text) to
    92-PROBE-BASELINE.md under an "AUTH-01 RED baseline" heading. Do NOT write an assertion that it
    is red — measure it. If it PASSES, AUTH-01's premise (no sign-out control exists on /) is wrong:
    record that as a finding in the SUMMARY and flag it to the orchestrator instead of working
    around it. This test is worth more than a new spec — it fails independently today (not
    quarantined; the :11 skip guards only the first describe) and plan 92-02's fix must flip it.

    THEN repair the 01-login oracle's LOCATOR (checker B-1, measured): LoginPage.ts:19 uses
    `getByRole('button', { name: RE })`, but the mounted control is a Radix `DropdownMenuItem` —
    role="menuitem", which is CORRECT a11y and must not be changed on the component side (Radix's
    Slot merge keeps slotProps.role even under asChild, so there is no component-side fix that
    is not an a11y regression). Widen the signOut() locator to accept both roles:
    `page.getByRole('button', { name: RE }).or(page.getByRole('menuitem', { name: RE }))`.
    The independent-oracle property is NOT weakened by this: the test fails today because no
    sign-out control exists anywhere, and it passes only once one is mounted — widening
    button → button|menuitem changes neither half of that. Run the RED measurement AFTER this
    edit so the recorded RED is the oracle that will later flip.

    THEN create tests/e2e/92-signout.spec.ts covering ONLY what 01-login does not (D-26.3 — do not
    duplicate its sidebar-signout → /login URL assertion). Three tests, copying 01-login
    conventions (fixtures, adminPage, env guards):

    1. "sign-out genuinely clears the session" (D-04): goto '/', assert
       localStorage['sb-zkrcjzdemdmwhearhfgg-auth-token'] is non-null (validates Assumption A1
       in-test), sign out via getByTestId('user-menu') → its sign-out item, then assert the storage
       key is NULL. The /login URL is incidental sequencing here — the assertion of record is the
       cleared session, which 01-login does not check.
    2. "/settings is reachable from navigation and exposes an independent sign-out" (AUTH-05/D-02):
       start on '/', open getByTestId('user-menu') and click its Settings item (scoped WITHIN the
       open menu) — this automates AUTH-05's "reachable from navigation" clause instead of leaving
       it to prose, and composes with the B-1 role fix (real buttons in the menu). Then click
       getByTestId('settings-signout'), assert /login + storage key null. TESTIDS ONLY — the shared
       name regex /sign out|logout|تسجيل الخروج|خروج/i is FORBIDDEN on this page: it matches the
       Data & Privacy "sign out all sessions" control too (strict-mode failure, and worse, the regex
       could click the destructive global sign-out).
    3. "invalidated session bounces the open tab" (AUTH-03): goto '/dashboard', page.evaluate the
       RESEARCH Pattern-4 storage manipulation — set expires_at to (now - 3600) AND set
       refresh_token to an invalid string (BOTH: corrupting only the refresh token does nothing,
       Pitfall 2) — then page.waitForURL('**/login', { timeout: 45_000 }). NO page.reload()
       anywhere: a reload exercises the beforeLoad guard, which already works and is a non-goal
       (D-18) — this test must prove the reactive open-tab path.

    These tests are EXPECTED RED until 92-02 lands (no user-menu testid, no settings control, no
    navigation seam).

  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/92-signout.spec.ts --project=chromium-en --list && grep -q "getByRole('menuitem'" tests/e2e/support/pages/LoginPage.ts && grep -q 'refresh_token' tests/e2e/92-signout.spec.ts && grep -q 'expires_at' tests/e2e/92-signout.spec.ts && grep -q 'settings-signout' tests/e2e/92-signout.spec.ts && ! grep -q 'page.reload' tests/e2e/92-signout.spec.ts && grep -q 'AUTH-01 RED baseline' .planning/phases/92-session-integrity-edge-function-auth/92-PROBE-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>LoginPage.signOut() accepts button|menuitem roles (component roles stay untouched — a11y over test convenience) and the plan states the oracle is not weakened; the 01-login sign-out test's actual pre-fix output is recorded AFTER the locator edit (measured, not asserted; a pass = surfaced finding); the new spec lists 3 tests; no test duplicates the 01-login URL assertion; the /settings test REACHES the page through the user-menu Settings item (never goto) and uses testids only, never the shared name regex; test 3 mutates BOTH expires_at and refresh_token and never reloads.</acceptance_criteria>
  <done>RED baseline on record; spec compiles and lists cleanly; red-until-92-02 status noted in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: 92-delegations-error.spec.ts — AUTH-04 forced-error scaffold</name>
  <files>tests/e2e/92-delegations-error.spec.ts</files>
  <read_first>tests/e2e/01-login.spec.ts (conventions), 92-VALIDATION.md Wave 0 bullet 2 (empty-200 gotcha)</read_first>
  <action>
    Create the CDP forced-error spec (no in-repo analog exists — this is the project's documented
    forced-error protocol): const cdp = await page.context().newCDPSession(page); await
    cdp.send('Network.enable'); await cdp.send('Network.setBlockedURLs', { urls: ['*my-delegations*'] });
    then goto '/delegations' and assert:
    - an element with role="alert" is visible containing the error heading ("Couldn't load
      delegations" / via i18n) — assert on the DOM, NEVER on network responses, because an auth/RLS
      denial can present as an EMPTY 200 and the empty state must not be mistaken for the error state;
    - the empty-state copy ("You haven't granted any delegations") is NOT rendered;
    - the stat cards show the em-dash '—', not '0';
    - a "Try again" button exists inside the alert region.
    Add a SECOND test — the happy half of criterion 4 ("renders real delegations when they are
    not rejected"), which otherwise has no oracle anywhere: navigate to '/delegations' with NO
    CDP blocking and assert role="alert" is NOT present and the page renders either delegation
    rows or the legitimate empty state (data-dependent on staging; both are honest non-error
    renders).
    TIMING (checker B-2): every failure assertion uses an explicit `{ timeout: 15_000 }`, with a
    comment stating why — a CDP-blocked request yields FunctionsFetchError / a plain Error with NO
    numeric `status`, so query-client.ts's 4xx short-circuit never fires and TanStack retries 4
    attempts at 1s+2s+4s backoff: `isError` arrives at ~7s, past Playwright's default 5s expect
    timeout. The long timeout is retry backoff, not flakiness — a correct implementation fails the
    default timeout.
    EXPECTED RED until plan 92-03 lands.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/92-delegations-error.spec.ts --project=chromium-en --list && grep -qE '15_?000' tests/e2e/92-delegations-error.spec.ts && grep -q 'setBlockedURLs' tests/e2e/92-delegations-error.spec.ts</automated>
  </verify>
  <acceptance_criteria>Spec blocks via CDP Network.setBlockedURLs; every failure assertion targets the DOM (role="alert", em-dash stats, absent empty-copy); zero assertions infer failure from response status; failure assertions carry { timeout: 15_000 } with the retry-backoff rationale in a comment.</acceptance_criteria>
  <done>Spec compiles and lists cleanly; red-until-92-03 status noted in the SUMMARY.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                |
| ------------------------------------ | ---------------------------------------------------------- |
| probe script → staging auth endpoint | real credentials cross from .env.test into a login request |
| e2e specs → staging-backed app       | test creds drive a real session                            |

## STRIDE Threat Register

| Threat ID | Category               | Component                  | Disposition | Mitigation Plan                                                                                      |
| --------- | ---------------------- | -------------------------- | ----------- | ---------------------------------------------------------------------------------------------------- |
| T-92-05   | Information Disclosure | scripts/probe-edge-auth.sh | mitigate    | never echo JWT/creds; print status codes only; .env.test stays git-ignored                           |
| T-92-02   | Spoofing               | probe verdict              | mitigate    | verdict rule fixed before data collection (D-16) so the reading cannot drift toward the chosen scope |
| T-92-13   | Tampering              | /settings e2e locator      | mitigate    | testid-only locators — the shared name regex could click the destructive global sign-out (D-28.3)    |
| T-92-SC   | Tampering              | package installs           | accept      | no npm/pip/cargo installs in this plan (RESEARCH Package Legitimacy Audit: none)                     |

</threat_model>

<verification>
- Probe script executable, baseline file holds ≥6 real status codes with the verdict rule above them, plus the AUTH-01 RED output.
- Both specs `--list` cleanly under chromium-en.
- No credential value in any committed file (grep the two specs and the baseline for '@' emails/passwords → only env-var references).
</verification>

<success_criteria>
Wave 0 complete: three artifacts exist, both baselines (probe codes + AUTH-01 RED) are captured
pre-fix, and the D-16 verdict branches are on record before any AUTH-02 edit mutates the population.
</success_criteria>

<output>
Create `.planning/phases/92-session-integrity-edge-function-auth/92-01-SUMMARY.md` when done,
noting which baseline branch (1/2/3) the recorded codes indicate and the AUTH-01 RED result.
</output>
