---
phase: 73-agent-platform-writes-generative-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260620000001_phase73_signal_actor_columns.sql
  - supabase/migrations/20260620000002_phase73_persist_brief_invoker_rpc.sql
autonomous: true
requirements: [GENUI-03]
must_haves:
  truths:
    - 'After a status change on intelligence_event, the row records WHICH user made the change (acknowledged_by / dismissed_by / escalated_by) plus status_changed_at, so GENUI-03 #2 (actor_id == auth.uid()) is verifiable for signal writes (D-06).'
    - "A new SECURITY INVOKER persist_brief RPC inserts a brief into public.briefs under the caller's JWT, pinning created_by = auth.uid(), with NO service-role path and NO AnythingLLM dependency (D-02)."
    - 'persist_brief commit is RLS-enforced: it succeeds only when the caller can_edit_dossier(dossier_id) (the existing briefs INSERT policy), and is clearance-gated the same as the conventional brief path.'
  artifacts:
    - path: 'supabase/migrations/20260620000001_phase73_signal_actor_columns.sql'
      provides: 'Idempotent ADD COLUMN of acknowledged_by/dismissed_by/escalated_by (uuid → auth.users) + status_changed_at (timestamptz) on intelligence_event'
      contains: 'ADD COLUMN IF NOT EXISTS'
    - path: 'supabase/migrations/20260620000002_phase73_persist_brief_invoker_rpc.sql'
      provides: 'CREATE OR REPLACE FUNCTION public.persist_brief(...) RETURNS uuid LANGUAGE plpgsql SECURITY INVOKER, GRANT EXECUTE TO authenticated'
      contains: 'SECURITY INVOKER'
  key_links:
    - from: 'persist_brief RPC body'
      to: 'public.briefs'
      via: 'INSERT ... created_by = auth.uid()'
      pattern: "INSERT INTO (public\\.)?briefs"
    - from: 'persist_brief RPC'
      to: 'auth.uid()'
      via: 'created_by pinned to verified caller identity (never a parameter)'
      pattern: "created_by[^;]*auth\\.uid\\(\\)"
---

<objective>
Lay the two DB foundations P73 writes require so the frontend approve-handlers (73-03) can commit under the caller's JWT and the actor can be asserted (GENUI-03):

1. **Signal actor migration (D-06):** `intelligence_event` records `status` but NOT who changed it. Add `acknowledged_by`, `dismissed_by`, `escalated_by` (uuid → `auth.users`) and `status_changed_at` so an approved dismiss/escalate write writes a verifiable `actor_id == auth.uid()`.
2. **`persist_brief` INVOKER RPC (D-02):** The two existing brief-write paths are both disqualified — the AI path (`dossiers-briefs-generate`) calls AnythingLLM (P74 retires it) and the manual path (`engagement-briefs`) switches to the service-role client (violates GENUI-03). Add a NEW `SECURITY INVOKER` `persist_brief` RPC that inserts into `public.briefs` under the caller JWT, pinning `created_by = auth.uid()`, relying on the existing `create_briefs_via_dossier_edit` INSERT RLS for authorization.

Purpose: Both are pure DB prerequisites with no agent/frontend coupling — they unblock every later wave.
Output: Two idempotent migrations applied to staging via the Supabase MCP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/73-agent-platform-writes-generative-ui/73-CONTEXT.md
@.planning/phases/73-agent-platform-writes-generative-ui/73-RESEARCH.md

<read_first>

<!-- The authoritative DB facts these migrations build on. Verified live 2026-06-20. -->

intelligence_event TODAY (source of truth = the P69 extend migration):
@supabase/migrations/20260614_phase69_signals_extend.sql

- status TEXT NOT NULL DEFAULT 'new' CHECK (status IN ('new','acknowledged','dismissed','escalated'))
- created_by UUID REFERENCES auth.users(id) ON DELETE SET NULL (added in P54 base)
- sensitivity_level INTEGER 1..4 ; organization_id UUID NOT NULL
- UPDATE RLS `intelligence_event_update_cleared`: USING + WITH CHECK both gate
  `sensitivity_level <= (SELECT clearance_level FROM profiles WHERE user_id = auth.uid())`
- There are NO acknowledged_by/dismissed_by/escalated_by/status_changed_at columns yet — THIS plan adds them.

briefs table TODAY (the table dossiers-briefs-generate inserts into):
@supabase/migrations/20250930005_create_briefs_table.sql

- dossier_id UUID NOT NULL REFERENCES dossiers(id)
- content_en JSONB NOT NULL CHECK (content_en ? 'summary' AND content_en ? 'sections')
- content_ar JSONB NOT NULL CHECK (content_ar ? 'summary' AND content_ar ? 'sections')
- created_by UUID (briefs also carries created_by/organization_id from 007_create_briefs.sql)
- RLS: SELECT "view_briefs_via_dossier" (sensitivity CASE gate);
  INSERT "create_briefs_via_dossier_edit" WITH CHECK (can_edit_dossier(dossier_id))
  @supabase/migrations/20260110100001_engagement_brief_linking.sql
- ADD COLUMN engagement_dossier_id UUID REFERENCES engagement_dossiers(id) (nullable)

The authoritative INVOKER digest RPC to MIRROR for style/security (clearance lookup via
profiles.user_id = auth.uid(), tenant via tenant_isolation.get_current_tenant_id(),
SECURITY INVOKER, GRANT EXECUTE authenticated):
@supabase/migrations/20260615_phase70_publish_digest_tenant_fix.sql

The service-role brief path that MUST NOT be reused (engagement-briefs handleCreateManualBrief →
getServiceClient() → SUPABASE_SERVICE_ROLE_KEY): see supabase/functions/engagement-briefs/index.ts L241-285.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Signal actor-column migration (D-06)</name>
  <files>supabase/migrations/20260620000001_phase73_signal_actor_columns.sql</files>
  <read_first>
    @supabase/migrations/20260614_phase69_signals_extend.sql (the intelligence_event status + RLS this extends)
  </read_first>
  <action>
    Write an idempotent migration that, per D-06, makes signal status changes attributable so GENUI-03 #2 is verifiable:
    - `ALTER TABLE public.intelligence_event ADD COLUMN IF NOT EXISTS acknowledged_by UUID REFERENCES auth.users(id) ON DELETE SET NULL`, and the same for `dismissed_by` and `escalated_by`.
    - `ADD COLUMN IF NOT EXISTS status_changed_at TIMESTAMPTZ` (nullable; set by the writer at status-change time — do NOT default to now() so existing rows stay NULL rather than claiming a false transition time).
    - Add a partial index `idx_intelligence_event_status_changed_at ON public.intelligence_event (organization_id, status_changed_at DESC) WHERE status_changed_at IS NOT NULL` (mirrors the existing idx naming in the P69 file).
    - Do NOT change the status CHECK, the existing RLS policies, or any DEFINER object. The frontend approve-handler (73-03) writes the matching `*_by` column + `status_changed_at` in the SAME caller-JWT UPDATE that flips `status`; the existing `intelligence_event_update_cleared` UPDATE RLS already authorizes that UPDATE (clearance both clauses), so NO policy change is needed.
    - Apply the migration to staging zkrcjzdemdmwhearhfgg via the Supabase MCP (per the global rule: apply migrations yourself via the Supabase MCP). Use the Supabase MCP `list_tables`/`apply_migration` flow; if the MCP is unavailable in-session, record the exact apply command in the SUMMARY and STOP for the operator.
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS \(acknowledged_by\|dismissed_by\|escalated_by\|status_changed_at\)" supabase/migrations/20260620000001_phase73_signal_actor_columns.sql | grep -qx 4 && echo OK</automated>
    Post-apply (MCP): a SELECT on information_schema.columns for table intelligence_event returns all four new columns (acknowledged_by, dismissed_by, escalated_by, status_changed_at).
  </verify>
  <acceptance_criteria>
    - SOURCE: the migration contains exactly the four `ADD COLUMN IF NOT EXISTS` statements above; the three `*_by` columns reference `auth.users(id)`.
    - SOURCE: the migration touches NO existing policy and NO DEFINER function (grep for `DROP POLICY`/`CREATE POLICY`/`SECURITY DEFINER` returns nothing in this file).
    - BEHAVIOR (post-apply): information_schema reports the four columns on public.intelligence_event; re-running the migration is a no-op (idempotent).
  </acceptance_criteria>
  <done>The four actor/timestamp columns exist on intelligence_event on staging; the migration is idempotent and touches no RLS/DEFINER object.</done>
</task>

<task type="auto">
  <name>Task 2: persist_brief SECURITY INVOKER RPC (D-02)</name>
  <files>supabase/migrations/20260620000002_phase73_persist_brief_invoker_rpc.sql</files>
  <read_first>
    @supabase/migrations/20260615_phase70_publish_digest_tenant_fix.sql (INVOKER RPC style to mirror)
    @supabase/migrations/20250930005_create_briefs_table.sql (briefs cols + INSERT RLS this RPC relies on)
  </read_first>
  <action>
    Write a migration creating `CREATE OR REPLACE FUNCTION public.persist_brief(p_dossier_id UUID, p_content_en JSONB, p_content_ar JSONB, p_engagement_dossier_id UUID DEFAULT NULL) RETURNS UUID LANGUAGE plpgsql SECURITY INVOKER` that persists a brief under the CALLER identity (this is D-02's "new INVOKER persist path" — the agent generates the draft content in 73-02; this RPC only persists, never generates, and NEVER touches AnythingLLM or the service role):
    - INSERT INTO public.briefs (dossier_id, content_en, content_ar, engagement_dossier_id, created_by) VALUES (p_dossier_id, p_content_en, p_content_ar, p_engagement_dossier_id, auth.uid()) RETURNING id.
    - `created_by` MUST be pinned to `auth.uid()` inside the function body — it is NOT a parameter (prevents actor spoofing; satisfies GENUI-03 #2 for briefs).
    - If briefs also requires organization_id NOT NULL on the live table, set it from the caller's tenant exactly as publish_digest does — `tenant_isolation.get_current_tenant_id()` — and RAISE EXCEPTION '...' USING ERRCODE='42501' when NULL (mirror publish_digest L51-54). Confirm the live NOT NULL set via the Supabase MCP `list_tables` before writing the INSERT column list; include only columns that exist on the live briefs table.
    - Do NOT add a clearance/edit check in the function body — the existing `create_briefs_via_dossier_edit` INSERT policy (`WITH CHECK (can_edit_dossier(dossier_id))`) is the authorization floor and runs because the function is SECURITY INVOKER. (Indistinguishable-empty is a tool-layer concern in 73-02/73-03; the RPC itself surfaces the RLS denial as the normal INSERT error.)
    - `GRANT EXECUTE ON FUNCTION public.persist_brief(UUID, JSONB, JSONB, UUID) TO authenticated;` (mirror the publish_digest grant). Do NOT grant to anon.
    - Apply to staging via the Supabase MCP; record the apply command in the SUMMARY if MCP is unavailable.
  </action>
  <verify>
    <automated>grep -Eq "SECURITY INVOKER" supabase/migrations/20260620000002_phase73_persist_brief_invoker_rpc.sql && grep -Eq "created_by[^;]*auth\.uid\(\)" supabase/migrations/20260620000002_phase73_persist_brief_invoker_rpc.sql && grep -Eq "GRANT EXECUTE ON FUNCTION public\.persist_brief.*TO authenticated" supabase/migrations/20260620000002_phase73_persist_brief_invoker_rpc.sql && ! grep -qi "service_role\|anythingllm" supabase/migrations/20260620000002_phase73_persist_brief_invoker_rpc.sql && echo OK</automated>
    Post-apply (MCP): pg_proc shows persist_brief with prosecdef = false (INVOKER); a same-session impersonated INSERT through the RPC pins created_by to the caller.
  </verify>
  <acceptance_criteria>
    - SOURCE: function is `SECURITY INVOKER`; `created_by` is set from `auth.uid()` (not from a parameter); EXECUTE granted to `authenticated` only; the file contains neither `service_role` nor `anythingllm` (case-insensitive).
    - SOURCE: INSERT targets `public.briefs` with only columns that exist on the live table (verified via MCP list_tables).
    - BEHAVIOR (post-apply, MCP): `pg_proc.prosecdef = false` for persist_brief; calling it as an authenticated impersonated user inserts one briefs row with `created_by = that user's auth.uid()`; calling it for a dossier the caller cannot edit fails the INSERT RLS (no row written).
  </acceptance_criteria>
  <done>persist_brief exists on staging as a SECURITY INVOKER RPC granted to authenticated, inserts into briefs with created_by pinned to auth.uid(), has zero service-role/AnythingLLM coupling, and is authorized by the existing can_edit_dossier INSERT policy.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                                                                                |
| ------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| caller (browser, caller JWT) → Postgres RLS | Every write must commit under the caller identity; RLS (`can_edit_dossier`, clearance, tenant) is the authorization floor. |
| DB function security context                | A `SECURITY DEFINER` brief insert would bypass RLS — explicitly avoided; persist_brief is INVOKER.                         |

## STRIDE Threat Register

| Threat ID  | Category               | Component              | Disposition | Mitigation Plan                                                                                                                                                                                                                                    |
| ---------- | ---------------------- | ---------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-73-01-01 | Elevation of Privilege | persist_brief RPC      | mitigate    | `SECURITY INVOKER` (not DEFINER) so the existing `create_briefs_via_dossier_edit` INSERT RLS runs under the caller; EXECUTE granted to `authenticated` only, never anon. Source from 73-RESEARCH §D "Privilege escalation / service-role leakage". |
| T-73-01-02 | Spoofing               | brief authorship       | mitigate    | `created_by` pinned to `auth.uid()` in the function body, never accepted as a parameter — the actor cannot be forged by the agent or the request.                                                                                                  |
| T-73-01-03 | Repudiation            | signal status change   | mitigate    | D-06 actor columns (`*_by` + `status_changed_at`) make every dismiss/escalate attributable to `auth.uid()`, closing the 73-RESEARCH §D "Actor non-repudiation gap (signals)".                                                                      |
| T-73-01-04 | Tampering              | brief content shape    | mitigate    | The existing `content_en/content_ar` CHECK (`? 'summary' AND ? 'sections'`) constrains the JSONB; the RPC does not relax it.                                                                                                                       |
| T-73-01-SC | Tampering              | npm/pip/cargo installs | mitigate    | No package installs in this plan (SQL-only). N/A but recorded per phase policy.                                                                                                                                                                    |

</threat_model>

<verification>
- Both migrations apply cleanly and idempotently to staging zkrcjzdemdmwhearhfgg via the Supabase MCP.
- `intelligence_event` gains exactly acknowledged_by/dismissed_by/escalated_by/status_changed_at.
- `persist_brief` exists as INVOKER, granted to authenticated, with created_by pinned to auth.uid() and zero service-role/AnythingLLM coupling.
</verification>

<success_criteria>

- GENUI-03 prerequisite: signal status changes are attributable (`*_by` columns) and the brief write path is a caller-JWT INVOKER RPC — no service-role surface is introduced.
- Re-running either migration is a no-op.
  </success_criteria>

<output>
Create `.planning/phases/73-agent-platform-writes-generative-ui/73-01-SUMMARY.md` when done. Record: the live briefs NOT NULL column set you verified via MCP, the exact persist_brief signature created, and (if MCP apply was unavailable) the apply command for the operator.
</output>
