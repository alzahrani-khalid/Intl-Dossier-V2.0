---
phase: 73-agent-platform-writes-generative-ui
plan: 04
type: execute
wave: 4
depends_on: ['73-03']
files_modified:
  - frontend/src/components/copilot/genui/InlineDossierCard.tsx
  - frontend/src/components/copilot/genui/InlineSignalCard.tsx
  - frontend/src/components/copilot/genui/genUiToolUIs.tsx
  - frontend/src/components/copilot/CopilotSurface.tsx
  - frontend/src/components/copilot/copilot-theme.css
  - frontend/src/i18n/en/copilot.json
  - frontend/src/i18n/ar/copilot.json
autonomous: true
requirements: [GENUI-01]
must_haves:
  truths:
    - "When the copilot answers using a read tool that returns a dossier or a signal, the surface renders the app's OWN token-bound component inline (UniversalDossierCard for dossiers, a signal card for signals) — not plain text — via assistant-ui makeAssistantToolUI renderers (GENUI-01, D-07 direct component renderers + fixed allowlist)."
    - "Each inline card deep-links into the app via the TanStack Router (getDossierDetailPath for dossiers; the signal's queue/detail route for signals), staying inside the SPA (CitationCard precedent)."
    - 'The inline cards are read-only: they render from already-cleared tool output (or a clearance-gated useDossier fetch) and carry NO write/edit actions; clearance stays enforced by RLS in the underlying read.'
    - 'The renderer allowlist is FIXED to UniversalDossierCard + the signal card (D-07) — arbitrary model-named components cannot be rendered.'
  artifacts:
    - path: 'frontend/src/components/copilot/genui/InlineDossierCard.tsx'
      provides: 'Read-only inline wrapper rendering UniversalDossierCard from a dossier id/row + router deep-link'
      contains: 'getDossierDetailPath'
    - path: 'frontend/src/components/copilot/genui/InlineSignalCard.tsx'
      provides: 'Read-only inline signal card (token-bound) + deep-link, no triage callbacks'
      contains: 'writingDirection'
    - path: 'frontend/src/components/copilot/genui/genUiToolUIs.tsx'
      provides: 'makeAssistantToolUI renderers keyed to get_dossier/list_dossiers + read_signals that render the inline cards'
      contains: 'makeAssistantToolUI'
    - path: 'frontend/src/components/copilot/CopilotSurface.tsx'
      provides: 'Mounts the genUI tool-UI renderers under AssistantRuntimeProvider (alongside the HITL renderers from 73-03)'
      contains: 'GenUiToolUIs'
  key_links:
    - from: 'InlineDossierCard'
      to: '/dossiers/{segment}/{id} route'
      via: 'getDossierDetailPath(dossierId, type) + useNavigate'
      pattern: 'getDossierDetailPath'
    - from: 'genUiToolUIs'
      to: 'UniversalDossierCard / InlineSignalCard'
      via: 'makeAssistantToolUI render keyed to read-tool ids (fixed allowlist)'
      pattern: 'makeAssistantToolUI'
---

<objective>
Render generative UI in the copilot surface (GENUI-01): when a read tool returns a dossier or a signal, show the app's own token-bound component inline (UniversalDossierCard / a signal card) with a working router deep-link, instead of plain markdown. Uses the assistant-ui tool-call mechanism (`makeAssistantToolUI`, D-07) with a FIXED allowlist of two renderers.

Purpose: GENUI-01 — the copilot's answers become the app's real components inline, deep-linking into the app, not a text wall. Built on the tool-UI mount established in 73-03.
Output: two inline read-only card wrappers + their tool-UI renderers + mount + bilingual copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@./CLAUDE.md
@.planning/phases/73-agent-platform-writes-generative-ui/73-CONTEXT.md
@.planning/phases/73-agent-platform-writes-generative-ui/73-RESEARCH.md

<read_first>
@frontend/src/components/copilot/CitationCard.tsx (router deep-link precedent — useNavigate({ to }))
@frontend/src/components/copilot/CopilotSurface.tsx (AssistantRuntimeProvider — mount genUI renderers here, alongside 73-03's HITL renderers)
@frontend/src/components/copilot/hitl/proposeToolUIs.tsx (73-03 — mirror its makeAssistantToolUI + mount pattern)
@frontend/src/components/dossier/UniversalDossierCard.tsx (the app component to render inline — note its props below)
@frontend/src/components/signals/SignalRow.tsx (the signal card precedent — render read-only, omit triage callbacks)
@frontend/src/lib/dossier-routes.ts (getDossierDetailPath / getDossierRouteSegment deep-link helpers)
@frontend/src/domains/dossiers/hooks/useDossier.ts (useDossier(id) — clearance-gated fetch for the full DossierWithExtension)
@frontend/src/i18n/en/copilot.json
@frontend/src/i18n/ar/copilot.json
</read_first>

<interfaces>
<!-- Exact component + tool contracts. Canonical import path is lowercase 'dossier/' (git-tracked; CLAUDE.md). -->

UniversalDossierCard (frontend/src/components/dossier/UniversalDossierCard.tsx):
interface UniversalDossierCardProps { dossier: DossierWithExtension; ... }
Import: `import { UniversalDossierCard } from '@/components/dossier/UniversalDossierCard'`
Deep-link: getDossierDetailPath(dossierId, type) → '/dossiers/{segment}/{id}'.

SignalRow (frontend/src/components/signals/SignalRow.tsx):
interface SignalRowProps { signal: Signal; isFocused: boolean; t: TFunction<'intelligence-signals'>; onClick: () => void }
For inline read-only genUI, render a token-bound signal card (you may reuse SignalRow with isFocused=false and onClick=navigate-to-signal, OR a thin InlineSignalCard if SignalRow's queue affordances don't fit inline). No triage (dismiss/escalate) actions inline — those go through the HITL path (73-03).

Read-tool OUTPUT shapes the genUI renderers receive (from 73-02/P72, agent-runtime/src/mastra/tools/dossier-lookups.ts + read-signals.ts):
get_dossier → { dossier: { id, name_en, name_ar, type, status, description_en, description_ar, ... } | null }
list_dossiers → { dossiers: Array<{ id, name_en, name_ar, type, status, ... }> }
read_signals → { signals: Array<Record<string, unknown>> } (rows carry id, title, status, severity, sensitivity_level, category, ...)
NOTE: these are PARTIAL rows, not full DossierWithExtension. To render UniversalDossierCard you either
(a) fetch the full DossierWithExtension via useDossier(row.id) inside InlineDossierCard (clearance-gated, shares the app QueryClient — preferred), or
(b) render a thin token-bound card from the partial row if useDossier is overkill. Pick (a) for fidelity.

assistant-ui tool-UI render props (makeAssistantToolUI, 0.14.23): { args, status, result, toolCallId }.
For READ renderers there is NO approval — render the card from `result` once status is complete; while running, render the existing streaming affordance or nothing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: InlineDossierCard + InlineSignalCard (read-only, token-bound, deep-linked)</name>
  <files>frontend/src/components/copilot/genui/InlineDossierCard.tsx, frontend/src/components/copilot/genui/InlineSignalCard.tsx, frontend/src/components/copilot/copilot-theme.css, frontend/src/i18n/en/copilot.json, frontend/src/i18n/ar/copilot.json</files>
  <read_first>
    @frontend/src/components/dossier/UniversalDossierCard.tsx
    @frontend/src/components/signals/SignalRow.tsx
    @frontend/src/lib/dossier-routes.ts
    @./CLAUDE.md (UI DoD + RTL rules)
  </read_first>
  <action>
    Create two read-only inline wrappers:
    - `InlineDossierCard({ dossierRow })` where `dossierRow` is the partial row from get_dossier/list_dossiers (must include `id` and `type`). Fetch the full `DossierWithExtension` via `useDossier(dossierRow.id)` (clearance-gated, shares the app QueryClient) and render `UniversalDossierCard dossier={...}`. While the fetch is pending, render a neutral token-bound placeholder (no clearance wording). Clicking the card deep-links via `useNavigate({ to: getDossierDetailPath(dossierRow.id, dossierRow.type) })` (CitationCard precedent). If the fetch returns nothing (above-clearance/not-found), render a neutral inline line — NO clearance/filtered/restricted wording (indistinguishable-empty).
    - `InlineSignalCard({ signal })` — a thin token-bound signal card (title, status, severity, category) that deep-links to the signal's route (the signals queue/detail route — confirm the route from the signals domain; reuse SignalRow if it fits inline with isFocused=false + onClick=navigate). Read-only: NO dismiss/escalate buttons inline (those are the HITL path). Bilingual via `useTranslation('intelligence-signals')` for signal labels and `'copilot'` for any new inline chrome.
    - Both cards follow CLAUDE.md DoD: flat `var(--surface)`, `1px solid var(--line)`, `var(--radius-sm)`, NO card shadow, logical properties, `writingDirection:'rtl'` + Tajawal in AR, NO `textAlign:'right'`, no raw hex, no Tailwind color literals.
    - Add any new inline-card chrome strings to BOTH en/copilot.json and ar/copilot.json under a `genui.*` block with full parity (the i18n trap: keys must exist in both or AR falls back to EN). Add `.copilot-genui*` classes to copilot-theme.css using existing tokens only.
  </action>
  <verify>
    <automated>cd frontend && test -f src/components/copilot/genui/InlineDossierCard.tsx && test -f src/components/copilot/genui/InlineSignalCard.tsx && grep -q "getDossierDetailPath" src/components/copilot/genui/InlineDossierCard.tsx && ! grep -nE "text-(left|right)|textAlign:\s*['\"]right|ml-[0-9]|mr-[0-9]|pl-[0-9]|pr-[0-9]|#[0-9a-fA-F]{6}|text-(blue|red|green|gray)-[0-9]" src/components/copilot/genui/InlineDossierCard.tsx src/components/copilot/genui/InlineSignalCard.tsx && node -e "const en=require('./src/i18n/en/copilot.json'),ar=require('./src/i18n/ar/copilot.json');const k=o=>Object.keys(o.genui||{}).length;if(k(en)===0||k(en)!==k(ar))process.exit(1)" && pnpm exec tsc --noEmit -p tsconfig.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: InlineDossierCard renders UniversalDossierCard (via useDossier) and deep-links with getDossierDetailPath; InlineSignalCard renders a token-bound signal card with a deep-link and no triage buttons.
    - SOURCE: neither card has raw hex / Tailwind color literals / physical-direction utilities / `textAlign:'right'`; AR uses writingDirection + Tajawal.
    - SOURCE: `genui.*` key count non-zero and identical between en/copilot.json and ar/copilot.json.
    - BEHAVIOR: above-clearance/not-found renders a neutral inline line (no clearance/filtered/restricted wording).
    - TEST: frontend type-checks.
  </acceptance_criteria>
  <done>Two read-only token-bound inline cards render the app's own dossier/signal components with working router deep-links and neutral empty states; EN/AR copy is at parity.</done>
</task>

<task type="auto">
  <name>Task 2: GenUI tool-UI renderers (fixed allowlist) + mount</name>
  <files>frontend/src/components/copilot/genui/genUiToolUIs.tsx, frontend/src/components/copilot/CopilotSurface.tsx</files>
  <read_first>
    @frontend/src/components/copilot/hitl/proposeToolUIs.tsx (73-03 — mirror makeAssistantToolUI usage + the CopilotSurface mount pattern)
    @frontend/src/components/copilot/CopilotSurface.tsx
  </read_first>
  <action>
    Create `genUiToolUIs.tsx` registering, via `makeAssistantToolUI` (D-07 direct component renderers; FIXED allowlist), read-result renderers:
    - `get_dossier` → render one `InlineDossierCard` from `result.dossier` (skip render if null → fall back to the default text part).
    - `list_dossiers` → render a vertical stack of `InlineDossierCard` from `result.dossiers` (cap the count rendered inline to a small N, e.g. 5, to keep the surface readable; the rest stay summarized in text).
    - `read_signals` → render a stack of `InlineSignalCard` from `result.signals` (same small cap).
    - These renderers are READ-ONLY (no approval, no addResult) — they render from `result` when `status` is complete; while running, render nothing (let the existing streaming/text parts show).
    - The allowlist is FIXED to these renderers backed by the two inline cards (D-07) — do NOT add a generic "render any component the model names" path.
    - Export `GenUiToolUIs` (the set of renderer components). Edit `CopilotSurface.tsx` to mount `<GenUiToolUIs />` inside `AssistantRuntimeProvider` ALONGSIDE the 73-03 `<ProposeToolUIs />` (both register on the same provider). Keep the runtime/JWT wiring unchanged. Coordinate the single edit to CopilotSurface so both mounts coexist (this plan is the later wave, so 73-03's mount is already present — add the genUI mount next to it, do not remove it).
  </action>
  <verify>
    <automated>cd frontend && grep -q "makeAssistantToolUI" src/components/copilot/genui/genUiToolUIs.tsx && grep -qE "get_dossier|list_dossiers|read_signals" src/components/copilot/genui/genUiToolUIs.tsx && grep -q "GenUiToolUIs\|genUiToolUIs" src/components/copilot/CopilotSurface.tsx && grep -q "ProposeToolUIs\|proposeToolUIs" src/components/copilot/CopilotSurface.tsx && pnpm exec tsc --noEmit -p tsconfig.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: genUiToolUIs.tsx registers renderers (makeAssistantToolUI) keyed to get_dossier/list_dossiers/read_signals, rendering ONLY InlineDossierCard/InlineSignalCard (fixed allowlist); no approval/addResult in these renderers.
    - SOURCE: CopilotSurface mounts BOTH GenUiToolUIs and the 73-03 ProposeToolUIs inside AssistantRuntimeProvider; runtime/JWT wiring unchanged.
    - BEHAVIOR: a dossier/signal read result renders the inline card(s); null/empty results fall back to text with no clearance wording.
    - TEST: frontend type-checks.
  </acceptance_criteria>
  <done>Read results for dossiers/signals render the app's own components inline via a fixed-allowlist tool-UI renderer set, mounted alongside the HITL renderers under AssistantRuntimeProvider.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                                                            |
| ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------- |
| model tool result → inline component | Only the fixed allowlist (dossier/signal cards) renders; the model cannot name an arbitrary component to mount.        |
| inline card data source → RLS        | The dossier fetch (useDossier) and the read-tool output are already clearance-gated; the card adds no privileged read. |

## STRIDE Threat Register

| Threat ID  | Category               | Component                        | Disposition | Mitigation Plan                                                                                                                                                                                                                         |
| ---------- | ---------------------- | -------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-73-04-01 | Tampering              | model-driven component rendering | mitigate    | FIXED allowlist of two renderers (D-07) — only UniversalDossierCard / the signal card render; no generic GenerativeUISpec "render any named component" path.                                                                            |
| T-73-04-02 | Information Disclosure | inline card empty state          | mitigate    | Above-clearance/not-found renders a neutral inline line — no clearance/filtered/restricted wording (indistinguishable-empty, carried P71 GRAPH-03). The card reads only already-cleared data (RLS-gated useDossier / read-tool output). |
| T-73-04-03 | Elevation of Privilege | inline read fetch                | mitigate    | InlineDossierCard fetches via the app's clearance-gated useDossier (caller session); it does not bypass RLS or read above-clearance fields.                                                                                             |
| T-73-04-04 | Tampering              | deep-link target                 | mitigate    | Deep-links are app-relative paths built by getDossierDetailPath from the (already-accessible) id+type and navigated via the router (CitationCard precedent) — no external/raw URL from the model is followed.                           |
| T-73-04-SC | Tampering              | npm installs                     | mitigate    | No new dependencies; if a task reaches for an install, STOP with a blocking checkpoint.                                                                                                                                                 |

</threat_model>

<verification>
- Frontend type-checks.
- Dossier/signal read results render the app's own token-bound components inline with working router deep-links; empty/above-clearance results render neutral text.
- The genUI mount coexists with the 73-03 HITL mount under AssistantRuntimeProvider.
</verification>

<success_criteria>

- GENUI-01: the copilot renders results as token-bound bilingual cards (the app's own components) inline, with deep-links into the app.
  </success_criteria>

<output>
Create `.planning/phases/73-agent-platform-writes-generative-ui/73-04-SUMMARY.md` when done. Record: whether InlineDossierCard used useDossier (option a) or the partial row (option b), the signal deep-link route you used, and the inline render cap N.
</output>
