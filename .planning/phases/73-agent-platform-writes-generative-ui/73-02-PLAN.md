---
phase: 73-agent-platform-writes-generative-ui
plan: 02
type: execute
wave: 2
depends_on: ['73-01']
files_modified:
  - agent-runtime/src/mastra/tools/propose-publish-digest.ts
  - agent-runtime/src/mastra/tools/propose-signal-status.ts
  - agent-runtime/src/mastra/tools/propose-work-item.ts
  - agent-runtime/src/mastra/tools/propose-brief.ts
  - agent-runtime/src/mastra/tools/index.ts
  - agent-runtime/src/mastra/agents/copilot.ts
  - agent-runtime/src/mastra/tools/tools.test.ts
autonomous: true
requirements: [GENUI-02, GENUI-03]
must_haves:
  truths:
    - 'The copilot exposes exactly four propose_* write-tools (propose_publish_digest, propose_signal_status, propose_work_item, propose_brief) that VALIDATE + ECHO structured args for a HITL card but NEVER commit server-side — no propose_* tool calls publish_digest, intelligence_event.update, tasks-create, work-item-dossiers, or persist_brief (D-03: the agent only proposes; the frontend commits).'
    - 'Each propose_* tool input is narrow-Zod-typed (UUIDs, enums, bounded strings) so the model cannot pass arbitrary SQL/columns; invalid args are rejected at the tool boundary (GENUI-02/threat: args mismatch).'
    - 'propose_brief generates the bilingual draft brief content from data the caller is cleared to see (the on-prem model + the caller-JWT read client), returning a SINGLE `content` object `{ en: {summary, sections}, ar: {summary, sections} }` — the exact jsonb envelope the reconciled persist_brief(p_content jsonb) expects (NOT separate content_en/content_ar args) — it NEVER calls AnythingLLM and NEVER persists (D-02).'
    - "The copilot agent's system prompt (EN + AR) no longer claims to be read-only; it states it can PROPOSE the four write actions for the user's confirmation and that nothing is committed without explicit approval. Per-decision citation: this plan implements D-01..D-03 (write set, brief INVOKER path, frontend-direct commit) — agent proposes, never commits."
  artifacts:
    - path: 'agent-runtime/src/mastra/tools/propose-publish-digest.ts'
      provides: 'propose_publish_digest tool — validates {dossierId, period, summary} and echoes them for the HITL card; no commit'
      contains: 'createTool'
    - path: 'agent-runtime/src/mastra/tools/propose-signal-status.ts'
      provides: 'propose_signal_status tool — validates {signalId, action: dismiss|escalate, reason?}; no commit'
      contains: 'createTool'
    - path: 'agent-runtime/src/mastra/tools/propose-work-item.ts'
      provides: 'propose_work_item tool — validates {title, assigneeId, priority?, dossierIds[], inheritanceSource?}; no commit'
      contains: 'createTool'
    - path: 'agent-runtime/src/mastra/tools/propose-brief.ts'
      provides: 'propose_brief tool — generates a single bilingual `content` draft { en:{summary,sections}, ar:{summary,sections} } (on-prem) for {dossierId}; no persist, no AnythingLLM'
      contains: 'createTool'
    - path: 'agent-runtime/src/mastra/tools/index.ts'
      provides: 'copilotTools roster extended with the four propose_* tools keyed by their ids'
      contains: 'propose_publish_digest'
  key_links:
    - from: 'each propose_* tool'
      to: 'context.requestContext authorization (caller JWT)'
      via: "supa.getAuthorization(...) assert non-empty before any read; createUserClient for propose_brief's read"
      pattern: 'getAuthorization'
    - from: 'copilotAgent.tools'
      to: 'the four propose_* tools'
      via: 'copilotTools roster'
      pattern: 'propose_(publish_digest|signal_status|work_item|brief)'
---

<objective>
Add the agent-runtime side of HITL writes: four `propose_*` write-tools that mirror the P72 reads-only tool pattern (narrow Zod, caller-JWT keystone, indistinguishable-empty) but are PROPOSE-ONLY — they validate/shape args (and, for brief, generate draft content from cleared data) and return them for a frontend confirmation card. They NEVER commit (D-03: the frontend approve-handler commits under the caller JWT in 73-03). Revise the copilot system prompt (EN+AR) so the agent proposes — rather than refuses — the four write actions.

Purpose: The agent's role in HITL is to PROPOSE with structured, validated args; the proven INVOKER commit paths stay on the frontend. This adds zero new service-role surface and keeps the keystone intact.
Output: 4 new tool files + extended registry + revised agent prompt + extended tool tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/73-agent-platform-writes-generative-ui/73-CONTEXT.md
@.planning/phases/73-agent-platform-writes-generative-ui/73-RESEARCH.md

<read_first>

<!-- The EXACT P72 read-tool pattern to mirror. Executor uses these directly — no exploration. -->

@agent-runtime/src/mastra/tools/\_supabase.ts (createUserClient + getAuthorization keystone)
@agent-runtime/src/mastra/tools/read-signals.ts (canonical narrow-Zod + indistinguishable-empty tool)
@agent-runtime/src/mastra/tools/generate-digest.ts (the READ-ONLY digest preview — propose_publish_digest is its WRITE-PROPOSE sibling; note generate-digest deliberately never names publish_digest)
@agent-runtime/src/mastra/tools/index.ts (copilotTools roster to extend)
@agent-runtime/src/mastra/agents/copilot.ts (the read-only EN/AR prompt to revise)
@agent-runtime/src/mastra/tools/tools.test.ts (the tool test harness to extend)
</read_first>

<interfaces>
<!-- Contracts the executor needs. Extracted from the live codebase. -->

Keystone (agent-runtime/src/mastra/tools/\_supabase.ts):

```ts
export function createUserClient(authorization: string): SupabaseClient // anon key + forwarded Bearer; NEVER service-role
export function getAuthorization(requestContext: { get: (k: string) => unknown }): string // '' if missing
```

Mastra tool shape (from read-signals.ts) — every propose\__ tool follows this:
createTool({ id, description, inputSchema: z.object({...}), outputSchema: z.object({...}),
execute: async (input, context) => { /_ assert auth; validate; return echoed/derived args \*/ } })
Re-export `export const createUserClient = supa.createUserClient` at top so tests can vi.spyOn it.

Registry (tools/index.ts) — extend the `copilotTools` object with the four new ids.

DB contracts the propose\_\* args must MATCH (so 73-03's commit succeeds):

- publish_digest(p_dossier_id uuid, p_period text, p_summary text, p_clearance_level_at_generation int) RETURNS uuid [INVOKER]
  → propose_publish_digest args: { dossierId: uuid, period: enum daily|weekly|monthly, summary: string }
- intelligence_event status flip (frontend UPDATE in 73-03): status ∈ acknowledged|dismissed|escalated; new D-06 actor cols
  → propose_signal_status args: { signalId: uuid, action: enum dismiss|escalate, reason?: string(<=500) }
- tasks-create edge fn body: { title, assignee_id, priority?, workflow_stage?, sla_deadline? } ; work-item-dossiers: { work_item_type, work_item_id, dossier_ids[], inheritance_source }
  → propose_work_item args: { title: string(<=500), assigneeId: uuid, priority?: enum low|medium|high|urgent, dossierIds: uuid[], inheritanceSource?: enum (default 'direct') }
- persist_brief(p_dossier_id uuid, p_content jsonb, p_title text DEFAULT NULL, p_summary text DEFAULT NULL) [INVOKER, from 73-01 — RECONCILED to live briefs schema]
  → propose_brief args IN: { dossierId: uuid } ; OUT: { dossierId, content: { en:{summary,sections[]}, ar:{summary,sections[]} } }
  (the single `content` object IS the p_content jsonb 73-03 passes; there are NO separate content_en/content_ar args — the live briefs table has no such columns).

Brief content shape persist_brief stores in the single `content` jsonb:
{ en: { summary: string, sections: [{title, content}, ...] }, ar: { summary: string, sections: [{title, content}, ...] } }.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three propose_* tools (digest, signal status, work item)</name>
  <files>agent-runtime/src/mastra/tools/propose-publish-digest.ts, agent-runtime/src/mastra/tools/propose-signal-status.ts, agent-runtime/src/mastra/tools/propose-work-item.ts</files>
  <read_first>
    @agent-runtime/src/mastra/tools/read-signals.ts (mirror this file's structure exactly)
    @agent-runtime/src/mastra/tools/_supabase.ts
  </read_first>
  <action>
    Create three propose-only Mastra tools, each in its own file, each mirroring read-signals.ts structure (re-export createUserClient at top; createTool with narrow Zod input + output; execute asserts auth then returns args). PROPOSE-ONLY: none of these three calls any RPC, edge function, or table mutation — they validate and ECHO the structured args the HITL card (73-03) renders, then the frontend commits. Per D-03, the agent never commits server-side.
    - propose-publish-digest.ts → id `propose_publish_digest`, description telling the model to use it when the user asks to PUBLISH a digest. inputSchema `{ dossierId: z.string().uuid(), period: z.enum(['daily','weekly','monthly']).default('daily'), summary: z.string().min(1).max(4000) }`. execute: assert `getAuthorization(...)` non-empty (return a neutral `{ proposed: false }` shape if missing — indistinguishable-empty, never a service-role fallback); otherwise return `{ proposed: true, action: 'publish_digest', args: { dossierId, period, summary } }`. Do NOT call publish_digest — the string must not appear as a call target (only as the `action` label).
    - propose-signal-status.ts → id `propose_signal_status`. inputSchema `{ signalId: z.string().uuid(), action: z.enum(['dismiss','escalate']), reason: z.string().max(500).optional() }`. execute: assert auth; return `{ proposed: true, action: 'signal_status', args: { signalId, action, reason } }`. (The status mapping dismiss→'dismissed' / escalate→'escalated' is applied by the frontend commit, not here.)
    - propose-work-item.ts → id `propose_work_item`. inputSchema `{ title: z.string().min(1).max(500), assigneeId: z.string().uuid(), priority: z.enum(['low','medium','high','urgent']).optional(), dossierIds: z.array(z.string().uuid()).min(1), inheritanceSource: z.enum(['direct','engagement','after_action','position','mou']).default('direct') }`. execute: assert auth; return `{ proposed: true, action: 'work_item', args: {...} }`.
    - outputSchema for all three: `z.object({ proposed: z.boolean(), action: z.string().optional(), args: z.record(z.string(), z.unknown()).optional() })`.
    - Narrow Zod is the threat control (no arbitrary columns/SQL reach the commit). Keep `.js` import specifiers (ESM, matching read-signals.ts importing `./_supabase.js`).
  </action>
  <verify>
    <automated>cd agent-runtime && grep -L "publish_digest\|\.rpc(\|\.from(\|\.insert(\|\.update(" src/mastra/tools/propose-signal-status.ts src/mastra/tools/propose-work-item.ts | grep -q propose && for f in propose-publish-digest propose-signal-status propose-work-item; do grep -q "createTool" "src/mastra/tools/$f.ts" || exit 1; done && pnpm exec tsc --noEmit -p tsconfig.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: each of the three files defines a `createTool` with the id and narrow Zod inputSchema above; the output is the `{ proposed, action?, args? }` shape.
    - SOURCE (propose-only): `propose-signal-status.ts` and `propose-work-item.ts` contain NO `.rpc(`, `.from(`, `.insert(`, `.update(`; `propose-publish-digest.ts` contains the literal `publish_digest` ONLY as the `action` string, never as a `.rpc('publish_digest')` call target.
    - BEHAVIOR: with a non-empty authorization, each tool returns `{ proposed: true, action, args }` echoing validated args; with an empty authorization each returns the neutral `{ proposed: false }` (no throw, no service-role fallback).
    - TEST: `tsc --noEmit` passes for the agent-runtime workspace.
  </acceptance_criteria>
  <done>Three propose-only write-tools exist, narrow-Zod-typed, committing nothing, returning validated args for the HITL card; the agent-runtime workspace type-checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: propose_brief tool with on-prem bilingual draft generation</name>
  <files>agent-runtime/src/mastra/tools/propose-brief.ts</files>
  <read_first>
    @agent-runtime/src/mastra/tools/generate-digest.ts (caller-JWT read + neutral-empty shape to mirror)
    @agent-runtime/src/mastra/tools/dossier-lookups.ts (how the existing tools read dossier data under the caller JWT — reuse the same read approach)
    @agent-runtime/src/llm-router.js or agent-runtime/src/llm-router.ts (getCopilotModel — the on-prem model used in copilot.ts; reuse for draft generation)
  </read_first>
  <behavior>
    - Given a cleared dossierId + non-empty authorization: returns `{ proposed: true, action: 'brief', args: { dossierId, content: { en, ar } } }` where `content.en` and `content.ar` are each `{ summary: string, sections: Array<{title,content}> }` — the single `content` object IS the jsonb envelope the reconciled persist_brief(p_content) stores (NOT separate contentEn/contentAr).
    - Given an EMPTY authorization, or a dossier the caller cannot read: returns the neutral `{ proposed: false }` (indistinguishable-empty — same shape for no-data, above-clearance, missing JWT, generation error). NO clearance/filtered/restricted token in the output.
    - NEVER calls AnythingLLM (the literal must not appear) and NEVER persists (no `.rpc('persist_brief')`, no `.insert`) — it only drafts; 73-03 persists on approval.
  </behavior>
  <action>
    Create propose-brief.ts (id `propose_brief`, description: use when the user asks to GENERATE/draft a brief for a dossier). inputSchema `{ dossierId: z.string().uuid() }`; outputSchema `{ proposed: z.boolean(), action: z.string().optional(), args: z.object({ dossierId: z.string(), content: z.object({ en: z.record(z.string(), z.unknown()), ar: z.record(z.string(), z.unknown()) }) }).optional() }`.
    execute: assert `getAuthorization(...)` non-empty (else `{ proposed: false }`); build a caller-JWT client via `createUserClient(authorization)`; read the dossier + the cleared context the brief summarizes using the SAME caller-JWT read approach the existing dossier-lookups tool uses (so RLS gates the source content — D-02's "on-prem generation" reads only cleared data). Generate the bilingual draft via the on-prem model from `getCopilotModel()` (reuse the copilot model; NO AnythingLLM, NO new dependency) into the `{summary, sections[]}` shape for EN and AR, and assemble them under a SINGLE `content: { en, ar }` object (this is what 73-03 forwards verbatim as persist_brief's `p_content`). Wrap the whole body so any read/generation failure returns `{ proposed: false }` (indistinguishable-empty). Do NOT call persist_brief or any insert.
    Note for the executor: this is the only NOVEL tool (the other three are mechanical echoes). If the on-prem model wiring needs a prompt, keep it minimal and bilingual; the heavy generation-substrate hardening is explicitly P74 (deferred) — P73 only needs a structurally valid bilingual `content` envelope that persist_brief will accept.
  </action>
  <verify>
    <automated>cd agent-runtime && grep -q "createTool" src/mastra/tools/propose-brief.ts && ! grep -qiE "anythingllm|persist_brief|\.insert\(|content_en|content_ar" src/mastra/tools/propose-brief.ts && grep -q "getAuthorization" src/mastra/tools/propose-brief.ts && pnpm exec tsc --noEmit -p tsconfig.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: propose-brief.ts defines `createTool` id `propose_brief`; output args carry a single `content` object with `en`/`ar` keys (NOT `contentEn`/`contentAr`); the file contains none of `anythingllm` (case-insensitive), `persist_brief`, `.insert(`, `content_en`, `content_ar`.
    - SOURCE: the dossier read uses `createUserClient(getAuthorization(...))` (caller JWT), never service-role.
    - BEHAVIOR: empty-auth / unreadable-dossier → `{ proposed: false }`; cleared dossier → `{ proposed: true, action:'brief', args:{dossierId, content:{ en:{summary,sections}, ar:{summary,sections} }} }`.
  </acceptance_criteria>
  <done>propose_brief drafts a single bilingual `content` envelope ({ en:{summary,sections}, ar:{summary,sections} } — the exact persist_brief p_content shape) from cleared data under the caller JWT, persists nothing, has zero AnythingLLM coupling, and returns indistinguishable-empty on any denial/error.</done>
</task>

<task type="auto">
  <name>Task 3: Register the four tools + revise the agent prompt (EN+AR) + extend tests</name>
  <files>agent-runtime/src/mastra/tools/index.ts, agent-runtime/src/mastra/agents/copilot.ts, agent-runtime/src/mastra/tools/tools.test.ts</files>
  <read_first>
    @agent-runtime/src/mastra/tools/index.ts (the copilotTools object to extend)
    @agent-runtime/src/mastra/agents/copilot.ts (COPILOT_SYSTEM_PROMPT + ARABIC_COPILOT_SYSTEM_PROMPT to revise)
    @agent-runtime/src/mastra/tools/tools.test.ts (the indistinguishable-empty + keystone assertions to extend)
  </read_first>
  <action>
    1. tools/index.ts — import the four propose_* tools and add them to the exported `copilotTools` object keyed by their ids (`propose_publish_digest`, `propose_signal_status`, `propose_work_item`, `propose_brief`), alongside the existing seven read tools. Update the barrel's header comment: the roster is no longer "reads-only" — it now includes four PROPOSE-only write-tools that commit nothing (the frontend commits under the caller JWT, D-03).
    2. agents/copilot.ts — revise BOTH `COPILOT_SYSTEM_PROMPT` and `ARABIC_COPILOT_SYSTEM_PROMPT`: replace the "You are read-only / never create, update, delete, or publish" clauses with: the agent can PROPOSE four actions for the user's confirmation — publish a digest, dismiss or escalate a signal, create + link a work item, and generate a brief — by calling the matching propose_* tool; it MUST state that nothing is committed until the user approves the confirmation card, and it must NOT claim a write succeeded (the frontend reports the result). Keep the bilingual parity, the citation rule, and the indistinguishable-empty no-answer rule intact. Update the Agent `description` to drop "Read-only".
    3. tools.test.ts — add cases proving, for each propose_* tool: (a) empty-authorization → the neutral `{ proposed: false }` (no throw); (b) valid args → `{ proposed: true, action, args }` echoing validated input; (c) no commit side effect — assert the tool never invokes `.rpc`/`.insert`/`.update` (use the existing spy harness in this file; for propose_brief, spy on createUserClient and assert no write call). Reuse the vi.spyOn(createUserClient) pattern already in the file.
  </action>
  <verify>
    <automated>cd agent-runtime && grep -q "propose_publish_digest" src/mastra/tools/index.ts && grep -q "propose_brief" src/mastra/tools/index.ts && ! grep -q "read-only\|read only" src/mastra/agents/copilot.ts && grep -q "propose" src/mastra/agents/copilot.ts && pnpm exec vitest run src/mastra/tools/tools.test.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `copilotTools` contains all four propose_* keys plus the original seven read keys (11 total).
    - SOURCE: copilot.ts no longer contains "read-only"/"read only" in either prompt or the description; both prompts mention proposing the four actions and that nothing commits without approval; AR prompt keeps parity (mentions the four actions + approval).
    - TEST: the extended tools.test.ts passes — each propose_* tool returns neutral `{proposed:false}` on empty auth, echoes validated args on valid input, and performs no commit call.
  </acceptance_criteria>
  <done>The agent advertises four propose-only write-tools, its EN+AR prompt proposes (not refuses) writes and guarantees approval-gating, and the tool tests prove propose-only + indistinguishable-empty behavior.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                                                                         |
| ------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| model output → propose\_\* tool input | The model picks args; narrow Zod rejects anything outside UUIDs/enums/bounded strings before they reach a card or a commit.                         |
| agent-runtime → Supabase              | Only propose*brief reads (caller JWT, RLS-gated); no propose*\* tool writes. The service-role key is never constructed (carried from P72 keystone). |
| caller JWT delivery                   | `context.requestContext.get('authorization')` must be non-empty; empty ⇒ neutral `{proposed:false}`, never a service-role fallback.                 |

## STRIDE Threat Register

| Threat ID  | Category                    | Component                 | Disposition | Mitigation Plan                                                                                                                                                                                                                            |
| ---------- | --------------------------- | ------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-73-02-01 | Elevation of Privilege      | propose\_\* tools         | mitigate    | No tool commits server-side; commit happens frontend-direct under the caller JWT (D-03). agent-runtime never holds the service-role key (73-RESEARCH §D).                                                                                  |
| T-73-02-02 | Tampering                   | model-supplied args       | mitigate    | Narrow Zod input on every propose\_\* tool (UUIDs/enums/length bounds) — no arbitrary SQL or columns can reach the commit path (73-RESEARCH §D "Cross-tenant / above-clearance write").                                                    |
| T-73-02-03 | Information Disclosure      | propose_brief source data | mitigate    | The draft is generated only from data the caller can READ (caller-JWT client, RLS-gated); indistinguishable-empty `{proposed:false}` on above-clearance/denial — no clearance/filtered/restricted token leaks (carried P71 GRAPH-03 lock). |
| T-73-02-04 | Spoofing / prompt-injection | "auto-commit" request     | mitigate    | The agent can only PROPOSE; the prompt forbids claiming a write succeeded; the actual commit requires the user's explicit Approve in 73-03 (73-RESEARCH §D "Auto-commit without approval / prompt-injection").                             |
| T-73-02-05 | Tampering                   | AnythingLLM substitution  | mitigate    | propose_brief uses the on-prem `getCopilotModel()` only; the literal `anythingllm` is absent from the file (D-02; P74 owns the retirement).                                                                                                |
| T-73-02-SC | Tampering                   | npm installs              | mitigate    | No new dependencies — assistant-ui/Mastra/Zod are already pinned (73-CONTEXT "no new deps"). If any task reaches for an install, STOP and surface a blocking checkpoint.                                                                   |

</threat_model>

<verification>
- agent-runtime type-checks (`tsc --noEmit`) and `tools.test.ts` passes.
- `copilotTools` exposes the four propose_* tools; none commits server-side.
- Both system prompts propose (not refuse) the four writes and guarantee approval-gating.
</verification>

<success_criteria>

- GENUI-02/03 (agent side): the copilot proposes writes with validated args and commits nothing server-side; the brief draft path is on-prem + caller-JWT, with persist deferred to the frontend INVOKER RPC.
  </success_criteria>

<output>
Create `.planning/phases/73-agent-platform-writes-generative-ui/73-02-SUMMARY.md` when done. Record the four tool ids + their exact arg schemas (73-03 keys its cards to these) and any on-prem model wiring choices for propose_brief.
</output>
