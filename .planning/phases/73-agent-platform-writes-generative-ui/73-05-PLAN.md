---
phase: 73-agent-platform-writes-generative-ui
plan: 05
type: execute
wave: 5
depends_on: ['73-04']
files_modified:
  - frontend/tests/e2e/copilot-writes.spec.ts
  - frontend/tests/e2e/copilot-genui.spec.ts
autonomous: false
requirements: [GENUI-01, GENUI-02, GENUI-03, GENUI-04]
must_haves:
  truths:
    - 'An automated E2E proves, in EN and AR, that ≥2 distinct write ops (e.g. dismiss signal + publish digest) each show a bilingual token-bound confirmation card and commit ONLY after Approve, and that Reject commits nothing (GENUI-02).'
    - "An automated/scripted check proves an approved write's DB row records the caller as actor (intelligence_event dismissed_by/escalated_by == auth.uid(); briefs/tasks created_by == auth.uid()) — never service-role (GENUI-03)."
    - 'An automated E2E proves that after an approved write, the conventional UI reflects the change in the SAME session with no manual reload (GENUI-04) — e.g. a dismissed signal leaves the queue / a published digest appears.'
    - "An automated E2E proves a dossier/signal read result renders the app's own inline card with a working router deep-link, in EN and AR (GENUI-01), and that a CDP-forced tool failure surfaces neutral role=alert copy with no clearance/filtered/restricted leakage (indistinguishable-empty)."
    - 'A blocking human-verify checkpoint confirms ≥2 write ops live on staging EN+AR (the phase success criteria require live-on-staging verification), since RLS-gated writes + the on-prem agent cannot be fully exercised by headless E2E alone.'
  artifacts:
    - path: 'frontend/tests/e2e/copilot-writes.spec.ts'
      provides: 'Playwright specs for HITL approve/reject (≥2 ops, EN+AR), cache-sync same-session, and the JWT-actor assertion scaffold'
      contains: 'setBlockedURLs'
    - path: 'frontend/tests/e2e/copilot-genui.spec.ts'
      provides: 'Playwright specs for inline genUI card render + deep-link (EN+AR) + indistinguishable-empty on forced failure'
      contains: "getByRole('alert')"
  key_links:
    - from: 'copilot-writes.spec.ts'
      to: 'the HITL confirmation card + commit'
      via: 'Approve/Reject buttons (EN+AR aria-names) → assert commit vs no-commit + cache refresh'
      pattern: 'Approve|Reject|موافقة|رفض'
    - from: 'actor assertion'
      to: 'DB row created_by/*_by == auth.uid()'
      via: 'scripted SQL/MCP check after an approved write in the same seeded session'
      pattern: "auth\\.uid|created_by|dismissed_by"
---

<objective>
Prove the four GENUI success criteria with bilingual E2E specs plus one blocking live-on-staging human-verify checkpoint. The phase's success criteria explicitly require live-on-staging verification of ≥2 write ops in EN+AR and a DB-row actor assertion — so this plan delivers automated specs for the deterministic parts AND a structured checkpoint for the live RLS/on-prem-agent parts that headless tests cannot fully exercise.

Purpose: Close GENUI-01..04 with evidence: HITL gating, caller-JWT actor, same-session cache sync, inline genUI + deep-links, and indistinguishable-empty under forced failure.
Output: two E2E spec files + a recorded live verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/73-agent-platform-writes-generative-ui/73-CONTEXT.md
@.planning/phases/73-agent-platform-writes-generative-ui/73-RESEARCH.md

<read_first>
@frontend/tests/e2e/copilot.spec.ts (THE pattern to mirror: FAB-open, EN/AR dual-name getByRole, dir="rtl"+Tajawal assertion, CDP Network.setBlockedURLs forced-error + role="alert" + indistinguishable-empty innerText check)
@e2e/intelligence-digests.spec.ts (digest publish/subscribe UI affordance names — for the publish-digest write op)
@frontend/src/components/copilot/hitl/ConfirmActionCard.tsx (the card's Approve/Reject affordances + the confirm.\* i18n keys to target)
@frontend/src/components/copilot/hitl/proposeToolUIs.tsx (which tool ids drive which card)
</read_first>

<interfaces>
<!-- Verification reality (MEMORY + 72-VALIDATION + project CDP UAT protocol). -->

- The e2e suite tests the DEPLOYED app vs staging zkrcjzdemdmwhearhfgg. The write/genUI tests require the agent-runtime /chat route reachable (DEPLOY-GATED on the GPU stack per the P72 SUMMARY — note this in the checkpoint).
- RLS denial returns an EMPTY 200, NOT an error status. NEVER assert on HTTP status. Force failures via CDP `Network.setBlockedURLs(['*/api/copilot/*', '*publish_digest*', '*persist_brief*', ...])` and assert `role="alert"` + neutral copy.
- Language persists under localStorage['id.locale'] (NOT i18nextLng); switch to AR via the ع topbar button. AR strings render with dir="rtl" + Tajawal.
- Test credentials come from env (.env.test): $TEST_USER_EMAIL / $TEST_USER_PASSWORD (CLAUDE.md). Admin login kazahrani@stats.gov.sa for the clearance-correct actor.
- BRIEF op precondition (reconciled 73-01): the brief write goes through `persist_brief`, gated by the additive `briefs_insert_via_dossier_edit` policy = `can_edit_dossier(source_dossier_id)` (owner OR admin/manager). The admin test user is admin/manager so can_edit_dossier is TRUE for any dossier — but if you run the brief op as a non-admin, pick a dossier the user OWNS (dossier_owners) or persist_brief returns NULL (neutral failure, no row). The persisted row carries the bilingual draft in a single `content` jsonb ({ en, ar }); there is NO content_en/content_ar/dossier_id column — assert on `content`, `created_by`, `source_dossier_id`.
- Intl.NumberFormat('ar') yields LATIN digits in Chrome — if any assertion touches numbers, use the app's toFormatLocale, do not hardcode digit glyphs.
- Screenshots can wedge in this CDP flow — prefer DOM assertions.
- Card affordances: the confirm.\* keys (from 73-03) supply the Approve/Reject/typed-confirm names in EN+AR — target them with dual-name regexes like /Approve|موافقة/ and /Reject|رفض/ (use the actual strings the executor put in copilot.json).
- DB actor assertion: after an approved signal dismiss in a seeded session, the intelligence_event row's status='dismissed' and dismissed_by == the test user's auth uid. Run this assertion via the Supabase MCP (service-role read to INSPECT the row is fine — it only READS to verify the actor the caller wrote) or a scripted SQL check; the WRITE under test must have gone through the caller JWT.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: HITL + cache-sync + actor E2E (copilot-writes.spec.ts)</name>
  <files>frontend/tests/e2e/copilot-writes.spec.ts</files>
  <read_first>
    @frontend/tests/e2e/copilot.spec.ts (mirror FAB-open + EN/AR + CDP patterns)
    @e2e/intelligence-digests.spec.ts (publish-digest UI names)
    @frontend/src/components/copilot/hitl/ConfirmActionCard.tsx (Approve/Reject affordance names)
  </read_first>
  <action>
    Write `copilot-writes.spec.ts` mirroring copilot.spec.ts conventions. Cover ≥2 distinct write ops (recommend: signal dismiss + publish digest) for GENUI-02/03/04:
    - GENUI-02 (confirm-before-commit), EN and AR: open the copilot, prompt it to perform the write (e.g. "dismiss signal X" / "publish the daily digest for this dossier"); assert a confirmation card (the ConfirmActionCard, token-bound) appears with the op's summary fields; assert NOTHING committed before Approve. Then click Approve (dual-name /Approve|موافقة/) and assert the card reaches a committed post-state. In a separate case, click Reject (/Reject|رفض/) + a reason and assert NO commit (the underlying row/list is unchanged).
    - GENUI-04 (cache sync, same session): after an approved dismiss, assert the dismissed signal leaves the signals queue / changes status in the SAME session with no page reload; after an approved publish, assert the digest appears. Assert via DOM (list membership / status), not screenshots.
    - GENUI-03 (actor) scaffold: after the approved dismiss, capture the signal id under test and assert (via a documented Supabase MCP / scripted SQL step in the spec's comments or a companion check) that intelligence_event.dismissed_by == the test user's uid and status='dismissed'. If the spec cannot run the SQL inline, encode the exact assertion query as a TODO the checkpoint (Task 2) executes live — but write the spec so the op-under-test is performed.
    - Strong-confirm: include a case asserting the publish-digest Approve stays DISABLED until the dossier name is typed (D-04).
    - Use CDP `Network.setBlockedURLs` to force a commit failure on one op and assert the card shows neutral error copy with NO clearance/filtered/restricted wording (indistinguishable-empty), and that nothing committed.
    - Tag each test with its requirement id in the title (GENUI-02 / -03 / -04). Use env creds ($TEST_USER_EMAIL/$TEST_USER_PASSWORD), localStorage['id.locale'] for AR, and DOM assertions only.
    - Mark agent-dependent tests with the project's deploy-gated annotation (test.fixme or a skip-with-reason) consistent with how copilot.spec.ts handles the DEPLOY-GATED /chat route, so CI stays green until the GPU stack is reachable — the live proof is the Task-2 checkpoint.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit -p tsconfig.json && pnpm exec playwright test tests/e2e/copilot-writes.spec.ts --list >/dev/null 2>&1 && grep -qE "GENUI-02|GENUI-03|GENUI-04" tests/e2e/copilot-writes.spec.ts && grep -q "setBlockedURLs" tests/e2e/copilot-writes.spec.ts && grep -qE "Approve|موافقة" tests/e2e/copilot-writes.spec.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: the spec contains tests titled with GENUI-02, GENUI-03, GENUI-04; covers ≥2 write ops; exercises Approve (commit) AND Reject (no commit) paths; includes the publish typed-confirm-disabled case and a CDP-forced neutral-error case.
    - SOURCE: assertions are DOM-based (no status-code asserts, no screenshot-dependent waits); AR path switches via localStorage['id.locale']; the actor assertion query is encoded (inline or as the checkpoint TODO).
    - TEST: `playwright test --list` resolves the file (it compiles + registers); frontend type-checks.
  </acceptance_criteria>
  <done>copilot-writes.spec.ts exercises HITL approve/reject for ≥2 ops in EN+AR, asserts same-session cache sync, encodes the JWT-actor DB assertion, includes the strong-confirm + indistinguishable-empty-on-failure cases, and is deploy-gate-annotated to keep CI green.</done>
</task>

<task type="auto">
  <name>Task 2: GenUI inline-card + deep-link + indistinguishable-empty E2E (copilot-genui.spec.ts)</name>
  <files>frontend/tests/e2e/copilot-genui.spec.ts</files>
  <read_first>
    @frontend/tests/e2e/copilot.spec.ts (CDP forced-error + dir="rtl"+Tajawal + EN/AR dual-name patterns)
    @frontend/src/components/copilot/genui/InlineDossierCard.tsx (the card + deep-link target to assert)
  </read_first>
  <action>
    Write `copilot-genui.spec.ts` for GENUI-01:
    - EN and AR: open the copilot, prompt a dossier/signal read ("show me the dossier X" / "what signals are on this dossier"); assert the app's OWN inline card renders (the InlineDossierCard / InlineSignalCard — target a stable selector/role/text it exposes), not just markdown. In AR, assert the surrounding surface is dir="rtl" with Tajawal (reuse copilot.spec.ts's computed-font assertion).
    - Deep-link: click the inline card and assert the router navigates to the dossier route (URL matches /dossiers/<segment>/<id>) — staying in-app.
    - Indistinguishable-empty: CDP-block the read tool/route and assert the surface shows neutral role="alert"/empty copy and the rendered dialog innerText contains NONE of clearance/filtered/restricted (mirror copilot.spec.ts AGENT-03).
    - Deploy-gate-annotate the agent-dependent tests as in Task 1.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit -p tsconfig.json && pnpm exec playwright test tests/e2e/copilot-genui.spec.ts --list >/dev/null 2>&1 && grep -q "GENUI-01" tests/e2e/copilot-genui.spec.ts && grep -qE "dossiers/.*\\$|getByRole\('alert'\)|setBlockedURLs" tests/e2e/copilot-genui.spec.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: the spec contains a GENUI-01 test rendering the inline app card in EN+AR, an inline-card deep-link navigation assertion (URL → /dossiers/...), and a CDP-forced indistinguishable-empty assertion (no clearance/filtered/restricted in dialog innerText).
    - TEST: `playwright test --list` resolves the file; frontend type-checks.
  </acceptance_criteria>
  <done>copilot-genui.spec.ts proves the inline app card renders in EN+AR with a working router deep-link and neutral indistinguishable-empty behavior on forced failure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-on-staging verification of HITL writes + actor (EN+AR)</name>
  <what-built>
    The full HITL write loop (4 propose_* tools → bilingual confirmation cards → caller-JWT commit via INVOKER paths → post-commit cache sync) plus inline genUI cards. The automated specs above are deploy-gated on the GPU/agent-runtime stack; this checkpoint captures the live proof the phase success criteria require.
  </what-built>
  <how-to-verify>
    Preconditions: the agent-runtime /chat stack is reachable (GPU stack up, per the P72/Phase-72 deploy gate) and you are logged in as the admin test user (kazahrani@stats.gov.sa; creds in .env.test).

    1. PUBLISH DIGEST (GENUI-02/03/04), EN then AR (switch via the ع topbar button):
       - Open the copilot, ask it to publish the daily digest for a dossier you can edit.
       - CONFIRM: a token-bound confirmation card appears; the Approve button is DISABLED until you type the dossier name (D-04 strong-confirm). Reject once and confirm nothing publishes.
       - Re-run, type the name, Approve. CONFIRM the digest publishes AND the conventional digests view reflects it in the SAME session (no reload).
    2. DISMISS SIGNAL (GENUI-02/03/04), EN then AR:
       - Ask the copilot to dismiss a specific signal. CONFIRM the confirmation card; Approve.
       - CONFIRM the signal leaves the queue / shows dismissed in the SAME session.
       - ACTOR (GENUI-03): via the Supabase MCP, run a SELECT on intelligence_event for that signal id and CONFIRM status='dismissed', dismissed_by == your auth uid, and status_changed_at is set. Repeat the created_by==uid check for a brief (persist_brief) or task you create. For the brief, generate it for a dossier the test admin CAN edit (persist_brief returns NULL — neutral failure — for a non-editable dossier, per the additive briefs_insert_via_dossier_edit policy); CONFIRM the persisted briefs row has created_by == your auth uid AND source_dossier_id == that dossier id.
    3. INDISTINGUISHABLE-EMPTY: trigger an above-clearance/blocked write or read and CONFIRM the surface shows neutral copy with NO "clearance/filtered/restricted" wording anywhere in the drawer.
    4. GENUI-01: ask for a dossier and CONFIRM the app's own inline card renders with a working deep-link, in EN and AR (dir=rtl + Tajawal).

    Record the signal id + the dismissed_by uid you observed, and which ops you verified in each language, in the SUMMARY.

  </how-to-verify>
  <resume-signal>Type "approved" once ≥2 write ops are verified live EN+AR with the DB actor matching auth.uid(); or describe the failures (which op/lang/criterion).</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                                                                                                                    |
| ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| test harness ↔ live RLS | The actor assertion READS the DB to confirm the caller wrote it; the write-under-test must have gone through the caller JWT, not service-role. |
| forced-failure path     | CDP network blocking simulates RLS denial / outage; the surface must stay indistinguishable-empty.                                             |

## STRIDE Threat Register

| Threat ID  | Category               | Component                       | Disposition | Mitigation Plan                                                                                                                                                                                          |
| ---------- | ---------------------- | ------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-73-05-01 | Information Disclosure | forced-failure copy             | mitigate    | The spec asserts the dialog innerText contains no clearance/filtered/restricted token under CDP-forced failure (indistinguishable-empty, carried P71 GRAPH-03 / P72 AGENT-03).                           |
| T-73-05-02 | Repudiation            | approved-write actor            | mitigate    | The GENUI-03 assertion confirms the DB row's `*_by`/`created_by` equals the caller's `auth.uid()` — proving the write committed under the user, not service-role.                                        |
| T-73-05-03 | Tampering              | test asserting on flaky signals | accept      | DOM-based assertions + deploy-gate annotations (no HTTP-status asserts, no screenshot waits) per the project CDP UAT protocol; flakiness risk is low and the live checkpoint is the authoritative proof. |
| T-73-05-SC | Tampering              | npm installs                    | mitigate    | No new dependencies (Playwright already present); if a task reaches for an install, STOP with a blocking checkpoint.                                                                                     |

</threat_model>

<verification>
- Both spec files type-check and resolve via `playwright test --list`.
- The specs cover GENUI-01..04 with DOM assertions, EN+AR, strong-confirm, and indistinguishable-empty.
- The blocking checkpoint records ≥2 write ops verified live EN+AR with the DB actor == auth.uid().
</verification>

<success_criteria>

- GENUI-01: inline app cards + deep-links proven in EN+AR.
- GENUI-02: HITL approve/reject proven for ≥2 ops (live + spec).
- GENUI-03: approved-write actor == auth.uid() confirmed against the DB; never service-role.
- GENUI-04: same-session cache sync proven with no reload.
  </success_criteria>

<output>
Create `.planning/phases/73-agent-platform-writes-generative-ui/73-05-SUMMARY.md` when done. Record: the live verification result (ops × language matrix), the observed signal id + dismissed_by uid, and any deploy-gated tests left fixme'd pending the GPU stack.
</output>
