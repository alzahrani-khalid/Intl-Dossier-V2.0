---
phase: 68-ai-foundations-remediation
plan: 04
type: execute
wave: 3
depends_on:
  - 68-01
  - 68-02
files_modified:
  - backend/src/ai/agents/chat-assistant.ts
  - backend/src/api/ai/chat.ts
  - tests/integration/chat-assistant-rls.test.ts
autonomous: true
requirements:
  - REMED-03

must_haves:
  truths:
    - "chat-assistant.ts imports no supabaseAdmin — all 5 tool functions read under caller's JWT (D-08 — supabaseAdmin retired from interactive assistant)"
    - 'ChatRequest carries authHeader field (Bearer <token>)'
    - 'queryCommitments reads from aa_commitments with correct column select (D-10 — legacy commitments repointed)'
    - 'getEngagementHistory reads from engagement_dossiers with correct column select (D-10 — legacy engagements repointed)'
    - 'Empty results on clearance-denied queries return generic empty array (D-09 — no clearance messaging)'
    - 'chat-assistant-rls test passes GREEN'
    - 'backend/src/middleware/clearance-check.ts is not modified — DB RLS is the P68 enforcement layer (D-04 scope guard)'
  artifacts:
    - path: 'backend/src/ai/agents/chat-assistant.ts'
      provides: '5 tool functions using JWT-scoped Supabase client; no supabaseAdmin import'
      contains: 'authHeader'
    - path: 'backend/src/api/ai/chat.ts'
      provides: 'passes authHeader from req.headers.authorization into ChatRequest'
      contains: 'authHeader'
    - path: 'tests/integration/chat-assistant-rls.test.ts'
      provides: 'REMED-03 no-supabaseAdmin + authHeader assertions GREEN'
      contains: 'supabaseAdmin'
  key_links:
    - from: 'backend/src/api/ai/chat.ts'
      to: 'backend/src/ai/agents/chat-assistant.ts'
      via: 'ChatRequest.authHeader'
      pattern: 'authHeader'
    - from: 'chat-assistant.ts tool functions'
      to: 'Supabase (profiles.clearance_level RLS)'
      via: 'createClient(SUPABASE_URL, SUPABASE_ANON_KEY, { global: { headers: { Authorization: authHeader } } })'
      pattern: 'SUPABASE_ANON_KEY'
---

<objective>
REMED-03: Retire `supabaseAdmin` from `chat-assistant.ts`. Swap all 5 tool functions to use a JWT-scoped Supabase client. Pass the caller's Bearer token from `chat.ts` into `ChatRequest.authHeader`. Repoint `queryCommitments` to `aa_commitments` and `getEngagementHistory` to `engagement_dossiers` per D-10.

Purpose: Closes the service-role elevation-of-privilege path in the interactive assistant. After this plan, a non-cleared user's assistant queries will return only within-clearance content, with indistinguishable-empty behavior on RLS denial (D-09).

Output: Two modified TypeScript files; chat-assistant-rls test GREEN; pnpm build passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/68-ai-foundations-remediation/68-RESEARCH.md
@.planning/phases/68-ai-foundations-remediation/68-PATTERNS.md
@.planning/phases/68-ai-foundations-remediation/68-01-SUMMARY.md
@.planning/phases/68-ai-foundations-remediation/68-02-SUMMARY.md

<interfaces>
<!-- Extracted from codebase — executor must read these before touching anything. -->

From backend/src/ai/agents/chat-assistant.ts (current shape):

- Line 14: `import { supabaseAdmin } from '../../config/supabase.js'` — REMOVE THIS IMPORT
- Lines 36–42 ChatRequest interface:
  ```typescript
  export interface ChatRequest {
    message: string
    conversationHistory?: ChatMessage[]
    organizationId: string
    userId: string
    language?: 'en' | 'ar'
  }
  ```
  ADD `authHeader: string` as required field.
- 5 tool functions that use supabaseAdmin: searchEntities (~158), getDossier (~230), queryCommitments (~261), getEngagementHistory (~300), listDossiers (~329)
- Current queryCommitments body selects from 'commitments' with columns `source, responsible, timeline, tracking` — THESE DO NOT EXIST on aa_commitments
- Current getEngagementHistory selects from 'engagements' with `location_en, location_ar` — THESE DO NOT EXIST on engagement_dossiers

From backend/src/api/ai/chat.ts (pattern to add):

```typescript
// req.headers.authorization already contains 'Bearer <token>' (set by authenticateToken middleware)
const authHeader = req.headers.authorization ?? ''
// Include in ChatRequest:
const chatRequest: ChatRequest = { ..., authHeader }
```

JWT-scoped client pattern (from search-semantic/index.ts lines 167–174, adapted for Node.js backend):

```typescript
import { createClient } from '@supabase/supabase-js'
// Use ANON_KEY (not SERVICE_ROLE_KEY) — RLS applies to role claims
const supabaseClient = createClient(process.env.SUPABASE_URL!, process.env.SUPABASE_ANON_KEY!, {
  global: { headers: { Authorization: authHeader } },
})
```

D-09 indistinguishable empty (existing pattern in chat-assistant.ts to keep):

```typescript
if (error) {
  logger.error('Query failed', { error, input })
  return { results: [] }
  // DO NOT add 'filtered by clearance' or 'access denied' messaging
}
```

A2 result from 68-01-SUMMARY.md (aa_commitments actual columns):
Use these exact columns for the rewritten queryCommitments select — read from 68-01-SUMMARY.md.

A3 result from 68-01-SUMMARY.md (engagement_dossiers actual columns):
Use these exact columns for the rewritten getEngagementHistory select — read from 68-01-SUMMARY.md.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite chat-assistant.ts — remove supabaseAdmin, wire JWT client, D-10 repoints</name>
  <files>backend/src/ai/agents/chat-assistant.ts</files>
  <read_first>
    - backend/src/ai/agents/chat-assistant.ts (read the FULL file — understand all 5 tool function bodies, the supabaseAdmin usage sites, the ChatRequest interface, and the error handling pattern; DO NOT modify the file until you understand every supabaseAdmin call site)
    - .planning/phases/68-ai-foundations-remediation/68-01-SUMMARY.md (A2: aa_commitments columns; A3: engagement_dossiers columns — use these EXACT columns in select lists)
    - .planning/phases/68-ai-foundations-remediation/68-PATTERNS.md § `backend/src/ai/agents/chat-assistant.ts` (the 5-function table with line references and select-change guidance)
    - .planning/phases/68-ai-foundations-remediation/68-RESEARCH.md § Pitfall 3 (aa_commitments column mismatch) and Pitfall 4 (chat.ts supabaseAdmin for logging — do NOT remove those)
  </read_first>
  <behavior>
    - `grep -r 'supabaseAdmin' backend/src/ai/agents/` exits 1 (grep finds nothing — supabaseAdmin not imported or used in agents dir)
    - `grep -c 'SUPABASE_ANON_KEY' backend/src/ai/agents/chat-assistant.ts` returns >= 1
    - `grep -c 'authHeader' backend/src/ai/agents/chat-assistant.ts` returns >= 5 (each tool function uses it)
    - `grep -c 'aa_commitments' backend/src/ai/agents/chat-assistant.ts` returns >= 1 (D-10 repoint)
    - `grep -c 'engagement_dossiers' backend/src/ai/agents/chat-assistant.ts` returns >= 1 (D-10 repoint)
    - `grep -c "from\\('commitments'\\)" backend/src/ai/agents/chat-assistant.ts` returns 0 (old table not referenced)
    - `grep -c "from\\('engagements'\\)" backend/src/ai/agents/chat-assistant.ts` returns 0 (old table not referenced)
  </behavior>
  <action>
    Make surgical changes to `backend/src/ai/agents/chat-assistant.ts`:

    CHANGE 1 — Remove the `supabaseAdmin` import (line 14). Add import for `createClient` from `@supabase/supabase-js` if not already present.

    CHANGE 2 — Extend `ChatRequest` interface: add `authHeader: string` as a required field (after `userId: string`).

    CHANGE 3 — Modify tool function signatures: Each of the 5 tool functions currently takes `(_organizationId: string)` or similar as the 2nd param. Thread `authHeader: string` into each function (either as a new param or by reading from the outer `ChatRequest` object depending on how they are currently structured — read the file to understand the actual calling convention before changing signatures).

    CHANGE 4 — Inside each tool function, replace the `supabaseAdmin.from(...)` calls with a per-call JWT-scoped client:
    ```typescript
    const supabaseClient = createClient(
      process.env.SUPABASE_URL!,
      process.env.SUPABASE_ANON_KEY!,
      { global: { headers: { Authorization: authHeader } } }
    )
    ```
    Then replace `supabaseAdmin.from(table)` with `supabaseClient.from(table)`.

    CHANGE 5 — D-10 repoint `queryCommitments`:
    - FROM: `from('commitments').select('id, title, type, status, priority, source, responsible, timeline, tracking, ...')`
    - TO: `from('aa_commitments').select('id, title, title_ar, type, status, priority, due_date, owner_user_id, is_deleted, created_at, updated_at')` — use the EXACT column list from A2 in 68-01-SUMMARY.md (do not guess, use the confirmed live columns)
    - Add `.eq('is_deleted', false)` filter (aa_commitments has this column confirmed)

    CHANGE 6 — D-10 repoint `getEngagementHistory`:
    - FROM: `from('engagements').select('id, engagement_type, engagement_category, location_en, location_ar')`
    - TO: `from('engagement_dossiers').select('id, engagement_type, engagement_category, engagement_date')` — use EXACT columns from A3 in 68-01-SUMMARY.md

    PRESERVE — D-09: the existing empty-return pattern `return { commitments: [] }` / `return { dossiers: [] }` on error. Do NOT add any message mentioning "clearance", "filtered", or "access denied".

    PRESERVE — Do NOT touch any function or file in `backend/src/api/ai/chat.ts` (that is Task 2's scope).

    Per project style: no semicolons, single quotes, 2-space indent, explicit return types (`explicit-function-return-type` ESLint rule is enforced), no `any`.

  </action>
  <verify>
    <automated>
      grep -r 'supabaseAdmin' backend/src/ai/agents/ && echo FOUND || echo NOT_FOUND
      grep -c 'SUPABASE_ANON_KEY' backend/src/ai/agents/chat-assistant.ts
      grep -c 'aa_commitments' backend/src/ai/agents/chat-assistant.ts
      pnpm --filter backend typecheck 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -r 'supabaseAdmin' backend/src/ai/agents/` exits 1 (no match = supabaseAdmin fully removed from agents directory)
    - `grep -c 'SUPABASE_ANON_KEY' backend/src/ai/agents/chat-assistant.ts` returns >= 1
    - `grep -c 'aa_commitments' backend/src/ai/agents/chat-assistant.ts` returns >= 1
    - `grep -c 'engagement_dossiers' backend/src/ai/agents/chat-assistant.ts` returns >= 1
    - `grep -c "from('commitments')" backend/src/ai/agents/chat-assistant.ts` returns 0
    - `grep -c "from('engagements')" backend/src/ai/agents/chat-assistant.ts` returns 0
    - `pnpm --filter backend typecheck` exits 0 (no TypeScript errors on this file)
    - ChatRequest interface contains `authHeader: string`
  </acceptance_criteria>
  <done>supabaseAdmin removed from chat-assistant.ts. All 5 tool functions use JWT-scoped client. aa_commitments and engagement_dossiers repointed. D-09 empty-return preserved. TypeScript clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire authHeader in chat.ts + flip chat-assistant-rls test GREEN</name>
  <files>backend/src/api/ai/chat.ts, tests/integration/chat-assistant-rls.test.ts</files>
  <read_first>
    - backend/src/api/ai/chat.ts (read the FULL file — find where ChatRequest is constructed and chatAssistantAgent is called; confirm supabaseAdmin uses in THIS file are operational-logging writes only — do NOT remove those)
    - .planning/phases/68-ai-foundations-remediation/68-PATTERNS.md § `backend/src/api/ai/chat.ts`
    - .planning/phases/68-ai-foundations-remediation/68-RESEARCH.md § Pitfall 4 (chat.ts supabaseAdmin logging must be preserved)
    - tests/integration/chat-assistant-rls.test.ts (the stub from plan 01 — confirm what assertions are expected)
  </read_first>
  <behavior>
    - `grep -c 'authHeader' backend/src/api/ai/chat.ts` returns >= 1 (the header is extracted and passed)
    - `pnpm --filter backend test --run tests/integration/chat-assistant-rls.test.ts` passes (grep for supabaseAdmin in agents dir now exits 1 = passes the "not found" assertion; ChatRequest has authHeader field)
    - `pnpm --filter backend typecheck` exits 0
    - `pnpm --filter backend test --run` exits 0 (full backend test suite)
  </behavior>
  <action>
    In `backend/src/api/ai/chat.ts`:

    CHANGE 1 — Extract authHeader from request. Find the location where the chat handler reads `req.user` (after the `supabaseAuth` middleware has run). Add:
    ```typescript
    const authHeader = req.headers.authorization ?? ''
    ```

    CHANGE 2 — Pass authHeader into ChatRequest. Find the object literal that creates the ChatRequest payload (the one passed to `chatAssistantAgent` or equivalent). Add `authHeader` to it.

    PRESERVE — Do NOT remove any `supabaseAdmin` calls in chat.ts that write to `ai_runs`, `ai_messages`, `ai_tool_calls`. These are operational logging writes (service-role is correct here). Audit: if any `supabaseAdmin` call in chat.ts reads user-facing DATA (dossiers, commitments, etc.) rather than writing to internal tables, flag it and remove that one. If all supabaseAdmin calls in chat.ts are writes to internal/logging tables, leave them all.

    Then update `tests/integration/chat-assistant-rls.test.ts` (from plan 01): The test stubs check for "supabaseAdmin NOT found in agents dir" and "authHeader in ChatRequest". These should now flip from RED to GREEN. If the test already checks these via file-read assertions, it will pass. Run the test and confirm GREEN.

    Run the full backend suite: `pnpm --filter backend test --run`

  </action>
  <verify>
    <automated>
      grep -c 'authHeader' backend/src/api/ai/chat.ts
      pnpm --filter backend test --run tests/integration/chat-assistant-rls.test.ts
      pnpm --filter backend test --run
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'authHeader' backend/src/api/ai/chat.ts` returns >= 1
    - `pnpm --filter backend test --run tests/integration/chat-assistant-rls.test.ts` passes GREEN
    - `pnpm --filter backend test --run` exits 0 (full backend suite)
    - `pnpm --filter backend typecheck` exits 0
    - `grep -r 'supabaseAdmin' backend/src/ai/agents/` exits 1 (no supabaseAdmin in agents dir — confirms Task 1 and Task 2 together)
    - No "filtered by clearance" or "access denied" string in backend/src/ai/agents/chat-assistant.ts (D-09 compliance)
  </acceptance_criteria>
  <done>authHeader wired from chat.ts into ChatRequest. chat-assistant-rls test GREEN. Full backend test suite passes. supabaseAdmin confirmed absent from agents directory.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                  |
| ---------------------------------- | ---------------------------------------------------------------------------- |
| HTTP request → chat handler        | JWT verified by `supabaseAuth` middleware before handler runs                |
| chat handler → Supabase DB         | JWT-scoped client; RLS enforces sensitivity_level <= clearance_level         |
| chat handler → ai_runs/ai_messages | Service-role logging writes (correct — internal tables, not user data reads) |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                              | Disposition | Mitigation Plan                                                                                  |
| ----------- | ---------------------- | ------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------ |
| T-EoP-SR    | Elevation of Privilege | supabaseAdmin in chat-assistant.ts tool fns            | mitigate    | Retire import; all 5 tool functions use JWT-scoped createClient with ANON_KEY                    |
| T-ID-EMPTY  | Information Disclosure | empty result reveals clearance-filtered content exists | mitigate    | D-09: empty array return; no "filtered by clearance" messaging; RLS returns [] indistinguishably |
| T-68-04-D10 | Tampering              | queryCommitments reads wrong (empty) commitments table | mitigate    | Repoint to aa_commitments; verify column select against A2 live schema from 68-01-SUMMARY        |
| T-68-04-SC  | Tampering              | npm/pip installs                                       | mitigate    | No new packages — createClient from existing @supabase/supabase-js                               |

</threat_model>

<verification>
1. `grep -r 'supabaseAdmin' backend/src/ai/agents/` exits 1 (grep exits 1 = not found)
2. `grep -c 'authHeader' backend/src/api/ai/chat.ts` >= 1
3. `pnpm --filter backend test --run tests/integration/chat-assistant-rls.test.ts` passes
4. `pnpm --filter backend test --run` exits 0
5. `pnpm --filter backend typecheck` exits 0
</verification>

<success_criteria>

- supabaseAdmin fully removed from backend/src/ai/agents/ directory
- ChatRequest carries authHeader (Bearer JWT)
- All 5 tool functions read under caller JWT (JWT-scoped client)
- queryCommitments reads aa_commitments; getEngagementHistory reads engagement_dossiers
- D-09 empty-return preserved; no clearance disclosure strings
- chat-assistant-rls integration test GREEN
- Full backend test suite passes
  </success_criteria>

<output>
Create `.planning/phases/68-ai-foundations-remediation/68-04-SUMMARY.md` when done.
Note whether any supabaseAdmin uses in chat.ts were operational-logging writes (expected) vs data reads (unexpected — flag if found).
</output>
