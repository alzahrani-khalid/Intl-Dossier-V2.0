---
phase: 68-ai-foundations-remediation
plan: 08
type: execute
wave: 5
depends_on:
  - 68-03
  - 68-04
  - 68-05
  - 68-06
  - 68-07
files_modified: []
autonomous: false
requirements:
  - REMED-01
  - REMED-02
  - REMED-03
  - REMED-04
  - REMED-05
  - REMED-06

must_haves:
  truths:
    - 'pnpm test (full suite) exits 0'
    - 'A level-1 JWT cannot retrieve above-clearance dossiers via semantic search on staging'
    - 'A level-1 JWT cannot retrieve above-clearance dossiers via the assistant on staging'
    - 'Both search and assistant clearance blocks hold in English and Arabic mode'
    - 'Langfuse or Phoenix shows a complete trace for one assistant request'
    - 'pnpm run lint (frontend) exits 0 (namespace guard passes)'
    - 'All 6 REMED requirements are demonstrably met on staging'
  artifacts:
    - path: '.planning/phases/68-ai-foundations-remediation/68-08-SUMMARY.md'
      provides: 'Phase 68 gate verification summary with all SQL results + UAT observations'
      contains: 'REMED-01'
  key_links:
    - from: 'level-1 JWT'
      to: 'dossiers (sensitivity_level=2)'
      via: 'search_semantic_clearance_gated RLS enforcement'
      pattern: 'sensitivity_level'
    - from: 'level-1 JWT'
      to: 'chat-assistant.ts tool fns'
      via: 'JWT-scoped Supabase client RLS'
      pattern: 'clearance_level'
---

<objective>
[BLOCKING] Phase 68 gate: run the full test suite, apply any remaining required SQL (A1 fulltext DEFINER remediation if needed), and perform live UAT on staging confirming all 6 REMED requirements are met. This plan is the hard gate before any v7.0 Phase 69+ work begins.

Purpose: The five REMED success criteria from ROADMAP.md § Phase 68 must be demonstrably satisfied by live evidence on staging (`zkrcjzdemdmwhearhfgg`), in both English and Arabic mode.

Output: Phase 68 completion gate — SUMMARY with all SQL verifications + UAT evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/68-ai-foundations-remediation/68-RESEARCH.md
@.planning/phases/68-ai-foundations-remediation/68-01-SUMMARY.md
@.planning/phases/68-ai-foundations-remediation/68-02-SUMMARY.md
@.planning/phases/68-ai-foundations-remediation/68-03-SUMMARY.md
@.planning/phases/68-ai-foundations-remediation/68-04-SUMMARY.md
@.planning/phases/68-ai-foundations-remediation/68-05-SUMMARY.md
@.planning/phases/68-ai-foundations-remediation/68-06-SUMMARY.md
@.planning/phases/68-ai-foundations-remediation/68-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite + A1 fulltext remediation if needed</name>
  <files>supabase/migrations/20260614000003_p68_fulltext_invoker.sql (only if A1 confirmed DEFINER)</files>
  <read_first>
    - .planning/phases/68-ai-foundations-remediation/68-01-SUMMARY.md (A1 result: is search_entities_fulltext SECURITY DEFINER or INVOKER?)
    - All 68-0N-SUMMARY.md files for confirmation of prior plan completions
  </read_first>
  <action>
    STEP 1 — Conditional A1 remediation.
    From 68-01-SUMMARY.md, read the A1 result: `SELECT prosecdef FROM pg_proc WHERE proname='search_entities_fulltext'`.

    IF A1 is `true` (DEFINER — keyword fallback also bypasses RLS): Create migration `20260614000003_p68_fulltext_invoker.sql` with a `CREATE OR REPLACE FUNCTION search_fulltext_clearance_gated(...)` SECURITY INVOKER RPC that mirrors the new semantic RPC pattern but uses `tsvector`/`tsquery` text search instead of vector distance. Apply via Supabase MCP. Update `search-semantic/index.ts` to call `search_fulltext_clearance_gated` instead of `search_entities_fulltext`. Deploy the updated edge function.

    IF A1 is `false` (INVOKER — fulltext correctly passes caller identity): No action needed for fulltext. Note this in SUMMARY.

    STEP 2 — Full suite.
    Run: `pnpm test`

    All tests must pass. If any test fails:
    - If it is one of the new REMED tests in RED state (unexpected regression), investigate and fix before proceeding.
    - If it is a pre-existing flaky test unrelated to this phase, document the failure in SUMMARY and proceed to the UAT step.

    STEP 3 — Pre-commit lint.
    Run: `pnpm run lint`
    Must exit 0 (ESLint clean + namespace guard clean).

    STEP 4 — Type check.
    Run: `pnpm typecheck`
    Must exit 0.

  </action>
  <verify>
    <automated>pnpm test && pnpm run lint && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0 (all tests pass; document any pre-existing unrelated failures separately)
    - `pnpm run lint` exits 0
    - `pnpm typecheck` exits 0
    - IF A1 was DEFINER: migration `20260614000003_p68_fulltext_invoker.sql` applied + `SELECT prosecdef FROM pg_proc WHERE proname='search_fulltext_clearance_gated'` returns `false`
    - IF A1 was INVOKER: note in SUMMARY, no action taken
  </acceptance_criteria>
  <done>Full suite passing. Lint clean. Type check clean. A1 remediation applied if required.</done>
</task>

<task type="auto">
  <name>Task 2: Staged SQL verification — all 6 REMED database-level gates</name>
  <files></files>
  <read_first>
    - .planning/phases/68-ai-foundations-remediation/68-VALIDATION.md (Per-Task Verification Map — run each automated SQL command)
    - All prior 68-0N-SUMMARYs for context
  </read_first>
  <action>
    Run the following SQL assertions via Supabase MCP `execute_sql` on staging `zkrcjzdemdmwhearhfgg` and record each result verbatim in the SUMMARY:

    REMED-01 verification:
    1. `SELECT get_user_clearance_level('00000000-0000-0000-0000-000000000000')` → must return `1`
    2. `SELECT prosecdef FROM pg_proc WHERE proname='get_user_clearance_level'` → must return `true` (SECURITY DEFINER — correct for the shim)
    3. `SELECT COUNT(*) FROM profiles WHERE clearance_level = 3` → must return `5` (no downgrade)
    4. `SELECT COUNT(*) FROM profiles WHERE clearance_level >= 2` → must return `>= 5` (backfill applied to analysts)

    REMED-02 verification:
    5. `SELECT prosecdef FROM pg_proc WHERE proname='search_semantic_clearance_gated'` → must return `false` (INVOKER)
    6. `SELECT indexname FROM pg_indexes WHERE tablename='ai_embeddings' AND indexname='idx_ai_embeddings_hnsw'` → must return `idx_ai_embeddings_hnsw`
    7. `SELECT proname FROM pg_proc WHERE proname='search_entities_semantic'` → must return `search_entities_semantic` (old RPC preserved but unused)

    REMED-03 verification (source assertions, not SQL):
    8. `grep -r 'supabaseAdmin' backend/src/ai/agents/` → must exit 1 (no match)
    9. `grep -c 'authHeader' backend/src/api/ai/chat.ts` → must return >= 1

    REMED-04 verification:
    10. `SELECT array_length(embedding, 1) FROM ai_embeddings LIMIT 5` → must return `1024` for any rows present (or `null` if still empty — that is acceptable; the dimension-test write confirmed 1024 in plan 05)
    11. `grep -c 'normalizeEmbedding' supabase/functions/search-semantic/index.ts` → must return `0`

    REMED-05 verification (infrastructure — see Task 3 for human):
    12. Record from 68-06-SUMMARY: whether Phoenix and Langfuse containers started successfully.

    REMED-06 verification:
    13. `node scripts/check-i18n-namespaces.mjs` → must exit 0
    14. `grep -c 'check-i18n-namespaces' frontend/package.json` → must return >= 1

    Print all 14 results to stdout. All must meet their expected value.

  </action>
  <verify>
    <automated>
      grep -r 'supabaseAdmin' backend/src/ai/agents/ && echo FOUND || echo NOT_FOUND
      grep -c 'normalizeEmbedding' supabase/functions/search-semantic/index.ts
      node scripts/check-i18n-namespaces.mjs
    </automated>
  </verify>
  <acceptance_criteria>
    - All 14 verification checks produce the expected results as listed above
    - Any unexpected result is investigated and resolved (not skipped)
  </acceptance_criteria>
  <done>All 14 SQL + source assertions verified. Results recorded in SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 68 AI Foundations Remediation. Six REMED requirements addressed:
    - REMED-01: get_user_clearance_level() compat shim reads profiles.clearance_level (1-4 scale)
    - REMED-02: search_semantic_clearance_gated (SECURITY INVOKER) replaces old DEFINER RPC; HNSW index on ai_embeddings
    - REMED-03: supabaseAdmin retired from chat-assistant.ts; all reads under caller JWT; aa_commitments + engagement_dossiers repointed
    - REMED-04: normalizeEmbedding removed; ai_embeddings writes at native 1024-dim
    - REMED-05: Langfuse + Phoenix containers in docker-compose.prod.yml; OTel wired to Phoenix
    - REMED-06: check-i18n-namespaces.mjs guards Lint CI against unregistered namespaces
  </what-built>
  <how-to-verify>
    Perform live UAT on staging `zkrcjzdemdmwhearhfgg`. This verifies ROADMAP Phase 68 success criteria 1–5.

    SETUP: Ensure a level-1 clearance user account exists on staging. Seed at least one dossier with `sensitivity_level = 2` (above level-1 clearance). Note the dossier name/title.

    REMED-01 (clearance scale):
    - In Supabase dashboard, confirm `SELECT clearance_level FROM profiles WHERE user_id = <level-1-user-id>` returns `1`
    - Confirm `SELECT clearance_level FROM profiles WHERE clearance_level = 3` returns 5 rows

    REMED-02 (semantic search clearance block — success criterion #1):
    - Sign in as the level-1 user (clearance=1)
    - In the app's search, type the name of the sensitivity_level=2 dossier
    - Confirm: ZERO results for that dossier returned (it is above clearance)
    - Switch to Arabic mode (ع button in topbar), repeat the search in Arabic
    - Confirm: ZERO results in Arabic mode as well
    - The search may return other level-1 dossiers — that is correct
    - If the search shows NO results at all (because ai_embeddings is still empty), confirm the keyword fallback path also returns 0 above-clearance results

    REMED-03 (assistant clearance block — success criterion #2):
    - Still signed in as the level-1 user
    - Open the AI assistant / chat interface
    - Ask: "Tell me about [the above-clearance dossier name]"
    - Confirm: The assistant returns a generic "no information available" response (indistinguishable from "nothing found") — NOT an error message, NOT "access denied", NOT "filtered by clearance"
    - Switch to Arabic mode, repeat the question in Arabic
    - Confirm same indistinguishable-empty response in Arabic

    REMED-05 (observability trace — success criterion #5):
    - Send the assistant request (from REMED-03 above)
    - Access Phoenix via SSH tunnel (`ssh -L 6006:localhost:6006 root@138.197.195.242`) then http://localhost:6006
    - Confirm at least one trace appears showing the chat request flow (prompt → model → response)
    - Confirm no external network egress in Docker logs: `docker logs intl-dossier-phoenix 2>&1 | grep -i 'https://' | head -5` — should show nothing external

    REMED-06 (lint guard):
    - `pnpm --filter frontend run lint` exits 0 on the clean codebase (no unregistered namespaces)

    If all pass: type "approved"
    If any fail: describe the specific failure (which REMED, what was observed vs expected)

  </how-to-verify>
  <resume-signal>
    Type "approved" if all 5 success criteria are confirmed on staging (EN + AR), or describe failures.
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                                                      |
| --------------------- | ------------------------------------------------------------------------------------------------ |
| UAT user → staging DB | Level-1 JWT used to confirm clearance block; no service-role used during UAT                     |
| Staging seed data     | Above-clearance dossier seeded for test; must be cleaned up or documented as permanent test data |

## STRIDE Threat Register

| Threat ID     | Category               | Component                                        | Disposition | Mitigation Plan                                                                                 |
| ------------- | ---------------------- | ------------------------------------------------ | ----------- | ----------------------------------------------------------------------------------------------- |
| T-EoP-SR      | Elevation of Privilege | supabaseAdmin in chat-assistant.ts (was)         | mitigate    | REMED-03 plan 04 retired all service-role reads from agents dir; verified by grep + UAT         |
| T-EoP-DEFINER | Elevation of Privilege | search_entities_semantic (SECURITY DEFINER, was) | mitigate    | REMED-02 plan 03 replaced with INVOKER RPC; old DEFINER not called; verified by prosecdef=false |
| T-ID-EMPTY    | Information Disclosure | assistant leaks clearance-filtered existence     | mitigate    | D-09: indistinguishable empty confirmed by UAT (no "access denied" or "filtered" messaging)     |
| T-Tamper-DIM  | Tampering              | normalizeEmbedding pad/truncate (was)            | mitigate    | Removed in plans 03+05; array_length=1024 asserted                                              |
| T-EoP-AC      | Elevation of Privilege | clearance access control completeness            | mitigate    | REMED-01 canonical scale; REMED-02 INVOKER RPC; REMED-03 JWT client; all verified by SQL + UAT  |

</threat_model>

<verification>
1. `pnpm test` exits 0
2. `pnpm run lint` exits 0
3. `pnpm typecheck` exits 0
4. All 14 SQL + source assertions in Task 2 meet expected values
5. Human UAT: level-1 user sees zero above-clearance results via search AND assistant, in EN AND AR
6. Human UAT: Phoenix trace visible for one assistant request
</verification>

<success_criteria>
Phase 68 is DONE when:

- [ ] Full test suite passes (`pnpm test` exits 0)
- [ ] `SELECT prosecdef FROM pg_proc WHERE proname='search_semantic_clearance_gated'` = false (INVOKER confirmed)
- [ ] `grep -r 'supabaseAdmin' backend/src/ai/agents/` exits 1 (no match)
- [ ] `SELECT array_length(embedding, 1) FROM ai_embeddings LIMIT 5` = 1024 (or null if no rows — dimension test in plan 05 confirmed 1024)
- [ ] `node scripts/check-i18n-namespaces.mjs` exits 0
- [ ] Human UAT: level-1 user blocked from above-clearance dossiers via both search AND assistant (EN + AR)
- [ ] Human UAT: Phoenix/Langfuse trace visible for one assistant request, zero external egress
- [ ] Phase 68 git tag `phase-68-base` created on the final merge commit
      </success_criteria>

<output>
Create `.planning/phases/68-ai-foundations-remediation/68-08-SUMMARY.md` when done.
Include: all 14 SQL verification results verbatim; UAT pass/fail for each success criterion; any A1 fulltext remediation taken; final green-or-red summary for each REMED-01..06.
This SUMMARY is the phase gate record — downstream phases (69+) may reference it to confirm Phase 68 is complete.
</output>
