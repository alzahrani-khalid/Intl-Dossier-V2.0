---
phase: 68-ai-foundations-remediation
plan: 07
type: execute
wave: 4
depends_on:
  - 68-01
files_modified:
  - scripts/check-i18n-namespaces.mjs
  - frontend/package.json
autonomous: true
requirements:
  - REMED-06

must_haves:
  truths:
    - 'scripts/check-i18n-namespaces.mjs exits 1 when any useTranslation call references a namespace not in src/i18n/index.ts'
    - 'scripts/check-i18n-namespaces.mjs exits 0 on the current clean codebase'
    - "Both string form useTranslation('ns') and array form useTranslation(['ns1','ns2']) are checked"
    - 'The script is invoked as part of the frontend lint step in frontend/package.json'
    - 'pnpm run lint (frontend) fails when an unregistered namespace is added'
  artifacts:
    - path: 'scripts/check-i18n-namespaces.mjs'
      provides: 'namespace registry validator — exits 1 on unregistered, 0 on clean'
      contains: 'useTranslation'
    - path: 'frontend/package.json'
      provides: 'lint script extended to call check-i18n-namespaces.mjs'
      contains: 'check-i18n-namespaces'
  key_links:
    - from: 'scripts/check-i18n-namespaces.mjs'
      to: 'frontend/src/i18n/index.ts'
      via: 'reads resources.en top-level keys as registry'
      pattern: 'resources'
    - from: 'frontend/package.json lint script'
      to: 'scripts/check-i18n-namespaces.mjs'
      via: 'node scripts/check-i18n-namespaces.mjs'
      pattern: 'check-i18n-namespaces'
---

<objective>
REMED-06: Wire the `check-i18n-namespaces.mjs` script (created as a stub in plan 01) into the `Lint` CI job by appending it to the frontend `lint` script in `package.json`. Verify the guard catches both string-form and array-form `useTranslation` namespace references, and exits 0 on the current clean codebase.

Purpose: Closes the silent-English-fallback bug root cause. Once wired, any PR that adds a `useTranslation('unregistered-ns')` call without registering it in `src/i18n/index.ts` will fail the required `Lint` CI context and block merge.

Output: Modified `frontend/package.json` lint script; working end-to-end guard verified via red/green tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/68-ai-foundations-remediation/68-RESEARCH.md
@.planning/phases/68-ai-foundations-remediation/68-PATTERNS.md
@.planning/phases/68-ai-foundations-remediation/68-01-SUMMARY.md

<interfaces>
<!-- Extracted from codebase — executor reads these before modifying. -->

From frontend/package.json (current lint script, line ~17):

```json
"lint": "cd .. && eslint -c eslint.config.mjs --max-warnings 0 'frontend/src/**/*.{ts,tsx}'"
```

Append: ` && node scripts/check-i18n-namespaces.mjs`
(The `cd ..` at the start means the script runs from repo root; `scripts/check-i18n-namespaces.mjs` path is relative to repo root — correct.)

From .github/workflows/ci.yml (Lint job reference — confirms lint runs pnpm run lint):
The Lint job is: `pnpm --filter frontend run lint` (or equivalent). Folding into the existing lint script means NO changes to ci.yml are needed — D-12 stays at 8 required branch-protection contexts.

From scripts/check-i18n-namespaces.mjs (stub created in plan 01):
The stub exists. This plan's Task 1 makes any needed improvements to handle edge cases, then Task 2 wires it into the lint script.

From frontend/src/i18n/index.ts (namespace registry):
The script must parse the `resources` object's top-level en-keys as the registered namespace set. The import names (e.g., `import enCommon from './en/common.json'`) OR the object keys in `resources: { en: { 'common': enCommon, ... } }` provide the registry. Parse the object key form (more reliable).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden check-i18n-namespaces.mjs — edge cases and array-form coverage</name>
  <files>scripts/check-i18n-namespaces.mjs</files>
  <read_first>
    - scripts/check-i18n-namespaces.mjs (the stub from plan 01 — understand current state before modifying)
    - scripts/check-edge-fn-schema-refs.mjs (the structural analog to confirm Node.js ESM patterns: walkFiles, regex extraction, exit 1, error messages with file+line)
    - frontend/src/i18n/index.ts (read the actual resources object structure to confirm the registry parsing approach)
    - .planning/phases/68-ai-foundations-remediation/68-PATTERNS.md § `scripts/check-i18n-namespaces.mjs`
  </read_first>
  <behavior>
    - `node scripts/check-i18n-namespaces.mjs` exits 0 on the current clean codebase (all existing useTranslation calls in frontend/src reference registered namespaces)
    - When a file containing `useTranslation('nonexistent-p68-canary')` is temporarily added to frontend/src, the script exits 1 with an error message identifying the file and the unregistered namespace
    - When a file containing `useTranslation(['registered-ns', 'nonexistent-p68-canary'])` is temporarily added, the script also exits 1
    - The script reports ALL unregistered namespaces (not just the first one) so a developer can fix all in one pass
    - The script does NOT report false positives on any currently registered namespace
  </behavior>
  <action>
    Review the stub from plan 01. Harden it with these specific behaviors if not already present:

    1. REGISTRY PARSING: Read `frontend/src/i18n/index.ts` as text. Extract the `resources: { en: { ... } }` object's key names using a regex like `/['"]([a-z][a-z0-9-]+)['"]\s*:/g` applied to the section after `resources:`. These keys are the registered namespace names. Alternatively, extract from the `resources.en` object structure. The registry must be a `Set<string>`.

    2. FILE WALKING: Walk `frontend/src/**/*.{ts,tsx}` (exclude `frontend/src/i18n/` itself and `node_modules`). Use Node.js `fs.readdirSync` recursive pattern (same as `check-edge-fn-schema-refs.mjs`).

    3. STRING FORM: Regex `/useTranslation\(\s*['"]([^'"]+)['"]/g` — capture group 1 is the namespace string.

    4. ARRAY FORM: Regex `/useTranslation\(\s*\[([^\]]+)\]/g` — capture group 1 is the array content. Then extract each string literal from the group: `/['"]([^'"]+)['"]/g` applied to the group content.

    5. SKIP: `i18n.t(` calls (not `useTranslation`) are NOT namespace calls — do not false-positive on them.

    6. ERROR REPORT: For each unregistered namespace found, print: `[i18n-guard] ERROR: Unregistered namespace '<ns>' in <file>:<line>`. After reporting all, `process.exit(1)`.

    7. LINE NUMBER: For accurate line number in the error, track line index during string scanning (split content by `\n`, scan each line).

    The script must run in under 5 seconds on the current codebase (~100 files). No external deps — only Node.js builtins (`fs`, `path`, `url`). No `any` if TypeScript-typed; the script is `.mjs` (untyped ES module) so standard JSDoc comments for clarity.

  </action>
  <verify>
    <automated>
      node scripts/check-i18n-namespaces.mjs
    </automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-i18n-namespaces.mjs` exits 0 on clean codebase
    - When `echo "import { useTranslation } from 'react-i18next'\nuseTranslation('nonexistent-p68-canary')" > /tmp/p68-test-ns.tsx && cp /tmp/p68-test-ns.tsx frontend/src/p68-test-ns.tsx && node scripts/check-i18n-namespaces.mjs; rm frontend/src/p68-test-ns.tsx` — the script exits 1 (then file is cleaned up)
    - Array-form: when a file with `useTranslation(['common', 'nonexistent-p68-canary'])` is placed in frontend/src, script exits 1 and identifies 'nonexistent-p68-canary'
    - No false positives on known registered namespaces like 'common', 'dossier', 'ai-chat'
    - Script completes in < 10 seconds
  </acceptance_criteria>
  <done>check-i18n-namespaces.mjs handles both string and array-form useTranslation. Exits 0 on clean. Exits 1 with file+line errors on unregistered. No false positives on known namespaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire guard into frontend lint script + end-to-end CI verification</name>
  <files>frontend/package.json</files>
  <read_first>
    - frontend/package.json (read the full file — find the lint script to append to, confirm the cd .. prefix is present, confirm no other changes are needed)
    - .github/workflows/ci.yml (read the Lint job to confirm it runs pnpm run lint or equivalent — no changes to ci.yml should be needed)
  </read_first>
  <behavior>
    - `pnpm --filter frontend run lint` exits 1 when an unregistered namespace file is present in frontend/src
    - `pnpm --filter frontend run lint` exits 0 on the current clean codebase
    - ci.yml does NOT need to change (D-12: stays at 8 required contexts)
  </behavior>
  <action>
    In `frontend/package.json`, modify the `lint` script to append the namespace guard:

    BEFORE: `"lint": "cd .. && eslint -c eslint.config.mjs --max-warnings 0 'frontend/src/**/*.{ts,tsx}'"`
    AFTER: `"lint": "cd .. && eslint -c eslint.config.mjs --max-warnings 0 'frontend/src/**/*.{ts,tsx}' && node scripts/check-i18n-namespaces.mjs"`

    The `cd ..` means the command runs from repo root, so `scripts/check-i18n-namespaces.mjs` is correctly resolved.

    Then verify end-to-end:
    1. `pnpm --filter frontend run lint` — must exit 0 on clean codebase.
    2. Create a temp canary file: `echo "import { useTranslation } from 'react-i18next'\nexport const X = () => { useTranslation('p68-canary-unregistered'); return null }" > frontend/src/p68-lint-canary.tsx`
    3. `pnpm --filter frontend run lint` — must exit 1 (ESLint passes; namespace guard catches the unregistered ns)
    4. `rm frontend/src/p68-lint-canary.tsx`
    5. Confirm ci.yml Lint job command does NOT need to change (it already runs the frontend lint script).

  </action>
  <verify>
    <automated>
      pnpm --filter frontend run lint
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'check-i18n-namespaces' frontend/package.json` returns >= 1
    - `pnpm --filter frontend run lint` exits 0 on the current clean codebase
    - After adding the canary file and running lint again → exits 1 with namespace error message (then canary removed)
    - ci.yml is NOT modified (D-12: 8 branch-protection contexts preserved)
    - `grep -c 'check-i18n-namespaces' .github/workflows/ci.yml` returns 0 (not added to ci.yml — it runs via the lint script already)
  </acceptance_criteria>
  <done>Frontend lint script includes namespace guard. pnpm run lint exits 0 on clean codebase. Canary test confirms exits 1 on unregistered namespace. ci.yml unchanged (stays at 8 required contexts).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                       |
| --------------------------------------- | ----------------------------------------------------------------- |
| CI Lint job → check-i18n-namespaces.mjs | Script runs with repo read-only access; no secret access required |

## STRIDE Threat Register

| Threat ID  | Category  | Component                                  | Disposition | Mitigation Plan                                                                          |
| ---------- | --------- | ------------------------------------------ | ----------- | ---------------------------------------------------------------------------------------- |
| T-68-07-FP | Tampering | false positive blocks legitimate namespace | mitigate    | Script tested against full codebase exits 0; false positives investigated before merging |
| T-68-07-SC | Tampering | npm/pip installs                           | mitigate    | No new dependencies — script uses only Node.js built-ins                                 |

</threat_model>

<verification>
1. `grep -c 'check-i18n-namespaces' frontend/package.json` >= 1
2. `node scripts/check-i18n-namespaces.mjs` exits 0 (clean codebase)
3. Canary file test: exits 1 on unregistered namespace (array-form and string-form both caught)
4. `pnpm --filter frontend run lint` exits 0 on clean codebase
5. `grep -c 'check-i18n-namespaces' .github/workflows/ci.yml` returns 0 (not added to CI separately)
</verification>

<success_criteria>

- check-i18n-namespaces.mjs hardened for string-form and array-form useTranslation
- Script exits 0 on clean codebase and 1 on unregistered namespace with file+line error
- Frontend lint script calls the guard
- pnpm run lint fails on unregistered namespace (canary test passes)
- D-12 honored: ci.yml stays at 8 required contexts (no new context added)
  </success_criteria>

<output>
Create `.planning/phases/68-ai-foundations-remediation/68-07-SUMMARY.md` when done.
Note whether any false positives were found during development and how they were resolved.
</output>
