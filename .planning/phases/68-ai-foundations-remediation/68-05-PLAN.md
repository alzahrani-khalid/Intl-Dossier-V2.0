---
phase: 68-ai-foundations-remediation
plan: 05
type: execute
wave: 4
depends_on:
  - 68-02
  - 68-03
files_modified:
  - backend/src/ai/embeddings-service.ts
  - tests/integration/embedding-dimension.test.ts
autonomous: true
requirements:
  - REMED-04

must_haves:
  truths:
    - 'EmbeddingsService writes new embeddings to ai_embeddings (owner_type/owner_id pattern) at native 1024-dim'
    - 'No pad/truncate is applied to backend-generated embeddings when AI_EMBEDDING_DIMENSIONS=1024'
    - 'array_length(embedding, 1) = 1024 for any row inserted into ai_embeddings'
    - 'embedding-dimension test passes GREEN after a test write'
  artifacts:
    - path: 'backend/src/ai/embeddings-service.ts'
      provides: 'storeEmbedding method writing to ai_embeddings with native-1024 dimension'
      contains: 'ai_embeddings'
    - path: 'tests/integration/embedding-dimension.test.ts'
      provides: 'REMED-04 array_length=1024 assertion GREEN'
      contains: 'array_length'
  key_links:
    - from: 'backend/src/ai/embeddings-service.ts'
      to: 'ai_embeddings table'
      via: "supabase.from('ai_embeddings').insert({ owner_type, owner_id, embedding, model, ... })"
      pattern: 'ai_embeddings'
    - from: 'ai_embeddings.embedding'
      to: 'vector(1024)'
      via: 'AI_EMBEDDING_DIMENSIONS=1024 env var'
      pattern: 'embedding_dim'
---

<objective>
REMED-04 (write path): Confirm the backend `EmbeddingsService` correctly produces 1024-dim vectors from the local ONNX BGE-M3 embedder (no pad/truncate corruption), and writes new embeddings to `ai_embeddings` using the `owner_type`/`owner_id` pattern. Verify via test write that `array_length(embedding, 1) = 1024`.

Purpose: The search path (plan 03) now queries `ai_embeddings` via the clearance-gated RPC. Plan 05 ensures new content actually lands in `ai_embeddings` at the correct native dimension. Without this, the vector store stays empty and searches always fall through to keyword.

Output: EmbeddingsService extended with `storeEmbedding` write path; embedding-dimension test GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/68-ai-foundations-remediation/68-RESEARCH.md
@.planning/phases/68-ai-foundations-remediation/68-PATTERNS.md
@.planning/phases/68-ai-foundations-remediation/68-01-SUMMARY.md
@.planning/phases/68-ai-foundations-remediation/68-02-SUMMARY.md
@.planning/phases/68-ai-foundations-remediation/68-03-SUMMARY.md

<interfaces>
<!-- Extracted from codebase — executor reads these before modifying. -->

From backend/src/ai/embeddings-service.ts (key sections):

- `AI_EMBEDDING_DIMENSIONS` env var default: `parseInt(process.env.AI_EMBEDDING_DIMENSIONS || '1024', 10)` (from backend/src/ai/config.ts — confirmed)
- Lines 167–169: truncation logic `if (embedding.length > this.dimensions) embedding = embedding.slice(0, this.dimensions)` — this truncates but does NOT pad; at 1024-dim input and `this.dimensions=1024`, truncation is a no-op
- The service already has methods for generating embeddings; it lacks a `storeEmbedding(ownerId, ownerType, content)` write path to `ai_embeddings`

From ai_embeddings table schema (RF-4, confirmed live):

```sql
id              uuid (PK, DEFAULT gen_random_uuid())
owner_type      embedding_owner_type (enum)
owner_id        uuid
content_hash    bytea
embedding       vector(1024)
model           text
model_version   text
embedding_dim   integer
created_at      timestamptz
expires_at      timestamptz
```

From 68-01-SUMMARY.md A6 (embedding_owner_type enum values):
Read A6 result — the executor must use only confirmed enum values when writing owner_type to ai_embeddings.

Pattern for upsert-on-content-hash (avoid duplicate embeds):

```typescript
// Use content_hash for idempotency: same content → same hash → upsert
const contentHash = crypto.createHash('sha256').update(content).digest()
await supabase.from('ai_embeddings').upsert(
  {
    owner_type: ownerType,
    owner_id: ownerId,
    content_hash: contentHash,
    embedding,
    model: 'bge-m3',
    model_version: '1.0',
    embedding_dim: embedding.length,
  },
  { onConflict: 'owner_type,owner_id' }, // upsert key — adjust if ai_embeddings has unique constraint on this pair
)
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add storeEmbedding write path to EmbeddingsService</name>
  <files>backend/src/ai/embeddings-service.ts</files>
  <read_first>
    - backend/src/ai/embeddings-service.ts (read the FULL file — understand existing methods, the `this.dimensions` field, the ONNX embedder path, and the Supabase client already available in the class)
    - backend/src/ai/config.ts (confirm AI_EMBEDDING_DIMENSIONS default = 1024)
    - .planning/phases/68-ai-foundations-remediation/68-01-SUMMARY.md (A4: BGE-M3 native output dim confirmation; A6: embedding_owner_type enum values)
    - .planning/phases/68-ai-foundations-remediation/68-RESEARCH.md § REMED-04 (the write path recommendation + normalizeEmbedding context)
  </read_first>
  <behavior>
    - `grep -c 'ai_embeddings' backend/src/ai/embeddings-service.ts` returns >= 1
    - A call to `embeddingsService.storeEmbedding(ownerId, ownerType, content)` inserts a row into `ai_embeddings` with `embedding_dim = 1024`
    - Content hash check: calling `storeEmbedding` twice with the same content upserts (no duplicate rows)
    - TypeScript compiles without errors
  </behavior>
  <action>
    Add a `storeEmbedding(ownerId: string, ownerType: string, content: string): Promise<void>` method to `EmbeddingsService`. The method must:

    1. Generate the embedding using the existing `embedText(content)` method (or equivalent internal call) — do NOT call normalizeEmbedding; use the raw output.
    2. Assert the dimension: `if (embedding.length !== 1024) throw new Error('Expected 1024-dim embedding, got ${embedding.length}')` — this makes bad dimensions fail loudly instead of silently corrupting the store.
    3. Compute SHA-256 content hash using Node.js `crypto.createHash('sha256').update(content).digest()`.
    4. Upsert to `ai_embeddings` using the Supabase client already in the class. Use the exact column names from the `<interfaces>` block. Set `embedding_dim: embedding.length` (= 1024).
    5. Use `owner_type` values only from the confirmed A6 enum — if a passed `ownerType` is not in the enum, the DB constraint will catch it; the method should propagate the error up (no swallowing).

    For the upsert conflict target, query `SELECT indexname FROM pg_indexes WHERE tablename = 'ai_embeddings'` via the existing Supabase client if needed to determine the right ON CONFLICT key — OR use a simpler `insert` with `ignoreDuplicates: true` if the table has no unique constraint on the pair.

    Per project style: no semicolons, single quotes, 2-space indent, explicit return types. No `any`. Error handling via `if (error) throw new Error(...)` pattern consistent with the file.

  </action>
  <verify>
    <automated>
      grep -c 'ai_embeddings' backend/src/ai/embeddings-service.ts
      pnpm --filter backend typecheck 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ai_embeddings' backend/src/ai/embeddings-service.ts` returns >= 1
    - `grep -c 'storeEmbedding' backend/src/ai/embeddings-service.ts` returns >= 1 (method exists)
    - `grep -c 'embedding.length !== 1024' backend/src/ai/embeddings-service.ts` returns >= 1 (dimension assertion present)
    - `pnpm --filter backend typecheck` exits 0
    - The method signature has explicit return type `Promise<void>`
  </acceptance_criteria>
  <done>storeEmbedding method added. Writes to ai_embeddings. Dimension assertion at 1024. Content hash for idempotency. TypeScript clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update embedding-dimension integration test + verify GREEN on staging</name>
  <files>tests/integration/embedding-dimension.test.ts</files>
  <read_first>
    - tests/integration/embedding-dimension.test.ts (the stub from plan 01 — understand current state)
    - backend/src/ai/embeddings-service.ts (the storeEmbedding method just written)
    - .planning/phases/68-ai-foundations-remediation/68-01-SUMMARY.md (A6: valid owner_type enum values to use in test write)
    - .planning/phases/68-ai-foundations-remediation/68-VALIDATION.md § Per-Task Verification Map REMED-04 row
  </read_first>
  <behavior>
    - Test writes a row to ai_embeddings (using a test owner_id that gets cleaned up)
    - `SELECT array_length(embedding, 1) FROM ai_embeddings WHERE owner_id = '<test-uuid>'` returns 1024
    - Test cleans up the test row after assertion (DELETE WHERE owner_id = '<test-uuid>')
    - `pnpm --filter backend test --run tests/integration/embedding-dimension.test.ts` exits 0
  </behavior>
  <action>
    Update `tests/integration/embedding-dimension.test.ts` to add a real behavioral test:

    1. Call `embeddingsService.storeEmbedding(testOwnerId, validOwnerType, 'test content for embedding dimension check')` where `testOwnerId` is a known UUID (e.g., `'00000000-0000-0000-0000-000000000099'`) and `validOwnerType` is a value confirmed in A6 from 68-01-SUMMARY.md.

    2. After the call, query staging via supabaseAdmin: `SELECT array_length(embedding, 1) AS dim FROM ai_embeddings WHERE owner_id = '00000000-0000-0000-0000-000000000099'`.

    3. Assert `dim === 1024`.

    4. In `afterEach`/`afterAll`, DELETE the test row: `DELETE FROM ai_embeddings WHERE owner_id = '00000000-0000-0000-0000-000000000099'`.

    Gate the full test with `if (!SUPABASE_URL || !SUPABASE_SERVICE_ROLE_KEY) { it.skip(...); return }` like the rls-audit test pattern.

    Also assert (via file read, not DB call) that `backend/src/ai/embeddings-service.ts` does NOT import `normalizeEmbedding` from any module — the pad/truncate function should not exist anywhere in the backend agents or embeddings-service.

    Per project style: no semicolons, single quotes, explicit return types.

  </action>
  <verify>
    <automated>pnpm --filter backend test --run tests/integration/embedding-dimension.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter backend test --run tests/integration/embedding-dimension.test.ts` exits 0
    - The test includes a SQL assertion `array_length(embedding, 1) = 1024`
    - Test row is cleaned up after assertion (no leftover test data in staging)
    - `grep -r 'normalizeEmbedding' backend/src/` exits 1 (normalizeEmbedding removed from all backend src files too)
  </acceptance_criteria>
  <done>embedding-dimension test GREEN. array_length=1024 verified on a real write to staging ai_embeddings. Test data cleaned up. normalizeEmbedding confirmed absent from backend/src.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                                                            |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| EmbeddingsService → ai_embeddings | Write uses existing Supabase client (service-role for backend writes — correct; no user data, only content embeddings) |
| embedding dimension               | Asserted at write time; vector(1024) column rejects wrong dimensions at DB level                                       |

## STRIDE Threat Register

| Threat ID    | Category  | Component                                            | Disposition | Mitigation Plan                                                                                                                              |
| ------------ | --------- | ---------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| T-Tamper-DIM | Tampering | normalizeEmbedding pads/truncates embedding geometry | mitigate    | Removed from edge fn (plan 03); backend truncation is a no-op at 1024-dim; explicit dimension assertion in storeEmbedding throws on mismatch |
| T-68-05-OT   | Tampering | wrong owner_type value fails enum CHECK              | accept      | DB constraint handles it; propagate error; test uses confirmed A6 enum value                                                                 |
| T-68-05-SC   | Tampering | npm/pip installs                                     | mitigate    | No new packages — uses existing @xenova/transformers and crypto (Node built-in)                                                              |

</threat_model>

<verification>
1. `grep -c 'ai_embeddings' backend/src/ai/embeddings-service.ts` >= 1
2. `grep -c 'embedding.length !== 1024' backend/src/ai/embeddings-service.ts` >= 1
3. `pnpm --filter backend test --run tests/integration/embedding-dimension.test.ts` exits 0
4. Supabase MCP: `SELECT array_length(embedding, 1) FROM ai_embeddings LIMIT 5` returns 1024 (after test write)
5. `grep -r 'normalizeEmbedding' backend/src/` exits 1
</verification>

<success_criteria>

- storeEmbedding method exists on EmbeddingsService
- New writes land in ai_embeddings at vector(1024), never 1536
- array_length=1024 verified via SQL on a test write
- normalizeEmbedding absent from all backend src
- embedding-dimension test GREEN
  </success_criteria>

<output>
Create `.planning/phases/68-ai-foundations-remediation/68-05-SUMMARY.md` when done.
Include the `SELECT array_length(embedding, 1) FROM ai_embeddings LIMIT 5` result from staging.
</output>
