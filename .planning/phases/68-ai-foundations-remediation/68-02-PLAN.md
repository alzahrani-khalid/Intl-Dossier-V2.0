---
phase: 68-ai-foundations-remediation
plan: 02
type: execute
wave: 2
depends_on:
  - 68-01
files_modified:
  - supabase/migrations/20260614000001_p68_clearance_canonical.sql
  - tests/security/rls-audit.test.ts
autonomous: true
requirements:
  - REMED-01

user_setup:
  - service: supabase-mcp
    why: 'Apply migration to staging zkrcjzdemdmwhearhfgg'
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: 'Already configured in MCP server'

must_haves:
  truths:
    - 'get_user_clearance_level() reads profiles.clearance_level (1-4 scale) after migration'
    - 'All 5 existing RLS policies that call get_user_clearance_level() continue working unchanged'
    - 'profiles.clearance_level backfill does not downgrade the 5 users manually set to level 3'
    - 'A level-1 user cannot SELECT a sensitivity_level=2 dossier via JWT-scoped client'
    - 'The migration is applied to staging and the REMED-01 rls-audit tests pass GREEN'
  artifacts:
    - path: 'supabase/migrations/20260614000001_p68_clearance_canonical.sql'
      provides: 'backfill + get_user_clearance_level() compat shim'
      contains: 'get_user_clearance_level'
    - path: 'tests/security/rls-audit.test.ts'
      provides: 'REMED-01 clearance assertions now GREEN'
      contains: 'REMED-01'
  key_links:
    - from: 'supabase/migrations/20260614000001_p68_clearance_canonical.sql'
      to: 'profiles.clearance_level'
      via: 'UPDATE backfill + CREATE OR REPLACE FUNCTION'
      pattern: 'clearance_level'
    - from: 'get_user_clearance_level'
      to: 'profiles.clearance_level'
      via: 'SELECT COALESCE(clearance_level, 1)'
      pattern: 'profiles.*user_id'
---

<objective>
REMED-01: Establish the single canonical clearance scale. Rewrite `get_user_clearance_level()` to read `profiles.clearance_level` (1–4 scale) instead of role-derived 1–3. Backfill `profiles.clearance_level` from role-derived mapping without downgrading manually-set values. Apply migration to staging. Verify the 5 RLS policies that call the function keep working.

Purpose: This is the foundation that every other REMED in this phase depends on. Without a canonical clearance scale, clearance-gated search (plan 03) and assistant RLS (plan 04) cannot be verified.

Output: One forward migration file; REMED-01 rls-audit tests passing GREEN on staging.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/68-ai-foundations-remediation/68-RESEARCH.md
@.planning/phases/68-ai-foundations-remediation/68-PATTERNS.md
@.planning/phases/68-ai-foundations-remediation/68-01-SUMMARY.md

<interfaces>
<!-- Extracted from source files the executor must know before touching anything. -->

From supabase/migrations/20250930001_helper_functions.sql (lines 42–55, the function being replaced):

```sql
CREATE OR REPLACE FUNCTION get_user_clearance_level(user_id UUID)
RETURNS INTEGER AS $$
DECLARE
  clearance INTEGER;
BEGIN
  SELECT CASE ur.role
    WHEN 'admin' THEN 3
    WHEN 'manager' THEN 3
    WHEN 'analyst' THEN 2
    ELSE 1
  END INTO clearance
  FROM user_roles ur
  WHERE ur.user_id = get_user_clearance_level.user_id
  ORDER BY clearance DESC LIMIT 1;
  RETURN COALESCE(clearance, 1);
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

From supabase/migrations/20251022000009_update_polymorphic_refs.sql (canonical clearance pattern, lines 100–103):

```sql
sensitivity_level <= (SELECT clearance_level FROM profiles WHERE id = auth.uid())
```

Note: profiles PK column is `user_id` (confirmed RF-3), not `id`. New policies use `WHERE user_id = auth.uid()`.

Live staging facts (from 68-01-SUMMARY.md — confirmed RF-3):

- profiles.clearance_level: 388 at level 1, 5 at level 3
- 5 RLS policies call get_user_clearance_level(): work_item_dossiers_select, view_wg_decisions_by_dossier_access, view_wg_deliverables_by_dossier_access, view_wg_meetings_by_dossier_access, view_wg_members_by_dossier_access
- Backfill guard: WHERE clearance_level = 1 — must NOT touch the 5 profiles at level 3
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write migration 20260614000001_p68_clearance_canonical.sql</name>
  <files>supabase/migrations/20260614000001_p68_clearance_canonical.sql</files>
  <read_first>
    - supabase/migrations/20250930001_helper_functions.sql (lines 42–62 — the function being replaced; read the FULL body to understand what to replace)
    - supabase/migrations/20251022000009_update_polymorphic_refs.sql (lines 95–125 — canonical clearance comparison pattern to confirm profiles.user_id column name)
    - .planning/phases/68-ai-foundations-remediation/68-PATTERNS.md § `supabase/migrations/YYYYMMDD_p68_clearance_canonical.sql` (the exact target SQL to write)
    - .planning/phases/68-ai-foundations-remediation/68-01-SUMMARY.md (A3 live fact: profiles column name confirmation)
  </read_first>
  <action>
    Create `supabase/migrations/20260614000001_p68_clearance_canonical.sql` with exactly two statements in order:

    STEP 1 — Backfill. UPDATE profiles.clearance_level from role-derived mapping using MAX() to pick highest role-clearance per user. The WHERE guard `p.clearance_level = 1` is LOAD-BEARING — it prevents downgrading the 5 profiles already at level 3. Do NOT omit the WHERE clause. Mapping: admin→3, manager→3, analyst→2, else→1.

    STEP 2 — Compat shim. CREATE OR REPLACE FUNCTION get_user_clearance_level(user_id UUID) RETURNS INTEGER LANGUAGE SQL SECURITY DEFINER STABLE. Body: `SELECT COALESCE(clearance_level, 1) FROM profiles WHERE profiles.user_id = get_user_clearance_level.user_id`. Add COMMENT ON FUNCTION describing the P68 purpose.

    The migration file format must match the repo pattern: UTF-8, LF line endings, leading `-- P68:` section comments. The filename timestamp `20260614000001` must sort after all existing migrations.

    Do NOT add BEGIN/COMMIT (Supabase MCP wraps in a transaction). Do NOT add DROP FUNCTION before CREATE OR REPLACE.

  </action>
  <verify>
    <automated>cat supabase/migrations/20260614000001_p68_clearance_canonical.sql | grep -c 'get_user_clearance_level'</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `supabase/migrations/20260614000001_p68_clearance_canonical.sql`
    - `grep -c 'clearance_level = 1' supabase/migrations/20260614000001_p68_clearance_canonical.sql` returns `>= 1` (the WHERE guard is present)
    - `grep -c 'SECURITY DEFINER' supabase/migrations/20260614000001_p68_clearance_canonical.sql` returns `1` (shim keeps SECURITY DEFINER — it reads from profiles, not a user-data table)
    - `grep -c 'STABLE' supabase/migrations/20260614000001_p68_clearance_canonical.sql` returns `1`
    - File does NOT contain `SECURITY INVOKER` (the shim is intentionally DEFINER for stability; only new RETRIEVAL RPCs are INVOKER per D-05/D-08)
  </acceptance_criteria>
  <done>Migration file written. Backfill SQL with WHERE guard present. Compat shim with SECURITY DEFINER STABLE present. COMMENT ON FUNCTION present.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply migration to staging + verify REMED-01 tests GREEN</name>
  <files>tests/security/rls-audit.test.ts</files>
  <read_first>
    - supabase/migrations/20260614000001_p68_clearance_canonical.sql (the migration just written — confirm it is correct before applying)
    - tests/security/rls-audit.test.ts (the REMED-01 block added in plan 01 — these should go GREEN after apply)
    - .planning/phases/68-ai-foundations-remediation/68-01-SUMMARY.md (A1–A6 facts, especially staging project ID `zkrcjzdemdmwhearhfgg`)
  </read_first>
  <action>
    Apply the migration using Supabase MCP `apply_migration` to project `zkrcjzdemdmwhearhfgg`. Pass the SQL content of `20260614000001_p68_clearance_canonical.sql` as the migration body.

    After applying, run these verification SQL queries via Supabase MCP `execute_sql`:

    1. `SELECT get_user_clearance_level('00000000-0000-0000-0000-000000000000')` — must return `1` (nonexistent user, COALESCE default)
    2. `SELECT COUNT(*) FROM profiles WHERE clearance_level = 3` — must return `5` (the 5 manually-set profiles must NOT have been downgraded)
    3. `SELECT COUNT(*) FROM profiles WHERE clearance_level > 1` — must return `>= 5` (some level-2+ profiles now exist after backfill, including any analysts)
    4. `SELECT prosecdef, prokind FROM pg_proc WHERE proname = 'get_user_clearance_level'` — prosecdef must be `true` (SECURITY DEFINER; STABLE function kind)

    Then run the backend test suite to flip REMED-01 tests GREEN:
    `pnpm --filter backend test --run tests/security/rls-audit.test.ts`

    If the REMED-01 tests still fail after apply (e.g., due to JWT test-user setup not present), annotate the specific failure reason in the SUMMARY — do not silently skip.

  </action>
  <verify>
    <automated>pnpm --filter backend test --run tests/security/rls-audit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Supabase MCP `apply_migration` call succeeds (no error)
    - `SELECT get_user_clearance_level('00000000-0000-0000-0000-000000000000')` returns `1`
    - `SELECT COUNT(*) FROM profiles WHERE clearance_level = 3` returns exactly `5` (no downgrade occurred)
    - `SELECT prosecdef FROM pg_proc WHERE proname = 'get_user_clearance_level'` returns `true`
    - `pnpm --filter backend test --run tests/security/rls-audit.test.ts` exits 0 for the REMED-01 clearance tests (shim-behavior assertion passes; the JWT-scoped level-1 isolation test may remain skipped/pending if a test JWT user is not pre-seeded — document in SUMMARY)
  </acceptance_criteria>
  <done>Migration applied to staging. Shim verified via SQL. Backfill confirmed (5 level-3 profiles untouched). rls-audit REMED-01 tests GREEN or documented as pending-JWT-setup.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                             |
| ------------------------------------- | --------------------------------------------------------------------------------------- |
| Migration writer → staging DB         | Migration applied via Supabase MCP with project-scoped access token                     |
| get_user_clearance_level() → profiles | SECURITY DEFINER — acceptable for this helper (reads profiles, not user-content tables) |

## STRIDE Threat Register

| Threat ID | Category               | Component             | Disposition | Mitigation Plan                                                                                                                    |
| --------- | ---------------------- | --------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| T-EoP-AC  | Elevation of Privilege | clearance backfill    | mitigate    | WHERE clearance_level = 1 guard prevents widening manually-set values; post-migrate COUNT(\*) WHERE clearance_level=3 must equal 5 |
| T-68-02-B | Tampering              | migration content     | mitigate    | Migration is reviewed before apply; MCP apply is transactional (rolls back on error)                                               |
| T-68-02-C | Elevation of Privilege | shim SECURITY DEFINER | accept      | Shim reads profiles (not sensitive content tables); STABLE + read-only subquery; same posture as prior impl                        |

</threat_model>

<verification>
1. `grep -c 'WHERE clearance_level = 1' supabase/migrations/20260614000001_p68_clearance_canonical.sql` → >= 1 (backfill guard present)
2. Supabase MCP: `SELECT get_user_clearance_level('00000000-0000-0000-0000-000000000000')` → 1
3. Supabase MCP: `SELECT COUNT(*) FROM profiles WHERE clearance_level = 3` → 5 (no downgrade)
4. `pnpm --filter backend test --run tests/security/rls-audit.test.ts` → passes REMED-01 block
</verification>

<success_criteria>

- Forward migration exists and is committed
- get_user_clearance_level() reads profiles.clearance_level on live staging
- 5 existing RLS policies calling the function continue working (no 500s on working-group queries)
- REMED-01 rls-audit tests GREEN or explicitly documented as pending JWT setup
- No profiles at level 3 were downgraded (COUNT = 5 confirmed)
  </success_criteria>

<output>
Create `.planning/phases/68-ai-foundations-remediation/68-02-SUMMARY.md` when done.
Include the four SQL verification results verbatim.
</output>
