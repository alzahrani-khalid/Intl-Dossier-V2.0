---
phase: 68-ai-foundations-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/security/rls-audit.test.ts
  - tests/integration/search-clearance.test.ts
  - tests/integration/chat-assistant-rls.test.ts
  - tests/integration/embedding-dimension.test.ts
  - scripts/check-i18n-namespaces.mjs
autonomous: true
requirements:
  - REMED-01
  - REMED-02
  - REMED-03
  - REMED-04
  - REMED-06

must_haves:
  truths:
    - 'A1–A6 live-DB facts are confirmed in writing before any implementation task runs'
    - 'All five Wave-0 test stubs exist (or are extended) and fail in RED state before code changes'
    - 'The i18n guard script exits 1 on an unregistered namespace call before the lint integration is wired'
  artifacts:
    - path: 'tests/security/rls-audit.test.ts'
      provides: 'REMED-01 canonical-scale clearance assertion + level-1-cannot-see-level-2 test'
      contains: 'get_user_clearance_level'
    - path: 'tests/integration/search-clearance.test.ts'
      provides: 'REMED-02 SECURITY INVOKER RPC assertion (RED — function does not exist yet)'
      exports: []
    - path: 'tests/integration/chat-assistant-rls.test.ts'
      provides: 'REMED-03 no-supabaseAdmin grep assertion (RED — admin still present)'
      exports: []
    - path: 'tests/integration/embedding-dimension.test.ts'
      provides: 'REMED-04 array_length=1024 assertion (RED — table empty)'
      exports: []
    - path: 'scripts/check-i18n-namespaces.mjs'
      provides: 'REMED-06 namespace registry check script'
      contains: 'useTranslation'
  key_links:
    - from: 'scripts/check-i18n-namespaces.mjs'
      to: 'frontend/src/i18n/index.ts'
      via: 'reads resources keys'
      pattern: 'resources'
    - from: 'tests/security/rls-audit.test.ts'
      to: 'profiles.clearance_level'
      via: 'supabaseAdmin.rpc(get_user_clearance_level)'
      pattern: 'get_user_clearance_level'
---

<objective>
Wave-0: Establish the test stubs and live-DB introspection ground-truth that every implementation task in this phase depends on.

Purpose: Ensure every REMED requirement has a RED failing test before code ships; close Assumptions A1–A6 so implementors have confirmed facts rather than guesses. This plan gates all implementation waves.

Output: Five test/script files (one extended, four new); console-printed A1–A6 DB facts for executor record-keeping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/68-ai-foundations-remediation/68-RESEARCH.md
@.planning/phases/68-ai-foundations-remediation/68-VALIDATION.md
@.planning/phases/68-ai-foundations-remediation/68-PATTERNS.md

<interfaces>
<!-- Key existing patterns the executor needs — extracted from codebase. -->

From tests/security/rls-audit.test.ts (existing sensitiveTables array to extend, lines 35–41):

```typescript
const sensitiveTables = ['persons', 'intelligence_event', 'intelligence_digest', 'dashboard_digest']
// Existing client pattern (lines 49–52):
supabaseAdmin = createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, {
  auth: { autoRefreshToken: false, persistSession: false },
})
```

From scripts/check-edge-fn-schema-refs.mjs (analog for the new i18n script):

```javascript
import fs from 'node:fs'
import path from 'node:path'
import { fileURLToPath } from 'node:url'
const scriptPath = fileURLToPath(import.meta.url)
const repoRoot = path.resolve(path.dirname(scriptPath), '..')
// walk .ts/.tsx, regex-extract literals, compare to registry, exit 1 on miss
```

From frontend/src/i18n/index.ts (namespace registry — ~110 namespaces):
The `resources` object has `en: { 'common': ..., 'dossier': ..., ... }` — the top-level keys of `resources.en` are the registered namespaces.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-DB A1–A6 introspection (record facts, gate assumptions)</name>
  <files>No files written — DB queries only; print results to stdout for executor record.</files>
  <read_first>
    - .planning/phases/68-ai-foundations-remediation/68-PATTERNS.md § "Wave-0 Verifications" table
    - .planning/phases/68-ai-foundations-remediation/68-RESEARCH.md § "Assumptions Log"
  </read_first>
  <action>
    Run the following six SQL queries against staging project `zkrcjzdemdmwhearhfgg` via Supabase MCP (`execute_sql`) and print each result. Do NOT guess or skip — these results determine implementation choices in later plans.

    A1 — fulltext RPC security mode:
    `SELECT proname, prosecdef FROM pg_proc WHERE proname = 'search_entities_fulltext'`
    Expected: `prosecdef = false` (INVOKER). If `true`, flag for plan 03 to also replace this RPC.

    A2 — aa_commitments column list:
    `SELECT column_name, data_type FROM information_schema.columns WHERE table_name = 'aa_commitments' ORDER BY ordinal_position`
    Record which columns exist — plan 04 query select list depends on this.

    A3 — engagement_dossiers column list:
    `SELECT column_name, data_type FROM information_schema.columns WHERE table_name = 'engagement_dossiers' ORDER BY ordinal_position`
    Record columns for plan 04.

    A4 — BGE-M3 ONNX output dimension (code assertion, not DB):
    Read `backend/src/ai/embeddings-service.ts` lines 160–175 and confirm what `embeds.data[0].embedding` dimension is before truncation. Note whether `AI_EMBEDDING_DIMENSIONS` env default is 1024 (from `backend/src/ai/config.ts`).

    A5 — pgvector and halfvec availability (already confirmed HIGH confidence but reconfirm):
    `SELECT extversion FROM pg_extension WHERE extname = 'vector'`
    Expected: `0.8.0` (confirmed by research; confirm again).

    A6 — embedding_owner_type enum values:
    `SELECT enum_range(NULL::embedding_owner_type)`
    Record all enum values — plan 03/04 writes to `ai_embeddings.owner_type` depend on this.

    Print each result with its assumption label (A1–A6) so the SUMMARY captures them for downstream plans.

  </action>
  <verify>
    <automated>All six Supabase MCP `execute_sql` calls succeed (no error response). Results are printed to stdout.</automated>
  </verify>
  <done>Six DB/code fact results printed. A1 fulltext RPC security mode confirmed. A2/A3 column lists recorded. A4 embedding dim source confirmed. A5 pgvector version confirmed. A6 enum values listed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave-0 test stubs — rls-audit extension + four new integration test files</name>
  <files>
    tests/security/rls-audit.test.ts,
    tests/integration/search-clearance.test.ts,
    tests/integration/chat-assistant-rls.test.ts,
    tests/integration/embedding-dimension.test.ts,
    scripts/check-i18n-namespaces.mjs
  </files>
  <read_first>
    - tests/security/rls-audit.test.ts (read existing sensitiveTables array and client pattern — extend, not replace)
    - scripts/check-edge-fn-schema-refs.mjs (analog for check-i18n-namespaces.mjs structure)
    - frontend/src/i18n/index.ts (namespace registry — parse the resources.en top-level keys)
    - .planning/phases/68-ai-foundations-remediation/68-PATTERNS.md § rls-audit.test.ts and § check-i18n-namespaces.mjs
    - .planning/phases/68-ai-foundations-remediation/68-VALIDATION.md § Wave 0 Requirements
  </read_first>
  <behavior>
    - rls-audit.test.ts REMED-01 block: `get_user_clearance_level('00000000-...')` → returns 1 (RED until migration applied, because current impl reads user_roles and returns null→1 or might throw; RED state is: the shim is not yet installed so behavior is the OLD impl)
    - rls-audit.test.ts REMED-01 block: JWT-scoped level-1 user SELECT on sensitivity_level=2 dossier → 0 rows (RED until clearance RLS is fixed)
    - search-clearance.test.ts: `SELECT prosecdef FROM pg_proc WHERE proname = 'search_semantic_clearance_gated'` → function does not exist yet (RED)
    - chat-assistant-rls.test.ts: `grep -r 'supabaseAdmin' backend/src/ai/agents/` → exits 0 (finds the import, meaning supabaseAdmin IS still present = RED for "should not exist" assertion)
    - embedding-dimension.test.ts: `SELECT count(*) FROM ai_embeddings` → 0 rows (stub passes in GREEN but the behavior test for array_length=1024 is RED until a write is made)
    - check-i18n-namespaces.mjs: `node scripts/check-i18n-namespaces.mjs` → exits 0 on current codebase (no unregistered namespaces outstanding); exits 1 when called with a file containing `useTranslation('unregistered-test-ns-12345')`
  </behavior>
  <action>
    1. Extend `tests/security/rls-audit.test.ts`: add a `describe('REMED-01: Canonical clearance scale')` block with two `it()` tests (shim behavior + level-1 cannot see level-2 dossier). Follow the existing `createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, { auth: { autoRefreshToken:false, persistSession:false } })` pattern at file top. Both tests should be environment-gated with `if (!SUPABASE_URL || !SUPABASE_SERVICE_ROLE_KEY) return` like the existing suite to avoid failures in non-Supabase CI.

    2. Create `tests/integration/search-clearance.test.ts`: A Vitest file that queries `pg_proc` via supabaseAdmin for `search_semantic_clearance_gated` and asserts `prosecdef = false`. Starts RED (function does not exist). Add a `todo` test for the live-UAT clearance-block assertion (mark as `it.todo`).

    3. Create `tests/integration/chat-assistant-rls.test.ts`: A Vitest file that executes `grep -r 'supabaseAdmin' backend/src/ai/agents/` via Node.js `child_process.execSync`. Asserts exit code is non-zero (i.e., grep finds NO supabaseAdmin — which is RED now since it IS present). Also assert the `ChatRequest` interface has an `authHeader` field (check by reading the file; use `fs.readFileSync`).

    4. Create `tests/integration/embedding-dimension.test.ts`: A Vitest file that queries `SELECT count(*) FROM ai_embeddings` (empty = 0) and `SELECT array_length(embedding, 1) FROM ai_embeddings LIMIT 1` (returns null when empty — stub passes; after plan 03 land, a test-write assertion can fill this). Gate with SUPABASE_URL env check.

    5. Create `scripts/check-i18n-namespaces.mjs`: Node.js ESM script. Follow the `check-edge-fn-schema-refs.mjs` structure: read `frontend/src/i18n/index.ts` and extract the top-level keys of the `resources.en` object (these are the registered namespaces). Walk all `.ts`/`.tsx` files in `frontend/src/`. Extract `useTranslation('ns')` (string form, regex: `/useTranslation\(\s*['"]([^'"]+)['"]/g`) AND `useTranslation(['ns1', 'ns2'])` (array form, regex: `/useTranslation\(\s*\[([^\]]+)\]/g`, then split on commas and strip quotes). For each namespace found, check it exists in the registry. Exit 1 with a descriptive error listing all unregistered namespaces and their source file + line. Exit 0 if all registered.

    Per project style: no semicolons, single quotes, 2-space indent, explicit return types on TypeScript functions, no `any`.

  </action>
  <verify>
    <automated>
      pnpm --filter backend test --run tests/security/rls-audit.test.ts
      pnpm --filter backend test --run tests/integration/search-clearance.test.ts
      pnpm --filter backend test --run tests/integration/chat-assistant-rls.test.ts
      node scripts/check-i18n-namespaces.mjs
    </automated>
  </verify>
  <acceptance_criteria>
    - `rls-audit.test.ts` compiles and runs (new REMED-01 block present; tests may fail in RED — that is expected pre-migration)
    - `tests/integration/search-clearance.test.ts` exists; the `prosecdef` assertion FAILS (function not yet created — RED state confirmed)
    - `tests/integration/chat-assistant-rls.test.ts` exists; the grep assertion FAILS (supabaseAdmin found — RED state confirmed)
    - `node scripts/check-i18n-namespaces.mjs` exits 0 on the current codebase (all existing useTranslation calls reference registered namespaces)
    - When a temporary file with `useTranslation('this-ns-does-not-exist-p68-test')` is placed in `frontend/src/`, `node scripts/check-i18n-namespaces.mjs` exits 1 — confirm by manual test and remove the temp file afterward
    - `pnpm --filter backend test --run` completes without compilation errors on the four new/modified test files (failures in RED state are acceptable; compile errors are not)
  </acceptance_criteria>
  <done>
    All five files exist. rls-audit extension is in place. Four integration test files start in RED state (correct). check-i18n-namespaces.mjs exits 0 on clean codebase and exits 1 on unregistered namespace. Executor has printed the A1–A6 DB facts from Task 1.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                                                      |
| ----------------------- | -------------------------------------------------------------------------------- |
| Test suite → staging DB | Test credentials (SUPABASE_SERVICE_ROLE_KEY) must be env-gated; never hard-coded |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                   | Disposition | Mitigation Plan                                                    |
| ---------- | ---------------------- | ------------------------------------------- | ----------- | ------------------------------------------------------------------ |
| T-68-01-SC | Tampering              | npm/pip installs                            | mitigate    | No new dependencies in this plan — scripts use Node built-ins only |
| T-68-01-AC | Elevation of Privilege | rls-audit test client uses service-role key | accept      | Test-only use; env-gated; mirrors existing test suite pattern      |

</threat_model>

<verification>
After completing both tasks:
1. `pnpm --filter backend test --run` — runs without compile errors (RED test failures are expected and correct)
2. `node scripts/check-i18n-namespaces.mjs` — exits 0 (clean codebase check)
3. A1–A6 fact results are printed and captured in SUMMARY for downstream plans
</verification>

<success_criteria>

- All five Wave-0 files exist with correct stub behavior
- A1–A6 DB assumptions are confirmed and recorded in plan SUMMARY
- No compile errors in any new file
- RED test failures confirmed where expected (search-clearance, chat-assistant-rls)
- i18n guard exits 0 on clean codebase and 1 on unregistered namespace
  </success_criteria>

<output>
Create `.planning/phases/68-ai-foundations-remediation/68-01-SUMMARY.md` when done.
Include the A1–A6 live-DB fact results verbatim in the SUMMARY — downstream plans depend on them.
</output>
