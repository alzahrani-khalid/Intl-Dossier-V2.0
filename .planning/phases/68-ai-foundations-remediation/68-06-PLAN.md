---
phase: 68-ai-foundations-remediation
plan: 06
type: execute
wave: 4
depends_on:
  - 68-02
files_modified:
  - deploy/docker-compose.prod.yml
  - backend/src/ai/mastra-config.ts
  - deploy/.env.example
autonomous: false
requirements:
  - REMED-05

user_setup:
  - service: langfuse
    why: 'Langfuse requires a Postgres database and secret keys for its own persistence'
    env_vars:
      - name: LANGFUSE_DATABASE_URL
        source: "Create a separate Postgres database for Langfuse (can reuse the droplet's postgres with a new DB, or use a separate Supabase project). Format: postgresql://user:password@host:5432/langfuse"
      - name: LANGFUSE_SECRET
        source: 'Generate a random 32+ char string: `openssl rand -hex 32`'
      - name: LANGFUSE_SALT
        source: 'Generate a random 32+ char string: `openssl rand -hex 32`'
      - name: OTEL_EXPORTER_OTLP_ENDPOINT
        source: 'Set to http://phoenix:4317 (within Docker network — no external host)'
  - service: phoenix
    why: 'Arize Phoenix requires no external account — runs fully self-hosted in Docker'
    env_vars: []

must_haves:
  truths:
    - 'Langfuse container is defined in docker-compose.prod.yml and in the intl-dossier Docker network'
    - 'Phoenix container is defined in docker-compose.prod.yml and in the intl-dossier Docker network'
    - 'Phoenix OTLP gRPC port 4317 is exposed within the network (not to external)'
    - 'mastra-config.ts passes telemetry.exporter to Mastra constructor pointed at phoenix:4317'
    - 'Zero egress: OTEL_EXPORTER_OTLP_ENDPOINT is an internal Docker network hostname'
    - 'Langfuse and Phoenix are NOT on ports: blocks for external access — use expose: not ports:'
  artifacts:
    - path: 'deploy/docker-compose.prod.yml'
      provides: 'langfuse + phoenix service blocks'
      contains: 'langfuse'
    - path: 'backend/src/ai/mastra-config.ts'
      provides: 'OTel telemetry config wired to Phoenix OTLP endpoint'
      contains: 'OTEL_EXPORTER_OTLP_ENDPOINT'
    - path: 'deploy/.env.example'
      provides: 'new env var templates for Langfuse and OTel'
      contains: 'LANGFUSE_DATABASE_URL'
  key_links:
    - from: 'backend/src/ai/mastra-config.ts'
      to: 'phoenix container:4317'
      via: 'OTLPTraceExporter({ url: process.env.OTEL_EXPORTER_OTLP_ENDPOINT })'
      pattern: 'OTEL_EXPORTER_OTLP_ENDPOINT'
    - from: 'deploy/docker-compose.prod.yml'
      to: 'intl-dossier network'
      via: 'networks: - intl-dossier'
      pattern: 'intl-dossier'
---

<objective>
REMED-05: Add Langfuse and Arize Phoenix containers to `docker-compose.prod.yml` in the `intl-dossier` Docker network with zero external egress. Wire Mastra's OTel telemetry config to export traces to Phoenix via OTLP gRPC on `http://phoenix:4317`.

Purpose: Enables an operator to trace a complete AI request (prompt → model → response) end-to-end in self-hosted observability. The phase success criterion #5 requires a visual trace in Langfuse or Phoenix after one chat request.

Output: Modified docker-compose.prod.yml, modified mastra-config.ts, updated .env.example. Human checkpoint to verify container health and one trace captured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/68-ai-foundations-remediation/68-RESEARCH.md
@.planning/phases/68-ai-foundations-remediation/68-PATTERNS.md
@.planning/phases/68-ai-foundations-remediation/68-02-SUMMARY.md

<interfaces>
<!-- Extracted from deploy/docker-compose.prod.yml and backend/src/ai/mastra-config.ts — executor reads these first. -->

From deploy/docker-compose.prod.yml (existing anythingllm service block pattern to replicate, lines ~131–165):

```yaml
anythingllm:
  image: mintplexlabs/anythingllm:latest
  container_name: intl-dossier-anythingllm
  restart: unless-stopped
  expose:
    - '3001'
  volumes:
    - anythingllm_data:/app/server/storage
  environment: ...
  healthcheck:
    test: ['CMD', 'curl', '-f', 'http://localhost:3001/api/health']
    interval: 30s
    timeout: 10s
  networks:
    - intl-dossier
```

Key rule: use `expose:` (within-network only), NOT `ports:` (which binds to host) for internal-only services.

From backend/src/ai/mastra-config.ts (current initializeMastra, lines 39–46):

```typescript
private initializeMastra(): void {
  try {
    this.mastra = new Mastra({})
    logger.info('Mastra initialized successfully')
  } catch (error) {
    logger.error('Failed to initialize Mastra', { error })
  }
}
```

Mastra telemetry config (from PATTERNS.md § mastra-config.ts):

```typescript
import { OTLPTraceExporter } from '@opentelemetry/exporter-trace-otlp-grpc'
const otelExporter = new OTLPTraceExporter({
  url: process.env.OTEL_EXPORTER_OTLP_ENDPOINT ?? 'http://phoenix:4317',
})
this.mastra = new Mastra({
  telemetry: {
    serviceName: 'intl-dossier-ai',
    exporter: otelExporter,
  },
})
// Fallback: if OTel init fails, create Mastra without telemetry (don't block startup)
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Langfuse + Phoenix to docker-compose.prod.yml and update .env.example</name>
  <files>deploy/docker-compose.prod.yml, deploy/.env.example</files>
  <read_first>
    - deploy/docker-compose.prod.yml (read the FULL file — find the services section, the `networks` section at the bottom confirming `intl-dossier` network name, and the existing healthcheck patterns to replicate)
    - .planning/phases/68-ai-foundations-remediation/68-PATTERNS.md § `deploy/docker-compose.prod.yml`
    - deploy/.env.example (read to understand existing env var naming conventions)
  </read_first>
  <action>
    In `deploy/docker-compose.prod.yml`, add two new service blocks at the end of the `services:` section (before `networks:`):

    SERVICE 1 — langfuse:
    - image: `langfuse/langfuse:latest`
    - container_name: `intl-dossier-langfuse`
    - restart: `unless-stopped`
    - expose: `'3000'` (internal only — not ports:)
    - environment: `DATABASE_URL: ${LANGFUSE_DATABASE_URL}`, `NEXTAUTH_URL: http://langfuse:3000`, `NEXTAUTH_SECRET: ${LANGFUSE_SECRET}`, `SALT: ${LANGFUSE_SALT}`
    - healthcheck: `test: ['CMD', 'wget', '--no-verbose', '--tries=1', '--spider', 'http://localhost:3000/api/public/health']`, `interval: 30s`, `timeout: 10s`, `retries: 3`
    - networks: `- intl-dossier`

    SERVICE 2 — phoenix:
    - image: `arizephoenix/phoenix:latest`
    - container_name: `intl-dossier-phoenix`
    - restart: `unless-stopped`
    - expose: `'6006'` (UI), `'4317'` (OTLP gRPC — internal to Docker network only; backend connects via `http://phoenix:4317`)
    - No environment block needed (Phoenix is configuration-free)
    - healthcheck: `test: ['CMD', 'wget', '--no-verbose', '--tries=1', '--spider', 'http://localhost:6006']`, `interval: 30s`, `timeout: 10s`, `retries: 3`
    - networks: `- intl-dossier`

    CRITICAL zero-egress constraint: Do NOT use `ports:` for Phoenix port 4317. The OTLP receiver must be reachable only within the Docker network from the backend container. Port 6006 (Phoenix UI) is also expose-only; access via SSH tunnel or nginx proxy if needed.

    In `deploy/.env.example`, add template lines:
    ```
    # REMED-05 Observability (Langfuse + Phoenix)
    LANGFUSE_DATABASE_URL=postgresql://postgres:password@host:5432/langfuse
    LANGFUSE_SECRET=<generate-with-openssl-rand-hex-32>
    LANGFUSE_SALT=<generate-with-openssl-rand-hex-32>
    OTEL_EXPORTER_OTLP_ENDPOINT=http://phoenix:4317
    ```

    Do NOT add actual secret values. Template only.

  </action>
  <verify>
    <automated>
      grep -c 'intl-dossier-langfuse' deploy/docker-compose.prod.yml
      grep -c 'intl-dossier-phoenix' deploy/docker-compose.prod.yml
      grep -c 'LANGFUSE_DATABASE_URL' deploy/.env.example
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'intl-dossier-langfuse' deploy/docker-compose.prod.yml` returns 1
    - `grep -c 'intl-dossier-phoenix' deploy/docker-compose.prod.yml` returns 1
    - `grep -c "ports:" deploy/docker-compose.prod.yml` should NOT have new ports: entries for langfuse or phoenix (use expose: only)
    - `grep -c 'LANGFUSE_DATABASE_URL' deploy/.env.example` returns >= 1
    - `grep -c 'OTEL_EXPORTER_OTLP_ENDPOINT' deploy/.env.example` returns >= 1
    - `grep -c 'intl-dossier' deploy/docker-compose.prod.yml` returns the same count as before PLUS 2 (for the two new network references in langfuse and phoenix blocks)
  </acceptance_criteria>
  <done>Langfuse and Phoenix service blocks added with expose-only ports, healthchecks, and intl-dossier network. .env.example updated with template vars.</done>
</task>

<task type="auto">
  <name>Task 2: Wire OTel telemetry in mastra-config.ts</name>
  <files>backend/src/ai/mastra-config.ts</files>
  <read_first>
    - backend/src/ai/mastra-config.ts (read the FULL file — find the initializeMastra method, the existing Mastra constructor call, and any existing imports at the top)
    - backend/package.json (check whether @opentelemetry/exporter-trace-otlp-grpc and @opentelemetry/sdk-node are already installed, or need to be added)
    - .planning/phases/68-ai-foundations-remediation/68-PATTERNS.md § `backend/src/ai/mastra-config.ts`
  </read_first>
  <action>
    Modify `backend/src/ai/mastra-config.ts`:

    CHANGE 1 — Add import: `import { OTLPTraceExporter } from '@opentelemetry/exporter-trace-otlp-grpc'`. If this package is not in `backend/package.json`, add it via `pnpm --filter backend add @opentelemetry/exporter-trace-otlp-grpc`.

    CHANGE 2 — Extend `initializeMastra()` to construct an `OTLPTraceExporter` pointed at `process.env.OTEL_EXPORTER_OTLP_ENDPOINT ?? 'http://phoenix:4317'` and pass it to `new Mastra({ telemetry: { serviceName: 'intl-dossier-ai', exporter: otelExporter } })`.

    CHANGE 3 — Wrap in try/catch: if OTel exporter construction fails (e.g., package missing or Phoenix not reachable at startup), fall back to `new Mastra({})` with a logger.warn. This prevents OTel misconfiguration from blocking backend startup.

    Verify the `@mastra/core` telemetry config key by reading `backend/package.json` for the `@mastra/core` version, then confirm the `telemetry` config key exists for that version. If the version is < 0.4.0 or the `telemetry` key is unavailable, use a `NodeSDK` wrapper instead and document the deviation in the SUMMARY.

    Per project style: no semicolons, single quotes, 2-space indent, explicit return types, no `any`.

  </action>
  <verify>
    <automated>
      grep -c 'OTEL_EXPORTER_OTLP_ENDPOINT' backend/src/ai/mastra-config.ts
      pnpm --filter backend typecheck 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'OTEL_EXPORTER_OTLP_ENDPOINT' backend/src/ai/mastra-config.ts` returns >= 1
    - `grep -c 'OTLPTraceExporter' backend/src/ai/mastra-config.ts` returns >= 1
    - `grep -c 'intl-dossier-ai' backend/src/ai/mastra-config.ts` returns >= 1 (serviceName present)
    - `grep -c 'phoenix:4317' backend/src/ai/mastra-config.ts` returns >= 1 (default OTLP endpoint)
    - `pnpm --filter backend typecheck` exits 0
    - The try/catch fallback is present: `this.mastra = new Mastra({})` appears as a fallback in the catch block
  </acceptance_criteria>
  <done>OTel telemetry wired in mastra-config.ts. OTLPTraceExporter pointed at phoenix:4317. Try/catch fallback to no-telemetry on startup failure. TypeScript clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Langfuse and Phoenix container blocks added to docker-compose.prod.yml. OTel telemetry wired in mastra-config.ts pointing to phoenix:4317. Both containers should come up when docker compose is run on the droplet.
  </what-built>
  <how-to-verify>
    On the DigitalOcean droplet (138.197.195.242):

    1. Pull latest changes: `git pull`
    2. Set required env vars in `/opt/intl-dossier/.env`:
       - `LANGFUSE_DATABASE_URL` — a Postgres connection string for Langfuse's own DB
       - `LANGFUSE_SECRET` — random 32+ char string
       - `LANGFUSE_SALT` — random 32+ char string
       - `OTEL_EXPORTER_OTLP_ENDPOINT=http://phoenix:4317`
    3. Start containers: `cd /opt/intl-dossier/deploy && docker compose -f docker-compose.prod.yml up -d phoenix langfuse`
    4. Wait 30 seconds, then check:
       - Phoenix health: `docker exec intl-dossier-phoenix wget -q -O- http://localhost:6006` (should return HTML)
       - Langfuse health: `docker exec intl-dossier-langfuse wget -q -O- http://localhost:3000/api/public/health` (should return `{"status":"ok"}`)
    5. Send one chat request to the backend API
    6. SSH-tunnel to Phoenix: `ssh -L 6006:localhost:6006 root@138.197.195.242` then open http://localhost:6006 in browser
    7. Confirm at least one trace appears in Phoenix showing the chat request path
    8. Confirm no external network calls from Phoenix/Langfuse to outside the Docker network (check `docker logs intl-dossier-phoenix` for any external URLs)

  </how-to-verify>
  <resume-signal>Type "approved" when Phoenix shows a trace from the chat request, or describe issues encountered.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                                             |
| ------------------------ | ----------------------------------------------------------------------- |
| Backend → Phoenix (OTLP) | Internal Docker network only (expose: not ports:)                       |
| Backend → Langfuse       | Internal Docker network only                                            |
| Phoenix UI → operator    | SSH tunnel or nginx proxy required — never directly exposed to internet |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                      | Disposition | Mitigation Plan                                                                                                                                                                 |
| ---------- | ---------------------- | ---------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-68-06-EG | Information Disclosure | OTel traces sent to external endpoint          | mitigate    | OTEL_EXPORTER_OTLP_ENDPOINT set to `http://phoenix:4317` (internal Docker DNS); no external OTLP URL configured                                                                 |
| T-68-06-LC | Information Disclosure | Langfuse UI exposed to internet                | mitigate    | `expose:` not `ports:` — Langfuse port 3000 only reachable within Docker network; operator accesses via SSH tunnel                                                              |
| T-68-06-SC | Tampering              | Langfuse/Phoenix Docker images from Docker Hub | mitigate    | Images are official (`langfuse/langfuse`, `arizephoenix/phoenix`); pinning to `:latest` is acceptable for P68 (operator can pin to specific digest if required in later phases) |

</threat_model>

<verification>
1. `grep -c 'intl-dossier-langfuse' deploy/docker-compose.prod.yml` >= 1
2. `grep -c 'intl-dossier-phoenix' deploy/docker-compose.prod.yml` >= 1
3. `grep -c 'OTEL_EXPORTER_OTLP_ENDPOINT' backend/src/ai/mastra-config.ts` >= 1
4. `pnpm --filter backend typecheck` exits 0
5. Human checkpoint: Phoenix shows trace after one chat request (manual verify required for success criterion #5)
</verification>

<success_criteria>

- Langfuse and Phoenix service blocks in docker-compose.prod.yml using expose: (not ports:) in intl-dossier network
- mastra-config.ts passes OTel telemetry to Mastra constructor
- deploy/.env.example has LANGFUSE\_\* and OTEL_EXPORTER_OTLP_ENDPOINT templates
- Human confirms Phoenix trace visible after one chat request on staging
- Zero external OTLP egress confirmed
  </success_criteria>

<output>
Create `.planning/phases/68-ai-foundations-remediation/68-06-SUMMARY.md` when done.
Note the Mastra @mastra/core version found and whether the telemetry config key was available natively.
</output>
