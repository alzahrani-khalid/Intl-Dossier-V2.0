---
phase: 65-engagement-positions-tab-legacy-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/_protected/engagements/$engagementId/positions.tsx
  - frontend/src/routeTree.gen.ts
  - frontend/src/components/workspace/WorkspaceTabNav.tsx
  - frontend/src/i18n/en/workspace.json
  - frontend/src/i18n/ar/workspace.json
  - frontend/src/components/positions/__tests__/EngagementPositionsTab.test.tsx
autonomous: true
requirements: [ENGPOS-01, ENGPOS-02]

key_decisions:
  - 'ENGPOS-01: canonical source = position_dossier_links keyed by the engagement dossiers.id (engagementId IS a dossiers.id); legacy engagement_positions (0 rows, write-deny RLS, no migration provenance) and the engagements-positions-* edges are declared legacy/deprecated — never read or written by new code'
  - 'Tab body = DossierPositionsTab reused verbatim with dossierId={engagementId} (countries-route precedent, route-file-direct render); attach-existing keeps default link_type related_to, create-new keeps applies_to (Phase 64 D-09 posture carried — no CONTEXT.md overrides exist)'
  - 'Plan 01 is the single owner of ALL workspace.json edits this phase (new tabs.positions + calendar.* reader keys + UI-SPEC copy updates) so wave-1 plans stay file-disjoint'

must_haves:
  truths:
    - 'The route /_protected/engagements/$engagementId/positions exists in the regenerated routeTree.gen.ts and renders DossierPositionsTab with dossierId = engagementId'
    - 'WorkspaceTabNav shows a Positions tab between Context and Tasks; the label resolves from tabs.positions in BOTH en and ar workspace bundles (no silent English fallback in AR — Pitfall 7)'
    - 'The ENGPOS-01 canonical-source decision is recorded in the route-file header comment and this plan frontmatter (roadmap success criterion 1: decision recorded and implemented)'
    - "The tab's onAttach persists each selected position via createPositionDossierLink(positionId, { dossier_id: engagementId }), invalidates ['dossier-position-links', engagementId] by prefix, and shows an honest partial-failure toast — unit-proven against an engagement id"
    - 'All new/updated workspace.json keys from the approved UI-SPEC copy contract land in en AND ar in the same commit'
  artifacts:
    - path: 'frontend/src/routes/_protected/engagements/$engagementId/positions.tsx'
      provides: 'Routed lazy Positions tab (ENGPOS-01 mount point) with decision header comment'
      contains: 'ENGPOS-01'
      min_lines: 25
    - path: 'frontend/src/components/workspace/WorkspaceTabNav.tsx'
      provides: 'WORKSPACE_TABS positions entry'
      contains: "labelKey: 'tabs.positions'"
    - path: 'frontend/src/i18n/ar/workspace.json'
      provides: 'AR tab label + calendar reader keys + copy updates'
      contains: 'المواقف'
    - path: 'frontend/src/components/positions/__tests__/EngagementPositionsTab.test.tsx'
      provides: 'ENGPOS-01/-02 unit rows from 65-VALIDATION.md'
      min_lines: 60
  key_links:
    - from: 'frontend/src/components/workspace/WorkspaceTabNav.tsx'
      to: 'frontend/src/routes/_protected/engagements/$engagementId/positions.tsx'
      via: "WORKSPACE_TABS path 'positions' → Link to /engagements/$engagementId/positions"
      pattern: "key: 'positions'"
    - from: 'frontend/src/routes/_protected/engagements/$engagementId/positions.tsx'
      to: 'frontend/src/components/positions/DossierPositionsTab.tsx'
      via: 'lazy import + dossierId={engagementId}'
      pattern: 'DossierPositionsTab'
    - from: 'frontend/src/components/positions/DossierPositionsTab.tsx'
      to: 'position_dossier_links (staging)'
      via: 'useDossierPositionLinks direct PostgREST SELECT under clearance RLS'
      pattern: 'dossier-position-links'
---

<objective>
Mount the engagement workspace Positions tab on the canonical data plane: a new routed lazy tab that reuses `DossierPositionsTab` with `dossierId = engagementId`, one `WORKSPACE_TABS` nav entry, and the phase's full bilingual i18n key set. Record the ENGPOS-01 canonical-source decision (position_dossier_links, NOT legacy engagement_positions) in the route-file header.

Purpose: ENGPOS-01 requires a routed Positions tab on the decided canonical source with the decision recorded; ENGPOS-02's persist/render/invalidate contract ships inside the reused component and is pinned here by unit tests against an engagement id.
Output: route file + regenerated routeTree.gen.ts + nav entry + en/ar workspace.json keys + the Wave-0 test file from 65-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-RESEARCH.md
@.planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-UI-SPEC.md

<interfaces>
<!-- Route file — verbatim target (research §Code Examples, countries-route analog). -->

```tsx
// frontend/src/routes/_protected/engagements/$engagementId/positions.tsx
// ENGPOS-01 decision: canonical positions source = position_dossier_links with
// dossier_id = engagementId (engagementId IS a dossiers.id). Legacy
// engagement_positions is deprecated — never read or write it.
import { createFileRoute } from '@tanstack/react-router'
import type { ReactElement } from 'react'
import { lazy, Suspense } from 'react'
import { TabSkeleton } from '@/components/workspace/TabSkeleton'

const DossierPositionsTab = lazy(() =>
  import('@/components/positions/DossierPositionsTab').then((m) => ({
    default: m.DossierPositionsTab,
  })),
)

export const Route = createFileRoute('/_protected/engagements/$engagementId/positions')({
  component: EngagementPositionsRoute,
})

function EngagementPositionsRoute(): ReactElement {
  const { engagementId } = Route.useParams()
  return (
    <Suspense fallback={<TabSkeleton type="list" />}>
      <DossierPositionsTab dossierId={engagementId} />
    </Suspense>
  )
}
```

<!-- Nav entry — WorkspaceTabNav.tsx WORKSPACE_TABS (verified L26-33). Insert after context (UI-SPEC A-1). -->

```typescript
{ key: 'positions', labelKey: 'tabs.positions', path: 'positions' },
```

<!-- i18n contract — ALL workspace.json edits this phase (UI-SPEC Copywriting Contract, binding).
     NEW keys (both files):
       tabs.positions             EN "Positions"            AR "المواقف"
       calendar.scheduledEvents   EN "Scheduled events"     AR "الأحداث المجدولة"
       calendar.entriesEmpty      EN "No scheduled events yet. Use Add event to schedule one."
                                  AR "لا توجد أحداث مجدولة بعد. استخدم إضافة حدث لجدولة حدث جديد."
       calendar.entriesError      EN "Failed to load scheduled events. Check your connection and try again."
                                  AR "فشل تحميل الأحداث المجدولة. تحقق من اتصالك وحاول مرة أخرى."
     UPDATED keys (sentence case + drop "+" prefix; consumers all touched this phase):
       actions.createTask         EN "Create Task" → "Create task"          AR unchanged "إنشاء مهمة"
       actions.addEvent           EN "Add Event" → "Add event"              AR unchanged "إضافة حدث"
       empty.tasks.action         EN "+ Create Task" → "Create task"        AR "+ إنشاء مهمة" → "إنشاء مهمة"
       empty.calendar.action      EN "+ Add Event" → "Add event"            AR "+ إضافة حدث" → "إضافة حدث"
       empty.tasks.body           EN → "Create a task for this engagement to start tracking work. New tasks appear in the main tasks list."
                                  AR → "أنشئ مهمة لهذا الارتباط لبدء تتبع العمل. تظهر المهام الجديدة في قائمة المهام الرئيسية."
       empty.context.body         EN → "Linked dossiers appear here when a host country, organizations, or participants are added to the engagement."
                                  AR → "تظهر الملفات المرتبطة هنا عند إضافة الدولة المضيفة أو المنظمات أو المشاركين إلى الارتباط."
       empty.docs.body            EN → "Generate an AI briefing to get started."
                                  AR → "أنشئ ملخصًا بالذكاء الاصطناعي للبدء."
     Do NOT remove the now-orphaned keys (actions.transitionStage, actions.linkDossier,
     empty.context.action, docs.upload) — UI-SPEC marks the sweep optional/harmless and
     wave-1 plans must stay decoupled. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the Wave-0 unit tests (EngagementPositionsTab.test.tsx) — RED first</name>
  <files>frontend/src/components/positions/__tests__/EngagementPositionsTab.test.tsx</files>
  <read_first>
    - frontend/src/components/positions/__tests__/NewPositionDialog.test.tsx (the Phase 64 test-pattern analog: module mocks with mutable state, colon-form t mock, decision-tagged test names)
    - frontend/src/components/positions/DossierPositionsTab.tsx (the onAttach handler L230-269; hooks consumed: useDossierPositionLinks, useDossier; AttachPositionDialog mounting)
    - frontend/src/components/workspace/WorkspaceTabNav.tsx (WORKSPACE_TABS shape, Link/useMatchRoute usage — to plan the router module mock)
  </read_first>
  <behavior>
    - Test (ENGPOS-01): WorkspaceTabNav renders a positions tab link targeting /engagements/$engagementId/positions, placed between context and tasks. Mock @tanstack/react-router (Link → anchor exposing its `to` prop, useMatchRoute → () => false) per the NewPositionDialog mock style. RED until the entry exists.
    - Test (ENGPOS-01): i18n key parity — load frontend/src/i18n/en/workspace.json and ar/workspace.json (direct JSON import; resolveJsonModule is on) and assert BOTH contain tabs.positions, calendar.scheduledEvents, calendar.entriesEmpty, calendar.entriesError with non-empty values, and that the AR tabs.positions value is 'المواقف'. RED until Task 2.
    - Test (ENGPOS-02): rendering DossierPositionsTab with dossierId = a fixed engagement-shaped uuid calls useDossierPositionLinks with that id (mock the hook, capture args).
    - Test (ENGPOS-02): the onAttach contract — mock '@/components/positions/AttachPositionDialog' to capture the onAttach prop; mock createPositionDossierLink from the positions repository. Invoke onAttach with two position ids where one mock call rejects: assert createPositionDossierLink called twice with { dossier_id: engagementId }, queryClient.invalidateQueries called with queryKey ['dossier-position-links', engagementId], and the error (partial-failure) toast fired. Repeat all-success: success toast fired. These pin existing component behavior against an engagement id (GREEN immediately — that is the point: regression pin).
  </behavior>
  <action>
    Create the test file mirroring NewPositionDialog.test.tsx structure (vi.mock react-i18next with colon-form keys, vi.mock useDirection, mutable mock state objects, decision-tagged test names like "renders positions nav entry (ENGPOS-01)"). Mock useDossier to return a minimal dossier row ({ id, type: 'engagement', name_en, name_ar }) so the WR-03 guard resolves. Wrap renders in a QueryClientProvider with a spy-able QueryClient. Run the file: the two ENGPOS-01 tests MUST fail (nav entry and i18n keys absent), the ENGPOS-02 tests MUST pass. Commit: test(65-01): add failing tests for engagement positions tab mount + attach contract.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/components/positions/__tests__/EngagementPositionsTab.test.tsx 2>&1 | grep -qE 'failed' && echo RED_CONFIRMED</automated>
  </verify>
  <done>Test file exists covering all four ENGPOS-01/-02 unit rows from 65-VALIDATION.md; ENGPOS-01 cases are RED, ENGPOS-02 attach-contract cases GREEN; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Route file + nav entry + i18n keys + route-tree regeneration — GREEN</name>
  <files>frontend/src/routes/_protected/engagements/$engagementId/positions.tsx, frontend/src/routeTree.gen.ts, frontend/src/components/workspace/WorkspaceTabNav.tsx, frontend/src/i18n/en/workspace.json, frontend/src/i18n/ar/workspace.json</files>
  <read_first>
    - frontend/src/routes/_protected/dossiers/countries/$id/positions.tsx (whole-file analog — copy/adapt exactly)
    - frontend/src/routes/_protected/engagements/$engagementId/overview.tsx (sibling route shape, param name engagementId)
    - .planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-UI-SPEC.md (§Copywriting Contract — the binding key/value table mirrored in the interfaces block above)
  </read_first>
  <action>
    1. Create the route file exactly as the interfaces block (including the ENGPOS-01 decision header comment — it is the recorded decision per RESEARCH §Canonical Source Decision). Route id '/_protected/engagements/$engagementId/positions', param engagementId, lazy DossierPositionsTab with Suspense + TabSkeleton type="list".
    2. Add the WORKSPACE_TABS entry { key: 'positions', labelKey: 'tabs.positions', path: 'positions' } in WorkspaceTabNav.tsx immediately after the context entry (UI-SPEC placement A-1). No other nav changes — styling/RTL/scroll-snap are inherited.
    3. Apply the FULL i18n contract from the interfaces block to BOTH frontend/src/i18n/en/workspace.json and ar/workspace.json in this same commit (Pitfall 7: a missing AR key silently falls back to English). Add the new calendar.* keys now even though their consumer ships in plan 65-05 — plan 01 is the single owner of workspace.json this phase. Preserve existing JSON formatting (lint-staged prettifies).
    4. Regenerate the route tree: run cd frontend && pnpm build (the TanStackRouterVite plugin writes src/routeTree.gen.ts — Pitfall 1: type-check fails against a stale tree). Stage routeTree.gen.ts IN THE SAME COMMIT as the route file.
    5. Run the Task 1 test file (all tests now GREEN) and pnpm type-check. Commit: feat(65-01): mount engagement Positions tab on position_dossier_links (ENGPOS-01).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/components/positions/__tests__/EngagementPositionsTab.test.tsx && pnpm type-check && grep -F 'engagements/$engagementId/positions' src/routeTree.gen.ts >/dev/null && grep -q 'ENGPOS-01' 'src/routes/_protected/engagements/$engagementId/positions.tsx' && python3 -c "import json; en=json.load(open('src/i18n/en/workspace.json')); ar=json.load(open('src/i18n/ar/workspace.json')); assert en['tabs']['positions']=='Positions' and ar['tabs']['positions']=='المواقف'; assert all(k in en['calendar'] and k in ar['calendar'] for k in ['scheduledEvents','entriesEmpty','entriesError']); print('I18N_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - routeTree.gen.ts contains the new route id and is committed together with the route file
    - The route file carries the ENGPOS-01 decision header comment
    - WORKSPACE_TABS has the positions entry between context and tasks
    - en + ar workspace.json both carry every new/updated key from the UI-SPEC table; orphaned keys NOT removed
    - Test file fully green; pnpm type-check exits 0
  </acceptance_criteria>
  <done>The engagement workspace has a routed, navigable Positions tab on the canonical plane, decision recorded, bilingual, unit-pinned.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                       |
| --------------------------------------- | ----------------------------------------------------------------- |
| browser → position_dossier_links (read) | the tab reads via direct PostgREST; RLS SELECT policy is the gate |
| browser → positions-dossiers-create     | attach writes cross the edge + clearance INSERT RLS               |

## STRIDE Threat Register

| Threat ID | Category               | Component                        | Disposition | Mitigation Plan                                                                                                                                                          |
| --------- | ---------------------- | -------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-65-01   | Information Disclosure | useDossierPositionLinks reader   | mitigate    | Reuse the existing clearance-gated SELECT policy (migration 20260610000002) — no service-role client in frontend code; no RLS changes in this plan                       |
| T-65-02   | Tampering (user trust) | new tab rendering canonical data | mitigate    | Tab reuses the live-verified Phase 64 component verbatim; unit tests pin reader id + attach/invalidate contract; live pass in 65-06 closes the loop                      |
| T-65-03   | Elevation of Privilege | legacy engagement_positions      | mitigate    | Decision header + plan decision forbid reading/writing the legacy table; NO INSERT-policy widening anywhere this phase (the drift class is fixed by NOT using the table) |
| T-65-SC   | Tampering              | npm/pip/cargo installs           | accept      | No package installs in this plan                                                                                                                                         |

</threat_model>

<verification>
- 65-VALIDATION.md rows realized: "route renders DossierPositionsTab with dossierId = engagementId" + "tabs.positions key parity" + "onAttach persists/invalidates/partial-toast" → Tasks 1-2
- Per-task gate: quick vitest run + pnpm type-check (sampling contract)
</verification>

<success_criteria>

- Routed Positions tab live in the workspace nav, reading position_dossier_links via the engagement's dossiers.id
- ENGPOS-01 decision recorded in code + frontmatter (roadmap criterion 1 "recorded and implemented")
- All phase workspace.json keys bilingual in one commit
  </success_criteria>

<output>
Create `.planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-01-SUMMARY.md` when done (record the ENGPOS-01 decision text verbatim for the phase SUMMARY roll-up).
</output>
