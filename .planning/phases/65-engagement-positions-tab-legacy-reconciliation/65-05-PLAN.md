---
phase: 65-engagement-positions-tab-legacy-reconciliation
plan: 05
type: execute
wave: 2
depends_on: [65-01, 65-04]
files_modified:
  - frontend/src/hooks/useEngagementCalendarEntries.ts
  - frontend/src/pages/engagements/workspace/CalendarTab.tsx
  - frontend/src/pages/engagements/workspace/__tests__/CalendarTabCtas.test.tsx
autonomous: true
requirements: [ENGPOS-03]

key_decisions:
  - 'Add Event CTAs (#6/#7) are RE-ENABLED together with the Scheduled-events in-tab reader — never without it (UI-SPEC §2 row 6: re-enabling without a render path is the R15-02 bug class in a new costume; the calendar-create v11 → calendar_entries.dossier_id write path removed the historic TODO blocker)'
  - "Reader = new hook useEngagementCalendarEntries keyed ['engagement-calendar-entries', engagementId], direct PostgREST on calendar_entries filtered dossier_id = engagementId — the SELECT RLS policy (quick 260604-lmy) is the gate; EventDialog already invalidates this key on success (landed in 65-04)"
  - 'Scheduled events section renders FIRST, above the derived Today/Upcoming/Past groups (UI-SPEC A-5: user-created content outranks derived metadata; the fix is "I add an event and immediately see it")'

must_haves:
  truths:
    - 'An event created via Add event renders in the Scheduled events section WITHOUT reload: EventDialog success invalidates ["engagement-calendar-entries", engagementId] and the reader refetches (the acceptance test for CTAs 6-7)'
    - 'Both Add event buttons (empty state + header) are live and open EventDialog with the engagement-typed context; the header button is normalized min-h-8 → min-h-11'
    - 'The Scheduled events section follows the UI-SPEC anatomy: sibling h3 heading style, 1px var(--line) bordered list with divide-y, rows at min-h-[var(--row-h)], locale-picked titles, day-first dates (en-GB for EN, toFormatLocale for AR), entry-type Badge via bilingual calendar:types.* keys'
    - 'The whole-tab empty state triggers only when derived events AND scheduled entries are BOTH empty (while the entries query is unresolved, derived-only behavior holds — no flash); its Add event button is live'
    - 'Zero disabled-no-op buttons remain anywhere in CalendarTab'
  artifacts:
    - path: 'frontend/src/hooks/useEngagementCalendarEntries.ts'
      provides: 'Typed calendar_entries reader for the engagement workspace'
      contains: 'engagement-calendar-entries'
      min_lines: 30
    - path: 'frontend/src/pages/engagements/workspace/__tests__/CalendarTabCtas.test.tsx'
      provides: 'ENGPOS-03 #6/#7 wiring + reader render assertions'
      min_lines: 70
  key_links:
    - from: 'frontend/src/pages/engagements/workspace/CalendarTab.tsx'
      to: 'frontend/src/components/Dossier/AddToDossierDialogs.tsx'
      via: 'named import { EventDialog } (exported by 65-04)'
      pattern: 'import \{.*EventDialog.*\} from'
    - from: 'frontend/src/hooks/useEngagementCalendarEntries.ts'
      to: 'calendar_entries (staging)'
      via: 'direct PostgREST SELECT filtered dossier_id = engagementId under SELECT RLS'
      pattern: "eq\\('dossier_id'"
    - from: 'EventDialog success (AddToDossierDialogs.tsx)'
      to: 'useEngagementCalendarEntries cache'
      via: "invalidateQueries ['engagement-calendar-entries', dossier_id]"
      pattern: 'engagement-calendar-entries'
---

<objective>
Make CalendarTab an honest surface: add the Scheduled-events reader (the one net-new visual surface this phase, per the approved UI-SPEC §3) and wire both Add event CTAs (#6/#7) to the exported EventDialog, so a created calendar_entries row renders in-tab without reload.

Purpose: ENGPOS-03 — re-enabling Add event without an in-tab render path would recreate the bug class being fixed (Pitfall 3: CalendarTab today renders only derived engagement dates and fetches no calendar entries).
Output: new reader hook + restructured CalendarTab + wiring/reader tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-RESEARCH.md
@.planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-UI-SPEC.md

<interfaces>
<!-- Reader hook contract (research §Code Examples + UI-SPEC §3; verified columns from the
     calendar-create v11 insert: dossier_id, title_en, title_ar, entry_type, event_date,
     event_time, duration_minutes, all_day, status): -->

```typescript
// frontend/src/hooks/useEngagementCalendarEntries.ts
// Reads user-created calendar_entries anchored to the engagement dossier.
// RLS SELECT policy (quick 260604-lmy: owner/attendee + dossier clearance) is the gate.
export interface EngagementCalendarEntry {
  id: string
  title_en: string
  title_ar: string | null
  event_date: string
  event_time: string | null
  all_day: boolean
  entry_type: string
}
// useQuery({
//   queryKey: ['engagement-calendar-entries', engagementId],
//   queryFn: select 'id, title_en, title_ar, event_date, event_time, all_day, entry_type'
//            from('calendar_entries').eq('dossier_id', engagementId)
//            .order('event_date', { ascending: true }),  // never .reverse() for RTL
//   enabled: engagementId !== '',
// })
```

<!-- Section anatomy (UI-SPEC §3, binding):
     Placement: FIRST content section after the header bar, above Today/Upcoming/Past.
     Heading: h3 "text-xs font-semibold text-muted-foreground uppercase tracking-wider"
       (exact sibling EventSection class string), copy t('calendar.scheduledEvents').
     List: rounded-md border + divide-y; rows flex items-center justify-between gap-3 px-3
       min-h-[var(--row-h)] (density token — NOT the sibling p-3 card pattern).
     Row start: title text-sm font-medium text-foreground, truncate + min-w-0;
       AR locale → title_ar if non-empty else title_en; EN → title_en.
     Row end: date+time meta text-xs text-muted-foreground + Badge variant="secondary"
       text-xs with t('calendar:types.' + entry_type, { defaultValue: entry_type }).
     Date: Intl.DateTimeFormat(locale, { weekday:'short', day:'numeric', month:'short' })
       with locale = 'en-GB' for EN (day-first "Tue 28 Apr") and toFormatLocale(i18n.language)
       for AR (lib/format-locale.ts — raw 'ar' yields Latin digits in Chrome, round-11 fact).
     Time: if !all_day && event_time → append HH:mm (24h), space-separated.
     Empty: single muted line t('calendar.entriesEmpty') — keys landed in 65-01.
     Error: same treatment in the danger token color, t('calendar.entriesError').
     Loading: no skeleton; heading renders, rows appear on resolve.
     Rows are NON-interactive: no hover fill, no chevron, no fake affordance.
     Tab-level empty interplay: whole-tab empty state only when derived events AND entries
     both empty; while entries unresolved, derived-only behavior (no flash). -->

<!-- Engagement-typed dossierContext + useDossier prop pattern: identical to 65-04's
     interfaces block (OverviewTab/TasksTab) — CalendarTab already imports useEngagement. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write reader + wiring tests — RED first</name>
  <files>frontend/src/pages/engagements/workspace/__tests__/CalendarTabCtas.test.tsx</files>
  <read_first>
    - frontend/src/components/positions/__tests__/NewPositionDialog.test.tsx (mock-style analog)
    - frontend/src/pages/engagements/workspace/CalendarTab.tsx (hooks consumed: useEngagement, useLifecycleHistory; the empty-state early return and header-bar structure — to scope mocks and assertions)
    - frontend/src/hooks/useDossierPositionLinks.ts (L121-135 — the direct-PostgREST client pattern the hook test mocks)
  </read_first>
  <behavior>
    - Reader (mock the supabase client chain): useEngagementCalendarEntries(engagementId) queries calendar_entries with eq('dossier_id', engagementId), orders event_date ascending, and uses queryKey ['engagement-calendar-entries', engagementId].
    - CalendarTab with mocked entries (one all_day, one timed): Scheduled events section renders BEFORE the derived sections; row shows the locale-picked title, day-first date, HH:mm for the timed entry, and the entry-type badge text.
    - Entries empty + derived events present: section renders the entriesEmpty line; whole-tab empty state NOT shown.
    - Entries present + derived events absent: normal layout renders (header bar + section), whole-tab empty state NOT shown.
    - Both empty (entries resolved): whole-tab empty state shows with a LIVE (not disabled) Add event button.
    - Both Add event buttons open the EventDialog stub (mock the named export, capture props) with dossierContext.dossier_type 'engagement' and inheritance_source 'direct'; no button in the rendered tab carries disabled as a dead no-op.
    - Query error: entriesError line renders.
    - Decision-tag test names with (ENGPOS-03).
  </behavior>
  <action>
    Create the test file with the behaviors above (mock react-i18next colon-form, useDirection, useEngagement, useLifecycleHistory, the new hook module where convenient, and the EventDialog named export from AddToDossierDialogs). RED: the hook does not exist and CalendarTab has neither section nor wiring. Commit: test(65-05): add failing tests for scheduled-events reader and Add Event wiring.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/pages/engagements/workspace/__tests__/CalendarTabCtas.test.tsx 2>&1 | grep -qE 'failed' && echo RED_CONFIRMED</automated>
  </verify>
  <done>Reader, anatomy, interplay, and wiring assertions exist and are RED; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement useEngagementCalendarEntries</name>
  <files>frontend/src/hooks/useEngagementCalendarEntries.ts</files>
  <read_first>
    - frontend/src/hooks/useDossierPositionLinks.ts (the direct-PostgREST + RLS reader analog — mirror its client import and error handling)
  </read_first>
  <action>
    Create the hook exactly per the interfaces contract: typed EngagementCalendarEntry rows, queryKey ['engagement-calendar-entries', engagementId], select the seven listed columns from calendar_entries filtered eq dossier_id, order event_date ascending, enabled guard on empty id, throw on PostgREST error (the tab maps it to the entriesError line). Explicit return type (ESLint), camelCase use-prefixed filename (hooks/** convention). NO service-role usage, NO edge function — the SELECT RLS policy is the enforcement point. Run the reader tests from Task 1 (GREEN for the hook describe) + pnpm type-check. Commit: feat(65-05): add engagement calendar entries reader hook.
  </action>
  <verify>
    <automated>cd frontend && pnpm type-check && grep -q "engagement-calendar-entries" src/hooks/useEngagementCalendarEntries.ts && grep -qF "eq('dossier_id'" src/hooks/useEngagementCalendarEntries.ts && ! grep -qi 'service_role\|service-role' src/hooks/useEngagementCalendarEntries.ts && echo HOOK_OK</automated>
  </verify>
  <acceptance_criteria>
    - Hook exists with the exact query key and filter; ascending order; typed rows; no RLS bypass
  </acceptance_criteria>
  <done>The render-path data source for CTAs 6-7 exists and matches the invalidation key EventDialog fires (65-04).</done>
</task>

<task type="auto">
  <name>Task 3: Restructure CalendarTab — section, interplay, CTA wiring — GREEN</name>
  <files>frontend/src/pages/engagements/workspace/CalendarTab.tsx</files>
  <read_first>
    - .planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-UI-SPEC.md (§3 Scheduled-events reader spec + §2 rows 6-7 — binding anatomy, copy keys, interplay)
    - frontend/src/lib/format-locale.ts (toFormatLocale — the AR digits fix)
  </read_first>
  <action>
    1. Consume useEngagementCalendarEntries(engagementId). Restructure the empty-state condition: whole-tab empty only when derived events.length === 0 AND (entries unresolved ? true : entries.length === 0) — current derived-only behavior while loading, no flash.
    2. Insert the Scheduled events section as the FIRST content section after the header bar, implementing the interfaces anatomy verbatim: heading t('calendar.scheduledEvents'), bordered divide-y list, var(--row-h) rows, locale-picked titles, the dedicated day-first short formatter (separate from the existing long dateFormatter used by derived sections — do not change that one), HH:mm for timed entries, secondary Badge via calendar:types.* with defaultValue, entriesEmpty muted line, entriesError danger line, no skeleton, non-interactive rows, logical properties only.
    3. Wire both Add event buttons (#6/#7): build dossierContext + useDossier per the 65-04 pattern, local eventDialogOpen state, import { EventDialog } from AddToDossierDialogs (exported in 65-04), replace disabled with the readiness guard + onClick in BOTH render paths (whole-tab empty state and header bar), normalize the header button min-h-8 → min-h-11, remove the R15-02/TODO comments (including the stale "events API lacks engagement_id filter" header note and the trailing conflict-detection TODO comment IF its claim is now stale — keep the conflict-detection TODO itself, reworded to drop the resolved blocker). NO new toasts (EventDialog owns feedback).
    4. Run the full Task 1 file (GREEN) + pnpm type-check. Commit: feat(65-05): scheduled-events reader + live Add Event CTAs in CalendarTab (ENGPOS-03).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/pages/engagements/workspace/__tests__/CalendarTabCtas.test.tsx && pnpm type-check && grep -q 'useEngagementCalendarEntries' src/pages/engagements/workspace/CalendarTab.tsx && grep -q 'EventDialog' src/pages/engagements/workspace/CalendarTab.tsx && ! grep -q 'min-h-8' src/pages/engagements/workspace/CalendarTab.tsx && echo CALENDAR_WIRED</automated>
  </verify>
  <acceptance_criteria>
    - Section anatomy and interplay match UI-SPEC §3 exactly (heading style, row-h rows, date/time formats, empty/error lines, placement first)
    - Both Add event buttons live in both render paths; header normalized to min-h-11; zero dead disabled props remain in the file
    - CalendarTabCtas.test.tsx fully green; type-check exits 0
  </acceptance_criteria>
  <done>CalendarTab is a real surface: created events persist AND render in-tab without reload.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                         |
| --------------------------------- | ------------------------------------------------------------------- |
| browser → calendar_entries (read) | direct PostgREST SELECT; the RLS policy from quick 260604-lmy gates |
| workspace UI → calendar-create    | user-initiated event writes cross edge validation + RLS             |

## STRIDE Threat Register

| Threat ID | Category               | Component                            | Disposition | Mitigation Plan                                                                                                                      |
| --------- | ---------------------- | ------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| T-65-14   | Information Disclosure | calendar_entries reader              | mitigate    | Direct PostgREST under the existing SELECT RLS (owner/attendee + dossier clearance); no service-role client; verify grep in Task 2   |
| T-65-15   | Tampering (user trust) | CTA re-enabled without render path   | mitigate    | The reader ships in the SAME plan as the wiring (UI-SPEC rule); invalidation-key match unit-pinned; live render check in 65-06       |
| T-65-16   | Tampering              | stale-cache false negatives (R12-04) | mitigate    | EventDialog invalidates ['engagement-calendar-entries', dossier_id] (65-04) matching the reader key exactly — asserted in both plans |
| T-65-SC   | Tampering              | npm/pip/cargo installs               | accept      | No package installs in this plan                                                                                                     |

</threat_model>

<verification>
- 65-VALIDATION.md row realized: "Re-enabled CTAs open dialogs with engagement-typed DossierContextForAction" (CalendarTab portion) + the in-tab render path for the ENGPOS-03 live CTA pass → Tasks 1-3
- Wave gate: full suite + size-limit after merge (the section adds minimal bytes to an existing lazy chunk; grep the whole size-limit log for `exceeded` — zero matches)
</verification>

<success_criteria>

- Dispositions #6-#7 implemented per the approved UI-SPEC with the Scheduled-events render path
- CalendarTab contains zero inert buttons; UI-SPEC §3 anatomy verified by unit assertions
  </success_criteria>

<output>
Create `.planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-05-SUMMARY.md` when done.
</output>
