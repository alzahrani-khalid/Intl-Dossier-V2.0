---
phase: 65-engagement-positions-tab-legacy-reconciliation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Dossier/AddToDossierDialogs.tsx
  - frontend/src/pages/engagements/workspace/OverviewTab.tsx
  - frontend/src/pages/engagements/workspace/TasksTab.tsx
  - frontend/src/pages/engagements/workspace/__tests__/CreateTaskCtas.test.tsx
autonomous: true
requirements: [ENGPOS-03]

key_decisions:
  - 'Dialog mounting = Option A (surgical): export the module-private TaskDialog and EventDialog from AddToDossierDialogs.tsx with unchanged ActionDialogProps signature; each tab mounts only the dialog it needs with local useState — NOT the DossierShell whole-AddToDossierDialogs mirror (avoids mounting 8 dialogs + context plumbing)'
  - 'TasksTab Create Task CTAs (#4/#5) are RE-ENABLED with the recorded caveat (Wave-0 Q1 answer): the kanban reads the legacy assignments plane via engagements-kanban-get, the three seed engagement-dossier ids dual-exist in legacy engagements so the board renders EMPTY (not 404), and TaskDialog-created tasks will never appear on it; new canonical engagements WITHOUT a legacy twin would still 404. The honest empty.tasks.body copy (plan 65-01) points users to the main tasks list. Kanban canonicalization = recorded out-of-scope follow-up'
  - 'All AddToDossierDialogs.tsx edits for the phase live in THIS plan (exports + TaskDialog kanban invalidation + EventDialog engagement-calendar-entries invalidation) so plan 65-05 never touches the file — single-owner discipline'

must_haves:
  truths:
    - "OverviewTab has NO Advance/Transition Stage button (disposition #2) and its Create task button opens TaskDialog with dossierContext { dossier_id: engagementId, dossier_type: 'engagement', inheritance_source: 'direct' }"
    - 'Both TasksTab Create task buttons (empty state + header) are live: enabled once the engagement profile resolves, opening TaskDialog — never a dead disabled (UI-SPEC re-enabled-CTA conduct)'
    - 'TaskDialog submit posts NO engagement_id field (tasks.engagement_id FKs the LEGACY engagements table — Pitfall 4) and links the task via work_item_dossiers with dossier_id = engagementId'
    - "TaskDialog success additionally invalidates ['engagement-kanban', engagementId]; EventDialog success additionally invalidates ['engagement-calendar-entries', engagementId] (no-op for dossier-page callers, the render-path key for plan 65-05)"
    - 'The workspace adds ZERO new toasts — the dialogs own all feedback (Pitfall 6 double-toast trap)'
  artifacts:
    - path: 'frontend/src/components/Dossier/AddToDossierDialogs.tsx'
      provides: 'Exported TaskDialog + EventDialog with workspace-aware invalidations'
      contains: 'export function TaskDialog'
    - path: 'frontend/src/pages/engagements/workspace/__tests__/CreateTaskCtas.test.tsx'
      provides: 'ENGPOS-03 wiring assertions: CTAs open dialog with engagement-typed context; submit payload has no engagement_id'
      min_lines: 70
  key_links:
    - from: 'frontend/src/pages/engagements/workspace/OverviewTab.tsx'
      to: 'frontend/src/components/Dossier/AddToDossierDialogs.tsx'
      via: 'named import { TaskDialog }'
      pattern: 'import \{.*TaskDialog.*\} from'
    - from: 'frontend/src/components/Dossier/AddToDossierDialogs.tsx (TaskDialog submit)'
      to: 'tasks-create edge + work_item_dossiers'
      via: 'useCreateTask then useCreateWorkItemDossierLinks(buildDossierLinkPayload)'
      pattern: 'buildDossierLinkPayload'
    - from: 'TaskDialog success block'
      to: "TasksTab kanban reader ['engagement-kanban', engagementId]"
      via: 'queryClient.invalidateQueries prefix'
      pattern: 'engagement-kanban'
---

<objective>
Wire the Create Task CTAs (dispositions #3, #4, #5) through the shipped TaskDialog, remove OverviewTab's redundant Transition Stage button (#2), and centralize the phase's AddToDossierDialogs.tsx changes: export TaskDialog/EventDialog and add the two workspace-scoped success invalidations.

Purpose: ENGPOS-03 demands these buttons become functional with a verified write path (tasks-create + work_item_dossiers) — the engagement IS a dossier, so the dossier-page dialogs work verbatim with an engagement-typed context. The kanban render-surface caveat is recorded, not papered over (Pitfall 2 / Wave-0 Q1 answer).
Output: exported dialogs + two wired tabs + wiring tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-RESEARCH.md
@.planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-UI-SPEC.md

<interfaces>
<!-- Dialog props (AddToDossierDialogs.tsx L86-92, verified) — signature stays UNCHANGED: -->

```typescript
interface ActionDialogProps {
  isOpen: boolean
  onClose: () => void
  dossier: Dossier // ignored by TaskDialog/EventDialog (destructured as _dossier)
  dossierContext: DossierContextForAction
  isRTL: boolean
}
```

<!-- Engagement-typed context (research §Code Examples — build from the shell-cached profile): -->

```typescript
import { useEngagement } from '@/domains/engagements/hooks/useEngagements'
import { useDossier } from '@/domains/dossiers/hooks/useDossier'
import type { DossierContextForAction } from '@/hooks/useAddToDossierActions'

const { data: profile } = useEngagement(engagementId) // cache hit — WorkspaceShell loaded it
const engagement = profile?.engagement
const dossierContext: DossierContextForAction | null = engagement
  ? {
      dossier_id: engagementId,
      dossier_type: 'engagement', // hard-code: DB stores 'engagement'; profile TS literal is the stale 'engagement_dossier'
      dossier_name_en: engagement.name_en,
      dossier_name_ar: engagement.name_ar ?? null,
      inheritance_source: 'direct', // the workspace IS this dossier's own page (NOT 'engagement')
    }
  : null
const { data: dossier } = useDossier(engagementId) // real fetch here (Pitfall 5) — required-but-ignored prop
// Mount guard + cast precedent (DossierShell.tsx L286-293):
// {dossierContext !== null && dossier !== undefined && (
//   <TaskDialog isOpen={open} onClose={() => setOpen(false)}
//     dossier={dossier as Parameters<typeof TaskDialog>[0]['dossier']}
//     dossierContext={dossierContext} isRTL={isRTL} />
// )}
```

<!-- TaskDialog submit today (L345-372): useCreateTask({ title, description, assignee_id, priority })
     → buildDossierLinkPayload('task', id, dossierContext) → invalidates
     ['dossier-tab','work_items', dossier_id] + workItemDossierKeys.timeline(dossier_id).
     ADD: queryClient.invalidateQueries({ queryKey: ['engagement-kanban', dossierContext.dossier_id] })
     EventDialog success block (L676-682) invalidates ['dossier-tab','engagements', dossier_id] +
     dossierOverviewKeys.detail(dossier_id).
     ADD: queryClient.invalidateQueries({ queryKey: ['engagement-calendar-entries', dossierContext.dossier_id] })
     Kanban reader key (useEngagementKanban.ts L42, verified): ['engagement-kanban', engagementId, sortBy]
     — prefix invalidation on ['engagement-kanban', engagementId] covers all sorts. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write CTA wiring tests — RED first</name>
  <files>frontend/src/pages/engagements/workspace/__tests__/CreateTaskCtas.test.tsx</files>
  <read_first>
    - frontend/src/components/positions/__tests__/NewPositionDialog.test.tsx (mock-style analog)
    - frontend/src/components/Dossier/AddToDossierDialogs.tsx (TaskDialog L319-373: form state, useCreateTask/useCreateWorkItemDossierLinks usage — to scope the submit-capture mocks)
    - frontend/src/pages/engagements/workspace/OverviewTab.tsx and TasksTab.tsx (hooks each consumes — useEngagement, useEngagementKanban, useLifecycleHistory etc. — to scope module mocks)
  </read_first>
  <behavior>
    - OverviewTab (ENGPOS-03 #2): queryByText('actions.transitionStage') is null; actions.logAfterAction still present.
    - OverviewTab (#3): with mocked useEngagement returning a profile (name_en/name_ar) and mocked useDossier returning a row, the Create task button is NOT disabled; clicking it renders TaskDialog open=true (mock the TaskDialog export as a prop-capturing stub) with dossierContext.dossier_type === 'engagement', dossier_id === engagementId, inheritance_source === 'direct'.
    - OverviewTab readiness guard: with useEngagement still loading (undefined), the button IS disabled (WR-03-style guard, not a dead disabled).
    - TasksTab (#4/#5): empty-state button and header button both enabled and both open the TaskDialog stub (assert for each render path).
    - TaskDialog payload (Pitfall 4): render the REAL TaskDialog with mocked useCreateTask/useCreateWorkItemDossierLinks (capture mutateAsync args); fill title and submit; assert the create payload object has NO engagement_id property, buildDossierLinkPayload linked dossier_id === engagementId, and queryClient.invalidateQueries was called with ['engagement-kanban', engagementId].
    - Decision-tag test names with (ENGPOS-03).
  </behavior>
  <action>
    Create the test file. Mock '@/components/Dossier/AddToDossierDialogs' selectively for the tab tests (stub TaskDialog capturing props) but import the real module for the payload test (separate describe with vi.doMock/vi.resetModules or a second test file section — executor's choice, keep it in this one file). RED: the tab tests fail (buttons disabled, no dialog import, transitionStage still present); the payload test partially fails (no ['engagement-kanban'] invalidation exists yet; TaskDialog not exported yet — assert via the module's named export so this is RED too). Commit: test(65-04): add failing wiring tests for Create Task CTAs.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/pages/engagements/workspace/__tests__/CreateTaskCtas.test.tsx 2>&1 | grep -qE 'failed' && echo RED_CONFIRMED</automated>
  </verify>
  <done>Wiring assertions exist for dispositions #2-#5 + the no-engagement_id payload contract; RED; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Export the dialogs + add the two workspace invalidations</name>
  <files>frontend/src/components/Dossier/AddToDossierDialogs.tsx</files>
  <read_first>
    - frontend/src/components/Dossier/AddToDossierDialogs.tsx (TaskDialog success block ~L346-373; EventDialog success block ~L654-690 — the exact insertion points)
  </read_first>
  <action>
    1. Add the export keyword to function TaskDialog( (L319) and function EventDialog( (L630). NO signature or behavior changes otherwise.
    2. In TaskDialog's success path (after the existing dossier-tab/work-items invalidations), add a prefix invalidation of ['engagement-kanban', dossierContext.dossier_id]. Harmless no-op on dossier pages (no active query); correct for the workspace.
    3. In EventDialog's success path (after the existing R12-04 invalidations), add a prefix invalidation of ['engagement-calendar-entries', dossierContext.dossier_id] — the render-path key plan 65-05's reader consumes. Add a one-line comment referencing 65-05.
    4. Do NOT add, remove, or move any toast (the pre-existing useCreateTask+sonner double-toast wart stays — Pitfall 6 says do not add a third, and do not "fix" the existing pair this phase). Commit: feat(65-04): export TaskDialog/EventDialog with workspace-scoped invalidations.
  </action>
  <verify>
    <automated>cd frontend && grep -q 'export function TaskDialog' src/components/Dossier/AddToDossierDialogs.tsx && grep -q 'export function EventDialog' src/components/Dossier/AddToDossierDialogs.tsx && grep -q "engagement-kanban" src/components/Dossier/AddToDossierDialogs.tsx && grep -q "engagement-calendar-entries" src/components/Dossier/AddToDossierDialogs.tsx && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - Both dialogs exported, ActionDialogProps unchanged, zero toast changes
    - Each success block gains exactly one invalidation line keyed on dossierContext.dossier_id
  </acceptance_criteria>
  <done>The dialogs are importable by workspace tabs and refresh the workspace readers on success.</done>
</task>

<task type="auto">
  <name>Task 3: Wire OverviewTab + TasksTab — GREEN</name>
  <files>frontend/src/pages/engagements/workspace/OverviewTab.tsx, frontend/src/pages/engagements/workspace/TasksTab.tsx</files>
  <read_first>
    - .planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-UI-SPEC.md (§2 rows 2-5 — binding before/after; re-enabled CTA conduct paragraph)
    - frontend/src/components/Dossier/DossierShell.tsx (~L286-293 — the dossier-prop cast precedent)
  </read_first>
  <action>
    1. OverviewTab: delete the disabled Transition Stage button (+R15-02 comment) from Quick Actions — the stepper above the tabs is the working surface (disposition #2). Wire Create task (#3): build dossierContext per the interfaces block (useEngagement is ALREADY imported in this file), add useDossier(engagementId), local taskDialogOpen state, replace the dead disabled with disabled={dossierContext === null || dossier === undefined} and onClick opening the dialog; mount the exported TaskDialog under the guard with the cast precedent. Keep variant/size/icon exactly as shipped (UI-SPEC: behavior changes only).
    2. TasksTab (#4/#5): same context/dossier/state pattern (add the useEngagement + useDossier imports). The empty-state early return and the main return are separate render paths — BOTH must be able to open the dialog, so render the guarded TaskDialog in both paths (or lift the shared JSX; smallest correct diff wins). Normalize the header button min-h-8 → min-h-11 (CLAUDE.md 44px floor on every interactive CTA; UI-SPEC spacing exceptions). Remove the R15-02 comments on both buttons.
    3. NO new toasts, NO engagement_id anywhere, NO changes to kanban drag/sort logic. Run the Task 1 tests (GREEN) + pnpm type-check. Commit: feat(65-04): wire Create Task CTAs in Overview and Tasks tabs (ENGPOS-03).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/pages/engagements/workspace/__tests__/CreateTaskCtas.test.tsx && pnpm type-check && ! grep -q 'transitionStage' src/pages/engagements/workspace/OverviewTab.tsx && grep -c 'TaskDialog' src/pages/engagements/workspace/TasksTab.tsx | xargs test 2 -le && echo WIRED</automated>
  </verify>
  <acceptance_criteria>
    - OverviewTab: Advance Stage gone; Create task opens TaskDialog with engagement-typed context; readiness guard only
    - TasksTab: both buttons live in both render paths; header button at min-h-11
    - CreateTaskCtas.test.tsx fully green; type-check exits 0
  </acceptance_criteria>
  <done>Three Create Task CTAs are functional with the honest kanban caveat carried by the plan-01 copy; one redundant CTA removed.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                       |
| --------------------------------- | ----------------------------------------------------------------- |
| workspace UI → tasks-create edge  | user-initiated task writes cross edge validation + RLS            |
| workspace UI → work-item-dossiers | dossier-link writes keyed on the canonical engagement dossiers.id |

## STRIDE Threat Register

| Threat ID | Category               | Component                       | Disposition | Mitigation Plan                                                                                                                          |
| --------- | ---------------------- | ------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| T-65-10   | Tampering              | tasks.engagement_id legacy FK   | mitigate    | NEVER pass engagement_id in CreateTaskRequest (Pitfall 4 — FK targets legacy engagements; 23503 risk); unit test asserts payload absence |
| T-65-11   | Tampering (user trust) | kanban render-surface mismatch  | mitigate    | Recorded caveat (Wave-0 Q1): honest empty-state copy (plan 65-01) + kanban invalidation anyway + out-of-scope follow-up in SUMMARY       |
| T-65-12   | Information Disclosure | dialog writes                   | mitigate    | Reuse existing edge-side validation + RLS paths verbatim (tasks-create, work-item-dossiers); no new write paths, no service-role usage   |
| T-65-13   | Tampering (user trust) | re-enabled CTA without renderer | mitigate    | Pitfall 8 test: write path AND render path — tasks render in the main tasks list + dossier work-item surfaces; live pass in 65-06        |
| T-65-SC   | Tampering              | npm/pip/cargo installs          | accept      | No package installs in this plan                                                                                                         |

</threat_model>

<verification>
- 65-VALIDATION.md rows realized: "Re-enabled CTAs open dialogs with engagement-typed DossierContextForAction" + "TaskDialog submit posts task WITHOUT engagement_id and links via work_item_dossiers" → Tasks 1-3
- Per-task gate: quick vitest run + pnpm type-check
</verification>

<success_criteria>

- Dispositions #2-#5 implemented per the approved UI-SPEC matrix with the recorded kanban caveat
- AddToDossierDialogs is fully phase-complete (exports + both invalidations) so 65-05 needs no edits there
  </success_criteria>

<output>
Create `.planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-04-SUMMARY.md` when done (include the kanban canonicalization out-of-scope follow-up).
</output>
