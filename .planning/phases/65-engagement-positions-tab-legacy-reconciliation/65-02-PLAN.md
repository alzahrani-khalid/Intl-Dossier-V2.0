---
phase: 65-engagement-positions-tab-legacy-reconciliation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/workspace/WorkspaceShell.tsx
  - frontend/src/pages/engagements/workspace/ContextTab.tsx
  - frontend/src/pages/engagements/workspace/DocsTab.tsx
  - frontend/src/pages/engagements/workspace/__tests__/RemovedCtas.test.tsx
autonomous: true
requirements: [ENGPOS-03]

key_decisions:
  - 'CTA dispositions per approved UI-SPEC matrix: #1 WorkspaceShell Transition Stage REMOVED (LifecycleStepperBar directly below is the working transition surface), #8 ContextTab Link Dossier REMOVED ×2 (linked dossiers are derived read-only data — no writable link table exists; re-enabling needs net-new backend design, out of scope), #9 DocsTab Upload REMOVED (attachment upload is a tracked backend P0 gap; PR #35 honest-not-fixed precedent)'
  - 'Orphaned workspace.json keys (actions.transitionStage, actions.linkDossier, empty.context.action, docs.upload) are intentionally LEFT in place — UI-SPEC marks the sweep optional/harmless and workspace.json is owned by plan 65-01 this wave'

must_haves:
  truths:
    - 'WorkspaceShell renders no Transition/Advance Stage button; the Log After-Action button and LifecycleStepperBar remain untouched and functional'
    - 'ContextTab renders no Link Dossier button in EITHER the empty state or the populated (tiers) state'
    - 'DocsTab renders no Upload button; the Generate Briefing button keeps its existing styling, placement, and handler'
    - 'No empty action containers or layout placeholders remain — sibling flex rows reflow naturally via existing gap (UI-SPEC rule: removed buttons leave no placeholder)'
    - 'No import orphaned by these removals (e.g. Upload icon in DocsTab) survives — CI lints the whole repo at --max-warnings 0'
  artifacts:
    - path: 'frontend/src/pages/engagements/workspace/__tests__/RemovedCtas.test.tsx'
      provides: 'ENGPOS-03 removal assertions: removed CTAs absent from rendered output, surviving real actions still present'
      min_lines: 50
  key_links:
    - from: 'frontend/src/components/workspace/WorkspaceShell.tsx'
      to: 'frontend/src/components/engagements/LifecycleStepperBar (existing)'
      via: 'the stepper stays the only stage-transition surface'
      pattern: 'LifecycleStepperBar'
    - from: 'frontend/src/pages/engagements/workspace/__tests__/RemovedCtas.test.tsx'
      to: 'ContextTab.tsx / DocsTab.tsx / WorkspaceShell.tsx'
      via: 'render assertions (queryByText on removed i18n keys returns null)'
      pattern: 'RemovedCtas'
---

<objective>
Remove the four inert CTAs that cannot be honestly wired (ENGPOS-03 dispositions #1, #8 ×2, #9 from the approved UI-SPEC matrix): WorkspaceShell's onClick-less Transition Stage button, ContextTab's two disabled Link Dossier buttons, and DocsTab's disabled Upload button — leaving zero placeholders and zero orphaned imports.

Purpose: ENGPOS-03's acceptance is "every formerly-disabled button is gone or functional". These four duplicate a working surface (stepper) or have no backend write path (derived context links, upload P0 gap) — removal is the only honest disposition (RESEARCH Pitfall 8).
Output: three cleaned components + a removal-assertion test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-UI-SPEC.md

<interfaces>
<!-- Exact removal sites (verified against current files 2026-06-12):
     1. WorkspaceShell.tsx ~L92-99: <Button variant="outline" ... disabled={isLoading}>{t('actions.transitionStage')}</Button>
        — inside the header actions div; the sibling Log After-Action Link button STAYS.
        LifecycleStepperBar renders directly below the header (~L116-126) and self-handles
        transitions via useLifecycleTransition — it is the working surface.
     2. ContextTab.tsx ~L196-199 (empty state, key empty.context.action) and ~L224-227
        (populated state, key actions.linkDossier) — both disabled no-ops with R15-02 comments.
     3. DocsTab.tsx ~L150-155: disabled Upload button (key docs.upload) in the header actions
        div; the sibling Generate Briefing button (real handler handleGenerateBriefing) STAYS.
     The R15-02 explanatory comments above each removed button are removed with the button. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write removal-assertion tests — RED first</name>
  <files>frontend/src/pages/engagements/workspace/__tests__/RemovedCtas.test.tsx</files>
  <read_first>
    - frontend/src/components/positions/__tests__/NewPositionDialog.test.tsx (mock style analog: vi.mock react-i18next returning keys, vi.mock useDirection, mutable mock state)
    - frontend/src/pages/engagements/workspace/ContextTab.tsx and DocsTab.tsx (hooks each consumes — to scope the module mocks)
    - frontend/src/components/workspace/WorkspaceShell.tsx (hooks/children consumed: useEngagement, WorkspaceTabNav, LifecycleStepperBar, router Link/Outlet — mock children as stubs)
  </read_first>
  <behavior>
    - ContextTab empty state (mock its data hooks to return zero linked dossiers): queryByText('empty.context.action') is null; the empty heading/body still render.
    - ContextTab populated state (mock tiers with one dossier): queryByText('actions.linkDossier') is null; DossierContextBadge content still renders (mock the badge as a stub).
    - DocsTab: queryByText('docs.upload') is null; the Generate Briefing button (actions.generateBriefing or empty.docs.action depending on state) IS present.
    - WorkspaceShell: queryByText('actions.transitionStage') is null; actions.logAfterAction IS present; the LifecycleStepperBar stub IS rendered. Mock @tanstack/react-router (Link → anchor, Outlet → null) and stub heavy children (WorkspaceTabNav, LifecycleStepperBar, LtrIsolate) as named test doubles.
    - Decision-tag every test name with (ENGPOS-03).
  </behavior>
  <action>
    Create the test file with the four behaviors above. The t-mock returns the key string, so absence assertions are queryByText on the raw key (project test convention). Run the file: all four removal assertions MUST be RED (the buttons still exist). Commit: test(65-02): add failing removal assertions for unwireable workspace CTAs.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/pages/engagements/workspace/__tests__/RemovedCtas.test.tsx 2>&1 | grep -qE 'failed' && echo RED_CONFIRMED</automated>
  </verify>
  <done>Removal assertions exist for all four CTA sites and are RED against current code; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the four buttons + orphaned imports — GREEN</name>
  <files>frontend/src/components/workspace/WorkspaceShell.tsx, frontend/src/pages/engagements/workspace/ContextTab.tsx, frontend/src/pages/engagements/workspace/DocsTab.tsx</files>
  <read_first>
    - .planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-UI-SPEC.md (§2 CTA Disposition table rows 1, 8, 9 — the binding before/after contract)
  </read_first>
  <action>
    1. WorkspaceShell.tsx: delete the Transition Stage Button element (and its preceding comment) from the header actions div, leaving the Log After-Action button as the sole child. Do NOT touch LifecycleStepperBar, the sticky layout, or anything else (Karpathy surgical-changes).
    2. ContextTab.tsx: delete both disabled buttons (+ their R15-02 comments). In the populated branch the trailing button leaves a fragment with only the tiers map — simplify the JSX accordingly. Remove the Plus lucide import IF it is now unused in the file (check remaining usages first).
    3. DocsTab.tsx: delete the disabled Upload button (+ comment) from the header actions div; Generate Briefing stays byte-identical. Remove the Upload lucide import if now orphaned.
    4. Confirm no empty wrapper divs remain (the actions containers still hold their surviving real buttons; gap reflows). Run the Task 1 tests (GREEN), pnpm type-check, and lint the three touched files. Commit: feat(65-02): remove unwireable workspace CTAs (ENGPOS-03 dispositions 1, 8, 9).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/pages/engagements/workspace/__tests__/RemovedCtas.test.tsx && pnpm type-check && ! grep -q 'transitionStage' src/components/workspace/WorkspaceShell.tsx && ! grep -qE 'linkDossier|empty\.context\.action' src/pages/engagements/workspace/ContextTab.tsx && ! grep -q "docs\.upload" src/pages/engagements/workspace/DocsTab.tsx && grep -q 'generateBriefing\|handleGenerateBriefing' src/pages/engagements/workspace/DocsTab.tsx && echo REMOVALS_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - Zero references to the four removed CTAs remain in the three components; surviving real actions (Log After-Action, Generate Briefing, Schedule-Poll-adjacent layout) untouched
    - No orphaned imports introduced by the removals (lint clean at --max-warnings 0 scope for touched files)
    - RemovedCtas.test.tsx fully green; type-check exits 0
  </acceptance_criteria>
  <done>Four of the nine round-15 inert CTAs are honestly gone, with render-level regression pins.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                         |
| ---------------------------- | ------------------------------------------------------------------- |
| user → workspace affordances | rendered buttons promise capabilities; inert ones tamper with trust |

## STRIDE Threat Register

| Threat ID | Category                 | Component                              | Disposition | Mitigation Plan                                                                                                         |
| --------- | ------------------------ | -------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------- |
| T-65-04   | Tampering (user trust)   | inert CTAs (R15-02 bug class)          | mitigate    | Remove all four unwireable buttons; render tests assert absence; ENGPOS-03 acceptance = gone OR functional, never inert |
| T-65-05   | Denial of Service (self) | accidental removal of working surfaces | mitigate    | Tests assert Log After-Action, Generate Briefing, and LifecycleStepperBar survive; surgical-diff rule in task actions   |
| T-65-SC   | Tampering                | npm/pip/cargo installs                 | accept      | No package installs in this plan                                                                                        |

</threat_model>

<verification>
- 65-VALIDATION.md row realized: "removed CTAs are absent from the rendered output" (ENGPOS-03, ContextTab/DocsTab/WorkspaceShell portion) → Tasks 1-2
- Per-task gate: quick vitest run + pnpm type-check
</verification>

<success_criteria>

- CTA dispositions #1, #8, #9 implemented exactly per the approved UI-SPEC matrix
- No placeholders, no orphaned imports, no touched working surfaces
  </success_criteria>

<output>
Create `.planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-02-SUMMARY.md` when done.
</output>
