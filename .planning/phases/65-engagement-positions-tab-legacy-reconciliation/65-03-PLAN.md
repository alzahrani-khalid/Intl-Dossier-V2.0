---
phase: 65-engagement-positions-tab-legacy-reconciliation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/dossier/EngagementDossierDetail.tsx
  - frontend/src/components/positions/EngagementPositionsSection.tsx
  - frontend/src/hooks/useEngagementPositions.ts
  - frontend/src/components/positions/PositionSuggestionsPanel.tsx
  - frontend/src/components/positions/BriefingPackGenerator.tsx
  - frontend/src/i18n/en/positions.json
  - frontend/src/i18n/ar/positions.json
  - supabase/functions/engagements-positions-attach/index.ts
  - supabase/functions/engagements-positions-list/index.ts
  - supabase/functions/engagements-positions-detach/index.ts
autonomous: true
requirements: [ENGPOS-01]

key_decisions:
  - 'Deletion boundary = the COMPONENT layer: EngagementDossierDetail + EngagementPositionsSection + useEngagementPositions (the mandated legacy stack) PLUS PositionSuggestionsPanel + BriefingPackGenerator (orphaned by this deletion, both sit on the legacy id space — anti-pattern 4). The deeper suggestions plumbing (domains/positions/hooks/usePositionSuggestions.ts, hooks/usePositionSuggestions.ts, getPositionSuggestions in positions.repository.ts, PositionSuggestion* types, domains/positions barrel exports) is KEPT and recorded as a dead-code follow-up — it is barrel-exported domain API, not workspace UI, and sweeping it in a parallel wave-1 plan risks type-check fallout (planner-verified the chain is wider than RESEARCH stated)'
  - 'The three engagements-positions-* edges are deprecated via source-header comments only; undeploy is an environment action with no executor MCP access — DEFERRED and recorded in the SUMMARY (RESEARCH Assumption A4 posture)'
  - 'EngagementDetailPage.tsx is OUT OF SCOPE — unrouted but not touched by this chain (Karpathy surgical-changes); mentioned in SUMMARY only'

must_haves:
  truths:
    - 'No file under frontend/src references useEngagementPositions, EngagementPositionsSection, EngagementDossierDetail, PositionSuggestionsPanel, or BriefingPackGenerator after deletion (grep zero, build green)'
    - 'engagement_positions is confirmed 0 rows via service-role REST immediately BEFORE the deletion commit (RESEARCH Open Q4 re-check — if any rows exist, STOP and surface)'
    - 'Each of the three engagements-positions-* edge index.ts files carries a DEPRECATED header naming ENGPOS-01 and pointing to position_dossier_links / positions-dossiers-create'
    - "The orphaned engagement_section.* i18n block is removed from BOTH en and ar positions.json, and no surviving code references 'engagement_section'"
    - 'The frontend builds and type-checks green after all deletions (nothing live imported the deleted chain)'
  artifacts:
    - path: 'supabase/functions/engagements-positions-attach/index.ts'
      provides: 'Source-level deprecation record (edge stays deployed; undeploy deferred)'
      contains: 'DEPRECATED'
  key_links:
    - from: 'supabase/functions/engagements-positions-*/index.ts DEPRECATED headers'
      to: 'position_dossier_links canonical plane'
      via: 'pointer text naming the ENGPOS-01 decision and the replacement edge'
      pattern: 'positions-dossiers-create'
    - from: 'deletion gate (pre-commit REST probe)'
      to: 'staging engagement_positions table'
      via: 'service-role PostgREST count check from .env.test'
      pattern: 'engagement_positions'
---

<objective>
Implement the legacy half of the ENGPOS-01 decision: delete the orphaned engagement_positions frontend stack (five dead files), sweep its i18n block, and mark the three zero-caller engagements-positions-* edge functions as deprecated in source — after re-confirming the legacy table is still empty.

Purpose: the canonical-source decision is only "implemented" when the losing plane cannot be silently extended. The stack is verified dead (import graph re-checked 2026-06-12) and the table has 0 rows + write-deny RLS + no migration provenance — nothing to migrate, everything to delete (RESEARCH §Legacy Reconciliation).
Output: deletions + deprecation headers + recorded deferrals/fences for the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-RESEARCH.md

<interfaces>
<!-- Deprecation header to place at the very top of each of the three edge index.ts files
     (engagements-positions-attach, -list, -detach). Source-level record only — the deployed
     artifact is unchanged until a future undeploy: -->

```ts
// DEPRECATED (Phase 65 / ENGPOS-01, 2026-06-12): this edge validates against the
// LEGACY `engagements` table and reads/writes `engagement_positions` (0 rows,
// write-deny RLS, no migration provenance). The canonical engagement-positions
// plane is `position_dossier_links` keyed by the engagement's dossiers.id,
// written via the `positions-dossiers-create` edge. Zero frontend callers.
// Do NOT extend or "fix" this function. Undeploy deferred (environment action)
// — see .planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-03-SUMMARY.md.
```

<!-- Pre-deletion staging gate (executors have no Supabase MCP — use service-role REST
     with .env.test vars; never echo key values): GET
     $SUPABASE_URL/rest/v1/engagement_positions?select=id&limit=3 with apikey +
     Authorization = SUPABASE_SERVICE_ROLE_KEY must return []. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify the import graph + staging emptiness gate, then delete the dead stack</name>
  <files>frontend/src/components/dossier/EngagementDossierDetail.tsx, frontend/src/components/positions/EngagementPositionsSection.tsx, frontend/src/hooks/useEngagementPositions.ts, frontend/src/components/positions/PositionSuggestionsPanel.tsx, frontend/src/components/positions/BriefingPackGenerator.tsx, frontend/src/i18n/en/positions.json, frontend/src/i18n/ar/positions.json</files>
  <read_first>
    - .planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-RESEARCH.md (§Legacy Reconciliation — deletion risk map; §Wave-0 Diagnostic Answers item 4)
    - .env.test (SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY for the gate probe — NEVER echo values)
  </read_first>
  <action>
    1. Import-graph re-verification (execution-time grep, the RESEARCH map is from 2026-06-12): for each of the five deletion targets, grep -rn its module name across frontend/src excluding the dead chain itself and excluding string self-matches. Expected: EngagementDossierDetail imported by nothing; EngagementPositionsSection only by EngagementDossierDetail; useEngagementPositions only by EngagementPositionsSection; PositionSuggestionsPanel and BriefingPackGenerator only by EngagementPositionsSection. If ANY unexpected importer appears, STOP and surface it instead of deleting.
    2. Staging emptiness gate (RESEARCH Open Q4): source .env.test and curl the service-role REST probe from the interfaces block. The response must be an empty array. If any row exists, STOP — do not delete, record the finding.
    3. Delete the five files. Do NOT touch domains/positions/hooks/usePositionSuggestions.ts, hooks/usePositionSuggestions.ts, getPositionSuggestions in positions.repository.ts, the PositionSuggestion* types, or the domains/positions barrels — that deeper plumbing is a recorded follow-up, not this phase's diff (see plan key_decisions; deviation from RESEARCH's "planner decides" is deliberate and planner-decided).
    4. Sweep the engagement_section block from BOTH frontend/src/i18n/en/positions.json and ar/positions.json (its only consumers die in this commit). Leave every other positions.json key untouched.
    5. Run cd frontend && pnpm build and pnpm type-check (proves nothing live imported the chain). Commit: refactor(65-03): delete orphaned engagement_positions frontend stack (ENGPOS-01).
  </action>
  <verify>
    <automated>cd frontend && ! grep -rqE 'useEngagementPositions|EngagementPositionsSection|EngagementDossierDetail|PositionSuggestionsPanel|BriefingPackGenerator' src --include='*.ts' --include='*.tsx' && ! grep -q 'engagement_section' src/i18n/en/positions.json && ! grep -q 'engagement_section' src/i18n/ar/positions.json && pnpm type-check && echo STACK_DELETED</automated>
  </verify>
  <acceptance_criteria>
    - All five files removed; zero surviving references (grep) and type-check green
    - The staging gate ran and returned 0 rows BEFORE the deletion commit (recorded in SUMMARY with the probe shape, no secrets)
    - engagement_section.* gone from both positions.json files; no other i18n keys touched
    - The kept suggestions plumbing (usePositionSuggestions chain) is explicitly listed in the SUMMARY as dead-code follow-up
  </acceptance_criteria>
  <done>The legacy frontend stack is gone, gated on live emptiness, with the surgical boundary recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Deprecation headers on the three legacy edges + scope-fence records</name>
  <files>supabase/functions/engagements-positions-attach/index.ts, supabase/functions/engagements-positions-list/index.ts, supabase/functions/engagements-positions-detach/index.ts</files>
  <read_first>
    - .planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-RESEARCH.md (§Legacy Reconciliation "Scope-fence as NOT-this-phase" — the four hazards to record verbatim)
  </read_first>
  <action>
    1. Prepend the DEPRECATED header from the interfaces block to each of the three edge index.ts files (comment only — no logic changes, no redeploy; the pre-commit frontend build does not compile supabase/functions).
    2. Draft the SUMMARY's deferral + fence section: (a) edge undeploy deferred — environment action via Supabase CLI, zero in-repo callers verified, hidden-external-caller risk per RESEARCH A4; (b) NOT-this-phase fences recorded verbatim from RESEARCH: briefing-packs-generate still reads engagement_positions (cross-surface hazard if resurrected); positions-list dossier filter joins engagement_positions (latent stale path — never pass a dossier filter); the tasks/assignments kanban plane straddles legacy engagements ids (canonicalization is its own phase-sized slice); dossiers-get extension map points engagement → legacy engagements (harmless maybeSingle miss); (c) unrouted EngagementDetailPage.tsx and the kept usePositionSuggestions plumbing as dead-code candidates.
    3. Commit: docs(65-03): mark engagements-positions-* edges deprecated per ENGPOS-01.
  </action>
  <verify>
    <automated>grep -q 'DEPRECATED' supabase/functions/engagements-positions-attach/index.ts && grep -q 'DEPRECATED' supabase/functions/engagements-positions-list/index.ts && grep -q 'DEPRECATED' supabase/functions/engagements-positions-detach/index.ts && grep -q 'positions-dossiers-create' supabase/functions/engagements-positions-attach/index.ts && echo EDGES_MARKED</automated>
  </verify>
  <acceptance_criteria>
    - All three edges carry the header naming ENGPOS-01, the canonical replacement, and the undeploy deferral pointer
    - No functional line of any edge changed (comment-only diff)
    - SUMMARY carries the deferral + all scope fences
  </acceptance_criteria>
  <done>The losing plane is documented as deprecated at every in-repo touchpoint; environment cleanup is an explicit recorded deferral.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                 |
| --------------------------------- | ----------------------------------------------------------- |
| service-role probe → staging      | .env.test service key used for the read-only emptiness gate |
| repo deletions → production build | deleting files that anything live might import              |

## STRIDE Threat Register

| Threat ID | Category               | Component                         | Disposition | Mitigation Plan                                                                                                            |
| --------- | ---------------------- | --------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------- |
| T-65-06   | Denial of Service      | deleting a still-imported module  | mitigate    | Execution-time grep of the import graph per target + pnpm build/type-check gate before commit; STOP rule on surprises      |
| T-65-07   | Tampering (data loss)  | legacy table with unnoticed rows  | mitigate    | Service-role REST count gate must return 0 rows immediately before the deletion commit; STOP rule otherwise                |
| T-65-08   | Elevation of Privilege | "fixing" the legacy plane         | mitigate    | NO migrations, NO INSERT policies, NO edge logic changes — deprecation is comment-only; the table is fixed by NOT using it |
| T-65-09   | Information Disclosure | service key leaking into evidence | mitigate    | Key sourced from .env.test as env var; acceptance criteria forbid secret values in output or SUMMARY                       |
| T-65-SC   | Tampering              | npm/pip/cargo installs            | accept      | No package installs in this plan                                                                                           |

</threat_model>

<verification>
- RESEARCH Open Q4 re-check realized as the Task 1 gate
- Phase-gate contribution: legacy reconciliation half of roadmap success criterion 1 (decision implemented on the losing side)
</verification>

<success_criteria>

- Legacy stack deleted with grep + build + type-check proof; staging gate recorded
- Three edges deprecated in source; undeploy deferral + all scope fences recorded for the phase SUMMARY
  </success_criteria>

<output>
Create `.planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-03-SUMMARY.md` when done (deletion list, gate evidence shape, deferral, fences — no secrets).
</output>
