---
phase: 65-engagement-positions-tab-legacy-reconciliation
plan: 06
type: execute
wave: 3
depends_on: [65-01, 65-02, 65-03, 65-04, 65-05]
files_modified: []
autonomous: true
requirements: [ENGPOS-01, ENGPOS-02, ENGPOS-03]

key_decisions:
  - 'ORCHESTRATOR-RUN plan: executor agents have NO Supabase MCP and NO browser — the orchestrator executes this plan inline (64-01/64-06 precedent). Wave-0 staging diagnostics are ALREADY ANSWERED (RESEARCH §Wave-0 Diagnostic Answers, 2026-06-12): UAT target = engagement dossier b0000002-0000-0000-0000-000000000001 "Bilateral consultation — ESCWA" (sensitivity 2 ≤ test-user clearance 3); 2 published positions exist for attach supply; the kanban renders empty (not 404) for the dual-existing seed ids — do NOT re-run these as open questions'

must_haves:
  truths:
    - 'A live attach from the engagement Positions tab persists a position_dossier_links row with dossier_id = b0000002-0000-0000-0000-000000000001 (SQL-verified) and the card renders WITHOUT reload (roadmap success criterion 2 — ENGPOS-02 live)'
    - 'A live create-new from the tab produces a positions row + an applies_to link row keyed on the engagement dossiers.id (64-06 matrix shape)'
    - 'Every formerly-disabled workspace CTA is observed GONE or FUNCTIONAL live: Create task ×3 → tasks row with engagement_id IS NULL + work_item_dossiers row; Add event ×2 → calendar_entries row with dossier_id = engagement id rendering in the Scheduled events section without reload; removed buttons absent (ENGPOS-03 acceptance)'
    - 'AR/RTL pass: tab label المواقف, dir=rtl with Tajawal, no horizontal overflow at 1280 and 1024'
    - 'Full suite + type-check + size-limit green; every verification row cleaned off staging with SELECT-count-0 confirmation; engagement_positions still 0 rows at phase close'
  artifacts:
    - path: '.planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-06-SUMMARY.md'
      provides: 'Live verification matrix: per-requirement evidence, SQL outputs, AR/width evidence references, gate outputs, cleanup confirmation, ENGPOS-01 decision + deferral roll-up'
  key_links:
    - from: 'live browser session (engagement workspace Positions tab)'
      to: 'positions-dossiers-create edge → position_dossier_links'
      via: 'AttachPositionDialog onAttach + NewPositionDialog two-step submit'
      pattern: 'positions-dossiers-create'
    - from: 'service-role SQL verification'
      to: 'position_dossier_links / tasks / work_item_dossiers / calendar_entries'
      via: 'mcp__supabase__execute_sql'
      pattern: "dossier_id = 'b0000002-0000-0000-0000-000000000001'"
---

<objective>
Run the phase-gate live verification on staging (ORCHESTRATOR-RUN — requires Supabase MCP + agent-browser, which executor agents lack): the routed Positions tab on the canonical plane, attach + create-new with DB evidence, the full nine-CTA disposition pass, AR/RTL + widths, gates, and cleanup.

Purpose: roadmap success criteria 2 and 3 are explicitly live criteria ("live-verified on staging", "re-enabled and functional") — build/type/unit gates alone cannot prove RLS, cache-invalidation render behavior, or visual RTL (milestone norm, 62/63/64 precedent).
Output: recorded verification matrix in the SUMMARY; staging left clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-RESEARCH.md
@.planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live Positions tab — attach + create-new on the ESCWA engagement + DB verification (ENGPOS-01/-02)</name>
  <files>none (live verification via agent-browser + Supabase MCP)</files>
  <read_first>
    - .planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-VALIDATION.md (§Live verification protocol — the binding step list)
    - .planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-01-SUMMARY.md (route landed; if 65-01 did not complete, STOP)
    - .env.test (TEST_USER_EMAIL / TEST_USER_PASSWORD for login — NEVER echo values)
  </read_first>
  <action>
    1. Ensure the dev server is up (cd frontend && pnpm dev in the background if :5173 is not responding; PORT=5001 + VITE_BACKEND_PROXY_TARGET if macOS AirPlay holds :5000). Login at http://localhost:5173 with the .env.test credentials via agent-browser.
    2. Navigate to /engagements/b0000002-0000-0000-0000-000000000001 — confirm the Positions tab appears in the nav between Context and Tasks. Click it: URL becomes /engagements/<id>/positions and the tab body renders (list may be empty; Create position button enables after the useDossier fetch resolves — Pitfall 5, expect a brief delay, not a defect).
    3. Attach-existing (ENGPOS-02): open Attach existing position → the dialog lists the 2 published positions (Wave-0 Q2) → select one → confirm success toast AND the card renders in the tab WITHOUT any reload. Screenshot before/after. MCP verify: SELECT id, position_id, dossier_id, link_type, created_at FROM position_dossier_links WHERE dossier_id = 'b0000002-0000-0000-0000-000000000001' ORDER BY created_at DESC LIMIT 3; — expect the new row (link_type related_to, the carried D-09 posture). Record output verbatim.
    4. Create-new: Create position → fill title_en 'Phase 65 live verification' / title_ar 'تحقق المرحلة ٦٥' → submit → success toast + card renders. MCP verify the positions row (status draft, author = test user) joined to its position_dossier_links row (dossier_id = the engagement id, link_type = 'applies_to').
    5. Record the ENGPOS-01 implementation evidence: the rendered tab IS reading position_dossier_links keyed by the engagement dossiers.id (the rows from steps 3-4 are what the tab shows). Do not clean up yet — Tasks 2-3 reuse the session/rows.
  </action>
  <verify>
    <automated>MISSING — live staging verification, manual-only justified per 65-VALIDATION.md (live RLS + TanStack cache behavior); the gate is the recorded SQL output showing both link rows for the engagement dossier id plus the without-reload screenshots referenced in the SUMMARY.</automated>
  </verify>
  <acceptance_criteria>
    - Positions tab routed, navigable, and rendering canonical rows on staging
    - Attach row + create-new applies_to row SQL-verified with dossier_id = the engagement id; both rendered without reload (same-session screenshots)
  </acceptance_criteria>
  <done>Roadmap success criteria 1 (implemented) and 2 (live-verified) are observed and recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Nine-CTA disposition pass + AR/RTL + widths (ENGPOS-03)</name>
  <files>none (live verification via agent-browser + Supabase MCP)</files>
  <read_first>
    - .planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-UI-SPEC.md (§2 disposition table — the binding GONE/LIVE checklist; §Verification Checklist)
  </read_first>
  <action>
    1. GONE checks (screenshots per surface): WorkspaceShell header has no Advance Stage button (Log After-Action + stepper present); OverviewTab quick actions has no Advance Stage; ContextTab has no Link Dossier in empty or populated state; DocsTab has no Upload (Generate Briefing present). No empty action containers.
    2. Create task LIVE checks: from OverviewTab, click Create task → TaskDialog opens with the engagement context badge → submit a task titled 'Phase 65 CTA verification'. Confirm the dialog's success toast. MCP verify: the tasks row exists with engagement_id IS NULL (Pitfall 4 — the legacy FK must NOT be set) AND a work_item_dossiers row links it to dossier_id = the engagement id. Repeat open/close (no submit) from BOTH TasksTab buttons (empty-state if reachable, else header) to confirm they are live. Observe and record the kanban caveat honestly: the board does NOT show the created task (legacy assignments plane — expected, recorded in 65-04), and the empty-state copy points to the main tasks list.
    3. Add event LIVE checks: from CalendarTab, click Add event (header) → EventDialog opens → submit an event 'Phase 65 event verification' dated tomorrow. Confirm: the Scheduled events section renders the new row WITHOUT reload (the CTA 6-7 acceptance), day-first date format. MCP verify the calendar_entries row (dossier_id = engagement id, organizer = test user).
    4. AR/RTL pass: click the topbar ع (language persists under id.locale). Confirm: Positions tab label المواقف, document dir=rtl with Tajawal, the Scheduled events heading/empty strings in Arabic, the created entry's date in Arabic month names with correct digits (toFormatLocale), no horizontal overflow. Screenshot at 1280px and 1024px in BOTH locales (4 screenshots minimum). Switch back to EN.
    5. Record the per-CTA matrix (1-9: surface, disposition, observed result, evidence ref) for the SUMMARY.
  </action>
  <verify>
    <automated>MISSING — live visual + RLS verification, manual-only justified per 65-VALIDATION.md; evidence = the per-CTA matrix + SQL outputs + the 4 width/locale screenshots referenced in the SUMMARY.</automated>
  </verify>
  <acceptance_criteria>
    - All nine dispositions observed exactly per UI-SPEC §2 (4 GONE, 5 LIVE with write+render paths)
    - tasks row has engagement_id NULL; work_item_dossiers + calendar_entries rows SQL-verified; event rendered in-tab without reload
    - AR/RTL + 1280/1024 evidence recorded; kanban caveat recorded honestly
  </acceptance_criteria>
  <done>Roadmap success criterion 3 (no inert buttons — re-enabled and functional, or removed) is observed live and recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gates, staging cleanup, and phase roll-up</name>
  <files>none (gates + live cleanup via Supabase MCP)</files>
  <read_first>
    - .planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-VALIDATION.md (§Sampling Rate — the phase-gate definition)
    - frontend/.size-limit.json (current budgets; project memory: grep ALL exceeded lines — failures stack hidden)
  </read_first>
  <action>
    1. Gates: cd frontend && pnpm exec vitest run (full suite), pnpm type-check, and pnpm exec size-limit 2>&1 | tee /tmp/size-limit-65.log with a case-insensitive grep for 'exceeded' — zero matches required. If a budget trips, record exact numbers and STOP for a budget discussion (never silently bump .size-limit.json).
    2. Cleanup via mcp__supabase__execute_sql (service role), dependency order, filtered by the ids recorded in Tasks 1-2: position_dossier_links → position_audience_groups → position_versions → positions (the create-new row + the attach link row only — NOT the pre-existing published positions); work_item_dossiers → tasks; calendar_entries. Confirm SELECT count = 0 per id per table.
    3. Final state checks: SELECT count(*) FROM engagement_positions; must still be 0 (the legacy plane stayed untouched all phase). Re-confirm position_dossier_links pre-existing rows (2 at phase start) are intact.
    4. Compile the SUMMARY: requirement → evidence matrix (ENGPOS-01 decision text + implementation; ENGPOS-02 SQL + no-reload; ENGPOS-03 nine-CTA matrix), gate outputs, cleanup confirmation, and the phase roll-up of recorded deferrals/fences from 65-03/65-04 (edge undeploy deferred; kanban canonicalization follow-up; briefing-packs-generate + positions-list hazards; EngagementDetailPage + usePositionSuggestions plumbing dead-code candidates). Note that REQUIREMENTS.md checkbox flips happen at /gsd:verify-work. No secrets anywhere.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run && pnpm type-check && pnpm exec size-limit 2>&1 | tee /tmp/size-limit-65.log && ! grep -qi 'exceeded' /tmp/size-limit-65.log</automated>
  </verify>
  <acceptance_criteria>
    - Full suite exits 0; type-check exits 0; zero size-limit exceeded lines (whole log)
    - All verification rows deleted with SELECT-count-0 proof; pre-existing data intact; engagement_positions still 0 rows
    - SUMMARY contains the complete evidence matrix + roll-up with zero secret values
  </acceptance_criteria>
  <done>The phase is gate-green, all three requirements live-verified, staging clean — ready for /gsd:verify-work.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                             |
| ----------------------------------- | ------------------------------------------------------- |
| test credentials → browser session  | .env.test secrets used for live login                   |
| verification SQL → staging database | service-role reads/deletes with elevated MCP privileges |

## STRIDE Threat Register

| Threat ID | Category               | Component                              | Disposition | Mitigation Plan                                                                                                                              |
| --------- | ---------------------- | -------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| T-65-17   | Information Disclosure | credentials/JWTs leaking into evidence | mitigate    | Secrets sourced from .env.test as env vars; acceptance criteria forbid secret values in SUMMARY, logs, or screenshots of credentials         |
| T-65-18   | Tampering              | service-role cleanup deletes           | mitigate    | Deletes filtered by the recorded verification-row ids only, dependency order, SELECT-count-0 confirmation; pre-existing rows asserted intact |
| T-65-19   | Tampering              | verifying against the wrong plane      | mitigate    | All SQL keyed on the canonical engagement dossiers.id; engagement_positions asserted STILL 0 rows at close                                   |
| T-65-SC   | Tampering              | npm/pip/cargo installs                 | accept      | No package installs in this plan                                                                                                             |

</threat_model>

<verification>
- 65-VALIDATION.md manual-only rows realized: attach persists+renders without reload (steps 2-3), create-new (step 4), CTA functional pass (step 5), AR/RTL+widths (step 6), cleanup (step 7) → Tasks 1-3
- Phase gate realized: full suite green + live staging verification before /gsd:verify-work
</verification>

<success_criteria>

- All three roadmap success criteria observed live with SQL + screenshot evidence
- Gates green; staging clean; deferrals and fences rolled up for the phase record
  </success_criteria>

<output>
Create `.planning/phases/65-engagement-positions-tab-legacy-reconciliation/65-06-SUMMARY.md` when done (evidence matrix, no secrets).
</output>
