---
phase: 101-ci-gates-green
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/vitest.integration.config.ts
  - .planning/phases/101-ci-gates-green/101-03-SUMMARY.md
autonomous: true
requirements: [CARRY-03]
success_criterion: 'The integration vitest config discovers exactly the 31 files CI run 31848669701 passed and nothing else, and the SUMMARY records decision D-3 as RESOLVED (option c) in the exact line shape the criterion-2 oracle greps, with the 204 named integration-in-name-only and option (a) recommended as a backlog requirement.'
routing:
  floor: frontier
truths:
    - 'Decisions covered - D-04: D-3 is RESOLVED as option (c) - the integration job stops asserting what it cannot measure: include is narrowed to the files CI already passes (31, derived from run 31848669701 and re-derived by the executor, never trusted), continue-on-error stays until a green run proves it can be dropped, the decision is written into this SUMMARY as a line matching ^- \*\*D-3\*\* .*RESOLVED, the 204 are named integration-in-name-only until option (a) is funded, and option (a) is RECOMMENDED as a backlog requirement - recorded, not created; D-13: no vitest RUN during planning (vitest list --filesOnly is discovery, not execution).'
must_haves:
  truths:
    - 'The SUMMARY carries the derivation of the 31 with its command and count (the CI log of job 94920141442, ANSI-stripped, the check-mark file rows minus the cross/chevron rows, 30 under tests/ plus src/utils/__tests__/validation.test.ts = 31, matching the log summary line 204 failed | 31 passed (235)), the observation that the base config include LEAKS into the integration config through mergeConfig array concatenation (which is why 236 files are discovered today and why the 31 are unit and intelligence files already covered by Tests (backend)), the D-3 RESOLVED line verbatim, the option (a) recommendation text, and a stated bound: narrowing include does not establish that any of the 204 excluded files is correct or incorrect - they were never measured against a database in CI and still are not.'
    - oracle: command
      command: |-
        ROOT=$(git rev-parse --show-toplevel 2>/dev/null) || { echo "INSTRUMENT-CANNOT-RUN: not in a git tree"; exit 3; }; cd "$ROOT/backend" || exit 3; PATH="/opt/homebrew/bin:$HOME/bin:$PATH"
        command -v pnpm >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: pnpm absent"; exit 3; }
        OUT=$(pnpm exec vitest list --filesOnly --config ./vitest.integration.config.ts 2>&1); RC=$?; [ "$RC" = 0 ] || { echo "INSTRUMENT-CANNOT-RUN: vitest list exited $RC :: $(printf '%s' "$OUT" | tail -3)"; exit 3; }
        FILES=$(printf '%s\n' "$OUT" | grep -E '\.test\.ts$' | sort -u); N=$(printf '%s\n' "$FILES" | grep -c .)
        CTRL=$(pnpm exec vitest list --filesOnly 2>/dev/null | grep -cE '\.test\.ts$')
        WANT='src/utils/__tests__/validation.test.ts
        tests/contract/ai-extract-status.test.ts
        tests/contract/ai-extract.test.ts
        tests/contract/pdf-generate.test.ts
        tests/deadline-checker.test.ts
        tests/digest-scheduler.test.ts
        tests/email-notifications.test.ts
        tests/intelligence/alert-fanout.integration.test.ts
        tests/intelligence/alert-rules.test.ts
        tests/intelligence/channel-adapter.test.ts
        tests/intelligence/digest-cron.integration.test.ts
        tests/intelligence/generate-digest.integration.test.ts
        tests/intelligence/subscriptions.test.ts
        tests/intelligence/webhook-payload-contract.test.ts
        tests/notification-queue.test.ts
        tests/push-notifications.test.ts
        tests/services/interaction-note-service.test.ts
        tests/unit/ai-briefs-manual.test.ts
        tests/unit/auth.service.test.ts
        tests/unit/auto-assignment-scoring.test.ts
        tests/unit/brief-generator-jwt.test.ts
        tests/unit/intake-linker-jwt.test.ts
        tests/unit/mfa-crypto.test.ts
        tests/unit/queue-processing.test.ts
        tests/unit/rate-limit.service.test.ts
        tests/unit/reembed-rag-chunks.test.ts
        tests/unit/reporting.service.test.ts
        tests/unit/search.service.test.ts
        tests/unit/sla-calculation.test.ts
        tests/unit/validation.test.ts
        tests/unit/vector.service.test.ts'
        TA=$(mktemp); TB=$(mktemp); printf '%s\n' "$FILES" > "$TA"; printf '%s\n' "$WANT" | sort > "$TB"; EXTRA=$(comm -23 "$TA" "$TB" | grep -c .); MISS=$(comm -13 "$TA" "$TB" | grep -c .); rm -f "$TA" "$TB"
        G=$(grep -cE '^- \*\*D-3\*\* .*RESOLVED' ../.planning/phases/101-ci-gates-green/101-03-SUMMARY.md 2>/dev/null); G=${G:-0}
        echo "P101-03 integration config discovers=$N (base config control=$CTRL) extra_vs_ci_green_list=$EXTRA missing=$MISS d3_resolved_lines=$G want discovers=31 extra=0 missing=0 d3=1"
        [ "${CTRL:-0}" -ge 1 ] || { echo "INSTRUMENT-CANNOT-RUN: the base config lists zero files - vitest list is not discovering anything"; exit 3; }
        [ "$N" = 31 ] && [ "$EXTRA" = 0 ] && [ "$MISS" = 0 ] || { echo "FAIL: the integration job would still collect $N files ($EXTRA outside the CI-green 31, $MISS of the 31 missing) - D-04 narrows include to exactly the files run 31848669701 passed"; exit 1; }
        [ "${G:-0}" = 1 ] || { echo "FAIL: 101-03-SUMMARY.md carries $G '- **D-3** ... RESOLVED' lines, want exactly 1"; exit 1; }
        echo PASS
      text: 'discovery, not execution: vitest list --filesOnly under the integration config must return exactly the 31 files named inline (hard-coded, sorted), with the base config listed as a control that discovery answers at all (26 files today), zero files outside the 31 and zero of the 31 missing; and 101-03-SUMMARY.md must carry exactly one line matching ^- \*\*D-3\*\* .*RESOLVED. Drilled 2026-09-10 at HEAD: exit 1 - discovers=236, 205 outside the list, d3 lines 0.'
    - oracle: judge
      text: 'The SUMMARY records the 31-file derivation with its command and the 204 failed | 31 passed summary line quoted, explains the mergeConfig concatenation as the mechanism, states the D-3 resolution as option (c) in the required line shape, names the 204 as integration-in-name-only, recommends option (a) as a backlog requirement without creating one, and states the bound that nothing about the 204 is established.'
---

<objective>
Close the local half of criterion 2: resolve and RECORD decision D-3, and make the integration job stop
asserting what it cannot measure (D-04, option c).

What RESEARCH §4 found. D-3 is a label for an open question ("how 235 integration test files get a
database"), not a decision. CI ran 235 files and 204 failed on `ECONNREFUSED` to `localhost:54321`; no
Supabase service has ever run in CI. Option (a) - `supabase start` in CI, 88 migrations from zero - is a
phase of its own; option (b) - pointing CI at staging - is REJECTED because it makes the integration job a
staging mutator.

What the planner measured tonight (§10). `backend/vitest.integration.config.ts` builds its config with
`mergeConfig(baseConfig, ...)`, and Vite's mergeConfig CONCATENATES arrays - so the integration `include`
is the base include (unit, services, security, intelligence, five root files, `src/utils/__tests__`) PLUS
the four integration globs. `vitest list --filesOnly` discovers 236 files today (235 in CI on 2026-08-14).
The 31 files CI passed are, by that mechanism, the base-config files that need no database: 30 under
`tests/` plus `src/utils/__tests__/validation.test.ts`. The `exclude` key is already assigned AFTER the
merge (`integrationConfig.test = {...}`); `include` is set the same way, and the leak stops.
</objective>

<context>
@.planning/phases/101-ci-gates-green/101-CONTEXT.md
@.planning/phases/101-ci-gates-green/101-RESEARCH.md

Ref notes: 101-CONTEXT.md - D-04 (option c, the line shape, the recommendation), D-13; 101-RESEARCH.md - §4 (what runs, the 54321 population, what D-3 says, the three options with cost), §8.2 (the criterion-2 oracle and its D-3 grep), §9 F6/F7, §10 (the 31-file derivation and the mergeConfig finding).

<interfaces>

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>the integration config discovers only what CI can pass and D-3 is recorded as resolved</name>
  <files>the paths in files_modified</files>
  <read_first>101-CONTEXT.md D-01..D-16 (ALL of them) and 101-RESEARCH.md §0-§10 (§8 holds the drilled oracle candidates, §10 the planner drills of 2026-09-10); re-RUN a recorded command rather than re-deriving its number a second way; then `backend/vitest.config.ts` (the base include) and `backend/vitest.integration.config.ts` (the mergeConfig shape)</read_first>
  <action>
    1. RE-DERIVE the 31 before trusting them (D-04: "the executor re-derives, never trusts 31"):
       ```
       gh run view -R alzahrani-khalid/Intl-Dossier-V2.0 --job 94920141442 --log-failed \
         | sed -E 's/\^\[\[[0-9;]*m//g' > /tmp/p101-int.log      # the log carries ESC as the two characters ^[
       grep -oE '✓ tests/[A-Za-z0-9_./-]+\.test\.ts|✓ src/[A-Za-z0-9_./-]+\.test\.ts' /tmp/p101-int.log | sed 's/✓ //' | sort -u   # 31 lines
       grep -oE '(❯|×) tests/[A-Za-z0-9_./-]+\.test\.ts' /tmp/p101-int.log | sed -E 's/^(❯|×) //' | sort -u | wc -l          # 204
       grep -E 'Test Files' /tmp/p101-int.log | tail -1                                                                      # 204 failed | 31 passed (235)
       ```
       The passed set and the failed set must not overlap (`comm -12` = 0). If your 31 differ from the
       list hard-coded in this plan's oracle, the oracle is wrong and you STOP and record the difference -
       you do not edit the oracle and you do not pick the list that passes.
    2. `backend/vitest.integration.config.ts`: after the existing `integrationConfig.test = { ...,
       exclude: [...] }` assignment, set `include` EXPLICITLY to the 31 paths (relative to `backend/`),
       one per line, sorted, with a comment block that (a) names run 31848669701 / job 94920141442 as the
       source, (b) explains the mergeConfig concatenation leak, (c) says "D-3 RESOLVED option (c), Phase
       101 - the 204 excluded files are integration-in-name-only until option (a) (supabase start in CI)
       is funded". Remove the four integration globs from the `mergeConfig` call (they would only be
       overwritten). Keep `testTimeout`/`hookTimeout` and `exclude` as they are.
    3. Verify discovery WITHOUT a run: `cd backend && pnpm exec vitest list --filesOnly --config ./vitest.integration.config.ts`
       must list exactly the 31; `pnpm exec vitest list --filesOnly` (base config) is the control that
       discovery answers at all. Record both outputs. Do NOT run the suite (D-13; and the run itself is
       what CI does under P101-07).
    4. Write the SUMMARY. It MUST contain exactly one line of the form
       `- **D-3** (the missing local Supabase DB at localhost:54321) RESOLVED as option (c) on 2026-09-1N: ...`
       - the criterion-2 oracle greps `^- \*\*D-3\*\* .*RESOLVED`. In the same section: the 204 are
       "integration-in-name-only until option (a) is funded", and one paragraph recommending option (a) as
       a backlog requirement (name it, e.g. `CI-DB-01: supabase start + db reset in the integration job`)
       WITHOUT creating it in REQUIREMENTS.md or ROADMAP.md (out of this plan's write scope).
    5. Run this plan's oracle and paste its output. State the bound: nothing about the 204 is established.
  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes.</acceptance_criteria>
  <done>The integration config discovers exactly the 31 CI-green files, the base config still discovers its own set as the control, and the SUMMARY carries the D-3 RESOLVED line, the 204 named honestly, and the option (a) recommendation.</done>
</task>

</tasks>

<verification>
POPULATION: `backend/vitest.integration.config.ts` and this plan's SUMMARY. OUTSIDE IT: the 204 test files
(untouched - not fixed, not deleted, not annotated), `backend/vitest.config.ts` (the base config is not this
plan's), the `test-integration` job definition and its `continue-on-error` (P101-04 owns ci.yml), and the
CI observation of the job going green (P101-07, criterion 2's other half).
</verification>

<success_criteria>
The integration vitest config discovers exactly the 31 files CI run 31848669701 passed and nothing else, and the SUMMARY records decision D-3 as RESOLVED (option c) in the exact line shape the criterion-2 oracle greps, with the 204 named integration-in-name-only and option (a) recommended as a backlog requirement.
</success_criteria>

<output>
Create `.planning/phases/101-ci-gates-green/101-03-SUMMARY.md` when done. It MUST record every command this
plan ran with its VERBATIM output, the populations it re-derived with the commands that produced them, every
zero beside the control that proves the instrument could have seen a non-zero, and anything left for a named
later task. It MUST carry the 31-file derivation, the mergeConfig finding, the D-3 RESOLVED line, the option (a) recommendation, and the bound on the 204.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete`.
Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as
**pending** no matter how many gates passed (measured on P99-32 and again on P99-61).
</output>
