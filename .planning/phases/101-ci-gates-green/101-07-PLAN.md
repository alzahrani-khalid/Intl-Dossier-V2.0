---
phase: 101-ci-gates-green
plan: 7
type: execute
wave: 4
depends_on: [101-01, 101-02, 101-03, 101-04, 101-05, 101-06]
files_modified:
  - .planning/phases/101-ci-gates-green/101-07-ATTESTATION.md
  - .planning/phases/101-ci-gates-green/101-07-SUMMARY.md
autonomous: false
requirements: [CARRY-02, CARRY-03, CARRY-05, CARRY-09]
success_criterion: 'On the latest main runs created after the phase PR merged, both e2e.yml shards and the six named ci.yml jobs - Security Scan, Tests (integration), Accessibility Tests (RTL + WCAG AA), RTL Portal + Component Smokes, RTL + Responsive Tests, Docker Build - conclude success with the CI run reporting exactly 18 jobs and no E2E Tests job; RTL Portal + Component Smokes is then the ninth required status context on main, added by job name after that job was green; and a smoke PR carrying a deliberately red smoke is observed mergeStateStatus BLOCKED with that check failing, all attested by the operator in a file the SUMMARY quotes verbatim.'
routing:
  floor: frontier
truths:
    - 'Decisions covered - D-12: criteria 1, 2 and 5 close on ONE operator act - merge the phase PR and read the latest main run - so this plan carries merge, observe, POST, smoke PR, attestation in order; D-03: the POST comes AFTER the criterion-5 run shows RTL Portal + Component Smokes = success, never before, because under strict true and enforce_admins true a required context that is red blocks every merge; D-02: the context string is the job NAME RTL Portal + Component Smokes, and the oracle FAILS if the key test-rtl-smokes is ever added; D-14: the criterion-5 oracle spells out 6 jobs, 6 success, total 18, E2E Tests 0 - the population D-07 and D-01 produced; D-01: Security Scan is in that population; D-16: the phase directory is committed before any engine run, so the PR contains the plans and SUMMARYs.'
must_haves:
  truths:
    - 'The SUMMARY quotes .planning/phases/101-ci-gates-green/101-07-ATTESTATION.md VERBATIM (who merged which PR at what time, the main run ids observed, the exact POST command and its response, the smoke PR number and the mergeStateStatus read), carries every oracle output verbatim with the run ids and creation times it read, and states the bounds: a green main run establishes that the suites as SHAPED by this phase are green - the 204 integration files, the quarantined E2E/a11y/RTL tests and the app defects behind them are not established (CONTEXT §5); the smoke PR is closed unmerged and its branch deleted, recorded in the attestation; and the wrapper first ran on Linux in this run, so a wrapper-caused red is P101-04 defect, named as such if it occurs.'
    - oracle: command
      command: |-
        PATH="/opt/homebrew/bin:$HOME/bin:$PATH"; command -v gh >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: gh absent"; exit 3; }; gh auth status >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: gh not authenticated"; exit 3; }; R=alzahrani-khalid/Intl-Dossier-V2.0
        RUN=$(gh run list -R $R --branch main --workflow "E2E Tests" --limit 1 --json databaseId,conclusion,createdAt -q '.[0]' 2>&1); RC=$?; [ "$RC" = 0 ] || { echo "INSTRUMENT-CANNOT-RUN: gh run list exited $RC :: $RUN"; exit 3; }
        ID=$(printf '%s' "$RUN" | node -pe 'JSON.parse(require("fs").readFileSync(0,"utf8")).databaseId'); AT=$(printf '%s' "$RUN" | node -pe 'JSON.parse(require("fs").readFileSync(0,"utf8")).createdAt')
        J=$(gh run view -R $R "$ID" --json jobs -q '[.jobs[] | select(.name|startswith("E2E (shard")) | .conclusion]' 2>&1); RC=$?; [ "$RC" = 0 ] || { echo "INSTRUMENT-CANNOT-RUN: gh run view exited $RC :: $J"; exit 3; }
        C=$(gh run view -R $R "$ID" --json jobs -q '[.jobs[] | select(.name=="Merge Playwright reports") | .conclusion]' 2>&1)
        echo "P101-C1 run=$ID created=$AT shards=$J (control merge-reports=$C) expected [\"success\",\"success\"] on a run created after planning start 2026-09-10T18:00:00Z"
        [ "$C" = '["success"]' ] || { echo "INSTRUMENT-CANNOT-RUN: the control job did not read success - the job query is not answering"; exit 3; }
        [ "$AT" \> "2026-09-10T18:00:00Z" ] || { echo "FAIL: the latest main E2E run predates this phase (created $AT) - no phase PR has been merged"; exit 1; }
        [ "$J" = '["success","success"]' ] || { echo "FAIL: E2E shards not both success"; exit 1; }; echo PASS
      text: 'criterion 1 on live GitHub state: the latest E2E Tests run on main must have been created after the planning start 2026-09-10T18:00:00Z (full ISO compare - round 1, issue 7; a run that predates the phase fails - it cannot be this phase evidence), both E2E (shard n/2) jobs must conclude success, and the Merge Playwright reports job is the control that the job query answers (exit 3 if it does not read success). Drilled 2026-09-10 at HEAD and again in round 1: exit 1 - run 31848669722 created 2026-08-14, shards failure failure, control success.'
    - oracle: command
      command: |-
        PATH="/opt/homebrew/bin:$HOME/bin:$PATH"; command -v gh >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: gh absent"; exit 3; }; gh auth status >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: gh not authenticated"; exit 3; }; R=alzahrani-khalid/Intl-Dossier-V2.0
        ROOT=$(git rev-parse --show-toplevel 2>/dev/null) || { echo "INSTRUMENT-CANNOT-RUN: not in a git tree"; exit 3; }
        RUN=$(gh run list -R $R --branch main --workflow CI --limit 1 --json databaseId,createdAt -q '.[0]' 2>&1); RC=$?; [ "$RC" = 0 ] || { echo "INSTRUMENT-CANNOT-RUN: gh run list exited $RC :: $RUN"; exit 3; }
        ID=$(printf '%s' "$RUN" | node -pe 'JSON.parse(require("fs").readFileSync(0,"utf8")).databaseId'); AT=$(printf '%s' "$RUN" | node -pe 'JSON.parse(require("fs").readFileSync(0,"utf8")).createdAt')
        C=$(gh run view -R $R "$ID" --json jobs -q '[.jobs[] | select(.name=="Tests (integration)") | .conclusion]' 2>&1); RC=$?; [ "$RC" = 0 ] || { echo "INSTRUMENT-CANNOT-RUN: gh run view exited $RC :: $C"; exit 3; }
        K=$(gh run view -R $R "$ID" --json jobs -q '[.jobs[] | select(.name=="Lint") | .conclusion]' 2>&1)
        G=$(grep -cE '^- \*\*D-3\*\* .*RESOLVED' "$ROOT/.planning/phases/101-ci-gates-green/101-03-SUMMARY.md" 2>/dev/null); G=${G:-0}
        echo "P101-C2 run=$ID created=$AT integration=$C (control Lint=$K) d3_resolved_lines=$G expected [\"success\"] on a run after 2026-09-10T18:00:00Z and d3=1"
        [ "$K" = '["success"]' ] || { echo "INSTRUMENT-CANNOT-RUN: the control job Lint did not read success"; exit 3; }
        [ "$AT" \> "2026-09-10T18:00:00Z" ] || { echo "FAIL: the latest main CI run predates this phase (created $AT)"; exit 1; }
        [ "$C" = '["success"]' ] && [ "$G" = 1 ] || { echo "FAIL: integration=$C d3_lines=$G"; exit 1; }; echo PASS
      text: 'criterion 2 on live state plus the recorded decision: the latest CI run on main created after 2026-09-10T18:00:00Z must show Tests (integration) success (Lint is the control), and 101-03-SUMMARY.md must carry exactly one line matching ^- \*\*D-3\*\* .*RESOLVED. Drilled 2026-09-10 at HEAD and again in round 1: exit 1 - run 31848669701 created 2026-08-14, integration failure, d3 lines 0, control success.'
    - oracle: command
      command: |-
        PATH="/opt/homebrew/bin:$HOME/bin:$PATH"; command -v gh >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: gh absent"; exit 3; }; gh auth status >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: gh not authenticated"; exit 3; }; R=alzahrani-khalid/Intl-Dossier-V2.0
        RUN=$(gh run list -R $R --branch main --workflow CI --limit 1 --json databaseId,createdAt -q '.[0]' 2>&1); RC=$?; [ "$RC" = 0 ] || { echo "INSTRUMENT-CANNOT-RUN: gh run list exited $RC :: $RUN"; exit 3; }
        ID=$(printf '%s' "$RUN" | node -pe 'JSON.parse(require("fs").readFileSync(0,"utf8")).databaseId'); AT=$(printf '%s' "$RUN" | node -pe 'JSON.parse(require("fs").readFileSync(0,"utf8")).createdAt')
        S=$(gh run view -R $R "$ID" --json jobs -q '[.jobs[] | select(.name=="Security Scan" or .name=="Tests (integration)" or .name=="Accessibility Tests (RTL + WCAG AA)" or .name=="RTL Portal + Component Smokes" or .name=="RTL + Responsive Tests" or .name=="Docker Build") | .conclusion] | "\(length) jobs, \(map(select(.=="success"))|length) success"' 2>&1); RC=$?; [ "$RC" = 0 ] || { echo "INSTRUMENT-CANNOT-RUN: gh run view exited $RC :: $S"; exit 3; }
        T=$(gh run view -R $R "$ID" --json jobs -q '.jobs | length' 2>&1); E=$(gh run view -R $R "$ID" --json jobs -q '[.jobs[] | select(.name=="E2E Tests")] | length' 2>&1)
        echo "P101-C5 run=$ID created=$AT $S (total jobs=$T, 'E2E Tests' jobs=$E) expected 6 jobs, 6 success, total 18, E2E Tests 0, run after 2026-09-10T18:00:00Z"
        [ "$AT" \> "2026-09-10T18:00:00Z" ] || { echo "FAIL: the latest main CI run predates this phase (created $AT)"; exit 1; }
        [ "$S" = "6 jobs, 6 success" ] && [ "$T" = 18 ] && [ "$E" = 0 ] || { echo "FAIL: population or conclusions off - $S total=$T e2e_tests=$E (D-14: a smaller population passing silently is the failure this guards)"; exit 1; }; echo PASS
      text: 'criterion 5 with the population spelled out (D-14): on the latest CI run on main created after 2026-09-10T18:00:00Z, the six names must read 6 jobs, 6 success, the run must report 18 jobs in total and zero jobs named E2E Tests - a renamed or deleted job shrinks the count and fails rather than passing on a smaller set. Drilled 2026-09-10 at HEAD and again in round 1: exit 1 - 6 jobs, 0 success, total 19, E2E Tests 1, created 2026-08-14.'
    - oracle: command
      command: |-
        PATH="/opt/homebrew/bin:$HOME/bin:$PATH"; command -v gh >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: gh absent"; exit 3; }; gh auth status >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: gh not authenticated"; exit 3; }; R=alzahrani-khalid/Intl-Dossier-V2.0
        ROOT=$(git rev-parse --show-toplevel 2>/dev/null) || { echo "INSTRUMENT-CANNOT-RUN: not in a git tree"; exit 3; }
        C=$(gh api repos/$R/branches/main/protection/required_status_checks/contexts 2>&1); RC=$?; [ "$RC" = 0 ] || { echo "INSTRUMENT-CANNOT-RUN: gh api exited $RC :: $C"; exit 3; }
        N=$(printf '%s' "$C" | node -pe 'JSON.parse(require("fs").readFileSync(0,"utf8")).length'); printf '%s' "$C" | grep -q '"Lint"' || { echo "INSTRUMENT-CANNOT-RUN: the control context Lint is not in the list - the API is not answering"; exit 3; }
        A=.planning/phases/101-ci-gates-green/101-07-ATTESTATION.md; PR=$(grep -oE '^SMOKE_PR=[0-9]+' "$ROOT/$A" 2>/dev/null | head -1 | cut -d= -f2)
        if [ -n "$PR" ]; then MS=$(gh pr view -R $R "$PR" --json mergeStateStatus -q .mergeStateStatus 2>&1); SC=$(gh pr checks -R $R "$PR" --json name,state -q '.[] | select(.name=="RTL Portal + Component Smokes") | .state' 2>/dev/null | head -1); else MS="<no SMOKE_PR= line in $A>"; SC="<n/a>"; fi
        echo "P101-C4 contexts_count=$N (8 at planning) has_name=$(printf '%s' "$C" | grep -c '"RTL Portal + Component Smokes"') has_key=$(printf '%s' "$C" | grep -c '"test-rtl-smokes"') smoke_pr=${PR:-<none>} mergeStateStatus=$MS smoke_check=$SC expected has_name=1 has_key=0 count=9 mergeStateStatus=BLOCKED smoke_check=FAILURE (gh --json state vocabulary is upper-case)"
        printf '%s' "$C" | grep -q '"test-rtl-smokes"' && { echo "FAIL: the KEY test-rtl-smokes was added, not the job NAME - a context no job reports blocks every PR forever (D-02)"; exit 1; }
        printf '%s' "$C" | grep -q '"RTL Portal + Component Smokes"' || { echo "FAIL: context 'RTL Portal + Component Smokes' absent from required_status_checks"; exit 1; }
        [ "$N" = 9 ] || { echo "FAIL: $N required contexts, want exactly 9 (the eight at planning plus this one)"; exit 1; }
        [ -n "$PR" ] || { echo "FAIL: no SMOKE_PR=<n> line in $A - the observed-BLOCKED half of criterion 4 has no subject"; exit 1; }
        [ "$MS" = BLOCKED ] && [ "$SC" = FAILURE ] || { echo "FAIL: smoke PR #$PR mergeStateStatus=$MS smoke_check=$SC - want BLOCKED with the smoke check FAILURE"; exit 1; }; echo PASS
      text: 'criterion 4 in both halves: the required contexts on main must contain the NAME RTL Portal + Component Smokes and must NOT contain the KEY test-rtl-smokes (the neighbouring-wrong case that blocks every PR forever), the count must be exactly 9 (the eight at planning plus this one; Lint is the control that the API answers), and the attestation file must carry a SMOKE_PR=<n> line whose PR reads mergeStateStatus BLOCKED with the RTL Portal + Component Smokes check in state FAILURE, read with gh pr checks --json name,state (the job name contains spaces, so a whitespace-split awk read the second WORD of the name and could never match - the round-1 BLOCKER; the JSON state vocabulary is upper-case, measured FAILURE on PR 98). Drilled 2026-09-10 at HEAD and again in round 1: exit 1 - 8 contexts, name absent, key absent, no attestation (the context-absent arm fires first at HEAD, so the smoke-check arm is verified by the PR-98 measurement, not by this drill).'
    - oracle: judge
      text: 'The SUMMARY quotes the attestation verbatim (never paraphrased), carries all four oracle outputs with run ids and creation times after the merge, records the ordering merge - observe green smoke row - POST - smoke PR - BLOCKED as performed in that order with timestamps, records the smoke PR closed unmerged, and states the three bounds in those terms.'
---

<objective>
Close criteria 1, 2, 4 and 5 on the only thing that can close them: a `main` run produced by merging the
phase PR, then the branch-protection POST, then a smoke PR observed BLOCKED (D-12, D-03). Nothing here
runs in a worktree; the worker quotes, reads live state, and records.

Order is the substance (RESEARCH §2, §8.4, §9 F8): `RTL Portal + Component Smokes` was 6/9 on `main`.
Under `strict: true` + `enforce_admins: true`, promoting it before it is green blocks every merge - so the
POST is performed only after the criterion-5 run shows that job `success`. And the string is the job NAME
(`ci.yml:381`), never the key `test-rtl-smokes` (`ci.yml:380`): a context no job reports can never turn
green.
</objective>

<context>
@.planning/phases/101-ci-gates-green/101-CONTEXT.md
@.planning/phases/101-ci-gates-green/101-RESEARCH.md

Ref notes: 101-CONTEXT.md - D-01, D-02, D-03, D-12, D-14, D-16, §5 (what stays unestablished); 101-RESEARCH.md - §1 (the runs and jobs at planning), §2 (branch protection: the eight contexts, the exact POST recorded-not-run, PUT vs POST vs DELETE), §8.1/§8.2/§8.4/§8.5 (the oracles and their drills), §8.7 (closure map), §10.

<interfaces>

</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Operator merges the phase PR, observes main, promotes the context after the smoke job is green, and proves BLOCKED with a smoke PR</name>
  <files>none in the tree except the attestation the operator writes</files>
  <action>
    Perform IN THIS ORDER and record each step's timestamp and output in the attestation file.

    1. Commit the phase directory (D-16) and open the phase PR from `milestone/v10.0-trust` (or the
       branch the engine consolidated on) to `main` with the six worktree plans' changes and SUMMARYs.
       Wait for the eight required checks: `gh pr checks <n> --required --fail-fast`. Merge.
    2. Observe the two `main` runs the merge produced:
       `gh run list -R alzahrani-khalid/Intl-Dossier-V2.0 --branch main --limit 4 --json databaseId,workflowName,conclusion,createdAt`
       and per run `gh run view <id> --json jobs -q '.jobs[] | "\(.conclusion)\t\(.name)"'`. Record the
       ids. If either run is red, STOP: read the failing job's first cause, name the owning plan
       (P101-04 for workflow shape and the wrapper on Linux; P101-05 for a spec; P101-03 for the
       integration include; P101-06 for credentials), and do not proceed to step 3 - the attestation
       records the red run and the SUMMARY is written PARKED with that owner named.
    3. Only when the CI run shows `RTL Portal + Component Smokes` = `success` (D-03), add the context BY
       NAME (D-02) - the exact call from RESEARCH §2, recorded there and run here for the first time:
       ```
       gh api -X POST repos/alzahrani-khalid/Intl-Dossier-V2.0/branches/main/protection/required_status_checks/contexts \
         --input - <<< '["RTL Portal + Component Smokes"]'
       ```
       (POST appends; PUT would replace the whole list; DELETE with the same body removes it.) Then read
       back: `gh api repos/alzahrani-khalid/Intl-Dossier-V2.0/branches/main/protection/required_status_checks/contexts`
       must list nine, including the name, never `test-rtl-smokes`.
    4. Prove the gate blocks: branch `p101-smoke-blocked` from `main`, make ONE deliberately red smoke -
       e.g. in `frontend/tests/e2e/direction-portals.spec.ts` change one `expect(...)` to an impossible
       value - push, open a PR titled `P101 smoke: deliberately red RTL smoke (do not merge)`, wait for
       checks, then read `gh pr view <n> --json mergeStateStatus -q .mergeStateStatus` = `BLOCKED` and
       `gh pr checks <n> --json name,state` showing `RTL Portal + Component Smokes` in state `FAILURE`. Record both verbatim. Close the PR
       unmerged and delete the branch; record that too.
    5. Write `.planning/phases/101-ci-gates-green/101-07-ATTESTATION.md`:
       ```
       # ATTESTATION - P101-07 main green, context promoted, smoke PR BLOCKED
       **Who:** <name> (operator)
       **Phase PR:** #<n>, merged <ISO time>, merge commit <sha>
       **main runs observed:** CI <run id> (<conclusion>), E2E Tests <run id> (<conclusion>), created <ISO>
       **Smoke job on that CI run:** RTL Portal + Component Smokes = success at <ISO> (D-03 satisfied before the POST)
       **POST performed:** <ISO>, command as above, response: <verbatim>
       **Contexts after:** <the nine, verbatim>
       SMOKE_PR=<n>
       **Smoke PR read:** mergeStateStatus=BLOCKED at <ISO>; RTL Portal + Component Smokes = FAILURE (gh pr checks --json name,state)
       **Smoke PR closed unmerged, branch deleted:** <ISO>
       ATTESTATION-END
       ```
       The `SMOKE_PR=<n>` line must be exactly that shape at the start of a line - the criterion-4 oracle
       reads it.
  </action>
  <done>Both main runs green in the six-plus-two population, the ninth context added by name after the smoke job was green, a smoke PR observed BLOCKED with the smoke check red and then closed unmerged, all in the attestation with timestamps.</done>
</task>

<task type="auto">
  <name>the four live-state oracles are read after the operator act and the attestation is quoted</name>
  <files>the paths in files_modified</files>
  <read_first>101-CONTEXT.md D-01..D-16 (ALL of them) and 101-RESEARCH.md §0-§10 (§8 holds the drilled oracle candidates, §10 the planner drills of 2026-09-10); re-RUN a recorded command rather than re-deriving its number a second way; then the attestation file if it exists</read_first>
  <action>
    1. If the attestation does not exist, or records a red run: write the SUMMARY with `status: PARKED`,
       the owed step and the named owner from the attestation, run all four oracles anyway and paste their
       (red) output, and stop. Never auto-answer the checkpoint.
    2. Otherwise quote the attestation VERBATIM under `## Operator attestation (quoted)`.
    3. Run the four oracles (C1, C2, C5, C4 - in that order, C4 last because it depends on the POST that
       depends on C5) and paste each output verbatim. Every run id and creation time they print must be
       after the merge time in the attestation.
    4. State the three bounds from the must-have truth. Mark CONTEXT §5's list ("what this phase does NOT
       establish even when green") as still true, verbatim.
  </action>
  <acceptance_criteria>All four oracles pass on live GitHub state after the attestation's merge timestamp, the attestation is quoted verbatim, and the ordering merge - green smoke row - POST - smoke PR - BLOCKED is recorded with timestamps.</acceptance_criteria>
  <done>C1, C2, C5 and C4 read PASS on runs created after the merge, the attestation is quoted, the bounds are stated.</done>
</task>

</tasks>

<verification>
POPULATION: the latest `main` runs of `CI` and `E2E Tests` created after the phase PR merge, the
`main` required-status-check contexts, the smoke PR's merge state, the attestation file and this
plan's SUMMARY. OUTSIDE IT: every file in the worktree plans (a red run is routed to its owner, not
fixed here), the 204 integration files, the quarantined tests and the application defects behind them
(Phase 102/103), `QA Sweep` and `Visual Regression (Phase 46)` in `e2e.yml` (CARRY-06, Phase 102), and
the Lighthouse / Eval Gate steps (CONTEXT §4).
</verification>

<success_criteria>
On the latest main runs created after the phase PR merged, both e2e.yml shards and the six named ci.yml jobs conclude success with the CI run reporting exactly 18 jobs and no E2E Tests job; RTL Portal + Component Smokes is then the ninth required status context on main, added by job name after that job was green; and a smoke PR carrying a deliberately red smoke is observed mergeStateStatus BLOCKED with that check failing, all attested by the operator in a file the SUMMARY quotes verbatim.
</success_criteria>

<output>
Create `.planning/phases/101-ci-gates-green/101-07-SUMMARY.md` when done. It MUST record every command this
plan ran with its VERBATIM output, the populations it re-derived with the commands that produced them, every
zero beside the control that proves the instrument could have seen a non-zero, and anything left for a named
later task. It MUST carry the attestation QUOTED VERBATIM, all four oracle outputs with run ids and creation times after the merge, the ordering with timestamps, and the three bounds.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete` (or `status: PARKED` with the named operator step still owed, when the human gate has not yet been performed).
Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as
**pending** no matter how many gates passed (measured on P99-32 and again on P99-61).
</output>
