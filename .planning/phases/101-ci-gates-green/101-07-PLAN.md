---
phase: 101-ci-gates-green
plan: 7
type: execute
wave: 4
depends_on: [101-01, 101-02, 101-03, 101-04, 101-05, 101-06, 101-08, 101-09, 101-10, 101-12, 101-13]
files_modified:
  - .planning/phases/101-ci-gates-green/101-07-ATTESTATION.md
  - .planning/phases/101-ci-gates-green/101-07-SUMMARY.md
autonomous: false
requirements: [CARRY-02, CARRY-03, CARRY-09]
success_criterion: 'On the latest main runs created after the phase PR merged, both e2e.yml shards and the six named ci.yml jobs - Security Scan, Tests (integration), Accessibility Tests (RTL + WCAG AA), RTL Portal + Component Smokes, RTL + Responsive Tests, Docker Build - conclude success with the CI run reporting exactly 18 jobs and no E2E Tests job, and the operator attests the merge and the run ids in a file the SUMMARY quotes verbatim.'
routing:
  floor: frontier
truths:
    - 'Decisions covered - D-12: criteria 1, 2 and 5 close on ONE operator act - merge the phase PR and read the latest main run - so this plan carries merge, observe, attestation; D-14: the criterion-5 oracle spells out 6 jobs, 6 success, total 18, E2E Tests 0 - the population D-07 and D-01 produced; D-01: Security Scan is in that population; D-16: the phase directory is committed before any engine run, so the PR contains the plans and SUMMARYs; D-03 is honoured by ORDER: the branch-protection POST and the smoke PR are P101-11, which depends on this plan and runs only after this run shows RTL Portal + Component Smokes = success. Round-2 split of the former 101-07 (8 acceptance items) into 101-07 (main run) and 101-11 (protection + smoke PR).'
must_haves:
  truths:
    - oracle: command
      command: |-
        PATH="/opt/homebrew/bin:$HOME/bin:$PATH"; command -v gh >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: gh absent"; exit 3; }; gh auth status >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: gh not authenticated"; exit 3; }; R=alzahrani-khalid/Intl-Dossier-V2.0
        RUN=$(gh run list -R $R --branch main --workflow "E2E Tests" --limit 1 --json databaseId,conclusion,createdAt -q '.[0]' 2>&1); RC=$?; [ "$RC" = 0 ] || { echo "INSTRUMENT-CANNOT-RUN: gh run list exited $RC :: $RUN"; exit 3; }
        ID=$(printf '%s' "$RUN" | node -pe 'JSON.parse(require("fs").readFileSync(0,"utf8")).databaseId'); AT=$(printf '%s' "$RUN" | node -pe 'JSON.parse(require("fs").readFileSync(0,"utf8")).createdAt')
        J=$(gh run view -R $R "$ID" --json jobs -q '[.jobs[] | select(.name|startswith("E2E (shard")) | .conclusion]' 2>&1); RC=$?; [ "$RC" = 0 ] || { echo "INSTRUMENT-CANNOT-RUN: gh run view exited $RC :: $J"; exit 3; }
        C=$(gh run view -R $R "$ID" --json jobs -q '[.jobs[] | select(.name=="Merge Playwright reports") | .conclusion]' 2>&1)
        echo "P101-C1 run=$ID created=$AT shards=$J (control merge-reports=$C) expected [\"success\",\"success\"] on a run created after planning start 2026-09-10T18:00:00Z"
        [ "$C" = '["success"]' ] || { echo "INSTRUMENT-CANNOT-RUN: the control job did not read success - the job query is not answering"; exit 3; }
        [ "$AT" \> "2026-09-10T18:00:00Z" ] || { echo "FAIL: the latest main E2E run predates this phase (created $AT) - no phase PR has been merged"; exit 1; }
        [ "$J" = '["success","success"]' ] || { echo "FAIL: E2E shards not both success"; exit 1; }; echo PASS
      text: 'criterion 1 on live GitHub state: the latest E2E Tests run on main must have been created after the planning start 2026-09-10T18:00:00Z (full ISO compare; a run that predates the phase fails - it cannot be this phase evidence), both E2E (shard n/2) jobs must conclude success, and the Merge Playwright reports job is the control that the job query answers (exit 3 if it does not read success). Drilled 2026-09-10 at HEAD, round 1 and round 2 (unchanged): exit 1 - run 31848669722 created 2026-08-14, shards failure failure, control success.'
    - oracle: command
      command: |-
        PATH="/opt/homebrew/bin:$HOME/bin:$PATH"; command -v gh >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: gh absent"; exit 3; }; gh auth status >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: gh not authenticated"; exit 3; }; R=alzahrani-khalid/Intl-Dossier-V2.0
        ROOT=$(git rev-parse --show-toplevel 2>/dev/null) || { echo "INSTRUMENT-CANNOT-RUN: not in a git tree"; exit 3; }
        RUN=$(gh run list -R $R --branch main --workflow CI --limit 1 --json databaseId,createdAt -q '.[0]' 2>&1); RC=$?; [ "$RC" = 0 ] || { echo "INSTRUMENT-CANNOT-RUN: gh run list exited $RC :: $RUN"; exit 3; }
        ID=$(printf '%s' "$RUN" | node -pe 'JSON.parse(require("fs").readFileSync(0,"utf8")).databaseId'); AT=$(printf '%s' "$RUN" | node -pe 'JSON.parse(require("fs").readFileSync(0,"utf8")).createdAt')
        C=$(gh run view -R $R "$ID" --json jobs -q '[.jobs[] | select(.name=="Tests (integration)") | .conclusion]' 2>&1); RC=$?; [ "$RC" = 0 ] || { echo "INSTRUMENT-CANNOT-RUN: gh run view exited $RC :: $C"; exit 3; }
        K=$(gh run view -R $R "$ID" --json jobs -q '[.jobs[] | select(.name=="Lint") | .conclusion]' 2>&1)
        G=$(grep -cE '^- \*\*D-3\*\* .*RESOLVED' "$ROOT/.planning/phases/101-ci-gates-green/101-03-SUMMARY.md" 2>/dev/null); G=${G:-0}
        echo "P101-C2 run=$ID created=$AT integration=$C (control Lint=$K) d3_resolved_lines=$G expected [\"success\"] on a run after 2026-09-10T18:00:00Z and d3=1"
        [ "$K" = '["success"]' ] || { echo "INSTRUMENT-CANNOT-RUN: the control job Lint did not read success"; exit 3; }
        [ "$AT" \> "2026-09-10T18:00:00Z" ] || { echo "FAIL: the latest main CI run predates this phase (created $AT)"; exit 1; }
        [ "$C" = '["success"]' ] && [ "$G" = 1 ] || { echo "FAIL: integration=$C d3_lines=$G"; exit 1; }; echo PASS
      text: 'criterion 2 on live state plus the recorded decision: the latest CI run on main created after 2026-09-10T18:00:00Z must show Tests (integration) success (Lint is the control), and 101-03-SUMMARY.md must carry exactly one line matching ^- \*\*D-3\*\* .*RESOLVED. Drilled 2026-09-10 at HEAD, round 1 and round 2 (unchanged): exit 1 - run 31848669701 created 2026-08-14, integration failure, d3 lines 0, control success.'
    - oracle: command
      command: |-
        PATH="/opt/homebrew/bin:$HOME/bin:$PATH"; command -v gh >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: gh absent"; exit 3; }; gh auth status >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: gh not authenticated"; exit 3; }; R=alzahrani-khalid/Intl-Dossier-V2.0
        RUN=$(gh run list -R $R --branch main --workflow CI --limit 1 --json databaseId,createdAt -q '.[0]' 2>&1); RC=$?; [ "$RC" = 0 ] || { echo "INSTRUMENT-CANNOT-RUN: gh run list exited $RC :: $RUN"; exit 3; }
        ID=$(printf '%s' "$RUN" | node -pe 'JSON.parse(require("fs").readFileSync(0,"utf8")).databaseId'); AT=$(printf '%s' "$RUN" | node -pe 'JSON.parse(require("fs").readFileSync(0,"utf8")).createdAt')
        S=$(gh run view -R $R "$ID" --json jobs -q '[.jobs[] | select(.name=="Security Scan" or .name=="Tests (integration)" or .name=="Accessibility Tests (RTL + WCAG AA)" or .name=="RTL Portal + Component Smokes" or .name=="RTL + Responsive Tests" or .name=="Docker Build") | .conclusion] | "\(length) jobs, \(map(select(.=="success"))|length) success"' 2>&1); RC=$?; [ "$RC" = 0 ] || { echo "INSTRUMENT-CANNOT-RUN: gh run view exited $RC :: $S"; exit 3; }
        T=$(gh run view -R $R "$ID" --json jobs -q '.jobs | length' 2>&1); E=$(gh run view -R $R "$ID" --json jobs -q '[.jobs[] | select(.name=="E2E Tests")] | length' 2>&1)
        echo "P101-C5 run=$ID created=$AT $S (total jobs=$T, 'E2E Tests' jobs=$E) expected 6 jobs, 6 success, total 18, E2E Tests 0, run after 2026-09-10T18:00:00Z"
        [ "$AT" \> "2026-09-10T18:00:00Z" ] || { echo "FAIL: the latest main CI run predates this phase (created $AT)"; exit 1; }
        [ "$S" = "6 jobs, 6 success" ] && [ "$T" = 18 ] && [ "$E" = 0 ] || { echo "FAIL: population or conclusions off - $S total=$T e2e_tests=$E (D-14: a smaller population passing silently is the failure this guards)"; exit 1; }; echo PASS
      text: 'criterion 5 with the population spelled out (D-14): on the latest CI run on main created after 2026-09-10T18:00:00Z, the six names must read 6 jobs, 6 success, the run must report 18 jobs in total and zero jobs named E2E Tests - a renamed or deleted job shrinks the count and fails rather than passing on a smaller set. Drilled 2026-09-10 at HEAD, round 1 and round 2 (unchanged): exit 1 - 6 jobs, 0 success, total 19, E2E Tests 1, created 2026-08-14.'
    - oracle: judge
      text: 'The SUMMARY quotes .planning/phases/101-ci-gates-green/101-07-ATTESTATION.md VERBATIM (who merged which PR at what time, the merge commit, the main run ids observed with conclusions and creation times) rather than paraphrasing it, carries the three oracle outputs verbatim with run ids and creation times after the merge, and states the bounds: a green main run establishes that the suites as SHAPED by this phase are green - the 204 integration files, the quarantined E2E/a11y/RTL tests and the app defects behind them are not established (CONTEXT §5); the wrapper first ran on Linux in this run, so a wrapper-caused red is a P101-04 defect named as such; and the branch-protection promotion and the smoke PR are P101-11, not claimed here.'
---

<objective>
Close criteria 1, 2 and 5 on the only thing that can close them: a `main` run produced by merging the
phase PR (D-12). Nothing here runs in a worktree; the worker quotes, reads live state, and records.
The branch-protection POST and the smoke PR (criterion 4) are P101-11, which depends on this plan so that
D-03's order - promote only after `RTL Portal + Component Smokes` is green on `main` - is enforced by
the dependency graph, not by a sentence.
</objective>

<context>
@.planning/phases/101-ci-gates-green/101-CONTEXT.md
@.planning/phases/101-ci-gates-green/101-RESEARCH.md

Ref notes: 101-CONTEXT.md - D-01, D-03, D-12, D-14, D-16, §5 (what stays unestablished); 101-RESEARCH.md - §1 (the runs and jobs at planning), §8.1/§8.2/§8.5 (the oracles and their drills), §8.7 (closure map), §10, §10.6 (the round-2 split).

<interfaces>

</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Operator merges the phase PR and observes the two main runs</name>
  <files>none in the tree except the attestation the operator writes</files>
  <action>
    1. Commit the phase directory (D-16) and open the phase PR from `milestone/v10.0-trust` (or the
       branch the engine consolidated on) to `main` with the worktree plans' changes and SUMMARYs. Wait
       for the eight required checks: `gh pr checks <n> --required --fail-fast`. Merge.
    2. Observe the two `main` runs the merge produced:
       `gh run list -R alzahrani-khalid/Intl-Dossier-V2.0 --branch main --limit 4 --json databaseId,workflowName,conclusion,createdAt`
       and per run `gh run view <id> --json jobs -q '.jobs[] | "\(.conclusion)\t\(.name)"'`. Record the
       ids. If either run is red, STOP: read the failing job's first cause, name the owning plan
       (P101-04 for workflow shape and the wrapper on Linux; P101-05/08/09/10/12/13 for a spec; P101-03 for
       the integration include; P101-06 for credentials) - the attestation records the red run and the
       SUMMARY is written PARKED with that owner named. P101-11 must not start on a red smoke row (D-03).
    3. Write `.planning/phases/101-ci-gates-green/101-07-ATTESTATION.md`:
       ```
       # ATTESTATION - P101-07 phase PR merged, main runs observed
       **Who:** <name> (operator)
       **Phase PR:** #<n>, merged <ISO time>, merge commit <sha>
       **main runs observed:** CI <run id> (<conclusion>), E2E Tests <run id> (<conclusion>), created <ISO>
       **Smoke job on that CI run:** RTL Portal + Component Smokes = <success|failure> at <ISO> (P101-11 may start only on success - D-03)
       ATTESTATION-END
       ```
  </action>
  <done>Both main runs green in the six-plus-two population, the run ids and the smoke-row conclusion in the attestation with timestamps.</done>
</task>

<task type="auto">
  <name>the three live-state oracles are read after the merge and the attestation is quoted</name>
  <files>the paths in files_modified</files>
  <read_first>101-CONTEXT.md D-01..D-16 (ALL of them) and 101-RESEARCH.md §0-§10 (§8 holds the drilled oracle candidates, §10 the planner drills of 2026-09-10); re-RUN a recorded command rather than re-deriving its number a second way; then the attestation file if it exists</read_first>
  <action>
    1. If the attestation does not exist, or records a red run: write the SUMMARY with `status: PARKED`,
       the owed step and the named owner from the attestation, run all three oracles anyway and paste
       their (red) output, and stop. Never auto-answer the checkpoint.
    2. Otherwise quote the attestation VERBATIM under `## Operator attestation (quoted)`.
    3. Run the three oracles (C1, C2, C5) and paste each output verbatim. Every run id and creation time
       they print must be after the merge time in the attestation.
    4. State the bounds from the judge text; mark CONTEXT §5's list as still true, verbatim.
  </action>
  <acceptance_criteria>All three oracles pass on live GitHub state after the attestation's merge timestamp and the attestation is quoted verbatim.</acceptance_criteria>
  <done>C1, C2 and C5 read PASS on runs created after the merge, the attestation is quoted, the bounds are stated.</done>
</task>

</tasks>

<verification>
POPULATION: the latest `main` runs of `CI` and `E2E Tests` created after the phase PR merge, the
attestation file and this plan's SUMMARY. OUTSIDE IT: every file in the worktree plans (a red run is
routed to its owner, not fixed here), the branch-protection contexts and the smoke PR (P101-11), the 204
integration files, the quarantined tests and the application defects behind them (Phase 102/103),
`QA Sweep` and `Visual Regression (Phase 46)` in `e2e.yml` (CARRY-06, Phase 102), and the Lighthouse /
Eval Gate steps (CONTEXT §4).
</verification>

<success_criteria>
On the latest main runs created after the phase PR merged, both e2e.yml shards and the six named ci.yml jobs conclude success with the CI run reporting exactly 18 jobs and no E2E Tests job, and the operator attests the merge and the run ids in a file the SUMMARY quotes verbatim.
</success_criteria>

<output>
Create `.planning/phases/101-ci-gates-green/101-07-SUMMARY.md` when done. It MUST record every command this
plan ran with its VERBATIM output, the populations it re-derived with the commands that produced them, every
zero beside the control that proves the instrument could have seen a non-zero, and anything left for a named
later task. It MUST carry the attestation QUOTED VERBATIM, the three oracle outputs with run ids and creation times after the merge, and the bounds.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete` (or `status: PARKED` with the named operator step still owed, when the human gate has not yet been performed).
Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as
**pending** no matter how many gates passed (measured on P99-32 and again on P99-61).
</output>
