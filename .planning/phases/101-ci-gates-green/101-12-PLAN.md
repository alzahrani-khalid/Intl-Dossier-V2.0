---
phase: 101-ci-gates-green
plan: 12
type: execute
wave: 3
depends_on: [101-01, 101-02]
files_modified:
  - frontend/tests/a11y/dossiers-rtl-a11y.spec.ts
  - frontend/tests/a11y/intake-accessibility.spec.ts
  - frontend/tests/a11y/positions-a11y-ar.spec.ts
  - frontend/tests/a11y/positions-a11y-en.spec.ts
  - frontend/tests/a11y/wcag-aa-comprehensive-audit.spec.ts
  - .planning/phases/101-ci-gates-green/101-12-SUMMARY.md
autonomous: true
requirements: [CARRY-02, CARRY-09]
success_criterion: 'Every one of the 18 tests CI reported red across these 5 spec files is fixed (spec drift) or carries an in-spec test.fixme(true, ''P101-QUAR <run-id>: <reason>'') naming the run id and the log line, this plan''s SUMMARY register has exactly one row per marker with a numeric cells column, and the a11y project through the wrapper at its full hard-coded population of 182 with unexpected=0 flaky=0 and skipped >= register cells >= register rows.'
routing:
  floor: frontier
truths:
    - 'Decisions covered - D-09: red tests are FIXED when the cause is spec drift and QUARANTINED with a tracked reason otherwise, in-spec test.fixme(true, P101-QUAR <run-id>: <reason>) per failing test, never a project-level skip, never a deleted assertion, application defects quarantined with the defect named and left to Phase 102/103, the executor deriving the failing-test list from the run logs with the count beside it; D-15: reasons live IN the spec, the tracker is THIS plan''s SUMMARY register (spec, test title, cause class, owner phase, cells), skipped reconciled as skipped >= sum(cells) >= rows; D-13: every frontend run goes through the wrapper (depends on P101-01) and the unit waits for P101-02 so wrapped runs never contend for port 5173 in one wave; D-02: no context is renamed. Round-2 split of the former 101-05 (30 files) into six units of at most 7 files.'
must_haves:
  truths:
    - oracle: command
      command: |-
        ROOT=$(git rev-parse --show-toplevel 2>/dev/null) || { echo "INSTRUMENT-CANNOT-RUN: not in a git tree"; exit 3; }; cd "$ROOT" || exit 3; PATH="/opt/homebrew/bin:$HOME/bin:$PATH"
        FILES="frontend/tests/a11y/dossiers-rtl-a11y.spec.ts frontend/tests/a11y/intake-accessibility.spec.ts frontend/tests/a11y/positions-a11y-ar.spec.ts frontend/tests/a11y/positions-a11y-en.spec.ts frontend/tests/a11y/wcag-aa-comprehensive-audit.spec.ts"
        NF=$(echo $FILES | wc -w | tr -d ' '); [ "$NF" = 5 ] || { echo "INSTRUMENT-CANNOT-RUN: the file list has $NF entries, want 5"; exit 3; }
        for f in $FILES; do [ -f "$f" ] || { echo "INSTRUMENT-CANNOT-RUN: $f missing - a red spec was deleted rather than fixed or quarantined"; exit 3; }; done
        M=$(cat $FILES | grep -c "test.fixme(true, 'P101-QUAR 3184866"); MB=$(cat $FILES | grep -c "test.fixme(true, 'P101-QUAR"); PF=$(cat $FILES | grep -c "test.fixme(")
        CTRL=$(grep -c "test.fixme(" frontend/tests/a11y/positions-keyboard-nav.spec.ts)
        S=.planning/phases/101-ci-gates-green/101-12-SUMMARY.md; REG=$(awk '/^## Quarantine register/{f=1;next} /^## /{f=0} f' "$S" 2>/dev/null | grep -E '^\| (tests/e2e/|frontend/tests/)[^|]+\|'); ROWS=$(printf '%s\n' "$REG" | grep -c .); CELLS=$(printf '%s\n' "$REG" | grep -E '^\|' | awk -F'|' '{n=$6; gsub(/ /,"",n); if(n ~ /^[0-9]+$/) c+=n; else bad++} END{print c+0" "bad+0}'); CSUM=${CELLS% *}; CBAD=${CELLS#* }
        FIXED=$(awk '/^## Fixed tests/{f=1;next} /^## /{f=0} f' "$S" 2>/dev/null | grep -cE '^\| FIXED \|')
        echo "P101-12-BOUND files=$NF markers with a run id=$M (any P101-QUAR=$MB, any fixme incl. pre-existing=$PF; control positions-keyboard-nav fixme=$CTRL) register rows=$ROWS cells total=$CSUM non-numeric=$CBAD fixed rows=$FIXED want the 18 (a11y job 94920552794) red tests each fixed or marked: markers+fixed>=1, markers==any-P101-QUAR, rows==markers, cells>=rows, non-numeric=0"
        [ "$CTRL" = 13 ] || { echo "INSTRUMENT-CANNOT-RUN: the fixme grep control returned $CTRL, want 13"; exit 3; }
        [ "$M" = "$MB" ] || { echo "FAIL: $M markers carry a run id 3184866*, $MB carry P101-QUAR at all - every reason names the run id (D-09)"; exit 1; }
        [ $((M+FIXED)) -ge 1 ] || { echo "FAIL: no marker and no FIXED row for the 18 (a11y job 94920552794) red tests in these files - nothing was fixed or quarantined"; exit 1; }
        [ "$ROWS" = "$M" ] || { echo "FAIL: the ## Quarantine register section has $ROWS rows for $M in-spec markers (D-15: one row per marker; FIXED rows live under ## Fixed tests and begin | FIXED |)"; exit 1; }
        [ "$CBAD" = 0 ] && [ "$CSUM" -ge "$ROWS" ] || { echo "FAIL: the register cells column (5th) must be a positive integer per row - $CBAD non-numeric, total $CSUM for $ROWS rows"; exit 1; }
        cd "$ROOT/frontend" || exit 3
        for t in python3 lsof node pnpm; do command -v $t >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: $t absent"; exit 3; }; done
        grep -q 'pw-run-reaped.mjs --lease-exec' playwright.config.ts || { echo "INSTRUMENT-CANNOT-RUN: frontend/playwright.config.ts is not routed through the lease writer (P101-01) - D-13 forbids an unwrapped run"; exit 3; }
        set -a; . ../.env.test 2>/dev/null; set +a; [ -n "${TEST_USER_EMAIL:-}" ] || { echo "INSTRUMENT-CANNOT-RUN: TEST_USER_EMAIL unset"; exit 3; }
        [ -z "${E2E_BASE_URL:-}" ] || { echo "INSTRUMENT-CANNOT-RUN: E2E_BASE_URL set"; exit 3; }
        [ "$(lsof -ti tcp:5173 -sTCP:LISTEN 2>/dev/null | wc -l | tr -d ' ')" = 0 ] || { echo "INSTRUMENT-CANNOT-RUN: 5173 already held"; exit 3; }
        FAIL=0; mkdir -p test-results; SUMM=../$S
        regcount() { awk '/^## Quarantine register/{f=1;next} /^## /{f=0} f' "$SUMM" 2>/dev/null | grep -E "^\| frontend/tests/($1)[^|]*\|" | awk -F'|' '{r++; n=$6; gsub(/ /,"",n); if(n ~ /^[0-9]+$/) c+=n} END{print r+0" "c+0}'; }
        one() { NAME=$1; N=$2; PAT=$3; shift 3; RC_=$(regcount "$PAT"); RROWS=${RC_% *}; RCELLS=${RC_#* }; L=$(pnpm exec playwright test "$@" --list 2>&1 | grep -oE "Total: [0-9]+ tests"); [ "$L" = "Total: $N tests" ] || { echo "INSTRUMENT-CANNOT-RUN: $NAME lists [$L], want 'Total: $N tests'"; exit 3; }
          MK="test-results/p101-12-$NAME.mark"; touch "$MK"; node ../scripts/pw-run-reaped.mjs -- "$@" > "test-results/p101-12-$NAME.wrapper.log" 2>&1; WRC=$?
          R=$(find test-results -maxdepth 1 -name 'pw-reaped-*.json' -newer "$MK" | head -1); [ -n "$R" ] && [ "$WRC" != 90 ] || { echo "FAIL: $NAME wrapper rc=$WRC published report=[$R] - an unclean session withholds the report (90)"; FAIL=1; return; }
          node -e 'const s=JSON.parse(require("fs").readFileSync(process.argv[1],"utf8")).stats;const N=+process.argv[2],RR=+process.argv[4],RC=+process.argv[5];console.log(`P101-12-RUN ${process.argv[3]} expected=${s.expected} unexpected=${s.unexpected} skipped=${s.skipped} flaky=${s.flaky} sum=${s.expected+s.skipped} register_rows=${RR} register_cells=${RC} want unexpected=0 flaky=0 expected+skipped=${N} skipped>=register_cells>=register_rows (pre-existing fixmes add to skipped, so >= not ==)`);process.exit(s.unexpected===0&&s.flaky===0&&s.expected+s.skipped===N&&s.skipped>=RC&&RC>=RR?0:1)' "$R" "$N" "$NAME" "$RROWS" "$RCELLS" || FAIL=1; }
        one a11y 182 "a11y/" --project=a11y
        [ "$FAIL" = 0 ] || exit 1; echo PASS
      text: 'the planning bound for exactly these 5 files, runnable without credentials: every file must still exist (a deleted spec exits 3), every P101-QUAR marker must carry a 3184866* run id, at least one marker or one FIXED row must exist (a unit that changed nothing fails), the ## Quarantine register section of THIS plan''s SUMMARY must have exactly one row per marker with a positive integer in the 5th (cells) column and cells total >= rows, and FIXED rows are read only under ## Fixed tests as lines beginning | FIXED |; the positions-keyboard-nav fixme count 13 is the grep control. Then the run half (D-13, exits 3 unless the frontend config is routed, credentials materialised, E2E_BASE_URL unset, 5173 free, --list at the hard-coded population): the a11y project through the wrapper at its full hard-coded population of 182 with unexpected=0 flaky=0 and skipped >= register cells >= register rows, the register rows and cells read per suite from this SUMMARY. Drilled 2026-09-10 round 2 at HEAD: exit 1 - markers 0, FIXED 0, rows 0, control 13.'
    - oracle: judge
      text: 'The SUMMARY derives the failing tests for exactly these 5 files from the run logs with the command, the ANSI-strip step (the log stores ESC as the two characters ^[) and the count beside the CI notice, carries a ## Quarantine register with five columns (spec, test title, cause class, owner phase, cells) and one row per marker, a ## Fixed tests table whose rows begin | FIXED | with the drift named, quotes the reporter stats block(s) verbatim with skipped reconciled as skipped >= sum(cells) >= rows, and never claims a quarantined behaviour is absent - only that its test is red.'
---

<objective>
Unit 12 of the criterion-1 / criterion-5 quarantine pass: the five red a11y specs - 5 spec files, 18 red tests on Accessibility Tests (RTL + WCAG AA), job 94920552794: dossiers-rtl-a11y 12 (two tests at :227:7 and :288:7 across six dossier types), intake-accessibility 2, positions-a11y-ar 1, positions-a11y-en 2, wcag-aa-comprehensive-audit 1.

First causes (RESEARCH §1.3, §10.2): the missingLabels check at dossiers-rtl-a11y.spec.ts:248 (buttons/links without an accessible name - an application a11y defect, Phase 102/103) and heading-order checks; the same spec was 61/61 locally in ORCH-2, so read each trace for environment dependence.

The rule (D-09): FIX when the cause is spec drift; QUARANTINE in the test body, first statement, as
`test.fixme(true, 'P101-QUAR <run-id>: <cause>; log line <n> of job <job-id>; owner Phase 10N')` when the
fix needs application code or a visual baseline decision. One marker per failing test. Never a deleted
`expect`, never a project-level skip, never a regenerated baseline. The tracker is this plan's own
SUMMARY (D-15). Nothing under `frontend/src` or `backend/src` is in scope.
</objective>

<context>
@.planning/phases/101-ci-gates-green/101-CONTEXT.md
@.planning/phases/101-ci-gates-green/101-RESEARCH.md

Ref notes: 101-CONTEXT.md - D-09, D-15, D-13, D-02; 101-RESEARCH.md - §1.2/§1.3 (failing step and first cause per suite, with log lines), §3 (why the root shards cannot run locally), §10.2 (the per-job derivation commands and counts), §10.6 (the round-2 split map).

<interfaces>

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>every red test in these 5 files is fixed for drift or quarantined in-spec with a tracked reason</name>
  <files>the paths in files_modified</files>
  <read_first>101-CONTEXT.md D-01..D-16 (ALL of them) and 101-RESEARCH.md §0-§10 (§8 holds the drilled oracle candidates, §10 the planner drills of 2026-09-10); re-RUN a recorded command rather than re-deriving its number a second way; then the job log(s) named above (`gh run view -R alzahrani-khalid/Intl-Dossier-V2.0 --job <id> --log-failed`, stripped with `sed -E 's/\^\[\[[0-9;]*m//g'`) - `command -v gh && gh auth status` first</read_first>
  <action>
    1. DERIVE the failing tests for THESE files only, with the count beside each (the RESEARCH §10.2
       command, filtered to this unit's paths). Dedupe parametrised tests on the full row and drop a row
       that is a strict prefix of another (the log truncates one line). Your total must equal
       18. Record the derivation verbatim.
    2. Classify each from its error in the log and act: spec drift -> fix the spec; application code, a
       contract, or a visual baseline -> quarantine with the form above, run id 31848669722 (shards) or
       31848669701 (CI), owner phase named.
    3. Run the suite(s) THROUGH the wrapper (this plan's oracle does it; needs 5173 free and P101-01 landed) and paste the stats block(s). A fixed test must pass; a quarantined test reads as skipped; unexpected must be 0. If a fix does not hold, quarantine it with the observed error rather than iterating on the application.
    4. Write the SUMMARY: `## Quarantine register` (read ONLY that section, up to the next `## `), one row
       per marker, FIVE columns `| <spec path> | <test title> | <cause class> | <owner phase> | <cells> |`
       (cells = reporter cells one marker skips: 1 for a plain test, N inside a loop). Then
       `## Fixed tests` with rows `| FIXED | <spec> | <test> | <drift named> |`. Run this plan's oracle
       and paste its output. State the bound from the judge text.
  </action>
  <acceptance_criteria>The oracle passes, the derivation count equals the CI notice for these files, and no path outside files_modified changes.</acceptance_criteria>
  <done>Every red test in these 5 files is fixed or carries a P101-QUAR marker naming its run id, the register matches the markers with a numeric cells column, the wrapped run(s) read unexpected=0 with skipped reconciled.</done>
</task>

</tasks>

<verification>
POPULATION: the 5 spec files in files_modified, the tests inside them CI reported red, and this
plan's SUMMARY. OUTSIDE IT: every other spec file (the other five units 101-05/08/09/10/12/13,
`dossiers-a11y.spec.ts` - P101-02), application code and visual baselines (Phase 102/103), the workflow
(P101-04), the configs (P101-01) and the CI observation (P101-07).
</verification>

<success_criteria>
Every one of the 18 tests CI reported red across these 5 spec files is fixed (spec drift) or carries an in-spec test.fixme(true, 'P101-QUAR <run-id>: <reason>') naming the run id and the log line, this plan's SUMMARY register has exactly one row per marker with a numeric cells column, and the a11y project through the wrapper at its full hard-coded population of 182 with unexpected=0 flaky=0 and skipped >= register cells >= register rows.
</success_criteria>

<output>
Create `.planning/phases/101-ci-gates-green/101-12-SUMMARY.md` when done. It MUST record every command this
plan ran with its VERBATIM output, the populations it re-derived with the commands that produced them, every
zero beside the control that proves the instrument could have seen a non-zero, and anything left for a named
later task. It MUST carry the per-file derivation with the count equal to the CI notice, the five-column register, the FIXED table, and the wrapped stats block(s) with skipped reconciled.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete`.
Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as
**pending** no matter how many gates passed (measured on P99-32 and again on P99-61).
</output>
