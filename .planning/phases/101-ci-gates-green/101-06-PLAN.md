---
phase: 101-ci-gates-green
plan: 6
type: execute
wave: 3
depends_on: []
files_modified:
  - .planning/phases/101-ci-gates-green/101-06-ATTESTATION.md
  - .planning/phases/101-ci-gates-green/101-06-SUMMARY.md
autonomous: false
requirements: [CARRY-02, CARRY-05]
success_criterion: 'All seven GitHub Actions secrets - the six E2E_{ADMIN,ANALYST,INTAKE}_{EMAIL,PASSWORD} and the previously absent E2E_SUPABASE_SERVICE_ROLE_KEY - show an updatedAt after the planning start 2026-09-10T18:00:00Z, the operator attests in a file that the staging test users were rotated, the old values no longer authenticate, and .env.test was updated, and the SUMMARY quotes that attestation verbatim; no secret value ever enters an agent session.'
routing:
  floor: frontier
truths:
    - 'Decisions covered - D-10: CARRY-01 rotation is still owed (the youngest E2E_* secret write is 2026-06-27, before the P88-02 exposure was filed) and it is NOT what blocks the E2E green, so it is a human-gate step here - the 92-10 checklist - that does not order before the worktree plans, and the same step adds the absent E2E_SUPABASE_SERVICE_ROLE_KEY secret that e2e.yml references; D-12: the human owns the act, the oracle reads live GitHub state by NAME and DATE only; brief rule: an agent never performs a secret write or reads a value.'
must_haves:
  truths:
    - 'The SUMMARY quotes .planning/phases/101-ci-gates-green/101-06-ATTESTATION.md VERBATIM (who rotated, when, which six users, that the old values were confirmed to no longer authenticate, that .env.test was updated, that the seventh secret was set - names only), carries the gh secret list output with names and updatedAt only, and states the bound: this plan establishes that seven secrets were WRITTEN after the planning date and that the operator SAYS the users were rotated; it does not establish that the new values work in CI - the next e2e.yml run under P101-07 does - and it never sees a value.'
    - oracle: command
      command: |-
        PATH="/opt/homebrew/bin:$HOME/bin:$PATH"; command -v gh >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: gh absent"; exit 3; }; gh auth status >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: gh not authenticated"; exit 3; }
        R=alzahrani-khalid/Intl-Dossier-V2.0; OUT=$(gh secret list -R $R --json name,updatedAt 2>&1); RC=$?; [ "$RC" = 0 ] || { echo "INSTRUMENT-CANNOT-RUN: gh secret list exited $RC :: $OUT"; exit 3; }
        CTRL=$(printf '%s' "$OUT" | node -pe 'JSON.parse(require("fs").readFileSync(0,"utf8")).filter(s=>s.name==="E2E_BASE_URL").length'); [ "$CTRL" = 1 ] || { echo "INSTRUMENT-CANNOT-RUN: control secret E2E_BASE_URL not visible - the store is not readable"; exit 3; }
        printf '%s' "$OUT" | node -e '
        const all=JSON.parse(require("fs").readFileSync(0,"utf8")); const want=["E2E_ADMIN_EMAIL","E2E_ADMIN_PASSWORD","E2E_ANALYST_EMAIL","E2E_ANALYST_PASSWORD","E2E_INTAKE_EMAIL","E2E_INTAKE_PASSWORD","E2E_SUPABASE_SERVICE_ROLE_KEY"];
        const cut=new Date("2026-09-10T18:00:00Z"); /* planning START (Riyadh evening 2026-09-10), not end of day */ let bad=0; for(const n of want){const s=all.find(x=>x.name===n); const ok=!!s && new Date(s.updatedAt)>cut; if(!ok) bad++; console.log(`  ${n}: ${s?"updated "+s.updatedAt:"ABSENT"} ${ok?"ok":"NOT written after planning start 2026-09-10T18:00:00Z"}`);}
        console.log(`P101-06 written_after_2026-09-10T18:00:00Z=${want.length-bad} of ${want.length} (names only; no value is ever read)`); process.exit(bad?1:0)'; RC=$?
        [ "$RC" = 0 ] || { echo "FAIL: CARRY-01 rotation and the E2E_SUPABASE_SERVICE_ROLE_KEY write are not all recorded after the planning date"; exit 1; }
        echo PASS
      text: 'reads the secret store by name and date only: the control secret E2E_BASE_URL must be visible (else the store is not readable, exit 3), and each of the seven named secrets must exist with updatedAt after the planning START 2026-09-10T18:00:00Z (full ISO compare, so an act performed later the same UTC day passes - round 1, issue 7); a missing or stale one fails. No value is requested by any call. Drilled 2026-09-10 at HEAD and again in round 1: exit 1 - 0 of 7 written after the cut-off, E2E_SUPABASE_SERVICE_ROLE_KEY absent.'
    - oracle: judge
      text: 'The SUMMARY quotes the attestation file verbatim under a heading Operator attestation (quoted) rather than restating it, carries the gh secret list names and dates, contains no string that could be a secret value (no key-shaped token, no password-shaped literal), and states the bound in those terms.'
---

<objective>
Discharge CARRY-01 as the human gate it is, and add the one secret CI references but does not have.

RESEARCH §3: all six `E2E_*` credential secrets exist and authenticate (the `e2e.yml` shards ran 220 tests
to assertion failures); rotation has NOT happened (`92-10-SUMMARY.md` is `status: PARKED`, gate
`92-10_g1: RED`; the youngest secret write is 2026-06-27). The exposed P88-02 values are the live ones.
`E2E_SUPABASE_SERVICE_ROLE_KEY` (referenced at `e2e.yml:28,70,105`) is absent from the store, so those
jobs get an empty string.

Nothing here runs in a worktree. The operator performs the 92-10 checklist; the worker's whole job is to
quote the attestation, run the name-and-date oracle, and write the SUMMARY. If the gate has not been
performed when this plan is dispatched, the SUMMARY is written with `status: PARKED` and the named owed
step, exactly as 92-10 did - never a checkpoint auto-answered.
</objective>

<context>
@.planning/phases/101-ci-gates-green/101-CONTEXT.md
@.planning/phases/101-ci-gates-green/101-RESEARCH.md

Ref notes: 101-CONTEXT.md - D-10, D-12; 101-RESEARCH.md - §3 (what auth.setup.ts requires, the secret list with dates, the PARKED 92-10 status), §9 F2/F9, §10; .planning/phases/92-session-integrity-edge-function-auth/92-10-SUMMARY.md (the operator checklist, quoted below).

<interfaces>

</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Operator rotates the six E2E test-user credentials and sets the seventh secret</name>
  <files>none - the checkpoint edits nothing in the tree; the operator writes the attestation file</files>
  <action>
    Only the operator can do this. Never paste any value into an agent session, a pane, or a file the
    repository tracks. The 92-10 checklist, unchanged:

    1. Rotate the staging test users' passwords (project `zkrcjzdemdmwhearhfgg` - Authentication - Users,
       or `auth.admin.updateUserById`) for the three roles admin / analyst / intake.
    2. Update the GitHub Actions repository secrets - SIX - by name only, from a terminal the agent
       cannot read:
       `gh secret set E2E_ADMIN_EMAIL`, `gh secret set E2E_ADMIN_PASSWORD`, `gh secret set E2E_ANALYST_EMAIL`,
       `gh secret set E2E_ANALYST_PASSWORD`, `gh secret set E2E_INTAKE_EMAIL`, `gh secret set E2E_INTAKE_PASSWORD`
       (each prompts for the value on stdin; `-R alzahrani-khalid/Intl-Dossier-V2.0` if not inside the clone).
    3. Set the SEVENTH, absent today: `gh secret set E2E_SUPABASE_SERVICE_ROLE_KEY` (the staging project's
       service-role key; `e2e.yml` reads it into `SUPABASE_SERVICE_ROLE_KEY`).
    4. Update local `.env.test` with the six new values (it is git-ignored; the tickmarkr `setup` hook
       copies it into worktrees).
    5. Confirm the old values no longer authenticate (a sign-in attempt with an old password must be
       rejected) - record the fact, never the value.
    6. The oracle's cut-off is the planning START, 2026-09-10T18:00:00Z (UTC) - a write stamped later
       on 2026-09-10 UTC reads green; one stamped before 18:00Z that day reads red and must be redone.
    7. Write `.planning/phases/101-ci-gates-green/101-06-ATTESTATION.md`:
       ```
       # ATTESTATION - P101-06 CARRY-01 credential rotation + E2E_SUPABASE_SERVICE_ROLE_KEY
       **Who rotated:** <name> (operator)
       **When:** <ISO timestamp>
       **Users rotated (three roles, six secrets by name):** E2E_ADMIN_*, E2E_ANALYST_*, E2E_INTAKE_*
       **Seventh secret set:** E2E_SUPABASE_SERVICE_ROLE_KEY
       **Old values confirmed rejected:** yes, at <timestamp>, by <method>
       **.env.test updated:** yes
       ATTESTATION-END
       ```
       No value appears in it. `gh secret list` (names and dates) is the only machine-readable trace.
  </action>
  <done>The seven secrets show an updatedAt after 2026-09-10T18:00:00Z, the attestation file exists with its seven lines filled and no value in it, and the operator has said so in writing.</done>
</task>

<task type="auto">
  <name>the rotation is recorded from the attestation and the secret store, by name and date only</name>
  <files>the paths in files_modified</files>
  <read_first>101-CONTEXT.md D-01..D-16 (ALL of them) and 101-RESEARCH.md §0-§10 (§8 holds the drilled oracle candidates, §10 the planner drills of 2026-09-10); re-RUN a recorded command rather than re-deriving its number a second way; then the attestation file if it exists</read_first>
  <action>
    1. If `.planning/phases/101-ci-gates-green/101-06-ATTESTATION.md` does not exist: write the SUMMARY
       with front-matter `status: PARKED` and the line `owed: operator step - CARRY-01 rotation and
       E2E_SUPABASE_SERVICE_ROLE_KEY (92-10 checklist)`, quote the 92-10 park for continuity, run the
       oracle anyway and paste its (red) output, and stop. Do not attempt any part of the checklist.
    2. Otherwise quote the attestation VERBATIM under `## Operator attestation (quoted)`. Do not
       summarise or restate it.
    3. Run `gh secret list -R alzahrani-khalid/Intl-Dossier-V2.0 --json name,updatedAt` and paste the
       output (names and dates only - that is all the API returns). Run this plan's oracle and paste it.
    4. Before saving, grep your own SUMMARY for anything key-shaped (`eyJ`, 40-plus-character base64
       runs, `password=`) and remove it; the judge reads for exactly this.
    5. State the bound: written after the date and attested, not proven to work in CI until P101-07's run.
  </action>
  <acceptance_criteria>The oracle passes on live GitHub state after the attestation's timestamp, the attestation is quoted verbatim, and no secret value appears anywhere in the tree.</acceptance_criteria>
  <done>Seven secrets dated after 2026-09-10T18:00:00Z, the attestation quoted verbatim, the bound stated.</done>
</task>

</tasks>

<verification>
POPULATION: the seven named GitHub Actions secrets (existence and date only), the attestation file, and
this plan's SUMMARY. OUTSIDE IT: every secret VALUE (never read), the staging auth users themselves
(the operator's, confirmed by attestation), local `.env.test` (git-ignored; the operator's), whether CI
authenticates with the new values (P101-07's `main` run), and every worktree plan.
</verification>

<success_criteria>
All seven GitHub Actions secrets show an updatedAt after the planning start 2026-09-10T18:00:00Z, the operator attests in a file that the staging test users were rotated, the old values no longer authenticate, and .env.test was updated, and the SUMMARY quotes that attestation verbatim; no secret value ever enters an agent session.
</success_criteria>

<output>
Create `.planning/phases/101-ci-gates-green/101-06-SUMMARY.md` when done. It MUST record every command this
plan ran with its VERBATIM output, the populations it re-derived with the commands that produced them, every
zero beside the control that proves the instrument could have seen a non-zero, and anything left for a named
later task. It MUST carry the attestation QUOTED VERBATIM, the names-and-dates secret listing, the oracle output, and the bound - and no value of any kind.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete` (or `status: PARKED` with the named operator step still owed, when the human gate has not yet been performed).
Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as
**pending** no matter how many gates passed (measured on P99-32 and again on P99-61).
</output>
