---
phase: 101-ci-gates-green
plan: 2
type: execute
wave: 2
depends_on: [101-01]
files_modified:
  - frontend/tests/a11y/focus-indicators.spec.ts
  - .planning/phases/101-ci-gates-green/101-02-SUMMARY.md
autonomous: true
requirements: [CARRY-04]
success_criterion: 'frontend/tests/a11y/dossiers-a11y.spec.ts passes 16 of 16 with zero skipped, zero flaky and zero unexpected under the a11y project through the reaped wrapper, its JSON reporter output is quoted in the SUMMARY as the run evidence, and frontend/tests/a11y/focus-indicators.spec.ts is deleted with the a11y gate population unchanged at 182 tests in 13 files.'
routing:
  floor: frontier
truths:
    - 'Decisions covered - D-05: the evidence is ONE spec, hard-coded - dossiers-a11y.spec.ts, 16 tests, zero fixme/skip, 16/16 in ORCH-2 under the same job and config as CI - asserted from the JSON reporter as expected=16 unexpected=0 skipped=0 flaky=0 with a --list precheck of exactly 16 so a stale spec exits 3 rather than passing on a different population, and F4 is read as never recorded in a phase artifact, which this SUMMARY is; D-11: focus-indicators.spec.ts is deleted (in no gate; hard-codes a login with literal credentials), the reason recorded, the values never echoed; D-13: the run goes through the reaped wrapper, which is why this plan depends on P101-01.'
must_haves:
  truths:
    - 'The SUMMARY carries the JSON reporter stats block VERBATIM (expected, unexpected, skipped, flaky) with the wrapper exit code and the path of the published report, the --list precheck output, the deletion of focus-indicators.spec.ts recorded as a git-tracked removal with its reason in one sentence and NO quotation of the literals it contained, and a stated bound: one spec green locally under the frontend config on this machine says nothing about the other 166 tests of the a11y project, which P101-05 quarantines or fixes, and nothing about the CI runner until the main run P101-07 observes.'
    - oracle: command
      command: |-
        ROOT=$(git rev-parse --show-toplevel 2>/dev/null) || { echo "INSTRUMENT-CANNOT-RUN: not in a git tree"; exit 3; }; cd "$ROOT/frontend" || exit 3
        PATH="/opt/homebrew/bin:$HOME/bin:$PATH"; for t in python3 lsof node pnpm; do command -v $t >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: $t absent"; exit 3; }; done
        grep -q 'pw-run-reaped.mjs --lease-exec' playwright.config.ts || { echo "INSTRUMENT-CANNOT-RUN: frontend/playwright.config.ts is not routed through the lease writer (P101-01) - an unwrapped run would leak the session CARRY-10 is about (D-13)"; exit 3; }
        set -a; . ../.env.test 2>/dev/null; set +a; [ -n "${TEST_USER_EMAIL:-}" ] || { echo "INSTRUMENT-CANNOT-RUN: TEST_USER_EMAIL unset - .env.test not materialised"; exit 3; }
        [ -z "${E2E_BASE_URL:-}" ] || { echo "INSTRUMENT-CANNOT-RUN: E2E_BASE_URL set - the run would not use the config's webServer"; exit 3; }
        [ "$(lsof -ti tcp:5173 -sTCP:LISTEN 2>/dev/null | wc -l | tr -d ' ')" = 0 ] || { echo "INSTRUMENT-CANNOT-RUN: 5173 already held"; exit 3; }
        L=$(pnpm exec playwright test tests/a11y/dossiers-a11y.spec.ts --project=a11y --list 2>&1 | grep -oE "Total: [0-9]+ tests"); [ "$L" = "Total: 16 tests" ] || { echo "INSTRUMENT-CANNOT-RUN: expected 'Total: 16 tests' listed, got [$L] - the spec or the a11y project changed under this oracle"; exit 3; }
        J="$PWD/test-results/p101-02-a11y.json"; mkdir -p test-results; rm -f "$J"
        node ../scripts/pw-run-reaped.mjs tests/a11y/dossiers-a11y.spec.ts a11y "$J" > test-results/p101-02-a11y.wrapper.log 2>&1; WRC=$?
        echo "P101-02 wrapper_rc=$WRC (0 = clean verdict AND playwright green; 90 = session unclean, report withheld)"
        [ -f "$J" ] || { echo "FAIL: no report published at $J (wrapper rc=$WRC) - see $(ls test-results/p101-02-a11y.json.unclean-* 2>/dev/null | head -1) and the wrapper log"; exit 1; }
        node -e 'const r=JSON.parse(require("fs").readFileSync(process.argv[1],"utf8"));const s=r.stats;console.log(`P101-02-C3 expected=${s.expected} unexpected=${s.unexpected} skipped=${s.skipped} flaky=${s.flaky} want expected=16 unexpected=0 skipped=0 flaky=0`);process.exit(s.expected===16&&s.unexpected===0&&s.skipped===0&&s.flaky===0?0:1)' "$J"; SRC=$?
        [ "$SRC" = 0 ] || { echo "FAIL: dossiers-a11y.spec.ts did not pass 16/16 with zero skips"; exit 1; }
        [ "$WRC" = 0 ] || { echo "FAIL: report shows 16/16 but the wrapper exited $WRC - the session verdict is part of this criterion"; exit 1; }
        echo PASS
      text: 'the criterion-3 oracle from RESEARCH §8.3, run THROUGH the wrapper in its legacy positional form so the final report path is known: precheck exits 3 unless the frontend config is routed (D-13), credentials are materialised, E2E_BASE_URL is unset, 5173 is free and --list prints exactly Total: 16 tests; the run must publish the report (an unclean session withholds it and exits 90 - that FAILS, it is not an instrument problem), the stats must read expected=16 unexpected=0 skipped=0 flaky=0, and the wrapper must exit 0. Drilled 2026-09-10 at HEAD: exit 3 at the routing precheck (the --list half was drilled separately: Total: 16 tests).'
    - oracle: command
      command: |-
        ROOT=$(git rev-parse --show-toplevel 2>/dev/null) || { echo "INSTRUMENT-CANNOT-RUN: not in a git tree"; exit 3; }; cd "$ROOT" || exit 3; PATH="/opt/homebrew/bin:$HOME/bin:$PATH"
        F=frontend/tests/a11y/focus-indicators.spec.ts
        T=$(git ls-files --error-unmatch "$F" >/dev/null 2>&1 && echo tracked || echo untracked); E=$([ -e "$F" ] && echo present || echo absent)
        CTRL=$(git ls-files --error-unmatch frontend/tests/a11y/dossiers-a11y.spec.ts >/dev/null 2>&1 && echo tracked || echo untracked)
        N=$(cd frontend && pnpm exec playwright test --project=a11y --list 2>&1 | grep -oE "Total: [0-9]+ tests in [0-9]+ files")
        echo "P101-02-D11 focus-indicators: index=$T disk=$E | control dossiers-a11y index=$CTRL | a11y project lists [$N] (want untracked absent tracked 'Total: 182 tests in 13 files')"
        [ "$CTRL" = tracked ] || { echo "INSTRUMENT-CANNOT-RUN: the control spec is not tracked - git ls-files is not answering"; exit 3; }
        [ -n "$N" ] || { echo "INSTRUMENT-CANNOT-RUN: playwright --list printed no Total line"; exit 3; }
        [ "$T" = untracked ] && [ "$E" = absent ] || { echo "FAIL: focus-indicators.spec.ts still $T/$E (D-11: deleted, secret-shaped literals gone from the tree)"; exit 1; }
        [ "$N" = "Total: 182 tests in 13 files" ] || { echo "FAIL: the a11y gate population moved to [$N] - the deletion must not touch an admitted spec"; exit 1; }
        echo PASS
      text: 'D-11 as a positive reading with a control: focus-indicators.spec.ts must be untracked AND absent while the control spec dossiers-a11y.spec.ts is still tracked (proving git ls-files answers), and the a11y project must still list exactly Total: 182 tests in 13 files so the deletion demonstrably touched nothing the gate admits. Drilled 2026-09-10 at HEAD: exit 1 (tracked/present; the list control read 182 in 13).'
    - oracle: judge
      text: 'The SUMMARY quotes the stats block and the wrapper exit verbatim, names the published report path, records the focus-indicators deletion with a one-sentence reason and without reproducing any literal from that file, and states the one-spec bound in those terms rather than claiming the a11y suite is green.'
---

<objective>
Close criterion 3 with run evidence recorded in a phase artifact - the thing RESEARCH §5 and §9 F4
found missing. `frontend/tests/a11y/dossiers-a11y.spec.ts` (16 tests, zero fixme/skip, axe over six seeded
dossier routes with the inherited storageState) was 16/16 in ORCH-2 on 2026-08-13 and runs under the same
`a11y` project the CI job uses; what has never existed is a SUMMARY that quotes the reporter.

The run goes through `scripts/pw-run-reaped.mjs` (D-13) in its legacy positional form
`<spec> <project> <jsonOut>`, which passes `--no-deps` and publishes the JSON to the path given, so the
oracle can read a known file. That wrapper only leases the frontend dev stack once P101-01 has landed;
before that the stack starts unleased, the verdict cannot be `clean`, and the wrapper exits 90 with the
report withheld - hence `depends_on: [101-01]`.

Also under D-11: delete `frontend/tests/a11y/focus-indicators.spec.ts`. It is in no gate (not in the
`a11y` project's admit list, `frontend/playwright.config.ts:132-146`) and hard-codes a login with literal
credentials (RESEARCH §9 F10). Record the reason; never echo the values.
</objective>

<context>
@.planning/phases/101-ci-gates-green/101-CONTEXT.md
@.planning/phases/101-ci-gates-green/101-RESEARCH.md

Ref notes: 101-CONTEXT.md - D-05 (the one spec, hard-coded 16), D-11 (the deletion), D-13; 101-RESEARCH.md - §5 (the per-config --list table and why dossiers-a11y is the cheapest candidate), §8.3 (the oracle and its --list drill), §9 F4/F10, §10.

<interfaces>

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>one a11y spec is shown green in this phase's record and the ungated credential-bearing spec is deleted</name>
  <files>the paths in files_modified</files>
  <read_first>101-CONTEXT.md D-01..D-16 (ALL of them) and 101-RESEARCH.md §0-§10 (§8 holds the drilled oracle candidates, §10 the planner drills of 2026-09-10); re-RUN a recorded command rather than re-deriving its number a second way; then `frontend/playwright.config.ts` (the a11y project admit list and the webServer block as P101-01 left it)</read_first>
  <action>
    1. Confirm P101-01 landed in this tree: `grep -n 'pw-run-reaped.mjs --lease-exec' frontend/playwright.config.ts`
       must hit. If it does not, STOP and record it - an unwrapped run is forbidden (D-13).
    2. `git rm frontend/tests/a11y/focus-indicators.spec.ts` (D-11). In the SUMMARY: "deleted; it was in no
       gate and hard-coded a login with literal credentials (RESEARCH §9 F10)". Do not quote the file.
       Then `cd frontend && pnpm exec playwright test --project=a11y --list` must still print
       `Total: 182 tests in 13 files` - the deletion touched nothing the gate admits.
    3. Run the criterion-3 oracle (this plan's first oracle) and paste its full output. It performs the
       `--list` precheck (exactly 16), then
       `node ../scripts/pw-run-reaped.mjs tests/a11y/dossiers-a11y.spec.ts a11y "$PWD/test-results/p101-02-a11y.json"`
       from `frontend/`, then reads `stats` from the published JSON. Also quote the `stats` object itself
       and the wrapper's last log lines (`test-results/p101-02-a11y.wrapper.log`, the verdict line).
    4. If the run is not 16/16: read the wrapper log and the per-test errors in the JSON before touching
       anything. A failure that needs application code is out of scope (Phase 102/103) and this plan then
       records the spec as NOT shown green with the failing test and error verbatim - it does not
       substitute another spec (D-05 hard-codes this one) and it does not annotate a test away (the
       oracle's skipped=0 clause exists for exactly that).
    5. Run the D-11 oracle (second oracle) and paste its output. State the one-spec bound.
  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes.</acceptance_criteria>
  <done>dossiers-a11y.spec.ts reads expected=16 unexpected=0 skipped=0 flaky=0 from a published wrapper report with wrapper exit 0, focus-indicators.spec.ts is gone from index and disk with the a11y population still 182 in 13, and the SUMMARY quotes all of it.</done>
</task>

</tasks>

<verification>
POPULATION: `frontend/tests/a11y/dossiers-a11y.spec.ts` as run under the `a11y` project on this machine,
`frontend/tests/a11y/focus-indicators.spec.ts` (deleted), and this plan's SUMMARY. OUTSIDE IT: the other
twelve a11y specs and their red tests (P101-05 owns those files), the config (P101-01), the CI job
definition (P101-04), and the CI observation of the a11y job (P101-07).
</verification>

<success_criteria>
frontend/tests/a11y/dossiers-a11y.spec.ts passes 16 of 16 with zero skipped, zero flaky and zero unexpected under the a11y project through the reaped wrapper, its JSON reporter output is quoted in the SUMMARY as the run evidence, and frontend/tests/a11y/focus-indicators.spec.ts is deleted with the a11y gate population unchanged at 182 tests in 13 files.
</success_criteria>

<output>
Create `.planning/phases/101-ci-gates-green/101-02-SUMMARY.md` when done. It MUST record every command this
plan ran with its VERBATIM output, the populations it re-derived with the commands that produced them, every
zero beside the control that proves the instrument could have seen a non-zero, and anything left for a named
later task. It MUST carry the stats block, the wrapper exit and report path, the --list prechecks, the deletion record without any literal from the deleted file, and the one-spec bound.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete`.
Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as
**pending** no matter how many gates passed (measured on P99-32 and again on P99-61).
</output>
