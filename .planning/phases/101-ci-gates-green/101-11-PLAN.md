---
phase: 101-ci-gates-green
plan: 11
type: execute
wave: 5
depends_on: [101-07]
files_modified:
  - .planning/phases/101-ci-gates-green/101-11-ATTESTATION.md
  - .planning/phases/101-ci-gates-green/101-11-SUMMARY.md
autonomous: false
requirements: [CARRY-05]
success_criterion: 'RTL Portal + Component Smokes is the ninth required status context on main, added by job name after P101-07 observed that job green on main, the key test-rtl-smokes is not a context, and a smoke PR carrying a deliberately red smoke is observed mergeStateStatus BLOCKED with that check in state FAILURE, all attested by the operator in a file the SUMMARY quotes verbatim.'
routing:
  floor: frontier
truths:
    - 'Decisions covered - D-03: promotion comes AFTER the smoke job is green on main, enforced by depends_on 101-07 (whose attestation records the smoke-row conclusion) and re-read here before the POST; D-02: the context string is the job NAME RTL Portal + Component Smokes, never the key test-rtl-smokes, and the oracle FAILS if the key is ever added because a context no job reports blocks every PR forever under strict true and enforce_admins true; D-12: one operator act, attested, quoted verbatim. Round-2 split: this is the criterion-4 half of the former 101-07.'
must_haves:
  truths:
    - oracle: command
      command: |-
        PATH="/opt/homebrew/bin:$HOME/bin:$PATH"; command -v gh >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: gh absent"; exit 3; }; gh auth status >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: gh not authenticated"; exit 3; }; R=alzahrani-khalid/Intl-Dossier-V2.0
        ROOT=$(git rev-parse --show-toplevel 2>/dev/null) || { echo "INSTRUMENT-CANNOT-RUN: not in a git tree"; exit 3; }
        C=$(gh api repos/$R/branches/main/protection/required_status_checks/contexts 2>&1); RC=$?; [ "$RC" = 0 ] || { echo "INSTRUMENT-CANNOT-RUN: gh api exited $RC :: $C"; exit 3; }
        N=$(printf '%s' "$C" | node -pe 'JSON.parse(require("fs").readFileSync(0,"utf8")).length'); printf '%s' "$C" | grep -q '"Lint"' || { echo "INSTRUMENT-CANNOT-RUN: the control context Lint is not in the list - the API is not answering"; exit 3; }
        A=.planning/phases/101-ci-gates-green/101-11-ATTESTATION.md; PR=$(grep -oE '^SMOKE_PR=[0-9]+' "$ROOT/$A" 2>/dev/null | head -1 | cut -d= -f2)
        if [ -n "$PR" ]; then MS=$(gh pr view -R $R "$PR" --json mergeStateStatus -q .mergeStateStatus 2>&1); SC=$(gh pr checks -R $R "$PR" --json name,state -q '.[] | select(.name=="RTL Portal + Component Smokes") | .state' 2>/dev/null | head -1); else MS="<no SMOKE_PR= line in $A>"; SC="<n/a>"; fi
        echo "P101-C4 contexts_count=$N (8 at planning) has_name=$(printf '%s' "$C" | grep -c '"RTL Portal + Component Smokes"') has_key=$(printf '%s' "$C" | grep -c '"test-rtl-smokes"') smoke_pr=${PR:-<none>} mergeStateStatus=$MS smoke_check=$SC expected has_name=1 has_key=0 count=9 mergeStateStatus=BLOCKED smoke_check=FAILURE (gh --json state vocabulary is upper-case)"
        printf '%s' "$C" | grep -q '"test-rtl-smokes"' && { echo "FAIL: the KEY test-rtl-smokes was added, not the job NAME - a context no job reports blocks every PR forever (D-02)"; exit 1; }
        printf '%s' "$C" | grep -q '"RTL Portal + Component Smokes"' || { echo "FAIL: context 'RTL Portal + Component Smokes' absent from required_status_checks"; exit 1; }
        [ "$N" = 9 ] || { echo "FAIL: $N required contexts, want exactly 9 (the eight at planning plus this one)"; exit 1; }
        [ -n "$PR" ] || { echo "FAIL: no SMOKE_PR=<n> line in $A - the observed-BLOCKED half of criterion 4 has no subject"; exit 1; }
        [ "$MS" = BLOCKED ] && [ "$SC" = FAILURE ] || { echo "FAIL: smoke PR #$PR mergeStateStatus=$MS smoke_check=$SC - want BLOCKED with the smoke check FAILURE"; exit 1; }; echo PASS
      text: 'criterion 4 in both halves: the required contexts on main must contain the NAME RTL Portal + Component Smokes and must NOT contain the KEY test-rtl-smokes (the neighbouring-wrong case that blocks every PR forever), the count must be exactly 9 (the eight at planning plus this one; Lint is the control that the API answers), and the attestation file must carry a SMOKE_PR=<n> line whose PR reads mergeStateStatus BLOCKED with the RTL Portal + Component Smokes check in state FAILURE, read with gh pr checks --json name,state (the job name contains spaces, so a whitespace-split awk read the second WORD of the name and could never match - the round-1 BLOCKER; the JSON state vocabulary is upper-case, measured FAILURE on PR 98). The SMOKE_PR= line is read from 101-11-ATTESTATION.md - THIS plan''s attestation (round 3 fix: the round-2 split left the oracle reading 101-07-ATTESTATION.md, which never carries that line, so criterion 4 was unsatisfiable - checker BLOCKER 9). Drilled 2026-09-10 at HEAD, rounds 1-3: exit 1 - 8 contexts, name absent, key absent, no attestation (the context-absent arm fires first at HEAD, so the smoke-check arm is verified by the PR-98 measurement, not by this drill).'
    - oracle: judge
      text: 'The SUMMARY quotes .planning/phases/101-ci-gates-green/101-11-ATTESTATION.md VERBATIM (the smoke-row conclusion re-read from 101-07-ATTESTATION.md before the POST, the exact POST command and its response, the nine contexts read back, the smoke PR number, the mergeStateStatus and check-state reads with timestamps, the PR closed unmerged and its branch deleted), carries the oracle output verbatim, and records the ordering green smoke row - POST - smoke PR - BLOCKED as performed in that order.'
---

<objective>
Close criterion 4: promote `RTL Portal + Component Smokes` to a required context on `main` BY NAME, only
after P101-07 has observed it green on `main` (D-03), and prove the gate blocks with a smoke PR observed
`BLOCKED`. The string is the job NAME (`ci.yml:381`), never the key `test-rtl-smokes` (`ci.yml:380`): under
`strict: true` + `enforce_admins: true` a context no job reports can never turn green (RESEARCH §2).
</objective>

<context>
@.planning/phases/101-ci-gates-green/101-CONTEXT.md
@.planning/phases/101-ci-gates-green/101-RESEARCH.md

Ref notes: 101-CONTEXT.md - D-02, D-03, D-12; 101-RESEARCH.md - §2 (branch protection: the eight contexts, the exact POST recorded-not-run, PUT vs POST vs DELETE), §8.4 (the oracle and its drill), §9 F5/F8, §10.6 (round-1 BLOCKER fix and the round-2 split).

<interfaces>

</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Operator promotes the context by name after the smoke job is green, then proves BLOCKED with a smoke PR</name>
  <files>none in the tree except the attestation the operator writes</files>
  <action>
    Perform IN THIS ORDER and record each step's timestamp and output in the attestation file.

    1. Re-read `.planning/phases/101-ci-gates-green/101-07-ATTESTATION.md`: the smoke job line must say
       `RTL Portal + Component Smokes = success`. If it does not, STOP - promoting a red job blocks every
       merge (D-03). Record the line you read.
    2. Add the context BY NAME (D-02) - the exact call from RESEARCH §2, recorded there and run here for
       the first time:
       ```
       gh api -X POST repos/alzahrani-khalid/Intl-Dossier-V2.0/branches/main/protection/required_status_checks/contexts \
         --input - <<< '["RTL Portal + Component Smokes"]'
       ```
       (POST appends; PUT would replace the whole list; DELETE with the same body removes it.) Read back:
       `gh api repos/alzahrani-khalid/Intl-Dossier-V2.0/branches/main/protection/required_status_checks/contexts`
       must list nine, including the name, never `test-rtl-smokes`.
    3. Prove the gate blocks: branch `p101-smoke-blocked` from `main`, make ONE deliberately red smoke -
       e.g. in `frontend/tests/e2e/direction-portals.spec.ts` change one `expect(...)` to an impossible
       value - push, open a PR titled `P101 smoke: deliberately red RTL smoke (do not merge)`, wait for
       checks, then read `gh pr view <n> --json mergeStateStatus -q .mergeStateStatus` = `BLOCKED` and
       `gh pr checks <n> --json name,state` showing `RTL Portal + Component Smokes` in state `FAILURE`.
       Record both verbatim. Close the PR unmerged and delete the branch; record that too.
    4. Write `.planning/phases/101-ci-gates-green/101-11-ATTESTATION.md`:
       ```
       # ATTESTATION - P101-11 context promoted by name, smoke PR BLOCKED
       **Who:** <name> (operator)
       **Smoke row re-read from 101-07-ATTESTATION.md:** RTL Portal + Component Smokes = success at <ISO>
       **POST performed:** <ISO>, command as above, response: <verbatim>
       **Contexts after:** <the nine, verbatim>
       SMOKE_PR=<n>
       **Smoke PR read:** mergeStateStatus=BLOCKED at <ISO>; RTL Portal + Component Smokes = FAILURE (gh pr checks --json name,state)
       **Smoke PR closed unmerged, branch deleted:** <ISO>
       ATTESTATION-END
       ```
       The `SMOKE_PR=<n>` line must be exactly that shape at the start of a line - the oracle reads it.
  </action>
  <done>The ninth context added by name after the smoke job was green, a smoke PR observed BLOCKED with the smoke check FAILURE and then closed unmerged, all in the attestation with timestamps.</done>
</task>

<task type="auto">
  <name>the criterion-4 oracle is read after the operator act and the attestation is quoted</name>
  <files>the paths in files_modified</files>
  <read_first>101-CONTEXT.md D-01..D-16 (ALL of them) and 101-RESEARCH.md §0-§10 (§8 holds the drilled oracle candidates, §10 the planner drills of 2026-09-10); re-RUN a recorded command rather than re-deriving its number a second way; then both attestation files if they exist</read_first>
  <action>
    1. If `101-11-ATTESTATION.md` does not exist: write the SUMMARY with `status: PARKED`, the owed step,
       run the oracle anyway and paste its (red) output, and stop. Never auto-answer the checkpoint.
    2. Otherwise quote the attestation VERBATIM under `## Operator attestation (quoted)`.
    3. Run the oracle and paste its output verbatim. Record the ordering with timestamps.
  </action>
  <acceptance_criteria>The oracle passes on live GitHub state after the attestation's POST timestamp, the attestation is quoted verbatim, and the ordering green smoke row - POST - smoke PR - BLOCKED is recorded with timestamps.</acceptance_criteria>
  <done>C4 reads PASS, the attestation is quoted, the ordering is recorded.</done>
</task>

</tasks>

<verification>
POPULATION: the `main` required-status-check contexts, the smoke PR's merge state and check state, the
attestation file and this plan's SUMMARY. OUTSIDE IT: the `main` runs themselves (P101-07), every file in
the worktree plans, and the smoke branch after deletion.
</verification>

<success_criteria>
RTL Portal + Component Smokes is the ninth required status context on main, added by job name after P101-07 observed that job green on main, the key test-rtl-smokes is not a context, and a smoke PR carrying a deliberately red smoke is observed mergeStateStatus BLOCKED with that check in state FAILURE, all attested by the operator in a file the SUMMARY quotes verbatim.
</success_criteria>

<output>
Create `.planning/phases/101-ci-gates-green/101-11-SUMMARY.md` when done. It MUST record every command this
plan ran with its VERBATIM output, the populations it re-derived with the commands that produced them, every
zero beside the control that proves the instrument could have seen a non-zero, and anything left for a named
later task. It MUST carry the attestation QUOTED VERBATIM, the oracle output, and the ordering with timestamps.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete` (or `status: PARKED` with the named operator step still owed, when the human gate has not yet been performed).
Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as
**pending** no matter how many gates passed (measured on P99-32 and again on P99-61).
</output>
