---
phase: 101-ci-gates-green
plan: 5
type: execute
wave: 3
depends_on: [101-01, 101-02]
files_modified:
  - tests/e2e/01-login.spec.ts
  - tests/e2e/03-dossier-navigation.spec.ts
  - tests/e2e/04-command-palette.spec.ts
  - tests/e2e/05-notifications.spec.ts
  - tests/e2e/06-work-item-crud.spec.ts
  - tests/e2e/07-calendar-events.spec.ts
  - tests/e2e/08-export-import.spec.ts
  - tests/e2e/10-operations-hub.spec.ts
  - tests/e2e/elected-official-create.spec.ts
  - tests/e2e/engagement-create.spec.ts
  - tests/e2e/forum-create.spec.ts
  - tests/e2e/fouc-bootstrap.spec.ts
  - tests/e2e/person-identity-fields.spec.ts
  - tests/e2e/phase-36-shell.spec.ts
  - tests/e2e/ar-smoke/command-palette.ar.spec.ts
  - tests/e2e/ar-smoke/dossier-navigation.ar.spec.ts
  - tests/e2e/ar-smoke/login.ar.spec.ts
  - tests/e2e/tailwind-remap-visual.spec.ts
  - tests/e2e/token-engine-sc.spec.ts
  - tests/e2e/typography.spec.ts
  - tests/e2e/working-group-create.spec.ts
  - frontend/tests/a11y/dossiers-rtl-a11y.spec.ts
  - frontend/tests/a11y/intake-accessibility.spec.ts
  - frontend/tests/a11y/positions-a11y-ar.spec.ts
  - frontend/tests/a11y/positions-a11y-en.spec.ts
  - frontend/tests/a11y/wcag-aa-comprehensive-audit.spec.ts
  - frontend/tests/e2e/calendar-rtl.spec.ts
  - frontend/tests/e2e/rtl-component-smokes.spec.ts
  - frontend/tests/e2e/dossier-rtl-mobile.spec.ts
  - .planning/phases/101-ci-gates-green/101-05-SUMMARY.md
autonomous: true
requirements: [CARRY-02, CARRY-09]
success_criterion: 'Every test that failed in main runs 31848669701 and 31848669722 across the 29 named spec files is either fixed (spec drift) or carries an in-spec test.fixme(true, P101-QUAR <run-id>: <reason>) naming the run id and the log line, the SUMMARY register has exactly one row per marker, the root E2E population still lists 220 tests in 65 files, and the three frontend suites run through the wrapper with zero unexpected and zero flaky at their full hard-coded populations of 9, 98 and 182.'
routing:
  floor: frontier
truths:
    - 'Decisions covered - D-09: red tests are FIXED when the cause is spec drift and QUARANTINED with a tracked reason otherwise, the form is in-spec test.fixme(true, P101-QUAR <run-id>: <reason>) per failing test - never a project-level skip, never a deleted assertion - a fix that needs application code is out of scope (Phase 102/103) and is quarantined with the defect named, and the executor derives the failing-test list from the run logs with the count beside it while the planner lists the spec files the logs name (29, derived in RESEARCH §10); D-15: reasons live IN the spec and the tracker is the SUMMARY register (spec, test title, cause class, owner phase), a marker grep with its count is a planning bound and the acceptance is the reporter reading skipped equal to the register; D-13: every frontend run goes through the wrapper, which is why this plan depends on P101-01, and it depends on P101-02 so two wrapped runs never contend for port 5173 in the same wave; D-02: no context is renamed.'
must_haves:
  truths:
    - 'The SUMMARY carries the failing-test derivation per job with the command, the ANSI-strip step (the log stores ESC as the two characters ^[), and the counts beside the CI notices (shard 1: 27 failed; shard 2: 36 failed, 24 of them the tailwind-remap-visual parameter grid; a11y: 18 failed; smokes: 3 failed; RTL + Responsive: 21 failed - 105 in all), the register with one row per in-spec marker and FIVE columns (spec, test title, cause class in {{spec-drift-quarantined, app-defect-quarantined, visual-baseline-quarantined}}, owner phase, cells = the number of reporter cells that one marker skips, 1 for a plain test and N for a test body inside a loop such as 18 for the dossier-rtl-mobile viewport loop or 24 for the tailwind-remap-visual grid), the reconciliation rule stated as skipped >= sum(cells) >= rows per suite (pre-existing fixmes add to skipped, so equality is not claimed), the FIXED tests in a separate table whose rows begin | FIXED | so the register grep cannot count them, the three frontend reporter stats blocks verbatim, and a stated bound: the root shards (e2e.yml) cannot run locally because the six E2E_* keys are absent (RESEARCH §3), so for the 21 root files the local proof is the marker census plus --list (220 in 65) and the run proof is the main run P101-07 observes; a quarantine records that a test is red, never that the behaviour it asserted is absent.'
    - oracle: command
      command: |-
        ROOT=$(git rev-parse --show-toplevel 2>/dev/null) || { echo "INSTRUMENT-CANNOT-RUN: not in a git tree"; exit 3; }; cd "$ROOT" || exit 3; PATH="/opt/homebrew/bin:$HOME/bin:$PATH"
        FILES="tests/e2e/01-login.spec.ts tests/e2e/03-dossier-navigation.spec.ts tests/e2e/04-command-palette.spec.ts tests/e2e/05-notifications.spec.ts tests/e2e/06-work-item-crud.spec.ts tests/e2e/07-calendar-events.spec.ts tests/e2e/08-export-import.spec.ts tests/e2e/10-operations-hub.spec.ts tests/e2e/elected-official-create.spec.ts tests/e2e/engagement-create.spec.ts tests/e2e/forum-create.spec.ts tests/e2e/fouc-bootstrap.spec.ts tests/e2e/person-identity-fields.spec.ts tests/e2e/phase-36-shell.spec.ts tests/e2e/ar-smoke/command-palette.ar.spec.ts tests/e2e/ar-smoke/dossier-navigation.ar.spec.ts tests/e2e/ar-smoke/login.ar.spec.ts tests/e2e/tailwind-remap-visual.spec.ts tests/e2e/token-engine-sc.spec.ts tests/e2e/typography.spec.ts tests/e2e/working-group-create.spec.ts frontend/tests/a11y/dossiers-rtl-a11y.spec.ts frontend/tests/a11y/intake-accessibility.spec.ts frontend/tests/a11y/positions-a11y-ar.spec.ts frontend/tests/a11y/positions-a11y-en.spec.ts frontend/tests/a11y/wcag-aa-comprehensive-audit.spec.ts frontend/tests/e2e/calendar-rtl.spec.ts frontend/tests/e2e/rtl-component-smokes.spec.ts frontend/tests/e2e/dossier-rtl-mobile.spec.ts"
        NF=$(echo $FILES | wc -w | tr -d ' '); [ "$NF" = 29 ] || { echo "INSTRUMENT-CANNOT-RUN: the file list has $NF entries, want 29"; exit 3; }
        for f in $FILES; do [ -f "$f" ] || { echo "INSTRUMENT-CANNOT-RUN: $f missing - a red spec was deleted rather than fixed or quarantined"; exit 3; }; done
        M=$(cat $FILES | grep -c "test.fixme(true, 'P101-QUAR 3184866"); MB=$(cat $FILES | grep -c "test.fixme(true, 'P101-QUAR"); PF=$(cat $FILES | grep -c "test.fixme(")
        L=$(pnpm exec playwright test --project=chromium-en --project=chromium-ar-smoke --no-deps --list 2>&1 | grep -oE "Total: [0-9]+ tests in [0-9]+ files")
        CTRL=$(grep -c "test.fixme(" frontend/tests/a11y/positions-keyboard-nav.spec.ts)
        S=.planning/phases/101-ci-gates-green/101-05-SUMMARY.md; REG=$(awk '/^## Quarantine register/{f=1;next} /^## /{f=0} f' "$S" 2>/dev/null | grep -E '^\| (tests/e2e/|frontend/tests/)[^|]+\|'); ROWS=$(printf '%s\n' "$REG" | grep -c .); CELLS=$(printf '%s\n' "$REG" | grep -E '^\|' | awk -F'|' '{n=$6; gsub(/ /,"",n); if(n ~ /^[0-9]+$/) c+=n; else bad++} END{print c+0" "bad+0}'); CSUM=${CELLS% *}; CBAD=${CELLS#* }
        echo "P101-05-BOUND quarantine markers with a run id=$M (any P101-QUAR=$MB, any fixme incl. pre-existing=$PF; control positions-keyboard-nav fixme=$CTRL) register rows=$ROWS register cells total=$CSUM (rows with a non-numeric cells column=$CBAD) root list=[$L] want markers>=1 markers==any-P101-QUAR rows==markers cells>=rows non-numeric=0 list='Total: 220 tests in 65 files'"
        [ "$CTRL" = 13 ] || { echo "INSTRUMENT-CANNOT-RUN: the fixme grep control returned $CTRL, want 13"; exit 3; }
        [ -n "$L" ] || { echo "INSTRUMENT-CANNOT-RUN: playwright --list printed no Total line"; exit 3; }
        [ "$L" = "Total: 220 tests in 65 files" ] || { echo "FAIL: the root E2E population moved to [$L] - a quarantine keeps every test listed (in-spec fixme, never a deleted test or a project-level skip)"; exit 1; }
        [ "$M" -ge 1 ] && [ "$M" = "$MB" ] || { echo "FAIL: $M markers carry a run id 3184866*, $MB carry P101-QUAR at all - every reason names the run id (D-09)"; exit 1; }
        [ "$ROWS" = "$M" ] || { echo "FAIL: the SUMMARY register (the ## Quarantine register section only) has $ROWS rows for $M in-spec markers (D-15: the SUMMARY is the tracker, one row per marker; FIXED rows live in their own table and begin | FIXED |)"; exit 1; }
        [ "$CBAD" = 0 ] && [ "$CSUM" -ge "$ROWS" ] || { echo "FAIL: the register cells column (5th) must be a positive integer per row - $CBAD rows are not numeric, total $CSUM for $ROWS rows (a parametrised test skips one cell per iteration; the column is what reconciles skipped against the register)"; exit 1; }
        echo PASS
      text: 'the planning bound for all 29 files, runnable without credentials: every one of the 29 must still exist (a deleted spec exits 3 - deletion is not a quarantine), the root E2E population must still list exactly Total: 220 tests in 65 files (an in-spec fixme keeps the test listed; a project-level skip or a deleted test would move this number), the marker count with a 3184866* run id must equal the count of any P101-QUAR marker (every reason names the run id) and be at least 1, and the SUMMARY register rows - counted ONLY inside the ## Quarantine register section (up to the next ## heading), lines beginning | tests/e2e/ or | frontend/tests/ - must equal the marker count exactly (D-15), with the fifth column (cells) a positive integer on every row and its total at least the row count; the pre-existing fixme population and the positions-keyboard-nav count of 13 are printed as the grep control. Drilled 2026-09-10 at HEAD and again in round 1: exit 1 - markers 0, rows 0, cells 0, list 220 in 65, control 13.'
    - oracle: command
      command: |-
        ROOT=$(git rev-parse --show-toplevel 2>/dev/null) || { echo "INSTRUMENT-CANNOT-RUN: not in a git tree"; exit 3; }; cd "$ROOT/frontend" || exit 3; PATH="/opt/homebrew/bin:$HOME/bin:$PATH"
        for t in python3 lsof node pnpm; do command -v $t >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: $t absent"; exit 3; }; done
        grep -q 'pw-run-reaped.mjs --lease-exec' playwright.config.ts || { echo "INSTRUMENT-CANNOT-RUN: frontend/playwright.config.ts is not routed through the lease writer (P101-01) - D-13 forbids an unwrapped run"; exit 3; }
        set -a; . ../.env.test 2>/dev/null; set +a; [ -n "${TEST_USER_EMAIL:-}" ] || { echo "INSTRUMENT-CANNOT-RUN: TEST_USER_EMAIL unset"; exit 3; }
        [ -z "${E2E_BASE_URL:-}" ] || { echo "INSTRUMENT-CANNOT-RUN: E2E_BASE_URL set"; exit 3; }
        [ "$(lsof -ti tcp:5173 -sTCP:LISTEN 2>/dev/null | wc -l | tr -d ' ')" = 0 ] || { echo "INSTRUMENT-CANNOT-RUN: 5173 already held"; exit 3; }
        FAIL=0; mkdir -p test-results; SUMM=../.planning/phases/101-ci-gates-green/101-05-SUMMARY.md
        regcount() { awk '/^## Quarantine register/{f=1;next} /^## /{f=0} f' "$SUMM" 2>/dev/null | grep -E "^\| frontend/tests/($1)[^|]*\|" | awk -F'|' '{r++; n=$6; gsub(/ /,"",n); if(n ~ /^[0-9]+$/) c+=n} END{print r+0" "c+0}'; }
        one() { NAME=$1; N=$2; PAT=$3; shift 3; RC_=$(regcount "$PAT"); RROWS=${RC_% *}; RCELLS=${RC_#* }; L=$(pnpm exec playwright test "$@" --list 2>&1 | grep -oE "Total: [0-9]+ tests"); [ "$L" = "Total: $N tests" ] || { echo "INSTRUMENT-CANNOT-RUN: $NAME lists [$L], want 'Total: $N tests'"; exit 3; }
          MK="test-results/p101-05-$NAME.mark"; touch "$MK"; node ../scripts/pw-run-reaped.mjs -- "$@" > "test-results/p101-05-$NAME.wrapper.log" 2>&1; WRC=$?
          R=$(find test-results -maxdepth 1 -name 'pw-reaped-*.json' -newer "$MK" | head -1); [ -n "$R" ] && [ "$WRC" != 90 ] || { echo "FAIL: $NAME wrapper rc=$WRC published report=[$R] - an unclean session withholds the report (90)"; FAIL=1; return; }
          node -e 'const s=JSON.parse(require("fs").readFileSync(process.argv[1],"utf8")).stats;const N=+process.argv[2],RR=+process.argv[4],RC=+process.argv[5];console.log(`P101-05-RUN ${process.argv[3]} expected=${s.expected} unexpected=${s.unexpected} skipped=${s.skipped} flaky=${s.flaky} sum=${s.expected+s.skipped} register_rows=${RR} register_cells=${RC} want unexpected=0 flaky=0 expected+skipped=${N} skipped>=register_cells>=register_rows (pre-existing fixmes add to skipped, so >= not ==)`);process.exit(s.unexpected===0&&s.flaky===0&&s.expected+s.skipped===N&&s.skipped>=RC&&RC>=RR?0:1)' "$R" "$N" "$NAME" "$RROWS" "$RCELLS" || FAIL=1; }
        one smokes 9 "e2e/direction-portals|e2e/calendar-rtl|e2e/rtl-component-smokes" e2e/direction-portals.spec.ts e2e/calendar-rtl.spec.ts e2e/rtl-component-smokes.spec.ts --project=chromium
        one rtl-mobile 98 "e2e/dossier-rtl-mobile" e2e/dossier-rtl-mobile.spec.ts --project=chromium
        one a11y 182 "a11y/" --project=a11y
        [ "$FAIL" = 0 ] || exit 1; echo PASS
      text: 'the frontend acceptance, three wrapped runs from ./frontend with hard-coded populations: smokes (direction-portals + calendar-rtl + rtl-component-smokes, --list must say 9), RTL + Responsive (dossier-rtl-mobile, 98) and the a11y project (182); each --list mismatch exits 3, each run must publish a report (exit 90 withholds it and FAILS), and each stats block must read unexpected=0 flaky=0 with expected+skipped equal to the population, and skipped must be at least the register cells total which must be at least the register rows for that suite''s spec files (rows read from the ## Quarantine register section of 101-05-SUMMARY.md; >= not == because pre-existing fixmes also skip) - the D-15 reconciliation for parametrised tests (round 1, issue 3). Precheck exits 3 unless the frontend config is routed (D-13), credentials are materialised, E2E_BASE_URL is unset and 5173 is free. Drilled 2026-09-10 at HEAD: exit 3 at the routing precheck (the three --list populations were drilled separately: 9, 98, 182).'
    - oracle: judge
      text: 'The SUMMARY carries the per-job derivation with counts matching the CI notices, a register with one row per marker whose reasons each name a run id and a log line, the three stats blocks verbatim with skipped reconciled against the register, the fixes described as spec drift with the drift named (for example the duplicated complementary landmark, the second password-labelled control, the .cal-dow class that no longer exists, the language-switch mechanism the app no longer honours), and the root-shards bound stated in those terms.'
---

<objective>
Make criterion 1 and the E2E/a11y/smokes/RTL rows of criterion 5 honest: every test that was red on
`main` on 2026-08-14 is either fixed - when the cause is spec drift - or quarantined in the spec with a
reason that names the run id and the log line (D-09), and the SUMMARY is the register (D-15).

The populations, derived tonight from the job logs (RESEARCH §10; the executor re-derives):

| job | run / job id | CI notice | files named |
| --- | --- | --- | --- |
| E2E (shard 1/2), root config | 31848669722 / 94920109119 | 27 failed | 14 under `tests/e2e/` |
| E2E (shard 2/2), root config | 31848669722 / 94920109185 | 36 failed (24 = tailwind-remap-visual grid) | 7 under `tests/e2e/` |
| Accessibility Tests (RTL + WCAG AA) | 31848669701 / 94920552794 | 18 failed | 5 under `frontend/tests/a11y/` |
| RTL Portal + Component Smokes | 31848669701 / 94920552761 | 3 failed | 2 under `frontend/tests/e2e/` |
| RTL + Responsive Tests | 31848669701 / 94920552740 | 21 failed | 1 under `frontend/tests/e2e/` |

First causes (RESEARCH §1.3): strict-mode locator drift (`getByLabel(/password/)` resolves to 2,
`getByRole('complementary')` resolves to 2), a Phase-33 token-engine contract, `missingLabels` on dossier
pages, `.cal-dow` gone from the calendar after the Linear refinement, `<html dir>` staying `ltr` because
the spec switches language by a mechanism the app no longer honours (language persists under `id.locale`).

`dossiers-a11y.spec.ts` (P101-02) and `focus-indicators.spec.ts` (deleted by P101-02) are NOT in this
plan's files. Nothing under `frontend/src` or `backend/src` is: an application defect is quarantined with
the defect named and left to Phase 102/103.
</objective>

<context>
@.planning/phases/101-ci-gates-green/101-CONTEXT.md
@.planning/phases/101-ci-gates-green/101-RESEARCH.md

Ref notes: 101-CONTEXT.md - D-09 (fix vs quarantine, the form), D-15 (register), D-13, D-02; 101-RESEARCH.md - §1.2/§1.3 (failing step and first cause per suite, with log lines), §3 (why the root shards cannot run locally), §5 (the a11y --list table), §8.1/§8.5, §10 (the 29-file derivation with commands and counts).

<interfaces>

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>every red E2E, a11y, smokes and RTL test is fixed for drift or quarantined in-spec with a tracked reason</name>
  <files>the 29 spec files in files_modified and the SUMMARY</files>
  <read_first>101-CONTEXT.md D-01..D-16 (ALL of them) and 101-RESEARCH.md §0-§10 (§8 holds the drilled oracle candidates, §10 the planner drills of 2026-09-10); re-RUN a recorded command rather than re-deriving its number a second way; then the five job logs named above (`gh run view --job <id> --log-failed`), stripped with `sed -E 's/\^\[\[[0-9;]*m//g'`</read_first>
  <action>
    1. DERIVE the failing tests per job, with the count beside each (D-09: the executor derives; the
       planner's file list is a bound). For each job id above:
       ```
       gh run view -R alzahrani-khalid/Intl-Dossier-V2.0 --job <id> --log-failed | sed -E 's/\^\[\[[0-9;]*m//g' > /tmp/p101-<id>.log
       grep -oE '\[[a-z0-9-]+\] › tests/[A-Za-z0-9_./-]+\.spec\.ts:[0-9]+:[0-9]+ › .*$' /tmp/p101-<id>.log | sed -E 's/ \(retry #[0-9]+\)//; s/[ ─]+$//' | sort -u
       grep -oE '[0-9]+ failed' /tmp/p101-<id>.log | tail -1
       ```
       Parametrised tests (the 24-cell `tailwind-remap-visual` grid, the per-dossier-type loops in
       `dossiers-rtl-a11y`, the viewport loop in `dossier-rtl-mobile`) share one `file:line:col` and differ
       by title - dedupe on the full row, and drop a row that is a strict prefix of another (the log
       truncates one line). Your per-job totals must equal the CI notices (27, 36, 18, 3, 21). Record the
       derivation verbatim.
    2. For each failing test decide, from its error in the log:
       - **spec drift** (the app changed; the assertion's intent still holds): FIX the spec. Examples:
         `getByLabel(/password/)` to the `#password` control; `getByRole('complementary')` to the named
         landmark; `.cal-dow` to whatever the calendar day-of-week header now renders
         (`frontend/src/components/calendar/CalendarMonthGrid.tsx` still emits `className="cal-dow"` with
         `data-testid="cal-dow"` at HEAD - so read the failure trace before assuming the class is gone; the
         count 0 may be a render-timing or route defect); the language switch in `dossier-rtl-mobile` to
         the `[data-lang]` topbar control `direction-portals.spec.ts` already uses.
       - **anything that needs application code** (the token-engine contract, `missingLabels` on a dossier
         page, a real a11y defect): QUARANTINE - inside the test body, first statement:
         `test.fixme(true, 'P101-QUAR <run-id>: <cause in one sentence>; log line <n> of job <job-id>; owner Phase 10N')`
         with `<run-id>` = 31848669722 (shards) or 31848669701 (CI). One marker per failing test, in the
         test, never `test.describe.configure`, never a project-level skip, never a deleted `expect`.
       - **a visual baseline** (tailwind-remap-visual, 24 cells): a baseline that no longer matches is an
         app-or-baseline question this phase does not answer (CARRY-06, Phase 102 owns the visual debt);
         quarantine each failing cell with the owner phase named. Do NOT regenerate baselines.
    3. Verify the root population WITHOUT a run (credentials are absent locally, RESEARCH §3):
       `pnpm exec playwright test --project=chromium-en --project=chromium-ar-smoke --no-deps --list` must
       still print `Total: 220 tests in 65 files`.
    4. Run the three frontend suites THROUGH the wrapper (this plan's second oracle does exactly this; it
       needs 5173 free and P101-01 landed) and paste the three stats blocks. A fixed test must now pass; a
       quarantined test reads as skipped; `unexpected` must be 0. If a fix does not hold, quarantine it
       with the observed error rather than iterating on the application.
    5. Write the register in the SUMMARY under `## Quarantine register` (the oracle reads ONLY that
       section, up to the next `## ` heading): one markdown row per in-spec marker, FIVE columns,
       `| <spec path> | <test title> | <cause class> | <owner phase> | <cells> |`, rows beginning with
       `| tests/e2e/` or `| frontend/tests/`. `cells` = how many reporter cells that one marker skips: 1
       for a plain test, N for a test body inside a loop (dossier-rtl-mobile viewport loop 18,
       tailwind-remap-visual grid 24, each dossiers-rtl-a11y test 6). The reconciliation the second
       oracle asserts is `skipped >= sum(cells) >= rows` per suite. Put the FIXED tests in a SEPARATE
       table under `## Fixed tests` whose rows begin `| FIXED | <spec> | <test> | <drift named> |` -
       that prefix keeps them out of the register grep. Run the first oracle (the bound) and paste it:
       marker count = register rows, cells total >= rows.
    6. State the root-shards bound from the must-have truth.
  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, both oracles pass, and no path outside files_modified changes.</acceptance_criteria>
  <done>Every one of the 105 red tests is fixed or carries a P101-QUAR marker naming its run id, the register matches the markers, the root list is still 220 in 65, and the three frontend suites run wrapped with unexpected=0 flaky=0 at 9, 98 and 182.</done>
</task>

</tasks>

<verification>
POPULATION: the 29 spec files named in files_modified, the tests inside them that CI reported red, and
this plan's SUMMARY. OUTSIDE IT: every other spec (the 44 root spec files that were green, the twelve a11y
specs' passing tests, `dossiers-a11y.spec.ts` - P101-02), application code (Phase 102/103 own the defects
behind quarantined tests), visual baselines (CARRY-06, Phase 102), the workflow (P101-04), the configs
(P101-01) and the CI observation (P101-07).
</verification>

<success_criteria>
Every test that failed in main runs 31848669701 and 31848669722 across the 29 named spec files is either fixed (spec drift) or carries an in-spec test.fixme(true, P101-QUAR <run-id>: <reason>) naming the run id and the log line, the SUMMARY register has exactly one row per marker, the root E2E population still lists 220 tests in 65 files, and the three frontend suites run through the wrapper with zero unexpected and zero flaky at their full hard-coded populations of 9, 98 and 182.
</success_criteria>

<output>
Create `.planning/phases/101-ci-gates-green/101-05-SUMMARY.md` when done. It MUST record every command this
plan ran with its VERBATIM output, the populations it re-derived with the commands that produced them, every
zero beside the control that proves the instrument could have seen a non-zero, and anything left for a named
later task. It MUST carry the per-job derivation with counts equal to the CI notices, the quarantine register and the fixed-tests table, the three wrapped stats blocks, and the root-shards bound.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete`.
Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as
**pending** no matter how many gates passed (measured on P99-32 and again on P99-61).
</output>
