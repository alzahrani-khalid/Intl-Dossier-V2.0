---
phase: 76-rtl-infrastructure-bridge-shadcn-logical-properties
plan: 03
type: execute
wave: 2
depends_on: [76-01]
files_modified:
  - frontend/src/components/ui/accordion.tsx
  - frontend/src/components/ui/dropdown-menu.tsx
  - frontend/src/components/ui/heroui-tabs.tsx
  - frontend/src/components/ui/navigation-menu.tsx
  - frontend/src/components/ui/scroll-area.tsx
  - frontend/src/components/ui/select.tsx
  - frontend/src/components/ui/slider.tsx
  - frontend/src/components/ui/toggle-group.tsx
  - frontend/src/lib/utils.ts
  - frontend/tests/e2e/direction-portals.spec.ts
  - frontend/tests/e2e/rtl-switching.spec.ts
autonomous: true
requirements: [RTLB-01, RTLB-02]

must_haves:
  truths:
    - 'Clicking the topbar language control flips document.dir AND every mounted Radix portal in the same frame, in both directions (76 success criterion 1)'
    - 'In Arabic, Popover/Tooltip/Dropdown/Sheet and the dossier drawer open/animate from the inline-start-correct edge, mirroring English (76 success criterion 2)'
    - 'No Radix wrapper pins a render-time-stale direction: default direction comes from the DirectionProvider context, not from getDocDir() prop defaults'
    - 'First-load ?lng=ar with a conflicting seeded id.locale=en still paints RTL (Pitfall 5 regression)'
  artifacts:
    - path: 'frontend/tests/e2e/direction-portals.spec.ts'
      provides: 'Same-frame flip + portal edge-correctness e2e (RTLB-02, Wave 0 gap)'
      min_lines: 60
  key_links:
    - from: 'frontend/src/components/ui/dropdown-menu.tsx'
      to: '@radix-ui/react-direction context'
      via: 'dir prop left undefined by default (Radix falls back to context)'
      pattern: "dir=\\{dir\\}"
    - from: 'frontend/tests/e2e/direction-portals.spec.ts'
      to: 'Topbar locale control'
      via: '[data-lang="ar"] click'
      pattern: 'data-lang'
---

<objective>
Make the single owner actually reach every Radix portal in the same frame: replace the 8 per-component `dir={dir ?? getDocDir()}` defaults with `dir={dir}` so the context (mounted in Plan 01) provides the default, then prove RTLB-01's same-frame bar and RTLB-02's portal edge correctness with a new e2e spec.

Purpose: `getDocDir()` reads `document.documentElement.dir` at RENDER time, but renders happen before the owner's `useLayoutEffect` in the same commit — the prop would pin the OLD direction and override the fresh Radix context (Radix: explicit `dir` prop beats context). Dropping the default is required for same-frame correctness, not cosmetic cleanup (RESEARCH Pitfall 7 + Open Q2 resolution).
Output: 8 simplified wrappers, getDocDir removed from lib/utils.ts, direction-portals.spec.ts, extended rtl-switching.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/76-RESEARCH.md

<interfaces>
<!-- Verified against the working tree 2026-07-02. -->

Wrapper pattern to change (identical shape in all 8 files; example from ui/select.tsx):

```tsx
import { cn, getDocDir } from '@/lib/utils'
return <SelectPrimitive.Root data-slot="select" dir={dir ?? getDocDir()} {...props} />
```

Target: `dir={dir}` — an undefined prop makes Radix resolve direction from the DirectionProvider context (prop overrides context ONLY when defined). Keep the `dir` prop in each wrapper's signature: per-instance overrides (e.g. LtrIsolate scenarios) must keep working.

getDocDir definition to delete once orphaned (frontend/src/lib/utils.ts lines 8-17). `cn` stays. Only ui/ files import getDocDir today.

Topbar locale control (frontend/src/components/layout/Topbar.tsx ~229-250): two radio buttons with `data-lang="en"` / `data-lang="ar"`, onClick → `setLocale(...)` (DesignProvider). These are the e2e toggle handles.

Existing e2e helpers to reuse:

- frontend/tests/e2e/rtl-switching.spec.ts — `authBypass(page)` (seeds auth-storage) + `seedLocale(page, 'en'|'ar')` (seeds localStorage id.locale); route used: /responsive-demo (NO topbar/portals — fine for seeded-load asserts only)
- frontend/tests/e2e/support/list-pages-auth.ts — `loginForListPages(page, 'ar'?)` real login for protected pages (topbar visible)
- frontend/tests/e2e/support/dossier-drawer-fixture.ts — `openDrawerForFixtureDossier(page, { id: FIXTURE_DOSSIER_ID, type: 'country' })` opens the Sheet-based drawer
- frontend/tests/e2e/dossier-drawer-rtl.spec.ts — geometry assertion pattern: `.drawer` locator, computed `insetInlineEnd === '0px'`, `rect.left === 0` under RTL

Portal content slots for assertions: `[data-slot="popover-content"]`, `[data-slot="dropdown-menu-content"]`, `[data-slot="select-content"]` (shadcn data-slot convention across ui/).

i18n detector order is `querystring` first (`?lng=ar` wins over localStorage id.locale) — the Pitfall 5 case.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop the getDocDir() defaults in all Radix wrappers; delete the orphaned helper</name>
  <files>frontend/src/components/ui/accordion.tsx, frontend/src/components/ui/dropdown-menu.tsx, frontend/src/components/ui/heroui-tabs.tsx, frontend/src/components/ui/navigation-menu.tsx, frontend/src/components/ui/scroll-area.tsx, frontend/src/components/ui/select.tsx, frontend/src/components/ui/slider.tsx, frontend/src/components/ui/toggle-group.tsx, frontend/src/lib/utils.ts</files>
  <read_first>
    - frontend/src/lib/utils.ts (getDocDir definition + JSDoc)
    - frontend/src/components/ui/select.tsx and frontend/src/components/ui/dropdown-menu.tsx (the exact `dir={dir ?? getDocDir()}` shape before editing the rest)
    - .planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/76-RESEARCH.md (Pitfall 7 — sequencing rationale; provider is already mounted by Plan 01)
  </read_first>
  <action>
    First run `grep -rn "getDocDir" frontend/src` and fix EVERY hit, not just the 8 named files (type-discriminator drift lesson: grep the whole surface before fixing named sites). Verified hits today: accordion.tsx, dropdown-menu.tsx, heroui-tabs.tsx, navigation-menu.tsx, scroll-area.tsx, select.tsx, slider.tsx, toggle-group.tsx, plus the lib/utils.ts definition.

    In each wrapper: change `dir={dir ?? getDocDir()}` to `dir={dir}` and remove `getDocDir` from the `@/lib/utils` import (keep `cn`). Do NOT remove the `dir` prop from any component signature — per-instance overrides must survive (Radix prop overrides context when defined; Phase 75 audit marks this wiring "must survive reskin", and context-derived defaults satisfy that intent).

    Then re-run the grep: with zero importers remaining, delete the `getDocDir` function and its JSDoc from frontend/src/lib/utils.ts (orphan created by this change — our mess, our cleanup). `cn` stays untouched.

    Do NOT touch frontend/src/components/ui/ltr-isolate.tsx (explicit dir="ltr" content boundary, retained by design).

  </action>
  <verify>
    <automated>test "$(grep -rln 'getDocDir' frontend/src | wc -l | tr -d ' ')" = "0" && cd frontend && pnpm type-check && pnpm exec vitest run src/components/ui/__tests__/direction.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "getDocDir" frontend/src` returns 0 hits (all call sites converted, definition deleted)
    - Each of the 8 wrapper files contains `dir={dir}` on its Radix Root (e.g. `grep -c "dir={dir}" frontend/src/components/ui/select.tsx` >= 1) and no `?? getDocDir()` remains anywhere
    - `cn` still exported from frontend/src/lib/utils.ts
    - `cd frontend && pnpm type-check` exits 0
    - frontend/src/components/ui/ltr-isolate.tsx unchanged (`git diff --stat` does not list it)
  </acceptance_criteria>
  <done>All Radix wrappers inherit direction from the DirectionProvider context by default; no render-time-stale direction source remains.</done>
</task>

<task type="auto">
  <name>Task 2: Write direction-portals.spec.ts and extend rtl-switching.spec.ts; run the RTL regression set</name>
  <files>frontend/tests/e2e/direction-portals.spec.ts, frontend/tests/e2e/rtl-switching.spec.ts</files>
  <read_first>
    - frontend/tests/e2e/rtl-switching.spec.ts (authBypass/seedLocale helpers; extend this file with live-toggle cases)
    - frontend/tests/e2e/support/list-pages-auth.ts (loginForListPages — real login onto pages with the topbar)
    - frontend/tests/e2e/dossier-drawer-rtl.spec.ts + frontend/tests/e2e/support/dossier-drawer-fixture.ts (proven drawer-open + geometry-assert patterns to reuse)
    - frontend/tests/e2e/list-pages-rtl.spec.ts (proven selectors for portal triggers on list pages — pick a stable dropdown/select trigger from here)
    - .planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/76-RESEARCH.md ("Same-frame flip verification (Playwright)" code example — the rAF-gated evaluate shape)
  </read_first>
  <action>
    Create frontend/tests/e2e/direction-portals.spec.ts with four cases (use `test.describe.configure({ retries: 1 })` like the drawer spec):

    1. Same-frame toggle (EN→AR and AR→EN): `loginForListPages(page)` onto a protected page with the topbar; click `[data-lang="ar"]`; in a single `requestAnimationFrame`-gated `page.evaluate` assert `document.documentElement.dir === 'rtl'` and `document.documentElement.lang === 'ar'`. Mirror for `[data-lang="en"]`.
    2. Portal across the toggle: open a Radix portal first (stable dropdown/select trigger from list-pages-rtl.spec.ts, or `[data-slot="popover-trigger"]` if one is present on the page), then click `[data-lang="ar"]`, then in ONE rAF-gated evaluate read both `document.documentElement.dir` and the portal content's direction (`getAttribute('dir')` falling back to `getComputedStyle(...).direction` on `[data-slot="dropdown-menu-content"]` / the applicable content slot) — both must be 'rtl'. Note: outside-click dismissal keeps the content mounted through its exit animation, so it stays queryable in that frame; the retries:1 config absorbs animation-timing variance.
    3. Portal opened after the toggle (edge correctness, RTLB-02): while in AR, open the same portal and assert its content computed `direction === 'rtl'`; then reuse the dossier-drawer-fixture helpers to open the drawer in AR and assert the drawer geometry (computed `insetInlineEnd === '0px'` and `rect.left === 0`) — the drawer is the Sheet side="right" portal.
    4. Pitfall 5 cold-load: `seedLocale(page, 'en')` then `page.goto('<route>?lng=ar')` and assert `html[dir="rtl"]` on first load (querystring detector beats the stale id.locale, and the owner's mount-time useLayoutEffect applies it before React paint).

    Extend frontend/tests/e2e/rtl-switching.spec.ts (keep the two existing seeded tests) with a live-toggle test: login, click `[data-lang="ar"]`, assert `html[dir="rtl"]` AND `localStorage['id.locale'] === 'ar'` (i18next detector caches persist it), then toggle back to EN and assert both again.

  </action>
  <verify>
    <automated>cd frontend && pnpm exec playwright test tests/e2e/direction-portals.spec.ts tests/e2e/rtl-switching.spec.ts tests/e2e/dossier-drawer-rtl.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - frontend/tests/e2e/direction-portals.spec.ts exists with the 4 cases above; contains `requestAnimationFrame` and `data-lang="ar"`
    - `cd frontend && pnpm exec playwright test tests/e2e/direction-portals.spec.ts` exits 0
    - `cd frontend && pnpm exec playwright test tests/e2e/rtl-switching.spec.ts` exits 0 (existing seeded cases + new live-toggle case)
    - `cd frontend && pnpm exec playwright test tests/e2e/dossier-drawer-rtl.spec.ts` exits 0 (drawer edge geometry regression green post-consolidation)
    - rtl-switching.spec.ts asserts `id.locale` equals the toggled language after a live toggle
  </acceptance_criteria>
  <done>Same-frame flip and portal edge correctness are e2e-proven in both directions; drawer geometry regression green; ?lng=ar cold-load behavior preserved.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                                                  |
| -------- | ------------------------------------------------------------------------------------------------------------ |
| none new | This plan removes a render-time DOM read and adds test files; no new input, dependency, or execution surface |

## STRIDE Threat Register

| Threat ID | Category  | Component                               | Disposition | Mitigation Plan                                                                                                                                           |
| --------- | --------- | --------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-76-05   | Tampering | Radix dir prop overrides (per-instance) | accept      | `dir` prop passthrough retained by design for LtrIsolate-style content boundaries; it selects rendering direction only — no security decision keys off it |
| T-76-SC   | Tampering | npm/pip/cargo installs                  | accept      | No packages installed by this plan                                                                                                                        |

</threat_model>

<verification>
- `grep -rn "getDocDir" frontend/src` → 0 hits
- `cd frontend && pnpm type-check && pnpm exec vitest run src/components/ui/__tests__/direction.test.tsx` → green
- `cd frontend && pnpm exec playwright test tests/e2e/direction-portals.spec.ts tests/e2e/rtl-switching.spec.ts tests/e2e/dossier-drawer-rtl.spec.ts` → green
</verification>

<success_criteria>

- 76 success criteria 1 and 2 e2e-proven: same-frame document+portal flip in both directions; portals and the dossier drawer open from the inline-start-correct edge in AR
- Zero remaining non-context direction defaults on Radix wrappers
  </success_criteria>

<output>
Create `.planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/76-03-SUMMARY.md` when done.
</output>
