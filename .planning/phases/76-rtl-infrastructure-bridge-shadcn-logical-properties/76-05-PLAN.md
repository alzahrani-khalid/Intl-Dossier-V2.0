---
phase: 76-rtl-infrastructure-bridge-shadcn-logical-properties
plan: 05
type: execute
wave: 4
depends_on: [76-04]
files_modified:
  - .planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/76-SRTL02-VERIFICATION.md
  - .planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/evidence/
autonomous: false
requirements: [SRTL-02]

must_haves:
  truths:
    - 'Calendar, Pagination, and Sidebar (the CLI-exempt components) are verified RTL-correct in Arabic post-migrate, with a written verification record and screenshots'
    - 'The three existing hand-patches (calendar rdp chevron rotations, pagination rtl:rotate-180, sidebar rtl:-scale-x-100) are confirmed functional in the browser, not just present in source'
    - "A human has approved the visual RTL correctness (SRTL-02's manual-verification bar)"
  artifacts:
    - path: '.planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/76-SRTL02-VERIFICATION.md'
      provides: 'SRTL-02 verification record: per-component checklist, evidence links, human sign-off'
      min_lines: 30
  key_links:
    - from: '76-SRTL02-VERIFICATION.md'
      to: 'evidence/ screenshots'
      via: 'relative links per component (calendar, pagination, sidebar)'
      pattern: 'evidence/'
---

<objective>
Verify the three CLI-exempt components (Calendar, Pagination, Sidebar) are RTL-correct in Arabic (SRTL-02). All three are ALREADY hand-patched in this repo — this is verify-no-regress after the migrate run, not build-from-scratch: automated evidence first (existing calendar e2e + source greps + AR screenshots), then a human visual sign-off.

Purpose: shadcn's docs exempt these three from the auto-migrate; the phase success criterion requires manual Arabic verification with a durable record.
Output: 76-SRTL02-VERIFICATION.md + evidence screenshots + human approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/76-RESEARCH.md

<interfaces>
<!-- Verified against the working tree 2026-07-02 (RESEARCH Pattern 5). -->

| Component                      | Existing patch (must be live)                                                   | Verify in AR                                                                             |
| ------------------------------ | ------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| ui/calendar.tsx ~27-28         | `rtl:**:[.rdp-button\_next>svg]:rotate-180` (+ `_previous`) on react-day-picker | Month-nav chevrons point correctly; next/prev advance the RIGHT way (next = later month) |
| ui/pagination.tsx lines 58, 71 | `rtl:rotate-180` on ChevronLeft/Right                                           | Prev/next affordances mirror; aria-current unaffected                                    |
| ui/sidebar.tsx line 329        | `rtl:-scale-x-100` on PanelLeft toggle icon                                     | Rail collapses to the correct edge; toggle icon mirrored                                 |

Existing automated coverage: frontend/tests/e2e/calendar-rtl.spec.ts (reusable as regression gate). No existing spec covers Pagination/Sidebar visuals — that is WHY this requirement is manual-verification (justified in RESEARCH Validation Architecture).

AR entry: topbar ع control (`[data-lang="ar"]`) or seeded `localStorage['id.locale'] = 'ar'`. Language persists under `id.locale`, NOT `i18nextLng`.

Screenshot tooling on this machine: Playwright screenshots via a throwaway spec/script, or browser-harness with a dedicated headless Chrome + BU_CDP_URL (the user's Chrome cannot be attached — established project fact). Dev server: `pnpm dev` → :5173 (Playwright webServer auto-starts it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated SRTL-02 evidence — e2e regression, source greps, AR screenshots, verification record</name>
  <files>.planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/76-SRTL02-VERIFICATION.md, .planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/evidence/</files>
  <read_first>
    - frontend/tests/e2e/calendar-rtl.spec.ts (existing calendar RTL assertions — run as-is; reuse its navigation pattern for the screenshot capture)
    - frontend/tests/e2e/support/list-pages-auth.ts (loginForListPages(page, 'ar') for authenticated AR captures)
    - frontend/src/components/ui/calendar.tsx, frontend/src/components/ui/pagination.tsx, frontend/src/components/ui/sidebar.tsx (confirm the hand-patches in current post-migrate state)
  </read_first>
  <action>
    1. Run `cd frontend && pnpm exec playwright test tests/e2e/calendar-rtl.spec.ts` — must pass (calendar RTL regression gate).
    2. Source-level confirmation the migrate run did not mangle the hand-patches (same greps as Plan 04, re-run here as the SRTL-02 record): pagination `rtl:rotate-180` count == 2; sidebar `rtl:-scale-x-100` >= 1; calendar `rotate-180` >= 2.
    3. Capture AR screenshots (Playwright throwaway capture spec using loginForListPages(page, 'ar'), or browser-harness with the dedicated headless Chrome): (a) /calendar month view showing the nav chevrons, (b) a paginated list page (dossiers list) showing prev/next pagination controls, (c) any protected page showing the sidebar expanded AND collapsed (toggle it). Save PNGs under .planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/evidence/ with names srtl02-calendar-ar.png, srtl02-pagination-ar.png, srtl02-sidebar-expanded-ar.png, srtl02-sidebar-collapsed-ar.png.
    4. Write 76-SRTL02-VERIFICATION.md: a table per component with columns (check, expected behavior from the interfaces table, evidence link, automated result, human sign-off: pending). Include the grep outputs and the calendar-rtl run result verbatim.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec playwright test tests/e2e/calendar-rtl.spec.ts && cd .. && test -f .planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/76-SRTL02-VERIFICATION.md && ls .planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/evidence/*.png | wc -l | awk '{exit ($1>=4)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && pnpm exec playwright test tests/e2e/calendar-rtl.spec.ts` exits 0
    - 76-SRTL02-VERIFICATION.md exists with one checklist table per component (calendar, pagination, sidebar) and links to >= 4 PNGs under evidence/
    - The record includes the three grep results proving the hand-patches present post-migrate
    - Screenshots are captured in Arabic (html dir=rtl visible in layout — Arabic text/mirrored chevrons in the images)
  </acceptance_criteria>
  <done>All automatable SRTL-02 evidence is captured and organized for human review; nothing left for the human except looking.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual sign-off on Calendar / Pagination / Sidebar RTL correctness</name>
  <files>.planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/76-SRTL02-VERIFICATION.md</files>
  <action>Present the Task 1 evidence pack to the user for SRTL-02's mandated manual verification; on approval, update every "human sign-off: pending" cell in 76-SRTL02-VERIFICATION.md to "approved (date)". No code changes.</action>
  <what-built>Automated SRTL-02 evidence pack: calendar-rtl e2e green, hand-patch greps green, and AR screenshots of all three CLI-exempt components under .planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/evidence/ (record: 76-SRTL02-VERIFICATION.md)</what-built>
  <how-to-verify>
    Review the screenshots in the evidence/ directory (or live at http://localhost:5173 after `pnpm dev`, switching to Arabic via the topbar ع control):
    1. Calendar (/calendar): month-nav chevrons are mirrored; the "next" control advances to the LATER month and "previous" to the earlier one (not swapped).
    2. Pagination (dossiers list): prev/next chevrons are mirrored; clicking next moves forward through pages; the current-page highlight is unaffected.
    3. Sidebar: the rail sits on the correct (inline-start = right in AR) edge; collapsing tucks it to that same edge; the collapse-toggle icon is mirrored.
    Then update the human sign-off column in 76-SRTL02-VERIFICATION.md from "pending" to approved (the executor will do the edit on your confirmation).
  </how-to-verify>
  <resume-signal>Type "approved" to confirm all three components are RTL-correct in Arabic, or describe which component misbehaves and how</resume-signal>
  <verify>
    <human-check>User has visually confirmed Calendar, Pagination, and Sidebar RTL correctness in Arabic per the how-to-verify steps</human-check>
  </verify>
  <done>76-SRTL02-VERIFICATION.md sign-off column reads "approved" for all three components, backed by the user's resume-signal</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                                                                                                  |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| none new | Verification-only plan: runs existing tests, captures screenshots, writes a planning-record markdown file; no production code, config, or dependency changes |

## STRIDE Threat Register

| Threat ID | Category               | Component                             | Disposition | Mitigation Plan                                                                                                                        |
| --------- | ---------------------- | ------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| T-76-08   | Information Disclosure | AR screenshots committed to .planning | mitigate    | Capture against local dev with test/seed data only (loginForListPages test credentials); no production data appears in evidence images |
| T-76-SC   | Tampering              | npm/pip/cargo installs                | accept      | No packages installed by this plan                                                                                                     |

</threat_model>

<verification>
- calendar-rtl.spec.ts green post-migrate
- 76-SRTL02-VERIFICATION.md complete with >= 4 evidence PNGs
- Human approval recorded (checkpoint resume-signal)
</verification>

<success_criteria>

- SRTL-02 satisfied: all three CLI-exempt components manually verified RTL-correct in Arabic, with durable evidence and explicit human sign-off
  </success_criteria>

<output>
Create `.planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/76-05-SUMMARY.md` when done.
</output>
