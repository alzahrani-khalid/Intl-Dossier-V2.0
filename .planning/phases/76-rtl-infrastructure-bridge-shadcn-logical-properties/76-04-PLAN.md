---
phase: 76-rtl-infrastructure-bridge-shadcn-logical-properties
plan: 04
type: execute
wave: 3
depends_on: [76-01, 76-02, 76-03]
files_modified:
  - frontend/src/components/ui/** (migrate rtl output — reviewed hunks only)
  - frontend/components.json
autonomous: true
requirements: [SRTL-01]

must_haves:
  truths:
    - 'pnpm dlx shadcn@latest migrate rtl has been run exactly once against components/ui/**, and its reviewed output (or a recorded empty-diff proof) exists as a single isolated commit — nothing else in that commit'
    - 'The repo carries no duplicate rtl:* utility token after the run (check-duplicate-rtl passes)'
    - 'ui/sheet.tsx paired ltr:/rtl: slide variants, ui/navigation-menu.tsx physical Radix-motion slides, and ui/alert-dialog.tsx centering idiom are NOT converted to logical slide utilities (known-broken tw-animate-css path rejected)'
    - 'components.json has rtl: true so future shadcn add installs transform at install time instead of ever re-running the bulk migrate'
  artifacts:
    - path: 'frontend/components.json'
      provides: 'Install-time RTL transform flag; @aceternity-pro registry untouched'
      contains: '"rtl": true'
  key_links:
    - from: 'frontend/src/components/ui'
      to: 'scripts/check-duplicate-rtl.mjs'
      via: 'post-migrate duplicate scan (SRTL-01 recurrence guard from Plan 02)'
      pattern: 'check-duplicate-rtl'
---

<objective>
Execute the one-shot `migrate rtl` protocol (SRTL-01): clean tree → CLI-arg verification → scoped run → hunk-by-hunk human-standard review with three named file checkpoints → single isolated commit → `"rtl": true` so the bulk transform never needs to run again.

Purpose: the upstream transform is not idempotent (shadcn-ui/ui #9891 — re-runs append duplicate `rtl:*` variants), and on this repo's legacy `new-york` style the output is best-effort, not trusted. The commit-isolation IS the audit/revert mechanism.
Output: one reviewed migrate commit on components/ui/\*\* (or recorded empty-diff evidence), components.json rtl flag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/76-RESEARCH.md

<interfaces>
<!-- Verified against the working tree 2026-07-02. -->

Known physical-class sites the transform will target (RESEARCH Pattern 4 expected-yield inventory — the review checkpoints):

1. `ui/alert-dialog.tsx` (~line 37): `left-[50%] … translate-x-[-50%] … slide-in-from-left-1/2` — a CENTERING idiom, direction-neutral. `left-[50%]→start-[50%]` is safe ONLY if the physical `translate-x-[-50%]` stays physical and the pair stays internally consistent (Tailwind v4 `translate` is a standalone property — established project fact). If in doubt, reject the hunk.
2. `ui/navigation-menu.tsx` (~line 67): `data-[motion=from-start]:slide-in-from-left-52` etc. — Radix's data-motion values are ALREADY logical (from-start/to-end); the physical slide classes are the intentional mapping. Converting them to logical slide utilities hits the tw-animate-css RTL bug. Likely reject.
3. `ui/sheet.tsx` (lines 42-44): paired variants already correct — e.g. `ltr:data-[state=open]:slide-in-from-right rtl:data-[state=open]:slide-in-from-left`. ANY change here is a regression. Reject.

Universal rejection rule: any hunk converting paired `ltr:`/`rtl:` slide variants into bare logical slide utilities (`slide-in-from-start`/`-end`) — tw-animate-css logical slides do not respect direction (upstream bug flagged in shadcn's own RTL docs).

Hand-patches that must survive verbatim (SRTL-02 depends on them; grep-assert post-run):

- `ui/calendar.tsx` ~27-28: `rtl:**:[.rdp-button\_next>svg]:rotate-180` (+ `_previous` twin)
- `ui/pagination.tsx` lines 58, 71: `rtl:rotate-180` on ChevronLeft/Right
- `ui/sidebar.tsx` line 329: `rtl:-scale-x-100` on the PanelLeft toggle icon

components.json today: style "new-york", registries include `"@aceternity-pro"` (Phase 79's job — DO NOT touch). Add top-level `"rtl": true`. Caveat A2: install-time auto-transform is only guaranteed on \*-nova styles; on new-york treat future `shadcn add` output as best-effort-review too.

CLI facts: shadcn@4.12.0 (npm-verified); glob-arg syntax `pnpm dlx shadcn@latest migrate rtl "src/components/ui/**"` is ASSUMED (A1, from milestone STACK.md) — verify with `--help` before running. `npx` is broken on this machine — always `pnpm dlx`. Pre-commit hook runs `pnpm build` + lint-staged (commit may be slow; lint-staged can leave MM artifacts).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run migrate rtl once, review every hunk, land the single isolated commit</name>
  <files>frontend/src/components/ui/** (reviewed migrate output only)</files>
  <read_first>
    - .planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/76-RESEARCH.md (Pattern 4 protocol — follow it step-by-step; Pitfall 2 + Pitfall 3)
    - frontend/src/components/ui/sheet.tsx (lines 35-50 — memorize the paired-variant baseline before the run)
    - frontend/src/components/ui/alert-dialog.tsx and frontend/src/components/ui/navigation-menu.tsx (the other two review checkpoints, current state)
    - frontend/src/components/ui/calendar.tsx, frontend/src/components/ui/pagination.tsx, frontend/src/components/ui/sidebar.tsx (the hand-patches that must survive)
  </read_first>
  <action>
    Protocol (RESEARCH Pattern 4, steps 1-4):
    1. Precondition: `git status --porcelain frontend/src` MUST be empty (waves 1-2 committed). Abort if not.
    2. Verify CLI syntax first: `cd frontend && pnpm dlx shadcn@latest migrate --help` (assumption A1 — check whether `migrate rtl` accepts a path/glob argument). If glob supported: `pnpm dlx shadcn@latest migrate rtl "src/components/ui/**"`. Fallback if not: run `pnpm dlx shadcn@latest migrate rtl` repo-scoped, then revert every change OUTSIDE frontend/src/components/ui/ with `git checkout -- <paths>` (safe here: the clean-tree precondition guarantees those files carry ONLY the unwanted codemod output; expected out-of-scope churn is zero since app code is already logical-only — verify with `git status --porcelain`).
    3. Review EVERY hunk of `git diff frontend/src/components/ui` against the three named checkpoints and the universal rejection rule in the interfaces block. Revert rejected hunks (`git checkout -- <file>` for wholly-rejected files, or surgical edit for mixed files). Expected total yield is SMALL — a handful of files.
    4. Post-run gates before committing: `node scripts/check-duplicate-rtl.mjs frontend/src` (exit 0), hand-patch greps (see acceptance criteria), `cd frontend && pnpm type-check`, `pnpm exec vitest run src/components/ui/__tests__/direction.test.tsx`.
    5. Commit ONLY the ui tree, nothing else, with pathspec scoping: message `feat(76): shadcn migrate rtl one-shot (components/ui)` committing `frontend/src/components/ui` alone — the isolation makes the run auditable and revertable, and the commit is the never-re-run marker.
    Edge case: if the reviewed diff is EMPTY (tree already fully logical), do NOT create an empty commit — record the CLI invocation + `git status` output as evidence in the SUMMARY; SRTL-01's "applied exactly once" is then satisfied by the recorded run + Task 2's rtl flag + Plan 02's recurrence guard.
    NEVER run migrate rtl a second time, under any circumstance, including "to fix" a bad hunk — fix by hand instead (idempotency bug #9891).
  </action>
  <verify>
    <automated>node scripts/check-duplicate-rtl.mjs frontend/src && cd frontend && pnpm type-check && pnpm exec vitest run src/components/ui/__tests__/direction.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one new commit touches frontend/src/components/ui and NOTHING else (`git show --stat HEAD` lists only frontend/src/components/ui/* paths) — OR the SUMMARY records the CLI run output proving an empty reviewed diff
    - `node scripts/check-duplicate-rtl.mjs frontend/src` exits 0 post-run
    - `grep -c "rtl:rotate-180" frontend/src/components/ui/pagination.tsx` returns 2 (hand-patch survived)
    - `grep -c "rtl:-scale-x-100" frontend/src/components/ui/sidebar.tsx` returns >= 1 (hand-patch survived)
    - `grep -c "rotate-180" frontend/src/components/ui/calendar.tsx` returns >= 2 (rdp next+previous chevron patches survived)
    - ui/sheet.tsx still contains `ltr:data-[state=open]:slide-in-from-right` and `rtl:data-[state=open]:slide-in-from-left` (paired variants unchanged)
    - `grep -rn "slide-in-from-start\|slide-out-to-start\|slide-in-from-end\|slide-out-to-end" frontend/src/components/ui` returns 0 hits (no known-broken logical slide utilities introduced)
    - `cd frontend && pnpm type-check` exits 0
  </acceptance_criteria>
  <done>The one-shot transform is applied (or proven empty), reviewed against all named checkpoints, committed in isolation, and duplicate-free.</done>
</task>

<task type="auto">
  <name>Task 2: Set rtl: true in components.json (separate commit; registry untouched)</name>
  <files>frontend/components.json</files>
  <read_first>
    - frontend/components.json (current keys: style new-york, tailwind block, aliases, registries with @aceternity-pro)
  </read_first>
  <action>
    Add top-level `"rtl": true` to frontend/components.json (sibling of "style"/"rsc"/"tsx"). Change NOTHING else — in particular the `"registries"` block with `"@aceternity-pro"` is Phase 79's surface. This makes future `pnpm dlx shadcn@latest add <component>` installs apply the RTL transform at install time, removing any future motive to re-run the bulk migrate (A2 caveat: on new-york style, treat install-time output as best-effort-review too — note this in the SUMMARY for future phases). Commit as its own docs-adjacent change, NOT squashed into Task 1's migrate commit (the migrate commit must stay pure).
  </action>
  <verify>
    <automated>node -e "const c=require('./frontend/components.json'); if(c.rtl!==true) process.exit(1); if(!c.registries['@aceternity-pro']) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/components.json parses as JSON and has top-level `"rtl": true`
    - `git diff HEAD~1 -- frontend/components.json` shows ONLY the rtl key addition (registries + style + tailwind blocks byte-identical)
    - The components.json change is NOT in the same commit as the migrate diff
  </acceptance_criteria>
  <done>Future component installs are transform-covered; the bulk migrate never needs a second run.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                                             |
| -------------------------- | ------------------------------------------------------------------------------------------------------- |
| npm registry → dev machine | `shadcn` CLI fetched and executed at dev time via pnpm dlx (never a runtime dep)                        |
| codemod → working tree     | the transform rewrites source files; scope and content are attacker-irrelevant but correctness-critical |

## STRIDE Threat Register

| Threat ID | Category  | Component                                      | Disposition | Mitigation Plan                                                                                                                                                  |
| --------- | --------- | ---------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-76-06   | Tampering | shadcn CLI (supply chain, dev-time)            | mitigate    | slopcheck [OK] + no-postinstall verified in RESEARCH audit; official shadcn-ui/ui repo; executed via pnpm dlx pinned invocation, never installed as a dependency |
| T-76-07   | Tampering | codemod rewriting files outside intended scope | mitigate    | Clean-tree precondition + scoped run (or scoped revert fallback) + full hunk review + pathspec-isolated commit; `git show --stat` assertion proves scope         |
| T-76-SC   | Tampering | npm/pip/cargo installs                         | mitigate    | Both packages in this phase are [OK] in RESEARCH Package Legitimacy Audit; no [ASSUMED]/[SUS] installs, so no blocking checkpoint required                       |

</threat_model>

<verification>
- Isolated migrate commit exists (or empty-diff evidence recorded)
- `node scripts/check-duplicate-rtl.mjs frontend/src` → 0
- Hand-patch + paired-variant greps green (Task 1 acceptance criteria)
- `cd frontend && pnpm run lint && pnpm type-check` → green
</verification>

<success_criteria>

- SRTL-01 satisfied: transform run exactly once, reviewed not trusted, committed as a single auditable diff, guarded against recurrence (Plan 02 check + rtl: true + the never-re-run rule)
  </success_criteria>

<output>
Create `.planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/76-04-SUMMARY.md` when done.
</output>
