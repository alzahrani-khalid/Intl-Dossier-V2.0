---
phase: 76-rtl-infrastructure-bridge-shadcn-logical-properties
plan: 02
type: execute
wave: 2
depends_on: [76-01]
files_modified:
  - scripts/check-duplicate-rtl.mjs
  - tools/rtl-fixtures/duplicate-rtl-bad.tsx
  - frontend/package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [SRTL-03]

must_haves:
  truths:
    - 'A build fails when any single className string literal contains the SAME rtl:* utility token twice (the shadcn #9891 re-run signature)'
    - "Two DIFFERENT rtl: tokens in one string remain legal — the check passes on the live tree including ui/sheet.tsx's paired variants"
    - 'The guard is proven live in CI by a positive-failure fixture step (guard theater impossible)'
  artifacts:
    - path: 'scripts/check-duplicate-rtl.mjs'
      provides: 'Exact-duplicate rtl: token detector over string literals'
      contains: 'rtl:'
    - path: 'tools/rtl-fixtures/duplicate-rtl-bad.tsx'
      provides: 'Positive-failure fixture the check MUST reject'
      contains: 'rtl:space-x-reverse rtl:space-x-reverse'
  key_links:
    - from: 'frontend/package.json'
      to: 'scripts/check-duplicate-rtl.mjs'
      via: 'lint script chain (mirrors check-i18n-namespaces.mjs wiring)'
      pattern: 'check-duplicate-rtl'
    - from: '.github/workflows/ci.yml'
      to: 'tools/rtl-fixtures'
      via: 'positive-failure step in the lint job'
      pattern: 'check-duplicate-rtl'
---

<objective>
Land the duplicate-`rtl:` CI guard (SRTL-03): a node script that fails the build when any className string contains an exact-duplicate `rtl:*` utility token, wired into the frontend lint chain and proven live in CI with a positive-failure fixture. This is also the recurrence guard for SRTL-01 (a second `migrate rtl` run would trip it).

Purpose: shadcn's `migrate rtl` transform is not idempotent (upstream #9891) — re-runs append duplicate `rtl:*` variants that make the cascade order-dependent. The guard makes recurrence build-breaking BEFORE Plan 04 runs the transform.
Output: scripts/check-duplicate-rtl.mjs, tools/rtl-fixtures/duplicate-rtl-bad.tsx, lint-chain + ci.yml wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/76-RESEARCH.md

<interfaces>
<!-- Verified against the working tree 2026-07-02. -->

Current frontend lint chain (frontend/package.json line 17 — note the `cd ..`: script paths are repo-root-relative):

```json
"lint": "cd .. && eslint -c eslint.config.mjs --max-warnings 0 'frontend/src/**/*.{ts,tsx}' && node scripts/check-i18n-namespaces.mjs"
```

CI positive-failure precedent (.github/workflows/ci.yml, lint job — mirror this shape):

```yaml
- name: Assert edge-fn schema check fails on bad fixture (positive-failure)
  shell: bash
  run: |
    set -e
    ! node scripts/check-edge-fn-schema-refs.mjs tools/edge-fn-fixtures/bad-schema-ref
```

Existing fixture dirs prove tools/ is outside every lint/tsc/build glob: tools/anythingllm-fixtures, tools/edge-fn-fixtures, tools/eslint-fixtures.

Detection core (RESEARCH Pattern 6 — string-literal tokenizer, NOT a line regex):

```js
const STRING_RE = /(["'`])((?:\\.|(?!\1)[^\\])*)\1/g
// per literal: split on /\s+/, keep tokens starting with 'rtl:',
// flag only a token seen TWICE within the SAME literal
```

CRITICAL false-positive trap: `frontend/src/components/ui/sheet.tsx` side variants legally carry multiple DIFFERENT `rtl:` tokens per string (e.g. `rtl:data-[state=closed]:slide-out-to-right rtl:data-[state=open]:slide-in-from-right`). A naive `rtl:.*rtl:` grep fails the build on day one. Only EXACT duplicate tokens are violations.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write check-duplicate-rtl.mjs and the positive-failure fixture</name>
  <files>scripts/check-duplicate-rtl.mjs, tools/rtl-fixtures/duplicate-rtl-bad.tsx</files>
  <read_first>
    - scripts/check-i18n-namespaces.mjs (repo conventions for check scripts: arg handling, output format, exit codes)
    - frontend/src/components/ui/sheet.tsx (lines 35-50 — the legitimate multi-rtl:-token strings the check MUST pass)
    - .planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/76-RESEARCH.md (Pattern 6 + the "Duplicate-rtl check core" code example)
  </read_first>
  <behavior>
    - `node scripts/check-duplicate-rtl.mjs tools/rtl-fixtures` exits NON-ZERO, printing the offending file and the duplicated token
    - `node scripts/check-duplicate-rtl.mjs frontend/src` exits 0 (16 rtl: occurrences repo-wide, all distinct per string — RESEARCH verified)
    - `node scripts/check-duplicate-rtl.mjs frontend/src/components/ui` exits 0 (sheet.tsx false-positive regression)
  </behavior>
  <action>
    Create tools/rtl-fixtures/duplicate-rtl-bad.tsx first (the failing test): a minimal non-imported TSX snippet whose className contains the #9891 signature `rtl:space-x-reverse rtl:space-x-reverse`. tools/ is outside all lint/tsc/build globs (see interfaces — three sibling fixture dirs prove the precedent); confirm with `cd frontend && pnpm run lint` still passing after the fixture lands.

    Then write scripts/check-duplicate-rtl.mjs (plain node, zero dependencies, ESM):
    - argv: `node scripts/check-duplicate-rtl.mjs <rootDir>`; no default-arg fallback needed but if omitted use `frontend/src`
    - walk rootDir recursively; include files with extensions .ts .tsx .js .jsx .mjs; skip directories named node_modules, dist, .git, coverage
    - per file: extract every string literal with the STRING_RE from the interfaces block (covers single/double/backtick quotes); per literal, split on whitespace, collect tokens with the `rtl:` prefix, and record a violation ONLY when the identical token appears 2+ times within that one literal
    - report each violation as `<file>: duplicated rtl token "<token>"`; exit 1 if any violations, else print a one-line summary (files scanned count) and exit 0
    - no eval, no child_process, no dynamic require — the script only reads files under the given root (T-76-03 mitigation)
    Follow check-i18n-namespaces.mjs conventions for logging style.

  </action>
  <verify>
    <automated>node scripts/check-duplicate-rtl.mjs frontend/src && node scripts/check-duplicate-rtl.mjs frontend/src/components/ui && ! node scripts/check-duplicate-rtl.mjs tools/rtl-fixtures</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-duplicate-rtl.mjs tools/rtl-fixtures; echo $?` prints a violation line containing "duplicate" and "rtl:space-x-reverse", then 1
    - `node scripts/check-duplicate-rtl.mjs frontend/src; echo $?` ends with 0
    - `node scripts/check-duplicate-rtl.mjs frontend/src/components/ui; echo $?` ends with 0 (sheet.tsx multi-distinct-rtl strings pass)
    - `grep -c "child_process\|eval(" scripts/check-duplicate-rtl.mjs` returns 0
    - `cd frontend && pnpm run lint` still exits 0 (fixture not picked up by ESLint)
  </acceptance_criteria>
  <done>Detector rejects the bad fixture, passes the live tree including sheet.tsx, and has no execution surface beyond file reads.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the guard into the frontend lint chain and the CI lint job</name>
  <files>frontend/package.json, .github/workflows/ci.yml</files>
  <read_first>
    - frontend/package.json (line 17 — current lint chain; the script runs after `cd ..` so paths are repo-root-relative)
    - .github/workflows/ci.yml (lint job, lines ~45-75 — "Run linting", "Check edge-fn schema references", and the positive-failure step to mirror)
  </read_first>
  <action>
    1. frontend/package.json: append `&& node scripts/check-duplicate-rtl.mjs frontend/src` to the existing "lint" script (after the check-i18n-namespaces.mjs call, same chain).
    2. .github/workflows/ci.yml, lint job, after the existing "Assert edge-fn schema check fails on bad fixture (positive-failure)" step, add two steps:
       - name: `Check duplicate rtl utilities` — run: `node scripts/check-duplicate-rtl.mjs frontend/src`
       - name: `Assert duplicate-rtl check fails on bad fixture (positive-failure)` — shell: bash, run block with `set -e` then `! node scripts/check-duplicate-rtl.mjs tools/rtl-fixtures` (byte-mirror the edge-fn precedent's shape).
    Change nothing else in ci.yml — the lint job's `pnpm run lint` already picks up the chain addition via turbo.
  </action>
  <verify>
    <automated>cd frontend && pnpm run lint && cd .. && grep -c "check-duplicate-rtl" .github/workflows/ci.yml | grep -qx "2"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json "lint" script contains `node scripts/check-duplicate-rtl.mjs frontend/src`
    - .github/workflows/ci.yml contains exactly 2 occurrences of `check-duplicate-rtl` (live-tree step + positive-failure step), both inside the lint job
    - `cd frontend && pnpm run lint` exits 0 end-to-end (eslint + i18n check + duplicate-rtl check)
    - `bash -c 'set -e; ! node scripts/check-duplicate-rtl.mjs tools/rtl-fixtures'` exits 0 (the CI step semantics work locally)
  </acceptance_criteria>
  <done>SRTL-03 live: every lint run (local + CI) fails on duplicated rtl: tokens; the fixture step proves the guard fires.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                                           |
| ---------------------- | ----------------------------------------------------------------------------------------------------- |
| repo files → CI script | the check script reads repo file contents during CI; file contents are attacker-influenceable via PRs |

## STRIDE Threat Register

| Threat ID | Category               | Component                       | Disposition | Mitigation Plan                                                                                                                                                                                                      |
| --------- | ---------------------- | ------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-76-03   | Elevation of Privilege | scripts/check-duplicate-rtl.mjs | mitigate    | Script performs read-only file walks; no eval/child_process/dynamic import of scanned content (asserted by grep in acceptance criteria); root path comes from package.json/ci.yml literals, never from scanned input |
| T-76-04   | Repudiation            | CI guard (guard theater)        | mitigate    | Positive-failure fixture step in ci.yml proves the check fires on a known-bad input on every CI run                                                                                                                  |
| T-76-SC   | Tampering              | npm/pip/cargo installs          | accept      | No packages installed by this plan (zero-dependency node script)                                                                                                                                                     |

</threat_model>

<verification>
- `node scripts/check-duplicate-rtl.mjs frontend/src` → exit 0
- `! node scripts/check-duplicate-rtl.mjs tools/rtl-fixtures` → true (fixture rejected)
- `cd frontend && pnpm run lint` → exit 0 with the new chain link
- ci.yml lint job has both new steps
</verification>

<success_criteria>

- SRTL-03 satisfied: a duplicated `rtl:*` utility anywhere in frontend/src fails lint locally and in CI
- Zero false positives on the live tree (sheet.tsx paired variants pass)
  </success_criteria>

<output>
Create `.planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/76-02-SUMMARY.md` when done.
</output>
