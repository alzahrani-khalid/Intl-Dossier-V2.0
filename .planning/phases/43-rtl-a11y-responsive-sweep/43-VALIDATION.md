---
phase: 43
slug: rtl-a11y-responsive-sweep
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-03
---

# Phase 43 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: `43-RESEARCH.md` §12 (Validation Architecture).

---

## Test Infrastructure

| Property               | Value                                                                                                                                                         |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Framework**          | Playwright `^1.58.2` (root devDep) + `@axe-core/playwright@^4.11.1` (frontend devDep); ESLint with `eslint-plugin-rtl-friendly` (already at `error` globally) |
| **Config file**        | `frontend/playwright.config.ts` (sweep specs live in `frontend/tests/e2e/`)                                                                                   |
| **Quick run command**  | `pnpm -C frontend lint` (5 sec)                                                                                                                               |
| **Full sweep command** | `pnpm -C frontend test:qa-sweep` (Wave 0 adds this script; runs the 4 blocking sweeps)                                                                        |
| **Estimated runtime**  | Lint ≈5 sec · single sweep ≈3 min · full sweep ≈11 min (2-shard split target <10 min)                                                                         |

---

## Sampling Rate

- **After every task commit:** Run `pnpm -C frontend lint`
- **After every plan wave:** Run `pnpm -C frontend test:qa-sweep`
- **Before `/gsd-verify-work`:** Full sweep + 8 focus-outline baselines reviewed + 5 `rotate-180` fixes confirmed via existing Phase 38–42 visual specs
- **Max feedback latency:** 10 minutes (CI gate)

---

## Per-Task Verification Map

> Detailed per-task mapping is generated by the planner from PLAN.md frontmatter. The matrix below is the per-requirement coverage map (RESEARCH.md §12 verbatim).

| Req ID | Behavior                                                                                                            | Test Type                   | Automated Command                                                      | File Exists                                    |
| ------ | ------------------------------------------------------------------------------------------------------------------- | --------------------------- | ---------------------------------------------------------------------- | ---------------------------------------------- |
| QA-01  | Zero `rtl-friendly/no-physical-properties` violations across v6.0 globs                                             | lint                        | `pnpm -C frontend lint`                                                | ✅ Phase 40 block exists; Wave 0 expands globs |
| QA-01  | Zero `no-restricted-syntax` violations on physical class literals                                                   | lint                        | `pnpm -C frontend lint`                                                | ✅ Phase 40 block exists; Wave 0 expands globs |
| QA-02  | Zero serious/critical axe violations × 15 routes × {EN, AR}                                                         | E2E (axe)                   | `pnpm -C frontend exec playwright test qa-sweep-axe.spec.ts`           | ❌ W0 — new spec                               |
| QA-02  | Tab-walk reaches all visible interactives × 15 routes                                                               | E2E (Playwright)            | `pnpm -C frontend exec playwright test qa-sweep-keyboard.spec.ts`      | ❌ W0 — new spec                               |
| QA-02  | Focus outline visible × 4 directions × 2 modes on Settings                                                          | E2E visual + programmatic   | `pnpm -C frontend exec playwright test qa-sweep-focus-outline.spec.ts` | ❌ W0 — new spec                               |
| QA-03  | No horizontal scroll, ≥44×44 touch targets, key landmarks visible at {320,640,768,1024,1536} × 15 routes × {EN, AR} | E2E (Playwright)            | `pnpm -C frontend exec playwright test qa-sweep-responsive.spec.ts`    | ❌ W0 — new spec                               |
| QA-04  | All directional icons documented + LTR/RTL screenshots                                                              | docs + opt-in E2E           | `pnpm -C frontend docs:rtl-icons`                                      | ❌ W0 — new spec + new doc                     |
| QA-04  | Icons flip via `scaleX(-1)` (NOT `rotate-180`) — 5 fix-in-scope items                                               | code edit + visual baseline | (covered by existing Phase 38–42 visual specs once fixed)              | ✅ visual specs exist; fixes needed            |

_Status legend: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky_

---

## Wave 0 Requirements

- [ ] `frontend/tests/e2e/helpers/v6-routes.ts` — V6_ROUTES registry (D-02)
- [ ] `frontend/tests/e2e/helpers/qa-sweep.ts` — shared `runAxe`, `forEachBreakpoint`, `assertFocusOutlineVisible`, `tabWalkAllInteractives`, `settlePage`
- [ ] `frontend/tests/e2e/helpers/contrast.ts` — OKLCH→luminance helper (or `culori` import)
- [ ] `frontend/eslint.config.js` — extend Phase 40 `no-restricted-syntax` glob to v6.0 surface
- [ ] `frontend/package.json` — add `test:qa-sweep` and `docs:rtl-icons` scripts
- [ ] `.github/workflows/e2e.yml` — add `qa-sweep` GA job invoking `pnpm -C frontend test:qa-sweep`
- [ ] `frontend/package.json` — add `culori` devDep (or commit inline OKLCH math helper)

---

## Manual-Only Verifications

| Behavior                                      | Requirement | Why Manual                                                           | Test Instructions                                                                                                                                                                |
| --------------------------------------------- | ----------- | -------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 8 focus-outline visual baselines reviewed     | QA-02       | First-time baselines require human approval before becoming the gate | After Wave 1, run `pnpm -C frontend exec playwright test qa-sweep-focus-outline --update-snapshots`, review the 8 PNGs under `qa-sweep-focus-outline.spec.ts-snapshots/`, commit |
| Directional-icon LTR/RTL screenshots reviewed | QA-04       | Generated screenshots are advisory documentation, not a CI gate      | After Wave 1, run `pnpm -C frontend docs:rtl-icons`, review PNG diffs under `docs/rtl-icons/`, commit                                                                            |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (helpers, scripts, ESLint glob, CI job)
- [ ] No watch-mode flags
- [ ] Feedback latency <10 minutes (CI sweep budget)
- [ ] `nyquist_compliant: true` set in frontmatter once Wave 0 lands

**Approval:** pending
