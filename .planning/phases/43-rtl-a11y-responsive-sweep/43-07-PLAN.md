---
phase: 43-rtl-a11y-responsive-sweep
plan: 07
type: execute
wave: 2
depends_on: ['43-00', '43-01', '43-02', '43-03', '43-04', '43-05', '43-06']
files_modified:
  - frontend/src/pages/Dashboard/widgets/VipVisits.tsx
  - frontend/src/pages/Dashboard/widgets/OverdueCommitments.tsx
  - frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx
  - frontend/src/pages/persons/PersonsListPage.tsx
  - frontend/src/components/calendar/UnifiedCalendar.tsx
  - frontend/tests/e2e/responsive-breakpoints.spec.ts
  - frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/
  - docs/rtl-icons/
autonomous: false
requirements: [QA-01, QA-02, QA-03, QA-04]
tags: [qa, gate, remediation, rotate-180, baselines]
user_setup: []

must_haves:
  truths:
    - '5 rotate-180 violations replaced with .icon-flip'
    - 'OverdueCommitments arrow-up-right now flips in RTL (was never flipping)'
    - 'pnpm -C frontend lint exits 0 across the full v6.0 surface'
    - 'pnpm -C frontend test:qa-sweep exits 0 (4 sweeps green)'
    - '8 focus-outline baselines committed and human-approved'
    - 'docs/rtl-icons/ PNG fixtures committed (regenerated by pnpm docs:rtl-icons)'
    - 'frontend/tests/e2e/responsive-breakpoints.spec.ts deleted (superseded)'
  artifacts:
    - path: 'frontend/src/pages/Dashboard/widgets/VipVisits.tsx'
      provides: 'rotate-180 → .icon-flip migration'
      contains: '.icon-flip'
    - path: 'frontend/src/pages/Dashboard/widgets/OverdueCommitments.tsx'
      provides: 'RTL flip added (was missing)'
      contains: '.icon-flip'
    - path: 'frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx'
      provides: 'rotate-180 → .icon-flip migration'
      contains: '.icon-flip'
    - path: 'frontend/src/pages/persons/PersonsListPage.tsx'
      provides: 'rotate-180 → .icon-flip migration'
      contains: '.icon-flip'
    - path: 'frontend/src/components/calendar/UnifiedCalendar.tsx'
      provides: 'rotate-180 → .icon-flip migration'
      contains: '.icon-flip'
  key_links:
    - from: 'qa-sweep CI gate'
      to: 'all 4 blocking sweeps'
      via: 'pnpm -C frontend test:qa-sweep exits 0'
      pattern: 'test:qa-sweep'
    - from: 'docs/rtl-icons.md image refs'
      to: 'docs/rtl-icons/*.png'
      via: 'committed PNG fixtures'
      pattern: "docs/rtl-icons/.*\\.png"
---

<objective>
Wave 2 gate + remediation. Land the five `rotate-180` → `.icon-flip` migrations identified in RESEARCH §6.C, run the full sweep + lint, fix every survivor in-phase (per D-10), generate + commit the 8 focus-outline baselines and the docs/rtl-icons/ PNG fixtures, and delete the obsolete `responsive-breakpoints.spec.ts`.

Purpose: Phase 43 IS the gate (per D-10). Without this wave, the sweeps in Waves 0-1 are scaffolding without enforcement.

Output:

- 5 component edits (4 chevron migrations + 1 add-where-missing)
- 8 committed focus-outline baselines (human-approved)
- 14 committed icon screenshot pairs (advisory, generated)
- `responsive-breakpoints.spec.ts` deleted
- `pnpm lint` and `pnpm test:qa-sweep` both exit 0
- 43-VERIFICATION.md authored with verdict and gap-or-pass disposition
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-rtl-a11y-responsive-sweep/43-CONTEXT.md
@.planning/phases/43-rtl-a11y-responsive-sweep/43-RESEARCH.md
@.planning/phases/43-rtl-a11y-responsive-sweep/43-VALIDATION.md
@frontend/src/pages/Dashboard/widgets/VipVisits.tsx
@frontend/src/pages/Dashboard/widgets/OverdueCommitments.tsx
@frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx
@frontend/src/pages/persons/PersonsListPage.tsx
@frontend/src/components/calendar/UnifiedCalendar.tsx
@frontend/tests/e2e/responsive-breakpoints.spec.ts

<interfaces>
.icon-flip CSS rule (frontend/src/styles/list-pages.css:861-863):
```css
html[dir='rtl'] .icon-flip { transform: scaleX(-1); }
```
Imported by main.tsx — globally available. To use, add `className="icon-flip"` to any directional icon. No JS conditional required (the CSS gates on `html[dir='rtl']`).

For lucide icons inside conditional render (when isRTL is needed in JS for other reasons), DO NOT use `className={isRTL ? '.icon-flip' : ''}` — the CSS gate already handles it. Use the bare `className="icon-flip"`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate 5 rotate-180 sites to .icon-flip (RESEARCH §6.C)</name>
  <files>
    frontend/src/pages/Dashboard/widgets/VipVisits.tsx,
    frontend/src/pages/Dashboard/widgets/OverdueCommitments.tsx,
    frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx,
    frontend/src/pages/persons/PersonsListPage.tsx,
    frontend/src/components/calendar/UnifiedCalendar.tsx
  </files>
  <read_first>
    - 43-RESEARCH.md §6.C (exact file:line references for each of the 5 sites)
    - frontend/src/styles/list-pages.css line 861-863 (verify .icon-flip rule is the canonical CSS)
    - Each of the 5 source files at the cited line numbers BEFORE editing
  </read_first>
  <action>
For each of the 5 files, perform the exact edit specified by RESEARCH §6.C. Use the Edit tool, NOT sed.

**1a. `frontend/src/pages/Dashboard/widgets/VipVisits.tsx` near line 123:**
Locate the `<ArrowRight>` (or equivalent lucide element) with `className={\`size-3 ${isRTL ? 'rotate-180' : ''}\`}`. Replace with:

```tsx
className = 'size-3 icon-flip'
```

Remove the now-unused `isRTL` reference at this site (keep the import / hook usage if other code still needs it).

**1b. `frontend/src/pages/Dashboard/widgets/OverdueCommitments.tsx` near line 172:**
Locate `<ArrowUpRight className="size-4 text-ink-soft"/>`. This icon CURRENTLY DOES NOT FLIP IN RTL — that is the bug RESEARCH §6.C calls out as "the most urgent." Add `.icon-flip`:

```tsx
<ArrowUpRight className="size-4 text-ink-soft icon-flip" />
```

No isRTL conditional needed — CSS handles the gating.

**1c. `frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx` near lines 68-70:**
Locate the chevron with `'rotate-180'` triggered by `isOpen && isRTL` (or similar combined state). The disclosure rotation (open vs closed) and the RTL mirror are TWO different transforms. Solution: the `isOpen` state stays `rotate-180` (or `rotate-90` for disclosure), but add `.icon-flip` for the RTL mirror — they compose correctly because Tailwind's rotate uses CSS rotate() and .icon-flip uses scaleX().

Concretely: keep the `isOpen ? 'rotate-180' : ''` (disclosure semantic), and ADD `icon-flip` as a static class. So:

```tsx
className={`chevron-icon icon-flip ${isOpen ? 'rotate-180' : ''}`}
```

The `rotate-180` here is for OPEN/CLOSED disclosure state, NOT RTL — document this in the inline comment so future readers don't try to delete it. RTL flip is handled by `.icon-flip`.

NOTE: This is the only site where `rotate-180` is RETAINED (for disclosure semantics), with `.icon-flip` added for RTL. Audit table in `docs/rtl-icons.md` already documents this nuance.

**1d. `frontend/src/pages/persons/PersonsListPage.tsx` near lines 387-388:**
Locate the chevron with `${isRTL ? 'rotate-180' : ''}`. Replace with:

```tsx
className = 'h-5 w-5 icon-flip'
```

(Preserve any other classes like `text-ink-soft` that may be present in the original — copy from the file.)

**1e. `frontend/src/components/calendar/UnifiedCalendar.tsx` near lines 202 and 213:**
Two sites: prev and next month nav. Both use `className={isRTL ? 'rotate-180' : ''}`. Replace both with:

```tsx
className = 'icon-flip'
```

Same RTL-CSS-gated pattern. Preserve any sizing classes.

After all 5 edits, run `pnpm -C frontend lint` to confirm no rule violations introduced. Confirm visually in `git diff` that the 5 edits are minimal (1-3 lines each).
</action>
<verify>
<automated>
{ ! grep -E "rotate-180" frontend/src/pages/Dashboard/widgets/VipVisits.tsx; } &&
{ ! grep -E "rotate-180" frontend/src/pages/Dashboard/widgets/OverdueCommitments.tsx; } &&
{ ! grep -E "rotate-180" frontend/src/pages/persons/PersonsListPage.tsx; } &&
{ ! grep -E "isRTL.*'rotate-180'" frontend/src/components/calendar/UnifiedCalendar.tsx; } &&
grep -q "icon-flip" frontend/src/pages/Dashboard/widgets/VipVisits.tsx &&
grep -q "icon-flip" frontend/src/pages/Dashboard/widgets/OverdueCommitments.tsx &&
grep -q "icon-flip" frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx &&
grep -q "icon-flip" frontend/src/pages/persons/PersonsListPage.tsx &&
grep -q "icon-flip" frontend/src/components/calendar/UnifiedCalendar.tsx &&
pnpm -C frontend lint 2>&1 | { ! grep -E "error.*VipVisits|error.*OverdueCommitments|error.*EngagementStageGroup|error.*PersonsListPage|error.*UnifiedCalendar" || true; }
</automated>
</verify>
<acceptance_criteria> - VipVisits.tsx: `rotate-180` removed, `icon-flip` present - OverdueCommitments.tsx: `icon-flip` added (was never flipping; bug fix) - EngagementStageGroup.tsx: `isRTL ? 'rotate-180'` removed; bare `rotate-180` retained ONLY for disclosure state with `.icon-flip` added for RTL; inline comment documents the distinction - PersonsListPage.tsx: `rotate-180` removed, `icon-flip` present - UnifiedCalendar.tsx: 2 sites — both `isRTL ? 'rotate-180'` removed, `icon-flip` present at both - `pnpm -C frontend lint` does not introduce new errors in any of the 5 files
</acceptance_criteria>
<done>
All 5 sites migrated; OverdueCommitments bug fix landed; lint clean on these files.
</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run pnpm lint, fix any v6.0-surface survivors</name>
  <files>(any v6.0-surface file flagged by lint)</files>
  <read_first>
    - 43-CONTEXT.md D-10 (Phase 43 IS the gate — fix survivors in-phase)
    - frontend/eslint.config.js (Wave 0 — confirm extended glob is in force)
  </read_first>
  <action>
Run `pnpm -C frontend lint` and capture output.

**If exit code is 0:** Done. Move to Task 3.

**If exit code is non-zero AND violations are `no-restricted-syntax` or `rtl-friendly/no-physical-properties` on v6.0 surface files:**

For EACH violation:

1. Open the cited file at the cited line.
2. Identify the physical class (e.g., `pl-4`, `mr-2`, `text-right`, `rounded-l-lg`).
3. Replace with the logical equivalent per CLAUDE.md table:
   - `pl-*` → `ps-*`, `pr-*` → `pe-*`
   - `ml-*` → `ms-*`, `mr-*` → `me-*`
   - `text-left` → `text-start`, `text-right` → `text-end`
   - `rounded-l-*` → `rounded-s-*`, `rounded-r-*` → `rounded-e-*`
   - `left-*` → `start-*`, `right-*` → `end-*`
4. Re-run `pnpm -C frontend lint`. Repeat until exit 0.

**If lint exits non-zero with errors that are NOT logical-properties violations** (e.g., type errors, unused vars):

- Those are pre-existing and NOT this plan's responsibility. Verify by checking `git stash && pnpm -C frontend lint && git stash pop` — if the same errors exist on the unstashed branch, document them as pre-existing in the SUMMARY and continue. If they appeared because of Wave 0/Wave 1 edits, fix them here.

DO NOT add `// eslint-disable-next-line` to silence violations. Phase 43 is the gate — fix the source.
</action>
<verify>
<automated>
pnpm -C frontend lint 2>&1 | tee /tmp/43-07-lint.log; LINT_EXIT=${PIPESTATUS[0]}; test "$LINT_EXIT" -eq 0
</automated>
</verify>
<acceptance_criteria> - `pnpm -C frontend lint` exits 0 - No `eslint-disable-next-line` comments added by this plan - If pre-existing non-logical-property violations exist, they are documented in 43-07-SUMMARY.md as pre-existing
</acceptance_criteria>
<done>
Lint exits 0 across the full v6.0 surface.
</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Delete responsive-breakpoints.spec.ts (superseded by qa-sweep-responsive)</name>
  <files>frontend/tests/e2e/responsive-breakpoints.spec.ts</files>
  <read_first>
    - frontend/tests/e2e/responsive-breakpoints.spec.ts (current contents — confirm it's the obsolete spec)
    - 43-RESEARCH.md §4 "responsive-breakpoints.spec.ts — recommendation: DELETE" (justification: hits /responsive-demo not v6.0 routes; uses obsolete authBypass; fully superseded by qa-sweep-responsive)
  </read_first>
  <action>
Delete the file:
```bash
rm frontend/tests/e2e/responsive-breakpoints.spec.ts
```

Confirm no other spec or doc imports from it:

```bash
grep -rn "responsive-breakpoints" frontend/ docs/ .github/ .planning/ 2>/dev/null | grep -v "43-rtl-a11y-responsive-sweep" | grep -v "43-RESEARCH" | grep -v "43-CONTEXT"
```

Expected: zero results outside Phase 43 doc references (which are intentionally documenting the deletion).

If grep returns any unexpected reference, investigate and remove cleanly.
</action>
<verify>
<automated>
{ ! test -f frontend/tests/e2e/responsive-breakpoints.spec.ts; } &&
HITS=$(grep -rln "responsive-breakpoints" frontend/ docs/ .github/ 2>/dev/null | wc -l) && [ "$HITS" -eq 0 ]
</automated>
</verify>
<acceptance_criteria> - File no longer exists - No remaining references in `frontend/`, `docs/`, or `.github/`
</acceptance_criteria>
<done>
Spec file deleted, no dangling references.
</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Generate + commit 14 docs/rtl-icons/ PNG fixtures</name>
  <files>docs/rtl-icons/</files>
  <read_first>
    - frontend/tests/e2e/qa-sweep-icon-screenshots.spec.ts (Plan 43-05 — iconFixtures list)
    - docs/rtl-icons.md (Plan 43-06 — image ref paths the PNGs must match)
  </read_first>
  <action>
Run `pnpm -C frontend docs:rtl-icons` to generate all 14 PNGs (7 icons × 2 directions). The script invokes `playwright test qa-sweep-icon-screenshots.spec.ts --update-snapshots --reporter=list`, but the spec uses `target.screenshot({ path })` (NOT `toHaveScreenshot`) so PNGs land at `docs/rtl-icons/*.png` directly — no snapshots directory.

```bash
pnpm -C frontend docs:rtl-icons
ls docs/rtl-icons/*.png | wc -l   # expect 14
```

If any of the 14 PNGs is missing, the corresponding fixture's selector failed. Inspect:

```bash
pnpm -C frontend docs:rtl-icons 2>&1 | grep -E "fail|error" | head -20
```

Common causes:

- Selector doesn't match (route layout differs from RESEARCH §6 assumption) → update the selector in `qa-sweep-icon-screenshots.spec.ts` and re-run
- Auth/data unavailable (e.g., /persons returns empty state) → seed data should be present in staging; if not, document as deferred

Commit the 14 PNGs along with any selector fixes.

This is a `checkpoint:human-verify` boundary: after generation, USER reviews the PNG diffs in `git status` to confirm icons render correctly in both directions.
</action>
<verify>
<automated>
test -d docs/rtl-icons &&
PNG_COUNT=$(ls docs/rtl-icons/*.png 2>/dev/null | wc -l | tr -d ' ') &&
[ "$PNG_COUNT" -ge 10 ] &&
test -f docs/rtl-icons/sparkline-polyline-ltr.png &&
test -f docs/rtl-icons/sparkline-polyline-rtl.png
</automated>
</verify>
<acceptance_criteria> - `docs/rtl-icons/` directory exists - At least 10 of the 14 expected PNGs are present (some fixtures may legitimately fail if their route lacks seed data — record in SUMMARY) - Sparkline pair (independent of all 5 rotate-180 fixes) MUST be present — it's the simplest fixture
</acceptance_criteria>
<done>
PNG fixtures committed; any missing fixtures documented with reason in SUMMARY.
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Generate + human-approve 8 focus-outline baselines</name>
  <what-built>
    Plan 43-04 created `qa-sweep-focus-outline.spec.ts` (8 tests). Task 5 runs it with `--update-snapshots`, which writes 8 PNGs to `frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/`. The user must review each baseline to confirm the focus ring is visible and consistent before they become the gate.
  </what-built>
  <how-to-verify>
    1. Run baseline generation:
       ```bash
       pnpm -C frontend exec playwright test qa-sweep-focus-outline.spec.ts --update-snapshots
       ```
    2. List the generated baselines:
       ```bash
       ls frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/
       ```
       Expected: 8 PNGs named `{direction}-{mode}-focused-primitive.png` (e.g., `chancery-light-focused-primitive.png`).
    3. Open each PNG and visually confirm:
       - A focus ring is visible on a focused element (button/input/link)
       - Ring color contrasts against background (>3:1 — assertFocusOutlineVisible probe also validates this programmatically)
       - No clipping, no rendering glitches
    4. If a baseline shows no visible focus ring, that combo has a real focus-token regression. File as a Phase 43 fix-in-scope, fix the token, re-run.
    5. Once all 8 baselines look correct, commit them.
  </how-to-verify>
  <resume-signal>Type "approved" when all 8 baselines are reviewed and committed, OR describe any issues for resolution.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Run full pnpm test:qa-sweep, fix every survivor in-phase</name>
  <files>(any v6.0-surface file flagged by sweep)</files>
  <read_first>
    - 43-CONTEXT.md D-10 (4 hard-blocking gates: lint, axe, responsive, keyboard — all must pass in-phase)
    - frontend/tests/e2e/qa-sweep-axe.spec.ts (Plan 43-01)
    - frontend/tests/e2e/qa-sweep-responsive.spec.ts (Plan 43-02)
    - frontend/tests/e2e/qa-sweep-keyboard.spec.ts (Plan 43-03)
    - frontend/tests/e2e/qa-sweep-focus-outline.spec.ts (Plan 43-04)
    - 43-RESEARCH.md §8 (touch-target edge cases — calendar nav HeroUI Button size, MyTasks checkbox)
  </read_first>
  <action>
Run the four blocking sweeps:
```bash
pnpm -C frontend test:qa-sweep
```

Capture the test report. For EACH failing test:

1. Read the failure message — it's structured as `[route][locale][breakpoint] description: actual vs expected` per RESEARCH "CI failure messages."
2. Open the cited route's component file.
3. Apply the minimal fix:
   - **Touch target <44×44** (likely candidates per RESEARCH §8: HeroUI Button `size="sm"`, MyTasks checkbox): bump size or wrap in 44×44 hit-area via padding. Per CONTEXT discretion, "padding counts."
   - **Horizontal overflow**: locate the offending element, add `min-w-0` or remove a fixed width.
   - **Missing landmark**: add `<main>`, `<nav>`, `<h1>`, or primary CTA — usually a real bug in the route.
   - **Mobile shell collapsed wrong**: AppShell sidebar selector mismatch — update either AppShell to expose the expected selector, or update the helper selector in `qa-sweep.ts` (preferred: fix the AppShell to expose `aside[data-sidebar]` for stability).
   - **Axe violation (serious/critical)**: read the violation rule, fix per axe documentation. Common: missing label, missing button name, contrast failure.
   - **Tab-walk membership mismatch**: focus-trap or hidden-tabbable. Investigate which element is unreachable.

4. Re-run `pnpm -C frontend test:qa-sweep` until exit 0.

**Strict cap on scope:** Fixes MUST be confined to logical-properties, touch-target wrapping, missing landmark insertion, axe semantic fixes, and focus-trap removal. If a sweep failure reveals a deeper architectural bug (e.g., a route requires a full re-architecture), STOP and add a checkpoint:decision task asking the user how to proceed.

Document every fix in 43-07-SUMMARY.md with file:line and one-line rationale.
</action>
<verify>
<automated>
pnpm -C frontend test:qa-sweep 2>&1 | tee /tmp/43-07-sweep.log; SWEEP_EXIT=${PIPESTATUS[0]}; test "$SWEEP_EXIT" -eq 0
</automated>
</verify>
<acceptance_criteria> - `pnpm -C frontend test:qa-sweep` exits 0 - All 4 blocking sweeps green (axe + responsive + keyboard + focus-outline) - Every fix recorded in 43-07-SUMMARY.md with file:line + rationale - No `test.skip` or `test.fixme` added to silence failures
</acceptance_criteria>
<done>
All 4 sweeps green; survivors fixed in-phase per D-10.
</done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Author 43-VERIFICATION.md (verdict)</name>
  <files>.planning/phases/43-rtl-a11y-responsive-sweep/43-VERIFICATION.md</files>
  <read_first>
    - 43-VALIDATION.md (sign-off checklist)
    - All 8 SUMMARY files from this phase (43-00 through 43-07)
  </read_first>
  <action>
Author `.planning/phases/43-rtl-a11y-responsive-sweep/43-VERIFICATION.md` with:

1. **Verdict line** — one of: PASS / PASS-WITH-DEVIATION / PARTIAL / BLOCKED
2. **Per-requirement table** — QA-01, QA-02, QA-03, QA-04 with status (VERIFIED / PARTIAL / BLOCKED) and evidence (sweep test name + commit SHA where it lands green)
3. **Sweep evidence**:
   - `pnpm -C frontend lint` exit code (0 if PASS)
   - `pnpm -C frontend test:qa-sweep` exit code (0 if PASS)
   - 8 focus-outline baselines committed at: `frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/`
   - 14 (or N actual) icon screenshot pairs committed at: `docs/rtl-icons/`
   - `responsive-breakpoints.spec.ts` deleted
4. **5 rotate-180 fixes table** — file, line, before/after one-liner
5. **Deviations** (if any):
   - List any non-logical-property lint survivors documented as pre-existing
   - List any failing icon-screenshot fixtures (likely empty-state issue, not a Phase 43 contract failure)
   - List any sweep tests that landed via shell selector update vs route-component fix
6. **CI gate confirmation**: `.github/workflows/e2e.yml` qa-sweep job present, was the next PR run green?
7. **Next-action** if PARTIAL or BLOCKED: what gap closure plan or human action would flip to PASS

Commit the file.
</action>
<verify>
<automated>
test -f .planning/phases/43-rtl-a11y-responsive-sweep/43-VERIFICATION.md &&
grep -qE "^\*\*Verdict\*\*:" .planning/phases/43-rtl-a11y-responsive-sweep/43-VERIFICATION.md &&
grep -q "QA-01" .planning/phases/43-rtl-a11y-responsive-sweep/43-VERIFICATION.md &&
grep -q "QA-02" .planning/phases/43-rtl-a11y-responsive-sweep/43-VERIFICATION.md &&
grep -q "QA-03" .planning/phases/43-rtl-a11y-responsive-sweep/43-VERIFICATION.md &&
grep -q "QA-04" .planning/phases/43-rtl-a11y-responsive-sweep/43-VERIFICATION.md
</automated>
</verify>
<acceptance_criteria> - VERIFICATION.md exists - Has Verdict line and per-requirement table covering QA-01..04 - Cites sweep evidence (lint exit, test:qa-sweep exit, snapshot counts, deleted spec) - 5 rotate-180 fixes documented
</acceptance_criteria>
<done>
Verification doc authored, verdict committed.
</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                              |
| --------------------------------- | ---------------------------------------------------------------------------------------- |
| Wave 1 specs → Wave 2 enforcement | Specs are scaffolding without Wave 2 gate; Wave 2 turns them into hard CI failures       |
| Local dev → CI                    | Sweeps run locally first; CI replays. Locally-passing sweeps may fail in CI on env diffs |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                                                | Disposition | Mitigation Plan                                                                                                                     |
| --------- | ---------------------- | -------------------------------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| T-43-16   | Tampering              | Survivor "fixed" with `eslint-disable` or `test.skip` instead of real fix                                | mitigate    | Task 2 + Task 6 acceptance criteria explicitly forbid silencing comments                                                            |
| T-43-17   | Repudiation            | rotate-180 → icon-flip migration breaks composition with rotate-90/180 disclosure (EngagementStageGroup) | mitigate    | Task 1c specifically calls out the disclosure semantic distinction; rotate-180 is RETAINED for that case + .icon-flip ADDED for RTL |
| T-43-18   | DoS                    | Sweep runtime blows 20-min CI budget                                                                     | accept      | If hit, Plan 43-07 SUMMARY recommends Wave 3 sharding follow-up; not a blocker for this milestone                                   |
| T-43-19   | Information disclosure | Focus-outline baselines reveal user data in Settings                                                     | mitigate    | Settings page renders user prefs UI, not user content; baselines are scoped to focused-element crops not full page                  |

</threat_model>

<verification>
End-of-phase gates:

1. `pnpm -C frontend lint` exits 0
2. `pnpm -C frontend test:qa-sweep` exits 0 (4 sweeps green)
3. 8 focus-outline baselines committed under `frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/`
4. ≥10 PNGs under `docs/rtl-icons/` (14 expected; document any missing in SUMMARY with reason)
5. `frontend/tests/e2e/responsive-breakpoints.spec.ts` does not exist
6. `grep -rln rotate-180 frontend/src/pages/Dashboard frontend/src/pages/persons frontend/src/components/calendar/UnifiedCalendar.tsx | xargs grep -l "isRTL.*rotate-180"` returns empty (rotate-180+isRTL pattern eliminated; bare rotate-180 for disclosure semantics retained only in EngagementStageGroup)
7. `.github/workflows/e2e.yml` `qa-sweep` job present (verified in Wave 0; re-confirmed here)
8. 43-VERIFICATION.md exists with verdict
   </verification>

<success_criteria>

- All four CI gates green (lint, axe, responsive, keyboard)
- 5 rotate-180 violations resolved (4 migrated, 1 retained for disclosure with .icon-flip composed)
- Focus-outline baselines human-approved
- Icon screenshots regenerated and committed
- Obsolete responsive-breakpoints.spec.ts deleted
- VERIFICATION.md verdict published — PASS or PASS-WITH-DEVIATION acceptable; PARTIAL/BLOCKED requires explicit next-action
  </success_criteria>

<output>
After completion, create `.planning/phases/43-rtl-a11y-responsive-sweep/43-07-SUMMARY.md` recording:
- 5 rotate-180 fixes applied (file:line + diff one-liner)
- Lint survivors fixed (file:line + class swap)
- Sweep survivors fixed (file:line + fix description)
- Pre-existing issues documented (NOT fixed, with reason)
- 8 focus-outline baselines committed (commit SHA)
- N PNG fixtures committed under docs/rtl-icons/ (N actual count)
- responsive-breakpoints.spec.ts deletion (commit SHA)
- 43-VERIFICATION.md verdict
</output>
