---
phase: 43-rtl-a11y-responsive-sweep
plan: 08
type: execute
wave: 3
depends_on: ['43-07', '43-12']
files_modified:
  - frontend/src/components/layout/Topbar.tsx
  - frontend/src/components/calendar/UnifiedCalendar.tsx
  - frontend/src/components/calendar/CalendarEntryForm.tsx
  - frontend/src/pages/Dashboard/widgets/MyTasks.tsx
  - frontend/src/components/advanced-search/AdvancedSearchFilters.tsx
  - frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx
  - frontend/src/components/ui/checkbox.tsx
  - frontend/src/index.css
autonomous: true
requirements: [QA-03]
tags: [qa, gate, remediation, touch-target, heroui]
gap_closure: true
user_setup: []

must_haves:
  truths:
    - 'Topbar tb-dir-btn radio buttons measure ≥44×44 px at every breakpoint (currently 32×36)'
    - 'HeroUI Checkbox call sites on calendar / dashboard / search-filter surfaces measure ≥44×44 px (currently 20×20)'
    - 'Calendar nav Button size="sm" prev/next measure ≥44×44 px (currently 32×36)'
    - 'qa-sweep-responsive.spec.ts touch-target gate passes for the post-auth v6.0 surface (login form bleed-through fixed by 43-12)'
  artifacts:
    - path: 'frontend/src/index.css'
      provides: '.touch-44 utility class — min-width / min-height / inline-flex centering for compact controls without bumping their visual size'
      contains: '.touch-44'
    - path: 'frontend/src/components/layout/Topbar.tsx'
      provides: 'tb-dir-btn min-h-11 min-w-11 enforced'
      contains: 'min-h-11'
    - path: 'frontend/src/components/calendar/UnifiedCalendar.tsx'
      provides: 'prev/next nav buttons wrapped to 44×44'
      contains: 'min-h-11'
    - path: 'frontend/src/components/calendar/CalendarEntryForm.tsx'
      provides: 'HeroUI Checkbox wrapped in touch-44 hit area'
      contains: 'touch-44'
  key_links:
    - from: 'qa-sweep-responsive.spec.ts assertTouchTargets'
      to: 'every interactive on the v6.0 surface ≥44×44'
      via: 'boundingBox().width >= 44 && height >= 44'
      pattern: '\\.toBeGreaterThanOrEqual\\(44\\)'
---

<objective>
Wrap or resize the three HeroUI primitives that fail the ≥44×44 touch-target gate on the post-auth v6.0 surface: HeroUI `Checkbox` (20×20), `Button size="sm"` (32×36) on calendar nav, and the topbar `tb-dir-btn` radio buttons (32×36). Add a single `.touch-44` utility class so we never invent the wrapper a-second-time, then apply it surgically to call sites identified in 43-VERIFICATION.md Class E.

Purpose: Closes Class E of 43-VERIFICATION.md. Per RESEARCH §8, this is the planner-anticipated remediation pattern: "bump size or wrap in 44×44 hit-area via padding."

Output: 6–8 component files modified; 1 new utility CSS class; qa-sweep-responsive.spec.ts touch-target gate passes for authenticated routes.

Wave dependency: This plan runs in WAVE 3 — AFTER 43-12 has merged the auth-storageState + main-scoped query changes. Without 43-12, the qa-sweep run in Task 5 would be flooded by login-form Class D bleed-through and could not truthfully verify Class E remediation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-rtl-a11y-responsive-sweep/43-CONTEXT.md
@.planning/phases/43-rtl-a11y-responsive-sweep/43-VERIFICATION.md
@.planning/phases/43-rtl-a11y-responsive-sweep/43-07-SUMMARY.md
@.planning/phases/43-rtl-a11y-responsive-sweep/43-12-SUMMARY.md

@frontend/design-system/inteldossier_handoff_design/README.md
@frontend/design-system/inteldossier_handoff_design/handoff/app.css
@frontend/src/components/layout/Topbar.tsx
@frontend/src/components/calendar/UnifiedCalendar.tsx
@frontend/src/components/calendar/CalendarEntryForm.tsx
@frontend/src/index.css
@frontend/tests/e2e/qa-sweep-responsive.spec.ts
@frontend/tests/e2e/helpers/qa-sweep.ts

<interfaces>
<!-- The .touch-44 utility class — single canonical CSS rule that downstream call sites
     reference. No HOC, no wrapper component. Pure CSS = one place to change. -->

Existing tokens in use (do NOT redefine):

- var(--row-h) — density-aware row height
- var(--radius-sm) — 8px

The .touch-44 class is added once to frontend/src/index.css:

```css
/* QA-03 — minimum 44×44 hit area for compact interactives (HeroUI Checkbox,
   Button size="sm", topbar radios). Visual size unchanged; the utility only
   guarantees the bounding box passes the touch-target gate. Applied as a
   parent <span/div> wrapper OR directly on the control via className merge. */
.touch-44 {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-inline-size: 44px;
  min-block-size: 44px;
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add .touch-44 utility class to index.css</name>
  <files>frontend/src/index.css</files>
  <read_first>
    - frontend/src/index.css (full file — locate the section where .icon-flip is defined and add .touch-44 immediately after it; same precedent: a single small utility class for cross-cutting RTL/a11y concerns)
    - frontend/design-system/inteldossier_handoff_design/handoff/app.css (canonical token usage pattern; confirm logical-property names)
  </read_first>
  <action>
    Append the following exact CSS block immediately after the existing `.icon-flip` definition in `frontend/src/index.css` (locate via `grep -n "icon-flip" frontend/src/index.css`):

    ```css
    /* QA-03 — minimum 44×44 hit area for compact interactives (HeroUI
       Checkbox, Button size="sm", topbar radio direction switcher). Pure
       hit-area expansion; visual size of the inner control is unchanged.
       Applied either as a parent wrapper className OR merged onto the
       control's own className when the control already carries the click
       handler. Logical-property `inline-size` / `block-size` keep RTL
       correct without extra rules. */
    .touch-44 {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-inline-size: 44px;
      min-block-size: 44px;
    }
    ```

    Do NOT introduce a Tailwind plugin, theme extension, or `@layer utilities` wrapper — this is a single global class that mirrors the precedent set by `.icon-flip` in the same file. Do NOT add raw hex colors. Do NOT wrap in `@media`.

  </action>
  <acceptance_criteria>
    - `grep -c "^\.touch-44 {" frontend/src/index.css` outputs `1`
    - `grep "min-inline-size: 44px" frontend/src/index.css` matches one line
    - `grep "min-block-size: 44px" frontend/src/index.css` matches one line
    - File contains no raw hex colors introduced by this task (`grep -E "#[0-9a-fA-F]{3,8}" frontend/src/index.css` count unchanged from baseline)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^\.touch-44 {" frontend/src/index.css | grep -q '^1$' && grep -q "min-inline-size: 44px" frontend/src/index.css && grep -q "min-block-size: 44px" frontend/src/index.css</automated>
  </verify>
  <done>`.touch-44` class defined once in `index.css`; matches the spec block exactly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply touch-target fix to topbar tb-dir-btn (highest-impact site — visible on every v6.0 route)</name>
  <files>frontend/src/components/layout/Topbar.tsx</files>
  <read_first>
    - frontend/src/components/layout/Topbar.tsx (full file — current `tb-dir-btn` block at lines ~131-153)
    - frontend/src/components/layout/Topbar.test.tsx (avoid breaking existing assertions on the radio group)
    - frontend/design-system/inteldossier_handoff_design/handoff/app.css (confirm `--row-h` density-aware row height precedent)
  </read_first>
  <action>
    In the `tb-dir-btn` `<button>` (the radio direction switcher loop, currently `h-9 px-2.5`), modify the className `cn(...)` call to add `min-h-11 min-w-11` while preserving the existing `h-9 px-2.5` visual. The result keeps the visual height at 36px on desktop but expands the hit box to 44×44 via min-size; the parent `<div className="tb-dir inline-flex overflow-hidden ...">` already centers content.

    Specifically:
    - Locate the `<button` element under the `DIRECTIONS.map((d) => {` loop.
    - Change `'tb-dir-btn h-9 px-2.5 font-body text-[11.5px] font-medium'` to `'tb-dir-btn h-9 min-h-11 min-w-11 px-2.5 font-body text-[11.5px] font-medium'`.
    - Also update the `max-sm:w-8 max-sm:px-0 max-sm:text-[10.5px]` line to `max-sm:min-w-11 max-sm:px-1 max-sm:text-[10.5px]` (drops `max-sm:w-8` and `max-sm:px-0` so the mobile hit box is 44×44; keeps the smaller text). Mobile visual: an 11px text glyph centered in a 44px box — acceptable per CLAUDE.md mobile read-only rules.

    Do NOT change the parent `<div className="tb-dir inline-flex overflow-hidden ...">` styling. Do NOT change aria-* attributes. Do NOT change the `tb-dir-label` / `tb-dir-short` spans. The `min-h-11` / `min-w-11` Tailwind classes resolve to 44px (Tailwind v4 default 4px scale × 11 = 44px).

  </action>
  <acceptance_criteria>
    - `grep -c "min-h-11 min-w-11" frontend/src/components/layout/Topbar.tsx` ≥ 1
    - `grep -c "max-sm:w-8" frontend/src/components/layout/Topbar.tsx` outputs `0`
    - `grep -c "tb-dir-btn" frontend/src/components/layout/Topbar.tsx` unchanged from baseline (still appears as the css class name)
    - `pnpm -C frontend lint frontend/src/components/layout/Topbar.tsx` exits 0 (no new violations introduced; pre-existing violations may remain)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "min-h-11 min-w-11" frontend/src/components/layout/Topbar.tsx && ! grep -q "max-sm:w-8" frontend/src/components/layout/Topbar.tsx</automated>
  </verify>
  <done>tb-dir-btn radio buttons render at min 44×44 on every breakpoint; visual height unchanged on desktop; mobile drops the explicit 32px width.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Apply touch-target fix to calendar nav buttons + EngagementStageGroup chevron</name>
  <files>frontend/src/components/calendar/UnifiedCalendar.tsx, frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx</files>
  <read_first>
    - frontend/src/components/calendar/UnifiedCalendar.tsx (lines ~190-210 — the two prev/next nav buttons that 43-07 already migrated to `.icon-flip`)
    - frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx (the disclosure chevron)
    - frontend/src/components/calendar/UnifiedCalendar.tsx Phase 39 visual baselines under frontend/tests/e2e/calendar-visual.spec.ts-snapshots/ (avoid breaking 1280 calendar baseline; the visual height of the button is unchanged)
  </read_first>
  <action>
    **In `frontend/src/components/calendar/UnifiedCalendar.tsx`:**
    Locate the two `<Button>` elements (HeroUI) that render the prev/next month/week nav (currently with `size="sm"` or equivalent compact size + `className="icon-flip"`). For each:
    - Add `min-h-11 min-w-11` to the existing className. If the button uses HeroUI v3 `<Button size="sm">`, change to `size="md"` and keep `min-h-11 min-w-11` as belt-and-braces (HeroUI v3 `size="md"` is 40×40 — short of 44).
    - Preserve `.icon-flip` and the existing `aria-label`.

    **In `frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx`:**
    Locate the chevron button (the disclosure trigger with `rotate-180` + `.icon-flip` from 43-07). The button is currently a `<button>` (native) — add `min-h-11 min-w-11` to its className. If it's wrapped in a parent that already provides ≥44×44 (e.g., a row), add `touch-44` class to that parent INSTEAD and leave the inner button alone. Confirm via reading the file which approach applies.

    Logical properties only. No raw hex. No card shadows. Do NOT change icon size. Do NOT change `aria-expanded` or other aria attributes.

  </action>
  <acceptance_criteria>
    - `grep -c "min-h-11" frontend/src/components/calendar/UnifiedCalendar.tsx` ≥ 2 (one per nav button)
    - `grep -c "min-h-11\|touch-44" frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx` ≥ 1
    - `grep -c "size=\"sm\"" frontend/src/components/calendar/UnifiedCalendar.tsx` decreases by ≥ 2 OR the call sites also add `min-h-11` (do NOT delete `size="sm"` if it's used elsewhere in the file for non-interactive controls)
    - `pnpm -C frontend lint frontend/src/components/calendar/UnifiedCalendar.tsx frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx` introduces 0 new errors
    - `.icon-flip` class still present on the calendar nav buttons (regression guard for 43-07 migration)
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'min-h-11' frontend/src/components/calendar/UnifiedCalendar.tsx)" -ge 2 && grep -qE "min-h-11|touch-44" frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx && grep -q "icon-flip" frontend/src/components/calendar/UnifiedCalendar.tsx</automated>
  </verify>
  <done>Calendar prev/next nav and EngagementStageGroup chevron each measure ≥44×44; .icon-flip retained on calendar nav.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Wrap HeroUI Checkbox call sites on v6.0 surfaces in .touch-44 hit area</name>
  <files>frontend/src/components/calendar/CalendarEntryForm.tsx, frontend/src/pages/Dashboard/widgets/MyTasks.tsx, frontend/src/components/advanced-search/AdvancedSearchFilters.tsx</files>
  <read_first>
    - frontend/src/components/ui/checkbox.tsx (the Checkbox primitive; understand the rendered DOM box — likely a 20×20 input + invisible larger label area)
    - frontend/src/components/calendar/CalendarEntryForm.tsx (line ~456 area — the existing Checkbox usage)
    - frontend/src/pages/Dashboard/widgets/MyTasks.tsx (line ~134 — Checkbox in the task list widget)
    - frontend/src/components/advanced-search/AdvancedSearchFilters.tsx (line ~277 — Checkbox in filter panel)
    - frontend/design-system/inteldossier_handoff_design/handoff/app.css (form control density precedent)

    Discovery sanity check: re-run `rg -n "<Checkbox\b" frontend/src/components/calendar frontend/src/pages/Dashboard/widgets frontend/src/components/advanced-search` to confirm the 3 files above are the actual v6.0-surface Checkbox call sites. (PersonsListPage.tsx and ActivityPage.tsx have ZERO `<Checkbox>` — that is why they were dropped from `files_modified` in the W1 revision.)

</read_first>
<action>
For each `<Checkbox>` call site in the 3 files above, wrap the Checkbox in a parent `<label className="touch-44 ...">` (or `<span className="touch-44">` if the Checkbox is already inside a label). The wrapper expands the hit box to 44×44 without bumping the visual checkbox size.

    Per-file approach:
    - **CalendarEntryForm.tsx (line ~456):** the Checkbox sits inside a form row. Wrap in `<span className="touch-44">{checkbox}</span>` OR add `touch-44` to the existing wrapping label.
    - **MyTasks.tsx (line ~134):** the Checkbox sits in a kanban-style task list row. The row likely already measures ≥44 tall — confirm by reading the row's className (look for `min-h`, `py-3`, `h-12`, etc.). If the row is already ≥44×44, add `touch-44` to the Checkbox itself; otherwise wrap.
    - **AdvancedSearchFilters.tsx (line ~277):** filter Checkbox in a vertical list. Wrap in `<span className="touch-44">{checkbox}</span>` and pair with the label text in a sibling so the label remains clickable as before.

    Decision rule (apply per call site):
    - If the Checkbox is bare (no surrounding 44×44 element), wrap in `<span className="touch-44">{checkbox}</span>`.
    - If the Checkbox is inside a row that already measures ≥44 in both dimensions (e.g., a list-row card), add `touch-44` className to the *checkbox itself* — do not wrap.
    - When in doubt, wrap.

    Limit edits to the 3 files in this task's `files`. Do NOT touch admin / ai-* / settings. Do NOT modify `frontend/src/components/ui/checkbox.tsx` itself — keep the primitive untouched per Karpathy "Surgical Changes" (changing the primitive risks regressing 100+ unrelated call sites).

    Logical properties only. No raw hex. No card shadows.

  </action>
  <acceptance_criteria>
    - `grep -c "touch-44" frontend/src/components/calendar/CalendarEntryForm.tsx frontend/src/pages/Dashboard/widgets/MyTasks.tsx frontend/src/components/advanced-search/AdvancedSearchFilters.tsx 2>/dev/null | awk -F: '{sum+=$2} END {print sum}'` outputs ≥ 3 (at least one wrap per file)
    - frontend/src/components/ui/checkbox.tsx is NOT modified (`git diff -- frontend/src/components/ui/checkbox.tsx` shows no changes)
    - `pnpm -C frontend lint <each touched file>` introduces 0 new errors
  </acceptance_criteria>
  <verify>
    <automated>! git diff HEAD -- frontend/src/components/ui/checkbox.tsx | grep -q "^[+-]" && test "$(grep -c 'touch-44' frontend/src/components/calendar/CalendarEntryForm.tsx frontend/src/pages/Dashboard/widgets/MyTasks.tsx frontend/src/components/advanced-search/AdvancedSearchFilters.tsx 2>/dev/null | awk -F: '{sum+=$2} END {print sum}')" -ge 3</automated>
  </verify>
  <done>Every Checkbox call site in the 3 v6.0-surface files either (a) wraps in `.touch-44` or (b) sits in a row that already measures ≥44 by visual layout. The primitive (`components/ui/checkbox.tsx`) is unchanged from HEAD.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Run qa-sweep-responsive.spec.ts and confirm touch-target gate passes for authenticated routes</name>
  <files>(verification only — no source changes; if survivors found, append additional .touch-44 wrappers in the file they appear)</files>
  <read_first>
    - frontend/tests/e2e/qa-sweep-responsive.spec.ts (the touch-target assertion battery — already scoped to `main` and using `waitForRouteReady` per 43-12)
    - frontend/tests/e2e/helpers/qa-sweep.ts (assertTouchTargets implementation + waitForRouteReady)
    - .planning/phases/43-rtl-a11y-responsive-sweep/43-VERIFICATION.md "Class E" subsection (real touch-target violations on v6.0 surface)
    - .planning/phases/43-rtl-a11y-responsive-sweep/43-12-SUMMARY.md (confirms the storageState + main-scope landed; expected pass-rate baseline post-12)
  </read_first>
  <action>
    Run `pnpm -C frontend test:qa-sweep -- qa-sweep-responsive.spec.ts --reporter=list 2>&1 | tee /tmp/43-08-touch-targets.log`.

    Because 43-12 has already landed (this plan is in Wave 3), every failure should now point at a real v6.0-surface element — login-form bleed-through (Class D) is no longer in the result set. For each surviving touch-target violation:
    1. Identify the file containing the offending element (selector hint in the failure message + grep).
    2. Apply the same `.touch-44` wrap or `min-h-11 min-w-11` className merge from Task 2-4 to that call site.
    3. Re-run `pnpm -C frontend test:qa-sweep -- qa-sweep-responsive.spec.ts --reporter=list` until all touch-target failures are resolved.

    Stop conditions:
    - If a survivor surfaces on `/login` despite 43-12 — the chromium-no-auth project may not be filtering correctly; STOP and surface as checkpoint:decision (handoff back to 43-12 maintainers, not 43-08).
    - If after 3 fix-iterations the touch-target gate still has authenticated-route survivors, STOP and surface as a checkpoint:decision in the SUMMARY (do NOT add eslint-disable, do NOT skip the test).

    Do NOT modify the qa-sweep-responsive.spec.ts itself — fix the source code, not the test. Do NOT modify helpers/qa-sweep.ts thresholds.

  </action>
  <acceptance_criteria>
    - `pnpm -C frontend test:qa-sweep -- qa-sweep-responsive.spec.ts --reporter=list` produces output where every remaining touch-target violation has been escalated to a checkpoint:decision in the plan SUMMARY OR no violations remain
    - No new file `eslint-disable` comments introduced for touch-target rules
    - `frontend/tests/e2e/qa-sweep-responsive.spec.ts` is unchanged vs HEAD (`git diff` returns empty for that path)
    - `frontend/tests/e2e/helpers/qa-sweep.ts` is unchanged vs HEAD (`git diff` returns empty for that path)
  </acceptance_criteria>
  <verify>
    <automated>! git diff HEAD -- frontend/tests/e2e/qa-sweep-responsive.spec.ts | grep -q "^[+-]" && ! git diff HEAD -- frontend/tests/e2e/helpers/qa-sweep.ts | grep -q "^[+-]" && pnpm -C frontend test:qa-sweep -- qa-sweep-responsive.spec.ts --reporter=list 2>&1 | tee /tmp/43-08-final.log | grep -E "Class E|touch-target" || true</automated>
  </verify>
  <done>qa-sweep-responsive touch-target gate passes for every v6.0 authenticated route, OR every remaining failure is documented as a checkpoint:decision.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                                       |
| ------------------------------------------ | --------------------------------------------------------------------------------- |
| user keyboard/touch → React event handlers | Hit-area expansion changes the DOM target the browser dispatches click/keydown to |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                                             | Disposition | Mitigation Plan                                                                                                                        |
| ---------- | ---------------------- | ------------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| T-43-08-01 | Tampering              | `.touch-44` wrapper accidentally captures clicks meant for sibling controls           | mitigate    | Use `inline-flex` + `min-inline-size`/`min-block-size` (no extra padding bleed); wrap only the control itself, not a multi-control row |
| T-43-08-02 | Denial of Service      | Hit-area enlargement creates overlapping click zones that block adjacent interactives | accept      | Designer review at 1280px is in scope of the 43-VERIFICATION re-run; visual baselines (1280) catch overlap regressions                 |
| T-43-08-03 | Information disclosure | none — pure CSS/className changes; no data flow                                       | accept      | n/a                                                                                                                                    |

</threat_model>

<verification>
- `grep -c "^\.touch-44 {" frontend/src/index.css` outputs `1`
- Topbar `tb-dir-btn` has `min-h-11 min-w-11` (`grep -q "min-h-11 min-w-11" frontend/src/components/layout/Topbar.tsx`)
- Calendar prev/next nav buttons have `min-h-11` (≥2 occurrences)
- EngagementStageGroup chevron has `min-h-11` OR `touch-44`
- At least one Checkbox call site wrapped with `touch-44` in EACH of the 3 files (CalendarEntryForm, MyTasks, AdvancedSearchFilters)
- `frontend/src/components/ui/checkbox.tsx` is unchanged vs HEAD
- `pnpm -C frontend lint` introduces 0 new errors on touched files
- `pnpm -C frontend test:qa-sweep -- qa-sweep-responsive.spec.ts` touch-target failures on authenticated routes drop to 0 (43-12 has already eliminated login-form bleed-through; any remaining failure is a real v6.0-surface bug to fix here)
</verification>

<success_criteria>
qa-sweep-responsive.spec.ts touch-target assertion passes for the v6.0 authenticated surface. The HeroUI primitive call sites that 43-VERIFICATION.md Class E flagged are remediated. Class D (login-form bleed-through) was eliminated by 43-12 in Wave 2 — this plan only sees real v6.0 violations.
</success_criteria>

<output>
After completion, create `.planning/phases/43-rtl-a11y-responsive-sweep/43-08-SUMMARY.md` documenting:
- Files modified (with exact line counts)
- Number of Checkbox call sites wrapped vs left unchanged (with rationale per site)
- qa-sweep-responsive.spec.ts pass/fail delta vs the post-43-12 baseline
- Any survivors escalated to checkpoint:decision
</output>
</content>
</invoke>
