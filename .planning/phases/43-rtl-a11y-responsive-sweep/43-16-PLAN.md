---
phase: 43-rtl-a11y-responsive-sweep
plan: 16
type: execute
wave: 2
depends_on: ['43-13', '43-14', '43-15']
files_modified:
  - frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/bureau-light-focused-primitive-chromium-darwin.png
  - frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/bureau-dark-focused-primitive-chromium-darwin.png
  - frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/chancery-light-focused-primitive-chromium-darwin.png
  - frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/chancery-dark-focused-primitive-chromium-darwin.png
  - frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/ministerial-light-focused-primitive-chromium-darwin.png
  - frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/ministerial-dark-focused-primitive-chromium-darwin.png
  - frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/situation-light-focused-primitive-chromium-darwin.png
  - frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/situation-dark-focused-primitive-chromium-darwin.png
autonomous: true
requirements: [QA-02, QA-03]
tags: [qa, gate, remediation, visual, snapshot, gap-closure, final-wave]
gap_closure: true
user_setup: []

must_haves:
  truths:
    - 'All 8 focus-outline visual baselines (Settings × 4 directions × 2 modes) reflect the post-43-13/43-14/43-15 layout state'
    - 'qa-sweep-focus-outline.spec.ts exits 0 against the regenerated baselines'
    - 'Full `pnpm -C frontend test:qa-sweep` exits 0 across all 4 sweeps (axe / responsive / keyboard / focus-outline) — Gap-1 / 2 / 3 / 4 all closed'
    - 'No production code changed in this plan; only PNG baseline files committed'
  artifacts:
    - path: 'frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/'
      provides: '8 regenerated PNGs (one per direction × mode) reflecting the post-Gap-1/2/3 DOM'
      contains: 'bureau-light-focused-primitive-chromium-darwin.png'
  key_links:
    - from: 'qa-sweep-focus-outline.spec.ts toMatchSnapshot calls'
      to: 'committed baselines under qa-sweep-focus-outline.spec.ts-snapshots/'
      via: 'Playwright auto-comparison with maxDiffPixelRatio: 0.01 (per playwright.config.ts:89)'
      pattern: 'toMatchSnapshot|toHaveScreenshot'
---

<objective>
Close Gap-4 from 43-HUMAN-UAT.md (focus-outline sweep — 8 fails, MEDIUM
severity) AND run the final full `pnpm -C frontend test:qa-sweep` to
confirm all 4 sweeps exit 0 simultaneously.

Gap-4 detail: All 8 baselines (Settings × 4 directions × 2 modes) fail
snapshot diff because they were captured before 43-08, 43-09, 43-10, 43-11
landed. 43-13 (.btn min-height) and 43-14 (DossierTable role swap) further
shift layout, so a baseline regeneration now MUST wait for 43-13/14/15
to ship.

Gap-1, Gap-2, Gap-3 fixes are spread across 43-13, 43-14, 43-15. This
plan is the FINAL WAVE — depends on all three landing first, then:

1. Regenerates the 8 PNG baselines under
   `frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/`
2. Runs the full `pnpm -C frontend test:qa-sweep` to confirm all 4 sweeps
   pass simultaneously (or documents which env is needed and what
   passes statically).

Purpose: Final acceptance gate for Phase 43 close-out. Without this plan,
Gap-1/2/3 fixes ship but Gap-4 keeps the sweep red.

Output: 8 regenerated PNG baselines committed. SUMMARY contains the
final per-sweep pass/fail summary.

Wave dependency: WAVE 2 — depends_on `[43-13, 43-14, 43-15]` (all of
which are Wave 1). MUST run last; running before Gap-1/2/3 land would
bake stale baselines that re-capture the same problem.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-rtl-a11y-responsive-sweep/43-CONTEXT.md
@.planning/phases/43-rtl-a11y-responsive-sweep/43-VERIFICATION.md
@.planning/phases/43-rtl-a11y-responsive-sweep/43-HUMAN-UAT.md
@.planning/phases/43-rtl-a11y-responsive-sweep/43-13-PLAN.md
@.planning/phases/43-rtl-a11y-responsive-sweep/43-14-PLAN.md
@.planning/phases/43-rtl-a11y-responsive-sweep/43-15-PLAN.md

@frontend/tests/e2e/qa-sweep-focus-outline.spec.ts
@frontend/playwright.config.ts

<interfaces>
<!-- The focus-outline sweep is a visual-regression test that screenshots
     the Settings page primitive (`__design` query-string hatch — bypasses
     auth) in 4 directions × 2 modes = 8 combinations. Baselines committed
     at frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/ as 8
     PNGs:

       bureau-light-focused-primitive-chromium-darwin.png
       bureau-dark-focused-primitive-chromium-darwin.png
       chancery-light-focused-primitive-chromium-darwin.png
       chancery-dark-focused-primitive-chromium-darwin.png
       ministerial-light-focused-primitive-chromium-darwin.png
       ministerial-dark-focused-primitive-chromium-darwin.png
       situation-light-focused-primitive-chromium-darwin.png
       situation-dark-focused-primitive-chromium-darwin.png

     Per playwright.config.ts:89, `expect.toHaveScreenshot.maxDiffPixelRatio
     = 0.01` — 1% pixel-diff tolerance.

     This sweep does NOT require Supabase auth (it uses `__design`
     hatch + the chromium-no-auth project), so it can run on any
     environment that has a working frontend dev server.

     Regeneration command:
       pnpm -C frontend exec playwright test qa-sweep-focus-outline.spec.ts --update-snapshots

     The `--update-snapshots` flag causes Playwright to write new PNGs at
     each failing comparison site instead of asserting against the existing
     baseline. After this, re-run WITHOUT the flag to confirm the new
     baselines are stable. -->

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Regenerate 8 focus-outline visual baselines (Settings × 4 directions × 2 modes)</name>
  <files>frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/bureau-light-focused-primitive-chromium-darwin.png, frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/bureau-dark-focused-primitive-chromium-darwin.png, frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/chancery-light-focused-primitive-chromium-darwin.png, frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/chancery-dark-focused-primitive-chromium-darwin.png, frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/ministerial-light-focused-primitive-chromium-darwin.png, frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/ministerial-dark-focused-primitive-chromium-darwin.png, frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/situation-light-focused-primitive-chromium-darwin.png, frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/situation-dark-focused-primitive-chromium-darwin.png</files>
  <read_first>
    - frontend/tests/e2e/qa-sweep-focus-outline.spec.ts (full file — confirm the test routine and the 4 × 2 matrix it produces)
    - frontend/playwright.config.ts (lines 70-89 — confirm `chromium-no-auth` project exists and `maxDiffPixelRatio: 0.01` is in effect)
    - .planning/phases/43-rtl-a11y-responsive-sweep/43-HUMAN-UAT.md (Gap-4 detail — all 8 baselines stale)
    - 43-13 + 43-14 + 43-15 SUMMARYs (confirm those plans landed and what they changed; verifies the rationale for regeneration)
  </read_first>
  <action>
    Execute snapshot regeneration in 3 steps.

    **Step 1 — Confirm dependency wave landed:**
    Verify all of 43-13, 43-14, 43-15 have produced their SUMMARY files:
    - `ls .planning/phases/43-rtl-a11y-responsive-sweep/43-13-SUMMARY.md`
    - `ls .planning/phases/43-rtl-a11y-responsive-sweep/43-14-SUMMARY.md`
    - `ls .planning/phases/43-rtl-a11y-responsive-sweep/43-15-SUMMARY.md`
    All 3 must exist. Also verify the production-code changes are present:
    - `grep -q "min-block-size: 2.75rem" frontend/src/styles/list-pages.css`
    - `! grep -q 'role="row"' frontend/src/components/list-page/DossierTable.tsx`
    - `grep -q "evaluateAll" frontend/tests/e2e/qa-sweep-keyboard.spec.ts`

    If any of those checks fail, STOP and surface as `checkpoint:decision` — the wave ordering is broken; do NOT regenerate baselines on a partial state.

    **Step 2 — Regenerate baselines:**
    Run `pnpm -C frontend exec playwright test qa-sweep-focus-outline.spec.ts --update-snapshots --reporter=list 2>&1 | tee /tmp/43-16-task1-update.log`.

    This rewrites the 8 PNGs with the post-Gap-1/2/3 DOM rendering. The `__design` route hatch in qa-sweep-focus-outline.spec.ts means this run does NOT need Supabase auth — runnable on any environment with a working frontend dev server.

    **Step 3 — Stability re-run:**
    Without `--update-snapshots`, re-run: `pnpm -C frontend exec playwright test qa-sweep-focus-outline.spec.ts --reporter=list 2>&1 | tee /tmp/43-16-task1-verify.log`. This confirms the freshly written baselines are themselves stable (no flapping pixel diffs from the local environment). Expect 8/8 pass.

    Commit the 8 PNG files. Verify via `git status frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/` that exactly 8 files are modified (or created if previously absent for this OS+browser combo).

    Stop conditions:
    - If Step 2 generates more than 8 PNGs, STOP — it means the spec matrix expanded beyond Settings × 4 × 2; surface as `checkpoint:decision`.
    - If Step 3 (stability re-run) produces any pixel diff after Step 2 wrote the new baselines, STOP — the test is flaky on this hardware; surface as `checkpoint:decision` with the per-pixel diff numbers.
    - If `--update-snapshots` cannot run because the dev server fails to start (typical: missing Vite env), STOP and document. The user can re-run this task on a working environment.

    Do NOT modify the spec file `qa-sweep-focus-outline.spec.ts`. Do NOT modify `playwright.config.ts`. Do NOT regenerate any other snapshot directory (axe / responsive / keyboard sweeps don't take pixel snapshots; calendar / dossier-list visual baselines are for separate phases).

  </action>
  <acceptance_criteria>
    - 43-13-SUMMARY.md, 43-14-SUMMARY.md, 43-15-SUMMARY.md all exist on disk
    - `grep -q "min-block-size: 2.75rem" frontend/src/styles/list-pages.css` exits 0 (43-13 landed)
    - `! grep -q 'role="row"' frontend/src/components/list-page/DossierTable.tsx` exits 0 (43-14 landed)
    - `grep -q "evaluateAll" frontend/tests/e2e/qa-sweep-keyboard.spec.ts` exits 0 (43-15 landed)
    - `ls frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/*.png | wc -l` outputs `8` (no more, no fewer)
    - All 8 expected filenames exist:
      - `bureau-light-focused-primitive-chromium-darwin.png`
      - `bureau-dark-focused-primitive-chromium-darwin.png`
      - `chancery-light-focused-primitive-chromium-darwin.png`
      - `chancery-dark-focused-primitive-chromium-darwin.png`
      - `ministerial-light-focused-primitive-chromium-darwin.png`
      - `ministerial-dark-focused-primitive-chromium-darwin.png`
      - `situation-light-focused-primitive-chromium-darwin.png`
      - `situation-dark-focused-primitive-chromium-darwin.png`
    - Step 3 stability re-run produces 8/8 pass for `qa-sweep-focus-outline.spec.ts`
    - `frontend/tests/e2e/qa-sweep-focus-outline.spec.ts` is byte-unchanged (`git diff HEAD -- frontend/tests/e2e/qa-sweep-focus-outline.spec.ts` returns empty)
    - `frontend/playwright.config.ts` is byte-unchanged
    - No file under `frontend/src/` was modified by this task
  </acceptance_criteria>
  <verify>
    <automated>test "$(ls frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/*.png 2>/dev/null | wc -l | tr -d ' ')" = "8" && ! git diff HEAD -- frontend/tests/e2e/qa-sweep-focus-outline.spec.ts frontend/playwright.config.ts | grep -q "^[+-]"</automated>
  </verify>
  <done>8 freshly regenerated PNG baselines committed; stability re-run reports 8/8 pass; spec file and playwright config byte-unchanged; no production code touched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Final acceptance — run full `pnpm -C frontend test:qa-sweep` and confirm 0 failures across all 4 sweeps</name>
  <files>(verification only — no source changes; if survivors found, escalate as checkpoint:decision)</files>
  <read_first>
    - frontend/package.json (the `test:qa-sweep` script — confirms it runs all 4 sweeps in sequence)
    - frontend/tests/e2e/qa-sweep-axe.spec.ts (axe sweep)
    - frontend/tests/e2e/qa-sweep-responsive.spec.ts (responsive sweep)
    - frontend/tests/e2e/qa-sweep-keyboard.spec.ts (post-43-15 state)
    - frontend/tests/e2e/qa-sweep-focus-outline.spec.ts (focus-outline sweep)
    - .planning/phases/43-rtl-a11y-responsive-sweep/43-VERIFICATION.md (success criteria for SC-2 + SC-3 NEEDS HUMAN/CI)
    - .planning/phases/43-rtl-a11y-responsive-sweep/43-HUMAN-UAT.md (the 4 gaps this final wave closes)
  </read_first>
  <action>
    Two-phase final verification.

    **Phase A — local static gate (always runnable):**
    1. `pnpm -C frontend lint 2>&1 | grep -c "rtl-friendly"` → expect `0` (QA-01 still satisfied; no regression from Gap-1/2/3 fixes).
    2. `pnpm -C frontend exec tsc --noEmit 2>&1 | tail -10` → expect zero `error TS` lines.
    3. `pnpm -C frontend exec vite build --mode development 2>&1 | tail -5` → expect `built in` line; no PostCSS / esbuild errors.
    4. `pnpm -C frontend exec vitest run frontend/src/components/list-page/__tests__/GenericListPage.test.tsx 2>&1 | tail -10` → expect 0 failed.

    All 4 must pass. If any fail, STOP and escalate as checkpoint:decision — Gap-1/2/3 fixes introduced a regression.

    **Phase B — full qa-sweep runtime gate (env-dependent):**
    Run `pnpm -C frontend test:qa-sweep --reporter=list 2>&1 | tee /tmp/43-16-task2-fullsweep.log`.

    Expected outcome on a Supabase-equipped env:
    - `qa-sweep-axe.spec.ts` 0 violations on every v6.0 route × en/ar (Gap-2 closure verified)
    - `qa-sweep-responsive.spec.ts` 0 touch-target failures on every v6.0 route × 5 breakpoints × en/ar (Gap-1 closure verified)
    - `qa-sweep-keyboard.spec.ts` visibleCount > 0 on every non-empty v6.0 route × en/ar; membership pass-rate ≥28/30 (Gap-3 closure verified — the spec is now truthful)
    - `qa-sweep-focus-outline.spec.ts` 8/8 pass against the regenerated baselines (Gap-4 closure verified)

    Total expectation: 4 sweeps × ~30 cases each = ~120 tests, all green.

    Acceptable alternative outcome (env missing VITE_SUPABASE_URL):
    - Runtime sweep aborts at globalSetup login. Document in SUMMARY:
      - Phase A static evidence is intact (proves no regression introduced)
      - Gap-4 sub-sweep (focus-outline) DID run because it uses `__design` route hatch (chromium-no-auth project) — record its 8/8 pass.
      - Document the env-blocking error verbatim and mark axe/responsive/keyboard runtime as "deferred to CI" — same path the rest of Phase 43 has used (43-08, 43-12, 43-13, 43-14, 43-15 all defer the same way).

    Survivor handling:
    - **Membership-failing routes from qa-sweep-keyboard.spec.ts** (counted=N reached=M where N≠M): these are real focus-trap or hidden-tabbable bugs. Phase 43 scope is to MAKE THE GATE TRUTHFUL (43-15 did that), not to fix every focus trap. Record per-route counted/reached in SUMMARY and surface as `checkpoint:decision` for follow-up plan(s).
    - **Any axe violation other than the 4 known classes** (button-name, color-contrast, scrollable-region-focusable, aria-required-children): also out of scope; record + surface.
    - **Any new touch-target violation** outside the 30 dashboard `.btn` failures from Gap-1: record file + selector + dimensions; if simple, fix in a follow-up plan; if disputed, surface as `checkpoint:decision`.

    Do NOT modify any spec, helper, or production file in this task. Do NOT add `.skip` to any test. Do NOT loosen `maxDiffPixelRatio`. Do NOT add eslint-disable comments.

  </action>
  <acceptance_criteria>
    - Phase A.1: `pnpm -C frontend lint 2>&1 | grep -c "rtl-friendly"` outputs `0`
    - Phase A.2: `pnpm -C frontend exec tsc --noEmit` exits 0 with no `error TS` matches in tail
    - Phase A.3: `pnpm -C frontend exec vite build --mode development` exits 0 with `built in` match
    - Phase A.4: `pnpm -C frontend exec vitest run frontend/src/components/list-page/__tests__/GenericListPage.test.tsx` exits 0
    - Phase B: either (a) `pnpm -C frontend test:qa-sweep` exits 0 across all 4 sweeps, OR (b) only qa-sweep-focus-outline runs (no-auth project) and shows 8/8 with a documented env-blocked deferral for the other 3 sweeps + Phase A intact
    - SUMMARY records the per-sweep result count: axe-sweep `X violations on Y routes`, responsive-sweep `X failures on Y routes`, keyboard-sweep `X membership-fails out of Y total`, focus-outline `8/8`
    - SUMMARY records any survivors (membership fails, new axe / touch-target violations) with file + selector + triage hypothesis — surfaced as checkpoint:decision but NOT fixed in this plan
    - No file modifications outside Task 1's 8 PNGs
    - `git diff HEAD -- frontend/src/ frontend/tests/e2e/qa-sweep-axe.spec.ts frontend/tests/e2e/qa-sweep-responsive.spec.ts frontend/tests/e2e/qa-sweep-keyboard.spec.ts frontend/tests/e2e/qa-sweep-focus-outline.spec.ts frontend/tests/e2e/helpers/qa-sweep.ts frontend/playwright.config.ts` returns empty (Task 2 makes zero source edits)
  </acceptance_criteria>
  <verify>
    <automated>pnpm -C frontend lint 2>&1 | grep -c "rtl-friendly" | grep -q "^0$" && pnpm -C frontend exec tsc --noEmit > /tmp/43-16-task2-tsc.log 2>&1 && ! grep -q "error TS" /tmp/43-16-task2-tsc.log && ! git diff HEAD -- frontend/src/ frontend/tests/e2e/qa-sweep-axe.spec.ts frontend/tests/e2e/qa-sweep-responsive.spec.ts frontend/tests/e2e/qa-sweep-keyboard.spec.ts frontend/tests/e2e/qa-sweep-focus-outline.spec.ts frontend/tests/e2e/helpers/qa-sweep.ts frontend/playwright.config.ts | grep -q "^[+-]"</automated>
  </verify>
  <done>Phase A static gate is green (lint clean, tsc clean, vite build clean, vitest GenericListPage clean). Phase B full sweep is either green (4/4 sweeps) or partially green (focus-outline 8/8) with the rest documented as deferred-to-ci with intact static evidence. Any survivors surface as checkpoint:decision data for follow-up, NOT as scope creep.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                   | Description                                                                                                                            |
| ---------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| Visual snapshot baseline → Playwright pixel comparison     | Stale or wrongly-regenerated baselines hide real visual regressions — both directions a risk                                           |
| Hardware / OS / browser version → snapshot pixel rendering | macOS Retina vs Linux non-Retina vs Windows produces different anti-aliasing — baseline file naming uses os/browser suffix to mitigate |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                                                                                                                    | Disposition | Mitigation Plan                                                                                                                                                                |
| ---------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-43-16-01 | Tampering              | Regenerating baselines could mask a real visual regression introduced by 43-13/14/15 (e.g. .btn 44px floor breaks card layout in some unanticipated context) | mitigate    | Step 3 stability re-run + manual visual scan of the 8 PNGs before commit; SUMMARY records pixel-by-pixel description of changes vs prior baselines.                            |
| T-43-16-02 | Tampering              | OS-specific baselines (`-darwin.png`) may not match CI Linux runners — sweep flakes in CI                                                                    | accept      | The filename suffix `chromium-darwin` already pins the baseline to macOS; CI runners producing `-linux.png` would need their own baselines (separate concern, separate phase). |
| T-43-16-03 | Repudiation            | "Deferred to CI" path on Phase B leaves one untrusted gate at phase close                                                                                    | accept      | Same path 43-08/12/13/14/15 used; static evidence chain (lint, tsc, build, vitest, focus-outline 8/8) is consistent with runtime expectations.                                 |
| T-43-16-04 | Information disclosure | None — baseline PNGs are internal QA artifacts                                                                                                               | accept      | n/a                                                                                                                                                                            |

</threat_model>

<verification>
- `ls frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/*.png | wc -l` outputs `8`
- `pnpm -C frontend exec playwright test qa-sweep-focus-outline.spec.ts` reports 8/8 pass
- `pnpm -C frontend lint 2>&1 | grep -c "rtl-friendly"` outputs `0`
- `pnpm -C frontend exec tsc --noEmit` exits 0
- `pnpm -C frontend exec vite build --mode development` exits 0
- `pnpm -C frontend exec vitest run frontend/src/components/list-page/__tests__/GenericListPage.test.tsx` exits 0
- `pnpm -C frontend test:qa-sweep` exits 0 OR is documented as env-blocked with intact static evidence
- No production code under `frontend/src/` is modified in this plan
- No spec file under `frontend/tests/e2e/` (other than the 8 PNGs) is modified
- `frontend/playwright.config.ts` is byte-unchanged
</verification>

<success_criteria>
The 8 focus-outline visual baselines reflect the post-43-13/14/15 DOM. The full `pnpm -C frontend test:qa-sweep` exits 0 across all 4 sweeps (axe / responsive / keyboard / focus-outline) on a Supabase-equipped environment, OR — if env not available locally — every static gate is green and only the runtime sweep is deferred to CI with a clean rationale (mirroring the deferral path 43-08/12/13/14/15 already used). Phase 43 close-out gates QA-02 + QA-03 are runtime-confirmed (or unambiguously deferred-with-evidence).
</success_criteria>

<output>
After completion, create `.planning/phases/43-rtl-a11y-responsive-sweep/43-16-SUMMARY.md` documenting:
- Confirmation that 43-13 / 43-14 / 43-15 SUMMARY files exist and their production-code changes are present in HEAD
- 8 regenerated PNG filenames + before/after pixel-diff descriptions (one paragraph per direction × mode)
- Step 3 stability re-run result for qa-sweep-focus-outline.spec.ts (must be 8/8)
- Phase A static gate results (lint / tsc / vite build / vitest GenericListPage — all 4 must pass)
- Phase B full qa-sweep result with per-sweep pass count OR env-blocked rationale + which sub-sweeps did run
- Per-route counted/reached numbers from qa-sweep-keyboard.spec.ts if Phase B ran
- Any survivors (membership fails, new axe / touch-target violations) listed as checkpoint:decision data for follow-up — explicitly NOT fixed in this plan
- Final Phase 43 status assertion: "All 4 UAT gaps closed at the source" + runtime confirmation status
</output>
</content>
