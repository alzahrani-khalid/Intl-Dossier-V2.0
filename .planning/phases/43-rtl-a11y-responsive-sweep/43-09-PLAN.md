---
phase: 43-rtl-a11y-responsive-sweep
plan: 09
type: execute
wave: 3
depends_on: ['43-07', '43-12']
files_modified:
  - frontend/public/locales/en/translation.json
  - frontend/public/locales/ar/translation.json
  - frontend/src/components/dossier/DossierDrawer/DrawerCtaRow.tsx
  - frontend/src/components/ui/heroui-modal.tsx
  - frontend/src/components/ui/calendar.tsx
  - frontend/src/components/ui/related-entity-carousel.tsx
  - frontend/src/components/ui/sidebar.tsx
  - frontend/src/components/ui/context-aware-fab.tsx
  - frontend/src/components/ui/mobile-action-bar.tsx
  - frontend/src/components/ui/form-wizard.tsx
  - frontend/src/pages/Dashboard/widgets/VipVisits.tsx
  - frontend/src/pages/Dashboard/widgets/OverdueCommitments.tsx
autonomous: true
requirements: [QA-02]
tags: [qa, gate, remediation, axe, button-name, aria-label]
gap_closure: true
user_setup: []

must_haves:
  truths:
    - 'Every HeroUI Button without text children carries an aria-label that resolves through i18next'
    - 'qa-sweep-axe.spec.ts button-name violations on the v6.0 surface drop to 0'
    - 'No new translation namespace introduced — all aria-labels resolve through translation.json common.actions'
    - 'Screen readers announce every icon-only HeroUI Button by its localized name in both EN and AR'
  artifacts:
    - path: 'frontend/public/locales/en/translation.json'
      provides: 'common.actions.* keys for icon-only Button labels'
      contains: '"actions"'
    - path: 'frontend/public/locales/ar/translation.json'
      provides: 'Arabic aria-labels parity'
      contains: '"actions"'
  key_links:
    - from: 'qa-sweep-axe.spec.ts'
      to: 'every HeroUI Button has accessible name'
      via: 'axe-core button-name rule'
      pattern: 'button-name'
    - from: 'icon-only HeroUI Button instances'
      to: 'i18next t("common.actions.<verb>")'
      via: 'aria-label prop on Button'
      pattern: 'aria-label=\\{t\\('
---

<objective>
Add `aria-label={t('common.actions.<verb>')}` to every icon-only HeroUI `Button` on the v6.0 surface. Use the existing top-level `common` namespace in `translation.json` — no new translation namespace file — staying in line with Karpathy "Surgical Changes."

Purpose: Closes 43-VERIFICATION.md Class A — the dominant axe-critical violation across the v6.0 surface. Per @heroui/react v3, the `<Button>` component does NOT auto-generate an accessible name from icon children; without explicit `aria-label`, axe flags `button-name` critical.

Output: ~10–15 component files modified; ~9 new translation keys under `common.actions` (EN + AR parity); qa-sweep-axe.spec.ts `button-name` violations drop to 0 on the v6.0 surface.

Wave dependency: This plan runs in WAVE 3 — AFTER 43-12 has merged the auth-storageState + main-scoped query changes. Without 43-12, the qa-sweep-axe run in Task 5 would be flooded by login-form Class D bleed-through and could not truthfully verify Class A remediation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-rtl-a11y-responsive-sweep/43-CONTEXT.md
@.planning/phases/43-rtl-a11y-responsive-sweep/43-VERIFICATION.md
@.planning/phases/43-rtl-a11y-responsive-sweep/43-07-SUMMARY.md
@.planning/phases/43-rtl-a11y-responsive-sweep/43-12-SUMMARY.md

@frontend/design-system/inteldossier_handoff_design/README.md
@frontend/public/locales/en/translation.json
@frontend/public/locales/ar/translation.json
@frontend/src/components/dossier/DossierDrawer/DrawerCtaRow.tsx
@frontend/src/components/ui/heroui-modal.tsx
@frontend/tests/e2e/qa-sweep-axe.spec.ts

<interfaces>
<!-- Translation keys this plan introduces. -->

EN (frontend/public/locales/en/translation.json) — add inside top-level "common" object:

```json
"actions": {
  "previous": "Previous",
  "next": "Next",
  "expand": "Expand",
  "collapse": "Collapse",
  "openMenu": "Open menu",
  "closeDialog": "Close dialog",
  "toggleSection": "Toggle section",
  "viewMore": "View more",
  "remove": "Remove"
}
```

AR (frontend/public/locales/ar/translation.json) — same shape with Arabic values:

```json
"actions": {
  "previous": "السابق",
  "next": "التالي",
  "expand": "توسيع",
  "collapse": "طي",
  "openMenu": "فتح القائمة",
  "closeDialog": "إغلاق الحوار",
  "toggleSection": "تبديل القسم",
  "viewMore": "عرض المزيد",
  "remove": "إزالة"
}
```

Consumer pattern at every Button call site:

```tsx
import { useTranslation } from 'react-i18next'
const { t } = useTranslation()
<Button aria-label={t('common.actions.previous')} {...rest}><Icon/></Button>
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit every icon-only HeroUI Button on the v6.0 surface and produce a fix list</name>
  <files>(no source changes — produces /tmp/43-09-button-audit.txt)</files>
  <read_first>
    - .planning/phases/43-rtl-a11y-responsive-sweep/43-VERIFICATION.md (Class A subsection)
    - frontend/src/components/ui/heroui-button.tsx (the Button wrapper — confirm whether the wrapper auto-injects aria-label; if it does, this plan is shorter)
    - frontend/tests/e2e/qa-sweep-axe.spec.ts (the axe gate)
  </read_first>
  <action>
    Run the following exact bash pipeline. It produces /tmp/43-09-button-audit.txt as a `path:linenumber` list of icon-only HeroUI `<Button>` call sites that lack an `aria-label` and lack visible text children. The pipeline is fully runnable — no prose steps.

    ```bash
    set -euo pipefail
    > /tmp/43-09-button-audit.txt
    rg -n --no-heading "<Button(\s|$|>)" frontend/src/components frontend/src/pages -g "*.tsx" \
      | grep -v "\.test\." \
      | grep -v "/admin/" \
      | grep -v "/ai-" \
      | awk -F: '{print $1":"$2}' \
      | while IFS=: read -r f l; do
          # Read 8 lines starting at the <Button line; check for aria-label
          ctx=$(awk -v line="$l" 'NR>=line && NR<=line+8' "$f")
          hasLabel=$(printf '%s\n' "$ctx" | grep -c 'aria-label' || true)
          # Check next 5 lines for visible text children between > and </Button>
          hasTextChild=$(printf '%s\n' "$ctx" | head -6 | grep -cE '>[A-Za-z؀-ۿ][^<{]+<' || true)
          # Check for translation-call children e.g. {t('foo')} which provide accessible name
          hasTextCall=$(printf '%s\n' "$ctx" | head -6 | grep -cE '\{t\(' || true)
          if [ "$hasLabel" = "0" ] && [ "$hasTextChild" = "0" ] && [ "$hasTextCall" = "0" ]; then
            echo "$f:$l" >> /tmp/43-09-button-audit.txt
          fi
        done
    # Cap at 50 entries to avoid mass-edit risk
    if [ "$(wc -l < /tmp/43-09-button-audit.txt)" -gt 50 ]; then
      echo "AUDIT-OVERFLOW: more than 50 icon-only buttons found — surface as checkpoint:decision" >&2
      exit 1
    fi
    ```

    Cap the audit at ≤50 sites. If >50 are found, the script exits non-zero — STOP and surface a checkpoint:decision in SUMMARY (mass-edit risk per Plan 43-07's strict scope cap).

    DO NOT auto-fix in this task — Task 1 is pure discovery. The executor reviews the audit and assigns the right `common.actions.<verb>` key to each site in Tasks 3–4.

  </action>
  <acceptance_criteria>
    - /tmp/43-09-button-audit.txt exists and is non-empty
    - Each line is in `path:linenumber` format
    - Line count ≤ 50
    - The audit MUST include matches for: DrawerCtaRow.tsx, heroui-modal.tsx, calendar.tsx, related-entity-carousel.tsx (sanity check against frontmatter — if any of these 4 are missing, the audit pattern is wrong and must be re-tuned)
  </acceptance_criteria>
  <verify>
    <automated>test -s /tmp/43-09-button-audit.txt && test "$(wc -l < /tmp/43-09-button-audit.txt)" -le 50 && for f in DrawerCtaRow.tsx heroui-modal.tsx calendar.tsx related-entity-carousel.tsx; do grep -q "$f" /tmp/43-09-button-audit.txt || { echo "Audit missed $f"; exit 1; }; done</automated>
  </verify>
  <done>Audit file lists every icon-only HeroUI Button without aria-label on the v6.0 surface, capped at 50, and includes the four canary files.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add common.actions translation keys to en/ and ar/ translation.json</name>
  <files>frontend/public/locales/en/translation.json, frontend/public/locales/ar/translation.json</files>
  <read_first>
    - frontend/public/locales/en/translation.json (lines 1-30 — locate the top-level "common" object)
    - frontend/public/locales/ar/translation.json (mirror EN structure)
  </read_first>
  <action>
    Add a new `"actions"` sub-key INSIDE the existing top-level `"common"` object in BOTH locale files. Place alphabetically (e.g. before or after `"clearAll"`).

    EN block to insert verbatim (with comma):
    ```json
    "actions": {
      "previous": "Previous",
      "next": "Next",
      "expand": "Expand",
      "collapse": "Collapse",
      "openMenu": "Open menu",
      "closeDialog": "Close dialog",
      "toggleSection": "Toggle section",
      "viewMore": "View more",
      "remove": "Remove"
    },
    ```

    AR block to insert verbatim:
    ```json
    "actions": {
      "previous": "السابق",
      "next": "التالي",
      "expand": "توسيع",
      "collapse": "طي",
      "openMenu": "فتح القائمة",
      "closeDialog": "إغلاق الحوار",
      "toggleSection": "تبديل القسم",
      "viewMore": "عرض المزيد",
      "remove": "إزالة"
    },
    ```

    DO NOT touch the existing `actions` blocks at deeper paths in translation.json (the file already has feature-scoped `actions` keys at lines ~305, ~667, ~1711 — those are unrelated).

    DO NOT introduce a new namespace file (no `actions.json` in either locale dir).

    Validate JSON parses after edit.

  </action>
  <acceptance_criteria>
    - `node -e "const d=require('./frontend/public/locales/en/translation.json'); if (!d.common.actions || !d.common.actions.previous) process.exit(1)"` exits 0
    - `node -e "const d=require('./frontend/public/locales/ar/translation.json'); if (!d.common.actions || !d.common.actions.previous) process.exit(1)"` exits 0
    - EN/AR key parity: `node -e "const e=require('./frontend/public/locales/en/translation.json').common.actions; const a=require('./frontend/public/locales/ar/translation.json').common.actions; if (Object.keys(e).sort().join(',') !== Object.keys(a).sort().join(',')) process.exit(1)"` exits 0
    - No new namespace file: `! test -f frontend/public/locales/en/actions.json && ! test -f frontend/public/locales/ar/actions.json`
  </acceptance_criteria>
  <verify>
    <automated>node -e "const e=require('./frontend/public/locales/en/translation.json'); const a=require('./frontend/public/locales/ar/translation.json'); if (!e.common.actions.previous || !a.common.actions.previous) process.exit(1); if (Object.keys(e.common.actions).sort().join(',') !== Object.keys(a.common.actions).sort().join(',')) process.exit(1)"</automated>
  </verify>
  <done>Both locale files have a top-level common.actions sub-tree with 9 parallel keys; JSON parses; no new namespace files exist.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Apply aria-label to Buttons in design-system primitives (heroui-modal, calendar, sidebar, related-entity-carousel, form-wizard, mobile-action-bar, context-aware-fab)</name>
  <files>frontend/src/components/ui/heroui-modal.tsx, frontend/src/components/ui/calendar.tsx, frontend/src/components/ui/sidebar.tsx, frontend/src/components/ui/related-entity-carousel.tsx, frontend/src/components/ui/form-wizard.tsx, frontend/src/components/ui/mobile-action-bar.tsx, frontend/src/components/ui/context-aware-fab.tsx</files>
  <read_first>
    - /tmp/43-09-button-audit.txt (the Task 1 output)
    - frontend/src/components/ui/heroui-modal.tsx (lines ~234, ~241 — the close + dialog Buttons)
    - frontend/src/components/ui/calendar.tsx (line ~151 — the prev/next nav Buttons)
    - frontend/src/components/ui/sidebar.tsx (line ~277 — sidebar trigger Button)
    - frontend/src/components/ui/related-entity-carousel.tsx (line ~122 — likely a "view more"/"next slide" Button)
    - frontend/src/components/ui/form-wizard.tsx (lines ~336, ~347, ~367, ~384, ~398 — wizard nav)
    - frontend/src/components/ui/mobile-action-bar.tsx (line ~125 — primary CTA bar)
    - frontend/src/components/ui/context-aware-fab.tsx (lines ~268, ~499 — floating action button)
  </read_first>
  <action>
    For each call site listed in /tmp/43-09-button-audit.txt that maps to one of the 7 files in this task's `files`:

    1. Confirm the Button is icon-only (children is a single `<Icon ...>` JSX, not text).
    2. Determine the closest verb match from `common.actions.{previous|next|expand|collapse|openMenu|closeDialog|toggleSection|viewMore|remove}`. Pick by intent:
       - heroui-modal close button → `closeDialog`
       - calendar prev/next → `previous` / `next`
       - sidebar trigger toggle → `openMenu`
       - related-entity-carousel arrows → `previous` / `next` (or `viewMore` if it's a "see more" affordance)
       - form-wizard step nav → `previous` / `next`
       - mobile-action-bar dismiss → `closeDialog`
       - context-aware-fab → use the closest existing translation key for the FAB's PRIMARY action; if none fits, add a new sub-key in this PR (document in SUMMARY)
    3. Add `aria-label={t('common.actions.<verb>')}` to the `<Button>` JSX. Ensure `useTranslation` is imported in the file (most already have it).
    4. If the Button is wrapped in a HeroUI compound (e.g. `<Button slot="close">`), the aria-label still goes on the Button itself.

    Do NOT change any non-aria props. Do NOT change icon children. Do NOT introduce a new aria-label key not in the `common.actions` set added by Task 2 (if a verb is genuinely missing, ADD it to BOTH locale files in the same commit — keep parity).

    Logical properties only. No raw hex. No card shadows.

  </action>
  <acceptance_criteria>
    - `grep -c "aria-label={t('common.actions" frontend/src/components/ui/heroui-modal.tsx frontend/src/components/ui/calendar.tsx frontend/src/components/ui/sidebar.tsx frontend/src/components/ui/related-entity-carousel.tsx frontend/src/components/ui/form-wizard.tsx frontend/src/components/ui/mobile-action-bar.tsx frontend/src/components/ui/context-aware-fab.tsx 2>/dev/null | awk -F: '{sum+=$2} END {print sum}'` ≥ 7 (one per file)
    - All 7 files import `useTranslation` from `react-i18next` (`grep -l "useTranslation" <file>` returns the file for each)
    - `pnpm -C frontend lint <each touched file>` introduces 0 new errors
    - No raw hex colors introduced
    - No new `eslint-disable` comments
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -rc "aria-label={t('common.actions" frontend/src/components/ui/heroui-modal.tsx frontend/src/components/ui/calendar.tsx frontend/src/components/ui/sidebar.tsx frontend/src/components/ui/related-entity-carousel.tsx frontend/src/components/ui/form-wizard.tsx frontend/src/components/ui/mobile-action-bar.tsx frontend/src/components/ui/context-aware-fab.tsx 2>/dev/null | awk -F: '{sum+=$2} END {print sum}')" -ge 7</automated>
  </verify>
  <done>Every icon-only Button in the 7 design-system primitive files carries `aria-label={t('common.actions.<verb>')}`; `useTranslation` imported; lint clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Apply aria-label to Buttons in feature-area files (DrawerCtaRow + dashboard widgets + remaining audit hits)</name>
  <files>frontend/src/components/dossier/DossierDrawer/DrawerCtaRow.tsx, frontend/src/pages/Dashboard/widgets/VipVisits.tsx, frontend/src/pages/Dashboard/widgets/OverdueCommitments.tsx</files>
  <read_first>
    - /tmp/43-09-button-audit.txt (full list)
    - frontend/src/components/dossier/DossierDrawer/DrawerCtaRow.tsx (full file — every CTA button)
    - frontend/src/pages/Dashboard/widgets/VipVisits.tsx (the ArrowRight 43-07 already migrated; if surrounded by a Button, that Button needs aria-label)
    - frontend/src/pages/Dashboard/widgets/OverdueCommitments.tsx (the ArrowUpRight 43-07 migrated; same)
    - frontend/public/locales/en/translation.json common.actions block (added in Task 2)
  </read_first>
  <action>
    For each remaining `path:linenumber` in /tmp/43-09-button-audit.txt that maps to a file in this task's `files`:

    1. Read the file at the cited line. Confirm the Button is icon-only.
    2. Add `aria-label={t('common.actions.<verb>')}` using the closest verb from the Task-2 set.
    3. For DrawerCtaRow CTA buttons: most are NOT icon-only (they typically have CTA text). Skip those — only fix true icon-only Buttons.
    4. For dashboard widget arrow buttons (VipVisits ArrowRight, OverdueCommitments ArrowUpRight): if these arrows are inside a `<Link>` or row, the row may already provide accessible name via its content; check via reading the JSX. If the arrow IS the entire interactive (e.g. a HeroUI `<Button isIconOnly>`), add `aria-label={t('common.actions.viewMore')}`.

    Any audit entry that does NOT map to one of these 3 files: DEFER to Task 5 (sweep verification) — it will catch any remaining via axe.

    Do NOT change any non-aria props. Logical properties only.

  </action>
  <acceptance_criteria>
    - At least 1 `aria-label={t('common.actions.` occurrence in each of the 3 files (`for f in <files>; do grep -q "aria-label={t('common.actions" "$f" || exit 1; done`)
    - `pnpm -C frontend lint <each touched file>` introduces 0 new errors
    - Pre-existing `.icon-flip` classes from 43-07 still present on VipVisits and OverdueCommitments (regression guard)
  </acceptance_criteria>
  <verify>
    <automated>for f in frontend/src/components/dossier/DossierDrawer/DrawerCtaRow.tsx frontend/src/pages/Dashboard/widgets/VipVisits.tsx frontend/src/pages/Dashboard/widgets/OverdueCommitments.tsx; do grep -q "aria-label={t('common.actions" "$f" || exit 1; done && grep -q "icon-flip" frontend/src/pages/Dashboard/widgets/VipVisits.tsx && grep -q "icon-flip" frontend/src/pages/Dashboard/widgets/OverdueCommitments.tsx</automated>
  </verify>
  <done>DrawerCtaRow icon-only buttons + VipVisits/OverdueCommitments arrow buttons carry `aria-label`. 43-07 `.icon-flip` regression guard passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Run qa-sweep-axe.spec.ts and confirm button-name violations on v6.0 surface drop to 0</name>
  <files>(verification only — fix any survivors in the file they appear)</files>
  <read_first>
    - frontend/tests/e2e/qa-sweep-axe.spec.ts (the axe gate — already scoped to `main` and using `waitForRouteReady` per 43-12)
    - frontend/tests/e2e/helpers/qa-sweep.ts (runAxe filter — currently `serious || critical`)
    - .planning/phases/43-rtl-a11y-responsive-sweep/43-VERIFICATION.md Class A
    - .planning/phases/43-rtl-a11y-responsive-sweep/43-12-SUMMARY.md (confirms storageState + main-scope landed)
  </read_first>
  <action>
    Run `pnpm -C frontend test:qa-sweep -- qa-sweep-axe.spec.ts --reporter=list 2>&1 | tee /tmp/43-09-axe.log`.

    Because 43-12 has already landed (this plan is in Wave 3), every failure should now point at a real v6.0-surface element — login-form bleed-through (Class D) is no longer in the result set. The axe spec's exit code is the source of truth:
    - `pnpm -C frontend test:qa-sweep -- qa-sweep-axe.spec.ts` exits non-zero if ANY axe violation remains on the v6.0 routes covered by V6_ROUTES.
    - Filter the captured log for `button-name` mentions to identify the offending sites; do NOT rely on a route-name regex (Playwright's default reporter does not include the route path in the violation summary).

    For each `button-name` violation identified in the log:
    1. Identify the source file via the failure's `target` selector + `rg`.
    2. If the file is in this plan's `files_modified` — fix in place using a `common.actions.<verb>` key (Task 2 vocabulary).
    3. If the file is NOT in this plan's `files_modified` — append to `files_modified` in the SUMMARY and fix it (gap-closure plans may discover additional sites). Cap at +5 additional files; beyond that, STOP and surface as checkpoint:decision (mass-edit risk).
    4. Re-run the spec until the spec exits 0 (or all remaining failures are non-`button-name` rules).

    Stop conditions:
    - If a `button-name` violation persists after 3 fix-iterations — STOP and surface as checkpoint:decision in SUMMARY.

    Do NOT modify the qa-sweep-axe.spec.ts itself — fix the source code. Do NOT modify helpers/qa-sweep.ts thresholds. Do NOT add `eslint-disable` or `test.skip`.

  </action>
  <acceptance_criteria>
    - `pnpm -C frontend test:qa-sweep -- qa-sweep-axe.spec.ts` exits 0 on the post-43-12 baseline (no `button-name` violations remain), OR every survivor is escalated to checkpoint:decision in SUMMARY
    - `frontend/tests/e2e/qa-sweep-axe.spec.ts` is unchanged vs HEAD (`git diff` returns empty for that path)
    - `frontend/tests/e2e/helpers/qa-sweep.ts` is unchanged vs HEAD (no thresholds altered)
    - No new `eslint-disable` for `jsx-a11y/*` rules introduced (`! git diff HEAD | grep -E "^\+.*eslint-disable.*jsx-a11y"`)
  </acceptance_criteria>
  <verify>
    <automated>! git diff HEAD -- frontend/tests/e2e/qa-sweep-axe.spec.ts | grep -q "^[+-]" && ! git diff HEAD -- frontend/tests/e2e/helpers/qa-sweep.ts | grep -q "^[+-]" && pnpm -C frontend test:qa-sweep -- qa-sweep-axe.spec.ts --reporter=list 2>&1 | tee /tmp/43-09-final.log; ! grep -q "button-name" /tmp/43-09-final.log</automated>
  </verify>
  <done>qa-sweep-axe.spec.ts reports zero `button-name` violations on the v6.0 authenticated surface; tests/helpers unchanged; no eslint-disables added.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                            |
| ------------------------------ | -------------------------------------------------------------------------------------- |
| screen reader → DOM aria-label | aria-label values are user-facing; bad i18n could leak debug text or untranslated keys |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                       | Disposition | Mitigation Plan                                                                                 |
| ---------- | ---------------------- | --------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------- |
| T-43-09-01 | Information disclosure | aria-label leaking translation key when AR locale missing entry | mitigate    | Task 2 acceptance enforces EN/AR key parity via `Object.keys().sort().join(',')` equality check |
| T-43-09-02 | Tampering              | Translation file edits break JSON parsing in production         | mitigate    | Task 2 acceptance runs `node -e "require(...)"` which throws on parse error                     |
| T-43-09-03 | Repudiation            | None — pure UI metadata; no audit-relevant action               | accept      | n/a                                                                                             |

</threat_model>

<verification>
- Both en/ and ar/ translation.json have `common.actions` sub-tree with 9 parallel keys
- ≥7 design-system primitives carry `aria-label={t('common.actions.<verb>')}`
- ≥3 feature files (DrawerCtaRow, VipVisits, OverdueCommitments) carry the same
- 43-07 `.icon-flip` migrations on VipVisits + OverdueCommitments preserved
- `pnpm -C frontend test:qa-sweep -- qa-sweep-axe.spec.ts` shows 0 `button-name` violations
- No new namespace files introduced
- No `eslint-disable` for `jsx-a11y` introduced
</verification>

<success_criteria>
qa-sweep-axe.spec.ts `button-name` violations on the v6.0 authenticated surface drop to 0. 43-12 (Wave 2) eliminated Class D login-route bleed-through; 43-09 (Wave 3) closes Class A. Translation parity (EN/AR) preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/43-rtl-a11y-responsive-sweep/43-09-SUMMARY.md` documenting:
- Audit count from /tmp/43-09-button-audit.txt and how many were fixed
- Final list of files modified (may exceed the frontmatter list — Task 5 may discover +5 more)
- Translation keys added (delta from baseline common.actions block — should be exactly 9, EN+AR parity)
- qa-sweep-axe `button-name` pass/fail delta vs the post-43-12 baseline
- Any survivors escalated to checkpoint:decision
</output>
</content>
</invoke>
