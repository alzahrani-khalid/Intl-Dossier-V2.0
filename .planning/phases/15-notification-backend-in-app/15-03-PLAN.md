---
phase: 15-notification-backend-in-app
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - frontend/src/hooks/useNotificationCenter.ts
  - frontend/src/locales/en/notification-center.json
  - frontend/src/locales/ar/notification-center.json
autonomous: false
requirements: [NOTIF-01, NOTIF-07]
must_haves:
  truths:
    - "User sees bell icon with unread count in the header"
    - "Clicking bell icon opens notification center panel with category tabs"
    - "User can mark individual notifications as read"
    - "User can bulk mark-all-read"
    - "Sonner toast fires when a new notification arrives via Realtime"
    - "Toast displays correct icon and color per notification category"
  artifacts:
    - path: "frontend/src/hooks/useNotificationCenter.ts"
      provides: "Toast dispatch on Realtime notification arrival"
      contains: "toast"
    - path: "frontend/src/locales/en/notification-center.json"
      provides: "English i18n strings for toast messages"
      contains: "toast.taskAssigned"
    - path: "frontend/src/locales/ar/notification-center.json"
      provides: "Arabic i18n strings for toast messages"
      contains: "toast.taskAssigned"
  key_links:
    - from: "frontend/src/hooks/useNotificationCenter.ts"
      to: "Supabase Realtime"
      via: "subscription on notifications table INSERT"
      pattern: "supabase.*channel.*notifications"
    - from: "frontend/src/hooks/useNotificationCenter.ts"
      to: "sonner toast"
      via: "toast() call on new notification"
      pattern: "toast\\("
---

<objective>
Verify the existing notification UI works end-to-end with real backend-dispatched notifications, wire Sonner toast for real-time arrivals, and add bilingual i18n strings.

Purpose: Deliver NOTIF-01 (bell icon + panel) and NOTIF-07 (mark-as-read) by verifying existing components work with real data, and add toast notifications for live arrivals per the UI-SPEC.

Output: Toast notifications fire on Realtime events with correct category styling, bilingual i18n strings for all notification copy, human verification of full flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-notification-backend-in-app/15-CONTEXT.md
@.planning/phases/15-notification-backend-in-app/15-UI-SPEC.md
@.planning/phases/15-notification-backend-in-app/15-01-SUMMARY.md

<interfaces>
<!-- Existing notification UI components (DO NOT rebuild per D-02) -->
From frontend/src/components/notifications/NotificationPanel.tsx:
- Bell icon trigger, popover panel, category tabs, mark-all-read button
- Already renders in Header.tsx

From frontend/src/hooks/useNotificationCenter.ts:
- Paginated queries, Realtime subscription, toast-on-arrival
- This hook already has a Realtime subscription -- verify it fires toasts correctly

From frontend/src/components/settings/sections/NotificationsSettingsSection.tsx:
- Per-category preference toggles (in-app, email per category)

Toast system:
- Sonner already installed and configured
- Import from 'sonner' or project toast wrapper
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire toast notifications for Realtime arrivals and add i18n strings</name>
  <files>
    frontend/src/hooks/useNotificationCenter.ts
    frontend/src/locales/en/notification-center.json
    frontend/src/locales/ar/notification-center.json
  </files>
  <read_first>
    frontend/src/hooks/useNotificationCenter.ts
    frontend/src/components/notifications/NotificationPanel.tsx
    frontend/src/components/ui/toast.tsx
    frontend/src/locales/en/notification-center.json
    frontend/src/locales/ar/notification-center.json
    frontend/src/domains/operations-hub/hooks/useAttentionRealtime.ts
  </read_first>
  <action>
    1. Read `frontend/src/hooks/useNotificationCenter.ts` to understand the existing Realtime subscription. Check if it already fires a toast on new notification INSERT events. If it does:
       - Verify the toast uses the correct Sonner variant per category (per UI-SPEC toast variants table):
         - `assignments` category -> `toast()` default
         - `deadlines` with type `deadline_approaching` -> `toast.warning()`
         - `deadlines` with type `deadline_overdue` -> `toast.error()`
         - `workflow` category -> `toast()` default
       - If the existing toast logic is simpler (e.g., just `toast(title)`), enhance it to use category-aware variants
       
    2. If the Realtime subscription does NOT fire toasts, add toast dispatch in the Realtime callback:
       ```typescript
       // Inside the Realtime INSERT handler:
       const notification = payload.new
       const category = notification.category
       const type = notification.type
       
       if (type === 'deadline_overdue') {
         toast.error(notification.title, { description: notification.message })
       } else if (type === 'deadline_approaching') {
         toast.warning(notification.title, { description: notification.message })
       } else {
         toast(notification.title, { description: notification.message })
       }
       ```

    3. Ensure toast has an onClick handler that navigates to `notification.action_url` if present:
       ```typescript
       toast(notification.title, {
         description: notification.message,
         action: notification.action_url ? {
           label: t('toast.view'),
           onClick: () => navigate({ to: notification.action_url }),
         } : undefined,
       })
       ```

    4. Add/update i18n strings in `frontend/src/locales/en/notification-center.json`:
       ```json
       {
         "panel": { "title": "Notifications" },
         "actions": {
           "markAllRead": "Mark all as read",
           "deleteConfirm": "Delete this notification?"
         },
         "empty": {
           "title": "No notifications yet",
           "description": "When you receive assignments, deadline alerts, or workflow updates, they will appear here."
         },
         "error": {
           "loadFailed": "Could not load notifications. Pull down to retry."
         },
         "toast": {
           "taskAssigned": "New task assigned: {{taskTitle}}",
           "deadlineApproaching": "Deadline approaching: {{taskTitle}}",
           "deadlineOverdue": "Overdue: {{taskTitle}}",
           "lifecycleChange": "Stage changed to {{stage}}",
           "view": "View"
         }
       }
       ```
       If the file already exists with some keys, MERGE the new keys -- do not overwrite existing keys that may be in use.

    5. Add/update i18n strings in `frontend/src/locales/ar/notification-center.json`:
       ```json
       {
         "panel": { "title": "الاشعارات" },
         "actions": {
           "markAllRead": "تعيين الكل كمقروء",
           "deleteConfirm": "حذف هذا الاشعار؟"
         },
         "empty": {
           "title": "لا توجد اشعارات بعد",
           "description": "عندما تتلقى مهام او تنبيهات مواعيد او تحديثات سير العمل، ستظهر هنا."
         },
         "error": {
           "loadFailed": "تعذر تحميل الاشعارات. اسحب للاسفل للمحاولة مرة اخرى."
         },
         "toast": {
           "taskAssigned": "مهمة جديدة: {{taskTitle}}",
           "deadlineApproaching": "اقتراب الموعد النهائي: {{taskTitle}}",
           "deadlineOverdue": "متاخر: {{taskTitle}}",
           "lifecycleChange": "تغيرت المرحلة الى {{stage}}",
           "view": "عرض"
         }
       }
       ```
  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/Coding\ Space/Intl-Dossier-V2.0 && grep -q "toast" frontend/src/hooks/useNotificationCenter.ts && grep -q "taskAssigned" frontend/src/locales/en/notification-center.json && grep -q "taskAssigned" frontend/src/locales/ar/notification-center.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/hooks/useNotificationCenter.ts` contains `toast.error` or `toast.warning` (category-aware toasts)
    - `frontend/src/hooks/useNotificationCenter.ts` contains Realtime subscription that handles INSERT events
    - `frontend/src/locales/en/notification-center.json` contains `taskAssigned` key
    - `frontend/src/locales/ar/notification-center.json` contains Arabic toast strings
    - Both locale files contain all keys from UI-SPEC copywriting contract (panel.title, actions.markAllRead, empty.title, empty.description, error.loadFailed, toast.*)
  </acceptance_criteria>
  <done>Toast notifications fire with correct Sonner variant per category. All i18n strings present in both English and Arabic. Existing Realtime subscription enhanced with category-aware toasts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full notification flow end-to-end</name>
  <files>none</files>
  <action>
    Human verification of the complete notification system across Plans 01, 02, and 03. No code changes -- this is a manual acceptance gate.
  </action>
  <what-built>
    Complete notification system: BullMQ async dispatch (Plan 01), three trigger categories (Plan 02), toast notifications + existing UI verification (Plan 03 Task 1).
  </what-built>
  <how-to-verify>
    1. Start the dev environment: `pnpm dev`
    2. Open the application in browser

    **NOTIF-01 -- Bell icon + panel:**
    3. Verify the bell icon appears in the header with unread count badge
    4. Click the bell icon -- notification center panel should open
    5. Verify category tabs are visible (All, Assignments, Deadlines, Workflow)

    **NOTIF-02 -- Task assignment notification:**
    6. Create a new task and assign it to yourself (or another test user)
    7. Within ~5 seconds, a Sonner toast should appear: "New task assigned: [task title]"
    8. The notification should appear in the notification panel under "Assignments" tab

    **NOTIF-07 -- Mark as read:**
    9. Click on a notification in the panel -- it should mark as read (unread dot disappears)
    10. Click "Mark all as read" -- all notifications should lose their unread indicator
    11. Bell icon badge count should update to reflect read state

    **NOTIF-08 -- Async dispatch verification:**
    12. Check backend terminal logs for: "Notification worker ready" and "Notification queue initialized"
    13. After creating a task, logs should show the job being processed (not synchronous)

    **RTL verification (if Arabic language available):**
    14. Switch to Arabic language
    15. Verify notification panel text is RTL-aligned
    16. Verify toast appears at top-start position
  </how-to-verify>
  <verify>Human approves all verification steps above</verify>
  <done>All NOTIF-01, NOTIF-02, NOTIF-07, NOTIF-08 verification steps pass. User types "approved".</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase Realtime -> Frontend | Realtime only delivers notifications matching RLS (user_id = auth.uid()) |
| Frontend -> Notification API | Mark-as-read uses authenticated API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | Info Disclosure | Supabase Realtime subscription | mitigate | RLS policy on notifications table ensures user only receives their own notifications via Realtime channel; user_id = auth.uid() filter |
| T-15-11 | Spoofing | mark-as-read API | mitigate | Existing RPC mark_category_as_read uses auth.uid() internally; user cannot mark another user's notifications |
| T-15-12 | Tampering | Toast notification content | accept | Toast displays server-provided title and message; React escapes all rendered content by default preventing injection |
</threat_model>

<verification>
1. Bell icon visible in header with badge count
2. Notification panel opens with category tabs
3. Task assignment triggers toast notification
4. Mark-as-read works individually and bulk
5. i18n strings present in both locale files
6. Backend logs show worker ready and queue initialized
</verification>

<success_criteria>
- Existing notification UI (bell, panel, tabs, mark-as-read) works with real backend-dispatched notifications
- Sonner toasts fire on Realtime arrivals with category-appropriate styling
- All i18n strings present in English and Arabic
- Human verification confirms full end-to-end flow
</success_criteria>

<output>
After completion, create `.planning/phases/15-notification-backend-in-app/15-03-SUMMARY.md`
</output>
