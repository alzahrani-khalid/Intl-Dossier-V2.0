---
phase: 83-token-debt-consolidation
plan: 05
type: execute
wave: 2
depends_on: [83-01]
files_modified:
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/components/ui/tooltip.tsx
  - frontend/src/components/ui/file-upload.tsx
  - frontend/src/components/ui/context-aware-fab.tsx
  - frontend/src/components/ui/sidebar.tsx
  - frontend/src/components/ui/expandable-card.tsx
  - frontend/src/components/ui/pull-to-refresh-indicator.tsx
  - frontend/src/components/guided-tours/TourTrigger.tsx
  - frontend/src/components/milestone-planning/MilestonePlannerEmptyState.tsx
  - frontend/src/router/index.tsx
  - frontend/src/components/type-specific-fields/TypeSpecificFields.tsx
  - frontend/src/pages/dossiers/DossierListPage.tsx
  - frontend/src/pages/** (banned-shadow strip, long tail)
  - frontend/src/components/** (banned-shadow strip long tail, EXCLUDING dirs owned by 83-03/83-04/83-06)
  - frontend/src/pages/WorkBoard/board.css
  - frontend/src/pages/Dashboard/widgets/dashboard.css
  - frontend/src/styles/calendar.css
  - frontend/src/components/tweaks/tweaks-drawer.css
  - frontend/src/styles/list-pages.css (line ~294 border-radius ONLY — surgical)
autonomous: true
requirements: [DEBT-02, DEBT-03, DEBT-04, DEBT-05]
must_haves:
  truths:
    - 'Satisfies D-83-03 (long tail): zero shadow-(sm|md|xl|2xl) matches remain anywhere in frontend/src outside index.css definition sites and the 83-06-owned modern-nav/copilot files; the context-aware-fab FABs keep a single static shadow-lg; borders unchanged'
    - "Satisfies D-83-04: zero numeric arbitrary radii (rounded-[Npx]) and zero on-scale border-radius px literals (6/8/12px) remain — swapped pixel-identically (rounded-[2px]→rounded-xs, 6px→var(--radius-sm), 8px→var(--radius), 12px→var(--radius-lg), 4px→calc(var(--radius)*0.5)); rounded-[var(--radius-*)] forms are canonical and NOT touched (RESEARCH Pitfall 1: 'simplifying' them to named utilities is a 6px→4px pixel change)"
    - "Satisfies D-83-02 (expandable-card): the live aceternity expandable-card's 8 Tailwind literals map to @theme utilities (bg-surface-4 modal card, text-ink, text-ink-mute, bg-accent CTA, hover:bg-surface-raised)"
    - 'Satisfies D-83-05 (long tail): TourTrigger, MilestonePlannerEmptyState, and pull-to-refresh-indicator decorative gradients flattened to flat tokens; micro radii (1-3px scrollbar/kbd) and pill radii (99/999/9999px) are KEPT (sub-scale detail and idiomatic full-radius, allowlisted by the gate)'
    - 'Satisfies D-83-09: styles/list-pages.css receives EXACTLY one edit (the ~:294 border-radius: 8px → var(--radius)); the [class~=…] compat shim block (~:976-1367) is byte-identical; no other carve-out file is touched'
  artifacts:
    - path: 'frontend/src/components/layout/Sidebar.tsx'
      provides: 'active-bar radius on the named 2px utility'
      contains: 'rounded-xs'
    - path: 'frontend/src/components/ui/expandable-card.tsx'
      provides: 'modal card on @theme utilities'
      contains: 'bg-surface-4'
  key_links:
    - from: 'frontend/src/pages/WorkBoard/board.css'
      to: 'Linear radius scale'
      via: 'border-radius: var(--radius-sm) / calc(var(--radius) * 0.5)'
      pattern: "var\\(--radius"
---

<objective>
Sweep the long tail: strip banned card shadows everywhere not owned by another slice, replace the
remaining hardcoded radii with the token scale (pixel-identical swaps), flatten the remaining
decorative gradients, and map expandable-card's Tailwind literals to @theme utilities.

Purpose: DEBT-03/04's bulk plus DEBT-02/05 remainders. Depends on 83-01 because several dead files
(ui/chart.tsx rounded-[2px], dead aceternity gradients/literals) would otherwise fail this plan's
gates — deletion already removed them.
Output: repo-wide DEBT-03/04 grep gates at zero (excluding 83-06-owned files); gradient gate at
allowlist-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/83-token-debt-consolidation/83-RESEARCH.md  (§DEBT-03 policy + judgment list; §DEBT-04 verified HeroUI radius mechanics + swap tables + CSS px classification; §DEBT-05 verdict table; §DEBT-02 expandable-card row; Pitfalls 1, 2, 4; Assumption A2)
@.planning/phases/83-token-debt-consolidation/83-CONTEXT.md   (D-83-02, D-83-03, D-83-04, D-83-05, D-83-09)
@frontend/DESIGN.md  (§Radius — Linear 6/8/12; §Elevation & depth)

<interfaces>
Verified radius mechanics (RESEARCH, built CSS): HeroUI redefines the rounded-* namespace
multiplicatively from --radius (8px): rounded-xs=2px, rounded-sm=4px, rounded-md=6px,
rounded-lg=8px, rounded-xl=12px. The Linear token names do NOT line up (--radius-sm 6px vs
rounded-sm 4px). Therefore:
- rounded-[var(--radius-sm)] etc. (58 sites) = canonical, pixel-faithful — DO NOT TOUCH.
- rounded-[2px] → rounded-xs (byte-identical 2px). Verify A2 first: confirm .rounded-xs compiles
  to calc(var(--radius) * .25) in the built/dev CSS; if the utility does not exist, use
  rounded-[calc(var(--radius)*0.25)] instead.
- rounded-e-[12px] belongs to NavigationShell (83-06 owns it — do not touch here).
CSS border-radius classification: on-scale 6px→var(--radius-sm), 8px→var(--radius),
12px→var(--radius-lg); half-scale 4px→calc(var(--radius) * 0.5); pills 99/999/9999px KEEP;
micro 1-3px (scrollbar thumbs, kbd, tiny indicators) KEEP; off-scale 10px (dashboard.css ~:890,
~:908) snap by selector role — card/panel surface → var(--radius-lg), control → var(--radius) —
record the choice, eyeball at the 83-07 gate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DEBT-03 long-tail shadow strip + FAB judgment</name>
  <files>frontend/src/components/ui/context-aware-fab.tsx, frontend/src/components/ui/sidebar.tsx, frontend/src/router/index.tsx, frontend/src/components/type-specific-fields/TypeSpecificFields.tsx, frontend/src/pages/dossiers/DossierListPage.tsx, plus every file matched by the exclusion-scoped grep below</files>
  <read_first>83-RESEARCH.md §DEBT-03 (policy items 1-4 — strip default, judgment list, definition sites, shadow-lg NOT banned); frontend/src/components/ui/context-aware-fab.tsx (the two FAB idioms ~:271/:502 and tooltip poppers ~:253/:536/:419); frontend/DESIGN.md §Elevation & depth</read_first>
  <action>
    Generate the strip list mechanically:
    `rg -ln '\bshadow-(sm|md|xl|2xl)\b' frontend/src -g '!index.css' -g '!**/components/analytics/**' -g '!**/components/dashboard-widgets/**' -g '!**/components/stakeholder-influence/**' -g '!**/components/report-builder/**' -g '!**/components/dossier/**' -g '!**/components/dossiers/**' -g '!**/components/relationships/**' -g '!**/components/sla-countdown/**' -g '!**/components/modern-nav/**' -g '!**/components/copilot/**' -g '!**/styles/modern-nav-tokens.css'`.
    The excluded dirs are owned by 83-03/83-04/83-06 — touching them breaks file-disjointness.
    This is a MECHANICAL, grep-list-driven sweep (~70+ files after the corrected exclusions):
    single-class deletions of the banned shadow-(sm|md|xl|2xl) tokens ONLY — no shadow-lg sites
    and no hover:shadow-* escalations are touched anywhere except the named judgment exceptions
    below, and no other classes on the same elements are modified. Save the generated strip-list
    (the rg -ln output) — it is committed as evidence in the summary.
    Default disposition for every match = DELETE the banned shadow class only (hairline border
    stays; `shadow-lg` alone is NOT banned — leave it; `hover:shadow-md` on list rows: strip the
    banned tier — hover-row shadow, when kept, uses the allowed tier).
    Judgment exceptions (RESEARCH §DEBT-03 item 2): context-aware-fab's floating FABs — the
    `shadow-md hover:shadow-lg` and `shadow-lg hover:shadow-xl` idioms each become a SINGLE static
    `shadow-lg`; its tooltip poppers are popovers → surface-3 + hairline, NO shadow → strip.
    ui/sidebar.tsx: strip all 3 (unused shadcn floating/inset variant classes + the
    data-[active=true]:shadow-sm). router/index.tsx error/pending screens are cards: strip
    shadow-xl/shadow-sm.
    Definition sites are NOT touched: index.css `--shadow-sm: none` etc. is the design system —
    KEEP; modern-nav-tokens.css and copilot-theme.css belong to 83-06.
  </action>
  <verify>
    <automated>! rg -q '\bshadow-(sm|md|xl|2xl)\b' frontend/src -g '!index.css' -g '!**/components/modern-nav/**' -g '!**/components/copilot/**' -g '!**/styles/modern-nav-tokens.css' && pnpm --dir frontend type-check</automated>
  </verify>
  <acceptance_criteria>The exclusion-scoped banned-shadow grep returns 0 across frontend/src (only index.css definitions + 83-06-owned files may still match their own grep); context-aware-fab has exactly one static shadow-lg per FAB and zero hover:shadow-* (`rg 'hover:shadow' frontend/src/components/ui/context-aware-fab.tsx` → 0); no shadow-lg-only sites were touched; type-check exit 0; lint clean on touched files; the generated strip-list (the rg -ln output) is committed as evidence in the summary.</acceptance_criteria>
  <done>Long-tail shadow discipline enforced repo-wide (minus 83-06 slice).</done>
</task>

<task type="auto">
  <name>Task 2: DEBT-04 radii — TSX rounded-[Npx] + CSS border-radius px literals</name>
  <files>frontend/src/components/layout/Sidebar.tsx, frontend/src/components/ui/tooltip.tsx, frontend/src/components/ui/file-upload.tsx, frontend/src/pages/WorkBoard/board.css, frontend/src/pages/Dashboard/widgets/dashboard.css, frontend/src/styles/calendar.css, frontend/src/components/tweaks/tweaks-drawer.css, frontend/src/styles/list-pages.css (line ~294 ONLY)</files>
  <read_first>83-RESEARCH.md §DEBT-04 (the verified mechanics paragraph, both swap tables, the CSS classification list, Pitfall 1, Assumption A2); each CSS file's matched lines (locate by rule content, not line number); frontend/DESIGN.md §Radius</read_first>
  <action>
    First verify A2: confirm `rounded-xs` resolves to 2px (calc(var(--radius) * .25)) — check the
    HeroUI theme layer in built or dev CSS. If absent, use rounded-[calc(var(--radius)*0.25)].
    TSX swaps (pixel-identical): Sidebar.tsx `before:rounded-[2px]` → `before:rounded-xs`;
    ui/tooltip.tsx arrow `rounded-[2px]` → `rounded-xs`; ui/file-upload.tsx swatch `rounded-[2px]`
    → `rounded-xs`. `rounded-[inherit]` is legit — leave. NavigationShell's rounded-e-[12px] is
    83-06's — do NOT touch.
    CSS swaps per the classification (interfaces block): on-scale 6px→var(--radius-sm) (board.css
    ~:296, dashboard.css ~:864), 8px→var(--radius) (list-pages.css ~:294, calendar.css ~:16/:118/:188),
    12px→var(--radius-lg) (dashboard.css ~:834); half-scale 4px→calc(var(--radius) * 0.5)
    (board.css ~:317/:323, calendar.css ~:68/:175, tweaks-drawer.css ~:60/:175); off-scale 10px
    (dashboard.css ~:890/:908) snap by selector role and record the choice.
    KEEP untouched: pills (99/999/9999px), micro radii 1-3px (scrollbar thumbs/kbd/indicators —
    converting 3px→6px is a visible change with no spec backing), and every
    `rounded-[var(--radius-*)]` occurrence (Pitfall 1 — canonical form, NOT debt).
    list-pages.css SURGICAL RULE (D-83-09): edit ONLY the single border-radius: 8px line (~:294).
    The [class~=…] compat shim block (~:976-1367) and everything else in that file stays
    byte-identical.
  </action>
  <verify>
    <automated>! rg -q 'rounded-(s-|e-|t-|b-)?\[[0-9]' frontend/src -g '!**/components/modern-nav/**' && ! rg -q 'border-radius:\s*(6|8|12)px' frontend/src/pages/WorkBoard/board.css frontend/src/pages/Dashboard/widgets/dashboard.css frontend/src/styles/calendar.css frontend/src/styles/list-pages.css && test "$(git diff --numstat -- frontend/src/styles/list-pages.css | awk '{print $1+$2}')" -le 2 && pnpm --dir frontend type-check</automated>
  </verify>
  <acceptance_criteria>`rg 'rounded-(s-|e-|t-|b-)?\[[0-9]' frontend/src` → 0 outside modern-nav (83-06 owns its one site); zero border-radius 6/8/12px literals in the four target CSS files; no border-radius: 10px remains in dashboard.css (snapped, choice recorded); git diff on list-pages.css shows exactly one changed line (~:294) and the shim block is byte-identical (`git diff -- frontend/src/styles/list-pages.css | grep -c 'class~'` → 0); zero `rounded-[var(` occurrences were modified (`git diff | grep -c 'rounded-\[var('` → 0); type-check exit 0.</acceptance_criteria>
  <done>Radii on the token scale, pixel-identical (except the recorded 10px snap); canonical forms untouched; shim byte-safe.</done>
</task>

<task type="auto">
  <name>Task 3: DEBT-05 long-tail gradients + DEBT-02 expandable-card literals</name>
  <files>frontend/src/components/guided-tours/TourTrigger.tsx, frontend/src/components/milestone-planning/MilestonePlannerEmptyState.tsx, frontend/src/components/ui/pull-to-refresh-indicator.tsx, frontend/src/components/ui/expandable-card.tsx</files>
  <read_first>83-RESEARCH.md §DEBT-05 verdict table (the four live long-tail rows + the KEEP allowlist) and §DEBT-02 expandable-card row; each file (full read); frontend/CLAUDE.md @theme utilities list</read_first>
  <action>
    Gradient flattens (decorative per the verdict table):
    - TourTrigger.tsx (~:97, :206) `bg-gradient-to-* from-primary/10 …` → `bg-accent/5`.
    - MilestonePlannerEmptyState.tsx (~:196) `from-primary/20 to-primary/5` circle → `bg-accent-soft`.
    - pull-to-refresh-indicator.tsx (~:144) `from-muted/50 to-transparent` pull-hint → `bg-muted/30`.
    KEEP (allowlist — do not touch): tweaks-drawer.css hue-picker rainbow track (DATA),
    globe-loader.css radial glow (token-driven signature visual), NavigationShell tint (83-06's
    file anyway), and all mask-image/[mask:…] fades (masks are not gradient backgrounds —
    expandable-card ~:160 and file-upload ~:65 masks stay).
    expandable-card.tsx literals (DEBT-02, D-83-02): text-black→text-ink; bg-black/20 scrim → keep
    OR bg-bg/60 (scrim is mode-legit — executor choice, record it); bg-white /
    `bg-white dark:bg-neutral-900` → bg-surface-4 (it is a modal card);
    `text-neutral-700 dark:text-neutral-200` → text-ink; `text-neutral-600 dark:text-neutral-400`
    → text-ink-mute; `bg-green-500 text-white` CTA pill → `bg-accent text-accent-fg`;
    `hover:bg-neutral-50 dark:hover:bg-neutral-800` → hover:bg-surface-raised.
  </action>
  <verify>
    <automated>! rg -q 'bg-gradient-' frontend/src/components/guided-tours frontend/src/components/milestone-planning frontend/src/components/ui/pull-to-refresh-indicator.tsx && ! rg -q '\b(text|bg|border)-(black|white|neutral|green|gray)-?[0-9]*\b' frontend/src/components/ui/expandable-card.tsx && pnpm --dir frontend type-check && pnpm --dir frontend exec vitest run tests/unit/design-system/</automated>
  </verify>
  <acceptance_criteria>Zero bg-gradient- in the three flattened files' dirs; expandable-card has zero Tailwind palette/black/white literals (scrim decision recorded); the four allowlisted gradient sites are untouched (`git diff --name-only | grep -E 'tweaks-drawer.css|globe-loader.css'` → empty); masks untouched; type-check exit 0; lint clean on touched files.</acceptance_criteria>
  <done>Decorative gradients flat; expandable-card on tokens; allowlist intact.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                           |
| -------- | ------------------------------------------------------------------------------------- |
| none new | Class and CSS-value swaps across presentation files; no new inputs, deps, or behavior |

## STRIDE Threat Register

| Threat ID | Category  | Component                  | Disposition | Mitigation Plan                                                                                |
| --------- | --------- | -------------------------- | ----------- | ---------------------------------------------------------------------------------------------- |
| T-83-05   | Tampering | list-pages.css compat shim | mitigate    | Surgical single-line edit; git-diff line-count + shim-block byte-identity checks in acceptance |
| T-83-SC   | Tampering | package installs           | accept      | No packages installed this phase                                                               |

</threat_model>

<verification>
- `rg '\bshadow-(sm|md|xl|2xl)\b' frontend/src -g '!index.css' -g '!**/components/modern-nav/**' -g '!**/components/copilot/**' -g '!**/styles/modern-nav-tokens.css'` → 0 (DEBT-03 gate minus 83-06 slice)
- `rg 'rounded-(s-|e-|t-|b-)?\[[0-9]' frontend/src -g '!**/components/modern-nav/**'` → 0 (DEBT-04 gate)
- `rg 'bg-gradient-|linear-gradient|radial-gradient' frontend/src` → only allowlisted sites + 83-06-owned files (DEBT-05 gate closes fully after 83-06)
- `git diff -- frontend/src/styles/list-pages.css` → exactly one hunk at ~:294; shim block byte-identical (D-83-09)
- `pnpm --dir frontend lint && pnpm --dir frontend type-check && pnpm --dir frontend exec vitest run` — green (83-VALIDATION.md wave gate)
- Render-parity: radius swaps are byte-identical computed values (visual specs stay green without re-baseline); shadow strips + gradient flattens are DECLARED controlled changes for the 83-07 eyeball
</verification>

<success_criteria>
DEBT-03 long tail stripped per D-83-03; DEBT-04 closed per D-83-04 with the canonical
rounded-[var(--radius-*)] forms untouched; DEBT-05 long tail flattened per D-83-05 with the
data/mask allowlist intact; expandable-card literals mapped per D-83-02; list-pages.css shim
byte-untouched per D-83-09.
</success_criteria>

<output>
Create `.planning/phases/83-token-debt-consolidation/83-05-SUMMARY.md` when done.
</output>
