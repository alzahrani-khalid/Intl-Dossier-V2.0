---
phase: 83-token-debt-consolidation
plan: 06
type: execute
wave: 2
depends_on: [83-01]
files_modified:
  - frontend/src/styles/modern-nav-tokens.css
  - frontend/src/components/copilot/copilot-theme.css
  - frontend/src/components/modern-nav/IconRail.tsx
  - frontend/src/components/modern-nav/NavigationShell/NavigationShell.tsx
  - frontend/src/components/modern-nav/** (any other consumer of deleted ladder vars)
  - frontend/src/routes/modern-nav-standalone.tsx
autonomous: true
requirements: [DEBT-04, DEBT-05, DEBT-06]
must_haves:
  truths:
    - "Satisfies D-83-06: modern-nav-tokens.css no longer defines a parallel --shadow-* ladder, a multiplicative --radius-* scale, a --space-* scale, or its own hsl color tables — the file's component classes consume design-system tokens (var(--sidebar-bg), var(--surface-3), var(--line), var(--ink), var(--ink-mute), var(--ok), var(--danger), var(--accent)); copilot-theme.css's half is documented satisfied-by-evidence (its --copilot-kit-* vars already alias DS tokens) with the one dead box-shadow declaration deleted"
    - "Satisfies D-83-05 (modern-nav share): the glassmorphism radials, panel-item shine, and text-shadows in modern-nav-tokens.css are deleted/flattened; IconRail's separator gradient becomes 1px solid var(--line) and its rail bg becomes var(--sidebar-bg); the NavigationShell two-identical-stop tint (~:243) is KEPT (renders flat — allowlisted)"
    - 'Satisfies D-83-04 (modern-nav share): NavigationShell rounded-e-[12px] → rounded-e-xl (pixel-identical 12px) or rounded-e-[var(--radius-lg)]'
    - 'Satisfies D-83-09: deletion safety proven per var — every deleted custom property has a zero-external-consumer grep recorded (external var() consumers resolve against index.css :root definitions which are NOT deleted); index.css itself, design-system/tokens/, and bootstrap.js are untouched'
    - 'Blast radius is confined to the /modern-nav-standalone demo route (RESEARCH: importer grep verified) — demo visual change is deliberate and exempt from render-parity; all other routes pixel-unchanged'
  artifacts:
    - path: 'frontend/src/styles/modern-nav-tokens.css'
      provides: 'modern-nav component classes consuming DS tokens, no parallel ladders'
      contains: 'var(--'
  key_links:
    - from: 'frontend/src/styles/modern-nav-tokens.css'
      to: 'design-system tokens (index.css :root / engine)'
      via: 'var(--sidebar-bg)/var(--surface-3)/var(--line)/var(--ink) consumption'
      pattern: "var\\(--surface-3\\)"
---

<objective>
Delete the bespoke parallel token ladders (DEBT-06): rewrite modern-nav-tokens.css to consume
design-system tokens, flatten its glass effects, fix the modern-nav component files (gradients,
radius, checklist dingbats), and delete copilot-theme.css's one dead shadow declaration.

Purpose: modern-nav-tokens.css is the ONLY real parallel ladder left (copilot is already
consolidated — RESEARCH verified its vars alias DS tokens). Verified safe: Tailwind v4 shadow
utilities inline their values so deleting the ladder cannot change any shadow-\* utility (Pitfall
2), and the file's component classes are consumed only by the /modern-nav-standalone demo route.
Output: DEBT-06 grep gates at zero; demo route deliberately restyled flat; all other routes untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/83-token-debt-consolidation/83-RESEARCH.md  (§DEBT-06 — the full ladder inventory + disposition table + deletion-safety fact; §DEBT-05 modern-nav rows; §DEBT-04 NavigationShell row; Pitfall 2)
@.planning/phases/83-token-debt-consolidation/83-CONTEXT.md   (D-83-04, D-83-05, D-83-06, D-83-09)
@frontend/DESIGN.md  (§Dark/Light token tables — the DS tokens the file re-points to)
@frontend/src/styles/modern-nav-tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: modern-nav-tokens.css — delete the ladders, re-point consumers, flatten glass; copilot-theme.css :758 cleanup</name>
  <files>frontend/src/styles/modern-nav-tokens.css, frontend/src/components/copilot/copilot-theme.css</files>
  <read_first>frontend/src/styles/modern-nav-tokens.css (FULL read — the disposition table maps to its sections); 83-RESEARCH.md §DEBT-06 (the 7-row disposition table + the deletion-safety fact listing the external files that resolve against index.css); frontend/src/index.css :root (the surviving definitions external consumers resolve to: --shadow-sm: none, --shadow, --shadow-lg, --space-N, --ease-out, --dur*)</read_first>
  <action>
    Apply the RESEARCH §DEBT-06 disposition table to modern-nav-tokens.css:
    - DELETE the --shadow-xs/sm/md/lg/xl ladder (+ -dark variants + .dark swap, ~:117-128/:203-207).
      Safe per Pitfall 2: Tailwind shadow utilities inline values; external var(--shadow-*)
      consumers resolve against index.css :root which stays.
    - DELETE the multiplicative --radius-xs…-3xl/-full scale (~:136-144) — byte-duplicate of the
      HeroUI theme layer; behavior-neutral.
    - DELETE the --space-0…16 rem scale (~:91-101); re-point any var(--space-N) consumer inside
      modern-nav files (index.css's px definitions remain and are equal at 16px root).
    - DELETE the hsl color tables (--icon-rail-*, --panel-*, --badge-*, --content-*,
      --success/warning-indicator, ~:14-77/:179-201); re-point their consumers within this file:
      rail/panel bg → var(--sidebar-bg)/var(--surface-3), borders → var(--line), text →
      var(--ink)/var(--ink-mute), indicators → var(--ok)/var(--danger), badge →
      var(--surface-raised)/var(--ink-mute).
    - FLATTEN the glass effects (~:264-545): .icon-button::after/.active radials and .panel-item
      shine + inset shadows → hover background: var(--surface-3); active bar keeps var(--accent);
      .metric-card MD3 !important shadows → 1px solid var(--line), no shadow; drop all text-shadows.
      Demo-only visual change — deliberate.
    - DELETE the duplicate --ease-out definition; KEEP the component dims (--icon-rail-width,
      --icon-button-size, --active-indicator-*, --panel-width, --panel-indent — local layout
      constants, not ladder-parallel) and keep-or-inline the remaining --transition-*/--z-* per
      discretion.
    Deletion safety per var group: BEFORE deleting each custom property, run
    `rg "var(--<name>" frontend/src -g '!styles/modern-nav-tokens.css' -g '!components/modern-nav/**'`
    and confirm every external consumer resolves against a surviving index.css :root definition
    (the RESEARCH safety table lists the known ones); record the greps.
    copilot-theme.css: delete the dead `box-shadow: var(--shadow-sm)` declaration (~:758 — it
    resolves to none via index.css, removal is pixel-identical). Leave the pill radii and the
    --copilot-kit-* alias block untouched — that alias block IS the DEBT-06 consumption mechanism
    (document copilot's half as satisfied-by-evidence in the summary).
  </action>
  <verify>
    <automated>! rg -q -- '--shadow-(xs|sm|md|lg|xl):' frontend/src/styles/modern-nav-tokens.css && ! rg -q -- '--radius-(xs|2xl|3xl|full):' frontend/src/styles/modern-nav-tokens.css && ! rg -q 'box-shadow: var\(--shadow-sm\)' frontend/src/components/copilot/copilot-theme.css && pnpm --dir frontend build</automated>
  </verify>
  <acceptance_criteria>Zero --shadow-*: definitions in modern-nav-tokens.css; zero parallel radius/space scale definitions; zero hsl( color-table definitions parallel to the Linear palette (`rg -c 'hsl(' frontend/src/styles/modern-nav-tokens.css` at or near 0, any survivor justified in summary); per-var zero-external-consumer greps recorded for every deleted property; copilot-theme.css diff is exactly the one dead box-shadow line; index.css untouched (`git diff --name-only | grep -c 'src/index.css'` → 0, D-83-09); `pnpm --dir frontend build` exit 0; lint clean on touched files.</acceptance_criteria>
  <done>Parallel ladders gone; file consumes DS tokens; copilot half closed.</done>
</task>

<task type="auto">
  <name>Task 2: Modern-nav component files — gradients, radius, checklist dingbats, demo before/after</name>
  <files>frontend/src/components/modern-nav/IconRail.tsx, frontend/src/components/modern-nav/NavigationShell/NavigationShell.tsx, frontend/src/routes/modern-nav-standalone.tsx</files>
  <read_first>83-RESEARCH.md §DEBT-05 verdict rows for IconRail/NavigationShell + §DEBT-04 NavigationShell row + §DEBT-08 optional-adds note; the three files (full read); 83-VALIDATION.md render-parity protocol (the demo route is EXPECTED to change)</read_first>
  <action>
    Before editing, capture a BEFORE screenshot of /modern-nav-standalone (dev server +
    browser-harness or agent-browser).
    - IconRail.tsx (~:181): separator shadow-gradient → `1px solid var(--line)` hairline;
      (~:232) rail bg `from-[hsl(…)]` gradient → `var(--sidebar-bg)` flat.
    - NavigationShell.tsx (~:238): `rounded-e-[12px]` → `rounded-e-xl` (pixel-identical 12px on
      the HeroUI scale) or `rounded-e-[var(--radius-lg)]` if rounded-e-xl does not compile;
      (~:243) the linear-gradient(color-mix(var(--bg)…)…) + texture tint is a two-identical-stop
      SOLID overlay — KEEP, it is allowlisted (not a visual gradient).
    - modern-nav-standalone.tsx: replace the checklist check-mark/heavy-check dingbat bullets with
      lucide `Check` icons (DEBT-08 optional-add, cheap while owning the file); the inert
      copy-string "(#1A1D26)" in the demo prose (~:99) — reword to name the token instead of a
      hex so the hex grep goes quiet, or leave with a justifying note (it is display copy, not a
      style value; record whichever).
    After edits capture an AFTER screenshot of the demo route — the flat restyle is the DELIBERATE
    change; attach both to the summary. Verify one non-demo route (e.g. /dashboard) renders
    unchanged (the modern-nav classes are demo-only, but the file is imported globally via
    index.css:5 — confirm no selector leaks).
  </action>
  <verify>
    <automated>! rg -q 'bg-gradient-|from-\[hsl' frontend/src/components/modern-nav && ! rg -q 'rounded-(s-|e-|t-|b-)?\[[0-9]' frontend/src/components/modern-nav && pnpm --dir frontend type-check && pnpm --dir frontend lint</automated>
  </verify>
  <acceptance_criteria>Zero decorative gradients in components/modern-nav/** (the NavigationShell tint is CSS in the style prop — if it matches the grep, it is the single allowlisted survivor, documented); zero numeric arbitrary radii in modern-nav; checklist dingbats replaced with lucide Check; before/after screenshots of /modern-nav-standalone captured and referenced in the summary; one non-demo route visually spot-checked; type-check + lint clean.</acceptance_criteria>
  <done>Modern-nav components flat + token-driven; demo change documented with screenshots.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                                |
| -------- | ------------------------------------------------------------------------------------------ |
| none new | CSS custom-property deletion + class swaps in a demo-scoped surface; no new inputs or deps |

## STRIDE Threat Register

| Threat ID | Category  | Component                    | Disposition | Mitigation Plan                                                                                      |
| --------- | --------- | ---------------------------- | ----------- | ---------------------------------------------------------------------------------------------------- |
| T-83-06   | DoS       | globally-imported stylesheet | mitigate    | Per-var zero-external-consumer greps before each deletion; build + non-demo route spot-check in-task |
| T-83-SC   | Tampering | package installs             | accept      | No packages installed this phase                                                                     |

</threat_model>

<verification>
- `rg -- '--shadow-(xs|sm|md|lg|xl):' frontend/src/styles/modern-nav-tokens.css` → 0 (DEBT-06 gate per 83-VALIDATION.md)
- Per-var zero-consumer greps recorded for every deleted property (deletion-safety table honored)
- `rg 'bg-gradient-|linear-gradient|radial-gradient' frontend/src` → only the ~4 allowlisted sites once this and 83-05 are both merged (DEBT-05 gate)
- `pnpm --dir frontend lint && pnpm --dir frontend type-check && pnpm --dir frontend exec vitest run && pnpm --dir frontend build` — green (83-VALIDATION.md wave gate)
- Demo route before/after screenshots; non-demo route unchanged
</verification>

<success_criteria>
DEBT-06 closed per D-83-06 (ladders deleted, DS tokens consumed, copilot half evidence-documented);
modern-nav gradients flattened per D-83-05 with the tint allowlisted; the last numeric radius
swapped per D-83-04; blast radius confined to the demo route; carve-outs untouched per D-83-09.
</success_criteria>

<output>
Create `.planning/phases/83-token-debt-consolidation/83-06-SUMMARY.md` when done.
</output>
