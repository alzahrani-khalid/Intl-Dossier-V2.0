---
phase: 83-token-debt-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/background-boxes.tsx
  - frontend/src/components/ui/world-map.tsx
  - frontend/src/components/ui/floating-dock.tsx
  - frontend/src/components/ui/animated-tooltip.tsx
  - frontend/src/components/ui/moving-border.tsx
  - frontend/src/components/ui/placeholders-and-vanish-input.tsx
  - frontend/src/components/ui/related-entity-carousel.tsx
  - frontend/src/components/ui/enhanced-progress.tsx
  - frontend/src/components/ui/chart.tsx
  - frontend/src/components/geographic-visualization/WorldMapVisualization.tsx
  - frontend/src/components/timeline/** (entire directory incl. __tests__ and index.ts)
  - frontend/src/styles/vertical-timeline.css
  - frontend/src/App.css
  - frontend/src/components/ui/COMPONENT_REGISTRY.md
  - frontend/package.json
  - pnpm-lock.yaml
autonomous: true
requirements: [DEBT-07, DEBT-01, DEBT-02, DEBT-05]
must_haves:
  truths:
    - 'Satisfies D-83-07 (vertical-timeline half): styles/vertical-timeline.css, its only consumer components/timeline/**, and the react-vertical-timeline-component dependency are deleted — every `!important` px row-height rule is gone with zero visual risk (nothing mounts them; RESEARCH verified zero external importers)'
    - 'Satisfies D-83-09 (carve-out discipline): zero carve-out files touched — styles/list-pages.css, types/*.ts migration comments, design-system/tokens/, index.css :root, public/bootstrap.js are all byte-identical after this plan (git diff shows no changes on those paths)'
    - 'The 9 zero-importer aceternity/shadcn ui files + WorldMapVisualization.tsx + App.css are deleted, removing ~35 raw hex, ~25 Tailwind literals, and ~8 gradients from the DEBT-01/02/05 inventories without migration work'
    - 'pnpm --dir frontend type-check, pnpm --dir frontend build, and the full frontend vitest suite all pass after deletion (no dangling imports, no test importing a deleted module)'
  artifacts:
    - path: 'frontend/package.json'
      provides: 'dependency list with zero vertical-timeline entries'
    - path: 'frontend/src/components/ui/COMPONENT_REGISTRY.md'
      provides: 'registry updated to drop deleted-component entries (or register them per check-deleted-components.sh convention)'
  key_links:
    - from: 'frontend/package.json'
      to: 'components/timeline (deleted)'
      via: 'react-vertical-timeline-component removed together with its only consumer'
      pattern: 'react-vertical-timeline-component'
---

<objective>
Delete the verified-dead code that carries a large share of the phase's token debt: 9 zero-importer
aceternity/shadcn `components/ui/` files, the entire `components/timeline/` directory,
`styles/vertical-timeline.css`, `WorldMapVisualization.tsx`, `App.css`, and the
`react-vertical-timeline-component` npm dependency.

Purpose: RESEARCH §Dead Code Discovery proved these files have ZERO importers (only mention is the
COMPONENT_REGISTRY.md docs file). Deletion removes ~35 hex, ~25 Tailwind literals, ~8 gradients, ALL
of DEBT-07's `!important` px rules, and several shadow/radius sites in one move with zero render
risk — nothing mounts any of it. This shrinks every downstream sweep slice.
Output: dead files removed, dependency dropped, registry bookkeeping done, green build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/83-token-debt-consolidation/83-RESEARCH.md  (§Dead Code Discovery — the table, the live look-alikes list, deletion hygiene)
@.planning/phases/83-token-debt-consolidation/83-CONTEXT.md   (D-83-07, D-83-09)
@frontend/src/components/ui/COMPONENT_REGISTRY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify liveness, then delete the dead files + registry bookkeeping</name>
  <files>frontend/src/components/ui/background-boxes.tsx, frontend/src/components/ui/world-map.tsx, frontend/src/components/ui/floating-dock.tsx, frontend/src/components/ui/animated-tooltip.tsx, frontend/src/components/ui/moving-border.tsx, frontend/src/components/ui/placeholders-and-vanish-input.tsx, frontend/src/components/ui/related-entity-carousel.tsx, frontend/src/components/ui/enhanced-progress.tsx, frontend/src/components/ui/chart.tsx, frontend/src/components/geographic-visualization/WorldMapVisualization.tsx, frontend/src/components/timeline/**, frontend/src/styles/vertical-timeline.css, frontend/src/App.css, frontend/src/components/ui/COMPONENT_REGISTRY.md</files>
  <read_first>83-RESEARCH.md §Dead Code Discovery (the dead-file table + the "Live look-alikes (do NOT delete)" list + deletion hygiene), frontend/src/components/ui/COMPONENT_REGISTRY.md, scripts/check-deleted-components.sh (Phase-79 convention — read to learn what bookkeeping deleted components require)</read_first>
  <action>
    For EACH file in the dead list, re-verify liveness immediately before deleting (research greps
    are the evidence trail but the tree may have moved): run
    `rg -l "&lt;module-basename-or-exported-name&gt;" frontend/src frontend/tests tests` and confirm the
    only matches are the file itself, its own tests, or COMPONENT_REGISTRY.md. Then `git rm` it.
    Delete `components/timeline/` as a WHOLE directory including `__tests__/` and `index.ts`
    (RESEARCH Pitfall 6: a partial delete leaves tests importing deleted components and vitest
    fails). Delete `styles/vertical-timeline.css` (imported only by the dead timeline components)
    and `frontend/src/App.css` (Vite scaffold leftover, never imported).
    DO NOT delete the live look-alikes: `ui/pull-to-refresh-indicator.tsx`, `ui/file-upload.tsx`,
    `ui/expandable-card.tsx` (used by dossier/ExpandableDossierCard), `ui/context-aware-fab.tsx`,
    and `pages/Dashboard/components/TimelineEventCard.tsx` (a DIFFERENT file from the dead
    `components/timeline/TimelineEventCard.tsx`).
    Update COMPONENT_REGISTRY.md to remove (or annotate per its own convention) the deleted
    entries, and run `scripts/check-deleted-components.sh` — if the Phase-79 pattern requires
    deleted component names to be registered somewhere, follow that convention.
    If any liveness re-grep finds a REAL importer, STOP for that file, leave it in place, and
    record the deviation in the summary (do not migrate it here — it falls to the Wave-2 slice
    that owns its directory).
  </action>
  <verify>
    <automated>! rg -l "background-boxes|floating-dock|animated-tooltip|moving-border|placeholders-and-vanish-input|related-entity-carousel|enhanced-progress|WorldMapVisualization|vertical-timeline" frontend/src --glob '!**/COMPONENT_REGISTRY.md' | grep -q . && pnpm --dir frontend type-check</automated>
  </verify>
  <acceptance_criteria>Every file in the dead-list is absent from disk (ls returns nothing); a per-file liveness re-grep was run before each git rm; components/timeline/ has no survivors (`ls frontend/src/components/timeline 2>&1` errors); the 5 live look-alikes are untouched (`git diff --name-only` does not list them); COMPONENT_REGISTRY.md updated; `scripts/check-deleted-components.sh` passes; `pnpm --dir frontend type-check` exit 0; carve-out paths untouched (`git diff --name-only | grep -E 'list-pages.css|design-system/tokens|bootstrap.js|src/types/'` → empty).</acceptance_criteria>
  <done>All dead files deleted, bookkeeping done, type-check green.</done>
</task>

<task type="auto">
  <name>Task 2: Drop react-vertical-timeline-component + full verification</name>
  <files>frontend/package.json, pnpm-lock.yaml</files>
  <read_first>frontend/package.json (lines ~92 `@types/react-vertical-timeline-component` and ~126 `react-vertical-timeline-component`), 83-RESEARCH.md §DEBT-07 (why deletion closes the vertical-timeline half) and Assumption A4</read_first>
  <action>
    Remove `react-vertical-timeline-component` and `@types/react-vertical-timeline-component` from
    frontend/package.json (their only consumer was the deleted components/timeline/ directory —
    RESEARCH A4). Run `pnpm install` from the repo root to regenerate pnpm-lock.yaml. Then run the
    full frontend verification battery: type-check, full vitest run, production build. This is the
    task that proves the deletion left no dangling reference anywhere (build breaks loudly if A4
    was wrong).
  </action>
  <verify>
    <automated>! grep -q "vertical-timeline" frontend/package.json && pnpm --dir frontend type-check && pnpm --dir frontend exec vitest run && pnpm --dir frontend build</automated>
  </verify>
  <acceptance_criteria>`grep -c vertical-timeline frontend/package.json` → 0; pnpm-lock.yaml regenerated (no vertical-timeline entries); full frontend vitest suite green; `pnpm --dir frontend build` exit 0; DEBT-07 re-audit gate passes: `rg -n '(min-)?height:\s*[0-9]+px\s*!important' frontend/src/styles/` → 0 matches.</acceptance_criteria>
  <done>Dependency removed, lockfile clean, build + tests green, DEBT-07 !important gate at zero.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                                   |
| -------- | --------------------------------------------------------------------------------------------- |
| none new | Pure deletion of unreferenced frontend files; no new inputs, endpoints, or dependencies added |

## STRIDE Threat Register

| Threat ID | Category  | Component        | Disposition | Mitigation Plan                                                                                                   |
| --------- | --------- | ---------------- | ----------- | ----------------------------------------------------------------------------------------------------------------- |
| T-83-01   | DoS       | build pipeline   | mitigate    | Per-file liveness re-grep before each git rm + type-check/build/vitest in-task catch any dangling import          |
| T-83-SC   | Tampering | package installs | accept      | No packages installed this phase (RESEARCH Package Legitimacy Audit: removal only — supply-chain surface shrinks) |

</threat_model>

<verification>
- `pnpm --dir frontend type-check` — exit 0
- `pnpm --dir frontend exec vitest run` — full suite green
- `pnpm --dir frontend build` — exit 0
- `rg -n '(min-)?height:\s*[0-9]+px\s*!important' frontend/src/styles/` → 0 (DEBT-07 gate, per 83-VALIDATION.md)
- `git diff --name-only <base>..HEAD | grep -E 'list-pages\.css|design-system/tokens|bootstrap\.js|src/types/'` → empty (D-83-09 carve-outs byte-untouched)
</verification>

<success_criteria>
DEBT-07's `!important` row-height debt is fully closed via deletion (D-83-07); ~35 hex / ~25
literals / ~8 gradients removed from the DEBT-01/02/05 inventories; one npm dependency removed;
zero render change (nothing mounted the deleted code); carve-outs byte-untouched (D-83-09).
</success_criteria>

<output>
Create `.planning/phases/83-token-debt-consolidation/83-01-SUMMARY.md` when done.
</output>
