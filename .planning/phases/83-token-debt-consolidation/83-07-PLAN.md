---
phase: 83-token-debt-consolidation
plan: 07
type: execute
wave: 3
depends_on: [83-03, 83-04, 83-05, 83-06]
files_modified:
  - eslint.config.mjs
  - frontend/tests/e2e/** (dashboard-widgets visual baselines — deliberate re-baseline only)
autonomous: false
requirements: [DEBT-01, DEBT-02, DEBT-03, DEBT-04, DEBT-05, DEBT-06, DEBT-07, DEBT-08]
must_haves:
  truths:
    - "Satisfies D-83-01 (the phase's own tell): the eslint.config.mjs Tier-B carve-out is tightened from 18 entries to exactly 3 (design-system/tokens/directions.ts, public/bootstrap.js, signature-visuals/flags/**) — `pnpm --dir frontend lint --max-warnings 0` now IS the DEBT-01/02 re-audit gate; any missed hex or palette literal fails CI"
    - "Satisfies D-83-09: the verified carve-outs are byte-untouched across the WHOLE phase — git diff over the phase range shows zero changes to the list-pages.css shim block, types/*.ts migration comments, and the three palette holders' pre-existing lines; node scripts/check-bootstrap-parity.mjs passes"
    - 'Every 83-VALIDATION.md re-audit grep gate passes: raw hex → comment-only remainder; palette literals → 0 outside shim/tests; banned shadows → 0; numeric arbitrary radii → 0; gradients → allowlist-only; modern-nav ladder defs → 0; !important px heights → 0; emoji-as-UI → data/JSDoc-only'
    - 'Playwright visual + RTL suites are green, with the dashboard-widgets chart-hue re-baseline landed as a knowing, human-eyeballed commit (the ONLY re-baseline; every other suite passes against existing baselines)'
    - 'The render-parity matrix walk (1400 + 1024 × dark + light × EN + AR) is human-verified: pixel-unchanged everywhere except the declared controlled-change list (chart/graph hues, card-shadow removal, ~6 gradient flattens, emoji→lucide, the demo route)'
    - "DEBT-07's list-pages half is closed as verified-not-debt with the RESEARCH evidence cited in the phase summary (the 20/28/34/32px values are icon/logo/sidebar-item/pill dimensions with no !important; the !important half was closed by 83-01's deletion)"
  artifacts:
    - path: 'eslint.config.mjs'
      provides: 'Tier-B carve-out shrunk to the 3 permanent holders'
      contains: 'signature-visuals/flags'
  key_links:
    - from: 'eslint.config.mjs'
      to: 'DEBT-01/02 enforcement'
      via: 'no-restricted-syntax hex + palette-literal rules now biting on all chart/graph files'
      pattern: 'no-restricted-syntax'
---

<objective>
Close the phase: tighten the ESLint hex/literal carve-out so lint becomes the permanent re-audit
gate, run every 83-VALIDATION.md grep gate + the full suite + Playwright visual/RTL, land the one
deliberate dashboard-widgets re-baseline, and human-verify the render-parity matrix.

Purpose: ROADMAP success criteria 1, 4, 5 — spec-audit re-run at clean/minor, carve-outs
byte-untouched with CI parity green, zero visual regressions dark+light × EN/AR.
Output: phase-complete evidence trail; lint as the standing DEBT-01/02 gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/83-token-debt-consolidation/83-VALIDATION.md  (the Requirements → Test Map — every gate this plan runs — and the Render-Parity Protocol)
@.planning/phases/83-token-debt-consolidation/83-RESEARCH.md  (§ESLint carve-out tightening; §Regression Safety table — the declared controlled-change list; §DEBT-07 list-pages evidence)
@.planning/phases/83-token-debt-consolidation/83-CONTEXT.md   (D-83-01 note on eslint.config.mjs:245; D-83-09)
@eslint.config.mjs  (the Tier-B carve-out block ~:244-268)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten the ESLint Tier-B carve-out + run all re-audit greps + full suite</name>
  <files>eslint.config.mjs</files>
  <read_first>eslint.config.mjs Tier-B block (~:244-268 — the 18-entry files list); 83-RESEARCH.md §ESLint carve-out tightening (the exact 3-entry target list); 83-VALIDATION.md Requirements → Test Map (every gate command)</read_first>
  <action>
    Shrink the Tier-B carve-out `files:` array to exactly:
    `frontend/src/design-system/tokens/directions.ts`, `frontend/public/bootstrap.js`,
    `frontend/src/components/signature-visuals/flags/**/*.{tsx,ts}` — removing the 15 chart/graph
    entries (they are now token-clean; the carve-out comment "for a future chart-token phase"
    should be updated to say the chart-token phase landed in 83). Update the comment prose, keep
    the rule structure.
    Then run and record EVERY 83-VALIDATION.md gate:
    - DEBT-01 hex: `rg -n "#[0-9a-fA-F]{6}\b" frontend/src -g '!design-system/tokens/*' -g '!index.css' -g '!**/signature-visuals/flags/**'` → comment-only remainder (list survivors, each must be a comment or the documented demo copy-string)
    - DEBT-02 literals: the palette-literal rg with `-g '!styles/list-pages.css' -g '!**/__tests__/**'` → 0
    - DEBT-03: `rg '\bshadow-(sm|md|xl|2xl)\b' frontend/src -g '!index.css'` → 0
    - DEBT-04: `rg 'rounded-(s-|e-|t-|b-)?\[[0-9]' frontend/src` → 0 and `rg 'border-radius:\s*(6|8|10|12)px' frontend/src` → 0 (micro ≤4px + pills allowlisted)
    - DEBT-05: gradient rg → only the ~4 allowlisted sites
    - DEBT-06: `rg -- '--shadow-(xs|sm|md|lg|xl):' frontend/src/styles/modern-nav-tokens.css` → 0
    - DEBT-07: `rg '(min-)?height:\s*[0-9]+px\s*!important' frontend/src/styles/` → 0
    - DEBT-08: the emoji unicode-range rg over components + routes → only data/JSDoc carve-outs
    - Parity: `node scripts/check-bootstrap-parity.mjs` → exit 0
    - Full suite: `pnpm --dir frontend lint` (--max-warnings 0 — this is now the standing gate),
      `pnpm --dir frontend type-check`, `pnpm --dir frontend exec vitest run`,
      `pnpm --dir frontend build`.
    If ANY gate fails, fix the residue in the owning file (or file a gap) before proceeding —
    record every gate command + result in the summary as the spec-audit re-run evidence.
  </action>
  <verify>
    <automated>pnpm --dir frontend lint && pnpm --dir frontend type-check && pnpm --dir frontend exec vitest run && node scripts/check-bootstrap-parity.mjs && ! rg -q '\bshadow-(sm|md|xl|2xl)\b' frontend/src -g '!index.css' && ! rg -q 'rounded-(s-|e-|t-|b-)?\[[0-9]' frontend/src</automated>
  </verify>
  <acceptance_criteria>eslint.config.mjs Tier-B files array has exactly 3 entries; lint clean at --max-warnings 0 (proving no missed hex/literal anywhere); all 8 DEBT gate greps recorded with passing results in the summary; parity guard exit 0; type-check + vitest + build green.</acceptance_criteria>
  <done>Lint is the standing DEBT-01/02 gate; every re-audit grep at clean/minor; evidence recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright visual + RTL suites with the one deliberate re-baseline</name>
  <files>frontend/tests/e2e/** (dashboard-widgets baseline snapshots only)</files>
  <read_first>83-VALIDATION.md (the e2e visual row + "chart-color re-baseline is a knowing commit"); 83-RESEARCH.md §Regression Safety (which classes are pixel-identical vs controlled change; Assumption A3); frontend/playwright.config.ts (the chromium-dashboard-widgets project with committed baselines)</read_first>
  <action>
    Run `pnpm --dir frontend exec playwright test dashboard-visual dashboard-widgets-visual
    list-pages-visual dossier-drawer-visual analytics-dashboard rtl-component-smokes --reporter=list`.
    Expected: dashboard-widgets snapshots FAIL on chart hues (the declared controlled change);
    every other suite MUST pass against existing baselines — a failure elsewhere is a REGRESSION,
    not a re-baseline candidate: fix it (or route it to the owning plan as a gap) before touching
    any baseline.
    For the dashboard-widgets failures: inspect each diff image and confirm the delta is ONLY the
    chart-series hue shift (+ the BenchmarkPreview flatten if it appears) — layout, text, borders,
    spacing identical. Then re-baseline with --update-snapshots scoped to that project only, and
    commit as a standalone knowing commit: `test(83-07): deliberate dashboard-widgets re-baseline
    (chart hues -> Linear chart tokens)`.
    If OTHER suites also intersect chart colors (RESEARCH A3), evaluate each the same way —
    controlled-change-only diffs may be re-baselined with the same discipline; anything else is a
    regression.
  </action>
  <verify>
    <automated>pnpm --dir frontend exec playwright test dashboard-visual dashboard-widgets-visual list-pages-visual dossier-drawer-visual analytics-dashboard rtl-component-smokes --reporter=list</automated>
  </verify>
  <acceptance_criteria>All six suites green; the only baseline changes in git are the dashboard-widgets project (plus any A3 additions, each justified with a diff-image note in the summary); the re-baseline is a standalone commit whose message declares it deliberate; zero non-controlled visual diffs.</acceptance_criteria>
  <done>Visual + RTL suites green with a documented, scoped re-baseline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Render-parity matrix walk (the phase's key gate)</name>
  <files>none (verification only — no file changes in this task)</files>
  <read_first>83-VALIDATION.md Manual-Only Render-Parity Protocol (the routes + matrix + declared controlled-change list); 83-RESEARCH.md §Regression Safety table; the 83-06 before/after demo screenshots</read_first>
  <action>
    Bring up the dev server, then PAUSE and present the matrix walk below to the human. Before
    pausing, pre-stage the walk: confirm the server renders /dashboard, and have the 83-06
    before/after screenshots at hand. Do NOT self-approve — this is the phase's key manual gate
    (whole-app token-swap parity cannot be unit-asserted app-wide; 83-VALIDATION.md).
  </action>
  <verify>
    <automated>MISSING — inherently manual gate per 83-VALIDATION.md Manual-Only protocol; automated coverage is Task 1's grep/lint gates + Task 2's Playwright visual suites, which run before this checkpoint</automated>
  </verify>
  <acceptance_criteria>Human types "approved" after walking every route in the matrix at 1400 + 1024 across dark/light and EN/AR, with zero UNDECLARED visual changes; any reported deviation is logged as a gap before the phase closes.</acceptance_criteria>
  <done>Human approval recorded; render-parity holds everywhere outside the declared controlled-change list.</done>
  <what-built>Phase 83 token-debt consolidation: all chart/graph colors on --chart-1..8 tokens, card shadows stripped, radii on the 6/8/12 scale, decorative gradients flattened, bespoke ladders deleted, emoji-as-UI replaced with lucide, carve-outs byte-untouched, lint tightened into the standing gate.</what-built>
  <how-to-verify>
    Start the dev server (`pnpm dev`, frontend on :5173). Walk the 83-VALIDATION.md matrix — for
    EACH route below, check 1400px and 1024px widths, dark and light modes, EN and AR
    (`?lng=ar` for first-paint RTL):
    1. /dashboard — widgets + charts (EXPECT: chart hues shifted to Linear families; card shadows
       gone; BenchmarkPreview flat; everything else pixel-unchanged)
    2. /analytics — chart fills + summary tiles (EXPECT: hue shift + tile soft-washes on tokens)
    3. A dossier detail → relationships tab — MiniRelationshipGraph (EXPECT: node/edge hues
       shifted; arrowheads and dot-grid COLORED, not black/transparent — the React Flow var()
       check; dots now correct in dark mode)
    4. /reports report-builder preview (EXPECT: 8-series categorical cycle on chart tokens)
    5. One list page (e.g. /dossiers/countries) (EXPECT: pixel-unchanged; the compat shim intact)
    6. One drawer (EXPECT: drawer shadow STILL present — --shadow-drawer is legitimate)
    7. /copilot drawer (EXPECT: pixel-unchanged)
    8. /modern-nav-standalone (EXPECT: deliberately flattened — compare the 83-06 before/after
       screenshots)
    9. SLA countdown badge anywhere it renders (EXPECT: lucide icons, not emoji, layout intact)
    Everything OUTSIDE the declared controlled-change list (chart/graph hues, card-shadow removal,
    ~6 gradient flattens, emoji→lucide, the demo route) must be pixel-unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" if the matrix holds, or describe any route/mode/lang/width where an UNDECLARED visual change appears</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                          |
| -------- | ------------------------------------------------------------------------------------ |
| none new | Lint-config tightening + test-baseline updates; no runtime code changes in this plan |

## STRIDE Threat Register

| Threat ID | Category    | Component          | Disposition | Mitigation Plan                                                                                        |
| --------- | ----------- | ------------------ | ----------- | ------------------------------------------------------------------------------------------------------ |
| T-83-07   | Repudiation | visual re-baseline | mitigate    | Re-baseline is a standalone declared commit, scoped to dashboard-widgets, human-eyeballed diff by diff |
| T-83-SC   | Tampering   | package installs   | accept      | No packages installed this phase                                                                       |

</threat_model>

<verification>
- Every 83-VALIDATION.md Requirements → Test Map row executed and recorded (the spec-audit re-run)
- `pnpm --dir frontend lint --max-warnings 0` — the new standing gate, clean
- `node scripts/check-bootstrap-parity.mjs` — exit 0 (carve-out holders healthy)
- Playwright visual + RTL suites green with only the declared re-baseline
- Human render-parity approval on the full matrix
- Phase summary documents: DEBT-07 list-pages verified-not-debt disposition (RESEARCH evidence), the controlled-change list, and every gate result
</verification>

<success_criteria>
ROADMAP Phase-83 success criteria all TRUE: (1) re-audit at clean/minor, (2) chart/graph colors
through --chart-1..n, (3) ladders deleted + row heights clean, (4) carve-outs byte-untouched +
parity guard green (D-83-09), (5) zero undeclared visual regressions dark+light × EN/AR. The
eslint carve-out is narrowed per D-83-01's note — the phase's own tell is resolved.
</success_criteria>

<output>
Create `.planning/phases/83-token-debt-consolidation/83-07-SUMMARY.md` when done.
</output>
