---
phase: 83-token-debt-consolidation
plan: 04
type: execute
wave: 2
depends_on: [83-02]
files_modified:
  - frontend/src/components/dossier/MiniRelationshipGraph.tsx
  - frontend/src/components/dossier/AddToDossierMenu.tsx
  - frontend/src/components/dossier/** (shadow strip within this dir)
  - frontend/src/components/dossiers/RelationshipGraph.tsx
  - frontend/src/components/dossiers/** (shadow strip within this dir)
  - frontend/src/components/relationships/RelationshipGraph.tsx
  - frontend/src/components/relationships/AdvancedGraphVisualization.tsx
  - frontend/src/components/relationships/EnhancedGraphVisualization.tsx
  - frontend/src/components/relationships/TouchOptimizedGraphControls.tsx
  - frontend/src/components/relationships/** (shadow strip within this dir)
  - frontend/src/components/sla-countdown/SLACountdown.tsx
autonomous: true
requirements: [DEBT-01, DEBT-03, DEBT-05, DEBT-08]
must_haves:
  truths:
    - "Satisfies D-83-01 (graph half): MiniRelationshipGraph's ~20 hex (NODE_COLORS, EDGE_COLORS, fallbacks, markerEnd, Background dots) and relationships/RelationshipGraph's 6 edge hex + label fill resolve through var(--chart-N)/semantic tokens — zero raw hex outside comments in the slice"
    - "Satisfies D-83-08: SLACountdown's 5 status emoji, its pause/resume control-button glyphs (⏸ ~L258 / ▶ ~L256), and dossiers/RelationshipGraph's empty/error-state emoji are replaced with lucide icons at layout-neutral sizes; legitimate data emoji (flags, reaction sets) untouched"
    - 'Satisfies D-83-03 (slice share): zero shadow-(sm|md|xl|2xl) in dossier/dossiers/relationships/sla-countdown dirs; the AddToDossierMenu FABs keep a single static shadow-lg (allowed tier) with the banned tier + hover escalation removed; its tooltip poppers get surface-3 + hairline, no shadow'
    - "Satisfies D-83-05 (slice share): TouchOptimizedGraphControls' zoom-slider gradient from-primary to-primary/70 is flattened to bg-accent"
    - 'React Flow markerEnd.color + Background color receive var() values (RESEARCH A1); if arrowheads/dots render black or transparent on the dev render, the documented fallback (getComputedStyle-resolved values for exactly those two props) is applied and recorded'
  artifacts:
    - path: 'frontend/src/components/dossier/MiniRelationshipGraph.tsx'
      provides: 'NODE_COLORS/EDGE_COLORS on chart tokens; Background dots on var(--line)'
      contains: 'var(--chart-'
    - path: 'frontend/src/components/sla-countdown/SLACountdown.tsx'
      provides: 'getStatusIcon(): ReactNode returning lucide icons'
      contains: 'ReactNode'
  key_links:
    - from: 'frontend/src/components/dossier/MiniRelationshipGraph.tsx'
      to: 'design-system chart tokens'
      via: 'React Flow style objects + markerEnd/Background props'
      pattern: "var\\(--chart-"
    - from: 'frontend/src/components/sla-countdown/SLACountdown.tsx'
      to: 'lucide-react'
      via: 'TriangleAlert/CirclePause/Check/Zap/CircleAlert/Pause/Play imports'
      pattern: 'lucide-react'
---

<objective>
Migrate the graph + dossier slice: React Flow node/edge palettes onto chart tokens, graph-component
shadows stripped, the zoom-control gradient flattened, and the two DEBT-08 emoji sites replaced
with lucide icons.

Purpose: DEBT-01's graph half plus all of DEBT-08. File-disjoint from 83-03/05/06 — this plan OWNS
components/dossier/**, components/dossiers/**, components/relationships/\*\*, and sla-countdown/.
dossiers/RelationshipGraph.tsx has both hex and emoji debt — one owner (this plan) per RESEARCH.
Output: slice-scoped grep gates at zero for hex, banned shadows, gradients, emoji-as-UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/83-token-debt-consolidation/83-RESEARCH.md  (§Chart-Palette per-site map + file-by-file graph rows; §DEBT-03 policy items 1-2; §DEBT-05 TouchOptimizedGraphControls row; §DEBT-08 full spec; Pitfalls 5, 7; Assumption A1)
@.planning/phases/83-token-debt-consolidation/83-CONTEXT.md   (D-83-01, D-83-03, D-83-05, D-83-08)
@frontend/DESIGN.md  (§Dark/Light token tables, §Elevation & depth)

<interfaces>
MiniRelationshipGraph routing (RESEARCH file-by-file): NODE_COLORS country→var(--chart-1),
organization→var(--chart-7), person→var(--chart-3), forum→var(--chart-4), engagement→var(--chart-6),
working_group→var(--chart-2), topic→var(--accent-ink); all fallbacks→var(--ink-faint);
EDGE_COLORS same hue routing; Background dots #e5e7eb→var(--line) — FLAGGED pixel change: the
light-gray dots are a light-theme leftover that renders wrong in dark mode; the token FIXES it.
relationships/RelationshipGraph: reports_to→var(--danger), collaborates_with→var(--chart-1),
partner→var(--chart-3), colleague→var(--chart-4), other→var(--ink-faint);
#ffffff node-label fill→var(--accent-fg) (pixel-identical: #ffffff both modes).

SLACountdown emoji → lucide (size h-3.5 w-3.5 inline-block; color from existing badge classes):
warning-emoji isBreached→TriangleAlert; pause-emoji isPaused→CirclePause; check-mark >25%→Check;
lightning >10%→Zap; red-circle ≤10%→CircleAlert.
SLACountdown pause/resume control button (~L253-259, verified against the current file): the
pause glyph `⏸ {t('sla.pause')}` → Pause and the resume glyph `▶ {t('sla.resume')}` → Play,
both h-3 w-3 (matches the size-3 pending spinner rendered in the same slot), icon + label in
inline-flex items-center gap-1 — layout-neutral. U+23F8 (⏸) and U+25B6 (▶) sit BELOW the old
U+2600 gate floor; the widened gate is what makes them visible.
dossiers/RelationshipGraph: error-state warning emoji (text-2xl span) → TriangleAlert h-6 w-6
text-destructive; empty-state link emoji (text-3xl span) → Link2 h-8 w-8 text-muted-foreground.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Graph palettes → chart tokens (MiniRelationshipGraph + relationships/RelationshipGraph) + A1 check</name>
  <files>frontend/src/components/dossier/MiniRelationshipGraph.tsx, frontend/src/components/relationships/RelationshipGraph.tsx</files>
  <read_first>Both files (full read — locate NODE_COLORS/EDGE_COLORS/markerEnd/Background by identifier, not line number); 83-RESEARCH.md §Chart-Palette "Do CSS vars work in recharts / React Flow?" (A1 + fallback) and the two graph rows in the file-by-file list</read_first>
  <action>
    Apply the routing in the interfaces block. MiniRelationshipGraph: NODE_COLORS (7 dossier-type
    hues + neutral fallback), EDGE_COLORS (same hue routing), all hex fallbacks → var(--ink-faint),
    markerEnd.color and Background color prop → their routed tokens (dots → var(--line)).
    relationships/RelationshipGraph: the 5 edge-type colors + the #ffffff node-label fill →
    var(--accent-fg).
    A1 verification: React Flow forwards markerEnd.color and Background color into SVG presentation
    attributes; var() works in current browsers but this exact pair lacks in-repo precedent. After
    the edit, start the dev server and render a dossier relationships tab (browser-harness or
    agent-browser screenshot is fine): arrowheads and the dot grid must be COLORED, not
    black/transparent. If either fails, apply the documented fallback — resolve exactly those two
    props via getComputedStyle at mount — and record the deviation in the summary.
    Do NOT restructure the components; color-value swaps only (visually a controlled hue shift,
    layout-identical).
  </action>
  <verify>
    <automated>! rg -q '#[0-9a-fA-F]{3,6}\b' frontend/src/components/dossier/MiniRelationshipGraph.tsx frontend/src/components/relationships/RelationshipGraph.tsx && rg -q 'var\(--chart-' frontend/src/components/dossier/MiniRelationshipGraph.tsx && pnpm --dir frontend type-check</automated>
  </verify>
  <acceptance_criteria>Zero non-comment hex in both files; NODE_COLORS/EDGE_COLORS contain var(--chart-N) values with all 7 dossier types distinctly colored; Background dots use var(--line) (declared dark-mode bugfix — record in summary); a rendered-graph check of arrowheads + dots is documented (screenshot or computed-style note); type-check exit 0; lint clean on both files.</acceptance_criteria>
  <done>Both graphs fully token-driven; A1 verified or fallback applied.</done>
</task>

<task type="auto">
  <name>Task 2: Slice shadows + gradient — graph visualizations, AddToDossierMenu FAB judgment, dir-wide strip</name>
  <files>frontend/src/components/relationships/AdvancedGraphVisualization.tsx, frontend/src/components/relationships/EnhancedGraphVisualization.tsx, frontend/src/components/relationships/TouchOptimizedGraphControls.tsx, frontend/src/components/dossier/AddToDossierMenu.tsx, frontend/src/components/dossier/**, frontend/src/components/dossiers/**, frontend/src/components/relationships/** (every file in these dirs matching the banned-shadow grep)</files>
  <read_first>83-RESEARCH.md §DEBT-03 policy (items 1 "strip" and 2 "judgment list" — AddToDossierMenu rows) + §DEBT-05 TouchOptimizedGraphControls row; frontend/src/components/dossier/AddToDossierMenu.tsx (the FAB idiom shadow-md hover:shadow-lg / shadow-lg hover:shadow-xl and the tooltip poppers)</read_first>
  <action>
    Generate the in-slice strip list mechanically:
    `rg -n '\bshadow-(sm|md|xl|2xl)\b' frontend/src/components/dossier frontend/src/components/dossiers frontend/src/components/relationships frontend/src/components/sla-countdown`.
    Default disposition = DELETE the banned class (borders stay; shadow-lg alone is NOT banned —
    leave it). Top offenders: AdvancedGraphVisualization (11), EnhancedGraphVisualization (9).
    Judgment exceptions (RESEARCH §DEBT-03 item 2): AddToDossierMenu's floating FAB buttons — the
    `shadow-md hover:shadow-lg` / `shadow-lg hover:shadow-xl` idioms become a SINGLE static
    `shadow-lg` (remove the banned tier and the hover escalation); its tooltip poppers are popovers
    per DESIGN.md — `--surface-3` background + hairline border, NO shadow → strip.
    DEBT-05: TouchOptimizedGraphControls zoom-slider fill `from-primary to-primary/70` (with its
    bg-gradient-to-* class) → `bg-accent` flat.
  </action>
  <verify>
    <automated>! rg -q '\bshadow-(sm|md|xl|2xl)\b' frontend/src/components/dossier frontend/src/components/dossiers frontend/src/components/relationships frontend/src/components/sla-countdown && ! rg -q 'bg-gradient-' frontend/src/components/relationships && pnpm --dir frontend type-check</automated>
  </verify>
  <acceptance_criteria>Zero banned-shadow matches across the four slice dirs; AddToDossierMenu FABs each carry exactly one static shadow-lg and zero hover:shadow-* escalations (`rg 'hover:shadow' frontend/src/components/dossier/AddToDossierMenu.tsx` → 0); zero bg-gradient- in relationships/; type-check exit 0; lint clean on touched files.</acceptance_criteria>
  <done>Slice shadow discipline enforced; gradient flattened.</done>
</task>

<task type="auto">
  <name>Task 3: DEBT-08 — SLACountdown + dossiers/RelationshipGraph emoji → lucide</name>
  <files>frontend/src/components/sla-countdown/SLACountdown.tsx, frontend/src/components/dossiers/RelationshipGraph.tsx</files>
  <read_first>Both files (full read — getStatusIcon at ~L144-151, its render site ~L212, and the pause/resume control button ~L253-259; the error/empty-state spans ~L299/L312); 83-RESEARCH.md §DEBT-08 (replacement table + return-type note, Pitfall 5)</read_first>
  <action>
    SLACountdown.tsx: change `getStatusIcon(): string` to `getStatusIcon(): ReactNode` (import
    `type ReactNode` from react — the explicit-function-return-type ESLint rule REQUIRES the
    annotation update, Pitfall 5). Import from lucide-react: TriangleAlert, CirclePause, Check,
    Zap, CircleAlert, Pause, Play. Replace each emoji return per the interfaces table at size
    `className="h-3.5 w-3.5 inline-block"` (color comes from the existing badge classes — do not
    add color classes). Adjust the render site so icon + text keep their current spacing (wrap in
    an inline-flex with gap if needed — layout-neutral).
    Pause/resume control button (~L253-259): replace `⏸ {t('sla.pause')}` with
    `<Pause className="h-3 w-3" />` + the label and `▶ {t('sla.resume')}` with
    `<Play className="h-3 w-3" />` + the label (inline-flex items-center gap-1 — matches the
    size-3 pending spinner rendered in the same slot; layout-neutral).
    dossiers/RelationshipGraph.tsx: error-state span → TriangleAlert h-6 w-6 text-destructive;
    empty-state span → Link2 h-8 w-8 text-muted-foreground. Also migrate any raw hex in this file
    per the canonical hue-routing map (one-owner rule: this plan owns the file for BOTH debt classes).
    LEAVE all legitimate data emoji untouched: ReactionPicker/comment.types EMOJI_LIST,
    multilingual-content.types flag codepoints, lib/person-display.ts, JSDoc-only occurrences.
  </action>
  <verify>
    <automated>! rg -q '[\x{2300}-\x{27BF}\x{FE0F}\x{1F300}-\x{1FAFF}]' frontend/src/components/sla-countdown/SLACountdown.tsx frontend/src/components/dossiers/RelationshipGraph.tsx && rg -q 'ReactNode' frontend/src/components/sla-countdown/SLACountdown.tsx && pnpm --dir frontend type-check && pnpm --dir frontend lint</automated>
  </verify>
  <acceptance_criteria>Zero emoji/control-glyph codepoints in both files (the WIDENED unicode-range grep → 0 — the range now includes U+23F8 ⏸, U+25B6 ▶, and the U+FE0F variation selector, all of which the old U+2600 floor missed); getStatusIcon has explicit `: ReactNode` return type; lucide imports present (TriangleAlert, CirclePause, Check, Zap, CircleAlert, Pause, Play in SLACountdown; TriangleAlert, Link2 in RelationshipGraph); zero non-comment hex in dossiers/RelationshipGraph.tsx; type-check exit 0; lint clean (explicit-function-return-type satisfied); data-emoji carve-out files untouched (`git diff --name-only | grep -E 'ReactionPicker|comment.types|multilingual-content|person-display'` → empty).</acceptance_criteria>
  <done>Emoji-as-UI replaced with lucide; data emoji untouched; lint + types green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                              |
| -------- | ---------------------------------------------------------------------------------------- |
| none new | Class/constant/icon swaps in presentation components; no new inputs, deps, or data paths |

## STRIDE Threat Register

| Threat ID | Category  | Component          | Disposition | Mitigation Plan                                                                       |
| --------- | --------- | ------------------ | ----------- | ------------------------------------------------------------------------------------- |
| T-83-04   | DoS       | React Flow renders | mitigate    | A1 rendered-graph check for markerEnd/Background var() support + documented fallback  |
| T-83-SC   | Tampering | package installs   | accept      | No packages installed this phase (lucide-react already installed, project convention) |

</threat_model>

<verification>
- Slice-scoped gates: `rg '#[0-9a-fA-F]{3,6}\b' <slice dirs> -g '!**/__tests__/**'` → comment-only; `rg '\bshadow-(sm|md|xl|2xl)\b' <slice dirs>` → 0; `rg 'bg-gradient-' frontend/src/components/relationships` → 0; the widened emoji unicode-range grep (`[\x{2300}-\x{27BF}\x{FE0F}\x{1F300}-\x{1FAFF}]`) on the two DEBT-08 files → 0
- `pnpm --dir frontend lint && pnpm --dir frontend type-check && pnpm --dir frontend exec vitest run` — green (83-VALIDATION.md wave gate)
- Render-parity: graph hues + emoji→lucide + Background-dot dark-mode fix are DECLARED controlled changes; layout pixel-unchanged — final eyeball at the 83-07 gate
</verification>

<success_criteria>
DEBT-01 graph half done per D-83-01; DEBT-08 fully closed per D-83-08; slice shadows stripped per
D-83-03 with the FAB judgment applied; zoom-slider gradient flattened per D-83-05. Slice-scoped
grep gates at zero; suite green.
</success_criteria>

<output>
Create `.planning/phases/83-token-debt-consolidation/83-04-SUMMARY.md` when done.
</output>
