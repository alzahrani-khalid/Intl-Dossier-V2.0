---
phase: 83-token-debt-consolidation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tests/unit/design-system/contrast.test.ts
  - frontend/src/design-system/tokens/types.ts
  - frontend/src/design-system/tokens/directions.ts
  - frontend/src/design-system/tokens/buildTokens.ts
  - frontend/public/bootstrap.js
  - frontend/src/index.css
  - scripts/check-bootstrap-parity.mjs
  - frontend/tests/unit/design-system/buildTokens.test.ts (only if it enumerates emitted vars)
  - frontend/tests/unit/design-system/fouc-bootstrap.test.ts (only if it enumerates emitted vars)
autonomous: true
requirements: [DEBT-01]
must_haves:
  truths:
    - 'Satisfies D-83-01: a shared chart-palette token module exposing --chart-1 … --chart-8 semantic series colors exists, with dark + derived-light values that meet AA (≥3:1 non-text contrast vs --surface and --bg in both modes), wired into the design system exactly the way the status palette is (design-system/tokens/ + index.css @theme)'
    - 'Satisfies D-83-09: the chart tokens are ADDED to the three byte-matched palette holders (directions.ts, bootstrap.js, index.css :root) without mangling their existing Linear palette — every pre-existing line is byte-identical, and scripts/check-bootstrap-parity.mjs passes with its tables EXTENDED to enforce the new chart vars (all six touch-points land in one commit per RESEARCH Pitfall 3)'
    - '7 of 8 chart values are byte-copies of already-AA-proven Linear literals (chart-1..6 = status-1..6 fg, chart-8 = danger family); only chart-7 (violet h~300) is newly derived with culori and proven by contrast tests'
    - 'The @theme mapping --color-chart-N: var(--chart-N) auto-generates text-chart-N / bg-chart-N / fill-chart-N / stroke-chart-N utilities (same mechanism as --color-status-N) — no consumer migration happens in this plan (additive, zero visual change)'
  artifacts:
    - path: 'frontend/tests/unit/design-system/contrast.test.ts'
      provides: 'Wave-0 chart contrast assertions: --chart-1..8 ≥3:1 vs --surface and --bg, both modes'
      contains: 'chart'
    - path: 'frontend/src/design-system/tokens/directions.ts'
      provides: 'chart: [8 hex] arrays in PALETTES.linear.dark and .light'
      contains: 'chart'
    - path: 'frontend/public/bootstrap.js'
      provides: 'ES5 chart arrays + first-paint loop for --chart-1..8'
      contains: 'chart'
    - path: 'scripts/check-bootstrap-parity.mjs'
      provides: 'EXTENDED_PALETTE chart loop enforcing the three-copy invariant for chart tokens'
      contains: 'chart'
  key_links:
    - from: 'frontend/src/design-system/tokens/buildTokens.ts'
      to: '--chart-1 … --chart-8 CSS vars'
      via: 'emit loop mirroring statusVars'
      pattern: 'chart'
    - from: 'frontend/src/index.css'
      to: 'Tailwind chart utilities'
      via: '@theme --color-chart-N: var(--chart-N)'
      pattern: '--color-chart-1'
---

<objective>
Create the shared chart-palette token module (DEBT-01 keystone): 8 categorical slots
`--chart-1 … --chart-8` added to all three byte-matched palette holders, emitted by the token
engine, exposed as `@theme` utilities, enforced by the extended parity guard, and proven by
contrast tests. NO consumer migration here — this plan is purely additive with zero visual change.

Purpose: every Wave-2 chart/graph migration slice (83-03, 83-04) consumes these tokens. RESEARCH
Pitfall 3: the guard silently ignores unknown vars, so all six touch-points MUST land together or
three-copy drift begins unchecked.
Output: chart tokens live in dark+light, parity guard green with extended tables, contrast tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/83-token-debt-consolidation/83-RESEARCH.md  (§Chart-Palette Token Module — the values table, the six touch-points, Pitfall 3)
@.planning/phases/83-token-debt-consolidation/83-CONTEXT.md   (D-83-01, D-83-09)
@frontend/DESIGN.md  (§Dark tokens, §Light tokens — the status palette this mirrors)
@frontend/src/design-system/CLAUDE.md  (how to add tokens; three-copy invariant)
@frontend/src/design-system/tokens/directions.ts
@frontend/src/design-system/tokens/buildTokens.ts
@scripts/check-bootstrap-parity.mjs

<interfaces>
Locked chart values (RESEARCH §Chart-Palette Token Module — dark canonical, light derived):

| Token     | Dark     | Light    | Source                    |
| --------- | -------- | -------- | ------------------------- |
| --chart-1 | #87adfa  | #3458ac  | byte-copy status-1 fg     |
| --chart-2 | #2ac4cc  | #00737c  | byte-copy status-2 fg     |
| --chart-3 | #6ac48c  | #007338  | byte-copy status-3 fg     |
| --chart-4 | #cbaa4b  | #7c5700  | byte-copy status-4 fg     |
| --chart-5 | #ef9179  | #9d381f  | byte-copy status-5 fg     |
| --chart-6 | #d991d2  | #873a82  | byte-copy status-6 fg     |
| --chart-7 | (derive) | (derive) | NEW violet h~300, culori  |
| --chart-8 | #e86154  | #be241f  | byte-copy semantic.danger |

Duplicate hexes under a new token name are fine — the parity guard compares values per token, not
uniqueness. Consumers (Wave 2) will write `fill="var(--chart-1)"` / `className="text-chart-1"`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive --chart-7 + write the Wave-0 chart contrast assertions (RED)</name>
  <files>frontend/tests/unit/design-system/contrast.test.ts</files>
  <read_first>frontend/tests/unit/design-system/contrast.test.ts (the existing status-palette contrast cases — mirror their structure exactly), 83-RESEARCH.md §Chart-Palette Token Module (values table + "mirror the Phase-77 culori + contrast.test.ts discipline"), frontend/src/design-system/tokens/directions.ts (the status arrays whose shape the chart arrays copy, and the --surface/--bg values to test against)</read_first>
  <behavior>
    - For each of --chart-1..8: dark value has ≥3:1 WCAG contrast (non-text graphics, WCAG 1.4.11) against BOTH dark --surface and dark --bg
    - For each of --chart-1..8: light value has ≥3:1 against BOTH light --surface and light --bg
    - Assertions read the values from PALETTES.linear (the directions.ts source), not hardcoded duplicates, so the test doubles as a drift guard
  </behavior>
  <action>
    First derive the two --chart-7 hexes: violet hue ~300, one dark + one light value, using culori
    (already a devDependency, Phase-77 discipline) — adjust lightness until the value clears 3:1
    against both --surface and --bg for its mode. Record the derivation (a throwaway node script is
    fine; do not commit the script). Then add the 8 chart contrast cases to contrast.test.ts
    mirroring the existing status cases. Run the design-system unit tests — the chart cases MUST
    fail (PALETTES.linear has no chart arrays yet). That RED run is the Wave-0 proof. Commit as
    `test(83-02): add failing chart-palette contrast assertions`.
    NOTE: pre-commit runs the build, not vitest — a red unit test commits cleanly.
  </action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run tests/unit/design-system/contrast.test.ts 2>&1 | grep -qi "fail"</automated>
  </verify>
  <acceptance_criteria>contrast.test.ts contains 8 chart cases asserting ≥3:1 vs --surface AND --bg in both modes; the chart cases fail (RED) while all pre-existing status/semantic cases still pass; the two derived --chart-7 hexes are recorded in the test or a constant with a comment naming hue ~300 + culori derivation.</acceptance_criteria>
  <done>RED committed: chart contrast cases exist and fail for exactly the right reason (tokens absent).</done>
</task>

<task type="auto">
  <name>Task 2: Implement all six touch-points + extend the parity guard (GREEN)</name>
  <files>frontend/src/design-system/tokens/types.ts, frontend/src/design-system/tokens/directions.ts, frontend/src/design-system/tokens/buildTokens.ts, frontend/public/bootstrap.js, frontend/src/index.css, scripts/check-bootstrap-parity.mjs, frontend/tests/unit/design-system/buildTokens.test.ts, frontend/tests/unit/design-system/fouc-bootstrap.test.ts</files>
  <read_first>83-RESEARCH.md §Chart-Palette Token Module "Where they live" (the numbered six touch-points — follow verbatim), scripts/check-bootstrap-parity.mjs (the EXTENDED_PALETTE status loop to mirror), frontend/public/bootstrap.js (ES5-only contract — no arrows/const/template literals; the status paint loop to mirror), frontend/src/index.css (the :root fallback block + @theme block where --color-status-N lives), frontend/src/design-system/tokens/buildTokens.ts (statusVars emit loop)</read_first>
  <action>
    Implement the six touch-points in ONE commit (RESEARCH Pitfall 3 — partial landing starts
    unchecked drift):
    1. tokens/types.ts — add `chart: string[]` (exactly 8) to DirectionModePalette with JSDoc
       mirroring the `status` entry.
    2. tokens/directions.ts — `chart: [...]` arrays in PALETTES.linear.dark and .light using the
       locked values table (interfaces block above; chart-7 from Task 1).
    3. tokens/buildTokens.ts — emit loop mirroring statusVars: `--chart-1` … `--chart-8`.
    4. public/bootstrap.js — add `chart:[...]` to BOTH P.linear.light and P.linear.dark + an ES5
       first-paint loop (no arrows, no const, no template literals — file is ES5-safe by contract).
    5. index.css — (a) :root fallback block: `--chart-1: #87adfa;` … (dark literals, byte-matching
       directions.ts dark); (b) @theme block: `--color-chart-1: var(--chart-1);` …
       `--color-chart-8: var(--chart-8);` (same mechanism as --color-status-N).
    6. scripts/check-bootstrap-parity.mjs — extend EXTENDED_PALETTE with a chart loop mirroring the
       status loop (~4 lines): for i in 0..7 push `['--chart-{i+1}', p => p.chart && p.chart[i]]`.
    Do NOT modify any existing palette line in the three holders — additions only (D-83-09).
    Then check buildTokens.test.ts / fouc-bootstrap.test.ts: if either enumerates emitted var
    counts or lists, update the expectation; otherwise leave untouched.
    Run the contrast tests (GREEN), the parity guard, type-check, and lint.
    Commit as `feat(83-02): add --chart-1..8 palette to all three holders + guard + engine`.
  </action>
  <verify>
    <automated>node scripts/check-bootstrap-parity.mjs && pnpm --dir frontend exec vitest run tests/unit/design-system/ && pnpm --dir frontend type-check</automated>
  </verify>
  <acceptance_criteria>`node scripts/check-bootstrap-parity.mjs` exit 0 WITH the chart loop present in EXTENDED_PALETTE (grep 'chart' scripts/check-bootstrap-parity.mjs → match); all design-system unit tests green including the 8 chart contrast cases; `rg -c -- '--chart-8' frontend/src/index.css` ≥ 2 (once in :root, once via @theme --color-chart-8); `grep -c 'chart' frontend/public/bootstrap.js` ≥ 2 (both mode palettes); `git diff` on directions.ts/bootstrap.js/index.css shows ONLY added lines, zero modified existing palette lines; `pnpm --dir frontend lint` clean; `pnpm --dir frontend type-check` exit 0.</acceptance_criteria>
  <done>Chart tokens live in both modes; three-copy invariant enforced by the extended guard; utilities text-chart-N/bg-chart-N/fill-chart-N/stroke-chart-N available; zero visual change (no consumers yet).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                                  |
| -------- | -------------------------------------------------------------------------------------------- |
| none new | Additive CSS custom-property definitions; no new inputs, dependencies, or runtime code paths |

## STRIDE Threat Register

| Threat ID | Category  | Component                    | Disposition | Mitigation Plan                                                                                                      |
| --------- | --------- | ---------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------- |
| T-83-02   | Tampering | three-copy palette invariant | mitigate    | Guard tables extended in the SAME commit as the token additions; `node scripts/check-bootstrap-parity.mjs` in verify |
| T-83-SC   | Tampering | package installs             | accept      | No packages installed (culori already a devDependency; RESEARCH Package Legitimacy Audit: none)                      |

</threat_model>

<verification>
- `node scripts/check-bootstrap-parity.mjs` — exit 0 (chart vars enforced across all three copies)
- `pnpm --dir frontend exec vitest run tests/unit/design-system/` — green incl. 8 chart contrast cases
- `pnpm --dir frontend lint && pnpm --dir frontend type-check` — clean
- `pnpm --dir frontend build` — exit 0 (bootstrap.js ES5 parse + bundle)
- Zero visual change: no consumer files modified (`git diff --name-only` contains only the 6 touch-point files + tests)
</verification>

<success_criteria>
DEBT-01's token module exists per D-83-01 (8 slots, dark+light, AA-proven, status-palette wiring
pattern); three-copy byte-match preserved and now ENFORCED for chart vars (D-83-09); Wave-2 slices
can consume `var(--chart-N)` and the auto-generated Tailwind utilities.
</success_criteria>

<output>
Create `.planning/phases/83-token-debt-consolidation/83-02-SUMMARY.md` when done.
</output>
