---
phase: 100-security-posture
plan: 15
type: execute
wave: 4
depends_on: [100-09, 100-10, 100-11, 100-12, 100-13, 100-14]
files_modified:
  - .planning/phases/100-security-posture/100-VERIFICATION.md
  - .planning/phases/100-security-posture/100-15-SUMMARY.md
autonomous: false
requirements: [DBSEC-01, DBSEC-02, DBSEC-03, DBSEC-04, DBSEC-05, CLIENTSEC-01, CLIENTSEC-02]
success_criterion: 'The posture arithmetic holds at 33 views with 12 invoker-enabled and 12 client-readable, and the sign-off file 100-15-SIGNOFF.md (overseer under the operator''s delegation) is quoted verbatim in a register that consumes all fourteen predecessor summaries and carries three stated bounds in its closing language.'
routing:
  floor: frontier
truths:
  - 'Decisions covered - D-29: the five empty-on-staging views'' bound must appear in the CLOSING language and in whatever the phase hands the roadmap; D-30: the global posture numbers are ARITHMETIC DIAGNOSTICS and client_but_not_invoker is printed rather than graded, because a zero that sets the exit code is a criterion no matter what the prose calls it; D-37: the per-object proofs are P100-12, P100-13 AND P100-14, this task consumes all FOURTEEN predecessor summaries, and the untracked-consumer bound rides into the completion text; D-31: the leaked-password bound is carried into the closing language too; D-17: every plan depends on the phase directory and the annotated ROADMAP having been COMMITTED before the run started.'
must_haves:
  truths:
    - 'The register consumes ALL FOURTEEN predecessor summaries and its evidence rows name P100-12, P100-13 AND P100-14 as the per-object proofs - including P100-14''s seven-field policy rows, its two criterion-2 state rows and its three behavioural reads, which are presented at the checkpoint rather than being a dependency whose result never reached the register'
    - 'The register carries §4 of 100-CONTEXT.md VERBATIM and all THREE bounds in the closing language the phase hands the roadmap: the five empty-on-staging views each with ''establishes an unbroken read path, NOT row scoping''; the untracked-consumer bound naming the loud-and-reversible break the 21 revocations risk; criterion 5''s contingency VERBATIM - Criterion 5 is COVERED CONTINGENT ON TWO HUMAN GATES - the P100-09 leaked-password toggle and the P100-15 operator sign-off - and its ''advisors report clean'' clause closes by CALIBRATED PROXY (catalog = advisor at HEAD, to the unit), NOT by direct observation of the advisor after the change. - and D-31 VERBATIM, that the oracle establishes rejection with HTTP 422 and weak_password for a non-length reason and NOT that the rejection was BECAUSE the password is breached, since another weak_password rule would pass identically, the discriminator being unobservable until the setting is enabled - recorded as OPEN - DEFERRED TO TOGGLE rather than closed - plus the four roadmap numbers that did not reproduce with their commands, the fixture population being 402 rather than the 415 total-auth-users count'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; set -a; . ./.env.test 2>/dev/null; set +a; command -v psql >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: psql absent"; exit 3; }; [ -n "${SUPABASE_DB_URL:-}" ] || { echo "INSTRUMENT-CANNOT-RUN: SUPABASE_DB_URL unset - .env.test not materialised in this worktree"; exit 3; }; PQ() { PSQL_OUT=$(psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 -c "$1" 2>&1); PSQL_RC=$?; }; PQ_OK() { PQ "$1"; [ "$PSQL_RC" = "0" ] || { echo "INSTRUMENT-CANNOT-RUN: psql exited $PSQL_RC for $2 :: $PSQL_OUT"; exit 3; }; }; PQ_OK "select (select count(*) from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname=''public'' and c.relkind=''v'')||'' ''||(select count(*) from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname=''public'' and c.relkind=''v'' and ''security_invoker=true'' = any(coalesce(c.reloptions,''{}'')))||'' ''||(select count(*) from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname=''public'' and c.relkind=''v'' and (has_table_privilege(''anon'',c.oid,''SELECT'') or has_table_privilege(''authenticated'',c.oid,''SELECT'')))||'' ''||(select count(*) from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname=''public'' and c.relkind=''v'' and (has_table_privilege(''anon'',c.oid,''SELECT'') or has_table_privilege(''authenticated'',c.oid,''SELECT'')) and not (''security_invoker=true'' = any(coalesce(c.reloptions,''{}''))))" "the phase posture"; set -- $PSQL_OUT; A=$1; B=$2; C=$3; CN=$4; [ "$#" = "4" ] || { echo "INSTRUMENT-CANNOT-RUN: the phase posture probe returned $# fields, expected 4"; exit 3; };  case "$A" in *[!0-9]*|"") echo "INSTRUMENT-CANNOT-RUN: the view-population field of the phase posture probe returned [$A], not a number"; exit 3;; esac;  case "$B" in *[!0-9]*|"") echo "INSTRUMENT-CANNOT-RUN: the invoker-count field of the phase posture probe returned [$B], not a number"; exit 3;; esac;  case "$C" in *[!0-9]*|"") echo "INSTRUMENT-CANNOT-RUN: the client-readable-count field of the phase posture probe returned [$C], not a number"; exit 3;; esac;  case "$CN" in *[!0-9]*|"") echo "INSTRUMENT-CANNOT-RUN: the client-but-not-invoker field of the phase posture probe returned [$CN], not a number"; exit 3;; esac; echo "P100-15-POSTURE views=$A invoker=$B client_readable=$C  [DIAGNOSTIC, NOT GRADED] client_but_not_invoker=$CN"; echo "P100-15-EXPECT views=33 invoker=12 client_readable=12  (the per-object proofs are P100-12, P100-13 and P100-14)"; [ "$A" = "33" ] || { echo "INSTRUMENT-CANNOT-RUN: public holds $A views, not the 33 this phase disposed of - the population moved under the probe"; exit 3; }; R=0; [ "$B" = "12" ] || { echo "FAIL: $B views carry security_invoker=true, expected the 12 with an authenticated caller"; R=1; }; [ "$C" = "12" ] || { echo "FAIL: $C views remain client-readable, expected 12 (33 minus the 21 restricted)"; R=1; }; [ "$R" = "0" ] && echo "PASS posture arithmetic (client_but_not_invoker printed above is cross-check arithmetic, not a criterion)"; exit $R'
      text: 'the phase arithmetic, asserted once because the plans that produced it ran concurrently. The GRADED values are positive - 33 views, 12 carrying security_invoker, 12 still client-readable - and client_but_not_invoker is PRINTED beside them without touching the exit code, because a zero that sets the exit code is a criterion whatever the prose calls it. The per-object proofs live in P100-12, P100-13 and P100-14. The probe exits 3 if public no longer holds 33 views.'
    - oracle: judge
      text: 'The register is honest: every green names an instrument run fresh in this task or in P100-12/13/14, every zero shows its control, all three bounds appear in the closing language and not only in prose, P100-14''s evidence is present rather than merely depended on, and no criterion is recorded closed on a worker''s assertion where the evidence is an operator attestation.'
---

<objective>
Assemble the phase's evidence register and take the operator sign-off.

**The per-object proofs are P100-12's, P100-13's AND P100-14's** - all three, freshly executed in wave 3.
An earlier draft of this task named only two and asked for "thirteen SUMMARYs" when there are fourteen
predecessor plans; P100-14's policy rows, criterion-2 state and behavioural reads were a dependency whose
result never reached the register. They do now, and they are presented at the checkpoint.

What this task adds on its own is the arithmetic that ties them together - 33 views, 12 converted, 12
still client-readable, 21 restricted. **`client_but_not_invoker` is PRINTED, not graded**: a zero called
diagnostic while it sets the exit code is still an absence acting as a criterion.

**Three bounds must be VISIBLE after this phase closes**, in the register AND in the completion text the
phase hands the roadmap - not only inside a plan's prose. Phase 92's failure was not the gap; it was the
gap being invisible afterwards.
</objective>

<context>
@.planning/phases/100-security-posture/100-CONTEXT.md
@.planning/phases/100-security-posture/100-RESEARCH.md

Ref notes: 100-CONTEXT.md - all of D-01..D-37, §3 (out of scope) and §4 (what this phase does NOT establish, carried forward verbatim); 100-RESEARCH.md - §0 (the criterion->plan map for the fifteen-plan graph), §1 (end-state targets), §7 (the roadmap numbers that did not reproduce, including the derived 402 fixture accounts).

<interfaces>

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>the register, the posture arithmetic, and the operator sign-off</name>
  <files>the paths in files_modified</files>
  <read_first>100-CONTEXT.md D-01..D-37 (ALL of them - a task that cites D-19 or later needs the whole range) and 100-RESEARCH.md §0-§11 (the round-3 and round-4 derivations live in §10 and §11); re-RUN a recorded command rather than re-deriving its number a second way</read_first>
  <action>
    Assemble `100-VERIFICATION.md` from ALL FOURTEEN predecessor SUMMARYs plus this task's own fresh
    arithmetic run. The evidence rows come from the three wave-3 re-proofs:

    - **P100-12** - grant rows for the twenty restricted views and the twelve materialized views, every
      grantee rendered; the twelve invoker flags.
    - **P100-13** - the function configuration with its 35-signature digest; the final caller census.
    - **P100-14** - the two seven-field policy rows with their expressions; the two criterion-2 state rows;
      the three behavioural reads.

    Then:

    - A criterion -> plan -> oracle -> observed-result table covering all seven roadmap criteria.
    - Every zero beside the control that proves its instrument could have seen a non-zero. A zero without
      its control is recorded as an unproven line, not a green.
    - **THE THREE BOUNDS, in the register AND in the completion text this phase hands the ROADMAP:**
      1. The five empty-on-staging views - `event_details`, `theme_details`,
         `relationship_health_summary`, `v_country_relationship_flows`,
         `engagement_recommendations_summary` - each with the sentence *"establishes an unbroken read path,
         NOT row scoping"*.
      2. The 21 revocations were validated against TRACKED REPOSITORY SOURCES ONLY. A consumer outside the tree - a BI tool, a Retool app, a saved dashboard query - would not have appeared in the census. The break, if it occurs, is LOUD (permission denied) and REVERSIBLE (re-GRANT). The operator was asked before the revocations landed.
      3. Criterion 5's contingency, VERBATIM: *"Criterion 5 is COVERED CONTINGENT ON TWO HUMAN GATES - the P100-09 leaked-password toggle and the P100-15 operator sign-off - and its "advisors report clean" clause closes by CALIBRATED PROXY (catalog = advisor at HEAD, to the unit), NOT by direct observation of the advisor after the change."* A COVERED verdict travels; its contingency
         must travel with it.
      4. D-31, VERBATIM: *"The leaked-password oracle establishes that a known breached password is
         rejected with HTTP 422 and error code `weak_password` for a non-`length` reason. It does NOT
         establish that it was rejected BECAUSE it is breached - another `weak_password` rule rejecting the
         same password would pass identically. The breach-specific discriminator is unobservable until
         leaked-password protection is enabled; see the operator handoff."* Record its status as
         **OPEN - DEFERRED TO TOGGLE**, not closed, and name the FIVE post-toggle steps P100-09 owns.
    - The roadmap numbers that did not reproduce (100-RESEARCH.md §7), each with its command: five
      persisted stores where the criterion says six; `unified_work_items` queried from two frontend files
      where DBSEC-01 says ten; 304 map files where the roadmap says 305; and the fixture-account population,
      **402** (338 + 64), where an earlier draft carried ~415 - which is the total `auth.users` count, a
      different population.
    - §4 of 100-CONTEXT.md carried forward VERBATIM.
    - The residue carried onward: to **Phase 101**, that nothing runs `get_advisors` on a schedule, so a
      later leaked-password toggle-off is DETECTABLE but not DETECTED; to **Phase 102**, the
      migration-ledger divergence and the 402 fixture accounts; to **Phase 103**, the 670 executable-grant
      and 4 extension-in-public advisor warnings.

    Every green must come from this task or from P100-12, P100-13 or P100-14, which ran after everything
    landed. Where a number can only be carried, say which plan produced it and when.

    If any criterion is not met, the register says which and the phase does not close - it parks by name.
  </action>
  <acceptance_criteria>The arithmetic oracle passes on a run made in this task, the register consumes all fourteen predecessor summaries and carries the three bounds in its closing language, and the sign-off file 100-15-SIGNOFF.md is quoted verbatim in the register (it answers the checkpoint in writing.</acceptance_criteria>
  <done>The posture arithmetic reads 33 views with 12 invoker-enabled and 12 client-readable, the register carries evidence from all three wave-3 re-proofs and the three bounds in the phase's closing language, and the sign-off file 100-15-SIGNOFF.md is quoted verbatim in the register (it answers the checkpoint.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Operator sign-off on the Phase 100 security posture</name>
  <files>none - the checkpoint edits nothing</files>
  <action>
    Present to the operator, from ALL THREE wave-3 re-proofs:
    - P100-12: the per-view and per-materialized-view grant rows with every grantee rendered, and the
      twelve invoker flags.
    - P100-13: the function configuration with its 35-signature digest, and the caller census
      owner=21 non-owner=2 with its HEAD baseline of 21/21 beside it.
    - P100-14: the two seven-field policy rows including their expressions, the two criterion-2 view
      state rows, AND the three behavioural reads.
    Plus P100-09's three-arm probe with the attestation naming who enabled the setting and the
    operator's answer on the D-31 bound; P100-10's storage-equality tests; and P100-11's config-branch
    values and manifest walk.

    Say out loud, in the words the register uses:
    - FIVE of the twelve converted views hold zero rows on staging, so for those five criterion 1
      establishes an unbroken read path and NOT row scoping. Criterion 1 COVERED does not mean row
      scoping verified for all twelve.
    - The 21 revocations were validated against TRACKED REPOSITORY SOURCES ONLY. A BI tool, a Retool app
      or a saved dashboard query would not have appeared in the census. The break, if it occurs, is
      LOUD (permission denied) and REVERSIBLE (re-GRANT).
    - Criterion 5 is COVERED CONTINGENT ON TWO HUMAN GATES - this checkpoint and the P100-09 toggle -
      and its "advisors report clean" clause closes by CALIBRATED PROXY, not by direct observation of
      the advisor after the change.
    - D-31, verbatim: the oracle establishes rejection with HTTP 422 and weak_password for a
      non-length reason. It does NOT establish rejection BECAUSE the password is breached - another
      weak_password rule would pass identically. Status: CLOSED - the operator enabled the toggle on 2026-09-10 and the rejection now carries reasons=[pwned] (100-09-ATTESTATION.md); the register records CLOSED.

    Ask for the phase sign-off. DECIDING rests with the overseer/operator in writing: the answer is `.planning/phases/100-security-posture/100-15-SIGNOFF.md` (overseer, under the operator's standing delegation of 2026-09-10) - the worker quotes it VERBATIM in the register under "Sign-off (quoted)", including its fifth bound (the defective dossier_relationships policy) and its statement that the operator's personal review is still owed; this checkpoint
    blocks until released through the ruled two-column process. No worker, orchestrator, or engine
    answers it.
  </action>
  <done>The operator has seen all three wave-3 re-proofs, the three stated bounds in the register's own words, and the carried findings, and has signed off in writing - or has named what must change.</done>
</task>

</tasks>

<verification>
POPULATION: the two planning artifacts in files_modified. OUTSIDE IT: every source, migration and
frontend file (a diff outside .planning here is a scope defect), the ROADMAP and REQUIREMENTS row flips
(the overseer's close-out acts, never a plan's), and every residue this register names as carried to
Phase 101, 102 or 103 - naming it is this plan's job, closing it is not.
</verification>

<success_criteria>
The posture arithmetic holds at 33 views with 12 invoker-enabled and 12 client-readable, and the sign-off file 100-15-SIGNOFF.md (overseer under the operator's delegation) is quoted verbatim in a register that consumes all fourteen predecessor summaries and carries three stated bounds in its closing language.
</success_criteria>

<output>
Create `.planning/phases/100-security-posture/100-15-SUMMARY.md` when done. It MUST record every command this
task ran with its VERBATIM output, the populations it re-derived with the commands that produced them, every
zero beside the control that proves the instrument could have seen a non-zero, and anything left for a named
later task. It MUST record the checkpoint answer verbatim with who gave it, the three bounds, and the residue carried to P101/P102/P103.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete`.
Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as
**pending** no matter how many gates passed (measured on P99-32 and again on P99-61).
</output>
