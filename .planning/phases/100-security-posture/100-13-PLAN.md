---
phase: 100-security-posture
plan: 13
type: execute
wave: 3
depends_on: [100-01, 100-02, 100-03, 100-04, 100-05, 100-06, 100-07, 100-08, 100-16, 100-17]
files_modified:
  - .planning/phases/100-security-posture/100-13-SUMMARY.md
  - .planning/phases/100-security-posture/100-VERIFICATION-STATE.md
autonomous: true
requirements: [DBSEC-05]
success_criterion: 'Fresh runs after every change confirm population-minus-35 functions carry their approved search_path with the same 35 pinned elsewhere by signature digest, and the caller census is unmoved at owner 21 and non-owner 2.'
routing:
  floor: frontier
truths:
  - 'Decisions covered - D-16: the criterion-1 census is re-run after every change so a moved boundary is attributable; D-22: 35 functions keep an explicit search_path this phase did not set, so the approved count is the population minus 35 (556 at planning, 557 after P100-17 added one pinned definer; derived live, never frozen - OVERSEER-RULING P100-13); D-36: those 35 are pinned by signature digest, because bucket sizes survive a balanced swap and identities do not; D-28: every number here comes from a run made in THIS task; D-37: this is one of three wave-3 re-proofs and P100-15 owns the register and the sign-off.'
must_haves:
  truths:
    - 'The SUMMARY compares this task''s caller-census run against the one P100-08 recorded and states whether they agree - and if they disagree, it names both runs and identifies the change that landed between them rather than reporting only the later number'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; set -a; . ./.env.test 2>/dev/null; set +a; command -v psql >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: psql absent"; exit 3; }; [ -n "${SUPABASE_DB_URL:-}" ] || { echo "INSTRUMENT-CANNOT-RUN: SUPABASE_DB_URL unset - .env.test not materialised in this worktree"; exit 3; }; PQ() { PSQL_OUT=$(psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 -c "$1" 2>&1); PSQL_RC=$?; }; PQ_OK() { PQ "$1"; [ "$PSQL_RC" = "0" ] || { echo "INSTRUMENT-CANNOT-RUN: psql exited $PSQL_RC for $2 :: $PSQL_OUT"; exit 3; }; }; PQ_OK "with f as (select p.oid::regprocedure::text sig, n.nspname, (select x from unnest(coalesce(p.proconfig,''{}'')) x where x like ''search\_path=%'') cfg from pg_proc p join pg_namespace n on n.oid=p.pronamespace where n.nspname in (''public'',''read_models'',''events'') and p.prokind=''f'' and not exists (select 1 from pg_depend d where d.objid=p.oid and d.deptype=''e'')), a as (select sig, cfg, case nspname when ''public'' then ''search_path=public, pg_temp'' else ''search_path=''||nspname||'', public, pg_temp'' end want from f), o as (select sig, cfg from a where cfg is not null and cfg <> want) select (select count(*) from a where cfg = want)||'' ''||(select count(*) from o)||'' ''||(select count(*) from a where cfg is null)||'' ''||(select count(*) from a)||'' ''||coalesce((select md5(string_agg(sig||''=>''||cfg, chr(10) order by sig)) from o),''EMPTY'')" "the search_path configuration census"; set -- $PSQL_OUT; APPROVED=$1; OTHER=$2; UNPINNED=$3; TOTAL=$4; DIGEST=$5; case "$DIGEST" in *[!0-9a-f]*|"") echo "INSTRUMENT-CANNOT-RUN: the identity-digest field of the search_path configuration census returned [$DIGEST], not an md5"; exit 3;; esac; [ "$#" = "5" ] || { echo "INSTRUMENT-CANNOT-RUN: the search_path configuration census returned $# fields, expected 5 (four counts and the identity digest)"; exit 3; };  case "$APPROVED" in *[!0-9]*|"") echo "INSTRUMENT-CANNOT-RUN: the at-approved field of the search_path configuration census returned [$APPROVED], not a number"; exit 3;; esac;  case "$OTHER" in *[!0-9]*|"") echo "INSTRUMENT-CANNOT-RUN: the other-bucket field of the search_path configuration census returned [$OTHER], not a number"; exit 3;; esac;  case "$UNPINNED" in *[!0-9]*|"") echo "INSTRUMENT-CANNOT-RUN: the unpinned field of the search_path configuration census returned [$UNPINNED], not a number"; exit 3;; esac;  case "$TOTAL" in *[!0-9]*|"") echo "INSTRUMENT-CANNOT-RUN: the population field of the search_path configuration census returned [$TOTAL], not a number"; exit 3;; esac; echo "P100-SEARCHPATH at_approved=$APPROVED other=$OTHER unpinned=$UNPINNED population=$TOTAL other_identity_digest=$DIGEST"; echo "P100-SEARCHPATH-EXPECT at_approved=$((TOTAL-35)) other=35 unpinned=0 population=$TOTAL (derived: population minus 35, floor 591) other_identity_digest=a10d3e1256979bf019d8a9b68c959cb8"; [ "$TOTAL" -ge "591" ] 2>/dev/null || { echo "INSTRUMENT-CANNOT-RUN: the non-extension function population is $TOTAL, below the planning floor 591 - the 591 measured at HEAD - it moved under the probe"; exit 3; }; [ "$DIGEST" = "a10d3e1256979bf019d8a9b68c959cb8" ] || { echo "FAIL: the 35 functions pinned to another value are not the SAME 35 - their signature-to-configuration digest is $DIGEST, expected a10d3e1256979bf019d8a9b68c959cb8. A balanced swap that keeps the counts at (population-35)/35 changes this digest and is caught here"; exit 1; }; [ "$OTHER" = "35" ] || { echo "FAIL: $OTHER functions carry a search_path other than the approved value, expected the same 35 this phase must not touch"; exit 1; }; [ "$APPROVED" = "$((TOTAL-35))" ] || { echo "FAIL: $APPROVED functions carry the approved explicit search_path for their schema; expected population minus 35 (the 548 this task pins plus the 8 already at that value)"; exit 1; }; echo "PASS search_path configuration"'
      text: 'the function configuration re-proven against the FINAL tree, pinned by identity as well as size: population minus 35 (557 as of P100-17) at the approved value, 0 unpinned, and the 35 pinned elsewhere hashing to the digest recorded at HEAD, so a balanced swap between buckets fails here even though every count would still read population-minus-35/35.'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; set -a; . ./.env.test 2>/dev/null; set +a; command -v psql >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: psql absent"; exit 3; }; [ -n "${SUPABASE_DB_URL:-}" ] || { echo "INSTRUMENT-CANNOT-RUN: SUPABASE_DB_URL unset - .env.test not materialised in this worktree"; exit 3; }; DBQ() { DBQ_OUT=$(psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 -c "$1" 2>&1); DBQ_RC=$?; DBQ=$(printf ''%s'' "$DBQ_OUT" | tail -1); }; resolve_uid() { DBQ "select id from auth.users where email = ''$1''"; [ "$DBQ_RC" = "0" ] || { echo "INSTRUMENT-CANNOT-RUN: the $2 identity lookup for $1 exited $DBQ_RC :: $DBQ_OUT"; exit 3; }; RU=$DBQ; case "$RU" in *[!0-9a-f-]*|"") echo "INSTRUMENT-CANNOT-RUN: the $2 identity ($1) did not resolve to a uuid (got [$RU])"; exit 3;; esac; }; census_count() { CC_OUT=$(printf ''begin;\nset local role authenticated;\nset local request.jwt.claims = %s;\nselect count(*) from %s;\ncommit;\n'' "''{\"sub\":\"$1\",\"role\":\"authenticated\"}''" "$2" | psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 2>&1); CC_RC=$?; CC=$(printf ''%s'' "$CC_OUT" | tail -1); if [ "$CC_RC" != "0" ]; then case "$CC_OUT" in *"permission denied for"*) echo "FAIL: the $3 caller is DENIED $2 - the read path this criterion depends on is gone"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: the $3 census of $2 exited $CC_RC :: $CC_OUT"; exit 3;; esac; fi; case "$CC" in *[!0-9]*|"") echo "INSTRUMENT-CANNOT-RUN: the $3 census of $2 returned [$CC], not a count"; exit 3;; esac; }; COUNT1() { DBQ "$1"; [ "$DBQ_RC" = "0" ] || { echo "INSTRUMENT-CANNOT-RUN: the $2 catalog probe exited $DBQ_RC :: $DBQ_OUT"; exit 3; }; case "$DBQ" in *[!0-9]*|"") echo "INSTRUMENT-CANNOT-RUN: the $2 catalog probe returned [$DBQ], not a count"; exit 3;; esac; }; resolve_uid kazahrani@stats.gov.sa owner; OWN=$RU; resolve_uid test.user@gmail.com non-owner; OTH=$RU; census_count "$OWN" unified_work_items owner; NO=$CC; census_count "$OTH" unified_work_items non-owner; NT=$CC; echo "P100-CENSUS unified_work_items owner=$NO other=$NT expected owner=21 other=2"; [ "$NO" = "21" ] || { echo "FAIL: owner census $NO, expected 21 - the legitimate caller lost rows"; exit 1; }; [ "$NT" = "2" ] || { echo "FAIL: non-owner census $NT, expected 2 - unified_work_items still bypasses RLS"; exit 1; }; echo "PASS census"'
      text: 'the final attribution run: after every conversion, revocation, policy and 548 search_path pins, a non-owner must still read exactly 2 rows from unified_work_items and the owner exactly 21. The same instrument, unchanged, read 21/21 at HEAD.'
    - oracle: judge
      text: '100-VERIFICATION-STATE.md carries only output produced in this task, each zero stands beside its control, and the census comparison against P100-08''s run is stated explicitly rather than left for a reader to notice.'
---

<objective>
Re-prove the function configuration, fresh, after every migration has landed - and re-run the
criterion-1 caller census one final time. The policy rows, the criterion-2 view state and the two
predecessor behavioural oracles are re-proven by P100-14, which runs beside this task in wave 3.

This is the phase's ATTRIBUTION point. The census read owner=21 / non-owner=21 at HEAD and must read
owner=21 / non-owner=2 now. P100-08 already re-ran it immediately after the search_path pinning; this
run is after everything, so if the two disagree the phase can name which change moved it.
</objective>

<context>
@.planning/phases/100-security-posture/100-CONTEXT.md
@.planning/phases/100-security-posture/100-RESEARCH.md

Ref notes: 100-CONTEXT.md - D-16, D-22, D-36, D-28, D-37; 100-RESEARCH.md - §4 (the census instrument and its HEAD drill), §9.5 (the per-schema census and the derived digest).

<interfaces>

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>the function configuration and the caller census are re-proven fresh</name>
  <files>the paths in files_modified</files>
  <read_first>100-CONTEXT.md D-01..D-37 (ALL of them - a task that cites D-19 or later needs the whole range) and 100-RESEARCH.md §0-§11 (the round-3 and round-4 derivations live in §10 and §11); re-RUN a recorded command rather than re-deriving its number a second way</read_first>
  <action>
    Run the two oracles below in this task, in one session, and record every line of their output verbatim
    into `100-VERIFICATION-STATE.md` alongside the SUMMARY.

    If the caller census disagrees with the run P100-08 recorded, say so explicitly and name both runs. That
    would mean a change landing after P100-08 moved the authorization boundary, and it is the single most
    important thing this task could find.

    Do NOT quote a number from a sibling SUMMARY.
  </action>
  <acceptance_criteria>Both oracles pass on runs made in this task and their verbatim output is recorded in both files.</acceptance_criteria>
  <done>Fresh runs in this task show population-minus-35 functions at the approved search_path with the 35 others matching their recorded signature digest, zero unpinned, and the caller census at owner=21 non-owner=2.</done>
</task>

</tasks>

<verification>
POPULATION: this plan's SUMMARY and the fresh oracle runs it records. OUTSIDE IT: every migration and
source file (a diff outside .planning here is a scope defect), the ROADMAP and REQUIREMENTS row flips
(the overseer's close-out acts, never a plan's), the register and the sign-off (P100-15 owns both, D-37),
and every residue this
phase names as carried to Phase 101, 102 or 103 - naming it is a job, closing it is not.
</verification>

<success_criteria>
Fresh runs after every change confirm population-minus-35 functions carry their approved search_path with the same 35 pinned elsewhere by signature digest, and the caller census is unmoved at owner 21 and non-owner 2.
</success_criteria>

<output>
Create `.planning/phases/100-security-posture/100-13-SUMMARY.md` when done. It MUST record every command this
task ran with its VERBATIM output, the populations it re-derived with the commands that produced them, every
zero beside the control that proves the instrument could have seen a non-zero, and anything left for a named
later task. It MUST carry the census comparison against P100-08's recorded run.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete`.
Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as
**pending** no matter how many gates passed (measured on P99-32 and again on P99-61).
</output>
