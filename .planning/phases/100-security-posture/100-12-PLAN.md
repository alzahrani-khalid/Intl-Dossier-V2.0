---
phase: 100-security-posture
plan: 12
type: execute
wave: 3
depends_on: [100-01, 100-02, 100-03, 100-04, 100-05, 100-06, 100-07, 100-08, 100-16]
files_modified:
  - .planning/phases/100-security-posture/100-12-SUMMARY.md
  - .planning/phases/100-security-posture/100-VERIFICATION-GRANTS.md
autonomous: true
requirements: [DBSEC-01, DBSEC-03]
success_criterion: 'Fresh runs after every change confirm the twenty restricted views and the twelve materialized views render postgres and service_role as their only grantees, and the twelve converted views are still invoker-enabled.'
routing:
  floor: frontier
truths:
  - 'Decisions covered - D-15: the eight database plans asserted per-object because they ran concurrently, so nothing has yet observed the FINAL state; D-28: a green produced before a later change is not evidence about the state after it, so every row here comes from a run made in THIS task; D-32: the grant assertions render EVERY grantee and pair them with has_table_privilege; D-37: this task is one of THREE wave-3 re-proofs (100-12 grants, 100-13 functions and census, 100-14 policies and criterion-2 plus the behavioural pair), and P100-15 owns the register and the sign-off.'
must_haves:
  truths:
    - 'Every row recorded in 100-VERIFICATION-GRANTS.md comes from an oracle run made in THIS task with the control printed beside it, the file states plainly that no number in it was quoted forward from a sibling plan''s SUMMARY, AND it carries the untracked-consumer bound verbatim: the 21 revocations were validated against tracked repository sources only, a BI tool, a Retool app or a saved dashboard query would not have appeared in the census, and the break it risks is loud (permission denied) and reversible (re-GRANT)'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; set -a; . ./.env.test 2>/dev/null; set +a; command -v psql >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: psql absent"; exit 3; }; [ -n "${SUPABASE_DB_URL:-}" ] || { echo "INSTRUMENT-CANNOT-RUN: SUPABASE_DB_URL unset - .env.test not materialised in this worktree"; exit 3; }; PQ() { PSQL_OUT=$(psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 -c "$1" 2>&1); PSQL_RC=$?; }; PQ_OK() { PQ "$1"; [ "$PSQL_RC" = "0" ] || { echo "INSTRUMENT-CANNOT-RUN: psql exited $PSQL_RC for $2 :: $PSQL_OUT"; exit 3; }; }; PQ_OK "with a as (select c.relname, coalesce((select string_agg(case when x.grantee=0 then ''PUBLIC'' else pg_get_userbyid(x.grantee) end||''=''||x.privilege_type, '','' order by case when x.grantee=0 then ''PUBLIC'' else pg_get_userbyid(x.grantee) end, x.privilege_type) from aclexplode(c.relacl) x),''(null-acl)'') acl, has_table_privilege(''anon'',c.oid,''SELECT'') an, has_table_privilege(''authenticated'',c.oid,''SELECT'') au from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname=''public'' and c.relkind=''v'' and c.relname in (''link_audit_logs_archival_eligible'',''citation_statistics'',''user_work_summary'',''ai_usage_summary'',''user_ai_usage'',''audit_logs_active'',''ai_interaction_summary'',''intelligence_cache_status'',''engagement_briefs'',''embedding_queue_stats'',''recent_field_changes'',''sla_compliance_by_assignee'',''engagement_analytics'',''commitment_analytics'',''work_item_analytics'',''top_contributors'',''user_digest_content_summary'',''v_dossier_extension_health'',''resolved_field_permissions'',''stakeholder_timeline_unified'')) select relname||'' | anon_can_select=''||an||'' authenticated_can_select=''||au||'' | ''||acl from a order by relname" "the P100-12-VIEWS grant census"; ROWS=$(printf ''%s\n'' "$PSQL_OUT" | command grep -c '' | ''); GOOD=$(printf ''%s\n'' "$PSQL_OUT" | command grep -c '' | anon_can_select=false authenticated_can_select=false | postgres=DELETE,postgres=INSERT,postgres=MAINTAIN,postgres=REFERENCES,postgres=SELECT,postgres=TRIGGER,postgres=TRUNCATE,postgres=UPDATE,service_role=DELETE,service_role=INSERT,service_role=MAINTAIN,service_role=REFERENCES,service_role=SELECT,service_role=TRIGGER,service_role=TRUNCATE,service_role=UPDATE$''); echo "P100-12-VIEWS named=20 rendered=$ROWS at_expected_full_state=$GOOD expected rendered=20 at_expected_full_state=20"; printf ''%s\n'' "$PSQL_OUT" | sed ''s/^/  GRANTS /''; [ "$ROWS" = "20" ] || { echo "INSTRUMENT-CANNOT-RUN: 20 names were asked for and the catalog rendered $ROWS rows - the name list is stale, so any verdict below is about a different population"; exit 3; }; [ "$GOOD" = "20" ] || { echo "FAIL: $GOOD of 20 relations render the exact expected end state - EVERY grantee listed (postgres and service_role only, no PUBLIC and no client role) AND has_table_privilege false for both anon and authenticated"; exit 1; }; echo "PASS grants"'
      text: 'the twenty restricted views re-proven against the FINAL tree with EVERY grantee rendered and has_table_privilege paired beside it, so a PUBLIC grant added after their own plan ran cannot hide behind a filtered row.'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; set -a; . ./.env.test 2>/dev/null; set +a; command -v psql >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: psql absent"; exit 3; }; [ -n "${SUPABASE_DB_URL:-}" ] || { echo "INSTRUMENT-CANNOT-RUN: SUPABASE_DB_URL unset - .env.test not materialised in this worktree"; exit 3; }; PQ() { PSQL_OUT=$(psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 -c "$1" 2>&1); PSQL_RC=$?; }; PQ_OK() { PQ "$1"; [ "$PSQL_RC" = "0" ] || { echo "INSTRUMENT-CANNOT-RUN: psql exited $PSQL_RC for $2 :: $PSQL_OUT"; exit 3; }; }; PQ_OK "with a as (select c.relname, coalesce((select string_agg(case when x.grantee=0 then ''PUBLIC'' else pg_get_userbyid(x.grantee) end||''=''||x.privilege_type, '','' order by case when x.grantee=0 then ''PUBLIC'' else pg_get_userbyid(x.grantee) end, x.privilege_type) from aclexplode(c.relacl) x),''(null-acl)'') acl, has_table_privilege(''anon'',c.oid,''SELECT'') an, has_table_privilege(''authenticated'',c.oid,''SELECT'') au from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname=''public'' and c.relkind=''m'' and c.relname in (''citation_network'',''aa_commitment_summary_by_dossier'',''sla_compliance_metrics'',''user_productivity_metrics'',''dossier_engagement_stats'',''dossier_commitment_stats'',''relationship_engagement_stats'',''relationship_commitment_stats'',''mv_tag_usage_analytics'',''team_entity_stats'',''dossier_list_mv'',''stakeholder_network_summary'')) select relname||'' | anon_can_select=''||an||'' authenticated_can_select=''||au||'' | ''||acl from a order by relname" "the P100-12-MATVIEWS grant census"; ROWS=$(printf ''%s\n'' "$PSQL_OUT" | command grep -c '' | ''); GOOD=$(printf ''%s\n'' "$PSQL_OUT" | command grep -c '' | anon_can_select=false authenticated_can_select=false | postgres=DELETE,postgres=INSERT,postgres=MAINTAIN,postgres=REFERENCES,postgres=SELECT,postgres=TRIGGER,postgres=TRUNCATE,postgres=UPDATE,service_role=DELETE,service_role=INSERT,service_role=MAINTAIN,service_role=REFERENCES,service_role=SELECT,service_role=TRIGGER,service_role=TRUNCATE,service_role=UPDATE$''); echo "P100-12-MATVIEWS named=12 rendered=$ROWS at_expected_full_state=$GOOD expected rendered=12 at_expected_full_state=12"; printf ''%s\n'' "$PSQL_OUT" | sed ''s/^/  GRANTS /''; [ "$ROWS" = "12" ] || { echo "INSTRUMENT-CANNOT-RUN: 12 names were asked for and the catalog rendered $ROWS rows - the name list is stale, so any verdict below is about a different population"; exit 3; }; [ "$GOOD" = "12" ] || { echo "FAIL: $GOOD of 12 relations render the exact expected end state - EVERY grantee listed (postgres and service_role only, no PUBLIC and no client role) AND has_table_privilege false for both anon and authenticated"; exit 1; }; echo "PASS grants"'
      text: 'the twelve materialized views re-proven the same way in the same session, so both populations are observed against one state of the database rather than two.'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; set -a; . ./.env.test 2>/dev/null; set +a; command -v psql >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: psql absent"; exit 3; }; [ -n "${SUPABASE_DB_URL:-}" ] || { echo "INSTRUMENT-CANNOT-RUN: SUPABASE_DB_URL unset - .env.test not materialised in this worktree"; exit 3; }; DBQ() { DBQ_OUT=$(psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 -c "$1" 2>&1); DBQ_RC=$?; DBQ=$(printf ''%s'' "$DBQ_OUT" | tail -1); }; resolve_uid() { DBQ "select id from auth.users where email = ''$1''"; [ "$DBQ_RC" = "0" ] || { echo "INSTRUMENT-CANNOT-RUN: the $2 identity lookup for $1 exited $DBQ_RC :: $DBQ_OUT"; exit 3; }; RU=$DBQ; case "$RU" in *[!0-9a-f-]*|"") echo "INSTRUMENT-CANNOT-RUN: the $2 identity ($1) did not resolve to a uuid (got [$RU])"; exit 3;; esac; }; census_count() { CC_OUT=$(printf ''begin;\nset local role authenticated;\nset local request.jwt.claims = %s;\nselect count(*) from %s;\ncommit;\n'' "''{\"sub\":\"$1\",\"role\":\"authenticated\"}''" "$2" | psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 2>&1); CC_RC=$?; CC=$(printf ''%s'' "$CC_OUT" | tail -1); if [ "$CC_RC" != "0" ]; then case "$CC_OUT" in *"permission denied for"*) echo "FAIL: the $3 caller is DENIED $2 - the read path this criterion depends on is gone"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: the $3 census of $2 exited $CC_RC :: $CC_OUT"; exit 3;; esac; fi; case "$CC" in *[!0-9]*|"") echo "INSTRUMENT-CANNOT-RUN: the $3 census of $2 returned [$CC], not a count"; exit 3;; esac; }; COUNT1() { DBQ "$1"; [ "$DBQ_RC" = "0" ] || { echo "INSTRUMENT-CANNOT-RUN: the $2 catalog probe exited $DBQ_RC :: $DBQ_OUT"; exit 3; }; case "$DBQ" in *[!0-9]*|"") echo "INSTRUMENT-CANNOT-RUN: the $2 catalog probe returned [$DBQ], not a count"; exit 3;; esac; }; COUNT1 "select count(*) from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname=''public'' and c.relkind=''v'' and c.relname in (''unified_work_items'',''mous_frontend'',''event_details'',''working_group_stats'',''theme_details'',''relationship_health_summary'',''v_country_engagement_metrics'',''v_country_relationship_flows'',''v_regional_engagement_summary'',''engagement_recommendations_summary'',''dossier_activity_timeline'',''entity_comments_with_details'') and ''security_invoker=true'' = any(coalesce(c.reloptions,''{}''))" "invoker-flag"; N=$DBQ; COUNT1 "select count(*) from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname=''public'' and c.relkind=''v'' and c.relname in (''unified_work_items'',''mous_frontend'',''event_details'',''working_group_stats'',''theme_details'',''relationship_health_summary'',''v_country_engagement_metrics'',''v_country_relationship_flows'',''v_regional_engagement_summary'',''engagement_recommendations_summary'',''dossier_activity_timeline'',''entity_comments_with_details'')" "named-view-presence"; T=$DBQ; echo "P100-12-INVOKER invoker_on=$N present=$T expected invoker_on=12 present=12"; [ "$T" = "12" ] || { echo "INSTRUMENT-CANNOT-RUN: expected 12 named views to exist, catalog holds $T - the name list is stale"; exit 3; }; [ "$N" = "12" ] || { echo "FAIL: $N of 12 named views carry security_invoker=true - $(( 12 - N )) still execute as the definer"; exit 1; }; echo "PASS invoker-flag"'
      text: 'all twelve converted views named at once and required to exist and carry security_invoker=true - the first observation of that set as a whole rather than in the four separate plans that produced it. Eleven of twelve leaves invoker_on=11 and fails naming the shortfall.'
    - oracle: judge
      text: 'The SUMMARY and 100-VERIFICATION-GRANTS.md carry only output produced in this task, each zero stands beside its control, the untracked-consumer bound appears verbatim, and any disagreement with a sibling plan''s recorded number is reported rather than reconciled.'
---

<objective>
Re-prove the grant half of the phase, fresh, after every migration has landed.

The eight database plans each asserted only the objects they owned, concurrently (D-15). Nothing has
re-run those assertions against the FINAL tree, and a green produced before a later change is not
evidence about the state after it. This task re-executes the repaired per-object oracles unchanged.

**This is one of three wave-3 re-proofs.** P100-13 re-proves the function configuration and the caller
census; P100-14 re-proves the policy rows, the criterion-2 view state AND both predecessor behavioural
oracles. Between the three, every per-object assertion in the phase is re-executed - and all three feed
the register in P100-15.
</objective>

<context>
@.planning/phases/100-security-posture/100-CONTEXT.md
@.planning/phases/100-security-posture/100-RESEARCH.md

Ref notes: 100-CONTEXT.md - D-15, D-28, D-32, D-37 (the fifteen-plan ownership map); 100-RESEARCH.md - §3 (the 12/21 split), §9.2 (the full-grantee expected end state).

<interfaces>

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>the view and materialized-view grant sets are re-proven fresh, after everything landed</name>
  <files>the paths in files_modified</files>
  <read_first>100-CONTEXT.md D-01..D-37 (ALL of them - a task that cites D-19 or later needs the whole range) and 100-RESEARCH.md §0-§11 (the round-3 and round-4 derivations live in §10 and §11); re-RUN a recorded command rather than re-deriving its number a second way</read_first>
  <action>
    Run the three oracles below in this task, in one session, and record every line of their output
    verbatim into `100-VERIFICATION-GRANTS.md` alongside the SUMMARY.

    Do NOT quote a number from a sibling SUMMARY. If an oracle disagrees with the plan that produced it,
    that disagreement is the finding and this task records it rather than reconciling it quietly.

    `100-VERIFICATION-GRANTS.md` carries the per-view grant rows for the twenty restricted views with every
    grantee rendered, the same for the twelve materialized views, the invoker flags for the twelve converted
    views, and beside each the control that proves its reader could see a TRUE.

    It also carries, verbatim, the untracked-consumer bound that the register and the phase completion text
    must both repeat:

    > The 21 revocations were validated against TRACKED REPOSITORY SOURCES ONLY. A consumer outside the tree - a BI tool, a Retool app, a saved dashboard query - would not have appeared in the census. The break, if it occurs, is LOUD (permission denied) and REVERSIBLE (re-GRANT). The operator was asked before the revocations landed.
  </action>
  <acceptance_criteria>All three oracles pass on runs made in this task and their verbatim output is recorded in both files.</acceptance_criteria>
  <done>Fresh runs in this task show all twenty restricted views and all twelve materialized views rendering postgres and service_role as their only grantees, and all twelve converted views carrying security_invoker=true.</done>
</task>

</tasks>

<verification>
POPULATION: this plan's SUMMARY and the fresh oracle runs it records. OUTSIDE IT: every migration and
source file (a diff outside .planning here is a scope defect), the ROADMAP and REQUIREMENTS row flips
(the overseer's close-out acts, never a plan's), the register and the sign-off (P100-15 owns both, D-37),
and every residue this
phase names as carried to Phase 101, 102 or 103 - naming it is a job, closing it is not.
</verification>

<success_criteria>
Fresh runs after every change confirm the twenty restricted views and the twelve materialized views render postgres and service_role as their only grantees, and the twelve converted views are still invoker-enabled.
</success_criteria>

<output>
Create `.planning/phases/100-security-posture/100-12-SUMMARY.md` when done. It MUST record every command this
task ran with its VERBATIM output, the populations it re-derived with the commands that produced them, every
zero beside the control that proves the instrument could have seen a non-zero, and anything left for a named
later task. It MUST state that every number came from a run made in this task, and carry the untracked-consumer bound verbatim.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete`.
Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as
**pending** no matter how many gates passed (measured on P99-32 and again on P99-61).
</output>
