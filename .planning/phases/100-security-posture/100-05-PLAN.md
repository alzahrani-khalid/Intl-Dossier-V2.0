---
phase: 100-security-posture
plan: 5
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260908000005_p100_unconsumed_views_restrict.sql
  - .planning/phases/100-security-posture/100-05-SUMMARY.md
autonomous: true
requirements: [DBSEC-01]
success_criterion: 'All twenty views with no tracked application caller render every grantee as postgres and service_role only, answer has_table_privilege false for anon and authenticated, and deny an authenticated read of all twenty.'
routing:
  floor: frontier
truths:
  - 'Decisions covered - D-03: these twenty have no application caller in tracked sources, so the disposition is RESTRICT, not convert; D-20: the source census is a PLANNING BOUND recorded in the SUMMARY and never an acceptance item; D-32: the grant assertion renders EVERY grantee including PUBLIC and pairs it with has_table_privilege, because a three-role filter cannot see a PUBLIC grant that anon and authenticated inherit; D-33: all twenty are read behaviourally, because one representative denial says nothing about the other nineteen; D-15: per-object assertions over a named list; D-13: idempotent migration applied with psql, no ledger row.'
must_haves:
  truths:
    - 'The SUMMARY carries the twenty-view consumer census AND its positive control (from(''dossiers'') returning 18 files) explicitly labelled a PLANNING BOUND rather than evidence of correctness, and states that the population is TRACKED REPOSITORY SOURCES ONLY, so a BI tool, a Retool app or a saved dashboard query reading one of these twenty would not have appeared in it - a break there is loud (permission denied) and reversible (re-GRANT)'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; set -a; . ./.env.test 2>/dev/null; set +a; command -v psql >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: psql absent"; exit 3; }; [ -n "${SUPABASE_DB_URL:-}" ] || { echo "INSTRUMENT-CANNOT-RUN: SUPABASE_DB_URL unset - .env.test not materialised in this worktree"; exit 3; }; PQ() { PSQL_OUT=$(psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 -c "$1" 2>&1); PSQL_RC=$?; }; PQ_OK() { PQ "$1"; [ "$PSQL_RC" = "0" ] || { echo "INSTRUMENT-CANNOT-RUN: psql exited $PSQL_RC for $2 :: $PSQL_OUT"; exit 3; }; }; PQ_OK "with a as (select c.relname, coalesce((select string_agg(case when x.grantee=0 then ''PUBLIC'' else pg_get_userbyid(x.grantee) end||''=''||x.privilege_type, '','' order by case when x.grantee=0 then ''PUBLIC'' else pg_get_userbyid(x.grantee) end, x.privilege_type) from aclexplode(c.relacl) x),''(null-acl)'') acl, has_table_privilege(''anon'',c.oid,''SELECT'') an, has_table_privilege(''authenticated'',c.oid,''SELECT'') au from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname=''public'' and c.relkind=''v'' and c.relname in (''link_audit_logs_archival_eligible'',''citation_statistics'',''user_work_summary'',''ai_usage_summary'',''user_ai_usage'',''audit_logs_active'',''ai_interaction_summary'',''intelligence_cache_status'',''engagement_briefs'',''embedding_queue_stats'',''recent_field_changes'',''sla_compliance_by_assignee'',''engagement_analytics'',''commitment_analytics'',''work_item_analytics'',''top_contributors'',''user_digest_content_summary'',''v_dossier_extension_health'',''resolved_field_permissions'',''stakeholder_timeline_unified'')) select relname||'' | anon_can_select=''||an||'' authenticated_can_select=''||au||'' | ''||acl from a order by relname" "the P100-05-GRANTS grant census"; ROWS=$(printf ''%s\n'' "$PSQL_OUT" | command grep -c '' | ''); GOOD=$(printf ''%s\n'' "$PSQL_OUT" | command grep -c '' | anon_can_select=false authenticated_can_select=false | postgres=DELETE,postgres=INSERT,postgres=MAINTAIN,postgres=REFERENCES,postgres=SELECT,postgres=TRIGGER,postgres=TRUNCATE,postgres=UPDATE,service_role=DELETE,service_role=INSERT,service_role=MAINTAIN,service_role=REFERENCES,service_role=SELECT,service_role=TRIGGER,service_role=TRUNCATE,service_role=UPDATE$''); echo "P100-05-GRANTS named=20 rendered=$ROWS at_expected_full_state=$GOOD expected rendered=20 at_expected_full_state=20"; printf ''%s\n'' "$PSQL_OUT" | sed ''s/^/  GRANTS /''; [ "$ROWS" = "20" ] || { echo "INSTRUMENT-CANNOT-RUN: 20 names were asked for and the catalog rendered $ROWS rows - the name list is stale, so any verdict below is about a different population"; exit 3; }; [ "$GOOD" = "20" ] || { echo "FAIL: $GOOD of 20 relations render the exact expected end state - EVERY grantee listed (postgres and service_role only, no PUBLIC and no client role) AND has_table_privilege false for both anon and authenticated"; exit 1; }; echo "PASS grants"'
      text: 'the positive end state, printed per view and matched whole with EVERY grantee rendered: PUBLIC would appear as its own entry and break the match, and the paired has_table_privilege answers catch a grant inherited through role membership that no ACL row shows. A probe returning nothing scores 0 of 20 and fails; a stale name list renders the wrong row count and exits 3. RED at HEAD, where all twenty carry anon and authenticated.'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; set -a; . ./.env.test 2>/dev/null; set +a; command -v psql >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: psql absent"; exit 3; }; [ -n "${SUPABASE_DB_URL:-}" ] || { echo "INSTRUMENT-CANNOT-RUN: SUPABASE_DB_URL unset - .env.test not materialised in this worktree"; exit 3; }; PQ() { PSQL_OUT=$(psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 -c "$1" 2>&1); PSQL_RC=$?; }; PQ_OK() { PQ "$1"; [ "$PSQL_RC" = "0" ] || { echo "INSTRUMENT-CANNOT-RUN: psql exited $PSQL_RC for $2 :: $PSQL_OUT"; exit 3; }; }; DBQ() { DBQ_OUT=$(psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 -c "$1" 2>&1); DBQ_RC=$?; DBQ=$(printf ''%s'' "$DBQ_OUT" | tail -1); }; resolve_uid() { DBQ "select id from auth.users where email = ''$1''"; [ "$DBQ_RC" = "0" ] || { echo "INSTRUMENT-CANNOT-RUN: the $2 identity lookup for $1 exited $DBQ_RC :: $DBQ_OUT"; exit 3; }; RU=$DBQ; case "$RU" in *[!0-9a-f-]*|"") echo "INSTRUMENT-CANNOT-RUN: the $2 identity ($1) did not resolve to a uuid (got [$RU])"; exit 3;; esac; }; census_count() { CC_OUT=$(printf ''begin;\nset local role authenticated;\nset local request.jwt.claims = %s;\nselect count(*) from %s;\ncommit;\n'' "''{\"sub\":\"$1\",\"role\":\"authenticated\"}''" "$2" | psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 2>&1); CC_RC=$?; CC=$(printf ''%s'' "$CC_OUT" | tail -1); if [ "$CC_RC" != "0" ]; then case "$CC_OUT" in *"permission denied for"*) echo "FAIL: the $3 caller is DENIED $2 - the read path this criterion depends on is gone"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: the $3 census of $2 exited $CC_RC :: $CC_OUT"; exit 3;; esac; fi; case "$CC" in *[!0-9]*|"") echo "INSTRUMENT-CANNOT-RUN: the $3 census of $2 returned [$CC], not a count"; exit 3;; esac; }; COUNT1() { DBQ "$1"; [ "$DBQ_RC" = "0" ] || { echo "INSTRUMENT-CANNOT-RUN: the $2 catalog probe exited $DBQ_RC :: $DBQ_OUT"; exit 3; }; case "$DBQ" in *[!0-9]*|"") echo "INSTRUMENT-CANNOT-RUN: the $2 catalog probe returned [$DBQ], not a count"; exit 3;; esac; }; READ() { R_OUT=$(printf ''begin;\nset local role authenticated;\nset local request.jwt.claims = %s;\nselect count(*) from %s;\ncommit;\n'' "''{\"sub\":\"$1\",\"role\":\"authenticated\"}''" "$2" | psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 2>&1); R_RC=$?; R_LAST=$(printf ''%s'' "$R_OUT" | tail -1); if [ "$R_RC" = "0" ]; then case "$R_LAST" in ""|*[!0-9]*) R_KIND=malformed;; *) R_KIND=answered; R_N=$R_LAST;; esac; elif [ "$R_RC" = "3" ]; then case "$R_OUT" in *"permission denied for"*) R_KIND=denied;; *) R_KIND=broken;; esac; else R_KIND=broken; fi; }; SR_READ() { R_OUT=$(printf ''begin;\nset local role service_role;\nselect count(*) from %s;\ncommit;\n'' "$1" | psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 2>&1); R_RC=$?; R_LAST=$(printf ''%s'' "$R_OUT" | tail -1); if [ "$R_RC" = "0" ]; then case "$R_LAST" in ""|*[!0-9]*) R_KIND=malformed;; *) R_KIND=answered; R_N=$R_LAST;; esac; elif [ "$R_RC" = "3" ]; then case "$R_OUT" in *"permission denied for"*) R_KIND=denied;; *) R_KIND=broken;; esac; else R_KIND=broken; fi; }; resolve_uid kazahrani@stats.gov.sa owner; OWN=$RU; resolve_uid test.user@gmail.com non-owner; OTH=$RU; RC=0; OUT=""; READ "$OTH" link_audit_logs_archival_eligible; case "$R_KIND" in denied) OUT="$OUT link_audit_logs_archival_eligible=denied";; answered) echo "FAIL: an authenticated caller still read link_audit_logs_archival_eligible and received $R_N rows where a permission denial is required"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading link_audit_logs_archival_eligible produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OTH" citation_statistics; case "$R_KIND" in denied) OUT="$OUT citation_statistics=denied";; answered) echo "FAIL: an authenticated caller still read citation_statistics and received $R_N rows where a permission denial is required"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading citation_statistics produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OTH" user_work_summary; case "$R_KIND" in denied) OUT="$OUT user_work_summary=denied";; answered) echo "FAIL: an authenticated caller still read user_work_summary and received $R_N rows where a permission denial is required"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading user_work_summary produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OTH" ai_usage_summary; case "$R_KIND" in denied) OUT="$OUT ai_usage_summary=denied";; answered) echo "FAIL: an authenticated caller still read ai_usage_summary and received $R_N rows where a permission denial is required"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading ai_usage_summary produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OTH" user_ai_usage; case "$R_KIND" in denied) OUT="$OUT user_ai_usage=denied";; answered) echo "FAIL: an authenticated caller still read user_ai_usage and received $R_N rows where a permission denial is required"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading user_ai_usage produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OTH" audit_logs_active; case "$R_KIND" in denied) OUT="$OUT audit_logs_active=denied";; answered) echo "FAIL: an authenticated caller still read audit_logs_active and received $R_N rows where a permission denial is required"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading audit_logs_active produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OTH" ai_interaction_summary; case "$R_KIND" in denied) OUT="$OUT ai_interaction_summary=denied";; answered) echo "FAIL: an authenticated caller still read ai_interaction_summary and received $R_N rows where a permission denial is required"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading ai_interaction_summary produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OTH" intelligence_cache_status; case "$R_KIND" in denied) OUT="$OUT intelligence_cache_status=denied";; answered) echo "FAIL: an authenticated caller still read intelligence_cache_status and received $R_N rows where a permission denial is required"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading intelligence_cache_status produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OTH" engagement_briefs; case "$R_KIND" in denied) OUT="$OUT engagement_briefs=denied";; answered) echo "FAIL: an authenticated caller still read engagement_briefs and received $R_N rows where a permission denial is required"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading engagement_briefs produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OTH" embedding_queue_stats; case "$R_KIND" in denied) OUT="$OUT embedding_queue_stats=denied";; answered) echo "FAIL: an authenticated caller still read embedding_queue_stats and received $R_N rows where a permission denial is required"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading embedding_queue_stats produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OTH" recent_field_changes; case "$R_KIND" in denied) OUT="$OUT recent_field_changes=denied";; answered) echo "FAIL: an authenticated caller still read recent_field_changes and received $R_N rows where a permission denial is required"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading recent_field_changes produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OTH" sla_compliance_by_assignee; case "$R_KIND" in denied) OUT="$OUT sla_compliance_by_assignee=denied";; answered) echo "FAIL: an authenticated caller still read sla_compliance_by_assignee and received $R_N rows where a permission denial is required"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading sla_compliance_by_assignee produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OTH" engagement_analytics; case "$R_KIND" in denied) OUT="$OUT engagement_analytics=denied";; answered) echo "FAIL: an authenticated caller still read engagement_analytics and received $R_N rows where a permission denial is required"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading engagement_analytics produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OTH" commitment_analytics; case "$R_KIND" in denied) OUT="$OUT commitment_analytics=denied";; answered) echo "FAIL: an authenticated caller still read commitment_analytics and received $R_N rows where a permission denial is required"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading commitment_analytics produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OTH" work_item_analytics; case "$R_KIND" in denied) OUT="$OUT work_item_analytics=denied";; answered) echo "FAIL: an authenticated caller still read work_item_analytics and received $R_N rows where a permission denial is required"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading work_item_analytics produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OTH" top_contributors; case "$R_KIND" in denied) OUT="$OUT top_contributors=denied";; answered) echo "FAIL: an authenticated caller still read top_contributors and received $R_N rows where a permission denial is required"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading top_contributors produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OTH" user_digest_content_summary; case "$R_KIND" in denied) OUT="$OUT user_digest_content_summary=denied";; answered) echo "FAIL: an authenticated caller still read user_digest_content_summary and received $R_N rows where a permission denial is required"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading user_digest_content_summary produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OTH" v_dossier_extension_health; case "$R_KIND" in denied) OUT="$OUT v_dossier_extension_health=denied";; answered) echo "FAIL: an authenticated caller still read v_dossier_extension_health and received $R_N rows where a permission denial is required"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading v_dossier_extension_health produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OTH" resolved_field_permissions; case "$R_KIND" in denied) OUT="$OUT resolved_field_permissions=denied";; answered) echo "FAIL: an authenticated caller still read resolved_field_permissions and received $R_N rows where a permission denial is required"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading resolved_field_permissions produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OTH" stakeholder_timeline_unified; case "$R_KIND" in denied) OUT="$OUT stakeholder_timeline_unified=denied";; answered) echo "FAIL: an authenticated caller still read stakeholder_timeline_unified and received $R_N rows where a permission denial is required"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading stakeholder_timeline_unified produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; SR_READ citation_statistics; case "$R_KIND" in answered) OUT="$OUT citation_statistics@service_role=$R_N/any"; [ "any" = "any" ] || [ "$R_N" = "any" ] || RC=1;; denied) echo "FAIL: service_role is DENIED citation_statistics - the revocation removed the retained access the backend reads with, not only the client access"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: the service_role read of citation_statistics produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; echo "P100-05 reads:$OUT"; [ "$RC" = "0" ] || { echo "FAIL: a recorded read count moved off its literal - a caller entitled to those rows lost them"; exit 1; }; echo "PASS reads"'
      text: 'all twenty read behaviourally, not one representative: each must be classified DENIED - psql exit 3 with a permission-denied ERROR - and a revoke that lands on nineteen fails naming the twentieth. Paired with the RETAINED-ACCESS arm the catalog rows cannot give: the same read as service_role must still ANSWER, so a REVOKE that over-reached and stripped the service role is caught at runtime rather than inferred from a grant. Any read that is neither answered nor denied exits 3.'
    - oracle: judge
      text: 'The DIFF''s changed hunk in supabase/migrations/20260908000005_p100_unconsumed_views_restrict.sql is cited explicitly and contains a REVOKE for each of the twenty named views; and the SUMMARY records both applies with exit statuses, the fresh post-apply per-view grant rows including every grantee, the twenty denial classifications plus the retained service-role read, and the twenty-row census carried as a stated planning bound with its control.'
---

<objective>
Take criterion 1's "restricted" branch for the twenty views that no tracked repository source queries.
`REVOKE ALL ... FROM anon, authenticated` on each.

**The grant assertion lists EVERY grantee, not a filtered three.** A `GRANT SELECT ... TO PUBLIC` on any
of the twenty would leave a three-role filter looking untouched while `anon` and `authenticated` inherit
the access, so the oracle renders the whole ACL - `PUBLIC` included - and pairs it with the
authoritative `has_table_privilege` answer, which also catches a grant arriving through role membership.

**And every one of the twenty is read**, not one representative: twenty relations, twenty denials.
</objective>

<context>
@.planning/phases/100-security-posture/100-CONTEXT.md
@.planning/phases/100-security-posture/100-RESEARCH.md

Ref notes: 100-CONTEXT.md - D-03 (the 12/21 split), D-20 (census as bound), D-32 (full-ACL rendering), D-33 (every relation read); 100-RESEARCH.md - §3 (the twenty-name list), §9.2 (the full-grantee ACL derivation and the expected end-state string), §8 (untracked consumers).

<interfaces>

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>the twenty views with no application caller carry a service-role-only grant set</name>
  <files>the paths in files_modified</files>
  <read_first>100-CONTEXT.md D-01..D-37 (ALL of them - a task that cites D-19 or later needs the whole range) and 100-RESEARCH.md §0-§11 (the round-3 and round-4 derivations live in §10 and §11); re-RUN a recorded command rather than re-deriving its number a second way</read_first>
  <action>
    The criteria close on LIVE staging state, so writing the migration file is half the task:

    1. Write the migration file listed in files_modified. It MUST be IDEMPOTENT and safely re-runnable
       (D-13: no ledger row is created, so it may be replayed). Guard every statement — `ALTER VIEW ...
       SET (security_invoker = true)` is already idempotent; wrap anything that is not in `DO $$ ... $$`.
    2. Apply it: `PATH="/opt/homebrew/bin:$PATH"; set -a; . ./.env.test; set +a;
       psql "$SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f <the migration file>` — capture the output verbatim.
    3. Apply it a SECOND time and capture that output too. A non-zero exit on the replay means the
       migration is not idempotent; fix the migration, do not record the failure as expected.
    4. Run this task's oracles and record their verbatim output in the SUMMARY.

    Twenty statements: `REVOKE ALL ON public.<view> FROM anon, authenticated;`

    The expected end state per view, derived live with EVERY grantee rendered (100-RESEARCH.md §9.2):

        postgres=DELETE,…,postgres=UPDATE,service_role=DELETE,…,service_role=UPDATE
        with has_table_privilege('anon', …,'SELECT') = false and the same for 'authenticated'

    Do not add a `GRANT ... TO PUBLIC` anywhere; the oracle renders PUBLIC and would fail.

    Re-run the consumer census for all twenty and paste it into the SUMMARY as a **stated planning bound**,
    with the `from('dossiers')` = 18 control beside it so a row of zeros is a measurement and not a broken
    sweep. Say that the population is TRACKED REPOSITORY SOURCES; a BI tool or saved dashboard query reading
    one of these twenty would not appear in it. **Do not present the census as evidence the change is
    correct** - it is evidence the change is low-risk, and the break it risks is loud and reversible.
  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes.</acceptance_criteria>
  <done>All twenty named views render postgres and service_role as their only grantees with has_table_privilege false for anon and authenticated, and an authenticated caller is denied on all twenty.</done>
</task>

</tasks>

<verification>
POPULATION: the paths in files_modified, and the named database objects this plan owns.
OUTSIDE IT: every other plan's migration file and objects (P100-01..12 run against ONE live database
concurrently — assert only what you own, by name, never a global total: that is D-15, and a global
assertion here makes the wave order-dependent), the migration ledger (D-13: no ledger row is
fabricated), the advisor's `*_security_definer_function_executable` and `extension_in_public` classes
(100-CONTEXT.md §3), and the phase-level arithmetic, the register and the sign-off, which P100-15 owns
(D-37).
</verification>

<success_criteria>
All twenty views with no tracked application caller render every grantee as postgres and service_role only, answer has_table_privilege false for anon and authenticated, and deny an authenticated read of all twenty.
</success_criteria>

<output>
Create `.planning/phases/100-security-posture/100-05-SUMMARY.md` when done. It MUST record every command this
task ran with its VERBATIM output, the populations it re-derived with the commands that produced them, every
zero beside the control that proves the instrument could have seen a non-zero, and anything left for a named
later task. It MUST carry the twenty-row census labelled as a bound, its control, and the untracked-consumer risk.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete`.
Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as
**pending** no matter how many gates passed (measured on P99-32 and again on P99-61).
</output>
