---
phase: 100-security-posture
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/store/clientResidue.ts
  - frontend/src/store/authStore.ts
  - frontend/src/store/authStore.signout.test.ts
  - .planning/phases/100-security-posture/100-10-SUMMARY.md
autonomous: true
requirements: [CLIENTSEC-01]
success_criterion: 'After the production SIGNED_OUT handler runs, enumerated browser storage equals the hardcoded allowlisted key/value map exactly, and a non-SIGNED_OUT auth event leaves every seeded key at its seeded value.'
routing:
  floor: frontier
truths:
  - 'Decisions covered - D-09: the criterion names six persisted stores and only FIVE exist, services/auth.ts having been deleted in Phase 97 commit e6ac817f3 - context for the worker, not an acceptance item; D-10: the seven named keys are a LOWER BOUND, established by an EXECUTABLE extractor whose script and enumerated output ship in 100-RESEARCH.md §10.6 (23 literal keys, 21 key expressions, 52 files) rather than by a prose approximation; D-11: the seam already exists and Phase 92 left this half open in writing; D-25: the acceptance is EXACT EQUALITY of enumerated storage against a non-empty hardcoded allowlist map, which subsumes the seeded-key sweep and the over-clearing neighbour in one positive observable.'
must_haves:
  truths:
    - 'The SUMMARY lists the allowlist entry by entry with one line of justification each, and carries the OUTPUT of the executable key extractor in 100-RESEARCH.md §10.6 - 52 files scanned, 23 literal keys, 21 key expressions, enumerated - stating that 23 is a LOWER BOUND because one literal is itself a template and several key expressions resolve differently per file, so static analysis cannot close the population - and it states that sessionStorage is OUTSIDE this census population and outside the sweep, so criterion 6''s storage boundary is written down rather than left implicit'
    - oracle: test
      test: 'enumerated browser storage after the SIGNED_OUT handler equals the allowlisted key/value map exactly'
      text: 'one positive observable that subsumes two absence tests: seed the seven keys the criterion names plus at least three it does not, seed the allowlisted keys with known values, drive the PRODUCTION entry point handleAuthStateChange(''SIGNED_OUT'', null), then enumerate storage and assert DEEP EQUALITY against a non-empty hardcoded key/value map. It cannot pass on an already-empty store, because the allowlisted entries must be present with their exact values; it cannot pass on an over-clearing sweep, for the same reason; and it cannot pass on a sweep that is never called, because the entry point is the production handler.'
    - oracle: test
      test: 'an auth event other than SIGNED_OUT leaves every seeded key at its seeded value'
      text: 'the scoping neighbour: seed the same map, drive handleAuthStateChange with a non-SIGNED_OUT event, and assert storage equals the FULL seeded map unchanged. Without it, a sweep wired to every auth state change - which would wipe an analyst''s state on a token refresh - passes the first test perfectly.'
    - oracle: judge
      text: 'The diff introduces positive contract text where the two stale Phase 92 bounding notes stood, stating what now holds rather than what used to leak; the SUMMARY defends each allowlist entry and carries the extractor''s enumerated output with its lower-bound statement.'
---

<objective>
Close criterion 6. Phase 92 gave the sign-out seam the in-memory half and left this one open on purpose.

**Clear by ALLOWLIST, and grade by EXACT EQUALITY.** The acceptance is not that seeded keys vanished -
that is an absence, satisfiable by a store that was empty to begin with. It is that after the production
`SIGNED_OUT` entry point runs, the enumerated contents of storage EQUAL a non-empty hardcoded allowlist
map.

The criterion names 5 persisted stores (the sixth, in `services/auth.ts`, does not exist) plus 2 raw
writers. **Those seven are a LOWER BOUND, and the census that says so is now an executable extractor**
whose script and enumerated output are in `100-RESEARCH.md` §10.6: 23 literal keys and 21 key
expressions, one of them a template literal, over 52 files. The population is not closed by static
analysis, which is exactly why the fix is a seam and not a list.
</objective>

<context>
@.planning/phases/100-security-posture/100-CONTEXT.md
@.planning/phases/100-security-posture/100-RESEARCH.md
@frontend/src/store/uiStore.ts
@frontend/src/domains/search/hooks/useAdvancedSearch.ts
@frontend/src/domains/dossiers/hooks/useQuickSwitcherSearch.ts

Ref notes: 100-CONTEXT.md - D-09, D-10 (the executable census and its lower-bound status), D-11, D-25; 100-RESEARCH.md - §10.6 (the extractor script, verbatim, with its enumerated output), §7.

<interfaces>

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>after sign-out, browser storage equals the allowlisted map exactly</name>
  <files>the paths in files_modified</files>
  <read_first>100-CONTEXT.md D-01..D-37 (ALL of them - a task that cites D-19 or later needs the whole range) and 100-RESEARCH.md §0-§11 (the round-3 and round-4 derivations live in §10 and §11); re-RUN a recorded command rather than re-deriving its number a second way</read_first>
  <action>
    Add `frontend/src/store/clientResidue.ts` exporting one function that wipes browser storage except a
    named allowlist, and call it from the `SIGNED_OUT` branch of `handleAuthStateChange` in
    `frontend/src/store/authStore.ts`, beside the existing `queryClient.clear()`.

    The allowlist is the whole design decision. It holds ONLY keys carrying no user identity whose loss
    would be a usability regression: the `id.` prefixed design-system keys (canonical locale, theme,
    density, direction) and the i18next language key. Everything else goes - including `ui-storage`, which
    the criterion names; language survives via the canonical `id.` locale key, which
    `frontend/src/components/language-provider/language-provider.tsx` reads FIRST. Verify that ordering
    before you rely on it.

    Guard the sweep: storage access throws in private windows and some embedded contexts, so wrap reads and
    writes in try/catch and let sign-out complete regardless.

    **Re-run the key census with the extractor in `100-RESEARCH.md` §10.6** - it is a runnable script, not a
    method note - and paste its enumerated output into the SUMMARY. State that 23 is a LOWER BOUND: one
    literal is itself a template (`${STORAGE_KEY_PREFIX}${entityType}`) and 21 further keys are bound to
    identifiers, several of which (`STORAGE_KEY`) hold different values in different files.

    Then RETIRE the two stale notes as an implementation step and REPLACE each with a positive statement of
    the contract that now holds. The replacement text is what the judge reads.

    Write the two tests below. Their leaf titles must match the acceptance items VERBATIM. Both must drive
    the PRODUCTION entry point `handleAuthStateChange`, never the sweep function directly.
  </action>
  <acceptance_criteria>Both named tests pass with their leaf titles verbatim, storage equality is asserted against a non-empty hardcoded map, and no path outside files_modified changes.</acceptance_criteria>
  <done>After the production SIGNED_OUT handler runs, enumerated browser storage equals the hardcoded allowlist map exactly, and a non-SIGNED_OUT event leaves every seeded key at its seeded value.</done>
</task>

</tasks>

<verification>
POPULATION: the paths in files_modified. OUTSIDE IT: the individual storage call sites (the sweep clears
their keys as a consequence of closing the class, and no acceptance item names them one by one -
100-CONTEXT.md §3 records that as the stated coverage bound), the sourcemap work (P100-11 owns it),
every database object, and deployment of this change to the droplet.
</verification>

<success_criteria>
After the production SIGNED_OUT handler runs, enumerated browser storage equals the hardcoded allowlisted key/value map exactly, and a non-SIGNED_OUT auth event leaves every seeded key at its seeded value.
</success_criteria>

<output>
Create `.planning/phases/100-security-posture/100-10-SUMMARY.md` when done. It MUST record every command this
task ran with its VERBATIM output, the populations it re-derived with the commands that produced them, every
zero beside the control that proves the instrument could have seen a non-zero, and anything left for a named
later task. It MUST carry the allowlist justification and the extractor's enumerated output with its lower-bound statement.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete`.
Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as
**pending** no matter how many gates passed (measured on P99-32 and again on P99-61).
</output>
