---
phase: 100-security-posture
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260908000003_p100_edge_views_invoker.sql
  - .planning/phases/100-security-posture/100-03-SUMMARY.md
autonomous: true
requirements: [DBSEC-01]
success_criterion: 'The seven views read by edge functions under the caller''s JWT execute as the caller, and the owner still reads 5, 3 and 32 on the three that hold data.'
routing:
  floor: frontier
truths:
  - 'Decisions covered - D-03: each of these seven is read by an edge function that runs as authenticated, which is why they are CONVERTED and not revoked - the client construction was verified per file by grepping for ANON_KEY and Authorization; D-15: per-object assertions only; D-13: idempotent migration applied with psql, no ledger row; D-16: wave 1, before the search_path pinning; D-18: a read that neither answers nor is denied exits 3.'
must_haves:
  truths:
    - 'The SUMMARY names, for each of the seven, the edge function that reads it and how that function builds its Supabase client - reporting any that in fact reads with the service role - plus the base tables, the row-deciding inputs, which of those a caller can still control, and the before/after non-owner counts; and it names theme_details, relationship_health_summary, v_country_relationship_flows and engagement_recommendations_summary as EMPTY on staging, whose greens establish an unbroken read path and NOT row scoping'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; set -a; . ./.env.test 2>/dev/null; set +a; command -v psql >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: psql absent"; exit 3; }; [ -n "${SUPABASE_DB_URL:-}" ] || { echo "INSTRUMENT-CANNOT-RUN: SUPABASE_DB_URL unset - .env.test not materialised in this worktree"; exit 3; }; DBQ() { DBQ_OUT=$(psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 -c "$1" 2>&1); DBQ_RC=$?; DBQ=$(printf ''%s'' "$DBQ_OUT" | tail -1); }; resolve_uid() { DBQ "select id from auth.users where email = ''$1''"; [ "$DBQ_RC" = "0" ] || { echo "INSTRUMENT-CANNOT-RUN: the $2 identity lookup for $1 exited $DBQ_RC :: $DBQ_OUT"; exit 3; }; RU=$DBQ; case "$RU" in *[!0-9a-f-]*|"") echo "INSTRUMENT-CANNOT-RUN: the $2 identity ($1) did not resolve to a uuid (got [$RU])"; exit 3;; esac; }; census_count() { CC_OUT=$(printf ''begin;\nset local role authenticated;\nset local request.jwt.claims = %s;\nselect count(*) from %s;\ncommit;\n'' "''{\"sub\":\"$1\",\"role\":\"authenticated\"}''" "$2" | psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 2>&1); CC_RC=$?; CC=$(printf ''%s'' "$CC_OUT" | tail -1); if [ "$CC_RC" != "0" ]; then case "$CC_OUT" in *"permission denied for"*) echo "FAIL: the $3 caller is DENIED $2 - the read path this criterion depends on is gone"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: the $3 census of $2 exited $CC_RC :: $CC_OUT"; exit 3;; esac; fi; case "$CC" in *[!0-9]*|"") echo "INSTRUMENT-CANNOT-RUN: the $3 census of $2 returned [$CC], not a count"; exit 3;; esac; }; COUNT1() { DBQ "$1"; [ "$DBQ_RC" = "0" ] || { echo "INSTRUMENT-CANNOT-RUN: the $2 catalog probe exited $DBQ_RC :: $DBQ_OUT"; exit 3; }; case "$DBQ" in *[!0-9]*|"") echo "INSTRUMENT-CANNOT-RUN: the $2 catalog probe returned [$DBQ], not a count"; exit 3;; esac; }; COUNT1 "select count(*) from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname=''public'' and c.relkind=''v'' and c.relname in (''theme_details'',''relationship_health_summary'',''v_country_engagement_metrics'',''v_country_relationship_flows'',''v_regional_engagement_summary'',''engagement_recommendations_summary'',''dossier_activity_timeline'') and ''security_invoker=true'' = any(coalesce(c.reloptions,''{}''))" "invoker-flag"; N=$DBQ; COUNT1 "select count(*) from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname=''public'' and c.relkind=''v'' and c.relname in (''theme_details'',''relationship_health_summary'',''v_country_engagement_metrics'',''v_country_relationship_flows'',''v_regional_engagement_summary'',''engagement_recommendations_summary'',''dossier_activity_timeline'')" "named-view-presence"; T=$DBQ; echo "P100-03-FLAG invoker_on=$N present=$T expected invoker_on=7 present=7"; [ "$T" = "7" ] || { echo "INSTRUMENT-CANNOT-RUN: expected 7 named views to exist, catalog holds $T - the name list is stale"; exit 3; }; [ "$N" = "7" ] || { echo "FAIL: $N of 7 named views carry security_invoker=true - $(( 7 - N )) still execute as the definer"; exit 1; }; echo "PASS invoker-flag"'
      text: 'all seven named views must exist (else exit 3) and all seven must carry security_invoker=true; converting six leaves invoker_on=6 and fails naming the shortfall. This is the per-object form D-15 requires, so it cannot read a sibling plan''s effect.'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; set -a; . ./.env.test 2>/dev/null; set +a; command -v psql >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: psql absent"; exit 3; }; [ -n "${SUPABASE_DB_URL:-}" ] || { echo "INSTRUMENT-CANNOT-RUN: SUPABASE_DB_URL unset - .env.test not materialised in this worktree"; exit 3; }; PQ() { PSQL_OUT=$(psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 -c "$1" 2>&1); PSQL_RC=$?; }; PQ_OK() { PQ "$1"; [ "$PSQL_RC" = "0" ] || { echo "INSTRUMENT-CANNOT-RUN: psql exited $PSQL_RC for $2 :: $PSQL_OUT"; exit 3; }; }; DBQ() { DBQ_OUT=$(psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 -c "$1" 2>&1); DBQ_RC=$?; DBQ=$(printf ''%s'' "$DBQ_OUT" | tail -1); }; resolve_uid() { DBQ "select id from auth.users where email = ''$1''"; [ "$DBQ_RC" = "0" ] || { echo "INSTRUMENT-CANNOT-RUN: the $2 identity lookup for $1 exited $DBQ_RC :: $DBQ_OUT"; exit 3; }; RU=$DBQ; case "$RU" in *[!0-9a-f-]*|"") echo "INSTRUMENT-CANNOT-RUN: the $2 identity ($1) did not resolve to a uuid (got [$RU])"; exit 3;; esac; }; census_count() { CC_OUT=$(printf ''begin;\nset local role authenticated;\nset local request.jwt.claims = %s;\nselect count(*) from %s;\ncommit;\n'' "''{\"sub\":\"$1\",\"role\":\"authenticated\"}''" "$2" | psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 2>&1); CC_RC=$?; CC=$(printf ''%s'' "$CC_OUT" | tail -1); if [ "$CC_RC" != "0" ]; then case "$CC_OUT" in *"permission denied for"*) echo "FAIL: the $3 caller is DENIED $2 - the read path this criterion depends on is gone"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: the $3 census of $2 exited $CC_RC :: $CC_OUT"; exit 3;; esac; fi; case "$CC" in *[!0-9]*|"") echo "INSTRUMENT-CANNOT-RUN: the $3 census of $2 returned [$CC], not a count"; exit 3;; esac; }; COUNT1() { DBQ "$1"; [ "$DBQ_RC" = "0" ] || { echo "INSTRUMENT-CANNOT-RUN: the $2 catalog probe exited $DBQ_RC :: $DBQ_OUT"; exit 3; }; case "$DBQ" in *[!0-9]*|"") echo "INSTRUMENT-CANNOT-RUN: the $2 catalog probe returned [$DBQ], not a count"; exit 3;; esac; }; READ() { R_OUT=$(printf ''begin;\nset local role authenticated;\nset local request.jwt.claims = %s;\nselect count(*) from %s;\ncommit;\n'' "''{\"sub\":\"$1\",\"role\":\"authenticated\"}''" "$2" | psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 2>&1); R_RC=$?; R_LAST=$(printf ''%s'' "$R_OUT" | tail -1); if [ "$R_RC" = "0" ]; then case "$R_LAST" in ""|*[!0-9]*) R_KIND=malformed;; *) R_KIND=answered; R_N=$R_LAST;; esac; elif [ "$R_RC" = "3" ]; then case "$R_OUT" in *"permission denied for"*) R_KIND=denied;; *) R_KIND=broken;; esac; else R_KIND=broken; fi; }; SR_READ() { R_OUT=$(printf ''begin;\nset local role service_role;\nselect count(*) from %s;\ncommit;\n'' "$1" | psql "$SUPABASE_DB_URL" -Atq -v ON_ERROR_STOP=1 2>&1); R_RC=$?; R_LAST=$(printf ''%s'' "$R_OUT" | tail -1); if [ "$R_RC" = "0" ]; then case "$R_LAST" in ""|*[!0-9]*) R_KIND=malformed;; *) R_KIND=answered; R_N=$R_LAST;; esac; elif [ "$R_RC" = "3" ]; then case "$R_OUT" in *"permission denied for"*) R_KIND=denied;; *) R_KIND=broken;; esac; else R_KIND=broken; fi; }; resolve_uid kazahrani@stats.gov.sa owner; OWN=$RU; resolve_uid test.user@gmail.com non-owner; OTH=$RU; RC=0; OUT=""; READ "$OWN" v_country_engagement_metrics; case "$R_KIND" in answered) OUT="$OUT v_country_engagement_metrics=$R_N/5"; [ "$R_N" = "5" ] || RC=1;; denied) echo "FAIL: the owner is now denied v_country_engagement_metrics - the change removed a read the caller is entitled to"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading v_country_engagement_metrics produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OWN" v_regional_engagement_summary; case "$R_KIND" in answered) OUT="$OUT v_regional_engagement_summary=$R_N/3"; [ "$R_N" = "3" ] || RC=1;; denied) echo "FAIL: the owner is now denied v_regional_engagement_summary - the change removed a read the caller is entitled to"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading v_regional_engagement_summary produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OWN" dossier_activity_timeline; case "$R_KIND" in answered) OUT="$OUT dossier_activity_timeline=$R_N/32"; [ "$R_N" = "32" ] || RC=1;; denied) echo "FAIL: the owner is now denied dossier_activity_timeline - the change removed a read the caller is entitled to"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading dossier_activity_timeline produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OWN" theme_details; case "$R_KIND" in answered) OUT="$OUT theme_details=$R_N(empty-on-staging)";; denied) echo "FAIL: theme_details no longer answers an authenticated caller - the conversion broke the read path its consumer depends on"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading theme_details produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OWN" relationship_health_summary; case "$R_KIND" in answered) OUT="$OUT relationship_health_summary=$R_N(empty-on-staging)";; denied) echo "FAIL: relationship_health_summary no longer answers an authenticated caller - the conversion broke the read path its consumer depends on"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading relationship_health_summary produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OWN" v_country_relationship_flows; case "$R_KIND" in answered) OUT="$OUT v_country_relationship_flows=$R_N(empty-on-staging)";; denied) echo "FAIL: v_country_relationship_flows no longer answers an authenticated caller - the conversion broke the read path its consumer depends on"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading v_country_relationship_flows produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; READ "$OWN" engagement_recommendations_summary; case "$R_KIND" in answered) OUT="$OUT engagement_recommendations_summary=$R_N(empty-on-staging)";; denied) echo "FAIL: engagement_recommendations_summary no longer answers an authenticated caller - the conversion broke the read path its consumer depends on"; exit 1;; *) echo "INSTRUMENT-CANNOT-RUN: reading engagement_recommendations_summary produced $R_KIND (psql rc=$R_RC) :: $R_OUT"; exit 3;; esac; echo "P100-03 reads:$OUT"; [ "$RC" = "0" ] || { echo "FAIL: a recorded read count moved off its literal - a caller entitled to those rows lost them"; exit 1; }; echo "PASS reads"'
      text: 'the no-regression half with every read CLASSIFIED: answered (psql 0 with a numeric), denied (psql 3 with a permission-denied ERROR), or neither - and neither exits 3 as INSTRUMENT-CANNOT-RUN rather than being charged to the application. 5, 3 and 32 are non-zero literals measured at HEAD and prove the census can see rows; the four empty views are required only to ANSWER, because a count assertion on an empty view is a check that cannot fail.'
    - oracle: judge
      text: 'The SUMMARY records both applies with exit statuses, the oracle output verbatim from a post-apply run, the per-view client-construction check including any view that turned out to read as service_role, and the four empty views named as bounds.'
---

<objective>
Convert the seven views whose only caller is a Supabase Edge Function that builds its client from
`SUPABASE_ANON_KEY` plus the request's `Authorization` header - and therefore executes as
`authenticated` - to `security_invoker = on`.

Revoking `authenticated` on these would break the function; converting is the disposition their consumer
role dictates (100-CONTEXT.md D-03).
</objective>

<context>
@.planning/phases/100-security-posture/100-CONTEXT.md
@.planning/phases/100-security-posture/100-RESEARCH.md
@supabase/functions/geographic-visualization/index.ts
@supabase/functions/dossier-activity-timeline/index.ts
@supabase/functions/themes/index.ts

Ref notes: 100-CONTEXT.md - D-03 (the consumer-role rule), D-18 (the read classification); 100-RESEARCH.md - §3 (each view's calling edge function), §6 (per-caller counts at HEAD: only v_country_engagement_metrics=5, v_regional_engagement_summary=3 and dossier_activity_timeline=32 hold data), §9 (psql exit codes).

<interfaces>

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>the seven edge-function views stop executing as their definer</name>
  <files>the paths in files_modified</files>
  <read_first>100-CONTEXT.md D-01..D-37 (ALL of them - a task that cites D-19 or later needs the whole range) and 100-RESEARCH.md §0-§11 (the round-3 and round-4 derivations live in §10 and §11); re-RUN a recorded command rather than re-deriving its number a second way</read_first>
  <action>
    The criteria close on LIVE staging state, so writing the migration file is half the task:

    1. Write the migration file listed in files_modified. It MUST be IDEMPOTENT and safely re-runnable
       (D-13: no ledger row is created, so it may be replayed). Guard every statement — `ALTER VIEW ...
       SET (security_invoker = true)` is already idempotent; wrap anything that is not in `DO $$ ... $$`.
    2. Apply it: `PATH="/opt/homebrew/bin:$PATH"; set -a; . ./.env.test; set +a;
       psql "$SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f <the migration file>` — capture the output verbatim.
    3. Apply it a SECOND time and capture that output too. A non-zero exit on the replay means the
       migration is not idempotent; fix the migration, do not record the failure as expected.
    4. Run this task's oracles and record their verbatim output in the SUMMARY.

    Seven statements, one per view: `ALTER VIEW public.<view> SET (security_invoker = true);`

    Completeness pass, recorded in the SUMMARY:

    - For each of the seven, name the edge function that reads it and confirm from that file how its
      Supabase client is constructed. If any one of them turns out to use `SUPABASE_SERVICE_ROLE_KEY` for
      the read, say so - that view could have been revoked instead, and the record should show the check
      was made rather than assumed.
    - For each view, name the base tables and their RLS state, which inputs decide a row, and which of
      those a caller can still control.
    - Record the non-owner count before and after for all seven.

    **Four of the seven are EMPTY on staging** (`theme_details`, `relationship_health_summary`,
    `v_country_relationship_flows`, `engagement_recommendations_summary`). Their green establishes an
    unbroken read path and nothing about row scoping. Say so explicitly.
  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes.</acceptance_criteria>
  <done>All seven views carry security_invoker=true on staging, and the owner reads exactly 5, 3 and 32 rows from the three that hold data while the other four answer rather than denying.</done>
</task>

</tasks>

<verification>
POPULATION: the paths in files_modified, and the named database objects this plan owns.
OUTSIDE IT: every other plan's migration file and objects (P100-01..12 run against ONE live database
concurrently — assert only what you own, by name, never a global total: that is D-15, and a global
assertion here makes the wave order-dependent), the migration ledger (D-13: no ledger row is
fabricated), the advisor's `*_security_definer_function_executable` and `extension_in_public` classes
(100-CONTEXT.md §3), and the phase-level arithmetic, the register and the sign-off, which P100-15 owns
(D-37).
</verification>

<success_criteria>
The seven views read by edge functions under the caller's JWT execute as the caller, and the owner still reads 5, 3 and 32 on the three that hold data.
</success_criteria>

<output>
Create `.planning/phases/100-security-posture/100-03-SUMMARY.md` when done. It MUST record every command this
task ran with its VERBATIM output, the populations it re-derived with the commands that produced them, every
zero beside the control that proves the instrument could have seen a non-zero, and anything left for a named
later task. It MUST name the four empty-on-staging views and say what their greens do not establish.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete`.
Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as
**pending** no matter how many gates passed (measured on P99-32 and again on P99-61).
</output>
