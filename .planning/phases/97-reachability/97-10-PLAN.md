---
phase: 97-reachability
plan: 10
type: execute
wave: 4
depends_on: [97-09, 97-05]
files_modified:
  - frontend/src/components/layout/navigation-config.ts # the administration-group entries the table ruled NAV ENTRY
  - frontend/src/components/keyboard-shortcuts/CommandPalette.tsx # branch A of the palette filing order
  - tests/e2e/97-nav04-rows.spec.ts # NEW — the KEEP-row click-through oracle
autonomous: true
requirements: [NAV-04]
success_criterion: '4 (routes with no inbound link are resolved) — this plan executes the NAV ENTRY half of the decision table; 97-11 executes the DELETE half.'
truths:
  - "Decisions covered — D-06: the nav entries this plan adds are exactly the rows the single-writer decision table ruled NAV ENTRY, no more and no fewer, and each is proven by a click-through rather than by its presence in a config object; D-02: no out-of-phase surface gains an entry — /delegations and the legal-holds region stay owned elsewhere and untouched, and the intentional double-mount and demo routes are not 'fixed'."
must_haves:
  truths:
    - 'Every route the table ruled NAV ENTRY has a live sidebar entry that clicks through to a rendering page'
    - 'The count of new administration-group entries equals the count of NAV ENTRY rows in the table'
    - 'The command palette derives isAdmin from the real user role instead of hardcoding true'
    - 'Every entry lands in navigation-config.ts, the one nav source the product shell renders'
  artifacts:
    - path: 'tests/e2e/97-nav04-rows.spec.ts'
      provides: 'click-through oracle for each KEEP row, role- and viewport-named'
      contains: '@covers NAV-04'
  key_links:
    - from: 'frontend/src/components/keyboard-shortcuts/CommandPalette.tsx'
      to: 'the real admin role check'
      via: 'useAuthStore-derived isAdmin replacing the hardcoded true at :522'
      pattern: 'isAdmin'
    - from: 'frontend/src/components/layout/navigation-config.ts'
      to: '/admin/ai-usage'
      via: 'an administration-group NavigationItem rendered by SidebarNavItem'
      pattern: '/admin/ai-usage'
---

<objective>
Execute the NAV ENTRY half of `97-NAV04-DECISIONS.md`, and close the palette defect this phase's
own work extends.

**Every entry lands in `navigation-config.ts`.** The app has two nav data sources and only one of
them renders: `components/modern-nav/` is mounted solely by `routes/modern-nav-standalone.tsx`. An
entry added there would pass a grep and render nowhere — the data-structure claim D-10 refuses.
This is also why `/monitoring` counts as having NO live inbound link today despite the link at
`navigationData.ts:262`.

**The palette is not a wrinkle note.** `CommandPalette.tsx:522` hardcodes `isAdmin=true`, so every
administration entry becomes a palette command for all users — and the entries this plan adds make
it one worse. `RULING-P97-01`'s filing order allows exactly two answers, and because this plan owns
both the palette and the nav wiring, branch A (fix in-phase) is the expected election. The
fallback is fully specified so execution never needs replanning.

Purpose: the surviving orphan routes become reachable, and adding them does not widen a known leak.
Output: nav entries, one honest admin check, one oracle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-reachability/97-NAV04-DECISIONS.md (97-09's single-writer table — the ONLY authority for which entries exist; this plan reads it and never writes it)
@.planning/phases/97-reachability/97-UI-SPEC.md (§NAV-04 — the entry table, the "a nav entry is not a status surface" rule, the icon defaults, the nav-visibility-is-not-authorization paragraph)
@.tickmarkr/overseer/RULING-P97-01-ADMIN-DISPOSAL.md (the filing order)
@.tickmarkr/overseer/RULING-P97-03-OMISSION-FINDINGS.md (§3 oracle naming; §4 the free EO propagation)
@tests/e2e/95-monitoring-mounts.spec.ts (the oracle pattern — read in full)

<interfaces>
Verified on disk 2026-08-17.

- Administration group (`navigation-config.ts:166-186`): pushed only when `isAdmin === true`; its
  items are the same four-key `NavigationItem` shape as everywhere else. Existing members include
  `admin-ai-settings` (`:178`) and `admin-system` (`:184`). NOTE `admin-approvals` at `:212` sits
  OUTSIDE this group and points at the TOP-LEVEL `/approvals` — a different route from
  `/admin/approvals`; do not repurpose it and do not let a substring match conflate them.
- 97-05 has already added the Elected Officials row to the DOSSIERS group in this same file. Do not
  touch that row; wave ordering is what keeps these two plans off each other's lines.
- UI-SPEC's named icon defaults: `BarChart3` for ai-usage, `Gauge` for monitoring. `Activity` is
  already spent on `/activity` and must not be duplicated. Labels already exist in BOTH locales:
  `common:navigation.aiUsage` (`common.json:178`) and `common:navigation.monitoring` (`:171`) —
  re-derive rather than assume, and if one is missing add it in the same commit.
- **A nav entry is not a status surface** (UI-SPEC §NAV-04): no badge, no dot, no "beta"/"dev only"
  tag, no disabled style, no tooltip caveat, no muted text to signal that `/monitoring` is degraded
  in production. The honesty lives on the destination page, which already renders
  `QueryErrorState` truthfully, and in the decision row, which is a document rather than a pixel.
- `CommandPalette.tsx`: consumes `createNavigationGroups` at `:104`, calls it with a hardcoded
  `true` at `:522`, and does NOT import `useAuthStore` today (verified: zero `authStore` hits in the
  file). The honest check to copy is `layout/Sidebar.tsx:51-55` —
  `user?.role === 'admin' || user?.role === 'super_admin'` off `useAuthStore()`.
- Free consequence already recorded by 97-05: the EO entry propagates to the palette through the
  same `createNavigationGroups` call (`RULING-P97-03` §4). It does not resolve this defect.
- Oracle pattern: inline auth from `95-monitoring-mounts.spec.ts:29-66`, `INTERNAL_STRING` at
  `:54-55`, `--project=chromium-en --no-deps`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: add exactly the entries the table ruled NAV ENTRY</name>
  <files>frontend/src/components/layout/navigation-config.ts</files>
  <read_first>.planning/phases/97-reachability/97-NAV04-DECISIONS.md in full (the authoritative list of NAV ENTRY rows — read it before writing a single item); frontend/src/components/layout/navigation-config.ts in full (the lucide import block :6-28, the administration group :166-186, the unrelated admin-approvals item at :212, and the dossiers row 97-05 added); frontend/src/components/layout/Sidebar.tsx:120-160 (the row DOM and the active-state rule); frontend/src/i18n/en/common.json and frontend/src/i18n/ar/common.json (the navigation label block); 97-UI-SPEC.md §NAV-04</read_first>
  <action>
    Read the decision table first and add ONE `NavigationItem` per NAV ENTRY row into the
    administration group — no more, no fewer. Each is the same four keys as its siblings, using the
    already-shipped label key and the UI-SPEC icon default, with new icons added to the file's single
    lucide import block. Per the ruled dispositions this is expected to be `/admin/ai-usage` and,
    if the table ruled it so, `/monitoring`; the table governs, not this sentence.

    Re-derive each label key's presence in BOTH locale files with `command grep` before relying on
    it, and add any missing side in the same commit — a key present in English and absent in Arabic
    renders English in both languages.

    Add nothing else: no badge, no status affordance, no group, no section header, no reordering,
    and no visual differentiation between these rows and their siblings. If the table ruled a route
    NOT-CHECKED or PARKED, it gets no entry and this plan's SUMMARY says so explicitly rather than
    leaving its absence to be inferred.
  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && F="$R/frontend/src/components/layout/navigation-config.ts" && T="$R/.planning/phases/97-reachability/97-NAV04-DECISIONS.md" && test -f "$F" && test -f "$T" && ROWS=$(command grep -c 'NAV ENTRY' "$T") && test -n "$ROWS" && test "$ROWS" -ge 1 && ADDED=$(command grep -c "id: 'admin-" "$F") && test -n "$ADDED" && BASE=2 && test "$ADDED" -ge "$BASE" && for p in $(command grep -o "'/admin/[a-z-]*'" "$T" | sort -u | tr -d "'"); do :; done && command grep -q "'/admin/ai-usage'" "$F" && command grep -q '"aiUsage"' "$R/frontend/src/i18n/en/common.json" && command grep -q '"aiUsage"' "$R/frontend/src/i18n/ar/common.json" && cd "$R" && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>The decision table exists and declares at least one NAV ENTRY row; the administration group's `admin-` item count is at least its pre-phase baseline of 2 (`admin-ai-settings`, `admin-system`) — a floor rather than an equality, because the exact number is the TABLE's to decide at execution and freezing it here would be the moving-number class this phase is explicitly guarding against (D-08 applied to a nav count). The one ruled-certain entry, `/admin/ai-usage`, is pinned by path, and its label key is asserted in BOTH locale files separately so a half-present key is named rather than averaged (D-12). `pnpm typecheck` green. GATE DRILL: RED observed on the undone tree for the subject's reason — `'/admin/ai-usage'` does not appear in this file today, while the two i18n assertions in the same chain pass, which is the instrument control proving the greps and roots are live rather than silently missing (C5). GREEN constructed in a scratch copy. The reachability proof is Task 3's, not this gate's: an entry in a config object is a data-structure claim, which D-10 does not accept.</acceptance_criteria>
  <done>The surviving orphan routes are registered in the nav source that actually renders.</done>
</task>

<task type="auto">
  <name>Task 2: the command palette derives isAdmin instead of asserting it</name>
  <files>frontend/src/components/keyboard-shortcuts/CommandPalette.tsx</files>
  <read_first>.planning/phases/97-reachability/97-NAV04-DECISIONS.md (which branch of the filing order was elected, and its evidence); frontend/src/components/keyboard-shortcuts/CommandPalette.tsx:95-115 and :500-545 (the createNavigationGroups import and the hardcoded call at :522 with its useMemo dep array); frontend/src/components/layout/Sidebar.tsx:45-60 (the honest check, verbatim); frontend/src/store/authStore.ts (the store's user shape and role field)</read_first>
  <action>
    **Branch A (elected unless the table says otherwise):** import `useAuthStore` into the palette,
    derive `isAdmin` exactly as `Sidebar.tsx:54` does — `user?.role === 'admin' || user?.role ===
    'super_admin'` — pass it to `createNavigationGroups` in place of the hardcoded `true`, and add it
    to the `useMemo` dependency array so the group list recomputes when the role resolves. That last
    part is not cosmetic: a memo that captures the first render's value would re-freeze the very bug
    in a subtler form.

    Change nothing else in the palette: no new commands, no reordering, no styling, no keyboard
    behaviour. The Elected Officials entry 97-05 added propagates here for free through the same
    call and needs no work.

    **Branch B (only if branch A proves not to be a small honest fix — for example the palette's
    render context cannot reach the auth store):** revert the attempt, leave the palette untouched,
    and hand the `PALETTE-ADMIN-01` register-row text — id, owner, and the evidence line — to 97-12,
    which is this phase's ONLY writer of `.planning/REQUIREMENTS.md`. Do not write that file here;
    two writers on one path is what the single-writer rule exists to prevent. Record in the SUMMARY
    exactly what blocked branch A.

    Either way, state plainly in the SUMMARY that this is a VISIBILITY fix, not an authorization
    one: the admin routes remain URL-reachable regardless, and server-side authorization is Phase
    100's RLS territory.
  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && P="$R/frontend/src/components/keyboard-shortcuts/CommandPalette.tsx" && S="$R/frontend/src/components/layout/Sidebar.tsx" && T="$R/.planning/phases/97-reachability/97-NAV04-DECISIONS.md" && test -f "$P" && test -f "$S" && test -f "$T" && command grep -q 'super_admin' "$S" && if command grep -q 'PALETTE-ADMIN-01 — branch B' "$T"; then command grep -q 'PALETTE-ADMIN-01' "$T"; else command grep -q 'useAuthStore' "$P" && command grep -q 'super_admin' "$P" && test "$(sed -n '/const allNavPages = useMemo/,/}, \[/p' "$P" | command grep -c 'true)')" -eq 0; fi && cd "$R" && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>The gate is BRANCH-AWARE, which is why it reads the decision table first: if branch B was elected the table must carry the `PALETTE-ADMIN-01` row, and if branch A was elected the palette must import the auth store, use the same `super_admin` check the Sidebar uses, and no longer pass a literal `true` — that last check is scoped INSIDE the `allNavPages` memo's own sed range so an unrelated `true)` elsewhere in a 900-line file cannot satisfy or break it (C4, C8). `Sidebar.tsx`'s `super_admin` check is asserted in the same chain as the instrument control: it proves the grep sees the token in a file that certainly contains it, so a zero in the palette is a real zero (C5). `pnpm typecheck` green. GATE DRILL: RED observed on the undone tree — on branch A because `useAuthStore` is absent from the palette today and the literal `true` is present inside the memo range; the branch-B arm is drilled by constructing a table stub carrying the branch-B marker without the row and observing red. GREEN constructed in a scratch copy for both arms.</acceptance_criteria>
  <done>Adding admin entries no longer widens a leak, or the leak has an owner and an id.</done>
</task>

<task type="auto">
  <name>Task 3: tests/e2e/97-nav04-rows.spec.ts — every KEEP row clicked, at the right role</name>
  <files>tests/e2e/97-nav04-rows.spec.ts</files>
  <read_first>.planning/phases/97-reachability/97-NAV04-DECISIONS.md (the KEEP rows this spec must cover, one test each); tests/e2e/95-monitoring-mounts.spec.ts in full (the header contract, inline auth, INTERNAL_STRING, the settle-not-hang assertion); frontend/src/components/layout/Sidebar.tsx:120-160 (the row DOM to click); frontend/src/pages/monitoring/Dashboard.tsx (the monitoring-health-error / monitoring-alerts-error testids the degraded state renders); .tickmarkr/overseer/RULING-P97-03-OMISSION-FINDINGS.md §3</read_first>
  <action>
    Header `// @covers NAV-04` in the 95 shape. WHAT THIS KILLS: an entry present in
    `navigation-config.ts` that renders nowhere, or renders somewhere blank.

    **The role naming is the point of this file.** The administration group is emitted only when
    `isAdmin` is true, so every test here proves a scoped claim and must SAY SO: each title contains
    `adminOnly user` and `desktop 1400`. Add one test titled with `ordinary non-admin user` that
    signs in as a non-admin if such an account is available and asserts the administration group is
    ABSENT from the sidebar — and, after Task 2, that its entries are absent from the command
    palette too. If no non-admin account is available in `.env.test`, mark that ONE test
    `test.fixme` with a comment naming the missing fixture; a skipped negative test must never read
    as a proven negative, and its absence is 97-12's weakest-point material.

    One test per KEEP row: open the sidebar, click the row's link by its accessible name inside the
    live `aside`, assert the URL, and assert the destination's own content settles. For
    `/monitoring`, the honest assertion is that the page mounts and each widget settles either to
    data or to its own inline error testid (`monitoring-health-error`, `monitoring-alerts-error`) —
    in production both settle to the error state because the API sits behind the backend's
    dev/test guard, so a spec that demanded data would be asserting something the product does not
    promise. Assert the leak regex in every test.

    Do not assert anything about DELETE rows here — 97-11 owns proving those paths are gone, and
    splitting that claim across two files would leave each half looking complete.
  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && F="$R/tests/e2e/97-nav04-rows.spec.ts" && test -f "$R/tests/e2e/95-monitoring-mounts.spec.ts" && test -f "$F" && command grep -q '@covers NAV-04' "$F" && command grep -q 'adminOnly user' "$F" && command grep -q 'ordinary non-admin user' "$F" && test "$(command grep -c 'modern-nav' "$F")" -eq 0 && D=$(command grep -c "adminOnly user\|ordinary non-admin user" "$F") && T=$(command grep -c "^\s*test(" "$F") && test "$D" -eq "$T" && cd "$R" && pnpm exec playwright test tests/e2e/97-nav04-rows.spec.ts --project=chromium-en --no-deps</automated>
  </verify>
  <acceptance_criteria>The spec exists, carries the `@covers NAV-04` header, names BOTH roles, contains zero `modern-nav` references, and every `test(` declaration carries a role-named title — the equality between role-named titles and test declarations is RULING-P97-03 §3 turned into a gate obligation, so an unnamed test added later goes red rather than quietly publishing an unscoped claim (C10). The behavioural run uses `--no-deps` because the `setup` project throws without six `E2E_*` keys `.env.test` does not carry (E2ECRED-01, Phase 101; D-12 forbids waiting on it), and it is a single path so the multi-path filter hazard does not arise. GATE DRILL: RED observed on the undone tree (the file does not exist, and before Task 1 the entries it clicks do not either); GREEN for the structural half constructed in a scratch copy. The Playwright half is `NOT CONSTRUCTED: requires the running dev stack, an admin session, and Task 1's entries; drilled at execution` — capture its per-test output as the attribution, not the exit code.</acceptance_criteria>
  <done>Every KEEP row is proven reachable, at a named role and a named viewport.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                                  |
| ----------------------------------- | ---------------------------------------------------------------------------------------------- |
| user role → nav/palette visibility  | the palette currently asserts admin rather than deriving it, exposing admin commands to all users |
| nav entry → destination page        | a new entry point must land on a page that renders honestly, degraded or not                 |

## STRIDE Threat Register

| Threat ID | Category               | Component                          | Disposition | Mitigation Plan                                                                                                          |
| --------- | ---------------------- | ---------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------- |
| T-97-29   | Elevation of Privilege | `CommandPalette.tsx:522`           | mitigate    | branch A derives `isAdmin` from the real role, matching `Sidebar.tsx:54`, with the memo dep added so it cannot re-freeze |
| T-97-30   | Spoofing               | nav visibility read as authorization | accept    | documented, not fixed here: routes stay URL-reachable and server-side authz is Phase 100's RLS territory                 |
| T-97-31   | Information Disclosure | `/monitoring` degraded page        | mitigate    | the oracle accepts the inline `QueryErrorState` testids as an honest settle and asserts no internals reach the DOM       |
| T-97-32   | Repudiation            | an entry not authorised by a row   | mitigate    | Task 1 reads the decision table first and adds only its NAV ENTRY rows; extras are a gate and review failure             |
| T-97-SC   | Tampering              | package installs                   | accept      | zero installs this phase                                                                                                 |

</threat_model>

<verification>
- All three gates green; `pnpm typecheck` green from the root.
- POPULATION DEFINITION: the NAV ENTRY rows of `97-NAV04-DECISIONS.md`, plus the palette defect.
  OUTSIDE IT: the DELETE rows (97-11), the intended-broken `/delegations` and legal-holds surfaces
  (Phases 102 and 100, untouched), `components/modern-nav/*` (classified, never edited), and the
  backend `NODE_ENV` guard behind `/monitoring`'s production degradation, which is named in the
  record and deliberately not widened into.
- If the non-admin negative test could not be run for want of a fixture, that is recorded as
  UNABLE TO MEASURE and handed to 97-12 — an unproven negative is not a proven one.
</verification>

<success_criteria>
Every route the table kept is reachable by clicking a real affordance as a named role, and the
phase did not widen the palette leak it inherited.
</success_criteria>

<output>
Create `.planning/phases/97-reachability/97-10-SUMMARY.md` when done. It MUST record: the NAV ENTRY
rows read from the table and the entries actually added, one to one; any NOT-CHECKED or PARKED row
that deliberately received no entry; which palette branch executed and, if B, exactly what blocked
A along with the register text handed to 97-12; the re-derived both-locale results for every label
key used; and the per-test colour of the NAV-04 oracle including the non-admin negative's state.
</output>
