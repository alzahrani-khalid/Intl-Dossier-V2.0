---
phase: 97-reachability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/97-digests-tab.spec.ts # NEW — NAV-03 tab click-through oracle
  - tests/e2e/97-list-create-affordances.spec.ts # NEW — NAV-03 eight-page oracle, DATA PRESENT
autonomous: true
requirements: [NAV-03]
success_criterion: '3 (Digests tab in the tab bar + every list page exposes a create affordance) — this plan writes the OBSERVERS only; both specs are expected RED until 97-08 lands.'
truths:
  - 'Decisions covered — D-05: the population is THE 8 LIST PAGES, hardcoded as a literal in the spec and named one-by-one, with elected-officials identified in the file as the 1-of-8 that already ships the affordance; D-10: both specs click the REAL rendered affordance and each test title names its role and viewport per RULING-P97-03 §3, and the pair is invoked with spec-file existence asserted FIRST because Playwright spec paths are FILTERS.'
must_haves:
  truths:
    - 'The create-affordance oracle asserts the affordance WITH ROWS PRESENT — the shipped empty-state CTA cannot satisfy it (Pitfall 4)'
    - 'The eight-page population is a hardcoded literal of length 8, asserted as 8 inside the spec'
    - 'The digests oracle clicks the tab in the real tab bar and asserts DigestsTab content mounts'
  artifacts:
    - path: 'tests/e2e/97-digests-tab.spec.ts'
      provides: 'NAV-03 digests tab click-through oracle'
      contains: '@covers NAV-03'
    - path: 'tests/e2e/97-list-create-affordances.spec.ts'
      provides: 'NAV-03 eight-page create-affordance oracle'
      contains: '@covers NAV-03'
  key_links:
    - from: 'tests/e2e/97-list-create-affordances.spec.ts'
      to: '/dossiers/{segment}/create'
      via: 'clicking the header create button and asserting the create route mounts'
      pattern: '/create'
---

<objective>
Write the two Wave-0 observers criterion 3 is judged by. They land before their subject (97-08)
and are expected RED.

The failure this pair exists to prevent is named in RESEARCH Pitfall 4: seven of the eight list
pages already pass an `onCreate` handler — into `ListEmptyState` only. A sweep that greps
`onCreate` declares 8/8 done while a user with data present has no create path at all. **The
affordance is only proven with rows on screen**, so data-presence is a precondition of the
oracle, not an assumption behind it.

Purpose: criterion 3 gets an observer that the current tree fails for the right reason.
Output: two spec files. No product code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-reachability/97-UI-SPEC.md (§NAV-03 — the exact recipe and the "visible WITH rows present" criterion; §Verification Notes "Hardcode 8")
@.planning/phases/97-reachability/97-PATTERNS.md (§Oracles; §NAV-03(b) — the EO model at elected-officials/index.tsx:196-209)
@.planning/phases/97-reachability/97-VALIDATION.md
@tests/e2e/95-monitoring-mounts.spec.ts (the house pattern — read in full)

<interfaces>
Verified on disk 2026-08-17:

- The EIGHT list pages (this is the population; `{segment}` is the URL segment):
  `countries`, `organizations`, `persons`, `forums`, `topics`, `working_groups`,
  `elected-officials`, `engagements` — each at `/dossiers/{segment}` with a
  `/dossiers/{segment}/create` route that exists on disk. NOTE `engagements` also mounts at
  top-level `/engagements`; that double-mount is INTENTIONAL and must not be "fixed" — this spec
  addresses the `/dossiers/engagements` mount.
- `elected-officials` is the 1-of-8 that already ships the affordance
  (`elected-officials/index.tsx:196-209`: `PageHeader` `actions` slot → `<Button asChild>` →
  `<Link to="/dossiers/elected-officials/create">` with a `Plus` icon). Its label comes from
  `elected-officials:list.add`; the other seven will use their existing
  `empty-states:list.{entity}.cta` key so header and empty-state say the same words.
- Tab bar: `components/workspace/WorkspaceTabNav.tsx:74-100` renders each `WORKSPACE_TABS` entry
  as a TanStack `<Link role="tab" aria-selected>`; the digests entry does not exist yet and the
  route `routes/_protected/engagements/$engagementId/digests.tsx` does (it lazy-loads
  `components/intelligence/DigestsTab.tsx`).
- Playwright: `--project=chromium-en --no-deps`. `--list` does NOT exempt the `setup` dependency
  project either (GATE-STANDARD C6) — pass `--no-deps` to `--list` too.
- Inline auth + `INTERNAL_STRING`: `95-monitoring-mounts.spec.ts:29-66` and `:54-55`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/e2e/97-digests-tab.spec.ts — the Digests tab is in the bar and reaches DigestsTab</name>
  <files>tests/e2e/97-digests-tab.spec.ts</files>
  <read_first>tests/e2e/95-monitoring-mounts.spec.ts in full; frontend/src/components/workspace/WorkspaceTabNav.tsx in full (WORKSPACE_TABS :26-35 and the Link recipe :74-100 — the DOM this spec queries); frontend/src/components/intelligence/DigestsTab.tsx (what content proves the tab mounted); 97-UI-SPEC.md §NAV-03(a)</read_first>
  <action>
    Header block `// @covers NAV-03` in the 95 shape (WHAT THIS KILLS: a tab entry present in the
    `WORKSPACE_TABS` array but never clicked is a data-structure claim, exactly what D-10 forbids).
    Inline auth + `INTERNAL_STRING` copied verbatim.

    The spec needs a real engagement id. Derive one inside the test rather than hardcoding a UUID:
    navigate to the engagements list, click the first row, and read the `$engagementId` out of the
    resulting URL. If the list is empty, fail with an explicit message naming the precondition —
    an empty fixture must read as UNABLE TO MEASURE, never as a pass.

    Two tests, both titled `ordinary authenticated user, desktop 1400`:
    (1) the tab bar (`[role="tablist"]` region rendered by `WorkspaceTabNav`) contains a tab whose
    accessible name is the Digests label, positioned after Signals; clicking it navigates to
    `/engagements/{id}/digests` and the DigestsTab's own content settles (assert a stable element
    the component renders — pick it by reading `DigestsTab.tsx`, and prefer a `data-testid` it
    already has; if it has none, assert its heading text and record in the SUMMARY that no testid
    existed).
    (2) the clicked tab carries `aria-selected="true"` afterwards and no OTHER tab does — a tab bar
    with two selected tabs is a navigation lie.

    Assert the leak regex in both tests.
  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && test -f "$R/tests/e2e/95-monitoring-mounts.spec.ts" && test -f "$R/tests/e2e/97-digests-tab.spec.ts" && command grep -q '@covers NAV-03' "$R/tests/e2e/97-digests-tab.spec.ts" && command grep -q 'aria-selected' "$R/tests/e2e/97-digests-tab.spec.ts" && command grep -q 'digests' "$R/tests/e2e/97-digests-tab.spec.ts" && D=$(command grep -c "ordinary authenticated user" "$R/tests/e2e/97-digests-tab.spec.ts") && T=$(command grep -c "^\s*test(" "$R/tests/e2e/97-digests-tab.spec.ts") && test "$D" -eq "$T" && test "$T" -ge 2</automated>
  </verify>
  <acceptance_criteria>The spec exists with the `@covers NAV-03` header, asserts `aria-selected`, and every `test(` declaration carries a role-and-viewport-named title (equality, not `-ge`, so an unnamed test added later goes red — RULING-P97-03 §3 as a gate obligation per C10). The 95 spec's existence in the same `&&` chain is the instrument control proving the search root and grep are live (C5); no `2>/dev/null` anywhere. GATE DRILL: recorded in THIS PLAN'S SUMMARY (97-12 consolidates it into `97-GATE-DRILL.md`, which it alone writes) — RED observed against the undone tree (file absent); GREEN constructed in a scratch copy. The Playwright RUN is excluded by design: the tab does not exist until 97-08.</acceptance_criteria>
  <done>The Digests tab has a click-through observer.</done>
</task>

<task type="auto">
  <name>Task 2: tests/e2e/97-list-create-affordances.spec.ts — eight pages, affordance visible WITH ROWS PRESENT</name>
  <files>tests/e2e/97-list-create-affordances.spec.ts</files>
  <read_first>tests/e2e/95-monitoring-mounts.spec.ts in full; frontend/src/routes/_protected/dossiers/elected-officials/index.tsx:190-215 (the shipped model this spec expects the other seven to match); frontend/src/components/list-page/ListPageShell.tsx in full (the header the affordance lands in); 97-UI-SPEC.md §NAV-03(b) and §Verification Notes</read_first>
  <action>
    Header `// @covers NAV-03`. WHAT THIS KILLS states Pitfall 4 in its own words: seven pages
    already pass `onCreate` into `ListEmptyState`, so a green built on an EMPTY list would certify
    a page that shows a user with data no way to create anything.

    Declare the population as a literal array of the eight `{segment}` strings from the interfaces
    block, each with the create path it must reach. Immediately assert `PAGES.length === 8` so a
    hand-edit that drops one fails loudly instead of silently narrowing the population — the
    hardcode-the-count law.

    One test per page (8 tests), each titled with the segment plus
    `ordinary authenticated user, desktop 1400`:
    - go to `/dossiers/{segment}`;
    - establish the DATA-PRESENT precondition explicitly: wait for the list region to contain at
      least one row. If the page settles EMPTY, the test fails with a message naming
      `DATA-PRECONDITION UNMET for {segment}` — this is UNABLE TO MEASURE, not a pass, and 97-08's
      executor must seed or record it (C2: a red for a fixture reason is labelled, never counted
      as the subject's red);
    - locate the create affordance in the page HEADER region (not inside an empty state) by its
      accessible name and assert it is visible;
    - click it and assert the URL becomes `/dossiers/{segment}/create` and the create surface's own
      content settles;
    - assert the leak regex.

    Add one final test asserting `elected-officials` is the page that ALREADY satisfies this on the
    undone tree, with a comment saying so — it is this spec's own positive control: if the whole
    file goes red including that page, the failure is the harness, not the subject.
  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && test -f "$R/tests/e2e/97-list-create-affordances.spec.ts" && test -d "$R/frontend/src/routes/_protected/dossiers" && P=$(ls -d "$R/frontend/src/routes/_protected/dossiers"/*/ | wc -l | tr -d ' ') && test -n "$P" && test "$P" -ge 8 && command grep -q '@covers NAV-03' "$R/tests/e2e/97-list-create-affordances.spec.ts" && command grep -q 'DATA-PRECONDITION UNMET' "$R/tests/e2e/97-list-create-affordances.spec.ts" && for s in countries organizations persons forums topics working_groups elected-officials engagements; do command grep -q "$s" "$R/tests/e2e/97-list-create-affordances.spec.ts" || exit 1; done && D=$(command grep -c "ordinary authenticated user" "$R/tests/e2e/97-list-create-affordances.spec.ts") && T=$(command grep -c "^\s*test(" "$R/tests/e2e/97-list-create-affordances.spec.ts") && test "$D" -eq "$T" && test "$T" -ge 9</automated>
  </verify>
  <acceptance_criteria>All eight segments appear in the spec (checked one at a time so the gate names WHICH one is missing rather than reporting a count that hides it); the data-present precondition is present as an explicit failure string, so an empty fixture reads as UNABLE TO MEASURE; every test title is role-and-viewport-named (equality against the `test(` count); the `dossiers` route directory is proven to exist and its subdirectory count proven `>= 8` in the same chain (C5 — a mistyped root would otherwise make the loop vacuously pass). GATE DRILL: recorded in THIS PLAN'S SUMMARY (97-12 consolidates it into `97-GATE-DRILL.md`, which it alone writes) — RED observed on the undone tree (file absent); GREEN constructed in scratch. The Playwright RUN is excluded: seven of eight pages fail by design until 97-08.</acceptance_criteria>
  <done>Criterion 3's eight-page half has an observer that an empty list cannot satisfy.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                                       |
| ------------------------ | ------------------------------------------------------------------ |
| test env → repo          | `.env.test` credentials read, never echoed, never committed       |
| rendered DOM → assertion | list and workspace surfaces may leak internals                    |

## STRIDE Threat Register

| Threat ID | Category               | Component           | Disposition | Mitigation Plan                                                                           |
| --------- | ---------------------- | ------------------- | ----------- | ------------------------------------------------------------------------------------------- |
| T-97-04   | Information Disclosure | spec source         | mitigate    | credentials via `process.env` only; helper throws on absence, prints nothing (95 pattern) |
| T-97-05   | Information Disclosure | list/workspace DOM  | mitigate    | `INTERNAL_STRING` asserted in every test, regex reused unwidened                          |
| T-97-SC   | Tampering              | package installs    | accept      | zero installs this phase (RESEARCH §Package Legitimacy Audit)                             |

</threat_model>

<verification>
- Both gates green.
- Both specs expected RED on run; the SUMMARY records the red with its attribution.
- The PAIR is invoked as `pnpm exec playwright test tests/e2e/97-digests-tab.spec.ts
  tests/e2e/97-list-create-affordances.spec.ts --project=chromium-en --no-deps` ONLY after both
  files are asserted to exist — two paths where one does not match silently drops the other at
  exit 0 (Pitfall 5). Any plan or gate invoking the pair repeats that existence assertion.
- POPULATION DEFINITION: the NAV-03 observers only. OUTSIDE IT: NAV-01/02 observers (97-01),
  NAV-04's (97-10), the `/engagements` top-level mount (intentional double-mount, untouched).
</verification>

<success_criteria>
Criterion 3 has two observers on disk, one of which cannot be satisfied by an empty list and the
other of which cannot be satisfied by an array entry.
</success_criteria>

<output>
Create `.planning/phases/97-reachability/97-02-SUMMARY.md` when done. It MUST record: the exact
test titles, the observed red of each spec with attribution, whether `DigestsTab.tsx` already
carried a `data-testid` (and which), and any page whose data-present precondition could not be met
in the current fixture — that list is 97-08's seeding obligation.
</output>
</content>
