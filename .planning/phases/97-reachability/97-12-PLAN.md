---
phase: 97-reachability
plan: 12
type: execute
wave: 5
depends_on: [97-01, 97-02, 97-03, 97-04, 97-05, 97-06, 97-07, 97-08, 97-09, 97-10, 97-11]
files_modified:
  - .planning/phases/97-reachability/97-CLOSING-DERIVATION.md # NEW — coverage, populations, weakest point
  - .planning/phases/97-reachability/97-GATE-DRILL.md # NEW — the consolidated per-gate C1..C10 record
  - .planning/REQUIREMENTS.md # the ONLY writer of this file in the phase
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04]
success_criterion: 'all four — this plan re-derives coverage by command, consolidates the drill record, updates the register, and names the phase weakest point.'
truths:
  - 'Decisions covered — D-01: the phase closes 4 requirements NAV-01..04, each mapped to its criterion and RE-DERIVED from the register by command, with the ROADMAP-versus-register match STATED rather than assumed, and no requirement, sub-item or route silently dropped; D-11: every gate meets GATE-STANDARD-P92 in BOTH directions or is UNPROVEN-labelled, drilled via scripts/gate-drill.mjs, with zero vacuous guards and post-close mutations naming the closed gates whose subjects they changed, C9a swept and C9b run through scripts/c9b-sweep.sh only; D-13: the mechanical decision-coverage gate runs green with uncovered [] and its falsification drill lands on disk.'
must_haves:
  truths:
    - 'Requirement coverage 4/4 is re-derived from the register by command, not asserted'
    - 'Decision coverage runs green with uncovered [] and a falsification drill is on disk'
    - 'Every gate in the phase has a drill row with a verdict, including CANNOT CONSTRUCT rows'
    - 'The report names the phase weakest point; a uniform pass is itself a finding'
    - 'The register rows for NAV-01..04 reflect what actually shipped, bounds included'
  artifacts:
    - path: '.planning/phases/97-reachability/97-CLOSING-DERIVATION.md'
      provides: 'coverage derivations, the closing population statements, and the named weakest point'
      contains: 'WEAKEST POINT'
    - path: '.planning/phases/97-reachability/97-GATE-DRILL.md'
      provides: 'one row per gate with both directions and a verdict'
      contains: 'CANNOT CONSTRUCT'
  key_links:
    - from: '.planning/REQUIREMENTS.md'
      to: '.planning/phases/97-reachability/97-CLOSING-DERIVATION.md'
      via: 'register status rows citing the closing derivation and its bounds'
      pattern: 'Phase 97'
---

<objective>
Close the phase honestly.

Three things make this plan more than bookkeeping. **Coverage is re-derived, not asserted** — the
ROADMAP line matches the register this phase for the first time in four phases, and the derivation
still runs and says so rather than trusting the agreement. **The drill record is consolidated by one
author** — GATE-STANDARD-P92 exists because per-plan repairs were each locally correct and the SET
was wrong; isolation was the mechanism, so one seat holds all twelve plans at once here. **The report
names the weakest point** — a uniform pass is a DOWNGRADE in the acceptance's own terms, which means
finding nothing weak is itself the finding to report.

**Single-writer, by name.** This plan is the phase's ONLY writer of `.planning/REQUIREMENTS.md`.
97-10 may have handed it a `PALETTE-ADMIN-01` register row and 97-04 may have handed it
`PARALLEL-TRUTH-01` residue; both arrive as text in a SUMMARY and are written here, in the
95-04 → 95-09 hand-off shape, so two plans never write one file.

Purpose: what shipped, what did not, and where the weakest green is.
Output: two records and the register.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.tickmarkr/overseer/GATE-STANDARD-P92.md (C1..C10 and the pass procedure — one author, all gates, one pass)
@.tickmarkr/overseer/ACCEPTANCE-P97-PLAN.md (the ten conditions this phase is graded against)
@.planning/phases/97-reachability/97-POPULATIONS.md (both populations, to restate as closing statements)
@.planning/phases/97-reachability/97-NAV04-DECISIONS.md (the decision table, for the coverage statement and any NOT-CHECKED or PARKED row)
@.planning/REQUIREMENTS.md (NAV-01..04 definitions at lines 132-135 and register rows **551-554**)

<interfaces>
Verified on disk 2026-08-17.

- Requirement coverage derivation: the four ids are `NAV-01`, `NAV-02`, `NAV-03`, `NAV-04`, defined
  at `.planning/REQUIREMENTS.md:132-135` and registered at rows **`551-554`** (all `Pending` at
  phase start, verified 2026-08-17). **`543-546` was the stale figure carried in this plan, in
  `97-CONTEXT.md` and in `ACCEPTANCE-P97-PLAN.md`; the rows have MOVED.** Re-derive with
  `command grep -n "^| NAV-0" .planning/REQUIREMENTS.md` rather than trusting any of the three —
  the register grows every phase, so a pinned line number is a decaying claim. The gate's own
  row-prefix grep is line-independent and verified matching, so this was a navigation error rather
  than a gate error; the two out-of-plan documents still carry the stale numbers and their
  correction is the orchestrator's, since this phase's commit pathspec is the PLAN files.
  The ROADMAP's `**Requirements:**` line for Phase 97 lists the same four — the match is REAL this
  phase and the derivation states it rather than assuming it.
- Decision coverage instrument: `node scripts/decision-coverage.mjs <phase_dir> <context_path>`.
  It strips fenced blocks and HTML comments first, scans frontmatter `must_haves` / `truths` /
  `objective` plus body sections matching `must_haves|truths|tasks|objective`, and matches a
  mechanical `\bD-NN\b`. It is STRICTER than the real gate (no soft-phrase match), so a green here
  is a conservative green. Everything from `### Claude's Discretion` onward is untracked.
- Gate drill instrument: `node scripts/gate-drill.mjs <phase_dir> --json --timeout <seconds>`.
  Note the argument quirk recorded in the script's own header — the documented bare `<dir>` form
  printed usage until it was fixed, so pass the flags. It extracts every automated-gate block,
  `bash -n` parses each, and runs each. **A green from this script is NOT evidence a gate is
  sound**: it cannot tell a red-because-the-subject-is-absent from a red-for-the-right-reason. The
  constructed-done-state half is authored per gate and lives in each plan's SUMMARY; this plan
  consolidates those rows.
- C9b instrument: `scripts/c9b-sweep.sh` ONLY (ACCEPTANCE condition 5).
- The drill row shape from GATE-STANDARD: `| plan.gate | C1 red | C1 green (how the done state was
constructed) | C2–C10 exceptions | verdict |` with verdict in
  `SOUND` / `REPAIRED (what changed)` / `CANNOT CONSTRUCT (what and why)`. **A gate whose done state
  was not constructed is reported CANNOT CONSTRUCT, never SOUND.**
- Known coarse-aggregate gates in this phase (`pnpm typecheck`, `pnpm lint`, `pnpm --filter frontend
build`, whole Playwright runs): for each, the drill row must carry the CAPTURED error text
  attributing the red to the gate's own subject. A non-zero exit alone proves only that something
  failed, and is graded as insufficient.
- Environment: `.planning/`-only commits skip the pre-commit build hook; prettier re-wraps
  frontmatter block sequences, so re-verify that machine-readable frontmatter still PARSES after
  every hook run (the P95 empty-map incident).
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: the consolidated gate-drill record, one author, all gates</name>
  <files>.planning/phases/97-reachability/97-GATE-DRILL.md</files>
  <read_first>.tickmarkr/overseer/GATE-STANDARD-P92.md in full (C1..C10 and the pass procedure, including why a single author holds all plans at once); scripts/gate-drill.mjs in full (what it establishes and, more importantly, what it does not); every 97-*-SUMMARY.md in the phase directory (each plan's own drill observations, which this record consolidates)</read_first>
  <action>
    Run `node scripts/gate-drill.mjs .planning/phases/97-reachability --json --timeout 900` and keep
    its output. It gives you the mechanical half: every automated-gate block parses, and each one's
    result against the tree as it now stands.

    Then write one row per gate in the GATE-STANDARD shape, consolidating the per-plan observations
    from the SUMMARYs. For every gate record how the RED was observed and — this is the clause that
    catches the dominant defect class — how the work-done state was CONSTRUCTED to observe the GREEN.
    Where a done state could not be constructed (anything needing the running dev stack, an admin
    session, or seeded rows), the row says `CANNOT CONSTRUCT (what and why)` and the verdict is not
    SOUND. An honest `NOT CONSTRUCTED: <what and why>` is acceptable; silence is not.

    **CLOSED-VOCABULARY CHECK on the verdict set, because a PARTIAL construction has no term of its
    own and the default reading is the wrong one.** GATE-STANDARD fixes three verdicts — `SOUND`,
    `REPAIRED (what changed)`, `CANNOT CONSTRUCT (what and why)` — and this phase has SIX gates whose
    grep half was constructed in scratch while their Playwright or dev-stack half carries a
    `NOT CONSTRUCTED` label (97-05 T3, 97-06 T2, 97-07 T3, 97-08 T1, 97-08 T3, 97-10 T3). There is no
    fourth term for "half constructed", and the standard already answers it: **a gate whose done
    state was not FULLY constructed is `CANNOT CONSTRUCT`, never `SOUND`** — state that rule at the
    top of the record so no row splits the difference, and name in each such row exactly which half
    was constructed and which was not. The set is otherwise complete; say so rather than leaving the
    check implicit.

    **THIRD-DIRECTION ROWS.** Two directions — red on the undone tree and green on a constructed done
    state — are now known to be BLIND to the green-on-WRONG class (`RULING-P97-06` §1, CONFIRMED on
    its first outing against this very plan set). Where a gate was drilled against a constructed
    WRONG state as well, record that third observation and what the wrong state was. The plan set
    carries them for 97-04 Task 1 (neutered `AssertNever`; hand-typed `DOSSIER_CARD_TYPES`), 97-05
    Task 2 (hoisted `ZERO_STATS`), 97-09 Task 1 (a linked path ruled `NAV ENTRY`; an orphan ruled
    `ALREADY-REACHABLE`; an invented term; a missing token), 97-10 Task 1 (a duplicate row; a row
    with no entry; an unauthorised entry), 97-11 Tasks 1 and 2 (the prose-contamination
    reproduction), and 97-12 Task 3 (a row left `Pending`; a second declared writer). A gate with no
    third-direction row is not thereby unsound — it is UNTESTED IN THAT DIRECTION, and the record
    says which, because a bound on the record is worth more than a clean word.

    For every coarse-aggregate gate, the row carries the CAPTURED text that attributes the red to
    that gate's own subject. `pnpm typecheck` exiting non-zero in a scratch copy establishes only
    that something failed; the compiler message naming the assertion is what establishes that the
    guard fired. The same rule applies to `pnpm lint`, the build, and any whole-suite run.

    Sweep C9 explicitly and record it: grep the whole plan set for every oracle, artifact, script and
    testid name that changed during execution, and paste the counts, so a rename that was correct in
    one plan and stale in its consumer is caught here rather than after acceptance.

    Run `bash scripts/c9b-sweep.sh` for the mock-versus-real consumer sweep — that script only,
    per ACCEPTANCE condition 5 — and record its output with each consumer triaged: a spec that is red
    because this phase legitimately changed the DOM is repaired and named; a mocked spec that would
    pass regardless is named NON-ORACLE and never cited as evidence.

    Close with the post-close mutation statement: 97-11's deletions changed route-path and
    route-count subjects, so name every gate in the phase whose subject that touched and its state
    afterwards.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && F="$R/.planning/phases/97-reachability/97-GATE-DRILL.md" && test -f "$R/scripts/gate-drill.mjs" && test -f "$R/scripts/c9b-sweep.sh" && test -f "$F" && node "$R/scripts/gate-drill.mjs" "$R/.planning/phases/97-reachability" --json --timeout 900 > /dev/null && PLANS=$(ls "$R/.planning/phases/97-reachability"/97-*-PLAN.md | wc -l | tr -d ' ') && test "$PLANS" -eq 12 && GATES=$(command grep -c '^ *<automated>' "$R/.planning/phases/97-reachability"/97-*-PLAN.md | awk -F: '{s+=$2} END {print s}') && test -n "$GATES" && test "$GATES" -gt 0 && ROWS=$(command grep -c '^| 97-' "$F") && test "$ROWS" -eq "$GATES" && command grep -q 'C1 green' "$F" && command grep -q 'c9b-sweep' "$F"</automated>
  </verify>
  <acceptance_criteria>The drill record carries EXACTLY one row per automated-gate block in the plan set, with both counts DERIVED in the same run — the gate count summed across the twelve plan files and the row count from the record — so a gate added or a row forgotten makes this red rather than leaving a gate unexamined by accident, which is the precise failure GATE-STANDARD's pass procedure was written to end (C4, C10). The plan count is pinned at 12 so a plan appearing or vanishing is caught rather than silently changing the denominator. MAX REACHABLE: `PLANS` = 12 today, equal to its threshold; `GATES` = **29** today, summed across the twelve files, so `GATES > 0` is reached and `ROWS -eq GATES` is reached the moment the record carries 29 rows — both sides derived in the same run, neither frozen. Beware one authoring hazard proven during plan revision: `gate-drill.mjs` pairs an OPENING gate tag written in PROSE with the next real closing tag and extracts the text between as a gate, producing a spurious PARSE-FAIL. Do not write the literal tag names in this record's commentary; name gates by `plan.task` instead. `gate-drill.mjs` is invoked with its flags, since the script's own header records that the documented bare-directory form printed usage instead of running (C3). Both instruments' existence is asserted first (C5); no `2>/dev/null` anywhere. GATE DRILL for this gate itself: RED observed on the undone tree (the record does not exist and the row count is 0 against a non-zero gate count); GREEN constructed in a scratch copy by writing a stub with the right number of rows. NOTE this gate checks the record's COMPLETENESS, not the soundness of any drilled gate — soundness is the per-row verdict's business, and the gate deliberately claims no more than it can check.</acceptance_criteria>
  <done>Every gate in the phase has been examined once, by one author, in one pass.</done>
</task>

<task type="auto">
  <name>Task 2: coverage re-derived, populations closed, weakest point named</name>
  <files>.planning/phases/97-reachability/97-CLOSING-DERIVATION.md</files>
  <read_first>.planning/REQUIREMENTS.md lines 128-140 and 548-556 (the four definitions and their register rows, which sit at 551-554 — the previously cited 538-550 range does not contain them); .planning/ROADMAP.md §Phase 97 (the goal, the four success criteria and the requirements line); .planning/phases/97-reachability/97-POPULATIONS.md and 97-NAV04-DECISIONS.md (the populations and the decision record to restate as closing statements); .tickmarkr/overseer/ACCEPTANCE-P97-PLAN.md (the ten conditions, especially 10); every 97-*-SUMMARY.md (what actually shipped, and every bound each plan recorded)</read_first>
  <action>
    **§1 Requirement coverage, by command.** Derive the four ids from the register and map each to
    the success criterion it serves and to the plans that closed it. Run the derivation rather than
    quoting it, and state the ROADMAP-versus-register comparison result explicitly — the agreement is
    real this phase, and saying so is what distinguishes a checked match from an assumed one. Include
    NAV-04's filed sub-items by name (`frontend/src/services/auth.ts`,
    `frontend/src/components/layout/QuickNavigationMenu.tsx`); a silent drop of either is a REJECT.

    **§2 Decision coverage.** Run
    `node scripts/decision-coverage.mjs .planning/phases/97-reachability .planning/phases/97-reachability/97-CONTEXT.md`
    and paste the result; it must be exit 0 with `uncovered: []`. Then run the FALSIFICATION DRILL
    and put it on disk: in a scratch copy remove one `D-NN` citation from one plan's `truths`, re-run,
    and record that the scanner goes red naming that decision — then restore and record the green.
    A coverage gate that has never been seen red is a gate nobody has tested.

    **§3 Closing population statements.** Restate both populations with what falls OUTSIDE each, and
    add the phase-level one: which surfaces this phase's four criteria cover, and which named
    surfaces it deliberately did not — `COPY-*` including the global English toast (Phase 98), Arabic
    naturalness and pixel RTL (Phase 99 and the operator parks), RLS and residue (Phase 100), CI
    (Phase 101), and `/delegations`, `GATESTD-01`, `WRITER-ROUTE-01`, `INSERT-SYNC-01` (Phase 102).
    Name the intended-broken exclusions as untouched with their owners.

    **§4 Bounds and open edges.** Two bounds are known at plan time and belong in this list whatever
    else execution finds. First, `97-RESEARCH.md`'s `## Open Questions` heading carries no `RESOLVED`
    marker even though all three questions are substantively disposed of (Q1 → D-06 plus CONTEXT
    Claude's-Discretion, Q2 → the evidence-at-execution PARK policy carried into 97-09, Q3 → 97-08
    electing `ListPageShell.actions`); the plan-revision round DECLINED to edit it because that round's
    commit pathspec was the PLAN files only, and editing a research artifact under a plan-file pathspec
    would have left an uncommitted change in a tree carrying exogenous paths. Record it as an open
    documentation edge with the orchestrator as owner, not as a resolved item. Second, coarse-aggregate
    gates (`pnpm typecheck`, `pnpm lint`, the frontend build, whole test runs) now redirect their stdout
    to stderr so the drill instrument captures the attributing text; a full per-gate tee-to-file was
    DECLINED as a worse trade — it multiplies edits across a dozen long single-line gates and risks the
    exit-code-through-a-pipe defect the phase is trying to avoid. The consequence is that a very long
    compiler output may be TAIL-truncated in the drill record; where that happens, the row quotes the
    message from a hand re-run and says so.

    Then collect every bound the plans recorded — any NOT-CHECKED or PARKED
    decision row, any oracle marked `test.fixme` and why, any page whose data-present precondition
    could not be met, any `CANNOT CONSTRUCT` drill row, the operator's RTL sitting, and whether the
    non-admin negative test ran. These are the phase's honest edges and they belong in one list.

    **§4b CLOSED-VOCABULARY REGISTER.** List every closed term set this phase defines, with its
    terms and a verdict of `COMPLETE` or `TERM ADDED — <term> — <what it would otherwise have
    forced>`. A missing term in a closed vocabulary does not merely fail to describe a state; it
    FORCES A WRONG DESCRIPTION, and the wrong description becomes an instruction downstream. Eight
    sets were audited at plan time and TWO were incomplete:

    | vocabulary | owner | verdict |
    | --- | --- | --- |
    | NAV-04 route decisions | 97-09 | **TERM ADDED — `ALREADY-REACHABLE`** — four candidates already have live nav rows and would have been forced into `NAV ENTRY`, instructing 97-10 to add duplicates |
    | NAV-04 route decisions | 97-09 | **TERM ADDED — `PARKED-RE-ESCALATED`** — the action already used it for a refuted trigger while the permitted list did not contain it |
    | dead-module decisions (`DELETE` / `OWN`) | 97-09 → 97-11 | COMPLETE |
    | palette branch (`BRANCH-A` / `BRANCH-B`) | 97-09 → 97-10 | COMPLETE — closed by `RULING-P97-01`'s filing order, which permits exactly two answers |
    | instrument RENDERED classes (`LIVE` / demo-only / dead-module) | 97-03 | COMPLETE, but `LIVE`'s MEANING was restated: it is "in the rendered tree by file location", never "proven mounted", which makes an inbound count a FLOOR for absence and a CEILING for presence |
    | residual hand-classification verdicts | 97-03 | COMPLETE for the instrument's stated population |
    | comparison outcome states | 97-06 | **TERM ADDED — `LOADING`** — an in-flight query rendering the empty state says "there are none" when the truth is "not yet known" |
    | gate-drill verdicts | 97-12 | COMPLETE by GATE-STANDARD, with the partial-construction rule made explicit: half-constructed is `CANNOT CONSTRUCT`, never `SOUND` |
    | oracle outcome states | 97-01 / 97-02 / 97-10 | COMPLETE once `SKIPPED` was separated from `PASSED` — see §4c |

    **§4c THE SKIPPED-IS-NOT-PASSED RULE, stated once for the whole phase.** `test.skip()` exits 0,
    so a skipped test and a passing test are indistinguishable to any gate that reads only an exit
    code. Every Playwright-running gate in this phase therefore derives an EXPECTED count from the
    spec's own declaration lines and requires the JSON reporter's PASSED total to equal it. Record,
    per spec, the expected and actual counts. A `test.fixme` is excluded from the expected count and
    named in §4 as a bound; a SKIP is not — it is red.

    **§5 WEAKEST POINT.** Name the single weakest green in the phase and say why it is weak — a green
    resting on an unstated qualifier, an oracle that proves less than its name suggests, a
    construction that could not be built. ACCEPTANCE condition 10 makes a uniform pass a DOWNGRADE,
    so if you genuinely cannot find a weak point, report THAT as the finding along with what you
    looked for; do not manufacture a weakness and do not omit the section.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && F="$R/.planning/phases/97-reachability/97-CLOSING-DERIVATION.md" && test -f "$F" && test -f "$R/scripts/decision-coverage.mjs" && test -f "$R/.planning/phases/97-reachability/97-CONTEXT.md" && OUT=$(node "$R/scripts/decision-coverage.mjs" "$R/.planning/phases/97-reachability" "$R/.planning/phases/97-reachability/97-CONTEXT.md") && printf '%s' "$OUT" | command grep -q '"uncovered": \[\]' && for id in NAV-01 NAV-02 NAV-03 NAV-04; do command grep -q "$id" "$F" || exit 1; done && command grep -q 'WEAKEST POINT' "$F" && command grep -q 'services/auth.ts' "$F" && command grep -q 'QuickNavigationMenu' "$F" && command grep -q 'falsification' "$F" && command grep -q 'ALREADY-REACHABLE' "$F" && command grep -q 'SKIPPED' "$F" && command grep -q 'Phase 98' "$F" && command grep -q 'Phase 100' "$F" && command grep -q 'Phase 101' "$F" && command grep -q 'Phase 102' "$F"</automated>
  </verify>
  <acceptance_criteria>The decision-coverage scanner is RUN by the gate — not merely cited — and must exit 0 with a literally empty `uncovered` array, captured by command substitution inside the `&&` chain so a non-zero node exit stops the gate rather than being discarded by a `;`; that is ACCEPTANCE condition 2 turned into a gate obligation rather than a claim. All four requirement ids appear, checked one at a time so the gate names which is missing; both NAV-04 sub-items appear by filename, because a silent drop of either is a REJECT in the acceptance's own Branches (condition 1); the four out-of-phase owner phases appear, which is condition 8's naming duty; and the `WEAKEST POINT` heading is present, which is condition 10 — a record without it is a DOWNGRADE by construction, and pinning the heading is the only part of that a machine can check. GATE DRILL: RED observed on the undone tree (the file is absent, so the loop exits on NAV-01); GREEN constructed in a scratch copy from a stub carrying every pinned token. The scanner half is drilled in BOTH directions by §2's own falsification drill, whose red output is captured and pasted rather than summarised.</acceptance_criteria>
  <done>The phase can be graded from its own records without re-deriving anything by hand.</done>
</task>

<task type="auto">
  <name>Task 3: the register — one writer, what actually shipped</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md lines 128-140 (the four definitions and the NAV-04 sub-item note) and 548-556 (the register rows at 551-554, all Pending at phase start); .planning/phases/97-reachability/97-CLOSING-DERIVATION.md (as just written); the 97-10 and 97-04 SUMMARYs (any PALETTE-ADMIN-01 or PARALLEL-TRUTH-01 row handed to this plan)</read_first>
  <action>
    Update the four `NAV-01`..`NAV-04` register rows — at **551-554** as of 2026-08-17, but
    RE-DERIVE the line numbers with `command grep -n "^| NAV-0"` rather than seeking to a pinned
    offset — to reflect what shipped. A row that shipped with a
    bound says so in the row — the P96 precedent is `Complete (BOUNDED — <residue> → <phase>,
    <RULING>)`, and a bound named in the register is worth more than a clean word that a reader has
    to go and disprove. Cite the closing derivation and any ruling that governed the outcome.

    Add the handed-off rows if they exist: `PALETTE-ADMIN-01` if 97-10 elected branch B, and
    `PARALLEL-TRUTH-01` if 97-04 found residue it could not re-point. Each carries an owner. If
    neither was handed off, state that in the SUMMARY — an empty hand-off stated is evidence, an
    unstated one is silence.

    Do not edit the requirement DEFINITIONS at lines 132-135 unless execution proved one factually
    wrong; if it did, correct it and say so explicitly in the SUMMARY, because a definition edited
    quietly at close time rewrites what the phase was graded against.

    This file has exactly one writer this phase. If any other plan's SUMMARY reports having written
    it, that is a single-writer violation and belongs in the closing record's bounds list.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && F="$R/.planning/REQUIREMENTS.md" && C="$R/.planning/phases/97-reachability/97-CLOSING-DERIVATION.md" && test -f "$F" && test -f "$C" && for id in NAV-01 NAV-02 NAV-03 NAV-04; do command grep -q "^| $id | Phase 97 — Reachability | " "$F" || exit 1; done && test "$(command grep -c "^| NAV-0[1-4] | Phase 97 — Reachability | Pending" "$F" || true)" -eq 0 && W=$(command grep -rln '^  - \.planning/REQUIREMENTS\.md' "$R/.planning/phases/97-reachability"/97-*-PLAN.md | wc -l | tr -d ' ') && test "$W" -eq 1</automated>
  </verify>
  <acceptance_criteria>All four register rows exist in their expected column shape and NONE still reads `Pending` — the zero-count is anchored to the fully-qualified row prefix rather than to a bare word, so an unrelated `Pending` elsewhere in a 550-line register cannot satisfy or break it (C4, C8). Exactly ONE plan file in the phase directory names `.planning/REQUIREMENTS.md` **in its `files_modified` frontmatter block** — the mechanical form of the single-writer rule (ACCEPTANCE condition 6), checked here rather than trusted because the violation is invisible from inside any one plan (C9).

**The derivation is SCOPED TO THE FRONTMATTER LIST ITEM, and that scoping is what makes the gate satisfiable at all.** The earlier form was a whole-file `command grep -rln 'REQUIREMENTS.md'` against a threshold of 1, and it measured **3** on the plan set as authored — the two extra hits being the sentences in 97-09 and 97-10 that STATE the single-writer rule ("…which is this phase's ONLY writer of `.planning/REQUIREMENTS.md`"). Its maximum achievable value was 3 against a threshold of 1, so no amount of correct work could turn it green and the phase's CLOSING plan could not close: an unsatisfiable oracle contaminated by the plan's own prose (C4 max-reachable compounded by C8). After this revision round the whole-file count is **4**, because the repairs discuss the rule more, not less — which is the clearest possible demonstration that a prose-contaminated negative count decays as the document improves. The anchored form `^  - \.planning/REQUIREMENTS\.md` matches only a `files_modified` list item and measures **1** on the tree as it stands. MAX REACHABLE: 1, equal to the threshold, and it is 1 today because 97-12 already declares the file — this clause is an ANTI-VIOLATION invariant rather than a progress clause, and the red-today clauses in this gate are the four `Pending` rows.

**The `Pending` zero-count is inside the `&&` chain, not after a bare `;`.** The earlier form read `… && P=$(command grep -c "…" "$F"); test "$P" -eq 0 && …` — the `;` was there to absorb `grep -c`'s exit 1 on a zero count, which is the right problem, but it DISCARDED every assertion before it: `test -f "$F"`, `test -f "$C"` and the four-id loop all became decorative. It failed closed today only by accident, because `$P` would have been unset and `test "" -eq 0` errors out. 97-03's own criteria names this class explicitly. The repair is `test "$(command grep -c '…' "$F" || true)" -eq 0` — the count in argument position, where the substitution's exit status is not the chain's, and `|| true` absorbs grep's zero-match exit without breaking anything upstream. The closing derivation's existence is asserted first, since a register row citing a record that does not exist is worse than one that cites nothing (C5). GATE DRILL: RED observed on the undone tree — all four rows read `Pending` today, so the `-eq 0` assertion fails on the real subject rather than vacuously; GREEN constructed in a scratch copy by editing the four rows. `git status --porcelain` identical before and after in the plans' own tree.</acceptance_criteria>
<done>The register says what happened, bounds included, written once.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                               |
| ------------------------------ | ----------------------------------------------------------------------------------------- |
| plan SUMMARYs → closing record | the closing record consolidates claims it did not itself observe; each is attributed      |
| closing record → register      | the register is the durable, cross-phase surface; a bound omitted here outlives the phase |

## STRIDE Threat Register

| Threat ID | Category    | Component                       | Disposition | Mitigation Plan                                                                                                        |
| --------- | ----------- | ------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------- |
| T-97-37   | Repudiation | unattributed coverage claims    | mitigate    | coverage and decision scanners are RUN by the gate, not cited; the falsification drill lands on disk with both colours |
| T-97-38   | Repudiation | a bound lost at close time      | mitigate    | §4 collects every recorded bound and the register rows carry them in the P96 `Complete (BOUNDED — …)` form             |
| T-97-39   | Tampering   | a definition edited at close    | mitigate    | definitions are edited only on proven fact and the change is stated, so the grading target cannot move quietly         |
| T-97-40   | Repudiation | a uniform pass read as strength | mitigate    | the WEAKEST POINT section is mandatory and machine-pinned; finding nothing weak is reported as the finding             |
| T-97-SC   | Tampering   | package installs                | accept      | zero installs this phase                                                                                               |

</threat_model>

<verification>
- All three gates green.
- `node scripts/decision-coverage.mjs` exit 0 with `uncovered: []`, and its falsification drill on
  disk showing both colours.
- `node scripts/gate-drill.mjs` run with flags and its output consolidated one row per gate.
- `bash scripts/c9b-sweep.sh` run and every consumer triaged.
- POPULATION DEFINITION: the phase's own records, gates and register rows. OUTSIDE IT: the product
  code (owned by 97-04 through 97-11), the independent verification pass (reserved path
  `97-VERIFICATION-INDEPENDENT.md`, a separate seat), and every out-of-phase surface named in §3.
</verification>

<success_criteria>
The phase can be accepted or rejected from its own artifacts: coverage derived, every gate drilled
and verdicted, every bound listed, and the weakest green named rather than averaged away.
</success_criteria>

<output>
Create `.planning/phases/97-reachability/97-12-SUMMARY.md` when done. It MUST record: the coverage
derivation output verbatim; the decision-coverage result and both colours of its falsification
drill; the gate-count-to-row-count reconciliation; the C9a sweep counts and the c9b triage; whether
any `PALETTE-ADMIN-01` or `PARALLEL-TRUTH-01` row was handed off and written, or that neither was;
any single-writer violation found; and the weakest point in one sentence, repeated verbatim from the
closing record so the two cannot drift.
</output>
