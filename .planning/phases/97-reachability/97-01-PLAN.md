---
phase: 97-reachability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/97-elected-officials-reachable.spec.ts # NEW — NAV-01 click-through oracle
  - tests/e2e/97-settings-nav.spec.ts # NEW — NAV-02 click-through oracle, VIEWPORT MATRIX
autonomous: true
requirements: [NAV-01, NAV-02]
success_criterion: '1 (Elected Officials on all four surfaces) and 2 (/settings/* renders navigation) — this plan writes the OBSERVERS only; both specs are expected RED until 97-05/97-06/97-07 land. Producer-before-consumer.'
truths:
  - 'Decisions covered — D-10: both specs are CLICK-THROUGH oracles (the real rendered affordance is clicked; page.goto is used only to reach a starting surface, never as the reachability claim) and each test title NAMES the role and the viewport it proves, per RULING-P97-03 §3; D-12: no visual baseline is committed, neither spec waits on E2ECRED-01 (inline sign-in + --no-deps), and both run single-instance against :5173 only.'
must_haves:
  truths:
    - 'The NAV-01 spec fails today because no Elected Officials row exists in the live sidebar'
    - 'The NAV-01 spec asserts the hub EO card count equals an INDEPENDENTLY DERIVED elected-officials total — never merely that a card rendered (RULING-P97-04 §2)'
    - 'The NAV-02 spec covers all six /settings children at BOTH viewports (desktop aside and mobile drawer), 12 observations, per RULING-P97-03 §2'
    - 'Neither spec references components/modern-nav/* for any reachability claim'
  artifacts:
    - path: 'tests/e2e/97-elected-officials-reachable.spec.ts'
      provides: 'NAV-01 click-through + real-count oracle'
      contains: '@covers NAV-01'
    - path: 'tests/e2e/97-settings-nav.spec.ts'
      provides: 'NAV-02 viewport-matrix oracle'
      contains: '@covers NAV-02'
  key_links:
    - from: 'tests/e2e/97-elected-officials-reachable.spec.ts'
      to: '/api/elected-officials'
      via: 'independent total derivation compared against the rendered chip'
      pattern: 'api/elected-officials'
    - from: 'tests/e2e/97-settings-nav.spec.ts'
      to: 'settings-nav-card / settings-back-to-app'
      via: 'DOM presence of the shipped settings nav column on every child'
      pattern: 'settings-back-to-app'
---

<objective>
Write the two Wave-0 click-through oracles that criteria 1 and 2 are judged by. They land BEFORE
their subjects (producer-before-consumer, D-10) and are expected RED until 97-05/97-06 build the
work. A spec written after the fix is a spec shaped by the fix.

Two rules from the rulings bind the content, not just the existence, of these files:

- **RULING-P97-04 §2 (C10):** "the card appears" is satisfied by the fabricated `0` this phase
  exists to remove. The NAV-01 spec asserts the EO count is REAL — equal to a total this spec
  derives itself from `/api/elected-officials` — and that the unavailable fields render the
  em-dash treatment, never `0`.
- **RULING-P97-03 §2 + §3:** NAV-02's population is the VIEWPORT MATRIX. `AppShell` suppresses
  BOTH Sidebar mounts, so a 1400-only spec would go green while mobile settings children have no
  nav at all — the confident-lie class. Every test title names its role and its viewport.

Purpose: the observers exist and are red for their subject's absence, not for a tooling reason.
Output: two spec files. No product code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-reachability/97-UI-SPEC.md (§Verification Notes — the binding oracle contract; §NAV-01(b) count honesty; §NAV-02 active-state contract)
@.planning/phases/97-reachability/97-PATTERNS.md (§Oracles — every structural element, quoted from 95-monitoring-mounts.spec.ts)
@.planning/phases/97-reachability/97-VALIDATION.md (Wave 0 list, run commands)
@.tickmarkr/overseer/RULING-P97-03-OMISSION-FINDINGS.md
@.tickmarkr/overseer/RULING-P97-04-PARALLEL-TRUTH-CLASS.md
@tests/e2e/95-monitoring-mounts.spec.ts (the house pattern — read in full before writing either file)

<interfaces>
Verified on disk 2026-08-17, HEAD `9c32c4db1`:

- Playwright projects (`playwright.config.ts`): `setup`, `chromium-en`, `chromium-ar-smoke`,
  `chromium-mobile`. `setup` throws without six `E2E_*` keys `.env.test` does not carry
  (E2ECRED-01) — every run in this phase passes `--no-deps`. `--list` does NOT exempt a
  dependency project either (GATE-STANDARD C6).
- Inline auth helper: copy `95-monitoring-mounts.spec.ts:29-66` verbatim
  (`TEST_USER_EMAIL`/`TEST_USER_PASSWORD` via `./support/pages/LoginPage`, throws loudly when
  absent, never echoes a value).
- Leak oracle: `INTERNAL_STRING` at `95-monitoring-mounts.spec.ts:54-55`. Its narrowing (the bare
  `supabase` arm was removed) is load-bearing — re-check the surface's own product copy before
  widening it back.
- LIVE sidebar = `components/layout/Sidebar.tsx` fed by `components/layout/navigation-config.ts`,
  mounted at `AppShell.tsx:196` (desktop aside, `hidden lg:block`) and `:252` (mobile drawer).
  `components/modern-nav/*` renders ONLY at `/modern-nav-standalone` — an assertion against it
  proves nothing.
- Sidebar row DOM (`Sidebar.tsx:128-157`): a TanStack `<Link>` carrying
  `aria-current={isActive ? 'page' : undefined}` and the translated label text.
- Settings nav DOM (`SettingsNavigation.tsx:89-122`): `nav.card.settings-nav-card`, the return
  affordance `data-testid="settings-back-to-app"`, and one `data-testid="settings-nav-{id}"`
  button per section with `aria-current="page"` on the active one.
- The six `/settings` children (derived from `routeTree.gen.ts` this session, count = 6):
  `/settings/webhooks`, `/settings/integrations`, `/settings/notifications`,
  `/settings/email-digest`, `/settings/calendar-sync`, `/settings/calendar/callback`.
- EO count source: `GET /api/elected-officials?limit=1` (Express, `baseUrl: 'express'`) returns
  `ElectedOfficialListResponse` whose `.total` is the real EO total
  (`domains/elected-officials/types/elected-official.types.ts:116-118`).
- The unavailable treatment already shipped in-tree: `data-testid="dossier-count-unavailable"`
  + `aria-label` from `common:errors.countUnavailable` (`i18n/{en,ar}/common.json:250`), rendering
  `—` (`DossierListPage.tsx:565-574`).
- Hub card testid `dossier-type-card-{type}` does NOT exist yet — 97-05 adds it. This spec
  targets it by name; that is the producer-before-consumer contract (C9: if 97-05 renames it,
  it repoints this file in the same edit).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/e2e/97-elected-officials-reachable.spec.ts — NAV-01 click-through + REAL-count oracle</name>
  <files>tests/e2e/97-elected-officials-reachable.spec.ts</files>
  <read_first>tests/e2e/95-monitoring-mounts.spec.ts in full (header contract, inline auth :29-66, INTERNAL_STRING :54-55, listener-before-navigation :88-110, settle-not-hang :112-115); 97-UI-SPEC.md §NAV-01 and §Verification Notes; frontend/src/components/layout/Sidebar.tsx:128-157 (the row DOM this spec clicks); frontend/src/pages/dossiers/DossierListPage.tsx:545-601 (the unavailable treatment and the card grid this spec reads)</read_first>
  <action>
    Write the spec with the `// @covers NAV-01` header block in the 95 shape: what this kills,
    the AUTHENTICATION paragraph (inline, `--no-deps`, E2ECRED-01 named), the NETWORK paragraph
    (natural — this is a reachability oracle against real dev-stack state). Copy the inline
    `signInInline` helper and `INTERNAL_STRING` verbatim.

    Five tests, each titled with its ROLE and VIEWPORT per RULING-P97-03 §3 — the title literally
    contains the words `ordinary authenticated user` and `desktop 1400` (or `mobile 390`), so a
    reader of a green run cannot mistake a scoped result for the general claim:

    (1) `sidebar row — ordinary authenticated user, desktop 1400`: set viewport 1400x900, sign in,
    land on `/`, then find the LIVE sidebar link by its accessible name (the `common:navigation.electedOfficials`
    English string) inside the desktop `aside.appshell-aside`, click it, and assert the URL becomes
    `/dossiers/elected-officials` AND the destination's own list content settles. Scope the locator
    to the aside so a match inside the command palette or the standalone demo route cannot satisfy it.

    (2) `hub type card — ordinary authenticated user, desktop 1400`: go to `/dossiers`; derive the
    EO total INDEPENDENTLY inside the test by issuing `page.request.get('/api/elected-officials?limit=1')`
    with the session's storage state and reading `.total`; then locate `[data-testid="dossier-type-card-elected_official"]`
    and assert its count chip's text equals `String(total)`. RULING-P97-04 §2: this equality IS the
    criterion — do NOT weaken it to a visibility check, and do NOT accept `0` unless the derived
    total is itself `0`. In the same test assert the fields that have no honest source render the
    em-dash: within that card, `[data-testid="dossier-count-unavailable"]` has a count of at least 1
    and the active/inactive figures are not the string `0`. If the independent request fails
    (non-2xx), the test asserts the chip ALSO renders the unavailable treatment rather than a number
    — a fabricated number when the source is down is the same defect in the other direction.

    (3) `hub type card click destination — ordinary authenticated user, desktop 1400`: clicking the
    EO card navigates to `/dossiers/elected-officials` (UI-SPEC's default; the in-place filter
    behaviour is forbidden for this pseudo-type because `dossiers.type` has no such value).

    (4) `compare selector — ordinary authenticated user, desktop 1400`: go to `/compare`, open the
    entity-type Select, choose the Elected Officials option by its accessible name, and assert the
    selector's own rendered result region settles to either real content or the shipped
    empty/error state — never a blank pane.

    (5) `create hub + create submit — ordinary authenticated user, desktop 1400`: `/dossiers/create`
    renders an Elected Officials card that reaches `/dossiers/elected-officials/create`; then drive
    the wizard's required fields and submit, asserting the app lands on an elected-officials detail
    URL. This is Assumption A1's behavioural leg. If the submit leg cannot be driven headlessly in
    a stable way, mark that ONE test `test.fixme` with a comment naming exactly what blocked it and
    that its state must be recorded in the NAV-01 decision line — do NOT delete it and do NOT let a
    skipped test read as a pass.

    Every test also asserts `await page.locator('body').innerText()` does not match `INTERNAL_STRING`.
    Nothing in this file may reference `components/modern-nav` or `/modern-nav-standalone`.
  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && test -f "$R/tests/e2e/95-monitoring-mounts.spec.ts" && test -f "$R/tests/e2e/97-elected-officials-reachable.spec.ts" && command grep -q '@covers NAV-01' "$R/tests/e2e/97-elected-officials-reachable.spec.ts" && command grep -q 'api/elected-officials' "$R/tests/e2e/97-elected-officials-reachable.spec.ts" && command grep -q 'dossier-count-unavailable' "$R/tests/e2e/97-elected-officials-reachable.spec.ts" && test "$(command grep -c 'modern-nav' "$R/tests/e2e/97-elected-officials-reachable.spec.ts")" -eq 0 && D=$(command grep -c "ordinary authenticated user" "$R/tests/e2e/97-elected-officials-reachable.spec.ts") && T=$(command grep -c "^\s*test(" "$R/tests/e2e/97-elected-officials-reachable.spec.ts") && test "$D" -eq "$T" && test "$T" -ge 5</automated>
  </verify>
  <acceptance_criteria>The spec exists; it carries the `@covers NAV-01` header; it derives the EO total from `/api/elected-officials` itself (the C10 obligation from RULING-P97-04 §2 — a criterion that only asserts a card appeared is a vacuous guard); it names the em-dash testid; it contains ZERO `modern-nav` references; and the count of role-and-viewport-named titles EQUALS the count of `test(` declarations (so a later-added unnamed test fails the gate — the C10 obligation on RULING-P97-03 §3), with at least 5 tests. The 95 spec's existence is asserted in the same chain as the instrument control proving the search root and the grep are both live (C5). GATE DRILL: recorded in THIS PLAN'S SUMMARY per D-11 (97-12 is the single writer of the consolidated `97-GATE-DRILL.md` and reads this row from the SUMMARY) — RED direction is observed against the undone tree (the file does not exist), GREEN direction is constructed in a scratch copy by writing the file. The Playwright RUN is NOT part of this gate: the spec is expected red until 97-05 lands, and grading a producer on its consumer's absence is the C2 error.</acceptance_criteria>
  <done>NAV-01 has a written observer whose green cannot be bought with a fabricated zero.</done>
</task>

<task type="auto">
  <name>Task 2: tests/e2e/97-settings-nav.spec.ts — NAV-02 across the VIEWPORT MATRIX</name>
  <files>tests/e2e/97-settings-nav.spec.ts</files>
  <read_first>tests/e2e/95-monitoring-mounts.spec.ts in full; 97-UI-SPEC.md §NAV-02 (the active-state contract: exactly ONE aria-current on the index, ZERO on any child); .tickmarkr/overseer/RULING-P97-03-OMISSION-FINDINGS.md §2 and §3; frontend/src/components/settings/SettingsNavigation.tsx:89-122 (the DOM contract); frontend/src/components/layout/AppShell.tsx:120-125 and :183-198 and :232-259 (both suppressed Sidebar mounts — the reason this is a matrix)</read_first>
  <action>
    Header block as in Task 1 but `// @covers NAV-02`, and the WHAT THIS KILLS paragraph states
    the RULING-P97-03 §2 finding in its own words: `AppShell` gates BOTH the desktop aside and the
    mobile drawer off the same flag, so a desktop-only oracle would report NAV-02 closed while
    mobile settings children have no navigation at all.

    Hardcode the child list as a literal array of the SIX paths named in the interfaces block
    (Playwright paths are filters and derived lists silently shrink — hardcode, never `readdir`).
    Add a first assertion that the array length is 6 so a hand-edit that drops one fails loudly.

    Run the matrix as two `test.describe` blocks that differ only in viewport, so the titles read
    `ordinary authenticated user, desktop 1400` and `ordinary authenticated user, mobile 390`.
    (Settings is NOT admin-gated; naming the role is still required so no reader infers an
    admin-scoped result.) Per describe, per child path — 12 observations total:

    - the settings nav column is present: `nav.settings-nav-card` visible AND
      `[data-testid="settings-back-to-app"]` visible (the return path is a large part of what
      "renders navigation" means for `/settings/calendar/callback`);
    - the global sidebar is absent: zero `aside.appshell-aside` in the DOM, and — at mobile — the
      drawer's Sidebar is likewise not mounted;
    - the nav contains ZERO `[aria-current="page"]` (UI-SPEC: none of the nine in-page sections is
      what is rendered, and three of them share a name with a different child route);
    - the child's own content region rendered (assert the page is not blank: the content column has
      non-empty text) and the body does not match `INTERNAL_STRING`.

    Two further tests, one per viewport:
    - `/settings` index: exactly ONE `[aria-current="page"]` in the nav, and `SettingsPage` content
      renders.
    - from a child (`/settings/webhooks`), clicking a DIFFERENT section row lands on `/settings`
      with THAT section rendered — not the default `profile`. Assert the landed section by its
      `data-testid="settings-nav-{id}"` carrying `aria-current="page"` after the navigation.

    Do not use `page.goto` as the reachability claim for the section click — the click is the claim;
    `goto` only establishes the starting child.
  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && test -f "$R/tests/e2e/97-settings-nav.spec.ts" && test -f "$R/frontend/src/routeTree.gen.ts" && N=$(sed -n '/interface FileRoutesByFullPath {/,/^}/p' "$R/frontend/src/routeTree.gen.ts" | command grep -c "'/settings/") && test -n "$N" && test "$N" -gt 0 && C=$(command grep -c "'/settings/" "$R/tests/e2e/97-settings-nav.spec.ts") && test "$C" -ge "$N" && command grep -q '@covers NAV-02' "$R/tests/e2e/97-settings-nav.spec.ts" && command grep -q 'settings-back-to-app' "$R/tests/e2e/97-settings-nav.spec.ts" && command grep -q 'aria-current' "$R/tests/e2e/97-settings-nav.spec.ts" && command grep -q 'desktop 1400' "$R/tests/e2e/97-settings-nav.spec.ts" && command grep -q 'mobile 390' "$R/tests/e2e/97-settings-nav.spec.ts"</automated>
  </verify>
  <acceptance_criteria>The spec exists and covers at least as many distinct `/settings/` child paths as the route tree declares — the threshold N is DERIVED from `routeTree.gen.ts` in the same chain, never the literal 6, so a seventh settings child added later makes this gate red instead of silently uncovered (C4). Both viewport labels are present, so a desktop-only regression of this file fails the gate (C10 against RULING-P97-03 §2, which makes silence on mobile a REJECT). The `routeTree.gen.ts` search root is proven to exist and `N` proven non-empty before it is compared, with no `2>/dev/null` anywhere (C5). GATE DRILL: recorded in THIS PLAN'S SUMMARY (97-12 consolidates it into `97-GATE-DRILL.md`, which it alone writes) — RED observed on the undone tree (file absent); GREEN constructed in a scratch copy. The Playwright RUN is deliberately excluded: this spec is expected red until 97-07 lands.</acceptance_criteria>
  <done>NAV-02 has an observer that cannot go green on desktop alone.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                       |
| ------------------------------ | --------------------------------------------------------------------------------- |
| test env → repo                | `.env.test` credentials are read, never echoed, never committed                   |
| rendered DOM → assertion       | product surfaces may leak internals; every test asserts the leak regex            |

## STRIDE Threat Register

| Threat ID | Category               | Component                    | Disposition | Mitigation Plan                                                                                              |
| --------- | ---------------------- | ---------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------ |
| T-97-01   | Information Disclosure | spec source                  | mitigate    | credentials read from `process.env` only; the helper throws on absence and never prints a value (95 pattern) |
| T-97-02   | Information Disclosure | surfaces under test          | mitigate    | `INTERNAL_STRING` assertion in every test; regex reused unwidened from `95-monitoring-mounts.spec.ts:54-55`  |
| T-97-03   | Spoofing               | nav visibility as authz      | accept      | documentation duty only — nav gating is client-side and proves nothing about access control (RESEARCH V4)    |
| T-97-SC   | Tampering              | npm/pip/cargo installs       | accept      | zero installs this phase; RESEARCH §Package Legitimacy Audit records nothing to check                        |

</threat_model>

<verification>
- Both gates green.
- Both specs are expected RED when run — that is the producer-before-consumer state, and the
  plan's SUMMARY records the red WITH its reason (subject absent), never as a defect.
- POPULATION DEFINITION: this plan covers the NAV-01 and NAV-02 observers only. OUTSIDE IT:
  NAV-03's two specs (97-02), NAV-04's spec (97-10), the Arabic/RTL pixel sitting (operator park,
  D-12 — no visual baselines committed).
</verification>

<success_criteria>
Criteria 1 and 2 each have a written, role-and-viewport-scoped, click-through observer on disk
before their subjects exist, and neither can be satisfied by a data-structure claim or a
fabricated zero.
</success_criteria>

<output>
Create `.planning/phases/97-reachability/97-01-SUMMARY.md` when done. It MUST record: the exact
test titles written (so the role/viewport naming is auditable without opening the specs), the
observed red output of each spec with its attribution, and — if test (5) was marked `test.fixme` —
the exact blocker text, which 97-09's decision table consumes.
</output>
</content>
</invoke>
