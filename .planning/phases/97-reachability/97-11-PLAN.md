---
phase: 97-reachability
plan: 11
type: execute
wave: 4
depends_on: [97-09]
files_modified:
  - frontend/src/routes/_protected/admin/approvals.tsx # CONDITIONAL DELETE — only if the table's row says DELETE
  - frontend/src/routes/_protected/admin/preview-layouts.tsx # CONDITIONAL DELETE — only if the table's row says DELETE
  - frontend/src/services/auth.ts # DELETE-or-OWN per the table's row
  - frontend/src/components/layout/QuickNavigationMenu.tsx # DELETE-or-OWN per the table's row
  - frontend/src/routeTree.gen.ts # REGENERATED, never hand-edited, committed in the same commit
autonomous: true
requirements: [NAV-04]
success_criterion: '4 (routes with no inbound link are resolved) — this plan executes the DELETE half of the decision table.'
truths:
  - 'Decisions covered — D-07: frontend/src/services/auth.ts is deleted only after its zero-importer derivation is RE-RUN at execution and instrument-tested against a known-imported control, and the same discipline governs QuickNavigationMenu.tsx; D-08: this plan is why route-count gates had to survive legitimate deletions — the regenerated routeTree.gen.ts moves the count by design, so this plan states its expected delta and re-derives rather than freezing any number.'
must_haves:
  truths:
    - 'No file is deleted without a row in the decision table authorising it'
    - 'Every deleted route path is absent from the regenerated routeTree.gen.ts'
    - 'Every zero-importer derivation was re-run at execution beside a known-imported control'
    - 'The regenerated route tree is committed in the same commit as the deletions'
    - 'The route count moved by a stated delta, not by an unexplained amount'
  artifacts:
    - path: 'frontend/src/routeTree.gen.ts'
      provides: 'the regenerated tree with the deleted paths absent'
      contains: 'FileRoutesByFullPath'
  key_links:
    - from: '.planning/phases/97-reachability/97-NAV04-DECISIONS.md'
      to: 'the deleted files'
      via: 'each deletion authorised by its own DELETE row'
      pattern: 'DELETE'
---

<objective>
Execute the DELETE half of `97-NAV04-DECISIONS.md`. Deleting is the smallest possible change and the
least reversible one, which is why it is split from deciding: this plan performs no judgement. If a
row does not say DELETE, nothing is deleted.

**Two rules make this plan safe, and both are law rather than caution.**

- **No SILENT route removal.** Every deletion has a recorded decision row, written before this plan
  ran, by a different plan. A deletion this plan cannot point at a row for is a defect.
- **Re-derive the zeros here, not from a report.** `RULING-P92-06` established that
  `services/auth.ts` had zero importers in Phase 92; `RULING-P97-03` §1 says the same for
  `QuickNavigationMenu.tsx` and explicitly refuses to let today's number be quoted. The tree moves.
  Both derivations are re-run at execution, unpiped so the exit code belongs to grep, and beside a
  known-imported positive control in the SAME run — because `grep` here is a ugrep wrapper honoring
  `.gitignore` and a false zero would close a deletion wrongly.

**This is the plan that makes D-08 concrete.** The route count moves by design. Every gate that
touches it derives at run time or states its delta; nothing anywhere freezes it.

Purpose: the tree carries nothing unowned, and nothing owned is removed by accident.
Output: authorised deletions and one regenerated tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-reachability/97-NAV04-DECISIONS.md (97-09's table — the ONLY authority for what may be deleted; this plan reads it and never writes it)
@.planning/phases/97-reachability/97-POPULATIONS.md (the route-population instrument and its blind spots)
@.tickmarkr/overseer/RULING-P97-01-ADMIN-DISPOSAL.md (the two conditional triggers and the re-escalation branch)
@.tickmarkr/overseer/RULING-P97-03-OMISSION-FINDINGS.md (§1 — the QuickNavigationMenu treatment and the what-did-its-list-know obligation)
@.planning/phases/95-routes-that-don-t-render/95-DEAD-04-DECISION.md (the precedent for committing a regenerated tree with its cause)

<interfaces>
Verified on disk 2026-08-17.

- `routeTree.gen.ts` is GENERATED and ESLint-ignored. Never hand-edit it: let the dev server or a
  build regenerate it, and commit the regenerated file in the SAME commit as the route deletion
  (the P95 precedent).
- ROUTE population instrument (re-run at execution, both before and after):
  `sed -n '/interface FileRoutesByFullPath {/,/^}/p' frontend/src/routeTree.gen.ts | command grep -c "':"`
  → **203 at HEAD today**. P95 pinned 202 and `/calendar` moved it, which is the drift that proves
  the point. Capture BEFORE and AFTER and state the delta; the delta is the claim, not the number.
- Zero-importer derivation shape (unpiped, with its control in the same run):
  `command grep -rn "services/auth'" /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0/frontend/src --include='*.ts' --include='*.tsx'`
  and, in the same run, the control `command grep -rn "store/authStore'" …` whose output must be
  NON-EMPTY. Piping either into `head` or `wc` makes `$?` the pipe tail's exit and destroys the
  semantics. Watchers fail closed: assert the condition AND the exit code.
- `services/auth.ts` is 18,404 bytes and carries a second `useAuthStore` zustand store persisted
  under `name: 'auth-storage'` (`:624`) — colliding with the live `store/authStore.ts:253` key —
  plus a module-level `supabase.auth.onAuthStateChange` (`:635`). Deleting it does NOT touch the
  localStorage entry: the live store owns that key and the dead module never runs.
- `QuickNavigationMenu.tsx` is 15,974 bytes and carries a second route list beside
  `navigation-config.ts`. The control shape for its sweep is a known-imported sibling from the same
  directory (the `Sidebar` import) — **re-run it; do not quote a previously observed number.**
- Runtime-state check already done (RESEARCH §Runtime State Inventory): no DB rows key on route
  paths or on either module; nginx confs carry no `/monitoring` or `/admin/*` locations; the only
  build artifact affected is `routeTree.gen.ts`. `.size-limit.json` Bundle Size Check is a required
  CI gate and deletions only shrink.
- Environment: `command grep` only, absolute paths, no `timeout` binary, and zsh does not word-split
  unquoted expansions — use `read -r`, arrays, or a bash shebang for any loop over paths.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: the authorised route deletions, and the regenerated tree</name>
  <files>frontend/src/routes/_protected/admin/approvals.tsx, frontend/src/routes/_protected/admin/preview-layouts.tsx, frontend/src/routeTree.gen.ts</files>
  <read_first>.planning/phases/97-reachability/97-NAV04-DECISIONS.md in full (which rows say DELETE, and the pasted comparison that fired each trigger); frontend/src/routes/_protected/admin/approvals.tsx and frontend/src/routes/_protected/approvals.tsx (the duplication comparison's two subjects — confirm the row's verdict still holds against the tree as it stands); frontend/src/routes/_protected/admin/preview-layouts.tsx (the ownership-claim subject); .planning/phases/95-routes-that-don-t-render/95-DEAD-04-DECISION.md (the same-commit regeneration precedent)</read_first>
  <action>
    Read the table first. Delete ONLY the route files whose rows say DELETE. A row that says PARKED,
    NOT-CHECKED, OWNED-ELSEWHERE, or re-escalated means the file stays — and if `/admin/approvals`'s
    row records a REFUTED duplication trigger, that route is untouched and this plan does not
    improvise a nav entry for it either; that belongs to the overseer, per the ruling.

    Before deleting each file, confirm the row's evidence still holds against the tree as it stands
    now. The table was written a wave ago and HEAD has moved since; a stale authorisation is not an
    authorisation. If it no longer holds, stop and record the divergence.

    After deleting, let the dev server or a build REGENERATE `routeTree.gen.ts` — never hand-edit it
    — and commit the regenerated file in the same commit as the deletions.

    Capture the route count BEFORE and AFTER with the population instrument and state the delta in
    the SUMMARY, naming which deletions account for it. A route count is a moving number in this
    phase by design, so the honest artifact is the delta and its cause, never the number alone.

    Also name, in the SUMMARY, which closed gates elsewhere in the phase have a route-count or
    route-path subject that this deletion changes — the post-close mutation law. Any gate that froze
    a count would be red now, which is precisely why none was written that way.

    Touch nothing owned elsewhere: `/delegations` belongs to Phase 102 and the legal-holds region to
    Phase 100; the `/engagements` double-mount is intentional; `scenario-sandbox` and
    `responsive-demo` were kept deliberately. Removing or repairing any of them is a REJECT.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && T="$R/.planning/phases/97-reachability/97-NAV04-DECISIONS.md" && G="$R/frontend/src/routeTree.gen.ts" && test -f "$T" && test -f "$G" && N=$(sed -n '/interface FileRoutesByFullPath {/,/^}/p' "$G" | command grep -c "':") && test -n "$N" && test "$N" -gt 100 && for p in /admin/approvals /admin/preview-layouts; do test "$(command grep -c "^DECISION $p: " "$T")" -eq 1 || exit 1; if command grep -q "^DECISION $p: DELETE$" "$T"; then if sed -n '/interface FileRoutesByFullPath {/,/^}/p' "$G" | command grep -q "'$p'"; then exit 1; fi; else if ! sed -n '/interface FileRoutesByFullPath {/,/^}/p' "$G" | command grep -q "'$p'"; then exit 1; fi; fi; done && command grep -q "'/delegations'" "$G" && cd "$R" && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>The gate is TABLE-DRIVEN in BOTH directions, which is what makes it non-vacuous: for each conditional route it reads the row and then asserts the state the row implies — a DELETE row requires the path to be ABSENT from the regenerated tree, and any other verdict requires it to be PRESENT. A gate that only checked absence would pass by accident on a refuted trigger; one that only checked presence would pass by accident on a completed deletion.

**The row is read as an ANCHORED DECISION TOKEN, never as prose — this is the repair on the phase's ONE irreversible operation.** The earlier form matched `"$p.*DELETE"` against `97-NAV04-DECISIONS.md`, a prose-and-table document, which matches ANY line where the subject and the word DELETE merely co-occur — INCLUDING a row that says the opposite. Reproduced during plan revision against a realistic stub: the rows `| /admin/approvals | PARKED-RE-ESCALATED | the CONDITIONAL DELETE trigger was REFUTED … |` and `| frontend/src/services/auth.ts | OWN | kept per DELETE-or-OWN review … |` BOTH matched the old pattern (count 1 each) and both fail the anchored form (count 0 each). This was not hypothetical: 97-09's own text uses the literals "CONDITIONAL DELETE" and "DELETE-or-OWN", and so do `REQUIREMENTS.md` and CONTEXT D-07, so the contaminating strings were near-certain to be in the very document the gate reads. On a REFUTED trigger the contaminated gate took its DELETE arm and demanded the route be GONE — inverting ACCEPTANCE condition 8's "No SILENT route removal" protection at the exact point it exists to protect, and pressuring an unauthorised, irreversible deletion. `^DECISION <subject>: DELETE$`, anchored at both ends, cannot be satisfied by any sentence. The token's EXISTENCE is asserted first (`-eq 1`), so a table with no decision for a conditional route is red rather than silently taking the else-arm. The route count is DERIVED at run time and only sanity-bounded (`> 100`) rather than pinned, because this phase moves it by design and a frozen literal is the moving-number class (D-08, ACCEPTANCE condition 3); the bound exists so a truncated or unparseable tree cannot pass as a legitimate deletion. MAX REACHABLE: `N > 100` is 203 today and stays above 100 after at most two route deletions; each conditional route's `DECISION` token count is 0 today and reaches exactly 1 when the table is written; both `-eq 1` token assertions and the presence/absence arms are reachable under EITHER verdict, so no disposal is pre-frozen by the gate. `/delegations` is asserted STILL PRESENT in the regenerated tree as the intended-broken exclusion control — the deletion sweep must not have touched Phase 102's surface (condition 8) — and it doubles as the instrument test proving the sed range and grep are matching real paths, so the absence assertions above it are real absences. `pnpm typecheck` green, which also proves the regenerated tree is coherent. GATE DRILL: RED observed by constructing each WRONG state in a scratch copy — delete a route whose row is not DELETE, then separately leave a DELETE-row route in place — and observing the corresponding arm fail, attributed by which loop iteration exited rather than by the aggregate status. GREEN observed on the constructed correct state. `git status --porcelain` identical before and after in the plans' own tree.</acceptance_criteria>
<done>Exactly the authorised routes are gone, the tree agrees, and the count moved by a stated amount.</done>
</task>

<task type="auto">
  <name>Task 2: the two parallel-truth modules, deleted or owned, with the zeros re-run</name>
  <files>frontend/src/services/auth.ts, frontend/src/components/layout/QuickNavigationMenu.tsx</files>
  <read_first>.planning/phases/97-reachability/97-NAV04-DECISIONS.md (both sub-item rows and the comparison the QuickNavigationMenu row recorded); frontend/src/services/auth.ts (the second store around :624 and the module-level onAuthStateChange around :635 — confirm they are still what the row says); frontend/src/components/layout/QuickNavigationMenu.tsx in full (its route list, so its recorded comparison can be confirmed before the file disappears); frontend/src/store/authStore.ts (the live store whose 'auth-storage' key the dead module collides with); frontend/src/components/layout/navigation-config.ts (the live list the comparison is against)</read_first>
  <action>
    RE-RUN both zero-importer derivations at execution, each unpiped so the exit code belongs to
    grep, each with a known-imported positive control in the SAME run whose output must be non-empty.
    Paste all four outputs into the SUMMARY. A zero without its control is not evidence — `grep` here
    is a ugrep wrapper honoring `.gitignore` and can be blind to a whole tree.

    If a derivation now finds an importer, do NOT delete. Record the importer and hand the row back
    for re-decision: the table authorised a deletion under stated conditions, not unconditionally.

    Where the row says DELETE, delete the file. Where it says OWN, leave the file and make its
    ownership real rather than nominal — at minimum a header comment naming the owner and the reason
    it survives, so the next sweep does not re-file it as dead.

    For `QuickNavigationMenu.tsx` specifically, before deleting, confirm the row's route-list
    comparison against the live `navigation-config.ts` still holds, and carry forward anything its
    list knew that the live config does not. `RULING-P97-03` §1 is explicit: a parallel list is also
    a record of intent, and deleting it silently discards whatever the live config forgot. If the
    comparison surfaces a route the live nav lacks, that is a reachability finding — hand it to 97-12
    for the closing record rather than deleting it out of existence.

    After both operations run `pnpm typecheck` and a frontend build. A dead module is only dead if
    removing it changes nothing that compiles.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && T="$R/.planning/phases/97-reachability/97-NAV04-DECISIONS.md" && test -f "$T" && test -d "$R/frontend/src" && CTRL=$(command grep -rn "store/authStore'" "$R/frontend/src" --include='*.ts' --include='*.tsx') && test -n "$CTRL" && test "$(command grep -c '^DECISION frontend/src/services/auth.ts: \(DELETE\|OWN\)$' "$T")" -eq 1 && test "$(command grep -c '^DECISION frontend/src/components/layout/QuickNavigationMenu.tsx: \(DELETE\|OWN\)$' "$T")" -eq 1 && if command grep -q "^DECISION frontend/src/services/auth.ts: DELETE$" "$T"; then test ! -f "$R/frontend/src/services/auth.ts"; else test -f "$R/frontend/src/services/auth.ts" && command grep -q 'owner' "$R/frontend/src/services/auth.ts"; fi && if command grep -q "^DECISION frontend/src/components/layout/QuickNavigationMenu.tsx: DELETE$" "$T"; then test ! -f "$R/frontend/src/components/layout/QuickNavigationMenu.tsx"; else test -f "$R/frontend/src/components/layout/QuickNavigationMenu.tsx" && command grep -q 'owner' "$R/frontend/src/components/layout/QuickNavigationMenu.tsx"; fi && IMP="${TMPDIR:-/tmp}/p97-11-importers.out" && { command grep -rn "services/auth'" "$R/frontend/src" --include='*.ts' --include='*.tsx' > "$IMP"; test ! -s "$IMP"; } && cd "$R" && pnpm typecheck 1>&2 && pnpm --filter intake-frontend build 1>&2</automated>
  </verify>
  <acceptance_criteria>**Both module rows are read as ANCHORED DECISION TOKENS with a closed two-term set (`DELETE` or `OWN`), asserted to exist EXACTLY ONCE before either arm is taken** — the same repair as Task 1, for the same reason: `"services/auth.ts.*DELETE"` matched a row reading "kept per DELETE-or-OWN review" in the revision reproduction, and its selected arm then demanded the file be ABSENT. The OWN arm no longer merely asserts the file survived: it also requires the word `owner` to appear IN the surviving file, which is the mechanical form of the action's "make its ownership real rather than nominal" and stops OWN from being the do-nothing verdict. The scratch importer probe writes to `${TMPDIR}` rather than into the phase's own deliverable directory, where it was untracked residue that broke this plan's own C1 "`git status --porcelain` identical" clause on every drill run (W-5). MAX REACHABLE: each token count is 0 today and reaches exactly 1; the importer probe's file is empty today (zero importers, verified) and stays empty.

The gate runs the positive control ITSELF and requires non-empty output before trusting any zero: if `store/authStore'` ever comes back empty, the instrument is blind and every zero in this gate is void — that is the exact failure a bare zero-check cannot see, and it is why the control precedes the assertion rather than accompanying it in prose. The control is captured by command substitution INSIDE the `&&` chain and the importer probe is wrapped in a brace group, so neither can discard the guards before it — an earlier draft of this gate used a bare `;` there and the drill caught it GREEN on the undone tree, which is the vacuous-guard class in its purest form. Nothing is piped, because a pipe would hand the exit status to the tail rather than to grep (watchers fail closed: condition AND exit code). Each module's presence is asserted in the direction its row implies, so an unauthorised deletion and a skipped authorised one both go red. `pnpm typecheck` AND `pnpm --filter intake-frontend build` are both green — the build is the actual test of deadness, since a module removed from a tree that still compiles and bundles is a module nothing needed; capture the build's own output for attribution, since a non-zero exit on an aggregate proves only that something failed. GATE DRILL: RED observed in a scratch copy by adding a single import of `services/auth` from a live file and watching the `IMP` assertion fire — that is a red for the subject's own reason, not a tooling red. GREEN observed on the constructed done state. `git status --porcelain` identical before and after in the plans' own tree.</acceptance_criteria>
<done>Both parallel-truth modules are resolved, and the tree still builds without them.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                                          |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------- |
| decision table → deletion          | files leave the tree only on a written row; the gate reads the row rather than trusting the operator |
| dead module → live persisted state | `services/auth.ts` would hijack the live `'auth-storage'` key the moment anything imported it        |

## STRIDE Threat Register

| Threat ID | Category          | Component                       | Disposition | Mitigation Plan                                                                                                           |
| --------- | ----------------- | ------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------- |
| T-97-33   | Tampering         | the dormant dual auth store     | mitigate    | deletion removes the collision entirely; the live store keeps sole ownership of the persist key                           |
| T-97-34   | Repudiation       | unauthorised or silent deletion | mitigate    | the gate reads the decision table and asserts the row-implied state in both directions; no row, no deletion               |
| T-97-35   | Denial of Service | a deletion breaking the build   | mitigate    | `pnpm typecheck` plus a real frontend build gate the change; Bundle Size Check is a required CI gate and deletions shrink |
| T-97-36   | Tampering         | intended-broken surfaces        | mitigate    | `/delegations` asserted still present as an exclusion control; the legal-holds region is untouched (Phases 102 and 100)   |
| T-97-SC   | Tampering         | package installs                | accept      | zero installs this phase                                                                                                  |

</threat_model>

<verification>
- Both gates green; `pnpm typecheck` and a frontend build both green.
- The route count is reported as BEFORE, AFTER and DELTA, with the deletions that caused it named.
- POPULATION DEFINITION: the DELETE rows of `97-NAV04-DECISIONS.md` and the two filed dead modules.
  OUTSIDE IT: every NAV ENTRY row (97-10), every route not named by a DELETE row, `/delegations`
  (Phase 102) and the legal-holds region (Phase 100) which are asserted untouched, and the
  intentional `/engagements` double-mount plus the deliberately kept `scenario-sandbox` and
  `responsive-demo`.
- If any authorisation proved stale or any zero turned non-zero, nothing is deleted and the row goes
  back for re-decision — recorded, not worked around.
</verification>

<success_criteria>
Exactly the files the table authorised are gone, the regenerated tree agrees, the count moved by a
stated delta, and the application still builds.
</success_criteria>

<output>
Create `.planning/phases/97-reachability/97-11-SUMMARY.md` when done. It MUST record: the route
count before, after and its delta with the causing deletions named; all four zero-importer
derivation outputs including both controls; whether either authorisation proved stale against the
moved tree; what `QuickNavigationMenu`'s route list contained that `navigation-config.ts` does not,
if it was deleted; the closed gates elsewhere in the phase whose route subjects this plan changed;
and the build output confirming deadness.
</output>
