---
phase: 97-reachability
plan: 08
type: execute
wave: 2
depends_on: [97-02]
files_modified:
  - frontend/src/components/workspace/WorkspaceTabNav.tsx # the digests tab entry
  - frontend/src/i18n/en/workspace.json # the ONE new key
  - frontend/src/i18n/ar/workspace.json # its Arabic twin, same commit
  - frontend/src/components/list-page/ListPageShell.tsx # one optional actions slot, seven consumers
  - frontend/src/components/list-page/__tests__/ListPageShell.test.tsx # the slot's unit pin
  - frontend/src/routes/_protected/dossiers/countries/index.tsx
  - frontend/src/routes/_protected/dossiers/organizations/index.tsx
  - frontend/src/routes/_protected/dossiers/forums/index.tsx
  - frontend/src/routes/_protected/dossiers/working_groups/index.tsx
  - frontend/src/routes/_protected/dossiers/topics/-TopicsListPage.tsx
  - frontend/src/routes/_protected/dossiers/persons/-PersonsListPage.tsx
  - frontend/src/pages/engagements/EngagementsListPage.tsx
autonomous: true
requirements: [NAV-03]
success_criterion: '3 (the engagement Digests tab appears in the tab bar, and every list page exposes a create affordance).'
truths:
  - "Decisions covered — D-05: the population is THE 8 LIST PAGES, enumerated in this plan, with elected-officials named as the 1-of-8 that already ships the affordance and is therefore the model rather than a target; the affordance follows the shipped .btn-primary Button recipe under the closest-existing-component law with no new variant; and any page whose create flow is known-broken has that state named in the record rather than silently linked; D-12: the one new tab label lands in BOTH locales in the SAME commit and every create button reuses the page's existing empty-state CTA key so header and empty state say the same words."
must_haves:
  truths:
    - 'The Digests tab is in the engagement tab bar and clicking it mounts the digests view'
    - 'All eight dossier-type list pages show a create affordance WITH ROWS PRESENT, not only when empty'
    - 'The seven added affordances come from ONE shared slot, not seven bespoke implementations'
    - 'The new tab label resolves in both en and ar'
  artifacts:
    - path: 'frontend/src/components/list-page/ListPageShell.tsx'
      provides: 'an optional actions slot in the existing page-head, byte-identical in shape to PageHeader.tsx:29'
      contains: 'actions'
    - path: 'frontend/src/i18n/ar/workspace.json'
      provides: 'the Arabic Digests tab label'
      contains: 'digests'
  key_links:
    - from: 'frontend/src/components/list-page/ListPageShell.tsx'
      to: 'the seven list pages'
      via: 'one actions prop rendered in the existing header, consumed by every shell page'
      pattern: 'actions'
    - from: 'frontend/src/components/workspace/WorkspaceTabNav.tsx'
      to: '/engagements/$engagementId/digests'
      via: 'a WORKSPACE_TABS entry rendered by the existing Link recipe'
      pattern: 'digests'
---

<objective>
Close criterion 3's two halves.

**The tab half is one array entry.** The route
`routes/_protected/engagements/$engagementId/digests.tsx` exists and lazy-loads a shipped
`DigestsTab`; `WORKSPACE_TABS` simply never listed it, so a built feature has no way in.

**The create-affordance half is one shared prop, not seven implementations.** RESEARCH read the
seven pages as heterogeneous — four on `ListPageShell`, engagements "delegating", persons and topics
"shell-less". A direct derivation says otherwise: `<ListPageShell` has exactly EIGHT render sites in
`frontend/src` — seven product pages and one test — and those seven ARE the seven pages that need
the affordance. So the work is one optional `actions` slot on the shell plus seven call-site edits.
The eighth page, elected-officials, is the one that already ships the affordance and is the only one
NOT on the shell; it is the model to copy, not a target.

**The failure this must not reproduce** (RESEARCH Pitfall 4): all seven already pass an `onCreate`
handler — into `ListEmptyState` only. A sweep that greps `onCreate` would declare 8/8 done while a
user with rows on screen has no create path. The affordance must be visible WITH DATA PRESENT, which
is why 97-02's oracle makes data-presence a precondition rather than an assumption.

Purpose: everything built here is reachable from the page a user is already on.
Output: one tab entry, one shared slot, seven call sites, one new key in two locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-reachability/97-UI-SPEC.md (§NAV-03 — the tab entry verbatim, the create recipe verbatim, the prescribed header-actions position, the no-fourth-variant rule, the shared-CTA-key rule)
@.planning/phases/97-reachability/97-PATTERNS.md (§C-1 the eight-render-site derivation with its command; §NAV-03(a) the tab array and Link recipe; §NAV-03(b) the EO model, the PageHeader actions slot, the shell header to extend, both call-site shapes; §S-4 i18n law)
@frontend/src/routes/_protected/dossiers/elected-officials/index.tsx (the 1-of-8 that already has it — read :190-215 as the model)
@frontend/src/components/layout/PageHeader.tsx (the actions-slot pattern to copy byte-for-byte)

<interfaces>
Verified on disk 2026-08-17 at HEAD `9c32c4db1`.

- **The `<ListPageShell` population, derived (re-run at execution):**
  `command grep -rln '<ListPageShell' /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0/frontend/src --include='*.tsx'`
  → **8** files: the seven product pages listed in `files_modified` plus
  `components/list-page/__tests__/ListPageShell.test.tsx`. **The test file is the instrument control
  for this sweep** — a run that returns 7 has silently dropped one and must not be trusted.
- `ListPageShell.tsx:4-12` props: `title`, `subtitle?`, `toolbar?`, `isLoading?`, `isEmpty?`,
  `emptyState?`, `children?` — no actions slot. Its header at `:48-55` is
  `<header className="page-head">` with a single child div.
- The slot to copy: `components/layout/PageHeader.tsx:11-31` — `actions?: React.ReactNode` on the
  props and `{actions != null && <div className="dash-hero-actions">{actions}</div>}` as the
  header's second child. Same `page-head` class, same wrapper, so the rendered position matches the
  EO model with zero new CSS.
- The EO model (`elected-officials/index.tsx:196-209`): a `<Button asChild>` wrapping a TanStack
  `<Link to="/dossiers/{segment}/create">` with `<Plus className="h-4 w-4 me-2" />` and the label,
  classed `min-h-11 min-w-11 w-full sm:w-auto`.
- Call-site shapes: countries/forums/organizations/working_groups invoke the shell DIRECTLY from
  their route files; topics (`-TopicsListPage.tsx:214`), persons (`-PersonsListPage.tsx:209`) and
  engagements (`pages/engagements/EngagementsListPage.tsx:285`) invoke it from inside a page
  component the route delegates to — the edit is in the wrapper component, not the route file. All
  three already accept `onCreate?: () => void` and pass it only into their empty state.
- Copy: use each page's EXISTING `empty-states:list.{entity}.cta` key so the header button and the
  empty-state CTA say the same words. All seven keys already exist in both locales — ZERO new copy
  for this half. `elected-officials` keeps its own shipped `elected-officials:list.add` label; its
  Title Case diverges from the sentence-case `empty-states` family, which is a `COPY-*` finding
  owned by Phase 98 — record it, do not fix it here.
- `WORKSPACE_TABS` (`WorkspaceTabNav.tsx:26-35`): eight entries of `{ key, labelKey, path }`, no
  `digests`. Render recipe at `:74-100` is unchanged — the new tab is one array entry and zero JSX.
  Place it after `signals` (the intelligence grouping). The component addresses its namespace via
  `useTranslation('workspace')` at `:50`, so keys stay dot-form INSIDE it; the colon rule applies to
  callers passing a fully-qualified key.
- `workspace:tabs.digests` is absent from BOTH locales — the ONE new key in this plan. Take the
  wording verbatim from the shipped `intelligence-digests:tab.label` so two surfaces do not name the
  same feature differently.
- Test analog: `ListPageShell.test.tsx:56` already has `it('renders toolbar when provided', …)` —
  the actions test is that test with one word changed.
- All eight `/dossiers/{segment}/create` routes exist on disk.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: the Digests tab</name>
  <files>frontend/src/components/workspace/WorkspaceTabNav.tsx, frontend/src/i18n/en/workspace.json, frontend/src/i18n/ar/workspace.json</files>
  <read_first>frontend/src/components/workspace/WorkspaceTabNav.tsx in full (the WorkspaceTab interface and WORKSPACE_TABS :20-35, the Link recipe :74-100, the namespace binding at :50); frontend/src/routes/_protected/engagements/$engagementId/digests.tsx (the route that already exists); frontend/src/i18n/en/workspace.json and frontend/src/i18n/ar/workspace.json (the tabs blocks); frontend/src/i18n/en/intelligence-digests.json (the shipped wording to copy verbatim); 97-UI-SPEC.md §NAV-03(a)</read_first>
  <action>
    Append one `WORKSPACE_TABS` entry — key `digests`, labelKey `tabs.digests`, path `digests` —
    positioned after `signals`. Nothing else in this file changes: the tab renders through the
    existing `Link` recipe, and the tab bar's legacy `bg-muted/50` chrome is inherited and
    explicitly out of scope.

    Add `tabs.digests` to BOTH `workspace.json` locale files in this same change, taking the English
    wording verbatim from the shipped `intelligence-digests` tab label and the Arabic likewise if it
    ships one; if the Arabic side has no shipped counterpart, use the Arabic already used for the
    same concept elsewhere rather than inventing new Arabic — Arabic naturalness sign-off is an
    operator park and no plan claims it. A key present in English and missing in Arabic renders
    English in BOTH languages, so half a key is worse than none.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && W="$R/frontend/src/components/workspace/WorkspaceTabNav.tsx" && EN="$R/frontend/src/i18n/en/workspace.json" && AR="$R/frontend/src/i18n/ar/workspace.json" && for f in "$W" "$EN" "$AR"; do test -f "$f" || exit 1; done && test -f "$R/frontend/src/routes/_protected/engagements/\$engagementId/digests.tsx" && N=$(sed -n "/const WORKSPACE_TABS: WorkspaceTab\[\] = \[/,/^\]/p" "$W" | command grep -c "key: '") && test "$N" -eq 9 && node -e "const e=require('$EN'),a=require('$AR');if(!e.tabs||!e.tabs.digests)process.exit(1);if(!a.tabs||!a.tabs.digests)process.exit(1);if(Object.keys(e.tabs).length!==Object.keys(a.tabs).length)process.exit(1)" && SPEC="$R/tests/e2e/97-digests-tab.spec.ts" && test -f "$SPEC" && EXP=$(command grep -c "^\s*test(" "$SPEC") && test "$EXP" -ge 2 && PJ="${TMPDIR:-/tmp}/p97-08-digests.json" && rm -f "$PJ" && cd "$R" && pnpm typecheck 1>&2 && { PLAYWRIGHT_JSON_OUTPUT_NAME="$PJ" pnpm exec playwright test tests/e2e/97-digests-tab.spec.ts --project=chromium-en --no-deps --reporter=json 1>&2 || true; test -f "$PJ"; } && PASSED=$(node -e 'const j=require(process.argv[1]);let n=0;const w=s=>{(s.suites||[]).forEach(w);(s.specs||[]).forEach(sp=>(sp.tests||[]).forEach(t=>(t.results||[]).forEach(r=>{if(r.status==="passed")n++})))};(j.suites||[]).forEach(w);console.log(n)' "$PJ") && test "$PASSED" -eq "$EXP"</automated>
  </verify>
  <acceptance_criteria>`WORKSPACE_TABS` holds exactly 9 entries — derived by counting `key: '` INSIDE the array's own sed range, so the threshold is scoped to a structured field and cannot be reached by prose or by an unrelated `key:` elsewhere in the file (C4, C8); 8 was the count before, so 9 is the assertion that exactly one tab was added and none lost. Both locale `tabs` blocks carry `digests` AND have equal key counts, parsed as JSON so a half-present key is named rather than averaged (D-12). The digests ROUTE file's existence is asserted in the same chain — a tab pointing at a route that does not exist would be a new dead link, which is the opposite of this requirement (C5 doubles as the instrument control here).

**This gate RUNS `97-digests-tab.spec.ts`, which no gate in the set previously did.** That was the sharpest omission in the plan set: 97-02 wrote the spec, listed it in `files_modified`, `must_haves`, its task name and its files list — and it appeared in no automated-gate block anywhere, so half of criterion 3 had no executed behavioural oracle at all. (Written without the literal tag names on purpose: `scripts/gate-drill.mjs` pairs an opening gate tag in PROSE with the next real closing tag and extracts the intervening text as a gate, which turns a plan's own commentary into a PARSE-FAIL — observed and fixed during this revision round.) Every sibling surface runs its spec inside its gate (97-06 T2, 97-07 T3, 97-08 T3, 97-10 T3), and CONTEXT **D-10 is a LOCKED user decision**: reachability is a click-through claim, "not merely present in a data structure". Every other clause in this gate is a data-structure claim — an array length, two JSON key sets, a file's existence — which is exactly the shape D-10 refuses, so without the run this gate could go fully green while the tab reached nothing. `EXP` is derived from the spec's own declaration count and `PASSED` must equal it, because `test.skip()` exits 0 (`RULING-P97-06` §2); the run's exit is swallowed inside a brace group whose value is `test -f "$PJ"`, preserving the `&&` chain.

MAX REACHABLE: `N` = 8 entries today + exactly 1 appended = 9, equal to the threshold; the two locale key counts are equal by construction once the one key lands in both; `EXP` = 2 declarations (the action mandates two tests) against a floor of 2, and `PASSED` reaches 2 when the tab renders and navigates.

`pnpm typecheck` green, output redirected to stderr so the drill captures the compiler message (W-11). GATE DRILL: RED observed on the undone tree for the subject's reason (the count is 8 today and neither locale has the key); GREEN for the structural half constructed in a scratch copy. The Playwright half is `NOT CONSTRUCTED: requires the running dev stack and at least one seeded engagement; drilled at execution` — the same honest label its four siblings carry, and the JSON count is the attribution.</acceptance_criteria>
<done>The built digests feature has a way in.</done>
</task>

<task type="auto">
  <name>Task 2: one actions slot on ListPageShell, with its unit pin</name>
  <files>frontend/src/components/list-page/ListPageShell.tsx, frontend/src/components/list-page/__tests__/ListPageShell.test.tsx</files>
  <read_first>frontend/src/components/list-page/ListPageShell.tsx in full (the props interface :4-12 and the header :48-55); frontend/src/components/layout/PageHeader.tsx in full (the actions slot to copy byte-for-byte at :11-31); frontend/src/components/list-page/__tests__/ListPageShell.test.tsx in full (the toolbar test at :56 is the template); 97-UI-SPEC.md §NAV-03(b) placement rules</read_first>
  <action>
    Add `actions?: ReactNode` to `ListPageShellProps` and render
    `{actions != null && <div className="dash-hero-actions">{actions}</div>}` as the second child of
    the existing `<header className="page-head">`. Copy the guard and the wrapper from
    `PageHeader.tsx:29` exactly — same class, same null check — so the rendered position matches the
    EO model and no new CSS is introduced. Do not touch `toolbar`: the toolbar stays for filter and
    search controls, and header-actions is the prescribed position for the create affordance.

    Add the unit test beside the existing toolbar one: renders the actions node when provided,
    renders nothing extra when omitted. Two assertions, mirroring the shipped test's shape.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && S="$R/frontend/src/components/list-page/ListPageShell.tsx" && T="$R/frontend/src/components/list-page/__tests__/ListPageShell.test.tsx" && test -f "$S" && test -f "$T" && test -f "$R/frontend/src/components/layout/PageHeader.tsx" && command grep -q 'actions?: ReactNode' "$S" && command grep -q 'dash-hero-actions' "$S" && command grep -q 'actions' "$T" && cd "$R" && pnpm --filter intake-frontend exec vitest run src/components/list-page/__tests__/ListPageShell.test.tsx 1>&2</automated>
  </verify>
  <acceptance_criteria>The prop and the exact wrapper class from the analog are present, and the shell's unit suite is green including the new case. `PageHeader.tsx`'s existence is asserted in the same chain as the instrument control proving the search root is live (C5). GATE DRILL: RED observed on the undone tree for the subject's reason — `dash-hero-actions` does not appear in `ListPageShell.tsx` today, though it does appear in `PageHeader.tsx`, which is exactly why the analog is pinned in the same chain rather than assumed. GREEN constructed in a scratch copy with the vitest reporter output captured as attribution, not the exit code alone.</acceptance_criteria>
  <done>Seven pages can now place a header action without seven implementations.</done>
</task>

<task type="auto">
  <name>Task 3: the seven call sites</name>
  <files>frontend/src/routes/_protected/dossiers/countries/index.tsx, frontend/src/routes/_protected/dossiers/organizations/index.tsx, frontend/src/routes/_protected/dossiers/forums/index.tsx, frontend/src/routes/_protected/dossiers/working_groups/index.tsx, frontend/src/routes/_protected/dossiers/topics/-TopicsListPage.tsx, frontend/src/routes/_protected/dossiers/persons/-PersonsListPage.tsx, frontend/src/pages/engagements/EngagementsListPage.tsx</files>
  <read_first>frontend/src/routes/_protected/dossiers/elected-officials/index.tsx:190-215 (the model, including its imports at :15-19); frontend/src/routes/_protected/dossiers/countries/index.tsx in full (the direct-consumer shape: the existing onCreate at :209-211 and the shell invocation at :240-259); frontend/src/routes/_protected/dossiers/topics/-TopicsListPage.tsx and frontend/src/routes/_protected/dossiers/persons/-PersonsListPage.tsx and frontend/src/pages/engagements/EngagementsListPage.tsx (the wrapper-consumer shape and the onCreate prop each already accepts); frontend/src/i18n/en/empty-states.json (the CTA keys to reuse)</read_first>
  <action>
    Pass an `actions` node to `ListPageShell` at each of the seven sites, copying the EO recipe
    verbatim: `<Button asChild className="min-h-11 min-w-11 w-full sm:w-auto">` wrapping a TanStack
    `<Link to="/dossiers/{segment}/create">` with `<Plus className="h-4 w-4 me-2" />` and the label
    from that page's OWN `empty-states:list.{entity}.cta` key. Reuse the page's existing `onCreate`
    intent — a `Link` is preferred over an onClick handler so the affordance is a real link a user
    can middle-click, matching the model. Do not delete the empty-state CTA; both paths coexist, and
    they now say the same words because they read the same key.

    For the three wrapper consumers the edit is inside the page component, not the route file, since
    that is where the shell is invoked.

    No new button variant, no icon-only button, no split button, no dropdown. `me-2` is logical and
    stays logical; the button is full-width below `sm` and auto above, exactly as the model does.

    Condition-8 duty: all eight create routes exist and the 2026-08-15 audit graded all eight as
    rendering, but submit-path state per type is NOT verified. If 97-02's oracle finds a create
    destination that does not render or does not accept a submit, name that page and that state in
    this plan's SUMMARY for 97-09's record rather than silently linking to it.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && D="$R/frontend/src" && SITES=$(command grep -rln '<ListPageShell' "$D" --include='*.tsx') && test -n "$SITES" && TOTAL=$(printf '%s\n' "$SITES" | wc -l | tr -d ' ') && test "$TOTAL" -eq 8 && printf '%s\n' "$SITES" | command grep -q '__tests__/ListPageShell.test.tsx' && PROD=$(printf '%s\n' "$SITES" | command grep -v '__tests__') && WITH=$(printf '%s\n' "$PROD" | while read -r f; do command grep -q 'actions=' "$f" && printf 'x\n'; done | wc -l | tr -d ' ') && test "$WITH" -eq "$((TOTAL - 1))" && SPEC="$R/tests/e2e/97-list-create-affordances.spec.ts" && test -f "$SPEC" && EXP=$(command grep -c "^\s*test(" "$SPEC") && test "$EXP" -ge 9 && PJ="${TMPDIR:-/tmp}/p97-08-affordances.json" && rm -f "$PJ" && cd "$R" && pnpm typecheck 1>&2 && { PLAYWRIGHT_JSON_OUTPUT_NAME="$PJ" pnpm exec playwright test tests/e2e/97-list-create-affordances.spec.ts --project=chromium-en --no-deps --reporter=json 1>&2 || true; test -f "$PJ"; } && PASSED=$(node -e 'const j=require(process.argv[1]);let n=0;const w=s=>{(s.suites||[]).forEach(w);(s.specs||[]).forEach(sp=>(sp.tests||[]).forEach(t=>(t.results||[]).forEach(r=>{if(r.status==="passed")n++})))};(j.suites||[]).forEach(w);console.log(n)' "$PJ") && test "$PASSED" -eq "$EXP"</automated>
  </verify>
  <acceptance_criteria>The shell population is ENUMERATED at run time by `grep -rln '<ListPageShell'` and the test file is asserted to be among them — but be precise about which half is derived and which is frozen, because the earlier wording called the whole thing DERIVED and it is not: `TOTAL -eq 8` is a FROZEN LITERAL (8 render sites measured at HEAD `9c32c4db1`), and only `WITH -eq $((TOTAL - 1))` is genuinely derived from the same run. The frozen 8 is deliberate and satisfiable — it is a population pin that goes red if a shell page appears or disappears, which is the behaviour this task wants — but it is a pin, not a derivation, and a reader must not take it for one (W-13). The test file is asserted to be among them — that is the instrument control from PATTERNS §C-1: a sweep returning 7 has silently dropped a site, and without the control a shrunken population would read as a clean pass. All 7 product sites pass `actions=`; the threshold is `TOTAL - 1`, derived from the same sweep in the same run, so adding an eighth shell page later makes this gate red rather than silently uncovered (C4). The behavioural half is 97-02's eight-page oracle, run with `--no-deps` because the `setup` project throws without six `E2E_*` keys (E2ECRED-01, Phase 101; D-12 forbids waiting on it) — and it is the C10 partner to the structural count, because only it can prove the affordance is visible WITH ROWS PRESENT, which is the actual criterion. It is invoked as a SINGLE path here; when this spec is run together with the digests spec, both files must be asserted to exist first, since two paths where one does not match silently drops the other at exit 0. **Its verdict is a COUNTED pass total, not an exit code:** `EXP` comes from the spec's own declaration count and `PASSED` must equal it, so a page whose test SILENTLY SKIPPED cannot ride out on exit 0 (`RULING-P97-06` §2) — and because 97-02's spec is written to FAIL, not skip, on an unmet data precondition, a red here still names the page rather than hiding it. The run's exit is swallowed inside a brace group whose value is `test -f "$PJ"`, so the `&&` chain is preserved and the count is the verdict. MAX REACHABLE: `TOTAL` = 8 today, `WITH` reaches 7 when all seven product sites pass `actions=`; `EXP` = 9 declarations (eight pages plus the elected-officials positive control) against a floor of 9, and `PASSED` reaches 9 when every page has rows and an affordance. GATE DRILL: RED observed on the undone tree for the subject's reason (`WITH` is 0 today); GREEN for the structural half constructed in a scratch copy. The Playwright half is `NOT CONSTRUCTED: requires the running dev stack and rows present on all eight lists; drilled at execution`, and any page whose data precondition cannot be met is recorded as UNABLE TO MEASURE with the page named, never counted as a pass.</acceptance_criteria>
  <done>A user looking at a full list can create the next one, on all eight pages.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                      |
| ---------------------------- | ---------------------------------------------------------------- |
| list page → create route     | a header affordance is a new entry point into eight create flows |
| i18n bundle → rendered label | a half-present key renders English in both locales               |

## STRIDE Threat Register

| Threat ID | Category               | Component                     | Disposition | Mitigation Plan                                                                                                     |
| --------- | ---------------------- | ----------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------- |
| T-97-22   | Spoofing               | create affordance visibility  | accept      | the button is discoverability only; the create routes are behind `_protected` and their own server-side checks      |
| T-97-23   | Information Disclosure | list/create destinations      | mitigate    | the oracle asserts no PostgREST/Postgres code or stack shape reaches the DOM on any touched surface                 |
| T-97-24   | Repudiation            | linking to an unverified flow | mitigate    | condition-8 duty: a create destination that does not render or submit is NAMED in the record, never silently linked |
| T-97-SC   | Tampering              | package installs              | accept      | zero installs this phase                                                                                            |

</threat_model>

<verification>
- All three gates green; `pnpm typecheck` green from the root; `pnpm lint` run for the i18n
  namespace check with its colour recorded.
- 97-02's two specs run and their colours recorded per test. When run as a PAIR, both spec files
  are asserted to exist first — Playwright paths are FILTERS and a non-matching path silently
  drops its partner at exit 0.
- POPULATION DEFINITION: the engagement workspace tab bar and the eight dossier-type list pages.
  OUTSIDE IT: the tab bar's inherited legacy chrome, the `/engagements` top-level mount (the
  double-mount is INTENTIONAL and untouched), `scenario-sandbox` and `responsive-demo` (kept
  deliberately), the create wizards' own internals, and the EO button's Title Case divergence
  (Phase 98's `COPY-*`).
</verification>

<success_criteria>
Criterion 3 holds behaviourally: the Digests tab reaches its view, and all eight list pages offer a
create path to a user who already has data.
</success_criteria>

<output>
Create `.planning/phases/97-reachability/97-08-SUMMARY.md` when done. It MUST record: the
execution-time `<ListPageShell` sweep output including the test-file control hit; the seven CTA keys
actually used, page by page; the Arabic source for `tabs.digests` and whether it was a shipped
string or a reuse; the per-page colour of the eight-page oracle including any page whose
data-present precondition could not be met; and any create destination whose render or submit state
was found wanting, which 97-09's record consumes.
</output>
