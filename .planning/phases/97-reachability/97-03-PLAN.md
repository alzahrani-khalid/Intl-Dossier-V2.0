---
phase: 97-reachability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/inbound-link-classify.mjs # NEW — the INBOUND-LINK instrument
  - .planning/phases/97-reachability/97-POPULATIONS.md # NEW — both closing derivations, single-writer
autonomous: true
requirements: [NAV-04]
success_criterion: '4 (every route with no inbound link is resolved) — this plan produces the EVIDENCE the decision table consumes; it decides no route.'
truths:
  - 'Decisions covered — D-09: two populations rule this phase and both are derived by instrument here — the ROUTE population mechanically from routeTree.gen.ts with blind spots stated, and the INBOUND-LINK population as the union of known link/navigation forms plus a suspect list, with the residual PRINTED and hand-classified as part of the deliverable and the count declared a FLOOR; every zero-inbound claim is instrument-tested against the known-linked /admin/ai-settings control in the same run; D-08: the route count is derived at run time and never frozen, because this phase deletes routes by design.'
must_haves:
  truths:
    - 'Running the instrument prints a POPULATION DEFINITION line and a FLOOR line, never a bare total'
    - 'A zero-inbound verdict for any path is accompanied, in the same run, by the pinned control /admin/ai-settings resolving NON-zero'
    - 'The instrument distinguishes /admin/approvals from the top-level /approvals — a substring sweep conflates them'
    - 'Hits inside components/modern-nav/ are classified NON-RENDERED, not counted as inbound links'
    - 'The instrument exits 2 (UNABLE TO MEASURE) rather than reporting a silent zero when an input is absent'
  artifacts:
    - path: 'scripts/inbound-link-classify.mjs'
      provides: 'mechanical INBOUND-LINK classifier with pinned controls and a printed residual'
      contains: 'UNABLE TO MEASURE'
    - path: '.planning/phases/97-reachability/97-POPULATIONS.md'
      provides: 'both closing derivations with blind spots and the hand-classified residual'
      contains: 'POPULATION DEFINITION'
  key_links:
    - from: 'scripts/inbound-link-classify.mjs'
      to: 'frontend/src/routeTree.gen.ts'
      via: 'the ROUTE population read from FileRoutesByFullPath at run time'
      pattern: 'FileRoutesByFullPath'
    - from: '.planning/phases/97-reachability/97-POPULATIONS.md'
      to: '.planning/phases/97-reachability/97-NAV04-DECISIONS.md'
      via: 'the evidence 97-09 cites per row'
      pattern: 'NAV04-DECISIONS'
---

<objective>
Build the instrument and write the two population derivations criterion 4 rests on. This plan
DECIDES NOTHING about any route — deciding is 97-09's single-writer job. It produces evidence.

Two lessons are load-bearing here and both are in-repo:

- **TRIGSWEEP (P96):** a syntactic sweep under-counts behaviour classes. The instrument is the
  union of known forms PLUS a suspect list, it PRINTS its residual for hand classification, and
  its count is declared a FLOOR — never a total. `scripts/trigsweep-classify.mjs` is the direct
  predecessor; copy its four structural moves.
- **Population-definition blindness (project memory):** a correct command returns a correct number
  about the wrong set. Both derivations state what falls OUTSIDE them.

Purpose: "no inbound link" becomes a measured, control-tested claim instead of an assertion.
Output: one script, one record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-reachability/97-RESEARCH.md (§INBOUND-LINK instrument — the ten known forms with HEAD file counts and the stated blind spots; §NAV-04 the live-vs-demo nav split)
@.planning/phases/97-reachability/97-PATTERNS.md (§NAV-04(d) — trigsweep's four moves, its exit contract, its PINS block and FLOOR wording, all quoted)
@scripts/trigsweep-classify.mjs (the analog — read in full)
@.planning/phases/95-routes-that-don-t-render/95-DEAD-04-DECISION.md (the residual-of-exactly-one precedent and the instrument-test-the-zero discipline)

<interfaces>
Verified on disk 2026-08-17 at HEAD `9c32c4db1`:

- ROUTE population instrument (P95's, `95-RESEARCH.md:679`), re-run this session → **203**:
  `sed -n '/interface FileRoutesByFullPath {/,/^}/p' frontend/src/routeTree.gen.ts | command grep -c "':"`
  P95 pinned 202; `/calendar` landed in P96. **203 is today's reading, not a constant** — this
  phase deletes routes, so every consumer derives it at run time (D-08).
- Admin paths in that interface, derived this session = **8**: `/admin/ai-settings`,
  `/admin/ai-usage`, `/admin/approvals`, `/admin/data-retention`, `/admin/field-permissions`,
  `/admin/preview-layouts`, `/admin/system`, `/admin/` (index → redirect). The register says 9 →
  **delta −1, stated not absorbed.**
- Known-forms union with HEAD file counts (RESEARCH §INBOUND-LINK): `<Link to=` 31, `navigate({ to:` 69,
  `redirect({ to:` 8, `router.navigate` 1, nav data structures 2, palette/shortcut `navigateTo(` 2,
  `<a href` 7, `window.location` 20, `window.open` 11, `history.replaceState` 1.
- Stated blind spots (the FLOOR declaration): computed paths via `getDossierDetailPath` /
  `getDossierRouteSegment` (33 files), template-literal `to={` props, runtime-built strings passed
  through variables, redirects held in server data, `useRecentNavigation` (a replay of visited
  paths — derivative, not an origin), and e2e `page.goto` (not a product link).
- PINNED CONTROL: `/admin/ai-settings` is linked at `components/layout/navigation-config.ts:178`.
  Any run that reports a zero for another path MUST resolve this one non-zero in the same run.
- CONFLATION TRAP: `navigation-config.ts:212-217` carries an item id `admin-approvals` whose path
  is the TOP-LEVEL `/approvals` — a different route from `/admin/approvals`. Match on the full
  path with a boundary; a substring match will report `/admin/approvals` as linked when it is not.
- NON-RENDERED CLASS: `components/modern-nav/*` (`navigationData.ts`, `IconRail`, `NavigationShell`,
  `ExpandedPanel`) is mounted only by `routes/modern-nav-standalone.tsx`. Its hits are classified
  `NON-RENDERED (demo-only)`, never counted as inbound links — including its `/monitoring` entry at
  `navigationData.ts:262`.
- Environment: `grep` here is a ugrep wrapper honoring `.gitignore` — the script must not shell out
  to bare `grep`; read files with `node:fs` and match in JS, which sidesteps the wrapper entirely.
  `timeout` does not exist on this machine. zsh does not word-split unquoted expansions.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/inbound-link-classify.mjs — the instrument</name>
  <files>scripts/inbound-link-classify.mjs</files>
  <read_first>scripts/trigsweep-classify.mjs in full (the four structural moves, the exit contract at :40-43 and :57-72, the PINS block at :99-119, the FLOOR wording at :161-170); 97-RESEARCH.md §INBOUND-LINK instrument; frontend/src/components/layout/navigation-config.ts in full (the LIVE nav data shape the instrument must parse, the :178 control, the :212 conflation trap)</read_first>
  <action>
    Node ESM script, no dependencies, reading files with `node:fs` so the ugrep wrapper is never in
    the path. Mirror trigsweep's structure move for move:

    (a) POPULATION enumerated mechanically, not guessed: parse `FileRoutesByFullPath` out of
    `frontend/src/routeTree.gen.ts` for the closed set of route paths.

    **The script's argument interface is exactly these three flags — implement all three; the gate
    below invokes each one:**
    - `--paths <p1,p2,...>` restricts the report to named paths (97-09 uses it for its candidate
      rows); with no argument, report the whole route population.
    - `--dead <file,...>` names files whose hits classify `NON-RENDERED (dead module)` (see (b)).
    - `--route-tree <path>` overrides the default `frontend/src/routeTree.gen.ts` input. This one
      exists so the FAIL-CLOSED path is drivable: the gate points it at a file that does not exist
      and requires exit 2. An unimplemented flag would leave the script reading the real tree and
      exiting 0, so the flag is load-bearing, not a convenience.

    (b) CLASSIFY each candidate inbound reference by the UNION of the ten known forms named in the
    interfaces block, each form a named regex in a `FORMS` array exactly as trigsweep does, so the
    output says WHICH form matched. Search root is `frontend/src`, excluding `routeTree.gen.ts` and
    `**/__tests__/**`. Every match is additionally tagged with a RENDERED class: `LIVE` by default,
    `NON-RENDERED (demo-only)` when the containing file is under `components/modern-nav/` or is
    `routes/modern-nav-standalone.tsx`, and `NON-RENDERED (dead module)` for files the caller names
    via `--dead <file,...>`. Only LIVE matches count toward a path's inbound-link count.

    (c) MATCH ON A FULL PATH WITH A BOUNDARY. `/admin/approvals` must not be satisfied by
    `/approvals`, and `/dossiers/persons` must not be satisfied by
    `/dossiers/persons/$id`-as-a-different-route — a reference is a match when the quoted or
    template path equals the route path or is that path followed by `/`, `'`, `"`, backtick, or `?`.
    Print the conflation guard's own self-test result (see the PINS block below).

    (d) RESIDUAL PRINTED: every reference that mentions a route-shaped string but matches no known
    form is printed as `RESIDUAL — hand-classify`, with file:line and the raw line. The residual is
    part of the deliverable; it is never silently assumed to be a non-link.

    (e) PINS, in trigsweep's spirit — if any of these ever stops holding, that is exit 1, a
    regression, not a smaller number quietly reported:
      - `/admin/ai-settings` resolves to at least one LIVE inbound link (the known-linked control);
      - `/admin/approvals` and `/approvals` resolve to DIFFERENT counts, proving the boundary match
        is doing work (if a future edit makes them genuinely equal, the pin's message says to
        re-derive rather than to loosen the matcher);
      - `navigationData.ts`'s `/monitoring` entry classifies as `NON-RENDERED (demo-only)`.

    (f) EXIT CONTRACT copied verbatim in spirit from trigsweep: `0` classified and residual listed;
    `1` a pinned invariant broke; `2` UNABLE TO MEASURE — an input file is absent or unparseable,
    printed as `UNABLE TO MEASURE — <reason>`, fail closed, never a silent zero.

    (g) Print, before the table, a `POPULATION DEFINITION:` line naming what is inside and what
    falls OUTSIDE (the blind spots from the interfaces block, verbatim), and after it a `FLOOR:`
    line stating that the inbound-link counts are floors because syntactic sweeps under-count
    behaviour classes.

    **CLOSED-VOCABULARY CHECK on the RENDERED classes — the third term's meaning must be stated,
    because it is not what a reader assumes.** The classes are `LIVE`, `NON-RENDERED (demo-only)`
    and `NON-RENDERED (dead module)`, and the set is complete only if `LIVE` is defined honestly:
    it means "in the rendered tree by file location, and not classified otherwise" — it does NOT
    mean "proven to be mounted". A link inside a component that nothing renders is `LIVE` under
    this instrument. Print that definition beside the FLOOR line, together with the consequence:
    **an inbound-link count is a FLOOR for the absence claim and a CEILING for the presence
    claim.** A zero may under-count (some link form was missed); a non-zero may over-count (the
    linking file may itself be unmounted). `/monitoring` is the in-repo proof of the second half —
    its only link lives in a tree mounted by one demo route — and it is why `--dead` exists.

    (h) Print, for each queried path, a `NAV-CONFIG ENTRY: <path> <file>:<line>` line whenever a
    match came from `components/layout/navigation-config.ts`, and `NAV-CONFIG ENTRY: <path> NONE`
    when none did. 97-09 consumes exactly this to distinguish a route that is ALREADY REACHABLE
    from a live sidebar row from one that needs a new entry — a distinction its decision vocabulary
    previously had no term for, which would have instructed 97-10 to add duplicate rows.

    House style: no semicolons, single quotes, explicit return types where the file is TS — this is
    `.mjs`, so match `trigsweep-classify.mjs`'s own style exactly.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && test -f "$R/scripts/trigsweep-classify.mjs" && test -f "$R/scripts/inbound-link-classify.mjs" && node --check "$R/scripts/inbound-link-classify.mjs" && OUT=$(node "$R/scripts/inbound-link-classify.mjs" --paths /admin/ai-settings,/admin/approvals,/approvals,/admin/preview-layouts) && printf '%s' "$OUT" | command grep -q 'POPULATION DEFINITION' && printf '%s' "$OUT" | command grep -q 'FLOOR' && printf '%s' "$OUT" | command grep -q 'CEILING' && printf '%s' "$OUT" | command grep -q 'RESIDUAL' && printf '%s' "$OUT" | command grep -q '/admin/ai-settings' && printf '%s' "$OUT" | command grep -q '^NAV-CONFIG ENTRY: /admin/ai-settings ' && printf '%s' "$OUT" | command grep -q '^NAV-CONFIG ENTRY: /admin/preview-layouts NONE$' && FC="${TMPDIR:-/tmp}/p97-03-failclosed.out" && { node "$R/scripts/inbound-link-classify.mjs" --route-tree "$R/frontend/src/does-not-exist.ts" --paths /admin/ai-settings > "$FC" 2>&1; test "$?" -eq 2; } && command grep -q 'UNABLE TO MEASURE' "$FC"</automated>
  </verify>
  <acceptance_criteria>The script parses (`node --check`), runs to exit 0 on a real query, and its output carries the POPULATION DEFINITION, FLOOR, CEILING and RESIDUAL markers plus the pinned control. The `NAV-CONFIG ENTRY:` lines are asserted in BOTH directions in one run — `/admin/ai-settings` must report a file:line (it is linked at `navigation-config.ts:178` today) and `/admin/approvals` must report `NONE` (the nav item named `admin-approvals` at `:212` points at the TOP-LEVEL `/approvals`, a different route). A boundary matcher that conflated the two would make the second assertion fail, so the two clauses together are the conflation guard's own behavioural test rather than a claim about it. The gate ALSO drives the fail-closed path with a deliberately absent route-tree input and asserts exit **2** with the `UNABLE TO MEASURE` text — that second half is what stops a future silent zero from reading as a clean run, and it is the C1 red-direction built into the gate itself. MAX REACHABLE: the only numeric threshold here is the fail-closed probe's `-eq 2`, which is an EXIT CODE the script's own exit contract defines (`0` classified, `1` pin broken, `2` UNABLE TO MEASURE) and is therefore reachable by construction the moment `--route-tree` is implemented as specified; every other clause is a presence assertion whose maximum is 1. The expected-nonzero probe is wrapped in a brace group so the `&&` chain is PRESERVED — a bare `;` there would discard every preceding assertion and let the gate pass on the strength of its last clause alone, which is the vacuous-guard class; its exit code is read directly, never through a pipe; the analog's existence is asserted first as the instrument control on the search root (C5); no `2>/dev/null`. GATE DRILL: both directions are OBSERVED by the gate on every run — the exit-2 half is the constructed failure state and the exit-0 half is the constructed success state, so no separate scratch tree is needed here; record both observations in THIS PLAN'S SUMMARY, which 97-12 consolidates into `97-GATE-DRILL.md` as that file's single writer.</acceptance_criteria>
  <done>"No inbound link" is a measurable, control-tested, floor-declared claim.</done>
</task>

<task type="auto">
  <name>Task 2: 97-POPULATIONS.md — both closing derivations, written down</name>
  <files>.planning/phases/97-reachability/97-POPULATIONS.md</files>
  <read_first>scripts/inbound-link-classify.mjs (as just written); .planning/phases/95-routes-that-don-t-render/95-DEAD-04-DECISION.md §"Ruling condition 1" (the enumeration-with-instrument-test shape to mirror); 97-RESEARCH.md §INBOUND-LINK instrument and §NAV-04 admin-route population; .tickmarkr/overseer/ACCEPTANCE-P97-PLAN.md condition 4</read_first>
  <action>
    Write the record with two sections, each in the closing-derivation shape: the command, its
    output pasted, what is INSIDE the population, what falls OUTSIDE it, and the instrument test
    that proves the zeros are real.

    **§1 ROUTE population.** Run the P95 instrument and paste both the command and its number.
    State the blind spots: it reads `FileRoutesByFullPath`, so it counts registered full paths and
    NOT lazy children invisible to that interface, NOT redirect targets that have no file, and NOT
    routes reachable only by search-param state. State D-08 explicitly: **this number moves this
    phase by design** — 97-11 deletes routes and regenerates the tree — so no gate anywhere freezes
    it, and any consumer either re-derives it or states an expected delta.
    Then derive the ADMIN sub-population and reconcile it against the register's 9: paste the
    command, the eight paths, and the sentence "delta −1 versus the register's 9, stated not
    absorbed", naming the redirect-only `/admin/` index as the eighth member so a reader can see
    which nine the register might have meant.

    **§2 INBOUND-LINK population.** Run the instrument over the nine candidate paths criterion 4
    names (the eight admin paths + `/monitoring`) and paste the full output including the residual.
    Hand-classify EVERY residual line right here, one line each, with a verdict: `link` /
    `not-a-link` / `non-rendered` / `needs-execution-recheck`. An unclassified residual is the whole
    point of printing it — leaving one is not permitted.
    State the blind spots verbatim from the instrument's own POPULATION DEFINITION line, so the
    document and the tool cannot drift, and restate BOTH the FLOOR sentence and the CEILING
    sentence — a zero may under-count, a non-zero may over-count, and `LIVE` means "in the rendered
    tree by file location", never "proven mounted".
    Record the control result explicitly: `/admin/ai-settings` resolved N LIVE inbound links in the
    same run as every zero reported here.

    **§2b LIVE NAV STATE, per candidate.** Paste the instrument's `NAV-CONFIG ENTRY:` lines for all
    nine candidates as their own short table: path, `navigation-config.ts` line or `NONE`. This is
    the population fact the plan set was previously blind to — measured at HEAD `9c32c4db1`, FOUR of
    the nine candidates ALREADY have a live sidebar row (`/admin/ai-settings` `:178`,
    `/admin/system` `:184`, `/admin/field-permissions` `:196`, `/admin/data-retention` `:208`), and
    four do not (`/admin/ai-usage`, `/admin/approvals`, `/admin/preview-layouts`, `/monitoring`).
    Re-derive it; do not copy those line numbers forward if the file has moved. Say plainly in this
    section that "candidate" here means "named by criterion 4", NOT "measured to have no inbound
    link" — the two sets differ by those four rows, and reading the first as the second is the
    population-definition error this whole document exists to prevent.

    **§3 What this document does NOT decide.** One short paragraph: no route is disposed of here;
    97-09's `97-NAV04-DECISIONS.md` is the single writer of every per-route decision, and this file
    is only the evidence it cites. Name the intended-broken exclusions so nobody reads them into the
    evidence: `/delegations` (owner: Phase 102) and the legal-holds region (owner: Phase 100) are
    OWNED ELSEWHERE and untouched; the `/engagements` double-mount, `scenario-sandbox` and
    `responsive-demo` are intentional shapes a reachability sweep must not "fix".

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && F="$R/.planning/phases/97-reachability/97-POPULATIONS.md" && test -f "$F" && command grep -q 'POPULATION DEFINITION' "$F" && command grep -q 'FLOOR' "$F" && command grep -q 'delta' "$F" && command grep -q '/admin/ai-settings' "$F" && command grep -q '/delegations' "$F" && command grep -q 'legal-hold' "$F" && for p in /admin/ai-settings /admin/ai-usage /admin/approvals /admin/data-retention /admin/field-permissions /admin/preview-layouts /admin/system /monitoring; do command grep -q -- "$p" "$F" || exit 1; done && test "$(command grep -c 'RESIDUAL' "$F")" -ge 1 && test "$(command grep -c 'needs-execution-recheck\|not-a-link\|non-rendered\|hand-classified' "$F")" -ge 1 && command grep -q 'CEILING' "$F" && NC=$(command grep -c 'NAV-CONFIG ENTRY:' "$F") && test "$NC" -ge 9 && CFG="$R/frontend/src/components/layout/navigation-config.ts" && test -f "$CFG" && LINKED=$(command grep -c "path: '/admin/" "$CFG") && test "$LINKED" -ge 1 && for p in $(command grep -o "path: '/admin/[a-z-]*'" "$CFG" | sed "s/.*'\(.*\)'/\1/" | sort -u); do command grep -q "NAV-CONFIG ENTRY: $p " "$F" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>The record exists and names all eight admin paths plus `/monitoring` individually (looped one at a time so the gate identifies WHICH is missing rather than reporting a count that hides it — C10 against the "each" in criterion 4); it carries the population definition, the FLOOR declaration, the stated delta, the pinned control, the residual section with at least one hand-classification verdict, and the two intended-broken exclusions by name (ACCEPTANCE condition 8). MAX REACHABLE: `RESIDUAL` ≥ 1 and the hand-classification verdict count ≥ 1 are reachable because the instrument PRINTS its residual and §2 requires every line classified — if the residual is genuinely empty, the record still carries the `RESIDUAL — none, and here is the run that shows it` line, which satisfies the count; `NC` ≥ 9 is reachable because §2b pastes one `NAV-CONFIG ENTRY:` line per candidate and there are nine candidates; `LINKED` ≥ 1 is 4 today. **CLOSED-VOCABULARY CHECK on the residual verdicts** — `link` / `not-a-link` / `non-rendered` / `needs-execution-recheck`: checked and COMPLETE for this instrument's population, because the search root excludes `**/__tests__/**` and everything under `tests/e2e` (so an e2e `page.goto` never surfaces as a residual needing an "out-of-population" term), and a link inside an unmounted file is covered by `non-rendered`. If execution finds a residual none of the four describes, add the term and say so rather than filing it under the nearest one. NOTE the `/admin/` index path is deliberately NOT in the per-path loop: its literal is a prefix of every other admin path and would match vacuously — it is covered by the delta sentence instead, which is why the delta text is pinned separately. GATE DRILL: recorded in THIS PLAN'S SUMMARY (97-12 consolidates it into `97-GATE-DRILL.md`, which it alone writes) — RED observed on the undone tree (file absent); GREEN constructed in a scratch copy with a stub containing the required markers.</acceptance_criteria>
  <done>Criterion 4 has its evidence base, with both populations bounded and both zeros controlled.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                                                    |
| ----------------------- | ------------------------------------------------------------------------------ |
| repo files → instrument | the script reads source text only; it writes nothing and executes nothing read |

## STRIDE Threat Register

| Threat ID | Category               | Component                | Disposition | Mitigation Plan                                                                                                       |
| --------- | ---------------------- | ------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------- |
| T-97-06   | Tampering              | classifier verdicts      | mitigate    | pinned controls fail the run (exit 1) if a known-linked path stops resolving — a loosened matcher cannot pass quietly |
| T-97-07   | Information Disclosure | pasted instrument output | mitigate    | output is route paths and file:line only; no credential, token or env value is read or printed                        |
| T-97-08   | Repudiation            | zero claims              | mitigate    | every zero is printed beside the same-run control result, so a zero can never be cited without its control            |
| T-97-SC   | Tampering              | package installs         | accept      | zero installs; the script has no dependencies                                                                         |

</threat_model>

<verification>
- Both gates green.
- The instrument's own exit-2 path is exercised by its gate, so the fail-closed behaviour is
  observed rather than asserted.
- POPULATION DEFINITION (this plan's own scope): the ROUTE population and the INBOUND-LINK
  population for the nine criterion-4 candidates. OUTSIDE IT: any per-route DECISION (97-09), the
  `services/auth.ts` and `QuickNavigationMenu.tsx` zero-importer derivations (97-11 re-runs those
  against a known-imported control at execution — they are module-import zeros, a different
  instrument from this link sweep), and every intended-broken surface named in §3.
</verification>

<success_criteria>
The decision table's author can cite measured, bounded, control-tested evidence for every one of
its nine rows without re-deriving anything by hand.
</success_criteria>

<output>
Create `.planning/phases/97-reachability/97-03-SUMMARY.md` when done. It MUST record: the route
count as derived on the day (with the note that it is expected to move), the admin sub-population
and the stated −1 delta, the instrument's pinned-control results, the residual count and how many
required hand classification, and any path whose inbound-link state is `needs-execution-recheck`.
</output>
