---
phase: 97-reachability
plan: 09
type: execute
wave: 3
depends_on: [97-03]
files_modified:
  - .planning/phases/97-reachability/97-NAV04-DECISIONS.md # NEW — the SINGLE-WRITER per-route decision table
autonomous: true
requirements: [NAV-04]
success_criterion: '4 (every route with no inbound link is resolved — the admin routes and /monitoring each get a nav entry or are deleted, with the decision RECORDED PER ROUTE).'
truths:
  - "Decisions covered — D-06: every candidate route is resolved with the decision RECORDED PER ROUTE in this ONE single-writer table, decision plus why plus named owner per row, with NOT-CHECKED beating silence, the /monitoring row consuming 95-DEAD-04-DECISION.md by name and deciding only the nav entry, and the admin population derived mechanically from routeTree.gen.ts and reconciled against the register's 9 with the delta stated not absorbed; D-07: frontend/src/services/auth.ts resolves DELETE-or-OWN with the why recorded, its zero-importer derivation RE-RUN at execution and instrument-tested against a known-imported control; D-02: the out-of-phase surfaces are named rather than assumed — /delegations and the legal-holds region record owned-elsewhere-untouched with their owners named."
must_haves:
  truths:
    - 'Every one of the nine candidate routes has a row with a decision, a why, and an owner where applicable'
    - 'A route that ALREADY has a live nav entry is recorded ALREADY-REACHABLE and gets no new entry — the term the closed vocabulary was missing, without which four already-reachable routes would have been forced into NAV ENTRY and 97-10 instructed to add duplicates'
    - 'Every decision is also emitted as an anchored machine-readable DECISION token, because the consuming gates previously branched on prose in which the subject and the word DELETE merely co-occurred'
    - 'Both conditional deletions record the actual comparison that fired or refuted their trigger'
    - 'Both dead modules have a row whose zero-importer derivation was RE-RUN and control-tested'
    - 'The palette hardcoded-isAdmin defect has one of the two written answers, elected here with evidence'
    - 'No row claims that a nav entry, or its absence, provides access control'
  artifacts:
    - path: '.planning/phases/97-reachability/97-NAV04-DECISIONS.md'
      provides: 'the single-writer per-route decision record for criterion 4'
      contains: 'NOT-CHECKED'
  key_links:
    - from: '.planning/phases/97-reachability/97-NAV04-DECISIONS.md'
      to: '.planning/phases/95-routes-that-don-t-render/95-DEAD-04-DECISION.md'
      via: 'the /monitoring row consuming the prior KEEP ruling by name'
      pattern: '95-DEAD-04-DECISION'
    - from: '.planning/phases/97-reachability/97-NAV04-DECISIONS.md'
      to: '.planning/phases/97-reachability/97-POPULATIONS.md'
      via: 'every row citing the measured inbound-link evidence rather than re-deriving by hand'
      pattern: '97-POPULATIONS'
---

<objective>
Write the record criterion 4 IS. The requirement's own text demands that the decision be RECORDED
per route either way, so this document is not a byproduct of the work — it is the deliverable.

**Single-writer, by name.** This plan is the only writer of
`.planning/phases/97-reachability/97-NAV04-DECISIONS.md`. 97-10 acts on its nav-entry rows and
97-11 acts on its deletion rows; neither writes into it, and nothing else in the phase writes a
per-route decision anywhere. **NOT-CHECKED beats silence** — a row that could not be established
says so, with what was tried.

**This plan decides; it changes no code.** Deciding and executing are split so that a deletion
cannot happen without a row first, and so that a refuted trigger stops the work rather than
improvising around it.

Purpose: nothing in the route tree is unowned, and every disposal is attributable.
Output: one document.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-reachability/97-POPULATIONS.md (97-03's measured evidence — cite it, do not re-derive by hand)
@.tickmarkr/overseer/RULING-P97-01-ADMIN-DISPOSAL.md (the three per-route rulings and the filing order)
@.tickmarkr/overseer/RULING-P97-03-OMISSION-FINDINGS.md (§1 QuickNavigationMenu in scope; §4 the free palette propagation)
@.planning/phases/95-routes-that-don-t-render/95-DEAD-04-DECISION.md (the record shape to mirror, and the /monitoring row's named input)
@.planning/phases/97-reachability/97-PARALLEL-TRUTH-CLASS.md (the committed class record — add rows here, never by editing that file)
@.planning/phases/97-reachability/97-RESEARCH.md (§NAV-04 — the per-route inbound-link and render table, and the two nav sources)

<interfaces>
Verified on disk 2026-08-17.

**The nine candidate rows.** Eight admin paths derived mechanically from `routeTree.gen.ts` —
`/admin/ai-settings`, `/admin/ai-usage`, `/admin/approvals`, `/admin/data-retention`,
`/admin/field-permissions`, `/admin/preview-layouts`, `/admin/system`, `/admin/` (index, a pure
redirect to ai-settings) — plus `/monitoring`. The register says **9 admin routes**; the mechanical
derivation says **8**. That is a **−1 delta the table STATES, not absorbs**; name the redirect-only
index as the eighth so a reader can see which nine the register may have meant.

**"Candidate" means NAMED BY CRITERION 4, not MEASURED TO HAVE NO INBOUND LINK — and the two sets
differ by four rows.** Measured in `components/layout/navigation-config.ts` at HEAD `9c32c4db1`, the
administration group holds SEVEN items and four of them point at candidate paths:

| candidate path             | live nav entry today             |
| -------------------------- | -------------------------------- |
| `/admin/ai-settings`       | YES — `navigation-config.ts:178` |
| `/admin/system`            | YES — `:184`                     |
| `/admin/field-permissions` | YES — `:196`                     |
| `/admin/data-retention`    | YES — `:208`                     |
| `/admin/ai-usage`          | none                             |
| `/admin/approvals`         | none                             |
| `/admin/preview-layouts`   | none                             |
| `/monitoring`              | none                             |

Those four rows are `ALREADY-REACHABLE` and 97-10 adds NOTHING for them. **Cite 97-03's evidence for
each, not this table:** `97-POPULATIONS.md` §2b pastes the instrument's `NAV-CONFIG ENTRY: <path>
<file>:<line>` line per candidate, which is the derivation these four rows quote. **Re-derive at
execution rather than copying these line numbers** — 97-05 inserts a row into the dossiers group of
the same file in wave 2, so every line number below it moves. The population was previously authored
from the register's "9 admin routes" and from `routeTree.gen.ts` and never once from the live nav
config, which is how four already-reachable routes came to be presented as orphans: the
population-definition error, inside the one requirement whose subject is population correctness.

NOTE the group also holds three items whose paths are NOT candidates — `admin-tags` → `/tags`
(`:190`), `admin-audit-logs` → `/audit-logs` (`:202`), and `admin-approvals` → the TOP-LEVEL
`/approvals` (`:214`). The last is the conflation trap: it is INSIDE the administration group and
its id looks like `/admin/approvals`, but its `path` is a different route. Do not read it as an
inbound link to `/admin/approvals`.

**Ruled dispositions (`RULING-P97-01`), which this table records rather than re-litigates:**

- `/admin/ai-usage` → **NAV ENTRY**, administration group, adminOnly. Its page renders with no
  seeded data, so its own empty state must be the honest one.
- `/admin/approvals` → **CONDITIONAL DELETE.** Trigger: an execution-time comparison confirming it
  duplicates the top-level `/approvals` surface by component/feature identity, **the comparison
  PASTED into the row**. If the comparison REFUTES duplication, do NOT improvise a nav entry — the
  row records the refutation and the park RE-ESCALATES to the overseer with the comparison as
  evidence.
- `/admin/preview-layouts` → **CONDITIONAL DELETE as a product route.** Trigger: the Phase-30–32
  reference is confirmed historical tooling with no live ownership claim, the check recorded. If a
  live ownership claim exists, the row records OWNED-ELSEWHERE with the owner named and nothing is
  deleted.

**`/monitoring`'s route KEEP is already ruled** (`RULING-P95-01`, recorded in
`95-DEAD-04-DECISION.md`) — this row decides ONLY the nav entry, and it must name the
condition-8 fact: the Dashboard's API moved under `/api/monitoring` INSIDE the backend's
`NODE_ENV === 'development' || 'test'` guard, so **in production both widgets settle to their
inline `QueryErrorState`s** (testids `monitoring-health-error`, `monitoring-alerts-error`). Name it;
do not hide it; do not widen scope to fix the guard. Refine the ruled input while you are there: the
`/monitoring` link at `components/modern-nav/navigationData.ts:262` lives in a nav tree mounted only
by `routes/modern-nav-standalone.tsx`, so the live shell has NO monitoring entry today.

**Two dead-module sub-item rows, both the same class** (`97-PARALLEL-TRUTH-CLASS.md`): a second
copy of a truth that already has a canonical home, inert only because nothing imports it.

- `frontend/src/services/auth.ts` — `RULING-P92-06`: zero importers, a SECOND zustand store on the
  live `'auth-storage'` key plus a module-level `onAuthStateChange` that never registers.
- `frontend/src/components/layout/QuickNavigationMenu.tsx` — `RULING-P97-03` §1: a second route
  list beside `navigation-config.ts`. **If it is deleted, the row must say what its route list
  contained that `navigation-config.ts` does NOT** — a parallel list is a record of intent, and
  deleting it silently discards whatever the live config forgot.

**The palette filing order (`RULING-P97-01`, the audit-is-not-a-queue law).**
`CommandPalette.tsx:522` calls `createNavigationGroups(…, true)` — isAdmin hardcoded — so every
administration entry becomes a palette command for all users, and the ai-usage entry this phase adds
makes it one worse. It gets ONE of two written answers: an in-phase fix, or a register row
`PALETTE-ADMIN-01` with an owner and the evidence line filed in the same commit as this table.
"Named in a wrinkle note" is neither.

**Intended-broken EXCLUSIONS (ACCEPTANCE condition 8):** `/delegations` (owner: Phase 102) and the
legal-holds region (owner: Phase 100). Their rows, if any, read "owned elsewhere, untouched" with
the owner named. Repairing, removing or re-homing them is a REJECT. Likewise the `/engagements`
double-mount is INTENTIONAL, and `scenario-sandbox` and `responsive-demo` were kept deliberately.

**Environment:** `command grep` only (plain `grep` is a ugrep wrapper honoring `.gitignore`);
unpiped, because piping into `head`/`wc` makes `$?` the pipe tail's exit; absolute paths; no
`timeout` binary on this machine.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: the nine route rows</name>
  <files>.planning/phases/97-reachability/97-NAV04-DECISIONS.md</files>
  <read_first>.planning/phases/97-reachability/97-POPULATIONS.md in full (97-03's measured inbound-link evidence and the stated delta); .planning/phases/95-routes-that-don-t-render/95-DEAD-04-DECISION.md in full (the record shape and the /monitoring input); .tickmarkr/overseer/RULING-P97-01-ADMIN-DISPOSAL.md in full; frontend/src/routes/_protected/admin/approvals.tsx and frontend/src/routes/_protected/approvals.tsx (the two surfaces the conditional-delete comparison must compare); frontend/src/routes/_protected/admin/preview-layouts.tsx (the ownership-claim check's subject)</read_first>
  <action>
    Write the document in the `95-DEAD-04-DECISION.md` shape. Open with the population: paste the
    mechanical admin derivation and the reconciliation sentence naming the −1 delta against the
    register's 9, and state that `/monitoring` makes nine rows.

    One row per route with these columns at minimum: route, decision, why, owner (where applicable),
    and the evidence citation. Every row's inbound-link evidence CITES `97-POPULATIONS.md` rather
    than re-deriving by hand — and every zero cited must have been produced in the same run as the
    `/admin/ai-settings` control.

    **THE DECISION VOCABULARY, corrected. Permitted decisions are exactly these SEVEN:**

    | term | meaning | who acts |
    | --- | --- | --- |
    | `NAV ENTRY` | no live nav entry today; one is to be ADDED | 97-10 adds it |
    | `ALREADY-REACHABLE` | a live nav entry ALREADY EXISTS; nothing to do | nobody — 97-10 adds NOTHING |
    | `DELETE` | the route/module is removed | 97-11 deletes it |
    | `OWN` | a dead module is KEPT with a named owner and a header comment | 97-11 annotates it |
    | `OWNED-ELSEWHERE-UNTOUCHED` | another phase owns it; this phase does not act | nobody |
    | `PARKED` | evidence underdetermines the disposal | nobody |
    | `PARKED-RE-ESCALATED` | a conditional trigger was REFUTED; back to the overseer with the comparison | overseer |
    | `NOT-CHECKED` | could not be established, with what was tried | nobody |

    **Two of those terms were MISSING from the earlier five-term set, and a missing term in a closed
    vocabulary does not merely fail to describe a state — it FORCES A WRONG DESCRIPTION.**

    - `ALREADY-REACHABLE` was absent, and FOUR of the nine candidates already have live sidebar rows
      today — measured at HEAD `9c32c4db1`: `/admin/ai-settings` (`navigation-config.ts:178`),
      `/admin/system` (`:184`), `/admin/field-permissions` (`:196`), `/admin/data-retention`
      (`:208`). With no term for them those four rows would have been forced into `NAV ENTRY`, and
      97-10 Task 1 reads this table as its instruction list: it would have added DUPLICATE sidebar
      rows for three of them. The candidate set is "the nine routes criterion 4 NAMES", which is NOT
      the same set as "the routes measured to have no inbound link" — 97-03 §2b derives the second,
      and every row must cite which set it is in.
    - `PARKED-RE-ESCALATED` was absent while this plan's own text already used it for a refuted
      conditional trigger. A term used in the action but missing from the permitted list is a
      vocabulary that cannot record its own instructions.

    **Each row ALSO emits a MACHINE-READABLE DECISION TOKEN on its own line, at column 0, in exactly
    this form and nothing else:**

    `DECISION <subject>: <TERM>`

    — for example `DECISION /admin/approvals: DELETE`. Emit one token line per subject for all eight
    loop candidates (the seven non-index admin paths plus `/monitoring`), for both dead modules by
    repo-relative path, and for `PALETTE-ADMIN-01` (whose terms are `BRANCH-A` or `BRANCH-B`; see
    Task 2). The token is the CONTRACT 97-10 and 97-11 read, and it exists because their gates
    previously matched `<subject>.*DELETE` against this prose document — a pattern that matches any
    line where the subject and the word DELETE merely co-occur, INCLUDING a row saying the opposite
    ("the CONDITIONAL DELETE trigger was REFUTED", "kept per DELETE-or-OWN review"). Both literals
    appear in this plan's own text, so the contamination was near-certain, and the arm it selects
    asserts the route is ABSENT — pressuring an unauthorised deletion on the phase's ONE irreversible
    operation. Consumers now match `^DECISION <subject>: <TERM>$`, anchored at both ends, which no
    prose sentence can satisfy. If you change the token's shape, repoint 97-10 Task 1, 97-10 Task 2
    and 97-11 Tasks 1 and 2 in the same edit (C9).

    Record the ruled rows as ruled, and do the two conditional triggers for real:
    - `/admin/approvals`: perform the component/feature-identity comparison against the top-level
      `/approvals` and PASTE it into the row. If it CONFIRMS duplication, the decision is DELETE with
      the comparison as its why. If it REFUTES duplication, the decision is PARKED-RE-ESCALATED with
      the comparison as evidence — do not improvise a nav entry, which the ruling forbids explicitly.
      Note in the row that the live nav's `admin-approvals` item at `navigation-config.ts:212` points
      at the TOP-LEVEL `/approvals`, a different route; a substring sweep conflates the two and would
      report this route as linked when it is not.
    - `/admin/preview-layouts`: check for a live ownership claim and record the check itself, not
      just its verdict. Confirmed-historical → DELETE; a live claim → OWNED-ELSEWHERE with the owner
      named and nothing deleted.

    For every row proposing a NAV ENTRY, name the destination's current state — including
    `/monitoring`'s production data-degradation and any admin page whose current render state you
    could not establish, which is a NOT-CHECKED note rather than an assumption. A route whose
    disposal is genuinely underdetermined by evidence is PARKED, not guessed.

    Add a short closing section listing the intended-broken exclusions by name with their owning
    phases, so a reader can see they were considered and deliberately untouched.

    **No row may state or imply that a nav entry, or its absence, provides access control.**
    `adminOnly` gating is client-side, the routes stay URL-reachable regardless, and server-side
    authorization is Phase 100's RLS territory.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && F="$R/.planning/phases/97-reachability/97-NAV04-DECISIONS.md" && test -f "$R/.planning/phases/97-reachability/97-POPULATIONS.md" && test -f "$F" && for p in /admin/ai-settings /admin/ai-usage /admin/approvals /admin/data-retention /admin/field-permissions /admin/preview-layouts /admin/system /monitoring; do command grep -q -- "$p" "$F" || exit 1; done && command grep -q 'delta' "$F" && command grep -q '95-DEAD-04-DECISION' "$F" && command grep -q '97-POPULATIONS' "$F" && command grep -q 'NOT-CHECKED' "$F" && command grep -q '/delegations' "$F" && command grep -q 'legal-hold' "$F" && command grep -q 'Phase 100' "$F" && command grep -q 'Phase 102' "$F" && CFG="$R/frontend/src/components/layout/navigation-config.ts" && test -f "$CFG" && git -C "$R" rev-parse -q --verify refs/tags/phase-97-base >/dev/null && CFGB="${TMPDIR:-/tmp}/p97-09-navcfg-base.ts" && git -C "$R" show phase-97-base:frontend/src/components/layout/navigation-config.ts > "$CFGB" && test -s "$CFGB" && LINKED=$(command grep -c "path: '/admin/" "$CFGB") && test "$LINKED" -ge 1 && for p in /admin/ai-settings /admin/ai-usage /admin/approvals /admin/data-retention /admin/field-permissions /admin/preview-layouts /admin/system /monitoring; do test "$(command grep -c "^DECISION $p: " "$F")" -eq 1 || exit 1; if command grep -q "path: '$p'" "$CFGB"; then command grep -q "^DECISION $p: \(ALREADY-REACHABLE\|DELETE\|PARKED\|PARKED-RE-ESCALATED\|NOT-CHECKED\|OWNED-ELSEWHERE-UNTOUCHED\)$" "$F" || exit 1; else command grep -q "^DECISION $p: \(NAV ENTRY\|DELETE\|PARKED\|PARKED-RE-ESCALATED\|NOT-CHECKED\|OWNED-ELSEWHERE-UNTOUCHED\)$" "$F" || exit 1; fi; done && TOK=$(command grep -c '^DECISION ' "$F") && test "$TOK" -ge 8 && test "$(command grep '^DECISION ' "$F" | command grep -vc ': \(NAV ENTRY\|ALREADY-REACHABLE\|DELETE\|OWN\|OWNED-ELSEWHERE-UNTOUCHED\|PARKED\|PARKED-RE-ESCALATED\|NOT-CHECKED\|BRANCH-A\|BRANCH-B\)$' || true)" -eq 0 && command grep -q 'ALREADY-REACHABLE' "$F"</automated>
  </verify>
  <acceptance_criteria>All eight admin paths and `/monitoring` appear, checked ONE AT A TIME so the gate names which is missing instead of reporting a count that hides it — that per-path loop is the C10 partner to criterion 4's word "every". The `/admin/` index is deliberately excluded from the loop because its literal is a prefix of every other admin path and would match vacuously; it is covered by the pinned `delta` sentence instead. Both prior records are cited by filename, `NOT-CHECKED` appears as a live vocabulary item, and both intended-broken exclusions appear with their owning phases (ACCEPTANCE condition 8). `97-POPULATIONS.md`'s existence is asserted first, since a table citing evidence that does not exist is worse than one that admits it has none (C5).

**The gate is TABLE-DRIVEN AGAINST THE LIVE CONFIG, in both directions, and that is what makes it non-vacuous.** For each of the eight loop candidates it requires EXACTLY ONE anchored `^DECISION <path>: <TERM>$` token — one, not "at least one", so a duplicated or contradictory row is red — and then branches on a fact it DERIVES at run time from `navigation-config.ts`: if the path already has a live nav row, its decision must be one of the terms that adds nothing (`ALREADY-REACHABLE`, `DELETE`, `PARKED`, `PARKED-RE-ESCALATED`, `NOT-CHECKED`, `OWNED-ELSEWHERE-UNTOUCHED`), and if it does not, its decision must be one of the terms that does not falsely claim reachability (`NAV ENTRY`, `DELETE`, `PARKED`, `PARKED-RE-ESCALATED`, `NOT-CHECKED`, `OWNED-ELSEWHERE-UNTOUCHED`). Both arms are POSITIVE alternations, never negations, so neither can pass vacuously — and neither forces a verdict: an already-linked route may still be ruled DELETE or PARKED, it simply may not be ruled NAV ENTRY, because that is the instruction that would make 97-10 add a DUPLICATE sidebar row. `LINKED` is asserted non-empty first as the instrument control on the config sweep (4 today), so a mistyped config path cannot send every candidate down the else-arm. The whole vocabulary is then closed: every `^DECISION ` line must end in one of the nine permitted terms, which catches an invented sixth term rather than letting it read as a decision.

**Note the shell hazard this gate hit and fixed during revision, because it is the W-6 class:** `BAD=$(… | grep -vc …)` assigns 0 correctly but EXITS 1 when the count is zero, so on a perfectly-formed table the `&&` chain died and the gate was unsatisfiable. It is written as `test "$( … || true)" -eq 0` — the count in argument position, where the substitution's exit status is not the chain's.

MAX REACHABLE: the eight per-candidate token counts are 0 today and reach exactly 1 each when the table is written; `TOK` reaches 8 (plus the module and palette tokens Task 2 adds); `BAD` reaches 0 when every term is permitted; `LINKED` is 4 today against a floor of 1.

GATE DRILL, six directions, all OBSERVED during plan revision: RED on the undone tree (exit 1, the file does not exist); GREEN on a constructed stub carrying every pinned token and a correct verdict per candidate (exit 0); and four constructed WRONG states each observed red — a linked path ruled `NAV ENTRY` (the duplicate-instruction defect), an unlinked orphan dismissed as `ALREADY-REACHABLE`, an invented term outside the vocabulary, and a candidate whose token line was omitted. NOTE the gate checks the record's COMPLETENESS and its INTERNAL CONSISTENCY WITH THE LIVE CONFIG, not the correctness of any judgement — the conditional triggers' verdicts remain the overseer's to grade, and this gate deliberately claims no more than it can check.</acceptance_criteria>
<done>Every candidate route is owned on paper before any of them is touched in code.</done>
</task>

<task type="auto">
  <name>Task 2: the two dead-module rows, and the palette's one written answer</name>
  <files>.planning/phases/97-reachability/97-NAV04-DECISIONS.md</files>
  <read_first>frontend/src/services/auth.ts (enough to confirm the second zustand store on the 'auth-storage' key around :624 and the module-level onAuthStateChange around :635); frontend/src/components/layout/QuickNavigationMenu.tsx in full (its route list — the row must compare it against the live config); frontend/src/components/layout/navigation-config.ts in full (the live list to compare against); frontend/src/components/keyboard-shortcuts/CommandPalette.tsx:500-540 (the hardcoded isAdmin at :522 and how createNavigationGroups is consumed at :104); frontend/src/components/layout/Sidebar.tsx:45-60 (the honest admin check, the analog for a branch-A fix); .tickmarkr/overseer/RULING-P97-03-OMISSION-FINDINGS.md</read_first>
  <action>
    Append the sub-item rows to the same single-writer document.

    **`frontend/src/services/auth.ts`** — DELETE-or-OWN with the why. RE-RUN the zero-importer
    derivation at execution and paste both halves: the unpiped `command grep` for its import
    specifier across `frontend/src`, and a known-imported positive control in the SAME run
    (`store/authStore'` is the right control shape) whose output must be NON-EMPTY. Read the exit
    code directly, never through a pipe. A zero without its control in the same run is not evidence.
    Recommend DELETE per `RULING-P92-06` if the derivation holds; 97-11 executes it.

    **`frontend/src/components/layout/QuickNavigationMenu.tsx`** — the same treatment, and one extra
    obligation from `RULING-P97-03` §1: if the decision is DELETE, the row must say **what its route
    list contained that `navigation-config.ts` does NOT**. Do the comparison and paste it. A parallel
    list is also a record of intent; deleting it silently discards whatever the live config forgot,
    and that difference may itself be a reachability finding this phase should own. Re-run its
    zero-importer derivation with a known-imported control of the same shape (the Sidebar import is
    the right control) — re-run it, do not quote a previously observed number.

    **The palette answer.** Elect ONE branch and write it into the table with its evidence:
    - **Branch A, fix in-phase** — justified when a plan in this set already owns the palette and nav
      wiring files. 97-10 owns both `CommandPalette.tsx` and `navigation-config.ts`, which is exactly
      the condition `RULING-P97-01` names, so branch A is the expected election: derive `isAdmin` in
      the palette the way `Sidebar.tsx:54` derives it and bind visibility to the real check. Record
      97-10 as the owner.
    - **Branch B, register row `PALETTE-ADMIN-01`** — if execution finds branch A is not a small
      honest fix (for instance the palette's render context cannot reach the auth store). Then the
      row carries the id, an owner, and the evidence line, and its register text is handed to 97-12,
      which is the phase's ONLY writer of `.planning/REQUIREMENTS.md` — a hand-off in the 95-04→95-09
      shape, so two plans never write one file.
    Both branches are fully specified here so execution never needs replanning. Whichever is
    elected, the row states plainly that the palette leak is a VISIBILITY defect and not an
    authorization one.

    **Emit the branch as a DECISION token, exactly `DECISION PALETTE-ADMIN-01: BRANCH-A` or
    `DECISION PALETTE-ADMIN-01: BRANCH-B`, on its own line at column 0 — one of the two, never
    neither and never both.** 97-10 Task 2's gate reads that line and cannot proceed without it. The
    earlier contract was an em-dash prose marker, `PALETTE-ADMIN-01 — branch B`, that 97-10's gate
    branched on and that THIS plan never instructed anyone to write: a token sweep across all twelve
    plans found the literal only inside 97-10's gate. If branch B were elected and the marker not
    written verbatim, 97-10's gate fell to its branch-A arm and demanded `useAuthStore` inside
    `CommandPalette.tsx` — which branch B explicitly forbids ("revert the attempt, leave the palette
    untouched") — making the gate unsatisfiable in the branch this plan itself sanctioned. The token
    replaces it, and 97-10 now asserts the token EXISTS before branching, so a missing decision is
    red rather than silently defaulting.

    **Emit DECISION tokens for the two dead modules too**, keyed by repo-relative path and taking
    `DELETE` or `OWN`: `DECISION frontend/src/services/auth.ts: DELETE|OWN` and
    `DECISION frontend/src/components/layout/QuickNavigationMenu.tsx: DELETE|OWN`. 97-11 Task 2
    previously matched `services/auth.ts.*DELETE` against this prose document — a pattern satisfied
    by a row reading "kept per DELETE-or-OWN review", whose selected arm then asserts the file is
    ABSENT and pressures an unauthorised deletion. The literal `DELETE-or-OWN` appears in this
    plan's own text, in `REQUIREMENTS.md` and in CONTEXT D-07, so the contamination was near-certain
    rather than hypothetical. Anchored tokens are immune to it.

    If 97-04's re-derivation reported any `PARALLEL-TRUTH-01` residue — a dossier-type list or
    `DossierType` declaration it could not re-point — add a row for it here with a named owner, in
    this table, never by editing the committed class record.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && F="$R/.planning/phases/97-reachability/97-NAV04-DECISIONS.md" && test -f "$F" && command grep -q 'services/auth.ts' "$F" && command grep -q 'QuickNavigationMenu' "$F" && command grep -q 'auth-storage' "$F" && command grep -q 'CommandPalette' "$F" && command grep -q 'PALETTE-ADMIN-01' "$F" && command grep -q 'store/authStore' "$F" && command grep -q 'control' "$F" && CTRL=$(command grep -rn "store/authStore'" "$R/frontend/src" --include='*.ts' --include='*.tsx') && test -n "$CTRL" && test "$(command grep -c '^DECISION PALETTE-ADMIN-01: \(BRANCH-A\|BRANCH-B\)$' "$F")" -eq 1 && test "$(command grep -c '^DECISION frontend/src/services/auth.ts: \(DELETE\|OWN\)$' "$F")" -eq 1 && test "$(command grep -c '^DECISION frontend/src/components/layout/QuickNavigationMenu.tsx: \(DELETE\|OWN\)$' "$F")" -eq 1</automated>
  </verify>
  <acceptance_criteria>Both dead modules have rows naming the specific parallel truth each carries (`auth-storage` for one, the route list for the other), the palette answer names `PALETTE-ADMIN-01` whichever branch was elected — so the id exists in the record either as the filed row or as the branch not taken — and the control specifier appears in the document, evidencing that the zeros were control-tested rather than merely observed. The gate ALSO runs the positive control itself and requires non-empty output: if that control ever comes back empty the instrument is broken and every zero in the document is void, which is precisely the failure mode a bare zero-check cannot see. The control's output is captured into a variable and tested for non-emptiness rather than piped, because piping would make the exit status belong to the pipe tail rather than to grep (watchers fail closed: condition AND exit code).

**Three anchored DECISION tokens are required, EXACTLY ONE of each, with a closed set of terms:** the palette branch (`BRANCH-A` or `BRANCH-B`) and one per dead module (`DELETE` or `OWN`). These are the machine-readable contract 97-10 Task 2 and 97-11 Task 2 consume. `-eq 1` rather than `-q` is deliberate: it catches a table that recorded BOTH branches, or both a DELETE and an OWN for one module, which a presence check would read as decided. MAX REACHABLE: each count is 0 today and reaches exactly 1 when the row is written; both terms in each alternation are legitimately elective, so no verdict is pre-frozen and the maximum is reachable on either branch.

GATE DRILL: RED observed on the undone tree (the rows do not exist); GREEN constructed in a scratch copy from a stub carrying all three tokens; and WRONG states drilled by writing both palette branches into the stub (count 2, red) and by writing a term outside each closed set (count 0, red). `git status --porcelain` identical before and after.</acceptance_criteria>
<done>The two parallel-truth modules and the palette defect each have a written answer with an owner.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                                  |
| ---------------------------------- | -------------------------------------------------------------------------------------------- |
| decision record → executing plans  | 97-10 and 97-11 act only on rows written here; an unrecorded deletion is forbidden           |
| dead module → live persisted state | `services/auth.ts` persists a SECOND store on the live `'auth-storage'` key if ever imported |

## STRIDE Threat Register

| Threat ID | Category    | Component                    | Disposition | Mitigation Plan                                                                                                               |
| --------- | ----------- | ---------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------- |
| T-97-25   | Tampering   | the dormant dual auth store  | mitigate    | the row resolves it DELETE-or-OWN; a module that would hijack the live persist key on first import is not left undecided      |
| T-97-26   | Elevation   | admin entries in the palette | mitigate    | branch A binds palette visibility to the real admin check; branch B files it with an owner — either way it stops being a note |
| T-97-27   | Repudiation | silent route removal         | mitigate    | no deletion without a row; both conditional triggers paste their comparison, and a refuted trigger re-escalates               |
| T-97-28   | Spoofing    | nav gating misread as authz  | mitigate    | an explicit sentence in the record: `adminOnly` is client-side and grants nothing; server-side authz is Phase 100's           |
| T-97-SC   | Tampering   | package installs             | accept      | zero installs this phase                                                                                                      |

</threat_model>

<verification>
- Both gates green.
- POPULATION DEFINITION: the nine candidate routes plus the two filed dead-module sub-items plus the
  palette filing order. OUTSIDE IT: every other route in the tree (their inbound-link state is
  measured in `97-POPULATIONS.md` but this requirement scopes to the nine), the intended-broken
  surfaces `/delegations` and the legal-holds region (owned by Phases 102 and 100, untouched), and
  the intentional shapes a reachability sweep must not "fix".
- This plan performs no deletion and adds no nav entry. If a row's evidence cannot be established,
  it is NOT-CHECKED or PARKED — never guessed, and never left blank.
</verification>

<success_criteria>
Nothing in the route tree is unowned: nine routes and two modules each carry a decision, a reason,
and an owner, and every deletion 97-11 performs has a row that authorised it.
</success_criteria>

<output>
Create `.planning/phases/97-reachability/97-09-SUMMARY.md` when done. It MUST record: the
execution-time admin population and the delta as re-derived on the day; the verdict of each
conditional trigger with the comparison that produced it; both zero-importer derivations with their
control output; which palette branch was elected and on what evidence; and any row that came out
NOT-CHECKED or PARKED, since those are the phase's honest edges and 97-12 must name them.
</output>
