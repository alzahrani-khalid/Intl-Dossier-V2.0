---
phase: 97-reachability
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/dossier-type-guards.ts # the ONE literal home + the derived card set + the compile-time anti-merge guard
  - frontend/src/lib/dossier-routes.ts # DOSSIER_TYPE_TO_ROUTE typed exhaustive over the card set
  - frontend/src/services/dossier-api.ts # :26 union aliases canonical; :702 bucket init derives the DB-7
  - frontend/src/types/dossier-context.types.ts # :71 union aliases canonical
  - frontend/src/types/relationship.types.ts # :46 union aliases canonical
  - frontend/src/components/intelligence/AlertRuleForm.tsx # :38 DB-7 list derives
  - frontend/src/routes/_protected.tsx # :20 card-8 whitelist derives
  - frontend/src/pages/dossiers/CreateDossierHub.tsx # :41-64 HubCardType + list derive
  - frontend/src/pages/dossiers/DossierListPage.tsx # :75 derives the DB-7 (widening to 8 is 97-05's, with the count fix)
autonomous: true
requirements: [NAV-01]
success_criterion: '1 (all 8 declared dossier types on all four surfaces) — this plan builds the single-homed type set criterion 1 is expressed in, and adds NO type to any surface.'
truths:
  - 'Decisions covered — D-03: this is the foundation of the Elected Officials work, and it deliberately does NOT expose the 8th type anywhere yet: it collapses the parallel type lists so that when 97-05 and 97-06 do expose it, there is one place to change and disagreement is impossible by construction rather than by authorship; D-02: the surfaces this phase does not own are named and untouched — no route, nav entry or copy changes here.'
must_haves:
  truths:
    - 'There is exactly ONE literal dossier-type list in frontend/src, and the card set is derived from it by spread'
    - 'Adding elected_official to the DB-7 constant fails the BUILD via a type assertion, not a comment'
    - 'Every previously hand-maintained type list and every duplicate DossierType union now resolves to the canonical home or is filed as PARALLEL-TRUTH-01 residue with an owner'
    - 'No surface gains or loses a dossier type in this plan — the CreateDossierHub 8-card order test still passes unchanged'
  artifacts:
    - path: 'frontend/src/lib/dossier-type-guards.ts'
      provides: 'DOSSIER_TYPES (7, literal), DossierType, DOSSIER_CARD_TYPES (8, derived), DossierCardType, and the AssertNever anti-merge guard'
      contains: 'DOSSIER_CARD_TYPES'
    - path: 'frontend/src/lib/dossier-routes.ts'
      provides: 'route-segment map made exhaustive over the card set by its type'
      contains: 'DossierCardType'
  key_links:
    - from: 'frontend/src/services/dossier-api.ts'
      to: 'frontend/src/lib/dossier-type-guards.ts'
      via: 'bucket initialisation imports the DB-7 constant instead of restating it'
      pattern: 'dossier-type-guards'
    - from: 'frontend/src/pages/dossiers/CreateDossierHub.tsx'
      to: 'frontend/src/lib/dossier-type-guards.ts'
      via: 'the hub card list imports the derived card set instead of restating it'
      pattern: 'DOSSIER_CARD_TYPES'
---

<objective>
Collapse the PARALLEL SOURCE OF TRUTH instance that `RULING-P97-04` identified and
`RULING-P97-05` re-prescribed, BEFORE any plan adds the eighth type to a surface. This ordering is
the point: if NAV-01 widened the surfaces first, it would add `elected_official` to several
hand-maintained lists and ship a fourth copy of the exact defect this phase diagnosed.

**The class, in one sentence:** a second copy of a truth that already has a canonical home, inert
or wrong until something reads it. Four instances are on the register
(`.planning/phases/97-reachability/97-PARALLEL-TRUTH-CLASS.md`, committed at `3330eac57` — read
it, do not recreate it); this plan closes the type-list one.

**There are TWO sets, and they must not be merged.** The 7 `dossiers.type` values are what the
database can hold; the 8-member card set is what the UI displays, because `elected_official` is
`persons.person_subtype` and not a dossier type at all. Merging them re-creates C-2 in a new
costume: a widened DB-side constant produces a bucket **no query can ever fill**, which renders a
fabricated `0`, which is the forbidden shape this phase exists to remove.

**The guard is the type system, not a comment.** `AppShell.tsx:121-124` is the in-repo precedent
for why: that comment was PRESENT and FALSE for the whole of its life, so a presence-pin protects
against deletion only, never against rot. The comments stay because they explain _why_ to a human,
but they are DOCUMENTARY — the compile-time assertion is the guarantee, and the acceptance criteria
below claim nothing else for them.

Purpose: one literal list plus one derived extension, so adding a ninth type is a one-place change.
Output: nine files re-pointed; zero behaviour change; the CreateDossierHub 8-card order test green
and untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-reachability/97-PARALLEL-TRUTH-CLASS.md (the committed class record — cite it, add rows only via 97-09's decision table)
@.tickmarkr/overseer/RULING-P97-04-PARALLEL-TRUTH-CLASS.md (why deriving is the default and the burden of proof is on not deriving)
@.planning/phases/97-reachability/97-PATTERNS.md (§S-1 local type-widening — the precedent this plan REPLACES with a derived set; §S-7 house style)
@frontend/src/pages/dossiers/CreateDossierHub.tsx (the 8-card order law at :51-64 and its file-header comment at :14-17)

<interfaces>
Verified on disk 2026-08-17 at HEAD `9c32c4db1`.

**FOUR `export type DossierType` declarations exist, all 7 members, three different orders** —
they agree by AUTHORSHIP, which is the class itself:
| Site | Order note |
| --- | --- |
| `lib/dossier-type-guards.ts:33` | CANONICAL HOME (named as such by `CreateDossierHub.tsx:14-17`); order differs from the others |
| `services/dossier-api.ts:26` | the union `DossierTypeStatsCard.tsx:35` actually imports |
| `types/dossier-context.types.ts:71` | — |
| `types/relationship.types.ts:46` | — |

**SEVEN runtime lists**, with the set each one legitimately wants:
| Site | Wants | Disposition in this plan |
| --- | --- | --- |
| `services/dossier-api.ts:702-711` | **DB-7** (bucket init for a query over `dossiers.type`) | derive `DOSSIER_TYPES` — NEVER widen |
| `components/intelligence/AlertRuleForm.tsx:38-46` | **DB-7** (`z.enum` source) | derive `DOSSIER_TYPES` |
| `pages/dossiers/DossierListPage.tsx:75-83` | DB-7 today, **card-8 after 97-05** | derive `DOSSIER_TYPES` here; 97-05 moves it to `DOSSIER_CARD_TYPES` together with the three-layer count fix |
| `pages/dossiers/CreateDossierHub.tsx:55-64` | **card-8** (already 8) | derive `DOSSIER_CARD_TYPES`; `HubCardType` becomes `DossierCardType` |
| `routes/_protected.tsx:20-30` | **card-8** (already 8, already correctly inverted locally) | derive `DOSSIER_CARD_TYPES` |
| `routes/_protected/compare.tsx:15-17` | DB-7 today, card-8 after 97-06 | **NOT this plan** — 97-06 owns it |
| `components/entity-comparison/EntityComparisonSelector.tsx:64-72` | icon map keyed by type | **NOT this plan** — 97-06 owns it |

**ORDER IS LOAD-BEARING.** `frontend/src/pages/dossiers/__tests__/CreateDossierHub.test.tsx:32`
pins "renders 8 dossier type cards in DOSSIER_TYPES enum order". The canonical literal must
therefore be written in the CreateDossierHub / D-02 order:
`country, organization, forum, engagement, topic, working_group, person` — which is also the order
already used by `dossier-api.ts:26`, `dossier-context.types.ts:71` and `_protected.tsx:20`.
`dossier-type-guards.ts:33`'s current order is DIFFERENT and is the one that changes; a union's
member order is semantically irrelevant, so this is safe, but write the array in the pinned order.

**The prescribed shape** (`RULING-P97-05`; `as const satisfies` is already the in-repo idiom at
`pages/engagements/EngagementsListPage.tsx:50` and `routes/_protected/kanban.tsx:22`):
`DOSSIER_TYPES` as a 7-member `as const` array; `DossierType = (typeof DOSSIER_TYPES)[number]`;
`DOSSIER_CARD_TYPES = [...DOSSIER_TYPES, 'elected_official'] as const`;
`DossierCardType = (typeof DOSSIER_CARD_TYPES)[number]`; and an `AssertNever<T extends never>`
alias applied to `Extract<DossierType, 'elected_official'>` so a merge becomes a TYPE ERROR.

**`lib/dossier-routes.ts`** already carries all 8 keys in `DOSSIER_TYPE_TO_ROUTE` (typed
`Record<string, string>`). Its file-header comment at `:10-11` ("elected_official is now a
person_subtype, all persons use /persons route") CONTRADICTS its own map at `:20` and the live
routes. The map is correct; the comment is stale. Fix the comment, never the map.

**Typecheck:** root `pnpm typecheck` → `turbo run type-check`; the frontend script is
`type-check` (NOT `typecheck`) when run from `frontend/` (GATE-STANDARD C3).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: the canonical home — one literal list, one derived card set, one compile-time anti-merge guard</name>
  <files>frontend/src/lib/dossier-type-guards.ts, frontend/src/lib/dossier-routes.ts, frontend/src/services/dossier-api.ts, frontend/src/types/dossier-context.types.ts, frontend/src/types/relationship.types.ts</files>
  <read_first>frontend/src/lib/dossier-type-guards.ts in full (the current union at :33, PersonSubtype, and every export the 34 importing files rely on); frontend/src/lib/dossier-routes.ts in full (the 8-key map at :12-36 and the stale header comment at :10-11); frontend/src/services/dossier-api.ts:20-40 (the duplicate union) and :674-745 (DossierTypeCount + the counts function); frontend/src/types/dossier-context.types.ts:60-80; frontend/src/types/relationship.types.ts:40-55; frontend/src/pages/engagements/EngagementsListPage.tsx:44-52 and frontend/src/routes/_protected/kanban.tsx:20-24 (the in-repo `as const satisfies` idiom)</read_first>
  <action>
    In `lib/dossier-type-guards.ts`, replace the hand-written union with the derived shape from the
    interfaces block: the 7-member `DOSSIER_TYPES` literal written in the PINNED order, `DossierType`
    derived from it, `DOSSIER_CARD_TYPES` built by spreading `DOSSIER_TYPES` and appending
    `'elected_official'`, `DossierCardType` derived from that, and the `AssertNever` alias applied to
    `Extract<DossierType, 'elected_official'>` under a name that says what it protects
    (`_EoIsNotADbType`). Keep every existing export in place — 34 files import from this module and
    none of their imports may change meaning. `PersonSubtype` stays exactly as it is.

    Put a short comment at EACH of the two declarations saying what set it is, that it must not be
    merged with the other, and what merging causes (an unfillable `dossiers.type` bucket → a
    fabricated `0` → the forbidden shape). Write these for the human reader and say in the code that
    the assertion below is the enforcement — do not imply the comment enforces anything.

    In `lib/dossier-routes.ts`, type `DOSSIER_TYPE_TO_ROUTE` as `Record<DossierCardType, string>` so
    a missing segment for any card type becomes a compile error rather than a runtime `undefined`.
    Do not change a single key or value. Correct the stale header comment at `:10-11` so it
    describes the map that actually exists — a comment that contradicts its own file is how the next
    reader "fixes" the map in the wrong direction.

    In `services/dossier-api.ts`, `types/dossier-context.types.ts` and `types/relationship.types.ts`,
    replace each duplicate `export type DossierType = …` union with an alias of the canonical import.
    These are structurally identical unions, so this is a no-op for every consumer; if any consumer
    does NOT typecheck afterwards, that consumer was relying on a divergence and the SUMMARY records
    which one and what diverged rather than papering over it with a cast.

    If the execution-time re-derivation of `export type DossierType =` across `frontend/src` finds a
    declaration this plan does not list, do not silently absorb it: re-point it if it is a plain
    alias, and otherwise hand its file and reason to 97-09 for a `PARALLEL-TRUTH-01` row with a named
    owner.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && G="$R/frontend/src/lib/dossier-type-guards.ts" && TSC="$R/frontend/node_modules/.bin/tsc" && test -f "$G" && test -x "$TSC" && test -d "$R/frontend/src" && command grep -q "^export const DOSSIER_CARD_TYPES = \[\.\.\.DOSSIER_TYPES, 'elected_official'\] as const$" "$G" && command grep -q "^\(export \)\?type _EoIsNotADbType = AssertNever<Extract<DossierType, 'elected_official'>>$" "$G" && N=$(sed -n "/export const DOSSIER_TYPES = \[/,/\] as const/p" "$G" | command grep -c "^\s*'") && test "$N" -eq 7 && U=$(command grep -rl '^export type DossierType =$' "$R/frontend/src" --include='*.ts' --include='*.tsx' | wc -l | tr -d ' ') && test "$U" -eq 0 && U1=$(command grep -rl "^export type DossierType = '" "$R/frontend/src" --include='*.ts' --include='*.tsx' | wc -l | tr -d ' ') && test "$U1" -eq 0 && H=$(command grep -rl '^export const DOSSIER_TYPES = \[' "$R/frontend/src" --include='*.ts' | wc -l | tr -d ' ') && test "$H" -eq 1 && command grep -q 'DossierCardType' "$R/frontend/src/lib/dossier-routes.ts" && S="${TMPDIR:-/tmp}/p97-04-guard-drill" && rm -rf "$S" && mkdir -p "$S" && cp "$G" "$S/merged.ts" && cp "$G" "$S/spread.ts" && MUT='const fs=require("fs");const q=String.fromCharCode(39);const p=process.argv[1];const del=process.argv[2]==="1";let s=fs.readFileSync(p,"utf8");const m=s.indexOf("export const DOSSIER_TYPES = [");if(m<0)process.exit(3);const i=s.indexOf("] as const",m);if(i<0)process.exit(3);s=s.slice(0,i)+"  "+q+"elected_official"+q+",\n"+s.slice(i);if(del){s=s.replace(/^(export )?type _EoIsNotADbType = .*$/m,"");s+="\nconst _p97CardCount: 9 = DOSSIER_CARD_TYPES.length\nvoid _p97CardCount\n"}fs.writeFileSync(p,s)' && node -e "$MUT" "$S/merged.ts" 0 && node -e "$MUT" "$S/spread.ts" 1 && test "$(sed -n "/export const DOSSIER_TYPES = \[/,/\] as const/p" "$S/merged.ts" | command grep -c "^\s*'")" -eq 8 && { OUT=$("$TSC" --noEmit --strict --pretty "$S/merged.ts" 2>&1); RC=$?; printf '%s\n' "$OUT" 1>&2; test "$RC" -ne 0; } && printf '%s' "$OUT" | command grep -q 'TS2344' && printf '%s' "$OUT" | command grep -q '_EoIsNotADbType' && "$TSC" --noEmit --strict --pretty "$S/spread.ts" 1>&2 && cd "$R" && pnpm typecheck 1>&2</automated>
  </verify>
  <acceptance_criteria>**This gate PERFORMS the wrong-thing mutation and observes the red; it does not read for a string that describes the guarantee.** `RULING-P97-06` §2 established by direct `tsc` run that the earlier form — `command grep -q 'AssertNever'` plus a typecheck of the UNMERGED tree — cannot distinguish a sound `AssertNever<T extends never>` from a NEUTERED `AssertNever<T>`: with the merge present the sound guard exits 2 with `TS2344` and the neutered one exits 0, CLEAN, while the old gate saw neither because it never merged anything. So the gate now copies the canonical module to `${TMPDIR}`, splices `'elected_official'` into `DOSSIER_TYPES` with `node` (asserting the member count moved 7 → 8 first, so a mutation that silently failed cannot be mistaken for a passing drill), runs the project's own `tsc --noEmit --strict --pretty` on that copy, and requires BOTH the literal `TS2344` and the literal `_EoIsNotADbType` in the captured output — `--pretty` echoes the offending source line, which is what makes the alias name appear at all, and the code is required because a botched splice produces `TS2552` instead and a bare non-zero exit would read it as success.

**The "derived by spread" claim is likewise checked BEHAVIOURALLY, not by substring.** A second hand-typed 8-member array named `DOSSIER_CARD_TYPES` would satisfy any presence grep while recreating the hand-maintained-duplicate class for the display set. So a SECOND scratch copy takes the same 7 → 8 merge, deletes the `_EoIsNotADbType` line, appends `const _p97CardCount: 9 = DOSSIER_CARD_TYPES.length`, and must compile CLEAN: a spread-derived set follows `DOSSIER_TYPES` to 9 and passes, a hand-typed one stays at 8 and fails `TS2322`. The exact-form anchor on the declaration line is kept as the cheap first guard, but the length probe is what makes the claim behavioural. Both drills were run against real copies of the real file during plan revision: sound guard + merge → exit 2 with `TS2344` naming `_EoIsNotADbType`; neutered guard + merge → exit 0 clean; spread-derived card set → clean; hand-typed card set → `TS2322: Type '8' is not assignable to type '9'`.

`DOSSIER_TYPES` contains exactly 7 members, counted from inside its own `as const` block by a sed range so the count is scoped to a structured field and cannot be contaminated by prose elsewhere in the file (C4). ZERO files still carry a HAND-WRITTEN `DossierType` union — checked in BOTH syntactic shapes now: `^export type DossierType =$` catches the multi-line form the four current sites use, and `^export type DossierType = '` catches a one-line re-typed duplicate, which the first anchor would have missed entirely. Exactly ONE file declares the literal `DOSSIER_TYPES` array. An earlier draft matched both union forms and demanded `-eq 1`: it counted 4 today and would still count 4 after the work, so its maximum achievable value never reached its threshold — an unsatisfiable oracle, caught by the C4 max-reachable check before execution.

MAX REACHABLE, clause by clause: `N` = 7 (the seven-member literal the action mandates) against `-eq 7`; `U` = 0 and `U1` = 0 (measured 4 and 0 today, both driven to 0 by re-pointing the four sites) against `-eq 0`; `H` = 1 (measured 0 today, becomes 1 when the canonical literal is written) against `-eq 1`; the merged-copy member count = 8 by construction against `-eq 8`. Every threshold is reachable and three of the five move during this task.

`pnpm typecheck` is green from the repo root, where the script is `typecheck`; the frontend-local script is `type-check` and the two are not interchangeable (C3). Its stdout is redirected to stderr (`1>&2`) so the drill instrument captures the compiler's own message rather than only an exit code — a coarse aggregate's non-zero exit proves only that SOMETHING failed (C2, W-11). GATE DRILL: RED observed on the undone tree (exit 1, verified during revision); GREEN observed on a CONSTRUCTED done state (exit 0, verified during revision against a scratch tree carrying the canonical block); plus two constructed WRONG states — neutered guard and hand-typed card array — each observed red. `git status --porcelain` is captured before and after and must be identical, per C1; every scratch artifact this gate writes lives under `${TMPDIR}`, never in the repo.</acceptance_criteria>
<done>One literal type list exists; merging the two sets breaks the build.</done>
</task>

<task type="auto">
  <name>Task 2: re-point the four remaining hand-maintained lists</name>
  <files>frontend/src/services/dossier-api.ts, frontend/src/components/intelligence/AlertRuleForm.tsx, frontend/src/routes/_protected.tsx, frontend/src/pages/dossiers/CreateDossierHub.tsx, frontend/src/pages/dossiers/DossierListPage.tsx</files>
  <read_first>frontend/src/services/dossier-api.ts:686-742 in full (the counts function whose throw-on-error contract must survive untouched); frontend/src/components/intelligence/AlertRuleForm.tsx:30-60 (the z.enum source and its DossierType import); frontend/src/routes/_protected.tsx:14-40 (the deep-link whitelist and its local inversion); frontend/src/pages/dossiers/CreateDossierHub.tsx:1-70 (HubCardType, the order law, the file-header comment); frontend/src/pages/dossiers/DossierListPage.tsx:70-90 (the list) and :448-470 (its first consumer); frontend/src/pages/dossiers/__tests__/CreateDossierHub.test.tsx (the order pin this must not break)</read_first>
  <action>
    Four mechanical re-points, each deleting a literal list and importing the right constant. Nothing
    else in any of these files changes.

    (a) `dossier-api.ts:702-711` — the bucket initialiser takes `DOSSIER_TYPES`. It is a query over
    `dossiers.type`, so it wants the DB-7 and must never be widened; leave a one-line comment saying
    so and pointing at the guard. The function's `throw` on PostgREST error is load-bearing
    (`useDossier.ts:679-686` records why: catching and returning zeros made a failed request
    indistinguishable from an empty database) — do not touch it, do not add a catch, do not add a
    default.

    (b) `AlertRuleForm.tsx:38-46` — the `z.enum` source takes `DOSSIER_TYPES`. `z.enum` needs a
    readonly non-empty tuple, which the `as const` array already is; if the compiler disagrees, the
    fix is a local `satisfies`, never a cast to `any` and never a second copy.

    (c) `_protected.tsx:20-30` — the quick-look deep-link whitelist takes `DOSSIER_CARD_TYPES` and
    its local `ValidDossierType` becomes `DossierCardType`. Its membership-test guard function stays
    exactly as written; this is the auth-gate layout route, so the diff is the list and nothing else.

    (d) `CreateDossierHub.tsx:41-64` — `HubCardType` becomes `DossierCardType` and the local
    `DOSSIER_TYPES` array becomes `DOSSIER_CARD_TYPES`. The 8 cards and their ORDER must be
    byte-identical in effect; run the pinning unit test and paste its output. Update the file-header
    comment at `:14-17`: it currently teaches "widen locally rather than touching the canonical
    domain type", which was right before this plan and is now the thing that produced four copies —
    replace it with a pointer to the canonical home and the derived card set.

    (e) `DossierListPage.tsx:75-83` takes `DOSSIER_TYPES` — the DB-7, unchanged behaviour. It does
    NOT take the card set here. Widening this page to 8 is 97-05's task and must happen in the same
    change as the three-layer count fix; widening it now would render the eighth card with a
    fabricated `0` for as long as the two plans are apart.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && A="$R/frontend/src/services/dossier-api.ts" && B="$R/frontend/src/components/intelligence/AlertRuleForm.tsx" && C="$R/frontend/src/routes/_protected.tsx" && D="$R/frontend/src/pages/dossiers/CreateDossierHub.tsx" && E="$R/frontend/src/pages/dossiers/DossierListPage.tsx" && for f in "$A" "$B" "$C" "$D" "$E"; do test -f "$f" || exit 1; command grep -q 'dossier-type-guards' "$f" || exit 1; done && test "$(command grep -c "^  const types: DossierType\[\] = \[$" "$A")" -eq 0 && command grep -q 'DOSSIER_TYPES' "$A" && test "$(command grep -c "^const VALID_DOSSIER_TYPES = \[$" "$C")" -eq 0 && command grep -q 'DOSSIER_CARD_TYPES' "$C" && command grep -q 'DOSSIER_CARD_TYPES' "$D" && command grep -q 'DOSSIER_TYPES' "$B" && command grep -q 'DOSSIER_TYPES' "$E" && cd "$R" && pnpm --filter intake-frontend exec vitest run src/pages/dossiers/__tests__/CreateDossierHub.test.tsx 1>&2</automated>
  </verify>
  <acceptance_criteria>All five files import from the canonical module. **The two literal arrays whose removal is mechanically checkable are pinned by a WHOLE-LINE DECLARATION ANCHOR, not by a sed range.** The earlier form counted quoted members inside `sed -n '/const types: DossierType\[\] = \[/,/^  \]/p'` and asserted `-eq 0`, which W-2 flagged as a range-scoped absence check with no non-empty precondition. The obvious repair — prefixing a `wc -l … -gt 3` non-empty guard — would have made BOTH of these gates PERMANENTLY UNSATISFIABLE, because the done state DELETES the declaration those ranges anchor on: the range legitimately becomes empty when the work is correct. So the assertion is inverted into the honest form instead — `command grep -c "^  const types: DossierType\[\] = \[$"` and `^const VALID_DOSSIER_TYPES = \[$`, whole-line anchors that a comment cannot satisfy (C8) and that go to 0 exactly when the literal is gone — each paired with a POSITIVE assertion that the file now consumes the shared constant, so an absence and a presence must both hold and a deleted-but-not-re-pointed file fails. `_protected.tsx` and `CreateDossierHub.tsx` reference the card set by name; `AlertRuleForm.tsx` and `DossierListPage.tsx` reference the DB-7 constant by name. MAX REACHABLE: both `-eq 0` clauses measure 1 today and reach 0 by deleting one declaration line each; the five presence clauses measure 0 today and reach 1 each. The CreateDossierHub 8-card ORDER test passes unchanged, which is the behaviour-preservation proof for the riskiest re-point, and its reporter output goes to stderr so the drill instrument captures the attribution rather than an exit code alone (W-11). GATE DRILL: RED is observed on the undone tree for the right reason — today the `dossier-type-guards` import is absent from at least `dossier-api.ts`, so the loop exits at its first file; GREEN is constructed in a scratch copy by performing the five re-points and observing exit 0. The vitest half runs in both directions in that same scratch copy; capture the test's own reporter output as the attribution, not the exit code alone. `git status --porcelain` identical before and after.</acceptance_criteria>
  <done>Six copies of the type set have become one literal list plus one derived extension.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                                                                     |
| ------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| client type set → PostgREST     | the DB-7 constant bounds what the counts query can bucket; widening it would query a value the DB CHECK rejects |
| deep-link search param → drawer | `_protected.tsx`'s whitelist is a membership test over the card set; it stays a whitelist, never a cast-through |

## STRIDE Threat Register

| Threat ID | Category  | Component                             | Disposition | Mitigation Plan                                                                                                          |
| --------- | --------- | ------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------ |
| T-97-09   | Tampering | `_protected.tsx` deep-link validation | mitigate    | membership test preserved verbatim (ASVS V5); the only change is where the member list comes from                        |
| T-97-10   | Tampering | type-set merge by a later author      | mitigate    | `AssertNever<Extract<DossierType, 'elected_official'>>` makes the merge a build failure; the comment is documentary only |
| T-97-11   | Spoofing  | nav/type visibility as authorization  | accept      | documentation duty — no type list gates access; server-side authz is Phase 100's RLS territory (RESEARCH §Security V4)   |
| T-97-SC   | Tampering | package installs                      | accept      | zero installs this phase (RESEARCH §Package Legitimacy Audit)                                                            |

</threat_model>

<verification>
- Both gates green; `pnpm typecheck` green from the repo root.
- The CreateDossierHub order test green and byte-unchanged.
- POPULATION DEFINITION: this plan covers every `export type DossierType` declaration and every
  hand-maintained dossier-type ARRAY in `frontend/src`, derived by
  `command grep -rn "export type DossierType =" frontend/src` and by the seven-site table in the
  interfaces block. OUTSIDE IT: `compare.tsx` and `EntityComparisonSelector.tsx` (97-06 owns them,
  and they must consume the same constants — C9), `lib/dossier-routes.ts`'s route-segment MAP
  (a segment map is not a type list and is correct to differ; this plan only makes its key set
  exhaustive by type), the backend and `shared/` workspaces, and any type list outside
  `frontend/src`.
- If execution finds a copy the table does not name, it is handed to 97-09 as a
  `PARALLEL-TRUTH-01` row with an owner — never absorbed silently.
</verification>

<success_criteria>
Adding a ninth dossier type is a one-place change, and merging the DB set with the card set fails
the build rather than shipping a bucket no query can fill.
</success_criteria>

<output>
Create `.planning/phases/97-reachability/97-04-SUMMARY.md` when done. It MUST record: the
execution-time re-derivation of `export type DossierType =` across `frontend/src` with its count
before and after; the captured compiler message from the anti-merge drill, quoted, naming
`_EoIsNotADbType` and its file:line; the CreateDossierHub order-test output; any consumer that
failed to typecheck against the aliased unions and what divergence it was relying on; and whether
any `PARALLEL-TRUTH-01` residue was found (if none, say so — an empty residue stated is evidence,
an unstated one is silence).
</output>
