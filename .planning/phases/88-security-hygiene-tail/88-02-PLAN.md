---
phase: 88-security-hygiene-tail
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .archive/implementation-reports/FRONT_DOOR_INTAKE_FINAL_STATUS.md
  - specs/020-complete-the-development/contracts/auth-api.yaml
  - specs/021-apply-gusto-design/quickstart.md
  - docker/anythingllm/DEBUGGING_AUTO_GENERATION.md
  - frontend/tests/e2e/global-setup.ts
autonomous: false
requirements: [SEC-02]
routing:
  pin:
    via: codex
    model: gpt-5.5

must_haves:
  truths:
    - 'No tracked file contains the real TEST_USER_PASSWORD value (old or new) — verified by git grep against the live value, exit non-zero'
    - "The staging test account's password is rotated, so the value retained in git history is dead"
    - 'Test suites and browser-automation flows still authenticate after externalization — login smoke exits 0 (phase success criterion 3, HARD gate)'
  artifacts:
    - path: 'specs/020-complete-the-development/contracts/auth-api.yaml'
      provides: 'Password literal replaced with an env-var reference'
    - path: '.archive/implementation-reports/FRONT_DOOR_INTAKE_FINAL_STATUS.md'
      provides: 'Password literal redacted'
    - path: 'specs/021-apply-gusto-design/quickstart.md'
      provides: 'Password literal redacted'
  key_links:
    - from: 'frontend/tests/e2e/global-setup.ts'
      to: '.env.test'
      via: 'process.env.TEST_USER_PASSWORD loaded by playwright.config.ts dotenv'
      pattern: 'TEST_USER_PASSWORD'
---

<objective>
Close SEC-02: the live test-account password VALUE sits in 3 tracked files (plus the account email in a 4th). Templates, gitignore, CI secret mapping, and env-var reads are already compliant (verified in RESEARCH) — the work is: redact the tracked values, ROTATE the account password (git history retains the old value, so scrubbing alone is insufficient), update `.env.test` + the GH Actions secret, and prove login paths still work.

Purpose: a credential in VCS is disclosed forever via history; rotation makes the historical value inert.
Output: 4 redacted tracked files, rotated staging credential, updated GH secret (operator-confirmed), green login smoke.

SECRET-HANDLING RULE (non-negotiable): the actual password value must NEVER appear in any command output, commit message, plan text, or log. Reference it only as `<the TEST_USER_PASSWORD value>` sourced from the gitignored `.env.test`. Grep checks read the value from env into a shell variable and pipe counts only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/88-security-hygiene-tail/88-RESEARCH.md
@.env.test.example

Verified facts from RESEARCH (do not re-derive):

- `.env.test` / `backend/.env.test` are NOT git-tracked; `.gitignore:24` covers `.env.*` with `!.env.*.example`
- All ~40 spec/test files already read `process.env.TEST_USER_PASSWORD`
- CI maps `secrets.E2E_ANALYST_PASSWORD` → `TEST_USER_PASSWORD` (ci.yml:245 et al); e2e.yml maps E2E_ADMIN/ANALYST/INTAKE
- Leak inventory (by value-grep): password in `.archive/implementation-reports/FRONT_DOOR_INTAKE_FINAL_STATUS.md`, `specs/020-complete-the-development/contracts/auth-api.yaml`, `specs/021-apply-gusto-design/quickstart.md`; email additionally in `docker/anythingllm/DEBUGGING_AUTO_GENERATION.md`
- `frontend/playwright.config.ts:39` globalSetup = `./tests/e2e/global-setup.ts`; config loads root `.env.test` via dotenv
- Rotation path: Supabase staging project `zkrcjzdemdmwhearhfgg`, `auth.admin.updateUserById(id, { password })` with the service-role key from `backend/.env`
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Redact the leaked values from the 4 tracked files</name>
  <files>.archive/implementation-reports/FRONT_DOOR_INTAKE_FINAL_STATUS.md, specs/020-complete-the-development/contracts/auth-api.yaml, specs/021-apply-gusto-design/quickstart.md, docker/anythingllm/DEBUGGING_AUTO_GENERATION.md</files>
  <read_first>The 4 files being edited (locate the literal occurrences by grepping for the value read from `.env.test` into a shell variable — never echo it), .env.test.example (the placeholder convention to mirror)</read_first>
  <action>In each file, replace every occurrence of the literal password with `$TEST_USER_PASSWORD` (yaml/md contexts: `<see .env.test — TEST_USER_PASSWORD>` where a shell-style var reads oddly) and every occurrence of the literal test email with `$TEST_USER_EMAIL`. Locate occurrences with `V=$(grep '^TEST_USER_PASSWORD=' .env.test | cut -d= -f2-); git grep -lF "$V"` — pipe file lists only, never the matched line content. Change nothing else in these historical docs (surgical-changes rule). Commit with a message that names the files, not the values.</action>
  <verify>
    <automated>V=$(grep '^TEST_USER_PASSWORD=' .env.test | cut -d= -f2-); test "$(git grep -clF "$V" | wc -l)" -eq 0 && E=$(grep '^TEST_USER_EMAIL=' .env.test | cut -d= -f2-); test "$(git grep -clF "$E" -- ':!*.example' | wc -l)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `V=$(grep '^TEST_USER_PASSWORD=' .env.test | cut -d= -f2-); git grep -clF "$V" | wc -l` returns 0 (no tracked file contains the password value)
    - `E=$(grep '^TEST_USER_EMAIL=' .env.test | cut -d= -f2-); git grep -clF "$E" | wc -l` returns 0 or lists only `.example`/docs files that reference it as an identifier placeholder — the 4 named files no longer contain it
    - `git diff --stat HEAD~1` touches exactly the 4 named files
    - No command output in the transcript contains the secret value
  </acceptance_criteria>
  <done>Working tree is clean of the live credential value; only env-var references remain in the 4 files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Rotate the staging password + update .env.test and GH Actions secret</name>
  <files>frontend/tests/e2e/global-setup.ts</files>
  <read_first>.planning/phases/88-security-hygiene-tail/88-RESEARCH.md (§SEC-02 rotation sequencing + Assumption A1), .github/workflows/ci.yml (lines ~244-245, secret mapping), backend/.env (service-role key location — do not print it)</read_first>
  <what-built>Working tree scrubbed of the credential (Task 1). Rotation is prepared: a one-off local script (written to the scratchpad, NOT the repo) that calls `supabaseAdmin.auth.admin.updateUserById(<user-id>, { password: <new value> })` against staging project zkrcjzdemdmwhearhfgg using the service-role key from backend/.env, generating a new random password and writing it into the gitignored root `.env.test` and `backend/.env.test` (TEST_USER_PASSWORD lines). The script prints ONLY a success/failure status, never the value.</what-built>
  <how-to-verify>
    Operator steps (ordering matters — Pitfall 2 in RESEARCH: rotate → local env → GH secret → smoke):
    1. Approve running the rotation script (or rotate via the Supabase dashboard yourself and update both `.env.test` files manually).
    2. Confirm which GH Actions secrets are backed by this account — RESEARCH Assumption A1: likely `E2E_ANALYST_PASSWORD`, possibly `E2E_ADMIN_PASSWORD`. The repo cannot read secret contents; only you know the mapping.
    3. Update the confirmed secret(s): `gh secret set E2E_ANALYST_PASSWORD` (paste the new value at the prompt — do not pass it as an argv).
    4. Decision item (RESEARCH Assumption A2): the hardcoded `Test123!@#` / `TestPassword123!@#OrgA/B` fixture passwords in 8 old e2e specs + tests/security/org-isolation.test.ts — confirm they are dead spec-kit fixtures to be recorded for Phase 89's e2e burn-down (default), or direct rotation now.
  </how-to-verify>
  <acceptance_criteria>
    - Rotation script (or dashboard action) reported success; old password no longer authenticates (a login attempt with the old value returns 400/invalid — script asserts this, printing status only)
    - Root `.env.test` and `backend/.env.test` carry the new value (verified by the Task 3 smoke, not by printing)
    - Operator confirmed GH secret(s) updated, or explicitly confirmed no GH secret holds this account's password
    - A2 decision recorded in the SUMMARY (fixture passwords: deferred to Phase 89 or rotated)
  </acceptance_criteria>
  <resume-signal>Type "rotated" once the Supabase password, local .env.test files, and GH secret(s) are updated — then Task 3 runs the smoke.</resume-signal>
  <action>Automate first, checkpoint after: write the one-off rotation script in the scratchpad (never the repo) using the service-role key from backend/.env and auth.admin.updateUserById against staging zkrcjzdemdmwhearhfgg; generate a new random password; update the gitignored root .env.test and backend/.env.test TEST_USER_PASSWORD lines; assert the OLD value no longer authenticates. Print status only — never the value. Then pause for the operator steps in how-to-verify (GH secret mapping is operator-only knowledge, RESEARCH A1).</action>
  <verify>
    <automated>MISSING — operator-gated: GH secret contents are not repo-readable; proof lands in Task 3's login smoke</automated>
  </verify>
  <done>Staging password rotated; old value dead (login attempt fails); both .env.test files updated; operator confirmed GH secret(s) updated or not applicable; A2 fixture decision recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Login-path verification smoke (HARD gate — phase success criterion 3)</name>
  <files>frontend/tests/e2e/global-setup.ts</files>
  <read_first>frontend/tests/e2e/global-setup.ts, frontend/playwright.config.ts (dotenv load of root .env.test, globalSetup wiring)</read_first>
  <action>No code change expected — this task PROVES rotation didn't break auth. Run one login-gated Playwright spec (dashboard.spec.ts) which exercises global-setup's authenticated storage state from the rotated `.env.test` credentials. If global-setup fails auth, fix ONLY the env plumbing (never hardcode a value). Then re-run the tracked-file grep against the NEW value to prove it wasn't accidentally committed anywhere.</action>
  <verify>
    <automated>cd frontend && pnpm exec playwright test tests/e2e/dashboard.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && pnpm exec playwright test tests/e2e/dashboard.spec.ts --project=chromium` exits 0 (global-setup authenticated with the rotated credential)
    - `V=$(grep '^TEST_USER_PASSWORD=' .env.test | cut -d= -f2-); git grep -clF "$V" | wc -l` returns 0 (NEW value absent from tracked files)
    - No secret value printed anywhere in the run
  </acceptance_criteria>
  <done>Browser-automation login works end-to-end with the rotated, externalized credential; neither old nor new value exists in tracked files.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                             | Description                                                             |
| ---------------------------------------------------- | ----------------------------------------------------------------------- |
| Git repo (public-ish VCS) → anyone with clone access | Tracked files + full history disclose the credential                    |
| CI runner → staging Supabase                         | GH Actions secrets must match the rotated credential or e2e auth breaks |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                              | Disposition  | Mitigation Plan                                                                  |
| --------- | ---------------------- | -------------------------------------------------------------------------------------- | ------------ | -------------------------------------------------------------------------------- |
| T-88-05   | Information Disclosure | Password literal in 3 tracked files + git history                                      | mitigate     | Redact working tree (Task 1) + rotate so the historical value is dead (Task 2)   |
| T-88-06   | Spoofing               | Anyone holding the leaked value can authenticate as the test account                   | mitigate     | Rotation invalidates the value; old-value login asserted to fail                 |
| T-88-07   | DoS                    | Broken-auth regression: rotation without env/CI update 401s every e2e and browser flow | mitigate     | Sequenced checkpoint (rotate → .env.test → GH secret) + hard-gate smoke (Task 3) |
| T-88-08   | Information Disclosure | Old fixture passwords (`Test123!@#` class) in dead specs                               | accept/defer | Operator decision at checkpoint (A2); default: record for Phase 89 burn-down     |

</threat_model>

<verification>
- Both grep assertions (old value AND new value absent from tracked files) return 0
- Playwright dashboard spec green via global-setup auth
- SUMMARY records the GH-secret confirmation and the A2 fixture decision — without ever naming values
</verification>

<success_criteria>

- Phase success criterion 2: no real secrets in tracked files; TEST_USER_PASSWORD-class value rotated and externalized
- Phase success criterion 3: test suites and browser-automation flows still authenticate (login smoke exits 0)
  </success_criteria>

<output>
Create `.planning/phases/88-security-hygiene-tail/88-02-SUMMARY.md` when done
</output>
