---
phase: 53-bundle-tightening-tag-provenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/.size-limit.json
  - frontend/docs/bundle-budget.md
autonomous: true
requirements:
  - BUNDLE-05
tags:
  - bundle
  - size-limit

must_haves:
  truths:
    - '`pnpm -C frontend size-limit` exits 0 against the lowered React vendor ceiling'
    - '`node frontend/scripts/assert-size-limit-matches.mjs` exits 0 after the React vendor glob is unchanged'
    - '`frontend/docs/bundle-budget.md` React vendor row shows the 2026-05-16 audit date, the fresh gz measurement, the new ~285 KB ceiling, and the D-03 rationale prose'
    - 'D-01: ceiling = ceil(measured + 5 KB) computed from the fresh measurement (≈285 KB gz when measured = 279.92)'
    - 'D-02: fresh `ANALYZE=true pnpm build` measurement taken on phase-53-base before locking the ceiling; only the React vendor row is retuned'
    - 'D-03: React vendor row in `frontend/docs/bundle-budget.md` is replaced (not appended) with Phase 53 audit date + rationale'
    - 'D-04: no separate audit artifact is added under `.planning/phases/53-*/`; the inline `bundle-budget.md` row update is the canonical evidence'
  artifacts:
    - path: 'frontend/.size-limit.json'
      provides: 'Tightened React vendor ceiling per Phase 49 D-03 min rule'
      contains: '"React vendor"'
    - path: 'frontend/docs/bundle-budget.md'
      provides: 'Updated React vendor rationale row (replaced, not appended) reflecting Phase 53 measurement'
      contains: 'React vendor'
  key_links:
    - from: 'frontend/.size-limit.json'
      to: 'dist/assets/react-vendor-*.js'
      via: 'size-limit glob match'
      pattern: 'react-vendor-\*\.js'
    - from: 'frontend/docs/bundle-budget.md'
      to: 'frontend/.size-limit.json'
      via: 'Ceiling value byte-match between doc row and JSON limit field'
      pattern: 'React vendor'
---

<objective>
Apply the Phase 49 D-03 rebaseline rule (`ceiling = min(current, measured + 5 KB)`) to the React vendor chunk so the `frontend/.size-limit.json` ceiling stops carrying the 69 KB Phase 49 deferred slack. Take a fresh `ANALYZE=true pnpm build` measurement on phase-53-base (post-`tunnel-rat` removal), compute the new ceiling, edit the one row in `.size-limit.json`, and replace the React vendor row in `frontend/docs/bundle-budget.md` with current numbers + rationale.

Purpose: BUNDLE-05 — tighten the only chunk that Phase 49 deliberately left loose, without touching any other ceiling or chunk strategy.
Output: One-field edit to `.size-limit.json`, one-row replacement in `bundle-budget.md`, both audited green by `pnpm size-limit` and the matching sanity script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/53-bundle-tightening-tag-provenance/53-CONTEXT.md
@.planning/phases/49-bundle-budget-reset/49-CONTEXT.md
@frontend/.size-limit.json
@frontend/docs/bundle-budget.md
@frontend/scripts/assert-size-limit-matches.mjs
@CLAUDE.md

<interfaces>
<!-- Current React vendor row (to be replaced verbatim, not appended) -->
<!-- File: frontend/docs/bundle-budget.md, row in the "## Ceilings" table -->

| React vendor | 279.92 kB | 349 kB | react + react-dom + scheduler. Plan 02 ordering fix unblocked tree-shaking: react-vendor dropped from 347.13 kB → 279.92 kB after `@heroui` / `@sentry` / `@dnd-kit` / `@radix-ui` / `@tanstack` paths stopped leaking through the `id.includes('react')` rule. Ceiling held at 349 kB per D-03 `min(current, measured + 5 kB)`; not lowered to keep the Plan-02 diff surgical. | 2026-05-12 |

<!-- Current React vendor entry in frontend/.size-limit.json (indices 8-15 of the array) -->

{
"name": "React vendor",
"path": "dist/assets/react-vendor-\*.js",
"limit": "349 KB",
"gzip": true,
"running": false
}

<!-- D-03 rule (Phase 49 CONTEXT): ceiling = ceil(measured + 5 KB) gz, rounded up to the next whole KB -->
<!-- D-01 expected ceiling: ceil(279.92 + 5) = 285 KB; recompute if D-02 fresh measurement diverges -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh ANALYZE measurement of react-vendor gz</name>
  <files>
    (no edits — measurement only; dist/ is build output, not source)
  </files>
  <read_first>
    - .planning/phases/53-bundle-tightening-tag-provenance/53-CONTEXT.md (D-02: take fresh measurement on phase-53-base)
    - .planning/phases/49-bundle-budget-reset/49-CONTEXT.md (D-03: `min(current, measured + 5 KB)` rule)
    - frontend/docs/bundle-budget.md (current 279.92 KB baseline row)
    - frontend/.size-limit.json (current "React vendor" entry)
  </read_first>
  <action>
    Run `ANALYZE=true pnpm -C frontend build` from the repo root to produce a fresh production build with the rollup visualizer enabled. After the build completes, list the React vendor chunk and capture its gzipped size:

    1. Run: `ANALYZE=true pnpm -C frontend build`
    2. Identify the React vendor chunk file: `ls -la frontend/dist/assets/react-vendor-*.js`
    3. Measure gz size: `gzip -c frontend/dist/assets/react-vendor-*.js | wc -c` (bytes) — divide by 1024 for KB.
    4. Cross-check by running `pnpm -C frontend size-limit --json` and reading the `size` field for the "React vendor" row (this is the canonical measurement size-limit will compare against the ceiling).
    5. Record both numbers (gzip pipeline + size-limit JSON) in a scratch note for use in Tasks 2 and 3. They should agree within rounding.

    Do NOT edit any file in this task. Do NOT touch `vite.config.ts` or any other `.size-limit.json` row (per CONTEXT.md D-02 last sentence: "Do not retune any other chunks — that's scope creep beyond BUNDLE-05"). If the measured value diverges materially from 279.92 KB (>3 KB shift), note the cause in the scratch note (likely a transitive dep moved between chunks; cross-reference `.planning/codebase/STACK.md` if non-obvious) but proceed — the new measurement is authoritative for the D-01 recomputation.

  </action>
  <verify>
    <automated>test -f frontend/dist/assets/react-vendor-*.js &amp;&amp; pnpm -C frontend size-limit --json | node -e "const d=JSON.parse(require('fs').readFileSync(0,'utf8'));const r=d.find(x=>x.name==='React vendor');if(!r){process.exit(1)}console.log('react-vendor size:',r.size,'bytes ('+(r.size/1024).toFixed(2)+' KB gz)')"</automated>
  </verify>
  <acceptance_criteria>
    - Production build completes with `ANALYZE=true` (visualizer HTML produced)
    - `frontend/dist/assets/react-vendor-*.js` exists
    - `pnpm -C frontend size-limit --json` returns a numeric `size` field for the "React vendor" entry
    - The measured value is recorded for use in Tasks 2 + 3
  </acceptance_criteria>
  <done>
    Fresh gz measurement of react-vendor captured (in KB), available for the D-01 computation in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Lower React vendor ceiling in frontend/.size-limit.json</name>
  <files>frontend/.size-limit.json</files>
  <read_first>
    - frontend/.size-limit.json (current "React vendor" entry — `"limit": "349 KB"`)
    - .planning/phases/53-bundle-tightening-tag-provenance/53-CONTEXT.md (D-01: ceiling = ceil(measured + 5), expected 285 KB)
    - .planning/phases/49-bundle-budget-reset/49-CONTEXT.md (D-14: zero net-new ceiling raises — this plan is a lowering, honors the policy)
  </read_first>
  <action>
    Compute the new ceiling per Phase 49 D-03 (applied verbatim in Phase 53 D-01):

    `new_ceiling_kb = ceil(measured_kb_from_task_1 + 5)`

    With the 2026-05-12 baseline of 279.92 KB this yields 285 KB. If Task 1 reported a different value, recompute honestly — e.g. 281.4 → 287 KB, 278.1 → 284 KB. Do NOT pre-lock 285 if the fresh measurement disagrees.

    Edit `frontend/.size-limit.json` and change ONLY the `"limit"` field of the entry whose `"name"` is `"React vendor"` from `"349 KB"` to the computed value (formatted as `"<NNN> KB"`, e.g. `"285 KB"`).

    Do NOT modify:
    - Any other entry's `limit` (Initial JS, TanStack vendor, HeroUI vendor, Sentry vendor, DnD vendor, Total JS, signature-visuals/* — all untouched per Phase 53 scope, CONTEXT.md §Deferred).
    - The `path`, `gzip`, `running`, or ordering of any entry.
    - Whitespace/formatting of unrelated lines (Karpathy §3 Surgical Changes).

    After saving, run `pnpm -C frontend size-limit` from the repo root and confirm it exits 0. If it fails because the measurement is above the new ceiling, the computation is wrong — re-check the rounding.

  </action>
  <verify>
    <automated>node -e "const c=JSON.parse(require('fs').readFileSync('frontend/.size-limit.json','utf8'));const r=c.find(x=>x.name==='React vendor');if(!r){console.error('React vendor entry missing');process.exit(1)}const m=r.limit.match(/^(\d+)\s*KB$/);if(!m){console.error('limit format wrong:',r.limit);process.exit(1)}const v=parseInt(m[1],10);if(v>=349||v<280){console.error('ceiling not lowered into expected range (280..348 KB):',v);process.exit(1)}console.log('React vendor ceiling:',v,'KB (lowered from 349)')" &amp;&amp; pnpm -C frontend size-limit</automated>
  </verify>
  <acceptance_criteria>
    - The `"React vendor"` entry's `"limit"` field in `frontend/.size-limit.json` is lowered to the value computed from `ceil(measured + 5)` (~285 KB given the 2026-05-12 baseline)
    - No other entry in `.size-limit.json` is changed (diff scoped to a single `"limit"` field)
    - `pnpm -C frontend size-limit` exits 0
    - `node frontend/scripts/assert-size-limit-matches.mjs` exits 0 (glob `dist/assets/react-vendor-*.js` still resolves to exactly 1 file)
  </acceptance_criteria>
  <done>
    React vendor ceiling tightened per D-01; size-limit run is green on the lowered value.
  </done>
</task>

<task type="auto">
  <name>Task 3: Replace React vendor row in frontend/docs/bundle-budget.md</name>
  <files>frontend/docs/bundle-budget.md</files>
  <read_first>
    - frontend/docs/bundle-budget.md (current "## Ceilings" table — React vendor row at line ~14)
    - .planning/phases/53-bundle-tightening-tag-provenance/53-CONTEXT.md (D-03: replace the row, do not append; verbatim rationale prose template)
    - .planning/phases/49-bundle-budget-reset/49-CONTEXT.md (D-09: living rationale doc, single-row-edit pattern)
  </read_first>
  <action>
    Replace the existing React vendor row in the `## Ceilings` markdown table of `frontend/docs/bundle-budget.md`. The current row is:

    `| React vendor         | 279.92 kB | 349 kB  | react + react-dom + scheduler. Plan 02 ordering fix unblocked tree-shaking: react-vendor dropped from 347.13 kB → 279.92 kB after \`@heroui\` / \`@sentry\` / \`@dnd-kit\` / \`@radix-ui\` / \`@tanstack\` paths stopped leaking through the \`id.includes('react')\` rule. Ceiling held at 349 kB per D-03 \`min(current, measured + 5 kB)\`; not lowered to keep the Plan-02 diff surgical. | 2026-05-12   |`

    Replace it (do NOT append, do NOT leave the Phase 49 prose — per CONTEXT.md D-03: "Existing Phase 49 prose is replaced (not appended) — the living doc reflects current state, the lineage lives in plan SUMMARY.md and git history") with a row using:

    - **gz size column**: the fresh measurement from Task 1, formatted `NNN.NN kB` (e.g. `279.92 kB`).
    - **Ceiling column**: the new ceiling from Task 2 formatted `NNN kB` (e.g. `285 kB`).
    - **Rationale column**: prose covering (a) what the chunk contains (`react + react-dom + scheduler`), (b) that Phase 53 applied D-03 verbatim, (c) the 5 KB / ~1.8% slack rationale for React minor-version drift, (d) cross-link to phase-49 deferral. Suggested wording (adjust numbers to match Task 1/2 outputs):

      `react + react-dom + scheduler. Phase 53 applied Phase 49 D-03 verbatim (\`min(current, measured + 5 kB)\`) after Phase 49 deferred the lowering to keep its Plan-02 diff surgical. Headroom 5 kB ≈ 1.8% slack absorbs React minor-version drift; a tighter ceiling (e.g. 284 kB) would trip on legitimate minor upgrades.`

    - **Last audited column**: `2026-05-16`.

    Do NOT modify:
    - The top `Last audited:` date at line 3 (per Karpathy §3 — that field refers to the full per-chunk re-audit; the row's own "Last audited" column carries the Phase 53 date).
    - Any other row in the Ceilings table (Initial JS, TanStack vendor, HeroUI vendor, Sentry vendor, DnD vendor, Total JS).
    - Any row in the "Residual vendor chunk" table.
    - The `Audit artifact:` reference at line 4 — Phase 53 chose explicitly (D-04) to make the inline row update the canonical evidence, so no new `.planning/phases/53-.../53-BUNDLE-AUDIT.md` artifact is referenced.

    Preserve the markdown table column alignment (the table is column-aligned with padding spaces — Prettier handles this on save; run `pnpm -C frontend exec prettier --write docs/bundle-budget.md` if formatting drifts).

  </action>
  <verify>
    <automated>grep -nE "^\| React vendor" frontend/docs/bundle-budget.md | grep -v "349 kB" &amp;&amp; grep -cE "Phase 53 applied|D-03 verbatim" frontend/docs/bundle-budget.md | grep -qE "^[1-9]" &amp;&amp; grep -qE "2026-05-16" frontend/docs/bundle-budget.md &amp;&amp; ! grep -E "^\| React vendor.*349 kB" frontend/docs/bundle-budget.md</automated>
  </verify>
  <acceptance_criteria>
    - React vendor row no longer shows `349 kB` in the Ceiling column
    - React vendor row shows the new ceiling matching `frontend/.size-limit.json` "React vendor" `limit` (byte-match: same number, same unit suffix)
    - Rationale prose mentions Phase 53 and D-03 (not the Phase 49 "not lowered to keep the Plan-02 diff surgical" wording)
    - Row's Last audited column is `2026-05-16`
    - No other row in the table is modified (`git diff frontend/docs/bundle-budget.md` shows changes only on the React vendor line)
  </acceptance_criteria>
  <done>
    Living rationale doc reflects current state of the React vendor ceiling, matching `.size-limit.json`.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                                                   |
| -------------------- | --------------------------------------------------------------------------------------------- |
| build → ceiling file | The `pnpm size-limit` gate trusts the integer in `.size-limit.json` to enforce on CI          |
| doc → enforcement    | `bundle-budget.md` is human-readable rationale; not parsed by tooling, so doc drift is silent |

## STRIDE Threat Register

| Threat ID | Category               | Component                        | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                        |
| --------- | ---------------------- | -------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-53-01   | Tampering              | `frontend/.size-limit.json`      | mitigate    | Task 2 verify gate confirms the new ceiling falls inside `[280, 348]` KB so a typo (e.g. `2850 KB`) is caught before commit; `pnpm size-limit` is also required-status on `main` per Phase 49 D-10 branch protection, so a too-loose value would fail the gate post-merge if it allowed regression                     |
| T-53-02   | Information disclosure | `frontend/docs/bundle-budget.md` | accept      | Doc is checked into a public repo by design; no secrets, no PII; the value disclosed (chunk size) is already inferable from the production bundle                                                                                                                                                                      |
| T-53-03   | Tampering (supply)     | `ANALYZE=true pnpm build` run    | accept      | Build runs in the developer's local workspace using the same lockfile-pinned tooling CI uses; no net-new install. `rollup-plugin-visualizer` is the only ANALYZE-gated tool and was vetted in Phase 49 D-05. Phase 53 adds no new install, so the legitimacy-gate protocol does not apply (no package-manager install) |

</threat_model>

<verification>
After all three tasks complete, run from the repo root:

```bash
pnpm -C frontend size-limit                              # MUST exit 0
node frontend/scripts/assert-size-limit-matches.mjs       # MUST exit 0 (glob still matches 1 file)
git diff --stat frontend/.size-limit.json frontend/docs/bundle-budget.md
# Expected: only .size-limit.json (1 line changed) and bundle-budget.md (1 row changed)
```

The CI `Bundle Size Check (size-limit)` job (already required-status on `main` per Phase 49 D-10) will enforce the tightened ceiling on the next push.
</verification>

<success_criteria>

- [ ] `frontend/.size-limit.json` "React vendor" `limit` lowered from `"349 KB"` to the D-03-computed value (~`"285 KB"`)
- [ ] `frontend/docs/bundle-budget.md` React vendor row replaced with fresh measurement, new ceiling, Phase 53 D-03 rationale, and `2026-05-16` audit date
- [ ] `pnpm -C frontend size-limit` exits 0 locally
- [ ] `node frontend/scripts/assert-size-limit-matches.mjs` exits 0
- [ ] No other `.size-limit.json` entry modified; no other `bundle-budget.md` row modified (Karpathy §3 Surgical Changes)
      </success_criteria>

<output>
Create `.planning/phases/53-bundle-tightening-tag-provenance/53-01-SUMMARY.md` capturing:
- Fresh measured react-vendor gz (KB) from Task 1
- Computed new ceiling (= ceil(measured + 5))
- Before/after `pnpm size-limit` output snippets
- Confirmation no other rows/entries were touched
</output>
