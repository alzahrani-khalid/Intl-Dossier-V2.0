---
phase: 53-bundle-tightening-tag-provenance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - BUNDLE-07
tags:
  - docs
  - alignment

must_haves:
  truths:
    - 'Both Node.js references in CLAUDE.md (Backend stack ~line 84, Technology Stack ~line 457) read `Node.js 22.13.0+`'
    - 'No CLAUDE.md line still references `Node.js 18+ LTS` or `Node.js 20.19.0+`'
    - 'The Node version in CLAUDE.md byte-matches the floor in `package.json` engines.node (`>=22.13.0`)'
    - 'D-11: both stale Node references in CLAUDE.md are updated to `Node.js 22.13.0+` (mechanical edit; no surrounding prose rewording)'
  artifacts:
    - path: 'CLAUDE.md'
      provides: 'Two Node engine references aligned to package.json engines.node'
      contains: 'Node.js 22.13.0+'
  key_links:
    - from: 'CLAUDE.md'
      to: 'package.json engines.node'
      via: 'Manual byte-match (no tooling validates this — humans read both)'
      pattern: "Node.js 22.13.0\\+"
---

<objective>
Mechanical literal find-and-replace on two CLAUDE.md lines so the documented Node floor matches the actual `engines.node: ">=22.13.0"` in `package.json`. No prose rewording, no other edits.

Purpose: BUNDLE-07 — close the docs-vs-engines drift before Phase 54 onboarding reads CLAUDE.md and gets a stale Node version.
Output: Two lines changed in CLAUDE.md; nothing else.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/53-bundle-tightening-tag-provenance/53-CONTEXT.md
@CLAUDE.md
@package.json

<interfaces>
<!-- CLAUDE.md current state — exact strings to be replaced -->
<!-- Line 84: -->
- **Backend**: Node.js 18+ LTS, Supabase (PostgreSQL 15+, Auth, RLS, Realtime, Storage), Redis 7.x

<!-- Line 457: -->

- **Runtime**: Node.js 20.19.0+, pnpm 10.29.1+ (monorepo via Turbo)

<!-- package.json engines (source of truth) -->

"engines": {
"node": ">=22.13.0",
"pnpm": ">=10.0.0",
...
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align both Node.js references in CLAUDE.md to 22.13.0+</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (lines 80-90 for Backend stack context; lines 450-465 for Technology Stack Runtime context)
    - package.json (lines 1-15 — `engines.node` is the source of truth at `">=22.13.0"`)
    - .planning/phases/53-bundle-tightening-tag-provenance/53-CONTEXT.md (D-11: both lines align, mechanical edit, no surrounding prose changes)
  </read_first>
  <action>
    Two literal find-and-replace edits in `CLAUDE.md` (per D-11). Use the Edit tool, not sed/awk — both replacements are unique strings in the file.

    **Replacement 1** (Core Tech Stack → Backend, line 84):
    - Find: `- **Backend**: Node.js 18+ LTS, Supabase (PostgreSQL 15+, Auth, RLS, Realtime, Storage), Redis 7.x`
    - Replace with: `- **Backend**: Node.js 22.13.0+, Supabase (PostgreSQL 15+, Auth, RLS, Realtime, Storage), Redis 7.x`

    **Replacement 2** (Technology Stack → Runtime, line 457):
    - Find: `- **Runtime**: Node.js 20.19.0+, pnpm 10.29.1+ (monorepo via Turbo)`
    - Replace with: `- **Runtime**: Node.js 22.13.0+, pnpm 10.29.1+ (monorepo via Turbo)`

    Do NOT modify:
    - The surrounding prose ("LTS" is the only descriptor being dropped — it does not need a replacement; the engines.node floor `>=22.13.0` is already an LTS version and re-stating LTS is redundant per the source of truth).
    - The pnpm version reference (`pnpm 10.29.1+`) — that's a separate floor from `package.json` `engines.pnpm: ">=10.0.0"` and is out of scope per CONTEXT.md (BUNDLE-07 names only the Node line).
    - PostgreSQL, Redis, or any other version number on either line.
    - Any other Node-related reference elsewhere in CLAUDE.md (BUNDLE-07 names exactly two lines; D-11 leaves the redundant duplication as a deferred cleanup per CONTEXT.md §Deferred).

    The matches must be exact-string unique — both lines above appear exactly once in CLAUDE.md (verified via grep). If the Edit tool reports ambiguity, the file has drifted from the expected state; stop and report.

  </action>
  <verify>
    <automated>test "$(grep -c '^- \*\*Backend\*\*: Node\.js 22\.13\.0\+,' CLAUDE.md)" = "1" &amp;&amp; test "$(grep -c '^- \*\*Runtime\*\*: Node\.js 22\.13\.0\+,' CLAUDE.md)" = "1" &amp;&amp; test "$(grep -c 'Node\.js 18\+ LTS' CLAUDE.md)" = "0" &amp;&amp; test "$(grep -c 'Node\.js 20\.19\.0\+' CLAUDE.md)" = "0" &amp;&amp; ENGINES_NODE=$(node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).engines.node)") &amp;&amp; test "$ENGINES_NODE" = ">=22.13.0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \*\*Backend\*\*: Node\.js 22\.13\.0\+,' CLAUDE.md` returns exactly 1
    - `grep -c '^- \*\*Runtime\*\*: Node\.js 22\.13\.0\+,' CLAUDE.md` returns exactly 1
    - `grep -c 'Node\.js 18\+ LTS' CLAUDE.md` returns 0 (the old Backend reference is gone)
    - `grep -c 'Node\.js 20\.19\.0\+' CLAUDE.md` returns 0 (the old Runtime reference is gone)
    - `package.json` `engines.node` reads `">=22.13.0"` (unchanged — this plan only aligns docs to the existing engines value)
    - `git diff CLAUDE.md` shows exactly 2 lines changed (the Backend line and the Runtime line)
  </acceptance_criteria>
  <done>
    Both stale Node references replaced with `Node.js 22.13.0+`; CLAUDE.md byte-matches package.json engines.node floor.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary       | Description                                                                    |
| -------------- | ------------------------------------------------------------------------------ |
| docs → humans  | CLAUDE.md is read by developers and AI agents to decide Node install version   |
| docs → tooling | No tooling parses CLAUDE.md's Node line; drift is silent until a human notices |

## STRIDE Threat Register

| Threat ID | Category               | Component | Disposition | Mitigation Plan                                                                                                                                                                        |
| --------- | ---------------------- | --------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-53-04   | Repudiation / drift    | CLAUDE.md | mitigate    | The verify gate compares CLAUDE.md against `package.json` engines.node at task close; future drift can be detected by re-running the same grep + node assertion in any onboarding lint |
| T-53-05   | Information disclosure | CLAUDE.md | accept      | No secrets; Node version is already public via `package.json` in the same repo                                                                                                         |

</threat_model>

<verification>
```bash
grep -n "Node.js" CLAUDE.md          # MUST show both lines reading "Node.js 22.13.0+"
node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).engines.node)"  # MUST print >=22.13.0
git diff --stat CLAUDE.md            # MUST show 2 changed lines, no insertions/deletions of unrelated content
```
</verification>

<success_criteria>

- [ ] Both Node.js references in CLAUDE.md read `Node.js 22.13.0+`
- [ ] No CLAUDE.md line still references `Node.js 18+ LTS` or `Node.js 20.19.0+`
- [ ] `package.json` `engines.node` still reads `">=22.13.0"` (untouched — alignment direction is docs → engines, not engines → docs)
- [ ] Diff scope: exactly 2 lines in CLAUDE.md (Karpathy §3 Surgical Changes)
      </success_criteria>

<output>
Create `.planning/phases/53-bundle-tightening-tag-provenance/53-02-SUMMARY.md` capturing:
- Before/after of both replaced lines
- `git diff --stat CLAUDE.md` showing only 2 lines changed
- Confirmation `package.json` engines.node is unchanged
</output>
