---
phase: 25-deferred-audit-fixes
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - backend/src/api/after-action.ts
  - frontend/src/routes/_protected/after-actions/$afterActionId.tsx
  - frontend/src/routes/_protected/engagements/$engagementId/after-action.tsx
autonomous: true
requirements:
  - D-41

must_haves:
  truths:
    - 'Saving an after-action record checks updated_at before writing'
    - 'Server returns 409 Conflict when updated_at mismatch'
    - 'Client shows destructive warning banner on conflict'
    - 'Silent overwrites are prevented in concurrent editing scenarios'
  artifacts:
    - path: 'backend/src/api/after-action.ts'
      provides: 'PUT/PATCH endpoint with optimistic lock check'
      contains: '409'
    - path: 'frontend/src/routes/_protected/after-actions/$afterActionId.tsx'
      provides: 'Conflict detection UI with warning banner'
      contains: 'conflict'
  key_links:
    - from: 'frontend after-action edit'
      to: 'backend PUT /after-actions/:id'
      via: 'updated_at comparison'
      pattern: 'updated_at'
    - from: 'backend PUT handler'
      to: 'database UPDATE'
      via: 'WHERE updated_at = $expected'
      pattern: 'WHERE.*updated_at'
---

<objective>
Implement after-action version conflict detection to prevent silent overwrites.

Purpose: D-41 requires optimistic locking via `updated_at` comparison. Both client-side pre-check and server-side WHERE clause needed to close the TOCTOU gap (Pitfall 5 from RESEARCH.md).
Output: Backend 409 response on conflict + frontend conflict warning banner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-deferred-audit-fixes/25-RESEARCH.md
@.planning/phases/25-deferred-audit-fixes/25-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add server-side optimistic locking to after-action PUT endpoint</name>
  <files>backend/src/api/after-action.ts</files>
  <read_first>
    backend/src/api/after-action.ts
  </read_first>
  <action>
    1. Read after-action.ts to find the PUT or PATCH handler for updating after-action records.
    2. Modify the update handler to:
       a. Accept `updated_at` in the request body (the client's known version timestamp)
       b. Use a conditional UPDATE query: `UPDATE after_action_records SET ... WHERE id = $1 AND updated_at = $2`
       c. Check the result row count. If 0 rows affected:
          - Fetch the current record to get the actual `updated_at`
          - Return 409 status with body: `{ error: 'CONFLICT', message: 'This record was modified by another user.', serverUpdatedAt: currentRecord.updated_at }`
       d. If 1 row affected, proceed normally with 200 response
    3. If the `after_action_records` table does NOT have an `updated_at` column, check by querying `SELECT column_name FROM information_schema.columns WHERE table_name = 'after_action_records'` via Supabase MCP. If missing, add a migration to add `updated_at TIMESTAMPTZ DEFAULT now()` with a trigger to auto-update on row change.
    4. Add express-validator check: `body('updated_at').isISO8601().withMessage('updated_at is required for conflict detection')`
    5. Follow project conventions: no semicolons, single quotes, trailing commas, explicit return types, Winston logger for errors
  </action>
  <verify>
    <automated>cd "/Users/khalidalzahrani/Desktop/Coding Space/Intl-Dossier-V2.0" && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep "409" backend/src/api/after-action.ts` shows conflict response status
    - `grep "updated_at" backend/src/api/after-action.ts` shows the conditional check
    - `grep "CONFLICT" backend/src/api/after-action.ts` shows the error code
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Backend returns 409 Conflict when updated_at mismatch on after-action save</done>
</task>

<task type="auto">
  <name>Task 2: Add conflict detection UI to after-action editor</name>
  <files>
    frontend/src/routes/_protected/after-actions/$afterActionId.tsx,
    frontend/src/routes/_protected/engagements/$engagementId/after-action.tsx
  </files>
  <read_first>
    frontend/src/routes/_protected/after-actions/$afterActionId.tsx,
    frontend/src/routes/_protected/engagements/$engagementId/after-action.tsx,
    frontend/src/components/ui/button.tsx
  </read_first>
  <action>
    1. Read both after-action edit routes to understand the save flow.
    2. In the save handler (mutation's onMutate or the form submit handler):
       a. Include `updated_at` from the locally loaded record in the PUT request body
       b. Handle 409 response specifically:
          - Set a `conflict` state: `const [conflict, setConflict] = useState<{ serverUpdatedAt: string } | null>(null)`
          - When 409 received, call `setConflict({ serverUpdatedAt: response.serverUpdatedAt })`
    3. Add conflict warning banner (renders when `conflict` is not null):
       ```tsx
       {conflict != null && (
         <div className="rounded-lg border border-destructive/50 bg-destructive/10 p-4 flex items-center justify-between gap-4">
           <div className="flex items-center gap-2">
             <AlertTriangle className="h-5 w-5 text-destructive shrink-0" />
             <p className="text-sm text-destructive">
               {t('conflict.warning')}
             </p>
           </div>
           <Button
             variant="destructive"
             size="sm"
             onClick={() => { window.location.reload() }}
           >
             {t('conflict.reviewChanges')}
           </Button>
         </div>
       )}
       ```
    4. Add i18n keys if missing:
       - `conflict.warning`: "This record was modified by another user. Review changes before saving."
       - `conflict.reviewChanges`: "Review Changes"
    5. Apply the same pattern to both route files (afterActionId.tsx and engagementId/after-action.tsx)
    6. Use logical RTL properties, 44px min touch targets on the CTA button
  </action>
  <verify>
    <automated>cd "/Users/khalidalzahrani/Desktop/Coding Space/Intl-Dossier-V2.0" && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep "conflict" frontend/src/routes/_protected/after-actions/\$afterActionId.tsx` shows conflict state and UI
    - `grep "409" frontend/src/routes/_protected/after-actions/\$afterActionId.tsx` shows 409 handling
    - `grep "updated_at" frontend/src/routes/_protected/after-actions/\$afterActionId.tsx` shows timestamp sent with save
    - `grep "destructive" frontend/src/routes/_protected/after-actions/\$afterActionId.tsx` shows warning banner styling
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Conflict detection UI shows destructive warning banner on 409, blocks silent overwrites</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary      | Description                                         |
| ------------- | --------------------------------------------------- |
| client -> API | updated_at timestamp from client is untrusted input |

## STRIDE Threat Register

| Threat ID | Category  | Component        | Disposition | Mitigation Plan                                                                                    |
| --------- | --------- | ---------------- | ----------- | -------------------------------------------------------------------------------------------------- |
| T-25-01   | Tampering | after-action PUT | mitigate    | Server-side WHERE updated_at = $expected prevents client-only bypass; client cannot skip the check |
| T-25-02   | Tampering | updated_at param | mitigate    | Validate with express-validator isISO8601() at route entry; reject malformed timestamps            |

</threat_model>

<verification>
- `pnpm typecheck` passes
- Backend returns 409 on updated_at mismatch
- Frontend shows warning banner on 409
- updated_at included in save requests
</verification>

<success_criteria>

- Server-side optimistic lock prevents TOCTOU race condition
- Client-side conflict UI shows destructive warning with reload CTA
- Both after-action editor routes handle conflicts
- No silent overwrites possible
  </success_criteria>

<output>
After completion, create `.planning/phases/25-deferred-audit-fixes/25-03-SUMMARY.md`
</output>
