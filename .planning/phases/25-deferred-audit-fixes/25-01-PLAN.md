---
phase: 25-deferred-audit-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Dossier/DossierCreateWizard.tsx
  - frontend/src/components/Dossier/wizard-steps/TypeSelectionStep.tsx
  - frontend/src/components/Dossier/wizard-steps/BasicInfoStep.tsx
  - frontend/src/components/Dossier/wizard-steps/TypeSpecificStep.tsx
  - frontend/src/components/Dossier/wizard-steps/RelationshipsStep.tsx
  - frontend/src/components/Dossier/wizard-steps/ReviewStep.tsx
  - frontend/src/components/Dossier/wizard-steps/shared.ts
autonomous: true
requirements:
  - C-12

must_haves:
  truths:
    - 'DossierCreateWizard.tsx is under 300 LOC and acts as orchestrator only'
    - 'Each wizard step component is under 300 LOC'
    - 'Wizard step navigation still works (forward, backward, submit)'
    - 'Form state is preserved when switching between steps'
    - 'TypeScript compilation succeeds with no errors'
  artifacts:
    - path: 'frontend/src/components/Dossier/DossierCreateWizard.tsx'
      provides: 'Slim orchestrator with step routing'
      max_lines: 300
    - path: 'frontend/src/components/Dossier/wizard-steps/TypeSelectionStep.tsx'
      provides: 'Step 0: dossier type picker'
      max_lines: 300
    - path: 'frontend/src/components/Dossier/wizard-steps/BasicInfoStep.tsx'
      provides: 'Step 1: name, description, type-agnostic fields'
      max_lines: 300
    - path: 'frontend/src/components/Dossier/wizard-steps/TypeSpecificStep.tsx'
      provides: 'Step 2: type-dependent extension fields'
      max_lines: 300
    - path: 'frontend/src/components/Dossier/wizard-steps/RelationshipsStep.tsx'
      provides: 'Step 3: link to other dossiers'
      max_lines: 300
    - path: 'frontend/src/components/Dossier/wizard-steps/ReviewStep.tsx'
      provides: 'Step 4: summary and submit'
      max_lines: 300
  key_links:
    - from: 'DossierCreateWizard.tsx'
      to: 'wizard-steps/*.tsx'
      via: 'dynamic import or direct import in switch-case'
      pattern: 'import.*wizard-steps'
---

<objective>
Decompose DossierCreateWizard (1979 LOC) into step components each under 300 LOC.

Purpose: C-12 requires the monolithic wizard to be split for maintainability. The wizard has 6 steps identified via the `renderStepContent()` switch-case at line 606.
Output: Slim orchestrator (~200 LOC) + 5 step components + shared types file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-deferred-audit-fixes/25-RESEARCH.md
@.planning/phases/25-deferred-audit-fixes/25-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared types and create step component files</name>
  <files>
    frontend/src/components/Dossier/wizard-steps/shared.ts,
    frontend/src/components/Dossier/wizard-steps/TypeSelectionStep.tsx,
    frontend/src/components/Dossier/wizard-steps/BasicInfoStep.tsx,
    frontend/src/components/Dossier/wizard-steps/TypeSpecificStep.tsx,
    frontend/src/components/Dossier/wizard-steps/RelationshipsStep.tsx,
    frontend/src/components/Dossier/wizard-steps/ReviewStep.tsx
  </files>
  <read_first>
    frontend/src/components/Dossier/DossierCreateWizard.tsx,
    frontend/src/routes/_protected/dossiers/create.tsx
  </read_first>
  <action>
    1. Read DossierCreateWizard.tsx fully to understand the step structure, form state shape, and all imports.
    2. Create `frontend/src/components/Dossier/wizard-steps/` directory.
    3. Create `shared.ts` with:
       - All type definitions used across steps (WizardFormData, StepProps interface with `formData`, `updateFormData`, `errors`, `dossierType` props)
       - Any shared constants (step labels, validation schemas)
       - Export everything as named exports
    4. For each of the 5 step components (TypeSelectionStep, BasicInfoStep, TypeSpecificStep, RelationshipsStep, ReviewStep):
       - Extract the JSX and logic from the corresponding case in `renderStepContent()` switch-case (line ~606)
       - Each component receives form state via props (StepProps interface from shared.ts)
       - Each component is a named export default function with explicit return type `: ReactElement`
       - Move step-specific validation logic into each step component
       - Move step-specific hooks (e.g., data fetching for relationships) into the step that uses them
       - Ensure all imports are correct (lucide-react icons, HeroUI components, hooks, etc.)
       - Each file MUST be under 300 LOC
    5. Follow project conventions: no semicolons, single quotes, trailing commas, explicit return types, no `any`, logical RTL properties (ms-*/me-*/ps-*/pe-*)
  </action>
  <verify>
    <automated>wc -l frontend/src/components/Dossier/wizard-steps/*.tsx frontend/src/components/Dossier/wizard-steps/*.ts | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `ls frontend/src/components/Dossier/wizard-steps/` shows 6 files: shared.ts, TypeSelectionStep.tsx, BasicInfoStep.tsx, TypeSpecificStep.tsx, RelationshipsStep.tsx, ReviewStep.tsx
    - `wc -l frontend/src/components/Dossier/wizard-steps/TypeSelectionStep.tsx` is under 300
    - `wc -l frontend/src/components/Dossier/wizard-steps/BasicInfoStep.tsx` is under 300
    - `wc -l frontend/src/components/Dossier/wizard-steps/TypeSpecificStep.tsx` is under 300
    - `wc -l frontend/src/components/Dossier/wizard-steps/RelationshipsStep.tsx` is under 300
    - `wc -l frontend/src/components/Dossier/wizard-steps/ReviewStep.tsx` is under 300
    - `grep "export" frontend/src/components/Dossier/wizard-steps/shared.ts` shows StepProps and WizardFormData exports
  </acceptance_criteria>
  <done>All 5 step components + shared types file created, each under 300 LOC</done>
</task>

<task type="auto">
  <name>Task 2: Refactor DossierCreateWizard into slim orchestrator</name>
  <files>frontend/src/components/Dossier/DossierCreateWizard.tsx</files>
  <read_first>
    frontend/src/components/Dossier/DossierCreateWizard.tsx,
    frontend/src/components/Dossier/wizard-steps/shared.ts,
    frontend/src/components/Dossier/wizard-steps/TypeSelectionStep.tsx,
    frontend/src/components/Dossier/wizard-steps/BasicInfoStep.tsx,
    frontend/src/components/Dossier/wizard-steps/TypeSpecificStep.tsx,
    frontend/src/components/Dossier/wizard-steps/RelationshipsStep.tsx,
    frontend/src/components/Dossier/wizard-steps/ReviewStep.tsx
  </read_first>
  <action>
    1. Rewrite DossierCreateWizard.tsx as orchestrator only (~200 LOC target, max 300):
       - Keep: step state management (currentStep, setCurrentStep), form state (useState or useReducer for WizardFormData), navigation handlers (next/prev/submit), stepper bar UI
       - Remove: all step-specific JSX that was moved to step components
       - Import all 5 step components from `./wizard-steps/`
       - Replace the `renderStepContent()` switch-case with a component map or simple switch that renders the imported step component, passing StepProps
       - Keep the stepper progress indicator at the top
       - Keep the "Discard Draft" confirmation dialog
       - Keep the submit handler that calls the API
    2. Ensure form state stays in this orchestrator component (NOT in individual steps) to prevent state loss when switching steps (Pitfall 4 from RESEARCH.md)
    3. Verify all existing imports in the route file (`create.tsx`) still work -- the component name and export must not change
    4. Run `pnpm typecheck` to confirm no TypeScript errors
  </action>
  <verify>
    <automated>cd "/Users/khalidalzahrani/Desktop/Coding Space/Intl-Dossier-V2.0" && wc -l frontend/src/components/Dossier/DossierCreateWizard.tsx && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l frontend/src/components/Dossier/DossierCreateWizard.tsx` is under 300
    - `grep "import.*wizard-steps" frontend/src/components/Dossier/DossierCreateWizard.tsx` shows imports from wizard-steps directory
    - `grep "renderStepContent" frontend/src/components/Dossier/DossierCreateWizard.tsx` returns nothing (old monolithic function removed)
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>DossierCreateWizard is under 300 LOC, imports step components, TypeScript compiles clean</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                       |
| -------- | ------------------------------------------------- |
| N/A      | Pure frontend refactor -- no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component       | Disposition | Mitigation Plan                                                         |
| --------- | -------- | --------------- | ----------- | ----------------------------------------------------------------------- |
| T-25-00   | N/A      | Wizard refactor | accept      | No new inputs, no new data flows -- pure decomposition of existing code |

</threat_model>

<verification>
- `pnpm typecheck` passes
- All 6 new files exist in wizard-steps/
- Each file under 300 LOC
- DossierCreateWizard.tsx under 300 LOC
</verification>

<success_criteria>

- DossierCreateWizard decomposed from 1979 LOC to ~200 LOC orchestrator
- 5 step components each under 300 LOC
- Shared types file exports StepProps interface
- No TypeScript errors
- Form state preserved across step transitions
  </success_criteria>

<output>
After completion, create `.planning/phases/25-deferred-audit-fixes/25-01-SUMMARY.md`
</output>
