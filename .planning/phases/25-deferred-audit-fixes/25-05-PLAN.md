---
phase: 25-deferred-audit-fixes
plan: 05
type: execute
wave: 3
depends_on: [04]
files_modified:
  - frontend/src/routes/_protected/dossiers/index.tsx
  - frontend/src/routes/_protected/dossiers/countries/index.tsx
  - frontend/src/routes/_protected/dossiers/organizations/index.tsx
  - frontend/src/routes/_protected/dossiers/forums/index.tsx
  - frontend/src/routes/_protected/dossiers/engagements/index.tsx
  - frontend/src/routes/_protected/dossiers/topics/index.tsx
  - frontend/src/routes/_protected/dossiers/working_groups/index.tsx
  - frontend/src/routes/_protected/dossiers/persons/index.tsx
  - frontend/src/routes/_protected/dossiers/elected-officials/index.tsx
  - frontend/src/routes/_protected/dossiers/working-groups/index.tsx
autonomous: false
requirements:
  - D-32
  - N-20
  - N-04
  - breadcrumb/skeleton rollout

must_haves:
  truths:
    - 'All 8 dossier type list pages render EntityBreadcrumbTrail'
    - 'All 8 dossier type list pages show loading skeletons during data fetch'
    - 'Active dossier type tab is visually highlighted with accent color'
    - 'working-groups route exists with kebab-case'
    - 'Old working_groups URL redirects to working-groups'
  artifacts:
    - path: 'frontend/src/routes/_protected/dossiers/index.tsx'
      provides: 'Dossier type navigation with active tab indicator'
      contains: 'active'
    - path: 'frontend/src/routes/_protected/dossiers/elected-officials/index.tsx'
      provides: 'Breadcrumb + skeleton on elected-officials (was missing)'
      contains: 'EntityBreadcrumbTrail'
    - path: 'frontend/src/routes/_protected/dossiers/working-groups/index.tsx'
      provides: 'Kebab-case route for working groups'
      contains: 'createFileRoute'
  key_links:
    - from: 'dossier list pages'
      to: 'EntityBreadcrumbTrail'
      via: 'import and render'
      pattern: 'EntityBreadcrumbTrail'
    - from: 'working_groups/index.tsx'
      to: 'working-groups/index.tsx'
      via: 'redirect'
      pattern: 'redirect.*working-groups'
---

<objective>
Roll out breadcrumbs and skeletons to all 8 dossier list pages, add active tab indicator, and standardize route naming.

Purpose: D-32 highlights the active dossier type tab. N-20/N-04 standardize URL naming to kebab-case with redirects. Breadcrumb/skeleton rollout ensures consistent UX across all dossier types.
Output: All 8 dossier list pages have breadcrumbs + skeletons + active tab. working_groups renamed to working-groups with redirect.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-deferred-audit-fixes/25-RESEARCH.md
@.planning/phases/25-deferred-audit-fixes/25-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add breadcrumbs, skeletons, and active tab indicator to all dossier list pages</name>
  <files>
    frontend/src/routes/_protected/dossiers/index.tsx,
    frontend/src/routes/_protected/dossiers/countries/index.tsx,
    frontend/src/routes/_protected/dossiers/organizations/index.tsx,
    frontend/src/routes/_protected/dossiers/forums/index.tsx,
    frontend/src/routes/_protected/dossiers/engagements/index.tsx,
    frontend/src/routes/_protected/dossiers/topics/index.tsx,
    frontend/src/routes/_protected/dossiers/working_groups/index.tsx,
    frontend/src/routes/_protected/dossiers/persons/index.tsx,
    frontend/src/routes/_protected/dossiers/elected-officials/index.tsx
  </files>
  <read_first>
    frontend/src/components/layout/EntityBreadcrumbTrail.tsx,
    frontend/src/routes/_protected/dossiers/countries/index.tsx,
    frontend/src/routes/_protected/dossiers/elected-officials/index.tsx,
    frontend/src/routes/_protected/dossiers/index.tsx,
    frontend/src/components/ui/skeleton.tsx
  </read_first>
  <action>
    1. Read EntityBreadcrumbTrail.tsx to understand its props API and how countries/index.tsx uses it (countries has ~20 breadcrumb refs -- it is the reference implementation).
    2. Read elected-officials/index.tsx which has 0 breadcrumb refs -- this is the worst case.
    3. For the dossier index route (dossiers/index.tsx -- the type navigation page):
       a. Find the dossier type tab/link navigation (the list of 8 type links)
       b. Add active tab indicator: compare current URL path segment against each tab's route
       c. Style active tab with `font-semibold` + accent underline/highlight:
          ```tsx
          <Link
            to="/dossiers/{type}"
            className={cn(
              'px-4 py-2 min-h-11 text-sm transition-colors',
              isActive
                ? 'font-semibold text-primary border-b-2 border-primary'
                : 'text-muted-foreground hover:text-foreground'
            )}
          >
          ```
       d. Use `var(--primary)` (maps to accent) for the active indicator color per UI-SPEC
    4. For each of the 8 dossier type list pages that is MISSING breadcrumbs:
       a. Import `EntityBreadcrumbTrail` from `@/components/layout/EntityBreadcrumbTrail`
       b. Render it at the top of the page content, above the list
       c. Pass the correct segments: Home > Dossiers > {Type Name}
       d. Current segment should be non-clickable with `font-semibold` per UI-SPEC
    5. For each of the 8 pages that is MISSING loading skeletons:
       a. Use the existing `Skeleton` component from `@/components/ui/skeleton`
       b. Show skeleton during `isLoading` state from the data query
       c. Skeleton layout should match the list grid structure (same columns, similar card heights)
       d. Use `animate-pulse` with `bg-muted` base (already built into Skeleton component)
    6. Use logical RTL properties (ms-*/me-*/ps-*/pe-*/text-start/text-end) throughout
    7. Touch targets: min-h-11 min-w-11 on breadcrumb links and tab links (44px)
  </action>
  <verify>
    <automated>cd "/Users/khalidalzahrani/Desktop/Coding Space/Intl-Dossier-V2.0" && for dir in countries organizations forums engagements topics working_groups persons elected-officials; do grep -l "EntityBreadcrumbTrail" "frontend/src/routes/_protected/dossiers/$dir/index.tsx" 2>/dev/null; done | wc -l && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep "EntityBreadcrumbTrail" frontend/src/routes/_protected/dossiers/elected-officials/index.tsx` shows import and usage
    - `grep "EntityBreadcrumbTrail" frontend/src/routes/_protected/dossiers/persons/index.tsx` shows import and usage
    - `grep "EntityBreadcrumbTrail" frontend/src/routes/_protected/dossiers/forums/index.tsx` shows import and usage
    - `grep "Skeleton" frontend/src/routes/_protected/dossiers/elected-officials/index.tsx` shows skeleton usage
    - `grep "border-primary\|text-primary" frontend/src/routes/_protected/dossiers/index.tsx` shows active tab styling
    - `grep "font-semibold" frontend/src/routes/_protected/dossiers/index.tsx` shows active tab weight
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>All 8 dossier list pages have breadcrumbs + skeletons, dossier index has active tab indicator</done>
</task>

<task type="auto">
  <name>Task 2: Standardize route naming to kebab-case with redirect for working_groups</name>
  <files>
    frontend/src/routes/_protected/dossiers/working-groups/index.tsx,
    frontend/src/routes/_protected/dossiers/working_groups/index.tsx
  </files>
  <read_first>
    frontend/src/routes/_protected/dossiers/working_groups/index.tsx,
    frontend/src/lib/dossier-routes.ts
  </read_first>
  <action>
    1. Create new directory `frontend/src/routes/_protected/dossiers/working-groups/` (kebab-case).
    2. Copy the content from `working_groups/index.tsx` to `working-groups/index.tsx`.
    3. Update the route path in the new file's `createFileRoute` call to use `/_protected/dossiers/working-groups/`.
    4. Convert `working_groups/index.tsx` into a redirect:
       ```typescript
       import { createFileRoute, redirect } from '@tanstack/react-router'

       export const Route = createFileRoute('/_protected/dossiers/working_groups/')({
         beforeLoad: (): never => {
           throw redirect({ to: '/dossiers/working-groups' })
         },
       })
       ```
    5. Also handle the detail route: if `working_groups/$id.tsx` exists, create `working-groups/$id.tsx` with the same content and convert the old one to a redirect with `throw redirect({ to: '/dossiers/working-groups/$id', params: { id: ctx.params.id } })`.
    6. Run `pnpm dev` briefly (or just `pnpm typecheck`) to regenerate `routeTree.gen.ts` after the directory change (Pitfall 1 from RESEARCH.md).
    7. Fix any TypeScript errors from broken route path strings in Link components throughout the codebase.
    8. Update `getDossierRouteSegment('working_group')` in dossier-routes.ts if it returns `'working_groups'` -- change to `'working-groups'`.
    9. Check all Link components that reference `working_groups` path and update to `working-groups`.

  </action>
  <verify>
    <automated>cd "/Users/khalidalzahrani/Desktop/Coding Space/Intl-Dossier-V2.0" && ls frontend/src/routes/_protected/dossiers/working-groups/index.tsx && grep "redirect" frontend/src/routes/_protected/dossiers/working_groups/index.tsx && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `ls frontend/src/routes/_protected/dossiers/working-groups/index.tsx` exists
    - `grep "createFileRoute" frontend/src/routes/_protected/dossiers/working-groups/index.tsx` shows kebab-case route path
    - `grep "redirect" frontend/src/routes/_protected/dossiers/working_groups/index.tsx` shows redirect to kebab-case
    - `grep "working-groups" frontend/src/routes/_protected/dossiers/working_groups/index.tsx` shows redirect target
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>working-groups kebab-case route exists, old working_groups redirects to it</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify breadcrumbs, skeletons, tab indicator, and route redirect</name>
  <what-built>
    Breadcrumbs and loading skeletons on all 8 dossier type list pages, active tab indicator on dossier type navigation, and working_groups -> working-groups route redirect.
  </what-built>
  <how-to-verify>
    1. Navigate to /dossiers -- verify type tabs are visible and the active type has accent color + bold text
    2. Click each of the 8 dossier types -- verify:
       a. Breadcrumb trail shows "Home > Dossiers > {Type Name}" at the top
       b. Loading skeleton appears briefly during data fetch
       c. Active tab updates to highlight the current type
    3. Navigate to /dossiers/working_groups -- verify it redirects to /dossiers/working-groups
    4. Check URL bar shows ?page=1 or clean URL (from Plan 04)
    5. Test in Arabic (RTL) mode -- breadcrumbs should flow right-to-left without .reverse()
  </how-to-verify>
  <action>Visual verification of breadcrumbs, skeletons, active tab indicator, and route redirect across all 8 dossier type list pages in both LTR and RTL modes.</action>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                    |
| -------- | ------------------------------------------------------------------------------ |
| URL path | Old underscore paths redirected -- no open redirect risk (target is hardcoded) |

## STRIDE Threat Register

| Threat ID | Category | Component      | Disposition | Mitigation Plan                                                                                         |
| --------- | -------- | -------------- | ----------- | ------------------------------------------------------------------------------------------------------- |
| T-25-05   | Spoofing | redirect route | accept      | Redirect target is hardcoded to internal path `/dossiers/working-groups` -- no user-controlled redirect |

</threat_model>

<verification>
- `pnpm typecheck` passes
- All 8 dossier list pages render EntityBreadcrumbTrail
- Dossier index page shows active tab indicator
- working_groups redirects to working-groups
- Visual verification via checkpoint
</verification>

<success_criteria>

- Consistent breadcrumbs and skeletons across all 8 dossier type list pages
- Active dossier type tab highlighted with accent color and bold text
- working-groups kebab-case route is canonical, working_groups redirects
- All route references updated to kebab-case
- RTL breadcrumbs render correctly without .reverse()
  </success_criteria>

<output>
After completion, create `.planning/phases/25-deferred-audit-fixes/25-05-SUMMARY.md`
</output>
