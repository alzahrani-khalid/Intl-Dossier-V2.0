---
phase: 82-date-number-formatting
plan: 04
type: execute
wave: 2
depends_on: [82-01]
files_modified:
  - frontend/src/pages/Dashboard/components/ActionBar.tsx
  - frontend/src/pages/Dashboard/components/AttentionItem.tsx
  - frontend/src/pages/Dashboard/widgets/WeekAhead.tsx
  - frontend/src/pages/Dashboard/widgets/MyTasks.tsx
  - frontend/src/pages/Dashboard/widgets/Digest.tsx
  - frontend/src/pages/my-work/components/WorkItemCard.tsx
  - frontend/src/pages/my-work/components/WorkSummaryHeader.tsx
  - frontend/src/pages/my-work/components/WorkItemTabs.tsx
  - frontend/src/pages/my-work/components/ProductivityMetrics.tsx
  - frontend/src/pages/my-work/components/TeamWorkloadPanel.tsx
  - frontend/src/pages/MyTasks.tsx
  - frontend/src/pages/Briefs/BriefsPage.tsx
  - frontend/src/pages/Briefs/__tests__/BriefsPage.test.tsx
  - frontend/src/pages/WorkingGroupsPage.tsx
  - frontend/src/pages/IntakeQueue.tsx
  - frontend/src/pages/AssignmentQueue.tsx
  - frontend/src/pages/engagements/workspace/DocsTab.tsx
  - frontend/src/pages/engagements/workspace/AuditTab.tsx
  - frontend/src/pages/engagements/workspace/CalendarTab.tsx
  - frontend/src/pages/webhooks/WebhooksPage.tsx
  - frontend/src/pages/advanced-search/AdvancedSearchPage.tsx
  - frontend/src/pages/users/UsersListPage.tsx
  - frontend/src/routes/_protected/approvals/index.tsx
  - frontend/src/routes/_protected/admin/approvals.tsx
  - frontend/src/routes/_protected/admin/data-retention.tsx
  - frontend/src/routes/_protected/admin/field-permissions.tsx
  - frontend/src/routes/_protected/stakeholder-influence.tsx
  - frontend/src/routes/_protected/dossiers/topics/-TopicsListPage.tsx
  - frontend/src/routes/_protected/dossiers/forums/index.tsx
autonomous: true
requirements: [FMT-01, FMT-02, FMT-04]
must_haves:
  truths:
    - "Satisfies D-82-01 (greeting half): the dashboard greeting date (ActionBar) reads day-first no-comma via formatDayFirst — no 'EEEE, MMMM d, yyyy' month-first comma shape remains; and the Intelligence Digest row timestamps ('May 8, 10:44 AM' today) read day-first via formatDateTime — Digest.tsx renders ON the dashboard, so this is mandatory for the 82-06 Task-3 no-Indic dashboard check"
    - "Satisfies D-82-02: every ad-hoc toLocaleDateString date render in this plan's pages/** and routes/** files routes through lib/format-date helpers, including named offender Briefs/BriefsPage.tsx (en-GB local shadow deleted) and the previously-unowned AssignmentQueue/CalendarTab/TopicsListPage/forums sites"
    - "Satisfies D-82-05: zero raw 'ar-SA' locale literals remain in the 29 files — the four my-work number surfaces (WorkSummaryHeader, WorkItemTabs, ProductivityMetrics, TeamWorkloadPanel) route through toFormatLocale; BriefsPage and pages/MyTasks toArDigits wraps removed in the same edit as their shadow removal (Pitfall 4 overlap files owned entirely by this plan); field-permissions' 'ar-SA' toLocaleString gone; BriefsPage test flipped from asserting Indic-present to Latin AND stripped of every 'toArDigits' string mention (header doc + it() name — the wave-4 gate greps strings)"
    - "WeekAhead's weekday-chip/day-number split uses date-fns 'EEE'/'dd' (Latin, day-first-compatible) and its time range uses formatTime — no 'ar-SA' 12-hour time remains"
  artifacts:
    - path: 'frontend/src/pages/Dashboard/components/ActionBar.tsx'
      provides: 'day-first greeting'
      contains: 'formatDayFirst'
    - path: 'frontend/src/pages/Dashboard/widgets/Digest.tsx'
      provides: 'dashboard digest timestamps day-first Latin'
      contains: 'formatDateTime'
    - path: 'frontend/src/pages/Briefs/BriefsPage.tsx'
      provides: 'named offender migrated + digit unwrap'
      contains: '@/lib/format-date'
  key_links:
    - from: 'frontend/src/pages/Dashboard/components/ActionBar.tsx'
      to: 'frontend/src/lib/format-date.ts'
      via: 'greeting date'
      pattern: 'formatDayFirst'
    - from: 'frontend/src/pages/my-work/components/*.tsx'
      to: 'frontend/src/lib/format-locale.ts'
      via: 'number formatting locale'
      pattern: 'toFormatLocale'
---

<objective>
Migrate all pages/** and routes/** date+number sites (29 files) onto `lib/format-date.ts` /
`toFormatLocale`: the FMT-01 dashboard greeting (ActionBar) and Digest timestamps, the WeekAhead
special case, the named offender BriefsPage (shadow + toArDigits together), pages/MyTasks (same
overlap treatment), the my-work number surfaces, and the admin/dossier-list route sites.

Purpose: FMT-02 bulk migration, slice 4 of 4, plus the FMT-01 greeting+digest acceptance surface.
Parallel with 82-02, 82-03, and 82-07 (zero file overlap). Output: 29 migrated files + 1 flipped
test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-date-number-formatting/82-RESEARCH.md  (§1 groups + "Special cases" WeekAhead + Group C + §2.3 greeting note + §3.3 BriefsPage test row — NOTE: its 'ar-SA' inventory is INCOMPLETE; Digest/my-work/AssignmentQueue/CalendarTab/TopicsListPage/forums sites below come from a fresh 2026-07-04 re-grep)
@frontend/src/lib/format-date.ts  (post-82-01)
@frontend/src/lib/format-locale.ts  (post-82-01)

<interfaces>
Same import surface and helper-selection rule as plans 82-02/82-03:

```typescript
import { formatDayFirst, formatDayFirstYear, formatTime, formatDateTime } from '@/lib/format-date'
import { toFormatLocale } from '@/lib/format-locale' // NUMBER formatting locales only
```

Rule: options include `year` → formatDayFirstYear; time-only → formatTime; date+time combo (incl.
`.toLocaleString(locale, {...})` and `Intl.DateTimeFormat` with hour/minute) → formatDateTime;
compact chip/axis → date-fns 'd MMM'; else formatDayFirst. Delete orphaned locale vars. NUMBER
sites keep their call shape with the locale swapped to toFormatLocale(...). Output byte-identical
in en/ar for dates (Pitfall 6).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard greeting + Digest + WeekAhead + my-work + overlap files BriefsPage/MyTasks (15 files)</name>
  <files>frontend/src/pages/Dashboard/components/ActionBar.tsx, frontend/src/pages/Dashboard/components/AttentionItem.tsx, frontend/src/pages/Dashboard/widgets/WeekAhead.tsx, frontend/src/pages/Dashboard/widgets/MyTasks.tsx, frontend/src/pages/Dashboard/widgets/Digest.tsx, frontend/src/pages/my-work/components/WorkItemCard.tsx, frontend/src/pages/my-work/components/WorkSummaryHeader.tsx, frontend/src/pages/my-work/components/WorkItemTabs.tsx, frontend/src/pages/my-work/components/ProductivityMetrics.tsx, frontend/src/pages/my-work/components/TeamWorkloadPanel.tsx, frontend/src/pages/MyTasks.tsx, frontend/src/pages/Briefs/BriefsPage.tsx, frontend/src/pages/Briefs/__tests__/BriefsPage.test.tsx, frontend/src/pages/WorkingGroupsPage.tsx, frontend/src/pages/IntakeQueue.tsx</files>
  <read_first>82-RESEARCH.md §1 "Special cases" (WeekAhead), Group C table (BriefsPage/MyTasks shadows), §2.3 dashboard-greeting paragraph, §3.3 BriefsPage test row, Open Question 1 (pp unit — resolved: leave mono-Latin). Re-grep exact line numbers at task start (RESEARCH dated 2026-07-04 — line drift expected): `rg -n "'ar-SA'|toLocale|toArDigits|MMM" <file>`.</read_first>
  <action>
    ActionBar :55 (FMT-01 named surface): replace date-fns format(new Date(), 'EEEE, MMMM d,
    yyyy') with formatDayFirst(new Date()) — the greeting must read day-first no-comma (e.g.
    'Sat 04 Jul'); drop the orphaned date-fns import if unused after the edit.
    AttentionItem :96 → helper rule. Dashboard/widgets/MyTasks :71 'MMM d' → date-fns
    format(d, 'd MMM'). Digest.tsx :139-142 (previously unowned — MANDATORY, renders ON the
    dashboard so 82-06 Task 3's /[٠-٩]/ dashboard check depends on it): the digest-row
    r.timestamp .toLocaleString(i18n.language==='ar'?'ar-SA':'en-US', {month,day,hour,minute}) →
    formatDateTime(r.timestamp); keep the ' · ' separator and r.source untouched.
    WorkItemCard :114 'MMM d' → 'd MMM'.
    my-work NUMBER surfaces (previously unowned, D-82-05): WorkSummaryHeader :104
    stat.value.toLocaleString(isRTL?'ar-SA':'en-US') → toFormatLocale; WorkItemTabs :25
    new Intl.NumberFormat(i18n.language==='ar'?'ar-SA':'en') → toFormatLocale(i18n.language);
    ProductivityMetrics :46 numberLocale var 'ar-SA' → toFormatLocale; TeamWorkloadPanel :30
    NumberFormat same swap. Numbers keep their call shape — only the locale changes.
    WeekAhead (special case, RESEARCH §1): lines :60-61 build a weekday chip + day number as two
    separate strings — formatDayFirst cannot be split, so use date-fns format(d, 'EEE') for the
    weekday and format(d, 'dd') for the day number (Latin natively, day-first-compatible, does
    not trip the future month-first guard); its formatTimeRange at :42-52 uses
    toLocaleTimeString('ar-SA') 12-hour → compose two formatTime(...) calls instead (keep the
    range separator as-is); delete both 'ar-SA' locale vars (:42, :58).
    pages/MyTasks.tsx (overlap file — date AND digit edits together): replace the local
    formatDueDate at :99 (en-GB day/month) with formatDayFirst from '@/lib/format-date'; remove
    the toArDigits import at :60 and unwrap the call at :318.
    BriefsPage.tsx (named offender + overlap file): delete the local formatDayFirst shadow at
    :90-94 and import from '@/lib/format-date'; remove the toArDigits import at :22 and unwrap
    the calls at :423 and :445. The 'pp' unit at :423 stays mono-Latin after the unwrap
    (RESEARCH Open Question 1 recommendation adopted — no copy change, that is Phase 84).
    BriefsPage.test.tsx :203-204 currently asserts Arabic-Indic digits ARE present — invert to
    assert they are ABSENT and the rendered date/number strings are Latin (keep the rest of the
    test intact). ALSO strip every 'toArDigits' string mention from the test file: the header doc
    line :9 ("renders Arabic-Indic digits via toArDigits…") and the it() name at :197 — reword
    both to describe the Latin-digit behavior. The wave-4 `rg -l "toArDigits"` gate matches
    comments and test names, not just imports.
    WorkingGroupsPage :445 and IntakeQueue :527-528 (multi-line) → helper rule.
  </action>
  <verify>
    <automated>cd frontend && rg -n "toLocaleDateString|toLocaleTimeString|toLocaleString\(" src/pages/Dashboard src/pages/MyTasks.tsx src/pages/Briefs/BriefsPage.tsx src/pages/WorkingGroupsPage.tsx src/pages/IntakeQueue.tsx -g '!**/__tests__/**' | wc -l | grep -qx "0" && ! rg -q "toArDigits" src/pages/Briefs src/pages/MyTasks.tsx && rg -n "'ar-SA'" src/pages/Dashboard src/pages/my-work | wc -l | grep -qx "0" && ! rg -q "EEEE, MMMM" src/pages/Dashboard/components/ActionBar.tsx && pnpm vitest run src/pages/Briefs/__tests__/BriefsPage.test.tsx && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>Zero raw date/time formatting in the source files (incl. Digest); no toArDigits string anywhere under src/pages/Briefs (source OR test — comments and it() names included) nor in MyTasks.tsx; zero 'ar-SA' across src/pages/Dashboard and src/pages/my-work; ActionBar has no month-first literal; BriefsPage test green asserting Latin digits; type-check exit 0.</acceptance_criteria>
  <done>Greeting + digest day-first; WeekAhead Latin day-first; my-work numbers Latin; both overlap files fully resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Engagements workspace + queues + admin/dossier routes (14 files)</name>
  <files>frontend/src/pages/engagements/workspace/DocsTab.tsx, frontend/src/pages/engagements/workspace/AuditTab.tsx, frontend/src/pages/engagements/workspace/CalendarTab.tsx, frontend/src/pages/AssignmentQueue.tsx, frontend/src/pages/webhooks/WebhooksPage.tsx, frontend/src/pages/advanced-search/AdvancedSearchPage.tsx, frontend/src/pages/users/UsersListPage.tsx, frontend/src/routes/_protected/approvals/index.tsx, frontend/src/routes/_protected/admin/approvals.tsx, frontend/src/routes/_protected/admin/data-retention.tsx, frontend/src/routes/_protected/admin/field-permissions.tsx, frontend/src/routes/_protected/stakeholder-influence.tsx, frontend/src/routes/_protected/dossiers/topics/-TopicsListPage.tsx, frontend/src/routes/_protected/dossiers/forums/index.tsx</files>
  <read_first>82-RESEARCH.md §1 A2/A3/B tables + adjacent toLocaleString list. Re-grep exact line numbers at task start (RESEARCH dated 2026-07-04 — line drift expected): `rg -n "'ar-SA'|toLocale|DateTimeFormat" <file>`.</read_first>
  <action>
    Sites (re-grepped 2026-07-04): DocsTab :58; AuditTab :96 (date+time options in one call →
    formatDateTime); CalendarTab (previously unowned): the 'ar-SA' locale var at :73 feeds an
    Intl.DateTimeFormat at :76 → replace the formatter with the matching format-date helper per
    its options and delete the var (the file already imports toFormatLocale — remove that import
    too if the edit orphans it). AssignmentQueue (previously unowned): dateLocale var :19 + the
    created_at .toLocaleString(dateLocale) at :179 → formatDateTime, delete the var.
    WebhooksPage :435 (multi-line); AdvancedSearchPage :209 (multi-line); UsersListPage :338,:386
    (multi-line, archival last-login/created dates → formatDayFirstYear); approvals/index :117 →
    helper rule AND delete the dateLocale 'ar-SA' var at :46; admin/approvals :212 (multi-line);
    admin/data-retention :433,:645 (multi-line) + the toLocaleString date+time combo at :823 →
    formatDateTime; admin/field-permissions toLocaleString('ar-SA') :631 → formatDateTime
    (D-82-05: 'ar-SA' Indic source); stakeholder-influence route :614-615 (multi-line).
    Dossier list pages (previously unowned): topics/-TopicsListPage :67
    Intl.DateTimeFormat(isRTL?'ar-SA':'en-US', {month,day,year}) on updated_at →
    formatDayFirstYear; forums/index :71 identical shape → formatDayFirstYear.
    Apply the helper-selection rule throughout; delete orphaned locale vars/ternaries. Render
    path only. Out of scope (explicitly, do not touch): routes/_protected/positions/$id/
    approvals.tsx :168 and versions.tsx :129 and commitments/StatusTimeline.tsx :52
    toLocaleString combos — they become Latin automatically via the 82-01 toFormatLocale
    correction and are outside D-82-02's locked toLocaleDateString scope.
  </action>
  <verify>
    <automated>cd frontend && rg -n "toLocaleDateString|toLocaleTimeString" src/pages/engagements/workspace src/pages/webhooks src/pages/advanced-search src/pages/users src/routes/_protected/approvals src/routes/_protected/admin/approvals.tsx src/routes/_protected/admin/data-retention.tsx src/routes/_protected/admin/field-permissions.tsx src/routes/_protected/stakeholder-influence.tsx | wc -l | grep -qx "0" && rg -n "'ar-SA'" src/pages/engagements/workspace src/pages/AssignmentQueue.tsx src/routes/_protected/admin/field-permissions.tsx src/routes/_protected/approvals src/routes/_protected/dossiers/topics/-TopicsListPage.tsx src/routes/_protected/dossiers/forums/index.tsx | wc -l | grep -qx "0" && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>Zero raw date/time formatting across the 14 files; zero 'ar-SA' in the workspace/queue/route files (incl. the previously-unowned CalendarTab, AssignmentQueue, TopicsListPage, forums); type-check exit 0.</acceptance_criteria>
  <done>All pages/routes sites migrated; FMT-02 grep gate now reachable phase-wide.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                               |
| -------- | ------------------------------------------------------------------------- |
| none new | Render-time formatting of already-fetched data (RESEARCH Security Domain) |

## STRIDE Threat Register

| Threat ID | Category        | Component                           | Disposition | Mitigation Plan                                                                |
| --------- | --------------- | ----------------------------------- | ----------- | ------------------------------------------------------------------------------ |
| T-82-01   | Tampering (XSS) | formatted strings rendered in React | mitigate    | React text-node rendering only; no dangerouslySetInnerHTML in any touched file |
| T-82-SC   | Tampering       | package installs                    | accept      | No packages installed this phase                                               |

</threat_model>

<verification>
- Combined task gates → zero raw date/time sites and zero 'ar-SA' across all 29 files
- `cd frontend && pnpm vitest run src/pages` — no regressions
- `cd frontend && pnpm type-check` exit 0
- Phase-wide FMT-02 gate (should now pass once 82-02/82-03/82-07 also land): `rg -n "toLocaleDateString" frontend/src -g '!**/__tests__/**' | grep -v "lib/format-date.ts" | grep -v "components/ui/calendar.tsx" | wc -l` → 0
</verification>

<success_criteria>
Greeting + digest day-first no-comma (D-82-01); all pages/routes date sites on the 4-helper
surface (D-82-02 incl. BriefsPage named offender + AssignmentQueue/CalendarTab/dossier-list
adds); my-work numbers Latin via toFormatLocale; overlap files' digit unwraps done (D-82-05
share); BriefsPage test asserts Latin and carries no toArDigits string.
</success_criteria>

<output>
Create `.planning/phases/82-date-number-formatting/82-04-SUMMARY.md` when done.
</output>
