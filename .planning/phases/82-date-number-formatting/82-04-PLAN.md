---
phase: 82-date-number-formatting
plan: 04
type: execute
wave: 2
depends_on: [82-01]
files_modified:
  - frontend/src/pages/Dashboard/components/ActionBar.tsx
  - frontend/src/pages/Dashboard/components/AttentionItem.tsx
  - frontend/src/pages/Dashboard/widgets/WeekAhead.tsx
  - frontend/src/pages/Dashboard/widgets/MyTasks.tsx
  - frontend/src/pages/my-work/components/WorkItemCard.tsx
  - frontend/src/pages/MyTasks.tsx
  - frontend/src/pages/Briefs/BriefsPage.tsx
  - frontend/src/pages/Briefs/__tests__/BriefsPage.test.tsx
  - frontend/src/pages/WorkingGroupsPage.tsx
  - frontend/src/pages/IntakeQueue.tsx
  - frontend/src/pages/engagements/workspace/DocsTab.tsx
  - frontend/src/pages/engagements/workspace/AuditTab.tsx
  - frontend/src/pages/webhooks/WebhooksPage.tsx
  - frontend/src/pages/advanced-search/AdvancedSearchPage.tsx
  - frontend/src/pages/users/UsersListPage.tsx
  - frontend/src/routes/_protected/approvals/index.tsx
  - frontend/src/routes/_protected/admin/approvals.tsx
  - frontend/src/routes/_protected/admin/data-retention.tsx
  - frontend/src/routes/_protected/admin/field-permissions.tsx
  - frontend/src/routes/_protected/stakeholder-influence.tsx
autonomous: true
requirements: [FMT-01, FMT-02, FMT-04]
must_haves:
  truths:
    - "Satisfies D-82-01 (greeting half): the dashboard greeting date (ActionBar) reads day-first no-comma via formatDayFirst — no 'EEEE, MMMM d, yyyy' month-first comma shape remains"
    - "Satisfies D-82-02: every ad-hoc toLocaleDateString date render in this plan's pages/** and routes/** files routes through lib/format-date helpers, including named offender Briefs/BriefsPage.tsx (en-GB local shadow deleted)"
    - "Satisfies D-82-05: BriefsPage and pages/MyTasks toArDigits wraps removed in the same edit as their shadow removal (Pitfall 4 overlap files owned entirely by this plan); field-permissions' 'ar-SA' toLocaleString gone; BriefsPage test flipped from asserting Indic-present to Latin"
    - "WeekAhead's weekday-chip/day-number split uses date-fns 'EEE'/'dd' (Latin, day-first-compatible) and its time range uses formatTime — no 'ar-SA' 12-hour time remains"
  artifacts:
    - path: 'frontend/src/pages/Dashboard/components/ActionBar.tsx'
      provides: 'day-first greeting'
      contains: 'formatDayFirst'
    - path: 'frontend/src/pages/Briefs/BriefsPage.tsx'
      provides: 'named offender migrated + digit unwrap'
      contains: '@/lib/format-date'
  key_links:
    - from: 'frontend/src/pages/Dashboard/components/ActionBar.tsx'
      to: 'frontend/src/lib/format-date.ts'
      via: 'greeting date'
      pattern: 'formatDayFirst'
    - from: 'frontend/src/pages/Briefs/BriefsPage.tsx'
      to: 'frontend/src/lib/format-date.ts'
      via: 'import replacing local shadow'
      pattern: '@/lib/format-date'
---

<objective>
Migrate all pages/** and routes/** date sites (20 files) onto `lib/format-date.ts`: the FMT-01
dashboard greeting (ActionBar), the WeekAhead special case, the named offender BriefsPage (shadow
+ toArDigits together), pages/MyTasks (same overlap treatment), and the admin/route sites.

Purpose: FMT-02 bulk migration, slice 3 of 3, plus the FMT-01 greeting acceptance surface.
Parallel with 82-02 and 82-03 (zero file overlap). Output: 20 migrated files + 1 flipped test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-date-number-formatting/82-RESEARCH.md  (§1 groups + "Special cases" WeekAhead + Group C + §2.3 greeting note + §3.3 BriefsPage test row)
@frontend/src/lib/format-date.ts  (post-82-01)

<interfaces>
Same import surface and helper-selection rule as plans 82-02/82-03:

```typescript
import { formatDayFirst, formatDayFirstYear, formatTime, formatDateTime } from '@/lib/format-date'
```

Rule: options include `year` → formatDayFirstYear; time-only → formatTime; date+time combo →
formatDateTime; compact chip/axis → date-fns 'd MMM'; else formatDayFirst. Delete orphaned locale
vars. Output byte-identical in en/ar (Pitfall 6).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard greeting + WeekAhead + overlap files BriefsPage/MyTasks (10 files)</name>
  <files>frontend/src/pages/Dashboard/components/ActionBar.tsx, frontend/src/pages/Dashboard/components/AttentionItem.tsx, frontend/src/pages/Dashboard/widgets/WeekAhead.tsx, frontend/src/pages/Dashboard/widgets/MyTasks.tsx, frontend/src/pages/my-work/components/WorkItemCard.tsx, frontend/src/pages/MyTasks.tsx, frontend/src/pages/Briefs/BriefsPage.tsx, frontend/src/pages/Briefs/__tests__/BriefsPage.test.tsx, frontend/src/pages/WorkingGroupsPage.tsx, frontend/src/pages/IntakeQueue.tsx</files>
  <read_first>82-RESEARCH.md §1 "Special cases" (WeekAhead), Group C table (BriefsPage/MyTasks shadows), §2.3 dashboard-greeting paragraph, §3.3 BriefsPage test row, Open Question 1 (pp unit — resolved: leave mono-Latin).</read_first>
  <action>
    ActionBar :55 (FMT-01 named surface): replace date-fns format(new Date(), 'EEEE, MMMM d,
    yyyy') with formatDayFirst(new Date()) — the greeting must read day-first no-comma (e.g.
    'Sat 04 Jul'); drop the orphaned date-fns import if unused after the edit.
    AttentionItem :96 → helper rule. Dashboard/widgets/MyTasks :71 'MMM d' → date-fns
    format(d, 'd MMM'). WorkItemCard :114 'MMM d' → 'd MMM'.
    WeekAhead (special case, RESEARCH §1): lines :60-61 build a weekday chip + day number as two
    separate strings — formatDayFirst cannot be split, so use date-fns format(d, 'EEE') for the
    weekday and format(d, 'dd') for the day number (Latin natively, day-first-compatible, does not
    trip the future month-first guard); its formatTimeRange at :42-52 uses
    toLocaleTimeString('ar-SA') 12-hour → compose two formatTime(...) calls instead (keep the
    range separator as-is).
    pages/MyTasks.tsx (overlap file — date AND digit edits together): replace the local
    formatDueDate at :99 (en-GB day/month) with formatDayFirst from '@/lib/format-date'; remove
    the toArDigits import at :60 and unwrap the call at :318.
    BriefsPage.tsx (named offender + overlap file): delete the local formatDayFirst shadow at
    :90-94 and import from '@/lib/format-date'; remove the toArDigits import at :22 and unwrap
    the calls at :423 and :445. The 'pp' unit at :423 stays mono-Latin after the unwrap
    (RESEARCH Open Question 1 recommendation adopted — no copy change, that is Phase 84).
    BriefsPage.test.tsx :203-204 currently asserts Arabic-Indic digits ARE present — invert to
    assert they are ABSENT and the rendered date/number strings are Latin (keep the rest of the
    test intact). WorkingGroupsPage :445 and IntakeQueue :527 (multi-line) → helper rule.
  </action>
  <verify>
    <automated>cd frontend && rg -n "toLocaleDateString|toLocaleTimeString" src/pages/Dashboard src/pages/my-work src/pages/MyTasks.tsx src/pages/Briefs/BriefsPage.tsx src/pages/WorkingGroupsPage.tsx src/pages/IntakeQueue.tsx -g '!**/__tests__/**' | wc -l | grep -qx "0" && ! rg -q "toArDigits" src/pages/Briefs/BriefsPage.tsx src/pages/MyTasks.tsx && ! rg -q "EEEE, MMMM" src/pages/Dashboard/components/ActionBar.tsx && pnpm vitest run src/pages/Briefs/__tests__/BriefsPage.test.tsx && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>Zero raw date/time formatting in the 9 source files; no toArDigits in BriefsPage/MyTasks; ActionBar has no month-first literal; BriefsPage test green asserting Latin digits; type-check exit 0.</acceptance_criteria>
  <done>Greeting day-first; WeekAhead Latin day-first; both overlap files fully resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Engagements workspace + admin routes + remaining pages (10 files)</name>
  <files>frontend/src/pages/engagements/workspace/DocsTab.tsx, frontend/src/pages/engagements/workspace/AuditTab.tsx, frontend/src/pages/webhooks/WebhooksPage.tsx, frontend/src/pages/advanced-search/AdvancedSearchPage.tsx, frontend/src/pages/users/UsersListPage.tsx, frontend/src/routes/_protected/approvals/index.tsx, frontend/src/routes/_protected/admin/approvals.tsx, frontend/src/routes/_protected/admin/data-retention.tsx, frontend/src/routes/_protected/admin/field-permissions.tsx, frontend/src/routes/_protected/stakeholder-influence.tsx</files>
  <read_first>82-RESEARCH.md §1 A2/A3/B tables + adjacent toLocaleString list for line numbers.</read_first>
  <action>
    Sites: DocsTab :58; AuditTab :96 (date+time options in one call → formatDateTime);
    WebhooksPage :435 (multi-line); AdvancedSearchPage :209 (multi-line); UsersListPage :338,386
    (multi-line, archival last-login/created dates → formatDayFirstYear); approvals/index :117;
    admin/approvals :212 (multi-line); admin/data-retention :433,645 (multi-line) + the
    toLocaleString date+time combo at :823 → formatDateTime; admin/field-permissions
    toLocaleString('ar-SA') :631 → formatDateTime (D-82-05: 'ar-SA' Indic source);
    stakeholder-influence route :614 (multi-line).
    Apply the helper-selection rule throughout; delete orphaned locale vars/ternaries. Render path
    only. Out of scope (explicitly, do not touch): routes/_protected/positions/$id/approvals.tsx
    :168 and versions.tsx :129 and commitments/StatusTimeline.tsx :52 toLocaleString combos — they
    become Latin automatically via the 82-01 toFormatLocale correction and are outside D-82-02's
    locked toLocaleDateString scope.
  </action>
  <verify>
    <automated>cd frontend && rg -n "toLocaleDateString|toLocaleTimeString" src/pages/engagements/workspace src/pages/webhooks src/pages/advanced-search src/pages/users src/routes/_protected/approvals src/routes/_protected/admin/approvals.tsx src/routes/_protected/admin/data-retention.tsx src/routes/_protected/admin/field-permissions.tsx src/routes/_protected/stakeholder-influence.tsx | wc -l | grep -qx "0" && ! rg -q "'ar-SA'" src/routes/_protected/admin/field-permissions.tsx && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>Zero raw date/time formatting across the 10 files; field-permissions has no 'ar-SA'; type-check exit 0.</acceptance_criteria>
  <done>All pages/routes sites migrated; FMT-02 grep gate now reachable phase-wide.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                               |
| -------- | ------------------------------------------------------------------------- |
| none new | Render-time formatting of already-fetched data (RESEARCH Security Domain) |

## STRIDE Threat Register

| Threat ID | Category        | Component                           | Disposition | Mitigation Plan                                                                |
| --------- | --------------- | ----------------------------------- | ----------- | ------------------------------------------------------------------------------ |
| T-82-01   | Tampering (XSS) | formatted strings rendered in React | mitigate    | React text-node rendering only; no dangerouslySetInnerHTML in any touched file |
| T-82-SC   | Tampering       | package installs                    | accept      | No packages installed this phase                                               |

</threat_model>

<verification>
- Combined task gates → zero raw date/time sites across all 20 files
- `cd frontend && pnpm vitest run src/pages` — no regressions
- `cd frontend && pnpm type-check` exit 0
- Phase-wide FMT-02 gate (should now pass once 82-02/82-03 also land): `rg -n "toLocaleDateString" frontend/src -g '!**/__tests__/**' | grep -v "lib/format-date.ts" | grep -v "components/ui/calendar.tsx" | wc -l` → 0
</verification>

<success_criteria>
Greeting day-first no-comma (D-82-01); all pages/routes date sites on the 4-helper surface
(D-82-02 incl. BriefsPage named offender); overlap files' digit unwraps done (D-82-05 share);
BriefsPage test asserts Latin.
</success_criteria>

<output>
Create `.planning/phases/82-date-number-formatting/82-04-SUMMARY.md` when done.
</output>
