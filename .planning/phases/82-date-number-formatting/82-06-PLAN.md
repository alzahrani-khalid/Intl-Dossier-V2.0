---
phase: 82-date-number-formatting
plan: 06
type: execute
wave: 4
depends_on: [82-05]
files_modified:
  - scripts/check-date-formatting.mjs
  - scripts/fixtures/check-date-formatting/offender.tsx
  - frontend/package.json
autonomous: true
requirements: [FMT-03]
must_haves:
  truths:
    - "Satisfies D-82-03: a standalone script guard (scripts/check-date-formatting.mjs, NOT an ESLint rule — flat-config 'off' overrides would silently swallow it in ~18 offender files per RESEARCH §5) fails CI on new raw toLocaleDateString outside lib/format-date.ts and on month-first date-fns format literals"
    - 'Satisfies D-82-03: the guard is wired into `pnpm lint` (frontend/package.json lint chain), making it CI-blocking with zero workflow changes; it exits 0 on the now-clean tree and exits 1 on the committed positive-failure fixture'
    - 'The allowlist contains exactly lib/format-date.ts (the single allowed call site) and components/ui/calendar.tsx (react-day-picker data attribute, the one non-user-visible site per RESEARCH §1 Group D) — no seeded offender allowlist'
    - 'Satisfies D-82-05 (app-wide lock): zero raw ''ar-SA'' locale literals remain anywhere in non-test frontend source — the one-time phase gate `rg "''ar-SA''" frontend/src` (tests excluded) returns nothing, proving the Waves 1-3 migrations covered the full re-grepped inventory'
    - 'Satisfies D-82-01/D-82-05 (phase acceptance): the AR UI render check finds zero Arabic-Indic digits on dashboard/kanban/calendar at 1400 and 1024, and the greeting reads day-first no-comma'
  artifacts:
    - path: 'scripts/check-date-formatting.mjs'
      provides: 'regression guard (two checks + allowlist + fixture arg)'
      min_lines: 60
    - path: 'scripts/fixtures/check-date-formatting/offender.tsx'
      provides: 'positive-failure fixture proving the guard fires'
  key_links:
    - from: 'frontend/package.json'
      to: 'scripts/check-date-formatting.mjs'
      via: 'lint script chain'
      pattern: 'check-date-formatting'
---

<objective>
Land the FMT-03 regression guard LAST, on the tree the earlier waves cleaned, and run the phase's
end-to-end acceptance checks (grep gates incl. the phase-wide 'ar-SA' zero gate, full suite, AR
no-Indic render check).

Purpose: lock the phase's invariants against regression. Output: guard script + fixture, lint
wiring, verified phase gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-date-number-formatting/82-RESEARCH.md  (§5 guard design in full — detection regexes verified against the live corpus)
@.planning/phases/82-date-number-formatting/82-VALIDATION.md  (Requirements → Test Map, Manual-Only Verifications)
@scripts/check-i18n-namespaces.mjs  (the template to mirror: node:fs walk, CLI dir arg, exit semantics, offender reporting)
@frontend/package.json  (line 17: the lint chain to append to; note it runs from repo root via `cd ..`)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: check-date-formatting.mjs guard + positive-failure fixture</name>
  <files>scripts/check-date-formatting.mjs, scripts/fixtures/check-date-formatting/offender.tsx</files>
  <read_first>scripts/check-i18n-namespaces.mjs in full (structure, walk, CLI arg, reporting), 82-RESEARCH.md §5 (both check designs + the verified month-first detector regexes + allowlist).</read_first>
  <behavior>
    - `node scripts/check-date-formatting.mjs` on the clean tree → exit 0
    - `node scripts/check-date-formatting.mjs scripts/fixtures/check-date-formatting` → exit 1, naming offender.tsx with file:line for BOTH a raw toLocaleDateString hit and a month-first date-fns hit
    - Allowlisted files (frontend/src/lib/format-date.ts, frontend/src/components/ui/calendar.tsx) never flagged
  </behavior>
  <action>
    Write the fixture FIRST: scripts/fixtures/check-date-formatting/offender.tsx containing (a) a
    line calling .toLocaleDateString('en-US') on a Date and (b) a date-fns call with the
    month-first literal 'MMM d, yyyy' — this proves the guard fires (RED until the script exists).
    Then write scripts/check-date-formatting.mjs mirroring check-i18n-namespaces.mjs: dependency-
    free node:fs recursive walk of frontend/src/**/*.{ts,tsx} (skip **/__tests__/** and
    *.test.ts/*.test.tsx — mirrors the FMT-02 grep gate scope), CLI dir arg retargets the scan
    root for the fixture. Check 1: flag any line matching the raw-toLocaleDateString pattern from
    RESEARCH §5, with an exact-relative-path allowlist of frontend/src/lib/format-date.ts and
    frontend/src/components/ui/calendar.tsx (data-attribute use). Check 2: capture date-fns format
    literals with the FMT_CALL regex from RESEARCH §5 and flag when a textual month token (MMM+)
    precedes a standalone day token (d/dd) — the monthFirst function is given verbatim in RESEARCH
    §5 and is verified to flag 'MMM d, yyyy'/'EEEE, MMMM d, yyyy'/'MMM dd, yyyy'/'MMM d' while
    passing 'd MMM'/'d MMM yyyy'/'dd MMM yyyy'/'EEE'/'MMMM yyyy'/'d'. Report each hit as
    'file:line — offending code', exit 1 on any hit, exit 0 clean. Header comment documents
    purpose (D-82-03), the allowlist rationale, and why this is a script not an ESLint rule
    (flat-config 'off' overrides, RESEARCH §5). No new dependencies.
  </action>
  <verify>
    <automated>node scripts/check-date-formatting.mjs && ! node scripts/check-date-formatting.mjs scripts/fixtures/check-date-formatting</automated>
  </verify>
  <acceptance_criteria>Guard exit 0 on frontend/src; exit 1 on the fixture dir naming both offender lines; allowlisted files untouched by check 1.</acceptance_criteria>
  <done>Guard exists, self-proven in both directions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire guard into pnpm lint + run phase-wide automated gates</name>
  <files>frontend/package.json</files>
  <read_first>frontend/package.json line 17 (the existing lint chain — it already cd's to repo root and chains three check-*.mjs scripts).</read_first>
  <action>
    Append `&& node scripts/check-date-formatting.mjs` to the end of the "lint" script in
    frontend/package.json (after check-bootstrap-parity.mjs; the chain already runs from repo
    root). Then run the full phase gates: (1) `cd frontend && pnpm lint` — must exit 0 with the
    guard in the chain; (2) the FMT-02 grep gate; (3) the FMT-04 grep gate; (4) the D-82-05
    app-wide 'ar-SA' gate — zero raw 'ar-SA' locale literals in non-test frontend source (the
    Waves 1-3 plans each own their slice of the re-grepped inventory; this gate proves no
    straggler was missed); (5) full frontend vitest suite. If any migration stragglers surface
    (line numbers drifted since RESEARCH, or an 'ar-SA' site the wave plans missed), fix them
    with the same helper-selection rule from the Wave-2 plans (dates → format-date helpers,
    numbers → toFormatLocale) rather than allowlisting — the guard ships with an empty offender
    list (RESEARCH §5 ordering constraint).
  </action>
  <verify>
    <automated>cd frontend && pnpm lint && rg -n "toLocaleDateString" src -g '!**/__tests__/**' | grep -v "lib/format-date.ts" | grep -v "components/ui/calendar.tsx" | wc -l | grep -qx "0" && rg -l "toArDigits" src | wc -l | grep -qx "0" && rg -n "'ar-SA'" src -g '!**/__tests__/**' -g '!*.test.*' | wc -l | grep -qx "0" && pnpm vitest run</automated>
  </verify>
  <acceptance_criteria>pnpm lint green including the new guard; FMT-02 gate 0 lines; toArDigits 0 files; app-wide 'ar-SA' gate 0 lines in non-test source (D-82-05 lock); full vitest suite green.</acceptance_criteria>
  <done>Guard CI-blocking; every phase grep gate green.</done>
</task>

<task type="auto">
  <name>Task 3: AR render verification — no Indic digits, day-first greeting (D-82-06 DoD)</name>
  <files></files>
  <read_first>82-VALIDATION.md "Manual-Only Verifications" table; MEMORY note: language persists under localStorage key `id.locale` (NOT i18nextLng); visual verification uses a dedicated headless Chrome via browser-harness/agent-browser (cannot attach the user's Chrome).</read_first>
  <action>
    Start the dev stack (frontend on :5173; backend PORT=5001 per the AirPlay note), log in with
    the .env.test credentials, set localStorage['id.locale'] = 'ar' and reload. On EACH of:
    the dashboard route, the kanban board route (locate via rg for the WorkBoard route file), and
    the calendar route — at BOTH 1400px and 1024px viewport widths — evaluate in the page:
    (1) /[٠-٩]/.test(document.body.textContent) must be false (zero Arabic-Indic digits);
    (2) on the dashboard, the greeting date must be day-first no-comma (shape like 'Sat 04 Jul',
    no comma, weekday first) AND the Intelligence Digest row timestamps must read day-first
    (82-04 migrated Digest.tsx onto formatDateTime — it renders on this same dashboard);
    (3) on the kanban board, any visible overdue chip must read 'متأخر <Latin digits> يوم' with
    no bare Latin 'd' adjacent to Arabic text. Repeat check (2) in EN
    (localStorage['id.locale'] = 'en'). Capture screenshots of dashboard AR\@1400 and
    kanban AR\@1400 as evidence in the SUMMARY. If browser automation is unavailable in this
    environment, report the exact manual steps and mark this verification as pending human
    confirmation in the SUMMARY — do NOT claim it passed without DOM evidence.
  </action>
  <verify>
    <automated>MISSING — app-wide bidi/visual render cannot be jsdom-asserted; evidence is the CDP DOM probe results + screenshots recorded in the SUMMARY (per 82-VALIDATION.md Manual-Only Verifications)</automated>
    <human-check>Dashboard/kanban/calendar in AR show zero Arabic-Indic digits at 1400 and 1024; greeting + digest timestamps day-first no-comma in EN and AR; kanban overdue chip 'متأخر N يوم'</human-check>
  </verify>
  <acceptance_criteria>DOM probe `/[٠-٩]/.test(document.body.textContent) === false` on all three AR routes at both widths; greeting day-first regex match; overdue chip Latin-digit + Arabic unit — all recorded with evidence in the SUMMARY.</acceptance_criteria>
  <done>Locked observable outcome verified by render, not just grep (Pitfall 5 closed).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary      | Description                                                                             |
| ------------- | --------------------------------------------------------------------------------------- |
| CI lint chain | New script executes at lint time; reads repo files only, dependency-free node: builtins |

## STRIDE Threat Register

| Threat ID | Category  | Component                 | Disposition | Mitigation Plan                                                                                                     |
| --------- | --------- | ------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------- |
| T-82-02   | Tampering | check-date-formatting.mjs | mitigate    | Dependency-free (node:fs/node:path only), no network, no eval; mirrors the vetted check-i18n-namespaces.mjs pattern |
| T-82-SC   | Tampering | package installs          | accept      | No packages installed this phase                                                                                    |

</threat_model>

<verification>
- `node scripts/check-date-formatting.mjs` → exit 0; fixture arg → exit 1
- `cd frontend && pnpm lint` → exit 0 (guard in chain, CI-blocking via the existing Lint workflow)
- FMT-02 gate 0 lines; `rg -l toArDigits frontend/src | wc -l` → 0
- App-wide D-82-05 gate: `rg -n "'ar-SA'" frontend/src -g '!**/__tests__/**' -g '!*.test.*' | wc -l` → 0
- `cd frontend && pnpm vitest run` full suite green
- AR render evidence in SUMMARY (three routes × two widths, no `٠-٩`)
</verification>

<success_criteria>
FMT-03 complete: guard active, CI-blocking, self-proven via fixture, empty allowlist beyond the
two designed exemptions (D-82-03). App-wide 'ar-SA' zero gate green (D-82-05 lock). Phase
acceptance evidence captured for FMT-01/FMT-04 render truths (D-82-01/D-82-05).
</success_criteria>

<output>
Create `.planning/phases/82-date-number-formatting/82-06-SUMMARY.md` when done.
</output>
