---
phase: 82-date-number-formatting
plan: 02
type: execute
wave: 2
depends_on: [82-01]
files_modified:
  - frontend/src/components/analytics/RelationshipHealthChart.tsx
  - frontend/src/components/analytics/EngagementMetricsChart.tsx
  - frontend/src/components/analytics/CommitmentFulfillmentChart.tsx
  - frontend/src/components/analytics/WorkloadDistributionChart.tsx
  - frontend/src/components/analytics/SummaryCard.tsx
  - frontend/src/components/sla-monitoring/SLAComplianceChart.tsx
  - frontend/src/components/sla-monitoring/SLAOverviewCards.tsx
  - frontend/src/components/sla-monitoring/SLAComplianceTable.tsx
  - frontend/src/components/sla-monitoring/SLAEscalationsList.tsx
  - frontend/src/components/audit-logs/AuditLogStatistics.tsx
  - frontend/src/components/audit-logs/AuditLogFilters.tsx
  - frontend/src/components/activity-feed/ActivityFeedFilters.tsx
  - frontend/src/components/tasks/TaskDetail.tsx
  - frontend/src/components/tasks/TaskCard.tsx
  - frontend/src/components/tasks/SLAIndicator.tsx
  - frontend/src/components/stakeholder-influence/InfluenceReport.tsx
  - frontend/src/components/stakeholder-influence/InfluenceMetricsPanel.tsx
  - frontend/src/components/stakeholder-timeline/StakeholderTimelineCard.tsx
  - frontend/src/components/field-history/FieldHistoryTimeline.tsx
autonomous: true
requirements: [FMT-02, FMT-04]
must_haves:
  truths:
    - "Satisfies D-82-02: every ad-hoc toLocaleDateString/toLocaleTimeString date render in this plan's 19 component files routes through lib/format-date helpers (render path only — no data/timestamp changes), and every date+time toLocaleString combo (TaskDetail, TaskCard, SLAIndicator, SLAEscalationsList, FieldHistoryTimeline) routes through formatDateTime"
    - "Satisfies D-82-05: zero raw 'ar-SA' locale literals remain in the 19 files — every locale-aware NUMBER site (analytics charts, SLAOverviewCards, SLAComplianceTable, AuditLogStatistics counts, EntityComparisonTable-excluded, FieldHistoryTimeline NumberFormat) formats via toFormatLocale(...) so AR renders Latin digits"
    - "Chart axis date ticks use date-fns 'd MMM' (day-first, guard-clean per RESEARCH Open Question 3)"
    - 'Satisfies D-82-06: no carve-out file touched; only render-path edits in the listed components'
  artifacts:
    - path: 'frontend/src/components/sla-monitoring/SLAOverviewCards.tsx'
      provides: 'SLA KPI numbers Latin-safe'
      contains: 'toFormatLocale'
    - path: 'frontend/src/components/tasks/TaskCard.tsx'
      provides: 'task card timestamps via canonical formatter'
      contains: '@/lib/format-date'
  key_links:
    - from: 'frontend/src/components/analytics/*.tsx'
      to: 'frontend/src/lib/format-locale.ts'
      via: 'number formatting locale'
      pattern: 'toFormatLocale'
    - from: "this plan's date sites"
      to: 'frontend/src/lib/format-date.ts'
      via: "import { formatDayFirst, formatDayFirstYear, formatTime, formatDateTime } from '@/lib/format-date'"
      pattern: '@/lib/format-date'
---

<objective>
Migrate the analytics/sla-monitoring/audit/tasks/stakeholder slice of the date+number inventory
(19 component files) onto the corrected `lib/format-date.ts` helpers, and eliminate every raw
`'ar-SA'` Indic number source in these files via `toFormatLocale`.

Purpose: FMT-02 bulk migration, slice 1 of 4. Runs in parallel with 82-03, 82-04, and 82-07
(zero file overlap). Output: 19 migrated component files, per-file mechanical edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-date-number-formatting/82-RESEARCH.md  (§1 inventory groups, "Typical Group-A migration" example, §3.4 items 4–5 — NOTE: its 'ar-SA' inventory is INCOMPLETE; the file/line lists in the tasks below are the authoritative re-grepped set, 2026-07-04)
@frontend/src/lib/format-date.ts  (post-82-01: the 4-helper surface)
@frontend/src/lib/format-locale.ts  (post-82-01: 'ar-u-nu-latn')

<interfaces>
Import surface (established by plan 82-01 — use directly, no exploration needed):

```typescript
import { formatDayFirst, formatDayFirstYear, formatTime, formatDateTime } from '@/lib/format-date'
// formatDayFirst(d)      → 'Tue 28 Apr'          — near-term dates, list rows, chips
// formatDayFirstYear(d)  → '28 Apr 2026'         — archival dates (current options include year)
// formatTime(d)          → '14:30 GST'           — replaces toLocaleTimeString
// formatDateTime(d)      → 'Tue 28 Apr 14:30 GST' — replaces date+time toLocaleString combos
import { toFormatLocale } from '@/lib/format-locale' // for NUMBER formatting locales only
```

Helper-selection rule (apply mechanically per site): current options include `year` →
`formatDayFirstYear`; site renders time only → `formatTime`; site renders date+time in one string
(incl. bare `.toLocaleString(locale)` and `.toLocaleString(locale, { dateStyle/timeStyle or
month+day+hour+minute })`) → `formatDateTime`; chart axis tick/tooltip → date-fns
`format(d, 'd MMM')`; everything else → `formatDayFirst`. All existing locale
arguments/ternaries (`isRTL ? 'ar-SA' : 'en-US'`, local `locale` vars, `toFormatLocale(...)`)
are DELETED at date sites — the helpers take the date alone. NUMBER sites (`.toLocaleString(...)`
on a number, `Intl.NumberFormat(...)`) KEEP their call shape but the locale argument becomes
`toFormatLocale(<the file's existing language/isRTL source>)`. Output is byte-identical in en and
ar for dates (Pitfall 6: do NOT localize month names for AR).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate analytics charts + sla-monitoring slice (9 files)</name>
  <files>frontend/src/components/analytics/RelationshipHealthChart.tsx, frontend/src/components/analytics/EngagementMetricsChart.tsx, frontend/src/components/analytics/CommitmentFulfillmentChart.tsx, frontend/src/components/analytics/WorkloadDistributionChart.tsx, frontend/src/components/analytics/SummaryCard.tsx, frontend/src/components/sla-monitoring/SLAComplianceChart.tsx, frontend/src/components/sla-monitoring/SLAOverviewCards.tsx, frontend/src/components/sla-monitoring/SLAComplianceTable.tsx, frontend/src/components/sla-monitoring/SLAEscalationsList.tsx</files>
  <read_first>82-RESEARCH.md §1 (Group A/B tables) and §3.4 item 4 (chart number lines). Re-grep exact line numbers at task start (`rg -n "'ar-SA'|toLocaleDateString|toLocaleTimeString" <file>`) — RESEARCH dated 2026-07-04, line drift expected. Read each target file only around the located lines (±15 lines).</read_first>
  <action>
    Per-file site list (line numbers re-grepped 2026-07-04 — re-locate by grep at task start):
    RelationshipHealthChart date tick :99 → date-fns format(d, 'd MMM'); numbers :46,:67 →
    toFormatLocale. EngagementMetricsChart tick :104; numbers :45,:65. CommitmentFulfillmentChart
    tick :111; numbers :46,:66. WorkloadDistributionChart numbers :44,:64. SummaryCard number :39.
    SLAComplianceChart date tick :68 → 'd MMM'; number :41 → toFormatLocale (previously unowned —
    the number tooltip at :41 is the same shape as the analytics charts).
    SLAOverviewCards :76,:83,:91 — three KPI count .toLocaleString(isRTL?'ar-SA':'en-US') →
    toFormatLocale(i18n.language or the file's existing language source).
    SLAComplianceTable :109,:114,:120,:177 — four row-count numbers, same toFormatLocale swap.
    SLAEscalationsList :119 — formatTimestamp's .toLocaleString(isRTL?'ar-SA':'en-US',
    {month,day,hour,minute}) → formatDateTime(timestamp) (date+time combo per the helper rule);
    delete the isRTL locale logic the edit orphans.
    Chart axis ticks/tooltips use date-fns format(d, 'd MMM') (compact, day-first, guard-clean —
    RESEARCH Open Question 3 recommendation adopted; date-fns is already imported or importable).
    Number edits (D-82-05): numbers stay locale-aware via toFormatLocale — now Latin in AR. Remove
    locale ternaries/vars that become unused after migration (only the ones YOUR edit orphans). Do
    not reformat surrounding code; each changed line traces to a listed site.
  </action>
  <verify>
    <automated>cd frontend && rg -n "toLocaleDateString|toLocaleTimeString" src/components/analytics src/components/sla-monitoring | wc -l | grep -qx "0" && rg -n "'ar-SA'" src/components/analytics src/components/sla-monitoring | wc -l | grep -qx "0" && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>Zero toLocaleDateString/toLocaleTimeString and zero 'ar-SA' literals remain across src/components/analytics and src/components/sla-monitoring; type-check exit 0; chart date ticks render day-first 'd MMM'; SLA KPI numbers route through toFormatLocale.</acceptance_criteria>
  <done>9 files migrated; charts + SLA monitoring Latin-number + day-first-tick.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate audit/activity/tasks/stakeholder/field-history slice (10 files)</name>
  <files>frontend/src/components/audit-logs/AuditLogStatistics.tsx, frontend/src/components/audit-logs/AuditLogFilters.tsx, frontend/src/components/activity-feed/ActivityFeedFilters.tsx, frontend/src/components/tasks/TaskDetail.tsx, frontend/src/components/tasks/TaskCard.tsx, frontend/src/components/tasks/SLAIndicator.tsx, frontend/src/components/stakeholder-influence/InfluenceReport.tsx, frontend/src/components/stakeholder-influence/InfluenceMetricsPanel.tsx, frontend/src/components/stakeholder-timeline/StakeholderTimelineCard.tsx, frontend/src/components/field-history/FieldHistoryTimeline.tsx</files>
  <read_first>82-RESEARCH.md §1 (A1/A2/B tables). Re-grep exact line numbers at task start (RESEARCH dated 2026-07-04 — line drift expected): `rg -n "'ar-SA'|toLocaleDateString|toLocaleTimeString|toLocaleString\(" <file>`.</read_first>
  <action>
    Per-file site list (re-grepped 2026-07-04):
    AuditLogStatistics — dates :127,:131 → helper rule; NUMBER counts :139,:159,:199
    (.toLocaleString(isRTL?'ar-SA':'en-US') on event counts) → toFormatLocale (previously
    unowned). AuditLogFilters :498,:500 → helper rule. ActivityFeedFilters :746,:748 → helper
    rule.
    TaskDetail — the multi-line toLocaleDateString at :273 → helper rule, PLUS the three
    date+time .toLocaleString(isRTL?'ar-SA':'en-US') combos at :216,:226,:237
    (created_at/updated_at/completed_at, previously unowned) → formatDateTime.
    TaskCard :93 (created_at) and :98 (sla_deadline) — bare .toLocaleString(isRTL?'ar-SA':'en-US')
    → formatDateTime (previously unowned file).
    SLAIndicator :173 — .toLocaleString(isRTL?'ar-SA':'en-US', {dateStyle:'medium',
    timeStyle:'short'}) on the deadline → formatDateTime (previously unowned file). Do NOT touch
    the RTL icon/flex logic around it.
    InfluenceReport :231,:401,:404 → helper rule. InfluenceMetricsPanel :389 → helper rule.
    StakeholderTimelineCard date :165 + time :170 → formatDayFirst + formatTime.
    FieldHistoryTimeline — date :109 → helper rule; the Intl.NumberFormat(isRTL?'ar-SA':'en-US')
    at :93 → toFormatLocale (number, previously unowned); the .toLocaleString combo at :228 →
    formatDateTime (previously unowned).
    Delete locale ternaries/vars each edit orphans. Render path only; no data/query changes.
  </action>
  <verify>
    <automated>cd frontend && rg -n "toLocaleDateString|toLocaleTimeString" src/components/audit-logs src/components/activity-feed/ActivityFeedFilters.tsx src/components/tasks/TaskDetail.tsx src/components/tasks/TaskCard.tsx src/components/tasks/SLAIndicator.tsx src/components/stakeholder-influence src/components/stakeholder-timeline src/components/field-history | wc -l | grep -qx "0" && rg -n "'ar-SA'" src/components/audit-logs src/components/activity-feed/ActivityFeedFilters.tsx src/components/tasks src/components/stakeholder-influence src/components/stakeholder-timeline src/components/field-history | wc -l | grep -qx "0" && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>Zero raw date/time formatting and zero 'ar-SA' literals remain across the 10 files (incl. the previously-unowned TaskCard, SLAIndicator, and the TaskDetail/AuditLogStatistics/FieldHistoryTimeline number+combo sites); type-check exit 0.</acceptance_criteria>
  <done>Slice complete; tasks/audit/stakeholder timestamps on formatDateTime, counts Latin via toFormatLocale.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                                                       |
| -------- | ----------------------------------------------------------------------------------------------------------------- |
| none new | Render-time formatting of already-fetched data; no new inputs, dependencies, or crypto (RESEARCH Security Domain) |

## STRIDE Threat Register

| Threat ID | Category        | Component                           | Disposition | Mitigation Plan                                                                |
| --------- | --------------- | ----------------------------------- | ----------- | ------------------------------------------------------------------------------ |
| T-82-01   | Tampering (XSS) | formatted strings rendered in React | mitigate    | React text-node rendering only; no dangerouslySetInnerHTML in any touched file |
| T-82-SC   | Tampering       | package installs                    | accept      | No packages installed this phase                                               |

</threat_model>

<verification>
- Per-plan grep gates: the two task gates above combined → zero raw date/time sites AND zero 'ar-SA' literals in all 19 files
- `cd frontend && pnpm type-check` exit 0
- `cd frontend && pnpm vitest run src/components/analytics src/components/audit-logs src/components/tasks` (nearest suites) — no regressions
</verification>

<success_criteria>
19 files route dates through the 4-helper surface (or date-fns 'd MMM' for chart ticks); every
number site Latin via toFormatLocale; zero 'ar-SA' in the slice. No data-path change; no
carve-out touched.
</success_criteria>

<output>
Create `.planning/phases/82-date-number-formatting/82-02-SUMMARY.md` when done.
</output>
