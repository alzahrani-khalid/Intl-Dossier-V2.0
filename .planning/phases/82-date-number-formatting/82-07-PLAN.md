---
phase: 82-date-number-formatting
plan: 07
type: execute
wave: 2
depends_on: [82-01]
files_modified:
  - frontend/src/components/legislation/LegislationDetail.tsx
  - frontend/src/components/legislation/LegislationList.tsx
  - frontend/src/components/entity-comparison/EntityComparisonTable.tsx
  - frontend/src/components/entity-links/EntitySearchDialog.tsx
  - frontend/src/components/geographic-visualization/WorldMapVisualization.tsx
  - frontend/src/components/milestone-planning/MilestoneCard.tsx
  - frontend/src/components/engagement-recommendations/RecommendationCard.tsx
  - frontend/src/components/relationships/AdvancedGraphVisualization.tsx
  - frontend/src/components/report-builder/ReportPreview.tsx
  - frontend/src/components/ui/pull-to-refresh-indicator.tsx
  - frontend/src/components/ui/file-upload.tsx
  - frontend/src/components/layout/ClassificationBar.tsx
  - frontend/src/components/meeting-minutes/MeetingMinutesCard.tsx
  - frontend/src/components/meeting-minutes/ActionItemsList.tsx
  - frontend/src/components/assignments/EscalationDashboard.tsx
  - frontend/src/components/availability-polling/AvailabilityPollVoter.tsx
  - frontend/src/components/availability-polling/AvailabilityPollResults.tsx
  - frontend/src/components/timeline/InteractiveTimeline.tsx
  - frontend/src/components/timeline/TimelineEventCard.tsx
  - frontend/src/components/timeline/EnhancedVerticalTimelineCard.tsx
  - frontend/src/components/compliance/ComplianceViolationAlert.tsx
  - frontend/src/components/list-page/EngagementsList.tsx
autonomous: true
requirements: [FMT-02, FMT-04]
must_haves:
  truths:
    - "Satisfies D-82-02: every ad-hoc toLocaleDateString/toLocaleTimeString/Intl.DateTimeFormat date render in this plan's 22 component files routes through lib/format-date helpers (render path only), including named offender MeetingMinutesCard.tsx (date-fns 'MMM d, yyyy')"
    - "Satisfies D-82-05: zero raw 'ar-SA' locale literals remain in the 22 files — ClassificationBar no longer forces 'ar-SA-u-nu-arab', the three timeline card formatters and ComplianceViolationAlert/EngagementsList no longer pass 'ar-SA' to Intl.DateTimeFormat/toLocaleString, and EntityComparisonTable's NumberFormat routes through toFormatLocale"
    - "No month-first date-fns literal survives in meeting-minutes/assignments/availability-polling: MeetingMinutesCard :95, ActionItemsList :221 ('MMM d'), EscalationDashboard :183 (BOTH format calls: range-start 'MMM dd' AND range-end 'MMM dd, yyyy') and :330 ('MMM dd'), AvailabilityPollVoter :285 + AvailabilityPollResults :220 ('EEEE, MMMM d' → formatDayFirst, weekday kept, identical treatment at both sites) and :392 ('MMM d') — the 82-06 guard's month-first detector finds nothing here"
    - 'Satisfies D-82-06: no carve-out file touched; only render-path edits in the listed components'
  artifacts:
    - path: 'frontend/src/components/meeting-minutes/MeetingMinutesCard.tsx'
      provides: 'named offender migrated'
      contains: 'formatDayFirstYear'
    - path: 'frontend/src/components/layout/ClassificationBar.tsx'
      provides: 'Indic date ribbon flipped to Latin day-first'
      contains: 'formatDayFirstYear'
  key_links:
    - from: "this plan's date sites"
      to: 'frontend/src/lib/format-date.ts'
      via: "import { formatDayFirst, formatDayFirstYear, formatTime, formatDateTime } from '@/lib/format-date'"
      pattern: '@/lib/format-date'
    - from: 'frontend/src/components/entity-comparison/EntityComparisonTable.tsx'
      to: 'frontend/src/lib/format-locale.ts'
      via: 'number formatting locale'
      pattern: 'toFormatLocale'
---

<objective>
Migrate the legislation/comparison/ui/layout/meetings/timeline/compliance slice (22 component
files) onto `lib/format-date.ts`, kill the ClassificationBar forced-Indic ribbon, and clear every
month-first date-fns literal so the 82-06 guard lands on a clean tree.

Purpose: FMT-02 bulk migration, slice 2 of 4 (split from the original 82-02 to stay within the
per-plan file budget, and to absorb the re-grepped 'ar-SA' offenders RESEARCH missed: timeline
cards, ComplianceViolationAlert, EngagementsList, ActionItemsList). Runs in parallel with 82-02,
82-03, and 82-04 (zero file overlap). Output: 22 migrated component files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-date-number-formatting/82-RESEARCH.md  (§1 inventory groups + §3.4 item 5 ClassificationBar — NOTE: its 'ar-SA' inventory is INCOMPLETE; the timeline/compliance/list-page/ActionItemsList sites below come from a fresh 2026-07-04 re-grep and are authoritative)
@frontend/src/lib/format-date.ts  (post-82-01: the 4-helper surface)
@frontend/src/lib/format-locale.ts  (post-82-01: 'ar-u-nu-latn')

<interfaces>
Same import surface and helper-selection rule as plan 82-02:

```typescript
import { formatDayFirst, formatDayFirstYear, formatTime, formatDateTime } from '@/lib/format-date'
import { toFormatLocale } from '@/lib/format-locale' // NUMBER formatting locales only
```

Rule: options include `year` → formatDayFirstYear; time-only → formatTime; date+time combo (incl.
`Intl.DateTimeFormat` with hour/minute options and bare `.toLocaleString(locale, {...})`) →
formatDateTime; compact chip/axis → date-fns 'd MMM'; else formatDayFirst. Helpers take the date
alone — delete locale ternaries/vars your edit orphans. NUMBER sites keep their call shape with
the locale swapped to toFormatLocale(...). Output byte-identical in en/ar for dates (Pitfall 6).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Legislation + comparison + visualization + ui slice (11 files)</name>
  <files>frontend/src/components/legislation/LegislationDetail.tsx, frontend/src/components/legislation/LegislationList.tsx, frontend/src/components/entity-comparison/EntityComparisonTable.tsx, frontend/src/components/entity-links/EntitySearchDialog.tsx, frontend/src/components/geographic-visualization/WorldMapVisualization.tsx, frontend/src/components/milestone-planning/MilestoneCard.tsx, frontend/src/components/engagement-recommendations/RecommendationCard.tsx, frontend/src/components/relationships/AdvancedGraphVisualization.tsx, frontend/src/components/report-builder/ReportPreview.tsx, frontend/src/components/ui/pull-to-refresh-indicator.tsx, frontend/src/components/ui/file-upload.tsx</files>
  <read_first>82-RESEARCH.md §1 (A1/A2/B tables + Group D). Re-grep exact line numbers at task start (RESEARCH dated 2026-07-04 — line drift expected): `rg -n "'ar-SA'|toLocaleDateString|toLocaleTimeString|toLocaleString\(" <file>`.</read_first>
  <action>
    Per-file site list (re-grepped 2026-07-04):
    LegislationDetail :417,:429,:469,:484,:499,:605,:694,:771 (8 mixed A1/A2 date lines → helper
    rule) PLUS the date+time .toLocaleString(isRTL?'ar-SA':'en-US') at :960 (entry.changed_at,
    previously unowned) → formatDateTime. LegislationList :396 (multi-line) → helper rule.
    EntityComparisonTable — date :114 → helper rule; the Intl.NumberFormat(isRTL?'ar-SA':'en-US')
    at :91 → toFormatLocale (number, previously unowned). MilestoneCard :103; RecommendationCard
    :169; AdvancedGraphVisualization :907,:910,:913; ReportPreview :143; EntitySearchDialog :586
    (multi-line); WorldMapVisualization :264 (multi-line) — all helper rule.
    pull-to-refresh-indicator date :77 + toLocaleTimeString :273 → formatDayFirst + formatTime.
    file-upload :113 (bare toLocaleDateString, 'modified {date}' caption) → formatDayFirstYear.
    Delete locale ternaries/vars each edit orphans; render path only; do not reformat surrounding
    code.
  </action>
  <verify>
    <automated>cd frontend && rg -n "toLocaleDateString|toLocaleTimeString" src/components/legislation src/components/entity-comparison src/components/entity-links/EntitySearchDialog.tsx src/components/geographic-visualization src/components/milestone-planning src/components/engagement-recommendations src/components/relationships/AdvancedGraphVisualization.tsx src/components/report-builder src/components/ui/pull-to-refresh-indicator.tsx src/components/ui/file-upload.tsx | wc -l | grep -qx "0" && rg -n "'ar-SA'" src/components/legislation src/components/entity-comparison src/components/entity-links/EntitySearchDialog.tsx src/components/geographic-visualization src/components/milestone-planning src/components/engagement-recommendations src/components/relationships/AdvancedGraphVisualization.tsx src/components/report-builder | wc -l | grep -qx "0" && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>Zero raw date/time formatting and zero 'ar-SA' remain in the 11 files (incl. LegislationDetail :960 and EntityComparisonTable :91); type-check exit 0.</acceptance_criteria>
  <done>11 files migrated; legislation/comparison timestamps canonical, numbers Latin.</done>
</task>

<task type="auto">
  <name>Task 2: Named offenders + month-first stragglers + timeline/compliance/list-page (11 files)</name>
  <files>frontend/src/components/layout/ClassificationBar.tsx, frontend/src/components/meeting-minutes/MeetingMinutesCard.tsx, frontend/src/components/meeting-minutes/ActionItemsList.tsx, frontend/src/components/assignments/EscalationDashboard.tsx, frontend/src/components/availability-polling/AvailabilityPollVoter.tsx, frontend/src/components/availability-polling/AvailabilityPollResults.tsx, frontend/src/components/timeline/InteractiveTimeline.tsx, frontend/src/components/timeline/TimelineEventCard.tsx, frontend/src/components/timeline/EnhancedVerticalTimelineCard.tsx, frontend/src/components/compliance/ComplianceViolationAlert.tsx, frontend/src/components/list-page/EngagementsList.tsx</files>
  <read_first>82-RESEARCH.md §1 (named offender + adjacent date-fns list) and §3.4 item 5 (ClassificationBar). Re-grep exact line numbers at task start (RESEARCH dated 2026-07-04 — line drift expected): `rg -n "MMM|'ar-SA'|toLocale" <file>`.</read_first>
  <action>
    ClassificationBar :74 (D-82-05 mandatory): replace the Intl.DateTimeFormat with
    'ar-SA-u-nu-arab' — which deliberately forces Indic — with formatDayFirstYear (it renders day
    numeric / month short / year); delete the now-unused locale logic.
    MeetingMinutesCard :95 (D-82-02 named offender): date-fns format(..., 'MMM d, yyyy') →
    formatDayFirstYear(minutes.meeting_date); drop the date-fns import if YOUR edit orphans it.
    Month-first date-fns stragglers (must be clean before the 82-06 guard lands):
    ActionItemsList :221 'MMM d' (due-date chip) → date-fns format(d, 'd MMM').
    EscalationDashboard — BOTH calls at :183 (range-start format(dateRange.start, 'MMM dd') →
    format(dateRange.start, 'd MMM') AND range-end format(dateRange.end, 'MMM dd, yyyy') →
    formatDayFirstYear(dateRange.end)) PLUS the second 'MMM dd' at :330 → format(d, 'd MMM').
    AvailabilityPollVoter :285 AND AvailabilityPollResults :220 are the IDENTICAL
    'EEEE, MMMM d' weekday-bearing pattern — treat both the same way: formatDayFirst(startDate)
    (weekday kept, day-first, guard-clean). AvailabilityPollResults :392 'MMM d' →
    format(d, 'd MMM'). Drop orphaned dateLocale vars/imports.
    Timeline cards (previously unowned 'ar-SA' Intl.DateTimeFormat formatters):
    TimelineEventCard :98 formatEventDate (year+month+day+hour+minute) → formatDateTime.
    EnhancedVerticalTimelineCard :112 formatEventDate (year+month+day) → formatDayFirstYear; :124
    formatEventTime (hour+minute) → formatTime. InteractiveTimeline :190 — this formatter takes a
    variable options object built by the enclosing function; read that function in full, then
    replace the Intl.DateTimeFormat call by branching onto the nearest helper per options-branch
    (year present → formatDayFirstYear; hour/minute present → formatDateTime or formatTime; else
    formatDayFirst). Keep the Arabic label strings around it (e.g. 'كل الوقت') untouched — only
    the digit-bearing date path changes.
    ComplianceViolationAlert :85 — Intl.DateTimeFormat(isRTL?'ar-SA':'en-US', {...}) → helper
    rule per its options. EngagementsList :160 — the multi-line
    .toLocaleString(i18n.language==='ar'?'ar-SA':'en-US', {weekday,month,day,hour,minute}) →
    formatDateTime(row.starts_at) (weekday+date+time combo — formatDateTime's 'Tue 28 Apr 14:30
    GST' covers it); keep the ' · location' suffix logic untouched.
  </action>
  <verify>
    <automated>cd frontend && ! rg -q "u-nu-arab" src/components/layout/ClassificationBar.tsx && ! rg -q "M{3,4},? d" src/components/meeting-minutes src/components/assignments/EscalationDashboard.tsx src/components/availability-polling && rg -n "'ar-SA'" src/components/layout/ClassificationBar.tsx src/components/timeline src/components/compliance/ComplianceViolationAlert.tsx src/components/list-page/EngagementsList.tsx | wc -l | grep -qx "0" && rg -n "toLocaleDateString|toLocaleTimeString" src/components/timeline src/components/compliance/ComplianceViolationAlert.tsx src/components/list-page/EngagementsList.tsx | wc -l | grep -qx "0" && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>ClassificationBar has no -u-nu-arab; the month-first detector regex `M{3,4},? d` (mirrors the 82-06 guard: matches bare 'MMM d'/'MMM dd'/'MMMM d'/'EEEE, MMMM d'/'MMM d, yyyy', passes 'd MMM'/'d MMM yyyy') finds nothing in meeting-minutes/EscalationDashboard/availability-polling; zero 'ar-SA' in timeline/compliance/list-page/layout files; type-check exit 0.</acceptance_criteria>
  <done>Named offenders + every month-first straggler cleared; timeline/compliance/list-page Indic sources gone; 82-06 guard reachable.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                                                       |
| -------- | ----------------------------------------------------------------------------------------------------------------- |
| none new | Render-time formatting of already-fetched data; no new inputs, dependencies, or crypto (RESEARCH Security Domain) |

## STRIDE Threat Register

| Threat ID | Category        | Component                           | Disposition | Mitigation Plan                                                                |
| --------- | --------------- | ----------------------------------- | ----------- | ------------------------------------------------------------------------------ |
| T-82-01   | Tampering (XSS) | formatted strings rendered in React | mitigate    | React text-node rendering only; no dangerouslySetInnerHTML in any touched file |
| T-82-SC   | Tampering       | package installs                    | accept      | No packages installed this phase                                               |

</threat_model>

<verification>
- Combined task gates → zero raw date/time sites, zero 'ar-SA', zero month-first date-fns literals across all 22 files
- `cd frontend && pnpm type-check` exit 0
- `cd frontend && pnpm vitest run src/components/timeline src/components/meeting-minutes` (nearest suites) — no regressions
</verification>

<success_criteria>
22 files route dates through the 4-helper surface (or date-fns 'd MMM' for compact chips);
ClassificationBar Latin; every month-first date-fns literal in this slice gone (guard-reachable);
timeline/compliance/list-page 'ar-SA' sources eliminated. No data-path change; no carve-out
touched.
</success_criteria>

<output>
Create `.planning/phases/82-date-number-formatting/82-07-SUMMARY.md` when done.
</output>
