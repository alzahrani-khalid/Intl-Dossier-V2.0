---
phase: 82-date-number-formatting
plan: 05
type: execute
wave: 3
depends_on: [82-02, 82-03, 82-04]
files_modified:
  - frontend/src/pages/WorkBoard/KCard.tsx
  - frontend/src/pages/WorkBoard/BoardToolbar.tsx
  - frontend/src/pages/WorkBoard/BoardColumn.tsx
  - frontend/src/pages/WorkBoard/__tests__/KCard.test.tsx
  - frontend/src/pages/WorkBoard/__tests__/BoardToolbar.test.tsx
  - frontend/src/pages/WorkBoard/__tests__/BoardColumn.test.tsx
  - frontend/src/i18n/en/unified-kanban.json
  - frontend/src/i18n/ar/unified-kanban.json
  - frontend/src/components/calendar/CalendarMonthGrid.tsx
  - frontend/src/components/calendar/WeekListMobile.tsx
  - frontend/src/components/calendar/__tests__/CalendarMonthGrid.test.tsx
  - frontend/src/components/calendar/__tests__/WeekListMobile.test.tsx
  - frontend/src/components/dossier/DossierDrawer/DrawerMetaStrip.tsx
  - frontend/src/components/dossier/DossierDrawer/MiniKpiStrip.tsx
  - frontend/src/components/dossier/DossierDrawer/OpenCommitmentsSection.tsx
  - frontend/src/components/dossier/DossierDrawer/UpcomingSection.tsx
  - frontend/src/components/dossier/DossierDrawer/__tests__/DrawerMetaStrip.test.tsx
  - frontend/src/components/dossier/DossierDrawer/__tests__/MiniKpiStrip.test.tsx
  - frontend/src/components/dossier/DossierDrawer/__tests__/UpcomingSection.test.tsx
  - frontend/src/components/activity-feed/ActivityList.tsx
  - frontend/src/components/activity-feed/__tests__/ActivityList.test.tsx
  - frontend/src/lib/i18n/toArDigits.ts
  - frontend/src/lib/i18n/__tests__/toArDigits.test.ts
autonomous: true
requirements: [FMT-04]
must_haves:
  truths:
    - "Satisfies D-82-04: the AR kanban overdue chip renders 'متأخر {{days}} يوم' — Latin digits + Arabic unit via new i18n key card.overdueBy with a `days` interpolation var (NOT `count`, Pitfall 3) — no bare Latin 'd' after Arabic text; EN keeps 'Overdue {{days}}d'"
    - 'Satisfies D-82-05: toArDigits.ts and its test are DELETED and every remaining consumer unwrapped — a filtered grep for toArDigits across frontend/src returns zero files (mechanical proof per RESEARCH §3.2)'
    - "Satisfies D-82-05: all 12 Indic-asserting test files now expect Latin digits (the RESEARCH §3.3 table), with fixture DATA strings (ActivityList.test line 82 'إيجاز ١٩') left untouched"
    - 'Satisfies D-82-06: the new AR unit string lives in src/i18n/ar/unified-kanban.json with the EN source driving it; namespace already registered — check-i18n-namespaces stays green'
  artifacts:
    - path: 'frontend/src/i18n/en/unified-kanban.json'
      provides: 'card.overdueBy EN source'
      contains: 'overdueBy'
    - path: 'frontend/src/i18n/ar/unified-kanban.json'
      provides: "card.overdueBy AR — 'متأخر {{days}} يوم'"
      contains: 'overdueBy'
  key_links:
    - from: 'frontend/src/pages/WorkBoard/KCard.tsx'
      to: 'unified-kanban i18n namespace'
      via: "t('card.overdueBy', { days: n })"
      pattern: "card\\.overdueBy"
---

<objective>
Complete digit policy D: fix the F5 mixed-script overdue chip at its true root cause
(`KCard.tsx:70`, not relativeTime), unwrap the 10 remaining `toArDigits` consumer files, DELETE
the module + its test, and flip the remaining Indic-asserting tests to Latin.

Purpose: FMT-04 / D-82-05's observable outcome — no Arabic-Indic digit source left in the tree.
Runs after all Wave-2 plans because the phase-wide `toArDigits → 0` gate needs their unwraps
(AfterActionsTable in 82-03; BriefsPage/MyTasks in 82-04; format-date/relativeTime in 82-01).
Output: kanban/calendar/drawer/activity files Latin, module deleted, 12 test files Latin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-date-number-formatting/82-RESEARCH.md  (§3.1 consumer table, §3.2 delete-the-module rationale, §3.3 test-flip table, §4 KCard root cause + exact fix)
@frontend/src/pages/WorkBoard/KCard.tsx
@frontend/src/i18n/en/unified-kanban.json  (card.overdue at line 69; overdueChip at line 96)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Kanban overdue fix — overdueBy i18n key + KCard/BoardToolbar/BoardColumn + tests</name>
  <files>frontend/src/i18n/en/unified-kanban.json, frontend/src/i18n/ar/unified-kanban.json, frontend/src/pages/WorkBoard/KCard.tsx, frontend/src/pages/WorkBoard/BoardToolbar.tsx, frontend/src/pages/WorkBoard/BoardColumn.tsx, frontend/src/pages/WorkBoard/__tests__/KCard.test.tsx, frontend/src/pages/WorkBoard/__tests__/BoardToolbar.test.tsx, frontend/src/pages/WorkBoard/__tests__/BoardColumn.test.tsx</files>
  <read_first>82-RESEARCH.md §4 in full (root cause, exact fix, Pitfall 3, unit-wording recommendation), KCard.tsx :60-85, the three test files at the lines in RESEARCH §3.3.</read_first>
  <behavior>
    - KCard EN overdue chip: 'Overdue 62d'; KCard AR overdue chip: 'متأخر 62 يوم' (Latin digits, Arabic unit, no bare Latin 'd')
    - BoardToolbar AR chip renders '27 متأخر' (Latin digits; plural-key fallback to base overdueChip works with raw count)
    - BoardColumn count renders '12' in AR
  </behavior>
  <action>
    Update the three test files FIRST (RED): KCard.test.tsx :166 replaces the /Overdue ٦٢d/
    expectation with the new EN 'Overdue 62d' and AR 'متأخر 62 يوم' expectations;
    BoardToolbar.test.tsx :134 /٢٧ متأخر/ → /27 متأخر/; BoardColumn.test.tsx :133-134 '١٢' →
    '12'. Then implement: (1) add "overdueBy" under the "card" object in BOTH
    src/i18n/en/unified-kanban.json (value: Overdue {{days}}d) and src/i18n/ar/unified-kanban.json
    (value: متأخر {{days}} يوم) — interpolation var is `days`, NOT `count`, to bypass i18next v25
    plural-suffix resolution (Pitfall 3; D-82-04's locked example is '230 يوم'; full word 'يوم'
    per the RESEARCH unit-wording recommendation). Keep the existing card.overdue key in place
    (unused after this edit — leave it, surgical change). (2) KCard.tsx :70 → return
    t('card.overdueBy', { days: n }); widen buildDueText's `t` parameter type (line 67, currently
    (key: string) => string) to accept an interpolation-options second argument; remove the
    toArDigits import at :23 and unwrap the second call at :79. (3) BoardToolbar.tsx :53 → pass
    the raw number: t('overdueChip', { count: overdueCount }) (existing key '{{count}} متأخر';
    base-key fallback covers AR plurals — the updated test proves the render); remove the
    toArDigits import at :19. (4) BoardColumn.tsx :67 → render {items.length} directly; remove
    the import at :29. The namespace is already registered — do NOT touch src/i18n/index.ts.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run src/pages/WorkBoard/__tests__/ && ! rg -q "toArDigits" src/pages/WorkBoard && node ../scripts/check-i18n-namespaces.mjs</automated>
  </verify>
  <acceptance_criteria>All WorkBoard tests green with Latin/mixed-free expectations; no toArDigits reference in src/pages/WorkBoard; both JSON files contain overdueBy with {{days}}; check-i18n-namespaces exit 0.</acceptance_criteria>
  <done>F5 root cause fixed: AR chip 'متأخر N يوم', EN 'Overdue Nd'.</done>
</task>

<task type="auto">
  <name>Task 2: Unwrap calendar/drawer/activity consumers, delete toArDigits, flip remaining tests</name>
  <files>frontend/src/components/calendar/CalendarMonthGrid.tsx, frontend/src/components/calendar/WeekListMobile.tsx, frontend/src/components/dossier/DossierDrawer/DrawerMetaStrip.tsx, frontend/src/components/dossier/DossierDrawer/MiniKpiStrip.tsx, frontend/src/components/dossier/DossierDrawer/OpenCommitmentsSection.tsx, frontend/src/components/dossier/DossierDrawer/UpcomingSection.tsx, frontend/src/components/activity-feed/ActivityList.tsx, frontend/src/lib/i18n/toArDigits.ts, frontend/src/lib/i18n/__tests__/toArDigits.test.ts, frontend/src/components/calendar/__tests__/CalendarMonthGrid.test.tsx, frontend/src/components/calendar/__tests__/WeekListMobile.test.tsx, frontend/src/components/dossier/DossierDrawer/__tests__/DrawerMetaStrip.test.tsx, frontend/src/components/dossier/DossierDrawer/__tests__/MiniKpiStrip.test.tsx, frontend/src/components/dossier/DossierDrawer/__tests__/UpcomingSection.test.tsx, frontend/src/components/activity-feed/__tests__/ActivityList.test.tsx</files>
  <read_first>82-RESEARCH.md §3.1 (per-file removal shapes) + §3.3 (per-test flips). Read each source file around the listed lines.</read_first>
  <action>
    Unwraps (per RESEARCH §3.1 removal-shape column): CalendarMonthGrid :101 — format(day, 'd')
    is already Latin, drop the wrap + import :24 + the Arabic-Indic doc comment at :9;
    WeekListMobile :87 + import :15 + doc :4 — same; DrawerMetaStrip :49,59 — pass the raw
    numbers (interp var is `n`, no plural machinery involved) + import :20; MiniKpiStrip :46 —
    render {c.val} directly INSIDE the existing LtrIsolate wrapper (KEEP the isolate — bidi
    protection stays) + import :16 + doc :10; OpenCommitmentsSection :69 — build the template as
    T + sign + abs directly (restores spec-true 'T-3'/'T+2' Latin mono) + import :32;
    UpcomingSection :72,75 — date-fns with the ar locale keeps Arabic month names, digits go
    Latin; drop wraps + import :19; ActivityList :149 + import :28 + doc :75 — the local
    formatRelativeTime AR suffixes (ث/د/س/ي) are already localized, keep them.
    Then DELETE frontend/src/lib/i18n/toArDigits.ts and
    frontend/src/lib/i18n/__tests__/toArDigits.test.ts entirely (RESEARCH §3.2: deletion beats a
    no-op shim — mechanical proof + prevents re-adoption).
    Test flips (exact rows from §3.3): CalendarMonthGrid.test.tsx :107-115 (test name + /[٠-٩]/
    assertions → Latin); WeekListMobile.test.tsx :95-96 ('٢' → '2'); DrawerMetaStrip.test.tsx
    :133 ('٢٢' → '22'); MiniKpiStrip.test.tsx :185-190 ('٢٢' → '22'); UpcomingSection.test.tsx
    :113+ (AR_DIGITS presence check → absence/Latin); ActivityList.test.tsx :154-162 (/٥/ → /5د/
    — but line 82 'إيجاز ١٩' is fixture DATA, leave it byte-identical).
  </action>
  <verify>
    <automated>cd frontend && test ! -f src/lib/i18n/toArDigits.ts && rg -l "toArDigits" src | wc -l | grep -qx "0" && pnpm vitest run src/components/calendar/__tests__/CalendarMonthGrid.test.tsx src/components/calendar/__tests__/WeekListMobile.test.tsx src/components/dossier/DossierDrawer/__tests__ src/components/activity-feed/__tests__/ActivityList.test.tsx && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>toArDigits.ts and its test deleted; `rg -l toArDigits frontend/src | wc -l` → 0 (the D-82-05 mechanical proof); all six flipped test files green; type-check exit 0.</acceptance_criteria>
  <done>Zero toArDigits references in the tree; all Indic test assertions flipped to Latin.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                         |
| -------- | ----------------------------------------------------------------------------------- |
| none new | Render-time digit display + one i18n string; no new inputs, dependencies, or crypto |

## STRIDE Threat Register

| Threat ID | Category        | Component                        | Disposition | Mitigation Plan                                                                                                        |
| --------- | --------------- | -------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------- |
| T-82-01   | Tampering (XSS) | i18n interpolation in KCard chip | mitigate    | i18next escapes interpolation values by default; `days` is a number computed from item data; React text-node rendering |
| T-82-SC   | Tampering       | package installs                 | accept      | No packages installed this phase                                                                                       |

</threat_model>

<verification>
- `rg -l "toArDigits" frontend/src | wc -l` → 0
- `cd frontend && pnpm vitest run src/pages/WorkBoard src/components/calendar src/components/dossier/DossierDrawer src/components/activity-feed` — green
- `node scripts/check-i18n-namespaces.mjs` (from repo root) — exit 0 (namespace untouched)
- `cd frontend && pnpm type-check` exit 0
</verification>

<success_criteria>
FMT-04 complete: AR overdue chip 'متأخر N يوم' (D-82-04); toArDigits retired app-wide with
mechanical grep proof (D-82-05); all 12 §3.3 test files expect Latin; new AR string in
src/i18n/ar with EN driving (D-82-06).
</success_criteria>

<output>
Create `.planning/phases/82-date-number-formatting/82-05-SUMMARY.md` when done.
</output>
