---
phase: 74-eval-gate-anythingllm-retirement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-runtime/evals/vitest.eval.config.ts
  - agent-runtime/evals/schemas/score.ts
  - agent-runtime/evals/lib/set-metrics.ts
  - agent-runtime/evals/correlation.eval.test.ts
  - agent-runtime/evals/fixtures/correlation/forum-membership.json
  - agent-runtime/evals/fixtures/correlation/shared-committees.json
  - agent-runtime/evals/fixtures/correlation/engagement-chain.json
  - agent-runtime/evals/fixtures/correlation/shortest-path.json
  - agent-runtime/evals/fixtures/correlation/_degraded.json
  - agent-runtime/evals/README.md
  - agent-runtime/package.json
autonomous: true
requirements: [EVAL-02]
must_haves:
  truths:
    - 'An `agent-runtime/evals/` workspace runs under its own `vitest.eval.config.ts`, isolated from the unit-test (`vitest.config.ts`) run so `pnpm --filter agent-runtime test` is unaffected'
    - 'EVAL-02 computes precision and recall of returned graph edges vs a golden edge set with NO LLM call — runnable in CI now without the GPU stack'
    - 'The correlation rubric passes at precision >= 0.75 / recall >= 0.70 on the authored fixtures and FAILS (non-zero exit) on the `_degraded.json` fixture (positive-failure proof)'
    - 'Implements EVAL-02 per D1 (computed set-overlap of query_graph edges, no judge)'
  artifacts:
    - path: 'agent-runtime/evals/vitest.eval.config.ts'
      provides: 'Isolated eval-only Vitest config (separate include glob, not run by the unit job)'
      contains: 'vitest.eval.config'
    - path: 'agent-runtime/evals/schemas/score.ts'
      provides: 'Zod score schemas reused by all rubrics (EVAL-01/02/03)'
      exports: ['SetMetricsSchema', 'RubricScoreSchema']
    - path: 'agent-runtime/evals/lib/set-metrics.ts'
      provides: 'Pure precision/recall/F1 over edge sets (no LLM, no network)'
      exports: ['computeSetMetrics', 'edgeKey']
    - path: 'agent-runtime/evals/correlation.eval.test.ts'
      provides: 'EVAL-02 rubric: asserts precision>=0.75 / recall>=0.70 and the degraded positive-failure'
      min_lines: 30
    - path: 'agent-runtime/evals/fixtures/correlation/forum-membership.json'
      provides: 'Golden edge-set fixture for one query_graph query_type'
      contains: 'edges'
  key_links:
    - from: 'agent-runtime/evals/correlation.eval.test.ts'
      to: 'agent-runtime/evals/lib/set-metrics.ts'
      via: 'import computeSetMetrics'
      pattern: 'computeSetMetrics'
    - from: 'agent-runtime/evals/correlation.eval.test.ts'
      to: 'agent-runtime/evals/fixtures/correlation'
      via: 'fixture load (returned edges vs golden edges)'
      pattern: 'fixtures/correlation'
    - from: 'agent-runtime/package.json'
      to: 'agent-runtime/evals/vitest.eval.config.ts'
      via: 'test:eval script'
      pattern: 'vitest.eval.config'
---

<objective>
Scaffold the `agent-runtime/evals/` eval harness and deliver the **EVAL-02** correlation-accuracy rubric — the one rubric that needs NO LLM and is fully CI-runnable today.

EVAL-02 (per D1) reframes "correlation accuracy" as **P71 analytic-graph link discovery**: precision/recall of the edges `query_graph` returns (forum membership / shared committees / engagement chain / shortest path) measured by computed set-overlap against an authored golden edge set. No judge, no GPU.

Purpose: Establish the harness layout, the shared Zod score schemas, and the pure set-metrics library that EVAL-01/03 (Plan 74-08) and the CI job (Plan 74-09) build on. Prove the threshold gate fails on a degraded fixture.
Output: `agent-runtime/evals/` workspace (eval-only Vitest config, score schemas, set-metrics lib, correlation rubric + golden fixtures, README), plus a `test:eval` script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-eval-gate-anythingllm-retirement/74-CONTEXT.md
@.planning/phases/74-eval-gate-anythingllm-retirement/74-RESEARCH.md

<read_first>

- agent-runtime/vitest.config.ts — the UNIT config. The eval config must be a SEPARATE file with a non-overlapping include glob so eval tests never run in the unit job (and vice versa). Note `passWithNoTests: true` and `pool: 'forks'`.
- agent-runtime/package.json — has `"test": "vitest run --config ./vitest.config.ts"`, `vitest ^4.1.7`, `zod 4.4.3`. Add `test:eval` alongside; do NOT change `test`.
- agent-runtime/src/mastra/tools/query-graph.ts — the EVAL-02 SUBJECT. `GRAPH_QUERY_TYPES = ['forum_membership','shared_committees','engagement_chain','shortest_path']` (Zod enum). Output shape: `{ nodes, edges, stats, ... }` passed through verbatim from the P71 `query_graph(p_query_type, p_entity_id, p_entity_id_2, p_window_days)` RPC. Fixtures mirror this returned shape.
- .github/workflows/ci.yml lines 64-73 — the positive-failure precedent: `! node scripts/check-edge-fn-schema-refs.mjs <bad-fixture>` asserts a known-bad input makes the checker exit non-zero. EVAL-02's degraded fixture follows the same "prove it fails" pattern.
  </read_first>

<interfaces>
<!-- query_graph returned-edge shape (from P71 RPC, passed through by queryGraphTool). -->
<!-- Fixtures store BOTH the returned edges and the golden edges in this shape. -->

An edge in a query_graph response (JSONB → object):

```typescript
// Minimal fields the metric keys on (source, target, type). Extra fields ignored.
interface GraphEdge {
  source: string // entity UUID
  target: string // entity UUID
  type?: string // relationship type (e.g. 'forum_membership', 'shared_committee')
}
```

Score schema this plan exports (consumed by 74-08 + 74-09):

```typescript
// SetMetricsSchema: { precision: number, recall: number, f1: number, truePositives: number, ... }
// RubricScoreSchema: { dimension: string, score: number (0..1), passed: boolean }
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harness scaffold — eval-only Vitest config, Zod score schemas, set-metrics lib</name>
  <files>agent-runtime/evals/vitest.eval.config.ts, agent-runtime/evals/schemas/score.ts, agent-runtime/evals/lib/set-metrics.ts, agent-runtime/evals/README.md, agent-runtime/package.json</files>
  <read_first>agent-runtime/vitest.config.ts, agent-runtime/package.json</read_first>
  <action>
    Create the `agent-runtime/evals/` harness foundation that EVAL-01/02/03 share (Claude's Discretion per D-decisions: workspace layout + Zod schemas).

    1. `vitest.eval.config.ts`: mirror `agent-runtime/vitest.config.ts` (node env, `pool: 'forks'`, `globals: true`, `passWithNoTests: true`, 30s timeouts, `@` alias to `./src`) but set `root: __dirname` and `include: ['evals/**/*.eval.test.ts']` so it collects ONLY `*.eval.test.ts` under `evals/`. Critically, the UNIT config's glob is `src/**/*.test.ts` + `tests/**/*.test.ts` — the eval glob must NOT overlap it, so naming eval tests `*.eval.test.ts` and placing them under `evals/` keeps the two runs disjoint. Verify (mentally) the unit `include` does not match `evals/**`.
    2. `schemas/score.ts`: export `SetMetricsSchema` (Zod object: `precision`, `recall`, `f1` as `z.number().min(0).max(1)`, plus `truePositives`, `falsePositives`, `falseNegatives` as `z.number().int().min(0)`) and `RubricScoreSchema` (Zod object: `dimension: z.string()`, `score: z.number().min(0).max(1)`, `passed: z.boolean()`, optional `notes: z.string().optional()`). Export inferred types `SetMetrics` and `RubricScore`. These are the structured Zod-validated score envelopes D4 requires (reused by the on-prem judge in 74-08). Use zod v4 import style consistent with the tools (`import { z } from 'zod'`). Explicit return types per the repo ESLint rule.
    3. `lib/set-metrics.ts`: export `edgeKey(edge): string` (stable canonical key for an edge — sort the {source,target} pair for undirected types so A→B and B→A collide, then append `type`; document the directedness choice in a comment) and `computeSetMetrics(returned: GraphEdge[], golden: GraphEdge[]): SetMetrics` computing precision = TP/(TP+FP), recall = TP/(TP+FN), f1 harmonic mean, guarding divide-by-zero (empty returned set → precision 1 only if golden also empty, else 0; document). PURE — no network, no LLM, no `process.env`. Validate the return through `SetMetricsSchema.parse(...)` before returning so a malformed metric throws.
    4. `README.md`: document the harness — what runs in CI now (EVAL-02 + positive-failure proofs, this plan) vs deploy-gated (EVAL-01/03 judge, 74-08), the `*.eval.test.ts` naming contract, and `pnpm --filter agent-runtime test:eval`.
    5. `package.json`: add `"test:eval": "vitest run --config ./evals/vitest.eval.config.ts"`. Do NOT modify the existing `"test"` script (it stays on `vitest.config.ts`). Do NOT add new runtime deps — vitest + zod are already pinned.

  </action>
  <verify>
    <automated>cd agent-runtime && node -e "const c=require('./evals/vitest.eval.config.ts'.replace(/\.ts$/,'')) " 2>/dev/null; grep -q "test:eval" package.json && grep -q "evals/vitest.eval.config" package.json && echo "script-wired"</automated>
  </verify>
  <done>`agent-runtime/evals/` has the eval-only Vitest config, `schemas/score.ts` exports `SetMetricsSchema`+`RubricScoreSchema`, `lib/set-metrics.ts` exports `computeSetMetrics`+`edgeKey` (pure), README exists, and `package.json` has a `test:eval` script that points at the eval config without touching `test`.</done>
</task>

<task type="auto">
  <name>Task 2: EVAL-02 correlation rubric + golden fixtures + positive-failure proof</name>
  <files>agent-runtime/evals/correlation.eval.test.ts, agent-runtime/evals/fixtures/correlation/forum-membership.json, agent-runtime/evals/fixtures/correlation/shared-committees.json, agent-runtime/evals/fixtures/correlation/engagement-chain.json, agent-runtime/evals/fixtures/correlation/shortest-path.json, agent-runtime/evals/fixtures/correlation/_degraded.json</files>
  <read_first>agent-runtime/src/mastra/tools/query-graph.ts, agent-runtime/evals/lib/set-metrics.ts, agent-runtime/evals/schemas/score.ts</read_first>
  <action>
    Deliver the EVAL-02 rubric (D1: computed precision/recall of query_graph link discovery, NO LLM). Authored golden fixtures, seed-style (D5).

    1. Author 4 golden fixtures (one per `GRAPH_QUERY_TYPES` member — forum_membership, shared_committees, engagement_chain, shortest_path), ≈4-8 edges each. Each fixture JSON = `{ "queryType": "<type>", "input": { "entityId": "<uuid>", "entityId2": "<uuid?>", "windowDays": <int?> }, "returned": GraphEdge[], "golden": GraphEdge[] }`. `returned` = the edges the tool WOULD return for that input (a recorded/representative response); `golden` = the human-curated correct edge set. Make `returned` overlap `golden` enough to clear precision >= 0.75 AND recall >= 0.70 (i.e. allow at most a small number of FP/FN per fixture). Use stable, obviously-synthetic UUIDs (e.g. `00000000-0000-4000-8000-0000000000NN`). These are fixtures, not live RPC calls — EVAL-02 is recorded-response set-overlap so it runs with no DB and no GPU.
    2. `_degraded.json`: same shape, but a deliberately BROKEN `returned` (e.g. half the golden edges missing + spurious edges) so precision/recall fall BELOW threshold. This is the positive-failure fixture.
    3. `correlation.eval.test.ts`:
       - For each of the 4 golden fixtures: load it, run `computeSetMetrics(returned, golden)`, `expect(m.precision).toBeGreaterThanOrEqual(0.75)` and `expect(m.recall).toBeGreaterThanOrEqual(0.70)`.
       - One positive-failure test: load `_degraded.json`, compute metrics, and assert the gate would REJECT it — `expect(m.precision >= 0.75 && m.recall >= 0.70).toBe(false)`. (This proves the rubric fails on a regression, mirroring `ci.yml:69-73`.)
       - Validate every metric through `SetMetricsSchema` (already done inside `computeSetMetrics`).
       - Add a comment block at the top citing **D1** (EVAL-02 = computed graph-link precision/recall, no judge) and **EVAL-02** so the decision-coverage gate and a future reader see the linkage.
    4. Thresholds as named constants (`PRECISION_MIN = 0.75`, `RECALL_MIN = 0.70`) — no magic numbers.

  </action>
  <verify>
    <automated>cd agent-runtime && pnpm vitest run --config ./evals/vitest.eval.config.ts evals/correlation.eval.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>`pnpm --filter agent-runtime test:eval` (or the scoped vitest run) passes the 4 golden correlation fixtures at precision>=0.75/recall>=0.70 and the `_degraded.json` test asserts the gate rejects it. No LLM/network is invoked. Top-of-file comment cites D1 + EVAL-02.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                                                                          |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| CI runner → eval harness           | Eval tests run untrusted-of-secrets: EVAL-02 must NOT require any AI endpoint or DB credential (it is recorded-fixture set-overlap). |
| eval harness → query_graph subject | The rubric measures the tool's edge output; it must not invoke the live RPC (no service-role, no caller JWT) in CI-mode.             |

## STRIDE Threat Register

| Threat ID  | Category               | Component               | Disposition | Mitigation Plan                                                                                                                                                 |
| ---------- | ---------------------- | ----------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-74-01-01 | Information disclosure | eval fixtures           | mitigate    | Fixtures use synthetic UUIDs only — no real dossier IDs, names, or clearance data committed.                                                                    |
| T-74-01-02 | Tampering              | eval-only Vitest config | mitigate    | Separate `*.eval.test.ts` glob keeps eval tests out of the unit job and unit tests out of the eval job; no overlap means neither run can be silently broadened. |
| T-74-01-03 | Elevation of privilege | set-metrics lib         | accept      | Pure computation, no `process.env`, no network, no Supabase client — cannot reach RLS or service-role; nothing to elevate.                                      |
| T-74-01-04 | Denial of service      | CI eval job             | accept      | EVAL-02 is pure in-memory set math over <40 edges total — bounded, sub-second; no external dependency to stall on.                                              |

</threat_model>

<verification>
- `agent-runtime/evals/` exists with the eval-only config, schemas, set-metrics lib, correlation rubric, 4 golden fixtures + 1 degraded fixture, README.
- `pnpm --filter agent-runtime test:eval` passes EVAL-02 and the degraded positive-failure assertion; it invokes no LLM and no DB.
- `pnpm --filter agent-runtime test` (the UNIT run) is unchanged — it does NOT pick up `evals/**` and still passes.
- `agent-runtime/package.json` has `test:eval`; `test` is untouched.
</verification>

<success_criteria>

- EVAL-02 computed precision/recall rubric runs CI-natively (no GPU) and gates at precision>=0.75 / recall>=0.70.
- The degraded fixture demonstrably fails the gate (positive-failure proof for the CI job in 74-09).
- The shared Zod score schemas + set-metrics lib are in place for EVAL-01/03 (74-08) and the CI eval-gate job (74-09) to consume.
  </success_criteria>

<output>
Create `.planning/phases/74-eval-gate-anythingllm-retirement/74-01-SUMMARY.md` when done.
</output>
