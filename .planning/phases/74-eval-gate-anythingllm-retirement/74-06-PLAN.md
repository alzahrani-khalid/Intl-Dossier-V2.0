---
phase: 74-eval-gate-anythingllm-retirement
plan: 06
type: execute
wave: 3
depends_on: [74-05]
files_modified:
  - supabase/functions/ai-extract/index.ts
  - supabase/functions/ai-summary-generate/index.ts
  - supabase/functions/multilang-content/index.ts
  - supabase/functions/translate-content/index.ts
  - supabase/functions/dossier-field-assist/index.ts
  - supabase/functions/word-assistant/index.ts
autonomous: true
requirements: [EVAL-04]
must_haves:
  truths:
    - 'The six text/generation AI edge functions generate via the on-prem vLLM helper (`_shared/onprem-llm.ts`), not AnythingLLM (D3)'
    - "Each function's existing non-AnythingLLM fallback (manual-entry / placeholder / word-by-word) is preserved so the surface degrades, not crashes"
    - 'Zero `anythingllm` reference (URL, workspace, /api/v1/workspace, /api/chat) remains in any of the six functions'
    - "Re-homes the remaining text-AI generation edge fns on-prem per D3 (FULL rip-out); word-assistant page kept by re-homing its edge fn (D3 're-home or remove')"
  artifacts:
    - path: 'supabase/functions/word-assistant/index.ts'
      provides: 'Writing-assistant edge fn generating on-prem (keeps the standalone page alive)'
      contains: 'onprem-llm'
    - path: 'supabase/functions/ai-extract/index.ts'
      provides: 'Bilingual extraction via on-prem vLLM'
      contains: 'onprem-llm'
  key_links:
    - from: 'supabase/functions/ai-extract/index.ts'
      to: '_shared/onprem-llm.ts'
      via: 'import generateStructuredJson'
      pattern: 'onprem-llm|generateStructuredJson'
    - from: 'supabase/functions/word-assistant/index.ts'
      to: '_shared/onprem-llm.ts'
      via: 'on-prem generation (page invokes this fn)'
      pattern: 'onprem-llm'
---

<objective>
Re-home the six remaining text/generation AI edge functions from AnythingLLM workspace/chat to the on-prem vLLM helper `_shared/onprem-llm.ts` (built in 74-05), completing the generative half of the FULL AnythingLLM rip-out (D3): `ai-extract`, `ai-summary-generate`, `multilang-content`, `translate-content`, `dossier-field-assist` (edge), and `word-assistant`.

Per D3, `word-assistant` (a standalone page with a hard edge-fn dependency — `WordAssistantPage` calls `supabase.functions.invoke('word-assistant')`) is kept by re-homing its edge fn on-prem rather than removing the page.

Each fn already has (or needs) a non-AnythingLLM degradation path (manual-entry / placeholder / word-by-word translation) — preserve it so the surface stays functional when the model is down (EVAL-04 "surfaces still work").

Depends on 74-05 (the shared `_shared/onprem-llm.ts` helper). File-disjoint from 74-07 (embedding/health/brief fns) so the two run in the same wave.

Purpose: Remove AnythingLLM from all text-generation edge fns (EVAL-04). Output: six fns generate on-prem; zero AnythingLLM reference in them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-eval-gate-anythingllm-retirement/74-CONTEXT.md
@.planning/phases/74-eval-gate-anythingllm-retirement/74-RESEARCH.md

<read_first>

- supabase/functions/\_shared/onprem-llm.ts (created in 74-05) — `generateStructuredJson({ systemPrompt, userPrompt, temperature?, timeoutMs? }): Promise<unknown>`, hits `${VLLM_BASE_URL}/v1/chat/completions` in JSON mode. For free-text (non-JSON) outputs (e.g. word-assistant prose, translate-content), if a plain-text variant is cleaner, add a sibling `generateText(...)` export to that file (same fetch, no `response_format`, return `choices[0].message.content` string) and use it. Keep both helpers in `_shared/onprem-llm.ts`.
- supabase/functions/ai-extract/index.ts (306 lines) — `callAnythingLLM(text, language)` (62) throws `'AI extraction failed. Please try manual entry.'` on failure (122). It returns a structured `ExtractionResult` (JSON). Re-home to `generateStructuredJson`; keep the manual-entry error as the degradation. Remove the AnythingLLM env/URL.
- supabase/functions/ai-summary-generate/index.ts (837 lines) — AnythingLLM `/api/chat` at 261, has a fallback (`fallback_refs=6`). Re-home the generation to the helper (JSON or text per the summary shape); keep the existing fallback.
- supabase/functions/multilang-content/index.ts (520) — `translateWithAI` uses AnythingLLM workspace (124-126) with a fallback. Re-home to `generateText`; keep fallback.
- supabase/functions/translate-content/index.ts (673) — AnythingLLM at 316 with a `generateFallbackTranslation` (160, `fallback_refs=10`). Re-home to `generateText`; keep the word-by-word fallback.
- supabase/functions/dossier-field-assist/index.ts (488) — AnythingLLM workspace chat at 294 with `generateFallbackFields` (104). Re-home to `generateStructuredJson` (it produces bilingual fields); keep the fallback. NOTE: this is the EDGE fn; the BACKEND `backend/src/api/ai/dossier-field-assist.ts` twin is handled in 74-07.
- supabase/functions/word-assistant/index.ts (282) — AnythingLLM workspace chat; the FE page `frontend/src/pages/word-assistant/WordAssistantPage.tsx` invokes it (145, 260). Re-home to `generateText`/`generateStructuredJson` per its response shape (`WordAssistantResponse`). KEEP the page + route — do not remove them (D3: re-home).
  </read_first>

<interfaces>
<!-- Shared on-prem helpers in _shared/onprem-llm.ts (74-05 + this plan). -->
generateStructuredJson({ systemPrompt, userPrompt, temperature?, timeoutMs? }): Promise<unknown>  // JSON mode
generateText({ systemPrompt, userPrompt, temperature?, timeoutMs? }): Promise<string>             // plain text (add here if absent)
<!-- Both target ${VLLM_BASE_URL}/v1/chat/completions; on-prem only; throw on missing env / non-ok. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-home extraction + summary + field-assist (structured JSON outputs)</name>
  <files>supabase/functions/ai-extract/index.ts, supabase/functions/ai-summary-generate/index.ts, supabase/functions/dossier-field-assist/index.ts</files>
  <read_first>supabase/functions/_shared/onprem-llm.ts, supabase/functions/ai-extract/index.ts, supabase/functions/ai-summary-generate/index.ts, supabase/functions/dossier-field-assist/index.ts</read_first>
  <action>
    Re-home the three structured-output generators to `generateStructuredJson` (D3), preserving each fallback.

    1. `ai-extract/index.ts`: replace `callAnythingLLM(text, language)` body with a `generateStructuredJson` call (system prompt = "bilingual data extractor, respond ONLY with the requested JSON"; user prompt = the existing extraction prompt incl. the text + language). Map the parsed JSON to the same `ExtractionResult` shape. Keep the `'AI extraction failed. Please try manual entry.'` throw as the degradation on helper failure. Remove AnythingLLM env/URL consts.
    2. `ai-summary-generate/index.ts`: re-home the `/api/chat` generation (261) to the helper (use `generateStructuredJson` if the summary is structured, else `generateText`). Preserve the existing fallback branch. Remove AnythingLLM env/comments.
    3. `dossier-field-assist/index.ts` (EDGE): replace the AnythingLLM workspace-chat (294) with `generateStructuredJson` producing the same bilingual field object; keep `generateFallbackFields` as the degradation. Remove AnythingLLM env/URL.
    4. In all three: `grep -in anythingllm` must return nothing afterward.

  </action>
  <verify>
    <automated>cd supabase/functions && test -z "$(grep -rin anythingllm ai-extract/index.ts ai-summary-generate/index.ts dossier-field-assist/index.ts)" && grep -l "onprem-llm" ai-extract/index.ts ai-summary-generate/index.ts dossier-field-assist/index.ts</automated>
  </verify>
  <done>All three fns generate via `generateStructuredJson`, keep their fallbacks, and `grep -in anythingllm` on the three files returns nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Re-home translation + multilang + word-assistant (text outputs); keep the word-assistant page</name>
  <files>supabase/functions/_shared/onprem-llm.ts, supabase/functions/multilang-content/index.ts, supabase/functions/translate-content/index.ts, supabase/functions/word-assistant/index.ts</files>
  <read_first>supabase/functions/_shared/onprem-llm.ts, supabase/functions/multilang-content/index.ts, supabase/functions/translate-content/index.ts, supabase/functions/word-assistant/index.ts, frontend/src/pages/word-assistant/WordAssistantPage.tsx</read_first>
  <action>
    Re-home the three text-output generators to the on-prem helper (D3), preserving fallbacks and the word-assistant page.

    1. `_shared/onprem-llm.ts`: if not already present, add a `generateText({ systemPrompt, userPrompt, temperature?, timeoutMs? }): Promise<string>` export — same `${VLLM_BASE_URL}/v1/chat/completions` fetch as `generateStructuredJson` but WITHOUT `response_format` (return `choices[0].message.content` as a string). Throw on missing env / non-ok.
    2. `multilang-content/index.ts`: replace the AnythingLLM workspace translate (124-126) with `generateText` (system = "professional EN<->AR translator"; user = the source text + target language). Keep the existing fallback.
    3. `translate-content/index.ts`: replace the AnythingLLM call (316) with `generateText`. Keep `generateFallbackTranslation` (the word-by-word/placeholder path) for when the helper throws. Remove AnythingLLM env.
    4. `word-assistant/index.ts`: replace the AnythingLLM workspace chat with `generateText` (or `generateStructuredJson` if `WordAssistantResponse` is structured). Preserve the response shape the FE page expects (`WordAssistantResponse`). DO NOT remove `frontend/src/routes/_protected/word-assistant.tsx`, `frontend/src/pages/word-assistant/WordAssistantPage.tsx`, or the nav entry — the page stays, now backed on-prem.
    5. In all three edge fns: `grep -in anythingllm` returns nothing.

  </action>
  <verify>
    <automated>cd supabase/functions && test -z "$(grep -rin anythingllm multilang-content/index.ts translate-content/index.ts word-assistant/index.ts)" && grep -q "generateText\|generateStructuredJson" word-assistant/index.ts && grep -q "generateText" _shared/onprem-llm.ts</automated>
  </verify>
  <done>`multilang-content`, `translate-content`, and `word-assistant` generate via the on-prem helper, the word-assistant page/route/nav are intact, fallbacks preserved, and `grep -in anythingllm` on the three edge fns returns nothing.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                                                                                                                  |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| edge fn → on-prem vLLM           | All six fns now reach only `${VLLM_BASE_URL}/v1/chat/completions` (on-prem). AnythingLLM (off-prem-capable) removed. Zero-egress.                            |
| FE word-assistant page → edge fn | The page invokes the `word-assistant` edge fn under the caller's Supabase session; re-homing changes only the fn's upstream model, not the page's auth path. |

## STRIDE Threat Register

| Threat ID  | Category               | Component               | Disposition | Mitigation Plan                                                                                                                                                              |
| ---------- | ---------------------- | ----------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-74-06-01 | Information disclosure | zero-egress invariant   | mitigate    | The shared helper targets only `VLLM_BASE_URL` (on-prem); no cloud/AnythingLLM endpoint. User content stays on-prem.                                                         |
| T-74-06-02 | Denial of service      | model unavailable       | mitigate    | Each fn keeps its non-AnythingLLM fallback (manual-entry / placeholder / word-by-word) so a vLLM outage degrades rather than 500s — preserves EVAL-04 "surfaces still work". |
| T-74-06-03 | Tampering              | model output parsing    | mitigate    | Structured fns parse JSON-mode output into known shapes; text fns return prose used as-is by the page (no eval/HTML injection introduced beyond the existing render path).   |
| T-74-06-04 | Elevation of privilege | keystone / service-role | accept      | Generator swap only; no new service-role usage and no change to the fns' existing DB-auth posture.                                                                           |

</threat_model>

<verification>
- `grep -rin anythingllm supabase/functions/{ai-extract,ai-summary-generate,multilang-content,translate-content,dossier-field-assist,word-assistant}/index.ts` returns nothing.
- Each fn imports/uses `_shared/onprem-llm.ts`; `generateText` exists in that helper.
- `frontend/src/routes/_protected/word-assistant.tsx` + `WordAssistantPage.tsx` + the nav entry still exist (page kept, re-homed).
- Each fn retains a degradation path (not a hard 500) when the helper throws.
</verification>

<success_criteria>

- The six text-AI edge fns generate on-prem via the shared helper (D3); AnythingLLM fully removed from them.
- word-assistant page kept and re-homed (D3 "re-home or remove" → re-home).
- Fallbacks preserved; zero-egress preserved; no new service-role.
  </success_criteria>

<output>
Create `.planning/phases/74-eval-gate-anythingllm-retirement/74-06-SUMMARY.md` when done.
</output>
