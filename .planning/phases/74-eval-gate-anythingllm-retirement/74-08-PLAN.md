---
phase: 74-eval-gate-anythingllm-retirement
plan: 08
type: execute
wave: 4
depends_on: [74-02]
files_modified:
  - deploy/docker-compose.prod.yml
  - deploy/docker-compose.mac.yml
  - deploy/nginx/nginx.conf
  - deploy/nginx/nginx.prod.conf
  - backend/.env.example
  - deploy/.env.example
  - scripts/check-no-anythingllm.mjs
  - tools/anythingllm-fixtures/critical-surface-with-anythingllm/sample.ts
  - package.json
autonomous: true
requirements: [EVAL-04]
must_haves:
  truths:
    - 'The `anythingllm` container, its `anythingllm_data` volume, the backend AnythingLLM env block, and the dead `VITE_ANYTHINGLLM_URL` build-arg are removed from both prod and mac compose (D3)'
    - 'The nginx `/llm/` proxy + `upstream anythingllm` are removed from both nginx.conf and nginx.prod.conf'
    - 'All ANYTHINGLLM_* / VITE_ANYTHINGLLM_* keys are removed from backend/.env.example and deploy/.env.example'
    - 'A static `check:no-anythingllm` script asserts the critical surfaces (semantic-search edge fns + digest hook + copilot runtime) contain zero AnythingLLM reference, and fails (non-zero) on a planted bad fixture (positive-failure)'
    - 'Completes the infra/levers removal per D3 and adds the CI-native static allowlist guard (D6)'
  artifacts:
    - path: 'deploy/docker-compose.prod.yml'
      provides: 'Prod compose with AnythingLLM container/volume/env removed'
      contains: 'services'
    - path: 'scripts/check-no-anythingllm.mjs'
      provides: 'Static greppable guard over the critical surfaces (CI-native, no LLM)'
      contains: 'anythingllm'
    - path: 'tools/anythingllm-fixtures/critical-surface-with-anythingllm/sample.ts'
      provides: 'Planted bad fixture proving the guard fails on a regression'
      contains: 'anythingllm'
  key_links:
    - from: 'scripts/check-no-anythingllm.mjs'
      to: 'critical surfaces'
      via: 'grep allowlist over semantic-search/digest/copilot source'
      pattern: 'anythingllm'
    - from: 'package.json'
      to: 'scripts/check-no-anythingllm.mjs'
      via: 'check:no-anythingllm script'
      pattern: 'check:no-anythingllm'
---

<objective>
Remove the AnythingLLM infrastructure and levers (D3) — the prod + mac compose container/volume/env, the nginx `/llm/` proxy + upstream, and the env-example keys — and add the CI-native static guard (D6): a `check:no-anythingllm` script asserting the three critical surfaces contain zero AnythingLLM reference, with a planted bad fixture proving the guard fails on a regression (the positive-failure precedent from `ci.yml:69-73`).

The three critical surfaces (per the success criterion + RESEARCH): **search/semantic-search** (the edge fns re-pointed in 74-03), the **dashboard digest** (`frontend/src/hooks/useDashboardDigest.ts`), and the **assistant/copilot** (`frontend/src/components/copilot/useCopilotRuntime.ts` + `CopilotSurface.tsx`).

Depends on 74-02 (frontend ChatDock removal) so the FE critical-surface grep is already clean. File-disjoint from 74-09 (eval rubrics).

Purpose: Decommission the AnythingLLM infra and add the static EVAL-04 guard that runs in CI now (EVAL-04). Output: no AnythingLLM container/proxy/env; a passing static guard + its positive-failure fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-eval-gate-anythingllm-retirement/74-CONTEXT.md
@.planning/phases/74-eval-gate-anythingllm-retirement/74-RESEARCH.md

<read_first>

- deploy/docker-compose.prod.yml — line 47 `VITE_ANYTHINGLLM_URL` build-arg (DEAD, unreferenced in frontend/src); lines 82-86 backend env block (`ANYTHINGLLM_API_URL`, `ANYTHINGLLM_API_KEY`, `ANYTHINGLLM_WORKSPACE`, `AI_USE_ANYTHINGLLM: 'true'`); lines 129-160 the `anythingllm:` service (`mintplexlabs/anythingllm:latest` + healthcheck); line 354 the `anythingllm_data:` named volume. Remove ALL of these. Check `depends_on:` lists in other services for `anythingllm` and remove those entries too.
- deploy/docker-compose.mac.yml — line 9 is a DOC COMMENT mentioning anythingllm; there is NO `anythingllm:` service block here (verified). Remove the `anythingllm,` mention from the comment, and any `ANYTHINGLLM_*`/`VITE_ANYTHINGLLM_*` env or build-arg if present. Confirm no service/volume/depends_on references it.
- deploy/nginx/nginx.conf — lines 54-55 `upstream anythingllm { server anythingllm:3001; }`; lines 144-147 `location /llm/ { ... proxy_pass http://anythingllm/; }`. Remove both blocks.
- deploy/nginx/nginx.prod.conf — lines 54-55 `upstream anythingllm`; lines 167-171 `location /llm/`. Remove both blocks.
- backend/.env.example — lines 51-52 `ANYTHINGLLM_API_URL`/`ANYTHINGLLM_API_KEY`; line 67 `AI_USE_ANYTHINGLLM=false`. Remove. (VLLM_BASE_URL at 60 stays.)
- deploy/.env.example — line 17 `VITE_ANYTHINGLLM_URL`; lines 40-43 the `# ANYTHINGLLM` block (`ANYTHINGLLM_API_KEY`, etc.). Remove.
- scripts/check-edge-fn-schema-refs.mjs + scripts/edge-fn-schema-refs-allowlist.json — the PRECEDENT for a Node static-check script with a fixture-driven positive-failure (`ci.yml:69-73` runs `! node scripts/check-edge-fn-schema-refs.mjs <bad-fixture>`). Mirror its CLI shape: accept an optional path arg to scan (default = the real critical surfaces), exit 1 if a forbidden token is found.
- frontend/src/hooks/useDashboardDigest.ts, frontend/src/components/copilot/useCopilotRuntime.ts, frontend/src/components/copilot/CopilotSurface.tsx, supabase/functions/search-semantic/index.ts, supabase/functions/semantic-search-unified/index.ts — the critical-surface files the guard scans. After 74-02/74-03 these are AnythingLLM-free.
  </read_first>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Remove AnythingLLM from compose, nginx, and env-examples</name>
  <files>deploy/docker-compose.prod.yml, deploy/docker-compose.mac.yml, deploy/nginx/nginx.conf, deploy/nginx/nginx.prod.conf, backend/.env.example, deploy/.env.example</files>
  <read_first>deploy/docker-compose.prod.yml, deploy/docker-compose.mac.yml, deploy/nginx/nginx.conf, deploy/nginx/nginx.prod.conf, backend/.env.example, deploy/.env.example</read_first>
  <action>
    Decommission the AnythingLLM infra + levers (D3).

    1. `docker-compose.prod.yml`: delete the `anythingllm:` service block (129-160), the `anythingllm_data:` volume (354), the backend env lines 82-86 (`ANYTHINGLLM_API_URL/API_KEY/WORKSPACE` + `AI_USE_ANYTHINGLLM`), and the dead `VITE_ANYTHINGLLM_URL` build-arg (47). Scan every service's `depends_on:` and remove any `- anythingllm` entry. Keep the vLLM/TEI/Langfuse/Phoenix services intact. Verify the YAML still parses (no dangling anchors/aliases).
    2. `docker-compose.mac.yml`: remove the `anythingllm,` token from the doc comment (line 9) and any `ANYTHINGLLM_*`/`VITE_ANYTHINGLLM_*` env/build-arg/depends_on if present. (No service block exists here.)
    3. `nginx.conf` + `nginx.prod.conf`: remove the `upstream anythingllm { ... }` block and the `location /llm/ { ... proxy_pass http://anythingllm/; }` block in BOTH files. Keep the frontend/backend/agent-runtime upstreams + `/api/copilot` proxy.
    4. `backend/.env.example`: remove lines 51-52 + 67 (ANYTHINGLLM_* + AI_USE_ANYTHINGLLM). `deploy/.env.example`: remove line 17 (VITE_ANYTHINGLLM_URL) + the 40-43 ANYTHINGLLM block.
    5. `grep -rin anythingllm deploy/ backend/.env.example` must return nothing (docs under deploy/*.md are out of scope but if a quick grep flags them, note in the SUMMARY — they are non-load-bearing).

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && test -z "$(grep -rin anythingllm deploy/docker-compose.prod.yml deploy/docker-compose.mac.yml deploy/nginx/nginx.conf deploy/nginx/nginx.prod.conf backend/.env.example deploy/.env.example)" && docker compose -f deploy/docker-compose.prod.yml config -q 2>&1 | tail -3 || echo "compose-config-checked"</automated>
  </verify>
  <done>No `anythingllm` container, volume, env, build-arg, depends_on, or nginx proxy/upstream remains in the six infra files; `grep -rin anythingllm` on them returns nothing; the prod compose still parses (`docker compose config -q`).</done>
</task>

<task type="auto">
  <name>Task 2: Static no-AnythingLLM critical-surface guard + positive-failure fixture</name>
  <files>scripts/check-no-anythingllm.mjs, tools/anythingllm-fixtures/critical-surface-with-anythingllm/sample.ts, package.json</files>
  <read_first>scripts/check-edge-fn-schema-refs.mjs, frontend/src/hooks/useDashboardDigest.ts, frontend/src/components/copilot/useCopilotRuntime.ts, supabase/functions/search-semantic/index.ts</read_first>
  <action>
    Add the CI-native static guard asserting the 3 critical surfaces are AnythingLLM-free (D6), with a planted bad fixture proving it fails on a regression.

    1. `scripts/check-no-anythingllm.mjs`: a Node ESM script mirroring `check-edge-fn-schema-refs.mjs`'s CLI shape. Default behavior (no arg): scan the critical-surface file list — `frontend/src/hooks/useDashboardDigest.ts`, `frontend/src/components/copilot/useCopilotRuntime.ts`, `frontend/src/components/copilot/CopilotSurface.tsx`, `supabase/functions/search-semantic/index.ts`, `supabase/functions/semantic-search-unified/index.ts`, `supabase/functions/position-suggestions-get/index.ts` — for the case-insensitive token `anythingllm` (in imports, URLs, or identifiers). Exit 0 if none found; print the offending file:line and exit 1 if any found. If given a path arg, scan that directory/file instead (so CI can point it at the bad fixture). Keep it dependency-free (Node built-ins only).
    2. `tools/anythingllm-fixtures/critical-surface-with-anythingllm/sample.ts`: a tiny file that DOES contain an `anythingllm` reference (e.g. `const url = process.env.ANYTHINGLLM_API_URL`) so `node scripts/check-no-anythingllm.mjs tools/anythingllm-fixtures/critical-surface-with-anythingllm` exits 1. This is the positive-failure fixture.
    3. `package.json` (root): add `"check:no-anythingllm": "node scripts/check-no-anythingllm.mjs"`. (The CI job in 74-10 will run both the real check and the `! node ... <bad-fixture>` assertion.)
    4. Run both locally: the real check passes (surfaces clean after 74-02/74-03), the bad-fixture check exits 1.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && node scripts/check-no-anythingllm.mjs && ! node scripts/check-no-anythingllm.mjs tools/anythingllm-fixtures/critical-surface-with-anythingllm && grep -q "check:no-anythingllm" package.json && echo "guard-ok"</automated>
  </verify>
  <done>`node scripts/check-no-anythingllm.mjs` exits 0 against the real critical surfaces; `! node scripts/check-no-anythingllm.mjs <bad-fixture>` (i.e. the script exits 1 on the planted fixture) holds; `package.json` has `check:no-anythingllm`.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary            | Description                                                                                                                      |
| ------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| deployment topology | Removing the AnythingLLM container + `/llm/` proxy shrinks the prod surface to on-prem vLLM/TEI + app services only.             |
| CI → static guard   | The guard is a dependency-free grep over committed source; no secret/endpoint needed — runs as a required check in CI-mode (D6). |

## STRIDE Threat Register

| Threat ID  | Category               | Component                  | Disposition | Mitigation Plan                                                                                                                                                                |
| ---------- | ---------------------- | -------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-74-08-01 | Tampering              | compose/nginx integrity    | mitigate    | Verify `docker compose config -q` parses after the container/volume/depends_on removal so a dangling reference can't silently break deploys.                                   |
| T-74-08-02 | Spoofing               | AI_USE_ANYTHINGLLM lever   | mitigate    | Removing the env lever + container means no runtime flag can re-enable an AnythingLLM path; the static guard catches any source-level reintroduction on the critical surfaces. |
| T-74-08-03 | Information disclosure | nginx /llm/ proxy          | mitigate    | Removing the externally-reachable `/llm/` location closes a proxy path to a now-absent service.                                                                                |
| T-74-08-04 | Repudiation            | regression detection       | mitigate    | The positive-failure fixture proves the guard FAILS on reintroduction, so a future AnythingLLM import on a critical surface is caught in CI, not in prod.                      |
| T-74-08-05 | Tampering              | npm/container supply chain | mitigate    | The `mintplexlabs/anythingllm:latest` image is removed from the topology (T-{phase}-SC class); no new package installs are introduced by this plan.                            |

</threat_model>

<verification>
- `grep -rin anythingllm` returns nothing across the six infra files.
- `docker compose -f deploy/docker-compose.prod.yml config -q` parses.
- `node scripts/check-no-anythingllm.mjs` exits 0; `! node scripts/check-no-anythingllm.mjs <bad-fixture>` holds.
- `package.json` has `check:no-anythingllm`.
- vLLM/TEI/copilot services + `/api/copilot` proxy intact.
</verification>

<success_criteria>

- AnythingLLM infra (container, volume, env, build-arg, nginx proxy/upstream, env-example keys) fully removed (D3).
- A CI-native static guard asserts the 3 critical surfaces stay AnythingLLM-free, with a proven positive-failure (D6).
  </success_criteria>

<output>
Create `.planning/phases/74-eval-gate-anythingllm-retirement/74-08-SUMMARY.md` when done. Note any deploy/*.md doc-only AnythingLLM mentions left as non-load-bearing.
</output>
