---
phase: 74-eval-gate-anythingllm-retirement
plan: 11
type: execute
wave: 6
depends_on: [74-06, 74-07, 74-10]
files_modified:
  - .planning/phases/74-eval-gate-anythingllm-retirement/74-VERIFICATION.md
autonomous: false
requirements: [EVAL-01, EVAL-03, EVAL-04]
must_haves:
  truths:
    - 'The EVAL-04 network-block UAT is executed live: with AnythingLLM blocked at the network layer, search/semantic-search, the dashboard digest, and the copilot assistant all still function (EN+AR)'
    - 'The live judge-scored EVAL-01 (briefing >=0.80) + EVAL-03 (Arabic >=0.75) rubrics pass against the on-prem gemma stack'
    - 'A full-repo `grep -rin anythingllm` over app code (excluding data-only legacy columns + docs) confirms the FULL rip-out is complete'
    - 'Closes the deploy-gated half of EVAL-01/03/04 once the GPU/gemma + TEI stack is live (same gate as P72/P73)'
  artifacts:
    - path: '.planning/phases/74-eval-gate-anythingllm-retirement/74-VERIFICATION.md'
      provides: 'Recorded live-verification evidence for EVAL-01/03/04'
      contains: 'EVAL-04'
  key_links:
    - from: '.planning/phases/74-eval-gate-anythingllm-retirement/74-VERIFICATION.md'
      to: '74-UAT.md'
      via: 'executes the network-block UAT spec'
      pattern: 'setBlockedURLs|network-block'
---

<objective>
Close the deploy-gated half of Phase 74: execute the EVAL-04 network-block live UAT (from `74-UAT.md`) and the live judge-scored EVAL-01/03 rubrics against the on-prem gemma/TEI stack, then record the evidence in `74-VERIFICATION.md`.

This is the phase-closing verification — it CANNOT run until the GPU/gemma host + TEI are live (the same deploy gate as P72/P73). It includes a human-verify checkpoint for the live bilingual UI checks and a final full-repo AnythingLLM-absence audit.

Depends on 74-06 + 74-07 (all edge/backend rip-out complete) and 74-10 (the CI eval-gate + the UAT spec exist).

Purpose: Verify EVAL-01/03/04 live and confirm the FULL rip-out. Output: `74-VERIFICATION.md` with recorded results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-eval-gate-anythingllm-retirement/74-CONTEXT.md
@.planning/phases/74-eval-gate-anythingllm-retirement/74-UAT.md

<read_first>

- .planning/phases/74-eval-gate-anythingllm-retirement/74-UAT.md (74-10) — the EVAL-04 network-block UAT spec to execute.
- agent-runtime/evals/ (74-01, 74-09) — the rubrics; live scoring activates when `EVAL_AI_URL`/`VLLM_BASE_URL` points at the on-prem gemma stack.
- MEMORY project_cdp_forced_error_uat_protocol — the CDP `Network.setBlockedURLs` execution mechanics.
- MEMORY project_v7_phase72_shipped_deploy_gated — the deploy-gate posture + the live-proof restore discipline (seed → prove → restore).
- The phase's deploy gate (GPU + gemma-4-12b + TEI 1024-dim + re-embed) — must be up before this plan runs.
  </read_first>
  </context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: DEPLOY GATE — bring up the on-prem gemma/TEI stack</name>
  <what-built>The full AnythingLLM rip-out + the eval harness are code-complete and CI-green (Waves 0-5). Live verification needs the on-prem serving stack.</what-built>
  <how-to-verify>
    Confirm the on-prem stack is reachable (same gate as P72/P73):
    1. vLLM `/v1/models` returns `gemma-4-12b` (or the configured on-prem model).
    2. TEI `/embed` returns a 1024-dim vector and `/rerank` responds.
    3. `EVAL_AI_URL`/`VLLM_BASE_URL` + `TEI_EMBED_URL` are set for the verification environment.
    4. (If not already done at the P72 gate) the bge-m3 re-embed of the corpus has run so semantic search has live vectors.
  </how-to-verify>
  <resume-signal>Type "stack up" once vLLM + TEI are reachable and the env vars are set, or "defer" to leave Phase 74 in the deploy-gated state (CI-green, live-verify pending).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Execute the EVAL-04 network-block UAT + live EVAL-01/03 scoring; full-repo audit</name>
  <files>.planning/phases/74-eval-gate-anythingllm-retirement/74-VERIFICATION.md</files>
  <read_first>.planning/phases/74-eval-gate-anythingllm-retirement/74-UAT.md, agent-runtime/evals/lib/judge.ts</read_first>
  <action>
    Run the deploy-gated verifications and record evidence.

    1. **EVAL-04 network-block UAT** (per `74-UAT.md`): with the on-prem stack up, use CDP `Network.setBlockedURLs` to block the AnythingLLM origin patterns, then exercise the 3 critical surfaces on staging (admin login from `.env.test`): semantic search (TEI-backed, returns results / graceful empty), dashboard digest (pure-SQL, unaffected), copilot assistant (on-prem turn; confirm ChatDock is gone). Assert via DOM (per `project_cdp_forced_error_uat_protocol`), capture network-tab evidence showing ZERO AnythingLLM requests. Do EN + AR where the surface is bilingual. Follow seed→prove→restore discipline for any seeded data.
    2. **Live EVAL-01/03 scoring:** with `EVAL_AI_URL`/`VLLM_BASE_URL` set, run `pnpm --filter agent-runtime test:eval`. The 74-09 judge tests now activate — confirm EVAL-01 briefs (EN+AR) score >= 0.80 and EVAL-03 Arabic >= 0.75 on the golden fixtures. Record the scores. (If the judge flags a fixture, decide: tune the fixture/threshold vs a genuine quality gap — do not silently lower a threshold below the success-criterion value.)
    3. **Full-repo AnythingLLM-absence audit:** run `grep -rin anythingllm` across `frontend/src`, `backend/src`, `supabase/functions`, `deploy/` (excluding `*.md` docs + the data-only legacy `anythingllm_*` columns in `_shared/validation-schemas.ts` + `intelligence-get` + `contact-directory.types.ts`/`database.types.ts`). The ONLY permitted hits are those documented data-only legacy fields; everything else must be zero. Record the audit output.
    4. Write `74-VERIFICATION.md`: per-requirement (EVAL-01/02/03/04) status with evidence — EVAL-02 + CI-mode gate already green from CI (cite the run); EVAL-01/03 live scores; EVAL-04 UAT results (the 3 surfaces, EN+AR, network evidence); the full-repo audit (with the explicit allowlist of data-only legacy hits). Mark any still-pending item clearly.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && grep -q "EVAL-04" .planning/phases/74-eval-gate-anythingllm-retirement/74-VERIFICATION.md && grep -qi "network-block\|setBlockedURLs" .planning/phases/74-eval-gate-anythingllm-retirement/74-VERIFICATION.md && grep -qi "EVAL-01\|0.80\|briefing" .planning/phases/74-eval-gate-anythingllm-retirement/74-VERIFICATION.md</automated>
  </verify>
  <done>The EVAL-04 network-block UAT is executed (3 surfaces work with AnythingLLM blocked, EN+AR, zero AnythingLLM requests), live EVAL-01 (>=0.80) + EVAL-03 (>=0.75) scores are recorded, the full-repo audit shows only documented data-only legacy hits, and `74-VERIFICATION.md` records per-requirement evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human sign-off on the live bilingual surfaces</name>
  <what-built>EVAL-04 network-block UAT + live EVAL-01/03 scoring executed and recorded in 74-VERIFICATION.md.</what-built>
  <how-to-verify>
    With AnythingLLM blocked at the network layer on staging, confirm by eye:
    1. Semantic search returns results (or a clean empty state) in EN and AR — no hang, no AnythingLLM error.
    2. The dashboard digest renders normally (unaffected — pure SQL).
    3. The copilot drawer answers a turn on-prem in EN and AR (RTL/Tajawal correct); the legacy ChatDock is gone (single assistant surface).
    4. The recorded EVAL-01/03 judge scores meet thresholds (>=0.80 / >=0.75).
  </how-to-verify>
  <resume-signal>Type "verified" if all surfaces work with AnythingLLM blocked and the scores meet thresholds, or describe any failure to feed a gap-closure pass.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                                                                 |
| ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| live staging → blocked AnythingLLM | The UAT proves zero runtime dependency on AnythingLLM by blocking it at the network layer — the strongest EVAL-04 evidence. |
| eval judge → on-prem gemma         | Live scoring contacts only the on-prem endpoint (zero-egress); fixtures are synthetic.                                      |

## STRIDE Threat Register

| Threat ID  | Category               | Component             | Disposition | Mitigation Plan                                                                                                                                      |
| ---------- | ---------------------- | --------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-74-11-01 | Repudiation            | EVAL-04 runtime proof | mitigate    | Network-tab evidence (zero AnythingLLM requests) + DOM-asserted surface success are recorded in 74-VERIFICATION.md — auditable proof, not assertion. |
| T-74-11-02 | Tampering              | threshold integrity   | mitigate    | If a live judge score misses, do NOT lower a threshold below the success-criterion value (0.80 / 0.75) to force a pass; record the gap instead.      |
| T-74-11-03 | Information disclosure | live UAT seed data    | mitigate    | Follow seed→prove→restore discipline (P72 precedent); synthetic seed rows are restored after the proof so staging is left clean.                     |
| T-74-11-04 | Elevation of privilege | impersonation in UAT  | accept      | Clearance-sensitive checks use authenticated impersonation (P69 pattern), never service-role; this plan adds no service-role.                        |
| T-74-11-05 | Denial of service      | stack not up          | accept      | The deploy-gate checkpoint allows "defer" — Phase 74 stays CI-green with live-verify pending, no forced failure.                                     |

</threat_model>

<verification>
- `74-VERIFICATION.md` records EVAL-04 (3 surfaces work with AnythingLLM blocked, EN+AR, zero AnythingLLM network requests), live EVAL-01 (>=0.80) + EVAL-03 (>=0.75) scores, and the full-repo audit (only documented data-only legacy hits).
- Human sign-off captured on the live bilingual surfaces.
- Any pending/failed item is explicitly recorded for gap closure.
</verification>

<success_criteria>

- EVAL-01/03/04 verified live against the on-prem gemma/TEI stack (deploy-gated half closed).
- The FULL AnythingLLM rip-out is confirmed by the full-repo audit (only data-only legacy columns remain, by design).
  </success_criteria>

<output>
Create `.planning/phases/74-eval-gate-anythingllm-retirement/74-11-SUMMARY.md` when done.
</output>
