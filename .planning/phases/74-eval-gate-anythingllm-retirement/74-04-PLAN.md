---
phase: 74-eval-gate-anythingllm-retirement
plan: 04
type: execute
wave: 2
depends_on: [74-02]
files_modified:
  - backend/src/ai/config.ts
  - backend/src/ai/llm-router.ts
  - backend/src/services/anythingllm.service.ts
  - backend/src/ai/embeddings-service.ts
  - backend/src/services/vector.service.ts
  - backend/src/services/semantic-search.service.ts
  - backend/src/api/ai.ts
  - backend/src/middleware/security.ts
autonomous: true
requirements: [EVAL-04]
must_haves:
  truths:
    - 'The `anythingllm` provider, its off-switch (`AI_USE_ANYTHINGLLM`/`ANYTHINGLLM_API_URL`), and the default-provider auto-selection that resolved to it are removed from backend AI config (D3)'
    - '`backend/src/services/anythingllm.service.ts` is deleted and nothing imports it'
    - '`llmRouter` no longer has an AnythingLLM provider branch; `chatAssistantAgent` (used by brief-generator) compiles and routes only to on-prem/cloud providers — never AnythingLLM'
    - 'Backend embedding still works via BGE-M3 (local) primary; the AnythingLLM embedding fallback is removed from vector/semantic-search services'
    - 'Removes the backend AnythingLLM provider + off-switch + CSP allow-entry per D3; preserves the caller-JWT keystone (no new service-role)'
  artifacts:
    - path: 'backend/src/ai/config.ts'
      provides: 'AI config with the anythingllm provider + off-switch removed'
      contains: 'AIProvider'
    - path: 'backend/src/ai/llm-router.ts'
      provides: 'Router with no AnythingLLM provider branch'
      contains: 'llmRouter'
  key_links:
    - from: 'backend/src/ai/llm-router.ts'
      to: 'on-prem/cloud providers only'
      via: 'REMOVED chatAnythingLLM branch + AnythingLLMService import'
      pattern: 'anythingllm'
    - from: 'backend/src/ai/config.ts'
      to: 'default provider'
      via: 'REMOVED AI_USE_ANYTHINGLLM auto-select'
      pattern: 'AI_USE_ANYTHINGLLM'
---

<objective>
Rip AnythingLLM out of the backend AI core (D3): remove the `anythingllm` provider, its off-switch levers (`AI_USE_ANYTHINGLLM` / `ANYTHINGLLM_API_URL`), the default-provider auto-selection that resolved to it, the `llmRouter` AnythingLLM branch, the standalone `anythingllm.service.ts` client, the AnythingLLM embedding fallback in the vector/semantic-search services, the AI health-check probe, and the CSP connect-src allow entry.

After this plan, `llmRouter` (and therefore `chatAssistantAgent`, which brief-generator still uses) routes only to on-prem (vLLM/Ollama) + cloud providers — never AnythingLLM. Embeddings stay on BGE-M3 (local) primary.

**Out of scope (handled elsewhere to avoid file overlap):**

- The legacy brief writer (`brief.service.ts`, `dossiers-briefs-generate`) and the backend `api/ai/dossier-field-assist.ts` AnythingLLM call → Plan 74-06.
- `intelligence-refresh*` country-intel generation → Plan 74-05.
- Edge functions, compose/nginx/env → Plans 74-05/06/07.

Depends on 74-02 (which already removed the `/api/ai/chat` route that drove `chatAssistantAgent` over AnythingLLM; this plan severs the provider underneath it).

Purpose: Remove AnythingLLM from the backend AI substrate (EVAL-04). Output: no `anythingllm` provider, service, off-switch, health probe, or CSP entry in the backend AI core.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-eval-gate-anythingllm-retirement/74-CONTEXT.md
@.planning/phases/74-eval-gate-anythingllm-retirement/74-RESEARCH.md

<read_first>

- backend/src/ai/config.ts — line 5 `AIProvider` union includes `'anythingllm'`; lines 117-122 the `anythingllm` provider entry (`enabled: !!ANYTHINGLLM_API_URL || AI_USE_ANYTHINGLLM === 'true'`); lines 155-158 a DEFAULT-PROVIDER selector that returns `'anythingllm'` when the off-switch is set. `getProviderConfig` (244) / `getPrivateProvider` (249) read the providers map. Remove the `anythingllm` union member, the provider entry, AND the default-provider auto-select branch. `getPrivateProvider` must resolve to vLLM/Ollama (the configured private provider), never anythingllm.
- backend/src/ai/llm-router.ts — line 14 `import AnythingLLMService`; line 105 `private anythingllm`; lines 116-123 init block; line 399-400 `if (provider === 'anythingllm') chatAnythingLLM(...)`; lines 501-530 `private async chatAnythingLLM(...)`; lines 558-561 the streaming AnythingLLM branch. Remove ALL of these. After removal, the provider-selection logic must never yield `'anythingllm'` (guaranteed by the config change). `chatAssistantAgent` imports `llmRouter` — it must still compile.
- backend/src/services/anythingllm.service.ts — the client. Imported ONLY by llm-router.ts (verified). DELETE the file after removing the llm-router import.
- backend/src/ai/embeddings-service.ts — lines 29, 37 include `'anythingllm'` in the `provider` union type, but the actual provider chain (273-307) is local / edge-function / openai — there is NO anythingllm runtime branch. Just remove `'anythingllm'` from the two union types.
- backend/src/services/vector.service.ts — `generateEmbeddingFromText` (BGE-M3 primary, lines ~65-90) then an AnythingLLM `/v1/embeddings` fallback (~90-110); `anythingLLMAvailable` field (25); `useBGEM3 = !AI_USE_ANYTHINGLLM` (31); a `checkAnythingLLMStatus` method. Remove the AnythingLLM fallback fetch + the `anythingLLMAvailable`/`checkAnythingLLMStatus` plumbing; keep BGE-M3 primary and the keyword/`fallbackSearch` degradation. `useBGEM3` should default true unconditionally (drop the `AI_USE_ANYTHINGLLM` read).
- backend/src/services/semantic-search.service.ts — lines 7, 113, 258, 265-266, 277 reference AnythingLLM in comments + a `checkAnythingLLMStatus` call. Re-word comments to BGE-M3-only; replace the `vectorService.checkAnythingLLMStatus()` availability check with the BGE-M3/local availability signal (or drop the fallback branch if it only guarded AnythingLLM).
- backend/src/api/ai.ts — lines 32-49 a health block probing AnythingLLM `/health` and an `anythingllmHealth` object folded into overall health. Remove the probe + the `anythingllmHealth` field; recompute overall health from the embedding/on-prem providers only. (The `/chat` mount is already gone from 74-02.)
- backend/src/middleware/security.ts — lines 123-124 a CSP `connect-src` entry `process.env.ANYTHINGLLM_API_URL || null`. Remove that line (the `.filter(Boolean)` keeps the array valid).
- backend/src/ai/agents/chat-assistant.ts — DO NOT edit here, but confirm it still compiles after llm-router loses the AnythingLLM branch (it binds via `llmRouter`, provider-agnostic). brief-generator.ts depends on it.
  </read_first>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the anythingllm provider, off-switch, llm-router branch, and client</name>
  <files>backend/src/ai/config.ts, backend/src/ai/llm-router.ts, backend/src/services/anythingllm.service.ts</files>
  <read_first>backend/src/ai/config.ts, backend/src/ai/llm-router.ts</read_first>
  <action>
    Sever the AnythingLLM provider from the backend AI config + router (D3).

    1. `config.ts`: remove `'anythingllm'` from the `AIProvider` union (line 5); delete the `anythingllm` provider entry (117-122); delete the default-provider auto-select branch that returns `'anythingllm'` when `AI_USE_ANYTHINGLLM`/`ANYTHINGLLM_API_URL` is set (155-158) — the default provider must fall through to the configured on-prem/cloud default with NO AnythingLLM consideration. Ensure `getPrivateProvider()` (249) resolves to the vLLM/Ollama private provider. Remove any now-dead `AI_USE_ANYTHINGLLM` / `ANYTHINGLLM_API_URL` reads in this file. The `providers` map is typed `Record<AIProvider, AIProviderConfig>` — after dropping the union member, also drop the map key so the type stays exhaustive.
    2. `llm-router.ts`: remove the `import AnythingLLMService` (14), the `private anythingllm` field (105), the init block (116-123), the dispatch branch `if (provider === 'anythingllm') ...` (399-400), the whole `chatAnythingLLM` method (501-530), and the streaming AnythingLLM branch (558-561). Remove the `providerHealth.set('anythingllm', ...)` line. After removal there must be no `anythingllm` identifier left. Verify the remaining provider branches (openai/anthropic/google/vllm/ollama) are intact and `chatAssistantAgent`'s usage of `llmRouter` still type-checks.
    3. Delete `backend/src/services/anythingllm.service.ts` (only llm-router imported it).
    4. `grep -rin anythingllm backend/src/ai/config.ts backend/src/ai/llm-router.ts` must return nothing, and `backend/src/services/anythingllm.service.ts` must not exist.

  </action>
  <verify>
    <automated>cd backend && test ! -f src/services/anythingllm.service.ts && test -z "$(grep -rin anythingllm src/ai/config.ts src/ai/llm-router.ts)" && test -z "$(grep -rn "anythingllm.service" src)" && pnpm type-check 2>&1 | tail -6</automated>
  </verify>
  <done>`AIProvider` no longer includes `anythingllm`, the provider entry + default auto-select are gone, `llm-router.ts` has no AnythingLLM branch/field/import, `anythingllm.service.ts` is deleted with no importers, and `pnpm --filter intake-backend type-check` is clean (chat-assistant.ts + brief-generator.ts compile).</done>
</task>

<task type="auto">
  <name>Task 2: Remove AnythingLLM embedding fallback, health probe, and CSP entry</name>
  <files>backend/src/ai/embeddings-service.ts, backend/src/services/vector.service.ts, backend/src/services/semantic-search.service.ts, backend/src/api/ai.ts, backend/src/middleware/security.ts</files>
  <read_first>backend/src/services/vector.service.ts, backend/src/services/semantic-search.service.ts, backend/src/api/ai.ts, backend/src/middleware/security.ts</read_first>
  <action>
    Remove the remaining backend AnythingLLM touchpoints (embedding fallback, health, CSP) while keeping BGE-M3 embeddings and search working (EVAL-04 "surfaces still work").

    1. `embeddings-service.ts`: remove `'anythingllm'` from the two `provider` union types (lines 29, 37). No runtime branch exists for it, so this is type-only.
    2. `vector.service.ts`: delete the AnythingLLM `/v1/embeddings` fallback fetch in `generateEmbeddingFromText`, the `anythingLLMAvailable` field, and the `checkAnythingLLMStatus` method. Set `useBGEM3 = true` unconditionally (drop the `AI_USE_ANYTHINGLLM` constructor read). Keep BGE-M3 primary and the keyword/`fallbackSearch` degradation path. After removal, `generateEmbeddingFromText` returns the BGE-M3 vector or `null` (then keyword fallback) — never AnythingLLM.
    3. `semantic-search.service.ts`: re-word the AnythingLLM comments (7, 258) to "BGE-M3"; remove the `checkAnythingLLMStatus()` availability call (265-266, 277) — use the BGE-M3/local availability instead, or drop the branch if it only existed to guard AnythingLLM. Update the warning string (113) to not name AnythingLLM.
    4. `api/ai.ts`: remove the AnythingLLM health probe (32-49) and the `anythingllmHealth` object; recompute `hasEmbeddingProvider`/overall health from the embedding + on-prem providers only.
    5. `security.ts`: remove the CSP `connect-src` line `process.env.ANYTHINGLLM_API_URL || null` (123-124). The `.filter(Boolean)` already tolerates the shorter array.
    6. `grep -rin anythingllm` across all five files must return nothing.

  </action>
  <verify>
    <automated>cd backend && test -z "$(grep -rin anythingllm src/ai/embeddings-service.ts src/services/vector.service.ts src/services/semantic-search.service.ts src/api/ai.ts src/middleware/security.ts)" && pnpm type-check 2>&1 | tail -6</automated>
  </verify>
  <done>No AnythingLLM embedding fallback, health probe, or CSP entry remains; BGE-M3 stays the primary embedder with keyword degradation; `grep -rin anythingllm` on the five files returns nothing; `pnpm --filter intake-backend type-check` is clean.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                                                              |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| backend → LLM provider       | After removal, `llmRouter` reaches only on-prem (vLLM/Ollama) + cloud providers; AnythingLLM (off-prem-capable) is gone. |
| browser CSP connect-src      | Dropping the AnythingLLM origin tightens the allow-list to actually-used endpoints.                                      |
| backend → embedding provider | BGE-M3 (local) primary; no off-prem embedder fallback.                                                                   |

## STRIDE Threat Register

| Threat ID  | Category               | Component                     | Disposition | Mitigation Plan                                                                                                                                                        |
| ---------- | ---------------------- | ----------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-74-04-01 | Tampering              | chat-assistant agent (shared) | mitigate    | Do NOT edit `chat-assistant.ts`/`brief-generator.ts`; verify both compile after the router loses its AnythingLLM branch (router is provider-agnostic via `llmRouter`). |
| T-74-04-02 | Elevation of privilege | keystone / service-role       | mitigate    | This plan removes a provider + fallbacks; it introduces NO `supabaseAdmin`/service-role usage. The caller-JWT keystone and RLS posture are untouched.                  |
| T-74-04-03 | Information disclosure | CSP allow-list                | mitigate    | Remove the AnythingLLM origin from `connect-src` so the browser cannot be steered to an unused external endpoint.                                                      |
| T-74-04-04 | Denial of service      | embedding availability        | mitigate    | BGE-M3 stays primary with a keyword/`fallbackSearch` degradation; removing the AnythingLLM fallback does not crash search when embeddings are unavailable.             |
| T-74-04-05 | Spoofing               | default-provider auto-select  | mitigate    | Remove the `AI_USE_ANYTHINGLLM` default-provider branch so a stray env var can no longer silently re-route traffic to AnythingLLM.                                     |

</threat_model>

<verification>
- `backend/src/services/anythingllm.service.ts` does not exist; no file imports `anythingllm.service`.
- `grep -rin "anythingllm\|AI_USE_ANYTHINGLLM\|ANYTHINGLLM_API_URL" backend/src/ai backend/src/services/vector.service.ts backend/src/services/semantic-search.service.ts backend/src/api/ai.ts backend/src/middleware/security.ts` returns nothing.
- `pnpm --filter intake-backend type-check` is clean; `chat-assistant.ts` + `brief-generator.ts` still compile.
- BGE-M3 remains the primary embedder; search degrades to keyword, not crash.
- NOTE for 74-06: `brief.service.ts` + backend `api/ai/dossier-field-assist.ts` still reference AnythingLLM — they are intentionally left for 74-06.
</verification>

<success_criteria>

- AnythingLLM provider, off-switch, default auto-select, router branch, client, embedding fallback, health probe, and CSP entry removed from the backend AI core (D3).
- On-prem/cloud routing + BGE-M3 embeddings preserved; keystone untouched (no new service-role).
  </success_criteria>

<output>
Create `.planning/phases/74-eval-gate-anythingllm-retirement/74-04-SUMMARY.md` when done. Note that `brief.service.ts` and backend `api/ai/dossier-field-assist.ts` remain for 74-06.
</output>
