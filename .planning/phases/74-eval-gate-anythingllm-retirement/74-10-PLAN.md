---
phase: 74-eval-gate-anythingllm-retirement
plan: 10
type: execute
wave: 5
depends_on: [74-01, 74-08, 74-09]
files_modified:
  - .github/workflows/ci.yml
  - .planning/phases/74-eval-gate-anythingllm-retirement/74-UAT.md
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-03, EVAL-04]
must_haves:
  truths:
    - 'CI has an `eval-gate` job that runs (CI-mode) EVAL-02 computed precision/recall + the EVAL-01/03 structural+positive-failure proofs + the `check:no-anythingllm` guard — as a REQUIRED check (D6)'
    - 'The job asserts the positive-failure fixtures FAIL (degraded correlation set + the planted critical-surface fixture) so a real regression breaks the build'
    - "Live judge scoring (EVAL-01/03) runs only when the judge endpoint secret is present (`if: ${{ secrets.EVAL_AI_URL != '' }}`) and is NON-blocking until the GPU host is permanent (D6)"
    - 'A `74-UAT.md` documents the EVAL-04 network-block live UAT (CDP `Network.setBlockedURLs`) proving the 3 critical surfaces still work with AnythingLLM blocked — deploy-gated, not a CI test (D6)'
    - 'Wires the CI eval-gate (EVAL-01/02/03) + the EVAL-04 network-block UAT spec per D6'
  artifacts:
    - path: '.github/workflows/ci.yml'
      provides: 'eval-gate job (two-mode: required CI-mode + deploy-gated live-mode)'
      contains: 'eval-gate'
    - path: '.planning/phases/74-eval-gate-anythingllm-retirement/74-UAT.md'
      provides: 'EVAL-04 network-block live UAT spec (CDP setBlockedURLs)'
      contains: 'setBlockedURLs'
  key_links:
    - from: '.github/workflows/ci.yml'
      to: 'agent-runtime eval harness'
      via: 'pnpm --filter agent-runtime test:eval + check:no-anythingllm'
      pattern: 'test:eval|check:no-anythingllm'
    - from: '.github/workflows/ci.yml'
      to: 'deploy-gated live scoring'
      via: "if secrets.EVAL_AI_URL != ''"
      pattern: 'EVAL_AI_URL'
---

<objective>
Wire the CI `eval-gate` job (D6, two-mode) and author the EVAL-04 network-block live-UAT spec.

**CI-mode (required check):** runs the CI-native evals — EVAL-02 computed precision/recall (74-01), the EVAL-01/03 structural + positive-failure proofs (74-09, judge absent), and the `check:no-anythingllm` static guard + its planted-fixture positive-failure (74-08). This gates the build now, no GPU.

**Live-mode (deploy-gated, non-blocking):** the generative EVAL-01/03 judge scoring runs only when `secrets.EVAL_AI_URL` is set, mirroring the P72/P73 deploy gate so CI stays green until the on-prem GPU host is permanent.

**EVAL-04 live UAT:** documented (not a CI test) — block the AnythingLLM endpoint at the network layer (CDP `Network.setBlockedURLs`, per `project_cdp_forced_error_uat_protocol`) and confirm the 3 critical surfaces (search suggestions, dashboard digest, assistant) still function. Deploy-gated.

Depends on 74-01 (EVAL-02 + harness), 74-08 (the static guard + script), 74-09 (EVAL-01/03 rubrics). This is the integration plan that makes the rubrics gate CI.

Purpose: Make AI-quality regression-gating live in CI (EVAL-01/02/03) and specify the EVAL-04 verification (EVAL-04). Output: the `eval-gate` CI job + the network-block UAT spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-eval-gate-anythingllm-retirement/74-CONTEXT.md
@.planning/phases/74-eval-gate-anythingllm-retirement/74-RESEARCH.md

<read_first>

- .github/workflows/ci.yml — the `type-check` job (127-150) is the cleanest job template to mirror: `runs-on: ubuntu-latest`, `needs: [repo-policy]`, `actions/checkout@v4`, `pnpm/action-setup@v4`, `actions/setup-node@v4` with `node-version: ${{ env.NODE_VERSION }}` + `cache: 'pnpm'`, `pnpm install --frozen-lockfile`, then `pnpm --filter <name> ...`. Top-level `env:` (line 9) holds `NODE_VERSION`. The secrets-gating precedent is the e2e job's `env:` block (223-229) using `${{ secrets.X }}`. The positive-failure precedent is repo-policy (64-73): `! node scripts/check-edge-fn-schema-refs.mjs <bad-fixture>`.
- agent-runtime/package.json — `test:eval` (74-01) runs the eval config; the workspace filter name is `agent-runtime` (pnpm-workspace.yaml line 7; package `"name": "agent-runtime"`).
- Root package.json — `check:no-anythingllm` (74-08).
- agent-runtime/evals/lib/judge.ts (74-09) — `isJudgeConfigured()` checks `EVAL_AI_URL || VLLM_BASE_URL`. The live-mode step sets that env from the secret so the 74-09 judge tests activate; CI-mode leaves it unset so they skip.
- .planning/phases/72-agent-platform-runtime-retrieval-reads/72-UAT.md — the UAT.md format precedent (sections, steps, pass criteria, deploy-gate note). Mirror it.
- MEMORY project_cdp_forced_error_uat_protocol — the network-block UAT pattern: block at the CDP layer (`Network.setBlockedURLs`), wait through TanStack retries, assert via DOM (role=alert / functional success), screenshots can wedge so prefer DOM assertions; `*` globs cross `/` in CDP patterns.
  </read_first>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Add the eval-gate CI job (required CI-mode + deploy-gated live-mode)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml, agent-runtime/package.json</read_first>
  <action>
    Add one `eval-gate` job to `.github/workflows/ci.yml` (D6), mirroring the `type-check` job scaffold.

    1. New job `eval-gate` (`name: Eval Gate`), `runs-on: ubuntu-latest`, `needs: [lint, type-check]` (the RESEARCH-specified deps; matches the `bundle-size-check`/`test-rtl-responsive` precedent that gates on lint+type-check). Standard checkout + pnpm + Node (`${{ env.NODE_VERSION }}`, `cache: 'pnpm'`) + `pnpm install --frozen-lockfile`.
    2. **CI-mode steps (required — no `continue-on-error`, no secret guard):**
       - `pnpm --filter agent-runtime test:eval` — runs EVAL-02 (74-01) + the EVAL-01/03 structural+degraded checks (74-09). With no judge env set, the 74-09 live-scoring assertions skip; EVAL-02 + all positive-failure structural checks run and must pass.
       - `pnpm run check:no-anythingllm` — the static critical-surface guard (74-08).
       - Positive-failure assertion: `! node scripts/check-no-anythingllm.mjs tools/anythingllm-fixtures/critical-surface-with-anythingllm` (asserts the guard exits 1 on the planted fixture), exactly mirroring the repo-policy bad-fixture step.
    3. **Live-mode step (deploy-gated, non-blocking):** a step `Live judge scoring (deploy-gated)` guarded by `if: ${{ secrets.EVAL_AI_URL != '' }}`, with `env: { EVAL_AI_URL: ${{ secrets.EVAL_AI_URL }} }` (and any judge key secret), running `pnpm --filter agent-runtime test:eval`. Because `isJudgeConfigured()` now sees the endpoint, the 74-09 judge tests activate. Mark it `continue-on-error: true` so it is NON-blocking until the GPU host is permanent (D6).
    4. Do NOT add `eval-gate` to any other job's `needs` that would make a deploy-gated step block (it only depends on lint+type-check; downstream `build`/`docker-build` are unaffected). Keep YAML valid (indentation matches sibling jobs).

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && grep -q "eval-gate" .github/workflows/ci.yml && grep -q "test:eval" .github/workflows/ci.yml && grep -q "check:no-anythingllm" .github/workflows/ci.yml && grep -q "EVAL_AI_URL" .github/workflows/ci.yml && node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); require('js-yaml')?.load?.(y) ?? 0" 2>/dev/null; python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo "yaml-valid"</automated>
  </verify>
  <done>`.github/workflows/ci.yml` has an `eval-gate` job (`needs: [lint, type-check]`) whose required steps run `test:eval` + `check:no-anythingllm` + the bad-fixture positive-failure, and whose deploy-gated `EVAL_AI_URL`-guarded live-scoring step is `continue-on-error: true`. The YAML parses.</done>
</task>

<task type="auto">
  <name>Task 2: Author the EVAL-04 network-block live-UAT spec</name>
  <files>.planning/phases/74-eval-gate-anythingllm-retirement/74-UAT.md</files>
  <read_first>.planning/phases/72-agent-platform-runtime-retrieval-reads/72-UAT.md</read_first>
  <action>
    Write `74-UAT.md` specifying the EVAL-04 network-block live UAT (D6 — a live UAT, NOT a CI test), mirroring the 72-UAT.md format.

    1. Header: phase, requirement EVAL-04, deploy-gated note (same GPU/staging posture as P72/P73), the success criterion verbatim ("the critical AI path — search suggestions, dashboard digest, assistant — makes zero AnythingLLM calls, verified by blocking the AnythingLLM endpoint at the network level and confirming all three surfaces continue to function").
    2. Setup: staging app (zkrcjzdemdmwhearhfgg), admin login from `.env.test`, the CDP bridge per `project_cdp_forced_error_uat_protocol`.
    3. The block step: use CDP `Network.setBlockedURLs` to block the AnythingLLM origin patterns (e.g. `*anythingllm*`, `*:3001/*`, and the deployed `/llm/*` path) — note that `*` globs cross `/` in CDP patterns.
    4. Three surface checks (each: action → expected result with the endpoint blocked):
       - **Search suggestions / semantic search:** run a semantic search; expect results (now TEI-backed, on-prem) or graceful degradation — never a hang/AnythingLLM error. Assert via DOM (results list / empty-state), not screenshots.
       - **Dashboard digest:** open the dashboard; the digest renders (it is pure-SQL, zero-LLM — must be wholly unaffected).
       - **Assistant (copilot):** open the copilot drawer, send a turn; expect an on-prem response (deploy-gated on the gemma stack) — and confirm the legacy ChatDock is GONE (no second assistant surface).
    5. Pass criteria + an evidence checklist (DOM assertions, network-tab showing zero AnythingLLM requests, EN+AR where relevant). Note the live judge-scoring half (EVAL-01/03) is verified by the deploy-gated CI live-mode step, cross-referenced here.
    6. Mark the whole UAT DEPLOY-GATED (runs after the GPU/gemma host + TEI are live), consistent with the phase's deploy gate.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && grep -q "setBlockedURLs" .planning/phases/74-eval-gate-anythingllm-retirement/74-UAT.md && grep -qi "search suggestions\|semantic" .planning/phases/74-eval-gate-anythingllm-retirement/74-UAT.md && grep -qi "digest" .planning/phases/74-eval-gate-anythingllm-retirement/74-UAT.md && grep -qi "copilot\|assistant" .planning/phases/74-eval-gate-anythingllm-retirement/74-UAT.md && grep -qi "deploy-gated" .planning/phases/74-eval-gate-anythingllm-retirement/74-UAT.md</automated>
  </verify>
  <done>`74-UAT.md` specifies the CDP `Network.setBlockedURLs` block of the AnythingLLM origin and the three surface checks (search, digest, copilot) with DOM-based pass criteria, marked deploy-gated, mirroring 72-UAT.md.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                                                                                                              |
| ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| CI required check        | CI-mode steps need no secret/endpoint — pure computed evals + static grep — so the required gate cannot be bypassed by a missing secret. |
| CI → judge secret        | Live scoring is secret-gated + non-blocking; a leaked-empty secret simply skips, never fails the build.                                  |
| live UAT → network layer | Blocking AnythingLLM at CDP proves zero-dependency at runtime, complementing the static source guard.                                    |

## STRIDE Threat Register

| Threat ID  | Category               | Component                      | Disposition | Mitigation Plan                                                                                                                                                 |
| ---------- | ---------------------- | ------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-74-10-01 | Tampering              | required vs non-blocking split | mitigate    | CI-mode steps have NO `continue-on-error` (a real regression fails the build); only the deploy-gated live-scoring step is `continue-on-error` + secret-guarded. |
| T-74-10-02 | Spoofing               | empty judge secret             | mitigate    | `if: ${{ secrets.EVAL_AI_URL != '' }}` skips live scoring when unset — no false green from a half-configured judge, and no hard failure either.                 |
| T-74-10-03 | Information disclosure | secrets in CI                  | accept      | The judge endpoint is provided as a GitHub secret only at the deploy-gated step; CI-mode never touches it.                                                      |
| T-74-10-04 | Repudiation            | EVAL-04 runtime proof          | mitigate    | The network-block UAT proves at runtime (not just statically) that the surfaces work with AnythingLLM unreachable — the exact success criterion.                |
| T-74-10-05 | Denial of service      | GPU host absent                | accept      | The deploy-gated steps + UAT do not run until the host exists; CI stays green meanwhile (D6).                                                                   |

</threat_model>

<verification>
- `.github/workflows/ci.yml` has `eval-gate` (`needs: [lint, type-check]`); CI-mode runs `test:eval` + `check:no-anythingllm` + the bad-fixture positive-failure (required); the `EVAL_AI_URL`-guarded live step is `continue-on-error`.
- The workflow YAML parses (`python3 -c "import yaml; yaml.safe_load(...)"`).
- `74-UAT.md` specifies the CDP network-block of AnythingLLM + the 3 surface checks with DOM pass criteria, marked deploy-gated.
</verification>

<success_criteria>

- CI eval-gate gates the build on EVAL-01/02/03 regressions in CI-mode (required), with live judge scoring deploy-gated + non-blocking (D6).
- EVAL-04 network-block live UAT is specified (deploy-gated), proving the critical surfaces work with AnythingLLM blocked.
  </success_criteria>

<output>
Create `.planning/phases/74-eval-gate-anythingllm-retirement/74-10-SUMMARY.md` when done.
</output>
