---
phase: 74-eval-gate-anythingllm-retirement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/_protected.tsx
  - frontend/src/components/ai/ChatDock.tsx
  - frontend/src/hooks/useAIChat.ts
  - frontend/src/domains/ai/hooks/useAIChat.ts
  - frontend/src/domains/ai/index.ts
  - backend/src/api/ai.ts
  - backend/src/api/ai/chat.ts
autonomous: true
requirements: [EVAL-04]
must_haves:
  truths:
    - 'The legacy ChatDock is no longer mounted; the P72/P73 copilot drawer is the sole assistant entry point (D2)'
    - '`POST /api/ai/chat` no longer exists — the route is unmounted and its router file is deleted'
    - 'No frontend code references `useAIChat` or `<ChatDock>`; the build and type-check stay green with no orphaned imports'
    - 'Retires the ChatDock assistant path per D2; `chatAssistantAgent` is intentionally KEPT because brief-generator.ts depends on it (its AnythingLLM provider is severed in 74-04, not here)'
  artifacts:
    - path: 'frontend/src/routes/_protected.tsx'
      provides: 'Protected layout with ChatDock removed (import + mount + now-orphaned handler)'
      contains: '_protected'
    - path: 'backend/src/api/ai.ts'
      provides: 'AI router with the /chat sub-route unmounted'
      contains: 'router'
  key_links:
    - from: 'frontend/src/routes/_protected.tsx'
      to: 'ChatDock'
      via: 'REMOVED import + mount'
      pattern: 'ChatDock'
    - from: 'backend/src/api/ai.ts'
      to: './ai/chat.js'
      via: "REMOVED router.use('/chat', chatRouter)"
      pattern: 'chat'
---

<objective>
Retire the legacy ChatDock assistant path (D2): un-mount `<ChatDock>` from the protected layout, delete the `POST /api/ai/chat` route and its router file, and remove the now-orphaned `useAIChat` hook + ChatDock component. The P72/P73 copilot drawer (`/api/copilot/chat`, on-prem only) becomes the sole assistant entry point.

This is the first AnythingLLM-removal slice and the cleanest: ChatDock is the only consumer of `/api/ai/chat`, and that route is the legacy AnythingLLM chat surface (`AI_USE_ANYTHINGLLM=true` in prod).

**Critical scope guard:** `chatAssistantAgent` (`backend/src/ai/agents/chat-assistant.ts`) is ALSO imported by `backend/src/ai/agents/brief-generator.ts` — so this plan does NOT delete that agent. It only removes the HTTP route + the frontend surface. The agent's AnythingLLM provider is severed centrally in Plan 74-04 (backend llm-router rip-out).

Purpose: Eliminate one of the two non-on-prem assistant surfaces on the critical path (EVAL-04). Output: ChatDock and `/api/ai/chat` gone; copilot is the only chat.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-eval-gate-anythingllm-retirement/74-CONTEXT.md
@.planning/phases/74-eval-gate-anythingllm-retirement/74-RESEARCH.md

<read_first>

- frontend/src/routes/\_protected.tsx — line 5 `import { ChatDock } from '@/components/ai/ChatDock'`, line 103 `<ChatDock onCitationClick={handleCitationClick} />`, line 79 `const handleCitationClick = (...)`. `handleCitationClick` is used ONLY by ChatDock (verified) — remove it too once ChatDock is gone, and drop any now-unused imports it pulled in.
- frontend/src/components/ai/ChatDock.tsx — line 23 `import { useAIChat } from '@/hooks/useAIChat'`. The whole component is being deleted.
- frontend/src/hooks/useAIChat.ts — 6-line deprecated re-export shim of the domains copy.
- frontend/src/domains/ai/hooks/useAIChat.ts — the 203-line real hook (calls `/api/ai/chat`). Delete it.
- frontend/src/domains/ai/index.ts — lines 9-10 export `useAIChat` + its types FROM the barrel. The barrel ALSO exports `useAIFieldAssist` (line 12) and `useGenerateBrief` (line 15) which are LIVE and unrelated — remove ONLY the `useAIChat` export lines (9-10), leave the rest of the barrel intact.
- backend/src/api/ai.ts — line 12 `import chatRouter from './ai/chat.js'`, line 112 `router.use('/chat', chatRouter)`. Remove both. Leave the other AI sub-routers (briefs, intake-linking, dossier-field-assist) mounted.
- backend/src/api/ai/chat.ts — the 391-line router (`POST /`, `GET /history`, `GET /:runId/messages`). It imports `chatAssistantAgent` from `../../ai/agents/chat-assistant.js`. Delete this file. Do NOT delete `chat-assistant.ts` — brief-generator.ts still imports `chatAssistantAgent`.
  </read_first>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Un-mount ChatDock + delete the frontend chat surface (D2)</name>
  <files>frontend/src/routes/_protected.tsx, frontend/src/components/ai/ChatDock.tsx, frontend/src/hooks/useAIChat.ts, frontend/src/domains/ai/hooks/useAIChat.ts, frontend/src/domains/ai/index.ts</files>
  <read_first>frontend/src/routes/_protected.tsx, frontend/src/components/ai/ChatDock.tsx, frontend/src/domains/ai/index.ts, frontend/src/domains/ai/hooks/useAIChat.ts</read_first>
  <action>
    Remove the ChatDock assistant surface from the frontend (D2 — copilot is the sole assistant).

    1. `_protected.tsx`: delete the ChatDock import (line 5), the `<ChatDock onCitationClick={handleCitationClick} />` mount (line 103), and the now-orphaned `handleCitationClick` handler (line 79). After removing the handler, drop any imports that existed ONLY to support it (e.g. a router/navigate helper used solely inside it) — verify each removed line traces to ChatDock and nothing else still uses it. Do NOT touch the copilot FAB/drawer wiring (the P72/P73 copilot mount stays).
    2. Delete `frontend/src/components/ai/ChatDock.tsx` entirely.
    3. Delete `frontend/src/hooks/useAIChat.ts` (the 6-line deprecated shim — it only re-exported the domains hook, which ChatDock consumed).
    4. Delete `frontend/src/domains/ai/hooks/useAIChat.ts` (the real 203-line hook that POSTs `/api/ai/chat`).
    5. `frontend/src/domains/ai/index.ts`: remove ONLY lines 9-10 (`export { useAIChat }` and `export type { ChatMessage, UseAIChatReturn } from './hooks/useAIChat'`). KEEP the `useAIFieldAssist`, `useGenerateBrief`, `aiRepo`, and `types` exports — they are live and unrelated.
    6. After edits, grep the whole `frontend/src` tree (excluding `.understand-anything/`) to confirm ZERO remaining references to `useAIChat` or `ChatDock`. If any consumer surfaces (e.g. a test or story), remove that reference too (it would be dead). `ChatMessage`/`UseAIChatReturn` type consumers, if any remain outside the deleted files, must also be cleaned.

  </action>
  <verify>
    <automated>cd frontend && { grep -rn "ChatDock\|useAIChat" src --include="*.ts" --include="*.tsx" | grep -v ".understand-anything"; } ; test -z "$(grep -rn 'ChatDock\|useAIChat' src --include='*.ts' --include='*.tsx' | grep -v '.understand-anything')" && pnpm type-check 2>&1 | tail -5</automated>
  </verify>
  <done>`<ChatDock>` is not mounted in `_protected.tsx`, `ChatDock.tsx` + both `useAIChat.ts` files are deleted, the `domains/ai` barrel no longer exports `useAIChat` (but still exports field-assist + brief), `grep -rn 'ChatDock\|useAIChat' frontend/src` (excluding `.understand-anything`) returns nothing, and `pnpm --filter intake-frontend type-check` is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the /api/ai/chat backend route (keep chat-assistant agent)</name>
  <files>backend/src/api/ai.ts, backend/src/api/ai/chat.ts</files>
  <read_first>backend/src/api/ai.ts, backend/src/api/ai/chat.ts</read_first>
  <action>
    Retire the `POST /api/ai/chat` route (D2). This is the HTTP entry that drove AnythingLLM via `chatAssistantAgent` → `llmRouter`.

    1. `backend/src/api/ai.ts`: remove line 12 `import chatRouter from './ai/chat.js'` and line 112 `router.use('/chat', chatRouter)`. Leave all other sub-router mounts (briefs, intake-linking, dossier-field-assist, health, etc.) untouched.
    2. Delete `backend/src/api/ai/chat.ts` (the router file — `POST /`, `GET /history`, `GET /:runId/messages`).
    3. **Do NOT** delete or modify `backend/src/ai/agents/chat-assistant.ts` — `chatAssistantAgent` is still imported by `backend/src/ai/agents/brief-generator.ts`. Its AnythingLLM dependency is removed centrally when the backend llm-router provider is severed in Plan 74-04. (Note this explicitly in the SUMMARY so 74-04 picks it up.)
    4. Grep `backend/src` to confirm nothing else imports `./ai/chat.js` or references the deleted `chatRouter`. If `chat.ts` exported any type consumed elsewhere, relocate or remove that consumer (it would be dead).
    5. Confirm `backend/src/ai/agents/chat-assistant.ts` still compiles and `chatAssistantAgent` is still exported (brief-generator depends on it).

  </action>
  <verify>
    <automated>cd backend && test ! -f src/api/ai/chat.ts && test -z "$(grep -rn "ai/chat.js\|chatRouter" src)" && grep -q "chatAssistantAgent" src/ai/agents/chat-assistant.ts && pnpm type-check 2>&1 | tail -5</automated>
  </verify>
  <done>`backend/src/api/ai/chat.ts` is deleted, `backend/src/api/ai.ts` no longer imports or mounts the chat router, `grep -rn 'ai/chat.js\|chatRouter' backend/src` returns nothing, `chatAssistantAgent` is still exported by `chat-assistant.ts`, and `pnpm --filter intake-backend type-check` is clean.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                                                                                                            |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| browser → /api/ai/chat             | The retired route ran under the caller JWT but routed to AnythingLLM (off-prem-capable). Removing it shrinks the assistant attack surface to the on-prem copilot only. |
| copilot drawer → /api/copilot/chat | The remaining assistant path — on-prem, RLS-enforcing, caller-JWT keystone (P72/P73). Untouched by this plan.                                                          |

## STRIDE Threat Register

| Threat ID  | Category               | Component                              | Disposition | Mitigation Plan                                                                                                                                                                                                         |
| ---------- | ---------------------- | -------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-74-02-01 | Tampering              | chat-assistant agent (shared)          | mitigate    | Do NOT delete `chat-assistant.ts` — brief-generator.ts imports it; deleting it would break briefs. Only the HTTP route + frontend surface are removed; the agent stays until its provider is severed centrally (74-04). |
| T-74-02-02 | Information disclosure | orphaned imports after handler removal | mitigate    | After removing `handleCitationClick` + ChatDock, grep confirms zero residual `ChatDock`/`useAIChat` references so no dead import leaks a code path.                                                                     |
| T-74-02-03 | Elevation of privilege | keystone preservation                  | accept      | This plan removes a caller-JWT route and a frontend hook; it introduces NO service-role usage and does not touch the copilot keystone (`createUserClient`/RequestContext). RLS posture unchanged.                       |
| T-74-02-04 | Denial of service      | copilot remains sole assistant         | accept      | The copilot drawer (P72/P73, deploy-gated to run live) is the only assistant after removal; no AnythingLLM dependency remains on this surface.                                                                          |

</threat_model>

<verification>
- `grep -rn 'ChatDock\|useAIChat' frontend/src` (excluding `.understand-anything`) returns nothing.
- `test ! -f backend/src/api/ai/chat.ts` and `grep -rn 'ai/chat.js\|chatRouter' backend/src` returns nothing.
- `grep -q chatAssistantAgent backend/src/ai/agents/chat-assistant.ts` succeeds (agent intentionally retained).
- `pnpm --filter intake-frontend type-check` and `pnpm --filter intake-backend type-check` are clean.
- The copilot FAB/drawer wiring in `_protected.tsx` is untouched.
</verification>

<success_criteria>

- ChatDock is un-mounted and deleted; the copilot drawer is the sole assistant entry point (D2).
- `POST /api/ai/chat` no longer exists (route unmounted, `chat.ts` deleted).
- No orphaned `useAIChat`/`ChatDock` references; type-check green on both packages.
- `chatAssistantAgent` retained for brief-generator; its AnythingLLM provider removal is handed to 74-04.
  </success_criteria>

<output>
Create `.planning/phases/74-eval-gate-anythingllm-retirement/74-02-SUMMARY.md` when done. Note explicitly that `chat-assistant.ts` was retained for brief-generator.ts and its AnythingLLM provider must be severed in 74-04.
</output>
