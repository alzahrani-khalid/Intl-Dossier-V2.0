---
phase: 74-eval-gate-anythingllm-retirement
plan: 07
type: execute
wave: 3
depends_on: [74-04, 74-05]
files_modified:
  - supabase/functions/embeddings-generate/index.ts
  - supabase/functions/intake-tickets-duplicates/index.ts
  - supabase/functions/positions-consistency-check/index.ts
  - supabase/functions/briefing-packs-generate/index.ts
  - supabase/functions/_shared/briefing-pack-generator.ts
  - supabase/functions/dossiers-briefs-generate/index.ts
  - supabase/functions/intake-ai-health/index.ts
  - backend/src/services/brief.service.ts
  - backend/src/api/ai/dossier-field-assist.ts
autonomous: true
requirements: [EVAL-04]
must_haves:
  truths:
    - 'Embedding-producing edge fns (`embeddings-generate`, `intake-tickets-duplicates`, `positions-consistency-check`) source vectors from on-prem TEI BGE-M3 (1024-dim), not AnythingLLM (D3)'
    - "The brief-pack translation + legacy brief generators no longer call AnythingLLM; `dossiers-briefs-generate`'s retired AnythingLLM branch is gated off"
    - 'The backend `BriefService` AnythingLLM generator and the backend `dossier-field-assist` AnythingLLM call are removed; `briefContextService` (used by brief-generator) is preserved'
    - '`intake-ai-health` reports on-prem service health, not AnythingLLM'
    - 'Completes the embedding + brief + backend-twin rip-out per D3; the legacy `anythingllm_*` DB columns are retained as nulled legacy fields (data-only, not a gap)'
  artifacts:
    - path: 'supabase/functions/embeddings-generate/index.ts'
      provides: 'Embedding microservice on TEI BGE-M3 (AnythingLLM fallback removed)'
      contains: 'TEI_EMBED_URL'
    - path: 'backend/src/services/brief.service.ts'
      provides: 'Brief context service retained; AnythingLLM generator removed/gated'
      contains: 'briefContextService'
  key_links:
    - from: 'supabase/functions/embeddings-generate/index.ts'
      to: 'TEI /embed (BGE-M3)'
      via: '1024-dim embed, AnythingLLM fallback removed'
      pattern: 'TEI_EMBED_URL|embed'
    - from: 'backend/src/api/ai/dossier-field-assist.ts'
      to: 'on-prem llm-router OR fallback'
      via: 'REMOVED callAnythingLLM'
      pattern: 'anythingllm'
---

<objective>
Complete the FULL AnythingLLM rip-out (D3) for the embedding, brief-pack, and backend brief/field-assist surfaces: re-point the three embedding-producing edge fns to on-prem TEI BGE-M3, sever AnythingLLM from the brief-pack translator + the already-retired `dossiers-briefs-generate`, remove the backend `BriefService` AnythingLLM generator + the backend `dossier-field-assist` AnythingLLM call (keeping `briefContextService` for brief-generator), and re-point `intake-ai-health` to report on-prem health.

The legacy `anythingllm_*` columns (`intelligence_reports.anythingllm_workspace_id/query/response_metadata`) and their Zod schema in `_shared/validation-schemas.ts` + reads in `intelligence-get` are DATA-ONLY (nulled by 74-05); they stay as legacy/optional fields and are NOT in scope to delete (renaming/dropping would risk inserts). This is an intentional carve-out, not an unplanned gap.

Depends on 74-04 (backend AI-core AnythingLLM removal — this plan finishes the two backend files it deferred) and 74-05 (the shared on-prem helper). File-disjoint from 74-06.

Purpose: Remove AnythingLLM from all remaining embedding + brief + backend-twin surfaces (EVAL-04). Output: TEI-backed embeddings, on-prem/gated brief paths, AnythingLLM-free backend brief/field-assist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-eval-gate-anythingllm-retirement/74-CONTEXT.md
@.planning/phases/74-eval-gate-anythingllm-retirement/74-RESEARCH.md

<read_first>

- agent-runtime/src/mastra/tools/hybrid-rag-search.ts 42-63 — TEI `/embed` pattern (POST `{ inputs }` → `number[][]` → `[0]`, assert 1024). Same pattern 74-03 used.
- supabase/functions/\_shared/onprem-llm.ts (74-05/74-06) — `generateStructuredJson` + `generateText` for on-prem chat (`${VLLM_BASE_URL}/v1/chat/completions`).
- supabase/functions/embeddings-generate/index.ts (573) — OpenAI-first (`generateOpenAIEmbedding`, 54) with an `generateAnythingLLMEmbedding` fallback (103, `provider: 'openai'|'anythingllm'|'fallback'` at 36). Replace the AnythingLLM fallback with a TEI BGE-M3 path (1024-dim); update the `provider` union to `'openai'|'tei'|'fallback'`. Keep OpenAI-first if that's the configured primary, but the SELF-HOSTED fallback becomes TEI (not AnythingLLM). NOTE the OpenAI path returns 1536-dim — keep that as-is for OpenAI; TEI returns 1024. The consuming tables vary; do not force-merge dims, just replace the AnythingLLM branch.
- supabase/functions/intake-tickets-duplicates/index.ts (227) — AnythingLLM `/api/embed` at 111 to generate a query embedding (cached in `ai_embeddings`). Replace with TEI `/embed` (1024-dim). Keep the cache-hit path (91-92) and the no-embedding fallback.
- supabase/functions/positions-consistency-check/index.ts (989) — AnythingLLM at 328 guarded by `includeRecommendations` — this is a CHAT generation (recommendations), not an embedding. Re-home to `generateText`/`generateStructuredJson`. The vector-similarity path (279) uses its own embeddings — check if it also calls AnythingLLM and re-point to TEI if so. Keep the text-similarity fallback (310).
- supabase/functions/briefing-packs-generate/index.ts (279) + supabase/functions/\_shared/briefing-pack-generator.ts (422) — `translateText` (generator 40, packs-generate 42) calls AnythingLLM `/api/v1/workspace/translate` (56). Re-home `translateText` to `generateText`. Both files reference it; fix the shared helper and confirm packs-generate uses the fixed version.
- supabase/functions/dossiers-briefs-generate/index.ts (396) — lines 12-20 read `ANYTHINGLLM_URL`/`KEY` and branch on them; lines 301-314 already COMMENT that this AnythingLLM generate+persist branch is "retired in P74". GATE it: remove the AnythingLLM env reads + the generate+persist branch entirely (return the already-supported response, or a clear 410/"use copilot propose_brief" path). The copilot `propose_brief` (P73) is the supported brief path — this legacy generator should not generate.
- supabase/functions/intake-ai-health/index.ts (240) — probes AnythingLLM embedding+classification model health (20-21, 45-47). Re-point to report TEI (`TEI_EMBED_URL`) + on-prem vLLM (`VLLM_BASE_URL`) health instead; keep the `{ status, services:{ embedding_model, classification_model } }` response shape.
- backend/src/services/brief.service.ts (DEFER-73-01-D) — exports `BriefService` (the AnythingLLM generator: `generateBrief` 74, `callAnythingLLM` 408) AND `briefContextService`/`BriefContext` (imported by `brief-generator.ts:15`). `api/ai.ts:104` does `new BriefService()`. Remove the AnythingLLM generator path (`callAnythingLLM`, the AnythingLLM `generateBrief` body) — either make `generateBrief` throw a clear "brief generation moved to the copilot propose_brief path" error or remove the method and its `api/ai.ts` route usage. KEEP `briefContextService`/`BriefContext` intact. Remove `anythingLLMUrl`/`anythingLLMKey` fields.
- backend/src/api/ai/dossier-field-assist.ts (backend twin) — `callAnythingLLM` (133) at 247 with `generateFallbackFields` (88, 274). Remove `callAnythingLLM`; route generation through the backend on-prem provider (the now-AnythingLLM-free `llmRouter`/private provider from 74-04) OR rely on `generateFallbackFields` if on-prem isn't wired for this path. Keep the fallback.
- backend/src/api/ai.ts:104 — `const briefService = new BriefService()` and any route that calls `briefService.generateBrief`. If `generateBrief` is removed, remove/adjust that route too (it would be dead).
  </read_first>

<interfaces>
<!-- TEI embed: POST {TEI_EMBED_URL}/embed {inputs} -> number[][] -> [0] (1024-dim). -->
<!-- On-prem chat: generateText/generateStructuredJson from _shared/onprem-llm.ts. -->
<!-- backend on-prem: llmRouter (AnythingLLM-free after 74-04) + getPrivateProvider() (vLLM/Ollama). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point the three embedding-producing edge fns to TEI BGE-M3</name>
  <files>supabase/functions/embeddings-generate/index.ts, supabase/functions/intake-tickets-duplicates/index.ts, supabase/functions/positions-consistency-check/index.ts</files>
  <read_first>agent-runtime/src/mastra/tools/hybrid-rag-search.ts, supabase/functions/_shared/onprem-llm.ts, supabase/functions/embeddings-generate/index.ts, supabase/functions/intake-tickets-duplicates/index.ts, supabase/functions/positions-consistency-check/index.ts</read_first>
  <action>
    Replace the AnythingLLM embedding/recommendation paths with on-prem TEI / vLLM (D3), preserving fallbacks.

    1. `embeddings-generate/index.ts`: replace `generateAnythingLLMEmbedding` with a TEI BGE-M3 path (`${TEI_EMBED_URL}/embed`, `{ inputs }`, `number[][]`, `[0]`, 1024-dim). Change the `provider` union from `'openai'|'anythingllm'|'fallback'` to `'openai'|'tei'|'fallback'` and update all `provider:` assignments. Keep OpenAI as the configured primary (1536-dim, unchanged); the self-hosted FALLBACK is now TEI. Remove AnythingLLM env/consts.
    2. `intake-tickets-duplicates/index.ts`: replace the AnythingLLM `/api/embed` call (111) with TEI `/embed` (1024-dim). Keep the `ai_embeddings` cache-hit path and the "no embedding → skip/empty" fallback. Remove AnythingLLM env.
    3. `positions-consistency-check/index.ts`: re-home the `includeRecommendations` AnythingLLM chat (328) to `generateText`/`generateStructuredJson` (recommendations text). If the vector-similarity path (279) also calls AnythingLLM for an embedding, re-point that to TEI `/embed`. Keep the text-similarity fallback (310). Remove AnythingLLM env.
    4. All three: `grep -in anythingllm` returns nothing.

  </action>
  <verify>
    <automated>cd supabase/functions && test -z "$(grep -rin anythingllm embeddings-generate/index.ts intake-tickets-duplicates/index.ts positions-consistency-check/index.ts)" && grep -lq "TEI_EMBED_URL" embeddings-generate/index.ts intake-tickets-duplicates/index.ts</automated>
  </verify>
  <done>All three fns source embeddings from TEI BGE-M3 (and positions recommendations from on-prem vLLM), keep their fallbacks, and `grep -in anythingllm` on the three files returns nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Sever AnythingLLM from the brief paths + backend brief/field-assist + intake-ai-health</name>
  <files>supabase/functions/briefing-packs-generate/index.ts, supabase/functions/_shared/briefing-pack-generator.ts, supabase/functions/dossiers-briefs-generate/index.ts, supabase/functions/intake-ai-health/index.ts, backend/src/services/brief.service.ts, backend/src/api/ai/dossier-field-assist.ts</files>
  <read_first>supabase/functions/_shared/onprem-llm.ts, supabase/functions/_shared/briefing-pack-generator.ts, supabase/functions/dossiers-briefs-generate/index.ts, supabase/functions/intake-ai-health/index.ts, backend/src/services/brief.service.ts, backend/src/api/ai/dossier-field-assist.ts, backend/src/api/ai.ts</read_first>
  <action>
    Remove AnythingLLM from the brief translator, the retired brief generator, the backend brief service + field-assist twin, and the AI health probe (D3).

    1. `_shared/briefing-pack-generator.ts` + `briefing-packs-generate/index.ts`: re-home `translateText` to `generateText` (on-prem EN<->AR translation). Fix the shared helper; confirm packs-generate uses it. Remove AnythingLLM env/URL in both.
    2. `dossiers-briefs-generate/index.ts`: GATE the retired AnythingLLM branch — remove the `ANYTHINGLLM_URL`/`KEY` reads (12-13) and the generate+persist branch (already commented "retired in P74", 301-314). The fn should NOT generate via AnythingLLM; return a clear response directing to the copilot `propose_brief` path (or a 410). Keep any non-generative behavior the fn still legitimately serves.
    3. `intake-ai-health/index.ts`: re-point the health probe from AnythingLLM (45-47) to TEI (`TEI_EMBED_URL`) + on-prem vLLM (`VLLM_BASE_URL`); keep the `{ status, services:{ embedding_model, classification_model }, fallback_active }` response shape (map embedding_model→TEI reachable, classification_model→vLLM reachable). Remove AnythingLLM env.
    4. `backend/src/services/brief.service.ts`: remove the AnythingLLM generator — `callAnythingLLM` (408), the AnythingLLM `generateBrief` body (74), and the `anythingLLMUrl`/`anythingLLMKey` fields (61-68). Either make `generateBrief` throw "brief generation moved to the copilot propose_brief path (P73)" or remove the method. **KEEP `briefContextService` + `BriefContext`** (brief-generator.ts:15 imports them). If `generateBrief` is removed, update `backend/src/api/ai.ts:104` + its route accordingly (remove the dead route).
    5. `backend/src/api/ai/dossier-field-assist.ts`: remove `callAnythingLLM` (133) + its use (247); route generation through the AnythingLLM-free backend `llmRouter`/private provider (from 74-04) OR fall through to `generateFallbackFields` (88) if on-prem chat isn't wired here. Keep the fallback. Remove AnythingLLM env.
    6. All six files: `grep -in anythingllm` returns nothing. `briefContextService` still exported.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && test -z "$(grep -rin anythingllm supabase/functions/briefing-packs-generate/index.ts supabase/functions/_shared/briefing-pack-generator.ts supabase/functions/dossiers-briefs-generate/index.ts supabase/functions/intake-ai-health/index.ts backend/src/services/brief.service.ts backend/src/api/ai/dossier-field-assist.ts)" && grep -q "briefContextService" backend/src/services/brief.service.ts && cd backend && pnpm type-check 2>&1 | tail -5</automated>
  </verify>
  <done>The brief translator + retired generator + backend brief/field-assist + health probe are AnythingLLM-free, `briefContextService` is preserved (brief-generator compiles), backend type-check is clean, and `grep -in anythingllm` on the six files returns nothing.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                                                                  |
| ---------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| edge fn → TEI / vLLM         | Embeddings via on-prem TEI; brief/recommendation generation via on-prem vLLM. AnythingLLM removed. Zero-egress.              |
| backend BriefService → model | The AnythingLLM brief generator is removed; brief generation is the copilot `propose_brief` path (P73, on-prem, caller-JWT). |
| data-only legacy columns     | `anythingllm_*` columns persist as nulled legacy fields; reads/schemas tolerate them.                                        |

## STRIDE Threat Register

| Threat ID  | Category               | Component                  | Disposition | Mitigation Plan                                                                                                                                                                                |
| ---------- | ---------------------- | -------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-74-07-01 | Tampering              | embedding dimension        | mitigate    | TEI path asserts 1024-dim; OpenAI 1536 kept distinct (no force-merge that would corrupt a vector store).                                                                                       |
| T-74-07-02 | Elevation of privilege | BriefService keystone      | mitigate    | Removing the AnythingLLM generator keeps brief generation on the copilot `propose_brief` caller-JWT path (RLS-gated); no service-role introduced. `briefContextService` (read-side) unchanged. |
| T-74-07-03 | Information disclosure | zero-egress                | mitigate    | All re-pointed paths target on-prem TEI/vLLM only; no cloud/AnythingLLM endpoint remains on these surfaces.                                                                                    |
| T-74-07-04 | Denial of service      | model/embedder unavailable | mitigate    | Each fn keeps a fallback (text-similarity, fallback fields, cache-hit, "use copilot"); a TEI/vLLM outage degrades rather than 500s.                                                            |
| T-74-07-05 | Repudiation            | retired brief generator    | accept      | `dossiers-briefs-generate` is gated to not generate; the supported audited path is the copilot propose+persist (P73). Legacy columns untouched.                                                |

</threat_model>

<verification>
- `grep -rin anythingllm` returns nothing across all nine edge/backend files in this plan.
- Embedding fns use `TEI_EMBED_URL` (1024-dim); brief/recommendation generation uses the on-prem helper/llmRouter.
- `briefContextService` still exported from `brief.service.ts`; `pnpm --filter intake-backend type-check` clean.
- `dossiers-briefs-generate` does NOT generate via AnythingLLM (gated to the copilot path).
- Legacy `anythingllm_*` columns + their Zod schema/reads remain (data-only, intentional) — NOT flagged as a gap.
</verification>

<success_criteria>

- Embedding + brief + backend-twin surfaces are fully AnythingLLM-free (D3); on-prem TEI/vLLM throughout.
- `briefContextService` preserved; fallbacks preserved; zero-egress; no new service-role.
  </success_criteria>

<output>
Create `.planning/phases/74-eval-gate-anythingllm-retirement/74-07-SUMMARY.md` when done. Note the data-only `anythingllm_*` legacy columns (validation-schemas + intelligence-get) are intentionally retained.
</output>
