---
phase: 55-designv2-main-merge-gate-enforcement
plan: 02
type: execute
wave: 2
depends_on:
  - 55-01
files_modified:
  - .github/workflows/ci.yml
  - .planning/phases/55-designv2-main-merge-gate-enforcement/55-02-SUMMARY.md
  - .planning/phases/55-designv2-main-merge-gate-enforcement/55-CI-JOBS-PR-EVIDENCE.json
autonomous: false
requirements:
  - MERGE-02
must_haves:
  truths:
    - 'Two new CI jobs `design-token-check` and `i18next-factory-check` exist in `.github/workflows/ci.yml` on origin/main with `name:` values matching D-13 verbatim (`Design Token Check` and `react-i18next Factory Check`)'
    - "Each new job uses the positive-failure pattern `! pnpm exec eslint -c eslint.config.mjs --max-warnings 0 <fixture>` (bash `!` inverts; `--max-warnings 0` upgrades the design-token fixture's `warn` severity to job failure)"
    - 'Both new jobs run green on the post-merge main HEAD (verified via `gh api .../actions/runs/<id>/jobs`)'
    - 'The PR adding these jobs passed all 6 existing required contexts before merge (no protection bypass)'
    - 'Both fixtures still fail standalone locally with `--max-warnings 0` (positive-failure proven)'
  artifacts:
    - path: '.github/workflows/ci.yml'
      provides: 'Two new top-level jobs `design-token-check` and `i18next-factory-check` after the existing `lint` job'
      contains: 'name: Design Token Check; name: react-i18next Factory Check'
    - path: '.planning/phases/55-designv2-main-merge-gate-enforcement/55-CI-JOBS-PR-EVIDENCE.json'
      provides: 'Captured `gh pr view --json` + post-merge `gh api .../actions/runs/<id>/jobs` proving the 2 new jobs reported success on main'
      contains: 'Design Token Check, react-i18next Factory Check, conclusion: success'
    - path: '.planning/phases/55-designv2-main-merge-gate-enforcement/55-02-SUMMARY.md'
      provides: 'Plan 02 closure record'
  key_links:
    - from: '.github/workflows/ci.yml `jobs.design-token-check.name`'
      to: 'branch protection `required_status_checks.contexts[]` (added in Plan 03)'
      via: 'exact string match'
      pattern: "jobs:\\n\\s+design-token-check:\\n\\s+name: Design Token Check"
    - from: '.github/workflows/ci.yml `jobs.i18next-factory-check.name`'
      to: 'branch protection `required_status_checks.contexts[]` (added in Plan 03)'
      via: 'exact string match'
      pattern: "jobs:\\n\\s+i18next-factory-check:\\n\\s+name: react-i18next Factory Check"
---

<objective>
Add 2 new CI jobs to `.github/workflows/ci.yml` on `main` via a separate PR (per D-15 — must not be bundled with the merge PR). Each job runs a positive-failure assertion against an existing fixture in `tools/eslint-fixtures/`. These jobs will be added to required-contexts on `main` branch protection in Plan 03.

This plan addresses MERGE-02 (preparation half). It DOES NOT touch branch protection — that's Plan 03 by D-15 sequencing (Pitfall 4: adding required contexts before jobs exist on main bricks all PRs).

Purpose: Land the CI contract for the 2 new gate contexts BEFORE protection is updated to require them. This is the hard-sequencing constraint from D-15 / RESEARCH Pitfall 4.

Output: Two new jobs on origin/main's `.github/workflows/ci.yml`; both reported success on main's most recent workflow run; PR merged green through the existing 6-context gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/55-designv2-main-merge-gate-enforcement/55-CONTEXT.md
@.planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md
@.planning/phases/55-designv2-main-merge-gate-enforcement/55-VALIDATION.md
@.planning/phases/55-designv2-main-merge-gate-enforcement/55-01-SUMMARY.md
@.github/workflows/ci.yml
@tools/eslint-fixtures/bad-design-token.tsx
@tools/eslint-fixtures/bad-vi-mock.ts
@eslint.config.mjs

<interfaces>
<!-- The existing CI workflow uses a `repo-policy` job that other jobs depend on -->
<!-- via `needs: [repo-policy]`. The 2 new jobs should follow this convention. -->
<!-- Existing job IDs include: repo-policy, lint, type-check, frontend-tests, -->
<!--                          backend-tests, integration-tests, e2e, rtl-responsive. -->
<!-- The new jobs install their own deps via pnpm/action-setup@v4 + actions/setup-node@v4. -->
<!-- env.NODE_VERSION is set at workflow level (already present in ci.yml). -->

<!-- D-13 LOCKED required-context names (exact case + spacing): -->
<!--   "Design Token Check"      (NOT "design-token-check") -->
<!--   "react-i18next Factory Check"  (NOT "i18next-factory-check") -->
<!-- These must be the value of `jobs.<id>.name:` because that's the string -->
<!-- GitHub reports as the check-run name (Pitfall 3). -->

<!-- Fixture severity confirmed empirically in RESEARCH (Pitfall 7): -->
<!--   bad-design-token.tsx → 3 warnings, 0 errors → exit 0 WITHOUT --max-warnings 0 -->
<!--   bad-vi-mock.ts → 1 error → exit 1 (already non-zero) -->
<!-- BOTH jobs use --max-warnings 0 for symmetric, defensive behavior. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Branch off main and prove fixture positive-failure locally</name>
  <files>
    (no file writes — local proof-of-mechanism)
  </files>
  <read_first>
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Pitfall 7: `bad-design-token.tsx` exits 0 without `--max-warnings 0`"
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Pattern 2: Positive-failure CI assertion"
    - tools/eslint-fixtures/bad-design-token.tsx
    - tools/eslint-fixtures/bad-vi-mock.ts
    - eslint.config.mjs (focus: lines 286-292 — fixture-specific severity override)
  </read_first>
  <action>
    Sync local main to origin/main HEAD (Plan 01 left it current; re-verify: `git checkout main && git pull --ff-only origin main`). Branch off main: `git checkout -b ci/phase-55-add-gate-jobs`. Locally prove the positive-failure mechanism — both `! pnpm exec eslint -c eslint.config.mjs --max-warnings 0 tools/eslint-fixtures/bad-design-token.tsx` and `! pnpm exec eslint -c eslint.config.mjs --max-warnings 0 tools/eslint-fixtures/bad-vi-mock.ts` MUST exit 0 (the `!` inverts eslint's non-zero exit). This proves the CI step will pass exactly when fixtures fail lint. If either exits non-zero, the fixture is no longer producing its expected violation and the gate is silently broken — stop and surface to user.
  </action>
  <verify>
    <automated>git checkout main && git pull --ff-only origin main && git checkout -b ci/phase-55-add-gate-jobs && (! pnpm exec eslint -c eslint.config.mjs --max-warnings 0 tools/eslint-fixtures/bad-design-token.tsx) && (! pnpm exec eslint -c eslint.config.mjs --max-warnings 0 tools/eslint-fixtures/bad-vi-mock.ts) && echo "POSITIVE-FAILURE PROVEN LOCALLY"</automated>
  </verify>
  <acceptance_criteria>
    - On a new branch `ci/phase-55-add-gate-jobs` based on origin/main HEAD
    - `! pnpm exec eslint -c eslint.config.mjs --max-warnings 0 tools/eslint-fixtures/bad-design-token.tsx` exits 0
    - `! pnpm exec eslint -c eslint.config.mjs --max-warnings 0 tools/eslint-fixtures/bad-vi-mock.ts` exits 0
    - No uncommitted changes
  </acceptance_criteria>
  <done>Positive-failure mechanism proven; ready to wire into ci.yml.</done>
</task>

<task type="auto">
  <name>Task 2: Add 2 new jobs to `.github/workflows/ci.yml` and commit</name>
  <files>
    .github/workflows/ci.yml
  </files>
  <read_first>
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Code Examples → Add 2 new CI jobs (plan 2 — `.github/workflows/ci.yml` patch)"
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-CONTEXT.md §"D-13, D-14"
    - .github/workflows/ci.yml (entire file — match indentation style, `needs:` convention, env.NODE_VERSION reference)
  </read_first>
  <action>
    Per D-14, append 2 new top-level jobs to `.github/workflows/ci.yml` AFTER the existing `lint` job block. Job IDs (kebab-case): `design-token-check` and `i18next-factory-check`. Job `name:` values (LITERAL, case- and space-sensitive per Pitfall 3): `Design Token Check` and `react-i18next Factory Check`. Each job: `runs-on: ubuntu-latest`, `needs: [repo-policy]` (matches the `lint` job convention from the existing workflow), steps = checkout + pnpm/action-setup@v4 + actions/setup-node@v4 (with `node-version: ${{ env.NODE_VERSION }}` and `cache: 'pnpm'`) + `pnpm install --frozen-lockfile` + the positive-failure assertion. The positive-failure step MUST start with `set -e` then `! pnpm exec eslint -c eslint.config.mjs --max-warnings 0 <fixture-path>` exactly per RESEARCH Code Examples — DO NOT use `&& exit 1 || exit 0` (masks set -e). Do not modify any existing job. Run `yamllint` or equivalent if available; otherwise sanity-check via `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` or `gh workflow view ci.yml --ref ci/phase-55-add-gate-jobs` after pushing.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q '^  design-token-check:$' .github/workflows/ci.yml && grep -q '    name: Design Token Check$' .github/workflows/ci.yml && grep -q '^  i18next-factory-check:$' .github/workflows/ci.yml && grep -q '    name: react-i18next Factory Check$' .github/workflows/ci.yml && grep -q 'tools/eslint-fixtures/bad-design-token.tsx' .github/workflows/ci.yml && grep -q 'tools/eslint-fixtures/bad-vi-mock.ts' .github/workflows/ci.yml && grep -q -- '--max-warnings 0' .github/workflows/ci.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo "YAML VALID + JOBS WIRED"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` parses as valid YAML
    - Contains `jobs.design-token-check.name: Design Token Check` (literal match)
    - Contains `jobs.i18next-factory-check.name: react-i18next Factory Check` (literal match)
    - Both jobs include the `--max-warnings 0` flag on the eslint invocation
    - Both jobs use the `!` inversion idiom (positive-failure)
    - No existing job in ci.yml was modified (`git diff main -- .github/workflows/ci.yml` shows only additions)
  </acceptance_criteria>
  <done>ci.yml has 2 new jobs wired with positive-failure assertions.</done>
</task>

<task type="auto">
  <name>Task 3: Push branch, open PR, watch CI until 6 existing required contexts pass + the 2 new jobs pass</name>
  <files>
    (PR creation + CI watch — no local file writes)
  </files>
  <read_first>
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Pitfall 4: Adding required contexts BEFORE the jobs exist on main"
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-CONTEXT.md §"D-15 sequencing"
  </read_first>
  <action>
    Commit changes on branch `ci/phase-55-add-gate-jobs` with message `ci(55): add design-token-check + i18next-factory-check jobs (MERGE-02)`. Push: `git push -u origin ci/phase-55-add-gate-jobs`. Open PR: `gh pr create --base main --head ci/phase-55-add-gate-jobs --title "Phase 55 Plan 02: add Design Token Check + react-i18next Factory Check CI jobs (MERGE-02)" --body "Adds 2 new CI jobs per D-13/D-14. Both use positive-failure pattern against existing fixtures in tools/eslint-fixtures/. Sequenced per D-15: jobs land on main BEFORE Plan 03 adds them to branch protection (Pitfall 4: adding required contexts before jobs exist bricks all PRs)."`. Resolve PR number. Wait for ALL 6 existing required contexts to pass AND for the 2 new jobs to report success: `gh pr checks <num> --watch`. The 2 new jobs will not be required-contexts yet (Plan 03 adds that) but they MUST report success here as a precondition for Plan 03.
  </action>
  <verify>
    <automated>git add .github/workflows/ci.yml && git commit -m "ci(55): add design-token-check + i18next-factory-check jobs (MERGE-02)" && git push -u origin ci/phase-55-add-gate-jobs && PR_NUM=$(gh pr create --base main --head ci/phase-55-add-gate-jobs --title "Phase 55 Plan 02: add Design Token Check + react-i18next Factory Check CI jobs (MERGE-02)" --body "Adds 2 new CI jobs per D-13/D-14. Sequenced per D-15." --json number --jq '.number' 2>/dev/null || gh pr list --head ci/phase-55-add-gate-jobs --json number --jq '.[0].number') && gh pr checks "$PR_NUM" --watch && gh pr view "$PR_NUM" --json statusCheckRollup --jq '[.statusCheckRollup[] | select(.name == "Design Token Check" or .name == "react-i18next Factory Check") | .conclusion] | unique' | grep -qx '\["SUCCESS"\]' && echo "BOTH NEW JOBS GREEN ON PR"</automated>
  </verify>
  <acceptance_criteria>
    - PR is open with base=main, head=ci/phase-55-add-gate-jobs
    - All 6 existing required contexts report `SUCCESS` on the PR
    - Both new jobs (`Design Token Check`, `react-i18next Factory Check`) report `SUCCESS`
    - No `failure`/`cancelled`/`pending` state on any required context
  </acceptance_criteria>
  <done>PR is green; ready for user to click Merge.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: PAUSE — user clicks Merge on the CI jobs PR</name>
  <what-built>
    PR `ci/phase-55-add-gate-jobs` is green on all 6 existing required contexts + 2 new jobs (verified Task 3).
  </what-built>
  <how-to-verify>
    Visit https://github.com/alzahrani-khalid/Intl-Dossier-V2.0/pulls and open the Phase 55 Plan 02 PR. Confirm all checks have passed.

    Click "Merge pull request" → "Create a merge commit" → "Confirm merge".

    (Squash is also acceptable here — this PR's purpose is to land 2 new jobs, not preserve historical commits. No signed tags are at stake on this branch. But `--no-ff` is the project default and is fine to use.)

    Reply with the merge commit SHA so Claude can verify post-merge job execution.

    Note: this merge will trigger `deploy.yml` on success of CI on the new main HEAD (same deploy-posture caveat as Plan 01 Task 5). The deploy decision from Plan 01 Task 5 carries forward — if you chose "disable temporarily" in Plan 01, the workflow is already disabled and there's nothing extra to do here.

  </how-to-verify>
  <resume-signal>Reply with the merge commit SHA after clicking Merge.</resume-signal>
  <files>(human gate — no file writes by Claude during this task)</files>
  <action>This is a human-gate checkpoint. Claude pauses here and waits for the resume-signal described below. See <what-built> and <how-to-verify> above for what the human must do.</action>
  <verify>
    <automated>echo "Checkpoint passed — proceed to next task only after human resume-signal received."</automated>
  </verify>
  <done>Human has supplied the resume-signal described above; Claude resumes with the captured value(s).</done>
</task>

<task type="auto">
  <name>Task 5: Verify both new jobs ran green on the post-merge main HEAD and capture evidence</name>
  <files>
    .planning/phases/55-designv2-main-merge-gate-enforcement/55-CI-JOBS-PR-EVIDENCE.json
  </files>
  <read_first>
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Pitfall 4 (belt-and-braces verification)"
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-VALIDATION.md "Both new CI jobs run green on main HEAD post-merge" row
  </read_first>
  <action>
    Sync local main: `git fetch origin && git checkout main && git pull --ff-only origin main`. Capture the closed PR's evidence and the post-merge CI run's job conclusions into a single JSON file. Steps: (a) Resolve PR number from `gh pr list --base main --state closed --head ci/phase-55-add-gate-jobs`. (b) Capture PR view: `gh pr view <num> --json mergeStateStatus,statusCheckRollup,mergeCommit,state` → write to `.planning/phases/55-designv2-main-merge-gate-enforcement/55-CI-JOBS-PR-EVIDENCE.json` keyed by `"pr"`. (c) Get most recent workflow run on main: `gh api 'repos/alzahrani-khalid/Intl-Dossier-V2.0/actions/workflows/ci.yml/runs?branch=main&per_page=1' --jq '.workflow_runs[0].id'`. (d) Fetch its jobs: `gh api repos/alzahrani-khalid/Intl-Dossier-V2.0/actions/runs/<run-id>/jobs` and merge into the JSON file keyed by `"main_run_jobs"`. (e) Assert both new job names appear with `conclusion: "success"` via `jq -e`.
  </action>
  <verify>
    <automated>git fetch origin && git checkout main && git pull --ff-only origin main && PR_NUM=$(gh pr list --base main --state closed --head ci/phase-55-add-gate-jobs --json number --jq '.[0].number') && PR_JSON=$(gh pr view "$PR_NUM" --json mergeStateStatus,statusCheckRollup,mergeCommit,state) && RUN_ID=$(gh api 'repos/alzahrani-khalid/Intl-Dossier-V2.0/actions/workflows/ci.yml/runs?branch=main&per_page=1' --jq '.workflow_runs[0].id') && JOBS_JSON=$(gh api "repos/alzahrani-khalid/Intl-Dossier-V2.0/actions/runs/$RUN_ID/jobs") && jq -n --argjson pr "$PR_JSON" --argjson run_id "$RUN_ID" --argjson jobs "$JOBS_JSON" '{pr: $pr, main_run_id: $run_id, main_run_jobs: $jobs}' > .planning/phases/55-designv2-main-merge-gate-enforcement/55-CI-JOBS-PR-EVIDENCE.json && jq -e '.main_run_jobs.jobs | map(select(.name == "Design Token Check" or .name == "react-i18next Factory Check")) | length == 2 and (map(.conclusion) | unique == ["success"])' .planning/phases/55-designv2-main-merge-gate-enforcement/55-CI-JOBS-PR-EVIDENCE.json && echo "BOTH NEW JOBS GREEN ON MAIN HEAD"</automated>
  </verify>
  <acceptance_criteria>
    - `55-CI-JOBS-PR-EVIDENCE.json` exists with `pr.state == "MERGED"`
    - Both new job names (`Design Token Check`, `react-i18next Factory Check`) appear under `main_run_jobs.jobs[]` with `conclusion == "success"`
    - Latest workflow run on `main` includes both new jobs (not just the PR run)
  </acceptance_criteria>
  <done>Both new jobs verified running green on main HEAD; Plan 03 unblocked.</done>
</task>

<task type="auto">
  <name>Task 6: Write Plan 02 SUMMARY</name>
  <files>
    .planning/phases/55-designv2-main-merge-gate-enforcement/55-02-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-CI-JOBS-PR-EVIDENCE.json
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Create `55-02-SUMMARY.md`. Sections required: (a) Outcome (PASS); (b) PR number + merge SHA; (c) The 2 new job IDs + their `name:` strings verbatim (so Plan 03 can copy them into protection-after.json); (d) Most recent main workflow run ID + the URL to that run; (e) Confirmation that Plan 03 (branch protection update) is now UNBLOCKED — Pitfall 4 satisfied; (f) Note on D-13's reversal of v6.3's D-09 "fold into Lint" decision — these jobs are now separate contexts for failure-attribution clarity, NOT folded.
  </action>
  <verify>
    <automated>test -f .planning/phases/55-designv2-main-merge-gate-enforcement/55-02-SUMMARY.md && grep -q 'Design Token Check' .planning/phases/55-designv2-main-merge-gate-enforcement/55-02-SUMMARY.md && grep -q 'react-i18next Factory Check' .planning/phases/55-designv2-main-merge-gate-enforcement/55-02-SUMMARY.md && grep -q -i 'pitfall 4\|unblocked\|plan 03\|plan-03' .planning/phases/55-designv2-main-merge-gate-enforcement/55-02-SUMMARY.md && echo "SUMMARY COMPLETE"</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY exists at `55-02-SUMMARY.md`
    - Contains the exact strings `Design Token Check` and `react-i18next Factory Check`
    - References Pitfall 4 / Plan 03 unblock-state
    - Notes D-13's reversal of D-09 fold-into-Lint
  </acceptance_criteria>
  <done>Plan 02 SUMMARY committed; Plan 03 ready to start.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                                               |
| ---------------------- | --------------------------------------------------------------------------------------------------------- |
| local → GitHub Actions | Workflow YAML change executes on GitHub-hosted runners; secrets remain in repo-secrets store (untouched). |
| CI runner → public npm | `pnpm install --frozen-lockfile` reads pinned lockfile only; no new packages added by this plan.          |
| eslint → fixture file  | Positive-failure assertion treats fixture as input; fixture lives in repo so no untrusted-input boundary. |

## STRIDE Threat Register

| Threat ID | Category  | Component                                       | Disposition | Mitigation Plan                                                                                                                                                                                              |
| --------- | --------- | ----------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-55-01   | Tampering | branch protection (not touched in this plan)    | accept      | Plan 02 explicitly does NOT modify protection. Plan 03 handles that with the round-trip JSON pattern.                                                                                                        |
| T-55-NJ   | Tampering | new CI job silently degrades to permissive      | mitigate    | `--max-warnings 0` upgrades warnings to job-failure; `!` inversion means the job ONLY passes when fixture fails lint. Task 1 proves this locally; Task 3 proves it on PR run; Task 5 proves it on main HEAD. |
| T-55-CN   | Tampering | check-run name drift from required-context name | mitigate    | Task 2 acceptance asserts literal `name: Design Token Check` + `name: react-i18next Factory Check`. Plan 03 reads these exact strings from this SUMMARY into protection-after.json.                          |
| T-55-SQ   | Tampering | CI YAML mutation breaks existing jobs           | mitigate    | Task 2 acceptance requires `git diff` shows only additions; existing jobs untouched (Karpathy §Surgical Changes).                                                                                            |
| T-55-SC   | Tampering | npm/pip/cargo installs                          | accept      | Only `pnpm install --frozen-lockfile` against pinned lockfile; no new packages added.                                                                                                                        |

</threat_model>

<verification>
Plan 02 closes when:
1. `.github/workflows/ci.yml` on origin/main has both new jobs with exact D-13 `name:` strings.
2. Both new jobs reported `conclusion: "success"` on the post-merge main workflow run.
3. `55-CI-JOBS-PR-EVIDENCE.json` committed with `main_run_jobs` containing both job names with `success`.
4. The PR was merged through the existing 6-context gate (no protection bypass).
5. `55-02-SUMMARY.md` committed.
</verification>

<success_criteria>
ROADMAP Phase 55 criterion #2 (post-merge main HEAD CI green) FULLY SATISFIED for the existing 6 contexts AND the 2 new jobs. Plan 03 is now safe to run (Pitfall 4 belt-and-braces satisfied).
</success_criteria>

<output>
Create `.planning/phases/55-designv2-main-merge-gate-enforcement/55-02-SUMMARY.md`.
</output>
