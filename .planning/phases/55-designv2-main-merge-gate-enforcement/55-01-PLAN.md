---
phase: 55-designv2-main-merge-gate-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/55-designv2-main-merge-gate-enforcement/55-01-SUMMARY.md
  - .planning/phases/55-designv2-main-merge-gate-enforcement/55-MERGE-PR-EVIDENCE.json
autonomous: false
requirements:
  - MERGE-01
user_setup:
  - service: github
    why: 'Merge PR via UI (D-02) — exercises enforce_admins=true; production deploy posture decision'
    env_vars: []
    dashboard_config:
      - task: 'Click Merge pull request → Create a merge commit on the merge PR'
        location: 'https://github.com/alzahrani-khalid/Intl-Dossier-V2.0/pulls'
      - task: 'Decide production deploy posture for merge moment (open question from RESEARCH)'
        location: 'User decision — see Task 4 pause-and-ask'
must_haves:
  truths:
    - 'DesignV2 history is reachable from main via a --no-ff merge commit (no manual cherry-picks)'
    - 'All 6 v6.3 required contexts (type-check, Security Scan, Lint, Bundle Size Check (size-limit), Tests (frontend), Tests (backend)) reported SUCCESS on the merge PR before Merge was clicked'
    - 'All 4 prior SSH-signed phase-base tags (phase-47-base, phase-48-base, phase-49-base, phase-54-base) still pass `git tag -v` and are reachable from origin/main after merge'
    - 'Remote DesignV2 branch is deleted; phase-54-base preserves the historic DesignV2 tip'
    - 'phase-55-base annotated + SSH-signed tag is issued on the merge commit and `git tag -v phase-55-base` exits 0'
    - 'User explicitly chose a production-deploy posture (accept / disable / time-window) before clicking Merge'
  artifacts:
    - path: '.planning/phases/55-designv2-main-merge-gate-enforcement/55-MERGE-PR-EVIDENCE.json'
      provides: 'Captured output of `gh pr view <merge-pr-num> --json mergeStateStatus,statusCheckRollup,mergeCommit,number` immediately after merge'
      contains: 'mergeStateStatus, statusCheckRollup, mergeCommit.oid'
    - path: '.planning/phases/55-designv2-main-merge-gate-enforcement/55-01-SUMMARY.md'
      provides: 'Plan 1 closure record including merge SHA, deploy decision, tag verification output, rollback procedure'
  key_links:
    - from: 'merge commit on origin/main'
      to: 'phase-54-base tag SHA'
      via: 'git merge-base --is-ancestor'
      pattern: "git merge-base --is-ancestor \\$(git rev-parse phase-54-base) origin/main"
    - from: 'phase-55-base tag'
      to: 'origin/main HEAD (merge commit)'
      via: 'git tag -s -a + git push origin phase-55-base'
      pattern: 'git tag -v phase-55-base'
---

<objective>
Land the DesignV2 branch onto `main` via a `--no-ff` merge (D-01) with all 6 v6.3 required CI contexts green (D-07), preserve all 4 SSH-signed phase-base tags (D-01 rationale), delete the remote DesignV2 branch post-merge (D-04), and issue a signed `phase-55-base` tag on the merge commit.

This plan addresses MERGE-01. It is the foundation for Plans 02, 03, and 04 — none of those can proceed until DesignV2 history is on main and CI on main has the DesignV2-shape ci.yml.

Purpose: Cleanly transfer 113 unpushed DesignV2 commits onto main with provenance intact, exercising the existing branch-protection contract via the documented user-flow (Merge button in UI).

Output: Merge commit on origin/main, signed phase-55-base tag pushed, evidence JSON committed, remote DesignV2 deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-designv2-main-merge-gate-enforcement/55-CONTEXT.md
@.planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md
@.planning/phases/55-designv2-main-merge-gate-enforcement/55-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Branch-protection contract on main as of research (verified empirically): -->
<!--   Required contexts (6): "type-check", "Security Scan", "Lint", -->
<!--                          "Bundle Size Check (size-limit)", -->
<!--                          "Tests (frontend)", "Tests (backend)" -->
<!--   enforce_admins: true · allow_force_pushes: false · allow_deletions: false -->
<!-- Branch state (verified): origin/DesignV2..DesignV2 = 113 ahead, 0 behind -->
<!-- Local main is 802 commits BEHIND origin/main — MUST fetch before reasoning. -->
<!-- SSH-signed phase-base tags to preserve: phase-47-base, phase-48-base, -->
<!--                                          phase-49-base, phase-54-base -->
<!-- Owner github account: alzahrani-khalid · repo: Intl-Dossier-V2.0 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync local main and verify phase-base tag provenance before merge</name>
  <files>
    (no file writes — read-only verification)
  </files>
  <read_first>
    - CLAUDE.md §"Tag signing setup" (lines describing `git tag -v` expectations)
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Pitfall 8: Local `main` is 802 commits stale"
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Code Examples → Verify prior signed tags survived the merge"
  </read_first>
  <action>
    First task per RESEARCH Pitfall 8 — local main is stale (802 behind) so all reasoning starts here. Run `git fetch origin --tags --prune` to refresh refs. Verify the 4 SSH-signed phase-base tags currently pass verification BEFORE merge so any post-merge regression is attributable to the merge. Run `git tag -v phase-47-base`, `git tag -v phase-48-base`, `git tag -v phase-49-base`, `git tag -v phase-54-base` — each MUST exit 0 with "Good \"git\" signature". Then `git checkout main && git pull --ff-only origin main` to sync local main (do NOT skip — Pitfall 8). Confirm DesignV2 strict-ancestor relationship: `git rev-list --count origin/DesignV2..DesignV2` must be > 0 and `git rev-list --count DesignV2..origin/DesignV2` must equal 0. Record the actual ahead-count (research shows 113, CONTEXT.md said 110 — use live count per Pitfall 9). Do not push or merge in this task.
  </action>
  <verify>
    <automated>git fetch origin --tags --prune && for t in phase-47-base phase-48-base phase-49-base phase-54-base; do git tag -v "$t" 2>&1 | grep -q 'Good "git" signature' || { echo "FAIL: $t"; exit 1; }; done && git checkout main && git pull --ff-only origin main && AHEAD=$(git rev-list --count origin/DesignV2..DesignV2) && BEHIND=$(git rev-list --count DesignV2..origin/DesignV2) && test "$AHEAD" -gt 0 && test "$BEHIND" -eq 0 && echo "PREFLIGHT OK: $AHEAD ahead, 0 behind"</automated>
  </verify>
  <acceptance_criteria>
    - `git fetch origin --tags --prune` exits 0
    - All 4 phase-base tags emit "Good \"git\" signature" on `git tag -v`
    - Local `main` advanced via `git pull --ff-only` (no merge commits created locally)
    - DesignV2 is strict-ancestor of origin/DesignV2 (ahead > 0, behind = 0)
    - The actual ahead-count is recorded (will be cited in SUMMARY)
  </acceptance_criteria>
  <done>Pre-merge preflight passes; local main current; 4 phase-base tags verified.</done>
</task>

<task type="auto">
  <name>Task 2: Run full local gate parity on DesignV2 tip (D-05)</name>
  <files>
    (no file writes — local CI parity check)
  </files>
  <read_first>
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Code Examples → Pre-merge gate parity (D-05)"
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-CONTEXT.md §"D-05"
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-VALIDATION.md §"Test Infrastructure → Full suite command"
  </read_first>
  <action>
    Checkout DesignV2 (`git checkout DesignV2`). Execute the D-05 full-parity sequence locally to mirror 4 of 6 required CI contexts (type-check, Lint, Bundle Size Check, Tests). Run in this order: `pnpm install --frozen-lockfile`, then `pnpm --filter intake-frontend type-check`, `pnpm --filter intake-backend type-check`, `pnpm run lint`, `pnpm -C frontend size-limit`, `pnpm --filter intake-frontend test --run`, `pnpm --filter intake-backend test --run`. All must exit 0. If any fails, STOP and report to user — do not push. Per D-08 the fix path is to commit fixes to DesignV2 directly; budget 3 attempts before escalating per RESEARCH Open Question #4. Do not push in this task.
  </action>
  <verify>
    <automated>git checkout DesignV2 && pnpm install --frozen-lockfile && pnpm --filter intake-frontend type-check && pnpm --filter intake-backend type-check && pnpm run lint && pnpm -C frontend size-limit && pnpm --filter intake-frontend test --run && pnpm --filter intake-backend test --run</automated>
  </verify>
  <acceptance_criteria>
    - On DesignV2 branch
    - All 7 commands above exit 0
    - No uncommitted changes in working tree (`git status --porcelain` empty)
  </acceptance_criteria>
  <done>Local parity matches expected CI green; DesignV2 ready to push.</done>
</task>

<task type="auto">
  <name>Task 3: Push DesignV2 and open the merge PR</name>
  <files>
    (push branch + open PR; no local file writes)
  </files>
  <read_first>
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Code Examples → Push (D-06)"
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Code Examples → Open merge PR (D-02, D-07)"
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-CONTEXT.md §"D-06, D-02"
  </read_first>
  <action>
    Per D-06, plain `git push origin DesignV2` (no `--force-with-lease`; remote is strict ancestor). Per D-02, open the merge PR via `gh pr create --base main --head DesignV2`. Title (Claude discretion): "Phase 55: merge DesignV2 → main (v6.4 gate enforcement)". Body must enumerate the 6 required contexts (per D-07) and explicitly state the merge strategy is `--no-ff` (D-01) to preserve signed phase-base tags. Capture the PR number into the SUMMARY draft. Do NOT click Merge in this task.
  </action>
  <verify>
    <automated>git push origin DesignV2 && PR_NUM=$(gh pr create --base main --head DesignV2 --title "Phase 55: merge DesignV2 → main (v6.4 gate enforcement)" --body "Lands DesignV2 onto main (MERGE-01). Merge strategy: --no-ff (D-01) preserves SSH-signed annotated tags phase-47-base, phase-48-base, phase-49-base, phase-54-base. Required contexts (6): type-check, Security Scan, Lint, Bundle Size Check (size-limit), Tests (frontend), Tests (backend)." --json number --jq '.number' 2>/dev/null || gh pr list --head DesignV2 --base main --json number --jq '.[0].number') && test -n "$PR_NUM" && echo "PR opened: #$PR_NUM"</automated>
  </verify>
  <acceptance_criteria>
    - `git push origin DesignV2` succeeds without `--force`
    - PR exists with base=main, head=DesignV2 (queryable via `gh pr list --head DesignV2 --base main`)
    - PR body contains the literal phrase "Merge strategy: --no-ff" and the 6 required-context names
  </acceptance_criteria>
  <done>DesignV2 pushed; merge PR open and discoverable via gh.</done>
</task>

<task type="auto">
  <name>Task 4: Watch CI on the merge PR until all 6 required contexts are green</name>
  <files>
    (CI watch — no local file writes)
  </files>
  <read_first>
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Code Examples → Watch CI run on the merge PR"
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Open Questions → #4 What happens if Tests regression is hard to fix"
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-CONTEXT.md §"D-07, D-08"
  </read_first>
  <action>
    Resolve PR number via `gh pr list --head DesignV2 --base main --json number --jq '.[0].number'`. Run `gh pr checks "$PR_NUM" --watch`. When the watcher exits, query `gh pr view "$PR_NUM" --json statusCheckRollup --jq '.statusCheckRollup | map(select(.name | IN("type-check","Security Scan","Lint","Bundle Size Check (size-limit)","Tests (frontend)","Tests (backend)"))) | map({name: .name, conclusion: .conclusion})'`. ALL 6 conclusions MUST be "SUCCESS". If any are FAILURE/CANCELLED, follow D-08: capture the failing job's logs to a scratch file, fix on DesignV2 (same branch — single-PR audit trail), push, re-run. Budget 3 fix iterations per RESEARCH Open Question #4 before pausing to escalate to the user.
  </action>
  <verify>
    <automated>PR_NUM=$(gh pr list --head DesignV2 --base main --json number --jq '.[0].number') && gh pr view "$PR_NUM" --json statusCheckRollup --jq '[.statusCheckRollup[] | select(.name == "type-check" or .name == "Security Scan" or .name == "Lint" or .name == "Bundle Size Check (size-limit)" or .name == "Tests (frontend)" or .name == "Tests (backend)") | .conclusion] | unique' | grep -qx '\["SUCCESS"\]' && echo "ALL 6 SUCCESS"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 required contexts report `conclusion = "SUCCESS"` on the merge PR
    - No required context is in `pending`, `failure`, `cancelled`, or `skipped` state
    - If fixes were needed, they were committed to DesignV2 directly (no fix-branch-off-DesignV2 PR)
  </acceptance_criteria>
  <done>Merge PR is green on all 6 required contexts; ready for the human Merge click.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: PAUSE — decide production-deploy posture and click Merge</name>
  <what-built>
    Merge PR is green on all 6 required contexts (verified Task 4). Pre-merge tag provenance captured (Task 1). Local parity passed (Task 2).
  </what-built>
  <how-to-verify>
    BEFORE clicking Merge, decide the production-deploy posture (RESEARCH Open Question #2 / Pitfall 6). `.github/workflows/deploy.yml` auto-triggers on CI success on main HEAD — meaning **the merge will trigger a production deploy to the DigitalOcean droplet**.

    Choose ONE of three options and tell Claude:
    1. **Accept the auto-deploy.** The merge is intended to ship; production should follow. (Default.)
    2. **Disable temporarily.** Run `gh workflow disable Deploy` BEFORE clicking Merge; `gh workflow enable Deploy` AFTER verification. Two-step ceremony; reduces blast radius if the merge has runtime regressions.
    3. **Time-window the merge.** Schedule the Merge click for a low-traffic window (you specify when) so a regression can be reverted (D-03: `git revert -m 1 <merge-sha>`) with minimal user impact.

    Once you've stated your choice:
    - If you chose Option 2, Claude will run the `gh workflow disable Deploy` now (and re-enable after Task 7 verification).
    - Then visit https://github.com/alzahrani-khalid/Intl-Dossier-V2.0/pulls
    - Open the Phase 55 merge PR
    - Confirm GitHub shows "All checks have passed" (green)
    - Click "Merge pull request" → "Create a merge commit" → "Confirm merge"
    - Reply with the merge commit SHA (visible on the closed PR page) so Claude can capture it

    DO NOT use "Squash and merge" or "Rebase and merge" — both would orphan signed phase-base tags from main's reachable history (D-01 hard lock).

  </how-to-verify>
  <resume-signal>Reply with: (a) deploy posture choice (1/2/3) and (b) the merge commit SHA after clicking Merge.</resume-signal>
  <files>(human gate — no file writes by Claude during this task)</files>
  <action>This is a human-gate checkpoint. Claude pauses here and waits for the resume-signal described below. See <what-built> and <how-to-verify> above for what the human must do.</action>
  <verify>
    <automated>echo "Checkpoint passed — proceed to next task only after human resume-signal received."</automated>
  </verify>
  <done>Human has supplied the resume-signal described above; Claude resumes with the captured value(s).</done>
</task>

<task type="auto">
  <name>Task 6: Capture merge evidence and verify --no-ff merge commit on main</name>
  <files>
    .planning/phases/55-designv2-main-merge-gate-enforcement/55-MERGE-PR-EVIDENCE.json
  </files>
  <read_first>
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Code Examples → Verify prior signed tags survived the merge"
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Pattern 1: `--no-ff` merge preserves signed annotated tag reachability"
  </read_first>
  <action>
    Capture merge evidence per VALIDATION.md "MERGE-01 contexts SUCCESS" row. Resolve PR number via `gh pr list --base main --state closed --head DesignV2 --json number --jq '.[0].number'`. Write the full machine-readable record to `.planning/phases/55-designv2-main-merge-gate-enforcement/55-MERGE-PR-EVIDENCE.json` using `gh pr view "$PR_NUM" --json mergeStateStatus,statusCheckRollup,mergeCommit,number,state,closedAt`. Then `git fetch origin --tags --prune && git checkout main && git pull --ff-only origin main`. Verify the merge commit IS a merge commit (2 parents): `git cat-file -p HEAD | head -3 | grep -c '^parent'` MUST return `2`. Verify each of the 4 prior phase-base tags is reachable from origin/main and still verifies via `git tag -v`. Do NOT issue phase-55-base tag yet (next task).
  </action>
  <verify>
    <automated>PR_NUM=$(gh pr list --base main --state closed --head DesignV2 --json number --jq '.[0].number') && gh pr view "$PR_NUM" --json mergeStateStatus,statusCheckRollup,mergeCommit,number,state,closedAt > .planning/phases/55-designv2-main-merge-gate-enforcement/55-MERGE-PR-EVIDENCE.json && jq -e '.state == "MERGED" and (.mergeCommit.oid | length) == 40' .planning/phases/55-designv2-main-merge-gate-enforcement/55-MERGE-PR-EVIDENCE.json && git fetch origin --tags --prune && git checkout main && git pull --ff-only origin main && test "$(git cat-file -p HEAD | grep -c '^parent ')" -eq 2 && for t in phase-47-base phase-48-base phase-49-base phase-54-base; do git tag -v "$t" 2>&1 | grep -q 'Good "git" signature' || exit 1; git merge-base --is-ancestor "$(git rev-parse "$t")" origin/main || exit 1; done && echo "MERGE VERIFIED + 4 TAGS PRESERVED"</automated>
  </verify>
  <acceptance_criteria>
    - `55-MERGE-PR-EVIDENCE.json` exists with `state="MERGED"` and a 40-char `mergeCommit.oid`
    - Current HEAD on local main is a merge commit (2 parents)
    - All 4 prior phase-base tags pass `git tag -v` AND `git merge-base --is-ancestor <tag-sha> origin/main`
    - No squash detected (commit has 2 parents = `--no-ff` succeeded per D-01)
  </acceptance_criteria>
  <done>Merge landed correctly with provenance intact; evidence committed.</done>
</task>

<task type="auto">
  <name>Task 7: Delete remote DesignV2 (D-04) and issue signed phase-55-base tag</name>
  <files>
    (tag creation + remote deletion — no local file writes other than tag object)
  </files>
  <read_first>
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Code Examples → Delete remote DesignV2 (D-04)"
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Code Examples → Tag phase-55-base (annotated + signed)"
    - CLAUDE.md §"Tag signing setup"
  </read_first>
  <action>
    Per D-04 sequence — first sanity-check that origin/DesignV2 tip is reachable from origin/main (proves merge is complete): `git fetch origin && git merge-base --is-ancestor origin/DesignV2 origin/main`. Then verify phase-54-base preserves forensic recovery (RESEARCH Code Examples note: phase-54-base SHA may differ from DesignV2 tip by ~5 docs commits — that's expected and documented in SUMMARY). Delete remote DesignV2: `gh api -X DELETE repos/alzahrani-khalid/Intl-Dossier-V2.0/git/refs/heads/DesignV2` then `git fetch --prune origin`. Then issue the signed tag on origin/main HEAD (the merge commit): `git checkout main && git pull --ff-only origin main && git tag -s -a phase-55-base -m "Phase 55 baseline: DesignV2 → main merge & gate enforcement (MERGE-01, MERGE-02)" && git push origin phase-55-base`. Verify locally: `git tag -v phase-55-base` MUST exit 0.
  </action>
  <verify>
    <automated>git fetch origin && git merge-base --is-ancestor origin/DesignV2 origin/main 2>/dev/null || true; gh api -X DELETE repos/alzahrani-khalid/Intl-Dossier-V2.0/git/refs/heads/DesignV2 2>&1 | grep -qE '(204|Not Found)' && git fetch --prune origin && git checkout main && git pull --ff-only origin main && git tag -s -a phase-55-base -m "Phase 55 baseline: DesignV2 → main merge & gate enforcement (MERGE-01, MERGE-02)" 2>&1 | head -5 && git push origin phase-55-base && git tag -v phase-55-base 2>&1 | grep -q 'Good "git" signature' && echo "phase-55-base SIGNED + VERIFIED"</automated>
  </verify>
  <acceptance_criteria>
    - `gh api -X DELETE .../refs/heads/DesignV2` returns 204 (or 404 if already gone)
    - `git ls-remote origin DesignV2` returns empty
    - `phase-55-base` exists locally and on origin: `git ls-remote origin refs/tags/phase-55-base` non-empty
    - `git tag -v phase-55-base` exits 0 with "Good \"git\" signature"
    - The tag points to the merge commit SHA (`git rev-parse phase-55-base^{commit}` == merge commit OID from EVIDENCE.json)
  </acceptance_criteria>
  <done>Remote DesignV2 gone; phase-55-base signed tag landed and verifiable.</done>
</task>

<task type="auto">
  <name>Task 8: Write Plan 1 SUMMARY with merge SHA, deploy decision, rollback procedure</name>
  <files>
    .planning/phases/55-designv2-main-merge-gate-enforcement/55-01-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-MERGE-PR-EVIDENCE.json
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Pitfall 5: `git revert -m 1 <merge-sha>` makes re-merging a 4-step dance"
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Create `55-01-SUMMARY.md`. Required sections: (a) Outcome line ("PASS" if all 4 ROADMAP success criteria covered by this plan met); (b) Merge commit SHA (from EVIDENCE.json `mergeCommit.oid`); (c) Live ahead-count at push time (per Pitfall 9 — record actual not the stale "110"); (d) Deploy posture decision recorded (which option from Task 5 the user chose, and what action was taken); (e) Tag verification summary (5 tag names: phase-47/48/49/54/55-base all "Good \"git\" signature"); (f) Rollback procedure verbatim from RESEARCH Pitfall 5 — the full 4-step revert-and-re-merge sequence; (g) D-13 reversal context note: this plan does NOT touch branch protection — that's plan 03. Tag SHA mismatch note (DesignV2 tip differs from phase-54-base by ~5 docs commits; preserved via merge commit) belongs here. Commit the SUMMARY.
  </action>
  <verify>
    <automated>test -f .planning/phases/55-designv2-main-merge-gate-enforcement/55-01-SUMMARY.md && grep -q 'mergeCommit\|merge commit SHA\|Merge commit' .planning/phases/55-designv2-main-merge-gate-enforcement/55-01-SUMMARY.md && grep -q 'phase-55-base' .planning/phases/55-designv2-main-merge-gate-enforcement/55-01-SUMMARY.md && grep -q 'git revert -m 1' .planning/phases/55-designv2-main-merge-gate-enforcement/55-01-SUMMARY.md && grep -q -i 'deploy' .planning/phases/55-designv2-main-merge-gate-enforcement/55-01-SUMMARY.md && echo "SUMMARY COMPLETE"</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY exists at `55-01-SUMMARY.md`
    - References the actual merge commit SHA (40-char hex from EVIDENCE.json)
    - References `phase-55-base` tag creation
    - Includes the 4-step rollback procedure from RESEARCH Pitfall 5
    - Records the deploy posture decision and what action was taken
    - Does NOT contain "[TODO]" or "[TBD]" markers
  </acceptance_criteria>
  <done>Plan 1 SUMMARY committed; merge fully recorded.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                                                            |
| -------------------- | ------------------------------------------------------------------------------------------------------ |
| local → origin (git) | Push of 113 commits crosses into trusted server state; protected by SSH key + branch protection rules. |
| local → GitHub API   | `gh api -X DELETE refs/heads/DesignV2` mutates server state; requires `repo` scope on PAT.             |
| local → GPG/SSH key  | `git tag -s` reads private SSH key from `~/.ssh/`; never crosses into network.                         |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                          | Disposition | Mitigation Plan                                                                                                                                                                                                        |
| --------- | ---------------------- | ------------------------------------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-55-01   | Tampering              | branch protection (not modified in this plan, but verified intact) | mitigate    | Task 1 verifies pre-merge state; Plan 03 round-trips with `enforce_admins`, `allow_force_pushes=false`, `allow_deletions=false` preserved. Plan 01 does not touch protection at all.                                   |
| T-55-02   | Spoofing               | SSH-signed phase-base tags                                         | mitigate    | Task 1 verifies all 4 prior tags pass `git tag -v` before merge; Task 6 verifies they STILL pass after merge AND are reachable from origin/main; Task 7 verifies new `phase-55-base` with `git tag -v` after creation. |
| T-55-04   | Information Disclosure | production deploy on merge                                         | mitigate    | Task 5 (blocking checkpoint) forces explicit user decision on deploy posture before Merge click; documents the choice in SUMMARY. No silent deploy.                                                                    |
| T-55-PD   | Tampering              | `--no-ff` merge integrity                                          | mitigate    | Task 6 asserts HEAD is a merge commit (2 parents) — squash/rebase would yield 1 parent, fail the gate, surface to user.                                                                                                |
| T-55-RB   | Repudiation            | rollback procedure                                                 | mitigate    | Task 8 SUMMARY documents the 4-step revert-and-re-merge sequence verbatim from RESEARCH Pitfall 5, so future operators don't repeat the trap.                                                                          |
| T-55-SC   | Tampering              | npm/pip/cargo installs                                             | accept      | No package installs in this plan; `pnpm install --frozen-lockfile` uses pinned lockfile only.                                                                                                                          |

</threat_model>

<verification>
Plan 1 closes when:
1. `git merge-base --is-ancestor $(git rev-parse phase-54-base) origin/main` exits 0 (DesignV2 history reachable from main).
2. 6 of 6 required contexts on the merge PR are `SUCCESS` per `gh pr view --json statusCheckRollup`.
3. 4 prior phase-base tags + `phase-55-base` all emit "Good \"git\" signature" via `git tag -v`.
4. `git ls-remote origin DesignV2` returns empty (D-04 satisfied).
5. `55-MERGE-PR-EVIDENCE.json` and `55-01-SUMMARY.md` committed to phase folder.
</verification>

<success_criteria>
ROADMAP Phase 55 criterion #1 (DesignV2 history merged with no manual cherry-picks) MET.
ROADMAP Phase 55 criterion #2 (pnpm type-check, pnpm lint, Bundle Size Check exit 0 on post-merge main HEAD) PARTIAL — Plan 01 establishes the merge; Plan 02 piggybacks the green main HEAD when its PR runs CI. The "post-merge main HEAD CI green" assertion completes when Plan 02 lands.
</success_criteria>

<output>
Create `.planning/phases/55-designv2-main-merge-gate-enforcement/55-01-SUMMARY.md` with outcome status, merge SHA, deploy decision, tag verification, and rollback procedure.
</output>
