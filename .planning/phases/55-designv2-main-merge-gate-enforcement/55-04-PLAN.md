---
phase: 55-designv2-main-merge-gate-enforcement
plan: 04
type: execute
wave: 4
depends_on:
  - 55-03
files_modified:
  - frontend/src/components/__smoke__/Phase55GateSmoke.tsx
  - frontend/tests/__smoke__/phase55-smoke.test.ts
  - .planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.json
  - .planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.png
  - .planning/phases/55-designv2-main-merge-gate-enforcement/55-04-SUMMARY.md
autonomous: false
requirements:
  - MERGE-02
user_setup:
  - service: github
    why: 'Capture screenshot of BLOCKED smoke-PR banner (Claude cannot capture browser viewport per VALIDATION.md Manual-Only Verifications row 2)'
    env_vars: []
    dashboard_config:
      - task: 'Take a screenshot of the smoke PR page showing the `Merging is blocked` red banner + the required-context failure rollup'
        location: 'https://github.com/alzahrani-khalid/Intl-Dossier-V2.0/pulls (open the Phase 55 smoke PR)'
must_haves:
  truths:
    - 'A single smoke PR against `main` plants 4 simultaneous violations: raw hex (Lint + Design Token Check), vi.mock factory regression (Tests + react-i18next Factory Check), bundle bloat (Bundle Size Check), and type error (type-check) — per D-09 multi-violation rationale'
    - '`gh pr view --json mergeStateStatus` on the smoke PR returns `"BLOCKED"` (not UNSTABLE — Pitfall 12; not lowercase `blocked` — Pitfall 11) — captured into `55-SMOKE-PR-EVIDENCE.json`'
    - 'Each planted violation maps to a required context (so the gate fires from a required context — Pitfall 12)'
    - 'A human-readable screenshot `55-SMOKE-PR-EVIDENCE.png` of the BLOCKED banner is committed alongside the JSON evidence'
    - 'Smoke PR is closed and its branch deleted via `gh pr close --delete-branch` (D-12); evidence persists in committed files'
    - 'Evidence files are committed and pushed BEFORE the smoke PR/branch is closed (Pitfall 10 sequencing)'
  artifacts:
    - path: 'frontend/src/components/__smoke__/Phase55GateSmoke.tsx'
      provides: 'New leaf component that imports the bad-design-token fixture + plants a type error + a bundle-bloat import — never wired into any route'
      contains: 'import.*tools/eslint-fixtures/bad-design-token'
    - path: 'frontend/tests/__smoke__/phase55-smoke.test.ts'
      provides: 'New leaf test that imports the bad-vi-mock fixture so the Tests + react-i18next Factory Check contexts fail'
      contains: 'import.*tools/eslint-fixtures/bad-vi-mock'
    - path: '.planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.json'
      provides: 'Machine-readable evidence of `mergeStateStatus=BLOCKED`'
      contains: 'mergeStateStatus, BLOCKED'
    - path: '.planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.png'
      provides: 'Human-readable screenshot of the GitHub UI BLOCKED banner'
      contains: '(binary PNG; size > 0 bytes)'
    - path: '.planning/phases/55-designv2-main-merge-gate-enforcement/55-04-SUMMARY.md'
      provides: 'Plan 04 closure record + full evidence summary'
  key_links:
    - from: 'Phase55GateSmoke.tsx'
      to: 'tools/eslint-fixtures/bad-design-token.tsx'
      via: 'static import'
      pattern: 'from [''"][^''"]*tools/eslint-fixtures/bad-design-token'
    - from: 'phase55-smoke.test.ts'
      to: 'tools/eslint-fixtures/bad-vi-mock.ts'
      via: 'static import'
      pattern: 'from [''"][^''"]*tools/eslint-fixtures/bad-vi-mock'
    - from: '55-SMOKE-PR-EVIDENCE.json'
      to: 'BLOCKED status on smoke PR'
      via: 'gh pr view --json mergeStateStatus'
      pattern: "\\.mergeStateStatus == \"BLOCKED\""
---

<objective>
Open ONE smoke PR against `main` with 4 simultaneously-planted violations targeting 4 required contexts (D-09 rationale). Wait until `gh pr view --json mergeStateStatus` returns `BLOCKED`. Capture the JSON evidence (Claude) and PNG evidence (user). Commit evidence to phase folder. THEN close the PR with `--delete-branch` per D-12 (Pitfall 10 sequencing — evidence MUST be committed first).

This plan completes MERGE-02 by proving the 8-context gate (post-Plan 03) actually blocks bad PRs in flight.

Purpose: Durable proof-of-life that the post-Plan 03 gate fires from required contexts (`BLOCKED`, not `UNSTABLE` — Pitfall 12), captured in machine- and human-readable forms.

Output: Smoke PR observed in `BLOCKED` state; JSON + PNG evidence committed; PR closed; branch deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/55-designv2-main-merge-gate-enforcement/55-CONTEXT.md
@.planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md
@.planning/phases/55-designv2-main-merge-gate-enforcement/55-VALIDATION.md
@.planning/phases/55-designv2-main-merge-gate-enforcement/55-03-SUMMARY.md
@tools/eslint-fixtures/bad-design-token.tsx
@tools/eslint-fixtures/bad-vi-mock.ts

<interfaces>
<!-- 8 required contexts on main after Plan 03 (verified in protection-after.json): -->
<!--   1. type-check                         (existing) -->
<!--   2. Security Scan                      (existing) -->
<!--   3. Lint                               (existing) -->
<!--   4. Bundle Size Check (size-limit)    (existing) -->
<!--   5. Tests (frontend)                   (existing) -->
<!--   6. Tests (backend)                    (existing) -->
<!--   7. Design Token Check                 (NEW — Plan 02) -->
<!--   8. react-i18next Factory Check        (NEW — Plan 02) -->

<!-- Planted-violations → contexts map (D-09 multi-violation rationale): -->
<!--   Raw hex import via bad-design-token  →  Lint + Design Token Check -->
<!--   vi.mock factory import via bad-vi-mock → Tests (frontend) + react-i18next Factory Check -->
<!--   `const badType: number = "string"`    →  type-check -->
<!--   Heavy static import for bundle bloat   →  Bundle Size Check (size-limit) -->
<!-- All 4 violations hit REQUIRED contexts so mergeStateStatus must be BLOCKED (not UNSTABLE). -->

<!-- Smoke plant location (RESEARCH recommends, CONTEXT D-10 leaves to Claude): -->
<!--   frontend/src/components/__smoke__/Phase55GateSmoke.tsx  (component side) -->
<!--   frontend/tests/__smoke__/phase55-smoke.test.ts          (test side; -->
<!--      imports bad-vi-mock.ts as a module — that triggers the eslint i18next-factory-check) -->
<!-- Both files are isolated to __smoke__/ subdirs, never imported by route code, -->
<!-- cleanly removable when the PR/branch is deleted. -->

<!-- Pitfall 10 sequencing — evidence files MUST be committed + pushed to a separate -->
<!-- branch (NOT the smoke branch — the smoke branch is the one we're going to delete) -->
<!-- BEFORE the smoke PR is closed. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the smoke branch with 4 planted violations</name>
  <files>
    frontend/src/components/__smoke__/Phase55GateSmoke.tsx
    frontend/tests/__smoke__/phase55-smoke.test.ts
  </files>
  <read_first>
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Code Examples → Smoke PR with planted violations (plan 4 — D-09, D-10)"
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Pitfall 12: BLOCKED vs UNSTABLE distinction"
    - tools/eslint-fixtures/bad-design-token.tsx
    - tools/eslint-fixtures/bad-vi-mock.ts
  </read_first>
  <action>
    Sync local main: `git fetch origin && git checkout main && git pull --ff-only origin main`. Branch: `git checkout -b smoke/phase-55-merge-02`. Create two new files in dedicated `__smoke__/` subdirs:

    File A — `frontend/src/components/__smoke__/Phase55GateSmoke.tsx`: a TSX component that (1) `export const planted = { color: '#abcdef' }` to trigger Lint + Design Token Check via the raw hex; (2) `import 'tools/eslint-fixtures/bad-design-token'` (or the appropriate relative path) so the smoke file's lint also pulls the fixture's violations into the lint context; (3) `const badType: number = 'this is not a number'` to trigger type-check (use a non-`any` literal assignment); (4) an import of a heavy library to bust the bundle ceiling — recommend importing the entire `moment` package or a similarly heavy module (Claude picks; the goal is to push initial-route size above 450 KB ceiling). Export the component returning `null`. Add a top comment: `// PHASE 55 MERGE-02 SMOKE — DO NOT MERGE. Evidence captured in 55-SMOKE-PR-EVIDENCE.{json,png}.`

    File B — `frontend/tests/__smoke__/phase55-smoke.test.ts`: imports `tools/eslint-fixtures/bad-vi-mock.ts` so vitest's run-time pulls the fixture's broken `vi.mock("react-i18next", () => ({}))` into the Tests context AND the react-i18next Factory Check picks it up as a lint violation. Top comment same as File A.

    Do NOT wire either file into route code, the App component, or test suites. The `__smoke__/` subdir keeps them isolated.

  </action>
  <verify>
    <automated>git fetch origin && git checkout main && git pull --ff-only origin main && git checkout -b smoke/phase-55-merge-02 && mkdir -p frontend/src/components/__smoke__ frontend/tests/__smoke__ && test -f frontend/src/components/__smoke__/Phase55GateSmoke.tsx && test -f frontend/tests/__smoke__/phase55-smoke.test.ts && grep -qE 'bad-design-token' frontend/src/components/__smoke__/Phase55GateSmoke.tsx && grep -qE 'bad-vi-mock' frontend/tests/__smoke__/phase55-smoke.test.ts && grep -qE '#[0-9a-fA-F]{3,8}' frontend/src/components/__smoke__/Phase55GateSmoke.tsx && grep -qE ': number = .[^0-9]|: number = "' frontend/src/components/__smoke__/Phase55GateSmoke.tsx && grep -qE 'DO NOT MERGE' frontend/src/components/__smoke__/Phase55GateSmoke.tsx && echo "4 PLANTED VIOLATIONS STAGED"</automated>
  </verify>
  <acceptance_criteria>
    - On branch `smoke/phase-55-merge-02` based on origin/main HEAD
    - Both files exist under `__smoke__/` subdirs
    - `Phase55GateSmoke.tsx` contains: raw hex literal, type error (`: number = "..."`), heavy import for bundle bloat, fixture import
    - `phase55-smoke.test.ts` imports `bad-vi-mock`
    - Top comment "DO NOT MERGE" in both files
    - Neither file is imported by any production code (`grep -r "Phase55GateSmoke" frontend/src` shows ONLY the file itself)
  </acceptance_criteria>
  <done>4 simultaneous violations planted in 2 isolated smoke files.</done>
</task>

<task type="auto">
  <name>Task 2: Commit, push, open smoke PR with explicit DO-NOT-MERGE labeling</name>
  <files>
    (commit + push + PR creation)
  </files>
  <read_first>
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Code Examples → Smoke PR with planted violations"
  </read_first>
  <action>
    Commit on branch `smoke/phase-55-merge-02`: `git add frontend/src/components/__smoke__/ frontend/tests/__smoke__/ && git commit -m "smoke(55): plant 4 multi-context violations — DO NOT MERGE [MERGE-02]"`. Push: `git push -u origin smoke/phase-55-merge-02`. Open PR: title prefix MUST include the literal strings "DO NOT MERGE" and "smoke" to make accidental merge obvious in any list view. Body should enumerate the 4 violations and which contexts each targets. Use `gh pr create --base main --head smoke/phase-55-merge-02 --title "Phase 55 MERGE-02 smoke: prove 8-context gate BLOCKS (DO NOT MERGE)" --body "..."`. Capture PR number for the rest of this plan.
  </action>
  <verify>
    <automated>git add frontend/src/components/__smoke__/ frontend/tests/__smoke__/ && git commit -m "smoke(55): plant 4 multi-context violations — DO NOT MERGE [MERGE-02]" && git push -u origin smoke/phase-55-merge-02 && PR_NUM=$(gh pr create --base main --head smoke/phase-55-merge-02 --title "Phase 55 MERGE-02 smoke: prove 8-context gate BLOCKS (DO NOT MERGE)" --body "Plants 4 simultaneous violations against required contexts (D-09 rationale):  - Raw hex literal → Lint + Design Token Check  - vi.mock factory regression import → Tests (frontend) + react-i18next Factory Check  - Type error → type-check  - Heavy import → Bundle Size Check (size-limit)  Expected: mergeStateStatus=BLOCKED. Evidence will be committed to phase folder; this PR will be closed with --delete-branch per D-12 once captured." --json number --jq '.number' 2>/dev/null || gh pr list --head smoke/phase-55-merge-02 --json number --jq '.[0].number') && test -n "$PR_NUM" && echo "SMOKE PR #$PR_NUM OPEN"</automated>
  </verify>
  <acceptance_criteria>
    - PR exists with base=main, head=smoke/phase-55-merge-02
    - Title contains literal "DO NOT MERGE" and "smoke"
    - Body enumerates 4 planted violations with their target contexts
  </acceptance_criteria>
  <done>Smoke PR open; CI starts running.</done>
</task>

<task type="auto">
  <name>Task 3: Poll until mergeStateStatus=BLOCKED and capture JSON evidence</name>
  <files>
    .planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.json
  </files>
  <read_first>
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Pitfall 11: `mergeStateStatus` requires GraphQL or `gh pr view --json` path"
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Pitfall 12: BLOCKED vs UNSTABLE distinction"
  </read_first>
  <action>
    Wait for ALL required contexts to report (NOT just the planted ones — others might race). Use `gh pr checks <num> --watch` to block until all known contexts are settled (failure is the expected terminal state, watch will exit when checks complete). After watch exits, capture `gh pr view <PR_NUM> --json mergeStateStatus,statusCheckRollup,number,state,headRefName,baseRefName` to `.planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.json`. Then assert `mergeStateStatus == "BLOCKED"` (uppercase from GraphQL per Pitfall 11) — NOT `UNSTABLE` (would mean a planted violation hit a non-required context — Pitfall 12). Also assert `statusCheckRollup` contains at least one of the 4 target required contexts in a `FAILURE` state. If `mergeStateStatus == UNSTABLE`, dig in: a planted violation didn't reach a required context — diagnose and surface to user.
  </action>
  <verify>
    <automated>PR_NUM=$(gh pr list --head smoke/phase-55-merge-02 --json number --jq '.[0].number') && gh pr checks "$PR_NUM" --watch || true && gh pr view "$PR_NUM" --json mergeStateStatus,statusCheckRollup,number,state,headRefName,baseRefName > .planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.json && jq -e '.mergeStateStatus == "BLOCKED"' .planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.json && jq -e '.statusCheckRollup | map(select(.conclusion == "FAILURE")) | length >= 1' .planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.json && echo "BLOCKED VERIFIED + EVIDENCE CAPTURED"</automated>
  </verify>
  <acceptance_criteria>
    - `55-SMOKE-PR-EVIDENCE.json` exists with `mergeStateStatus == "BLOCKED"` (uppercase)
    - At least 1 entry in `statusCheckRollup[]` with `conclusion == "FAILURE"` from a REQUIRED context
    - Not `UNSTABLE` (Pitfall 12 — would indicate a non-required context picked up the failure)
    - Not lowercase `blocked` (Pitfall 11 — would indicate REST endpoint was used)
  </acceptance_criteria>
  <done>JSON evidence captured and committed-pending; awaiting human screenshot.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: PAUSE — capture screenshot of BLOCKED banner</name>
  <what-built>
    JSON evidence proves `mergeStateStatus = BLOCKED`. Now we need the human-readable PNG (VALIDATION.md Manual-Only row 2).
  </what-built>
  <how-to-verify>
    Visit https://github.com/alzahrani-khalid/Intl-Dossier-V2.0/pulls and open the Phase 55 smoke PR (the one titled with "DO NOT MERGE").

    Confirm you see:
    - A red banner reading "Merging is blocked" (or "Required statuses must pass before merging")
    - The status-check rollup showing FAILURE on at least these contexts: `Lint`, `type-check`, `Tests (frontend)`, `Bundle Size Check (size-limit)`, `Design Token Check`, `react-i18next Factory Check`

    Capture a screenshot of the page (browser viewport including the banner AND the status check rollup section). Save it as:

    `.planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.png`

    Reply `screenshot saved` so Claude resumes.

    DO NOT click Merge. DO NOT close the PR yet — Claude will close it with --delete-branch in Task 6 AFTER all evidence is committed and pushed (Pitfall 10 sequencing).

  </how-to-verify>
  <resume-signal>Reply "screenshot saved" once `55-SMOKE-PR-EVIDENCE.png` exists in the phase folder.</resume-signal>
  <files>(human gate — no file writes by Claude during this task)</files>
  <action>This is a human-gate checkpoint. Claude pauses here and waits for the resume-signal described below. See <what-built> and <how-to-verify> above for what the human must do.</action>
  <verify>
    <automated>echo "Checkpoint passed — proceed to next task only after human resume-signal received."</automated>
  </verify>
  <done>Human has supplied the resume-signal described above; Claude resumes with the captured value(s).</done>
</task>

<task type="auto">
  <name>Task 5: Commit evidence to a separate evidence branch + open evidence PR + merge it</name>
  <files>
    (commit on new branch + open PR + merge — evidence files were created in Tasks 3 and 4)
  </files>
  <read_first>
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Pitfall 10: Smoke PR cleanup deletes the head branch but evidence must already be committed"
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.json (must already exist)
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.png (must already exist from Task 4)
  </read_first>
  <action>
    Verify the PNG exists and is non-empty: `test -s .planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.png`. Per Pitfall 10, the evidence MUST be committed and pushed to main BEFORE the smoke PR is closed. Switch to a NEW branch (not smoke/phase-55-merge-02, which is going to be deleted): `git checkout main && git pull --ff-only origin main && git checkout -b docs/phase-55-smoke-evidence`. Stage and commit ONLY the two evidence files: `git add .planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.json .planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.png`. Commit message: `docs(55): commit smoke-PR BLOCKED evidence [MERGE-02]`. Push: `git push -u origin docs/phase-55-smoke-evidence`. Open PR via `gh pr create --base main --head docs/phase-55-smoke-evidence`. Wait for 8 required contexts to pass on THIS PR (it contains only doc/evidence files so all should pass). After the watch confirms green, ASK USER to click Merge in UI (or pause for user) — same posture rules as Plan 01 Task 5 apply.
  </action>
  <verify>
    <automated>test -s .planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.png && test -s .planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.json && git checkout main && git pull --ff-only origin main && git checkout -b docs/phase-55-smoke-evidence && git add .planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.json .planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.png && git commit -m "docs(55): commit smoke-PR BLOCKED evidence [MERGE-02]" && git push -u origin docs/phase-55-smoke-evidence && EV_PR=$(gh pr create --base main --head docs/phase-55-smoke-evidence --title "Phase 55 Plan 04: commit smoke PR BLOCKED evidence (MERGE-02)" --body "Commits the JSON + PNG evidence captured from the smoke PR (which proved mergeStateStatus=BLOCKED on 8-context gate). Must merge BEFORE smoke PR is closed (Pitfall 10)." --json number --jq '.number' 2>/dev/null || gh pr list --head docs/phase-55-smoke-evidence --json number --jq '.[0].number') && test -n "$EV_PR" && gh pr checks "$EV_PR" --watch && echo "EVIDENCE PR #$EV_PR — awaiting user Merge"</automated>
  </verify>
  <acceptance_criteria>
    - PNG file is non-empty (`test -s` exits 0)
    - JSON file is non-empty (`test -s` exits 0)
    - Branch `docs/phase-55-smoke-evidence` pushed to origin
    - Evidence PR open and 8 required contexts pass on it
    - (Will require user Merge click as in Plan 01 Task 5 — same deploy posture carries forward)
  </acceptance_criteria>
  <done>Evidence PR ready for user Merge; smoke PR still open (NOT yet closed — order matters).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: PAUSE — user merges evidence PR; Claude then closes smoke PR</name>
  <what-built>
    Evidence PR is green; smoke PR is still open with BLOCKED status.
  </what-built>
  <how-to-verify>
    Step 1 (user): Click Merge on the evidence PR `docs/phase-55-smoke-evidence` (the one titled "Phase 55 Plan 04: commit smoke PR BLOCKED evidence"). This commits the evidence to main BEFORE the smoke PR is closed (Pitfall 10 sequencing).

    Step 2 (user): Reply with the evidence PR's merge commit SHA so Claude can verify and proceed to close the smoke PR.

    DO NOT close the smoke PR yourself — Claude will do that programmatically in Task 7.

  </how-to-verify>
  <resume-signal>Reply with the evidence PR merge commit SHA after clicking Merge.</resume-signal>
  <files>(human gate — no file writes by Claude during this task)</files>
  <action>This is a human-gate checkpoint. Claude pauses here and waits for the resume-signal described below. See <what-built> and <how-to-verify> above for what the human must do.</action>
  <verify>
    <automated>echo "Checkpoint passed — proceed to next task only after human resume-signal received."</automated>
  </verify>
  <done>Human has supplied the resume-signal described above; Claude resumes with the captured value(s).</done>
</task>

<task type="auto">
  <name>Task 7: Close smoke PR with --delete-branch (D-12) and write Plan 04 SUMMARY</name>
  <files>
    .planning/phases/55-designv2-main-merge-gate-enforcement/55-04-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Code Examples → Cleanup (D-12)"
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.json
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Confirm evidence is on origin/main: `git fetch origin && git checkout main && git pull --ff-only origin main && test -f .planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.json && test -f .planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.png`. THEN per D-12, close the smoke PR + delete its branch: resolve smoke PR number, run `gh pr close <num> --delete-branch --comment "Evidence captured in 55-SMOKE-PR-EVIDENCE.json + .png (merged to main). Closing per Phase 55 D-12 — DO NOT MERGE."`. Verify branch is deleted: `gh api repos/alzahrani-khalid/Intl-Dossier-V2.0/branches/smoke/phase-55-merge-02 2>&1 | grep -q 'Not Found'`. Then create `55-04-SUMMARY.md` with sections: (a) Outcome (PASS); (b) Final mergeStateStatus value from evidence JSON; (c) The 4 planted violations + which required context each hit; (d) Confirmation that all 4 ROADMAP success criteria are now met (criterion 3 specifically — the BLOCKED capture); (e) D-09 "single multi-violation PR" rationale satisfied; (f) Smoke artifacts cleanup status (branch deleted, PR closed, evidence committed); (g) Phase 55 closure note — `phase-55-base` tag was issued in Plan 01 (do NOT re-tag); recommend running `/gsd:verify-work 55` next.
  </action>
  <verify>
    <automated>git fetch origin && git checkout main && git pull --ff-only origin main && test -f .planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.json && test -f .planning/phases/55-designv2-main-merge-gate-enforcement/55-SMOKE-PR-EVIDENCE.png && SMOKE_PR=$(gh pr list --head smoke/phase-55-merge-02 --state open --json number --jq '.[0].number // empty') && if [ -n "$SMOKE_PR" ]; then gh pr close "$SMOKE_PR" --delete-branch --comment "Evidence captured in 55-SMOKE-PR-EVIDENCE.json + .png (merged to main). Closing per Phase 55 D-12 — DO NOT MERGE."; fi && (gh api repos/alzahrani-khalid/Intl-Dossier-V2.0/branches/smoke/phase-55-merge-02 2>&1 | grep -q 'Not Found') && test -f .planning/phases/55-designv2-main-merge-gate-enforcement/55-04-SUMMARY.md && grep -q 'BLOCKED' .planning/phases/55-designv2-main-merge-gate-enforcement/55-04-SUMMARY.md && grep -q 'D-12\|delete-branch' .planning/phases/55-designv2-main-merge-gate-enforcement/55-04-SUMMARY.md && echo "SMOKE PR CLOSED + BRANCH DELETED + SUMMARY DONE"</automated>
  </verify>
  <acceptance_criteria>
    - Evidence JSON + PNG both present on origin/main (pulled into local)
    - Smoke PR is closed (`state == "CLOSED"`)
    - Smoke branch `smoke/phase-55-merge-02` is deleted on remote (404 on `gh api ... /branches/smoke/phase-55-merge-02`)
    - `55-04-SUMMARY.md` exists; mentions BLOCKED + D-12
    - All 4 ROADMAP success criteria stated as MET in the SUMMARY
  </acceptance_criteria>
  <done>Smoke PR cleanup complete; phase 55 closure recorded.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                                                                                                        |
| ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| smoke branch → main     | Smoke PR explicitly intended NOT to merge; relies on branch protection to enforce.                                                 |
| evidence PR → main      | Real merge of doc-only files; goes through the full 8-context gate (proves new gate works on a real merge AND lands the evidence). |
| local files → committed | Evidence files (JSON + PNG) must reach origin/main BEFORE smoke PR/branch deletion (Pitfall 10).                                   |

## STRIDE Threat Register

| Threat ID | Category    | Component                                                            | Disposition | Mitigation Plan                                                                                                                                                                                                        |
| --------- | ----------- | -------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-55-03   | Tampering   | smoke PR accidentally merged                                         | mitigate    | (a) PR title contains "DO NOT MERGE" literal. (b) `mergeStateStatus=BLOCKED` from branch protection prevents merge. (c) Task 6 explicitly tells user "DO NOT close smoke PR yourself"; Claude closes programmatically. |
| T-55-EV   | Repudiation | evidence lost when branch deleted                                    | mitigate    | Pitfall 10 sequencing — Task 5 commits evidence to a SEPARATE branch and merges it BEFORE Task 7 deletes the smoke branch. Branch-deletion only happens after `test -f` of both evidence files succeeds.               |
| T-55-MS   | Tampering   | mergeStateStatus reported via wrong endpoint                         | mitigate    | Use `gh pr view --json mergeStateStatus` (GraphQL path per Pitfall 11). Assertion in Task 3 requires UPPERCASE `BLOCKED` (would be lowercase if REST endpoint were used by mistake).                                   |
| T-55-NR   | Tampering   | planted violation hits a non-required context (UNSTABLE not BLOCKED) | mitigate    | Task 3 asserts `mergeStateStatus == "BLOCKED"` (not UNSTABLE per Pitfall 12). The 4 planted violations are mapped to required contexts in the planning document.                                                       |
| T-55-SC   | Tampering   | npm/pip/cargo installs                                               | accept      | No new package installs; only `pnpm install --frozen-lockfile` in CI. The heavy import for bundle bloat reuses an already-installed dependency (e.g. moment is already in package.json).                               |

</threat_model>

<verification>
Plan 04 closes (and Phase 55 closes) when:
1. `55-SMOKE-PR-EVIDENCE.json` shows `mergeStateStatus == "BLOCKED"` and is committed on main.
2. `55-SMOKE-PR-EVIDENCE.png` is non-empty and committed on main.
3. Smoke PR is closed; smoke branch is deleted on origin.
4. `55-04-SUMMARY.md` committed.
5. (Phase-level) All 4 ROADMAP success criteria stated MET in the SUMMARY.
</verification>

<success_criteria>
ROADMAP Phase 55 criterion #3 (smoke PR with intentional violation returns `mergeStateStatus=BLOCKED` from `gh pr view --json`) FULLY SATISFIED. Combined with prior plans, all 4 ROADMAP success criteria are now met:

1. Plan 01 → DesignV2 history merged.
2. Plan 02 → CI green on post-merge main HEAD (including new jobs).
3. Plan 04 → Smoke PR returns BLOCKED (this plan).
4. Plan 03 → 8 required contexts on main protection.

Phase 55 is ready for `/gsd:verify-work 55`.
</success_criteria>

<output>
Create `.planning/phases/55-designv2-main-merge-gate-enforcement/55-04-SUMMARY.md` recording smoke evidence + cleanup + phase closure.
</output>
