---
phase: 52-heroui-v3-kanban-migration
plan: 05
type: execute
wave: 5
depends_on:
  - 52-04
files_modified:
  - frontend/tests/e2e/tasks-tab-visual.spec.ts
  - frontend/tests/e2e/engagement-kanban-dialog-visual.spec.ts
  - frontend/tests/e2e/tasks-tab-dnd.spec.ts
  - frontend/tests/e2e/tasks-tab-keyboard.spec.ts
  - frontend/tests/e2e/tasks-tab-a11y.spec.ts
  - frontend/tests/e2e/engagement-kanban-dialog-dnd.spec.ts
  - frontend/tests/e2e/engagement-kanban-dialog-keyboard.spec.ts
  - frontend/tests/e2e/engagement-kanban-dialog-a11y.spec.ts
  - .planning/phases/52-heroui-v3-kanban-migration/52-VALIDATION.md
  - .planning/phases/52-heroui-v3-kanban-migration/52-SUMMARY.md
autonomous: false
requirements:
  - KANBAN-04
tags:
  - kanban
  - visual-baselines
  - playwright
  - a11y
  - human-verify

must_haves:
  truths:
    - A seeded fixture engagement exists on staging with composition todo:2 / in_progress:2 / review:1 / done:2 / cancelled:1 OR is created by an idempotent seed step in this plan
    - 8 new Playwright visual baselines exist on disk (2 specs × 4 viewports) under tests/e2e/__screenshots__/ for tasks-tab-visual.spec.ts and engagement-kanban-dialog-visual.spec.ts
    - All 8 baselines are human-reviewed and approved via checkpoint:human-verify
    - axe-core specs report zero serious/critical violations on both surfaces
    - The Plan-01 test.fixme placeholders in 6 dnd/keyboard/a11y specs are flipped to real test() blocks asserting behavior parity
    - 12 existing kanban-*.spec.ts files (WorkBoard regression anchor) remain green
    - 52-VALIDATION.md status column shows all rows ✅ green
    - 52-SUMMARY.md exists capturing phase verdict, deviations, follow-ups
  artifacts:
    - path: frontend/tests/e2e/__screenshots__/tasks-tab-visual.spec.ts-snapshots/
      provides: 4 PNG baselines (LTR×{1280,768}, RTL×{1280,768})
    - path: frontend/tests/e2e/__screenshots__/engagement-kanban-dialog-visual.spec.ts-snapshots/
      provides: 4 PNG baselines (LTR×{1280,768}, RTL×{1280,768})
    - path: .planning/phases/52-heroui-v3-kanban-migration/52-SUMMARY.md
      provides: Phase verdict + deviations + follow-ups
      contains: 'KANBAN-01'
  key_links:
    - from: frontend/tests/e2e/tasks-tab-visual.spec.ts
      to: seeded engagement in staging Supabase
      via: PHASE_52_FIXTURE_ENGAGEMENT_ID env var or hardcoded UUID
      pattern: 'SEEDED_ENGAGEMENT_ID'
    - from: 52-SUMMARY.md
      to: 52-VALIDATION.md status column
      via: per-task verification map
      pattern: '✅ green'
---

<objective>
Close Phase 52 by regenerating EN+AR visual baselines for both Kanban surfaces, wiring axe-core a11y assertions, flipping the Wave-0 `test.fixme` placeholders to real behavior assertions, verifying the 12 existing kanban-*.spec.ts regression anchor stays green, and writing 52-SUMMARY.md with the phase verdict. This plan carries the only human-touch in the phase: a checkpoint:human-verify to approve the 8 new baseline PNGs AND the mid-drag overlay visual parity (52-RESEARCH Open Question 3).

Purpose: KANBAN-04 success criterion #4 (visual baselines regenerated, human-reviewed, committed) is the only requirement requiring human eyes. Everything else in the phase is automated. This plan packages the human-verify cleanly so the rest of the phase can run autonomously.

Output: 8 new PNG baselines, 6 flipped Playwright specs, 1 SUMMARY.md, 52-VALIDATION.md status column flipped to ✅ across all rows. The kibo-ui module is gone; the new primitive is the live surface; the migration is sealed and ready for Plan 53 (bundle tightening).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-heroui-v3-kanban-migration/52-CONTEXT.md
@.planning/phases/52-heroui-v3-kanban-migration/52-PATTERNS.md
@.planning/phases/52-heroui-v3-kanban-migration/52-RESEARCH.md
@.planning/phases/52-heroui-v3-kanban-migration/52-VALIDATION.md
@.planning/phases/52-heroui-v3-kanban-migration/52-UI-SPEC.md
@frontend/tests/e2e/kanban-visual.spec.ts
@frontend/tests/e2e/kanban-a11y.spec.ts
@frontend/tests/e2e/kanban-dnd.spec.ts
@frontend/tests/e2e/keyboard-navigation-kanban.spec.ts
@frontend/playwright.config.ts
@.planning/phases/52-heroui-v3-kanban-migration/52-01-PLAN.md

<interfaces>
<!-- Plan-01 spec skeletons that need flipping from test.fixme to test() -->

```typescript
// tests/e2e/tasks-tab-dnd.spec.ts (currently test.fixme stub)
// Mirror frontend/tests/e2e/kanban-dnd.spec.ts shape:
//   - addInitScript for i18nextLng
//   - goto /engagements/${SEEDED_ENGAGEMENT_ID}
//   - locate card by role + visible name
//   - page.mouse.move(...).down().move(...).up() OR page.dragTo() per dnd-kit recipe
//   - reload page; assert card is in new column (drop persistence)
```

```typescript
// tests/e2e/tasks-tab-keyboard.spec.ts shape (mirror keyboard-navigation-kanban.spec.ts):
//   - Tab to first card; assert focus visible
//   - Space (pick up); assert announcement region updates
//   - Arrow Right; assert column hover
//   - Space (drop); assert column placement
```

<!-- Existing 12 regression-anchor specs that must stay green: -->
<!-- kanban-a11y, kanban-dnd, kanban-filters, kanban-render, kanban-responsive, kanban-rtl, kanban-search, kanban-visual, keyboard-navigation-kanban, drag-task-between-kanban-columns, open-kanban-board, realtime-kanban-updates-two-windows, performance/kanban-drag-drop-latency -->
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed fixture engagement on staging (idempotent) and surface ID via env var</name>
  <files>
    .planning/phases/52-heroui-v3-kanban-migration/52-FIXTURE.md
  </files>
  <read_first>
    - .planning/phases/52-heroui-v3-kanban-migration/52-CONTEXT.md D-16
    - .planning/phases/52-heroui-v3-kanban-migration/52-RESEARCH.md Assumption A7 + Open Question 2
    - .planning/STATE.md §"Phase 40 summary" g0 entry (Phase 40-12 idempotent seed precedent via Supabase MCP)
    - frontend/src/types/work-item.types.ts (WorkflowStage enum)
    - frontend/src/domains/engagements/hooks/useEngagementKanban.ts (data shape consumers expect)
  </read_first>
  <action>
    Step 1 — query staging Supabase via MCP to find an existing engagement with the D-16 composition (`todo:2, in_progress:2, review:1, done:2, cancelled:1`):
      Use Supabase MCP `execute_sql` (read-only) with a query like:
      ```sql
      SELECT e.id, e.title_en, COUNT(*) FILTER (WHERE ea.workflow_stage='todo') AS todo,
             COUNT(*) FILTER (WHERE ea.workflow_stage='in_progress') AS in_progress,
             COUNT(*) FILTER (WHERE ea.workflow_stage='review') AS review,
             COUNT(*) FILTER (WHERE ea.workflow_stage='done') AS done,
             COUNT(*) FILTER (WHERE ea.workflow_stage='cancelled') AS cancelled
      FROM engagements e
      JOIN engagement_assignments ea ON ea.engagement_id = e.id
      GROUP BY e.id, e.title_en
      HAVING COUNT(*) FILTER (WHERE ea.workflow_stage='todo') = 2
         AND COUNT(*) FILTER (WHERE ea.workflow_stage='in_progress') = 2
         AND COUNT(*) FILTER (WHERE ea.workflow_stage='review') = 1
         AND COUNT(*) FILTER (WHERE ea.workflow_stage='done') = 2
         AND COUNT(*) FILTER (WHERE ea.workflow_stage='cancelled') = 1;
      ```
      Adjust table/column names if the schema differs (the actual schema in use is determined by reading useEngagementKanban.ts).

    Step 2 (path A — engagement exists):
      Record the engagement UUID. Create `.planning/phases/52-heroui-v3-kanban-migration/52-FIXTURE.md` documenting:
        - The fixture engagement UUID
        - Composition snapshot (verified via MCP query)
        - Discovery method ("found existing — no seed migration needed")
        - The env var contract: specs read `PHASE_52_FIXTURE_ENGAGEMENT_ID` from `.env.test` and fall back to the hardcoded UUID.
      Update `.env.test.example` (NOT `.env.test` — that's local-only) with:
      ```
      PHASE_52_FIXTURE_ENGAGEMENT_ID=<uuid-from-query>
      ```

    Step 2 (path B — no engagement matches):
      Use Supabase MCP `apply_migration` to create an idempotent seed migration. Mirror the Phase 40-12 + 40-20 pattern from STATE.md. Migration file name pattern: `20260516120000_phase_52_kanban_fixture_seed.sql`. The migration:
        - Inserts one engagement row with a fixed UUID (planner-chosen, e.g., `00000000-0000-0052-0000-000000000001`)
        - Inserts 8 engagement_assignment rows with that engagement_id, distributed 2/2/1/2/1 across the 5 WorkflowStage values
        - Uses `ON CONFLICT (id) DO UPDATE SET ...` or `ON CONFLICT (id) DO NOTHING` for idempotency
        - Respects all RLS, tenant_id, created_by, NOT NULL constraints — read the existing schema first via `list_tables` + sample row inspection
      After applying, re-run the Step 1 verification query to confirm the composition matches. Record the fixed UUID in 52-FIXTURE.md.

    Step 3 — wire env var into the 8 visual + dnd + keyboard + a11y specs:
      Each spec already reads `process.env.PHASE_52_FIXTURE_ENGAGEMENT_ID ?? '<TBD>'` (per Plan 01). Either:
        - Replace the `'<TBD>'` fallback with the actual UUID hardcoded (acceptable since the UUID is non-sensitive test fixture data, will be committed to git in Path B's migration anyway), OR
        - Document in 52-FIXTURE.md that CI + local devs must set `PHASE_52_FIXTURE_ENGAGEMENT_ID` in `.env.test`.
      Both options are valid; pick one based on whether the engagement UUID is meant to be ephemeral (env-var) or canonical (hardcoded in spec). Phase 40 used the hardcoded approach; following that precedent reduces CI flakiness.

  </action>
  <verify>
    <automated>test -f .planning/phases/52-heroui-v3-kanban-migration/52-FIXTURE.md &amp;&amp; grep -E "PHASE_52_FIXTURE_ENGAGEMENT_ID|fixture engagement UUID|00000000-0000-0052" .planning/phases/52-heroui-v3-kanban-migration/52-FIXTURE.md | head -3</automated>
  </verify>
  <done>
    52-FIXTURE.md exists. A seeded engagement on staging matches the D-16 composition. The 8 Playwright spec files have access to the UUID via env var or hardcoded constant. Path B migration (if used) is idempotent on re-apply.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip 6 Wave-0 test.fixme stubs to real Playwright assertions (dnd + keyboard + a11y across both surfaces)</name>
  <files>
    frontend/tests/e2e/tasks-tab-dnd.spec.ts
    frontend/tests/e2e/tasks-tab-keyboard.spec.ts
    frontend/tests/e2e/tasks-tab-a11y.spec.ts
    frontend/tests/e2e/engagement-kanban-dialog-dnd.spec.ts
    frontend/tests/e2e/engagement-kanban-dialog-keyboard.spec.ts
    frontend/tests/e2e/engagement-kanban-dialog-a11y.spec.ts
  </files>
  <read_first>
    - frontend/tests/e2e/kanban-dnd.spec.ts (mouse-drag pattern)
    - frontend/tests/e2e/keyboard-navigation-kanban.spec.ts (Tab/Space/Arrow/Space pattern)
    - frontend/tests/e2e/kanban-a11y.spec.ts (axe-core wiring)
    - frontend/tests/e2e/drag-task-between-kanban-columns.spec.ts (alternate drag pattern)
    - .planning/phases/52-heroui-v3-kanban-migration/52-VALIDATION.md rows 52-XX-04 through 52-XX-09 and 52-XX-18
    - .planning/phases/52-heroui-v3-kanban-migration/52-UI-SPEC.md §"Accessibility Contract"
  </read_first>
  <action>
    For each of the six spec files, replace `test.fixme(...)` with `test(...)` and supply behavior assertions. Use SEEDED_ENGAGEMENT_ID from Task 1 in all goto targets.

    tasks-tab-dnd.spec.ts (KANBAN-01):
      Drive mouse drag from a card in `todo` to a card in `in_progress`, reload page, assert the moved card now appears in `in_progress`. Use the matrix from Plan 01 but parameterize iteratively only one viewport (1280×800 LTR) for the dnd happy-path — keyboard/touch parity gets its own spec. Pattern:
      ```typescript
      await page.goto(`/engagements/${SEEDED_ENGAGEMENT_ID}`)
      const card = page.getByRole('button', { name: /TASK .{8}/i }).first()
      const targetColumn = page.locator('[data-droppable-id="in_progress"]').first()
      await card.hover()
      await page.mouse.down()
      await targetColumn.hover()
      await page.mouse.up()
      // reload + re-query
      await page.reload()
      const movedCard = page.locator('[data-droppable-id="in_progress"]').locator('button', { hasText: <captured card text> })
      await expect(movedCard).toBeVisible()
      ```
      If `data-droppable-id` doesn't exist on the new primitive (the executor must verify), use the column header text + parent traversal instead.

    tasks-tab-keyboard.spec.ts (KANBAN-01 — keyboard parity):
      Tab to focus the first card; Space to pick up; ArrowRight (LTR) or ArrowLeft (RTL — note the directional flip); Space to drop. Reload, assert position. dnd-kit's KeyboardSensor announces moves via the `accessibility.announcements` API — assert the announcement text appears in `[role="status"]` or similar live-region locator.

    tasks-tab-a11y.spec.ts (KANBAN-04):
      Inject `@axe-core/playwright` (verify it's installed; if not, install it in this plan — `pnpm --filter frontend add -D @axe-core/playwright` is acceptable since this is the dedicated a11y wiring task), run `await new AxeBuilder({ page }).analyze()`, filter for serious+critical, assert zero violations.

    engagement-kanban-dialog-dnd.spec.ts (KANBAN-02):
      Same mouse-drag pattern but goto `/dossiers/engagements/${SEEDED_ENGAGEMENT_ID}`, then trigger the dialog via the button selector confirmed in 52-PATTERNS.md (`page.getByRole('button', { name: /kanban|board/i }).first().click()`), then drive the drag inside the dialog.

    engagement-kanban-dialog-keyboard.spec.ts (KANBAN-02): same as dialog dnd + keyboard handling.

    engagement-kanban-dialog-a11y.spec.ts (KANBAN-04): same axe pattern inside the dialog context.

    For ALL six specs:
      - Use `await page.waitForLoadState('networkidle')` after goto.
      - Use `await page.evaluate(() => document.fonts.ready)` before assertions that touch text.
      - For RTL test parameters, set `localStorage.setItem('i18nextLng', 'ar')` via `addInitScript` AND swap directional arrow keys.
      - Use `test.describe.parallel(...)` if multiple matrix iterations are written; for the dnd/keyboard specs, keep ONE iteration per surface to avoid CI cost explosion (visual specs already cover the full matrix).

    Run `pnpm --filter frontend exec playwright test tests/e2e/tasks-tab-{dnd,keyboard,a11y} tests/e2e/engagement-kanban-dialog-{dnd,keyboard,a11y}` after each spec is flipped; iteratively fix selector / timing issues until each spec passes.

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm exec playwright test tests/e2e/tasks-tab-dnd tests/e2e/tasks-tab-keyboard tests/e2e/tasks-tab-a11y tests/e2e/engagement-kanban-dialog-dnd tests/e2e/engagement-kanban-dialog-keyboard tests/e2e/engagement-kanban-dialog-a11y --reporter=list 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    Zero `test.fixme` in any of the six specs. All six pass on at least one viewport×direction matrix iteration. axe-core specs report zero serious/critical violations on both surfaces.
  </done>
</task>

<task type="auto">
  <name>Task 3: Generate 8 new visual baselines for both Kanban surfaces (EN+AR × 1280+768)</name>
  <files>
    frontend/tests/e2e/__screenshots__/tasks-tab-visual.spec.ts-snapshots/
    frontend/tests/e2e/__screenshots__/engagement-kanban-dialog-visual.spec.ts-snapshots/
  </files>
  <read_first>
    - frontend/tests/e2e/tasks-tab-visual.spec.ts (Plan 01 — fully real spec, no fixme)
    - frontend/tests/e2e/engagement-kanban-dialog-visual.spec.ts (Plan 01)
    - frontend/tests/e2e/kanban-visual.spec.ts (existing 4-baseline pattern — DO NOT regenerate; this is the regression anchor)
    - .planning/phases/52-heroui-v3-kanban-migration/52-CONTEXT.md D-14, D-15, D-17
    - .planning/phases/52-heroui-v3-kanban-migration/52-UI-SPEC.md §"Visual Baseline Matrix"
    - .planning/phases/52-heroui-v3-kanban-migration/52-RESEARCH.md §"Common Pitfalls" Pitfall 6 (font readiness) + Pitfall 8 (auth flow)
  </read_first>
  <action>
    Step 1 — confirm authentication setup:
      `.env.test` must exist with `TEST_USER_EMAIL` + `TEST_USER_PASSWORD` (per CLAUDE.md). If absent, surface this as a Plan 05 entry-block and request the user populate `.env.test` before proceeding. The Plan-01 spec files do NOT override `storageState` — they inherit `tests/e2e/global-setup.ts`. Verify by running `pnpm --filter frontend exec playwright test --list tests/e2e/tasks-tab-visual.spec.ts` and confirming the spec discovers normally.

    Step 2 — generate baselines:
      `cd frontend && pnpm exec playwright test tests/e2e/tasks-tab-visual.spec.ts tests/e2e/engagement-kanban-dialog-visual.spec.ts --update-snapshots --reporter=list 2>&1 | tail -40`
      This first-run command WRITES the 8 baseline PNGs under `frontend/tests/e2e/__screenshots__/<spec-file>-snapshots/<test-name>-<browser>.png`. Each spec produces 4 baselines (4 matrix iterations).

    Step 3 — stability replay:
      `cd frontend && pnpm exec playwright test tests/e2e/tasks-tab-visual.spec.ts tests/e2e/engagement-kanban-dialog-visual.spec.ts --reporter=list 2>&1 | tail -20`
      Re-running WITHOUT `--update-snapshots` must pass — the just-generated baselines should produce zero pixel drift on the same machine. If diffs appear, investigate:
        - Non-deterministic fonts (Pitfall 6) — ensure `document.fonts.ready` await is present
        - Animation timing — ensure addStyleTag killing animation/transition durations is present
        - Network flakiness — ensure `waitForLoadState('networkidle')` is awaited
      Re-run 3× total to confirm zero flake:
        `for i in 1 2 3; do pnpm --filter frontend exec playwright test tests/e2e/tasks-tab-visual tests/e2e/engagement-kanban-dialog-visual --reporter=list 2>&1 | tail -5; done`
      All three runs must report PASS for all 8 baselines.

    Step 4 — verify baseline file count:
      `find frontend/tests/e2e/__screenshots__/tasks-tab-visual.spec.ts-snapshots -name "*.png" | wc -l` returns 4.
      `find frontend/tests/e2e/__screenshots__/engagement-kanban-dialog-visual.spec.ts-snapshots -name "*.png" | wc -l` returns 4.

    Step 5 — DO NOT commit baselines yet — Task 4's human-verify checkpoint must approve them first.

  </action>
  <verify>
    <automated>find frontend/tests/e2e -name "*tasks-tab-visual*-snapshots" -o -name "*engagement-kanban-dialog-visual*-snapshots" 2>/dev/null | xargs -I {} find {} -name "*.png" 2>/dev/null | wc -l</automated>
  </verify>
  <done>
    8 PNG baselines exist on disk (4 per surface). Stability replay shows zero pixel drift across 3 runs. Files staged but NOT committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of 8 baselines + mid-drag DragOverlay parity</name>
  <files>
    frontend/tests/e2e/__screenshots__/tasks-tab-visual.spec.ts-snapshots/
    frontend/tests/e2e/__screenshots__/engagement-kanban-dialog-visual.spec.ts-snapshots/
  </files>
  <action>
    Human verification of 8 baseline PNGs (Task 3 output) + mid-drag DragOverlay parity (52-RESEARCH Open Question 3). See &lt;what-built&gt; for the full list of artifacts under review and &lt;how-to-verify&gt; for the step-by-step inspection protocol. The executor PAUSES here until the user provides the resume-signal. No file mutations occur in this task — it gates the commit step in Task 5.
  </action>
  <what-built>
    Two new Playwright visual specs each producing 4 baseline PNGs:
      - frontend/tests/e2e/__screenshots__/tasks-tab-visual.spec.ts-snapshots/
        - tasks-tab-ltr-1280.png
        - tasks-tab-ltr-768.png
        - tasks-tab-rtl-1280.png
        - tasks-tab-rtl-768.png
      - frontend/tests/e2e/__screenshots__/engagement-kanban-dialog-visual.spec.ts-snapshots/
        - engagement-kanban-dialog-ltr-1280.png
        - engagement-kanban-dialog-ltr-768.png
        - engagement-kanban-dialog-rtl-1280.png
        - engagement-kanban-dialog-rtl-768.png

    The migration intentionally changes some visual surface characteristics from the pre-migration kibo-ui rendering:
      - STAGE_COLORS stage-specific column tints are GONE — all columns are now flat `bg-muted/30 border-muted` (D-05/D-06)
      - Cancelled column adds a `border-danger/30` 1px outline (D-07)
      - Card hover shadow is replaced by `bg-surface-raised` + `border-line-soft` (D-09/D-10)
      - KanbanTaskCard SLA colors are migrated from Tailwind palette (red-600/orange-600/yellow-600/emerald-600) to semantic tokens (danger/warn/ok) IF Plan 03 Task 3 absorbed those disables (otherwise unchanged)
    These changes ARE intentional and on-spec per UI-SPEC §"Color". Reviewer judgment is needed to confirm no UNINTENTIONAL drift occurred (e.g., RTL flow broke, fonts loaded wrong, Tajawal cascade missing, dialog title rendering off).

    The mid-drag DragOverlay (52-RESEARCH Open Question 3) is NOT captured in the static baseline; pickup-and-mid-drag visual parity is a manual check on this checkpoint.

  </what-built>
  <how-to-verify>
    1. Pull or check out the branch with the new baselines.
    2. Open each of the 8 PNG files (`frontend/tests/e2e/__screenshots__/tasks-tab-visual.spec.ts-snapshots/*.png` and `frontend/tests/e2e/__screenshots__/engagement-kanban-dialog-visual.spec.ts-snapshots/*.png`) in an image viewer.
    3. For each baseline, verify:
       - Five columns visible (To do / In progress / Review / Done / Cancelled)
       - Cancelled column shows a 1px terracotta-tinted border outline (NOT a body tint)
       - All other columns share the same flat muted background
       - Card hover state NOT visible (static snapshot; hover only fires under pointer)
       - SLA chip colors are restrained warm tones (danger/warn/ok) — NOT bright Tailwind red/yellow/green
       - Arabic baselines (`*-rtl-*.png`): text reads right-to-left; Tajawal font is applied; column order visually reversed relative to LTR
       - Mobile baselines (`*-768.png`): mobile-stacked accordion sections visible (NOT 5-column horizontal scroll)
       - No emoji or marketing voice in any visible copy
       - Border colors resolve to terracotta family (danger) on cancelled, neutral muted on others — NOT bright Tailwind colors
    4. Mid-drag DragOverlay manual check (52-RESEARCH Open Question 3):
       - `cd frontend && pnpm dev` (or use the deployed staging instance)
       - Navigate to `/engagements/&lt;fixture-engagement-id&gt;` (use the UUID from Task 1)
       - Drag a card halfway between two columns; hold the mouse button
       - Take a screenshot of the mid-drag state
       - Confirm: the dragged-card overlay is visible at the pointer position, shows a `ring-2 ring-accent` (terracotta) outline, and the source card at original position shows `opacity-30`
       - Repeat for `EngagementKanbanDialog`: navigate to `/dossiers/engagements/&lt;fixture-engagement-id&gt;`, click the Kanban dialog trigger, drag a card mid-drag, screenshot
    5. If a visual is correct, approve. If any visual is wrong, describe the issue (which file, what's wrong) and reject.
  </how-to-verify>
  <verify>
    <human-check>Human reviewer confirms all 8 baseline PNGs render correctly per &lt;how-to-verify&gt; and that mid-drag DragOverlay parity matches the pre-migration kibo-ui rendering. No automated command can substitute for this — the entire purpose of this checkpoint is the human pixel-level review.</human-check>
  </verify>
  <done>
    User has provided the resume-signal ("approved" — phase proceeds to Task 5; or "reject: ..." — execution rolls back the relevant artifact and triggers CONTEXT D-02 escape hatch or KanbanProvider DragOverlay slot strategy investigation per 52-RESEARCH Open Question 3).
  </done>
  <resume-signal>
    Type "approved" if all 8 baselines + 2 mid-drag screenshots look correct.
    Type "reject: &lt;description&gt;" if any baseline or mid-drag rendering is wrong — execution will rollback the relevant artifact and re-investigate (likely fallback: re-add tunnel-rat per CONTEXT D-02 escape hatch, or fix DragOverlay slot strategy in KanbanProvider.tsx).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 5: Run the 12-spec regression anchor + commit baselines + write 52-SUMMARY.md</name>
  <files>
    .planning/phases/52-heroui-v3-kanban-migration/52-VALIDATION.md
    .planning/phases/52-heroui-v3-kanban-migration/52-SUMMARY.md
  </files>
  <read_first>
    - frontend/tests/e2e/kanban-*.spec.ts (verify discovery)
    - .planning/phases/52-heroui-v3-kanban-migration/52-CONTEXT.md D-17
    - .planning/phases/52-heroui-v3-kanban-migration/52-VALIDATION.md (all 19 status-column rows must flip to ✅)
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY.md format)
    - .planning/phases/51-design-token-compliance-gate/51-SUMMARY.md (for verdict-format precedent)
  </read_first>
  <action>
    Step 1 — regression anchor (D-17):
      `cd frontend && pnpm exec playwright test tests/e2e/kanban-a11y.spec.ts tests/e2e/kanban-dnd.spec.ts tests/e2e/kanban-filters.spec.ts tests/e2e/kanban-render.spec.ts tests/e2e/kanban-responsive.spec.ts tests/e2e/kanban-rtl.spec.ts tests/e2e/kanban-search.spec.ts tests/e2e/kanban-visual.spec.ts tests/e2e/keyboard-navigation-kanban.spec.ts tests/e2e/drag-task-between-kanban-columns.spec.ts tests/e2e/open-kanban-board.spec.ts tests/e2e/realtime-kanban-updates-two-windows.spec.ts tests/e2e/performance/kanban-drag-drop-latency.spec.ts --reporter=list 2>&1 | tail -30`
      All 12 specs (= 12 file paths above; the count comes from CONTEXT.md D-17 enumeration; if any are missing in the codebase, document the gap in the SUMMARY).
      Expected: all green. They target `/kanban` → WorkBoard, which Phase 52 did NOT touch — the only regression vector is shared `@dnd-kit/core` version stability (unchanged, verified in Plan 02). If any spec turns red, investigate before declaring done.

    Step 2 — commit baselines:
      `git add frontend/tests/e2e/__screenshots__/tasks-tab-visual.spec.ts-snapshots/ frontend/tests/e2e/__screenshots__/engagement-kanban-dialog-visual.spec.ts-snapshots/`
      `git commit -m "test(52): regenerate Kanban visual baselines for new primitive — EN+AR × 1280+768"` (do NOT actually commit yet — let the overall GSD `git_commit` step at end of plan handle it; just stage the files).

    Step 3 — flip 52-VALIDATION.md status column:
      Open `.planning/phases/52-heroui-v3-kanban-migration/52-VALIDATION.md`. Update the Per-Task Verification Map: all 19 rows should now have:
        - `Plan` column filled in (52-01 / 52-02 / 52-03 / 52-04 / 52-05) per the table below
        - `File Exists` column ✅
        - `Status` column ✅ green (assuming all prior plans + this plan's tasks completed)
      Plan-to-row mapping:
        - Rows 52-XX-01 through 52-XX-03 → Plan 52-02 (KanbanProvider/Board/Card unit tests turning GREEN)
        - Rows 52-XX-04 through 52-XX-09 → Plan 52-05 Task 2 (flipped fixmes)
        - Rows 52-XX-10 through 52-XX-15 → Plan 52-04 (deletion + ESLint + type-check)
        - Rows 52-XX-16 through 52-XX-19 → Plan 52-05 Tasks 3 + 5 (baselines + regression anchor)
      Also flip frontmatter `nyquist_compliant: true` and `wave_0_complete: true`.

    Step 4 — write 52-SUMMARY.md:
      Use the GSD summary template structure (see `$HOME/.claude/get-shit-done/templates/summary.md`). Required sections:
        - Frontmatter: phase, status (`shipped`), verdict (`PASS` or `PASS-WITH-DEVIATION` depending on Plan 03 Task 3 outcome / Pitfall 7 outcome / any baseline approval-rejections), date
        - §1 Phase verdict (single sentence: "Phase 52 ships HeroUI v3 Kanban migration with full behavior parity; kibo-ui removed; tunnel-rat removed; 8 new baselines committed; 12-spec regression anchor green.")
        - §2 KANBAN-01 through KANBAN-04 closure (one paragraph each, with file paths + commit hashes + verification evidence)
        - §3 Tier-C disable accounting (before/after; 9 → 0 or 9 → 4)
        - §4 Deviations (any departures from CONTEXT/UI-SPEC/PATTERNS — e.g., warning/warn token consolidation, Pitfall 7 outcome, baseline regeneration anomalies)
        - §5 Open follow-ups (e.g., Phase 53 bundle ceiling recalibration after tunnel-rat removal; future KanbanTaskCard polish if deferred)
        - §6 Inheritance to Phase 53 (the chunk-composition shift after tunnel-rat removal is Phase 53's input — flag the relevant `frontend/package.json` diff)

    Step 5 — final workspace gate:
      `pnpm --filter frontend lint && pnpm --filter frontend type-check && pnpm --filter frontend build && bash scripts/check-deleted-components.sh` — all must exit 0 cumulatively.

    Step 6 — STATE.md update (light touch — minimal mutation):
      Open `.planning/STATE.md`. Append a `### Phase 52 summary` table row (mirroring the Phase 51 close-summary section) capturing wave/plan completion and the SHIPPED verdict. Update the `Phase 52` row in the `v6.3 Phase Map` table from `Not started — —` to `5/5 — Shipped — 2026-05-16` (or actual date).

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm exec playwright test tests/e2e/kanban-visual tests/e2e/kanban-a11y tests/e2e/kanban-dnd tests/e2e/kanban-rtl --reporter=list 2>&amp;1 | tail -10 &amp;&amp; test -f .planning/phases/52-heroui-v3-kanban-migration/52-SUMMARY.md &amp;&amp; grep -c "PASS\|SHIPPED\|KANBAN-0[1-4]" .planning/phases/52-heroui-v3-kanban-migration/52-SUMMARY.md</automated>
  </verify>
  <done>
    12 existing kanban-*.spec.ts files all green. 8 new baselines staged for commit. 52-VALIDATION.md status column flipped to ✅ across all 19 rows; `nyquist_compliant: true` set. 52-SUMMARY.md exists with §1-6 sections + verdict. Final workspace lint+type-check+build+CI-gate all exit 0. STATE.md Phase 52 row reflects SHIPPED.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                                    |
| ------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| Staging Supabase ↔ Playwright spec    | Fixture engagement composition must remain stable across runs; non-deterministic seed = flaky visual baselines |
| Playwright runtime ↔ pre-commit hooks | Baseline PNG generation must not be eaten by lint/format hooks                                                 |
| Human reviewer ↔ baseline approval    | The only non-automatable gate; phase cannot ship without explicit approval                                     |

## STRIDE Threat Register

| Threat ID | Category    | Component                                                        | Disposition | Mitigation Plan                                                                                                                                                                                                                                               |
| --------- | ----------- | ---------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-52-17   | Tampering   | Fixture engagement composition drift                             | mitigate    | Task 1 either reuses existing engagement (verified at composition match) OR seeds idempotently with ON CONFLICT preserving counts. 52-FIXTURE.md documents the contract; future schema changes must keep the composition intact                               |
| T-52-18   | Spoofing    | Baseline screenshots committed without human review              | mitigate    | Task 3 explicitly does NOT commit; Task 4 checkpoint:human-verify is `gate="blocking"`; only Task 5 commits AFTER approval                                                                                                                                    |
| T-52-19   | Tampering   | DragOverlay mid-drag visual parity (52-RESEARCH Open Question 3) | mitigate    | Task 4 includes explicit mid-drag manual verification step; reject path triggers CONTEXT D-02 fallback (re-add tunnel-rat) — escape hatch is documented                                                                                                       |
| T-52-20   | Repudiation | 52-VALIDATION.md status drift                                    | mitigate    | Task 5 Step 3 explicitly maps each of 19 rows to its closing plan; final state ✅ across all rows with nyquist_compliant: true                                                                                                                                |
| T-52-SC   | Tampering   | New a11y dependency (@axe-core/playwright)                       | mitigate    | Task 2 only installs @axe-core/playwright if it's not already present; package legitimacy verified per 52-RESEARCH (3M+ weekly downloads, official @axe-core npm scope, used by Phase 39 kanban-a11y.spec.ts already — same package). Anchors V1 Architecture |

</threat_model>

<verification>
After all five tasks complete:

1. 8 PNG baselines exist: `find frontend/tests/e2e/__screenshots__/{tasks-tab,engagement-kanban-dialog}-visual.spec.ts-snapshots -name "*.png" 2>/dev/null | wc -l` returns 8.
2. Stability: re-running visual specs without `--update-snapshots` 3× returns PASS each time.
3. 6 flipped specs: `grep -c "test.fixme" frontend/tests/e2e/tasks-tab-{dnd,keyboard,a11y}.spec.ts frontend/tests/e2e/engagement-kanban-dialog-{dnd,keyboard,a11y}.spec.ts | awk -F: '{sum+=$2} END {print sum}'` returns 0.
4. 12 regression-anchor specs green: `pnpm --filter frontend exec playwright test tests/e2e/kanban-{a11y,dnd,filters,render,responsive,rtl,search,visual} tests/e2e/keyboard-navigation-kanban tests/e2e/drag-task-between-kanban-columns tests/e2e/open-kanban-board tests/e2e/realtime-kanban-updates-two-windows tests/e2e/performance/kanban-drag-drop-latency --reporter=list 2>&1 | grep -c "passed"` returns ≥ 12.
5. 52-SUMMARY.md exists with all six required sections.
6. 52-VALIDATION.md: `grep -c "✅" .planning/phases/52-heroui-v3-kanban-migration/52-VALIDATION.md` returns ≥ 19. `grep "nyquist_compliant: true" .planning/phases/52-heroui-v3-kanban-migration/52-VALIDATION.md` matches.
7. Workspace gate cumulative: `pnpm --filter frontend lint && pnpm --filter frontend type-check && pnpm --filter frontend build && bash scripts/check-deleted-components.sh` all exit 0.
8. Human approval received via Task 4 checkpoint.
   </verification>

<success_criteria>

- 8 new visual baselines created, human-approved, committed to git.
- 6 Wave-0 spec stubs flipped from `test.fixme` to real assertions; all 6 pass.
- 12 existing kanban-\*.spec.ts regression-anchor specs green.
- 52-VALIDATION.md status column flipped to ✅ across all 19 rows; `nyquist_compliant: true`.
- 52-SUMMARY.md written with phase verdict.
- STATE.md Phase 52 row reflects SHIPPED status.
- Workspace lint + type-check + build + CI-gate all exit 0.
- Human checkpoint:human-verify (Task 4) approved.
  </success_criteria>

<output>
Create `.planning/phases/52-heroui-v3-kanban-migration/52-05-SUMMARY.md` documenting:
- 8 baseline PNGs with file paths + sizes
- Mid-drag DragOverlay manual verification outcome (approved / fallback to D-02 if rejected)
- 6 flipped specs with test-pass counts
- 12-spec regression anchor results
- 52-VALIDATION.md status delta (0/19 → 19/19 ✅)
- 52-SUMMARY.md filing reference
- STATE.md mutation summary
- Phase 52 final verdict (PASS / PASS-WITH-DEVIATION) with all deviations from prior plans aggregated
</output>
