---
phase: 17-seed-data-first-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260407000001_add_is_seed_data_columns.sql
autonomous: true
requirements: [SEED-01, SEED-03]
must_haves:
  truths:
    - 'Every seeded table has an is_seed_data BOOLEAN NOT NULL DEFAULT false column'
    - 'Idempotency check on is_seed_data is index-backed and fast'
  artifacts:
    - path: supabase/migrations/20260407000001_add_is_seed_data_columns.sql
      provides: 'Schema tagging for seed identification (D-14)'
  key_links:
    - from: 'is_seed_data column'
      to: 'populate_diplomatic_seed RPC idempotency check (Plan 17-02)'
      via: 'EXISTS query in RPC'
---

<objective>
Add `is_seed_data BOOLEAN NOT NULL DEFAULT false` to every table that Phase 17 will populate, with partial indexes to support fast idempotency checks.

Purpose: Enables D-13 (idempotent on empty only) and D-14 (tag-based identification) without polluting production rows.
Output: A single applied migration covering all seeded tables.
</objective>

<context>
@.planning/phases/17-seed-data-first-run/17-CONTEXT.md
@.planning/phases/17-seed-data-first-run/17-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Locate work_items canonical CREATE and finalize table list</name>
  <files>(read-only investigation)</files>
  <action>
    Grep `supabase/migrations/` for `CREATE TABLE.*work_items` (and `CREATE TABLE.*public\.work_items`) to confirm the canonical CREATE file. Also confirm there is no separate `dossier_relationships` table (per RESEARCH §2 open question 3) — if one exists, include it in the column list. Document findings as a comment block at the top of the migration file.

    Final canonical list (per RESEARCH §2):
    - dossiers
    - countries
    - organizations
    - forums
    - engagements
    - working_groups
    - persons (covers former elected_officials)
    - topics
    - work_items
    - work_item_dossiers
    - any dossier_relationships table if found

  </action>
  <verify>
    <automated>grep -rn "CREATE TABLE.*work_items" supabase/migrations/ | head -5</automated>
  </verify>
  <done>Canonical CREATE for work_items located; final table list documented in migration header comment.</done>
</task>

<task type="auto">
  <name>Task 2: Write column-add migration with partial indexes</name>
  <files>supabase/migrations/20260407000001_add_is_seed_data_columns.sql</files>
  <action>
    Create migration with header comment listing all tables and rationale (Phase 17, D-14). For each table in the finalized list, emit:

    ```sql
    ALTER TABLE public.{table}
      ADD COLUMN IF NOT EXISTS is_seed_data BOOLEAN NOT NULL DEFAULT false;

    CREATE INDEX IF NOT EXISTS idx_{table}_is_seed_data
      ON public.{table} (is_seed_data)
      WHERE is_seed_data = true;
    ```

    Use `IF NOT EXISTS` on both column and index to keep the migration safely re-runnable in dev. Wrap the entire migration in a single transaction (BEGIN/COMMIT).

    Add a final `COMMENT ON COLUMN public.{table}.is_seed_data IS 'Phase 17: marks rows inserted by populate_diplomatic_seed RPC for idempotency.';` for each table.

  </action>
  <verify>
    <automated>test -f supabase/migrations/20260407000001_add_is_seed_data_columns.sql &amp;&amp; grep -c "ADD COLUMN IF NOT EXISTS is_seed_data" supabase/migrations/20260407000001_add_is_seed_data_columns.sql</automated>
  </verify>
  <done>Migration file exists with one ALTER + one partial index per seeded table, all wrapped in a transaction.</done>
</task>

<task type="auto">
  <name>Task 3: Apply migration via Supabase MCP</name>
  <files>(applied to staging Supabase project zkrcjzdemdmwhearhfgg)</files>
  <action>
    Apply the migration using the Supabase MCP `apply_migration` tool against project `zkrcjzdemdmwhearhfgg` (per CLAUDE.md global rule — never run psql directly). After apply, query `information_schema.columns` via Supabase MCP to confirm `is_seed_data` exists on every table in the list.
  </action>
  <verify>
    <automated>echo "Verify via Supabase MCP: SELECT table_name FROM information_schema.columns WHERE column_name='is_seed_data' AND table_schema='public' ORDER BY table_name;"</automated>
  </verify>
  <done>Migration applied; verification query returns one row per seeded table.</done>
</task>

</tasks>

<verification>
- All seeded tables have `is_seed_data` column with `NOT NULL DEFAULT false`.
- Partial index exists on each (`WHERE is_seed_data = true`).
- Migration applied to staging via Supabase MCP without errors.
- No existing rows mutated (DEFAULT false applies to new column on existing data).
</verification>

<success_criteria>

- SEED-03 (idempotency infrastructure) prerequisite satisfied.
- Plan 17-02 can use `EXISTS (SELECT 1 FROM countries WHERE is_seed_data = true)` as its idempotency gate.
  </success_criteria>

<output>
After completion, create `.planning/phases/17-seed-data-first-run/17-01-SUMMARY.md`.
</output>
