---
phase: 06-architecture-consolidation
plan: 03
type: execute
wave: 2
depends_on: ['06-01']
files_modified:
  - frontend/src/domains/work-items/repositories/work-items.repository.ts
  - frontend/src/domains/work-items/hooks/useWorkItemDossierLinks.ts
  - frontend/src/domains/work-items/hooks/useWorkflowAutomation.ts
  - frontend/src/domains/work-items/hooks/useSLAMonitoring.ts
  - frontend/src/domains/work-items/hooks/useUpdateSuggestionAction.ts
  - frontend/src/domains/work-items/types/index.ts
  - frontend/src/domains/work-items/index.ts
  - frontend/src/domains/relationships/repositories/relationships.repository.ts
  - frontend/src/domains/relationships/hooks/useRelationships.ts
  - frontend/src/domains/relationships/hooks/useCreateRelationship.ts
  - frontend/src/domains/relationships/types/index.ts
  - frontend/src/domains/relationships/index.ts
  - frontend/src/domains/documents/repositories/documents.repository.ts
  - frontend/src/domains/documents/hooks/useDocuments.ts
  - frontend/src/domains/documents/hooks/useExportData.ts
  - frontend/src/domains/documents/types/index.ts
  - frontend/src/domains/documents/index.ts
  - frontend/src/domains/persons/repositories/persons.repository.ts
  - frontend/src/domains/persons/hooks/usePersons.ts
  - frontend/src/domains/persons/types/index.ts
  - frontend/src/domains/persons/index.ts
  - frontend/src/domains/topics/repositories/topics.repository.ts
  - frontend/src/domains/topics/hooks/useTopics.ts
  - frontend/src/domains/topics/types/index.ts
  - frontend/src/domains/topics/index.ts
autonomous: true
requirements:
  - ARCH-02
  - ARCH-04

must_haves:
  truths:
    - 'Work-items domain has repository + 4 hooks migrated'
    - 'Relationships domain has repository + 2 hooks migrated'
    - 'Documents domain has repository + 2 hooks migrated'
    - 'Persons domain has repository + 1 hook migrated'
    - 'Topics domain has repository + 1 hook migrated'
    - 'All 10 hooks route through domain repos, not direct fetch()'
  artifacts:
    - path: 'frontend/src/domains/work-items/repositories/work-items.repository.ts'
      provides: 'Work item API operations'
      exports:
        [
          'getWorkItemDossierLinks',
          'getWorkflowAutomation',
          'getSLAMonitoring',
          'updateSuggestionAction',
        ]
    - path: 'frontend/src/domains/relationships/repositories/relationships.repository.ts'
      provides: 'Relationship API operations'
      exports: ['getRelationships', 'createRelationship']
    - path: 'frontend/src/domains/documents/repositories/documents.repository.ts'
      provides: 'Document API operations'
      exports: ['getDocuments', 'exportData']
    - path: 'frontend/src/domains/persons/repositories/persons.repository.ts'
      provides: 'Person API operations'
      exports: ['getPersons']
    - path: 'frontend/src/domains/topics/repositories/topics.repository.ts'
      provides: 'Topic API operations'
      exports: ['getTopics']
  key_links:
    - from: 'frontend/src/domains/*/hooks/*.ts'
      to: 'frontend/src/domains/*/repositories/*.ts'
      via: 'import from ../repositories/'
      pattern: "Repo\\."
    - from: 'frontend/src/domains/*/repositories/*.ts'
      to: 'frontend/src/lib/api-client.ts'
      via: 'import { apiGet, apiPost }'
      pattern: 'apiGet|apiPost'
---

<objective>
Migrate 5 smaller domains (work-items, relationships, documents, persons, topics) totaling 10 hooks to the repository pattern.

Purpose: Clears the smaller domains in one plan. These are independent of Plan 02 and can run in parallel (same wave).
Output: Five complete domain directories with repos, hooks, types, barrels, and re-exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-architecture-consolidation/06-CONTEXT.md
@.planning/phases/06-architecture-consolidation/06-RESEARCH.md
@.planning/phases/06-architecture-consolidation/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate work-items (4) and relationships (2) domains</name>
  <files>
    frontend/src/domains/work-items/repositories/work-items.repository.ts,
    frontend/src/domains/work-items/hooks/useWorkItemDossierLinks.ts,
    frontend/src/domains/work-items/hooks/useWorkflowAutomation.ts,
    frontend/src/domains/work-items/hooks/useSLAMonitoring.ts,
    frontend/src/domains/work-items/hooks/useUpdateSuggestionAction.ts,
    frontend/src/domains/work-items/types/index.ts,
    frontend/src/domains/work-items/index.ts,
    frontend/src/domains/relationships/repositories/relationships.repository.ts,
    frontend/src/domains/relationships/hooks/useRelationships.ts,
    frontend/src/domains/relationships/hooks/useCreateRelationship.ts,
    frontend/src/domains/relationships/types/index.ts,
    frontend/src/domains/relationships/index.ts
  </files>
  <read_first>
    frontend/src/hooks/useWorkItemDossierLinks.ts,
    frontend/src/hooks/useWorkflowAutomation.ts,
    frontend/src/hooks/useSLAMonitoring.ts,
    frontend/src/hooks/useUpdateSuggestionAction.ts,
    frontend/src/hooks/useRelationships.ts,
    frontend/src/hooks/useCreateRelationship.ts,
    frontend/src/lib/api-client.ts,
    frontend/src/domains/dossiers/repositories/dossiers.repository.ts
  </read_first>
  <action>
    Apply the established domain migration pattern for two domains:

    **Work-items domain (4 hooks):**
    1. Types: Extract from 4 source hooks into `domains/work-items/types/index.ts`
    2. Repository (`work-items.repository.ts`): Plain function exports — `getWorkItemDossierLinks(workItemId)`, `getWorkflowAutomation(params)`, `getSLAMonitoring(params)`, `updateSuggestionAction(id, data)`. Read source hooks for exact endpoints and params.
    3. Hooks: 4 files in `domains/work-items/hooks/`, each importing from repo
    4. Barrel: `domains/work-items/index.ts`
    5. Re-exports: Replace 4 files in `frontend/src/hooks/`

    **Relationships domain (2 hooks):**
    1. Types: Extract into `domains/relationships/types/index.ts`
    2. Repository (`relationships.repository.ts`): `getRelationships(filters)`, `createRelationship(data)`. Read source hooks for exact endpoints.
    3. Hooks: 2 files in `domains/relationships/hooks/`
    4. Barrel: `domains/relationships/index.ts`
    5. Re-exports: Replace 2 files in `frontend/src/hooks/`

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && grep -c "fetch(" frontend/src/domains/work-items/hooks/*.ts frontend/src/domains/relationships/hooks/*.ts 2>/dev/null | grep -v ":0" || echo "No raw fetch - PASS"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/domains/work-items/repositories/work-items.repository.ts exists and contains `import { apiGet` from `@/lib/api-client`
    - frontend/src/domains/work-items/hooks/useWorkItemDossierLinks.ts does NOT contain `fetch(`
    - frontend/src/domains/work-items/index.ts exists
    - frontend/src/domains/relationships/repositories/relationships.repository.ts exists
    - frontend/src/domains/relationships/hooks/useRelationships.ts does NOT contain `fetch(`
    - frontend/src/domains/relationships/index.ts exists
    - frontend/src/hooks/useWorkItemDossierLinks.ts contains `from '@/domains/work-items'`
    - frontend/src/hooks/useRelationships.ts contains `from '@/domains/relationships'`
  </acceptance_criteria>
  <done>6 hooks across work-items and relationships domains migrated with zero raw fetch() and re-exports</done>
</task>

<task type="auto">
  <name>Task 2: Migrate documents (2), persons (1), and topics (1) domains</name>
  <files>
    frontend/src/domains/documents/repositories/documents.repository.ts,
    frontend/src/domains/documents/hooks/useDocuments.ts,
    frontend/src/domains/documents/hooks/useExportData.ts,
    frontend/src/domains/documents/types/index.ts,
    frontend/src/domains/documents/index.ts,
    frontend/src/domains/persons/repositories/persons.repository.ts,
    frontend/src/domains/persons/hooks/usePersons.ts,
    frontend/src/domains/persons/types/index.ts,
    frontend/src/domains/persons/index.ts,
    frontend/src/domains/topics/repositories/topics.repository.ts,
    frontend/src/domains/topics/hooks/useTopics.ts,
    frontend/src/domains/topics/types/index.ts,
    frontend/src/domains/topics/index.ts
  </files>
  <read_first>
    frontend/src/hooks/useDocuments.ts,
    frontend/src/hooks/useExportData.ts,
    frontend/src/hooks/usePersons.ts,
    frontend/src/hooks/useTopics.ts,
    frontend/src/lib/api-client.ts,
    frontend/src/domains/dossiers/repositories/dossiers.repository.ts
  </read_first>
  <action>
    **Documents domain (2 hooks):**
    1. Types + Repository: `getDocuments(filters)`, `exportData(params)` in `documents.repository.ts`
    2. Hooks: 2 files, barrel, re-exports

    **Persons domain (1 hook):**
    1. Types + Repository: `getPersons(filters)` in `persons.repository.ts`
    2. Hook: 1 file, barrel, re-export

    **Topics domain (1 hook):**
    1. Types + Repository: `getTopics(filters)` in `topics.repository.ts`
    2. Hook: 1 file, barrel, re-export

    After all three, run `pnpm --filter frontend tsc --noEmit` to verify.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && pnpm --filter frontend tsc --noEmit 2>&1 | tail -5 && grep -c "fetch(" frontend/src/domains/documents/hooks/*.ts frontend/src/domains/persons/hooks/*.ts frontend/src/domains/topics/hooks/*.ts 2>/dev/null | grep -v ":0" || echo "No raw fetch - PASS"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/domains/documents/repositories/documents.repository.ts exists
    - frontend/src/domains/documents/hooks/useDocuments.ts does NOT contain `fetch(`
    - frontend/src/domains/persons/repositories/persons.repository.ts exists
    - frontend/src/domains/persons/hooks/usePersons.ts does NOT contain `fetch(`
    - frontend/src/domains/topics/repositories/topics.repository.ts exists
    - frontend/src/domains/topics/hooks/useTopics.ts does NOT contain `fetch(`
    - frontend/src/hooks/useDocuments.ts contains `from '@/domains/documents'`
    - frontend/src/hooks/usePersons.ts contains `from '@/domains/persons'`
    - frontend/src/hooks/useTopics.ts contains `from '@/domains/topics'`
    - `pnpm --filter frontend tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>4 hooks across documents, persons, and topics migrated, TypeScript clean, all re-exports in place</done>
</task>

</tasks>

<verification>
- `pnpm --filter frontend tsc --noEmit` passes
- 5 domain directories created: work-items/, relationships/, documents/, persons/, topics/
- `grep -rn "fetch(" frontend/src/domains/{work-items,relationships,documents,persons,topics}/hooks/` returns nothing
- 10 re-export files in `frontend/src/hooks/` point to respective `@/domains/*`
</verification>

<success_criteria>

- 5 complete domain directories
- 10 hooks migrated with zero raw fetch()
- All backward-compat re-exports working
- TypeScript compilation clean
  </success_criteria>

<output>
After completion, create `.planning/phases/06-architecture-consolidation/06-03-SUMMARY.md`
</output>
