---
phase: 06-architecture-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/api-client.ts
  - frontend/src/lib/__tests__/api-client.test.ts
  - frontend/src/domains/dossiers/repositories/dossiers.repository.ts
  - frontend/src/domains/dossiers/hooks/useDossiers.ts
  - frontend/src/domains/dossiers/hooks/useDossier.ts
  - frontend/src/domains/dossiers/hooks/useDossierActivityTimeline.ts
  - frontend/src/domains/dossiers/hooks/useDossierRecommendations.ts
  - frontend/src/domains/dossiers/hooks/useDossierFirstSearch.ts
  - frontend/src/domains/dossiers/hooks/useQuickSwitcherSearch.ts
  - frontend/src/domains/dossiers/hooks/useNoResultsSuggestions.ts
  - frontend/src/domains/dossiers/types/index.ts
  - frontend/src/domains/dossiers/index.ts
  - frontend/src/hooks/useDossiers.ts
  - frontend/src/hooks/useDossier.ts
  - frontend/src/hooks/useDossierActivityTimeline.ts
  - frontend/src/hooks/useDossierRecommendations.ts
  - frontend/src/hooks/useDossierFirstSearch.ts
  - frontend/src/hooks/useQuickSwitcherSearch.ts
  - frontend/src/hooks/useNoResultsSuggestions.ts
  - frontend/src/components/layout/MainLayout.tsx
  - frontend/src/hooks/useMobile.tsx
autonomous: true
requirements:
  - ARCH-02
  - ARCH-04

must_haves:
  truths:
    - 'apiClient centralizes auth headers, base URL, error handling, and JSON parsing for all API calls'
    - 'Dossiers domain has repository + hooks + types in frontend/src/domains/dossiers/'
    - 'All 7 dossier hooks route through dossiers.repository.ts instead of raw fetch()'
    - 'Old hook import paths still work via re-exports'
    - 'Deprecated MainLayout and useMobile files are removed'
  artifacts:
    - path: 'frontend/src/lib/api-client.ts'
      provides: 'Shared API client with apiGet, apiPost, apiPut, apiPatch, apiDelete'
      exports: ['apiGet', 'apiPost', 'apiPut', 'apiPatch', 'apiDelete']
    - path: 'frontend/src/lib/__tests__/api-client.test.ts'
      provides: 'Unit tests for apiClient auth, error handling, JSON parsing'
      min_lines: 50
    - path: 'frontend/src/domains/dossiers/repositories/dossiers.repository.ts'
      provides: 'Plain function exports for all dossier API operations'
      exports:
        [
          'getDossiers',
          'getDossier',
          'getDossierActivityTimeline',
          'getDossierRecommendations',
          'getDossierFirstSearch',
          'getQuickSwitcherSearch',
          'getNoResultsSuggestions',
        ]
    - path: 'frontend/src/domains/dossiers/index.ts'
      provides: 'Barrel re-export for dossiers domain'
  key_links:
    - from: 'frontend/src/domains/dossiers/hooks/useDossiers.ts'
      to: 'frontend/src/domains/dossiers/repositories/dossiers.repository.ts'
      via: 'import * as dossiersRepo'
      pattern: "dossiersRepo\\."
    - from: 'frontend/src/domains/dossiers/repositories/dossiers.repository.ts'
      to: 'frontend/src/lib/api-client.ts'
      via: 'import { apiGet, apiPost }'
      pattern: 'apiGet|apiPost'
    - from: 'frontend/src/hooks/useDossiers.ts'
      to: 'frontend/src/domains/dossiers/index.ts'
      via: 're-export for backward compat'
      pattern: 'export.*from.*@/domains/dossiers'
---

<objective>
Create the shared apiClient, migrate the dossiers domain (7 hooks) as the canonical pattern, and clean up Phase 5 deprecated files.

Purpose: Establishes the foundation that all subsequent domain migrations follow. The apiClient is the single dependency for all repos. Dossiers domain serves as the reference implementation.
Output: Working apiClient with tests, complete dossiers domain (repo + hooks + types + barrel), backward-compat re-exports at old paths, deprecated files removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-architecture-consolidation/06-CONTEXT.md
@.planning/phases/06-architecture-consolidation/06-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared apiClient with unit tests and delete deprecated files</name>
  <files>
    frontend/src/lib/api-client.ts,
    frontend/src/lib/__tests__/api-client.test.ts,
    frontend/src/components/layout/MainLayout.tsx,
    frontend/src/hooks/useMobile.tsx
  </files>
  <read_first>
    frontend/src/hooks/useDossiers.ts,
    frontend/src/lib/supabase.ts,
    frontend/src/components/layout/MainLayout.tsx,
    frontend/src/hooks/useMobile.tsx,
    frontend/vite.config.ts,
    frontend/vitest.config.ts
  </read_first>
  <behavior>
    - Test 1: apiGet sends GET request with Authorization header containing session token
    - Test 2: apiPost sends POST request with JSON-stringified body and Content-Type application/json
    - Test 3: apiGet throws Error with status code and statusText when response.ok is false
    - Test 4: apiGet uses Edge Functions base URL by default (VITE_SUPABASE_URL + '/functions/v1')
    - Test 5: apiGet uses Express base URL when options.baseUrl === 'express' (VITE_API_URL)
    - Test 6: apiPut, apiPatch, apiDelete use correct HTTP methods
  </behavior>
  <action>
    **Create `frontend/src/lib/api-client.ts`** (per D-03):

    Import `supabase` from `@/lib/supabase`. Define constants:
    - `EDGE_BASE` = `import.meta.env.VITE_SUPABASE_URL + '/functions/v1'`
    - `EXPRESS_BASE` = `import.meta.env.VITE_API_URL || ''`

    Define `ApiClientOptions` interface with optional `baseUrl: 'edge' | 'express'`.

    Create `getAuthHeaders()` async function that calls `supabase.auth.getSession()` and returns `{ 'Content-Type': 'application/json', Authorization: 'Bearer ' + session?.access_token }`.

    Export 5 functions — all with explicit return types (per project conventions, no semicolons, single quotes, trailing commas):
    - `apiGet<T>(path: string, options?: ApiClientOptions): Promise<T>` — GET request, default Edge base
    - `apiPost<T>(path: string, body: unknown, options?: ApiClientOptions): Promise<T>` — POST with JSON body
    - `apiPut<T>(path: string, body: unknown, options?: ApiClientOptions): Promise<T>` — PUT with JSON body
    - `apiPatch<T>(path: string, body: unknown, options?: ApiClientOptions): Promise<T>` — PATCH with JSON body
    - `apiDelete<T>(path: string, options?: ApiClientOptions): Promise<T>` — DELETE request

    All functions: resolve base URL from options (default 'edge'), call `getAuthHeaders()`, call `fetch()`, check `response.ok`, throw `new Error('API error ${response.status}: ${response.statusText}')` on failure, return `response.json() as Promise<T>`.

    **Create `frontend/src/lib/__tests__/api-client.test.ts`**:

    Mock `@/lib/supabase` to return `{ data: { session: { access_token: 'test-token' } } }` from `auth.getSession()`. Mock `fetch` globally. Write tests for all 6 behaviors above. Use `vi.stubEnv('VITE_SUPABASE_URL', 'https://test.supabase.co')` and `vi.stubEnv('VITE_API_URL', 'http://localhost:3001')`.

    **Delete deprecated files** (per D-13):
    - Delete `frontend/src/components/layout/MainLayout.tsx`
    - Delete `frontend/src/hooks/useMobile.tsx`
    - Grep for any remaining imports of these files. If found, they should already be migrated to NavigationShell / useResponsive (Phase 5 completed this). If any straggler imports exist, update them.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && pnpm --filter frontend test -- --run src/lib/__tests__/api-client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/lib/api-client.ts exists and contains `export async function apiGet`
    - frontend/src/lib/api-client.ts contains `export async function apiPost`
    - frontend/src/lib/api-client.ts contains `export async function apiPut`
    - frontend/src/lib/api-client.ts contains `export async function apiPatch`
    - frontend/src/lib/api-client.ts contains `export async function apiDelete`
    - frontend/src/lib/api-client.ts contains `supabase.auth.getSession()`
    - frontend/src/lib/api-client.ts contains `VITE_SUPABASE_URL`
    - frontend/src/lib/api-client.ts contains `VITE_API_URL`
    - frontend/src/lib/__tests__/api-client.test.ts exists and contains at least 5 `it(` or `test(` calls
    - frontend/src/components/layout/MainLayout.tsx does NOT exist
    - frontend/src/hooks/useMobile.tsx does NOT exist
    - `grep -r "MainLayout" frontend/src/` returns zero results (no straggler imports)
    - `grep -r "useMobile" frontend/src/` returns zero results (no straggler imports)
  </acceptance_criteria>
  <done>apiClient exports 5 functions, all tests pass, deprecated files deleted with no broken imports</done>
</task>

<task type="auto">
  <name>Task 2: Migrate dossiers domain — repository, hooks, types, barrel, and re-exports</name>
  <files>
    frontend/src/domains/dossiers/repositories/dossiers.repository.ts,
    frontend/src/domains/dossiers/hooks/useDossiers.ts,
    frontend/src/domains/dossiers/hooks/useDossier.ts,
    frontend/src/domains/dossiers/hooks/useDossierActivityTimeline.ts,
    frontend/src/domains/dossiers/hooks/useDossierRecommendations.ts,
    frontend/src/domains/dossiers/hooks/useDossierFirstSearch.ts,
    frontend/src/domains/dossiers/hooks/useQuickSwitcherSearch.ts,
    frontend/src/domains/dossiers/hooks/useNoResultsSuggestions.ts,
    frontend/src/domains/dossiers/types/index.ts,
    frontend/src/domains/dossiers/index.ts,
    frontend/src/hooks/useDossiers.ts,
    frontend/src/hooks/useDossier.ts,
    frontend/src/hooks/useDossierActivityTimeline.ts,
    frontend/src/hooks/useDossierRecommendations.ts,
    frontend/src/hooks/useDossierFirstSearch.ts,
    frontend/src/hooks/useQuickSwitcherSearch.ts,
    frontend/src/hooks/useNoResultsSuggestions.ts
  </files>
  <read_first>
    frontend/src/hooks/useDossiers.ts,
    frontend/src/hooks/useDossier.ts,
    frontend/src/hooks/useDossierActivityTimeline.ts,
    frontend/src/hooks/useDossierRecommendations.ts,
    frontend/src/hooks/useDossierFirstSearch.ts,
    frontend/src/hooks/useQuickSwitcherSearch.ts,
    frontend/src/hooks/useNoResultsSuggestions.ts,
    frontend/src/lib/api-client.ts,
    frontend/src/domains/shared/types/result.ts
  </read_first>
  <action>
    **Create domain directory structure:**
    ```
    frontend/src/domains/dossiers/
      repositories/dossiers.repository.ts
      hooks/useDossiers.ts
      hooks/useDossier.ts
      hooks/useDossierActivityTimeline.ts
      hooks/useDossierRecommendations.ts
      hooks/useDossierFirstSearch.ts
      hooks/useQuickSwitcherSearch.ts
      hooks/useNoResultsSuggestions.ts
      types/index.ts
      index.ts
    ```

    **Step 1 — Types:** Read each of the 7 source hooks. Extract all type definitions (interfaces, type aliases) used for API request params and response shapes. Place them in `frontend/src/domains/dossiers/types/index.ts`. If types are already defined in a shared location, import from there instead of duplicating.

    **Step 2 — Repository:** Create `dossiers.repository.ts` (per D-02, plain functions):
    - Import `{ apiGet, apiPost, apiPut, apiPatch, apiDelete }` from `@/lib/api-client`
    - For each of the 7 hooks, extract the fetch() call into a plain exported function:
      - `getDossiers(filters)` — calls `apiGet('/dossiers-list?...')`
      - `getDossier(id)` — calls `apiGet('/dossier-detail/{id}')` (check actual endpoint path in source hook)
      - `getDossierActivityTimeline(id, params)` — check source hook for exact endpoint
      - `getDossierRecommendations(id)` — check source hook for exact endpoint
      - `getDossierFirstSearch(query)` — check source hook for exact endpoint
      - `getQuickSwitcherSearch(query)` — check source hook for exact endpoint
      - `getNoResultsSuggestions(query)` — check source hook for exact endpoint
    - Preserve exact URL paths and query param serialization from source hooks
    - All functions must have explicit return types (per project conventions)

    **Step 3 — Hooks:** For each of the 7 hooks, create a new file in `domains/dossiers/hooks/` that:
    - Imports from `../repositories/dossiers.repository` (use `import * as dossiersRepo` or named imports)
    - Imports from `@tanstack/react-query`
    - Preserves the exact hook signature (same params, same return type) per D-12
    - Preserves existing query key structure to avoid cache invalidation issues
    - Replaces inline `fetch()` + `getAuthHeaders()` with repo function call in `queryFn`/`mutationFn`
    - No `fetch(`, no `getAuthHeaders(`, no `API_BASE_URL` in the hook files

    **Step 4 — Barrel:** Create `frontend/src/domains/dossiers/index.ts` that re-exports all hooks and the repo:
    ```typescript
    export * from './hooks/useDossiers'
    export * from './hooks/useDossier'
    // ... all 7 hooks
    export * as dossiersRepo from './repositories/dossiers.repository'
    ```

    **Step 5 — Backward compat re-exports (per D-11):** Replace each original hook file in `frontend/src/hooks/` with a re-export:
    ```typescript
    // frontend/src/hooks/useDossiers.ts
    export { useDossiers, dossierKeys } from '@/domains/dossiers'
    ```
    Do this for all 7 hook files. This preserves all existing `import { useDossiers } from '@/hooks/useDossiers'` across 43+ route files.

    **Step 6 — Verify build:** Run `pnpm --filter frontend tsc --noEmit` (or `pnpm build`) to confirm no type errors from the migration.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && pnpm --filter frontend tsc --noEmit 2>&1 | tail -5 && grep -c "fetch(" frontend/src/domains/dossiers/hooks/*.ts | grep -v ":0" || echo "No raw fetch in dossier hooks - PASS"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/domains/dossiers/repositories/dossiers.repository.ts exists and contains `import { apiGet` from `@/lib/api-client`
    - frontend/src/domains/dossiers/repositories/dossiers.repository.ts contains `export function getDossiers`
    - frontend/src/domains/dossiers/hooks/useDossiers.ts exists and contains `dossiersRepo` or imports from `../repositories/`
    - frontend/src/domains/dossiers/hooks/useDossiers.ts does NOT contain `fetch(`
    - frontend/src/domains/dossiers/hooks/useDossiers.ts does NOT contain `getAuthHeaders`
    - frontend/src/domains/dossiers/index.ts exists and contains re-exports for all 7 hooks
    - frontend/src/hooks/useDossiers.ts contains `export` and `from '@/domains/dossiers'`
    - frontend/src/hooks/useDossier.ts contains `export` and `from '@/domains/dossiers'`
    - `grep -c "fetch(" frontend/src/domains/dossiers/hooks/*.ts` shows 0 for every file
    - `pnpm --filter frontend tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>All 7 dossier hooks migrated to domain repository pattern, zero raw fetch() calls in domain hooks, all old import paths work via re-exports, TypeScript compilation passes</done>
</task>

</tasks>

<verification>
- `pnpm --filter frontend tsc --noEmit` passes (no type errors)
- `pnpm --filter frontend test -- --run src/lib/__tests__/api-client.test.ts` passes
- `grep -rn "fetch(" frontend/src/domains/dossiers/hooks/` returns nothing
- `grep -rn "getAuthHeaders" frontend/src/domains/dossiers/hooks/` returns nothing
- All 7 re-export files in `frontend/src/hooks/` contain `from '@/domains/dossiers'`
- `frontend/src/components/layout/MainLayout.tsx` does not exist
- `frontend/src/hooks/useMobile.tsx` does not exist
</verification>

<success_criteria>

- Shared apiClient with 5 exported functions and passing unit tests
- Complete dossiers domain: types + repository + 7 hooks + barrel
- Zero raw fetch() in any dossier domain hook
- Backward-compatible re-exports at old hook paths
- Deprecated Phase 5 files deleted
- TypeScript compilation clean
  </success_criteria>

<output>
After completion, create `.planning/phases/06-architecture-consolidation/06-01-SUMMARY.md`
</output>
