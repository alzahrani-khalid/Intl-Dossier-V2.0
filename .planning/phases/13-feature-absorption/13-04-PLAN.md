---
phase: 13-feature-absorption
plan: 04
type: execute
wave: 2
depends_on:
  - 13-02
files_modified:
  - frontend/src/pages/engagements/workspace/DocsTab.tsx
  - frontend/src/pages/engagements/workspace/CalendarTab.tsx
  - frontend/src/pages/dossiers/DossierListPage.tsx
  - frontend/src/components/keyboard-shortcuts/CommandPalette.tsx
  - frontend/src/i18n/locales/en/workspace.json
  - frontend/src/i18n/locales/ar/workspace.json
autonomous: true
requirements:
  - ABSORB-02
  - ABSORB-05
  - ABSORB-06

must_haves:
  truths:
    - "User can trigger Generate Briefing from workspace DocsTab and from Cmd+K command"
    - "User can trigger Schedule Poll from workspace CalendarTab"
    - "User can trigger Export Dossiers and Import Dossiers from dossier list view action bar"
    - "Absorbed features are accessible BOTH from contextual location AND via Cmd+K commands (dual-access per D-10)"
  artifacts:
    - path: "frontend/src/pages/engagements/workspace/DocsTab.tsx"
      provides: "DocsTab with verified Generate Briefing action"
    - path: "frontend/src/pages/engagements/workspace/CalendarTab.tsx"
      provides: "CalendarTab with Schedule Poll action button"
    - path: "frontend/src/pages/dossiers/DossierListPage.tsx"
      provides: "List view with Export/Import action buttons"
  key_links:
    - from: "frontend/src/pages/engagements/workspace/CalendarTab.tsx"
      to: "frontend/src/components/availability-polling/AvailabilityPollCreator.tsx"
      via: "import and render polling UI inside CalendarTab"
      pattern: "AvailabilityPollCreator"
    - from: "frontend/src/pages/dossiers/DossierListPage.tsx"
      to: "frontend/src/components/export/ExportDialog.tsx"
      via: "import and trigger ExportDialog from action button"
      pattern: "ExportDialog"
    - from: "frontend/src/components/keyboard-shortcuts/CommandPalette.tsx"
      to: "quickActions"
      via: "Updated Cmd+K commands for briefing, graph, polling"
      pattern: "quickActions"
---

<objective>
Absorb AI Briefings (ABSORB-02), Availability Polling (ABSORB-05), and Export/Import (ABSORB-06) into their contextual locations. Per D-12, briefings go to DocsTab. Per D-15, polling goes to CalendarTab. Per D-16, export/import go to list view action buttons. Per D-10, all absorbed features must also be accessible via Cmd+K commands for dual-access discoverability.

Purpose: Three smaller absorption targets consolidated into one plan. Each feature moves from a standalone page into the exact contextual location where users naturally work with that functionality.
Output: DocsTab briefing action verified, CalendarTab schedule action added, list view export/import buttons added, Cmd+K commands updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-feature-absorption/13-CONTEXT.md
@.planning/phases/13-feature-absorption/13-RESEARCH.md
@.planning/phases/13-feature-absorption/13-UI-SPEC.md
@.planning/phases/13-feature-absorption/13-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/pages/engagements/workspace/DocsTab.tsx:
```typescript
// ~270 lines, already has "Generate Briefing" action via useGenerateEngagementBrief
// ABSORB-02: Verify this works; minimal changes needed
```

From frontend/src/pages/engagements/workspace/CalendarTab.tsx:
```typescript
// Existing calendar tab with engagement-scoped events
// ABSORB-05: Add "Schedule Poll" action button
```

From frontend/src/pages/dossiers/DossierListPage.tsx:
```typescript
// Main dossier list page with filter/sort controls
// ABSORB-06: Add Export/Import action buttons to action bar
```

From frontend/src/components/availability-polling/:
```typescript
// AvailabilityPollCreator.tsx -- poll creation UI
// AvailabilityPollVoter.tsx -- poll voting UI
// AvailabilityPollResults.tsx -- poll results UI
```

From frontend/src/components/export/ExportDialog.tsx:
```typescript
// Existing export dialog component
```

From frontend/src/components/export-import/:
```typescript
// ExportDialog.tsx (different from above), index.ts
// Export/import functionality components
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Absorb polling into CalendarTab and export/import into list views</name>
  <files>
    frontend/src/pages/engagements/workspace/CalendarTab.tsx
    frontend/src/pages/dossiers/DossierListPage.tsx
    frontend/src/pages/engagements/workspace/DocsTab.tsx
    frontend/src/i18n/locales/en/workspace.json
    frontend/src/i18n/locales/ar/workspace.json
  </files>
  <read_first>
    frontend/src/pages/engagements/workspace/CalendarTab.tsx
    frontend/src/pages/engagements/workspace/DocsTab.tsx
    frontend/src/pages/dossiers/DossierListPage.tsx
    frontend/src/components/availability-polling/AvailabilityPollCreator.tsx
    frontend/src/components/availability-polling/AvailabilityPollVoter.tsx
    frontend/src/components/availability-polling/AvailabilityPollResults.tsx
    frontend/src/domains/import/hooks/useAvailabilityPolling.ts
    frontend/src/components/export/ExportDialog.tsx
    frontend/src/components/export-import/ExportDialog.tsx
    frontend/src/components/export-import/index.ts
    frontend/src/pages/availability-polling/AvailabilityPollingPage.tsx
    frontend/src/pages/export-import/ExportImportPage.tsx
    frontend/src/i18n/locales/en/workspace.json
    frontend/src/i18n/locales/ar/workspace.json
  </read_first>
  <action>
    **1. CalendarTab -- Add "Schedule Poll" action (ABSORB-05, D-15):**
    - Add a "Schedule Poll" button in the CalendarTab action bar area (alongside any existing controls like "+ Add Event")
    - Button spec per UI-SPEC: accent-colored, minimum 44x44px touch target (`min-h-11 min-w-11`), icon-only on mobile (`CalendarClock` from lucide-react) with tooltip, icon+label on sm+ (`"Schedule Poll"` / `"جدولة استطلاع"`)
    - On click: open a dialog/panel containing `AvailabilityPollCreator` component (from `@/components/availability-polling/AvailabilityPollCreator`)
    - Pre-link the poll to the current engagement via engagementId prop (read CalendarTab to understand how it receives engagement context)
    - Use AdaptiveDialog for the poll creation UI wrapper (dialog on desktop, bottom sheet on mobile)
    - Add state: `const [pollDialogOpen, setPollDialogOpen] = useState(false)`
    - After poll creation success, close dialog and optionally refresh calendar

    **2. DossierListPage -- Add Export/Import buttons (ABSORB-06, D-16):**
    - Add "Export Dossiers" and "Import Dossiers" buttons to the DossierListPage action bar area
    - Read DossierListPage.tsx to identify the existing action bar/header area where filter and sort controls live
    - Button specs per UI-SPEC:
      - "Export Dossiers" / "تصدير الملفات": accent-colored on sm+, icon-only (`Download` from lucide-react) on mobile with tooltip
      - "Import Dossiers" / "استيراد الملفات": ghost variant on sm+, icon-only (`Upload` from lucide-react) on mobile with tooltip
      - Both have min-h-11 min-w-11 touch targets
    - "Export Dossiers" click: Open ExportDialog (from `@/components/export/ExportDialog` or `@/components/export-import/ExportDialog` -- read both files to determine which is the correct one) pre-filtered to current list view context (pass current filter/search state as props)
    - "Import Dossiers" click: Open file picker or import dialog. Read ExportImportPage to understand the import flow, then embed the import UI in an AdaptiveDialog.
    - Add states: `const [exportDialogOpen, setExportDialogOpen] = useState(false)` and `const [importDialogOpen, setImportDialogOpen] = useState(false)`

    **3. DocsTab -- Verify Generate Briefing (ABSORB-02, D-12):**
    - Read DocsTab.tsx to confirm the "Generate Briefing" action already exists via `useGenerateEngagementBrief`
    - If it exists and is functional: no code changes needed, just document in acceptance criteria
    - If it's missing or incomplete: add a "Generate Briefing" button per UI-SPEC (accent-colored, text "Generate Briefing" / "انشاء ايجاز", with loading state "Generating briefing..." / "جارٍ انشاء الايجاز...")
    - Ensure the Generate Briefing action is prominent (not hidden in a menu)

    **4. i18n updates** (workspace.json EN and AR):
    - EN: `"calendar": { "schedulePoll": "Schedule Poll" }`, `"docs": { "generateBriefing": "Generate Briefing", "generating": "Generating briefing..." }`
    - AR: `"calendar": { "schedulePoll": "جدولة استطلاع" }`, `"docs": { "generateBriefing": "انشاء ايجاز", "generating": "جارٍ انشاء الايجاز..." }`
    - Add list view keys if dossier namespace is used: `"list": { "exportDossiers": "Export Dossiers", "importDossiers": "Import Dossiers" }` / `"list": { "exportDossiers": "تصدير الملفات", "importDossiers": "استيراد الملفات" }`
    - Use correct i18n namespace (read existing files to determine -- may be `workspace`, `dossier`, or `common`)

    RTL: All buttons use logical properties. Icon placement with `me-2`. Text alignment with `text-start`.
  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/Coding\ Space/Intl-Dossier-V2.0 && pnpm exec tsc --noEmit --project frontend/tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CalendarTab.tsx contains `AvailabilityPollCreator` import or reference to polling component
    - CalendarTab.tsx contains `schedulePoll` or `Schedule Poll` string/i18n key
    - CalendarTab.tsx contains `pollDialogOpen` or equivalent state for poll dialog
    - CalendarTab.tsx contains `min-h-11` on the Schedule Poll button
    - DossierListPage.tsx contains `ExportDialog` import
    - DossierListPage.tsx contains `exportDialogOpen` or equivalent state
    - DossierListPage.tsx contains `Download` or `Upload` icon import from lucide-react
    - DocsTab.tsx contains `generateBriefing` or `Generate Briefing` or `useGenerateEngagementBrief`
    - No `ml-`, `mr-`, `pl-`, `pr-`, `text-left`, `text-right` in modified code
  </acceptance_criteria>
  <done>CalendarTab has Schedule Poll action that opens AvailabilityPollCreator in a dialog. DossierListPage has Export/Import action buttons with ExportDialog integration. DocsTab Generate Briefing action confirmed working.</done>
</task>

<task type="auto">
  <name>Task 2: Register Cmd+K commands for absorbed features (dual-access per D-10)</name>
  <files>
    frontend/src/components/keyboard-shortcuts/CommandPalette.tsx
  </files>
  <read_first>
    frontend/src/components/keyboard-shortcuts/CommandPalette.tsx
    frontend/src/components/keyboard-shortcuts/KeyboardShortcutProvider.tsx
    frontend/src/pages/engagements/workspace/DocsTab.tsx
    frontend/src/pages/engagements/workspace/CalendarTab.tsx
  </read_first>
  <action>
    **Update `CommandPalette.tsx` quickActions** to register Cmd+K commands for all absorbed features per D-10:

    Update or add these commands to the quickActions array:
    1. **"Generate Briefing"** / **"انشاء ايجاز"**: Command that navigates to the current engagement's Docs tab (if in engagement context) or shows a message to select an engagement first. ID: `cmd-generate-briefing`. Icon: `FileText` from lucide-react.
    2. **"View Network"** / **"عرض الشبكة"**: Command that opens FullScreenGraphModal for the current dossier (if in dossier context) or navigates to `/dossiers`. ID: `cmd-view-network`. Icon: `Network` or `GitBranch` from lucide-react.
    3. **"Schedule Poll"** / **"جدولة استطلاع"**: Command that navigates to the current engagement's Calendar tab (if in engagement context). ID: `cmd-schedule-poll`. Icon: `CalendarClock` from lucide-react.
    4. **"Export Dossiers"** / **"تصدير الملفات"**: Command that navigates to `/dossiers` and triggers export dialog. ID: `cmd-export-dossiers`. Icon: `Download` from lucide-react.

    **Remove or update stale quickActions** that point to absorbed standalone routes:
    - `nav-analytics`: Change to navigate to `/dashboard` (analytics is now dashboard zone)
    - `nav-briefing-books`: Replace with `cmd-generate-briefing` above
    - `nav-relationships` or graph-related: Replace with `cmd-view-network` above
    - Remove any commands pointing to `/search`, `/availability-polling`, `/export`, `/export-import`

    Each command must call `incrementCommandUsage(commandId)` in its onSelect handler (function added in Plan 02).

    **Context awareness:** For commands that need entity context (briefing needs engagement, network needs dossier), use the current URL to detect context:
    ```typescript
    const location = useRouterState({ select: (s) => s.location })
    // Extract engagementId from /engagements/$id/... pattern
    // Extract dossierId from /dossiers/.../$id/... pattern
    ```
    If no context: navigate to the list page for that entity type.

    Add i18n keys to quick-switcher.json for new command labels if not already present.
  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/Coding\ Space/Intl-Dossier-V2.0 && pnpm exec tsc --noEmit --project frontend/tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CommandPalette.tsx contains `cmd-generate-briefing` command ID
    - CommandPalette.tsx contains `cmd-view-network` command ID
    - CommandPalette.tsx contains `cmd-schedule-poll` command ID
    - CommandPalette.tsx contains `cmd-export-dossiers` command ID
    - CommandPalette.tsx does NOT contain navigation to `/analytics` as a standalone page destination
    - CommandPalette.tsx does NOT contain navigation to `/briefing-books` as a standalone page destination
    - CommandPalette.tsx does NOT contain navigation to `/search` as a standalone page destination
    - Each new command calls `incrementCommandUsage` or equivalent usage tracking
  </acceptance_criteria>
  <done>All absorbed features registered as Cmd+K commands for dual-access. Stale commands pointing to removed routes updated. Command usage tracking integrated for all new commands.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes: `cd frontend && pnpm exec tsc --noEmit`
- CalendarTab has Schedule Poll button and dialog
- DossierListPage has Export/Import buttons
- DocsTab has Generate Briefing action
- Cmd+K commands registered for all absorbed features
- No stale commands pointing to removed standalone routes
</verification>

<success_criteria>
- All 3 absorbed features (briefing, polling, export) accessible from their contextual locations
- All absorbed features also accessible via Cmd+K commands (dual-access per D-10)
- No Cmd+K commands point to standalone routes that will be removed
- RTL compliant, bilingual
</success_criteria>

<output>
After completion, create `.planning/phases/13-feature-absorption/13-04-SUMMARY.md`
</output>
