---
phase: 13-feature-absorption
plan: 05
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
  - 13-03
  - 13-04
files_modified:
  - frontend/src/routes/_protected/analytics.tsx
  - frontend/src/routes/_protected/briefing-books.tsx
  - frontend/src/routes/_protected/search.tsx
  - frontend/src/routes/_protected/relationships/graph.tsx
  - frontend/src/routes/_protected/availability-polling.tsx
  - frontend/src/routes/_protected/export.tsx
  - frontend/src/routes/_protected/export-import.tsx
  - frontend/src/components/layout/navigation-config.ts
autonomous: false
requirements:
  - ABSORB-01
  - ABSORB-02
  - ABSORB-03
  - ABSORB-04
  - ABSORB-05
  - ABSORB-06

must_haves:
  truths:
    - "Navigating to /analytics redirects to /dashboard"
    - "Navigating to /briefing-books redirects to /dashboard"
    - "Navigating to /search redirects to /dashboard"
    - "Navigating to /relationships/graph redirects to /dossiers"
    - "Navigating to /availability-polling redirects to /dashboard"
    - "Navigating to /export redirects to /dossiers"
    - "Navigating to /export-import redirects to /dossiers"
    - "Sidebar no longer shows nav items for analytics, briefing-books, advanced-search, availability-polling"
    - "No 404 errors from old bookmarked URLs"
  artifacts:
    - path: "frontend/src/routes/_protected/analytics.tsx"
      provides: "Redirect to /dashboard"
      contains: "redirect"
    - path: "frontend/src/components/layout/navigation-config.ts"
      provides: "Cleaned navigation without absorbed page items"
  key_links:
    - from: "frontend/src/routes/_protected/analytics.tsx"
      to: "/dashboard"
      via: "TanStack Router redirect in beforeLoad"
      pattern: "redirect.*dashboard"
    - from: "frontend/src/components/layout/navigation-config.ts"
      to: "sidebar rendering"
      via: "createNavigationGroups excluding absorbed items"
      pattern: "createNavigationGroups"
---

<objective>
Replace all absorbed standalone route files with redirects to their new contextual locations, and remove absorbed navigation items from the sidebar configuration. Per D-09, old standalone routes redirect to new locations with no broken links or 404s. This is the final cleanup plan that runs after all features have been absorbed into their contextual locations.

Purpose: Ensures no dead routes exist and the navigation reflects the new hub-and-spoke architecture. Users with bookmarks to old pages seamlessly arrive at the correct new location.
Output: 7 route files replaced with redirects, navigation config cleaned of absorbed items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-feature-absorption/13-CONTEXT.md
@.planning/phases/13-feature-absorption/13-RESEARCH.md
@.planning/phases/13-feature-absorption/13-UI-SPEC.md
@.planning/phases/13-feature-absorption/13-01-SUMMARY.md
@.planning/phases/13-feature-absorption/13-02-SUMMARY.md
@.planning/phases/13-feature-absorption/13-03-SUMMARY.md
@.planning/phases/13-feature-absorption/13-04-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/routes/_protected/analytics.tsx (current, to be replaced):
```typescript
// Currently renders AnalyticsDashboardPage
// Will be replaced with redirect to /dashboard
```

From frontend/src/components/layout/navigation-config.ts:
```typescript
export interface NavigationItem {
  id: string
  label: string
  path: string
  icon?: LucideIcon
  badge?: number
  secondary?: boolean
}
export interface NavigationGroup { ... }
export interface NavigationSection { ... }
export const createNavigationGroups = (...) => NavigationGroup[]
export const createNavigationSections = (...) => NavigationSection[]
// Items to remove: briefing-books (id, path /briefing-books), analytics (id, path /analytics),
// advanced-search (id, path /search), availability-polling (id, path /availability-polling)
```

Route redirect pattern (from TanStack Router):
```typescript
import { createFileRoute, redirect } from '@tanstack/react-router'
export const Route = createFileRoute('/_protected/analytics')({
  beforeLoad: () => {
    throw redirect({ to: '/dashboard' })
  },
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace absorbed route files with redirects and clean navigation config</name>
  <files>
    frontend/src/routes/_protected/analytics.tsx
    frontend/src/routes/_protected/briefing-books.tsx
    frontend/src/routes/_protected/search.tsx
    frontend/src/routes/_protected/relationships/graph.tsx
    frontend/src/routes/_protected/availability-polling.tsx
    frontend/src/routes/_protected/export.tsx
    frontend/src/routes/_protected/export-import.tsx
    frontend/src/components/layout/navigation-config.ts
  </files>
  <read_first>
    frontend/src/routes/_protected/analytics.tsx
    frontend/src/routes/_protected/briefing-books.tsx
    frontend/src/routes/_protected/search.tsx
    frontend/src/routes/_protected/relationships/graph.tsx
    frontend/src/routes/_protected/availability-polling.tsx
    frontend/src/routes/_protected/export.tsx
    frontend/src/routes/_protected/export-import.tsx
    frontend/src/components/layout/navigation-config.ts
  </read_first>
  <action>
    **Replace each absorbed route file** with a redirect. Use this exact pattern for each file, adapting the route path and redirect target:

    **`frontend/src/routes/_protected/analytics.tsx`** -> redirect to `/dashboard` (per D-09, UI-SPEC redirect table):
    ```typescript
    import { createFileRoute, redirect } from '@tanstack/react-router'

    export const Route = createFileRoute('/_protected/analytics')({
      beforeLoad: () => {
        throw redirect({ to: '/dashboard' })
      },
    })
    ```

    **`frontend/src/routes/_protected/briefing-books.tsx`** -> redirect to `/dashboard`:
    ```typescript
    import { createFileRoute, redirect } from '@tanstack/react-router'

    export const Route = createFileRoute('/_protected/briefing-books')({
      beforeLoad: () => {
        throw redirect({ to: '/dashboard' })
      },
    })
    ```

    **`frontend/src/routes/_protected/search.tsx`** -> redirect to `/dashboard`:
    ```typescript
    import { createFileRoute, redirect } from '@tanstack/react-router'

    export const Route = createFileRoute('/_protected/search')({
      beforeLoad: () => {
        throw redirect({ to: '/dashboard' })
      },
    })
    ```

    **`frontend/src/routes/_protected/relationships/graph.tsx`** -> redirect to `/dossiers`:
    ```typescript
    import { createFileRoute, redirect } from '@tanstack/react-router'

    export const Route = createFileRoute('/_protected/relationships/graph')({
      beforeLoad: () => {
        throw redirect({ to: '/dossiers' })
      },
    })
    ```

    **`frontend/src/routes/_protected/availability-polling.tsx`** -> redirect to `/dashboard`:
    ```typescript
    import { createFileRoute, redirect } from '@tanstack/react-router'

    export const Route = createFileRoute('/_protected/availability-polling')({
      beforeLoad: () => {
        throw redirect({ to: '/dashboard' })
      },
    })
    ```

    **`frontend/src/routes/_protected/export.tsx`** -> redirect to `/dossiers`:
    ```typescript
    import { createFileRoute, redirect } from '@tanstack/react-router'

    export const Route = createFileRoute('/_protected/export')({
      beforeLoad: () => {
        throw redirect({ to: '/dossiers' })
      },
    })
    ```

    **`frontend/src/routes/_protected/export-import.tsx`** -> redirect to `/dossiers`:
    ```typescript
    import { createFileRoute, redirect } from '@tanstack/react-router'

    export const Route = createFileRoute('/_protected/export-import')({
      beforeLoad: () => {
        throw redirect({ to: '/dossiers' })
      },
    })
    ```

    **After replacing route files**, regenerate the TanStack Router route tree (per Research pitfall 2):
    ```bash
    cd frontend && pnpm exec tsr generate
    ```

    **Update `frontend/src/components/layout/navigation-config.ts`**:
    - In the `createNavigationGroups` function, REMOVE these navigation items by their id:
      - `briefing-books` (id: 'briefing-books', path: '/briefing-books')
      - `analytics` (id: 'analytics', path: '/analytics')
      - `advanced-search` (id: 'advanced-search', path: '/search')
      - `availability-polling` (id: 'availability-polling', path: '/availability-polling')
    - Do NOT remove other nav items. Only remove the 4 items for absorbed pages.
    - If `createNavigationSections` also references these items, remove from there too.
    - Keep the navigation group structure intact (Operations, Dossiers, Administration groups).

    **Verify no other files import the old standalone page components** that are no longer rendered. The route files are now redirects, so they don't import page components. But verify that nothing else imports:
    - `AnalyticsDashboardPage`
    - `BriefingBooksPage`
    - `DossierSearchPage`
    - `RelationshipGraphPage` (still used by FullScreenGraphModal from Plan 03 -- do NOT remove)
    - `AvailabilityPollingPage`
    - `ExportImportPage`
    
    If any non-route file imports these, update or remove the import. The page component FILES themselves should NOT be deleted (they contain reusable logic extracted by Plans 01-04), only the route files redirect away from them.
  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/Coding\ Space/Intl-Dossier-V2.0/frontend && pnpm exec tsr generate && pnpm exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - analytics.tsx contains `redirect({ to: '/dashboard' })` and does NOT import AnalyticsDashboardPage
    - briefing-books.tsx contains `redirect({ to: '/dashboard' })` and does NOT import BriefingBooksPage
    - search.tsx contains `redirect({ to: '/dashboard' })` and does NOT import DossierSearchPage
    - relationships/graph.tsx contains `redirect({ to: '/dossiers' })` and does NOT import RelationshipGraphPage
    - availability-polling.tsx contains `redirect({ to: '/dashboard' })` and does NOT import AvailabilityPollingPage
    - export.tsx contains `redirect({ to: '/dossiers' })`
    - export-import.tsx contains `redirect({ to: '/dossiers' })`
    - navigation-config.ts does NOT contain `id: 'briefing-books'` as a nav item
    - navigation-config.ts does NOT contain `id: 'analytics'` as a nav item with path '/analytics'
    - navigation-config.ts does NOT contain `id: 'advanced-search'` as a nav item
    - navigation-config.ts does NOT contain `id: 'availability-polling'` as a nav item
    - TypeScript compilation passes after route tree regeneration
    - `pnpm exec tsr generate` completes without errors
  </acceptance_criteria>
  <done>All 7 absorbed standalone routes replaced with redirects. Navigation config cleaned of 4 absorbed nav items. Route tree regenerated. No broken imports.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all feature absorptions and redirects</name>
  <action>Human verification of the complete Phase 13 feature absorption. Start the dev server with `pnpm dev` and walk through every absorbed feature to confirm correct placement, functionality, redirects, and RTL support.</action>
  <what-built>
    Complete Phase 13 feature absorption:
    1. Dashboard KPI analytics widgets (Plan 01)
    2. Dossier overview analytics cards for all 7 types (Plan 01)
    3. Enhanced Cmd+K quick switcher with empty state, grouped results, entity sub-groups (Plan 02)
    4. Network graph in RelationshipSidebar + full-screen modal (Plan 03)
    5. Briefing, polling, export absorbed into workspace tabs + list views (Plan 04)
    6. All 7 standalone routes redirect to new locations (this plan)
    7. Sidebar navigation cleaned of absorbed items
  </what-built>
  <how-to-verify>
    **Dashboard Analytics (ABSORB-01):**
    1. Navigate to /dashboard
    2. Verify "Overview" zone appears as FIRST zone with 4 KPI cards (Total Dossiers, Active Engagements, Upcoming Deadlines, Open Work Items)
    3. On mobile viewport: verify 2-column grid. On desktop: verify 4-column grid

    **Dossier Analytics Cards (ABSORB-01):**
    4. Navigate to any country dossier detail page -> Overview tab
    5. Verify a context-specific analytics card appears in the overview grid
    6. Repeat for at least one Organization and one Person dossier

    **Cmd+K Quick Switcher (ABSORB-03):**
    7. Press Cmd+K (or Ctrl+K on non-Mac)
    8. Before typing: verify 5 recent dossiers and 5 commands appear
    9. Type a dossier name: verify results grouped by type (Countries, Organizations, etc.)
    10. Type something with no matches: verify "No results found" message

    **Network Graph (ABSORB-04):**
    11. Navigate to any dossier with relationships
    12. Verify mini-graph appears in RelationshipSidebar above tier groups
    13. Click "Expand Graph" -> verify full-screen modal opens with interactive graph
    14. Close modal

    **AI Briefings (ABSORB-02):**
    15. Navigate to any engagement workspace -> Docs tab
    16. Verify "Generate Briefing" action exists and is accessible

    **Polling (ABSORB-05):**
    17. Navigate to any engagement workspace -> Calendar tab
    18. Verify "Schedule Poll" button exists

    **Export/Import (ABSORB-06):**
    19. Navigate to /dossiers (list page)
    20. Verify "Export Dossiers" and "Import Dossiers" buttons exist in action bar

    **Route Redirects (D-09):**
    21. Navigate to /analytics -> verify redirect to /dashboard
    22. Navigate to /briefing-books -> verify redirect to /dashboard
    23. Navigate to /search -> verify redirect to /dashboard
    24. Navigate to /relationships/graph -> verify redirect to /dossiers
    25. Navigate to /availability-polling -> verify redirect to /dashboard

    **Sidebar (all ABSORB):**
    26. Verify sidebar does NOT show: Analytics, Briefing Books, Advanced Search, Availability Polling

    **RTL Check:**
    27. Switch to Arabic language
    28. Verify Cmd+K renders correctly in RTL
    29. Verify KPI cards align correctly in RTL
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compilation: `cd frontend && pnpm exec tsc --noEmit`
- Route tree generation: `cd frontend && pnpm exec tsr generate`
- All 7 route files contain redirect pattern
- Navigation config excludes absorbed items
- Human verification of full feature absorption
</verification>

<success_criteria>
- All 7 standalone routes redirect correctly (no 404s)
- Sidebar navigation cleaned of absorbed page items
- All absorbed features accessible from contextual locations
- All absorbed features accessible via Cmd+K commands
- Full RTL + bilingual support verified
- No broken imports or TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-feature-absorption/13-05-SUMMARY.md`
</output>
