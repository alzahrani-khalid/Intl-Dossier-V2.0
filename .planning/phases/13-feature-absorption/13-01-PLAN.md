---
phase: 13-feature-absorption
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Dashboard/components/AnalyticsWidget.tsx
  - frontend/src/components/analytics/KpiCard.tsx
  - frontend/src/components/analytics/DossierAnalyticsCard.tsx
  - frontend/src/pages/Dashboard/OperationsHub.tsx
  - frontend/src/pages/dossiers/CountryOverviewTab.tsx
  - frontend/src/pages/dossiers/OrganizationOverviewTab.tsx
  - frontend/src/pages/dossiers/TopicOverviewTab.tsx
  - frontend/src/pages/dossiers/ForumOverviewTab.tsx
  - frontend/src/pages/dossiers/WorkingGroupOverviewTab.tsx
  - frontend/src/pages/dossiers/PersonOverviewTab.tsx
  - frontend/src/pages/dossiers/ElectedOfficialOverviewTab.tsx
  - frontend/src/domains/analytics/hooks/useAnalyticsDashboard.ts
  - frontend/src/i18n/locales/en/dashboard.json
  - frontend/src/i18n/locales/ar/dashboard.json
autonomous: true
requirements:
  - ABSORB-01

must_haves:
  truths:
    - "User sees KPI summary widgets (Total Dossiers, Active Engagements, Upcoming Deadlines, Open Work Items) on the dashboard"
    - "Each dossier overview tab shows a context-specific analytics metric card tailored to its type"
    - "Dashboard KPI widgets render as 2-col grid on mobile, 4-col on sm+"
  artifacts:
    - path: "frontend/src/pages/Dashboard/components/AnalyticsWidget.tsx"
      provides: "KPI summary grid for dashboard zone"
      exports: ["AnalyticsWidget"]
    - path: "frontend/src/components/analytics/KpiCard.tsx"
      provides: "Single KPI metric card (value + label + trend)"
      exports: ["KpiCard"]
    - path: "frontend/src/components/analytics/DossierAnalyticsCard.tsx"
      provides: "Per-type analytics card for dossier overview tabs"
      exports: ["DossierAnalyticsCard"]
  key_links:
    - from: "frontend/src/pages/Dashboard/OperationsHub.tsx"
      to: "frontend/src/pages/Dashboard/components/AnalyticsWidget.tsx"
      via: "ZoneCollapsible wrapping AnalyticsWidget"
      pattern: "ZoneCollapsible.*AnalyticsWidget"
    - from: "frontend/src/pages/Dashboard/components/AnalyticsWidget.tsx"
      to: "frontend/src/domains/analytics/hooks/useAnalyticsDashboard.ts"
      via: "useAnalyticsDashboard hook for data"
      pattern: "useAnalyticsDashboard"
    - from: "frontend/src/pages/dossiers/CountryOverviewTab.tsx"
      to: "frontend/src/components/analytics/DossierAnalyticsCard.tsx"
      via: "import and render in overview grid"
      pattern: "DossierAnalyticsCard"
---

<objective>
Absorb analytics content into the dashboard as KPI summary widgets and into each dossier overview tab as context-specific metric cards. Per D-05, dashboard widgets are high-level glanceable KPIs only (total dossiers by type, active engagements, upcoming deadlines, work item stats). Per D-06, each dossier type gets tailored analytics cards. Per D-11, this replaces the standalone analytics page functionality.

Purpose: Analytics data is now always visible in context -- on the dashboard for operational overview and on each dossier page for entity-specific metrics -- rather than hidden behind a separate page.
Output: AnalyticsWidget in dashboard, KpiCard component, DossierAnalyticsCard in all 7 dossier overview tabs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-feature-absorption/13-CONTEXT.md
@.planning/phases/13-feature-absorption/13-RESEARCH.md
@.planning/phases/13-feature-absorption/13-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/pages/Dashboard/components/ZoneCollapsible.tsx:
```typescript
export function ZoneCollapsible({
  title,
  defaultExpanded,
  badgeCount,
  children,
}: ZoneCollapsibleProps): React.ReactElement
```

From frontend/src/pages/Dashboard/OperationsHub.tsx:
```typescript
export function OperationsHub(): ReactElement
// Uses zone-based grid layout with ZoneCollapsible wrappers
// Pattern: <ZoneCollapsible title={...} defaultExpanded={true}>
//            <ComponentInside role={role} />
//          </ZoneCollapsible>
```

From frontend/src/domains/analytics/hooks/useAnalyticsDashboard.ts:
```typescript
// Existing hook providing analytics data -- extend if needed
export function useAnalyticsDashboard()
```

From frontend/src/pages/dossiers/overview-cards/SharedSummaryStatsCard.tsx:
```typescript
// Pattern for overview cards: receives dossierId, uses hook for data, renders card
// All overview cards follow: ({ dossierId }: { dossierId: string }) => ReactElement
```

From frontend/src/pages/dossiers/CountryOverviewTab.tsx:
```typescript
// Grid layout pattern:
// <div className="grid grid-cols-1 md:grid-cols-2 gap-4 sm:gap-6">
//   <SharedSummaryStatsCard dossierId={dossierId} />
//   <BilateralSummaryCard dossierId={dossierId} />
//   ...
// </div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create KpiCard and AnalyticsWidget components for dashboard</name>
  <files>
    frontend/src/components/analytics/KpiCard.tsx
    frontend/src/pages/Dashboard/components/AnalyticsWidget.tsx
    frontend/src/pages/Dashboard/OperationsHub.tsx
    frontend/src/domains/analytics/hooks/useAnalyticsDashboard.ts
    frontend/src/i18n/locales/en/dashboard.json
    frontend/src/i18n/locales/ar/dashboard.json
  </files>
  <read_first>
    frontend/src/pages/Dashboard/OperationsHub.tsx
    frontend/src/pages/Dashboard/components/ZoneCollapsible.tsx
    frontend/src/pages/Dashboard/components/QuickStatsBar.tsx
    frontend/src/pages/Dashboard/components/QuickStatCard.tsx
    frontend/src/domains/analytics/hooks/useAnalyticsDashboard.ts
    frontend/src/domains/analytics/repositories/analytics.repository.ts
    frontend/src/domains/analytics/types/index.ts
    frontend/src/i18n/locales/en/dashboard.json
    frontend/src/i18n/locales/ar/dashboard.json
    frontend/src/pages/analytics/AnalyticsDashboardPage.tsx
  </read_first>
  <action>
    **Create `frontend/src/components/analytics/KpiCard.tsx`** (per D-05, UI-SPEC KPI card spec):
    - Props: `{ label: string, value: number | string | undefined, trend?: { direction: 'up' | 'down' | 'neutral', percentage: number }, isLoading?: boolean }`
    - Render a card with: metric value in 24px/1.5rem semibold (text-4xl font-semibold), label in 11px/0.6875rem semibold uppercase tracking-wider (text-xs font-semibold uppercase tracking-wider), optional trend arrow with percentage
    - Trend arrows: use Lucide `TrendingUp` / `TrendingDown` icons with `--heroui-success` for up, `--heroui-danger` for down (universal arrows, no RTL flip needed per UI-SPEC)
    - Loading state: skeleton placeholder matching card dimensions using existing Skeleton component
    - Surface color: `bg-surface` (maps to `--heroui-surface`)
    - Padding: 16px (p-4 = md spacing token)
    - Rounded corners: `rounded-lg`
    - RTL: use `text-start` for label alignment, logical properties only
    - Explicit return type `ReactElement`
    - No semicolons, single quotes, trailing commas per code style

    **Create `frontend/src/pages/Dashboard/components/AnalyticsWidget.tsx`** (per D-05, UI-SPEC):
    - Props: `{ role?: DashboardRole }` (import DashboardRole from existing types)
    - Use `useAnalyticsDashboard()` hook to fetch data
    - Render 4 KpiCard instances in a grid: `grid grid-cols-2 sm:grid-cols-4 gap-3`
    - KPI cards (per UI-SPEC copywriting):
      1. "Total Dossiers" / "اجمالي الملفات" -- `data.totalDossiers`
      2. "Active Engagements" / "الارتباطات النشطة" -- `data.activeEngagements`
      3. "Upcoming Deadlines" / "المواعيد القادمة" -- `data.upcomingDeadlines`
      4. "Open Work Items" / "عناصر العمل المفتوحة" -- `data.openWorkItems`
    - Error state: muted text "Unable to load analytics. Check your connection and try again." / "تعذر تحميل التحليلات. تحقق من الاتصال وحاول مرة اخرى." inside the zone
    - Empty state: "No analytics data available yet..." / "لا تتوفر بيانات تحليلية بعد..."
    - Use `useTranslation('dashboard')` namespace
    - Explicit return type `ReactElement`

    **Update `useAnalyticsDashboard` hook** if it does not already return `totalDossiers`, `activeEngagements`, `upcomingDeadlines`, `openWorkItems` fields. Extend the hook to call the analytics repository and return these 4 KPIs. Use 5-minute stale time for TanStack Query (staleTime: 5 * 60 * 1000).

    **Update `OperationsHub.tsx`**: Add `AnalyticsWidget` as the FIRST ZoneCollapsible zone (focal point per UI-SPEC), before existing AttentionZone. Use:
    ```
    <ZoneCollapsible title={t('zones.overview')} defaultExpanded={true}>
      <AnalyticsWidget role={role} />
    </ZoneCollapsible>
    ```
    Import AnalyticsWidget from `./components/AnalyticsWidget`.

    **Update i18n files**: Add to `dashboard.json` EN and AR:
    - EN: `"zones": { "overview": "Overview" }`, `"kpi": { "totalDossiers": "Total Dossiers", "activeEngagements": "Active Engagements", "upcomingDeadlines": "Upcoming Deadlines", "openWorkItems": "Open Work Items" }`, `"analytics": { "empty": "No analytics data available yet. Start by creating dossiers and engagements.", "error": "Unable to load analytics. Check your connection and try again." }`
    - AR: `"zones": { "overview": "نظرة عامة" }`, `"kpi": { "totalDossiers": "اجمالي الملفات", "activeEngagements": "الارتباطات النشطة", "upcomingDeadlines": "المواعيد القادمة", "openWorkItems": "عناصر العمل المفتوحة" }`, `"analytics": { "empty": "لا تتوفر بيانات تحليلية بعد. ابدا بانشاء ملفات وارتباطات.", "error": "تعذر تحميل التحليلات. تحقق من الاتصال وحاول مرة اخرى." }`
  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/Coding\ Space/Intl-Dossier-V2.0 && pnpm exec tsc --noEmit --project frontend/tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/analytics/KpiCard.tsx exists and exports `KpiCard`
    - frontend/src/pages/Dashboard/components/AnalyticsWidget.tsx exists and exports `AnalyticsWidget`
    - AnalyticsWidget.tsx contains `grid grid-cols-2 sm:grid-cols-4 gap-3`
    - AnalyticsWidget.tsx contains `useAnalyticsDashboard`
    - KpiCard.tsx contains `text-4xl` and `font-semibold` for metric value
    - KpiCard.tsx contains `TrendingUp` or `TrendingDown` import from lucide-react
    - OperationsHub.tsx contains `AnalyticsWidget` import
    - OperationsHub.tsx contains `ZoneCollapsible` wrapping `AnalyticsWidget`
    - dashboard.json (EN) contains `"totalDossiers": "Total Dossiers"`
    - dashboard.json (AR) contains `"totalDossiers": "اجمالي الملفات"`
    - No `ml-`, `mr-`, `pl-`, `pr-`, `text-left`, `text-right` in new files
  </acceptance_criteria>
  <done>Dashboard shows KPI widget zone with 4 metric cards (2-col mobile, 4-col sm+) as the first zone in OperationsHub. Loading, error, and empty states handled. i18n complete for EN and AR.</done>
</task>

<task type="auto">
  <name>Task 2: Add context-specific analytics cards to all 7 dossier overview tabs</name>
  <files>
    frontend/src/components/analytics/DossierAnalyticsCard.tsx
    frontend/src/pages/dossiers/CountryOverviewTab.tsx
    frontend/src/pages/dossiers/OrganizationOverviewTab.tsx
    frontend/src/pages/dossiers/TopicOverviewTab.tsx
    frontend/src/pages/dossiers/ForumOverviewTab.tsx
    frontend/src/pages/dossiers/WorkingGroupOverviewTab.tsx
    frontend/src/pages/dossiers/PersonOverviewTab.tsx
    frontend/src/pages/dossiers/ElectedOfficialOverviewTab.tsx
  </files>
  <read_first>
    frontend/src/pages/dossiers/CountryOverviewTab.tsx
    frontend/src/pages/dossiers/OrganizationOverviewTab.tsx
    frontend/src/pages/dossiers/TopicOverviewTab.tsx
    frontend/src/pages/dossiers/ForumOverviewTab.tsx
    frontend/src/pages/dossiers/WorkingGroupOverviewTab.tsx
    frontend/src/pages/dossiers/PersonOverviewTab.tsx
    frontend/src/pages/dossiers/ElectedOfficialOverviewTab.tsx
    frontend/src/pages/dossiers/overview-cards/SharedSummaryStatsCard.tsx
    frontend/src/domains/analytics/repositories/analytics.repository.ts
    frontend/src/domains/analytics/types/index.ts
    frontend/src/components/analytics/KpiCard.tsx
  </read_first>
  <action>
    **Create `frontend/src/components/analytics/DossierAnalyticsCard.tsx`** (per D-06):
    - Props: `{ dossierId: string, dossierType: DossierType }` where DossierType is the union of all 8 types
    - Renders a card with type-specific metrics. Use a switch on `dossierType` to show relevant metrics:
      - `country`: engagement count + active topics count
      - `organization`: member countries count + meeting frequency
      - `forum`: session count + upcoming session date
      - `engagement`: (skip -- engagements use workspace, not dossier overview)
      - `topic`: connected anchors count + position count
      - `working_group`: member count + deliverables count
      - `person`: interaction timeline count + organization affiliation count
      - `elected_official`: committee count + term status
    - Fetch data using analytics repository -- create `useAnalyticsForDossier(dossierId, dossierType)` hook that calls `analytics.repository.getDossierAnalytics(dossierId, dossierType)`. If this repository method does not exist, create it calling `/api/analytics/dossier/${dossierId}` endpoint (or compute from existing domain hooks -- read the analytics repository first to understand what's available).
    - Card styling: match existing overview card pattern from SharedSummaryStatsCard (same border, padding, rounded corners)
    - Title: "Analytics" / "التحليلات" (add to dossier i18n namespace)
    - Each metric rendered as a small KpiCard-like row: label + value pair
    - Loading: Skeleton matching card dimensions
    - RTL: logical properties only (ms-*, me-*, text-start, etc.)

    **Update each overview tab** to add DossierAnalyticsCard to the existing grid:
    - CountryOverviewTab: add `<DossierAnalyticsCard dossierId={dossierId} dossierType="country" />` to the grid
    - OrganizationOverviewTab: add with dossierType="organization"
    - TopicOverviewTab: add with dossierType="topic"
    - ForumOverviewTab: add with dossierType="forum"
    - WorkingGroupOverviewTab: add with dossierType="working_group"
    - PersonOverviewTab: add with dossierType="person"
    - ElectedOfficialOverviewTab: add with dossierType="elected_official"
    
    Place the DossierAnalyticsCard after SharedSummaryStatsCard (or first in grid if SharedSummaryStatsCard not present) in each overview tab. Follow the existing grid pattern: `grid grid-cols-1 md:grid-cols-2 gap-4 sm:gap-6`.
  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/Coding\ Space/Intl-Dossier-V2.0 && pnpm exec tsc --noEmit --project frontend/tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/analytics/DossierAnalyticsCard.tsx exists and exports `DossierAnalyticsCard`
    - DossierAnalyticsCard.tsx contains `dossierId` and `dossierType` in props
    - DossierAnalyticsCard.tsx contains switch or conditional logic for at least `country`, `organization`, `topic`, `person`
    - CountryOverviewTab.tsx contains `DossierAnalyticsCard` import
    - OrganizationOverviewTab.tsx contains `DossierAnalyticsCard` import
    - TopicOverviewTab.tsx contains `DossierAnalyticsCard` import
    - ForumOverviewTab.tsx contains `DossierAnalyticsCard` import
    - WorkingGroupOverviewTab.tsx contains `DossierAnalyticsCard` import
    - PersonOverviewTab.tsx contains `DossierAnalyticsCard` import
    - ElectedOfficialOverviewTab.tsx contains `DossierAnalyticsCard` import
    - No `ml-`, `mr-`, `pl-`, `pr-`, `text-left`, `text-right` in DossierAnalyticsCard.tsx
  </acceptance_criteria>
  <done>All 7 dossier overview tabs show a context-specific analytics card with type-tailored metrics. Cards follow existing overview card grid pattern.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes: `cd frontend && pnpm exec tsc --noEmit`
- OperationsHub renders AnalyticsWidget as first ZoneCollapsible zone
- All 7 dossier overview tabs include DossierAnalyticsCard
- No physical CSS properties (ml-, mr-, pl-, pr-, text-left, text-right) in new files
- i18n keys present in both EN and AR dashboard.json
</verification>

<success_criteria>
- Dashboard KPI widgets visible: 4 cards in 2x2 (mobile) / 4-col (sm+) grid as first dashboard zone
- Each of 7 dossier overview tabs shows type-specific analytics card
- Loading, error, and empty states handled for all analytics components
- RTL compliant with logical properties only
- Bundle budget maintained (AnalyticsWidget is inside ZoneCollapsible which is part of route-split dashboard)
</success_criteria>

<output>
After completion, create `.planning/phases/13-feature-absorption/13-01-SUMMARY.md`
</output>
