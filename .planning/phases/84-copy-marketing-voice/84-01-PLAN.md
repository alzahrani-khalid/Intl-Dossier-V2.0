---
phase: 84-copy-marketing-voice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/i18n/en/empty-states.json
  - frontend/src/i18n/en/guided-tours.json
  - frontend/src/i18n/en/relationships.json
  - frontend/src/i18n/en/progressive-disclosure.json
  - frontend/src/i18n/ar/empty-states.json
  - frontend/src/i18n/ar/guided-tours.json
  - frontend/src/i18n/ar/relationships.json
  - frontend/src/i18n/ar/progressive-disclosure.json
autonomous: true
requirements: [COPY-01]

must_haves:
  truths:
    - 'D-84-01: empty-states.json and guided-tours.json contain no user-visible exclamation marks — the 7 flagged empty-states strings (L201, L213, L408, L449, L482, L486, L491) and 14 flagged guided-tours strings (L14, L17, L36, L65, L74, L94, L95, L124, L149, L179, L204, L229, L254, L279) are reworded to calm sentence-case prose'
    - "D-84-02: 'Discover'/'discover' is removed from guided-tours.json (L59, L101), relationships.json (L35), and progressive-disclosure.json (L21), replaced with neutral verbs (Explore/See/Find/View/Browse) that keep meaning without marketing tone"
    - "D-84-03: empty-states.json L37 no longer reads 'easily accessible' (drop 'easily') and L287 no longer reads 'easy discovery' (reworded plain — 'easier to find' is acceptable)"
    - "D-84-04: guided-tours.json L17 no longer contains 'Let us show you around' nor '!' — replaced with a plain equivalent such as 'New here? Start with a quick tour.'"
    - "D-84-05: en drives ar — every changed en value's counterpart in frontend/src/i18n/ar/<file>.json mirrors the calm register (Arabic '!' removed where the en '!' was dropped; 'اكتشف'/'لاكتشاف'/'لسهولة الاكتشاف' marketing equivalents reworded); ar keys untouched"
    - "D-84-09: frontend/src/i18n/en/duplicate-detection.json is byte-untouched — L52 'This action cannot be easily undone' is a legitimate destructive-action warning, NOT marketing voice; it is the whitelisted false positive in the COPY-01 grep"
    - 'Values only, never keys: en/ar leaf-key sets of all four namespaces are unchanged (baseline leaf counts: empty-states 318, guided-tours 157, relationships 210, progressive-disclosure 92) and label-parity.test.ts passes'
    - 'Case-insensitive grep for discover|easily|unleash across frontend/src/i18n/en/ returns matches ONLY in duplicate-detection.json'
  artifacts:
    - path: frontend/src/i18n/en/empty-states.json
      provides: "Marketing-voice-free empty-state copy (no '!', no 'easily accessible', no 'easy discovery')"
    - path: frontend/src/i18n/en/guided-tours.json
      provides: "Marketing-voice-free tour copy (no '!', no 'Discover', no 'Let us show you around')"
    - path: frontend/src/i18n/en/relationships.json
      provides: "L35 connectDesc without 'Discover'"
    - path: frontend/src/i18n/en/progressive-disclosure.json
      provides: "L21 advanced hint without 'discover'"
    - path: frontend/src/i18n/ar/empty-states.json
      provides: 'Arabic mirror of corrected empty-states register'
    - path: frontend/src/i18n/ar/guided-tours.json
      provides: 'Arabic mirror of corrected guided-tours register'
    - path: frontend/src/i18n/ar/relationships.json
      provides: 'Arabic mirror of corrected relationships L35'
    - path: frontend/src/i18n/ar/progressive-disclosure.json
      provides: 'Arabic mirror of corrected progressive-disclosure L21'
  key_links:
    - from: 'frontend/src/i18n/index.ts'
      to: 'frontend/src/i18n/{en,ar}/{empty-states,guided-tours,relationships,progressive-disclosure}.json'
      via: 'static-bundled namespace imports (no http-backend)'
      pattern: 'empty-states|guided-tours|relationships|progressive-disclosure'
---

<objective>
Remove marketing voice from the four target `en` i18n namespaces and mirror the corrected calm
register into their `ar` counterparts, per COPY-01 (finding F15).

Purpose: CLAUDE.md voice rules ban "Discover", "Easily", "Unleash", exclamation marks, and
marketing phrasing in user-visible copy. These four namespaces are the only violators; `en` is
the source of truth and `ar` follows it.

Output: 8 edited JSON files (4 en + 4 ar), values only, key sets byte-identical to before.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-copy-marketing-voice/84-CONTEXT.md
@CLAUDE.md

Scope discipline (from 84-CONTEXT.md, locked):

- ONLY the 8 files in files_modified. No other namespace, no component copy.
- Values only — never add/remove/rename a key. `label-parity.test.ts` and the leaf-key parity
  gate below must stay green.
- Only the strings named in the tasks get reworded. Do NOT sweep the files for Title Case or
  other taste issues — F16–F21 taste calls are Phase 85.
- DO NOT TOUCH `frontend/src/i18n/en/duplicate-detection.json` (D-84-09 carve-out).
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Copy-edit the four en namespaces (D-84-01..04)</name>
  <files>frontend/src/i18n/en/empty-states.json, frontend/src/i18n/en/guided-tours.json, frontend/src/i18n/en/relationships.json, frontend/src/i18n/en/progressive-disclosure.json</files>
  <read_first>
    - .planning/phases/84-copy-marketing-voice/84-CONTEXT.md (D-84-01..04, D-84-09, Claude's Discretion)
    - frontend/src/i18n/en/empty-states.json (the 9 target lines below)
    - frontend/src/i18n/en/guided-tours.json (the 16 target lines below)
    - frontend/src/i18n/en/relationships.json (L35 only)
    - frontend/src/i18n/en/progressive-disclosure.json (L21 only)
  </read_first>
  <action>
    Edit string VALUES only (never keys) at exactly these sites. Replacement wording is Claude's
    discretion within these constraints: plain, sentence-case, no '!', no banned tokens
    (discover/easily/unleash, "Let us show you around", first-person "we"), meaning preserved.
    "easier" is acceptable (the ban is the marketing "Easily"/"discover" pairing).

    empty-states.json — D-84-01 ('!' removal, 7 sites) + D-84-03:
    - L201 "You're all caught up! New notifications will appear here." → drop '!' ("You're all caught up. …")
    - L213 "Great work! You have no pending tasks." → drop '!'
    - L408 "Great work! You currently have no pending assignments. …" → drop '!'
    - L449 title "Your Team is Active!" → calm sentence case, no '!' (e.g. "Your team is active")
    - L482 "Invitation sent successfully!" → "Invitation sent." or similar
    - L486 title "Be the First!" → calm sentence case, no '!' (e.g. "Be the first to contribute")
    - L491 "…Every contribution counts!" → drop '!'
    - L37 (D-84-03) "…organized and easily accessible." → drop "easily" ("…organized and accessible.")
    - L287 (D-84-03) "Add tags and link to relevant dossiers for easy discovery." → reword off
      "easy discovery" (e.g. "…to make them easier to find.")

    guided-tours.json — D-84-01 ('!' removal, 14 sites) + D-84-02 + D-84-04:
    - L14 "letsGo": "Let's Go!" → calm CTA value ("Get started" or "Continue"); key stays "letsGo"
    - L17 (D-84-04) "New here? Let us show you around!" → drop BOTH the marketing phrasing and
      the '!' (e.g. "New here? Start with a quick tour.")
    - L36 "Welcome to GASTAT Dossier!" → drop '!'
    - L65 "action": "Let's Go!" → same calm CTA as L14
    - L74 "Welcome to Dossiers!" → drop '!'
    - L94 "You're Ready to Go!" → calm sentence case, no '!'
    - L95 "That's all the basics! …" → drop '!' (period)
    - L124 "Start Building Your Network!" → sentence case, no '!'
    - L149 "Keep Your Documents Organized!" → sentence case, no '!'
    - L179 "Start Logging Engagements!" → sentence case, no '!'
    - L204 "Generate Your First Brief!" → sentence case, no '!'
    - L229 "Document Your Positions!" → sentence case, no '!'
    - L254 "Start Tracking MOUs!" → sentence case, no '!'
    - L279 "Track Your Commitments!" → sentence case, no '!'
    - L59 (D-84-02) "…to discover hidden connections…" → neutral verb (e.g. "…to find hidden connections…")
    - L101 (D-84-02) "Discover how to create relationships…" → neutral verb (e.g. "See how to create relationships…")

    relationships.json — D-84-02:
    - L35 "connectDesc": "Discover hidden patterns across your data." → neutral verb (e.g. "Find hidden patterns across your data.")

    progressive-disclosure.json — D-84-02:
    - L21 "advanced": "Return later to discover advanced features" → neutral verb (e.g. "Return later to explore advanced features")

    Record every changed key path + old/new value as you go — Task 2 mirrors exactly this set
    into ar. Do NOT touch en/duplicate-detection.json (D-84-09) or any other en file.

  </action>
  <verify>
    <automated>cd frontend && grep -c '!' src/i18n/en/empty-states.json; grep -c '!' src/i18n/en/guided-tours.json; grep -rliE 'discover|easily|unleash' src/i18n/en/; git status --porcelain src/i18n/en/duplicate-detection.json; node -e "['empty-states','guided-tours','relationships','progressive-disclosure'].forEach(f=>JSON.parse(require('fs').readFileSync('src/i18n/en/'+f+'.json','utf8')))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '!' frontend/src/i18n/en/empty-states.json` returns 0
    - `grep -c '!' frontend/src/i18n/en/guided-tours.json` returns 0
    - `grep -rliE 'discover|easily|unleash' frontend/src/i18n/en/` outputs EXACTLY one path: duplicate-detection.json (the D-84-09 whitelisted false positive)
    - `git status --porcelain frontend/src/i18n/en/duplicate-detection.json` outputs nothing (byte-untouched, D-84-09)
    - All four edited files still parse as JSON (node JSON.parse loop exits 0)
    - `git diff --stat frontend/src/i18n/en/` shows changes ONLY in the four target files
  </acceptance_criteria>
  <done>All 27 named en string values reworded to calm, sentence-case, no-'!', no-banned-token prose; keys unchanged; duplicate-detection.json untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the corrected register into ar (D-84-05) + run the full gate battery</name>
  <files>frontend/src/i18n/ar/empty-states.json, frontend/src/i18n/ar/guided-tours.json, frontend/src/i18n/ar/relationships.json, frontend/src/i18n/ar/progressive-disclosure.json</files>
  <read_first>
    - .planning/phases/84-copy-marketing-voice/84-CONTEXT.md (D-84-05, D-84-09)
    - Task 1's list of changed en key paths (the ar edit set is exactly the mirrors of these keys)
    - frontend/src/i18n/ar/empty-states.json, ar/guided-tours.json, ar/relationships.json, ar/progressive-disclosure.json (the mirror lines of the changed en keys)
  </read_first>
  <action>
    For EVERY en key changed in Task 1 (and ONLY those keys), update the same key's value in the
    corresponding frontend/src/i18n/ar/<file>.json to the same calm register:
    - Remove the Arabic-side '!' wherever the en '!' was dropped (ar/empty-states.json has 7 '!'
      lines, ar/guided-tours.json has 14 — the same key paths as en).
    - Reword the Arabic marketing equivalents at the mirror sites: ar/guided-tours.json L59
      ("لاكتشاف روابط مخفية") and L101 ("اكتشف كيفية…"), ar/relationships.json L35 ("اكتشف
      الأنماط المخفية…"), ar/progressive-disclosure.json L21 ("لاكتشاف الميزات المتقدمة"),
      ar/empty-states.json L287 ("لسهولة الاكتشاف") — use neutral Arabic verbs (e.g. "استكشف"،
      "اعرض"، "تعرف على"، "للعثور على") matching the new en meaning. Keep natural, formal Arabic.
    - CRITICAL non-target: ar/progressive-disclosure.json L175 "اكتشاف التعارضات" means
      "conflict detection" — its en counterpart was NOT changed, so leave it untouched. Never
      sweep ar independently for the token; mirror only changed en keys (en drives ar, D-84-05).
    - Do NOT add/remove/rename any ar key. Do NOT touch ar/duplicate-detection.json.
    Then run the full gate battery (verify below).
  </action>
  <verify>
    <automated>cd frontend && grep -c '!' src/i18n/ar/empty-states.json; grep -c '!' src/i18n/ar/guided-tours.json; node -e "const flat=(o,p='')=>Object.entries(o).flatMap(([k,v])=>v&&typeof v==='object'?flat(v,p+k+'.'):[p+k]);['empty-states','guided-tours','relationships','progressive-disclosure'].forEach(f=>{const en=flat(JSON.parse(require('fs').readFileSync('src/i18n/en/'+f+'.json','utf8'))).sort().join('\n');const ar=flat(JSON.parse(require('fs').readFileSync('src/i18n/ar/'+f+'.json','utf8'))).sort().join('\n');if(en!==ar){console.error('KEY DRIFT: '+f);process.exit(1)}});console.log('key parity OK')" && pnpm vitest run src/i18n/label-parity.test.ts && pnpm type-check && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '!' frontend/src/i18n/ar/empty-states.json` returns 0 and `grep -c '!' frontend/src/i18n/ar/guided-tours.json` returns 0 (D-84-05 mirror of the D-84-01 sweep)
    - ar/progressive-disclosure.json L175 "اكتشاف التعارضات" is UNCHANGED (legitimate "detection", en counterpart not in sweep)
    - Leaf-key parity node check prints "key parity OK" — en/ar key sets identical per file (baselines: 318/157/210/92)
    - `pnpm vitest run src/i18n/label-parity.test.ts` passes (the en/ar key-parity gate stays green)
    - `(cd frontend && pnpm type-check)` exits 0
    - `(cd frontend && pnpm lint)` clean (includes check-i18n-namespaces.mjs)
    - `git status --porcelain frontend/src/` shows changes ONLY in the 8 files_modified paths (duplicate-detection.json en+ar untouched)
  </acceptance_criteria>
  <done>Every changed en value's ar counterpart reads in the same calm register; key sets unchanged; label-parity + type-check + lint green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                                               |
| -------- | --------------------------------------------------------------------------------------------------------- |
| none new | Static-bundled i18n JSON string values only; no code, no keys, no input handling, no dependencies touched |

## STRIDE Threat Register

| Threat ID | Category  | Component         | Disposition | Mitigation Plan                                                                                                                                                            |
| --------- | --------- | ----------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-84-01   | Tampering | i18n JSON bundles | mitigate    | JSON.parse gate + label-parity test + lint (check-i18n-namespaces.mjs) prove structural integrity; values render via i18next interpolation (escaped by default), unchanged |

</threat_model>

<verification>
Phase-level COPY-01 gates (mirror of 84-CONTEXT §Specific Ideas):

1. `grep -rliE 'discover|easily|unleash' frontend/src/i18n/en/` → exactly `frontend/src/i18n/en/duplicate-detection.json` (D-84-09 whitelist)
2. `grep -c '!' frontend/src/i18n/en/empty-states.json` → 0; `grep -c '!' frontend/src/i18n/en/guided-tours.json` → 0 (and the two ar mirrors → 0)
3. Leaf-key parity node check over all four en/ar pairs → "key parity OK"
4. `(cd frontend && pnpm vitest run src/i18n/label-parity.test.ts)` → pass
5. `(cd frontend && pnpm type-check)` → exit 0
6. `(cd frontend && pnpm lint)` → clean
7. `git status --porcelain frontend/src/i18n/en/duplicate-detection.json frontend/src/i18n/ar/duplicate-detection.json` → empty
   </verification>

<success_criteria>

- COPY-01 satisfied: marketing voice removed from the four en namespaces; en drives ar
- All 7 phase-level gates above pass
- Exactly 8 files changed; zero key additions/removals/renames
  </success_criteria>

<output>
Create `.planning/phases/84-copy-marketing-voice/84-01-SUMMARY.md` when done
</output>
