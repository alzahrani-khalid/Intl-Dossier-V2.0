---
phase: 67-per-type-engagement-contracts-legacy-detail-cleanup
plan: 04
type: execute
wave: 2
depends_on: [67-01, 67-02]
files_modified:
  - frontend/src/components/dossier/CountryDossierDetail.tsx
  - frontend/src/components/dossier/ForumDossierDetail.tsx
  - frontend/src/components/dossier/OrganizationDossierDetail.tsx
  - frontend/src/components/dossier/PersonDossierDetail.tsx
  - frontend/src/components/dossier/TopicDossierDetail.tsx
  - frontend/src/components/dossier/WorkingGroupDossierDetail.tsx
  - frontend/src/pages/dossiers/CountryDossierPage.tsx
  - frontend/src/pages/dossiers/ForumDossierPage.tsx
  - frontend/src/pages/dossiers/OrganizationDossierPage.tsx
  - frontend/src/pages/dossiers/PersonDossierPage.tsx
  - frontend/src/pages/dossiers/TopicDossierPage.tsx
  - frontend/src/pages/dossiers/WorkingGroupDossierPage.tsx
  - frontend/src/pages/engagements/EngagementDetailPage.tsx
  - frontend/src/components/dossier/DossierDetailLayout.tsx
  - frontend/src/components/dossier/index.ts
autonomous: true
requirements: [PERENG-03]

key_decisions:
  - 'DELETE (not route) the entire legacy detail plane — RESEARCH census: zero external importers, zero routeTree.gen.ts references, zero test references; the routed dossier surface is DossierShell + lazy per-tab routes (260605-s32). Routing them would resurrect a parallel surface the per-tab architecture replaced'
  - 'One commit per closed sub-graph (65-03 discipline): commit 1 = the 13 root files (6 details + 6 page wrappers + EngagementDetailPage — pages import details, nothing imports pages); commit 2 = DossierDetailLayout + its barrel line (its only post-commit-1 importer is components/dossier/index.ts L40, a single line with no dependents — RESEARCH says deleting is clean here)'
  - 'Execution-time re-grep gates BEFORE each destructive commit (the RESEARCH census can go stale) — STOP if an unexpected importer appears. Known false positive to ignore: TypeCardErrorStates.test.tsx matches similarly-named overview CARDS, not these components'
  - 'sections/* and transitive orphans are NOT deleted here — they only become orphaned once this plan lands; 67-05 (wave 3) sweeps them with fresh grep gates'
  - 'Sequenced after wave 1 (depends_on 67-01/67-02) so whole-repo type-check/build gates run against a stable tree, not mid-task parallel edits'

must_haves:
  truths:
    - 'The 13 root dead-chain files no longer exist; no surviving file imports any of them'
    - 'DossierDetailLayout and its barrel export line are gone; components/dossier/index.ts still compiles'
    - 'pnpm type-check and pnpm build exit 0 after EACH destructive commit (verified by exit code — pre-commit build does not block on failure)'
  artifacts:
    - path: 'frontend/src/components/dossier/index.ts'
      provides: 'Barrel without the DossierDetailLayout export'
  key_links:
    - from: 'deletion commits'
      to: 'PERENG-03 grep gate (67-VALIDATION row 4)'
      via: 'zero surviving references to deleted module names'
      pattern: 'DossierDetail'
---

<objective>
Delete the legacy detail root chain: 6 `*DossierDetail` components, their 6 `pages/dossiers/*DossierPage` wrappers, the 812-line `EngagementDetailPage`, and `DossierDetailLayout` + its barrel line — gated on execution-time importer re-greps with green builds per commit.

Purpose: PERENG-03 — the plane is a fully closed dead graph (RESEARCH census); deleting it removes ~3,700 lines and the raw-key i18n debt that lives inside it.
Output: root chain gone; sections/\* and transitive leaves orphaned and ready for the 67-05 sweep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/67-per-type-engagement-contracts-legacy-detail-cleanup/67-PATTERNS.md

<interfaces>
<!-- Deletion set, commit 1 (13 files — paths verified on disk 2026-06-13):
     frontend/src/components/dossier/{Country,Forum,Organization,Person,Topic,WorkingGroup}DossierDetail.tsx
     frontend/src/pages/dossiers/{Country,Forum,Organization,Person,Topic,WorkingGroup}DossierPage.tsx
     frontend/src/pages/engagements/EngagementDetailPage.tsx
     NOTE: pages/dossiers/ also contains the LIVE overview-cards/ directory — delete
     ONLY the 6 wrapper files, never the directory. -->

<!-- Deletion set, commit 2:
     frontend/src/components/dossier/DossierDetailLayout.tsx
     frontend/src/components/dossier/index.ts L40: export { DossierDetailLayout } from './DossierDetailLayout'
     (remove only that line) -->

<!-- Gate shape (per module name, BEFORE deleting): grep -rn "<Name>" frontend/src
     and require every hit to be inside the deletion set itself (or the known
     TypeCardErrorStates false positive). After deleting: the same grep must be
     empty. grep exits 1 on no match — loop accordingly. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-grep gate + delete the 13-file root chain</name>
  <files>frontend/src/components/dossier/CountryDossierDetail.tsx, frontend/src/components/dossier/ForumDossierDetail.tsx, frontend/src/components/dossier/OrganizationDossierDetail.tsx, frontend/src/components/dossier/PersonDossierDetail.tsx, frontend/src/components/dossier/TopicDossierDetail.tsx, frontend/src/components/dossier/WorkingGroupDossierDetail.tsx, frontend/src/pages/dossiers/CountryDossierPage.tsx, frontend/src/pages/dossiers/ForumDossierPage.tsx, frontend/src/pages/dossiers/OrganizationDossierPage.tsx, frontend/src/pages/dossiers/PersonDossierPage.tsx, frontend/src/pages/dossiers/TopicDossierPage.tsx, frontend/src/pages/dossiers/WorkingGroupDossierPage.tsx, frontend/src/pages/engagements/EngagementDetailPage.tsx</files>
  <action>
    1. GATE (do not skip): for each of the 13 module names, grep -rn the bare name across frontend/src. Every hit must be (a) inside the 13-file deletion set, (b) the file defining it, or (c) the known TypeCardErrorStates.test.tsx false positive on overview-card names. ANY other hit → STOP, report the importer, do not delete (65-03 STOP rule). Also confirm routeTree.gen.ts has zero matches.
    2. git rm the 13 files in one commit (closed sub-graph). Run cd frontend && pnpm type-check and pnpm build — verify BOTH exit 0 yourself (pre-commit build does not block on failure, project memory). If either fails, the gate missed an importer: restore via git, investigate, STOP if unresolvable.
    3. Commit: chore(67-04): delete legacy *DossierDetail/*DossierPage root chain + EngagementDetailPage (PERENG-03).
  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0/frontend && for n in CountryDossierDetail ForumDossierDetail OrganizationDossierDetail PersonDossierDetail TopicDossierDetail WorkingGroupDossierDetail CountryDossierPage ForumDossierPage OrganizationDossierPage PersonDossierPage TopicDossierPage WorkingGroupDossierPage; do grep -rn "$n" src && exit 1; done; grep -rn "pages/engagements/EngagementDetailPage\|from './EngagementDetailPage'" src && exit 1; pnpm type-check && echo ROOT_CHAIN_CLEAN</automated>
  </verify>
  <done>13 files gone, zero surviving references, type-check + build green, single commit.</done>
</task>

<task type="auto">
  <name>Task 2: Delete DossierDetailLayout + barrel line, close with full gates</name>
  <files>frontend/src/components/dossier/DossierDetailLayout.tsx, frontend/src/components/dossier/index.ts</files>
  <action>
    1. GATE: grep -rn "DossierDetailLayout" frontend/src — after Task 1, hits must be ONLY its own file and the barrel line in components/dossier/index.ts. Any other importer → STOP and report (fallback: keep + record per the 65-03 barrel-API precedent instead of deleting).
    2. git rm DossierDetailLayout.tsx and remove ONLY the export line from components/dossier/index.ts (Edit tool — touch nothing else in the barrel).
    3. Run cd frontend && pnpm type-check, pnpm build, AND the full unit suite (pnpm exec vitest run) — wave-close sampling per 67-VALIDATION. Verify exit codes.
    4. Commit: chore(67-04): delete DossierDetailLayout and its barrel export (PERENG-03).
  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0/frontend && (grep -rn "DossierDetailLayout" src; test $? -eq 1) && pnpm type-check && pnpm exec vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Zero references to all 15 deleted modules; barrel compiles; full suite green; build exit 0
  </acceptance_criteria>
  <done>Root chain fully removed; sections/* + transitive leaves are now provably orphaned for 67-05.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                          |
| -------- | ---------------------------------------------------- |
| none new | pure dead-code deletion; no runtime state, no inputs |

## STRIDE Threat Register

| Threat ID | Category          | Component                     | Disposition | Mitigation Plan                                                                                                                                 |
| --------- | ----------------- | ----------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| T-67-09   | Denial of Service | accidental live-code deletion | mitigate    | Execution-time re-grep gate with STOP rule before every destructive commit; type-check + build + full suite per commit with exit codes verified |
| T-67-SC   | Tampering         | npm/pip/cargo installs        | accept      | No package installs in this plan                                                                                                                |

</threat_model>

<verification>
- 67-VALIDATION PERENG-03 grep-gate row partially realized (root-chain names); completed phase-wide in 67-05
- Per-commit: type-check + build exit codes; wave close: full vitest suite
</verification>

<success_criteria>

- Roadmap success criterion 3 (root half): every legacy unrouted \*DossierDetail component and wrapper is deleted with zero surviving references
  </success_criteria>

<output>
Create `.planning/phases/67-per-type-engagement-contracts-legacy-detail-cleanup/67-04-SUMMARY.md` when done (record per-commit gate evidence + any STOP events).
</output>
