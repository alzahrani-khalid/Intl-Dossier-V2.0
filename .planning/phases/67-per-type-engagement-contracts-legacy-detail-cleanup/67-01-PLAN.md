---
phase: 67-per-type-engagement-contracts-legacy-detail-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/dossier/tabs/DossierEngagementsTab.tsx
  - frontend/src/components/dossier/tabs/__tests__/DossierEngagementsTab.test.tsx
  - frontend/src/i18n/en/dossier-shell.json
  - frontend/src/i18n/ar/dossier-shell.json
  - frontend/src/routes/_protected/dossiers/organizations/$id/engagements.tsx
  - frontend/src/routes/_protected/dossiers/persons/$id/engagements.tsx
  - frontend/src/routes/_protected/dossiers/elected-officials/$id/engagements.tsx
autonomous: true
requirements: [PERENG-01, PERENG-02]

key_decisions:
  - 'PERENG-01 DECIDED: Option B (wire the read) per 67-RESEARCH recommendation — the org tab reads engagement_dossiers.host_organization_id, making the tab consistent with the shipped useOrganizations KPI tally. Option A (documented generic-only) rejected: it perpetuates the 260607-j2e KPI-vs-tab inconsistency. Option C (wizard host picker) NOT implemented — research names it optional scope; recorded as a deferred idea in the SUMMARY'
  - 'Per-type branches are SEPARATE useQuery calls inside DossierEngagementsTab (keys ["dossier-tab","engagements-hosted",id] and ["dossier-tab","engagements-participation",id]), gated by a new optional dossierType prop — per-section error granularity falls out of separate queries; the Phase 66 contract (throw, never swallow) applies to each queryFn AND retroactively to the generic branch (OVRERR-01 gap on this tab)'
  - 'Participation reads ONLY the canonical plane: engagement_participants ⋈ engagement_dossiers ⋈ dossiers. person_engagements and legacy engagements are NEVER touched (ENGPOS-01 precedent)'
  - '67-01 is the SINGLE OWNER of frontend/src/i18n/{en,ar}/dossier-shell.json this phase — the three new keys (sections.hostedEngagements/participation/history) land here, EN+AR same commit, parity unit-asserted'
  - 'Pitfall 6 (KPI counts hosted-only vs tab showing hosted+participation+generic) and Pitfall 7 (calendar past window = 7 days) are DOCUMENTED in the SUMMARY, not changed — no count badge is rendered (UI-SPEC A-4) so no number-vs-number contradiction exists in UI'

must_haves:
  truths:
    - 'An organization dossier whose id matches engagement_dossiers.host_organization_id rows shows a "Hosted engagements" section on its Engagements tab with linked engagement names and type badges'
    - 'A person or EO dossier with engagement_participants rows shows a "Participation" section with linked engagement names and bilingual role badges'
    - 'A per-type branch query success with 0 rows renders NO section at all (no heading, no empty line); a per-type branch error with no cached data renders the section heading + role="alert" sectionError line — failure never impersonates absence'
    - 'A generic-branch failure renders the role="alert" sectionError line instead of "No engagements yet" (OVRERR-01 closed on this tab)'
    - 'sections.hostedEngagements, sections.participation, sections.history exist in BOTH en and ar dossier-shell.json with the exact UI-SPEC strings'
  artifacts:
    - path: 'frontend/src/components/dossier/tabs/DossierEngagementsTab.tsx'
      provides: 'Hosted + participation sections, dossierType prop, Phase 66 error contract on all three branches'
      contains: 'engagement_participants'
    - path: 'frontend/src/components/dossier/tabs/__tests__/DossierEngagementsTab.test.tsx'
      provides: 'Hosted/participation render, absence-on-empty, forced-error (per-type + generic), History sub-heading, i18n parity tests'
      min_lines: 200
    - path: 'frontend/src/i18n/ar/dossier-shell.json'
      provides: 'AR section headings'
      contains: 'المشاركات المستضافة'
  key_links:
    - from: 'frontend/src/components/dossier/tabs/DossierEngagementsTab.tsx'
      to: 'engagement_participants / engagement_dossiers (PostgREST)'
      via: 'supabase two-step .eq() + .in() merge (names from dossiers, never an extension-table embed)'
      pattern: "from\\('engagement_participants'\\)"
    - from: 'frontend/src/routes/_protected/dossiers/organizations/$id/engagements.tsx'
      to: 'DossierEngagementsTab'
      via: 'dossierType="organization" prop (each route knows its type statically)'
      pattern: 'dossierType'
    - from: 'frontend/src/components/dossier/tabs/DossierEngagementsTab.tsx'
      to: 'frontend/src/i18n/en/dossier.json overview.sectionError (Phase 66 key)'
      via: "colon-form t('dossier:overview.sectionError')"
      pattern: 'dossier:overview.sectionError'
---

<objective>
Extend DossierEngagementsTab with the two per-type contract sections — "Hosted engagements" (org, reads host_organization_id) and "Participation" (person/EO, reads canonical engagement_participants) — and bring the whole tab up to the Phase 66 error contract, with the three new bilingual dossier-shell keys.

Purpose: PERENG-01 (decision implemented: wire the read) and the tab half of PERENG-02. Today the tab reads neither host_organization_id nor any participation table, and silently drops isError.
Output: per-type sections per the APPROVED 67-UI-SPEC anatomy + RED→GREEN test suite + EN/AR keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/67-per-type-engagement-contracts-legacy-detail-cleanup/67-UI-SPEC.md
@.planning/phases/67-per-type-engagement-contracts-legacy-detail-cleanup/67-RESEARCH.md

<interfaces>
<!-- Current tab (129 lines, read it once in full): single useQuery on
     fetchDossierOverview(['related_dossiers','calendar_events']), destructures
     { data, isLoading } WITHOUT isError, renders empty.engagements.title on 0 entries.
     Existing chrome (History icon h3, dot-timeline, badge pill) is UNCHANGED. -->

<!-- New prop (literal union local to the tab — only these three routes pass it): -->

```typescript
interface DossierEngagementsTabProps {
  dossierId: string
  dossierType?: 'organization' | 'person' | 'elected_official'
}
```

<!-- Branch queries (RESEARCH Pattern 2 — names live on dossiers, NEVER an embed
     on the extension table). Each queryFn throws DossierOverviewAPIError
     (exported from '@/services/dossier-overview.service') on ANY step error: -->

```typescript
// hosted (enabled: dossierType === 'organization'), key ['dossier-tab','engagements-hosted',dossierId]
const { data: ext, error } = await supabase
  .from('engagement_dossiers')
  .select('id, engagement_type, start_date')
  .eq('host_organization_id', dossierId)
// throw new DossierOverviewAPIError('Failed to fetch hosted engagements', 500, 'HOSTED_ENGAGEMENTS_FETCH_FAILED', error.message)
// then names: supabase.from('dossiers').select('id, name_en, name_ar, created_at').in('id', ids) — throw on error too

// participation (enabled: dossierType === 'person' || dossierType === 'elected_official'),
// key ['dossier-tab','engagements-participation',dossierId]
// step 1: from('engagement_participants').select('engagement_id, role').eq('participant_dossier_id', dossierId)
// step 2: from('engagement_dossiers').select('id, engagement_type, start_date').in('id', engagementIds)
// step 3: from('dossiers').select('id, name_en, name_ar, created_at').in('id', engagementIds)
// merge → rows { id, name (locale-picked), date: start_date ?? created_at, role } — code PARTICIPATION_FETCH_FAILED
```

<!-- Section anatomy (UI-SPEC §1, binding): h3 className "text-xs font-semibold
     text-muted-foreground uppercase tracking-wider", space-y-2 to a
     "rounded-md border divide-y" list; row "flex items-center justify-between
     gap-3 px-3 min-h-[var(--row-h)]"; row start = <Link to={getDossierDetailPath(id,'engagement')}
     className="text-sm font-medium truncate min-w-0 text-start hover:underline">;
     row end = formatDayFirst date (text-xs text-muted-foreground) then ONE badge
     pill "bg-muted text-muted-foreground text-xs px-2 py-0.5 rounded-full".
     Sort start_date DESC (fallback dossiers.created_at); never .reverse().
     RENDER RULE: success+0 rows → section fully ABSENT. error && data == null →
     heading + <p role="alert" className="text-sm text-[var(--danger)] text-center py-8">
     {t('dossier:overview.sectionError', { defaultValue: 'Failed to load this section. Check your connection and try again.' })}</p>.
     Cached rows + refetch failure → rows, no error line (stale-while-error). -->

<!-- Badges (colon-form, defaultValue slug fallback):
     hosted   → t('engagements:types.' + engagement_type, { defaultValue: engagement_type })
     particip → t('engagements:participantRoles.' + role, { defaultValue: role })
     Both key blocks exist EN+AR (verified: host/guest/delegate/head_of_delegation/
     speaker/observer/organizer/support_staff/interpreter/other; bilateral_meeting/...). -->

<!-- Tab-level interplay (UI-SPEC §4): "History" sub-heading (same h3 class,
     t('sections.history')) renders above the generic timeline ONLY when a per-type
     section rendered above it (rows or error) AND the generic block itself renders
     content (entries or its error line) — no dead headings over nothing.
     Tab-level empty copy renders ONLY when generic entries AND per-type rows are all
     empty AND no branch errored. Generic branch gains isError: isError && data == null
     → sectionError line in the existing empty-line slot. Skeleton: TabSkeleton while
     ANY enabled query isLoading (disabled queries report isLoading false). -->

<!-- i18n — the phase's three new keys, NEW top-level "sections" object in dossier-shell.json
     (verified: no existing top-level "sections" key; current keys are tabs/header/sidebar/
     graph/empty/error/emptyState). EXACT strings (UI-SPEC, binding): -->

```jsonc
// en: "sections": { "hostedEngagements": "Hosted engagements", "participation": "Participation", "history": "History" }
// ar: "sections": { "hostedEngagements": "المشاركات المستضافة", "participation": "المشاركة", "history": "السجل" }
```

<!-- Route swap (all three routes are byte-similar 65-01 lazy+Suspense files):
     only change is passing dossierType to <DossierEngagementsTab dossierId={id} dossierType="..."/>.
     organizations → "organization"; persons → "person"; elected-officials → "elected_official".
     Keep TabSkeleton type="list" fallback. The other 4 type routes are NOT touched. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the tab test suite — RED first</name>
  <files>frontend/src/components/dossier/tabs/__tests__/DossierEngagementsTab.test.tsx</files>
  <read_first>
    - frontend/src/components/dossier/tabs/__tests__/DossierEngagementsTab.test.tsx (existing 105-line file: react-i18next echo mock, Link-as-anchor mock, fetchDossierOverview mock, makeResponse/renderWithClient helpers — extend, do not rewrite)
    - frontend/src/components/dossier/tabs/DossierEngagementsTab.tsx (full read once)
  </read_first>
  <behavior>
    - Add a vi.hoisted per-table supabase mock for '@/lib/supabase': from(table) returns a self-returning chainable (select/eq/in) whose awaited terminal resolves the per-test configured { data, error } for that table. Tables used: engagement_dossiers, engagement_participants, dossiers. Reset the table map in beforeEach.
    - Test (RED): org type + hosted rows — configure engagement_dossiers .eq result [{ id: 'e1', engagement_type: 'bilateral_meeting', start_date: '2026-05-01' }] and dossiers .in result [{ id: 'e1', name_en: 'OECD Summit', name_ar: 'قمة', created_at: '2026-04-01' }]; render with dossierType="organization" (generic mock resolves empty) → expect 'Hosted engagements' heading, 'OECD Summit' link, 'bilateral_meeting' badge (echo mock returns defaultValue).
    - Test (RED): person type + participation rows — engagement_participants → [{ engagement_id: 'e1', role: 'delegate' }], engagement_dossiers .in → ext row, dossiers .in → name row; render with dossierType="person" → expect 'Participation' heading, name link, 'delegate' badge.
    - Test (RED): absence rule — person type, participation success with data: [] → expect queryByText('Participation') to be null AND no role="alert".
    - Test (RED): per-type error ≠ empty — person type, engagement_participants resolves { data: null, error: { message: 'forced' } } → expect 'Participation' heading PRESENT, role="alert" with the sectionError defaultValue copy, and 'No engagements yet' ABSENT.
    - Test (RED): generic error — fetchDossierOverview rejects (no dossierType prop) → expect role="alert" sectionError copy and 'No engagements yet' ABSENT (current code shows the empty state — this is the OVRERR-01 pin).
    - Test (RED): History sub-heading — person type with participation rows AND generic entries → 'History' present; no dossierType (generic entries only) → 'History' absent.
    - Test (RED): i18n parity — direct-import en/ar dossier-shell.json; assert sections.hostedEngagements/participation/history exist non-empty in BOTH, and ar.sections.hostedEngagements === 'المشاركات المستضافة'.
    - Existing 2 tests must stay GREEN unchanged (no dossierType → branch queries disabled → no supabase calls).
  </behavior>
  <action>
    Extend the test file following its existing patterns (echo t honouring defaultValue — extend the special-case map so 'empty.engagements.title' still returns 'No engagements yet'). Keep QueryClient retry: false. Run the file: new behavior tests MUST fail, existing two MUST pass. Commit: test(67-01): add failing per-type section + error-contract tests for DossierEngagementsTab.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/components/dossier/tabs/__tests__/DossierEngagementsTab.test.tsx 2>&1 | grep -qE '[1-9][0-9]* failed' && echo RED_CONFIRMED</automated>
  </verify>
  <done>67-VALIDATION rows 1-2 (unit halves) realized as failing tests incl. the Wave-0 forced-error gap; existing tests green; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement sections + error contract + i18n keys + route props — GREEN</name>
  <files>frontend/src/components/dossier/tabs/DossierEngagementsTab.tsx, frontend/src/i18n/en/dossier-shell.json, frontend/src/i18n/ar/dossier-shell.json, frontend/src/routes/_protected/dossiers/organizations/$id/engagements.tsx, frontend/src/routes/_protected/dossiers/persons/$id/engagements.tsx, frontend/src/routes/_protected/dossiers/elected-officials/$id/engagements.tsx</files>
  <read_first>
    - frontend/src/routes/_protected/dossiers/organizations/$id/engagements.tsx (one of the three byte-similar routes — read one, edit all three identically)
  </read_first>
  <action>
    1. DossierEngagementsTab: add the optional dossierType prop and the two enabled-gated useQuery branches exactly per the interfaces block (separate query keys, STALE_TIME.NORMAL, queryFns importing supabase from '@/lib/supabase' and DossierOverviewAPIError from '@/services/dossier-overview.service'; throw on EVERY step error — never swallow into ?? []). Render the per-type section per the UI-SPEC anatomy in the interfaces block, between the existing tab header and the generic timeline: org → sections.hostedEngagements with types.* badge; person/EO → sections.participation with participantRoles.* badge. Apply the render rule (absent on success+0 rows; heading + role="alert" line on error with no cached data; stale-while-error keeps rows). Add isError to the generic query destructure and render the sectionError line in the empty-line slot when isError && data == null. Add the conditional History sub-heading per the interplay rules. Skeleton while any enabled query isLoading. Logical properties only; no new dir attribute (inherit the tab root); ESLint explicit return types; t lookups colon-form for cross-namespace keys (dot-form leaks raw keys — project memory).
    2. Add the "sections" object with the three exact EN and AR strings to both dossier-shell.json files in this same commit; touch nothing else in them.
    3. Edit the three route files to pass dossierType (organization/person/elected_official). Do NOT touch countries/forums/topics/working_groups routes.
    4. Run the Task 1 file (all GREEN), pnpm type-check, and eslint on the touched files. Commit: feat(67-01): per-type hosted/participation sections + error contract on DossierEngagementsTab (PERENG-01, PERENG-02).
    5. Note for the SUMMARY (do not change code): KPI divergence (Pitfall 6) and 7-day calendar window (Pitfall 7) documented; Option C (wizard host picker) deferred.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/components/dossier/tabs/__tests__/DossierEngagementsTab.test.tsx && pnpm type-check && grep -q "engagement_participants" src/components/dossier/tabs/DossierEngagementsTab.tsx && ! grep -q "person_engagements" src/components/dossier/tabs/DossierEngagementsTab.tsx && python3 -c "import json; en=json.load(open('src/i18n/en/dossier-shell.json')); ar=json.load(open('src/i18n/ar/dossier-shell.json')); assert en['sections']['hostedEngagements']=='Hosted engagements'; assert ar['sections']['hostedEngagements']=='المشاركات المستضافة'; assert ar['sections']['participation']=='المشاركة'; assert ar['sections']['history']=='السجل'; print('I18N_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All Task 1 tests green; existing tests green; type-check exit 0
    - Tab never reads person_engagements or legacy engagements; names come from dossiers via .in() merge (no extension-table embed)
    - Empty per-type sections are fully absent; errored branches render heading + role="alert"; generic error never renders the empty copy
    - Three keys EN+AR in the same commit
  </acceptance_criteria>
  <done>Org/person/EO Engagements tabs honor their per-type contracts in unit-verified form; live proof deferred to 67-06.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                                          |
| ------------------------- | -------------------------------------------------------------------- |
| browser → PostgREST reads | new branch queries ride existing permissive SELECT RLS (Q5 verified) |

## STRIDE Threat Register

| Threat ID | Category                 | Component                       | Disposition | Mitigation Plan                                                                                                     |
| --------- | ------------------------ | ------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------- |
| T-67-01   | Repudiation (of failure) | per-type branch queries         | mitigate    | Phase 66 contract on all three branches — errors throw and render role="alert", never impersonate empty             |
| T-67-02   | Information Disclosure   | DossierOverviewAPIError.details | mitigate    | details carries PostgREST messages for console/debug only; UI renders only the generic localized sectionError key   |
| T-67-03   | Elevation of Privilege   | branch reads                    | accept      | Read-only queries under existing SELECT RLS (verified live, Q5); no policy changes, no new write paths in this plan |
| T-67-SC   | Tampering                | npm/pip/cargo installs          | accept      | Zero package installs this phase (RESEARCH: Installation: none)                                                     |

</threat_model>

<verification>
- 67-VALIDATION rows realized: PERENG-01 unit row + PERENG-02 unit (tab) row + Wave-0 forced-error gap
- Per-task gate: quick vitest run of the touched file + pnpm type-check
</verification>

<success_criteria>

- Roadmap success criterion 1 (unit half): hosted engagement renders on the org tab from mocked branches; decision (Option B) implemented and recorded
- Roadmap success criterion 2 (tab half): participation rows render on person/EO with bilingual role badges
- OVRERR-01 contract closed on this tab (error ≠ empty, unit-pinned)
  </success_criteria>

<output>
Create `.planning/phases/67-per-type-engagement-contracts-legacy-detail-cleanup/67-01-SUMMARY.md` when done (record: Option B decision, Pitfall 6/7 documentation notes, Option C deferral).
</output>
