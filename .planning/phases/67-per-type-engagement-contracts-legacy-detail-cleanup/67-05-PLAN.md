---
phase: 67-per-type-engagement-contracts-legacy-detail-cleanup
plan: 05
type: execute
wave: 3
depends_on: [67-04]
files_modified:
  - frontend/src/components/dossier/sections/CommitteeAssignments.tsx
  - frontend/src/components/dossier/sections/ContactPreferencesSection.tsx
  - frontend/src/components/dossier/sections/DecisionLogs.tsx
  - frontend/src/components/dossier/sections/DeliverablesTracker.tsx
  - frontend/src/components/dossier/sections/ElectedOfficialProfile.tsx
  - frontend/src/components/dossier/sections/EngagementInformation.tsx
  - frontend/src/components/dossier/sections/FollowUpActions.tsx
  - frontend/src/components/dossier/sections/InstitutionalProfile.tsx
  - frontend/src/components/dossier/sections/InteractionHistory.tsx
  - frontend/src/components/dossier/sections/MeetingSchedule.tsx
  - frontend/src/components/dossier/sections/MemberOrganizations.tsx
  - frontend/src/components/dossier/sections/OrgHierarchy.tsx
  - frontend/src/components/dossier/sections/OrganizationAffiliations.tsx
  - frontend/src/components/dossier/sections/OutcomesSummary.tsx
  - frontend/src/components/dossier/sections/ParticipantsList.tsx
  - frontend/src/components/dossier/sections/PositionsHeld.tsx
  - frontend/src/components/dossier/sections/ProfessionalProfile.tsx
  - frontend/src/components/dossier/sections/StaffDirectory.tsx
  - frontend/src/components/dossier/sections/TermHistory.tsx
  - frontend/src/components/dossier/CollapsibleSection.tsx
  - frontend/src/components/timeline/CountryTimeline.tsx
  - frontend/src/components/timeline/OrganizationTimeline.tsx
  - frontend/src/components/timeline/PersonTimeline.tsx
  - frontend/src/components/timeline/EngagementTimeline.tsx
  - frontend/src/components/key-contacts-panel/KeyContactsPanel.tsx
  - frontend/src/components/engagements/ForumSessionCreator.tsx
  - frontend/src/components/engagements/EngagementBriefsSection.tsx
  - frontend/src/components/dossier-timeline/DossierTimeline.tsx
  - frontend/src/types/person.types.ts
autonomous: true
requirements: [PERENG-02, PERENG-03]

key_decisions:
  - 'The InteractionHistory mapping is NOT salvaged as code: the participation contract was rebuilt in 67-01 per the APPROVED UI-SPEC (registered dossier-shell/engagements namespaces, canonical engagement_dossiers ids), making the legacy renderer (legacy dossier:sections.person.* keys, legacy /engagements/$id id-space risk) fully superseded — delete it with the rest'
  - 'EngagementTimeline.tsx (pre-existing zero-importer dead code, not in the timeline barrel) is deleted in the same sweep IF its execution-time grep is clean — RESEARCH names it same-class; record if kept'
  - 'PersonEngagementWithDetails type honesty lands HERE (Task 3), deliberately after InteractionHistory is deleted (Task 1) — it was the last typed consumer of the legacy link fields (link.attended/link.notes); rewriting earlier would break type-check/lint mid-phase. PersonDetailPage accesses link via any-cast: canonical rows simply never render the Attended badge (runtime-safe)'
  - 'KEEP + record (65-03 barrel-API precedent): InteractiveTimeline/CompactInteractiveTimeline (timeline barrel), stakeholder-timeline barrel, multilingual barrel. Dead i18n key blocks in dossier.json are recorded, not swept (only removable with per-key grep proof — low value)'
  - 'person_engagements is recorded as legacy-dead (FK→legacy engagements, 0 rows, 0 writers; never read/write from new code) — no destructive DB migration this phase'

must_haves:
  truths:
    - 'components/dossier/sections/ no longer exists; the 8 transitive orphan files (+ EngagementTimeline if grep-clean) are deleted; zero surviving references to any deleted module'
    - 'PersonEngagementWithDetails.link reflects the canonical engagement_participants row (participant_type/participant_dossier_id/attendance_status present; legacy person_id/attended gone) — the 67-03 migration emission and the type agree'
    - 'pnpm type-check, pnpm build, and the full vitest suite are green; size-limit reports ZERO exceeded lines (whole log grepped)'
    - 'Barrel-kept components, dead i18n key blocks, and the person_engagements legacy-dead decision are recorded in the SUMMARY'
  artifacts:
    - path: 'frontend/src/types/person.types.ts'
      provides: 'Canonical participation link shape for recent_engagements'
      contains: 'participant_dossier_id'
  key_links:
    - from: 'frontend/src/types/person.types.ts'
      to: 'supabase/migrations/20260613100000_get_person_full_recent_engagements_canonical.sql'
      via: 'link = to_jsonb(engagement_participants row); engagement = {id,name_en,name_ar,engagement_type,engagement_category,location_en,location_ar} — type mirrors emission'
      pattern: 'participant_type'
---

<objective>
Sweep the now-orphaned leaves: all 19 components/dossier/sections files, the 8 transitive orphans created by the 67-04 root deletion (+ pre-existing dead EngagementTimeline), then make the recent_engagements TypeScript contract honest against the canonical emission, and close PERENG-03 with the phase-wide grep gate, full suite, and size-limit.

Purpose: PERENG-03 completion (the raw-key i18n debt dies with these files; survivors all use registered namespaces) + the PERENG-02 type-honesty tail that had to wait for InteractionHistory's deletion.
Output: dead plane fully gone; honest types; recorded follow-ups.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/67-per-type-engagement-contracts-legacy-detail-cleanup/67-PATTERNS.md

<interfaces>
<!-- Transitive orphan set (paths verified on disk; sole importers were all deleted in 67-04):
     frontend/src/components/dossier/CollapsibleSection.tsx        (not barrel-exported)
     frontend/src/components/timeline/CountryTimeline.tsx          (not in timeline barrel)
     frontend/src/components/timeline/OrganizationTimeline.tsx     (not in barrel)
     frontend/src/components/timeline/PersonTimeline.tsx           (not in barrel)
     frontend/src/components/timeline/EngagementTimeline.tsx       (pre-existing dead; not in barrel)
     frontend/src/components/key-contacts-panel/KeyContactsPanel.tsx  (dir contains ONLY this file — remove the dir)
     frontend/src/components/engagements/ForumSessionCreator.tsx
     frontend/src/components/engagements/EngagementBriefsSection.tsx
     frontend/src/components/dossier-timeline/DossierTimeline.tsx     (dir contains ONLY this file — remove the dir)
     GREP CAUTION: DossierActivityTimeline (LIVE, routed) and the timeline barrel's
     InteractiveTimeline are DIFFERENT modules — gate on exact names/import paths;
     "DossierTimeline" is not a substring of "DossierActivityTimeline" but verify hits by path.
     KeyContactsCard (LIVE overview card) ≠ KeyContactsPanel. -->

<!-- Type rewrite target (frontend/src/types/person.types.ts — PersonEngagement,
     currently { id, person_id, engagement_id, role, notes?, attended, created_at, created_by? }).
     Canonical engagement_participants row shape (migration 20260110000006, line-verified): -->

```typescript
/** Canonical engagement_participants row — the `link` half of recent_engagements (PERENG-02). */
export interface PersonEngagement {
  id: string
  engagement_id: string
  participant_type: 'person' | 'organization' | 'country' | 'external'
  participant_dossier_id: string | null
  role: EngagementRole
  attendance_status: string
  notes?: string | null
  created_at: string
  created_by?: string | null
}
```

<!-- PersonEngagementWithDetails.engagement stays AS-IS ({ id, name_en, name_ar,
     engagement_type, engagement_category, location_en?, location_ar? }) — the 67-03
     migration emits exactly these fields. PersonFullProfile residual drift
     (current_role/roles/affiliations/relationships keys absent from the live RPC) is
     PRE-EXISTING and out of scope — record it, do not widen the edit. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-grep gate + delete all 19 sections (incl. InteractionHistory)</name>
  <files>frontend/src/components/dossier/sections/ (all 19 files)</files>
  <action>
    1. GATE: grep -rn "dossier/sections" frontend/src and per-name greps for all 19 component names. After 67-04, every hit must be inside components/dossier/sections itself (intra-set imports are fine — the whole set dies together). Any external hit → STOP and report.
    2. git rm -r frontend/src/components/dossier/sections (one commit — closed sub-graph).
    3. cd frontend && pnpm type-check && pnpm build — verify exit codes yourself.
    4. Commit: chore(67-05): delete orphaned components/dossier/sections plane (19 files) (PERENG-03).
  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0/frontend && test ! -d src/components/dossier/sections && (grep -rn "dossier/sections" src; test $? -eq 1) && pnpm type-check && echo SECTIONS_CLEAN</automated>
  </verify>
  <done>sections/ directory gone, zero references, gates green, single commit.</done>
</task>

<task type="auto">
  <name>Task 2: Re-grep gate + delete the transitive orphans</name>
  <files>frontend/src/components/dossier/CollapsibleSection.tsx, frontend/src/components/timeline/CountryTimeline.tsx, frontend/src/components/timeline/OrganizationTimeline.tsx, frontend/src/components/timeline/PersonTimeline.tsx, frontend/src/components/timeline/EngagementTimeline.tsx, frontend/src/components/key-contacts-panel/KeyContactsPanel.tsx, frontend/src/components/engagements/ForumSessionCreator.tsx, frontend/src/components/engagements/EngagementBriefsSection.tsx, frontend/src/components/dossier-timeline/DossierTimeline.tsx</files>
  <action>
    1. GATE per file (the interfaces-block caution applies — verify hits by import path, not substring): grep -rn each exact component name + its import path across frontend/src. Expect zero hits outside the file itself. EngagementTimeline: if a live importer surfaces, KEEP it and record (same-class optional candidate); for the other 8 an unexpected importer is a STOP.
    2. git rm the grep-clean files; remove the now-empty key-contacts-panel/ and dossier-timeline/ directories. Do NOT touch components/timeline/index.ts (InteractiveTimeline stays barrel-exported), stakeholder-timeline/, or multilingual/.
    3. cd frontend && pnpm type-check && pnpm build && pnpm exec vitest run — verify exit codes.
    4. Commit: chore(67-05): delete transitive orphans of the legacy detail plane (PERENG-03).
  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0/frontend && for n in CollapsibleSection CountryTimeline OrganizationTimeline PersonTimeline KeyContactsPanel ForumSessionCreator EngagementBriefsSection; do grep -rn "$n" src && exit 1; done; (grep -rn "dossier-timeline/DossierTimeline\|from './DossierTimeline'" src; test $? -eq 1) && pnpm type-check && echo ORPHANS_CLEAN</automated>
  </verify>
  <done>Orphans deleted (or kept+recorded per the EngagementTimeline carve-out), full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Canonical link type + phase gates + records</name>
  <files>frontend/src/types/person.types.ts</files>
  <read_first>
    - frontend/src/types/person.types.ts (PersonEngagement ~L260-278, PersonEngagementWithDetails ~L283-294, PersonFullProfile ~L72-79 — one read)
  </read_first>
  <action>
    1. Rewrite PersonEngagement to the canonical shape in the interfaces block (doc comment naming PERENG-02 + the 67-03 migration); leave PersonEngagementWithDetails.engagement and PersonFullProfile untouched. grep -rn "\.attended\|person_id" usages of the type to confirm no typed consumer remains (InteractionHistory was the last — deleted in Task 1; PersonDetailPage reads via any-cast).
    2. cd frontend && pnpm type-check && pnpm exec vitest run && pnpm build.
    3. Phase-wide PERENG-03 grep gate (67-VALIDATION row 4): run the full deleted-name sweep (root names from 67-04 + all Task 1/2 names) — zero hits.
    4. pnpm exec size-limit — grep the WHOLE log for 'exceeded' (must be zero lines; deletions should shrink budgets, project memory: failures can stack hidden).
    5. Commit: refactor(67-05): make recent_engagements link type canonical (PERENG-02).
    6. SUMMARY records (no code): barrel-kept follow-ups (InteractiveTimeline/CompactInteractiveTimeline, stakeholder-timeline, multilingual); dead dossier.json sections.* key blocks left in place (per-key grep proof required to sweep — recorded follow-up); person_engagements declared legacy-dead (never read/write from new code; no destructive migration); PersonFullProfile residual drift (current_role/roles/affiliations/relationships not in live RPC — pre-existing, out of scope).
  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0/frontend && grep -q 'participant_dossier_id' src/types/person.types.ts && pnpm type-check && pnpm exec vitest run 2>&1 | tail -3 && pnpm exec size-limit 2>&1 | (grep -c 'exceeded'; true) | head -1</automated>
  </verify>
  <acceptance_criteria>
    - Type mirrors the 67-03 emission; zero exceeded budget lines; full suite + build green
    - All four records present in the SUMMARY
  </acceptance_criteria>
  <done>PERENG-03 complete in-repo; PERENG-02 type contract honest; phase ready for the 67-06 live gate.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                       |
| -------- | ------------------------------------------------- |
| none new | dead-code deletion + type edit; no runtime inputs |

## STRIDE Threat Register

| Threat ID | Category          | Component                     | Disposition | Mitigation Plan                                                                                                                                                             |
| --------- | ----------------- | ----------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-67-10   | Denial of Service | accidental live-code deletion | mitigate    | Per-file execution-time grep with import-path verification (DossierActivityTimeline/KeyContactsCard lookalike caution) + STOP rule + per-commit type-check/build/full-suite |
| T-67-11   | Repudiation       | lying type contract           | mitigate    | 260605-r92 class — link type rewritten to match the actual RPC emission so consumers cannot trust phantom fields                                                            |
| T-67-SC   | Tampering         | npm/pip/cargo installs        | accept      | No package installs in this plan                                                                                                                                            |

</threat_model>

<verification>
- 67-VALIDATION PERENG-03 grep gate realized phase-wide; type/build/size gates realized
</verification>

<success_criteria>

- Roadmap success criterion 3 complete: nothing of the legacy plane survives, and every surviving phase surface renders registered-namespace strings in EN and AR
  </success_criteria>

<output>
Create `.planning/phases/67-per-type-engagement-contracts-legacy-detail-cleanup/67-05-SUMMARY.md` when done (include the four records and total lines deleted).
</output>
