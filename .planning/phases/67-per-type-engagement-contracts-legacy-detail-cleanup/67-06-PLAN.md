---
phase: 67-per-type-engagement-contracts-legacy-detail-cleanup
plan: 06
type: execute
wave: 4
depends_on: [67-01, 67-02, 67-03, 67-04, 67-05]
files_modified: []
autonomous: true
requirements: [PERENG-01, PERENG-02, PERENG-03]

key_decisions:
  - 'ORCHESTRATOR-RUN plan: executor agents have NO Supabase MCP and NO browser — the orchestrator executes this plan inline (64-01/66-08 precedent). It applies the 67-03 migration via Supabase MCP, seeds/cleans staging rows, and runs the browser EN/AR protocol'
  - 'Migration safety: capture SELECT get_person_full(<id>) BEFORE applying; after applying, the three pre-existing keys (person/active_committees/key_staff) must deep-equal the pre-capture — the rename-and-wrap composition guarantees this; the comparison proves it'
  - 'EO leg verified LIVE without a subtype flip: Q2 found exactly 1 person with person_subtype = elected_official — resolve its id at seed time and seed a participation row for its dossier'
  - 'Forced branch failure is a CLIENT-SIDE network block (CDP Network.setBlockedURLs on rest/v1/engagement_participants) — never RLS/grant tampering (66-08 lesson: tampering returns empty 200s and exercises the EMPTY path)'
  - 'All seeds are reverted in the same session; the pre-existing participation row (b0000011-…, head_of_delegation) must be untouched at close'

must_haves:
  truths:
    - 'The 67-03 migration is applied on staging; SELECT get_person_full returns the three pre-existing keys unchanged plus recent_engagements'
    - 'An org dossier with a seeded host_organization_id engagement shows "Hosted engagements" with a working link, EN + AR (PERENG-01 live)'
    - 'A person dossier AND the EO dossier with seeded participation rows show "Participation" with the bilingual role badge; the persons edge returns non-empty recent_engagements for the seeded person (PERENG-02 live)'
    - 'A wizard-created engagement with one participant persists an engagement_participants row with created_by set (write fix live)'
    - 'A CDP-forced participation failure renders the heading + role="alert" Arabic error copy under dir=rtl — never the empty state; no overflow at 1280/1024'
    - 'All seeds cleaned up (host NULL again, seeded participant rows deleted, wizard test dossier deleted); full gates green (vitest, type-check, build, size-limit zero exceeded)'
  artifacts:
    - path: '.planning/phases/67-per-type-engagement-contracts-legacy-detail-cleanup/67-06-SUMMARY.md'
      provides: 'Live verification matrix: per-requirement evidence, pre/post RPC key comparison, seed + cleanup confirmations, AR/width evidence, gate outputs'
  key_links:
    - from: 'Supabase MCP apply_migration'
      to: 'supabase/migrations/20260613100000_get_person_full_recent_engagements_canonical.sql'
      via: 'orchestrator applies the executor-authored file verbatim'
      pattern: 'get_person_full_base'
    - from: 'seeded engagement_participants row'
      to: '/dossiers/persons/<id>/engagements Participation section'
      via: 'canonical reverse-lookup branch (67-01)'
      pattern: 'participant_dossier_id'
---

<objective>
Run the phase gate live on staging (ORCHESTRATOR-RUN — requires Supabase MCP + browser, which executor agents lack): apply the get_person_full repoint migration with a pre/post shape proof, seed the host + participation rows, verify all three requirements in the browser in both locales, prove the wizard write fix end-to-end, force a branch failure, then clean up and close with the full gate suite.

Purpose: success criteria 1 and 2 are live criteria — staging has NULL hosts on all engagement rows and near-zero participation rows, so only seeds + browser prove the contracts; only an MCP apply makes the RPC wiring real.
Output: verification matrix in the SUMMARY; staging restored (host NULL, seeded rows deleted, pre-existing row intact).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/67-per-type-engagement-contracts-legacy-detail-cleanup/67-VALIDATION.md
@.planning/phases/67-per-type-engagement-contracts-legacy-detail-cleanup/67-RESEARCH.md
@.planning/phases/67-per-type-engagement-contracts-legacy-detail-cleanup/67-UI-SPEC.md

<interfaces>
<!-- Seed ids (RESEARCH §Open Question Answers Q6, resolve full UUIDs live before use):
     orgs: b0000001-…-0005 (OECD), …-0006 (GCC Stat Centre), b0000000-…-aaaa (GASTAT)
     persons (subtype standard): a0000000-…-0501..0505
     EO person id: SELECT d.id FROM dossiers d JOIN persons p ON p.id = d.id WHERE p.person_subtype = 'elected_official' LIMIT 1
     engagements: b0000002-…-0001..0003
     PRE-EXISTING row to protect: engagement_participants for person b0000011-… role head_of_delegation on b0000002-…-0003 -->

<!-- Exact seed/cleanup SQL: RESEARCH §Live Verification Protocol (host UPDATE/NULL revert;
     participant INSERT role 'delegate' with created_by = kazahrani@stats.gov.sa user id;
     DELETE scoped by engagement_id + participant_dossier_id + role). -->

<!-- Expected UI (binding, 67-UI-SPEC): "Hosted engagements"/"المشاركات المستضافة" and
     "Participation"/"المشاركة" headings (uppercase-tracked class, sentence-case copy);
     delegate badge "Delegate"/"مندوب"; rows min-h-[var(--row-h)] bordered list; error line
     role="alert" with dossier:overview.sectionError copy "تعذر تحميل هذا القسم..." in AR;
     empty org/person tabs pixel-identical to pre-phase (section ABSENT). -->

<!-- Login: kazahrani@stats.gov.sa, password in .env.test. App locale toggle: topbar EN/ع
     buttons (persists under localStorage id.locale). Local dev app on :5173 → staging. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the migration with a pre/post shape proof (PERENG-02 RPC half)</name>
  <files>none (Supabase MCP)</files>
  <action>
    1. PRE-CAPTURE: via MCP execute_sql, run SELECT get_person_full('<a0000000-…-0501 full id>') and store the full JSON; confirm top-level keys are exactly person/active_committees/key_staff (matches the 67-03 probe).
    2. Apply supabase/migrations/20260613100000_get_person_full_recent_engagements_canonical.sql verbatim via MCP apply_migration (same name).
    3. POST-PROOF: re-run the same SELECT. Assert (a) the three pre-existing keys deep-equal the pre-capture, (b) recent_engagements key is present (null/empty acceptable pre-seed), (c) get_person_full_base exists (SELECT 1 FROM pg_proc WHERE proname='get_person_full_base'). Any mismatch → STOP, do not proceed to seeds; the composition failed and must be diagnosed before the UI legs.
    4. Sanity: the persons edge fn needs NO redeploy (calls the RPC by name) — verify with an authenticated GET to the persons edge for the same id returning 200 with the recent_engagements key present.
  </action>
  <verify>
    <automated>MISSING — orchestrator-run: MCP SQL pre/post outputs recorded in the SUMMARY matrix (no executor-runnable command for staging DDL)</automated>
  </verify>
  <done>Migration live; three keys byte-stable; recent_engagements present; edge passthrough confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Seed + browser protocol EN/AR (PERENG-01 + PERENG-02 live)</name>
  <files>none (Supabase MCP + browser)</files>
  <action>
    Resolve full ids (interfaces block), then run the RESEARCH §Live Verification Protocol:
    1. HOST: UPDATE engagement_dossiers SET host_organization_id = '<OECD id>' WHERE id = '<engagement id>'. Browser → /dossiers/organizations/<OECD id>/engagements: "Hosted engagements" section with the engagement name linking to the live workspace; type badge; EN then AR (ع toggle, dir=rtl, Tajawal). A different org with no hosted rows shows NO section (absence rule).
    2. PARTICIPATION: INSERT engagement_participants (role 'delegate', created_by = kazahrani user id) for one standard person dossier AND for the EO person dossier. Browser → /dossiers/persons/<id>/engagements and /dossiers/elected-officials/<EO id>/engagements: "Participation" with the Delegate/مندوب badge, linked name, EN + AR. RPC: SELECT get_person_full('<person id>') → recent_engagements contains the seeded row (link.role = 'delegate', engagement names present).
    3. WIZARD WRITE FIX: in the browser, create an engagement via the wizard with one participant; SELECT * FROM engagement_participants WHERE engagement_id = '<new dossier id>' → row exists WITH created_by set. Record the new dossier id for Task 3 cleanup.
    4. FORCED ERROR: CDP Network.setBlockedURLs on */rest/v1/engagement_participants*; reload the person tab in AR → "المشاركة" heading + role="alert" line with the Arabic sectionError copy; "No engagements yet" copy ABSENT; generic history below unaffected. Unblock → refetch recovers without reload.
    5. WIDTHS: 1280 and 1024, EN + AR — no overflow, rows respect var(--row-h).
    Capture evidence (screenshots/DOM assertions) per step for the SUMMARY matrix.
  </action>
  <verify>
    <automated>MISSING — orchestrator-run: browser DOM assertions + MCP row checks recorded per-step in the SUMMARY matrix</automated>
  </verify>
  <done>All three live UI truths + the RPC truth + the write-fix truth evidenced in both locales.</done>
</task>

<task type="auto">
  <name>Task 3: Cleanup + full phase gates</name>
  <files>none</files>
  <action>
    1. CLEANUP via MCP: UPDATE engagement_dossiers SET host_organization_id = NULL WHERE id = '<engagement id>'; DELETE the two seeded participant rows (scoped by engagement_id + participant_dossier_id + role = 'delegate'); DELETE FROM dossiers WHERE id = '<wizard test dossier id>' (cascades extension + participants). CONFIRM: seeded-row count 0; host column NULL on all 3 original rows; the pre-existing b0000011-… head_of_delegation row still present (count 1).
    2. GATES (frontend): pnpm exec vitest run (full), pnpm type-check, pnpm build, pnpm exec size-limit with the whole log grepped for 'exceeded' (zero lines). Verify exit codes.
    3. Write the SUMMARY verification matrix: per-requirement evidence rows, pre/post RPC comparison, seed/cleanup confirmations, AR/width evidence refs, gate outputs.
  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0/frontend && pnpm exec vitest run 2>&1 | tail -3 && pnpm type-check && pnpm exec size-limit 2>&1 | (grep -c 'exceeded'; true) | head -1</automated>
  </verify>
  <acceptance_criteria>
    - Staging restored exactly (seeds gone, pre-existing row intact, no orphan test dossier)
    - Full gate suite green; SUMMARY matrix complete for all three requirements
  </acceptance_criteria>
  <done>Phase 67 live-verified end-to-end; ready for /gsd:verify-work and milestone close.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                             |
| -------------------------- | ------------------------------------------------------- |
| orchestrator MCP → staging | DDL apply + seed/cleanup writes on the staging database |
| browser session → app      | authenticated test-user verification                    |

## STRIDE Threat Register

| Threat ID | Category               | Component                        | Disposition | Mitigation Plan                                                                                                           |
| --------- | ---------------------- | -------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------- |
| T-67-12   | Tampering              | staging data                     | mitigate    | Seeds are minimal, id-scoped, and reverted in-session with count-confirmed cleanup; pre-existing row explicitly protected |
| T-67-13   | Elevation of Privilege | applied SECURITY DEFINER wrapper | mitigate    | Pre/post deep-equal proof on the three keys + person-scoped WHERE confirmed in the applied body before any seed/UI step   |
| T-67-14   | Information Disclosure | credentials in evidence          | mitigate    | Login from .env.test; secrets never appear in SUMMARY/screenshots; only ids and key names recorded                        |
| T-67-SC   | Tampering              | npm/pip/cargo installs           | accept      | No package installs in this plan                                                                                          |

</threat_model>

<verification>
- 67-VALIDATION manual-only rows realized: org hosted seed, person/EO participation seed, live RPC shape, AR/RTL widths
- Phase gate: full suite + type-check + build + size-limit zero exceeded
</verification>

<success_criteria>

- Roadmap success criteria 1-3 all evidenced LIVE: hosted engagement visible on the org tab; participation visible on person AND EO tabs with recent_engagements wired through the RPC/edge; legacy plane gone with zero rendered deltas
  </success_criteria>

<output>
Create `.planning/phases/67-per-type-engagement-contracts-legacy-detail-cleanup/67-06-SUMMARY.md` when done (the live verification matrix).
</output>
