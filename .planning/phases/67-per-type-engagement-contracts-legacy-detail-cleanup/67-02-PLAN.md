---
phase: 67-per-type-engagement-contracts-legacy-detail-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/dossier/wizard/config/engagement.config.ts
  - frontend/src/components/dossier/wizard/__tests__/engagement-participants-payload.test.ts
autonomous: true
requirements: [PERENG-02]

key_decisions:
  - 'Fix the PAYLOAD, never the policy (RESEARCH threat register): the wizard postCreate insert gains created_by = auth user id so it SATISFIES the existing engagement_participants WITH CHECK (created_by = auth.uid()) — the RLS policy is untouched'
  - 'If no authenticated user id can be resolved, postCreate warns and returns without inserting (same observable behavior as the silent RLS drop today, but explicit) — adding wizard error UI is out of scope per UI-SPEC ("payload-only; no UI change")'
  - 'The insert-error console.warn stays (post-create best-effort path); with the payload fixed, the warn path becomes the exception instead of the rule'

must_haves:
  truths:
    - 'engagementWizardConfig.postCreate inserts engagement_participants rows that each include created_by set to the authenticated user id'
    - 'A unit test pins the payload: every row carries created_by alongside engagement_id/participant_type/participant_dossier_id/role'
    - 'postCreate with zero participants still inserts nothing'
  artifacts:
    - path: 'frontend/src/components/dossier/wizard/__tests__/engagement-participants-payload.test.ts'
      provides: 'RED-first payload pin (67-VALIDATION Wave-0 gap)'
      min_lines: 60
    - path: 'frontend/src/components/dossier/wizard/config/engagement.config.ts'
      provides: 'created_by in the participants insert payload'
      contains: 'created_by'
  key_links:
    - from: 'frontend/src/components/dossier/wizard/config/engagement.config.ts'
      to: 'engagement_participants INSERT RLS (WITH CHECK created_by = auth.uid())'
      via: 'created_by from supabase.auth.getUser() in every inserted row'
      pattern: 'created_by'
---

<objective>
Fix the load-bearing wizard write bug (RESEARCH Pitfall 1): engagement.config.ts postCreate inserts engagement_participants rows WITHOUT created_by, the column has no default, RLS WITH CHECK (created_by = auth.uid()) rejects NULL, and the error is console.warn-swallowed — so every wizard-created engagement silently loses its participants today.

Purpose: PERENG-02's write half — without this fix the participation contract (67-01) is honest but permanently empty for wizard-created engagements.
Output: payload fix pinned by a RED-first unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/67-per-type-engagement-contracts-legacy-detail-cleanup/67-RESEARCH.md

<interfaces>
<!-- Current postCreate (engagement.config.ts L73-84): builds rows via
     buildParticipantRows(newDossierId, data) — each row { engagement_id,
     participant_type: 'country'|'organization'|'person', participant_dossier_id,
     role: 'delegate' } — then supabase.from('engagement_participants').insert(rows),
     console.warn on error. NO created_by anywhere. NO supabase.auth call. -->

<!-- Target shape (RESEARCH §Code Examples — wizard write fix): -->

```typescript
const { data: auth } = await supabase.auth.getUser()
const userId = auth.user?.id
if (userId == null) {
  console.warn('engagement postCreate: no authenticated user — skipping participant insert')
  return
}
const rows = buildParticipantRows(newDossierId, data).map((r) => ({ ...r, created_by: userId }))
```

<!-- Live policy (Q5-verified, do NOT modify): INSERT "Users can create engagement
     participants" WITH CHECK (created_by = auth.uid()). The edge writer
     (addParticipant) already sets created_by: user.id — this fix brings the wizard
     to parity with it. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Payload-pin test — RED first</name>
  <files>frontend/src/components/dossier/wizard/__tests__/engagement-participants-payload.test.ts</files>
  <read_first>
    - frontend/src/components/dossier/wizard/config/engagement.config.ts (full read once — 84 lines)
    - frontend/src/components/dossier/wizard/schemas/engagement.schema.ts (EngagementFormData fields needed to build a minimal valid-enough object for postCreate — postCreate only reads participant_*_ids)
  </read_first>
  <behavior>
    - Mock '@/lib/supabase' with vi.hoisted state: auth.getUser resolves { data: { user: { id: 'user-1' } }, error: null }; from('engagement_participants') returns { insert: insertSpy } where insertSpy resolves { error: null } and captures its argument.
    - Test (RED): call engagementWizardConfig.postCreate('eng-1', formData with participant_person_ids: ['p1'] and participant_country_ids: ['c1']) — assert insertSpy called once with 2 rows, and EVERY row has created_by === 'user-1', engagement_id === 'eng-1', role === 'delegate', and the correct participant_type/participant_dossier_id pairing. RED today because no row carries created_by.
    - Test (GREEN from the start): postCreate with all participant id arrays empty → insertSpy NOT called.
    - Test (RED): auth.getUser resolves { data: { user: null } } → insertSpy NOT called (explicit skip, no throw).
  </behavior>
  <action>
    Create the test file (kebab/camel naming is fine — wizard __tests__ already mixes; the file is a .test.ts so the components/** PascalCase rule does not apply to it, mirror the existing __tests__ filenames). Build formData as a minimal Partial cast through unknown to EngagementFormData (postCreate reads only the three id arrays). Run the file: payload and no-user tests MUST fail, empty-participants test MUST pass. Commit: test(67-02): add failing created_by payload pin for wizard engagement participants.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/components/dossier/wizard/__tests__/engagement-participants-payload.test.ts 2>&1 | grep -qE '[1-9][0-9]* failed' && echo RED_CONFIRMED</automated>
  </verify>
  <done>67-VALIDATION Wave-0 row (wizard payload pin) exists and is RED against current behavior; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Add created_by to the postCreate payload — GREEN</name>
  <files>frontend/src/components/dossier/wizard/config/engagement.config.ts</files>
  <action>
    Apply the target shape from the interfaces block inside postCreate: resolve the user id via supabase.auth.getUser() BEFORE building rows; explicit null check (strict-boolean-expressions) with warn-and-return when absent; map created_by onto every row immutably. Keep buildParticipantRows untouched; keep the existing insert-error console.warn. Do NOT touch the RLS policy, the schema, or filterExtensionData. Run the Task 1 file (all GREEN) + pnpm type-check. Commit: fix(67-02): include created_by in wizard engagement_participants insert (PERENG-02).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/components/dossier/wizard/__tests__/engagement-participants-payload.test.ts && pnpm type-check && grep -q 'created_by' src/components/dossier/wizard/config/engagement.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - All Task 1 tests green; type-check exit 0
    - Payload satisfies WITH CHECK (created_by = auth.uid()) — policy file untouched
  </acceptance_criteria>
  <done>Wizard-created engagements accrue participation rows; live create→row proof runs in 67-06.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                              |
| --------------------------------- | -------------------------------------------------------- |
| browser → engagement_participants | authenticated client INSERT crossing RLS WITH CHECK gate |

## STRIDE Threat Register

| Threat ID | Category               | Component                   | Disposition | Mitigation Plan                                                                                                              |
| --------- | ---------------------- | --------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------- |
| T-67-04   | Elevation of Privilege | engagement_participants RLS | mitigate    | Fix the payload (created_by from the session), NEVER weaken the WITH CHECK policy — the exact anti-pattern named in RESEARCH |
| T-67-05   | Spoofing               | created_by value            | accept      | Client-supplied created_by is server-validated by WITH CHECK (created_by = auth.uid()) — spoofed ids are rejected by RLS     |
| T-67-SC   | Tampering              | npm/pip/cargo installs      | accept      | No package installs in this plan                                                                                             |

</threat_model>

<verification>
- 67-VALIDATION Wave-0 wizard-payload row realized RED→GREEN
- Per-task gate: quick vitest run + pnpm type-check
</verification>

<success_criteria>

- postCreate payload carries created_by on every row; unit-pinned; no policy change
  </success_criteria>

<output>
Create `.planning/phases/67-per-type-engagement-contracts-legacy-detail-cleanup/67-02-SUMMARY.md` when done.
</output>
