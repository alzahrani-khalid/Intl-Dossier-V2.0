---
phase: 57-phase-52-deviation-closure-d-19-d-23
plan: 04
type: execute
wave: 3
depends_on: ['57-02']
files_modified:
  - .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-LIVE-RUN-SUMMARY.md
  - .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-LIVE-RUN.log
  - .planning/phases/57-phase-52-deviation-closure-d-19-d-23/live-run-screenshots/visual.png
  - .planning/phases/57-phase-52-deviation-closure-d-19-d-23/live-run-screenshots/a11y.png
  - .planning/phases/57-phase-52-deviation-closure-d-19-d-23/live-run-screenshots/dnd.png
  - .planning/phases/57-phase-52-deviation-closure-d-19-d-23/live-run-screenshots/keyboard.png
  - .planning/phases/52-heroui-v3-kanban-migration/52-VERIFICATION.md
autonomous: false
requirements: [DEVIATE-04]
requirements_addressed: [DEVIATE-04]
user_setup:
  - service: supabase-staging
    why: 'Live tasks-tab Playwright run executes against staging Supabase project'
    env_vars:
      - name: SUPABASE_URL
        source: 'Supabase dashboard for project zkrcjzdemdmwhearhfgg → Project Settings → API → URL'
      - name: SUPABASE_ANON_KEY
        source: 'Supabase dashboard → Project Settings → API → anon public key'
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: 'Supabase dashboard → Project Settings → API → service_role secret (NEVER bundle this client-side)'
      - name: TEST_USER_EMAIL
        source: 'Doppler / .env.test — test analyst account on staging'
      - name: TEST_USER_PASSWORD
        source: 'Doppler / .env.test — paired password for TEST_USER_EMAIL'
      - name: PHASE_52_FIXTURE_ENGAGEMENT_ID
        source: 'Defaults to 00000000-0000-0052-0000-000000000001; override only if D-57-16 staging-verification reveals fixture drift'
      - name: E2E_BASE_URL
        source: 'Defaults to http://localhost:5173; operator picks whether to test against local dev server or a tunneled staging-frontend URL'
    dashboard_config:
      - task: 'Verify Phase 52 fixture engagement exists with the 8-row workflow_stage composition (D-57-16)'
        location: 'Supabase MCP execute_sql against project zkrcjzdemdmwhearhfgg; see 52-FIXTURE.md §Procedure step 1 for the discovery SQL'
  - service: git-ssh-signing
    why: 'Phase close issues an annotated + SSH-signed phase-57-base tag (D-57-22)'
    env_vars:
      - name: GIT_SIGNING_KEY
        source: 'Local ~/.gitconfig (user.signingkey points to ~/.ssh/<github-enrolled-key>.pub per CLAUDE.md §"Tag signing setup")'
    dashboard_config:
      - task: 'Confirm SSH signing key is enrolled on github.com as a Signing Key for the GitHub-side "Verified" badge'
        location: 'github.com → Settings → SSH and GPG keys → Signing keys'
tags: [live-run, playwright, staging, host-operator, artifacts, signed-tag]

must_haves:
  truths:
    - 'All 30 tests across frontend/tests/e2e/tasks-tab-{visual,a11y,dnd,keyboard}.spec.ts pass under the canonical run (existing test.skip entries count as expected-skip, not failure) (D-57-18)'
    - 'Phase 52 fixture engagement 00000000-0000-0052-0000-000000000001 verified on staging with the 8-row composition (todo:2, in_progress:2, review:1, done:2, cancelled:1) before the run; local-operator-on-staging path per D-57-15 (no ephemeral branch), and Phase 52 fixture UUID reused per D-57-16'
    - '57-LIVE-RUN-SUMMARY.md exists in the phase folder with: pass/fail table per spec, operator name, timestamp, commit SHA, environment, playwright --version (D-57-17)'
    - '57-LIVE-RUN.log exists with raw Playwright stdout from the canonical run (D-57-17)'
    - 'live-run-screenshots/{visual,a11y,dnd,keyboard}.png — one representative screenshot per spec — committed (D-57-17)'
    - '.planning/phases/52-heroui-v3-kanban-migration/52-VERIFICATION.md updated to record D-19/21/22/23 as RESOLVED with cross-link to Phase 57 plan + commit SHA; frontmatter status flips from passed_with_deviation to passed (cross-phase update per CONTEXT.md §specifics)'
    - 'Annotated + SSH-signed phase-57-base tag exists on the phase-completion commit, is pushed to origin, and `git tag -v phase-57-base` exits 0 with `Good "git" signature` output (D-57-22)'
    - 'No regression on the 8 required CI contexts (`type-check`, `Security Scan`, `Lint`, `Bundle Size Check (size-limit)`, `Tests (frontend)`, `Tests (backend)`, `Design Token Check`, `react-i18next Factory Check`) on the phase-completion commit (cross-cutting v6.4 milestone gate)'
  artifacts:
    - path: '.planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-LIVE-RUN-SUMMARY.md'
      provides: 'Operator-authored summary table of the canonical run'
      contains: 'tasks-tab-visual'
    - path: '.planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-LIVE-RUN.log'
      provides: 'Raw Playwright stdout from the canonical run'
      contains: 'Running'
    - path: '.planning/phases/57-phase-52-deviation-closure-d-19-d-23/live-run-screenshots/visual.png'
      provides: 'One representative screenshot from tasks-tab-visual.spec.ts'
      contains: ''
    - path: '.planning/phases/57-phase-52-deviation-closure-d-19-d-23/live-run-screenshots/a11y.png'
      provides: 'One representative screenshot from tasks-tab-a11y.spec.ts'
      contains: ''
    - path: '.planning/phases/57-phase-52-deviation-closure-d-19-d-23/live-run-screenshots/dnd.png'
      provides: 'One representative screenshot from tasks-tab-dnd.spec.ts'
      contains: ''
    - path: '.planning/phases/57-phase-52-deviation-closure-d-19-d-23/live-run-screenshots/keyboard.png'
      provides: 'One representative screenshot from tasks-tab-keyboard.spec.ts'
      contains: ''
    - path: 'phase-57-base (git tag, refs/tags/phase-57-base)'
      provides: 'Annotated + SSH-signed phase-completion tag; verifiable via `git tag -v phase-57-base`'
      contains: 'Good "git" signature'
  key_links:
    - from: 'frontend/tests/e2e/tasks-tab-*.spec.ts'
      to: 'staging Supabase fixture engagement 00000000-0000-0052-0000-000000000001'
      via: 'PHASE_52_FIXTURE_ENGAGEMENT_ID env var (defaults to canonical UUID)'
      pattern: '00000000-0000-0052-0000-000000000001'
    - from: '57-LIVE-RUN-SUMMARY.md'
      to: '52-VERIFICATION.md D-23-LIVE-PLAYWRIGHT-DEFERRED'
      via: 'closure status flip + cross-phase commit SHA reference'
      pattern: 'D-23'
    - from: 'phase-57-base tag'
      to: 'phase-completion commit on main'
      via: '`git tag -as phase-57-base -m <body>` then `git push origin phase-57-base`'
      pattern: 'phase-57-base'
---

<objective>
Close D-23 (live tasks-tab Playwright run) by executing the canonical 30-test run on seeded staging data with a host operator and committing the artifact package (summary + log + 4 screenshots) to the phase folder. At phase close, update 52-VERIFICATION.md to flip D-19/21/22/23 from `passed_with_deviation` to `passed` (cross-phase update), then issue the annotated + SSH-signed `phase-57-base` tag on the phase-completion commit and push it to origin (D-57-22 — final task; operator confirms).

Purpose: Phase 52 D-23 carried PASS-WITH-DEVIATION because the 30 enumerated tests had NOT been executed live against seeded staging data — only compile/type/enumerate verification was done worktree-side. ROADMAP success criterion #4 requires the artifact package to land in the phase folder. The phase-57-base tag continues the v6.2/v6.3/v6.4 closing convention so downstream phases (58, 59) and milestone-audit tooling can reach the phase-completion commit deterministically.

Output:

- 57-LIVE-RUN-SUMMARY.md with pass/fail per spec + environment metadata
- 57-LIVE-RUN.log with raw Playwright stdout
- 4 representative screenshots (one per spec file)
- Updated 52-VERIFICATION.md frontmatter (cross-phase closure record)
- Annotated + SSH-signed `phase-57-base` tag pushed to origin

**Execution context:** Inline on `main` with atomic per-task commits (D-57-20); verification sequencing across Phase 57 plans is D-19 → D-22 → D-21 → D-23 (D-57-21) — this plan's `wave` + `depends_on` frontmatter enforces that order.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-CONTEXT.md
@.planning/phases/52-heroui-v3-kanban-migration/52-VERIFICATION.md
@.planning/phases/52-heroui-v3-kanban-migration/52-FIXTURE.md
@CLAUDE.md
@.env.test.example
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Phase 52 fixture engagement integrity on staging Supabase (D-57-16)</name>
  <what-built>Per D-57-16 the live run reuses the Phase 52 canonical fixture engagement UUID `00000000-0000-0052-0000-000000000001`. Before running 30 tests against staging, the operator confirms (a) the engagement exists, (b) its assignments composition still matches the D-16 contract (todo:2, in_progress:2, review:1, done:2, cancelled:1 = 8 rows). If drift is observed, a one-shot reseed SQL is committed at .planning/phases/57-.../57-FIXTURE-RESEED.sql per the Claude's Discretion item ("create only if first staging-verify task detects drift").</what-built>
  <how-to-verify>
    1. Operator runs the discovery SQL from 52-FIXTURE.md §Procedure step 1 via Supabase MCP `mcp__claude_ai_Supabase__execute_sql` against project `zkrcjzdemdmwhearhfgg` (eu-west-2):
       ```sql
       SELECT a.workflow_stage, COUNT(*)
       FROM assignments a
       WHERE a.engagement_id = '00000000-0000-0052-0000-000000000001'
       GROUP BY a.workflow_stage
       ORDER BY a.workflow_stage;
       ```
    2. Expected result (D-16 composition contract):
       ```
       cancelled   1
       done        2
       in_progress 2
       review      1
       todo        2
       ```
    3. If results match exactly: proceed to Task 2. Do NOT create 57-FIXTURE-RESEED.sql.
    4. If results show drift (rows missing, counts wrong, or engagement absent): operator constructs a one-shot SQL reseed script using live `auth.users.id` for `$TEST_USER_EMAIL` (CLAUDE.md "Test Credentials") + a real `dossiers.id` from staging, following the procedure in 52-FIXTURE.md §Procedure step 3. Operator commits the script at `.planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-FIXTURE-RESEED.sql` and applies via Supabase MCP `mcp__claude_ai_Supabase__apply_migration` (or the equivalent execute_sql idempotent ON CONFLICT path). Then re-run the verification SQL to confirm the composition.
    5. Document the outcome (matched vs drifted+reseeded) in a brief operator-side note that becomes Task 4's 57-LIVE-RUN-SUMMARY.md preamble.
  </how-to-verify>
  <resume-signal>Type "fixture-verified" (with one-line summary: matched exactly, or drifted + reseed applied + composition now matches) or describe blocker.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Execute the canonical 30-test Playwright run on staging</name>
  <what-built>The four tasks-tab spec files (visual, a11y, dnd, keyboard) enumerate 30 tests total (with existing test.skip entries for the 768 mobile cells in dnd + keyboard, which count as expected-skip per D-57-18). The host operator runs them all against the seeded staging engagement, capturing raw stdout and per-spec screenshots. No flake budget: if the first run shows non-deterministic failure, the operator diagnoses root cause rather than re-running (D-57-18).</what-built>
  <how-to-verify>
    1. Operator sources environment: `.env.test` must contain valid SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, TEST_USER_EMAIL, TEST_USER_PASSWORD, PHASE_52_FIXTURE_ENGAGEMENT_ID (defaults to canonical UUID), E2E_BASE_URL (local dev server or staging tunnel — operator picks).
    2. Operator starts the dev server pointing at staging Supabase (e.g., `pnpm --filter ./frontend dev` with VITE_SUPABASE_URL pointed at staging) OR uses a staging-frontend tunnel URL via E2E_BASE_URL.
    3. Operator runs (host-side): `cd frontend && pnpm exec playwright test tests/e2e/tasks-tab-visual.spec.ts tests/e2e/tasks-tab-a11y.spec.ts tests/e2e/tasks-tab-dnd.spec.ts tests/e2e/tasks-tab-keyboard.spec.ts --project=chromium --reporter=list 2>&1 | tee .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-LIVE-RUN.log`
    4. Operator confirms log shows exactly 30 tests enumerated with the result distribution: passed = 30 - (number of expected skips); skipped = expected skips (768 cells in dnd + keyboard per D-19 scope-out, plus any "No cards seeded" skips). NO unexpected failures allowed (D-57-18 strict bar).
    5. If a spec fails: operator does NOT re-run. Operator diagnoses root cause first. Common causes: stale dev server, fixture drift, network glitch (these justify a re-run only AFTER confirming the cause). If a true product/spec defect surfaces, raise it as a quick-task and do NOT close D-23 in this plan; document the blocker and exit the plan with a "blocked" status to be revisited.
    6. Operator captures one representative screenshot per spec via `pnpm exec playwright test --headed --screenshot=only-on-failure ...` OR by running an additional one-off snippet that loads /engagements/{fixture} and grabs `tasks-tab.png` via `page.screenshot()`. Files land at:
       - `.planning/phases/57-phase-52-deviation-closure-d-19-d-23/live-run-screenshots/visual.png` (engagement workspace, Tasks tab visible, kanban surface populated)
       - `.planning/phases/57-phase-52-deviation-closure-d-19-d-23/live-run-screenshots/a11y.png` (same engagement, capturing the rendered Tasks tab axe-target surface)
       - `.planning/phases/57-phase-52-deviation-closure-d-19-d-23/live-run-screenshots/dnd.png` (mid-drag or post-drag state showing the in_progress column with a moved card)
       - `.planning/phases/57-phase-52-deviation-closure-d-19-d-23/live-run-screenshots/keyboard.png` (focus ring on a kcard or the dnd-kit announcement region visible)
    7. On any failing spec, the per-spec trace.zip is committed alongside at `.planning/phases/57-.../live-run-traces/<failing-spec>.zip`. NO full Playwright HTML report bundle is committed (too large; D-57-17).
  </how-to-verify>
  <resume-signal>Type "live-run-complete" (with the exact pass/fail count from the log tail, e.g., "30 tests, 24 passed, 6 expected skips, 0 unexpected failures") or describe blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Author 57-LIVE-RUN-SUMMARY.md with the pass/fail matrix + environment metadata</name>
  <files>.planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-LIVE-RUN-SUMMARY.md</files>
  <read_first>
    - .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-LIVE-RUN.log (raw stdout from Task 2)
    - .planning/phases/52-heroui-v3-kanban-migration/52-SUMMARY.md (acknowledged-deviation prose style — match for tonal consistency)
    - .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-CONTEXT.md (D-57-17: summary structure spec; D-57-18: acceptance bar)
    - .planning/phases/52-heroui-v3-kanban-migration/52-VERIFICATION.md (D-23-LIVE-PLAYWRIGHT-DEFERRED row for closure cross-reference)
  </read_first>
  <action>
    Compose 57-LIVE-RUN-SUMMARY.md with frontmatter (phase, plan: 04, type: live-run, operator, run_timestamp, commit_sha, playwright_version, environment) and these sections:
    1. **Summary** — one-paragraph verdict ("All 30 tests passed (X passed, Y expected skips, 0 unexpected failures)" or the actual outcome).
    2. **Environment** — table: SUPABASE_URL host, fixture engagement UUID, fixture composition (verified from Task 1), Playwright version, Node version, browser channel (chromium), dev server / staging-frontend URL.
    3. **Pass/fail matrix** — table with one row per spec file × matrix-row (4 rows × 4 files = 16 rows, plus the 4 mobile expected-skip rows from dnd + keyboard + 2 mobile rows from a11y + 4 visual rows = exactly 30 enumerated tests; show each with Status (passed | expected-skip | failed) + skip-reason if applicable).
    4. **Mid-drag DragOverlay parity** — captured by tasks-tab-dnd.spec.ts assertions; cross-link to `frontend/tests/e2e/__phase52-mid-drag__/tasks-tab-mid-drag.png` (existing Phase 52 capture).
    5. **Screenshots** — link to the 4 representative PNGs in live-run-screenshots/.
    6. **Cross-phase update** — call out that 52-VERIFICATION.md will be updated at phase close (Task 4 of this plan) to flip D-19/21/22/23 from `passed_with_deviation` to `passed`.
    7. **Open follow-ups** — any operator-noted defects requiring quick-tasks.
    Tone: match the Phase 52 acknowledged-deviation prose. No emoji. No marketing voice. Dates as `Tue 18 May` (CLAUDE.md date format). Times as `14:30 GST`.
  </action>
  <verify>
    <automated>test -f .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-LIVE-RUN-SUMMARY.md && grep -q "operator" .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-LIVE-RUN-SUMMARY.md && grep -q "commit_sha" .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-LIVE-RUN-SUMMARY.md && grep -q "playwright_version" .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-LIVE-RUN-SUMMARY.md && grep -qE "tasks-tab-(visual|a11y|dnd|keyboard)" .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-LIVE-RUN-SUMMARY.md</automated>
    <human-check>Operator confirms the summary mirrors the actual log outcome (no fabricated pass/fail entries) and that screenshot files are present at the listed paths.</human-check>
  </verify>
  <acceptance_criteria>
    - 57-LIVE-RUN-SUMMARY.md frontmatter has: phase, plan, type, operator, run_timestamp, commit_sha, playwright_version, environment
    - Pass/fail matrix lists exactly 30 rows aligned with the spec enumeration
    - Mid-drag DragOverlay parity referenced
    - 4 screenshot files exist at live-run-screenshots/{visual,a11y,dnd,keyboard}.png
    - Cross-phase update intent stated (52-VERIFICATION.md to be updated in Task 4)
    - No emoji; no marketing voice; CLAUDE.md date format applied
  </acceptance_criteria>
  <done>Summary committed; matrix matches the log; screenshots present.</done>
</task>

<task type="auto">
  <name>Task 4: Cross-phase update — flip 52-VERIFICATION.md frontmatter for D-19/21/22/23</name>
  <files>.planning/phases/52-heroui-v3-kanban-migration/52-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/52-heroui-v3-kanban-migration/52-VERIFICATION.md (current `deviations_acknowledged` frontmatter block at L13-31; current `status: passed_with_deviation` at L4)
    - .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-01-SUMMARY.md (commit SHA of D-19 closure)
    - .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-02-SUMMARY.md (commit SHA of D-21 closure including baseline regen)
    - .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-03-SUMMARY.md (commit SHA of D-22 closure)
    - .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-LIVE-RUN-SUMMARY.md (commit SHA of D-23 live run)
  </read_first>
  <action>
    Edit .planning/phases/52-heroui-v3-kanban-migration/52-VERIFICATION.md frontmatter:
    1. For each deviation row (D-19-MOBILE-TOUCH-DND-SCOPE-OUT, D-21-PHASE-39-REGRESSION-DEFERRED, D-22-LTR-RTL-BYTE-IDENTITY, D-23-LIVE-PLAYWRIGHT-DEFERRED), APPEND a `resolution:` block under each entry capturing: resolved_in_phase (`57-phase-52-deviation-closure-d-19-d-23`), resolved_in_plan (`57-01` / `57-02` / `57-03` / `57-04`), commit_sha (the per-plan closure commit), resolved_date (today's date in `Tue 18 May` format), and resolution_summary (one line: "Closed by ADR + mobile <select> assertion (D-19)", "Closed by WorkBoard migration + ESLint guard + baseline regen (D-21)", "Closed by ?lng URL fix + hash-comparison meta-test + ESLint guard (D-22)", "Closed by live 30-test Playwright run on seeded staging (D-23)").
    2. Update top-level frontmatter `status: passed_with_deviation` → `status: passed` ONLY IF all 5 deviations are closed (D-20 already closed in v6.3; D-19/21/22/23 close in this phase; D-52-08 timeout was post-execution closure already). If all are closed, flip to `passed`. Otherwise leave as `passed_with_deviation` and document the residual deviation in 57-LIVE-RUN-SUMMARY.md Open Follow-ups.
    3. Update `re_verification` block at L8-12 (currently `previous_status: none`) to `previous_status: passed_with_deviation`, `previous_score: 4/4 must-haves verified (2 with documented deferrals)`, `gaps_closed: [D-19, D-21, D-22, D-23]`, `gaps_remaining: []`, `regressions: []`.
    4. Append a `verified_at_phase_57:` timestamp (`2026-05-18T<HH:MM>:00Z`).
    5. DO NOT alter the rest of 52-VERIFICATION.md body. The deviation rows in the body table stay as historical record; the frontmatter resolution blocks are the machine-readable closure trail.
  </action>
  <verify>
    <automated>grep -q "resolved_in_phase" .planning/phases/52-heroui-v3-kanban-migration/52-VERIFICATION.md && grep -q "57-phase-52-deviation-closure-d-19-d-23" .planning/phases/52-heroui-v3-kanban-migration/52-VERIFICATION.md && grep -q "gaps_closed" .planning/phases/52-heroui-v3-kanban-migration/52-VERIFICATION.md && grep -qE "(D-19|D-21|D-22|D-23)" .planning/phases/52-heroui-v3-kanban-migration/52-VERIFICATION.md && grep -qE "^status: (passed|passed_with_deviation)$" .planning/phases/52-heroui-v3-kanban-migration/52-VERIFICATION.md</automated>
    <human-check>Operator confirms: (a) `git diff .planning/phases/52-heroui-v3-kanban-migration/52-VERIFICATION.md` shows ONLY frontmatter additions and the status flip (no body edits), (b) each resolution block has a commit SHA that resolves via `git cat-file -e <sha>`, (c) the frontmatter is valid YAML (test with `python -c "import yaml; yaml.safe_load(open('.planning/phases/52-heroui-v3-kanban-migration/52-VERIFICATION.md').read().split('---')[1])"`).</human-check>
  </verify>
  <acceptance_criteria>
    - 52-VERIFICATION.md frontmatter has a `resolution:` block under each of D-19/21/22/23 with resolved_in_phase, resolved_in_plan, commit_sha, resolved_date, resolution_summary
    - All four commit SHAs resolve (`git cat-file -e <sha>` exit 0 for each)
    - `re_verification` block updated with gaps_closed listing all four deviations
    - `status` flipped to `passed` IF all deviations closed, else `passed_with_deviation` with residual documented
    - Body table of 52-VERIFICATION.md unchanged (only frontmatter edits)
    - Frontmatter is valid YAML
  </acceptance_criteria>
  <done>Cross-phase closure record committed. 52-VERIFICATION.md frontmatter reflects D-19/21/22/23 resolution with per-plan provenance.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Issue + push annotated SSH-signed phase-57-base tag on the phase-completion commit (D-57-22)</name>
  <what-built>Per D-57-22 the phase-completion commit (the Task 4 cross-phase update commit, OR the most recent atomic commit on `main` if Task 4 produces multiple commits) receives an annotated + SSH-signed `phase-57-base` tag, pushed to origin. This continues the v6.2/v6.3/v6.4-Phase-55-56 convention (`phase-NN-base` reachable from `main`, `git tag -v <name>` exits 0 with `Good "git" signature`). Tagging is the FINAL task of Plan 57-04 (and of Phase 57 overall) — explicitly operator-confirmed because the SSH signing key is sensitive and the push to origin is irreversible.</what-built>
  <how-to-verify>
    1. Operator confirms local git signing config is correct (`git config --global gpg.format` returns `ssh`; `git config --global user.signingkey` points to an ssh public key path; `~/.ssh/allowed_signers` contains the matching email + pubkey line) per CLAUDE.md §"Tag signing setup". This is one-time setup per machine; verify before tagging.
    2. Operator confirms the current `main` HEAD is the desired phase-completion commit (typically the Task 4 cross-phase update commit). Run: `git log -1 --oneline main` and `git status` (should be clean).
    3. Operator runs (host-side):
       ```bash
       git tag -as phase-57-base -m "$(cat <<'EOF'
       phase-57-base — Phase 52 deviation closure (D-19, D-21, D-22, D-23)

       Closes:
       - D-19 MOBILE-TOUCH-DND-SCOPE-OUT  (ADR + mobile <select> assertion; plan 57-01)
       - D-21 PHASE-39-REGRESSION-DEFERRED (WorkBoard migration to shared primitive + ESLint guard + 4 regenerated visual baselines; plan 57-02)
       - D-22 LTR-RTL-BYTE-IDENTITY        (?lng URL fix + querystring i18n detector + hash-comparison meta-test + ESLint guard; plan 57-03)
       - D-23 LIVE-PLAYWRIGHT-DEFERRED     (canonical 30-test run on staging + artifact package; plan 57-04)

       v6.4 milestone progress: 11/14 requirements verified.
       EOF
       )"
       ```
       Body adapts the Phase 53/54/55/56 precedent (single-line subject + multi-line closures list + commit anchor reference).
    4. Operator verifies signature locally before pushing: `git tag -v phase-57-base` MUST exit 0 and the output MUST contain the literal string `Good "git" signature` (CLAUDE.md §"Tag signing setup" final command).
    5. Operator pushes the tag: `git push origin phase-57-base`. Confirm output reports `[new tag] phase-57-base -> phase-57-base`.
    6. Operator runs `gh api repos/:owner/:repo/git/refs/tags/phase-57-base --jq '.object.sha'` to confirm the tag is registered on the remote and points to the expected SHA.
    7. Operator records the tag SHA + signed-by + push timestamp in `57-04-SUMMARY.md` (created at end of plan).
    8. If `git tag -v` fails (signature invalid, key not enrolled, allowed_signers missing): operator does NOT push. Operator diagnoses the signing config, fixes, deletes the local tag (`git tag -d phase-57-base`), and re-issues. If `~/.ssh/allowed_signers` is missing entirely, follow CLAUDE.md §"Tag signing setup" three-command bootstrap.
    9. No tag amends. If the tag must be replaced (e.g., points at wrong commit), delete locally (`git tag -d phase-57-base`), delete remote (`git push --delete origin phase-57-base`) — operator confirms remote delete is intentional — then re-issue.

  </how-to-verify>
  <resume-signal>Type "phase-57-base-pushed" (with the resolved tag SHA + the local `git tag -v phase-57-base` exit code 0 confirmation) or describe blocker.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                                     |
| ------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| operator host → staging Supabase      | live credentials cross this boundary; service_role key is high-value                                            |
| Playwright stdout → committed log     | run log may contain timestamps + auth-success messages; check for accidental secret leakage before commit       |
| screenshots → committed PNGs          | screenshots may capture session UI; check for accidental PII leakage before commit                              |
| local SSH signing key → annotated tag | private key signs the tag; pushed to origin makes the signature publicly verifiable                             |
| origin remote → phase-57-base ref     | tag push is effectively immutable provenance; tampering after-the-fact requires force-push (audit-trail signal) |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                                         | Disposition | Mitigation Plan                                                                                                                                                                                      |
| ---------- | ---------------------- | --------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | -------- | ------------------------------------------------------------------------------------------------------------- |
| T-57-04-01 | Information Disclosure | 57-LIVE-RUN.log may contain JWT or session token fragments                        | mitigate    | Operator greps the log for token patterns before commit: `grep -iE "(eyJhbGc                                                                                                                         | service_role | password | secret)" .planning/phases/57-.../57-LIVE-RUN.log` MUST return empty. If matches surface, redact before commit |
| T-57-04-02 | Information Disclosure | screenshots may show authenticated user name, real assignee names, or staging PII | accept      | Staging fixture engagement uses synthetic test-user accounts; the captured screenshots show the canonical 8-row composition. Operator visually inspects before commit                                |
| T-57-04-03 | Tampering              | fixture reseed SQL touches staging assignments table                              | mitigate    | Reseed uses `ON CONFLICT (id) DO UPDATE` per 52-FIXTURE.md procedure step 3; idempotent; reseed UUID set is reserved (00000000-0000-0052-XXXX); reseed only runs if Task 1 detects drift             |
| T-57-04-04 | Repudiation            | live-run artifacts must be reproducible                                           | mitigate    | 57-LIVE-RUN-SUMMARY.md captures commit_sha + playwright_version + run_timestamp + operator email + fixture composition; reseed SQL (if used) is committed at 57-FIXTURE-RESEED.sql                   |
| T-57-04-05 | Spoofing               | fixture engagement UUID hijack                                                    | accept      | Canonical UUID `00000000-0000-0052-0000-000000000001` is hardcoded in 8 spec files; staging fixture verified Task 1                                                                                  |
| T-57-04-06 | Spoofing / Repudiation | phase-57-base tag could be issued unsigned or by a non-enrolled key               | mitigate    | Task 5 protocol REQUIRES `git tag -as` (annotated + signed) AND `git tag -v` MUST output `Good "git" signature` before push. CLAUDE.md §"Tag signing setup" bootstrap is the canonical signing chain |
| T-57-04-07 | Tampering              | secrets leaked into tag annotation message body                                   | mitigate    | Tag message body is template-controlled (no env-var interpolation); operator visually inspects before push; tag message contains only public deviation IDs + plan numbers                            |

</threat_model>

<verification>
- Phase 52 fixture engagement verified on staging with 8-row composition (Task 1)
- Canonical 30-test Playwright run executed; 0 unexpected failures (Task 2)
- 57-LIVE-RUN-SUMMARY.md + 57-LIVE-RUN.log + 4 screenshots committed (Task 3)
- 52-VERIFICATION.md frontmatter updated with per-deviation resolution blocks; status flipped to passed (Task 4)
- `phase-57-base` annotated SSH-signed tag exists locally AND on origin; `git tag -v phase-57-base` exits 0 with `Good "git" signature` (Task 5)
- Operator-side secret-leak grep on 57-LIVE-RUN.log returns empty
- 8 required CI contexts remain green on the phase-completion commit (cross-cutting v6.4 milestone gate)
</verification>

<success_criteria>

- DEVIATE-04 ROADMAP success criterion #4 met: live tasks-tab run executed on seeded staging with host operator; artifact package in phase folder
- 52-VERIFICATION.md status: passed (assumes all four deviations close cleanly)
- D-19/21/22/23 all carry a resolved_in_plan + commit_sha provenance trail
- `phase-57-base` SSH-signed tag reachable from `main` via `git tag -l phase-57-base` and `git tag -v phase-57-base` exits 0 with `Good "git" signature`
- Phase 57 ready to ship; v6.4 progresses to 11/14 requirements verified
  </success_criteria>

<output>
Create `.planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-04-SUMMARY.md` when done. Include: fixture verification outcome (Task 1), live-run command + exact pass/expected-skip/fail counts (Task 2), 4 screenshot file sizes (Task 3 sanity), 52-VERIFICATION.md frontmatter diff stat (Task 4), `phase-57-base` tag SHA + signed-by + push timestamp (Task 5), operator name + run_timestamp, secret-leak grep outcome (must be empty), `git tag -v phase-57-base` exit code + signature verification output, and any residual quick-tasks raised.
</output>
