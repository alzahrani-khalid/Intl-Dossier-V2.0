---
phase: 57-phase-52-deviation-closure-d-19-d-23
plan: 02
type: execute
wave: 2
depends_on: ['57-03']
files_modified:
  - frontend/src/pages/WorkBoard/WorkBoard.tsx
  - frontend/src/pages/WorkBoard/BoardColumn.tsx
  - frontend/src/pages/WorkBoard/__tests__/WorkBoard.test.tsx
  - frontend/src/pages/WorkBoard/__tests__/BoardColumn.test.tsx
  - eslint.config.mjs
  - tools/eslint-fixtures/bad-direct-dndkit-import.tsx
  - frontend/src/components/kanban/__tests__/eslint-ban-dndkit.test.ts
  - frontend/tests/e2e/kanban-visual.spec.ts-snapshots/kanban-ltr-1280-chromium-darwin.png
  - frontend/tests/e2e/kanban-visual.spec.ts-snapshots/kanban-ltr-768-chromium-darwin.png
  - frontend/tests/e2e/kanban-visual.spec.ts-snapshots/kanban-rtl-1280-chromium-darwin.png
  - frontend/tests/e2e/kanban-visual.spec.ts-snapshots/kanban-rtl-768-chromium-darwin.png
  - .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-PRE-MIGRATION-FAILURES.md
autonomous: false
requirements: [DEVIATE-02]
requirements_addressed: [DEVIATE-02]
tags: [workboard, dnd-kit, kanban-primitive, eslint-ban, visual-baselines, host-operator]

must_haves:
  truths:
    - 'frontend/src/pages/WorkBoard/WorkBoard.tsx imports KanbanProvider from @/components/kanban (NOT from @dnd-kit/core directly) (D-57-06, D-57-07)'
    - 'The 8 Phase 39 frontend/tests/e2e/kanban-*.spec.ts files exit 0 against the migrated /kanban route in CI/locally (DEVIATE-02 ROADMAP criterion #2)'
    - 'ESLint no-restricted-imports rule in eslint.config.mjs bans direct @dnd-kit/core imports under frontend/src/pages/** and frontend/src/components/** EXCEPT frontend/src/components/kanban/** (D-57-08)'
    - 'Positive-failure fixture tools/eslint-fixtures/bad-direct-dndkit-import.tsx exists and `pnpm exec eslint -c eslint.config.mjs --max-warnings 0 tools/eslint-fixtures/bad-direct-dndkit-import.tsx` exits 1 (D-57-08)'
    - 'Meta-test frontend/src/components/kanban/__tests__/eslint-ban-dndkit.test.ts exists and asserts ESLint exits non-zero on the bad fixture (mirrors existing eslint-ban.test.ts pattern) (D-57-08)'
    - 'The 4 kanban-*.spec.ts visual baselines (kanban-{ltr,rtl}-{1280,768}-chromium-darwin.png) are regenerated on the canonical macOS chromium host AND committed (D-57-09)'
    - 'Pre-migration failure triage report at .planning/phases/57-.../57-PRE-MIGRATION-FAILURES.md exists and records the 8 kanban-*.spec.ts files exact failure state on main HEAD before migration (D-57-10)'
  artifacts:
    - path: 'frontend/src/pages/WorkBoard/WorkBoard.tsx'
      provides: 'WorkBoard page consuming KanbanProvider + KanbanBoard + KanbanCard from @/components/kanban (no direct @dnd-kit/core imports)'
      contains: "from '@/components/kanban'"
    - path: 'eslint.config.mjs'
      provides: 'no-restricted-imports rule banning direct @dnd-kit/core outside frontend/src/components/kanban/**'
      contains: '@dnd-kit/core'
    - path: 'tools/eslint-fixtures/bad-direct-dndkit-import.tsx'
      provides: 'Positive-failure fixture proving the @dnd-kit/core ban fires'
      contains: '@dnd-kit/core'
    - path: 'frontend/src/components/kanban/__tests__/eslint-ban-dndkit.test.ts'
      provides: 'Vitest meta-test asserting ESLint non-zero exit on the bad fixture'
      contains: 'eslint'
    - path: '.planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-PRE-MIGRATION-FAILURES.md'
      provides: 'Pre-migration triage of the 4 known kanban-*.spec.ts regressions per D-57-10'
      contains: 'kanban-'
  key_links:
    - from: 'frontend/src/pages/WorkBoard/WorkBoard.tsx'
      to: 'frontend/src/components/kanban/'
      via: "import { KanbanProvider, KanbanBoard, KanbanCards, KanbanCard, type DragEndEvent } from '@/components/kanban'"
      pattern: "from '@/components/kanban'"
    - from: 'eslint.config.mjs no-restricted-imports rule'
      to: '@dnd-kit/core'
      via: 'paths/patterns entry with files/ignores carve-out for frontend/src/components/kanban/**'
      pattern: '@dnd-kit/core'
    - from: 'frontend/tests/e2e/kanban-visual.spec.ts'
      to: 'frontend/tests/e2e/kanban-visual.spec.ts-snapshots/*.png'
      via: 'Playwright toHaveScreenshot()'
      pattern: 'toHaveScreenshot'
---

<objective>
Close D-21 (Phase 39 kanban-*.spec.ts regression follow-up) by migrating frontend/src/pages/WorkBoard/WorkBoard.tsx to consume the shared @/components/kanban primitive (surface swap, NOT full absorb), regenerating the 4 /kanban visual baselines on the canonical host, and adding an ESLint boundary rule that prevents future direct @dnd-kit/core imports outside frontend/src/components/kanban/**.

Purpose: Phase 52 PASS-WITH-DEVIATION D-21 carried because WorkBoard.tsx still imports @dnd-kit/core directly (L29-40). The Phase 39 kanban-\*.spec.ts files target /kanban (WorkBoard) — they verify the shared @dnd-kit/core primitive only AFTER WorkBoard consumes it. This plan delivers that consumption.

Output:

- Migrated WorkBoard.tsx using KanbanProvider/KanbanBoard/KanbanCard/KanbanCards from @/components/kanban
- Preserved BoardToolbar, board.css; BoardColumn/KCard re-cast as render adapters under the shared primitive's slot API (D-57-07 surface swap)
- ESLint no-restricted-imports rule + positive-failure fixture + meta-test (3-layer template mirrors Phase 52 KANBAN-03)
- 4 regenerated visual baselines committed (host-operator task)
- Pre-migration failure triage report

**Execution context:** Inline on `main` with atomic per-task commits (D-57-20); verification sequencing across Phase 57 plans is D-19 → D-22 → D-21 → D-23 (D-57-21) — this plan's `wave` + `depends_on` frontmatter enforces that order.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-CONTEXT.md
@.planning/phases/52-heroui-v3-kanban-migration/52-VERIFICATION.md
@.planning/phases/52-heroui-v3-kanban-migration/52-05-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Shared Kanban primitive interfaces (from frontend/src/components/kanban/index.ts) -->

Exported symbols (frontend/src/components/kanban/index.ts):

```typescript
export { KanbanProvider, KanbanContext } from './KanbanProvider'
export type {
  KanbanColumnProps,
  KanbanContextProps,
  KanbanItemProps,
  KanbanProviderProps,
} from './KanbanProvider'
export { KanbanBoard } from './KanbanBoard'
export type { KanbanBoardProps } from './KanbanBoard'
export { KanbanCards } from './KanbanCards'
export type { KanbanCardsProps } from './KanbanCards'
export { KanbanCard } from './KanbanCard'
export type { KanbanCardProps } from './KanbanCard'
export { KanbanHeader } from './KanbanHeader'
export type { KanbanHeaderProps } from './KanbanHeader'
export type { DragEndEvent } from '@dnd-kit/core'
```

KanbanItemProps shape (from KanbanProvider.tsx):

```typescript
export type KanbanItemProps = {
  id: string
  name: string
  column: string
} & Record<string, unknown>
```

KanbanProviderProps signature (from KanbanProvider.tsx):

```typescript
export type KanbanProviderProps<T, C> = Omit<DndContextProps, 'children'> & {
  children: (column: C) => ReactNode
  className?: string
  columns: C[]
  data: T[]
  onDataChange?: (data: T[]) => void
  onDragStart?: (event: DragStartEvent) => void
  onDragEnd?: (event: DragEndEvent) => void
  onDragOver?: (event: DragOverEvent) => void
}
```

KanbanBoard exposes `data-droppable-id={id}` on its outer div (KanbanBoard.tsx L31).
KanbanCard exposes `data-card-id={id}` (KanbanCard.tsx L37).
The shared primitive INTERNALLY uses MouseSensor (distance:8), TouchSensor (delay:200, tolerance:5), KeyboardSensor (sortableKeyboardCoordinates).

Current WorkBoard.tsx direct @dnd-kit imports (L32-41):

```typescript
import {
  DndContext,
  KeyboardSensor,
  MouseSensor,
  TouchSensor,
  useSensor,
  useSensors,
  type DragEndEvent,
} from '@dnd-kit/core'
import { sortableKeyboardCoordinates } from '@dnd-kit/sortable'
```

ESLint ban template (from Phase 52 KANBAN-03, eslint.config.mjs L124-157 for kibo-ui):

```javascript
'no-restricted-imports': [
  'error',
  {
    paths: [...],
    patterns: [{ group: ['@/components/kibo-ui/*', ...], message: '...' }],
  },
],
```

ESLint fixture template (tools/eslint-fixtures/bad-kibo-ui-import.tsx):

```typescript
import { KanbanProvider } from '@/components/kibo-ui/kanban'
void KanbanProvider
export {}
```

ESLint meta-test template (frontend/src/components/kanban/**tests**/eslint-ban.test.ts) — uses execSync against pnpm exec eslint with 60_000ms timeout.
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-migration triage — record kanban-*.spec.ts failure state on main HEAD</name>
  <files>.planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-PRE-MIGRATION-FAILURES.md</files>
  <read_first>
    - .planning/phases/52-heroui-v3-kanban-migration/52-05-SUMMARY.md (the 4 observed regressions context per D-57-10)
    - frontend/tests/e2e/kanban-a11y.spec.ts (Phase 39 anchor; current shape against /kanban route)
    - frontend/tests/e2e/kanban-dnd.spec.ts
    - frontend/tests/e2e/kanban-filters.spec.ts
    - frontend/tests/e2e/kanban-render.spec.ts
    - frontend/tests/e2e/kanban-responsive.spec.ts
    - frontend/tests/e2e/kanban-rtl.spec.ts
    - frontend/tests/e2e/kanban-search.spec.ts
    - frontend/tests/e2e/kanban-visual.spec.ts
    - .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-CONTEXT.md (D-57-10 triage protocol)
  </read_first>
  <action>
    Operator-host task: run the 8 Phase 39 kanban-*.spec.ts files against current main HEAD before any migration code change. Command: `cd frontend && pnpm exec playwright test tests/e2e/kanban-a11y.spec.ts tests/e2e/kanban-dnd.spec.ts tests/e2e/kanban-filters.spec.ts tests/e2e/kanban-render.spec.ts tests/e2e/kanban-responsive.spec.ts tests/e2e/kanban-rtl.spec.ts tests/e2e/kanban-search.spec.ts tests/e2e/kanban-visual.spec.ts --project=chromium --reporter=list 2>&1 | tee /tmp/57-pre-migration.log`. Then author .planning/phases/57-.../57-PRE-MIGRATION-FAILURES.md with frontmatter (date, commit SHA, playwright --version, operator email) and a table: one row per failing/skipped/passed test with columns Spec | Test name | Status | Stack trace head | Likely cause (migration-induced vs structural). For each failure classify as (a) "likely fixed by migration" (e.g., shared primitive renders different DOM and current spec asserts old DOM shape), (b) "structural — fold into migration scope" (e.g., spec assumes a feature that does not exist regardless of primitive choice), or (c) "spec bug — write a small follow-up". The classification informs Task 2 scope. Include the exact failure summary line from the Playwright list reporter. If `pnpm exec playwright test` cannot run locally because dev server is unavailable, document the blocker and propose the smallest staging-server invocation that unblocks the triage (cite CLAUDE.md Test Credentials section).
  </action>
  <verify>
    <automated>test -f .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-PRE-MIGRATION-FAILURES.md && grep -q "commit SHA" .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-PRE-MIGRATION-FAILURES.md && grep -qE "kanban-(a11y|dnd|filters|render|responsive|rtl|search|visual)\.spec\.ts" .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-PRE-MIGRATION-FAILURES.md</automated>
    <human-check>Operator confirms /tmp/57-pre-migration.log captures the exact Playwright list-reporter output and that the triage table classifies every failing test as migration-induced, structural, or spec-bug.</human-check>
  </verify>
  <acceptance_criteria>
    - .planning/phases/57-.../57-PRE-MIGRATION-FAILURES.md exists with frontmatter (date, commit_sha, playwright_version, operator)
    - Triage table lists all 8 spec files with per-test rows
    - Every failure row has a classification: migration-induced | structural | spec-bug
    - File commits cleanly (`git diff --stat` shows only the new file)
  </acceptance_criteria>
  <done>Triage report committed. Failure classifications guide Task 2 scope (structural failures fold into the migration scope; spec-bugs become follow-up notes).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate WorkBoard to shared @/components/kanban primitive + ESLint ban + fixture + meta-test</name>
  <files>frontend/src/pages/WorkBoard/WorkBoard.tsx, frontend/src/pages/WorkBoard/BoardColumn.tsx, frontend/src/pages/WorkBoard/__tests__/WorkBoard.test.tsx, frontend/src/pages/WorkBoard/__tests__/BoardColumn.test.tsx, eslint.config.mjs, tools/eslint-fixtures/bad-direct-dndkit-import.tsx, frontend/src/components/kanban/__tests__/eslint-ban-dndkit.test.ts</files>
  <read_first>
    - frontend/src/pages/WorkBoard/WorkBoard.tsx (current direct @dnd-kit/core imports at L32-41 — replace these)
    - frontend/src/pages/WorkBoard/BoardColumn.tsx (current SortableContext usage at L18, L73 — note this still imports @dnd-kit/sortable directly; either keep it via the carve-out OR replace with the shared primitive's column slot — choose surface-swap path per D-57-07)
    - frontend/src/pages/WorkBoard/__tests__/WorkBoard.test.tsx (current @dnd-kit/core mock at L47-65 — update to mock the shared @/components/kanban surface instead)
    - frontend/src/components/kanban/KanbanProvider.tsx (shared primitive surface: KanbanProvider columns + data + onDataChange + onDragEnd)
    - frontend/src/components/kanban/KanbanBoard.tsx (data-droppable-id attribute + outer div)
    - frontend/src/components/kanban/KanbanCard.tsx (data-card-id attribute + useSortable wiring)
    - frontend/src/components/kanban/KanbanCards.tsx (SortableContext slot)
    - frontend/src/components/kanban/index.ts (full exported surface)
    - eslint.config.mjs L124-157 (existing kibo-ui ban — pattern to mirror)
    - eslint.config.mjs L286-311 (existing fixture-scoped rule blocks — pattern to mirror)
    - tools/eslint-fixtures/bad-kibo-ui-import.tsx (existing fixture template — exact shape to mirror)
    - frontend/src/components/kanban/__tests__/eslint-ban.test.ts (meta-test template — execSync + 60_000ms timeout pattern)
    - .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-PRE-MIGRATION-FAILURES.md (read Task 1 output; absorb any structural failures into this task's scope)
  </read_first>
  <behavior>
    - WorkBoard.tsx imports come from @/components/kanban (not @dnd-kit/core)
    - Visual / functional output of /kanban route MUST be preserved: same 4 columns (todo, in_progress, review, done), same KCard styling via board.css, same BoardToolbar, same client-side filter/search/overdue derivation, same drag-end mutation wiring through useUnifiedKanbanStatusUpdate
    - Cancelled-items filter (Confirmation #8 in WorkBoard.tsx) MUST remain — cancelled cards are filtered OUT of the visible board
    - D-03 sensor gating (DnD only when columnMode === 'status') MUST be preserved through the shared primitive (consult KanbanProvider props; the shared primitive accepts an optional sensors override via the DndContextProps spread at frontend/src/components/kanban/KanbanProvider.tsx:94 — pass an empty sensors array via the `...props` spread when mode !== 'status', OR wrap the board in a presentation-only path that does not render KanbanProvider; choose the simpler of the two and document)
    - 8 Phase 39 kanban-*.spec.ts files exit 0 after migration (excluding any spec-bug classifications from Task 1)
    - WorkBoard.test.tsx vitest unit tests pass; mock surface updated from @dnd-kit/core to @/components/kanban
    - ESLint rule fires on tools/eslint-fixtures/bad-direct-dndkit-import.tsx (exit 1)
    - Meta-test eslint-ban-dndkit.test.ts passes (asserts ESLint exits non-zero)
    - No other frontend/src file regresses to direct @dnd-kit/core import (workspace-wide `grep -rE "from ['\\\"]@dnd-kit/core['\\\"]" frontend/src --include='*.tsx' --include='*.ts'` returns only frontend/src/components/kanban/** matches)
  </behavior>
  <action>
    Step 1 — Edit eslint.config.mjs: extend the existing frontend `no-restricted-imports` block (L124-157) by adding a new pattern entry: `{ group: ['@dnd-kit/core', '@dnd-kit/core/*'], message: 'Direct @dnd-kit/core imports are banned outside frontend/src/components/kanban/*. Use the shared primitive (KanbanProvider, KanbanBoard, KanbanCards, KanbanCard) from @/components/kanban instead. See docs/adr/0001-mobile-dnd-scope-out.md and 57-CONTEXT.md D-57-08.' }`. Then add a carve-out config block targeting `frontend/src/components/kanban/**/*.{ts,tsx}` that disables the rule (`'no-restricted-imports': 'off'`) so the primitive itself can import @dnd-kit/core. Then add a fixture-scoped block (mirroring L294-311) for `tools/eslint-fixtures/bad-direct-dndkit-import.tsx` that re-applies the @dnd-kit/core ban with `'error'` severity.

    Step 2 — Create tools/eslint-fixtures/bad-direct-dndkit-import.tsx with content matching the bad-kibo-ui-import.tsx shape: a header comment citing 57-CONTEXT.md D-57-08, a single `import { DndContext } from '@dnd-kit/core'` line, `void DndContext`, and `export {}`.

    Step 3 — Create frontend/src/components/kanban/__tests__/eslint-ban-dndkit.test.ts mirroring eslint-ban.test.ts byte-for-byte except: describe name → 'direct @dnd-kit/core import ban fixture', it-title → 'fails lint once eslint.config.mjs bans direct @dnd-kit/core outside the shared primitive', fixture path → 'tools/eslint-fixtures/bad-direct-dndkit-import.tsx', timeout 60_000ms.

    Step 4 — Migrate WorkBoard.tsx: replace L32-41 direct @dnd-kit/core imports with `import { KanbanProvider, KanbanBoard, KanbanCards, KanbanCard, type DragEndEvent } from '@/components/kanban'`. Replace L132-141 sensor stack with the shared primitive's internal sensors (KanbanProvider wires MouseSensor/TouchSensor/KeyboardSensor itself; delete the local `useSensor`/`useSensors` calls). To preserve D-03 sensor gating, pass `sensors={[]}` to KanbanProvider when `mode !== 'status'` (KanbanProvider spreads DndContextProps including the optional `sensors` prop — verify against KanbanProvider.tsx:94 spread). Replace the L239 `<DndContext sensors={dndSensors} onDragEnd={handleDragEnd}>` wrapper with `<KanbanProvider columns={columnDescriptors} data={kanbanItems} onDragEnd={handleDragEnd} sensors={mode === 'status' ? undefined : []}>` where `columnDescriptors` is `STAGES.map(stage => ({ id: stage, name: t(`columns.${stage}`) }))` and `kanbanItems` projects the existing `visibleItems` into `{ id, name, column: stage, ...rest }` KanbanItemProps shape. Keep BoardColumn + BoardToolbar + KCard exactly as they are visually (board.css and KCard.tsx untouched); the BoardColumn render adapter consumes the column descriptor passed by KanbanProvider's `children: (column: C) => ReactNode` callback. Inside each BoardColumn, replace the existing SortableContext (BoardColumn.tsx L73) with `KanbanCards` + per-item `KanbanCard` from the shared primitive (KanbanCard provides useSortable + data-card-id automatically). Where KCard's existing styling (kcard, overdue, done classes) attaches to the article element, render KCard INSIDE the shared KanbanCard's `children` slot — KanbanCard wraps with a div + Card; KCard's article becomes a nested presentational child. Verify that the resulting DOM still has `data-droppable-id="todo|in_progress|review|done"` on the column root AND `data-card-id="..."` on each card so the existing 8 kanban-*.spec.ts selectors continue to resolve.

    Step 5 — Edit BoardColumn.tsx to remove the now-redundant `import { SortableContext, verticalListSortingStrategy } from '@dnd-kit/sortable'` (L18) and the SortableContext wrapper at L72-77; render KCards directly inside KanbanCards from the shared primitive. The BoardColumn becomes a thin render adapter: header (h3 + count + add button) + slot for the shared primitive's KanbanCards. If removing @dnd-kit/sortable from BoardColumn introduces a workspace-wide import that no longer resolves, audit and clean up the unused dependency cascade. Do NOT delete board.css; do NOT delete KCard.tsx.

    Step 6 — Update WorkBoard.test.tsx + BoardColumn.test.tsx: replace `vi.mock('@dnd-kit/core', ...)` blocks with `vi.mock('@/components/kanban', () => ({ KanbanProvider: <minimal mock that captures onDragEnd + renders children>, KanbanBoard: <pass-through div with data-droppable-id>, KanbanCards: <pass-through>, KanbanCard: <pass-through with data-card-id> }))`. The existing assertion on `lastDragEndHandler` (WorkBoard.test.tsx L51-65) re-targets the shared primitive's onDragEnd. Update BoardColumn.test.tsx if it references SortableContext directly.

    Step 7 — Workspace gate (operator host): `cd frontend && pnpm typecheck && pnpm lint && pnpm exec vitest run src/pages/WorkBoard src/components/kanban`. All three exit 0. Then run the 8 Phase 39 specs: `pnpm exec playwright test tests/e2e/kanban-a11y.spec.ts tests/e2e/kanban-dnd.spec.ts tests/e2e/kanban-filters.spec.ts tests/e2e/kanban-render.spec.ts tests/e2e/kanban-responsive.spec.ts tests/e2e/kanban-rtl.spec.ts tests/e2e/kanban-search.spec.ts tests/e2e/kanban-visual.spec.ts --project=chromium --reporter=list`. All 8 specs exit 0 (excluding any spec-bug classifications carried over from Task 1 — those become a follow-up note in 57-02-SUMMARY).

    Step 8 — Atomic commit cadence per D-57-19: separate commits for (1) ESLint rule + fixture + meta-test, (2) WorkBoard.tsx + BoardColumn.tsx migration, (3) WorkBoard.test.tsx + BoardColumn.test.tsx mock surface update, (4) baseline regen + SUMMARY (in Task 3).

  </action>
  <verify>
    <automated>grep -q "from '@/components/kanban'" frontend/src/pages/WorkBoard/WorkBoard.tsx && ! grep -E "^import .* from '@dnd-kit/core'" frontend/src/pages/WorkBoard/WorkBoard.tsx && grep -q "@dnd-kit/core" eslint.config.mjs && test -f tools/eslint-fixtures/bad-direct-dndkit-import.tsx && test -f frontend/src/components/kanban/__tests__/eslint-ban-dndkit.test.ts && cd frontend && pnpm exec eslint -c ../eslint.config.mjs --max-warnings 0 ../tools/eslint-fixtures/bad-direct-dndkit-import.tsx; test $? -eq 1</automated>
    <human-check>Operator confirms: (a) `pnpm --filter ./frontend typecheck` exits 0, (b) `pnpm --filter ./frontend lint` exits 0, (c) `pnpm --filter ./frontend exec vitest run src/pages/WorkBoard src/components/kanban` exits 0, (d) the 8 kanban-*.spec.ts files exit 0 against /kanban on the migrated build (or document remaining failures as spec-bugs with rationale).</human-check>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/WorkBoard/WorkBoard.tsx has NO direct `from '@dnd-kit/core'` import; the only import path is `from '@/components/kanban'`
    - frontend/src/pages/WorkBoard/BoardColumn.tsx has NO direct `from '@dnd-kit/sortable'` or `from '@dnd-kit/core'` import
    - `grep -rE "from ['\"]@dnd-kit/(core|sortable|utilities)['\"]" frontend/src --include='*.tsx' --include='*.ts'` returns ONLY matches under frontend/src/components/kanban/**
    - eslint.config.mjs contains the new pattern entry banning `@dnd-kit/core` with the carve-out for frontend/src/components/kanban/**
    - `cd frontend && pnpm exec eslint -c ../eslint.config.mjs --max-warnings 0 ../tools/eslint-fixtures/bad-direct-dndkit-import.tsx` exits with code 1
    - frontend/src/components/kanban/__tests__/eslint-ban-dndkit.test.ts passes under `pnpm --filter ./frontend exec vitest run src/components/kanban/__tests__/eslint-ban-dndkit.test.ts` (60_000ms timeout)
    - `pnpm --filter ./frontend typecheck` exits 0
    - `pnpm --filter ./frontend lint` exits 0
    - `pnpm --filter ./frontend exec vitest run src/pages/WorkBoard` exits 0
    - The 8 Phase 39 kanban-*.spec.ts files exit 0 against the migrated build on the operator host (excluding any spec-bug carryovers documented in 57-PRE-MIGRATION-FAILURES.md classification (c))
    - data-droppable-id="todo|in_progress|review|done" attributes still present in the /kanban DOM (verified by kanban-dnd.spec.ts selectors at runtime)
  </acceptance_criteria>
  <done>WorkBoard consumes the shared primitive; ESLint guard + fixture + meta-test in place; 8 Phase 39 specs green; workspace lint/typecheck/vitest all green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Regenerate the 4 kanban-*.spec.ts visual baselines on canonical host (D-57-09)</name>
  <what-built>WorkBoard now renders the shared @dnd-kit/core primitive's DOM (data-droppable-id, data-card-id, Card primitive wrapping KCard). The existing kanban-{ltr,rtl}-{1280,768}-chromium-darwin.png baselines were captured against the OLD direct-import WorkBoard render — they now diverge from the new render. The 4 baselines must be regenerated on the canonical macOS chromium host (Phase 46 + Phase 52 baseline-regeneration precedent).</what-built>
  <how-to-verify>
    1. Operator confirms current branch is the Phase 57 working branch with Task 2 commits landed.
    2. Operator runs (host-side): `cd frontend && pnpm exec playwright test tests/e2e/kanban-visual.spec.ts --project=chromium --update-snapshots --reporter=list 2>&1 | tee /tmp/57-baseline-regen.log`
    3. Operator confirms 4 PNG files at `frontend/tests/e2e/kanban-visual.spec.ts-snapshots/` are modified (git status shows changes to `kanban-{ltr,rtl}-{1280,768}-chromium-darwin.png`)
    4. Operator runs stability replay 3× without `--update-snapshots`: `for i in 1 2 3; do pnpm exec playwright test tests/e2e/kanban-visual.spec.ts --project=chromium --reporter=list 2>&1 | tail -10; done` — each iteration MUST exit 0
    5. Operator runs Plan 57-03's hash-comparison meta-test (which exists by Wave 1 close; this plan depends on 57-03): `pnpm --filter ./frontend exec vitest run tests/security/baseline-byte-distinction.test.ts` MUST exit 0 (asserts ltr vs rtl PNGs are byte-distinct after regen)
    6. Visually inspect the 4 PNGs: confirm 4 columns visible (todo, in_progress, review, done), no cancelled column rendered (WorkBoard filters cancelled out per Confirmation #8), KCard styling (border-inline-start danger for overdue, opacity 0.55 for done) preserved, and LTR baseline reads left-to-right while RTL baseline reads right-to-left with Tajawal font applied.
    7. Operator commits the 4 PNG changes with message `chore(57-02): regenerate /kanban visual baselines for shared primitive migration (D-21)` — separate atomic commit from Task 2 code changes.
  </how-to-verify>
  <resume-signal>Type "baselines-regenerated" (with the commit SHA of the baseline-only commit) or describe blocker.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                                |
| --------------------------------- | ------------------------------------------------------------------------------------------ |
| client→/kanban route              | TanStack Router renders WorkBoard with user-supplied filters; existing auth flow unchanged |
| WorkBoard ↔ useUnifiedKanban hook | mutation crosses to Supabase RLS; data path unchanged by migration                         |
| ESLint boundary rule              | static analysis on TypeScript source; runtime exposure: none                               |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                     | Disposition | Mitigation Plan                                                                                                                                                                                                                  |
| ---------- | ---------------------- | ------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-57-02-01 | Tampering              | WorkBoard migration preserves drag-end mutation payload shape | mitigate    | `handleDragEnd` signature unchanged: `(event: DragEndEvent) => void`; payload to `useUnifiedKanbanStatusUpdate.mutate` (itemId/source/newStatus/newWorkflowStage) identical pre/post; WorkBoard.test.tsx mocks assert this       |
| T-57-02-02 | Information Disclosure | board.css token regressions during regen                      | accept      | No CSS rewrite; board.css is untouched. D-05 design-token rule at `error` would fire if anyone reintroduced raw hex                                                                                                              |
| T-57-02-03 | Denial of Service      | ESLint rule false-positives blocking unrelated PRs            | mitigate    | The ban is scoped via `patterns` (group: '@dnd-kit/core', '@dnd-kit/core/\*') with a per-file carve-out for frontend/src/components/kanban/\*\*; existing primitive untouched                                                    |
| T-57-02-04 | Spoofing               | Fixture file misclassified as production code                 | mitigate    | tools/eslint-fixtures/bad-direct-dndkit-import.tsx lives outside frontend/src and has `export {}` to mark it as a typechecked but isolated module; ESLint fixture block (eslint.config.mjs) scopes the ban back ON for this file |

</threat_model>

<verification>
- WorkBoard.tsx no longer imports @dnd-kit/core directly; all imports come from @/components/kanban
- BoardColumn.tsx no longer imports @dnd-kit/sortable
- 8 Phase 39 kanban-*.spec.ts files exit 0 against the migrated build (host operator verifies)
- ESLint ban + fixture + meta-test 3-layer template in place
- 4 visual baselines regenerated on canonical host AND committed AND pass byte-distinction check from 57-03
- WorkBoard.test.tsx + BoardColumn.test.tsx vitest unit tests pass with updated mock surface
- `pnpm --filter ./frontend lint` exits 0 (the new ban does not break any existing imports)
</verification>

<success_criteria>

- DEVIATE-02 ROADMAP success criterion #2 met: 8 Phase 39 kanban-\*.spec.ts run green against the shared @dnd-kit/core primitive
- 52-VERIFICATION.md D-21-PHASE-39-REGRESSION-DEFERRED row can be flipped from `passed_with_deviation` to `passed`
- Future regressions prevented by the ESLint boundary rule (positive-failure fixture proves the rule fires)
- /kanban visual fidelity preserved (regenerated baselines + stability replay confirm)
  </success_criteria>

<output>
Create `.planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-02-SUMMARY.md` when done. Include: pre-migration failure triage findings (Task 1), exact files modified, commit SHAs (atomic cadence per D-57-19), workspace gate exit codes, 8 spec pass/fail table, link to baseline-regen commit, any spec-bug carryovers folded forward to a follow-up phase or quick-task.
</output>
