---
phase: 02-naming-file-structure
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - frontend/src/components/AfterActionForm.tsx
  - frontend/src/components/AttachmentUploader.tsx
  - frontend/src/components/DossierCard.tsx
  - frontend/src/components/ErrorBoundary.tsx
  - frontend/src/components/theme-error-boundary.tsx
  # Plus ~32 more standalone .tsx files at components/ root
  # Each moves into its own kebab-case subdirectory
autonomous: true
requirements: [ARCH-01]

must_haves:
  truths:
    - 'Zero standalone .tsx files remain at frontend/src/components/ root (all wrapped in kebab-case subdirs)'
    - 'Component files inside new subdirectories are PascalCase'
    - 'No barrel exports (index.ts) exist in any component directory'
    - 'All import paths referencing moved files are updated'
    - 'pnpm build succeeds with zero errors'
  artifacts:
    - path: 'frontend/src/components/error-boundary/ErrorBoundary.tsx'
      provides: 'Moved standalone component in kebab-case subdir'
    - path: 'frontend/src/components/dossier-card/DossierCard.tsx'
      provides: 'Moved standalone component in kebab-case subdir'
    - path: 'frontend/src/components/after-action-form/AfterActionForm.tsx'
      provides: 'Moved standalone component in kebab-case subdir'
  key_links:
    - from: 'all consumers of ErrorBoundary'
      to: 'frontend/src/components/error-boundary/ErrorBoundary.tsx'
      via: 'updated import path'
      pattern: "from '@/components/error-boundary/ErrorBoundary'"
    - from: 'all consumers of DossierCard'
      to: 'frontend/src/components/dossier-card/DossierCard.tsx'
      via: 'updated import path'
      pattern: "from '@/components/dossier-card/DossierCard'"
---

<objective>
Move all ~37 standalone component .tsx files from the frontend/src/components/ root into their own kebab-case subdirectories, preserving PascalCase filenames inside.

Purpose: Enforces D-02 (standalone files get kebab-case subdirectories) and D-04 (PascalCase files inside dirs). Depends on Plan 01 because some standalone files may reference renamed hooks/directories.

Output: ~37 new kebab-case subdirectories, each containing one PascalCase component file, with all import paths updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-naming-file-structure/02-CONTEXT.md
@.planning/phases/02-naming-file-structure/02-RESEARCH.md
@.planning/phases/02-naming-file-structure/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit standalone component files at components/ root and deduplicate</name>
  <files>
    frontend/src/components/*.tsx (all standalone files at root)
  </files>
  <read_first>
    frontend/src/components/
  </read_first>
  <action>
    Before moving files, audit the exact list of standalone .tsx files at `frontend/src/components/` root:
    1. Run `ls frontend/src/components/*.tsx` to get the complete list
    2. Check for duplicates: Research noted 2 possible duplicates (AfterActionForm.tsx and AttachmentUploader.tsx may exist both at root and inside subdirs). For each duplicate:
       - Compare file contents to determine which is canonical
       - If root copy is identical to subdir copy, delete the root copy (it is redundant)
       - If different, keep the subdir version (it is likely more up-to-date) and delete the root copy
       - Run `pnpm knip` to verify no broken references after dedup
    3. Produce the final list of standalone files that need subdirectories

    Also handle the edge case from research: `theme-error-boundary.tsx` is already kebab-case but per D-02 still needs a subdirectory. Per D-04, rename the FILE to PascalCase inside its new dir:
    - `theme-error-boundary.tsx` -> `theme-error-boundary/ThemeErrorBoundary.tsx`
    - Update the import path AND any `export default` references

    Output: A clean, deduplicated list of files ready for Task 2. No standalone .tsx duplicates remain.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && pnpm build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - No duplicate component files exist (same component at root AND in a subdir)
    - `pnpm build` exits 0 after dedup
    - `pnpm knip` reports zero new unused exports/files from dedup
  </acceptance_criteria>
  <done>Standalone file list audited, duplicates resolved, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Move all standalone component files into kebab-case subdirectories and update imports</name>
  <files>
    Each standalone .tsx at frontend/src/components/ root moves to:
    frontend/src/components/{kebab-case-name}/{PascalCaseName}.tsx

    Examples:
    - AfterActionForm.tsx -> after-action-form/AfterActionForm.tsx
    - DossierCard.tsx -> dossier-card/DossierCard.tsx
    - ErrorBoundary.tsx -> error-boundary/ErrorBoundary.tsx
    - IntakeTicketModal.tsx -> intake-ticket-modal/IntakeTicketModal.tsx
    + all files importing from these components (~37 import references)

  </files>
  <read_first>
    frontend/src/components/ErrorBoundary.tsx
    frontend/src/components/DossierCard.tsx
  </read_first>
  <action>
    Per D-02, D-03, D-04, D-09: Move each standalone component file into its own kebab-case subdirectory using `git mv`. The PascalCase filename is preserved inside the new directory.

    For each standalone .tsx file at `frontend/src/components/` root:
    1. Derive the kebab-case directory name from the PascalCase filename:
       - `AfterActionForm.tsx` -> directory `after-action-form/`
       - `DossierCard.tsx` -> directory `dossier-card/`
       - `ErrorBoundary.tsx` -> directory `error-boundary/`
       - General rule: split on uppercase, lowercase, join with hyphens
    2. Create the directory: `mkdir -p frontend/src/components/{kebab-case-dir}/`
    3. Move the file: `git mv frontend/src/components/FooBar.tsx frontend/src/components/foo-bar/FooBar.tsx`
    4. Find all import references: `grep -rl "from '@/components/FooBar'" frontend/src/` and `grep -rl "from '@/components/FooBar'" tests/`
    5. Update each import from `@/components/FooBar` to `@/components/foo-bar/FooBar`
    6. Per D-03: Do NOT create index.ts barrel exports in the new directories

    Process files in batches by import count (highest first) to catch issues early:
    - High-ref files first: ErrorBoundary, DossierCard, IntakeTicketModal
    - Then medium-ref files
    - Then zero-ref files (may be dead code -- move anyway, Knip will flag later)

    After all moves and import updates:
    1. Run `pnpm build` to verify zero broken imports
    2. Run `pnpm dev` briefly to regenerate `routeTree.gen.ts` if it changed
    3. Verify no `index.ts` files were accidentally created: `find frontend/src/components/ -name 'index.ts' -not -path '*/ui/*' -not -path '*/work-creation/*'`

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && ls frontend/src/components/*.tsx 2>/dev/null | wc -l | grep -q '^0$' && echo "PASS: No standalone tsx at root" || echo "FAIL: Standalone tsx files remain"</automated>
  </verify>
  <acceptance_criteria>
    - `ls frontend/src/components/*.tsx 2>/dev/null` returns empty (zero standalone .tsx files at root)
    - `find frontend/src/components/ -maxdepth 2 -name '*.tsx' -path '*/[a-z]*/*[A-Z]*' | head -5` returns results (PascalCase files inside kebab-case dirs)
    - `find frontend/src/components/ -name 'index.ts' -not -path '*/ui/*' -not -path '*/work-creation/*' | wc -l` returns 0 (no barrel exports per D-03)
    - `grep -r "from '@/components/[A-Z][a-zA-Z]*'" frontend/src/ | grep -v "from '@/components/ui/" | wc -l` returns 0 (no old direct-root import paths)
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>All standalone component files moved into kebab-case subdirectories with PascalCase filenames, all import paths updated, no barrel exports, pnpm build passes</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `ls frontend/src/components/*.tsx 2>/dev/null` returns nothing (no standalone files at root)
2. `find frontend/src/components/ -name 'index.ts' -not -path '*/ui/*' -not -path '*/work-creation/*' | wc -l` returns 0
3. `pnpm build` exits 0
4. `pnpm knip` reports zero new findings
</verification>

<success_criteria>

- Zero standalone .tsx files at frontend/src/components/ root
- Each former standalone file is in a kebab-case subdirectory with PascalCase filename
- No barrel exports (index.ts) in any new component directory
- All import paths updated
- pnpm build passes
- git blame preserved (used git mv)
  </success_criteria>

<output>
After completion, create `.planning/phases/02-naming-file-structure/02-02-SUMMARY.md`
</output>
