---
phase: 66-overview-error-contract-timeline-cross-links
plan: 06
type: execute
wave: 2
depends_on: [66-01]
files_modified:
  - frontend/src/components/dossier/DossierDrawer/DossierDrawer.tsx
  - frontend/src/components/dossier/DossierDrawer/DrawerHead.tsx
  - frontend/src/components/dossier/DossierDrawer/__tests__/DossierDrawer.test.tsx
  - frontend/src/components/dossier/dossier-overview/DossierOverview.tsx
  - frontend/src/components/dossier/DossierActivityTimeline.tsx
  - frontend/src/i18n/en/dossier-overview.json
  - frontend/src/i18n/ar/dossier-overview.json
autonomous: true
requirements: [OVRERR-01]

key_decisions:
  - 'Drawer error branch reuses the DORMANT dossier-drawer:error.* keys verbatim (A-5 — verified present in en+ar: load_failed_heading/load_failed_body/retry); ZERO new drawer i18n keys'
  - 'The drawer is the ONLY surface with a Retry button this phase (A-3); cards rely on the global TanStack retry'
  - 'Raw error.message renders are removed from BOTH existing surfaces (Pitfall 9): DossierOverview.tsx L234 → t(error.description) only; DossierActivityTimeline.tsx L176 → its existing local t(timeline.error) key only (A-6 alternative — no second new key)'
  - 'Checker FLAG (UI-SPEC sign-off, non-blocking) implemented while owning the file: dossier-overview.json error.title sentence-cased in BOTH locales if currently Title Case; this plan is the sole owner of dossier-overview.json this phase'

must_haves:
  truths:
    - 'A failed overview query with no cached data renders the drawer error branch (heading + body + Retry, role="alert") — NEVER a permanent skeleton; data-loading is false in that state'
    - 'Retry triggers the overview query refetch'
    - 'With cached overview present and a failed background refetch, the drawer renders its sections normally (stale-while-error)'
    - 'DrawerHead never strands in its own skeleton: on a dossier-core error the head keeps its chrome and functional close button with title fallback —'
    - 'No surface touched by this plan renders error.message: DossierOverview and DossierActivityTimeline error states show only generic localized copy'
  artifacts:
    - path: 'frontend/src/components/dossier/DossierDrawer/DossierDrawer.tsx'
      provides: 'Error branch between skeleton and sections'
      contains: 'load_failed_heading'
    - path: 'frontend/src/components/dossier/DossierDrawer/__tests__/DossierDrawer.test.tsx'
      provides: 'Error-branch + stale-while-error + retry tests (66-VALIDATION row 3)'
  key_links:
    - from: 'frontend/src/components/dossier/DossierDrawer/DossierDrawer.tsx'
      to: 'frontend/src/i18n/en/dossier-drawer.json (dormant error.* keys)'
      via: "t('error.load_failed_heading') in the dossier-drawer namespace"
      pattern: 'load_failed_heading'
    - from: 'frontend/src/components/dossier/DossierDrawer/DossierDrawer.tsx'
      to: 'useDossierOverview refetch'
      via: 'Retry button onClick'
      pattern: 'refetch'
---

<objective>
Close the drawer's third dishonest mode (permanent skeleton on error) with an explicit error branch + Retry, harden DrawerHead against core-query errors, and remove the two raw error.message renders (information disclosure, Pitfall 9).

Purpose: after 66-01 lands fail-the-query, DossierDrawer's `overviewLoading || !overview` keying would hold the skeleton FOREVER on error (Pitfall 5) — neither empty nor failed. The raw-message fixes complete the OVRERR-01 security posture on the surfaces this phase touches.
Output: honest drawer (UI-SPEC §2) + sanitized error copy on DossierOverview/DossierActivityTimeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/66-overview-error-contract-timeline-cross-links/66-UI-SPEC.md

<interfaces>
<!-- Drawer error branch — UI-SPEC §2, BINDING. Current body (DossierDrawer.tsx L94-106):
     data-loading={overviewLoading || !overview} → skeleton-or-sections. Target structure: -->

```tsx
const { data: overview, isLoading: overviewLoading, isError: overviewIsError, refetch } =
  useDossierOverview(dossierId ?? undefined, { /* options unchanged */ })
const overviewFailed = overviewIsError && overview === null  // hook returns data || null

// body:
<div className="drawer-body" data-loading={overviewLoading && !overview ? 'true' : 'false'}>
  {overviewFailed ? (
    <div role="alert" className="px-4 py-16 text-center space-y-2">
      <p className="text-sm font-semibold text-[var(--danger)]">{t('error.load_failed_heading')}</p>
      <p className="text-sm text-muted-foreground">{t('error.load_failed_body')}</p>
      <Button variant="outline" size="sm" className="min-h-11" onClick={() => void refetch()}>
        {t('error.retry')}
      </Button>
    </div>
  ) : overviewLoading || !overview ? (
    <DrawerSkeleton />
  ) : (
    <>{/* existing five sections unchanged */}</>
  )}
</div>
```

<!-- Dormant keys (VERIFIED in both locales — reuse verbatim, no JSON edits):
     dossier-drawer:error.load_failed_heading  "Could not load this dossier" / "تعذّر تحميل هذا الملف"
     dossier-drawer:error.load_failed_body     "Try refreshing the page or open the full dossier." / "حاول تحديث الصفحة أو افتح الملف الكامل."
     dossier-drawer:error.retry                "Retry" / "إعادة المحاولة"
     Button import: @/components/ui/button (existing outline variant only — no danger button). -->

<!-- DrawerHead (DrawerHead.tsx ~L47-70): destructures { data } from useDossier + useDossierOverview;
     name falls back to ''. Target: also read the dossier query's error/isError; when the core query
     errored and no dossier data, title renders '—' and all chrome (type chip, close button) stays
     functional. No head-level alert — the body branch carries the message (UI-SPEC §2). -->

<!-- Raw-message fixes (line-verified):
     DossierOverview.tsx L234:  {error?.message || t('error.description')}  →  {t('error.description')}
     DossierActivityTimeline.tsx L176:  {error?.message || t('timeline.error', 'Failed to load activities')}
                                        →  {t('timeline.error', 'Failed to load activities')}
     Raw detail goes to console.error only if a log already exists — do not add render-time logging. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drawer error-branch tests — RED first</name>
  <files>frontend/src/components/dossier/DossierDrawer/__tests__/DossierDrawer.test.tsx</files>
  <read_first>
    - frontend/src/components/dossier/DossierDrawer/__tests__/DossierDrawer.test.tsx (existing mock structure for useDossierDrawer/useDossier/useDossierOverview — extend, do not restructure)
  </read_first>
  <behavior>
    - Test (RED): overview mock { isError: true, data: null, isLoading: false, refetch: spy } → role="alert" present with the load_failed_heading copy; DrawerSkeleton ABSENT; drawer-body data-loading="false".
    - Test (RED): clicking Retry calls the refetch spy once.
    - Test (RED): stale-while-error — overview mock { isError: true, data: <minimal overview> } → sections render (e.g. MiniKpiStrip content), no alert.
    - Test (RED): DrawerHead with dossier query { isError: true, data: undefined } → close button still rendered and clickable; title region shows '—'.
  </behavior>
  <action>
    Extend the existing test file with the four cases (decision-tagged names referencing OVRERR-01/Pitfall 5). Reuse its existing render harness and mocks; add isError/refetch fields to the mocked hook returns. Run: all four RED. Commit: test(66-06): add failing drawer error-branch tests.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run 'src/components/dossier/DossierDrawer/__tests__/DossierDrawer.test.tsx' 2>&1 | grep -qE 'failed' && echo RED_CONFIRMED</automated>
  </verify>
  <done>Error-branch, retry, stale, and head-resilience cases exist and are RED; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Drawer error branch + DrawerHead fallback — GREEN</name>
  <files>frontend/src/components/dossier/DossierDrawer/DossierDrawer.tsx, frontend/src/components/dossier/DossierDrawer/DrawerHead.tsx</files>
  <action>
    Implement the interfaces-block structure in DossierDrawer.tsx: destructure isError + refetch, compute overviewFailed, reorder the body branches (error → skeleton → sections), set data-loading true only while genuinely loading without data. Import Button from @/components/ui/button. Keys via the file's existing useTranslation('dossier-drawer') instance. In DrawerHead.tsx: read the dossier query's error state and fall the title back to '—' when the core query failed with no data, keeping chip + close button untouched (close must never depend on query health). All spacing per UI-SPEC (px-4 py-16 space-y-2, min-h-11 Retry); logical properties only; no new shadows/colors. Run Task 1 file (GREEN) + pnpm type-check. Commit: feat(66-06): drawer error branch replaces permanent skeleton (OVRERR-01, Pitfall 5).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run 'src/components/dossier/DossierDrawer/__tests__/DossierDrawer.test.tsx' && pnpm type-check && grep -q 'load_failed_heading' src/components/dossier/DossierDrawer/DossierDrawer.tsx</automated>
  </verify>
  <done>Forced overview failure shows heading/body/Retry; cached data still renders; head never strands.</done>
</task>

<task type="auto">
  <name>Task 3: Remove raw error.message renders + sentence-case retitle (Pitfall 9 + checker FLAG)</name>
  <files>frontend/src/components/dossier/dossier-overview/DossierOverview.tsx, frontend/src/components/dossier/DossierActivityTimeline.tsx, frontend/src/i18n/en/dossier-overview.json, frontend/src/i18n/ar/dossier-overview.json</files>
  <read_first>
    - frontend/src/components/dossier/dossier-overview/DossierOverview.tsx (ONLY the error card region ~L226-243)
    - frontend/src/components/dossier/DossierActivityTimeline.tsx (ONLY the error region ~L170-180)
  </read_first>
  <action>
    1. DossierOverview.tsx L234: drop the error?.message fallback chain — render t('error.description') only. Keep the retry button and card layout untouched.
    2. DossierActivityTimeline.tsx L176: drop error?.message — render the existing t('timeline.error', 'Failed to load activities') only.
    3. dossier-overview.json (en + ar): if error.title is Title Case (e.g. 'Failed to Load Overview'), sentence-case it per the project voice rule; mirror any needed AR adjustment. Touch nothing else in those files.
    4. Repo-grep the two components to confirm no other error.message render remains in them. Run pnpm type-check + the quick suites for both component dirs if tests exist. Commit: fix(66-06): generic localized error copy — no raw PostgREST messages in UI (OVRERR-01, Pitfall 9).
  </action>
  <verify>
    <automated>cd frontend && pnpm type-check && ! grep -n 'error?.message\|error\.message' src/components/dossier/dossier-overview/DossierOverview.tsx src/components/dossier/DossierActivityTimeline.tsx | grep -q .</automated>
  </verify>
  <done>No touched surface can leak column/table names or English-only backend strings to users.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                                          |
| ------------------------- | -------------------------------------------------------------------- |
| query error objects → DOM | PostgREST/edge error messages must never render (column/table names) |

## STRIDE Threat Register

| Threat ID | Category                 | Component                                 | Disposition | Mitigation Plan                                                                                  |
| --------- | ------------------------ | ----------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------ |
| T-66-11   | Information Disclosure   | DossierOverview + DossierActivityTimeline | mitigate    | Raw error?.message render chains deleted (Task 3); grep gate proves absence                      |
| T-66-12   | Repudiation (of failure) | DossierDrawer permanent skeleton          | mitigate    | Explicit error branch with role="alert" + Retry; data-loading honest; tests pin all three states |
| T-66-13   | Denial of Service (UX)   | DrawerHead close button on failed queries | mitigate    | Close functionality asserted independent of query health (test case 4)                           |
| T-66-SC   | Tampering                | npm/pip/cargo installs                    | accept      | No package installs in this plan                                                                 |

</threat_model>

<verification>
- 66-VALIDATION row 3 realized (drawer error branch, extend existing test file)
- Per-task gate: quick vitest run of the drawer test + pnpm type-check
</verification>

<success_criteria>

- Drawer renders exactly one of: skeleton (loading), error branch (failed, no cache), sections (data) — never a stuck skeleton
- Zero raw error.message renders on touched surfaces
  </success_criteria>

<output>
Create `.planning/phases/66-overview-error-contract-timeline-cross-links/66-06-SUMMARY.md` when done.
</output>
