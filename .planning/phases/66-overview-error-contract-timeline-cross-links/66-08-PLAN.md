---
phase: 66-overview-error-contract-timeline-cross-links
plan: 08
type: execute
wave: 3
depends_on: [66-01, 66-02, 66-03, 66-04, 66-05, 66-06, 66-07]
files_modified: []
autonomous: true
requirements: [OVRERR-01, OVRERR-02]

key_decisions:
  - 'ORCHESTRATOR-RUN plan: executor agents have NO Supabase MCP and NO browser — the orchestrator executes this plan inline (64-06/65-06 precedent). It deploys the 66-03 edge change via Supabase MCP/CLI and runs the live forced-error protocol via browser CDP'
  - 'Forced errors are CLIENT-SIDE network blocks ONLY (CDP Network.setBlockedURLs) — NEVER RLS/grant tampering, which returns empty 200s and exercises the EMPTY path (Pitfall 1). Zero staging rows are created; the only cleanup is unblocking'
  - 'Staging facts pre-answered (RESEARCH §Open Question Answers, 2026-06-13): calendar_entries = 6 (dead /calendar/<uuid> emission IS live-reproducible pre-deploy), mous = 0 (mou dead link data-dormant), activity_stream nav-url rows = 0. Do NOT re-run these as open questions — re-probe only the post-deploy emission'

must_haves:
  truths:
    - 'unified-timeline is REDEPLOYED and its live output for a dossier with calendar rows contains NO /calendar/<uuid>, /mous/<uuid>, or ?tab= navigation_url values; calendar rows carry null'
    - 'A CDP-blocked section table renders the danger error line on exactly the cards whose includeSections variant contains that section (pre-recorded blast radius, Pitfall 4) — while a genuinely empty dossier (Saudi) renders unchanged muted empty copy: empty and failed are visually distinct in the same session'
    - 'Unblocking + refetch recovers every errored card without reload; a forced drawer failure shows the error branch (never a stuck skeleton) and Retry recovers it'
    - 'AR pass: Arabic error copy under dir=rtl with Tajawal at 1280 and 1024 with no overflow'
    - 'Phase gates green: full vitest suite, pnpm type-check exit 0, size-limit with zero exceeded lines (whole log)'
  artifacts:
    - path: '.planning/phases/66-overview-error-contract-timeline-cross-links/66-08-SUMMARY.md'
      provides: 'Live verification matrix: per-requirement evidence, blocked-table blast-radius table, emission probe output, AR/width evidence refs, gate outputs'
  key_links:
    - from: 'live browser session (CDP Network.setBlockedURLs)'
      to: 'overview card error lines'
      via: 'blocked PostgREST/edge URL → query rejection → role="alert" render'
      pattern: 'rest/v1/dossier_relationships'
    - from: 'Supabase MCP/CLI deploy'
      to: 'supabase/functions/unified-timeline (66-03 source)'
      via: 'edge function redeploy + GET emission probe'
      pattern: 'unified-timeline'
---

<objective>
Run the phase gate live on staging (ORCHESTRATOR-RUN — requires Supabase MCP + browser CDP, which executor agents lack): deploy the 66-03 edge fix and probe its emissions, execute the forced-error protocol proving empty ≠ failed on real cards/drawer in both locales, and close with the full gate suite.

Purpose: roadmap success criteria 2 and 3 are live criteria — unit suites prove the contract shape, but only network-level blocking proves the error path end-to-end (TanStack cache behavior, retry timing, visual RTL), and only a deploy makes the edge fix real (Pitfall 10).
Output: recorded verification matrix in the SUMMARY; staging untouched (no rows created).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/66-overview-error-contract-timeline-cross-links/66-VALIDATION.md
@.planning/phases/66-overview-error-contract-timeline-cross-links/66-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy unified-timeline + emission probe (OVRERR-02 live half)</name>
  <files>none (Supabase MCP/CLI + HTTP probe)</files>
  <read_first>
    - .planning/phases/66-overview-error-contract-timeline-cross-links/66-03-SUMMARY.md (the committed emission fix; if 66-03 did not complete, STOP)
  </read_first>
  <action>
    1. Deploy supabase/functions/unified-timeline to staging (zkrcjzdemdmwhearhfgg) via Supabase MCP or CLI (project memory: edge fns deploy via CLI/MCP; verify the new version number).
    2. Emission probe (RESEARCH Q2): with a test-user JWT, request the unified-timeline endpoint for a dossier that has calendar rows (the Indonesia engagement dossier or China — calendar_entries = 6 on staging). Assert the response contains ZERO navigation_url values matching /calendar/<uuid>, /mous/<uuid>, or containing ?tab= ; calendar rows carry navigation_url null; interaction/intelligence rows carry bare /dossiers/... paths. Record the probe output (URLs only, no tokens) verbatim for the SUMMARY.
    3. /activity spot-check in the browser: rows without nav URLs remain non-interactive (existing behavior; activity_stream nav-url rows = 0, pre-answered).
  </action>
  <verify>
    <automated>MISSING — live deploy + staging HTTP probe, manual-only justified per 66-VALIDATION (Deno edges have no Vitest harness); the gate is the recorded emission-probe output showing zero dead URLs.</automated>
  </verify>
  <done>Deployed edge matches the repo source; live emissions are mounted-or-null (roadmap success criterion 3 server half observed).</done>
</task>

<task type="auto">
  <name>Task 2: Live forced-error protocol — cards, drawer, recovery, AR/RTL (OVRERR-01 live)</name>
  <files>none (browser CDP + dev server)</files>
  <read_first>
    - .planning/phases/66-overview-error-contract-timeline-cross-links/66-VALIDATION.md (§Live forced-error protocol — the binding step list)
    - .env.test (TEST_USER_EMAIL / TEST_USER_PASSWORD — NEVER echo values)
  </read_first>
  <action>
    1. Dev server on :5173 → staging (PORT=5001 + VITE_BACKEND_PROXY_TARGET if AirPlay holds :5000); login per .env.test.
    2. Pre-record the expected blast radius from each card's includeSections (RESEARCH §Card Inventory) for the country tab before blocking — shared-variant cards erroring together is CORRECT behavior, not collateral damage (Pitfall 4).
    3. Block `*rest/v1/dossier_relationships*` via CDP Network.enable + Network.setBlockedURLs. Reload China's country overview AND Saudi's (genuinely empty). Expected: related_dossiers-dependent cards show the danger alert line (allow ~7s for the 3x retry to exhaust — Pitfall 3, not a defect); non-dependent cards render normally; Saudi's healthy-but-empty sections still show muted empty copy. Screenshot both — empty vs failed visually distinct in one session.
    4. Unblock (setBlockedURLs []) → refetch/reload → all cards recover. Repeat the block/observe/unblock cycle once with `*rest/v1/calendar_entries*` and once with the dossier-activity-timeline function URL (covers the edge-fn path + SharedRecentActivityCard).
    5. Drawer: with a block active, open a dossier drawer (?dossier= deep link) → error branch (heading/body/Retry), data-loading false, close button works; unblock → Retry recovers in place.
    6. AR pass: topbar ع → Arabic error copy, dir=rtl, Tajawal, no overflow; evidence at 1280 and 1024 in both locales (4 screenshots minimum). Switch back to EN.
    7. Record the observed-vs-expected blast-radius matrix per blocked URL for the SUMMARY.
  </action>
  <verify>
    <automated>MISSING — live network-layer forced errors + visual RTL, manual-only justified per 66-VALIDATION §Manual-Only Verifications; the gate is the blast-radius matrix + before/after/recovery screenshots referenced in the SUMMARY.</automated>
  </verify>
  <acceptance_criteria>
    - Forced failures render role="alert" danger lines on exactly the dependent cards; zero trustworthy-looking zeros
    - Saudi empty states unchanged; recovery works unblocked; drawer error branch + Retry observed
    - AR/RTL evidence at both widths
  </acceptance_criteria>
  <done>Roadmap success criterion 2 observed live: empty and failed are unmistakably different states.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gates + evidence roll-up</name>
  <files>none (gates + SUMMARY)</files>
  <read_first>
    - frontend/.size-limit.json (current budgets; project memory: grep ALL exceeded lines — failures stack hidden; d3-geospatial sits near its cap)
  </read_first>
  <action>
    1. Gates: cd frontend && pnpm exec vitest run (full suite — includes all six Wave-0/extended test files from 66-01..66-07), pnpm type-check, and pnpm exec size-limit 2>&1 | tee /tmp/size-limit-66.log with a case-insensitive grep for 'exceeded' — zero matches required (whole log). If a budget trips, record exact numbers and STOP for a budget discussion (never silently bump .size-limit.json).
    2. Confirm no staging cleanup is owed: this plan created zero rows; assert CDP blocks were cleared (final setBlockedURLs [] recorded in Task 2).
    3. Compile the SUMMARY: requirement → evidence matrix (OVRERR-01: contract decision text + service/card/drawer evidence + live blast-radius matrix + AR refs; OVRERR-02: A-7/A-8 dispositions + emission probe output + suppression/guard evidence), gate outputs, and the phase deferral roll-up (relationship-metadata enrichment DEFERRED to backlog per RESEARCH Open Question Answer 4; optional matchRoute refactor noted from A1). No secrets anywhere. REQUIREMENTS.md checkbox flips happen at /gsd:verify-work.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run && pnpm type-check && pnpm exec size-limit 2>&1 | tee /tmp/size-limit-66.log && ! grep -qi 'exceeded' /tmp/size-limit-66.log</automated>
  </verify>
  <acceptance_criteria>
    - Full suite exit 0; type-check exit 0; zero size-limit exceeded lines
    - SUMMARY carries the complete evidence matrix + deferral roll-up with zero secret values
  </acceptance_criteria>
  <done>Phase gate-green, both requirements live-verified, staging clean — ready for /gsd:verify-work.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                     |
| ---------------------------------- | ----------------------------------------------- |
| test credentials → browser session | .env.test secrets used for live login           |
| MCP/CLI deploy → staging edge      | elevated deploy privileges change live behavior |

## STRIDE Threat Register

| Threat ID | Category               | Component                            | Disposition | Mitigation Plan                                                                                                  |
| --------- | ---------------------- | ------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------- |
| T-66-16   | Information Disclosure | credentials/JWTs in evidence         | mitigate    | Secrets sourced as env vars; acceptance criteria forbid secret values in SUMMARY, logs, or probe transcripts     |
| T-66-17   | Denial of Service      | CDP blocks left active / stale state | mitigate    | Every block paired with an explicit setBlockedURLs [] step; blocks are session-local to the verification browser |
| T-66-18   | Tampering              | error simulation via RLS/grants      | mitigate    | PROHIBITED by plan decision — client-side network blocking only (Pitfall 1); zero staging mutations to clean up  |
| T-66-SC   | Tampering              | npm/pip/cargo installs               | accept      | No package installs in this plan                                                                                 |

</threat_model>

<verification>
- 66-VALIDATION manual-only rows realized: forced section error live (Task 2), empty-vs-failed distinction (Task 2 step 3), edge emissions clean post-redeploy (Task 1), AR/RTL copy + widths (Task 2 step 6)
- Phase gate realized: full suite + type-check + size-limit before /gsd:verify-work
</verification>

<success_criteria>

- All three roadmap success criteria observed live with probe output + screenshot evidence
- Gates green; staging untouched; deferrals rolled up
  </success_criteria>

<output>
Create `.planning/phases/66-overview-error-contract-timeline-cross-links/66-08-SUMMARY.md` when done (evidence matrix, no secrets).
</output>
