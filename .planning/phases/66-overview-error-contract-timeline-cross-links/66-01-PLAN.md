---
phase: 66-overview-error-contract-timeline-cross-links
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/services/dossier-overview.service.ts
  - frontend/src/services/__tests__/dossier-overview.service.test.ts
  - frontend/src/i18n/en/dossier.json
  - frontend/src/i18n/ar/dossier.json
autonomous: true
requirements: [OVRERR-01]

key_decisions:
  - 'OVRERR-01 contract DECIDED (roadmap success criterion 1): Contract A — fail-the-query. Every section fetcher in dossier-overview.service.ts throws DossierOverviewAPIError on a PostgREST/edge error; fetchDossierOverview keeps Promise.all so the first rejection rejects the query for that section variant. Contracts B (section-level error metadata) and C (explicit unknown state) rejected per 66-RESEARCH §Error Contract Decision: per-card query variants already give per-card granularity'
  - '66-01 is the SINGLE OWNER of frontend/src/i18n/{en,ar}/dossier.json this phase — the one new key (overview.sectionError) lands here so both wave-2 card plans build on a tree where it exists (Pitfall 7: same-commit EN+AR)'
  - 'Error status stays 500 (default; transient errors keep the global 3x retry). The optional schema-error→400 fast-fail mapping from Pitfall 3 is NOT implemented — documented in the service header comment instead (keep simple)'
  - 'The two deliberate permanent-empty groups in fetchDocuments (briefs L517-523, attachments L525-527 — verified-schema honest empties) are NOT converted to throws'

must_haves:
  truths:
    - 'fetchRelatedDossiers, fetchDocuments, fetchWorkItems (all 6 inner queries), fetchCalendarEvents, and fetchKeyContacts each reject with DossierOverviewAPIError when their Supabase query returns an error — no console.error-and-continue, no undestructured error'
    - 'fetchActivityTimeline no longer catches: a rejection from fetchUnifiedDossierActivities propagates out of fetchDossierOverview'
    - 'fetchDossierOverview rejects when any requested section fetcher rejects (Promise.all preserved)'
    - 'overview.sectionError exists in BOTH en/dossier.json and ar/dossier.json inside the existing top-level overview object, with the exact UI-SPEC strings — parity unit-asserted'
    - 'The fail-the-query decision is recorded in a service-file header comment tagged OVRERR-01'
  artifacts:
    - path: 'frontend/src/services/__tests__/dossier-overview.service.test.ts'
      provides: 'Per-fetcher forced-error rejection suite (66-VALIDATION row 1) + EN↔AR sectionError parity assertion (row 4)'
      min_lines: 80
    - path: 'frontend/src/services/dossier-overview.service.ts'
      provides: 'All 7 fetchers honest (fail-the-query)'
      contains: 'OVRERR-01'
    - path: 'frontend/src/i18n/ar/dossier.json'
      provides: 'AR section error copy'
      contains: 'تعذر تحميل هذا القسم'
  key_links:
    - from: 'frontend/src/services/dossier-overview.service.ts'
      to: 'frontend/src/lib/query-client.ts'
      via: 'DossierOverviewAPIError.status feeds the global retry predicate (3x backoff, 4xx fail-fast)'
      pattern: 'DossierOverviewAPIError'
    - from: 'frontend/src/i18n/en/dossier.json'
      to: 'wave-2 card error branches (66-04/66-05)'
      via: "t('overview.sectionError') in the dossier namespace"
      pattern: 'sectionError'
---

<objective>
Implement the OVRERR-01 service layer: make all 7 section fetchers in dossier-overview.service.ts fail-the-query (throw DossierOverviewAPIError instead of swallowing PostgREST errors), record the contract decision, and land the phase's single new i18n key (overview.sectionError) in both languages.

Purpose: roadmap success criterion 1 — the contract is decided AND applied at the layer where errors are currently destroyed. Without this, even error-aware consumers (DossierAnalyticsCard, DossierOverview.tsx) never see section failures.
Output: honest service layer + RED→GREEN service test suite + bilingual error copy for the wave-2 card sweep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/66-overview-error-contract-timeline-cross-links/66-RESEARCH.md
@.planning/phases/66-overview-error-contract-timeline-cross-links/66-VALIDATION.md

<interfaces>
<!-- Existing error class (service L252-264) — reuse, do not redefine. -->

```typescript
export class DossierOverviewAPIError extends Error {
  code: string
  status: number
  details?: string
  constructor(message: string, status: number, code: string, details?: string)
}
```

<!-- Throw shape per fetcher (mirrors fetchDossierCore L298-305; RESEARCH §Code Example 1). -->

```typescript
// fetchRelatedDossiers — replaces the console.error at L362-364:
if (outError || inError) {
  const cause = outError ?? inError
  throw new DossierOverviewAPIError(
    'Failed to fetch related dossiers',
    500,
    'RELATED_DOSSIERS_FETCH_FAILED',
    cause?.message,
  )
}
```

<!-- Swallow-site inventory (line-verified at dda49065; 66-PATTERNS §Layer 1):
     fetchRelatedDossiers  L362-364  console.error → continue        → throw (code RELATED_DOSSIERS_FETCH_FAILED)
     fetchDocuments        L475-477  console.error (position links)  → throw (DOCUMENTS_FETCH_FAILED)
     fetchDocuments        L503-505  console.error (mous ×2)         → throw (DOCUMENTS_FETCH_FAILED)
     fetchWorkItems        L607,633,643,686,695,736 — error NEVER destructured on 6 queries
                                                                      → destructure + throw each (WORK_ITEMS_FETCH_FAILED)
     fetchCalendarEvents   L807      error not destructured           → destructure + throw (CALENDAR_EVENTS_FETCH_FAILED)
     fetchKeyContacts      L860      error not destructured           → destructure + throw (KEY_CONTACTS_FETCH_FAILED)
     fetchActivityTimeline L908-916  try/catch → empty section        → DELETE the catch (UnifiedActivityAPIError propagates)
     KEEP UNCHANGED: fetchDossierCore (already throws); briefs/attachments permanent-empty groups L517-527. -->

<!-- i18n — the phase's ONE new key (UI-SPEC §Copywriting Contract, binding; inside the EXISTING top-level "overview" object in both files): -->

```jsonc
// frontend/src/i18n/en/dossier.json
"sectionError": "Failed to load this section. Check your connection and try again."
// frontend/src/i18n/ar/dossier.json
"sectionError": "تعذر تحميل هذا القسم. تحقق من اتصالك وحاول مرة أخرى."
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the forced-error service test suite — RED first</name>
  <files>frontend/src/services/__tests__/dossier-overview.service.test.ts</files>
  <read_first>
    - frontend/src/services/dossier-overview.service.ts (FULL read once: every fetcher's chain shape — .eq().single() for core, awaited .eq() for relationships, .in()/.gte() variants for work items/calendar — extract all chain shapes in one pass)
    - frontend/src/pages/dossiers/overview-cards/__tests__/SharedRecentActivityCard.test.tsx (vi.hoisted mutable-mock-state pattern + i18n echo mock)
    - frontend/src/services/unified-dossier-activity.service.ts (fetchUnifiedDossierActivities export + UnifiedActivityAPIError, for the activity-timeline mock)
  </read_first>
  <behavior>
    - Test per fetcher (RED against current swallow behavior): mock '@/lib/supabase' with a vi.hoisted chainable thenable builder keyed by table name — from(table) returns a self-returning proxy whose awaited terminal resolves the configured { data, error }. Configure 'dossiers' to succeed (minimal core row) and the target table to resolve { data: null, error: { message: 'forced', code: '42703' } }. Call fetchDossierOverview({ dossier_id: 'd1', include_sections: [<only the target section>] }) and assert it REJECTS with DossierOverviewAPIError. Target tables: dossier_relationships (related_dossiers), position_dossier_links AND mous (documents), work_item_dossiers AND tasks (work_items), calendar_entries (calendar_events), key_contacts (key_contacts).
    - Test: vi.mock '@/services/unified-dossier-activity.service' so fetchUnifiedDossierActivities rejects; include_sections ['activity_timeline'] → fetchDossierOverview rejects (RED: today the catch swallows it).
    - Regression pin (GREEN from the start): blocked 'dossiers' table → rejects with code DOSSIER_FETCH_FAILED (fetchDossierCore already honest).
    - Parity test (66-VALIDATION row 4): direct-import frontend/src/i18n/en/dossier.json and ar/dossier.json; assert both contain overview.sectionError with non-empty values and that the AR value starts with 'تعذر' (RED until Task 2 adds the keys).
  </behavior>
  <action>
    Create the test file (AAA structure, decision-tagged test names like 'fetchRelatedDossiers rejects on PostgREST error (OVRERR-01)'). Build the chainable supabase mock once with a per-table result map mutated per test via vi.hoisted state; supabase.auth.getSession is NOT exercised by fetchDossierOverview — stub it anyway for safety. Do not spin up a QueryClient — these are pure service tests calling fetchDossierOverview directly (no retry delays, Pitfall 3 avoided). Run the file: all forced-error tests and the parity test MUST fail; the fetchDossierCore pin MUST pass. Commit: test(66-01): add failing forced-error suite for overview section fetchers.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/services/__tests__/dossier-overview.service.test.ts 2>&1 | grep -qE 'failed' && echo RED_CONFIRMED</automated>
  </verify>
  <done>Test file exists realizing 66-VALIDATION rows 1 and 4; swallow-path tests RED, core-fetcher pin GREEN; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Fail-the-query in all fetchers + decision comment + bilingual i18n key — GREEN</name>
  <files>frontend/src/services/dossier-overview.service.ts, frontend/src/i18n/en/dossier.json, frontend/src/i18n/ar/dossier.json</files>
  <read_first>
    - frontend/src/services/dossier-overview.service.ts (already read in Task 1 — do NOT re-read; work from the swallow-site inventory in the interfaces block)
  </read_first>
  <action>
    1. Apply the throw shape from the interfaces block at every swallow site in the inventory: fetchRelatedDossiers (replace the console.error), fetchDocuments (both sites — throw on positionLinksError and on mous1Error/mous2Error), fetchWorkItems (destructure error on all 6 queries — links, tasks, users, both aa_commitments queries, intake_tickets — and throw on each), fetchCalendarEvents, fetchKeyContacts (destructure + throw). Delete fetchActivityTimeline's try/catch so the mapping returns directly and UnifiedActivityAPIError propagates. Use the per-fetcher error codes from the inventory; status 500; pass the PostgREST error.message as details (details are NEVER rendered in UI — consoles only, per the phase threat model). Strict-boolean rule: test errors with explicit checks (if (error) is fine for PostgREST error objects per existing fetchDossierCore precedent).
    2. Add a header comment above DossierOverviewAPIError tagged OVRERR-01 recording the decision: 'OVRERR-01 contract: fail-the-query. Every section fetcher throws DossierOverviewAPIError on a PostgREST/edge error; Promise.all rejects the section-variant query; cards render role="alert" error lines from isError. Status 500 by default — transient errors keep the global 3x retry (lib/query-client.ts).'
    3. Add overview.sectionError to BOTH en/dossier.json and ar/dossier.json (exact strings from the interfaces block) in this same commit. Touch nothing else in those files.
    4. Run the Task 1 file (all GREEN) and pnpm type-check. Commit: feat(66-01): fail-the-query across overview section fetchers (OVRERR-01).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/services/__tests__/dossier-overview.service.test.ts && pnpm type-check && grep -q 'OVRERR-01' src/services/dossier-overview.service.ts && python3 -c "import json; en=json.load(open('src/i18n/en/dossier.json')); ar=json.load(open('src/i18n/ar/dossier.json')); assert en['overview']['sectionError'].startswith('Failed to load this section'); assert ar['overview']['sectionError'].startswith('تعذر تحميل هذا القسم'); print('I18N_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Zero console.error-and-continue or undestructured-error sites remain in the 7 fetchers (briefs/attachments honest empties preserved)
    - Decision header comment present; test suite fully green; type-check exit 0
    - sectionError in both locales, same commit
  </acceptance_criteria>
  <done>Service layer is honest: any section failure rejects the query; copy ready for the card sweep.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                           |
| ------------------------------ | ------------------------------------------------------------------------------------- |
| browser → PostgREST/edge reads | section errors arrive at the client; the service now surfaces instead of masking them |

## STRIDE Threat Register

| Threat ID | Category                 | Component                       | Disposition | Mitigation Plan                                                                                                                                                            |
| --------- | ------------------------ | ------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-66-01   | Repudiation (of failure) | section fetchers                | mitigate    | The plan's core fix — fail-the-query so failures can never impersonate trustworthy empty/zero states                                                                       |
| T-66-02   | Information Disclosure   | DossierOverviewAPIError.details | mitigate    | details carries PostgREST messages for console/debug only; no UI in this plan renders it, and wave-2 cards render only the generic localized key (enforced in 66-04/05/06) |
| T-66-SC   | Tampering                | npm/pip/cargo installs          | accept      | No package installs in this plan                                                                                                                                           |

</threat_model>

<verification>
- 66-VALIDATION rows realized: 'Every section fetcher rejects' (Task 1-2) + 'sectionError key parity EN↔AR' (Task 1 parity assertion, GREEN at Task 2)
- Per-task gate: quick vitest run of the touched file + pnpm type-check
</verification>

<success_criteria>

- Roadmap success criterion 1 first half: contract decided (recorded in code + frontmatter) and applied at the service layer
- All forced-error service tests green; i18n parity green
  </success_criteria>

<output>
Create `.planning/phases/66-overview-error-contract-timeline-cross-links/66-01-SUMMARY.md` when done (record the contract decision text verbatim for the phase roll-up).
</output>
