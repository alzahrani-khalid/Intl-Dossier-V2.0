---
phase: 66-overview-error-contract-timeline-cross-links
plan: 04
type: execute
wave: 2
depends_on: [66-01]
files_modified:
  - frontend/src/pages/dossiers/overview-cards/SharedSummaryStatsCard.tsx
  - frontend/src/pages/dossiers/overview-cards/SharedRecentActivityCard.tsx
  - frontend/src/pages/dossiers/overview-cards/BilateralSummaryCard.tsx
  - frontend/src/pages/dossiers/overview-cards/KeyContactsCard.tsx
  - frontend/src/pages/dossiers/overview-cards/EngagementsByStageCard.tsx
  - frontend/src/pages/dossiers/overview-cards/KeyRepresentativesCard.tsx
  - frontend/src/pages/dossiers/overview-cards/MembershipStructureCard.tsx
  - frontend/src/pages/dossiers/overview-cards/MoUStatusCard.tsx
  - frontend/src/pages/dossiers/overview-cards/__tests__/OverviewCardErrorStates.test.tsx
  - frontend/src/pages/dossiers/overview-cards/__tests__/SharedRecentActivityCard.test.tsx
autonomous: true
requirements: [OVRERR-01]

key_decisions:
  - 'Error-line anatomy is the UI-SPEC §1 contract VERBATIM: <p role="alert" className="text-sm text-[var(--danger)] text-center py-8"> with t(overview.sectionError) — one shared key (A-2), no retry button on cards (A-3), no icon, card chrome/title untouched (A-4), text-destructive banned in new code'
  - 'State precedence uniform: skeleton → ERROR (before empty) → empty → data. Stale-while-error mandatory: error line ONLY when cached data is unavailable (data === null for useDossierOverview consumers; data === undefined for raw useQuery/domain hooks)'
  - 'Card split: 66-04 owns shared + country + organization cards (8 files); 66-05 owns the type-tab remainder (11). Test files partitioned: 66-04 owns OverviewCardErrorStates.test.tsx + the existing SharedRecentActivityCard.test.tsx; 66-05 owns TypeCardErrorStates.test.tsx — zero overlap with 66-05 in the shared __tests__ dir'

must_haves:
  truths:
    - 'Each of the 8 cards with a forced query error and no cached data renders role="alert" containing the sectionError copy — and its empty-state copy (No data available / No recent activity / *.empty) is ABSENT from the DOM'
    - 'With cached data present and isError true, each card renders its data and NO error line (stale-while-error)'
    - 'Genuinely empty data with no error still renders the unchanged empty copy and NO alert (empty ≠ error)'
    - 'SharedSummaryStatsCard allZero stats with a healthy query still render the empty line — but a FAILED query never renders zeros or the empty line (the trustworthy-zero collapse is broken)'
  artifacts:
    - path: 'frontend/src/pages/dossiers/overview-cards/__tests__/OverviewCardErrorStates.test.tsx'
      provides: 'Parameterized forced-error suite for the 8 cards (66-VALIDATION row 2, this plan half)'
      min_lines: 100
    - path: 'frontend/src/pages/dossiers/overview-cards/SharedSummaryStatsCard.tsx'
      provides: 'Error branch before the empty/allZero branch'
      contains: 'sectionError'
  key_links:
    - from: 'frontend/src/pages/dossiers/overview-cards/SharedSummaryStatsCard.tsx'
      to: 'frontend/src/i18n/en/dossier.json (66-01)'
      via: "t('overview.sectionError') in the dossier namespace"
      pattern: 'overview.sectionError'
    - from: 'frontend/src/pages/dossiers/overview-cards/MoUStatusCard.tsx'
      to: 'TanStack query isError (own useQuery, queryFn already throws)'
      via: 'isError destructure + error-before-empty branch'
      pattern: 'isError'
---

<objective>
Apply the card-layer half of the OVRERR-01 contract to the shared, country, and organization overview cards (8 files): destructure isError and render the UI-SPEC §1 section error line before the empty branch, with stale-while-error semantics.

Purpose: roadmap success criterion 2 — a forced section fetch error must render an explicit error state on the card, never trustworthy zeros/empties. The service (66-01) now rejects; these cards currently drop the flag.
Output: 8 honest cards + RED→GREEN parameterized test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/66-overview-error-contract-timeline-cross-links/66-UI-SPEC.md
@.planning/phases/66-overview-error-contract-timeline-cross-links/66-PATTERNS.md

<interfaces>
<!-- The error branch — UI-SPEC §1, BINDING. Inserted after the isLoading branch,
     BEFORE the empty check. Reuse the card's OWN existing container + h3 title verbatim;
     only the body slot changes. SharedSummaryStatsCard target shape: -->

```tsx
const { data, isLoading, isError } = useDossierOverview(dossierId, {
  includeSections: ['related_dossiers', 'work_items', 'calendar_events', 'activity_timeline'],
})
// ... existing isLoading branch unchanged ...
if (isError && data === null) {
  return (
    <div className="bg-card rounded-lg border p-4 sm:p-6" dir={isRTL ? 'rtl' : 'ltr'}>
      <h3 className="text-base font-semibold leading-tight text-start mb-4">
        {t('overview.summary', { defaultValue: 'Summary' })}
      </h3>
      <p role="alert" className="text-sm text-[var(--danger)] text-center py-8">
        {t('overview.sectionError', {
          defaultValue: 'Failed to load this section. Check your connection and try again.',
        })}
      </p>
    </div>
  )
}
```

<!-- Per-card hook family + data-unavailable comparison (RESEARCH §Card Inventory, verified):
     SharedSummaryStatsCard      useDossierOverview            data === null
     BilateralSummaryCard        useDossierOverview            data === null
     KeyContactsCard             useDossierOverview            data === null
     EngagementsByStageCard      useDossierOverview            data === null
     KeyRepresentativesCard      useDossierOverview            data === null
     MembershipStructureCard     useDossierOverview            data === null
     SharedRecentActivityCard    useDossierActivityTimeline    data unavailable per the hook's exposed shape (read the hook; queryFn already throws)
     MoUStatusCard               own useQuery (throws, L40-49) data === undefined
     useDossierOverview returns data: query.data || null (verified hook L77) — never undefined. -->

<!-- Test mock pattern (existing SharedRecentActivityCard.test.tsx precedent): vi.hoisted
     mutable state + barrel-path mock + i18n echo mock honouring defaultValue: -->

```typescript
const overviewState = vi.hoisted(() => ({
  data: null as unknown,
  isLoading: false,
  isError: false,
}))
vi.mock('@/hooks/useDossierOverview', () => ({
  useDossierOverview: () => ({
    data: overviewState.data,
    isLoading: overviewState.isLoading,
    isError: overviewState.isError,
    error: null,
    refetch: vi.fn(),
  }),
}))
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Forced-error card tests for the 8 cards — RED first</name>
  <files>frontend/src/pages/dossiers/overview-cards/__tests__/OverviewCardErrorStates.test.tsx, frontend/src/pages/dossiers/overview-cards/__tests__/SharedRecentActivityCard.test.tsx</files>
  <read_first>
    - frontend/src/pages/dossiers/overview-cards/__tests__/SharedRecentActivityCard.test.tsx (the binding mock pattern: vi.hoisted state, BARREL mock of @/hooks/useDossierActivityTimeline, i18n echo honouring defaultValue)
    - frontend/src/pages/dossiers/overview-cards/MoUStatusCard.tsx (inline useQuery shape + supabase usage, to design its forced-rejection mock)
    - ONE useDossierOverview card you have not seen (e.g. BilateralSummaryCard.tsx) to confirm the destructure-line/empty-copy pattern generalizes — do not read all 8
  </read_first>
  <behavior>
    - Parameterized (it.each over the 6 useDossierOverview cards): mock state { isError: true, data: null, isLoading: false } → getByRole('alert') matches /failed to load this section/i AND the card's empty copy (No data available / *.empty variants) is queryBy-ABSENT. RED today (cards ignore isError).
    - Stale-while-error (representative: SharedSummaryStatsCard): isError true + data = a minimal DossierOverviewResponse with nonzero stats → stats render, NO role="alert".
    - Empty ≠ error regression pin (SharedSummaryStatsCard): isError false + data with all-zero stats → 'No data available' present, no alert.
    - SharedRecentActivityCard (extend its existing test file): isError true + data unavailable → alert present, 'No recent activity' absent.
    - MoUStatusCard: mock '@/lib/supabase' so its queryFn rejects; render inside a fresh QueryClientProvider with retry: false (Pitfall 3) → await the alert.
  </behavior>
  <action>
    Create OverviewCardErrorStates.test.tsx (decision-tagged names like 'BilateralSummaryCard renders error state, not empty state, on section failure (OVRERR-01)') and extend SharedRecentActivityCard.test.tsx with the isError flip. Reuse the existing i18n echo mock so assertions run against the defaultValue strings. Run both files: new error-state cases MUST be RED; existing SharedRecentActivityCard cases stay GREEN. Commit: test(66-04): add failing forced-error tests for shared/country/org overview cards.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/pages/dossiers/overview-cards/__tests__/OverviewCardErrorStates.test.tsx 2>&1 | grep -qE 'failed' && echo RED_CONFIRMED</automated>
  </verify>
  <done>Forced-error suite exists for all 8 cards (66-VALIDATION row 2, this half); RED confirmed; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Error branch in all 8 cards — GREEN</name>
  <files>frontend/src/pages/dossiers/overview-cards/SharedSummaryStatsCard.tsx, frontend/src/pages/dossiers/overview-cards/SharedRecentActivityCard.tsx, frontend/src/pages/dossiers/overview-cards/BilateralSummaryCard.tsx, frontend/src/pages/dossiers/overview-cards/KeyContactsCard.tsx, frontend/src/pages/dossiers/overview-cards/EngagementsByStageCard.tsx, frontend/src/pages/dossiers/overview-cards/KeyRepresentativesCard.tsx, frontend/src/pages/dossiers/overview-cards/MembershipStructureCard.tsx, frontend/src/pages/dossiers/overview-cards/MoUStatusCard.tsx</files>
  <read_first>
    - Each card file ONCE as you edit it (extract destructure line, container, title, empty branch in one pass — no re-reads)
  </read_first>
  <action>
    For each of the 8 cards: add isError to the hook destructure; insert the error branch from the interfaces block after the isLoading branch and BEFORE the empty/allZero check, reusing the card's own container div, dir handling, and h3 title exactly (chrome untouched, A-4). Use the per-card data comparison from the interfaces table (data === null for useDossierOverview consumers; data === undefined for MoUStatusCard; read SharedRecentActivityCard's hook return shape once and match it). The error line is the only new element: role="alert", text-sm text-[var(--danger)] text-center py-8, t('overview.sectionError', { defaultValue: ... }) — never text-destructive, no icon, no retry, no new dir attribute (inherits the card container). Empty copy and all other branches byte-identical. Run Task 1 files (GREEN) + pnpm type-check. Commit: feat(66-04): per-card section error lines for shared/country/org overview cards (OVRERR-01).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/pages/dossiers/overview-cards/__tests__/OverviewCardErrorStates.test.tsx src/pages/dossiers/overview-cards/__tests__/SharedRecentActivityCard.test.tsx && pnpm type-check && [ "$(grep -l 'overview.sectionError' src/pages/dossiers/overview-cards/SharedSummaryStatsCard.tsx src/pages/dossiers/overview-cards/SharedRecentActivityCard.tsx src/pages/dossiers/overview-cards/BilateralSummaryCard.tsx src/pages/dossiers/overview-cards/KeyContactsCard.tsx src/pages/dossiers/overview-cards/EngagementsByStageCard.tsx src/pages/dossiers/overview-cards/KeyRepresentativesCard.tsx src/pages/dossiers/overview-cards/MembershipStructureCard.tsx src/pages/dossiers/overview-cards/MoUStatusCard.tsx | wc -l | tr -d ' ')" = "8" ] && ! grep -l 'text-destructive' src/pages/dossiers/overview-cards/SharedSummaryStatsCard.tsx src/pages/dossiers/overview-cards/BilateralSummaryCard.tsx | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - All 8 cards carry the error branch with the exact UI-SPEC anatomy; empty branches unchanged
    - Suite green; type-check exit 0; no text-destructive introduced
  </acceptance_criteria>
  <done>Shared/country/org cards distinguish failed from empty with stale-while-error semantics.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                   |
| --------------------- | ------------------------------------------------------------- |
| query error → card UI | error objects cross into render; only generic copy may render |

## STRIDE Threat Register

| Threat ID | Category                 | Component              | Disposition | Mitigation Plan                                                                                                                      |
| --------- | ------------------------ | ---------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| T-66-07   | Information Disclosure   | card error lines       | mitigate    | Cards render ONLY t('overview.sectionError') — never error.message/details (Pitfall 9); pinned by tests asserting the localized copy |
| T-66-08   | Repudiation (of failure) | 8 card empty branches  | mitigate    | Error checked BEFORE empty; tests assert empty copy ABSENT under forced error                                                        |
| T-66-SC   | Tampering                | npm/pip/cargo installs | accept      | No package installs in this plan                                                                                                     |

</threat_model>

<verification>
- 66-VALIDATION row 2 realized for the shared/country/org half (RED-first, vi.hoisted mocks)
- Per-task gate: quick vitest run of the two test files + pnpm type-check
</verification>

<success_criteria>

- Forced isError on any of the 8 cards renders the danger alert line, never the empty state
- Cached data survives background-refetch failures unblanked
  </success_criteria>

<output>
Create `.planning/phases/66-overview-error-contract-timeline-cross-links/66-04-SUMMARY.md` when done.
</output>
