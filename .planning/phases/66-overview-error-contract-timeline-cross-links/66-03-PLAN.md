---
phase: 66-overview-error-contract-timeline-cross-links
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/unified-timeline/index.ts
autonomous: true
requirements: [OVRERR-02]

key_decisions:
  - 'A-7 (orchestrator product call, BINDING — RESEARCH §Open Question Answers #1): calendar timeline rows are SUPPRESSED — navigation_url: null. /calendar has no event-focus search param (verified routes/_protected/calendar.tsx), so a bare list link is a dishonest affordance'
  - 'MoU rows emit /mous (mounted list); interaction/intelligence rows emit the bare getDossierDetailPath(...) — the ?tab= params were dead anyway ($id index beforeLoad redirect drops search; no interactions/intelligence tab exists on any type)'
  - 'This plan is CODE-ONLY. The staging redeploy of unified-timeline is orchestrator/Supabase-MCP territory and happens in 66-08 (64-01 split precedent) — repo edits change nothing on staging until then (Pitfall 10)'

must_haves:
  truths:
    - 'unified-timeline/index.ts emits NO /calendar/$id, /mous/$id, or ?tab= navigation_url values'
    - 'Calendar rows emit navigation_url: null with an A-7 SUPPRESS decision comment at the emission site'
    - 'Interaction and intelligence rows emit the bare mounted dossier path via the existing getDossierDetailPath helper'
    - 'Repo-wide, unified-timeline remains the ONLY navigation_url builder (same-bug-class scan recorded)'
  artifacts:
    - path: 'supabase/functions/unified-timeline/index.ts'
      provides: 'Mounted-or-null navigation_url emissions for all 4 row types'
      contains: 'A-7'
  key_links:
    - from: 'supabase/functions/unified-timeline/index.ts'
      to: 'supabase/functions/_shared/dossier-routes.ts'
      via: 'getDossierDetailPath for interaction/intelligence rows (already imported)'
      pattern: 'getDossierDetailPath'
    - from: 'supabase/functions/unified-timeline/index.ts (mou rows)'
      to: '/mous (mounted list route)'
      via: 'static literal emission'
      pattern: "navigation_url: '/mous'"
---

<objective>
Fix the four dishonest navigation_url emissions at their single source — supabase/functions/unified-timeline/index.ts — so the server only ever emits mounted paths or null (suppress).

Purpose: OVRERR-02 at the source of truth. The frontend guard (66-02/66-07) makes the requirement hold even with deploy drift, but honest emissions make the data correct for Phase 67's routed future. Calendar dead links are live-reproducible on staging today (6 calendar_entries rows).
Output: corrected edge function source, committed; deploy + live probe happen in 66-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/66-overview-error-contract-timeline-cross-links/66-RESEARCH.md

<interfaces>
<!-- The four emission sites (line-verified at dda49065) and their replacements
     (RESEARCH §Code Example 5 + Open Question Answer #1): -->

```typescript
// L178  calendar rows — A-7 SUPPRESS (no mounted detail route; /calendar has no focus param)
navigation_url: null,
// L239  interaction rows — drop the dead ?tab=interactions suffix
navigation_url: getDossierDetailPath(dossier_id, dossier_type),
// L306  intelligence rows — drop the dead ?tab=intelligence suffix
navigation_url: getDossierDetailPath(dossier_id, dossier_type),
// L360  mou rows — /mous/$id is unmounted; the list is
navigation_url: '/mous',
```

<!-- Facts: getDossierDetailPath is ALREADY imported (used at L239/306). The metadata
     objects are inline-typed — if a navigation_url type annotation exists, widen it to
     string | null; frontend consumers null-check via the 66-02 guard either way. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the four emissions + record the A-7 decision at the calendar site</name>
  <files>supabase/functions/unified-timeline/index.ts</files>
  <read_first>
    - supabase/functions/unified-timeline/index.ts (the four metadata blocks around L169-182, L235-242, L301-309, L354-364 — Grep 'navigation_url' first, then read each block; do not read the whole file)
  </read_first>
  <action>
    Apply the four replacements from the interfaces block. Above the calendar emission add a one-line comment: 'OVRERR-02 A-7: calendar rows SUPPRESS — /calendar/$id is unmounted and /calendar has no event-focus param; null removes the View-details affordance client-side.' Above the mou emission: '/mous/$id is unmounted; route to the list.' If TypeScript types the metadata field, widen navigation_url to string | null. Make no other changes to the function (surgical — Deno edge fns have no Vitest harness; correctness is proven by the grep gates below and the 66-08 live probe). Commit: fix(66-03): emit mounted-or-null navigation_url from unified-timeline (OVRERR-02).
  </action>
  <verify>
    <automated>! grep -nE 'navigation_url: `/(calendar|mous)/\$' supabase/functions/unified-timeline/index.ts && ! grep -n '?tab=' supabase/functions/unified-timeline/index.ts && grep -c 'navigation_url' supabase/functions/unified-timeline/index.ts | grep -q '^4$' && grep -q 'A-7' supabase/functions/unified-timeline/index.ts</automated>
  </verify>
  <done>All four emission sites produce mounted paths or null; A-7 decision recorded at the site.</done>
</task>

<task type="auto">
  <name>Task 2: Same-bug-class scan — assert unified-timeline stays the only navigation_url builder</name>
  <files>none (read-only scan, results recorded in SUMMARY)</files>
  <action>
    Project-memory lesson (engagement_type discriminator drift): grep the whole repo before declaring one call site fixed. Run: grep -rn 'navigation_url' supabase/functions backend/src frontend/src --include='*.ts' --include='*.tsx' | grep -v __tests__ and classify every hit as BUILDER (writes a URL) or CONSUMER (reads one). Expected: builders only in supabase/functions/unified-timeline/index.ts (fixed in Task 1); consumers in TimelineEventCard/EnhancedVerticalTimelineCard/ActivityList (66-07's surface) and type definitions. Also grep for sibling dead-path emitters: grep -rnE '"/(calendar|mous|documents)/" *\+|`/(calendar|mous|documents)/\$' frontend/src backend/src supabase/functions --include='*.ts' --include='*.tsx' | grep -v __tests__ — expected hits only in files owned by 66-02 (already fixed in its wave-1 worktree) or none. If an UNOWNED builder/emitter is found, STOP and surface it to the orchestrator (do not widen this plan's file ownership). Record the classified hit list in the SUMMARY.
  </action>
  <verify>
    <automated>! grep -rn 'navigation_url:' supabase/functions backend/src --include='*.ts' | grep -v 'unified-timeline' | grep -v __tests__ | grep -q .</automated>
  </verify>
  <done>Single-builder fact re-confirmed and recorded; no unowned dead-path emitters remain.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                    |
| ----------------------------- | -------------------------------------------------------------- |
| edge function → client router | server-built URLs are navigated client-side (guarded in 66-07) |

## STRIDE Threat Register

| Threat ID | Category  | Component                 | Disposition | Mitigation Plan                                                                                                                                                                        |
| --------- | --------- | ------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-66-05   | Tampering | navigation_url emissions  | mitigate    | Emissions reduced to static literals ('/mous', null) and the shared getDossierDetailPath helper — no user-controlled URL composition; frontend guard (66-02/66-07) is defense-in-depth |
| T-66-06   | Tampering | repo/staging deploy drift | mitigate    | Deploy explicitly deferred to 66-08 (orchestrator MCP) with a before/after emission probe — the drift window is closed at the phase gate                                               |
| T-66-SC   | Tampering | npm/pip/cargo installs    | accept      | No package installs in this plan                                                                                                                                                       |

</threat_model>

<verification>
- 66-VALIDATION row 'Edge emissions clean' is code-side satisfied here (grep gates); the live half (manual-only, post-redeploy probe) is realized in 66-08
</verification>

<success_criteria>

- Roadmap success criterion 3 at the source: no emitted timeline URL can dead-end (mounted path or null)
- Single-builder invariant re-confirmed repo-wide
  </success_criteria>

<output>
Create `.planning/phases/66-overview-error-contract-timeline-cross-links/66-03-SUMMARY.md` when done (include the deploy-pending note for 66-08 and the scan hit list).
</output>
