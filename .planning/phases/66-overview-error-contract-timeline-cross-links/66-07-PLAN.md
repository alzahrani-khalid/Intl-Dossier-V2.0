---
phase: 66-overview-error-contract-timeline-cross-links
plan: 07
type: execute
wave: 2
depends_on: [66-02]
files_modified:
  - frontend/src/components/timeline/TimelineEventCard.tsx
  - frontend/src/components/timeline/EnhancedVerticalTimelineCard.tsx
  - frontend/src/components/timeline/__tests__/TimelineEventCard.test.tsx
  - frontend/src/components/activity-feed/ActivityList.tsx
  - frontend/src/components/activity-feed/__tests__/ActivityList.test.tsx
autonomous: true
requirements: [OVRERR-02]

key_decisions:
  - 'Suppression-as-absence (UI-SPEC §3, binding): when resolveTimelineNavUrl returns null, the View-details button AND its handler are NOT in the DOM — no disabled button, no dead chevron, no cursor-pointer; row content unchanged; sibling flex reflows via existing gap'
  - 'The timeline cards are guarded even though their only reaching surfaces are unrouted today (Phase 67 routes-or-deletes the parents; the requirement is absolute — RESEARCH inventory row 8)'
  - 'ActivityList swaps its inline R-05 computation for resolveTimelineNavUrl (a strict superset: same scheme/`//`/`\` rejections + mountedness). Existing Test 6/7 fixtures using non-mounted relative paths are updated to mounted ones — the REJECTION matrix rows stay untouched'

must_haves:
  truths:
    - 'TimelineEventCard and EnhancedVerticalTimelineCard render NO View-details affordance when navigation_url is /calendar/<uuid>, /mous/<uuid>, absent, or unsafe — and render it (navigating via the resolved URL) for /mous, /tasks/<id>, and per-type dossier paths'
    - 'ActivityList rows with unmounted navigation_url values (e.g. stale /mous/<uuid> in activity_stream metadata) are fully non-interactive: no role, no tabIndex, no onClick, no onKeyDown'
    - 'The existing ActivityList R-05/WR-01 rejection matrix (Test 7) is still green — the guard swap loosens nothing'
    - 'handleNavigate in both timeline cards navigates only to guard-resolved URLs (never the raw metadata value)'
  artifacts:
    - path: 'frontend/src/components/timeline/__tests__/TimelineEventCard.test.tsx'
      provides: 'Suppression/render matrix for both timeline cards (66-VALIDATION row 6)'
      min_lines: 60
    - path: 'frontend/src/components/timeline/TimelineEventCard.tsx'
      provides: 'Guard-gated View details'
      contains: 'resolveTimelineNavUrl'
    - path: 'frontend/src/components/activity-feed/ActivityList.tsx'
      provides: 'Mountedness-extended row guard'
      contains: 'resolveTimelineNavUrl'
  key_links:
    - from: 'frontend/src/components/timeline/TimelineEventCard.tsx'
      to: 'frontend/src/lib/timeline-navigation.ts (66-02)'
      via: 'import + render/handler gating'
      pattern: 'resolveTimelineNavUrl'
    - from: 'frontend/src/components/activity-feed/ActivityList.tsx'
      to: 'frontend/src/lib/timeline-navigation.ts (66-02)'
      via: 'safeNavUrl computation replaced by the shared guard'
      pattern: 'resolveTimelineNavUrl'
---

<objective>
Gate all three navigation_url consumers on the shared mounted-route guard: TimelineEventCard and EnhancedVerticalTimelineCard suppress the View-details affordance for unmountable targets; ActivityList extends its R-05 row guard with mountedness.

Purpose: roadmap success criterion 3's frontend half — even if the deployed edge lags the 66-03 source fix (documented drift failure mode) or stale DB rows carry dead URLs, no rendered affordance can dead-end on the notFound page.
Output: guarded consumers + RED→GREEN suppression tests + extended ActivityList matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/66-overview-error-contract-timeline-cross-links/66-UI-SPEC.md

<interfaces>
<!-- Guard import (created by 66-02, wave 1 — exists in this tree): -->

```typescript
import { resolveTimelineNavUrl } from '@/lib/timeline-navigation'
```

<!-- TimelineEventCard.tsx — current sites (verified):
     L117-121 handleNavigate: navigates raw event.metadata.navigation_url if truthy
     L327-337 render: {event.metadata.navigation_url && (<Button ...>view_details</Button>)}
     Target: compute once near the top —
       const navUrl = resolveTimelineNavUrl(event.metadata.navigation_url)
     handleNavigate navigates navUrl only when navUrl !== null; the render gate becomes
     {navUrl !== null && ( ...same Button... )}. EnhancedVerticalTimelineCard.tsx has the
     identical pair at L194-199 (handler, which also calls setIsActive(false)) and L421-431
     (gated actions container) — same transformation; keep the setIsActive call. -->

<!-- ActivityList.tsx — current guard (L107-115):
       const navUrl = a.metadata?.navigation_url
       const safeNavUrl = typeof navUrl === 'string' && navUrl.startsWith('/') && !navUrl.startsWith('//') && !navUrl.includes('\\') ? navUrl : null
     Target:
       const safeNavUrl = resolveTimelineNavUrl(a.metadata?.navigation_url)
     Everything downstream (interactive flag, onClick/onKeyDown handlers, role/tabIndex omission)
     stays byte-identical. Preserve the R-05/WR-01 rationale comment, noting the rules now live
     in lib/timeline-navigation.ts. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Suppression/render matrix for both timeline cards — RED first</name>
  <files>frontend/src/components/timeline/__tests__/TimelineEventCard.test.tsx</files>
  <read_first>
    - frontend/src/components/timeline/TimelineEventCard.tsx (FULL read once: props/TimelineEvent shape, i18n namespace, what minimal event fixture renders without crashing)
    - frontend/src/components/timeline/EnhancedVerticalTimelineCard.tsx (same pass: note the expand/active mechanics needed to reach the actions container in a test)
  </read_first>
  <behavior>
    - TimelineEventCard with navigation_url '/calendar/0a1b-uuid' → NO view_details button (queryByText/queryByRole absent), no disabled placeholder. RED today (card trusts truthy URLs).
    - TimelineEventCard with '/mous' → button present; clicking it calls the mocked navigate with '/mous'.
    - TimelineEventCard with navigation_url null/undefined → no button (pin of existing behavior, GREEN).
    - EnhancedVerticalTimelineCard describe block: '/mous/0a1b-uuid' → actions container absent; '/dossiers/countries/abc' → present. Drive whatever expand/active state is required per the read_first pass.
  </behavior>
  <action>
    Create the test file (this is the 66-VALIDATION Wave-0 file; both cards covered here — the filename matches the validation map). Mock @tanstack/react-router useNavigate to a spy; i18n echo mock; minimal TimelineEvent fixtures with only the fields the render path touches. Run: suppression cases RED. Commit: test(66-07): add failing View-details suppression tests for timeline cards.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/components/timeline/__tests__/TimelineEventCard.test.tsx 2>&1 | grep -qE 'failed' && echo RED_CONFIRMED</automated>
  </verify>
  <done>Suppression matrix exists for both cards; RED confirmed; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Gate the timeline cards + extend the ActivityList guard — GREEN</name>
  <files>frontend/src/components/timeline/TimelineEventCard.tsx, frontend/src/components/timeline/EnhancedVerticalTimelineCard.tsx, frontend/src/components/activity-feed/ActivityList.tsx, frontend/src/components/activity-feed/__tests__/ActivityList.test.tsx</files>
  <read_first>
    - frontend/src/components/activity-feed/__tests__/ActivityList.test.tsx (Test 6 + Test 7 fixtures — identify which relative-path fixtures are NOT on the mounted allowlist and need retargeting to e.g. /tasks/abc)
  </read_first>
  <action>
    1. Apply the interfaces-block transformation to both timeline cards: compute navUrl via resolveTimelineNavUrl once, gate handleNavigate and the View-details render on navUrl !== null, navigate navUrl (never the raw metadata). No styling/layout changes; the affordance is absent, not disabled (UI-SPEC §3).
    2. ActivityList: replace the inline safeNavUrl computation with the guard call; keep the comment block, pointing to lib/timeline-navigation.ts for the rules.
    3. ActivityList.test.tsx: retarget any accepted-fixture paths that aren't mounted (e.g. a generic '/foo/1') to mounted equivalents ('/tasks/1'); ADD mountedness rejection cases: '/calendar/<uuid>' and '/mous/<uuid>' rows render non-interactive (no role, no tabIndex). The existing Test 7 rejection rows (https://, //, javascript:, '', backslash) must pass UNMODIFIED — if any fails, the guard regressed; fix the guard usage, not the test.
    4. Run both test files + the 66-02 guard matrix + pnpm type-check. GREEN. Commit: feat(66-07): gate navigation_url consumers on the mounted-route guard (OVRERR-02).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/components/timeline/__tests__/TimelineEventCard.test.tsx src/components/activity-feed/__tests__/ActivityList.test.tsx && pnpm type-check && [ "$(grep -l 'resolveTimelineNavUrl' src/components/timeline/TimelineEventCard.tsx src/components/timeline/EnhancedVerticalTimelineCard.tsx src/components/activity-feed/ActivityList.tsx | wc -l | tr -d ' ')" = "3" ]</automated>
  </verify>
  <acceptance_criteria>
    - All three consumers import and gate on the shared guard; no raw metadata navigation remains
    - Existing R-05/WR-01 rejection matrix green unmodified; new mountedness cases green
  </acceptance_criteria>
  <done>No timeline/activity affordance can navigate to an unmounted route, regardless of what the server or DB emits.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                        |
| -------------------------------------- | ------------------------------------------------------------------ |
| edge/DB navigation_url → client router | server- and DB-sourced URLs cross into navigate() — guard required |

## STRIDE Threat Register

| Threat ID | Category  | Component                                 | Disposition | Mitigation Plan                                                                                                                                                        |
| --------- | --------- | ----------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-66-14   | Tampering | open redirect via navigation_url          | mitigate    | resolveTimelineNavUrl preserves every R-05/WR-01 rejection (existing Test 7 must pass unmodified — explicit gate) and adds mountedness; applied to ALL three consumers |
| T-66-15   | Tampering | dishonest affordances (dead View details) | mitigate    | Suppression-as-absence pinned by RED-first render tests on both cards                                                                                                  |
| T-66-SC   | Tampering | npm/pip/cargo installs                    | accept      | No package installs in this plan                                                                                                                                       |

</threat_model>

<verification>
- 66-VALIDATION rows realized: 'TimelineEventCard/EnhancedVerticalTimelineCard suppress View details' (Task 1-2) + 'ActivityList guard extension keeps the rejection matrix green' (Task 2)
- Per-task gate: quick vitest run of the touched test files + pnpm type-check
</verification>

<success_criteria>

- Roadmap success criterion 3 frontend half: every rendered timeline affordance routes to a mounted destination or is absent
- Zero R-05 regressions
  </success_criteria>

<output>
Create `.planning/phases/66-overview-error-contract-timeline-cross-links/66-07-SUMMARY.md` when done.
</output>
