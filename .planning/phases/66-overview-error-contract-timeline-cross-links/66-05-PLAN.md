---
phase: 66-overview-error-contract-timeline-cross-links
plan: 05
type: execute
wave: 2
depends_on: [66-01]
files_modified:
  - frontend/src/pages/dossiers/overview-cards/ForumMetadataCard.tsx
  - frontend/src/pages/dossiers/overview-cards/ForumSessionsCard.tsx
  - frontend/src/pages/dossiers/overview-cards/ConnectedAnchorsCard.tsx
  - frontend/src/pages/dossiers/overview-cards/PositionTrackerCard.tsx
  - frontend/src/pages/dossiers/overview-cards/DeliverablesTrackerCard.tsx
  - frontend/src/pages/dossiers/overview-cards/MeetingScheduleCard.tsx
  - frontend/src/pages/dossiers/overview-cards/MemberListCard.tsx
  - frontend/src/pages/dossiers/overview-cards/PersonMetadataCard.tsx
  - frontend/src/pages/dossiers/overview-cards/EngagementHistoryCard.tsx
  - frontend/src/pages/dossiers/overview-cards/ElectedOfficialOfficeCard.tsx
  - frontend/src/pages/dossiers/overview-cards/ElectedOfficialCommitteesCard.tsx
  - frontend/src/pages/dossiers/overview-cards/__tests__/TypeCardErrorStates.test.tsx
autonomous: true
requirements: [OVRERR-01]

key_decisions:
  - 'Same UI-SPEC §1 anatomy and precedence as 66-04 (error before empty; stale-while-error; one shared key; no retry; chrome untouched). This plan covers the forum/topic/working-group/person/elected-official card set (11 files) — file-disjoint from 66-04 within the same directory, including the __tests__ partition (this plan owns ONLY TypeCardErrorStates.test.tsx)'
  - 'A2 RESOLVED at plan time: apiGet throws on non-2xx (lib/api-client.ts L61) — useElectedOfficial queries already reject; EO cards need ONLY the card-layer change (no hook edits, the hook file is NOT in this plan)'
  - "PositionTrackerCard: useDossierPositionLinks already throws (hook L169) and exposes error — the card adds the error branch using the hook's exposed shape; if cached positions exist, render them (stale-while-error)"
  - "ElectedOfficial cards translate from the elected-officials namespace today — the error line still uses the dossier-namespace key via colon form t('dossier:overview.sectionError') (one key for all 19 cards, A-2; colon form per Pitfall 7)"

must_haves:
  truths:
    - 'Each of the 11 cards with a forced query error and no cached data renders role="alert" containing the sectionError copy, with its empty copy ABSENT'
    - 'ElectedOfficialOfficeCard no longer renders all-dash rows on a failed useElectedOfficial query — it renders the error line (dash rows were a third trustworthy-looking failure mode)'
    - 'With cached data present and isError true, cards render data and NO error line'
    - 'Genuinely empty sections still render the unchanged empty copy (pixel-identical, UI-SPEC interaction state 4)'
  artifacts:
    - path: 'frontend/src/pages/dossiers/overview-cards/__tests__/TypeCardErrorStates.test.tsx'
      provides: 'Parameterized forced-error suite for the 11 type-tab cards (66-VALIDATION row 2, this plan half)'
      min_lines: 110
    - path: 'frontend/src/pages/dossiers/overview-cards/PositionTrackerCard.tsx'
      provides: 'Error branch consuming the hook-exposed error'
      contains: 'sectionError'
    - path: 'frontend/src/pages/dossiers/overview-cards/ElectedOfficialOfficeCard.tsx'
      provides: 'Error branch replacing the all-dash failure mode'
      contains: 'sectionError'
  key_links:
    - from: 'frontend/src/pages/dossiers/overview-cards/PositionTrackerCard.tsx'
      to: 'useDossierPositionLinks (throws at hook L169)'
      via: 'error destructure + error-before-empty branch'
      pattern: 'sectionError'
    - from: 'frontend/src/pages/dossiers/overview-cards/ElectedOfficialOfficeCard.tsx'
      to: 'frontend/src/lib/api-client.ts (apiGet throws on non-2xx)'
      via: 'useElectedOfficial isError'
      pattern: 'isError'
---

<objective>
Apply the card-layer half of the OVRERR-01 contract to the remaining 11 type-tab overview cards (forum, topic, working group, person, elected official): isError destructure + UI-SPEC §1 error line before the empty branch, stale-while-error semantics.

Purpose: completes roadmap success criterion 2 across the full 19-card inventory (66-04 covers the other 8). The EO cards' all-dash failure rendering is a third dishonest mode this plan removes.
Output: 11 honest cards + RED→GREEN parameterized test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/66-overview-error-contract-timeline-cross-links/66-UI-SPEC.md
@.planning/phases/66-overview-error-contract-timeline-cross-links/66-PATTERNS.md

<interfaces>
<!-- Error-branch anatomy: identical to 66-04 (UI-SPEC §1, binding) — insert after isLoading,
     BEFORE empty; reuse each card's own container/h3/dir; the <p> is the only new element: -->

```tsx
<p role="alert" className="text-sm text-[var(--danger)] text-center py-8">
  {t('overview.sectionError', {
    defaultValue: 'Failed to load this section. Check your connection and try again.',
  })}
</p>
```

<!-- Per-card hook family + data-unavailable comparison (RESEARCH §Card Inventory + A2 resolution):
     ForumMetadataCard            useDossierOverview         data === null
     ForumSessionsCard            useDossierOverview         data === null
     ConnectedAnchorsCard         useDossierOverview         data === null
     DeliverablesTrackerCard      useDossierOverview         data === null
     MeetingScheduleCard          useDossierOverview         data === null
     MemberListCard               useDossierOverview         data === null
     PersonMetadataCard           useDossierOverview         data === null
     EngagementHistoryCard        useDossierOverview         data === null
     PositionTrackerCard          useDossierPositionLinks    error != null && no cached positions (read the hook's exposed shape — it throws at L169 and exposes error at ~L86)
     ElectedOfficialOfficeCard    useElectedOfficial         isError && data === undefined (raw useQuery; apiGet throws — verified api-client.ts L61)
     ElectedOfficialCommitteesCard useElectedOfficial        isError && data === undefined
     NAMESPACE NOTE: cards already on useTranslation('dossier') use t('overview.sectionError', ...);
     EO cards (elected-officials namespace) use the colon form t('dossier:overview.sectionError', ...)
     — dot-form lookups leak raw keys (Pitfall 7 / project memory). -->

<!-- Test mocks: mirror 66-04's vi.hoisted pattern. Mock targets per family —
     '@/hooks/useDossierOverview' (barrel), PositionTrackerCard's hook at the path the CARD
     imports (read its import line), and the EO cards' useElectedOfficial at the path THEY import. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Forced-error card tests for the 11 type-tab cards — RED first</name>
  <files>frontend/src/pages/dossiers/overview-cards/__tests__/TypeCardErrorStates.test.tsx</files>
  <read_first>
    - frontend/src/pages/dossiers/overview-cards/__tests__/SharedRecentActivityCard.test.tsx (mock pattern reference ONLY — do not modify; that file is 66-04's)
    - frontend/src/pages/dossiers/overview-cards/PositionTrackerCard.tsx + the useDossierPositionLinks hook it imports (exposed return shape, one pass)
    - frontend/src/pages/dossiers/overview-cards/ElectedOfficialOfficeCard.tsx (useElectedOfficial import path + the all-dash render to assert ABSENT)
  </read_first>
  <behavior>
    - Parameterized it.each over the 8 useDossierOverview cards: { isError: true, data: null, isLoading: false } → role="alert" with /failed to load this section/i AND that card's empty copy absent. RED today.
    - PositionTrackerCard: hook mock returns error: new Error('forced') with empty/undefined positions → alert present; with cached positions + error → positions render, no alert.
    - ElectedOfficialOfficeCard + ElectedOfficialCommitteesCard: hook mock { isError: true, data: undefined } → alert present AND no dash-placeholder rows ('—' cells absent from the office card body). RED today (EO cards render dashes on error).
    - Empty-pin (one representative, e.g. MemberListCard): healthy empty data → existing empty copy, no alert.
  </behavior>
  <action>
    Create TypeCardErrorStates.test.tsx with the vi.hoisted mutable-state mocks per hook family and the i18n echo mock honouring defaultValue (assertions against the defaultValue string work for both plain and colon-form keys). Decision-tagged test names. Run: error-state cases RED, empty-pin GREEN. Commit: test(66-05): add failing forced-error tests for type-tab overview cards.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/pages/dossiers/overview-cards/__tests__/TypeCardErrorStates.test.tsx 2>&1 | grep -qE 'failed' && echo RED_CONFIRMED</automated>
  </verify>
  <done>Forced-error suite exists for all 11 cards (66-VALIDATION row 2, this half); RED confirmed; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Error branch in all 11 cards — GREEN</name>
  <files>frontend/src/pages/dossiers/overview-cards/ForumMetadataCard.tsx, frontend/src/pages/dossiers/overview-cards/ForumSessionsCard.tsx, frontend/src/pages/dossiers/overview-cards/ConnectedAnchorsCard.tsx, frontend/src/pages/dossiers/overview-cards/PositionTrackerCard.tsx, frontend/src/pages/dossiers/overview-cards/DeliverablesTrackerCard.tsx, frontend/src/pages/dossiers/overview-cards/MeetingScheduleCard.tsx, frontend/src/pages/dossiers/overview-cards/MemberListCard.tsx, frontend/src/pages/dossiers/overview-cards/PersonMetadataCard.tsx, frontend/src/pages/dossiers/overview-cards/EngagementHistoryCard.tsx, frontend/src/pages/dossiers/overview-cards/ElectedOfficialOfficeCard.tsx, frontend/src/pages/dossiers/overview-cards/ElectedOfficialCommitteesCard.tsx</files>
  <read_first>
    - Each card file ONCE as you edit it (destructure line, container, title, empty branch in one pass)
  </read_first>
  <action>
    For each card: add isError/error to the hook destructure and insert the error branch per the interfaces block — after isLoading, before empty, reusing the card's own container/dir/h3 exactly. Use the per-card comparison from the interfaces table. EO cards: the error branch REPLACES the dash-fallback path when the query errored (genuinely-missing single fields in healthy data keep their dashes); use the colon-form dossier:overview.sectionError key. PositionTrackerCard: match the hook's actual exposed shape (read once in Task 1). No text-destructive, no icons, no retry, no layout changes; empty branches byte-identical. Run Task 1 file (GREEN) + pnpm type-check. Commit: feat(66-05): per-card section error lines for type-tab overview cards (OVRERR-01).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/pages/dossiers/overview-cards/__tests__/TypeCardErrorStates.test.tsx && pnpm type-check && [ "$(grep -rl 'sectionError' src/pages/dossiers/overview-cards --include='*.tsx' | grep -v __tests__ | wc -l | tr -d ' ')" -ge "19" ]</automated>
  </verify>
  <acceptance_criteria>
    - All 11 cards carry the error branch with the exact UI-SPEC anatomy; EO dash-on-error mode gone
    - Suite green; type-check exit 0
  </acceptance_criteria>
  <done>Together with 66-04, all 19 overview cards distinguish failed from empty.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                             |
| ----------------------------- | ----------------------------------------------------------------------- |
| query/express error → card UI | error objects cross into render; only generic localized copy may render |

## STRIDE Threat Register

| Threat ID | Category                 | Component                | Disposition | Mitigation Plan                                                                                                                                             |
| --------- | ------------------------ | ------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-66-09   | Information Disclosure   | card error lines         | mitigate    | Only t('overview.sectionError') renders — apiGet's `API error 500: ...` strings and PostgREST messages never reach the DOM; tests assert the localized copy |
| T-66-10   | Repudiation (of failure) | EO all-dash failure mode | mitigate    | Errored EO queries render the alert line, not dash rows that read as "no office data"                                                                       |
| T-66-SC   | Tampering                | npm/pip/cargo installs   | accept      | No package installs in this plan                                                                                                                            |

</threat_model>

<verification>
- 66-VALIDATION row 2 realized for the type-tab half (RED-first, vi.hoisted mocks per hook family)
- Per-task gate: quick vitest run of the test file + pnpm type-check
</verification>

<success_criteria>

- Forced isError on any of the 11 cards renders the danger alert line, never the empty state or dash rows
- Empty states pixel-identical to pre-phase
  </success_criteria>

<output>
Create `.planning/phases/66-overview-error-contract-timeline-cross-links/66-05-SUMMARY.md` when done.
</output>
