---
phase: 85-linear-taste-refinements-f16-f21
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/components/settings/SettingsNavigation.tsx
  - frontend/src/i18n/en/settings.json
  - frontend/src/i18n/ar/settings.json
autonomous: false
requirements: [TASTE-03, TASTE-05]

must_haves:
  truths:
    - 'Satisfies D-85-03: on /settings routes the global app sidebar is suppressed and the settings sub-nav is the single nav column, with a back-to-app link whose chevron flips in RTL'
    - 'Satisfies D-85-05: the settings sub-nav renders its nine sections under muted uppercase group headers (default buckets Account / Privacy & access / Connected, user-confirmable at the checkpoint)'
    - 'Satisfies D-85-07: back-link icon spacing uses logical ms-/me- utilities; group headers use var(--ink-faint); no physical-direction classes introduced'
    - 'A user on /settings sees exactly ONE nav column (the settings sub-nav), not the current two, and can return to the app in one click'
  artifacts:
    - path: 'frontend/src/components/layout/AppShell.tsx'
      provides: 'Route-conditional Sidebar suppression keyed off the existing useRouterState pathname'
      contains: '/settings'
    - path: 'frontend/src/components/settings/SettingsNavigation.tsx'
      provides: 'Back-to-app link + grouped SECTIONS render with muted group headers'
      contains: 'backToApp'
    - path: 'frontend/src/i18n/en/settings.json'
      provides: 'backToApp + navGroups.* keys (EN)'
      contains: 'backToApp'
    - path: 'frontend/src/i18n/ar/settings.json'
      provides: 'backToApp + navGroups.* keys (AR — no EN fallback leakage)'
      contains: 'backToApp'
  key_links:
    - from: 'frontend/src/components/layout/AppShell.tsx'
      to: 'frontend/src/components/settings/SettingsNavigation.tsx'
      via: 'sidebar suppressed on /settings frees the rail the SettingsLayout 240px nav column already occupies'
      pattern: "startsWith\\('/settings'\\)"
    - from: 'frontend/src/components/settings/SettingsNavigation.tsx'
      to: 'frontend/src/i18n/en/settings.json'
      via: 't() keys for back link and group headers in the settings namespace'
      pattern: 'navGroups'
---

<objective>
Land TASTE-03/F18 (settings single-nav + back-to-app) and TASTE-05/F20 (grouped settings sub-nav) — both edit `SettingsNavigation.tsx`, so they ship in one plan.

Purpose: today `/settings` shows TWO nav columns simultaneously — the global 256px `<Sidebar/>` from AppShell AND the 240px settings nav inside `SettingsLayout`'s grid. F18 makes settings a full-page mode with a single grouped sub-nav and a return affordance; F20 gives that sub-nav Linear-style muted section headers.

Output: route-conditional Sidebar in `AppShell.tsx`, back link + grouped sections in `SettingsNavigation.tsx`, new i18n keys in both `settings.json` bundles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/85-linear-taste-refinements-f16-f21/85-CONTEXT.md
@.planning/phases/85-linear-taste-refinements-f16-f21/85-PATTERNS.md
@frontend/DESIGN.md
@frontend/src/components/layout/AppShell.tsx
@frontend/src/components/settings/SettingsNavigation.tsx
@frontend/src/components/settings/SettingsLayout.tsx
@frontend/src/pages/TaskDetailPage.tsx

<interfaces>
Facts verified against the live code (2026-07-05):

- `AppShell.tsx:120` already reads `const pathname = useRouterState({ select: (s) => s.location.pathname })` (used for drawer-close-on-navigate at 143-148) — reuse it; no new hook.
- `<Sidebar/>` mounts TWICE in AppShell: desktop `<aside className="... hidden lg:block ...">` (lines 178-189) and inside the mobile `Drawer.Body` (lines 234-242). Both are unconditional today.
- `SettingsLayout.tsx:59-67` already renders its own 240px nav column (`gridTemplateColumns: '240px 1fr'`) containing `<SettingsNavigation/>` inside `<main>` — the root cause is the DOUBLE nav column; the fix is suppression in AppShell, NOT a new grid.
- `SettingsNavigation.tsx:20-30` — the flat `SECTIONS: SectionDef[]` (profile, general, appearance, notifications, security, accessibility, data-privacy, email-digest, integrations) rendered by one flat `.map()` (lines 54-71). No back link exists yet. Uses `useTranslation('settings')`.
- Back-link analog: `TaskDetailPage.tsx:98-111` — ghost Button + `<ChevronRight className={isRTL ? '' : 'rotate-180'}>` + logical `ms-*`/`me-*` spacing + `navigate({ to: ... })`.
- Group-header style: the app sidebar's real group header (`Sidebar.tsx:118-122`) is `text-[10px] font-semibold tracking-[0.1em] uppercase` — PATTERNS confirms 10px/600 (NOT the CONTEXT paraphrase "11px medium"); in the settings context (on `--surface`, not sidebar-ink) the color token is `text-[var(--ink-faint)]`.
- i18n: bundles are static (`src/i18n/index.ts`); edit `src/i18n/{en,ar}/settings.json` (NOT public/locales — dead). An AR key left missing silently falls back to EN — both files must get every new key.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: F18 — suppress the global Sidebar on /settings routes</name>
  <files>frontend/src/components/layout/AppShell.tsx</files>
  <read_first>.planning/phases/85-linear-taste-refinements-f16-f21/85-PATTERNS.md (TASTE-03 section), frontend/src/components/layout/AppShell.tsx, frontend/src/components/settings/SettingsLayout.tsx</read_first>
  <action>
    Per D-85-03 (locked): derive `const isSettingsRoute = pathname.startsWith('/settings')` from the EXISTING `pathname` at AppShell.tsx:120 (no new hook, no new import). Gate BOTH `<Sidebar/>` mounts on `!isSettingsRoute`: (1) the desktop `<aside>` at lines 178-189 — suppress the whole aside so the content column takes the freed rail (SettingsLayout's own 240px nav column becomes the single nav); (2) the mobile drawer Sidebar at lines 234-242 — if suppressing only the inner `<Sidebar/>` leaves an empty drawer, gate the drawer trigger/mount with the same flag instead (executor's judgment on the exact node; the invariant is: no route on /settings ever shows the global sidebar, and no dead empty drawer is reachable). Do not touch any other AppShell chrome (topbar stays).
  </action>
  <acceptance_criteria>
    - `grep -n "startsWith('/settings')" frontend/src/components/layout/AppShell.tsx` returns exactly one derivation site, referenced by both gates
    - No new hooks/imports added to AppShell.tsx for this change
    - On `/settings` the DOM contains zero elements from the global Sidebar component; on `/` (and any non-settings route) the sidebar renders exactly as before
    - `cd frontend && pnpm type-check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && pnpm type-check && pnpm test --run src/components/settings</automated>
  </verify>
  <done>/settings renders a single nav column (the settings sub-nav); all other routes are byte-identical in behavior.</done>
</task>

<task type="auto">
  <name>Task 2: F18+F20 — back-to-app link and grouped sections in SettingsNavigation</name>
  <files>frontend/src/components/settings/SettingsNavigation.tsx, frontend/src/i18n/en/settings.json, frontend/src/i18n/ar/settings.json</files>
  <read_first>.planning/phases/85-linear-taste-refinements-f16-f21/85-PATTERNS.md (TASTE-03 + TASTE-05 sections), frontend/DESIGN.md (ink-faint token), frontend/src/components/settings/SettingsNavigation.tsx, frontend/src/pages/TaskDetailPage.tsx (lines 98-111, back-link idiom), frontend/src/components/layout/Sidebar.tsx (lines 118-122, group-header idiom)</read_first>
  <action>
    Per D-85-03 + D-85-05 (locked), D-85-07 (RTL discipline):

    1. Back link (F18): add a "Back to app" affordance as the FIRST element inside the nav card, above the section list. Copy the TaskDetailPage.tsx:98-111 idiom exactly: ghost-style button/link, `ChevronRight` from lucide with `className={isRTL ? '' : 'rotate-180'}` (points left in LTR = back; right in RTL = back), logical `ms-*`/`me-*` icon spacing, `isRTL` derived from `i18n.language === 'ar'` (the file already calls `useTranslation('settings')` — destructure `i18n` too). Navigate to `/` via TanStack Router (`useNavigate` or `<Link to="/">` — match whichever the file's siblings use). Label = `t('backToApp')`. Sentence case, no marketing voice, no literal "‹" character (the chevron icon IS the glyph).
    2. Grouping (F20): restructure the flat `SECTIONS` array (lines 20-30) into a `NAV_GROUPS: { labelKey: string; sections: SectionDef[] }[]` constant with the default buckets — Account → profile, general, appearance, notifications; Privacy & access → security, accessibility, data-privacy; Connected → email-digest, integrations. Replace the single flat `.map()` (lines 54-71) with a nested groups→sections map, rendering a muted header div before each group styled `text-[10px] font-semibold tracking-[0.1em] uppercase text-[var(--ink-faint)]` (the Sidebar.tsx:118-122 idiom re-tokened for the surface context — 10px/600 per PATTERNS, NOT 11px/medium). Keep each section button's existing markup, className, aria, and onChange wiring byte-identical — only the iteration structure changes. Uppercase here is legitimate (group headers = the sanctioned mono/table-header class of uppercase). **Preserve the `export const NAV_ITEMS = SECTIONS` backward-compat stub at line 83:** either keep the flat `SECTIONS` array as the source and derive `NAV_GROUPS` from it, or redefine the export as `NAV_ITEMS = NAV_GROUPS.flatMap((g) => g.sections)` — do NOT leave `NAV_ITEMS` referencing a removed symbol (that breaks `pnpm type-check`/`build`).
    3. i18n: add to BOTH `src/i18n/en/settings.json` and `src/i18n/ar/settings.json`, following each file's existing key style: `backToApp` (EN "Back to app"; AR "العودة إلى التطبيق") and `navGroups.account` / `navGroups.privacyAccess` / `navGroups.connected` (EN "Account" / "Privacy & access" / "Connected"; AR "الحساب" / "الخصوصية والوصول" / "الخدمات المتصلة"). Never leave an AR key missing — static bundles fall back to EN silently in both languages.

    The exact bucket labels are the user's call per CONTEXT — ship the default and confirm at the Task 3 checkpoint (do not block).

  </action>
  <acceptance_criteria>
    - `grep -c "backToApp" frontend/src/components/settings/SettingsNavigation.tsx` ≥ 1; `grep -c "backToApp" frontend/src/i18n/en/settings.json` = 1; `grep -c "backToApp" frontend/src/i18n/ar/settings.json` = 1
    - `grep -c "navGroups" frontend/src/i18n/en/settings.json` ≥ 1 and same for ar (all three group keys present in both files)
    - `grep -n "rotate-180" frontend/src/components/settings/SettingsNavigation.tsx` shows the chevron flip gated on isRTL; `grep -Ec 'ml-|mr-|pl-|pr-|text-left|text-right' frontend/src/components/settings/SettingsNavigation.tsx` returns 0
    - All nine section ids (profile, general, appearance, notifications, security, accessibility, data-privacy, email-digest, integrations) still render, distributed 4/3/2 across the three groups
    - `grep -c "var(--ink-faint)" frontend/src/components/settings/SettingsNavigation.tsx` ≥ 1 on the group header; no raw hex introduced
    - `pnpm --dir frontend lint` passes the i18n namespace check (scripts/check-i18n-namespaces.mjs)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && pnpm type-check && pnpm test --run src/components/settings && pnpm build</automated>
  </verify>
  <done>Settings sub-nav shows the back link on top and nine sections under three muted group headers; EN and AR both resolve every new key.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human render-parity sign-off — settings nav (EN/AR × 1024/1400) + bucket confirmation</name>
  <what-built>F18: global sidebar suppressed on /settings with a back-to-app link (chevron flips in RTL); F20: settings sections grouped under Account / Privacy & access / Connected muted headers (default buckets — your call to keep or rename).</what-built>
  <how-to-verify>
    1. `pnpm --dir frontend dev`, open http://localhost:5173/settings (EN, dark): exactly ONE nav column; "Back to app" at top with a left-pointing chevron; three muted uppercase group headers; clicking the back link returns to the app home; every section still opens.
    2. Confirm the grouping buckets/labels (Account / Privacy & access / Connected) — say so if you want different buckets or names.
    3. http://localhost:5173/settings?lng=ar: nav column sits on the RIGHT; chevron points right; group headers and back link read correctly in Arabic.
    4. Check both at ~1024px and ~1400px widths; toggle light mode once to confirm header contrast.
    5. Navigate to any non-settings route: the global sidebar is back, unchanged.
  </how-to-verify>
  <action>Start the dev server if not running, present what was built and the how-to-verify steps to the user, and wait for explicit sign-off. Do not self-certify visuals; do not proceed past this gate on your own judgment.</action>
  <verify>
    <human-check>User walks the how-to-verify steps (EN/AR, dark/light) and confirms render parity</human-check>
  </verify>
  <done>User replied "approved" (or raised issues that were addressed and re-verified)</done>
  <resume-signal>Type "approved" (optionally with renamed buckets) or describe issues</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                      |
| -------- | -------------------------------------------------------------------------------- |
| none new | Layout-conditional render + static nav restructure behind the existing auth gate |

## STRIDE Threat Register

| Threat ID | Category          | Component                                             | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                |
| --------- | ----------------- | ----------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-85-02   | (none applicable) | AppShell.tsx / SettingsNavigation.tsx / settings.json | accept      | Route check is a client-side render condition, not an authz control (auth stays in \_protected.tsx beforeLoad — untouched). No new user input, no data flow, static i18n strings only, no injection vector, no package installs. Zero new attack surface — explicitly trivial. |

</threat_model>

<verification>
- `cd frontend && pnpm type-check` exits 0; `pnpm build` succeeds
- `cd frontend && pnpm test --run src/components/settings` green (SettingsLayout suite)
- `pnpm --dir frontend lint` green (i18n namespace check + no physical-direction classes)
- Human render-parity sign-off (Task 3) received, including bucket confirmation — visuals are NOT self-certified
</verification>

<success_criteria>

- TASTE-03: single nav column on /settings, back-to-app link with RTL-correct chevron, sidebar intact everywhere else
- TASTE-05: nine sections grouped under three muted headers matching the sidebar group-header idiom, tokens only
- EN + AR key parity; zero physical-direction utilities introduced
  </success_criteria>

<output>
Create `.planning/phases/85-linear-taste-refinements-f16-f21/85-02-SUMMARY.md` when done (record the user's bucket decision).
</output>
