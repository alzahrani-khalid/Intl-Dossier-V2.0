---
phase: 85-linear-taste-refinements-f16-f21
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/WorkBoard/board.css
  - frontend/src/pages/WorkBoard/KCard.tsx
  - frontend/src/pages/WorkBoard/BoardColumn.tsx
  - frontend/tests/e2e/kanban-render.spec.ts
  - frontend/tests/e2e/kanban-rtl.spec.ts
autonomous: false
requirements: [TASTE-01, TASTE-06]

must_haves:
  truths:
    - 'Satisfies D-85-01: overdue kanban cards no longer show the full-height red inline-start edge bar; overdue is carried on a red mono due-date chip'
    - 'Satisfies D-85-06: each rendered kanban column header (todo/in_progress/review/done) shows a small token-colored status glyph before the column name'
    - 'Satisfies D-85-07: the .kcard.overdue class is still applied to the article element (Playwright selectors keep resolving); only the CSS border declaration is gone; all directional CSS stays on logical properties'
    - 'An analyst viewing an all-overdue board sees a calm card wall with red due chips, not a wall of red edge bars'
  artifacts:
    - path: 'frontend/src/pages/WorkBoard/board.css'
      provides: 'Removed .kcard.overdue border rule; new .kcard-foot .kdue.is-overdue rule; .col-glyph shrink guard'
      contains: '.kdue.is-overdue'
    - path: 'frontend/src/pages/WorkBoard/KCard.tsx'
      provides: 'kdue/is-overdue classes on the due-text span, gated on item.is_overdue; unchanged overdue class on the article'
      contains: 'is-overdue'
    - path: 'frontend/src/pages/WorkBoard/BoardColumn.tsx'
      provides: 'Stage-keyed inline SVG status glyph as first child of .col-head'
      contains: 'col-glyph'
    - path: 'frontend/tests/e2e/kanban-render.spec.ts'
      provides: 'Updated BOARD-02 assertion: overdue card carries .kdue.is-overdue, no 3px inline-start border'
      contains: 'kdue'
  key_links:
    - from: 'frontend/src/pages/WorkBoard/KCard.tsx'
      to: 'frontend/src/pages/WorkBoard/board.css'
      via: 'kdue is-overdue className on the due span matching the .kcard-foot .kdue.is-overdue rule'
      pattern: 'kdue'
    - from: 'frontend/src/pages/WorkBoard/BoardColumn.tsx'
      to: 'frontend/src/pages/WorkBoard/WorkBoard.tsx'
      via: 'existing stage prop (WorkflowStage) keys the glyph map — no new plumbing'
      pattern: 'stage'
---

<objective>
Land TASTE-01/F16 (kanban overdue reframing) and TASTE-06/F21 (column-header status glyphs) on the WorkBoard surface — both touch `board.css`, so they ship in one plan.

Purpose: with all-overdue seed data the current `.kcard.overdue` 3px red edge bar turns the board into a wall of red, and it reads as a priority accent when it is an overdue marker. Overdue moves onto the due-date text (red, semibold). Column headers gain a small stage-status glyph, matching Linear's board affordance.

Output: edited `board.css`, `KCard.tsx`, `BoardColumn.tsx`, plus updated `kanban-render.spec.ts` / `kanban-rtl.spec.ts` (both currently assert the exact 3px border this plan removes — leaving them would make CI fail on a correct change).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/85-linear-taste-refinements-f16-f21/85-CONTEXT.md
@.planning/phases/85-linear-taste-refinements-f16-f21/85-PATTERNS.md
@frontend/DESIGN.md
@frontend/src/pages/WorkBoard/board.css
@frontend/src/pages/WorkBoard/KCard.tsx
@frontend/src/pages/WorkBoard/BoardColumn.tsx

<interfaces>
Facts verified against the live code (2026-07-05):

- `board.css:28-30` is the rule to delete: `.kcard.overdue { border-inline-start: 3px solid var(--danger); }`. The file header comment (line 3) documents that border and must be updated in the same edit.
- `KCard.tsx:117` keeps `item.is_overdue && 'overdue'` on the `<article>` — DO NOT remove (Playwright + KCard.test.tsx line 113 assert the class).
- `KCard.tsx:134-136` current due render: `<LtrIsolate><span className="font-mono">{dueText}</span></LtrIsolate>` — no overdue modifier exists yet. `cn` is already imported (line 23).
- `BoardColumn.tsx:62-75` is the LIVE column header (`<header className="col-head">` with `<h3 id={titleId}>` first). `WorkBoard.tsx`'s `.col-head` usage is only the loading skeleton — do not touch it.
- `board.css:126-132`: `.col-head h3 { flex-grow: 1 }` — a glyph prepended before `<h3>` sits at inline-start and the header layout holds (space-between is absorbed by the growing h3).
- Stages rendered: `['todo','in_progress','review','done']` (`WorkBoard.tsx:55`); `cancelled` exists on `WorkflowStage` but is filtered out (`WorkBoard.tsx:71-73`).
- CSS-shape analog for the new due rule: `index.css:884-888` `.task-due.today, .task-due.high { color: var(--danger); font-weight: 600; }`.
- SVG analog: `frontend/src/components/signature-visuals/Donut.tsx` — token-only stroke/fill, plain `<circle>`, `strokeDasharray`, no motion library.
- e2e coupling: `frontend/tests/e2e/kanban-render.spec.ts:24-31` asserts `borderInlineStartWidth === '3px'` on `.kcard.overdue`; `frontend/tests/e2e/kanban-rtl.spec.ts:22-27` asserts a >0 physical right border in RTL. Both assertions pin the border this plan removes.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: F16 — move overdue from edge bar to red due chip; update the two coupled e2e specs</name>
  <files>frontend/src/pages/WorkBoard/board.css, frontend/src/pages/WorkBoard/KCard.tsx, frontend/tests/e2e/kanban-render.spec.ts, frontend/tests/e2e/kanban-rtl.spec.ts</files>
  <read_first>.planning/phases/85-linear-taste-refinements-f16-f21/85-PATTERNS.md (TASTE-01 section), frontend/DESIGN.md (danger token), frontend/src/pages/WorkBoard/board.css, frontend/src/pages/WorkBoard/KCard.tsx, frontend/tests/e2e/kanban-render.spec.ts, frontend/tests/e2e/kanban-rtl.spec.ts, frontend/src/pages/WorkBoard/__tests__/KCard.test.tsx</read_first>
  <action>
    Per D-85-01 (locked) with the D-85-07 carve-out:

    1. board.css: delete ONLY the `border-inline-start: 3px solid var(--danger);` declaration block at lines 28-30 (the whole `.kcard.overdue { ... }` rule since it contains nothing else). Update the stale file-header comment on line 3 ("BOARD-02: .kcard.overdue uses border-inline-start...") to state that overdue is now carried on `.kdue.is-overdue` (Phase 85 D-85-01). Immediately after the existing `.kcard-foot .font-mono` block (lines 71-75), add a new rule `.kcard-foot .kdue.is-overdue` with exactly `color: var(--danger); font-weight: 600;` — mirroring the `.task-due.today/.high` shape at index.css:884-888. The new `.kcard-foot .kdue.is-overdue` is (0,3,0) vs `.kcard-foot .font-mono` (0,2,0), so it wins on specificity regardless of order; placing it after `.kcard-foot .font-mono` keeps the cascade unambiguous.
    2. KCard.tsx: change the due span at lines 134-136 from `className="font-mono"` to `className={cn('font-mono kdue', item.is_overdue && 'is-overdue')}` (cn already imported line 23). Do NOT touch line 117's `item.is_overdue && 'overdue'` on the article — Playwright selectors and KCard.test.tsx depend on the class attachment.
    3. kanban-render.spec.ts: replace the BOARD-02 border assertion (lines 24-31). New assertion inside the same `count() > 0` guard: (a) `getComputedStyle(el).borderInlineStartWidth` equals `'1px'` (the base `.kcard` hairline — proves the 3px bar is gone), (b) the overdue card contains a `span.kdue.is-overdue` and its computed `fontWeight` is `'600'`, (c) its computed `color` equals the computed color of an in-page probe element styled `color: var(--danger)` (append a probe div to body inside `page.evaluate`, compare `getComputedStyle(...).color` strings — this resolves the token deterministically). Update the file-header comment to describe the new contract.
    4. kanban-rtl.spec.ts: the spec's purpose is logical-property parity. Replace the border-right assertion (lines 22-27) with: under `dir=rtl`, the overdue card's `borderRightWidth === borderLeftWidth` (symmetric 1px — no leftover physical bias) AND `page.locator('.kcard.overdue .kdue.is-overdue')` count ≥ 1 (the red chip renders in RTL too; digits stay LTR via the existing LtrIsolate wrapper — do not modify it). Update the header comment.

    Discretion note (granted by CONTEXT): the OPTIONAL F16 priority-bars glyph is DEFERRED — CONTEXT marks it explicitly optional and delegates ship/defer; the red due chip alone carries overdue, and priority already renders as the High/Medium chip. Record the deferral in the SUMMARY.

    Tokens only (`var(--danger)`), logical properties only, no raw hex.

  </action>
  <acceptance_criteria>
    - `grep -c 'border-inline-start: 3px solid var(--danger)' frontend/src/pages/WorkBoard/board.css` returns 0
    - `grep -c '\.kdue\.is-overdue' frontend/src/pages/WorkBoard/board.css` returns 1, and the rule body is exactly `color: var(--danger); font-weight: 600;` (two declarations, no others)
    - `grep -n "is-overdue" frontend/src/pages/WorkBoard/KCard.tsx` shows the due span gated on `item.is_overdue`; `grep -c "'overdue'" frontend/src/pages/WorkBoard/KCard.tsx` still ≥ 1 (article class attachment intact)
    - `grep -c "kdue" frontend/tests/e2e/kanban-render.spec.ts` ≥ 1 and `grep -c "toBe('3px')" frontend/tests/e2e/kanban-render.spec.ts` returns 0
    - `grep -c "kdue" frontend/tests/e2e/kanban-rtl.spec.ts` ≥ 1
    - No raw hex and no Tailwind color literals introduced in any touched file
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && pnpm test --run src/pages/WorkBoard && pnpm type-check</automated>
  </verify>
  <done>Overdue cards render with the plain 1px card border and a red semibold mono due chip; both e2e specs assert the new contract; KCard unit tests (overdue class + "Overdue 3d" text) still pass unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: F21 — stage status glyph on the BoardColumn header</name>
  <files>frontend/src/pages/WorkBoard/BoardColumn.tsx, frontend/src/pages/WorkBoard/board.css</files>
  <read_first>.planning/phases/85-linear-taste-refinements-f16-f21/85-PATTERNS.md (TASTE-06 section), frontend/DESIGN.md (status/semantic tokens), frontend/src/pages/WorkBoard/BoardColumn.tsx, frontend/src/components/signature-visuals/Donut.tsx, frontend/src/pages/WorkBoard/__tests__/BoardColumn.test.tsx</read_first>
  <action>
    Per D-85-06 (locked):

    1. BoardColumn.tsx: add a module-level `STAGE_GLYPHS: Record<WorkflowStage, ReactElement>` map (or a `stageGlyph(stage)` helper with an explicit return type, per the repo's explicit-return-type lint rule). Each glyph is a 14px inline `<svg>` (`width={14} height={14} viewBox="0 0 14 14"`, `className="col-glyph"`, `aria-hidden="true"`, `focusable="false"` — decorative; the adjacent `<h3>` already names the stage, so role="img" would double-announce). Shapes, borrowing Donut.tsx's plain-circle/strokeDasharray technique, tokens only:
       - `todo`: empty ring — circle r=5, `fill="none"`, `stroke="var(--ink-faint)"`, strokeWidth 1.5
       - `in_progress`: filled amber dot — circle r=5, `fill="var(--warn)"`
       - `review`: dashed ring — circle r=5, `fill="none"`, `stroke="var(--ink-faint)"`, strokeWidth 1.5, `strokeDasharray="2 2"`
       - `done`: green check — a short two-segment check `<path>` with `stroke="var(--ok)"`, `fill="none"`, strokeWidth 2, round linecap
       - `cancelled`: reuse the todo ring (type-totality only — WorkBoard filters cancelled out, it never renders)
       Render the glyph as the LITERAL FIRST child of `<header className="col-head">` (line 62), before `<h3 id={titleId}>`, keyed off the existing `stage` prop. No new props, no new imports beyond `ReactElement` if not already present.
    2. board.css: add a one-declaration rule `.col-glyph { flex-shrink: 0; }` near the `.col-head` block so the glyph never compresses at narrow column widths.

    RTL: none needed — a single inline SVG in a flex row has no directional properties (D-85-07).

  </action>
  <acceptance_criteria>
    - `grep -n "col-glyph" frontend/src/pages/WorkBoard/BoardColumn.tsx` shows the svg rendered inside `.col-head` before the `<h3>`
    - `grep -c 'aria-hidden="true"' frontend/src/pages/WorkBoard/BoardColumn.tsx` ≥ 1 on the glyph svg
    - All four rendered stages map to distinct shapes; only `var(--ink-faint)`, `var(--warn)`, `var(--ok)` appear as glyph colors — `grep -E '#[0-9a-fA-F]{3,8}' frontend/src/pages/WorkBoard/BoardColumn.tsx` returns no color hex introduced by this change
    - `grep -c '\.col-glyph' frontend/src/pages/WorkBoard/board.css` returns 1
    - BoardColumn.test.tsx passes UNMODIFIED (role queries for heading/button are unaffected by an aria-hidden svg)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && pnpm test --run src/pages/WorkBoard/__tests__/BoardColumn.test.tsx && pnpm type-check && pnpm build</automated>
  </verify>
  <done>Each of the four board columns shows its stage glyph at inline-start of the header (right edge in RTL, automatically); build and unit tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human render-parity sign-off — WorkBoard (EN/AR × dark/light)</name>
  <what-built>F16: overdue edge bar removed, overdue now a red semibold due chip; F21: stage status glyphs on the four column headers. Optional F16 priority-bars glyph deferred (CONTEXT marked it optional).</what-built>
  <how-to-verify>
    1. `pnpm --dir frontend dev`, open http://localhost:5173/kanban (EN, dark).
    2. Confirm: no card shows a red inline-start edge bar; overdue cards show a red "Overdue Nd" mono chip; the board no longer reads as a wall of red.
    3. Confirm the four column headers show: empty ring (To Do), amber dot (In Progress), dashed ring (Review), green check (Done), sitting before the column name.
    4. Toggle light mode (topbar theme control): both the red chip and the glyphs stay legible.
    5. Open http://localhost:5173/kanban?lng=ar (AR/RTL): glyphs sit at the RIGHT edge of each header; red due chips render with LTR digits; no horizontal-flip artifacts. Check at 1024px and 1400px widths.
  </how-to-verify>
  <action>Start the dev server if not running, present what was built and the how-to-verify steps to the user, and wait for explicit sign-off. Do not self-certify visuals; do not proceed past this gate on your own judgment.</action>
  <verify>
    <human-check>User walks the how-to-verify steps (EN/AR, dark/light) and confirms render parity</human-check>
  </verify>
  <done>User replied "approved" (or raised issues that were addressed and re-verified)</done>
  <resume-signal>Type "approved" or describe issues (e.g. glyph color/shape adjustments)</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                               |
| -------- | ------------------------------------------------------------------------- |
| none new | Visual-only CSS/class/inline-SVG changes on an already-authenticated page |

## STRIDE Threat Register

| Threat ID | Category          | Component                               | Disposition | Mitigation Plan                                                                                                                                                                                   |
| --------- | ----------------- | --------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-85-01   | (none applicable) | board.css / KCard.tsx / BoardColumn.tsx | accept      | No new user input, no data flow, no auth/authz surface, no injection vector (SVGs are static JSX, no dangerouslySetInnerHTML), no package installs. Zero new attack surface — explicitly trivial. |

</threat_model>

<verification>
- `cd frontend && pnpm type-check` exits 0
- `cd frontend && pnpm test --run src/pages/WorkBoard` green (KCard, BoardColumn, WorkBoard, BoardToolbar suites)
- `cd frontend && pnpm build` succeeds
- `grep -c 'border-inline-start: 3px solid var(--danger)' frontend/src/pages/WorkBoard/board.css` returns 0; `grep -c '\.kdue\.is-overdue' frontend/src/pages/WorkBoard/board.css` returns 1
- Human render-parity sign-off (Task 3) received — visuals are NOT self-certified
</verification>

<success_criteria>

- TASTE-01: overdue carried on the red due chip, edge bar gone, `.kcard.overdue` class attachment intact, e2e specs updated to the new contract
- TASTE-06: four stage glyphs render token-colored before each column name, RTL-neutral
- Zero regressions in the WorkBoard unit suites; dark/light and EN/AR verified by a human
  </success_criteria>

<output>
Create `.planning/phases/85-linear-taste-refinements-f16-f21/85-01-SUMMARY.md` when done (record the deferred optional F16 priority glyph explicitly).
</output>
