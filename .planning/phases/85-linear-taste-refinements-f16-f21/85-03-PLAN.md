---
phase: 85-linear-taste-refinements-f16-f21
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.css
  - frontend/src/components/layout/Sidebar.tsx
autonomous: false
requirements: [TASTE-02]

must_haves:
  truths:
    - 'Satisfies D-85-02: the settings sub-nav active item is a neutral gray pill (var(--surface-raised) fill, var(--ink) text) with the 2px accent ::before stripe kept verbatim — accent reserved for primary buttons and on-toggles'
    - "Satisfies D-85-07: the ::before stripe's inset-inline-start anchoring is untouched; no physical-direction properties introduced; light-mode legibility of the neutral pill is human-verified, not assumed"
    - 'The main app sidebar active pill remains neutral and visibly distinct from its background in BOTH color modes (already true via its color-mix fill — verified divergence, not re-broken)'
  artifacts:
    - path: 'frontend/src/index.css'
      provides: '.settings-nav.active neutralized fill; ::before accent stripe unchanged'
      contains: '.settings-nav.active'
  key_links:
    - from: 'frontend/src/index.css .settings-nav.active'
      to: 'frontend/DESIGN.md surface ladder'
      via: 'var(--surface-raised) + var(--ink) replace var(--accent-soft) + var(--accent-ink)'
      pattern: '--surface-raised'
---

<objective>
Land TASTE-02/F17 — neutralize the active-nav fill so color = meaning: accent (indigo) is reserved for primary buttons and on-toggles; nav selection is a subtle neutral pill plus the 2px accent stripe.

Purpose: `.settings-nav.active` currently fills with `var(--accent-soft)` — decorative accent. The main `Sidebar.tsx` active item, per the verified pattern map, is ALREADY neutral (a `color-mix(sidebar-ink 10%)` gray tint with a correct 2px accent `::before` stripe) — CONTEXT's assumption that it needs an accent→neutral swap does not reproduce in the code.

Output: one surgical `index.css` edit; a documented, checkpoint-gated decision on the optional `Sidebar.tsx` token-alignment (default: leave it — swapping to `var(--surface-raised)` would make the pill near-invisible on the light sidebar per DESIGN.md token math: light `--surface-raised` #f6f7f7 vs light sidebar bg #f5f6f6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/85-linear-taste-refinements-f16-f21/85-CONTEXT.md
@.planning/phases/85-linear-taste-refinements-f16-f21/85-PATTERNS.md
@frontend/DESIGN.md
@frontend/src/index.css
@frontend/src/components/layout/Sidebar.tsx

<interfaces>
Facts verified against the live code (2026-07-05):

- `index.css:795-807` is the exact edit site:
  `.settings-nav.active { background: var(--accent-soft); color: var(--accent-ink); }` followed by
  `.settings-nav.active::before { ... inset-inline-start: 0; ... width: 2px; background: var(--accent); ... }`.
  The `::before` block is already logical-property correct and MUST stay byte-identical.
- `Sidebar.tsx:161-168` active item is ALREADY neutral: `bg-[color-mix(in_srgb,var(--sidebar-ink)_10%,transparent)]` + `before:bg-[var(--accent)] before:w-0.5 before:start-0` stripe. PATTERNS flags the CONTEXT-suggested swap to `var(--surface-raised)` as a checkpoint-worthy call, and DESIGN.md token math shows that swap would fail light-mode visibility (1 rgb-unit off the light sidebar bg). The color-mix tint, being derived from sidebar-ink, guarantees contrast in both modes.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: F17 — neutralize .settings-nav.active fill, keep the accent stripe</name>
  <files>frontend/src/index.css</files>
  <read_first>.planning/phases/85-linear-taste-refinements-f16-f21/85-PATTERNS.md (TASTE-02 section), frontend/DESIGN.md (surface ladder + light-derived table), frontend/src/index.css (lines 795-807), frontend/src/components/layout/Sidebar.tsx (lines 161-168)</read_first>
  <action>
    Per D-85-02 (locked) as verified by PATTERNS:

    1. In `index.css` at the `.settings-nav.active` rule (line ~795): change `background: var(--accent-soft)` to `background: var(--surface-raised)` and `color: var(--accent-ink)` to `color: var(--ink)`. Exactly those two declarations; nothing else in the rule.
    2. Leave the `.settings-nav.active::before` block (lines ~800-807) byte-identical — the 2px `var(--accent)` stripe anchored via `inset-inline-start: 0` is the kept accent signal (D-85-07).
    3. `Sidebar.tsx`: make NO edit by default. The active fill is already neutral (`color-mix(sidebar-ink 10%)`) with the correct accent stripe — per the planning constraint this swap is optional discretion, and the DESIGN.md token math shows `var(--surface-raised)` would be near-invisible against the light sidebar (#f6f7f7 on #f5f6f6). Using the existing color-mix per verified divergence (CONTEXT assumed an accent fill that does not exist in this file); the question is surfaced to the user at Task 2. If the user asks for the swap at the checkpoint, apply it then (single className token change at Sidebar.tsx:167) and re-verify light mode.

  </action>
  <acceptance_criteria>
    - `grep -A2 '^\.settings-nav\.active {' frontend/src/index.css` shows `background: var(--surface-raised);` and `color: var(--ink);` — and `grep -c 'accent-soft' <(grep -A2 '^\.settings-nav\.active {' frontend/src/index.css)` returns 0
    - The `.settings-nav.active::before` block is unchanged: still contains `inset-inline-start: 0`, `width: 2px`, `background: var(--accent)` (verify via `git diff frontend/src/index.css` showing only the two-declaration change)
    - `git diff --quiet -- frontend/src/components/layout/Sidebar.tsx` exits 0 (untouched by default; only changes if the user asks at Task 2)
    - No raw hex, no Tailwind color literals introduced
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && pnpm type-check && pnpm build</automated>
  </verify>
  <done>Settings sub-nav active item renders as a neutral gray pill with the accent stripe; main sidebar behavior unchanged pending the user's checkpoint call.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human render-parity sign-off — active-nav pills (dark/light × EN/AR) + sidebar discretion call</name>
  <what-built>`.settings-nav.active` fill neutralized to `var(--surface-raised)`/`var(--ink)`, 2px accent stripe kept. Main sidebar active pill left as-is: it is already a neutral gray tint (color-mix of sidebar-ink) with the correct accent stripe, and swapping it to `var(--surface-raised)` would make it nearly invisible in light mode (token values 1 unit apart). Your call whether to swap anyway for token-ladder purity.</what-built>
  <how-to-verify>
    1. `pnpm --dir frontend dev`, open http://localhost:5173/settings (EN, dark): the active section in the settings sub-nav is a subtle gray pill with a thin indigo stripe at its inline-start edge — no indigo fill.
    2. Toggle light mode: the gray pill must still be distinguishable from the nav background (this is the flagged light-mode risk — look closely).
    3. Check the MAIN app sidebar (any non-settings route) in dark AND light: the active item's existing gray pill + indigo stripe. Decide: keep the current color-mix fill (recommended — visible in both modes) or swap to var(--surface-raised) (ladder-pure but near-invisible in light mode).
    4. http://localhost:5173/settings?lng=ar: the stripe sits on the RIGHT edge of the active pill (inline-start in RTL).
  </how-to-verify>
  <action>Start the dev server if not running, present what was built and the how-to-verify steps to the user, and wait for explicit sign-off. Do not self-certify visuals; do not proceed past this gate on your own judgment.</action>
  <verify>
    <human-check>User walks the how-to-verify steps (EN/AR, dark/light) and confirms render parity</human-check>
  </verify>
  <done>User replied "approved" (or raised issues that were addressed and re-verified)</done>
  <resume-signal>Type "approved" (keep sidebar as-is) or "swap sidebar" (apply the var(--surface-raised) swap), or describe issues</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                        |
| -------- | -------------------------------------------------- |
| none new | Two CSS declaration swaps on an authenticated page |

## STRIDE Threat Register

| Threat ID | Category          | Component               | Disposition | Mitigation Plan                                                                                                                                                          |
| --------- | ----------------- | ----------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-85-03   | (none applicable) | index.css / Sidebar.tsx | accept      | Pure CSS token substitution. No user input, no data flow, no auth/authz surface, no injection vector, no package installs. Zero new attack surface — explicitly trivial. |

</threat_model>

<verification>
- `cd frontend && pnpm type-check` exits 0; `pnpm build` succeeds
- `git diff frontend/src/index.css` shows exactly two changed declarations inside `.settings-nav.active`; the `::before` block untouched
- Human render-parity sign-off (Task 2) received, including the explicit light-mode pill legibility check and the sidebar discretion call — visuals are NOT self-certified
</verification>

<success_criteria>

- TASTE-02: settings-nav active = neutral pill (`--surface-raised`/`--ink`) + kept 2px accent stripe; accent no longer used as nav decoration
- Main sidebar active state remains neutral AND legible in both color modes (either untouched color-mix or user-elected swap, verified)
- Zero regressions on non-settings routes
  </success_criteria>

<output>
Create `.planning/phases/85-linear-taste-refinements-f16-f21/85-03-SUMMARY.md` when done (record the user's sidebar-fill decision and the light-mode verification outcome).
</output>
