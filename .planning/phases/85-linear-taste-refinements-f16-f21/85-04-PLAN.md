---
phase: 85-linear-taste-refinements-f16-f21
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/styles/list-pages.css
  - frontend/src/components/settings/sections/ProfileSettingsSection.tsx
  - frontend/src/components/settings/sections/GeneralSettingsSection.tsx
  - frontend/src/components/settings/sections/SecuritySettingsSection.tsx
  - frontend/src/components/settings/sections/AccessibilitySettingsSection.tsx
  - frontend/src/components/settings/sections/AppearanceSettingsSection.tsx
autonomous: false
requirements: [TASTE-04]

must_haves:
  truths:
    - 'Satisfies D-85-04: settings form-field labels render sentence-case (DISPLAY NAME → Display name) via a scoped .label-field modifier — the global .label recipe, .t-label, and labelVariants stay byte-unchanged'
    - 'Satisfies D-85-07: uppercase meta labels elsewhere (KPI strips, table headers, topbar) keep their uppercase .label styling; Arabic rendering (no letter case, Tajawal weight/spacing) is unaffected'
    - 'A user reading a settings form sees calm 13px sentence-case field labels instead of shouting 11px uppercase'
  artifacts:
    - path: 'frontend/src/styles/list-pages.css'
      provides: '.label-field modifier (incl. a .dir-linear compound selector so it wins the cascade over .dir-linear .label)'
      contains: '.label-field'
    - path: 'frontend/src/components/settings/sections/ProfileSettingsSection.tsx'
      provides: 'label-field appended to every form-field <Label> className'
      contains: 'label-field'
  key_links:
    - from: 'frontend/src/components/settings/sections/*.tsx'
      to: 'frontend/src/styles/list-pages.css'
      via: 'label-field className riding the existing cn(labelVariants(), className) merge in components/ui/label.tsx (no code change there)'
      pattern: 'label-field'
---

<objective>
Land TASTE-04/F19 — sentence-case form-field labels on the settings forms, scoped via a new `.label-field` modifier class. The shared `.label` recipe and `labelVariants` are untouched: they also style legitimate uppercase meta labels across the app (KPI strips, topbar, activity sections).

Purpose: UPPERCASE form-field labels violate the project's own voice rule (uppercase is reserved for classification ribbons, mono labels, table-column headers). EN-only visual change — Arabic has no letter case.

Output: one new CSS modifier in `list-pages.css` + `label-field` appended to the field `<Label>` classNames in the five settings sections that import the Label primitive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/85-linear-taste-refinements-f16-f21/85-CONTEXT.md
@.planning/phases/85-linear-taste-refinements-f16-f21/85-PATTERNS.md
@frontend/DESIGN.md
@frontend/src/styles/list-pages.css
@frontend/src/components/ui/label.tsx
@frontend/src/components/settings/sections/ProfileSettingsSection.tsx

<interfaces>
Facts verified against the live code (2026-07-05):

- `list-pages.css:552-567`: `.label { font-size: 11px; font-weight: 600; color: var(--ink-faint); text-transform: uppercase; letter-spacing: 0.08em; }` followed by `.dir-linear .label { ... }` (same values, Linear-direction override).
- CASCADE TRAP: `.dir-linear .label` has specificity (0,2,0) and `.dir-linear` IS the active direction class — a bare `.label-field` (0,1,0) would LOSE to it for every property both set. The new rule must include a `.dir-linear .label.label-field` compound selector AND be placed after the `.dir-linear .label` block.
- `components/ui/label.tsx:7-9`: `labelVariants = cva('label leading-none ...')`; the component merges `cn(labelVariants(), className)` — an appended `label-field` className flows through with NO change to label.tsx. Do not edit label.tsx or labelVariants.
- `.t-label` (`index.css:489-495`) has ZERO settings-form consumers (grep-verified in PATTERNS) — leave it alone entirely.
- Consumer shape (`ProfileSettingsSection.tsx:177-179` and same shape at 203, 212, 232, 252, 274): `<Label htmlFor="display_name" className="text-start block">{t('profile.displayName')}</Label>`. The other four Label-importing sections (GeneralSettingsSection, SecuritySettingsSection, AccessibilitySettingsSection, AppearanceSettingsSection) follow analogous shapes. NotificationsSettingsSection and DataPrivacySettingsSection do not import the Label primitive — out of scope.
- Modifier-class analog: the `.chip`/`.chip-danger`/`.chip-warn` family in the same stylesheet (base + suffix modifier, base never edited).
- Tajawal-forcing rules key off `.label` at `index.css:520/:541` — untouched by a suffix class, so AR weight/spacing is naturally preserved.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: F19 — add the scoped .label-field modifier to list-pages.css</name>
  <files>frontend/src/styles/list-pages.css</files>
  <read_first>.planning/phases/85-linear-taste-refinements-f16-f21/85-PATTERNS.md (TASTE-04 section), frontend/DESIGN.md (ink-mute token, type scale), frontend/src/styles/list-pages.css (lines 545-570), frontend/src/components/ui/label.tsx</read_first>
  <action>
    Per D-85-04 (locked): immediately AFTER the existing `.dir-linear .label` block (ends ~line 567), add one new rule with the selector list `.label.label-field, .dir-linear .label.label-field` and the body: `text-transform: none; letter-spacing: normal; font-size: 13px; font-weight: 500; color: var(--ink-mute);`. The compound `.dir-linear .label.label-field` (0,3,0) + source-order placement is REQUIRED — a bare `.label-field` would lose the cascade to `.dir-linear .label` (0,2,0) under the active `dir-linear` root class. Mirror the sibling `.chip`/`.chip-*` modifier idiom: the base `.label` rule and the `.dir-linear .label` override stay byte-identical. Do not touch `.t-label` in index.css and do not edit `components/ui/label.tsx`/`labelVariants`.
  </action>
  <acceptance_criteria>
    - `grep -c '\.label\.label-field' frontend/src/styles/list-pages.css` returns 1, with `text-transform: none` in its body, positioned AFTER the `.dir-linear .label` block
    - The base `.label` block still contains `text-transform: uppercase` and `font-size: 11px` (byte-unchanged): `grep -A5 '^\.label {' frontend/src/styles/list-pages.css | grep -c 'text-transform: uppercase'` returns 1
    - `git diff --quiet -- frontend/src/components/ui/label.tsx` exits 0 and `git diff --quiet -- frontend/src/index.css` exits 0 (neither file touched by this plan)
    - Only `var(--ink-mute)` used for color — no raw hex
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && pnpm build</automated>
  </verify>
  <done>.label-field exists as a cascade-winning suffix modifier; every existing .label consumer is visually byte-identical until the class is applied.</done>
</task>

<task type="auto">
  <name>Task 2: F19 — apply label-field to the five settings sections' field labels</name>
  <files>frontend/src/components/settings/sections/ProfileSettingsSection.tsx, frontend/src/components/settings/sections/GeneralSettingsSection.tsx, frontend/src/components/settings/sections/SecuritySettingsSection.tsx, frontend/src/components/settings/sections/AccessibilitySettingsSection.tsx, frontend/src/components/settings/sections/AppearanceSettingsSection.tsx</files>
  <read_first>.planning/phases/85-linear-taste-refinements-f16-f21/85-PATTERNS.md (TASTE-04 section), each of the five section files (grep `<Label` first to enumerate every call site per file)</read_first>
  <action>
    Per D-85-04 (locked): in each of the five files, append `label-field` to the `className` prop of every FORM-FIELD `<Label>` (labels bound to an input/select/switch via `htmlFor` or wrapping a field control) — e.g. `className="text-start block"` becomes `className="label-field text-start block"`. Known Profile call sites: lines 177, 203, 212, 232, 252, 274; enumerate the other four files by grepping `<Label` — every field-label call site in these five files gets the class (do not skip any; do not add it to non-Label uppercase meta text). No other prop, markup, or i18n change. This is an EN-only visual change; AR needs no edits (no letter case; Tajawal rules key off `.label`, which is untouched).
  </action>
  <acceptance_criteria>
    - `grep -rc 'label-field' frontend/src/components/settings/sections/ProfileSettingsSection.tsx` equals the file's `<Label` count (every field label covered); same per-file equality for the other four sections
    - `grep -rn 'label-field' frontend/src/components/settings/sections/ | wc -l` ≥ 6 (Profile alone has 6 call sites)
    - `git diff --stat` for this task touches ONLY the five section files — no changes under components/ui/, no changes to Notifications/DataPrivacy sections
    - Rendered check: /settings profile fields show "Display name"-style sentence case at 13px/500 var(--ink-mute); dashboard KPI labels remain uppercase
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && pnpm type-check && pnpm test --run src/components/settings && pnpm build</automated>
  </verify>
  <done>All field labels across the five settings sections carry label-field; every uppercase .label consumer elsewhere is unaffected.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human render-parity sign-off — form labels (EN/AR × dark/light)</name>
  <what-built>Settings form-field labels now render sentence-case 13px/500 in var(--ink-mute) via a scoped .label-field modifier; the global uppercase .label recipe is untouched.</what-built>
  <how-to-verify>
    1. `pnpm --dir frontend dev`, open http://localhost:5173/settings (EN, dark): Profile/General/Security/Accessibility/Appearance field labels read "Display name", not "DISPLAY NAME" — 13px, medium, muted.
    2. Spot-check that uppercase survives where it should: dashboard KPI strip labels and any table column headers are still uppercase.
    3. Toggle light mode: label contrast holds.
    4. http://localhost:5173/settings?lng=ar: Arabic labels look identical to before this phase (no weight/spacing shift — Tajawal untouched).
  </how-to-verify>
  <action>Start the dev server if not running, present what was built and the how-to-verify steps to the user, and wait for explicit sign-off. Do not self-certify visuals; do not proceed past this gate on your own judgment.</action>
  <verify>
    <human-check>User walks the how-to-verify steps (EN/AR, dark/light) and confirms render parity</human-check>
  </verify>
  <done>User replied "approved" (or raised issues that were addressed and re-verified)</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                   |
| -------- | --------------------------------------------- |
| none new | CSS modifier class + className string appends |

## STRIDE Threat Register

| Threat ID | Category          | Component                                    | Disposition | Mitigation Plan                                                                                                                                                          |
| --------- | ----------------- | -------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-85-04   | (none applicable) | list-pages.css / settings section components | accept      | Static class additions only. No user input, no data flow, no auth/authz surface, no injection vector, no package installs. Zero new attack surface — explicitly trivial. |

</threat_model>

<verification>
- `cd frontend && pnpm type-check` exits 0; `pnpm build` succeeds; `pnpm test --run src/components/settings` green
- `.label` base rule and `components/ui/label.tsx` byte-unchanged (git diff gates in Task 1)
- Per-file `label-field` count equals `<Label` field call-site count in each of the five sections
- Human render-parity sign-off (Task 3) received — visuals are NOT self-certified
</verification>

<success_criteria>

- TASTE-04: settings form-field labels sentence-case via scoped modifier; `.t-label`, `.label`, and `labelVariants` untouched; uppercase meta labels app-wide unaffected; AR rendering identical
  </success_criteria>

<output>
Create `.planning/phases/85-linear-taste-refinements-f16-f21/85-04-SUMMARY.md` when done.
</output>
