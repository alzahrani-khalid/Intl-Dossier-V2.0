---
phase: 96-real-numbers
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/96-extension-rows.spec.ts # NEW
  - .planning/phases/96-real-numbers/96-10-CLASSIFICATION.md # NEW — the classification product pin (revision 1)
  - frontend/src/hooks/useCountries.ts # modify ONLY if classified drop-capable (a no-op is recorded)
  - frontend/src/hooks/useOrganizations.ts # modify ONLY if classified drop-capable (a no-op is recorded)
autonomous: true
requirements: [COUNT-02]
success_criterion: '5 (first half) — a dossier without an extension row appears in both its type list and the hub count (persons 16/16, engagements 5/5)'
truths:
  - "Decisions covered — D-07: no type-list surface may drop a dossier the hub counts — the canonical list path (dossiers-list → list_dossiers_optimized → LEFT-joined dossier_list_mv) is verified sound, every type-list read path is mechanically classified drop-capable-or-not, any drop-capable path found is fixed to left-join semantics, and SC5's fixture (a dossier WITHOUT an extension row) is synthetic, namespaced and cleaned with CHECK constraints verified via pg_constraint before ANY insert; aa_commitments has NO FK to dossiers so no oracle joins through one; D-13: staff_profiles seeding stays OUT of scope — the research tripwire passed (zero criterion-surface reads) and this plan does not touch it"
must_haves:
  truths:
    - 'A namespaced fixture dossier with NO extension row renders in its type list AND is included in the hub count, same-clock'
    - 'Every type-list read path for the 7 live dossier types is classified (drop-capable or not) with the classification recorded at a pinned artifact path the gate checks'
    - 'Absent extension fields render as absent — never fabricated placeholder values, never a dropped row'
    - 'The mv-freshness mechanism is identified and the oracle forces a refresh rather than trusting staleness luck'
  artifacts:
    - path: 'tests/e2e/96-extension-rows.spec.ts'
      provides: 'the SC5 fixture oracle — insert, refresh, observe list + count, clean'
      contains: '@covers COUNT-02'
    - path: '.planning/phases/96-real-numbers/96-10-CLASSIFICATION.md'
      provides: 'the 7-row type-list classification table with its literal population sentence — the task product the gate pins (no no-op satisfiability)'
      contains: 'POPULATION: the 7 type-list surfaces'
  key_links:
    - from: 'tests/e2e/96-extension-rows.spec.ts'
      to: 'dossier_list_mv'
      via: 'service-role fixture insert + refresh_dossier_list_mv_force + list DOM observation'
      pattern: 'refresh_dossier_list_mv'
---

<objective>
Close COUNT-02 (success criterion 5, first half). Research re-derived the live gaps — person
16/15, engagement 5/3, and TWO NEW beyond the register: organization 5/2, topic 2/1 — and
established that the CANONICAL list path is SOUND today (dossiers-list edge fn →
list_dossiers_optimized → dossier_list_mv, LEFT-joined, behaviourally confirmed 16/16
persons). Zero `!inner` embeds exist in frontend/src (instrument-tested). The residual risk
is (a) any list surface that reads extension tables FIRST, and (b) the mv-staleness window
(refresh machinery exists in pg_proc; NO pg_cron job references it).

**Close shape:** VERIFY the paths mechanically + PROVE with a fixture — fix only what the
classification finds drop-capable. The register's counts are FLOORS re-derived at execution,
never trusted from entry text. The classification is a PRODUCT, not prose: it lands at
`.planning/phases/96-real-numbers/96-10-CLASSIFICATION.md` and the gate pins it (revision 1 —
the gate is no longer satisfiable by a no-op).

**Fixture preconditions (verified, Derivation 4):** `dossiers_type_check` has SEVEN values —
`elected_official` is NOT a dossier type (person subtype); NOT-NULL-without-default columns
are exactly type, name*en, name_ar; name CHECKs require non-empty. Minimal insert:
(type, name_en, name_ar). The validate*\*\_type triggers live on EXTENSION tables and fire
nothing for a dossier without an extension row.

**Forbidden shape, named (condition 8):** a fallback that renders an empty list over rows the
server sent; fabricated placeholder values for absent extension fields.

Purpose: the hub and the lists count the same reality.
Output: the classification artifact, any left-join fixes it demands, the fixture oracle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-real-numbers/96-RESEARCH.md (§Derivation 4 — the gaps, the sound canonical path, the fixture preconditions, the mv machinery; §Pitfall 6 — the mv freshness trap)
@.planning/phases/96-real-numbers/96-PATTERNS.md (§1 — the spec family; the probe-report-rls.mjs fixture posture)

<interfaces>
Verified by research 2026-08-17:
- Canonical path: GET /functions/v1/dossiers-list?type=person&limit=100 → 16 rows / total 16
  (LEFT-joined mv, complete). The classification's job is the OTHER surfaces: hooks/pages
  that read extension tables first (candidates: frontend/src/hooks/useCountries.ts:132,
  useOrganizations.ts:106 — both read engagement_dossiers for count-merges; a count-merge
  that yields 0 for a missing extension row is ABSENT-as-absent, NOT a drop — classify,
  don't reflexively rewrite).
- mv refresh machinery (pg_proc): refresh_dossier_list_mv, queue_dossier_list_mv_refresh,
  refresh_dossier_list_mv_force; no pg_cron reference — the oracle calls the force variant
  via service role after the fixture insert (Pitfall 6).
- Fixture cleanup: DELETE the dossier row (no extension row exists to cascade) + force
  refresh again; run in the spec's finally.
- C9b consumers (mock-vs-real): the dossier-list surfaces have shipped specs
  (frontend/tests/e2e/create-view-dossier.spec.ts and dossier-* family — REAL): this plan
  only ADDS a row transiently and (at most) changes hook read semantics to non-dropping —
  if any hook edit lands, run create-view-dossier.spec.ts once, record colour, repair
  in-task if red for a correct change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classify every type-list read path; fix only what can drop</name>
  <files>frontend/src/hooks/useCountries.ts, frontend/src/hooks/useOrganizations.ts, .planning/phases/96-real-numbers/96-10-CLASSIFICATION.md</files>
  <read_first>frontend/src/hooks/useCountries.ts and useOrganizations.ts in full (the extension merges); the route/page component for each of the 7 type lists (enumerate them from frontend/src/routes/_protected — countries, organizations, forums, engagements, topics, working-groups, persons — and pin which data hook each mounts); 96-RESEARCH.md Derivation 4</read_first>
  <action>
    (1) MECHANICAL ENUMERATION (the population): for each of the 7 live dossier types, pin
    the list surface's data path (which hook/edge fn/query renders the rows) by reading the
    route file and its hook — record a 7-row table: type → surface file → data mechanism →
    classification (drop-capable | absent-as-absent | canonical-mv). A path is DROP-CAPABLE
    only if a missing extension row excludes the DOSSIER ROW from the rendered list (an
    extension-first SELECT, an inner-join semantic, or a merge that filters unmatched rows).
    A count-merge yielding 0/absent fields is ABSENT-AS-ABSENT — correct, untouched.
    Instrument-test the enumeration: the persons list MUST classify canonical-mv (the
    behaviourally-confirmed sound path) — if it does not, the instrument is wrong, stop.
    (2) FIX only classified drop-capable paths: rewrite to dossier-first with left-join
    semantics (fetch dossiers, attach extension fields where present — the useCountries
    extension-merge shape is the analog), never introducing a .select('…extension!inner(…)')
    embed (the repo has zero — do not add the drop mechanism while fixing it). If ZERO paths
    classify drop-capable, both hook files are recorded no-ops (the frontmatter names them
    so ownership is explicit; a no-op is a recorded outcome, not silence). (3) WRITE THE
    PRODUCT (revision 1 — the gate pins it): the 7-row table lands at
    `.planning/phases/96-real-numbers/96-10-CLASSIFICATION.md` as a markdown table whose
    FIRST column is the dossier type value (one row each for country, organization, forum,
    engagement, topic, working_group, person), preceded by the literal population sentence
    `POPULATION: the 7 type-list surfaces` and the falls-outside list: relationship tabs and
    detail views that read extension tables BY DESIGN (their population is extension rows,
    stated), the dossiers hub cards (count the dossiers table directly), elected-officials
    surfaces (person subtype, not a dossier type). The SUMMARY restates and cites the
    artifact. (4) If any hook edit landed: run
    frontend/tests/e2e/create-view-dossier.spec.ts once, record colour, repair in-task if
    red for a correct change.
  </action>
  <verify>
    <automated>test -f .planning/phases/96-real-numbers/96-10-CLASSIFICATION.md && command grep -q 'POPULATION: the 7 type-list surfaces' .planning/phases/96-real-numbers/96-10-CLASSIFICATION.md && test "$(command grep -Ec '^\| (country|organization|forum|engagement|topic|working_group|person) ' .planning/phases/96-real-numbers/96-10-CLASSIFICATION.md)" -eq 7 && test -d frontend/src && command grep -rql --include='*.ts' --include='*.tsx' 'useQuery' frontend/src >/dev/null && test "$(command grep -rl --include='*.ts' --include='*.tsx' -- '!inner' frontend/src | wc -l | tr -d ' ')" -eq 0 && cd frontend && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>The classification artifact exists at its pinned path carrying the literal population sentence and exactly 7 type-keyed table rows (the task's PRODUCT — the gate can no longer pass on a no-op tree); zero !inner embeds exist in frontend/src *.ts/*.tsx SOURCE files after any fix (the gate's population, stated: TypeScript source only — the unscoped variant was observed at authoring matching stale code snapshots inside the git-ignored frontend/src/.understand-anything/ analysis dump, a population bug caught by the drill itself; the useQuery positive control in the same chain proves the scoped grep instrument sees frontend/src — C5-compliant, the zero is believed only past it); type-check clean; persons classifies canonical-mv (the instrument test). GATE DRILL (re-drilled at revision 1 — three artifact arms prepended; an edited gate is a new gate): RED now NATURAL and observed on the real tree 2026-08-17 (exit 1 — the classification artifact does not exist, the subject's own reason; this replaces the constructed-red-only posture and cures the no-op satisfiability). GREEN: the three new arms constructed in scratch (synthetic classification file — observed exit 0; both counter-directions also drilled: a 6-row table failed the -eq 7 arm exit 1, a missing population sentence failed the grep arm exit 1). The !inner scan + useQuery control + type-check arms were observed green on the real tree at original authoring and are unchanged; full-chain green re-drilled at execution once the artifact lands.</acceptance_criteria>
  <done>Every list path is classified with the population stated in a gate-pinned artifact; nothing drop-capable survives; nothing sound was rewritten.</done>
</task>

<task type="auto">
  <name>Task 2: The SC5 fixture oracle — 96-extension-rows.spec.ts</name>
  <files>tests/e2e/96-extension-rows.spec.ts</files>
  <read_first>tests/e2e/95-sandbox-error.spec.ts (family shape); scripts/probe-report-rls.mjs:1-80 (fixture posture: namespaced ids printed, cleanup in finally, exit-labelled); 96-RESEARCH.md Derivation 4 (the verified CHECK list) and Pitfall 6</read_first>
  <action>
    Write exactly 2 tests, header `// @covers COUNT-02 (criterion 5)` plus the
    population-definition block. Setup (service-role client from SUPABASE_SERVICE_ROLE_KEY,
    the cleanup-helper precedent, never echoed): re-verify the CHECK constraints live before
    inserting (query pg_constraint for dossiers_type_check via a service-role rpc/select —
    if the 7-value set changed, fail with a labelled message rather than inserting); insert
    ONE fixture dossier (type 'person', name_en 'P96 COUNT02 FIXTURE', name_ar 'تثبيت P96')
    with NO extension row; call refresh_dossier_list_mv_force via service role (Pitfall 6 —
    never trust staleness luck); cleanup in finally: delete the row by returned id (printed),
    force-refresh again. Test 1 (list + count agree, same-clock): goto the persons list
    page; in ONE DOM snapshot assert the fixture row renders AND the page's total/count
    display includes it; separately run the one-statement SQL batch via service role:
    SELECT (count of dossiers type=person) AS hub, (count of mv rows type=person) AS list —
    assert hub == list WITH the fixture present. Test 2 (absent renders as absent): the
    fixture row's cells render without fabricated values — assert its row text does not
    contain 'undefined', 'NaN', or 'null' literals, and the row is present with its real
    name. Never assert emptiness as success; a failed insert fails loudly with the printed
    reason.
  </action>
  <verify>
    <automated>test -f tests/e2e/96-extension-rows.spec.ts && test "$(pnpm exec playwright test tests/e2e/96-extension-rows.spec.ts --project=chromium-en --no-deps --list 2>/dev/null | command grep -c '›')" -eq 2 && pnpm exec playwright test tests/e2e/96-extension-rows.spec.ts --project=chromium-en --no-deps</automated>
  </verify>
  <acceptance_criteria>Spec exists first, lists exactly 2 tests with --no-deps, passes; the fixture is namespaced, CHECK-verified before insert, id-printed, and cleaned in finally; the hub==list batch is one statement (condition 7). GATE DRILL: RED observed on undone tree (exit 1 — test -f fails, subject absent: the spec is this task's product). GREEN UNPROVEN pre-execution — needs the dev server + service key + staging; drill at execution.</acceptance_criteria>
  <done>SC5's first half is a re-runnable behavioural proof with a synthetic, cleaned fixture.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                             |
| ------------------------- | ------------------------------------------------------- |
| service-role key in tests | fixture insert/cleanup bypasses RLS — key never echoed  |
| fixture data → staging    | synthetic namespaced row, transient, cleaned in finally |

## STRIDE Threat Register

| Threat ID | Category               | Component                | Disposition | Mitigation Plan                                                                       |
| --------- | ---------------------- | ------------------------ | ----------- | ------------------------------------------------------------------------------------- |
| T-96-24   | Tampering              | fixture insert           | mitigate    | CHECKs re-verified live pre-insert; single namespaced row; finally-cleanup + id print |
| T-96-25   | Information Disclosure | service-role key         | mitigate    | env-only (.env.test, gitignored), never logged — the cleanup-helper precedent         |
| T-96-26   | Spoofing (of data)     | fabricated absent fields | mitigate    | absent-as-absent assertion in Test 2 (forbidden shape named)                          |
| T-96-SC   | Tampering              | package installs         | accept      | zero installs this phase                                                              |

</threat_model>

<verification>
- Both gates green (behavioural halves at execution per drill labels; the amended Task 1
  gate carries a NATURAL red via its classification-artifact arms, drilled both directions
  at revision 1).
- POPULATION DEFINITION (D-15): the 7 type-list surfaces (table at the pinned
  96-10-CLASSIFICATION.md artifact, restated in the SUMMARY); falls outside —
  extension-by-design readers (stated per row), the hub cards, elected-officials surfaces,
  and the mv's refresh cadence beyond the forced-refresh oracle (identified + stated, owned
  by no criterion here).
- The register's 16/15, 5/3 (+ the new 5/2, 2/1) are FLOORS — re-derived at execution in the
  SUMMARY, never restated from the entry.
</verification>

<success_criteria>
Criterion 5's first half holds behaviourally: a dossier with no extension row is visible in
its list and counted by the hub, with every read path classified in a gate-pinned artifact
and the mv seam controlled.
</success_criteria>

<output>
Create `.planning/phases/96-real-numbers/96-10-SUMMARY.md` when done. It MUST record: the
7-row classification table (citing the 96-10-CLASSIFICATION.md artifact as its home), the
re-derived per-type gap counts, the no-op-or-fix outcome per named hook, the fixture id +
cleanup proof, and the hub==list batch result.
</output>
