---
phase: 96-real-numbers
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/trigsweep-classify.mjs # NEW — the behaviour-classifying instrument
  - .planning/phases/96-real-numbers/96-TRIGSWEEP-DRILL.md # NEW — the both-direction drill artifact (this plan is its ONLY writer)
autonomous: true
requirements: [TRIGSWEEP-01]
success_criterion: 'filed-finding close — TRIGSWEEP-01: a behaviour-derived BEFORE-trigger instrument, tested in both directions, with the residual hand-classified and every count filed as a FLOOR'
truths:
  - 'Decisions covered — D-11: the trigger population derives from BEHAVIOUR (every BEFORE ROW trigger that writes a NEW. column HOWEVER EXPRESSED), never one regex — the classifier is the union of ALL FOUR known forms plus non-plpgsql-as-suspect, the residual is LISTED and hand-classified as part of the deliverable, the instrument is control-tested in BOTH directions (a synthetic writer it must catch, a synthetic non-writer it must not) before any count is believed, and 193/194 is a FLOOR, never a total; D-15: the derivation states its population definition and what falls outside it; D-20: the drill artifact 96-TRIGSWEEP-DRILL.md has exactly one writer (this plan) and ends with a terminal marker — 96-VERIFICATION-INDEPENDENT.md stays reserved and unclaimed; D-21: decision coverage is green mechanically with the falsification drill on disk (96-COVERAGE-DRILL.md, produced at planning)'
must_haves:
  truths:
    - 'The instrument classifies by the union of all four known syntactic forms PLUS language-based suspicion, and prints every residual trigger for hand-classification — the residual is a deliverable, never silently assumed non-writing'
    - 'A synthetic BEFORE trigger that writes NEW. is CAUGHT by the instrument; a synthetic RAISE-only trigger is NOT flagged — both directions observed and recorded on disk'
    - 'The derivation output states its population definition (public-schema, non-internal, BEFORE, FOR EACH ROW) and what falls outside (AFTER/INSTEAD OF, statement-level, non-public schemas, other-row writers)'
    - 'The six ORCH-BRIEF meaningful rewrites are listed explicitly in the output; every count is filed as a FLOOR'
  artifacts:
    - path: 'scripts/trigsweep-classify.mjs'
      provides: 'the behaviour classifier — house script posture, labelled exits, residual listing'
      contains: 'RESIDUAL'
    - path: '.planning/phases/96-real-numbers/96-TRIGSWEEP-DRILL.md'
      provides: 'the both-direction control record + the classified population + the hand-classified residual'
      contains: 'TRIGSWEEP-DRILL-END'
  key_links:
    - from: 'scripts/trigsweep-classify.mjs'
      to: 'pg_trigger/pg_proc (staging catalog)'
      via: 'the Derivation 1 CTE + the four-form union classifier, captured via MCP and processed by the script'
      pattern: 'pg_trigger'
---

<objective>
Close TRIGSWEEP-01: the inherited instrument, re-derived from behaviour. The lineage is the
point — P94's sweep matched only `:=` and saw 15% of its class; this phase's OWN research then
found a THIRD form (`SELECT … INTO NEW.x`, 2 triggers) invisible to both known regexes, and a
FOURTH blindness (`BEGIN NEW.updated_at = NOW();` on one line) that defeated the sharper
regex built in the same session — the TENTH instance of syntactic-form under-counting a
behaviour class, produced by the instrument built to fix the ninth. No syntactic instrument
survives contact. The close is therefore an instrument that (a) enumerates the population
mechanically from the catalog, (b) classifies by the UNION of every known form plus
non-plpgsql-as-suspect, (c) LISTS the residual for hand-classification as part of the
deliverable, and (d) is control-tested in BOTH directions before its count is believed.

Live floor at research: 212 BEFORE ROW triggers across 181 tables; ≥194 NEW-writers. Every
number is a FLOOR pinned to 2026-08-17 — re-derived at execution.

Purpose: the next phase that asks "what rewrites my writes?" inherits an instrument, not an
anecdote.
Output: the classifier script + the drill artifact with both directions observed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-real-numbers/96-RESEARCH.md (§Derivation 1 — the CTE, the four forms, the residual census, the instrument prescription; §Code Examples — the classifier SQL verbatim; §Pitfall 2)
@.planning/phases/96-real-numbers/96-PATTERNS.md (§9 — the probe-report-rls.mjs script posture)
@.tickmarkr/overseer/P94-TRIGGER-SWEEP.md (the inherited instrument AND its boundary)

<interfaces>
Verified by research 2026-08-17:
- Population CTE (Derivation 1, verbatim): pg_trigger ⋈ pg_class ⋈ pg_namespace ⋈ pg_proc ⋈
  pg_language WHERE NOT tgisinternal AND nspname='public' AND (tgtype & 2)=2 AND (tgtype & 1)=1.
- Classifier (Code Examples, verbatim): non-plpgsql → writer-suspect; `NEW\.[a-zA-Z_]+\s*:=`
  → writer (:=); statement-position `=` INCLUDING after BEGIN|THEN|ELSE|LOOP on the same
  line → writer (=); `INTO\s+(STRICT\s+)?NEW\.` → writer (INTO); whole-record `NEW :=` →
  writer (record); ELSE → 'RESIDUAL — hand-classify'.
- Known residual classes from the research census (the hand-classification's calibration):
  ~18 true non-writers (validate_dossier_type guards ×8, circular-delegation/role guards,
  attachment limits, version-snapshot immutability, audit-deletion guard) and 2
  OTHER-row-writing triggers (ensure_single_default_view, tr_enforce_single_default_layout)
  — side-effecting but NOT NEW-writers; the classifier must not confuse the two.
- The six named meaningful rewrites the output must list: staff_profiles.version,
  assignments._version, entity_comments.*, organization_leadership.is_current,
  legislations.version, intelligence_sources.next_scan_at (all plain-= class).
- Both-direction control (execution-leg DDL via MCP): one migration-created-and-dropped pair
  — a synthetic BEFORE INSERT trigger on a scratch table writing NEW (must classify writer)
  and one that only RAISEs NOTICE (must classify residual→hand-classified non-writer or
  non-writer) — created, swept, dropped in the same leg; ids printed; the research leg was
  read-only and did NOT run this control, so the count is labelled
  instrument-untested-in-both-directions until this drill runs.
- Script I/O shape (discretion, exercised): the catalog rows are captured via
  mcp__supabase__execute_sql into a JSON file; scripts/trigsweep-classify.mjs takes that
  file as argv, applies the union classifier in JS (mirroring the SQL classes), and emits
  the classification table + residual list + floor statement; exit 0 = classified with
  residual listed, exit 1 = a classification invariant broke, exit 2 = 'UNABLE TO MEASURE —
  input file absent/unparseable' (fail closed, never a silent zero).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The classifier instrument — scripts/trigsweep-classify.mjs</name>
  <files>scripts/trigsweep-classify.mjs</files>
  <read_first>scripts/probe-report-rls.mjs:1-80 (the house script posture to copy: header contract, labelled exits, no-secret rule); 96-RESEARCH.md §Code Examples (the classifier, verbatim — port each regex class to JS faithfully, including the same-line-after-BEGIN|THEN|ELSE|LOOP statement-position class)</read_first>
  <action>
    Create scripts/trigsweep-classify.mjs per the interfaces I/O shape: argv[2] is a JSON
    file of catalog rows (tgname, relname, proname, lanname, prosrc) captured by the
    Derivation 1 CTE via MCP (the exact SQL lives in the script header comment for re-run);
    classify each row: non-plpgsql → 'writer-suspect (non-plpgsql)'; then the four-form
    union in order (:=, statement-position = including same-line-after-BEGIN/THEN/ELSE/LOOP,
    INTO [STRICT] NEW., whole-record NEW :=); everything else → 'RESIDUAL — hand-classify'.
    Output: a per-class count table, the FULL residual list (tgname/relname/proname), a check
    that each of the six named meaningful rewrites appears in a writer class (exit 1 if any
    is missing — they are known plain-= writers; their absence means the classifier
    regressed), and the closing lines: the POPULATION DEFINITION sentence and 'FLOOR: N
    writers of M BEFORE ROW triggers — never a total'. Exit contract: 0 classified with
    residual listed; 1 invariant broke; 2 'UNABLE TO MEASURE — <reason>'. No credentials
    touched (the script reads a file, not the DB — the capture step is MCP's).
  </action>
  <verify>
    <automated>test -f scripts/trigsweep-classify.mjs && node --check scripts/trigsweep-classify.mjs && command grep -q 'RESIDUAL' scripts/trigsweep-classify.mjs && command grep -q 'UNABLE TO MEASURE' scripts/trigsweep-classify.mjs && command grep -q 'FLOOR' scripts/trigsweep-classify.mjs && command grep -q 'staff_profiles' scripts/trigsweep-classify.mjs</automated>
  </verify>
  <acceptance_criteria>Script exists, parses under node --check, and carries the residual class, the labelled exit-2 posture, the floor language and the six-rewrites invariant (staff_profiles pin). GATE DRILL: RED observed on undone tree (exit 1 — test -f fails, subject absent: the script is this task's product). GREEN constructed in scratch (synthetic script satisfying every pin, node --check green — observed exit 0).</acceptance_criteria>
  <done>The instrument exists, fails closed, and cannot silently lose the known writers.</done>
</task>

<task type="auto">
  <name>Task 2: Run the derivation with the both-direction control; file the drill artifact</name>
  <files>.planning/phases/96-real-numbers/96-TRIGSWEEP-DRILL.md</files>
  <read_first>96-RESEARCH.md §Derivation 1 in full (the census, the residual calibration, the control prescription); .tickmarkr/overseer/P94-TRIGGER-SWEEP.md (the boundary being inherited)</read_first>
  <action>
    (1) Capture the live catalog via mcp__supabase__execute_sql (the Derivation 1 CTE +
    prosrc) into the JSON input; run the classifier. (2) BOTH-DIRECTION CONTROL, before the
    count is believed (D-11): via mcp__supabase__apply_migration create a scratch table
    p96_trigsweep_control with TWO BEFORE INSERT ROW triggers — one whose function writes
    `NEW.touched = now()` in the plain-= same-line-after-BEGIN form (the hardest known
    class), one whose function only RAISEs NOTICE; re-capture + re-run: the writer MUST be
    caught in a writer class, the RAISE-only one MUST land residual and be hand-classified
    non-writer; then DROP the table and both functions in a second migration (created and
    dropped in the same leg — printed names, both migrations via MCP). (3) HAND-CLASSIFY
    every residual trigger (the deliverable): one line each — tgname, relname, verdict
    (non-writer-guard | other-row-writer | WRITER-missed-by-classifier), and if ANY residual
    is a missed writer, the classifier gains its form and the run repeats (the
    tenth-instance lesson, applied to this instrument's own residual). (4) Write
    .planning/phases/96-real-numbers/96-TRIGSWEEP-DRILL.md: the control record (both
    directions, verbatim outputs), the classification table, the full hand-classified
    residual, the six meaningful rewrites confirmed present, the population definition and
    its outside, every count labelled FLOOR with the capture date, ending with the terminal
    marker line TRIGSWEEP-DRILL-END. This plan is the artifact's ONLY writer (D-20).
  </action>
  <verify>
    <automated>test -f .planning/phases/96-real-numbers/96-TRIGSWEEP-DRILL.md && command grep -q 'TRIGSWEEP-DRILL-END' .planning/phases/96-real-numbers/96-TRIGSWEEP-DRILL.md && command grep -qi 'POPULATION' .planning/phases/96-real-numbers/96-TRIGSWEEP-DRILL.md && command grep -q 'FLOOR' .planning/phases/96-real-numbers/96-TRIGSWEEP-DRILL.md && command grep -qi 'synthetic writer' .planning/phases/96-real-numbers/96-TRIGSWEEP-DRILL.md && command grep -qi 'residual' .planning/phases/96-real-numbers/96-TRIGSWEEP-DRILL.md</automated>
  </verify>
  <acceptance_criteria>The drill artifact exists with the terminal marker, the population definition, floor language, the synthetic-writer control record and the residual section. The control itself (staging DDL create-sweep-drop) is UNPROVEN until execution — needs MCP; the artifact pins make its absence unfakeable only together with the verbatim outputs the action demands. GATE DRILL: RED observed on undone tree (exit 1 — test -f fails, subject absent: the artifact is this task's product). GREEN constructed in scratch (synthetic artifact satisfying every pin — observed exit 0; the REAL green additionally requires the control outputs, drilled at execution).</acceptance_criteria>
  <done>The count is believed only past a two-direction control; the residual is classified, not assumed; the floor is filed with its date.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                  |
| --------------------- | ------------------------------------------------------------ |
| control DDL → staging | scratch table + triggers created and dropped in the same leg |

## STRIDE Threat Register

| Threat ID | Category          | Component             | Disposition | Mitigation Plan                                                                                                              |
| --------- | ----------------- | --------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------- |
| T-96-27   | Tampering         | synthetic control DDL | mitigate    | dedicated scratch table only (p96_trigsweep_control), no RLS-exposed data, created+dropped via MCP migrations, names printed |
| T-96-28   | Denial of Service | catalog capture       | accept      | read-only catalog queries; no production; staging only                                                                       |
| T-96-SC   | Tampering         | package installs      | accept      | zero installs this phase                                                                                                     |

</threat_model>

<verification>
- Both gates green (control halves at execution per drill labels).
- POPULATION DEFINITION (D-15): public-schema, non-internal, BEFORE, FOR EACH ROW triggers;
  falls outside — AFTER/INSTEAD OF triggers, statement-level triggers, non-public schemas,
  and functions that write OTHER rows (side-effecting non-NEW-writers, classified apart).
- Every count in the artifact is a FLOOR with its capture date; the register's ≈193 and the
  research ≥194 are floors this run supersedes, never restates.
</verification>

<success_criteria>
TRIGSWEEP-01 closes as an instrument: behaviour-derived population, four-form-union
classifier that fails closed, hand-classified residual, six known rewrites confirmed, both
directions observed on disk, floors filed.
</success_criteria>

<output>
Create `.planning/phases/96-real-numbers/96-11-SUMMARY.md` when done. It MUST record: the
final classification counts, the residual verdict list, the control outputs verbatim, the
create/drop migration names, and the floor statement.
</output>
