---
phase: 96-real-numbers
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/_protected/calendar.tsx # becomes a layout
  - frontend/src/routes/_protected/calendar/index.tsx # NEW — holds the page body
  - frontend/src/components/calendar/UnifiedCalendar.tsx
  - frontend/src/pages/events/EventsPage.tsx
autonomous: true
requirements: [DEAD-07]
success_criterion: '3 (partial — /calendar, /calendar/new, /events) — the grid renders empty or not, the create form mounts, the month pads by the real weekday offset with working navigation; /word-assistant and the family spec close in 96-05'
truths:
  - "Decisions covered — D-05: /calendar renders a grid (empty or not — the empty-month wizard stops replacing it), /calendar/new mounts the create form (the missing <Outlet/> is the DEAD-08 class, fixed by the P95 legislation analog), /events pads the month by the REAL weekday offset with working month navigation; calendar_entries stays canonical and calendar_events stays a separate model — no consolidation; D-02: out-of-phase surfaces are named, not assumed — nav/reachability of these routes is P97's domain and this plan does not touch navigation"
must_haves:
  truths:
    - 'Navigating to /calendar/new renders the create form, not the parent page only'
    - 'An empty month on /calendar renders a GRID of day cells with weekday headers — never a full-screen wizard, a blank, or an error'
    - 'On /events the first date cell sits under its true weekday column and prev/next month navigation works'
    - 'A calendar query failure renders the shared QueryErrorState — not the bare text-destructive Card'
  artifacts:
    - path: 'frontend/src/routes/_protected/calendar.tsx'
      provides: 'layout route with <Outlet/> (the legislation.tsx analog)'
      contains: 'Outlet'
    - path: 'frontend/src/routes/_protected/calendar/index.tsx'
      provides: 'the index child holding the calendar page body'
      contains: 'createFileRoute'
    - path: 'frontend/src/components/calendar/UnifiedCalendar.tsx'
      provides: 'grid-always rendering + shared error state'
      contains: 'QueryErrorState'
    - path: 'frontend/src/pages/events/EventsPage.tsx'
      provides: 'weekday-offset padding + month navigation'
      contains: 'getDay'
  key_links:
    - from: 'frontend/src/routes/_protected/calendar.tsx'
      to: 'frontend/src/routes/_protected/calendar/new.tsx'
      via: '<Outlet/> making the registered child reachable in render'
      pattern: 'Outlet'
    - from: 'frontend/src/pages/events/EventsPage.tsx'
      to: 'date-fns'
      via: 'getDay(monthStart) leading cells + addMonths/subMonths nav state'
      pattern: 'addMonths'
---

<objective>
Close the /calendar, /calendar/new and /events legs of DEAD-07 (success criterion 3). Three
distinct mechanisms, all pinned by research: (a) `calendar.tsx` is the parent of
`calendar/new.tsx` in the generated tree and renders NO `<Outlet/>` — the DEAD-08 class P95
fixed on legislation, same fix; (b) `UnifiedCalendar.tsx:124` computes `isCalendarEmpty` from
the CURRENT-MONTH window and `:181-190` replaces the entire grid with `CalendarEmptyWizard` —
staging's 9 rows all sit around April 2026, so today's month renders the wizard, violating
"grid, empty or not"; (c) `EventsPage.tsx:40-55` renders `eachDayOfInterval` straight into
`grid-cols-7` with no weekday offset and no month navigation.

**House rule, named:** `calendar_entries` is canonical for /calendar; `/events` keeps reading
the `event_details` VIEW over the separate `calendar_events` family (0 rows — renders
truthfully empty with correct chrome). NO consolidation.

Purpose: the calendar family renders truthfully on every surface it owns.
Output: layout conversion + grid-always + offset/nav. The family spec lands in 96-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-real-numbers/96-RESEARCH.md (§Derivation 8 — all four mechanisms)
@.planning/phases/96-real-numbers/96-PATTERNS.md (§6 — the legislation.tsx template verbatim; §7 — in-file month-nav pattern)
@.planning/phases/96-real-numbers/96-UI-SPEC.md (§3 — the binding render contract per surface)
@frontend/src/routes/_protected/legislation.tsx (the P95 DEAD-08 analog, read in full)

<interfaces>
Verified by research 2026-08-17:
- Route tree: `ProtectedCalendarRouteWithChildren` — calendar.tsx parents calendar/new.tsx;
  routeTree.gen.ts is GENERATED (dev server/build regenerates it — never hand-edit).
- The analog shape (legislation.tsx): layout exports `component: () => <Outlet/>` and keeps
  `validateSearch` on the layout so deep links validate for the subtree; the body moves to
  the index child which reads validated search via getRouteApi.
- UnifiedCalendar already imports date-fns month-nav (`addMonths`/`subMonths` at :87-91) —
  the exact pattern EventsPage copies. Its bare error Card at :149-155 uses text-destructive
  — replaced by the shared QueryErrorState (frontend/src/components/error-states/
  QueryErrorState.tsx — props: variant 'page'|'inline', onRetry, isRetrying; testid
  query-error-state).
- EventsPage reads the event_details VIEW (its start_datetime column is REAL there — :268;
  do not confuse with DEAD-06's phantom column on calendar_entries) and quotes search params
  via quotePostgrestValue — keep both.
- calendar-get edge fn is deployed and returns the 9 calendar_entries rows (probed 200).
- C9b consumers (mock-vs-real), shipped specs that assert these surfaces' DOM:
  frontend/tests/e2e/calendar-render.spec.ts, calendar-rtl.spec.ts, calendar-visual.spec.ts,
  calendar-a11y.spec.ts, calendar-mobile.spec.ts (all REAL — live-login Playwright), and
  tests/e2e/07-calendar-events.spec.ts (REAL). The wizard removal and layout conversion can
  change DOM these assert: run the five frontend calendar specs + 07-calendar-events once
  after Task 2, record each colour, and update any red-for-a-correct-change spec IN THE SAME
  TASK (C9b rule a) or record it as a named non-consumer with the reason (rule b).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: calendar.tsx becomes a layout; the body moves to calendar/index.tsx</name>
  <files>frontend/src/routes/_protected/calendar.tsx, frontend/src/routes/_protected/calendar/index.tsx</files>
  <read_first>frontend/src/routes/_protected/legislation.tsx in full (the template); frontend/src/routes/_protected/calendar.tsx in full (what the body is, whether validateSearch exists); frontend/src/routes/_protected/calendar/new.tsx (the child that must mount)</read_first>
  <action>
    Mirror the legislation analog exactly: calendar.tsx becomes a layout whose component is
    `() => <Outlet/>`, keeping any existing route options (validateSearch, beforeLoad,
    loaders) ON THE LAYOUT so the subtree keeps validating; create
    frontend/src/routes/_protected/calendar/index.tsx as the index child holding the previous
    page body (the CalendarPage/UnifiedCalendar composition), reading validated search via
    getRouteApi if the layout validates. Add the header comment convention from the analog
    (why the file is a layout, citing DEAD-07/the DEAD-08 class). Let the dev server or build
    regenerate routeTree.gen.ts — never hand-edit it. Verify in-browser behaviour is Task 2's
    spec-family concern (96-05); this task's own proof is structural.
  </action>
  <verify>
    <automated>test -f frontend/src/routes/_protected/calendar/index.tsx && command grep -q '<Outlet' frontend/src/routes/_protected/calendar.tsx && command grep -q 'createFileRoute' frontend/src/routes/_protected/calendar/index.tsx && cd frontend && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>calendar.tsx renders Outlet; the index child exists as a route file; type-check clean (which also proves routeTree regenerated with the new child). GATE DRILL: RED observed on undone tree (exit 1 — index.tsx absent, subject absent: this task creates it). GREEN: grep half constructed in scratch (observed exit 0); type-check half UNPROVEN pre-execution — needs the real conversion; drill at execution.</acceptance_criteria>
  <done>/calendar/new mounts its form; /calendar still renders its page via the index child.</done>
</task>

<task type="auto">
  <name>Task 2: UnifiedCalendar — the grid always renders; errors use the shared vocabulary</name>
  <files>frontend/src/components/calendar/UnifiedCalendar.tsx</files>
  <read_first>frontend/src/components/calendar/UnifiedCalendar.tsx in full (the isCalendarEmpty computation :124, the wizard replacement :181-190, the bare error Card :149-155, the month-nav pattern :87-91); 96-UI-SPEC.md §3 /calendar contract</read_first>
  <action>
    (1) The grid renders unconditionally: remove the empty-month branch that swaps the entire
    grid for CalendarEmptyWizard (:181-190) and the isCalendarEmpty gating of the grid — an
    empty month is a GRID of day cells with weekday headers (UI-SPEC: never a blank region,
    an error state, or an infinite spinner). The wizard is NOT kept on this branch (the
    create affordance already exists in the page chrome); remove its import if that leaves
    zero references in the file. (2) Replace the bare error Card (:149-155,
    text-destructive) with QueryErrorState variant page wired to the query's refetch
    (onRetry, isRetrying) — the inherited vocabulary. (3) Keep existing chrome: 1px
    var(--line) cell borders, --surface, token radii, the today-marker accent as shipped;
    logical properties only. (4) C9b triage IN THIS TASK: run the six shipped consumer specs
    named in the interfaces block once after the edit; record each colour in the SUMMARY;
    update any spec that is red because it asserted the wizard-replaced empty state (a
    correct-change red) in this same task; name any non-red spec as still-sound.
  </action>
  <verify>
    <automated>command grep -q 'QueryErrorState' frontend/src/components/calendar/UnifiedCalendar.tsx && test "$(command grep -c 'CalendarEmptyWizard' frontend/src/components/calendar/UnifiedCalendar.tsx)" -eq 0 && cd frontend && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>QueryErrorState pin present (doubles as the instrument test that the file is readable, so the -eq 0 count is not vacuous); zero CalendarEmptyWizard references remain in the file (C8 note: do not leave a comment naming it); type-check clean; all six shipped consumer colours recorded with any correct-change reds repaired in-task. GATE DRILL: RED observed on undone tree (exit 1 — QueryErrorState absent from the file today, the subject's own reason). GREEN: grep half constructed in scratch (observed exit 0); type-check + consumer runs UNPROVEN pre-execution — drill at execution.</acceptance_criteria>
  <done>An empty April-less month renders a grid; a failed query renders the shared error state.</done>
</task>

<task type="auto">
  <name>Task 3: EventsPage — real weekday offset + month navigation</name>
  <files>frontend/src/pages/events/EventsPage.tsx</files>
  <read_first>frontend/src/pages/events/EventsPage.tsx in full (the grid render :40-55, the event_details read :262-271, quotePostgrestValue usage); frontend/src/components/calendar/UnifiedCalendar.tsx:87-91 (the nav-state pattern to copy); 96-UI-SPEC.md §3 /events contract</read_first>
  <action>
    (1) Pad the month grid with `getDay(monthStart)` leading empty cells so date cell 1 sits
    under its true weekday column (import getDay from date-fns — already the house library in
    this file family). (2) Add month navigation: currentMonth state with
    `subMonths`/`addMonths` handlers cloned from the UnifiedCalendar pattern; prev/next
    chevrons flip in RTL (`className={isRTL ? 'rotate-180' : ''}`); the month heading updates
    from the state; the event_details query window follows the displayed month. (3) Weekday
    headers keep the table-column-header treatment (the one permitted UPPERCASE/mono
    context); grid chrome per tokens; keep reading event_details (its start_datetime column
    is real THERE) and keep quotePostgrestValue on any interpolated filter value. The 0-row
    calendar_events family renders a truthfully empty grid with correct chrome — no
    consolidation, no seeding.
  </action>
  <verify>
    <automated>command grep -q 'getDay(' frontend/src/pages/events/EventsPage.tsx && command grep -q 'addMonths' frontend/src/pages/events/EventsPage.tsx && command grep -q 'subMonths' frontend/src/pages/events/EventsPage.tsx && cd frontend && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>getDay padding and both nav imports present in the file; type-check clean. The rendered-offset behavioural proof (first cell's column index equals the real offset, mirrored in RTL) is the 96-05 family spec's test 3 — producer before consumer. GATE DRILL: RED observed on undone tree (exit 1 — getDay absent from the file today, the subject's own reason: research greps confirmed no offset/nav exists). GREEN: grep half constructed in scratch (observed exit 0); type-check half UNPROVEN pre-execution — drill at execution.</acceptance_criteria>
  <done>Day 1 sits under its true weekday; prev/next month navigation works; the empty family renders truthfully.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                            |
| --------------------- | ------------------------------------------------------ |
| PostgREST/edge → grid | query failures render the shared error state, no leaks |

## STRIDE Threat Register

| Threat ID | Category               | Component              | Disposition | Mitigation Plan                                                                 |
| --------- | ---------------------- | ---------------------- | ----------- | ------------------------------------------------------------------------------- |
| T-96-09   | Information Disclosure | calendar error renders | mitigate    | QueryErrorState only — i18n copy, role=alert; no error.message in JSX (P93 law) |
| T-96-10   | Tampering              | search-param injection | mitigate    | quotePostgrestValue kept on every interpolated filter (EventsPage precedent)    |
| T-96-SC   | Tampering              | package installs       | accept      | zero installs this phase                                                        |

</threat_model>

<verification>
- All three gates green (type-check halves at execution per drill labels).
- Six shipped consumer specs run + colours recorded; correct-change reds repaired in-task.
- POPULATION DEFINITION (D-15): the close covers /calendar, /calendar/new and /events renders
  on the dev-server surface; falls outside — /word-assistant (96-05), nav/reachability of
  these routes (P97), calendar-sync and drawer surfaces (untouched), Arabic pixels (operator
  park).
</verification>

<success_criteria>
Criterion 3's first three legs hold structurally and render truthfully; the family spec
(96-05) proves them behaviourally.
</success_criteria>

<output>
Create `.planning/phases/96-real-numbers/96-04-SUMMARY.md` when done. It MUST record: the
layout-conversion shape chosen, the wizard-removal decision as executed, all six consumer
spec colours with attributions, and the routeTree regeneration evidence.
</output>
