---
phase: 96-real-numbers
plan: 06
type: execute
wave: 2
depends_on: [96-02] # the get_commitment_fulfillment bucket fix must be live before this chart ships (RESEARCH Open Question 3)
files_modified:
  - frontend/src/domains/analytics/repositories/analytics.repository.ts
  - frontend/src/domains/analytics/hooks/useAnalyticsDashboard.ts
  - frontend/src/pages/analytics/AnalyticsDashboardPage.tsx
  - frontend/src/components/analytics/AnalyticsPreviewOverlay.tsx # delete-or-unreference (recorded)
  - frontend/src/components/analytics/sample-data.ts # delete-or-unreference (recorded)
  - tests/e2e/96-analytics-real.spec.ts # NEW
autonomous: true
requirements: [DEAD-05]
success_criterion: '1 — /analytics shows real data or is honestly disabled; no fabricated sparklines, donuts or Insights-you-will-gain over a backend endpoint that does not exist'
truths:
  - "Decisions covered — D-03: the RECORDED branch decision is Branch A (real data) — the deployed analytics-dashboard edge fn serves five endpoints backed by live RPCs proven table-derived by SOURCE READ, and the frontend repoints from the dead express base to it; if a single widget's RPC turns out broken at execution, that widget ALONE takes the region-scoped honest-disable state per UI-SPEC Branch B (per-widget recorded, and only then do the analytics:disabled.* keys land, both locales same commit); D-01: this plan maps DEAD-05 to success criterion 1 in the 9-requirement register set, re-derivable by command"
must_haves:
  truths:
    - 'The /analytics page loads real payloads from the deployed edge fn — no request ever targets the dead express route'
    - 'The strings preview.insightsYouWillGain resolves to and any generateSample* output never reach the DOM on /analytics'
    - "The commitments chart's overdue series counts the stored fact (via 96-02's bucket fix) — a chart the COUNT-04 fix cannot silently zero"
    - 'Summary-card deltas follow the DEAD-06 trend rule: a delta renders only from a completed comparison'
  artifacts:
    - path: 'frontend/src/domains/analytics/repositories/analytics.repository.ts'
      provides: 'edge-fn repoint — per-endpoint getter(s), throw-shape preserved (D-01 comment)'
      contains: 'endpoint='
    - path: 'tests/e2e/96-analytics-real.spec.ts'
      provides: 'branch-invariant fabrication-absence oracle + real-render arm'
      contains: '@covers DEAD-05'
  key_links:
    - from: 'frontend/src/domains/analytics/repositories/analytics.repository.ts'
      to: 'supabase edge fn analytics-dashboard'
      via: 'apiGet default edge base — ${VITE_SUPABASE_URL}/functions/v1/analytics-dashboard?endpoint=…'
      pattern: 'analytics-dashboard'
    - from: 'frontend/src/pages/analytics/AnalyticsDashboardPage.tsx'
      to: 'frontend/src/domains/analytics/hooks/useAnalyticsDashboard.ts'
      via: 'five per-endpoint queries replacing the single-object adapter'
      pattern: 'useAnalytics'
---

<objective>
Close DEAD-05 (success criterion 1) on the RECORDED Branch A: real data. The break is one
line — `analytics.repository.ts:16` calls `apiGet('/analytics-dashboard…', { baseUrl:
'express' })` and the Express backend serves no such route — while the `analytics-dashboard`
EDGE fn is deployed and answering 200 with real payloads from five live RPCs, every one
proven table-derived by SOURCE READ (a 200 alone proves nothing — the DEAD-09 lesson;
Derivation 6's per-endpoint table is the evidence). The repair is a repoint plus response-shape
adaptation, not a build.

**Population seams, stated (condition 4):** analytics "engagements" counts
`dossier_interactions` (NOT `engagement_dossiers`) and "workload" counts `assignments` — both
different populations than the dashboard KPI, OUTSIDE the SC4 agreement set, stated here so
agreement claims never span them. The engagements and relationships endpoints are REAL but
EMPTY-backed today (0 rows each) — their zero-filled series are truthful empty renders, and no
oracle may treat a zero series as proof the path works.

**Forbidden shapes, named (condition 8):** no fabricated sparklines/donuts/"Insights you'll
gain" (the register names the copy itself); no sample-data mode on this route even as an
opt-in; server-sent hex colors (#10B981) are never adopted — chart series map to the
chart-palette carve-out tokens.

Purpose: the analytics surface stops performing and starts reporting.
Output: repoint + fabrication removal + branch-invariant e2e oracle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-real-numbers/96-RESEARCH.md (§Derivation 6 — the per-endpoint table map, the repoint shape, the recorded branch decision)
@.planning/phases/96-real-numbers/96-PATTERNS.md (§5 — the repository current state verbatim + api-client edge default)
@.planning/phases/96-real-numbers/96-UI-SPEC.md (§1 — both branches contracted; the branch-invariant oracle)

<interfaces>
Verified by research 2026-08-17:
- api-client.ts:13-16 defaults baseUrl to 'edge'; :80-81 resolves edge to
  ${VITE_SUPABASE_URL}/functions/v1${path} — dropping `{ baseUrl: 'express' }` IS the repoint.
- The deployed fn serves ?endpoint=summary|engagements|relationships|commitments|workload
  (+ optional startDate/endDate); payloads {success: true, data: {…}} camelCase, ONE endpoint
  per call. The page adapter (AnalyticsDashboardPage.tsx:74-107) expects a single object with
  all five sections — the chosen shape (discretion, exercised): the repository grows ONE
  parameterized getter getAnalyticsEndpoint(endpoint, params) and the hook grows five
  parallel useQuery calls keyed analyticsKeys.dashboard({endpoint, …}); the page consumes the
  five results (per-region loading/error per UI-SPEC).
- The repository THROWS by design (its D-01 header comment) — keep that shape; do not
  convert to result unions mid-API.
- Fabrication sites: AnalyticsPreviewOverlay.tsx (preview.insightsYouWillGain :159),
  generateSample* in components/analytics/sample-data.ts, the sample-data mode wiring at
  AnalyticsDashboardPage.tsx:44-49 and :173-196.
- C9b consumers (mock-vs-real), shipped specs asserting this surface:
  tests/e2e/93-analytics-error.spec.ts — REAL, forced-only (1 test), CDP-blocks
  '*analytics-dashboard*' which ALSO matches the new functions/v1 URL, so its forced error
  keeps rendering post-repoint (expected still-sound; the SPA module URL is
  AnalyticsDashboardPage.tsx — no hyphen match, no blank-page risk); run once, record colour,
  update in-task only if red. frontend/tests/e2e/analytics-dashboard.spec.ts — REAL
  (live-login; the P93 C9b amendment's own named miss): run once, record colour, update
  in-task if red for a correct change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint the repository and adapt the shape to five endpoint queries</name>
  <files>frontend/src/domains/analytics/repositories/analytics.repository.ts, frontend/src/domains/analytics/hooks/useAnalyticsDashboard.ts, frontend/src/pages/analytics/AnalyticsDashboardPage.tsx</files>
  <read_first>frontend/src/domains/analytics/repositories/analytics.repository.ts in full (25 lines — the D-01 throw-shape comment stays true); frontend/src/domains/analytics/hooks/useAnalyticsDashboard.ts in full; frontend/src/pages/analytics/AnalyticsDashboardPage.tsx (the :74-107 adapter + :44-49/:173-196 sample wiring); frontend/src/lib/api-client.ts:1-110 (the edge default)</read_first>
  <action>
    (1) Repository: replace getAnalyticsDashboard with a parameterized
    getAnalyticsEndpoint(endpoint, params) calling
    apiGet(`/analytics-dashboard?endpoint=${endpoint}&…`) with NO baseUrl option (edge
    default). Type the five endpoint payloads from the live response shapes ({success, data}
    camelCase — unwrap data in the repository or hook, one choice applied consistently). Keep
    the throw-shape and its D-01 header comment. (2) Hook: five useQuery calls (or one helper
    generating them) keyed analyticsKeys.dashboard({endpoint, timeRange…}); explicit return
    types. (3) Page: the :74-107 single-object adapter consumes the five query results;
    per-region loading (MetricsGridSkeleton/ChartSkeleton as shipped) and per-region
    QueryErrorState variant inline on a failed widget while succeeded siblings render
    (UI-SPEC Branch A + the region rule). Summary-card deltas follow the trend rule: render a
    delta only from a completed comparison — where the summary payload carries no prior
    period, the delta row is ABSENT (no 0.0%). Map any server-sent hex to the chart-palette
    carve-out tokens; never pass server colors into recharts. (4) If any endpoint proves
    broken live at execution: that widget alone renders the UI-SPEC Branch B honest-disable
    state (data-testid analytics-disabled-state, no role=alert, no retry), the
    analytics:disabled.* keys land in BOTH locales in the same commit, and the per-widget
    branch record goes in the SUMMARY.
  </action>
  <verify>
    <automated>test "$(command grep -c "baseUrl: 'express'" frontend/src/domains/analytics/repositories/analytics.repository.ts)" -eq 0 && command grep -q 'endpoint=' frontend/src/domains/analytics/repositories/analytics.repository.ts && command grep -q 'analytics-dashboard' frontend/src/domains/analytics/repositories/analytics.repository.ts && cd frontend && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>Zero express-base calls remain in the repository (the endpoint= and analytics-dashboard positive pins prove the file is readable, so the -eq 0 is not vacuous); the five-endpoint shape compiles. GATE DRILL: RED observed on undone tree (exit 1 — the file carries baseUrl: 'express' today, the subject's own reason). GREEN: grep half constructed in scratch (synthetic repository with the repointed shape — observed exit 0); type-check half UNPROVEN pre-execution — drill at execution.</acceptance_criteria>
  <done>Every /analytics data request targets the deployed edge fn; the page renders five real regions.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the fabrication layer from the route</name>
  <files>frontend/src/pages/analytics/AnalyticsDashboardPage.tsx, frontend/src/components/analytics/AnalyticsPreviewOverlay.tsx, frontend/src/components/analytics/sample-data.ts</files>
  <read_first>AnalyticsDashboardPage.tsx (:44-49, :173-196 — the sample-data mode and overlay wiring); the two fabrication files' import graphs (run: bash scripts/c9b-sweep.sh with the files as extra identifiers is NOT available pre-tag — use command grep -rln 'AnalyticsPreviewOverlay\|generateSample' frontend/src to enumerate importers, instrument-tested against the page's own known import)</read_first>
  <action>
    Remove the sample-data toggle, the generateSample* wiring, and the
    AnalyticsPreviewOverlay render from AnalyticsDashboardPage.tsx. Then the delete-vs-
    unreference decision, executed mechanically: enumerate remaining importers of
    AnalyticsPreviewOverlay.tsx and sample-data.ts with command grep -rln over frontend/src
    (instrument-test: the same grep must find the page BEFORE your edit — run before and
    after); if zero importers remain, DELETE both files (recorded); if any importer remains
    outside this route, leave the files and record the importer list (the visual contract is
    DOM-absence on this route, not file deletion). NAMED condition-5 exception (revision 1):
    this inline grep is a source-importer enumeration, not a C9b consumer sweep; the C9b
    consumer set for this surface is triaged in Task 3 and re-derived by 96-09's phase-close
    script run — scripts/c9b-sweep.sh remains the only C9b entry point (D-19). The
    analytics:disabled.* keys are NOT added on Branch A (keys land only for the branch that
    ships — UI-SPEC).
  </action>
  <verify>
    <automated>test "$(command grep -c 'AnalyticsPreviewOverlay' frontend/src/pages/analytics/AnalyticsDashboardPage.tsx)" -eq 0 && test "$(command grep -c 'generateSample' frontend/src/pages/analytics/AnalyticsDashboardPage.tsx)" -eq 0 && command grep -q 'QueryErrorState' frontend/src/pages/analytics/AnalyticsDashboardPage.tsx && cd frontend && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>Zero overlay/sample references remain in the page (the QueryErrorState positive pin proves readability); the importer enumeration before/after is recorded with its instrument test; the delete-vs-unreference decision is recorded as executed; the enumeration is labelled in the SUMMARY as the NAMED condition-5 exception (source-importer enumeration, not a C9b sweep). GATE DRILL: RED observed on undone tree (exit 1 — the page imports the overlay today, the subject's own reason). GREEN: grep half constructed in scratch (observed exit 0); type-check half UNPROVEN pre-execution — drill at execution.</acceptance_criteria>
  <done>No fabricated visual can reach the /analytics DOM; the decision trail is mechanical.</done>
</task>

<task type="auto">
  <name>Task 3: Branch-invariant oracle — 96-analytics-real.spec.ts + shipped-consumer triage</name>
  <files>tests/e2e/96-analytics-real.spec.ts</files>
  <read_first>tests/e2e/95-sandbox-error.spec.ts in full (the family shape); tests/e2e/93-analytics-error.spec.ts in full (the shipped consumer — its block pattern and assertion); 96-UI-SPEC.md §Verification Notes (the DEAD-05 branch-invariant oracle)</read_first>
  <action>
    Write exactly 2 tests, header `// @covers DEAD-05 (criterion 1)` plus the
    population-definition block. Test 1 (fabrication absence — branch-invariant): goto
    /analytics, wait for settle (content or error, 15s budget), assert body innerText does
    NOT contain the resolved preview pitch string ("Insights you'll gain" — pin the literal
    the i18n key resolves to, read from frontend/src/i18n/en at authoring time) and does not
    match INTERNAL_STRING; assert no element carries a sample-data marker (pin whatever
    stable identifier the sample components rendered — read them before writing the pin; if
    deleted in Task 2, keep the innerText pin as the oracle). Test 2 (real render): the page
    settles to at least one rendered chart region or per-region error/honest-disable state —
    never a spinner past budget and never a blank main region; if all five regions error, the
    test still passes on the truthful-error branch BUT records it (both outcomes truthful;
    fabrication is the only failure). SAME TASK, C9b triage: run
    tests/e2e/93-analytics-error.spec.ts and frontend/tests/e2e/analytics-dashboard.spec.ts
    once each; record colours; update in-task any red-for-a-correct-change (93's broad
    '*analytics-dashboard*' block still matches the edge URL — expected still-sound).
  </action>
  <verify>
    <automated>test -f tests/e2e/96-analytics-real.spec.ts && test "$(pnpm exec playwright test tests/e2e/96-analytics-real.spec.ts --project=chromium-en --no-deps --list 2>/dev/null | command grep -c '›')" -eq 2 && pnpm exec playwright test tests/e2e/96-analytics-real.spec.ts --project=chromium-en --no-deps && pnpm exec playwright test tests/e2e/93-analytics-error.spec.ts --project=chromium-en --no-deps</automated>
  </verify>
  <acceptance_criteria>Spec exists first, lists exactly 2 tests with --no-deps, passes; the shipped 93 spec still passes in the same gate (the consumer stays green, C9b rule a). GATE DRILL: RED observed on undone tree (exit 1 — test -f fails, subject absent: the spec is this task's product). GREEN UNPROVEN pre-execution — needs Tasks 1-2 + dev server; drill at execution.</acceptance_criteria>
  <done>Criterion 1 is behaviourally observable and the shipped consumers are triaged, not discovered.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary         | Description                                                  |
| ---------------- | ------------------------------------------------------------ |
| edge fn → charts | payload numbers render as data; failures render the contract |

## STRIDE Threat Register

| Threat ID | Category               | Component               | Disposition | Mitigation Plan                                                                 |
| --------- | ---------------------- | ----------------------- | ----------- | ------------------------------------------------------------------------------- |
| T-96-13   | Spoofing (of data)     | sample/preview layer    | mitigate    | removed from the route; DOM-absence oracle branch-invariant                     |
| T-96-14   | Information Disclosure | analytics error renders | mitigate    | QueryErrorState family only; INTERNAL_STRING asserted; 93 spec kept green       |
| T-96-15   | Tampering              | server-sent hex colors  | mitigate    | mapped to chart-palette carve-out tokens, never adopted (ESLint hex rule holds) |
| T-96-SC   | Tampering              | package installs        | accept      | zero installs this phase                                                        |

</threat_model>

<verification>
- All three gates green (behavioural halves at execution per drill labels).
- POPULATION DEFINITION (D-15): the close covers the five analytics endpoints as classified
  in Derivation 6's table (each traced to its base tables by prosrc); falls outside — the
  correctness of the underlying populations vs other surfaces (the engagements/workload seams
  stated in the objective are OUTSIDE the SC4 agreement set), empty-backed endpoints' future
  data (truthful zeros today), organization-benchmarks (not on this page's five).
- Condition-5 posture: Task 2's importer grep is a NAMED exception (source-importer
  enumeration, not a consumer sweep); every C9b consumer sweep this phase runs goes through
  scripts/c9b-sweep.sh (96-09's gate).
</verification>

<success_criteria>
Criterion 1 holds on Branch A: real table-derived data (or per-widget honest-disable, recorded)
with zero fabricated visuals reachable in the DOM, proven behaviourally.
</success_criteria>

<output>
Create `.planning/phases/96-real-numbers/96-06-SUMMARY.md` when done. It MUST record: the
per-endpoint branch record (A for each, or B per-widget with reason), the delete-vs-unreference
decision + importer lists (labelled as the NAMED condition-5 exception), both shipped-consumer
colours, and the empty-backed-endpoints note.
</output>
