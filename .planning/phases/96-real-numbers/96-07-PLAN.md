---
phase: 96-real-numbers
plan: 07
type: execute
wave: 2
depends_on: [96-02] # kanban RPC semantics + stored-overdue arm must be live before agreement is asserted
files_modified:
  - supabase/migrations/20260817500004_p96_dashboard_stats_truth.sql # NEW
  - frontend/src/components/commitments/PersonalCommitmentsDashboard.tsx
  - frontend/src/pages/my-work/MyWorkDashboard.tsx
  - frontend/src/hooks/useUnifiedWork.ts
  - tests/e2e/96-count-agreement.spec.ts # NEW
autonomous: true
requirements: [COUNT-01]
success_criterion: '4 — the dashboard KPI, the /my-work badge, footer and rendered rows, the /commitments tabs and the kanban board report the same number for the same work'
truths:
  - 'Decisions covered — D-06: the unification mechanism (discretion, exercised) is shared-derivation with stated seams — every surface counts through the unified_work_items-family relations with its per-surface filter STATED, the dashboard engagement KPI is re-pointed to the same population the engagements list renders (the proven 3-vs-5 seam), and agreement between two surfaces sharing a wrong filter is never accepted as evidence; D-15: the agreement claim names its surface set (dashboard KPI strip, /my-work badge+footer+rows, /commitments tabs, kanban board+chip) and what falls outside (/custom-dashboard widgets, the analytics populations); D-16: every agreement oracle is CONTEMPORANEOUS — one DOM snapshot or one single-statement SQL batch, or the seam is stated'
must_haves:
  truths:
    - 'On /my-work, the badge, the footer total and the rendered row count derive from the same response and cannot structurally diverge'
    - 'The /commitments active tab counts stored overdue as active work (8 stored-overdue rows no longer vanish from "active" while the board shows them)'
    - 'The dashboard engagement KPI and the engagements list count the same population (the 3-vs-5 seam is gone or stated on the KPI label)'
    - 'Every rendered agreement is captured same-clock; cross-page comparisons run as one-statement SQL batches'
  artifacts:
    - path: 'supabase/migrations/20260817500004_p96_dashboard_stats_truth.sql'
      provides: 'get_dashboard_stats with the engagement population unified to the list and every KPI source+filter documented'
      contains: 'get_dashboard_stats'
    - path: 'tests/e2e/96-count-agreement.spec.ts'
      provides: 'same-clock DOM agreement oracles per surface + recorded SQL-batch shape for cross-page'
      contains: '@covers COUNT-01'
  key_links:
    - from: 'frontend/src/pages/my-work/MyWorkDashboard.tsx'
      to: 'frontend/src/hooks/useUnifiedWork.ts'
      via: 'badge/footer/rows all derived from one fetched result set'
      pattern: 'useMyWorkDashboard'
    - from: 'tests/e2e/96-count-agreement.spec.ts'
      to: 'frontend/src/pages/my-work/MyWorkDashboard.tsx'
      via: 'single-DOM-snapshot equality of badge, footer and row count'
      pattern: 'my-work'
---

<objective>
Close COUNT-01 (success criterion 4): one source of truth for work-item counts. Research
enumerated the surface set mechanically (Derivation 5): FIVE distinct mechanisms currently
compute "the same work" — get_dashboard_stats (RPC over engagement_dossiers⋈dossiers +
unified_work_items), unified-work-list (edge fn over get_user_work_summary +
get_unified_work_items), three direct aa_commitments head-counts (/commitments), the
hand-rolled kanban RPCs, and direct view selects (/custom-dashboard — OUTSIDE the set,
stated). Live same-clock disagreement is proven: KPI says 3 engagements, the list renders 5 —
both "correct" for their own filter, which is exactly the class ACCEPTANCE condition 4 kills.

**The agreement claim's population (condition 4, binding):** the surfaces compared are the
dashboard KPI strip, /my-work badge+footer+rows, /commitments tabs, and the kanban
board+chip. Falls outside: /custom-dashboard widgets (share the view, stated outside per
ROADMAP wording), the analytics populations (dossier_interactions/assignments — stated in
96-06), archived/deleted work.

**Unification mechanism (discretion, exercised):** shared-derivation with stated seams — NOT
a single-RPC rewrite (no decision requires it and it would widen the diff). Each surface's
filter is aligned or stated; the known wrong-population reads are re-pointed.

**Forbidden shape, named (condition 8):** a fallback that renders an empty state or a zero
over rows the server sent; a count widget whose query failed renders the error contract,
never 0.

Purpose: the phase's goal sentence, made checkable surface by surface.
Output: one migration, two surface reconciliations, one same-clock oracle spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-real-numbers/96-RESEARCH.md (§Derivation 5 — the surface table + the proven 3-vs-5 batch; §Code Examples — the same-clock SQL shape; A2 — the intra-/my-work seam is ASSUMED and must be pinned here)
@.planning/phases/96-real-numbers/96-PATTERNS.md (§8 — the filter seams as pattern source)
@supabase/migrations/20260330000001_operations_hub_rpcs.sql (get_dashboard_stats current shape :275+)

<interfaces>
Verified by research 2026-08-17:
- get_dashboard_stats engagement count today: engagement_dossiers ⋈ dossiers WHERE
  d.status='active' AND ed.lifecycle_stage != 'closed' → 3; the engagements list renders
  dossiers WHERE type='engagement' AND status='active' → 5. The re-point target is the list
  population (one number for one population).
- /my-work: MyWorkDashboard.tsx:88 consumes useMyWorkDashboard from
  frontend/src/hooks/useUnifiedWork.ts (149 lines) → edge fn unified-work-list → RPCs
  get_user_work_summary (view user_work_summary) + get_unified_work_items (view
  unified_work_items). The badge-18/footer-21/rows-11 defect's exact intra-surface mechanism
  is ASSUMED (A2) — pin it by reading the hook + page before editing: the fix direction is
  structural (badge and footer derive from the SAME response as the rows: the list response's
  pagination total), not numeric patching.
- /commitments tabs (PersonalCommitmentsDashboard.tsx:33-69): three head-counts in one
  queryFn — active = status IN (pending,in_progress) [excludes stored overdue — the seam],
  overdue = computed date comparison, completed. Post-96-02 the stored notion wins for
  commitments: active becomes status IN (pending,in_progress,overdue); the overdue tab count
  becomes status = 'overdue'.
- View seams that STAY (stated, not silently changed): unified_work_items commitments arm
  requires owner_user_id IS NOT NULL (external-owner commitments fall outside "my work" by
  construction); tasks arm requires assignee_id IS NOT NULL AND is_deleted=false.
- C9b consumers (mock-vs-real): tests/e2e/96-overdue-badge.spec.ts (REAL, 96-09 — later
  wave); WorkBoard.test.tsx (MOCKED — insulated from RPC changes); no shipped spec asserts
  /my-work or /commitments counts (exec-time c9b-sweep in 96-09 re-derives).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: get_dashboard_stats tells one truth per KPI — migration</name>
  <files>supabase/migrations/20260817500004_p96_dashboard_stats_truth.sql</files>
  <read_first>The CURRENT get_dashboard_stats prosrc pulled via mcp__supabase__execute_sql (never the stale migration copy); 96-RESEARCH.md Derivation 5's surface table (every KPI's source + filter)</read_first>
  <action>
    Author supabase/migrations/20260817500004_p96_dashboard_stats_truth.sql: CREATE OR
    REPLACE get_dashboard_stats from the live prosrc with two truth changes and one
    documentation duty: (1) the engagement KPI counts the list population —
    `(SELECT count(*) FROM public.dossiers WHERE type = 'engagement' AND status = 'active')`
    — replacing the engagement_dossiers⋈lifecycle join count (the 3-vs-5 seam closes on the
    KPI side; the lifecycle-filtered number was a narrower population wearing a general
    label); (2) the active-work KPI's filter set is aligned verbatim to the
    unified_work_items active definition the /my-work path uses (status NOT IN
    completed/cancelled/closed/converted, same assigned_to semantics) so KPI-vs-badge
    agreement is by shared derivation; (3) the migration header documents EVERY KPI's source
    relation + filter (upcoming-events reads engagement_dossiers ∪ calendar_entries
    .event_date — stated; deadlines reads intake_tickets external_deadline ≤48h — stated) —
    the filter-seam record condition 4 demands. Keep SECURITY posture; re-state GRANT.
    Apply via mcp__supabase__apply_migration. Verify with the recorded same-clock batch
    (single statement): engagement KPI count == list count, from Derivation 5's proven shape.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260817500004_p96_dashboard_stats_truth.sql && command grep -q 'get_dashboard_stats' supabase/migrations/20260817500004_p96_dashboard_stats_truth.sql && command grep -q "type = 'engagement'" supabase/migrations/20260817500004_p96_dashboard_stats_truth.sql && command grep -q 'GRANT EXECUTE' supabase/migrations/20260817500004_p96_dashboard_stats_truth.sql && command grep -qi 'POPULATION\|-- seam' supabase/migrations/20260817500004_p96_dashboard_stats_truth.sql</automated>
  </verify>
  <acceptance_criteria>Migration exists re-creating get_dashboard_stats with the list-population engagement count, aligned active-work filters, seam documentation and grants. The same-clock equality batch runs via MCP at execution (UNPROVEN in this gate — staging state). GATE DRILL: RED observed on undone tree (exit 1 — test -f fails, subject absent). GREEN constructed in scratch (synthetic migration satisfying the five pins — observed exit 0).</acceptance_criteria>
  <done>One population per KPI, every seam written down where the RPC lives.</done>
</task>

<task type="auto">
  <name>Task 2: /commitments and /my-work stop disagreeing with themselves</name>
  <files>frontend/src/components/commitments/PersonalCommitmentsDashboard.tsx, frontend/src/pages/my-work/MyWorkDashboard.tsx, frontend/src/hooks/useUnifiedWork.ts</files>
  <read_first>PersonalCommitmentsDashboard.tsx:30-70 (the three head-counts); frontend/src/hooks/useUnifiedWork.ts in full (149 lines — the summary+items shape); MyWorkDashboard.tsx (where badge, footer and rows each get their number — pin the A2 mechanism here and record it)</read_first>
  <action>
    (1) PersonalCommitmentsDashboard: active tab count becomes
    .in('status', ['pending', 'in_progress', 'overdue']) — stored overdue IS active work
    (the winning-notion record, 96-02); the overdue tab count becomes .eq('status',
    'overdue') — the stored notion, replacing the computed date comparison; completed
    unchanged. The three counts stay in one queryFn — a sub-second three-query window, the
    stated seam (condition 7 allows a stated seam; the rendered-tab agreement oracle in Task
    3 is single-DOM). (2) /my-work: pin the badge-18/footer-21/rows-11 mechanism by reading
    the hook and page (A2), then make the badge and footer STRUCTURALLY derive from the same
    response that renders the rows — the list response's total (pagination total or
    items.length where unpaginated); if the summary RPC's number is still shown anywhere, it
    labels itself as the summary population or is dropped (one number, one response). Record
    the pinned mechanism in the SUMMARY. (3) A failed count query renders the error contract,
    never 0 (forbidden shape) — verify each touched count render has an isError branch.
  </action>
  <verify>
    <automated>command grep -q "'overdue'" frontend/src/components/commitments/PersonalCommitmentsDashboard.tsx && command grep -q "eq('status', 'overdue')" frontend/src/components/commitments/PersonalCommitmentsDashboard.tsx && cd frontend && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>The active filter includes stored overdue and the overdue tab reads the stored status (both pinned); type-check clean; the A2 mechanism pinned + recorded; badge/footer/rows share one response structurally (source-verified, behavioural half in Task 3). GATE DRILL: RED observed on undone tree (exit 1 — the file's active filter carries no 'overdue' today, the subject's own reason). GREEN: grep half constructed in scratch (observed exit 0); type-check half UNPROVEN pre-execution — drill at execution.</acceptance_criteria>
  <done>No surface in the set can disagree with itself; the commitments tabs count the stored fact.</done>
</task>

<task type="auto">
  <name>Task 3: Same-clock agreement oracle — 96-count-agreement.spec.ts</name>
  <files>tests/e2e/96-count-agreement.spec.ts</files>
  <read_first>tests/e2e/95-sandbox-error.spec.ts (the family shape); 96-RESEARCH.md §Pattern 4 + Code Examples (the one-statement SQL batch shape); 96-UI-SPEC.md §Verification Notes (agreement is a moment)</read_first>
  <action>
    Write exactly 3 tests, header `// @covers COUNT-01 (criterion 4)` plus the
    population-definition block naming the surface set and its outsides (verbatim from this
    plan's objective). Test 1 (/my-work internal agreement, single DOM snapshot): goto
    /my-work, settle, then in ONE evaluate pass read the badge number, the footer total and
    the rendered row count from the same DOM — assert badge == footer AND rows consistent
    with the totals under the stated pagination rule (if paginated, rows == page size or
    total, whichever is smaller — read the rule from the page while writing the pin). Test 2
    (/commitments internal agreement, single DOM snapshot): tab counts vs the rendered list
    length for the selected tab in one snapshot; switching tabs re-checks once. Test 3
    (cross-surface, seam-stated): capture the dashboard KPI active-work number, then the
    /my-work badge — two pages, two clocks — so the assertion is NOT DOM equality; instead
    the test runs the ONE-STATEMENT SQL batch (Derivation 5 shape: SELECT (KPI derivation) AS
    a, (badge derivation) AS b) through the service-role client (SUPABASE_SERVICE_ROLE_KEY
    from .env.test, the e2e cleanup-helper precedent — never echoed) and asserts a == b at
    the DB layer, recording both rendered numbers as evidence, with the cross-page seam
    stated in the test's comment. Never assert emptiness as agreement; a failed query fails
    the test (error contract asserted absent of internal strings).
  </action>
  <verify>
    <automated>test -f tests/e2e/96-count-agreement.spec.ts && test "$(pnpm exec playwright test tests/e2e/96-count-agreement.spec.ts --project=chromium-en --no-deps --list 2>/dev/null | command grep -c '›')" -eq 3 && pnpm exec playwright test tests/e2e/96-count-agreement.spec.ts --project=chromium-en --no-deps</automated>
  </verify>
  <acceptance_criteria>Spec exists first, lists exactly 3 tests with --no-deps, passes. Each agreement is one DOM snapshot or a one-statement SQL batch with the seam stated (condition 7 compliant by construction). GATE DRILL: RED observed on undone tree (exit 1 — test -f fails, subject absent). GREEN UNPROVEN pre-execution — needs Tasks 1-2 + 96-02 live + dev server + service key; drill at execution.</acceptance_criteria>
  <done>Criterion 4 is a moment-anchored, re-runnable observation, not a screenshot pair.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                              |
| ------------------------- | -------------------------------------------------------- |
| client → definer RPC      | get_dashboard_stats is a definer read for authenticated  |
| service-role key in tests | the SQL-batch oracle uses the service key — never echoed |

## STRIDE Threat Register

| Threat ID | Category               | Component                     | Disposition | Mitigation Plan                                                                      |
| --------- | ---------------------- | ----------------------------- | ----------- | ------------------------------------------------------------------------------------ |
| T-96-16   | Elevation of Privilege | get_dashboard_stats replace   | mitigate    | live-prosrc base, minimal diff, GRANT to authenticated only, no new relations read   |
| T-96-17   | Information Disclosure | service-role key in the spec  | mitigate    | env-only, never logged; the cleanup-helper precedent; .env.test gitignored           |
| T-96-18   | Spoofing (of data)     | zero-on-failure count renders | mitigate    | error contract on failed count queries, asserted in Task 2/3 (forbidden shape named) |
| T-96-SC   | Tampering              | package installs              | accept      | zero installs this phase                                                             |

</threat_model>

<verification>
- All three gates green (behavioural halves at execution per drill labels).
- POPULATION DEFINITION (D-15): stated in the objective and repeated in the spec header —
  the four compared surfaces; outside: /custom-dashboard, analytics populations,
  external-owner commitments (view seam, stated), unassigned tasks (view seam, stated).
- Agreement claims are same-clock or seam-stated (D-16/condition 7) — no cross-time DOM pair
  is ever the evidence.
</verification>

<success_criteria>
Criterion 4 holds: within the named surface set, the same work yields the same number, proven
by single-snapshot DOM oracles and one-statement SQL batches, with every excluded population
written down.
</success_criteria>

<output>
Create `.planning/phases/96-real-numbers/96-07-SUMMARY.md` when done. It MUST record: the
pinned A2 mechanism, the KPI seam table (each KPI → relation + filter), the three-tab window
seam, and all three test colours with the SQL-batch numbers.
</output>
