---
phase: 96-real-numbers
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/useWidgetDashboard.ts
  - frontend/src/components/dashboard-widgets/KpiWidget.tsx
  - tests/e2e/96-custom-dashboard-truth.spec.ts # NEW
autonomous: true
requirements: [DEAD-06]
success_criterion: '2 — /custom-dashboard queries columns that exist (calendar_entries.event_date), renders its chart, and shows trend deltas computed from completed requests rather than 0.0% from aborted ones'
truths:
  - 'Decisions covered — D-04: the events query reads calendar_entries.event_date/event_time (start_datetime does not exist — the 42703 on every EventsWidget render stops), and a trend delta renders ONLY from a comparison request that SETTLED successfully with a real prior count — 0.0% from an aborted request is the forbidden confident-lie shape; D-16: the trend oracle is behavioural via CDP with the block pattern NARROWED to the exact /rest/v1 table path (a broad pattern blanks the SPA — the 95-03 lesson)'
must_haves:
  truths:
    - 'EventsWidget renders rows (or a truthful empty/error state) — never a 42703 failure from a phantom column'
    - 'Under a blocked comparison request the KPI trend row is ABSENT from the DOM — no 0.0%, no placeholder, no em-dash masquerading as data'
    - 'With the comparison succeeding, the trend row renders a real signed percentage from the prior-period count'
  artifacts:
    - path: 'frontend/src/hooks/useWidgetDashboard.ts'
      provides: 'event_date/event_time query + trend: null unless the comparison settled'
      contains: 'event_date'
    - path: 'frontend/src/components/dashboard-widgets/KpiWidget.tsx'
      provides: 'trend row omitted entirely on null, stable data-testid for the oracle'
      contains: 'kpi-trend'
    - path: 'tests/e2e/96-custom-dashboard-truth.spec.ts'
      provides: 'CDP narrowed-block trend-absence oracle + natural settle arm'
      contains: '@covers DEAD-06'
  key_links:
    - from: 'frontend/src/components/dashboard-widgets/KpiWidget.tsx'
      to: 'frontend/src/hooks/useWidgetDashboard.ts'
      via: 'trend prop null-guard — absent row, never a fabricated neutral'
      pattern: 'kpi-trend'
    - from: 'tests/e2e/96-custom-dashboard-truth.spec.ts'
      to: 'frontend/src/components/dashboard-widgets/KpiWidget.tsx'
      via: 'DOM absence/presence of data-testid=kpi-trend under CDP block vs natural'
      pattern: 'kpi-trend'
---

<objective>
Close DEAD-06 (success criterion 2): `/custom-dashboard` stops querying a phantom column and
stops fabricating trend deltas. Research pinned both defects to the line: `fetchEvents` at
`useWidgetDashboard.ts:587-606` selects/filters/orders on `start_datetime` (live columns are
`event_date` date + `event_time` time + `all_day` — 42703 on every render), and the KPI fetch
fabricates neutral trends (`:281` zero-default, `:195/:213/:244/:273` `previous.count || value`
coalesces, `:224/:255` hardcoded `previousValue = value`).

**Forbidden shape, named (condition 8):** "0.0%" derived from an aborted or never-run
comparison is the confident-lie class. The truthful render of an unknown delta is an ABSENT
row (Pattern 3, truthful-absence).

Purpose: the last always-broken widget renders, and every trend on this surface is real.
Output: hook truth fixes, widget absence-rendering, one CDP e2e spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-real-numbers/96-RESEARCH.md (§Derivation 7 — every fabrication pinned; §Code Examples — the exact replacement query)
@.planning/phases/96-real-numbers/96-PATTERNS.md (§1 — the spec-family shape incl. the narrowed-block rule for PostgREST reads)
@.planning/phases/96-real-numbers/96-UI-SPEC.md (§2 — the binding trend contract: absent row, no placeholder)

<interfaces>
Verified by research 2026-08-17:
- Replacement query (RESEARCH Code Examples, verbatim target): from('calendar_entries')
  .select('id, title_en, title_ar, entry_type, event_date, event_time, all_day,
  description_en, description_ar').gte('event_date', now.toISOString().slice(0, 10))
  .order('event_date', {ascending: true}).order('event_time', {ascending: true,
  nullsFirst: false}) — map event_date+event_time to the widget's startDate.
- The trend fabrication sites (line numbers are pins at scout; the seam is the symbol):
  trendPercentage zero-default at :281; `previous.count || value` at :195/:213/:244/:273;
  `previousValue = value` at :224/:255; fetchTasks overdue at :638 omits cancelled — align to
  `status NOT IN ('completed','cancelled')` (the COUNT-04 tasks notion, stated seam).
- These KPI reads are PostgREST (supabase-js) — the CDP block pattern targets
  `*/rest/v1/unified_work_items*` (the comparison reads), NEVER a broad `*dashboard*` pattern
  (blanks the SPA — 95-03 lesson) and NOT functions/v1 (wrong tier for this widget).
- Chart widgets (completion-trend, intake-volume-trend) read live relations and render once
  EventsWidget's 42703 stops failing its region; errors already propagate (throws at
  :597/:628 — the P93 retrofit). Do not convert this hook to result unions (match the
  surface's established shape).
- C9b consumers (mock-vs-real): no shipped spec asserts /custom-dashboard DOM (research found
  none; the exec-time c9b-sweep in 96-09 re-derives this). The new spec is this plan's own.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kill the phantom column and the fabricated trends in useWidgetDashboard</name>
  <files>frontend/src/hooks/useWidgetDashboard.ts</files>
  <read_first>frontend/src/hooks/useWidgetDashboard.ts in full (the fetchEvents block :587-606, every trend site listed in interfaces, the fetchTasks overdue formula :638); 96-RESEARCH.md §Derivation 7</read_first>
  <action>
    (1) fetchEvents: replace the start_datetime select/filter/order with the verbatim
    replacement query from the interfaces block; map event_date + event_time (+ all_day) to
    the widget's startDate field; zero references to start_datetime remain in this file.
    (2) Trend truth: the hook's KPI result type carries `trend: TrendData | null`; `trend` is
    non-null ONLY when the comparison request settled successfully AND returned a real prior
    count (no `|| value` coalesce, no `previousValue = value` hardcode, no zero-default
    percentage). Every fabrication site in the interfaces list is removed; a failed, aborted,
    or never-run comparison yields `trend: null`.
    (3) fetchTasks overdue at :638: exclude cancelled as well as completed (the unified tasks
    notion — cite the 96-02 winning-notion record; a cancelled task is not overdue work).
    (4) Explicit return types stay (ESLint); no `any`; the hook keeps its throw-on-error
    shape (P93 retrofit — do not mix result unions into this API).
  </action>
  <verify>
    <automated>command grep -q "event_date" frontend/src/hooks/useWidgetDashboard.ts && command grep -q "event_time" frontend/src/hooks/useWidgetDashboard.ts && test "$(command grep -c 'start_datetime' frontend/src/hooks/useWidgetDashboard.ts)" -eq 0 && test "$(command grep -c 'previousValue = value' frontend/src/hooks/useWidgetDashboard.ts)" -eq 0 && cd frontend && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>event_date/event_time pins present (they double as the instrument test proving the file is readable, so the two -eq 0 counts cannot pass vacuously); zero start_datetime and zero hardcoded previousValue fabrications remain in the file (C8 note: the action forbids leaving comments naming either token); frontend type-check clean. GATE DRILL: RED observed on undone tree (exit 1 — `event_date` absent from the calendar_entries query today, the subject's own reason). GREEN: grep half constructed in scratch (synthetic hook file with pins — observed exit 0); the type-check half is UNPROVEN pre-execution (needs the real edit; a scratch file cannot type-check the workspace) — drill at execution.</acceptance_criteria>
  <done>The events query reads live columns; no code path can emit a trend that was not computed from a settled comparison.</done>
</task>

<task type="auto">
  <name>Task 2: KpiWidget renders absence, not a neutral lie</name>
  <files>frontend/src/components/dashboard-widgets/KpiWidget.tsx</files>
  <read_first>frontend/src/components/dashboard-widgets/KpiWidget.tsx in full (the trend row markup); 96-UI-SPEC.md §2 (absent row — no placeholder, no em-dash)</read_first>
  <action>
    The trend row (direction arrow + percentage + from-last-period label) renders ONLY when
    `trend` is non-null; on null the row is omitted entirely — no placeholder value, no
    em-dash, no reserved empty space styled as data. Add `data-testid="kpi-trend"` to the
    trend row root (the oracle's stable hook — UI-SPEC Verification Notes: the count is
    DOM-derivable without pixel diffing). Percentages format via lib/format-locale (Latin
    digits both locales by policy — not a defect, do not "fix"). No other visual change:
    chrome, tokens, and layout as shipped.
  </action>
  <verify>
    <automated>command grep -q 'data-testid="kpi-trend"' frontend/src/components/dashboard-widgets/KpiWidget.tsx && command grep -q 'trend' frontend/src/components/dashboard-widgets/KpiWidget.tsx && cd frontend && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>The kpi-trend testid exists on the trend row; the row's render is guarded on non-null trend (source-verified in review; the behavioural half is Task 3's spec); type-check clean. GATE DRILL: RED observed on undone tree (exit 1 — kpi-trend testid absent, the subject's own reason). GREEN: grep half constructed in scratch (observed exit 0); type-check half UNPROVEN pre-execution — drill at execution.</acceptance_criteria>
  <done>An unknown delta renders as nothing; a known delta renders as a real signed percentage.</done>
</task>

<task type="auto">
  <name>Task 3: CDP truth oracle — 96-custom-dashboard-truth.spec.ts</name>
  <files>tests/e2e/96-custom-dashboard-truth.spec.ts</files>
  <read_first>tests/e2e/95-sandbox-error.spec.ts in full — clone its four load-bearing pieces (inline auth, narrowed block + requestfailed instrument test, DOM-only assertions, INTERNAL_STRING regex); 96-PATTERNS.md §1 (the block targets /rest/v1 for this widget)</read_first>
  <action>
    Write exactly 2 tests, header `// @covers DEAD-06 (criterion 2)` on line 1 plus an
    oracle-population-definition block (the observable surface is the dev server at :5173,
    chromium-en, inline auth — the 95-family statement). Test 1 (forced trend absence): CDP
    Network.setBlockedURLs with the NARROWED pattern `*/rest/v1/unified_work_items*`, goto
    /custom-dashboard, assert: zero `data-testid="kpi-trend"` elements are visible after the
    page settles, body innerText contains no `0.0%`, body innerText does not match
    INTERNAL_STRING, and the requestfailed instrument-test recorded at least one blocked
    /rest/v1/unified_work_items request (discriminating a real force from a no-op pattern).
    Test 2 (natural settle): no block; goto /custom-dashboard; the EventsWidget region settles
    to content, a truthful empty state, or the inline error contract — never a spinner past a
    15s retry-backoff budget; body does not match INTERNAL_STRING; any VISIBLE kpi-trend
    element's text matches a signed-percentage shape (regex on % with a leading digit or
    sign), never the literal `0.0%` alongside a blocked/absent comparison. Both truthful
    outcomes pass; only the untruthful ones fail. DOM-only assertions; never assert emptiness
    as error (RLS denials read as empty 200s).
  </action>
  <verify>
    <automated>test -f tests/e2e/96-custom-dashboard-truth.spec.ts && test "$(pnpm exec playwright test tests/e2e/96-custom-dashboard-truth.spec.ts --project=chromium-en --no-deps --list 2>/dev/null | command grep -c '›')" -eq 2 && pnpm exec playwright test tests/e2e/96-custom-dashboard-truth.spec.ts --project=chromium-en --no-deps</automated>
  </verify>
  <acceptance_criteria>Spec exists first (paths are FILTERS — D-17), lists exactly 2 tests with --no-deps (hardcoded count, never list-derived), passes. GATE DRILL: RED observed on undone tree (exit 1 — test -f fails, subject absent: the spec is this task's product). GREEN UNPROVEN pre-execution — needs Tasks 1-2 landed plus the dev server; drill both directions at execution per GATE-STANDARD C1.</acceptance_criteria>
  <done>Criterion 2 has a deterministic forced-absence oracle and a natural-truth arm.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                                    |
| ------------------ | -------------------------------------------------------------- |
| PostgREST → widget | query failures must render the error contract, never 0 or 0.0% |

## STRIDE Threat Register

| Threat ID | Category               | Component               | Disposition | Mitigation Plan                                                                   |
| --------- | ---------------------- | ----------------------- | ----------- | --------------------------------------------------------------------------------- |
| T-96-07   | Information Disclosure | widget error renders    | mitigate    | inherited P93 throw→isError path; spec asserts INTERNAL_STRING never matches body |
| T-96-08   | Spoofing (of data)     | fabricated trend deltas | mitigate    | trend: null unless settled comparison; DOM-absence oracle under forced block      |
| T-96-SC   | Tampering              | package installs        | accept      | zero installs this phase                                                          |

</threat_model>

<verification>
- All three gates green (behavioural halves at execution per drill labels).
- POPULATION DEFINITION (D-15): the close covers /custom-dashboard's KPI widgets and
  EventsWidget on the dev-server surface, chromium-en; falls outside — the chart widgets'
  numeric correctness beyond rendering (they read live relations and are covered by the SC4
  seam statement as outside the agreement set), Arabic pixels (operator park).
</verification>

<success_criteria>
Criterion 2 holds behaviourally: live columns queried, chart region renders, and no trend
delta exists that was not computed from a completed request.
</success_criteria>

<output>
Create `.planning/phases/96-real-numbers/96-03-SUMMARY.md` when done. It MUST record: every
fabrication site removed (before/after line refs), the fetchTasks overdue alignment, both
spec-arm colours, and the DERIVED FIX (plan-check-1 info item): this plan's CDP oracle
overrides 96-UI-SPEC's functions/v1 block-pattern line for the PostgREST tier — the narrowed
`*/rest/v1/unified_work_items*` pattern is correct for these supabase-js reads (functions/v1
is the wrong tier for this widget) — recorded as a derived fix, not a deviation.
</output>
