---
phase: 96-real-numbers
plan: 09
type: execute
wave: 3
depends_on: [96-02, 96-08] # the stored-overdue arm + the WorkBoard.tsx export land first (shared file ⇒ later wave)
files_modified:
  - frontend/src/pages/WorkBoard/WorkBoard.tsx
  - frontend/src/pages/WorkBoard/KCard.tsx
  - frontend/src/pages/WorkBoard/__tests__/WorkBoard.test.tsx # extend :546, never duplicate
  - tests/e2e/96-overdue-badge.spec.ts # NEW
autonomous: true
requirements: [COUNT-04]
success_criterion: 'filed-finding close — COUNT-04 render half: overdue renders expressively from ONE signal; chip count == badged-card count at any instant; refusal ships unchanged'
truths:
  - "Decisions covered — D-09: overdue renders expressively as the card-level badge derived from the SAME unified signal as the toolbar chip (stored status for commitments, computed per-source formula for tasks/intake — the 96-02 winning-notion record), so chip count == badged-card count in one DOM snapshot; D-10: P94's drag-refusal interaction ships byte-unchanged (branch (a), RULING-P96-01) — no new column, no drag-target change, and any future allow-and-reflect revisit is a post-close mutation that must name this plan's gates among its changed subjects; D-19: the phase-close C9b consumer sweep runs ONLY through scripts/c9b-sweep.sh against phase-96-base, control-tested before any zero is believed, with every consumer row carrying mock-vs-real"
must_haves:
  truths:
    - 'Every card whose unified overdue signal is true renders exactly one overdue badge with a stable data-testid'
    - 'At any instant, the toolbar chip number equals the count of badged cards in the DOM (same-clock, one snapshot)'
    - 'A card in the Done column is never badged (completed work is not overdue by the unified formulas)'
    - 'The P94 refusal interaction is byte-unchanged — no drag semantics touched'
  artifacts:
    - path: 'frontend/src/pages/WorkBoard/KCard.tsx'
      provides: 'the overdue badge rendering whenever is_overdue is true (day-count variant when known, plain overdue label when not) with data-testid'
      contains: 'kcard-overdue'
    - path: 'tests/e2e/96-overdue-badge.spec.ts'
      provides: 'same-clock chip==badges DOM oracle'
      contains: '@covers COUNT-04'
  key_links:
    - from: 'frontend/src/pages/WorkBoard/WorkBoard.tsx'
      to: 'frontend/src/pages/WorkBoard/KCard.tsx'
      via: 'overdueCount and the badge both derive from item.is_overdue — one signal'
      pattern: 'is_overdue'
    - from: 'tests/e2e/96-overdue-badge.spec.ts'
      to: 'frontend/src/pages/WorkBoard/KCard.tsx'
      via: 'DOM count of data-testid=kcard-overdue vs the chip number, one snapshot'
      pattern: 'kcard-overdue'
---

<objective>
Close COUNT-04's render half: `overdue` renders expressively from ONE signal, and the
agreement between the chip and the cards is observable same-clock. After 96-02, the RPC's
`is_overdue` field IS the unified signal (stored status for commitments; per-source computed
formulas for tasks/intake). KCard already carries the overdue vocabulary (`.kcard.overdue`
class, the red mono due chip with `card.overdueBy`) — the deltas are: the badge renders even
when `days_until_due` is unknown (today the overdueBy text requires a numeric day count —
KCard.tsx:72-74 — so a known-overdue row with an unknown day count renders NO overdue text),
a stable `data-testid` makes the count DOM-derivable, and the chip and badges are proven to
share the signal.

**The prescribed shape is the card-level badge, NOT a new column** (UI-SPEC §7): a dedicated
Overdue column would create a new drag target — exactly the fork RULING-P96-01 closed as (a)
KEEP REFUSAL. No drag or mutation-path code is touched in this plan.

**Post-close mutation naming (condition 3):** extending WorkBoard.test.tsx modifies a file
that is a subject of Phase 94's closed WorkBoard gates (the drag-refusal oracles). The
extension ADDS assertions and changes none — every existing assertion stays green in the same
run; the refusal spec surface is untouched.

**Forbidden shapes, named (condition 8):** no re-bucketing of overdue cards (display-only
badge; resolveBoardStage untouched); commitment_status_history never read as user intent.

Purpose: the two-notions defect ends where the user sees it.
Output: badge + fallback label, extended unit anchor, same-clock e2e oracle, and the
phase-close C9b sweep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-real-numbers/96-RESEARCH.md (§Derivation 2 — the census this closes; the WorkBoard pins)
@.planning/phases/96-real-numbers/96-PATTERNS.md (§8 — the KCard chip vocabulary + WorkBoard.test.tsx:546 anchor)
@.planning/phases/96-real-numbers/96-UI-SPEC.md (§7 — the binding badge contract: copy keys, chrome, placement, LtrIsolate)

<interfaces>
Verified by scout 2026-08-17:
- KCard.tsx:72-74: overdueBy text ONLY when is_overdue AND days_until_due is a number; :118
  the .kcard gains class 'overdue' on is_overdue; :136 the .kdue chip gains 'is-overdue'.
- WorkBoard.tsx: overdueCount at :244-247 = visibleItems.filter(it => it.is_overdue) —
  already the same field the cards read; BoardToolbar renders it as the .overdue-chip
  (mono 11px var(--danger), board.css:225-231) via t('overdueChip', {count}).
- Copy keys (EXIST both locales): unified-kanban:overdue ("Overdue"), unified-kanban:
  overdueBy ("Overdue {{days}}d"). The badge uses overdueBy when days known, plain overdue
  when not; numeric portion LtrIsolate-wrapped in Arabic (the existing chip precedent).
- WorkBoard.test.tsx:546-552 asserts data-overdue on the mocked toolbar == '2' — EXTEND this
  block (badge-count == chip-count against the same mocked items), never duplicate the file's
  heavy mock setup (:19-118; note the :90-95 warning — mock @/components/work-creation at
  the barrel).
- C9b consumers (mock-vs-real): WorkBoard.test.tsx, KCard.test.tsx, BoardColumn.test.tsx —
  MOCKED (render-contract oracles, non-oracles for data truth — all three run in this plan's
  gates); tests/e2e/96-overdue-badge.spec.ts — REAL (this plan's own); the P94 refusal
  oracles — untouched subjects, named above.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: One signal, one badge — KCard fallback + testid; extend the unit anchors</name>
  <files>frontend/src/pages/WorkBoard/KCard.tsx, frontend/src/pages/WorkBoard/WorkBoard.tsx, frontend/src/pages/WorkBoard/__tests__/WorkBoard.test.tsx</files>
  <read_first>frontend/src/pages/WorkBoard/KCard.tsx in full (144 lines); frontend/src/pages/WorkBoard/__tests__/KCard.test.tsx (the existing overdue assertions :106-114, :153-176, :206-226 — keep green); frontend/src/pages/WorkBoard/__tests__/WorkBoard.test.tsx:19-118 (mock setup) and :536-567 (the anchor to extend); 96-UI-SPEC.md §7</read_first>
  <action>
    (1) KCard: when item.is_overdue is true and days_until_due is NOT a number, the due chip
    renders the plain unified-kanban:overdue label (colon-form; today such a row shows no
    overdue text at all — the gap). Add data-testid="kcard-overdue" to the overdue-state due
    chip element (present exactly when is_overdue — the DOM-count hook; UI-SPEC names the
    planner's testid). Chrome unchanged: the existing .kdue.is-overdue danger treatment,
    LtrIsolate on the numeric day count in Arabic, meta-row placement, var(--row-h)-derived
    card sizing. (2) WorkBoard: no signal change needed (overdueCount already filters
    it.is_overdue — the same field) — verify and leave; do NOT touch resolveBoardStage or
    any drag/mutation path (D-10). (3) WorkBoard.test.tsx: extend the :546 block — with the
    mocked items, assert the rendered count of data-testid="kcard-overdue" equals the
    toolbar's data-overdue value (badge==chip against one mocked dataset); run KCard.test.tsx
    and BoardColumn.test.tsx unchanged and keep every existing assertion green (MOCKED
    consumers — render contracts, stated non-oracles for data truth).
  </action>
  <verify>
    <automated>command grep -q 'kcard-overdue' frontend/src/pages/WorkBoard/KCard.tsx && cd frontend && pnpm exec vitest run src/pages/WorkBoard/__tests__/WorkBoard.test.tsx src/pages/WorkBoard/__tests__/KCard.test.tsx src/pages/WorkBoard/__tests__/BoardColumn.test.tsx src/pages/WorkBoard/__tests__/stage-status-parity.test.ts --reporter=basic</automated>
  </verify>
  <acceptance_criteria>The testid exists on the overdue chip; all four WorkBoard-family unit files pass in one run — the extended badge==chip assertion, every pre-existing assertion (P94 subjects stay green), and 96-08's parity test (C9 coherence: the export this run imports). GATE DRILL: RED observed on undone tree (exit 1 — kcard-overdue absent today, the subject's own reason). GREEN: grep half constructed in scratch (observed exit 0); the vitest half UNPROVEN pre-execution — needs the edits + 96-08's test on disk; drill at execution.</acceptance_criteria>
  <done>A known-overdue row always shows its badge; badge and chip provably share one signal at the unit layer.</done>
</task>

<task type="auto">
  <name>Task 2: Same-clock DOM oracle + the phase-close C9b sweep</name>
  <files>tests/e2e/96-overdue-badge.spec.ts</files>
  <read_first>tests/e2e/95-sandbox-error.spec.ts (the family shape); scripts/c9b-sweep.sh header (usage + branch order); 96-UI-SPEC.md §Verification Notes (the badge oracle)</read_first>
  <action>
    (1) Write exactly 2 tests, header `// @covers COUNT-04` plus the population-definition
    block (dev-server chromium-en; the board's visible item set under default filters — what
    filters exclude falls outside, stated). Test 1 (same-clock agreement): goto /kanban,
    settle, then in ONE evaluate pass read the toolbar overdue-chip number and count visible
    [data-testid="kcard-overdue"] elements — assert equality (condition 7 by construction:
    one DOM snapshot); also assert zero badged cards inside the Done column region. Test 2
    (natural truth): the board settles to columns with cards or truthful empty columns —
    never a spinner past a 15s budget; body does not match INTERNAL_STRING; the chip renders
    a number (Latin digits both locales by policy). (2) PHASE-CLOSE C9b SWEEP (D-19,
    condition 5): run `bash scripts/c9b-sweep.sh phase-96-base` (the tag exists once
    execution started — if MISSING TAG prints, STOP and surface it; never substitute an
    inline sweep). The script self-runs its control test; triage every candidate line to
    consumer/non-consumer with mock-vs-real noted, and record the full triage table in the
    SUMMARY — a zero-candidate result is believed only because the script's control branch
    proved the machinery sees.
  </action>
  <verify>
    <automated>test -f tests/e2e/96-overdue-badge.spec.ts && test "$(pnpm exec playwright test tests/e2e/96-overdue-badge.spec.ts --project=chromium-en --no-deps --list 2>/dev/null | command grep -c '›')" -eq 2 && pnpm exec playwright test tests/e2e/96-overdue-badge.spec.ts --project=chromium-en --no-deps</automated>
  </verify>
  <acceptance_criteria>Spec exists first, lists exactly 2 tests with --no-deps, passes; the c9b-sweep ran through the script ONLY, with its triage table (every row mock-vs-real) in the SUMMARY. GATE DRILL: RED observed on undone tree (exit 1 — test -f fails, subject absent). GREEN UNPROVEN pre-execution — needs Task 1 + 96-02/96-08 live + dev server + the phase-96-base tag; drill at execution.</acceptance_criteria>
  <done>Chip==badges is a moment-anchored observation; the phase's consumer set is swept fail-closed.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary        | Description                                             |
| --------------- | ------------------------------------------------------- |
| RPC → board DOM | is_overdue renders as state; no server text reaches JSX |

## STRIDE Threat Register

| Threat ID | Category            | Component           | Disposition | Mitigation Plan                                                            |
| --------- | ------------------- | ------------------- | ----------- | -------------------------------------------------------------------------- |
| T-96-22   | Spoofing (of state) | overdue badge/chip  | mitigate    | one signal (RPC is_overdue) for both renders; same-clock DOM oracle        |
| T-96-23   | Tampering           | drag/mutation paths | mitigate    | untouched by construction (D-10); P94 refusal oracles stay green in Task 1 |
| T-96-SC   | Tampering           | package installs    | accept      | zero installs this phase                                                   |

</threat_model>

<verification>
- Both gates green (behavioural halves at execution per drill labels).
- POPULATION DEFINITION (D-15): the badge oracle covers the board's visible item set under
  default filters on the dev-server surface; falls outside — filtered-out items (the chip
  counts visibleItems by construction — same population, stated), other surfaces' overdue
  renders (closed by 96-02/96-07), Arabic pixels (operator park).
- C9b sweep executed through the inherited script only, control-proven, triaged mock-vs-real.
</verification>

<success_criteria>
COUNT-04's render half holds: one signal, expressive badge, chip==badges same-clock, Done
never badged, refusal byte-unchanged — and the phase's consumer set is swept before close.
</success_criteria>

<output>
Create `.planning/phases/96-real-numbers/96-09-SUMMARY.md` when done. It MUST record: the
KCard fallback behaviour as shipped, the four-unit-file run result, both spec colours, and
the full c9b-sweep triage table (every row mock-vs-real).
</output>
