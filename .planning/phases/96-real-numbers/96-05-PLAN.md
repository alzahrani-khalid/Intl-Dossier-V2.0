---
phase: 96-real-numbers
plan: 05
type: execute
wave: 2
depends_on: [96-04]
files_modified:
  - frontend/src/pages/word-assistant/WordAssistantPage.tsx
  - frontend/src/i18n/en/common.json # wordAssistant.checking — same commit as ar
  - frontend/src/i18n/ar/common.json
  - tests/e2e/96-calendar-family.spec.ts # NEW — the DEAD-07 family oracle (all four surfaces)
autonomous: true
requirements: [DEAD-07]
success_criterion: '3 (completes) — /word-assistant status badge reflects a LIVE probe; the family spec proves all four DEAD-07 surfaces behaviourally'
truths:
  - "Decisions covered — D-05: the word-assistant badge reflects a LIVE probe — it initialises to a checking state and settles ONLY from the probe result, never defaulting to connected (the probe target is the file's own functions.invoke check_grammar POST, run on mount regardless of mode); D-17: the family spec asserts its own existence FIRST, hardcodes 4 tests, and runs with --no-deps including --list; D-18: no oracle depends on the e2e setup project — inline auth from .env.test creds against the single :5173 instance"
must_haves:
  truths:
    - 'The badge renders a checking state before the probe settles and NEVER shows connected without a 2xx probe response'
    - 'With the probe URL CDP-blocked, the badge settles to disconnected'
    - 'wordAssistant.checking exists in BOTH locale common.json files in the same commit'
    - 'The four DEAD-07 surfaces have one behavioural spec: grid on empty month, form mounts at /calendar/new, real weekday offset + working nav on /events, three-state badge on /word-assistant'
  artifacts:
    - path: 'frontend/src/pages/word-assistant/WordAssistantPage.tsx'
      provides: 'three-state connection pill settled from the live probe only'
      contains: 'checking'
    - path: 'tests/e2e/96-calendar-family.spec.ts'
      provides: 'the DEAD-07 family oracle — 4 hardcoded tests'
      contains: '@covers DEAD-07'
  key_links:
    - from: 'frontend/src/pages/word-assistant/WordAssistantPage.tsx'
      to: 'supabase.functions.invoke word-assistant'
      via: 'the in-file check_grammar POST as the mount-time probe'
      pattern: 'check_grammar'
    - from: 'tests/e2e/96-calendar-family.spec.ts'
      to: 'frontend/src/routes/_protected/calendar/index.tsx'
      via: 'DOM assertions on the grid/new-form/offset/badge states 96-04 produced'
      pattern: 'calendar'
---

<objective>
Complete DEAD-07 (success criterion 3): the /word-assistant badge stops lying, and the whole
calendar family gets its behavioural oracle. Research pinned the badge defect exactly:
`WordAssistantPage.tsx:57` initialises `useState(true)` (asserts connected before anything
settles), and in the default `fallback` mode (`:88`, `:127`; VITE_WORD_ASSISTANT_MODE is set
in NO env file — grep-verified) the probe is SKIPPED and isConnected forced true. The
`word-assistant` edge fn IS deployed (probe → 405 on GET = a function answering, accepts
POST).

**Forbidden shape, named (condition 8):** a status badge rendering connected (or
disconnected) while the truth is unknown is the confident-lie class — the pill's initial
state is checking, and it settles from the probe result only.

Purpose: the last DEAD-07 surface reflects reality; criterion 3 becomes observable.
Output: three-state pill + both-locale key + the 4-test family spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-real-numbers/96-RESEARCH.md (§Derivation 8 — the word-assistant mechanism + the exercised probe-target discretion)
@.planning/phases/96-real-numbers/96-UI-SPEC.md (§3 — the three-state pill table: checking/connected/disconnected chrome + keys; §Verification Notes — the badge oracle)
@.planning/phases/96-real-numbers/96-PATTERNS.md (§1 — the spec-family shape; §No Analog Found — the pill is composed, not copied)

<interfaces>
Verified by research/scout 2026-08-17:
- The probe target (discretion, exercised): the file's own
  supabase.functions.invoke('word-assistant', { body: { action: 'check_grammar', text:
  'health check' } }) POST (pinned :251-263) — run on mount regardless of assistantMode.
- The keys live in the DEFAULT namespace: frontend/src/i18n/en/common.json:501 holds the
  nested `wordAssistant` object (connected/disconnected exist there); `wordAssistant.checking`
  is NEW in the SAME object, both locales, same commit. Bare t('wordAssistant.checking')
  resolves via the default namespace — this is not cross-namespace dot-form.
- Suggested ar rendering (UI-SPEC, executor aligns glossary): جارٍ التحقق من الاتصال.
- Pill chrome (UI-SPEC table, binding): checking = bg-surface-raised text-ink-mute, dot
  bg-ink-faint; connected = bg-success/10 text-success as shipped; disconnected =
  bg-danger/10 text-danger as shipped. Rounded-full pill, dot + label, Label type (12/400).
- CDP block pattern for the spec: */functions/v1/word-assistant* (narrowed — the SPA module
  URL for WordAssistantPage.tsx carries no hyphenated word-assistant?  it DOES:
  pages/word-assistant/ — so the narrowed pattern MUST include functions/v1, exactly as
  written, never a bare *word-assistant*).
- C9b consumers (mock-vs-real): no shipped spec asserts /word-assistant DOM (research found
  none; exec-time c9b-sweep in 96-09 re-derives). The 96-04 surfaces' shipped consumers were
  triaged in 96-04.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three-state pill settled from the live probe; key in both locales</name>
  <files>frontend/src/pages/word-assistant/WordAssistantPage.tsx, frontend/src/i18n/en/common.json, frontend/src/i18n/ar/common.json</files>
  <read_first>frontend/src/pages/word-assistant/WordAssistantPage.tsx in full (the useState(true) at :57, assistantMode at :88/:127, the invoke at :251-263, the pill markup at :292-299); 96-UI-SPEC.md §3 pill table; frontend/src/i18n/en/common.json:495-520 (the wordAssistant object shape)</read_first>
  <action>
    (1) Replace the boolean connection state with a three-value union
    'checking' | 'connected' | 'disconnected', initial 'checking'. (2) On mount run the probe
    — the in-file functions.invoke check_grammar POST with the literal text 'health check' —
    REGARDLESS of assistantMode (delete the fallback-mode skip that forces connected); settle
    to connected on a 2xx (no error in the invoke result), disconnected on any error/non-2xx;
    the badge state changes ONLY from the probe settle. assistantMode keeps governing which
    backend the composer uses — it no longer fabricates connection status. (3) Pill renders
    per the UI-SPEC table (checking/connected/disconnected chrome verbatim from the
    interfaces block; label keys wordAssistant.checking / .connected / .disconnected);
    logical properties only. (4) Add "checking" to the nested wordAssistant object in BOTH
    frontend/src/i18n/en/common.json ("Checking connection") and ar/common.json (the UI-SPEC
    suggested rendering) in this same commit. Explicit return types; no floating promises
    (void the mount-effect promise properly).
  </action>
  <verify>
    <automated>node -e "const en=require('./frontend/src/i18n/en/common.json'),ar=require('./frontend/src/i18n/ar/common.json');process.exit(en.wordAssistant&&en.wordAssistant.checking&&ar.wordAssistant&&ar.wordAssistant.checking?0:1)" && command grep -q "checking" frontend/src/pages/word-assistant/WordAssistantPage.tsx && test "$(command grep -c 'useState(true)' frontend/src/pages/word-assistant/WordAssistantPage.tsx)" -eq 0 && cd frontend && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>Both locale files parse as JSON and carry wordAssistant.checking (the node -e parse is the instrument test); the page carries the checking state and zero useState(true) initialisations remain (C8 note: do not leave a comment containing the literal useState(true)); type-check clean. GATE DRILL: RED observed on undone tree (exit 1 — the node -e key check fails, subject absent: the key is this task's product). GREEN: JSON + grep halves constructed in scratch (synthetic common.json pair + page copy — observed exit 0); type-check half UNPROVEN pre-execution — drill at execution.</acceptance_criteria>
  <done>The badge starts at checking and can only reach connected through a live 2xx probe; both locales carry the new copy.</done>
</task>

<task type="auto">
  <name>Task 2: The DEAD-07 family oracle — 96-calendar-family.spec.ts</name>
  <files>tests/e2e/96-calendar-family.spec.ts</files>
  <read_first>tests/e2e/95-sandbox-error.spec.ts in full (the four load-bearing pieces to clone: inline auth, narrowed CDP block + requestfailed instrument test, DOM-only assertions, INTERNAL_STRING); 96-UI-SPEC.md §Verification Notes (the /events weekday oracle + the badge oracle)</read_first>
  <action>
    Write exactly 4 tests, header `// @covers DEAD-07 (criterion 3)` on line 1 plus the
    oracle-population-definition block (dev-server surface at :5173, chromium-en, inline
    auth). Test 1 (/calendar grid): goto /calendar; the month grid renders (weekday headers +
    day cells present as DOM elements) within a 15s budget even when the visible month has
    zero events; no full-screen wizard replaces it; no spinner persists past the budget; body
    does not match INTERNAL_STRING. Test 2 (/calendar/new mounts): goto /calendar/new; the
    create form's fields render (assert at least the form element and a labelled input via
    roles) — a parent-only render fails. Test 3 (/events offset + nav): goto /events; compute
    the displayed month's real first-weekday offset IN THE TEST via date-fns getDay on the
    heading's month, and assert the first date cell's grid column index equals it; click next
    then prev and assert the month heading changed and returned. Test 4 (/word-assistant
    badge): CDP Network.setBlockedURLs with EXACTLY '*/functions/v1/word-assistant*' (the
    narrowed pattern — a bare *word-assistant* also matches the SPA module URL and blanks the
    page); goto /word-assistant; assert the badge NEVER shows the connected label, and
    settles to the disconnected state within the budget; the requestfailed instrument-test
    records at least one blocked functions/v1/word-assistant request. DOM-only assertions
    throughout; both truthful outcomes pass where two exist; never assert emptiness as error.
  </action>
  <verify>
    <automated>test -f tests/e2e/96-calendar-family.spec.ts && test "$(pnpm exec playwright test tests/e2e/96-calendar-family.spec.ts --project=chromium-en --no-deps --list 2>/dev/null | command grep -c '›')" -eq 4 && pnpm exec playwright test tests/e2e/96-calendar-family.spec.ts --project=chromium-en --no-deps</automated>
  </verify>
  <acceptance_criteria>Spec exists first (paths are FILTERS), lists exactly 4 tests with --no-deps (hardcoded count), passes. GATE DRILL: RED observed on undone tree (exit 1 — test -f fails, subject absent: the spec is this task's product). GREEN UNPROVEN pre-execution — needs 96-04's surfaces + Task 1's pill + the dev server; drill both directions at execution per GATE-STANDARD C1.</acceptance_criteria>
  <done>Criterion 3 is behaviourally observable across all four surfaces with one deterministic spec.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary          | Description                                                   |
| ----------------- | ------------------------------------------------------------- |
| probe → badge     | connection status derives only from a real response           |
| server error → UI | probe failures render the disconnected pill, never error text |

## STRIDE Threat Register

| Threat ID | Category               | Component            | Disposition | Mitigation Plan                                                                         |
| --------- | ---------------------- | -------------------- | ----------- | --------------------------------------------------------------------------------------- |
| T-96-11   | Spoofing (of status)   | connection badge     | mitigate    | three-state pill; connected unreachable without 2xx; CDP-block oracle proves it         |
| T-96-12   | Information Disclosure | probe error handling | mitigate    | probe errors settle the pill only; no error.message reaches JSX (P93 law, spec asserts) |
| T-96-SC   | Tampering              | package installs     | accept      | zero installs this phase                                                                |

</threat_model>

<verification>
- Both gates green (behavioural halves at execution per drill labels).
- POPULATION DEFINITION (D-15): the family oracle covers the four DEAD-07 surfaces on the
  dev-server chromium-en surface; falls outside — the word-assistant composer's grammar
  functionality itself (only the badge truth is in criterion), Arabic pixels (operator park),
  production builds (devModeGuard-free surfaces unaffected here).
</verification>

<success_criteria>
Criterion 3 holds in full: grid renders empty-or-not, the form mounts, the offset and nav are
real, and the badge reflects a live probe — all proven by one 4-test spec.
</success_criteria>

<output>
Create `.planning/phases/96-real-numbers/96-05-SUMMARY.md` when done. It MUST record: the
probe-settle behaviour as implemented, the ar glossary alignment for the new key, and all four
test colours.
</output>
