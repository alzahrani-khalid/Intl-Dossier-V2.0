---
phase: 96-real-numbers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260817500001_p96_scenario_rls_recursion.sql # NEW
  - scripts/probe-scenario-rls.mjs # NEW — the two-sided row-set proof
  - tests/e2e/95-sandbox-error.spec.ts # header annotation only; shown-still-sound in the SAME task (D-12)
autonomous: true
requirements: [SANDBOX-500-01]
success_criterion: 'filed-finding close — SANDBOX-500-01: the deployed scenario-sandbox answers non-500 and /scenario-sandbox renders content or a truthful empty state; the P95 error state remains the failure branch'
truths:
  - 'Decisions covered — D-12: the sandbox is made to WORK by breaking the proven 42P17 RLS recursion (scenarios ↔ scenario_collaborators) via the P94 definer precedent, and tests/e2e/95-sandbox-error.spec.ts is shown-still-sound in the SAME task that lands the fix; D-22: the change is a migration file applied through the Supabase MCP against staging zkrcjzdemdmwhearhfgg, never ad-hoc DDL; D-14: every gate here is authored to GATE-STANDARD C1–C10 and drilled both directions or labelled UNPROVEN with what it needs'
must_haves:
  truths:
    - 'GET /rest/v1/scenarios as an authenticated user returns rows or an empty 200 — never 42P17 infinite recursion'
    - 'scripts/probe-edge-auth.sh scenario-sandbox reports a non-500 status (the FETCH_FAILED mask is gone)'
    - 'Row visibility is UNCHANGED: owner sees own+collaborated, collaborator sees exactly theirs, stranger sees none (two-sided proof on disk)'
    - 'tests/e2e/95-sandbox-error.spec.ts passes post-fix: forced arm still renders the error state; natural arm settles to content or truthful empty, never a spinner past budget'
  artifacts:
    - path: 'supabase/migrations/20260817500001_p96_scenario_rls_recursion.sql'
      provides: 'is_scenario_owner SECURITY DEFINER boolean + re-created scenario_collaborators policies with the EXISTS-over-scenarios clause replaced'
      contains: 'is_scenario_owner'
    - path: 'scripts/probe-scenario-rls.mjs'
      provides: 'two-sided row-set invariance proof (the P94 probe-report-rls.mjs protocol)'
      contains: 'UNABLE TO MEASURE'
  key_links:
    - from: 'supabase/migrations/20260817500001_p96_scenario_rls_recursion.sql'
      to: 'scenario_collaborators policies'
      via: 'is_scenario_owner(scenario_id) substituted for EXISTS (SELECT 1 FROM scenarios …)'
      pattern: 'is_scenario_owner'
    - from: 'tests/e2e/95-sandbox-error.spec.ts'
      to: 'frontend/src/routes/_protected/scenario-sandbox.tsx'
      via: 'natural arm settles to content once the 500 is gone (its header anticipates the flip)'
      pattern: 'query-error-state'
---

<objective>
Close SANDBOX-500-01 on the WORKING sandbox. The cause is DERIVED AND PROVEN (96-RESEARCH
Derivation 0): a 42P17 infinite RLS recursion between `scenarios_select_own_or_collaborated`
(EXISTS over `scenario_collaborators`) and `scenario_collaborators_select` (EXISTS over
`scenarios` back). The deployed fn source is correct — the 500 originates in PostgREST. The fix
mirrors the in-repo precedent `20260816500001_p94_report_rls_recursion.sql` exactly: a
SECURITY DEFINER STABLE single-row boolean owner check breaks ONE direction of the cycle on the
COLLABORATORS side; row sets unchanged by construction, proven two-sided.

**Post-close mutation naming (condition 3 / D-14):** this plan changes the subject of Phase
95's closed gate for plan 95-03 Task 2 (the `95-sandbox-error.spec.ts` run gate): the natural
arm's truthful outcome flips from error-state to content. The spec was written to accept both
outcomes (its header, lines 33-37); this plan re-runs it and annotates the header rather than
rewriting assertions.

Purpose: the last surface with no working state on staging starts rendering real data (or a
truthful empty state — the P95 vocabulary covers empty; an empty-but-200 sandbox is WORKING).
Output: one migration (applied), one proof script + its recorded run, the re-run 95 spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-real-numbers/96-RESEARCH.md (§Derivation 0 — the proven cause, the fix shape, Pitfall 1; §Security Domain — V4 row-visibility rule)
@.planning/phases/96-real-numbers/96-PATTERNS.md (§2 — the P94 migration copied verbatim with names adapted; §9 — the probe script posture)
@supabase/migrations/20260816500001_p94_report_rls_recursion.sql (the analog, read in full)
@scripts/probe-report-rls.mjs (the proof-script analog, read lines 1-80 for posture)
@tests/e2e/95-sandbox-error.spec.ts (the C9b consumer — read its header in full)

<interfaces>
Verified by research 2026-08-17 (live staging):
- The cycle: `scenarios_select_own_or_collaborated` (SELECT on scenarios) ↔
  `scenario_collaborators_select` (SELECT on scenario_collaborators). INSERT/UPDATE/DELETE
  policies on scenario_collaborators ALSO carry EXISTS-over-scenarios (non-recursing for
  SELECT, but the analog's three-policy sweep replaces the clause in all of them).
- Proof of cause: GET /rest/v1/scenarios with user JWT → 42P17; GET /functions/v1/scenario-sandbox
  → 500 FETCH_FAILED (the P95-recorded mask, reproduced).
- Origin migration: 20260114300001_scenario_sandbox.sql (do NOT edit it; supersede via new migration).
- Fix function signature: `public.is_scenario_owner(p_scenario_id uuid) RETURNS boolean
  LANGUAGE sql SECURITY DEFINER STABLE SET search_path = ''` returning
  `s.created_by = auth.uid() FROM public.scenarios s WHERE s.id = p_scenario_id`;
  GRANT EXECUTE to authenticated. COMMENT documenting single-row boolean scope (the P94 shape).
- C9b consumers of this change (mock-vs-real): tests/e2e/95-sandbox-error.spec.ts — REAL
  (live-login CDP spec; updated-or-shown-sound in the SAME task, D-12);
  frontend/tests/scenario-sandbox-verification.spec.ts — REAL (asserts h1/tabs only; expected
  still-sound, run once and record colour).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Break the 42P17 cycle by migration; show the 95 spec still-sound in the same task</name>
  <files>supabase/migrations/20260817500001_p96_scenario_rls_recursion.sql, tests/e2e/95-sandbox-error.spec.ts</files>
  <read_first>supabase/migrations/20260816500001_p94_report_rls_recursion.sql in full (header discipline, was-comments, definer shape); 96-RESEARCH.md §Derivation 0 and Pitfall 1; tests/e2e/95-sandbox-error.spec.ts header (lines 1-44)</read_first>
  <action>
    (1) Re-derive the LIVE policy quals first (the analog's discipline — research copy is
    evidence, not a substitute): via mcp__supabase__execute_sql run the pg_policy read from
    Derivation 0 (`SELECT polname, polcmd, pg_get_expr(polqual, polrelid),
    pg_get_expr(polwithcheck, polrelid) FROM pg_policy WHERE polrelid IN
    ('public.scenarios'::regclass, 'public.scenario_collaborators'::regclass)`), and record
    every scenario_collaborators policy verbatim in the migration's `-- was:` comments.
    (2) Author supabase/migrations/20260817500001_p96_scenario_rls_recursion.sql mirroring the
    P94 analog: create `public.is_scenario_owner(p_scenario_id uuid)` per the interfaces block
    (SECURITY DEFINER, STABLE, `SET search_path = ''`, boolean single-row owner check on
    `public.scenarios.created_by = auth.uid()`, COMMENT, GRANT EXECUTE to authenticated), then
    DROP/CREATE each scenario_collaborators policy that carries EXISTS-over-scenarios,
    substituting ONLY that clause with `public.is_scenario_owner(scenario_id)` — everything
    else verbatim. Pitfall 1 is binding: `scenarios_select_own_or_collaborated` is UNTOUCHED
    (breaking the wrong direction leaves the cycle alive or widens visibility). Header records:
    cause + live derivation date, the cycle diagram, row-set-unchanged-by-construction claim,
    and names scripts/probe-scenario-rls.mjs as the two-sided proof.
    (3) Apply via mcp__supabase__apply_migration against staging zkrcjzdemdmwhearhfgg (D-22 —
    never supabase db push, never execute_sql DDL).
    (4) SAME TASK (D-12): re-run `pnpm exec playwright test tests/e2e/95-sandbox-error.spec.ts
    --project=chromium-en --no-deps` and record the colour. The spec is designed still-sound
    (both arms accept the working state); append one dated line to its header comment noting
    the P96 flip ("2026-08-17 P96/SANDBOX-500-01: the RLS recursion is fixed; the natural arm
    now settles to content — both outcomes remain truthful") — an annotation, not an assertion
    change. If either arm is RED, attribute before proceeding: a forced-arm red means the error
    branch regressed (a real defect of this task); a natural-arm red past the budget means the
    fix did not land.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260817500001_p96_scenario_rls_recursion.sql && command grep -q 'is_scenario_owner' supabase/migrations/20260817500001_p96_scenario_rls_recursion.sql && command grep -q 'SECURITY DEFINER' supabase/migrations/20260817500001_p96_scenario_rls_recursion.sql && command grep -q "search_path = ''" supabase/migrations/20260817500001_p96_scenario_rls_recursion.sql && command grep -q -- '-- was:' supabase/migrations/20260817500001_p96_scenario_rls_recursion.sql && test "$(command grep -c 'scenarios_select_own_or_collaborated' supabase/migrations/20260817500001_p96_scenario_rls_recursion.sql | head -1)" -le 1 && command grep -q 'P96/SANDBOX-500-01' tests/e2e/95-sandbox-error.spec.ts</automated>
  </verify>
  <acceptance_criteria>Migration file exists with the definer function (pinned search_path, was-comments quoting live quals), touches the collaborators side only (the scenarios policy name may appear at most once — in a comment stating it is untouched); the 95 spec carries the dated P96 annotation and its recorded run colour is in the SUMMARY. GATE DRILL: RED observed on undone tree (exit 1 — first `test -f` fails, subject absent: the migration file is this task's product). GREEN constructed in scratch (synthetic migration file + annotated spec copy satisfying every pin — observed exit 0). The Playwright re-run half is UNPROVEN pre-execution — needs the migration applied on staging; drill at execution per GATE-STANDARD C1.</acceptance_criteria>
  <done>The 42P17 cycle is broken by an applied migration mirroring the P94 precedent; the C9b consumer spec is shown still-sound (or updated) in this same task.</done>
</task>

<task type="auto">
  <name>Task 2: Two-sided row-set proof + deploy-tier probe evidence</name>
  <files>scripts/probe-scenario-rls.mjs</files>
  <read_first>scripts/probe-report-rls.mjs in full (the analog: exit-code contract 0/1/2, .env.test fallback loading, namespaced fixtures, cleanup in finally, printed ids, no-secret rule); 96-RESEARCH.md §Security Domain (V4 row)</read_first>
  <action>
    (1) Create scripts/probe-scenario-rls.mjs mirroring probe-report-rls.mjs: exit 0 = every
    assertion held and cleanup verified; exit 1 = an assertion failed (pre-migration this is
    the 42P17 RED); exit 2 = 'UNABLE TO MEASURE — credentials absent' (GATE-STANDARD C2: not a
    red). Assertions (the P94 two-sided protocol, adapted): as user A (owner) create a
    namespaced fixture scenario (title prefixed P96-RLSPROOF-), SELECT it back (owner sees
    own); add user B as collaborator row, as B SELECT sees exactly the collaborated scenario;
    as a third authenticated identity (or B on a non-collaborated scenario) SELECT returns
    zero of A's other rows (stranger sees none). Every fixture id printed; cleanup in finally;
    never echo a key or JWT. Population definition printed in output: the proof covers
    SELECT visibility on scenarios + scenario_collaborators for three identity classes; falls
    outside: INSERT/UPDATE/DELETE behaviour (their policies changed clause-for-clause with row
    sets preserved by construction) and service-role access (bypasses RLS).
    (2) Run it post-migration; record the pass in the SUMMARY.
    (3) Run `bash scripts/probe-edge-auth.sh scenario-sandbox` — expect a non-500 status
    (200 or a truthful 4xx on a bare GET; the defect was 500). Record the line verbatim.
  </action>
  <verify>
    <automated>test -f scripts/probe-scenario-rls.mjs && command grep -q 'UNABLE TO MEASURE' scripts/probe-scenario-rls.mjs && command grep -q 'P96-RLSPROOF-' scripts/probe-scenario-rls.mjs && command grep -q 'finally' scripts/probe-scenario-rls.mjs && node --check scripts/probe-scenario-rls.mjs && node scripts/probe-scenario-rls.mjs && OUT=$(bash scripts/probe-edge-auth.sh scenario-sandbox) && printf '%s\n' "$OUT" | command grep -q 'scenario-sandbox -> ' && ! printf '%s\n' "$OUT" | command grep -q 'scenario-sandbox -> 500'</automated>
  </verify>
  <acceptance_criteria>Proof script exists with the labelled exit-2 posture, namespaced fixtures and finally-cleanup; `node --check` parses it; its live run exits 0 (all three identity classes hold); the probe line shows a non-500 status with the positive shape pin proving the probe emitted output. GATE DRILL: RED observed on undone tree (exit 1 — `test -f` fails, subject absent: the script is this task's product; and on the pre-migration tree the script run itself is the 42P17 RED, the correct red for its subject). GREEN: the static half constructed in scratch (synthetic script satisfying pins, node --check green); the live-run + probe halves are UNPROVEN pre-execution — need the applied migration and .env.test credentials; drill at execution.</acceptance_criteria>
  <done>Row-set invariance is proven two-sided on disk; the deployed function answers non-500 with recorded probe evidence.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary            | Description                                                        |
| ------------------- | ------------------------------------------------------------------ |
| authenticated → RLS | policy quals are the row-visibility gate this plan rewrites        |
| server error → DOM  | the FETCH_FAILED envelope and PG codes must never render (P93 law) |

## STRIDE Threat Register

| Threat ID | Category               | Component                          | Disposition | Mitigation Plan                                                                                                    |
| --------- | ---------------------- | ---------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------ |
| T-96-01   | Elevation of Privilege | is_scenario_owner SECURITY DEFINER | mitigate    | boolean-only, single-row scope, `SET search_path = ''`, COMMENT documenting scope, GRANT to authenticated only     |
| T-96-02   | Information Disclosure | scenario_collaborators policies    | mitigate    | clause-for-clause substitution preserves row sets; scripts/probe-scenario-rls.mjs proves three identity classes    |
| T-96-03   | Information Disclosure | sandbox error render               | mitigate    | inherited P95 QueryErrorState remains the failure branch; the 95 spec's INTERNAL_STRING assertion re-run this plan |
| T-96-SC   | Tampering              | package installs                   | accept      | zero installs this phase (UI-SPEC registry-safety)                                                                 |

</threat_model>

<verification>
- Migration pins green (definer + was-comments + collaborators-side-only + spec annotation).
- probe-scenario-rls.mjs exit 0 (two-sided proof); probe-edge-auth non-500 recorded verbatim.
- 95-sandbox-error.spec.ts 2/2 post-fix, colour recorded (C9b, REAL consumer).
- frontend/tests/scenario-sandbox-verification.spec.ts run once, colour recorded (C9b triage).
- POPULATION DEFINITION (D-15): the close covers the scenario-sandbox function + the scenarios/
  scenario_collaborators SELECT path for authenticated users on staging; falls outside —
  production (droplet untouched), whether the test user sees any rows post-fix (an
  empty-but-200 sandbox is a truthful WORKING state, RESEARCH Open Question 1).
</verification>

<success_criteria>
SANDBOX-500-01 closes on the WORKING sandbox: PostgREST answers scenarios reads without 42P17,
the deployed fn answers non-500, row visibility is proven unchanged, and the P95 error state
remains the failure branch (forced arm still green).
</success_criteria>

<output>
Create `.planning/phases/96-real-numbers/96-01-SUMMARY.md` when done. It MUST record: the live
pg_policy quals as re-derived, the probe-edge-auth line verbatim, both spec colours, the
probe-scenario-rls run result, and whether the sandbox rendered content or truthful-empty.
</output>
