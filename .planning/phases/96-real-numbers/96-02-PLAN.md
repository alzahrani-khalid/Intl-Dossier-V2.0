---
phase: 96-real-numbers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260817500002_p96_commitment_overdue_insert_gap.sql # NEW
  - supabase/migrations/20260817500003_p96_rpc_count_truth.sql # NEW
autonomous: true
requirements: [COUNT-04, COUNT-03, DEAD-05]
success_criterion: 'filed-finding close — COUNT-04 DB half (INSERT gap + one stored notion) + SC5 DB half (kanban Done can fill) + the DEAD-05 fulfillment-bucket coupling (RESEARCH Open Question 3, resolved)'
truths:
  - 'Decisions covered — D-08: COUNT-03 is VERIFY-not-build — this plan writes NO new sync; its only tasks-related change is kanban RPC read semantics, and commitment_status_history is never treated as user-intent evidence; D-09: the INSERT gap closes by re-timing commitment_overdue_check to BEFORE INSERT OR UPDATE via migration, and the two-notions seam is unified with the winning notion stated per surface (RULING-P96-01 condition 3); D-10: the refusal interaction ships byte-unchanged — branch (a); D-22: both changes are migration files applied through the Supabase MCP, aa_commitments five-value lifecycle never renamed'
must_haves:
  truths:
    - 'INSERT of a past-due aa_commitments row returns status=overdue (the trigger now fires on INSERT)'
    - 'get_commitment_fulfillment overdue bucket counts stored status=overdue (today it is blind to all 8 stored rows and would go permanently 0 after the INSERT-gap fix)'
    - 'get_unified_work_kanban includes completed rows so the Done column can fill (the decided SC5 semantics), and its commitments-arm is_overdue reads the stored status'
    - 'The kanban intake arm and the unified_work_items view compute overdue with ONE formula (the flat 3-day divergence is gone)'
  artifacts:
    - path: 'supabase/migrations/20260817500002_p96_commitment_overdue_insert_gap.sql'
      provides: 'commitment_overdue_check re-timed to BEFORE INSERT OR UPDATE + the stated decision on the 2 existing past-due pending rows'
      contains: 'BEFORE INSERT OR UPDATE'
    - path: 'supabase/migrations/20260817500003_p96_rpc_count_truth.sql'
      provides: 'get_commitment_fulfillment bucket fix + get_unified_work_kanban/get_kanban_column_counts Done semantics + stored-overdue commitments arm + unified intake SLA formula'
      contains: 'get_unified_work_kanban'
  key_links:
    - from: 'supabase/migrations/20260817500002_p96_commitment_overdue_insert_gap.sql'
      to: 'aa_commitments'
      via: 'DROP TRIGGER IF EXISTS + CREATE TRIGGER commitment_overdue_check (name unchanged — alphabetical fire order before commitment_status_audit is load-bearing)'
      pattern: 'commitment_overdue_check'
    - from: 'supabase/migrations/20260817500003_p96_rpc_count_truth.sql'
      to: 'get_commitment_fulfillment'
      via: "overdue bucket re-pointed to status = 'overdue'"
      pattern: 'get_commitment_fulfillment'
---

<objective>
Close the DB half of COUNT-04 and the DB preconditions of SC5 and DEAD-05, as ONE coupled
change set (96-RESEARCH Open Question 3, resolved in-session): the INSERT-gap fix would
silently zero `get_commitment_fulfillment`'s overdue series forever (its filter counts
`pending`/`in_progress` past-due only — blind to stored `overdue`), so the bucket fix lands
WITH the trigger re-timing, in this plan, before the analytics repoint (96-06) ships that chart.

**The winning-notion record (RULING-P96-01 condition 3 — the unification's own record):**

- COMMITMENTS, on every surface (kanban card badge + toolbar chip, /commitments tabs, the
  analytics fulfillment chart): the STORED `aa_commitments.status='overdue'` wins. The trigger
  is authoritative — bidirectional on UPDATE, and after this plan also INSERT-time.
- TASKS and INTAKE, on the board and /my-work: the COMPUTED deadline comparison wins — no
  stored overdue notion exists for either; the formula is unified per source type (tasks:
  `sla_deadline < now()` not completed/cancelled; intake: the urgency-scaled SLA the
  unified_work_items view already uses — the kanban RPC's flat `submitted_at + 3 days` is
  replaced by it).
- The refusal interaction ships byte-unchanged (branch (a), D-10). A future allow-and-reflect
  revisit is a post-close mutation that must NAME the gates whose subjects it changes — at
  minimum this plan's gates and 96-09's badge/chip oracles.

**SC5 Done-column semantics, DECIDED (RESEARCH Open Question 2):** the kanban Done column
SHOWS completed work. `get_unified_work_kanban`'s tasks and commitments arms change their
exclusion from `status NOT IN ('completed','cancelled')` to `status <> 'cancelled'`, making
the existing `completed → 'done'` column_key mapping live; `get_kanban_column_counts` matches.
No recency bound (staging volumes are small; a later bound would be a post-close mutation
naming this plan's gates and 96-07/96-09's agreement oracles as its changed subjects). The
intake arm's own lifecycle (`converted`/`closed`) is unchanged — a stated seam, not a drop.
**Reconciliation rule for SC4 (stated here, where the population changes — revision 1):**
this filter change makes the board's TOTAL population structurally UNEQUAL to the SC4
active-work surfaces (the board now holds completed rows in Done that no other compared
surface counts). The rule: the SC4 kanban leg compares the board's ACTIVE population — rows
in columns excluding Done (cancelled is never returned) — the same-work subset, measured
same-clock by 96-07 Task 3's kanban leg (Test 4) with the completed-rows-in-Done seam and the
client-identity seam stated in its capture (ACCEPTANCE condition 7's seam-stated branch).

**Forbidden shapes, named (condition 8):** no client-side re-implementation of what the
trigger enforces (this plan touches only DB objects); `commitment_status_history` is never
read as user-intent evidence (BEFORE triggers fire alphabetically — the audit reads the
REWRITTEN status by design).

Purpose: one stored fact, one notion per source type, a Done column that can fill.
Output: two migrations, applied to staging with recorded same-clock verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-real-numbers/96-RESEARCH.md (§Derivation 2 — the trigger census + the six-formula table; §Derivation 3 — the Done starvation; §Pitfall 3 — history re-fire; §Code Examples)
@.planning/phases/96-real-numbers/96-PATTERNS.md (§3 — the trigger re-timing shape + the operations_hub_rpcs CREATE OR REPLACE shape)
@supabase/migrations/20260330000001_operations_hub_rpcs.sql (RPC migration conventions: SECURITY DEFINER + GRANT shape)

<interfaces>
Verified by research 2026-08-17 (live staging catalog):
- aa_commitments triggers: `commitment_overdue_check` BEFORE UPDATE (the INSERT blindness —
  structural proof); `commitment_status_audit` BEFORE UPDATE OF status; live data: 8 stored
  overdue (all past-due) + 2 past-due pending (the register's two rows).
- `get_commitment_fulfillment` overdue bucket (prosrc-verified):
  `COUNT(*) FILTER (WHERE status IN ('pending','in_progress') AND due_date < NOW())` — blind
  to stored overdue.
- `get_unified_work_kanban` + `get_kanban_column_counts`: hand-rolled unions, all arms filter
  `status NOT IN ('completed','cancelled')`; column_key CASE maps completed→'done' (dead
  code today); intake overdue hardcodes `submitted_at + 3 days` vs the view's urgency-scaled
  SLA (24/48/72h/7d).
- Identity mechanism (checker-verified 2026-08-17): both kanban RPCs read `auth.uid()` and
  RAISE 'Not authenticated' when NULL, and NEITHER signature carries p_user_id
  (20251207000001_fix_kanban_intake_status_mapping.sql:17-22,51,54-55;
  20260206200005_kanban_pagination.sql:20,24) — an MCP SQL session or bare service-role call
  cannot execute them; any execution-time readback of them runs as an authenticated user
  (the .env.test TEST_USER, D-18 inline auth).
- A1 (Assumptions Log): `check_commitment_overdue()`'s INSERT-safety (no OLD reference) is
  ASSUMED from the register's quoted body — the full prosrc MUST be re-read before authoring.
- Latest kanban RPC bodies live across supabase/migrations/2026020620000*.sql — re-derive the
  CURRENT prosrc from pg_proc at execution time and CREATE OR REPLACE from that, never from a
  stale migration copy.
- C9b consumers of these RPC changes (mock-vs-real): frontend/src/hooks/useUnifiedKanban.ts
  (REAL — runtime consumer, its TypeScript row types must still fit: the change adds rows with
  status='completed', no column changes); WorkBoard.test.tsx / KCard.test.tsx /
  BoardColumn.test.tsx (MOCKED — non-oracles for data truth, run by 96-09);
  tests/e2e/96-count-agreement.spec.ts + 96-overdue-badge.spec.ts (REAL — produced by 96-07 /
  96-09, both later waves: producer before consumer).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-time commitment_overdue_check to BEFORE INSERT OR UPDATE</name>
  <files>supabase/migrations/20260817500002_p96_commitment_overdue_insert_gap.sql</files>
  <read_first>96-RESEARCH.md §Derivation 2 and §Pitfall 3; 96-PATTERNS.md §3 (the exact DROP/CREATE shape); the FULL live prosrc of check_commitment_overdue() pulled via mcp__supabase__execute_sql BEFORE authoring (A1: confirm no OLD reference — if OLD IS referenced, guard the branch with TG_OP checks and record the deviation)</read_first>
  <action>
    (1) Via mcp__supabase__execute_sql pull `SELECT prosrc FROM pg_proc WHERE proname =
    'check_commitment_overdue'` and paste it into the migration header as a `-- was:` record.
    Confirm INSERT-safety (A1). (2) Author
    supabase/migrations/20260817500002_p96_commitment_overdue_insert_gap.sql:
    `DROP TRIGGER IF EXISTS commitment_overdue_check ON public.aa_commitments;` then
    `CREATE TRIGGER commitment_overdue_check BEFORE INSERT OR UPDATE ON public.aa_commitments
    FOR EACH ROW EXECUTE FUNCTION public.check_commitment_overdue();` — the trigger NAME is
    unchanged (alphabetical fire order before commitment_status_audit is load-bearing, D-08).
    (3) The stated decision on existing rows (Pitfall 3), recorded in the header AND executed:
    the 2 existing past-due `pending` rows ARE migrated by a touch-UPDATE in the same
    migration (`UPDATE public.aa_commitments SET updated_at = updated_at WHERE status =
    'pending' AND due_date < CURRENT_DATE`) — the trigger coerces them to overdue and
    commitment_status_audit records the coercion, which is truthful by design (the history
    records what the DB decided — never read as user intent). POPULATION DEFINITION in the
    header: rows with status='pending' AND due_date < CURRENT_DATE at apply time (2 at
    research time — a floor, re-derived); falls outside: rows whose due_date passes later
    without a write (now covered at INSERT and every UPDATE), other statuses.
    (4) Apply via mcp__supabase__apply_migration. (5) Verify with recorded same-clock SQL (one
    statement): INSERT a namespaced past-due row (title 'P96-INSERTGAP-CHECK', CHECKs already
    verified in Derivation 4 for dossiers; for aa_commitments derive its CHECK list via
    pg_constraint FIRST per house rule) RETURNING status — expect 'overdue' — then DELETE it
    in the same session; and `SELECT status, count(*) FROM aa_commitments GROUP BY 1` showing
    zero past-due pending remain. Record both results verbatim in the SUMMARY.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260817500002_p96_commitment_overdue_insert_gap.sql && command grep -q 'BEFORE INSERT OR UPDATE' supabase/migrations/20260817500002_p96_commitment_overdue_insert_gap.sql && command grep -q 'commitment_overdue_check' supabase/migrations/20260817500002_p96_commitment_overdue_insert_gap.sql && command grep -q -- '-- was:' supabase/migrations/20260817500002_p96_commitment_overdue_insert_gap.sql && command grep -qi 'POPULATION' supabase/migrations/20260817500002_p96_commitment_overdue_insert_gap.sql</automated>
  </verify>
  <acceptance_criteria>Migration exists pinning the re-timed trigger under its unchanged name, carries the was-record of the live prosrc and the stated population definition for the data touch. The INSERT-returns-overdue behavioural half runs as recorded SQL via MCP at execution (UNPROVEN in this gate by construction — staging state cannot be constructed in scratch). GATE DRILL: RED observed on undone tree (exit 1 — test -f fails, subject absent: the file is this task's product). GREEN constructed in scratch (synthetic migration satisfying all five pins — observed exit 0).</acceptance_criteria>
  <done>The trigger fires on INSERT; the two register rows are coerced with the decision stated; the behavioural proof is recorded same-clock SQL.</done>
</task>

<task type="auto">
  <name>Task 2: RPC count truth — fulfillment bucket, Done semantics, stored-overdue arm, one intake SLA</name>
  <files>supabase/migrations/20260817500003_p96_rpc_count_truth.sql</files>
  <read_first>The CURRENT prosrc of get_commitment_fulfillment, get_unified_work_kanban, get_kanban_column_counts pulled via mcp__supabase__execute_sql (never a stale migration copy); the unified_work_items view definition (for the urgency-scaled intake SLA formula to copy verbatim); 20260330000001_operations_hub_rpcs.sql:275-351 (SECURITY DEFINER + GRANT shape)</read_first>
  <action>
    Author supabase/migrations/20260817500003_p96_rpc_count_truth.sql with three
    CREATE OR REPLACE FUNCTION statements, each starting from the live prosrc with the
    minimal diff, each keeping its existing SECURITY posture and re-stating GRANT EXECUTE:
    (1) get_commitment_fulfillment: the overdue bucket becomes
    `COUNT(*) FILTER (WHERE status = 'overdue')` — the stored notion wins for commitments
    (RULING-P96-01 condition 3; after Task 1 the past-due pending class is empty at rest).
    Other buckets unchanged.
    (2) get_unified_work_kanban: tasks and commitments arms change `status NOT IN
    ('completed','cancelled')` to `status <> 'cancelled'` (Done fills — the decided SC5
    semantics); the commitments arm's is_overdue expression becomes `status = 'overdue'`;
    the intake arm's overdue expression adopts the urgency-scaled SLA formula copied verbatim
    from the unified_work_items view (replacing the flat 3-day rule). Intake lifecycle
    filtering unchanged (stated seam).
    (3) get_kanban_column_counts: the same filter change so its per-column counts (including
    done) agree with the board rows by construction.
    Migration header states each function's filter seams before/after (D-15: which relation,
    which filter, who else reads a different one — cite the Derivation 2/5 tables) AND the
    SC4 reconciliation rule from the objective: the board's TOTAL population now includes
    completed rows in Done; the SC4-compared quantity is the board's ACTIVE population
    (column_key <> 'done'), measured by 96-07 Test 4 with the seam stated.
    Apply via mcp__supabase__apply_migration. Verify with recorded same-clock evidence:
    (a) the DEPLOYED FUNCTION itself in one MCP SQL statement (checker pass-3 — the fn is
    SECURITY DEFINER with no auth.uid() reference, so a direct call with defaults executes
    under MCP and is population-commensurable): `SELECT (SELECT overdue FROM
    get_commitment_fulfillment()) AS a, (SELECT count(*) FROM aa_commitments WHERE
    status='overdue') AS b` — assert a == b, so the post-apply observation measures the
    RPC's own overdue bucket, never a plain-SQL re-derivation of it; (b) the done-column readback
    measures the RPC ITSELF — get_unified_work_kanban reads auth.uid() and RAISES 'Not
    authenticated' when NULL, with NO p_user_id parameter (see interfaces), so MCP SQL and
    bare service-role cannot call it: run it through an AUTHENTICATED supabase-js client
    signed in as the .env.test TEST_USER (TEST_USER_EMAIL/TEST_USER_PASSWORD, the D-18
    inline-auth route — a scratch node script is fine; password and keys never echoed),
    count returned rows with column_key='done', and record the number. Given staging has
    completed tasks it should be > 0 (3 repaired rows arrive in 96-08 — at THIS plan's close
    the done-column population is whatever staging holds FOR THE TEST_USER; record the
    number, never assert a literal). The single-call readback has no cross-call seam; the
    client identity (TEST_USER through auth.uid()) is stated with the recorded result.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260817500003_p96_rpc_count_truth.sql && command grep -q 'get_commitment_fulfillment' supabase/migrations/20260817500003_p96_rpc_count_truth.sql && command grep -q 'get_unified_work_kanban' supabase/migrations/20260817500003_p96_rpc_count_truth.sql && command grep -q 'get_kanban_column_counts' supabase/migrations/20260817500003_p96_rpc_count_truth.sql && command grep -q "status = 'overdue'" supabase/migrations/20260817500003_p96_rpc_count_truth.sql && command grep -q 'GRANT EXECUTE' supabase/migrations/20260817500003_p96_rpc_count_truth.sql</automated>
  </verify>
  <acceptance_criteria>One migration re-creates all three RPCs from live prosrc with the four decided truth changes and re-stated grants; the header states each filter seam and the SC4 reconciliation rule. The parity SQL (a) runs via MCP and the done-column readback (b) runs through the authenticated TEST_USER client at execution (both UNPROVEN in this gate — staging state). GATE DRILL (re-drilled at revision 2 — the gate command is BYTE-UNCHANGED from revision 1; only the action's verify-(b) mechanism prose changed, stated per GATE-STANDARD): RED re-observed on undone tree 2026-08-17 (exit 1 — test -f fails, subject absent). GREEN re-constructed in scratch 2026-08-17 (synthetic migration satisfying all six pins, path-adapted — observed exit 0).</acceptance_criteria>
  <done>One stored notion for commitments everywhere, one computed formula per other source type, a Done column that can fill, and a fulfillment chart the INSERT-gap fix cannot silently zero.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary            | Description                                                      |
| ------------------- | ---------------------------------------------------------------- |
| client → RPC        | the three RPCs are SECURITY DEFINER read paths for authenticated |
| trigger → audit row | history rows record coerced values by design (never user intent) |

## STRIDE Threat Register

| Threat ID | Category               | Component                           | Disposition | Mitigation Plan                                                                                          |
| --------- | ---------------------- | ----------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------- |
| T-96-04   | Elevation of Privilege | CREATE OR REPLACE on definer RPCs   | mitigate    | start from live prosrc, minimal diff, GRANT re-stated to authenticated only, no new tables read          |
| T-96-05   | Tampering              | data touch-UPDATE on aa_commitments | mitigate    | population-scoped UPDATE (past-due pending only), stated in header, trigger performs the actual coercion |
| T-96-06   | Repudiation            | commitment_status_history           | accept      | history records DB-decided values by design; the plans never read it as user intent (condition 8)        |
| T-96-SC   | Tampering              | package installs                    | accept      | zero installs this phase                                                                                 |

</threat_model>

<verification>
- Both migration gates green (static pins); both applied via MCP with recorded same-clock
  verification results in the SUMMARY (SQL via MCP where no auth.uid() dependency exists; the
  kanban done-column readback via the authenticated TEST_USER client — the RPC reads
  auth.uid() and takes no p_user_id; behavioural halves labelled UNPROVEN until then).
- POPULATION DEFINITIONS (D-15): Task 1 header (past-due pending rows at apply time); Task 2
  header (per-RPC filter seams before/after; falls outside — the unified_work_items VIEW
  bodies themselves, unchanged this plan; /custom-dashboard's direct view reads, stated
  outside the SC4 set).
- The SC4 reconciliation rule this plan's filter change necessitates is stated in the
  objective and the Task 2 migration header, and MEASURED by 96-07 Test 4 (the kanban
  active-population leg — never a claimed-but-unmeasured surface).
- Producer-before-consumer: 96-06 (fulfillment chart), 96-07 (count agreement), 96-08 (Done
  oracle), 96-09 (badge/chip) all consume these DB states in later waves.
</verification>

<success_criteria>
INSERT of past-due carries overdue; stored overdue is the one commitments notion on every
surface; the Done column can fill; the fulfillment chart counts the stored fact. All proven by
recorded same-clock evidence (SQL via MCP; the kanban done-column readback through the
authenticated TEST_USER client), populations stated.
</success_criteria>

<output>
Create `.planning/phases/96-real-numbers/96-02-SUMMARY.md` when done. It MUST record: the full
was-prosrc of all four DB objects, the A1 confirmation, the INSERT-returns-overdue result, the
fulfillment parity batch result, the done-column row count observed through the authenticated
TEST_USER client, and the winning-notion table restated.
</output>
