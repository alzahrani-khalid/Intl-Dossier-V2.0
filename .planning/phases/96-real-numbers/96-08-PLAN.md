---
phase: 96-real-numbers
plan: 08
type: execute
wave: 2
depends_on: [96-02] # the decided Done semantics (kanban RPC) must be live for the lands-in-Done oracle
files_modified:
  - frontend/src/pages/WorkBoard/WorkBoard.tsx # export STAGE_TO_STATUS only
  - frontend/src/pages/WorkBoard/__tests__/stage-status-parity.test.ts # NEW
  - supabase/functions/tasks-update/index.ts
  - backend/src/services/tasks.service.ts
  - supabase/functions/assignments-my-assignments/index.ts
  - supabase/migrations/20260817500005_p96_task_stage_status_repair.sql # NEW — data repair only
autonomous: true
requirements: [COUNT-03]
success_criterion: '5 (second half) — a completed task leaves the dashboard Overdue widget and lands in kanban Done; status and workflow_stage stay in sync'
truths:
  - 'Decisions covered — D-08: COUNT-03 closes by VERIFY-not-build — trg_sync_task_status exists and is confirmed by catalog read; the work is the parity oracle between the live CASE and the client STAGE_TO_STATUS copies (they agree by AUTHORSHIP, not construction), the status-direct writer sweep, and repairing the 3 already-divergent rows; NO new sync is written client-side or DB-side; D-23: every derivation uses command grep or explicit file args, every zero is instrument-tested, exit codes are captured directly, and loops use bash semantics'
must_haves:
  truths:
    - 'The exported STAGE_TO_STATUS map, the tasks-update edge fn copy, and the live trigger CASE agree cell-for-cell on all five pairs — proven by a unit test plus a recorded prosrc derivation'
    - 'Zero tasks rows diverge (workflow_stage vs the trigger-derived status) after the data repair — re-derived by the recorded GROUP BY'
    - 'No writer updates tasks.status without the matching workflow_stage — the three flagged writers route intent through workflow_stage (the trigger derives status), never a TS re-derivation of status'
    - 'A completed task lands in kanban Done (the decided 96-02 semantics), observable on staging'
    - "A completed task leaves the dashboard's Overdue widget — the /my-work WorkSummaryHeader Overdue stat card — closed as the STATED VIEW INVARIANT: unified_work_items' tasks arm computes is_overdue only when status is not completed/cancelled/done, asserted by recorded same-clock SQL (zero completed-and-overdue rows per source arm)"
  artifacts:
    - path: 'frontend/src/pages/WorkBoard/__tests__/stage-status-parity.test.ts'
      provides: 'the five hardcoded stage→status pairs asserted against the exported client map'
      contains: 'STAGE_TO_STATUS'
    - path: 'supabase/migrations/20260817500005_p96_task_stage_status_repair.sql'
      provides: 'population-scoped repair of the 3 divergent rows (stage follows completed status)'
      contains: 'workflow_stage'
  key_links:
    - from: 'frontend/src/pages/WorkBoard/__tests__/stage-status-parity.test.ts'
      to: 'frontend/src/pages/WorkBoard/WorkBoard.tsx'
      via: 'import of the exported STAGE_TO_STATUS'
      pattern: 'STAGE_TO_STATUS'
    - from: 'supabase/functions/tasks-update/index.ts'
      to: 'trg_sync_task_status'
      via: 'status-intent writes carry workflow_stage; the trigger derives status'
      pattern: 'workflow_stage'
---

<objective>
Close COUNT-03 (success criterion 5, second half) by VERIFY-not-build. The sync trigger
EXISTS — `trg_sync_task_status` BEFORE UPDATE on tasks derives status from workflow_stage
(done also stamping completed_at); research captured its live definition. The work is
confirming the invariant and fixing what breaks it: (a) the invariant is ALREADY VIOLATED on
staging — 3 rows at workflow_stage='todo' with status='completed' (completed tasks bucketed
Todo — the register's "kanban Done can fill" defect made flesh); (b) three status-direct
writers leave stage stale (`tasks-update/index.ts:212`, `tasks.service.ts:371`,
`assignments-my-assignments/index.ts:122`); (c) TWO independent TS copies of the trigger's
mapping exist (WorkBoard.tsx:83-89 AND tasks-update/index.ts:200) — they agree with the
trigger by AUTHORSHIP, not construction, so the parity oracle covers both.

**SC5's Overdue-widget clause, named and closed (revision 1):** the widget is the /my-work
WorkSummaryHeader "Overdue" stat card (`WorkSummaryHeader.tsx:40-45`, rendering
`summary.overdue_count` ← `user_work_summary.overdue_count` = `count(*) FILTER (WHERE
is_overdue)` over `unified_work_items` — migration
`20260610000001_capture_unified_work_stack.sql:142`); the kanban toolbar overdue chip shares
the same arm formulas post-96-02. The clause closes as a STATED INVARIANT with its
derivation: `unified_work_items`' tasks arm computes `is_overdue` as `status::text <> ALL
(ARRAY['completed','cancelled','done']) AND sla_deadline IS NOT NULL AND sla_deadline <
now()` (`20260610000001_capture_unified_work_stack.sql:79-82`) — a completed task CANNOT
appear in the widget's population by construction. "By construction" is then ASSERTED, not
presumed: Task 1 records the same-clock SQL (zero rows where status='completed' AND
is_overdue, per source arm). Falls outside: commitments-only widgets (the dashboard
`OverdueCommitments.tsx` — commitments' overdue is the stored notion, owned by 96-02);
/custom-dashboard's separate computed overdue metric (outside per 96-03's population — its
formula is aligned there); `MyTasks.tsx`'s per-row due labels (per-task date state, not an
overdue-count widget).

**Forbidden shape, named (condition 8):** a client-side re-implementation of what the DB
trigger enforces. The writer fix direction is therefore: writers express INTENT as
workflow_stage (inverse-mapped from the requested status where the API accepts status) and
let the trigger derive status — never a TS derivation of status from stage as the source of
truth. The existing dual-write in useUnifiedKanban:398-426 is the benign precedent (the
trigger derives the same value).

Purpose: completion means the same thing in every column of every surface.
Output: parity test + prosrc derivation, three writer fixes with deploy evidence, one
data-repair migration, and the Overdue-widget invariant record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-real-numbers/96-RESEARCH.md (§Derivation 3 — the live trigger def, the broken rows, the writer census, the Done starvation; §Pattern 2)
@.planning/phases/96-real-numbers/96-PATTERNS.md (§4 — the vitest harness + assertion shape; §8 — the dual-write pattern)

<interfaces>
Verified by research/scout 2026-08-17:
- The five pairs (live CASE, register-confirmed): todo→pending, in_progress→in_progress,
  review→review, done→completed (+completed_at when null), cancelled→cancelled; the trigger
  fires only when workflow_stage IS DISTINCT FROM OLD. Tasks have NO INSERT-time sync (only
  set_task_sla_deadline fires BEFORE INSERT) — a stated population edge, mitigated by the
  writer rule, not by a new trigger (verify-not-build).
- STAGE_TO_STATUS lives at WorkBoard.tsx:83-89 (not currently exported — export it; the
  parity test imports it). tasks-update/index.ts:200 carries its OWN copy (STAGE_TO_STATUS
  in the fn) — Deno code, not importable by vitest: its parity is proven by a recorded
  side-by-side in the derivation, not the unit test.
- The three writers: tasks-update/index.ts:212 (status write without stage when
  body.status set and body.workflow_stage undefined — note :192-200 already derives status
  FROM stage in the other direction); tasks.service.ts:371; assignments-my-assignments/
  index.ts:122. Fix: each accepts the status intent, sets workflow_stage =
  STATUS_TO_STAGE[status] (pending→todo, in_progress→in_progress, review→review,
  completed→done, cancelled→cancelled) alongside, letting the trigger derive status — the
  dual-write precedent. Introduce the token STATUS_TO_STAGE in each writer (the gate pins it).
- The divergence population (live): workflow_stage='todo' + status='completed' ×3. Repair
  direction: user intent was completion (status was directly written) — set
  workflow_stage='done' and let the trigger re-derive/stamp.
- The Overdue-widget invariant chain (revision 1): unified_work_items tasks arm is_overdue
  (migration 20260610000001:79-82, the <> ALL completed/cancelled/done guard) →
  user_work_summary.overdue_count (:142 count FILTER is_overdue) → unified-work-list edge fn
  → useUnifiedWork.ts → WorkSummaryHeader.tsx:40-45 (the named widget).
- The kanban drag path (useUnifiedKanban.ts:398-426) writes BOTH fields — benign, untouched.
- C9b consumers (mock-vs-real): WorkBoard.test.tsx / KCard.test.tsx / BoardColumn.test.tsx —
  MOCKED (insulated; run by 96-09); tests/e2e/06-work-item-crud.spec.ts — REAL (asserts task
  CRUD flows; run once after Task 2's edge redeploys, record colour, update in-task if red
  for a correct change); the tasks-update fn is ALSO consumed by the kanban drag mutation at
  runtime (REAL — its request shape is unchanged: both fields accepted before and after).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parity oracle + invariant repair — the verify half</name>
  <files>frontend/src/pages/WorkBoard/WorkBoard.tsx, frontend/src/pages/WorkBoard/__tests__/stage-status-parity.test.ts, supabase/migrations/20260817500005_p96_task_stage_status_repair.sql</files>
  <read_first>frontend/src/pages/WorkBoard/WorkBoard.tsx:80-120 (the map + resolveBoardStage); frontend/src/pages/WorkBoard/__tests__/KCard.test.tsx:15-39 (the harness — the parity test itself is pure data, no render); 96-PATTERNS.md §4; the live trigger prosrc via mcp__supabase__execute_sql (the Derivation 3 query, re-run)</read_first>
  <action>
    (1) Export STAGE_TO_STATUS from WorkBoard.tsx (add export keyword — no other change to
    the file in this plan; 96-09 owns the render edits, sequenced after). (2) Create
    stage-status-parity.test.ts: hardcode the five pairs and assert the exported map equals
    them exactly (Object.entries length 5 + per-pair equality) — a drift in the client copy
    fails it; a drift in the trigger fails the recorded derivation below. No react/i18n
    mocks needed (pure data). (3) Recorded derivation (via MCP, pasted in the SUMMARY):
    re-pull trg_sync_task_status's prosrc and lay the live CASE beside the exported map AND
    beside tasks-update/index.ts:200's copy, cell-for-cell — the three-way parity record
    (POPULATION: the five workflow_stage values; outside: INSERT-time, where no trigger
    fires — stated). (4) Author + apply
    supabase/migrations/20260817500005_p96_task_stage_status_repair.sql: UPDATE
    public.tasks SET workflow_stage = 'done' WHERE status = 'completed' AND workflow_stage
    NOT IN ('done') — population stated in the header (3 rows at research time, a floor,
    re-derived at apply); the trigger re-derives status (same value) and stamps
    completed_at where null. Then the recorded invariant check: the Derivation 3 GROUP BY
    returns ZERO rows where the pair diverges from the five-pair table. (5) The
    Overdue-widget invariant record (revision 1 — SC5's clause): pull the unified_work_items
    view definition via MCP and paste the tasks arm's is_overdue CASE into the SUMMARY
    beside the migration-pinned formula (20260610000001:79-82 — the <> ALL
    completed/cancelled/done guard), then run the recorded same-clock assertion in ONE
    statement: `SELECT source, count(*) FROM unified_work_items WHERE status = 'completed'
    AND is_overdue GROUP BY source` — MUST return zero rows (per source arm; an empty result
    is believed because the view was just read and the formula derivation shows why). This
    closes "a completed task leaves the /my-work WorkSummaryHeader Overdue stat card" as a
    derived-and-asserted invariant, never "probably true by construction". Falls outside
    (stated): commitments-only widgets (OverdueCommitments.tsx — stored notion, 96-02);
    /custom-dashboard's computed metric (96-03's population). Data repair only — no schema,
    no new trigger (verify-not-build).
  </action>
  <verify>
    <automated>command grep -q 'export const STAGE_TO_STATUS' frontend/src/pages/WorkBoard/WorkBoard.tsx && test -f frontend/src/pages/WorkBoard/__tests__/stage-status-parity.test.ts && test -f supabase/migrations/20260817500005_p96_task_stage_status_repair.sql && command grep -qi 'POPULATION' supabase/migrations/20260817500005_p96_task_stage_status_repair.sql && cd frontend && pnpm exec vitest run src/pages/WorkBoard/__tests__/stage-status-parity.test.ts --reporter=basic</automated>
  </verify>
  <acceptance_criteria>The map is exported; the parity test exists and passes (5 pairs hardcoded — a mapping drift on either side of the client copy fails it); the repair migration exists with its stated population; the three-way prosrc parity record, the zero-divergence GROUP BY, AND the Overdue-widget invariant record (view-arm formula derivation + the zero-rows completed-and-overdue assertion per source arm, same-clock) are in the SUMMARY. GATE DRILL: RED observed on undone tree (exit 1 — the export does not exist today, the subject's own reason). GREEN: grep + file halves constructed in scratch (observed exit 0); the vitest run half UNPROVEN pre-execution — needs the real export + test; drill at execution. (The invariant assertion is recorded MCP SQL like the other derivations — UNPROVEN in this gate by construction, staging state.)</acceptance_criteria>
  <done>The invariant is confirmed, its violations are repaired with a stated population, every mapping copy is pinned to the trigger, and the named Overdue widget provably cannot show a completed task.</done>
</task>

<task type="auto">
  <name>Task 2: The status-direct writers route intent through workflow_stage</name>
  <files>supabase/functions/tasks-update/index.ts, backend/src/services/tasks.service.ts, supabase/functions/assignments-my-assignments/index.ts</files>
  <read_first>supabase/functions/tasks-update/index.ts:100-230 in full (it ALREADY derives status from stage at :192-200 — understand both directions before editing); backend/src/services/tasks.service.ts:340-400; supabase/functions/assignments-my-assignments/index.ts:100-140; frontend/src/hooks/useUnifiedKanban.ts:398-426 (the benign dual-write precedent)</read_first>
  <action>
    In each of the three writers, wherever tasks.status is written without workflow_stage:
    introduce STATUS_TO_STAGE (pending→todo, in_progress→in_progress, review→review,
    completed→done, cancelled→cancelled) and set workflow_stage = STATUS_TO_STAGE[status]
    alongside the status intent — the trigger then derives status from stage (same value by
    the Task 1 parity) and stamps completed_at. NEVER derive status from stage in TS as the
    source of truth (forbidden shape; tasks-update's existing :192-200 stage→status
    convenience derivation may remain — it writes both fields, the benign dual-write — but
    must not gain new call sites). Grep each file for EVERY status write site before editing
    (the type-discriminator-drift lesson: plans name one site, more exist — enumerate with
    command grep -n "status" per file and classify each write, recording the census in the
    SUMMARY). Redeploy both edge fns via Supabase CLI/MCP and capture
    `bash scripts/probe-edge-auth.sh tasks-update assignments-my-assignments` — every line
    must show a non-401 status and no '404-kind: gateway'. Run
    tests/e2e/06-work-item-crud.spec.ts once (C9b, REAL); record colour; repair in-task if
    red for a correct change.
  </action>
  <verify>
    <automated>command grep -q 'STATUS_TO_STAGE' supabase/functions/tasks-update/index.ts && command grep -q 'STATUS_TO_STAGE' backend/src/services/tasks.service.ts && command grep -q 'STATUS_TO_STAGE' supabase/functions/assignments-my-assignments/index.ts && OUT=$(bash scripts/probe-edge-auth.sh tasks-update assignments-my-assignments) && printf '%s\n' "$OUT" | command grep -q 'tasks-update -> ' && ! printf '%s\n' "$OUT" | command grep -q ' -> 401' && ! printf '%s\n' "$OUT" | command grep -q '404-kind: gateway'</automated>
  </verify>
  <acceptance_criteria>All three writers carry the STATUS_TO_STAGE routing (the token is NEW in each file — pinned); both edge fns redeployed with probe evidence (positive shape pin + no 401 + no gateway-404); the per-file status-write census is in the SUMMARY; 06-work-item-crud colour recorded. GATE DRILL: RED observed on undone tree (exit 1 — STATUS_TO_STAGE absent from all three files today, the subject's own reason). GREEN: grep half constructed in scratch (observed exit 0); the probe half UNPROVEN pre-execution — needs the redeploys; drill at execution.</acceptance_criteria>
  <done>No write path can desynchronise the pair; deploys are evidenced, not assumed.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary         | Description                                              |
| ---------------- | -------------------------------------------------------- |
| client → edge fn | task writes cross auth + validation in the fns edited    |
| repair migration | data UPDATE on tasks — population-scoped, trigger-driven |

## STRIDE Threat Register

| Threat ID | Category    | Component             | Disposition | Mitigation Plan                                                                                               |
| --------- | ----------- | --------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- |
| T-96-19   | Tampering   | writer edits in 3 fns | mitigate    | intent routed through validated workflow_stage values only (VALID_WORKFLOW_STAGES stays); no auth-path change |
| T-96-20   | Tampering   | data-repair migration | mitigate    | population-scoped WHERE, header-stated, trigger performs derivation; applied via MCP only                     |
| T-96-21   | Repudiation | completed_at stamping | accept      | the trigger stamps it as designed; history semantics unchanged                                                |
| T-96-SC   | Tampering   | package installs      | accept      | zero installs this phase                                                                                      |

</threat_model>

<verification>
- Both gates green (behavioural halves at execution per drill labels).
- POPULATION DEFINITIONS (D-15): parity — the five stage values (outside: INSERT-time, stated
  + writer-mitigated); repair — completed-status rows with non-done stage at apply time (a
  floor); writer census — every status write site per file, enumerated and classified in the
  SUMMARY (the named three are a floor, never assumed the total).
- Overdue-widget invariant (revision 1): the named widget is the /my-work WorkSummaryHeader
  Overdue stat card (+ the kanban chip, same formulas); population — unified_work_items'
  per-arm is_overdue formulas, asserted zero completed-and-overdue rows per arm same-clock;
  falls outside — commitments-only widgets (OverdueCommitments.tsx, stored notion per 96-02)
  and /custom-dashboard's separate computed metric (96-03's population).
- The lands-in-kanban-Done behavioural half is observed via 96-09's board oracles + the
  96-02 done-column SQL (this plan's repair is what populates Done truthfully).
</verification>

<success_criteria>
Criterion 5's second half holds: the pair cannot silently diverge (writers routed, parity
pinned three ways), the existing divergence is repaired with a stated population, completed
tasks can land in Done, and the named Overdue widget provably excludes completed tasks by the
derived-and-asserted view invariant.
</success_criteria>

<output>
Create `.planning/phases/96-real-numbers/96-08-SUMMARY.md` when done. It MUST record: the
three-way parity table, the zero-divergence GROUP BY result, the per-file status-write census,
the probe lines verbatim, the 06-work-item-crud colour, and the Overdue-widget invariant
record (the view-arm is_overdue formula beside the migration pin + the zero-rows
completed-and-overdue assertion result per source arm).
</output>
