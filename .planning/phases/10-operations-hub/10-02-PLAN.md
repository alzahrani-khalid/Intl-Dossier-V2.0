---
phase: 10-operations-hub
plan: 02
type: execute
wave: 2
depends_on: ['10-01']
files_modified:
  - frontend/src/pages/Dashboard/components/AttentionZone.tsx
  - frontend/src/pages/Dashboard/components/AttentionItem.tsx
  - frontend/src/pages/Dashboard/components/EmptyAttention.tsx
  - frontend/src/pages/Dashboard/components/TimelineZone.tsx
  - frontend/src/pages/Dashboard/components/TimelineEventCard.tsx
  - frontend/src/pages/Dashboard/components/EngagementsZone.tsx
  - frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx
  - frontend/src/pages/Dashboard/components/QuickStatsBar.tsx
  - frontend/src/pages/Dashboard/components/QuickStatCard.tsx
  - frontend/src/pages/Dashboard/components/ActivityFeed.tsx
  - frontend/src/pages/Dashboard/components/ActivityFeedItem.tsx
autonomous: true
requirements:
  - OPS-01
  - OPS-02
  - OPS-03
  - OPS-04

must_haves:
  truths:
    - 'Attention zone renders overdue items with red severity styling and due-soon with yellow'
    - 'Timeline zone groups events into Today, Tomorrow, This Week, Next Week sections'
    - 'Engagements zone shows collapsible sections per lifecycle stage with count badges'
    - 'Quick Stats bar shows 4 metric cards with icons and click-through navigation'
    - 'Activity feed shows recent actions in reverse chronological order'
    - 'Empty Attention zone shows green success banner per D-03'
  artifacts:
    - path: 'frontend/src/pages/Dashboard/components/AttentionZone.tsx'
      provides: 'Attention items list with severity grouping'
      min_lines: 30
    - path: 'frontend/src/pages/Dashboard/components/AttentionItem.tsx'
      provides: 'Individual attention card with cva severity variants'
      contains: 'attentionItemVariants'
    - path: 'frontend/src/pages/Dashboard/components/TimelineZone.tsx'
      provides: 'Day-grouped timeline with show-all expand'
      min_lines: 40
    - path: 'frontend/src/pages/Dashboard/components/EngagementsZone.tsx'
      provides: 'Lifecycle stage groups with collapsible sections'
      min_lines: 30
    - path: 'frontend/src/pages/Dashboard/components/QuickStatsBar.tsx'
      provides: '4-card stats grid with navigation'
      min_lines: 20
    - path: 'frontend/src/pages/Dashboard/components/ActivityFeed.tsx'
      provides: 'Recent activity list'
      min_lines: 20
  key_links:
    - from: 'AttentionItem.tsx'
      to: 'useNavigate'
      via: 'onClick navigates to entity route'
      pattern: 'useNavigate|navigate'
    - from: 'QuickStatCard.tsx'
      to: 'router'
      via: 'onClick navigates to filtered list view'
      pattern: 'navigate'
    - from: 'EngagementStageGroup.tsx'
      to: 'Collapsible'
      via: 'shadcn Collapsible component for expand/collapse'
      pattern: 'Collapsible'
---

<objective>
Build all 5 zone UI components and their subcomponents for the Operations Hub dashboard. Each zone is self-contained, receives data via props, and handles its own loading/empty/error states.

Purpose: These are the visual building blocks that Plan 03 will compose into the full OperationsHub page layout. Building them as isolated components enables independent testing and reuse.
Output: 11 React components covering all 5 dashboard zones
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-operations-hub/10-CONTEXT.md
@.planning/phases/10-operations-hub/10-UI-SPEC.md
@.planning/phases/10-operations-hub/10-01-SUMMARY.md

@frontend/src/domains/operations-hub/types/operations-hub.types.ts
@frontend/src/components/ui/card.tsx
@frontend/src/components/ui/badge.tsx
@frontend/src/components/ui/button.tsx
@frontend/src/components/ui/skeleton.tsx
@frontend/src/components/ui/collapsible.tsx
@frontend/src/components/ui/separator.tsx

<interfaces>
<!-- Types from Plan 01 that zone components consume -->

From frontend/src/domains/operations-hub/types/operations-hub.types.ts:

```typescript
export type AttentionSeverity = 'red' | 'orange' | 'yellow'
export type AttentionItemType =
  | 'overdue_work'
  | 'due_soon_work'
  | 'sla_at_risk'
  | 'stalled_engagement'
export interface AttentionItemData {
  id
  item_type
  severity
  title
  title_ar
  entity_id
  entity_type
  deadline
  days_overdue
  days_in_stage
  lifecycle_stage
  engagement_name
  engagement_name_ar
}
export interface TimelineEvent {
  id
  title
  title_ar
  start_date
  end_date
  event_type
  engagement_id
  engagement_name
  engagement_name_ar
  lifecycle_stage
}
export type GroupedEvents = Record<TimelineGroup, TimelineEvent[]>
export interface StageGroup {
  stage: LifecycleStage
  stage_count: number
  top_engagements: StageEngagement[]
}
export interface DashboardStats {
  active_engagements
  open_tasks
  sla_at_risk
  upcoming_week
}
export interface ActivityItemData {
  id
  action
  entity_type
  entity_id
  entity_name
  entity_name_ar
  actor_name
  created_at
  metadata
}
```

From UI-SPEC Severity Color Contract:

```
red:    border-destructive/50  bg-destructive/5  hover:bg-destructive/10  text-destructive
orange: border-warning/50      bg-warning/5      hover:bg-warning/10      text-warning
yellow: border-yellow-500/50   bg-yellow-500/5   hover:bg-yellow-500/10   text-yellow-600
```

From UI-SPEC Quick Stat icons:

```
Active Engagements → Briefcase
Open Tasks         → CheckSquare
SLA At Risk        → AlertTriangle (alert badge when value > 0)
Upcoming This Week → CalendarDays
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AttentionZone + AttentionItem + EmptyAttention + TimelineZone + TimelineEventCard</name>
  <files>
    frontend/src/pages/Dashboard/components/AttentionZone.tsx
    frontend/src/pages/Dashboard/components/AttentionItem.tsx
    frontend/src/pages/Dashboard/components/EmptyAttention.tsx
    frontend/src/pages/Dashboard/components/TimelineZone.tsx
    frontend/src/pages/Dashboard/components/TimelineEventCard.tsx
  </files>
  <read_first>
    frontend/src/domains/operations-hub/types/operations-hub.types.ts
    frontend/src/components/ui/card.tsx
    frontend/src/components/ui/badge.tsx
    frontend/src/components/ui/skeleton.tsx
    frontend/src/components/ui/separator.tsx
    frontend/src/components/ui/button.tsx
    frontend/src/components/ui/ltr-isolate.tsx
    .planning/phases/10-operations-hub/10-UI-SPEC.md
    .planning/phases/10-operations-hub/10-CONTEXT.md
  </read_first>
  <action>
**All components use:** `useTranslation('operations-hub')` for i18n, `useDirection()` for RTL, logical Tailwind properties only (`ms-*`, `me-*`, `ps-*`, `pe-*`, `text-start`, `text-end`), explicit return types on all functions, no semicolons, single quotes, trailing commas.

**1. `AttentionItem.tsx`** — Props: `{ item: AttentionItemData, onClick: () => void }`.

Define `attentionItemVariants` using `cva` from `class-variance-authority`:

```
base: 'flex items-center gap-3 rounded-lg border p-3 sm:p-4 transition-colors cursor-pointer min-h-11'
variants.severity:
  red: 'border-destructive/50 bg-destructive/5 hover:bg-destructive/10'
  orange: 'border-warning/50 bg-warning/5 hover:bg-warning/10'
  yellow: 'border-yellow-500/50 bg-yellow-500/5 hover:bg-yellow-500/10'
defaultVariants: { severity: 'yellow' }
```

Component renders: severity badge (using Badge with text from `t(`severity.${severityKey}`)` where severityKey maps item_type to overdue/due_soon/sla_at_risk/stalled), item title (use `i18n.language === 'ar' && item.title_ar ? item.title_ar : item.title`), and deadline/days info. For stalled items, show `t('severity.stalled_detail', { days: Math.floor(item.days_in_stage ?? 0), stage: t(`stages.${item.lifecycle_stage}`) })`. Wrap deadline dates in `<LtrIsolate>`. Badge text color per severity: red = `text-destructive`, orange = `text-warning`, yellow = `text-yellow-600`. Use `role="button"` and `tabIndex={0}` with `onKeyDown` for Enter/Space. `aria-label` describing the item.

**2. `EmptyAttention.tsx`** — No props.
Renders green success banner per UI-SPEC: `<div className="flex items-center gap-2 border-success/50 bg-success/5 rounded-lg p-3 sm:p-4">`. Lucide `CircleCheck` icon (20px) with `text-success`. Text: `<span className="text-success font-semibold text-sm">{t('zones.attention.empty')}</span>`.

**3. `AttentionZone.tsx`** — Props: `{ items: AttentionItemData[], isLoading: boolean, isError: boolean, onRetry: () => void }`.

- Loading: 3 Skeleton cards with `className="h-16 rounded-lg"` per skeleton, wrapped with faint severity-colored borders
- Error: inline error card with `t('error.load_failed', { zone: t('zones.attention.title') })` and retry Button calling `onRetry`
- Empty (items.length === 0 and not loading): render `<EmptyAttention />`
- Data: map items sorted by severity order (red first, then orange, then yellow per D-13) into `<AttentionItem>` components. Each item's `onClick` calls `navigate()` to the appropriate route based on `entity_type`: task -> `/tasks/${entity_id}`, intake -> `/intake/${entity_id}`, engagement -> `/engagements/${entity_id}`, commitment -> `/tasks/${entity_id}`.
- Container: `role="region"` with `aria-label={t('zones.attention.title')}`, `aria-live="polite"` per accessibility contract.

**4. `TimelineEventCard.tsx`** — Props: `{ event: TimelineEvent, onClick: () => void }`.
Renders: time (formatted from `start_date` using `date-fns` `format(parseISO(event.start_date), 'HH:mm')`), wrapped in `<LtrIsolate>`. Event title (bilingual). Engagement name as muted subtext if present. Type icon from lucide based on `event_type`. Container: `flex items-center gap-3 p-3 rounded-lg hover:bg-muted/50 cursor-pointer min-h-11 transition-colors`. `role="button"`, `tabIndex={0}`, keyboard support.

**5. `TimelineZone.tsx`** — Props: `{ events: GroupedEvents, isLoading: boolean, isError: boolean, onRetry: () => void }`.

- Loading: 2 skeleton day groups, each with 2 event card skeletons
- Error: inline error card with retry
- Empty (all groups empty): muted text `t('zones.timeline.empty')`
- Data: iterate day groups in order (today, tomorrow, this_week, next_week). For each non-empty group: render section header with `t(`zones.timeline.${group}`)` label using `text-sm font-normal text-muted-foreground`. Show max 5 events per section. If group has more than 5, show "Show all events" button that toggles `showAll` state (per-group) to remove the limit (inline expand per UI-SPEC interaction contract). Separate day groups with `<Separator />`.
- Container: `role="region"` with `aria-label={t('zones.timeline.title')}`.
  </action>
  <verify>
  <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && pnpm --filter frontend exec tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria> - frontend/src/pages/Dashboard/components/AttentionItem.tsx contains `attentionItemVariants` (cva severity system) - frontend/src/pages/Dashboard/components/AttentionItem.tsx contains `border-destructive/50` (red severity class) - frontend/src/pages/Dashboard/components/AttentionItem.tsx contains `border-warning/50` (orange severity class) - frontend/src/pages/Dashboard/components/AttentionItem.tsx contains `border-yellow-500/50` (yellow severity class) - frontend/src/pages/Dashboard/components/AttentionItem.tsx contains `min-h-11` (44px touch target) - frontend/src/pages/Dashboard/components/AttentionItem.tsx contains `useTranslation('operations-hub')` (i18n) - frontend/src/pages/Dashboard/components/EmptyAttention.tsx contains `border-success/50` and `bg-success/5` - frontend/src/pages/Dashboard/components/EmptyAttention.tsx contains `CircleCheck` (lucide icon) - frontend/src/pages/Dashboard/components/AttentionZone.tsx contains `aria-live="polite"` (accessibility) - frontend/src/pages/Dashboard/components/AttentionZone.tsx contains `role="region"` (ARIA landmark) - frontend/src/pages/Dashboard/components/TimelineZone.tsx contains `role="region"` - frontend/src/pages/Dashboard/components/TimelineEventCard.tsx contains `min-h-11` - frontend/src/pages/Dashboard/components/TimelineEventCard.tsx contains `LtrIsolate` (time numbers) - No `ml-` or `mr-` or `text-left` or `text-right` in any component (RTL compliance) - TypeScript compiles cleanly
  </acceptance_criteria>
  <done>AttentionZone renders severity-colored items sorted red > orange > yellow with green empty banner. TimelineZone groups events by day with show-all expand. All components are accessible, RTL-safe, bilingual, and have loading/error/empty states.</done>
  </task>

<task type="auto">
  <name>Task 2: EngagementsZone + QuickStatsBar + ActivityFeed + subcomponents</name>
  <files>
    frontend/src/pages/Dashboard/components/EngagementsZone.tsx
    frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx
    frontend/src/pages/Dashboard/components/QuickStatsBar.tsx
    frontend/src/pages/Dashboard/components/QuickStatCard.tsx
    frontend/src/pages/Dashboard/components/ActivityFeed.tsx
    frontend/src/pages/Dashboard/components/ActivityFeedItem.tsx
  </files>
  <read_first>
    frontend/src/domains/operations-hub/types/operations-hub.types.ts
    frontend/src/types/lifecycle.types.ts
    frontend/src/components/ui/card.tsx
    frontend/src/components/ui/badge.tsx
    frontend/src/components/ui/collapsible.tsx
    frontend/src/components/ui/skeleton.tsx
    frontend/src/components/ui/ltr-isolate.tsx
    .planning/phases/10-operations-hub/10-UI-SPEC.md
    .planning/phases/10-operations-hub/10-CONTEXT.md
  </read_first>
  <action>
**All components use:** `useTranslation('operations-hub')` for i18n, logical Tailwind properties only, explicit return types, no semicolons, single quotes, trailing commas.

**1. `EngagementStageGroup.tsx`** — Props: `{ stage: LifecycleStage, engagements: StageEngagement[], count: number }`.
Uses shadcn `Collapsible` component. Header row: stage label from `LIFECYCLE_STAGE_LABELS[stage][i18n.language === 'ar' ? 'ar' : 'en']`, count `Badge` with `text-sm font-semibold`. Chevron icon that rotates on expand (use `isRTL ? 'rotate-180' : ''` for RTL directional icon per CLAUDE.md Rule 5). Header is `min-h-11` touch target. Expanded content: list of engagements (max 5 by default). Each engagement row: engagement name (bilingual), type badge, click navigates to `/engagements/${engagement.id}`. "Show all engagements" link if count > 5, triggers inline expand (removes limit). Use `aria-expanded` on header, `aria-controls` linking to content div.

**2. `EngagementsZone.tsx`** — Props: `{ stages: StageGroup[], isLoading: boolean, isError: boolean, onRetry: () => void }`.

- Loading: 3 skeleton stage group headers
- Error: inline error with retry
- Empty (no stages or all counts zero): muted text `t('zones.engagements.empty')` plus Button link "New Engagement"
- Data: iterate stages in `LIFECYCLE_STAGES` order (intake -> preparation -> briefing -> execution -> follow_up -> closed). For each stage present in data, render `<EngagementStageGroup>`. Skip stages with 0 count.
- Container: `role="region"` with `aria-label={t('zones.engagements.title')}`.

**3. `QuickStatCard.tsx`** — Props: `{ label: string, value: number, icon: LucideIcon, onClick: () => void, alertBadge?: boolean, isLoading?: boolean }`.
Renders Card component. Content: icon (24px, `text-primary`), metric value wrapped in `<LtrIsolate>` with `text-4xl sm:text-5xl font-semibold` (maps to `--text-metric` 56px at sm+ breakpoint — use `text-[56px]` to match UI-SPEC Display size), and label text `text-sm font-normal text-muted-foreground`. If `alertBadge` is true and value > 0, show small red dot/badge in top-end corner. Container: `cursor-pointer hover:bg-muted/50 transition-colors min-h-11 p-4`. `aria-label="{value} {label}"` for screen readers.

**4. `QuickStatsBar.tsx`** — Props: `{ stats: DashboardStats, isLoading: boolean }`.
Grid: `grid grid-cols-2 sm:grid-cols-4 gap-4`. 4 QuickStatCard instances:

- Active Engagements: icon=`Briefcase`, value=`stats.active_engagements`, onClick navigates to `/engagements`
- Open Tasks: icon=`CheckSquare`, value=`stats.open_tasks`, onClick navigates to `/tasks`
- SLA At Risk: icon=`AlertTriangle`, value=`stats.sla_at_risk`, alertBadge=true, onClick navigates to `/intake`
- Upcoming This Week: icon=`CalendarDays`, value=`stats.upcoming_week`, onClick navigates to `/calendar`
  Loading state: 4 skeleton cards with pulsing number placeholder.

**5. `ActivityFeedItem.tsx`** — Props: `{ item: ActivityItemData }`.
Renders: actor name, action description, entity name (bilingual, clickable to entity route), relative timestamp using `date-fns` `formatDistanceToNow`. Layout: `flex items-start gap-3 py-2`. Timestamp in `text-sm text-muted-foreground`, wrapped in `<LtrIsolate>`.

**6. `ActivityFeed.tsx`** — Props: `{ items: ActivityItemData[], isLoading: boolean, isError: boolean, onRetry: () => void }`.

- Loading: 5 skeleton activity rows
- Error: inline error with retry
- Empty: muted text `t('zones.activity.empty')`
- Data: render max 10 items as `<ActivityFeedItem>` in reverse chronological order (already sorted by API)
- Container: `role="region"` with `aria-label={t('zones.activity.title')}`.
  </action>
  <verify>
  <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && pnpm --filter frontend exec tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria> - frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx contains `Collapsible` (shadcn component) - frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx contains `aria-expanded` - frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx contains `LIFECYCLE_STAGE_LABELS` (bilingual labels) - frontend/src/pages/Dashboard/components/EngagementsZone.tsx contains `role="region"` - frontend/src/pages/Dashboard/components/QuickStatCard.tsx contains `LtrIsolate` (metric numbers LTR) - frontend/src/pages/Dashboard/components/QuickStatCard.tsx contains `aria-label` (screen reader support) - frontend/src/pages/Dashboard/components/QuickStatCard.tsx contains `min-h-11` (touch target) - frontend/src/pages/Dashboard/components/QuickStatsBar.tsx contains `grid-cols-2 sm:grid-cols-4` (responsive grid) - frontend/src/pages/Dashboard/components/QuickStatsBar.tsx contains `Briefcase` and `CheckSquare` and `AlertTriangle` and `CalendarDays` - frontend/src/pages/Dashboard/components/ActivityFeed.tsx contains `role="region"` - frontend/src/pages/Dashboard/components/ActivityFeedItem.tsx contains `formatDistanceToNow` (relative time) - No `ml-` or `mr-` or `text-left` or `text-right` in any component - TypeScript compiles cleanly
  </acceptance_criteria>
  <done>EngagementsZone shows collapsible stage groups ordered by lifecycle. QuickStatsBar renders 4 metric cards with click-through navigation. ActivityFeed shows recent items with relative timestamps. All components are accessible, RTL-safe, and have loading/error/empty states.</done>
  </task>

</tasks>

<verification>
- `pnpm --filter frontend exec tsc --noEmit` passes
- All 11 component files exist in `frontend/src/pages/Dashboard/components/`
- No physical direction classes (`ml-`, `mr-`, `text-left`, `text-right`) in any new component
- All zone containers have `role="region"` and `aria-label`
- All interactive elements have `min-h-11` touch targets
</verification>

<success_criteria>

- 11 zone components render their data with loading, error, and empty states
- Severity system uses cva variants matching UI-SPEC color contract exactly
- All text is bilingual via i18n namespace
- All metric numbers wrapped in LtrIsolate
- Components are accessible with ARIA landmarks, live regions, and keyboard support
- TypeScript compiles cleanly
  </success_criteria>

<output>
After completion, create `.planning/phases/10-operations-hub/10-02-SUMMARY.md`
</output>
