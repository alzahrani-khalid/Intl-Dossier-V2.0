---
phase: 64-new-position-from-dossier
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/domains/positions/hooks/usePositionTypes.ts
  - frontend/src/domains/positions/hooks/useAudienceGroups.ts
  - frontend/src/domains/positions/repositories/positions.repository.ts
  - frontend/src/domains/positions/types/index.ts
  - frontend/src/domains/positions/index.ts
  - frontend/src/i18n/en/positions.json
  - frontend/src/i18n/ar/positions.json
autonomous: true
requirements: [POSNEW-01, POSNEW-02]

must_haves:
  truths:
    - 'usePositionTypes and useAudienceGroups read the lookup tables directly via the supabase client (RLS SELECT verified open to authenticated users) with 30-minute staleTime'
    - 'translateContent posts to /translate-content via apiPost and THROWS on any non-2xx — the deployed edge returns 503 when AnythingLLM is down, and callers treat any throw as "translation unavailable" (D-07; shared api-client behavior unchanged)'
    - 'Every create_dialog and validation i18n key exists in BOTH en/positions.json and ar/positions.json (missing AR keys silently fall back to English — project memory)'
    - 'positions:dossier_tab.create_position reads "Create position"/"إنشاء موقف" (the current "Attach Position" value becomes a lie after D-13) and dossier_tab.attach_existing exists in both languages'
  artifacts:
    - path: 'frontend/src/domains/positions/hooks/usePositionTypes.ts'
      provides: 'Lookup query hook for position_types'
      contains: "queryKey: ['position-types']"
    - path: 'frontend/src/domains/positions/hooks/useAudienceGroups.ts'
      provides: 'Lookup query hook for audience_groups'
      contains: "queryKey: ['audience-groups']"
    - path: 'frontend/src/domains/positions/repositories/positions.repository.ts'
      provides: 'translateContent edge wrapper'
      contains: 'translateContent'
    - path: 'frontend/src/i18n/en/positions.json'
      provides: 'create_dialog + validation key blocks'
      contains: 'create_dialog'
  key_links:
    - from: 'frontend/src/domains/positions/hooks/usePositionTypes.ts'
      to: 'position_types table'
      via: 'supabase client PostgREST read'
      pattern: "from\\('position_types'\\)"
    - from: 'frontend/src/domains/positions/repositories/positions.repository.ts'
      to: 'translate-content edge function'
      via: 'apiPost'
      pattern: 'apiPost.*translate-content'
    - from: 'frontend/src/domains/positions/index.ts'
      to: 'new lookup hooks'
      via: 'barrel re-export'
      pattern: 'usePositionTypes'
---

<objective>
Build the foundation layer the dialog consumes: two lookup hooks (position types, audience groups), the `translateContent` repository wrapper with its request/response types, barrel exports, and ALL new i18n keys (EN+AR) for the create dialog, validation messages, and the D-13 tab relabel.

Purpose: POSNEW-01 needs real lookup data for the type picker and audience checkboxes; D-07/D-08 need a typed translate wrapper; every UI string must exist in both bundles before the dialog renders.
Output: two new hooks, extended repository + types + barrel, complete bilingual key set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/64-new-position-from-dossier/64-PATTERNS.md

<interfaces>
<!-- Live-verified lookup row shapes (RESEARCH probes #7/#8, staging 2026-06-12) -->

```typescript
// position_types — 2 rows on staging (Standard Position / موقف قياسي, Critical Position / موقف حرج)
interface PositionTypeRow {
  id: string
  name_en: string
  name_ar: string
  approval_stages: number
}

// audience_groups — 4 rows on staging (All Staff/جميع الموظفين, Management, Policy Officers, External Relations)
interface AudienceGroupRow {
  id: string
  name_en: string
  name_ar: string
}
```

<!-- Deployed translate-content v2 contract (RESEARCH Pattern 4 — verified vs live deploy, NOT the repo source) -->

```typescript
// POST /translate-content  { text (≤10,000 chars), direction: 'en_to_ar' | 'ar_to_en', content_type: 'title' | 'content' }
// 2xx → { translated_text, confidence, source_language, target_language, metadata }
// 503 when AnythingLLM is down (currently true on staging) → apiPost THROWS (handleResponse rejects all non-2xx)
```

<!-- Existing repository wrapper shape to mirror (positions.repository.ts L76-78) -->

```typescript
export async function createPosition(data: CreatePositionRequest): Promise<Position> {
  return apiPost<Position>('/positions-create', data)
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create usePositionTypes and useAudienceGroups lookup hooks + barrel exports</name>
  <files>frontend/src/domains/positions/hooks/usePositionTypes.ts, frontend/src/domains/positions/hooks/useAudienceGroups.ts, frontend/src/domains/positions/index.ts</files>
  <read_first>
    - frontend/src/hooks/useDossierPositionLinks.ts (the direct-supabase-client useQuery analog — key shape, queryFn with throw-on-error, staleTime, exported result interface at L81-88)
    - frontend/src/domains/positions/index.ts (the barrel — match its existing export style exactly)
    - .planning/phases/64-new-position-from-dossier/64-PATTERNS.md (§usePositionTypes + useAudienceGroups — core pattern and ESLint traps)
  </read_first>
  <action>
    Create `frontend/src/domains/positions/hooks/usePositionTypes.ts`: export interface `PositionTypeRow` (id, name_en, name_ar, approval_stages — per interfaces block) and an exported function `usePositionTypes` returning `useQuery` with queryKey `['position-types']`, queryFn selecting `id, name_en, name_ar, approval_stages` from `position_types` via the `supabase` client (`@/lib/supabase`), `.order('name_en')`, throwing `new Error(error.message)` on PostgREST error, and `staleTime: 30 * 60 * 1000` (lookup data). Create `frontend/src/domains/positions/hooks/useAudienceGroups.ts` identically for `audience_groups` with queryKey `['audience-groups']`, selecting `id, name_en, name_ar`, exporting `AudienceGroupRow`. Do NOT put default-resolution logic (Standard/All Staff name-match) in the hooks — that lives in the dialog (D-05/D-06). Add both hooks and both row types to the barrel `frontend/src/domains/positions/index.ts` in its Hooks section, matching the existing `export { … } from './hooks/…'` style. ESLint constraints: camelCase filenames with use prefix, explicit function return types (annotate the hook return with the project's convention — `UseQueryResult<PositionTypeRow[], Error>` from @tanstack/react-query or an exported result interface like the analog), no `any`, single quotes, no semicolons.
  </action>
  <verify>
    <automated>grep -q "from('position_types')" frontend/src/domains/positions/hooks/usePositionTypes.ts && grep -q "from('audience_groups')" frontend/src/domains/positions/hooks/useAudienceGroups.ts && grep -q 'usePositionTypes' frontend/src/domains/positions/index.ts && grep -q 'useAudienceGroups' frontend/src/domains/positions/index.ts && cd frontend && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - Both hook files exist with queryKeys `['position-types']` / `['audience-groups']`, 30-minute staleTime, and throw-on-error queryFns
    - Both hooks and row types are re-exported from `frontend/src/domains/positions/index.ts`
    - Neither hook contains name-match default logic (no 'Standard Position' or 'All Staff' literals in the hooks)
    - `cd frontend && pnpm type-check` exits 0
  </acceptance_criteria>
  <done>The dialog can read both lookup tables through typed, cached domain hooks.</done>
</task>

<task type="auto">
  <name>Task 2: Add translateContent repository wrapper and its types</name>
  <files>frontend/src/domains/positions/repositories/positions.repository.ts, frontend/src/domains/positions/types/index.ts</files>
  <read_first>
    - frontend/src/domains/positions/repositories/positions.repository.ts (the file being modified — mirror createPosition L76-78 and createPositionDossierLink L163-168 exactly)
    - frontend/src/domains/positions/types/index.ts (the file being modified — match its section-comment style)
    - frontend/src/lib/api-client.ts (apiPost + handleResponse — confirm the throw-on-non-2xx behavior the contract comment documents; do NOT modify this file)
  </read_first>
  <action>
    In `frontend/src/domains/positions/types/index.ts` add two exported interfaces: `TranslateContentInput` with `text: string`, `direction: 'en_to_ar' | 'ar_to_en'`, `content_type: 'title' | 'content'`; and `TranslateContentResponse` with `translated_text: string`, `confidence: number`, `source_language: string`, `target_language: string`, `metadata: Record<string, unknown>`. In `positions.repository.ts` add `export async function translateContent(input: TranslateContentInput): Promise<TranslateContentResponse>` returning `apiPost<TranslateContentResponse>('/translate-content', input)`, mirroring createPosition's shape. Above the function, add a contract comment stating: contract verified against DEPLOYED v2 (not repo source); edge enforces a 10,000-char text limit; when AnythingLLM is down the deployed edge returns HTTP 503 so apiPost THROWS — callers must treat any throw as "translation unavailable" and never block submission on it (D-07, RESEARCH Pitfall 4). Do not modify shared api-client behavior (Pitfall 3).
  </action>
  <verify>
    <automated>grep -q 'export async function translateContent' frontend/src/domains/positions/repositories/positions.repository.ts && grep -q "apiPost<TranslateContentResponse>('/translate-content'" frontend/src/domains/positions/repositories/positions.repository.ts && grep -q 'TranslateContentInput' frontend/src/domains/positions/types/index.ts && cd frontend && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - `translateContent` exists in the repository, typed with the two new interfaces, posting to `/translate-content`
    - The contract comment names the 503/AI_UNAVAILABLE deployed behavior and the never-blocks-submission rule
    - `frontend/src/lib/api-client.ts` has zero diff (git diff --name-only must not list it)
    - `cd frontend && pnpm type-check` exits 0
  </acceptance_criteria>
  <done>The dialog's translate buttons have a typed, documented edge wrapper that fails safe.</done>
</task>

<task type="auto">
  <name>Task 3: Add all create-dialog i18n keys (EN+AR) and the D-13 tab relabel</name>
  <files>frontend/src/i18n/en/positions.json, frontend/src/i18n/ar/positions.json</files>
  <read_first>
    - frontend/src/i18n/en/positions.json (the file being modified — existing top-level blocks use snake_case keys; dossier_tab.create_position currently reads "Attach Position")
    - frontend/src/i18n/ar/positions.json (the AR twin — every key added to EN must land here too)
    - .planning/phases/64-new-position-from-dossier/64-UI-SPEC.md (§Copywriting Contract — the exact EN and AR strings; copy them verbatim)
  </read_first>
  <action>
    In BOTH `en/positions.json` and `ar/positions.json` add two new top-level blocks with snake_case keys (matching the namespace's existing style), using the exact strings from the UI-SPEC Copywriting Contract:
    1. `create_dialog`: `type_label` (Position type / نوع الموقف), `title_en_label` (Title (English) / العنوان (إنجليزي)), `title_ar_label` (Title (Arabic) / العنوان (عربي)), `content_en_label` (Content (English) — optional / المحتوى (إنجليزي) — اختياري), `content_ar_label` (Content (Arabic) — optional / المحتوى (عربي) — اختياري), `audience_legend` (Audience groups / مجموعات الجمهور), `translate_to_ar` (Translate to Arabic / ترجمة إلى العربية), `translate_to_en` (Translate to English / ترجمة إلى الإنجليزية), `translating` (Translating… / جارٍ الترجمة…), `translate_unavailable` (Translation unavailable. Enter the text manually. / الترجمة غير متاحة. أدخل النص يدويًا.), `toast_success` (Position created / تم إنشاء الموقف), `toast_open_position` (Open position / فتح الموقف), `toast_partial_failure` (Position created, but linking to this dossier failed. / تم إنشاء الموقف، لكن تعذّر ربطه بهذا الملف.), `toast_retry_link` (Retry link / إعادة محاولة الربط), `toast_error` (Failed to create position. Try again. / فشل إنشاء الموقف. حاول مرة أخرى.), `lookup_error` (Couldn't load form options. Close the dialog and try again. / تعذّر تحميل خيارات النموذج. أغلق مربع الحوار وحاول مرة أخرى.)
    2. `validation`: `type_required` (Select a position type / اختر نوع الموقف), `title_en_required` (English title is required / العنوان الإنجليزي مطلوب), `title_ar_required` (Arabic title is required / العنوان العربي مطلوب), `title_max_length` (Title must be 200 characters or less / يجب ألا يتجاوز العنوان 200 حرف), `audience_required` (Select at least one audience group / اختر مجموعة جمهور واحدة على الأقل)
    3. In the existing `dossier_tab` block: UPDATE `create_position` value to "Create position" (EN) / "إنشاء موقف" (AR), and ADD `attach_existing`: "Attach existing position" (EN) / "إرفاق موقف موجود" (AR).
    Voice rules: sentence case, no emoji, no exclamation marks. Validate both files parse as JSON after editing.
  </action>
  <verify>
    <automated>python3 -c "
import json
for lang in ('en','ar'):
    d = json.load(open(f'frontend/src/i18n/{lang}/positions.json'))
    cd = d['create_dialog']; v = d['validation']; dt = d['dossier_tab']
    for k in ('type_label','title_en_label','title_ar_label','content_en_label','content_ar_label','audience_legend','translate_to_ar','translate_to_en','translating','translate_unavailable','toast_success','toast_open_position','toast_partial_failure','toast_retry_link','toast_error','lookup_error'):
        assert k in cd, f'{lang} create_dialog.{k} missing'
    for k in ('type_required','title_en_required','title_ar_required','title_max_length','audience_required'):
        assert k in v, f'{lang} validation.{k} missing'
    assert 'attach_existing' in dt, f'{lang} dossier_tab.attach_existing missing'
assert json.load(open('frontend/src/i18n/en/positions.json'))['dossier_tab']['create_position'] == 'Create position'
assert json.load(open('frontend/src/i18n/ar/positions.json'))['dossier_tab']['create_position'] == 'إنشاء موقف'
print('I18N_KEYS_OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - All 16 create_dialog keys and all 5 validation keys exist in BOTH language files with the UI-SPEC strings
    - dossier_tab.create_position equals "Create position" (EN) / "إنشاء موقف" (AR); dossier_tab.attach_existing exists in both
    - Both files parse as valid JSON; the verify script prints I18N_KEYS_OK
    - No other existing keys were removed or altered (git diff shows only additions plus the one create_position value change per file)
  </acceptance_criteria>
  <done>Every string the dialog and the rewired tab will render exists bilingually before any UI is built.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                     |
| ------------------------------- | --------------------------------------------------------------- |
| client → PostgREST lookup reads | authenticated reads of reference tables under RLS               |
| client → translate-content edge | user text forwarded to the AI backend through the edge function |

## STRIDE Threat Register

| Threat ID | Category               | Component                              | Disposition | Mitigation Plan                                                                                                                                  |
| --------- | ---------------------- | -------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-64-04   | Information Disclosure | position_types / audience_groups reads | accept      | Reference data only (names + stage counts); RLS SELECT is intentionally open to authenticated users (verified live, probes #7/#8)                |
| T-64-05   | Denial of Service      | translateContent → AI edge             | mitigate    | Edge enforces the 10,000-char limit server-side (verified in source); the dialog (64-03) disables the button while a call is in flight           |
| T-64-06   | Tampering              | shared api-client error contract       | mitigate    | api-client is explicitly NOT modified (Pitfall 3 — changing handleResponse would affect every consumer); acceptance criterion enforces zero diff |
| T-64-SC   | Tampering              | npm/pip/cargo installs                 | accept      | No package installs in this plan (zero new dependencies — RESEARCH Standard Stack)                                                               |

</threat_model>

<verification>
- `cd frontend && pnpm type-check` exits 0 after each task; the i18n key script prints I18N_KEYS_OK
- Wave-merge gate: `cd frontend && pnpm exec vitest run` (full frontend unit suite) still green — this plan adds no behavior to tested components
</verification>

<success_criteria>

- Lookup hooks, translate wrapper, types, and barrel exports exist and type-check
- The complete bilingual string set for Plan 64-03's dialog and Plan 64-04's tab is in place
- Zero new packages, zero api-client changes
  </success_criteria>

<output>
Create `.planning/phases/64-new-position-from-dossier/64-02-SUMMARY.md` when done.
</output>
