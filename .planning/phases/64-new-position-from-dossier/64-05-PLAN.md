---
phase: 64-new-position-from-dossier
plan: 05
type: execute
wave: 3
depends_on: [64-03]
files_modified:
  - frontend/src/components/positions/DossierPositionsTab.tsx
autonomous: true
requirements: [POSNEW-01, POSNEW-02]

must_haves:
  truths:
    - 'The Positions tab primary "Create position" button opens NewPositionDialog — it no longer opens the mislabeled attach-existing dialog (D-13)'
    - 'Attach-existing remains available as a secondary outline button "Attach existing position" beside the primary (UI-SPEC recorded decision: side-by-side, not a menu)'
    - 'The tab builds DossierContextForAction from useDossier(dossierId) — a cache hit on dossierKeys.detail since the shell already loaded it (zero extra network)'
    - 'The existing attach-existing flow (Promise.allSettled + partial-error toast + dossier-scoped invalidation) is preserved unchanged'
  artifacts:
    - path: 'frontend/src/components/positions/DossierPositionsTab.tsx'
      provides: 'D-13 rewire: both dialogs wired, primary creates, secondary attaches'
      contains: 'NewPositionDialog'
  key_links:
    - from: 'frontend/src/components/positions/DossierPositionsTab.tsx'
      to: 'frontend/src/components/positions/NewPositionDialog.tsx'
      via: 'primary Create position button → showNewPositionDialog state'
      pattern: 'NewPositionDialog'
    - from: 'frontend/src/components/positions/DossierPositionsTab.tsx'
      to: 'cached dossier detail'
      via: 'useDossier(dossierId) → DossierContextForAction'
      pattern: 'useDossier'
    - from: 'frontend/src/components/positions/DossierPositionsTab.tsx'
      to: 'AttachPositionDialog'
      via: 'secondary Attach existing position button'
      pattern: 'attach_existing'
---

<objective>
Rewire the Positions tab (entry point 2): the "Create position" button currently opens the attach-existing dialog — a mislabel that hid the broken create path. Point it at NewPositionDialog and demote attach-existing to a labeled secondary button.

Purpose: D-13 — the tab is where POSNEW-02's live criterion is observed (new position renders without manual refresh, driven by the dialog's dossier-scoped invalidation).
Output: tab with two honest buttons, dossier context built from cache.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/64-new-position-from-dossier/64-PATTERNS.md

<interfaces>
<!-- NewPositionDialog props contract (published by 64-03) -->

```typescript
import { NewPositionDialog } from './NewPositionDialog' // same directory — match the tab's relative-import style
export interface NewPositionDialogProps {
  isOpen: boolean
  onClose: () => void
  dossierContext: DossierContextForAction
  isRTL: boolean
}
```

<!-- DossierContextForAction (useAddToDossierActions.tsx L35-41) — build it from the cached dossier -->

```typescript
export interface DossierContextForAction {
  dossier_id: string
  dossier_type: DossierType
  dossier_name_en: string
  dossier_name_ar: string | null
  inheritance_source: InheritanceSource // 'direct' for the tab entry
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire handleCreatePosition to NewPositionDialog and add the secondary attach button (D-13)</name>
  <files>frontend/src/components/positions/DossierPositionsTab.tsx</files>
  <read_first>
    - frontend/src/components/positions/DossierPositionsTab.tsx (the file being modified — handleCreatePosition L49-52, primary button L77-84, attach flow onAttach L195-227 which must be preserved verbatim)
    - frontend/src/hooks/useAddToDossierActions.tsx (L35-41 — DossierContextForAction field names and the InheritanceSource union; confirm 'direct' is a member)
    - frontend/src/domains/dossiers/hooks/useDossier.ts (useDossier signature L111 and dossierKeys.detail L78 — the cache-hit source for dossier names/type)
    - .planning/phases/64-new-position-from-dossier/64-UI-SPEC.md (§Positions tab header — side-by-side buttons decision, labels, variants)
  </read_first>
  <action>
    All changes in DossierPositionsTab.tsx (the tab uses relative imports and `useTranslation(['positions', 'common'])` — keep both conventions):
    1. Add `const [showNewPositionDialog, setShowNewPositionDialog] = useState(false)` alongside the existing showAttachDialog state. Change `handleCreatePosition` to `setShowNewPositionDialog(true)` — `showAttachDialog` stays for the secondary action.
    2. Dossier context: call `useDossier(dossierId)` (import from the domains/dossiers hooks path using the file's relative-import style) — this is a cache hit on dossierKeys.detail because the dossier shell already loaded it. Derive `isRTL` from the already-present i18n instance (`i18n.language === 'ar'`; extend the existing useTranslation destructure to include i18n). Build `DossierContextForAction` from the loaded dossier: dossier_id = dossierId, dossier_type = the dossier's type field, dossier_name_en / dossier_name_ar from the dossier row, inheritance_source: 'direct'. Render NewPositionDialog ONLY when the dossier data is loaded (conditional render guard — practically immediate given the cache hit).
    3. Header actions (UI-SPEC §Positions tab header): the primary Button keeps `t('positions:dossier_tab.create_position')` (its value now reads "Create position" via 64-02) and the default variant; ADD a secondary `Button variant="outline"` labeled `t('positions:dossier_tab.attach_existing')` with an aria-label, opening the attach dialog (`setShowAttachDialog(true)`). Primary sits closest to reading start; logical properties only; both min-h-11 on small screens per existing button classes.
    4. Render `<NewPositionDialog isOpen={showNewPositionDialog} onClose={() => setShowNewPositionDialog(false)} dossierContext={...} isRTL={isRTL} />` near the existing AttachPositionDialog render. Do NOT touch the existing onAttach Promise.allSettled flow, its toasts, or its invalidation — the attach-existing path intentionally keeps the default link_type (related_to) while the create path passes applies_to inside the dialog.
    5. ESLint: explicit return types on new handlers, no floating promises, no `any`. Run type-check and the dialog test file (regression guard on the imported component).
  </action>
  <verify>
    <automated>cd frontend && grep -q 'NewPositionDialog' src/components/positions/DossierPositionsTab.tsx && grep -q 'attach_existing' src/components/positions/DossierPositionsTab.tsx && grep -q "inheritance_source: 'direct'" src/components/positions/DossierPositionsTab.tsx && grep -q 'Promise.allSettled' src/components/positions/DossierPositionsTab.tsx && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - handleCreatePosition opens NewPositionDialog (sets showNewPositionDialog), not the attach dialog
    - A secondary outline button labeled via `positions:dossier_tab.attach_existing` opens AttachPositionDialog
    - DossierContextForAction is built from useDossier data with inheritance_source 'direct'; NewPositionDialog renders only when dossier data is loaded
    - The existing onAttach block (Promise.allSettled, partial-error toast, ['dossier-position-links', dossierId] invalidation) is byte-identical — git diff does not touch it
    - `cd frontend && pnpm type-check` exits 0
  </acceptance_criteria>
  <done>Entry point 2 is honest: Create position creates, Attach existing attaches — both reachable from the tab header.</done>
</task>

<task type="auto">
  <name>Task 2: Wave gate — full frontend suite and lint on touched files</name>
  <files>none (verification gates)</files>
  <read_first>
    - frontend/src/components/positions/DossierPositionsTab.tsx (the Task 1 result — sanity-check the diff scope before running gates)
  </read_first>
  <action>
    Run the wave-merge gates for the parallel wave-3 plans' shared surface: `cd frontend && pnpm exec vitest run` (full unit suite — was 1,245+ pass / 0 fail baseline) and `pnpm exec eslint src/components/positions/DossierPositionsTab.tsx --max-warnings 0` (CI lints the whole repo at --max-warnings 0; the staged-only pre-commit hook can miss violations — project memory). If the full suite surfaces failures OUTSIDE this phase's files, record them in the SUMMARY and do not paper over them; if they are inside this phase's files, fix before completing.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run && pnpm exec eslint src/components/positions/DossierPositionsTab.tsx --max-warnings 0</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && pnpm exec vitest run` exits 0 (zero failed tests)
    - ESLint on the touched file exits 0 with --max-warnings 0
  </acceptance_criteria>
  <done>The tab rewire is suite-green and lint-clean against CI's whole-repo standard.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                                           |
| ----------------------- | --------------------------------------------------------------------- |
| tab → NewPositionDialog | dossier context (id/type/names) passed from cached, RLS-filtered data |

## STRIDE Threat Register

| Threat ID | Category               | Component                           | Disposition | Mitigation Plan                                                                                                                           |
| --------- | ---------------------- | ----------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| T-64-13   | Spoofing               | dossier context fields in the badge | accept      | Names render via React text nodes (auto-escaped); the context comes from the RLS-filtered dossierKeys.detail cache, not user input        |
| T-64-14   | Elevation of Privilege | create/link from the tab            | mitigate    | All writes go through the dialog's edge functions (server-side validation + RLS, T-64-08/T-64-10) — the tab adds no write path of its own |
| T-64-SC   | Tampering              | npm/pip/cargo installs              | accept      | No package installs in this plan                                                                                                          |

</threat_model>

<verification>
- Greps prove both buttons + context wiring; full suite + targeted ESLint green
- The POSNEW-02 "renders without refresh" behavior is exercised live in plan 64-06 (this plan provides the surface)
</verification>

<success_criteria>

- D-13 complete: the mislabel is gone, both actions are reachable and honestly labeled
- Zero changes to the attach-existing flow's behavior
- Full suite and CI-grade lint green
  </success_criteria>

<output>
Create `.planning/phases/64-new-position-from-dossier/64-05-SUMMARY.md` when done.
</output>
