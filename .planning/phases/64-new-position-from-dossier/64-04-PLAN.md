---
phase: 64-new-position-from-dossier
plan: 04
type: execute
wave: 3
depends_on: [64-03]
files_modified:
  - frontend/src/components/positions/NewPositionDialog.tsx
  - frontend/src/components/positions/__tests__/NewPositionDialog.test.tsx
  - frontend/src/components/dossier/AddToDossierDialogs.tsx
autonomous: true
requirements: [POSNEW-01, POSNEW-02]

must_haves:
  truths:
    - 'Submission runs two steps from the client: positions-create via useCreatePosition.mutateAsync, then createPositionDossierLink with EXPLICIT link_type applies_to — the edge defaults to related_to if omitted (D-09/D-10, Pitfall 2)'
    - "Full success invalidates the dossier-scoped readers — ['dossier-position-links', dossierId] prefix and dossierOverviewKeys.detail(dossierId) — closes the dialog, and shows a success toast with an Open position action (D-12)"
    - 'If the position creates but the link write fails, a warning toast (10s) states it with a Retry link action that re-calls the link write — never rendered as clean success (D-11, Phase 62 honesty precedent)'
    - 'If the create itself fails, the dialog stays open with typed input intact and a generic localized error toast (api-client strips the edge bilingual body — Pitfall 3, api-client unchanged)'
    - 'The Add-to-Dossier menu Position action now opens the rebuilt dialog: the broken payload (position_type_id = dossier_id, blank title_ar, empty audience_groups, no link write) no longer exists in AddToDossierDialogs.tsx'
  artifacts:
    - path: 'frontend/src/components/positions/NewPositionDialog.tsx'
      provides: 'Complete two-step submit with honest failure states'
      contains: "link_type: 'applies_to'"
    - path: 'frontend/src/components/dossier/AddToDossierDialogs.tsx'
      provides: 'PositionDialog as a thin wrapper rendering NewPositionDialog'
      contains: 'NewPositionDialog'
  key_links:
    - from: 'frontend/src/components/positions/NewPositionDialog.tsx'
      to: 'positions-create edge'
      via: 'useCreatePosition.mutateAsync'
      pattern: 'mutateAsync'
    - from: 'frontend/src/components/positions/NewPositionDialog.tsx'
      to: 'position_dossier_links'
      via: "createPositionDossierLink(position.id, { dossier_id, link_type: 'applies_to' })"
      pattern: "link_type: 'applies_to'"
    - from: 'frontend/src/components/dossier/AddToDossierDialogs.tsx'
      to: 'frontend/src/components/positions/NewPositionDialog.tsx'
      via: 'PositionDialog thin wrapper'
      pattern: 'NewPositionDialog'
---

<objective>
Complete the dialog: unskip and implement the two-step submit (create → link with applies_to), dossier-scoped cache invalidation, and the three honest outcome states (full success / partial failure with retry / total failure). Then gut the broken `PositionDialog` in `AddToDossierDialogs.tsx` into a thin wrapper around `NewPositionDialog`, wiring entry point 1.

Purpose: POSNEW-02 — the position_dossier_links row must be written for the originating dossier and the Positions tab must refresh without manual reload (the invalidation set is the original bug class, Pitfall 5).
Output: full six-test GREEN dialog + Add-to-Dossier menu entry rewired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/64-new-position-from-dossier/64-PATTERNS.md
@.planning/phases/64-new-position-from-dossier/64-UI-SPEC.md

<interfaces>
<!-- Attach edge contract (verified source + deployed v11 — RESEARCH Code Examples) -->

```typescript
// POST /functions/v1/positions-dossiers-create?positionId=<uuid>
// Body: { dossier_id: string, link_type?: 'applies_to'|'related_to'|'endorsed_by'|'opposed_by', notes?: string }
// link_type DEFAULTS to 'related_to' — pass 'applies_to' explicitly (D-09, Pitfall 2)
// 201 → link row; 409 → already linked; 403 → RLS denied
export async function createPositionDossierLink(
  positionId: string,
  input: CreatePositionDossierLinkInput,
): Promise<PositionDossierLink>
```

<!-- Invalidation key map (RESEARCH Pitfall 5 — readers verified at the cited lines) -->

```typescript
// ['dossier-position-links', dossierId]        ← DossierPositionsTab reader (useDossierPositionLinks.ts L122; prefix covers all filter variants)
// dossierOverviewKeys.detail(dossierId)        ← overview reads position_dossier_links (dossier-overview.service.ts L465-470; factory L1127-1136)
// ['positions', 'list']                        ← already invalidated by useCreatePosition onSuccess — do not duplicate
// ['position-dossier-links', position.id]      ← inverse key, position detail page (useCreatePositionDossierLink.ts L21)
```

<!-- sonner action API (RESEARCH Assumption A1 — confirm at build time; sonner ^2.0.7 already in package.json) -->

```typescript
toast.success(message, { action: { label: string, onClick: () => void } })
toast.warning(message, { duration: 10000, action: { label, onClick } })
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unskip the submit-flow tests and implement the two-step submit with honest failure states (full GREEN)</name>
  <files>frontend/src/components/positions/NewPositionDialog.tsx, frontend/src/components/positions/__tests__/NewPositionDialog.test.tsx</files>
  <read_first>
    - frontend/src/components/positions/NewPositionDialog.tsx (the 64-03 form layer being completed — onSubmit is a stub)
    - frontend/src/components/positions/__tests__/NewPositionDialog.test.tsx (tests d/e/f are it.skip with the 64-04 handoff comment — these define the contract)
    - frontend/src/components/dossier/AddToDossierDialogs.tsx (EventDialog L743-752 — the R12-04 dossier-scoped invalidation precedent and its comment discipline; TaskDialog L346-373 — create-then-link sequencing)
    - .planning/phases/64-new-position-from-dossier/64-UI-SPEC.md (§Interaction States 7-10 — the binding contract for in-flight, full success, partial failure, total failure)
  </read_first>
  <action>
    1. In the test file: change `it.skip` to `it` on tests d, e, f and delete the handoff comments. Run the file — exactly 3 failures (RED for the submit flow). Commit the RED state.
    2. Implement onSubmit in NewPositionDialog.tsx (replacing the stub) using `form.handleSubmit`:
       - Step 1: `const position = await createPosition.mutateAsync(values)` where values is the validated CreatePositionRequest (position_type_id, title_en, title_ar, optional content_en/content_ar, audience_groups). On throw: `toast.error(t('positions:create_dialog.toast_error'))`, keep the dialog OPEN with input intact (UI-SPEC state 10), return. The hook already invalidates ['positions','list'].
       - Step 2 in its OWN try/catch (D-11 requires distinguishing the two failures): `await createPositionDossierLink(position.id, { dossier_id: dossierContext.dossier_id, link_type: 'applies_to' })` — link_type explicit, never omitted (Pitfall 2).
       - Partial failure (step 2 throws): close the dialog and reset the form (re-submitting would duplicate the position — UI-SPEC state 9), then `toast.warning(t('positions:create_dialog.toast_partial_failure'), { duration: 10000, action: { label: t('positions:create_dialog.toast_retry_link'), onClick: retryLink } })`. `retryLink` re-calls createPositionDossierLink with the same positionId + applies_to args; on retry success run the full-success path below (success toast + dossier-scoped invalidations); on retry failure re-show the same warning toast. No dossier-scoped invalidation at the initial partial-failure point.
       - Full success (both steps resolve): await `queryClient.invalidateQueries({ queryKey: ['dossier-position-links', dossierContext.dossier_id] })` (prefix), `queryClient.invalidateQueries({ queryKey: dossierOverviewKeys.detail(dossierContext.dossier_id) })`, and `queryClient.invalidateQueries({ queryKey: ['position-dossier-links', position.id] })` — copy the EventDialog R12-04 comment style explaining WHY (the mutation hooks only know global/inverse keys; the tab and overview read dossier-scoped keys and would stay stale). Then reset the form, call onClose, and `toast.success(t('positions:create_dialog.toast_success'), { action: { label: t('positions:create_dialog.toast_open_position'), onClick: () => navigate({ to: '/positions/$id', params: { id: position.id } }) } })` using useNavigate from @tanstack/react-router. If the sonner action API differs from the interfaces block (Assumption A1), confirm against the installed sonner v2 types and adapt — do not build a custom toast body.
       - In-flight: submit button disabled with Loader2 me-2 animate-spin while pending (UI-SPEC state 7); guard against double-submit.
       - ESLint: await every invalidation (no-floating-promises); explicit return types; no `any`.
    3. Run the full test file — 6/6 GREEN — then type-check.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/components/positions/__tests__/NewPositionDialog.test.tsx 2>&1 | grep -qE '6 passed' && ! grep -q 'it\.skip' src/components/positions/__tests__/NewPositionDialog.test.tsx && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains zero `it.skip`; `cd frontend && pnpm exec vitest run src/components/positions/__tests__/NewPositionDialog.test.tsx` reports 6 passed / 0 failed / 0 skipped
    - NewPositionDialog.tsx contains exactly one `link_type: 'applies_to'` literal in the submit path plus one in the retry path (or a shared helper used by both) — and zero link writes without an explicit link_type
    - Step 2 failure path calls toast.warning (not toast.success) and the retry action re-invokes createPositionDossierLink — proven by test f
    - Full-success path invalidates ['dossier-position-links', dossierId] and dossierOverviewKeys.detail(dossierId) — proven by test e
    - `cd frontend && pnpm type-check` exits 0
  </acceptance_criteria>
  <done>The dialog persists a valid position AND its applies_to dossier link, refreshes every dossier-scoped reader, and renders failure honestly — POSNEW-01/POSNEW-02 client contract complete.</done>
</task>

<task type="auto">
  <name>Task 2: Gut the broken PositionDialog in AddToDossierDialogs.tsx into a thin NewPositionDialog wrapper</name>
  <files>frontend/src/components/dossier/AddToDossierDialogs.tsx</files>
  <read_first>
    - frontend/src/components/dossier/AddToDossierDialogs.tsx (the file being modified — PositionDialog L604-695 with the broken submit at L627-633; main-export wiring L1149-1194 where commonProps = { dossier, dossierContext, isRTL })
    - frontend/src/components/positions/NewPositionDialog.tsx (the props contract: isOpen, onClose, dossierContext, isRTL)
  </read_first>
  <action>
    Replace the entire `PositionDialog` function body (L604-695) with a thin render: `return <NewPositionDialog isOpen={isOpen} onClose={onClose} dossierContext={dossierContext} isRTL={isRTL} />`, importing `NewPositionDialog` from `@/components/positions/NewPositionDialog`. Keep the PositionDialog function name and its ActionDialogProps signature so the main-export wiring (L1175-1179) and dialogStates plumbing remain untouched. Remove ONLY the imports that gutting orphans — at minimum the deprecated `useCreatePosition` shim import from `@/hooks/useCreatePosition` (L52) if PositionDialog was its only consumer; verify each candidate import has no other usage in the file before removing (Textarea, Input, etc. are used by sibling dialogs — leave them). Do not modify any sibling dialog, the file-local DossierContextBadge (still used by siblings), or ACTION_GROUPS. The broken payload literals (`position_type_id: dossierContext.dossier_id`, `title_ar: ''`, `audience_groups: []`) must no longer exist anywhere in the file.
  </action>
  <verify>
    <automated>cd frontend && grep -q 'NewPositionDialog' src/components/dossier/AddToDossierDialogs.tsx && ! grep -q 'position_type_id: dossierContext.dossier_id' src/components/dossier/AddToDossierDialogs.tsx && ! grep -q "title_ar: ''" src/components/dossier/AddToDossierDialogs.tsx && ! grep -q "from '@/hooks/useCreatePosition'" src/components/dossier/AddToDossierDialogs.tsx && pnpm type-check && pnpm exec vitest run src/components/positions/__tests__/NewPositionDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - AddToDossierDialogs.tsx imports NewPositionDialog and PositionDialog renders it with the four props; the main export wiring at L1175-1179 is unchanged
    - Zero occurrences of `position_type_id: dossierContext.dossier_id`, `title_ar: ''`, or the deprecated `@/hooks/useCreatePosition` import remain in the file
    - No sibling dialog (Commitment/Event/Task/Intake/Health/MoU) lines changed — git diff confines to the PositionDialog body and the import block
    - `cd frontend && pnpm type-check` exits 0 and the dialog test file still passes 6/6
  </acceptance_criteria>
  <done>Entry point 1 (Add-to-Dossier menu → Position) now opens the working quick-create dialog; the broken form is gone.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                           | Description                                                    |
| -------------------------------------------------- | -------------------------------------------------------------- |
| client submit → positions-create edge              | validated form payload crosses to the server with the user JWT |
| client link write → positions-dossiers-create edge | arbitrary dossier_id could be supplied programmatically        |

## STRIDE Threat Register

| Threat ID | Category               | Component                             | Disposition | Mitigation Plan                                                                                                                                             |
| --------- | ---------------------- | ------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-64-10   | Elevation of Privilege | IDOR via arbitrary dossier_id in link | mitigate    | positions-dossiers-create v11 enforces RLS + clearance server-side (round-12 fix, battle-tested; 403 on denial) — client passes only the context dossier_id |
| T-64-11   | Repudiation            | partial failure masked as success     | mitigate    | D-11 contract is test-pinned (test f): link failure → toast.warning + retry, toast.success never fires; UI never renders clean success on failure           |
| T-64-12   | Information Disclosure | server error bodies in the UI         | accept      | api-client strips response bodies on non-2xx (Pitfall 3); the UI shows only generic localized copy — no server internals leak through this path             |
| T-64-SC   | Tampering              | npm/pip/cargo installs                | accept      | No package installs in this plan                                                                                                                            |

</threat_model>

<verification>
- VALIDATION.md rows realized: "Step 2 called with link_type applies_to + dossier-scoped invalidation" → test d/e; "Partial failure → warning toast, retry callable" → test f
- `cd frontend && pnpm exec vitest run src/components/positions/__tests__/NewPositionDialog.test.tsx && pnpm type-check` exits 0 (6 passed)
- Wave-merge gate: `cd frontend && pnpm exec vitest run` (full suite) green
</verification>

<success_criteria>

- The two-step create→link flow with explicit applies_to is implemented and unit-pinned; all six dialog tests GREEN
- The Add-to-Dossier Position entry renders the rebuilt dialog; the broken payload is eliminated from the codebase
- Failure states are honest: partial ≠ success, total failure preserves user input
  </success_criteria>

<output>
Create `.planning/phases/64-new-position-from-dossier/64-04-SUMMARY.md` when done.
</output>
