---
phase: 64-new-position-from-dossier
plan: 06
type: execute
wave: 4
depends_on: [64-01, 64-04, 64-05]
files_modified: []
autonomous: true
requirements: [POSNEW-01, POSNEW-02]

must_haves:
  truths:
    - 'A live create from the Positions tab on a country dossier succeeds, and the position_dossier_links row exists on staging with link_type applies_to for the originating dossier (DB-verified — the POSNEW-02 roadmap criterion)'
    - 'The new position renders on the dossier Positions tab WITHOUT a manual page reload, in the same authenticated session that created it (drafts are author-visible only — Pitfall 7)'
    - 'The Add-to-Dossier menu entry (entry point 1) also produces a valid create + link, live-verified'
    - 'In Arabic mode the dialog renders AR labels with dir=rtl and Tajawal; the title_ar field is RTL even in EN locale; the translate button failure path shows the unavailable toast and leaves fields untouched (AnythingLLM is down on staging — the failure path IS the live-verifiable path, D-07)'
    - 'Full frontend suite, type-check, and bundle budgets are green; all verification rows are cleaned off staging'
  artifacts:
    - path: '.planning/phases/64-new-position-from-dossier/64-06-SUMMARY.md'
      provides: 'Live verification matrix: per-entry-point results, SQL evidence (link_type, dossier_id), AR/RTL + width evidence, gate outputs, cleanup confirmation'
  key_links:
    - from: 'live browser session (Positions tab + Add-to-Dossier menu)'
      to: 'positions-create + positions-dossiers-create edges'
      via: 'NewPositionDialog two-step submit'
      pattern: 'positions-create'
    - from: 'service-role SQL verification'
      to: 'position_dossier_links'
      via: 'mcp__supabase__execute_sql'
      pattern: "link_type = 'applies_to'"
---

<objective>
Run the phase-gate live verification on staging: create a position from BOTH entry points on a country dossier, DB-verify the position and its applies_to link row, observe the tab refreshing without reload, verify AR/RTL and the translate failure UX, run the full gates, and clean up.

Purpose: roadmap success criteria 2 and 3 are explicitly live criteria ("DB-verified on staging", "without a manual refresh — live-verified"); build/type/unit gates alone cannot prove them (milestone norm, Phases 62/63 precedent).
Output: recorded verification matrix in the SUMMARY; staging left clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/64-new-position-from-dossier/64-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live create from both entry points on a country dossier + DB verification (POSNEW-02)</name>
  <files>none (live verification via agent-browser + Supabase MCP)</files>
  <read_first>
    - .planning/phases/64-new-position-from-dossier/64-RESEARCH.md (§Pitfall 6 — Positions tab is routed ONLY on country/topic dossiers; §Pitfall 7 — drafts visible to author only; verify with the creating session)
    - .planning/phases/64-new-position-from-dossier/64-01-SUMMARY.md (confirmation that the RLS restore landed — if 64-01 did not complete, STOP)
    - .env.test (TEST_USER_EMAIL / TEST_USER_PASSWORD for login — NEVER echo values)
  </read_first>
  <action>
    1. Ensure the dev server is up (`cd frontend && pnpm dev` in the background if :5173 is not responding; the app proxies to staging Supabase). Login at http://localhost:5173 with the .env.test credentials using agent-browser (open → snapshot -i → click/fill by ref).
    2. Entry point 2 — the tab (the without-refresh criterion): navigate to the Saudi Arabia country dossier (`/dossiers/countries/9b9a04af-50b0-408c-878d-9d07f77a74ab`) → Positions tab. Confirm the header shows BOTH buttons (primary "Create position", secondary "Attach existing position"). Click Create position. In the dialog confirm the D-05/D-06 defaults live (type shows Standard Position; All Staff checked) and that submit is disabled. Fill title_en `Phase 64 live verification A` and title_ar `تحقق المرحلة ٦٤ أ`, submit, and confirm the success toast appears AND the new position renders in the tab list WITHOUT any page reload or manual refresh action. Screenshot before/after.
    3. Entry point 1 — the menu: from the same dossier, open the Add-to-Dossier menu → Position. Create a second position (`Phase 64 live verification B` / `تحقق المرحلة ٦٤ ب`), this time narrowing audience to Management only (proves the multi-select round-trips a non-default selection). Confirm the success toast.
    4. DB verification via `mcp__supabase__execute_sql` (project zkrcjzdemdmwhearhfgg, service role): for both created positions select the positions row (status = 'draft', author_id = the test user, position_type_id resolving to Standard Position, title_ar non-empty) joined to position_dossier_links (dossier_id = 9b9a04af-50b0-408c-878d-9d07f77a74ab, link_type = 'applies_to') and position_audience_groups (A → All Staff; B → Management). Record the SQL output verbatim (ids included) in the SUMMARY.
    5. Record the entry-point matrix (entry, position id, toast observed, tab-refresh observed, link row verified) for the SUMMARY. Do not clean up yet — Task 2 reuses the session and Task 3 cleans up.
  </action>
  <verify>
    <automated>MISSING — live staging verification, manual-only justified per VALIDATION.md (requires live RLS + cache behavior); the gate is the recorded SQL output showing link_type = 'applies_to' rows for both creates plus the before/after tab screenshots referenced in the SUMMARY.</automated>
  </verify>
  <acceptance_criteria>
    - Both entry points produced a 201 create (success toast, no error toast) — positions exist in the DB with status draft, author = test user, non-empty title_ar, real position_type_id
    - Both position_dossier_links rows exist with dossier_id = the SA dossier and link_type = 'applies_to' (SQL output recorded)
    - Position A appeared in the Positions tab list without a page reload (screenshot evidence; same session as the create — Pitfall 7)
    - Position B's audience round-tripped as Management only (position_audience_groups SQL output)
  </acceptance_criteria>
  <done>POSNEW-02's two live criteria are observed and recorded: link row DB-verified, tab refresh without manual reload.</done>
</task>

<task type="auto">
  <name>Task 2: AR/RTL, per-field direction, translate failure UX, and width verification</name>
  <files>none (live verification via agent-browser)</files>
  <read_first>
    - .planning/phases/64-new-position-from-dossier/64-UI-SPEC.md (§Bilingual & RTL Contract and §Interaction States 4-6 — the binding checks)
    - CLAUDE.md (§Definition of Done UI checklist; language persists under localStorage id.locale — switch via the topbar ع button, NOT i18nextLng)
  </read_first>
  <action>
    1. EN locale checks first: open the New Position dialog and confirm title_ar/content_ar inputs render dir=rtl (Arabic placeholder/typed text flows right-to-left) while the UI is LTR — per-field direction is content-driven, not locale-driven.
    2. Translate failure path (the live-verifiable D-07 path — AnythingLLM is down on staging): type text in title_en, click "Translate to Arabic". Expect: button shows the in-flight spinner then the localized "Translation unavailable. Enter the text manually." error toast; title_ar stays EMPTY (no placeholder text like [ترجمة مطلوبة] pasted in); submit still becomes enabled after typing title_ar manually. Screenshot the toast. If the AI backend happens to be up and translation succeeds instead, record that the happy path was observed live and verify the draft-fill is editable.
    3. AR locale: click the topbar ع button. Re-open the dialog from the tab. Confirm: document dir=rtl with Tajawal, all dialog labels/legend/buttons render the Arabic strings from positions.json AR (type نوع الموقف, audience مجموعات الجمهور, submit إنشاء موقف), position-type options and audience labels show name_ar values, title_en field still renders dir=ltr, and validation messages appear in Arabic (blur an emptied required field). Switch back to EN afterward.
    4. Widths (desktop-primary): screenshot the open dialog at 1280px and 1024px viewport widths in both EN and AR — no clipped controls, no horizontal overflow, footer buttons reachable.
    5. Record all evidence references in the SUMMARY. Note explicitly: the translate HAPPY path and the D-11 partial-failure path are covered by unit tests (mocked) only — partial failure cannot be safely forced on live staging; this is the accepted phase posture per VALIDATION.md and D-07.
  </action>
  <verify>
    <automated>MISSING — live visual verification, manual-only justified per VALIDATION.md; evidence = screenshots referenced in the SUMMARY (AR dialog, translate-unavailable toast, 1280/1024 widths).</automated>
  </verify>
  <acceptance_criteria>
    - title_ar renders RTL in EN locale; title_en renders LTR in AR locale (screenshots)
    - Translate click on staging produced the unavailable error toast with the target field untouched, and manual entry still enabled submission (or the documented happy-path observation if AI was up)
    - AR mode shows Arabic labels for every dialog control and bilingual lookup names; dir=rtl + Tajawal confirmed
    - Dialog usable at 1280px and 1024px in both locales (4 screenshots)
  </acceptance_criteria>
  <done>The bilingual/RTL contract and the non-blocking translate behavior are observed live; accepted coverage gaps are recorded honestly.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gates (suite, types, bundle) and staging cleanup</name>
  <files>none (gates + live cleanup via Supabase MCP)</files>
  <read_first>
    - .planning/phases/64-new-position-from-dossier/64-VALIDATION.md (§Sampling Rate — the phase-gate definition this task realizes)
    - frontend/.size-limit.json (current budgets — entry and total were within ~10 KB of limits at last check; project memory: grep ALL exceeded lines, failures stack hidden)
  </read_first>
  <action>
    1. Gates: `cd frontend && pnpm exec vitest run` (full suite), `pnpm type-check`, and `pnpm exec size-limit` — tee the size-limit output and grep the FULL log case-insensitively for `exceeded`: zero matches required. If a budget trips, record the exact numbers and stop for a budget discussion rather than silently bumping .size-limit.json.
    2. Cleanup via `mcp__supabase__execute_sql` (service role): delete the two verification positions in dependency order — position_dossier_links, position_audience_groups, position_versions, then positions — filtered by the two ids recorded in Task 1. Confirm with SELECT counts = 0 for both ids across all four tables. Record the cleanup confirmation in the SUMMARY.
    3. Compile the SUMMARY: entry-point matrix, SQL evidence, AR/width evidence references, gate outputs, cleanup confirmation, and the explicit coverage notes (partial-failure path = unit-only; translate happy path = unit-only unless observed; Positions tab exists only on country/topic dossiers — other types create+link correctly via the menu but render no tab, per Pitfall 6 and phase scope).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run && pnpm type-check && pnpm exec size-limit 2>&1 | tee /tmp/size-limit-64.log && ! grep -qi 'exceeded' /tmp/size-limit-64.log</automated>
  </verify>
  <acceptance_criteria>
    - Full frontend suite exits 0; type-check exits 0; size-limit log contains zero `exceeded` lines (whole output, case-insensitive)
    - Both verification positions and all their child rows are deleted from staging — SELECT counts 0 recorded
    - SUMMARY contains the complete evidence matrix with zero secret values
  </acceptance_criteria>
  <done>The phase is gate-green, live-verified on both roadmap criteria, and staging carries zero verification residue — ready for /gsd:verify-work.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                             |
| ----------------------------------- | ------------------------------------------------------- |
| test credentials → browser session  | .env.test secrets used for live login                   |
| verification SQL → staging database | service-role reads/deletes with elevated MCP privileges |

## STRIDE Threat Register

| Threat ID | Category               | Component                              | Disposition | Mitigation Plan                                                                                                                            |
| --------- | ---------------------- | -------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| T-64-15   | Information Disclosure | credentials/JWTs leaking into evidence | mitigate    | Secrets sourced from .env.test as env vars; acceptance criteria forbid secret values in SUMMARY, logs, or screenshots of credential fields |
| T-64-16   | Tampering              | service-role cleanup deletes           | mitigate    | Deletes filtered by the two recorded position ids only, in dependency order, with SELECT-count-0 confirmation — no broad deletes           |
| T-64-SC   | Tampering              | npm/pip/cargo installs                 | accept      | No package installs in this plan                                                                                                           |

</threat_model>

<verification>
- VALIDATION.md manual-only row realized: "Link row exists in DB; tab renders new position without refresh" → Task 1
- VALIDATION.md phase gate realized: full suite green + live staging verification before /gsd:verify-work → Task 3
</verification>

<success_criteria>

- Roadmap success criteria 2 and 3 observed live and recorded with SQL + screenshot evidence
- Bilingual/RTL contract verified at both DoD widths; translate failure path verified non-blocking
- All gates green; staging clean
  </success_criteria>

<output>
Create `.planning/phases/64-new-position-from-dossier/64-06-SUMMARY.md` when done (evidence matrix, no secrets).
</output>
