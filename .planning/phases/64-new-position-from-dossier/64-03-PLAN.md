---
phase: 64-new-position-from-dossier
plan: 03
type: execute
wave: 2
depends_on: [64-02]
files_modified:
  - frontend/src/components/positions/NewPositionDialog.tsx
  - frontend/src/components/positions/__tests__/NewPositionDialog.test.tsx
autonomous: true
requirements: [POSNEW-01]

must_haves:
  truths:
    - 'Submit is disabled until position type, title_en, title_ar, and at least one audience group are valid, with localized inline messages (D-04)'
    - 'Standard Position is preselected and All Staff is pre-checked by name-match with fallback to the first row — never by hard-coded UUID (D-05/D-06)'
    - 'Each title and content field pair has a translate button that fills the COUNTERPART field as an editable draft on 2xx only; any thrown error shows the unavailable toast and leaves the field untouched — submission never depends on the AI (D-07/D-08)'
    - 'title_ar and content_ar inputs render dir=rtl with the Arabic font even in EN locale; title_en/content_en always dir=ltr (UI-SPEC Bilingual Contract)'
    - 'The dialog is visually indistinguishable in chrome from its sibling commitment/event dialogs: sm:max-w-lg, icon title, DossierContextBadge, min-h-11 footer buttons (D-01)'
  artifacts:
    - path: 'frontend/src/components/positions/NewPositionDialog.tsx'
      provides: 'Extracted quick-create dialog: RHF+Zod form, lookups, defaults, translate assists'
      contains: 'zodResolver'
      min_lines: 200
    - path: 'frontend/src/components/positions/__tests__/NewPositionDialog.test.tsx'
      provides: 'Wave 0 unit coverage for all six POSNEW behavior rows (submit-flow cases staged as skips for 64-04)'
      contains: 'NewPositionDialog'
      min_lines: 150
  key_links:
    - from: 'frontend/src/components/positions/NewPositionDialog.tsx'
      to: 'position_types / audience_groups lookups'
      via: 'usePositionTypes / useAudienceGroups domain hooks'
      pattern: 'usePositionTypes'
    - from: 'frontend/src/components/positions/NewPositionDialog.tsx'
      to: 'translate-content edge'
      via: 'translateContent repository wrapper'
      pattern: 'translateContent'
    - from: 'frontend/src/components/positions/__tests__/NewPositionDialog.test.tsx'
      to: 'frontend/src/components/positions/NewPositionDialog.tsx'
      via: 'component import under test'
      pattern: "from '../NewPositionDialog'"
---

<objective>
Build the form layer of the extracted `NewPositionDialog`, test-first: props contract + skeleton, the full six-case test file (submit-flow cases staged as explicit skips for plan 64-04), then the form — type picker, bilingual titles with translate assists, optional content pair, audience checkboxes, Zod validation with i18n-key messages, and name-match defaults.

Purpose: POSNEW-01 — the dialog must offer a real position-type picker, bilingual title fields, and audience-group selection that can satisfy positions-create validation (replacing position_type_id = dossier_id, blank title_ar, empty audience_groups).
Output: NewPositionDialog form layer with the form-subset tests GREEN; submit flow lands in 64-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/64-new-position-from-dossier/64-PATTERNS.md
@.planning/phases/64-new-position-from-dossier/64-UI-SPEC.md

<interfaces>
<!-- Props contract — defined in Task 1, consumed by 64-04 (AddToDossierDialogs) and 64-05 (DossierPositionsTab) -->

```typescript
import type { DossierContextForAction } from '@/hooks/useAddToDossierActions'
// DossierContextForAction (useAddToDossierActions.tsx L35-41):
// { dossier_id: string; dossier_type: DossierType; dossier_name_en: string;
//   dossier_name_ar: string | null; inheritance_source: InheritanceSource }

export interface NewPositionDialogProps {
  isOpen: boolean
  onClose: () => void
  dossierContext: DossierContextForAction
  isRTL: boolean
}
```

<!-- Payload contract — types/position.ts L77-89 (CreatePositionRequest is an alias of CreatePositionPayload, L125) -->

```typescript
export interface CreatePositionPayload {
  position_type_id: string
  title_en: string
  title_ar: string
  content_en?: string
  content_ar?: string
  audience_groups: string[] // Array of audience group IDs
}
// thematic_category / rationale / alignment fields exist but are OMITTED this phase (deferred per CONTEXT)
```

<!-- Zod schema shape (messages are i18n KEYS rendered via t(errors.field.message) — TaskEditDialog precedent) -->

```typescript
const newPositionSchema = z.object({
  position_type_id: z.string().min(1, 'positions:validation.type_required'),
  title_en: z
    .string()
    .min(1, 'positions:validation.title_en_required')
    .max(200, 'positions:validation.title_max_length'),
  title_ar: z
    .string()
    .min(1, 'positions:validation.title_ar_required')
    .max(200, 'positions:validation.title_max_length'),
  content_en: z.string().optional(),
  content_ar: z.string().optional(),
  audience_groups: z.array(z.string()).min(1, 'positions:validation.audience_required'),
})
```

<!-- Module paths the dialog imports (and the test mocks — same paths, exactly) -->

```typescript
import { usePositionTypes } from '@/domains/positions/hooks/usePositionTypes'
import { useAudienceGroups } from '@/domains/positions/hooks/useAudienceGroups'
import { useCreatePosition } from '@/domains/positions/hooks/useCreatePosition'
import {
  createPositionDossierLink,
  translateContent,
} from '@/domains/positions/repositories/positions.repository'
import { dossierOverviewKeys } from '@/services/dossier-overview.service'
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Props contract + skeleton, then the six-case test file (RED)</name>
  <files>frontend/src/components/positions/NewPositionDialog.tsx, frontend/src/components/positions/__tests__/NewPositionDialog.test.tsx</files>
  <read_first>
    - frontend/src/components/dossier/__tests__/ExportDossierDialog.test.tsx (the Phase-62 dialog-test analog: mutable test state, mock-before-import structure, decision-tagged test names)
    - frontend/src/components/dossier/AddToDossierDialogs.tsx (L604-695 PositionDialog frame to replicate in the skeleton; L117-144 the file-local DossierContextBadge to copy; L87-93 ActionDialogProps shape)
    - .planning/phases/64-new-position-from-dossier/64-PATTERNS.md (§NewPositionDialog.test.tsx — full mock list; §NewPositionDialog — import set)
    - .planning/phases/64-new-position-from-dossier/64-VALIDATION.md (§Per-Task Verification Map — the five behavior rows this file must cover)
  </read_first>
  <action>
    1. Create `frontend/src/components/positions/NewPositionDialog.tsx` as a SKELETON: export `NewPositionDialogProps` (interfaces block) and a named `NewPositionDialog` component rendering only the sibling dialog frame — Dialog/DialogContent `sm:max-w-lg`, DialogTitle with MessageSquare icon reading `t('dossier:addToDossier.dialogs.position.title')`, DialogDescription, a local `DossierContextBadge` sub-component copied from AddToDossierDialogs.tsx L117-144 (copy it locally because AddToDossierDialogs.tsx is owned by plan 64-04 — do NOT import from or edit that file; the 27-line badge reads `t('dossier:addToDossier.context.linkedTo')`), and a DialogFooter with Cancel (outline) + a permanently-disabled submit Button, all `min-h-11`. Use `useTranslation(['positions', 'dossier'])` with colon-form keys throughout (`positions:create_dialog.*`, `dossier:addToDossier.*` — never dot-form, project i18n trap). No form logic yet.
    2. Create `frontend/src/components/positions/__tests__/NewPositionDialog.test.tsx` following the ExportDossierDialog analog (mocks declared BEFORE importing the component). Mock by these exact module paths: `react-i18next` (t returns the key or a local map value; i18n.language from mutable test state), `@/domains/positions/hooks/usePositionTypes` (return data: 2 staging-shaped rows — Standard Position/موقف قياسي and Critical Position/موقف حرج with distinct ids, isLoading false, error null), `@/domains/positions/hooks/useAudienceGroups` (4 rows: All Staff/جميع الموظفين, Management, Policy Officers, External Relations), `@/domains/positions/hooks/useCreatePosition` (returns mutateAsync spy `createMock` + isPending false), `@/domains/positions/repositories/positions.repository` (createPositionDossierLink spy `linkMock`, translateContent spy `translateMock`), `sonner` (toast.success/warning/error spies), `@tanstack/react-router` (useNavigate spy). Render inside a real QueryClientProvider and `vi.spyOn(queryClient, 'invalidateQueries')`. baseProps: isOpen true, onClose spy, dossierContext { dossier_id: 'd-1', dossier_type: 'country', dossier_name_en: 'Saudi Arabia', dossier_name_ar: 'السعودية', inheritance_source: 'direct' }, isRTL false.
    3. Write exactly these six tests (names verbatim — task 2 and plan 64-04 filter on them):
       (a) `it('disables submit until type, both titles, and at least one audience group are valid (POSNEW-01, D-04)')` — submit disabled initially; after typing into title_en and title_ar (defaults supply type + audience) submit becomes enabled; clearing title_ar disables it again.
       (b) `it('preselects Standard Position and pre-checks All Staff by name-match (POSNEW-01, D-05/D-06)')` — the type control shows Standard Position; the All Staff checkbox is checked; the other three are unchecked.
       (c) `it('translate failure leaves the target field untouched and never blocks submission (POSNEW-01, D-07)')` — translateMock rejects; click the EN-title translate button; title_ar value stays empty, toast.error called once, and submit can still become enabled by typing manually.
       (d) `it.skip('submits real position_type_id and audience_groups then links with applies_to (POSNEW-02, D-09/D-10)')` — fill titles, submit; createMock called with payload whose position_type_id is the Standard row id, title_ar non-empty, audience_groups = [All Staff id]; then linkMock called with (created position id, { dossier_id: 'd-1', link_type: 'applies_to' }).
       (e) `it.skip('invalidates dossier-position-links and overview keys on full success (POSNEW-02, D-12)')` — invalidateQueries called with queryKey ['dossier-position-links', 'd-1'] and with dossierOverviewKeys.detail('d-1'); toast.success called; onClose called.
       (f) `it.skip('shows a warning toast with retry, not success, when the link write fails (POSNEW-02, D-11)')` — createMock resolves, linkMock rejects; toast.warning called once with an action whose onClick re-invokes linkMock with the same applies_to args; toast.success NOT called.
       Tests d/e/f use `it.skip` with a one-line comment `// unskipped + implemented in plan 64-04 (submit flow)`. Tests a–c are ACTIVE and must FAIL against the skeleton (RED). Drive interactions via @testing-library user-event; avoid Radix Select pointer interaction by relying on the D-05 default for the type value. ESLint: explicit return types on all mock functions, no `any`, single quotes, no semicolons.
    4. Run the file and confirm tests a–c fail for behavior reasons (missing form), not import/transform crashes. Commit the RED state.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/components/positions/__tests__/NewPositionDialog.test.tsx 2>&1 | grep -qE '3 failed|failed.*3' && echo RED_CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - NewPositionDialog.tsx exists exporting NewPositionDialogProps and NewPositionDialog (named exports), rendering the sibling frame with a local DossierContextBadge; it imports nothing from AddToDossierDialogs.tsx
    - Test file exists with the six named tests; d/e/f are it.skip with the 64-04 handoff comment; a–c are active
    - `cd frontend && pnpm exec vitest run src/components/positions/__tests__/NewPositionDialog.test.tsx` reports exactly 3 failed / 3 skipped — RED for behavior reasons (no import crash)
    - All mocks target the exact module paths listed in the interfaces block (so the implementation's imports resolve to them without test edits)
  </acceptance_criteria>
  <done>The behavior contract for POSNEW-01/POSNEW-02 is pinned in a runnable test file; the component skeleton fixes the props contract for plans 64-04/64-05.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the form layer — pickers, validation, defaults, translate assists (form-subset GREEN)</name>
  <files>frontend/src/components/positions/NewPositionDialog.tsx</files>
  <read_first>
    - frontend/src/components/positions/NewPositionDialog.tsx (the Task 1 skeleton being extended)
    - frontend/src/components/tasks/TaskEditDialog.tsx (RHF+Zod with i18n-key messages: useForm + zodResolver wiring L70-80, FormField render L155-171)
    - frontend/src/components/waiting-queue/AssigneeFilter.tsx (L41-52 — Checkbox + Label pairing with logical text-start)
    - .planning/phases/64-new-position-from-dossier/64-UI-SPEC.md (§Dialog layout, §Interaction States 1-6, §Bilingual & RTL Contract — the binding visual/interaction contract)
  </read_first>
  <action>
    Extend NewPositionDialog.tsx into the full form per the UI-SPEC layout contract (top to bottom: DossierContextBadge, type Select, title_en, title_ar, content_en, content_ar, audience fieldset, footer):
    1. Form state: `useForm` with `zodResolver(newPositionSchema)` (schema from the interfaces block — messages are the `positions:validation.*` keys added in 64-02), `mode: 'onTouched'`, defaultValues all empty / audience_groups []. Use the `Form`/`FormField`/`FormItem`/`FormLabel`/`FormControl`/`FormMessage` primitives from `@/components/ui/form`; render validation messages via `t(message)`. Submit button `disabled={!form.formState.isValid}` plus pending state later (64-04). Form-level onSubmit stays a no-op stub this plan (one-line comment: implemented in 64-04).
    2. Lookups: `usePositionTypes()` + `useAudienceGroups()`. While either isLoading: render the type Select trigger and checkbox group disabled and keep submit disabled (UI-SPEC state 1, no skeletons). On either error: replace the affected control group with the inline 12px `var(--danger)` message `t('positions:create_dialog.lookup_error')` and keep submit disabled (state 2).
    3. Defaults (D-05/D-06): when both lookups have data and the dialog is open and position_type_id is still '', apply once via form.reset or setValue: position_type_id = `(types.find((x) => x.name_en === 'Standard Position') ?? types[0])?.id`, audience_groups = `[(groups.find((x) => x.name_en === 'All Staff') ?? groups[0])?.id]`. Never hardcode UUIDs.
    4. Type picker: `Select` family from `@/components/ui/select`, min-h-11 trigger, label `t('positions:create_dialog.type_label')`, option label = `isRTL ? row.name_ar || row.name_en : row.name_en`.
    5. Title/content pairs: Input for titles (max enforced by Zod), Textarea rows={3} for contents. Per-field direction is CONTENT-driven, not locale-driven: title_en/content_en always `dir="ltr"`; title_ar/content_ar always `dir="rtl"` with the Arabic font class — in BOTH locales. Labels use the 64-02 keys; required fields (type, both titles, audience legend) carry the project's asterisk + aria-required convention. Label rows for fields with translate buttons: `flex items-center justify-between` (logical start/end only).
    6. Translate assists (D-07/D-08): one ghost `size="sm"` Button per field (4 total: EN title → fills title_ar labeled `t('positions:create_dialog.translate_to_ar')`; AR title → fills title_en labeled translate_to_en; same pair for contents with content_type 'content'). Handler: set in-flight state, call `translateContent({ text: <source field value>, direction, content_type })`; on resolve `form.setValue(targetField, res.translated_text, { shouldValidate: true, shouldDirty: true })` without stealing focus; on ANY throw `toast.error(t('positions:create_dialog.translate_unavailable'))` and leave the target untouched; finally clear in-flight. While in flight: the clicked button AND its direction pair are disabled, icon swaps Languages → Loader2 animate-spin, and an `aria-live="polite"` region announces `t('positions:create_dialog.translating')`. Disable-while-in-flight IS the debounce. No confidence display. Buttons disabled when the source field is empty.
    7. Audience checkboxes (D-03): fieldset with legend `t('positions:create_dialog.audience_legend')`, `grid grid-cols-1 sm:grid-cols-2 gap-2`, one Checkbox+Label row per group (whole row clickable via htmlFor), bilingual label pick by isRTL, checked from the audience_groups array field via setValue add/remove with shouldValidate.
    8. Design rules: tokens only (no raw hex, no Tailwind color literals), logical properties only (ms-/me-/ps-/pe-/text-start), min-h-11 inputs and footer buttons, no new button variants, sentence case, no emoji. Run the form-subset tests and type-check.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/components/positions/__tests__/NewPositionDialog.test.tsx -t 'D-04' && pnpm exec vitest run src/components/positions/__tests__/NewPositionDialog.test.tsx -t 'D-05' && pnpm exec vitest run src/components/positions/__tests__/NewPositionDialog.test.tsx -t 'D-07' && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - Tests a (D-04), b (D-05/D-06), c (D-07) pass; d/e/f remain skipped; zero failing tests in the file
    - NewPositionDialog.tsx contains `mode: 'onTouched'`, `zodResolver`, and the name-match literals `'Standard Position'` and `'All Staff'` with `?? types[0]` / `?? groups[0]` fallbacks
    - title_ar and content_ar controls carry `dir="rtl"`; title_en and content_en carry `dir="ltr"` (grep-verifiable)
    - Zero occurrences of `ml-`, `mr-`, `pl-`, `pr-`, `text-left`, `text-right` Tailwind utilities and zero raw hex colors in NewPositionDialog.tsx
    - `cd frontend && pnpm type-check` exits 0
  </acceptance_criteria>
  <done>The form layer satisfies POSNEW-01's picker/titles/audience contract with localized validation and non-blocking translate assists; submit wiring is the only remaining gap (64-04).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                       |
| ------------------------------ | ----------------------------------------------------------------- |
| user input → form state        | untrusted text enters RHF state and (in 64-04) the create payload |
| AI edge response → form fields | machine-generated text written into controlled inputs             |

## STRIDE Threat Register

| Threat ID | Category               | Component                     | Disposition | Mitigation Plan                                                                                                                                                                    |
| --------- | ---------------------- | ----------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-64-07   | Tampering              | translated text into fields   | mitigate    | Values flow only into React controlled inputs (auto-escaped); fill on 2xx only; never paste fallback/placeholder text (Pitfall 4); no dangerouslySetInnerHTML anywhere in the file |
| T-64-08   | Elevation of Privilege | client-side validation bypass | mitigate    | Client Zod is UX-only; positions-create re-validates type/titles/audience server-side against real tables (verified live, probe #3) — the server remains the authority             |
| T-64-09   | Denial of Service      | repeated translate clicks     | mitigate    | Clicked + paired buttons disabled while in flight (the debounce); edge enforces the 10k-char limit server-side                                                                     |
| T-64-SC   | Tampering              | npm/pip/cargo installs        | accept      | No package installs in this plan                                                                                                                                                   |

</threat_model>

<verification>
- VALIDATION.md rows realized: "Submit disabled until valid", "Defaults by name-match", "Translate failure non-blocking" → active tests a–c GREEN; submit-flow rows staged as named skips for 64-04
- `cd frontend && pnpm exec vitest run src/components/positions/__tests__/NewPositionDialog.test.tsx && pnpm type-check` exits 0 at plan end (3 passed / 3 skipped)
- Wave-merge gate: full frontend suite green
</verification>

<success_criteria>

- POSNEW-01 form contract implemented and unit-pinned: real type picker, required bilingual titles, audience multi-select, localized inline validation, robust defaults
- Translate assist is a drafting aid that fails safe — submission never depends on the AI
- Props contract published for the two entry-point plans
  </success_criteria>

<output>
Create `.planning/phases/64-new-position-from-dossier/64-03-SUMMARY.md` when done.
</output>
