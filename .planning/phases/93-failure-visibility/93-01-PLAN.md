---
phase: 93-failure-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/error-states/QueryErrorState.tsx
  - frontend/src/components/error-states/__tests__/QueryErrorState.test.tsx
  - frontend/src/i18n/en/common.json
  - frontend/src/i18n/ar/common.json
autonomous: true
requirements: [TRUST-02, TRUST-04]
truths:
  - 'Decisions covered — D-03: one shared QueryErrorState component, reused by every surface this phase touches, never bespoke error markup per page; D-04: the error state is bilingual, announced via role="alert", and internal-string-free, with keys landing in BOTH locales in the same commit; D-17: the phase-93-base tag (C7 anchor) is verified signed before any consumer gate runs'
must_haves:
  truths:
    - 'QueryErrorState renders variant A (page) with data-testid="query-error-state" and variant B (inline) with data-testid="query-error-inline", both role="alert", both retry-wired'
    - 'All seven new common:errors.* keys exist in en AND ar, and the en/ar errors key SETS are equal — key-set equality is the property that protects the Arabic user, derived, never a frozen count'
    - 'refs/tags/phase-93-base exists and git tag -v exits 0'
    - "DelegationManagementPage.tsx keeps its Phase 92 inline error block — a NAMED NON-CONSUMER of the shared component this phase (C9b: tests/e2e/92-delegations-error.spec.ts asserts that block's current DOM; extracting it would red a prior phase's spec for a correct change). Retrofit deferred; whichever phase swaps it updates that spec in the SAME task"
  artifacts:
    - path: 'frontend/src/components/error-states/QueryErrorState.tsx'
      provides: 'the shared error state every criterion-2/3/5 surface consumes; the data-testid contract every 93-* spec keys on'
      contains: 'query-error-state'
    - path: 'frontend/src/i18n/en/common.json'
      provides: 'errors.retry / errors.queryFailed.* / errors.queryFailedInline / errors.countUnavailable / errors.incompleteRecord.*'
      contains: 'queryFailed'
  key_links:
    - from: 'frontend/src/components/error-states/QueryErrorState.tsx'
      to: 'frontend/src/components/ui/alert.tsx'
      via: 'variant B composes the shipped Alert destructive variant (role="alert" built in)'
      pattern: 'Alert'
    - from: 'frontend/src/components/error-states/__tests__/QueryErrorState.test.tsx'
      to: 'frontend/src/components/error-states/QueryErrorState.tsx'
      via: 'unit test is the C9a positive control of the data-testid contract before any consumer plan runs'
      pattern: 'query-error-state'
---

<objective>
Produce the one shared query-error component (D-03/D-04) plus its seven bilingual i18n keys, and
anchor the phase (phase-93-base tag, C7). This plan is the PRODUCER for every wave-2/3 surface plan:
the `query-error-state` / `query-error-inline` testids and the `common:errors.*` keys are the
cross-plan contract, positive-controlled here by the component's own unit test.

Purpose: 13 empty-state components exist and zero error-state components — that absence is a
plausible cause of the whole failure-rendered-as-emptiness class. Build the missing vocabulary once.
Output: `components/error-states/QueryErrorState.tsx` + test + keys in both locales + verified tag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-failure-visibility/93-UI-SPEC.md  (Component Contracts §1 + Copywriting Contract are BINDING)
@.planning/phases/93-failure-visibility/93-PATTERNS.md  (§1 — the four analogs to compose; excerpts verbatim)
@frontend/DESIGN.md
@frontend/src/design-system/CLAUDE.md

<interfaces>
Verified by research/patterns (re-read at execution, never retype from memory):
- Variant A markup already exists inline (unshared) at
  `pages/delegations/DelegationManagementPage.tsx:215-237` — lift it; two deltas per UI-SPEC:
  icon is AlertCircle (not AlertTriangle), retry button carries NO icon.
- Variant B composes `components/ui/alert.tsx` destructive variant as shipped (role="alert" built
  in, logical properties already: `start-4`, `ps-7`). Do not restyle alert.tsx.
- Prop-interface idiom: `components/empty-states/EmptyState.tsx:16-37` (JSDoc'd props, variant
  unions, testId default, named export function).
- Test idiom: `components/empty-states/__tests__/ListEmptyState.test.tsx:17-57` (vi.mock
  react-i18next with t echoing keys; useDirection mock; plain DOM queries).
- i18n: `common` namespace is registered in `src/i18n/index.ts` for BOTH languages — extend its
  existing `errors` object; do NOT create a new namespace (an unregistered namespace silently
  falls back to English in both locales). `public/locales` is dead — never touch it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify (or create) the signed phase-93-base tag</name>
  <files></files>
  <read_first>Root CLAUDE.md §Tag signing setup (signing is user-local and already configured on this machine — phase-92-base verifies)</read_first>
  <action>
    The tag is expected to exist before execution (C7). Verify it. If absent, create it at the
    phase-base commit: the parent of the earliest commit whose subject contains "(93-" if any
    Phase-93 plan commit already landed, otherwise current HEAD. Create as an annotated SSH-signed
    tag: `git tag -s phase-93-base -m "Phase 93 base: failure visibility" <commit>`. Do NOT push
    the tag (tags push after merge, per project convention). Every later C7 scope diff in this
    phase anchors to this tag, and the anchor's existence is verified in the same chain as any
    diff that uses it.
  </action>
  <verify>
    <automated>git rev-parse -q --verify refs/tags/phase-93-base >/dev/null && git tag -v phase-93-base</automated>
  </verify>
  <acceptance_criteria>refs/tags/phase-93-base resolves; `git tag -v phase-93-base` exits 0 with a good signature; the tagged commit is the post-planning, pre-execution HEAD — planning complete, so every later code change in this phase is execution work. (Corrected per `RULING-P93-02` Ruling A, the one authorized plan-text edit of this phase: the original clause read "the tagged commit predates every Phase-93 plan commit", which the created tag deliberately does not satisfy. The tag points at `e185f175`, matching Phase 92's precedent — anchoring earlier would pull the `scripts/gate-drill.mjs` repair into this phase's code-scope diff and every `scripts/` guard would flag it. Retagging was refused.)</acceptance_criteria>
  <done>The C7 anchor exists and is signed.</done>
</task>

<task type="auto">
  <name>Task 2: QueryErrorState component + unit test (the testid contract, positive-controlled)</name>
  <files>frontend/src/components/error-states/QueryErrorState.tsx, frontend/src/components/error-states/__tests__/QueryErrorState.test.tsx</files>
  <read_first>93-UI-SPEC.md §Component Contracts 1 (whole section — the visual contract); 93-PATTERNS.md §1 analogs A-D; frontend/src/components/ui/alert.tsx in full; frontend/src/components/empty-states/EmptyState.tsx:16-37; frontend/src/pages/delegations/DelegationManagementPage.tsx:215-237</read_first>
  <action>
    Create `components/error-states/QueryErrorState.tsx` (kebab-case folder, PascalCase file per
    ESLint per-directory rules). Named export function, explicit return type. Props (internal shape
    is discretion; the contract below is not): a `variant` union 'page' | 'inline', an `onRetry`
    callback wired to TanStack `refetch`, optional `className`, optional `testId` override.
    Contract, per UI-SPEC:
    - variant 'page': centered, max-w-md, py-12; AlertCircle size-8 text-danger inside a size-16
      rounded-full bg-danger/10 wrapper (opacity modifier, never a -soft utility); title 16px/600
      text-ink from t('errors.queryFailed.title'); body 13px/400 text-ink-mute from
      t('errors.queryFailed.description'); ONE .btn-primary labelled t('errors.retry') with NO
      icon; wrapper role="alert" and data-testid="query-error-state".
    - variant 'inline': composes the shipped Alert destructive variant (role="alert" inherited);
      AlertCircle size-4; one-line t('errors.queryFailedInline') at 13px; retry as .btn-ghost at
      the inline-end; data-testid="query-error-inline"; never stretches to fill an empty card.
    - Retry: while a refetch is in flight the button is disabled with aria-disabled, label
      unchanged. A second failure re-renders the same state.
    - Copy: i18n keys ONLY via useTranslation('common') — no error object prop is rendered. One
      structured exception: an optional bilingual-envelope prop ({ message_en, message_ar }) MAY
      render, selected by current language; message/details/code fields never reach JSX. No
      English-default second arguments on any t() call (that grows AR-04a's mask population).
    - Tokens only (no raw hex, no palette literals); logical properties only; sentence case; no
      emoji; radii from --radius tokens; no shadows, no gradients.
    Create the unit test mirroring ListEmptyState.test.tsx's mocks (react-i18next t echoes keys;
    useDirection mock). Assert: role="alert" present in both variants; data-testid values are
    exactly "query-error-state" (page) and "query-error-inline" (inline); exactly one .btn-primary
    in page variant and exactly one .btn-ghost in inline variant; clicking retry calls onRetry
    once; title/body render the echoed keys errors.queryFailed.title / errors.queryFailedInline.
    This test IS the C9a positive control for every consumer spec's testid selector.
    C9b boundary, explicit: this is an EXTRACTION — the variant-A markup is lifted FROM
    DelegationManagementPage.tsx:215-237, but that page is NOT retrofitted to consume the new
    component in this phase. tests/e2e/92-delegations-error.spec.ts (a shipped Phase 92 spec)
    asserts that block's current DOM shape, so swapping it would turn a prior phase's spec red for
    a correct change. The page stays on its inline block; it is recorded as a named non-consumer.
    Do not "clean it up" while you are in the neighborhood.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/components/error-states/__tests__/QueryErrorState.test.tsx && grep -q 'query-error-state' src/components/error-states/QueryErrorState.tsx && grep -q 'query-error-inline' src/components/error-states/QueryErrorState.tsx && test "$(grep -v '^[[:space:]]*//' src/components/error-states/QueryErrorState.tsx | grep -cE "t\('[^']+',[[:space:]]*'")" -eq 0 && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>Unit test green asserting both testids, role="alert", one retry per variant wired to onRetry, and key echoes; zero two-argument t() calls in the component; type-check clean. The three states' discriminators from UI-SPEC hold: this component is danger/AlertCircle/retry — it is NOT used for not-found (neutral/nav) or degraded (warn/status), which other plans render differently on purpose (D-05).</acceptance_criteria>
  <done>The shared error component exists with its testid contract proven by its own test.</done>
</task>

<task type="auto">
  <name>Task 3: Seven bilingual keys in the existing common namespace</name>
  <files>frontend/src/i18n/en/common.json, frontend/src/i18n/ar/common.json</files>
  <read_first>93-UI-SPEC.md §Copywriting Contract (the exact en copy and suggested ar renderings); frontend/src/i18n/en/common.json existing errors object; frontend/src/i18n/ar/common.json existing errors object</read_first>
  <action>
    Extend the EXISTING `errors` object in BOTH files in the same edit with exactly these keys and
    the UI-SPEC copy: errors.retry ("Try again" / "إعادة المحاولة"), errors.queryFailed.title
    ("Unable to load data" / "تعذر تحميل البيانات"), errors.queryFailed.description ("The request
    failed. Try again, and contact an administrator if it keeps failing." / UI-SPEC ar),
    errors.queryFailedInline ("Unable to load this section." / UI-SPEC ar),
    errors.countUnavailable ("Count unavailable" / "العدد غير متاح"), errors.incompleteRecord.title
    ("Incomplete record" / "سجل غير مكتمل"), errors.incompleteRecord.description ("This record is
    missing its linked dossier data. Some sections cannot be shown." / UI-SPEC ar). Align Arabic
    terminology with the existing dossiers/engagements namespace glossary before committing
    (UI-SPEC's ar column is the suggested rendering, not a byte-contract). Sentence case, no
    exclamation marks, no emoji. No other plan in this phase edits frontend/src/i18n/** — this
    plan owns both files.
  </action>
  <verify>
    <automated>node -e "const en=require('./frontend/src/i18n/en/common.json'),ar=require('./frontend/src/i18n/ar/common.json');const flat=(o,p=[])=>Object.entries(o||{}).flatMap(([k,v])=>typeof v==='object'&&v!==null?flat(v,[...p,k]):[[...p,k].join('.')]);const ek=new Set(flat(en.errors)),ak=new Set(flat(ar.errors));const symdiff=[...ek].filter(k=>!ak.has(k)).concat([...ak].filter(k=>!ek.has(k)));if(symdiff.length){console.error('errors key-set mismatch:',symdiff);process.exit(1)};const paths=[['errors','retry'],['errors','queryFailed','title'],['errors','queryFailed','description'],['errors','queryFailedInline'],['errors','countUnavailable'],['errors','incompleteRecord','title'],['errors','incompleteRecord','description']];const get=(o,p)=>p.reduce((a,k)=>a&&a[k],o);process.exit(paths.every(p=>{const e=get(en,p),a=get(ar,p);return typeof e==='string'&&typeof a==='string'&&e!==a})?0:1)"</automated>
  </verify>
  <acceptance_criteria>The en and ar `errors` key SETS are EQUAL (derived recursively from both files and compared — not a frozen count; the sets are identical today, so this gate is RED only if the edit breaks parity, which is exactly the regression it exists to catch), AND the 7 named keys resolve in both locales with differing values. Population: the full errors object in both files; outside it: every other top-level key in common.json, untouched. Both files changed in the same commit.</acceptance_criteria>
  <done>Every consumer plan can address common:errors.* in both languages.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                        |
| ---------------------------- | -------------------------------------------------- |
| query rejection → DOM render | internal error objects cross into user-facing copy |

## STRIDE Threat Register

| Threat ID | Category               | Component        | Disposition | Mitigation Plan                                                                                                                                                                                           |
| --------- | ---------------------- | ---------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-93-01   | Information Disclosure | QueryErrorState  | mitigate    | component renders i18n keys only; the sole structured exception is the bilingual envelope (message_en/message_ar); message/details/code never reach JSX; unit test + downstream copy-rule oracles enforce |
| T-93-SC   | Tampering              | package installs | accept      | zero installs this phase (research Package Legitimacy Audit: nothing to audit)                                                                                                                            |

</threat_model>

<verification>
- Unit test green; type-check clean; en/ar errors key-set equality + the 7 keys (node gate).
- C9b resolution recorded: option (b) — DelegationManagementPage is the extraction source and a
  named non-consumer; the shipped 92 spec keeps asserting its unchanged inline DOM.
- `git tag -v phase-93-base` exits 0.
- Visual conformance to UI-SPEC (spacing/type/color tables) is re-checked by the phase-end human
  render pass; this plan's gates prove structure and contract, not pixels.
</verification>

<success_criteria>
The producer artifacts exist and are positive-controlled: testid contract proven by the unit test,
seven keys proven bilingual by derivation, C7 anchor verified. Every wave-2/3 plan can now consume
them without codebase exploration.
</success_criteria>

<output>
Create `.planning/phases/93-failure-visibility/93-01-SUMMARY.md` when done.
</output>
