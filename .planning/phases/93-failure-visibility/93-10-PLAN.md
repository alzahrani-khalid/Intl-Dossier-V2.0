---
phase: 93-failure-visibility
plan: 10
type: execute
wave: 2
depends_on: [93-01]
files_modified:
  - frontend/src/domains/tags/hooks/useTagHierarchy.ts
  - frontend/src/components/tags/TagAnalytics.tsx
  - frontend/src/components/positions/AttachmentUploader.tsx
  - tests/e2e/93-tags-attachments-error.spec.ts
autonomous: true
requirements: [TRUST-02]
truths:
  - "Decisions covered — D-25: Tag Analytics is a STUB rendering an error over a SUCCESS (useTagAnalytics resolves a shape the component cannot read) — the fix REPOINTS the queryFn at tag-hierarchy/analytics + mv_tag_usage_analytics, which match the component's TagAnalyticsRow types column-for-column, rather than inventing a new endpoint or keeping a dead surface pretending"
must_haves:
  truths:
    - 'Tag Analytics renders data (or genuine emptiness) on success and query-error-state only on a REAL rejection — the error-over-success inverse lie is structurally impossible (the error branch keys on isError, not !stats)'
    - 'Position attachments render query-error-inline on a rejected attachments query — never "No attachments yet"'
    - 'If mv_tag_usage_analytics is unreadable/unpopulated on staging (assumption A1), the honest error/empty render IS the phase-correct outcome, recorded — not a red'
  artifacts:
    - path: 'frontend/src/domains/tags/hooks/useTagHierarchy.ts'
      provides: 'useTagAnalytics repointed at tag-hierarchy/analytics (invoke, throw on error) — the Promise.resolve stub is gone'
      contains: 'tag-hierarchy/analytics'
    - path: 'tests/e2e/93-tags-attachments-error.spec.ts'
      provides: 'forced-error oracles for both surfaces with scoped block patterns'
      contains: 'query-error-inline'
  key_links:
    - from: 'frontend/src/components/tags/TagAnalytics.tsx'
      to: 'frontend/src/domains/tags/hooks/useTagHierarchy.ts'
      via: 'isError (real rejection) drives the error branch; stats compute only from resolved data'
      pattern: 'useTagAnalytics'
---

<objective>
Close criterion 2's remaining two surfaces: repoint the Tag Analytics stub at the real endpoint
whose materialized view already speaks the component's types (D-25), and give the position
attachments section the inline error branch its `data: x = []` mask suppresses. Both were
audit-named confident lies — one in each direction.

Purpose: an error rendered over a success is the mirror image of the phase's defect and the same
class of lie.
Output: two honest surfaces + forced-error spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-failure-visibility/93-RESEARCH.md  (§Criterion 2 surfaces 3-4 — the stub mechanism and the MV column match)
@.planning/phases/93-failure-visibility/93-PATTERNS.md  (§4 in-file throw analog; §5 spec template)
@frontend/src/domains/tags/hooks/useTagHierarchy.ts
@frontend/src/components/tags/TagAnalytics.tsx
@frontend/src/components/positions/AttachmentUploader.tsx

<interfaces>
Verified by research (re-read at execution):
- useTagAnalytics (useTagHierarchy.ts:144-150) is a refactor stub: queryFn: () =>
  Promise.resolve({ totalTags: 0, categories: [], usage: [] }) — always succeeds, never touches
  the network. TagAnalytics.tsx shims the result as { data: TagAnalyticsRow[] } (:65-67); the stub
  has no data property → stats null → `if (error || !stats)` (:167) renders t('errors.loadFailed')
  = "Failed to load tags" over a success.
- The real endpoint: GET tag-hierarchy/analytics (supabase/functions/tag-hierarchy/index.ts:239-242)
  reads mv_tag_usage_analytics, whose 16 columns are exactly the TagAnalyticsRow interface
  (TagAnalytics.tsx:46-63). Repoint = one queryFn diff: functions.invoke GET, throw on error
  (the useFieldPermissions.ts invoke idiom is the analog).
- A1 caveat: the MV's staleness/grants were not verified live — if it errors or is empty, the
  honest render is the criterion-correct outcome; DBSEC-03 (Phase 100) owns the MV-exposure class.
- AttachmentUploader.tsx:43 — data: existingAttachments = [], isLoading from
  usePositionAttachments (invoke, throws on error). No isError → any rejection renders
  t('positions:attachments_uploader.noAttachments'). The underlying CORS condition is not
  re-diagnosed here — the honest inline error IS the phase-correct outcome if it persists.
- Tag mutation policies (tag_categories/entity_tag_assignments) are 93-04's migration; their
  oracle is mutation-path SQL there, NOT the analytics render here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint the stub; error branch keys on isError</name>
  <files>frontend/src/domains/tags/hooks/useTagHierarchy.ts, frontend/src/components/tags/TagAnalytics.tsx</files>
  <read_first>useTagHierarchy.ts:130-170 (the stub in context); TagAnalytics.tsx in full (:46-63 types, :65-67 shim, :167 branch); frontend/src/hooks/useFieldPermissions.ts (the functions.invoke GET → throw idiom)</read_first>
  <action>
    Hook: replace the Promise.resolve stub with a real fetch of `tag-hierarchy/analytics` via
    supabase.functions.invoke (GET), throwing on error, returning the rows in the shape the
    component's shim expects ({ data: TagAnalyticsRow[] } — align the shim or the return, one
    side, smallest diff). Cap retries at 2 for this invoke-backed query (UI-SPEC retry policy —
    invoke errors carry no status, so the default ladder runs full otherwise).
    Component: the error branch keys on isError ONLY — `error || !stats` is dismantled; a resolved
    empty dataset renders the existing empty vocabulary, a rejection renders QueryErrorState
    variant 'inline' (the analytics card owns the query) with retry; the hardcoded
    "Failed to load tags"-over-success render is unreachable by construction.
  </action>
  <verify>
    <automated>cd frontend && F=src/domains/tags/hooks/useTagHierarchy.ts && test -f "$F" && test "$(grep -v '^[[:space:]]*//' "$F" | grep -c 'Promise\.resolve({ totalTags')" -eq 0 && grep -q 'tag-hierarchy/analytics' "$F" && test "$(grep -v '^[[:space:]]*//' src/components/tags/TagAnalytics.tsx | grep -cE 'error \|\| !stats')" -eq 0 && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>The stub literal is gone and the real endpoint named (RED today: stub present, endpoint absent); the `error || !stats` collapse is gone from the component; type-check clean. Success renders data/emptiness, rejection renders the shared inline state — the two truths D-25 says the repoint restores at once.</acceptance_criteria>
  <done>Tag Analytics is a live surface telling the truth in both directions.</done>
</task>

<task type="auto">
  <name>Task 2: AttachmentUploader inline error + the two-surface spec</name>
  <files>frontend/src/components/positions/AttachmentUploader.tsx, tests/e2e/93-tags-attachments-error.spec.ts</files>
  <read_first>AttachmentUploader.tsx:30-60 (the destructure) and ~:480-500 (the noAttachments render); tests/e2e/92-delegations-error.spec.ts (template)</read_first>
  <action>
    Component: destructure isError/refetch from usePositionAttachments; while isError the
    attachments SECTION renders QueryErrorState variant 'inline' with retry (the section owns the
    query — UI-SPEC "which variant where"); the noAttachments empty copy renders only on a
    resolved-empty result. Cap retries at 2 (invoke-backed).
    Spec: TWO tests, 92-template clone (inline auth, --no-deps, 15s budgets, DOM-only):
    1. Tags: CDP block `*tag-hierarchy*` → visit /tags analytics tab → query-error-inline visible;
       no internal-string regex match.
    2. Attachments: navigate to a real position detail (select an id via the positions list or a
       service-role query in beforeAll), block `*/attachments*` (scoped so the position detail
       fetch itself is NOT blocked — the assertion that the position title renders proves the
       scoping), assert query-error-inline in the attachments section and the noAttachments copy
       absent.
  </action>
  <verify>
    <automated>cd frontend && grep -q 'isError' src/components/positions/AttachmentUploader.tsx && grep -q 'QueryErrorState' src/components/positions/AttachmentUploader.tsx && cd .. && OUT=$(pnpm exec playwright test tests/e2e/93-tags-attachments-error.spec.ts --project=chromium-en --no-deps 2>&1) && echo "$OUT" | grep -qE '\b2 passed'</automated>
  </verify>
  <acceptance_criteria>2 passed under --no-deps (threshold 2 = the two tests above); the attachments test's position-detail-renders assertion proves the block pattern discriminated; "No attachments yet" cannot render over a rejection.</acceptance_criteria>
  <done>Both surfaces render failure as failure, emptiness as emptiness.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                     |
| --------------------------------- | ----------------------------------------------- |
| invoke rejection → section render | failure crosses into empty/onboarding copy here |

## STRIDE Threat Register

| Threat ID | Category               | Component          | Disposition | Mitigation Plan                                                       |
| --------- | ---------------------- | ------------------ | ----------- | --------------------------------------------------------------------- |
| T-93-19   | Repudiation            | AttachmentUploader | mitigate    | isError branch; empty copy gated on resolved-empty only               |
| T-93-20   | Repudiation (inverse)  | TagAnalytics       | mitigate    | error branch keys on isError; success can no longer render error copy |
| T-93-21   | Information Disclosure | forced renders     | mitigate    | spec regex; QueryErrorState renders i18n only                         |
| T-93-SC   | Tampering              | package installs   | accept      | zero installs                                                         |

</threat_model>

<verification>
- Source gates + type-check + spec 2/2 --no-deps.
- A1 fallback recorded if hit: an honest error/empty on the natural tags visit closes the
  criterion; making the MV fresh/readable is Phase 100's (DBSEC-03) business.
</verification>

<success_criteria>
Criterion 2's last two surfaces hold: the inverse lie is dismantled by repointing at the endpoint
that already matches, and the attachments mask is replaced by the shared inline state — both proven
by rendering oracles with discriminating blocks.
</success_criteria>

<output>
Create `.planning/phases/93-failure-visibility/93-10-SUMMARY.md` when done.
</output>
