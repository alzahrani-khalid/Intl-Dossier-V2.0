---
phase: 93-failure-visibility
plan: 08
type: execute
wave: 2
depends_on: [93-01]
files_modified:
  - frontend/src/hooks/useWidgetDashboard.ts
  - frontend/src/pages/custom-dashboard/CustomDashboardPage.tsx
  - tests/e2e/93-custom-dashboard-error.spec.ts
autonomous: true
requirements: [TRUST-01]
truths:
  - "Decisions covered — D-21: site 6 sits over supabase-js, which NEVER throws — deleting the catch at useWidgetDashboard.ts:726 changes NOTHING; the fix destructures error from each of the four sub-queries and throws FIRST (the file's own siblings at :597,628,656,685 are the pattern), and the widgetData aggregation that discards isError is repaired in the same plan so widget rejections stop rendering as zeros"
must_haves:
  truths:
    - 'A rejected stats sub-query rejects fetchStatsSummary (thrown .error), and the stats chrome renders em-dash/inline error — never four confident zeros'
    - 'The widgetData aggregation surfaces per-widget isError; a failed widget renders query-error-inline while succeeding siblings render normally'
    - 'useWidgetDashboard.ts contains no zero-filled catch fallback (the "Failed to fetch stats summary" catch is gone)'
  artifacts:
    - path: 'frontend/src/hooks/useWidgetDashboard.ts'
      provides: 'fetchStatsSummary with per-result error throws; aggregation carrying isError per widget index'
      contains: 'throw'
    - path: 'tests/e2e/93-custom-dashboard-error.spec.ts'
      provides: 'forced-rejection oracle for the stats region (never zeros) and per-widget isolation'
      contains: 'query-error-inline'
  key_links:
    - from: 'frontend/src/pages/custom-dashboard/CustomDashboardPage.tsx'
      to: 'frontend/src/hooks/useWidgetDashboard.ts'
      via: 'per-widget isError consumed from the repaired aggregation (today only widgetData data crosses)'
      pattern: 'isError'
---

<objective>
Close TRUST-01 site 6 plus the aggregation swallow (research's non-catch classes 1 and 2):
fetchStatsSummary checks and throws each sub-query's .error BEFORE its catch is deleted (supabase-js
never rejects — the deletion alone is a no-op), and the widgetData aggregation stops discarding
isError so CustomDashboardPage can render per-widget honesty. Widget rejections ALREADY render as
zeros today through this seam — this is a live lie, not a hypothetical.

Purpose: the last of the six D-02 sites, and the one where the naive fix does nothing at all.
Output: honest stats fetcher + per-widget error surfacing + rendering oracle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-failure-visibility/93-RESEARCH.md  (§TRUST-01 non-catch classes 1-2; §Code Examples "Site 6 fix shape")
@.planning/phases/93-failure-visibility/93-PATTERNS.md  (§4 — the in-file if (error) throw error analog)
@frontend/src/hooks/useWidgetDashboard.ts
@frontend/src/pages/custom-dashboard/CustomDashboardPage.tsx

<interfaces>
Verified by research (re-read at execution):
- fetchStatsSummary (useWidgetDashboard.ts:697-742): four Promise.all'd PostgREST count queries
  consumed as `activeDossiers.count || 0` with NO error destructure; outer catch at :726 returns a
  zero-filled object. PostgREST builders resolve {data, error, count} — an RLS-denied query
  resolves with count null and renders 0.
- The file's own convention: `if (error) throw error` at :597,628,656,685; fetchKpiData/
  fetchChartData catch-and-RETHROW at :292,582.
- widgetData useMemo (~:856-864) maps widgetQueries[index]?.data into a record and drops error
  state; CustomDashboardPage receives only widgetData. KPI/chart/events fetchers already throw
  today, so their rejections already render as zeros/empty through this seam.
- UI contract: failed widget region → variant B (query-error-inline); failed counts → em-dash with
  aria-label common:errors.countUnavailable; succeeding siblings render normally (UI-SPEC "which
  variant where").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Site-6 fix shape + aggregation surfaces isError</name>
  <files>frontend/src/hooks/useWidgetDashboard.ts</files>
  <read_first>useWidgetDashboard.ts:580-742 (siblings with the throw convention + the defect) and ~:840-880 (the widgetData memo); 93-RESEARCH.md §Code Examples "Site 6 fix shape"</read_first>
  <action>
    fetchStatsSummary: destructure/check each of the four results and `if (error) throw error` for
    each (loop or per-result, matching the file's own comment voice), switch the success-path
    reads to `?? 0` (null impossible on success), THEN delete the outer try/catch whole — the
    "Failed to fetch stats summary" fallback goes with it. Order matters and is the whole lesson:
    check-and-throw first, delete second.
    Aggregation: extend the widgetData memo (or add a parallel memo/return field) so the consumer
    receives per-widget error state — e.g. each entry carries { data, isError, refetch } or a
    sibling record keyed the same way; internal shape is discretion, the contract is that
    CustomDashboardPage can ask "did widget i fail" and retry it. Keep explicit return types.
  </action>
  <verify>
    <automated>cd frontend && F=src/hooks/useWidgetDashboard.ts && test -f "$F" && test "$(grep -v '^[[:space:]]*//' "$F" | grep -c 'Failed to fetch stats summary')" -eq 0 && test "$(awk '/async function fetchStatsSummary/,/^  }$/' "$F" | grep -c 'throw')" -ge 1 && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>The zero-filled catch is gone (RED today: the "Failed to fetch stats summary" string is present); fetchStatsSummary's body contains at least one throw (the per-result error surfacing); the aggregation exposes per-widget error state consumable by the page; type-check clean.</acceptance_criteria>
  <done>A denied stats query rejects instead of resolving to zeros, and widget failures survive the aggregation.</done>
</task>

<task type="auto">
  <name>Task 2: CustomDashboardPage renders per-widget honesty + spec</name>
  <files>frontend/src/pages/custom-dashboard/CustomDashboardPage.tsx, tests/e2e/93-custom-dashboard-error.spec.ts</files>
  <read_first>CustomDashboardPage.tsx in full (how widgetData is consumed today); frontend/src/components/error-states/QueryErrorState.tsx; tests/e2e/92-delegations-error.spec.ts (template)</read_first>
  <action>
    Page: for each widget whose query is in error, render QueryErrorState variant 'inline' in that
    widget's region with onRetry wired to that widget's refetch; stats figures fed by
    fetchStatsSummary render em-dash with aria-label t('common:errors.countUnavailable') while in
    error; succeeding widgets render untouched.
    Spec: ONE test, 92-template clone (inline auth, --no-deps, 15s budget). Force rejection of the
    stats sub-queries — derive the blockable request shape from the four table names in
    fetchStatsSummary (page.route predicate preferred over CDP URL globs, since these are
    /rest/v1/<table> count queries; abort only the matching requests). Assert on the DOM: the
    stats region shows em-dash figures or query-error-inline (never the digit-0 chrome rendered
    from a rejection), and the page shell itself still renders (not a full-page crash). If the
    dashboard requires configured widgets and staging has none, the stats region is still always
    rendered — scope the assertions to it and state that scoping in the spec header.
  </action>
  <verify>
    <automated>cd frontend && grep -q 'query-error-inline\|QueryErrorState' src/pages/custom-dashboard/CustomDashboardPage.tsx && cd .. && OUT=$(pnpm exec playwright test tests/e2e/93-custom-dashboard-error.spec.ts --project=chromium-en --no-deps 2>&1) && echo "$OUT" | grep -qE '\b1 passed'</automated>
  </verify>
  <acceptance_criteria>Page consumes per-widget error state and renders the inline variant per failed region; 1 passed under --no-deps; the spec's blocked-set derivation is stated in its header (population: the four stats tables; outside it: widget-grid fetchers, asserted only as not-crashing).</acceptance_criteria>
  <done>Widget and stats failures render as failures, per-region, with siblings intact.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                      |
| ------------------------------------ | ------------------------------------------------ |
| PostgREST {error} results → UI state | unchecked .error converts denial into zeros here |

## STRIDE Threat Register

| Threat ID | Category    | Component              | Disposition | Mitigation Plan                                                                          |
| --------- | ----------- | ---------------------- | ----------- | ---------------------------------------------------------------------------------------- |
| T-93-15   | Repudiation | fetchStatsSummary      | mitigate    | per-result throw before catch deletion; an RLS denial now rejects instead of rendering 0 |
| T-93-16   | Repudiation | widgetData aggregation | mitigate    | isError carried through; failed widgets render inline error, not empty/zero              |
| T-93-SC   | Tampering   | package installs       | accept      | zero installs                                                                            |

</threat_model>

<verification>
- Source gates + type-check + spec 1/1 --no-deps.
- Note for the closing derivation (93-15): this plan closes D-02 site 6 AND research swallow
  classes 1-2 at this seam; class 3 (the 26 `data: x = []` masks) is fixed only at
  criterion-named surfaces — the floor and the not-swept remainder are stated there.
</verification>

<success_criteria>
Site 6 closed the only way it can be (error-check first), the aggregation no longer launders
failure into zeros, and the rendered per-widget states are proven by a rendering oracle.
</success_criteria>

<output>
Create `.planning/phases/93-failure-visibility/93-08-SUMMARY.md` when done.
</output>
