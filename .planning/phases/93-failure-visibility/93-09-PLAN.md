---
phase: 93-failure-visibility
plan: 09
type: execute
wave: 3
depends_on: [93-01, 93-04]
files_modified:
  - frontend/src/routes/_protected/admin/field-permissions.tsx
  - frontend/src/routes/_protected/admin/data-retention.tsx
  - tests/e2e/93-admin-surfaces-error.spec.ts
autonomous: true
requirements: [TRUST-02]
truths:
  - "Decisions covered — D-25: /admin/field-permissions' edge function and RLS are FINE (19 rows reachable by an admin — the break is the data: x = [] mask plus a missing isError branch); its filters are silently never sent — found in passing, NOT fixed here, said aloud so no reader infers coverage; and /admin/data-retention's legal-holds region STILL errors after the 4-policy migration BY DESIGN (legal_holds is residual-11, RLS-AUTHUSERS-01 → Phase 100) — the oracle asserts that residual error rather than tripping on it"
must_haves:
  truths:
    - '/admin/field-permissions shows the rules the database holds (19 live) and shows the error state — never "0 Permissions" — when its query fails'
    - '/admin/data-retention renders per-region honesty: policies region no longer errors (93-04 landed), legal-holds region shows the error state (residual by design), and any forced failure renders error — never confident emptiness'
    - 'A criterion-2 close here does NOT imply the whole data-retention page went green — the residual is named in the oracle itself'
  artifacts:
    - path: 'frontend/src/routes/_protected/admin/field-permissions.tsx'
      provides: 'isError wiring → page-level QueryErrorState; stats never compute from a rejected query'
      contains: 'isError'
    - path: 'tests/e2e/93-admin-surfaces-error.spec.ts'
      provides: 'happy + forced oracles for both admin surfaces, incl. the legal-holds residual assertion'
      contains: 'query-error-state'
  key_links:
    - from: 'frontend/src/routes/_protected/admin/data-retention.tsx'
      to: 'frontend/src/domains/audit/repositories/audit.repository.ts'
      via: 'six hooks (useRetentionPolicies … useLegalHolds) whose isError is finally consumed'
      pattern: 'isError'
---

<objective>
Close criterion 2's two admin surfaces: wire the isError branches field-permissions and
data-retention never had, replacing the `data: x = []` masks' confident emptiness with the shared
error state — and prove both directions live: field-permissions rendering its 19 real rules, and
data-retention rendering per-region honesty including the BY-DESIGN legal-holds residual error.

Purpose: "0 Permissions" over a DB holding 19 rules is the audit's exemplar confident lie.
Output: two honest admin routes + a four-test spec that asserts the residual instead of tripping on it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-failure-visibility/93-RESEARCH.md  (§Criterion 2 surfaces 1-2 — per-surface break diagnosis)
@.planning/phases/93-failure-visibility/93-PATTERNS.md  (§Shared Patterns — page-level isError wiring idiom)
@frontend/src/routes/_protected/admin/field-permissions.tsx
@frontend/src/routes/_protected/admin/data-retention.tsx

<interfaces>
Verified by research (re-read at execution):
- field-permissions.tsx:118-126 destructures data: permissions = [] / data: definitions = [] /
  data: auditData from three hooks with NO isError anywhere in the file; stats cards compute
  permissions.length (:163-167) → ANY rejection renders "0 Permissions". The function returns 200
  to an admin today; the DB holds 19 rules. The audit tab's fetch 403s for non-admins → silently
  empty (same mask).
- Known-and-not-fixed (D-25): fetchFieldPermissions builds searchParams and never appends them to
  the invoke URL (useFieldPermissions.ts:41-52) — the filters are dead. OUT of scope; flag in a
  comment-free way: state it in the SUMMARY, do not silently fix or silently skip.
- data-retention.tsx:143-155 destructures SIX hooks (useRetentionPolicies, useRetentionStatistics,
  usePendingActions, useExpiringRecords, useExecutionLog, useLegalHolds) — every one data: x = [],
  zero isError. Transport: domains/audit/repositories/audit.repository.ts:72-100 → apiGet →
  data-retention fn (200 after 93-04 for policy-backed regions; legal_holds still 42501s —
  residual-11, Phase 100).
- Both routes require an admin session; TEST_USER_* creds are the admin account Phase 92's specs
  used inline.
- Invoke-backed queries carry no numeric status → full retry ladder (~7s) — 15s budgets.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: field-permissions — isError wiring, stats honesty</name>
  <files>frontend/src/routes/_protected/admin/field-permissions.tsx</files>
  <read_first>field-permissions.tsx in full; 93-PATTERNS.md §Shared Patterns (destructure-and-comment idiom + em-dash statFigure); frontend/src/components/error-states/QueryErrorState.tsx</read_first>
  <action>
    Destructure isError/refetch from the permissions and definitions hooks. When the page's
    primary (permissions) query is in error: render QueryErrorState variant 'page' replacing the
    page body, onRetry → refetch; stats cards render em-dash with
    aria-label={t('common:errors.countUnavailable')} — permissions.length is never computed from a
    rejected query's [] default. When only the audit tab's query errors (403 for non-admins is a
    real rejection): that tab renders variant 'inline'. Happy path byte-identical: 19 rules
    render. Do NOT touch useFieldPermissions.ts' dead filters (D-25 — out of scope, recorded).
  </action>
  <verify>
    <automated>cd frontend && F=src/routes/_protected/admin/field-permissions.tsx && test -f "$F" && grep -q 'isError' "$F" && grep -q 'QueryErrorState' "$F" && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>isError consumed for the page query and the audit tab; the "0 Permissions" render is unreachable while any backing query is rejected; happy path unchanged. The dead-filters finding is restated in the SUMMARY as found-not-fixed.</acceptance_criteria>
  <done>The page can no longer count to zero over a failure.</done>
</task>

<task type="auto">
  <name>Task 2: data-retention — six-hook wiring with the named residual</name>
  <files>frontend/src/routes/_protected/admin/data-retention.tsx</files>
  <read_first>data-retention.tsx in full (the :143-155 destructures and each region's render); frontend/src/components/error-states/QueryErrorState.tsx</read_first>
  <action>
    Destructure isError/refetch from all six hooks. Each REGION owns its query: a failed region
    renders QueryErrorState variant 'inline' (with retry) in place of its list/stat content;
    sibling regions render normally. No page-level collapse unless the policies (primary) query
    fails — then variant 'page'. Add a TS-statement-position comment above the legal-holds
    destructure (never inside JSX — the 92-03 lesson): the legal-holds region is EXPECTED to error
    until Phase 100 (RLS-AUTHUSERS-01 — legal_holds is one of the residual 11 auth.users policies)
    and must not be "fixed" by widening the 93-04 migration. Counts render em-dash while their
    query is in error.
  </action>
  <verify>
    <automated>cd frontend && F=src/routes/_protected/admin/data-retention.tsx && test -f "$F" && test "$(grep -c 'isError' "$F")" -ge 6 && grep -q 'QueryErrorState' "$F" && grep -q 'RLS-AUTHUSERS-01' "$F" && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>Six isError destructures (threshold 6 = the six hooks the research names); per-region inline states; the residual-11 comment names RLS-AUTHUSERS-01 so a later hand reads the intent in the file itself.</acceptance_criteria>
  <done>Six confident-empty regions become six honest regions, one of which honestly errors by design.</done>
</task>

<task type="auto">
  <name>Task 3: Four-test spec — happy and forced, residual asserted</name>
  <files>tests/e2e/93-admin-surfaces-error.spec.ts</files>
  <read_first>tests/e2e/92-delegations-error.spec.ts (template); 93-UI-SPEC.md §Verification Notes (copy-rule regex)</read_first>
  <action>
    Four tests, inline admin auth, --no-deps, 15s budgets, DOM-only:
    1. field-permissions happy: natural visit renders >0 permission rows AND the permissions stat
       is not the digit 0 (record the actual count — 19 expected — in the test output).
    2. field-permissions forced: CDP block `*field-permissions*` → [data-testid="query-error-state"]
       visible, body text does not match /(42501|42703|42P01|permission denied|supabase)/i.
    3. data-retention natural (runs AFTER 93-04 by wave order): the policies region is NOT in
       error state (rows or legitimate empty), AND the legal-holds region IS showing
       query-error-inline — the residual asserted as the expected render, with a comment citing
       RLS-AUTHUSERS-01/Phase 100 so Phase 100 knows to flip this assertion.
    4. data-retention forced: CDP block `*data-retention*` → error state renders, never the six
       confident-empty regions; no internal-string regex match.
  </action>
  <verify>
    <automated>OUT=$(pnpm exec playwright test tests/e2e/93-admin-surfaces-error.spec.ts --project=chromium-en --no-deps 2>&1) && echo "$OUT" | grep -qE '\b4 passed'</automated>
  </verify>
  <acceptance_criteria>4 passed under --no-deps (threshold 4 = the four tests enumerated above, verifiable via --list --no-deps). Test 3's residual assertion means a criterion-2 close here cannot silently imply the whole page went green — the oracle itself names what is still broken and why.</acceptance_criteria>
  <done>Both admin surfaces are proven honest in both directions, residual included.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                     |
| ------------------------------ | ----------------------------------------------- |
| invoke rejection → admin pages | failure crosses into confident-zero chrome here |

## STRIDE Threat Register

| Threat ID | Category               | Component            | Disposition | Mitigation Plan                                                             |
| --------- | ---------------------- | -------------------- | ----------- | --------------------------------------------------------------------------- |
| T-93-17   | Repudiation            | both admin routes    | mitigate    | isError branches render the shared state; masks can no longer count to zero |
| T-93-18   | Information Disclosure | forced-error renders | mitigate    | spec regex forbids Postgres codes/supabase strings in the DOM               |
| T-93-SC   | Tampering              | package installs     | accept      | zero installs                                                               |

</threat_model>

<verification>
- Spec 4/4 --no-deps; source gates; type-check.
- Said out loud, twice (in the page comment and the spec): legal-holds errors by design until
  Phase 100; field-permissions filters are dead and deliberately not fixed here.
</verification>

<success_criteria>
Criterion 2's admin half holds live: 19 real rules render, failure renders as failure on both
routes, and the residual is asserted — not hidden, not "fixed".
</success_criteria>

<output>
Create `.planning/phases/93-failure-visibility/93-09-SUMMARY.md` when done.
</output>
