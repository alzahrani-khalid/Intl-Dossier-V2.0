---
phase: 93-failure-visibility
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/ai-interaction-logger.ts
  - supabase/functions/dossier-stats/dashboard-aggregations.ts
autonomous: true
requirements: [PIN-2390-01]
truths:
  - "Decisions covered — D-16: both 2.39.0 helpers are bumped to @2 and the six importers redeployed; the closing derivation WIDENS from --include=index.ts to --include=*.ts — the narrow population is the whole reason this defect was invisible to Phase 92's green"
must_haves:
  truths:
    - 'Zero files under supabase/functions (any *.ts, not just index.ts) match @supabase/supabase-js@2.3x'
    - 'All six importers are redeployed so the deployed bundles stop re-fetching the deprecated specifier'
    - 'All six importers still probe non-401 after redeploy (regression guard — they were non-401 before, this is not the discriminating evidence)'
  artifacts:
    - path: 'supabase/functions/_shared/ai-interaction-logger.ts'
      provides: 'createClient import on @2 (value import — constructs a service-role client at :149)'
      contains: 'supabase-js@2'
    - path: 'supabase/functions/dossier-stats/dashboard-aggregations.ts'
      provides: 'SupabaseClient type import on @2'
      contains: 'supabase-js@2'
  key_links:
    - from: 'supabase/functions/_shared/ai-interaction-logger.ts'
      to: 'supabase/functions/{ai-interaction-logs,ai-summary-generate,dossier-field-assist,positions-consistency-check,translate-content}/index.ts'
      via: 'import — the five deployed bundles inline the helper, so the fix only lands via redeploy'
      pattern: 'ai-interaction-logger'
---

<objective>
Close PIN-2390-01 (D-16): bump the two non-index.ts helpers still pinning
`@supabase/supabase-js@2.39.0` to `@2`, redeploy the six functions whose deployed bundles inline
them, and close with the WIDENED derivation over `*.ts` — the population definition whose
narrowness (`index.ts` only) is the entire reason Phase 92's green never saw these two files.

Purpose: deployed bundles re-fetch the deprecated specifier until their importers are redeployed;
the source edit alone changes nothing in production.
Output: two bumped helpers; six redeployed functions; widened grep → 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-failure-visibility/93-RESEARCH.md  (§PIN-2390-01 — sites and importers re-verified in tree)
@supabase/functions/_shared/ai-interaction-logger.ts
@supabase/functions/dossier-stats/dashboard-aggregations.ts

<interfaces>
Verified by research (re-read at execution):
- `_shared/ai-interaction-logger.ts:12` — `import { createClient, SupabaseClient } from
  'https://esm.sh/@supabase/supabase-js@2.39.0'`; constructs a service-role client at :149.
  Importers (grep `ai-interaction-logger` under supabase/functions --include='*.ts', minus
  _shared itself): ai-interaction-logs, ai-summary-generate, dossier-field-assist,
  positions-consistency-check, translate-content — 5.
- `dossier-stats/dashboard-aggregations.ts:1` — `import { SupabaseClient } from
  "https://esm.sh/@supabase/supabase-js@2.39.0"` (type-only usage at :32). Importer:
  dossier-stats/index.ts:4 — 1.
- Edit is a version-tag change ONLY: `@2.39.0` → `@2`, keeping the esm.sh source style (matching
  the Phase 92 AUTH-02 convention; do not switch to jsr: — changing import SOURCE style is beyond
  the pin fix).
- Not an auth regression either way: (a) builds a service-role client; every getUser auth path
  runs through the already-migrated index.ts on @2 — which is why all six probe non-401 today.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump both helper specifiers to @2</name>
  <files>supabase/functions/_shared/ai-interaction-logger.ts, supabase/functions/dossier-stats/dashboard-aggregations.ts</files>
  <read_first>Both files in full (the import lines plus the createClient construction at ai-interaction-logger.ts:149, to confirm no @2-incompatible API usage rides along)</read_first>
  <action>
    In each file, change only the version tag inside the esm.sh import URL:
    `https://esm.sh/@supabase/supabase-js@2.39.0` → `https://esm.sh/@supabase/supabase-js@2`.
    No other edits — not the import style, not the identifiers, not adjacent code. Re-derive the
    importer list before Task 2 and state it: `grep -rl 'ai-interaction-logger' supabase/functions
    --include='*.ts'` (minus _shared) must yield the 5 named functions, plus dossier-stats via
    `grep -rl 'dashboard-aggregations' supabase/functions --include='*.ts'`. If the derived list
    differs from the six named, STOP and record the delta before deploying — the deploy set is
    derived, never quoted.
  </action>
  <verify>
    <automated>test -d supabase/functions && test "$(grep -rlE '@supabase/supabase-js@2\.3[0-9]' supabase/functions --include='*.ts' | wc -l)" -eq 0</automated>
  </verify>
  <acceptance_criteria>The widened closing derivation (population: every *.ts under supabase/functions; matching rule: the 2.3x specifier regex; outside it: non-.ts files — none plausible — and already-deployed bundles, which Task 2 owns) returns 0. RED today: exactly 2 files match. If a match survives inside a comment, reword the comment — the derivation counts file content, and a lingering literal re-creates the invisibility this requirement exists to kill.</acceptance_criteria>
  <done>No source file under supabase/functions pins a 2.3x supabase-js.</done>
</task>

<task type="auto">
  <name>Task 2: Redeploy the six importers; probe as regression guard</name>
  <files></files>
  <read_first>scripts/probe-edge-auth.sh usage header; .claude/skills/edge-function-add/SKILL.md §7 Deploy</read_first>
  <action>
    Deploy each of the six importers:
    `supabase functions deploy <name> --project-ref zkrcjzdemdmwhearhfgg` for
    ai-interaction-logs, ai-summary-generate, dossier-field-assist, positions-consistency-check,
    translate-content, dossier-stats. Record each command's exit and the `supabase functions list`
    updated_at column for the six in the SUMMARY — that freshness evidence, not the probe, is what
    shows the deployed bundles were rebuilt (the probe was already non-401 before this plan; it
    can only catch a regression, and is labelled as such). No shared deploy-ledger file is
    created (per-plan evidence only).
  </action>
  <verify>
    <automated>OUT=$(scripts/probe-edge-auth.sh ai-interaction-logs ai-summary-generate dossier-field-assist positions-consistency-check translate-content dossier-stats) && test "$(echo "$OUT" | grep -c ' -> ')" -eq 6 && test "$(echo "$OUT" | grep -c ' -> 401')" -eq 0</automated>
  </verify>
  <acceptance_criteria>Six probe lines (threshold 6 = the derived importer count from Task 1), zero 401s. This gate is a REGRESSION guard: it is green before the work too, and the plan says so — the discriminating evidence for "deployed bundles refreshed" is the six same-day updated_at rows recorded in the SUMMARY, plus Task 1's grep → 0. C1 note: the gate's RED direction is constructible by probing a broken/undeployed name; its green does not by itself prove this plan's work.</acceptance_criteria>
  <done>The six deployed bundles no longer fetch the deprecated specifier, and nothing regressed to 401.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                         |
| ---------------------------- | --------------------------------------------------- |
| deployed bundle → esm.sh CDN | version pin decides which supabase-js code executes |

## STRIDE Threat Register

| Threat ID | Category               | Component                                 | Disposition | Mitigation Plan                                                                                                                       |
| --------- | ---------------------- | ----------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| T-93-04   | Tampering              | esm.sh import specifiers                  | mitigate    | pin moves to the maintained @2 line (same package, same source style); widened \*.ts derivation prevents a future helper hiding a pin |
| T-93-05   | Elevation of Privilege | ai-interaction-logger service-role client | accept      | pre-existing service-role usage, unchanged by a version bump; scoping enforced in the importing handlers                              |
| T-93-SC   | Tampering              | package installs                          | accept      | zero package-manager installs; the only dependency-adjacent edit is the version tag                                                   |

</threat_model>

<verification>
- Widened grep → 0 (RED: 2 today); six deploys exit 0 with fresh updated_at; probe 6 lines, 0×401.
- Plan 93-15 re-runs the widened derivation with its population statement at phase close.
</verification>

<success_criteria>
PIN-2390-01 closed: no 2.3x pin anywhere under supabase/functions in source, and no deployed bundle
of the six importers built against one — with the derivation widened so this class cannot hide in a
helper again.
</success_criteria>

<output>
Create `.planning/phases/93-failure-visibility/93-03-SUMMARY.md` when done.
</output>
