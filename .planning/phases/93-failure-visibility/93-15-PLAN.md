---
phase: 93-failure-visibility
plan: 15
type: execute
wave: 4
depends_on:
  [93-01, 93-02, 93-03, 93-04, 93-05, 93-06, 93-07, 93-08, 93-09, 93-10, 93-11, 93-12, 93-13, 93-14]
files_modified:
  - .planning/phases/93-failure-visibility/93-VERIFICATION.md
autonomous: true
requirements: [TRUST-01, TRUST-02, TRUST-03, TRUST-04, DELEG-01, DR-42501, AUDIT-42703, PIN-2390-01]
truths:
  - "Decisions covered — D-02: the TRUST-01 closing derivation restates its population (the 6 defect sites vs the 45-site shape; the scan states which it measured); D-17: GATE-STANDARD C1-C10 incl. C9a governs the whole-set gate drill, one author, all plans, one pass; D-18: every closing derivation in the verification doc states its POPULATION DEFINITION and what falls outside it — a derivation whose scope is unstated is a finding, not a pass; D-19: the full behavioural oracle set is re-run as the phase's render-state evidence"
must_haves:
  truths:
    - 'Every closing derivation in 93-VERIFICATION.md carries: search root, glob, matching rule, and the cases the method CANNOT see'
    - 'The full Phase-93 spec set plus the updated 92-delegations spec run green under --no-deps, and the live probe statuses match the intended end-state (my-delegations 5xx BY DESIGN; data-retention 200; audit-logs-viewer 200; field-permissions 200; six PIN importers non-401)'
    - 'The intended-broken register is explicit: /delegations errors until Phase 102; data-retention legal-holds errors until Phase 100; field-permissions filters silently dead (found, not fixed); AUDIT-DROP-01/AUDIT-ZERO-01 filed to Phase 94; 26 data:x=[] mask sites are a FLOOR and only criterion-named instances were fixed; /tasks/queue errors until Phase 95; /analytics errors until Phase 96'
  artifacts:
    - path: '.planning/phases/93-failure-visibility/93-VERIFICATION.md'
      provides: 'closing derivations with populations, the gate-drill table (C1 both directions per gate), probe outputs, spec results, and the intended-broken register'
      contains: 'POPULATION'
  key_links:
    - from: '.planning/phases/93-failure-visibility/93-VERIFICATION.md'
      to: 'scripts/gate-drill.mjs'
      via: 'the mechanical half of the drill; the done-state constructions stay authored per gate'
      pattern: 'gate-drill'
---

<objective>
Close the phase the way Phase 92 learned to: re-run every closing derivation with its population
definition stated (D-18), run the full behavioural oracle set (D-19), drill every gate in the plan
set both directions (D-17/C1), and write the intended-broken register so no later reader infers
coverage that was never claimed.

Purpose: Phase 92 shipped a correct command that measured the wrong set; this plan exists so
Phase 93 cannot.
Output: 93-VERIFICATION.md — derivations, drill table, probe/spec evidence, register.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/GATE-STANDARD.md
@.tickmarkr/overseer/P92-EXEC-REPORT.md  (the house style for the evidence table)
@.planning/phases/93-failure-visibility/93-RESEARCH.md  (§Validation Architecture — the phase requirements → test map being discharged)

<interfaces>
The derivations to re-run, each with population + outside-statement (the D-02/D-08/D-11/D-16
template):
1. TRUST-01: the widened catch-scan over the 6 named files → 0 in-class sites. Population:
   brace-matched catch-and-return over the 6 files; MEASURES THE DEFECT (6), not the shape (45).
   Outside: .catch() expression bodies, catch-and-assign, select/placeholderData — triaged out by
   research; the 26 data:x=[] masks — floor, criterion-named instances only.
2. Criterion 5: bucket-(a) grep → 0 over the 25-site population (23 in 93-14 + the two fixed in
   93-06/93-07). Outside: bucket (b) boundaries, BotIntegrationsSettings toast, multiline renders.
3. PIN-2390-01: grep -rlE '@supabase/supabase-js@2\.3[0-9]' supabase/functions --include='*.ts'
   → 0. Outside: non-.ts files, already-deployed bundles (covered by the 93-03 redeploy evidence).
4. Anti-grant guard: uncommented GRANT SELECT on auth.users across both migration roots → 0.
   Outside: grants applied by hand outside the migrations dirs; the live-catalog half is
   point-in-time MCP evidence (D-23 label).
5. i18n: the 7 common:errors.* keys resolve in en AND ar (the 93-01 node derivation re-run).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the closing derivations, populations stated</name>
  <files>.planning/phases/93-failure-visibility/93-VERIFICATION.md</files>
  <read_first>93-RESEARCH.md §TRUST-01 (the widened-scan method being re-run); each derivation's population text in this plan's interfaces block</read_first>
  <action>
    Execute derivations 1-5 from the interfaces block and record each in 93-VERIFICATION.md as:
    the command, its output, its POPULATION DEFINITION (search root, glob, matching rule), and
    WHAT FALLS OUTSIDE IT. For derivation 1, state explicitly whether the number measured is the
    defect (6) or the shape (45) — D-02's closing rule. Then write the intended-broken register
    (the must_haves list verbatim, each row with its owning phase: DELEG-02/SEED-DELEG-01 →
    Phase 102; RLS-AUTHUSERS-01 → Phase 100; field-permissions filters → filed;
    AUDIT-DROP-01/AUDIT-ZERO-01 → Phase 94; DEAD-02 → Phase 95; DEAD-05 → Phase 96; the
    data:x=[] floor). A finding that fails any derivation is recorded as OPEN with its number —
    never silently absorbed.
  </action>
  <verify>
    <automated>V=.planning/phases/93-failure-visibility/93-VERIFICATION.md && test -f "$V" && test "$(grep -c 'POPULATION' "$V")" -ge 5 && grep -q 'DELEG-02' "$V" && grep -q 'RLS-AUTHUSERS-01' "$V" && grep -q 'AUDIT-DROP-01' "$V"</automated>
  </verify>
  <acceptance_criteria>All 5 derivations recorded with populations and outside-statements (threshold 5 = the enumerated derivations); the intended-broken register names every deliberately-open item with its owning phase. NOT-CHECKED beats silence: anything not re-derived is labelled NOT-CHECKED, never omitted.</acceptance_criteria>
  <done>The phase's numbers all say what set they measured.</done>
</task>

<task type="auto">
  <name>Task 2: Full oracle run — specs, probe, gate drill</name>
  <files></files>
  <read_first>.planning/GATE-STANDARD.md §The pass procedure (one author, all gates, one pass; the drill-table row format); scripts/gate-drill.mjs header (its invocation)</read_first>
  <action>
    (1) Specs: run every Phase-93 spec plus the updated 92 spec under --no-deps
    (tests/e2e/93-*.spec.ts and tests/e2e/92-delegations-error.spec.ts, --project=chromium-en).
    Expected counts come from `--list --no-deps` run FIRST and pasted (C6 — never reasoned from
    "listing executes nothing").
    (2) Probe: scripts/probe-edge-auth.sh over my-delegations data-retention audit-logs-viewer
    field-permissions engagement-dossiers ai-interaction-logs ai-summary-generate
    dossier-field-assist positions-consistency-check translate-content dossier-stats — paste the
    output; assert the intended end-states (my-delegations 5xx BY DESIGN, data-retention 200,
    audit-logs-viewer 200, field-permissions 200, the rest non-401).
    (3) Drill: run scripts/gate-drill.mjs across the 93 plan set for the mechanical half; for the
    C1 done-state half, record the drill table (plan.gate | C1 red | C1 green construction |
    C2-C10 exceptions | verdict) in 93-VERIFICATION.md — a gate whose done state was not
    constructed is CANNOT CONSTRUCT with what it needs, never SOUND. The MCP-only checks (93-04's
    pg_policies/grantee evidence) carry their D-23 labels. Work in a scratch worktree where
    construction requires edits; git status --porcelain byte-identical before/after.
  </action>
  <verify>
    <automated>OUT=$(pnpm exec playwright test tests/e2e/93-analytics-error.spec.ts tests/e2e/93-dossier-list-counts-error.spec.ts tests/e2e/93-custom-dashboard-error.spec.ts tests/e2e/93-admin-surfaces-error.spec.ts tests/e2e/93-tags-attachments-error.spec.ts tests/e2e/93-dossier-notfound.spec.ts tests/e2e/93-degraded-engagement.spec.ts tests/e2e/93-report-notfound.spec.ts tests/e2e/93-tasks-queue-error.spec.ts tests/e2e/92-delegations-error.spec.ts --project=chromium-en --no-deps 2>&1) && echo "$OUT" | grep -qE '[0-9]+ passed' && test "$(echo "$OUT" | grep -cE '[0-9]+ failed')" -eq 0 && OUT2=$(scripts/probe-edge-auth.sh my-delegations data-retention audit-logs-viewer field-permissions) && echo "$OUT2" | grep -qE 'my-delegations -> 5[0-9][0-9]' && echo "$OUT2" | grep -qE 'data-retention -> 200' && echo "$OUT2" | grep -qE 'audit-logs-viewer -> 200' && echo "$OUT2" | grep -qE 'field-permissions -> 200'</automated>
  </verify>
  <acceptance_criteria>All ten specs pass with zero failed lines (the per-spec expected counts — 1/1/1/4/2/2/3/1/1/2 = 18 tests — verified against the pasted --list --no-deps output rather than frozen here); probe matches the intended end-state per function including the BY-DESIGN 5xx; the drill table covers every automated gate in the plan set with a verdict, and no unconstructed done state is folded into SOUND.</acceptance_criteria>
  <done>The phase's render-state claims are closed by rendering, its live claims by probing, and its gates by drilling.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                  |
| --------------------------- | -------------------------------------------- |
| evidence → close-out claims | unstated scopes convert findings into passes |

## STRIDE Threat Register

| Threat ID | Category               | Component           | Disposition | Mitigation Plan                                                                          |
| --------- | ---------------------- | ------------------- | ----------- | ---------------------------------------------------------------------------------------- |
| T-93-32   | Repudiation            | closing derivations | mitigate    | population + outside-statement mandatory per derivation (D-18); NOT-CHECKED over silence |
| T-93-33   | Information Disclosure | probe/spec evidence | mitigate    | probe never echoes credentials; spec output contains no secrets                          |
| T-93-SC   | Tampering              | package installs    | accept      | zero installs                                                                            |

</threat_model>

<verification>
- 93-VERIFICATION.md exists with ≥5 POPULATION-stated derivations, the drill table, probe/spec
  outputs, and the intended-broken register.
- The tree is byte-identical after the drill's constructions (porcelain check recorded).
</verification>

<success_criteria>
The phase closes on evidence whose scope is stated: derivations with populations, oracles that
rendered, gates drilled both directions, and every deliberately-broken surface named with its
owning phase — no reader can infer coverage that was never claimed.
</success_criteria>

<output>
Create `.planning/phases/93-failure-visibility/93-15-SUMMARY.md` when done.
</output>
