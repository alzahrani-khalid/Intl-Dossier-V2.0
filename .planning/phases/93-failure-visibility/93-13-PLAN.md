---
phase: 93-failure-visibility
plan: 13
type: execute
wave: 2
depends_on: [93-01]
files_modified:
  - frontend/src/routes/_protected/reports/$reportId.tsx
  - tests/e2e/93-report-notfound.spec.ts
autonomous: true
requirements: [TRUST-03]
truths:
  - 'Decisions covered — D-06: report builder gets the loader-based notFound() shape (it has NO fetch to reuse: useReportBuilderState is a stub that ignores the id, so /reports/<any-id> silently renders a fresh empty builder — a lie of a different flavor); the WRITE-06 42P17 hazard is stated: the custom_reports by-id read may REJECT for all ids until Phase 94 fixes the mutually recursive SELECT policies, and that rejection rendering the ERROR state is the honest pre-Phase-94 outcome, not a red'
must_haves:
  truths:
    - '/reports/<well-formed-absent-id> never renders the builder as if the report exists — it renders the root 404 page (row absent) or the shared error state (read rejected), and the route has both a loader and an errorComponent to guarantee it'
    - 'The 42P17 policy-recursion hazard is written into the plan, the route comment, and the spec — Phase 94 (WRITE-06) owns the fix; an error render here pre-Phase-94 is expected'
  artifacts:
    - path: 'frontend/src/routes/_protected/reports/$reportId.tsx'
      provides: 'loader (by-id fetch → notFound()) + route errorComponent (QueryErrorState) + the 42P17 hazard comment'
      contains: 'notFound'
    - path: 'tests/e2e/93-report-notfound.spec.ts'
      provides: 'the honest-disjunction oracle: absent id never renders a fresh builder'
      contains: 'query-error-state'
  key_links:
    - from: 'frontend/src/routes/_protected/reports/$reportId.tsx'
      to: 'frontend/src/lib/query-client.ts'
      via: "loader imports the queryClient module singleton (no router-context change needed) and ensureQueryData's the by-id query"
      pattern: 'queryClient'
---

<objective>
Give the report builder route the fetch it never had, so it can have a not-found: a loader on
$reportId.tsx runs a custom_reports by-id read — no row → throw notFound(); rejection → the route's
own errorComponent renders QueryErrorState (so a pre-Phase-94 42P17 rejection renders the honest
error, not the router's leaking default). There is no full precedent in the tree — this composes
the three partial analogs 93-PATTERNS names.

Purpose: /reports/<any-id> rendering a fresh empty builder "looks like the report" — a confident
lie about a record that may not exist.
Output: loader + errorComponent + not-found spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-failure-visibility/93-PATTERNS.md  (§No Analog Found — the three partials to compose: loader syntax from tasks/$id.tsx:4-15; by-id query + throw from useScheduledReports.ts:224-230; queryClient singleton from lib/query-client.ts:88)
@.planning/phases/93-failure-visibility/93-RESEARCH.md  (§Criterion 3 report-builder row — the seam and the 42P17 hazard)
@frontend/src/routes/_protected/reports/$reportId.tsx

<interfaces>
Verified by research/patterns (re-read at execution):
- useReportBuilderState (domains/misc/hooks/useReportBuilder.ts:120-127) ignores initialReportId
  entirely — the route currently renders a fresh builder for ANY id. Do not rebuild the hook; the
  loader owns existence, the builder keeps its current behavior for ids that exist.
- Loader shape: the tree's only loader (routes/_protected/tasks/$id.tsx:8-15) merely echoes
  params — compose: loader syntax from there; the by-id supabase query with `if (error) throw
  error` from hooks/useScheduledReports.ts:224-230 (`from('custom_reports').select(...)`);
  queryClient is a module singleton (lib/query-client.ts:88) importable directly — no
  router-context change.
- 42P17 hazard (WRITE-06, Phase 94): custom_reports ↔ report_shares SELECT policies are mutually
  recursive — the by-id read may reject for ALL ids until Phase 94. A rejection propagating to the
  route errorComponent and rendering QueryErrorState is the honest pre-Phase-94 render (assumption
  A4 — both outcomes are honest; neither is a red).
- notFound() thrown from a loader is the docs-recommended shape (no flicker); the root boundary
  catches it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Loader + errorComponent on $reportId.tsx</name>
  <files>frontend/src/routes/_protected/reports/$reportId.tsx</files>
  <read_first>routes/_protected/reports/$reportId.tsx in full; routes/_protected/tasks/$id.tsx:4-15 (loader syntax); hooks/useScheduledReports.ts:220-235 (the by-id query idiom); lib/query-client.ts:80-95 (the singleton export); frontend/src/components/error-states/QueryErrorState.tsx</read_first>
  <action>
    Add a `loader` to the route: import the queryClient singleton and ensureQueryData a
    custom_reports by-id query (select minimal columns — id is enough for existence; `if (error)
    throw error`, matching the useScheduledReports idiom). Result semantics: query resolves with
    no row → `throw notFound()` (root 404 renders); query rejects → let the rejection propagate to
    the route's `errorComponent`, which this task also adds: a component rendering QueryErrorState
    variant 'page' with a retry that re-navigates/invalidates the route. Add a TS-statement-position
    comment naming the WRITE-06 42P17 hazard and Phase 94 as its owner, so the error render
    pre-Phase-94 is read as intended, not as this route being broken. The builder's happy path for
    an EXISTING id is unchanged (the loader only gates existence; it does not feed the stub hook).
  </action>
  <verify>
    <automated>cd frontend && F='src/routes/_protected/reports/$reportId.tsx' && test -f "$F" && grep -q 'loader' "$F" && grep -q 'notFound' "$F" && grep -q 'errorComponent' "$F" && grep -q 'QueryErrorState' "$F" && grep -q '42P17' "$F" && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>The route carries loader + notFound throw + errorComponent + the 42P17 comment; type-check clean. Rejections render the shared state at THIS route (the router's defaultErrorComponent — 93-14's seam — is never reached from here), so this plan has no dependency on 93-14.</acceptance_criteria>
  <done>The route knows whether its report exists before it renders a builder.</done>
</task>

<task type="auto">
  <name>Task 2: Not-found spec with the honest disjunction</name>
  <files>tests/e2e/93-report-notfound.spec.ts</files>
  <read_first>tests/e2e/92-delegations-error.spec.ts (template); the builder component's DOM (identify a selector that marks "the builder rendered as if the report exists" — e.g. its title input/canvas region — at authoring time)</read_first>
  <action>
    ONE test, inline auth, --no-deps, 15s budget, DOM-only: visit
    /reports/<crypto.randomUUID()> and assert (a) the builder-ready UI is ABSENT (the selector
    pinned at authoring time — the fresh-empty-builder lie is the thing being killed), AND (b)
    EITHER the root 404 page renders (by-id read worked, row absent) OR
    [data-testid="query-error-state"] renders (read rejected — the expected pre-Phase-94 outcome
    under the 42P17 hazard). Header comment explains the disjunction and cites WRITE-06/Phase 94,
    with the instruction that Phase 94 tightens (b) to the 404-only arm once the recursion is
    fixed. Record WHICH arm the run took in the test output (console annotation) so the SUMMARY
    can state the observed live behavior.
  </action>
  <verify>
    <automated>OUT=$(pnpm exec playwright test tests/e2e/93-report-notfound.spec.ts --project=chromium-en --no-deps 2>&1) && echo "$OUT" | grep -qE '\b1 passed'</automated>
  </verify>
  <acceptance_criteria>1 passed under --no-deps. The assertion's primary conjunct (no fresh builder for an absent id) is unconditional; the disjunction only covers WHICH honest state renders, and the observed arm is recorded — never left ambiguous.</acceptance_criteria>
  <done>A nonexistent report id can no longer cosplay as a report.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                   |
| ------------------------- | --------------------------------------------- |
| loader rejection → render | pre-P94 42P17 rejections cross here (ASVS V7) |

## STRIDE Threat Register

| Threat ID | Category               | Component            | Disposition | Mitigation Plan                                                                                                                   |
| --------- | ---------------------- | -------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------- |
| T-93-27   | Information Disclosure | route errorComponent | mitigate    | QueryErrorState i18n only — the 42P17 text never reaches the DOM (route-level errorComponent preempts the leaking router default) |
| T-93-28   | Repudiation            | $reportId route      | mitigate    | fresh-builder-for-any-id path removed; existence gated in the loader                                                              |
| T-93-SC   | Tampering              | package installs     | accept      | zero installs                                                                                                                     |

</threat_model>

<verification>
- Spec 1/1 --no-deps; source gate; type-check.
- Said out loud: the by-id read may reject for all ids until Phase 94 (WRITE-06) — the error state
  is the honest pre-P94 render; the spec's recorded arm states which behavior staging exhibited.
  (The `$` in $reportId.tsx is literal — TanStack route filename; gates quote it single-quoted.)
</verification>

<success_criteria>
Criterion 3's report-builder third holds: a well-formed absent id renders a page-level honest state
(404 or error, per the recorded live behavior), never a fresh builder, with the hazard and its
owner named in the file itself.
</success_criteria>

<output>
Create `.planning/phases/93-failure-visibility/93-13-SUMMARY.md` when done.
</output>
