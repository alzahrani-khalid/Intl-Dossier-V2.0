---
phase: 93-failure-visibility
plan: 04
type: execute
wave: 2
depends_on: [93-02]
files_modified:
  - supabase/migrations/20260815_phase93_rewrite_auth_users_policies.sql
autonomous: true
requirements: [DR-42501]
truths:
  - 'Decisions covered — D-09: the corrected diagnosis governs (the 42501 is the data_retention_policies RLS policy evaluating EXISTS on auth.users; the fix is a policy rewrite, not a handler change); D-10: exactly 4 policies rewritten (data_retention_policies ×1, tag_categories ×2, entity_tag_assignments ×1) with the raw_*_meta_data reads DELETED; D-11: the class is 15 policies over 13 tables, a LOWER BOUND, and the residual 11 are RLS-AUTHUSERS-01 → Phase 100, untouched here; D-12: GRANT SELECT ON auth.users is REFUSED and a gate enforces the refusal; D-23: the anti-grant gate lands as a migrations-tree regression guard plus MCP live evidence labelled CANNOT CONSTRUCT (no DSN); D-24: the predicate is public.is_platform_admin(auth.uid()) per RULING-P93-01 ADDENDUM 2'
must_haves:
  truths:
    - 'The live probe flips: data-retention -> 200 (it is 500 today, and the 500 is the policy predicate raising 42501 before comparing anything)'
    - "No migration file in the repo contains an uncommented GRANT SELECT on auth.users — and the gate stays red for any future hand that takes the database's own HINT"
    - "The residual 11 auth.users policies (legal_holds included) are NOT widened into this migration — /admin/data-retention's legal-holds region erroring after this plan is BY DESIGN (Phase 100)"
  artifacts:
    - path: 'supabase/migrations/20260815_phase93_rewrite_auth_users_policies.sql'
      provides: 'the 4-policy rewrite onto is_platform_admin(auth.uid()), idempotent, with the anti-grant refusal comment'
      contains: 'is_platform_admin'
  key_links:
    - from: 'supabase/migrations/20260815_phase93_rewrite_auth_users_policies.sql'
      to: 'supabase/functions/data-retention/index.ts'
      via: 'policy predicate no longer raises 42501, so the :233 policies query succeeds and the fn returns 200 — the effect is observable four layers up, so the oracle is the live probe'
      pattern: 'data_retention_policies'
---

<objective>
Rewrite the 4 RLS policies this phase's criteria exercise (D-10) off their `auth.users` predicates
and onto `public.is_platform_admin(auth.uid())` (D-24), via `apply_migration` only — closing
DR-42501 at its real seam (D-09: the policy, not the handler). Land the REQUIRED anti-grant gate
(D-12/D-23) so the database's own `HINT: GRANT SELECT ON auth.users…` can never be taken silently:
taking it converts 8 fail-closed metadata-role policies into a live self-service admin escalation.

Purpose: the surface's blocker is a policy predicate no client role can evaluate; the safety of the
8 dormant policies is exactly the fail-closed state this migration must not disturb.
Output: applied migration; pg_policies evidence; probe `data-retention -> 200`; anti-grant guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-failure-visibility/93-RESEARCH.md  (§DR-42501 — the exact 4-policy SQL draft at lines 505-531 is the text to use)
@.planning/phases/93-failure-visibility/93-PATTERNS.md  (§3 — the SEC-BE-01 sweep analog + the verified inline precedent)
@.claude/skills/supabase-migration-safety/SKILL.md
@.tickmarkr/overseer/PARK-P93.md  (the live-catalog evidence and re-runnable SQL behind the ruling)

<interfaces>
Verified by research/ruling (re-read at execution):
- `public.is_platform_admin(uid)` exists (`20260627000001_sec_helper_is_platform_admin.sql:22-46`):
  SECURITY DEFINER, STABLE, pinned search_path, granted to authenticated; reads
  public.users.role = 'admin' OR an active public.user_roles admin row. It IS D-10's "unified
  authz read", packaged, and recursion-proof (SECURITY DEFINER sidesteps users' own RLS).
- The 4 target policies and their FOR clauses: data_retention_policies "Admin can manage retention
  policies" FOR ALL (USING + WITH CHECK); tag_categories "Tag creators or admins can update" FOR
  UPDATE; tag_categories "Only non-system tags can be deleted by creators" FOR DELETE;
  entity_tag_assignments "Authenticated users can remove tag assignments" FOR DELETE. The tag
  policies keep their creator-OR-admin intent (created_by/assigned_by = auth.uid() OR helper).
- The data-retention handler gates on userRecord?.role === 'admin' exactly — is_platform_admin's
  role = 'admin' arm matches; no super_admin mismatch on this surface (super_admin exists in
  neither store — research OQ3 resolved by D-24).
- The 3 tag policies gate MUTATIONS (update/delete), not the analytics read — their oracle is a
  role-switched SQL probe (PARK-P93 §(d) method), not a render.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the idempotent 4-policy migration with the refusal comment</name>
  <files>supabase/migrations/20260815_phase93_rewrite_auth_users_policies.sql</files>
  <read_first>93-RESEARCH.md §DR-42501 (the drafted SQL — use it verbatim as the base); 93-PATTERNS.md §3 (SEC-BE-01 analog: DROP POLICY IF EXISTS before every CREATE POLICY); supabase/migrations/20260627000002_sec_be_01_admin_rls_db_role.sql</read_first>
  <action>
    Create the migration file from the research draft: 4 × (DROP POLICY IF EXISTS + CREATE POLICY)
    using public.is_platform_admin(auth.uid()) — FOR ALL on data_retention_policies carries both
    USING and WITH CHECK; the two tag_categories policies and the entity_tag_assignments policy
    keep their creator arms (created_by = auth.uid() / assigned_by = auth.uid() /
    is_system = FALSE) exactly as drafted. The raw_user_meta_data / raw_app_meta_data reads are
    DELETED, not preserved alongside (D-10). Header comment MUST carry the anti-grant refusal —
    that Postgres's own 42501 HINT proposes `GRANT SELECT ON auth.users TO authenticated` and that
    taking it arms 8 fail-closed metadata-role policies (6 read/write, 1 anon-reachable) — with
    every such literal kept on `--` comment lines (the guard gate excludes comment lines; an
    uncommented occurrence anywhere in migrations goes red). Scope discipline: exactly these 4
    policies — legal_holds and the rest of the residual 11 belong to Phase 100
    (RLS-AUTHUSERS-01); widening the migration "to fix the legal-holds region" breaks the
    ruling's scope and is forbidden.
  </action>
  <verify>
    <automated>M=supabase/migrations/20260815_phase93_rewrite_auth_users_policies.sql && test -f "$M" && test "$(grep -c 'is_platform_admin' "$M")" -ge 4 && test "$(grep -c 'DROP POLICY IF EXISTS' "$M")" -eq 4 && test "$(grep -v '^[[:space:]]*--' "$M" | grep -c 'auth\.users')" -eq 0 && test "$(grep -v '^[[:space:]]*--' "$M" | grep -ci 'raw_user_meta_data\|raw_app_meta_data')" -eq 0</automated>
  </verify>
  <acceptance_criteria>Exactly 4 DROP/CREATE pairs (threshold 4 = D-10's policy count); ≥4 is_platform_admin occurrences (4 policies × at least one predicate each; FOR ALL contributes 2); zero uncommented auth.users or raw_*_meta_data references (population: non-`--` lines of this one file; the refusal comment lives on `--` lines by construction).</acceptance_criteria>
  <done>The migration text implements D-10/D-24 with the refusal on the record.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply via Supabase MCP; verify at the catalog and four layers up</name>
  <files></files>
  <read_first>.claude/skills/supabase-migration-safety/SKILL.md (apply via mcp__supabase__apply_migration; RLS cannot be verified through the service-role connection); .tickmarkr/overseer/PARK-P93.md §(d) (the role-switched probe method)</read_first>
  <action>
    Apply the migration with mcp__supabase__apply_migration (never execute_sql for DDL, never
    supabase db push). Post-apply, in order: (1) re-query pg_policies via mcp__supabase__execute_sql
    for the 4 policy names — paste the qual/with_check texts into the SUMMARY and confirm each
    contains is_platform_admin and none contains auth.users or raw_*_meta_data; (2) run
    mcp__supabase__get_advisors and record the result; (3) attempt the PARK-P93 §(d) role-switched
    probe via execute_sql (SET LOCAL ROLE authenticated + request.jwt.claims in one transaction)
    against data_retention_policies to observe the predicate deciding instead of raising — if the
    transaction shape is not reproducible through the MCP, record that check as CANNOT CONSTRUCT
    with what it needs, never fold it into a pass; (4) run the live
    information_schema.table_privileges check (grantee list for SELECT on auth.users must be
    exactly `postgres`) via execute_sql and record it labelled `CANNOT CONSTRUCT as a bash gate
    (no DSN; MCP-only)` per D-23 — it is point-in-time evidence, not a standing gate, and the plan
    says so.
  </action>
  <verify>
    <automated>OUT=$(scripts/probe-edge-auth.sh data-retention) && echo "$OUT" | grep -qE 'data-retention -> 200'</automated>
  </verify>
  <acceptance_criteria>Probe flips 500 → 200 (RED today: the 42501-driven 500 is the current live state, re-verified same day as CONTEXT; GREEN: post-apply). pg_policies shows the 4 rewritten predicates; advisors run recorded; the auth.users grantee check returns exactly one row, postgres, recorded with its D-23 label. The tag-policy mutation oracle is the role-switched SQL (or an honest CANNOT CONSTRUCT) — never a service-role read, which bypasses RLS and proves nothing.</acceptance_criteria>
  <done>The migration is live, verified at the catalog AND behaviourally at the surface's own probe.</done>
</task>

<task type="auto">
  <name>Task 3: The standing anti-grant regression guard (D-12/D-23 part 1)</name>
  <files></files>
  <read_first>.planning/GATE-STANDARD.md C5/C8 (root existence; negative greps must not trip on prose)</read_first>
  <action>
    No file is created — this task executes and records the repository-wide guard that plan 93-15
    re-runs at phase close and any future phase can inherit: no migration file under
    supabase/migrations (483+ files) or backend/migrations contains an UNCOMMENTED
    `GRANT SELECT … auth.users`. Drill both directions per C1: RED is constructed in a scratch
    worktree by dropping `GRANT SELECT ON auth.users TO authenticated;` into a throwaway migration
    (count goes to 1); GREEN is the clean tree (count 0). The plans' own tree must be
    byte-identical afterwards (git status --porcelain before/after). Record both observations in
    the SUMMARY's gate-drill table. What the guard does NOT establish, stated: a grant applied by
    hand outside the migrations directories is invisible to it; the live-catalog half is Task 2's
    MCP evidence, point-in-time.
  </action>
  <verify>
    <automated>test -d supabase/migrations && test -d backend/migrations && test "$(grep -rniE 'grant[[:space:]]+select[^;]*auth\.users' supabase/migrations backend/migrations | grep -vE '^[^:]+:[0-9]+:[[:space:]]*--' | wc -l)" -eq 0</automated>
  </verify>
  <acceptance_criteria>Guard passes on the clean tree AND was observed red on the constructed forbidden state (both directions in the drill table). Both roots proven to exist in the same chain (C5, no 2>/dev/null anywhere). Comment-line exclusion means the migration's own refusal comment cannot trip it (C8).</acceptance_criteria>
  <done>A later hand taking the database's HINT turns a gate red instead of a screen green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                   |
| ------------------------------------ | ------------------------------------------------------------- |
| authenticated client → RLS predicate | the access-control decision this migration rewrites (ASVS V4) |
| migration author → auth.users grants | the HINT-as-exploit seam D-12 refuses                         |

## STRIDE Threat Register

| Threat ID | Category               | Component                                       | Disposition | Mitigation Plan                                                                                                                                                               |
| --------- | ---------------------- | ----------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-93-06   | Elevation of Privilege | raw_user_meta_data role reads                   | mitigate    | DELETED from all 4 predicates (any session can self-set metadata via auth.updateUser); is_platform_admin reads public.users.role/user_roles, which sessions cannot self-grant |
| T-93-07   | Elevation of Privilege | GRANT SELECT ON auth.users                      | mitigate    | refused (D-12); standing migrations-tree guard + live grantee evidence (D-23); refusal comment in the migration                                                               |
| T-93-08   | Elevation of Privilege | fail-open rewrite (predicate typo → USING true) | mitigate    | post-apply pg_policies re-query pasted; role-switched behavioural probe; get_advisors run                                                                                     |
| T-93-SC   | Tampering              | package installs                                | accept      | zero installs                                                                                                                                                                 |

</threat_model>

<verification>
- Probe `data-retention -> 200` (the behavioural oracle — the only change in this phase observable
  four layers up). pg_policies + advisors + grantee evidence in the SUMMARY with D-23 labels.
- Anti-grant guard drilled both directions.
- Said out loud: `/admin/data-retention`'s legal-holds region STILL errors after this migration —
  `legal_holds` is residual-11 (Phase 100). Plan 93-09's oracle asserts that residual error rather
  than tripping on it. A criterion-2 close implying the whole page went green is this milestone's
  own failure mode.
</verification>

<success_criteria>
DR-42501 closed at its real seam: the 4 policies decide instead of raising, data-retention returns
200 live, the residual 11 remain untouched and filed, and the exploit the database itself suggests
is fenced by a drilled gate.
</success_criteria>

<output>
Create `.planning/phases/93-failure-visibility/93-04-SUMMARY.md` when done.
</output>
