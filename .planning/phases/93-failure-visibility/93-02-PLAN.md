---
phase: 93-failure-visibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/my-delegations/index.ts
  - supabase/functions/data-retention/index.ts
  - tests/e2e/92-delegations-error.spec.ts
autonomous: true
requirements: [DELEG-01, TRUST-04]
truths:
  - 'Decisions covered — D-13: DELEG-01 is closed as VISIBILITY ONLY (the swallow stops, a real error status with a bilingual body returns, the table is NOT repointed — that is DELEG-02, Phase 102); D-20: the updated oracle keeps inline auth + --no-deps, never the Playwright setup project'
must_haves:
  truths:
    - 'my-delegations returns a real 5xx with a bilingual envelope when its queries fail — never 200 {"granted":[],"received":[],"total":0} over a 42P01'
    - '/delegations visibly ERRORS after this phase until Phase 102 lands DELEG-02 + SEED-DELEG-01 — that is the INTENDED outcome, recorded so nobody fixes it back to a confident empty list'
    - 'data-retention client-facing error bodies no longer carry the details passthrough (criterion-5 server half at this seam); its 500-vs-200 status is untouched here — plan 93-04 owns the flip'
    - 'CONFIRMED C9b instance, not a risk: 92-delegations-error.spec.ts test 2 goes hard red on the handler change at THREE assertions — :101 (getByRole(alert) toHaveCount 0 → the alert now renders), :110 (emptyState.or(delegationRows) visible → neither renders), :114 (stat toHaveText /^\d+$/ → becomes the em-dash) — so test 2 is INVERTED in the same task, and after this plan BOTH tests assert the error state, differing only in how the failure is induced (CDP block vs the real 42P01)'
  artifacts:
    - path: 'supabase/functions/my-delegations/index.ts'
      provides: 'early-return bilingual error envelope at both query-error sites'
      contains: 'message_ar'
    - path: 'tests/e2e/92-delegations-error.spec.ts'
      provides: 'natural-visit test updated to expect the honest error state (intended until Phase 102)'
      contains: 'DELEG-02'
  key_links:
    - from: 'supabase/functions/my-delegations/index.ts'
      to: 'frontend/src/pages/delegations/DelegationManagementPage.tsx'
      via: 'the Phase 92-03 error state (role="alert") now renders on the NATURAL visit because the fn returns a real error'
      pattern: 'QUERY_FAILED'
---

<objective>
Close DELEG-01 as visibility-only (D-13): my-delegations stops swallowing the 42P01 on the
nonexistent `public.delegations` relation and returns a real error status with a bilingual,
internal-string-free body — so `/delegations` renders the error state Phase 92 already shipped.
Also strip the `details: error` passthrough from data-retention's client-facing bodies while its
natural 500 is still live and observable (plan 93-04's migration flips it to 200 afterwards — that
ordering is why this plan is wave 1 and 93-04 depends on it).

Purpose: a confident 200-with-empty-arrays over a missing relation is the exact anti-pattern this
milestone exists to kill, on an AUTH-04 surface.
Output: deployed my-delegations + data-retention; updated 92-delegations-error spec green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-failure-visibility/93-PATTERNS.md  (§2 — the envelope analog with the one leaking line)
@.claude/skills/edge-function-add/SKILL.md
@supabase/functions/my-delegations/index.ts
@supabase/functions/data-retention/index.ts

<interfaces>
Verified by research (re-read at execution):
- my-delegations swallow sites: both query results handled as `if (error) { console.error(…) }
  else if (data) { … }` (granted ~:197, received ~:233), falling through to
  200 {granted:[],received:[],total:0}. The handler queries via supabaseAdmin (service-role) —
  leave that unchanged; the 42P01 fires regardless of client.
- The envelope to converge on: `{ error: { code, message_en, message_ar } }` with a real 4xx/5xx
  status — data-retention/index.ts:274-289 is the analog, minus its `details: error` line.
  Diagnostics go to console.error only. Never echo the 42P01 text or the missing relation name.
- data-retention has ~20 envelopes (grep message_ar, lines 66..657); every `details:` passthrough
  in a client-facing body goes; the envelope itself is already correct.
- The Phase 92 spec: tests/e2e/92-delegations-error.spec.ts — test 1 forces the error via CDP;
  test 2 asserts the UNBLOCKED visit shows no alert. After this plan the natural visit errors BY
  DESIGN, so test 2 must be updated or the suite lies the other way.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: my-delegations — replace both swallows with the bilingual error envelope</name>
  <files>supabase/functions/my-delegations/index.ts</files>
  <read_first>supabase/functions/my-delegations/index.ts in full; 93-PATTERNS.md §2 (envelope shape); supabase/functions/data-retention/index.ts:274-289 (the analog)</read_first>
  <action>
    At BOTH query-error sites (granted ~:197, received ~:233): on `error`, keep the
    console.error diagnostic, then return early with status 500 and body
    `{ error: { code: 'QUERY_FAILED', message_en: 'Failed to load delegations', message_ar:
    'فشل في تحميل التفويضات' } }` using the function's existing CORS headers. Do NOT repoint
    `.from("delegations")` at any other table (D-13 — the repoint is DELEG-02, Phase 102, decided
    beside seeding). Do NOT include `details`, the PostgREST error object, the 42P01 code, or
    words naming the missing relation in the body. No other behavior changes.
  </action>
  <verify>
    <automated>F=supabase/functions/my-delegations/index.ts && test -f "$F" && test "$(grep -c 'message_ar' "$F")" -ge 2 && test "$(grep -v '^[[:space:]]*//' "$F" | grep -cE 'details:[[:space:]]*error')" -eq 0</automated>
  </verify>
  <acceptance_criteria>Both swallow sites return the envelope early (threshold 2 = the two query-error sites D-13 names — one envelope per site). No details passthrough on non-comment lines. The `.from("delegations")` targets are byte-unchanged (the 42P01 still fires server-side; only its visibility changes).</acceptance_criteria>
  <done>A failed delegations query is a failed request at the HTTP layer.</done>
</task>

<task type="auto">
  <name>Task 2: data-retention — strip the details passthrough from client-facing bodies</name>
  <files>supabase/functions/data-retention/index.ts</files>
  <read_first>supabase/functions/data-retention/index.ts in full (locate every client-facing envelope; grep message_ar for the ~20 sites)</read_first>
  <action>
    In every Response body returned to the caller, delete the `details: error` (and any
    `details: <internal>`) property; keep code/message_en/message_ar; add or keep
    `console.error(error)` beside each so diagnostics still reach logs. Do not change any status
    code, query, or the `:112` role-lookup — plan 93-04 owns the policy migration that makes this
    function 200; this task only stops the leak while the 500 is still live. Do not add comments
    containing the literal `details:` (the gate counts property-position occurrences on
    non-comment lines).
  </action>
  <verify>
    <automated>F=supabase/functions/data-retention/index.ts && test -f "$F" && test "$(grep -v '^[[:space:]]*//' "$F" | grep -cE 'details:[[:space:]]*error')" -eq 0 && test "$(grep -c 'message_ar' "$F")" -ge 1</automated>
  </verify>
  <acceptance_criteria>Zero `details: error` passthroughs remain on non-comment lines (population: property-position matches in this one file; outside it: other functions' envelopes — audit-logs-viewer is plan 93-05's, my-delegations is Task 1's). The bilingual envelopes and every status code are unchanged.</acceptance_criteria>
  <done>The 42501 text can no longer transit to any client while the error is still reproducible live.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy both functions; update and run the delegations oracle</name>
  <files>tests/e2e/92-delegations-error.spec.ts</files>
  <read_first>tests/e2e/92-delegations-error.spec.ts in full (test 2 is the natural-visit half being updated); scripts/probe-edge-auth.sh usage header</read_first>
  <action>
    Deploy: `supabase functions deploy my-delegations --project-ref zkrcjzdemdmwhearhfgg` and the
    same for data-retention (CLI, or MCP deploy_edge_function as fallback). Record the deploy
    exits and `supabase functions list` updated_at evidence in the SUMMARY — this phase keeps NO
    shared deploy ledger file (each plan records its own; the Phase 92 C9a ledger instance is why
    no cross-plan artifact is created here).
    Then INVERT the spec's test 2 ("unblocked load renders without the error alert") — confirmed
    hard red on three assertions, so this is not optional and not a judgement call:
    - :101 `expect(page.getByRole('alert')).toHaveCount(0)` → the unblocked load now renders the
      error alert; assert the alert IS visible (same shape test 1 uses).
    - :110 `expect(emptyState.or(delegationRows)).toBeVisible()` → neither renders; assert the
      empty-state copy is ABSENT.
    - :114 `expect(statValue(...)).toHaveText(/^\d+$/)` → the stat is the em-dash; assert the
      em-dash, exactly as test 1 already does at :77.
    Test 2 encoded the invariant "unblocked implies success"; that invariant was NEVER true — the
    200-with-empty-arrays was the lie. After this plan both tests assert the error state, differing
    only in how the failure is induced (CDP block vs the real 42P01). Put a comment above test 2
    recording WHY it inverted, citing DELEG-02 AND SEED-DELEG-01 (Phase 102): when Phase 102
    repoints the table and seeds rows, test 2 inverts BACK, and that hand needs the reason, not a
    bug hunt. Keep test 1 (CDP-forced) byte-identical — DelegationManagementPage.tsx is NOT edited
    by this phase (C9b option (b), recorded in 93-01), so test 1's DOM selectors stay valid.
    Keep inline auth from TEST_USER_EMAIL/TEST_USER_PASSWORD, the 15s retry-ladder budget, and
    exactly 2 tests in the file; every invocation carries --no-deps (D-20/C6).
  </action>
  <verify>
    <automated>OUT=$(scripts/probe-edge-auth.sh my-delegations) && echo "$OUT" | grep -qE 'my-delegations -> 5[0-9][0-9]' && OUT2=$(pnpm exec playwright test tests/e2e/92-delegations-error.spec.ts --project=chromium-en --no-deps 2>&1) && echo "$OUT2" | grep -qE '\b2 passed'</automated>
  </verify>
  <acceptance_criteria>Probe shows my-delegations returning 5xx (RED today: it returns 200 — the confident lie; GREEN after deploy: a real error). Both spec tests pass under --no-deps — expected result stated BEFORE the run: "2 passed" (threshold 2 derived from the spec's own test count, verifiable via --list --no-deps). Test 2's inversion covers exactly the three confirmed-red assertions (:101, :110, :114 — the C9b evidence), and its comment names DELEG-02 + SEED-DELEG-01 so the intended-error state is never "fixed" back to emptiness and Phase 102 knows to invert it back.</acceptance_criteria>
  <done>/delegations is honestly broken, loudly, until Phase 102 decides and seeds.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                       |
| ---------------------------- | ------------------------------------------------- |
| edge function body → browser | PostgREST/Postgres internals cross here if leaked |

## STRIDE Threat Register

| Threat ID | Category               | Component                                    | Disposition | Mitigation Plan                                                                                           |
| --------- | ---------------------- | -------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------- |
| T-93-02   | Information Disclosure | my-delegations / data-retention error bodies | mitigate    | bilingual envelope only; 42P01/42501 text and relation names to console.error, never the body (gated)     |
| T-93-03   | Repudiation            | my-delegations                               | mitigate    | a failed query is a 5xx, not a 200-with-empty-arrays — the failure can no longer be denied by the payload |
| T-93-SC   | Tampering              | package installs                             | accept      | zero installs (esm.sh version tags untouched in this plan)                                                |

</threat_model>

<verification>
- Probe: `my-delegations -> 5xx`; updated spec 2/2 green `--no-deps`.
- data-retention source gate: zero `details:` passthroughs; statuses unchanged (the 500→200 flip
  is 93-04's, gated there by `data-retention -> 200`).
- Said out loud, for every later reader: `/delegations` will visibly ERROR from this plan until
  Phase 102. An erroring page beats a lying page.
</verification>

<success_criteria>
DELEG-01's visibility half is closed with live evidence: the fn fails loudly with a clean bilingual
body, and the Phase 92 UI error state renders on a natural visit. Criterion 5's server half is
closed at the data-retention seam while its error path was still constructible.
</success_criteria>

<output>
Create `.planning/phases/93-failure-visibility/93-02-SUMMARY.md` when done.
</output>
