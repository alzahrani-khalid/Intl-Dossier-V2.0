---
phase: 93-failure-visibility
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/audit-logs-viewer/index.ts
autonomous: true
requirements: [AUDIT-42703, TRUST-04]
truths:
  - "Decisions covered — D-14: the viewer targets public.audit_log (75 live rows, trigger-written — the ruling's staleness condition is DISCHARGED), remapped onto its real columns; D-15: the internal-string leak (42703 text, column names in 500 bodies) is fixed unconditionally; D-26: the statistics replacement PROMOTES the aggregate already written at index.ts:284-300 and deletes the audit_statistics query — no new aggregate is authored"
must_haves:
  truths:
    - 'audit-logs-viewer returns 200 with rows from the live audit_log (75 rows today) instead of a 42703-driven 500'
    - 'Error bodies carry the bilingual envelope only — no column names, no Postgres codes, no details passthrough'
    - "AUDIT-DROP-01 and AUDIT-ZERO-01 (the audit-log WRITERS) are Phase 94's, deliberately untouched here — if the pending overseer ruling folds AUDIT-DROP-01 in mid-phase, it attaches to THIS plan as an added task on backend/src/services/auth.service.ts:847"
  artifacts:
    - path: 'supabase/functions/audit-logs-viewer/index.ts'
      provides: 'selects/filters/search/CSV remapped to entity_type/action/entity_id/old_values/new_values; statistics from the promoted in-file aggregate; leak-free envelopes'
      contains: 'entity_type'
  key_links:
    - from: 'supabase/functions/audit-logs-viewer/index.ts'
      to: 'public.audit_log'
      via: 'JWT-scoped client (RLS applies; admin passes per migration-visible SELECT policies)'
      pattern: "from\\('audit_log'\\)"
---

<objective>
Make audit-logs-viewer read the table that exists, with the columns it has (D-14), promote its own
fallback aggregate to the only statistics path (D-26), and stop the 500 bodies leaking
`column audit_log.table_name does not exist` (D-15). The surface is currently non-functional over a
live table holding 75 rows.

Purpose: a 500 that names columns is a criterion-5 violation; a viewer that cannot read a live
audit table is a lying admin surface.
Output: deployed audit-logs-viewer; probe 200; leak-free error path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-failure-visibility/93-RESEARCH.md  (§AUDIT-42703 — the column-by-column field map is the edit spec)
@.planning/phases/93-failure-visibility/93-PATTERNS.md  (§2 — envelope analog)
@supabase/functions/audit-logs-viewer/index.ts

<interfaces>
Verified by research (re-read at execution):
- Real audit_log columns: id, tenant_id, entity_type, entity_id, action, user_id, timestamp,
  old_values, new_values, ip_address, user_agent, session_id, additional_context.
- Field map (handler → real): table_name→entity_type (also in filters :73-74, search or() :106-107,
  CSV fields :125,140); operation→action; row_id→entity_id; old_data/new_data→old_values/new_values;
  changed_fields→NONE (drop from UI+CSV — smallest honest diff); user_email/user_role→second query
  keyed on the page's distinct user_ids against public.users (PostgREST embed needs a declared FK —
  assumption A2 — so the keyed second query is the safe default; email is varchar, cast ::text only
  if surfaced through a RETURNS TABLE RPC); timestamp/session_id/ip_address/user_agent/user_id keep;
  request_id→drop or read from additional_context.
- audit_statistics does not exist; the aggregate fallback ALREADY WRITTEN at :284-300 (groups
  audit_log rows by action over the date range) IS the replacement — promote it, delete the
  audit_statistics query at :277-280.
- Leak sites: `details: error` at :167, :204, :239 et al. — strip per the 93-02 envelope rule.
- Client is JWT-scoped (:371-377) so RLS applies; the admin probe user passes (migration-visible
  SELECT policies admit admin/editor via public.users). The live probe is the oracle either way.
- The sibling audit_logs (0 rows) and its writers (AUDIT-ZERO-01), and
  backend/src/services/auth.service.ts:847 (AUDIT-DROP-01), are Phase 94's — do not "helpfully"
  touch any writer here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remap list/filter/search/CSV onto the real audit_log columns</name>
  <files>supabase/functions/audit-logs-viewer/index.ts</files>
  <read_first>supabase/functions/audit-logs-viewer/index.ts in full; 93-RESEARCH.md §AUDIT-42703 field map (the edit spec — every rename it lists, everywhere it lists it)</read_first>
  <action>
    Apply the field map to every select (:51-68, :192-194, :209-211), every filter (:73-74), the
    search or() (:106-107), and the CSV fields (:125,140): table_name→entity_type,
    operation→action, row_id→entity_id, old_data→old_values, new_data→new_values. Drop
    changed_fields from the response and CSV. Resolve user_email/user_role with a second query
    against public.users keyed on the page's distinct user_id values (not an embed — A2: no
    declared FK verified). Drop request_id or read it from additional_context. Response property
    names facing the frontend may keep their existing external shape only if the frontend consumes
    them today — inspect the consuming hook before renaming the wire contract, and state in the
    SUMMARY which external names were preserved vs renamed.
  </action>
  <verify>
    <automated>F=supabase/functions/audit-logs-viewer/index.ts && test -f "$F" && grep -q 'entity_type' "$F" && test "$(grep -v '^[[:space:]]*//' "$F" | grep -cE "'table_name'|\"table_name\"")" -eq 0 && test "$(grep -v '^[[:space:]]*//' "$F" | grep -cE "'old_data'|'new_data'")" -eq 0</automated>
  </verify>
  <acceptance_criteria>Zero quoted references to the phantom columns (table_name/old_data/new_data as string literals on non-comment lines; population: this one file; outside it: the frontend's display labels, which are its own vocabulary and not a DB column claim); entity_type present in the selects. The DB queries name only columns audit_log has.</acceptance_criteria>
  <done>Every query the viewer sends is answerable by the live table.</done>
</task>

<task type="auto">
  <name>Task 2: Promote the aggregate; strip the leak; deploy and probe</name>
  <files></files>
  <read_first>supabase/functions/audit-logs-viewer/index.ts:277-300 (the audit_statistics query to delete and the fallback aggregate to promote); scripts/probe-edge-auth.sh usage header</read_first>
  <action>
    Delete the audit_statistics query (:277-280) and make the in-file aggregate (:284-300) the only
    statistics path (D-26 — promote, don't author). Strip every `details: error` passthrough from
    client-facing bodies; keep the bilingual envelope (code, message_en, message_ar) and
    console.error diagnostics (D-15). Then deploy:
    `supabase functions deploy audit-logs-viewer --project-ref zkrcjzdemdmwhearhfgg`, recording the
    exit and updated_at evidence in the SUMMARY (no shared ledger). Capture one authenticated
    response body (adapt the probe script's JWT mint inline; never echo credentials) showing ≥1 of
    the 75 live rows, and one error-path body (e.g. an invalid query param) showing the envelope
    without column names or Postgres codes — both pasted into the SUMMARY as the row-presence and
    leak evidence the status-only gate cannot see.
  </action>
  <verify>
    <automated>F=supabase/functions/audit-logs-viewer/index.ts && test "$(grep -v '^[[:space:]]*//' "$F" | grep -c 'audit_statistics')" -eq 0 && test "$(grep -v '^[[:space:]]*//' "$F" | grep -cE 'details:[[:space:]]*error')" -eq 0 && OUT=$(scripts/probe-edge-auth.sh audit-logs-viewer) && echo "$OUT" | grep -qE 'audit-logs-viewer -> 200'</automated>
  </verify>
  <acceptance_criteria>audit_statistics gone; zero details passthroughs; probe flips 500 → 200 (RED today: live 42703-driven 500, re-verified same day as CONTEXT). Row-presence (≥1 of 75) and leak-free error body evidenced in the SUMMARY. AUDIT-DROP-01/AUDIT-ZERO-01 writers untouched — the closing statement names them as Phase 94's, with this plan as the attach point if the pending ruling folds AUDIT-DROP-01 in.</acceptance_criteria>
  <done>The admin audit surface reads the live table and fails clean when it fails.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                       |
| ----------------------------- | ------------------------------------------------- |
| edge function body → browser  | 42703 text/column names leak here today (ASVS V7) |
| JWT-scoped client → audit_log | RLS is the read boundary; admin-only per policies |

## STRIDE Threat Register

| Threat ID | Category               | Component                    | Disposition | Mitigation Plan                                                                             |
| --------- | ---------------------- | ---------------------------- | ----------- | ------------------------------------------------------------------------------------------- |
| T-93-09   | Information Disclosure | audit-logs-viewer 500 bodies | mitigate    | D-15: bilingual envelope only; diagnostics to console.error; details passthrough grep-gated |
| T-93-10   | Information Disclosure | audit rows → non-admin       | mitigate    | client stays JWT-scoped (:371-377); RLS decides; never switched to service-role             |
| T-93-SC   | Tampering              | package installs             | accept      | zero installs                                                                               |

</threat_model>

<verification>
- Probe `audit-logs-viewer -> 200` (RED: 500 today); source gates for phantom columns, statistics
  promotion, and leak; row-presence + clean-error-body evidence in the SUMMARY.
- Writers untouched: no diff under supabase/functions/** insert sites or backend/src — the plan's
  files_modified is the single viewer file.
</verification>

<success_criteria>
AUDIT-42703 closed: the viewer reads audit_log's real columns, statistics come from the promoted
in-file aggregate, the leak is gone unconditionally, and the writer-side defects remain cleanly
filed to Phase 94 with a named attach point.
</success_criteria>

<output>
Create `.planning/phases/93-failure-visibility/93-05-SUMMARY.md` when done.
</output>
