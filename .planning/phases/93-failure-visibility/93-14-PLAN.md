---
phase: 93-failure-visibility
plan: 14
type: execute
wave: 2
depends_on: [93-01]
files_modified:
  - frontend/src/pages/AssignmentQueue.tsx
  - frontend/src/router/index.tsx
  - frontend/src/lib/query-client.ts
  - frontend/src/pages/Countries.tsx
  - frontend/src/pages/MyAssignments.tsx
  - frontend/src/pages/TaskDetailPage.tsx
  - frontend/src/pages/WorkingGroupsPage.tsx
  - frontend/src/pages/audit-logs/AuditLogsPage.tsx
  - frontend/src/pages/forums/ForumsPage.tsx
  - frontend/src/pages/my-work/components/WorkItemList.tsx
  - frontend/src/routes/_protected/positions.tsx
  - frontend/src/routes/_protected/positions/$positionId.tsx
  - frontend/src/routes/_protected/scenario-sandbox.tsx
  - frontend/src/routes/_protected/tasks/$id.tsx
  - frontend/src/components/activity-feed/EnhancedActivityFeed.tsx
  - frontend/src/components/commitments/CommitmentsList.tsx
  - frontend/src/components/contacts/InteractionTimeline.tsx
  - frontend/src/components/dossier/tabs/DossierDocumentsTab.tsx
  - frontend/src/components/dossiers/DossierMoUsTab.tsx
  - frontend/src/components/elected-officials/ElectedOfficialListTable.tsx
  - frontend/src/components/geographic-visualization/WorldMapVisualization.tsx
  - frontend/src/components/positions/DossierPositionsTab.tsx
  - frontend/src/components/report-builder/ReportBuilder.tsx
  - frontend/src/components/version-comparison/VersionComparison.tsx
  - frontend/src/components/workflow-automation/WorkflowTestDialog.tsx
  - tests/e2e/93-tasks-queue-error.spec.ts
autonomous: true
requirements: [TRUST-04]
truths:
  - "Decisions covered — D-08: no server-originated error.message reaches the user; the 37/39-site population is PARTITIONED and both halves stated (bucket (a) query/transport-origin = the defect; bucket (c) RHF/author-written = correct, untouched); D-22: the fix targets THREE SEAMS — the bucket-(a) JSX sites, router/index.tsx's defaultErrorComponent, and the global mutation onError fallback — and states what is OUTSIDE the population: the 7 exception-boundary renders (bucket b) and the BotIntegrationsSettings toast are NOT swept, named here so the closing derivation says so instead of implying total coverage"
must_haves:
  truths:
    - "/tasks/queue renders the shared error state with i18n copy — the raw supabase-js message (criterion 5's exemplar) is gone"
    - "Every bucket-(a) site in this plan's enumerated population drops its error.message operand, keeping/adding an i18n fallback; AnalyticsDashboardPage and DossierListPage are EXCLUDED because plans 93-06/93-07 own those files (stated both places)"
    - 'The two default seams (router defaultErrorComponent, global mutation onError) render generic i18n copy — an internal string can no longer reach a user BY DEFAULT'
  artifacts:
    - path: 'frontend/src/pages/AssignmentQueue.tsx'
      provides: 'criterion-5 exemplar fixed: QueryErrorState variant page, no error.message'
      contains: 'QueryErrorState'
    - path: 'tests/e2e/93-tasks-queue-error.spec.ts'
      provides: 'forced-error oracle for /tasks/queue with the internal-string regex'
      contains: 'query-error-state'
  key_links:
    - from: 'frontend/src/router/index.tsx'
      to: 'frontend/src/i18n/en/common.json'
      via: 'defaultErrorComponent renders common:errors.queryFailed.* instead of error?.name/error?.message'
      pattern: 'queryFailed'
---

<objective>
Close criterion 5 at its three seams (D-22): the enumerated bucket-(a) JSX sites drop their
error.message operands, the router's defaultErrorComponent stops rendering error name/message
full-page, and the global mutation onError toast stops printing raw error.message — with the
population and its stated exclusions on the record (D-08).

Purpose: those three seams are where an internal string reaches a user BY DEFAULT; fixing them is
the root-cause set rather than a per-file sweep.
Output: leak-free defaults + exemplar surface on the shared state + forced-error oracle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-failure-visibility/93-RESEARCH.md  (§Criterion 5 — the D-08 partition table IS the site list; §outside-population notes (i)-(iii))
@frontend/src/pages/AssignmentQueue.tsx
@frontend/src/router/index.tsx
@frontend/src/lib/query-client.ts

<interfaces>
Verified by research (re-read at execution):
- Bucket (a) = 25 query/transport-origin JSX renders. TWO are excluded here by file ownership:
  pages/analytics/AnalyticsDashboardPage.tsx:275 (plan 93-06) and
  pages/dossiers/DossierListPage.tsx:817 (plan 93-07). The remaining 23 are this plan's
  files_modified (list above; exemplar pages/AssignmentQueue.tsx:48
  `{error.message || t('queue.error')}` — the fallback key is right, the first operand must go).
- Bucket (c) (RHF field messages, t('error.message') key literals, ActionableErrorMessage's
  messageKey, validation badges) is CORRECT and untouched.
- Bucket (b) (7 exception-boundary renders) is OUTSIDE this plan's population per D-22 — named,
  not swept. Likewise BotIntegrationsSettings.tsx:154's toast.
- Seam 2: router/index.tsx:69-86 defaultErrorComponent renders error?.name and error?.message
  raw, full-page — it WILL be the surface any loader-thrown non-404 error hits once criterion-3
  loaders exist elsewhere.
- Seam 3: lib/query-client.ts:62-75 global mutation onError toasts raw error.message. (Its
  onSuccess "Operation completed successfully" half is WRITE-04, Phase 94 — do not touch.)
- The i18n copy comes from 93-01's common:errors.* keys; toast/router code imports the i18n
  singleton for t() outside React context. /tasks/queue invokes assignments-queue, which is NOT
  DEPLOYED (DEAD-02, Phase 95) — the natural visit errors today and will KEEP erroring, honestly,
  until Phase 95; the spec forces the error via CDP so it stays deterministic either way.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The three seams — exemplar page, router default, mutation onError</name>
  <files>frontend/src/pages/AssignmentQueue.tsx, frontend/src/router/index.tsx, frontend/src/lib/query-client.ts</files>
  <read_first>AssignmentQueue.tsx in full; router/index.tsx:60-95 (defaultErrorComponent + defaultNotFoundComponent); lib/query-client.ts in full (the onError seam and the singleton export); frontend/src/components/error-states/QueryErrorState.tsx</read_first>
  <action>
    AssignmentQueue: replace the :48 error render with QueryErrorState variant 'page' (UI-SPEC
    lists /tasks/queue as a variant-A surface), onRetry → refetch; no error.message anywhere.
    Router defaultErrorComponent: keep its layout role but render generic i18n copy
    (common:errors.queryFailed.title/.description via the i18n singleton) instead of error?.name /
    error?.message; diagnostics go to console.error inside the component. Do not disturb
    defaultNotFoundComponent.
    query-client onError: the toast fallback message becomes t('common:errors.queryFailedInline')
    (i18n singleton import — this module is outside React context); the raw error goes to
    console.error only. Leave the onSuccess toast untouched (WRITE-04/Phase 94 owns it, stated).
  </action>
  <verify>
    <automated>cd frontend && grep -q 'QueryErrorState' src/pages/AssignmentQueue.tsx && test "$(grep -v '^[[:space:]]*//' src/pages/AssignmentQueue.tsx | grep -vE 'console\.|throw |new Error' | grep -cE 'error\?\.message|error\.message')" -eq 0 && test "$(grep -v '^[[:space:]]*//' src/router/index.tsx | grep -vE 'console\.|throw |new Error' | grep -cE 'error\?\.(name|message)|error\.(name|message)')" -eq 0 && test "$(grep -v '^[[:space:]]*//' src/lib/query-client.ts | grep -vE 'console\.|throw |new Error' | grep -cE 'error\.message')" -eq 0 && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>All three seams render/toast i18n copy only (render-position error.message/name count 0 on non-comment, non-console/throw lines per the D-08 method); the onSuccess toast is byte-unchanged; type-check clean.</acceptance_criteria>
  <done>No internal string reaches a user by default, and the exemplar surface uses the shared state.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the remaining bucket-(a) sites</name>
  <files>frontend/src/pages/Countries.tsx, frontend/src/pages/MyAssignments.tsx, frontend/src/pages/TaskDetailPage.tsx, frontend/src/pages/WorkingGroupsPage.tsx, frontend/src/pages/audit-logs/AuditLogsPage.tsx, frontend/src/pages/forums/ForumsPage.tsx, frontend/src/pages/my-work/components/WorkItemList.tsx, frontend/src/routes/_protected/positions.tsx, frontend/src/routes/_protected/positions/$positionId.tsx, frontend/src/routes/_protected/scenario-sandbox.tsx, frontend/src/routes/_protected/tasks/$id.tsx, frontend/src/components/activity-feed/EnhancedActivityFeed.tsx, frontend/src/components/commitments/CommitmentsList.tsx, frontend/src/components/contacts/InteractionTimeline.tsx, frontend/src/components/dossier/tabs/DossierDocumentsTab.tsx, frontend/src/components/dossiers/DossierMoUsTab.tsx, frontend/src/components/elected-officials/ElectedOfficialListTable.tsx, frontend/src/components/geographic-visualization/WorldMapVisualization.tsx, frontend/src/components/positions/DossierPositionsTab.tsx, frontend/src/components/report-builder/ReportBuilder.tsx, frontend/src/components/version-comparison/VersionComparison.tsx, frontend/src/components/workflow-automation/WorkflowTestDialog.tsx</files>
  <read_first>93-RESEARCH.md §Criterion 5 partition table (the exact file:line list — this is the population); each file's cited line in context before editing it</read_first>
  <action>
    At each cited site: drop the error.message (or err.message) operand from the RENDER, keeping
    the existing i18n fallback where one exists (`{error.message || t('x')}` → `{t('x')}`); where
    no fallback exists, use t('common:errors.queryFailedInline') for section-level renders or
    t('common:errors.queryFailed.title') for page-level ones — never author new keys (93-01 owns
    i18n files; this plan adds ZERO i18n edits). The one structured exception stands: a body
    carrying the project's bilingual envelope (message_en/message_ar) MAY render by language.
    console.error/throw/toast lines in these files are NOT the defect and are untouched (bucket
    definition). Surgical: one-line edits, no adjacent reformatting, no isError restructuring
    beyond what the operand drop requires.
  </action>
  <verify>
    <automated>cd frontend && test "$(grep -v '^[[:space:]]*//' src/pages/Countries.tsx src/pages/MyAssignments.tsx src/pages/TaskDetailPage.tsx src/pages/WorkingGroupsPage.tsx src/pages/audit-logs/AuditLogsPage.tsx src/pages/forums/ForumsPage.tsx src/pages/my-work/components/WorkItemList.tsx src/routes/_protected/positions.tsx 'src/routes/_protected/positions/$positionId.tsx' src/routes/_protected/scenario-sandbox.tsx 'src/routes/_protected/tasks/$id.tsx' src/components/activity-feed/EnhancedActivityFeed.tsx src/components/commitments/CommitmentsList.tsx src/components/contacts/InteractionTimeline.tsx src/components/dossier/tabs/DossierDocumentsTab.tsx src/components/dossiers/DossierMoUsTab.tsx src/components/elected-officials/ElectedOfficialListTable.tsx src/components/geographic-visualization/WorldMapVisualization.tsx src/components/positions/DossierPositionsTab.tsx src/components/report-builder/ReportBuilder.tsx src/components/version-comparison/VersionComparison.tsx src/components/workflow-automation/WorkflowTestDialog.tsx | grep -vE 'console\.|throw |new Error|toast' | grep -cE 'error\?\.message|error\.message|err\.message')" -eq 0 && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>Zero render-position error.message matches across the 22 enumerated files (RED today: every file carries its cited site; population: non-comment lines minus console/throw/new Error/toast lines — the D-08 matching rule; OUTSIDE it, stated: bucket (b) boundaries, the BotIntegrationsSettings toast, multiline/template renders the single-line grep cannot see, and the two files owned by 93-06/93-07); type-check clean.</acceptance_criteria>
  <done>The enumerated bucket-(a) population is closed with its exclusions on the record.</done>
</task>

<task type="auto">
  <name>Task 3: Forced-error spec for /tasks/queue</name>
  <files>tests/e2e/93-tasks-queue-error.spec.ts</files>
  <read_first>tests/e2e/92-delegations-error.spec.ts (template); 93-UI-SPEC.md §Verification Notes (the copy-rule regex)</read_first>
  <action>
    ONE test, inline auth, --no-deps, 15s budget, DOM-only: CDP-block `*assignments-queue*`,
    visit /tasks/queue, assert [data-testid="query-error-state"] visible with a retry button, and
    the page body text does NOT match /(42501|42703|42P01|permission denied|supabase|FunctionsHttpError|FunctionsFetchError)/i.
    Header comment: assignments-queue is UNDEPLOYED (DEAD-02, Phase 95) so the natural visit also
    errors today — the CDP block keeps this spec deterministic before AND after Phase 95 deploys
    the function, which is why the natural state is not asserted.
  </action>
  <verify>
    <automated>OUT=$(pnpm exec playwright test tests/e2e/93-tasks-queue-error.spec.ts --project=chromium-en --no-deps 2>&1) && echo "$OUT" | grep -qE '\b1 passed'</automated>
  </verify>
  <acceptance_criteria>1 passed under --no-deps. The criterion's own sentence ("/tasks/queue no longer prints the raw supabase-js message") is closed by a rendered state plus the regex, not a grep alone.</acceptance_criteria>
  <done>Criterion 5's exemplar has a deterministic rendering oracle.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                          |
| ----------------------------------- | ---------------------------------------------------- |
| rejection object → JSX/toast/router | internal strings cross into user copy here (ASVS V7) |

## STRIDE Threat Register

| Threat ID | Category               | Component                                             | Disposition | Mitigation Plan                                                                                   |
| --------- | ---------------------- | ----------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------- |
| T-93-29   | Information Disclosure | 23 bucket-(a) render sites                            | mitigate    | operand drops with i18n fallbacks; population-scoped grep gate; spec regex on the exemplar        |
| T-93-30   | Information Disclosure | router defaultErrorComponent + global onError         | mitigate    | generic i18n copy by default; diagnostics to console.error                                        |
| T-93-31   | Information Disclosure | bucket (b) boundaries + BotIntegrationsSettings toast | accept      | OUTSIDE this phase's population by D-22, named in the closing derivation — not silently uncovered |
| T-93-SC   | Tampering              | package installs                                      | accept      | zero installs                                                                                     |

</threat_model>

<verification>
- All three gates + type-check + spec 1/1 --no-deps.
- Said out loud: /tasks/queue keeps erroring (honestly) until Phase 95 deploys assignments-queue;
  the onSuccess lying toast is WRITE-04/Phase 94; bucket (b) and the settings toast are not swept.
</verification>

<success_criteria>
Criterion 5 holds at its root-cause seams: no user-facing error contains an internal string on the
enumerated population, the two default paths render i18n copy, and everything deliberately outside
the population is named where the closing derivation will restate it.
</success_criteria>

<output>
Create `.planning/phases/93-failure-visibility/93-14-SUMMARY.md` when done.
</output>
