---
phase: 93-failure-visibility
plan: 06
type: execute
wave: 2
depends_on: [93-01]
files_modified:
  - frontend/src/domains/analytics/repositories/analytics.repository.ts
  - frontend/src/pages/analytics/AnalyticsDashboardPage.tsx
  - tests/e2e/93-analytics-error.spec.ts
autonomous: true
requirements: [TRUST-01]
truths:
  - "Decisions covered — D-01: the three repository swallows are DELETED (the transport already throws; no result union is grafted on); D-02: sites 1-3 of the six-site TRUST-01 population close here, with the population definition restated in the closing check; D-20: the oracle authenticates inline and runs --no-deps; D-21: the swallow-deletion and the consumer's error-branch repair land in the SAME task — AnalyticsDashboardPage's existing isError branch renders error?.message, so deleting the swallow alone would trade a lying screen for a differently-lying one"
must_haves:
  truths:
    - 'A rejected analytics query reaches AnalyticsDashboardPage as a rejection and renders the shared QueryErrorState — never error?.message, never fabricated data'
    - 'analytics.repository.ts contains zero catch-and-return-{data: null} sites (3 today)'
    - '/analytics rendering an honest error until its backend endpoints exist is the INTENDED post-phase state — DEAD-05 (Phase 96) owns making the data real'
  artifacts:
    - path: 'frontend/src/domains/analytics/repositories/analytics.repository.ts'
      provides: 'thin wrappers over apiGet that let rejections through (criterion 1 exemplar)'
      contains: 'apiGet'
    - path: 'tests/e2e/93-analytics-error.spec.ts'
      provides: 'forced-error oracle: blocked analytics fetch renders query-error-state with no internal string'
      contains: 'query-error-state'
  key_links:
    - from: 'frontend/src/pages/analytics/AnalyticsDashboardPage.tsx'
      to: 'frontend/src/components/error-states/QueryErrorState.tsx'
      via: 'isError branch renders the shared component (variant per UI-SPEC: page owns the primary query)'
      pattern: 'QueryErrorState'
---

<objective>
Close TRUST-01 sites 1-3 (D-02): delete the three catch-and-return-{data: null} swallows in
analytics.repository.ts (D-01) and, in the same motion, repair their consumer — the
AnalyticsDashboardPage isError branch that currently renders `error?.message` (a criterion-5 leak)
becomes the shared QueryErrorState (D-21 pairing rule).

Purpose: criterion 1's exemplar — the isError branches already written in the pages stop being dead
code, and what they render stops leaking.
Output: honest repository + honest page + forced-error spec green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-failure-visibility/93-RESEARCH.md  (§TRUST-01 sites 1-3 + blast radius; §Code Examples D-01 deletion shape)
@.planning/phases/93-failure-visibility/93-PATTERNS.md  (§5 — the forced-error spec analog)
@frontend/src/domains/analytics/repositories/analytics.repository.ts
@frontend/src/pages/analytics/AnalyticsDashboardPage.tsx

<interfaces>
Verified by research (re-read at execution):
- Sites 1-3: analytics.repository.ts:13,25,37 — try/catch around apiGet(..., { baseUrl:
  'express' }) returning { data: null }. apiGet throws ApiError on non-OK
  (lib/api-client.ts:111-118) — the wrapper collapses to the bare call (D-01 deletion shape in
  RESEARCH §Code Examples).
- Consumers: AnalyticsDashboardPage HAS an isError branch at :269 that renders
  `{error?.message || t('errors.networkError')}` at :275 — the D-21 pairing target.
  Dashboard/components/AnalyticsWidget.tsx already renders t('analytics.error') — safe, untouched.
- The endpoints are Express-backed and per the repository's own comment "not available" — after
  this plan the page errors honestly where it fabricated; Phase 96 (DEAD-05) makes the data real.
- Spec template: tests/e2e/92-delegations-error.spec.ts (inline auth from
  TEST_USER_EMAIL/TEST_USER_PASSWORD, CDP Network.setBlockedURLs, RETRY_BACKOFF_TIMEOUT 15s,
  DOM-only assertions).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the three swallows AND repair the page branch (same task, D-21)</name>
  <files>frontend/src/domains/analytics/repositories/analytics.repository.ts, frontend/src/pages/analytics/AnalyticsDashboardPage.tsx</files>
  <read_first>analytics.repository.ts in full; AnalyticsDashboardPage.tsx:250-300 (the isError branch and what surrounds it); 93-RESEARCH.md §Code Examples "D-01 deletion shape"; frontend/src/components/error-states/QueryErrorState.tsx (the contract produced by 93-01)</read_first>
  <action>
    Repository: delete all three try/catch wrappers whole — each function collapses to the bare
    apiGet call (D-01: with nothing left to log locally, the console.warn goes too). Keep explicit
    return types; remove imports the deletion orphans.
    Page: replace the :275 render with the shared QueryErrorState (variant 'page' — the page's
    primary query owns the region per UI-SPEC "which variant where"), onRetry wired to the query's
    refetch. The error object is never rendered; no error?.message operand survives anywhere in
    the file. Surgical: no adjacent reformatting, no other branches touched.
  </action>
  <verify>
    <automated>cd frontend && F=src/domains/analytics/repositories/analytics.repository.ts && test -f "$F" && test "$(grep -v '^[[:space:]]*//' "$F" | grep -c 'data: null')" -eq 0 && grep -q 'query-error-state\|QueryErrorState' src/pages/analytics/AnalyticsDashboardPage.tsx && test "$(grep -v '^[[:space:]]*//' src/pages/analytics/AnalyticsDashboardPage.tsx | grep -vE 'console\.|throw |new Error' | grep -cE 'error\?\.message|error\.message')" -eq 0 && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>Zero `data: null` fabrications in the repository (RED today: 3 — population: non-comment lines of this file; outside it: the other five D-02 sites, owned by 93-07/93-08); the page renders QueryErrorState and zero error.message on render lines (console./throw/new Error lines excluded per the D-08 method); type-check clean.</acceptance_criteria>
  <done>Rejections flow repository → TanStack Query → an honest, leak-free page state.</done>
</task>

<task type="auto">
  <name>Task 2: Forced-error spec for /analytics</name>
  <files>tests/e2e/93-analytics-error.spec.ts</files>
  <read_first>tests/e2e/92-delegations-error.spec.ts in full (the template: inline auth, CDP block, 15s budget, DOM-only assertions); 93-UI-SPEC.md §Verification Notes (the copy-rule regex)</read_first>
  <action>
    Author ONE test cloned from the 92 template: sign in inline, CDP-block the analytics fetches
    (block URL pattern matching the express analytics endpoints — derive the exact request URLs
    from the repository's apiGet paths, e.g. `*analytics-dashboard*`), visit /analytics, assert
    [data-testid="query-error-state"] visible within the 15s retry budget, a retry button inside
    the alert region, AND that the page body text does NOT match
    /(42501|42703|42P01|permission denied|supabase|networkError|stack)/i. All assertions on the
    DOM, never a response status. Header comment states inline auth + --no-deps (D-20) and that
    the natural /analytics visit also errors today by design until Phase 96 (DEAD-05) — which is
    why the spec forces the error rather than asserting the natural state.
  </action>
  <verify>
    <automated>OUT=$(pnpm exec playwright test tests/e2e/93-analytics-error.spec.ts --project=chromium-en --no-deps 2>&1) && echo "$OUT" | grep -qE '\b1 passed'</automated>
  </verify>
  <acceptance_criteria>1 passed under --no-deps (threshold 1 = the spec's own test count, verifiable via --list --no-deps). The spec keys on the 93-01 testid contract (positive-controlled there by the component's unit test) and the UI-SPEC copy-rule regex.</acceptance_criteria>
  <done>Criterion 1's exemplar has a behavioural oracle that renders, not a grep.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                  |
| ----------------------------- | -------------------------------------------- |
| query rejection → page render | error objects cross into user copy (ASVS V7) |

## STRIDE Threat Register

| Threat ID | Category               | Component              | Disposition | Mitigation Plan                                                                     |
| --------- | ---------------------- | ---------------------- | ----------- | ----------------------------------------------------------------------------------- |
| T-93-11   | Information Disclosure | AnalyticsDashboardPage | mitigate    | error?.message render replaced by QueryErrorState i18n copy; grep-gated; spec regex |
| T-93-12   | Repudiation            | analytics repository   | mitigate    | rejections propagate; { data: null } fabrication deleted (gated)                    |
| T-93-SC   | Tampering              | package installs       | accept      | zero installs                                                                       |

</threat_model>

<verification>
- Source gates (data: null → 0; error.message render lines → 0) + type-check + spec 1/1 --no-deps.
- Said out loud: /analytics will show the honest error state until Phase 96 lands real endpoints —
  intended, not a regression.
</verification>

<success_criteria>
Sites 1-3 of the TRUST-01 population are closed with the D-21 pairing intact: the swallow is gone
AND its consumer renders the shared error state, leak-free, proven by a rendering oracle.
</success_criteria>

<output>
Create `.planning/phases/93-failure-visibility/93-06-SUMMARY.md` when done.
</output>
