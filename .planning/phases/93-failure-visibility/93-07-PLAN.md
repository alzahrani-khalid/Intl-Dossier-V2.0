---
phase: 93-failure-visibility
plan: 07
type: execute
wave: 2
depends_on: [93-01]
files_modified:
  - frontend/src/domains/dossiers/hooks/useDossier.ts
  - frontend/src/pages/dossiers/DossierListPage.tsx
  - tests/e2e/93-dossier-list-counts-error.spec.ts
autonomous: true
requirements: [TRUST-01]
truths:
  - "Decisions covered — D-01: the two useDossier swallows (emptyCounts at :683, 0 at :738) are DELETED; D-21: deleting them alone reproduces the identical lie one layer up — DossierListPage renders seven confident zero-cards from typeStatsMap?.[type] ?? {count: 0} the moment the counts query rejects — so the deletion and the page's counts error branch land in the SAME task"
must_haves:
  truths:
    - 'A rejected counts query renders em-dash count chrome (aria-label common:errors.countUnavailable) and a variant-B region error on DossierListPage — never seven zero-cards'
    - 'useDossier.ts contains zero catch-and-return fabrications (emptyCounts / 0)'
    - 'The dossier list itself still renders rows while only the counts query is rejected — the forced block discriminates the two queries'
  artifacts:
    - path: 'frontend/src/pages/dossiers/DossierListPage.tsx'
      provides: "counts isError branch: em-dash stats + query-error-inline region; the :817 error.message render is also fixed here (this plan owns the file — stated exclusion from 93-14's sweep)"
      contains: 'countUnavailable'
    - path: 'tests/e2e/93-dossier-list-counts-error.spec.ts'
      provides: 'forced counts-rejection oracle proving em-dash + inline error + list rows intact'
      contains: 'query-error-inline'
  key_links:
    - from: 'frontend/src/pages/dossiers/DossierListPage.tsx'
      to: 'frontend/src/domains/dossiers/hooks/useDossier.ts'
      via: 'isError destructured from useDossierCounts (never read today)'
      pattern: 'useDossierCounts'
---

<objective>
Close TRUST-01 sites 4-5 (D-02): delete the emptyCounts/0 fabrications in useDossier.ts and, in the
same task, give DossierListPage the counts error branch it lacks — em-dash count chrome plus a
variant-B region state — so the rejection renders honestly instead of as seven zero-cards (the
D-21 trap research pinned at this exact seam).

Purpose: trading a lying screen for a differently-lying one is not this phase's goal.
Output: honest counts hooks + honest list page + a discriminating forced-error spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-failure-visibility/93-RESEARCH.md  (§TRUST-01 sites 4-5 + DossierListPage blast radius)
@.planning/phases/93-failure-visibility/93-PATTERNS.md  (§Shared Patterns — the em-dash statFigure idiom from DelegationManagementPage:112-113)
@frontend/src/domains/dossiers/hooks/useDossier.ts
@frontend/src/pages/dossiers/DossierListPage.tsx

<interfaces>
Verified by research (re-read at execution):
- Site 4: useDossier.ts:683 (useDossierCounts queryFn) catches and returns emptyCounts (all zeros,
  6 types). Its transport getDossierCountsByType (services/dossier-api.ts:686) already checks
  .error and throws DossierAPIError — deletion suffices.
- Site 5: useDossier.ts:738 (useDossierCountByType queryFn) catches and returns 0; transport is
  fetch-based and throws — deletion suffices.
- Consumer: DossierListPage.tsx:244 destructures { data: counts, isLoading: countsLoading,
  refetch } — NO isError. On rejection today: typeStatsMap null → the type-overview grid renders
  `typeStatsMap?.[type] ?? { count: 0, … }` (:537-541) — seven confident zero-cards; and
  totalDossiersUnfiltered goes undefined.
- Also in this file: :817 renders error.message (bucket (a) of the D-08 partition). This plan owns
  the file, so it fixes that site too; plan 93-14's sweep EXCLUDES this file and says so.
- UI contract: failed counts render em-dash '—' with aria-label from common:errors.countUnavailable
  (never a fake 0), the owning region shows variant B (UI-SPEC "Small numeric chrome").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete both swallows AND land the page's counts error branch (same task, D-21)</name>
  <files>frontend/src/domains/dossiers/hooks/useDossier.ts, frontend/src/pages/dossiers/DossierListPage.tsx</files>
  <read_first>useDossier.ts:660-760 (both queryFns whole); DossierListPage.tsx in full (the :244 destructure, the :537-541 grid, the :817 error render); DelegationManagementPage.tsx:112-113 (the em-dash statFigure idiom); frontend/src/components/error-states/QueryErrorState.tsx</read_first>
  <action>
    Hook: delete both catch blocks whole — each queryFn collapses to its transport call; remove
    the emptyCounts constant if the deletion orphans it (lint will flag it). No result unions, no
    logging left behind for its own sake (D-01).
    Page: destructure isError (and keep refetch) from useDossierCounts. While isError: every count
    figure in the type-overview grid and totals chrome renders the em-dash '—' with
    aria-label={t('common:errors.countUnavailable')} — never 0; the counts region renders
    QueryErrorState variant 'inline' with onRetry wired to refetch; list rows (the separate list
    query) keep rendering untouched. Also fix the :817 render in this same file: drop the
    error.message operand, keep the i18n fallback (this file is EXCLUDED from 93-14's sweep
    because this plan owns it — the exclusion is stated in both plans). Loading and happy paths
    stay byte-identical.
  </action>
  <verify>
    <automated>cd frontend && F=src/domains/dossiers/hooks/useDossier.ts && test -f "$F" && test "$(grep -v '^[[:space:]]*//' "$F" | grep -c 'emptyCounts')" -eq 0 && grep -q 'countUnavailable' src/pages/dossiers/DossierListPage.tsx && test "$(grep -v '^[[:space:]]*//' src/pages/dossiers/DossierListPage.tsx | grep -vE 'console\.|throw |new Error' | grep -cE 'error\?\.message|error\.message')" -eq 0 && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>emptyCounts gone from the hook (RED today: present — population: non-comment lines of useDossier.ts; outside it: the other four D-02 sites, owned by 93-06/93-08); the page carries the countUnavailable chrome; zero error.message render lines in the page (:817 fixed); type-check clean. C9b consumers, triaged HELD-BY-CONSTRAINT: shipped specs assert this page's happy-path DOM (tests/e2e/03-dossier-navigation.spec.ts, 04-command-palette.spec.ts, 08-export-import.spec.ts, ar-smoke/dossier-navigation.ar.spec.ts via the POM tests/e2e/support/pages/DossierListPage.ts) — the byte-identical-happy-path requirement above is what keeps them green; if any happy-path selector must change, update the POM in this same task and say so.</acceptance_criteria>
  <done>A rejected counts query is visibly unknown, not zero — at the hook AND at the page.</done>
</task>

<task type="auto">
  <name>Task 2: Discriminating forced-error spec</name>
  <files>tests/e2e/93-dossier-list-counts-error.spec.ts</files>
  <read_first>tests/e2e/92-delegations-error.spec.ts (template); services/dossier-api.ts:680-700 (the counts request shape — derive its distinguishing URL/method before writing the block)</read_first>
  <action>
    Author ONE test: sign in inline, then block ONLY the counts request. Derive the discriminator
    at authoring time: the counts query is a supabase-js count query (head:true) against the same
    /rest/v1/dossiers resource the list also queries, so CDP setBlockedURLs (URL-only matching)
    may be too blunt — prefer page.route() with a predicate on request method (HEAD) and/or the
    count query's distinguishing query-string, aborting only matches. The test asserts, all on
    the DOM within the 15s budget: em-dash figures visible in the counts chrome (and NOT the digit
    0 rendered from a rejection), [data-testid="query-error-inline"] visible in the counts region,
    AND at least one dossier list row/card rendered normally — proving the block discriminated.
    Header comment: inline auth + --no-deps (D-20); the discriminator's derivation stated.
  </action>
  <verify>
    <automated>OUT=$(pnpm exec playwright test tests/e2e/93-dossier-list-counts-error.spec.ts --project=chromium-en --no-deps 2>&1) && echo "$OUT" | grep -qE '\b1 passed'</automated>
  </verify>
  <acceptance_criteria>1 passed under --no-deps. The spec proves all three conjuncts (em-dash, inline error, live rows) in one rendered state — a block that also kills the list query fails the third conjunct and is therefore self-checking.</acceptance_criteria>
  <done>The zero-cards lie is impossible to reintroduce without a red spec.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                  |
| ------------------------------ | -------------------------------------------- |
| counts rejection → list chrome | failure crosses into confident numerals here |

## STRIDE Threat Register

| Threat ID | Category               | Component         | Disposition | Mitigation Plan                                                                  |
| --------- | ---------------------- | ----------------- | ----------- | -------------------------------------------------------------------------------- |
| T-93-13   | Repudiation            | DossierListPage   | mitigate    | em-dash + aria-label on failed counts; zero-card fallback unreachable on isError |
| T-93-14   | Information Disclosure | :817 error render | mitigate    | error.message operand dropped; i18n fallback only (grep-gated)                   |
| T-93-SC   | Tampering              | package installs  | accept      | zero installs                                                                    |

</threat_model>

<verification>
- Source gates + type-check + spec 1/1 --no-deps.
- The count-consistency questions (persons 16 vs 15, engagements 5 vs 3) are COUNT-02, Phase 96 —
  this plan makes count failure honest, not count values correct.
</verification>

<success_criteria>
Sites 4-5 closed with the D-21 pairing intact: the fabrications are gone, the page renders unknown
as unknown, and the discriminating spec proves the region-level honesty without breaking the list.
</success_criteria>

<output>
Create `.planning/phases/93-failure-visibility/93-07-SUMMARY.md` when done.
</output>
