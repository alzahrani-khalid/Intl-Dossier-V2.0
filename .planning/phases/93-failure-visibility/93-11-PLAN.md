---
phase: 93-failure-visibility
plan: 11
type: execute
wave: 2
depends_on: [93-01]
files_modified:
  - frontend/src/components/dossier/DossierShell.tsx
  - tests/e2e/93-dossier-notfound.spec.ts
autonomous: true
requirements: [TRUST-03]
truths:
  - "Decisions covered — D-05: a failed request and an absent record are different states with different renders (rejection → QueryErrorState; well-formed-but-absent ID → page-level not-found), never one component; D-06: not-found uses TanStack Router's notFound() — and this phase ESTABLISHES the pattern: ZERO notFound() call sites exist in frontend/src today (the corrected premise), so there is nothing to copy, only the root boundary (routes/__root.tsx:72) to throw INTO"
must_haves:
  truths:
    - 'A well-formed but nonexistent dossier ID renders the root 404 page on all seven typed dossier layouts — one edit in DossierShell covers them all'
    - 'A non-404 rejection on dossier detail renders QueryErrorState — never the 404 page, never titleless chrome, never skeletons resolving to "Check your connection"'
  artifacts:
    - path: 'frontend/src/components/dossier/DossierShell.tsx'
      provides: "the codebase's FIRST notFound() throw (404 discriminated by DossierAPIError.status) plus the shell's missing isError branch"
      contains: 'notFound'
    - path: 'tests/e2e/93-dossier-notfound.spec.ts'
      provides: 'absent-ID → 404 page; forced rejection on a REAL id → error state (the D-05 discriminator, both directions)'
      contains: 'query-error-state'
  key_links:
    - from: 'frontend/src/components/dossier/DossierShell.tsx'
      to: 'frontend/src/routes/__root.tsx'
      via: 'thrown notFound() caught by the root notFoundComponent (fuzzy default) — the boundary exists, this adds the first thrower'
      pattern: 'notFound'
---

<objective>
Establish the not-found pattern (D-06, corrected premise): DossierShell — mounted by all seven typed
dossier detail layouts — discriminates a 404 (throw notFound() → root 404 page) from every other
rejection (QueryErrorState), replacing today's titleless chrome-on-any-failure. One component edit
covers seven routes.

Purpose: criterion 3 names dossier detail; today "Check your connection and try again" renders for
a record that simply is not there — D-05's exact collapse.
Output: first notFound() thrower + shell error state + two-direction spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-failure-visibility/93-RESEARCH.md  (§Criterion 3 — per-route seams table + §Code Examples "Component-throw notFound")
@.planning/phases/93-failure-visibility/93-PATTERNS.md  (§Shared Patterns "Not-found boundary")
@frontend/src/components/dossier/DossierShell.tsx

<interfaces>
Verified by research (re-read at execution):
- DossierShell.tsx:84 — useDossier(dossierId) → services/dossier-api.ts getDossier → dossiers-get
  fn, which returns 404 NOT_FOUND for an absent id (dossiers-get/index.ts:143-149);
  DossierAPIError carries .status; the global retry (lib/query-client.ts:29-40) short-circuits
  4xx → the 404 rejects in ONE round-trip (no retry storm, no skeleton wall).
- DossierShell has NO isError branch today — any failure renders titleless chrome. This edit is
  ALSO the D-05 error-state wiring for dossier detail.
- Component-throw is supported by TanStack Router v1.170.7 (thrown notFound() caught under
  default fuzzy mode by the root notFoundComponent, routes/__root.tsx:72). The shape is drafted
  in 93-RESEARCH.md §Code Examples.
- Do NOT restyle NotFoundPage's legacy classes (surgical rule); record-specific copy is permitted
  but not required — the bar is: absent ID → the root 404 page, never the error state.
- Seven consumers: routes/_protected/dossiers/{countries,organizations,forums,persons,
  elected-officials,topics,working_groups}/$id.tsx all mount DossierShell — zero route-file edits.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DossierShell — discriminate 404 from rejection</name>
  <files>frontend/src/components/dossier/DossierShell.tsx</files>
  <read_first>DossierShell.tsx in full (the :84 query and everything that renders below it); 93-RESEARCH.md §Code Examples "Component-throw notFound" (the exact shape); frontend/src/services/dossier-api.ts DossierAPIError class (its .status field)</read_first>
  <action>
    Destructure isError/error/refetch from useDossier. Before the main render: if isError and the
    error is a DossierAPIError with status 404, `throw notFound()` (import from
    @tanstack/react-router — the codebase's first thrower; the root boundary catches). Any other
    isError renders QueryErrorState variant 'page' (the shell's primary query owns the page) with
    onRetry → refetch. Loading keeps the existing skeleton, bounded per UI-SPEC (skeleton only
    while isLoading; the 404 rejects in one round-trip so the not-found page appears in
    single-request time). Happy path byte-identical across all seven dossier types.
  </action>
  <verify>
    <automated>cd frontend && F=src/components/dossier/DossierShell.tsx && test -f "$F" && grep -q 'notFound' "$F" && grep -q 'QueryErrorState' "$F" && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>The shell throws notFound() on status-404 rejections and renders the shared error state on every other rejection; no code path renders the chrome shell without identity while isError; type-check clean. The two states share no component (D-05).</acceptance_criteria>
  <done>Seven dossier detail routes tell absence from failure with one edit.</done>
</task>

<task type="auto">
  <name>Task 2: Two-direction spec (absent → 404; forced rejection → error state)</name>
  <files>tests/e2e/93-dossier-notfound.spec.ts</files>
  <read_first>tests/e2e/92-delegations-error.spec.ts (template — inline auth, budgets); routes/__root.tsx:12-27 (the 404 page's DOM: the oversized 404 numeral is the marker)</read_first>
  <action>
    TWO tests, inline auth, --no-deps, DOM-only:
    1. Absent ID: visit /dossiers/countries/<crypto.randomUUID()> — assert the root 404 page
       renders (the 404 numeral + "Page not found" heading) within single-request time (use the
       15s budget but assert no skeleton region remains once the 404 shows), and assert the
       error-state testid and "Check your connection"-style copy are ABSENT.
    2. Forced rejection on a REAL id: fetch one existing dossier id in beforeAll via a
       service-role query (SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY from .env.test; never echo
       either), CDP-block `*dossiers-get*`, visit that dossier's detail — assert
       [data-testid="query-error-state"] visible and the 404 numeral ABSENT.
    The two absent/present assertions in each test are the D-05 discriminator made mechanical.
  </action>
  <verify>
    <automated>OUT=$(pnpm exec playwright test tests/e2e/93-dossier-notfound.spec.ts --project=chromium-en --no-deps 2>&1) && echo "$OUT" | grep -qE '\b2 passed'</automated>
  </verify>
  <acceptance_criteria>2 passed under --no-deps (threshold 2 = the two tests above). Each test asserts BOTH the expected state's presence AND the wrong state's absence — collapsing the two states cannot pass.</acceptance_criteria>
  <done>The absence/failure distinction is proven rendered, both directions.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                          |
| ----------------------------- | ------------------------------------ |
| 404 vs 5xx rejection → render | the D-05 discrimination happens here |

## STRIDE Threat Register

| Threat ID | Category               | Component        | Disposition | Mitigation Plan                                                                         |
| --------- | ---------------------- | ---------------- | ----------- | --------------------------------------------------------------------------------------- |
| T-93-22   | Information Disclosure | DossierShell     | mitigate    | error object never rendered; QueryErrorState i18n only; spec regex via 93-15's full run |
| T-93-23   | Repudiation            | DossierShell     | mitigate    | titleless-chrome-on-failure path removed; every failure has a named render              |
| T-93-SC   | Tampering              | package installs | accept      | zero installs                                                                           |

</threat_model>

<verification>
- Spec 2/2 --no-deps; source gate; type-check.
- Engagement detail's notFound is deliberately NOT here — it is 93-12's, ordered after the server
  learns to distinguish absent-ID from missing-extension (D-06a).
</verification>

<success_criteria>
Criterion 3's dossier-detail third holds: absent renders absent, failure renders failure, on all
seven layouts, with the codebase's first notFound() throw established as the pattern.
</success_criteria>

<output>
Create `.planning/phases/93-failure-visibility/93-11-SUMMARY.md` when done.
</output>
