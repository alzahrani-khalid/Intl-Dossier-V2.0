---
phase: 93-failure-visibility
plan: 12
type: execute
wave: 2
depends_on: [93-01]
files_modified:
  - supabase/functions/engagement-dossiers/index.ts
  - frontend/src/components/workspace/WorkspaceShell.tsx
  - tests/e2e/93-degraded-engagement.spec.ts
autonomous: true
requirements: [TRUST-03, TRUST-04]
truths:
  - "Decisions covered — D-06a: the get_engagement_full server change (TRUST-04) is the PRODUCER and engagement-detail not-found (TRUST-03) is its CONSUMER — today the fn collapses a missing extension row into the same 404 as an absent ID, so the server task lands FIRST in this plan and the client task consumes its new contract; D-07: the degraded state is NAMED — the record's identity plus an explicit incomplete-record callout, never chrome with an empty title; D-19: producer-before-consumer ordering with the cross-artifact contract positive-controlled inside one plan"
must_haves:
  truths:
    - 'engagement-dossiers returns 200 + { engagement: null, dossier: {identity} } for an engagement dossier missing its extension row, and keeps 404 for a truly absent id — the two cases are distinguishable at the client for the first time'
    - 'WorkspaceShell renders identity + a role="status" warn callout (Incomplete record) for the degraded case, throws notFound() for a true 404, and renders QueryErrorState for other rejections — three distinct states, no titleless chrome'
  artifacts:
    - path: 'supabase/functions/engagement-dossiers/index.ts'
      provides: 'the degraded-200 contract (follow-up dossiers query on missing extension)'
      contains: 'engagement: null'
    - path: 'tests/e2e/93-degraded-engagement.spec.ts'
      provides: 'service-role fixture (engagement dossier without extension) proving the degraded render; random-UUID 404; forced-rejection error state'
      contains: 'role="status"'
  key_links:
    - from: 'frontend/src/components/workspace/WorkspaceShell.tsx'
      to: 'supabase/functions/engagement-dossiers/index.ts'
      via: "the degraded-200 body shape — produced by Task 1, consumed by Task 2, exercised end-to-end by Task 3's fixture"
      pattern: 'engagement'
---

<objective>
Land TRUST-04 and engagement detail's TRUST-03 in dependency order (D-06a): first teach
engagement-dossiers to return 200 + base-dossier identity when only the extension row is missing
(keeping 404 for true absence), then teach WorkspaceShell to render the three now-distinguishable
states — degraded (named callout), not-found (root 404), and error (shared state).

Purpose: until the server distinguishes them, criteria 3 and 4 are indistinguishable at the client
and D-05's whole distinction collapses on this route.
Output: deployed fn with the new contract; three-state WorkspaceShell; fixture-driven spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-failure-visibility/93-RESEARCH.md  (§Criterion 4 — detection mechanism + repair shape; §Criterion 3 engagement row)
@.planning/phases/93-failure-visibility/93-UI-SPEC.md  (§3 Degraded state — the visual contract: Alert DEFAULT variant, AlertTriangle text-warn, role="status")
@supabase/functions/engagement-dossiers/index.ts
@frontend/src/components/workspace/WorkspaceShell.tsx

<interfaces>
Verified by research (re-read at execution):
- engagement_dossiers.id is a 1:1 extension keyed by the dossier id (migration 20260110000006:18-19).
  Degraded condition: a dossiers row with type='engagement' and no engagement_dossiers row.
  Staging likely holds ~2 such rows today (COUNT-02: hub 5 vs list 3) — but the spec constructs
  its own fixture regardless (C1 done-state).
- The collapse: get_engagement_full selects from engagement_dossiers by id (20260110000006:373-382);
  missing extension → data.engagement null → the fn returns the same 404 as a truly absent id
  (engagement-dossiers/index.ts:400-401).
- Repair shape (research §Criterion 4): in the GET-single branch, when !data?.engagement, run ONE
  follow-up query on dossiers (id = :id AND type = 'engagement'): row exists → 200 with
  { engagement: null, dossier: { id, name_en, name_ar, type, status } }; no row → keep the 404.
  Client-side double-fetch was considered and rejected (two round-trips per miss; logic spreads).
- WorkspaceShell.tsx:44 — useEngagement(engagementId) → apiGet('/engagement-dossiers/${id}').
  Today's degraded render: displayName falls to '' (:56-59) under an <h1> that renders regardless —
  the titleless chrome shell.
- UI contract (UI-SPEC §3): identity first; Alert DEFAULT variant with AlertTriangle overridden to
  text-warn (warn, not danger — nothing failed); title common:errors.incompleteRecord.title, body
  .description; wrapper role="status"; dependent sections SUPPRESSED (not rendered as empty states
  with create CTAs — a second confident lie); no retry, no accent, no new Alert variant.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server — the degraded-200 contract (PRODUCER)</name>
  <files>supabase/functions/engagement-dossiers/index.ts</files>
  <read_first>engagement-dossiers/index.ts GET-single branch in full (:380-410 region); supabase/migrations/20260110000006 get_engagement_full definition (:373-382)</read_first>
  <action>
    In the GET-single branch: when the RPC yields no engagement, add ONE follow-up query on
    `dossiers` filtered `id = :id AND type = 'engagement'` (same client — JWT-scoped, RLS
    applies). Row found → return 200 with `{ engagement: null, dossier: { id, name_en, name_ar,
    type, status } }` (identity from the base row). No row → keep the existing 404 byte-identical
    (absent-ID semantics for the criterion-3 seam). Any query error on the follow-up returns the
    bilingual envelope (93-02's shape), never a PostgREST passthrough. Deploy:
    `supabase functions deploy engagement-dossiers --project-ref zkrcjzdemdmwhearhfgg`; record
    exit + updated_at in the SUMMARY (no shared ledger).
  </action>
  <verify>
    <automated>F=supabase/functions/engagement-dossiers/index.ts && test -f "$F" && grep -q 'engagement: null' "$F" && grep -qE "type.{0,20}engagement" "$F"</automated>
  </verify>
  <acceptance_criteria>The degraded branch exists in source and is deployed; 404 for true absence is unchanged. The full behavioural proof (200-degraded vs 404-absent) is Task 3's fixture spec — the intra-plan consumer of this contract.</acceptance_criteria>
  <done>The server distinguishes the two cases the client could never tell apart.</done>
</task>

<task type="auto">
  <name>Task 2: WorkspaceShell — three distinct states (CONSUMER)</name>
  <files>frontend/src/components/workspace/WorkspaceShell.tsx</files>
  <read_first>WorkspaceShell.tsx in full (:44 query, :56-59 displayName fall-through, the section renders below); 93-UI-SPEC.md §3 (the degraded contract, binding); frontend/src/components/ui/alert.tsx (default variant to compose); frontend/src/components/error-states/QueryErrorState.tsx</read_first>
  <action>
    Three branches, in this order:
    1. Degraded (200, engagement null, dossier present): render the page header with the base
       row's identity (name_en/name_ar per locale), then the callout: Alert DEFAULT variant
       (border-line, bg-surface) with AlertTriangle icon overridden to text-warn, wrapper
       role="status", title t('common:errors.incompleteRecord.title'), body
       t('common:errors.incompleteRecord.description'); dependent sections (tabs/regions needing
       extension data) are SUPPRESSED, not rendered as empty states. No retry, no accent, no new
       Alert variant.
    2. Not-found (rejection with status 404): throw notFound() → root 404 page.
    3. Other rejections: QueryErrorState variant 'page', onRetry → refetch; cap retries at 2 if
       the query is invoke/apiGet-without-status on this path.
    The titleless-<h1> fall-through is unreachable in all three. Happy path byte-identical.
  </action>
  <verify>
    <automated>cd frontend && F=src/components/workspace/WorkspaceShell.tsx && test -f "$F" && grep -q 'notFound' "$F" && grep -q 'incompleteRecord' "$F" && grep -q 'role="status"' "$F" && grep -q 'QueryErrorState' "$F" && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>All three renders present and mutually exclusive; the degraded state carries identity + role="status" + warn (never danger, never retry); no code path renders an empty-title header; type-check clean.</acceptance_criteria>
  <done>An incomplete record is named as incomplete, an absent one as absent, a failure as failure.</done>
</task>

<task type="auto">
  <name>Task 3: Fixture-driven spec — all three states end-to-end</name>
  <files>tests/e2e/93-degraded-engagement.spec.ts</files>
  <read_first>tests/e2e/92-delegations-error.spec.ts (template); .claude/skills/supabase-migration-safety/SKILL.md §Verify CHECK/enum constraints (derive dossiers'' required columns before writing the fixture INSERT)</read_first>
  <action>
    THREE tests, inline auth, --no-deps, 15s budgets, DOM-only:
    1. Degraded: beforeAll inserts a fixture via service-role REST (SUPABASE_URL +
       SUPABASE_SERVICE_ROLE_KEY from .env.test; never echo either): one dossiers row with
       type='engagement', bilingual names, and whatever NOT NULL/CHECK columns the schema requires
       (derive from the dossiers migrations at authoring time — do not guess enum values), and NO
       engagement_dossiers row. Visit /engagements/<fixture-id> — assert the fixture's name
       renders in the header, [role="status"] callout visible with the incomplete-record heading,
       and NO empty-title h1, NO query-error-state, NO 404 numeral. afterAll deletes the fixture
       row (teardown always runs).
    2. Absent: visit /engagements/<crypto.randomUUID()> — assert the root 404 page, and the
       degraded callout + error state ABSENT.
    3. Forced rejection: CDP-block `*engagement-dossiers*` on the fixture id (insert before,
       teardown after) — assert query-error-state visible, 404 numeral and role="status" callout
       ABSENT.
  </action>
  <verify>
    <automated>OUT=$(pnpm exec playwright test tests/e2e/93-degraded-engagement.spec.ts --project=chromium-en --no-deps 2>&1) && echo "$OUT" | grep -qE '\b3 passed'</automated>
  </verify>
  <acceptance_criteria>3 passed under --no-deps (threshold 3 = the three states, one test each). Each test asserts its state's presence AND the other two states' absence — the three-way discrimination is mechanical. The fixture insert/teardown leaves staging count-identical (verified by the teardown's delete response in the spec).</acceptance_criteria>
  <done>The producer-consumer contract is proven live, end-to-end, in one plan.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                            |
| ----------------------------------- | ------------------------------------------------------ |
| fn response shape → shell render    | the absent/degraded/failed discrimination crosses here |
| spec service-role fixture → staging | test data with RLS bypassed (insert/teardown only)     |

## STRIDE Threat Register

| Threat ID | Category               | Component             | Disposition | Mitigation Plan                                                                    |
| --------- | ---------------------- | --------------------- | ----------- | ---------------------------------------------------------------------------------- |
| T-93-24   | Information Disclosure | engagement-dossiers   | mitigate    | degraded body carries identity fields only; errors use the bilingual envelope      |
| T-93-25   | Repudiation            | WorkspaceShell        | mitigate    | titleless chrome unreachable; three named states, each asserted present AND absent |
| T-93-26   | Information Disclosure | spec service-role key | mitigate    | key read from env, never echoed, never in argv; fixture deleted in teardown        |
| T-93-SC   | Tampering              | package installs      | accept      | zero installs                                                                      |

</threat_model>

<verification>
- Spec 3/3 --no-deps (the behavioural close for criteria 3-engagement and 4); source gates;
  type-check; deploy evidence in SUMMARY.
- D-06a honored structurally: Task 1 (producer) precedes Task 2 (consumer) inside one plan — no
  cross-plan C9a exposure for this contract.
</verification>

<success_criteria>
An engagement dossier missing its extension row renders a named, degraded state with its identity;
a truly absent id renders the 404 page; a failure renders the error state — three states, proven
live with a constructed fixture.
</success_criteria>

<output>
Create `.planning/phases/93-failure-visibility/93-12-SUMMARY.md` when done.
</output>
