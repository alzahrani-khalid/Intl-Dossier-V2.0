---
phase: 37-signature-visuals
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/pnpm-lock.yaml
  - frontend/.size-limit.json
  - frontend/vitest.config.ts
  - frontend/playwright.config.ts
  - frontend/src/components/signature-visuals/index.ts
  - frontend/src/components/signature-visuals/flags/index.ts
  - frontend/src/components/signature-visuals/__tests__/.gitkeep
  - frontend/e2e/signature-visuals/.gitkeep
autonomous: true
requirements: [VIZ-01, VIZ-02, VIZ-03, VIZ-04, VIZ-05]
tags: [frontend, infra, bundle-size]
must_haves:
  truths:
    - "Developer can run `pnpm --filter frontend ls d3-geo topojson-client world-atlas` and see all three installed with pinned versions"
    - "Developer can run `pnpm --filter frontend size` and see the signature-visuals/d3 lazy chunk budget enforced"
    - "Folder `frontend/src/components/signature-visuals/` exists with barrel + flags subfolder"
  artifacts:
    - path: "frontend/package.json"
      provides: "pinned d3-geo, topojson-client, world-atlas, @types/d3-geo, @types/topojson-client dependencies"
      contains: "d3-geo"
    - path: "frontend/.size-limit.json"
      provides: "bundle-size budget for the lazy d3 chunk"
      contains: "signature-visuals"
    - path: "frontend/src/components/signature-visuals/index.ts"
      provides: "empty placeholder barrel — subsequent plans append exports"
    - path: "frontend/src/components/signature-visuals/flags/index.ts"
      provides: "placeholder barrel — plan 05 populates with 24 flag components"
  key_links:
    - from: "frontend/package.json"
      to: "pnpm-lock.yaml"
      via: "pnpm install"
      pattern: "d3-geo"
    - from: "frontend/.size-limit.json"
      to: "CI bundle-size check"
      via: "pnpm --filter frontend size script"
      pattern: "size-limit"
---

<objective>
Wave 0 infra — add the three missing runtime dependencies, scaffold the signature-visuals folder tree, and wire up the size-limit bundle budget. This plan does NOT write any component code; it only prepares the ground so Wave 1 can start with zero surprises.

Purpose: Every downstream plan assumes d3-geo / topojson-client / world-atlas are installed and that barrel files exist to append exports into. Without this plan, Plan 02 crashes at `import('d3-geo')`.
Output: Installed deps, populated lockfile, scaffolded folders, empty barrels, size-limit config, confirmed test infra.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-signature-visuals/37-CONTEXT.md
@.planning/phases/37-signature-visuals/37-RESEARCH.md
@.planning/phases/37-signature-visuals/37-VALIDATION.md
@.planning/phases/37-signature-visuals/37-PATTERNS.md
@CLAUDE.md

<interfaces>
No new TS interfaces in this plan. The scaffolded barrels start empty and are appended to by later plans.

Existing relevant interfaces:
```ts
// frontend/src/types/dossier-context.types.ts (line ~71)
export type DossierType =
  | 'country' | 'organization' | 'forum' | 'engagement'
  | 'topic' | 'working_group' | 'person' | 'elected_official'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 00-1: Verify latest semver and install pinned d3-geo + topojson-client + world-atlas + types</name>
  <files>frontend/package.json, frontend/pnpm-lock.yaml</files>
  <read_first>
    - frontend/package.json (current dependencies block — confirm d3-geo NOT present)
    - .planning/phases/37-signature-visuals/37-RESEARCH.md §"Standard Stack" (lines 90-123) — version pins and install command
    - .planning/phases/37-signature-visuals/37-RESEARCH.md §"Assumptions Log" A1 — the versions below are ASSUMED and must be re-verified via `npm view`
  </read_first>
  <action>
Per D-05 + D-06 + T-37-03 (supply-chain mitigation), install runtime d3 stack pinned to exact versions. NO caret range (`^`) — exact pins only to satisfy T-37-03.

Step 1 — Verify current semver on npm (single run, capture output):
```bash
npm view d3-geo version
npm view topojson-client version
npm view world-atlas version
npm view @types/d3-geo version
npm view @types/topojson-client version
```
Record the output in the task summary. Expected RESEARCH.md baselines: `d3-geo@3.1.1`, `topojson-client@3.1.0`, `world-atlas@2.0.2`. If npm returns a NEWER patch/minor, use the newer version. If npm returns a NEWER major, STOP and flag to user — d3-geo v4 / topojson-client v4 are not covered by this research.

Step 2 — Install with EXACT semver (no `^`, no `~`):
```bash
pnpm --filter frontend add -E d3-geo@<exact-version> topojson-client@<exact-version> world-atlas@<exact-version>
pnpm --filter frontend add -DE @types/d3-geo@<exact-version> @types/topojson-client@<exact-version>
```
The `-E` / `--save-exact` flag strips the caret. Verify afterwards that `package.json` shows `"d3-geo": "3.1.1"` (no `^`), not `"^3.1.1"`.

Step 3 — Run `pnpm install` at the monorepo root to update `pnpm-lock.yaml`.

Do NOT vendor `countries-110m.json` into the repo — D-06 forbids it.
Do NOT add `d3` (full bundle) — D-05 locks submodule-only to keep the chunk ≤ 45KB.
Do NOT upgrade `framer-motion` — D-13 bans it for this phase.
  </action>
  <verify>
    <automated>pnpm --filter frontend ls d3-geo topojson-client world-atlas @types/d3-geo @types/topojson-client</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '"d3-geo":\s*"[0-9]' frontend/package.json` returns 1 line (exact version, NO `^`)
    - `grep -E '"topojson-client":\s*"[0-9]' frontend/package.json` returns 1 line (exact version, NO `^`)
    - `grep -E '"world-atlas":\s*"[0-9]' frontend/package.json` returns 1 line (exact version, NO `^`)
    - `grep -E '"@types/d3-geo":\s*"[0-9]' frontend/package.json` returns 1 line (exact version, NO `^`)
    - `grep -E '"@types/topojson-client":\s*"[0-9]' frontend/package.json` returns 1 line (exact version, NO `^`)
    - `pnpm --filter frontend ls d3-geo topojson-client world-atlas` exits 0 and shows all three present
    - `pnpm-lock.yaml` contains `d3-geo`, `topojson-client`, `world-atlas` entries (grep-verifiable)
    - `grep -c '"d3":' frontend/package.json` returns 0 (we did NOT install the full d3 bundle)
  </acceptance_criteria>
  <done>All five packages installed with exact (non-caret) semver; pnpm-lock.yaml updated; `pnpm --filter frontend typecheck` still passes (no stray import errors).</done>
</task>

<task type="auto">
  <name>Task 00-2: Scaffold signature-visuals folders + empty barrels + test/e2e directories</name>
  <files>frontend/src/components/signature-visuals/index.ts, frontend/src/components/signature-visuals/flags/index.ts, frontend/src/components/signature-visuals/__tests__/.gitkeep, frontend/e2e/signature-visuals/.gitkeep</files>
  <read_first>
    - frontend/src/design-system/hooks/index.ts (barrel analog — one-line named re-export pattern)
    - .planning/phases/37-signature-visuals/37-PATTERNS.md §"Pattern Assignments" for `index.ts` barrel pattern
    - frontend/playwright.config.ts (to confirm testDir — RESEARCH Open Question O-8 notes uncertainty between `frontend/e2e/` vs `frontend/tests/e2e/`)
  </read_first>
  <action>
Per D-02, create the signature-visuals folder tree:

1. `mkdir -p frontend/src/components/signature-visuals/flags`
2. `mkdir -p frontend/src/components/signature-visuals/__tests__`

Create `frontend/src/components/signature-visuals/index.ts` with a JSDoc header and NO exports yet (plans 01–07 will append):
```ts
/**
 * Phase 37 — Signature Visuals barrel.
 * Primitives: GlobeLoader, FullscreenLoader, GlobeSpinner, DossierGlyph, Sparkline, Donut.
 * Consumers: `import { DossierGlyph } from '@/components/signature-visuals'`
 * See .planning/phases/37-signature-visuals/ for context.
 */
export {}
```

Create `frontend/src/components/signature-visuals/flags/index.ts` identically (plan 05 will populate with 24 flag re-exports + `FlagKey` union + `flags` map per PATTERNS.md lines 120-140):
```ts
/**
 * Phase 37 — 24 country flag SVGs barrel (verbatim port from handoff glyph.jsx).
 * ISO set: SA, AE, ID, EG, QA, JO, BH, OM, KW, PK, MA, TR, CN, IT, FR, DE, GB, US, JP, KR, IN, BR, EU, UN.
 * Populated by plan 05.
 */
export {}
```

Add `.gitkeep` files to `frontend/src/components/signature-visuals/__tests__/` and `frontend/e2e/signature-visuals/`.

Open `frontend/playwright.config.ts` and resolve Open Question O-8: which directory Playwright scans. If it scans `frontend/tests/e2e/` (Phase 36 convention), ALSO create `mkdir -p frontend/tests/e2e/signature-visuals/` and put the `.gitkeep` there instead. Only ONE e2e directory should exist — pick whichever matches the config.

Do NOT export from the barrels yet — later plans append. An empty `export {}` keeps the file as an ES module.
  </action>
  <verify>
    <automated>pnpm --filter frontend typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/components/signature-visuals/index.ts` exists (file read returns > 0 bytes)
    - File `frontend/src/components/signature-visuals/flags/index.ts` exists
    - Directory `frontend/src/components/signature-visuals/__tests__/` exists (ls non-empty)
    - Directory `frontend/e2e/signature-visuals/` OR `frontend/tests/e2e/signature-visuals/` exists (whichever matches Playwright config)
    - `grep -c 'export' frontend/src/components/signature-visuals/index.ts` returns ≥ 1 (at minimum `export {}`)
    - `grep -c 'export' frontend/src/components/signature-visuals/flags/index.ts` returns ≥ 1
    - `pnpm --filter frontend typecheck` exits 0 (no broken imports introduced)
  </acceptance_criteria>
  <done>Folder tree exists; empty barrels are ES modules; typecheck passes; placeholder directories visible in `git status`.</done>
</task>

<task type="auto">
  <name>Task 00-3: Add .size-limit.json bundle budget + confirm vitest/playwright configs support signature-visuals</name>
  <files>frontend/.size-limit.json, frontend/package.json (scripts block), frontend/vitest.config.ts, frontend/playwright.config.ts</files>
  <read_first>
    - frontend/package.json (scripts block — confirm no existing `size` script)
    - frontend/vitest.config.ts (confirm `environment: 'jsdom'` + `@testing-library/jest-dom` setup)
    - frontend/playwright.config.ts (confirm `testDir`, axe-core integration, RTL locale support)
    - .planning/phases/37-signature-visuals/37-RESEARCH.md §"Assumptions Log" A9 — d3-geo ~45KB + topojson-client ~14KB + world-atlas ~35KB gzipped ≈ 95KB → budget 100KB
  </read_first>
  <action>
Create `frontend/.size-limit.json` per RESEARCH.md A9 budget (~95KB gzip for the d3 lazy chunk, 5KB headroom → 100KB ceiling). Per NEW-PATTERN note in PATTERNS.md line 522, no codebase analog exists — this is the canonical config shape:

```json
[
  {
    "name": "signature-visuals/d3-lazy-chunk",
    "path": "frontend/dist/assets/*d3-geo*.js",
    "limit": "100 kB",
    "gzip": true
  },
  {
    "name": "signature-visuals/static-primitives",
    "path": "frontend/dist/assets/*signature-visuals*.js",
    "limit": "15 kB",
    "gzip": true,
    "ignore": ["d3-geo", "topojson-client", "world-atlas"]
  }
]
```

Add `size-limit` + `@size-limit/file` dev deps and a `size` script:
```bash
pnpm --filter frontend add -D size-limit @size-limit/file
```

In `frontend/package.json` scripts block, add:
```json
"size": "size-limit"
```

Do NOT add `@size-limit/preset-app` (ships puppeteer, 200MB+ install) — D-07 / A9 need only the file preset.

Confirm `frontend/vitest.config.ts` has:
- `environment: 'jsdom'` (required for matchMedia mocks)
- A setup file that imports `@testing-library/jest-dom` (for `.toBeInTheDocument()` etc.)
If either is missing, add them. Do NOT change other vitest settings.

Confirm `frontend/playwright.config.ts` has:
- A `testDir` that includes signature-visuals specs (either `./e2e` or `./tests/e2e` — match whichever dir Task 00-2 created)
- Either `@axe-core/playwright` is importable from existing specs OR no axe helper exists (plan 07 will wire it up)
- An RTL project or locale fixture if the config supports multi-locale test runs (Phase 36 likely added one — confirm)

Document findings in a short comment in the task commit message; no code changes in vitest/playwright configs unless a required setting is genuinely missing.
  </action>
  <verify>
    <automated>pnpm --filter frontend build && pnpm --filter frontend size</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/.size-limit.json` exists and is valid JSON (`node -e "JSON.parse(require('fs').readFileSync('frontend/.size-limit.json'))"` exits 0)
    - `grep -c '"limit"' frontend/.size-limit.json` returns ≥ 2 (d3 chunk + static primitives entries)
    - `grep -c '"gzip": true' frontend/.size-limit.json` returns ≥ 2
    - `grep '"size":' frontend/package.json` returns at least one match (script added)
    - `pnpm --filter frontend ls size-limit` exits 0
    - `pnpm --filter frontend ls @size-limit/file` exits 0
    - `grep -c 'jsdom' frontend/vitest.config.ts` returns ≥ 1
    - `grep -c 'testDir' frontend/playwright.config.ts` returns ≥ 1
  </acceptance_criteria>
  <done>`.size-limit.json` is valid; `pnpm size` script is registered; after next successful build, `pnpm --filter frontend size` runs without config errors (the chunk won't exist yet — that's expected until plan 02 ships GlobeLoader and build splits it).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Package maintainer compromise could inject malicious code (T-37-03) |
| build output (`dist/assets/*`) → CDN | Bundle contents served to browsers; budget enforcement is a defense signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03 | Tampering | d3-geo / topojson-client / world-atlas installs | mitigate | Exact semver pins (no `^`) in package.json (Task 00-1 acceptance criteria); pnpm-lock.yaml committed; maintainers are Mike Bostock (`d3-geo`, `world-atlas`) and topojson org — well-known. Future CI audit for integrity hashes deferred to a later phase, noted in CONTEXT `deferred`. |

</threat_model>

<verification>
- `pnpm --filter frontend typecheck` — no broken imports after scaffold
- `pnpm --filter frontend ls d3-geo topojson-client world-atlas` — all three present
- `pnpm --filter frontend size` — config parses (budget not enforced until first component ships)
</verification>

<success_criteria>
- All three runtime deps + two type packages installed with exact (non-caret) semver
- Empty folder tree exists at `frontend/src/components/signature-visuals/{,flags,__tests__}` + matching e2e dir
- `.size-limit.json` exists with two budget entries (d3 chunk + static primitives) and `gzip: true`
- `size` script registered in `frontend/package.json`
- Test infra confirmed: vitest jsdom + playwright testDir
- Downstream plans can safely assume these deps + folders exist
</success_criteria>

<output>
After completion, create `.planning/phases/37-signature-visuals/37-00-SUMMARY.md` recording:
- Exact versions installed (from `npm view` output)
- Which e2e directory was chosen (`frontend/e2e/` vs `frontend/tests/e2e/`) and why
- Whether vitest/playwright configs needed edits
- Total install size delta (bytes added to node_modules)
</output>
</content>
