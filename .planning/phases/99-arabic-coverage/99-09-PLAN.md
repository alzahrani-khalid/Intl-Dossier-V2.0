---
phase: 99-arabic-coverage
plan: 09
type: execute
wave: 3
depends_on: [99-06, 99-07, 99-08]
files_modified:
  - 'frontend/src/{i18n/en/intake.json,i18n/ar/intake.json,i18n/en/work-creation.json,i18n/ar/work-creation.json,components/triage-panel/TriagePanel.tsx,pages/TicketDetail.tsx,pages/IntakeQueue.tsx,components/duplicate-comparison/DuplicateComparison.tsx,components/work-creation/forms/IntakeQuickForm.tsx}'
  - .planning/phases/99-arabic-coverage/99-09-SUMMARY.md
autonomous: true
requirements: [AR-03, AR-04a, AR-04b]
success_criterion: 'Land the dynamic carriers and the rendered intake surface for the intake/triage lane, proven by the acceptance items below in a single pass.'
routing:
  floor: frontier
truths:
  - "Decisions covered — D-24 steps 1-2 only: this lane AUTHORS keys in en AND ar and VERIFIES resolution; it drops NO second argument (deletion is the mask lanes, behind this plan in the DAG); D-25: every authored key is authored twice — there is no EN-exists/AR-missing class, en values byte-identical to the existing second-argument literal so the en render is pixel-unchanged; D-26: the placement mechanism is colon-prefixing repaired sites to the feature namespace (intake:/work-creation:) with the hook left untouched, so no unrepaired key in the same file shifts resolution; D-27: the two dynamic-prefix members named by the instrument of record (TriagePanel requestType.options, TicketDetail ticketDetail.tabs) are repaired by enum-key authoring and verified through partA_maskfinder, because both acceptance greps are blind to them BY CONSTRUCTION; D-23: every touched binding lands in explicit colon form; D-32: the intake queue header is one of the two MASK members of the four named criterion-3 surfaces — repaired here by authoring + repoint, not by extraction; D-29: UI99-C6 is the binding UI-contract row this lane closes, and it closes on the RENDERED test, not on a source grep — the `UI99-C6 ar intake queue` test in 99-ar03-leak.spec.ts is invoked by this plan's own command oracle; D-07: a criterion member closes on a rendered surface or a drilled instrument, never on source text alone."
must_haves:
  truths:
    - 'The dynamic-prefix sites are repaired as atomic units: the enum keys for every value the prefix can take are authored in both locales in the SAME commit as the site rewrite, the prefix is colon-prefixed to its namespace, and the variable second argument is dropped ONLY at those dynamic sites'
    - "Dot-form common.* sites inside these five files are NOT this lane's business: the flatten plan rewrote every one whose key resolves at the flattened root, and the rest are the common-owner lane's. This lane neither rewrites nor authors a common key."
    - 'ar values follow the voice law (sentence case, no exclamation, no first-person plural) and the ruled glossary terms; the operator naturalness review stands over all ar copy as recorded debt, not a blocker'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; python3 "$R/scripts/partA_maskfinder.py" "$R" --control >/dev/null || exit 1; OUT="$(python3 "$R/scripts/partA_maskfinder.py" "$R")" || exit 1; printf "%s\n" "$OUT" | command grep -q "UNRESOLVED dynamic t() key prefixes:" || exit 1; printf "%s\n" "$OUT" | command grep -qE "TriagePanel\.tsx|TicketDetail\.tsx" && exit 1; test "$(command grep -c "t(''intake:" "$R/frontend/src/pages/IntakeQueue.tsx")" -ge 3'
      text: "the instrument of record no longer lists this lane's two dynamic-prefix members (control run first), and IntakeQueue carries at least three explicit intake: colon keys — RED at HEAD on both clauses"
---

<objective>
Land the dynamic carriers and the rendered intake surface for the intake/triage lane, proven by the acceptance items below in a single pass.

This task is part 2 of 2 of the intake/triage lane; the parts are ordered by `depends_on` and share one
file surface, so each part reads the tree the part before it produced. The task-unit contract caps a
task at six acceptance items and twenty-four surface, which is why the lane is cut this way.
</objective>

<context>
@.planning/phases/99-arabic-coverage/99-CONTEXT.md
@.planning/phases/99-arabic-coverage/99-RESEARCH.md
@scripts/partA_maskfinder.py
@frontend/src/pages/IntakeQueue.tsx
@frontend/src/components/triage-panel/TriagePanel.tsx
@tests/e2e/99-ar03-leak.spec.ts

Ref notes: 99-CONTEXT.md — D-23..D-27, D-29, D-32; 99-RESEARCH.md — §1.4-§1.5 population and lanes; §3 the dynamic class; §6.1 row 2 the intake surface; partA_maskfinder.py — instrument of record for the dynamic class — run --control first; 99-ar03-leak.spec.ts — authored in 99-01; the UI99-C6 test is this lane's rendered oracle

<interfaces>
Verified 2026-08-18 at 9ffa2b80c (strings anchor, lines drift):

- IntakeQueue.tsx binds `['common','intake']`; its header sites read `t('intake.description', …)`,
  `t('intake.createNew', …)`, `t('intake.filters.pendingTriage', …)` — dot-form paths resolving in
  NEITHER namespace (common.json has no intake subtree; intake.json has no top-level `intake` key).
  English renders under dir="rtl" through the mask — the UI99-C6 defect mechanism.
- TriagePanel binds intake; TicketDetail's dynamic site is `ticketDetail.tabs` with ns intake
  (maskfinder names `pages/TicketDetail.tsx:189`); TriagePanel's dynamic site is
  `intake.form.requestType.options` (`TriagePanel.tsx:321`, RAW-KEY class — renders a dotted key).
- Re-derive the lane population at task start with the strict audit (--scope over the five files)
  and record it; plan-time numbers: TriagePanel 40, TicketDetail 23, IntakeQueue 17+1,
  DuplicateComparison 19, IntakeQuickForm 13 two-arg unresolved sites; intake-targeted distinct
  keys 86; work-creation-targeted 2.
- Dot-form `common.X` sites in these files were handled by the flatten plan, which owns every one
  whose key resolves at the flattened root; the residue belongs to the common-owner lane. This
  lane touches no `common.*` site — a change to one here is a scope defect.
- The en value rule: where a site carries a second-argument literal, the authored en value is
  BYTE-IDENTICAL to it (en render pixel-unchanged); ar is authored fresh under the glossary. Where
  a raw-key site has no default, author en from the surrounding surface copy and say so.
- The intake queue's ar TITLE VALUE rename to قائمة الاستقبال is the glossary lane's (D-16); this
  lane authors the header/description/button keys, not the page title value.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>the intake/triage lane — the dynamic carriers and the rendered intake surface</name>
  <files>the paths in files_modified</files>
  <read_first>the five source files in full; frontend/src/i18n/en/intake.json + ar pair (existing shape — author INTO the existing subtree structure, never a new top-level namespace-echo key); 99-RESEARCH.md §3</read_first>
  <action>
    Parts before this one landed in P99-08. Do only what THIS task's acceptance items still require, using the lane method below as the standing method; re-run this task's oracles against the tree the earlier part produced and repair any residue INSIDE files_modified. Author nothing outside them.

    --- THE LANE METHOD (carried verbatim from the pre-recut plan; the acceptance items above bound what THIS task owes) ---

    1. Run the strict audit scoped to the five files; record the starting population (sites,
       distinct, per class, per locale) and confirm its twoArgTotal is nonzero — a scope filter
       that matches nothing produces a vacuous zero.
    2. For every unresolved site: author the key in en AND ar (en byte-identical to the mask
       literal where one exists; ar per glossary + voice law), and rewrite the site's key to
       explicit colon form (intake: / work-creation:). KEEP the second argument byte-identical.
       Do not change any useTranslation call.
    3. The UI99-C6 trio: rewrite the three IntakeQueue header sites to intake: keys with authored
       values in both locales — the ar copy uses the استقبال family, NOT قائمة الانتظار.
    4. The two dynamic-prefix members: enumerate every runtime value each prefix can take, author
       the full enum key set in both locales, colon-prefix the dynamic key expression, and drop
       the VARIABLE second argument at those sites only.
    5. Verify: scoped strict audit zero; maskfinder no longer lists the two members; en/ar leaf
       parity for both authored namespaces; then run the rendered UI99-C6 test and record it.

  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes. The lane's own criteria, carried: The scoped strict audit reads zero on both classes in both locales with the starting population recorded beside it; every authored en leaf has its ar pair; the two maskfinder members are gone with the control run first; the `UI99-C6 ar intake queue` test collects at 1 and passes; no second argument outside the two dynamic sites changed; no JSON outside the four owned files changed; type-check green.</acceptance_criteria>
  <done>the dynamic carriers and the rendered intake surface is landed for the intake/triage lane, proven by this task's own oracles rather than by the lane's later tasks.</done>
</task>

</tasks>

<verification>
POPULATION: the paths in files_modified. OUTSIDE IT: every other task's files, the exogenous paths
(D-03), and every obligation this task's acceptance items do not name — those belong to the lane
parts before and after this one, named in `depends_on` and in the SUMMARY.

The lane's own population statement, carried verbatim from the pre-recut plan:
POPULATION: the ten listed files. OUTSIDE IT: common.json in either locale (the flatten plan and
the common-owner lane), the intake queue ar TITLE VALUE rename (the glossary lane, D-16), every
second-argument drop outside the two dynamic atomic units (the deletion lanes), and every other
UI99-C row.
</verification>

<success_criteria>
Land the dynamic carriers and the rendered intake surface for the intake/triage lane, proven by the acceptance items below in a single pass.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-09-SUMMARY.md` when done. It MUST record every command this task ran with its verbatim output, the
populations it re-derived with the commands that produced them, and anything left for a named later
task. The lane's own SUMMARY contract, carried verbatim from the pre-recut plan (satisfy the parts
this task owns): record in the file named under <output> (99-04-SUMMARY.md in the pre-recut plan) when done. It MUST record: the
starting vs ending scoped audit numbers; the enum value sets derived for the two dynamic prefixes;
the UI99-C6 rendered run output; and the statement that no second argument outside the dynamic
sites was dropped and no common.* site was touched.
</output>
