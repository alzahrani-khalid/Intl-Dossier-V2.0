---
phase: 99-arabic-coverage
plan: 30
type: execute
wave: 9
depends_on: [99-27, 99-28, 99-29]
files_modified:
  - .planning/phases/99-arabic-coverage/99-30-SUMMARY.md
autonomous: true
requirements: [AR-04a, AR-04b]
success_criterion: 'Land the repo-wide strict proof for the AR-04a gatekeeper, proven by the acceptance items below in a single pass.'
routing:
  floor: frontier
truths:
  - "Decisions covered — D-24: the conjunction order is a property of the DAG, not of a worker's intentions. This plan is a VERIFICATION-ONLY engine task that edits no source file at all, and every deletion lane `depends_on` it, so a worker structurally cannot delete a default before the repo-wide strict proof has passed its own gate battery. In the first draft the verify and the delete were two inner task blocks inside ONE compiled task, which means one gate battery running AFTER the whole diff — the order existed in prose and nowhere in the graph; D-27: the class both acceptance greps cannot see closes on partA_maskfinder reading zero unresolved prefixes with its control run first; D-28: AR-04a closes on the two-arg population and AR-04b on the raw-key population — no blended figure is cited anywhere in this plan or its SUMMARY; D-06: every zero is paired with a positive control in the same run; D-05: the populations are named with what falls outside them."
must_haves:
  truths:
    - 'Decisions covered — D-05, D-06, D-24, D-27, D-28: verification only, as its own DAG node, every zero controlled, the two populations kept separate. || This plan edits NOTHING. Its diff contains its own SUMMARY and no other file. A source or JSON change in this task means the gatekeeper became the thing it exists to gate.'
    - 'The strict audit, run UNSCOPED, reports ZERO unresolved two-arg AND ZERO unresolved raw-key sites in BOTH locales — and reports a NONZERO twoArgTotal, because a zero-unresolved reading from an instrument that walked nothing is the vacuous green this whole gate exists to refuse || partA_maskfinder reports zero unresolved dynamic prefixes with its --control run FIRST, and scripts/resolve-check.mjs reports zero routing misses with scripts/neg-taskcard.mjs still printing 3x MISS=true (text asserted — its exit code is 0 either way)'
    - "ANY nonzero STOPS the phase here: the residue is named site by site with the lane that owns it, this task stays RED, and nothing is deleted. Running AR-04a's check (a) on an unproven tree is destructively satisfiable — at plan time 473 sites referenced keys resolving in NEITHER locale, and deleting their defaults converts every one into a raw-key render in BOTH languages at once while the grep goes green. || The SUMMARY records the re-derived deletion population the lanes behind this one will consume, in BOTH of its classes and with their file counts, so no lane derives it privately: the two-arg literal class and the `t(key, { defaultValue })` options class are different populations of the same requirement and D-28 forbids blending them."
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node "$R/scripts/i18n-audit-strict.mjs" "$R" --json | node -e "let s=[];process.stdin.on(\"data\",d=>s.push(d)).on(\"end\",()=>{const j=JSON.parse(s.join(\"\"));if(!(j.twoArgTotal>0)){console.error(\"twoArgTotal is 0 — the instrument walked nothing and its zeros are vacuous\");process.exit(1)}if(j.twoArgUnresolved!==0||j.rawKeyUnresolved!==0||j.twoArgUnresolvedAr!==0||j.rawKeyUnresolvedAr!==0){console.error(\"unresolved sites survive\",JSON.stringify(j).slice(0,400));process.exit(1)}})" && python3 "$R/scripts/partA_maskfinder.py" "$R" --control >/dev/null && python3 "$R/scripts/partA_maskfinder.py" "$R" | command grep -q "UNRESOLVED dynamic t() key prefixes: 0" && node "$R/scripts/resolve-check.mjs" "$R" | command grep -q "routings with a miss: 0" && test "$(node "$R/scripts/neg-taskcard.mjs" "$R" | command grep -c "MISS=true")" -eq 3'
      text: 'repo-wide strict zero in BOTH locales and BOTH classes with a nonzero mask total proving the walk happened, the class-blind instrument at zero with its control run first, the resolution harness green, and the phase negative control still discriminating — RED at HEAD on every leading clause'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; test -f "$R/.planning/phases/99-arabic-coverage/99-16-SUMMARY.md" && command grep -q "twoArgTotal" "$R/.planning/phases/99-arabic-coverage/99-16-SUMMARY.md" && command grep -q "defaultValue" "$R/.planning/phases/99-arabic-coverage/99-16-SUMMARY.md" && test "$(cd "$R" && git diff --name-only HEAD -- frontend backend supabase tests scripts | wc -l | tr -d " ")" -eq 0'
      text: 'the gatekeeper recorded BOTH deletion populations by name in its SUMMARY and changed no file under frontend/backend/supabase/tests/scripts — the second clause is what makes "verification only" a checked property rather than a promise — RED at HEAD (the SUMMARY does not exist)'
---

<objective>
Land the repo-wide strict proof for the AR-04a gatekeeper, proven by the acceptance items below in a single pass.

This task is part 1 of 1 of the AR-04a gatekeeper; the parts are ordered by `depends_on` and share one
file surface, so each part reads the tree the part before it produced. The task-unit contract caps a
task at six acceptance items and twenty-four surface, which is why the lane is cut this way.
</objective>

<context>
@.planning/phases/99-arabic-coverage/99-CONTEXT.md
@.planning/phases/99-arabic-coverage/99-RESEARCH.md
@scripts/i18n-audit-strict.mjs
@scripts/partA_maskfinder.py
@scripts/resolve-check.mjs
@scripts/neg-taskcard.mjs

Ref notes: 99-CONTEXT.md — D-05, D-06, D-24, D-27, D-28; 99-RESEARCH.md — §1 the mask population and the order constraint; §3 the blind class; i18n-audit-strict.mjs — from 99-02, the strict model

<interfaces>
Re-derived on disk 2026-08-18 at 9ffa2b80c; commands in 99-REVISION-1.md §Derivations:

- The deletion population is TWO classes, and the first draft's single 161-file scope covered only
  one of them while its oracle demanded both reach zero:
  - two-arg literal `t('key', 'Default')` — **1768 sites / 161 files** by the committed audit's
    cross-line matcher (exactly reproduced at this sha). The line-bound acceptance grep sees 1680
    of them unanchored and 1657 word-bounded; the 111-site gap is calls that span a newline and
    the 23-site gap is identifier tails that are not `t()` at all.
  - `t('key', { defaultValue: … })` options — **314 sites / 103 files**, 88 of which are NOT in
    the two-arg file set. Same requirement, different call shape.
  - UNION: **2082 sites / 249 files.**
- The destructive-satisfiability hazard this task's DAG position retires: 473 of the two-arg sites
  referenced keys resolving in NEITHER locale at plan time. The authoring lanes fixed that; this
  task re-proves it before deletion exists anywhere downstream.
- Zero mask sites live in test files under the audit's exact regex — re-derive rather than trust
  it, and state the test-file policy the strict instrument applies (it excludes `*.test.*` and
  `__tests__` from BOTH classes, one policy for both, not one per clause).
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>the AR-04a gatekeeper — the repo-wide strict proof</name>
  <files>the paths in files_modified</files>
  <read_first>the four instrument headers; every lane SUMMARY in the worktree lineage (for their named residues)</read_first>
  <action>
    This is the first part of the lane, so the lane method below is yours to execute in full over the paths in files_modified. Re-derive every population before relying on it (D-04), run each oracle and record its verbatim output, and name in the SUMMARY anything you leave for a later part.

    --- THE LANE METHOD (carried verbatim from the pre-recut plan; the acceptance items above bound what THIS task owes) ---

    Run, in order, recording each output verbatim: the strict audit unscoped (--json; require zero
    unresolved two-arg AND raw-key, en AND ar, and a nonzero twoArgTotal); maskfinder --control then
    the live run (require zero unresolved prefixes); resolve-check (zero routing misses);
    neg-taskcard (3x MISS=true by text). Then re-derive BOTH deletion populations — the two-arg
    literal class and the defaultValue-options class — with their site and file counts, and write
    them into the SUMMARY as the manifest the deletion lanes consume.

    ANY nonzero stops the phase: name each residue site with the lane that owns it and fail the
    task. Do NOT author keys — JSON is out of scope here by design, and a gatekeeper that repairs
    what it finds is no longer a gate.

  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes. The lane's own criteria, carried: All four instruments at zero/green with outputs recorded verbatim; both deletion populations re-derived and recorded with their counts and their class definitions; `git diff` empty across frontend/backend/supabase/tests/scripts.</acceptance_criteria>
  <done>The tree is proven strictly resolving in both locales, by evidence in its own gate, before any deletion lane is reachable.</done>
</task>

</tasks>

<verification>
POPULATION: the paths in files_modified. OUTSIDE IT: every other task's files, the exogenous paths
(D-03), and every obligation this task's acceptance items do not name — those belong to the lane
parts before and after this one, named in `depends_on` and in the SUMMARY.

The lane's own population statement, carried verbatim from the pre-recut plan:
POPULATION: nothing — this plan reads. OUTSIDE IT: every deletion (the lanes behind it), every
authoring lane's residue (theirs to fix, named here if found), and the rendered battery (the
closing plan).
</verification>

<success_criteria>
Land the repo-wide strict proof for the AR-04a gatekeeper, proven by the acceptance items below in a single pass.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-30-SUMMARY.md` when done. It MUST record every command this task ran with its verbatim output, the
populations it re-derived with the commands that produced them, and anything left for a named later
task. The lane's own SUMMARY contract, carried verbatim from the pre-recut plan (satisfy the parts
this task owns): record in the file named under <output> (99-16-SUMMARY.md in the pre-recut plan) when done. It MUST record: the four
instrument outputs verbatim; both deletion populations with site/file counts and their class
definitions; the test-file policy applied; and the `git diff --name-only` output proving nothing
was edited.
</output>
