---
phase: 99-arabic-coverage
plan: 27
type: execute
wave: 8
depends_on: [99-26]
files_modified:
  - scripts/glossary-census.mjs
  - scripts/glossary-senses.json
  - scripts/glossary-senses.d/brief-stance.json
  - .planning/phases/99-arabic-coverage/99-27-SUMMARY.md
autonomous: true
requirements: [AR-01]
success_criterion: 'Repair and positively control the glossary classifier before the dossier-A value sweep consumes it.'
routing:
  floor: frontier
must_haves:
  truths:
    - 'The production glossary classifier changes only at the ruled seams: the base computer-file key-path family gains token semantics, the brief-artifact row gains the distinct plural identity إحاطات, and --control gains the discriminating dossier/profile/brief cases; no earlier row, control, or sense exception is removed or weakened || A broader precedence rewrite, a Profile-only blacklist, or a local wrapper that leaves glossary-census.mjs unsound is a red'
    - 'At the production classify entry point, a genuine file/attachment path still resolves computer-file-or-attachment, an unlisted organizationProfile-shaped path resolves UNCLASSIFIED, and the same Profile shape with an exact row resolves profile-page-or-summary || Matching lowercase file inside Profile, rejecting real file paths, or merely storing an exact row that first-match classification cannot consume is a red'
    - 'The production brief-artifact row enumerates singular إحاطة and plural إحاطات as distinct, non-overlapping competing terms with Arabic word-boundary patterns, --control proves an allowlisted plural and a planted unclassified plural are both visible, and the stale wrong-sense row in brief-stance.json for the singular exact-five handoff is removed || Double-counting plural text under the singular identity, hiding the plural from the census, or retaining that wrong-sense exception is a red'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; set -u; R="$PWD"; C="$(mktemp -t p9927c.XXXXXX)" || exit 1; J="$(mktemp -t p9927j.XXXXXX)" || { rm -f "$C"; exit 1; }; cleanup() { rm -f "$C" "$J"; }; trap "cleanup; exit 1" HUP INT TERM; trap "cleanup" EXIT; node "$R/scripts/glossary-census.mjs" "$R" --control > "$C" || exit 1; node -e "const j=JSON.parse(require(\"fs\").readFileSync(process.argv[1],\"utf8\"));const names=[\"realFilePreserved\",\"unlistedProfileRejected\",\"exactProfileSelected\",\"briefPluralSeen\",\"plantedUnclassifiedBriefPluralCaught\"];if(j.control!==\"PASS\"||names.some(k=>j[k]!==true))throw new Error(\"missing discriminating control: \"+JSON.stringify(j))" "$C" || exit 1; node "$R/scripts/glossary-census.mjs" "$R" --row brief-artifact --json > "$J" 2>/dev/null; rc=$?; test "$rc" -eq 1 || exit 1; node -e "const j=JSON.parse(require(\"fs\").readFileSync(process.argv[1],\"utf8\"));const got=j.unclassified.map(x=>x.file+\":\"+x.keyPath+\":\"+x.term).sort();const want=[\"contextual-suggestions.json:suggestions.upcomingEngagement.description:إحاطات\",\"dossier-overview.json:documentType.brief:إحاطة\",\"dossier-overview.json:documents.empty.brief:إحاطات\",\"dossier-overview.json:documents.tabs.briefs:إحاطات\",\"dossier.json:templates.category.thematic:إحاطات\"].sort();if(JSON.stringify(got)!==JSON.stringify(want))throw new Error(\"brief handoff is not the exact five: \"+JSON.stringify(got))" "$J"'
      text: 'the production control ledger proves the genuine-file pass, accidental-Profile fail, exact-profile pass, and both plural-brief cases, then the live repo-wide row fails on exactly the five ruled artifact values handed to P99-44 — a control-only stub, a vacuous zero, or any hidden/additional residue is red'
---

<objective>
Repair and positively control the glossary classifier before the dossier-A value sweep consumes it.

This is part 1 of 2 of the dossier-A rerun created by RULING-P99-100. It owns only the source
instrument, base senses, brief/stance overlay, and its own evidence. P99-44 depends on it and owns
the Arabic values plus dossier-A judgments. The split is the compiler-enforced product boundary:
five acceptance items x four files patterns = 20, within the maximum surface of 24. The fifth item is
the task body's `<done>` outcome, which the GSD compiler counts with the four frontmatter truths.
</objective>

<context>
@.planning/phases/99-arabic-coverage/rulings/RULING-P99-100-DOSSIER-A-SPLIT.md
@.planning/phases/99-arabic-coverage/rulings/RULING-P99-99-DOSSIER-A-RERUN.md
@.planning/phases/99-arabic-coverage/99-CONTEXT.md
@.planning/phases/99-arabic-coverage/99-RESEARCH.md
@scripts/glossary-census.mjs
@scripts/glossary-senses.json
@scripts/glossary-senses.d/brief-stance.json

Ref notes: read RULING-P99-100 first; RULING-P99-99 carries the measured review and judge-retry
findings; D-17/D-18 define the dossier/file/profile and brief/session boundaries.
</context>

<tasks>

<task type="auto">
  <name>repair the glossary classifier and expose the exact dossier-A handoff</name>
  <files>the paths in files_modified</files>
  <read_first>RULING-P99-100-DOSSIER-A-SPLIT.md first; then RULING-P99-99-DOSSIER-A-RERUN.md; glossary-census.mjs classify/load/control paths; glossary-senses.json; brief-stance.json</read_first>
  <action>
    Implement the file-token boundary and the three dossier/profile control cases exactly as ruled.
    Give singular إحاطة and plural إحاطات non-overlapping Arabic word-boundary patterns, and add both
    the plural's allowlisted and planted-red
    controls. Remove only the stale wrong-sense row for the singular exact-five handoff and revalidate every
    other brief/stance row. Run --control first, then the exact-five handoff oracle. Do not rewrite an
    Arabic bundle or dossier-a.json in this task; those are P99-44's dependent value/classification unit.
  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes. The SUMMARY records every control field and the exact five live unclassified paths, explicitly naming P99-44 as their owner.</acceptance_criteria>
  <done>The production classifier distinguishes genuine files from Profile, sees plural brief artifacts, and exposes exactly the five values the dependent sweep must repair.</done>
</task>

</tasks>

<verification>
POPULATION: glossary-census.mjs, the ruled base file row, the brief/stance row, the new control cases,
and this task's SUMMARY. OUTSIDE IT: every Arabic bundle and dossier-a.json (P99-44), every other
glossary family, English values, key names, and application source.
</verification>

<success_criteria>
Repair and positively control the glossary classifier before the dossier-A value sweep consumes it.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-27-SUMMARY.md`. Record the control JSON verbatim, the
live brief-artifact JSON/exit status, the exact five handoff paths, the before/after base pattern, the
brief plural identity, the stale-row removal, and every changed path. This is instrument evidence only;
do not claim the live row is green.
</output>
