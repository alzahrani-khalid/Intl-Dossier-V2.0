---
phase: 99-arabic-coverage
plan: 10
type: execute
wave: 3
depends_on: [99-06, 99-07]
files_modified:
  - 'frontend/src/{i18n/en/countries.json,i18n/ar/countries.json,i18n/en/organizations.json,i18n/ar/organizations.json,i18n/en/graph.json,i18n/ar/graph.json,i18n/en/relationships.json,i18n/ar/relationships.json,i18n/en/dossier.json,i18n/ar/dossier.json,i18n/en/dossier-context.json,i18n/ar/dossier-context.json,i18n/en/dossier-search.json,i18n/ar/dossier-search.json,i18n/en/engagements.json,i18n/ar/engagements.json,i18n/en/lifecycle.json,i18n/ar/lifecycle.json,i18n/en/contacts.json,i18n/ar/contacts.json,pages/Countries.tsx,pages/Organizations.tsx,components/relationships/GraphVisualization.tsx,components/relationships/RelationshipNavigator.tsx,pages/DossierSearchPage.tsx}'
  - .planning/phases/99-arabic-coverage/99-10-SUMMARY.md
autonomous: true
requirements: [AR-03, AR-04a, AR-04b]
success_criterion: 'Land authoring and colon form for the dossier-family lane, proven by the acceptance items below in a single pass.'
routing:
  floor: frontier
truths:
  - "Decisions covered — D-24 steps 1-2: authoring + verification only, no second-argument drops (the deletion lanes own deletion, sequenced behind this plan in the DAG); D-25: every key authored in en AND ar, en byte-identical to the mask literal; D-26: heavy-file placement via colon-prefix to the feature namespace (countries:/organizations:/graph:/relationships:), hooks untouched so unrepaired keys in the same files cannot shift; D-27: the dynamic carriers here (Countries.tsx countries.status, Organizations.tsx organizations.status, RelationshipNavigator dossier.type/dossier.status ×3) repair as atomic enum units verified through partA_maskfinder; D-32: the search suggestion chips are one of the two HARDCODED members of the four named criterion-3 surfaces — extraction + authoring, not repoint; D-29: UI99-C8 is the binding UI-contract row this lane closes and it closes on the RENDERED `UI99-C8 ar search chips` test, invoked by this plan's own command oracle — not on the source absence-grep beside it; D-07: a criterion member closes on a rendered surface or a drilled instrument, never on source text alone; D-17/D-18: authored ar values use the ruled terms (دوسيه for the dossier object, مشاركة for engagement, موقف for stance) — sense-aware, never a blanket rewrite."
must_haves:
  truths:
    - 'Decisions covered — D-07, D-17, D-18, D-24(1-2), D-25, D-26, D-27, D-29, D-32: authoring-first in both locales, colon form, atomic dynamic units, chips extracted, glossary-conformant ar.'
    - 'Every unresolved two-arg and raw-key site in the five source files resolves in BOTH locales afterwards (scoped strict-audit zero), second arguments byte-untouched outside the dynamic atomic units'
    - "The chips array in DossierSearchPage renders from dossier-search: keys authored in both locales — proper nouns that legitimately stay Latin (G20) are named in the UI99-C9 allowlist by 99-01's spec, and the ar values for the rest are Arabic (السعودية family), so the raw literals Saudi Arabia/UN/climate no longer appear in the source"
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node "$R/scripts/i18n-audit-strict.mjs" "$R" --scope "frontend/src/pages/Countries.tsx,frontend/src/pages/Organizations.tsx,frontend/src/components/relationships/GraphVisualization.tsx,frontend/src/components/relationships/RelationshipNavigator.tsx,frontend/src/pages/DossierSearchPage.tsx" --json | node -e "let s=[];process.stdin.on(\"data\",d=>s.push(d)).on(\"end\",()=>{const j=JSON.parse(s.join(\"\"));if(j.twoArgUnresolved!==0||j.rawKeyUnresolved!==0||j.twoArgUnresolvedAr!==0||j.rawKeyUnresolvedAr!==0)process.exit(1)})" && ! command grep -n "''Saudi Arabia''" "$R/frontend/src/pages/DossierSearchPage.tsx" && command grep -q "dossier-search" "$R/frontend/src/pages/DossierSearchPage.tsx"'
      text: 'scoped strict-audit zero across the five lane files AND the hardcoded chip literal is gone while the file still binds dossier-search (absence+presence pair) — RED at HEAD on both halves'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; cd "$R" && test -f tests/e2e/99-ar03-leak.spec.ts && pnpm exec playwright test tests/e2e/99-ar03-leak.spec.ts -g "UI99-C8 ar search chips" --project=chromium-en --no-deps --list 2>&1 | command grep -qE "Total: 1 test in 1 file" && pnpm exec playwright test tests/e2e/99-ar03-leak.spec.ts -g "UI99-C8 ar search chips" --project=chromium-en --no-deps'
      text: 'the RENDERED UI99-C8 test — ONE spec path, ONE named test, its count hardcoded from --list so the filter cannot silently select nothing — passes under asserted ar with the chips rendering Arabic — RED at HEAD (the spec does not exist; after 99-01 it is red on the raw English chip literals)'
---

<objective>
Land authoring and colon form for the dossier-family lane, proven by the acceptance items below in a single pass.

This task is part 1 of 2 of the dossier-family lane; the parts are ordered by `depends_on` and share one
file surface, so each part reads the tree the part before it produced. The task-unit contract caps a
task at six acceptance items and twenty-four surface, which is why the lane is cut this way.
</objective>

<context>
@.planning/phases/99-arabic-coverage/99-CONTEXT.md
@.planning/phases/99-arabic-coverage/99-RESEARCH.md
@scripts/partA_maskfinder.py
@frontend/src/pages/Countries.tsx
@frontend/src/pages/DossierSearchPage.tsx
@tests/e2e/99-ar03-leak.spec.ts

Ref notes: 99-CONTEXT.md — D-17, D-18, D-23..D-27, D-32; 99-RESEARCH.md — §1.5 lane table; §3 dynamic members; §6.1 row 4 the chips; 99-ar03-leak.spec.ts — authored in 99-01; the UI99-C8 test is this lane's rendered oracle

<interfaces>
Verified 2026-08-18 at 9ffa2b80c:

- Countries.tsx and Organizations.tsx bind BARE useTranslation() (alias → common), which is why
  their 29+28 unresolved keys default to the common bucket; the colon-prefix mechanism moves the
  repaired keys to countries:/organizations: WITHOUT touching the hook — never change the hook,
  it would shift every other t() in the file.
- Dynamic carriers (maskfinder names them): `pages/Countries.tsx:275` (countries.status),
  `pages/Organizations.tsx:300` (organizations.status), `RelationshipNavigator.tsx:166,212`
  (dossier.type) and `:218` (dossier.status). Enumerate the enum values from the type they map
  (dossier types are the 8 ruled types; statuses from the status union each component consumes).
- The chips: `DossierSearchPage.tsx` renders the literal array
  `['Saudi Arabia', 'UN', 'G20', 'climate']` (search the STRING, not the line); the file binds
  dossier-search. Extraction = keys in dossier-search.json (both locales) + a t()-mapped render.
- Authoring-only namespaces (no source edit — the sites already bind them): dossier (wizard
  review steps, DossierTypeGuide/StatsCard learnMore), dossier-context (DossierLinksWidget),
  engagements (EngagementsList), lifecycle (useLifecycle), contacts (usePersonDossiers).
  Re-derive each slice with the strict audit --scope at task start.
- ar glossary at authoring time: dossier object = دوسيه (ملف stays legal for file/profile
  senses — sense-aware), engagement = مشاركة, position-as-stance = موقف, country = دولة/الدول.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>the dossier-family lane — authoring and colon form</name>
  <files>the paths in files_modified</files>
  <read_first>the five source files in full; the ten en/ar JSON pairs (author into existing shapes); 99-RESEARCH.md §3 for the dynamic anatomy</read_first>
  <action>
    This is the first part of the lane, so the lane method below is yours to execute in full over the paths in files_modified. Re-derive every population before relying on it (D-04), run each oracle and record its verbatim output, and name in the SUMMARY anything you leave for a later part.

    --- THE LANE METHOD (carried verbatim from the pre-recut plan; the acceptance items above bound what THIS task owes) ---

    1. Strict audit --scope over the five source files; record the starting population. Then a
       second scoped run over the authoring-only consumers (the hooks/components named in
       interfaces) to enumerate the dossier/dossier-context/engagements/lifecycle/contacts key
       slices; record them.
    2. Author every missing key in en AND ar in this lane's ten namespace pairs (en
       byte-identical to the mask literal where one exists; ar per ruled glossary + voice law).
       Rewrite each repaired two-arg/raw-key site in the five source files to explicit colon
       form. KEEP second arguments. No hook changes.
    3. Dynamic units: enumerate enum values, author full sets both locales, colon-prefix the
       dynamic expressions (e.g. countries:status. + value), drop the variable second argument
       at those sites only, same commit per unit.
    4. Chips: replace the literal array with a t()-driven list over dossier-search: keys
       authored in both locales; G20 may keep a Latin value in ar per the UI99-C9 allowlist —
       record the choice.
    5. Rewrite this lane's dot-form common.X sites (if the scoped audit surfaces any) to
       common:X only where the key resolves at the flattened root; leave and NAME the rest for
       99-06.
    6. Verify: scoped strict audit zero (both classes, both locales) over the five files; the
       authoring-only slices resolve (scoped audit over their consumers); maskfinder clean of
       the three carriers; en/ar leaf parity for all ten pairs; frontend type-check green.

  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes. The lane's own criteria, carried: Scoped strict-audit zero with starting numbers recorded; chips extracted with the absence+presence pair holding; three dynamic carriers gone from the instrument of record with its control run first; en/ar parity across all ten pairs; no second-argument change outside dynamic units; type-check green.</acceptance_criteria>
  <done>The dossier family resolves in both locales through explicit colon keys; the chips are i18n-driven; the dynamic carriers are retired.</done>
</task>

</tasks>

<verification>
POPULATION: the paths in files_modified. OUTSIDE IT: every other task's files, the exogenous paths
(D-03), and every obligation this task's acceptance items do not name — those belong to the lane
parts before and after this one, named in `depends_on` and in the SUMMARY.

The lane's own population statement, carried verbatim from the pre-recut plan:
POPULATION: the 26 listed files. OUTSIDE IT: common.json authoring (the common-owner lane), nav labels and page
TITLE values (the glossary lanes — this lane authors keys, the glossary sweep aligns values), the countries
nav-label البلدان→الدول value change (the glossary lanes per the requirement's own arrow), second-argument
drops (99-09), rendered chip assertion (99-01's spec, re-run in the closing battery).
</verification>

<success_criteria>
Land authoring and colon form for the dossier-family lane, proven by the acceptance items below in a single pass.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-10-SUMMARY.md` when done. It MUST record every command this task ran with its verbatim output, the
populations it re-derived with the commands that produced them, and anything left for a named later
task. The lane's own SUMMARY contract, carried verbatim from the pre-recut plan (satisfy the parts
this task owns): record in the file named under <output> (99-05-SUMMARY.md in the pre-recut plan) when done. It MUST record: starting
vs ending scoped audit numbers; the enum sets per dynamic carrier; the authoring-only slices per
namespace; the G20/proper-noun choice; and every common-targeted key left for the common-owner lane by name.
</output>
