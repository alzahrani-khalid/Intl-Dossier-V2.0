---
phase: 99-arabic-coverage
plan: 53
type: execute
wave: 11
depends_on: [99-52]
files_modified:
  - 'scripts/fixtures/dynamic-key-audit/**'
  - .planning/phases/99-arabic-coverage/99-53-SUMMARY.md
autonomous: true
requirements: [AR-04a]
success_criterion: 'Re-derive the translator-binding form set from the language and drill every form by the production shape it names, so a cell cannot pass while blind to the shape it claims.'
routing:
  floor: frontier
truths:
  - 'RULING-P99-282 and RULING-P99-251: THE CORPUS OWNER AND THE INSTRUMENT OWNER MUST DIFFER. This task owns the fixture corpus, its generator and its matrix; the instrument task DEPENDS ON this one and MAY NOT TOUCH these paths, and this task MAY NOT TOUCH the instrument, its tests, any production caller, or any locale bundle. An instrument graded against a corpus it authored is the circularity four runs were spent removing.'
must_haves:
  truths:
    - 'THE FORM SET IS RE-DERIVED FROM THE LANGUAGE, NOT EXTENDED ROW BY ROW. The existing seven forms are a list of shapes someone thought of, which RULING-P99-274 forbids: a translator reaches a call site through a bounded set of BINDING MECHANISMS the language provides, and the set is derived by enumerating those mechanisms - direct call, destructuring with and without rename, aliased import, re-export, property access, parameter passing, assignment and reassignment, and shadowing - not by appending a row per defect report. Adding an eighth row to the existing seven satisfies neither this truth nor its oracle.'
    - 'EVERY CELL DRILLS THE SHAPE ITS NAME CLAIMS, AND THIS IS A REPAIR OF A MEASURED FALSE POSITIVE, NOT A NEW REQUIREMENT. Measured at 2e3f26bad: the matrix carries 4/4 rows naming the destructured form, and 0/4 of their fixture callers drill a renamed translator binding - 3/4 drill the un-renamed const { t } = useTranslation(ns) and 1/4 drills a renamed HOOK, const { useTranslation: factory } = i18n, which renames a different symbol. The matrix NAMED destructuring while drilling only its easy variant, so twenty-eight green cells coexisted with a production shape none of them exercised. A cell whose fixture does not contain the shape its name claims is a false coverage claim and fails this task.'
    - "THE TWO MEASURED PRODUCTION FAILURES ARE DRILLED BY THEIR PRODUCTION SHAPE AND NAMED BY FILE AND LINE, NEVER BY THE WORD RENAME. (1) A renamed destructured translator, const { t: an aliased translator } = useTranslation('quickswitcher'), at the command-palette file named in <context>, lines 431-432 at 2e3f26bad, which the instrument today refuses to classify - it fails CLOSED, so the site is visible but unclassifiable. (2) A parameter-bound translator, where t reaches the call through a function parameter rather than a hook binding, at the analytic-result-view file named in <context>, at 2e3f26bad - which the instrument today drops silently: it fails OPEN, so the site appears in NEITHER counter and lane3Sites reads 11 against a ruled 13. Naming these by the word rename admits a fixture that renames the hook and closes the finding without closing the defect."
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; F="$R/scripts/fixtures/dynamic-key-audit"; M="$F/FORM-MATRIX.tsv"; test -f "$M" || { echo "FAIL: no matrix"; exit 1; }; FORMS=$(awk -F"\t" "NR>0{print \$2}" "$M" | sort -u | grep -c .); IDS=$(awk -F"\t" "NR>0{print \$1}" "$M" | sort -u | grep -c .); ROWS=$(grep -c . "$M"); echo "identifiers=$IDS forms=$FORMS rows=$ROWS"; test "$ROWS" -eq "$((IDS*FORMS))" || { echo "FAIL: matrix is not a complete identifier x form cross-product ($IDS x $FORMS = $((IDS*FORMS)), got $ROWS)"; exit 1; }; test "$FORMS" -gt 7 || { echo "FAIL: form set was not re-derived - $FORMS forms is not more than the seven it replaces"; exit 1; }; REN=$(command grep -rlE "const \{ *t: *[A-Za-z_][A-Za-z0-9_]* *\} *= *useTranslation\(" "$F" | grep -c .); echo "fixtures drilling a RENAMED TRANSLATOR destructure=$REN"; test "$REN" -ge 1 || { echo "FAIL: no fixture drills const { t: alias } = useTranslation(ns)"; exit 1; }; CTX=0; PAR=0; SCANNED=0; cd "$F" || { echo "INSTRUMENT-CANNOT-RUN: fixture root unreadable: $F"; exit 3; }; while IFS= read -r rel; do SCANNED=$((SCANNED+1));   if command grep -qE "const *\{[^}]*\bt\b[^}]*\} *= *(ctx|props|arg[s]?)\b" "$rel"; then CTX=$((CTX+1)); fi;   if command grep -qE "function [A-Za-z]+\([^)]*\bt\b[^)]*\)|\([^)]*\bt\b[^)]*\) *=>" "$rel"; then PAR=$((PAR+1)); fi; done < <(command find . -name caller.tsx -type f); echo "scanned=$SCANNED parameter-bound=$PAR ctx-object-bound=$CTX"; test "$SCANNED" -gt 0 || { echo "INSTRUMENT-CANNOT-RUN: the fixture scan enumerated ZERO caller.tsx files; a count of 0 below would be indistinguishable from a real absence, so this oracle verified NOTHING"; exit 3; }; test "$PAR" -ge 1 || { echo "FAIL: no fixture drills a parameter-bound translator (scanned $SCANNED files, so this is a real absence, not an instrument failure)"; exit 1; }; test "$CTX" -ge 1 || { echo "FAIL: no fixture drills the PRODUCTION ctx-object destructure const { t, ... } = ctx (scanned $SCANNED files, so this is a real absence). The direct-parameter form already CLASSIFIES and cannot expose the fail-OPEN defect."; exit 1; }; cd "$R" || exit 3; B=$(mktemp -d); cp -R "$F" "$B/work"; ( cd "$B" && node "$B/work/generate-fixtures.mjs" --check ) > "$B/out" 2>&1; CHK=$?; command diff -r "$F" "$B/work" > "$B/drift" 2>&1 || true; DRIFT=$(grep -c . "$B/drift" || true); echo "generator-check-exit=$CHK reproduction-drift-lines=$DRIFT"; head -3 "$B/drift" 2>/dev/null; FREL="scripts/fixtures/dynamic-key-audit"; BYTES=$(command find "$F" -type f -exec cat {} \; | command wc -c | command tr -d " "); FILES=$(command find "$F" -type f | grep -c .); test "$FILES" -gt 0 || { echo "INSTRUMENT-CANNOT-RUN: corpus enumerated ZERO files"; exit 3; }; BR=$(git rev-parse --abbrev-ref HEAD) || { echo "INSTRUMENT-CANNOT-RUN: cannot read branch"; exit 3; }; RUNBR="${BR%--*}"; test "$RUNBR" != "$BR" || { echo "INSTRUMENT-CANNOT-RUN: not on a task branch, cannot derive the task base"; exit 3; }; TASKBASE=$(git merge-base HEAD "$RUNBR") || { echo "INSTRUMENT-CANNOT-RUN: no merge-base with $RUNBR"; exit 3; }; ADD=0; while IFS= read -r rel; do n=$(git diff --no-index --binary /dev/null "$rel" 2>/dev/null | command wc -c | command tr -d " "); case "$n" in ""|*[!0-9]*) echo "INSTRUMENT-CANNOT-RUN: could not measure the add-diff of $rel"; exit 3;; esac; ADD=$((ADD+n)); done < <(command find "$FREL" -type f); test "$ADD" -gt "$BYTES" || { echo "INSTRUMENT-CANNOT-RUN: measured corpus add-diff $ADD is not greater than its own content $BYTES, so the diff measurement is not working"; exit 3; }; OVH=$(( (ADD - BYTES) / FILES )); TOTAL=$(git diff --binary "$TASKBASE"..HEAD | command wc -c | command tr -d " "); case "$TOTAL" in ""|*[!0-9]*) echo "INSTRUMENT-CANNOT-RUN: task diff unmeasurable against $TASKBASE"; exit 3;; esac; test "$TOTAL" -gt 0 || { echo "INSTRUMENT-CANNOT-RUN: task diff measured ZERO bytes against $TASKBASE"; exit 3; }; CAP=155000; echo "corpus-content=$BYTES corpus-files=$FILES corpus-add-diff=$ADD overhead-per-file=$OVH task-diff=$TOTAL cap=$CAP headroom=$((CAP-TOTAL))"; rm -rf "$B"; test "$CHK" -eq 0 && test "$DRIFT" -eq 0 && test "$TOTAL" -le "$CAP"'
      text: 'the matrix is a COMPLETE identifier-by-form cross-product with a form set strictly larger than the seven it replaces. DECLARED HANDOFF (RULING-P99-345): re-deriving this corpus to more than seven forms NECESSARILY breaks the committed consumer suite scripts/i18n-dynamic-key-audit.test.mjs, which pins exactly 28 rows over the seven old forms at line 699 and calls assertFixturePair on fixture roots this task renames. That file is NOT in this task''s files[] - it belongs to P99-54 - so no in-scope edit can reconcile it, and two review rounds have now failed on exactly that. THIS IS A DESIGNED HANDOFF, NOT A DEFECT IN THIS TASK: the consumer is re-scoped by P99-54, which owns the file. A review finding that the consumer suite is red is therefore EXPECTED for this task and must not block it; a finding that the corpus itself is wrong still must. The worker is not required to make that suite green and must not attempt it by shrinking the cross-product back to seven forms, so appending one row for one defect fails; at least one fixture literally contains const { t: <alias> } = useTranslation(<ns>) and at least one binds a translator through a function parameter, so the two MEASURED production shapes are drilled rather than named; the generator still rebuilds the corpus byte-identically into a COPY with zero drift, so a reproduction check never writes to the artifact it verifies; and the PREDICTED DIFF - not the corpus content - stays within gates.diffCap so the work still reaches the LLM gates (RULING-P99-353, applying the model RULING-P99-292 measured and banked). The old 100000-byte CONTENT ceiling was on the WRONG VARIABLE: diff overhead is per-FILE and path-length dependent, so a content bound buys none of the diff capacity it exists to buy. Measured on the seven-form corpus at 114 files: 82189 bytes of content became a 123177-byte add-diff, an overhead of 359.5 bytes per file - and R292 recorded 414.7 over a different population, which is why this oracle RE-MEASURES rather than carrying either constant. The oracle now measures the corpus add-diff directly, derives overhead-per-file as a DIAGNOSTIC so the worker can see the lever, and gates on the REAL task diff against the 155000 cap. THIS BOUND IS STEERABLE AND THE WORKER OWNS IT: the projection at this overhead is 140671 bytes for eight forms (FITS, 14329 to spare), 158525 for nine and 176019 for ten (both OVER). Nothing in this task requires ten - the assertion is FORMS greater than seven - so the budget is met by choosing the form count, the files per root and the path lengths, and NEVER by shrinking back to the seven forms this task replaces, which remains forbidden. A red here means the corpus does not fit the gate that must read it, which is a real finding and not an instrument fault'
    - 'THE CORPUS STAYS MINIMAL AND GENERATED. Every corpus file is produced by the committed generator from the committed matrix, and the generator MAY NOT decide what the corpus contains. The corpus is minimal IN THE TASK DIFF AND IN EVERY COMMIT MADE BY THE ATTEMPT UNDER REVIEW; commits inherited from an EARLIER attempt of the same task are OUT OF SCOPE for that clause, because review-retry appends to the same candidate branch and an earlier commit is immutable to the worker. A minimal corpus sheds FILES, not merely bytes: 922 files, not 15583 lines, is what broke the gate cap in run 0053.'
---

<objective>
Re-derive the translator-binding form set from the language and drill every form by the production shape it
names, so a cell cannot pass while blind to the shape it claims.
</objective>

<context>
@.planning/phases/99-arabic-coverage/99-52-PLAN.md
@.planning/phases/99-arabic-coverage/99-52-SUMMARY.md
@scripts/fixtures/dynamic-key-audit/FORM-MATRIX.tsv
@frontend/src/components/keyboard-shortcuts/CommandPalette.tsx
@frontend/src/components/relationships/AnalyticResultView.tsx
</context>

<tasks>
<task type="auto">
  <name>re-derive the form set and drill the two measured production shapes</name>
  <files>the paths in files_modified</files>
  <read_first>the committed FORM-MATRIX.tsv and every fixture root named by a destructured cell</read_first>
  <action>
    Enumerate the binding mechanisms the language provides for a translator to reach a call site, and
    derive the form set from that enumeration. Regenerate the matrix as a complete identifier-by-form
    cross-product, and regenerate every fixture from the generator so each cell's caller literally contains
    the shape its name claims - in particular a destructured cell must drill the RENAMED destructure, which
    0/4 of today's four destructured cells do. Add the two measured production shapes as first-class forms:
    the renamed destructured translator seen at CommandPalette.tsx:431-432, and the parameter-bound
    translator seen in AnalyticResultView.tsx. Record in the SUMMARY, for every cell, the shape its fixture
    contains, so a future reader can check a name against its drill without running anything.
    Do NOT touch the instrument, its tests, any production caller, or any locale bundle.
  </action>
  <acceptance_criteria>Every acceptance item above passes and nothing outside the corpus changes.</acceptance_criteria>
  <done>Every named form is drilled by a fixture containing that form's production shape, and the corpus still rebuilds byte-identically into a copy.</done>
</task>
</tasks>

<verification>
POPULATION: the fixture corpus, its generator, its matrix, and this SUMMARY. OUTSIDE IT: the instrument and
its tests (the task that depends on this one), every production caller and locale bundle (99-49 and its
successor), every fallback deletion (99-31 through 99-38), and the literal gatekeeper battery (99-50).
</verification>

<success_criteria>
Re-derive the translator-binding form set from the language and drill every form by the production shape it
names, so a cell cannot pass while blind to the shape it claims.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-53-SUMMARY.md` recording the VERBATIM stdout and stderr of
every oracle command, the derived form set with the language mechanism each form corresponds to, a
cell-by-cell table of the shape each fixture actually contains, and the MEASURED corpus content bytes and
file count.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete`. Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as **pending** no matter how many gates passed — measured on P99-32 and again on P99-61, which passed 7/7 and still compiled as pending (PRODUCT ENTRY 36). Verify with `node scripts/completion-contract-check.mjs <journal> <ref>`.
</output>
