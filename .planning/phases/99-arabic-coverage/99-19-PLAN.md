---
phase: 99-arabic-coverage
plan: 19
type: execute
wave: 4
depends_on: [99-16, 99-18, 99-12]
# depends_on carries ONE edge that is NOT a data dependency: 99-12 is a PORT-CONTENTION
# serialization added by RULING-P99-19 (c-iv). This plan consumes nothing 99-12 produces; the
# edge exists solely so two rendered oracles never hold the dev stack's ports at once
# (pnpm dev starts vite 5173 + backend 5001 + agent-runtime 4100, and the engine runs
# concurrency 3). REMOVE THIS EDGE the day per-worktree port plumbing lands (c-iii).
files_modified:
  - 'frontend/src/{routes/__root.tsx,components/modern-nav/ExpandedPanel/NavigationSection.tsx,components/layout/EntityBreadcrumbTrail.tsx,components/table/DataTable.tsx,components/table/AdvancedDataTable.tsx,components/guided-tours/TourOverlay.tsx,components/guided-tours/OnboardingTourTrigger.tsx,components/modern-nav/ExpandedPanel/DocumentTree.tsx,routes/modern-nav-standalone.tsx,components/forms/FormSection.tsx,components/forms/ArrayFieldManager.tsx,components/forms/ContextualHelp.tsx,components/forms/FormErrorDisplay.tsx,components/forms/UnifiedFileUpload.tsx,pages/availability-polling/AvailabilityPollingPage.tsx,pages/sla-monitoring/SLADashboardPage.tsx,pages/intelligence/IntelligencePage.tsx,pages/monitoring/Dashboard.tsx,components/entity-links/EntitySearchDialog.tsx,components/entity-links/AISuggestionPanel.tsx,components/ai/EntityLinkSuggestions.tsx,components/calendar/ConflictResolution/ReschedulingSuggestions.tsx,components/compliance/ComplianceRulesManager.tsx,components/dossier/wizard/StepGuidanceBanner.tsx,components/dossier-recommendations/DossierRecommendationCard.tsx,components/duplicate-detection/DuplicateCandidatesList.tsx,components/engagement-recommendations/RecommendationsPanel.tsx,components/forums/ForumDetailsDialog.tsx,components/legislation/LegislationDetail.tsx,components/legislation/LegislationList.tsx,components/milestone-planning/ConvertMilestoneDialog.tsx,components/milestone-planning/MilestoneCard.tsx,components/milestone-planning/AddMilestoneDialog.tsx,components/workflow-automation/ActionBuilder.tsx,pages/analytics/AnalyticsDashboardPage.tsx,pages/webhooks/WebhooksPage.tsx,routes/_protected/admin/ai-usage.tsx,pages/Dashboard/components/AttentionItem.tsx,components/calendar/CalendarEntryForm.tsx,components/calendar/ConflictResolution/WhatIfScenarioPanel.tsx,components/elected-officials/ElectedOfficialListTable.tsx,components/export-import/ExportDialog.tsx,components/export-import/ImportDialog.tsx,components/positions/PositionDossierLinker.tsx,components/settings/BotIntegrationsSettings.tsx,components/sla-monitoring/SLAEscalationsList.tsx,components/sla-monitoring/SLAPolicyForm.tsx,components/stakeholder-timeline/StakeholderAnnotationDialog.tsx,components/stakeholder-timeline/StakeholderInteractionDialog.tsx}'
  - .planning/phases/99-arabic-coverage/99-19-SUMMARY.md
autonomous: true
requirements: [AR-01, AR-03, AR-04a, AR-04b]
success_criterion: 'Land the dynamic carriers and the chrome literals for the chrome rewrite lane, proven by the acceptance items below in a single pass.'
routing:
  floor: frontier
truths:
  - "Decisions covered — D-15: the 404 repoint is expressed against the POST-flatten key shape (notFound.* at the namespace root) and no pre-flatten shape is hardcoded anywhere; D-24 steps 2-3 for the named atomic units only: the nav fallback and the 404 defaults are dropped in the same commit that points them at keys the authoring lane already landed and this plan re-probes — an atomic repoint, never a mass drop; D-26: no key is authored here, only pointed at (the authoring lane owns every JSON leaf), so a missing key STOPS this plan rather than being quietly written; D-27: the four dynamic carriers (the entityTypes prefix, the recommendation-card types prefix, the intelligence.classification prefix and the stages prefix) retire through partA_maskfinder with its control run first; D-35: the hardcoded-literal class members that sit on criterion-3 routes are extracted here — the class is bounded by the rendered UI99-C9 scan, not by any source grep; D-02: the nav-mask repair is a criterion-1 member — the nav itself was an AR-04a mask surface; D-29: UI99-C5 is the binding UI-contract row this lane closes and it closes on the RENDERED `UI99-C5 ar 404` test, invoked by this plan's own command oracle; D-07: a criterion member closes on a rendered surface or a drilled instrument, never on source text alone."
must_haves:
  truths:
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; cd "$R" && test -f tests/e2e/99-ar03-leak.spec.ts && pnpm exec playwright test tests/e2e/99-ar03-leak.spec.ts -g "UI99-C5 ar 404" --project=chromium-en --no-deps --list 2>&1 | command grep -qE "Total: 1 test in 1 file" && node "$R/scripts/pw-run-reaped.mjs" -- tests/e2e/99-ar03-leak.spec.ts -g "UI99-C5 ar 404" --project=chromium-en --no-deps'
      text: 'the RENDERED UI99-C5 test — ONE spec path, ONE named test, its count hardcoded from --list — passes: the 404 body renders the Arabic notFound family, including the بحث that rides the relocated common:search.label. This oracle stays RED across the flatten (which repoints only the search call) and goes green ONLY here, when the 404 renders through the new path — RED at HEAD (the spec does not exist).'
---

<objective>
Land the dynamic carriers and the chrome literals for the chrome rewrite lane, proven by the acceptance items below in a single pass.

This task is part 3 of 3 of the chrome rewrite lane; the parts are ordered by `depends_on` and share one
file surface, so each part reads the tree the part before it produced. The task-unit contract caps a
task at six acceptance items and twenty-four surface, which is why the lane is cut this way.
</objective>

<context>
@.planning/phases/99-arabic-coverage/99-CONTEXT.md
@.planning/phases/99-arabic-coverage/99-RESEARCH.md
@scripts/partA_maskfinder.py
@frontend/src/routes/__root.tsx
@frontend/src/components/modern-nav/ExpandedPanel/NavigationSection.tsx
@tests/e2e/99-ar03-leak.spec.ts

Ref notes: 99-CONTEXT.md — D-02, D-15, D-24, D-26, D-27, D-29, D-35; 99-RESEARCH.md — §2.3 the colon-miss classes; §3; §6.1 row 1 the 404; §6.2 the hardcoded class; partA_maskfinder.py — the instrument of record — run --control first; 99-ar03-leak.spec.ts — authored in 99-01; the UI99-C5 test is this lane's rendered oracle

<interfaces>
Verified 2026-08-18 at 9ffa2b80c; re-derive post-flatten at task start:

- The nav mask: `{t(item.labelKey, item.label)}` in NavigationSection (search the STRING). The four
  keys landed in the authoring lane; PROBE each one resolving in both locales before dropping the
  fallback, and drop it in the same commit — the atomic-unit rule.
- The 404 (UI99-C5): NotFoundPage in \_\_root.tsx masks four keys; the translated copy lives at the
  flattened root as notFound.{title,message,goBack,goHome}. The repair is a REPOINT to
  common:notFound.\* plus dropping those four defaults in the same commit; the goHome copy replaces
  the Dashboard button label, which the contract row allows. The file's `t('common.search', …)`
  and `t('common.dashboard', …)` sites were already rewritten by the flatten plan — do not touch
  them again.
- Colon-residual and missAll: the flatten plan de-prefixed every `common:common.X` site and
  rewrote every dot-form site whose key resolves at the root. What remains here is the missAll
  class — dot-form sites whose key was absent from the root and which the authoring lane has now
  landed: rewrite each into its bound namespace where one exists, `common:` colon form otherwise.
  RE-DERIVE with the strict audit; do not carry a count.
- Dynamic carriers here: EntityBreadcrumbTrail (entityTypes), DossierRecommendationCard (types),
  IntelligencePage (intelligence.classification), AttentionItem (stages). Their enum sets exist
  already; this plan colon-prefixes the expressions and drops the variable second argument.
- The chrome literals: monitoring Dashboard "Overall:", EntitySearchDialog Match:/Level:/Last used:,
  the AISuggestionPanel member. Keys already authored; extract against them.
- Estimated logic-diff size for this task: **~14,000 bytes** (84 measured rewrite lines at x2 plus
  the nav/404/chrome/carrier edits), well under the 60,000-byte gates.diffCap. Stated as a floor.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>the chrome rewrite lane — the dynamic carriers and the chrome literals</name>
  <files>the paths in files_modified</files>
  <read_first>the authoring lane's SUMMARY (which keys landed where); the re-derived missAll site list from the strict audit ; navigationData.ts (all 28 labelKeys + category tooltips); __root.tsx NotFoundPage; the four dynamic carrier sites; 99-RESEARCH.md §6.1-§6.2</read_first>
  <action>
    Parts before this one landed in P99-18. Do only what THIS task's acceptance items still require, using the lane method below as the standing method; re-run this task's oracles against the tree the earlier part produced and repair any residue INSIDE files_modified. Author nothing outside them.

    --- THE LANE METHOD (carried verbatim from the pre-recut plan; the acceptance items above bound what THIS task owes) ---

    1. Strict audit unscoped; take this plan's files' slice as the rewrite queue; record it.
    2. Rewrite each site to the form that resolves — into its bound namespace where one exists,
       `common:` colon form otherwise. PROBE each target key in both locales before rewriting; a
       probe miss STOPS the plan and names the key for the authoring lane. Author nothing.
    3. Verify the scoped strict audit reads zero over this plan's files and that
       `git status` shows no JSON change.



    1. Probe the four navigation keys resolving in both locales, then change the NavigationSection
       render to t(item.labelKey) with no fallback, in one commit with the probe recorded.
    2. Repoint the NotFoundPage's four masked sites to common:notFound.{title,message,goBack,goHome}
       and drop their second arguments in the same commit.
    3. Colon-prefix the four dynamic carriers and drop their variable second arguments.
    4. Replace the chrome literals with t() calls against the already-authored keys.
    5. Verify: this plan's first oracle; then the rendered `UI99-C5 ar 404` test and record it.

  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes. The lane's own criteria, carried: The rewrite queue is recorded with its derivation; every rewritten site's target key was probed resolving in both locales first; scoped strict audit zero; zero JSON files in the diff; type-check green. Nav renders keys-only with the four keys probed resolving first; 404 repointed with no English 404 string left and the rendered UI99-C5 test collecting at 1 and passing; four carriers retired with the maskfinder control run first; chrome literals extracted with their absence+presence pairs; no JSON in the diff; type-check green.</acceptance_criteria>
  <done>The nav, the 404, the carriers and the chrome literals are Arabic-capable, and the 404 is proven on the rendered surface by this task's own -g "UI99-C5 ar 404" command oracle — never by grep and never by prose.</done>
</task>

</tasks>

<verification>
POPULATION: the paths in files_modified. OUTSIDE IT: every other task's files, the exogenous paths
(D-03), and every obligation this task's acceptance items do not name — those belong to the lane
parts before and after this one, named in `depends_on` and in the SUMMARY.

The lane's own population statement, carried verbatim from the pre-recut plan:
POPULATION: the source files listed plus this plan's SUMMARY. OUTSIDE IT: every i18n JSON without
exception (the authoring lane), the family lanes' source files, all ar VALUE sweeping (the glossary
lanes), every second-argument drop outside the named atomic units (the deletion lanes), and the
repo-wide zero claims (the gatekeeper).
</verification>

<success_criteria>
Land the dynamic carriers and the chrome literals for the chrome rewrite lane, proven by the acceptance items below in a single pass.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-19-SUMMARY.md` when done. It MUST record every command this task ran with its verbatim output, the
populations it re-derived with the commands that produced them, and anything left for a named later
task. The lane's own SUMMARY contract, carried verbatim from the pre-recut plan (satisfy the parts
this task owns): record in the file named under <output> (99-08-SUMMARY.md in the pre-recut plan) when done. It MUST record: the
rewrite queue derivation and counts; the four nav-key resolution probes; the 404 repoint mapping;
the enum sets per dynamic carrier as colon-prefixed; the chrome extraction key names; the rendered
UI99-C5 output; and the statement that no i18n JSON changed.
</output>
