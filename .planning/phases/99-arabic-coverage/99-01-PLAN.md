---
phase: 99-arabic-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 'tests/e2e/{helpers/settle.ts,99-ar02-dates.spec.ts,99-ar03-leak.spec.ts,fixtures/99-positions-seed.mjs}'
  - .planning/phases/99-arabic-coverage/99-01-SUMMARY.md
autonomous: true
requirements: [AR-02, AR-03]
success_criterion: 'Land the settle helper and the dates spec for the rendered oracles, proven by the acceptance items below in a single pass.'
routing:
  floor: frontier
truths:
  - "Decisions covered — D-07: criteria 2 and 3 close on RENDERED surfaces, so the specs authored here are invoked by `command:` truths in the plans that turn them green and again in the closing battery — a spec that only a <verify> block names is never run by the engine; D-08: both specs are run against the unfixed tree and observed RED before any repair plan runs; D-09: every Playwright invocation names ONE spec path, asserts the file exists, and gates on a HARDCODED expected test count taken from --list, never derived from the list just passed; D-33: every criterion-2/3 assertion sits behind the hoisted settle-and-locale-assertion pair copied from 98-copy04-voice.spec.ts:248-265, NEVER from the pre-law 95/96 helpers; D-34: both locale legs run inside chromium-en via ?lng= plus the locale assertion, never via the ar-smoke project whose ar-SA browser locale produces Indic digits; D-35: the ONE leg this phase inherits as undriven-by-data — the position read-only banner — is repaired with a deterministic fixture, which A-05 as amended makes the PRIMARY remedy; a skip happens only by the overseer's written ruling; D-31: Latin digits are policy, so UI99-C2 asserts ZERO Arabic-Indic codepoints as a regression guard; D-06: every zero-assertion is paired with a positive control in the same run; D-29: UI99-C1..C11 is the binding UI contract and every row this phase can render is driven by one of these two specs."
must_haves:
  truths:
    - 'Decisions covered — D-06, D-07, D-08, D-09, D-29, D-31, D-33, D-34, D-35: rendered oracles authored red-first, one spec path per invocation with hardcoded counts, settle law hoisted from the 98-copy04 pair, both legs via ?lng= inside chromium-en, the C7 data precondition closed by fixture rather than by skip.'
    - "tests/e2e/helpers/settle.ts exports the settle sequence and the locale assertion (page, lng, surface) with mechanism identical to 98-copy04-voice.spec.ts:248-265 (main visible -> best-effort networkidle -> 3s dwell; the root element's lang attribute asserted with the surface named in the failure message), and both specs import from it"
    - '99-ar02-dates.spec.ts declares EXACTLY 8 tests: the ar leg and the en non-regression leg for each of /calendar, /dossiers, /events (6), plus the UI99-C3 relative-time pair on /activity — the ar leg asserting the Arabic relative-time family (منذ followed by a Latin-digit run) and its en leg asserting the English "ago" family survives (2). The count 8 is the number the invoking command oracles hardcode; changing the spec changes that number in every invoking plan in the same commit.'
    - '99-ar03-leak.spec.ts declares EXACTLY 10 tests: UI99-C5 404 (ar) + its en presence control, UI99-C6 /my-work/intake (ar) with the committed capture floor 34 + its en presence control, UI99-C7 as THREE separately counted tests — one per ruled status (under_review, approved, published) each driving its own fixture position — UI99-C8 /search chips (ar), UI99-C9 the Latin-run scan over the named route set, UI99-C10 the Tajawal computed-font probe'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; test -f "$R/tests/e2e/helpers/settle.ts" && test -f "$R/tests/e2e/99-ar02-dates.spec.ts" && test -f "$R/tests/e2e/99-ar03-leak.spec.ts" && test -f "$R/tests/e2e/fixtures/99-positions-seed.mjs" && command grep -q "expectLocale" "$R/tests/e2e/99-ar02-dates.spec.ts" && command grep -q "expectLocale" "$R/tests/e2e/99-ar03-leak.spec.ts" && command grep -q "helpers/settle" "$R/tests/e2e/99-ar02-dates.spec.ts" && command grep -q "helpers/settle" "$R/tests/e2e/99-ar03-leak.spec.ts" && ! command grep -q "95-search-renders\|95-queue-renders\|96-count-agreement" "$R/tests/e2e/99-ar03-leak.spec.ts" && command grep -q "pg_constraint" "$R/tests/e2e/fixtures/99-positions-seed.mjs" && cd "$R" && pnpm exec playwright test tests/e2e/99-ar02-dates.spec.ts --project=chromium-en --no-deps --list 2>&1 | command grep -qE "Total: 8 tests in 1 file" || exit 1; J="$R/.p99-red-dates.json"; node "$R/scripts/pw-run-reaped.mjs" tests/e2e/99-ar02-dates.spec.ts chromium-en "$J"; node "$R/scripts/pw-red-assert.mjs" "$J" 8 "UI99-C1C2C4 ar /calendar"'
      text: 'the two specs, the hoisted settle helper and the C7 fixture exist, import the law-conformant pair, do not build on the pre-law 95/96 helpers, and the fixture verifies the CHECK constraint before inserting — RED at HEAD (none of the four files exists)'
---

<objective>
Land the settle helper and the dates spec for the rendered oracles, proven by the acceptance items below in a single pass.

This task is part 1 of 3 of the rendered oracles; the parts are ordered by `depends_on` and share one
file surface, so each part reads the tree the part before it produced. The task-unit contract caps a
task at six acceptance items and twenty-four surface, which is why the lane is cut this way.
</objective>

<context>
@.planning/phases/99-arabic-coverage/99-CONTEXT.md
@.planning/phases/99-arabic-coverage/99-RESEARCH.md
@tests/e2e/98-copy04-voice.spec.ts
@frontend/src/lib/format-date.ts
@playwright.config.ts

Ref notes: 99-CONTEXT.md — D-06..D-09, D-29..D-35; 99-RESEARCH.md — §5.4, §5.5, §6.1–6.4 — the UI99-C1..C11 contract; 98-copy04-voice.spec.ts — the ONLY law-conformant settle pair — lines ~248-265, floors table ~163; format-date.ts — the red mechanism (four absolute formatters hardcode en-GB; formatRelativeTime already reads i18n.language); playwright.config.ts — webServer gated on E2E_BASE_URL; chromium-en is the project both legs run in

<interfaces>
Verified on disk 2026-08-18 at 9ffa2b80c (strings are anchors, line numbers drift):

- Settle pair: `const settle = async (page: Page)` and `const expectLocale` in
  `tests/e2e/98-copy04-voice.spec.ts` — main visible -> `networkidle` best-effort (`.catch`) ->
  3s dwell; `document.documentElement.lang` asserted. FOUR `const settle` definitions exist in
  `tests/e2e/` (grep it); ONLY the 98-copy04 pair is law-conformant — the 95/96 helpers are
  pre-law and mechanism-divergent (D-33 amendment). Copy the 98-copy04 pair verbatim into the
  helper; do not import from or extend the 95/96 specs.
- Date red mechanism: `frontend/src/lib/format-date.ts` hardcodes `en-GB` in its four absolute
  formatters (doc comments say so), so any `?lng=ar` surface consuming it renders `Tue 28 Apr`
  tokens — the §5.4 discriminator: RED = `/\b(Mon|Tue|Wed|Thu|Fri|Sat|Sun|Jan|Feb|Mar|Apr|May|Jun|Jul|Aug|Sep|Oct|Nov|Dec)\b/`
  inside date cells under ar.
- UI99-C3, the leg the first draft omitted: `/activity` EXISTS
  (`frontend/src/routes/_protected/activity.tsx`) and `formatRelativeTime` at
  `lib/format-date.ts:107` is the only relative-time producer. The ar assertion is the Arabic
  relative family (`منذ` followed by a Latin-digit run per D-31); the en leg asserts the English
  family survives byte-shape. Drive `/activity?lng=ar` and `/activity?lng=en`.
- UI99-C7's three banner sentences live in `frontend/src/routes/_protected/positions/$id/index.tsx`
  around lines 58-64 — a status ternary over `under_review` / `approved` / `published`, each
  sentence containing the literal `Read Only`, with no `t()` at all. THREE separately counted
  tests, one per status, each driving its own seeded position: a single test over "whichever
  status exists" lets two of three branches regress green.
- The fixture: seed via the service-role Supabase client from `.env.test`. **Query the CHECK
  constraint before inserting** —
  `SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conrelid = 'public.positions'::regclass
AND contype = 'c'` — and assert `under_review`, `approved`, `published` are members; a mismatch
  aborts with the constraint text printed rather than inserting a value the constraint rejects.
  Seeded rows carry a recognisable title prefix and are torn down in an afterAll.
- `?lng=` does NOT survive the `/intake/queue`->`/my-work/intake` redirect — locale must be
  ASSERTED on the landed page, never inherited (UI99-C11).
- The 404 surface: `routes/__root.tsx` NotFoundPage — English strings "Page not found",
  "Go back", "Dashboard"; no existing spec navigates a nonexistent route.
- `--list` does NOT start the webServer, so the collection clause in this plan's oracles is cheap
  and cannot be reddened by a dev-stack hiccup; the run clause that follows it can.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>the rendered oracles — the settle helper and the dates spec</name>
  <files>the paths in files_modified</files>
  <read_first>tests/e2e/98-copy04-voice.spec.ts in full (the settle/expectLocale pair, the VISITED_SURFACES + capture-floor mechanism, the ?lng= redirect note); 99-RESEARCH.md §5.4-§5.5; playwright.config.ts ; frontend/src/routes/_protected/positions/$id/index.tsx (the three banner sentences and the status ternary — the `$` in the path is why this file is named here in prose rather than as an `@` context ref, which the compiler drops); 99-RESEARCH.md §6.1-§6.4; .env.test</read_first>
  <action>
    This is the first part of the lane, so the lane method below is yours to execute in full over the paths in files_modified. Re-derive every population before relying on it (D-04), run each oracle and record its verbatim output, and name in the SUMMARY anything you leave for a later part.

    --- THE LANE METHOD (carried verbatim from the pre-recut plan; the acceptance items above bound what THIS task owes) ---

    Hoist `settle(page)` and `expectLocale(page, lng, surface)` from 98-copy04-voice.spec.ts into
    tests/e2e/helpers/settle.ts, mechanism byte-equivalent. Do NOT modify the 98-copy0N specs and
    do NOT touch the 95/96 pre-law helpers.

    99-ar02-dates.spec.ts — EXACTLY 8 tests, each `test(...)` at top level so `--list` counts them:
      1-3 ar legs for /calendar, /dossiers, /events: `?lng=ar`, expectLocale, settle; date-bearing
          cells in main match Arabic month/weekday names, contain Latin digits, and main contains
          ZERO Arabic-Indic codepoints [٠-٩][۰-۹] (UI99-C2, D-31); the calendar/events month
          headers carry an Arabic month name (UI99-C4).
      4-6 en legs for the same three routes: the same cells still match the `Tue 28 Apr` / `MMM`
          English token shape byte-exact — the non-regression leg that makes the ar green
          discriminating.
      7   UI99-C3 ar: /activity?lng=ar — relative-time strings match the Arabic family (`منذ`
          followed by a Latin-digit run), and no English `ago`/`minutes`/`hours` token survives
          in the relative-time cells.
      8   UI99-C3 en: /activity?lng=en — the English relative family still renders (the presence
          control that keeps test 7's absence assertion honest).
    Every absence assertion pairs with a presence control in the same test. Import settle and
    expectLocale from ./helpers/settle. RED today on all four ar legs by mechanism.



    1. tests/e2e/fixtures/99-positions-seed.mjs: FIRST query
       `SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conrelid='public.positions'::regclass
       AND contype='c'` and assert `under_review`, `approved`, `published` appear in it — abort with
       the constraint text if not. Then insert one position per status with a recognisable title
       prefix, export `seedPositions()` returning `{under_review, approved, published}` ids, and
       export a teardown. A seed failure THROWS; it never degrades to a skip.
    2. 99-ar03-leak.spec.ts — EXACTLY 10 tests, all under asserted ar unless stated:
       1  UI99-C5 ar: /definitely-not-a-route-99?lng=ar -> settle -> the four English 404 strings
          absent from main, the Arabic notFound family present.
       2  UI99-C5 en control: the same route under ?lng=en still renders the English strings
          (presence control for test 1).
       3  UI99-C6 ar: /my-work/intake, capture floor >=34 — "Review and classify incoming
          requests", "New Request", "Pending Triage" absent, Arabic present. Locale ASSERTED on
          the landed page (the redirect drops ?lng=).
       4  UI99-C6 en control: the same surface under en still carries those strings.
       5-7 UI99-C7, one test PER STATUS: for each of under_review, approved, published drive
          /positions/<seeded id>?lng=ar and assert the banner is Arabic-script with no 3+ Latin
          run outside the allowlist and no "Read Only" literal. Three tests, three fixtures,
          three counted results — never one test over whichever status happens to exist.
       8  UI99-C8 ar: /search empty-state chips are not the raw literals 'Saudi Arabia'/'UN'/
          'climate'; allowlisted proper nouns (G20) named with their reason.
       9  UI99-C9: over {404, /my-work/intake, /search, /dashboard, /dossiers, /my-work, /calendar}
          no [A-Za-z]{3,} run in main outside the NAMED allowlist (each entry carries a reason
          line: GST per D-30, G20, ISO codes, the IntelDossier brand mark, mono T±N SLA tokens).
       10 UI99-C10: one page.evaluate per ar leg samples a text node + one heading in main and
          asserts computed fontFamily begins with Tajawal.
    3. Run both clauses of this plan's third oracle and record the outputs.

  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes. The lane's own criteria, carried: `playwright test tests/e2e/99-ar02-dates.spec.ts --list` prints `Total: 8 tests`; the run FAILS on the unfixed tree with the ar legs red on English month/weekday tokens and the /activity leg red on the English relative family, while the en legs pass; per-test output captured for the SUMMARY. A spec that fails to collect, times out on collection, or passes anywhere on an ar leg is a defect in the spec, not a green. `--list` prints `Total: 10 tests`; the run FAILS on the unfixed tree on the named English strings and Latin runs; the fixture seeds three positions after printing the constraint text it verified, and its ids appear in the SUMMARY; no test in the file can pass by skipping.</acceptance_criteria>
  <done>The dates oracle exists at exactly 8 collected tests, imports the hoisted law pair, and its RED on the unfixed tree is observed by this task's own command oracle — one spec path, the count hardcoded from --list, the run asserted non-zero — never reported in prose.</done>
</task>

</tasks>

<verification>
POPULATION: the paths in files_modified. OUTSIDE IT: every other task's files, the exogenous paths
(D-03), and every obligation this task's acceptance items do not name — those belong to the lane
parts before and after this one, named in `depends_on` and in the SUMMARY.

The lane's own population statement, carried verbatim from the pre-recut plan:
POPULATION: the five listed files only. OUTSIDE IT: every repair (later waves), the static
instruments (99-02 owns them), the committed loose audit, the 98-copy0N specs, the 95/96 pre-law
helpers, the ar-smoke project (D-34 — not used), and every i18n JSON. The RED register in the
SUMMARY is what makes every later green mean something.
</verification>

<success_criteria>
Land the settle helper and the dates spec for the rendered oracles, proven by the acceptance items below in a single pass.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-01-SUMMARY.md` when done. It MUST record every command this task ran with its verbatim output, the
populations it re-derived with the commands that produced them, and anything left for a named later
task. The lane's own SUMMARY contract, carried verbatim from the pre-recut plan (satisfy the parts
this task owns): record in the file named under <output> (99-01-SUMMARY.md in the pre-recut plan) when done. It MUST record: per spec —
the `--list` output with its count, the RED run output per test; the fixture's constraint query
output and the three seeded ids; and the statement that nothing outside the five files changed.
</output>
