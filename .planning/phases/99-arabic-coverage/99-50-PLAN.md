---
phase: 99-arabic-coverage
plan: 50
type: execute
wave: 10
depends_on: [99-30, 99-34, 99-49]
files_modified:
  - .planning/phases/99-arabic-coverage/99-50-SUMMARY.md
autonomous: true
requirements: [AR-04a, AR-04b]
success_criterion: 'Re-run the repaired literal and nonliteral bilingual gatekeeper before releasing every remaining deletion lane.'
routing:
  floor: frontier
truths:
  - 'RULING-P99-105: this verification-only task is the new DAG conjunction. Every unfinished deletion lane depends on it; it edits no source, test, script, or locale file.'
must_haves:
  truths:
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node "$R/scripts/i18n-dynamic-key-audit.mjs" "$R" --self-check && AUDITJSON="$(mktemp -t p9950audit).json" || { echo "INSTRUMENT-CANNOT-RUN: mktemp failed"; exit 3; }; AUDITERR="$AUDITJSON.err"; node "$R/scripts/i18n-dynamic-key-audit.mjs" "$R" --profile ar04-live --json > "$AUDITJSON" 2>"$AUDITERR"; AUDITST=$?; node -e "JSON.parse(require(\"node:fs\").readFileSync(process.argv[1],\"utf8\"))" "$AUDITJSON" 2>/dev/null || { echo "INSTRUMENT-CANNOT-RUN: the dynamic-key audit produced no parseable JSON (exit $AUDITST), so it did not run to a verdict"; head -3 "$AUDITERR"; rm -f "$AUDITJSON" "$AUDITERR"; exit 3; }; echo "audit-exit=$AUDITST (a GATEKEEPER exits nonzero on CONTENT; JSON parsed, so it reached a verdict)"; AUDITJSON="$AUDITJSON" node -e "const d=JSON.parse(require(\"node:fs\").readFileSync(process.env.AUDITJSON,\"utf8\"));const cp=d.callerPopulations||{};let bad=0;const AGV=\"frontend/src/components/relationships/AdvancedGraphVisualization.tsx\";const pk=u=>(u.file===AGV?\"AGV\":u.file)+\"|\"+(u.proofSite||\"none\");const FENCE={\"AGV|graph-type-map\":2,\"AGV|graph-relationship-map\":1};const seen={};for(const u of (d.unclassified||[])){seen[pk(u)]=(seen[pk(u)]||0)+1}const extra=[];for(const k of Object.keys(seen)){const allow=FENCE[k]||0;if(seen[k]>allow)extra.push(k+\" x\"+(seen[k]-allow))}const shortfall=[];for(const k of Object.keys(FENCE)){if((seen[k]||0)<FENCE[k])shortfall.push(k+\" have \"+(seen[k]||0)+\" want \"+FENCE[k])}console.log(\"unclassified observed=\"+JSON.stringify(seen));if(extra.length){console.error(\"FAIL: unclassified rows OUTSIDE the declared P99-49 fence: \"+JSON.stringify(extra));bad=1}if(shortfall.length){console.error(\"FAIL: the declared fence rows are not all present: \"+JSON.stringify(shortfall)+\" - the gatekeeper must not pass a tree that lost a definitional row either\");bad=1}const co=d.counts||{};console.log(\"missing en=\"+co.missingEn+\" ar=\"+co.missingAr);if(co.missingEn!==0||co.missingAr!==0){console.error(\"FAIL: missing/routing failures remain - en \"+co.missingEn+\" ar \"+co.missingAr);bad=1}const LI=x=>String(x||\"\").startsWith(\"list.\"),entOf=k=>{const q=String(k).split(\".\");return q.length>=3?q.slice(1,-1).join(\".\"):null};const fe={};for(const r of (d.rows||[])){if(!LI(r.family))continue;const e=entOf(r.key);if(e)(fe[r.family]=fe[r.family]||new Set()).add(e)}const lf=Object.keys(fe).sort();if(lf.length===0){console.error(\"INSTRUMENT-CANNOT-RUN: no list rows enumerated, so completeness could not have failed\");process.exit(3)}const ref=[...fe[lf[0]]].sort().join(\"|\");const ragged=lf.filter(f=>[...fe[f]].sort().join(\"|\")!==ref);console.log(\"list completeness: families=\"+lf.length+\" entities=\"+fe[lf[0]].size+\" product=\"+(lf.length*fe[lf[0]].size)+\" listLeaves=\"+cp.listLeaves+\" | DIAGNOSTICS ONLY listSites=\"+cp.listSites+\" lane3Sites=\"+cp.lane3Sites);if(ragged.length){console.error(\"FAIL: list families cover different entity sets: \"+JSON.stringify(ragged));bad=1}if(lf.length*fe[lf[0]].size!==cp.listLeaves){console.error(\"FAIL: families x entities does not equal listLeaves - the cross-product is not whole\");bad=1}rm=0;process.exit(bad)"; ST=$?; rm -f "$AUDITJSON" "$AUDITERR"; exit $ST'
      text: 'the repaired nonliteral census runs after its discriminating self-check and proves exact nonempty caller populations, complete leaf resolution in en and ar, and zero unclassified fallback-bearing calls in the ruled profiles; the gatekeeper asserts ZERO unclassified rows OTHER THAN the ones P99-49''s fence DECLARES - a relation to a declared set, never an absolute expect-3 - and it fails equally if a declared fence row is MISSING, so a tree cannot pass by losing a definitional row either. The frozen --expect-list-sites 9, --expect-list-leaves 153 and --expect-lane3-sites 13 flags are REMOVED (RULING-P99-326 arriving at its third site: they are frozen counts on a moving population, and lane3Sites measured 11 not 13, so that flag alone made this criterion unpassable). In their place the gatekeeper asserts the same base-free list COMPLETENESS relation P99-49 uses, and the counts are printed as diagnostics. The old byte-identity claim to P99-49''s command is dropped: it was false once P99-49 was repaired, and identity of command text was never what made the gatekeeper sound - the shared RELATION is'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node "$R/scripts/i18n-audit-strict.mjs" "$R" --json | node -e "let s=[];process.stdin.on(\"data\",d=>s.push(d)).on(\"end\",()=>{const j=JSON.parse(s.join(\"\"));if(!(j.twoArgTotal>0))process.exit(2);if(j.twoArgUnresolved!==0||j.rawKeyUnresolved!==0||j.twoArgUnresolvedAr!==0||j.rawKeyUnresolvedAr!==0)process.exit(3)})" && { MC=$(python3 "$R/scripts/partA_maskfinder.py" "$R" --control 2>&1); ST=$?; test "$ST" -eq 0 || { echo "INSTRUMENT-CANNOT-RUN: maskfinder --control exited $ST"; exit 3; }; NC=$(printf "%s\n" "$MC" | command grep -c "^CONTROL " || true); test "$NC" -ge 4 || { echo "INSTRUMENT-CANNOT-RUN: control printed $NC CONTROL lines, fewer than the four documented polarities, so they cannot be asserted"; exit 3; }; OKC=$(printf "%s\n" "$MC" | command grep -cE "= True \(expect True\)|= False \(expect False\)" || true); test "$OKC" -eq "$NC" || { echo "FAIL: the mask-finder control is NOT discriminating - $((NC-OKC)) of $NC polarities disagree with their stated expectation:"; printf "%s\n" "$MC"; exit 1; }; echo "mask-finder control polarities asserted: $OKC/$NC"; } && { MF=$(python3 "$R/scripts/partA_maskfinder.py" "$R" 2>&1); ST=$?; test "$ST" -eq 0 || { echo "INSTRUMENT-CANNOT-RUN: mask finder did not report the expected summary exited $ST; its printed summary is not a verdict"; printf "%s\n" "$MF" | tail -3; exit 3; }; printf "%s\n" "$MF" | command grep -q "UNRESOLVED dynamic t() key prefixes: 0" || { echo "FAIL: mask finder did not report the expected summary"; exit 1; }; } && { RC=$(node "$R/scripts/resolve-check.mjs" "$R" 2>&1); ST=$?; test "$ST" -eq 0 || { echo "INSTRUMENT-CANNOT-RUN: resolve-check did not report the expected summary exited $ST; its printed summary is not a verdict"; printf "%s\n" "$RC" | tail -3; exit 3; }; printf "%s\n" "$RC" | command grep -q "routings with a miss: 0" || { echo "FAIL: resolve-check did not report the expected summary"; exit 1; }; } && { NT=$(node "$R/scripts/neg-taskcard.mjs" "$R" 2>&1); ST=$?; test "$ST" -eq 0 || { echo "INSTRUMENT-CANNOT-RUN: neg-taskcard.mjs exited $ST; its MISS rows are not a content verdict"; exit 3; }; NM=$(printf "%s\n" "$NT" | command grep -c "MISS=true" || true); test "$NM" -eq 3 || { echo "FAIL: negative control printed $NM MISS=true rows, expected 3"; exit 1; }; }'
      text: 'the original gatekeeper battery also re-runs on the repaired tree: nonvacuous literal masks remain for the downstream deletion population, all strict unresolved classes are zero in both locales, controls discriminate, routing misses are zero, and the negative control remains 3x MISS=true'
    - 'The SUMMARY keeps the literal and nonliteral populations separate, records every command and verbatim output, names the P99-30 proof as superseded for dynamic-leaf authority, and records any nonzero or unclassified row as RED rather than repairing it.'
    - 'The task diff contains 99-50-SUMMARY.md and nothing else. A source, test, script, or locale change means the gatekeeper edited what it exists to grade and fails scope.'
---

<objective>
Re-run the repaired literal and nonliteral bilingual gatekeeper before releasing every remaining deletion lane.
</objective>

<context>
@.planning/phases/99-arabic-coverage/rulings/RULING-P99-105-DYNAMIC-GATE-REPAIR.md
@.planning/phases/99-arabic-coverage/99-30-SUMMARY.md
@scripts/i18n-audit-strict.mjs
@scripts/partA_maskfinder.py
</context>

<tasks>
<task type="auto">
  <name>run the repaired AR-04a gatekeeper</name>
  <files>the paths in files_modified</files>
  <read_first>RULING-P99-105, then P99-48 and P99-49 summaries; P99-30 is historical evidence only</read_first>
  <action>
    Run the dynamic self-check and live profile, then the complete original gatekeeper battery in the
    authored order. Record outputs verbatim and keep the two populations separate. Any missing,
    unclassified, malformed, empty, or nonzero row stops here and names its owning upstream task; do
    not edit it in a verification-only task.
  </action>
  <acceptance_criteria>Every acceptance item above passes and the SUMMARY is the only changed path.</acceptance_criteria>
  <done>The repaired literal-plus-nonliteral bilingual proof is green in its own gate battery before any unfinished fallback deletion is reachable.</done>
</task>
</tasks>

<verification>
POPULATION: the fresh proof SUMMARY only. OUTSIDE IT: every source, test, script, locale, and deletion;
those are upstream evidence or downstream work, never writable by this gatekeeper.
</verification>

<success_criteria>
Re-run the repaired literal and nonliteral bilingual gatekeeper before releasing every remaining deletion lane.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-50-SUMMARY.md` with all controlled outputs, separate
population tables, explicit supersession of P99-30 for dynamic leaves, and an empty source-diff proof.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete`. Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as **pending** no matter how many gates passed — measured on P99-32 and again on P99-61, which passed 7/7 and still compiled as pending (PRODUCT ENTRY 36). Verify with `node scripts/completion-contract-check.mjs <journal> <ref>`.
</output>
