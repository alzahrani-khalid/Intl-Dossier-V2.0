---
phase: 99-arabic-coverage
plan: 50
type: execute
wave: 10
depends_on: [99-30, 99-34, 99-49]
files_modified:
  - .planning/phases/99-arabic-coverage/99-50-SUMMARY.md
autonomous: true
requirements: [AR-04a, AR-04b]
success_criterion: 'Re-run the repaired literal and nonliteral bilingual gatekeeper before releasing every remaining deletion lane.'
routing:
  floor: frontier
truths:
  - 'RULING-P99-105: this verification-only task is the new DAG conjunction. Every unfinished deletion lane depends on it; it edits no source, test, script, or locale file.'
must_haves:
  truths:
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node "$R/scripts/i18n-dynamic-key-audit.mjs" "$R" --self-check && node "$R/scripts/i18n-dynamic-key-audit.mjs" "$R" --profile ar04-live --expect-list-sites 9 --expect-lane3-sites 13 --expect-unclassified 0 --expect-missing-en 0 --expect-missing-ar 0'
      text: 'the repaired nonliteral census runs after its discriminating self-check and proves exact nonempty caller populations, complete leaf resolution in en and ar, and zero unclassified fallback-bearing calls in the ruled profiles'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node "$R/scripts/i18n-audit-strict.mjs" "$R" --json | node -e "let s=[];process.stdin.on(\"data\",d=>s.push(d)).on(\"end\",()=>{const j=JSON.parse(s.join(\"\"));if(!(j.twoArgTotal>0))process.exit(2);if(j.twoArgUnresolved!==0||j.rawKeyUnresolved!==0||j.twoArgUnresolvedAr!==0||j.rawKeyUnresolvedAr!==0)process.exit(3)})" && python3 "$R/scripts/partA_maskfinder.py" "$R" --control >/dev/null && python3 "$R/scripts/partA_maskfinder.py" "$R" | command grep -q "UNRESOLVED dynamic t() key prefixes: 0" && node "$R/scripts/resolve-check.mjs" "$R" | command grep -q "routings with a miss: 0" && test "$(node "$R/scripts/neg-taskcard.mjs" "$R" | command grep -c "MISS=true")" -eq 3'
      text: 'the original gatekeeper battery also re-runs on the repaired tree: nonvacuous literal masks remain for the downstream deletion population, all strict unresolved classes are zero in both locales, controls discriminate, routing misses are zero, and the negative control remains 3x MISS=true'
    - 'The SUMMARY keeps the literal and nonliteral populations separate, records every command and verbatim output, names the P99-30 proof as superseded for dynamic-leaf authority, and records any nonzero or unclassified row as RED rather than repairing it.'
    - 'The task diff contains 99-50-SUMMARY.md and nothing else. A source, test, script, or locale change means the gatekeeper edited what it exists to grade and fails scope.'
---

<objective>
Re-run the repaired literal and nonliteral bilingual gatekeeper before releasing every remaining deletion lane.
</objective>

<context>
@.planning/phases/99-arabic-coverage/rulings/RULING-P99-105-DYNAMIC-GATE-REPAIR.md
@.planning/phases/99-arabic-coverage/99-30-SUMMARY.md
@scripts/i18n-audit-strict.mjs
@scripts/partA_maskfinder.py
</context>

<tasks>
<task type="auto">
  <name>run the repaired AR-04a gatekeeper</name>
  <files>the paths in files_modified</files>
  <read_first>RULING-P99-105, then P99-48 and P99-49 summaries; P99-30 is historical evidence only</read_first>
  <action>
    Run the dynamic self-check and live profile, then the complete original gatekeeper battery in the
    authored order. Record outputs verbatim and keep the two populations separate. Any missing,
    unclassified, malformed, empty, or nonzero row stops here and names its owning upstream task; do
    not edit it in a verification-only task.
  </action>
  <acceptance_criteria>Every acceptance item above passes and the SUMMARY is the only changed path.</acceptance_criteria>
  <done>The repaired literal-plus-nonliteral bilingual proof is green in its own gate battery before any unfinished fallback deletion is reachable.</done>
</task>
</tasks>

<verification>
POPULATION: the fresh proof SUMMARY only. OUTSIDE IT: every source, test, script, locale, and deletion;
those are upstream evidence or downstream work, never writable by this gatekeeper.
</verification>

<success_criteria>
Re-run the repaired literal and nonliteral bilingual gatekeeper before releasing every remaining deletion lane.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-50-SUMMARY.md` with all controlled outputs, separate
population tables, explicit supersession of P99-30 for dynamic leaves, and an empty source-diff proof.
</output>
