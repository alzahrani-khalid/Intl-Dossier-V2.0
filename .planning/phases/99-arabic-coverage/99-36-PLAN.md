---
phase: 99-arabic-coverage
plan: 36
type: execute
wave: 10
depends_on: [99-50, 99-61]
files_modified:
  - '{frontend/src/pages/Organizations.tsx,frontend/src/pages/TaskDetailPage.tsx,frontend/src/pages/TicketDetail.tsx,frontend/src/pages/WaitingQueue.tsx}'
  - .planning/phases/99-arabic-coverage/99-36-SUMMARY.md
autonomous: true
requirements: [AR-04a]
success_criterion: 'Land the deletion for mask deletion lane 6 part 1 of 3, proven by the acceptance items below in a single pass.'
routing:
  floor: frontier
truths:
  - "Decisions covered — D-24: this lane is reachable only behind the verification-only gatekeeper, which proved repo-wide strict zero in both locales in its OWN gate battery — the conjunction order is a DAG property here, not a worker's intention; D-20: the drop is a DELETION, not a conversion — no working dot-form key is rewritten, because churning ~1295 already-resolving sites is the breakage D-20 forbids; D-23 as clarified 2026-08-18 (RULING-P99-05 §4): deleting a redundant second argument at a site whose resolution was just verified is NOT a binding-level change, so colon form is not forced here — that would be D-21's wholesale conversion arriving early; D-28: this lane closes on the two-arg population only and cites no blended AR-04a/AR-04b figure; D-06: every zero is paired with a positive control in the same run; D-39: this lane is FILE-DISJOINT from every sibling deletion lane, which is why they run concurrently."
must_haves:
  truths:
    - "Decisions covered — D-06, D-20, D-23, D-24, D-28, D-39: deletion-only, behind the gatekeeper, no conversion, one population, every zero controlled, file-disjoint from its siblings. || Both mask shapes are deleted across this lane's files: `t('key', 'Literal')` -> `t('key')`, `t('key', 'Literal', opts)` -> `t('key', opts)`, and the object-form fallback-text option is removed while every remaining option is kept — because interpolation options are not masks. The object-form fallback class is a SEPARATE population from the literal class: 314 sites across 103 files repo-wide, 88 of which carry no literal-class site at all, and the first draft's single 161-file scope covered only the literal class while demanding both reach zero."
    - "COMPANION TESTS — This part owns NO companion test: no test file that asserts a literal it deletes imports any of its sources, verified by `.tickmarkr/overseer/verify-lane-closure.mjs`, which RESOLVES imports rather than matching basenames — a basename match reported a trap here that does not exist. If a deletion here reds a test outside files_modified, STOP and record it; do not edit another part''s surface. || KEYS are byte-untouched. The diff contains deletions of default arguments and nothing else — no key string, no namespace prefix, no hook, no import moves. Spot-diff a sample and state it. COMPANION TEST FIXTURES, OWNED AND CONDITIONALLY REPAIRABLE (RULING-P99-429). The companion tests named in files_modified are owned by this lane. TRIGGER: if this lane's deletions cause one of them to render or assert a BARE i18n KEY as visible text or accessible name, it MUST be repaired as below; OTHERWISE it MUST be left BYTE-UNCHANGED - ownership grants authority to repair a break this lane causes, never a mandate to rewrite a test that still passes. WHEN REPAIRED: (a) expected copy comes from a STATIC top-level import of the relevant frontend/src/i18n/{en,ar}/<ns>.json, as DossierEngagementsTab.test.tsx:6-7 does, and any hand-copied copy object is deleted; (b) the react-i18next mock RESOLVES keys against the real bundle loaded with await vi.importActual of that same JSON INSIDE the vi.mock factory - a static import cannot serve this side because vi.mock is hoisted above it - with no private copy, no default-argument fallback and no key fallback; (c) the resolved en AND ar values are both asserted, the ar assertion being what proves the fallback is gone; (d) the rendered output must NOT contain the bare key; (e) existing assertions still exist and still run, not deleted, skipped, .only-ed or loosened. AND NO PRODUCTION-SIDE ESCAPE: the suite is made green by repairing the TEST. No production change may be made whose EFFECT is that an assertion passes independently of whether the key resolves - that covers a literal second-argument default, an object-form default option, and any conditional, wrapper, concatenation or logical-or that substitutes for or augments the value returned by t(). If a key fails to resolve, production must render the failure, not disguise it. || NO i18n JSON changes in this lane — a JSON edit here means the gatekeeper's proof was wrong or scope broke"
    - 'A mask site found in a file OUTSIDE this lane''s list means the tree moved between the gatekeeper''s manifest and this lane: STOP and record it, do not widen scope || This part''s logic-diff budget is **~34309 bytes** against the engine''s 60,000-byte gates.diffCap. It is NOT a plan-time floor: it is the pre-split lane''s ENGINE-MEASURED 107645-byte diff (fetchTaskDiff at the attempt''s own HEAD, 1.79x over cap), apportioned by this part''s 80/251 share of the lane''s mask sites — so it inherits a measurement, not an estimate, and it inherits the pre-split churn too, which makes it an OVER-estimate of deletion-only work. On top of that it carries 0 companion-test allowance(s) at 5,100 bytes each — MEASURED, not guessed: the one companion repaired in the landed lane 5 cost 5,036 logic-diff bytes. Test companions carry no masks, so they cost budget without earning apportioned budget (RULING-P99-468 order 4). Every part is held at or under 45,000 bytes, >=25% under the 60,000 cap, per RULING-P99-465 and the decomposition rule. The pre-split floor for this lane read ~44066 bytes and the truth was 107645: a 2x miss, which is exactly why a measured apportionment replaces it. A diff-cap trip returns park:"human" with the engine''s own note that the diff cannot shrink by retrying — it is un-retryable, after the work is done, which is why the parts are cut this small.'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node "$R/scripts/i18n-audit-strict.mjs" "$R" --scope "frontend/src/pages/Organizations.tsx,frontend/src/pages/TaskDetailPage.tsx,frontend/src/pages/TicketDetail.tsx,frontend/src/pages/WaitingQueue.tsx" --json | node -e "let s=[];process.stdin.on(\"data\",d=>s.push(d)).on(\"end\",()=>{const j=JSON.parse(s.join(\"\"));if(j.twoArgTotal!==0){console.error(\"mask sites survive in this lane: \"+j.twoArgTotal);process.exit(1)}if(j.rawKeyTotal===0){console.error(\"POSITIVE CONTROL FAILED — the scope filter matched no t() sites at all, so this zero is about the empty set\");process.exit(1)}})" && { SF=("$R/frontend/src/pages/Organizations.tsx" "$R/frontend/src/pages/TaskDetailPage.tsx" "$R/frontend/src/pages/TicketDetail.tsx" "$R/frontend/src/pages/WaitingQueue.tsx"); MISS=0; for f in "${SF[@]}"; do test -r "$f" || MISS=$((MISS+1)); done; test "$MISS" -eq 0 || { echo "INSTRUMENT-CANNOT-RUN: $MISS of ${#SF[@]} scoped files unreadable, so an empty search result cannot be read as absence; this leg verified NOTHING"; exit 3; }; HITS=$(node "$R/scripts/i18n-fallback-option-scan.mjs" "${SF[@]}") || { echo "INSTRUMENT-CANNOT-RUN: the multi-line fallback-option scan could not run, so an empty result cannot be read as absence"; exit 3; }; test -z "$HITS" || { echo "FAIL: defaultValue fallback survives in the scoped set:"; printf "%s\n" "$HITS" | head -5; exit 1; }; echo "scoped-files=${#SF[@]} defaultValue-hits=0"; }'
      text: "the strict instrument's twoArgTotal — EVERY mask site, resolved or not, seen by the cross-line matcher — reads ZERO across this lane's files, with a nonzero rawKeyTotal as the positive control that the scope matched real t() calls, AND no fallback-text option survives in them. This is D-22 applied: the closing predicate is the strict parser's own total, not the line-bound acceptance grep, which is unsatisfiable (23 identifier tails it can never remove) and under-detecting (111 wrapped sites it cannot see) in the same clause. RED at HEAD (80 mask sites in this part)."
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; cd "$R" || { echo "INSTRUMENT-CANNOT-RUN: repo root unreadable"; exit 3; }; BR=$(git -C "$R" rev-parse --abbrev-ref HEAD) || { echo "INSTRUMENT-CANNOT-RUN: cannot read the current branch"; exit 3; }; RUNBR="${BR%--*}"; test "$RUNBR" != "$BR" || { echo "INSTRUMENT-CANNOT-RUN: branch $BR is not a tickmarkr task branch, so the task base cannot be derived; refusing to compare against an assumed base"; exit 3; }; git -C "$R" rev-parse --verify --quiet "$RUNBR" >/dev/null || { echo "INSTRUMENT-CANNOT-RUN: run branch $RUNBR not found"; exit 3; }; TASKBASE=$(git -C "$R" merge-base HEAD "$RUNBR") || { echo "INSTRUMENT-CANNOT-RUN: merge-base failed"; exit 3; }; case "$TASKBASE" in "") echo "INSTRUMENT-CANNOT-RUN: derived an empty task base"; exit 3;; esac; I18NCHANGED=$(git -C "$R" diff --name-only "$TASKBASE"..HEAD -- frontend/src/i18n) || { echo "INSTRUMENT-CANNOT-RUN: git diff over the task range failed"; exit 3; }; NI=$(printf "%s" "$I18NCHANGED" | command grep -c . || true); echo "task-base=$TASKBASE i18n-files-changed-by-this-lane=$NI"; test "$NI" -eq 0 || { echo "FAIL: this lane changed $NI i18n file(s) across $TASKBASE..HEAD: $I18NCHANGED"; exit 1; }; node "$R/scripts/i18n-audit-strict.mjs" "$R" --scope "frontend/src/pages/Organizations.tsx,frontend/src/pages/TaskDetailPage.tsx,frontend/src/pages/TicketDetail.tsx,frontend/src/pages/WaitingQueue.tsx" --json | node -e "let s=[];process.stdin.on(\"data\",d=>s.push(d)).on(\"end\",()=>{const j=JSON.parse(s.join(\"\"));if(j.twoArgUnresolved!==0||j.rawKeyUnresolved!==0||j.twoArgUnresolvedAr!==0||j.rawKeyUnresolvedAr!==0){console.error(\"the drop created unresolved sites — a key was rewritten, not just a default deleted\",JSON.stringify(j).slice(0,300));process.exit(1)}})" && { NT=$(node "$R/scripts/neg-taskcard.mjs" "$R") || { echo "INSTRUMENT-CANNOT-RUN: neg-taskcard.mjs exited nonzero; its MISS count cannot be trusted as a content verdict"; exit 3; }; NM=$(printf "%s" "$NT" | command grep -c "MISS=true" || true); echo "negative-control MISS rows=$NM"; test "$NM" -eq 3 || { echo "FAIL: negative control printed $NM MISS=true rows, expected 3 (producer exited 0, so this is a real content change)"; exit 1; }; }'
      text: 'no i18n JSON changed in this lane, the scoped strict audit still reads zero UNRESOLVED after the drop (a nonzero here means a key moved, which is the one way a deletion-only diff can go wrong), and the phase negative control still prints 3x MISS=true — RED at HEAD'
---

<objective>
Land the deletion for mask deletion lane 6, proven by the acceptance items below in a single pass.

This task is part 1 of 4 of mask deletion lane 6. The three parts are FILE-DISJOINT and run
CONCURRENTLY — no part reads a tree another part produced, so none depends on another. The lane was cut
in three because its pre-split single-task form measured 107645 logic-diff bytes against the engine''s
60,000-byte gates.diffCap (RULING-P99-465). The task-unit contract caps a
task at six acceptance items and twenty-four surface, which is why the lane is cut this way.
</objective>

<context>
@.planning/phases/99-arabic-coverage/99-CONTEXT.md
@.planning/phases/99-arabic-coverage/99-RESEARCH.md
@.planning/phases/99-arabic-coverage/rulings/RULING-P99-07-B05-DIFFCAP.md
@scripts/i18n-audit-strict.mjs
@scripts/neg-taskcard.mjs

Ref notes: 99-CONTEXT.md — D-20, D-23 as clarified, D-24, D-28; 99-RESEARCH.md — §1 the mask population and the order constraint; RULING-P99-07 — why this lane is a slice and what its byte budget is; i18n-audit-strict.mjs — from 99-02; its twoArgTotal is the closing predicate, the acceptance grep is a diagnostic

<interfaces>
Re-derived on disk 2026-08-18 at 9ffa2b80c; the gatekeeper's SUMMARY carries the authoritative
manifest and this lane reconciles against it rather than against these plan-time floors:

- This lane's slice: **4 source files**, **80 mask sites** (both classes), estimated
  **~34309 logic-diff bytes** at x2 per changed line — a modified line is a removed line plus an
  added line in a -U0 logic diff. Measured with the committed audit's cross-line matcher.
- The deletion shapes are in must_haves. Variable second arguments at the dynamic sites were
  already dropped by their owning authoring lanes as part of their atomic enum units; the
  maskfinder's zero at the gatekeeper confirms none survived.
- After the drop, working dot-form keys REMAIN dot-form. They resolve, and ROADMAP criterion 4 is
  satisfied because no English default exists anywhere; the wholesale dot->colon conversion is
  D-21's recorded NO-SHIP, queued as new scope for Phase 102.
- Sibling deletion lanes run concurrently with this one and are file-disjoint from it by
  construction (D-39). Nothing in this lane reads or writes a sibling's file.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>mask deletion lane 6 part 1 of 3 — the deletion</name>
  <files>the paths in files_modified</files>
  <read_first>the gatekeeper's SUMMARY (the authoritative two-class manifest); the deletion shapes in must_haves</read_first>
  <action>
    This is the first part of the lane, so the lane method below is yours to execute in full over the paths in files_modified. Re-derive every population before relying on it (D-04), run each oracle and record its verbatim output, and name in the SUMMARY anything you leave for a later part.

    --- THE LANE METHOD (carried verbatim from the pre-recut plan; the acceptance items above bound what THIS task owes) ---

    1. Reconcile this lane's file list against the gatekeeper's manifest; a site in a file outside
       the list STOPS the lane and is recorded.
    2. Remove the second-argument literal and the fallback-text option at every mask site per the
       deletion shapes. Keys, namespaces, hooks, imports: byte-untouched.
    3. Re-run the scoped strict audit (twoArgTotal zero, unresolved still zero, rawKeyTotal nonzero
       as the control) and the fallback-text-option grep; confirm `git diff` touches no i18n JSON;
       neg-taskcard still 3x MISS=true; frontend type-check green.

  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes. The lane's own criteria, carried: The diff is deletions of default arguments only — spot-diff a sample and state it; scoped twoArgTotal zero with the rawKeyTotal control nonzero; scoped unresolved still zero (proving no key moved); no JSON in the diff; neg-taskcard undefused; type-check green.</acceptance_criteria>
  <done>No t() call in this slice passes an English default; a missing key now shows as missing in both locales — which the gatekeeper's proven zero makes an empty set today.</done>
</task>

</tasks>

<verification>
POPULATION: the paths in files_modified. OUTSIDE IT: every other task's files, the exogenous paths
(D-03), and every obligation this task's acceptance items do not name — those belong to the lane
parts before and after this one, named in `depends_on` and in the SUMMARY.

The lane's own population statement, carried verbatim from the pre-recut plan:
POPULATION: this part's 4 source files, deletion-only. OUTSIDE IT: every i18n JSON (a JSON edit
here is a scope defect), the sibling deletion lanes' files, test files (the strict instrument
excludes `*.test.*` and `__tests__` from both classes, one stated policy), the wholesale dot->colon
conversion (D-21 NO-SHIP), and the rendered re-proofs (the closing plan).
</verification>

<success_criteria>
Land the deletion for mask deletion lane 6, proven by the acceptance items below in a single pass.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-36-SUMMARY.md` when done. It MUST record every command this task ran with its verbatim output, the
populations it re-derived with the commands that produced them, and anything left for a named later
task. The lane's own SUMMARY contract, carried verbatim from the pre-recut plan (satisfy the parts
this task owns): record in the file named under <output> (99-22-SUMMARY.md in the pre-recut plan) when done. It MUST record: the drop
site/file counts reconciled against the gatekeeper's manifest, by class; the scoped zero register
with its controls; the sampled spot-diff showing deletions only; and the explicit statement that no
key was rewritten and no JSON changed.
</output>
