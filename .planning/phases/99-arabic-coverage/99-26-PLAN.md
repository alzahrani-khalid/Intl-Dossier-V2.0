---
phase: 99-arabic-coverage
plan: 26
type: execute
wave: 7
depends_on: [99-25]
files_modified:
  - 'frontend/src/i18n/{ar/activity-feed.json,ar/admin.json,ar/agenda.json,ar/ai-admin.json,ar/ai-brief.json,ar/audit-logs.json,ar/calendar-sync.json,ar/citations.json,ar/common.json,ar/contacts.json,ar/copilot.json,ar/dashboard-widgets.json,ar/delegation.json,ar/dossier-context.json,ar/dossier-drawer.json,ar/dossier.json,ar/elected-official-wizard.json,ar/elected-officials.json,ar/empty-states.json,ar/engagement-briefs.json,ar/engagements.json,ar/entity-comparison.json,ar/entity-linking.json,ar/fab.json,ar/field-permissions.json,ar/form-wizard.json,ar/graph.json,ar/guided-tours.json,ar/intake.json,ar/keyboard-shortcuts.json,ar/legislation.json,ar/multilingual.json,ar/persons.json,ar/positions.json,ar/preview-layouts.json,ar/progressive-disclosure.json,ar/progressive-form.json,ar/settings.json,ar/stakeholder-interactions.json,ar/tags.json,ar/workflow-automation.json,ar/workspace.json}'
  - scripts/glossary-senses.d/brief-stance.json
  - .planning/phases/99-arabic-coverage/99-26-SUMMARY.md
autonomous: true
requirements: [AR-01]
success_criterion: 'Land the sense-aware sweep for glossary C, the brief-artifact and stance families, proven by the acceptance items below in a single pass.'
routing:
  floor: frontier
truths:
  - "Decisions covered — D-18: brief = ملخص for the ARTIFACT with إحاطة reserved for briefing SESSIONS and موجز swept — the artifact/session split is glossary law; position-as-stance = موقف with منصب reserved for office/post, and the تطوير المنصب wrong-sense instance repairs to the موقف family; D-19: term values stay in i18n leaves, never inlined in code, so a term swap remains a mechanical leaf-rename + parity re-run through phase close; D-04: the census is re-derived at task start and again after, with both numbers recorded; D-05: what falls OUTSIDE this lane's population is stated — the other term families and the other ar-bundle slices; D-39: this lane is FILE-DISJOINT from every lane that can run beside it, which is why it owns a slice of the ar bundles rather than the whole tree."
must_haves:
  truths:
    - "Decisions covered — D-04, D-05, D-17, D-18, D-19, D-39: sense-aware sweep over one term family and one file slice, classification recorded as a committed artifact, populations re-derived. || Every occurrence of موجز / منصب in this lane's ar bundles is CLASSIFIED — either rewritten to ملخص / موقف or listed in scripts/glossary-senses.d/brief-stance.json with its key path and its sense. The census refuses to go green while any occurrence is UNCLASSIFIED, and that refusal is the whole point: it is what stops a blanket substring rewrite from passing as judgment."
    - 'إحاطة must SURVIVE at its session-sense paths (the over-sweep guard: sweeping it is the destructive move) and منصب must survive only at office-sense paths || The sweep touches ar VALUES only: no ar leaf KEY is renamed, no en value changes, no source file changes, and bundle parity is structurally unchanged — a structural diff in any ar file is a defect'
    - "This lane's overlay file only ADDS rows to the base allowlist; removing or weakening a base row (the already-ruled sense exceptions) is a red, because that is how a sweep launders an unclassified occurrence into an allowed one || The re-derived before and after counts for this lane's term row are recorded with their commands (D-04), and what falls outside this lane's slice is named (D-05)"
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node "$R/scripts/glossary-census.mjs" "$R" --control && node "$R/scripts/glossary-census.mjs" "$R" --row brief-artifact && node "$R/scripts/glossary-census.mjs" "$R" --row stance'
      text: 'the drilled census, its planted control caught first, reads ZERO unclassified occurrences for the brief-artifact row AND the stance row REPO-WIDE — this lane owns every ar bundle carrying موجز or منصب — RED at HEAD (the census does not exist until 99-02, then red on 177 unclassified lines across 42 files)'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node -e "const fs=require(\"fs\"),path=require(\"path\");const R=process.argv[1];const dir=R+\"/frontend/src/i18n\";const leaves=(o,p)=>Object.entries(o).flatMap(([k,v])=>(v&&typeof v===\"object\")?leaves(v,p+k+\".\"):[p+k]);let checked=0,bad=0;for(const f of fs.readdirSync(dir+\"/en\")){if(!f.endsWith(\".json\"))continue;const en=JSON.parse(fs.readFileSync(dir+\"/en/\"+f,\"utf8\"));const ar=JSON.parse(fs.readFileSync(dir+\"/ar/\"+f,\"utf8\"));const a=new Set(leaves(ar,\"\"));for(const k of leaves(en,\"\")){checked++;if(!a.has(k)){console.error(\"parity broken \"+f+\":\"+k);bad++}}}if(checked===0){console.error(\"POSITIVE CONTROL FAILED — no leaves walked\");process.exit(1)}if(bad)process.exit(1);const ov=JSON.parse(fs.readFileSync(R+\"/scripts/glossary-senses.d/brief-stance.json\",\"utf8\"));if(!Array.isArray(ov.rows)||ov.rows.length===0){console.error(\"this lane recorded no classification rows — either nothing was judged or the trail was not committed\");process.exit(1)}console.log(\"SWEEP-STRUCT-OK checked=\"+checked+\" rows=\"+ov.rows.length)" "$R"'
      text: 'bundle parity is structurally unchanged repo-wide with the walked-leaf count as its positive control, AND this lane committed a non-empty classification overlay — a sweep that judged nothing recorded nothing, and that is a red — RED at HEAD (the overlay file does not exist)'
---

<objective>
Land the sense-aware sweep for glossary C, the brief-artifact and stance families, proven by the acceptance items below in a single pass.

This task is part 1 of 1 of glossary C, the brief-artifact and stance families; the parts are ordered by `depends_on` and share one
file surface, so each part reads the tree the part before it produced. The task-unit contract caps a
task at six acceptance items and twenty-four surface, which is why the lane is cut this way.
</objective>

<context>
@.planning/phases/99-arabic-coverage/99-CONTEXT.md
@.planning/phases/99-arabic-coverage/99-RESEARCH.md
@.planning/phases/99-arabic-coverage/rulings/RULING-P99-07-B05-DIFFCAP.md
@scripts/glossary-census.mjs
@scripts/glossary-senses.json

Ref notes: 99-CONTEXT.md — D-04, D-05, D-17..D-19, D-39; 99-RESEARCH.md — §4.3-§4.4 the census with its sense-pollution notes; RULING-P99-07 — why this lane is a slice and what its byte budget is; glossary-census.mjs — the drilled instrument from 99-02 — run --control first, never let it rewrite

<interfaces>
Re-derived on disk 2026-08-18 at 9ffa2b80c over `frontend/src/i18n/ar/*.json`; commands in
99-REVISION-1.md §Derivations. RE-DERIVE at task start (D-04) — these are the plan-time floors:

- This lane's slice: **42 ar bundle files**, **177 lines** carrying موجز / منصب.
- Estimated logic-diff size: **~24644 bytes** — changed lines counted at x2 (a modified line is a
  removed line plus an added line in a -U0 logic diff), measured with the MULTI-LINE-capable walk
  over the actual bundle text rather than with a line-bound grep. Budget per lane is ~45,000, kept
  deliberately below the 60,000 cap because these numbers are FLOORS: the sweep may touch adjacent
  lines. A lane that measures over budget at execution is re-cut before the work starts, never
  parked after.
- Re-derived at this sha: موجز 128 occurrences / 124 lines / 28 files; منصب 53/53/18; إحاطة 65/65/16; تطوير المنصب 1/1/1. The occurrence and line counts differ because some lines carry two occurrences — two populations, both stated (D-05).
- The classification rule, which is the lane's actual method: for EVERY occurrence, either the value
  is rewritten to the ruled term, or the key path goes into this lane's overlay with a one-line
  sense reason. There is no third outcome, and "I looked and it seemed fine" is not one of the two.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>glossary C, the brief-artifact and stance families — the sense-aware sweep</name>
  <files>the paths in files_modified</files>
  <read_first>the D-17/D-18 rows in 99-CONTEXT.md; scripts/glossary-senses.json (the base allowlist and the senses already ruled); 99-RESEARCH.md §4.3</read_first>
  <action>
    This is the first part of the lane, so the lane method below is yours to execute in full over the paths in files_modified. Re-derive every population before relying on it (D-04), run each oracle and record its verbatim output, and name in the SUMMARY anything you leave for a later part.

    --- THE LANE METHOD (carried verbatim from the pre-recut plan; the acceptance items above bound what THIS task owes) ---

    1. Run the census with --control FIRST — an instrument whose planted control is not caught is
       not trusted for this sweep, whatever it prints afterwards. Then run it over this lane's slice
       and record the before counts.
    2. For every occurrence in this slice: decide the SENSE, then either rewrite the value to the
       ruled term or add {term, file, keyPath, sense, reason} to this lane's overlay. Never run a
       substring replace across the slice — that is the destructive class D-17 names by name, and
       the census is built so that its output cannot distinguish a swept occurrence from a judged
       one unless you actually judge.
    3. Re-run the census; record the after counts; confirm zero unclassified for this row and slice.
    4. Confirm bundle parity is structurally unchanged and no en value, key name or source file
       moved.

  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes. The lane's own criteria, carried: The control was run before the sweep and caught its planted occurrence; before and after counts recorded with their commands; zero unclassified occurrences for this row in this slice; a non-empty overlay committed; parity structurally unchanged; no en value, key name or source file touched.</acceptance_criteria>
  <done>This slice reads one Arabic term for the brief-artifact and stance object, with every exception judged and recorded rather than assumed.</done>
</task>

</tasks>

<verification>
POPULATION: the paths in files_modified. OUTSIDE IT: every other task's files, the exogenous paths
(D-03), and every obligation this task's acceptance items do not name — those belong to the lane
parts before and after this one, named in `depends_on` and in the SUMMARY.

The lane's own population statement, carried verbatim from the pre-recut plan:
POPULATION: this lane's ar bundle slice plus its overlay. OUTSIDE IT: the other term families, the
other ar-bundle slices, every en value, every key NAME, every source file, Arabic NATURALNESS beyond
the ruled rows (operator's, deferred), and the repo-wide census (the closing battery).
</verification>

<success_criteria>
Land the sense-aware sweep for glossary C, the brief-artifact and stance families, proven by the acceptance items below in a single pass.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-26-SUMMARY.md` when done. It MUST record every command this task ran with its verbatim output, the
populations it re-derived with the commands that produced them, and anything left for a named later
task. The lane's own SUMMARY contract, carried verbatim from the pre-recut plan (satisfy the parts
this task owns): record in the file named under <output> (99-12-SUMMARY.md in the pre-recut plan) when done. It MUST record: the
control result; the before and after census counts with their commands; the overlay row count and a
sample of the sense reasons; the parity assertion; and what falls outside this lane's slice.
</output>
