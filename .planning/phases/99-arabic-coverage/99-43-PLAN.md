---
phase: 99-arabic-coverage
plan: 43
type: execute
wave: 7
depends_on: [99-06, 99-07, 99-10, 99-12, 99-22]
# RULING-P99-163 split (part C): the dedicated instrument-cutover task, sequenced LAST. It
# consumes the keys 99-12 (lane bundles) and 99-22 (the three common.json items) authored.
# 99-10 carries ONE edge that is NOT a data dependency: the PORT-CONTENTION serialization from
# RULING-P99-19 (c-iv) travels with the rendered oracle, which lives here now. REMOVE that edge
# the day per-worktree port plumbing lands (c-iii).
files_modified:
  - 'frontend/src/{components/tasks/TaskDetail.tsx,components/tasks/TaskCard.tsx,components/tasks/AddContributorDialog.tsx,components/tasks/ContributorsList.tsx,components/waiting-queue/AssignmentDetailsModal.tsx,components/waiting-queue/ReminderButton.tsx,components/waiting-queue/AgingIndicator.tsx,components/waiting-queue/EscalationDialog.tsx,pages/WaitingQueue.tsx,pages/TaskDetailPage.tsx,components/commitment-editor/CommitmentEditor.tsx,routes/_protected/positions/$id/index.tsx}'
  - 'frontend/{tests/setup.ts,tests/component/AfterActionForm.test.tsx,tests/component/DecisionList.test.tsx,tests/component/BulkActionToolbar.test.tsx,tests/component/FilterPanel.test.tsx,tests/component/SLAIndicator.test.tsx,tests/component/BriefGenerationPanel.manual.test.tsx,tests/component/AssignmentDetailsModal.test.tsx,tests/component/CommitmentList.test.tsx,tests/component/ContributorsList.test.tsx,tests/component/EscalationDialog.test.tsx,tests/component/ReminderButton.test.tsx,tests/component/TaskCard.test.tsx,tests/unit/FormInput.test.tsx,tests/accessibility/waiting-queue-a11y.test.tsx,src/components/after-actions/__tests__/AfterActionsTable.test.tsx,src/components/forms/__tests__/SearchableSelect.a11y.test.tsx,src/components/FirstRun/FirstRunModal.test.tsx,src/components/settings/__tests__/SettingsLayout.test.tsx,src/components/list-page/__tests__/EngagementsList.test.tsx,src/pages/engagements/__tests__/EngagementsListPage.test.tsx,src/components/dossier/DossierDrawer/__tests__/DossierDrawer.test.tsx}'
  - .planning/phases/99-arabic-coverage/99-43-SUMMARY.md
autonomous: true
requirements: [AR-03, AR-04a, AR-04b]
success_criterion: 'Cut the test instrument over to real bundles and land colon form for the tasks/queues/positions lane, with every collateral suite retargeted per the committed ledger, proven by the acceptance items below in a single pass.'
routing:
  floor: frontier
  pin: { via: omp, model: anthropic/claude-opus-5 }
truths:
  - "Decisions covered — D-07, D-11, D-18, D-24(1-2), D-26, D-27, D-29, D-32, D-35: TaskCard is the recorded broken exemplar and colon-prefixes to assignments:, the ruled near-neighbour; the six dynamic carriers repair as atomic enum units verified through partA_maskfinder; the banner extraction closes UI99-C7 on the THREE RENDERED tests over 99-01's committed fixture; D-07 — every criterion member closes on a rendered surface or a drilled instrument, never source text alone; this task is the LAST of the RULING-P99-163 split and authors NOTHING — every value it retargets onto was authored by 99-12 and 99-22."
must_haves:
  truths:
    - "Decisions covered — D-07, D-11, D-26, D-27, D-29, D-32, D-35, and RULING-P99-163 (colon form + banner + instrument cutover + ledger retargets in one atomic pass, authoring nothing) — enforced together with ALL of the following as ONE conjunctive item, no clause passing while another fails: changes to frontend/tests/setup.ts are LIMITED to mirroring production i18n resource loading (namespaces, resources, the translation→common alias, fallback config exactly as src/i18n/index.ts loads them) — no fallback hacks, no key-echo shims, no exact-key exemptions of any kind; every colon-form t() resolves through the real i18next instance. The six component tests under frontend/tests/component/ named in files_modified from the former plan, the sole component test under frontend/src/components/after-actions/__tests__/, and the sole a11y test under frontend/src/components/forms/__tests__/ are under the values-tracking rule: assertions update ONLY to authored bundle values at unchanged query and shape strength, never deleted, loosened, made shape-insensitive, or pointed at test-only fixtures. The thirteen collateral suites named in .planning/phases/99-arabic-coverage/99-12-ATT2-LEDGER.md §0.1/§0.2 change ONLY as the ledger's per-assertion remedy column specifies — the two decisions/confidence rows retarget onto 99-22's repaired interpolation-carrying copy — and the acceptance judge and the reviewer verify every test-file diff against exactly this rule and the ledger."
    - 'The two zero-run suites under frontend/src/components/list-page/__tests__/ and frontend/src/pages/engagements/__tests__/ (ledger §0.2) have their OWN per-file mocks repaired — the router mock gains the missing export its page renders; the stale react-i18next factory is completed or removed — so both files COLLECT AND RUN; and the three banner sentences are replaced by t() calls onto the positions: readOnlyBanner keys 99-12 authored: the literal "Read Only" no longer appears in the route file while a positions: t() call does (absence+presence pair); scripts/neg-taskcard.mjs is NOT edited and still prints 3× MISS=true'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node "$R/scripts/i18n-audit-strict.mjs" "$R" --scope "frontend/src/components/tasks/TaskDetail.tsx,frontend/src/components/tasks/TaskCard.tsx,frontend/src/components/tasks/AddContributorDialog.tsx,frontend/src/components/tasks/ContributorsList.tsx,frontend/src/components/waiting-queue/AssignmentDetailsModal.tsx,frontend/src/components/waiting-queue/ReminderButton.tsx,frontend/src/components/waiting-queue/AgingIndicator.tsx,frontend/src/components/waiting-queue/EscalationDialog.tsx,frontend/src/pages/WaitingQueue.tsx,frontend/src/pages/TaskDetailPage.tsx,frontend/src/components/commitment-editor/CommitmentEditor.tsx" --json | node -e "let s=[];process.stdin.on(\"data\",d=>s.push(d)).on(\"end\",()=>{const j=JSON.parse(s.join(\"\"));if(j.twoArgUnresolved!==0||j.rawKeyUnresolved!==0||j.twoArgUnresolvedAr!==0||j.rawKeyUnresolvedAr!==0)process.exit(1)})"'
      text: 'scoped strict-audit zero (both classes, both locales) across the eleven t()-bearing lane files — RED at HEAD (the lane holds its unresolved sites until the colon-form rewrite lands here)'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; cd "$R" && test -f tests/e2e/99-ar03-leak.spec.ts && pnpm exec playwright test tests/e2e/99-ar03-leak.spec.ts -g "UI99-C7 ar banner" --project=chromium-en --no-deps --list 2>&1 | command grep -qE "Total: 3 tests in 1 file" && node "$R/scripts/pw-run-reaped.mjs" -- tests/e2e/99-ar03-leak.spec.ts -g "UI99-C7 ar banner" --project=chromium-en --no-deps'
      text: "the THREE RENDERED banner tests — one per ruled status over 99-01's deterministic fixture, ONE spec path, the count hardcoded at 3 from --list so a filter that selects one branch cannot pass for three — all pass under asserted ar. A missing position is a FIXTURE failure, never a skip. RED at HEAD (the Read Only sentences are still hardcoded)."
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; A="src/components/list-page/__tests__/Engagements""List.test.tsx"; B="src/pages/engagements/__tests__/Engagements""ListPage.test.tsx"; cd "$R/frontend" && test -f "$A" && test -f "$B" && OUT="$(pnpm exec vitest run "$A" "$B" 2>&1)"; rc=$?; printf "%s\n" "$OUT" | command grep -qE "Test Files  2 passed \(2\)" && printf "%s\n" "$OUT" | node -e "let s=[];process.stdin.on(\"data\",d=>s.push(d)).on(\"end\",()=>{const m=s.join(\"\").match(/Tests  (\\d+) passed/);if(!m||+m[1]<18)process.exit(1)})" && [ "$rc" -eq 0 ]'
      text: 'the two formerly zero-run suites collect, run, and pass — exactly 2 test files, at least 18 executed tests, exit 0 — RED at HEAD (both files throw at collection on their own incomplete per-file mocks)'
---

<objective>
Cut the test instrument over to real bundles and land colon form for the tasks/queues/positions
lane, with every collateral suite retargeted per the committed ledger, in one atomic pass.

This is part C (LAST) of the RULING-P99-163 split. The atomicity is measured, not assumed: the
ledger §0.3 proved by execution that the colon-form rewrite and the collateral retargets are each
red without the cutover, and the cutover is red without them. Everything separable was separated —
99-12 authored the lane bundles, 99-22 authored/repaired the three common.json items — so this
task retargets onto existing values and authors nothing.
</objective>

<context>
@.planning/phases/99-arabic-coverage/99-CONTEXT.md
@.planning/phases/99-arabic-coverage/99-RESEARCH.md
@.planning/phases/99-arabic-coverage/99-12-ATT2-LEDGER.md
@scripts/partA_maskfinder.py
@scripts/neg-taskcard.mjs
@frontend/src/components/tasks/TaskCard.tsx
@tests/e2e/99-ar03-leak.spec.ts

Ref notes: 99-12-ATT2-LEDGER.md is the DERIVATION of every collateral edit — §0.1 is the
per-assertion table (file, what it asserts, what the real bundle renders, remedy), §0.2 the two
zero-run suites with their measured mechanisms, §0.3 the coupling proof. The banner route file is
`frontend/src/routes/_protected/positions/$id/index.tsx` — named here in prose and listed in
files_modified, because an `@` ref containing `$` is silently DROPPED at compile (contract §7).
The preserved lineage (ledger header) is a verified worked example of the source and granted-test
half of this task; the collateral half follows the §0.1 remedy column. neg-taskcard.mjs — NOT
edited — its premise is a lane constraint.

<interfaces>
Carried from the former plan (verified 2026-08-18 at 9ffa2b80c; re-derive at start per D-04):

- TaskCard prefixes priority./status./work_item. through a bare hook — colon-prefix to
  assignments: families 99-12 authored. AssignmentDetailsModal prefixes waitingQueue.\* — same.
- CommitmentEditor prefixes afterActions.commitments.tracking — commitments: families from 99-12.
- The six dynamic carriers: colon-prefix the expressions, drop the variable second argument at
  those sites only, one commit per atomic unit (D-27), verified through partA_maskfinder
  (control first).
- The banner: three status-ternary sentences in the positions $id route → t() onto
  positions:readOnlyBanner.{under_review,approved,published} (99-12 authored both locales).
- Dot-form common.X sites rewrite to common:X only where the key resolves at the flattened root;
  still-missing common keys are left and NAMED (the common-owner lane).
- setup.ts: replace the legacy map + key-echo with the real i18next instance loaded exactly as
  src/i18n/index.ts loads it. Then the granted tests track authored values, and the thirteen
  collateral suites retarget per the ledger — including the DecisionList/SLAIndicator rows onto
  the copy 99-22 repaired/authored (interpolation restored; sla.approaching now exists).
- The two zero-run suites: fix their own mocks so they run (ledger §0.2 names the two defects);
  their tests then pass on their own or receive ledger-scoped retargets only.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>the instrument cutover — colon form, banner, setup, and the ledger retargets in one pass</name>
  <files>the paths in files_modified</files>
  <read_first>99-12-ATT2-LEDGER.md IN FULL; frontend/tests/setup.ts; src/i18n/index.ts (loading shape to mirror); the twelve source files</read_first>
  <action>
    Execute in this order, committing per coherent unit: (1) colon-form rewrite across the twelve
    source files + banner extraction + the six dynamic atomic units — values all exist (99-12);
    (2) setup.ts cutover to the real i18next instance (mirror production loading exactly; no
    dual paths); (3) granted-test values-tracking + the 42 collateral retargets per ledger §0.1 +
    the two zero-run suites' own-mock repairs per §0.2; (4) run every oracle plus
    partA_maskfinder (control first) and neg-taskcard (3× MISS=true), recording verbatim output.
    Author NO key anywhere. If any retarget's target value is missing from a bundle, STOP and
    record it — that is a 99-12/99-22 gap, not license to author here.
  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes.</acceptance_criteria>
  <done>The lane resolves in both locales through the real instrument; every suite the cutover touches is green by honest retarget; the banner is Arabic-capable; the negative control still discriminates.</done>
</task>

</tasks>

<verification>
POPULATION: the 12 source files + the 22 test-side files (setup.ts, the 8 granted, the 13
collateral) in files_modified. OUTSIDE IT: every bundle (99-12/99-22 authored them all — this
task authors NOTHING); the gate's wider pre-existing FAIL list (AppShell/Sidebar/dossiers routes
— baseline-forgiven, untouched); every raw-colon-key site beyond these files (bounded by the
att-1 gate evidence; no instance sweeps); scripts/neg-taskcard.mjs (never edited, never defused);
the exogenous paths (D-03).

DIFF-CAP NOTE (RULING-P99-163(5)): this part is measured at ~96-102K non-.planning bytes —
straddling the 100000 cap. A scoped gates.diffCap grant (125000, this task only, restore at its
terminal) is the NAMED ask riding the recompile checkpoint; it is never silently absorbed.
</verification>

<success_criteria>
Cut the test instrument over to real bundles and land colon form for the tasks/queues/positions
lane, with every collateral suite retargeted per the committed ledger, proven by the acceptance
items below in a single pass.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-43-SUMMARY.md` when done. It MUST record: starting
vs ending scoped audit numbers; the maskfinder control-then-clean pair; every collateral retarget
as (file, old assertion, new assertion, ledger row); the two zero-run suites' mock repairs and
their executed-test counts; the neg-taskcard 3× MISS=true output verbatim; the final non-.planning
diff size against the scoped cap; and anything left with its named owner.
</output>
