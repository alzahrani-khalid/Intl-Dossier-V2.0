---
phase: 99-arabic-coverage
plan: 23
type: execute
wave: 6
depends_on: [99-09, 99-11, 99-13, 99-16, 99-19, 99-21, 99-22]
files_modified:
  - 'frontend/{src/i18n/ar/common.json,src/i18n/en/common.json,src/i18n/ar/intake.json,src/i18n/ar/dashboard.json,src/i18n/ar/persons.json,src/i18n/en/persons.json,src/i18n/ar/countries.json,src/i18n/ar/engagements.json,src/pages/MoUs/MousPage.tsx}'
  - 'scripts/{nav-title-agreement.mjs,glossary-senses.d/tiebreaks.json}'
  - .planning/phases/99-arabic-coverage/99-23-SUMMARY.md
autonomous: true
requirements: [AR-01]
success_criterion: 'Land the ruled tie-breaks and the queue collision for the nav-title agreement lane, proven by the acceptance items below in a single pass.'
routing:
  floor: frontier
truths:
  - "Decisions covered — D-16: the queue-name collision is a DEFECT — intake retitles to قائمة الاستقبال (the nav already says it) while waiting keeps قائمة الانتظار; D-17: the dashboard tie-break lands لوحة الدوسيهات because the current لوحة الملفات renders the dossier OBJECT as ملف, which D-17 reserves for files and profiles; D-19: term values stay in i18n leaves, never inlined in code, so a term swap remains a mechanical leaf-rename + parity re-run through phase close; D-29: UI99-C6 is the binding UI-contract row this lane's intake retitle touches, and it is re-run RENDERED here rather than asserted by grep; D-07: criterion 1 closes on the drilled nav-title instrument and the rendered surface, never on source text alone; D-04/D-05: every census number is re-derived at task start with its command and its outside-population statement; D-38: this lane applies ONLY ruled decisions — any further disagreeing pair discovered during the 28-row walk ESCALATES to the overseer by name and is NOT repaired by an invented policy."
must_haves:
  truths:
    - 'Any FURTHER disagreeing pair found during the 28-row walk that no ruled row decides is ESCALATED to the overseer by name in the SUMMARY and left unrepaired — a worker never applies title-wins or any other invented policy. Escalating leaves this task RED on that row, which is the correct outcome: over-gating is recoverable, an invented Arabic information architecture is not.'
    - 'The memoranda page title mis-anchor is repaired: MousPage stops titling itself through the generic mous.title value ("Title"/"العنوان" in both locales today) and resolves a real page-title key whose values match the nav label pair in both locales (مذكرات التفاهم on the ar side), in explicit colon form against the post-flatten shape'
    - "The queue collision is retired: ar/intake.json's TITLE value becomes قائمة الاستقبال while قائمة الانتظار survives for the waiting queue. Re-derive both before and after — at plan time قائمة الاستقبال already occurs twice in ar/common.json (the nav labels navigation.intake and navigation.intakeQueue), so a clause keyed on mere PRESENCE of that string is a keep-true guard and not a discriminator; the discriminating clause is that ar/intake.json's own title carries it."
    - 'Every tie-break and every walked anchor is recorded in scripts/glossary-senses.d/tiebreaks.json as a machine-checkable row, so the decision trail is a committed artifact the closing battery re-reads rather than SUMMARY prose'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node -e "const fs=require(\"fs\");const R=process.argv[1];const rd=p=>JSON.parse(fs.readFileSync(R+\"/frontend/src/i18n/\"+p,\"utf8\"));const arc=rd(\"ar/common.json\"),intake=rd(\"ar/intake.json\"),dash=rd(\"ar/dashboard.json\"),pers=rd(\"ar/persons.json\");const s=JSON.stringify;if(!s(intake).includes(\"قائمة الاستقبال\")){console.error(\"intake title not قائمة الاستقبال\");process.exit(1)}if(!fs.readFileSync(R+\"/frontend/src/i18n/ar/common.json\",\"utf8\").includes(\"قائمة الانتظار\")){console.error(\"waiting-queue term vanished — over-sweep\");process.exit(1)}if(!s(dash).includes(\"لوحة الدوسيهات\")||s(dash).includes(\"لوحة الملفات\")){console.error(\"dashboard tie-break not applied as ruled\");process.exit(1)}if(s(pers).includes(\"جهات الاتصال الرئيسية\")){console.error(\"persons title still carries the retired contacts vocabulary\");process.exit(1)}if(!s(pers).includes(\"الأشخاص\")){console.error(\"persons title does not carry the ruled الأشخاص family\");process.exit(1)}if(arc.navigation?.countries!==\"الدول\"){console.error(\"countries nav label not الدول, got \"+arc.navigation?.countries);process.exit(1)}console.log(\"TIEBREAK-OK\")" "$R" && ! command grep -q "جهات الاتصال الرئيسية" "$R/frontend/src/i18n/ar/persons.json"'
      text: 'the three ruled tie-breaks are applied exactly as ruled and the queue collision is retired, each clause naming the value it demands and the value it forbids so a partial sweep cannot pass — RED at HEAD (dashboard reads لوحة الملفات, persons reads جهات الاتصال الرئيسية, countries nav reads البلدان, intake.json carries no استقبال title)'
---

<objective>
Land the ruled tie-breaks and the queue collision for the nav-title agreement lane, proven by the acceptance items below in a single pass.

This task is part 2 of 3 of the nav-title agreement lane; the parts are ordered by `depends_on` and share one
file surface, so each part reads the tree the part before it produced. The task-unit contract caps a
task at six acceptance items and twenty-four surface, which is why the lane is cut this way.
</objective>

<context>
@.planning/phases/99-arabic-coverage/99-CONTEXT.md
@.planning/phases/99-arabic-coverage/99-RESEARCH.md
@.planning/phases/99-arabic-coverage/rulings/RULING-P99-05-COVERAGE-TIEBREAKS-PRECOMMITS.md
@scripts/nav-title-agreement.mjs
@frontend/src/components/modern-nav/navigationData.ts
@frontend/src/pages/MoUs/MousPage.tsx

Ref notes: 99-CONTEXT.md — D-16..D-19, D-29, D-38; 99-RESEARCH.md — §4 in full — the two anchors, the 13 derived pairs, the proposed table; RULING-P99-05 §3 — the criterion-semantics clarification and the three ruled tie-breaks, READ WHOLE; nav-title-agreement.mjs — 99-02's 13-row version — extend, keep the control

<interfaces>
Verified on disk 2026-08-18 at 9ffa2b80c:

- The two anchors per nav entry: labelKey -> common.json navigation.\* (ar), and the page's own
  title key in its namespace (§4.2 names 13; the other 15 route to pages whose heading anchors come
  from deeper layout components — walk each route to its rendered heading source with the same
  method and record the anchor you found).
- The three tie-break pairs, read at this sha:
  persons — nav `الأشخاص` (EN "People") vs `ar/persons.json` title `جهات الاتصال الرئيسية`
  (EN "Key Contacts"). RULED: الأشخاص family wins both sides; the EN title repairs too.
  positions — nav `المواقف` (EN "Positions") vs the positions library title (EN "Positions
  Library"). RULED CONFORMANT: same object term both sides. NO EDIT.
  dashboard — the masked `navigation.dashboardOverview` label (authored in the authoring lane) vs
  `ar/dashboard.json` title `لوحة الملفات` (EN "Dossier Dashboard"). RULED: `لوحة الدوسيهات`
  both sides — `ملف` is reserved by D-17 for files and profiles.
- Ruled repairs among the 13: countries `البلدان`->`الدول` (nav follows title, the requirement's
  own arrow); engagements `الارتباطات`->`المشاركات` — note the engagements NAV LABEL is a member
  of the ارتباط family, so its value lands in the ارتباط lane behind this one; what this lane owns
  is the PAIR AGREEMENT and the title anchor; intake title `قائمة الانتظار`->`قائمة الاستقبال`;
  mous — the PAGE side is the defect (mis-anchored to the generic "Title" value), so the title
  repoints while the nav stays `مذكرات التفاهم`.
- Re-derived at this sha: `قائمة الانتظار` occurs 13 times across 3 ar files; `قائمة الاستقبال`
  occurs twice in one file (`ar/common.json`, navigation.intake and navigation.intakeQueue). A
  presence check on the second string is therefore ALREADY TRUE at HEAD and is a keep-true guard,
  not a discriminator — the discriminating clause is that `ar/intake.json` carries it.
- Estimated logic-diff size for this task: **~7,000 bytes** of value edits plus the checker
  extension (~15 appended rows). Well under the 60,000-byte gates.diffCap; stated as a floor.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>the nav-title agreement lane — the ruled tie-breaks and the queue collision</name>
  <files>the paths in files_modified</files>
  <read_first>the 99-02 checker (its row shape, its --json mode and its control); navigationData.ts; 99-RESEARCH.md §4.2 including the derivation bound ; RULING-P99-05 §3 whole; the current values of every anchor you are about to change</read_first>
  <action>
    Parts before this one landed in P99-22. Do only what THIS task's acceptance items still require, using the lane method below as the standing method; re-run this task's oracles against the tree the earlier part produced and repair any residue INSIDE files_modified. Author nothing outside them.

    --- THE LANE METHOD (carried verbatim from the pre-recut plan; the acceptance items above bound what THIS task owes) ---

    Walk each of the 15 underived nav targets to its rendered heading anchor and append the rows,
    each declaring its OBJECT TERM so the checker asserts term agreement rather than byte-equality.
    Add a --json mode emitting {rows:[…]} so the row count is machine-assertable. Do NOT delete a
    mismatching row to reach green — the row-count clause in this plan's oracle exists because
    nothing else structurally prevents that.



    1. Record every current value first (the D-19 reversal window needs the before).
    2. Apply: persons -> the الأشخاص family on both AR anchors and the Persons family on the EN
       title; dashboard -> لوحة الدوسيهات on both anchors; positions -> NO EDIT; countries nav ->
       الدول; intake title -> قائمة الاستقبال with قائمة الانتظار untouched for waiting.
    3. Repoint the MousPage title to a real key with values MoUs / مذكرات التفاهم (the en value
       authored in en/common.json — the single en common edit), colon-form, post-flatten shape.
    4. Record every applied decision as a row in scripts/glossary-senses.d/99-10.json.
    5. If the walk surfaced a disagreeing pair no ruled row decides: STOP on that row, name it in
       the SUMMARY with both anchors and both candidate terms, and escalate. Do not repair it.
    6. Verify: the checker green at 28 rows with its control; the tie-break oracle; the rendered
       UI99-C6 pair.

  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes. The lane's own criteria, carried: All 28 rows encoded with the walked anchors recorded; --json reports 28 rows; the planted control is still caught; the checker's live output names every remaining mismatch. Each ruled tie-break applied with its before value recorded; positions untouched; the queue collision retired with the waiting-queue term surviving; MousPage titling through real copy in both locales; every decision a committed row; any unruled pair escalated by name rather than repaired; the rendered UI99-C6 pair still green.</acceptance_criteria>
  <done>the ruled tie-breaks and the queue collision is landed for the nav-title agreement lane, proven by this task's own oracles rather than by the lane's later tasks.</done>
</task>

</tasks>

<verification>
POPULATION: the paths in files_modified. OUTSIDE IT: every other task's files, the exogenous paths
(D-03), and every obligation this task's acceptance items do not name — those belong to the lane
parts before and after this one, named in `depends_on` and in the SUMMARY.

The lane's own population statement, carried verbatim from the pre-recut plan:
POPULATION: the twelve listed files. OUTSIDE IT: the four term-family sweeps (the lanes behind
this one), Arabic NATURALNESS beyond the ruled rows (operator's, deferred), every en value except
the two named keys, every source file except MousPage.tsx, and the repo-wide rendered battery
(the closing plan).
</verification>

<success_criteria>
Land the ruled tie-breaks and the queue collision for the nav-title agreement lane, proven by the acceptance items below in a single pass.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-23-SUMMARY.md` when done. It MUST record every command this task ran with its verbatim output, the
populations it re-derived with the commands that produced them, and anything left for a named later
task. The lane's own SUMMARY contract, carried verbatim from the pre-recut plan (satisfy the parts
this task owns): record in the file named under <output> (99-10-SUMMARY.md in the pre-recut plan) when done. It MUST record: the 15
walked anchors; every applied tie-break with its BEFORE and AFTER value (the D-19 reversal list);
any unruled pair escalated, by name, with both anchors; the MoUs repoint mapping; the checker's
--json row count; and the rendered UI99-C6 output.
</output>
