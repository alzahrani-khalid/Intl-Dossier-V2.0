**RULING CLOSURE — RULING-P99-48. No worker ran for this record. This file exists so the compiler's sibling-summary pre-mark records the task done; the evidence it points to is journal- and git-recorded, not produced here.**

# Phase 99 Plan 02 — dates rendered-red record

Status: closed as already delivered by `RULING-P99-48`.

## Contract already present at baseRef

`tests/e2e/99-ar02-dates.spec.ts` collects exactly 8 tests and uses the shared settle/locale law. Its Arabic calendar, dossier, events, and activity legs remain intentionally red before P99-20/P99-21; English legs are non-regression controls.

## Evidence map

- Engine run `run-20260819-011350-0000000000000019` recorded P99-02 `task-done` at 2026-08-19T01:52:40Z after acceptance and review. Merge `e1c15789e` was intentionally not retained in the protected branch because the worker added no product bytes; the inherited oracle files were already present.
- Runs 0020 and 0021 each emitted `baseline-warning kind:vacuous-oracle` for P99-02 after executing its complete command oracle at baseRef. The command hardcodes the 8-test collection count and runs `pw-red-assert` against the required Arabic calendar title.
- The crash-proof assertion from RULING-P99-27 cannot pass on a zero-spec report or a hook/fixture-only failure.
- `.tickmarkr/overseer/P99-RECOVERY-REPORT-R2.md` §5.2 independently observed 8 tests run, 4 failed in application-body assertions, with the Arabic calendar and events titles present.

## Boundaries

This is a ruling record, not a summary-only claim of new product work. P99-20/P99-21 own the date repairs and fresh green run. Once those repairs land, this red-first oracle is false by design and P99-02 must not be reopened without a new ruling. Nothing here grades P99-07…41.
