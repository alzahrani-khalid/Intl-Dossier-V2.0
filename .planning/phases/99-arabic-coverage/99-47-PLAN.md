---
phase: 99-arabic-coverage
plan: 47
type: execute
wave: 9
depends_on: [99-46]
files_modified:
  - scripts/i18n-audit-strict.mjs
  - .planning/phases/99-arabic-coverage/99-47-SUMMARY.md
autonomous: true
requirements: [AR-04a, AR-04b]
success_criterion: 'Repair and positively control the strict audit test-population contract before P99-30 reruns.'
routing:
  floor: frontier
  pin: { via: codex, model: gpt-5.6-sol }
truths:
  - 'Decisions covered — D-05, D-06, D-24, D-27, D-28 and RULING-P99-101/P99-102: the production audit excludes files named *.test.* and every __tests__ subtree from BOTH the mask and raw-key populations, while retaining a nonempty production walk and the canonical binding model.'
must_haves:
  truths:
    - 'The production walker applies one explicit, shared test-source policy before either class is counted: any *.test.* file and any file below a __tests__ path is excluded. The policy is positively controlled in --self-check for a normal source, a colocated test file, and a __tests__ file; exclusions are not inferred from a clean result.'
    - 'The live repo proof independently counts production TypeScript files and excluded test TypeScript files, requires the excluded population to be nonempty, and requires the audit scanned-file count to equal the independent production count. The unscoped audit then reports zero unresolved mask and raw-key sites in BOTH locales with both totals nonzero.'
    - 'Only the strict instrument and this SUMMARY change. Locale data and production/test source are byte-untouched; the 30/35 production repairs remain owned by P99-45/P99-46, and this policy repair is never credited with greening them.'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node "$R/scripts/i18n-audit-strict.mjs" "$R" --self-check | node -e "let s=[];process.stdin.on(\"data\",d=>s.push(d)).on(\"end\",()=>{const j=JSON.parse(s.join(\"\"));if(!j.passed||j.checks?.productionSourceIncluded!==true||j.checks?.testFileExcluded!==true||j.checks?.testsDirectoryExcluded!==true){console.error(\"test-population self-check missing or red\",j.checks);process.exit(1)}})" && node "$R/scripts/i18n-audit-strict.mjs" "$R" --json | node -e "const fs=require(\"fs\"),path=require(\"path\");let s=[];process.stdin.on(\"data\",d=>s.push(d)).on(\"end\",()=>{const j=JSON.parse(s.join(\"\")),root=process.argv[1]+\"/frontend/src\";let production=0,excluded=0;const walk=d=>{for(const e of fs.readdirSync(d)){const p=path.join(d,e),st=fs.statSync(p);if(st.isDirectory()){if(e===\"node_modules\"||e===\"i18n\")continue;if(e===\"__tests__\"){const count=x=>{for(const q of fs.readdirSync(x)){const y=path.join(x,q),z=fs.statSync(y);if(z.isDirectory())count(y);else if(/\\.tsx?$/.test(q))excluded++}};count(p);continue}walk(p)}else if(/\\.tsx?$/.test(e)){if(/\\.test\\./.test(e))excluded++;else production++}}};walk(root);if(excluded===0||production===0||j.scope.length!==0||j.scannedFiles!==production){console.error(\"live population policy mismatch\",{excluded,production,scanned:j.scannedFiles,scope:j.scope});process.exit(1)}if(j.twoArgTotal<=0||j.rawKeyTotal<=0||j.twoArgUnresolved!==0||j.rawKeyUnresolved!==0||j.twoArgUnresolvedEn!==0||j.rawKeyUnresolvedEn!==0||j.twoArgUnresolvedAr!==0||j.rawKeyUnresolvedAr!==0){console.error(\"strict production audit not green/nonempty\",JSON.stringify(j).slice(0,400));process.exit(1)}console.log(\"P99-47-POLICY-OK production=\"+production+\" excludedTests=\"+excluded)})" "$R"'
      text: 'the self-check positively discriminates production/test/__tests__ paths, an independent live census proves a nonempty excluded-test population and exact production scanned-file equality, and the unscoped production audit is nonempty and zero-unresolved in both classes/locales — RED at HEAD because the walker includes tests and the production residue is nonzero'
---

<objective>
Repair and positively control the strict audit test-population contract before P99-30 reruns.
</objective>

<context>
@.planning/phases/99-arabic-coverage/rulings/RULING-P99-102-GSD-SUMMARY-STATE-AND-ROUTE.md
@.planning/phases/99-arabic-coverage/rulings/RULING-P99-101-AR04A-PREDECESSORS.md
@.planning/phases/99-arabic-coverage/99-30-RED-HANDOFF.md
@.planning/phases/99-arabic-coverage/99-CONTEXT.md
@.planning/phases/99-arabic-coverage/99-RESEARCH.md
@scripts/i18n-audit-strict.mjs
</context>

<tasks>
<task type="auto">
  <name>strict audit test-population policy repair</name>
  <files>the paths in files_modified</files>
  <read_first>RULING-P99-102 first, then RULING-P99-101, then 99-30-RED-HANDOFF.md section 4 and the strict instrument</read_first>
  <action>
    Implement one production-source predicate shared by the walk for both mask and raw-key classes. Exclude *.test.* files and __tests__ subtrees, add three-polarity self-check coverage, and preserve every other binding/resolution rule. Run the self-check and the independent live production/test census before accepting the repo-wide zero; record exact outputs and the fact that production repairs came from P99-45/P99-46, not from this instrument change.
  </action>
  <acceptance_criteria>Every acceptance item above is satisfied, the production/test policy is positively controlled and live-counted, the unscoped production audit is zero-unresolved in both locales/classes, and no path outside files_modified changes.</acceptance_criteria>
  <done>The strict instrument measures the ruled production population and proves the repaired tree ready for the independent P99-30 gatekeeper.</done>
</task>
</tasks>

<verification>
POPULATION: scripts/i18n-audit-strict.mjs production/test discovery policy and its in-file self-check. OUTSIDE: all source/locale repairs, test edits, deletion, and any change to the canonical binding model.
</verification>

<success_criteria>
Self-check and independent live census prove the test exclusion, and the unscoped production audit is nonempty and zero-unresolved in both locales/classes.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-47-SUMMARY.md` with the policy diff, three-polarity self-check, independent live census, and verbatim zero proof.
</output>
