**RULING CLOSURE — RULING-P99-48. No worker ran for this record. This file exists so the compiler's sibling-summary pre-mark records the task done; the evidence it points to is journal- and git-recorded, not produced here.**

# Phase 99 Plan 03 — leakage rendered-red record

Status: closed as already delivered by `RULING-P99-48`.

## Contract already present at baseRef

`tests/e2e/99-ar03-leak.spec.ts` collects exactly 10 tests: Arabic/English 404 coverage, Arabic/English intake coverage, three fixture-driven position-banner statuses, Arabic search chips, Latin-run scan, and Tajawal font probe. The C7 fixture refuses rather than skipping when its database precondition is absent.

## Evidence map

- Engine run `run-20260819-011350-0000000000000019` recorded P99-03 `task-done` at 2026-08-19T02:28:43Z after acceptance and review. Merge `725a0ef53` was intentionally not retained in the protected branch because the inherited oracle files were already present.
- Runs 0020 and 0021 each emitted `baseline-warning kind:vacuous-oracle` for P99-03 after executing its complete command oracle at baseRef. The command hardcodes the 10-test collection count and requires the Arabic 404 failure title through `pw-red-assert`.
- RULING-P99-27 makes zero-spec, hook, and fixture-only failures ineligible. These warnings therefore record rendered-red observations rather than the missing-credential false green that caused the earlier halt.
- The underlying spec and fixture content descend from run-0015's consolidated merge `117511875`, plus the worktree environment-resolution fixes `3f3566157` and `223dbec50`.

## Boundaries

This record adds no product bytes. P99-08/10/12/19/24 and the closing battery own the rendered repairs and fresh green proofs. The red-first oracle is expected to become false as those lanes land and P99-03 must not be reopened without a new ruling. Nothing here grades P99-07…41.
