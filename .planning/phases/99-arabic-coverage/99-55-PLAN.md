---
phase: 99-arabic-coverage
plan: 55
type: execute
wave: 11
depends_on: [99-49]
files_modified:
  - 'frontend/src/i18n/{en,ar}/graph.json'
  - .planning/phases/99-arabic-coverage/99-55-SUMMARY.md
autonomous: true
requirements: [AR-04a]
success_criterion: 'Close the residue 99-49 excludes: the two owned graph legend leaves resolve in both locales, and the the command-palette component population is proved resolved rather than left unassigned.'
routing:
  floor: frontier
truths:
  - 'RULING-P99-298 and RULING-P99-300: this task exists so that the narrowing in 99-49 ASSIGNS rather than merely excludes. A re-baseline without a successor does not own the residue, it NORMALISES it, and the phase would close with rows every future oracle expects to find.'
must_haves:
  truths:
    - "THE TWO OWNED LEAVES RESOLVE IN BOTH LOCALES, AND THIS TASK'S ZERO IS ASSERTED OVER THEM AND NOTHING ELSE. graph:type.individual and graph:type.mou are read by the NODE_COLORS legend in AdvancedGraphVisualization.tsx - line 1853 at 2e3f26bad and line 1857 at fb0ad51e3, WHICH IS WHY THIS TASK IDENTIFIES THE ROWS BY FILE AND KEY AND NEVER BY LINE: 99-49 edits that file and moves the number. It is a counted lane-3 site whose domain the instrument PROVES closed - domainSource NODE_COLORS entries slice, closedDomain true - so these are genuine product gaps and not an artifact of measurement. Measured at 2e3f26bad both keys are absent from en and ar; measured again at fb0ad51e3, after a full repair attempt, both were STILL absent, which is why they need an owner rather than an assumption. Only the two named leaves are added: no other key family changes, and no production caller is edited."
    - 'THE COMMAND-PALETTE POPULATION IS NOT OWNED BY THIS TASK AND THAT IS AN OPEN QUESTION, NOT A SILENT DROP. RULING-P99-299 assigned it here, and the 2.1.4 compiler REFUSES that assignment: a criterion naming a symbol defined outside files[] cannot be satisfied by a worker scoped to files[] (OBS-248). Its three sites read unclassified only because the instrument refused a renamed destructured translator, which 99-54 removes, and the underlying code is correct and complete - measured at 2e3f26bad the 8 group-key values resolve 8/8 in en and 8/8 in ar and the 27 navigation labels 27/27 and 27/27 - so nothing here needs repair. What it needs is an owner whose files[] contains that component, or a ruling registering it as requiring none. This task owns the two legend leaves only, and says so rather than absorbing an assignment it cannot express.'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node "$R/scripts/i18n-dynamic-key-audit.mjs" "$R" --self-check || { echo "FAIL: self-check"; exit 1; }; AUDITJSON="$(mktemp -t p99audit).json" || { echo "INSTRUMENT-CANNOT-RUN: mktemp failed"; exit 3; }; AUDITERR="$AUDITJSON.err"; node "$R/scripts/i18n-dynamic-key-audit.mjs" "$R" --profile ar04-live --json > "$AUDITJSON" 2>"$AUDITERR"; AUDITST=$?; node -e "JSON.parse(require(\"node:fs\").readFileSync(process.argv[1],\"utf8\"))" "$AUDITJSON" 2>/dev/null || { echo "INSTRUMENT-CANNOT-RUN: the dynamic-key audit produced no parseable JSON (exit $AUDITST), so it did not run to a verdict"; head -3 "$AUDITERR"; rm -f "$AUDITJSON" "$AUDITERR"; exit 3; }; echo "audit-exit=$AUDITST (nonzero here is a CONTENT verdict, not an execution failure; JSON parsed)"; AUDITJSON="$AUDITJSON" node -e "const d=JSON.parse(require(\"node:fs\").readFileSync(process.env.AUDITJSON,\"utf8\"));const AGV=\"frontend/src/components/relationships/AdvancedGraphVisualization.tsx\";const site=r=>r.file+\":\"+r.line;const rows=d.rows||[];const owned=rows.filter(r=>r.file===AGV&&(r.key===\"type.individual\"||r.key===\"type.mou\"));const ok=owned.filter(r=>r.en===\"ok\"&&r.ar===\"ok\");console.log(\"owned-legend-rows-resolved=\"+ok.length+\"/\"+owned.length);let bad=0;if(owned.length!==2){console.error(\"FAIL: owned legend row count is \"+owned.length+\"/2 - the population moved under this task\");bad=1}const osites=[...new Set(owned.map(site))];console.log(\"owned-legend-sites=\"+JSON.stringify(osites));if(owned.length===2&&osites.length!==1){console.error(\"FAIL: the two owned legend leaves are NOT co-located at one site - \"+JSON.stringify(osites)+\" - so two matching keys elsewhere in the file are satisfying this oracle rather than the intended legend call\");bad=1}const CP=\"frontend/src/components/keyboard-shortcuts/CommandPalette.tsx\";const cpRows=rows.filter(r=>r.file===CP);const cpUnc=(d.unclassified||[]).filter(u=>u.file===CP);const cpOk=cpRows.filter(r=>r.en===\"ok\"&&r.ar===\"ok\");console.log(\"command-palette population=\"+cpOk.length+\"/\"+cpRows.length+\" unclassified=\"+cpUnc.length);if(cpRows.length===0){console.error(\"FAIL: the command-palette population is EMPTY - the criterion requires a non-empty population precisely so the assignment cannot be satisfied by an empty set the way a vacuous every() would be\");bad=1}if(cpUnc.length!==0){console.error(\"FAIL: \"+cpUnc.length+\" command-palette call(s) remain UNCLASSIFIED\");bad=1}if(cpRows.length>0&&cpOk.length!==cpRows.length){console.error(\"FAIL: \"+(cpRows.length-cpOk.length)+\"/\"+cpRows.length+\" command-palette leaves UNRESOLVED\");bad=1}if(ok.length!==owned.length){console.error(\"FAIL: \"+(owned.length-ok.length)+\"/\"+owned.length+\" owned legend leaves still unresolved\");bad=1}process.exit(bad)"'
      text: 'the two owned legend leaves at AdvancedGraphVisualization.tsx:1857 resolve in BOTH locales - unreachable before this task, since both were measured absent at 2e3f26bad and still absent at fb0ad51e3 after a full repair attempt - while the the command-palette component population is reported n over total and must be non-empty, fully classified and fully resolved, so the assignment cannot be satisfied by an empty set the way a vacuous every() would be'
    - 'THE DIFF IS TWO LOCALE FILES AND THIS SUMMARY. No production caller is edited, no instrument or fixture is touched, no other key family changes, and no fallback or default argument is deleted. A production edit made to satisfy an audit is the harm RULING-P99-296 recorded this instrument already causing twice, and this task has no reason to reach for it: its gap is two missing translations.'
---

<objective>
Close the residue 99-49 excludes: the two owned graph legend leaves resolve in both locales, and the
command-palette population is proved resolved rather than left unassigned.
</objective>

<context>
@.planning/phases/99-arabic-coverage/99-49-PLAN.md
@frontend/src/components/relationships/AdvancedGraphVisualization.tsx
</context>

<tasks>
<task type="auto">
  <name>add the two owned legend leaves and prove the assigned population</name>
  <files>the paths in files_modified</files>
  <read_first>the residue split, then the NODE_COLORS legend at AdvancedGraphVisualization.tsx:1857</read_first>
  <action>
    Add graph:type.individual and graph:type.mou to the en and ar graph bundles, matching the wording and
    register of the sibling type leaves already present in each locale. Add nothing else. Then run the live
    profile and record, verbatim, that the two owned legend rows resolve in both locales and that the
    command-palette population is non-empty, fully classified and fully resolved. Do NOT edit any production
    caller, the instrument, the fixtures, or any other key family.
  </action>
  <acceptance_criteria>Every acceptance item above passes, with a diff of exactly two locale files plus this SUMMARY.</acceptance_criteria>
  <done>The owned legend pair resolves in en and ar, and the command-palette population is proved classified and resolved rather than merely excluded.</done>
</task>
</tasks>

<verification>
POPULATION: the two owned graph legend leaves in both locales, the command-palette caller population as an
ASSIGNED-and-proved set, and this SUMMARY. OUTSIDE IT: the three statically unprovable
AdvancedGraphVisualization domains at :1546, :1564 and :1649, which RULING-P99-241 owns at the 99-50
boundary as a definitional ruling and which this task may not attempt to close; the instrument and its
fixtures (99-53 and 99-54); every other production caller and locale family (99-49); every fallback
deletion (99-31 through 99-38); and the literal gatekeeper battery (99-50).
</verification>

<success_criteria>
Close the residue 99-49 excludes: the two owned graph legend leaves resolve in both locales, and the
command-palette population is proved resolved rather than left unassigned.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-55-SUMMARY.md` recording the VERBATIM stdout and stderr of
every oracle command, the two added leaves with their en and ar values, the command-palette population as n
over total, and a diff statement proving no production caller was edited.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete`. Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as **pending** no matter how many gates passed — measured on P99-32 and again on P99-61, which passed 7/7 and still compiled as pending (PRODUCT ENTRY 36). Verify with `node scripts/completion-contract-check.mjs <journal> <ref>`.
</output>
