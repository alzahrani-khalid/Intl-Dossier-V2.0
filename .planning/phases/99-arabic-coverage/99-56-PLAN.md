---
phase: 99-arabic-coverage
plan: 56
type: execute
wave: 9
depends_on: [99-48, 99-51, 99-54]
files_modified:
  - scripts/i18n-dynamic-key-audit.mjs
  - .planning/phases/99-arabic-coverage/99-56-SUMMARY.md
autonomous: true
requirements: [AR-04a]
success_criterion: 'Own the dynamic-key audit classifier and give it the one classification rule it is missing, proven by its own fixture ledger and never by the residue it reports.'
routing:
  floor: frontier
truths:
  - 'RULING-P99-372 and RULING-P99-385 (TRG-21): this task exists because the remedy for the unclassified CommandPalette residue was OWNERLESS. Measured across all 55 prior tasks: three tasks list scripts/i18n-dynamic-key-audit.mjs — 99-48, 99-51 and 99-54 — and ALL THREE ARE done, so none can be edited without destroying a completed task. Of the remaining tasks only 99-07 may write any scripts/ path and it is a DIFFERENT file (i18n-binding-census.mjs). No remaining task could add the rule, so 99-49 criterion could not be satisfied by any worker in the graph. Three other remedies were refused and for ONE reason (RULING-P99-376 section 2): admitting the rows to the audit FENCE, loosening 99-49 criterion, and extending 99-49 files so the graded worker could edit its own grader are an exemption, a looser criterion and a self-editable oracle — the SAME LIE AT THREE LEVELS. The rows are PROVABLE, not unprovable: the instrument own output reads CommandPalette.tsx|NONE, where NONE is the classifier reporting that it HAS NO RULE for the shape. An exemption list must be defined by the property that earns exemption, never by who is currently failing.'
must_haves:
  truths:
    - 'THIS TASK IS GRADED ON CLASSIFICATION BEHAVIOUR AND MUST NOT BE GRADED ON THE RESIDUE IT REPORTS. That is a REQUIREMENT of this task, not a preference (RULING-P99-385 section 3.3). The rule to add is for one shape: A VARIABLE PASSED AS THE WHOLE KEY, SOURCED FROM A STATIC LITERAL COLLECTION. Measured at a2309ac6d, that shape occurs in TWO sub-forms and the rule must cover BOTH, because covering only the easy one leaves the residue exactly where it is. (i) an IN-FILE MAP: the variable is bound from a member of a module-level object literal declared in the SAME file, then passed whole. (ii) a CROSS-MODULE COLLECTION: the variable is a property of an element of a collection returned by a helper and flattened, whose literal members are declared in a DIFFERENT module — so the trace crosses a call, a flatMap and a module boundary. Sub-form (ii) is the harder one and it is NOT optional. The instrument already carries the machinery for it: its domain readers already resolve named constants out of other files, and its existing provers are the pattern to mirror rather than replace. BOTH sub-forms end in a trailing logical-or fallback, so the proven domain is the collection literal members and the fallback branch is an UNPROVEN TAIL the rule must not silently absorb into the closed domain. The concrete files, symbols and line numbers behind both sub-forms are given in the task body, which is where they belong: naming them here would assert authority over source this task may only READ. The rule must classify these and return a CLOSED domain carrying the collection literal member keys. NO criterion of this task may assert anything about the live repository unclassified count, the FENCE observed set, or how many rows any profile reports, because the classifier under repair is the instrument that produces those numbers and an instrument owner graded by its own output inherits exactly the conflict that made 99-49 unsatisfiable. The residue claim belongs to 99-49, which is the correct owner and depends on this task.'
    - 'BOTH POLARITIES ARE PROVEN, AND THE NEGATIVE ONE IS WHAT MAKES THE RULE A RULE. Evidence is the instrument own --self-check ledger, whose fixtures carry DECLARED GROUND TRUTH authored in this task and are therefore an external expectation rather than a self-report. Three named checks are added to that ledger and each must appear AND be true: staticCollectionWholeKeyClassifies (a whole-key variable read from a static literal collection is classified, returning non-null); staticCollectionDomainIsClosed (that classification returns a CLOSED domain — closed true — whose keys are exactly the collection literal members, so the rule proves a domain rather than guessing one); and runtimeWholeKeyStaysUnclassified (a whole-key variable sourced from a RUNTIME value — a function parameter, a fetch result, or any binding the parser cannot resolve to a literal collection — is STILL unclassified, returning null). Without that third check the rule would be indistinguishable from one that classifies every dynamic key, which would silently empty the residue rather than explain it. A check that is ABSENT from the ledger is a FAILURE, never a pass — the oracle below names the three and fails closed on any that is missing.'
    - 'THE PRE-EXISTING LEDGER STAYS GREEN AND THE DIFF IS TWO FILES. The seven checks already in the ledger — resolvedLeafPasses, existingPrefixMissingLeafFails, enOnlyFailsArabic, arOnlyFailsEnglish, unknownCallShapeFails, interpolationOnlyOptionsNotFallback, defaultValueOptionsAreFallback — must all still hold, so the overall ledger still reports selfCheck PASS; a new rule that breaks unknownCallShapeFails has classified something it must not. No production source, no locale bundle, no fixture outside the instrument and no other task instrument is edited. The diff is scripts/i18n-dynamic-key-audit.mjs and this SUMMARY.'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; OUT=$(node "$R/scripts/i18n-dynamic-key-audit.mjs" "$R" --self-check 2>/dev/null); printf "%s" "$OUT" | node -e "let s=[];process.stdin.on(\"data\",d=>s.push(d)).on(\"end\",()=>{let j;try{j=JSON.parse(s.join(\"\"))}catch(e){console.error(\"INSTRUMENT-CANNOT-RUN: --self-check produced no parseable ledger, so it did not run to a verdict and this leg verified NOTHING\");process.exit(3)}const c=j.checks||{};const need=[\"staticCollectionWholeKeyClassifies\",\"staticCollectionDomainIsClosed\",\"runtimeWholeKeyStaysUnclassified\"];const missing=need.filter(k=>!(k in c));if(missing.length){console.error(\"FAIL: the ledger does not carry the new classification checks: \"+missing.join(\",\")+\" — an ABSENT check is not a passing check\");process.exit(1)}const bad=need.filter(k=>c[k]!==true);if(bad.length){console.error(\"FAIL: classification checks did not hold: \"+bad.join(\",\"));process.exit(1)}const prior=[\"resolvedLeafPasses\",\"existingPrefixMissingLeafFails\",\"enOnlyFailsArabic\",\"arOnlyFailsEnglish\",\"unknownCallShapeFails\",\"interpolationOnlyOptionsNotFallback\",\"defaultValueOptionsAreFallback\"];const lost=prior.filter(k=>!(k in c));if(lost.length){console.error(\"FAIL: pre-existing checks disappeared from the ledger: \"+lost.join(\",\")+\" — a rule may not be proven by deleting the checks that constrain it\");process.exit(1)}const regressed=prior.filter(k=>c[k]!==true);if(regressed.length){console.error(\"FAIL: pre-existing checks regressed: \"+regressed.join(\",\"));process.exit(1)}if(j.selfCheck!==\"PASS\"){console.error(\"FAIL: ledger reports selfCheck=\"+j.selfCheck);process.exit(1)}console.log(\"CLASSIFIER-BEHAVIOUR-OK positive+closed+negative asserted, all 7 prior checks intact\")})"'
      text: 'the classifier fixture ledger carries the new rule proven in BOTH directions and the seven prior checks intact. RED AT THIS TASK BASE by construction and verified so: the three names do not exist in scripts/i18n-dynamic-key-audit.mjs at a2309ac6d, so the missing-check branch fires and this leg cannot be satisfied by anything except adding the rule and its fixtures. The oracle reads the LEDGER, never the exit status, because --self-check is a GATEKEEPER not a reporter — it prints the ledger and then exits 1 when a check fails, so gating on its exit code would report a real content failure as an instrument failure. An unparseable ledger exits 3 and is reported as verified NOTHING rather than as a pass. This oracle deliberately asserts NOTHING about the repository residue, the FENCE set, or any profile count: that evidence belongs to 99-49.'
    - 'THE MOTIVATING POPULATION IS MEASURED, AND ITS RESOLUTION IS NOT THIS TASK CLAIM. Three call sites in the command-palette component are what made the missing rule visible, and the fixtures in this task are modelled on their measured shape so the rule is built for a real form rather than an invented one; the sites are identified in the task body. Measured at a2309ac6d and recorded so the next reader need not re-derive it: BOTH domains resolve COMPLETELY in BOTH locales — the cross-module label collection resolves 27 of 27 in en and 27 of 27 in ar, and the in-file map resolves 8 of 8 in en and 8 of 8 in ar. That matters beyond bookkeeping: classifying these three rows moves them out of the unclassified set WITHOUT creating a single new missing leaf, so the remedy cannot fail by relocating the failure from one assertion of the consuming task into another. If a later reader finds those numbers changed, the remedy premise changed with them and must be re-checked before this task is trusted. Stating the motivating population without claiming its resolution is the whole separation this task exists to hold.'
---

<objective>
Own scripts/i18n-dynamic-key-audit.mjs and add the one classification rule it lacks: a variable passed
as the WHOLE key, sourced from a static literal collection. Prove it in the instrument own fixture
ledger, in both directions, and assert nothing about the residue the instrument reports.
</objective>

<context>
@scripts/i18n-dynamic-key-audit.mjs
@.planning/phases/99-arabic-coverage/99-49-PLAN.md
@frontend/src/components/keyboard-shortcuts/CommandPalette.tsx
@frontend/src/components/layout/navigation-config.ts

Ref notes: the ledger and its fixture pattern are runSelfCheck in the instrument — synthetic sources
parsed in-process with declared expectations, which is the pattern to extend rather than replace.
classifyCall is where the rule belongs; closedDomain and describeDomain are how a proven domain is
returned. CommandPalette.tsx and navigation-config.ts are READ-ONLY here and are listed for the shape only — they are
in context[], not files[], and nothing in this task may modify them.

THE MEASURED SHAPE, so the worker does not re-derive it (all figures taken at a2309ac6d):
  - sub-form (i), in-file map: CommandPalette.tsx:1392 binds `groupKey` from `dossierTypeGroupKeys`, a
    module-level `Record<string,string>` literal at CommandPalette.tsx:318 with exactly 8 entries
    (`groups.countries` … `groups.electedOfficials`), and passes it whole at line 1397 through the
    `quickswitcher` namespace. All 8 resolve in en and ar.
  - sub-form (ii), cross-module: CommandPalette.tsx:1546 and :1554 pass `page.label`, where `page` comes
    from `createNavigationGroups(...).flatMap((g) => g.items)` at CommandPalette.tsx:530-533. The literals
    are 27 distinct `label:` values in navigation-config.ts, read through the `common` namespace. All 27
    resolve in en and ar.
  - both sites end in a `|| fallback` and both carry a SECOND ARGUMENT fallback, so they are mask-bearing;
    the fallback is the unproven tail and must not be folded into the closed domain.
</context>

<tasks>
<task type="auto">
  <name>add the static-literal-collection whole-key rule and its two-polarity ledger checks</name>
  <files>the paths in files_modified</files>
  <read_first>runSelfCheck and its existing seven checks, then classifyCall and its fall-through, then the three CommandPalette.tsx sites for the shape</read_first>
  <action>
    1. Read the three CommandPalette.tsx sites and record the shape verbatim in the SUMMARY: a
       variable holding the whole key, bound from a member of a static literal collection.
    2. Add the classification rule to classifyCall. It must resolve the collection to its literal
       members and return a CLOSED domain of those members. It must NOT fire when the variable
       cannot be traced to a static literal collection.
    3. Extend runSelfCheck with synthetic fixtures and the three named checks
       staticCollectionWholeKeyClassifies, staticCollectionDomainIsClosed and
       runtimeWholeKeyStaysUnclassified. The negative fixture must use a binding the parser cannot
       resolve to a literal collection.
    4. Run --self-check and record the full ledger verbatim. Then DRILL THE NEW RULE BOTH WAYS: break
       the rule on purpose, observe the positive check go false, restore it, and record both readings.
       A rule whose check has only ever been observed passing has not been shown to be a check.
    5. ONE commit carrying the instrument and this SUMMARY.
  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes. The ledger is recorded whole, not summarised; the both-ways drill is recorded with the observed false reading as well as the true one; and the SUMMARY states plainly that this task asserts nothing about the residue count, naming 99-49 as the owner of that claim.</acceptance_criteria>
  <done>The classifier has a rule for the whole-key static-literal-collection shape, that rule is proven to fire on the shape and proven NOT to fire on a runtime-sourced key, the seven prior ledger checks still hold, and no claim about the reported residue appears anywhere in this task.</done>
</task>

</tasks>

<verification>
POPULATION: scripts/i18n-dynamic-key-audit.mjs and this SUMMARY. OUTSIDE IT: the residue count and
the FENCE observed set, which belong to 99-49; every production source and locale bundle; every other
instrument. A green here means the classifier BEHAVES correctly on declared fixtures. It does NOT mean
the repository residue changed, and it must never be cited for that.
</verification>
