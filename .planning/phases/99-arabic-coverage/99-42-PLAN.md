---
phase: 99-arabic-coverage
plan: 42
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/i18n-binding-census.mjs
  - scripts/fixtures/i18n-binding-census-controls.tsx
  - .planning/phases/99-arabic-coverage/99-07-SUMMARY.md
  - .planning/phases/99-arabic-coverage/99-42-SUMMARY.md
autonomous: true
requirements: [AR-04b]
success_criterion: 'Repair the i18n binding-census proof instrument so its effective-namespace model is ordered, resource-aware and locale-aware, proven by the acceptance items below in a single pass.'
routing:
  floor: frontier
must_haves:
  truths:
    - 'RESOLUTION LAW (compiled acceptance, not prose): the census resolves a call by FIRST DEFINED LOOKUP walking the namespace list in order, per supported locale, over the real locale bundles for BOTH en and ar. A namespace whose bundle DEFINES the key path stops the search even when the value is an OBJECT; an object stops it and marks the call object-return, and a later namespace MUST NOT be allowed to win after any earlier definition. A `.some()`-style order-blind test is forbidden. An explicit `ns:` key prefix takes precedence over an unresolvable or invisible `t` binding. Locale divergence is reported per locale and never collapsed. Dynamic namespace arrays, non-literal keys and generic property-access `t()` are excluded from resolved partitions and counted in their own named blind populations. Scope: the produced diff and `files_modified` contain ONLY the four declared paths and NO application, route, component or locale-JSON file; naming `frontend/src/routes/__root.tsx` in SUMMARY prose is required and is not a scope violation.'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; set -u; R="$PWD"; T="$(mktemp -t p9942a.XXXXXX)" || exit 1; trap "rm -f \"$T\"; exit 1" HUP INT TERM; trap "rm -f \"$T\"" EXIT; node "$R/scripts/i18n-binding-census.mjs" "$R" --self-check --json > "$T" 2>/dev/null; rc=$?; test $rc -eq 0 || exit 1; test -s "$T" || exit 1; node -e "const j=JSON.parse(require(\"fs\").readFileSync(process.argv[1],\"utf8\"));const want=[\"later-common\",\"scalar-shadow\",\"object-shadow\",\"locale-divergent\",\"dynamic-array\",\"property-access\",\"non-literal\"];const c=j.controls;if(!Array.isArray(c))throw new Error(\"no controls ledger\");const names=c.map(x=>x&&x.name);const uniq=new Set(names);if(uniq.size!==7||names.length!==7)throw new Error(\"controls not exactly 7 unique: \"+JSON.stringify(names));for(const w of want){const hit=c.find(x=>x&&x.name===w);if(!hit)throw new Error(\"missing control \"+w);if(hit.passed!==true)throw new Error(\"control not passed: \"+w)}if(j.positives!==9)throw new Error(\"positives total must be exactly 9, got: \"+j.positives)" "$T" || exit 1; rm -f "$T" || exit 1; trap - EXIT'
      text: 'the self-check emits a machine JSON control ledger whose `controls` array holds EXACTLY the seven unique named cases later-common, scalar-shadow, object-shadow, locale-divergent, dynamic-array, property-access and non-literal, each with `passed:true`, plus a `positives` total of EXACTLY nine — a banner or free-floating names cannot satisfy it'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; set -u; R="$PWD"; B=4cdfdf28725f4c48d00ccb6f7aec2084f032d6cb; D="$(mktemp -d -t p9942b.XXXXXX)" || exit 1; J="$D/c.json"; cleanup() { git -C "$R" worktree remove --force "$D/wt" || return 1; rm -rf "$D" || return 1; test ! -e "$D/wt" || return 1; }; trap "cleanup; exit 1" HUP INT TERM; trap "cleanup || exit 1" EXIT; git -C "$R" worktree add --detach -q "$D/wt" "$B" || exit 1; cp "$R/scripts/i18n-binding-census.mjs" "$D/wt/scripts/" || exit 1; mkdir -p "$D/wt/scripts/fixtures" && cp "$R/scripts/fixtures/i18n-binding-census-controls.tsx" "$D/wt/scripts/fixtures/" || exit 1; ln -sfn "$R/node_modules" "$D/wt/node_modules"; node "$D/wt/scripts/i18n-binding-census.mjs" "$D/wt" --base "$B" --json > "$J" 2>/dev/null; rc=$?; test $rc -eq 1 || exit 1; test -s "$J" || exit 1; node -e "const j=require(process.argv[1]);const p=j.partitions||{};const g=o=>[o&&o.sites,o&&o.files];const eq=(a,b)=>a[0]===b[0]&&a[1]===b[1];if(j.universeSourceRef!==process.argv[2])throw new Error(\"wrong universe: \"+j.universeSourceRef);if(!eq(g(p.union),[213,102])||!eq(g(p.commonColonResolved),[79,52])||!eq(g(p.translationColonExplicit),[5,2])||!eq(g(p.dotForm),[125,48])||!eq(g(p.nsOption),[4,2]))throw new Error(\"partitions wrong: \"+JSON.stringify(p))" "$J" "$B" || exit 1; cleanup || exit 1; trap - EXIT'
      text: 'at the fixed pre-flatten base the checker exits with its documented findings status 1 and complete parseable JSON naming that base as `universeSourceRef`, reporting the controlled union 213 sites / 102 files partitioned exactly 79/52 + 5/2 + 125/48 + 4/2 as separate named fields'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; set -u; R="$PWD"; B=4cdfdf28725f4c48d00ccb6f7aec2084f032d6cb; D="$(mktemp -d -t p9942c.XXXXXX)" || exit 1; J="$D/c.json"; cleanup() { git -C "$R" worktree remove --force "$D/wt" || return 1; rm -rf "$D" || return 1; test ! -e "$D/wt" || return 1; }; trap "cleanup; exit 1" HUP INT TERM; trap "cleanup || exit 1" EXIT; git -C "$R" worktree add --detach -q "$D/wt" "$B" || exit 1; cp "$R/scripts/i18n-binding-census.mjs" "$D/wt/scripts/" || exit 1; mkdir -p "$D/wt/scripts/fixtures" && cp "$R/scripts/fixtures/i18n-binding-census-controls.tsx" "$D/wt/scripts/fixtures/" || exit 1; ln -sfn "$R/node_modules" "$D/wt/node_modules"; node "$D/wt/scripts/i18n-binding-census.mjs" "$D/wt" --base "$B" --json > "$J" 2>/dev/null; rc=$?; test $rc -eq 1 || exit 1; test -s "$J" || exit 1; node -e "const j=require(process.argv[1]);const d=j.deprefixClass||{};const b=j.blind||{};const g=o=>[o&&o.sites,o&&o.files];const eq=(a,b2)=>a[0]===b2[0]&&a[1]===b2[1];const need=[[d.total,[100,59],\"deprefix.total\"],[d.resolved,[79,52],\"deprefix.resolved\"],[d.outside,[21,14],\"deprefix.outside\"],[b.dynamicNamespace,[276,27],\"blind.dynamicNamespace\"],[b.dynamicNamespaceLiteralKey,[236,25],\"blind.dynLiteral\"],[b.dynamicNamespaceNonLiteralKey,[40,16],\"blind.dynNonLiteral\"],[b.nonLiteralKey,[725,295],\"blind.nonLiteralKey\"],[b.propertyAccess,[0,0],\"blind.propertyAccess\"],[b.union,[961,303],\"blind.union\"]];for(const [o,want,name] of need){if(!o||!Number.isInteger(o.sites)||!Number.isInteger(o.files))throw new Error(\"missing or non-integer pair: \"+name);if(!eq(g(o),want))throw new Error(name+\" wrong: \"+JSON.stringify(o))}" "$J" || exit 1; cleanup || exit 1; trap - EXIT'
      text: 'the same historical run reports the FULL `common:common.*` de-prefix class as 100/59 split 79/52 resolved + 21/14 outside, and every blind population as an EXACT integer PAIR — dynamic namespace 276/27 (236/25 literal-key + 40/16 non-literal-key), non-literal key 725/295, property access 0/0, blind union 961/303 — where an absent field fails rather than reading as zero'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; set -u; R="$PWD"; B=4cdfdf28725f4c48d00ccb6f7aec2084f032d6cb; node "$R/scripts/i18n-binding-census.mjs" "$R" --base definitely-not-a-ref --json > /dev/null 2>&1; test $? -ne 0 || exit 1; T="$(mktemp -t p9942d.XXXXXX)" || exit 1; trap "rm -f \"$T\"; exit 1" HUP INT TERM; trap "rm -f \"$T\"" EXIT; node "$R/scripts/i18n-binding-census.mjs" "$R" --base "$B" --json > "$T" 2>/dev/null; rc=$?; test $rc -eq 0 || exit 1; test -s "$T" || exit 1; node -e "const j=JSON.parse(require(\"fs\").readFileSync(process.argv[1],\"utf8\"));const g=o=>[o&&o.sites,o&&o.files];const eq=(a,b)=>a[0]===b[0]&&a[1]===b[1];if(j.universeSourceRef!==process.argv[2])throw new Error(\"wrong universe: \"+j.universeSourceRef);const gv=j.governed;if(!gv||!Number.isInteger(gv.sites)||!Number.isInteger(gv.files))throw new Error(\"governed pair missing or non-integer: \"+JSON.stringify(gv));if(!eq(g(gv),[213,102]))throw new Error(\"governed population wrong: \"+JSON.stringify(gv));if(!Number.isInteger(j.scannedFiles)||!Number.isInteger(j.literalKeyCalls)||!Number.isInteger(j.nonLiteralKeyCalls))throw new Error(\"scanner fields must be integers: \"+JSON.stringify([j.scannedFiles,j.literalKeyCalls,j.nonLiteralKeyCalls]));if(!(j.scannedFiles>=1700))throw new Error(\"scannedFiles floor: \"+j.scannedFiles);if(!((j.literalKeyCalls+j.nonLiteralKeyCalls)>=9000))throw new Error(\"call population floor: \"+j.literalKeyCalls+\"/\"+j.nonLiteralKeyCalls);const b=j.blind||{};const pair=(o,n)=>{if(!o||!Number.isInteger(o.sites)||!Number.isInteger(o.files))throw new Error(\"missing or non-integer blind pair: \"+n);return [o.sites,o.files]};const landed=[[b.dynamicNamespace,[274,27],\"blind.dynamicNamespace\"],[b.dynamicNamespaceLiteralKey,[234,25],\"blind.dynamicNamespaceLiteralKey\"],[b.dynamicNamespaceNonLiteralKey,[40,16],\"blind.dynamicNamespaceNonLiteralKey\"],[b.nonLiteralKey,[725,295],\"blind.nonLiteralKey\"],[b.propertyAccess,[0,0],\"blind.propertyAccess\"],[b.union,[959,303],\"blind.union\"]];for(const [o,want,n] of landed){if(!eq(pair(o,n),want))throw new Error(n+\" wrong: \"+JSON.stringify(o))}if(!Array.isArray(j.unrepointed)||j.unrepointed.length!==0||j.doublePrefixed!==0)throw new Error(\"post-flatten residue: \"+j.unrepointed.length+\"/\"+j.doublePrefixed);if(!Array.isArray(j.localeDivergent))throw new Error(\"localeDivergent not reported\")" "$T" "$B" || exit 1; rm -f "$T" || exit 1; trap - EXIT'
      text: 'a bad `--base` exits NONZERO, and the landed tree scanned against the FIXED pre-flatten base 4cdfdf287 exits 0 naming that universe, reports the governed population 213/102, proves a real scan (`scannedFiles` >= 1700 and >= 9000 modelled calls), pins ALL SIX landed blind pairs exactly — dynamic 274/27 (234/25 literal + 40/16 non-literal), non-literal 725/295, property 0/0, union 959/303, which differ from the base pairs by construction — reports `localeDivergent`, and shows zero unrepointed and zero double-prefix residue'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; set -u; R="$PWD"; S="$R/.planning/phases/99-arabic-coverage/99-07-SUMMARY.md"; test -f "$S" || exit 1; command grep -qiF "pristine \`git archive\`" "$S" && exit 1; command grep -qxF "PROVENANCE: isolated git worktree at 4cdfdf28725f4c48d00ccb6f7aec2084f032d6cb" "$S" || exit 1; command grep -qxF "BOUNDARY EXCEPTION: frontend/src/routes/__root.tsx common:dashboard.title preserved because common.dashboard was absent pre-flatten and the dot form rendered English on the Arabic root navigation" "$S" || exit 1'
      text: 'the original 99-07 SUMMARY carries an EXACT affirmative provenance line naming the isolated git worktree and base SHA, and an EXACT boundary-exception line naming `frontend/src/routes/__root.tsx` and `common:dashboard.title` as preserved for the Arabic nav — both matched as WHOLE LINES so `NOT PROVENANCE:` or `NO BOUNDARY EXCEPTION:` cannot pass, while only the superseded pristine-git-archive phrase is banned so truthful historical prose survives'
---

<objective>
Repair the i18n binding-census proof instrument so its effective-namespace model is ordered, resource-aware and locale-aware. RULING-P99-114 found the shipped checker unsound: it marks a namespace array common-routed when any static element aliases common.json, so it accepts the defining earlier-shadow negative. The merged runtime flatten is correct and is NOT reopened here; only the proof instrument and the two summaries change.
</objective>

<context>
@.planning/phases/99-arabic-coverage/99-07-PLAN.md
@scripts/i18n-binding-census.mjs
@scripts/fixtures/i18n-binding-census-controls.tsx
</context>

# P99-42 — repair the binding-census proof instrument

## Why this task exists

`useTranslation(['sla','translation']); t('common.actions')` resolves at `sla` in both locales, because
`sla.common.actions` is a scalar in each. The shipped checker flags it anyway. A method that fails a
defining negative control cannot prove its positives. Two cross-vendor audits and an OVERSEER
re-execution agree the CORRECTED resource-aware model reproduces the same 213 / 102 population: the
number is right, the instrument is not.

## Boundaries

Outside this task: any application, route, component or locale-JSON change; any reopening of the flatten;
any change to `99-08`, `99-14` or `99-41`; any cap or barrier movement. Every population claim is measured
against the fixed pre-flatten base `4cdfdf28725f4c48d00ccb6f7aec2084f032d6cb`. Naming
`frontend/src/routes/__root.tsx` in SUMMARY prose is required; modifying it is forbidden.
