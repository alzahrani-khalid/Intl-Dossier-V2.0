---
phase: 99-arabic-coverage
plan: 20
type: execute
wave: 5
depends_on: [99-06, 99-07, 99-16, 99-19]
files_modified:
  - 'frontend/src/{lib/format-date.ts,components/forums/ForumDetailsDialog.tsx,components/availability-polling/AvailabilityPollResults.tsx,components/assignments/EscalationDashboard.tsx,components/calendar/UnifiedCalendar.tsx,components/dossier/DossierDrawer/UpcomingSection.tsx,components/signals/SignalRow.tsx,components/meeting-minutes/ActionItemsList.tsx,components/dossiers/DossierMoUsTab.tsx,components/engagement-recommendations/RecommendationCard.tsx,components/sla-monitoring/SLAComplianceChart.tsx,components/analytics/CommitmentFulfillmentChart.tsx,components/analytics/EngagementMetricsChart.tsx,components/analytics/RelationshipHealthChart.tsx,pages/forums/ForumsPage.tsx,pages/data-library/DataLibraryPage.tsx,pages/intelligence/IntelligencePage.tsx,pages/Dashboard/widgets/MyTasks.tsx,pages/Dashboard/widgets/WeekAhead.tsx,pages/MoUs/MousPage.tsx,pages/WorkBoard/KCard.tsx,pages/events/EventsPage.tsx,pages/reports/ReportsPage.tsx,components/empty-states/NotificationPreviewTimeline.tsx}'
  - .planning/phases/99-arabic-coverage/99-20-SUMMARY.md
autonomous: true
requirements: [AR-02]
success_criterion: 'Land the formatter and the raw-site census for the AR-02 date lane, proven by the acceptance items below in a single pass.'
routing:
  floor: frontier
truths:
  - "Decisions covered — D-30: GST is ALLOWLISTED — formatTime keeps emitting 14:30 GST and no توقيت الخليج substitution is made (revisit trigger: the operator Arabic review objecting); D-31: Latin digits are DELIBERATE POLICY — the repair routes through ar-u-nu-latn or the date-fns ar locale object, and an Arabic-Indic digit anywhere in main is a REGRESSION the spec REDs on; D-07: the criterion closes on the RENDERED 99-ar02 surfaces, not on this plan's source greps alone; D-29: UI99-C1..C4 are the binding UI-contract rows this plan closes and they close on the RENDERED 99-ar02 spec, invoked by this plan's own command oracle at its hardcoded 8-test count — not on the source-literal grep beside it, which D-07 forbids as a sole closer; UI99-C1/C2/C4: no English weekday/month token in main under ar, zero Indic codepoints, month headers Arabic — the contract rows this plan turns green."
must_haves:
  truths:
    - 'Decisions covered — D-07, D-09, D-29, D-30, D-31 + UI99-C1/C2/C3/C4: the formatter localizes name tokens under ar at call time, digits stay Latin, GST stays, relative time stays the sanctioned formatRelativeTime output.'
    - "The four absolute formatters in lib/format-date.ts read the active language at call time (the formatRelativeTime idiom — never a build-time constant, so a mid-session flip re-renders correctly): under ar they emit Arabic weekday/month names with Latin digits; under en their output is BYTE-IDENTICAL to today (the doc comments are amended: Policy D's digit half survives, its name half is superseded by AR-02 — cite the phase)"
    - 'Every raw date-fns English-name format site in the listed files (26 sites / 22 files at plan time — re-derive with the recorded grep and reconcile) routes through lib/format-date helpers; after the change the ONLY file under frontend/src carrying an English month/weekday format literal is lib/format-date.ts itself'
    - 'scripts/check-date-formatting.mjs stays STRICT-green at 0 burn-down rows — no new toLocaleDateString, no ar-SA literal, no 12-hour token enters with the repair'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; ! (command grep -rnE "format\([^)]*[''\"][^''\"]*\b(MMM|MMMM|EEE)\b" "$R/frontend/src" --include="*.tsx" --include="*.ts" | command grep -v "lib/format-date" | command grep -v -E "\.test\.|__tests__") && command grep -qE "\b(MMM|EEE)\b" "$R/frontend/src/lib/format-date.ts" && node "$R/scripts/check-date-formatting.mjs" && cd "$R" && test -f tests/e2e/99-ar02-dates.spec.ts && pnpm exec playwright test tests/e2e/99-ar02-dates.spec.ts -g "UI99-C1C2C4 ar /calendar|UI99-C1 en control /calendar" --project=chromium-en --no-deps --list 2>&1 | command grep -qE "Total: 2 tests in 1 file" && pnpm exec playwright test tests/e2e/99-ar02-dates.spec.ts -g "UI99-C1C2C4 ar /calendar|UI99-C1 en control /calendar" --project=chromium-en --no-deps'
      text: 'zero English-name date-fns format literals outside lib/format-date.ts (positive control: the formatter itself still carries the en-leg tokens, so the pattern is provably findable) AND the date guard stays green — RED at HEAD (26 sites match)'
---

<objective>
Land the formatter and the raw-site census for the AR-02 date lane, proven by the acceptance items below in a single pass.

This task is part 1 of 2 of the AR-02 date lane; the parts are ordered by `depends_on` and share one
file surface, so each part reads the tree the part before it produced. The task-unit contract caps a
task at six acceptance items and twenty-four surface, which is why the lane is cut this way.
</objective>

<context>
@.planning/phases/99-arabic-coverage/99-CONTEXT.md
@.planning/phases/99-arabic-coverage/99-RESEARCH.md
@frontend/src/lib/format-date.ts
@frontend/src/lib/format-locale.ts
@scripts/check-date-formatting.mjs
@tests/e2e/99-ar02-dates.spec.ts

Ref notes: 99-CONTEXT.md — D-07, D-09, D-29..D-31; 99-RESEARCH.md — §5 in full — the formatter finding, the guard blindness list, §5.4 the discriminator, UI99-C1..C4; format-locale.ts — toFormatLocale — the ar-u-nu-latn pin; check-date-formatting.mjs — the guard — order-checking, locale-blind; it must stay green, it cannot prove this plan; 99-ar02-dates.spec.ts — the oracle this plan turns green

<interfaces>
Verified 2026-08-18 at 9ffa2b80c:

- format-date.ts: formatDayFirst / formatTime / formatDayFirstYear / formatDateTime hardcode
  en-GB with `_locale` deliberately unused (Policy D comments); formatRelativeTime already reads
  i18n.language at call time and passes the date-fns ar locale OBJECT — the idiom to mirror.
  154 consumer files (re-derive: grep -rln "lib/format-date").
- The raw-site census (re-derive with: command grep -rnE "format\((._,\s_)?['\"][^'\"]\*(EEE|MMM)"
  over frontend/src, excluding tests and format-date itself): 26 sites / 22 files at plan time,
  including the two MMMM yyyy month-nav headers (UnifiedCalendar, EventsPage) whose Arabic was
  explicitly routed to AR-02 by P98's D-25. Sites already passing an ar-aware { locale } are
  verified, not churned — classify each site in the SUMMARY.
- Digits: format-locale.ts pins ar → ar-u-nu-latn explicitly; date-fns ar locale interpolates
  with String() (Latin). NEVER format with bare ar-SA or default ar number formatting — the
  guard bans the literal and UI99-C2 REDs the render.
- The chart-axis sites (SLAComplianceChart, the three analytics charts) are user-visible date
  labels — in population, same mechanism.
- The 99-ar02 spec's en leg asserts the byte-exact English shape survives — the non-regression
  half of the flip.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>the AR-02 date lane — the formatter and the raw-site census</name>
  <files>the paths in files_modified</files>
  <read_first>lib/format-date.ts in full (all five functions + Policy D comments); lib/format-locale.ts; the 26-site census re-derived; tests/e2e/99-ar02-dates.spec.ts (what green requires)</read_first>
  <action>
    This is the first part of the lane, so the lane method below is yours to execute in full over the paths in files_modified. Re-derive every population before relying on it (D-04), run each oracle and record its verbatim output, and name in the SUMMARY anything you leave for a later part.

    --- THE LANE METHOD (carried verbatim from the pre-recut plan; the acceptance items above bound what THIS task owes) ---

    1. Re-derive the raw-site census and classify (already-localized / formatter-routable /
       needs-locale-param); record it.
    2. Amend the four absolute formatters: read the active language at call time; ar output uses
       Arabic month/weekday names with Latin digits (date-fns ar locale object or Intl with
       toFormatLocale); en output byte-identical to today (unit-assert a fixed date across both
       languages inside the existing format-date test file if one exists — otherwise the spec's
       en leg is the guard). Amend the Policy D doc comments to record the AR-02 supersession of
       the name half.
    3. Route the raw sites through format-date helpers (extending the module with a localized
       month-year helper for the two MMMM yyyy headers is in-scope for format-date.ts); after
       this step no file outside lib/format-date.ts carries an English month/weekday format
       literal.
    4. Walk NotificationPreviewTimeline's timeAgo provenance; repair or record per the truth.
    5. Verify: the command-oracle grep predicate; the date guard green; then run the 99-ar02
       spec (single path, chromium-en, both legs) and record the green with the en-leg
       non-regression output.

  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes. The lane's own criteria, carried: The census is recorded with per-site classification; zero English-name format literals outside the formatter (positive control in the oracle); the guard stays at 0 rows; the 99-ar02 spec — authored RED in 99-01 — runs GREEN in both legs with the en leg byte-identical; the provenance walk is recorded; type-check green. The rendered run is a `command:` truth in this plan's own acceptance, so it is not optional and there is no NOT-CONSTRUCTED path: an unrunnable spec leaves this task RED and parks to the overseer by name.</acceptance_criteria>
  <done>An Arabic session's dates read Arabic with Latin digits; English dates are unchanged; the rendered oracle flipped, observed by this task's own command oracle running the /calendar ar leg and its en control rather than reported in prose.</done>
</task>

</tasks>

<verification>
POPULATION: the paths in files_modified. OUTSIDE IT: every other task's files, the exogenous paths
(D-03), and every obligation this task's acceptance items do not name — those belong to the lane
parts before and after this one, named in `depends_on` and in the SUMMARY.

The lane's own population statement, carried verbatim from the pre-recut plan:
POPULATION: the 25 listed files. OUTSIDE IT: NotificationList's hardcoded bilingual headers
(correct Arabic today — a maintenance defect outside criterion 2, recorded not repaired), i18n
JSON values (the glossary lanes), the GST token (allowlisted, D-30), and every date INSIDE test files.
</verification>

<success_criteria>
Land the formatter and the raw-site census for the AR-02 date lane, proven by the acceptance items below in a single pass.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-20-SUMMARY.md` when done. It MUST record every command this task ran with its verbatim output, the
populations it re-derived with the commands that produced them, and anything left for a named later
task. The lane's own SUMMARY contract, carried verbatim from the pre-recut plan (satisfy the parts
this task owns): record in the file named under <output> (99-09-SUMMARY.md in the pre-recut plan) when done. It MUST record: the
re-derived census with per-site classification; the formatter design chosen (call-time read
mechanism, ar name source); the 99-ar02 run results per leg; the provenance-walk verdict; and
the guard's 0-row output.
</output>
