---
phase: 99-arabic-coverage
plan: 14
type: execute
wave: 3
depends_on: [99-06, 99-07]
files_modified:
  - 'frontend/src/i18n/{en/common.json,ar/common.json,en/admin.json,ar/admin.json,en/forums.json,ar/forums.json,en/field-permissions.json,ar/field-permissions.json,en/dashboard-widgets.json,ar/dashboard-widgets.json,en/guided-tours.json,ar/guided-tours.json,en/milestone-planning.json,ar/milestone-planning.json,en/settings.json,ar/settings.json,en/ai-admin.json,ar/ai-admin.json,en/entity-linking.json,ar/entity-linking.json,en/dossier-recommendations.json,ar/dossier-recommendations.json,en/progressive-disclosure.json,ar/progressive-disclosure.json,en/validation.json,ar/validation.json,en/tags.json,ar/tags.json,en/briefing-books.json,ar/briefing-books.json,en/collaboration.json,ar/collaboration.json,en/actionable-errors.json,ar/actionable-errors.json,en/workflow-automation.json,ar/workflow-automation.json,en/operations-hub.json,ar/operations-hub.json}'
  - .planning/phases/99-arabic-coverage/99-14-SUMMARY.md
# TEMP RULING-P99-108 CAP BARRIER: remove after P99-07 completes and both overlays return to 60000.
autonomous: false
requirements: [AR-01, AR-03, AR-04a, AR-04b]
success_criterion: 'Land the long tail and the nav keys for the common-owner authoring lane, proven by the acceptance items below in a single pass.'
routing:
  floor: frontier
truths:
  - "Decisions covered — D-26: this plan is the SINGLE owner of common.json after the flatten — the long tail authors here while the heavy files were re-bound by the family lanes — and no key literally named common is ever authored at top level; D-25: every key is authored TWICE, en byte-identical to the mask literal where one exists; D-24 steps 1 only: this plan AUTHORS and drops nothing — it does not even rewrite a call site, so the D-24 hazard cannot arise inside it; D-15: every key this plan authors is expressed against the POST-flatten shape (notFound.* at the namespace root, error.label / search.label for the two relocated scalars) and no pre-flatten shape is hardcoded anywhere; D-27: the four dynamic carriers' enum key SETS are authored by P99-16 (part 3 of this lane), never pre-authored here; D-19: term values live in i18n leaves, never inlined in code, so the glossary lanes can still rename them mechanically; D-05: the population is stated with what falls outside it — this plan writes JSON and nothing else."
must_haves:
  truths:
    - 'Decisions covered — D-05, D-15, D-19, D-24(1), D-25, D-26: JSON-only authoring, both locales, post-flatten shapes, inversion guard held. (D-27's enum sets are P99-16's, part 3 of this lane.)'
    - "ZERO source files change in this plan. Its diff contains only frontend/src/i18n/**/*.json and its own SUMMARY. The mechanism split is deliberate: authoring and rewriting were one 88-file task in the first draft, measured at ~50,700 logic bytes against the engine's 60,000-byte gates.diffCap, and a diff-cap trip parks un-retryably AFTER the work is done."
    - 'No top-level key literally named common is authored in either locale (the D-15 inversion guard), and no key is authored at a double-prefixed path (`common.actions.*` inside common.json is the inversion re-armed under another name) — the de-prefixed form `actions.*` at the root is what the rewrite lane points at'
    - 'navigation.dashboardOverview, navigation.taskQueue, navigation.taskEscalations and the navigation.workflow tooltip exist in BOTH locales with ar values consistent with the ruled glossary — re-derive the missing set from navigationData.ts x both common.json at task start rather than trusting this list'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node -e "const fs=require(\"fs\");const load=l=>JSON.parse(fs.readFileSync(process.argv[1]+\"/frontend/src/i18n/\"+l+\"/common.json\",\"utf8\"));for(const loc of [\"en\",\"ar\"]){const j=load(loc);if(j.common!==undefined){console.error(loc+\" top-level common key re-created — inversion guard tripped\");process.exit(1)}if(typeof j.error?.label!==\"string\"||typeof j.search?.label!==\"string\"){console.error(loc+\" the relocated scalars are gone — this plan overwrote the flatten\");process.exit(1)}for(const k of [\"dashboardOverview\",\"taskQueue\",\"taskEscalations\",\"workflow\"])if(j.navigation?.[k]===undefined){console.error(loc+\" navigation.\"+k+\" missing\");process.exit(1)}for(const k of [\"noResults\",\"goBack\",\"collapse\",\"expand\",\"add\",\"more\",\"cardView\",\"tableView\",\"toggleColumns\",\"firstPage\",\"previousPage\",\"nextPage\",\"lastPage\"])if(j[k]===undefined){console.error(loc+\" missAll key missing at root: \"+k);process.exit(1)}if(j.priority!==undefined||j.work_item!==undefined){console.error(loc+\" priority/work_item authored into common — the phase negative control would be defused\");process.exit(1)}}console.log(\"AUTHOR-OK\")" "$R" && test "$(node "$R/scripts/neg-taskcard.mjs" "$R" | command grep -c "MISS=true")" -eq 3'
      text: 'both locales carry the four nav keys and the thirteen missAll keys at the flattened root, the inversion guard holds, the relocated error.label/search.label survive this plan, no priority/work_item key was authored into common, and the phase negative control still prints 3x MISS=true (text asserted, its exit code is 0 either way) — RED at HEAD on the nav and missAll clauses'
---

<objective>
Land the long tail and the nav keys for the common-owner authoring lane, proven by the acceptance items below in a single pass.

This task is part 1 of 3 of the common-owner authoring lane; the parts are ordered by `depends_on` and share one
file surface, so each part reads the tree the part before it produced. The task-unit contract caps a
task at six acceptance items and twenty-four surface, which is why the lane is cut this way.
</objective>

<context>
@.planning/phases/99-arabic-coverage/99-CONTEXT.md
@.planning/phases/99-arabic-coverage/99-RESEARCH.md
@scripts/partA_maskfinder.py
@frontend/src/components/modern-nav/navigationData.ts
@frontend/src/i18n/en/common.json
@frontend/src/i18n/ar/common.json

Ref notes: 99-CONTEXT.md — D-15, D-19, D-24..D-27; 99-RESEARCH.md — §1.5 the common bucket; §2.3 the colon-miss classes; §3 the dynamic members; navigationData.ts — 28 labelKeys + category tooltips; the two common.json files are read POST-FLATTEN — the shape this plan authors into is the flattened root, never the nested subtree

<interfaces>
Verified 2026-08-18 at 9ffa2b80c, to be RE-DERIVED post-flatten at task start (D-04):

- Missing nav keys in BOTH locales: navigation.dashboardOverview, navigation.taskQueue,
  navigation.taskEscalations, tooltip navigation.workflow.
- The de-prefixed residual set: the flatten plan de-prefixed 100 `common:common.X` sites (59
  files, 37 distinct keys). 79 of those sites resolved through the nested subtree and now resolve
  at the root for free; the ~21 whose key was absent are named in that plan's SUMMARY and are
  authored HERE. `common.common.actions` held 9 keys, so the actions family is mostly a free ride
  and the derivation must be run rather than assumed either way.
- The missAll keys the flatten plan left byte-untouched, verified absent from the root before and
  after the flatten: noResults, goBack, collapse, expand, add, more, cardView, tableView,
  toggleColumns, firstPage, previousPage, nextPage, lastPage. (`dashboard` is NOT in this set — it
  already exists at the root, so the flatten plan owned its rewrite.)
- Authoring-only consumers (they bind common/translation or their own ns and are never edited by
  any plan): the modern-nav panels, utils/ai-errors.ts, QueryErrorBoundary, EditApprovalFlow,
  PDFGeneratorButton, VersionHistoryViewer, AutoSaveFormWrapper, WorkItemLinker, after-actions
  routes, admin/system, field-permissions and settings sections, users pages, EscalationDashboard,
  SignalRow, LegislationDetail/List raw tails, TagHierarchyManager, ProgressiveEmptyState, plus
  the named handoffs the family lanes record in their SUMMARIES.
- One class that will surface here and has no owner elsewhere: 12 raw-key sites in six `__tests__`
  files plus 2 in components/active-filters/useActiveFilters.ts and 3 in
  components/signals/SignalRow.tsx. Driving the test-file sites to zero would mean authoring
  test-mock keys into the shipped bundle. RESOLUTION: the strict instrument excludes `*.test.*`
  and `__tests__` from BOTH classes (99-02 authors it that way; a single stated test-file policy
  for both classes, not one per clause), and the 5 non-test sites are authored here and named in
  the SUMMARY.
- Estimated logic-diff size for this task: **~37,100 bytes** — ~412 distinct keys x 2 locales of
  added JSON lines at the measured average line length, added lines counting once. Stated as a
  floor; the instrument is the committed loose audit's missing-key lists, and the re-derivation
  at task start governs.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>the common-owner authoring lane — the long tail and the nav keys</name>
  <files>the paths in files_modified</files>
  <read_first>en/ar common.json POST-flatten (the shape you author into); the flatten plan's SUMMARY (the de-prefixed keys it left missing, by name); the family lanes' SUMMARIES (their named handoffs); 99-RESEARCH.md §2.3, §3</read_first>
  <action>
    This is the first part of the lane, so the lane method below is yours to execute in full over the paths in files_modified. Re-derive every population before relying on it (D-04), run each oracle and record its verbatim output, and name in the SUMMARY anything you leave for a later part.

    --- THE LANE METHOD (carried verbatim from the pre-recut plan; the acceptance items above bound what THIS task owes) ---

    1. Run the strict audit UNSCOPED; subtract the family lanes' file sets; the remainder targeting
       common and this plan's small namespaces is the authoring queue — record it with its
       derivation and its per-namespace breakdown.
    2. Author every queued key in en AND ar (en byte-identical to the mask literal where present;
       ar per ruled glossary + voice law). Author into the existing subtree shapes; never create a
       top-level key named common; never author a `common.*` path inside common.json.
    3. Author the four nav keys, the thirteen missAll keys, and the de-prefixed residual keys.
       (The chrome-literal keys and the four dynamic carriers' enum sets are part 3's — P99-16.)
    4. Verify: the inversion + nav + missAll oracle; full en/ar leaf parity across every namespace;
       neg-taskcard still 3x MISS=true; and confirm `git status` shows no source file changed.

  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes. The lane's own criteria, carried: The authoring queue is recorded with its derivation and per-namespace counts; both oracles pass; en/ar leaf parity holds repo-wide with the walked-leaf count printed as the control; the diff contains no file outside frontend/src/i18n and this plan's SUMMARY.</acceptance_criteria>
  <done>Every key this part owns — the long tail, the four nav keys, the thirteen missAll keys, and the de-prefixed residual keys — exists in both locales, at the post-flatten shape, with the inversion guard held; the chrome keys and carrier enum sets are P99-16's surface.</done>
</task>

</tasks>

<verification>
POPULATION: the paths in files_modified. OUTSIDE IT: every other task's files, the exogenous paths
(D-03), and every obligation this task's acceptance items do not name — those belong to the lane
parts before and after this one, named in `depends_on` and in the SUMMARY.

The lane's own population statement, carried verbatim from the pre-recut plan:
POPULATION: the 39 listed files. OUTSIDE IT: every source file without exception (the rewrite lane
owns them), the family lanes' namespaces, all ar VALUE sweeping (the glossary lanes), and every
second-argument drop (the deletion lanes).
</verification>

<success_criteria>
Land the long tail and the nav keys for the common-owner authoring lane, proven by the acceptance items below in a single pass.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-14-SUMMARY.md` when done. It MUST record every command this task ran with its verbatim output, the
populations it re-derived with the commands that produced them, and anything left for a named later
task. The lane's own SUMMARY contract (the parts this task owns, post-recut): it MUST record: the
authoring queue derivation with counts by namespace; the four nav keys with both values; the
de-prefixed residual keys authored, by name; the five non-test raw-key sites authored and the
test-file policy applied; the parity walk output; and the statement that no source file changed.
(The enum sets per dynamic carrier and the chrome key names are recorded by P99-16's SUMMARY.)
</output>
