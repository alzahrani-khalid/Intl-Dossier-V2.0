---
phase: 99-arabic-coverage
plan: 12
type: execute
wave: 3
depends_on: [99-06, 99-07]
# RULING-P99-163 split (part A): this plan is AUTHORING ONLY. The colon-form source rewrite, the
# banner extraction, every test edit, and the tests/setup.ts instrument cutover moved to 99-43
# (the dedicated cutover task, sequenced LAST). The 99-10 PORT-CONTENTION edge moved to 99-43
# with the rendered oracle — this plan runs no rendered surface.
files_modified:
  - 'frontend/src/i18n/{en/assignments.json,ar/assignments.json,en/positions.json,ar/positions.json,en/tasks-page.json,ar/tasks-page.json,en/commitments.json,ar/commitments.json}'
  - .planning/phases/99-arabic-coverage/99-12-SUMMARY.md
autonomous: true
requirements: [AR-03, AR-04a, AR-04b]
success_criterion: 'Author the tasks/queues/positions lane keys in both locales, proven by the acceptance items below in a single pass.'
routing:
  floor: frontier
  pin: { via: omp, model: anthropic/claude-opus-5 }
truths:
  - 'Decisions covered — D-18, D-24(1-2), D-25, D-26: authoring-first in both locales; every key authored twice with en byte-identical to the mask literal; placement into the assignments:/positions:/tasks-page:/commitments: namespaces; ar per glossary (مهمة for task, تكليف for assignment, موقف for stance — منصب is reserved for office/post and never appears in stance-sense copy). Source rewrites, test edits, and the instrument cutover are OUTSIDE this plan by RULING-P99-163 — they are 99-43, which consumes the keys this plan authors.'
must_haves:
  truths:
    - 'Decisions covered — D-18, D-24(1-2), D-25, D-26: key authoring only, both locales, en byte-identical to mask literals, glossary register; the split boundary per RULING-P99-163 keeps every non-bundle byte untouched.'
    - 'The authored set covers the lane populations recorded in .planning/phases/99-arabic-coverage/99-12-ATT2-LEDGER.md §1 and the full enum sets of the six dynamic carriers derived from their consuming types — authored in BOTH locales; where an enum family already resolves (the priority.low family), only the probed gaps are authored, never duplicate subtrees; no key lands in common.json or the translation alias (that would defuse the phase negative control); every source, test, and setup byte is untouched'
    - 'The three read-only banner sentences are authored as positions: readOnlyBanner keys (under_review / approved / published) in en and ar — ar in the موقف register, sentence case, no exclamation — and the positions keys the MFA step-up surface consumes (ledger §1 population) are authored; key authoring ONLY, every consumer file stays byte-untouched; the four pre-existing ar-only keys in assignments are OUTSIDE this population and are left, named in the SUMMARY'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node -e "const fs=require(\"fs\");const p=l=>JSON.parse(fs.readFileSync(process.argv[1]+\"/frontend/src/i18n/\"+l,\"utf8\"));const flat=(o,pre)=>Object.entries(o).flatMap(([k,v])=>v&&typeof v===\"object\"?flat(v,pre+k+\".\"):[pre+k]);let bad=0;for(const loc of[\"en\",\"ar\"]){const pos=p(loc+\"/positions.json\");for(const s of[\"under_review\",\"approved\",\"published\"]){const v=pos.readOnlyBanner&&pos.readOnlyBanner[s];if(typeof v!==\"string\"||!v.length){console.error(\"missing \"+loc+\" readOnlyBanner.\"+s);bad=1}}}for(const ns of[\"assignments\",\"positions\",\"tasks-page\",\"commitments\"]){const en=flat(p(\"en/\"+ns+\".json\"),\"\");const ar=new Set(flat(p(\"ar/\"+ns+\".json\"),\"\"));for(const k of en)if(!ar.has(k)){console.error(\"ar missing \"+ns+\":\"+k);bad=1}}process.exit(bad)" "$R"'
      text: 'the three banner sentinel keys exist as non-empty strings in BOTH locales, and every en key across the four pairs resolves in ar (en→ar direction only — the four pre-existing ar-only strays in assignments are outside this population) — RED at HEAD on the sentinel clause (the readOnlyBanner subtree does not exist in either locale)'
---

<objective>
Author the tasks/queues/positions lane keys in both locales, proven by the acceptance items below in a single pass.

This is part A of the RULING-P99-163 split of the former P99-12: authoring split out because it is
green under any test instrument, while the colon-form rewrite and every test edit are provably
coupled to the tests/setup.ts cutover (measured in the committed ledger §0.3) and live in 99-43.
</objective>

<context>
@.planning/phases/99-arabic-coverage/99-CONTEXT.md
@.planning/phases/99-arabic-coverage/99-RESEARCH.md
@.planning/phases/99-arabic-coverage/99-12-ATT2-LEDGER.md
@scripts/partA_maskfinder.py

Ref notes: 99-CONTEXT.md — D-18, D-24, D-25, D-26; 99-RESEARCH.md §3 — the dynamic member list;
99-12-ATT2-LEDGER.md — §1 carries the re-derived lane populations and the verbatim authoring
reference (the preserved lineage authored this exact set and its bundle commits gate-passed);
the consuming components are READ-ONLY reference for enum derivation — no source file changes.

<interfaces>
Verified 2026-08-18 at 9ffa2b80c (carried; re-derive at start per D-04):

- The assignments pair needs the priority/status/work_item enum gaps (probe the existing
  priority.low family first; author only gaps) and the waitingQueue status/priority/entityType
  subtree.
- The commitments pair needs the tracking enums — the aa_commitments status lifecycle is FIVE
  values (pending/in_progress/completed/cancelled/overdue — no review), per the project verified
  carve-out; enumerate from the consuming type.
- The positions pair needs the three readOnlyBanner sentences (both locales, موقف register) and
  the step-up surface keys the ledger §1 names.
- The tasks-page pair needs the gaps the ledger §1 population records.
- Re-derive every population before relying on it (D-04); record starting and ending numbers.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>the tasks/queues/positions lane — authoring only</name>
  <files>the paths in files_modified</files>
  <read_first>en/ar assignments.json (extend existing enums, never duplicate); 99-12-ATT2-LEDGER.md §1; 99-RESEARCH.md §3</read_first>
  <action>
    Author every missing key the lane consumes, in en AND ar, across the four pairs — en byte
    identical to the mask literal (D-25), ar per the glossary register (D-18), placement per D-26.
    Derive enum sets from the consuming types (read-only). One commit per coherent key family.
    Do NOT touch any source, test, or setup file; do NOT author into common.json or the
    translation alias. Run the oracle and record its verbatim output.
  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes.</acceptance_criteria>
  <done>The four bundle pairs carry every lane key in both locales; 99-43 can retarget onto them without authoring anything.</done>
</task>

</tasks>

<verification>
POPULATION: the 8 bundle files. OUTSIDE IT: every source/test/setup byte (99-43); common.json
(99-22 per RULING-P99-163(3)); the four pre-existing ar-only assignments keys (left, named);
rendered proofs (99-43/99-13); the exogenous paths (D-03).
</verification>

<success_criteria>
Author the tasks/queues/positions lane keys in both locales, proven by the acceptance items below in a single pass.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-12-SUMMARY.md` when done. It MUST record: the
re-derived starting populations with their commands; the enum sets per dynamic carrier; the
banner key names with both locale values; the oracle output verbatim; and every key deliberately
left (the ar-only strays; anything common-targeted) with its named owner.
</output>
