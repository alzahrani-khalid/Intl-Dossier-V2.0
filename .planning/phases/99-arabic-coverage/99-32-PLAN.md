---
phase: 99-arabic-coverage
plan: 32
type: execute
wave: 10
depends_on: [99-30]
files_modified:
  - 'frontend/src/components/{dossier/wizard/review/CountryReviewStep.tsx,dossier/wizard/review/EngagementReviewStep.tsx,dossier/wizard/review/ForumReviewStep.tsx,dossier/wizard/review/OrganizationReviewStep.tsx,dossier/wizard/review/PersonReviewStep.tsx,dossier/wizard/review/TopicReviewStep.tsx,duplicate-comparison/DuplicateComparison.tsx,duplicate-detection/DuplicateCandidateCard.tsx,duplicate-detection/DuplicateCandidatesList.tsx,duplicate-detection/MergeDialog.tsx,elected-officials/ElectedOfficialListTable.tsx,empty-states/ListEmptyState.tsx,empty-states/TutorialEmptyState.tsx,empty-states/VideoTutorial.tsx,engagement-recommendations/RecommendationsPanel.tsx,entity-links/AISuggestionPanel.tsx,entity-links/EntitySearchDialog.tsx,export-import/ImportValidationResults.tsx,forms/ContextualHelp.tsx,forms/FormErrorDisplay.tsx,forms/FormSection.tsx,forms/UserPicker.tsx,forums/ForumDetailsDialog.tsx,geographic-visualization/MapFilterControls.tsx,geographic-visualization/WorldMapVisualization.tsx,guided-tours/OnboardingTourTrigger.tsx,input-dialog/InputDialog.tsx,intake-form/IntakeForm.tsx,intelligence/BilateralOpportunities.tsx,intelligence/EconomicDashboard.tsx}'
  - .planning/phases/99-arabic-coverage/99-32-SUMMARY.md
autonomous: true
requirements: [AR-04a]
success_criterion: 'Land the deletion for mask deletion lane 2, proven by the acceptance items below in a single pass.'
routing:
  floor: frontier
truths:
  - "Decisions covered — D-24: this lane is reachable only behind the verification-only gatekeeper, which proved repo-wide strict zero in both locales in its OWN gate battery — the conjunction order is a DAG property here, not a worker's intention; D-20: the drop is a DELETION, not a conversion — no working dot-form key is rewritten, because churning ~1295 already-resolving sites is the breakage D-20 forbids; D-23 as clarified 2026-08-18 (RULING-P99-05 §4): deleting a redundant second argument at a site whose resolution was just verified is NOT a binding-level change, so colon form is not forced here — that would be D-21's wholesale conversion arriving early; D-28: this lane closes on the two-arg population only and cites no blended AR-04a/AR-04b figure; D-06: every zero is paired with a positive control in the same run; D-39: this lane is FILE-DISJOINT from every sibling deletion lane, which is why they run concurrently."
must_haves:
  truths:
    - "Decisions covered — D-06, D-20, D-23, D-24, D-28, D-39: deletion-only, behind the gatekeeper, no conversion, one population, every zero controlled, file-disjoint from its siblings. || Both mask shapes are deleted across this lane's files: `t('key', 'Literal')` -> `t('key')`, `t('key', 'Literal', opts)` -> `t('key', opts)`, and the object-form fallback-text option is removed while every remaining option is kept — because interpolation options are not masks. The object-form fallback class is a SEPARATE population from the literal class: 314 sites across 103 files repo-wide, 88 of which carry no literal-class site at all, and the first draft's single 161-file scope covered only the literal class while demanding both reach zero."
    - "KEYS are byte-untouched. The diff contains deletions of default arguments and nothing else — no key string, no namespace prefix, no hook, no import moves. Spot-diff a sample and state it. || NO i18n JSON changes in this lane — a JSON edit here means the gatekeeper's proof was wrong or scope broke"
    - 'A mask site found in a file OUTSIDE this lane''s list means the tree moved between the gatekeeper''s manifest and this lane: STOP and record it, do not widen scope || This lane''s estimated logic-diff size is **~40498 bytes** against the engine''s 60,000-byte gates.diffCap, measured with the committed audit''s MULTI-LINE matcher over the actual file text — never with the line-bound grep whose blindness produced the original floor. The budget is ~45,000 per lane, deliberately below the cap because these numbers are FLOORS. A diff-cap trip returns park:"human" with the engine''s own note that the diff cannot shrink by retrying — it is un-retryable, after the work is done, which is why the lane is this size.'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node "$R/scripts/i18n-audit-strict.mjs" "$R" --scope "frontend/src/components/dossier/wizard/review/CountryReviewStep.tsx,frontend/src/components/dossier/wizard/review/EngagementReviewStep.tsx,frontend/src/components/dossier/wizard/review/ForumReviewStep.tsx,frontend/src/components/dossier/wizard/review/OrganizationReviewStep.tsx,frontend/src/components/dossier/wizard/review/PersonReviewStep.tsx,frontend/src/components/dossier/wizard/review/TopicReviewStep.tsx,frontend/src/components/duplicate-comparison/DuplicateComparison.tsx,frontend/src/components/duplicate-detection/DuplicateCandidateCard.tsx,frontend/src/components/duplicate-detection/DuplicateCandidatesList.tsx,frontend/src/components/duplicate-detection/MergeDialog.tsx,frontend/src/components/elected-officials/ElectedOfficialListTable.tsx,frontend/src/components/empty-states/ListEmptyState.tsx,frontend/src/components/empty-states/TutorialEmptyState.tsx,frontend/src/components/empty-states/VideoTutorial.tsx,frontend/src/components/engagement-recommendations/RecommendationsPanel.tsx,frontend/src/components/entity-links/AISuggestionPanel.tsx,frontend/src/components/entity-links/EntitySearchDialog.tsx,frontend/src/components/export-import/ImportValidationResults.tsx,frontend/src/components/forms/ContextualHelp.tsx,frontend/src/components/forms/FormErrorDisplay.tsx,frontend/src/components/forms/FormSection.tsx,frontend/src/components/forms/UserPicker.tsx,frontend/src/components/forums/ForumDetailsDialog.tsx,frontend/src/components/geographic-visualization/MapFilterControls.tsx,frontend/src/components/geographic-visualization/WorldMapVisualization.tsx,frontend/src/components/guided-tours/OnboardingTourTrigger.tsx,frontend/src/components/input-dialog/InputDialog.tsx,frontend/src/components/intake-form/IntakeForm.tsx,frontend/src/components/intelligence/BilateralOpportunities.tsx,frontend/src/components/intelligence/EconomicDashboard.tsx" --json | node -e "let s=[];process.stdin.on(\"data\",d=>s.push(d)).on(\"end\",()=>{const j=JSON.parse(s.join(\"\"));if(j.twoArgTotal!==0){console.error(\"mask sites survive in this lane: \"+j.twoArgTotal);process.exit(1)}if(j.rawKeyTotal===0){console.error(\"POSITIVE CONTROL FAILED — the scope filter matched no t() sites at all, so this zero is about the empty set\");process.exit(1)}})" && ! (command grep -rnE "t\([^)]*defaultValue" "$R/frontend/src/components/dossier/wizard/review/CountryReviewStep.tsx" "$R/frontend/src/components/dossier/wizard/review/EngagementReviewStep.tsx" "$R/frontend/src/components/dossier/wizard/review/ForumReviewStep.tsx" "$R/frontend/src/components/dossier/wizard/review/OrganizationReviewStep.tsx" "$R/frontend/src/components/dossier/wizard/review/PersonReviewStep.tsx" "$R/frontend/src/components/dossier/wizard/review/TopicReviewStep.tsx" "$R/frontend/src/components/duplicate-comparison/DuplicateComparison.tsx" "$R/frontend/src/components/duplicate-detection/DuplicateCandidateCard.tsx" "$R/frontend/src/components/duplicate-detection/DuplicateCandidatesList.tsx" "$R/frontend/src/components/duplicate-detection/MergeDialog.tsx" "$R/frontend/src/components/elected-officials/ElectedOfficialListTable.tsx" "$R/frontend/src/components/empty-states/ListEmptyState.tsx" "$R/frontend/src/components/empty-states/TutorialEmptyState.tsx" "$R/frontend/src/components/empty-states/VideoTutorial.tsx" "$R/frontend/src/components/engagement-recommendations/RecommendationsPanel.tsx" "$R/frontend/src/components/entity-links/AISuggestionPanel.tsx" "$R/frontend/src/components/entity-links/EntitySearchDialog.tsx" "$R/frontend/src/components/export-import/ImportValidationResults.tsx" "$R/frontend/src/components/forms/ContextualHelp.tsx" "$R/frontend/src/components/forms/FormErrorDisplay.tsx" "$R/frontend/src/components/forms/FormSection.tsx" "$R/frontend/src/components/forms/UserPicker.tsx" "$R/frontend/src/components/forums/ForumDetailsDialog.tsx" "$R/frontend/src/components/geographic-visualization/MapFilterControls.tsx" "$R/frontend/src/components/geographic-visualization/WorldMapVisualization.tsx" "$R/frontend/src/components/guided-tours/OnboardingTourTrigger.tsx" "$R/frontend/src/components/input-dialog/InputDialog.tsx" "$R/frontend/src/components/intake-form/IntakeForm.tsx" "$R/frontend/src/components/intelligence/BilateralOpportunities.tsx" "$R/frontend/src/components/intelligence/EconomicDashboard.tsx" | command grep -v -E "\.test\.|__tests__")'
      text: "the strict instrument's twoArgTotal — EVERY mask site, resolved or not, seen by the cross-line matcher — reads ZERO across this lane's files, with a nonzero rawKeyTotal as the positive control that the scope matched real t() calls, AND no defaultValue option survives in them. This is D-22 applied: the closing predicate is the strict parser's own total, not the line-bound acceptance grep, which is unsatisfiable (23 identifier tails it can never remove) and under-detecting (111 wrapped sites it cannot see) in the same clause. RED at HEAD (256 mask sites in this lane)."
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; cd "$R" && test "$(git diff --name-only HEAD -- frontend/src/i18n | wc -l | tr -d " ")" -eq 0 && node "$R/scripts/i18n-audit-strict.mjs" "$R" --scope "frontend/src/components/dossier/wizard/review/CountryReviewStep.tsx,frontend/src/components/dossier/wizard/review/EngagementReviewStep.tsx,frontend/src/components/dossier/wizard/review/ForumReviewStep.tsx,frontend/src/components/dossier/wizard/review/OrganizationReviewStep.tsx,frontend/src/components/dossier/wizard/review/PersonReviewStep.tsx,frontend/src/components/dossier/wizard/review/TopicReviewStep.tsx,frontend/src/components/duplicate-comparison/DuplicateComparison.tsx,frontend/src/components/duplicate-detection/DuplicateCandidateCard.tsx,frontend/src/components/duplicate-detection/DuplicateCandidatesList.tsx,frontend/src/components/duplicate-detection/MergeDialog.tsx,frontend/src/components/elected-officials/ElectedOfficialListTable.tsx,frontend/src/components/empty-states/ListEmptyState.tsx,frontend/src/components/empty-states/TutorialEmptyState.tsx,frontend/src/components/empty-states/VideoTutorial.tsx,frontend/src/components/engagement-recommendations/RecommendationsPanel.tsx,frontend/src/components/entity-links/AISuggestionPanel.tsx,frontend/src/components/entity-links/EntitySearchDialog.tsx,frontend/src/components/export-import/ImportValidationResults.tsx,frontend/src/components/forms/ContextualHelp.tsx,frontend/src/components/forms/FormErrorDisplay.tsx,frontend/src/components/forms/FormSection.tsx,frontend/src/components/forms/UserPicker.tsx,frontend/src/components/forums/ForumDetailsDialog.tsx,frontend/src/components/geographic-visualization/MapFilterControls.tsx,frontend/src/components/geographic-visualization/WorldMapVisualization.tsx,frontend/src/components/guided-tours/OnboardingTourTrigger.tsx,frontend/src/components/input-dialog/InputDialog.tsx,frontend/src/components/intake-form/IntakeForm.tsx,frontend/src/components/intelligence/BilateralOpportunities.tsx,frontend/src/components/intelligence/EconomicDashboard.tsx" --json | node -e "let s=[];process.stdin.on(\"data\",d=>s.push(d)).on(\"end\",()=>{const j=JSON.parse(s.join(\"\"));if(j.twoArgUnresolved!==0||j.rawKeyUnresolved!==0||j.twoArgUnresolvedAr!==0||j.rawKeyUnresolvedAr!==0){console.error(\"the drop created unresolved sites — a key was rewritten, not just a default deleted\",JSON.stringify(j).slice(0,300));process.exit(1)}})" && test "$(node "$R/scripts/neg-taskcard.mjs" "$R" | command grep -c "MISS=true")" -eq 3'
      text: 'no i18n JSON changed in this lane, the scoped strict audit still reads zero UNRESOLVED after the drop (a nonzero here means a key moved, which is the one way a deletion-only diff can go wrong), and the phase negative control still prints 3x MISS=true — RED at HEAD'
---

<objective>
Land the deletion for mask deletion lane 2, proven by the acceptance items below in a single pass.

This task is part 1 of 1 of mask deletion lane 2; the parts are ordered by `depends_on` and share one
file surface, so each part reads the tree the part before it produced. The task-unit contract caps a
task at six acceptance items and twenty-four surface, which is why the lane is cut this way.
</objective>

<context>
@.planning/phases/99-arabic-coverage/99-CONTEXT.md
@.planning/phases/99-arabic-coverage/99-RESEARCH.md
@.planning/phases/99-arabic-coverage/rulings/RULING-P99-07-B05-DIFFCAP.md
@scripts/i18n-audit-strict.mjs
@scripts/neg-taskcard.mjs

Ref notes: 99-CONTEXT.md — D-20, D-23 as clarified, D-24, D-28; 99-RESEARCH.md — §1 the mask population and the order constraint; RULING-P99-07 — why this lane is a slice and what its byte budget is; i18n-audit-strict.mjs — from 99-02; its twoArgTotal is the closing predicate, the acceptance grep is a diagnostic

<interfaces>
Re-derived on disk 2026-08-18 at 9ffa2b80c; the gatekeeper's SUMMARY carries the authoritative
manifest and this lane reconciles against it rather than against these plan-time floors:

- This lane's slice: **30 source files**, **256 mask sites** (both classes), estimated
  **~40498 logic-diff bytes** at x2 per changed line — a modified line is a removed line plus an
  added line in a -U0 logic diff. Measured with the committed audit's cross-line matcher.
- The deletion shapes are in must_haves. Variable second arguments at the dynamic sites were
  already dropped by their owning authoring lanes as part of their atomic enum units; the
  maskfinder's zero at the gatekeeper confirms none survived.
- After the drop, working dot-form keys REMAIN dot-form. They resolve, and ROADMAP criterion 4 is
  satisfied because no English default exists anywhere; the wholesale dot->colon conversion is
  D-21's recorded NO-SHIP, queued as new scope for Phase 102.
- Sibling deletion lanes run concurrently with this one and are file-disjoint from it by
  construction (D-39). Nothing in this lane reads or writes a sibling's file.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>mask deletion lane 2 — the deletion</name>
  <files>the paths in files_modified</files>
  <read_first>the gatekeeper's SUMMARY (the authoritative two-class manifest); the deletion shapes in must_haves</read_first>
  <action>
    This is the first part of the lane, so the lane method below is yours to execute in full over the paths in files_modified. Re-derive every population before relying on it (D-04), run each oracle and record its verbatim output, and name in the SUMMARY anything you leave for a later part.

    --- THE LANE METHOD (carried verbatim from the pre-recut plan; the acceptance items above bound what THIS task owes) ---

    1. Reconcile this lane's file list against the gatekeeper's manifest; a site in a file outside
       the list STOPS the lane and is recorded.
    2. Remove the second-argument literal and the fallback-text option at every mask site per the
       deletion shapes. Keys, namespaces, hooks, imports: byte-untouched.
    3. Re-run the scoped strict audit (twoArgTotal zero, unresolved still zero, rawKeyTotal nonzero
       as the control) and the defaultValue grep; confirm `git diff` touches no i18n JSON;
       neg-taskcard still 3x MISS=true; frontend type-check green.

  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes. The lane's own criteria, carried: The diff is deletions of default arguments only — spot-diff a sample and state it; scoped twoArgTotal zero with the rawKeyTotal control nonzero; scoped unresolved still zero (proving no key moved); no JSON in the diff; neg-taskcard undefused; type-check green.</acceptance_criteria>
  <done>No t() call in this slice passes an English default; a missing key now shows as missing in both locales — which the gatekeeper's proven zero makes an empty set today.</done>
</task>

</tasks>

<verification>
POPULATION: the paths in files_modified. OUTSIDE IT: every other task's files, the exogenous paths
(D-03), and every obligation this task's acceptance items do not name — those belong to the lane
parts before and after this one, named in `depends_on` and in the SUMMARY.

The lane's own population statement, carried verbatim from the pre-recut plan:
POPULATION: this lane's 30 source files, deletion-only. OUTSIDE IT: every i18n JSON (a JSON edit
here is a scope defect), the sibling deletion lanes' files, test files (the strict instrument
excludes `*.test.*` and `__tests__` from both classes, one stated policy), the wholesale dot->colon
conversion (D-21 NO-SHIP), and the rendered re-proofs (the closing plan).
</verification>

<success_criteria>
Land the deletion for mask deletion lane 2, proven by the acceptance items below in a single pass.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-32-SUMMARY.md` when done. It MUST record every command this task ran with its verbatim output, the
populations it re-derived with the commands that produced them, and anything left for a named later
task. The lane's own SUMMARY contract, carried verbatim from the pre-recut plan (satisfy the parts
this task owns): record in the file named under <output> (99-18-SUMMARY.md in the pre-recut plan) when done. It MUST record: the drop
site/file counts reconciled against the gatekeeper's manifest, by class; the scoped zero register
with its controls; the sampled spot-diff showing deletions only; and the explicit statement that no
key was rewritten and no JSON changed.
</output>
