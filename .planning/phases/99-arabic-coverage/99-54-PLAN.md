---
phase: 99-arabic-coverage
plan: 54
type: execute
wave: 11
depends_on: [99-53, 99-51]
files_modified:
  - 'scripts/i18n-dynamic-key-audit{,.test}.mjs'
  - .planning/phases/99-arabic-coverage/99-54-SUMMARY.md
autonomous: true
requirements: [AR-04a]
success_criterion: 'Repair the two measured translator-binding blind spots so the instrument classifies the production shapes it currently drops or refuses.'
routing:
  floor: frontier
truths:
  - 'RULING-P99-282 and RULING-P99-251: this task owns the INSTRUMENT and its tests only. The fixture corpus, its generator and its matrix belong to 99-53, which this task DEPENDS ON and MAY NOT TOUCH; every production caller and locale bundle belongs to 99-49 and its successor. Widening this task to adjust the corpus that grades it is the graded editing the grader.'
must_haves:
  truths:
    - 'THE PARAMETER-BOUND TRANSLATOR IS CLASSIFIED INSTEAD OF DROPPED, AND THIS BLIND SPOT FAILS OPEN. Today the instrument follows only translators bound by a useTranslation call, so a t arriving through a function parameter appears in NEITHER the classified nor the unclassified counter - the site is invisible, not reported. Measured at 2e3f26bad with no production change: lane3Sites reads 11 against a ruled 13, and the two absent sites are the the analytic-result-view component type labels. A fail-open blind spot is worse than a fail-closed one because its output looks complete.'
    - 'THE RENAMED DESTRUCTURED TRANSLATOR IS CLASSIFIED INSTEAD OF REFUSED, AND THIS BLIND SPOT FAILS CLOSED. Today const { t: <alias> } = useTranslation(<ns>) is reported unclassified with reason "unsupported useTranslation translator binding", at the command-palette file named in <context>, lines 431-432 at 2e3f26bad for either aliased translator. Measured at 2e3f26bad, 3/8 unclassified rows carry family unclassified.translator-binding and all three are that shape. The underlying code is CORRECT and COMPLETE - its key domains resolve 8/8 in en and 8/8 in ar for the group keys and 27/27 in en and 27/27 in ar for the navigation labels - so the instrument is declining to classify working code.'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node "$R/scripts/i18n-dynamic-key-audit.mjs" "$R" --self-check || { echo "FAIL: self-check"; exit 1; }; node --test "$R/scripts/i18n-dynamic-key-audit.test.mjs" || { echo "FAIL: instrument test suite"; exit 1; }; AUDITJSON="$(mktemp -t p99audit).json" || { echo "INSTRUMENT-CANNOT-RUN: mktemp failed"; exit 3; }; AUDITERR="$AUDITJSON.err"; node "$R/scripts/i18n-dynamic-key-audit.mjs" "$R" --profile ar04-live --json > "$AUDITJSON" 2>"$AUDITERR"; AUDITST=$?; node -e "JSON.parse(require(\"node:fs\").readFileSync(process.argv[1],\"utf8\"))" "$AUDITJSON" 2>/dev/null || { echo "INSTRUMENT-CANNOT-RUN: the dynamic-key audit produced no parseable JSON (exit $AUDITST), so it did not run to a verdict"; head -3 "$AUDITERR"; rm -f "$AUDITJSON" "$AUDITERR"; exit 3; }; echo "audit-exit=$AUDITST (nonzero here is a CONTENT verdict, not an execution failure; JSON parsed)"; AUDITJSON="$AUDITJSON" node -e "const d=JSON.parse(require(\"node:fs\").readFileSync(process.env.AUDITJSON,\"utf8\"));const cp=d.callerPopulations;const tb=(d.unclassified||[]).filter(u=>u.family===\"unclassified.translator-binding\");console.log(\"lane3Sites=\"+cp.lane3Sites+\" translator-binding-unclassified=\"+tb.length+\"/\"+(d.unclassified||[]).length);const L=s=>String(s||\"\").startsWith(\"lane3.\");const observed=new Set();for(const r of (d.rows||[]))if(L(r.family))observed.add(r.family);for(const u of (d.unclassified||[]))if(L(u.family))observed.add(u.family);const rules=new Set();for(const pr of (d.proofRows||[]))if(L(pr.family))rules.add(pr.family);const uncovered=[...observed].filter(f=>!rules.has(f));const dead=[...rules].filter(f=>!observed.has(f));console.log(\"lane3 observed-families=\"+observed.size+\" handling-rules=\"+rules.size+\" | DIAGNOSTICS ONLY lane3Sites=\"+cp.lane3Sites+\" lane3Families=\"+cp.lane3Families+\" listLeaves=\"+cp.listLeaves);if(observed.size===0||rules.size===0){console.error(\"INSTRUMENT-CANNOT-RUN: one side of the coverage relation is EMPTY (observed=\"+observed.size+\" rules=\"+rules.size+\"), so neither direction could have failed; this leg verified nothing\");process.exit(3)}if(uncovered.length){console.error(\"FAIL: lane3 family observed with no handling rule (a new unhandled kind): \"+JSON.stringify(uncovered));process.exit(1)}if(dead.length){console.error(\"FAIL: lane3 handling rule matches no observed family (a dead rule): \"+JSON.stringify(dead));process.exit(1)}if(tb.length!==0){console.error(\"FAIL: \"+tb.length+\" renamed-destructure rows still unclassified\");process.exit(1)}"'
      text: 'the instrument self-check and its own suite pass, and against the UNCHANGED production tree the live profile makes the parameter-bound sites VISIBLE without a production edit, asserted as ZERO rows remaining in family unclassified.translator-binding - that zero IS the decisive quantity and the oracle compares it. lane3Sites is expected to rise from the measured 11 toward 13 as a CONSEQUENCE of that, and is reported as a DIAGNOSTIC ONLY, never compared: per RULING-P99-326 no ruled TYPE set exists for lane3, so a frozen site count is an F1 threshold on a moving population. Text and oracle are deliberately aligned here after RULING-P99-335: a criterion must not name a number it does not compare.translator-binding, so the renamed destructure is classified rather than refused; both assertions are unreachable before this repair and neither can be satisfied by editing production, which this task may not touch'
    - 'THE REPAIR IS GENERAL, NOT A PAIR OF SPECIAL CASES. The recogniser follows the binding mechanisms the 99-53 matrix now enumerates, and every cell of that matrix passes in both polarities. A branch keyed on an aliased-translator identifier name, a context-parameter name, or either of the two files named in <context>, fails this task: it would close the two measured reports while leaving every unmeasured instance of the same mechanism blind, which is the defect this repair exists to remove.'
    - 'THE CONSUMER SUITE IS RE-SCOPED TO ITS GRADING MOMENT (RULING-P99-345). scripts/i18n-dynamic-key-audit.test.mjs is in this task''s files[] and currently constrains all future work in three ways that P99-53''s re-derived corpus necessarily violates: line 699 pins exactly 28 rows over the seven forms P99-53 replaces; lines 656 and 659 call assertFixturePair on fixture roots P99-53 renames; and line 738 requires the instrument''s CURRENT --self-check and lane3 --rows output to appear verbatim in 99-51-SUMMARY.md. THE THIRD IS THE STRUCTURAL ONE: P99-51''s real claim was that ITS summary faithfully recorded ITS OWN output AT ITS GRADING MOMENT, which is not an invariant over all future trees - as written, ANY change to the instrument''s output, by ANY task, forever, breaks a COMPLETED task''s test. It has already broken twice by two independent mechanisms, a whitespace reformat and a removed audit-line marker. This task re-scopes it to that moment - pinning the recorded snapshot rather than re-deriving live output - or retires it with its verdict recorded, and updates the row-count and fixture-root pins to the corpus P99-53 delivers. It may NOT satisfy them by reverting P99-53''s work. P99-51''s own acceptance is NOT re-opened: it passed at its grading moment and that verdict stands. THE INSTRUMENT MAY NOT ACQUIRE A VACUOUS ASSERTION, AND IT ALREADY CARRIES ONE. acceptanceCriteria[5] enforces diff scope with git status --short, whose changedPaths list is EMPTY on the clean committed tree the gates run in, so .every() passes vacuously; the reviewer-proposed changedPaths.length > 0 would invert it into a guaranteed FAILURE in that same environment. Any diff-scope assertion this task touches inspects the COMMITTED DIFF against the task base. Every count this instrument reports is reported as n over total, never as a bare boolean and never as a bare numerator, because a ratio cannot hide an empty population and a boolean can.'
---

<objective>
Repair the two measured translator-binding blind spots so the instrument classifies the production shapes it
currently drops or refuses.
</objective>

<context>
@.planning/phases/99-arabic-coverage/99-53-PLAN.md
@.planning/phases/99-arabic-coverage/99-51-SUMMARY.md
@scripts/i18n-dynamic-key-audit.mjs
@frontend/src/components/keyboard-shortcuts/CommandPalette.tsx
@frontend/src/components/relationships/AnalyticResultView.tsx
</context>

<tasks>
<task type="auto">
  <name>classify the parameter-bound and renamed-destructured translator bindings</name>
  <files>the paths in files_modified</files>
  <read_first>the 99-53 matrix and the fixture cells drilling the two shapes</read_first>
  <action>
    Follow translators through the binding mechanisms the 99-53 matrix enumerates rather than through a
    list of recognised syntax. A translator reaching a call site through a function parameter must be
    classified, not dropped; a translator bound by a renamed destructure must be classified, not refused.
    Drive the change from the fixture cells in both polarities. Do NOT special-case an identifier name or a
    file path. Do NOT touch the corpus, the generator, the matrix, any production caller, or any locale
    bundle. Record the live profile output before and after, verbatim, against the unchanged production tree.
  </action>
  <acceptance_criteria>Every acceptance item above passes, with no corpus edit and no production edit.</acceptance_criteria>
  <done>lane3Sites reads 13 and no row remains in family unclassified.translator-binding, with production untouched.</done>
</task>
</tasks>

<verification>
POPULATION: the instrument, its test suite, and this SUMMARY. OUTSIDE IT: the fixture corpus and its matrix
(99-53), every production caller and locale bundle (99-49 and its successor), the three statically
unprovable AdvancedGraphVisualization domains at :1546, :1564 and :1649 whose domains are built from runtime
rows and which RULING-P99-241 owns at the 99-50 boundary, every fallback deletion (99-31 through 99-38), and
the literal gatekeeper battery (99-50).
</verification>

<success_criteria>
Repair the two measured translator-binding blind spots so the instrument classifies the production shapes it
currently drops or refuses.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-54-SUMMARY.md` recording the VERBATIM stdout and stderr of
every oracle command, the before and after live-profile figures against the unchanged production tree, the
binding mechanism each repair follows, and an explicit statement that no identifier name or file path was
special-cased.
</output>
