---
phase: 99-arabic-coverage
plan: 13
type: execute
wave: 3
depends_on: [99-06, 99-07, 99-12]
files_modified:
  - 'frontend/src/{i18n/en/assignments.json,i18n/ar/assignments.json,i18n/en/positions.json,i18n/ar/positions.json,i18n/en/tasks-page.json,i18n/ar/tasks-page.json,i18n/en/commitments.json,i18n/ar/commitments.json,components/tasks/TaskDetail.tsx,components/tasks/TaskCard.tsx,components/tasks/AddContributorDialog.tsx,components/tasks/ContributorsList.tsx,components/waiting-queue/AssignmentDetailsModal.tsx,components/waiting-queue/ReminderButton.tsx,components/waiting-queue/AgingIndicator.tsx,components/waiting-queue/EscalationDialog.tsx,pages/WaitingQueue.tsx,pages/TaskDetailPage.tsx,components/commitment-editor/CommitmentEditor.tsx,routes/_protected/positions/$id/index.tsx}'
  - .planning/phases/99-arabic-coverage/99-13-SUMMARY.md
autonomous: true
requirements: [AR-03, AR-04a, AR-04b]
success_criterion: 'Land the banner extraction and the three rendered banner states for the tasks/queues/positions lane, proven by the acceptance items below in a single pass.'
routing:
  floor: frontier
truths:
  - 'Decisions covered — D-11: TaskCard is the recorded broken exemplar — its three prefixes MISS through ns translation at HEAD; the repair colon-prefixes them to assignments:, where priority.low already resolves ("Low"), the correct near-neighbour by ruling; D-24 steps 1-2: authoring + verification only, second arguments kept for the deletion lanes; D-25: every key authored twice, en byte-identical to the mask literal; D-26: placement via colon-prefix to assignments:/positions:/tasks-page:/commitments:, hooks untouched; D-27: the six dynamic carriers repair as atomic enum units verified through partA_maskfinder; D-29: UI99-C7 is the binding UI-contract row this lane closes and it closes on the THREE RENDERED banner tests — one per ruled status, driven by 99-01''s committed fixture — invoked by this plan''s own command oracle rather than on the source absence-grep beside it; D-07: a criterion member closes on a rendered surface or a drilled instrument, never on source text alone; D-35: the banner''s data precondition is retired by that fixture, which A-05 as amended makes the PRIMARY remedy — this lane may not record it UNDRIVEN-BY-DATA; D-32: the position read-only banner is one of the two HARDCODED members of the four named criterion-3 surfaces — extraction + authoring; D-18: banner ar copy uses the موقف family (منصب is reserved for office/post and never appears in stance-sense copy).'
must_haves:
  truths:
    - 'TaskCard/TaskDetail work_item, priority and status prefixes colon-prefix to assignments: and resolve in BOTH locales — where assignments.json already carries the enum (priority.low family) the repair authors only the ar/en gaps the resolution probe finds, never duplicate subtrees'
    - 'scripts/neg-taskcard.mjs is NOT edited and its bundle probes stay valid: no priority.*/status.*/work_item.* key is authored into common.json or the translation alias by this lane (that would silently defuse the phase negative control)'
    - 'Dot-form common.X sites in these files rewrite to common:X only where the key resolves at the flattened root (AgingIndicator days does); still-missing common keys are left and NAMED for 99-06'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; python3 "$R/scripts/partA_maskfinder.py" "$R" --control >/dev/null || exit 1; OUT="$(python3 "$R/scripts/partA_maskfinder.py" "$R")" || exit 1; printf "%s\n" "$OUT" | command grep -q "UNRESOLVED dynamic t() key prefixes:" || exit 1; printf "%s\n" "$OUT" | command grep -qE "TaskCard\.tsx|TaskDetail\.tsx|AddContributorDialog\.tsx|ContributorsList\.tsx|AssignmentDetailsModal\.tsx|CommitmentEditor\.tsx" && exit 1; true && ! command grep -n "Read Only" "$R/frontend/src/routes/_protected/positions/\$id/index.tsx" && command grep -q "positions:" "$R/frontend/src/routes/_protected/positions/\$id/index.tsx" && test "$(node "$R/scripts/neg-taskcard.mjs" "$R" | command grep -c "MISS=true")" -eq 3'
      text: 'the six dynamic carriers are gone from the instrument of record (control first), the banner literal is replaced by positions: keys (absence+presence pair), and the phase negative control still prints 3× MISS=true — RED at HEAD on the first two clauses'
---

<objective>
Land the banner extraction and the three rendered banner states for the tasks/queues/positions lane, proven by the acceptance items below in a single pass.

This task is part 2 of 2 of the tasks/queues/positions lane; the parts are ordered by `depends_on` and share one
file surface, so each part reads the tree the part before it produced. The task-unit contract caps a
task at six acceptance items and twenty-four surface, which is why the lane is cut this way.
</objective>

<context>
@.planning/phases/99-arabic-coverage/99-CONTEXT.md
@.planning/phases/99-arabic-coverage/99-RESEARCH.md
@scripts/partA_maskfinder.py
@scripts/neg-taskcard.mjs
@frontend/src/components/tasks/TaskCard.tsx
@tests/e2e/99-ar03-leak.spec.ts

Ref notes: 99-CONTEXT.md — D-11, D-18, D-23..D-27, D-32; 99-RESEARCH.md — §3 the dynamic member list; §6.1 row 3 the banner; 99-ar03-leak.spec.ts — authored in 99-01; its three UI99-C7 tests are this lane's rendered oracle. The banner route file is `frontend/src/routes/_protected/positions/$id/index.tsx` — named here in prose and listed in files_modified, because an `@` ref containing `$` is silently DROPPED at compile (contract §7) and the first draft lost exactly this ref; neg-taskcard.mjs — NOT edited — its premise is a lane constraint

<interfaces>
Verified 2026-08-18 at 9ffa2b80c:

- TaskCard prefixes `priority.`/`status.`/`work_item.` through a bare hook (alias → common →
  MISS); `t('priority.low')` bound to assignments resolves to "Low" — colon-prefix to
  `assignments:priority.` etc. Probe every enum member in BOTH locales before relying on the
  existing subtree; author only the gaps.
- AssignmentDetailsModal prefixes `waitingQueue.status/priority/entityType` (bare hook) —
  author the waitingQueue enum subtree in assignments.json, both locales.
- CommitmentEditor's prefix `afterActions.commitments.tracking` — author the tracking enums in
  commitments.json; note the aa_commitments status lifecycle is FIVE values
  (pending/in_progress/completed/cancelled/overdue — no review), per the project's verified
  carve-out; enumerate from the type the component consumes.
- The banner: three status-ternary sentences beginning "Position Under Review - Read Only"
  (search the STRING) in the positions $id route — no t() at all today. UI99-C7's rendered
  assertion carries a DATA PRECONDITION (a position in one of the three states) owned by
  99-01/99-10's spec, not by this lane; this lane's oracle is source-shaped (absence+presence),
  deliberately.
- StepUpMFA (24 raw-key sites) and the positions route (4) already bind positions — authoring
  alone repairs them; re-derive the slice with the strict audit and do not edit those files.
- Re-derive lane population at start: plan-time two-arg unresolved — TaskDetail 23, WaitingQueue
  19, ReminderButton 20, AddContributorDialog 8, AssignmentDetailsModal 7, EscalationDialog 5,
  TaskDetailPage 5, ContributorsList 2, TaskCard 2; raw-key — CommitmentEditor 7, StepUpMFA 24,
  positions route 4, WorkItemLinker 8 (WorkItemLinker's keys target common → the common-owner lane authors, no
  source edit here).
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>the tasks/queues/positions lane — the banner extraction and the three rendered banner states</name>
  <files>the paths in files_modified</files>
  <read_first>the twelve source files in full; en/ar assignments.json (the existing priority/status enums — extend, never duplicate); 99-RESEARCH.md §3</read_first>
  <action>
    Parts before this one landed in P99-12. Do only what THIS task's acceptance items still require, using the lane method below as the standing method; re-run this task's oracles against the tree the earlier part produced and repair any residue INSIDE files_modified. Author nothing outside them.

    --- THE LANE METHOD (carried verbatim from the pre-recut plan; the acceptance items above bound what THIS task owes) ---

    1. Strict audit --scope over the eleven t()-bearing lane files plus a second scoped run over
       StepUpMFA and the positions route; record starting populations.
    2. Author every missing key in en AND ar across assignments/positions/tasks-page/commitments
       (en byte-identical to mask literals; ar per glossary: مهمة for task, تكليف for
       assignment, موقف for stance). Rewrite repaired sites to explicit colon form; keep second
       arguments; no hook changes.
    3. Dynamic units (six files): enumerate enum values from the consuming types, author full
       sets both locales, colon-prefix the expressions, drop the variable second argument at
       those sites only, one commit per atomic unit.
    4. Banner: extract the three sentences to positions: keys (both locales, موقف register,
       sentence case, no exclamation); replace the ternary literals with t() calls.
    5. Rewrite this lane's resolvable dot-common sites to common:X (probe first); leave and
       name the rest for 99-06.
    6. Verify: scoped audits zero; maskfinder clean of the six carriers (control first);
       neg-taskcard still prints 3× MISS=true; en/ar parity on the four pairs; type-check green.

  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes. The lane's own criteria, carried: Scoped strict-audit zero on the full eleven-file scope (the verify block spot-checks three; the oracle runs the full list) with starting numbers recorded; six carriers gone with the control run first; banner extracted with the absence+presence pair; neg-taskcard undefused (3× MISS=true asserted by text); en/ar parity; no second-argument change outside dynamic units; type-check green.</acceptance_criteria>
  <done>the banner extraction and the three rendered banner states is landed for the tasks/queues/positions lane, proven by this task's own oracles rather than by the lane's later tasks.</done>
</task>

</tasks>

<verification>
POPULATION: the paths in files_modified. OUTSIDE IT: every other task's files, the exogenous paths
(D-03), and every obligation this task's acceptance items do not name — those belong to the lane
parts before and after this one, named in `depends_on` and in the SUMMARY.

The lane's own population statement, carried verbatim from the pre-recut plan:
POPULATION: the 21 listed files. OUTSIDE IT: common.json authoring (the common-owner lane — WorkItemLinker and
the leftover common-targeted keys are named handoffs), second-argument drops (the deletion lanes), the
rendered banner assertion with its data precondition (99-01's spec, re-run in the closing battery), and
scripts/neg-taskcard.mjs (constraint: never edited, never defused).
</verification>

<success_criteria>
Land the banner extraction and the three rendered banner states for the tasks/queues/positions lane, proven by the acceptance items below in a single pass.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-13-SUMMARY.md` when done. It MUST record every command this task ran with its verbatim output, the
populations it re-derived with the commands that produced them, and anything left for a named later
task. The lane's own SUMMARY contract, carried verbatim from the pre-recut plan (satisfy the parts
this task owns): record in the file named under <output> (99-06-SUMMARY.md in the pre-recut plan) when done. It MUST record: starting
vs ending scoped audit numbers; the enum sets per dynamic carrier; the banner key names and both
locale values; every common-targeted key left for the common-owner lane by name; and the neg-taskcard 3× MISS=true
post-change output verbatim.
</output>
