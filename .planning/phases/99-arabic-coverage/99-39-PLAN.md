---
phase: 99-arabic-coverage
plan: 39
type: execute
wave: 11
depends_on: [99-31, 99-32, 99-33, 99-34, 99-35, 99-36, 99-37, 99-38, 99-57, 99-58, 99-59, 99-60, 99-61, 99-62]
files_modified:
  - .planning/phases/99-arabic-coverage/99-VERIFICATION.md
  - .planning/phases/99-arabic-coverage/99-39-SUMMARY.md
autonomous: true
requirements: [AR-01, AR-02, AR-03, AR-04a, AR-04b]
success_criterion: 'Land the static battery for the consolidated re-proof, proven by the acceptance items below in a single pass.'
routing:
  floor: frontier
truths:
  - 'Decisions covered — D-38: this plan is the phase''s human gate — the rendered sign-off is a HUMAN judgment, never auto-answered by a worker, the orchestrator, or the engine; D-19: the glossary reversal window closes AT this gate — the operator sees the tie-break list and the swept terms while a term swap is still a mechanical leaf-rename; D-02: every criterion is named by at least one oracle in the evidence register, and a criterion no oracle names fails grading; D-07: every criterion closes on a rendered surface or a drilled instrument, never on a source grep alone — which is why both Playwright specs are `command:` truths in this plan''s own acceptance and not <verify> prose; D-09: one spec path per invocation, file existence asserted, the expected count HARDCODED at 8 and 10 rather than derived from the list just passed; D-05: the register states what falls OUTSIDE each population; D-35: the legs D-35 NAMED as undriven at HEAD were DRIVEN by this phase — the 404, the ar leg of the intake queue, the /search chips and the position banner all have specs, and the banner''s data precondition was retired by a committed fixture, so no leg closes on "NOT CONSTRUCTED".'
must_haves:
  truths:
    - "Decisions covered — D-02, D-05, D-07, D-09, D-19, D-35, D-38: consolidated re-proof, both rendered specs executed by this plan's own gate, populations bounded in writing, human-gated."
    - '99-VERIFICATION.md maps criterion->plan->oracle->observed result for all four criteria: (1) the 28-row nav-title checker green with its control + the drilled glossary census at zero unclassified across EVERY ruled row repo-wide; (2) 99-ar02 all 8 tests green; (3) 99-ar03 all 10 tests green including the three fixture-driven banner tests; (4) the strict audit at twoArgTotal zero and unresolved zero in both locales + maskfinder zero + resolve-check green + neg-taskcard 3x MISS=true — each with its control result beside it, each run fresh in THIS task, none quoted from earlier summaries'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; test -f "$R/.planning/phases/99-arabic-coverage/99-VERIFICATION.md" && command grep -q "criterion" "$R/.planning/phases/99-arabic-coverage/99-VERIFICATION.md" && node "$R/scripts/nav-title-agreement.mjs" "$R" --control && node "$R/scripts/nav-title-agreement.mjs" "$R" && node "$R/scripts/glossary-census.mjs" "$R" --control && node "$R/scripts/glossary-census.mjs" "$R" && node "$R/scripts/i18n-audit-strict.mjs" "$R" --self-check && node "$R/scripts/i18n-audit-strict.mjs" "$R" --json | node -e "let s=[];process.stdin.on(\"data\",d=>s.push(d)).on(\"end\",()=>{const j=JSON.parse(s.join(\"\"));if(j.rawKeyTotal===0){console.error(\"POSITIVE CONTROL FAILED — the instrument walked no t() sites\");process.exit(1)}if(j.twoArgTotal!==0||j.twoArgUnresolved!==0||j.rawKeyUnresolved!==0||j.twoArgUnresolvedAr!==0||j.rawKeyUnresolvedAr!==0){console.error(\"AR-04 not closed\",JSON.stringify(j).slice(0,300));process.exit(1)}})" && { MC=$(python3 "$R/scripts/partA_maskfinder.py" "$R" --control 2>&1); ST=$?; test "$ST" -eq 0 || { echo "INSTRUMENT-CANNOT-RUN: maskfinder --control exited $ST"; exit 3; }; NC=$(printf "%s\n" "$MC" | command grep -c "^CONTROL " || true); test "$NC" -ge 4 || { echo "INSTRUMENT-CANNOT-RUN: control printed $NC CONTROL lines, fewer than the four documented polarities, so they cannot be asserted"; exit 3; }; OKC=$(printf "%s\n" "$MC" | command grep -cE "= True \(expect True\)|= False \(expect False\)" || true); test "$OKC" -eq "$NC" || { echo "FAIL: the mask-finder control is NOT discriminating - $((NC-OKC)) of $NC polarities disagree with their stated expectation:"; printf "%s\n" "$MC"; exit 1; }; echo "mask-finder control polarities asserted: $OKC/$NC"; } && { MF=$(python3 "$R/scripts/partA_maskfinder.py" "$R" 2>&1); ST=$?; test "$ST" -eq 0 || { echo "INSTRUMENT-CANNOT-RUN: mask finder did not report the expected summary exited $ST; its printed summary is not a verdict"; printf "%s\n" "$MF" | tail -3; exit 3; }; printf "%s\n" "$MF" | command grep -q "prefixes: 0" || { echo "FAIL: mask finder did not report the expected summary"; exit 1; }; } && { NT=$(node "$R/scripts/neg-taskcard.mjs" "$R" 2>&1); ST=$?; test "$ST" -eq 0 || { echo "INSTRUMENT-CANNOT-RUN: neg-taskcard.mjs exited $ST; its MISS rows are not a content verdict"; exit 3; }; NM=$(printf "%s\n" "$NT" | command grep -c "MISS=true" || true); test "$NM" -eq 3 || { echo "FAIL: negative control printed $NM MISS=true rows, expected 3"; exit 1; }; } && { RC=$(node "$R/scripts/resolve-check.mjs" "$R" 2>&1); ST=$?; test "$ST" -eq 0 || { echo "INSTRUMENT-CANNOT-RUN: resolve-check did not report the expected summary exited $ST; its printed summary is not a verdict"; printf "%s\n" "$RC" | tail -3; exit 3; }; printf "%s\n" "$RC" | command grep -q "routings with a miss: 0" || { echo "FAIL: resolve-check did not report the expected summary"; exit 1; }; } && { DEADIMP=$(command grep -rn "EnhancedActivityFeed" "$R/frontend/src" --include="*.ts" --include="*.tsx" | command grep -v "activity-feed/EnhancedActivityFeed.tsx:" | command grep -c . || true); LIVEIMP=$(command grep -rn "SharedRecentActivityCard" "$R/frontend/src" --include="*.ts" --include="*.tsx" | command grep -v "SharedRecentActivityCard.tsx:" | command grep -c . || true); echo "date-format controls: exempt-file importers=$DEADIMP (expect 0) live-control importers=$LIVEIMP (expect >0)"; test "$LIVEIMP" -gt 0 || { echo "INSTRUMENT-CANNOT-RUN: the LIVE control found zero importers for a component known to have them, so the importer census cannot discriminate and a zero for the exempt file would prove nothing"; exit 3; }; test "$DEADIMP" -eq 0 || { echo "FAIL: the dead-code exemption is VOID - EnhancedActivityFeed gained $DEADIMP importer(s), so its two offending rows return to the burn-down"; exit 1; }; } && node "$R/scripts/check-date-formatting.mjs"'
      text: "the full STATIC battery re-runs green in this task's own gate, every instrument preceded by its control and the strict audit's rawKeyTotal standing as the positive control against a walked-nothing zero — RED at HEAD on every clause"
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; cd "$R" || exit 3; for spec in tests/e2e/99-ar02-dates.spec.ts tests/e2e/99-ar03-leak.spec.ts; do test -f "$spec" || { echo "INSTRUMENT-CANNOT-RUN: missing $spec"; exit 3; }; done; C2=$(pnpm exec playwright test tests/e2e/99-ar02-dates.spec.ts --project=chromium-en --no-deps --list 2>/dev/null | command grep -c "99-ar02-dates.spec.ts:"); C3=$(pnpm exec playwright test tests/e2e/99-ar03-leak.spec.ts --project=chromium-en --no-deps --list 2>/dev/null | command grep -c "99-ar03-leak.spec.ts:"); test "$C2" -eq 8 || { echo "INSTRUMENT-CANNOT-RUN: ar02 collected $C2, expected 8 — a playwright path is a FILTER and a missing spec collects silently"; exit 3; }; test "$C3" -eq 10 || { echo "INSTRUMENT-CANNOT-RUN: ar03 collected $C3, expected 10"; exit 3; }; command -v lsof >/dev/null 2>&1 || { echo "INSTRUMENT-CANNOT-RUN: lsof absent, cannot establish who holds the dev-server port"; exit 3; }; HOLDER=$(lsof -tnP -iTCP:5173 -sTCP:LISTEN 2>/dev/null | head -1); if test -n "$HOLDER"; then HCWD=$(lsof -a -p "$HOLDER" -d cwd -Fn 2>/dev/null | command grep "^n" | head -1 | cut -c2-); case "$HCWD" in "$R"|"$R"/*) PW_REUSE=1; export PW_REUSE; echo "reusing dev server pid $HOLDER rooted in THIS worktree";; *) echo "INSTRUMENT-CANNOT-RUN: port 5173 held by pid $HOLDER rooted at ${HCWD:-unknown}, which is NOT this worktree; refusing to measure a foreign tree"; exit 3;; esac; fi; OUT=$(pnpm exec playwright test tests/e2e/99-ar02-dates.spec.ts tests/e2e/99-ar03-leak.spec.ts --project=chromium-en --no-deps --reporter=line 2>&1); ST=$?; printf "%s\n" "$OUT" | command grep -qE "(^|[^0-9])18 passed" || { echo "FAIL: rendered battery is not 18/18"; printf "%s\n" "$OUT" | tail -25; exit 1; }; test "$ST" -eq 0 || { echo "FAIL: playwright exited $ST despite an 18-passed line"; exit 1; }; echo "rendered battery executed by THIS gate: 18/18 (ar02 8 + ar03 10)"'
      text: 'Both rendered specs EXECUTE through this plan own typed gate, not through a scratchpad command recorded in prose. The collection control runs first and hardcodes 8 and 10 because a playwright spec path is a FILTER: a renamed or missing spec collects nothing and would otherwise report success. Execution then asserts the 18-passed line AND the exit status. The line pattern is deliberately NOT anchored at line start: the reporter emits ANSI cursor codes before the count, so an anchored pattern never matches and the oracle reds a green run. It still refuses to match 118 passed. because a pipeline status can report the last command rather than playwright. Added by RULING-P99-535 after review found the rendered proof was ad hoc. RULING-P99-537 adds the port guard: the worker own test run leaves a dev server bound, so the oracle collided with it. Reuse is allowed ONLY after proving the holder is rooted in THIS worktree; a foreign holder is INSTRUMENT-CANNOT-RUN, never a silent reuse, because this repo measured a 42-hour-old server from another tree answering this exact URL and reporting a pass.'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node "$R/scripts/completion-contract-check.mjs" --summaries "$R/.planning/phases/99-arabic-coverage"'
      text: 'every 99-NN-SUMMARY.md that EXISTS in the phase directory opens with front-matter carrying `status: complete`. Completion is derived from that marker, and nothing in the engine ensures a worker writes it — P99-32 (R428) and P99-61 (R478) each passed all seven gates and still compiled as `pending`, the second time changing the value of a landing decision. This is the ENGINE-RUN form of that guard: the journal form cannot be an oracle because a task worktree has no `.tickmarkr/runs/`, so this reads the tracked phase directory instead. It asserts presence-implies-marker, never existence, so a phase mid-flight is not failed for summaries not yet written; and it exits 3 rather than 0 on an empty directory, because an empty population proves nothing. RED against a directory containing a markerless SUMMARY (drilled 1/2, exit 1); GREEN at HEAD (51/51).'
---

<objective>
Land the static battery for the consolidated re-proof, proven by the acceptance items below in a single pass.

This task is part 1 of 3 of the consolidated re-proof; the parts are ordered by `depends_on` and share one
file surface, so each part reads the tree the part before it produced. The task-unit contract caps a
task at six acceptance items and twenty-four surface, which is why the lane is cut this way.
</objective>

<context>
@.planning/phases/99-arabic-coverage/99-CONTEXT.md
@.planning/phases/99-arabic-coverage/99-RESEARCH.md
@tests/e2e/99-ar02-dates.spec.ts
@tests/e2e/99-ar03-leak.spec.ts
@scripts/nav-title-agreement.mjs
@scripts/glossary-census.mjs

Ref notes: 99-CONTEXT.md — D-02, D-05, D-07, D-09, D-19, D-35, D-38 + the deferred list; 99-RESEARCH.md — §0 the criterion map; §8 the not-knowable-from-here list

<interfaces>

  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>the consolidated re-proof — the static battery</name>
  <files>the paths in files_modified</files>
  <read_first>every 99-NN-SUMMARY present in the lineage (for bounds to carry forward, never for greens to quote); the two spec files</read_first>
  <action>
    This is the first part of the lane, so the lane method below is yours to execute in full over the paths in files_modified. Re-derive every population before relying on it (D-04), run each oracle and record its verbatim output, and name in the SUMMARY anything you leave for a later part.

    --- THE LANE METHOD (carried verbatim from the pre-recut plan; the acceptance items above bound what THIS task owes) ---

    Run fresh, in one session, recording each output: the static battery (strict audit --self-check
    then --json, maskfinder --control + live, resolve-check, neg-taskcard text-assert,
    nav-title-agreement --control + live, glossary-census --control + live, check-date-formatting);
    then the rendered battery (99-ar02 then 99-ar03, one path per invocation, --list count checked
    first). Assemble 99-VERIFICATION.md: criterion->plan->oracle->observed result, controls beside
    zeros, bounds and residues named, and the honored-evidence table for the five waived decisions
    carried verbatim.



    Present to the operator: the rendered ar captures of the 404, /my-work/intake, the /search chips,
    one dated surface, /activity's relative time, and the position banner in all three seeded states;
    the D-19 tie-break list from the nav-title lane's SUMMARY; and the residue register. Ask for the
    phase sign-off. DECIDING rests with the overseer/operator in writing; this checkpoint blocks
    until released through the ruled two-column process (decision: overseer; approve command:
    orchestrator). No worker, orchestrator, or engine answers it.

  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes. The lane's own criteria, carried: Every battery member green in THIS run with outputs captured into the register; both specs collect at 8 and 10 and pass; no green quoted from an earlier wave; no leg recorded NOT CONSTRUCTED — an unrunnable leg leaves this task red and parks by name.</acceptance_criteria>
  <done>The register exists and every criterion has a fresh, controlled, named proof.</done>
</task>

</tasks>

<verification>
POPULATION: the paths in files_modified. OUTSIDE IT: every other task's files, the exogenous paths
(D-03), and every obligation this task's acceptance items do not name — those belong to the lane
parts before and after this one, named in `depends_on` and in the SUMMARY.

The lane's own population statement, carried verbatim from the pre-recut plan:
POPULATION: the two planning artifacts. OUTSIDE IT: every source and i18n file (a diff outside
.planning here is a scope defect), the ROADMAP/REQUIREMENTS row flips and the register updates (the
overseer's close-out acts, never a plan's), and the deferred residues owned by later phases.
</verification>

<success_criteria>
Land the static battery for the consolidated re-proof, proven by the acceptance items below in a single pass.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-39-SUMMARY.md` when done. It MUST record every command this task ran with its verbatim output, the
populations it re-derived with the commands that produced them, and anything left for a named later
task. The lane's own SUMMARY contract, carried verbatim from the pre-recut plan (satisfy the parts
this task owns): record in the file named under <output> (99-25-SUMMARY.md in the pre-recut plan) when done. It MUST record: the battery
results table; the two spec results per test; the three banner-state outcomes; the checkpoint answer
verbatim with who gave it; and the residue list carried to P102/P103.

**FRONT-MATTER IS MANDATORY.** The file MUST open with YAML front-matter containing `status: complete`. Completion is derived from that marker at the next compile, so a SUMMARY without it makes this task read as **pending** no matter how many gates passed — measured on P99-32 and again on P99-61, which passed 7/7 and still compiled as pending (PRODUCT ENTRY 36). Verify with `node scripts/completion-contract-check.mjs <journal> <ref>`.
</output>
