---
phase: 99-arabic-coverage
plan: 07
type: execute
wave: 2
depends_on: [99-03, 99-06]
files_modified:
  - 'frontend/src/{i18n/en/common.json,i18n/ar/common.json,auth/LoginPage.tsx,auth/RegisterPage.tsx,auth/ResetPasswordPage.tsx,components/ai-extraction-button/AIExtractionButton.tsx,components/ai/EntityLinkSuggestions.tsx,components/availability-polling/AvailabilityPollCreator.tsx,components/briefing-books/BriefingBookBuilder.tsx,components/bulk-actions/BulkActionPreviewDialog.tsx,components/calendar/CalendarEmptyWizard.tsx,components/calendar/ConflictResolution/ConflictResolutionPanel.tsx,components/calendar/ConflictResolution/ReschedulingSuggestions.tsx,components/calendar/ConflictResolution/SchedulingConflictComparison.tsx,components/commitment-editor/CommitmentEditor.tsx,components/contacts/BatchContactReview.tsx,components/contacts/BusinessCardScanner.tsx,components/contacts/ContactForm.tsx,components/contacts/DocumentExtractor.tsx,components/decision-list/DecisionList.tsx,components/delegation/CreateDelegationDialog.tsx,components/delegation/DelegationCard.tsx,components/delegation/RevokeDelegationDialog.tsx,components/dossier-recommendations/DossierRecommendationCard.tsx,components/duplicate-detection/DuplicateCandidatesList.tsx,components/edit-approval-flow/EditApprovalFlow.tsx,components/engagement-recommendations/RecommendationsPanel.tsx,components/entity-links/EntityLinkManager.tsx,components/entity-links/EntitySearchDialog.tsx,components/entity-links/LinkCard.tsx,components/entity-templates/TemplateSelector.tsx,components/export-import/ImportDialog.tsx,components/export-import/ImportValidationResults.tsx,components/follow-up-list/FollowUpList.tsx,components/forms/FormErrorDisplay.tsx,components/forums/ForumDetailsDialog.tsx,components/input-dialog/InputDialog.tsx,components/layout/Header.tsx,components/layout/nav-user.tsx,components/legislation/LegislationDetail.tsx,components/legislation/LegislationList.tsx,components/list-page/ToolbarSearch.tsx,components/meeting-minutes/ActionItemsList.tsx,components/meeting-minutes/AttendeesList.tsx,components/modern-nav/Dashboard/ExecutionsTabs.tsx,components/modern-nav/ExpandedPanel/DocumentTree.tsx,components/notifications/NotificationItem.tsx,components/notifications/NotificationPreferences.tsx,components/onboarding/MilestonesCelebration.tsx,components/pdf-generator-button/PDFGeneratorButton.tsx,components/position-editor/PositionEditor.tsx,components/positions/AttachPositionDialog.tsx,components/positions/PositionDossierLinker.tsx,components/report-builder/ColumnBuilder.tsx,components/report-builder/FilterBuilder.tsx,components/report-builder/GroupingBuilder.tsx,components/report-builder/ReportBuilder.tsx,components/report-builder/ReportPreview.tsx,components/report-builder/SaveReportDialog.tsx,components/report-builder/SavedReportsList.tsx,components/report-builder/ScheduleReportDialog.tsx,components/report-builder/SortingBuilder.tsx,components/risk-list/RiskList.tsx,components/sla-monitoring/SLAAtRiskList.tsx,components/sla-monitoring/SLAPolicyForm.tsx,components/table/DataTable.tsx,components/tags/TagAnalytics.tsx,components/tags/TagHierarchyManager.tsx,components/tags/TagSelector.tsx,components/tasks/AddContributorDialog.tsx,components/tasks/ConflictDialog.tsx,components/tasks/DeleteTaskDialog.tsx,components/tasks/TaskEditDialog.tsx,components/tasks/WorkItemLinker.tsx,components/ui/confirm-remove-button.tsx,components/ui/dialog.tsx,components/ui/heroui-modal.tsx,components/ui/sidebar.tsx,components/waiting-queue/AgingIndicator.tsx,components/waiting-queue/AssignmentDetailsModal.tsx,components/version-comparison/VersionComparison.tsx,components/version-history-viewer/VersionHistoryViewer.tsx,components/waiting-queue/EscalationDialog.tsx,components/workflow-automation/ActionBuilder.tsx,components/workflow-automation/ConditionBuilder.tsx,components/workflow-automation/WorkflowExecutionsList.tsx,components/workflow-automation/WorkflowRuleCard.tsx,pages/IntakeQueue.tsx,pages/MoUs/MousPage.tsx,pages/WaitingQueue.tsx,pages/WorkingGroupsPage.tsx,pages/analytics/AnalyticsDashboardPage.tsx,pages/data-library/DataLibraryPage.tsx,pages/entity-comparison/EntityComparisonPage.tsx,pages/events/EventsPage.tsx,pages/geographic-visualization/GeographicVisualizationPage.tsx,pages/intelligence/IntelligencePage.tsx,pages/reports/ReportsPage.tsx,pages/sla-monitoring/SLADashboardPage.tsx,pages/webhooks/WebhooksPage.tsx,router/index.tsx,routes/__root.tsx,routes/_protected/admin/ai-usage.tsx,routes/_protected/after-actions/$afterActionId.tsx,routes/_protected/after-actions/$afterActionId/versions.tsx,routes/_protected/engagements/$engagementId/after-action.tsx,routes/_protected/positions/$id/approvals.tsx,routes/_protected/positions/$id/versions.tsx,routes/_protected/stakeholder-influence.tsx}'
  - .planning/phases/99-arabic-coverage/99-07-SUMMARY.md
autonomous: true
requirements: [AR-04b]
success_criterion: 'Land the atomic flatten for the common.json flatten, proven by the acceptance items below in a single pass.'
routing:
  floor: frontier
truths:
  - 'Decisions covered — D-12 AS CORRECTED 2026-08-18 (RULING-P99-06): the nested duplicate subtree named common (56 keys / 67 leaves, both locales) merges into the namespace root, and it is NOT collision-free — nested `error` (scalar) vs root `error` (OBJECT) and nested `search` (scalar) vs root `search` (OBJECT) are TWO TYPE CLASHES per locale. The ruled repair relocates the scalars to `error.label` and `search.label` (target verified absent in both locales) and repoints their 13 dot-form consumers in this same commit; D-13: both locales plus the site rewrite land in ONE commit, with scripts/resolve-check.mjs run before AND after and scripts/neg-taskcard.mjs output-asserted (3x MISS=true — assert the text, its exit code is 0 either way) — and because that harness cannot see the flatten (zero of its 214 lookups route through the nested subtree), the discriminating instrument for this change is the type-aware collision oracle below, not the harness; D-14: this plan is the depends_on ancestor of every conversion-touching plan — the sequencing lives in the DAG; D-15: no plan hardcodes the pre-flatten key shape, and this plan authors NO new top-level key literally named common, ever; D-05: the population this plan owns is stated with what falls outside it.'
must_haves:
  truths:
    - 'Decisions covered — D-05, D-12, D-13, D-14, D-15: one atomic commit; the two type clashes relocated the ruled way; the collision oracle type-aware and positively controlled; the inversion retired, never re-creatable. || Both locale common.json files carry NO top-level key named common after the change, and every leaf path that lived in the nested subtree resolves at the namespace root (67 moved leaves per locale, re-derived in-task before the merge) || The TWO type clashes are relocated, not merged: nested `common.error` ("Error"/"خطأ") becomes root `error.label` and nested `common.search` ("Search"/"بحث") becomes root `search.label` in BOTH locales, leaving root `error` at 2 children and root `search` at 10 children (12 leaves) intact. A naive deep-merge in either direction destroys live copy and is the failure this plan exists to avoid. || All 13 dot-form consumers of the two relocated scalars are repointed in the SAME commit: 5 `t(''common.error''…)` sites (DataLibraryPage.tsx, MousPage.tsx, engagements/$engagementId/after-action.tsx, after-actions/$afterActionId.tsx, after-actions/$afterActionId/versions.tsx) become `t(''common:error.label''…)`, and 8 `t(''common.search''…)` sites (list-page/ToolbarSearch.tsx x2, table/DataTable.tsx, modern-nav/Dashboard/ExecutionsTabs.tsx, modern-nav DocumentTree.tsx, MousPage.tsx, events/EventsPage.tsx, routes/__root.tsx) become `t(''common:search.label''…)`. The 404 surface renders بحث through `common.search`, so criterion 3''s UI99-C5 leg rides this repoint.'
    - "Every dot-form t('common.X') site whose X RESOLVES through the nested subtree at HEAD is rewritten to explicit colon form common:X in the SAME commit as the JSON flatten. Re-derived 2026-08-18 at 9ffa2b80c by simulating the ruled merge: **130 owed sites / 50 files** post-flatten (127 sites / 48 files / 45 distinct keys measured against the pre-flatten nested subtree — the class GROWS by the three relocated-scalar consumers whose key path becomes a root object); the first draft and checker B both measured 120 / 46 with a slightly tighter binding filter — BOTH numbers are stated because they are two models of one class, and the in-task re-derivation is the one that governs. `t('common.dashboard')` at routes/__root.tsx:37 is IN this class (root `dashboard` exists today and survives the flatten), so it is rewritten here. || Every `common:common.X` double-prefixed site is de-prefixed to `common:X` in this same commit — re-derived 100 sites / 59 files / 37 distinct keys, of which 79 sites across 52 files RESOLVE through the nested subtree today and BREAK the moment the flatten lands. This class was owned by no plan in the first draft while 32-35 of its files were owned by no plan at all; it moves here because D-13's own reason (a partial land breaks working sites) applies to it identically. Where the de-prefixed key does not exist at the flattened root (21 sites), the site is still de-prefixed — it misses before and after, and the key is authored by the common-owner lane; it is NAMED in the SUMMARY as a handoff. || The dot-form common.* sites whose key does NOT exist at the flattened root are NOT owed by this plan and are left byte-untouched — the not-owed class is defined by the RESOLUTION PREDICATE (a key absent from the post-flatten root), never by a file list, so this plan's own oracle cannot demand what this clause forbids. Re-derived at plan time: the 14 distinct keys behind them (noResults, goBack, collapse, expand, add, more, cardView, tableView, toggleColumns, firstPage, previousPage, nextPage, lastPage) are absent from the root before and after the flatten and belong to the common-owner lane. || scripts/resolve-check.mjs reports zero routing misses BEFORE the change and AFTER it in the same task run, and scripts/neg-taskcard.mjs prints MISS=true on all three negative-control lines both times (output asserted, not exit code). This is D-13's named ritual and it is run — but it is recorded in the SUMMARY as a KEEP-TRUE guard, not as proof of the flatten: zero of the harness's 214 lookups resolve through the nested subtree, so it prints the same number after a completely botched merge. The discriminating proof is the type-aware oracle."
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node -e "const fs=require(\"fs\");const load=p=>JSON.parse(fs.readFileSync(p,\"utf8\"));const clash=(nested,root)=>{const out=[];for(const k of Object.keys(nested||{}))if(Object.prototype.hasOwnProperty.call(root,k)){const a=typeof nested[k],b=typeof root[k];if(a!==b)out.push(k+\":\"+a+\"/\"+b)}return out};const ctl=clash({x:\"s\"},{x:{y:1}});if(ctl.length!==1){console.error(\"POSITIVE CONTROL FAILED — the collision check cannot see a scalar-vs-object clash\");process.exit(1)}for(const loc of [\"en\",\"ar\"]){const j=load(process.argv[1]+\"/frontend/src/i18n/\"+loc+\"/common.json\");if(j.common!==undefined){console.error(loc+\" nested common subtree still present\");process.exit(1)}if(typeof j.error!==\"object\"||typeof j.search!==\"object\"){console.error(loc+\" error/search are not objects — a naive merge overwrote them\");process.exit(1)}if(typeof j.error.label!==\"string\"||typeof j.search.label!==\"string\"){console.error(loc+\" error.label / search.label missing — the relocated scalars were dropped\");process.exit(1)}if(Object.keys(j.search).length<11||Object.keys(j.error).length<3){console.error(loc+\" root error/search lost children — merge destroyed the object\");process.exit(1)}for(const k of [\"all\",\"cancel\",\"loading\",\"notFound\"]){if(j[k]===undefined){console.error(loc+\" root missing flattened leaf \"+k);process.exit(1)}}if(typeof j.notFound?.title!==\"string\"){console.error(loc+\" notFound.title not a string\");process.exit(1)}}console.log(\"FLATTEN-TYPE-OK\")" "$R"'
      text: 'TYPE-AWARE collision oracle with its own scalar-vs-object POSITIVE CONTROL run first: both locales flattened, no top-level common key, root error/search still OBJECTS with their children intact and each carrying the relocated .label scalar, spot flattened leaves present — RED at HEAD (the nested subtree is present and error.label/search.label do not exist). The instrument that failed on this change was a child-set comparison, which a scalar trivially satisfies; it is not re-trusted on a clean run.'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node -e "const fs=require(\"fs\"),path=require(\"path\");const R=process.argv[1];const root=JSON.parse(fs.readFileSync(R+\"/frontend/src/i18n/en/common.json\",\"utf8\"));const get=(o,p)=>p.split(\".\").reduce((a,k)=>(a&&typeof a===\"object\")?a[k]:undefined,o);const walk=(d,acc=[])=>{for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory()){if(e.name!==\"node_modules\"&&e.name!==\"i18n\")walk(p,acc)}else if(/\\.(ts|tsx)$/.test(e.name))acc.push(p)}return acc};const bound=s=>{const ms=[...s.matchAll(/useTranslation\\(\\s*([^)]*)\\)/g)];if(ms.length===0)return true;for(const m of ms){const a=m[1].trim();if(!a)return true;const ss=[...a.matchAll(/[\x27\x22]([^\x27\x22]+)[\x27\x22]/g)].map(x=>x[1]);if(ss.length===0)return true;if(ss.includes(\"common\")||ss.includes(\"translation\"))return true}return false};let owed=0,dbl=0,seen=0,owedFiles=new Set();for(const f of walk(R+\"/frontend/src\")){const s=fs.readFileSync(f,\"utf8\");const cb=bound(s);for(const m of s.matchAll(/\\bt\\(\\s*[\x27\x22]common\\.([A-Za-z0-9_.]+)[\x27\x22]/g)){seen++;if(cb&&get(root,m[1])!==undefined){owed++;owedFiles.add(path.relative(R,f))}}for(const m of s.matchAll(/common:common\\./g))dbl++}if(seen===0){console.error(\"POSITIVE CONTROL FAILED — the walk found no dot-form common. sites at all\");process.exit(1)}if(owed!==0){console.error(\"dot-form sites still resolving at the flattened root: \"+owed+\" in \"+owedFiles.size+\" files -> \"+[...owedFiles].slice(0,5).join(\",\"));process.exit(1)}if(dbl!==0){console.error(\"double-prefixed common:common. sites survive: \"+dbl);process.exit(1)}console.log(\"REWRITE-OK owed=0 dbl=0 scanned=\"+seen)" "$R"'
      text: "zero dot-form t(common.X) sites survive whose file BINDS common (or translation, or a bare hook) AND whose X resolves at the flattened root — the OWED class, defined by BINDING plus RESOLUTION rather than by a file list, so neither the wrong-ns-bound sites this plan is forbidden to touch nor the keys the common-owner lane authors later can be demanded by it. The first draft's version of this clause demanded zero matches over a fixed 46-file list while acceptance[4] forbade touching 16 of the sites inside it; a predicate cannot contradict its own plan. Zero double-prefixed common:common. sites survive repo-wide, and the walk's own scanned-site count is the positive control that the scan reached source at all — RED at HEAD (16 owed sites resolve at the PRE-flatten root, plus 100 double-prefixed sites; post-flatten the owed class is the simulated 130 sites across 50 files as the 67 nested leaves and the two relocated scalars arrive at the root, which is what makes this predicate grow into its job rather than shrink out of it)"
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node -e "const ar=JSON.parse(require(\"fs\").readFileSync(process.argv[1]+\"/frontend/src/i18n/ar/common.json\",\"utf8\"));if(ar.common!==undefined||typeof ar.notFound?.title!==\"string\"||typeof ar.search?.label!==\"string\")process.exit(1)" "$R" && node "$R/scripts/resolve-check.mjs" "$R" | command grep -q "routings with a miss: 0" && test "$(node "$R/scripts/neg-taskcard.mjs" "$R" | command grep -c "MISS=true")" -eq 3'
      text: 'the ar locale is flattened with its relocated search.label present (the discriminating clause — RED at HEAD) AND the committed resolution harness stays green post-flatten with its negative control still discriminating (keep-true clauses, recorded as such because neither can see this change)'
---

<objective>
Land the atomic flatten for the common.json flatten, proven by the acceptance items below in a single pass.

This task is part 1 of 1 of the common.json flatten; the parts are ordered by `depends_on` and share one
file surface, so each part reads the tree the part before it produced. The task-unit contract caps a
task at six acceptance items and twenty-four surface, which is why the lane is cut this way.
</objective>

<context>
@.planning/phases/99-arabic-coverage/99-CONTEXT.md
@.planning/phases/99-arabic-coverage/99-RESEARCH.md
@.planning/phases/99-arabic-coverage/rulings/RULING-P99-06-B01-FLATTEN-BASIS.md
@scripts/resolve-check.mjs
@scripts/neg-taskcard.mjs
@frontend/src/i18n/en/common.json

Ref notes: 99-CONTEXT.md — D-12 AS CORRECTED, D-13..D-15; 99-RESEARCH.md — §2.3-§2.5 — the inversion, the blast radius, the sequencing; RULING-P99-06 — the type-clash finding and the ruled relocation, READ WHOLE; resolve-check.mjs — the committed real-i18next harness — keep-true here, it cannot see this change; neg-taskcard.mjs — its negative control — output must be asserted, exit code is 0 unconditionally

<interfaces>
Re-derived on disk 2026-08-18 at 9ffa2b80c — commands in 99-REVISION-1.md §Derivations:

- Both locales: 38 top-level keys, 0 top-level scalars, nested subtree literally named `common`
  with 56 keys / 67 leaves. **TWO same-name pairs, both TYPE CLASHES, both locales:**
  `common.error` is the scalar "Error"/"خطأ" while root `error` is an OBJECT with 2 children
  (failedToLoadData, unknownError); `common.search` is the scalar "Search"/"بحث" while root
  `search` is an OBJECT with 10 children / 12 leaves. The relocation targets `error.label` and
  `search.label` are ABSENT in both locales (verified — the last failure on this change teaches
  exactly this check). The other 54 nested keys move clean; ar's 12 extra leaves ride along.
  The earlier "child key sets are disjoint — 0 overwrites" derivation is FALSE and superseded:
  a scalar has no child keys, so the check was structurally incapable of seeing a type clash.
- Consumer counts, re-derived: 5 `t('common.error'…)` sites and 8 `t('common.search'…)` sites =
  13 dot-form consumers, listed by file in must_haves. A bare-substring count of the two paths
  returns 7 and 8 — the extra two `common.error` occurrences are not `t()` calls; two numbers,
  two populations, both stated (D-05).
- The atomic rewrite set: dot-form `t('common.X')` where X exists in the nested subtree, in files
  binding common/translation or a bare hook — **127 sites / 48 files / 45 distinct keys** by this
  plan's model; **120 / 46 / 45** by the first draft's and checker B's tighter binding filter.
  Distinct-key counts agree exactly; the file delta is `components/positions/PositionDossierLinker.tsx`
  and `pages/intelligence/IntelligencePage.tsx`. RE-DERIVE in-task (D-04) and reconcile against
  files_modified; a site in a file OUTSIDE files_modified means the tree moved: STOP and record.
- The double-prefix class: `common:common.X` — **100 sites / 59 files / 37 distinct keys**, of
  which **79 sites / 52 files resolve through the nested subtree today** and break at the flatten.
  The claim on record that "the nested subtree carries NO actions._ keys — all author" is FALSE:
  `common.common.actions` holds 9 keys in both locales and ~30 `common:common.actions._` sites
  resolve today. They are a rewrite, not an authoring job.
- NOT owed here: dot-form `common.*` sites whose X is absent from the post-flatten root (the
  14 distinct keys named in must_haves) — the common-owner lane authors those. The 33 colon-form
  `common:X` misses whose path lives in the nested subtree fix THEMSELVES at flatten — verify a
  sample resolves, touch nothing for them.
- `resolve-check.mjs` at HEAD: `214 lookups across 11 routings x 2 locales — routings with a
miss: 0`. **Zero of those lookups resolve through the nested subtree** (its two `translation`
  routings resolve `waitingQueue.entityStatus.*`, which lives at the ROOT), so it prints the same
  number before and after this change regardless of correctness. Run it — D-13 names it — and
  record it as a keep-true guard, not as the flatten's proof.
- `neg-taskcard.mjs` prints three `MISS=true` TaskCard lines through ns `translation` at HEAD and
  MUST still print them after this plan; same keep-true status.
- Estimated logic-diff size for this task: **~41,400 bytes** (247 rewritten source lines at x2,
  plus the two nested JSON blocks) against the engine's 60,000-byte `gates.diffCap`. It is the
  largest atomic task in the phase that cannot legally be split — D-13 makes atomicity the
  requirement — and it sits ~31% under the cap with the estimate stated as a floor.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>the common.json flatten — the atomic flatten</name>
  <files>the paths in files_modified</files>
  <read_first>RULING-P99-06 whole; 99-RESEARCH.md §2.3-§2.5; scripts/resolve-check.mjs and scripts/neg-taskcard.mjs headers; both common.json files (the nested subtree shape and the root error/search objects)</read_first>
  <action>
    This is the first part of the lane, so the lane method below is yours to execute in full over the paths in files_modified. Re-derive every population before relying on it (D-04), run each oracle and record its verbatim output, and name in the SUMMARY anything you leave for a later part.

    --- THE LANE METHOD (carried verbatim from the pre-recut plan; the acceptance items above bound what THIS task owes) ---

    1. PROVE-BEFORE: run resolve-check (expect "routings with a miss: 0") and neg-taskcard
       (expect exactly 3 MISS=true lines); record both outputs and label them keep-true.
    2. RE-DERIVE, with the TYPE dimension: for each locale compare every nested key name against
       the root, and for each same-name pair compare TYPES. Expect exactly the two known clashes;
       any THIRD clash aborts the task and is recorded. Run your comparison against a synthetic
       scalar-vs-object pair first and confirm it reports one clash — an instrument that cannot
       see the planted clash is the instrument that already failed here.
    3. FLATTEN: in each locale move the 54 clean nested keys to the root; move nested `error`'s
       scalar to `error.label` and nested `search`'s scalar to `search.label` INSIDE the existing
       root objects, leaving their existing children untouched; then delete the nested key. Never
       author any new top-level key named common (D-15's standing guard).
    4. REWRITE, all in this commit:
       (a) the 13 relocated-scalar consumers -> `common:error.label` / `common:search.label`;
       (b) every re-derived dot-form site whose key resolves at the flattened root ->
           `t('common:X'…)`, second arguments and options KEPT byte-identical (the mask drop is
           a later plan's, ordering is the DAG's);
       (c) every `common:common.X` -> `common:X`, including the ~21 whose key does not exist at
           the root — de-prefixing is correct either way and the missing keys are NAMED for the
           common-owner lane.
       No other key, string, hook or import changes.
    5. PROVE-AFTER: the type-aware collision check again; the rewrite scan at zero owed / zero
       double-prefixed; resolve-check and neg-taskcard again (keep-true); plus a spot mirror
       probe: common:all, common:notFound.title, common:error.label, common:search.label,
       common:error.failedToLoadData and common:search.placeholder all resolve in BOTH locales —
       the last four are the keys the first draft's four-key spot check omitted, which is exactly
       where the undecidable merge lived.
    6. ONE commit carrying the 2 JSON files + all rewritten sources. Then run the frontend
       type-check.

  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes. The lane's own criteria, carried: The commit is ONE and carries both JSON files plus every rewritten source (atomicity is a property of the history); the type-aware collision check ran with its planted control BEFORE the merge logic was trusted; the six-key post-flatten resolution probe passes in both locales; the re-derived site lists and their reconciliation against files_modified are recorded (dot sites/distinct/files, double-prefix sites/files, and the two model numbers where they differ); the before AND after harness outputs are recorded and labelled keep-true; type-check green.</acceptance_criteria>
  <done>The inversion is dead: nested subtree gone in both locales, the two type clashes relocated rather than merged, every site that leaned on the subtree rewritten to colon form in the same commit, proven by an instrument that can see a type collision.</done>
</task>

</tasks>

<verification>
POPULATION: the paths in files_modified. OUTSIDE IT: every other task's files, the exogenous paths
(D-03), and every obligation this task's acceptance items do not name — those belong to the lane
parts before and after this one, named in `depends_on` and in the SUMMARY.

The lane's own population statement, carried verbatim from the pre-recut plan:
POPULATION: the 110 listed files. OUTSIDE IT: dot-form common sites whose key is absent from the
post-flatten root (the common-owner lane), all key AUTHORING, every second-argument drop (the
deletion lanes). The plan is deliberately narrow: structure, the two ruled relocations, and the
rewrite the structure forces — nothing else.
</verification>

<success_criteria>
Land the atomic flatten for the common.json flatten, proven by the acceptance items below in a single pass.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-07-SUMMARY.md` when done. It MUST record every command this task ran with its verbatim output, the
populations it re-derived with the commands that produced them, and anything left for a named later
task. The lane's own SUMMARY contract, carried verbatim from the pre-recut plan (satisfy the parts
this task owns): record in the file named under <output> (99-03-SUMMARY.md in the pre-recut plan) when done. It MUST record: the
single commit hash; the type-clash re-derivation with its planted-control result; the relocation
mapping for both scalars and all 13 consumers; the re-derived rewrite numbers (dot sites/distinct/
files under BOTH binding models, double-prefix sites/files) and their reconciliation; the
before+after resolve-check and neg-taskcard outputs verbatim with their keep-true label; the
six-key resolution probe; and the double-prefixed keys left missing at the root, by name, for the
common-owner lane.
</output>
