---
phase: 99-arabic-coverage
plan: 49
type: execute
wave: 10
depends_on: [99-54]
files_modified:
  - 'frontend/src/components/relationships/{AdvancedGraphVisualization,AnalyticResultView}.tsx'
  - frontend/src/components/modern-nav/IconRail/IconRail.tsx
  - 'frontend/src/i18n/{en,ar}/{empty-states,graph,common}.json'
  - .planning/phases/99-arabic-coverage/99-49-SUMMARY.md
autonomous: true
requirements: [AR-04a]
success_criterion: 'Establish bilingual leaf resolution for the ruled lane-2 and lane-3 dynamic domains before any fallback is deleted.'
routing:
  floor: frontier
truths:
  - 'RULING-P99-105: the independent P99-48 instrument owns classification; this task owns only the production key-routing and locale population it grades.'
must_haves:
  truths:
    - 'Both empty-states bundles gain exactly the 32 missing list leaves re-derived from the callers: cta for commitment, document, dossier, event, generic, mou, position, and task; plus hint, firstDescription, createFirst, create, firstTitle, and import for elected_official, topic, work_item, and working_group. The eight firstTitle/import leaves across those four types were omitted from the earlier 24-leaf count measured in run 0045 and are restored here; this omitted-list eight is a different eight from the cluster eight below and the two are never interchangeable. Every en and ar value is nonempty, meaningful localized copy; no existing locale leaf is removed or rewritten merely to satisfy the census.'
    - 'AdvancedGraphVisualization routes the cluster label through graph:type.<canonical-type> for exactly eight dossier-card values — country, organization, person, forum, topic, working_group, engagement, and elected_official — and through graph:type.unknown for every other runtime string. The unknown route is an explicit source branch the AST can prove alongside canonical membership, never a fallback argument or an inference from expression text. Both graph bundles own the nonempty localized unknown leaf. The final observable varies across person, elected_official, and an out-of-domain value: the first two render their resource labels and the last renders the localized unknown label, never a raw type or top-level graph key. Keep the fallback-bearing call in place for P99-33 to delete after P99-50; this task repairs routing, not masks.'
    - 'The two remaining unbounded domains in the ruled lane-3 population are bounded the same way the cluster is, because an unbounded domain is exactly what the census reports unclassified. AnalyticResultView routes its two type.<runtime> labels through graph:type.<canonical-type> only when the AST can prove membership in the production dossier-card display constant, and through graph:type.unknown otherwise - its node type is declared an unbounded string, so no proof is available without the check. IconRail routes item.tooltipKey through its production tooltip constant when membership is provable and through a localized unknown route otherwise, because items arrives as a prop and the component default bounds nothing. Every unknown leaf is present and nonempty in both locales in the bundle its own caller namespace resolves to - graph for the two graph callers, the default namespace bundle for IconRail. No fallback argument is deleted anywhere; deletion remains P99-31 through P99-38 after P99-50.'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node "$R/scripts/i18n-dynamic-key-audit.mjs" "$R" --self-check || { echo "FAIL: self-check"; exit 1; }; AUDITJSON="$(mktemp -t p99audit).json" || { echo "INSTRUMENT-CANNOT-RUN: mktemp failed"; exit 3; }; AUDITERR="$AUDITJSON.err"; node "$R/scripts/i18n-dynamic-key-audit.mjs" "$R" --profile ar04-live --json > "$AUDITJSON" 2>"$AUDITERR"; AUDITST=$?; node -e "JSON.parse(require(\"node:fs\").readFileSync(process.argv[1],\"utf8\"))" "$AUDITJSON" 2>/dev/null || { echo "INSTRUMENT-CANNOT-RUN: the dynamic-key audit produced no parseable JSON (exit $AUDITST), so it did not run to a verdict"; head -3 "$AUDITERR"; rm -f "$AUDITJSON" "$AUDITERR"; exit 3; }; echo "audit-exit=$AUDITST (nonzero here is a CONTENT verdict, not an execution failure; JSON parsed)"; AUDITJSON="$AUDITJSON" node -e "const d=JSON.parse(require(\"node:fs\").readFileSync(process.env.AUDITJSON,\"utf8\"));const AGV=\"frontend/src/components/relationships/AdvancedGraphVisualization.tsx\";const site=r=>r.file+\":\"+r.line;const isOwned=r=>r.file===AGV&&(r.key===\"type.individual\"||r.key===\"type.mou\");const miss=(d.rows||[]).filter(r=>r.en!==\"ok\"||r.ar!==\"ok\");const missOut=miss.filter(r=>!isOwned(r));const missOwned=miss.filter(isOwned);const unc=d.unclassified||[];const uncSites=[...new Set(unc.map(site))].sort();const pk=u=>(u.file===AGV?\"AGV\":u.file)+\"|\"+(u.proofSite||\"none\");const byProof={};unc.forEach(u=>{byProof[pk(u)]=(byProof[pk(u)]||0)+1});const EXPECT={\"AGV|graph-type-map\":2,\"AGV|graph-relationship-map\":1};const cp=d.callerPopulations;console.log(\"lane3Sites=\"+cp.lane3Sites+\" listSites=\"+cp.listSites+\" listLeaves=\"+cp.listLeaves);console.log(\"unclassified=\"+unc.length+\" distinct-sites=\"+uncSites.length+\"/\"+unc.length+\" -> \"+JSON.stringify(uncSites));console.log(\"missing-outside-owned=\"+missOut.length+\"/\"+miss.length+\" missing-owned-by-successor=\"+missOwned.length+\"/\"+miss.length);let bad=0;const L=s=>String(s||\"\").startsWith(\"lane3.\");const observed=new Set();for(const r of (d.rows||[]))if(L(r.family))observed.add(r.family);for(const u of (d.unclassified||[]))if(L(u.family))observed.add(u.family);const rules=new Set();for(const pr of (d.proofRows||[]))if(L(pr.family))rules.add(pr.family);const uncovered=[...observed].filter(f=>!rules.has(f));const dead=[...rules].filter(f=>!observed.has(f));console.log(\"lane3 observed-families=\"+observed.size+\" handling-rules=\"+rules.size+\" | DIAGNOSTICS ONLY lane3Sites=\"+cp.lane3Sites+\" lane3Families=\"+cp.lane3Families+\" listLeaves=\"+cp.listLeaves);if(observed.size===0||rules.size===0){console.error(\"INSTRUMENT-CANNOT-RUN: one side of the coverage relation is EMPTY (observed=\"+observed.size+\" rules=\"+rules.size+\"), so neither direction could have failed; this leg verified nothing\");process.exit(3)}if(uncovered.length){console.error(\"FAIL: lane3 family observed with no handling rule (a new unhandled kind): \"+JSON.stringify(uncovered));process.exit(1)}if(dead.length){console.error(\"FAIL: lane3 handling rule matches no observed family (a dead rule): \"+JSON.stringify(dead));process.exit(1)}const LI=x=>String(x||\"\").startsWith(\"list.\");const entOf=k=>{const q=String(k).split(\".\");return q.length>=3?q.slice(1,-1).join(\".\"):null};const lrows=(d.rows||[]).filter(r=>LI(r.family));const famEnt={};for(const r of lrows){const e=entOf(r.key);if(e){(famEnt[r.family]=famEnt[r.family]||new Set()).add(e)}}const lf=Object.keys(famEnt).sort();if(lf.length===0){console.error(\"INSTRUMENT-CANNOT-RUN: no list rows enumerated, so the completeness relation could not have failed\");process.exit(3)}const refset=[...famEnt[lf[0]]].sort().join(\"|\");const ragged=lf.filter(f=>[...famEnt[f]].sort().join(\"|\")!==refset);console.log(\"list completeness: families=\"+lf.length+\" entities-per-family=\"+famEnt[lf[0]].size+\" product=\"+(lf.length*famEnt[lf[0]].size)+\" listLeaves=\"+cp.listLeaves);if(ragged.length){console.error(\"FAIL: list families do not cover the SAME entity set - \"+JSON.stringify(ragged)+\" differ from \"+lf[0]+\"; a zero bought by dropping an entity from some families but not others\");bad=1}if(lf.length*famEnt[lf[0]].size!==cp.listLeaves){console.error(\"FAIL: families x entities (\"+(lf.length*famEnt[lf[0]].size)+\") does not equal listLeaves (\"+cp.listLeaves+\") - the cross-product is not whole\");bad=1}const lobs=new Set();for(const r of (d.rows||[]))if(LI(r.family))lobs.add(r.family);for(const u of (d.unclassified||[]))if(LI(u.family))lobs.add(u.family);const lrul=new Set();for(const pr of (d.proofRows||[]))if(LI(pr.family))lrul.add(pr.family);const lunc=[...lobs].filter(f=>!lrul.has(f));const ldead=[...lrul].filter(f=>!lobs.has(f));console.log(\"list observed-families=\"+lobs.size+\" handling-rules=\"+lrul.size+\" | DIAGNOSTICS ONLY listSites=\"+cp.listSites+\" listLeaves=\"+cp.listLeaves);if(lobs.size===0||lrul.size===0){console.error(\"INSTRUMENT-CANNOT-RUN: one side of the list coverage relation is EMPTY (observed=\"+lobs.size+\" rules=\"+lrul.size+\"), so neither direction could have failed\");process.exit(3)}if(lunc.length){console.error(\"FAIL: list family observed with no handling rule: \"+JSON.stringify(lunc));bad=1}if(ldead.length){console.error(\"FAIL: list handling rule matches no observed family (dead rule): \"+JSON.stringify(ldead));bad=1}const fkeys=Object.keys(EXPECT).sort();const okeys=Object.keys(byProof).sort();const same=fkeys.length===okeys.length&&fkeys.every((k,i)=>k===okeys[i]&&EXPECT[k]===byProof[k]);console.log(\"FENCE observed=\"+JSON.stringify(byProof)+\" expected=\"+JSON.stringify(EXPECT));if(!same){console.error(\"FAIL: unclassified IDENTITY is not exactly the three statically unprovable AdvancedGraphVisualization domains, expected 2 graph-type-map plus 1 graph-relationship-map; got \"+JSON.stringify(byProof));bad=1}if(missOut.length!==0){console.error(\"FAIL: \"+missOut.length+\" missing leaves outside the successor-owned set: \"+JSON.stringify(missOut.map(r=>site(r)+\" \"+r.key)));bad=1}if(missOwned.length!==2){console.error(\"FAIL: successor-owned missing set is \"+missOwned.length+\", expected exactly 2 - narrowing must not absorb or invent the owned rows\");bad=1}process.exit(bad)"'
      text: "the audit is asserted over the ruled population with the excluded rows NAMED rather than merely dropped: the unclassified residue must be EXACTLY the three statically unprovable AdvancedGraphVisualization domains at :1546, :1564 and :1649 BY FILE AND LINE - a count of three is not enough, because a substitution holds a count while changing which rows it counts - and every missing leaf OUTSIDE the successor-owned AdvancedGraphVisualization.tsx:1857 pair must be zero, while that owned pair must still number exactly two, so a narrowing cannot quietly absorb the rows it excludes; lane3Sites, listSites and listLeaves are reported as DIAGNOSTICS ONLY and are never compared: per RULING-P99-326 no ruled TYPE set exists for lane3, so a frozen count is an F1 threshold on a moving population, and the coverage RELATION below (every observed family covered by a handling rule, every rule matching an observed family) is what this criterion asserts instead. ANTI-SHRINK PROTECTION, PARTIALLY CLOSED - and the remaining hole is named exactly (RULING-P99-337). The clause guards against a zero bought by shrinking the cross-product. That is now asserted BASE-FREE as completeness on the current tree: every list family must cover the SAME entity set, and families x entities must equal listLeaves. It is non-circular - the entity domain comes from an AST prover reading PRODUCTION source (proofSite list-entity-config), not from a list hardcoded in the instrument. WHAT IT CATCHES: an ASYMMETRIC shrink, where an entity is dropped from some families but not others; the family sets go ragged and the oracle fails. An empty row set exits 3 rather than passing vacuously. WHAT IT DOES NOT CATCH, MEASURED NOT ASSUMED: a UNIFORM shrink - the SAME entity removed from ALL nine families at once. The same-entity-set invariant still holds and the product still holds, at 9 x 16 = 144 instead of 9 x 17 = 153. That specific dodge is undetected by this criterion. WHY IT IS NOT CLOSED HERE: catching it requires comparing the corpus entity set against the domain the prover derives from production, and the instrument PROVES that domain but does not EXPOSE its members - proofRows carry status, proofSite, family, file, line, expression and namespaces only. The fix is therefore an instrument change, EXPOSE THE PROVEN DOMAIN MEMBERS ON THE PROOFROW, recorded in the milestone backlog. It is deliberately NOT a gate on this criterion: blocking on it would be disproportionate to one narrowly-named dodge, and inventing a substitute threshold here would reintroduce the frozen-number defect RULING-P99-326 removed. EVERY EXCLUDED SITE HAS A NAMED OWNER: the three AdvancedGraphVisualization domains derive from runtime rows, so no static analyser can close them, the instrument's refusal is SOUND rather than blind, and RULING-P99-241 owns them at the 99-50 boundary as a DEFINITIONAL ruling; the legend pair type.individual and type.mou is owned by this task's successor. A narrowing that excludes without assigning does not remove a defect, it hides one. AND THE FAILING NUMBERS OF RUN 0056 MAY NOT BE WRITTEN IN AS EXPECTATIONS: re-baselining to the measured unclassified 6 with missing 2 and 2 would enshrine three instrument artifacts and three unprovable domains as a permanent expectation, and of those eight residue rows exactly 2 of 8 are closable by product work FENCE DECLARATION (RULING-P99-327) — this EXPECT map is a FENCE, not a coverage threshold, and the distinction is recorded here so the next reader does not have to guess as we did. It fences the THREE statically-unprovable AdvancedGraphVisualization domains recorded as bucket (c) of the carried classification — ruled DEFINITIONAL and explicitly NOT an instrument defect — whose nodeTypes is a useMemo over new Set(rawNodes.map(n => n.type)) and therefore cannot be proven statically. Its EXACTNESS IS THE GUARD: a fourth unclassified pair means a new unprovable domain appeared and must be ruled before it is admitted, and a missing pair means one became provable and the fence must shrink. It is keyed by proofSite, NOT by line number, which is why it survived the drift from lines 1546/1564/1649 to 1542/1560/1645 — a line-keyed fence would have broken on an unrelated edit above, which is exactly the defect P99-53 tripped in P99-51. DELIBERATE UPDATE CONDITION: this map changes only when a ruling admits a new definitional residue or retires an existing one; it is never widened to accommodate a measurement."
    - 'The production diff is limited to the three ruled key-routing repairs - the graph cluster, the two AnalyticResultView type labels, and the IconRail tooltip - plus the named locale leaves. No fallback/default argument is deleted, no test-mode branch or English result helper is added, and no key family outside the 32 list leaves plus the unknown leaves those three repairs require changes.'
---

<objective>
Establish bilingual leaf resolution for the ruled lane-2 and lane-3 dynamic domains before any fallback is deleted.
</objective>

<context>
@.planning/phases/99-arabic-coverage/rulings/RULING-P99-105-DYNAMIC-GATE-REPAIR.md
@frontend/src/lib/dossier-type-guards.ts
@frontend/src/components/empty-states/ListEmptyState.tsx
@frontend/src/components/relationships/AdvancedGraphVisualization.tsx
</context>

<tasks>
<task type="auto">
  <name>repair the ruled dynamic key population</name>
  <files>the paths in files_modified</files>
  <read_first>RULING-P99-105 and the complete P99-48 pre-repair rows</read_first>
  <action>
    Add only the 32 caller-derived list leaves in both locales. Bound the two remaining unbounded lane-3
    domains the same way as the cluster: prove membership against the production constant and route
    everything else to an explicit localized unknown leaf, in AnalyticResultView and in IconRail.
    Repair ClusterNode so its production input is
    bounded by DOSSIER_CARD_TYPES plus a localized unknown sentinel rather than passed as an unprefixed
    graph key. Preserve the fallback argument for the ordered deletion lane. Run P99-48's self-check
    before its live profile and record both outputs verbatim; any unclassified or missing row stops.
  </action>
  <acceptance_criteria>Every acceptance item above passes, with no fallback deletion and no path outside files_modified.</acceptance_criteria>
  <done>Every ruled list and lane-3 nonliteral key domain resolves to a concrete leaf in en and ar before deletion is reachable.</done>
</task>
</tasks>

<verification>
POPULATION: every production routing site needed to bound the ruled lane-2 and lane-3 dynamic domains -
derived from the callers in the ruled profile population, which is the graph cluster and the two
AnalyticResultView type labels and the IconRail tooltip - plus the resource namespaces those callers
resolve to, in both locales, and the SUMMARY. This block previously said ONE production routing site
while the success criterion claimed lane-3, which is thirteen sites; the contradiction is corrected
here, not widened. OUTSIDE IT: the instrument (P99-48), every fallback deletion (P99-31 through
P99-38), tests, and every type.<runtime> caller outside the ruled profile population.
</verification>

<success_criteria>
Establish bilingual leaf resolution for the ruled lane-2 and lane-3 dynamic domains before any fallback is deleted.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-49-SUMMARY.md` with the exact added-key inventory,
cluster-domain cases, controlled audit output, and a diff statement proving no fallback was deleted.
</output>
