---
phase: 99-arabic-coverage
plan: 49
type: execute
wave: 10
depends_on: [99-48]
files_modified:
  - frontend/src/components/relationships/AdvancedGraphVisualization.tsx
  - 'frontend/src/i18n/{en,ar}/empty-states.json'
  - 'frontend/src/i18n/{en,ar}/graph.json'
  - .planning/phases/99-arabic-coverage/99-49-SUMMARY.md
autonomous: true
requirements: [AR-04a]
success_criterion: 'Establish bilingual leaf resolution for the ruled lane-2 and lane-3 dynamic domains before any fallback is deleted.'
routing:
  floor: frontier
truths:
  - 'RULING-P99-105: the independent P99-48 instrument owns classification; this task owns only the production key-routing and locale population it grades.'
must_haves:
  truths:
    - 'Both empty-states bundles gain exactly the 24 missing list leaves measured in run 0045: cta for commitment, document, dossier, event, generic, mou, position, and task; plus hint, firstDescription, createFirst, and create for elected_official, topic, work_item, and working_group. Every en and ar value is nonempty, meaningful localized copy; no existing locale leaf is removed or rewritten merely to satisfy the census.'
    - 'AdvancedGraphVisualization routes the cluster label through graph:type.<canonical-type> for the closed eight-value DOSSIER_CARD_TYPES display domain and through graph:type.unknown for every other runtime string. Both graph bundles own the nonempty localized unknown leaf. The final observable varies across person, elected_official, and an out-of-domain value: the first two render their resource labels and the last renders the localized unknown label, never a raw type or top-level graph key. Keep the fallback-bearing call in place for P99-33 to delete after P99-50; this task repairs routing, not masks.'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node "$R/scripts/i18n-dynamic-key-audit.mjs" "$R" --self-check && node "$R/scripts/i18n-dynamic-key-audit.mjs" "$R" --profile ar04-live --expect-list-sites 9 --expect-lane3-sites 13 --expect-unclassified 0 --expect-missing-en 0 --expect-missing-ar 0'
      text: 'the independent instrument first discriminates, then proves every ruled dynamic domain resolves to complete leaves in both locales with the exact nonempty caller populations and no unclassified fallback-bearing call'
    - 'The production diff is limited to the cluster key-routing repair and the named locale leaves. No fallback/default argument is deleted, no test-mode branch or English result helper is added, and no key family outside the 24 list leaves plus graph:type.unknown changes.'
---

<objective>
Establish bilingual leaf resolution for the ruled lane-2 and lane-3 dynamic domains before any fallback is deleted.
</objective>

<context>
@.planning/phases/99-arabic-coverage/rulings/RULING-P99-105-DYNAMIC-GATE-REPAIR.md
@frontend/src/lib/dossier-type-guards.ts
@frontend/src/components/empty-states/ListEmptyState.tsx
@frontend/src/components/relationships/AdvancedGraphVisualization.tsx
</context>

<tasks>
<task type="auto">
  <name>repair the ruled dynamic key population</name>
  <files>the paths in files_modified</files>
  <read_first>RULING-P99-105 and the complete P99-48 pre-repair rows</read_first>
  <action>
    Add only the measured list leaves in both locales. Repair ClusterNode so its production input is
    bounded by DOSSIER_CARD_TYPES plus a localized unknown sentinel rather than passed as an unprefixed
    graph key. Preserve the fallback argument for the ordered deletion lane. Run P99-48's self-check
    before its live profile and record both outputs verbatim; any unclassified or missing row stops.
  </action>
  <acceptance_criteria>Every acceptance item above passes, with no fallback deletion and no path outside files_modified.</acceptance_criteria>
  <done>Every ruled list and lane-3 nonliteral key domain resolves to a concrete leaf in en and ar before deletion is reachable.</done>
</task>
</tasks>

<verification>
POPULATION: one production routing site, the two named resource namespaces in both locales, and the
SUMMARY. OUTSIDE IT: the instrument (P99-48), every fallback deletion (P99-31 through P99-38), and tests.
</verification>

<success_criteria>
Establish bilingual leaf resolution for the ruled lane-2 and lane-3 dynamic domains before any fallback is deleted.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-49-SUMMARY.md` with the exact added-key inventory,
cluster-domain cases, controlled audit output, and a diff statement proving no fallback was deleted.
</output>
