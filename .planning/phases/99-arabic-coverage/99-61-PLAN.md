---
phase: 99-arabic-coverage
plan: 61
type: execute
wave: 10
depends_on: [99-50]
files_modified:
  - 'frontend/src/i18n/{en,ar}/intake.json'
  - .planning/phases/99-arabic-coverage/99-61-SUMMARY.md
autonomous: true
requirements: [AR-04a]
success_criterion: 'Add the one missing reachable intake queue-status key to both locales, proven by the acceptance items below in a single pass.'
routing:
  floor: frontier
truths:
  - "Decisions covered — RULING-P99-465: the ordered four-key population was harvested from DELETED defaultValue fallback strings, not from the reachable value set, and a fallback string can name a value the code never produces. Re-derived from the type unions and the call sites, exactly ONE of the four is a real gap. D-06: every zero is paired with a positive control in the same run. D-24: this task is additive and sits behind the same verification-only gatekeeper as the deletion lanes; adding a reachable key cannot break the gatekeeper's proven repo-wide strict zero. D-20: this is a locale ADDITION, not a key rewrite — no source t() call is touched by this task."
must_haves:
  truths:
    - 'THE POPULATION IS EXACTLY ONE KEY: `queue.status.draft`, added to BOTH `frontend/src/i18n/en/intake.json` and `frontend/src/i18n/ar/intake.json`. It is real: `queue.status.${ticket.status}` is built by TEMPLATE INTERPOLATION from a ticket''s status value at two intake call sites (both named in <interfaces>), so its reachable value set is the `TicketStatus` union, which has EIGHT members in both of its declarations (`VALID_TICKET_STATUSES` in components/unified-kanban/utils/status-transitions.ts and `type TicketStatus` in types/intake.ts); each locale carries SEVEN. `draft` is the difference, absent from en AND ar. The criterion is that positive arithmetic — union 8 against locale 8 after the add — never an absence grep.'
    - 'THE OTHER THREE ORDERED KEYS ARE STRUCK AND MUST NOT BE WRITTEN (RULING-P99-465). They are named one by one in <interfaces> below, each with the evidence that struck it: two of them ALREADY EXIST in both locales under their real namespaces, and the third names a value that is in no union feeding any queue key in this repo. Writing any of the three would satisfy a criterion, turn a gate green, and leave the product carrying dead keys forever — the Phase-94 fabrication class at locale-key granularity. Do NOT add them, and do NOT queue them as backlog: two are already satisfied and the third is not a thing. The second oracle enforces this positively rather than by inspection: exactly TWO leaf keys may differ across the task range, so a fourth key written anywhere in these bundles FAILS.'
    - 'NO SOURCE FILE CHANGES. This task touches the two intake locale bundles and its SUMMARY, nothing else. It does not delete, restore or edit any `t()` call, any fallback argument, or any component — those belong to the deletion lanes. In particular it MUST NOT restore an out-of-population fallback to hide a miss: the point of this task is that the miss is REPAIRED at the locale, so the deletion lanes can delete the fallback without a bare key reaching a user.'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; node "$R/scripts/i18n-queue-status-coverage.mjs" "$R"'
      text: 'the committed coverage probe exits 0: it re-derives the TicketStatus union from BOTH declarations and refuses to run if they disagree, then asserts each locale key set is SET-EQUAL to that union — so a missing reachable key and an unreachable extra key both FAIL — and asserts the ar value is Arabic script rather than an English string copied across. The union size it prints is the positive control: a zero-length union exits 3 as INSTRUMENT-CANNOT-RUN rather than passing vacuously. RED at HEAD: exit 1, union=8, en=7 ar=7, missing=[draft] in both locales.'
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; cd "$R" || { echo "INSTRUMENT-CANNOT-RUN: repo root unreadable"; exit 3; }; BR=$(git -C "$R" rev-parse --abbrev-ref HEAD) || { echo "INSTRUMENT-CANNOT-RUN: cannot read the current branch"; exit 3; }; RUNBR="${BR%--*}"; test "$RUNBR" != "$BR" || { echo "INSTRUMENT-CANNOT-RUN: branch $BR is not a tickmarkr task branch, so the task base cannot be derived; refusing to compare against an assumed base"; exit 3; }; git -C "$R" rev-parse --verify --quiet "$RUNBR" >/dev/null || { echo "INSTRUMENT-CANNOT-RUN: run branch $RUNBR not found"; exit 3; }; TASKBASE=$(git -C "$R" merge-base HEAD "$RUNBR") || { echo "INSTRUMENT-CANNOT-RUN: merge-base failed"; exit 3; }; case "$TASKBASE" in "") echo "INSTRUMENT-CANNOT-RUN: derived an empty task base"; exit 3;; esac; SRC=$(git -C "$R" diff --name-only "$TASKBASE"..HEAD -- . ":(exclude)frontend/src/i18n/en/intake.json" ":(exclude)frontend/src/i18n/ar/intake.json" ":(exclude).planning") || { echo "INSTRUMENT-CANNOT-RUN: git diff over the task range failed"; exit 3; }; NS=$(printf "%s" "$SRC" | command grep -c . || true); echo "task-base=$TASKBASE non-locale-files-changed=$NS"; test "$NS" -eq 0 || { echo "FAIL: this task changed $NS file(s) outside the two locale bundles: $SRC"; exit 1; }; CK=$(node "$R/scripts/i18n-queue-status-coverage.mjs" "$R" --changed-keys "$TASKBASE") || { echo "INSTRUMENT-CANNOT-RUN: the changed-keys probe exited nonzero, so its key list cannot be trusted"; exit 3; }; echo "$CK"; test "$CK" = "changed-leaf-keys=2 ar:queue.status.draft en:queue.status.draft" || { echo "FAIL: expected exactly the two draft leaf keys and nothing else, got: $CK"; exit 1; }'
      text: 'the diff adds EXACTLY the two draft leaf keys and nothing else: no file outside the two locale bundles changed, and the leaf-key differ set across the task range is exactly {ar:queue.status.draft, en:queue.status.draft}. This is the anti-fabrication guard — writing any of the three struck keys, or editing any existing string, makes this set larger and FAILS. RED at HEAD (the keys do not exist, so the set is empty, not the expected pair).'
---

<objective>
Add the one missing reachable intake queue-status key to both locales, proven by the acceptance items
below in a single pass.

This task exists because the deletion lanes remove the English `defaultValue` fallbacks that were
MASKING this gap. Deleting the mask without repairing the locale makes `draft` render as a bare key
to a user; repairing the locale first makes the deletion safe. It is ordered BEFORE mask deletion
lane 6 part 1 (99-36), which owns TicketDetail.tsx and Organizations.tsx — the two files whose
fallbacks hide this key today.
</objective>

<context>
@.planning/phases/99-arabic-coverage/99-CONTEXT.md
@.tickmarkr/overseer/RULING-P99-465-I18N-POPULATION.md
@scripts/i18n-queue-status-coverage.mjs

Ref notes: RULING-P99-465 — why the ordered four-key population was wrong and which single key survived
re-derivation; 99-CONTEXT.md — D-06, D-20, D-24; i18n-queue-status-coverage.mjs — the committed probe
that is this task's closing predicate. The probe is NOT in files_modified: it is the grader, and the
graded does not edit the grader.

<interfaces>
Derived on disk 2026-08-29 at 0b0370e58a25:

- `TicketStatus` = draft, submitted, triaged, assigned, in_progress, converted, closed, merged (8).
  Declared TWICE — `VALID_TICKET_STATUSES` (status-transitions.ts:24) and `type TicketStatus`
  (types/intake.ts:13). The probe refuses to run if they disagree.
- `queue.status` in each locale carries the other SEVEN. Only `draft` is absent, from both.
- House style for the value: en uses the plain label ("Submitted", "Closed"); ar uses the existing
  bundle's vocabulary, which already renders draft as **مسودة** at `actions.saveDraft`
  ("حفظ كمسودة"). en "Draft" / ar "مسودة" is consistent with both.
- The two call sites that build the key are IntakeQueue.tsx:492 and TicketDetail.tsx:143; both
  interpolate the ticket status value, neither can be reached with a status outside the union.
- THE THREE STRUCK KEYS, and why each was struck (RULING-P99-465):
  - `queue.priority.critical` — MISFILED. `critical` is an `Urgency` value, not a priority one, and
    `queue.urgency.critical` already exists in BOTH locales ("Critical" / "حرج") with two live readers.
  - `queue.requestType.dossier` — MISFILED, and that namespace has zero readers anywhere. The real
    reader is `queue.types.*` in **assignments.json**, where `dossier` already exists in BOTH locales
    ("Dossier" / "دوسيه").
  - `queue.priority.normal` — FABRICATED. `normal` is in no union that feeds any queue key here.
  Do not add them. Do not queue them. Two are already satisfied and the third is not a thing.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>the one reachable intake queue-status key</name>
  <files>the paths in files_modified</files>
  <read_first>RULING-P99-465, then the must_haves above, then scripts/i18n-queue-status-coverage.mjs so you know exactly what the closing predicate asserts</read_first>
  <action>
    1. Re-derive the population before relying on it (D-04): run the probe and record its verbatim
       output, which prints the union, both locale key sets, and the missing set.
    2. Add `draft` to `queue.status` in BOTH `frontend/src/i18n/en/intake.json` and
       `frontend/src/i18n/ar/intake.json`. One key per file. Preserve each file's existing key order
       convention, indentation and trailing newline; change no other string.
    3. Add NOTHING else. The other three ordered keys are struck — see must_haves. If you believe a
       fourth key is missing, STOP and record it in the SUMMARY rather than writing it.
    4. Re-run both oracles and record their verbatim output.
  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes. The coverage probe exits 0 with union=8, en=8, ar=8, missing=0, extra=0. The changed-keys probe reports exactly the two draft leaf keys.</acceptance_criteria>
  <done>Every value the `TicketStatus` union can produce has a translation in both locales, so a deletion lane can remove the English fallback at these sites without a bare key reaching a user.</done>
</task>

</tasks>

<verification>
POPULATION: the two intake locale bundles, and within them the single leaf key `queue.status.draft`.
OUTSIDE IT: every source file (this task changes none), every other locale bundle and namespace, the
three struck keys of RULING-P99-465, the deletion lanes' `t()` sites, and every other coverage gap in
the repo — a gap found here that is not this key is RECORDED in the SUMMARY, never fixed in place.

The probe at scripts/i18n-queue-status-coverage.mjs is the grader and is NOT in this task's surface.
Editing it is a scope violation, not a repair.
</verification>

<success_criteria>
Add the one missing reachable intake queue-status key to both locales, proven by the acceptance items
below in a single pass.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-61-SUMMARY.md` when done. It MUST record: both oracle
commands with verbatim output; the union and both locale key sets before and after; the explicit
statement that exactly two leaf keys changed and no source file did; and, if any other coverage gap
was observed, the gap and the fact that it was left alone.
</output>
