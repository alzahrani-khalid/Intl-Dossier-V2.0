---
phase: 99-arabic-coverage
plan: 06
type: execute
wave: 1
depends_on: [99-05]
files_modified:
  - 'scripts/{i18n-audit-strict.mjs,nav-title-agreement.mjs,glossary-census.mjs,glossary-senses.d/.gitkeep,glossary-senses.json}'
  - .planning/phases/99-arabic-coverage/99-06-SUMMARY.md
autonomous: true
requirements: [AR-01, AR-04a, AR-04b]
success_criterion: 'Land the drilled glossary census for the static instruments, proven by the acceptance items below in a single pass.'
routing:
  floor: frontier
truths:
  - "Decisions covered — D-08: every instrument authored here is run against the unfixed tree and observed RED before any repair plan runs; D-22: the strict instrument must FIND the delta the loose model hides (re-derived 2026-08-18 at 9ffa2b80c: 9 two-arg + 42 raw-key sites) — an instrument that cannot see that delta is not strict; D-17/D-18: the glossary census enumerates EVERY ruled row with its sense exceptions (ملف for files/profiles, إحاطة for briefing sessions, منصب for office) and classifies every occurrence — a blanket substring rewrite is D-17's own named destructive class and the census exists to make one detectable; D-06: every zero-assertion is paired with a positive control in the same run; D-10: the strict candidate model is the SHIPPED app's — no fallbackNS, no defaultNS, translation aliased to common; D-04: every number the instruments print is re-derived at run time, never carried from this plan."
must_haves:
  truths:
    - "The 99-02-SUMMARY records each instrument RED against the unfixed tree with its command and observed output: the strict audit's live delta vs the committed loose audit (>=9 two-arg + >=42 raw-key hidden), the nav checker's mismatch and missing-key lists, and the glossary census's per-row unclassified counts"
    - "NOTHING outside the five listed files changes — the committed loose audit scripts/i18n-mask-audit.mjs stays byte-identical as P98's instrument of record (D-22 permits a strict variant BESIDE it), no source repair, no i18n JSON edit"
    - oracle: command
      command: 'PATH="/opt/homebrew/bin:$PATH"; R="$PWD"; test -f "$R/scripts/glossary-senses.json" && test -d "$R/scripts/glossary-senses.d" && node "$R/scripts/glossary-census.mjs" "$R" --control && node "$R/scripts/glossary-census.mjs" "$R" --census | command grep -qE "ارتباط" && ! node "$R/scripts/glossary-census.mjs" "$R"'
      text: 'the glossary census exists with its base sense allowlist and its overlay directory, catches its planted banned occurrence (control), prints a census naming the ارتباط row (positive control that the walk reaches ar values at all), and is RED on the unfixed tree with unclassified occurrences outstanding — RED at HEAD (the file does not exist)'
---

<objective>
Land the drilled glossary census for the static instruments, proven by the acceptance items below in a single pass.

This task is part 3 of 3 of the static instruments; the parts are ordered by `depends_on` and share one
file surface, so each part reads the tree the part before it produced. The task-unit contract caps a
task at six acceptance items and twenty-four surface, which is why the lane is cut this way.
</objective>

<context>
@.planning/phases/99-arabic-coverage/99-CONTEXT.md
@.planning/phases/99-arabic-coverage/99-RESEARCH.md
@scripts/i18n-mask-audit.mjs
@frontend/src/i18n/index.ts
@frontend/src/components/modern-nav/navigationData.ts

Ref notes: 99-CONTEXT.md — D-04..D-10, D-16..D-18, D-22; 99-RESEARCH.md — §1.3 the strict-model gap; §4.1-§4.3 the nav anchors and the glossary census with its sense-pollution notes; i18n-mask-audit.mjs — the committed LOOSE audit — model to extend, file NOT edited; i18n/index.ts — confirm no fallbackNS/defaultNS and find the translation->common alias registrations; navigationData.ts — 28 NavigationItems, labelKey/label/path

<interfaces>
Verified on disk 2026-08-18 at 9ffa2b80c (re-derive before relying on any number):

- The committed audit's matcher `/\bt\(\s*'([^']+)'\s*,\s*'([^']*)'/g` runs over WHOLE FILE TEXT,
  so `\s*` crosses newlines and it sees wrapped calls. The AR-04a acceptance grep is line-bound
  and does not. Re-derived at this sha, three populations of one class:
  `1680` (unanchored line-bound grep) / `1657` (word-bounded line-bound grep) /
  `1768` (`total_two_arg_sites`, the committed cross-line matcher). The 23-site gap between 1680
  and 1657 is identifier tails — `storage.set(...)`, `params.set(...)`, `formatDayFirst(...)`,
  `.not(...)` — and the 111-site gap between 1657 and 1768 is calls spanning a newline. **The
  strict instrument's `twoArgTotal` is the population; the grep is a diagnostic.**
- Loose-audit gap (re-derived, strict scratch model): the committed audit appends `common` to
  every candidate list and lacks the `translation`->common alias; strict two-arg misses 481
  (loose 473; 9 hidden), strict raw-key misses 335 (loose 297; 42 hidden). The shipped init
  declares NO fallbackNS and NO defaultNS; `translation` is aliased to common at its resource
  registration sites (search `translation:` in `frontend/src/i18n/index.ts`).
- Nav pairs: `navigationData.ts` labelKey values `navigation.*` resolve against `common.json`;
  missing in BOTH locales at HEAD: `navigation.dashboardOverview`, `navigation.taskQueue`,
  `navigation.taskEscalations`, plus tooltip `navigation.workflow`. The 13 derived title anchors
  and verdicts are the table in 99-RESEARCH.md §4.2 (7 MISMATCH, 6 match) — encode those rows
  exactly as derived; rows for the 15 not-yet-derived nav targets are NOT invented here, the
  glossary lane walks and appends them.
- Glossary census populations re-derived 2026-08-18 at 9ffa2b80c over `frontend/src/i18n/ar/*.json`
  (occurrence count / line count / file count — two of these are different populations and both
  are stated, per D-05):
  `ارتباط` 171/171/32 · `موجز` 128/124/28 · `إحاطة` 65/65/16 · `منصب` 53/53/18 ·
  `مشارك` 239/235/58 · `ملف` 578/558/87 · `دوسيه` 199/199/8 · `دوسييه` 2/2/1 ·
  `الملف الشخصي` 11/11/6 · `تطوير المنصب` 1/1/1 · `قائمة الانتظار` 13/13/3 ·
  `قائمة الاستقبال` 2/2/1.
  Commands on record in 99-REVISION-1.md §Derivations. `قائمة الاستقبال` already exists at HEAD
  in `ar/common.json` (the nav label) — the defect is that `ar/intake.json`'s TITLE does not use
  it, so a census clause keyed on mere presence is a keep-true guard, not a discriminator.
- The census's job is CLASSIFICATION, not verdicts on Arabic: it says "this occurrence of ملف is
  at a key path nobody has declared a file/profile sense for", and a human or a sweep lane
  resolves it by either rewriting the value or adding the path to the allowlist with a reason.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>the static instruments — the drilled glossary census</name>
  <files>the paths in files_modified</files>
  <read_first>scripts/i18n-mask-audit.mjs in full (regexes, file walk, namespace model — mirror the walk byte-for-byte, change ONLY the candidate model); frontend/src/i18n/index.ts; 99-RESEARCH.md §1.3 ; navigationData.ts; 99-RESEARCH.md §4.1-§4.4; the D-16/D-17/D-18 rows in 99-CONTEXT.md; the ruled tie-breaks in .planning/phases/99-arabic-coverage/rulings/RULING-P99-05-COVERAGE-TIEBREAKS-PRECOMMITS.md §3</read_first>
  <action>
    Parts before this one landed in P99-05. Do only what THIS task's acceptance items still require, using the lane method below as the standing method; re-run this task's oracles against the tree the earlier part produced and repair any residue INSIDE files_modified. Author nothing outside them.

    --- THE LANE METHOD (carried verbatim from the pre-recut plan; the acceptance items above bound what THIS task owes) ---

    Same regexes and walk as the committed audit, candidate model STRICT — declared namespaces
    only, no appended common, translation aliased to common (D-10) — both locales, both classes,
    `--json` emitting at minimum {twoArgUnresolved, rawKeyUnresolved, twoArgUnresolvedAr,
    rawKeyUnresolvedAr, twoArgTotal, rawKeyTotal, sites:[...]}, and `--scope <path-prefixes>`.
    `--self-check` runs the model against an EMBEDDED fixture proving (a) it flags a site the loose
    model would call resolved, (b) it does NOT flag a site resolving through the translation alias,
    (c) it counts a WRAPPED `t('k',\n '  Default')` site in twoArgTotal, (d) it counts a one-line
    site in twoArgTotal, (e) both locales checked — exits nonzero if any leg fails. The fixture
    keeps the control valid after the phase's repairs land; the live delta is recorded in the
    SUMMARY, never hardcoded in the check. The committed loose audit is NOT edited.



    1. nav-title-agreement.mjs: the 13 §4.2 rows as data {labelKey, titleNamespace, titleKey,
       ruledTerm?}; ar-side resolution for both anchors; exits nonzero listing mismatches and
       missing keys; `--control` plants a synthetic mismatching row and exits zero only if caught.
       Encode the criterion semantics the overseer clarified: a pair AGREES when nav and title
       carry the same OBJECT TERM, sense-consistent — NOT byte-equality (byte-equality would
       indict EN's own "Positions" -> "Positions Library"). A row therefore declares its object
       term and the checker asserts both anchors carry it.
    2. glossary-census.mjs: rows for every ruled D-17/D-18 term pair; walk every ar/*.json leaf
       value; classify each occurrence of each competing term as ruled-term / allowlisted-sense /
       UNCLASSIFIED; nonzero while any UNCLASSIFIED survives, printing file:keypath:value.
       `--census` prints per-row counts (the before/after the sweep lanes record). `--control`
       plants a banned occurrence and exits zero only if caught. NO rewriting — ever.
    3. glossary-senses.json (the base) and the empty `scripts/glossary-senses.d/` overlay
       directory the census globs and merges over it (later-lane rows win only by adding, never by
       removing a base row — a removal is a red): seed the base with the already-ruled exceptions — the الملف الشخصي and
       مرفق ملف families for ملف, إحاطة for briefing sessions, منصب for office/post, and the two
       دوسييه brand-mark values in ar/common.json (the shell appName and the logo-mark alt).
       Each entry carries {term, keyPathPattern, sense, reason}.

  </action>
  <acceptance_criteria>Every acceptance item above is satisfied with its command output recorded verbatim, and no path outside files_modified changes. The lane's own criteria, carried: The fixture self-check passes with the wrapped control among its legs; the live run reports unresolved populations consistent with the recorded strict re-derivation (~481 two-arg / ~335 raw-key — re-derive, record what you got, both locales) and a twoArgTotal in the 1768 family; the delta vs the committed loose audit (>=9 two-arg + >=42 raw-key hidden) is computed and recorded (D-22's required control). The nav checker's planted control is caught AND its live run exits nonzero naming its mismatches and missing keys; the census's planted control is caught, its --census names every ruled row with re-derived counts, and its live run exits nonzero with the unclassified population printed; the seeded allowlist covers exactly the exceptions ruled so far and no more.</acceptance_criteria>
  <done>Criterion 1 has a drilled instrument covering every ruled row rather than three spot values, and the nav checker can fail.</done>
</task>

</tasks>

<verification>
POPULATION: the paths in files_modified. OUTSIDE IT: every other task's files, the exogenous paths
(D-03), and every obligation this task's acceptance items do not name — those belong to the lane
parts before and after this one, named in `depends_on` and in the SUMMARY.

The lane's own population statement, carried verbatim from the pre-recut plan:
POPULATION: the six listed files only. OUTSIDE IT: the rendered specs (99-01 owns them), every
repair, the committed loose audit (stays as P98's instrument of record), and every i18n JSON —
this plan READS the ar bundles and WRITES none of them.
</verification>

<success_criteria>
Land the drilled glossary census for the static instruments, proven by the acceptance items below in a single pass.
</success_criteria>

<output>
Create `.planning/phases/99-arabic-coverage/99-06-SUMMARY.md` when done. It MUST record every command this task ran with its verbatim output, the
populations it re-derived with the commands that produced them, and anything left for a named later
task. The lane's own SUMMARY contract, carried verbatim from the pre-recut plan (satisfy the parts
this task owns): record in the file named under <output> (99-02-SUMMARY.md in the pre-recut plan) when done. It MUST record: per
instrument — the command, the observed RED output, and the control result; the strict-vs-loose
delta numbers with the command; the nav checker's mismatch + missing-key lists; the glossary
census per-row counts and unclassified totals; and the statement that nothing outside the six
files changed.
</output>
