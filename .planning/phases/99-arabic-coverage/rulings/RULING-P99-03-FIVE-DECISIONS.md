> **WORKER-REACHABLE COPY.** Authoritative original: `.tickmarkr/overseer/RULING-P99-03-FIVE-DECISIONS.md`, which is
> gitignored and unreachable from a task worktree. Copied here 2026-08-19 so the tracked files that
> cite this ruling resolve. Verbatim at copy time; if the two differ, the `.tickmarkr` original governs.

# RULING P99-03 — the five §7 decisions; planner released on 99-CONTEXT confirmation

2026-08-18, OVERSEER (wK:p7X). Inputs: `P99-DECISION-REQUEST-01.md` (REQUEST-END),
`99-RESEARCH.md` §7+§8 read whole at sha1 `200c4d1ef34e8fccd7136c829be0e1743b671dc3`.
`RULING-P99-01` §3 condition 1 confirmed satisfied (UI99-C1..C11 in the ruled form).

## #1 — FLATTEN (Reading A). Conditions:

> **⚠ SUPERSEDED-BASIS (2026-08-18, `RULING-P99-06`, checker B B-01):** the "proven
> collision-free (67 leaves, 0 overwrites)" basis below is FALSE — the collision instrument
> compared child key sets and was structurally blind to the two scalar-vs-object TYPE clashes
> (`common.error`, `common.search`, both locales). The FLATTEN decision STANDS; the merge
> mechanics are repaired by ruled relocation (`error.label` / `search.label` + 13 consumer
> repoints). Read `RULING-P99-06` before acting on anything in this section.

a. §2.5 MANDATORY SEQUENCING holds absolutely: the flatten lands and is PROVEN before any
dot→colon conversion of any kind runs anywhere in the phase.
b. One atomic engine task: both locales' `common.json` + the 120-site/46-file `common.X`→
`common:X` rewrite in one commit, whose `command:` oracle runs the COMMITTED resolution
harness (`scripts/resolve-check.mjs`) before AND after in the same gate, plus
`scripts/neg-taskcard.mjs` — run the negative control or the harness is unfalsifiable.
c. UI99-C5's 404 repair is expressed relative to the POST-FLATTEN key shape (it repoints onto
`common.notFound.*`, which lives in the nested subtree). No plan hardcodes the pre-flatten shape.

Basis: merge proven collision-free (67 leaves, 0 overwrites, both locales); 33/72 colon-misses
self-repair; KEEP's erosion failure mode is the destructive conversion `RULING-P98A2-11` exists
to prevent, made permanent. FLATTEN's failure is bounded by a committed instrument; KEEP's is
unbounded in time.

## #2 — Glossary: all four rows RULED, as the delegated product call, recorded with cheap reversal

- **d) queue collision — DEFECT, not taste:** intake = `قائمة الاستقبال` (nav already says it);
  waiting = `قائمة الانتظار`. Two surfaces sharing one Arabic name is a correctness failure under
  criterion 1's own nav-label↔page-title clause. The موقف/منصب sense split (§4.4) also stands:
  Position-as-stance = موقف; the F18 `تطوير المنصب` wrong-sense instance repairs to the موقف family.
- **a) dossier = `دوسيه`** for the OBJECT; `ملف` remains legal for files/profiles. **The sweep is
  therefore SENSE-AWARE by construction, never mechanical** — a mechanical ملف→دوسيه rewrite is
  the same destructive-conversion class as the nested-common inversion. Criterion 1's own text
  anticipated this row ("dossier is one word, not دوسيه / ملف / دوسييه").
- **b) engagement = `مشاركة`** — the requirement text itself reads الارتباطات → المشاركات;
  ambiguity cost accepted and recorded.
- **c) brief = `ملخص`** for the brief ARTIFACT; `إحاطة` reserved for briefing SESSIONS; `موجز`
  swept. The artifact/session split is glossary law, stated in AR-01's table.

**Operator visibility, stated here and in my report:** these are Arabic product-voice calls made
under the standing delegation because waiting has a measured cost and reversal is cheap — a term
swap is a mechanical leaf-rename plus a parity re-run, and the operator may override ANY row up
to phase close at exactly that cost. The seat's recommendations were adopted only after reading
§4.3/§7's evidence for each; none was adopted on taste alone.

## #3 — SPLIT (seat's recommendation): re-bind the top ~10 files, dump the long tail

Re-bind the §1.5 heavy files (TriagePanel, Countries, Organizations, TicketDetail, …) to feature
namespaces; the long tail authors into `common`. Condition: the re-bind set is ENUMERATED in the
plan from §1.5's table (named files), never carried as a bare count; the count is re-derived at
plan time, not quoted.

## #4 — GST: CARVE OUT (Reading A), onto UI99-C9's allowlist

Basis is stronger than analogy: the project's own design spec (CLAUDE.md, dates/times table)
mandates `14:30 GST` as the product's time format — the criterion names weekday/month names and
`GST` is neither. Same class as `RULING-P98A2-17`'s SLA `T±N` mono tokens. Revisit trigger, as
the seat wrote it: the operator's Arabic review objecting. The conditional UI99-C9 row resolves
to: allowlisted.

## #5 — READING B (behavioral). AR-04b's acceptance for Phase 99 means:

**What the phase must FIX:** every `t()` site where the mask+dot conjunction or the unresolved
tail makes a missing Arabic key render English (or a raw key) — the STRICT-model populations
(§2.2's 335 raw-key sites + the strict two-arg mask population; every number re-derived at plan
time, none frozen). Already-resolving dot-form keys inside their bound namespace are working
code; converting them is churn with proven breakage risk (§2.3) and zero behavioral delta.

**Conditions:**
a. **The acceptance instrument goes STRICT.** `i18n-mask-audit.mjs`'s namespace model is looser
than the shipped app (no `fallbackNS`/`defaultNS` — verified by the orchestrator AND by the
seat, mechanism confirmed). The phase repairs the committed audit (or ships a strict variant
beside it) so acceptance check (b) counts what the app actually does. POSITIVE CONTROL
REQUIRED: the strict instrument must FIND the 9 two-arg + 42 raw-key sites the loose model
hides — that delta set is the control; an instrument that cannot see it is not strict.
b. **Direction of travel preserved:** every binding this phase TOUCHES (new keys, re-binds,
repairs) lands in explicit colon form. The convention advances through every repaired site
without churning working ones.
c. **Reading A is NOT owed and is not silently dropped:** the remaining working dot-form tail is
a recorded NO-SHIP-THIS-PHASE with its condition — if the operator wants the convention landed
wholesale, that is new scope, queued explicitly (candidate: the Phase 102 debt tail), sequenced
after #1's flatten, and instrumented by resolution checks per §2.3's proof.

Basis: the ROADMAP success criterion (behavioral wording) is what must be TRUE for the phase; the
requirement row's convention wording describes the mechanism for repaired sites, not a mandate to
rewrite 7,086 working ones. The milestone is Trust & Correctness; the 20× larger reading buys no
additional truth.

## Record corrections executed with this ruling (my column)

- `REQUIREMENTS.md` AR-04b dated note: units mislabel corrected in place (306 = SITES, 309−3 FPs,
  280 distinct keys; 353 = distinct keys), attribution dated.
- `98-CLOSING-DERIVATION.md:377`: the false tree-claim "there is no `chromium-ar` project" scoped
  to what was true (P98's runs), with `playwright.config.ts:40` `chromium-ar-smoke` cited.

## Process

- Assemble `99-CONTEXT.md` as D-NN decisions from this ruling; bring it to me for confirmation
  BEFORE the planner consumes it (as you proposed).
- Keep `p99-research`'s pane open until 99-CONTEXT.md is confirmed; then close it (deliverable on
  disk, marker verified, sha pinned).
- Planner then dispatches on the held brief; the two independent checks run codex `gpt-5.6-sol`
  FIRST per SO-9.

RULING-END
