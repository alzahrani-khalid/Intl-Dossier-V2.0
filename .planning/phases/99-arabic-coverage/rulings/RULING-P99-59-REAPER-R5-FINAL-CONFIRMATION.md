# RULING-P99-59 — final static confirmation of R5 candidate

**Decision:** Release the R5 frozen candidate to the original Grok and Codex reviewer lineages. No source edit, commit, rendered run, standalone gate, compile, or Phase-99 run is released.

## Review authority

Use only the R5 hashes in `.tickmarkr/overseer/P99R-R5-REPAIR-HANDOFF.md` §3. Re-hash before reading and after finishing; mismatch is `HEAD-MOVED`.

## Common checks

Both reviewers must:

- re-run the four-locale 105/105 matrix, 21-row failure-shape/path matrix, 15-row argv matrix, and zero-residue checks;
- confirm the entry-body proof markers establish the spawned child actually executed;
- verify the plans remain the R4 one-line `;` → `&&` edits and all actual wrapper callers use fail-closed composition;
- state explicitly whether any prior F1–F7/N1/N2/R3/R4 repair regressed.

Frame the review as completeness: enumerate every input each safety decision depends on, confirm each is bound, and state what remains unestablished. Do not ask models to develop bypasses.

## Grok assignment

Confirm strict start parsing, valid-hex causal regressions, lease filename/body/full-tuple revalidation after census and before every signal/unlink, late-foreign report quarantine with actual `&&` composition, and corrected criteria text. Examine the implementer's residuals:

- whether the 21-row reinterpretation proves the same parent/child exit contract;
- whether a foreign final appearing on the clean path before publish can destroy or misattribute evidence;
- whether the strict calendar parser accepts impossible values outside the tested classes.

Write `.tickmarkr/overseer/P99R-VERIFY-R5-REPORT.md`, ending `REAPER-VERIFY-R5-END`.

## Codex assignment

Confirm the three R4 material gaps are closed: semantic start validity, post-census immediate lease authority, and supported `&&` consumer safety. Verify criteria no longer claim universal semicolon support. Re-run the same matrices and report any prior repair regression. If provider policy limits a construction, record the untested input and evidence already obtained; do not rephrase around the restriction.

Write `.tickmarkr/overseer/CONSULT-REAPER-SAFETY-R6-REPORT.md`, ending `REAPER-VERDICT-R6-END`.

Both seats are read-only and may not start a rendered Playwright stack or run tickmarkr.

## Completion

Both markers, stable hashes, and writer exits are required. The orchestrator writes `.tickmarkr/overseer/P99R-R5-REVIEW-HANDOFF.md`, ending `ORCH-REAPER-R5-REVIEW-END`, with an explicit yes/no: **is any static source repair still required for the supported repository callers?** Stop for OVERSEER ruling.

Only a no-material-defect result from both reviewers releases the bounded real rendered run.
