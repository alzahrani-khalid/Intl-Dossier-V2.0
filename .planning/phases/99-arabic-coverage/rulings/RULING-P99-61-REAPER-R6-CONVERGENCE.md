# RULING-P99-61 — R6 convergence confirmation

**Decision:** Release the R6 frozen candidate to the original Grok and Codex reviewer lineages for one convergence check. No source edit, commit, rendered run, standalone gate, compile, or Phase-99 run is released.

## Authority

Review only the hashes in `.tickmarkr/overseer/P99R-R6-REPAIR-HANDOFF.md` §3. Re-hash before and after; mismatch is `HEAD-MOVED`.

## Common checks

Both reviewers must re-run:

- four-locale 118/118 matrix;
- 21-row failure-shape/path matrix and 15-row argv matrix;
- all clean/unclean real `&&` composition rows, with consumer call counts;
- zero-residue checks;
- the three R4 plan composition lines and all ten executing wrapper routes.

State explicitly whether any prior F1–F7/N1/N2/R3/R4/R5 mechanism regressed.

## Grok

Enumerate and confirm every input for:

1. no-overwrite publish and pending/final provenance;
2. strict canonical/attainable start identity;
3. full lease authority including SID and wrapper pair at every round/unlink;
4. direct Playwright group identity and independent final-zero proof;
5. port-holder PID plus birth identity;
6. criteria/header agreement with actual supported callers.

Evaluate the handoff limits against the supported environments: the pending and final report are in the same directory, so cross-filesystem publication is not part of the runtime contract. Write `.tickmarkr/overseer/P99R-VERIFY-R6-REPORT.md`, ending `REAPER-VERIFY-R6-END`.

## Codex

Re-confirm the clean-path provenance defect and its five unbound inputs from R5 as repaired/partial/not repaired. Verify missing pending, pre-existing final, late final, failed publish, clean publish, reused group/port identities, and mutated SID/wrapper fields. If provider policy limits a construction, record the untested input and evidence obtained; do not rephrase around the restriction.

Write `.tickmarkr/overseer/CONSULT-REAPER-SAFETY-R7-REPORT.md`, ending `REAPER-VERDICT-R7-END`.

Both seats are read-only. No rendered Playwright stack and no tickmarkr.

## Completion

Both markers, stable hashes, and writer exits are required. The orchestrator writes `.tickmarkr/overseer/P99R-R6-REVIEW-HANDOFF.md`, ending `ORCH-REAPER-R6-REVIEW-END`, with an explicit yes/no: **is any material static defect still open for the supported repository callers and filesystems?** Stop for OVERSEER ruling.

A no-material-defect result from both reviewers releases the bounded real rendered run; it does not itself release a commit or Phase-99 run.
