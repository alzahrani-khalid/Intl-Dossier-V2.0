# RULING-P99-67 — R9 final convergence confirmation

**Decision:** Release the R9 frozen candidate to the original Grok and Codex reviewer lineages. No source edit, commit, rendered run, standalone gate, compile, or Phase-99 run is released.

## Authority

Use only the R9 hashes in `.tickmarkr/overseer/P99R-R9-REPAIR-HANDOFF.md` §3. Re-hash before and after; mismatch is `HEAD-MOVED`.

## Mandatory common checks

Both reviewers must reconstruct and execute the full historical matrices on the R9 bytes:

- seven failure shapes × canonical, `/var` alias, directory symlink = 21 rows, each with entry and dispatch/body proof;
- CLI/import argv matrix = 15 rows;
- four-locale 132/132 matrix;
- zero residue.

Also verify the R9-specific rows:

- `reg28` genuinely overlaps two same-final `runMode` lifecycles and leaves A's final plus B's pending evidence untouched;
- `reg29` re-reads own PGID at the destructive boundary, refuses a changed/unavailable identity, and never evaluates later targets;
- `reg8`/`reg13` remain present and inverted, not deleted;
- all actual repository wrapper callers use `&&`.

## Grok dynamic assignment

Run the real-process and locale rows, exported escape reaper, concurrent lifecycle, and own-PGID call-order controls. State whether any prior F1–F7/N1/N2/R3–R8 mechanism regressed. Write `.tickmarkr/overseer/P99R-VERIFY-R9-REPORT.md`, ending `REAPER-VERIFY-R9-END`.

## Codex static assignment

Run under `workspace-write -a never` with orchestrator tamper control. Confirm quarantine is absent from all filesystem mutation paths, shared final evidence is never moved/deleted, nonce staging is unique, and every exported reap revalidates self identity. Run the two matrices; record `ps`-dependent rows as environment-limited rather than failed if sandbox denies them. Write `.tickmarkr/overseer/CONSULT-REAPER-SAFETY-R10-REPORT.md`, ending `REAPER-VERDICT-R10-END`.

Both seats are read-only. No rendered Playwright stack and no tickmarkr.

## Completion

Both markers, stable hashes, and writer exits are required. The orchestrator writes `.tickmarkr/overseer/P99R-R9-REVIEW-HANDOFF.md`, ending `ORCH-REAPER-R9-REVIEW-END`, answering yes/no: **is any material static or synthetic-process defect still open for supported callers?** Stop for OVERSEER ruling.

A no-material-defect result from both releases the bounded real rendered run only.
