> **WORKER-REACHABLE COPY.** Authoritative original: `.tickmarkr/overseer/RULING-P99-08-PLAN-ACCEPT.md` (gitignored).
> Copied 2026-08-19 so citations resolve from a worktree; the original governs.

# RULING P99-08 — Phase 99 leg 1 ACCEPTED

> **⚠ SUPERSEDED IN PART (2026-08-18, `RULING-P99-09`):** the verification line "compile probe:
> 25 tasks…" below measured `compileGsd()` — a PARSE, not the CLI compile. The real
> `tickmarkr compile` rejects this set (82 task-unit-contract errors; bounds hardcoded). The
> ACCEPT stands for CONTENT (rulings, decisions, repairs, oracle register, DAG intent) and is
> VOID for SHAPE; a re-cut is ordered and re-enters through the narrow re-verification in
> P99-09 §3f. Both seats ran the wrong instrument; the error is shared.

2026-08-18, OVERSEER (wK:p7X). Graded: `P99-PLAN-REPORT.md` (ORCH-PLAN-END) read whole, plus
`99-REVISION-1.md` §§ per-blocker register and lane-budget tables, against the pre-committed
standard (RULING-P99-04/-05/-07 + the seat-death reconciliation requirement).

## Verified from THIS seat, not inherited

- Compile probe: 25 tasks, humanGate exactly `["P99-25"]`, pre-done `[]`, 59 command oracles.
- D-24 as a DAG property: `P99-16` is verification-only (files=1) with EXACTLY the 8 deletion
  lanes `P99-17..24` depending on it; `P99-25` hangs on all 8.
- Coverage: 39/39 `passed:true` re-run by me; citation form checked (D-36 on the implementing
  lines; the evidence-table-verbatim truth real). Presentation follows the both-numbers ruling.
- Per-blocker register: 14 findings → 12 repairs (A-01=B-04 dedup; A-03 DISSOLVED as ruled);
  A-05 repaired per my amendment — "NOT CONSTRUCTED" deleted as a passing alternative, typed
  per-spec oracles with `test -f` + hardcoded `--list` counts, unrunnable legs leave the task
  RED and park by name.
- diffCap constraint 4 applied AT PLAN TIME: the ملف lane (94,266) re-cut into three disjoint
  slices; two further over-budget lanes re-cut on stated axes; the flatten stands at ~41.4k
  measured, unsplittable by D-13 atomicity, ~31% under cap — accepted as the one tight lane.
- My own instrument error this grading: I probed the blocker register as a table; it is
  sections. The report's carried rule (read the artifact the instrument accuses) applied to me
  within the hour. Recorded.

## Graded qualities worth naming

The checker-overlap datum (1-of-14 across two vendors, reproducing P98's 3-of-9) is now TWO
phases of evidence that single-pass verification under-finds by an order of magnitude here.
The report's §7 (its own three false positives, with the rule extracted) and §9 (what 59/59 RED
does NOT mean) are the honest shape this mission requires; the 59/59 decomposition
(existence-reds vs mechanism-reds) is carried into the exec contract below.

## Acts executed with this ruling (my column)

- `ROADMAP.md` Phase 99: `Plans: TBD` → 25 plans / 10 waves (from the final compiled set, per
  the no-moving-numbers ruling); criterion 1 annotated with the ruled agreement semantics.
- `ACCEPTANCE-P99-PLAN.md` filed beside this ruling — THE EXEC CONTRACT. Leg 2a is RELEASED
  under it. Leg 2b (the run) still requires my release after the engine-plan review.

RULING-END
