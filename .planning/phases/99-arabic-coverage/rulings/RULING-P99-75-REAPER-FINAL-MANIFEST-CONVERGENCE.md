# RULING-P99-75 — final manifest convergence confirmation

**Decision:** Release the R13 selftest/criteria candidate to Grok dynamic and Codex static confirmation. Runtime remains frozen. No source edit, commit, rendered run, standalone gate, compile, or Phase-99 run is released.

## Authority

Use `.tickmarkr/overseer/P99R-R13-REPAIR-HANDOFF.md` §3 hashes. Preserve all five evidence paths. Re-hash before and after; runtime mismatch voids the round.

## Grok dynamic assignment

Run four locales with unique manifests and immediate external checks. Require 217/217 and 194/194. Execute independently:

- real parent SIGKILL while blocked in managed spawnSync; partial manifest must contain pending/child ownership and checker must fail non-complete;
- exact cleanup of that manifest's identities;
- forced throw for every declared fixture family;
- reg35 omitted-registration RED→GREEN;
- reg37 plantSession forced-throw;
- reg38 seven controls, including schema and expected-family failures;
- EACCES/malformed manifest persistence.

Verify all five preserved evidence paths remain unchanged. Write `.tickmarkr/overseer/P99R-VERIFY-RESIDUE-R13.md`, ending `REAPER-RESIDUE-R13-VERIFY-END`.

## Codex static assignment

Under `workspace-write -a never`, confirm all spawn/spawnSync fixture paths pass through centralized ownership, all families have local `try/finally`, the bridge persists before parent blocking, schema/expected-family validation cannot be derived from observed records, and 208→217 / 129→194 are fully accounted without deleted protections. Run controls available in sandbox; mark real-process rows environment-limited.

Write `.tickmarkr/overseer/CONSULT-REAPER-RESIDUE-R13.md`, ending `REAPER-RESIDUE-R13-CONSULT-END`.

Both seats are read-only. Runtime may not change. No rendered Playwright stack and no tickmarkr.

## Completion

Both markers, stable hashes, writer exits, unchanged runtime and preserved evidence paths are required. The orchestrator writes `.tickmarkr/overseer/P99R-R13-REVIEW-HANDOFF.md`, ending `ORCH-REAPER-R13-REVIEW-END`, answering whether zero-residue ownership is structurally and dynamically complete for the selftest. Stop for OVERSEER ruling.

A two-reviewer yes releases the bounded real rendered run only.
