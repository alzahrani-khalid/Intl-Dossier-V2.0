# RULING-P99-57 — final static confirmation of R4 reaper candidate

**Decision:** Release the R4 frozen candidate to the original Grok and Codex reviewer lineages. No source edit, commit, rendered run, standalone gate, compile, or Phase-99 run is released.

## Review authority

Use only the hashes in `.tickmarkr/overseer/P99R-R4-REPAIR-HANDOFF.md` §4. Re-hash before reading and after finishing; mismatch is `HEAD-MOVED`.

## Required common checks

Both reviewers must:

- re-run the full four-locale 101/101 matrix and zero-residue checks;
- reconstruct and run the **original seven failure shapes** across canonical, `/var` alias, and directory-symlink invocation — 21 rows — because R4 changed the entry guard and the implementer's new 15-row argv matrix is complementary, not a substitute;
- run the new 15-row CLI/import argv matrix;
- confirm the three closed plans changed only `;` to `&&` and remain compiler-done;
- state whether any previously confirmed F1–F7/N1/N2/R3/R4 repair regressed.

## Grok assignment

Resume its lineage once. Regrade the six RULING-P99-56 repairs:

1. complete lease schema and filename/body nonce binding;
2. lease revalidation immediately before every signal/unlink;
3. pre-spawn port tri-state and child spawn count zero on unavailable;
4. pending/final/foreign report behavior with real command composition;
5. T11 out-of-band dummy ownership and tri-state death;
6. canonical CLI and ambiguous import behavior;
7. `PLAYWRIGHT_JSON_OUTPUT_FILE` reaching Playwright 1.60's reporter path.

Write `.tickmarkr/overseer/P99R-VERIFY-R4-REPORT.md`, ending `REAPER-VERIFY-R4-END`.

## Codex assignment

Resume its finding-owner lineage once. Re-confirm every consolidated residual from R3 plus R4-N5/N6 and the foreign-final/report-publication path. Attack malformed-but-non-empty starts, valid-hex nonce mismatch, missing wrapper identity, lease replacement mid-round, failed quarantine, and the imported-runtime unresolved argv construction. Verify the original seven failure shapes actually execute the entry body on all three paths.

Write `.tickmarkr/overseer/CONSULT-REAPER-SAFETY-R5-REPORT.md`, ending `REAPER-VERDICT-R5-END`.

Both seats are read-only and may not start a rendered Playwright stack or run tickmarkr.

## Completion

Both markers, stable hashes, and writer exits are required. The orchestrator writes `.tickmarkr/overseer/P99R-R4-REVIEW-HANDOFF.md`, ending `ORCH-REAPER-R4-REVIEW-END`, with an explicit yes/no: **is any static source repair still required?** Stop for OVERSEER ruling.

Only a no-material-defect result from both reviewers can release the bounded real rendered run. Synthetic/static confirmation never substitutes for that run.
