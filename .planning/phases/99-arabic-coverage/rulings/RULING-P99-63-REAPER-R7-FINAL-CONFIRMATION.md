# RULING-P99-63 — R7 final static confirmation

**Decision:** Accept the `reapLeakedPorts` discovery-time identity adaptation as in scope. Requiring mandatory identity while leaving the exported utility unable to act would be a regression; discovery-time `psLstart` capture plus fail-closed spare is the only available authority for an already-running orphan.

Release the R7 frozen candidate to the original Grok and Codex reviewer lineages. No source edit, commit, rendered run, standalone gate, compile, or Phase-99 run is released.

## Authority

Use only the R7 hashes in `.tickmarkr/overseer/P99R-R7-REPAIR-HANDOFF.md` §3. Re-hash before and after; mismatch is `HEAD-MOVED`.

## Grok dynamic confirmation

Run under the environment that permits real process introspection:

- four-locale 126/126 matrix and zero residue;
- same-final/two-nonce concurrency rows, including independent pending bytes and no cross-consumption;
- all direct-group identity rows: missing identity, ESRCH, same-start zombie, different-start zombie, live process, impostor, final-zero conjunction;
- the exported `reapLeakedPorts` discovery-time identity path and unverifiable-identity spare;
- both entry matrices and actual `&&` composition.

Write `.tickmarkr/overseer/P99R-VERIFY-R7-REPORT.md`, ending `REAPER-VERIFY-R7-END`.

## Codex static confirmation

Run under `workspace-write -a never` with the same tamper control; its inability to call `ps` is an evidence limit, not a candidate failure. Confirm:

- every pending-path call site is nonce-bound and same-final concurrency is causally tested;
- publication rejects a nonce/path mismatch;
- no direct-group caller can signal with missing birth identity;
- non-ESRCH resolves to gone only for same-start zombie proof;
- `reapLeakedPorts` cannot act when discovery identity is unavailable;
- no prior repair regressed statically.

Write `.tickmarkr/overseer/CONSULT-REAPER-SAFETY-R8-REPORT.md`, ending `REAPER-VERDICT-R8-END`.

Both seats are read-only. No rendered Playwright stack and no tickmarkr.

## Completion

Both markers, stable hashes, and writer exits are required. The orchestrator writes `.tickmarkr/overseer/P99R-R7-REVIEW-HANDOFF.md`, ending `ORCH-REAPER-R7-REVIEW-END`, with an explicit yes/no: **is any material static or synthetic-process defect still open for the supported repository callers?** Stop for OVERSEER ruling.

A no-material-defect result from both reviewers releases the bounded real rendered run, not a commit or Phase-99 run.
