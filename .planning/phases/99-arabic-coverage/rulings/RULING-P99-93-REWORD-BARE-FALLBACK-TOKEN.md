# RULING-P99-93 — remove the final bare camelCase token in P99-31…38

**Decision:** R92's backtick premise was false. The compiler extracts every camelCase/underscore token from rendered acceptance items regardless of formatting. Reword the final identical bare token in P99-31…P99-38.

Replace `the defaultValue grep` with `the fallback-text-option grep` in the eight lane-method instructions only. The hyphenated prose preserves meaning and is not a program identifier under the compiler's extractor.

The compiler source loop reports every violating symbol per task; CODE_EXT excludes `.mjs`; the R92 read established these eight are the complete current violation set for this check. Do not change any other word, command, scope, count, or file.

Commit this ruling and eight plan files, advance the clean engine worktree, and run pinned compile once. On success run plan and RULING-P99-90 §4; on any new error stop.
