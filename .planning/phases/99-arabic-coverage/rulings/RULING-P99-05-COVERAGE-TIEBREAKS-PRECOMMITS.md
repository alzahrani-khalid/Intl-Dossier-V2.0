> **WORKER-REACHABLE COPY.** The authoritative original is `.tickmarkr/overseer/RULING-P99-05-COVERAGE-TIEBREAKS-PRECOMMITS.md`,
> which is gitignored and therefore unreachable from a task worktree. This copy was placed here by the
> OVERSEER on 2026-08-18 (`RULING-P99-14`) so the 17 tasks citing it as context can actually read it.
> Verbatim at copy time; if the two ever differ, the `.tickmarkr` original governs.

# RULING P99-05 — coverage standard, tie-breaks, D-23 boundary, and pre-commitments on checker A

2026-08-18, OVERSEER (wK:p7X). Inputs: `P99-LEG1-NOTE-02.md` (NOTE-END), `99-PLAN-CHECK-A.md`
(PLAN-CHECK-END, read whole), `99-PLAN-INDEX.md` §7/§8. Verified from this seat: the three
tie-break pairs' actual bundle strings (EN dashboard "Dossier Dashboard"; EN persons title
"Key Contacts"; EN positions "Positions Library"). Checker B is still writing — nothing below
quotes it, and the consolidated repair round waits for it; the pre-commitments exist so that
round starts the moment B settles.

## 1. Decision coverage: 33/39 HONEST beats 39/39 MANUFACTURED — with one real gap ordered fixed

- **Waived as citation-truths, accepted on machine evidence:** D-01, D-03, D-36, D-37, D-39
  (set-level properties, each verified by your own instrument with a positive control, per your
  table). The waiver's condition: that evidence table travels VERBATIM into `P99-PLAN-REPORT.md`.
  A citation added only to move a token matcher is gate-gaming; I will not order it.
- **D-29 is a REAL gap, not a citation gap:** `99-08` and `99-10` drive rendered surfaces and
  cite no `UI99-Cn`. The revision round (already owed for the blockers) adds the citations WITH
  the substance A-05/A-06 found missing behind them. `99-02`/`99-09` render nothing; waived.
- Leg-1 exit standard is therefore: the honored-evidence table + D-29 repaired where surfaces
  render. The gate's `passed:false` is quoted, not laundered.

## 2. ROADMAP `Plans: TBD` — mine, and NOT NOW

The plan count is a moving number (A-02's split alone changes it). I update the ROADMAP line at
leg-1 ACCEPTANCE, from the final compiled set. Never freeze a moving number.

## 3. The three tie-break pairs — RULED; the title-wins default is DEAD

**Criterion-semantics clarification first (rule-4 act, so the oracle is buildable):** criterion
1's "a nav label matches the title of the page it opens" means the same OBJECT TERM,
sense-consistent — NOT byte-equality. Byte-equality would indict EN's own structure
("Positions" nav → "Positions Library" title). The defect class is a nav and title carrying
DIFFERENT object terms (person vs contact), or two surfaces sharing ONE name (the intake/waiting
collision).

- **persons — `الأشخاص` wins, nav AND title.** The dossier type is `person`; "contacts" is
  RETIRED vocabulary in this product (route namespace hygiene, contacts retired). The AR title
  `جهات الاتصال الرئيسية` translates EN's `"Key Contacts"` faithfully — the EN title is itself
  the retired-vocabulary instance. AR title becomes `الأشخاص`-family; the EN title repairs to
  the Persons family in the same act IF `en/persons.json` is inside an existing lane's
  `files_modified` (it should be — the authoring lanes own both locales); if not, it goes on the
  register as a named residue, not silently.
- **positions — CONFORMANT AS-IS; pair dissolved.** Nav `المواقف` and title `مكتبة المواقف`
  share the object term (mirroring EN "Positions" / "Positions Library"). Under the semantics
  above there is no defect. No edit.
- **dashboard — `لوحة الدوسيهات`, nav and title.** EN says "Dossier Dashboard"; the current AR
  `لوحة الملفات` renders dossier as `ملف`, which D-17 reserves for files/profiles —
  nonconformant on its face. The masked English nav label is an AR-04a repair that lands the
  same term.
- **Standing rule replacing the default:** any FURTHER unruled pair discovered at execution
  ESCALATES to me — they are enumerable and cheap to rule; a worker never applies title-wins or
  any invented policy. This plus the rulings above dissolve A-03 WITHOUT a mid-run human gate on
  99-08; `autonomous: true` stands there, and P99-10's human gate stands as the rendered
  sign-off.

## 4. §7.2 — D-23's boundary: the planner's deletion-only reading is CORRECT

D-23's "every binding this phase TOUCHES" means BINDING-LEVEL change — a new key, a re-bind, a
resolution repair. Deleting a proven-redundant second argument at a site whose resolution was
just verified is not a binding change; forcing colon-form there is D-21's wholesale conversion
arriving early, which D-20 exists to refuse. 99-09 stays deletion-only. I am amending D-23 in
`99-CONTEXT.md` with one clarifying sentence, dated, so the engine's judges read the same law.

## 5. Pre-commitments on checker A's blockers (repairs land in the consolidated round after B)

- **A-01 — endorsed.** The strict parser's `twoArgTotal === 0` (with one-line AND wrapped
  positive controls in its fixture) is the closing predicate; the grep is demoted to diagnostic
  and gains `\b`. This is D-22 applied — the committed loose audit was already ruled inadequate.
- **A-02 — the split is REQUIRED, not optional.** A verification-only engine task proving
  repo-wide strict-zero in BOTH locales, then a deletion task that `depends_on` it. This is
  exactly the D-24-in-the-DAG property I pre-committed to grade; as compiled it is absent, and
  checker A is right.
- **A-03 — dissolved by §3 above.**
- **A-04 — endorsed.** A drilled glossary instrument enumerating EVERY ruled row with its sense
  exceptions (`ملف`, `إحاطة`, `منصب`) and classifying every occurrence; its controlled oracle
  joins both the 99-08 acceptance and the 99-10 battery. Never a blanket substring rewrite —
  that is D-17's own law.
- **A-05 — endorsed AS AMENDED.** "NOT CONSTRUCTED" is never a self-service pass. The
  D-35-NAMED undriven legs may close bounded — those are pre-ruled with owners. Any leg NOT on
  D-35's list that cannot run leaves its task red and PARKS to me by name. For UI99-C7:
  deterministic fixtures are the PRIMARY remedy (staging seed, CHECK-constraints verified before
  insert); a skip happens only by my written ruling. The typed oracles A-05 demands (per-spec
  file-existence + hardcoded test-count + the run itself) are ordered.
- **A-06 — endorsed.** The `/activity?lng=ar` relative-time leg (Latin digits asserted) is
  added; the three banner states are three separately counted tests over fixtures per A-05.

RULING-END
