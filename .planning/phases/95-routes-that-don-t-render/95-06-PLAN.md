---
phase: 95-routes-that-don-t-render
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/reports/index.ts
  - frontend/src/pages/reports/ReportsPage.tsx
  - frontend/src/i18n/en/report-builder.json
  - frontend/src/i18n/ar/report-builder.json
  - scripts/probe-report-generate.mjs # NEW — behavioural POST probe
  # frontend/src/pages/reports/generate-entry.ts + its pin test change ONLY IF behaviour changes —
  # the conditional is governed in Task 2; if touched, both land in the same commit (C9b)
autonomous: true
requirements: [DEAD-09]
success_criterion: 'filed-finding close (DEAD-09) — real report generation; the honest unavailable terminal state is the only fallback'
truths:
  - 'Decisions covered — D-08: generation ships REAL work (storage artifact + signed url) and the honest unavailable terminal state is the ONLY permitted fallback — the rename-never-ships-alone law holds and no change converts a visible failure into a fabricated success; D-16: the generate-entry pin is a C9b consumer carried with the mock-vs-real column from authoring — updated in the same task if generate-entry.ts behaviour changes, else recorded as a named non-consumer with the reason; D-19: the reports redeploy ships with scripts/probe-edge-auth.sh evidence plus a POST probe returning a fetchable url'
must_haves:
  truths:
    - 'The reports POST no longer answers 202 job theater — it gathers real data per type, uploads a real artifact to the private bucket, and answers {url, status: completed} synchronously; a completed entry ALWAYS carries a url'
    - 'FORBIDDEN SHAPE (ACCEPTANCE-P95 condition 8, named): fabricating a completed entry or a byte-stream for a format not really produced — unsupported formats keep the honest report-builder:generate.unavailable terminal state'
    - 'The generate flow renders pending / completed-with-url / failed truthfully per UI-SPEC §6; the three NEW report-builder:generate.* keys land in BOTH locales in the same commit, colon-form'
    - 'The redeployed function answers a real POST whose returned signed url is FETCHABLE (probe evidence, not absence-of-error)'
  artifacts:
    - path: 'supabase/functions/reports/index.ts'
      provides: 'real synchronous POST: type/format guard kept, data gathered, artifact uploaded, createSignedUrl(path, 86400) returned'
      contains: 'createSignedUrl'
    - path: 'frontend/src/pages/reports/ReportsPage.tsx'
      provides: 'pending/failed rendering on the mutation, formats offered restricted to what the server really produces'
      contains: 'generate.pending'
    - path: 'scripts/probe-report-generate.mjs'
      provides: 'behavioural oracle: authenticated POST, asserts response.url, fetches it, exit 0 only on a fetchable artifact; exit 2 when creds missing (UNABLE TO MEASURE, never a pass)'
      contains: 'process.exit(2)'
  key_links:
    - from: 'frontend/src/pages/reports/ReportsPage.tsx'
      to: 'frontend/src/pages/reports/generate-entry.ts'
      via: 'onSuccess maps through buildGeneratedReportEntry — completed-only-with-url / else-unavailable already; if the server returns {url,status:completed} the pure fn may not change'
      pattern: 'buildGeneratedReportEntry'
    - from: 'supabase/functions/reports/index.ts'
      to: 'supabase/functions/pdf-generate/index.ts'
      via: 'the deployed storage precedent copied: storage.from(private).upload + createSignedUrl 86400'
      pattern: 'createSignedUrl'
---

<objective>
Close DEAD-09 (filed-finding close): the reports POST is a MOCK — `reports/index.ts:266-285`
mints a job_id, setTimeout(console.log), answers `202 {job_id, status:'pending'}`; no url, no
work. Phase 94 made the client HONEST (completed-only-with-url, else the bilingual
`generate.unavailable` terminal state). What remains — and what Phase 95 owns — is REAL
generation.

Chosen path (research candidate 3, RECOMMENDED — the other two are dead: the "Express
/report-builder/generate" candidate DOES NOT EXIST anywhere in the tree, and custom-reports
executes saved configs inline with no artifact): make the `reports` POST real. The fn's own GET
branch already gathers real summary data; the POST serializes it (JSON first, a minimal ~15-line
CSV join/escape second — no serializer library), uploads to Storage bucket `private`
(pdf-generate's deployed precedent), returns `{url: signedUrl, status: 'completed'}`
synchronously — the fake-async 202 job theater is deleted.

Honest-format law (D-08): real minimal generation is CSV/JSON. Do NOT fabricate a PDF or Excel
byte-stream. The page's offered generate formats are restricted to what the server really
produces; anything else keeps the `generate.unavailable` terminal state. No change may convert
a visible failure into an invisible fabricated success (RULING-P94-04 §PARK-94-06).

Output: real POST + deploy with probe evidence + truthful client states + i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/95-routes-that-don-t-render/95-RESEARCH.md (§DEAD-09 — candidate assessment incl. the corrected nonexistent-Express seam; §Code Examples — the storage artifact pattern)
@.planning/phases/95-routes-that-don-t-render/95-PATTERNS.md (§DEAD-09 — the mock excerpt, the pdf-generate excerpt, the edge auth/CORS pattern)
@.planning/phases/95-routes-that-don-t-render/95-UI-SPEC.md (§6 — the pending/completed/failed/unavailable contract; §Copywriting — the three NEW keys + suggested Arabic)

<interfaces>
Verified by research at HEAD:
- The mock: `reports/index.ts:266-285`; the `:258` guard requires `type`+`format` — KEEP it.
- Storage precedent (`pdf-generate/index.ts:383-397`, deployed, bucket `private` exists on
  staging): `storage.from('private').upload(filePath, bytes, {contentType, cacheControl:'3600'})`
  then `createSignedUrl(filePath, 86400)`. Artifact path shape: `reports/<type>-<Date.now()>.<ext>`
  — the path is SERVER-composed from the validated type, never from raw user input.
- Auth/CORS for the touched fn: the house pattern (`_shared/cors.ts` getCorsHeaders /
  handleCorsPreflightRequest; anon client + injected Authorization header + getUser) — mirror
  assignments-queue/index.ts:39-97 if the POST branch lacks it; never a bare getUser on a plain
  anon client.
- Client seam: `ReportsPage.tsx:183-208` — mutation invokes `reports` with {type, format,
  parameters}; onSuccess maps through `buildGeneratedReportEntry` (generate-entry.ts) which is
  ALREADY completed-only-with-url / else-unavailable. If the server returns {url,
  status:'completed'}, generate-entry.ts likely needs NO change.
- C9b consumer row (mock-vs-real, from authoring): `generate-entry.test.ts` — REAL consumer of
  generate-entry.ts (5/5 green, 596ms, run by research). Updated in the SAME task if
  generate-entry.ts changes; else recorded as a NAMED non-consumer with the reason
  ("server-side change only; the pure fn contract is unchanged").
- i18n: namespace `report-builder` registered both languages (i18n/index.ts:312,448). NEW keys
  (verified absent — only generate.unavailable exists under generate.*): generate.pending
  ("Generating report" / "جارٍ إنشاء التقرير"), generate.failed ("Report generation failed. Try
  again." / "فشل إنشاء التقرير. حاول مرة أخرى."), generate.completed ("Open report" /
  "فتح التقرير"). Colon-form addressing only. Arabic naturalness sign-off is an OPERATOR park —
  no claim here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the reports POST real (guard kept, artifact + signed url, job theater deleted)</name>
  <files>supabase/functions/reports/index.ts</files>
  <read_first>reports/index.ts in full (the :258 guard, the GET branch data gathering to reuse, the mock at :266-285, the fn auth/CORS state); pdf-generate/index.ts:377-401 (the storage pattern to copy); supabase/functions/assignments-queue/index.ts:39-97 (the auth/CORS house pattern if the POST branch needs it)</read_first>
  <action>
    Replace :266-285: keep the :258 type/format guard; for a supported format (json, csv):
    gather the data for the requested `type` by reusing the GET branch's own summary-data
    queries (counts over countries/organizations/mous/events/intelligence — same file, extract
    a shared helper rather than duplicating); serialize (JSON.stringify for json; a minimal
    inline CSV join/escape for csv — quote fields containing commas/quotes/newlines, no
    library); upload to `storage.from('private')` at `reports/<validated-type>-<Date.now()>.<ext>`
    with the correct contentType; `createSignedUrl(path, 86400)`; answer
    `200 {url: signedUrl, status: 'completed'}`. On any failure answer an error status with a
    generic message envelope — never a fabricated success, never internals in message text.
    For an UNSUPPORTED format (pdf, excel — nothing real produces them) answer
    `501 {error: {code: 'FORMAT_UNAVAILABLE', ...}}` so the client keeps the honest
    generate.unavailable terminal state. Delete the setTimeout/job_id/202 theater entirely
    (nothing keeps it — research State-of-the-Art). Auth/CORS stay on the house pattern.
  </action>
  <verify>
    <automated>test "$(command grep -c 'setTimeout' supabase/functions/reports/index.ts)" -eq 0 && command grep -q 'createSignedUrl' supabase/functions/reports/index.ts && command grep -qE "from\('private'\)" supabase/functions/reports/index.ts && test "$(command grep -cE 'status: ?202\b' supabase/functions/reports/index.ts)" -eq 0 && command grep -q 'signedUrl' supabase/functions/reports/index.ts</automated>
  </verify>
  <acceptance_criteria>Zero setTimeout and zero 202 responses remain in the file (instrument-tested by the createSignedUrl/private positive pins in the same chain — do not write "setTimeout" or a "status: 202" literal in comments; bare year strings like 2026 are safe, the gate matches the status literal form only); a completed response always carries url (the signedUrl pin is 0 today — drilled red; do NOT pin on the string status: 'completed', which already exists at :60 in the mock job object and cannot go red); unsupported formats answer 501 FORMAT_UNAVAILABLE; the :258 guard survives byte-equivalent.</acceptance_criteria>
  <done>The POST does real work: real data, real artifact, real signed url — or an honest refusal.</done>
</task>

<task type="auto">
  <name>Task 2: Truthful client states + i18n keys + the C9b pin decision</name>
  <files>frontend/src/pages/reports/ReportsPage.tsx, frontend/src/i18n/en/report-builder.json, frontend/src/i18n/ar/report-builder.json</files>
  <read_first>ReportsPage.tsx:150-230 (the templates array with its per-template formats, the mutation, onSuccess mapping); generate-entry.ts + __tests__/generate-entry.test.ts in full (decide the C9b disposition against the REAL new server response); 95-UI-SPEC §6 and §Copywriting (the exact three keys, styles, and Arabic renderings)</read_first>
  <action>
    (1) Restrict the offered generate formats to what Task 1 really produces (csv, json):
    remove or disable pdf/excel options in the templates the generate affordance offers —
    an offered format that fabricates is the forbidden shape; record the restriction in the
    SUMMARY as the honest-format decision.
    (2) Render the three states per UI-SPEC §6: pending — mutation isPending disables the
    generate button (aria-disabled, label unchanged) and renders the pending row from
    `t('report-builder:generate.pending')`, neutral text-ink-mute; failed — mutation isError
    renders AlertCircle + text-danger + role="alert" with `t('report-builder:generate.failed')`
    and retry via the existing generate affordance, no raw error.message in JSX; completed —
    the entry's "Open report" action (`t('report-builder:generate.completed')`, .btn-ghost —
    accent not spent) opens the REAL url. generate.unavailable rendering ships unchanged as the
    terminal fallback.
    (3) Add the three keys to BOTH locale files in this same commit (values from UI-SPEC
    Copywriting incl. the suggested Arabic; naturalness is an operator park — no claim).
    (4) C9b PIN DECISION, executed not deferred: read the new server response against
    generate-entry.ts. If its behaviour must change, change it AND update
    generate-entry.test.ts IN THIS TASK (both files join the commit). If it needs no change
    (expected — the server now returns exactly the completed-with-url shape the fn already
    maps), run the pin once (`cd frontend && pnpm exec vitest run
    src/pages/reports/__tests__/generate-entry.test.ts`) and record it in the SUMMARY as a
    NAMED non-consumer of this task's edits with the reason, plus its green run as regression
    evidence.
  </action>
  <verify>
    <automated>cd frontend && command grep -q 'report-builder:generate.pending' src/pages/reports/ReportsPage.tsx && command grep -q 'report-builder:generate.failed' src/pages/reports/ReportsPage.tsx && node -e "const en=require('./src/i18n/en/report-builder.json').generate,ar=require('./src/i18n/ar/report-builder.json').generate;for(const k of ['pending','failed','completed','unavailable']){if(!en[k]||!ar[k]){console.error('MISSING key '+k);process.exit(1)}}" && pnpm exec vitest run src/pages/reports/__tests__/generate-entry.test.ts && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>Both new-state keys addressed colon-form in the page; all four generate.* keys present in BOTH locales (the node check fails per missing key by name); the generate-entry pin green (updated-in-task or named-non-consumer — the disposition written in the SUMMARY with the reason); type-check clean; pdf/excel no longer offered as generate formats.</acceptance_criteria>
  <done>The generate flow renders pending/completed/failed truthfully in both locales; the pin disposition is recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Redeploy + behavioural POST probe (a fetchable artifact, observed)</name>
  <files>scripts/probe-report-generate.mjs</files>
  <read_first>scripts/probe-edge-auth.sh (the evidence format + never-echo-credentials rule); the P94 probe shape (scripts/probe-after-action-publish.mjs — exit 2 on missing creds, read-back assertion, prints its subjects)</read_first>
  <action>
    (1) Deploy: `supabase functions deploy reports --project-ref zkrcjzdemdmwhearhfgg`; then
    `bash scripts/probe-edge-auth.sh reports` — paste the `reports -> status` line into the
    SUMMARY (D-19).
    (2) Write `scripts/probe-report-generate.mjs` in the P94 probe shape:
    read TEST_USER_EMAIL/TEST_USER_PASSWORD from .env.test — absent ⇒ print what is missing and
    `process.exit(2)` (UNABLE TO MEASURE, never a pass — GATE-STANDARD C2); mint a JWT via
    signInWithPassword; POST /functions/v1/reports with a type the :258 guard accepts and
    format 'json'; assert the response carries `url` and `status === 'completed'`; FETCH the
    url and assert HTTP 200 with a non-empty body (the artifact, observed — not the absence of
    an error); also POST format 'pdf' and assert the response is NOT a completed-with-url
    (the honest-refusal branch, observed); print status codes and the artifact byte count only
    — never tokens, never the signed url itself (it grants access; print its length). Exit 0
    only if both assertions held.
  </action>
  <verify>
    <automated>test -f scripts/probe-report-generate.mjs && command grep -q 'process.exit(2)' scripts/probe-report-generate.mjs && bash scripts/probe-edge-auth.sh reports | command grep -E 'reports -> [0-9]{3}' && node scripts/probe-report-generate.mjs</automated>
  </verify>
  <acceptance_criteria>Probe exits 0 having observed a fetchable artifact from a real POST AND the honest refusal on an unsupported format; exits 2 without creds (distinct code, never counted as pass). GATE LABEL: green direction UNPROVEN pre-execution — needs Task 1 deployed to staging; red constructible now (the deployed mock answers 202 with no url → the probe assertion fails). Deploy evidence lines in the SUMMARY.</acceptance_criteria>
  <done>DEAD-09 closes on observed behaviour: a real POST returns a real fetchable artifact, and unsupported formats refuse honestly.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                     |
| --------------------------- | --------------------------------------------------------------- |
| client → reports POST       | JWT auth via house pattern; type/format validated at :258       |
| artifact → requester        | signed URL is the access grant — TTL and bucket privacy matter  |

## STRIDE Threat Register

| Threat ID | Category               | Component            | Disposition | Mitigation Plan                                                                                                  |
| --------- | ---------------------- | -------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------- |
| T-95-13   | Information Disclosure | signed artifact URLs | mitigate    | bucket `private` (never public URLs); createSignedUrl TTL 86400 per the deployed pdf-generate precedent; probe prints url length, never the url |
| T-95-14   | Tampering              | storage object path  | mitigate    | path composed server-side from the :258-validated type + timestamp — no raw user input reaches the path          |
| T-95-15   | Repudiation/Integrity  | fabricated success   | mitigate    | completed ALWAYS carries a url; unsupported formats 501; the probe observes the artifact, not the status word    |
| T-95-16   | Spoofing               | POST auth            | mitigate    | house auth pattern (injected Authorization + getUser); CORS via _shared/cors.ts                                  |
| T-95-SC   | Tampering              | package installs     | accept      | zero installs this phase (CSV serializer is ~15 inline lines, no library)                                        |

</threat_model>

<verification>
- Fn gates green (theater deleted, artifact pattern present, guard kept).
- Client gates green (colon-form keys, both locales, pin disposition executed).
- Deploy + probe: real POST → fetchable artifact; honest refusal observed (execution-time; labels honoured).
</verification>

<success_criteria>
DEAD-09 closes as a filed-finding: report generation does REAL work with a fetchable artifact,
renders pending/completed/failed truthfully in both locales, and the honest unavailable state
remains the only fallback for anything not really produced.
</success_criteria>

<output>
Create `.planning/phases/95-routes-that-don-t-render/95-06-SUMMARY.md` when done. It MUST carry:
the deploy + probe evidence lines, the honest-format restriction decision, and the
generate-entry pin C9b disposition (updated-in-task or named-non-consumer with reason).
</output>
