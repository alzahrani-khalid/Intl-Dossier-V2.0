---
phase: 95-routes-that-don-t-render
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/domains/audit/hooks/useRetentionPolicies.ts
  - frontend/src/routes/_protected/admin/data-retention.tsx
  - frontend/src/domains/audit/hooks/__tests__/useRetentionPolicies.test.ts # NEW
autonomous: true
requirements: [RETENTION-CAST-01]
success_criterion: 'filed-finding close (RETENTION-CAST-01) — the six false casts unwrap the real {data:[...]} envelope; never a coerce-to-empty'
truths:
  - 'Decisions covered — D-10: the six casts in useRetentionPolicies.ts are fixed to unwrap the real {data:[...]} envelope with the count SIX re-derived by command before the edit (never trusted — the scout counted 11 cast-shaped lines, reconciled by research as a different pattern), the FORBIDDEN FIX SHAPE Array.isArray(x) ? x : [] is named and banned, and the asRows consumption-point repair in data-retention.tsx is reconciled in the same plan per C9'
must_haves:
  truths:
    - 'Each of the six queryFns unwraps and VALIDATES the envelope: body.data not an array ⇒ THROW (renders the error state) — never coerce to [] (the coerce renders "No Policies" over rows the server sent, the confident-lie class this milestone exists to kill)'
    - 'With a mocked {data:[...N...]} envelope the policies table renders N rows; {data:[]} renders the empty state; a bare-array or malformed body renders the error state, never empty'
    - 'asRows in data-retention.tsx (the b71ad62b consumption-point workaround tolerating the hook lie) is DELETED; the page consumes the hooks validated arrays directly; the per-region QueryErrorState gating stays'
    - 'The DR-SUBPATH-01 server parse (every non-policies sub-path 404s) is P100 property — NOT fixed here; those regions render their error state, which is truthful'
  artifacts:
    - path: 'frontend/src/domains/audit/hooks/useRetentionPolicies.ts'
      provides: 'six validate-or-throw envelope unwraps replacing the six as-Promise casts'
      contains: 'data'
    - path: 'frontend/src/domains/audit/hooks/__tests__/useRetentionPolicies.test.ts'
      provides: 'unit oracle: N-row envelope maps, empty maps, malformed THROWS'
      contains: 'malformed'
    - path: 'frontend/src/routes/_protected/admin/data-retention.tsx'
      provides: 'asRows deleted; direct consumption of validated hook data; region error gating retained'
      contains: 'QueryErrorState'
  key_links:
    - from: 'frontend/src/routes/_protected/admin/data-retention.tsx'
      to: 'frontend/src/domains/audit/hooks/useRetentionPolicies.ts'
      via: 'the page is the hooks only consumer besides the compat re-export — reconciled in the same plan (C9)'
      pattern: 'useRetentionPolicies'
---

<objective>
Close RETENTION-CAST-01 (filed-finding close): `useRetentionPolicies.ts` casts a `{data:[...]}`
envelope as if it were a bare array — six times (`as Promise<T[]>` at :71,:129,:190,:205,:220,
:228, re-derived by research; RE-DERIVE AGAIN before editing, D-10). The route crashed on the
real payload; 93-09 repaired the CONSUMPTION point (`asRows` in data-retention.tsx) because the
hook was outside its files_modified — the six false casts remained.

FORBIDDEN FIX SHAPE, named per ACCEPTANCE-P95 condition 8: `Array.isArray(x) ? x : []` — it
renders "No Policies" over rows the server sent. The permitted shape is validate-or-throw
(RESEARCH §Code Examples): unwrap `body.data`; not an array ⇒ THROW ⇒ the error state renders.

Frontend-only; no deploy. The behavioural ban on the coerce shape is the unit test itself: a
coerce would fail the malformed-throws case — the honest oracle, in place of a prose-trippable
negative grep (GATE-STANDARD C8).

Output: six unwraps + unit oracle + consumer reconcile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/95-routes-that-don-t-render/95-RESEARCH.md (§RETENTION — the re-derivation, the server envelope verification, the DR-SUBPATH-01 boundary)
@.planning/phases/95-routes-that-don-t-render/95-PATTERNS.md (§RETENTION — the lying cast excerpt, the permitted shape, the asRows block to delete)
@.planning/phases/95-routes-that-don-t-render/95-UI-SPEC.md (§8 — the oracle statement: N rows / empty only over truthful [] / error never rendered as empty)

<interfaces>
Verified by research at HEAD:
- Cast population derivation (RUN BEFORE EDITING, paste result in the SUMMARY):
  `command grep -n "as Promise<" frontend/src/domains/audit/hooks/useRetentionPolicies.ts`
  → expected exactly 6 (:71 RetentionPolicy[], :129 LegalHold[], :190 RetentionStatistics[],
  :205 PendingRetentionAction[], :220 ExpiringEntity[], :228 RetentionExecutionLog[]). If the
  count differs from 6, STOP and reconcile against the register before editing.
- Server envelope (verified in fn source): data-retention/index.ts answers `{ data: [...] }`
  for lists (:254 and the list branch). Server-side today only /policies works — the
  second-to-last-segment parse (DR-SUBPATH-01, :119-124) 404s every other sub-path. P100
  property — those queries reject → ApiError(404) → per-region error state (truthful).
- Permitted shape (RESEARCH verbatim):
  `const body = (await api(...)) as { data?: unknown }; if (!Array.isArray(body?.data)) throw new Error('malformed retention envelope'); return body.data as T[]`
- Consumer: `data-retention.tsx:156-164` asRows (delete, including its :148-154 comment block);
  the page's per-region QueryErrorState gating (:439-444 + seven inline usages) STAYS. Only
  other consumer: the compat re-export `frontend/src/hooks/useRetentionPolicies.ts` (re-export
  only — no change; named).
- Unit-test analog: `useQuickSwitcherSearch.test.ts:10-37` (colocated __tests__, plain vitest,
  factory + exact expectations; mock the repository fn, prefer pinning pure behaviour;
  frontend/tests/setup.ts mocks react-i18next English-only — assert roles/counts, not
  translations).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-derive the six, write the oracle, replace the casts</name>
  <files>frontend/src/domains/audit/hooks/useRetentionPolicies.ts, frontend/src/domains/audit/hooks/__tests__/useRetentionPolicies.test.ts</files>
  <read_first>useRetentionPolicies.ts in full (261 lines — all six queryFns and the repository fns they call); frontend/src/domains/audit/repositories/audit.repository.ts:70-142 (what the repository fns actually return); the unit-test analog useQuickSwitcherSearch.test.ts:10-37</read_first>
  <behavior>
    - Test: mocked repository returning {data:[3 policy rows]} → the hook queryFn resolves to exactly those 3 rows.
    - Test: {data:[]} → resolves to [] (a truthful empty).
    - Test: a bare array (the OLD lying assumption) → THROWS 'malformed retention envelope'.
    - Test: {data: 'not-an-array'} and undefined → THROWS.
    - Test: at least one non-policies queryFn (e.g. legal holds) validates the same way (the fix is all six, the test samples ≥2 of them).
  </behavior>
  <action>
    FIRST run the cast-population derivation (interfaces above) and paste it into the SUMMARY —
    the edit proceeds only on a re-derived count of 6. Then replace each of the six
    `as Promise<T[]>` casts with the validate-or-throw unwrap (the permitted shape, verbatim
    semantics; an async queryFn per RESEARCH §Code Examples). Do NOT use Array.isArray coercion
    to []; do NOT write the string `as Promise<` in comments in this file (a gate counts it).
    Export or structure the unwrap so the test can pin it without renderHook plumbing (e.g. a
    shared `unwrapListEnvelope<T>(body): T[]` helper used by all six — one guard in the shared
    function beats six copies). Query keys, staleTime, and hook signatures unchanged.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/domains/audit/hooks/__tests__/useRetentionPolicies.test.ts && test "$(command grep -c 'as Promise<' src/domains/audit/hooks/useRetentionPolicies.ts)" -eq 0 && command grep -q 'useQuery' src/domains/audit/hooks/useRetentionPolicies.ts && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>The derivation paste shows the pre-edit count (6) and the post-edit count is 0 (instrument-tested by the useQuery positive pin in the same chain); the unit oracle is green INCLUDING the malformed-throws cases — the behavioural ban: a coerce-to-[] implementation cannot pass them; type-check clean; hook public signatures unchanged.</acceptance_criteria>
  <done>All six queryFns return honest validated arrays or throw; the lie is structurally impossible without failing the oracle.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the consumer — delete asRows, keep the region error gating</name>
  <files>frontend/src/routes/_protected/admin/data-retention.tsx</files>
  <read_first>data-retention.tsx:140-200 (asRows and its comment block, every call site) and :430-450 (the region gating that STAYS); the compat re-export frontend/src/hooks/useRetentionPolicies.ts (verify it is re-export-only)</read_first>
  <action>
    Delete `asRows` (:156-164) AND its :148-154 comment block; replace every asRows(...) call
    with direct consumption of the hook's now-validated arrays. The per-region QueryErrorState
    gating (:439-444 and the seven inline usages) stays byte-equivalent. The "No policies" empty
    state renders only over a truthful [] (which the hook now guarantees). Do not write the
    identifier asRows in any surviving comment (a gate counts it). The compat re-export is a
    named non-change. DR-SUBPATH-01 regions (statistics, legal-holds, etc.) keep rendering
    their error states — truthful; do not touch the server parse (P100).
  </action>
  <verify>
    <automated>cd frontend && test "$(command grep -c 'asRows' src/routes/_protected/admin/data-retention.tsx)" -eq 0 && command grep -q 'QueryErrorState' src/routes/_protected/admin/data-retention.tsx && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>Zero asRows occurrences remain (instrument-tested by the QueryErrorState positive pin proving the file reads); the region error gating survives (the same pin); type-check clean — which also proves every former asRows call site now type-checks against the hooks honest T[] returns.</acceptance_criteria>
  <done>The consumption-point workaround is gone; the page consumes honest data and renders truthful states per region.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                          |
| ---------------------- | -------------------------------------------------------------------- |
| server envelope → UI   | malformed/unexpected bodies must render as errors, never as empties  |
| admin gate             | data-retention remains admin-gated server-side; RLS untouched        |

## STRIDE Threat Register

| Threat ID | Category               | Component               | Disposition | Mitigation Plan                                                                                          |
| --------- | ---------------------- | ----------------------- | ----------- | ---------------------------------------------------------------------------------------------------------- |
| T-95-18   | Tampering              | envelope unwrap         | mitigate    | validate-or-throw: a tampered or malformed body renders the error state; the unit oracle pins the throw   |
| T-95-19   | Information Disclosure | row visibility          | mitigate    | NO widened visibility: frontend-only unwrap of what the server already sent under its own RLS/admin gate; no query, policy, or endpoint changes |
| T-95-SC   | Tampering              | package installs        | accept      | zero installs this phase                                                                                   |

</threat_model>

<verification>
- Derivation pasted (6 → 0); unit oracle green incl. throw cases; type-check clean.
- asRows gone; region gating intact.
</verification>

<success_criteria>
RETENTION-CAST-01 closes as a filed-finding: the six false casts are validate-or-throw unwraps,
the consumption-point workaround is deleted, and "No policies" can only ever render over a
truthful empty array.
</success_criteria>

<output>
Create `.planning/phases/95-routes-that-don-t-render/95-08-SUMMARY.md` when done. It MUST carry
the pre-edit cast derivation paste and name the compat re-export as the only other (unchanged)
consumer, plus the DR-SUBPATH-01 boundary note (error states truthful, server parse untouched).
</output>
