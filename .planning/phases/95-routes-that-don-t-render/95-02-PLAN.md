---
phase: 95-routes-that-don-t-render
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/useAssignmentQueue.ts
  - tests/e2e/95-queue-renders.spec.ts # NEW
  - scripts/probe-edge-auth.sh # ships the 404-kind gateway|function disambiguation (overseer ship check 2026-08-16); fn -> status line format UNCHANGED (C9a)
  # supabase/functions/assignments-queue/ is REDEPLOYED (stale v11 of 2026-07-13 → current source), not edited — source audited sound by research
autonomous: true
requirements: [DEAD-02]
success_criterion: '2 — /tasks/queue renders its queue against a deployed assignments-queue function'
truths:
  - 'Decisions covered — D-04 (as corrected by the 2026-08-16 planning-leg gate drill): assignments-queue exists in-tree and the missing half is a CURRENT deployment — staging runs stale v11 of 2026-07-13 whose own 404 masqueraded as not-deployed; this plan redeploys the audited-sound source via Supabase CLI with version-increment + probe evidence, and a fixed function that is not redeployed has not been fixed; D-19: deploy via Supabase CLI/MCP with scripts/probe-edge-auth.sh evidence lines in the SUMMARY; D-14: the queue spec asserts its own file existence first, hardcodes its test count including for --list, and always passes --no-deps'
must_haves:
  truths:
    - 'The deployed assignments-queue version EXCEEDS the stale baseline v11: at planning time the platform manifest lists it ACTIVE at version 11, updated 2026-07-13 (pre-P92-auth-fleet), while the probe answers 404 — the deploy of the current audited-sound in-tree source is what moves the version, and the version comparison is the red-capable deployment oracle (the manifest-inclusion check is green today and can never go red)'
    - '/tasks/queue settles to exactly one of the four truthful states — populated rows, EmptyState, or query-error-state — never a spinner past the retry budget, never raw internal strings'
    - 'Queue filters reach the function: the invoke-with-GET+body transport (which never serializes params and throws TypeError in browsers) is replaced by apiGet with a query string'
    - 'tests/e2e/93-tasks-queue-error.spec.ts is a NAMED NON-CONSUMER by its own header (CDP-blocked, deploy-agnostic) — it is not touched and stays green before and after the deploy'
  artifacts:
    - path: 'frontend/src/hooks/useAssignmentQueue.ts'
      provides: 'apiGet transport carrying filters as a real query string the function can read from url.searchParams'
      contains: 'apiGet'
    - path: 'tests/e2e/95-queue-renders.spec.ts'
      provides: 'post-deploy natural-state behavioural oracle for criterion 2 (NOT CDP-blocked)'
      contains: '@covers DEAD-02'
  key_links:
    - from: 'frontend/src/hooks/useAssignmentQueue.ts'
      to: 'frontend/src/lib/api-client.ts'
      via: 'apiGet resolves the edge base and attaches the session JWT'
      pattern: 'apiGet'
    - from: 'tests/e2e/95-queue-renders.spec.ts'
      to: 'supabase/functions/assignments-queue/index.ts'
      via: 'natural network request against the DEPLOYED function (execution-time oracle, producer = the deploy task in this plan)'
      pattern: 'tasks/queue'
---

<objective>
Close DEAD-02 (success criterion 2): `/tasks/queue` renders its queue against a DEPLOYED
`assignments-queue` function.

Probe-verified 2026-08-16: `assignments-queue -> 404` on staging. **PREMISE CORRECTED
2026-08-16 (planning-leg gate drill):** the function is NOT absent — the platform manifest
lists it **ACTIVE, version 11, updated 2026-07-13**, predating the P92 auth fleet. The probe's
404 is a FUNCTION-emitted 404 from that stale deployment, not a gateway not-deployed 404 — the
probe line alone cannot distinguish the two, which is how the not-deployed reading survived
research. The repair is unchanged in substance: REDEPLOY the current audited-sound in-tree
source (house auth pattern, house CORS, response shape byte-matches the client's
`QueueListResponse`) — DO NOT rewrite it (D-04, as corrected). P94's intended-broken register
forbade this repair there; Phase 95 is the owner — repairing it now is the mission. The
deployment oracle is the VERSION comparison (baseline v11 derived 2026-08-16 by
`supabase functions list --project-ref zkrcjzdemdmwhearhfgg` — re-derive and paste at
execution start; a deploy increments it), never manifest inclusion, which is vacuously green.

Secondary in-scope client defect: `useAssignmentQueue.ts:56-59` invokes with
`{method:'GET', body}` — invoke never serializes body into a query string (the fn reads
`url.searchParams`), and a browser GET+body fetch throws TypeError. The filters can never work
without the transport fix.

Purpose: criterion 2, plus the D-19 deploy-evidence discipline.
Output: transport fix + deploy with probe evidence + natural-state e2e oracle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/95-routes-that-don-t-render/95-RESEARCH.md (§DEAD-02 — source audit, the two invoke defects, deploy command; §Open Questions 1 — the staff_profiles derivation)
@.planning/phases/95-routes-that-don-t-render/95-PATTERNS.md (§DEAD-02 — the exact replacement shape)
@.planning/phases/95-routes-that-don-t-render/95-UI-SPEC.md (§3 — the four-state contract; no visual redesign)

<interfaces>
Verified by research at HEAD:
- `useAssignmentQueue.ts:29-35` — `QueueListResponse {items, total_count, page, page_size, total_pages}`,
  byte-matches the fn response. `:56-59` — the defective invoke call to replace.
- Replacement analog (dossiers.repository.ts:139-155 shape): build URLSearchParams, then
  `return apiGet<QueueListResponse>(`/assignments-queue?${params.toString()}`)`.
- `apiGet` (frontend/src/lib/api-client.ts:111-118) attaches the session JWT and resolves the
  edge base `VITE_SUPABASE_URL/functions/v1`.
- Fn role gate (`assignments-queue/index.ts`): no staff_profiles row → 404 "User profile not
  found"; role staff → 403; supervisor scoped to unit; admin sees all. All of those are
  TRUTHFUL error/empty renders on the page — do not "fix" them into empties (Pitfall 8).
- `pages/AssignmentQueue.tsx` is already a P93 error-state surface (QueryErrorState wired) —
  the page needs no edit; only the hook transport changes.
- Deploy evidence pattern: `supabase functions deploy assignments-queue --project-ref
  zkrcjzdemdmwhearhfgg` then `bash scripts/probe-edge-auth.sh assignments-queue` (stdout
  `fn -> status`, never echoes credentials).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the invoke transport with apiGet in useAssignmentQueue</name>
  <files>frontend/src/hooks/useAssignmentQueue.ts</files>
  <read_first>useAssignmentQueue.ts in full (the params it builds, :56-59 the invoke call); frontend/src/domains/dossiers/repositories/dossiers.repository.ts:139-155 (the URLSearchParams → apiGet shape to mirror); frontend/src/lib/api-client.ts:64-118 (base resolution + auth headers)</read_first>
  <action>
    Replace the `supabase.functions.invoke('assignments-queue', {method:'GET', ...body})` call
    with `apiGet<QueueListResponse>(`/assignments-queue?${params.toString()}`)` where `params`
    is the URLSearchParams the hook already assembles. Remove the now-unused supabase functions
    import if this change orphans it. Keep query keys, staleTime, and the hook's public
    signature unchanged — this is a transport swap, not a redesign. ApiError on non-2xx flows to
    isError → the page's existing QueryErrorState. Do not write the string `functions.invoke`
    in comments in this file (a gate counts non-comment occurrences; keep comments clean too).
  </action>
  <verify>
    <automated>cd frontend && command grep -q 'apiGet' src/hooks/useAssignmentQueue.ts && test "$(command grep -v '^\s*//' src/hooks/useAssignmentQueue.ts | command grep -c 'functions.invoke')" -eq 0 && command grep -q 'assignments-queue' src/hooks/useAssignmentQueue.ts && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>The hook transports via apiGet with a real query string; zero non-comment `functions.invoke` occurrences remain in the file (the `assignments-queue` positive pin in the same chain instrument-tests the zero); type-check clean; hook public signature unchanged (consumers uncompiled-unchanged — verified by type-check).</acceptance_criteria>
  <done>Filters can reach the function; the browser TypeError path is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy assignments-queue to staging with probe evidence</name>
  <files>scripts/probe-edge-auth.sh</files>
  <read_first>supabase/functions/assignments-queue/index.ts (confirm the audited-sound state — auth via injected Authorization + getUser, CORS via _shared/cors.ts, response shape — do NOT edit it); scripts/probe-edge-auth.sh (the evidence format)</read_first>
  <action>
    (0) SHIP the 404-disambiguation into `scripts/probe-edge-auth.sh` FIRST (overseer ship
    check, 2026-08-16): when a probe returns 404, the script prints one extra indented line
    `    404-kind: gateway|function  body: <first 160 bytes of the response body>` — derived
    from the body itself (the platform's not-deployed 404 body names the function-routing
    miss; anything else is the function's own 404). If the body matches neither known shape,
    print it raw with `404-kind: unknown` — never classify silently. The existing
    `fn -> status` line format is UNCHANGED (C9a: every consumer gate in this phase and every
    recorded P92/P94 evidence line greps `-> [0-9]{3}` on that line — the new line is
    additive). Controls: probe a known-deployed function (`reports`) → no 404 line; probe a
    known-absent slug (`no-such-fn-95`) → `404-kind: gateway` line printed.
    Then deploy: `supabase functions deploy assignments-queue --project-ref zkrcjzdemdmwhearhfgg`
    (CLI 2.106.0 on PATH; Supabase MCP deploy is the fallback). Then evidence:
    (1) Version derivation — the deployment oracle: re-derive the pre-deploy baseline by
    `supabase functions list --project-ref zkrcjzdemdmwhearhfgg` (JSON; at planning time
    assignments-queue is ACTIVE version 11, updated 2026-07-13) and paste it; post-deploy the
    version MUST EXCEED the pasted baseline. Manifest inclusion is NOT evidence — it is green
    before the work (the stale v11 is listed) and can never go red.
    (2) `bash scripts/probe-edge-auth.sh assignments-queue` — paste the `fn -> status` line into
    the SUMMARY (D-19). Statuses 200/401/403/404-with-profile-body are all "deployed".
    (3) Evidence for interpreting the natural page state (research Open Question 1): via
    Supabase MCP execute_sql, run
    `SELECT sp.role, sp.unit_id FROM staff_profiles sp JOIN auth.users u ON u.id = sp.user_id WHERE u.email = '<TEST_USER_EMAIL from .env.test>';`
    and `SELECT count(*) FROM assignment_queue;` — record both results in the SUMMARY (never
    echo credentials). This is read-only evidence; do NOT insert rows to force a populated state.
  </action>
  <verify>
    <automated>test "$(supabase functions list --project-ref zkrcjzdemdmwhearhfgg | node -e 'let d="";process.stdin.on("data",c=>d+=c).on("end",()=>{const f=JSON.parse(d).find(x=>x.slug==="assignments-queue");console.log(f?f.version:0)})')" -gt 11 && command grep -q '404-kind' scripts/probe-edge-auth.sh && bash scripts/probe-edge-auth.sh assignments-queue | command grep -E 'assignments-queue -> [0-9]{3}'</automated>
  </verify>
  <acceptance_criteria>The deployed assignments-queue VERSION exceeds the v11 baseline (RED today, drilled 2026-08-16: version is exactly 11, so `-gt 11` fails; green only by the deploy, which IS the work; baseline re-derived and pasted in the SUMMARY before deploying); `scripts/probe-edge-auth.sh` carries the 404-kind disambiguation (statically pinned in the chain) with its two control outputs pasted in the SUMMARY (known-deployed → no 404 line; known-absent slug → `404-kind: gateway`); a probe status line recorded in the SUMMARY with its 404-kind line if any; the staff_profiles/queue-count derivation recorded. No source edit to the function itself (git status clean under supabase/functions/).</acceptance_criteria>
  <done>The function is deployed; the SUMMARY carries the D-19 evidence lines.</done>
</task>

<task type="auto">
  <name>Task 3: Natural-state oracle — 95-queue-renders.spec.ts</name>
  <files>tests/e2e/95-queue-renders.spec.ts</files>
  <read_first>tests/e2e/93-tasks-queue-error.spec.ts in full — it is BOTH the pattern source (inline auth :28-36, 15s retry budget :42-54, INTERNAL_STRING regex, DOM-only assertions) AND a NAMED NON-CONSUMER by its own header :10-15 (CDP-blocked, deterministic before and after the deploy — do not touch it); pages/AssignmentQueue.tsx (the testids/roles of the four states)</read_first>
  <action>
    Write exactly 2 tests, header `// @covers DEAD-02` on line 1 plus a WHY block. NOT
    CDP-blocked (Pitfall 6 — this spec asserts the natural state against the deployed fn; it is
    an execution-time oracle whose producer is Task 2, ordered before it in this plan). Inline
    auth; 15s budget. Test 1: goto /tasks/queue; assert the page settles to EXACTLY ONE of:
    queue rows visible / the empty state / query-error-state (403 and profile-404 render the
    error state, which is truthful — the criterion is "renders against a DEPLOYED function",
    not "renders rows"); assert no spinner/skeleton remains after settle; assert body innerText
    does not match the INTERNAL_STRING regex. RECORD the observed state: the test logs a
    `QUEUE-STATE: rows|empty|error` line the runner output preserves, and the SUMMARY
    transcribes it. BOUND DISPOSITION (warning-7 binding): if the observed state is NOT rows,
    criterion 2 closes with a NAMED BOUND recorded in the 95-09 closing register — bound text:
    "criterion 2 verified as renders-against-a-deployed-function; the populated-rows render was
    not exercised (observed state: <empty|error>), explained by the Task 2
    staff_profiles/queue-count derivation" — never folded into an unqualified pass. Test 2:
    select one filter (e.g. a priority) and assert the page settles again to one of the
    truthful states with no thrown page error — this half proves the Task 1 transport (pre-fix,
    selecting a filter throws TypeError client-side).
  </action>
  <verify>
    <automated>test -f tests/e2e/95-queue-renders.spec.ts && test "$(pnpm exec playwright test tests/e2e/95-queue-renders.spec.ts --project=chromium-en --no-deps --list | command grep -c '›')" -eq 2 && pnpm exec playwright test tests/e2e/95-queue-renders.spec.ts --project=chromium-en --no-deps</automated>
  </verify>
  <acceptance_criteria>Spec exists first, lists exactly 2 tests with --no-deps, passes post-deploy. The SUMMARY records the observed `QUEUE-STATE` line; if it is not `rows`, the SUMMARY carries the NAMED BOUND text verbatim for the 95-09 closing register (the register's criterion-2 row cites it — an unbounded "criterion 2 PASS" over a non-rows observation is a REJECT). GATE LABEL: green direction UNPROVEN pre-execution — needs the Task 2 deploy plus the running app; red constructible now (undeployed fn → perpetual retry → budget exceeded or error-state-with-mismatched-expectation). 93-tasks-queue-error.spec.ts remains byte-identical (named non-consumer).</acceptance_criteria>
  <done>Criterion 2 has a natural-state oracle that only passes against a deployed function rendering truthfully, and its non-rows outcome (if that is what staging shows) closes bounded, not silently.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                  |
| --------------------------- | ------------------------------------------------------------ |
| browser → assignments-queue | JWT via apiGet Authorization header; verify_jwt default-on   |
| role gate                   | staff_profiles role check stays SERVER-side under caller RLS |

## STRIDE Threat Register

| Threat ID | Category               | Component        | Disposition | Mitigation Plan                                                                                                                            |
| --------- | ---------------------- | ---------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| T-95-03   | Spoofing               | queue transport  | mitigate    | apiGet attaches the session JWT; the fn authenticates via injected-header getUser (house pattern, unedited)                                |
| T-95-04   | Elevation of Privilege | role gate        | mitigate    | supervisor/admin scoping enforced in the fn under caller RLS — deployed unmodified; client renders whatever the server decides, truthfully |
| T-95-05   | Information Disclosure | error rendering  | mitigate    | page renders QueryErrorState (P93 retrofit stands); e2e asserts INTERNAL_STRING never matches body text                                    |
| T-95-SC   | Tampering              | package installs | accept      | zero installs this phase                                                                                                                   |

</threat_model>

<verification>
- Transport gate green (apiGet in, invoke out, type-check).
- Deployed version > 11 (the red-capable deployment oracle); probe line + 404-kind evidence in
  SUMMARY; probe-edge-auth.sh ships the disambiguation with both controls pasted.
- e2e spec: 2 tests, --no-deps, green post-deploy (execution-time; label honoured); QUEUE-STATE
  recorded and, if not rows, the named bound staged for 95-09.
</verification>

<success_criteria>
Phase success criterion 2 holds behaviourally: /tasks/queue settles to a truthful state against
a deployed assignments-queue, and filters reach the function. A non-rows natural state closes
the criterion with the named bound in the 95-09 closing register, never an unqualified pass.
</success_criteria>

<output>
Create `.planning/phases/95-routes-that-don-t-render/95-02-SUMMARY.md` when done. It MUST carry
the probe evidence line, the function-manifest confirmation, the staff_profiles/queue-count
derivation results, the observed `QUEUE-STATE` line, and — if that state is not rows — the
NAMED BOUND text for the 95-09 closing register.
</output>
