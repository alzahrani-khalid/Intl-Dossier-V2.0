---
phase: 95-routes-that-don-t-render
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/_protected/scenario-sandbox.tsx
  - frontend/src/domains/misc/hooks/useScenarioSandbox.ts # retry cap on the scenarios query
  - tests/e2e/95-sandbox-error.spec.ts # NEW
autonomous: true
requirements: [DEAD-03]
success_criterion: '3 — /scenario-sandbox either loads or shows an error; a backend 500 is never pixel-identical to still loading'
truths:
  - 'Decisions covered — D-05: the error state reuses the shared QueryErrorState component Phase 93 extracted (role="alert", bilingual, internal-string-free) — the bespoke Alert branch with its hardcoded English Retry literal is deleted; D-13: criterion 3 is OBSERVED behaviourally by tests/e2e/95-sandbox-error.spec.ts using CDP Network.setBlockedURLs, asserting the error state via DOM roles, never inferring from emptiness'
must_haves:
  truths:
    - 'A backend 500 (the deployed fn 500s FETCH_FAILED today, probe-verified) renders QueryErrorState with role="alert" and data-testid="query-error-state" — DOM-distinct from the neutral loading spinner'
    - 'The hardcoded English "Retry" literal at scenario-sandbox.tsx:319 and the legacy text-primary/text-muted-foreground classes in that branch do not survive'
    - 'Loading renders only while isLoading; the retry budget is bounded (no retry on 4xx, at most 2 on 5xx/network) so a 500 surfaces as an error within seconds, not an indefinite spinner'
    - 'No rendered text on this surface matches the internal-string pattern (42501/42703/42P01, supabase, FunctionsHttpError, stack shapes)'
  artifacts:
    - path: 'frontend/src/routes/_protected/scenario-sandbox.tsx'
      provides: 'QueryErrorState variant page wired to refetch, replacing the bespoke :312-323 branch'
      contains: 'QueryErrorState'
    - path: 'frontend/src/domains/misc/hooks/useScenarioSandbox.ts'
      provides: 'bounded retry on the scenarios query (no 4xx retry, max 2 on 5xx/network)'
      contains: 'retry'
    - path: 'tests/e2e/95-sandbox-error.spec.ts'
      provides: 'CDP forced-error oracle cloned from the proven 93 pattern'
      contains: '@covers DEAD-03'
  key_links:
    - from: 'frontend/src/routes/_protected/scenario-sandbox.tsx'
      to: 'frontend/src/components/error-states/QueryErrorState.tsx'
      via: 'variant page + onRetry={refetch} + isRetrying'
      pattern: 'QueryErrorState'
    - from: 'tests/e2e/95-sandbox-error.spec.ts'
      to: 'frontend/src/routes/_protected/scenario-sandbox.tsx'
      via: 'CDP Network.setBlockedURLs forcing the error, asserting data-testid="query-error-state"'
      pattern: 'query-error-state'
---

<objective>
Close DEAD-03 (success criterion 3): `/scenario-sandbox` either loads or shows an error — a
backend 500 is never pixel-identical to "still loading".

Probe-verified: the deployed `scenario-sandbox` fn returns 500 `FETCH_FAILED` today. The 500's
server-side root cause is UNDIAGNOSED and criterion 3 does NOT require fixing it ("loads OR
shows an error") — this plan makes the failure render truthfully. Today ~7s of default retries
precede a bespoke error branch (`scenario-sandbox.tsx:312-323`) with a hardcoded English
"Retry" literal and legacy classes — exactly the defect class P93 closed.

Purpose: criterion 3 via reuse of the shipped error vocabulary — zero new visual language.
Output: QueryErrorState retrofit + bounded retry + CDP e2e oracle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/95-routes-that-don-t-render/95-RESEARCH.md (§DEAD-03 — retry-window math, the bespoke branch, the devModeGuard note; §Open Questions 2 — the optional 500 diagnosis SQL)
@.planning/phases/95-routes-that-don-t-render/95-PATTERNS.md (§DEAD-03 — the branch being deleted, verbatim, and the replacement analogs)
@.planning/phases/95-routes-that-don-t-render/95-UI-SPEC.md (§1 — the binding contract: variant A page-level, variant B section-scoped, transition rules)

<interfaces>
Verified by research at HEAD:
- The branch to delete: `scenario-sandbox.tsx:312-323` (inline Alert, hardcoded `Retry` JSX
  literal at :319-320, legacy `text-primary`/`text-muted-foreground` classes).
- Replacement contract: `QueryErrorState.tsx:47-60` — `variant: 'page'|'inline'`, `onRetry`,
  `isRetrying`, optional bilingual `message`, testIds `query-error-state`/`query-error-inline`.
  Page-level analog: `AssignmentQueue.tsx:48`. Section-scoped analog with region gating:
  `data-retention.tsx:439-444`.
- Data flow: `useScenarios` (domains/misc/hooks/useScenarioSandbox.ts) → `getScenarios` →
  `apiGet('/scenario-sandbox?…')`; page consumes `scenariosData?.data || []` (success envelope
  `{data, pagination}` — compatible on success).
- `useUpdateScenario`/`useDeleteScenario`/`useCloneScenario` are STUB mutations resolving
  `{success:true}` without a network call — OUT OF SCOPE; no plan asserts edit/delete
  truthfulness on this page.
- Route guard: `beforeLoad: devModeGuard` redirects to /dashboard in production builds without
  VITE_DEV_MODE. ORACLE POPULATION DEFINITION: the observable surface is the dev server /
  VITE_DEV_MODE build — the e2e runs against the dev server where the guard passes.
- Copy: `common:errors.queryFailed.*` + `common:errors.retry` exist in BOTH locales — no new
  keys expected. If any new string is genuinely needed: `src/i18n/{en,ar}/scenario-sandbox.json`
  in the SAME commit, colon-form only (namespace registered for both languages,
  i18n/index.ts:342,478).
- C9b consumer, triaged by research: `frontend/tests/scenario-sandbox-verification.spec.ts`
  asserts h1/tabs on this page (live-login spec). The retrofit keeps both visible — run it once
  after Task 1 and record its colour in the SUMMARY; if red, attribute before proceeding.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the bespoke error branch with QueryErrorState; bound the retries</name>
  <files>frontend/src/routes/_protected/scenario-sandbox.tsx, frontend/src/domains/misc/hooks/useScenarioSandbox.ts</files>
  <read_first>scenario-sandbox.tsx:290-340 (the branch and its surroundings) and the page's section structure (does the comparison panel run its own query? if yes it gets variant B); useScenarioSandbox.ts (the useScenarios query options); QueryErrorState.tsx:47-60; 95-UI-SPEC.md §1 (the exact visual/transition contract); frontend/src/lib/query-client.ts (the default retry ladder being overridden)</read_first>
  <action>
    (1) In scenario-sandbox.tsx replace :312-323 with `<QueryErrorState variant="page"
    onRetry={() => void refetch()} isRetrying={isFetching} />` — the AssignmentQueue analog. The
    hardcoded `Retry` literal, the inline Alert markup, and the legacy classes in that branch
    are deleted. Remove imports the change orphans. If the comparison panel has its own query,
    give its failure `variant="inline"` (variant B) so succeeded siblings render — if it shares
    the primary query, skip (record which in the SUMMARY).
    (2) In useScenarioSandbox.ts add a bounded `retry` option to the useScenarios query:
    no retry when the error is a 4xx ApiError, at most 2 retries otherwise — the inherited
    93-UI-SPEC transition contract. Loading (spinner/skeleton) renders only while `isLoading`.
    (3) No new copy expected — QueryErrorState defaults to common:errors.queryFailed.* (both
    locales). Any genuinely new key: both locale files, same commit, colon-form.
    (4) Evidence (optional, read-only): via Supabase MCP execute_sql run
    `SELECT to_regclass('public.scenarios');` and record the result in the SUMMARY — the 500's
    root cause is out of criterion; recorded, not fixed.
    (5) Run `frontend/tests/scenario-sandbox-verification.spec.ts` once and record its colour in
    the SUMMARY (C9b triage — research expects it unaffected since h1 and tabs survive).
  </action>
  <verify>
    <automated>cd frontend && command grep -q 'QueryErrorState' src/routes/_protected/scenario-sandbox.tsx && test "$(command grep -cE '^\s*Retry\s*$' src/routes/_protected/scenario-sandbox.tsx)" -eq 0 && command grep -q 'onRetry' src/routes/_protected/scenario-sandbox.tsx && command grep -q 'retry' src/domains/misc/hooks/useScenarioSandbox.ts && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>QueryErrorState renders the page-level failure (positive pin); zero standalone `Retry` JSX literals remain (the pattern ^\s*Retry\s*$ cannot collide with the onRetry prop, and the onRetry pin in the same chain instrument-tests the file is readable); the scenarios query carries a bounded retry option; type-check clean; the scenario-sandbox-verification spec colour recorded in the SUMMARY.</acceptance_criteria>
  <done>A 500 on this page renders the shared error state within the bounded retry window; no hardcoded-English or legacy-class error markup survives.</done>
</task>

<task type="auto">
  <name>Task 2: CDP forced-error oracle — 95-sandbox-error.spec.ts</name>
  <files>tests/e2e/95-sandbox-error.spec.ts</files>
  <read_first>tests/e2e/93-tasks-queue-error.spec.ts in full — clone it wholesale: inline auth (:28-36), RETRY_BACKOFF_TIMEOUT 15s (:46-54), INTERNAL_STRING regex (:42-43), CDP block + DOM-only assertions (:62-78); 95-RESEARCH.md §DEAD-03 (the devModeGuard population note)</read_first>
  <action>
    Write exactly 2 tests, header `// @covers DEAD-03` on line 1 plus a WHY block stating the
    population: this oracle observes the dev-server surface (devModeGuard redirects on prod
    builds without VITE_DEV_MODE). Test 1 (forced): CDP `Network.setBlockedURLs` with
    `['*scenario-sandbox*']`, goto /scenario-sandbox, expect `data-testid="query-error-state"`
    visible within 15s, expect no element with a spinner/skeleton role visible alongside it,
    and body innerText does not match INTERNAL_STRING. Test 2 (natural): no CDP block; goto
    /scenario-sandbox and assert the page settles within 15s to either real content or
    query-error-state — never a spinner past the budget (the deployed fn 500s today, so the
    natural state at execution is likely the error state; both outcomes are truthful and both
    pass). DOM-only assertions; forced errors NEVER by asserting emptiness (RLS denials read as
    empty 200s — D-13).
  </action>
  <verify>
    <automated>test -f tests/e2e/95-sandbox-error.spec.ts && test "$(pnpm exec playwright test tests/e2e/95-sandbox-error.spec.ts --project=chromium-en --no-deps --list | command grep -c '›')" -eq 2 && pnpm exec playwright test tests/e2e/95-sandbox-error.spec.ts --project=chromium-en --no-deps</automated>
  </verify>
  <acceptance_criteria>Spec exists first, lists exactly 2 tests with --no-deps, passes. GATE LABEL: green direction UNPROVEN pre-execution — needs Task 1 landed (the current bespoke branch has no query-error-state testid, so the spec is red on the undone tree — the correct red). Drill both directions at execution per GATE-STANDARD C1.</acceptance_criteria>
  <done>Criterion 3 has a deterministic forced-error oracle plus a natural-state check; error is DOM-distinct from loading.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                             |
| --------------------- | ------------------------------------------------------- |
| server error → DOM    | the FETCH_FAILED envelope and any PG code never render  |

## STRIDE Threat Register

| Threat ID | Category               | Component              | Disposition | Mitigation Plan                                                                                  |
| --------- | ---------------------- | ---------------------- | ----------- | ------------------------------------------------------------------------------------------------ |
| T-95-06   | Information Disclosure | sandbox error render   | mitigate    | QueryErrorState only — i18n copy, role="alert"; spec asserts INTERNAL_STRING never matches body  |
| T-95-07   | Denial of Service      | unbounded retry ladder | mitigate    | retry capped (0 on 4xx, ≤2 otherwise) — a failing backend is no longer hammered nor masked       |
| T-95-SC   | Tampering              | package installs       | accept      | zero installs this phase                                                                         |

</threat_model>

<verification>
- Retrofit gate green (component in, literal out, retry bounded, type-check).
- e2e spec: 2 tests, --no-deps, green post-retrofit (execution-time; label honoured).
- scenario-sandbox-verification.spec.ts colour recorded (C9b triage).
</verification>

<success_criteria>
Phase success criterion 3 holds behaviourally: a backend 500 on /scenario-sandbox renders the
shared error state (role="alert", retry) within a bounded window and is DOM-distinct from
loading.
</success_criteria>

<output>
Create `.planning/phases/95-routes-that-don-t-render/95-03-SUMMARY.md` when done. It MUST record:
the to_regclass diagnosis result (recorded, not fixed), the comparison-panel variant decision,
and the scenario-sandbox-verification.spec.ts triage colour.
</output>
