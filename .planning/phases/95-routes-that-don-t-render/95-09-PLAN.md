---
phase: 95-routes-that-don-t-render
plan: 09
type: execute
wave: 2
depends_on: [95-01, 95-02, 95-03, 95-04, 95-05, 95-06, 95-07, 95-08]
files_modified:
  - scripts/c9b-sweep.sh # NEW — fail-closed C9b consumer-sweep instrument (beside gate-drill.mjs / decision-coverage.mjs; future phases inherit it)
  - .planning/phases/95-routes-that-don-t-render/95-CLOSING-REGISTER.md # NEW — this plan's own register file
  - .planning/phases/95-routes-that-don-t-render/95-DEAD-04-DECISION.md # NEW — the phase record Phase 97 reads
  - .planning/REQUIREMENTS.md # the 8 Phase 95 rows → Complete + the DEAD-04 register note; NO other rows
autonomous: true
requirements:
  [DEAD-01, DEAD-02, DEAD-03, DEAD-04, DEAD-08, DEAD-09, NOTFOUND-COMPONENT-01, RETENTION-CAST-01]
success_criterion: 'closing derivations + records — this plan implements no fixes; it derives, drills, and records for all eight'
truths:
  - 'Decisions covered — D-01: requirement coverage 8/8 is re-derived from plan frontmatter by command against the register set, each mapped to its success criterion or filed-finding close; D-02: the out-of-phase exclusion list is restated in the closing register, named not assumed; D-11: every gate in the plan set is drilled via scripts/gate-drill.mjs with both directions observed or labelled UNPROVEN with what it needs — a script green is NOT soundness evidence; D-12: every closing derivation states its population definition and what falls outside it, the route population derived mechanically from routeTree.gen.ts never a hand list, with the proxy-claimed prefixes and runtime-route blind spots stated; D-17: no two writers share an output path — 95-VERIFICATION-INDEPENDENT.md is RESERVED for the independent verifier and this plan does not claim it; D-18: decision coverage is green mechanically with a falsification drill on disk, the gate seen RED before its green is trusted; D-20: grep here is a ugrep wrapper honoring gitignore so every sweep uses command grep or explicit paths and every zero is instrument-tested against a known-present token'
must_haves:
  truths:
    - 'The closing register exists at 95-CLOSING-REGISTER.md with a terminal marker, carrying every derivation WITH its population definition, the named weakest point (ACCEPTANCE condition 10 — a uniform pass with nothing named weak is a DOWNGRADE), and the named-omissions roll-up from the eight SUMMARYs'
    - 'The DEAD-04 KEEP decision is recorded at 95-DEAD-04-DECISION.md citing RULING-P95-01 and mirrored as a REQUIREMENTS.md register note — the artifact Phase 97 criterion 4 consumes by name'
    - 'Exactly the eight Phase 95 register rows flip Pending→Complete; the diff anchored to phase-95-base shows NO other REQUIREMENTS.md row touched (Phase 92/93 rows are other phases property — a ruling, not an opportunistic fix)'
    - 'The decision-coverage falsification drill is ON DISK: the gate observed RED (a citation truth removed in a scratch copy), then GREEN restored — both outputs pasted'
    - 'The C9b consumer sweep runs ONLY through the NEW fail-closed scripts/c9b-sweep.sh — #!/usr/bin/env bash shebang + pipefail, array-derived roots (the measured zsh defect: an unquoted $ROOTS scalar is never word-split by zsh, so the sweep searched 0 files where bash finds 611 describe( hits — a false-clean sweep), a known-present-token control that exits INSTRUMENT-FAILED before any zero is trusted, and the reject-unsafe-id branch in place of the BSD-broken sed escape (GATESTD-01 worked around in the new script; GATE-STANDARD.md itself is NOT edited)'
  artifacts:
    - path: 'scripts/c9b-sweep.sh'
      provides: 'fail-closed C9b consumer sweep: bash-pinned, array roots, node_modules-pruned find, control-token self-test, unsafe-id rejection'
      contains: 'INSTRUMENT-FAILED'
    - path: '.planning/phases/95-routes-that-don-t-render/95-CLOSING-REGISTER.md'
      provides: 'the closing derivations register: route population, coverage re-derivations, gate drill, C9a/C9b sweeps, weakest point, named omissions'
      contains: 'CLOSING-REGISTER-END'
    - path: '.planning/phases/95-routes-that-don-t-render/95-DEAD-04-DECISION.md'
      provides: 'KEEP + narrow-proxy mechanism record for Phase 97, citing RULING-P95-01'
      contains: 'RULING-P95-01'
    - path: '.planning/REQUIREMENTS.md'
      provides: 'the eight rows Complete + the DEAD-04 note; nothing else touched'
      contains: 'RULING-P95-01'
  key_links:
    - from: '.planning/phases/95-routes-that-don-t-render/95-DEAD-04-DECISION.md'
      to: '.planning/phases/95-routes-that-don-t-render/95-04-SUMMARY.md'
      via: 'the record is transcribed from 95-04 SUMMARY section "DEAD-04 decision for the record" (single-writer hand-off)'
      pattern: 'DEAD-04'
    - from: '.planning/phases/95-routes-that-don-t-render/95-CLOSING-REGISTER.md'
      to: 'scripts/c9b-sweep.sh'
      via: 'the C9b candidate list in the register is the script output, never an inline shell paste'
      pattern: 'c9b-sweep'
---

<objective>
Close the phase set: derivations, drills, and records. This plan implements NO fixes — every
fix belongs to plans 95-01..95-08. It runs after all eight (wave 2), reads their SUMMARYs
(including every `## BLOCKED` section — read-and-empty is part of plan close, the P94 lesson),
and produces the closing register, the DEAD-04 record for Phase 97, and the register close-outs.

PRECONDITION (C7 anchor): the SSH-signed tag `phase-95-base` must exist at the pre-execution
HEAD — created by the execution orchestrator BEFORE wave 1 (the convention resumed at Phase 92;
signing config is user-local and present). Every anchored derivation below fails loudly with
MISSING TAG if it does not.

RESERVED PATH (D-17): `95-VERIFICATION-INDEPENDENT.md` belongs to the independent gsd-verifier
on the execution leg. This plan does not create, edit, or pre-populate it. The closing register
is this plan's OWN file.

NEW INSTRUMENT (warning-12 binding, overseer-upgraded): the C9b consumer sweep does NOT run as
an inline shell block — a rule in code is obeyed by everyone; a rule in prose only by whoever
read it. The measured defect: zsh does not word-split an unquoted `$ROOTS` scalar, so the
GATE-STANDARD inline block run under zsh searched 0 files and reported a clean sweep where bash
finds 611 `describe(` hits. This plan ships `scripts/c9b-sweep.sh` — a fail-closed, bash-pinned
script beside gate-drill.mjs/decision-coverage.mjs that future phases inherit. GATE-STANDARD.md
itself is NOT edited (GATESTD-01: worked around, never fixed mid-phase unruled — D-11).

Output: scripts/c9b-sweep.sh + 95-CLOSING-REGISTER.md + 95-DEAD-04-DECISION.md + the eight
register rows Complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/GATE-STANDARD.md (C1-C10 incl. C9a/C9b — the drill this plan runs; the C9b derivation SEMANTICS are preserved verbatim in scripts/c9b-sweep.sh, its fragile inline shell replaced)
@.planning/phases/95-routes-that-don-t-render/95-RESEARCH.md (§Route Population Derivation — the command this plan reuses VERBATIM, and its four named blind spots)
@.tickmarkr/overseer/ACCEPTANCE-P95-PLAN.md (conditions 1, 2, 4, 10 — what this plan's outputs are graded against)
@.tickmarkr/overseer/RULING-P95-01-PARK-MONITORING.md (condition 3 — the record this plan writes)

<interfaces>
- Route population command (RESEARCH verbatim — do not re-derive the derivation):
  `sed -n '/interface FileRoutesByFullPath {/,/^}/p' frontend/src/routeTree.gen.ts | command grep -oE "'[^']+':" | tr -d "':" | sort -u`
  (201 routes at research HEAD; the count changes with 95-05's consolidation — re-run, state
  the new count and the delta). BLIND SPOTS, stated as part of the rule: (1) proxy-claimed
  prefixes — second source `command grep -nE "^\s+'/[^']*': \{" frontend/vite.config.ts`
  (post-95-04 the /monitoring entry is gone; the residue is the exclusion record); (2) prod
  reverse-proxy claims — `command grep -n "location" deploy/nginx/*.conf`; (3) runtime-created
  routes — none (routing is exclusively file-based); (4) routeFileIgnorePattern-excluded files.
- Requirement re-derivation population: the nine 95-*-PLAN.md files on disk; the register set is
  REQUIREMENTS.md rows for DEAD-01..04, DEAD-08, DEAD-09, NOTFOUND-COMPONENT-01,
  RETENTION-CAST-01.
- Decision coverage: `node scripts/decision-coverage.mjs .planning/phases/95-routes-that-don-t-render .planning/phases/95-routes-that-don-t-render/95-CONTEXT.md`
  — scans frontmatter must_haves/truths/objective + matching body headings ONLY; two-digit D-NN
  tokens only.
- Gate drill: `node scripts/gate-drill.mjs` (mechanical half); the construct-the-done-state half
  is authored per gate and recorded per the GATE-STANDARD row format.
- C9b sweep interface: `[C9B_ROOT_HINT=<dir>] bash scripts/c9b-sweep.sh phase-95-base
  [extra-identifiers...]` — always invoked with an explicit `bash` prefix AND self-pinned to
  bash by shebang; `C9B_ROOT_HINT` (REQUIRED property, Task 0) overrides the roots-derivation
  base directory (default `.`); spec in Task 0 below. Known consumer for the live-identifier
  self-test: `data-retention` → tests/e2e/93-admin-surfaces-error.spec.ts (names the route and
  the fn path).
- Register row shape (current): `| DEAD-01 | Phase 95 — Routes That Don't Render | Pending |`.
- The seven exogenous uncommitted paths (.agents/skills/tickmarkr-*, .claude/skills/tickmarkr-*,
  CLAUDE.md, AGENTS.md, tickmarkr.spec.md) are NEVER staged, reverted, or committed — commits
  use explicit pathspecs only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Ship the fail-closed C9b sweep instrument — scripts/c9b-sweep.sh</name>
  <files>scripts/c9b-sweep.sh</files>
  <read_first>GATE-STANDARD.md §C9b in full — the derivation semantics (tag-anchored changed-file set, basename→identifier with the index/parent-dir rule and the $-param strip, stoplist, word-boundary grep, candidate-not-defect framing) are PRESERVED; only the fragile shell mechanics are replaced. Also the C9b amendment notes: four test roots, 591 spec files, the $-metacharacter degeneration.</read_first>
  <action>
    Create `scripts/c9b-sweep.sh`, executable, with these REQUIRED properties (each is a
    graded acceptance below):
    (1) SHELL PINNED: first line `#!/usr/bin/env bash`; `set -o pipefail` immediately after.
    The script never relies on the invoking shell — callers may use `bash scripts/c9b-sweep.sh`
    but the shebang alone must suffice. WHY (measured, 2026-08-16): zsh does not word-split an
    unquoted `$ROOTS` scalar, so the GATE-STANDARD inline block under zsh passed the whole
    multi-line string as ONE grep argument — 0 files searched vs 611 `describe(` hits under
    bash. A false-clean sweep is worse than no sweep.
    (2) ARRAY ROOTS, PRUNED FIND, REQUIRED ROOT-HINT OVERRIDE: the roots-derivation base
    directory is `"${C9B_ROOT_HINT:-.}"` — the `C9B_ROOT_HINT` env override is a REQUIRED
    property of the script, not an optional convenience (the control drill below depends on
    it, and the verify chain pins the variable name statically). Derive the test roots per
    GATE-STANDARD (`find "$base" -maxdepth 3 -type d -name tests` with
    `-path '*/node_modules/*'` excluded via `-prune` in the find expression itself, never
    post-filtered — the 94-07 lesson: an unpruned find hangs) into a bash ARRAY via
    `while IFS= read -r` (or `mapfile`), never an unquoted scalar. Exit non-zero with
    `MISSING ROOTS` if the array is empty. File lists flow `find ... -print > list` then
    `xargs /usr/bin/grep -lE ... < list` — stdin, never `xargs -a` (GNU-only; this is a BSD
    Mac).
    (3) FAIL-CLOSED CONTROL SELF-TEST BEFORE OUTPUT: before emitting ANY per-identifier
    result, run the exact same sweep machinery — same roots array, same file list, same
    `\b${id}\b` ERE grep pipeline — against the known-present control token `describe`.
    THE CONTROL TOKEN IS PINNED as the word-safe bare identifier `describe`, NOT
    `describe(`, for two measured reasons: (a) `describe(` interpolated into the
    word-boundary ERE yields `\bdescribe(\b` — an unbalanced-parentheses INVALID ERE;
    `/usr/bin/grep -lE` prints "parentheses not balanced", exits 2, and emits nothing, so
    that control would return 0 files on EVERY run and the script would exit
    INSTRUMENT-FAILED unconditionally, including in its own live self-test drill; (b)
    `describe(` is exactly the shape the UNSAFE branch in property (4) rejects — the
    control must be a token the machinery accepts. `describe` under `\bdescribe\b` is
    verified to match tests/e2e/93-admin-surfaces-error.spec.ts (and hundreds of sibling
    spec files). If the control returns 0 files, print `INSTRUMENT-FAILED: control token
    returned 0` and exit non-zero. Do NOT improvise a different control design — token,
    pipeline, and roots/file list are pinned HERE; the script REFUSES to report zeros from
    machinery that cannot see a token known to appear in hundreds of spec files.
    (3b) BRANCH ORDER (fixed — earlier branches mask later ones): 1. tag verify
    (`MISSING TAG`) → 2. roots array non-empty (`MISSING ROOTS`) → 3. control self-test
    (`INSTRUMENT-FAILED`) → 4. per-identifier output. Any drill targeting a later branch
    must satisfy every earlier branch first.
    (4) IDENTIFIER DERIVATION + HYGIENE: `$1` is the base tag (verify with
    `git rev-parse -q --verify "refs/tags/$1"` — exit `MISSING TAG` otherwise); identifiers
    derive from `git diff --name-only "$1" -- frontend/src supabase/functions backend/src`
    exactly as GATE-STANDARD specifies (basename minus extension; parent dir when basename is
    `index`; strip the leading `$` router marker; the GATE-STANDARD stoplist echoes AMBIGUOUS
    and continues). Any further args (`$2..`) are EXTRA identifiers appended through the same
    machinery. SAFETY: instead of the BSD-broken sed metacharacter escape, use the 93-15
    reject-unsafe-id check — `case "$id" in *[^A-Za-z0-9_-]*) echo "UNSAFE (triage by hand):
    $id"; continue;; esac` — an id that is not purely word-safe is REPORTED and skipped, never
    interpolated into an ERE. GATE-STANDARD.md is NOT edited (GATESTD-01 workaround lives in
    this script).
    (5) OUTPUT: word-boundary match (`\b${id}\b`) per identifier across the roots; hits print
    as `file <- consumer1 consumer2 ...` candidate lines (candidates, not defects — the caller
    triages). Zero-hit identifiers print nothing (the control test already proved the
    machinery sees).
    SELF-TEST DRILLS (run at creation, paste outputs in this plan's SUMMARY): (a) control
    fires — create a scratch directory containing an EMPTY subdirectory named `tests` (the
    roots find must SUCCEED: a dir named tests exists, but no file in it carries the control
    token), then run IN THIS REPO so branch 1 passes (`phase-95-base` verifies — a bare
    scratch repo would trip MISSING TAG, the wrong branch) with
    `C9B_ROOT_HINT=<scratch> bash scripts/c9b-sweep.sh phase-95-base` and observe
    `INSTRUMENT-FAILED` + non-zero exit — per the branch order, branches 1-2 are satisfied
    so branch 3 is the one that fires; (b) UNSAFE branch fires — pass the synthetic extra
    identifier `a.b(` and observe the UNSAFE line; (c) live identifier finds a known consumer —
    `bash scripts/c9b-sweep.sh phase-95-base data-retention` output includes
    tests/e2e/93-admin-surfaces-error.spec.ts.
  </action>
  <verify>
    <automated>test -f scripts/c9b-sweep.sh && head -1 scripts/c9b-sweep.sh | command grep -qF '#!/usr/bin/env bash' && command grep -q 'pipefail' scripts/c9b-sweep.sh && command grep -q 'INSTRUMENT-FAILED' scripts/c9b-sweep.sh && command grep -qF 'A-Za-z0-9_-' scripts/c9b-sweep.sh && command grep -q 'C9B_ROOT_HINT' scripts/c9b-sweep.sh && git rev-parse -q --verify refs/tags/phase-95-base >/dev/null && bash scripts/c9b-sweep.sh phase-95-base data-retention | command grep -q 'admin-surfaces-error'</automated>
  </verify>
  <acceptance_criteria>Script exists, bash-shebanged, pipefail set, carries the INSTRUMENT-FAILED control branch, the character-class unsafe-id check, and the C9B_ROOT_HINT override (all five statically pinned in the chain); the control token is the word-safe `describe` run through the same `\b${id}\b` ERE pipeline (never `describe(` — an invalid ERE under that pipeline AND the exact shape the UNSAFE branch rejects); the live self-test finds the known consumer (data-retention → 93-admin-surfaces-error.spec.ts) through the full array machinery; the three self-test drill outputs (control-fires via the scratch `tests` dir + C9B_ROOT_HINT, UNSAFE-fires on `a.b(`, live-hit) are pasted in the SUMMARY. GATE LABEL: the tag-dependent halves are UNPROVEN until phase-95-base exists (created before wave 1); the script's static pins are drillable at authoring. GATE-STANDARD.md byte-identical (git diff empty for that path).</acceptance_criteria>
  <done>Future C9b sweeps cannot silently search nothing: the instrument fails closed, in code, regardless of the invoking shell.</done>
</task>

<task type="auto">
  <name>Task 1: Closing derivations with population definitions (route set, coverage, drills, sweeps)</name>
  <files>.planning/phases/95-routes-that-don-t-render/95-CLOSING-REGISTER.md</files>
  <read_first>All eight SUMMARYs 95-01..95-08 IN FULL including every `## BLOCKED` section (a static green over an unread BLOCKED concealed an unmet criterion for 4h in P92 — read-and-empty or read-and-dispositioned, per SUMMARY); GATE-STANDARD C9b (the semantics scripts/c9b-sweep.sh implements); 95-RESEARCH §Route Population Derivation</read_first>
  <action>
    Verify the anchor first: `git rev-parse -q --verify refs/tags/phase-95-base >/dev/null ||
    { echo "MISSING TAG"; exit 1; }`. Then write 95-CLOSING-REGISTER.md containing, each WITH
    its population definition and what falls outside it (D-12):
    (1) ROUTE POPULATION: the verbatim command, its output count, the delta from 201, and the
    four blind spots with their second-source derivations pasted (vite proxy table residue,
    nginx locations, runtime none, ignore pattern). Instrument-test every zero against a
    known-present token (D-20; the repo grep is a gitignore-honoring ugrep wrapper — use
    command grep everywhere).
    (2) REQUIREMENT COVERAGE 8/8 re-derived by command:
    `for id in DEAD-01 DEAD-02 DEAD-03 DEAD-04 DEAD-08 DEAD-09 NOTFOUND-COMPONENT-01 RETENTION-CAST-01; do command grep -l "requirements:.*$id" .planning/phases/95-routes-that-don-t-render/95-*-PLAN.md || echo "MISSING $id"; done`
    — paste the mapping table (requirement → plan → success criterion / filed-finding close,
    from each plan's success_criterion frontmatter line). Population: the nine plan files on
    disk; outside it: nothing (the phase dir is the complete plan set).
    (3) DECISION COVERAGE with the falsification drill ON DISK (D-18): run the script (expect
    exit 0, uncovered: []); then in a SCRATCH COPY of the phase dir remove one citation truth,
    re-run, observe exit 1 with that D-NN reported uncovered, restore, re-run green — paste all
    three outputs. The plans' own tree byte-identical afterwards (git status --porcelain before
    and after).
    (4) GATE DRILL: `node scripts/gate-drill.mjs` over the plan set; paste the summary; for
    each gate record the GATE-STANDARD row (C1 red / C1 green construction / exceptions /
    verdict) — every execution-time gate that ran green during execution cites its SUMMARY
    evidence; any gate whose done state was never constructed is recorded CANNOT CONSTRUCT,
    never SOUND. A script green is NOT soundness evidence — say so in the register.
    (5) C9a/C9b: enumerate cross-plan artifacts from files_modified AND every <files> block
    across the nine plans (paste the producer→consumer table — expected: the five e2e specs,
    the two unit tests, the probe script, the fixture, each produced and consumed inside ONE
    plan; the cross-plan artifacts are the 95-04 SUMMARY section consumed by this plan's
    Task 2 and scripts/c9b-sweep.sh produced by Task 0 and consumed here). Run the C9b
    consumer sweep ONLY via `bash scripts/c9b-sweep.sh phase-95-base` (warning-12 binding:
    NEVER the inline GATE-STANDARD block — the script is the instrument; its control
    self-test must have passed, i.e. no INSTRUMENT-FAILED line, before any zero in its
    output is trusted); paste the candidate list and its triage, carrying the mock-vs-real
    column (D-16) — the known rows: generate-entry.test.ts (real, dispositioned in 95-06),
    93-tasks-queue-error.spec.ts (NAMED NON-CONSUMER by header),
    scenario-sandbox-verification.spec.ts (triaged in 95-03), tests/contract/positions-*
    (variable-name collision, non-consumers), 93-admin-surfaces-error.spec.ts (REAL, re-run
    green in 95-08), monitoring.dashboard.test.tsx (MOCKED → NON-ORACLE, dispositioned in
    95-04), version-comparison.spec.ts + consistency-resolution.spec.ts (REAL, pre-existing
    non-runnable, dispositioned in 95-05), positions-a11y-{en,ar}.spec.ts (fixme
    non-consumers, 95-05).
    (6) NAMED OMISSIONS roll-up from the SUMMARYs (at minimum: stale search validTypes deferred;
    PositionAnalyticsCard mount dropped; scenario-sandbox 500 root cause recorded-not-fixed;
    honest-format restriction to csv/json; the DEAD-02 observed-state NAMED BOUND if 95-02
    recorded one — transcribe it into the criterion-2 row verbatim) and the OUT-OF-PHASE
    exclusion restatement (D-02).
    (7) THE WEAKEST POINT, NAMED (ACCEPTANCE condition 10): derive it from what actually
    happened — the planning-time candidate is the 95-05 slots oracle's staging-data
    precondition (a truthful red that depends on seeded positions in a trigger-rendering
    status), but name whatever execution proved weakest. A uniform pass with nothing named
    weak is a DOWNGRADE.
    End the file with the literal terminal marker `CLOSING-REGISTER-END`.
  </action>
  <verify>
    <automated>git rev-parse -q --verify refs/tags/phase-95-base >/dev/null && test -f .planning/phases/95-routes-that-don-t-render/95-CLOSING-REGISTER.md && command grep -q 'CLOSING-REGISTER-END' .planning/phases/95-routes-that-don-t-render/95-CLOSING-REGISTER.md && command grep -qi 'weakest' .planning/phases/95-routes-that-don-t-render/95-CLOSING-REGISTER.md && command grep -q 'c9b-sweep' .planning/phases/95-routes-that-don-t-render/95-CLOSING-REGISTER.md && node scripts/decision-coverage.mjs .planning/phases/95-routes-that-don-t-render .planning/phases/95-routes-that-don-t-render/95-CONTEXT.md && for id in DEAD-01 DEAD-02 DEAD-03 DEAD-04 DEAD-08 DEAD-09 NOTFOUND-COMPONENT-01 RETENTION-CAST-01; do command grep -l "requirements:.*$id" .planning/phases/95-routes-that-don-t-render/95-*-PLAN.md >/dev/null || { echo "MISSING $id"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>Register exists with terminal marker, a named weakest point, AND the c9b-sweep citation (its C9b section shows the script invocation line + output, never an inline shell paste — the grep pin binds it); decision coverage exit 0 AND the falsification drill outputs (red seen, green restored) pasted in the register; requirement loop finds all eight (its failure branch names the missing id — red constructible by deleting one requirements entry in a scratch copy); every derivation in the register states its population and its blind spots; every zero instrument-tested (the C9b zeros specifically via the script's own control self-test). GATE LABEL: the drill halves referencing execution SUMMARYs are UNPROVEN pre-execution — needs the eight plans executed.</acceptance_criteria>
  <done>The phase's closing claims are re-derived, drilled, and written down with their limits — not asserted; the C9b sweep cannot have silently searched nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Records — the DEAD-04 decision file and the register close-outs</name>
  <files>.planning/phases/95-routes-that-don-t-render/95-DEAD-04-DECISION.md, .planning/REQUIREMENTS.md</files>
  <read_first>95-04-SUMMARY.md section "DEAD-04 decision for the record" (the single source this record transcribes); RULING-P95-01-PARK-MONITORING.md (cite it, do not paraphrase its conditions away); REQUIREMENTS.md rows 86-95 and the register table rows for the eight (read the exact row text before editing); the DEAD-09 supersession sub-bullet at REQUIREMENTS.md:95 (the house convention for dated register notes)</read_first>
  <action>
    (1) Write 95-DEAD-04-DECISION.md — the artifact Phase 97 criterion 4 consumes: decision
    KEEP (branch a) per RULING-P95-01-PARK-MONITORING.md; mechanism = API moved to
    /api/monitoring (backend remount inside the surviving dev/test guard) + vite /monitoring
    proxy entry deleted + the Dashboard's two enumerated calls authenticated via apiGet;
    enumeration 2/2 resolving (cite the 95-04 SUMMARY evidence); nginx carries no mirroring
    claim (cite the pasted derivation); what P97 inherits: a nav-entry decision
    (navigationData.ts:262 still links /monitoring — deliberately untouched here, P97's NAV-*
    scope). End with a terminal marker `DECISION-RECORD-END`.
    (2) REQUIREMENTS.md, two edits ONLY: (a) under the DEAD-04 entry (~line 89) add a dated
    sub-bullet in the DEAD-09-supersession house style: "RESOLVED 2026-08-16 (Phase 95):
    branch (a) KEEP + narrow proxy per `RULING-P95-01-PARK-MONITORING.md`; mechanism and
    evidence at `.planning/phases/95-routes-that-don-t-render/95-DEAD-04-DECISION.md`."
    (b) flip EXACTLY the eight Phase 95 register-table rows from Pending to Complete
    (DEAD-01..04 rows 522-525, DEAD-08 row 529, DEAD-09 row 530, NOTFOUND-COMPONENT-01 row
    ~568, RETENTION-CAST-01 row ~572 — re-locate by content, not line number). Phase 92/93
    rows and every other row are UNTOUCHED — their correction is a ruling, not an
    opportunistic fix.
    (3) Commit with explicit pathspecs only (this plan's four files_modified paths); the seven
    exogenous uncommitted paths are never staged.
  </action>
  <verify>
    <automated>test -f .planning/phases/95-routes-that-don-t-render/95-DEAD-04-DECISION.md && command grep -q 'RULING-P95-01' .planning/phases/95-routes-that-don-t-render/95-DEAD-04-DECISION.md && command grep -q 'DECISION-RECORD-END' .planning/phases/95-routes-that-don-t-render/95-DEAD-04-DECISION.md && command grep -q 'RULING-P95-01' .planning/REQUIREMENTS.md && for id in 'DEAD-01' 'DEAD-02' 'DEAD-03' 'DEAD-04' 'DEAD-08' 'DEAD-09' 'NOTFOUND-COMPONENT-01' 'RETENTION-CAST-01'; do command grep -qE "^\| $id \| Phase 95 [^|]*\| Complete \|" .planning/REQUIREMENTS.md || { echo "NOT COMPLETE: $id"; exit 1; }; done && git rev-parse -q --verify refs/tags/phase-95-base >/dev/null && test "$(git diff phase-95-base -- .planning/REQUIREMENTS.md | command grep -E '^[+-]\|' | command grep -cv 'Phase 95')" -eq 0</automated>
  </verify>
  <acceptance_criteria>The decision file exists with the ruling citation and terminal marker; all eight rows read Complete (the loop's failure branch names the row — red constructible now: every row reads Pending at planning time); the tag-anchored diff of REQUIREMENTS.md table rows shows ZERO changed rows outside Phase 95 (C7 — anchored to phase-95-base, never HEAD; the non-table DEAD-04 sub-bullet line contains "Phase 95" so it does not trip the guard); commit pathspecs explicit, exogenous paths untouched (git status shows them still uncommitted-unstaged).</acceptance_criteria>
  <done>Phase 97 has its DEAD-04 record by name; the register reflects exactly this phase's eight closes and nothing else.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                                                         |
| ------------------------- | ----------------------------------------------------------------------------------- |
| register edits → history  | REQUIREMENTS.md is shared truth — scope-anchored diff is the guard                  |
| sweep instrument → claims | a sweep that cannot see is a forged clean bill — the control self-test is the guard |

## STRIDE Threat Register

| Threat ID | Category    | Component            | Disposition | Mitigation Plan                                                                                                                                                |
| --------- | ----------- | -------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-95-20   | Tampering   | REQUIREMENTS.md rows | mitigate    | tag-anchored diff gate proves only Phase 95 rows changed; explicit pathspec commits; exogenous paths never staged                                              |
| T-95-21   | Repudiation | closing claims       | mitigate    | every derivation carries its command, its output paste, and its population definition — re-derivable, not quoted                                               |
| T-95-22   | Repudiation | C9b sweep output     | mitigate    | scripts/c9b-sweep.sh fails closed (INSTRUMENT-FAILED) when its machinery cannot see a known-present token; bash-pinned so shell semantics cannot silently vary |
| T-95-SC   | Tampering   | package installs     | accept      | zero installs this phase                                                                                                                                       |

</threat_model>

<verification>
- scripts/c9b-sweep.sh shipped with its three self-test drills recorded (control-fires, UNSAFE-fires, live-hit).
- Register file with marker, weakest point, drill outputs, population definitions, script-run C9b section.
- Decision file + eight Complete rows + zero out-of-phase row changes (tag-anchored).
- 95-VERIFICATION-INDEPENDENT.md untouched by this plan (reserved path — assert it was not created here).
</verification>

<success_criteria>
The phase closes on re-derived evidence: 8/8 requirement coverage by command, decision coverage
drilled red-then-green on disk, gates drilled per GATE-STANDARD, the route population derived
mechanically with stated blind spots, the C9b consumer sweep run through a fail-closed
instrument that future phases inherit, the DEAD-04 decision recorded where Phase 97 reads it,
and the weakest point named.
</success_criteria>

<output>
Create `.planning/phases/95-routes-that-don-t-render/95-09-SUMMARY.md` when done. It MUST state
that 95-VERIFICATION-INDEPENDENT.md was neither created nor written by this plan (the reserved
path held), and carry the three c9b-sweep.sh self-test drill outputs.
</output>
