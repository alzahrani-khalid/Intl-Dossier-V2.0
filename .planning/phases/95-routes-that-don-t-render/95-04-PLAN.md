---
phase: 95-routes-that-don-t-render
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/vite.config.ts
  - backend/src/index.ts
  - frontend/src/pages/monitoring/Dashboard.tsx
  - tests/e2e/95-monitoring-mounts.spec.ts # NEW
autonomous: true
requirements: [DEAD-04]
success_criterion: '4 — /monitoring resolves to the SPA route rather than raw proxy JSON, decision recorded'
truths:
  - 'Decisions covered — D-06: RULED KEEP the SPA route and the proxy stops claiming the bare /monitoring prefix (RULING-P95-01-PARK-MONITORING.md) — this plan carries all three attached conditions: the mechanical caller enumeration shown to resolve post-change, the deploy/ nginx check in this same plan, and the decision hand-off to the 95-09 record that Phase 97 reads'
must_haves:
  truths:
    - 'A browser document request for /monitoring reaches the SPA (content-type text/html, MonitoringDashboard mounts) — never raw backend JSON'
    - 'The Dashboard population of API callers — exactly 2, enumerated mechanically from the command-grep sweep, not a hand list — resolves after the change: both calls answer via /api/monitoring/* with the Authorization header apiGet attaches'
    - 'The backend dev/test-only guard around the monitoring contract router SURVIVES the remount — production exposes no new route; a prod backend-absent response renders per-widget inline error states, never perpetual Loading'
    - 'deploy/ nginx carries NO mirroring /monitoring location claim — re-derived in this plan with the sweep instrument-tested against the known-present location /api/ token'
  artifacts:
    - path: 'frontend/vite.config.ts'
      provides: 'the /monitoring proxy entry deleted; the generic /api entry carries the moved calls'
      contains: "'/api'"
    - path: 'backend/src/index.ts'
      provides: 'monitoringContractRouter remounted at /api/monitoring inside the surviving NODE_ENV dev/test guard'
      contains: 'api/monitoring'
    - path: 'frontend/src/pages/monitoring/Dashboard.tsx'
      provides: 'both fetches moved to apiGet(path, {baseUrl: express}) with per-widget isError inline error states'
      contains: 'apiGet'
    - path: 'tests/e2e/95-monitoring-mounts.spec.ts'
      provides: 'behavioural oracle: HTML content-type, SPA mount, both API calls resolve in-app'
      contains: '@covers DEAD-04'
  key_links:
    - from: 'frontend/src/pages/monitoring/Dashboard.tsx'
      to: 'backend/src/index.ts'
      via: 'apiGet express base → relative /api/monitoring/* → Vite generic /api proxy (dev) / nginx location /api/ (prod, zero nginx edit needed — proxy_pass forwards the full path)'
      pattern: 'api/monitoring'
    - from: 'frontend/vite.config.ts'
      to: 'frontend/src/routes/_protected/monitoring.tsx'
      via: 'deleting the /monitoring proxy entry returns document requests to the SPA'
      pattern: 'monitoring'
---

<objective>
Close DEAD-04 (success criterion 4) under RULING-P95-01: KEEP the SPA route; the proxy stops
claiming the bare `/monitoring` prefix. Mechanism (research-recommended, decided against the
mechanical enumeration as the ruling requires): MOVE the API under `/api/monitoring` rather than
narrowing the proxy — narrowing fixes dev only and silently rots if the Dashboard ever adds a
call; the moved path works in dev (generic `/api` Vite proxy) AND prod (nginx `location /api/`
proxy_pass with no URI part forwards the full path — verified nginx.prod.conf:133-138) with
zero nginx edit.

The three ruling conditions, carried in this plan's own text:
1. MECHANICAL ENUMERATION (population, not a hand list): string-literal `'/monitoring`
   occurrences under frontend/src excluding the generated tree — exactly 2 API calls at
   Dashboard.tsx:34 (`/monitoring/health`, refetch 5s) and :40 (`/monitoring/alerts`, refetch
   10s), both bare relative fetch with NO Authorization header; the only other hit is the nav
   entry (navigationData.ts:262, a route link — P97's concern). Planning-time drill 2026-08-16:
   the sweep matches exactly navigationData.ts + Dashboard.tsx — the route file itself uses
   '/_protected/monitoring' and never matched the pattern (research's "and the route file"
   clause corrected here). Dashboard.tsx imports only useQuery — no hooks files can hide
   further calls. Each of the 2
   is shown to resolve after the change (Task 2 evidence + the e2e).
2. The production reverse proxy (deploy/ nginx) is checked for a mirroring claim IN THIS PLAN
   (Task 2) — research found NO /monitoring location in any of the four conf files; re-derived
   at execution.
3. The KEEP decision + mechanism is RECORDED where Phase 97 reads it: this plan's SUMMARY
   states the decision citing RULING-P95-01; the physical phase-record file
   (95-DEAD-04-DECISION.md) and the REQUIREMENTS.md register note are WRITTEN BY PLAN 95-09
   (single-writer rule, D-17) from this plan's SUMMARY. This plan does not touch those paths.

Also fixed because the ruling's condition 1 demands resolving callers: the perpetual-loading lie
(Dashboard has NO error branch — a prod backend-absent response shows "Loading health..."
forever). Per-widget isError → inline error states.

Output: proxy-table fix + remount + authenticated Dashboard calls + e2e oracle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/95-routes-that-don-t-render/95-RESEARCH.md (§DEAD-04 — the full collision picture, the mechanism analysis, the nginx derivation)
@.planning/phases/95-routes-that-don-t-render/95-PATTERNS.md (§DEAD-04 — the exact excerpts: proxy entry, guard block, fetch shape)
@.tickmarkr/overseer/RULING-P95-01-PARK-MONITORING.md (the three binding conditions)

<interfaces>
Verified by research at HEAD:
- `frontend/vite.config.ts:130-133` — the `'/monitoring': { target: backendProxyTarget,
  changeOrigin: true }` entry to DELETE; the generic `'/api'` entry at :110-113 STAYS and
  carries the moved calls.
- `backend/src/index.ts:85` — `app.use('/monitoring', monitoringContractRouter)` inside
  `if (NODE_ENV === 'development' || 'test')` (:83, security comment :78-82) — remount at
  `/api/monitoring`; the guard and comment MUST survive.
- `Dashboard.tsx:32-42` — the two useQuery fetches via a local `fetchJSON` helper. Replace with
  `apiGet('/api/monitoring/health', { baseUrl: 'express' })` and
  `apiGet<Alert[]>('/api/monitoring/alerts', { baseUrl: 'express' })` (api-client.ts:64-79:
  express base = VITE_API_URL || '' → relative in dev → Vite /api proxy; prod nginx
  location /api/ forwards the full path). apiGet adds the Authorization header
  /monitoring/alerts requires (requireAuthHeader — presence-check only).
- `/monitoring/health` is open; `/monitoring/alerts` requires a Bearer token.
- Inline error analog: data-retention.tsx:439-444 region pattern → `QueryErrorState
  variant="inline"` per failed widget.
- nginx derivation (re-run at execution): `command grep -n "location" deploy/nginx/*.conf` —
  research result: locations are /, /health, /.well-known/acme-challenge/, /api/copilot/, /api/,
  /ws/ only. Instrument-test the zero against the known-present `location /api/`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remount at /api/monitoring, delete the proxy claim, authenticate the Dashboard calls</name>
  <files>backend/src/index.ts, frontend/vite.config.ts, frontend/src/pages/monitoring/Dashboard.tsx</files>
  <read_first>backend/src/index.ts:78-95 (the guard block and its security comment — both survive); frontend/vite.config.ts:100-134 (the proxy table); frontend/src/pages/monitoring/Dashboard.tsx in full (90 lines — the 2 fetches, the no-error-branch defect); frontend/src/lib/api-client.ts:64-118 (baseUrl express semantics); frontend/src/components/error-states/QueryErrorState.tsx:47-60</read_first>
  <action>
    (1) backend/src/index.ts:85 → `app.use('/api/monitoring', monitoringContractRouter)` — the
    NODE_ENV dev/test guard and its comment stay byte-equivalent around it.
    (2) Delete the `'/monitoring'` entry from the vite proxy table (:130-133). Touch no sibling
    entries (`/notifications-center`, `/analytics-dashboard` etc. are P96+ concerns — out of
    scope by name).
    (3) Dashboard.tsx: replace both `fetchJSON('/monitoring/...')` calls with
    `apiGet('/api/monitoring/health', { baseUrl: 'express' })` and
    `apiGet<Alert[]>('/api/monitoring/alerts', { baseUrl: 'express' })`; delete the local
    fetchJSON helper if orphaned. Add per-widget `isError` handling: each failed widget renders
    `QueryErrorState variant="inline"` (data-retention region analog) while succeeded siblings
    render — the prod backend-absent case now renders truthfully instead of "Loading health..."
    forever. Keep refetch intervals. No visual redesign; tokens/logical properties only.
    (4) Re-run the CONDITION-1 enumeration post-change and paste into the SUMMARY:
    `command grep -rn "'/monitoring" frontend/src --include='*.ts' --include='*.tsx' | command grep -v routeTree.gen`
    — expected residual population: the nav entry (navigationData.ts) ONLY; zero API-call hits
    (drilled at planning 2026-08-16: the pattern matches navigationData.ts + Dashboard.tsx today;
    the route file uses '/_protected/monitoring' and never matched; the moved calls start with
    '/api/monitoring so the pattern no longer matches them — the enumeration discriminates by
    construction).
  </action>
  <verify>
    <automated>command grep -qF "app.use('/api/monitoring'" backend/src/index.ts && command grep -B8 "app.use('/api/monitoring'" backend/src/index.ts | command grep -q "NODE_ENV" && test "$(command grep -cF "'/monitoring':" frontend/vite.config.ts)" -eq 0 && command grep -qF "'/api':" frontend/vite.config.ts && command grep -qF "baseUrl: 'express'" frontend/src/pages/monitoring/Dashboard.tsx && test "$(command grep -cF "'/monitoring/" frontend/src/pages/monitoring/Dashboard.tsx)" -eq 0 && command grep -q 'QueryErrorState' frontend/src/pages/monitoring/Dashboard.tsx && test "$(command grep -rl "'/monitoring" frontend/src --include='*.ts' --include='*.tsx' | command grep -v routeTree.gen | command grep -v navigationData | command grep -v 'routes/_protected/monitoring.tsx' | wc -l)" -eq 0 && command grep -rl "'/monitoring" frontend/src --include='*.ts' --include='*.tsx' | command grep -q navigationData</automated>
  </verify>
  <acceptance_criteria>Remount present INSIDE the surviving NODE_ENV guard (the -B8 grep binds both); the vite '/monitoring': entry count is 0 with the '/api': positive pin instrument-testing the read; Dashboard carries baseUrl express + QueryErrorState and zero old-path literals; the repo-wide enumeration reduces to exactly the nav entry + route file (the trailing navigationData pin is the instrument test proving the sweep can see hits). Sibling proxy entries untouched (git diff shows only the one deletion in vite.config.ts).</acceptance_criteria>
  <done>The SPA owns /monitoring document requests; the two API callers resolve via /api/monitoring with auth; failures render truthfully per widget.</done>
</task>

<task type="auto">
  <name>Task 2: Ruling conditions 2 — nginx check — and 1 — resolution evidence</name>
  <files></files>
  <read_first>deploy/nginx/*.conf (all four files — the location tables); 95-RESEARCH.md §DEAD-04 condition-2 derivation (nginx.prod.conf:133-138 proxy_pass semantics)</read_first>
  <action>
    (1) CONDITION 2, re-derived in this plan: run `command grep -n "location" deploy/nginx/*.conf`
    and paste the full output into the SUMMARY. Assert no `/monitoring` location exists;
    instrument-test the zero against the known-present `location /api/`. If a /monitoring claim
    HAS appeared since research, STOP and surface it — do not edit nginx silently.
    (2) CONDITION 1 resolution evidence: with the dev stack running (frontend :5173, backend per
    repo dev config — PORT=5001 per house note), run
    `curl -s -o /dev/null -w '%{http_code}' http://localhost:5173/api/monitoring/health` →
    expect 200, and the same for `/api/monitoring/alerts` WITHOUT a token → expect 401
    (requireAuthHeader presence-check — 401 here PROVES the route resolves to the backend, and
    the in-app authenticated 200 is asserted by Task 3's spec). Also
    `curl -s -o /dev/null -w '%{content_type}' http://localhost:5173/monitoring` → expect it to
    start with text/html (the SPA owns the document request). Paste all three lines into the
    SUMMARY.
    (3) CONDITION 3 hand-off: the SUMMARY MUST contain a section titled `DEAD-04 decision for
    the record` stating: KEEP per RULING-P95-01-PARK-MONITORING.md, mechanism = API moved to
    /api/monitoring + proxy entry deleted + Dashboard calls authenticated, enumeration count 2/2
    resolving. Plan 95-09 writes the phase-record file and the register note FROM this section
    (single-writer, D-17).
  </action>
  <verify>
    <automated>command grep -n "location" deploy/nginx/*.conf | command grep -q "location /api/" && test "$(command grep -n "location" deploy/nginx/*.conf | command grep -c "location /monitoring")" -eq 0</automated>
  </verify>
  <acceptance_criteria>The nginx location table is pasted in the SUMMARY with zero /monitoring claims (the location /api/ pin instrument-tests the sweep); the three curl evidence lines are in the SUMMARY (GATE LABEL: the curl half is UNPROVEN pre-execution — needs the running dev stack; run at execution, never faked); the `DEAD-04 decision for the record` section exists in the SUMMARY with the ruling citation.</acceptance_criteria>
  <done>Conditions 1 and 2 hold with pasted derivations; condition 3 is staged for the 95-09 record.</done>
</task>

<task type="auto">
  <name>Task 3: Behavioural oracle — 95-monitoring-mounts.spec.ts</name>
  <files>tests/e2e/95-monitoring-mounts.spec.ts</files>
  <read_first>tests/e2e/93-report-notfound.spec.ts (natural-state analog: inline auth, conjunction pattern); frontend/src/pages/monitoring/Dashboard.tsx (post-Task-1: the DOM nodes and testids to assert)</read_first>
  <action>
    Write exactly 2 tests, header `// @covers DEAD-04` on line 1 plus a WHY block. Inline auth,
    --no-deps, 15s budgets, natural network. Test 1 (mount): request `/monitoring` via the
    page's APIRequestContext or page.goto response — assert the response content-type starts
    with text/html AND a known MonitoringDashboard DOM node renders (pick a stable
    heading/testid from the post-Task-1 file) — never raw JSON. Test 2 (callers resolve):
    goto /monitoring while capturing responses; assert BOTH `/api/monitoring/health` and
    `/api/monitoring/alerts` responses are observed with ok status (the app's apiGet sends the
    JWT), and each widget settles to data or its inline error — no widget remains in a loading
    state past the budget, and body innerText does not match the INTERNAL_STRING regex.
  </action>
  <verify>
    <automated>test -f tests/e2e/95-monitoring-mounts.spec.ts && test "$(pnpm exec playwright test tests/e2e/95-monitoring-mounts.spec.ts --project=chromium-en --no-deps --list | command grep -c '›')" -eq 2 && pnpm exec playwright test tests/e2e/95-monitoring-mounts.spec.ts --project=chromium-en --no-deps</automated>
  </verify>
  <acceptance_criteria>Spec exists first, lists exactly 2 tests with --no-deps, passes. GATE LABEL: green direction UNPROVEN pre-execution — needs Task 1 landed + dev stack (backend included) running; red constructible now (the proxy currently swallows the document request — content-type is not HTML). Drill both directions at execution.</acceptance_criteria>
  <done>Criterion 4 has a behavioural oracle: SPA mount + both callers resolving, truthfully rendered.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                          |
| ------------------------------ | -------------------------------------------------------------------- |
| browser → /api/monitoring      | alerts requires a Bearer token; the calls NOW carry it (they did not) |
| dev/test router → production   | the NODE_ENV guard is the boundary — it must survive the remount     |

## STRIDE Threat Register

| Threat ID | Category               | Component                  | Disposition | Mitigation Plan                                                                                          |
| --------- | ---------------------- | -------------------------- | ----------- | --------------------------------------------------------------------------------------------------------- |
| T-95-08   | Elevation of Privilege | monitoring router exposure | mitigate    | remount stays INSIDE the dev/test NODE_ENV guard (gate greps guard adjacency); prod mounts nothing new    |
| T-95-09   | Spoofing               | Dashboard API calls        | mitigate    | bare unauthenticated fetches replaced by apiGet with the session JWT (ASVS V2 — the call gains the header it lacked) |
| T-95-10   | Information Disclosure | widget error rendering     | mitigate    | inline QueryErrorState per widget; no raw response bodies in JSX; spec asserts INTERNAL_STRING absent      |
| T-95-SC   | Tampering              | package installs           | accept      | zero installs this phase                                                                                   |

</threat_model>

<verification>
- Static gate green (remount-in-guard, proxy entry gone, authenticated calls, enumeration reduced).
- nginx derivation pasted, zero /monitoring claims, instrument-tested.
- e2e spec: 2 tests, --no-deps, green post-change (execution-time; labels honoured).
- SUMMARY carries the `DEAD-04 decision for the record` section for 95-09.
</verification>

<success_criteria>
Phase success criterion 4 holds behaviourally: /monitoring resolves to the SPA route, both
enumerated API callers resolve with auth, production carries no mirroring claim, and the KEEP
decision is staged for the record Phase 97 reads.
</success_criteria>

<output>
Create `.planning/phases/95-routes-that-don-t-render/95-04-SUMMARY.md` when done. It MUST carry:
the post-change enumeration paste, the nginx location paste, the three curl lines, and the
`DEAD-04 decision for the record` section citing RULING-P95-01.
</output>
