---
phase: 95-routes-that-don-t-render
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/_protected/positions/$id.tsx # becomes the slot layout
  - frontend/src/routes/_protected/positions/$id/index.tsx # NEW — default (editor) child
  - frontend/src/routes/_protected/positions/$id/approvals.tsx # stripped to panel content
  - frontend/src/routes/_protected/positions/$id/versions.tsx # stripped to panel content
  - frontend/src/routes/_protected/positions/$positionId.tsx # DELETED
  - frontend/src/routes/_protected/positions.tsx # the one route-id repoint (:306)
  - frontend/src/routes/_protected/legislation.tsx # becomes layout with Outlet
  - frontend/src/routes/_protected/legislation/index.tsx # NEW — list body child
  - frontend/src/routeTree.gen.ts # regenerated, SAME commit
  - frontend/tests/e2e/version-comparison.spec.ts # C9b REAL consumer — only if its post-consolidation re-run reds on a surface this plan moved (see Task 1 disposition rule)
  - frontend/tests/e2e/consistency-resolution.spec.ts # C9b REAL consumer — same rule
  - tests/e2e/95-slots-tabs.spec.ts # NEW
autonomous: true
requirements: [DEAD-08]
success_criterion: '5 — one route file per slot; legislation.tsx renders an Outlet; positions approvals/versions children drive tab state'
truths:
  - 'Decisions covered — D-07: slot consolidation is a tree-wide operation — the complete inbound-link population (route-id and template-string forms) is re-derived by command before the edit and every route-id consumer is repointed in the same edit, with shipped-phase test consumers enumerated per C9b; D-14: the slots spec asserts its own existence first, hardcodes its test count including for --list, and always passes --no-deps; D-16: the C9b rows carry mock-vs-real from authoring — version-comparison.spec.ts and consistency-resolution.spec.ts are REAL consumers re-run before and after in Task 1, the positions-a11y specs are NAMED non-consumers by test.fixme'
must_haves:
  truths:
    - 'Exactly one dynamic positions slot survives: $id (research survivor recommendation — one repoint vs four+, the children directory is already $id/, and $id.tsx carries the Tabs the UI-SPEC contract is written against); $positionId.tsx is DELETED with its sole PositionAnalyticsCard mount recorded as a NAMED omission'
    - 'Deep-linking /positions/:id/approvals opens with aria-selected="true" on the approvals trigger; selecting a tab navigates; browser back moves tab state; the tab strip does not remount the page header'
    - 'legislation.tsx renders <Outlet/> (the criterion text verbatim) keeping validateSearch on the layout; the legislation detail page is reachable'
    - 'routeTree.gen.ts is regenerated in the SAME commit as the file moves and is never hand-edited'
    - 'The shipped C9b consumers of the contested surfaces are dispositioned by MEASURED re-run in Task 1, not assumption: version-comparison.spec.ts and consistency-resolution.spec.ts (REAL — but pre-existing non-runnable: hardcoded creds/UUID AND asserted testids absent from the whole tree at HEAD, derived by command 2026-08-16), positions-a11y-{en,ar} (NAMED non-consumers — test.fixme)'
  artifacts:
    - path: 'frontend/src/routes/_protected/positions/$id.tsx'
      provides: 'slot layout: header + URL-driven tab strip + Outlet'
      contains: 'Outlet'
    - path: 'frontend/src/routes/_protected/legislation.tsx'
      provides: 'layout with Outlet, validateSearch retained'
      contains: 'Outlet'
    - path: 'frontend/src/routes/_protected/legislation/index.tsx'
      provides: 'the list body as the index child (reads search via getRouteApi)'
      contains: 'getRouteApi'
    - path: 'tests/e2e/95-slots-tabs.spec.ts'
      provides: 'behavioural oracle: deep-link tab state, back-moves-tabs, legislation detail reachable'
      contains: '@covers DEAD-08'
  key_links:
    - from: 'frontend/src/routes/_protected/positions.tsx'
      to: 'frontend/src/routes/_protected/positions/$id.tsx'
      via: 'the :306 navigate repointed from /positions/$positionId to /positions/$id, deleting both as-any casts'
      pattern: 'positions/\$id'
    - from: 'frontend/src/routes/_protected/positions/$id.tsx'
      to: 'frontend/src/routes/_protected/positions/$id/approvals.tsx'
      via: 'children render only through the parent Outlet; active tab derives from the matched child'
      pattern: 'Outlet'
---

<objective>
Close DEAD-08 (success criterion 5): one owner per slot. At HEAD both `positions/$id.tsx` and
`positions/$positionId.tsx` register the same dynamic level (rank-order undefined behaviour);
`$id/approvals.tsx` and `$id/versions.tsx` are unreachable in render (parent has no Outlet);
`legislation.tsx` renders no Outlet so `legislation/$id.tsx` is unreachable.

Survivor: `$id` (planner discretion per D-07, decided on the research enumeration — one
route-id repoint vs four+, the children directory already exists, the Tabs live in $id.tsx).
The consolidation is TREE-WIDE: the inbound-link population is re-derived by command before the
edit (the P-drift lesson: a plan that names 1 site when 5 need it).

DEFINED BEHAVIOUR — deep link without trigger (warning-6 binding): the approvals trigger renders
only for statuses `under_review` / `approved` / `published` ($id.tsx:130-133, the existing
conditional, kept). A deep link to `/positions/:id/approvals` for a position whose status hides
the trigger must NOT render a selected tab the strip does not offer: the approvals child
redirects (replace navigation) to `/positions/$id` — the editor index — so URL and strip never
disagree. This is implemented in Task 1 and stated in Task 3's WHY block.

Purpose: criterion 5 — reachable details, URL-driven tabs, one owner per slot.
Output: consolidated positions tree + legislation layout/child split + regen + C9b consumer
dispositions + e2e oracle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/95-routes-that-don-t-render/95-RESEARCH.md (§DEAD-08 — the state table, the COMPLETE inbound-link sweep with population definition, the target structure, TanStack 1.170.8 conventions, assumption A2/A4)
@.planning/phases/95-routes-that-don-t-render/95-PATTERNS.md (§DEAD-08 — the layout analog positions.tsx:39-45, the tab strip :124-142, the :305-308 repoint excerpt)
@.planning/phases/95-routes-that-don-t-render/95-UI-SPEC.md (§5 — the exact tab contract: same primitive, only the state source changes)

<interfaces>
Verified by research at HEAD (re-derive the sweep at execution before editing):
- Inbound-link population (command-grep sweep, template-string `/positions/${...}` and route-id
  forms, frontend/src excluding routeTree.gen and the contested files):
  - Param-name-AGNOSTIC (URL built directly — UNAFFECTED by the survivor choice):
    useQuickSwitcherSearch.ts:95, LinkedItemsList.tsx:38, AssignmentDetailsModal.tsx:102,374,
    EntityLinkManager.tsx:197, DossierSearchPage.tsx:134, entityHistoryStore.ts:217.
  - Route-id '/positions/$id' (stay correct): NewPositionDialog.tsx:239,
    routes/_protected/approvals/index.tsx:96, the two child Back links ($id/approvals.tsx:89,
    $id/versions.tsx:84 — these are STRIPPED anyway).
  - Route-id '/positions/$positionId': EXACTLY ONE — positions.tsx:306 (with two as-any casts).
  - Legislation '/legislation/$id': LegislationDetail.tsx:864, LegislationList.tsx:385,454,
    legislation.tsx:58 (create-success navigate) — all keep working; the path does not change.
- TanStack Router 1.170.8: flat file + same-named directory = parent + children; children
  render only through the parent's <Outlet/>. Active-tab derivation: useChildMatches /
  useMatchRoute or match on location.
- A2 (HeroUI gotcha): drive the existing `Tabs` value/onValueChange and navigate in the handler
  — do NOT wrap triggers in Link asChild (filterDOMProps drops aria; verify aria-selected
  renders in-browser before pinning the oracle).
- C9b CONSUMER ROWS (mock-vs-real column carried from authoring, D-16 — CORRECTED at checker
  iteration 1: the earlier claim "NO shipped test names the contested route files" was FALSE):
  | consumer | state | mock-vs-real | what it asserts | disposition |
  | `frontend/tests/e2e/version-comparison.spec.ts:43,81` | live | REAL (no mocks; drives the real app) | goto /positions/<id>/versions; testids version-1, version-2, compare-versions-button, diff-en-container, diff-ar-container, metadata-changes-table; version-status text | re-run before AND after in Task 1, named acceptance (pre-existing non-runnable — see below) |
  | `frontend/tests/e2e/consistency-resolution.spec.ts:89,148` | live | REAL | goto /positions/<id>; view-consistency-button flow | re-run before AND after in Task 1, named acceptance (same) |
  | `frontend/tests/a11y/positions-a11y-en.spec.ts` + `-ar` | fixme | — | position detail + version history a11y | NAMED NON-CONSUMERS: their contested-surface tests are `test.fixme` (editor/diff a11y debt, :42-43 and ~:55) — not runnable consumers until un-fixmed |
  | `tests/contract/positions-*` | live | MOCKED/contract | edge-fn contracts; `positionId` is a VARIABLE name, not a route file reference | triaged non-consumers (variable-name collision) |
  Both live REAL consumers are PRE-EXISTING NON-RUNNABLE for two independent reasons that
  predate this phase, derived by command 2026-08-16: (1) hardcoded credentials
  (`drafter@gastat.gov.sa` / `TestPassword123!`) and a hardcoded position UUID
  (`123e4567-e89b-12d3-a456-426614174000`) not present on staging; (2) the testids they assert
  exist NOWHERE in the tree — `command grep -rln 'compare-versions-button\|diff-en-container\|metadata-changes-table' frontend/src`
  returns zero files, and `data-testid="version-` matches nothing — the specs pin an
  aspirational DOM contract the shipped tree never rendered. Satisfying that contract is
  feature work, NOT DEAD-08 slot consolidation — explicitly out of scope. The Task 1
  disposition rule binds what the re-runs must show.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Positions slot consolidation — $id survives, URL-driven tabs, one repoint</name>
  <files>frontend/src/routes/_protected/positions/$id.tsx, frontend/src/routes/_protected/positions/$id/index.tsx, frontend/src/routes/_protected/positions/$id/approvals.tsx, frontend/src/routes/_protected/positions/$id/versions.tsx, frontend/src/routes/_protected/positions/$positionId.tsx, frontend/src/routes/_protected/positions.tsx, frontend/src/routeTree.gen.ts, frontend/tests/e2e/version-comparison.spec.ts, frontend/tests/e2e/consistency-resolution.spec.ts</files>
  <read_first>RE-DERIVE the inbound sweep FIRST and diff against the interfaces list above (command grep -rn over frontend/src for the template-string and route-id forms — if new sites appeared since research, repoint them in the same edit): positions/$id.tsx in full (254 ln — the Tabs at :124-142, the approvals-trigger status conditional at :130-133, and the header that must not remount); positions/$positionId.tsx (304 ln — note its unique PositionAnalyticsCard mount before deleting); positions/$id/approvals.tsx and versions.tsx (the standalone headers/Back links that do NOT survive; the panel content — e.g. the VersionComparison mount — that DOES); positions.tsx:300-310 (the repoint site); frontend/tests/e2e/version-comparison.spec.ts + consistency-resolution.spec.ts (the C9b REAL consumers — the URLs and asserted testids, noting those testids are absent from the tree at HEAD); frontend/playwright.config.ts (the globalSetup + webServer entries the re-runs resolve — see action 0); the TanStack file-route conventions section of 95-RESEARCH §DEAD-08</read_first>
  <action>
    (0) C9b BASELINE RE-RUN (before any edit): run each live REAL consumer in its OWN
    invocation (paths are filters, D-14 — never two paths in one command):
    `cd frontend && pnpm exec playwright test tests/e2e/version-comparison.spec.ts` and
    separately `pnpm exec playwright test tests/e2e/consistency-resolution.spec.ts`. These
    invocations resolve `frontend/playwright.config.ts`, which carries BOTH
    `globalSetup: './tests/e2e/global-setup.ts'` (pre-authenticates and writes storageState)
    and a `webServer` entry that spawns `pnpm dev` — set `E2E_BASE_URL` to the target app URL
    so the webServer spawn is avoided. THREE possible failure steps, recorded DISTINCTLY:
    (i) GLOBALSETUP ABORT — a globalSetup failure aborts the run BEFORE the spec executes;
    record that run as `UNABLE TO MEASURE (C2)`, never as a spec failure step. Note
    `--no-deps` CANNOT skip a globalSetup — it is a config hook, not a dependency project.
    (ii) the spec's login step (the expected baseline: hardcoded drafter@gastat.gov.sa creds
    that predate this phase); (iii) any later in-spec step. Their asserted testids are also
    absent from the tree, so they cannot green regardless. Record exit code + which of the
    three steps each run reached.
    (1) `$id.tsx` becomes the slot LAYOUT: keep the page header and the Tabs strip (:124-142
    visuals unchanged — the exact primitive IS the visual, including the status conditional
    :130-133 that renders the approvals trigger only for under_review/approved/published);
    content renders via `<Outlet/>`. Active tab derives from the matched child route (editor
    for the index, approvals/versions for the children); `onValueChange` navigates via
    useNavigate — NOT Link asChild (A2).
    (2) NEW `$id/index.tsx`: the default editor tab content at /positions/$id (moved out of the
    old $id.tsx body).
    (3) Strip `$id/approvals.tsx` / `$id/versions.tsx` to tab-panel content — ONLY their
    standalone headers and Back links are deleted (UI-SPEC: the tab strip does not remount the
    page header); the panel content itself (the VersionComparison mount, the approvals chain)
    survives intact — a wholesale content deletion would silently change what the C9b
    consumers could ever assert. DEEP-LINK-WITHOUT-TRIGGER (defined behaviour): the approvals
    child, when loaded for a position whose status is NOT in the trigger set, redirects
    (replace) to `/positions/$id` — URL and strip never disagree.
    (4) DELETE `$positionId.tsx`. NAMED OMISSION for the SUMMARY: it uniquely mounted
    PositionAnalyticsCard; the mount is dropped with the file (dead-or-ambiguous today),
    recorded, never silent.
    (5) Repoint positions.tsx:305-308 to `to: '/positions/$id', params: { id: position.id }`
    deleting BOTH as-any casts.
    (6) Regenerate routeTree.gen.ts (run the frontend dev server or build once — the
    TanStackRouterVite plugin regenerates; never hand-edit) and commit it IN THE SAME COMMIT as
    the file moves (Pitfall 3).
    (7) C9b POST-CONSOLIDATION RE-RUN + BOUND DISPOSITION: re-run both live consumers exactly
    as in (0) (same E2E_BASE_URL discipline, same three-step failure taxonomy). Branch (a): a
    spec fails at the SAME pre-route step before and after (login) — record it in the SUMMARY
    as REAL consumer / PRE-EXISTING NON-RUNNABLE with both outputs (the consolidation did not
    change its failure mode; its creds/UUID debt AND its absent-testid contract predate this
    phase and are NOT fixed here — building the comparison-UI testids is feature work outside
    DEAD-08). Branch (a) applies ONLY to failure steps INSIDE the spec. Branch (b): a spec
    reaches the contested surface and reds on something this task moved (a route resolution,
    a surviving panel element) — UPDATE it IN THIS TASK and re-run to the same-or-better
    step. Branch (c): globalSetup aborts (in the baseline run, the post run, or both) — the
    aborted side(s) measured NOTHING about the consumer; record `UNABLE TO MEASURE (C2)` for
    that side and NEVER fold it into branch (a) as "same step before and after". Silence, a
    changed failure mode left unexplained, or a globalSetup abort laundered into a
    disposition, is a REJECT.
  </action>
  <verify>
    <automated>cd frontend && test -f 'src/routes/_protected/positions/$id/index.tsx' && test ! -f 'src/routes/_protected/positions/$positionId.tsx' && command grep -q '<Outlet' 'src/routes/_protected/positions/$id.tsx' && test "$(command grep -c 'positionId' src/routeTree.gen.ts)" -eq 0 && command grep -qF "/positions/\$id/approvals" src/routeTree.gen.ts && test "$(command grep -cF "positionId' as any" src/routes/_protected/positions.tsx)" -eq 0 && command grep -qF "'/positions/\$id'" src/routes/_protected/positions.tsx && command grep -q 'VersionComparison' 'src/routes/_protected/positions/$id/versions.tsx' && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>One dynamic slot in the generated tree: the `positionId` count in routeTree.gen.ts is 0 — THIS zero is the regen proof and it is RED at HEAD (positionId registrations exist today); the `/positions/$id/approvals` positive pin in the same chain is an INSTRUMENT TEST proving the grep can see hits in the generated file — it is already present at HEAD (10 occurrences), so it proves the read, NOT the regen (corrected at checker iteration 1: the earlier "could only exist post-regen" claim was false). The layout renders Outlet; the repoint is typed (both as-any casts gone, the '/positions/$id' route-id pin present); the stripped versions child still mounts VersionComparison (panel-content-survives pin — present at HEAD, guards the strip from over-deleting); the re-derived inbound sweep pasted in the SUMMARY with each route-id site's disposition; the C9b before/after re-runs of version-comparison.spec.ts and consistency-resolution.spec.ts recorded per the branch rule in action (7) — each spec's disposition NAMED (pre-existing-non-runnable with identical IN-SPEC failure step, updated in this task, or UNABLE TO MEASURE (C2) on a globalSetup abort — never folded into branch (a)); type-check clean; regen and moves in one commit.</acceptance_criteria>
  <done>/positions/:id has one owner; approvals/versions render through the layout as URL-driven tabs; the shipped REAL consumers are dispositioned by measurement, not assumption.</done>
</task>

<task type="auto">
  <name>Task 2: Legislation layout + index child</name>
  <files>frontend/src/routes/_protected/legislation.tsx, frontend/src/routes/_protected/legislation/index.tsx, frontend/src/routeTree.gen.ts</files>
  <read_first>legislation.tsx in full (85 ln — the validateSearch to keep on the layout, the :58 create-success navigate); legislation/$id.tsx (the detail that becomes reachable); positions.tsx:39-45 (the layout analog to copy)</read_first>
  <action>
    `legislation.tsx` becomes a layout: `component: () => <Outlet/>` with `validateSearch`
    retained on the layout so `/legislation?type=…` deep links keep working. The list body moves
    to NEW `legislation/index.tsx`, which reads the search params via `getRouteApi` from the
    layout route. The :58 create-success navigate and every '/legislation/$id' consumer
    (LegislationDetail.tsx:864, LegislationList.tsx:385,454) keep working unchanged — the path
    does not change; re-verify by grep after the move. Regen routeTree.gen.ts in the SAME
    commit.
  </action>
  <verify>
    <automated>cd frontend && test -f src/routes/_protected/legislation/index.tsx && command grep -q '<Outlet' src/routes/_protected/legislation.tsx && command grep -q 'validateSearch' src/routes/_protected/legislation.tsx && command grep -q 'getRouteApi' src/routes/_protected/legislation/index.tsx && command grep -qF "'/legislation/'" src/routeTree.gen.ts && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>Layout renders Outlet with validateSearch retained; the index child exists and reads search via getRouteApi; the generated tree registers the '/legislation/' index path (a key that did not exist pre-split — the red direction); type-check clean.</acceptance_criteria>
  <done>The legislation detail page is reachable; list deep links with search params still work.</done>
</task>

<task type="auto">
  <name>Task 3: Behavioural oracle — 95-slots-tabs.spec.ts</name>
  <files>tests/e2e/95-slots-tabs.spec.ts</files>
  <read_first>tests/e2e/93-report-notfound.spec.ts (natural-state analog; crypto.randomUUID for absent ids); the post-Task-1 $id.tsx (trigger values/labels + the :130-133 status conditional to assert against); the post-Task-2 legislation files</read_first>
  <action>
    Write exactly 3 tests, header `// @covers DEAD-08` on line 1 plus a WHY block stating BOTH:
    (a) the DATA PRECONDITION — tests 1-2 need ≥1 position in a status that renders the
    approvals trigger (under_review/approved/published — the $id.tsx:130-133 conditional); the
    spec derives a REAL position id by visiting /positions and picking a row whose status badge
    is in that set (use the list's status filter if available); if no such position exists the
    test fails with the named message 'DATA PRECONDITION: no position in
    under_review/approved/published on staging' (a truthful red, recorded, never skipped
    silently); and (b) the DEFINED deep-link-without-trigger behaviour — /positions/:id/approvals
    for a status outside the set replace-redirects to the editor index (Task 1's defined
    behaviour); if the list scan also yields a position OUTSIDE the set, assert the redirect
    opportunistically in test 2; if none exists, record 'defined-but-not-oracled: no
    out-of-set position on staging' in the SUMMARY. Test 1 (deep-link + back): goto
    /positions/<id>/approvals — assert the approvals trigger has aria-selected="true" (A2:
    verify the attribute actually renders; if HeroUI swallows it, fix the composition per the
    plan note before pinning); click the versions tab — assert the URL ends /versions; browser
    back — assert approvals is active again and the page header did NOT remount (assert the
    header element is the same instance via a data-loaded marker or by asserting no full-page
    loading state appeared). Test 2 (tab navigation from editor): goto /positions/<id> — editor
    tab active; click approvals — URL changes and panel renders (plus the opportunistic
    redirect assertion above). Test 3 (legislation detail reachable): goto
    /legislation/<crypto.randomUUID()> — assert the LIST page's unique element (e.g. its
    create button/heading) is ABSENT and the detail route's own surface (its content, error or
    not-found state) rendered — pre-fix this URL shows the list (the child never rendered),
    which is exactly the lie this asserts against; plus goto /legislation and assert the list
    still renders (the index child works).
  </action>
  <verify>
    <automated>test -f tests/e2e/95-slots-tabs.spec.ts && test "$(pnpm exec playwright test tests/e2e/95-slots-tabs.spec.ts --project=chromium-en --no-deps --list | command grep -c '›')" -eq 3 && pnpm exec playwright test tests/e2e/95-slots-tabs.spec.ts --project=chromium-en --no-deps</automated>
  </verify>
  <acceptance_criteria>Spec exists first, lists exactly 3 tests with --no-deps, passes. GATE LABEL: green direction UNPROVEN pre-execution — needs Tasks 1-2 landed + the running app + ≥1 staging position in a trigger-rendering status (under_review/approved/published — the data precondition is a NAMED red naming that status set, not a skip). Red constructible now: at HEAD the approvals child never renders (parent has no Outlet), so test 1 fails. The deep-link-without-trigger behaviour is asserted opportunistically or recorded defined-but-not-oracled. Drill both directions at execution.</acceptance_criteria>
  <done>Criterion 5 has a behavioural oracle for deep-linked tab state, back-navigation, and legislation reachability, with its data precondition stated in terms of the statuses that actually render the trigger.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary            | Description                                                        |
| ------------------- | ------------------------------------------------------------------ |
| route tree → guards | all touched routes stay under \_protected — auth wrapper unchanged |

## STRIDE Threat Register

| Threat ID | Category  | Component                | Disposition | Mitigation Plan                                                                                                                                              |
| --------- | --------- | ------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-95-11   | Spoofing  | deep-linked child routes | mitigate    | approvals/versions/legislation-detail remain children of \_protected — the beforeLoad session check applies unchanged; the spec exercises them authenticated |
| T-95-12   | Tampering | routeTree.gen.ts         | mitigate    | generated only by TanStackRouterVite, committed same-commit, never hand-edited (gate pins post-regen registrations)                                          |
| T-95-SC   | Tampering | package installs         | accept      | zero installs this phase                                                                                                                                     |

</threat_model>

<verification>
- Static gates green (one slot, Outlets, typed repoint, regen registrations, panel-content pin).
- Re-derived inbound sweep pasted in SUMMARY; named omission (PositionAnalyticsCard) recorded.
- C9b consumer dispositions recorded from MEASURED before/after re-runs (version-comparison,
  consistency-resolution — REAL, pre-existing non-runnable; positions-a11y — fixme non-consumers).
- e2e spec: 3 tests, --no-deps, green post-consolidation (execution-time; labels honoured).
</verification>

<success_criteria>
Phase success criterion 5 holds behaviourally: one route file per slot, legislation detail
reachable through the layout Outlet, and positions approvals/versions children drive tab state
with deep-link and back-button fidelity.
</success_criteria>

<output>
Create `.planning/phases/95-routes-that-don-t-render/95-05-SUMMARY.md` when done. It MUST record:
the re-derived inbound-link sweep (population stated), the disposition of every route-id site,
the NAMED omission (PositionAnalyticsCard mount dropped with $positionId.tsx), and the C9b
consumer table with dispositions: version-comparison.spec.ts + consistency-resolution.spec.ts
(REAL — before/after re-run outputs and the branch taken; pre-existing non-runnable causes
named: hardcoded creds/UUID + asserted testids absent from the tree at HEAD; any globalSetup
abort recorded as UNABLE TO MEASURE (C2), never folded into a disposition),
positions-a11y-{en,ar}.spec.ts (NAMED non-consumers — test.fixme, editor/diff a11y debt),
tests/contract/positions-* (variable-name collision, non-consumers).
</output>
