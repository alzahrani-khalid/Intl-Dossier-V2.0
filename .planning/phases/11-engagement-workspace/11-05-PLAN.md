---
phase: 11-engagement-workspace
plan: 05
type: execute
wave: 3
depends_on: ['11-01', '11-02', '11-03', '11-04']
files_modified:
  - frontend/src/pages/engagements/workspace/AuditTab.tsx
  - frontend/src/routes/_protected/dossiers/engagements/$id.tsx
autonomous: false
requirements:
  - WORK-09
  - WORK-10
must_haves:
  truths:
    - 'Audit tab shows activity timeline scoped to this engagement with stage transitions logged'
    - '/dossiers/engagements/:id redirects to /engagements/:id/overview'
    - 'All 6 workspace tabs render correctly with engagement-scoped data'
    - 'Deep-linking to any tab URL opens that tab directly'
    - 'Mobile horizontal scroll tabs work with auto-scroll to active tab'
  artifacts:
    - path: 'frontend/src/pages/engagements/workspace/AuditTab.tsx'
      provides: 'Lifecycle transition timeline + activity log'
      min_lines: 40
    - path: 'frontend/src/routes/_protected/dossiers/engagements/$id.tsx'
      provides: 'Redirect from dossier route to workspace route'
  key_links:
    - from: 'frontend/src/pages/engagements/workspace/AuditTab.tsx'
      to: 'frontend/src/domains/engagements/hooks/useLifecycle.ts'
      via: 'useLifecycleHistory for transition records'
      pattern: 'useLifecycleHistory'
    - from: 'frontend/src/pages/engagements/workspace/AuditTab.tsx'
      to: 'frontend/src/components/engagements/LifecycleTimeline.tsx'
      via: 'Reuse LifecycleTimeline component for transition display'
      pattern: 'import.*LifecycleTimeline'
    - from: 'frontend/src/routes/_protected/dossiers/engagements/$id.tsx'
      to: '/engagements/$engagementId/overview'
      via: 'TanStack Router redirect in beforeLoad'
      pattern: 'redirect.*engagements.*overview'
---

<objective>
Build the Audit tab, wire the dossier route redirect, and perform human verification of the complete workspace.

Purpose: The Audit tab (D-04/Audit) completes the tab set. The dossier redirect (D-08) ensures old links work. Human verification confirms the entire workspace functions correctly end-to-end.

Output: AuditTab.tsx (lifecycle timeline), dossier redirect, verified workspace.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-engagement-workspace/11-CONTEXT.md
@.planning/phases/11-engagement-workspace/11-RESEARCH.md
@.planning/phases/11-engagement-workspace/11-UI-SPEC.md
@.planning/phases/11-engagement-workspace/11-01-SUMMARY.md
@.planning/phases/11-engagement-workspace/11-02-SUMMARY.md
@.planning/phases/11-engagement-workspace/11-03-SUMMARY.md
@.planning/phases/11-engagement-workspace/11-04-SUMMARY.md

<interfaces>
<!-- Key hooks and components the executor needs -->

From frontend/src/domains/engagements/hooks/useLifecycle.ts:

```typescript
export function useLifecycleHistory(engagementId: string): UseQueryResult<LifecycleTransition[]>
```

From frontend/src/components/engagements/LifecycleTimeline.tsx:

```typescript
export function LifecycleTimeline({ engagementId }: { engagementId: string }): ReactElement
```

From frontend/src/types/lifecycle.types.ts:

```typescript
export interface LifecycleTransition {
  id: string
  engagement_id: string
  from_stage: LifecycleStage | null
  to_stage: LifecycleStage
  transitioned_by: string
  transitioned_at: string
  note?: string
  user_name?: string
  duration_in_stage_seconds?: number
}
```

TanStack Router redirect pattern:

```typescript
import { createFileRoute, redirect } from '@tanstack/react-router'
export const Route = createFileRoute('/_protected/dossiers/engagements/$id')({
  beforeLoad: ({ params }) => {
    throw redirect({
      to: '/engagements/$engagementId/overview',
      params: { engagementId: params.id },
    })
  },
})
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build AuditTab + wire dossier route redirect</name>
  <files>
    frontend/src/pages/engagements/workspace/AuditTab.tsx
    frontend/src/routes/_protected/dossiers/engagements/$id.tsx
  </files>
  <read_first>
    frontend/src/pages/engagements/workspace/AuditTab.tsx
    frontend/src/components/engagements/LifecycleTimeline.tsx
    frontend/src/domains/engagements/hooks/useLifecycle.ts
    frontend/src/routes/_protected/dossiers/engagements/$id.tsx
    frontend/src/components/ui/card.tsx
    frontend/src/hooks/useDirection.ts
    frontend/src/i18n/en/workspace.json
  </read_first>
  <action>
    **AuditTab.tsx** (per D-04 Audit, L-07 in UI-SPEC, WORK-09):

    Replace stub with lifecycle transition timeline + activity log.

    **1. Data fetching:**
    - Get `engagementId` from `useParams({ from: '/_protected/engagements/$engagementId' })`
    - `useLifecycleHistory(engagementId)` — lifecycle transition records
    - `useTranslation('workspace')` for labels

    **2. Lifecycle Transitions Timeline (primary section):**
    - Heading: "Lifecycle Transitions" in `text-xl font-semibold`
    - Reuse `LifecycleTimeline` component: `<LifecycleTimeline engagementId={engagementId} />`
    - If LifecycleTimeline does not accept `engagementId` as prop and instead requires transitions data, pass `transitions` from `useLifecycleHistory` directly
    - The LifecycleTimeline already renders transitions with user info, dates, notes (per RESEARCH Don't Hand-Roll table)

    **3. Activity Log (secondary section):**
    - Heading: "Activity Log" in `text-xl font-semibold`
    - Per RESEARCH Open Question 2: For MVP, use lifecycle transitions as the sole audit data source (no dedicated activity stream endpoint yet)
    - Render lifecycle transitions as activity entries in a simpler format below the timeline:
      - Each entry: timestamp (`text-xs text-muted-foreground`), actor name, action description ("Transitioned from [stage] to [stage]"), optional note
      - Vertical list: `space-y-3`
    - If the LifecycleTimeline already shows all this info, the Activity Log section can be minimal or show a "Full activity stream coming soon" placeholder

    **4. Empty state:**
    - `t('empty.audit.heading')` and `t('empty.audit.body')` when no transitions exist

    **5. Layout:**
    - Container: `space-y-6 p-4 sm:p-6 lg:p-8`
    - RTL: `useDirection()`, logical properties only
    - No `ml-*`, `mr-*`, `text-left`, `text-right`
    - Explicit return type, default export

    ---

    **Dossier route redirect** (per D-08, Pattern 4 in RESEARCH, Pitfall 1):

    Modify `frontend/src/routes/_protected/dossiers/engagements/$id.tsx`:
    - Replace the current component rendering with a `beforeLoad` redirect
    - Import `createFileRoute`, `redirect` from `@tanstack/react-router`
    - In `beforeLoad({ params })`: throw `redirect({ to: '/engagements/$engagementId/overview', params: { engagementId: params.id } })`
    - CRITICAL: Map `params.id` to `engagementId` (Pitfall 1 — param name mismatch between dossier route `$id` and engagement route `$engagementId`)
    - Remove any component import (EngagementDossierPage etc.) — the route never renders, it always redirects

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && pnpm --filter frontend exec tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `AuditTab.tsx` contains `useLifecycleHistory` import or `LifecycleTimeline` import
    - `AuditTab.tsx` contains `useParams` import from `@tanstack/react-router`
    - `AuditTab.tsx` contains `export default function AuditTab`
    - `AuditTab.tsx` contains empty state using `t('empty.audit` keys
    - `$id.tsx` (dossier route) contains `redirect` import from `@tanstack/react-router`
    - `$id.tsx` contains `beforeLoad` in the route config
    - `$id.tsx` contains `engagementId: params.id` (param mapping)
    - `$id.tsx` contains `overview` as redirect target
    - `$id.tsx` does NOT contain a `component` property in route config (pure redirect)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Audit tab renders lifecycle transition timeline via LifecycleTimeline component + activity entries. Dossier route /dossiers/engagements/:id redirects to /engagements/:id/overview with correct param mapping.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete engagement workspace</name>
  <files>
    frontend/src/components/workspace/WorkspaceShell.tsx
    frontend/src/components/workspace/WorkspaceTabNav.tsx
    frontend/src/pages/engagements/workspace/OverviewTab.tsx
    frontend/src/pages/engagements/workspace/ContextTab.tsx
    frontend/src/pages/engagements/workspace/TasksTab.tsx
    frontend/src/pages/engagements/workspace/CalendarTab.tsx
    frontend/src/pages/engagements/workspace/DocsTab.tsx
    frontend/src/pages/engagements/workspace/AuditTab.tsx
  </files>
  <read_first>
    frontend/src/components/workspace/WorkspaceShell.tsx
    frontend/src/components/workspace/WorkspaceTabNav.tsx
  </read_first>
  <action>
    Human verification of the complete engagement workspace. Start the dev server with `cd frontend && pnpm dev`, then verify all 9 areas below.

    **What was built across Plans 01-05:**
    - WorkspaceShell (header + LifecycleBar + tab navigation)
    - 6 URL-driven tabs (Overview, Context, Tasks, Calendar, Docs, Audit)
    - LifecycleBar with popover summaries and transition interaction
    - Dossier route redirect from /dossiers/engagements/:id
    - After-action route preserved within workspace
    - Mobile horizontal scroll tabs
    - RTL support throughout

    **Verification steps:**

    1. Basic workspace rendering: Navigate to an engagement from dashboard or `/engagements/{some-id}`. Confirm WorkspaceShell renders with engagement name, LifecycleBar, and 6 tabs. Default tab is Overview (URL shows `/engagements/{id}/overview`).

    2. Tab navigation (desktop): Click each tab (Overview, Context, Tasks, Calendar, Docs, Audit). URL updates to `/engagements/{id}/{tab-name}`. Active tab has bottom border highlight. Tab content changes while shell stays persistent.

    3. Deep-linking: Copy URL `/engagements/{id}/tasks`, paste in new browser tab. Opens directly to Tasks tab.

    4. LifecycleBar interaction: Click a completed stage — popover shows transition details. Click an adjacent upcoming stage — immediate transition. Click a non-adjacent stage — inline note prompt appears.

    5. Dossier redirect: Navigate to `/dossiers/engagements/{id}`. Redirects to `/engagements/{id}/overview`.

    6. After-action route: Navigate to `/engagements/{id}/after-action`. Form renders, tab navigation hidden, LifecycleBar visible.

    7. Tab content: Overview shows metrics + participants. Context shows linked dossiers. Tasks shows kanban. Calendar shows dates. Docs shows briefs + Generate Briefing button. Audit shows lifecycle timeline.

    8. Mobile (resize to < 640px): Tabs scroll horizontally. Active tab auto-scrolls into view. LifecycleBar shows abbreviated labels.

    9. RTL (switch to Arabic): Tabs flow right-to-left. LifecycleBar stays LTR. All text aligned correctly.

    Resume signal: Type "approved" if workspace is functional, or describe any issues found.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && pnpm --filter frontend exec tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms workspace renders with all 6 tabs
    - Human confirms deep-linking works
    - Human confirms LifecycleBar interaction works
    - Human confirms dossier redirect works
    - Human confirms after-action route preserved
    - Human confirms mobile tab scrolling
    - Human confirms RTL layout
  </acceptance_criteria>
  <done>Human has verified the complete engagement workspace is functional across desktop, mobile, and RTL. All 6 tabs render, deep-linking works, lifecycle interactions work, and dossier redirect is operational.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd frontend && pnpm exec tsc --noEmit`
2. Dev server starts without errors
3. All 6 tabs navigable and render content
4. Deep-linking works for all tab URLs
5. Dossier redirect works
6. After-action route preserved
7. Mobile horizontal tabs scroll
8. RTL layout correct
</verification>

<success_criteria>

- Complete engagement workspace functional with all 6 tabs
- LifecycleBar shows popovers on completed stages
- URL-driven navigation works with deep-linking
- Dossier route redirects correctly
- After-action route works within workspace
- Mobile and RTL verified by human
- TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/11-engagement-workspace/11-05-SUMMARY.md`
</output>
