---
phase: 11-engagement-workspace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/_protected/engagements/$engagementId.tsx
  - frontend/src/routes/_protected/engagements/$engagementId/index.tsx
  - frontend/src/routes/_protected/engagements/$engagementId/overview.tsx
  - frontend/src/routes/_protected/engagements/$engagementId/context.tsx
  - frontend/src/routes/_protected/engagements/$engagementId/tasks.tsx
  - frontend/src/routes/_protected/engagements/$engagementId/calendar.tsx
  - frontend/src/routes/_protected/engagements/$engagementId/docs.tsx
  - frontend/src/routes/_protected/engagements/$engagementId/audit.tsx
  - frontend/src/components/workspace/WorkspaceShell.tsx
  - frontend/src/components/workspace/WorkspaceTabNav.tsx
  - frontend/src/components/workspace/TabSkeleton.tsx
  - frontend/src/i18n/en/workspace.json
  - frontend/src/i18n/ar/workspace.json
  - frontend/src/i18n/index.ts
autonomous: true
requirements:
  - WORK-01
  - WORK-10
must_haves:
  truths:
    - 'User sees WorkspaceShell with persistent tab navigation (Overview, Context, Tasks, Calendar, Docs, Audit) when viewing any engagement'
    - 'Workspace tabs are URL-driven via nested routes — sharing a URL like /engagements/123/tasks opens directly to the Tasks tab'
    - 'Navigating to /engagements/:id redirects to /engagements/:id/overview'
    - 'After-action child route at /engagements/:id/after-action continues to work'
    - 'All workspace tabs are lazy-loaded via route-based code splitting'
  artifacts:
    - path: 'frontend/src/components/workspace/WorkspaceShell.tsx'
      provides: 'Layout shell with engagement header, LifecycleBar, tab nav, Outlet'
      min_lines: 40
    - path: 'frontend/src/components/workspace/WorkspaceTabNav.tsx'
      provides: 'URL-driven tab navigation using TanStack Router Link elements'
      min_lines: 30
    - path: 'frontend/src/components/workspace/TabSkeleton.tsx'
      provides: 'Loading skeleton variants (summary, kanban, list, cards)'
      min_lines: 20
    - path: 'frontend/src/routes/_protected/engagements/$engagementId.tsx'
      provides: 'Layout route rendering WorkspaceShell with Outlet'
      exports: ['Route']
    - path: 'frontend/src/routes/_protected/engagements/$engagementId/index.tsx'
      provides: 'Redirect from /engagements/:id to /engagements/:id/overview'
      exports: ['Route']
  key_links:
    - from: 'frontend/src/routes/_protected/engagements/$engagementId.tsx'
      to: 'frontend/src/components/workspace/WorkspaceShell.tsx'
      via: 'imports and renders WorkspaceShell'
      pattern: 'import.*WorkspaceShell'
    - from: 'frontend/src/components/workspace/WorkspaceTabNav.tsx'
      to: '@tanstack/react-router'
      via: 'Link elements with useMatchRoute for active state'
      pattern: 'import.*Link.*useMatchRoute.*@tanstack/react-router'
    - from: 'frontend/src/routes/_protected/engagements/$engagementId/tasks.tsx'
      to: 'frontend/src/pages/engagements/workspace/TasksTab.tsx'
      via: 'React.lazy dynamic import'
      pattern: 'lazy.*import.*TasksTab'
---

<objective>
Build the WorkspaceShell layout, URL-driven tab navigation, loading skeletons, i18n namespace, and the complete nested route structure for the engagement workspace.

Purpose: This is the structural foundation that all tab content (Plans 03-05) renders inside. Without this shell and route tree, no tab content can be displayed or deep-linked.

Output: WorkspaceShell component, WorkspaceTabNav component, TabSkeleton component, 8 route files (layout + index redirect + 6 tab stubs), workspace i18n namespace (en + ar).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-engagement-workspace/11-CONTEXT.md
@.planning/phases/11-engagement-workspace/11-RESEARCH.md
@.planning/phases/11-engagement-workspace/11-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From frontend/src/types/lifecycle.types.ts:

```typescript
export type LifecycleStage =
  | 'intake'
  | 'preparation'
  | 'briefing'
  | 'execution'
  | 'follow_up'
  | 'closed'
export const LIFECYCLE_STAGES: readonly LifecycleStage[]
export interface LifecycleStepperBarProps {
  engagementId: string
  currentStage: LifecycleStage
  onTransition?: (stage: LifecycleStage) => void
  compact?: boolean
}
```

From frontend/src/domains/engagements/hooks/useEngagements.ts:

```typescript
export function useEngagement(engagementId: string): UseQueryResult<EngagementDetailResponse>
```

From frontend/src/components/engagements/LifecycleStepperBar.tsx:

```typescript
export function LifecycleStepperBar(props: LifecycleStepperBarProps): ReactElement
```

From frontend/src/components/ui/ltr-isolate.tsx:

```typescript
export function LtrIsolate({ children }: { children: ReactNode }): ReactElement
```

Route pattern from existing \_protected.tsx:

```typescript
import { createFileRoute, Outlet } from '@tanstack/react-router'
export const Route = createFileRoute('/_protected')({ component: ProtectedLayout })
function ProtectedLayout() { return <Outlet /> }
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WorkspaceShell, WorkspaceTabNav, TabSkeleton components + i18n namespace</name>
  <files>
    frontend/src/components/workspace/WorkspaceShell.tsx
    frontend/src/components/workspace/WorkspaceTabNav.tsx
    frontend/src/components/workspace/TabSkeleton.tsx
    frontend/src/i18n/en/workspace.json
    frontend/src/i18n/ar/workspace.json
    frontend/src/i18n/index.ts
  </files>
  <read_first>
    frontend/src/components/engagements/LifecycleStepperBar.tsx
    frontend/src/components/ui/ltr-isolate.tsx
    frontend/src/domains/engagements/hooks/useEngagements.ts
    frontend/src/hooks/useDirection.ts
    frontend/src/i18n/index.ts
    frontend/src/routes/_protected/engagements/$engagementId.tsx
    frontend/src/components/ui/skeleton.tsx
    frontend/src/index.css
  </read_first>
  <action>
    **WorkspaceShell.tsx** (per D-09 — new engagement-specific shell, DossierDetailLayout untouched):

    Create `frontend/src/components/workspace/` directory. Build `WorkspaceShell` as the layout container:

    1. Accept `engagementId` as prop (string) and `children` (ReactNode for Outlet content)
    2. Fetch engagement data using `useEngagement(engagementId)` from `@/domains/engagements/hooks/useEngagements`
    3. Render vertical stack structure (per L-01 in UI-SPEC):
       - **Header bar** (sticky `top-0 z-20`): engagement name (`text-3xl font-semibold`), type badge, action buttons (Transition Stage, Log After-Action). Show skeleton while loading.
       - **LifecycleBar** (sticky below header `z-[15]`): Wrap `LifecycleStepperBar` in `LtrIsolate`. Pass `engagementId`, `currentStage` from engagement data, `compact={true}` for mobile.
       - **WorkspaceTabNav** (sticky below lifecycle `z-10`): Rendered conditionally — hidden when current child route is `after-action`. Detect via `useMatchRoute` checking for `/engagements/$engagementId/after-action`.
       - **Content area**: Render `{children}` (the `<Outlet />` from the layout route)
    4. Use `useDirection()` hook for RTL. Set `dir` attribute on the root container.
    5. All text uses `useTranslation('workspace')` namespace.
    6. Padding: `px-4 sm:px-6 lg:px-8` on content area (per spacing scale md=16px mobile, xl=32px desktop).
    7. Background: `bg-background` on root container.
    8. Mobile-first: base styles for mobile, then sm/md/lg breakpoints.
    9. Touch targets: All action buttons `min-h-11 min-w-11`.
    10. Logical properties only: `ms-*`, `me-*`, `ps-*`, `pe-*`. No physical `ml-*`, `mr-*`.

    **WorkspaceTabNav.tsx** (per D-07, D-10, I-01 in UI-SPEC):

    1. Define `WORKSPACE_TABS` constant array: `[{key: 'overview', labelKey: 'tabs.overview', path: 'overview'}, {key: 'context', labelKey: 'tabs.context', path: 'context'}, {key: 'tasks', labelKey: 'tabs.tasks', path: 'tasks'}, {key: 'calendar', labelKey: 'tabs.calendar', path: 'calendar'}, {key: 'docs', labelKey: 'tabs.docs', path: 'docs'}, {key: 'audit', labelKey: 'tabs.audit', path: 'audit'}]`
    2. Accept `engagementId: string` prop.
    3. Use TanStack Router `Link` components (NOT Radix Tabs state — per anti-pattern warning).
    4. Active tab detection: Use `useMatchRoute()` to check current route against each tab path.
    5. Active state: `border-b-2 border-primary text-primary bg-background`
    6. Inactive state: `text-muted-foreground hover:text-foreground`
    7. Container: `role="tablist"` with `aria-label={t('tabs.ariaLabel')}`, `overflow-x-auto snap-x snap-mandatory` for mobile horizontal scroll (per D-10).
    8. Each tab link: `min-h-11 min-w-11 px-3 py-2 text-sm font-medium whitespace-nowrap snap-center rounded-t-md transition-colors`, with `aria-selected` attribute based on active state.
    9. Container background: `bg-muted/50 border-b`
    10. Auto-scroll active tab into view on mount: `useEffect` with `ref.scrollIntoView({ behavior: 'smooth', inline: 'center' })` on the active tab element (per D-10, Pitfall 4 in RESEARCH).
    11. RTL: Use `useDirection()` hook, set `dir` on nav container. Tab order naturally follows direction since first tab (Overview) appears at reading start.
    12. Sticky: `sticky` positioning (exact top offset passed as prop or computed from shell).

    **TabSkeleton.tsx** (per Loading Skeletons in UI-SPEC):

    1. Accept `type: 'summary' | 'kanban' | 'list' | 'cards'` prop.
    2. `summary`: 4 metric card skeletons (`grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4`) each `h-20 rounded-md animate-pulse bg-muted`, plus 2-column placeholder blocks below.
    3. `kanban`: 4 column headers + 3 card skeletons per column (`h-16 rounded-md animate-pulse bg-muted`).
    4. `list`: 6 row skeletons (`h-12 rounded-md animate-pulse bg-muted`) with staggered widths (100%, 85%, 90%, 75%, 95%, 80%).
    5. `cards`: 3 section header skeletons + 4 badge skeletons per section.
    6. Use Skeleton component from `@/components/ui/skeleton` if available, otherwise plain divs with `animate-pulse bg-muted rounded-md`.

    **i18n workspace namespace** (en + ar per Copywriting Contract in UI-SPEC):

    Create `frontend/src/i18n/en/workspace.json`:
    ```json
    {
      "tabs": {
        "overview": "Overview",
        "context": "Context",
        "tasks": "Tasks",
        "calendar": "Calendar",
        "docs": "Docs",
        "audit": "Audit",
        "ariaLabel": "Engagement workspace tabs"
      },
      "header": {
        "engagement": "Engagement",
        "loading": "Loading engagement..."
      },
      "actions": {
        "transitionStage": "Advance Stage",
        "generateBriefing": "Generate Briefing",
        "addEvent": "Add Event",
        "linkDossier": "Link Dossier",
        "logAfterAction": "Log After-Action",
        "createTask": "Create Task"
      },
      "overview": {
        "whatsNext": "What's Next",
        "keyMetrics": "Key Metrics",
        "participants": "Participants",
        "recentActivity": "Recent Activity",
        "quickActions": "Quick Actions",
        "currentStage": "Current Stage",
        "daysInStage": "Days in Stage",
        "taskProgress": "Task Progress",
        "upcomingEvent": "Next Event",
        "deadline": "Deadline"
      },
      "empty": {
        "tasks": { "heading": "No tasks yet", "body": "Create your first task for this engagement to start tracking work.", "action": "+ Create Task" },
        "calendar": { "heading": "No events scheduled", "body": "Add events to track meetings, deadlines, and milestones for this engagement.", "action": "+ Add Event" },
        "docs": { "heading": "No documents yet", "body": "Upload documents or generate an AI briefing to get started.", "action": "Generate Briefing" },
        "audit": { "heading": "No activity recorded", "body": "Activity will appear here as the engagement progresses through lifecycle stages." },
        "context": { "heading": "No linked dossiers", "body": "Link relevant countries, organizations, and topics to build the engagement context.", "action": "+ Link Dossier" }
      },
      "error": {
        "tabLoad": "Failed to load tab content. Check your connection and try again.",
        "transition": "Stage transition failed. The engagement may have been updated by another user. Refresh and try again.",
        "generateBrief": "Briefing generation failed. The AI service may be temporarily unavailable. Try again in a few minutes."
      },
      "mobile": {
        "stageAbbrev": {
          "intake": "Int",
          "preparation": "Prep",
          "briefing": "Brf",
          "execution": "Exec",
          "follow_up": "F/U",
          "closed": "Cls"
        }
      },
      "destructive": {
        "deleteEngagement": "This will permanently delete the engagement and all associated tasks, events, and documents. This action cannot be undone.",
        "backwardTransition": "Moving backward to {{stage}} will not remove any completed work items. A note is recommended to explain the reason."
      }
    }
    ```

    Create `frontend/src/i18n/ar/workspace.json` with the Arabic translations from the UI-SPEC Copywriting Contract:
    - `tabs.overview`: "نظرة عامة", `tabs.context`: "السياق", `tabs.tasks`: "المهام", `tabs.calendar`: "التقويم", `tabs.docs`: "المستندات", `tabs.audit`: "السجل"
    - `actions.transitionStage`: "تقديم المرحلة", `actions.generateBriefing`: "إنشاء ملخص", `actions.addEvent`: "إضافة حدث", `actions.linkDossier`: "ربط ملف", `actions.logAfterAction`: "تسجيل التقييم"
    - `tabs.ariaLabel`: "علامات تبويب مساحة العمل"
    - Mirror the same structure as en/workspace.json with Arabic translations for all keys.
    - `empty.*` headings and bodies translated to Arabic.
    - `error.*` messages translated to Arabic.
    - `mobile.stageAbbrev`: same abbreviated codes (Int, Prep, etc.) are fine in Arabic context.

    **Register namespace** in `frontend/src/i18n/index.ts`:
    Add `workspace` to the namespace imports and resource bundles alongside existing namespaces (follow the same pattern as `lifecycle`, `engagements`, etc.).

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && pnpm --filter frontend exec tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/workspace/WorkspaceShell.tsx` exists and contains `export function WorkspaceShell`
    - `frontend/src/components/workspace/WorkspaceShell.tsx` contains `useEngagement(engagementId)` call
    - `frontend/src/components/workspace/WorkspaceShell.tsx` contains `LifecycleStepperBar` import
    - `frontend/src/components/workspace/WorkspaceShell.tsx` contains `LtrIsolate` import
    - `frontend/src/components/workspace/WorkspaceShell.tsx` contains `useDirection` import
    - `frontend/src/components/workspace/WorkspaceShell.tsx` contains `after-action` string for conditional tab hiding
    - `frontend/src/components/workspace/WorkspaceShell.tsx` contains `sticky` class for header
    - `frontend/src/components/workspace/WorkspaceTabNav.tsx` exists and contains `export function WorkspaceTabNav`
    - `frontend/src/components/workspace/WorkspaceTabNav.tsx` contains `Link` import from `@tanstack/react-router`
    - `frontend/src/components/workspace/WorkspaceTabNav.tsx` contains `useMatchRoute` import
    - `frontend/src/components/workspace/WorkspaceTabNav.tsx` contains `role="tablist"`
    - `frontend/src/components/workspace/WorkspaceTabNav.tsx` contains `aria-selected`
    - `frontend/src/components/workspace/WorkspaceTabNav.tsx` contains `scrollIntoView`
    - `frontend/src/components/workspace/WorkspaceTabNav.tsx` contains `overflow-x-auto`
    - `frontend/src/components/workspace/TabSkeleton.tsx` exists and contains `export function TabSkeleton`
    - `frontend/src/components/workspace/TabSkeleton.tsx` contains `summary` and `kanban` and `list` and `cards` string literals
    - `frontend/src/i18n/en/workspace.json` exists and contains `"overview"` key
    - `frontend/src/i18n/ar/workspace.json` exists and contains `"نظرة عامة"` string
    - `frontend/src/i18n/index.ts` contains `workspace` string
    - No `ml-*`, `mr-*`, `text-left`, `text-right` in any workspace component files
    - No `useState` for active tab tracking in WorkspaceTabNav (URL-driven per D-07)
  </acceptance_criteria>
  <done>WorkspaceShell renders engagement header + LifecycleBar + tab nav + Outlet area. WorkspaceTabNav uses TanStack Router Links with URL-driven active state, horizontal scroll on mobile, auto-scroll to active tab. TabSkeleton provides 4 loading variants. i18n namespace registered with en + ar translations.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure engagement routes — layout route + index redirect + 6 lazy tab routes</name>
  <files>
    frontend/src/routes/_protected/engagements/$engagementId.tsx
    frontend/src/routes/_protected/engagements/$engagementId/index.tsx
    frontend/src/routes/_protected/engagements/$engagementId/overview.tsx
    frontend/src/routes/_protected/engagements/$engagementId/context.tsx
    frontend/src/routes/_protected/engagements/$engagementId/tasks.tsx
    frontend/src/routes/_protected/engagements/$engagementId/calendar.tsx
    frontend/src/routes/_protected/engagements/$engagementId/docs.tsx
    frontend/src/routes/_protected/engagements/$engagementId/audit.tsx
  </files>
  <read_first>
    frontend/src/routes/_protected/engagements/$engagementId.tsx
    frontend/src/routes/_protected/engagements/$engagementId/after-action.tsx
    frontend/src/routes/_protected/engagements/index.tsx
    frontend/src/routes/_protected.tsx
    frontend/src/components/workspace/WorkspaceShell.tsx
    frontend/src/components/workspace/TabSkeleton.tsx
  </read_first>
  <action>
    **Convert $engagementId.tsx to layout route** (per D-07, Pattern 1 in RESEARCH):

    Replace the current content of `frontend/src/routes/_protected/engagements/$engagementId.tsx`:
    - Currently it renders `EngagementDetailPage` directly as the component
    - Convert to a layout route that renders `WorkspaceShell` with `<Outlet />`
    - Import `createFileRoute`, `Outlet` from `@tanstack/react-router`
    - Import `WorkspaceShell` from `@/components/workspace/WorkspaceShell`
    - The route component function extracts `engagementId` from `Route.useParams()`
    - Returns `<WorkspaceShell engagementId={engagementId}><Outlet /></WorkspaceShell>`
    - Explicit return type: `ReactElement`

    **Create index.tsx redirect** (per D-07 — default route redirects to overview):

    Create `frontend/src/routes/_protected/engagements/$engagementId/index.tsx`:
    - Import `createFileRoute`, `redirect` from `@tanstack/react-router`
    - Use `beforeLoad` to throw redirect to `/engagements/$engagementId/overview` with `params: { engagementId: params.engagementId }`
    - This handles existing Operations Hub links that navigate to `/engagements/${id}` without tab suffix

    **Create 6 lazy-loaded tab route files** (per Pattern 3 in RESEARCH):

    Each file follows the same pattern. Create these files in `frontend/src/routes/_protected/engagements/$engagementId/`:

    `overview.tsx`:
    - `createFileRoute('/_protected/engagements/$engagementId/overview')`
    - `React.lazy(() => import('@/pages/engagements/workspace/OverviewTab'))`
    - Wrapped in `<Suspense fallback={<TabSkeleton type="summary" />}>`

    `context.tsx`:
    - `createFileRoute('/_protected/engagements/$engagementId/context')`
    - `React.lazy(() => import('@/pages/engagements/workspace/ContextTab'))`
    - `<Suspense fallback={<TabSkeleton type="cards" />}>`

    `tasks.tsx`:
    - `createFileRoute('/_protected/engagements/$engagementId/tasks')`
    - `React.lazy(() => import('@/pages/engagements/workspace/TasksTab'))`
    - `<Suspense fallback={<TabSkeleton type="kanban" />}>`

    `calendar.tsx`:
    - `createFileRoute('/_protected/engagements/$engagementId/calendar')`
    - `React.lazy(() => import('@/pages/engagements/workspace/CalendarTab'))`
    - `<Suspense fallback={<TabSkeleton type="list" />}>`

    `docs.tsx`:
    - `createFileRoute('/_protected/engagements/$engagementId/docs')`
    - `React.lazy(() => import('@/pages/engagements/workspace/DocsTab'))`
    - `<Suspense fallback={<TabSkeleton type="list" />}>`

    `audit.tsx`:
    - `createFileRoute('/_protected/engagements/$engagementId/audit')`
    - `React.lazy(() => import('@/pages/engagements/workspace/AuditTab'))`
    - `<Suspense fallback={<TabSkeleton type="list" />}>`

    **Create placeholder tab page components** so routes resolve (actual content in Plans 03-05):

    Create `frontend/src/pages/engagements/workspace/` directory with these stub files:
    - `OverviewTab.tsx`: `export default function OverviewTab(): ReactElement { return <div>Overview tab placeholder</div> }`
    - `ContextTab.tsx`: same pattern
    - `TasksTab.tsx`: same pattern
    - `CalendarTab.tsx`: same pattern
    - `DocsTab.tsx`: same pattern
    - `AuditTab.tsx`: same pattern

    Each stub must use `export default` (required for React.lazy dynamic import).

    **Verify after-action route still works:**
    The existing `frontend/src/routes/_protected/engagements/$engagementId/after-action.tsx` is already in the correct directory. Since `$engagementId.tsx` is now a layout route, `after-action.tsx` becomes a child route automatically. The WorkspaceShell conditionally hides tab navigation for this route (implemented in Task 1).

    **Explicit return types** on all component functions (per ESLint `@typescript-eslint/explicit-function-return-type` rule). Import `ReactElement` from `react`.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && pnpm --filter frontend exec tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/routes/_protected/engagements/$engagementId.tsx` contains `Outlet` import from `@tanstack/react-router`
    - `frontend/src/routes/_protected/engagements/$engagementId.tsx` contains `WorkspaceShell` import
    - `frontend/src/routes/_protected/engagements/$engagementId.tsx` does NOT contain `EngagementDetailPage` import (old behavior removed)
    - `frontend/src/routes/_protected/engagements/$engagementId/index.tsx` contains `redirect` and `beforeLoad`
    - `frontend/src/routes/_protected/engagements/$engagementId/index.tsx` contains `overview` as redirect target
    - `frontend/src/routes/_protected/engagements/$engagementId/overview.tsx` contains `React.lazy`
    - `frontend/src/routes/_protected/engagements/$engagementId/overview.tsx` contains `TabSkeleton` import
    - `frontend/src/routes/_protected/engagements/$engagementId/tasks.tsx` contains `React.lazy`
    - `frontend/src/routes/_protected/engagements/$engagementId/context.tsx` contains `React.lazy`
    - `frontend/src/routes/_protected/engagements/$engagementId/calendar.tsx` contains `React.lazy`
    - `frontend/src/routes/_protected/engagements/$engagementId/docs.tsx` contains `React.lazy`
    - `frontend/src/routes/_protected/engagements/$engagementId/audit.tsx` contains `React.lazy`
    - `frontend/src/routes/_protected/engagements/$engagementId/after-action.tsx` still exists (not deleted)
    - `frontend/src/pages/engagements/workspace/OverviewTab.tsx` exists with `export default`
    - All 6 stub tab files exist in `frontend/src/pages/engagements/workspace/`
    - TypeScript compiles without errors (`tsc --noEmit` exits 0)
  </acceptance_criteria>
  <done>$engagementId.tsx is a layout route rendering WorkspaceShell + Outlet. Index route redirects to overview. All 6 tab routes are lazy-loaded with Suspense fallbacks. After-action child route preserved. Stub tab pages exist so routes resolve.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd frontend && pnpm exec tsc --noEmit`
2. Route tree generates: TanStack Router auto-generates route tree on dev server start
3. Navigate to `/engagements/any-id` — should redirect to `/engagements/any-id/overview`
4. Navigate to `/engagements/any-id/tasks` — should show tasks tab stub with WorkspaceShell chrome
5. Navigate to `/engagements/any-id/after-action` — should show after-action form without tab navigation
6. All workspace components use logical properties only (no `ml-*`, `mr-*`)
</verification>

<success_criteria>

- WorkspaceShell renders with engagement header, LifecycleBar (in LtrIsolate), and tab navigation
- 6 workspace tabs are individually addressable via URL (deep-linkable)
- Tab navigation uses TanStack Router Links (not useState)
- All tabs lazy-loaded via React.lazy with Suspense fallbacks
- After-action route works within the workspace layout (tabs hidden)
- i18n namespace registered with en + ar translations
- TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/11-engagement-workspace/11-01-SUMMARY.md`
</output>
