---
phase: 11-engagement-workspace
plan: 04
type: execute
wave: 2
depends_on: ['11-01']
files_modified:
  - frontend/src/pages/engagements/workspace/TasksTab.tsx
  - frontend/src/pages/engagements/workspace/CalendarTab.tsx
  - frontend/src/pages/engagements/workspace/DocsTab.tsx
autonomous: true
requirements:
  - WORK-06
  - WORK-07
  - WORK-08
must_haves:
  truths:
    - 'Tasks tab shows scoped kanban filtered to this engagement with columns grouped by workflow stage'
    - 'Calendar tab shows events for this engagement with Add Event action'
    - 'Docs tab shows documents organized by stage with upload capability and Generate Briefing AI action'
  artifacts:
    - path: 'frontend/src/pages/engagements/workspace/TasksTab.tsx'
      provides: 'Inline kanban board using useEngagementKanban with drag-and-drop'
      min_lines: 50
    - path: 'frontend/src/pages/engagements/workspace/CalendarTab.tsx'
      provides: 'Engagement dates and linked events in chronological list'
      min_lines: 40
    - path: 'frontend/src/pages/engagements/workspace/DocsTab.tsx'
      provides: 'Briefs + files organized by lifecycle stage with Generate Briefing action'
      min_lines: 50
  key_links:
    - from: 'frontend/src/pages/engagements/workspace/TasksTab.tsx'
      to: 'frontend/src/domains/engagements/hooks/useEngagementKanban.ts'
      via: 'useEngagementKanban for columns, stats, handleDragEnd'
      pattern: 'useEngagementKanban'
    - from: 'frontend/src/pages/engagements/workspace/DocsTab.tsx'
      to: 'frontend/src/domains/engagements/hooks/useEngagementBriefs.ts'
      via: 'useEngagementBriefs for listing, useGenerateEngagementBrief for AI generation'
      pattern: 'useEngagementBriefs|useGenerateEngagementBrief'
---

<objective>
Build the Tasks, Calendar, and Docs tab content components for the engagement workspace.

Purpose: These three tabs represent the operational core — task management (D-03), scheduling (D-04/Calendar), and document/briefing management (D-04/Docs). Tasks tab embeds a full kanban board replacing the dialog-based EngagementKanbanDialog.

Output: TasksTab.tsx (inline kanban), CalendarTab.tsx (engagement events), DocsTab.tsx (briefs + files with AI generation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-engagement-workspace/11-CONTEXT.md
@.planning/phases/11-engagement-workspace/11-RESEARCH.md
@.planning/phases/11-engagement-workspace/11-UI-SPEC.md
@.planning/phases/11-engagement-workspace/11-01-SUMMARY.md

<interfaces>
<!-- Key hooks and components the executor needs -->

From frontend/src/domains/engagements/hooks/useEngagementKanban.ts:

```typescript
export type WorkflowStage = 'todo' | 'in_progress' | 'review' | 'done' | 'cancelled'
export type KanbanColumns = Record<WorkflowStage, KanbanAssignment[]>
export function useEngagementKanban(engagementId: string): {
  columns: KanbanColumns | undefined
  isLoading: boolean
  assignments: KanbanAssignment[]
  stats: {
    todo: number
    in_progress: number
    review: number
    done: number
    total: number
    completionPercent: number
  }
  handleDragEnd: (assignmentId: string, newStage: WorkflowStage) => void
  sortBy: SortOption
  setSortBy: (sort: SortOption) => void
}
```

From frontend/src/domains/engagements/hooks/useEngagementBriefs.ts:

```typescript
export function useEngagementBriefs(engagementId: string): UseQueryResult<Brief[]>
export function useGenerateEngagementBrief(): UseMutationResult<
  Brief,
  Error,
  { engagementId: string }
>
export function useEngagementBriefContext(engagementId: string): UseQueryResult<BriefContext>
```

From frontend/src/domains/engagements/hooks/useEngagements.ts:

```typescript
export function useEngagement(engagementId: string): UseQueryResult<EngagementDetailResponse>
// EngagementDetailResponse includes: start_date, end_date, lifecycle_stage, etc.
```

From frontend/src/components/engagements/EngagementBriefsSection.tsx:

```typescript
export function EngagementBriefsSection({ engagementId }: { engagementId: string }): ReactElement
```

From frontend/src/components/assignments/EngagementKanbanDialog.tsx:
// Reference for kanban UI patterns — extract column rendering logic

```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build TasksTab — inline kanban board with drag-and-drop</name>
  <files>
    frontend/src/pages/engagements/workspace/TasksTab.tsx
  </files>
  <read_first>
    frontend/src/pages/engagements/workspace/TasksTab.tsx
    frontend/src/domains/engagements/hooks/useEngagementKanban.ts
    frontend/src/components/assignments/EngagementKanbanDialog.tsx
    frontend/src/components/ui/card.tsx
    frontend/src/components/ui/badge.tsx
    frontend/src/components/ui/button.tsx
    frontend/src/hooks/useDirection.ts
    frontend/src/i18n/en/workspace.json
    frontend/src/i18n/en/assignments.json
  </read_first>
  <action>
    Replace the stub in `frontend/src/pages/engagements/workspace/TasksTab.tsx` with a full inline kanban board (per D-03, L-04, I-04 in UI-SPEC):

    **1. Data fetching:**
    - Get `engagementId` from `useParams({ from: '/_protected/engagements/$engagementId' })`
    - `useEngagementKanban(engagementId)` — returns `columns`, `stats`, `handleDragEnd`, `isLoading`, `sortBy`, `setSortBy`
    - `useTranslation('workspace')` and `useTranslation('assignments')` for labels

    **2. Kanban board layout:**
    - **Desktop (md+):** Horizontal columns using `flex gap-4 overflow-x-auto` (per L-04)
      - 4 visible columns: Todo, In Progress, Review, Done (skip Cancelled unless non-empty)
      - Each column: `min-w-[280px] flex-shrink-0 bg-muted/30 rounded-lg p-3`
      - Column header: stage name in `text-sm font-semibold` + count badge
      - Cards stack vertically: `space-y-2`
    - **Mobile (< md):** Single-column stacked view
      - Each workflow stage is a collapsible section (per L-04)
      - Section header: stage name + count, tap to expand/collapse
      - Drag disabled on touch — each card has a "Move to..." dropdown/menu for stage changes

    **3. Kanban card rendering:**
    - Each `KanbanAssignment` renders as a Card:
      - Title: assignment title in `text-sm font-medium`
      - Priority badge: color-coded (urgent=red, high=orange, medium=yellow, low=gray)
      - Assignee: name or avatar if available
      - Deadline: formatted date if present, `text-xs text-muted-foreground`
    - Card: `p-3 rounded-md border bg-card cursor-grab active:cursor-grabbing shadow-sm hover:shadow-md transition-shadow`
    - Min touch target: card is naturally > 44px tall

    **4. Drag-and-drop (desktop only):**
    - Use `@dnd-kit/core` — import `DndContext`, `DragOverlay`, `useSensor`, `useSensors`, `PointerSensor`
    - Reference existing `EngagementKanbanDialog` for the dnd-kit setup pattern
    - On drag end: call `handleDragEnd(assignmentId, newStage)` from the hook
    - Visual feedback during drag: card gets `shadow-lg opacity-75`, drop zone gets `border-primary/50 border-dashed border-2`
    - Use `DragOverlay` for the ghost card during drag

    **5. Stats bar (above columns):**
    - Show `stats.total` total, `stats.completionPercent`% complete
    - Sort dropdown: `sortBy` value, onChange calls `setSortBy`
    - `[+ Create Task]` button (`variant="outline"`, `size="sm"`) — placeholder action

    **6. Empty state:**
    - When `columns` is empty/all-empty: show `t('empty.tasks.heading')`, `t('empty.tasks.body')`, `t('empty.tasks.action')` button
    - Center the empty state vertically in the tab content area

    **7. Loading state:**
    - When `isLoading`: show `TabSkeleton type="kanban"` (already imported by route)

    **8. RTL and responsive:**
    - Use `useDirection()`. In RTL, kanban columns flow right-to-left (first column = reading start). No `.reverse()` — flex-row with RTL handles it.
    - All logical properties. No `ml-*`, `mr-*`, `text-left`, `text-right`.
    - Touch targets: all interactive elements `min-h-11`.

    **9. Export:** `export default function TasksTab(): ReactElement`
  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && pnpm --filter frontend exec tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `TasksTab.tsx` contains `useEngagementKanban` import
    - `TasksTab.tsx` contains `useParams` import from `@tanstack/react-router`
    - `TasksTab.tsx` contains `DndContext` import from `@dnd-kit/core`
    - `TasksTab.tsx` contains `handleDragEnd` reference
    - `TasksTab.tsx` contains `export default function TasksTab`
    - `TasksTab.tsx` contains `min-w-[280px]` or similar column min-width
    - `TasksTab.tsx` contains `overflow-x-auto` for horizontal scroll
    - `TasksTab.tsx` contains empty state using `t('empty.tasks` keys
    - No `ml-*`, `mr-*`, `text-left`, `text-right` in the file
    - No `.reverse()` call on any array
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Tasks tab renders full inline kanban board with 4 workflow stage columns, drag-and-drop on desktop via @dnd-kit/core, mobile stacked view with "Move to" dropdowns, stats bar, and empty state.</done>
</task>

<task type="auto">
  <name>Task 2: Build CalendarTab and DocsTab — engagement events and document management</name>
  <files>
    frontend/src/pages/engagements/workspace/CalendarTab.tsx
    frontend/src/pages/engagements/workspace/DocsTab.tsx
  </files>
  <read_first>
    frontend/src/pages/engagements/workspace/CalendarTab.tsx
    frontend/src/pages/engagements/workspace/DocsTab.tsx
    frontend/src/domains/engagements/hooks/useEngagements.ts
    frontend/src/domains/engagements/hooks/useEngagementBriefs.ts
    frontend/src/components/engagements/EngagementBriefsSection.tsx
    frontend/src/components/ui/card.tsx
    frontend/src/components/ui/button.tsx
    frontend/src/components/ui/badge.tsx
    frontend/src/hooks/useDirection.ts
    frontend/src/i18n/en/workspace.json
    frontend/src/types/lifecycle.types.ts
  </read_first>
  <action>
    **CalendarTab.tsx** (per D-04 Calendar, L-05 in UI-SPEC, WORK-07):

    Replace stub with engagement calendar view. NOTE: Events API lacks `engagement_id` filter (confirmed blocker in RESEARCH). Use the engagement's own dates as primary content, per research recommendation.

    **1. Data fetching:**
    - Get `engagementId` from `useParams`
    - `useEngagement(engagementId)` — for `start_date`, `end_date`, lifecycle stage, related date fields
    - `useTranslation('workspace')` for labels

    **2. Layout (per L-05):**
    - **Header bar:** Date range display showing engagement start_date to end_date, formatted with `Intl.DateTimeFormat` respecting locale
    - **`[+ Add Event]` button** (`variant="outline"`, `size="sm"`) — pre-linked to this engagement (placeholder action, will link to event creation flow when available)

    **3. Event list:**
    - Since the events API does not yet support engagement_id filtering, show engagement's own dates as calendar entries:
      - Start date: "Engagement Start" event card
      - End date: "Engagement Deadline" event card
      - Current lifecycle stage entry date (from lifecycle history): "Entered [stage]" event card
    - Group events by date headers (Today, Upcoming, Past)
    - Each event card: icon, title, date, time if available, type badge
    - Card: `p-3 rounded-md border bg-card` in `space-y-3` vertical list

    **4. Conflict detection placeholder:**
    - Note in a comment that conflict detection endpoint exists at `POST /api/events/conflicts` but is not wired until events API supports engagement_id filter
    - Add a TODO comment for backend extension

    **5. Empty state:**
    - `t('empty.calendar.heading')`, `t('empty.calendar.body')`, `t('empty.calendar.action')` button

    **6. RTL, responsive, explicit return type, default export.**

    ---

    **DocsTab.tsx** (per D-04 Docs, L-06 in UI-SPEC, WORK-08):

    Replace stub with document management view.

    **1. Data fetching:**
    - Get `engagementId` from `useParams`
    - `useEngagementBriefs(engagementId)` — list of AI + legacy briefs
    - `useGenerateEngagementBrief()` — mutation for AI briefing generation
    - `useTranslation('workspace')` for labels

    **2. Header (per L-06):**
    - Title: "Documents" in `text-xl font-semibold`
    - `[Generate Briefing]` button (`variant="default"`, `size="sm"`) — calls `generateBrief.mutate({ engagementId })`
    - Button shows spinner during generation (`generateBrief.isPending`), disabled state
    - Layout: `flex items-center justify-between` for header row

    **3. Document list:**
    - Reuse `EngagementBriefsSection` component if it renders well inline (read its implementation first)
    - If EngagementBriefsSection is too tightly coupled to its current container, extract the brief card rendering pattern and use it directly:
      - Each brief card: title, type badge (AI Brief / Legacy), created date, lifecycle stage badge if associated
      - Cards in `space-y-3` vertical list
    - Organize by lifecycle stage if briefs have stage association (collapsible sections per stage)
    - Upload placeholder: `[Upload Document]` button (`variant="outline"`) — placeholder for future file upload capability

    **4. Generate Briefing interaction (per I-05 in UI-SPEC):**
    - Click "Generate Briefing" → button shows spinner, disabled
    - Success: new brief appears in list, success toast (handled by mutation hook)
    - Error: error toast with `t('error.generateBrief')` message

    **5. Empty state:**
    - `t('empty.docs.heading')`, `t('empty.docs.body')`, `t('empty.docs.action')` button (which triggers generation)

    **6. RTL, responsive, explicit return type, default export.**
    - Container: `space-y-4 p-4 sm:p-6 lg:p-8`
    - No `ml-*`, `mr-*`, `text-left`, `text-right`
  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && pnpm --filter frontend exec tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `CalendarTab.tsx` contains `useEngagement` import
    - `CalendarTab.tsx` contains `useParams` import from `@tanstack/react-router`
    - `CalendarTab.tsx` contains `export default function CalendarTab`
    - `CalendarTab.tsx` contains `start_date` or `end_date` reference (engagement dates)
    - `CalendarTab.tsx` contains `Add Event` or `addEvent` string
    - `CalendarTab.tsx` contains empty state text from workspace i18n
    - `DocsTab.tsx` contains `useEngagementBriefs` import
    - `DocsTab.tsx` contains `useGenerateEngagementBrief` import
    - `DocsTab.tsx` contains `export default function DocsTab`
    - `DocsTab.tsx` contains `isPending` or loading state for generate button
    - `DocsTab.tsx` contains `Generate Briefing` or `generateBriefing` string
    - `DocsTab.tsx` contains empty state text from workspace i18n
    - No `ml-*`, `mr-*`, `text-left`, `text-right` in either file
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Calendar tab shows engagement date range, key dates from lifecycle history, and Add Event placeholder. Docs tab shows briefs list (reusing EngagementBriefsSection or extracted pattern), Generate Briefing AI action with loading state, and upload placeholder. Both have proper empty states.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd frontend && pnpm exec tsc --noEmit`
2. Navigate to `/engagements/:id/tasks` — kanban board renders with columns
3. Navigate to `/engagements/:id/calendar` — engagement dates display
4. Navigate to `/engagements/:id/docs` — briefs list renders, Generate Briefing button visible
5. Empty states render when no data
</verification>

<success_criteria>
- Tasks tab shows inline kanban with drag-and-drop (desktop) and stacked view (mobile)
- Calendar tab shows engagement date range and lifecycle date entries
- Docs tab shows briefs with Generate Briefing action
- All tabs use existing hooks (no new API calls except future events API extension)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-engagement-workspace/11-04-SUMMARY.md`
</output>
```
