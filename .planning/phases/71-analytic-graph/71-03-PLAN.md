---
phase: 71-analytic-graph
plan: 03
type: execute
wave: 3
depends_on: ['71-02']
files_modified:
  - .planning/phases/71-analytic-graph/71-03-SUMMARY.md
autonomous: false
requirements: [GRAPH-01, GRAPH-03, GRAPH-04]
must_haves:
  truths:
    - 'The query_graph migration is APPLIED and LIVE on staging zkrcjzdemdmwhearhfgg (function invocable, not merely a file on disk)'
    - 'The analytic-graph edge function is DEPLOYED to staging and reachable under a caller JWT'
    - 'The three backend integration tests from 71-01 (GRAPH-01 rows, GRAPH-03 clearance, GRAPH-04 INVOKER) run GREEN against live staging'
    - 'GRAPH-01/03/04 are verifiable from here — the false-positive gap (build+types pass without the live migration) is closed'
  artifacts:
    - path: '.planning/phases/71-analytic-graph/71-03-SUMMARY.md'
      provides: 'record of the applied migration version, deployed edge-fn version, and the green integration-test run'
      contains: 'query_graph'
  key_links:
    - from: 'staging zkrcjzdemdmwhearhfgg'
      to: 'public.query_graph'
      via: 'Supabase MCP apply_migration'
      pattern: 'query_graph'
    - from: 'backend integration tests'
      to: 'live query_graph RPC'
      via: 'supabase.rpc against staging'
      pattern: "rpc\\(['\"]query_graph"
---

<objective>
Apply the `query_graph` migration to staging via Supabase MCP, deploy the `analytic-graph` edge function, and run the three backend integration tests (authored RED in 71-01) GREEN against live staging. This is the gate that makes GRAPH-01/03/04 actually verifiable.

Purpose: Build and TypeScript checks pass WITHOUT the live migration (types come from generated config, not the live DB) — a false-positive verification. GRAPH-01/03/04 cannot be confirmed until `query_graph` is live and invocable. The executor does not hold Supabase MCP, so the migration-apply is an operator/orchestrator checkpoint.
Output: live `query_graph` RPC + deployed `analytic-graph` edge fn on staging + a green integration-test run recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-analytic-graph/71-RESEARCH.md
@.planning/phases/71-analytic-graph/71-VALIDATION.md
@supabase/migrations/20260617_phase71_query_graph.sql
@supabase/functions/analytic-graph/index.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Apply query_graph migration to staging via Supabase MCP</name>
  <action>CHECKPOINT — operator-only (the gsd-executor does not hold Supabase MCP). The operator/orchestrator applies `supabase/migrations/20260617_phase71_query_graph.sql` to staging project `zkrcjzdemdmwhearhfgg` via the Supabase MCP `apply_migration` tool (migration name `phase71_query_graph`), then verifies the function is live, INVOKER, and least-privilege per the `<how-to-verify>` SQL below, then types the resume-signal. Execution pauses here until applied.</action>
  <read_first>
    - supabase/migrations/20260617_phase71_query_graph.sql (the exact SQL to apply — authored in 71-02)
    - CLAUDE.md (Deployment Configuration → staging project Intl-Dossier `zkrcjzdemdmwhearhfgg`, eu-west-2; "Migrations: Use Supabase MCP to apply migrations")
    - .planning/phases/71-analytic-graph/71-RESEARCH.md (Environment Availability — "Supabase MCP (migration apply): orchestrator; executor lacks it"; 64-01/67-06 precedent)
  </read_first>
  <what-built>
    The `query_graph` multiplexed `SECURITY INVOKER` RPC (+ supporting indexes, REVOKE/GRANT) is authored at `supabase/migrations/20260617_phase71_query_graph.sql` but is NOT yet on the live database. The gsd-executor cannot apply it (no Supabase MCP). The operator/orchestrator applies the forward migration to staging `zkrcjzdemdmwhearhfgg` via the Supabase MCP `apply_migration` tool, then execution resumes.
  </what-built>
  <how-to-verify>
    1. Apply `supabase/migrations/20260617_phase71_query_graph.sql` to project `zkrcjzdemdmwhearhfgg` via Supabase MCP `apply_migration` (name e.g. `phase71_query_graph`).
    2. Confirm the function exists and is INVOKER:
       `SELECT proname, prosecdef FROM pg_proc WHERE proname = 'query_graph';`  → one row, `prosecdef = false` (INVOKER, not DEFINER).
    3. Confirm least-privilege grant:
       `SELECT has_function_privilege('anon', 'public.query_graph(text,uuid,uuid,integer)', 'EXECUTE');`  → `false`;
       `SELECT has_function_privilege('authenticated', 'public.query_graph(text,uuid,uuid,integer)', 'EXECUTE');`  → `true`.
    4. Smoke-invoke (any in-clearance entity id): `SELECT public.query_graph('forum_membership', '<a real dossier id>'::uuid);` → returns a shaped JSONB (`nodes`/`edges`/`stats` keys present), no error.
  </how-to-verify>
  <verify>Operator confirms via the `<how-to-verify>` SQL: `pg_proc` shows `query_graph` with `prosecdef=false`; anon EXECUTE=false / authenticated EXECUTE=true; `query_graph('forum_membership', …)` returns shaped JSONB.</verify>
  <done>query_graph is live on staging, INVOKER, anon-denied/authenticated-granted, and returns shaped JSONB on a smoke invocation.</done>
  <resume-signal>Type "applied" once `pg_proc` shows `query_graph` with `prosecdef=false`, anon EXECUTE=false / authenticated EXECUTE=true, and the smoke `query_graph('forum_membership', …)` returns shaped JSONB. Paste the smoke-result keys (or describe failures).</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Deploy the analytic-graph edge function to staging</name>
  <action>CHECKPOINT — deploy the `analytic-graph` edge fn to staging `zkrcjzdemdmwhearhfgg` via Supabase CLI (`supabase functions deploy analytic-graph --project-ref zkrcjzdemdmwhearhfgg`) or Supabase MCP, then verify per the `<how-to-verify>` curls (401 unauth, 200 + {nodes,edges,stats} authed, 400 on bad queryType), then type the resume-signal. Reuses existing SUPABASE_URL/SUPABASE_ANON_KEY — no new secrets.</action>
  <read_first>
    - supabase/functions/analytic-graph/index.ts (the function to deploy — authored in 71-02)
    - supabase/functions/graph-traversal/index.ts (the deployed sibling proving SUPABASE_URL/SUPABASE_ANON_KEY are present on staging)
    - STATE.md / MEMORY (Edge fn deploy precedent — deploy via Supabase CLI or MCP; reuses existing SUPABASE_URL/SUPABASE_ANON_KEY, no new secrets)
  </read_first>
  <what-built>
    The `analytic-graph` edge fn forwards the caller JWT to `query_graph` and shapes `{nodes, edges, stats}`. It must be deployed to staging so the FE hook (plan 71-04) and the live UAT (plan 71-05) can reach `/functions/v1/analytic-graph`.
  </what-built>
  <how-to-verify>
    1. Deploy via Supabase CLI (`supabase functions deploy analytic-graph --project-ref zkrcjzdemdmwhearhfgg`) or Supabase MCP.
    2. Unauthenticated call returns 401:
       `curl -s -o /dev/null -w "%{http_code}" "https://zkrcjzdemdmwhearhfgg.supabase.co/functions/v1/analytic-graph?queryType=forum_membership&entityId=<id>"` → `401`.
    3. Authenticated call (Bearer a real staging access token) returns 200 with `{nodes, edges, stats}`:
       `curl -s -H "Authorization: Bearer <token>" "https://…/functions/v1/analytic-graph?queryType=forum_membership&entityId=<id>"` → JSON with `nodes`/`edges`/`stats`.
    4. Invalid `queryType` returns 400 (whitelist enforced).
  </how-to-verify>
  <verify>Operator confirms: unauth call → 401; authed call → 200 with `{nodes,edges,stats}`; invalid queryType → 400.</verify>
  <done>analytic-graph edge fn deployed to staging and reachable under a caller JWT (401 unauth / 200 authed / 400 bad queryType).</done>
  <resume-signal>Type "deployed" once the unauthenticated call returns 401, an authenticated call returns 200 with `{nodes,edges,stats}`, and an invalid queryType returns 400. Note the deployed function version.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run backend integration tests GREEN against live staging</name>
  <read_first>
    - backend/tests/intelligence/query-graph.integration.test.ts (GRAPH-01 — from 71-01)
    - backend/tests/intelligence/query-graph.clearance.integration.test.ts (GRAPH-03 — from 71-01)
    - backend/tests/intelligence/query-graph.invoker.integration.test.ts (GRAPH-04 — from 71-01)
    - backend/tests/intelligence/fixtures/analytic-graph-seed.ts (the seed/restore the tests use)
    - .planning/phases/71-analytic-graph/71-VALIDATION.md (## Per-Task Verification Map — the commands + expected pass)
  </read_first>
  <action>
    With `query_graph` live (Task 1) and the edge fn deployed (Task 2), run the three backend integration tests against staging. They seed the RF-7 high-sensitivity fixture, exercise the RPC, and restore. Confirm: GRAPH-01 returns the seeded forum/WG/engagement rows; GRAPH-03 shows the higher-clearance count STRICTLY greater than the lower with no clearance-revealing copy in the payload; GRAPH-04 returns zero above-clearance nodes under the low-clearance JWT and the node appears under the high-clearance JWT. If a test fails, the defect is in the migration SQL (plan 71-02) — fix the SQL there, re-apply (loop back to Task 1), and re-run. Ensure the fixture is restored after the run (no `TEST` rows linger on staging). Record the applied migration version, deployed edge-fn version, fixture cleanup confirmation, and the green test output in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `pnpm --filter backend test -- query-graph.integration.test.ts --run` exits 0 (GRAPH-01 GREEN)
    - `pnpm --filter backend test -- query-graph.clearance.integration.test.ts --run` exits 0 (GRAPH-03 GREEN — strictly-increasing counts)
    - `pnpm --filter backend test -- query-graph.invoker.integration.test.ts --run` exits 0 (GRAPH-04 GREEN — zero above-clearance nodes under low-clearance JWT)
    - Post-run, no fixture rows remain: a `SELECT count(*) FROM dossiers WHERE name_en LIKE 'TEST %'` (or equivalent fixture marker) returns 0 (restore verified)
    - SUMMARY records the applied migration version + deployed edge-fn version
  </acceptance_criteria>
  <verify>
    <automated>cd backend && pnpm test -- query-graph.integration.test.ts query-graph.clearance.integration.test.ts query-graph.invoker.integration.test.ts --run 2>&1 | tail -20</automated>
  </verify>
  <done>All three backend integration tests are GREEN against live staging; fixture restored; migration + edge-fn versions recorded. GRAPH-01/03/04 are now verifiable.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                                                              |
| --------------------- | -------------------------------------------------------------------------------------------------------- |
| operator → staging DB | The migration is applied to live staging via Supabase MCP; a malformed migration could weaken clearance. |
| test runner → staging | Tests seed/restore high-sensitivity fixtures on live staging.                                            |

## STRIDE Threat Register

| Threat ID     | Category               | Component                                                                | Disposition | Mitigation Plan                                                                                                                                                           |
| ------------- | ---------------------- | ------------------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-71-03-FP    | Information Disclosure | false-positive verification (build/types pass without live migration)    | mitigate    | This plan's entire purpose: assert the function is LIVE (`pg_proc prosecdef=false`) + invocable, and run the integration tests against live staging — not file existence. |
| T-71-03-GRANT | Elevation of Privilege | migration applied but anon retains EXECUTE                               | mitigate    | Checkpoint verify step asserts `has_function_privilege('anon', …)` = false and `authenticated` = true post-apply.                                                         |
| T-71-03-LEAK  | Information Disclosure | fixture high-sensitivity rows left on staging after a failed/aborted run | mitigate    | Acceptance asserts zero `TEST `-marked fixture rows remain; tests restore in `afterAll`; operator can manually delete by the `TEST` marker.                               |
| T-71-03-SC    | Tampering              | npm installs                                                             | accept      | No package installs; this plan applies an already-authored migration and runs existing tests.                                                                             |

</threat_model>

<verification>
- `query_graph` is live on staging with INVOKER + anon-denied/authenticated-granted.
- `analytic-graph` edge fn deployed: 401 unauth, 200 + `{nodes,edges,stats}` authed, 400 on bad queryType.
- All three backend integration tests GREEN; RF-7 fixture restored.
</verification>

<success_criteria>

- The migration is applied and verified live (function invocable, INVOKER, least-privilege grant).
- The edge fn is deployed and reachable under a caller JWT.
- GRAPH-01/03/04 integration tests pass against staging; no fixture residue.
  </success_criteria>

<output>
Create `.planning/phases/71-analytic-graph/71-03-SUMMARY.md` when done (record migration version, edge-fn version, green test output, fixture-cleanup confirmation).
</output>
