---
phase: 71-analytic-graph
plan: 04
type: execute
wave: 4
depends_on: ['71-03']
files_modified:
  - frontend/src/hooks/useAnalyticGraph.ts
  - frontend/src/routes/_protected/relationships/graph.tsx
  - frontend/src/components/relationships/AnalyticQueryPicker.tsx
  - frontend/src/components/relationships/AnalyticResultView.tsx
  - frontend/src/pages/relationships/RelationshipGraphPage.tsx
autonomous: true
requirements: [GRAPH-01, GRAPH-03]
must_haves:
  truths:
    - 'A user can run who-sits-on-which-forum / shared-committees / engagement-chains (+ shortest-path) from the Network panel Analyze mode and receive a structured result (GRAPH-01, D-03, D-04)'
    - 'Launching the Analyze mode from a dossier pre-fills the primary entity via the dossierId search param (D-02)'
    - 'The structured result is the default view; a Graph/List tab flip renders the React Flow highlighted subgraph reusing AdvancedGraphVisualization (D-03)'
    - "A lower-clearance caller sees fewer rows/nodes with identical chrome and NO 'filtered by clearance' copy anywhere (GRAPH-03 indistinguishable-empty)"
    - 'All new UI is token-bound (no raw hex, no card shadows, var(--row-h)/1px var(--line)) with logical properties + dir/Tajawal for AR per the UI-SPEC'
    - 'This plan satisfies D-04 Analyze-mode + D-03 structured-result + D-01 query set (citation: implements D-01, D-02, D-03, D-04, GRAPH-01, GRAPH-03)'
  artifacts:
    - path: 'frontend/src/hooks/useAnalyticGraph.ts'
      provides: 'TanStack Query wrapper that calls /functions/v1/analytic-graph under the session token'
      contains: 'analytic-graph'
    - path: 'frontend/src/components/relationships/AnalyticQueryPicker.tsx'
      provides: 'query-template selector + typed inputs (1/2 entities + window N), dossier-context pre-fill, Run analysis CTA'
      contains: 'AnalyticQueryPicker'
    - path: 'frontend/src/components/relationships/AnalyticResultView.tsx'
      provides: 'structured result keyed to query type (list/table/timeline/path); indistinguishable-empty'
      contains: 'AnalyticResultView'
    - path: 'frontend/src/pages/relationships/RelationshipGraphPage.tsx'
      provides: 'Analyze mode driven off the mode search param + structured-result region + Graph/List flip'
      contains: 'analyze'
    - path: 'frontend/src/routes/_protected/relationships/graph.tsx'
      provides: 'extended validateSearch (mode/query/entity2/windowDays)'
      contains: 'mode'
  key_links:
    - from: 'frontend/src/hooks/useAnalyticGraph.ts'
      to: '/functions/v1/analytic-graph'
      via: 'fetch with Authorization Bearer session.access_token'
      pattern: 'functions/v1/analytic-graph'
    - from: 'frontend/src/pages/relationships/RelationshipGraphPage.tsx'
      to: 'frontend/src/components/relationships/AnalyticResultView.tsx'
      via: 'renders AnalyticResultView in the structured (List) tab'
      pattern: 'AnalyticResultView'
    - from: 'frontend/src/pages/relationships/RelationshipGraphPage.tsx'
      to: 'frontend/src/components/relationships/AdvancedGraphVisualization.tsx'
      via: 'Graph-view tab feeds nodes/edges/centerNodeId'
      pattern: 'AdvancedGraphVisualization'
---

<objective>
Build the analyst surface for the Analyze experience: a `useAnalyticGraph` hook (calls the live `analytic-graph` edge fn), an extended route search schema, the `AnalyticQueryPicker` (query-template selector + typed inputs + dossier-context pre-fill), the `AnalyticResultView` (structured result per query type), and the "Analyze" mode wired into `RelationshipGraphPage.tsx` with the existing Graph/List tab flip.

Purpose: Deliver GRAPH-01 from the Network panel (D-04) with the structured-result-primary + graph-view-secondary contract (D-03) and entity pre-fill (D-02), reusing the panel's Tabs, stats strip, `AdvancedGraphVisualization`, and `handleNodeSelect` rather than rebuilding. Clearance reduction (GRAPH-03) renders as fewer rows with zero clearance-revealing copy.
Output: 5 FE files (1 new hook, 2 new components, 2 extended).

Note: the `AnalyticResultView` + Analyze-mode panel built here is the full-panel **deep-link target** that the Cmd+K "Analyze:" entries navigate to (`?mode=analyze`). GRAPH-02 itself is owned and cited by plan 71-05 (the Cmd+K entry-point + compact inline result); this plan deliberately does not claim GRAPH-02 in its `requirements`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/71-analytic-graph/71-PATTERNS.md
@.planning/phases/71-analytic-graph/71-UI-SPEC.md

<interfaces>
<!-- Extracted from the live analog files this phase reuses (71-PATTERNS.md verified). -->

RelationshipGraphPage.tsx (the page to extend):
L133 const { t } = useTranslation('graph')
L135 const { dossierId: startDossierId } = routeApi.useSearch()
L179-187 handleNodeSelect(nodeId) → getDossierDetailPath(nodeId, node?.type) → navigate (type-aware; bare /dossiers/$id does NOT exist)
L221-273 controls grid in <Card><CardContent> with <Label>+<Select> idiom
L276-370 stats strip (complexity Badge L285-299, query_time_ms L355-360, performance_warning L363-366)
L395-405 <Tabs value={activeTab} onValueChange={...}> with graph/list TabsTrigger
L407-416 Graph tab → <AdvancedGraphVisualization nodes/edges/centerNodeId/onNodeClick/height/showMiniMap>
L438-444 List tab → <RelationshipNavigator …>

route graph.tsx (extend in place):
GraphSearch { dossierId?: string }
validateSearch returns { dossierId: typeof dossierId === 'string' && dossierId.length > 0 ? dossierId : undefined }

AdvancedGraphVisualization props (reuse for Graph view): { nodes, edges, centerNodeId, onNodeClick, height, showMiniMap, onPathFind }
PathFindingPanel (AdvancedGraphVisualization L593-606): entity-select idiom — useTranslation('graph') + useDirection() + sourceId/targetId useState
GlobeSpinner: frontend/src/components/signature-visuals/GlobeSpinner.tsx — props { size?: number } (the loading state)

fetchGraphData analog (RelationshipGraphPage L93-154): supabase.auth.getSession() → fetch(`${VITE_SUPABASE_URL}/functions/v1/graph-traversal?…`, { headers:{ Authorization:`Bearer ${session.access_token}` } }) → useQuery({ queryKey:['graph-traversal',…], staleTime:30000 })

analytic-graph edge fn HTTP contract (plan 71-02, live after 71-03):
GET /functions/v1/analytic-graph?queryType=&entityId=&entityId2=&windowDays= → { nodes, edges, stats:{ node_count, edge_count, query_time_ms, performance_warning } }
</interfaces>

@frontend/src/components/relationships/**tests**/AnalyticQueryPicker.test.tsx
@frontend/src/components/relationships/**tests**/AnalyticResultView.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: useAnalyticGraph hook + extended route search schema</name>
  <read_first>
    - frontend/src/pages/relationships/RelationshipGraphPage.tsx (L93-154 — fetchGraphData + useQuery pattern to mirror exactly)
    - frontend/src/routes/_protected/relationships/graph.tsx (L5-18 — GraphSearch + validateSearch string-guard idiom to extend)
    - .planning/phases/71-analytic-graph/71-PATTERNS.md (### useAnalyticGraph.ts + ### graph.tsx sections — the copy targets + the param set)
  </read_first>
  <action>
    Create `frontend/src/hooks/useAnalyticGraph.ts`: a `fetchAnalyticGraph` that calls `supabase.auth.getSession()`, throws if no session, builds `URLSearchParams` from `queryType`/`entityId`/`entityId2`/`windowDays`, fetches `${import.meta.env.VITE_SUPABASE_URL}/functions/v1/analytic-graph?…` with `Authorization: Bearer ${session.access_token}`, throws on `!response.ok`, returns `response.json()` (mirror RelationshipGraphPage L94-130). Wrap in `useQuery({ queryKey: ['analytic-graph', queryType, entityId, entityId2, windowDays], queryFn, enabled: !!entityId, staleTime: 30000 })` (mirror L144-154). Define explicit return type (an `AnalyticGraphResult` interface: `{ nodes: AnalyticNode[]; edges: AnalyticEdge[]; stats: { node_count: number; edge_count: number; query_time_ms: number; performance_warning: string | null } }`) plus the hook's `UseQueryResult`-shaped return — ESLint `explicit-function-return-type`. No `console.log`.
    Extend `frontend/src/routes/_protected/relationships/graph.tsx`: add to `GraphSearch` and `validateSearch` (following the existing string-guard idiom): `mode?: 'analyze'` (`search.mode === 'analyze' ? 'analyze' : undefined`), `query?` (whitelist the 4 query types; else undefined), `entity2?` (string-guard like dossierId), `windowDays?` (`typeof search.windowDays === 'number'` clamped 1–365, else undefined). Do NOT remove the existing `dossierId`.
  </action>
  <acceptance_criteria>
    - `frontend/src/hooks/useAnalyticGraph.ts` exists; `grep -c "functions/v1/analytic-graph" frontend/src/hooks/useAnalyticGraph.ts` >= 1
    - `grep -c "queryKey: \['analytic-graph'" frontend/src/hooks/useAnalyticGraph.ts` >= 1 AND `grep -c "staleTime: 30000" frontend/src/hooks/useAnalyticGraph.ts` >= 1
    - `grep -c "console.log" frontend/src/hooks/useAnalyticGraph.ts` == 0
    - `graph.tsx` validateSearch now references `mode` and `windowDays`: `grep -Ec "mode|windowDays" frontend/src/routes/_protected/relationships/graph.tsx` >= 2 AND still references `dossierId`
    - `pnpm --filter intake-frontend exec tsc --noEmit` reports no errors in these two files
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit 2>&1 | grep -E "useAnalyticGraph|relationships/graph" | grep -vq error && echo "hook + route type-clean"</automated>
  </verify>
  <done>useAnalyticGraph mirrors the graph-traversal fetch+TanStack pattern against the analytic-graph edge fn; route search schema carries mode/query/entity2/windowDays.</done>
</task>

<task type="auto">
  <name>Task 2: AnalyticQueryPicker + AnalyticResultView components</name>
  <read_first>
    - frontend/src/components/relationships/AdvancedGraphVisualization.tsx (L593-606 PathFindingPanel — entity-select idiom + useTranslation('graph') + useDirection())
    - frontend/src/pages/relationships/RelationshipGraphPage.tsx (L224-265 Select controls idiom; L340-361 stats grid; L438-444 RelationshipNavigator list-render contract)
    - frontend/src/components/relationships/RelationshipNavigator.tsx (the list-row + onNodeSelect contract the membership result mirrors)
    - frontend/src/components/signature-visuals/GlobeSpinner.tsx (the loading-state component, size prop)
    - .planning/phases/71-analytic-graph/71-UI-SPEC.md (## State Inventory, ## Typography, ## Color, ## Copywriting Contract incl. the LOCKED no-clearance-copy rule; ## Layout & Responsive)
    - frontend/src/components/relationships/__tests__/AnalyticQueryPicker.test.tsx + AnalyticResultView.test.tsx (the RED tests these components must turn GREEN)
    - CLAUDE.md (Visual Design Source of Truth + Definition of Done UI checklist + RTL logical-properties table)
  </read_first>
  <action>
    Create `frontend/src/components/relationships/AnalyticQueryPicker.tsx`: a named-props component (`AnalyticQueryPickerProps`, no `React.FC`, explicit return type) exposing a query-template `<Select>` over the 4 templates (`forum_membership`, `shared_committees`, `engagement_chain`, `shortest_path` — label keys per the UI-SPEC Copywriting table), 1 entity input for forum_membership/engagement_chain and 2 entity inputs for shared_committees/shortest_path, plus a window-N input shown ONLY for engagement_chain ("Over the last {N} days"). A `defaultEntityId` prop pre-fills the primary entity (D-02). A single primary `.btn-primary` "Run analysis" CTA fires `onRun({ queryType, entityId, entityId2, windowDays })`. Use `useTranslation('graph')` + `useDirection()` exactly as PathFindingPanel; mount inside the panel's `<Card><CardContent>` controls region mirroring the L224-265 Select idiom. Conditional template-4 visibility: render shortest_path unconditionally (RF-4 confirmed low-cost → shipped).
    Create `frontend/src/components/relationships/AnalyticResultView.tsx`: a named-props component rendering the structured primary result keyed to query type — membership → a list at `var(--row-h)` rows (mirror RelationshipNavigator's row + `onNodeSelect` contract); shared_committees → an intersection table; engagement_chain → an ordered timeline (day-first dates `Tue 28 Apr` via the shared `formatDayFirst` util, newest-first per RF-2); shortest_path → an ordered hop sequence with a mono hop count. Render the empty state with the UI-SPEC "No results" copy (path uses the "No connection found" variant), the error state with the `--danger`/`--danger-soft` banner copy, and `GlobeSpinner` for loading. CRITICAL (GRAPH-03 / P68 D-09): the reduced-clearance state is IDENTICAL chrome to a genuine empty/partial result — emit NO string matching `clearance|filtered|restricted|hidden|permission` anywhere. Token-bound chrome only: `1px solid var(--line)`, no card shadows, no raw hex, no Tailwind color literals, logical props (`ms-*`/`pe-*`/`text-start`), `dir`/Tajawal for AR. 4 type roles only per the UI-SPEC Typography table.
  </action>
  <acceptance_criteria>
    - Both component files exist
    - `pnpm --filter intake-frontend test -- --run AnalyticQueryPicker.test.tsx` exits 0 (was RED in 71-01, now GREEN)
    - `pnpm --filter intake-frontend test -- --run AnalyticResultView.test.tsx` exits 0
    - No clearance-revealing copy: `grep -Eic "clearance|filtered|restricted|hidden|permission" frontend/src/components/relationships/AnalyticResultView.tsx` == 0
    - No raw hex / no Tailwind color literal: `grep -Ec "#[0-9a-fA-F]{3,6}\b" frontend/src/components/relationships/AnalyticQueryPicker.tsx frontend/src/components/relationships/AnalyticResultView.tsx` == 0 AND `grep -Ec "(text|bg|border)-(blue|red|green|gray|slate|indigo|amber)-[0-9]{2,3}" frontend/src/components/relationships/AnalyticResultView.tsx` == 0
    - No physical-direction utilities: `grep -Ec "\b(ml-|mr-|pl-|pr-|text-left|text-right)\b" frontend/src/components/relationships/AnalyticResultView.tsx` == 0
    - Row height uses the density token: `grep -c "var(--row-h)" frontend/src/components/relationships/AnalyticResultView.tsx` >= 1
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && pnpm test -- --run AnalyticQueryPicker.test.tsx AnalyticResultView.test.tsx 2>&1 | tail -15 && [ "$(grep -Eic 'clearance|filtered|restricted|permission' src/components/relationships/AnalyticResultView.tsx)" = "0" ] && echo "no clearance copy"</automated>
  </verify>
  <done>Picker renders 4 templates + typed inputs + dossier pre-fill + Run analysis; result view renders list/table/timeline/path with token-bound chrome, GlobeSpinner loading, and zero clearance-revealing copy. Both FE component tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Wire "Analyze" mode into RelationshipGraphPage</name>
  <read_first>
    - frontend/src/pages/relationships/RelationshipGraphPage.tsx (whole file — extend in place; L133/L135 search read, L179-187 handleNodeSelect, L221-273 controls, L276-370 stats, L395-416 Tabs+AdvancedGraphVisualization)
    - frontend/src/hooks/useAnalyticGraph.ts (Task 1 — the result source)
    - frontend/src/components/relationships/AnalyticQueryPicker.tsx + AnalyticResultView.tsx (Task 2 — the mounted surfaces)
    - .planning/phases/71-analytic-graph/71-UI-SPEC.md (### Surfacing contract D-04 + ## State Inventory; the GlobeSpinner-for-analytic-run rule)
    - .planning/phases/71-analytic-graph/71-PATTERNS.md (### RelationshipGraphPage section — reuse Tabs/stats/handleNodeSelect, URL-as-state)
  </read_first>
  <action>
    Extend `RelationshipGraphPage.tsx` to read the new search params (`mode`, `query`, `entity2`, `windowDays`) via the existing `routeApi.useSearch()`. When `mode === 'analyze'`: render the `AnalyticQueryPicker` in the controls `<Card>` region (pre-filled `defaultEntityId={startDossierId}` from the existing `dossierId` anchor — D-02), drive its `onRun` to set the query params and call `useAnalyticGraph`, and render the result region with a Graph/List `<Tabs>` flip (reuse the existing L395-405 Tabs) where: the List/structured tab (DEFAULT) renders `AnalyticResultView` and the Graph tab feeds `AdvancedGraphVisualization` (`nodes/edges/centerNodeId/onNodeClick={handleNodeSelect}/height/showMiniMap`). Reuse the existing stats strip + `performance_warning` + complexity badge for the analytic result's stats (RF-8). Use `GlobeSpinner` (not Skeleton) for the analytic-run loading state per the UI-SPEC. Keep the existing degrees-traversal exploration intact when `mode !== 'analyze'` (additive, no regression). Mode is URL-driven (URL-as-state), not new local state where the URL can carry it. Every analytic node/row click MUST route through `handleNodeSelect` (type-aware `getDossierDetailPath`; the bare `/dossiers/$id` route does not exist).
  </action>
  <acceptance_criteria>
    - `RelationshipGraphPage.tsx` references the new mode + components: `grep -c "AnalyticQueryPicker" frontend/src/pages/relationships/RelationshipGraphPage.tsx` >= 1 AND `grep -c "AnalyticResultView" …` >= 1 AND `grep -c "useAnalyticGraph" …` >= 1
    - Analyze mode is search-param driven: `grep -c "'analyze'" frontend/src/pages/relationships/RelationshipGraphPage.tsx` >= 1
    - Graph view reuses the existing renderer: `grep -c "AdvancedGraphVisualization" frontend/src/pages/relationships/RelationshipGraphPage.tsx` >= 1 (already present; analytic Graph tab feeds it)
    - Analytic node clicks reuse type-aware nav: the analytic Graph/result onClick references `handleNodeSelect` (`grep -c "handleNodeSelect" …` >= 2 — existing + analytic)
    - Existing degrees-traversal path preserved: `grep -c "traverse" frontend/src/pages/relationships/RelationshipGraphPage.tsx` unchanged from baseline (no removal of the existing exploration)
    - `pnpm --filter intake-frontend exec tsc --noEmit` reports no errors in RelationshipGraphPage.tsx
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit 2>&1 | grep "RelationshipGraphPage" | grep -vq error && grep -q "AnalyticQueryPicker" src/pages/relationships/RelationshipGraphPage.tsx && grep -q "'analyze'" src/pages/relationships/RelationshipGraphPage.tsx && echo "Analyze mode wired"</automated>
  </verify>
  <done>Analyze mode renders the picker (entity pre-filled) + structured result with Graph/List flip reusing AdvancedGraphVisualization + stats strip + GlobeSpinner; degrees-traversal preserved; node clicks type-aware.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                                                                          |
| -------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| browser → analytic-graph edge fn | The FE sends queryType/entity ids under the session JWT; clearance is enforced server-side (RPC), not in the client. |
| rendered result → analyst        | Reduced-clearance results must be indistinguishable from empty (no UI leak of above-clearance existence).            |

## STRIDE Threat Register

| Threat ID            | Category               | Component                                                                                 | Disposition | Mitigation Plan                                                                                                                                                                     |
| -------------------- | ---------------------- | ----------------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------- | ------ | -------------------------------------------------------------------------------------------------------------------- |
| T-71-04-INFO-COPY    | Information Disclosure | result view leaking "filtered by clearance"/badge revealing withheld above-clearance data | mitigate    | AnalyticResultView emits zero `clearance                                                                                                                                            | filtered | restricted | hidden | permission` strings; reduced state = identical chrome to empty (acceptance grep == 0); UI-SPEC LOCKED copy contract. |
| T-71-04-CLIENT-TRUST | Information Disclosure | client attempting to widen results by tampering params                                    | accept      | Clearance is enforced in the INVOKER RPC under the caller JWT (plan 71-02); the client cannot bypass it — param tampering only changes which (still clearance-filtered) query runs. |
| T-71-04-XSS          | Tampering              | rendering name_en/name_ar/entity content                                                  | mitigate    | React escapes by default; NO `dangerouslySetInnerHTML` in the new components; result content is DB-sourced + already clearance-filtered.                                            |
| T-71-04-NAV          | Information Disclosure | node click hitting a non-existent bare route or wrong dossier                             | mitigate    | All node/row clicks route through `handleNodeSelect` → `getDossierDetailPath(id, type)` (type-aware; the bare /dossiers/$id route does not exist).                                  |
| T-71-04-SC           | Tampering              | npm installs                                                                              | accept      | No new packages — reuses @xyflow/react, @supabase/supabase-js, TanStack Query, i18next already in package.json (71-RESEARCH.md: zero new dependencies).                             |

</threat_model>

<verification>
- useAnalyticGraph calls the live analytic-graph edge fn under the session token with the 30s-cache TanStack pattern.
- AnalyticQueryPicker (4 templates, typed inputs, dossier pre-fill) + AnalyticResultView (list/table/timeline/path, token-bound, no clearance copy) — both component tests GREEN.
- RelationshipGraphPage Analyze mode is URL-driven, reuses Tabs/stats/AdvancedGraphVisualization/handleNodeSelect, GlobeSpinner loading, degrees-traversal preserved.
- All new UI passes the design-token + RTL grep gates (no raw hex, no color literals, no physical-direction utilities, var(--row-h)).
</verification>

<success_criteria>

- GRAPH-01 runnable from the Network panel Analyze mode for all 4 query types, structured-result default with Graph/List flip.
- Entity pre-filled from the dossier anchor (D-02); result-graph reuses AdvancedGraphVisualization (D-03).
- GRAPH-03 reduction renders as fewer rows with zero clearance-revealing copy.
- Both FE component tests from 71-01 turn GREEN.
  </success_criteria>

<output>
Create `.planning/phases/71-analytic-graph/71-04-SUMMARY.md` when done.
</output>
