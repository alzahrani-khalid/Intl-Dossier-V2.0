---
phase: 71-analytic-graph
plan: 05
type: execute
wave: 5
depends_on: ['71-04']
files_modified:
  - frontend/src/components/keyboard-shortcuts/CommandPalette.tsx
  - frontend/src/components/dossier/DossierAnalyzeButton.tsx
  - frontend/src/i18n/en/graph.json
  - frontend/src/i18n/ar/graph.json
  - frontend/src/i18n/en/keyboard-shortcuts.json
  - frontend/src/i18n/ar/keyboard-shortcuts.json
autonomous: false
requirements: [GRAPH-01, GRAPH-02, GRAPH-03, GRAPH-04]
must_haves:
  truths:
    - "A user can launch an analytic graph query from Cmd+K ('Analyze: …') with the entity pre-filled from the current dossier and see a compact inline result + a deep-link to the Network panel Analyze mode (GRAPH-02, D-03, D-04)"
    - "A per-dossier 'Analyze' affordance (.btn-ghost) deep-links to /relationships/graph?dossierId=<id>&mode=analyze with the entity pre-filled (D-02, D-04)"
    - 'All new copy lives under the already-registered graph + keyboard-shortcuts namespaces (en+ar) — no new namespace, no silent English fallback'
    - "Cmd+K reduced-clearance results are silently fewer/zero rows with NO 'filtered by clearance' copy (GRAPH-03 indistinguishable-empty)"
    - 'The live UAT proves all 4 success criteria on staging EN+AR (run from panel + Cmd+K; strictly-increasing counts across clearance levels via CDP forced-error; query_graph direct invocation returns zero above-clearance nodes)'
    - 'This plan closes GRAPH-02 and the cross-cutting verification bar (citation: implements D-03, D-04, GRAPH-02, and verifies GRAPH-01/03/04 live)'
  artifacts:
    - path: 'frontend/src/components/keyboard-shortcuts/CommandPalette.tsx'
      provides: 'cmd-analyze-* parameterized entries + compact inline result + deep-link'
      contains: 'analyze'
    - path: 'frontend/src/components/dossier/DossierAnalyzeButton.tsx'
      provides: 'per-dossier Analyze affordance pre-filling the entity'
      contains: 'mode=analyze'
    - path: 'frontend/src/i18n/en/graph.json'
      provides: 'analyze.* keys (EN)'
      contains: 'analyze'
    - path: 'frontend/src/i18n/ar/graph.json'
      provides: 'analyze.* keys (AR)'
      contains: 'analyze'
  key_links:
    - from: 'frontend/src/components/keyboard-shortcuts/CommandPalette.tsx'
      to: '/relationships/graph?...mode=analyze'
      via: 'location.pathname regex → extract dossier id → navigateTo deep-link'
      pattern: 'mode=analyze'
    - from: 'frontend/src/components/dossier/DossierAnalyzeButton.tsx'
      to: '/relationships/graph'
      via: 'navigate with dossierId + mode=analyze'
      pattern: 'relationships/graph'
    - from: 'frontend/src/i18n/index.ts'
      to: 'frontend/src/i18n/{en,ar}/graph.json'
      via: 'graph namespace already registered (extend, do not create)'
      pattern: 'graph'
---

<objective>
Complete the three-entry-point Analyze experience (D-04): add parameterized `cmd-analyze-*` entries with a compact inline result to `CommandPalette.tsx` (GRAPH-02), add a per-dossier "Analyze" affordance, author the `analyze.*` i18n keys (EN+AR) under the already-registered namespaces, then run the load-bearing live UAT on staging proving all four success criteria EN+AR.

Purpose: GRAPH-02 (Cmd+K launch) + the per-dossier affordance are the remaining two of the three entry points; the live UAT (seed→observe→restore, dual-clearance via CDP forced-error, direct `query_graph` invocation) is the cross-cutting verification bar that this milestone treats as the real proof.
Output: extended CommandPalette + new DossierAnalyzeButton + 4 i18n files (en/ar × graph/keyboard-shortcuts) + a passing live UAT recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/71-analytic-graph/71-PATTERNS.md
@.planning/phases/71-analytic-graph/71-UI-SPEC.md
@.planning/phases/71-analytic-graph/71-VALIDATION.md

<interfaces>
<!-- Verified analog signatures (71-PATTERNS.md). -->

CommandPalette.tsx:
L335-342 interface QuickActionItem { id: string; label: string; icon: React.ElementType; action: () => void; shortcut?: string; category?: string }
L411 const { t } = useTranslation('keyboard-shortcuts')
L429-437 currentContext = useMemo((): RouteContext => { for (ctx of routeContexts) if (ctx.pattern?.test(pathname)) return ctx; … }, [location.pathname])
L746-757 cmd-view-network — existing "graph deep-link from pathname" precedent
L771-782 cmd-generate-briefing — the pathname-regex context idiom to copy:
const m = /\/engagements\/([^/]+)/.exec(location.pathname); if (m) navigateTo(`/engagements/${m[1]}/docs`) else navigateTo('/dossiers/engagements')
L823 quickActions useMemo dep array [t, formatShortcut, navigateTo, location.pathname]

dossier route map — frontend/src/lib/dossier-routes.ts:
getDossierRouteSegment(type) → 'countries'|'organizations'|'forums'|… ; route shape /dossiers/{segment}/{id}
(Cmd+K analyze regex matches these segments to extract the current dossier id.)

graph namespace registered: frontend/src/i18n/index.ts (en + ar). keyboard-shortcuts registered (en + ar).
Copy strings (EN/AR) — 71-UI-SPEC.md ## Copywriting Contract table (Analyze / تحليل, "Analyze:" / تحليل:, template labels, count lines, empty/error, "Open in the Network panel" / افتح في لوحة الشبكة, "Running analysis…" / جارٍ تشغيل التحليل…).

Deep-link target (plan 71-04, live): /relationships/graph?dossierId=<id>&mode=analyze&query=<type>
</interfaces>

@frontend/src/components/keyboard-shortcuts/**tests**/CommandPalette.analyze.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n analyze.* keys (en + ar) under registered namespaces</name>
  <read_first>
    - frontend/src/i18n/en/graph.json + frontend/src/i18n/ar/graph.json (existing keys + structure to extend)
    - frontend/src/i18n/en/keyboard-shortcuts.json + frontend/src/i18n/ar/keyboard-shortcuts.json (quickActions.* structure for the Analyze command labels)
    - frontend/src/i18n/index.ts (confirm graph + keyboard-shortcuts are registered — do NOT add a new namespace)
    - .planning/phases/71-analytic-graph/71-UI-SPEC.md (## Copywriting Contract — the EXACT EN+AR strings; the LOCKED clearance-reduction copy contract: zero clearance/restricted/filtered strings)
    - MEMORY (i18n static-bundle: unregistered namespaces fall back to English in BOTH langs; dot vs colon separator pitfalls)
  </read_first>
  <action>
    Add an `analyze` object to BOTH `frontend/src/i18n/en/graph.json` and `frontend/src/i18n/ar/graph.json` with keys for every UI-SPEC Copywriting row: mode entry, picker heading ("Choose an analysis" / اختر تحليلاً), the 4 template labels, the time-window control, the "Run analysis" CTA, result-region heading, the 4 result count lines (membership/intersection/chain/path), Graph/List view tab labels, empty-state heading + the 3 empty bodies (generic + path "No connection found"), error state, "Open in the Network panel" deep-link footer, and "Running analysis…" loading. AR values must be the exact translations in the UI-SPEC table (proper Arabic, not English fallback). Add the Cmd+K command labels (`quickActions.analyzeForumMembership`, `…SharedCommittees`, `…EngagementChains`, `…ShortestPath`, prefixed "Analyze:" / تحليل:) to BOTH `keyboard-shortcuts.json` files. CRITICAL: NO key value may contain `clearance`/`filtered`/`restricted`/`hidden`/`permission` in EITHER language (indistinguishable-empty). Keep valid JSON (prettier-normalized). Do NOT create a new namespace — these two namespaces are already registered.
  </action>
  <acceptance_criteria>
    - `analyze` object present in both files: `python3 -c "import json; assert 'analyze' in json.load(open('frontend/src/i18n/en/graph.json'))" && python3 -c "import json; assert 'analyze' in json.load(open('frontend/src/i18n/ar/graph.json'))"` exits 0
    - EN+AR key parity for the analyze object (same key set): a key-diff of `en/graph.json` vs `ar/graph.json` under `analyze` is empty
    - AR is genuinely translated, not English: `python3 -c "import json; a=json.load(open('frontend/src/i18n/ar/graph.json'))['analyze']; import re; assert any(re.search(r'[؀-ۿ]', str(v)) for v in a.values())"` exits 0 (contains Arabic script)
    - Zero clearance-revealing copy in EITHER language: `grep -Eic "clearance|filtered|restricted|hidden|permission" frontend/src/i18n/en/graph.json frontend/src/i18n/ar/graph.json` == 0
    - All four JSON files parse: `for f in frontend/src/i18n/{en,ar}/{graph,keyboard-shortcuts}.json; do python3 -c "import json,sys; json.load(open(sys.argv[1]))" "$f"; done` exits 0
    - Cmd+K labels present: `grep -c "nalyze" frontend/src/i18n/en/keyboard-shortcuts.json` >= 1
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && for f in src/i18n/en/graph.json src/i18n/ar/graph.json src/i18n/en/keyboard-shortcuts.json src/i18n/ar/keyboard-shortcuts.json; do python3 -c "import json,sys;json.load(open(sys.argv[1]))" "$f" || exit 1; done && python3 -c "import json,re; a=json.load(open('src/i18n/ar/graph.json'))['analyze']; assert any(re.search(r'[؀-ۿ]',str(v)) for v in a.values())" && [ "$(grep -Eic 'clearance|filtered|restricted' src/i18n/en/graph.json src/i18n/ar/graph.json)" = "0" ] && echo "i18n keys valid + AR translated + no clearance copy"</automated>
  </verify>
  <done>analyze.* keys added to en/ar graph + keyboard-shortcuts (registered namespaces); AR genuinely translated; EN/AR parity; zero clearance-revealing copy; all JSON valid.</done>
</task>

<task type="auto">
  <name>Task 2: Cmd+K Analyze entries + per-dossier Analyze affordance</name>
  <read_first>
    - frontend/src/components/keyboard-shortcuts/CommandPalette.tsx (L335-342 QuickActionItem, L429-437 currentContext, L746-757 cmd-view-network, L771-782 cmd-generate-briefing regex idiom, L823 dep array)
    - frontend/src/lib/dossier-routes.ts (getDossierRouteSegment + the /dossiers/{segment}/{id} route shape — for the pathname regex)
    - frontend/src/components/keyboard-shortcuts/__tests__/CommandPalette.analyze.test.tsx (the RED test to turn GREEN)
    - frontend/src/i18n/en/keyboard-shortcuts.json (Task 1 — the command labels)
    - .planning/phases/71-analytic-graph/71-UI-SPEC.md (### Surfacing contract D-04 — the affordance uses .btn-ghost; Cmd+K compact inline result + deep-link)
    - .planning/phases/71-analytic-graph/71-PATTERNS.md (### CommandPalette section — cmd-analyze-* via pathname regex, zero new state)
  </read_first>
  <action>
    Extend `CommandPalette.tsx`: add `cmd-analyze-forum-membership`, `cmd-analyze-shared-committees`, `cmd-analyze-engagement-chains`, `cmd-analyze-shortest-path` `QuickActionItem`s to the `quickActions` useMemo. Each `action`: read the current dossier id from `location.pathname` via a regex over the dossier route segments (mirror the `cmd-generate-briefing` idiom L771-782, using the `frontend/src/lib/dossier-routes.ts` segment map); if a dossier id is found, `navigateTo('/relationships/graph?dossierId=<id>&mode=analyze&query=<type>')` (entity pre-filled — D-02); else `navigateTo('/relationships/graph?mode=analyze&query=<type>')` (the picker requires manual entity entry). Use `t('quickActions.analyze…')` labels from the `keyboard-shortcuts` namespace (L411 `useTranslation('keyboard-shortcuts')`). Register the analyze actions in the `currentContext`/`routeContexts` so they rank up on dossier routes (L429-437). The Cmd+K compact inline result (GRAPH-02) is the top-N rows + count line + "Open in the Network panel" footer (deep-link to the full Analyze mode); the full panel is the deep-link target (D-03) — keep the inline render compact, reuse the `graph` namespace count-line keys. Keep the existing dep-array shape (add nothing new beyond `t/navigateTo/location.pathname` which are already deps).
    Create `frontend/src/components/dossier/DossierAnalyzeButton.tsx`: a named-props component (`{ dossierId: string }`, explicit return type) rendering a `.btn-ghost` "Analyze" button (label `t('analyze.modeEntry')` from `graph`) that navigates to `/relationships/graph?dossierId=${dossierId}&mode=analyze`. Token-bound, logical props, `dir`/Tajawal-safe. This is a secondary action (.btn-ghost, NOT .btn-primary per the UI-SPEC surfacing contract). (Mounting it on dossier pages is a thin addition; place it where the existing per-dossier action affordances live — the executor wires one canonical mount point and notes it.)
  </action>
  <acceptance_criteria>
    - `pnpm --filter intake-frontend test -- --run CommandPalette.analyze.test.tsx` exits 0 (was RED in 71-01, now GREEN)
    - Cmd+K has the 4 analyze actions: `grep -Ec "cmd-analyze-(forum-membership|shared-committees|engagement-chains|shortest-path)" frontend/src/components/keyboard-shortcuts/CommandPalette.tsx` == 4
    - Deep-link target present: `grep -c "mode=analyze" frontend/src/components/keyboard-shortcuts/CommandPalette.tsx` >= 1
    - `DossierAnalyzeButton.tsx` exists; `grep -c "btn-ghost" frontend/src/components/dossier/DossierAnalyzeButton.tsx` >= 1 AND `grep -c "mode=analyze" frontend/src/components/dossier/DossierAnalyzeButton.tsx` >= 1
    - No clearance-revealing copy added: `grep -Eic "clearance|filtered|restricted" frontend/src/components/keyboard-shortcuts/CommandPalette.tsx` == 0
    - `pnpm --filter intake-frontend exec tsc --noEmit` reports no errors in the two files
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && pnpm test -- --run CommandPalette.analyze.test.tsx 2>&1 | tail -12 && [ "$(grep -Ec 'cmd-analyze-(forum-membership|shared-committees|engagement-chains|shortest-path)' src/components/keyboard-shortcuts/CommandPalette.tsx)" = "4" ] && grep -q "mode=analyze" src/components/dossier/DossierAnalyzeButton.tsx && echo "Cmd+K + affordance wired"</automated>
  </verify>
  <done>Four cmd-analyze-* entries pre-fill the entity from the dossier pathname + deep-link with a compact inline result; DossierAnalyzeButton (.btn-ghost) deep-links to Analyze mode. Cmd+K test GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Live UAT on staging — all 4 success criteria, EN+AR</name>
  <action>CHECKPOINT — run the load-bearing live UAT on staging `zkrcjzdemdmwhearhfgg` per the `<how-to-verify>` script: seed the RF-7 fixture, exercise the 4 ROADMAP success criteria in BOTH EN and AR (panel run + Cmd+K; dual-clearance strictly-increasing counts via CDP `Network.setBlockedURLs`; direct `query_graph` invocation returns zero above-clearance nodes under a low-clearance JWT and the node under a high-clearance JWT), then restore the fixture and type the resume-signal with the recorded per-clearance counts. Pauses for human verification.</action>
  <read_first>
    - .planning/phases/71-analytic-graph/71-VALIDATION.md (## Manual-Only Verifications — the 3 UAT rows + the CDP forced-error protocol)
    - .planning/phases/71-analytic-graph/71-RESEARCH.md (### Live UAT criteria #1–#4)
    - backend/tests/intelligence/fixtures/analytic-graph-seed.ts (the seed/restore to reuse for the UAT fixtures)
    - MEMORY (CDP forced-error UAT = Network.setBlockedURLs via cdp-url bridge; language persists under localStorage id.locale; local dev app :5173 → staging; admin kazahrani@stats.gov.sa, pw in .env.test)
  </read_first>
  <what-built>
    The full Analyze experience is live: the Network-panel Analyze mode (71-04), Cmd+K Analyze entries + per-dossier affordance (this plan), backed by the live `query_graph` INVOKER RPC + `analytic-graph` edge fn (71-02/71-03). This checkpoint runs the load-bearing live UAT on staging that the milestone treats as the real proof of GRAPH-01..04.
  </what-built>
  <how-to-verify>
    Seed the RF-7 high-sensitivity fixture (`seedAnalyticGraphFixture`) on staging. Then, on the running app (`:5173` → staging `zkrcjzdemdmwhearhfgg`), in BOTH EN and AR (toggle via the topbar EN/ع buttons; AR persists under `localStorage.id.locale`):
    1. **Criterion #1/#2 (run + Cmd+K):** From a seeded country/org dossier, open `/relationships/graph?dossierId=<id>&mode=analyze`, run "Who sits on which forum" → non-empty structured result. Then via Cmd+K → "Analyze: …" (entity pre-filled from the dossier) → compact inline result + "Open in the Network panel" deep-link. Confirm both in EN and AR (Tajawal, dir=rtl, day-first dates).
    2. **Criterion #3 (clearance reduction — the proof):** Log in as a clearance-1 account and a clearance-3 (and clearance-4) account; run the IDENTICAL query from each; record node/edge counts; assert STRICTLY INCREASING counts with clearance. Assert NO "filtered by clearance" / "restricted" / classification string anywhere (search the rendered UI). Verify the RLS-denied path via CDP `Network.setBlockedURLs` (denial returns empty `200`s — assert on `role="alert"`/empty-state + reduced counts, NOT HTTP status).
    3. **Criterion #4 (agent-tool direct invocation):** Invoke `query_graph` DIRECTLY (`/rest/v1/rpc/query_graph` or `supabase.rpc`) under a LOW-clearance JWT for a query whose full result includes the sensitivity-3/4 fixture node; assert the returned JSONB contains ZERO above-clearance nodes. Repeat under a HIGH-clearance JWT and confirm the node appears (proves the gate is real, not just empty data).
    Restore the fixture (`restoreAnalyticGraphFixture`) and confirm no `TEST `-marked rows remain on staging.
  </how-to-verify>
  <verify>Human confirms all 4 success criteria: panel + Cmd+K non-empty result EN+AR; strictly-increasing node/edge counts clearance-1 < clearance-3 ≤ clearance-4 with zero clearance-revealing copy (CDP forced-error empty-state asserted); direct query_graph returns zero above-clearance nodes under low-clearance and the node under high-clearance; fixture restored.</verify>
  <done>All four ROADMAP success criteria pass live on staging EN+AR; per-clearance counts recorded; fixture restored (no TEST rows remain).</done>
  <resume-signal>Type "UAT passed" once: (1) the query runs from the panel AND Cmd+K with a non-empty result in EN AND AR; (2) clearance-1 < clearance-3 ≤ clearance-4 node/edge counts with zero clearance-revealing copy (CDP forced-error empty-state asserted); (3) direct `query_graph` invocation returns zero above-clearance nodes under low-clearance and the node appears under high-clearance; and the fixture is restored. Paste the recorded counts per clearance level (or describe failures).</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                                                   |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| Cmd+K / affordance → Network panel | Pathname-derived dossier id flows into a deep-link; the actual query runs server-side under the caller JWT.   |
| live UAT → staging                 | Seeds/restores high-sensitivity fixtures on live staging; dual-clearance accounts exercise the real RLS gate. |

## STRIDE Threat Register

| Threat ID             | Category               | Component                                                                                              | Disposition | Mitigation Plan                                                                                                                                                                                                                                      |
| --------------------- | ---------------------- | ------------------------------------------------------------------------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------- | ------ | -------------------------------------------------------------------------------------------------------------------------- |
| T-71-05-INFO-COPY     | Information Disclosure | Cmd+K inline result / i18n copy revealing withheld above-clearance data                                | mitigate    | Zero `clearance                                                                                                                                                                                                                                      | filtered | restricted | hidden | permission` in either language (acceptance grep == 0); reduced result = silently fewer rows; UI-SPEC LOCKED copy contract. |
| T-71-05-INFO-DEEPLINK | Information Disclosure | classified ids/names leaking in the deep-link URL or logs                                              | mitigate    | The deep-link carries only the (already-accessible) anchor `dossierId` + query type — no result content; result payloads are clearance-filtered server-side; no console.log of result content.                                                       |
| T-71-05-FP            | Information Disclosure | UAT passing on all-sensitivity-1 data (clearance "appears" to work because nothing is above clearance) | mitigate    | The RF-7 fixture seeds sensitivity-3 + sensitivity-4 participation off a low anchor; the UAT asserts STRICTLY increasing counts and that the high-clearance account SEES the node the low-clearance account does not (gate is real, not empty data). |
| T-71-05-LEAK          | Information Disclosure | fixture left on staging after UAT                                                                      | mitigate    | `restoreAnalyticGraphFixture`; resume-signal requires confirming zero `TEST `-marked rows remain.                                                                                                                                                    |
| T-71-05-SC            | Tampering              | npm installs                                                                                           | accept      | No new packages — extends CommandPalette + adds one component + JSON, all on existing deps.                                                                                                                                                          |

</threat_model>

<verification>
- i18n: analyze.* keys in en/ar graph + keyboard-shortcuts; AR genuinely translated; EN/AR parity; zero clearance copy; valid JSON.
- Cmd+K: 4 cmd-analyze-* entries pre-fill entity from pathname + deep-link with a compact inline result; Cmd+K test GREEN.
- Per-dossier .btn-ghost Analyze affordance deep-links to Analyze mode.
- Live UAT: all 4 success criteria pass on staging EN+AR (panel run + Cmd+K; strictly-increasing counts via CDP forced-error; direct query_graph invocation zero above-clearance nodes); fixture restored.
</verification>

<success_criteria>

- GRAPH-02 satisfied: an analytic query launches from Cmd+K with clearance-gated results inline + deep-link, verified live EN+AR.
- The third entry point (per-dossier affordance) deep-links with the entity pre-filled (D-04).
- All copy under registered namespaces, EN+AR, zero clearance-revealing strings.
- The live UAT confirms all four ROADMAP success criteria on staging; fixture restored.
  </success_criteria>

<output>
Create `.planning/phases/71-analytic-graph/71-05-SUMMARY.md` when done (record the per-clearance-level counts, the EN+AR confirmation, the direct-invocation result, and fixture cleanup).
</output>
