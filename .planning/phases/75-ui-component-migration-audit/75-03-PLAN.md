---
phase: 75-ui-component-migration-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/75-ui-component-migration-audit/75-AUDIT-aceternity-contracts.md
autonomous: true
requirements: [AUDIT-04]

must_haves:
  truths:
    - 'All 8 Aceternity-styled form components have a written contract covering RHF wiring, Zod linkage, ARIA attributes, keyboard/focus behavior, RTL behavior, and explicitly non-contractual animation-only behavior'
    - 'Each contract leads with its liveness evidence: 7 of 8 have zero external call sites; SearchableSelect is live only via UserPicker'
    - "The UserPicker facade contract is captured with its 4 live consumers, since Phase 79 consumers bind through UserPicker's props, not SearchableSelect's"
    - "A 'Phase 79 rescope input' section records the dead-code finding without rescoping Phase 79 — that decision belongs to the user at Phase 79 planning"
  artifacts:
    - path: '.planning/phases/75-ui-component-migration-audit/75-AUDIT-aceternity-contracts.md'
      provides: 'AUDIT-04 behavioral contracts; consumed by Phase 79 (rebuild-or-delete)'
      contains: 'Phase 79 rescope input'
  key_links:
    - from: 'each contract section'
      to: 'frontend/src/components/forms/<Component>.tsx'
      via: 'contract fields read out of component source, liveness from symbol grep'
      pattern: 'aria-'
    - from: 'SearchableSelect contract'
      to: 'frontend/src/components/forms/UserPicker.tsx'
      via: 'facade contract subsection listing the 4 consumers'
      pattern: 'UserPicker'
---

<objective>
Create the AUDIT-04 contracts artifact: one behavioral contract per Aceternity-styled form component (all 8), each led by fresh liveness evidence, plus the UserPicker facade contract and a Phase 79 rescope-input note.

Purpose: Phase 79 rebuilds (or deletes) these components on HeroUI v3/Radix and must provably preserve RHF/Zod validation, ARIA error announcement, and keyboard-focus behavior (ROADMAP Phase 79 criteria 1-2 verify against THESE contracts). The 7-of-8-dead finding changes the rebuild-vs-delete calculus — record it, do not decide it (RESEARCH Open Question 1 resolution).

Output: `.planning/phases/75-ui-component-migration-audit/75-AUDIT-aceternity-contracts.md`. Docs-only.

Commit note: use `HUSKY=0` for `.planning/*.md` commits (lint-staged churn).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/75-ui-component-migration-audit/75-CONTEXT.md
@.planning/phases/75-ui-component-migration-audit/75-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Artifact skeleton, liveness evidence, and contracts for the 5 simple dead components</name>
  <files>.planning/phases/75-ui-component-migration-audit/75-AUDIT-aceternity-contracts.md</files>
  <read_first>
    - .planning/phases/75-ui-component-migration-audit/75-RESEARCH.md (sections: "The 8 Aceternity-styled components (AUDIT-04)", "Architecture Patterns" Pattern 3, "Code Examples" AUDIT-04 block, "Common Pitfalls" 5)
    - frontend/src/components/forms/FormInputAceternity.tsx
    - frontend/src/components/forms/FormTextareaAceternity.tsx
    - frontend/src/components/forms/FormSelectAceternity.tsx
    - frontend/src/components/forms/FormCheckboxAceternity.tsx
    - frontend/src/components/forms/FormRadioAceternity.tsx
    - frontend/src/components/forms/index.ts (barrel — exported by all, imported by nothing)
  </read_first>
  <action>
    Create `75-AUDIT-aceternity-contracts.md` with:

    1. **Header**: scope (AUDIT-04, consumed by Phase 79), the definition that "Aceternity" here means a `variant="aceternity"` style + `motion/react` usage — not a library import (the ESLint ban is live and CI-enforced), and the warning that `motion/react` is imported by 18 of 22 files in `forms/` so motion usage is NOT the Aceternity discriminator.

    2. **Liveness evidence block**: run the per-component liveness loop from RESEARCH Code Examples (AUDIT-04 block) verbatim, including the UserPicker consumers grep; paste the raw dated output (expected: `FormInputAceternity: 0` ... `SearchableSelect: 0` with UserPicker consumers listed). Also record that `components/forms/index.ts` exports all 8 but nothing imports the barrel (symbol-level grep is the liveness authority — Pitfall "barrel-presence as liveness").

    3. **Contract template statement**: each contract has exactly these fields (RESEARCH Pattern 3): Liveness; RHF wiring (`register`/`FieldError` prop contract, ref chain, error display path incl. server-echoed errors); Zod linkage (where schemas live at the — dead or live — call sites); ARIA (`aria-invalid`, `aria-describedby`, `role="alert"`/`aria-live` error announcement, label association); Keyboard/focus (tab order, focus-visible treatment, Escape/blur handling); RTL (`isRTL`-conditional `ps-12`/`pe-12` icon spacing, icon-side logic); Animation-only behavior (explicitly NON-contractual: rotating placeholders, motion reveals — what Phase 79 may drop).

    4. **Five contract sections** (one `##` heading each, heading contains the component name): FormInputAceternity, FormTextareaAceternity, FormSelectAceternity, FormCheckboxAceternity, FormRadioAceternity. Fill every template field by reading each component file; each Liveness field states "0 external call sites — dead code, barrel-only export" plus the evidence command. For FormInputAceternity specifically, capture the observed shape from RESEARCH: RHF `UseFormRegister`/`FieldError` props, rotating-placeholder `setInterval`, `isRTL`-conditional `ps-12`/`pe-12`, error-conditional border/focus-ring classes, hard-coded Aceternity shadow literals (inside the ESLint carve-out zone), `AnimatePresence` error reveal — verify each against the source, correct if drifted.

    5. Placeholder `##` headings for the remaining 3 components (Task 2 and Task 3).

  </action>
  <verify>
    <automated>C=.planning/phases/75-ui-component-migration-audit/75-AUDIT-aceternity-contracts.md; test -f "$C" && grep -q 'FormInputAceternity: 0' "$C" && grep -q 'FormRadioAceternity: 0' "$C" && for n in FormInputAceternity FormTextareaAceternity FormSelectAceternity FormCheckboxAceternity FormRadioAceternity; do grep -q "^## .*$n" "$C" || exit 1; done && grep -qi 'non-contractual' "$C" && grep -q 'ps-12' "$C"</automated>
  </verify>
  <acceptance_criteria>
    - Artifact exists; liveness block contains the pasted loop output with `<Component>: 0` lines for all 5 components covered here
    - Each of the 5 components has a `##` section containing all 7 template fields (Liveness, RHF wiring, Zod linkage, ARIA, Keyboard/focus, RTL, Animation-only)
    - Every ARIA field cites the actual attributes found in source (FormInputAceternity has 4 ARIA attrs/roles per research — verify against source, record actual)
    - Every Liveness field includes the evidence command, not just the verdict
    - Animation-only fields explicitly say the behavior is non-contractual for Phase 79
  </acceptance_criteria>
  <done>Skeleton + dated liveness evidence + 5 complete contracts for the simple dead components.</done>
</task>

<task type="auto">
  <name>Task 2: Contracts for FormFieldWithValidation and SmartInput</name>
  <files>.planning/phases/75-ui-component-migration-audit/75-AUDIT-aceternity-contracts.md</files>
  <read_first>
    - .planning/phases/75-ui-component-migration-audit/75-AUDIT-aceternity-contracts.md (Task 1 output — template fields and liveness block)
    - frontend/src/components/forms/FormFieldWithValidation.tsx (8 ARIA attrs/roles per research — the densest validation wrapper)
    - frontend/src/components/forms/SmartInput.tsx (6 ARIA attrs/roles)
  </read_first>
  <action>
    Fill the two placeholder sections using the same 7-field template. Both components carry `variant?: 'default' | 'aceternity'` (not a named-Aceternity file) — record the variant-prop marker in each section header line.

    For FormFieldWithValidation: capture the full validation-wrapper contract — how it composes label + control + error region, the `aria-describedby` chaining to the error element, `role="alert"`/`aria-live` usage on error reveal, and how RHF errors (client) versus server-echoed errors flow through (the error display path distinction from RESEARCH Pattern 3). Enumerate the actual ARIA attributes found (research measured 8 — record the live list).

    For SmartInput: capture its input contract — RHF registration surface, suggestion/assist behavior if present in source, ARIA (research measured 6 attrs/roles), keyboard handling (Tab/Escape/blur), and RTL-conditional spacing. Liveness for both: 0 external call sites — dead code; cite the liveness block from Task 1.

  </action>
  <verify>
    <automated>C=.planning/phases/75-ui-component-migration-audit/75-AUDIT-aceternity-contracts.md; grep -q '^## .*FormFieldWithValidation' "$C" && grep -q '^## .*SmartInput' "$C" && grep -q 'aria-describedby' "$C" && grep -q 'aria-invalid' "$C" && grep -qE 'role="alert"|aria-live' "$C"</automated>
  </verify>
  <acceptance_criteria>
    - Both sections contain all 7 template fields
    - FormFieldWithValidation's ARIA field lists the actual attributes found in source, including the error-announcement mechanism (`role="alert"` or `aria-live` — record which)
    - The client-vs-server error display path distinction is documented for FormFieldWithValidation
    - Both Liveness fields state 0 external call sites citing the Task 1 evidence block
  </acceptance_criteria>
  <done>The two variant-prop validation components have complete contracts with live-verified ARIA inventories.</done>
</task>

<task type="auto">
  <name>Task 3: SearchableSelect contract, UserPicker facade contract, and the Phase 79 rescope-input note</name>
  <files>.planning/phases/75-ui-component-migration-audit/75-AUDIT-aceternity-contracts.md</files>
  <read_first>
    - .planning/phases/75-ui-component-migration-audit/75-AUDIT-aceternity-contracts.md (Tasks 1-2 output)
    - frontend/src/components/forms/SearchableSelect.tsx (13 ARIA attrs/roles — combobox pattern, the only live one)
    - frontend/src/components/forms/UserPicker.tsx (the live facade)
    - frontend/src/components/tasks/TaskEditDialog.tsx (UserPicker consumer)
    - frontend/src/components/dossier/AddToDossierDialogs.tsx (UserPicker consumer)
    - frontend/src/components/dossier/wizard/steps/OrgDetailsStep.tsx (UserPicker consumer)
    - frontend/src/components/work-creation/forms/TaskQuickForm.tsx (UserPicker consumer)
  </read_first>
  <action>
    Complete the artifact:

    1. **SearchableSelect contract** (7-field template): this is the only transitively-live component — Liveness states "0 direct external call sites, LIVE via UserPicker" with the consumers grep evidence. Capture the combobox contract in detail: ARIA combobox pattern (research measured 13 attrs/roles — record the live list: role, aria-expanded, aria-activedescendant, listbox/option roles, etc. as actually present), full keyboard nav (arrow keys, Enter select, Escape close, type-ahead/filtering behavior), focus management on open/close, RTL-conditional rendering.

    2. **UserPicker facade contract** subsection: UserPicker's own props surface (what the 4 consumers actually bind to), what it maps down into SearchableSelect, and each consumer's usage in one line (TaskEditDialog, AddToDossierDialogs, OrgDetailsStep, TaskQuickForm). Phase 79 must preserve the FACADE's contract; SearchableSelect internals may change freely behind it.

    3. **Phase 79 rescope input** section (exactly this heading): record that 7 of 8 components are dead code (zero external call sites; the forms barrel is imported by nothing; the milestone research claim that ValidationDemoPage consumes them is stale — that page was deleted in the PR #88/#89 demo cleanup), that Phase 79's ROADMAP still says "8 form components rebuilt on HeroUI v3/Radix", and that the rebuild-vs-delete decision for the dead 7 is a USER decision at Phase 79 planning — this audit supplies evidence only and does not rescope Phase 79 (RESEARCH Open Question 1 resolution).

    4. **Completeness pass**: confirm all 8 `##` component sections exist with all 7 fields; add a top summary table (component | liveness | ARIA count | rebuild-relevant notes) so Phase 79's planner reads the state in ten seconds.

  </action>
  <verify>
    <automated>C=.planning/phases/75-ui-component-migration-audit/75-AUDIT-aceternity-contracts.md; grep -q '^## .*SearchableSelect' "$C" && grep -q 'UserPicker' "$C" && grep -q 'TaskEditDialog' "$C" && grep -q 'AddToDossierDialogs' "$C" && grep -q 'OrgDetailsStep' "$C" && grep -q 'TaskQuickForm' "$C" && grep -q 'Phase 79 rescope input' "$C" && test "$(grep -cE '^## .*(FormInputAceternity|FormTextareaAceternity|FormSelectAceternity|FormCheckboxAceternity|FormRadioAceternity|FormFieldWithValidation|SmartInput|SearchableSelect)' "$C")" -ge 8</automated>
  </verify>
  <acceptance_criteria>
    - All 8 component sections exist (grep count of component `##` headings >= 8)
    - SearchableSelect's ARIA field lists the live combobox attribute inventory and its keyboard-nav behaviors individually (arrows, Enter, Escape at minimum)
    - UserPicker facade subsection names all 4 consumers and states the facade-preservation rule for Phase 79
    - "Phase 79 rescope input" section exists, records the 7-dead finding + stale ValidationDemoPage claim, and explicitly defers the rebuild-vs-delete decision to the user at Phase 79 planning
    - Top summary table covers all 8 components with liveness and ARIA-count columns
  </acceptance_criteria>
  <done>AUDIT-04 complete: 8 contracts + facade contract + rescope-input evidence, addressable by Phase 79 planning.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                                                                                                 |
| ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| contracts → Phase 79 rebuild | Phase 79's a11y/validation success criteria verify against these contracts; an omitted ARIA or keyboard behavior here becomes an invisible regression there |
| audit doc → git history      | committed planning docs are permanent                                                                                                                       |

## STRIDE Threat Register

| Threat ID | Category                                                         | Component                                   | Disposition  | Mitigation Plan                                                                                                                                                         |
| --------- | ---------------------------------------------------------------- | ------------------------------------------- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-75-04   | Elevation of Privilege / validation bypass (downstream, ASVS V5) | RHF/Zod + ARIA error-announcement contracts | mitigate     | 7-field template makes every validation/announcement path an explicit contract field; verify gates require aria-invalid/aria-describedby/role=alert coverage in the doc |
| T-75-05   | Denial of (future) Service — wasted rebuild of dead code         | Phase 79 scope                              | mitigate     | every contract leads with fresh liveness evidence; rescope-input section flags 7-of-8-dead without deciding for the user                                                |
| T-75-01   | Information Disclosure                                           | 75-AUDIT-aceternity-contracts.md            | mitigate     | component source facts only — no runtime data, no gated-RPC samples                                                                                                     |
| T-75-SC   | Tampering                                                        | package installs                            | accept (n/a) | no packages installed this phase                                                                                                                                        |

</threat_model>

<verification>
- All 8 contracts present with the 7 fixed fields; each leads with liveness evidence
- Liveness loop output pasted and dated; re-runnable verbatim
- UserPicker facade contract present with 4 named consumers
- "Phase 79 rescope input" section present; Phase 79 NOT rescoped
</verification>

<success_criteria>

- ROADMAP success criterion 4: each of the 8 components has RHF/Zod, ARIA, and keyboard-focus contracts captured in writing before any rebuild
- RESEARCH Open Question 1 resolved as recommended (capture all 8, lead with liveness, defer the rescope decision)
- Phase 79 criteria 1-2 have a concrete written baseline to verify against
  </success_criteria>

<output>
Create `.planning/phases/75-ui-component-migration-audit/75-03-SUMMARY.md` when done.
</output>
