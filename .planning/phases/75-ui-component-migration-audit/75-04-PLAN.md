---
phase: 75-ui-component-migration-audit
plan: 04
type: execute
wave: 2
depends_on: ['75-01']
files_modified:
  - .planning/phases/75-ui-component-migration-audit/75-AUDIT-classification.md
autonomous: true
requirements: [AUDIT-01]

must_haves:
  truths:
    - 'Every non-test .ts/.tsx file in components/ui/ (73 as of planning) and components/forms/ (22) has a per-file classification row'
    - 'No replace-with-shadcn-primitive row anywhere in the artifact has an empty Behaviors cell (downgrade rule enforced mechanically)'
    - "The 9 Aceternity-derived ui/ primitives are classified keep-custom with a 'dead — delete candidate' Notes advisory backed by fresh 0-importer evidence (world-map: 1 importer)"
    - 'The three heroui-* lookalike files are classified on their ACTUAL implementation (CVA/Radix), not their filenames'
    - 'Row coverage reconciles arithmetically with the live file/directory counts, recorded in a Coverage reconciliation section'
  artifacts:
    - path: '.planning/phases/75-ui-component-migration-audit/75-AUDIT-classification.md'
      provides: 'AUDIT-01 per-file tier + enforcement sweep + coverage reconciliation; consumed by Phases 76 and 77'
      contains: 'Coverage reconciliation'
  key_links:
    - from: 'per-file ui/ rows'
      to: 'frontend/src/components/ui/COMPONENT_REGISTRY.md'
      via: 'registry used as naming seed, never as evidence (stale since Phase 48)'
      pattern: 'components/ui/'
    - from: 'Behaviors cells'
      to: 'downgrade rule in the artifact header'
      via: 'awk enforcement gate — empty behaviors on primitive-replace rows fail verification'
      pattern: 'replace-with-shadcn-primitive'
---

<objective>
Complete the AUDIT-01 classification artifact: per-file rows for every non-test file in `components/ui/` and `components/forms/` (the only tiers where primitive-replace is plausible), then a mechanical enforcement sweep (downgrade rule, domain-signal defaults) and an arithmetic coverage reconciliation.

Purpose: this tier is where Phase 77 (TOKEN-06 re-skin + the ~74-literal ESLint carve-out decision) and Phase 79 (dead Aceternity-derived primitives) get their actionable rows. The rules were fixed by plan 75-01; this plan applies them at file granularity.

Output: `75-AUDIT-classification.md` completed (both tiers + reconciliation). Docs-only — no production code changes.

Commit note: use `HUSKY=0` for `.planning/*.md` commits (lint-staged churn).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-ui-component-migration-audit/75-AUDIT-classification.md
@.planning/phases/75-ui-component-migration-audit/75-RESEARCH.md
@frontend/src/components/ui/COMPONENT_REGISTRY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-file rows for components/ui/ — first alphabetical half (through heroui-switch.tsx)</name>
  <files>.planning/phases/75-ui-component-migration-audit/75-AUDIT-classification.md</files>
  <read_first>
    - .planning/phases/75-ui-component-migration-audit/75-AUDIT-classification.md (rulebook header, row schema, domain-signal lists from plan 75-01)
    - frontend/src/components/ui/COMPONENT_REGISTRY.md (naming seed — stale, orientation only, never evidence)
    - .planning/phases/75-ui-component-migration-audit/75-RESEARCH.md (sections: "Aceternity-derived ui/ primitives", "Common Pitfalls" 1 and 3, "Code Examples" Aceternity-derived liveness block)
    - The component files being classified in this batch (enumerate with the sort command in the action; triage by grep on imports/domain signals first, read fully only where the classification or Behaviors cell is ambiguous)
  </read_first>
  <action>
    Fill the "Per-file classification: components/ui/ and components/forms/" section for the FIRST 37 files of `find frontend/src/components/ui -type f \( -name '*.tsx' -o -name '*.ts' \) ! -name '*.test.*' | sort` — the batch ends at `ui/heroui-switch.tsx` (next file, `ui/heroui-tabs.tsx`, opens Task 2's batch). One row per file, Surface cell = `components/ui/<file>` exactly as emitted by the find (verification greps key on the `components/ui/` prefix).

    Per-row rules (in addition to the header rulebook):
    - Classify by imports and rendered output, never by filename/docstring. In this batch that means: `heroui-button/card/forms/modal/skeleton.tsx` are real HeroUI v3 wrappers (keep as HeroUI wrappers — classification note "HeroUI v3 wrapper — Phase 78 surface, not a shadcn target"); `heroui-chip.tsx` and `heroui-switch.tsx` are lookalikes with NO HeroUI import — classify on their actual CVA/Radix/plain implementation and flag the stale chip docstring in Notes.
    - Aceternity-derived dead primitives in this batch (`animated-tooltip`, `background-boxes`, `expandable-card`, `floating-dock`): run the Aceternity-derived liveness loop from RESEARCH Code Examples verbatim ONCE, paste the dated output into the artifact's evidence appendix, and classify all nine names it covers as keep-custom with Notes "dead — delete candidate (0 importers; Phase 79/77 input)" — taxonomy is never extended; the advisory lives in Notes (RESEARCH Open Question 3). `world-map` (Task 2's batch) is the exception with 1 importer.
    - Genuine primitive candidates (e.g. token-bound button/badge/card/input-style wrappers) may classify replace-with-shadcn-primitive ONLY with a concrete Behaviors cell: name the token bindings (var(--*) usage), RTL logical-property behavior, focus-visible treatment, aria wiring, density (`--row-h`) obedience — whatever the file actually implements that a shadcn primitive must reproduce. An empty Behaviors cell downgrades to keep-custom per the header rule.
    - Direction-owner files (any file here that sets `dir=` or is RTL infrastructure, e.g. `ltr-isolate.tsx` when reached) classify keep-custom per the narrow RTL reading.
    - Files inside the ESLint color-literal carve-out with hard-coded literals get Notes "TOKEN-06 carve-out — Phase 77 keep-or-migrate decision" so Phase 77 can grep for its scope.

  </action>
  <verify>
    <automated>A=.planning/phases/75-ui-component-migration-audit/75-AUDIT-classification.md; test "$(grep -cE '^\| components/ui/' "$A")" -ge 37 && grep -q 'animated-tooltip' "$A" && grep -qi 'delete candidate' "$A" && grep -q 'heroui-chip' "$A"</automated>
  </verify>
  <acceptance_criteria>
    - At least 37 rows matching `^| components/ui/` exist, covering every file of the sorted batch through heroui-switch.tsx
    - The Aceternity-derived liveness loop output is pasted (dated) in an evidence appendix; the batch's dead primitives carry "dead — delete candidate" in Notes with keep-custom classification
    - heroui-chip.tsx and heroui-switch.tsx rows classify on actual implementation with the lookalike fact in Evidence; the 5 real HeroUI wrapper rows are marked as Phase 78 surface
    - Every replace-with-shadcn-primitive row in the batch has a non-empty Behaviors cell naming at least one concrete behavior (token binding, RTL, focus, ARIA, or density)
  </acceptance_criteria>
  <done>First 37 ui/ files classified per-file with evidence; dead-primitive advisory and lookalike traps recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Per-file rows for the remaining ui/ files and all of components/forms/</name>
  <files>.planning/phases/75-ui-component-migration-audit/75-AUDIT-classification.md</files>
  <read_first>
    - .planning/phases/75-ui-component-migration-audit/75-AUDIT-classification.md (header + Task 1 rows + evidence appendix)
    - .planning/phases/75-ui-component-migration-audit/75-RESEARCH.md (section "The 8 Aceternity-styled components (AUDIT-04)" — liveness table reused for forms/ rows)
    - The component files being classified in this batch (remaining ui/ from heroui-tabs.tsx onward, plus all 22 non-test forms/ files; same grep-first triage as Task 1)
  </read_first>
  <action>
    Add per-file rows for (a) the REMAINING files of the sorted ui/ list (from `ui/heroui-tabs.tsx` to the end — 36 files as of planning; use the live list) and (b) ALL non-test files in `components/forms/` (22 as of planning), same row schema.

    Batch-specific rules:
    - `heroui-tabs.tsx` is the third lookalike (@radix-ui/react-tabs, no HeroUI) — classify on the Radix implementation.
    - Aceternity-derived dead primitives in this batch (`layout-grid`, `moving-border`, `placeholders-and-vanish-input`, `text-generate-effect`): keep-custom + "dead — delete candidate" Notes, citing the Task 1 evidence appendix. `world-map.tsx`: 1 live importer (`geographic-visualization/WorldMapVisualization.tsx`) — keep-custom, Notes "single importer; not a delete candidate without Phase 77 review".
    - `ltr-isolate.tsx` and any other direction-owner file: keep-custom per the narrow RTL reading (RTL infrastructure).
    - forms/ rows: the 8 Aceternity-styled components classify keep-custom with Notes "AUDIT-04 contract captured in 75-AUDIT-aceternity-contracts.md; 7 of 8 dead — Phase 79 rescope input" (do not duplicate the contracts here; cross-reference the artifact). `UserPicker.tsx` and `ContextualHelp.tsx` are the live forms files — classify with their actual consumer evidence (UserPicker: 4 consumers; ContextualHelp/FieldLabelWithHelp: wizard steps). Remaining forms/ files classify on their own imports/usage with liveness evidence per row (symbol-level grep, never barrel presence).
    - Same primitive-replace discipline as Task 1: non-empty Behaviors cell or downgrade.

  </action>
  <verify>
    <automated>A=.planning/phases/75-ui-component-migration-audit/75-AUDIT-classification.md; test "$(grep -cE '^\| components/ui/' "$A")" -eq "$(find frontend/src/components/ui -type f \( -name '*.tsx' -o -name '*.ts' \) ! -name '*.test.*' | wc -l | tr -d ' ')" && test "$(grep -cE '^\| components/forms/' "$A")" -eq "$(find frontend/src/components/forms -type f \( -name '*.tsx' -o -name '*.ts' \) ! -name '*.test.*' | wc -l | tr -d ' ')" && grep -q 'world-map' "$A" && grep -q 'UserPicker' "$A"</automated>
  </verify>
  <acceptance_criteria>
    - Count of `^| components/ui/` rows equals the live non-test ui/ file count (73 as of planning) — full per-file coverage, self-calibrating against tree drift
    - Count of `^| components/forms/` rows equals the live non-test forms/ file count (22 as of planning)
    - world-map row records its 1 importer by name; the other 4 dead primitives in this batch carry the delete-candidate Notes advisory
    - All 8 Aceternity-styled forms/ rows cross-reference 75-AUDIT-aceternity-contracts.md instead of duplicating contracts
    - UserPicker and ContextualHelp rows cite named consumers as evidence
  </acceptance_criteria>
  <done>Per-file tier complete: every non-test ui/ and forms/ file has an evidenced row.</done>
</task>

<task type="auto">
  <name>Task 3: Enforcement sweep and coverage reconciliation</name>
  <files>.planning/phases/75-ui-component-migration-audit/75-AUDIT-classification.md</files>
  <read_first>
    - .planning/phases/75-ui-component-migration-audit/75-AUDIT-classification.md (the full artifact — both tiers)
    - .planning/phases/75-ui-component-migration-audit/75-RESEARCH.md (section "Verified Ground Truth" — domain-signal file sets, for the cross-check)
  </read_first>
  <action>
    Three mechanical passes over the completed artifact, then a reconciliation section:

    1. **Downgrade-rule sweep**: find every row classified replace-with-shadcn-primitive; any with an empty/dash-only Behaviors cell is rewritten to keep-custom with Notes "downgraded — no behaviors listed". The awk gate in verify enforces this.

    2. **Domain-signal cross-check**: for each file on the clearance (10) and flags/glyphs (13) lists in the artifact's baseline section, confirm its covering row (per-file row if in ui/forms — none are expected there — otherwise its directory row, or a per-file exception row) is keep-custom or block-with-domain-wrapper. Confirm every direction-owner file in ui/ (from the 94-file dir-owner set) has a keep-custom row. Record the check's result as one line per list ("clearance: 10/10 covered keep-custom — PASS").

    3. **Coverage reconciliation section** (exactly the heading "Coverage reconciliation"): record the arithmetic — live non-test file count under components/ (`find frontend/src/components -type f \( -name '*.tsx' -o -name '*.ts' \) ! -name '*.test.*' | wc -l`), per-file rows (ui + forms counts), per-directory rows (count), and the statement that per-directory rows cover their directories' remaining N files (compute N = total − ui − forms). Paste command outputs, dated.

    4. **Consumer hand-off summary** at the top of the artifact: three bullets — for Phase 76 (the direction-owner surface + RTL-infrastructure rows), for Phase 77 (the TOKEN-06 carve-out rows + re-skin scope = replace/keep verdicts in ui/), for Phase 79 (the dead-primitive delete candidates + the forms/ cross-reference). Include the final label tallies (count per classification label).

  </action>
  <verify>
    <automated>A=.planning/phases/75-ui-component-migration-audit/75-AUDIT-classification.md; awk -F'|' '$3 ~ /replace-with-shadcn-primitive/ { c=$5; gsub(/[ \t—–-]/, "", c); if (c == "") bad++ } END { exit bad > 0 }' "$A" && grep -q 'Coverage reconciliation' "$A" && grep -q '10/10' "$A" && awk -F'|' '$2 ~ /ltr-isolate/ && $3 !~ /keep-custom/ {bad++} END {exit bad>0}' "$A"</automated>
  </verify>
  <acceptance_criteria>
    - awk downgrade gate passes: zero replace-with-shadcn-primitive rows with empty/dash-only Behaviors cells anywhere in the artifact
    - Domain-signal cross-check lines present with explicit per-list PASS results (clearance 10/10, flags/glyphs 13/13, ui/ direction-owners)
    - "Coverage reconciliation" section contains the dated find-command outputs and the arithmetic tying rows to the live tree (per-file rows + per-directory-covered files = total non-test files)
    - Consumer hand-off summary present with per-label tallies and one bullet each for Phases 76, 77, 79
    - ltr-isolate.tsx (and every RTL-infrastructure row) is keep-custom
  </acceptance_criteria>
  <done>AUDIT-01 artifact complete and internally enforced: every surface labeled, downgrade rule mechanically clean, coverage arithmetic reconciled, hand-off summaries addressed to Phases 76/77/79.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                                                                                           |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| classification rows → Phases 77/79 executors | replace/delete verdicts here become code deletions and re-skins later; a wrong "replace" on a domain surface drops protected behavior |
| audit doc → git history                      | committed planning docs are permanent                                                                                                 |

## STRIDE Threat Register

| Threat ID | Category                                           | Component                                         | Disposition  | Mitigation Plan                                                                                                                          |
| --------- | -------------------------------------------------- | ------------------------------------------------- | ------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| T-75-02   | Elevation of Privilege (downstream)                | per-file classification of domain-signal surfaces | mitigate     | Task 3 cross-check requires explicit 10/10 and 13/13 PASS lines; ltr-isolate awk gate; narrow-RTL keep-custom rule from the 75-01 header |
| T-75-06   | Tampering (self) — classification without evidence | Evidence cells                                    | mitigate     | every row requires a non-empty Evidence cell; liveness claims come from pasted symbol greps, never barrel presence or filenames          |
| T-75-01   | Information Disclosure                             | 75-AUDIT-classification.md                        | mitigate     | paths/commands/counts only — no runtime data                                                                                             |
| T-75-SC   | Tampering                                          | package installs                                  | accept (n/a) | no packages installed this phase                                                                                                         |

</threat_model>

<verification>
- ui/ and forms/ per-file row counts equal live non-test file counts (self-calibrating find comparisons)
- Downgrade awk gate green over the whole artifact
- Domain cross-check PASS lines present; RTL-infrastructure rows keep-custom
- Coverage reconciliation arithmetic present and dated
</verification>

<success_criteria>

- ROADMAP success criterion 1 fully satisfied: every hand-rolled surface labeled with one of the three labels; every primitive-replace row lists preserved behaviors; empty lists downgraded
- ROADMAP success criterion 5 mechanically enforced across both tiers
- Phases 76, 77, and 79 each have an addressable hand-off summary
  </success_criteria>

<output>
Create `.planning/phases/75-ui-component-migration-audit/75-04-SUMMARY.md` when done.
</output>
