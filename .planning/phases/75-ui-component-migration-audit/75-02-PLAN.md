---
phase: 75-ui-component-migration-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/75-ui-component-migration-audit/75-AUDIT-heroui-confirmation.md
autonomous: true
requirements: [AUDIT-02, AUDIT-03]

must_haves:
  truths:
    - 'The complete @heroui/react import-site list (expected: 8 files) is recorded with per-file import names and API-style evidence'
    - 'A green `pnpm --dir frontend type-check` is recorded as the mechanical v3-conformance proof, alongside the compound-usage spot-check of the Card/Checkbox/Switch/Modal wrappers'
    - 'Zero imports of the 8 listed removed names plus Ripple are recorded with the exact command and its exit code — a confirmation, not a hunt'
    - 'The two false-positive traps are documented: v3 flat-named subcomponent exports (TweaksDrawer) are conformant, and the three heroui-* lookalike files contain no HeroUI at all'
    - 'The Autocomplete-exists-in-3.0.5 nuance is documented so Phase 78 does not chase a stale removal claim'
  artifacts:
    - path: '.planning/phases/75-ui-component-migration-audit/75-AUDIT-heroui-confirmation.md'
      provides: 'AUDIT-02 + AUDIT-03 confirmation evidence; consumed by Phase 78 (bump + regression sweep)'
      contains: 'Import-site count:'
  key_links:
    - from: '.planning/phases/75-ui-component-migration-audit/75-AUDIT-heroui-confirmation.md'
      to: 'frontend/src (live tree)'
      via: 'machine-readable summary lines re-derivable by re-running the recorded commands'
      pattern: 'Import-site count: [0-9]+'
    - from: 'Phase 78 re-run protocol section'
      to: 'AUDIT-02/03 evidence commands'
      via: 'same commands, expected outputs listed'
      pattern: 'type-check'
---

<objective>
Create the AUDIT-02/03 confirmation artifact: the verified HeroUI v3 import inventory with API-style evidence and the mechanical type-check proof (AUDIT-02), plus the zero-imports confirmation for the v3-removed component names with the documented nuances (AUDIT-03).

Purpose: Phase 78 (HeroUI 3.0.5 → 3.2.1 bump) re-runs exactly these commands as its regression sweep. This artifact is a confirmation with evidence — CONTEXT.md explicitly forbids re-opening a v2→v3 migration narrative.

Output: `.planning/phases/75-ui-component-migration-audit/75-AUDIT-heroui-confirmation.md`. Docs-only — no production code changes (the stale `heroui-chip.tsx` docstring is FLAGGED here, fixed in Phase 78).

Commit note: use `HUSKY=0` for `.planning/*.md` commits (lint-staged churn).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/75-ui-component-migration-audit/75-CONTEXT.md
@.planning/phases/75-ui-component-migration-audit/75-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the AUDIT-02 import inventory, compound-usage spot-check, and type-check proof</name>
  <files>.planning/phases/75-ui-component-migration-audit/75-AUDIT-heroui-confirmation.md</files>
  <read_first>
    - .planning/phases/75-ui-component-migration-audit/75-RESEARCH.md (sections: "HeroUI surface (AUDIT-02)", "Common Pitfalls" 2 and 3, "Code Examples" AUDIT-02 block)
    - frontend/src/components/ui/heroui-forms.tsx (compound Checkbox.Control/.Indicator, Switch.Control/.Thumb usage)
    - frontend/src/components/ui/heroui-modal.tsx (compound Modal.* usage)
    - frontend/src/components/ui/heroui-card.tsx
    - frontend/src/components/tweaks/TweaksDrawer.tsx (flat-named subcomponent imports — the conformant nuance)
    - frontend/src/components/ui/heroui-chip.tsx (lookalike with stale docstring)
    - frontend/src/components/ui/heroui-switch.tsx (lookalike)
    - frontend/src/components/ui/heroui-tabs.tsx (lookalike)
  </read_first>
  <action>
    Create `75-AUDIT-heroui-confirmation.md` with:

    1. **Header** stating scope: AUDIT-02 + AUDIT-03 confirmations for Phase 78; evidence style is command + raw output + date + interpretation, per RESEARCH Pattern 2.

    2. **AUDIT-02 import inventory**: run the import-site list command from RESEARCH Code Examples (AUDIT-02 block) verbatim; paste raw output. Build the 8-row table matching RESEARCH "HeroUI surface": file | what it imports | API style. Expected files: `ui/heroui-button.tsx`, `ui/heroui-card.tsx`, `ui/heroui-forms.tsx`, `ui/heroui-modal.tsx`, `ui/heroui-skeleton.tsx`, `layout/AppShell.tsx`, `tweaks/TweaksDrawer.tsx`, `layout/ConcurrentDrawers.test.tsx`. If the live list differs, record the delta as a finding (do not force-fit the expected list). Add machine-readable line: `Import-site count: <N>`.

    3. **Compound-usage spot-check** across the ROADMAP-required wrappers (Card/Checkbox/Switch/Modal): run the spot-check grep from RESEARCH Code Examples verbatim, paste output, and interpret (dot-notation compound API confirmed in heroui-forms.tsx and heroui-modal.tsx; heroui-card.tsx wraps `Card`).

    4. **Mechanical conformance proof**: run `pnpm --dir frontend type-check`; record command, exit code, and date. Machine-readable line: `Type-check: exit <code>`. If non-zero, failures are pre-existing and get listed + triaged as findings, not hidden (RESEARCH assumption A2).

    5. **Nuance 1 — flat-named subcomponent exports are v3**: document that `TweaksDrawer.tsx` imports `DrawerBackdrop, DrawerContent, DrawerDialog, DrawerHeader, DrawerBody` and that `node_modules/@heroui/react/dist/components/drawer/index.d.ts` exports exactly these names — v3 ships both spellings; the removed v2 pattern is the prop-driven monolith (`<Modal title isOpen>` with content props). Verify the dist file exists and grep it for `DrawerBackdrop`; paste the evidence.

    6. **Nuance 2 — heroui-* filename does not mean HeroUI**: document that `heroui-chip.tsx` (CVA + @radix-ui/react-slot; stale docstring claims "Real @heroui/react Chip primitive"), `heroui-switch.tsx` (plain button+span), and `heroui-tabs.tsx` (@radix-ui/react-tabs) contain no `@heroui/react` import. Flag the stale chip docstring as a Phase 78 cleanup candidate — do NOT edit the component in this phase.

    7. Note `@heroui-pro/react` 1.0.0-beta.6 is installed with 0 imports; first import will need `HEROUI_AUTH_TOKEN` in CI.

  </action>
  <verify>
    <automated>H=.planning/phases/75-ui-component-migration-audit/75-AUDIT-heroui-confirmation.md; LIVE=$(grep -rln "@heroui/react" --include='*.tsx' --include='*.ts' frontend/src | wc -l | tr -d ' '); test -f "$H" && grep -q "Import-site count: $LIVE" "$H" && grep -q 'Type-check: exit 0' "$H" && grep -q 'DrawerBackdrop' "$H" && grep -q 'heroui-chip' "$H" && grep -q 'heroui-tabs' "$H" && grep -q 'HEROUI_AUTH_TOKEN' "$H"</automated>
  </verify>
  <acceptance_criteria>
    - Artifact exists and its `Import-site count: N` line equals the live `grep -rln "@heroui/react" ... | wc -l` count at verification time
    - The import table lists every live import site with its imported names and an API-style verdict
    - `Type-check: exit 0` recorded (or, if red, the failure list is present and the summary says so — verify gate then requires triage before done)
    - Nuance sections for flat-named Drawer exports and the three heroui-* lookalikes both present, each with pasted command evidence
    - Stale `heroui-chip.tsx` docstring flagged as Phase 78 cleanup, with an explicit "no code edits in Phase 75" note
  </acceptance_criteria>
  <done>AUDIT-02 confirmed with dated evidence: complete import inventory, compound spot-check, green type-check, both false-positive traps documented for Phase 78.</done>
</task>

<task type="auto">
  <name>Task 2: Record the AUDIT-03 removed-components confirmation and the Phase 78 re-run protocol</name>
  <files>.planning/phases/75-ui-component-migration-audit/75-AUDIT-heroui-confirmation.md</files>
  <read_first>
    - .planning/phases/75-ui-component-migration-audit/75-AUDIT-heroui-confirmation.md (Task 1 output)
    - .planning/phases/75-ui-component-migration-audit/75-RESEARCH.md (sections: "v3-removed components (AUDIT-03)", "Code Examples" AUDIT-03 block, "State of the Art")
  </read_first>
  <action>
    Append to the artifact:

    1. **AUDIT-03 confirmation**: run the removed-names grep from RESEARCH Code Examples (AUDIT-03 block) verbatim — it covers the 8 requirement names (Navbar, Snippet, User, Spacer, Image, Code, Autocomplete, DateInput) plus Ripple. Paste command, raw output, and exit code. Expected: 0 hits, exit 1. Machine-readable line: `Removed-name import hits: 0 (exit 1)`. If any hit appears, list each hit with a per-hit replacement plan (native HTML per official migration docs, or v3 ComboBox/DateField for Autocomplete/DateInput) — this is the ROADMAP criterion-3 regression path.

    2. **Removed-list nuance**: document that the official v3-removed list is 7 names (Code, Image, Navbar, Ripple, Snippet, Spacer, User — replaced with native HTML, per heroui.com incremental-migration docs); the project requirement's 8-name list adds Autocomplete + DateInput and omits Ripple; AND `Autocomplete` exists as an export in installed `@heroui/react` 3.0.5 (`node_modules/@heroui/react/dist/components/autocomplete/index.d.ts`) — so the recorded claim is "0 imports of any of the 9 checked names", an import-evidence statement, not a removal claim. Verify the autocomplete dist file exists and paste the check.

    3. **Phase 78 re-run protocol** section: list the exact commands Phase 78 re-runs after the 3.0.5→3.2.1 bump (import-site grep, removed-names grep, `pnpm --dir frontend type-check`, compound spot-check) with their expected outputs (same import list, exit 1, exit 0). Note version-coupling: `@heroui/react` and `@heroui/styles` bump in lockstep. Note `npm view @heroui/react version` returned 3.2.1 on 2026-07-02.

    4. **Findings summary** at the top of the artifact (after the header): one table with the AUDIT-02 verdict (confirmed on v3 compound API / residual flat-prop call sites: none-or-list) and AUDIT-03 verdict (confirmed unused / regressions: none-or-list), so Phase 78's planner reads the conclusion in ten seconds.

  </action>
  <verify>
    <automated>H=.planning/phases/75-ui-component-migration-audit/75-AUDIT-heroui-confirmation.md; grep -rnE "import\s*\{[^}]*\b(Navbar|Snippet|User|Spacer|Image|Code|Autocomplete|DateInput|Ripple)\b[^}]*\}\s*from\s*['\"]@heroui/react['\"]" --include='*.tsx' --include='*.ts' frontend/src >/dev/null; test $? -eq 1 && grep -q 'Removed-name import hits: 0 (exit 1)' "$H" && grep -q 'Ripple' "$H" && grep -q 'autocomplete/index.d.ts' "$H" && grep -qi 'Phase 78 re-run' "$H"</automated>
  </verify>
  <acceptance_criteria>
    - The removed-names grep re-run at verification time still exits 1 (0 hits) AND the artifact records `Removed-name import hits: 0 (exit 1)` with the verbatim command
    - The 7-vs-8-name discrepancy and the Autocomplete-exists-in-3.0.5 fact are both documented with the dist-file path as evidence
    - A "Phase 78 re-run protocol" section lists every evidence command with its expected output
    - A findings-summary table near the top states the AUDIT-02 and AUDIT-03 verdicts in one row each
    - If (and only if) the grep found hits: each hit has a named replacement plan row
  </acceptance_criteria>
  <done>AUDIT-03 confirmed with dated evidence and nuances; Phase 78 has a copy-paste re-run protocol and a ten-second findings summary.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                                                          |
| ----------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| audit doc → Phase 78 executor | Phase 78 runs a bump trusting this confirmation; a wrong verdict here turns the "light phase" into a silent breakage |
| audit doc → git history       | committed planning docs are permanent                                                                                |

## STRIDE Threat Register

| Threat ID | Category                     | Component                       | Disposition  | Mitigation Plan                                                                                                                                                       |
| --------- | ---------------------------- | ------------------------------- | ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-75-03   | Repudiation / stale-evidence | confirmation verdicts           | mitigate     | every verdict pairs command + raw output + date + machine-readable summary line; verify gates re-run the greps live so the doc cannot claim what the tree contradicts |
| T-75-01   | Information Disclosure       | 75-AUDIT-heroui-confirmation.md | mitigate     | paths, package names, and command output only — no secrets, no runtime data                                                                                           |
| T-75-SC   | Tampering                    | package installs                | accept (n/a) | no packages installed this phase; @heroui-pro token requirement is documented, not exercised                                                                          |

</threat_model>

<verification>
- Artifact exists with both AUDIT-02 and AUDIT-03 sections, each command + raw output + date + interpretation
- Machine-readable lines (`Import-site count:`, `Type-check: exit`, `Removed-name import hits:`) all present and consistent with live re-runs
- Both nuances (flat-named exports; Autocomplete-in-3.0.5) documented
- Phase 78 re-run protocol present
</verification>

<success_criteria>

- ROADMAP success criterion 2: v3 compound API confirmed, residual flat-prop call sites recorded (expected: none)
- ROADMAP success criterion 3: removed names confirmed unused; regression path (per-hit replacement plan) defined
- No code edits; no v2→v3 migration narrative re-opened
  </success_criteria>

<output>
Create `.planning/phases/75-ui-component-migration-audit/75-02-SUMMARY.md` when done.
</output>
