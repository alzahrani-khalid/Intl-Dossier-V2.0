---
phase: 75-ui-component-migration-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/75-ui-component-migration-audit/75-AUDIT-classification.md
autonomous: true
requirements: [AUDIT-01]

must_haves:
  truths:
    - 'The classification artifact exists with the fixed 3-label taxonomy, the empty-behaviors downgrade rule, and the criterion-5 domain-signal defaults stated in its header'
    - 'The RTL keep-custom trigger is operationalized with the narrow direction-owner reading, explicitly stated in the artifact header (RESEARCH Open Question 2 resolved, not silently assumed)'
    - 'Every top-level directory under frontend/src/components/ except ui/ and forms/ has a per-directory classification row with domain-signal and evidence cells'
    - 'Directories on the clearance/flags/dossier-type domain-signal lists classify keep-custom or shadcn-block-with-domain-wrapper, never replace-with-shadcn-primitive'
  artifacts:
    - path: '.planning/phases/75-ui-component-migration-audit/75-AUDIT-classification.md'
      provides: 'AUDIT-01 classification (header rules + per-directory tier); consumed by Phases 76 and 77'
      contains: 'replace-with-shadcn-primitive'
  key_links:
    - from: '.planning/phases/75-ui-component-migration-audit/75-AUDIT-classification.md'
      to: '.planning/phases/75-ui-component-migration-audit/75-RESEARCH.md'
      via: 'evidence commands reused verbatim from RESEARCH Code Examples, with dated raw output pasted'
      pattern: 'find . -type f'
    - from: 'per-directory rows'
      to: 'domain-signal file lists (clearance 10 / flags 13 / dossier-type 62)'
      via: 'domain-signal cell citing which list hits the directory'
      pattern: 'keep-custom'
---

<objective>
Create the AUDIT-01 classification artifact and complete its first tier: the header rulebook (taxonomy, downgrade rule, domain-signal defaults, narrow RTL operationalization), a dated evidence baseline, and one classification row per top-level component directory outside `ui/` and `forms/`.

Purpose: Phases 76 (RTL bridge surface) and 77 (TOKEN-06 re-skin scope) read this artifact to know exactly which surfaces are replaceable and which carry protected domain behavior. This plan sets the rules and covers the ~114-directory feature tier; plan 75-04 adds the per-file tier for `ui/` + `forms/`.

Output: `.planning/phases/75-ui-component-migration-audit/75-AUDIT-classification.md` (header + evidence baseline + per-directory table). Docs-only — no production code changes.

Commit note: `.planning/*.md` commits historically need `HUSKY=0` because lint-staged/prettier churns partially-staged planning files; pre-format tables with prettier-compatible markdown.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/75-ui-component-migration-audit/75-CONTEXT.md
@.planning/phases/75-ui-component-migration-audit/75-RESEARCH.md
@.planning/codebase/STRUCTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the classification artifact skeleton with rulebook header and dated evidence baseline</name>
  <files>.planning/phases/75-ui-component-migration-audit/75-AUDIT-classification.md</files>
  <read_first>
    - .planning/phases/75-ui-component-migration-audit/75-RESEARCH.md (sections: "Verified Ground Truth", "Architecture Patterns" Pattern 1, "Code Examples" AUDIT-01 block, "Open Questions" Q2 and Q3)
    - .planning/phases/75-ui-component-migration-audit/75-CONTEXT.md (decisions section — taxonomy and defaults are fixed)
    - .planning/codebase/STRUCTURE.md (directory-purpose orientation)
  </read_first>
  <action>
    Create `75-AUDIT-classification.md` with these sections, in order:

    1. **Header / rulebook** stating verbatim rules: (a) taxonomy is fixed at exactly three labels — `replace-with-shadcn-primitive`, `keep-custom (domain-specific)`, `replace-with-shadcn-block`; (b) the downgrade rule — any `replace-with-shadcn-primitive` row with an empty "Behaviors the primitive must preserve" cell is invalid and is downgraded to keep-custom; (c) the criterion-5 default — components touching clearance, RTL directionality, flags/glyphs, or dossier-type logic default to keep-custom or shadcn-block-with-domain-wrapper, never primitive-replace; (d) the RTL operationalization — adopt the NARROW direction-owner reading from RESEARCH Open Question 2: "touching RTL directionality" means the component owns direction (sets a `dir=` attribute — 94 files, implements direction-conditional positioning/animation/portal logic, or is RTL infrastructure such as `rtl-wrapper/`, `ui/ltr-isolate.tsx`, `modern-nav/` direction logic); mere `isRTL` consumption for icon flips or logical spacing does NOT force keep-custom — that behavior goes into the "Behaviors to preserve" cell instead. State explicitly that this narrow reading was a planner decision resolving RESEARCH Open Question 2 so the criterion-5 audit trail is honest. (e) classification is by imports and rendered output, never by filename or docstring (the `heroui-chip.tsx` lookalike trap).

    2. **Row schema** — one fixed 6-column table format used by BOTH tiers: `| Surface | Classification | Domain signals (clearance/RTL/flags/dossier-type) | Behaviors the primitive must preserve | Evidence | Notes |`. Surface cells are repo-relative paths: directory rows end in `/` (e.g. `components/dossier/`), file rows are full paths (e.g. `components/ui/badge.tsx`). Advisory labels like "dead — delete candidate" go in Notes only; the taxonomy itself is never extended (RESEARCH Open Question 3 resolution).

    3. **Evidence baseline** — re-run the AUDIT-01 evidence command block from RESEARCH.md "Code Examples" (tree size, test count, clearance grep, flags/glyphs grep, dossier-type count, dir-owner count) plus `ls -d frontend/src/components/*/ | wc -l` for the top-level directory count, and paste the raw dated output. Expected values as of research date 2026-07-02: 799 total files, 99 tests, 10 clearance files, 13 flags/glyphs files, 62 dossier-type files, 94 dir-owner files, 116 top-level directories — if any number drifted, record the new number (the tree moves with merges; fresh output wins over research).

    4. **Domain-signal file lists** — copy the pre-computed clearance (10 files) and flags/glyphs (13 files) lists from RESEARCH "Verified Ground Truth" into the artifact, and record the dossier-type list via its evidence command output (62 files). These lists drive the criterion-5 defaults in both tiers.

    5. Empty placeholder headings for "Per-directory classification" (filled by Task 2) and "Per-file classification: components/ui/ and components/forms/" plus "Coverage reconciliation" (filled by plan 75-04).

    Do not embed any runtime data, secrets, or gated-RPC payload samples — file paths, component names, counts, and commands only.

  </action>
  <verify>
    <automated>A=.planning/phases/75-ui-component-migration-audit/75-AUDIT-classification.md; test -f "$A" && grep -q 'replace-with-shadcn-primitive' "$A" && grep -q 'replace-with-shadcn-block' "$A" && grep -q 'keep-custom' "$A" && grep -qi 'downgrade' "$A" && grep -qi 'direction-owner\|direction owner' "$A" && grep -q 'Behaviors the primitive must preserve' "$A" && grep -q 'ls -d frontend/src/components' "$A" && grep -q 'DossierGlyph' "$A"</automated>
  </verify>
  <acceptance_criteria>
    - Artifact file exists at the exact path above
    - Header states all three taxonomy labels, the downgrade rule, the criterion-5 default, and the narrow RTL direction-owner reading with an explicit note that it resolves RESEARCH Open Question 2
    - The 6-column row schema (Surface | Classification | Domain signals | Behaviors the primitive must preserve | Evidence | Notes) appears as a stated format rule
    - Evidence baseline section contains the verbatim commands from RESEARCH Code Examples AUDIT-01 block AND their raw output, dated with the execution date
    - Clearance (10-file) and flags/glyphs (13-file) lists appear verbatim; dossier-type list appears as command output
    - grep finds zero occurrences of live data values (the artifact mentions only paths/commands/counts)
  </acceptance_criteria>
  <done>Artifact skeleton committed with rulebook header, dated evidence baseline, domain-signal lists, and empty tier placeholders.</done>
</task>

<task type="auto">
  <name>Task 2: Classify every top-level component directory outside ui/ and forms/ (one row each)</name>
  <files>.planning/phases/75-ui-component-migration-audit/75-AUDIT-classification.md</files>
  <read_first>
    - .planning/phases/75-ui-component-migration-audit/75-AUDIT-classification.md (the rulebook and lists written in Task 1)
    - .planning/codebase/STRUCTURE.md (directory purposes)
    - .planning/phases/75-ui-component-migration-audit/75-RESEARCH.md (section "Verified Ground Truth" — largest-directory counts and domain-signal sets)
  </read_first>
  <action>
    Fill the "Per-directory classification" section with exactly one row per top-level directory under `frontend/src/components/` EXCEPT `ui/` and `forms/` (those get per-file rows in plan 75-04). Enumerate directories with `ls -d frontend/src/components/*/` — 116 total as of planning, so 114 rows expected; use the live count.

    Rules per row:
    - Surface cell format: `components/<dirname>/` (trailing slash — the verification grep keys on it). Note two non-lowercase entries exist: `FirstRun/` and `__tests__/`.
    - `__tests__/` classifies keep-custom with Notes "test-only directory — out of migration scope".
    - Directories containing any file from the clearance list (calendar, copilot, intelligence, dossier, signals, entity-links), the flags/glyphs list (signature-visuals, list-page, calendar, intelligence, dossier), or ≥1 dossier-type-logic file MUST classify keep-custom or replace-with-shadcn-block + domain-wrapper; cite the triggering list in the Domain signals cell (e.g. "clearance: SignalsQueue.tsx, EscalateSignalDialog.tsx, CaptureSignalForm.tsx").
    - Feature/domain directories with no plausible generic replacement (the expected majority) classify keep-custom with a one-line evidence note (e.g. "domain workflow UI; no shadcn analog").
    - Where a directory plausibly maps to a shadcn BLOCK (e.g. dashboard-like or shell-like compositions such as `dashboard-widgets/`, `layout/`, `modern-nav/`, `empty-states/`), you may classify replace-with-shadcn-block ONLY with the domain-wrapper qualifier when any domain signal hits, and the Behaviors cell must name what the block must preserve (e.g. AppShell's HeroUI Drawer usage, direction-aware nav in modern-nav).
    - replace-with-shadcn-primitive is expected to be RARE at directory granularity; any such row must have a non-empty Behaviors cell or be downgraded per the header rule.
    - A directory may be split with per-file exception rows beneath its directory row if genuinely mixed (keep exceptions minimal; the directory row still exists and states "except rows below" in Notes).
    - Every row's Evidence cell cites a concrete check: a grep hit from the domain-signal lists, a named representative file, or "dir listing + STRUCTURE.md".

    After the table, add a one-paragraph "Tier-1 summary for Phase 76/77" noting: the direction-owner directories (from the 94-file dir-owner set) that Phase 76's `migrate rtl` and portal work will touch, and that TOKEN-06 scope detail for `ui/` lands in the per-file tier (plan 75-04).

  </action>
  <verify>
    <automated>A=.planning/phases/75-ui-component-migration-audit/75-AUDIT-classification.md; test "$(grep -cE '^\| components/[A-Za-z0-9_-]+/ ' "$A")" -eq "$(( $(ls -d frontend/src/components/*/ | wc -l | tr -d ' ') - 2 ))" && awk -F'|' '$2 ~ /components\/(calendar|copilot|intelligence|dossier|signals|entity-links|signature-visuals|list-page)\/ / && $3 !~ /keep-custom|domain-wrapper|shadcn-block/ {bad++} END {exit bad>0}' "$A"</automated>
  </verify>
  <acceptance_criteria>
    - Row count of lines matching `^| components/<name>/ ` equals (live top-level directory count − 2) — i.e. every directory except ui/ and forms/ has exactly one row
    - Zero directory rows for calendar/, copilot/, intelligence/, dossier/, signals/, entity-links/, signature-visuals/, list-page/ carry a bare replace-with-shadcn-primitive classification (awk gate exits 0)
    - Every replace-with-shadcn-primitive row (if any) has a non-empty Behaviors cell
    - Every row has a non-empty Evidence cell
    - "Tier-1 summary for Phase 76/77" paragraph exists after the table
  </acceptance_criteria>
  <done>Per-directory tier complete: 114 rows (live count − 2), domain-signal directories all keep-custom/block-with-wrapper, summary paragraph addressed to Phases 76/77.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                                   |
| --------------------------------- | --------------------------------------------------------------------------------------------- |
| audit doc → git history           | committed planning docs are permanent; anything pasted into them is disclosed to repo readers |
| audit doc → Phases 76/77 planners | downstream phases act on classifications without re-deriving them                             |

## STRIDE Threat Register

| Threat ID | Category                            | Component                                              | Disposition  | Mitigation Plan                                                                                                                                                           |
| --------- | ----------------------------------- | ------------------------------------------------------ | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-75-01   | Information Disclosure              | 75-AUDIT-classification.md                             | mitigate     | artifact carries only file paths, component names, commands, and counts — no runtime data, no gated-RPC payload samples, no secrets (Task 1 action + acceptance criteria) |
| T-75-02   | Elevation of Privilege (downstream) | classification rows for clearance-touching directories | mitigate     | pre-computed clearance/flags/dossier-type lists force keep-custom or block-with-wrapper; awk gate in Task 2 verify blocks primitive-replace on those directories          |
| T-75-SC   | Tampering                           | package installs                                       | accept (n/a) | this phase installs nothing — docs-only; no npm/pip/cargo activity                                                                                                        |

</threat_model>

<verification>
- Artifact exists with rulebook header (taxonomy, downgrade rule, criterion-5 defaults, narrow RTL reading stated as Open Question 2 resolution)
- Per-directory row count reconciles with the live `ls -d` count minus 2
- Domain-signal directory gate passes (no primitive-replace on clearance/flags/dossier surfaces)
- All evidence outputs dated
</verification>

<success_criteria>

- ROADMAP success criterion 1 (partially — directory tier): every non-ui/forms surface labeled with one of the three fixed labels, with evidence
- ROADMAP success criterion 5: domain-signal surfaces default keep-custom/block-with-wrapper — enforced by an automated gate
- RESEARCH Open Questions 2 and 3 resolved in writing inside the artifact
  </success_criteria>

<output>
Create `.planning/phases/75-ui-component-migration-audit/75-01-SUMMARY.md` when done.
</output>
