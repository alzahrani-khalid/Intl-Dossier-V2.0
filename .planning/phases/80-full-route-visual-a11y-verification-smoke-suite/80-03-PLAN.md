---
phase: 80-full-route-visual-a11y-verification-smoke-suite
plan: 03
type: execute
wave: 3
depends_on: ['80-02']
files_modified:
  - frontend/tests/a11y/dossiers-rtl-a11y.spec.ts
  - frontend/tests/a11y/intake-accessibility.spec.ts
  - frontend/tests/a11y/positions-a11y-en.spec.ts
  - frontend/tests/a11y/positions-a11y-ar.spec.ts
  - frontend/tests/e2e/qa-sweep-axe-4axis.spec.ts
  - frontend/package.json
  - frontend/src/** (conditional — only files named by fix verdicts in 80-A11Y-BASELINE.md)
  - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md
autonomous: true
requirements: [VERIFY-02]
must_haves:
  truths:
    - "Per CONTEXT decision 'Axe-core a11y sweep (VERIFY-02)': every in-gate hard failure has an explicit fix-vs-record decision logged in 80-A11Y-BASELINE.md — a test.fixme without a root-cause rationale, or bulk-fixme-ing to go green, is laundering and forbidden"
    - "Records use ONLY the existing in-repo convention: test.fixme(true, '80: recorded pre-migration baseline — <root cause>') plus a TRACKED APP A11Y DEBT comment (10 existing instances mirror this) — no new allowlist/baseline file machinery is invented"
    - 'Every NEW-on-HEAD (migration-caused) item from the 80-02 must-fix list is FIXED, never recorded'
    - 'pnpm -C frontend exec playwright test --project=a11y exits 0 on the reference env, and the 4-axis sweep exits 0 (with pre-existing violations recorded via the same convention)'
    - 'The 8 quarantined specs stay quarantined — their playwright.config.ts comment IS the recorded disposition; re-repair is explicitly out of budget'
  artifacts:
    - path: '.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md'
      provides: 'decision column (fixed | recorded) with rationale for every hard-failure row'
    - path: 'frontend/package.json'
      provides: 'test:a11y script repointed to the real gate'
      contains: 'playwright test --project=a11y'
  key_links:
    - from: 'frontend/tests/a11y/*.spec.ts record edits'
      to: '80-A11Y-BASELINE.md decision rows'
      via: "matching '80: recorded pre-migration baseline' reason strings"
      pattern: '80: recorded pre-migration baseline'
    - from: 'frontend/src fixes (if any)'
      to: 'the failing a11y assertions'
      via: 're-run of the exact failing spec proving green'
      pattern: '--project=a11y'
---

<objective>
Close VERIFY-02: for every characterized hard failure, either fix it (app or test, on local evidence) or record it as pre-migration baseline using the established `test.fixme` + `TRACKED APP A11Y DEBT` convention — then prove the a11y gate and the 4-axis sweep green on the reference environment. This is the last plan that may touch `frontend/src` before the visual replay (Plan 80-04), so the human triages exactly the pixels that get recaptured.

Purpose: the a11y CI job goes green honestly — decisions logged per failure, migration-caused items fixed, nothing laundered.
Output: edited a11y specs (+ conditional surgical src fixes), repointed dead `test:a11y` script, finalized decision ledger, green `--project=a11y` + 4-axis runs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-CONTEXT.md
@.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-RESEARCH.md
@.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-PATTERNS.md
@.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md
@frontend/tests/a11y/intake-accessibility.spec.ts
@frontend/tests/a11y/dossiers-rtl-a11y.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-failure fix-vs-record decisions and edits</name>
  <files>frontend/tests/a11y/dossiers-rtl-a11y.spec.ts, frontend/tests/a11y/intake-accessibility.spec.ts, frontend/tests/a11y/positions-a11y-en.spec.ts, frontend/tests/a11y/positions-a11y-ar.spec.ts, frontend/tests/e2e/qa-sweep-axe-4axis.spec.ts (conditional), frontend/src/** (conditional), .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md</files>
  <read_first>
    - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md — the classified failure table + must-fix list (the work order for this task)
    - frontend/tests/a11y/intake-accessibility.spec.ts — lines ~33-38 and ~109-112, the verbatim record convention (fixme + TRACKED APP A11Y DEBT comment)
    - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-PATTERNS.md — §"frontend/tests/a11y/*.spec.ts fix-vs-record edits" (assertion shapes under adjudication: main landmark, unlabeled-count < 5, h1Count >= 1, first-Tab-interactive) and §"frontend/src/** per-failure a11y fixes"
    - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-RESEARCH.md — §VERIFY-02 step 3 classification hints + Landmines
    - For each src file a fix touches: that component's source, to mirror its existing aria/i18n patterns (e.g. Sidebar.tsx aria-label={t('shell.menu.open')} — i18n-keyed, never hardcoded English)
  </read_first>
  <action>
    Work the ledger row-by-row; fix-vs-record is Claude's discretion (CONTEXT) EXCEPT: every NEW-on-HEAD item MUST be fixed. For FIX verdicts — app bugs: typical class is icon-button/link `aria-label` gaps (genuine, small); use i18n-keyed labels via the component's existing namespace (unregistered namespaces silently fall back to EN in BOTH languages — register in `src/i18n/index.ts` if adding one); all CLAUDE.md rules bind (var(--*) tokens only, logical properties `ms-*`/`ps-*`/`text-start` only — ESLint errors on physical); HeroUI v3 gotcha from Phase 79: HeroUI `Button` drops `role`/`aria-invalid`/`aria-required` via filterDOMProps — use `asChild` with a plain `button` if those attrs are needed. Test bugs (stale assertions): correct the assertion with a comment naming the evidence. Keyboard-nav failures (intake-accessibility ~:54, positions-a11y-en ~:74, positions-a11y-ar ~:75 — first Tab must land on an interactive element): decide on local DOM evidence whether it is a real focus-order fix (tabindex/skip-link) or a stale assertion. For RECORD verdicts: add `test.fixme(true, '80: recorded pre-migration baseline — <root cause>')` directly above the failing assertion's test plus a `// TRACKED APP A11Y DEBT:` comment block explaining the debt, exactly mirroring the intake-accessibility precedent; the intake heading-skip h1-to-h3 is already fixme'd — leave it recorded. 4-axis sweep failures classified pre-existing get the same fixme convention inside `qa-sweep-axe-4axis.spec.ts` (per route x axis, never a blanket skip). Update the ledger's decision column (fixed | recorded) + rationale for EVERY row as you go; flag any ambiguous UX judgment (e.g. renaming a visible heading) in the ledger rather than checkpointing. Hard boundaries: do NOT touch the 8 quarantined specs, the vitest waiting-queue-a11y T091-07 baseline, or anything in the e2e.yml deployed-app class. Keep src diffs surgical — every src commit re-triggers the full required-check gauntlet including Bundle Size Check and the pre-commit build.
  </action>
  <verify>
    <automated>grep -rn "80: recorded pre-migration baseline" frontend/tests/a11y frontend/tests/e2e/qa-sweep-axe-4axis.spec.ts | wc -l && grep -c "TRACKED APP A11Y DEBT" frontend/tests/a11y/*.spec.ts | awk -F: '{s+=$2} END {print s}'</automated>
  </verify>
  <acceptance_criteria>
    - Every hard-failure row in 80-A11Y-BASELINE.md carries decision `fixed` or `recorded` plus a rationale — zero undecided rows
    - The count of `80: recorded pre-migration baseline` fixme strings in the test tree equals the ledger's `recorded` count exactly
    - Zero `recorded` decisions on NEW-on-HEAD items (cross-check against the 80-02 must-fix list)
    - Every new record has BOTH the fixme reason string AND a TRACKED APP A11Y DEBT comment (total TRACKED comments >= 10 pre-existing + new records)
    - If src was touched: `pnpm -C frontend type-check` exits 0 and `pnpm -C frontend lint` exits 0 on the touched files
  </acceptance_criteria>
  <done>Every failure has a logged, honest disposition; migration-caused items are fixed in code.</done>
</task>

<task type="auto">
  <name>Task 2: Green proof on the reference env + dead-script hygiene</name>
  <files>frontend/package.json, .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md</files>
  <read_first>
    - frontend/package.json — line ~16, the dead `test:a11y` script (`vitest run --config vitest.a11y.config.ts` — that config file does not exist)
    - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-RESEARCH.md — §VERIFY-02 step 5 (green the a11y job) + "Broken script (hygiene)"
  </read_first>
  <action>
    Prove the gate green and fix the hygiene item. (1) With `E2E_BASE_URL` unset and :5173 serving the MAIN tree, run `pnpm -C frontend exec playwright test --project=a11y --retries=2` (CI parity — flaky retry-passes do not fail runs) and `pnpm -C frontend exec playwright test qa-sweep-axe-4axis.spec.ts --project=chromium --retries=1`; both must exit 0. If either reds, return to Task-1 discipline for the specific failure (decision + edit), never blanket-skip. (2) Repoint the dead `test:a11y` script in `frontend/package.json` to `playwright test --project=a11y` (the real gate; the vitest a11y config it referenced never existed). (3) Append a "Gate green" closing section to 80-A11Y-BASELINE.md: final run commands, exit codes, date, and the note that the CI `Accessibility Tests (RTL + WCAG AA)` job follows on the next push/PR (its redness was assertion-level; the infra pattern is proven). Do not modify the CI job itself — it already exists and needs no change.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec playwright test --project=a11y --retries=2 && pnpm exec playwright test qa-sweep-axe-4axis.spec.ts --project=chromium --retries=1 && grep -q "playwright test --project=a11y" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `--project=a11y` exits 0 (hard failures zero; fixme skips and retry-passes acceptable)
    - `qa-sweep-axe-4axis.spec.ts` run exits 0
    - `frontend/package.json` `test:a11y` invokes the Playwright a11y project; the phantom `vitest.a11y.config.ts` reference is gone
    - Ledger "Gate green" section records commands + exit codes + date
  </acceptance_criteria>
  <done>VERIFY-02 is locally green with a fully honest paper trail; the CI job greens itself on the next push.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                        |
| ------------------------------------ | ---------------------------------------------------------------------------------- |
| test suite -> app DOM (staging data) | assertions adjudicate real rendered output; edits here define what "passing" means |

## STRIDE Threat Register

| Threat ID | Category    | Component                               | Disposition | Mitigation Plan                                                                                                                 |
| --------- | ----------- | --------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------- |
| T-80-07   | Tampering   | false-green via bulk fixme              | mitigate    | one decision row per failure with root cause; fixme-count must equal ledger recorded-count; NEW-on-HEAD items are fix-mandatory |
| T-80-08   | Repudiation | untraceable record decisions            | mitigate    | reason strings carry the `80:` phase marker; ledger row + TRACKED APP A11Y DEBT comment pair per record                         |
| T-80-09   | Tampering   | src "fixes" regressing visuals silently | mitigate    | this plan runs BEFORE the visual replay/triage (Plan 80-04), so any pixel effect of a11y fixes is human-triaged                 |
| T-80-SC   | Tampering   | npm/pip/cargo installs                  | accept      | zero package installs this phase                                                                                                |

</threat_model>

<verification>
- `--project=a11y` and the 4-axis sweep exit 0 on the reference env
- Ledger decision column complete; record-count parity between specs and ledger
- Quarantine list and waiting-queue-a11y T091-07 untouched (`git diff --name-only` contains none of the 8 quarantined spec files)
</verification>

<success_criteria>

- VERIFY-02 satisfied: no new violations vs the recorded baseline, evidenced B ⊆ A, gate green without laundering
- The a11y CI job's next run has no hard failures attributable to this tree
  </success_criteria>

<output>
Create `.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-03-SUMMARY.md` when done.
</output>
