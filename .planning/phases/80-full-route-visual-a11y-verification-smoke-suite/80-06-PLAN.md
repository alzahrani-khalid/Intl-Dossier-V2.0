---
phase: 80-full-route-visual-a11y-verification-smoke-suite
plan: 06
type: execute
wave: 6
depends_on: ['80-01', '80-05']
files_modified:
  - frontend/tests/e2e/rtl-component-smokes.spec.ts
  - frontend/tests/e2e/calendar-rtl.spec.ts
  - .github/workflows/ci.yml
autonomous: false
requirements: [FOUC-02]
must_haves:
  truths:
    - "Per CONTEXT decision 'CI smoke gating (FOUC-02)': the new RTL smokes ship as a SEPARATE, green-from-birth ci.yml job (the separate-job path is chosen and called out explicitly) — never coupled to the pre-existing red visual/a11y jobs and never the e2e.yml deployed-app pattern (issue-#31 red class)"
    - 'The new smokes use DOM/computed-style assertions ONLY (inset-inline-end / getBoundingClientRect / computed transform) — zero toHaveScreenshot, so the job runs identically on darwin (local) and ubuntu (CI) with no snapshot-suffix trap'
    - 'calendar-rtl is evergreen: page.clock.install pins the displayed month to July 2026 where the SRTL-02 seed rows live — the August-2026 cliff is defused'
    - "'Gate the build' is real only when the job's context joins branch protection — that promotion is a repo-admin HUMAN checkpoint, and a declined/deferred decision is recorded honestly (job exists + advisory), never silently claimed as gating"
  artifacts:
    - path: 'frontend/tests/e2e/rtl-component-smokes.spec.ts'
      provides: 'the 3 FOUC-02 coverage gaps: Popover RTL, Pagination RTL (/users), Sidebar RTL'
      min_lines: 60
    - path: '.github/workflows/ci.yml'
      provides: "test-rtl-smokes job (name: RTL Portal + Component Smokes) mirroring test-a11y's local-dev-server pattern"
      contains: 'test-rtl-smokes'
    - path: 'frontend/tests/e2e/calendar-rtl.spec.ts'
      provides: 'clock-frozen, CI-proof AR calendar smoke'
      contains: 'clock.install'
  key_links:
    - from: '.github/workflows/ci.yml test-rtl-smokes'
      to: 'direction-portals.spec.ts + calendar-rtl.spec.ts + rtl-component-smokes.spec.ts'
      via: "the job's single playwright run step (--project=chromium)"
      pattern: 'rtl-component-smokes'
    - from: 'test-rtl-smokes env'
      to: 'GitHub secrets E2E_ADMIN_EMAIL / E2E_ADMIN_PASSWORD'
      via: 'TEST_USER_* env mapping (pagination /users route is admin-gated)'
      pattern: 'E2E_ADMIN_EMAIL'
    - from: 'branch protection required contexts'
      to: 'RTL Portal + Component Smokes'
      via: 'human checkpoint (gh api or Settings UI), verified by gh api GET'
      pattern: 'required_status_checks'
---

<objective>
Deliver FOUC-02: fill the three smoke gaps (Popover / Pagination / Sidebar RTL), CI-proof the calendar smoke against the August seed cliff, wire all portal + component RTL smokes into a NEW green-from-birth ci.yml job on the proven local-dev-server pattern, prove it green locally, and run the human checkpoint that makes it an actual required check. Dropdown/Sheet-drawer/Tooltip coverage already exists in direction-portals.spec.ts (Phase 76, proven green) — it is wired in, not rewritten.

Purpose: the RTL/portal/FOUC guarantees from Phases 76-77 become un-regressable — a red smoke blocks the build.
Output: 1 new spec, 1 CI-proofed spec, 1 new ci.yml job, local green proof, and the recorded gating decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-CONTEXT.md
@.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-RESEARCH.md
@.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-PATTERNS.md
@frontend/tests/e2e/direction-portals.spec.ts
@frontend/tests/e2e/calendar-rtl.spec.ts
@frontend/src/components/ui/popover.tsx
@frontend/src/components/ui/pagination.tsx
@frontend/src/components/layout/Sidebar.tsx
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build rtl-component-smokes.spec.ts + clock-freeze calendar-rtl.spec.ts</name>
  <files>frontend/tests/e2e/rtl-component-smokes.spec.ts, frontend/tests/e2e/calendar-rtl.spec.ts</files>
  <read_first>
    - frontend/tests/e2e/direction-portals.spec.ts — the exact analog: imports (loginForListPages, dossier-drawer-fixture), describe retries 1, 1280x800 viewport beforeEach, the computed-direction assertion (~lines 125-131) and the inset-inline-end edge assertion (~lines 134-149)
    - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-PATTERNS.md — §rtl-component-smokes target-selector table (verified selectors + assertions for all 3 tests) and §calendar-rtl clock-freeze pattern
    - frontend/src/components/ui/popover.tsx — data-slot="popover-trigger" (~:11) / data-slot="popover-content" (~:31)
    - frontend/src/components/ui/pagination.tsx — a[aria-label="Go to previous page"] (~:54) / "Go to next page" (~:66), chevron svg with rtl:rotate-180
    - frontend/src/components/layout/Sidebar.tsx — aside role="navigation" className="sidebar sb ..." (~:74-81)
    - frontend/tests/e2e/calendar-rtl.spec.ts — the existing 32 lines (assertions stay byte-similar) + dashboard-widgets-visual.spec.ts ~:59 for the page.clock.install call shape
    - .planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/76-SRTL02-VERIFICATION.md — §1 (SRTL-02 seed rows: title_en LIKE 'SRTL-02 regression seed %', July 2026, organizer-visibility RLS note) and §2 (pagination sole live consumer = /users, admin-gated; live sidebar = layout/Sidebar.tsx, NOT ui/sidebar.tsx)
  </read_first>
  <action>
    Create `frontend/tests/e2e/rtl-component-smokes.spec.ts` mirroring direction-portals structure (describe retries 1; beforeEach sets 1280x800; loginForListPages(page, 'ar'); await document.fonts.ready before every geometry read; deterministic waitFor visible before interaction — never waitForTimeout). Three tests, DOM/computed-style assertions ONLY (hard rule: zero toHaveScreenshot — committed baselines are darwin-named and this spec must run on ubuntu CI): (1) Popover RTL — navigate to a verified `ui/popover.tsx` consumer surface (candidates, pick the first that mounts cleanly under the test account: the waiting-queue FilterPanel, the audit-logs AuditLogFilters, or a task/commitment-form date-picker popover; locate the mounting route by grepping the chosen component's route file); click `[data-slot="popover-trigger"]`, await `[data-slot="popover-content"]` visible, assert its computed direction === 'rtl' and that its bounding box is positioned sanely relative to the trigger's box (overlap or adjacency on the inline axis, not off-viewport). (2) Pagination RTL — goto `/users` (admin-gated; local .env.test user IS admin, CI uses E2E_ADMIN_*); if pagination controls render (needs >1 page of users — if staging has too few rows, assert the chevron-icon transform on the rendered pagination nav regardless of page count, and record which branch ran), assert the prev/next anchors by aria-label and that the chevron svg computed transform equals the 180-degree matrix 'matrix(-1, 0, 0, -1, 0, 0)' under AR (the rtl:rotate-180 utility). (3) Sidebar RTL — on any authed route in AR, locate `aside.sidebar` (the live layout/Sidebar.tsx — NOT the unmounted ui/sidebar.tsx), boundingBox, assert Math.round(box.x + box.width) === page.viewportSize().width (rail at inline-start = physical RIGHT edge). Then CI-proof `calendar-rtl.spec.ts`: add `await page.clock.install({ time: new Date('2026-07-15T12:00:00Z') })` BEFORE page.goto('/calendar') so the displayed month is pinned to July 2026 where the 3 SRTL-02 seed rows live (defuses the 2026-08-01 red cliff); keep every existing assertion byte-similar (.cal-dow count 7, Arabic dow labels, Arabic-Indic digits, zero Western digits); add a header comment documenting the clock<->seed coupling (rows: calendar_entries title_en LIKE 'SRTL-02 regression seed %', July 2026, visible via organizer RLS) and that re-seeding goes through the orchestrator (Supabase MCP).
  </action>
  <verify>
    <automated>cd frontend && grep -c "toHaveScreenshot" tests/e2e/rtl-component-smokes.spec.ts | grep -qx 0 && grep -q "clock.install" tests/e2e/calendar-rtl.spec.ts && pnpm exec playwright test rtl-component-smokes.spec.ts calendar-rtl.spec.ts --project=chromium --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `playwright test rtl-component-smokes.spec.ts calendar-rtl.spec.ts --project=chromium` exits 0 locally
    - `grep -c "toHaveScreenshot" rtl-component-smokes.spec.ts` = 0 (screenshot-free, platform-independent)
    - The Popover test asserts computed direction 'rtl' on `[data-slot="popover-content"]`; the Pagination test asserts the chevron transform matrix on `/users`; the Sidebar test asserts the right-edge geometry on `aside.sidebar`
    - calendar-rtl installs the frozen clock BEFORE navigation and its original assertions are unchanged in substance
    - No frontend/src file modified (`git status --porcelain -- frontend/src` empty)
  </acceptance_criteria>
  <done>All FOUC-02 smoke coverage exists and passes locally; the calendar smoke no longer expires in August.</done>
</task>

<task type="auto">
  <name>Task 2: Add the test-rtl-smokes ci.yml job (verbatim test-a11y copy + 3 deltas) + full local green proof</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml — lines 334-372, the `test-a11y` job (the VERBATIM copy source: checkout, pnpm/action-setup, setup-node with NODE_VERSION cache pnpm, pnpm install --frozen-lockfile, playwright install --with-deps chromium, run step in ./frontend, always() artifact upload) + the workflow-level env NODE_VERSION/PNPM_VERSION (~:10-16, do not redefine)
    - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-PATTERNS.md — §ci.yml deltas table (name, creds, run command, artifact name)
    - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-RESEARCH.md — §FOUC-02 step 3 rationale (why the separate green-from-birth job; why NEVER e2e.yml) + Landmines (analyst creds 403 on /users; no screenshots on ubuntu)
  </read_first>
  <action>
    Insert a new job `test-rtl-smokes` into `.github/workflows/ci.yml` directly after `test-a11y`, copied VERBATIM from the test-a11y job with EXACTLY these deltas and nothing else: (a) job id `test-rtl-smokes`, display name `RTL Portal + Component Smokes` (this exact string becomes the future required status context — spelling is load-bearing); (b) creds: `TEST_USER_EMAIL: ${{ secrets.E2E_ADMIN_EMAIL }}` and `TEST_USER_PASSWORD: ${{ secrets.E2E_ADMIN_PASSWORD }}` (admin — the pagination smoke's /users route 403s under analyst; both secrets already exist, consumed by e2e.yml), keeping `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` from the same E2E_* secrets as test-a11y; (c) run step: `pnpm exec playwright test direction-portals.spec.ts calendar-rtl.spec.ts rtl-component-smokes.spec.ts --project=chromium --reporter=html` in working-directory ./frontend; plus the mechanical artifact rename to `rtl-smokes-report`. Keep `needs: [lint, type-check]` and the local-dev-server pattern (E2E_BASE_URL never set — Playwright auto-starts pnpm dev per playwright.config.ts webServer). Explicitly FORBIDDEN: touching e2e.yml, referencing E2E_BASE_URL, adding the 4-axis sweep to this job (runtime budget), modifying test-a11y or any other existing job, or making this job depend on the red visual/a11y jobs. Add a one-line comment above the job citing the CONTEXT decision: separate green-from-birth job chosen over repairing the issue-#31 red class (deployed-app repair = infra work outside this phase). Then the full local green proof: run the exact CI trio command locally (`pnpm -C frontend exec playwright test direction-portals.spec.ts calendar-rtl.spec.ts rtl-component-smokes.spec.ts --project=chromium`) — exit 0 = the phase-exit proof; the GitHub PR run at v8.0 push is the birth certificate (Task 3). Sanity-check the YAML edit by diff-reviewing indentation against the test-a11y block (grep-anchored structural asserts below).
  </action>
  <verify>
    <automated>grep -q "test-rtl-smokes:" .github/workflows/ci.yml && grep -q "RTL Portal + Component Smokes" .github/workflows/ci.yml && grep -q "E2E_ADMIN_EMAIL" .github/workflows/ci.yml && grep -c "E2E_BASE_URL" .github/workflows/ci.yml | grep -qx 0 && cd frontend && pnpm exec playwright test direction-portals.spec.ts calendar-rtl.spec.ts rtl-component-smokes.spec.ts --project=chromium --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml contains job `test-rtl-smokes` named exactly `RTL Portal + Component Smokes`, with needs [lint, type-check], E2E_ADMIN_* creds, the 3-spec chromium run step, and an `rtl-smokes-report` artifact upload
    - `grep -c "E2E_BASE_URL" .github/workflows/ci.yml` = 0 (the deployed-app pattern never enters ci.yml)
    - e2e.yml and all pre-existing ci.yml jobs are byte-untouched (`git diff --name-only` lists only ci.yml; `git diff .github/workflows/ci.yml` shows only the inserted block)
    - The local trio run exits 0 (direction-portals 5 + calendar-rtl 1 + rtl-component-smokes 3 = 9 tests)
  </acceptance_criteria>
  <done>The green-from-birth smoke job exists on the proven infra pattern with local green proof in hand.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Make it gate — branch-protection required-context addition (repo admin)</name>
  <files>none (GitHub branch-protection settings)</files>
  <read_first>
    - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-RESEARCH.md — §FOUC-02 step 4 (gating mechanics, the 8 current required contexts verified via gh api, honesty requirement) + Open Question 1 + Assumption A1 (POST endpoint shape is [ASSUMED] — verify with gh api --help before use; the Settings UI is the fallback and the human is doing this step anyway)
  </read_first>
  <action>PAUSE for the repo admin: after the job is observed green on a real GitHub run (v8.0 PR — the birth certificate), add the context 'RTL Portal + Component Smokes' to main's required status checks (Settings UI, or gh api after verifying the endpoint shape per assumption A1); verify via the gh api GET; if deferred or declined, record that outcome verbatim in the SUMMARY instead — never claim gating that GitHub does not enforce.</action>
  <what-built>The `RTL Portal + Component Smokes` job in ci.yml with local green proof (9/9). A workflow job failing is ADVISORY until its context is added to branch protection — this checkpoint is what makes FOUC-02's "gate the build" literally true.</what-built>
  <how-to-verify>
    1. Sequencing reality: the job can only be observed green on GitHub once v8.0 pushes (main is local-ahead; the v8.0 PR run is the birth certificate). If that PR is not yet open, you may DEFER this step to the PR moment — say so and it is recorded.
    2. At/after the v8.0 PR: confirm the job is green on the PR run (Checks tab, or `gh run view <run-id> --json jobs`). Known contingency: if calendar-rtl reds in CI with an empty-grid signature ("التقويم فارغ"), the cause is SRTL-02 seed-row RLS visibility for the E2E_ADMIN account (rows are organizer-scoped) — the orchestrator re-seeds with the CI account as organizer via MCP, then re-run
    3. Add the required context: GitHub Settings -> Branches -> main -> required status checks -> add `RTL Portal + Component Smokes`; or via API after verifying the endpoint shape with `gh api --help` (the POST contexts endpoint is assumption A1)
    4. Verify: `gh api repos/:owner/:repo/branches/main/protection --jq '.required_status_checks.contexts'` lists `RTL Portal + Component Smokes` alongside the existing 8
  </how-to-verify>
  <acceptance_criteria>
    - EITHER the gh api GET output lists the new required context (gating is real)
    - OR the decision is recorded honestly in the SUMMARY: "declined — FOUC-02 delivered as job-exists + advisory" or "deferred to the v8.0 PR — local 9/9 green is the phase-exit proof; gating completes at PR time" — with the reason; never silently claimed
  </acceptance_criteria>
  <verify>
    <automated>gh api repos/:owner/:repo/branches/main/protection --jq '.required_status_checks.contexts' | grep -c "RTL Portal + Component Smokes" || echo "not-yet-gated: decision must be recorded in SUMMARY"</automated>
  </verify>
  <resume-signal>Reply "gated" (context added + verified), "deferred to v8.0 PR", or "declined: <reason>"</resume-signal>
  <done>The gating decision is made and recorded — FOUC-02's build-gate claim matches GitHub reality.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                      |
| ----------------------------------------- | ---------------------------------------------------------------- |
| GitHub Actions runner -> staging Supabase | E2E*ADMIN*_ + E2E*SUPABASE*_ secrets flow into the new job's env |
| repo admin -> branch protection           | privileged settings change controlling what blocks merges        |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                        | Disposition | Mitigation Plan                                                                                                                                                            |
| --------- | ---------------------- | ---------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-80-16   | Information disclosure | CI secrets in the new job                                        | mitigate    | reuse EXISTING repo secrets via ${{ secrets.* }} only (none created); no env-dumping steps; artifact = playwright HTML report with the same retention posture as test-a11y |
| T-80-17   | Elevation of privilege | admin creds scope in CI                                          | accept      | admin creds are required by the /users pagination smoke and are already consumed by e2e.yml; scope limited to this one job (test-a11y keeps analyst)                       |
| T-80-18   | Tampering              | gating theater (job exists but blocks nothing)                   | mitigate    | dedicated human checkpoint with gh api GET verification; declined/deferred outcomes recorded verbatim (V14 configuration control)                                          |
| T-80-19   | Denial of service      | time-bombed smoke (August seed cliff) rotting the required check | mitigate    | page.clock.install pins July 2026; clock<->seed coupling documented in the spec header; CI RLS-visibility contingency documented in the checkpoint                         |
| T-80-SC   | Tampering              | npm/pip/cargo installs                                           | accept      | zero package installs this phase                                                                                                                                           |

</threat_model>

<verification>
- Local trio run green (9 tests); new spec screenshot-free; calendar clock-frozen
- ci.yml gains exactly one job; e2e.yml untouched; no E2E_BASE_URL anywhere in ci.yml
- Gating decision recorded (added / deferred / declined) with gh api evidence where applicable
</verification>

<success_criteria>

- FOUC-02 satisfied: portal + Calendar/Pagination/Sidebar RTL smokes run in CI on a green-from-birth job, with the build-gating promotion executed or its status recorded honestly
- The separate-job choice and the pre-existing-red exclusion are called out in writing (CONTEXT discretion exercised explicitly)
  </success_criteria>

<output>
Create `.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-06-SUMMARY.md` when done.
</output>
