---
phase: 80-full-route-visual-a11y-verification-smoke-suite
plan: 04
type: execute
wave: 4
depends_on: ['80-03']
files_modified:
  - frontend/tests/e2e/dashboard-widgets-visual.spec.ts
  - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-VISUAL-RECOMPARE.md
autonomous: false
requirements: [VERIFY-01]
must_haves:
  truths:
    - "Per CONTEXT decision 'Human visual-diff review is REQUIRED (autonomous:false checkpoint)': every one of the 43 baselined surfaces is adjudicated by the human as intended-Linear vs regression (vs documented dynamic-content) — visual diffs are NEVER auto-approved"
    - "Per CONTEXT decision 'Visual re-compare (VERIFY-01)': the replay runs against the untouched 77-01 pre-token baseline (commit 14191cb85) with --retries=0 and WITHOUT --update-snapshots — the diff evidence is preserved for the human, not destroyed"
    - 'Coverage is exactly the 10 baselined specs / 43 tests; the exclusions (calendar-visual + dashboard-visual non-baselined stubs, stale tests/visual/theme-visual, qa-sweep-focus-outline already-recaptured-in-77-07) are restated in the ledger — never silently expanded'
    - 'The staging b0000002-* seed re-refresh is applied by the ORCHESTRATOR via Supabase MCP (executors have no MCP), and FROZEN_TIME is realigned to the same seed date before replay'
  artifacts:
    - path: '.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-VISUAL-RECOMPARE.md'
      provides: '43-row per-shot diff inventory + human verdict ledger + coverage statement, mirroring 77-BASELINE-VALIDATION.md structure'
    - path: 'frontend/tests/e2e/dashboard-widgets-visual.spec.ts'
      provides: 'FROZEN_TIME realigned to the seed date (46-era seed/clock coupling honored)'
      contains: 'FROZEN_TIME'
  key_links:
    - from: 'replay run'
      to: 'frontend/test-results/ diff triads + HTML report'
      via: '--retries=0 --reporter=html with NO --update-snapshots'
      pattern: 'retries=0'
    - from: '80-VISUAL-RECOMPARE.md verdicts'
      to: 'Plan 80-05 fix + recapture tasks'
      via: 'regression verdict list'
      pattern: 'regression'
---

<objective>
Execute the VERIFY-01 evidence phase in the mandated anti-laundering order: orchestrator-applied staging seed refresh -> FROZEN_TIME realignment -> full replay of the 43 pre-token baselines (diffs preserved, nothing updated) -> the LOCKED human diff-triage checkpoint producing a per-shot verdict ledger. Runs after Plan 80-03 so all src-mutating a11y fixes have landed — the human triages the exact final pixels.

Purpose: VERIFY-01's control is the order replay -> human review -> (fix) -> update. This plan is the replay + review half; Plan 80-05 may only recapture after the verdicts here are approved.
Output: `80-VISUAL-RECOMPARE.md` with 43 human verdicts; realigned FROZEN_TIME; untouched baseline PNGs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-CONTEXT.md
@.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-RESEARCH.md
@.planning/phases/77-linear-token-system/77-BASELINE-VALIDATION.md
@frontend/tests/e2e/dashboard-widgets-visual.spec.ts
@frontend/playwright.config.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Orchestrator applies the staging seed re-refresh (Supabase MCP)</name>
  <files>none (staging data only)</files>
  <read_first>
    - .planning/phases/77-linear-token-system/77-BASELINE-VALIDATION.md — §2 "Staging seed refresh": the verbatim, idempotent, dates-only SQL for the 3 deterministic b0000002-* engagement_dossiers rows (today +2h / tomorrow 10:00 / +2d..+4d VIP), project zkrcjzdemdmwhearhfgg
    - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-RESEARCH.md — §VERIFY-01 step 1 + Pitfall 2 (seed/clock fragility: without both halves, WeekAhead/VipVisits render empty and the capture crashes)
  </read_first>
  <action>PAUSE for the orchestrator: apply the 77-BASELINE-VALIDATION.md §2 seed SQL verbatim via Supabase MCP against project zkrcjzdemdmwhearhfgg (3 dates-only UPDATEs on the b0000002-* engagement_dossiers rows), verify with a SELECT, and report the seed date. The executor does NOT attempt any staging write.</action>
  <what-built>Nothing yet — this is the orchestrator-only pre-flight. Executors have no Supabase MCP access (established project fact); the seed refresh is a data-only UPDATE on staging.</what-built>
  <how-to-verify>
    1. Orchestrator runs the 77-BASELINE-VALIDATION.md §2 UPDATE statements verbatim via Supabase MCP against project zkrcjzdemdmwhearhfgg (3 rows: b0000002-0000-0000-0000-000000000001/-002/-003, NOW()-anchored intervals)
    2. Orchestrator verifies with a SELECT: all 3 rows have start_date between today and today+2 days
    3. Orchestrator states the seed date (today's date) in the reply — the executor writes it into the ledger header and Task 2 uses it for FROZEN_TIME
  </how-to-verify>
  <acceptance_criteria>
    - MCP SELECT output shows the 3 b0000002-* rows with today-relative start/end dates
    - The seed date is stated explicitly in the resume message
  </acceptance_criteria>
  <verify>
    <automated>MISSING — data-only staging change; evidence is the MCP SELECT output quoted in 80-VISUAL-RECOMPARE.md header (written in Task 2)</automated>
  </verify>
  <resume-signal>Reply "seeded YYYY-MM-DD" (the date the SQL ran)</resume-signal>
  <done>Staging widget fixtures are today-anchored; the seed date is known.</done>
</task>

<task type="auto">
  <name>Task 2: FROZEN_TIME realign + full replay (NO update) + draft diff inventory</name>
  <files>frontend/tests/e2e/dashboard-widgets-visual.spec.ts, .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-VISUAL-RECOMPARE.md</files>
  <read_first>
    - frontend/tests/e2e/dashboard-widgets-visual.spec.ts — line ~11, the FROZEN_TIME constant (currently 2026-07-02T12:00:00Z) + its 77-01 coupling comment; do NOT touch the bare-name pathTemplate (77-01 §6 pinned it deliberately)
    - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-RESEARCH.md — §VERIFY-01 steps 1-2 (the exact 10-spec replay command) + Landmines (E2E_BASE_URL leak; darwin-only; date-text diffs are the documented dynamic class)
    - .planning/phases/77-linear-token-system/77-BASELINE-VALIDATION.md — the ledger structure to mirror (§§1-7 headers) and §5's coverage-statement discipline
  </read_first>
  <action>
    (1) Edit the FROZEN_TIME constant in dashboard-widgets-visual.spec.ts to the Task-1 seed date at 12:00:00Z, preserving the coupling comment (clock and seed MUST share a date — the 46-era fragility). (2) Preconditions: confirm `E2E_BASE_URL` is unset in the shell (if it leaks, tests hit a deployed app instead of auto-starting dev) and :5173 serves the MAIN tree; this machine is darwin (all default-path PNGs are -chromium-darwin-named). (3) Replay: run the research §VERIFY-01 step-2 command verbatim — all 10 specs (list-pages-visual, dashboard-widgets-visual, kanban-visual, tasks-tab-visual, tasks-page-visual, activity-page-visual, after-actions-page-visual, briefs-page-visual, settings-page-visual, dossier-drawer-visual) with `--retries=0 --reporter=html` and NO `--update-snapshots`. Mass failures are the MECHANISM: each failing shot emits expected/actual/diff PNGs under frontend/test-results/ plus the HTML report. (4) Tripwire: `git status` must show ZERO modified snapshot PNGs — any modified baseline PNG before the Task-3 checkpoint is laundering; abort and investigate. (5) Draft `80-VISUAL-RECOMPARE.md` mirroring the 77-BASELINE-VALIDATION structure: header (env, date, HEAD SHA, seed date from Task 1, replay command verbatim); a 43-row per-shot inventory (spec / test / snapshot name / diff present y-n / artifact path) with an empty Verdict column for Task 3; a reviewer pre-brief that widget date-string diffs are the documented third class 'dynamic content, not theme' (77-01 §1 discipline) and that intended-Linear palette changes are EXPECTED everywhere; the §5-style coverage statement — exactly 10 specs / 43 tests, with the exclusions restated verbatim (calendar-visual.spec.ts + dashboard-visual.spec.ts = non-baselined stubs with 0 committed PNGs; tests/visual/theme-visual.spec.ts = stale legacy outside testMatch; qa-sweep-focus-outline = structurally replaced, already recaptured Linear in 77-07 commit 229a39c8a); and a note listing the a11y-fix commits from Plan 80-03 that landed between the 77 baseline and this replay (so diff causes are fully attributable). Wrap wide tables in prettier-ignore fences.
  </action>
  <verify>
    <automated>git status --porcelain -- 'frontend/tests/e2e/*-snapshots' 'frontend/tests/e2e/__snapshots__' | wc -l | grep -qx 0 && grep -q "FROZEN_TIME" frontend/tests/e2e/dashboard-widgets-visual.spec.ts && grep -qc "prettier-ignore" .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-VISUAL-RECOMPARE.md</automated>
  </verify>
  <acceptance_criteria>
    - Zero baseline PNGs modified (`git status --porcelain` over both snapshot roots is empty)
    - FROZEN_TIME equals the Task-1 seed date at 12:00:00Z with the coupling comment intact
    - The replay ran with --retries=0 and without --update-snapshots (command quoted verbatim in the ledger header)
    - Ledger has exactly 43 inventory rows, the coverage statement with all 4 exclusions, the dynamic-content pre-brief, and the interim a11y-fix attribution note
    - dashboard-widgets WeekAhead/VipVisits rendered rows (not empty) — proof the seed/clock coupling held
  </acceptance_criteria>
  <done>All diff evidence exists and is organized for human adjudication; baselines untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN DIFF TRIAGE — adjudicate all 43 surfaces (LOCKED decision)</name>
  <files>.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-VISUAL-RECOMPARE.md</files>
  <read_first>
    - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-VISUAL-RECOMPARE.md — the 43-row inventory and pre-brief from Task 2
    - frontend/DESIGN.md — the Linear spec (the intended-Linear reference for classification)
  </read_first>
  <action>PAUSE for the human reviewer (LOCKED decision — do NOT auto-approve): present the HTML report + diff triads; scribe the reviewer's per-row verdict (intended-Linear | regression | dynamic content, not theme) into the 80-VISUAL-RECOMPARE.md Verdict column exactly as adjudicated; extract the regression subset into a 'Regressions to fix' list; record the approval line (reviewer, date).</action>
  <what-built>The complete replay diff evidence: per-shot expected/actual/diff PNG triads under frontend/test-results/ and the Playwright HTML report with side-by-side viewers, indexed by the 43-row ledger inventory.</what-built>
  <how-to-verify>
    1. Open the report: `pnpm -C frontend exec playwright show-report` (or open the HTML report path recorded in the ledger)
    2. Walk every one of the 43 rows; for each, classify: `intended-Linear` (palette/type/radii change matching frontend/DESIGN.md), `regression` (unexplained visual damage), or `dynamic content, not theme` (widget date strings etc. — pre-briefed class)
    3. Dictate or enter verdicts; the executor scribes them into the ledger's Verdict column exactly as adjudicated
    4. Confirm the coverage statement (10 specs / 43 tests, 4 exclusions) matches your expectation — any expansion must be explicit, never assumed
  </how-to-verify>
  <acceptance_criteria>
    - All 43 rows carry a human verdict (no blanks, no executor-invented verdicts)
    - An approval line exists: reviewer, date, and the sentence that the triage is complete
    - The `regression` subset (possibly empty) is extracted into a "Regressions to fix" list for Plan 80-05
  </acceptance_criteria>
  <verify>
    <automated>grep -cE "intended-Linear|regression|dynamic content" .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-VISUAL-RECOMPARE.md</automated>
  </verify>
  <resume-signal>Type "triage approved" once every row is adjudicated (or describe rows needing rework)</resume-signal>
  <done>Every visual diff is human-classified; recapture in Plan 80-05 is now — and only now — permitted.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                    |
| ----------------------------------- | ------------------------------------------------------------------------------ |
| orchestrator MCP -> staging DB      | privileged data write (seed refresh) — orchestrator/human only, never executor |
| executor -> committed baseline PNGs | the verification target itself; any write before triage destroys the control   |

## STRIDE Threat Register

| Threat ID | Category               | Component                                              | Disposition | Mitigation Plan                                                                                                                                    |
| --------- | ---------------------- | ------------------------------------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-80-10   | Tampering              | baseline laundering (--update-snapshots before review) | mitigate    | replay task carries --retries=0 and no update flag; git-status PNG tripwire; recapture lives in a separate later plan gated on the Task-3 approval |
| T-80-11   | Repudiation            | auto-approved diffs                                    | mitigate    | Task 3 is checkpoint:human-verify (LOCKED decision); verdicts + reviewer + date committed in the ledger                                            |
| T-80-12   | Elevation of privilege | executor attempting staging writes                     | mitigate    | seed refresh is a checkpoint:human-action executed by the orchestrator via MCP; executor has no DB credentials beyond read-path test login         |
| T-80-SC   | Tampering              | npm/pip/cargo installs                                 | accept      | zero package installs this phase                                                                                                                   |

</threat_model>

<verification>
- Baseline PNGs byte-identical to their 14191cb85 lineage (git status clean over snapshot dirs)
- 80-VISUAL-RECOMPARE.md: 43 verdicts + approval line + coverage statement + exclusions
- FROZEN_TIME date == seed date
</verification>

<success_criteria>

- The human has adjudicated 100% of the diff surface; regressions (if any) are enumerated for Plan 80-05
- No baseline was modified before approval — the anti-laundering order held
  </success_criteria>

<output>
Create `.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-04-SUMMARY.md` when done.
</output>
