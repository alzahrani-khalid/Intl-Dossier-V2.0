---
phase: 80-full-route-visual-a11y-verification-smoke-suite
plan: 05
type: execute
wave: 5
depends_on: ['80-04']
files_modified:
  - frontend/src/** (conditional — only files named by regression verdicts)
  - frontend/tests/e2e/*-snapshots/** (Linear baseline PNGs)
  - frontend/tests/e2e/__snapshots__/dashboard-widgets/** (Linear baseline PNGs)
  - frontend/tests/e2e/dashboard-widgets-visual.spec.ts (conditional FROZEN_TIME re-realign)
  - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-VISUAL-RECOMPARE.md
autonomous: false
requirements: [VERIFY-01]
must_haves:
  truths:
    - "Per CONTEXT decision 'Visual re-compare (VERIFY-01)': --update-snapshots runs ONLY after the human triage approval from Plan 80-04 — the order replay -> review -> fix -> update IS the anti-laundering control"
    - 'Every `regression` verdict is fixed and re-verified against the OLD (14191cb85) baseline before any recapture — no regression is papered over by the new baselines'
    - 'The new Linear baselines replay clean (43/43 at --retries=2, the 77-01 replay-proof discipline) before being committed; the Bureau lineage remains recoverable at commit 14191cb85'
    - 'The recapture happens on darwin (this Mac) — default-path PNGs are -chromium-darwin-named and the dashboard-widgets bare-name pathTemplate is deliberately preserved'
  artifacts:
    - path: 'frontend/tests/e2e/__snapshots__/dashboard-widgets'
      provides: 'recaptured Linear widget baselines (8 shots)'
    - path: '.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-VISUAL-RECOMPARE.md'
      provides: 'closed ledger: fixes applied, replay-proof result, new-lineage commit reference'
      contains: 'replay proof'
  key_links:
    - from: 'regression fixes in frontend/src'
      to: 're-replay of ONLY the affected specs vs the OLD baseline'
      via: 'per-fix verification before recapture'
      pattern: '--retries=0'
    - from: 'recaptured PNGs'
      to: 'clean no-update replay at --retries=2'
      via: '77-01 replay-proof discipline (byte-stable)'
      pattern: '--retries=2'
---

<objective>
Close VERIFY-01: fix every human-adjudicated regression, then — and only then — recapture the 43 baselines as the new Linear lineage, prove them byte-stable with a clean replay, and commit PNGs + ledger together. A seed-currency gate guards the 46-era seed/clock drift (if a day has passed since the Plan 80-04 seed, the widget rows fall out of the WeekAhead window and the recapture would be garbage).

Purpose: the committed Linear baselines become the reference for all future visual regression work; their birth must be provably post-review and reproducible.
Output: new Linear baseline PNGs committed, regression fixes (if any), closed 80-VISUAL-RECOMPARE.md ledger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-CONTEXT.md
@.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-RESEARCH.md
@.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-VISUAL-RECOMPARE.md
@.planning/phases/77-linear-token-system/77-BASELINE-VALIDATION.md
@frontend/DESIGN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Seed-currency gate (orchestrator)</name>
  <files>none (staging data + possibly the FROZEN_TIME constant in Task 2)</files>
  <read_first>
    - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-VISUAL-RECOMPARE.md — header seed date from Plan 80-04 Task 1
    - .planning/phases/77-linear-token-system/77-BASELINE-VALIDATION.md — §2 seed SQL (re-apply verbatim if stale)
  </read_first>
  <action>PAUSE for the orchestrator: compare today's date against the ledger's seed date; if same-day reply 'seed current', otherwise re-apply the §2 seed SQL via Supabase MCP and reply 'reseeded' with the date. The executor records the outcome in the ledger and, if reseeded, realigns FROZEN_TIME in Task 2 before any run.</action>
  <what-built>Nothing — this is a drift guard. The b0000002-* rows were seeded NOW()-anchored on the Plan 80-04 seed date; if today is a later date, rows drop out of the server-side NOW() window and WeekAhead/VipVisits render differently than the triaged evidence.</what-built>
  <how-to-verify>
    1. Compare today's date with the seed date in the 80-VISUAL-RECOMPARE.md header
    2. If SAME day: reply "seed current YYYY-MM-DD" — nothing to do
    3. If LATER: orchestrator re-applies the §2 SQL verbatim via Supabase MCP (project zkrcjzdemdmwhearhfgg), verifies via SELECT, and replies "reseeded YYYY-MM-DD" — the executor must then update FROZEN_TIME to the new date in Task 2 and note the re-seed in the ledger
  </how-to-verify>
  <acceptance_criteria>
    - The resume message states either "seed current" or "reseeded" with an explicit date
    - If reseeded: the ledger gains a re-seed note and Task 2 realigns FROZEN_TIME before anything runs
  </acceptance_criteria>
  <verify>
    <automated>MISSING — staging-data currency check; evidence is the dated resume message recorded in the ledger</automated>
  </verify>
  <resume-signal>Reply "seed current YYYY-MM-DD" or "reseeded YYYY-MM-DD"</resume-signal>
  <done>Seed and clock are guaranteed same-day for the recapture.</done>
</task>

<task type="auto">
  <name>Task 2: Fix regression verdicts and re-verify against the OLD baseline</name>
  <files>frontend/src/** (only files implicated by regression verdicts), .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-VISUAL-RECOMPARE.md, frontend/tests/e2e/dashboard-widgets-visual.spec.ts (only if Task 1 reseeded)</files>
  <read_first>
    - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-VISUAL-RECOMPARE.md — the "Regressions to fix" list (the work order; may be empty)
    - frontend/DESIGN.md — Linear token tables + recipe rules (regressions are usually token wiring: wrong surface-ladder step, missing var(--*), hairline vs shadow)
    - For each implicated component: its source + the closest styled sibling under frontend/src/components/
  </read_first>
  <action>
    If Task 1 reseeded, first update FROZEN_TIME in dashboard-widgets-visual.spec.ts to the new date (12:00:00Z, comment preserved). Then, for each row on the "Regressions to fix" list: root-cause the visual damage against the Linear spec (typical classes: un-tokenized color literal, wrong surface step, lost hairline border, radius hard-coded off the 6/8/12 scale); fix in frontend/src under full CLAUDE.md discipline (var(--*) tokens only, logical properties only, no new button variants, no shadows on cards); then re-run ONLY the affected spec(s) with --retries=0 and NO --update-snapshots against the STILL-OLD baseline and confirm the remaining diff for that surface is now exclusively intended-Linear or dynamic-content class — quote the re-run result in the ledger row and flip its verdict to `fixed (was regression)`. If the regression list is empty, write an explicit "No regression verdicts — nothing to fix" line in the ledger (never skip silently). Keep every fix surgical: each src commit re-triggers the required-check gauntlet (type-check, lint, Bundle Size, pre-commit build).
  </action>
  <verify>
    <automated>grep -qE "No regression verdicts|fixed \(was regression\)" .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-VISUAL-RECOMPARE.md && cd frontend && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - Every `regression` row is now `fixed (was regression)` with the re-run evidence quoted — or the explicit no-regressions line exists
    - No baseline PNG was modified in this task (`git status --porcelain` over both snapshot roots still empty)
    - `pnpm -C frontend type-check` and `pnpm -C frontend lint` exit 0 if src was touched
    - If src was touched: `pnpm -C frontend exec playwright test --project=a11y` exits 0 — a visual-regression fix must not silently reintroduce an a11y violation greened in Plan 80-03 (regression-net closure)
  </acceptance_criteria>
  <done>Zero unexplained regressions remain; the tree is ready to become the new baseline.</done>
</task>

<task type="auto">
  <name>Task 3: Recapture the 43 Linear baselines + replay proof + lineage commit</name>
  <files>frontend/tests/e2e/*-snapshots/**, frontend/tests/e2e/__snapshots__/dashboard-widgets/**, .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-VISUAL-RECOMPARE.md</files>
  <read_first>
    - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-RESEARCH.md — §VERIFY-01 step 5 (recapture + replay-proof discipline) + Landmines (darwin-only; pathTemplate deliberately bare-named)
    - .planning/phases/77-linear-token-system/77-BASELINE-VALIDATION.md — §4 replay proof + §7 commit-scope discipline (the exact precedent to mirror)
  </read_first>
  <action>
    Recapture and prove. (1) Preconditions: Plan 80-04 Task-3 approval exists in the ledger (hard gate — if the approval line is absent, STOP); `E2E_BASE_URL` unset; darwin host; :5173 serving main. (2) Recapture: run the same 10-spec command from the replay but with `--update-snapshots` — this regenerates all 43 PNGs (default-path shots keep -chromium-darwin names; dashboard-widgets keeps its bare-name pathTemplate — do not "fix" it). (3) Replay proof: immediately re-run the same 10 specs WITHOUT --update-snapshots at `--retries=2` (CI parity) — all 43 must pass, proving the new PNGs are byte-stable under re-render (the 77-01 discipline). A failure here means a nondeterminism leak (fonts, clock, seed) — diagnose, never loosen thresholds in playwright.config.ts. (4) Close the ledger: append the recapture + replay-proof commands, 43/43 result, date, and the lineage statement — new Linear baselines supersede the Bureau set, which remains recoverable at commit 14191cb85; note the FROZEN_TIME <-> seed-date coupling for the next recapture (inherent 46-era fragility, carried forward). (5) Commit PNGs + ledger + (if changed) the FROZEN_TIME constant together as the new-baseline commit, message following the 77-01 precedent (test(80-05): commit VERIFY-01 Linear visual baseline + triage ledger).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec playwright test list-pages-visual.spec.ts dashboard-widgets-visual.spec.ts kanban-visual.spec.ts tasks-tab-visual.spec.ts tasks-page-visual.spec.ts activity-page-visual.spec.ts after-actions-page-visual.spec.ts briefs-page-visual.spec.ts settings-page-visual.spec.ts dossier-drawer-visual.spec.ts --retries=2 --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - The replay-proof run exits 0 with 43/43 passing and zero snapshot writes (`git status --porcelain` over snapshot roots empty AFTER the proof run)
    - The baseline commit contains ONLY: snapshot PNGs, 80-VISUAL-RECOMPARE.md, and (if realigned) dashboard-widgets-visual.spec.ts — verified via `git show --stat`
    - Ledger closes with the lineage statement referencing 14191cb85 and the seed/FROZEN_TIME coupling note
    - playwright.config.ts thresholds untouched (`git diff --name-only` does not list it)
  </acceptance_criteria>
  <done>VERIFY-01 closed: 43 human-ratified Linear baselines committed, reproducible, with Bureau lineage preserved.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                                                         |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| recapture step -> committed baseline PNGs | this task intentionally rewrites the verification reference — legitimacy comes solely from the prior human approval |

## STRIDE Threat Register

| Threat ID | Category    | Component                                           | Disposition | Mitigation Plan                                                                                                                |
| --------- | ----------- | --------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------ |
| T-80-13   | Tampering   | recapture before/without approval                   | mitigate    | Task-3 hard-gates on the 80-04 approval line in the ledger; wave ordering enforces the checkpoint precedes this plan           |
| T-80-14   | Tampering   | threshold loosening to force the replay proof green | mitigate    | playwright.config.ts is out of files_modified; acceptance asserts it unchanged; nondeterminism must be diagnosed, not absorbed |
| T-80-15   | Repudiation | untraceable baseline lineage                        | mitigate    | ledger + commit message record old lineage (14191cb85), new lineage, seed date, and FROZEN_TIME coupling                       |
| T-80-SC   | Tampering   | npm/pip/cargo installs                              | accept      | zero package installs this phase                                                                                               |

</threat_model>

<verification>
- 43/43 clean replay at --retries=2 on the new baselines
- Regression list fully resolved (or explicitly empty)
- Commit scope matches the 77-01 precedent; config thresholds untouched
</verification>

<success_criteria>

- Every diff from the re-compare is now either human-ratified intended-Linear, documented dynamic-content, or fixed — no unexplained regressions (VERIFY-01 satisfied)
- The new Linear baseline lineage is committed and reproducible
  </success_criteria>

<output>
Create `.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-05-SUMMARY.md` when done.
</output>
