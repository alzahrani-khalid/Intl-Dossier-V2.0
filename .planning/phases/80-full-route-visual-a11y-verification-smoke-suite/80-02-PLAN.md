---
phase: 80-full-route-visual-a11y-verification-smoke-suite
plan: 02
type: execute
wave: 2
depends_on: ['80-01']
files_modified:
  - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md
autonomous: true
requirements: [VERIFY-02]
must_haves:
  truths:
    - "Per CONTEXT decision 'Axe-core a11y sweep (VERIFY-02)': the pre-migration baseline is RECORDED from an actual pre-token run (git worktree at commit 14191cb85), not inferred or asserted — no baseline laundering"
    - "'No new violations' is evidenced as B ⊆ A per test/route/axis in 80-A11Y-BASELINE.md, with an explicit verdict line"
    - 'Any failure present on HEAD but absent at 14191cb85 is flagged migration-caused and routed to the Plan 80-03 must-fix list — such items may NEVER be recorded-as-baseline'
  artifacts:
    - path: '.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md'
      provides: 'Finalized recorded pre-migration baseline: set A (pre-token) + set B (HEAD) + B⊆A comparison + must-fix list'
      contains: '14191cb85'
  key_links:
    - from: 'pre-token worktree run'
      to: 'HEAD run (set B from 80-01)'
      via: 'same staging DB, same-day fairness statement recorded in the ledger'
      pattern: 'same-day'
    - from: '80-A11Y-BASELINE.md must-fix list'
      to: 'Plan 80-03 fix-vs-record task'
      via: 'NEW-on-HEAD classification column'
      pattern: 'NEW-on-HEAD'
---

<objective>
Record the honest pre-migration a11y baseline (set A) by running the identical suites on a git worktree pinned to the pre-token commit `14191cb85`, then finalize the A/B comparison: "no new violations" = B ⊆ A per test/route/axis. v8.0 is entirely local (main is ~142 commits ahead of origin), so the pre-token tree is directly checkable — code is the only variable when both legs run same-day against the same staging DB.

Purpose: VERIFY-02's comparison is only meaningful against a RECORDED baseline. This plan produces that record and the definitive migration-caused/pre-existing classification that constrains every fix-vs-record decision.
Output: finalized `80-A11Y-BASELINE.md` (sets A + B, comparison, must-fix list). No repo code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-CONTEXT.md
@.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-RESEARCH.md
@.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md
@frontend/playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the pre-token leg (set A) in a worktree at 14191cb85</name>
  <files>.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md</files>
  <read_first>
    - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-RESEARCH.md — §VERIFY-02 "Recommended approach" step 2 (worktree method) + Assumptions A3
    - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md — the set-B record and characterization from 80-01 (this task mirrors those exact run commands)
    - frontend/tests/e2e/qa-sweep-axe-4axis.spec.ts — the spec to overlay into the worktree
  </read_first>
  <action>
    Build and run the A-leg. (1) `git worktree add ../intl-pre-token 14191cb85` (detached HEAD — read-only lineage; never commit there). (2) Copy the gitignored env files the harness needs (verified absent from checkouts): repo-root `.env.test` to `../intl-pre-token/.env.test` and `frontend/.env.development` to `../intl-pre-token/frontend/.env.development` (loginForListPages throws on missing `TEST_USER_EMAIL`/`TEST_USER_PASSWORD` — that error means the copy was incomplete). (3) `pnpm install --frozen-lockfile` at the worktree root (same lockfile era; chromium browsers are already cached for Playwright 1.60.0). (4) Overlay the 4-axis spec: copy `frontend/tests/e2e/qa-sweep-axe-4axis.spec.ts` from main into the same worktree path — a test-only overlay so both trees run the identical sweep; note the overlay in the ledger (its helper imports `qa-sweep.ts`/`v6-routes.ts` exist at 14191cb85, Phase-43 era). (5) CRITICAL port hygiene: before the worktree run, kill any dev server on :5173 (`lsof -ti:5173` then kill) — otherwise Playwright's `reuseExistingServer` silently scans the MAIN tree and poisons set A. (6) In the worktree, with `E2E_BASE_URL` unset, run `pnpm -C frontend exec playwright test --project=a11y --retries=1` and `pnpm -C frontend exec playwright test qa-sweep-axe-4axis.spec.ts --project=chromium --retries=1`; archive raw logs to the scratchpad. (7) Distill set A into a new "Pre-token leg (set A)" ledger section: per test (a11y project) and per route x locale x theme (sweep), pass / hard-fail with assertion / violation ids. The pre-token tree renders Bureau light-default — expected and irrelevant to axe semantics; note it. (8) Same-day fairness: if the ledger's set-B date is not today, re-run the two HEAD legs now (kill :5173 first so Playwright boots MAIN's server) and update set B before comparison. (9) Cleanup: kill the worktree's dev server, then `git worktree remove ../intl-pre-token --force`.
  </action>
  <verify>
    <automated>grep -q "set A" .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md && grep -q "14191cb85" .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md && git worktree list | grep -cv "intl-pre-token" | grep -qv "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - Ledger has a "Pre-token leg (set A)" section stating worktree commit `14191cb85`, run date, and both run commands verbatim
    - Set A covers BOTH suites: the a11y project (5 specs) and the 60-scan 4-axis sweep (overlay noted)
    - Fairness statement present: sets A and B ran same-day against the same staging DB (or the HEAD re-run is recorded)
    - `git worktree list` shows no `intl-pre-token` entry after cleanup; main tree `git status` untouched
  </acceptance_criteria>
  <done>Set A is recorded from a real pre-token run — the baseline exists as committed evidence, not assertion.</done>
</task>

<task type="auto">
  <name>Task 2: A/B comparison — finalize the recorded baseline and the must-fix list</name>
  <files>.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md</files>
  <read_first>
    - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md — sets A and B as recorded
    - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-RESEARCH.md — §VERIFY-02 Landmines (best-practice tag divergence; no laundering) + Assumptions A4
  </read_first>
  <action>
    Add the comparison section that makes the baseline usable. For every hard failure / violation in either set, classify per test/route/axis into exactly one of: `pre-existing` (in A and B — the recorded baseline), `fixed-on-HEAD` (in A, not B — improvement, note only), `NEW-on-HEAD` (in B, not A — migration-caused). Emit: (a) an explicit verdict line — "B ⊆ A: TRUE" or "B ⊆ A: FALSE — N migration-caused items listed below"; (b) the must-fix list enumerating every NEW-on-HEAD item (possibly empty) — these are Plan 80-03 FIX-mandatory (recording one as baseline would be laundering, per the locked decision); (c) a per-spec tag-divergence note: `dossiers-rtl-a11y` includes the axe `best-practice` tag while `runAxe` does not — record deltas as-is, do NOT unify the tag sets; (d) a scope reminder that the 8 quarantined specs (playwright.config.ts comment) and the vitest `waiting-queue-a11y` T091-07 failure keep their existing recorded dispositions. Update the 80-01 characterization rows with their A/B classification so Plan 80-03 reads one table. Keep prettier-ignore fences around wide tables.
  </action>
  <verify>
    <automated>grep -qE "B ⊆ A|B .= A" .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md && grep -q "NEW-on-HEAD" .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md && grep -q "best-practice" .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - Every hard-failure row carries exactly one classification in {pre-existing, fixed-on-HEAD, NEW-on-HEAD}
    - The explicit B ⊆ A verdict line exists; if FALSE, every migration-caused item appears in the must-fix list
    - best-practice tag-divergence note and quarantine/T091-07 disposition reminders present
    - No source or test files changed by this plan (`git status --porcelain -- frontend/` empty)
  </acceptance_criteria>
  <done>The recorded pre-migration baseline is complete and every downstream fix-vs-record decision has its A/B constraint.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                      | Description                                             |
| --------------------------------------------- | ------------------------------------------------------- |
| worktree (pre-token code) -> staging Supabase | old code runs against live staging data with test creds |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                       | Disposition | Mitigation Plan                                                                                                                 |
| --------- | ---------------------- | ------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------- |
| T-80-04   | Tampering              | recorded baseline (set A)                                                       | mitigate    | set A produced by an actual run at the immutable commit `14191cb85`; commands + date committed in the ledger; raw logs archived |
| T-80-05   | Spoofing               | wrong-tree scan (reuseExistingServer serving main while "testing" the worktree) | mitigate    | mandatory :5173 kill-guard before each leg; ledger records which tree served each run                                           |
| T-80-06   | Information disclosure | env-file copies in the worktree                                                 | mitigate    | copies live only inside `../intl-pre-token` (outside the repo), removed with `git worktree remove --force`; never committed     |
| T-80-SC   | Tampering              | npm/pip/cargo installs                                                          | accept      | `pnpm install --frozen-lockfile` in the worktree resolves the historical committed lockfile only — no new packages              |

</threat_model>

<verification>
- `80-A11Y-BASELINE.md` contains sets A and B, the B ⊆ A verdict, per-row classifications, and the must-fix list
- Worktree removed; main tree clean of unintended changes
</verification>

<success_criteria>

- The pre-migration baseline is RECORDED (real run at 14191cb85), satisfying VERIFY-02's comparison precondition
- Migration-caused items (if any) are isolated and mandated to fix — the laundering path is structurally closed
  </success_criteria>

<output>
Create `.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-02-SUMMARY.md` when done.
</output>
