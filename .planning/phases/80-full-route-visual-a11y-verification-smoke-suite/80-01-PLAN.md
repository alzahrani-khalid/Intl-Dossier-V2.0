---
phase: 80-full-route-visual-a11y-verification-smoke-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tests/e2e/qa-sweep-axe-4axis.spec.ts
  - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md
autonomous: true
requirements: [VERIFY-02]
must_haves:
  truths:
    - "Per CONTEXT decision 'Axe-core a11y sweep (VERIFY-02)': the live a11y failure set is characterized on ONE reference environment (local seeded dev) with a per-failure root-cause class — never planned from the stale '2 hard failures' requirement text (live CI reality is ~9 hard / 5 flaky and shifts run-to-run)"
    - 'All 6 testDossierIds rows, FIXTURE_DOSSIER_ID, and the 3 SRTL-02 July-2026 calendar_entries seed rows are verified live in staging BEFORE any failure is classified (a missing row turns a scan into a 404-page scan that mimics an a11y failure)'
    - "A 4-axis axe sweep (V6_ROUTES x en/ar x light/dark) exists as an EXPLICIT, called-out coverage extension and has produced a recorded HEAD-leg (set B) result — dark-only implicit scanning is no longer the sweep's ceiling"
  artifacts:
    - path: 'frontend/tests/e2e/qa-sweep-axe-4axis.spec.ts'
      provides: 'VERIFY-02 4-axis sweep vehicle (15 routes x en/ar x light/dark = 60 scans)'
      contains: 'runAxe'
    - path: '.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md'
      provides: 'Per-failure characterization table + HEAD-leg (set B) results + fixture-liveness proof'
  key_links:
    - from: 'frontend/tests/e2e/qa-sweep-axe-4axis.spec.ts'
      to: 'frontend/tests/e2e/helpers/qa-sweep.ts'
      via: 'runAxe import — inline AxeBuilder re-implementation is forbidden by the qa-sweep-axe spec header'
      pattern: "from './helpers/qa-sweep'"
    - from: 'frontend/tests/e2e/qa-sweep-axe-4axis.spec.ts'
      to: 'localStorage id.theme'
      via: 'page.addInitScript theme pin registered before first navigation (first-paint-safe)'
      pattern: "id\\.theme"
---

<objective>
Establish the honest ground truth for VERIFY-02: verify the load-bearing staging fixture rows are alive, characterize every hard a11y failure on the single reference environment (local seeded dev against staging Supabase), and build + run the explicit 4-axis axe sweep on HEAD. This produces the B-leg (post-migration) of the recorded baseline and the authoritative per-failure list that all fix-vs-record decisions in Plan 80-03 derive from.

Purpose: VERIFY-02 forbids baseline laundering — decisions must be made from evidence gathered on one stated environment, not from shifting CI logs. This plan gathers that evidence.
Output: `frontend/tests/e2e/qa-sweep-axe-4axis.spec.ts` (new sweep spec) + `80-A11Y-BASELINE.md` (characterization + set B), no src changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-CONTEXT.md
@.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-RESEARCH.md
@.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-PATTERNS.md
@frontend/tests/e2e/qa-sweep-axe.spec.ts
@frontend/tests/e2e/helpers/qa-sweep.ts
@frontend/tests/e2e/helpers/v6-routes.ts
@frontend/tests/fixtures/dossier-fixtures.ts
@frontend/tests/e2e/support/dossier-drawer-fixture.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixture-liveness probe + local a11y characterization run</name>
  <files>.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md</files>
  <read_first>
    - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-RESEARCH.md — §VERIFY-02 "Concrete assets" + "Recommended approach" steps 1-2 + "Landmines" (staging data drift, environment split-brain)
    - frontend/tests/fixtures/dossier-fixtures.ts — the 6 `testDossierIds` (e.g. engagement `b0000002-0000-0000-0000-000000000003`)
    - frontend/tests/e2e/support/dossier-drawer-fixture.ts — `FIXTURE_DOSSIER_ID` (country)
    - frontend/playwright.config.ts — the `a11y` project block (~lines 132-142: exactly 5 gated specs) and the 8-spec quarantine comment (~lines 118-131) — quarantined specs are OUT of scope
    - .planning/phases/76-rtl-infrastructure-bridge-shadcn-logical-properties/76-SRTL02-VERIFICATION.md — §1 environment note (the 3 `calendar_entries` rows with `title_en LIKE 'SRTL-02 regression seed %'`, `organizer_id` = the test user, July 2026)
  </read_first>
  <action>
    Two steps, in order. (1) Liveness probe: write a throwaway Node script in the session scratchpad (NOT the repo) that uses `@supabase/supabase-js` (already installed) with `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` from `frontend/.env.development` and `auth.signInWithPassword` with `TEST_USER_EMAIL`/`TEST_USER_PASSWORD` from repo-root `.env.test` (never hardcode creds; the script reads env/dotenv). Query: (a) `dossiers` rows for all 6 `testDossierIds` ids, (b) the `FIXTURE_DOSSIER_ID` dossier, (c) `calendar_entries` where `title_en` starts with `SRTL-02 regression seed` and `event_date` falls in July 2026. If ANY of the 6+1 dossier rows or fewer than 3 calendar rows resolve: STOP the task and escalate to the orchestrator for a Supabase-MCP re-seed (executors have no MCP access — established project fact); do not classify any failure against dead fixtures. (2) Characterization run: with `E2E_BASE_URL` unset (Playwright auto-starts `pnpm dev` on :5173), run `pnpm -C frontend exec playwright test --project=a11y --retries=1` and capture full output (save the raw log to the scratchpad). Create `80-A11Y-BASELINE.md` with: a header stating reference environment (local seeded dev, this Mac), date, HEAD commit SHA; the fixture-liveness proof (row counts per query); and a per-failure characterization table — one row per HARD failure (retry-passes are flaky, note separately) with columns: spec file, test title, failing assertion (e.g. `main` landmark / unlabeled-count `< 5` / `h1Count >= 1` / first-Tab-interactive), and root-cause class in {app-bug, test-bug, data-drift}. Note the pre-existing vitest `waiting-queue-a11y` T091-07 failure as out-of-scope baseline (CI `Tests (frontend)` is green — do not chase). Wrap wide markdown tables in prettier-ignore fences (lint-staged churns `.planning` tables).
  </action>
  <verify>
    <automated>test -f .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md && grep -q "Reference environment" .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md && grep -qE "app-bug|test-bug|data-drift" .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - Probe output (quoted in the ledger) shows 6/6 `testDossierIds` + `FIXTURE_DOSSIER_ID` resolved and >= 3 SRTL-02 July-2026 calendar rows — OR the task stopped with an explicit orchestrator escalation (no classification against dead fixtures)
    - `pnpm -C frontend exec playwright test --project=a11y --retries=1` completed (red is expected and fine — this is characterization, not a gate)
    - Every hard failure from the run has a ledger row with spec, test title, assertion, and root-cause class; flaky (retry-pass) tests listed separately; the 10 existing `test.fixme` skips acknowledged as already-recorded debt
    - Ledger header states env + date + `git rev-parse --short HEAD`
  </acceptance_criteria>
  <done>The authoritative per-failure list exists on the stated reference env and all load-bearing fixture rows are proven live.</done>
</task>

<task type="auto">
  <name>Task 2: Build the 4-axis axe sweep spec and record the HEAD leg (set B)</name>
  <files>frontend/tests/e2e/qa-sweep-axe-4axis.spec.ts, .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-A11Y-BASELINE.md</files>
  <read_first>
    - frontend/tests/e2e/qa-sweep-axe.spec.ts — the whole 39-line analog (loop shape, spec-header prohibition on inline AxeBuilder)
    - frontend/tests/e2e/helpers/qa-sweep.ts — `runAxe` (lines ~39-52), `settlePage`, `waitForRouteReady`
    - frontend/tests/e2e/helpers/v6-routes.ts — `V6_ROUTES` (15 routes, single source of truth)
    - frontend/tests/e2e/dashboard-widgets-visual.spec.ts — lines ~52-58, the `addInitScript` localStorage `id.theme` pin pattern
    - .planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-PATTERNS.md — §qa-sweep-axe-4axis (verbatim loop + theme-pin excerpts)
  </read_first>
  <action>
    Create `frontend/tests/e2e/qa-sweep-axe-4axis.spec.ts` as a sibling of `qa-sweep-axe.spec.ts` (auto-discovered by the `chromium` project testMatch). Header comment: "Phase 80 VERIFY-02 — explicit, called-out 4-axis extension of qa-sweep-axe (15 routes x en/ar x light/dark); do not re-implement AxeBuilder inline — use runAxe". Loop: for each route of `V6_ROUTES`, for each locale of `route.locales`, for each theme of a `['light','dark']` const — test titled `${route.name} [${locale}] [${theme}] — zero serious/critical axe violations`. Inside each test: register `page.addInitScript` seeding `window.localStorage['id.theme']` to the axis theme (try/catch-wrapped, BEFORE any navigation so bootstrap.js reads it at first paint — do NOT use the `window.__design.setMode` hatch), then `loginForListPages(page, locale)`, `page.goto(route.path)`, `settlePage(page)`, `waitForRouteReady(page)`, `runAxe(page, { include: 'main' })`. Then run the HEAD leg locally: `E2E_BASE_URL` unset, `pnpm -C frontend exec playwright test qa-sweep-axe-4axis.spec.ts --project=chromium --retries=1` (60 scans — a red run is DATA, not task failure). Append a "HEAD leg (set B)" section to `80-A11Y-BASELINE.md`: per route x locale x theme, pass or the serious/critical violation ids reported by runAxe. Explicitly note in both the spec header and the ledger: this extension satisfies VERIFY-02's "all four axes" and is called out per the CONTEXT no-silent-expansion discipline; it is NOT wired into the FOUC-02 CI smoke job (runtime budget — it stays a local baseline vehicle).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec playwright test qa-sweep-axe-4axis.spec.ts --project=chromium --list 2>&1 | tail -3 && grep -c "AxeBuilder" tests/e2e/qa-sweep-axe-4axis.spec.ts | grep -qx 0 && grep -q "from './helpers/qa-sweep'" tests/e2e/qa-sweep-axe-4axis.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `playwright test qa-sweep-axe-4axis.spec.ts --list --project=chromium` reports 60 tests (sum over V6_ROUTES of |locales| x 2 themes; record the actual count in the ledger if any route is single-locale)
    - `grep -c "AxeBuilder" frontend/tests/e2e/qa-sweep-axe-4axis.spec.ts` = 0 (no inline re-implementation) and the spec imports `runAxe` from `./helpers/qa-sweep`
    - The HEAD run completed and the ledger's "HEAD leg (set B)" section has one result row per scan (pass or violation ids)
    - Spec header + ledger both carry the explicit called-out-extension statement
  </acceptance_criteria>
  <done>The 4-axis sweep exists, follows the established gate helper, and set B is recorded — ready for the pre-token A-leg comparison in Plan 80-02.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                               |
| ---------------------------------- | ----------------------------------------------------------------------------------------- |
| local test env -> staging Supabase | authenticated test creds from `.env.test` cross to staging (project zkrcjzdemdmwhearhfgg) |

## STRIDE Threat Register

| Threat ID | Category               | Component                                  | Disposition | Mitigation Plan                                                                                                                     |
| --------- | ---------------------- | ------------------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| T-80-01   | Tampering              | a11y baseline evidence                     | mitigate    | characterization runs on ONE stated reference env with date + SHA recorded in the committed ledger; raw logs archived to scratchpad |
| T-80-02   | Repudiation            | fixture data drift misread as a11y failure | mitigate    | liveness probe gates classification; STOP-and-escalate on missing rows                                                              |
| T-80-03   | Information disclosure | test creds in probe script                 | mitigate    | script lives in session scratchpad (not repo), reads creds from `.env.test`/env only, never hardcoded or committed                  |
| T-80-SC   | Tampering              | npm/pip/cargo installs                     | accept      | zero package installs this phase (RESEARCH Package Legitimacy Audit: none; pinned Playwright 1.60.0 / @axe-core/playwright 4.11.3)  |

</threat_model>

<verification>
- `80-A11Y-BASELINE.md` exists with env header, fixture-liveness proof, per-failure characterization table, and HEAD-leg (set B) section
- `qa-sweep-axe-4axis.spec.ts` lists 60 tests, imports `runAxe`, contains zero inline `AxeBuilder`
- No `frontend/src` or snapshot PNG files modified (`git status --porcelain` over `frontend/src` and `frontend/tests/e2e/*-snapshots` is empty)
</verification>

<success_criteria>

- Every hard a11y failure on the reference env has a characterized ledger row with a root-cause class
- Set B (HEAD, 4 axes) is recorded and reproducible from the stated commands
- Fixture liveness proven (or escalated) before any classification happened
  </success_criteria>

<output>
Create `.planning/phases/80-full-route-visual-a11y-verification-smoke-suite/80-01-SUMMARY.md` when done.
</output>
