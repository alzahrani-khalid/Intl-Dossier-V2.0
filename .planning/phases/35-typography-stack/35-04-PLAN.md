---
phase: 35-typography-stack
plan: 04
type: execute
wave: 2
depends_on: [35-01, 35-02]
files_modified:
  - frontend/src/index.css
requirements:
  - TYPO-01
  - TYPO-03
  - TYPO-04
requirements_addressed:
  - TYPO-01
  - TYPO-03
  - TYPO-04
autonomous: true

must_haves:
  truths:
    - '`frontend/src/index.css` no longer declares `--text-family`, `--text-family-rtl`, `--display-family`, `--text-weight`, `--display-weight` in `:root`'
    - 'All 13 `font-family: var(--text-family|--display-family*)` rewrites in existing rules now reference `var(--font-body)`, `var(--font-display)`, `var(--font-mono)`'
    - "All 4 legacy `html[dir='rtl']` font-family blocks removed (replaced by D-07 cascade)"
    - 'The 48-line verbatim Tajawal cascade block from handoff `project/src/app.css` lines 129-176 is appended byte-for-byte'
    - 'The Wave-0 `tajawal-cascade.test.ts` drift guard (RED at Wave 0) is GREEN'
    - 'The `@theme` font-var form matches the Plan 35-01 Task 2 A1 verdict (self-ref OR literal-fallback)'
  artifacts:
    - path: 'frontend/src/index.css'
      provides: 'Rewritten legacy font refs + appended Tajawal cascade + @theme font utilities'
      contains: '/* ============ Arabic typography override: Tajawal ============ */'
  key_links:
    - from: 'frontend/src/index.css'
      to: 'browser CSS cascade'
      via: 'var(--font-body) etc. resolve at paint time to :root values written by applyTokens()'
      pattern: "var\\(--font-body\\)"
    - from: 'frontend/src/index.css'
      to: "html[dir='rtl'] RTL override cascade"
      via: 'appended cascade block (D-07)'
      pattern: "html\\[dir='rtl'\\] \\*"
---

<objective>
Mutate `frontend/src/index.css` in two atomic commits: (A) rewrite all 13 legacy `var(--text-family*|--display-family)` references inline + delete 5 `:root` legacy var declarations + delete 4 legacy RTL font-family blocks; (B) append the verbatim 48-line Tajawal cascade from handoff `project/src/app.css` lines 129-176 AFTER the existing Phase 33 `@theme` block and BEFORE any Phase 33-era utility layers.

This plan is serialized after Plan 35-02 (needs `--font-*` emission) because the rewritten rules will reference `var(--font-body)` etc., which must be populated at runtime. A1 verdict from Plan 35-01 Task 2 is already recorded — this plan converts the probe into its final form.

Purpose: Deliver TYPO-01 (per-direction triplet routing via CSS vars) and TYPO-03 (verbatim Arabic cascade) and TYPO-04 (`[dir="ltr"].mono` carve-out in the cascade).

Output: A mutated `index.css` with (a) zero legacy `--text-family*` / `--display-family` references anywhere, (b) exactly 13 `var(--font-{body,display,mono})` references in existing rules, (c) the verbatim 48-line Tajawal block present, (d) the `@theme` font-var triplet in its final form per A1 verdict. `tajawal-cascade.test.ts` from Plan 35-01 flips GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-typography-stack/35-CONTEXT.md
@.planning/phases/35-typography-stack/35-RESEARCH.md
@.planning/phases/35-typography-stack/35-PATTERNS.md
@.planning/phases/35-typography-stack/35-01-SUMMARY.md

<interfaces>
<!-- Current index.css line map (verified via grep 2026-04-21; see STATE + PATTERNS §index.css) -->

Line 35: `@theme {` — block start (Phase 33-06 established)
Lines 35-115: @theme color/shadow tokens + Plan 35-01 Task 2 probe (3 `--font-*` lines, form TBD per A1 verdict)
Lines 150-154: `:root` block with 5 LEGACY declarations (to DELETE):
Line 150: `--display-family: 'Inter', system-ui, sans-serif;`
Line 151: `--display-weight: 600;`
Line 152: `--text-family: 'Inter', system-ui, sans-serif;`
Line 153: `--text-family-rtl: 'Readex Pro', system-ui, sans-serif;`
Line 154: `--text-weight: 400;`
Lines 211, 216, 238, 259, 266, 286, 302, 319, 328 (9 lines): `font-family: var(...)` — REWRITE
Lines 293, 312, 324, 333: body of 4 `html[dir='rtl']` blocks — DELETE blocks

(Line numbers drift as edits apply — executor uses the LITERAL-STRING find/replace pairs below, not line numbers, to avoid cascading offset bugs.)

Handoff verbatim Tajawal cascade source:
/tmp/inteldossier-handoff/inteldossier/project/src/app.css lines 129-176 (48 lines)
(Full block reproduced verbatim in Task 2 action — do not abbreviate.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite 13 legacy font-family refs + delete 5 :root legacy vars + delete 4 legacy RTL blocks</name>
  <files>frontend/src/index.css</files>
  <read_first>
    - frontend/src/index.css (current lines 140-340 for full legacy block context)
    - .planning/phases/35-typography-stack/35-RESEARCH.md (§Line-by-line `index.css` mutation map — lines 462-482)
    - .planning/phases/35-typography-stack/35-PATTERNS.md (§`frontend/src/index.css` — Mutation A)
  </read_first>
  <action>
    Perform the following find/replace operations in `frontend/src/index.css`. Use LITERAL STRING search-and-replace (not line numbers — line numbers will drift as earlier edits land). Apply these 18 operations in order:

    **Group 1 — Delete 5 `:root` legacy var declarations** (one `:root { ... }` block near the top after `@theme`). Remove these exact 5 lines (including their indentation):

    1. Delete line containing `--display-family: 'Inter', system-ui, sans-serif;`
    2. Delete line containing `--display-weight: 600;`
    3. Delete line containing `--text-family: 'Inter', system-ui, sans-serif;`
    4. Delete line containing `--text-family-rtl: 'Readex Pro', system-ui, sans-serif;`
    5. Delete line containing `--text-weight: 400;`

    (If the surrounding `:root { ... }` block becomes empty after these deletions, delete the entire `:root { }` block. If it still has other tokens like color fallbacks, leave the block but with fewer entries.)

    **Group 2 — Rewrite 9 `font-family` references** (surgical string replacements; each string appears only once in the file — confirmed via grep):

    6. Replace `font-family: var(--text-family);` → `font-family: var(--font-body);` — this literal appears in the `html` rule, the `body` rule, the `p, span, div` rule, the `html[dir='ltr']` rule, and the `.font-text` utility rule. Apply globally (all 5 occurrences of this exact literal).
    7. Replace `font-family: var(--display-family);` → `font-family: var(--font-display);` — 3 occurrences (`h1,h2,h3,h4,h5,h6` rule, `html[dir='ltr'] h1..h6` rule, `.font-display` utility rule).
    8. Replace `font-family: ui-monospace, SFMono-Regular, 'SF Mono', Menlo, Consolas, monospace;` → `font-family: var(--font-mono);` — 1 occurrence (`code, pre, kbd` rule).

    **Group 3 — Delete 4 legacy RTL blocks** (blocks that will be replaced wholesale by the Tajawal cascade in Task 2). Delete each entire block including its selector and braces:

    9. Delete the block:
       ```
       html[dir='rtl'] {
         font-family: var(--text-family-rtl);
       }
       ```
       (Also deletes any body selector combined here; look for the block whose ONLY font-family line was `var(--text-family-rtl)`.)

    10. Delete the block:
        ```
        html[dir='rtl'] h1,
        html[dir='rtl'] h2,
        html[dir='rtl'] h3,
        html[dir='rtl'] h4,
        html[dir='rtl'] h5,
        html[dir='rtl'] h6 {
          font-family: var(--text-family-rtl);
        }
        ```

    11. Delete the block:
        ```
        html[dir='rtl'] .font-display {
          font-family: var(--text-family-rtl);
        }
        ```

    12. Delete the block:
        ```
        html[dir='rtl'] .font-text {
          font-family: var(--text-family-rtl);
        }
        ```

    **Group 4 — Finalize @theme font-var form per A1 verdict** (replacement depends on Plan 35-01 Task 2 SUMMARY):

    - IF `35-01-SUMMARY.md` recorded `A1 verdict: SAFE`: leave the 3 probe lines as-is; replace only the comment header. Change the probe comment block:
      ```
      /* Phase 35 Wave-0 smoke probe — A1: same-name @theme self-reference for fonts.
         ...multi-line probe comment... */
      ```
      to the final comment:
      ```
      /* Phase 35 — D-01: font-family utilities (TYPO-01).
         Tailwind v4 exposes `--font-*` entries as `font-*` utility classes.
         RHS `var(--font-*)` refs are populated by buildTokens()/applyTokens() at boot. */
      ```

    - IF `35-01-SUMMARY.md` recorded `A1 verdict: UNSAFE`: keep the literal-fallback form (Chancery defaults serve as @theme LHS literals; applyTokens overrides at runtime) and replace the WARNING comment with:
      ```
      /* Phase 35 — D-01: font-family utilities (TYPO-01) — literal-fallback form (A1 UNSAFE).
         LHS values match Chancery defaults; applyTokens() overrides at runtime per direction.
         Phase 33-06 shadow workaround pattern (commit e5fcacec). */
      ```

    **Post-edit grep audit — MUST pass before committing**:

    ```bash
    # Zero legacy refs remain
    grep -c "var(--text-family)" frontend/src/index.css       # MUST print 0
    grep -c "var(--text-family-rtl)" frontend/src/index.css   # MUST print 0
    grep -c "var(--display-family)" frontend/src/index.css    # MUST print 0
    grep -c "\\-\\-text-family:" frontend/src/index.css        # MUST print 0
    grep -c "\\-\\-text-family-rtl:" frontend/src/index.css    # MUST print 0
    grep -c "\\-\\-display-family:" frontend/src/index.css     # MUST print 0
    grep -c "\\-\\-text-weight:" frontend/src/index.css        # MUST print 0
    grep -c "\\-\\-display-weight:" frontend/src/index.css     # MUST print 0

    # New refs present
    grep -c "var(--font-body)" frontend/src/index.css          # MUST print at least 5
    grep -c "var(--font-display)" frontend/src/index.css       # MUST print at least 3
    grep -c "var(--font-mono)" frontend/src/index.css          # MUST print at least 1
    ```

    If any of the first 8 counts is non-zero, legacy refs leaked — re-audit and delete.

  </action>
  <verify>
    <automated>bash -c "grep -c 'var(--text-family)' frontend/src/index.css; grep -c 'var(--display-family)' frontend/src/index.css; grep -c 'var(--font-body)' frontend/src/index.css; grep -c 'var(--font-display)' frontend/src/index.css"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var(--text-family)" frontend/src/index.css` returns `0`
    - `grep -c "var(--text-family-rtl)" frontend/src/index.css` returns `0`
    - `grep -c "var(--display-family)" frontend/src/index.css` returns `0`
    - `grep -c -- "--text-family:" frontend/src/index.css` returns `0`
    - `grep -c -- "--text-family-rtl:" frontend/src/index.css` returns `0`
    - `grep -c -- "--display-family:" frontend/src/index.css` returns `0`
    - `grep -c -- "--text-weight:" frontend/src/index.css` returns `0`
    - `grep -c -- "--display-weight:" frontend/src/index.css` returns `0`
    - `grep -c "var(--font-body)" frontend/src/index.css` returns at least `5`
    - `grep -c "var(--font-display)" frontend/src/index.css` returns at least `3`
    - `grep -c "var(--font-mono)" frontend/src/index.css` returns at least `1`
    - `grep -c "ui-monospace, SFMono-Regular" frontend/src/index.css` returns `0` (hardcoded mono stack gone)
    - `pnpm -C frontend exec vite build 2>&1 | grep -Ec "error|ERROR"` returns `0` (Tailwind generator still green — A1 decision held)
  </acceptance_criteria>
  <done>Zero legacy font-family vars remain; all 13 existing rules reference `var(--font-*)`; 4 legacy RTL blocks deleted; @theme font-var form finalized per A1 verdict; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Append verbatim 48-line Tajawal cascade (D-07)</name>
  <files>frontend/src/index.css</files>
  <read_first>
    - frontend/src/index.css (current state after Task 1 — find insertion point)
    - .planning/phases/35-typography-stack/35-RESEARCH.md (§Code Examples — lines 391-458 — full 48-line verbatim block)
    - .planning/phases/35-typography-stack/35-PATTERNS.md (§`index.css` Mutation B + landmines)
    - /tmp/inteldossier-handoff/inteldossier/project/src/app.css (lines 129-176 — authoritative source)
  </read_first>
  <action>
    Append the following 48-line block to `frontend/src/index.css`. Insertion point: AFTER the existing Phase 33 `@theme` block closes (after the line `}` that ends `@theme { ... }`), and BEFORE the first Phase 33-era `@layer utilities` or `@layer components` block (or at the end of the file if no such layer exists). Verify by grep: the new block should be preceded by the `@theme { ... }` closing brace and followed by either `@layer` directives or EOF.

    Write EXACTLY these 48 lines (byte-for-byte from `/tmp/inteldossier-handoff/inteldossier/project/src/app.css` lines 129-176). Do not edit whitespace, comments, selector order, or `!important` markers. Preserve single-quotes, exact selector lists, and line breaks:

    ```css
    /* ============ Arabic typography override: Tajawal ============ */
    /* When document is RTL, Tajawal takes precedence for all type — including */
    /* the Arabic-ع button (which is LTR by default) and Latin glyphs mixed in. */
    html[dir='rtl'],
    html[dir='rtl'] body {
      font-family: 'Tajawal', 'Inter', system-ui, sans-serif;
    }
    html[dir='rtl'] * {
      font-family: 'Tajawal', 'Inter', system-ui, sans-serif;
    }
    /* Defeat inline fontFamily:'var(--font-display)' in RTL (Fraunces lacks Arabic) */
    html[dir='rtl'] [style*='--font-display'] {
      font-family: 'Tajawal', system-ui, sans-serif !important;
    }
    /* Display (serifs, grotesks) still use Tajawal in Arabic — no fallback serif */
    html[dir='rtl'] .page-title,
    html[dir='rtl'] .card-title,
    html[dir='rtl'] .drawer-title,
    html[dir='rtl'] .kpi-value,
    html[dir='rtl'] .week-dd,
    html[dir='rtl'] .label,
    html[dir='rtl'] .sb-group,
    html[dir='rtl'] .dir-chancery,
    html[dir='rtl'] .dir-situation,
    html[dir='rtl'] .dir-ministerial,
    html[dir='rtl'] .dir-bureau {
      font-family: 'Tajawal', system-ui, sans-serif;
    }
    /* Let mono stay mono for non-Arabic tokens (⌘K, T−3), but when inside RTL
       container with Arabic content use Tajawal */
    html[dir='rtl'] .mono:not([dir='ltr']) {
      font-family: 'Tajawal', ui-monospace, monospace;
    }
    /* Keep the tiny LTR-isolated spans (kbd, T−3 etc.) in mono */
    html[dir='rtl'] [dir='ltr'].mono,
    html[dir='rtl'] kbd[dir='ltr'] {
      font-family: 'JetBrains Mono', ui-monospace, monospace;
    }

    /* Force Tajawal on chips/labels/tags in RTL — these have direction-class overrides */
    /* (.dir-bureau .chip, .dir-situation .chip etc.) which would otherwise win and  */
    /* show Arabic glyphs in Inter/JetBrains Mono (no Arabic coverage → tofu).        */
    html[dir='rtl'] .chip,
    html[dir='rtl'] .chip *,
    html[dir='rtl'] .label,
    html[dir='rtl'] .digest-tag,
    html[dir='rtl'] .task-due,
    html[dir='rtl'] .week-time,
    html[dir='rtl'] .week-day,
    html[dir='rtl'] .kpi-label,
    html[dir='rtl'] .kpi-delta,
    html[dir='rtl'] .overdue-days,
    html[dir='rtl'] .overdue-owner,
    html[dir='rtl'] .recent-when,
    html[dir='rtl'] .cls-chip {
      font-family: 'Tajawal', system-ui, sans-serif !important;
    }

    /* The Arabic-ع button in the locale switcher should also use Tajawal */
    html[dir='rtl'] .tb-locale-btn,
    .tb-locale-btn[data-lang='ar'] {
      font-family: 'Tajawal', system-ui, sans-serif;
    }
    ```

    Do NOT abbreviate. Do NOT add blank lines inside the block. Do NOT modify `!important` markers (Pitfall 3 — downstream phases rely on this precedence).

    Post-append verification:

    ```bash
    # The cascade must be present byte-for-byte
    diff <(sed -n '129,176p' /tmp/inteldossier-handoff/inteldossier/project/src/app.css) <(grep -A 100 "============ Arabic typography override: Tajawal ============" frontend/src/index.css | head -48)
    # diff MUST produce zero output lines (or be skipped if handoff source unavailable)
    ```

  </action>
  <verify>
    <automated>pnpm -C frontend exec vitest --run tests/unit/design-system/tajawal-cascade.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "============ Arabic typography override: Tajawal ============" frontend/src/index.css` returns `1`
    - `grep -c "html\[dir='rtl'\] \*" frontend/src/index.css` returns at least `1`
    - `grep -c "\[style\*='--font-display'\]" frontend/src/index.css` returns `1`
    - `grep -c "html\[dir='rtl'\] \[dir='ltr'\].mono" frontend/src/index.css` returns `1`
    - `grep -c "'JetBrains Mono', ui-monospace, monospace;" frontend/src/index.css` returns at least `1`
    - `grep -c "html\[dir='rtl'\] \.chip" frontend/src/index.css` returns `1`
    - `grep -c "!important" frontend/src/index.css` returns at least `2` (one in defeat rule, one in chip/label block)
    - `grep -c "\.tb-locale-btn\[data-lang='ar'\]" frontend/src/index.css` returns `1`
    - `pnpm -C frontend exec vitest --run tests/unit/design-system/tajawal-cascade.test.ts 2>&1 | grep -c "FAIL"` returns `0`
    - `pnpm -C frontend exec vitest --run tests/unit/design-system/tajawal-cascade.test.ts 2>&1 | grep -E "Tests.*passed"` shows `passed`
    - `pnpm -C frontend exec vite build 2>&1 | grep -Ec "error|ERROR"` returns `0`
    - If `/tmp/inteldossier-handoff/inteldossier/project/src/app.css` exists: `diff` between lines 129-176 of handoff and the 48-line block in index.css produces zero output
  </acceptance_criteria>
  <done>48-line verbatim Tajawal cascade present in index.css; byte-for-byte matches handoff source; drift-guard test GREEN.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                         | Description                                                                              |
| ------------------------------------------------ | ---------------------------------------------------------------------------------------- |
| Tailwind v4 `@theme` generator → compiled CSS    | A1 verdict (Plan 35-01) gates whether self-reference form or literal-fallback form lands |
| `html[dir='rtl']` cascade → Arabic text surfaces | `!important` markers in chip/label block are load-bearing for Phase 36-42 text rendering |

## STRIDE Threat Register

| Threat ID | Category               | Component                                         | Disposition | Mitigation Plan                                                                                                                                                                                     |
| --------- | ---------------------- | ------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-35-03   | Tampering              | Tajawal cascade byte-drift                        | mitigate    | Task 2 `diff` + `tajawal-cascade.test.ts` drift guard (authored Plan 35-01) compare against handoff `app.css` lines 129-176. ASVS V14 compliant via source-of-truth alignment. Severity LOW-MEDIUM. |
| T-35-04   | Denial of Service      | `@theme` self-reference                           | mitigate    | Already gated by Plan 35-01 Task 2 A1 verdict. This plan applies the final form chosen there. Severity LOW (already resolved upstream).                                                             |
| T-35-08   | Information Disclosure | RTL glyph fallback (Fraunces lacks Arabic → tofu) | mitigate    | `[style*='--font-display']` defeat rule + `!important` chip/label block prevent direction-class overrides from showing tofu glyphs. Severity: usability bug, not security. Accept classification.   |

</threat_model>

<verification>
- Zero legacy var references remain in `frontend/src/index.css`
- 48-line Tajawal cascade byte-for-byte matches handoff source
- `tajawal-cascade.test.ts` GREEN (all 5 it-blocks pass)
- `pnpm -C frontend exec vite build` exits 0 (Tailwind generator doesn't crash — A1 decision holds)
- `pnpm -C frontend exec vitest --run tests/unit/design-system/buildTokens.test.ts` still GREEN (Plan 35-02 regression check)
</verification>

<success_criteria>

- [ ] 13 `font-family: var(--text-family*|--display-family)` references rewritten to `var(--font-{body,display,mono})`
- [ ] 5 `:root` legacy font var declarations deleted
- [ ] 4 legacy `html[dir='rtl']` font-family blocks deleted
- [ ] 48-line Tajawal cascade appended byte-for-byte
- [ ] `@theme` font-var triplet in final form (per A1 verdict)
- [ ] `tajawal-cascade.test.ts` GREEN
- [ ] `vite build` exits 0
      </success_criteria>

<output>
After completion, create `.planning/phases/35-typography-stack/35-04-SUMMARY.md` capturing:
- Pre/post grep counts for all 8 "must-be-zero" legacy tokens
- Byte-diff result against handoff app.css
- Final @theme font-var form applied (SAFE / UNSAFE)
- Build verification result
</output>
