---
phase: 35-typography-stack
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/pnpm-lock.yaml
  - frontend/src/index.css
  - frontend/tests/unit/design-system/fonts.test.ts
  - frontend/tests/unit/design-system/tajawal-cascade.test.ts
  - frontend/tests/e2e/typography.spec.ts
  - frontend/tests/e2e/fixtures/typo-04-fixture.html
autonomous: true
requirements:
  - TYPO-01
  - TYPO-02
  - TYPO-03
  - TYPO-04
requirements_addressed:
  - TYPO-01
  - TYPO-02
  - TYPO-03
  - TYPO-04

must_haves:
  truths:
    - '`pnpm -C frontend dev` boots without Tailwind v4 generator crash after a 3-var `@theme` smoke extension (Assumption A1 resolved)'
    - 'All 8 `@fontsource[-variable]/*` packages resolve in `frontend/node_modules/`'
    - 'Three Wave 0 test files exist and are authored (RED state) so subsequent waves can go GREEN'
    - 'TYPO-04 fixture HTML file exists for E2E consumption'
  artifacts:
    - path: 'frontend/tests/unit/design-system/fonts.test.ts'
      provides: 'fontsource dependency + import-string drift guard (RED)'
      min_lines: 30
    - path: 'frontend/tests/unit/design-system/tajawal-cascade.test.ts'
      provides: '48-line verbatim cascade drift guard (RED)'
      min_lines: 25
    - path: 'frontend/tests/e2e/typography.spec.ts'
      provides: 'Playwright TYPO-01..04 scaffold (RED skip-guarded)'
      min_lines: 40
    - path: 'frontend/tests/e2e/fixtures/typo-04-fixture.html'
      provides: "Static fixture for `[dir='ltr'].mono` carve-out assertion"
      contains: 'data-testid="typo04-probe"'
    - path: 'frontend/package.json'
      provides: '8 new @fontsource dependencies'
      contains: '@fontsource-variable/inter'
  key_links:
    - from: 'frontend/package.json'
      to: 'node_modules/@fontsource-variable/inter/wght.css'
      via: 'pnpm install'
      pattern: '@fontsource-variable/inter'
    - from: 'frontend/src/index.css'
      to: 'Tailwind v4 `@theme` generator'
      via: 'Wave-0 smoke probe — three `--font-*` vars'
      pattern: '--font-display: '
---

<objective>
Wave 0 — install the 8 fontsource packages, resolve the HIGH-severity Assumption A1 (`@theme` self-reference for font tokens) via a minimal smoke probe, and scaffold every Wave 0 gap listed in `35-VALIDATION.md` (three test files + one fixture) so downstream waves can run RED→GREEN.

Purpose: De-risk the Tailwind v4 `@theme` collision pattern that crashed Phase 33-06 on shadows (commit `e5fcacec`). If the same-name `--font-display: var(--font-display)` pattern crashes the generator, fall back to either (a) distinct-name aliases (`--tw-font-display`) or (b) inline literal fallbacks with runtime override (Phase 33-06's shadow workaround). Also install deps and author the three Wave 0 test files referenced in VALIDATION.md §"Wave 0 Requirements".

Output: 8 new dependencies in `frontend/package.json`; a smoke-test verdict on A1 committed to `.planning/phases/35-typography-stack/35-01-SUMMARY.md`; three test scaffolds (RED — they fail because downstream code doesn't exist yet); one TYPO-04 fixture HTML.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-typography-stack/35-CONTEXT.md
@.planning/phases/35-typography-stack/35-RESEARCH.md
@.planning/phases/35-typography-stack/35-PATTERNS.md
@.planning/phases/35-typography-stack/35-VALIDATION.md

<interfaces>
<!-- Package matrix — verified 2026-04-22 against registry.npmjs.org. -->
<!-- 8 packages · 16 CSS sub-path imports total. Use these strings verbatim. -->

Dependencies (exact pin syntax for `pnpm add`):

- @fontsource-variable/inter@^5.2.8
- @fontsource-variable/public-sans@^5.2.7
- @fontsource-variable/space-grotesk@^5.2.10
- @fontsource-variable/fraunces@^5.2.9
- @fontsource-variable/jetbrains-mono@^5.2.8
- @fontsource/ibm-plex-sans@^5.2.8
- @fontsource/ibm-plex-mono@^5.2.7
- @fontsource/tajawal@^5.2.7

Required import literals (will be added in Plan 35-02; tested via grep here):

- `@fontsource-variable/inter/wght.css`
- `@fontsource-variable/public-sans/wght.css`
- `@fontsource-variable/space-grotesk/wght.css`
- `@fontsource-variable/fraunces/wght.css`
- `@fontsource-variable/jetbrains-mono/wght.css`
- `@fontsource/ibm-plex-sans/400.css`
- `@fontsource/ibm-plex-sans/500.css`
- `@fontsource/ibm-plex-sans/600.css`
- `@fontsource/ibm-plex-sans/700.css`
- `@fontsource/ibm-plex-mono/400.css`
- `@fontsource/ibm-plex-mono/500.css`
- `@fontsource/tajawal/400.css`
- `@fontsource/tajawal/500.css`
- `@fontsource/tajawal/700.css`

(16 imports total — 5 variable + 11 classic weights)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install 8 @fontsource packages into frontend workspace</name>
  <files>frontend/package.json, frontend/pnpm-lock.yaml</files>
  <read_first>
    - frontend/package.json (current dependencies)
    - .planning/phases/35-typography-stack/35-RESEARCH.md (§Standard Stack — version matrix)
    - .planning/phases/35-typography-stack/35-PATTERNS.md (§Shared Patterns — Package Matrix)
    - CLAUDE.md (§Commands — `pnpm -C frontend add` convention)
  </read_first>
  <action>
    Install the 8 fontsource packages per D-03 using `pnpm -C frontend add` (NEVER at repo root — per CLAUDE.md §Commands). Run this exact command:

    ```bash
    cd frontend && pnpm add \
      @fontsource-variable/inter@^5.2.8 \
      @fontsource-variable/public-sans@^5.2.7 \
      @fontsource-variable/space-grotesk@^5.2.10 \
      @fontsource-variable/fraunces@^5.2.9 \
      @fontsource-variable/jetbrains-mono@^5.2.8 \
      @fontsource/ibm-plex-sans@^5.2.8 \
      @fontsource/ibm-plex-mono@^5.2.7 \
      @fontsource/tajawal@^5.2.7
    ```

    Then verify each sub-path file exists in `node_modules`:

    ```bash
    for f in \
      'node_modules/@fontsource-variable/inter/wght.css' \
      'node_modules/@fontsource-variable/public-sans/wght.css' \
      'node_modules/@fontsource-variable/space-grotesk/wght.css' \
      'node_modules/@fontsource-variable/fraunces/wght.css' \
      'node_modules/@fontsource-variable/jetbrains-mono/wght.css' \
      'node_modules/@fontsource/ibm-plex-sans/400.css' \
      'node_modules/@fontsource/ibm-plex-sans/500.css' \
      'node_modules/@fontsource/ibm-plex-sans/600.css' \
      'node_modules/@fontsource/ibm-plex-sans/700.css' \
      'node_modules/@fontsource/ibm-plex-mono/400.css' \
      'node_modules/@fontsource/ibm-plex-mono/500.css' \
      'node_modules/@fontsource/tajawal/400.css' \
      'node_modules/@fontsource/tajawal/500.css' \
      'node_modules/@fontsource/tajawal/700.css'; do
      test -f "frontend/$f" && echo "OK $f" || echo "MISSING $f"
    done
    ```

    Every line MUST print `OK` (14 files). If any `MISSING`, halt and report to planner — version pin may be stale.

  </action>
  <verify>
    <automated>grep -c '"@fontsource-variable/inter":' frontend/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@fontsource-variable/inter" frontend/package.json` returns `1`
    - `grep -c "@fontsource-variable/public-sans" frontend/package.json` returns `1`
    - `grep -c "@fontsource-variable/space-grotesk" frontend/package.json` returns `1`
    - `grep -c "@fontsource-variable/fraunces" frontend/package.json` returns `1`
    - `grep -c "@fontsource-variable/jetbrains-mono" frontend/package.json` returns `1`
    - `grep -c "@fontsource/ibm-plex-sans" frontend/package.json` returns `1`
    - `grep -c "@fontsource/ibm-plex-mono" frontend/package.json` returns `1`
    - `grep -c "@fontsource/tajawal" frontend/package.json` returns `1`
    - `test -f frontend/node_modules/@fontsource-variable/fraunces/wght.css` exit code `0`
    - `test -f frontend/node_modules/@fontsource/tajawal/400.css` exit code `0`
    - `pnpm -C frontend install --frozen-lockfile` exit code `0` (lockfile in sync)
  </acceptance_criteria>
  <done>8 dependencies present in `package.json`; 14 verified CSS sub-paths under `node_modules`; `pnpm-lock.yaml` regenerated.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke-test Assumption A1 — `@theme` self-reference for font tokens</name>
  <files>frontend/src/index.css</files>
  <read_first>
    - frontend/src/index.css (lines 35-115 — existing @theme block)
    - .planning/phases/35-typography-stack/35-RESEARCH.md (§Common Pitfalls — Pitfall 1; §Architecture Pattern 3; §Assumptions Log A1)
    - .planning/phases/35-typography-stack/35-PATTERNS.md (§Shared — Phase 33-06 `@theme` self-reference crash avoidance)
    - .planning/STATE.md (§Wave 2 commits — `e5fcacec fix(33-06): inline shadow literals in @theme to avoid self-reference`)
  </read_first>
  <action>
    Append three probe lines inside the existing `@theme { ... }` block in `frontend/src/index.css` (block opens at line 35). Add exactly this text as the last lines inside `@theme` (BEFORE the closing `}`):

    ```css
      /* Phase 35 Wave-0 smoke probe — A1: same-name @theme self-reference for fonts.
         If this crashes `pnpm -C frontend dev` with "... is not a function" (see Phase 33-06
         commit e5fcacec), revert to distinct names (`--tw-font-display: var(--font-display);`)
         OR inline literal fallbacks. Plan 35-03 replaces these three lines with the real
         decision. Do NOT delete this probe without running `pnpm -C frontend dev` first. */
      --font-display: var(--font-display);
      --font-body: var(--font-body);
      --font-mono: var(--font-mono);
    ```

    Then run the probe:

    ```bash
    cd frontend && pnpm dev &
    DEV_PID=$!
    sleep 12
    # Hit the dev server on the CSS URL that Phase 33-09 uses for SC verification
    curl -sf -o /dev/null -w '%{http_code}' http://localhost:5173/src/index.css > /tmp/phase35-a1-status.txt || echo 000 > /tmp/phase35-a1-status.txt
    kill $DEV_PID 2>/dev/null
    wait $DEV_PID 2>/dev/null
    STATUS=$(cat /tmp/phase35-a1-status.txt)
    echo "A1 probe HTTP status: $STATUS"
    test "$STATUS" = "200"
    ```

    If exit code is 0 (HTTP 200): A1 is SAFE — leave the three probe lines in place; Plan 35-03 will convert them to the final form. Record verdict `SAFE` in `35-01-SUMMARY.md`.

    If exit code is non-zero (HTTP 500 or connection refused): A1 is UNSAFE — revert by replacing the three probe lines with literal fallbacks (matches Phase 33-06 shadow workaround):

    ```css
      /* A1 UNSAFE — literal fallbacks on LHS; runtime override via applyTokens(). */
      --font-display: 'Inter', system-ui, sans-serif;
      --font-body: 'Inter', system-ui, sans-serif;
      --font-mono: ui-monospace, SFMono-Regular, Menlo, monospace;
    ```

    Record verdict `UNSAFE — literal-fallback fallback applied` in `35-01-SUMMARY.md`. Plan 35-03 will reference this verdict.

  </action>
  <verify>
    <automated>cd frontend && pnpm dev &amp; DEV_PID=$!; sleep 12; curl -sf -o /dev/null -w '%{http_code}' http://localhost:5173/src/index.css; kill $DEV_PID 2>/dev/null; wait 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 35 Wave-0 smoke probe" frontend/src/index.css` returns `1` OR `grep -c "A1 UNSAFE — literal-fallback fallback applied" frontend/src/index.css` returns `1`
    - `grep -c "^  --font-display:" frontend/src/index.css` returns at least `1` (inside @theme)
    - `grep -c "^  --font-body:" frontend/src/index.css` returns at least `1`
    - `grep -c "^  --font-mono:" frontend/src/index.css` returns at least `1`
    - Dev server probe (`curl http://localhost:5173/src/index.css`) returns HTTP 200 OR the fallback literal pattern is applied (file contains `'Inter', system-ui, sans-serif` on a `--font-body:` line inside `@theme`)
    - `.planning/phases/35-typography-stack/35-01-SUMMARY.md` contains `A1 verdict:` followed by either `SAFE` or `UNSAFE`
  </acceptance_criteria>
  <done>A1 verdict recorded; @theme block extended with 3 font vars (either self-ref or literal-fallback form); dev server boots without generator crash.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold 3 Wave-0 test files + TYPO-04 fixture HTML (RED state)</name>
  <files>frontend/tests/unit/design-system/fonts.test.ts, frontend/tests/unit/design-system/tajawal-cascade.test.ts, frontend/tests/e2e/typography.spec.ts, frontend/tests/e2e/fixtures/typo-04-fixture.html</files>
  <read_first>
    - frontend/tests/unit/design-system/fouc-bootstrap.test.ts (canonical drift-guard pattern — readFileSync + regex + it.each)
    - frontend/tests/e2e/rtl-switching.spec.ts (auth + seedLocale helpers — lines 1-20)
    - frontend/tests/e2e/ai-degradation.spec.ts (network interception pattern — lines 30-49)
    - .planning/phases/35-typography-stack/35-RESEARCH.md (§Validation Architecture — lines 548-617 — fixture + test map)
    - .planning/phases/35-typography-stack/35-PATTERNS.md (§`frontend/tests/unit/design-system/fonts.test.ts` + §`frontend/tests/e2e/typography.spec.ts`)
  </read_first>
  <action>
    Create four files. All four SHOULD fail their assertions at end-of-Wave-0 (RED state) because downstream code does not exist yet. Plans 35-02 through 35-05 turn them GREEN.

    **File 1: `frontend/tests/unit/design-system/fonts.test.ts`** — drift guard asserting 8 deps + 14 import literals. Write exactly:

    ```ts
    import { readFileSync } from 'node:fs'
    import { resolve } from 'node:path'
    import { describe, expect, it } from 'vitest'

    const FRONTEND_ROOT = resolve(__dirname, '../../..')
    const PACKAGE_JSON = resolve(FRONTEND_ROOT, 'package.json')
    const FONTS_TS = resolve(FRONTEND_ROOT, 'src/fonts.ts')

    const REQUIRED_DEPS = [
      '@fontsource-variable/inter',
      '@fontsource-variable/public-sans',
      '@fontsource-variable/space-grotesk',
      '@fontsource-variable/fraunces',
      '@fontsource-variable/jetbrains-mono',
      '@fontsource/ibm-plex-sans',
      '@fontsource/ibm-plex-mono',
      '@fontsource/tajawal',
    ] as const

    const REQUIRED_IMPORTS = [
      "@fontsource-variable/inter/wght.css",
      "@fontsource-variable/public-sans/wght.css",
      "@fontsource-variable/space-grotesk/wght.css",
      "@fontsource-variable/fraunces/wght.css",
      "@fontsource-variable/jetbrains-mono/wght.css",
      "@fontsource/ibm-plex-sans/400.css",
      "@fontsource/ibm-plex-sans/500.css",
      "@fontsource/ibm-plex-sans/600.css",
      "@fontsource/ibm-plex-sans/700.css",
      "@fontsource/ibm-plex-mono/400.css",
      "@fontsource/ibm-plex-mono/500.css",
      "@fontsource/tajawal/400.css",
      "@fontsource/tajawal/500.css",
      "@fontsource/tajawal/700.css",
    ] as const

    describe('Phase 35 — fontsource dependency drift guard (TYPO-02)', () => {
      const pkg = JSON.parse(readFileSync(PACKAGE_JSON, 'utf8')) as {
        dependencies: Record<string, string>
      }

      it.each(REQUIRED_DEPS)('package.json declares %s with ^5.x pin', (name) => {
        const version = pkg.dependencies[name]
        expect(version, `missing dependency: ${name}`).toBeDefined()
        expect(version).toMatch(/^\^5\./)
      })

      it('fonts.ts exists and is non-empty', () => {
        const src = readFileSync(FONTS_TS, 'utf8')
        expect(src.length).toBeGreaterThan(100)
      })

      it.each(REQUIRED_IMPORTS)('fonts.ts imports %s', (path) => {
        const src = readFileSync(FONTS_TS, 'utf8')
        expect(src).toContain(`'${path}'`)
      })
    })
    ```

    **File 2: `frontend/tests/unit/design-system/tajawal-cascade.test.ts`** — drift guard asserting 48-line cascade present. Write exactly:

    ```ts
    import { readFileSync, existsSync } from 'node:fs'
    import { resolve } from 'node:path'
    import { describe, expect, it } from 'vitest'

    const FRONTEND_ROOT = resolve(__dirname, '../../..')
    const INDEX_CSS = resolve(FRONTEND_ROOT, 'src/index.css')
    const HANDOFF_CSS = '/tmp/inteldossier-handoff/inteldossier/project/src/app.css'

    describe('Phase 35 — Tajawal RTL cascade drift guard (TYPO-03)', () => {
      it('index.css contains the verbatim Tajawal cascade header', () => {
        const css = readFileSync(INDEX_CSS, 'utf8')
        expect(css).toContain('/* ============ Arabic typography override: Tajawal ============ */')
      })

      it('index.css contains the [style*="--font-display"] defeat rule (TYPO-03)', () => {
        const css = readFileSync(INDEX_CSS, 'utf8')
        expect(css).toMatch(/html\[dir='rtl'\]\s*\[style\*='--font-display'\]\s*\{[^}]*'Tajawal'[^}]*!important/)
      })

      it('index.css contains the [dir="ltr"].mono JetBrains Mono carve-out (TYPO-04)', () => {
        const css = readFileSync(INDEX_CSS, 'utf8')
        expect(css).toMatch(/html\[dir='rtl'\]\s*\[dir='ltr'\]\.mono[\s\S]*?'JetBrains Mono'/)
      })

      it('index.css contains the !important chip/label/tag Tajawal override block', () => {
        const css = readFileSync(INDEX_CSS, 'utf8')
        expect(css).toMatch(/html\[dir='rtl'\]\s*\.chip[\s\S]*?'Tajawal'[\s\S]*?!important/)
      })

      it('index.css block matches handoff source byte-for-byte (when handoff available)', () => {
        if (!existsSync(HANDOFF_CSS)) {
          console.warn('handoff app.css not mounted — skipping byte-for-byte check')
          return
        }
        const handoff = readFileSync(HANDOFF_CSS, 'utf8')
        // Extract lines 129-176 (inclusive) from handoff source.
        const handoffLines = handoff.split('\n').slice(128, 176).join('\n').trim()
        const css = readFileSync(INDEX_CSS, 'utf8')
        expect(css).toContain(handoffLines)
      })
    })
    ```

    **File 3: `frontend/tests/e2e/typography.spec.ts`** — Playwright spec covering TYPO-01..04. Write exactly:

    ```ts
    import { test, expect, type Page } from '@playwright/test'

    type Direction = 'chancery' | 'situation' | 'ministerial' | 'bureau'
    type Locale = 'en' | 'ar'

    async function authBypass(page: Page): Promise<void> {
      await page.addInitScript(() => {
        const payload = {
          state: {
            user: { id: 'test-user', email: 'test@example.com', name: 'Test' },
            isAuthenticated: true,
          },
          version: 0,
        }
        localStorage.setItem('auth-storage', JSON.stringify(payload))
      })
    }

    async function seedDirection(page: Page, dir: Direction): Promise<void> {
      await page.addInitScript((d: Direction): void => {
        localStorage.setItem('id.dir', d)
      }, dir)
    }

    async function seedLocale(page: Page, loc: Locale): Promise<void> {
      await page.addInitScript((l: Locale): void => {
        localStorage.setItem('id.locale', l)
      }, loc)
    }

    const EXPECTED_DISPLAY: Record<Direction, RegExp> = {
      chancery: /^"?Fraunces"?/,
      situation: /^"?Space Grotesk"?/,
      ministerial: /^"?Public Sans"?/,
      bureau: /^"?Inter"?/,
    }

    test.describe('Phase 35 — Typography E2E (TYPO-01..04)', () => {
      test('TYPO-02 — zero requests to fonts.googleapis.com or fonts.gstatic.com', async ({ page }) => {
        await authBypass(page)
        await seedLocale(page, 'en')
        await seedDirection(page, 'chancery')
        const requests: string[] = []
        page.on('request', (req) => requests.push(req.url()))
        await page.goto('/')
        await page.waitForLoadState('networkidle')
        const leaks = requests.filter((u) => /fonts\.(googleapis|gstatic)\.com/.test(u))
        expect(leaks, `google fonts leak: ${leaks.join(', ')}`).toEqual([])
      })

      for (const dir of ['chancery', 'situation', 'ministerial', 'bureau'] as const) {
        test(`TYPO-01 — ${dir}: getComputedStyle(h1).fontFamily matches expected display font`, async ({ page }) => {
          await authBypass(page)
          await seedLocale(page, 'en')
          await seedDirection(page, dir)
          await page.goto('/')
          await page.waitForLoadState('networkidle')
          const fontFamily = await page.evaluate(() => {
            const el = document.querySelector('h1, [data-testid="page-title"]')
            return el ? window.getComputedStyle(el).fontFamily : ''
          })
          expect(fontFamily).toMatch(EXPECTED_DISPLAY[dir])
        })
      }

      test('TYPO-03 — html[dir="rtl"] body computes to Tajawal-first cascade', async ({ page }) => {
        await authBypass(page)
        await seedLocale(page, 'ar')
        await seedDirection(page, 'chancery')
        await page.goto('/')
        await page.waitForLoadState('networkidle')
        const bodyFamily = await page.evaluate(() => window.getComputedStyle(document.body).fontFamily)
        expect(bodyFamily).toMatch(/^"?Tajawal"?/)
      })

      test('TYPO-04 — [dir="ltr"].mono inside RTL fixture renders JetBrains Mono', async ({ page }) => {
        await page.goto('/tests/e2e/fixtures/typo-04-fixture.html')
        const probeFamily = await page.evaluate(() => {
          const el = document.querySelector('[data-testid="typo04-probe"]')
          return el ? window.getComputedStyle(el).fontFamily : ''
        })
        const kbdFamily = await page.evaluate(() => {
          const el = document.querySelector('[data-testid="typo04-kbd"]')
          return el ? window.getComputedStyle(el).fontFamily : ''
        })
        expect(probeFamily).toMatch(/^"?JetBrains Mono"?/)
        expect(kbdFamily).toMatch(/^"?JetBrains Mono"?/)
      })
    })
    ```

    **File 4: `frontend/tests/e2e/fixtures/typo-04-fixture.html`** — minimal static fixture. Write exactly:

    ```html
    <!doctype html>
    <html dir="rtl" lang="ar">
      <head>
        <meta charset="UTF-8" />
        <title>TYPO-04 fixture</title>
        <link rel="stylesheet" href="/src/index.css" />
      </head>
      <body>
        <main>
          <p>هذا نص عربي</p>
          <span class="mono" dir="ltr" data-testid="typo04-probe">⌘K</span>
          <kbd dir="ltr" data-testid="typo04-kbd">T−3</kbd>
        </main>
      </body>
    </html>
    ```

    Create parent directories as needed (`frontend/tests/e2e/fixtures/`). Do NOT add any test runner skip markers — tests are expected to fail (RED) at end of Wave 0; Plans 35-02 through 35-05 flip them GREEN.

  </action>
  <verify>
    <automated>test -f frontend/tests/unit/design-system/fonts.test.ts && test -f frontend/tests/unit/design-system/tajawal-cascade.test.ts && test -f frontend/tests/e2e/typography.spec.ts && test -f frontend/tests/e2e/fixtures/typo-04-fixture.html</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/tests/unit/design-system/fonts.test.ts` exit 0
    - `test -f frontend/tests/unit/design-system/tajawal-cascade.test.ts` exit 0
    - `test -f frontend/tests/e2e/typography.spec.ts` exit 0
    - `test -f frontend/tests/e2e/fixtures/typo-04-fixture.html` exit 0
    - `grep -c "REQUIRED_DEPS" frontend/tests/unit/design-system/fonts.test.ts` returns at least `1`
    - `grep -c "Tajawal RTL cascade drift guard" frontend/tests/unit/design-system/tajawal-cascade.test.ts` returns `1`
    - `grep -c "EXPECTED_DISPLAY" frontend/tests/e2e/typography.spec.ts` returns at least `1`
    - `grep -c "data-testid=\"typo04-probe\"" frontend/tests/e2e/fixtures/typo-04-fixture.html` returns `1`
    - `grep -c "data-testid=\"typo04-kbd\"" frontend/tests/e2e/fixtures/typo-04-fixture.html` returns `1`
    - `pnpm -C frontend exec vitest --run tests/unit/design-system/fonts.test.ts 2>&1 | grep -E "FAIL|PASS"` shows FAIL for at least the `fonts.ts imports` row (RED state expected — Plan 35-02 creates `fonts.ts`)
  </acceptance_criteria>
  <done>4 Wave-0 files exist; scaffolds compile in TypeScript; tests are RED pending downstream waves.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                 |
| ----------------------------- | ----------------------------------------------------------- |
| npm registry → `pnpm install` | Third-party tarballs downloaded and executed during install |
| Dev server → browser          | `pnpm dev` serves index.css; crashes reveal generator state |

## STRIDE Threat Register

| Threat ID | Category          | Component                      | Disposition | Mitigation Plan                                                                                                                                                                                                                                                        |
| --------- | ----------------- | ------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-35-01   | Tampering         | `@fontsource*` npm packages    | mitigate    | `pnpm install` pins SHA-512 integrity hash per package in `pnpm-lock.yaml`; version pin (`^5.2.x`) blocks major-version tampering; Task 1 acceptance asserts `--frozen-lockfile` install succeeds.                                                                     |
| T-35-04   | Denial of Service | `@theme` Tailwind v4 generator | mitigate    | Task 2 smoke-probe catches the Phase 33-06 self-reference crash before downstream waves consume it. Verdict recorded in SUMMARY; fallback to literal-on-LHS pattern matches the shadow workaround (`e5fcacec`). Severity LOW-MEDIUM — contained to one `@theme` entry. |

</threat_model>

<verification>
- `pnpm -C frontend install --frozen-lockfile` exits 0
- `pnpm -C frontend dev` boots; `curl http://localhost:5173/src/index.css` returns 200 (A1 SAFE) OR file reverted to literal fallbacks (A1 UNSAFE)
- Three Wave 0 test files + one fixture HTML exist; TypeScript compiles; tests are appropriately RED
- `.planning/phases/35-typography-stack/35-01-SUMMARY.md` records the A1 verdict and the installed package versions
</verification>

<success_criteria>

- [ ] 8 `@fontsource[-variable]/*` packages present in `frontend/package.json` with `^5.x` pins
- [ ] 14 expected CSS sub-path files present under `frontend/node_modules/`
- [ ] A1 verdict (SAFE or UNSAFE) recorded in `35-01-SUMMARY.md`
- [ ] `frontend/src/index.css` `@theme` block contains 3 `--font-*` entries (self-ref or literal-fallback form)
- [ ] 4 Wave-0 files exist and are syntactically valid TypeScript/HTML
- [ ] Threat T-35-01 + T-35-04 mitigations in place
      </success_criteria>

<output>
After completion, create `.planning/phases/35-typography-stack/35-01-SUMMARY.md` capturing:
- Installed versions (8 rows)
- A1 verdict (SAFE | UNSAFE)
- Final `@theme` font-var form chosen (self-ref | literal-fallback | distinct-names)
- RED-state expectations for the 4 Wave-0 artifacts
</output>
