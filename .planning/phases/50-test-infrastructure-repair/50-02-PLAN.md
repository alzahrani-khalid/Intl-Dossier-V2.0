---
phase: 50-test-infrastructure-repair
plan: 02
type: execute
wave: 2
depends_on:
  - 50-01
files_modified:
  - frontend/src/components/dossier/wizard/hooks/useCountryAutoFill.ts
  - frontend/src/components/dossier/wizard/hooks/__tests__/useCountryAutoFill.test.ts
  - frontend/tests/unit/design-system/fouc-bootstrap.test.ts
  - frontend/src/pages/Dashboard/widgets/__tests__/KpiStrip.test.tsx
  - frontend/tests/unit/components/ui/heroui-wrappers.test.tsx
  - frontend/src/routes/_protected/dossiers/forums/__tests__/ForumsListPage.test.tsx
  - frontend/src/lib/__tests__/api-client.test.ts
autonomous: true
requirements:
  - TEST-02
user_setup: []

must_haves:
  truths:
    - '`pnpm --filter intake-frontend test --run` exits 0 — per D-01, FE exit-0 is non-negotiable; no `queued-with-rationale` disposition is acceptable for the FE default runner at phase exit.'
    - "`useCountryAutoFill` correctly auto-fills `iso_code_2`, `iso_code_3`, `region` (post-`REGION_MAP` mapping to `'asia'`), and `capital_en` from the REST Countries v3.1 contract (`cca2`, `cca3`, `region`, `capital`). The impl rejects `undefined` reference-data fields without writing to the form."
    - "Every residual failing FE test after Plan 50-01 is either (a) fixed green in this plan, or (b) classified `integration-only` and moved to the integration runner via Plan 50-03's split (cross-plan handoff). No FE default-runner test exits this plan in `queued-with-rationale` state."
    - 'Per D-10, each failing-test fix is preceded by `git log -p` archaeology on both the asserting line AND the impl under test, and the test name (contract) prevails unless explicitly noted.'
  artifacts:
    - path: 'frontend/src/components/dossier/wizard/hooks/useCountryAutoFill.ts'
      provides: 'Hook with `match.cca2 != null` / `match.cca3 != null` / `match.region != null` defensive null-guards (D-09 product hardening)'
      contains: 'match.cca2 != null'
    - path: 'frontend/src/components/dossier/wizard/hooks/__tests__/useCountryAutoFill.test.ts'
      provides: "Test using REST Countries v3.1 fixture shape (`cca2`, `cca3`, `region`, `capital`) + assertion `region: 'asia'` post-REGION_MAP"
      contains: "cca2: 'SA'"
  key_links:
    - from: 'frontend/src/components/dossier/wizard/hooks/__tests__/useCountryAutoFill.test.ts'
      to: 'frontend/src/components/dossier/wizard/hooks/useCountryAutoFill.ts'
      via: 'Test mocks `useQuery` with the REST Countries shape the impl actually reads — fields `cca2`/`cca3`/`region`/`capital` (NOT `code`/`code3`)'
      pattern: "cca2.*'SA'"
    - from: 'Final FE test runner output'
      to: 'TEST-02 phase requirement'
      via: "`pnpm --filter intake-frontend test --run` exits 0 → FE green proof for TEST-02 (extension per VALIDATION.md row 'Full FE green')"
      pattern: 'Test Files.*passed.*0 failed'
---

<objective>
Finish the frontend full-green sweep that Plan 50-01 started. Plan 50-01 cleared 175 (config) + 20 (factory) + 11 (dead-import) = 206 failures via three cascade fixes. This plan handles the residual ~5-15 per-file product/test regressions surfaced once the cascades clear, plus the 2 known `useCountryAutoFill` regressions (1 test-side mock-shape fix + 1 product null-guard hardening).

Per CONTEXT §D-09/D-10 and PATTERNS §useCountryAutoFill, the verdict on `useCountryAutoFill` is **TEST IS WRONG, IMPL IS (mostly) RIGHT** — the test in commit `7943ad20` (2026-04-15) has been failing since first commit because it mocks `code`/`code3` (wrong) instead of REST Countries `cca2`/`cca3` (correct per impl line 7). The impl additionally needs a defensive `match.cca2 != null` guard so future drift can't slip `undefined` to the form.

The other 4 files (fouc-bootstrap, KpiStrip, heroui-wrappers, ForumsListPage, api-client) are listed in RESEARCH §Other product-class failures with likely diagnoses but require per-file `git log -p` triage per D-10 before fixing.

**Exit-0 is non-negotiable per D-01.** Every residual failing FE test at the start of this plan MUST be either (a) fixed green here, or (b) classified `integration-only` and moved into Plan 50-03's integration split via cross-plan handoff (file is removed from the default FE runner glob and added to the integration runner's include). The `queued-with-rationale` disposition that was acceptable in earlier drafts is NOT acceptable for the FE default runner — the phase-exit contract is `pnpm --filter intake-frontend test --run` exits 0.

Purpose: After this plan lands, `pnpm --filter intake-frontend test --run` exits 0 → TEST-02 (extension per VALIDATION) is SATISFIED → frontend test gate can be registered in Plan 50-05 without risking immediate breakage.

Output:

- `frontend/src/components/dossier/wizard/hooks/useCountryAutoFill.ts` with 3 added `!= null` guards
- `frontend/src/components/dossier/wizard/hooks/__tests__/useCountryAutoFill.test.ts` with corrected mock shape + assertion
- All other residual FE failing tests (~5-15 files; bounded — see Task 2 `<files>`) either fixed green OR moved to integration runner per cross-plan handoff with Plan 50-03
- A handoff entry in this plan's SUMMARY listing any files moved to integration (so Plan 50-03 / 50-04 can absorb)
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-test-infrastructure-repair/50-CONTEXT.md
@.planning/phases/50-test-infrastructure-repair/50-RESEARCH.md
@.planning/phases/50-test-infrastructure-repair/50-PATTERNS.md
@.planning/phases/50-test-infrastructure-repair/50-VALIDATION.md
@.planning/phases/50-test-infrastructure-repair/50-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. -->

From `frontend/src/components/dossier/wizard/hooks/useCountryAutoFill.ts` (CURRENT impl — 72 lines):

- `RestCountryResult` interface (lines 6-11): `cca2: string`, `cca3: string`, `region: string`, `capital?: string[]` — non-optional except `capital`
- `REGION_MAP` (lines 14-21): `{ Asia: 'asia', Africa: 'africa', Europe: 'europe', Americas: 'americas', Oceania: 'oceania', Antarctic: 'antarctic' }`
- `fetchCountryReference` (lines 23-33): fetches `https://restcountries.com/v3.1/name/${name}?fields=cca2,cca3,region,capital`, returns first array element
- `useCountryAutoFill` hook (lines 39-71): uses `useQuery`; in the `useEffect` reads `match.cca2`, `match.cca3`, `match.region` (mapped via REGION_MAP), `match.capital[0]`
- Lines 55-66: 4 fill-when-empty blocks. Lines 55, 58, 61 use `match.cca2 !== ''` / `match.cca3 !== ''` / `match.region !== ''` (only empty-string guard — `undefined` passes through). Line 67 ALREADY uses `match.capital != null` — copy this idiom.

From `frontend/src/components/dossier/wizard/hooks/__tests__/useCountryAutoFill.test.ts` (CURRENT test — 110 lines):

- Lines 60-66 (first `it` block fixture — WRONG): `const mockMatch = { code: 'SA', code3: 'SAU', name_en: ..., name_ar: ..., region: 'Asia' }`
- Lines 83-89 (second `it` block fixture — WRONG, same shape)
- Line 77: `expect(form.setValue).toHaveBeenCalledWith('iso_code_2', 'SA')` — CURRENTLY fails with "Received: undefined" because impl reads `match.cca2`, mock provides `code`
- Line 78: `expect(form.setValue).toHaveBeenCalledWith('iso_code_3', 'SAU')` — same
- Line 79: `expect(form.setValue).toHaveBeenCalledWith('region', 'Asia')` — WRONG ASSERTION; impl maps `'Asia'` → `'asia'` via REGION_MAP, so correct assertion is `'asia'`
- Line 106: `expect(form.setValue).not.toHaveBeenCalledWith('region', 'Asia')` — must update to `'asia'`
- vi.mock('@tanstack/react-query', ...) line 7-9: keep verbatim; the `vi.mocked(useQuery).mockReturnValue(...)` per-test override pattern (lines 68, 91) is correct, don't touch

From RESEARCH §Other product-class failures (the 5 representative residual files — hot-list below; remainder discovered post-50-01):

- `frontend/tests/unit/design-system/fouc-bootstrap.test.ts` — 9 failures: "Drift guard literal-match against `bootstrap.js` palette (Phase 33/34 baseline) — confirm if intended or stale"
- `frontend/src/pages/Dashboard/widgets/__tests__/KpiStrip.test.tsx` — 2 failures: "Dashboard widget assertion drift (Phase 38 baseline)"
- `frontend/tests/unit/components/ui/heroui-wrappers.test.tsx` — 2 failures: "HeroUI v3 wrapper test drift (Phase 33/35 baseline)"
- `frontend/src/routes/_protected/dossiers/forums/__tests__/ForumsListPage.test.tsx` — 4 failures: "`waitFor('Forums')` timeout — likely route auth wrapper issue"
- `frontend/src/lib/__tests__/api-client.test.ts` — 6 failures: "All 'MSW intercepted request without handler' — env URL is `undefined/functions/v1/...` because no Supabase env set in tests"

The exact residual file set is discovered at Task 1 + Task 2 start by reading Plan 50-01's SUMMARY and re-running the FE test runner.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix useCountryAutoFill regression — test mock shape + impl null-guards (D-09 + D-10)</name>
  <files>frontend/src/components/dossier/wizard/hooks/useCountryAutoFill.ts, frontend/src/components/dossier/wizard/hooks/__tests__/useCountryAutoFill.test.ts</files>
  <read_first>
    - frontend/src/components/dossier/wizard/hooks/useCountryAutoFill.ts (full file — current 72-line impl reading `match.cca2`, `match.cca3`, `match.region`, `match.capital`)
    - frontend/src/components/dossier/wizard/hooks/__tests__/useCountryAutoFill.test.ts (full file — current 110-line test with wrong `code`/`code3` mock and wrong `'Asia'` assertion)
    - .planning/phases/50-test-infrastructure-repair/50-RESEARCH.md §Wizard Regression Triage (lines 794-829 — git archaeology: TEST IS WRONG since commit 7943ad20 on 2026-04-15)
    - .planning/phases/50-test-infrastructure-repair/50-PATTERNS.md §`useCountryAutoFill.ts` and §`useCountryAutoFill.test.ts` (target shapes verbatim)
    - .planning/phases/50-test-infrastructure-repair/50-CONTEXT.md D-09 (root-cause fixes allowed), D-10 (test name = contract spec)
  </read_first>
  <action>
    Two-file fix per D-10 archaeology verdict in RESEARCH (which already did the `git log -p` work):

    **A) Fix the test (`useCountryAutoFill.test.ts`) — primary fix per "TEST IS WRONG" verdict:**
    - In the first `it('auto-fills empty form fields when reference match found', ...)` block (around lines 59-80), change the `mockMatch` literal at lines 60-66 from `{ code: 'SA', code3: 'SAU', name_en: ..., name_ar: ..., region: 'Asia' }` to the REST Countries v3.1 contract `{ cca2: 'SA', cca3: 'SAU', region: 'Asia', capital: ['Riyadh'] }`. Drop `name_en` / `name_ar` — they are not on the `RestCountryResult` interface (lines 6-11 of impl) and never were read.
    - Change the line-79 assertion from `expect(form.setValue).toHaveBeenCalledWith('region', 'Asia')` to `expect(form.setValue).toHaveBeenCalledWith('region', 'asia')` — the impl maps `'Asia'` → `'asia'` via `REGION_MAP` at line 62.
    - Add a new assertion immediately after line 79: `expect(form.setValue).toHaveBeenCalledWith('capital_en', 'Riyadh')` — the impl line 68 fills `capital_en` from `match.capital[0]`, so the test should cover that contract now that the fixture provides it.
    - In the second `it('does not overwrite user-entered values', ...)` block (around lines 82-109), apply the SAME mockMatch shape fix at lines 83-89.
    - Update line 106 `expect(form.setValue).not.toHaveBeenCalledWith('region', 'Asia')` to `expect(form.setValue).not.toHaveBeenCalledWith('region', 'asia')` (must match the post-REGION_MAP value).
    - Do NOT touch the `vi.mock('@tanstack/react-query', ...)` block at lines 7-9 or the `vi.mocked(useQuery).mockReturnValue(...)` per-test overrides at lines 68 and 91 — those are the correct vitest patterns.

    **B) Harden the impl (`useCountryAutoFill.ts`) — D-09 product hardening:**
    - At line 55, extend the guard `if (current.iso_code_2 === '' && match.cca2 !== '')` to `if (current.iso_code_2 === '' && match.cca2 != null && match.cca2 !== '')` — the `!= null` matches both `null` and `undefined`.
    - At line 58, same treatment for `iso_code_3` / `match.cca3`: add `match.cca3 != null && ` before `match.cca3 !== ''`.
    - At line 61, same treatment for `region` / `match.region`: add `match.region != null && ` before `match.region !== ''`.
    - Do NOT touch the `match.capital != null` guard at line 67 — it is the in-file precedent and already correct.
    - Do NOT widen the TypeScript interface `RestCountryResult` (lines 6-11) to make fields optional — that would cascade through callers. The null-guard is a runtime defense only, per PATTERNS §useCountryAutoFill.ts Gotchas.

    Run the test BEFORE and AFTER each diff. After A+B both land, both `it` blocks pass green.

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -c "cca2: 'SA'" src/components/dossier/wizard/hooks/__tests__/useCountryAutoFill.test.ts | grep -q "^1$" &amp;&amp; grep -c "match.cca2 != null" src/components/dossier/wizard/hooks/useCountryAutoFill.ts | grep -q "^1$" &amp;&amp; grep -c "match.cca3 != null" src/components/dossier/wizard/hooks/useCountryAutoFill.ts | grep -q "^1$" &amp;&amp; pnpm exec vitest --run src/components/dossier/wizard/hooks/__tests__/useCountryAutoFill.test.ts 2>&amp;1 | tail -3 | grep -E "Test Files.*passed" | grep -v "failed"</automated>
  </verify>
  <done>
    Both `it` blocks in `useCountryAutoFill.test.ts` pass green. `grep "cca2: 'SA'" useCountryAutoFill.test.ts` returns 2 (both fixtures fixed). `grep "match.cca2 != null" useCountryAutoFill.ts` returns 1 (and same for cca3, region). The `RestCountryResult` interface at lines 6-11 is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Triage and fix every residual FE failing test per D-10 (fix-green or split-to-integration; no queued-with-rationale)</name>
  <files>
    Pattern: any frontend test file failing after Plan 50-01 lands (residual ~5-15 files; named hot-list below; bounded by `find frontend -type f \( -name '*.test.ts' -o -name '*.test.tsx' \) \( -path 'frontend/**/__tests__/**' -o -path 'frontend/tests/**' \)`).
    Hot-list (must-fix; remainder discovered post-50-01):
    - frontend/src/components/dossier/wizard/hooks/useCountryAutoFill.ts
    - frontend/src/components/dossier/wizard/hooks/__tests__/useCountryAutoFill.test.ts
    - frontend/tests/unit/design-system/fouc-bootstrap.test.ts
    - frontend/src/pages/Dashboard/widgets/__tests__/KpiStrip.test.tsx
    - frontend/tests/unit/components/ui/heroui-wrappers.test.tsx
    - frontend/src/routes/_protected/dossiers/forums/__tests__/ForumsListPage.test.tsx
    - frontend/src/lib/__tests__/api-client.test.ts
    Upper bound: 15 files total. If post-50-01 discovery shows &gt;15 failing residuals, STOP and escalate — phase scope was misjudged and a phase-split decision belongs to the user, not to this plan.
  </files>
  <read_first>
    - .planning/phases/50-test-infrastructure-repair/50-01-SUMMARY.md (Plan 50-01 handoff — exact residual failing-file list at start of this plan)
    - .planning/phases/50-test-infrastructure-repair/50-RESEARCH.md §Other product-class failures (the 5 representative files + likely root cause for each)
    - .planning/phases/50-test-infrastructure-repair/50-CONTEXT.md D-01 (FE exit-0 = phase done; non-negotiable), D-09, D-10, D-11 (mixed assertion-text preserved — both i18n keys and English strings remain valid)
    - .planning/phases/50-test-infrastructure-repair/50-03-PLAN.md (Plan 50-03 integration split — `tests/contract/**`, `tests/contracts/**`, `tests/integration/**`, `tests/performance/**` are BE integration globs; for FE files that need to move, this plan creates an FE integration entry coordinated with 50-03 / 50-04)
    - frontend/tests/unit/design-system/fouc-bootstrap.test.ts (the file under triage — read full)
    - frontend/public/bootstrap.js (the consumer fouc-bootstrap.test.ts drift-guards against; check current palette/font literals)
    - frontend/src/pages/Dashboard/widgets/__tests__/KpiStrip.test.tsx (read full)
    - frontend/src/pages/Dashboard/widgets/KpiStrip.tsx (the impl KpiStrip.test.tsx tests against — current state)
    - frontend/tests/unit/components/ui/heroui-wrappers.test.tsx (read full)
    - frontend/src/components/ui/heroui-button.tsx, frontend/src/components/ui/heroui-card.tsx, frontend/src/components/ui/heroui-chip.tsx, frontend/src/components/ui/heroui-skeleton.tsx (impls under test)
    - frontend/src/routes/_protected/dossiers/forums/__tests__/ForumsListPage.test.tsx (read full)
    - frontend/src/routes/_protected/dossiers/forums/index.tsx OR the route file that renders ForumsListPage (read to understand the `waitFor('Forums')` timeout — is the heading text 'Forums' still rendered after the Phase 36 shell rework?)
    - frontend/src/lib/__tests__/api-client.test.ts (read full)
    - frontend/src/lib/api-client.ts (the impl under test — note how it builds URLs and which env vars it consumes)
  </read_first>
  <action>
    Apply the D-10 archaeology procedure to every residual failing FE test after Plan 50-01 lands. For each file, the disposition MUST be ONE of:

    - **(a) fixed-green:** Test passes in the default FE runner after this plan's edit.
    - **(b) integration-only → split:** Test is genuinely environment-dependent (needs real Supabase / real Express / real network). Move it out of the default FE runner glob by adding the file (or its directory) to `frontend/vitest.config.ts` `exclude` (creating an `exclude` array if absent), and add a coordinated handoff entry to this plan's SUMMARY so Plan 50-03 / 50-04 can absorb it into the integration runner story. Plan 50-04 audit records the move with `disposition: split-to-integration`.

    **`queued-with-rationale` is NOT acceptable for the FE default runner at phase exit per D-01.** Exit-0 is non-negotiable.

    Per-file procedure:

    1. Run `pnpm --filter intake-frontend test --run &lt;file&gt; 2>&amp;1 | tail -30` to capture the current failure signature.
    2. `git log --oneline -- &lt;file&gt;` to enumerate the test-file commit history.
    3. `git log --oneline -- &lt;impl-under-test&gt;` to enumerate the impl-side history.
    4. If the test was authored AFTER the relevant impl commit and asserts a behavior the impl never produced → **TEST IS WRONG** → update the test.
    5. If the test predates the impl AND the impl recently changed in a way that broke the test → **IMPL DRIFTED** → either revert the impl change or update the test (per D-10, test name = contract spec; deviating requires explicit rationale recorded in the commit message and in Plan 50-04 audit handoff).
    6. If the test cannot pass without infrastructure (env var, real Supabase, missing handler) → first attempt **rescue as unit**: add a missing MSW handler in `frontend/tests/mocks/handlers.ts` OR stub env via `vi.stubEnv(...)`. If rescue is genuinely not viable, then **(b) split-to-integration** — exclude from the default runner glob and hand off.
    7. If the test is genuinely dead (imports deleted modules, asserts behavior no longer in the codebase, etc.) → delete with rationale in the commit message. Plan 50-04 audit records as `disposition: deleted-dead`.

    Discovery upper-bound check: After reading Plan 50-01's SUMMARY at task start, count the residual failing files. If &gt;15, STOP and surface a phase-split recommendation to the operator (do not silently expand scope). 15 is the upper bound — the planner judged the phase size against this ceiling.

    **Per-file heuristic from RESEARCH (use as starting hypothesis, verify before fixing):**

    - `fouc-bootstrap.test.ts` (9 failures): Drift-guard test that pins `frontend/public/bootstrap.js` palette/font literals to `tokens/directions.ts`. RESEARCH §Phase 35-04 noted "1 known-defective plan-internal byte-for-byte test" — verify against current `bootstrap.js` and `tokens/directions.ts`. Likely fix: update the drift-guard's expected literal to match the post-Phase-35 byte-for-byte form, OR (if a sub-test is truly broken) mark the byte-for-byte sub-test `.skip` with a TODO referencing the substantive drift guards that DO pass; the file itself must remain green.
    - `KpiStrip.test.tsx` (2 failures): Dashboard widget assertion drift from Phase 38. Likely fix: update the test's expected text/props to match the current widget (Phase 38 PASS-WITH-DEVIATION noted multiple Rule-3 API adaptations including `Donut value+variants[3]`).
    - `heroui-wrappers.test.tsx` (2 failures): HeroUI v3 wrapper drift from Phase 33-05. Likely fix: update the wrapper assertion to match the current rewrite (`feat(33-05): rewrite heroui-button to use real @heroui/react Button primitive`).
    - `ForumsListPage.test.tsx` (4 failures): `waitFor('Forums')` timeout. Likely cause: route now lives behind `_protected.tsx` (Phase 36-05) and the test renders without the AppShell wrapper; or the heading text changed. Fix: either wrap the test render in the auth context provider OR update the `waitFor` selector to the new heading.
    - `api-client.test.ts` (6 failures, all "MSW intercepted request without handler"): Tests build URLs against `undefined/functions/v1/...` because the Supabase env vars are absent. Likely fix: add `vi.stubEnv('VITE_SUPABASE_URL', 'http://localhost:54321')` and `VITE_SUPABASE_ANON_KEY` stubs in a `beforeAll` inside the test, OR register matching MSW handlers in `frontend/tests/mocks/handlers.ts`. If still not viable as a unit test, split-to-integration per disposition (b).

    For each file, commit separately with a message of the form `fix(50-02): &lt;file&gt; — &lt;diagnosis verdict&gt;` (or `split(50-02): &lt;file&gt; → integration` for disposition-b moves). Per-file commits give reviewers per-file granularity.

    Final step: `pnpm --filter intake-frontend test --run` exits 0 (zero failing test files in the default runner). Any file moved to integration must be excluded from the default-runner glob via `frontend/vitest.config.ts` so it does not appear in the default-runner failure list.

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm exec vitest --run 2>&amp;1 | tail -8 | grep -E "Test Files.*passed" | grep -vE "[1-9]+[0-9]* failed" &amp;&amp; pnpm exec vitest --run 2>&amp;1 | tail -8 | grep -E "Tests.*passed" | grep -vE "[1-9]+[0-9]* failed" &amp;&amp; pnpm exec vitest --run 2>&amp;1 | tail -1 | grep -qE "exit code 0|^$"</automated>
  </verify>
  <done>
    `pnpm --filter intake-frontend test --run` exits 0 (D-01 non-negotiable). Every residual FE failing test is either (a) fixed green in this plan, or (b) classified `integration-only` and excluded from the default FE runner glob with a coordinated handoff entry for Plan 50-03 / 50-04. ZERO files exit this plan in `queued-with-rationale` state for the FE default runner. Each fixed/split file has its own commit per D-10 per-file granularity. Any files moved to integration are listed in this plan's SUMMARY for Plan 50-03 (runner config) and Plan 50-04 (audit) to absorb.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                                                                                        |
| --------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| Test fixture ↔ product impl | The `useCountryAutoFill` test mocks `useQuery` with attacker-controlled data; impl must defensively guard against malformed input. |
| Test runner ↔ env vars      | `api-client.test.ts` consumes `VITE_SUPABASE_URL` — stubbed env in tests must not leak into product runtime config.                |

## STRIDE Threat Register

| Threat ID  | Category                   | Component                                                                                                                           | Disposition | Mitigation Plan                                                                                                                                                               |
| ---------- | -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-50-02-01 | T (Tampering)              | `useCountryAutoFill` could write `undefined` to the country form if REST Countries returns malformed JSON.                          | mitigate    | Task 1 part B adds `match.cca2 != null && match.cca3 != null && match.region != null` runtime guards. The existing line 67 `match.capital != null` precedent is the template. |
| T-50-02-02 | I (Information disclosure) | `api-client.test.ts` stubbed env vars (`VITE_SUPABASE_URL`) could leak into production code paths if not properly scoped.           | mitigate    | `vi.stubEnv` is scoped to the test file's lifecycle; vitest restores env on file teardown. Use `vi.unstubAllEnvs()` in `afterEach` if added.                                  |
| T-50-02-03 | D (DoS via test hangs)     | `ForumsListPage.test.tsx` `waitFor('Forums')` 4-failure cluster could indicate a hang vector if root cause is unguarded route auth. | accept      | Test-runner only; not a production path. Task 2 triage either fixes the test render context or updates the selector.                                                          |

</threat_model>

<verification>
**Phase-level checks (post-plan):**

- `pnpm --filter intake-frontend test --run` exits 0 (full FE green per VALIDATION row "TEST-02 extension: Full FE green"). D-01 non-negotiable.
- `useCountryAutoFill.test.ts` has 2 occurrences of `cca2: 'SA'` and 2 occurrences of `cca3: 'SAU'` (both `it` blocks fixed).
- `useCountryAutoFill.ts` has `match.cca2 != null`, `match.cca3 != null`, `match.region != null` — three new null-guard occurrences.
- Per-file commits exist for each fixed/split regression (`git log --oneline frontend/src/components/dossier/wizard/hooks/useCountryAutoFill.ts` shows a 50-02 fix commit).
- `pnpm --filter intake-frontend lint` exits 0 (no new violations introduced by the changes).
- Zero FE default-runner files in `queued-with-rationale` state at phase exit. Any moved-to-integration files are listed in `50-02-SUMMARY.md` and excluded from the default runner glob.
  </verification>

<success_criteria>

- **TEST-02 fully SATISFIED:** Both the 4 wizard tests (cleared by Plan 50-01) and the 2 `useCountryAutoFill` regressions pass green; full `pnpm --filter intake-frontend test --run` exits 0.
- **D-01 satisfied:** FE exit-0 is achieved with no `queued-with-rationale` carve-outs in the default runner.
- **D-09 hardening:** `useCountryAutoFill.ts` has 3 new `!= null` guards.
- **D-10 archaeology applied:** Each residual triage file has a per-file diagnostic verdict recorded in its commit message.
- **No regressions in Plan 50-01's gains:** Cascade-cleared tests (G1+G2+G3 = 206 files) remain green.
- **Audit / integration handoff:** Any `split-to-integration` files listed in `50-02-SUMMARY.md` ready for Plan 50-03 (runner config) and Plan 50-04 (audit) to absorb.
  </success_criteria>

<output>
After completion, create `.planning/phases/50-test-infrastructure-repair/50-02-SUMMARY.md` with:
- Per-file disposition (fixed-green / split-to-integration / deleted-dead) — note the absence of any `queued-with-rationale` row for the FE default runner per D-01
- Git-archaeology evidence for each (test commit history + impl commit history summary)
- Final FE test runner output (`Test Files: N passed (N)`, exit code 0)
- List of any `split-to-integration` files + the cross-plan handoff items for Plan 50-03 / 50-04
</output>
