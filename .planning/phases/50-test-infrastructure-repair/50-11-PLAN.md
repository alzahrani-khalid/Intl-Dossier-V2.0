---
phase: 50-test-infrastructure-repair
plan: 11
type: execute
wave: 3
depends_on:
  - 50-01
files_modified:
  - frontend/tests/accessibility/entity-search.a11y.test.ts
  - frontend/tests/a11y/wcag-compliance.test.ts
  - frontend/tests/performance/validation-speed.test.ts
  - frontend/tests/accessibility/waiting-queue-a11y.test.tsx
  - frontend/vitest.config.ts
  - frontend/playwright.config.ts
autonomous: true
requirements:
  - TEST-02
user_setup: []

must_haves:
  truths:
    - "AT TASK START, the executor re-runs `pnpm --filter intake-frontend exec vitest --run --reporter=default tests/a11y tests/accessibility tests/performance 2>&1 | tail -20` and confirms the 4 files in this plan's `files_modified` are still failing in the shapes recorded below. If any auto-closed via 50-09 polyfill, document and shrink scope."
    - '`tests/accessibility/entity-search.a11y.test.ts` no longer surfaces `Playwright Test did not expect test.describe() to be called here` under vitest — disposition is one of: (a) renamed to `.spec.ts` so vitest excludes it per the Plan 50-01 vitest config (`exclude: [..., "**/*.spec.*"]`), (b) rewritten as a true vitest unit with `import { describe, it } from "vitest"`. Default disposition is rename (Karpathy §2 simplicity).'
    - '`tests/a11y/wcag-compliance.test.ts` and `tests/performance/validation-speed.test.ts` no longer produce `Transform failed` esbuild parse errors — per file the verdict is one of: (a) repair (single-line syntax fix), (b) delete (import target was deleted in Phase 33/34/36), (c) split-to-integration via `frontend/vitest.config.ts` `exclude` addition. `queued-with-rationale` is NOT acceptable for default-runner files (D-01).'
    - '`tests/accessibility/waiting-queue-a11y.test.tsx` 30 failing tests are diagnosed per D-10; assertions and impl are reconciled. The file passes `pnpm exec vitest --run <file>` green OR is split-to-integration with handoff to Plan 50-04 audit.'
    - 'No file in scope exits this plan in `queued-with-rationale` state for the default FE runner per D-01 non-negotiable amendment.'
    - 'When CC-5 disposition Path A (rename to `.spec.ts`) is chosen, `frontend/playwright.config.ts` is updated (testMatch or testDir) if and only if the existing globs do not pick up the renamed file; otherwise the config is left untouched.'
    - 'When CC-6 disposition split-to-integration is chosen for any file, `frontend/vitest.config.ts` `exclude` array gains a single entry per split file.'
  artifacts:
    - path: 'frontend/tests/accessibility/entity-search.a11y.spec.ts'
      provides: 'CC-5 Playwright misglobbed file renamed to .spec.ts so vitest excludes it'
      contains: 'test.describe'
  key_links:
    - from: 'frontend/tests/accessibility/entity-search.a11y.test.ts (renamed or rewritten)'
      to: 'frontend/vitest.config.ts exclude glob `**/*.spec.*` (Plan 50-01)'
      via: 'Renaming `.test.ts` → `.spec.ts` moves the file out of the include glob `**/*.test.{ts,tsx}` AND into the exclude glob; vitest skips it; Playwright continues to pick it up at `pnpm test:visual` time'
      pattern: '\\.spec\\.ts$'
    - from: 'CC-6 split-to-integration dispositions'
      to: 'frontend/vitest.config.ts exclude array'
      via: 'Each split file appends a glob entry to the exclude array; Plan 50-04 audit and Plan 50-05 integration runner pick up the file thereafter'
      pattern: "exclude:\\s*\\["
---

<objective>
Close 4 single-class outlier failures that did not auto-close via Plan 50-06/50-09 cross-cutting fixes. These are mechanically distinct from the provider-cascade (50-09), i18n-text drift (50-10), and design-system/route harness drift (50-12) work:

1. **CC-5 (1 file) — `tests/accessibility/entity-search.a11y.test.ts`:** Uses Playwright `test.describe()` API but is named `*.test.ts`, so vitest's collection glob (`**/*.test.{ts,tsx}`) picks it up and Playwright's `_currentSuite` API throws `Playwright Test did not expect test.describe() to be called here.` Fix: rename to `.spec.ts` (default per Karpathy §2) OR rewrite as a true vitest unit if `git log` shows it was authored as one and Playwright imports were added by mistake.

2. **CC-6 (2 files) — `tests/a11y/wcag-compliance.test.ts` + `tests/performance/validation-speed.test.ts`:** Both surface esbuild `Transform failed` errors during vitest collection. The file body never executes (0 tests collected). Per-file diagnosis: likely import of a deleted module OR invalid TS syntax. Fix per D-10 archaeology + D-09 root-cause discretion.

3. **Per-file (1 file) — `tests/accessibility/waiting-queue-a11y.test.tsx`:** 30 failing tests, primarily axe assertions on modal/focus behavior. Investigate per D-10; if assertions drifted with the impl (Plan 39 waiting-queue work) update assertions; if the impl regressed update the impl per D-09.

Plan 50-11 OWNS 6 file modifications (4 test files + 2 config files conditional on disposition). Below the ≤8 ceiling. Runs in parallel with Plans 50-10 and 50-12 (no file-overlap with either — confirmed against PCH-50R-01 ownership boundary; setup.ts is owned by 50-10).

This plan deliberately inherits the disposition framework from the archived Plan 50-07 (CC-5 + CC-6) and supplements it with the per-file waiting-queue a11y triage that was originally a 50-08 hot-list item.

Purpose: Remove four mechanical-bug-class outlier failures from the residual set so Plan 50-13's final-mile per-file triage has fewer items.

Output:

- `frontend/tests/accessibility/entity-search.a11y.test.ts` either renamed to `.spec.ts` OR rewritten as vitest unit (with original path deleted if renamed).
- `frontend/tests/a11y/wcag-compliance.test.ts` fixed, deleted, or split.
- `frontend/tests/performance/validation-speed.test.ts` fixed, deleted, or split.
- `frontend/tests/accessibility/waiting-queue-a11y.test.tsx` passing green or split-to-integration.
- `frontend/vitest.config.ts` updated with exclude entries for any split-to-integration files (conditional).
- `frontend/playwright.config.ts` updated with testMatch entry for the renamed `.spec.ts` (conditional on existing config not already picking it up).
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-test-infrastructure-repair/50-CONTEXT.md
@.planning/phases/50-test-infrastructure-repair/50-RESEARCH.md
@.planning/phases/50-test-infrastructure-repair/50-PATTERNS.md
@.planning/phases/50-test-infrastructure-repair/50-07-PLAN.archived.md
@CLAUDE.md

<interfaces>
<!-- Per-file diagnosis prep. -->

From `frontend/vitest.config.ts` (POST-Plan-50-01):

- `include: ['**/*.test.{ts,tsx}']`
- `exclude: ['node_modules/', 'dist/', 'build/', '**/*.spec.*']`
- Renaming a file from `*.test.ts` to `*.spec.ts` excludes it from vitest collection. Playwright's own glob (`playwright.config.ts`'s `testDir: './tests/e2e'` plus other globs) picks up `.spec.ts` files from its own paths.

From `/tmp/phase50-06-task4-full-after-mock-fix.log`:

- `entity-search.a11y.test.ts` (0 tests collected, FAIL):

  ```
  TestTypeImpl._currentSuite ../node_modules/.pnpm/playwright@1.58.2/node_modules/playwright/lib/common/testType.js:75:13
  TestTypeImpl._describe .../testType.js:115:24
  Function.describe .../transform.js:282:12
  tests/accessibility/entity-search.a11y.test.ts:18:6
  ```

  Playwright API in a vitest-collected file. Default rename disposition.

- `wcag-compliance.test.ts` (0 tests collected, FAIL):

  ```
  ❯ failureErrorWithLog ../node_modules/.pnpm/esbuild@0.27.4/node_modules/esbuild/lib/main.js:1748:15
  ```

  esbuild Transform failed during file load. Per-file diagnosis needed (read full file + identify failing import or syntax).

- `validation-speed.test.ts` (0 tests collected, FAIL): same Transform failed shape.

- `waiting-queue-a11y.test.tsx` (30 failures): each test fails individually (axe assertions, ARIA label checks, focus-trap checks). Likely a11y impl drift since Phase 39 / 40 — modals may have moved to different portal subtree, ARIA names changed, etc.

From `.planning/phases/50-test-infrastructure-repair/50-07-PLAN.archived.md` (the original CC-5/CC-6 disposition framework — preserved verbatim here):

- CC-5 Path A (default): `git mv` rename. No content changes.
- CC-5 Path B (only if `git log -p` shows the file was authored as vitest unit and Playwright imports were added by mistake): rewrite imports to `import { describe, it } from 'vitest'`.
- CC-6 disposition tree:
  - DELETE: test imports a deleted-in-Phase-33/34/36 module and the feature it tested no longer exists.
  - REPAIR: fixable syntax error or renamed-import path update.
  - SPLIT-TO-INTEGRATION: test genuinely needs real infra (`validation-speed.test.ts` may need real Express perf instrumentation). Add to `frontend/vitest.config.ts` exclude + handoff entry in Plan 50-04 audit.

From `frontend/playwright.config.ts` (project's Playwright config):

- Confirm Playwright picks up `.spec.ts` files from `tests/accessibility/`. If Playwright's glob doesn't cover `tests/accessibility/`, the renamed file becomes orphan — must add to Playwright's `testDir` OR move under `tests/e2e/`. Verify at task time. If a `testMatch` edit is required, Plan 50-11 owns `frontend/playwright.config.ts` for this single-line append (listed in `files_modified` accordingly).

From `frontend/tests/accessibility/waiting-queue-a11y.test.tsx` (failing test signatures from the log):

- "should pass axe accessibility checks" (modal)
- "should have proper ARIA labels for all elements"
- "should trap focus within modal when open"
- Component under test: likely `AssignmentDetailsModal` + related waiting-queue surface. Plan 50-09 fixed `AssignmentDetailsModal.test.tsx` for provider migration — that may auto-close some of these too. Verify at Task 0.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 0: Discovery — verify in-scope files still fail; capture per-file signatures</name>
  <files>.planning/phases/50-test-infrastructure-repair/50-11-DISCOVERY.md</files>
  <read_first>
    - .planning/phases/50-test-infrastructure-repair/50-09-SUMMARY.md if exists (some a11y failures may have auto-closed via the polyfill or provider migration)
  </read_first>
  <action>
    Standard discovery gate.

    1. Run `pnpm --filter intake-frontend exec vitest --run --reporter=default tests/a11y tests/accessibility tests/performance 2>&1 | tee /tmp/phase50-11-discovery.log`.

    2. For each of the 4 files in scope, capture pass/fail count + first failure signature.

    3. **Ceiling check:** if MORE than 8 of THIS PLAN'S files are failing, halt. Scope owns 4 — well below ceiling. If any auto-closed, document and shrink scope.

    4. Write `50-11-DISCOVERY.md` enumerating per-file state.

    5. Commit `chore(50-11): record discovery snapshot for ≤8 ceiling check`.

  </action>
  <verify>
    <automated>test -f /tmp/phase50-11-discovery.log &amp;&amp; test -f .planning/phases/50-test-infrastructure-repair/50-11-DISCOVERY.md</automated>
  </verify>
  <done>
    DISCOVERY.md records the 4 in-scope files' current state.
  </done>
</task>

<task type="auto">
  <name>Task 1: CC-5 + CC-6 — entity-search.a11y rename + wcag-compliance + validation-speed disposition (3 files; may touch playwright.config.ts + vitest.config.ts)</name>
  <files>frontend/tests/accessibility/entity-search.a11y.test.ts, frontend/tests/a11y/wcag-compliance.test.ts, frontend/tests/performance/validation-speed.test.ts, frontend/vitest.config.ts, frontend/playwright.config.ts</files>
  <read_first>
    - frontend/tests/accessibility/entity-search.a11y.test.ts (first 40 lines — identify framework: Playwright vs vitest)
    - frontend/tests/a11y/wcag-compliance.test.ts (full file — identify all imports + their disk existence)
    - frontend/tests/performance/validation-speed.test.ts (full file — identify all imports + their disk existence)
    - frontend/vitest.config.ts (POST-Plan-50-01 — confirm `include` + `exclude` shape)
    - frontend/playwright.config.ts (confirm Playwright picks up `tests/accessibility/**/*.spec.ts`)
    - .planning/phases/50-test-infrastructure-repair/50-07-PLAN.archived.md (the per-file disposition tree — preserved verbatim)
    - .planning/STATE.md §Phase 33/34/36 summaries (deletion history of theme-provider, LanguageToggle, MainLayout, etc. — for verifying CC-6 import-of-deleted-module diagnosis)
  </read_first>
  <action>
    Three independent file dispositions; each gets its own commit. Config files (`vitest.config.ts`, `playwright.config.ts`) are touched only when the chosen disposition requires it.

    **A) CC-5 — `tests/accessibility/entity-search.a11y.test.ts`:**

    Read first 30 lines. If it imports `test` from `@playwright/test` OR uses Playwright APIs (`page.goto`, `expect(page).toHaveTitle`, etc.), it's a misnamed Playwright file.

    Default action (Path A): `git mv frontend/tests/accessibility/entity-search.a11y.test.ts frontend/tests/accessibility/entity-search.a11y.spec.ts`. NO content changes.

    Verify:
    - `git status` shows `R` rename.
    - `pnpm --filter intake-frontend exec vitest --run 2>&1 | grep -c "Playwright Test did not expect"` returns 0.
    - Read `frontend/playwright.config.ts` `testDir` and `testMatch` patterns. If Playwright's glob includes `tests/accessibility/**/*.spec.ts`, the rename works — leave playwright.config.ts UNTOUCHED. If NOT, append `tests/accessibility/**/*.spec.ts` to the `testMatch` array (preserve existing patterns). If `playwright.config.ts` is owned by another phase (e.g., Phase 33-09 root config) and cannot be safely modified, instead move the file to `frontend/tests/e2e/accessibility/entity-search.a11y.spec.ts` so Playwright's existing `testDir: './tests/e2e'` glob picks it up. Record which sub-path (config edit vs file move) was chosen in the commit body.

    Path B (only if `git log -p` shows the file was authored as vitest unit with Playwright imports added by mistake): rewrite imports. Replace `import { test, expect } from '@playwright/test'` with `import { describe, it, expect } from 'vitest'`. Replace `test.describe(...)` with `describe(...)` and `test(...)` with `it(...)`. Path B is RARE; default to Path A.

    Commit: `fix(50-11): rename entity-search.a11y.test.ts to .spec.ts (CC-5 Playwright misglobbed)`.

    **B) CC-6 — `tests/a11y/wcag-compliance.test.ts`:**

    Step 1: Read the full file to identify all imports.

    Step 2: For each import, check disk existence: `ls <imported-path>` or `find . -name '<imported-module>' 2>/dev/null | head -3`. Cross-reference deletions with `git log --diff-filter=D --name-only --pretty=format: -- <imported-path>` to confirm.

    Step 3: Also try to reproduce the esbuild error: `pnpm --filter intake-frontend exec vitest --run tests/a11y/wcag-compliance.test.ts 2>&1 | head -30` — the error names a specific line+column.

    Step 4 disposition:
    - **delete:** If imports a deleted-in-Phase-33/34/36 module that no longer exists. Commit: `delete(50-11): wcag-compliance.test.ts — imports deleted theme-provider module from Phase 33-07`. NO config edits.
    - **repair:** If fixable syntax. Commit: `fix(50-11): repair wcag-compliance.test.ts (CC-6 <root-cause>)`. NO config edits.
    - **split-to-integration:** If needs real infra (rare for a11y tests; verify before choosing). Append the file glob to `frontend/vitest.config.ts` `exclude` array (single-line append preserving array literal style). Commit: `split(50-11): wcag-compliance.test.ts → integration (needs <infra>)` — the commit's diff MUST include both the test file (untouched/moved) and vitest.config.ts (one new exclude entry).

    **C) CC-6 — `tests/performance/validation-speed.test.ts`:**

    Same procedure as B. Performance tests are MORE likely to be `split-to-integration` candidates because real perf instrumentation needs real services. If the test asserts on `< Xms` for an Express endpoint, it cannot pass under mocks.

    Step 4 disposition: same tree as B. If split-to-integration is chosen, the vitest.config.ts exclude edit is bundled with B's same commit (a single-coordinate vitest.config.ts edit) OR a separate commit if B chose repair/delete.

    Commit (one of the disposition messages above).

    **CRITICAL — `queued-with-rationale` is NOT acceptable per D-01.** If neither delete, repair, nor split is viable, halt and escalate.

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; { test -f tests/accessibility/entity-search.a11y.spec.ts || grep -q "from 'vitest'" tests/accessibility/entity-search.a11y.test.ts; } &amp;&amp; pnpm exec vitest --run --reporter=default 2&gt;&amp;1 | grep -c "Playwright Test did not expect" | grep -q "^0$" &amp;&amp; pnpm exec vitest --run --reporter=default 2&gt;&amp;1 | grep -c "Transform failed" | grep -q "^0$"</automated>
  </verify>
  <done>
    Three files dispositioned: entity-search.a11y renamed or rewritten; wcag-compliance + validation-speed each deleted, repaired, or split. FE runner output has 0 `Playwright Test did not expect` AND 0 `Transform failed` errors. If split-to-integration was chosen, `frontend/vitest.config.ts` exclude array has the new entry. If the renamed `.spec.ts` needed a Playwright config update, `frontend/playwright.config.ts` testMatch reflects it. Each disposition has its own commit with rationale.
  </done>
</task>

<task type="auto">
  <name>Task 2: Per-file triage — waiting-queue-a11y.test.tsx (30 failures, D-10 archaeology)</name>
  <files>frontend/tests/accessibility/waiting-queue-a11y.test.tsx, frontend/vitest.config.ts</files>
  <read_first>
    - frontend/tests/accessibility/waiting-queue-a11y.test.tsx (full file — 30 failing tests; identify the patterns)
    - frontend/src/components/waiting-queue/AssignmentDetailsModal.tsx (impl — Plan 50-09 fixed AssignmentDetailsModal.test.tsx provider migration; verify the same provider need here)
    - frontend/src/components/waiting-queue/*.tsx (other waiting-queue components the test touches)
    - frontend/tests/utils/render.tsx (renderWithProviders availability)
    - `git log --oneline -- frontend/tests/accessibility/waiting-queue-a11y.test.tsx` (authoring history)
    - `git log --oneline -- frontend/src/components/waiting-queue/` (impl change history — Phase 39 / 40 / 41 likely changed shapes)
    - .planning/phases/50-test-infrastructure-repair/50-CONTEXT.md D-09, D-10
  </read_first>
  <action>
    D-10 archaeology + fix. The split-to-integration disposition (if chosen) requires editing `frontend/vitest.config.ts` exclude array.

    1. Re-run `pnpm --filter intake-frontend exec vitest --run tests/accessibility/waiting-queue-a11y.test.tsx 2>&1 | head -60` — capture per-test failure signatures.

    2. **Step A — provider migration check:** If the file uses bare `render` from `@testing-library/react` + manual `<I18nextProvider>` (similar to the 7 files Plan 50-09 migrated), migrate it to `renderWithProviders as render` from `@tests/utils/render`. This may auto-close all 30 failures if the root cause was provider absence (same diagnosis as ContributorsList / FilterPanel).

    3. **Step B — re-run after migration:** If failures persist, classify them:
       - Axe `serious/critical` violations on a modal — read the modal component (impl). If a recent change removed an ARIA label, restore it (IMPL DRIFTED + D-09 root-cause). If the test expected an ARIA label the impl never had, update test (TEST WRONG).
       - Focus-trap assertions — verify the modal still uses focus-trap-react or HeroUI's built-in focus trap. If Phase 33-05 / 36-04 changed the modal's focus-management library, the test's selector may need updating.
       - ResizeObserver / portal-subtree issues — Plan 50-09 polyfill should cover ResizeObserver; portal-subtree assertions (`document.body.querySelector(...)`) may need updating per HeroUI v3 portal behavior.

    4. **Step C — disposition:**
       - If all 30 close via provider migration + per-test assertion tweaks, commit: `fix(50-11): waiting-queue-a11y test — migrate to renderWithProviders + reconcile assertions (D-10)`. No vitest.config.ts edit.
       - If the test fundamentally needs real DOM measurements (axe checks color-contrast in jsdom which has no layout), classify as `split-to-integration`: append the file glob to `frontend/vitest.config.ts` `exclude` array. Commit (single commit covering the test file move/leave + config edit): `split(50-11): waiting-queue-a11y → integration (axe color-contrast needs real DOM)`.
       - DO NOT delete the file — a11y tests are too valuable to drop. Split is the right disposition if jsdom is genuinely insufficient.

    5. Verify post-fix:
       - `pnpm --filter intake-frontend exec vitest --run tests/accessibility/waiting-queue-a11y.test.tsx 2>&1 | tail -5` — exit 0 OR file no longer in default-runner glob.
       - `pnpm --filter intake-frontend lint tests/accessibility/waiting-queue-a11y.test.tsx` — exit 0.

    Commit body includes the per-failure-cluster verdict + `git log -p` evidence.

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; { ! test -f tests/accessibility/waiting-queue-a11y.test.tsx || pnpm exec vitest --run --reporter=default tests/accessibility/waiting-queue-a11y.test.tsx 2&gt;&amp;1 | tail -5 | grep -E "Test Files" | awk '{ for(i=1;i&lt;=NF;i++) if ($i ~ /failed/) { print "FAIL: still failing"; exit 1 } print "PASS"; exit 0 }'; } &amp;&amp; pnpm exec vitest --run --reporter=default 2&gt;&amp;1 | grep -c "waiting-queue-a11y.*failed" | grep -q "^0$"</automated>
  </verify>
  <done>
    `tests/accessibility/waiting-queue-a11y.test.tsx` either passes green individually OR is split-to-integration via `frontend/vitest.config.ts` exclude (with handoff entry to Plan 50-04 audit). No `queued-with-rationale` disposition.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                                                                                                                         |
| ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Vitest collection ↔ file extension             | The post-50-01 `include` + `exclude` makes file extension a security gate; renaming a file flips the gate.                                                          |
| esbuild Transform failure ↔ runtime safety     | A Transform failure masks the file's content; deleting/repairing without root-cause understanding can silently remove a real regression check.                      |
| axe a11y assertions ↔ jsdom layout             | axe relies on layout for color-contrast and focus-visibility checks; jsdom has no layout. Tests asserting on color-contrast under jsdom are false-positive-prone.   |
| Vitest exclude array ↔ default-runner contract | Each split-to-integration entry SILENTLY removes coverage from the default gate; must be paired with a Plan 50-04 audit row + Plan 50-05 integration runner pickup. |

## STRIDE Threat Register

| Threat ID  | Category                   | Component                                                                                                                 | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                      |
| ---------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-50-11-01 | E (Elevation of privilege) | Renaming `entity-search.a11y.test.ts` to `.spec.ts` could orphan a real a11y test if Playwright doesn't pick the file up. | mitigate    | Task 1 verifies Playwright's `testMatch` includes `tests/accessibility/**/*.spec.ts`; if not, the plan EITHER appends to playwright.config.ts testMatch OR moves the file under `tests/e2e/accessibility/` (Playwright's existing testDir). Plan 50-04 audit records the file as `disposition: split-to-playwright`. |
| T-50-11-02 | T (Tampering)              | Deleting a CC-6 file could remove a real regression check if the diagnosis is wrong.                                      | mitigate    | Task 1 part B/C requires `git log --diff-filter=D` evidence the imported module was deleted (not renamed) before recommending delete. Per-file commit message records the evidence.                                                                                                                                  |
| T-50-11-03 | I (Information disclosure) | Splitting waiting-queue-a11y to integration could hide a real a11y regression from the default gate.                      | accept      | Plan 50-04 audit records the split with rationale ("axe color-contrast needs real DOM"); Plan 50-05's integration runner (advisory but RUNNING) still surfaces violations in CI logs. The default gate excludes the test only because the test is jsdom-impossible, not because it's optional.                       |
| T-50-11-04 | T (Tampering)              | An incorrect vitest.config.ts exclude glob could silently bury unrelated tests under the same pattern.                    | mitigate    | Each split entry uses a literal file path (not a wildcard); Plan 50-04 audit greps the exclude array against the SUMMARY's recorded split files to detect over-broad globs.                                                                                                                                          |

</threat_model>

<verification>
**Phase-level checks (post-plan):**

- `frontend/tests/accessibility/entity-search.a11y.test.ts` either no longer exists (renamed to `.spec.ts`) OR no longer imports from `'@playwright/test'`.
- `frontend/tests/a11y/wcag-compliance.test.ts` either no longer exists, no longer produces `Transform failed`, OR is excluded from the default-runner glob.
- `frontend/tests/performance/validation-speed.test.ts` same condition.
- `frontend/tests/accessibility/waiting-queue-a11y.test.tsx` passes green OR is split-to-integration.
- `pnpm --filter intake-frontend exec vitest --run` output contains 0 occurrences of `Playwright Test did not expect` and 0 occurrences of `Transform failed`.
- Each per-file disposition has its own commit with rationale in the commit body.
- `pnpm --filter intake-frontend lint` exits 0.
- If split-to-integration was chosen for any file, `frontend/vitest.config.ts` exclude array contains the literal file path (commit-body evidence preserved).
- If CC-5 Path A required a Playwright config edit, `frontend/playwright.config.ts` testMatch reflects it (with commit-body evidence).
  </verification>

<success_criteria>

- **TEST-02 progress:** 4 outlier files removed from the residual failure set.
- **CLAUDE.md byte-match contract preserved:** no `.spec.ts` rename leaves a Playwright orphan.
- **No regression to Plan 50-09/10:** the 7 + 6 files those plans closed stay green.
- **PCH-50R-04 / PCH-50R-07 mitigations:** vitest.config.ts and playwright.config.ts are listed in files_modified to cover any disposition that requires them.

</success_criteria>

<output>
After completion, create `.planning/phases/50-test-infrastructure-repair/50-11-SUMMARY.md` with:
- Per-file disposition for CC-5 (Path A vs Path B + rationale).
- Per-file disposition for the two CC-6 files (delete / repair / split + archaeology evidence).
- waiting-queue-a11y disposition (provider-migration auto-close / assertion reconciliation / split).
- vitest.config.ts and playwright.config.ts edits (paste relevant diff lines).
- Pre/post failing-file counts.
- Retain `.planning/phases/50-test-infrastructure-repair/50-11-DISCOVERY.md` as a permanent audit artifact for Plan 50-04 (per PCH-50R-08 — DO NOT delete).
- Cross-plan handoff to Plan 50-04 audit for any `split-to-integration` files.
</output>
</content>
