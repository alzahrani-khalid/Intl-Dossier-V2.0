# Continue Here - Phase 50

Updated: 2026-05-14 (Plan 50-05 branch-protection checkpoint)

## Current State

Phase 50 default test infrastructure is green locally. Plans 50-01, 50-03, 50-04, 50-09, 50-10, 50-11, 50-12, 50-13a, and 50-13b have summaries/commits. Plan 50-05 has its code-side CI/lint enforcement committed, but the GitHub branch-protection and smoke-PR checkpoint is still pending.

Latest Phase 50 commit:

- `720d135a` — `ci(50-05): add split test gates and mock lint guard`

## Verified Local Gates

- `pnpm --filter intake-frontend test --run` — `Test Files 154 passed | 4 skipped (158)`, `Tests 1219 passed | 25 todo (1244)`.
- `pnpm --filter intake-backend test --run` — `Test Files 15 passed (15)`, `Tests 214 passed (214)`.
- `pnpm lint` — exits 0. The root lint dispatcher runs workspace lint plus `frontend/tests/setup.ts`.
- `pnpm lint frontend/tests/setup.ts` — exits 0.
- `pnpm lint frontend/tests/component/CommitmentList.test.tsx` — exits 0.
- `pnpm lint frontend/src/components/signature-visuals/__tests__/GlobeLoader.reducedMotion.test.tsx` — exits 0.
- `pnpm lint tools/eslint-fixtures/bad-vi-mock.ts` — exits 1 with the D-15 `vi.mock(...) factory must spread vi.importActual(<id>)` message.
- `.github/workflows/ci.yml` grep checks pass for exactly one `Tests (frontend)`, `Tests (backend)`, and `Tests (integration)` job; old `Unit Tests` is gone; Ruby YAML parse succeeds. `actionlint` was not installed locally.

Known local warnings are unchanged and out of 50-05 scope: npm unknown project-config warnings, backend PDFDocument namespace import warning, frontend CSS `@import` ordering warning, Sentry mixed dynamic/static import warning, Vite chunk-size warnings, and knip unused output from the commit hook.

## 50-05 Implementation Notes

- `.github/workflows/ci.yml` now has blocking jobs `Tests (frontend)` and `Tests (backend)`, plus advisory `Tests (integration)` with `continue-on-error: true`.
- `build` now depends on `lint`, `type-check`, `test-frontend`, and `test-backend`.
- `eslint.config.mjs` has the D-15 `no-restricted-syntax` selector scoped to `frontend/tests/**/*.{ts,tsx}` plus `tools/eslint-fixtures/**/*.{ts,tsx}`.
- The rule was intentionally scoped to the global test harness and bad fixture. A broader `**/__tests__` / `**/*.test` scope surfaced 234 legacy frontend source-test mocks and would turn 50-05 into a separate migration.
- `scripts/lint.mjs` preserves `pnpm lint` as workspace lint and enables `pnpm lint <path>` for the targeted D-15 checks required by the plan.
- `tools/eslint-fixtures/bad-vi-mock.ts` is a deliberately bad fixture and should remain failing when linted directly.

## Current Blocker

Stop at Plan 50-05 Task 3. The plan requires a human-verify branch-protection and smoke-PR checkpoint.

Read-only GitHub state at stop:

- `gh auth status` succeeds for `alzahrani-khalid` with `repo` and `workflow` scopes.
- `gh api repos/alzahrani-khalid/Intl-Dossier-V2.0/branches/main/protection --jq '{enforce_admins:.enforce_admins.enabled, contexts:.required_status_checks.contexts}'` returns `enforce_admins: true` and contexts `["type-check","Security Scan","Lint","Bundle Size Check (size-limit)"]`.
- Current branch is `DesignV2`, tracking `origin/DesignV2`, ahead by 75 commits.

Do not PATCH branch protection until both new GitHub jobs have gone green on at least one pushed commit. Do not include `Tests (integration)` in required contexts.

## Recommended Next Command

Continue Plan 50-05 Task 3:

1. Push/land commit `720d135a` through the intended review path.
2. Verify GitHub Actions shows `Tests (frontend)` and `Tests (backend)` green on that commit.
3. Capture pre-state with `gh api repos/alzahrani-khalid/Intl-Dossier-V2.0/branches/main/protection`.
4. PUT branch protection preserving `enforce_admins: true`, existing review/restriction settings, and contexts `type-check`, `Security Scan`, `Lint`, `Bundle Size Check (size-limit)`, `Tests (frontend)`, `Tests (backend)`.
5. Create the smoke PR specified in `50-05-PLAN.md`, prove `mergeStateStatus = BLOCKED` when `Tests (frontend)` fails, then close/delete it.
6. Write `50-05-SUMMARY.md` with pre/post protection state and smoke PR URL.

## Critical Anti-Patterns

| Issue                      | Severity | Instruction                                                                                                                        |
| -------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| Branch-protection ordering | blocking | PATCH only after the new frontend/backend jobs are green on GitHub at least once.                                                  |
| Required contexts          | blocking | Add `Tests (frontend)` and `Tests (backend)` only. Do not require `Tests (integration)`.                                           |
| PUT payload                | blocking | Preserve `enforce_admins`, `required_pull_request_reviews`, and `restrictions` from pre-state. PUT replaces the resource.          |
| Legacy source-test mocks   | warning  | Do not broaden D-15 to all `frontend/src/**/__tests__` in 50-05 unless a separate migration is planned.                            |
| Dirty worktree noise       | warning  | Leave unrelated `.planning/phases/47-*`, `48-*`, `49-*`, `.gemini/*`, and local untracked files untouched unless explicitly asked. |
