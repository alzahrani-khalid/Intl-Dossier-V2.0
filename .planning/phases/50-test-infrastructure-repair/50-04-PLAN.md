---
phase: 50-test-infrastructure-repair
plan: 04
type: execute
wave: 4
depends_on:
  - 50-01
  - 50-03
  - 50-09
  - 50-10
  - 50-11
  - 50-12
  - 50-13a
  - 50-13b
files_modified:
  - .planning/phases/50-test-infrastructure-repair/50-TEST-AUDIT.md
  - frontend/docs/test-setup.md
  - backend/docs/test-setup.md
autonomous: true
requirements:
  - TEST-03
  - TEST-04
user_setup: []

must_haves:
  truths:
    - 'A single audit artifact `.planning/phases/50-test-infrastructure-repair/50-TEST-AUDIT.md` enumerates every failing test at phase start as an individual per-file row (218 FE + 208 BE = 426 distinct file-path rows minimum — compressed-by-class summaries are NOT accepted) with five columns: `workspace`, `file_path`, `failure_class`, `disposition`, `rationale_or_commit_ref`.'
    - "The audit's summary table totals match the per-row totals; the `Phase-exit state` section records `pnpm --filter intake-frontend test --run` and `pnpm --filter intake-backend test --run` exit codes (both 0) with ISO 8601 timestamps."
    - '`frontend/docs/test-setup.md` documents the vitest runner architecture, the `vi.importActual` + spread contract for `react-i18next`, canonical mock recipes for supabase/Redis/BullMQ/LLM, the unit-vs-integration runner split, common pitfalls, and the D-15 ESLint rule (preview — full rule lands in Plan 50-05).'
    - "`backend/docs/test-setup.md` documents backend-specific concerns (Node env, `pool: 'forks'`, global mocks per D-07/D-08, env-var contract under mocks, integration runner) and cross-references the FE doc for shared concepts."
    - "VALIDATION row TEST-04 grep gate passes: `grep -v '^#' frontend/docs/test-setup.md | grep -cE 'vi\\.importActual|initReactI18next|integration runner'` ≥5 (using a comment-stripped count per the grep gate hygiene rule)."
  artifacts:
    - path: '.planning/phases/50-test-infrastructure-repair/50-TEST-AUDIT.md'
      provides: 'Phase-50 audit artifact (TEST-03 anchor) — full failure inventory + dispositions + phase-exit state'
      contains: 'Phase-exit state'
    - path: 'frontend/docs/test-setup.md'
      provides: 'Comprehensive frontend test-setup contributor reference (TEST-04 anchor)'
      contains: 'vi.importActual'
    - path: 'backend/docs/test-setup.md'
      provides: 'Backend-specific test-setup pointer doc (TEST-04 anchor)'
      contains: 'test:integration'
  key_links:
    - from: '.planning/phases/50-test-infrastructure-repair/50-TEST-AUDIT.md'
      to: 'Plan 50-01 / 50-02 / 50-03 SUMMARYs'
      via: 'Audit aggregates per-file dispositions recorded by upstream plan SUMMARYs'
      pattern: "fixed-in-phase\\|split-to-integration\\|queued-with-rationale\\|deleted-dead"
    - from: 'frontend/docs/test-setup.md'
      to: 'frontend/tests/setup.ts (post-Plan-50-01 shape)'
      via: 'Doc cites the live `vi.importActual` + spread factory as the canonical example'
      pattern: 'vi.importActual'
---

<objective>
Produce the two paperwork artifacts that close TEST-03 and TEST-04 once the test surfaces are green:

1. **`50-TEST-AUDIT.md`** (TEST-03) — a single classified artifact enumerating every failing test at phase start (218 FE + 208 BE = 426 distinct per-file rows minimum per RESEARCH §Failure Inventory), columned per CONTEXT D-04: `workspace, file, failure_class, disposition, rationale`. Rows MUST be per-file (one row per individual test file path); compressed-by-class summary rows ("frontend/tests/e2e/\*_/_.spec.ts (175 files)") are NOT acceptable for the body — the body is the inventory. Class-grouped summaries belong only in the Summary section. The audit aggregates per-file dispositions recorded by the SUMMARYs of Plans 50-01, 50-02, 50-03 and records the phase-exit state (both workspaces' `pnpm test --run` exit codes + timestamps).

2. **`frontend/docs/test-setup.md`** + **`backend/docs/test-setup.md`** (TEST-04) — contributor reference documentation per CONTEXT D-14. The FE doc is comprehensive: vitest runner architecture, `react-i18next` mock contract with the `vi.importActual` + spread precedent, canonical recipes for supabase/Redis/BullMQ/LLM mocking, the unit-vs-integration runner split, common pitfalls, fixture patterns, and a preview of the D-15 ESLint rule. The BE doc is shorter — cross-references the FE doc for shared concepts and documents only BE-specific concerns (Node env, `pool: 'forks'`, the supabaseAdmin chain mock, env-var contract under mocks, `test:integration` invocation).

This plan does NOT touch test code, configs, or the ESLint rule itself (those are owned by 50-01/02/03 and 50-05). It is doc/audit-only and runs after the workspaces are green.

Purpose: TEST-03 + TEST-04 cannot be marked Satisfied until the audit exists and the docs are written. This plan also flags non-blocking debt (`backend/tests/contracts/vitest.config.ts` dead config, root-level `/vitest.config.ts` legacy stray) for downstream phase cleanup.

Output:

- `.planning/phases/50-test-infrastructure-repair/50-TEST-AUDIT.md` (YAML frontmatter + summary tables + per-workspace per-file failure tables + phase-exit state + queued tech debt; size determined by the 426 individual file-path rows + sections — no fixed line target)
- `frontend/docs/test-setup.md` (~250-350 lines, 7 sections per PATTERNS §`frontend/docs/test-setup.md`)
- `backend/docs/test-setup.md` (~120-180 lines, 5 sections per PATTERNS §`backend/docs/test-setup.md`)
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-test-infrastructure-repair/50-CONTEXT.md
@.planning/phases/50-test-infrastructure-repair/50-RESEARCH.md
@.planning/phases/50-test-infrastructure-repair/50-PATTERNS.md
@.planning/phases/50-test-infrastructure-repair/50-VALIDATION.md
@.planning/phases/50-test-infrastructure-repair/50-01-SUMMARY.md
@.planning/phases/50-test-infrastructure-repair/50-02-SUMMARY.md
@.planning/phases/50-test-infrastructure-repair/50-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Source artifacts for the audit + docs. -->

From `.planning/milestones/v6.2-MILESTONE-AUDIT.md` (closest YAML-frontmatter analog for the audit doc — PATTERNS §`50-TEST-AUDIT.md`):

- YAML frontmatter starts with `milestone:`, has `audited`, `status`, `re_audit`, `scores`, `tech_debt`
- Then H1 with verdict marker
- Two-line "Audited / HEAD" header
- H2 sections: Milestone Scope → Verification Summary → Findings → Final State

From `frontend/docs/bundle-budget.md` (closest analog for FE doc):

- H1 title (concise)
- Two-line "Last audited / Audit artifact" header
- Short prose intro (1-2 paragraphs)
- H2 sections with pipe-aligned markdown tables and fenced code blocks

From RESEARCH §Common Pitfalls (7 named pitfalls):

1. Spread missing `await` on `importActual`
2. Chain mocks that return new objects break per-test refinement
3. `vi.clearAllMocks()` in `afterEach` wipes per-test mock implementations (use clear for call history, reset for impl)
4. Playwright `.spec.ts` files in `tests/` re-picked by vitest after config split
5. ESLint rule firing on the canonical `frontend/tests/setup.ts` itself (whitelist `importActual + SpreadElement` pattern)
6. BullMQ `Queue` mock breaks when `close()` is awaited at shutdown (use `vi.fn(async () => undefined)`)
7. Backend `pool: 'forks'` + global mock state — each test file is its own process; cross-file state does NOT persist (don't assume "global Redis" carries across files)

From RESEARCH §Mock Factory Specs:

- FE: `frontend/tests/setup.ts` D-05 factory (recipe at RESEARCH lines 213-254)
- BE: `backend/tests/setup.ts` D-07/D-08 (recipe at RESEARCH lines 274-414, covering supabase/ioredis/bullmq/anthropic/openai/xenova)

From RESEARCH §Failure Inventory totals at phase start (the audit rows):

- Frontend 218 failing test files distributed across class-PW (175), class-M (20), class-D (11), class-S (4), class-T (2), class-F (1), class-R (~5)
- Backend 208 failing test files distributed across class-I-Supabase (~64), class-I-Express (~171), class-E (~104), class-H (24), class-M (31), class-A (17), class-N (~25) — with overlap between classes (a single file can be in 2 classes)

From RESEARCH §Open Question 1 (RESOLVED) — FE integration runner: "Plan 50-01 deletes the 5 dead files. Plan 50-04 audit decides whether any others survive — if so, FE gets its own `vitest.integration.config.ts`."

From PATTERNS §Note on root vitest.config.ts: "A third vitest config exists at `/vitest.config.ts` (root level, 41 lines, jsdom env, coverage thresholds 80/80/80/80). Not referenced by any workspace `pnpm test` script. Likely a legacy stray; planner should flag in audit as `disposition: queued-out-of-scope` or `deleted-dead`."

From PATTERNS §`backend/tests/contracts/vitest.config.ts` mention: "This second config is dead code from a prior attempt; planner can either consume its shape OR delete it. Recommend deletion as part of Phase 50 cleanup (note in `50-TEST-AUDIT.md`)."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 50-TEST-AUDIT.md (TEST-03) — per-file enumeration of all 426 failing tests</name>
  <files>.planning/phases/50-test-infrastructure-repair/50-TEST-AUDIT.md</files>
  <read_first>
    - .planning/phases/50-test-infrastructure-repair/50-01-SUMMARY.md (FE Wave 1 dispositions — the 175 + 20 + 11 cascade-cleared file lists, with full per-file path enumeration)
    - .planning/phases/50-test-infrastructure-repair/50-02-SUMMARY.md (FE Wave 2 per-file regression dispositions including useCountryAutoFill + 5 representative files)
    - .planning/phases/50-test-infrastructure-repair/50-03-SUMMARY.md (BE Wave 1 dispositions — global mocks + integration split + per-file triage)
    - .planning/phases/50-test-infrastructure-repair/50-CONTEXT.md D-04 (five-column structure: workspace, file, failure_class, disposition, rationale — D-04 says "enumerates every failing test at phase start"; per-file granularity is non-negotiable)
    - .planning/phases/50-test-infrastructure-repair/50-RESEARCH.md §Failure Inventory + §Validation evidence checklist
    - .planning/phases/50-test-infrastructure-repair/50-VALIDATION.md (TEST-03 row — `wc -l` ≥218 PLUS per-file regex gate in this plan's verify; summary table cross-check + phase-exit state)
    - .planning/phases/50-test-infrastructure-repair/50-PATTERNS.md §`50-TEST-AUDIT.md` (full target outline)
    - .planning/milestones/v6.2-MILESTONE-AUDIT.md (YAML frontmatter shape to mirror)
  </read_first>
  <action>
    Create `.planning/phases/50-test-infrastructure-repair/50-TEST-AUDIT.md` per the structure in PATTERNS §`50-TEST-AUDIT.md`. **Per-file enumeration is mandatory per D-04** — every individual failing test file path at phase start gets its own row; compressed-by-class representations ("frontend/tests/e2e/**/*.spec.ts (175 files)") are NOT acceptable for the body inventory. Class-grouped summaries belong only in the Summary section.

    Required sections in order:

    1. **YAML frontmatter** with these keys: `phase: 50-test-infrastructure-repair`, `audited: <ISO 8601 timestamp at task start>`, `status: passed` (set after all upstream SUMMARYs confirm green), `scores:` block containing `frontend_tests: N/N` (per Plan 50-02 summary), `backend_tests: N/N` (per Plan 50-03 summary), `audit_rows: 218 + 208 = 426`, `dispositions:` map with totals per class (`fixed-in-phase`, `split-to-integration`, `queued-with-rationale`, `deleted-dead`, `queued-out-of-scope`).

    2. **H1**: `# Phase 50 Test Audit — Test Infrastructure Repair`. Two-line header below: `**Audited:** <timestamp>` and `**Phase-start HEAD:** <sha of DesignV2 at Plan 50 start>` and `**Phase-exit HEAD:** <sha at task time>`. Get the SHAs via `git rev-parse HEAD` at task time + use Plan 50-01's commit history to find the phase-start SHA.

    3. **H2 "Summary by failure class"** — a markdown table with columns `Workspace | Class | Count | Disposition`. This is the ONLY place class-grouped summary rows are allowed. Rows include at minimum:
       - frontend, playwright-misglobbed, 175, fixed-in-phase (50-01 G1)
       - frontend, module-eval (initReactI18next), 20, fixed-in-phase (50-01 G2)
       - frontend, dead-import, 11, deleted-dead (50-01 G3)
       - frontend, supabase-env (class-S), 4, fixed-in-phase OR deleted-dead per 50-02 verdict
       - frontend, transform-failed, 2, fixed-in-phase OR queued-with-rationale per 50-02 verdict
       - frontend, faker-missing, 1, fixed-in-phase (cleared via config exclude in 50-01)
       - frontend, product-regression, ~5 (exact count per 50-02), fixed-in-phase
       - backend, integration-only (real-Supabase), ~64, split-to-integration
       - backend, integration-only (real-Express), ~171, split-to-integration
       - backend, env-missing (class-E), ~104, fixed-in-phase (D-07 mocks)
       - backend, helper-missing (class-H), 24, fixed-in-phase OR deleted-dead per 50-03
       - backend, module-not-found (class-M), 31, fixed-in-phase OR deleted-dead
       - backend, api-shape drift (class-A), 17, fixed-in-phase
       - backend, null-deref on supabase mock (class-N), ~25, fixed-in-phase (D-07 mocks)
       Counts must sum to 218 (FE) and 208 (BE) when overlap is reconciled per RESEARCH §Failure Inventory.

    4. **H2 "Frontend failures (218 rows)"** — a markdown table with columns `File | Class | Disposition | Rationale / Commit Ref`. **Every individual failing FE test file path gets its OWN row.** Populate by enumerating Plan 50-01 + 50-02 SUMMARY entries. The 175 playwright-misglobbed files each get a row (each row's File column contains a real path like `frontend/tests/e2e/auth/login.spec.ts` — not a glob); Rationale can be terse (`50-01 G1 — vitest config exclude, see commit <sha>`) and may be identical across all 175 rows, but the rows themselves are individual. Same for the 20 class-M, 11 class-D, etc. — each file path is its own row. If Plan 50-01 SUMMARY lists files by directory shorthand, expand to the full path list before authoring this section.

    5. **H2 "Backend failures (208 rows)"** — same per-file structure as Section 4, aggregating Plan 50-03's SUMMARY. Each of the ~64 + ~171 integration files (with overlap reconciled) gets its own row; each helper-missing / module-not-found / api-shape-drift file gets its own row.

    6. **H2 "Phase-exit state"** — a markdown table with columns `Workspace | pnpm test --run exit code | Timestamp | Notes`. Run both commands at task time:
       - `cd frontend && pnpm exec vitest --run 2>&amp;1 | tail -3` → capture exit code via `echo $?`; record ISO timestamp via `date -u +"%Y-%m-%dT%H:%M:%SZ"`.
       - `cd backend && pnpm exec vitest --run 2>&amp;1 | tail -3` → same.
       Both rows must report exit code 0. Notes column can record `Test Files: N passed (N) | Tests: M passed (M)` summary.

    7. **H2 "Queued tech debt (out of Phase 50 scope)"** — a markdown table with columns `Item | Workspace | Linked phase / ticket`. Required rows:
       - Playwright visual baselines regen (deferred per D-12) — Phase 52 KANBAN-04
       - `backend/tests/contracts/vitest.config.ts` dead config (40 lines, unreferenced) — recommend deletion in a follow-up
       - Root-level `/vitest.config.ts` (41 lines, jsdom env, not consumed by any pnpm script) — recommend deletion in a follow-up
       - FE coverage thresholds (CONTEXT line 124 claimed they exist; verified absent — deferred per CONTEXT §80%+ coverage push)
       - BE coverage-vs-narrow-suite trade-off (after the D-03 split, default `pnpm test --coverage` likely under-reports against the 80/80/75/80 thresholds; mitigation: run coverage against unit + integration combined; recorded in `backend/docs/test-setup.md`)
       - Any per-file `queued-with-rationale` items from Plan 50-02 SUMMARY (FE product regressions that couldn't be fixed inside the plan budget)
       - Any per-file `queued-with-rationale` items from Plan 50-03 SUMMARY

    8. **H2 "Sources"** — list the three SUMMARY files this audit aggregates (`50-01-SUMMARY.md`, `50-02-SUMMARY.md`, `50-03-SUMMARY.md`) plus the four phase-prep docs (`50-CONTEXT.md`, `50-RESEARCH.md`, `50-PATTERNS.md`, `50-VALIDATION.md`).

    Style: markdown with pipe-aligned tables. No emoji per CLAUDE.md. Sentence-case for prose; title case for H2 headers. Match the style of `v6.2-MILESTONE-AUDIT.md` for the YAML frontmatter shape.

    **Per-row file-path uniqueness contract:** When this task completes, an awk/grep extraction of the File column of Sections 4 and 5 must yield ≥400 distinct full-path strings ending in `.test.ts`, `.test.tsx`, or `.spec.ts`. The verify command below makes this an automated gate.

  </action>
  <verify>
    <automated>test -f .planning/phases/50-test-infrastructure-repair/50-TEST-AUDIT.md &amp;&amp; grep -c "^## Frontend" .planning/phases/50-test-infrastructure-repair/50-TEST-AUDIT.md | awk '$1 &gt;= 1 { exit 0 } { exit 1 }' &amp;&amp; grep -c "^## Backend" .planning/phases/50-test-infrastructure-repair/50-TEST-AUDIT.md | awk '$1 &gt;= 1 { exit 0 } { exit 1 }' &amp;&amp; grep -ic "disposition" .planning/phases/50-test-infrastructure-repair/50-TEST-AUDIT.md | awk '$1 &gt;= 5 { exit 0 } { exit 1 }' &amp;&amp; grep -c "Phase-exit state" .planning/phases/50-test-infrastructure-repair/50-TEST-AUDIT.md | grep -q "^1$" &amp;&amp; wc -l < .planning/phases/50-test-infrastructure-repair/50-TEST-AUDIT.md | awk '$1 &gt;= 218 { exit 0 } { exit 1 }' &amp;&amp; awk -F'|' '/^\|.*\.(test|spec)\.tsx?[[:space:]]*\|/ {print $2}' .planning/phases/50-test-infrastructure-repair/50-TEST-AUDIT.md | sed 's/^[[:space:]]*//; s/[[:space:]]*$//' | sort -u | wc -l | awk '$1 &gt;= 400 { exit 0 } { print "FAIL: only " $1 " distinct per-file rows in audit (expected ≥400)"; exit 1 }'</automated>
  </verify>
  <done>
    `.planning/phases/50-test-infrastructure-repair/50-TEST-AUDIT.md` exists with YAML frontmatter (phase, audited, status, scores, dispositions). H2 sections present: "Summary by failure class", "Frontend failures (218 rows)", "Backend failures (208 rows)", "Phase-exit state", "Queued tech debt", "Sources". The body of Sections 4 and 5 contains ≥400 distinct full-path strings ending in `.test.ts`, `.test.tsx`, or `.spec.ts` (one row per failing file — compressed-by-class summaries appear ONLY in the Summary section). Phase-exit state rows show both workspaces exit code 0 with ISO 8601 timestamps. The dead `backend/tests/contracts/vitest.config.ts` and the legacy root `/vitest.config.ts` are listed in queued tech debt. All grep / awk checks in `<verify>` pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author frontend/docs/test-setup.md (TEST-04 primary)</name>
  <files>frontend/docs/test-setup.md</files>
  <read_first>
    - frontend/docs/bundle-budget.md (closest analog — H1 + two-line header + H2 tables + fenced code blocks)
    - .planning/phases/50-test-infrastructure-repair/50-PATTERNS.md §`frontend/docs/test-setup.md` (target outline — 7 sections)
    - .planning/phases/50-test-infrastructure-repair/50-RESEARCH.md §Mock Factory Specs (full recipes verbatim), §Common Pitfalls (7 pitfalls verbatim), §Integration Runner Choice (D-03 explained)
    - frontend/tests/setup.ts (POST-Plan-50-01 state — quote the live factory as the canonical example)
    - frontend/tests/component/CommitmentList.test.tsx:451-462 (in-repo `vi.importActual` precedent — quote verbatim)
    - .planning/phases/50-test-infrastructure-repair/50-CONTEXT.md D-14 (FE doc is comprehensive)
    - .planning/phases/50-test-infrastructure-repair/50-VALIDATION.md (TEST-04 grep gate: `grep -v '^#' frontend/docs/test-setup.md | grep -cE 'vi\\.importActual|initReactI18next|integration runner'` ≥5 — count must come from non-comment lines per grep-gate-hygiene rule)
    - CLAUDE.md (Design rules §No emoji in user-visible copy + §No marketing voice)
  </read_first>
  <action>
    Author `frontend/docs/test-setup.md` per the 7-section outline in PATTERNS:

    1. **Title block** (H1 + two-line header):
       - `# Frontend test setup`
       - `Last reviewed: 2026-05-13 (Phase 50)`
       - `Phase artifact: .planning/phases/50-test-infrastructure-repair/50-TEST-AUDIT.md`
       - Short prose intro (2-3 sentences): explain this is the contract for the frontend test runner, who it's for (new contributors), and the source phase.

    2. **## 1. Test runner architecture**
       - Describe the vitest config (`frontend/vitest.config.ts`): `environment: 'jsdom'`, `setupFiles: ['./tests/setup.ts']`, `include: ['**/*.test.{ts,tsx}']` (post-Plan-50-01), the path aliases `@` and `@tests`, and the MSW server wiring in `frontend/tests/mocks/server.ts`.
       - Note the absence of coverage thresholds today (CONTEXT line 124 was incorrect; documented honestly).
       - Reference `pnpm --filter intake-frontend test --run` as the canonical invocation.

    3. **## 2. The react-i18next mock contract**
       - State the D-05 contract: vitest test files transitively import `frontend/src/i18n/index.ts` which calls `i18n.use(initReactI18next).init(...)` at module-eval. The global mock MUST surface `initReactI18next` (and every other real export) — otherwise the entire dependent module graph fails to evaluate.
       - The canonical pattern is `vi.importActual` + spread + override. Quote the live `frontend/tests/setup.ts` factory verbatim as the example.
       - Explain why list-every-export is fragile: every new SDK export must be hand-added, and a missing export breaks tests at module-eval (not assertion time, so the error message is far from the root cause).
       - Cite the two in-repo precedents that established this pattern (`frontend/tests/component/CommitmentList.test.tsx:451-462` + `frontend/src/components/signature-visuals/__tests__/GlobeLoader.reducedMotion.test.tsx:18-22`).
       - Sub-heading anchor: `## 2. The react-i18next mock contract` — link target for the D-15 ESLint rule message (Plan 50-05 will reference `#react-i18next-precedent`; use a markdown header that produces that anchor or document an alternate).
       - Mention the `t()` fallback `(key) =&gt; translations[key] ?? key` (D-06) and the 60-key after-action translation map; explain the D-11 mixed-assertion-text rule (both i18n keys and English strings remain valid assertion targets).

    4. **## 3. Mocking external dependencies (canonical recipes)**
       - For each mock category, give a fenced TypeScript code block ready to copy-paste:
         a. Supabase — chainable `from()` builder; per-test refinement via `vi.mocked(supabaseAdmin.from).mockImplementation(...)`.
         b. Redis — Map-backed `vi.fn()` factory (mention BE only — FE doesn't use Redis directly).
         c. BullMQ — Queue/Worker/QueueEvents as `vi.fn().mockImplementation(...)` (BE only).
         d. LLM SDK (Anthropic / OpenAI) — canned response shapes (BE only).
       - Cross-reference `backend/docs/test-setup.md` for full recipes — this section gives the pattern; the BE doc has the verbatim shapes.

    5. **## 4. Unit vs integration runner**
       - Define the D-03 split: default `pnpm test` runs ONLY mocked unit/component tests; `pnpm test:integration` runs the opt-in real-service suite.
       - For frontend: most "integration" tests in `frontend/tests/integration/` were dead-import orphans (deleted in Plan 50-01). FE may not need a separate integration runner today; if added later (Plan 50-04 audit verdict), the same `mergeConfig` pattern from `backend/vitest.integration.config.ts` applies.
       - For backend: link to `backend/docs/test-setup.md` for `pnpm --filter intake-backend test:integration` semantics.
       - Explain why the default suite is mocked-only: clean checkouts pass tests without infrastructure; CI parallelism preserved; no flaky network in the gate.

    6. **## 5. Common pitfalls**
       - Copy verbatim the 7 pitfalls from RESEARCH §Common Pitfalls. Each pitfall: "What goes wrong" + "Why it happens" + "How to avoid" + "Warning signs".

    7. **## 6. Fixture patterns**
       - Co-located `__tests__/` directories per `TESTING.md` convention.
       - Factory functions that build mock data with deterministic defaults + spread for overrides (e.g., `createMockForm({ iso_code_2: 'KW' })`).
       - `vi.mocked(useQuery).mockReturnValue(...)` per-test override on top of a global stub.

    8. **## 7. Custom ESLint rule: vi-mock-exports-required (D-15)**
       - Brief description of the D-15 rule: it flags `vi.mock(<id>, factory)` calls whose factory return object lacks a `SpreadElement`. The intent is to force the `importActual + spread` pattern as the safe default.
       - Show an example BAD factory (object literal without spread) and an example GOOD factory (with `await vi.importActual + spread`).
       - Note that the rule lives in `eslint.config.mjs` scoped to test-file globs (Plan 50-05 ships it).
       - Anchor: `#custom-eslint-rule-vi-mock-exports-required` — Plan 50-05's lint-error message can link here.

    Style: pipe-aligned tables, fenced TypeScript code blocks, sentence-case prose. No emoji. No marketing voice ("Discover", "Easily", "Unleash" — banned per CLAUDE.md). Date format `Tue 28 Apr` if needed in prose; ISO 8601 in metadata.

    File size target: ~250-350 lines.

    **CRITICAL — VALIDATION grep gate:** Run `grep -v '^#' frontend/docs/test-setup.md | grep -cE 'vi\.importActual|initReactI18next|integration runner'` BEFORE finalizing — must be ≥5. Per the grep-gate-hygiene rule in the role spec, the count must come from NON-comment lines (the `-v '^#'` filter strips markdown headers which start with `#`). If the count falls short, add more occurrences in prose body — DO NOT pad headers, since headers start with `#` and are filtered out by the grep.

  </action>
  <verify>
    <automated>test -f frontend/docs/test-setup.md &amp;&amp; grep -v '^#' frontend/docs/test-setup.md | grep -cE 'vi\.importActual|initReactI18next|integration runner' | awk '$1 &gt;= 5 { exit 0 } { print "FAIL: only " $1 " matches in non-comment lines"; exit 1 }' &amp;&amp; grep -c "^## 1\\. Test runner architecture" frontend/docs/test-setup.md | grep -q "^1$" &amp;&amp; grep -c "^## 2\\. The react-i18next mock contract" frontend/docs/test-setup.md | grep -q "^1$" &amp;&amp; grep -c "^## 5\\. Common pitfalls" frontend/docs/test-setup.md | grep -q "^1$"</automated>
  </verify>
  <done>
    `frontend/docs/test-setup.md` exists with all 7 H2 sections. The grep-hygiene-stripped count (excluding `^#` lines) of `vi.importActual|initReactI18next|integration runner` is ≥5 per VALIDATION TEST-04. No emoji (`grep -P '[\\x{1F300}-\\x{1F9FF}]' frontend/docs/test-setup.md` returns 0 matches). No banned marketing words ("Discover", "Easily", "Unleash"). The doc cites the in-repo `vi.importActual` precedents and quotes the live setup.ts factory.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author backend/docs/test-setup.md (TEST-04 secondary)</name>
  <files>backend/docs/test-setup.md</files>
  <read_first>
    - frontend/docs/test-setup.md (POST-Task-2 state — the BE doc cross-references shared concepts here)
    - .planning/phases/50-test-infrastructure-repair/50-PATTERNS.md §`backend/docs/test-setup.md` (5-section target outline)
    - .planning/phases/50-test-infrastructure-repair/50-RESEARCH.md §Mock Factory Specs (BE) (lines 274-414 — full canonical recipes)
    - .planning/phases/50-test-infrastructure-repair/50-CONTEXT.md D-14 (BE doc is short, BE-specific only)
    - backend/tests/setup.ts (POST-Plan-50-03 state — quote the live global mocks)
    - backend/vitest.config.ts + backend/vitest.integration.config.ts (POST-Plan-50-03 state — quote the include/exclude shape)
    - backend/package.json (POST-Plan-50-03 state — quote the `test:integration` script)
  </read_first>
  <action>
    Author `backend/docs/test-setup.md` per the 5-section outline in PATTERNS. Target ~120-180 lines — short, BE-specific, heavy on cross-references to the FE doc.

    1. **Title block**:
       - `# Backend test setup`
       - `Last reviewed: 2026-05-13 (Phase 50)`
       - `Sibling doc: frontend/docs/test-setup.md (shared concepts)`
       - Short prose intro: this doc covers backend-specific concerns; for the `vi.importActual` pattern, runner architecture, and pitfalls common to both workspaces, see the sibling FE doc.

    2. **## 1. What's different about backend**
       - `environment: 'node'` (vs FE jsdom)
       - `pool: 'forks'` for full process isolation — each test file is its own subprocess; in-memory stub state (Redis Map, etc.) does NOT leak between files.
       - No MSW; backend doesn't intercept HTTP — instead it mocks the SDK clients directly (supabaseAdmin, openai, anthropic).
       - Pure ESM (`"type": "module"` in `backend/package.json`); vitest hoists `vi.mock(...)` automatically.

    3. **## 2. Global mocks in tests/setup.ts (D-07/D-08)**
       - Quote the live `backend/tests/setup.ts` shape (post-Plan-50-03) for each external dep:
         a. `@/config/supabase` — chainable `from()` builder + `rpc` + `auth` + `storage`. Show per-test refinement: `vi.mocked(supabaseAdmin.from).mockImplementation((table) => customChain)`.
         b. `ioredis` — Map-backed default + named `Redis` (CJS interop).
         c. `bullmq` — Queue, Worker, QueueEvents as `vi.fn().mockImplementation(...)`.
         d. `@anthropic-ai/sdk` — `messages.create` returning canned text.
         e. `openai` — `embeddings.create` returning 1536-dim zero vector.
         f. `@xenova/transformers` — `pipeline()` returning async embed function.
       - For each, give a fenced TypeScript snippet for per-test override (matching the recipes in `frontend/docs/test-setup.md` §3 but with BE-specific paths).

    4. **## 3. Env-var contract**
       - The `dotenv` loading order at the top of `backend/tests/setup.ts`: `.env` first, then `.env.test` overrides. `process.env.NODE_ENV = 'test'`.
       - Explain that env vars are mostly moot under global mocks — production code that READS `process.env.SUPABASE_URL` to build a Supabase client is bypassed because the client itself is mocked.
       - Real env vars are still needed for `pnpm test:integration`. Document the minimum env set (link to `.env.test.example` if it exists; if not, list `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`).

    5. **## 4. Integration runner**
       - `pnpm --filter intake-backend test:integration` invokes `backend/vitest.integration.config.ts`.
       - The config extends `backend/vitest.config.ts` via `mergeConfig`, narrowing `include` to the 4 integration directories (`tests/contract/**`, `tests/contracts/**`, `tests/integration/**`, `tests/performance/**`) with `testTimeout: 60000` (2× base).
       - **Important — global mocks inherited:** `mergeConfig` inherits `setupFiles`, so integration tests load the global mocks too. If a test needs the REAL `supabaseAdmin` (etc.), it must call `vi.unmock('@/config/supabase')` at file top. This is a documented trade-off per PATTERNS — alternative would be a second `tests/setup.integration.ts` that omits global mocks, deferred per Karpathy until needed.
       - CI: integration runner is advisory (`continue-on-error: true` in `.github/workflows/ci.yml`, NOT in `required_status_checks.contexts` per D-13).
       - Coverage: default `pnpm test --coverage` may FAIL the 80/80/75/80 thresholds (`backend/vitest.config.ts:24-27`) because the unit suite is narrow now. Recommended workaround: run `pnpm test:coverage` against unit + integration combined (a separate concern; not gated by Phase 50).

    6. **## 5. See also**
       - `frontend/docs/test-setup.md` for shared mock patterns and the `vi.importActual` precedent.
       - `.planning/phases/50-test-infrastructure-repair/50-TEST-AUDIT.md` for the phase-start failing-test inventory.

    Style: same as FE doc — pipe tables, fenced code blocks, sentence-case prose, no emoji, no marketing voice.

  </action>
  <verify>
    <automated>test -f backend/docs/test-setup.md &amp;&amp; grep -c "^## 1\\. What's different about backend" backend/docs/test-setup.md | grep -q "^1$" &amp;&amp; grep -c "^## 2\\. Global mocks in tests/setup.ts" backend/docs/test-setup.md | grep -q "^1$" &amp;&amp; grep -c "^## 4\\. Integration runner" backend/docs/test-setup.md | grep -q "^1$" &amp;&amp; grep -c "test:integration\\|mergeConfig\\|pool: 'forks'" backend/docs/test-setup.md | awk '$1 &gt;= 3 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>
    `backend/docs/test-setup.md` exists with all 5 H2 sections. Cross-references `frontend/docs/test-setup.md` for shared concepts. Documents `mergeConfig` inheritance behavior, `pool: 'forks'` isolation, and the integration runner's `vi.unmock(...)` requirement for tests that need real services. Does not duplicate FE doc content for shared concepts.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                                                                                                                                                     |
| -------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Audit artifact ↔ test exit codes | The audit's "Phase-exit state" rows are the trust anchor for downstream phases — if recorded incorrectly, Plan 50-05's branch protection PATCH could lock `main` against a falsely-green state. |
| Docs ↔ contributors              | The docs become the canonical pattern reference; an incorrect example would propagate the bug class the phase was created to fix.                                                               |

## STRIDE Threat Register

| Threat ID  | Category                         | Component                                                                                                                  | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                          |
| ---------- | -------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-50-04-01 | R (Repudiation / falsified gate) | A bad audit row could claim "frontend exits 0" when it doesn't, allowing Plan 50-05 to register a gate against red.        | mitigate    | Task 1 explicitly runs `pnpm --filter <workspace> test --run; echo $?` at task time and records the exit code + ISO timestamp. Plan 50-05 re-runs the same commands before the branch-protection PATCH (per VALIDATION §Phase Requirements → Test Map). Audit is informational; gate-register is independently verified. |
| T-50-04-02 | I (Information disclosure)       | Docs could embed env-var values, hostnames, or secrets in canonical mock recipes.                                          | mitigate    | Mock recipes use placeholder strings only (`'mock-job-id'`, `new Array(1536).fill(0)`, `'{"mock": true}'`). No real keys, no real URLs. The env-var contract section names the variables but does not embed values.                                                                                                      |
| T-50-04-03 | T (Tampering)                    | A contributor reads `frontend/docs/test-setup.md`, copies an outdated factory shape, regresses the `initReactI18next` bug. | mitigate    | The doc's canonical example is the LIVE `frontend/tests/setup.ts` factory (quoted at doc-write time). The D-15 ESLint rule (Plan 50-05) statically enforces the pattern. Two layers of defense.                                                                                                                          |

</threat_model>

<verification>
**Phase-level checks (post-plan):**

- `.planning/phases/50-test-infrastructure-repair/50-TEST-AUDIT.md` exists with YAML frontmatter (`phase`, `audited`, `status`, `scores`, `dispositions`), H2 "Frontend failures", H2 "Backend failures", H2 "Phase-exit state", H2 "Queued tech debt", H2 "Sources".
- The audit's body Sections 4 and 5 contain ≥400 distinct per-file path rows (per-row regex assertion in Task 1's verify) — compressed-by-class representations are NOT present in the body inventory.
- `frontend/docs/test-setup.md` exists; non-comment grep count of `vi.importActual|initReactI18next|integration runner` ≥5.
- `backend/docs/test-setup.md` exists with 5 H2 sections.
- Both docs have NO emoji (`grep -P '[\\x{1F300}-\\x{1F9FF}]'` returns 0).
- Both docs avoid marketing voice (`grep -iE 'discover|easily|unleash|you're in|we are excited'` returns 0).
- All upstream Plan SUMMARYs (`50-01-SUMMARY.md`, `50-02-SUMMARY.md`, `50-03-SUMMARY.md`) are cited in the audit's Sources section.
- The audit's "Phase-exit state" table shows both `pnpm --filter intake-frontend test --run` exit 0 and `pnpm --filter intake-backend test --run` exit 0 with ISO 8601 timestamps recorded at task time.
- Total artifacts created: 3 (audit + 2 docs). No production code touched.
  </verification>

<success_criteria>

- **TEST-03 SATISFIED:** `50-TEST-AUDIT.md` exists with phase-start failure inventory (≥400 distinct per-file rows in the body), dispositions, phase-exit state, queued tech debt.
- **TEST-04 SATISFIED:** `frontend/docs/test-setup.md` and `backend/docs/test-setup.md` exist; FE doc passes the ≥5 grep gate per VALIDATION.
- **Audit cross-checks:** Summary table totals match per-row counts; phase-exit state shows both workspaces green.
- **No production code touched:** This plan is doc/audit-only; `git diff --stat` shows changes only under `.planning/` and `*/docs/`.
- **Handoff ready:** Plan 50-05 can reference `frontend/docs/test-setup.md#custom-eslint-rule-vi-mock-exports-required` in its lint-error message.
  </success_criteria>

<output>
After completion, create `.planning/phases/50-test-infrastructure-repair/50-04-SUMMARY.md` with:
- Confirmation of the three artifacts (audit + 2 docs) with their final line counts
- Cross-check: audit's per-workspace failure totals (218 FE / 208 BE) match the summary table totals
- The non-comment grep count for FE doc against the VALIDATION TEST-04 gate (≥5 matches)
- The per-file uniqueness count (Task 1 verify gate ≥400) proving body rows are individual files, not class summaries
- List of tech-debt items deferred to follow-up phases or future cleanup
- Confirmation that Plan 50-05's preconditions (FE green + BE green + docs in place) are met
</output>
