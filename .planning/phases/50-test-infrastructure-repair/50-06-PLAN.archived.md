---
phase: 50-test-infrastructure-repair
plan: 06
type: execute
wave: 2
depends_on:
  - 50-01
files_modified:
  - .planning/phases/50-test-infrastructure-repair/50-02-PLAN.md
  - .planning/phases/50-test-infrastructure-repair/50-02-PLAN.archived.md
  - .planning/phases/50-test-infrastructure-repair/50-01-SUMMARY.md
  - frontend/package.json
  - frontend/tests/setup.ts
  - frontend/tests/utils/render.tsx
  - frontend/src/components/calendar/__tests__/WeekListMobile.test.tsx
  - frontend/src/routes/_protected/dossiers/forums/__tests__/ForumsListPage.test.tsx
  - frontend/tests/component/AfterActionForm.test.tsx
  - frontend/tests/component/CommitmentList.test.tsx
  - frontend/tests/component/DecisionList.test.tsx
  - frontend/tests/component/SLAIndicator.test.tsx
  - frontend/tests/component/TaskCard.test.tsx
  - frontend/tests/unit/routes.test.tsx
  - frontend/tests/unit/monitoring.dashboard.test.tsx
autonomous: true
requirements:
  - TEST-02
  - TEST-04
user_setup: []

must_haves:
  truths:
    - 'After this plan lands, the FE default runner failing-test-files count drops monotonically per task; aggregate target is ≥80% of the 34 residuals auto-cleared via the three cross-cutting setup fixes (CC-1 jest-dom, CC-2 Supabase env stubs, CC-3+CC-4 renderWithProviders).'
    - '`frontend/tests/setup.ts` imports `@testing-library/jest-dom/vitest` BEFORE any `vi.mock` runs, registering matchers `toBeInTheDocument`, `toHaveClass`, `toHaveAttribute`, `toContainElement` on chai. `@testing-library/jest-dom` is declared in `frontend/package.json` `devDependencies`.'
    - "`frontend/tests/setup.ts` calls `vi.stubEnv('VITE_SUPABASE_URL', 'http://localhost:54321')` and `vi.stubEnv('VITE_SUPABASE_ANON_KEY', 'test-anon-key')` at top-of-file (BEFORE every `vi.mock` per Pitfall 5 ordering), so `frontend/src/lib/supabase.ts:8` no longer throws `Missing Supabase environment variables` at module-eval."
    - '`frontend/tests/utils/render.tsx` exports `renderWithProviders` that wraps the subject in `LanguageProvider` and a fresh-per-render `QueryClientProvider` (CC-3 + CC-4); the 9 CC-3/CC-4 test files have their `render` import migrated to `renderWithProviders`.'
    - 'Plan 50-02 is archived as `50-02-PLAN.archived.md` with a 5-line supersession header naming 50-06/50-07/50-08 as replacements and the commit reference. `50-02-PLAN.md` no longer exists at its original path (replaced by the archived rename).'
    - "`.planning/phases/50-test-infrastructure-repair/50-01-SUMMARY.md` exists, documents the 218→34 failure-file drop, lists the 8 deleted test paths verbatim from `git show 93f56b2b --stat` (with a deviation note recording that Plan 50-01's plan body claimed ~11 deletions but the git-verified actual count is 8 — a 27% over-estimate driven by Plan 50-01 counting via RESEARCH §Failure Inventory while the executor narrowed via `git log --diff-filter=D`), confirms the 4 wizard tests pass, and includes the handoff list to 50-06/07/08."
    - 'No test file touched by this plan exits in `queued-with-rationale` state for the FE default runner per the operator gap brief amendment to D-01. CC-1/CC-2/CC-3/CC-4 mechanically clear failures by registering matchers, stubbing env, and wrapping render contexts — no per-file rationale carve-outs are introduced. Any file that genuinely cannot be cleared by these CC mechanisms is handed off to Plan 50-08 for D-10 archaeology, not queued.'
  artifacts:
    - path: 'frontend/tests/setup.ts'
      provides: 'Test setup with jest-dom matchers registered (CC-1) + Supabase env stubs (CC-2) layered before the existing react-i18next mock'
      contains: '@testing-library/jest-dom/vitest'
    - path: 'frontend/package.json'
      provides: '`@testing-library/jest-dom` declared in devDependencies (matcher source)'
      contains: '@testing-library/jest-dom'
    - path: 'frontend/tests/utils/render.tsx'
      provides: 'renderWithProviders helper wrapping LanguageProvider + fresh QueryClientProvider (CC-3 + CC-4)'
      contains: 'renderWithProviders'
    - path: '.planning/phases/50-test-infrastructure-repair/50-02-PLAN.archived.md'
      provides: 'Archived Plan 50-02 with supersession header'
      contains: 'Superseded by 50-06'
    - path: '.planning/phases/50-test-infrastructure-repair/50-01-SUMMARY.md'
      provides: 'Plan 50-01 handoff summary (cascade-fix dispositions + 8 git-verified deleted files [11→8 deviation noted] + residual 34-file handoff)'
      contains: '218'
  key_links:
    - from: 'frontend/tests/setup.ts'
      to: 'every FE test file using jest-dom matchers'
      via: 'side-effect import `@testing-library/jest-dom/vitest` registers chai matchers globally before any test executes'
      pattern: 'jest-dom/vitest'
    - from: 'frontend/tests/setup.ts vi.stubEnv calls'
      to: 'frontend/src/lib/supabase.ts:8 env-var guard'
      via: "`vi.stubEnv('VITE_SUPABASE_URL', ...)` runs at setup-eval before any test imports `supabase.ts`, satisfying the module-eval check"
      pattern: "vi\\.stubEnv\\('VITE_SUPABASE_URL'"
    - from: 'frontend/tests/utils/render.tsx renderWithProviders'
      to: '9 migrated test files'
      via: "Each migrated file replaces `import { render } from '@testing-library/react'` (or equivalent) with `import { renderWithProviders } from '@tests/utils/render'` and updates call sites"
      pattern: 'renderWithProviders'
---

<objective>
Land the three cross-cutting setup gap fixes that auto-clear ~80% of the 34 residual FE failing files identified in the operator gap brief (per `.continue-here.md`):

1. **CC-1 — register `@testing-library/jest-dom` matchers globally** (≥8 visibly-failing files + likely more once cascades clear). Add `@testing-library/jest-dom` to `frontend/package.json` devDependencies; add `import '@testing-library/jest-dom/vitest'` at the TOP of `frontend/tests/setup.ts` before any `vi.mock` runs (Pitfall 5 ordering).

2. **CC-2 — stub Supabase env vars at setup-eval time** (6 files blowing up at module-eval). Add two `vi.stubEnv(...)` calls near the top of `frontend/tests/setup.ts` so `frontend/src/lib/supabase.ts:8` no longer throws `Missing Supabase environment variables`.

3. **CC-3 + CC-4 — create `renderWithProviders` test util + migrate the 9 affected files** (8 CC-3 + 1 CC-4 file). New file `frontend/tests/utils/render.tsx` exports `renderWithProviders` that wraps children in `LanguageProvider` and a fresh `QueryClientProvider`. Each of the 9 listed test files updates its `render(<X/>)` call sites to `renderWithProviders(<X/>)`.

Plus two housekeeping items REQUIRED by the operator gap brief:

4. **Archive `50-02-PLAN.md`** — rename to `50-02-PLAN.archived.md` and prepend a supersession header naming 50-06/07/08 (operator gap brief item 1).
5. **Write `50-01-SUMMARY.md`** — Plan 50-01 cascade fix is committed (`93f56b2b`) but has no SUMMARY, so `gsd-execute-phase` could re-attempt the cascade fix. This plan authors the SUMMARY using `git show 93f56b2b --stat` for the deleted-file list and live runner output for the post-state (operator gap brief item 2).

Per the gap brief, these three CC fixes are the highest-leverage work in the new Wave 2 — they collapse the 34-file residual to ~7 files of genuine per-file drift (which Plan 50-08 finishes). 50-06 and 50-07 are independent and can run in parallel.

**Exit-0 is NOT achieved at this plan's end** — that is the cumulative phase-exit contract across 50-06 + 50-07 + 50-08. This plan's done state is monotonic decrease in failing-files count after each task, AND verified completion of the four CC fixes + two housekeeping items.

Purpose: Replace the blocked Plan 50-02 with a bounded execution unit that handles the cross-cutting structural gaps in the FE test infrastructure. After 50-06+50-07 land, the residual file set is small enough for Plan 50-08 to triage per-file under the ≤12-file ceiling.

Output:

- `frontend/package.json` with `@testing-library/jest-dom` in devDependencies
- `frontend/tests/setup.ts` augmented with jest-dom side-effect import + 2 `vi.stubEnv` calls (existing react-i18next mock preserved verbatim per Plan 50-01)
- `frontend/tests/utils/render.tsx` new test util exporting `renderWithProviders`
- 9 test files migrated to `renderWithProviders`
- `50-02-PLAN.md` archived as `50-02-PLAN.archived.md` with header
- `.planning/phases/50-test-infrastructure-repair/50-01-SUMMARY.md` authored from `git show 93f56b2b` evidence
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-test-infrastructure-repair/50-CONTEXT.md
@.planning/phases/50-test-infrastructure-repair/50-RESEARCH.md
@.planning/phases/50-test-infrastructure-repair/50-PATTERNS.md
@.planning/phases/50-test-infrastructure-repair/50-VALIDATION.md
@.planning/phases/50-test-infrastructure-repair/.continue-here.md
@.planning/phases/50-test-infrastructure-repair/50-01-PLAN.md
@.planning/phases/50-test-infrastructure-repair/50-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Do not re-explore the codebase. -->

From `frontend/tests/setup.ts` (POST-Plan-50-01 state — see commit `93f56b2b`):

- Line 1-3: imports (`beforeAll, afterEach, afterAll, vi` from 'vitest'; `cleanup` from '@testing-library/react'; `server` from './mocks/server')
- Lines 6-81: `vi.mock('react-i18next', async () => { const actual = await vi.importActual<typeof import('react-i18next')>('react-i18next'); return { ...actual, useTranslation: ..., Trans: ... } })` — KEEP VERBATIM, this is Plan 50-01's D-05 fix
- Lines 83-93: MSW lifecycle (`beforeAll`/`afterEach`/`afterAll`) — KEEP VERBATIM
- Local style: semicolons + double quotes (Plan 50-01 PATTERNS — DO NOT reformat)

The CC-1 + CC-2 additions go in two NEW blocks, placed precisely:

- jest-dom side-effect import: NEW line 1 (above `import { beforeAll ... } from 'vitest'`) OR after the existing imports but BEFORE the `vi.mock` call. The side-effect-import-first convention (top-of-file) matches the standard jest-dom recipe; placing it after vitest imports is also valid because side-effect imports run after declaration imports in the same module. Pick top-of-file for clarity.
- `vi.stubEnv` calls: AFTER all imports but BEFORE the `vi.mock('react-i18next', ...)` call. Per Pitfall 5 (mock-factories evaluate before in-body code? no — `vi.mock` is hoisted, but `vi.stubEnv` at module-eval is OK because it's not consumed inside the mock factory itself — it's read by transitive `supabase.ts` imports at consumer-test-eval time). Place as a small block: `vi.stubEnv('VITE_SUPABASE_URL', 'http://localhost:54321'); vi.stubEnv('VITE_SUPABASE_ANON_KEY', 'test-anon-key');` immediately after the import block.

From `frontend/src/lib/supabase.ts` (line 8 — the throw site):

- Module-level code that reads `import.meta.env.VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` and throws `Missing Supabase environment variables (VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY)` if either is undefined.
- `vi.stubEnv(...)` populates `import.meta.env` for vitest — verified Vitest API. Stubs are scoped to the test file lifecycle; vitest auto-restores on test-file teardown. No need for an explicit `vi.unstubAllEnvs()` here because setup.ts stubs are intentionally global for the runner lifecycle.

From `frontend/package.json` (CURRENT — must verify):

- `devDependencies` block: `@testing-library/react`, `@testing-library/user-event`, `vitest`, `msw`, etc. — confirm shape via Read before editing.
- The version of `@testing-library/jest-dom` to add: use `^6.5.0` (latest stable as of 2026 per repo standard practice). Use `pnpm add -D -F intake-frontend @testing-library/jest-dom` so pnpm resolves the latest matching the workspace constraint.

From `frontend/src/contexts/LanguageContext.tsx` (consumer that throws `useLanguage must be used within a LanguageProvider`):

- Exports `LanguageProvider` (React component wrapping i18next + RTL) and `useLanguage()` hook.
- Path import: `import { LanguageProvider } from '@/contexts/LanguageContext'` (project alias `@` = `frontend/src/` per Plan 50-01 PATTERNS).

The new `frontend/tests/utils/render.tsx` shape (CC-3 + CC-4 helper):

- File path: `frontend/tests/utils/render.tsx` (NEW directory `frontend/tests/utils/` — confirm absent before creating).
- Exports `renderWithProviders` and re-exports everything from `@testing-library/react` (so callers can `import { renderWithProviders, screen, fireEvent } from '@tests/utils/render'`).
- Signature: `export function renderWithProviders(ui: ReactElement, options?: { queryClient?: QueryClient }): RenderResult` (typed; explicit return type per project ESLint rule `@typescript-eslint/explicit-function-return-type`).
- Internally builds a fresh `new QueryClient({ defaultOptions: { queries: { retry: false } } })` per render, wraps `ui` in `<QueryClientProvider client={queryClient}><LanguageProvider>{children}</LanguageProvider></QueryClientProvider>`, then calls `render(...)` from `@testing-library/react`.
- Style: NO semicolons (root Prettier `semi: false` — `frontend/tests/utils/` is a new directory, NOT covered by setup.ts's local semicolon style; new files follow root Prettier).
- Single-quote strings, trailing commas all, arrow parens always.

From `.continue-here.md` and gap-brief residual inventory — the 9 files migrating to `renderWithProviders`:

CC-3 (LanguageProvider missing):

1. `frontend/src/components/calendar/__tests__/WeekListMobile.test.tsx`
2. `frontend/src/routes/_protected/dossiers/forums/__tests__/ForumsListPage.test.tsx` (also CC-1 — jest-dom matchers needed; both CC-1 + CC-3 close once both fixes land)
3. `frontend/tests/component/AfterActionForm.test.tsx`
4. `frontend/tests/component/CommitmentList.test.tsx` (also CC-1)
5. `frontend/tests/component/DecisionList.test.tsx`
6. `frontend/tests/component/SLAIndicator.test.tsx`
7. `frontend/tests/component/TaskCard.test.tsx`
8. `frontend/tests/unit/routes.test.tsx` (also CC-2 — supabase env)

CC-4 (QueryClient missing): 9. `frontend/tests/unit/monitoring.dashboard.test.tsx`

Some files appear in multiple CC buckets (e.g., `ForumsListPage` is CC-1 + CC-3). The shared `renderWithProviders` migration closes CC-3 + CC-4 mechanically; CC-1 and CC-2 close via the setup.ts edits regardless of whether the file uses the new render helper.

From `git show 93f56b2b --stat` (verified at planning time, for Task 4 SUMMARY content):

Deleted (verified by `git show` with `-` line-count > 0 and `0` insertions):

1. `frontend/tests/a11y/theme-selector.test.tsx` (366 lines)
2. `frontend/tests/integration/language-switch.test.tsx` (371 lines)
3. `frontend/tests/integration/registry-validation.test.tsx` (67 lines)
4. `frontend/tests/integration/test_cross_tab_sync.test.tsx` (302 lines)
5. `frontend/tests/integration/test_language_switch.test.tsx` (240 lines)
6. `frontend/tests/integration/test_preference_persistence.test.tsx` (259 lines)
7. `frontend/tests/unit/LanguageToggle.test.tsx` (263 lines)
8. `frontend/tests/unit/components.test.tsx` (647 lines)

Modified (not deleted):

- `frontend/src/components/__tests__/ConsistencyPanel.test.tsx` (2 lines changed)
- `frontend/tests/setup.ts` (174 lines, both ±)
- `frontend/tests/unit/hooks/responsive.test.ts` (375 lines, both ±)
- `frontend/vitest.config.ts` (27 lines, both ±)

Note: Plan 50-01's plan body claimed 11 deleted files; the actual count is 8 per `git show`. The SUMMARY (Task 4) MUST record the actual 8, not the planned 11, with a brief deviation note.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive 50-02-PLAN.md + add supersession header</name>
  <files>.planning/phases/50-test-infrastructure-repair/50-02-PLAN.md, .planning/phases/50-test-infrastructure-repair/50-02-PLAN.archived.md</files>
  <read_first>
    - .planning/phases/50-test-infrastructure-repair/50-02-PLAN.md (full file — to confirm it is the version with `discovery ceiling >15 = STOP` and `~5-15` design budget that the operator brief flags as blocked)
    - .planning/phases/50-test-infrastructure-repair/.continue-here.md (operator gap brief — output_contract item 3 specifies rename + 5-line header + supersession note)
  </read_first>
  <action>
    Two-step archive:

    1. Rename `.planning/phases/50-test-infrastructure-repair/50-02-PLAN.md` to `.planning/phases/50-test-infrastructure-repair/50-02-PLAN.archived.md` using `git mv` (so rename is staged and history is preserved).

    2. Prepend a 5-line supersession header to `50-02-PLAN.archived.md` ABOVE the existing YAML frontmatter block. The header lines are plain text (NOT YAML, NOT a fenced block) and read:
       - Line 1: `> ARCHIVED 2026-05-14: Superseded by Plan 50-06, 50-07, 50-08.`
       - Line 2: `> Reason: Discovery ceiling exceeded — design budget was ~5-15 residual failing FE files; actual residual after Plan 50-01 was 34 files (operator brief `.continue-here.md`).`
       - Line 3: `> Replacement plans split the residual into bounded sub-plans per `.continue-here.md` ratified path (≤12 files each).`
       - Line 4: `> Commit that recommended this archive: see Task 1 of 50-06-PLAN.md.`
       - Line 5: a blank line separating the header from the original YAML.

    Do NOT modify any content below the 5-line header — the original plan body, frontmatter, tasks, threat model, verification, success criteria, and output sections remain byte-identical so reviewers can audit what was superseded.

    Do NOT delete `50-02-PLAN.md` at its old path independently — the `git mv` rename IS the deletion at the old path.

  </action>
  <verify>
    <automated>test ! -f .planning/phases/50-test-infrastructure-repair/50-02-PLAN.md &amp;&amp; test -f .planning/phases/50-test-infrastructure-repair/50-02-PLAN.archived.md &amp;&amp; head -5 .planning/phases/50-test-infrastructure-repair/50-02-PLAN.archived.md | grep -c "Superseded by Plan 50-06, 50-07, 50-08" | grep -q "^1$" &amp;&amp; head -5 .planning/phases/50-test-infrastructure-repair/50-02-PLAN.archived.md | grep -c "ARCHIVED 2026-05-14" | grep -q "^1$"</automated>
  </verify>
  <done>
    `50-02-PLAN.md` no longer exists at its original path. `50-02-PLAN.archived.md` exists with a 5-line supersession header above the original YAML. `git status` shows the rename as `R` (renamed) and the modification as the header prepend. No other content changed in the archived file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register jest-dom matchers (CC-1) — add dep + side-effect import in setup.ts</name>
  <files>frontend/package.json, frontend/tests/setup.ts</files>
  <read_first>
    - frontend/package.json (full file — verify current `devDependencies` block; confirm `@testing-library/jest-dom` is NOT already present per the operator gap brief)
    - frontend/tests/setup.ts (full file — POST-Plan-50-01 shape, see commit `93f56b2b` — lines 1-3 imports, lines 6-81 react-i18next mock per D-05, lines 83-93 MSW lifecycle)
    - .planning/phases/50-test-infrastructure-repair/.continue-here.md §CC-1 (the 8 visibly-failing files + scope explanation)
    - .planning/phases/50-test-infrastructure-repair/50-RESEARCH.md (search for any existing jest-dom imports in the monorepo before authoring — RESEARCH §Common Pitfalls Pitfall 5 about setup ordering)
    - .planning/phases/50-test-infrastructure-repair/50-CONTEXT.md D-09 (root-cause fixes allowed — adding the missing dependency is the root cause for CC-1)
  </read_first>
  <action>
    Two-file change:

    **A) `frontend/package.json` — add `@testing-library/jest-dom` to devDependencies.**

    Use the workspace pnpm command: `pnpm add -D -F intake-frontend @testing-library/jest-dom`. This writes the dependency into `frontend/package.json` `devDependencies` at the version pnpm resolves (expect `^6.x` per current ecosystem). The command also updates `pnpm-lock.yaml` automatically — do NOT hand-edit the lockfile.

    BEFORE running the install, grep the monorepo for any existing `jest-dom` import to confirm the executor isn't overwriting a different prior decision: `grep -rln "jest-dom" frontend backend shared --include='*.ts' --include='*.tsx' --include='*.json' 2>/dev/null`. Expected: zero hits (or hits only in this plan's text). If hits exist in production code, STOP and surface to the operator — the CC-1 diagnosis may be incomplete.

    **B) `frontend/tests/setup.ts` — add side-effect import at top.**

    Insert ONE new line at the TOP of the file (above the existing `import { beforeAll, afterEach, afterAll, vi } from 'vitest'` line):

    `import '@testing-library/jest-dom/vitest'`

    The `/vitest` sub-path is the canonical vitest-specific entry point for jest-dom v6+ (registers matchers on chai via `expect.extend(...)` and adds TypeScript type augmentation). DO NOT use the bare `'@testing-library/jest-dom'` import — that variant requires manual `expect.extend(matchers)` and is the older Jest-specific path.

    Match local style of setup.ts: this file uses DOUBLE-QUOTED strings + semicolons per Plan 50-01 PATTERNS. Write the new line as `import "@testing-library/jest-dom/vitest";` (double quotes, trailing semicolon) to match. The new import is the FIRST line of the file; the existing imports follow on subsequent lines without other changes.

    Do NOT touch the existing react-i18next mock factory (lines 6-81 post-50-01), the `translations` map, or the MSW lifecycle (lines 83-93). Those are Plan 50-01's deliverables and must remain byte-identical.

    After the install + edit, run the verify command. The grep MUST find exactly one occurrence of the new import in setup.ts and one occurrence of the new dep in package.json.

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -c '"@testing-library/jest-dom"' package.json | awk '$1 &gt;= 1 { exit 0 } { exit 1 }' &amp;&amp; grep -c "@testing-library/jest-dom/vitest" tests/setup.ts | grep -q "^1$" &amp;&amp; head -1 tests/setup.ts | grep -qE "import [\"']@testing-library/jest-dom/vitest[\"']" &amp;&amp; pnpm exec vitest --run --reporter=verbose tests/component/ConsistencyPanel.test.tsx 2>&amp;1 | tail -10 | grep -qvE "Invalid Chai property: toBeInTheDocument"</automated>
  </verify>
  <done>
    `frontend/package.json` lists `@testing-library/jest-dom` in `devDependencies`. `frontend/tests/setup.ts` line 1 is `import "@testing-library/jest-dom/vitest";`. The existing react-i18next mock + MSW lifecycle blocks are byte-identical to their post-50-01 state. Running any single file from the CC-1 inventory no longer produces `Invalid Chai property: toBeInTheDocument` errors. The FE runner failing-files count is DOWN from 34 (capture pre/post counts in commit message).
  </done>
</task>

<task type="auto">
  <name>Task 3: Stub Supabase env vars at setup-eval (CC-2)</name>
  <files>frontend/tests/setup.ts</files>
  <read_first>
    - frontend/tests/setup.ts (POST-Task-2 state — must have the new jest-dom import at line 1, then existing imports, then existing mock + lifecycle)
    - frontend/src/lib/supabase.ts (lines 1-15 — the env-var throw site; confirm the exact var names `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` and the throw message)
    - .planning/phases/50-test-infrastructure-repair/.continue-here.md §CC-2 (the 6 affected files + scope)
    - .planning/phases/50-test-infrastructure-repair/50-RESEARCH.md §Common Pitfalls Pitfall 5 (mock-factory ordering — `vi.stubEnv` runs at setup-eval time and is consumed by transitive imports at test-eval time, so ordering before `vi.mock` is sufficient)
  </read_first>
  <action>
    Single-file edit. Insert TWO new statements into `frontend/tests/setup.ts` AFTER the import block (after the last `import ...` line) and BEFORE the `vi.mock('react-i18next', ...)` call (currently around line 6 post-50-01).

    The two new statements, exactly:

    ```
    vi.stubEnv("VITE_SUPABASE_URL", "http://localhost:54321");
    vi.stubEnv("VITE_SUPABASE_ANON_KEY", "test-anon-key");
    ```

    (Match the file's local style: double quotes + semicolons per PATTERNS.)

    Placement rationale (Pitfall 5):
    - Vitest hoists `vi.mock(...)` calls to the TOP of the module at transform time, so the mock factory itself doesn't see the `vi.stubEnv` effect — but it doesn't need to. The transitive consumer (`frontend/src/lib/supabase.ts:8`) reads `import.meta.env` at module-eval time, which happens when a TEST file imports it, by which point the test runner has executed `setup.ts` synchronously and the stubs are in place.
    - Placing the stubs BEFORE `vi.mock` (rather than after) is purely a readability choice; functionally they could go after as well. Choose before for clarity.

    The two stubbed values are placeholders only:
    - `VITE_SUPABASE_URL` = `http://localhost:54321` — the standard local Supabase port. No real Supabase runs in unit tests; the env var only needs to be non-empty to satisfy the module-eval guard.
    - `VITE_SUPABASE_ANON_KEY` = `test-anon-key` — a non-empty placeholder.

    Per Threat T-50-06-02 below: `vi.stubEnv` is scoped to the test runner lifecycle; vitest auto-restores `import.meta.env` on runner teardown. No `vi.unstubAllEnvs()` call needed in `afterAll` — the runner handles it.

    Do NOT add `vi.unstubAllEnvs()` to `afterEach` — that would re-throw the env-missing error mid-suite.

    Do NOT change anything else in setup.ts. Do NOT reorder imports. Do NOT touch the react-i18next mock or the MSW lifecycle.

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -c 'vi\.stubEnv("VITE_SUPABASE_URL"' tests/setup.ts | grep -q "^1$" &amp;&amp; grep -c 'vi\.stubEnv("VITE_SUPABASE_ANON_KEY"' tests/setup.ts | grep -q "^1$" &amp;&amp; pnpm exec vitest --run --reporter=verbose tests/unit/routes.test.tsx 2>&amp;1 | tail -10 | grep -qvE "Missing Supabase environment variables"</automated>
  </verify>
  <done>
    `frontend/tests/setup.ts` contains exactly one `vi.stubEnv("VITE_SUPABASE_URL", ...)` and one `vi.stubEnv("VITE_SUPABASE_ANON_KEY", ...)` call, both placed before `vi.mock('react-i18next', ...)`. Running `pnpm exec vitest --run tests/unit/routes.test.tsx` no longer fails with `Missing Supabase environment variables`. FE runner failing-files count is DOWN from the post-Task-2 count (capture pre/post in commit message).
  </done>
</task>

<task type="auto">
  <name>Task 4: Create renderWithProviders test util + migrate 9 files (CC-3 + CC-4)</name>
  <files>frontend/tests/utils/render.tsx, frontend/src/components/calendar/__tests__/WeekListMobile.test.tsx, frontend/src/routes/_protected/dossiers/forums/__tests__/ForumsListPage.test.tsx, frontend/tests/component/AfterActionForm.test.tsx, frontend/tests/component/CommitmentList.test.tsx, frontend/tests/component/DecisionList.test.tsx, frontend/tests/component/SLAIndicator.test.tsx, frontend/tests/component/TaskCard.test.tsx, frontend/tests/unit/routes.test.tsx, frontend/tests/unit/monitoring.dashboard.test.tsx</files>
  <read_first>
    - frontend/src/contexts/LanguageContext.tsx (confirm exports `LanguageProvider` + `useLanguage`; note the import path `@/contexts/LanguageContext`)
    - frontend/tsconfig.json + frontend/vitest.config.ts (POST-Plan-50-01) — confirm `@/` alias resolves to `frontend/src/` and `@tests/` alias resolves to `frontend/tests/` (Plan 50-01 left these unchanged)
    - Each of the 9 files listed under <files> — read the current `render(...)` call sites + imports so the migration is mechanical and not breaking other assertions
    - .planning/phases/50-test-infrastructure-repair/.continue-here.md §CC-3 (8 files) and §CC-4 (1 file)
    - .planning/phases/50-test-infrastructure-repair/50-RESEARCH.md §Mock Factory Specs (TanStack Query mock conventions — `new QueryClient({ defaultOptions: { queries: { retry: false } } })` is the canonical disable-retries pattern for unit tests)
    - CLAUDE.md §Conventions (project code style: NO semicolons per root Prettier `semi: false`; single quotes; explicit return types required by `@typescript-eslint/explicit-function-return-type`)
  </read_first>
  <action>
    Two-step change:

    **A) Create `frontend/tests/utils/render.tsx` (NEW file).**

    The file exports `renderWithProviders` and re-exports everything from `@testing-library/react`. Contents specification (write following project conventions — NO semicolons, single quotes, explicit return types, arrow parens always):

    - Imports needed:
      - `import { ReactElement, ReactNode } from 'react'`
      - `import { render, RenderResult } from '@testing-library/react'`
      - `import { QueryClient, QueryClientProvider } from '@tanstack/react-query'`
      - `import { LanguageProvider } from '@/contexts/LanguageContext'`
    - Helper `createTestQueryClient` (small internal function, NOT exported): returns `new QueryClient({ defaultOptions: { queries: { retry: false }, mutations: { retry: false } } })`. Explicit return type `QueryClient`.
    - Exported `renderWithProviders`:
      - Signature: `export function renderWithProviders(ui: ReactElement, options?: { queryClient?: QueryClient }): RenderResult`
      - Body: `const client = options?.queryClient ?? createTestQueryClient(); return render(ui, { wrapper: ({ children }: { children: ReactNode }) => <QueryClientProvider client={client}><LanguageProvider>{children}</LanguageProvider></QueryClientProvider> })`
      - Explicit return type `RenderResult` on the function signature.
    - Re-export everything from `@testing-library/react` so callers can `import { renderWithProviders, screen, fireEvent, waitFor } from '@tests/utils/render'`: add `export * from '@testing-library/react'` as the last line.

    Confirm `frontend/tests/utils/` is a new directory (no existing files). Use `mkdir -p frontend/tests/utils` before writing.

    Style verification: run `pnpm --filter intake-frontend exec prettier --check tests/utils/render.tsx` after writing — must exit 0.

    **B) Migrate each of the 9 files** listed under `<files>` (excluding the new `render.tsx` itself).

    For each file:
    1. Locate the existing `import { render, screen, ... } from '@testing-library/react'` line (or equivalent).
    2. Replace with `import { renderWithProviders as render, screen, ... } from '@tests/utils/render'`. Using the `as render` alias keeps every call site (`render(<X/>)`) unchanged — minimal diff, maximum migration mechanicality.
    3. If the test file already imports `render` from `@testing-library/react` AND ALSO imports `LanguageProvider` or `QueryClientProvider` to wrap manually, REMOVE those manual wrappers — `renderWithProviders` already provides them. Test the file after the migration to confirm assertions still pass.
    4. If the test file uses `renderHook` from `@testing-library/react`, it needs a separate `renderHookWithProviders` helper — for THIS plan, leave `renderHook` calls alone and only migrate `render` calls. If a CC-3/CC-4 file uses `renderHook` exclusively and not `render`, escalate to Plan 50-08 (per-file drift) and move the file from this task's hot list.

    File-by-file:
    1. `frontend/src/components/calendar/__tests__/WeekListMobile.test.tsx` — replace render import.
    2. `frontend/src/routes/_protected/dossiers/forums/__tests__/ForumsListPage.test.tsx` — replace render import; this file also relies on CC-1 (jest-dom matchers); both close once Tasks 2+4 land.
    3. `frontend/tests/component/AfterActionForm.test.tsx` — replace render import.
    4. `frontend/tests/component/CommitmentList.test.tsx` — replace render import. CRITICAL: this file already has a per-file `vi.mock('react-i18next', ...)` at line 451 (the in-repo precedent that informs setup.ts's D-05 mock). Per Plan 50-01 PATTERNS the per-file mock is intentional and must be preserved; only the `render` import line changes.
    5. `frontend/tests/component/DecisionList.test.tsx` — replace render import.
    6. `frontend/tests/component/SLAIndicator.test.tsx` — replace render import.
    7. `frontend/tests/component/TaskCard.test.tsx` — replace render import.
    8. `frontend/tests/unit/routes.test.tsx` — replace render import. This file also relies on CC-2 (Supabase env stubs); both close once Tasks 3+4 land.
    9. `frontend/tests/unit/monitoring.dashboard.test.tsx` — replace render import. This file's failure was `No QueryClient set` (CC-4); the new helper's `QueryClientProvider` wrap closes it.

    Per-file commit cadence: ONE commit covering all 9 file migrations is acceptable (each migration is one-line). The commit message lists the 9 paths. Use `fix(50-06): migrate 9 FE tests to renderWithProviders (CC-3 + CC-4)`.

    Lint check: after all 9 migrations, run `pnpm --filter intake-frontend lint --max-warnings=0` against the migrated paths to confirm no new violations. The new `tests/utils/render.tsx` MUST also pass lint.

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; test -f tests/utils/render.tsx &amp;&amp; grep -c "export function renderWithProviders" tests/utils/render.tsx | grep -q "^1$" &amp;&amp; grep -c "LanguageProvider" tests/utils/render.tsx | grep -q "^1$" &amp;&amp; grep -c "QueryClientProvider" tests/utils/render.tsx | grep -q "^1$" &amp;&amp; grep -lr "renderWithProviders" tests/component/AfterActionForm.test.tsx tests/component/CommitmentList.test.tsx tests/component/DecisionList.test.tsx tests/component/SLAIndicator.test.tsx tests/component/TaskCard.test.tsx tests/unit/routes.test.tsx tests/unit/monitoring.dashboard.test.tsx src/components/calendar/__tests__/WeekListMobile.test.tsx src/routes/_protected/dossiers/forums/__tests__/ForumsListPage.test.tsx 2>&amp;1 | wc -l | awk '$1 == 9 { exit 0 } { print "FAIL: only " $1 " of 9 files migrated"; exit 1 }' &amp;&amp; pnpm exec vitest --run --reporter=default 2>&amp;1 | tail -5 | grep -E "Test Files" | awk '{ for(i=1;i&lt;=NF;i++) if ($i ~ /failed/) { prev=$(i-1); gsub(/[^0-9]/, "", prev); if (prev+0 &lt; 15) { print "PASS: residual " prev " failed files (≤15 after CC-1+CC-2+CC-3+CC-4)"; exit 0 } else { print "FAIL: " prev " failed files, expected ≤15"; exit 1 } } }'</automated>
  </verify>
  <done>
    `frontend/tests/utils/render.tsx` exists with `renderWithProviders` exporting + re-exports of `@testing-library/react`. All 9 listed files import `renderWithProviders` (verified by grep). Workspace lint passes (`pnpm --filter intake-frontend lint`). FE runner failing-files count is ≤15 after CC-1+CC-2+CC-3+CC-4 land (expected ≤10 per the gap brief estimate). **Cross-plan ceiling constraint:** combined with Plan 50-07's outlier closures (4 files), the residual passed to Plan 50-08 MUST be ≤12 — otherwise Plan 50-08 Task 0 will HALT and escalate per its discovery upper-bound check. If post-50-06 residual is 13–15, Plan 50-07 alone may not close enough to drop ≤12; in that case surface to operator before 50-08 starts.
  </done>
</task>

<task type="auto">
  <name>Task 5: Author 50-01-SUMMARY.md from git show 93f56b2b evidence</name>
  <files>.planning/phases/50-test-infrastructure-repair/50-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/50-test-infrastructure-repair/50-01-PLAN.md (full file — to extract goal statement, must_haves, requirements list)
    - $HOME/.claude/get-shit-done/templates/summary.md (template structure — YAML frontmatter, "Phase X Summary" H1, Performance, Accomplishments, Task Commits, Files Created/Modified, Decisions Made, Deviations from Plan)
    - .planning/phases/50-test-infrastructure-repair/.continue-here.md (operator brief — confirms commit `93f56b2b` is Plan 50-01's cascade fix and that no summary exists yet)
    - .planning/phases/50-test-infrastructure-repair/50-03-SUMMARY.md (sibling SUMMARY — style/format reference; do NOT copy content, only structure)
    - run `git show 93f56b2b --stat` at task time — to verify the file-change list matches the canonical 8-deleted + 4-modified observed at planning time
    - run `pnpm --filter intake-frontend test --run 2>&amp;1 | tail -10` at task time — to capture the post-state failing-file count for the SUMMARY's "Final State" section
  </read_first>
  <action>
    Author `.planning/phases/50-test-infrastructure-repair/50-01-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Sections required, in order:

    **1. YAML frontmatter:**
    - `phase: 50-test-infrastructure-repair`
    - `plan: 01`
    - `subsystem: testing`
    - `tags: [vitest, jest-dom, react-i18next, supabase, testing-infrastructure]`
    - `requires:` — block listing nothing (Plan 50-01 was Wave 1, no upstream)
    - `provides:` — bullet list: `Frontend vitest collection narrowed to *.test.{ts,tsx}`, `react-i18next mock D-05 factory using importActual spread`, `8 dead-import test files deleted`, `4 wizard tests pass green`
    - `affects: [50-06, 50-07, 50-08]` — the new Wave 2 plans depend on this
    - `tech-stack:` block with `added: []` and `patterns: [vi.importActual + spread pattern for SDK mocks]`
    - `key-files:` block with `created: []` and `modified: [frontend/vitest.config.ts, frontend/tests/setup.ts]`
    - `key-decisions:` — bullet list: `Use vi.importActual<typeof import('react-i18next')>('react-i18next') TYPED spread per D-05`, `Preserve 60-key translations map verbatim per D-06`, `Delete dead-import tests vs updating to new module paths`
    - `patterns-established:` — bullet list: `D-05: vi.importActual + spread + override for all SDK mocks where production module has multiple consumed exports`
    - `requirements-completed: [TEST-01, TEST-02]` — copied from `50-01-PLAN.md` frontmatter `requirements` field
    - `duration:` — approximate based on commit timestamp; SUMMARY records `~unknown — backfilled` if not measurable
    - `completed: 2026-05-14` — the commit date of `93f56b2b`

    **2. H1 + opener:**
    - H1: `# Phase 50-01: Frontend Test Collection Cascades Repair Summary`
    - One-liner: `Three cascade-clearing fixes (narrow vitest globs, register react-i18next importActual spread mock, delete 8 dead-import test files) dropped frontend failing test files from 218 to 34 in a single commit (~85% reduction).`

    **3. Performance block:**
    - Duration: `~unknown — Plan 50-01 SUMMARY backfilled by Plan 50-06 Task 5`
    - Started: `before 2026-05-14T09:21Z (commit author date)`
    - Completed: `2026-05-14T06:21Z (commit `93f56b2b` UTC equivalent of 09:21 +0300)`
    - Tasks: `3 completed (G1 vitest config, G2 setup.ts factory, G3 dead-import cleanup)`
    - Files modified: `4` (vitest.config.ts, tests/setup.ts, ConsistencyPanel.test.tsx, responsive.test.ts) plus `8` deletions

    **4. Accomplishments:**
    - G1 cascade closed: vitest no longer collects Playwright `.spec.ts` files (175 files cleared from collection)
    - G2 cascade closed: react-i18next mock factory uses D-05 `vi.importActual` + spread pattern; `initReactI18next` and 12+ other exports are now real exports inside test consumers (20 files cleared)
    - G3 cascade closed: 8 dead-import test files deleted (legacy theme-provider, deleted DossierCard / LanguageToggle / `src/i18n/config` consumers from Phases 33/34/36)
    - 4 wizard tests now pass green (`CreateWizardShell` × 2 + `SharedBasicInfoStep` × 1 + `useCreateDossierWizard` × 1) — TEST-02's original audit anchor SATISFIED
    - FE failing files: 218 → 34 (verified by re-running `pnpm --filter intake-frontend test --run` at SUMMARY write time)

    **5. Task Commits:**
    - All three tasks landed in a single commit: `93f56b2b — fix(50-01): repair frontend test collection cascades`
    - The single-commit consolidation deviates from Plan 50-01's documented "ONE commit per task" preference; record in §Deviations.

    **6. Files Created/Modified:**

    Use the canonical 8-deleted list from `git show 93f56b2b --stat` verified at planning time (also verified at task time):

    Modified:
    - `frontend/vitest.config.ts` (27 lines changed, ±) — added include/exclude for vitest collection
    - `frontend/tests/setup.ts` (174 lines changed, ±) — rewrote react-i18next mock per D-05
    - `frontend/src/components/__tests__/ConsistencyPanel.test.tsx` (2 lines changed, ±) — minor adjustment per cascade landing
    - `frontend/tests/unit/hooks/responsive.test.ts` (375 lines changed, ±) — touched as part of the cascade fix

    Deleted (8 files — VERIFIED via `git show 93f56b2b --stat` at planning time):
    - `frontend/tests/a11y/theme-selector.test.tsx` (366 lines)
    - `frontend/tests/integration/language-switch.test.tsx` (371 lines)
    - `frontend/tests/integration/registry-validation.test.tsx` (67 lines)
    - `frontend/tests/integration/test_cross_tab_sync.test.tsx` (302 lines)
    - `frontend/tests/integration/test_language_switch.test.tsx` (240 lines)
    - `frontend/tests/integration/test_preference_persistence.test.tsx` (259 lines)
    - `frontend/tests/unit/LanguageToggle.test.tsx` (263 lines)
    - `frontend/tests/unit/components.test.tsx` (647 lines)

    Total: 4 modified + 8 deleted = 12 file-changes (2,801 lines removed, 292 added per `git show` totals).

    **7. Decisions Made:**
    - Single-commit consolidation (G1+G2+G3 in `93f56b2b`) — diverges from Plan 50-01's per-task commit cadence preference; cumulative cascade-impact justified atomicity (rolling back one cascade fix without the others would leave the runner in a worse state than pre-50-01).
    - Deleted vs updated dead-import tests — the import targets were genuinely deleted in Phases 33/34/36 (not renamed); D-09 archaeology approved deletion. Plan 50-04 audit will record each as `disposition: deleted-dead`.

    **8. Deviations from Plan:**
    - Plan 50-01 body claimed `~11` dead-import deletions in G3; actual count was 8 per `git show 93f56b2b --stat` (a 27% over-estimate). Cause: Plan 50-01's class-D count was based on RESEARCH §Failure Inventory which used a different counting method; the executor's `git log --diff-filter=D` verification narrowed the safely-deletable set to 8 distinct paths. Three files originally flagged class-D survived because their imports targeted modules that were renamed (not deleted) in Phases 33/34/36.
    - Plan 50-01 expected residual count ≤15 post-plan; actual is 34. Cause: the 175 + 20 + 11 cascade-clear estimate over-counted because (a) some module-eval cascades were transitive of multiple causes (a single file showed up in both class-M and class-D buckets), and (b) the cross-cutting setup gaps (CC-1 jest-dom, CC-2 supabase env, CC-3 LanguageProvider) were NOT in Plan 50-01's scope and now require Plan 50-06 to close. The 34-file residual is being handled by the new Wave 2 plans (50-06 + 50-07) and Wave 3 (50-08).

    **9. Handoff to next plan(s):**
    - The 34-file residual surfaces three cross-cutting setup gaps (CC-1, CC-2, CC-3/CC-4) plus per-file drift; the operator gap brief at `.continue-here.md` documents the bucketing.
    - Plan 50-06 (this plan) handles CC-1 + CC-2 + CC-3 + CC-4 (cross-cutting setup gaps; expected ~80% auto-clear of the 34).
    - Plan 50-07 handles single-class outliers + scrape-regex drift (CC-5 Playwright misnamed file, CC-6 transform-failed, PF-2 fouc-bootstrap regex).
    - Plan 50-08 handles per-file product/test drift triage (D-10 archaeology; ≤12-file ceiling).
    - The 4 wizard tests originally counted by TEST-02 are PASSING after 50-01 and must remain green through 50-06/07/08 landing.

    Style: pipe-aligned markdown tables where appropriate, sentence-case prose, no emoji per CLAUDE.md, no marketing voice.

    File size target: ~120-180 lines (template-driven; sized to mirror `50-03-SUMMARY.md`).

  </action>
  <verify>
    <automated>test -f .planning/phases/50-test-infrastructure-repair/50-01-SUMMARY.md &amp;&amp; grep -c "^phase: 50-test-infrastructure-repair" .planning/phases/50-test-infrastructure-repair/50-01-SUMMARY.md | grep -q "^1$" &amp;&amp; grep -c "^plan: 01" .planning/phases/50-test-infrastructure-repair/50-01-SUMMARY.md | grep -q "^1$" &amp;&amp; grep -c "93f56b2b" .planning/phases/50-test-infrastructure-repair/50-01-SUMMARY.md | awk '$1 &gt;= 1 { exit 0 } { exit 1 }' &amp;&amp; grep -c "components.test.tsx" .planning/phases/50-test-infrastructure-repair/50-01-SUMMARY.md | awk '$1 &gt;= 1 { exit 0 } { exit 1 }' &amp;&amp; grep -c "218" .planning/phases/50-test-infrastructure-repair/50-01-SUMMARY.md | awk '$1 &gt;= 1 { exit 0 } { exit 1 }' &amp;&amp; grep -c "34" .planning/phases/50-test-infrastructure-repair/50-01-SUMMARY.md | awk '$1 &gt;= 1 { exit 0 } { exit 1 }' &amp;&amp; grep -c "requirements-completed: \[TEST-01, TEST-02\]" .planning/phases/50-test-infrastructure-repair/50-01-SUMMARY.md | grep -q "^1$"</automated>
  </verify>
  <done>
    `.planning/phases/50-test-infrastructure-repair/50-01-SUMMARY.md` exists with the template's required sections. Frontmatter declares phase + plan + requirements-completed [TEST-01, TEST-02]. The 8 deleted file paths from `git show 93f56b2b --stat` are listed verbatim in §Files Created/Modified. Pre/post failing-file counts (218 → 34) are recorded. Deviations from Plan 50-01 (11 vs 8 deletions, ≤15 vs 34 residual) are explained with cause. `gsd-execute-phase` will not re-attempt Plan 50-01's cascade fix after this SUMMARY lands.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                                                                                                                                                                        |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Test runner ↔ env stubs             | `vi.stubEnv` writes to `import.meta.env` for the vitest lifecycle; the stubs must not leak into production builds (production reads `import.meta.env` at build time via Vite, never at runtime in this app).                       |
| Test util ↔ global providers        | `renderWithProviders` registers two React Context providers (Language + Query) per-render; a misconfigured provider could mask real provider issues in production by always rendering tests "as if" both providers were available. |
| Setup.ts ↔ ordering of side-effects | Side-effect import + `vi.stubEnv` + `vi.mock` ordering matters (Pitfall 5); a regression in the file's structure could silently disable matchers, stubs, or the i18n mock.                                                         |

## STRIDE Threat Register

| Threat ID  | Category                   | Component                                                                                                                                                             | Disposition | Mitigation Plan                                                                                                                                                                                                                                                 |
| ---------- | -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-50-06-01 | T (Tampering)              | A future contributor edits `frontend/tests/setup.ts` and accidentally moves `vi.stubEnv` calls BELOW `vi.mock`, breaking the env-var contract for Supabase consumers. | mitigate    | Pitfall 5 ordering documented in `frontend/docs/test-setup.md` (Plan 50-04). Plan 50-05's D-15 ESLint rule covers the `vi.mock` factory shape but NOT the broader `setup.ts` ordering; this is a documented-but-unenforced contract — explicitly accepted.      |
| T-50-06-02 | I (Information disclosure) | `vi.stubEnv('VITE_SUPABASE_URL', 'http://localhost:54321')` could leak placeholder URL into product bundles if `import.meta.env` is read at runtime.                  | mitigate    | Vite reads `import.meta.env` at BUILD time only; runtime reads return frozen values. The stub is scoped to the test runner process. No production bundle ever loads `frontend/tests/setup.ts`.                                                                  |
| T-50-06-03 | E (Elevation of privilege) | `renderWithProviders` always wraps in `LanguageProvider`, masking tests that ought to FAIL when run outside a provider.                                               | accept      | Plan 50-08 retains a procedure for per-file triage where the disposition explicitly tests provider absence (use `render` directly from `@testing-library/react` for those). The default-wrap helper improves the common path; the niche path remains supported. |

</threat_model>

<verification>
**Phase-level checks (post-plan):**

- `50-02-PLAN.md` no longer exists at the original path; `50-02-PLAN.archived.md` exists with a 5-line supersession header above the original YAML.
- `50-01-SUMMARY.md` exists with the template's sections, the 8 deleted paths verbatim from `git show 93f56b2b --stat`, and the 218→34 failure-count pair.
- `frontend/package.json` lists `@testing-library/jest-dom` in `devDependencies`.
- `frontend/tests/setup.ts` line 1 is the jest-dom side-effect import; the file also contains both `vi.stubEnv` calls placed BEFORE the existing `vi.mock('react-i18next', ...)` call.
- `frontend/tests/utils/render.tsx` exports `renderWithProviders` wrapping `LanguageProvider` + `QueryClientProvider` (fresh per render).
- Each of the 9 listed test files imports `renderWithProviders` from `@tests/utils/render` (or the relative path equivalent).
- `pnpm --filter intake-frontend test --run` failing-file count is monotonically decreasing relative to the pre-plan baseline of 34. Expected post-plan count is ≤15 (per gap brief estimate that CC fixes auto-clear ~80% of 34).
- `pnpm --filter intake-frontend lint` exits 0 (no new violations introduced by the new test util or the migration).
- The 4 wizard tests from Plan 50-01 continue to pass green (no regression).
  </verification>

<success_criteria>

- **TEST-02 progress:** ≥80% of the 34 residuals auto-cleared via CC-1/CC-2/CC-3/CC-4 setup fixes. Cumulative phase-exit-0 contract delegated to Plan 50-08.
- **TEST-04 progress:** The setup.ts shape (jest-dom + env stubs + react-i18next mock) is the canonical shape Plan 50-04's docs will cite. No production docs touched here — only the implementation that the docs will describe.
- **Operator gap-brief housekeeping satisfied:** `50-02-PLAN.md` archived with supersession header; `50-01-SUMMARY.md` authored from verifiable git evidence; `50-06/07/08` named as replacements throughout.
- **Renderer helper available for downstream consumers:** `renderWithProviders` is the canonical render entry point for any future FE test needing LanguageProvider + QueryClient; Plan 50-04's docs MUST cite it.
- **No regression in Plan 50-01's gains:** The 4 wizard tests pass green; the dead-import deletions stay deleted.

</success_criteria>

<output>
After completion, create `.planning/phases/50-test-infrastructure-repair/50-06-SUMMARY.md` with:
- Pre/post failing-file counts (34 → expected ≤15) captured from runner output at SUMMARY write time
- Per-task commit hashes (Task 1 archive, Task 2 jest-dom, Task 3 env stubs, Task 4 renderWithProviders migration, Task 5 50-01-SUMMARY)
- Confirmation that `50-02-PLAN.archived.md` has the 5-line supersession header
- Confirmation that `50-01-SUMMARY.md` has the 8 deleted file paths verbatim
- Handoff list to Plan 50-07 (the CC-5 + CC-6 + PF-2 outliers) and Plan 50-08 (the residual per-file drift)
</output>
