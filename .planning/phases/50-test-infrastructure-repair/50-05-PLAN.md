---
phase: 50-test-infrastructure-repair
plan: 05
type: execute
wave: 5
depends_on:
  - 50-01
  - 50-03
  - 50-04
  - 50-09
  - 50-10
  - 50-11
  - 50-12
  - 50-13a
  - 50-13b
files_modified:
  - .github/workflows/ci.yml
  - eslint.config.mjs
  - tools/eslint-fixtures/bad-vi-mock.ts
autonomous: false
requirements:
  - TEST-01
  - TEST-04
user_setup: []

must_haves:
  truths:
    - '`.github/workflows/ci.yml` declares three new jobs: `Tests (frontend)` (name verbatim, blocking), `Tests (backend)` (name verbatim, blocking), and `Tests (integration)` (advisory via `continue-on-error: true`, NOT in required contexts).'
    - '`main` branch protection on the GitHub repo `alzahrani-khalid/Intl-Dossier-V2.0` lists `Tests (frontend)` and `Tests (backend)` in `required_status_checks.contexts` alongside the existing four contexts (`type-check`, `Security Scan`, `Lint`, `Bundle Size Check (size-limit)`); `enforce_admins: true` is preserved.'
    - '`eslint.config.mjs` declares a new test-files-scoped block with a `no-restricted-syntax` rule (D-15) that flags `vi.mock(<id>, factory)` calls whose returned object lacks a `SpreadElement`. The rule is the cheapest viable Option 1 per RESEARCH §ESLint Rule Implementation Choice.'
    - '`tools/eslint-fixtures/bad-vi-mock.ts` exists as a deliberately-bad regression target; `pnpm lint tools/eslint-fixtures/bad-vi-mock.ts` exits ≠0 (D-15 rule fires). `pnpm lint frontend/tests/setup.ts` exits 0 (canonical factory passes).'
    - 'Workspace `pnpm lint` exits 0 globally — the new rule does NOT break any existing test file (verified on `frontend/tests/component/CommitmentList.test.tsx:451` and `frontend/src/components/signature-visuals/__tests__/GlobeLoader.reducedMotion.test.tsx:18` which already use the `importActual + spread` pattern).'
  artifacts:
    - path: '.github/workflows/ci.yml'
      provides: 'Three new CI jobs (`Tests (frontend)`, `Tests (backend)`, `Tests (integration)`) mirroring the v6.2 type-check job shape'
      contains: 'Tests (frontend)'
    - path: 'eslint.config.mjs'
      provides: 'D-15 `no-restricted-syntax` rule scoped to test files'
      contains: 'ArrowFunctionExpression'
    - path: 'tools/eslint-fixtures/bad-vi-mock.ts'
      provides: 'Regression fixture proving the D-15 rule fires'
      contains: "vi.mock('react-i18next'"
  key_links:
    - from: 'main branch protection on GitHub'
      to: '.github/workflows/ci.yml job names'
      via: 'GitHub status check context names = `name:` field of each workflow job; PATCH `required_status_checks.contexts` to include the two new blocking names'
      pattern: "Tests \\(frontend\\)|Tests \\(backend\\)"
    - from: 'eslint.config.mjs D-15 rule'
      to: 'frontend/tests/setup.ts post-Plan-50-01 shape'
      via: 'Rule selector accepts factories whose ObjectExpression has a SpreadElement (the post-50-01 shape) and rejects factories without (the pre-50-01 broken shape)'
      pattern: 'SpreadElement'
---

<objective>
Lock in Phase 50's gains with three enforcement mechanisms that prevent regression:

1. **CI jobs (D-13)** — Add `Tests (frontend)`, `Tests (backend)`, and advisory `Tests (integration)` jobs to `.github/workflows/ci.yml`, mirroring the existing v6.2 `type-check` job shape. Then PATCH `main` branch protection to add the two non-advisory jobs to `required_status_checks.contexts` alongside the existing four contexts.

2. **D-15 ESLint rule** — Add a `no-restricted-syntax` selector to `eslint.config.mjs` (test-file glob scope) that flags any `vi.mock(<id>, factory)` whose factory returns an `ObjectExpression` without a `SpreadElement`. The cheapest viable Option 1 per RESEARCH §ESLint Rule Implementation Choice — ~10 lines added to the existing flat config, no new plugin package.

3. **Regression fixture (D-15 evidence)** — Commit a deliberately-bad `tools/eslint-fixtures/bad-vi-mock.ts` so the rule has a verifiable regression target. The fixture is excluded from production builds and only exists to prove the rule fires.

Per CONTEXT D-13: `Tests (integration)` is advisory-only (not in required contexts). Per CONTEXT D-15: the rule is added at the root flat config so it runs under the existing `Lint` PR-blocking context — no new CI gate registration for the rule itself.

This plan has TWO checkpoints because (a) branch-protection PATCH is irreversible without admin scope (`checkpoint:human-verify` for the smoke PR proof), and (b) the YAML edit + PATCH ordering matters (per PATTERNS §Gotchas: PATCH must happen AFTER both new jobs go green on at least one commit to avoid blocking the very PR that adds them).

Purpose: TEST-01 (FE factory contract) is enforced by D-15 going forward. TEST-04 (docs) is reinforced by the rule's error message pointing back to `frontend/docs/test-setup.md`. The two CI gates prevent any future PR from regressing the FE or BE test suites without explicit override.

Output:

- `.github/workflows/ci.yml` with three new jobs (and the legacy `test-unit` job deleted per RESEARCH §CI Gate Registration recommendation)
- `eslint.config.mjs` with a new test-files-scoped `no-restricted-syntax` block (D-15)
- `tools/eslint-fixtures/bad-vi-mock.ts` regression fixture
- `main` branch protection updated via `gh api -X PUT` to add the two new required contexts
- Smoke PR proof of the gate (manual verification via `checkpoint:human-verify`)
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-test-infrastructure-repair/50-CONTEXT.md
@.planning/phases/50-test-infrastructure-repair/50-RESEARCH.md
@.planning/phases/50-test-infrastructure-repair/50-PATTERNS.md
@.planning/phases/50-test-infrastructure-repair/50-VALIDATION.md
@.planning/phases/50-test-infrastructure-repair/50-04-SUMMARY.md
@frontend/docs/test-setup.md
@backend/docs/test-setup.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. -->

From `.github/workflows/ci.yml` (CURRENT):

- Lines 1-13: workflow name, triggers (push to main/develop/feature/\*, PR to main/develop), env block with `NODE_VERSION: '22.13.0'` and `PNPM_VERSION: '10.29.1'`
- Lines 15-41: `repo-policy` job — blocks committed env files and build artifacts (treat as the "needs" parent for new jobs)
- Lines 43-63: `lint` job — `name: Lint` → context `Lint`
- Lines 65-88: `type-check` job — `name: type-check` → context `type-check` — THE ANALOG. Two-step run pattern: `pnpm --filter intake-frontend type-check` then `pnpm --filter intake-backend type-check`. Mirror this exact shape but split into TWO jobs (one per workspace).
- Lines 90-117: `test-unit` job — runs `pnpm run test -- --coverage` at REPO ROOT (Turbo runs all workspaces in parallel — NO per-workspace gate). Per RESEARCH §CI Gate Registration recommendation: DELETE this job; replace with two scoped jobs.
- Lines 119+ : `test-e2e`, other jobs — leave untouched.

From `gh api repos/alzahrani-khalid/Intl-Dossier-V2.0/branches/main/protection` (verified live per RESEARCH §CI Gate Registration):

```json
{
  "required_status_checks": {
    "strict": true,
    "contexts": ["type-check", "Security Scan", "Lint", "Bundle Size Check (size-limit)"]
  },
  "enforce_admins": { "enabled": true }
}
```

Target post-PATCH `required_status_checks.contexts`:

```
["type-check", "Security Scan", "Lint", "Bundle Size Check (size-limit)", "Tests (frontend)", "Tests (backend)"]
```

DO NOT include `Tests (integration)` in required contexts (D-13: advisory-only).

From `eslint.config.mjs` (CURRENT structure verified):

- Lines 1-9: imports
- Lines 10-26: global ignores block (does NOT include `tools/` — confirm before adding fixture)
- Lines 28-30: js + tseslint base configs
- Lines 32-68: shared rules block (`@typescript-eslint/no-explicit-any: error`, `@typescript-eslint/no-floating-promises: error`, etc.)
- Lines 70-200: frontend override block. Lines 148-198 hold the existing `no-restricted-syntax` array (13 selectors for RTL physical-CSS bans — `ml-`, `mr-`, `pl-`, `pr-`, `text-left`, `text-right`, `left-`, `right-`, `rounded-l-`, `rounded-r-`, `border-l-`, `border-r-`)
- Lines 202-212: RTL-friendly plugin block (`rtl-friendly/no-physical-properties: warn`)
- Lines 214-225: UI library exception block (`files: ['frontend/**/components/ui/**']`, `no-restricted-syntax: off`)
- Lines 226-242: backend block

From PATTERNS §`eslint.config.mjs` Gotchas:

- The existing frontend `no-restricted-syntax` block (lines 148-198) MUST NOT be merged with the new D-15 rule — ESLint flat config does NOT auto-merge two `no-restricted-syntax` arrays across blocks. The cleanest path: add a new top-level block scoped to test-file globs (the test-rule block REPLACES the RTL block for files matching its glob, but tests rarely use Tailwind class literals so the trade-off is acceptable).
- AST selector for Option 1: `"CallExpression[callee.object.name='vi'][callee.property.name='mock'] ArrowFunctionExpression > ObjectExpression:not(:has(SpreadElement))"`. This flags any `vi.mock(<id>, () => ({ ... }))` whose returned `ObjectExpression` has no `SpreadElement` direct-child.

From RESEARCH §Pitfall 5 + §Sources:

- The selector MUST PASS on:
  - `frontend/tests/setup.ts` (post-Plan-50-01 — has spread of importActual)
  - `frontend/tests/component/CommitmentList.test.tsx:451-462` (has spread of importActual)
  - `frontend/src/components/signature-visuals/__tests__/GlobeLoader.reducedMotion.test.tsx:18-22` (has typed spread of importActual)
- The selector MUST FIRE on:
  - `tools/eslint-fixtures/bad-vi-mock.ts` (factory returns `({ useTranslation: ... })` with NO spread)

Verify BOTH directions BEFORE finalizing the rule.

From CONTEXT D-15:

- Rule named `vi-mock-exports-required` — but the actual implementation is a `no-restricted-syntax` selector under the existing rule (not a separately-named rule). The "name" is conceptual; the message in the rule output cites the contract.
- Cheapest viable path: AST selector in `eslint.config.mjs`. Skip Option 2 (local plugin) per RESEARCH §ESLint Rule Implementation Choice recommendation.

From VALIDATION row Gate (D-15):

- `pnpm lint frontend/tests/setup.ts` exits 0 (canonical setup passes)
- `pnpm lint tools/eslint-fixtures/bad-vi-mock.ts` exits ≠0 (rule fires on the bad fixture)
- The fixture path `tools/eslint-fixtures/` is greenfield per PATTERNS §`tools/eslint-fixtures/` (NOVEL — no existing precedent). Confirm `tools/` not in eslint ignores; add to git-tracked sources.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Add three new CI jobs to .github/workflows/ci.yml (D-13)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (full file — focus on lines 65-117: existing `type-check` job is the analog; `test-unit` job to be deleted)
    - .planning/phases/50-test-infrastructure-repair/50-RESEARCH.md §CI Gate Registration Mechanism (full verbatim job shapes at lines 608-689)
    - .planning/phases/50-test-infrastructure-repair/50-PATTERNS.md §`.github/workflows/ci.yml`
    - .planning/phases/50-test-infrastructure-repair/50-CONTEXT.md D-13
  </read_first>
  <action>
    Three-edit diff to `.github/workflows/ci.yml`:

    **A) Delete the existing `test-unit` job (lines 90-117).** Per RESEARCH §CI Gate Registration Mechanism: "Existing `test-unit` job runs `pnpm run test -- --coverage` at REPO ROOT — no per-workspace gate. Needs to be split into TWO jobs. Per RESEARCH the old `test-unit` job is 'wrong shape' and can be DELETED." Remove the entire job block including its `name: Unit Tests`, `runs-on`, `needs`, `steps`, and the trailing `codecov` upload step (the codecov step at lines 113-117 also goes — coverage is no longer collected at this gate per D-13).

    **B) Add three new jobs in the deleted job's place (immediately after the `repo-policy` and adjacent to `lint`/`type-check`):**

    - `test-frontend` job with `name: Tests (frontend)` (the `name:` field becomes the GitHub status context — exact case and parens matter per PATTERNS Gotchas)
    - `test-backend` job with `name: Tests (backend)`
    - `test-integration` job with `name: Tests (integration)` + `continue-on-error: true` (advisory-only per D-13)

    Each job MUST mirror the `type-check` job shape (lines 65-88) verbatim:
    - `runs-on: ubuntu-latest`
    - `needs: [repo-policy]`
    - `steps`:
      1. `uses: actions/checkout@v4`
      2. `name: Setup pnpm` + `uses: pnpm/action-setup@v4`
      3. `name: Setup Node.js` + `uses: actions/setup-node@v4` with `node-version: ${{ env.NODE_VERSION }}` and `cache: 'pnpm'`
      4. `name: Install dependencies` + `run: pnpm install --frozen-lockfile`
      5. The test-run step.

    Test-run step per job:
    - `test-frontend`: `run: pnpm --filter intake-frontend test --run`
    - `test-backend`: `run: pnpm --filter intake-backend test --run`
    - `test-integration`: `run: pnpm --filter intake-backend test:integration`

    For `test-integration`, add `continue-on-error: true` immediately under `needs: [repo-policy]`. This makes the job non-blocking. Combined with NOT adding it to `required_status_checks.contexts` (Task 3 below), it stays advisory per D-13.

    Note about Tests (integration) execution: today it WILL fail because no real Supabase/Redis runs in the CI sandbox. That's expected per D-13 ("advisory-only, NOT blocking"). Per RESEARCH §Open Question 2 — Plan 50-05 may also set `if: false` on `test-integration` to disable the job entirely on first land, promoting to active-advisory once dev-env contract is documented. Recommendation: leave `continue-on-error: true` and let the job RUN (so it surfaces its own failure as warning), but document in `backend/docs/test-setup.md` why it's expected to fail in CI today.

    **C) Preserve verbatim everything else in the file:** the `name:` at line 1, the `on:` block, the `env:` block (NODE_VERSION + PNPM_VERSION), the `repo-policy` / `lint` / `type-check` jobs unchanged, and every job from `test-e2e` (line 119) onward (including the `bundle-size-check` job that registers the Bundle Size context).

    Style: YAML 2-space indent matching the rest of the file. Block separators (one blank line between jobs).

  </action>
  <verify>
    <automated>grep -c "name: Tests (frontend)" .github/workflows/ci.yml | grep -q "^1$" &amp;&amp; grep -c "name: Tests (backend)" .github/workflows/ci.yml | grep -q "^1$" &amp;&amp; grep -c "name: Tests (integration)" .github/workflows/ci.yml | grep -q "^1$" &amp;&amp; grep -c "continue-on-error: true" .github/workflows/ci.yml | awk '$1 &gt;= 1 { exit 0 } { exit 1 }' &amp;&amp; grep -c "name: Unit Tests" .github/workflows/ci.yml | grep -q "^0$" &amp;&amp; grep -c "pnpm --filter intake-frontend test --run" .github/workflows/ci.yml | grep -q "^1$" &amp;&amp; grep -c "pnpm --filter intake-backend test --run" .github/workflows/ci.yml | grep -q "^1$" &amp;&amp; grep -c "pnpm --filter intake-backend test:integration" .github/workflows/ci.yml | grep -q "^1$" &amp;&amp; { actionlint .github/workflows/ci.yml || python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))"; }</automated>
  </verify>
  <done>
    `.github/workflows/ci.yml` has exactly one job each named `Tests (frontend)`, `Tests (backend)`, `Tests (integration)`. The old `Unit Tests` job is removed. Each new job has the proper `pnpm --filter ... test --run` invocation. The integration job has `continue-on-error: true`. Existing jobs (`repo-policy`, `lint`, `type-check`, `test-e2e`, `bundle-size-check`, etc.) are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add D-15 no-restricted-syntax rule to eslint.config.mjs + create bad fixture</name>
  <files>eslint.config.mjs, tools/eslint-fixtures/bad-vi-mock.ts</files>
  <read_first>
    - eslint.config.mjs (full file — focus on the existing `no-restricted-syntax` block at lines 148-198 inside the frontend override block, the UI library exception block at lines 214-225, and the backend block at lines 226-242)
    - .planning/phases/50-test-infrastructure-repair/50-RESEARCH.md §ESLint Rule Implementation Choice (Option 1 recommendation + the AST selector + Pitfall 5 — verify on real fixtures BEFORE landing)
    - .planning/phases/50-test-infrastructure-repair/50-PATTERNS.md §`eslint.config.mjs` (Gotchas — flat config block-scoping; existing block lines 148-198 used as the verbatim analog)
    - .planning/phases/50-test-infrastructure-repair/50-CONTEXT.md D-15
    - frontend/tests/setup.ts (POST-Plan-50-01 — the canonical PASSING factory; rule MUST NOT fire here)
    - frontend/tests/component/CommitmentList.test.tsx:451-462 (existing PASSING precedent — rule MUST NOT fire here)
    - frontend/src/components/signature-visuals/__tests__/GlobeLoader.reducedMotion.test.tsx:18-22 (existing PASSING precedent — rule MUST NOT fire here)
    - frontend/docs/test-setup.md (POST-Plan-50-04 — the rule's error message should anchor-link to the doc section explaining the contract)
  </read_first>
  <action>
    Two-file change:

    **A) Add a new top-level block to `eslint.config.mjs` declaring the D-15 rule.** Insert between the existing UI library exception block (ends ~line 225) and the existing backend block (starts ~line 226). The new block:

    - `files: ['**/tests/**/*.{ts,tsx}', '**/__tests__/**/*.{ts,tsx}', '**/*.test.{ts,tsx}', 'tools/eslint-fixtures/**/*.{ts,tsx}']` — scoped to test files PLUS the fixture path (so the rule actually fires on the fixture in Task 2B).
    - `rules: { 'no-restricted-syntax': ['error', { selector: ..., message: ... }] }`

    Selector (Option 1 per RESEARCH §Code Examples lines 1156-1172) — supply this exact string (single-quoted in JS source) as the `selector` field: `CallExpression[callee.object.name='vi'][callee.property.name='mock'] ArrowFunctionExpression > ObjectExpression:not(:has(SpreadElement))`. The AST node names match `@typescript-eslint/parser` output exactly; do not transliterate.

    Message field (cite the docs anchor) — supply this exact string as the `message` field: `vi.mock(...) factory must spread vi.importActual(<id>). Pattern: async () => ({ ...(await vi.importActual<typeof import("mod")>("mod")), override: ... }). See frontend/docs/test-setup.md §The react-i18next mock contract.` (Note: drop the backtick-fenced template-literal markers when authoring the JS string; the message is a plain string, not a template.)

    Pitfall 5 verification (MANDATORY before declaring done):
    - Run `pnpm lint frontend/tests/setup.ts` → MUST exit 0.
    - Run `pnpm lint frontend/tests/component/CommitmentList.test.tsx` → MUST exit 0 for the `vi.mock` at line 451.
    - Run `pnpm lint 'frontend/src/components/signature-visuals/__tests__/GlobeLoader.reducedMotion.test.tsx'` → MUST exit 0 for the `vi.mock` at line 18.
    - Run `pnpm lint tools/eslint-fixtures/bad-vi-mock.ts` → MUST exit ≠0 with the D-15 message.

    If the selector fires on a precedent file, refine it before committing. Acceptable refinements:
    - Tighten the path: only flag when `ArrowFunctionExpression` is asynchronous would require a different selector — DON'T tighten this way; D-15 also wants to catch sync factories that omit the spread.
    - Add a `.ignorePatterns` for the lint-fixture-style files that intentionally show non-spread (rare; D-15's value is enforcement, not documentation tolerance).
    - If neither refinement works, fall back to RESEARCH §Option 3 (Hybrid: keep the selector as advisory + standalone script) — but document the fall-back rationale and re-open D-15 as `queued-with-rationale` in the audit.

    **B) Create `tools/eslint-fixtures/bad-vi-mock.ts` (NEW — `tools/` is greenfield per PATTERNS §`tools/eslint-fixtures/`):**

    File body (~10-15 lines) MUST contain, in order:
    - A leading comment block (4-5 `//` lines) stating: that this is the Phase 50 D-15 regression fixture, that `pnpm lint tools/eslint-fixtures/bad-vi-mock.ts` MUST exit non-zero, that the factory deliberately omits a SpreadElement, and a pointer to `frontend/docs/test-setup.md §The react-i18next mock contract`.
    - An ES import statement: `import { vi } from 'vitest'` (single-quoted, no semicolon per root Prettier `semi: false`).
    - One blank line.
    - A single `vi.mock(...)` call against `'react-i18next'` whose factory is an arrow function returning an object literal with a single key `useTranslation` (no spread of `vi.importActual`). The `useTranslation` value is a function returning `{ t: (key: string) => key }`. This shape is precisely what the D-15 selector flags.

    Style: match root Prettier config (`semi: false`, `singleQuote: true`). The file does NOT need to be a real test (no `.test.ts` extension required — the `tools/eslint-fixtures/**/*.{ts,tsx}` glob in the rule covers it).

    Verify `tools/eslint-fixtures/` is NOT in `.gitignore` or `eslint.config.mjs` global ignores (it isn't, per PATTERNS verification). The file gets committed.

    Confirm rule fires by running `pnpm lint tools/eslint-fixtures/bad-vi-mock.ts` and capturing the exit code + message — log both into the SUMMARY.

  </action>
  <verify>
    <automated>grep -c "ArrowFunctionExpression &gt; ObjectExpression:not(:has(SpreadElement))" eslint.config.mjs | grep -q "^1$" &amp;&amp; test -f tools/eslint-fixtures/bad-vi-mock.ts &amp;&amp; pnpm lint frontend/tests/setup.ts &amp;&amp; ! pnpm lint tools/eslint-fixtures/bad-vi-mock.ts &amp;&amp; pnpm lint frontend/tests/component/CommitmentList.test.tsx</automated>
  </verify>
  <done>
    `eslint.config.mjs` declares a new test-files-scoped block with a `no-restricted-syntax` rule using the verified AST selector. `tools/eslint-fixtures/bad-vi-mock.ts` exists with the deliberately-bad factory. `pnpm lint frontend/tests/setup.ts` exits 0 (canonical passes). `pnpm lint tools/eslint-fixtures/bad-vi-mock.ts` exits ≠0 (rule fires). The two existing precedents (`CommitmentList.test.tsx:451`, `GlobeLoader.reducedMotion.test.tsx:18`) continue to pass lint. Workspace-wide `pnpm lint` exits 0 (no new violations elsewhere).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Update main branch protection + verify with smoke PR (D-13)</name>
  <what-built>
    Tasks 1+2 added three CI jobs and a D-15 ESLint rule. This checkpoint walks through the operator-required step that updates GitHub branch protection on `main` to require the two new contexts, then proves the gate is live via a smoke PR.

    The branch-protection PATCH cannot be safely executed by Claude alone because:
    - It requires `gh auth` with `admin:repo` scope (not always present in CI agents).
    - It is destructive (a malformed PUT clears `required_pull_request_reviews` and `restrictions`).
    - The smoke-PR proof requires interaction with the GitHub UI to confirm `mergeStateStatus=BLOCKED`.

    Per PATTERNS §`.github/workflows/ci.yml` Gotchas: PATCH must happen AFTER both new jobs go green on at least one commit, else the first push to a feature branch fails. The sequence below enforces this ordering.

  </what-built>
  <how-to-verify>
    1. **Confirm both new jobs go green on `DesignV2` HEAD:**
       - `gh run list --branch DesignV2 --workflow ci.yml --limit 5` — find the latest CI run since Task 1 landed.
       - In the run details, confirm `Tests (frontend)` = `completed / success` and `Tests (backend)` = `completed / success`.
       - If either job is red, STOP and return to Plan 50-02 / 50-03 — branch protection cannot register against red.

    2. **Capture the current branch-protection state for audit trail:**
       - Run `gh api repos/alzahrani-khalid/Intl-Dossier-V2.0/branches/main/protection > /tmp/protection-pre-50-05.json`. Verify `enforce_admins.enabled = true` and `required_status_checks.contexts` lists the existing four `["type-check", "Security Scan", "Lint", "Bundle Size Check (size-limit)"]`.

    3. **Apply the PATCH (PUT with full payload to preserve unrelated fields):**
       - Run:
         ```
         gh api -X PUT repos/alzahrani-khalid/Intl-Dossier-V2.0/branches/main/protection --input - <<'JSON'
         {
           "required_status_checks": {
             "strict": true,
             "contexts": [
               "type-check",
               "Security Scan",
               "Lint",
               "Bundle Size Check (size-limit)",
               "Tests (frontend)",
               "Tests (backend)"
             ]
           },
           "enforce_admins": true,
           "required_pull_request_reviews": null,
           "restrictions": null
         }
         JSON
         ```
       - Per PATTERNS §`.github/workflows/ci.yml` Gotchas: the PUT MUST include `required_pull_request_reviews` and `restrictions` (both `null` here per pre-state) or the call CLEARS them. If `protection-pre-50-05.json` shows non-null values for those fields, replicate them exactly in the PUT body.

    4. **Verify the PATCH took effect:**
       - Run `gh api repos/alzahrani-khalid/Intl-Dossier-V2.0/branches/main/protection | jq -r '.required_status_checks.contexts[]' | grep -c '^Tests'` — expect output `2`.
       - Run `gh api repos/alzahrani-khalid/Intl-Dossier-V2.0/branches/main/protection | jq -r '.enforce_admins.enabled'` — expect `true`.

    5. **Smoke-PR proof (the gate is alive):**
       - Create a feature branch: `git checkout -b smoke/phase-50-tests-gate`.
       - Add a deliberately-failing test (e.g., append `it('phase-50 smoke fail', () => expect(true).toBe(false))` to any existing FE test file, in a SEPARATE commit clearly labelled `test(50-05): deliberate fail for smoke gate proof — DELETE BEFORE MERGE`).
       - Push the branch and open PR: `gh pr create --base main --head smoke/phase-50-tests-gate --title 'Phase 50 smoke: tests gate proof' --body 'Deliberate test failure to prove Tests (frontend) blocks merge. Will close --delete-branch.'`.
       - Wait for CI runs to complete (or fail). Confirm `Tests (frontend) = fail` AND `mergeStateStatus = BLOCKED` via `gh pr view <PR-number> --json mergeStateStatus,statusCheckRollup | jq`.
       - Close the smoke PR: `gh pr close <PR-number> --delete-branch`. Confirm the branch is deleted.

    6. **Record evidence in this plan's SUMMARY:**
       - Pre-PATCH protection state (the 4 contexts).
       - Post-PATCH protection state (the 6 contexts).
       - Smoke PR number + verified `mergeStateStatus=BLOCKED` proof.
       - Smoke PR closed + branch deleted.

    Per CONTEXT D-13: `Tests (integration)` is NOT added to required contexts; it runs as advisory only and must not appear in `required_status_checks.contexts`.

  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `approved <smoke-PR-URL>` — branch protection updated, smoke PR demonstrated gate, PR closed and branch deleted.
    - `failed <reason>` — describe what went wrong (PATCH error, smoke PR didn't BLOCK, etc.) so Plan 50-05 can be amended.
    - `paused <reason>` — needs out-of-session admin scope or human-only follow-up.
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                      | Description                                                                                                                                                                                 |
| --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| GitHub branch protection ↔ workflow job names | Status check context names ARE the workflow job `name:` field. A typo in either side silently disables the gate.                                                                            |
| ESLint rule selector ↔ AST parser             | AST node names (`CallExpression`, `ArrowFunctionExpression`, `ObjectExpression`, `SpreadElement`) must match `@typescript-eslint/parser` exactly; a misspelling silently disables the rule. |
| `gh api PUT` ↔ existing protection settings   | A PUT (vs PATCH) replaces the entire resource; omitting `required_pull_request_reviews` / `restrictions` clears them.                                                                       |

## STRIDE Threat Register

| Threat ID  | Category                   | Component                                                                                                                                                                                                                                                 | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---------- | -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-50-05-01 | E (Elevation of privilege) | `gh api PUT` with under-scoped token weakens enforcement (e.g., clears `enforce_admins` to false).                                                                                                                                                        | mitigate    | Task 3 step 2 captures pre-state to `/tmp/protection-pre-50-05.json` for audit trail. The PUT body explicitly sets `enforce_admins: true`. Task 3 step 4 verifies post-state via `jq` query.                                                                                                                                                                                                                                                                                                                                      |
| T-50-05-02 | I (Information disclosure) | The smoke PR could leak the test infrastructure attack surface (e.g., `mergeStateStatus` reasoning).                                                                                                                                                      | accept      | Smoke PR contains a contrived test failure only; no production secrets, no real-data fixtures. Closed immediately after proof per D-13 v6.2 precedent.                                                                                                                                                                                                                                                                                                                                                                            |
| T-50-05-03 | T (Tampering)              | A future contributor adds a `vi.mock(...)` factory that manually lists all exports correctly today but misses tomorrow's SDK exports. The D-15 rule's selector forces the `importActual + spread` pattern, but is the false-negative coverage acceptable? | accept      | Per RESEARCH §ESLint Rule Implementation Choice: Option 1 enforces the PATTERN, not coverage. A contributor who lists all exports correctly AND gets it right will still be flagged — they must convert to spread. This is a feature, not a bug.                                                                                                                                                                                                                                                                                  |
| T-50-05-04 | T (Tampering)              | The ESLint rule has a false-negative letting a future setup.ts ship without required exports.                                                                                                                                                             | mitigate    | The bad fixture (`tools/eslint-fixtures/bad-vi-mock.ts`) is a committed regression target. If the rule ever stops firing on it (e.g., parser upgrade changes AST node names), the lint job goes green when it shouldn't — CI failure mode is "rule disabled silently". Mitigation: Plan 50-04's audit lists the bad-fixture pattern; add a positive `pnpm lint tools/eslint-fixtures/bad-vi-mock.ts` check that EXPECTS non-zero exit to a follow-up health-check script (out of Phase 50 scope; record as `tech_debt` in audit). |

</threat_model>

<verification>
**Phase-level checks (post-plan):**

- `.github/workflows/ci.yml` has three new jobs by name (`Tests (frontend)`, `Tests (backend)`, `Tests (integration)`). The old `Unit Tests` job is removed.
- `gh api repos/alzahrani-khalid/Intl-Dossier-V2.0/branches/main/protection | jq -r '.required_status_checks.contexts[]'` lists exactly 6 contexts: `type-check`, `Security Scan`, `Lint`, `Bundle Size Check (size-limit)`, `Tests (frontend)`, `Tests (backend)`.
- `gh api ... | jq -r '.enforce_admins.enabled'` returns `true`.
- `eslint.config.mjs` has the new test-files-scoped `no-restricted-syntax` block with the verified AST selector.
- `tools/eslint-fixtures/bad-vi-mock.ts` exists and triggers the rule.
- `pnpm lint frontend/tests/setup.ts` exits 0.
- `pnpm lint tools/eslint-fixtures/bad-vi-mock.ts` exits ≠0.
- Workspace `pnpm lint` exits 0 (the existing CommitmentList + GlobeLoader precedent files still pass).
- Smoke PR closed (no orphan branch on remote).
- Both `Tests (frontend)` and `Tests (backend)` jobs were green on at least one commit BEFORE the branch-protection PATCH.

**Manual checkpoint (Task 3):** Smoke PR demonstrated `mergeStateStatus = BLOCKED` with `Tests (frontend) = fail`; PR closed `--delete-branch` per v6.2 phase 47/48/49 precedent.
</verification>

<success_criteria>

- **D-13 SATISFIED:** Branch protection on `main` includes the two new required contexts; `enforce_admins=true` preserved; smoke PR proves the gate fires.
- **D-15 SATISFIED:** ESLint rule lives in `eslint.config.mjs`, fires on the bad fixture, passes on canonical setup.ts + precedents.
- **TEST-01 reinforced:** D-15 prevents future regression of the `initReactI18next` pattern at lint-time (in addition to runtime test verification).
- **TEST-04 reinforced:** Rule message anchor-links to `frontend/docs/test-setup.md` — every lint failure becomes a learning opportunity.
- **Audit trail complete:** Smoke PR URL + protection-pre + protection-post recorded in `50-05-SUMMARY.md` per v6.2 precedent.
- **No regression:** Workspace-wide `pnpm lint` exits 0 with the new rule active.
  </success_criteria>

<output>
After completion, create `.planning/phases/50-test-infrastructure-repair/50-05-SUMMARY.md` with:
- The smoke PR URL + `mergeStateStatus=BLOCKED` proof + close + branch-delete confirmation
- Pre-PATCH `required_status_checks.contexts` (4) + post-PATCH (6)
- Confirmation that `enforce_admins=true` is preserved
- The verified ESLint rule selector + pass/fail proof on the bad fixture + the two precedent files
- A note that `Tests (integration)` is advisory-only (per D-13, NOT in required contexts) and may run with `continue-on-error: true` until the dev-env contract is documented in a follow-up phase
- Phase-50-exit attestation: TEST-01/02/03/04 all SATISFIED; ready for `/gsd-verify-phase 50`
</output>
</content>
</invoke>
