---
phase: 50-test-infrastructure-repair
plan: 07
type: execute
wave: 2
depends_on:
  - 50-01
files_modified:
  - frontend/tests/accessibility/entity-search.a11y.test.ts
  - frontend/tests/a11y/wcag-compliance.test.ts
  - frontend/tests/performance/validation-speed.test.ts
  - frontend/tests/unit/design-system/fouc-bootstrap.test.ts
autonomous: true
requirements:
  - TEST-02
user_setup: []

must_haves:
  truths:
    - 'After this plan lands, the four single-class outlier failures (CC-5 Playwright misnamed file, CC-6 two transform-failed files, PF-2 fouc-bootstrap scrape regex) are resolved — each file is either fixed-green in the default FE runner, renamed to `.spec.ts` (excluded by vitest config per Plan 50-01), or deleted with rationale.'
    - 'CC-5: `frontend/tests/accessibility/entity-search.a11y.test.ts` no longer triggers `Playwright Test did not expect test.describe() to be called here` under vitest — either renamed to `.spec.ts` so vitest skips it (per Plan 50-01 `exclude: ["**/*.spec.*"]` glob) or rewritten as a true vitest unit test.'
    - 'CC-6: `frontend/tests/a11y/wcag-compliance.test.ts` and `frontend/tests/performance/validation-speed.test.ts` no longer produce `Transform failed` esbuild parse errors — root-cause diagnosed per file (import-of-deleted-module vs invalid TS syntax) and fixed.'
    - 'PF-2: `frontend/tests/unit/design-system/fouc-bootstrap.test.ts` 9 scrape failures resolved — the regex that extracts `chancery.light`, `chancery.dark`, `situation.light`, `situation.dark`, `ministerial.light`, `ministerial.dark` palettes from `frontend/public/bootstrap.js` is updated to match the current bootstrap.js literal shape, OR the bootstrap.js source is resynced to the regex-expected canonical form.'
    - 'No CC-5/CC-6/PF-2 file exits this plan in `queued-with-rationale` state for the FE default runner per D-01 non-negotiable. Files moved to `.spec.ts` (Playwright) ARE acceptable because vitest excludes them — they are not in the "default runner" set.'
  artifacts:
    - path: 'frontend/tests/unit/design-system/fouc-bootstrap.test.ts'
      provides: 'fouc-bootstrap drift-guard test with regex aligned to current bootstrap.js palette shape'
      contains: 'chancery'
  key_links:
    - from: 'frontend/tests/unit/design-system/fouc-bootstrap.test.ts'
      to: 'frontend/public/bootstrap.js'
      via: 'Test regex extracts direction.theme palette objects from bootstrap.js literal; either the regex updates to match bootstrap.js OR bootstrap.js resyncs to match the regex'
      pattern: 'chancery|situation|ministerial'
    - from: 'frontend/tests/accessibility/entity-search.a11y.test.ts (renamed or rewritten)'
      to: 'frontend/vitest.config.ts exclude glob `**/*.spec.*` (post-Plan-50-01)'
      via: 'Renaming `.test.ts` → `.spec.ts` moves the file out of the vitest include glob `**/*.test.{ts,tsx}` AND into the exclude glob `**/*.spec.*` — both gates exclude it; the Playwright runner picks it up at `pnpm test:visual` time'
      pattern: '\\.spec\\.ts$'
---

<objective>
Close the four single-class outlier failures identified in the operator gap brief at `.continue-here.md`. These are mechanically-distinct from the cross-cutting setup gaps (Plan 50-06) and the per-file drift triage (Plan 50-08) — each is a one-off bug class with a small, targeted fix. Bundling them in a single small plan avoids three trivial standalone plans.

1. **CC-5 — `tests/accessibility/entity-search.a11y.test.ts` (1 file, 1 root cause).** File is misnamed `*.test.ts` so vitest's collection glob `**/*.test.{ts,tsx}` (post-Plan-50-01) picks it up; but the file body uses Playwright's `test.describe(...)` API which throws `Playwright Test did not expect test.describe() to be called here` when run under vitest. Fix: either (a) rename to `.spec.ts` so the post-Plan-50-01 `exclude: ["**/*.spec.*"]` glob skips it, OR (b) rewrite as a true vitest unit using jest-dom + axe-core matchers.

2. **CC-6 — `tests/a11y/wcag-compliance.test.ts` + `tests/performance/validation-speed.test.ts` (2 files, esbuild `Transform failed`).** Per-file diagnosis required at task time: likely an import of a deleted module OR invalid TypeScript syntax (likely the former, given Phases 33/34/36 produced many module renames). Fix-or-delete per D-10 archaeology + D-09 root-cause discretion.

3. **PF-2 — `tests/unit/design-system/fouc-bootstrap.test.ts` (1 file, 9 sub-test failures).** Drift-guard regex pins `frontend/public/bootstrap.js` palette/font literals to `frontend/src/design-system/tokens/directions.ts`. The regex no longer matches the current bootstrap.js literal shape (a structural change in the JS palette landed AFTER the regex was authored). Fix: either update the regex to match the current literal form, OR resync bootstrap.js to the regex-expected canonical form. Per Plan 50-01 PATTERNS the test was already noted as "1 known-defective plan-internal byte-for-byte test"; resolve it here.

This plan runs in parallel with Plan 50-06 (both depend only on Plan 50-01) — neither blocks the other. The cumulative phase-exit-0 contract (D-01) is satisfied across 50-06 + 50-07 + 50-08; this plan's done-state is that the four outlier files are no longer failing the default FE runner.

Purpose: Remove four mechanical-bug-class failures from the residual triage set so Plan 50-08's per-file ceiling (≤12 files) is honored. The CC-5 fix in particular reduces friction for the executor — once a file is renamed to `.spec.ts`, vitest doesn't see it and the runner's failure list shrinks immediately.

Output:

- `frontend/tests/accessibility/entity-search.a11y.test.ts` either renamed to `.spec.ts` or rewritten as vitest unit (with original `.test.ts` path no longer existing if renamed).
- `frontend/tests/a11y/wcag-compliance.test.ts` fixed or deleted per per-file diagnosis.
- `frontend/tests/performance/validation-speed.test.ts` fixed or deleted per per-file diagnosis.
- `frontend/tests/unit/design-system/fouc-bootstrap.test.ts` regex aligned to current bootstrap.js shape (OR bootstrap.js resynced).
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-test-infrastructure-repair/50-CONTEXT.md
@.planning/phases/50-test-infrastructure-repair/50-RESEARCH.md
@.planning/phases/50-test-infrastructure-repair/50-PATTERNS.md
@.planning/phases/50-test-infrastructure-repair/.continue-here.md
@.planning/phases/50-test-infrastructure-repair/50-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Per-file diagnosis prep — read these before authoring fixes. -->

From `frontend/vitest.config.ts` (POST-Plan-50-01):

- `include: ['**/*.test.{ts,tsx}']`
- `exclude: ['node_modules/', 'dist/', 'build/', '**/*.spec.*']`
- Renaming a file from `*.test.ts` to `*.spec.ts` moves it OUT of `include` (no longer matched) AND INTO `exclude` (defensive). Either gate would exclude it; both apply for redundancy.

From `.continue-here.md` §CC-5:

- File: `frontend/tests/accessibility/entity-search.a11y.test.ts`
- Error verbatim: `Playwright Test did not expect test.describe() to be called here.`
- Root cause: file uses Playwright API but vitest picks it up due to `.test.ts` extension.
- Operator-suggested fix path: "either rename file to `.spec.ts` (gets excluded by the post-50-01 vitest config) OR rewrite to vitest if intended as unit."
- Decision recommendation: rename to `.spec.ts` is the cheapest viable fix per Karpathy Coding Principles §2 (Simplicity First). Only rewrite if there's evidence the test was authored AS a vitest unit and someone accidentally pulled in Playwright imports.

From `.continue-here.md` §CC-6:

- Files: `frontend/tests/a11y/wcag-compliance.test.ts`, `frontend/tests/performance/validation-speed.test.ts`
- Error class: `Transform failed` (esbuild parse error during vitest collection).
- Operator note: "investigate per-file at execution time — likely TS syntax / import-of-deleted-module"
- Both files are in `frontend/tests/` (not src) so they are not consumed by production code. Deletion is a viable disposition under D-10 if the test was orphaned by Phases 33/34/36.

From `.continue-here.md` §PF-2 (per-file drift table row):

- File: `frontend/tests/unit/design-system/fouc-bootstrap.test.ts`
- Error: `Failed to scrape chancery.light/dark, situation.light/dark, ministerial.light/dark palette from bootstrap.js (9 failures)`
- Root cause: "Scrape regex doesn't match current `frontend/public/bootstrap.js` palette shape. Either update regex OR resync bootstrap.js to canonical literal form."
- This file was originally listed in Plan 50-02 (now archived) as a per-file drift candidate. Plan 50-07 absorbs it because its fix is mechanically the same shape as the CC-5/CC-6 outliers (one-off, one-file fix with a clear root-cause).

From `frontend/public/bootstrap.js` (the FOUC-bootstrap consumer that fouc-bootstrap.test.ts guards):

- Per CLAUDE.md project rules: "its palette/font literals must byte-match `tokens/directions.ts`"
- Per Plan 50-01 PATTERNS §`frontend/tests/unit/design-system/fouc-bootstrap.test.ts`: "1 known-defective plan-internal byte-for-byte test"
- The file is the FOUC bootstrap — a small inline JS that paints first-frame design tokens before React hydrates. Its structure is a literal object map of `{ direction: { theme: { palette } } }`.

From `frontend/src/design-system/tokens/directions.ts` (the canonical source of truth for the palette):

- Exports `directions` object with `bureau`, `chancery`, `situation`, `ministerial` keys, each with `light` and `dark` sub-keys, each with a palette object.
- The fouc-bootstrap.test.ts drift guard exists specifically to prevent bootstrap.js and directions.ts from drifting apart.

Per-file decision tree (apply at task execution time):

For CC-5 (entity-search.a11y.test.ts):

- Default action: `git mv frontend/tests/accessibility/entity-search.a11y.test.ts frontend/tests/accessibility/entity-search.a11y.spec.ts`. No content changes. Vitest now ignores the file; Playwright continues to pick it up via its own `tests/accessibility/` glob.
- If `git log -p` shows the file was authored as a vitest unit and Playwright imports were added later by mistake, rewrite instead — but the default action is rename per Karpathy §2 + operator brief.

For CC-6 (wcag-compliance.test.ts, validation-speed.test.ts):

- Step 1: capture the exact esbuild error for each file via `pnpm exec vitest --run <file> 2>&amp;1 | head -50`. The error names the failing import or the syntax position.
- Step 2: `git log -p <file>` for the test history, `git log --diff-filter=D --name-only --pretty=format: -- <imported-module-path>` to verify if the imported module was deleted.
- Step 3 disposition:
  - If import of a deleted-in-Phase-33/34/36 module → delete the test file with rationale (matches Plan 50-01 G3 cascade pattern).
  - If syntactically broken TS → fix the syntax minimally (single-line repair).
  - If genuinely environment-dependent (e.g., needs a real Express server for `validation-speed.test.ts`) → move to integration runner via `frontend/vitest.config.ts` `exclude` addition + cross-plan handoff for Plan 50-04 audit. The integration-only disposition is `split-to-integration`, NOT `queued-with-rationale`.

For PF-2 (fouc-bootstrap.test.ts):

- Step 1: read the full test file to identify the scrape regex.
- Step 2: read `frontend/public/bootstrap.js` to identify the current literal shape for the 3 failing directions (chancery / situation / ministerial; the 4th direction `bureau` apparently passes — the failures are scoped to the 3 non-default directions).
- Step 3 disposition:
  - If regex is too strict and bootstrap.js shape is intentionally newer → update the regex.
  - If bootstrap.js literal drifted from `tokens/directions.ts` form → resync bootstrap.js to match directions.ts (per CLAUDE.md "its palette/font literals must byte-match"); the regex is the contract, the impl is the regression.
  - If both regex AND bootstrap.js drifted from directions.ts → update both to match directions.ts (the canonical source).
- The 9-failure count breaks down as 3 directions × 3 things (light palette, dark palette, possibly font) — confirm by reading the test's `describe`/`it` block structure.

Test commits for this plan are per-file (one commit each), with messages like:

- `fix(50-07): rename entity-search.a11y.test.ts to .spec.ts (CC-5 Playwright)` (or `rewrite as vitest` if that path is chosen)
- `fix(50-07): delete wcag-compliance.test.ts (CC-6 import of deleted module)` (or `repair syntax` if that's the actual root cause)
- `fix(50-07): repair validation-speed.test.ts (CC-6)` (or `delete`)
- `fix(50-07): update fouc-bootstrap regex to match current bootstrap.js shape (PF-2)` (or `resync bootstrap.js`)
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve CC-5 — entity-search.a11y.test.ts (rename to .spec.ts or rewrite)</name>
  <files>frontend/tests/accessibility/entity-search.a11y.test.ts</files>
  <read_first>
    - frontend/tests/accessibility/entity-search.a11y.test.ts (full file — identify whether body uses `import { test } from '@playwright/test'` or vitest `it`/`describe`)
    - frontend/vitest.config.ts (POST-Plan-50-01 — `include: ['**/*.test.{ts,tsx}']`, `exclude: ['**/*.spec.*']`)
    - playwright.config.ts at repo root (confirm where Playwright collects from — typically `tests/e2e/**/*.spec.ts` plus `tests/visual/**`; if `tests/accessibility/**/*.spec.ts` is in the Playwright include, the rename target works)
    - .planning/phases/50-test-infrastructure-repair/.continue-here.md §CC-5
    - .planning/phases/50-test-infrastructure-repair/50-CONTEXT.md D-12 (Playwright out of Phase 50 scope; rename is acceptable disposition because vitest excludes `.spec.ts`)
  </read_first>
  <action>
    Single-file disposition. Read the file's first 30 lines to identify the test framework.

    **Path A (default — rename to .spec.ts):**

    If the file imports `test` from `@playwright/test` or uses Playwright-specific APIs (`page.goto`, `expect(page).toHaveTitle`, etc.), it is a Playwright test misnamed `.test.ts`. Rename it: `git mv frontend/tests/accessibility/entity-search.a11y.test.ts frontend/tests/accessibility/entity-search.a11y.spec.ts`. NO content changes — only the file extension.

    Verify the rename takes effect:
    - `git status` shows `R frontend/tests/accessibility/entity-search.a11y.test.ts -> frontend/tests/accessibility/entity-search.a11y.spec.ts`.
    - `pnpm --filter intake-frontend exec vitest --run --reporter=default 2>&amp;1 | grep -c "Playwright Test did not expect"` returns 0.
    - Playwright will continue to pick up the file when `pnpm test:visual` (or equivalent) runs because Playwright's own glob includes `tests/**/*.spec.ts`. Out of scope for this plan to verify Playwright runs — D-12 explicitly defers Playwright to Phase 52.

    Commit message: `fix(50-07): rename entity-search.a11y.test.ts to .spec.ts (CC-5 Playwright API misglobbed)`.

    **Path B (rewrite as vitest — only if Path A is inappropriate):**

    Choose Path B ONLY if `git log -p <file>` shows the file was authored as a vitest unit (e.g., uses `import { describe, it, expect } from 'vitest'`) and the `test.describe` call comes from an accidental import of Playwright's `test` symbol (overshadowing vitest's `test`). In that case, fix the import:
    - Replace `import { test } from '@playwright/test'` with `import { describe, it, expect } from 'vitest'`.
    - Replace any `test.describe(...)` with `describe(...)` and `test(...)` with `it(...)`.
    - Add `@testing-library/jest-dom` matchers (already registered globally by Plan 50-06 Task 2).
    - Wrap the rendered component in `renderWithProviders` from `@tests/utils/render` (Plan 50-06 Task 4) if it needs Language/Query context.

    Default to Path A unless Path B is clearly indicated.

    Do NOT delete the file — it is presumably a real a11y test, just in the wrong framework.

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; { test -f tests/accessibility/entity-search.a11y.spec.ts || grep -c "from 'vitest'" tests/accessibility/entity-search.a11y.test.ts | grep -q "^1$"; } &amp;&amp; pnpm exec vitest --run --reporter=default 2>&amp;1 | grep -c "Playwright Test did not expect" | grep -q "^0$"</automated>
  </verify>
  <done>
    Either `frontend/tests/accessibility/entity-search.a11y.spec.ts` exists (Path A — vitest no longer collects it) OR `frontend/tests/accessibility/entity-search.a11y.test.ts` imports from `'vitest'` instead of `'@playwright/test'` (Path B — rewritten as unit). FE runner output has zero `Playwright Test did not expect` errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolve CC-6 — wcag-compliance.test.ts + validation-speed.test.ts (per-file diagnosis)</name>
  <files>frontend/tests/a11y/wcag-compliance.test.ts, frontend/tests/performance/validation-speed.test.ts</files>
  <read_first>
    - frontend/tests/a11y/wcag-compliance.test.ts (full file — identify imports + check for invalid TS syntax)
    - frontend/tests/performance/validation-speed.test.ts (full file — same)
    - For each imported module, verify whether it exists on disk via `ls <path>` or `git log --diff-filter=D --name-only --pretty=format: -- <path>` to check deletion history
    - .planning/phases/50-test-infrastructure-repair/.continue-here.md §CC-6
    - .planning/phases/50-test-infrastructure-repair/50-CONTEXT.md D-09 (root-cause fixes allowed), D-10 (per-file archaeology)
    - .planning/STATE.md (Phase 33/34/36 module-rename history — same context Plan 50-01 G3 used)
  </read_first>
  <action>
    Per-file diagnosis using `pnpm exec vitest` + `git log -p` archaeology. Apply for each of the two files independently.

    **Step 1 — capture exact error per file:**

    ```
    cd frontend && pnpm exec vitest --run tests/a11y/wcag-compliance.test.ts 2>&1 | head -50
    cd frontend && pnpm exec vitest --run tests/performance/validation-speed.test.ts 2>&1 | head -50
    ```

    The esbuild `Transform failed` error names a specific line + column. Possible causes:
    - Import of a deleted module → error message shows the missing path.
    - Invalid TS syntax → error message points at the syntax position.
    - Use of a feature unsupported by the current esbuild target → message names the feature.

    **Step 2 — diagnose per file:**

    A) For each broken import, run `ls <imported-path>` to check existence. If absent, run `git log --diff-filter=D --follow --name-only --pretty=format: -- <imported-path>` to confirm deletion (vs rename).

    B) For each syntax error, read the named line and identify the actual problem (typo, missing comma, mismatched brace, unsupported TS feature).

    **Step 3 — disposition per file** (per D-10 archaeology):

    - **delete:** Test imports a module that was deleted in Phase 33/34/36 and is conceptually obsolete (i.e., the feature it tested no longer exists). Commit message: `fix(50-07): delete <file> (CC-6 import of deleted module <name>; obsolete per Phase XX-YY)`.
    - **repair:** Test has fixable syntax error or a renamed-import that needs path update. Commit message: `fix(50-07): repair <file> (CC-6 <root cause>)`.
    - **split-to-integration:** Test genuinely needs real infrastructure (`validation-speed.test.ts` may need real backend perf instrumentation). Add to `frontend/vitest.config.ts` `exclude` array and hand off to Plan 50-04 audit. Commit message: `split(50-07): <file> → integration (CC-6 needs <infra>)`.

    `queued-with-rationale` is NOT an acceptable disposition for the default FE runner per D-01. If neither delete, repair, nor split is viable, STOP and surface to operator — phase scope was misjudged.

    For each file, the per-file commit lists the root cause in the commit body. Plan 50-04 audit absorbs the per-file disposition into the `50-TEST-AUDIT.md` body.

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; { ! test -f tests/a11y/wcag-compliance.test.ts || pnpm exec vitest --run --reporter=default tests/a11y/wcag-compliance.test.ts 2>&amp;1 | tail -10 | grep -qvE "Transform failed"; } &amp;&amp; { ! test -f tests/performance/validation-speed.test.ts || pnpm exec vitest --run --reporter=default tests/performance/validation-speed.test.ts 2>&amp;1 | tail -10 | grep -qvE "Transform failed"; } &amp;&amp; pnpm exec vitest --run --reporter=default 2>&amp;1 | grep -c "Transform failed" | grep -q "^0$"</automated>
  </verify>
  <done>
    Both CC-6 files have a disposition: either deleted (file no longer exists), repaired (file exists and is collected without `Transform failed`), or split-to-integration (file no longer matches the default-runner glob; added to vitest.config.ts exclude). FE runner output has zero `Transform failed` errors. Each disposition has its own commit per D-10 per-file granularity.
  </done>
</task>

<task type="auto">
  <name>Task 3: Resolve PF-2 — fouc-bootstrap.test.ts scrape regex alignment</name>
  <files>frontend/tests/unit/design-system/fouc-bootstrap.test.ts</files>
  <read_first>
    - frontend/tests/unit/design-system/fouc-bootstrap.test.ts (full file — identify the 9 failing assertions + the scrape regex)
    - frontend/public/bootstrap.js (full file — identify the current literal shape for the 4 directions `bureau`, `chancery`, `situation`, `ministerial`; the 9 failures concern the 3 non-default directions)
    - frontend/src/design-system/tokens/directions.ts (canonical source of truth — confirm the 4-direction palette shape)
    - CLAUDE.md §Visual Design Source of Truth (FOUC bootstrap palette/font literals MUST byte-match `tokens/directions.ts`)
    - .planning/phases/50-test-infrastructure-repair/.continue-here.md §PF-2
    - .planning/phases/50-test-infrastructure-repair/50-CONTEXT.md D-09 (root-cause fixes allowed — updating bootstrap.js IS a product change if it's the source of drift)
  </read_first>
  <action>
    Decide whether the regex or bootstrap.js is the drifted side.

    **Step 1 — read the test:**

    Identify the scrape regex(es). The 9-failure breakdown is 3 directions × 3 things (likely light palette + dark palette + font). The test likely has:
    - A `describe` per direction (`chancery`, `situation`, `ministerial`)
    - An `it` per theme (`light`, `dark`) per direction, or per palette key.

    Note the exact regex pattern(s) used to extract literals from `frontend/public/bootstrap.js`. Typical pattern: `/chancery:\\s*\\{\\s*light:\\s*\\{([\\s\\S]*?)\\}/` or similar JSON-ish scrape.

    **Step 2 — read bootstrap.js:**

    Compare the bootstrap.js palette literal shape for `chancery`, `situation`, `ministerial` against:
    - (a) the test's regex (does the regex match the current literal?)
    - (b) `tokens/directions.ts` canonical form (per CLAUDE.md, bootstrap.js must byte-match this)

    **Step 3 — disposition:**

    Case I — `bootstrap.js` is the drifted side (its literal form is out of sync with `tokens/directions.ts`):
    - Update `bootstrap.js` to match `tokens/directions.ts` literally. This is the D-09 root-cause fix — restoring the byte-match contract per CLAUDE.md.
    - DO NOT change the test's regex (the regex is the contract).
    - Per CLAUDE.md "the FOUC bootstrap that paints first-frame tokens is at `frontend/public/bootstrap.js` — its palette/font literals must byte-match `tokens/directions.ts`."

    Case II — the test's regex is the drifted side (bootstrap.js correctly matches `tokens/directions.ts`, but the regex is too strict or written for an older shape):
    - Update the regex to match the current bootstrap.js literal shape.
    - DO NOT touch bootstrap.js.

    Case III — both are drifted from `tokens/directions.ts` (rare):
    - Update bootstrap.js to match `tokens/directions.ts` (D-09 root-cause).
    - Update the test regex to match the post-fix bootstrap.js shape.

    **Step 4 — verify:**

    After the fix, run `pnpm --filter intake-frontend exec vitest --run tests/unit/design-system/fouc-bootstrap.test.ts 2>&amp;1 | tail -15` and confirm ALL 9 sub-tests pass green. If any of the 9 still fail, the disposition was wrong — re-diagnose.

    Per CLAUDE.md "FOUC bootstrap that paints first-frame tokens" rule: bootstrap.js literals MUST stay in byte-match sync with `tokens/directions.ts`. This test exists explicitly to enforce that contract. Do NOT weaken the regex to be more permissive (e.g., adding `.skip` to sub-tests) — that defeats the drift-guard's purpose. If a true byte-mismatch is unfixable in this plan, escalate to operator (do NOT silently skip sub-tests).

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm exec vitest --run --reporter=default tests/unit/design-system/fouc-bootstrap.test.ts 2>&amp;1 | tail -10 | grep -E "Test Files.*passed" | grep -vE "[1-9]+[0-9]* failed"</automated>
  </verify>
  <done>
    `pnpm exec vitest --run tests/unit/design-system/fouc-bootstrap.test.ts` exits 0 (all 9 sub-tests pass green). One of three dispositions applied: (Case I) bootstrap.js updated to match tokens/directions.ts canonical form, (Case II) the test's regex updated to match current bootstrap.js shape, or (Case III) both. NO sub-tests were `.skip`-ed; NO weakened drift guard. Commit message documents which case applied.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                                                                                                                                        |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Vitest collection ↔ file extension         | The post-Plan-50-01 `include: ['**/*.test.{ts,tsx}']` + `exclude: ['**/*.spec.*']` makes file extension a security gate; renaming a file flips the gate.                           |
| Test regex ↔ production bootstrap literal  | The fouc-bootstrap test regex is the contract; if weakened, real first-frame paint drift can ship undetected.                                                                      |
| esbuild Transform failure ↔ runtime safety | A `Transform failed` masks the actual test (the file's contents are unreachable); deleting/repairing without root-cause understanding can silently remove a real regression check. |

## STRIDE Threat Register

| Threat ID  | Category                   | Component                                                                                                                                                                                    | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                 |
| ---------- | -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-50-07-01 | E (Elevation of privilege) | Renaming `entity-search.a11y.test.ts` to `.spec.ts` could mask a real a11y regression if Playwright doesn't actually pick the file up.                                                       | mitigate    | Task 1 verifies the rename and notes that Playwright collection verification is out of scope per D-12 (deferred to Phase 52). Plan 50-04 audit records the file as `disposition: split-to-playwright` (NOT `deleted-dead`) so a future agent re-includes it in the Playwright runner if needed. |
| T-50-07-02 | T (Tampering)              | Weakening the fouc-bootstrap regex would let a future contributor ship a bootstrap.js whose palette literals no longer byte-match `tokens/directions.ts`, causing FOUC during initial paint. | mitigate    | Task 3 explicitly forbids `.skip` on sub-tests and weakening the regex to be more permissive. The fix MUST be either bootstrap.js or the regex; either case preserves the byte-match contract. Threat is mitigated by Task 3's explicit guardrails + verify gate (all 9 sub-tests must pass).   |
| T-50-07-03 | T (Tampering)              | Deleting a CC-6 file could remove a real regression check if the diagnosis is wrong.                                                                                                         | mitigate    | Task 2 requires `git log --diff-filter=D` evidence to confirm the imported module was deleted (not renamed) before recommending deletion. Per-file commit message records the evidence. Plan 50-04 audit records the per-file disposition.                                                      |

</threat_model>

<verification>
**Phase-level checks (post-plan):**

- `frontend/tests/accessibility/entity-search.a11y.test.ts` either no longer exists (renamed to `.spec.ts`) or no longer imports from `'@playwright/test'` (rewritten as vitest).
- `frontend/tests/a11y/wcag-compliance.test.ts` either no longer exists, no longer produces `Transform failed`, or no longer matches the default-runner glob (split-to-integration).
- `frontend/tests/performance/validation-speed.test.ts` same condition as above.
- `frontend/tests/unit/design-system/fouc-bootstrap.test.ts` passes all 9 sub-tests.
- `pnpm --filter intake-frontend test --run` output contains 0 occurrences of `Playwright Test did not expect` and 0 occurrences of `Transform failed`.
- Each per-file disposition has a single commit with rationale in the commit body.
- `pnpm --filter intake-frontend lint` exits 0.

</verification>

<success_criteria>

- **TEST-02 progress:** Four outlier files removed from the residual failure set. Combined with Plan 50-06 (CC-1/CC-2/CC-3/CC-4), the residual passed to Plan 50-08 should be ≤7 files (per gap brief estimate of ~80% auto-clear from 50-06 alone, with 50-07 finishing the structural outliers).
- **CLAUDE.md byte-match contract preserved:** Whether the fouc-bootstrap fix is Case I, II, or III, the byte-match relationship between `frontend/public/bootstrap.js` and `tokens/directions.ts` is restored (or the drift-guard is updated to match the current canonical form).
- **No regression to Plan 50-01 or Plan 50-06 gains:** The 4 wizard tests still pass; the renderWithProviders migration is unaffected.
- **Cross-plan handoff to Plan 50-04 audit:** Each file's disposition (renamed / repaired / deleted / split) is recorded in this plan's SUMMARY for the audit to absorb.

</success_criteria>

<output>
After completion, create `.planning/phases/50-test-infrastructure-repair/50-07-SUMMARY.md` with:
- Per-file disposition for CC-5 (Path A vs Path B chosen + rationale)
- Per-file disposition for the two CC-6 files (delete / repair / split + git archaeology evidence)
- PF-2 disposition (Case I / II / III + which file was the drifted side)
- Pre/post failing-file counts captured from runner output at SUMMARY write time
- Confirmation that Plan 50-08's input set (residual files for per-file drift triage) is ≤12 per the operator gap brief ceiling
</output>
