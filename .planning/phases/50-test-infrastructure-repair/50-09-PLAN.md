---
phase: 50-test-infrastructure-repair
plan: 09
type: execute
wave: 2
depends_on:
  - 50-01
files_modified:
  - frontend/tests/setup.ts
  - frontend/tests/component/BulkActionToolbar.test.tsx
  - frontend/tests/component/EscalationDialog.test.tsx
  - frontend/tests/component/FilterPanel.test.tsx
  - frontend/tests/component/ConflictDialog.test.tsx
  - frontend/tests/component/ContributorsList.test.tsx
  - frontend/tests/component/AssignmentDetailsModal.test.tsx
  - frontend/tests/component/ReminderButton.test.tsx
autonomous: true
requirements:
  - TEST-02
  - TEST-04
user_setup: []

must_haves:
  truths:
    - 'AT TASK START, the executor re-runs `pnpm --filter intake-frontend exec vitest --run --reporter=default 2>&1 | tail -5` and counts failing files. If >8 ON FILES THIS PLAN OWNS (post-50-06 set listed in `files_modified`), STOP and surface to operator — phase scope was misjudged again.'
    - '`frontend/tests/setup.ts` defines a `globalThis.ResizeObserver` polyfill (constructor with `observe`/`unobserve`/`disconnect` no-op methods) AND a `window.matchMedia` polyfill (returns `{matches: false, media, addListener, removeListener, addEventListener, removeEventListener, dispatchEvent: () => false}`) at the top of the file BEFORE any `vi.mock` runs. Both polyfills are jsdom-environment-conditional (`if (typeof window !== "undefined")` for matchMedia; `globalThis.ResizeObserver ??= class {...}` for ResizeObserver) so node-env tests do not break.'
    - 'Each of the 7 component test files in this plan imports `renderWithProviders` from `@tests/utils/render` and replaces every existing `render` / `render(<X/>, {wrapper: ...})` call site. Local per-file `<I18nextProvider>` / `<QueryClientProvider>` / custom wrapper helpers (`createWrapper()` in `ReminderButton.test.tsx`) are DELETED — the provider stack now comes from `renderWithProviders` exclusively (LanguageProvider + fresh QueryClientProvider per render).'
    - "`tests/component/ReminderButton.test.tsx` `vi.mock('@/hooks/use-waiting-queue-actions', ...)` is hoisted to module top (currently inside `describe`) AND the test body that calls `vi.mocked(useReminderAction)` imports `useReminderAction` from `@/hooks/use-waiting-queue-actions` at module top so `vi.mocked()` has a real binding (no more `ReferenceError: useReminderAction is not defined`)."
    - 'After this plan lands, each of the 7 listed component test files passes green on its own (`pnpm exec vitest --run tests/component/<file>` exits 0 per file). Combined with the polyfill, the FE default-runner failing-file count drops monotonically. NO `queued-with-rationale` disposition is used for any file (D-01).'
  artifacts:
    - path: 'frontend/tests/setup.ts'
      provides: 'Test setup with ResizeObserver + matchMedia jsdom polyfills layered before existing jest-dom matchers + Supabase env stubs + react-i18next mock'
      contains: 'ResizeObserver'
    - path: 'frontend/tests/component/BulkActionToolbar.test.tsx'
      provides: 'BulkActionToolbar test using renderWithProviders (LanguageProvider supplied)'
      contains: '@tests/utils/render'
    - path: 'frontend/tests/component/ReminderButton.test.tsx'
      provides: 'ReminderButton test with top-level vi.mock + real import of useReminderAction for vi.mocked() binding'
      contains: 'vi.mocked(useReminderAction)'
  key_links:
    - from: 'frontend/tests/setup.ts ResizeObserver / matchMedia polyfills'
      to: '@radix-ui/react-use-size + frontend/src/components/responsive consumers'
      via: 'Constructor stubs satisfy module-eval-time `new ResizeObserver(...)` and runtime `window.matchMedia(...).matches` lookups inside Radix and useResponsive hook'
      pattern: 'globalThis\\.ResizeObserver|window\\.matchMedia'
    - from: '7 migrated test files'
      to: '@tests/utils/render renderWithProviders'
      via: "Each file replaces `import { render } from '@testing-library/react'` (or `render` plus custom wrappers) with `import { renderWithProviders as render, ... } from '@tests/utils/render'` and removes any local `<LanguageProvider>` / `<I18nextProvider>` / `createWrapper()` glue"
      pattern: 'renderWithProviders'
---

<objective>
Close the residual cross-cutting setup gaps that Plan 50-06 partially addressed. After 50-06 added jest-dom matchers, Supabase env stubs, and the `renderWithProviders` helper, the FE runner still reports 30 failing files. Investigation of `/tmp/phase50-06-task4-full-after-mock-fix.log` shows TWO remaining cross-cutting causes:

1. **Missing jsdom polyfills (ResizeObserver, matchMedia).** Several Radix-based components instantiate `ResizeObserver` at render-time (`@radix-ui/react-use-size`); jsdom does not provide it. `useResponsive` and similar hooks rely on `window.matchMedia` to compute viewport — jsdom returns `undefined` and the hook reports `'desktop'` regardless of CSS breakpoints. Both are global gaps. Plan 50-06 did not add either polyfill.

2. **7 component test files were NOT migrated to `renderWithProviders` in 50-06's Task 4** (only 9 of the affected 16 landed). The remaining 7 still use local `<I18nextProvider i18n={i18n}>` + manual `<QueryClientProvider>` glue, which does NOT satisfy the project's `LanguageProvider` context. The component impl (`useLanguage` from `@/components/language-provider/language-provider`) throws `useLanguage must be used within a LanguageProvider` because `<I18nextProvider>` is a different provider.

This plan adds the two polyfills AND migrates the 7 outstanding files. Together with 50-06's prior work, this should auto-clear the provider-cascade subset of the 30 residual files (verified by re-running the FE runner at task end and confirming a monotonic drop below 30).

The 7 files in scope (per `/tmp/phase50-06-task4-full-after-mock-fix.log` provider-error subset):

- `frontend/tests/component/BulkActionToolbar.test.tsx`
- `frontend/tests/component/EscalationDialog.test.tsx`
- `frontend/tests/component/FilterPanel.test.tsx`
- `frontend/tests/component/ConflictDialog.test.tsx`
- `frontend/tests/component/ContributorsList.test.tsx`
- `frontend/tests/component/AssignmentDetailsModal.test.tsx`
- `frontend/tests/component/ReminderButton.test.tsx` (additionally has a hoisting-shaped `vi.mocked()` reference error)

Plan 50-09 OWNS 8 file modifications (1 setup.ts + 7 tests). Below the ≤8 ceiling per the operator gap brief.

Purpose: Mechanically clear the provider-cascade and polyfill failures so subsequent per-class plans (50-10 i18n drift, 50-11 a11y/perf, 50-12 design-system + route harness) work on a smaller, semantically-coherent residual.

Output:

- `frontend/tests/setup.ts` augmented with `ResizeObserver` + `matchMedia` polyfills (existing jest-dom import, Supabase env stubs, react-i18next mock preserved verbatim).
- 7 component test files migrated to `renderWithProviders` with local provider glue + custom wrapper helpers deleted.
- `ReminderButton.test.tsx` `vi.mock` hoisted to module-top + `useReminderAction` imported at module-top so `vi.mocked(useReminderAction)` resolves.
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-test-infrastructure-repair/50-CONTEXT.md
@.planning/phases/50-test-infrastructure-repair/50-RESEARCH.md
@.planning/phases/50-test-infrastructure-repair/50-PATTERNS.md
@.planning/phases/50-test-infrastructure-repair/50-06-BLOCKER.archived.md
@.planning/phases/50-test-infrastructure-repair/50-06-PLAN.archived.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Do not re-explore. -->

From `frontend/tests/setup.ts` (CURRENT, post-50-06 — lines 1-7):

- Line 1: `import '@testing-library/jest-dom/vitest'` (CC-1, 50-06 Task 2)
- Line 2: `import { beforeAll, afterEach, afterAll, vi } from 'vitest'`
- Line 3: `import { cleanup } from '@testing-library/react'`
- Line 4: `import { server } from './mocks/server'`
- Line 5: blank
- Line 6: `vi.stubEnv('VITE_SUPABASE_URL', 'http://localhost:54321')` (CC-2)
- Line 7: `vi.stubEnv('VITE_SUPABASE_ANON_KEY', 'test-anon-key')` (CC-2)
- Lines 9+: `vi.mock('react-i18next', async () => { ... })` + MSW lifecycle (Plan 50-01 D-05 verbatim)
- Local style for this file: SINGLE QUOTES + NO SEMICOLONS (verified by Read of head)

The polyfill block goes immediately AFTER line 7 (`vi.stubEnv` for ANON_KEY) and BEFORE line 9 (`vi.mock`). This ordering keeps env stubs visible to module-eval consumers, and keeps polyfills installed BEFORE any factory or test setup touches `window` / `globalThis`. Side-effect imports (line 1) already register chai matchers; polyfills below them are fine because the polyfills are environment fixups, not chai matchers.

From `frontend/tests/utils/render.tsx` (CURRENT, post-50-06 — verified by Read):

- Exports `renderWithProviders(ui, options?)` wrapping `<QueryClientProvider><LanguageProvider>` (NOTE: project's LanguageProvider lives at `@/components/language-provider/language-provider`, NOT `@/contexts/LanguageContext` — the actual file ships LanguageProvider that exposes `useLanguage`).
- Re-exports everything from `@testing-library/react` so callers can `import { renderWithProviders as render, screen, waitFor, ... } from '@tests/utils/render'`.

From `frontend/src/components/language-provider/language-provider.tsx` (consumer that throws line 210):

- `useLanguage` throws `useLanguage must be used within a LanguageProvider` if the React context is missing.
- The failing tests render components like `<BulkActionToolbar/>` / `<FilterPanel/>` / `<ConflictDialog/>` / `<ContributorsList/>` / `<AssignmentDetailsModal/>` that transitively call `useLanguage` — none of them work under `<I18nextProvider i18n={i18n}>` alone. They REQUIRE `<LanguageProvider>` from project's own context. `renderWithProviders` supplies it.

Per-file CURRENT shape (read at planning time; verify at task time before editing):

- `BulkActionToolbar.test.tsx` lines 1-5: `import { render, screen, fireEvent } from '@testing-library/react'` + per-file `vi.mock('react-i18next', async () => ...)` block at lines 5-30. The per-file react-i18next mock is a workaround for the missing LanguageProvider — it should be DELETED once renderWithProviders supplies the provider; the global setup.ts mock already covers translation lookups.
- `EscalationDialog.test.tsx`: similar shape — local i18n mock + bare `render`.
- `FilterPanel.test.tsx`: local `<I18nextProvider>` wrapper + `global.ResizeObserver = vi.fn()...` polyfill at file top (lines 17-21).
- `ConflictDialog.test.tsx`: local `<I18nextProvider>` wrapper + `global.ResizeObserver = vi.fn()...` polyfill (lines 18-23).
- `ContributorsList.test.tsx`: local `<I18nextProvider>` + `global.ResizeObserver` (similar pattern).
- `AssignmentDetailsModal.test.tsx`: bare `render` + local `QueryClient` (lines 4-6) — no LanguageProvider at all today.
- `ReminderButton.test.tsx` lines 1-30: bare `render` + a `createWrapper()` helper at the bottom of the imports block + `vi.mock('@/hooks/use-waiting-queue-actions', () => ({ useReminderAction: () => ({...}) }))` at lines 12-22. The test body at line 86+ uses `vi.mocked(useReminderAction).mockReturnValue(...)` — but `useReminderAction` is never IMPORTED at module-top, so `vi.mocked()` receives `undefined`. Fix: add `import { useReminderAction } from '@/hooks/use-waiting-queue-actions'` at module-top so `vi.mocked()` has a real symbol.

Per-file LOCAL POLYFILLS that become REDUNDANT after this plan's setup.ts polyfill lands:

- `FilterPanel.test.tsx` lines 17-21: `global.ResizeObserver = vi.fn().mockImplementation(() => ({...}))` — DELETE after setup.ts polyfill is in place (per-file polyfill duplicates global; harmless but adds noise).
- `ConflictDialog.test.tsx` lines 18-23: same — DELETE.
- `ContributorsList.test.tsx` lines ~22-26: same — DELETE.

NOT every file has a local polyfill; only delete what exists.

From the operator gap brief at `50-06-BLOCKER.archived.md`:

- "≤8 failing files at plan-start" per-plan ceiling — verified by Task 0 below.
- "Task 1 re-run vitest, count failed files, STOP if > N" — Task 0 implements this gate.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 0: Discovery + ceiling-check — verify ≤8 files in this plan's scope still fail</name>
  <files>.planning/phases/50-test-infrastructure-repair/50-09-DISCOVERY.md</files>
  <read_first>
    - .planning/phases/50-test-infrastructure-repair/50-06-BLOCKER.archived.md (residual 30-file list + hard ceiling-discipline pattern)
    - /tmp/phase50-06-task4-full-after-mock-fix.log (the captured runner output if still on disk; otherwise re-run as below)
    - .planning/phases/50-test-infrastructure-repair/.continue-here.md if present (operator gap brief)
  </read_first>
  <action>
    Discovery + halt gate. Mirrors the archived 50-08 Task 0 pattern.

    1. Run `pnpm --filter intake-frontend exec vitest --run --reporter=default 2>&1 | tee /tmp/phase50-09-discovery.log`.

    2. Extract the count of failing files from the summary line (`Test Files N failed`) and the per-file list (look for `❯ <path> (... failed)` markers). Use:
       ```
       grep -E "^ ❯ .* \\(.*failed\\)" /tmp/phase50-09-discovery.log | awk '{print $2}' | sort -u
       ```

    3. **Ceiling check (this plan's scope is 7 component test files):** count how many of the 7 files in this plan's `files_modified` are present in the failing-set. The 7 files are:
       - tests/component/BulkActionToolbar.test.tsx
       - tests/component/EscalationDialog.test.tsx
       - tests/component/FilterPanel.test.tsx
       - tests/component/ConflictDialog.test.tsx
       - tests/component/ContributorsList.test.tsx
       - tests/component/AssignmentDetailsModal.test.tsx
       - tests/component/ReminderButton.test.tsx

       If MORE than 8 of THIS PLAN'S files are failing, STOP and surface to the operator. (8 is the ≤8 ceiling; this plan owns 7, so any expansion via newly-discovered drift in scope files triggers halt.)

       If FEWER than 7 are failing (some auto-closed by a transitive fix), proceed and document the auto-closed ones in the SUMMARY at end.

    4. Write `.planning/phases/50-test-infrastructure-repair/50-09-DISCOVERY.md` with:
       - ISO 8601 timestamp.
       - Total FE failing files count.
       - Per-this-plan-scope failing files (the 7 minus any auto-closed).
       - The pre-plan baseline 30 (from the BLOCKER).

    5. Commit: `chore(50-09): record discovery snapshot for ≤8 ceiling check`. The commit is audit evidence.

  </action>
  <verify>
    <automated>test -f /tmp/phase50-09-discovery.log &amp;&amp; test -f .planning/phases/50-test-infrastructure-repair/50-09-DISCOVERY.md &amp;&amp; grep -cE "tests/component/(BulkActionToolbar|EscalationDialog|FilterPanel|ConflictDialog|ContributorsList|AssignmentDetailsModal|ReminderButton)\.test\.tsx" .planning/phases/50-test-infrastructure-repair/50-09-DISCOVERY.md | awk '$1 &lt;= 8 { exit 0 } { print "HALT: " $1 " &gt; 8 in scope"; exit 1 }'</automated>
  </verify>
  <done>
    `50-09-DISCOVERY.md` exists with the failing-file enumeration. The count of in-scope files (the 7 component tests this plan owns) is ≤8. If >8, this task halted without advancing to Task 1.
  </done>
</task>

<task type="auto">
  <name>Task 1: Add ResizeObserver + matchMedia jsdom polyfills to tests/setup.ts</name>
  <files>frontend/tests/setup.ts</files>
  <read_first>
    - frontend/tests/setup.ts (full file — POST-50-06 state; line 1 jest-dom import, lines 6-7 vi.stubEnv, line 9+ react-i18next vi.mock)
    - /tmp/phase50-09-discovery.log (or /tmp/phase50-06-task4-full-after-mock-fix.log) — confirm `ResizeObserver is not defined` shows up in AfterActionForm.test.tsx and other Radix-consuming tests
    - frontend/src/hooks/useResponsive.ts (the consumer that reads `window.matchMedia` and reports `'desktop'` when undefined)
    - frontend/tests/component/FilterPanel.test.tsx lines 17-21 (the in-repo polyfill precedent — copy the shape verbatim, just install globally instead of per-file)
  </read_first>
  <action>
    Single-file edit. Insert TWO new polyfill blocks into `frontend/tests/setup.ts` AFTER the existing `vi.stubEnv` line for `VITE_SUPABASE_ANON_KEY` (currently line 7) and BEFORE the `vi.mock('react-i18next', ...)` call (currently line 9 or thereabouts).

    The two new blocks, exactly (match local style — SINGLE QUOTES, NO SEMICOLONS per the file's existing style; the existing setup.ts is single-quote / no-semicolon based — verify by re-reading the file before editing):

    ```
    // jsdom polyfill: ResizeObserver (used by @radix-ui/react-use-size + signature-visuals)
    if (typeof globalThis.ResizeObserver === 'undefined') {
      globalThis.ResizeObserver = class ResizeObserver {
        observe(): void {}
        unobserve(): void {}
        disconnect(): void {}
      }
    }

    // jsdom polyfill: window.matchMedia (used by useResponsive + Tailwind breakpoint hooks)
    if (typeof window !== 'undefined' && typeof window.matchMedia === 'undefined') {
      Object.defineProperty(window, 'matchMedia', {
        writable: true,
        configurable: true,
        value: (query: string): MediaQueryList => ({
          matches: false,
          media: query,
          onchange: null,
          addListener: () => {},
          removeListener: () => {},
          addEventListener: () => {},
          removeEventListener: () => {},
          dispatchEvent: () => false,
        }),
      })
    }
    ```

    Placement rationale:
    - AFTER `vi.stubEnv` (so the env contract is set first; mirrors 50-06 ordering decision).
    - BEFORE `vi.mock` (so any module the mock factory transitively imports already has the polyfills available; relevant for the react-i18next factory's transitive resolution of `react-dom` which sometimes touches `window.matchMedia` at module-eval).

    Each polyfill is GUARDED with `typeof X === 'undefined'` so:
    - Tests that run in `node` env (rare in FE, but possible) skip the matchMedia block.
    - Tests that ship their own polyfill (e.g., `FilterPanel.test.tsx` still has its own — until Task 2 below removes the per-file one) are not double-installed (the guard makes re-install a no-op).

    DO NOT touch any other line in setup.ts. DO NOT reorder existing imports. DO NOT change the react-i18next mock factory. DO NOT change the MSW lifecycle block at the bottom of the file.

    After the edit, run `pnpm --filter intake-frontend exec vitest --run tests/component/AfterActionForm.test.tsx 2>&1 | grep -c "ResizeObserver is not defined"` — must return 0 (the polyfill installed). The test may still fail on other assertions; that is Plan 50-10's domain.

    Commit: `fix(50-09): add ResizeObserver + matchMedia jsdom polyfills to tests/setup.ts`. Body lists the two polyfills and notes that per-file polyfills in FilterPanel/ConflictDialog/ContributorsList become redundant (deleted in Task 2).

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -c "globalThis\\.ResizeObserver" tests/setup.ts | grep -q "^1$" &amp;&amp; grep -c "window\\.matchMedia" tests/setup.ts | grep -q "^1$" &amp;&amp; pnpm exec vitest --run tests/component/AfterActionForm.test.tsx 2&gt;&amp;1 | grep -c "ResizeObserver is not defined" | grep -q "^0$"</automated>
  </verify>
  <done>
    `frontend/tests/setup.ts` defines `globalThis.ResizeObserver` and `window.matchMedia` (each guarded by `typeof === 'undefined'`). Running any Radix-consuming test (e.g., AfterActionForm) no longer surfaces `ResizeObserver is not defined`. The existing jest-dom import, Supabase env stubs, react-i18next mock, and MSW lifecycle blocks remain byte-identical.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate 7 component tests to renderWithProviders + fix ReminderButton hoisting</name>
  <files>frontend/tests/component/BulkActionToolbar.test.tsx, frontend/tests/component/EscalationDialog.test.tsx, frontend/tests/component/FilterPanel.test.tsx, frontend/tests/component/ConflictDialog.test.tsx, frontend/tests/component/ContributorsList.test.tsx, frontend/tests/component/AssignmentDetailsModal.test.tsx, frontend/tests/component/ReminderButton.test.tsx</files>
  <read_first>
    - Each of the 7 listed files in full — to identify the existing `render` call sites, any `<I18nextProvider>` / `<QueryClientProvider>` / `createWrapper()` glue, and any per-file `vi.mock` blocks that may need preservation
    - frontend/tests/utils/render.tsx (the helper signature + provider stack — LanguageProvider + QueryClientProvider)
    - frontend/src/components/language-provider/language-provider.tsx (confirm `useLanguage` throws when context is missing — line 210)
    - .planning/phases/50-test-infrastructure-repair/50-CONTEXT.md D-09 (root-cause fixes allowed), D-11 (no bulk i18n-key rewrites — assertions stay as authored)
    - .planning/phases/50-test-infrastructure-repair/50-06-PLAN.archived.md Task 4 (the canonical migration pattern that the 9 already-migrated files use — mirror that pattern verbatim for these 7)
    - frontend/tests/component/AfterActionForm.test.tsx (an EXAMPLE of a successfully-migrated file from 50-06 — its imports show the canonical `renderWithProviders as render` alias pattern; copy that import shape)
  </read_first>
  <action>
    Seven-file mechanical migration. Each file follows the same template; only the per-file vi.mock / wrapper-removal details differ.

    **Common migration steps per file:**

    1. Replace the `render` import:
       - REMOVE: `import { render, screen, ... } from '@testing-library/react'` (or any equivalent).
       - ADD: `import { renderWithProviders as render, screen, ... } from '@tests/utils/render'` — preserve the other named imports (`screen`, `fireEvent`, `waitFor`, `within`, `userEvent` stays from `@testing-library/user-event`).

    2. Remove local provider glue (only if present):
       - DELETE any `import { QueryClient, QueryClientProvider } from '@tanstack/react-query'` at the top of the file IF the only use was to wrap a render — the helper provides a fresh QueryClient per render.
       - DELETE any `import { I18nextProvider } from 'react-i18next'` + `import i18n from '../../src/i18n'` glue.
       - DELETE any `<I18nextProvider i18n={i18n}>` / `<QueryClientProvider client={queryClient}>` wrappers at call sites. The helper's wrapper supplies LanguageProvider + QueryClientProvider; nested wrappers are harmless but noisy.
       - DELETE any local `createWrapper()` / `renderWithWrappers()` helpers (e.g., `ReminderButton.test.tsx` defines one).

    3. Remove local ResizeObserver polyfills (only if present — Task 1's global polyfill supersedes):
       - In `FilterPanel.test.tsx`, `ConflictDialog.test.tsx`, `ContributorsList.test.tsx`: DELETE the `global.ResizeObserver = vi.fn().mockImplementation(...)` block near the top (typically lines 17-23).

    4. Preserve all OTHER per-file `vi.mock(...)` calls (especially mocks of feature-specific hooks, services, child components — those are deliberate test isolation).

    5. Verify: `pnpm --filter intake-frontend exec vitest --run <file>` — must exit 0. If a test fails on an i18n-text assertion (`'Bulk Actions'` not found because t() returns 'bulk_actions.label'), that is Plan 50-10's domain — do NOT rewrite assertions in this plan (D-11). Only mechanical provider migration here.

    **Per-file additional fix:**

    **`BulkActionToolbar.test.tsx`:**
    - The file has a LOCAL `vi.mock('react-i18next', async () => { ... ({ t: key => translations[key] || key }) ... })` at lines 5-30 that overrides the global setup.ts mock with a richer translation map. This is a workaround for missing LanguageProvider — DELETE the local vi.mock entirely; the global setup.ts mock + global LanguageProvider supplied by renderWithProviders is the right shape. The component reads from `useLanguage`, not `useTranslation` directly for non-i18n bits, so the global mock suffices.
    - Replace `render(<BulkActionToolbar ... />)` call sites with the aliased `render(<BulkActionToolbar ... />)` (same call shape; alias hides the implementation swap).

    **`EscalationDialog.test.tsx`:**
    - Standard provider migration. If the file has its own per-file `vi.mock('react-i18next', ...)`, DELETE it (the global setup.ts mock is sufficient).

    **`FilterPanel.test.tsx`:**
    - Standard provider migration + DELETE local `global.ResizeObserver` polyfill (Task 1 supplied globally).
    - The file's `beforeEach` recreates `queryClient` — DELETE that block; the helper makes a fresh client per render.

    **`ConflictDialog.test.tsx`:**
    - Standard provider migration + DELETE local `global.ResizeObserver` polyfill.
    - The reported `TypeError: Cannot convert undefined or null to object` at `ConflictDialog.tsx:59` (`Object.keys(localChanges)`) is from a test rendering `<ConflictDialog ... />` with `localChanges={undefined}`. After provider migration, the same test will still fail with the same error UNLESS the test passes a defined `localChanges` prop. Inspect the failing test cases:
      - For each `render(<ConflictDialog />)` call whose props omit `localChanges`, ADD `localChanges={{}}` (empty object) as a sane default — the impl's `Object.keys(localChanges)` then returns `[]` instead of throwing. This is a test-fixture fix (D-10: TEST WRONG verdict) NOT an impl change.
      - If multiple call sites omit the prop, use a `createDefaultProps()` factory in the test file's `describe` setup so it stays DRY.

    **`ContributorsList.test.tsx`:**
    - Standard provider migration + DELETE local `global.ResizeObserver` polyfill.
    - This file ALSO imports `Database` from `backend/src/types/database.types` — leave that import alone; it is a type-only import, not a runtime concern.

    **`AssignmentDetailsModal.test.tsx`:**
    - Standard provider migration. The local `QueryClient` + per-test `beforeEach` clearing pattern goes away (helper makes fresh client per render).
    - If any test passes a `wrapper:` option to `render(...)`, REMOVE the wrapper option (helper sets `wrapper` internally; supplying another wrapper overrides ours).

    **`ReminderButton.test.tsx` (additional hoisting fix):**
    - Standard provider migration: replace local `render` + `createWrapper()` calls with `renderWithProviders as render`.
    - DELETE the `createWrapper()` helper function at the top of the file.
    - **Fix the `vi.mocked(useReminderAction)` ReferenceError:** At module-top (after the `import { ... } from '@testing-library/react'` line is replaced with the alias), ADD:
      ```
      import { useReminderAction } from '@/hooks/use-waiting-queue-actions'
      import { useToast } from '@/hooks/use-toast'
      ```
      These imports give `vi.mocked(useReminderAction)` and `vi.mocked(useToast)` a real binding to retrieve the mocked module's symbol. Without these imports the symbols are undefined at module-eval, and `vi.mocked(undefined)` throws.
    - Confirm the `vi.mock('@/hooks/use-waiting-queue-actions', () => ({ useReminderAction: ... }))` block is at MODULE TOP (above `describe`); if it is nested inside `describe`, MOVE it to module top per the vitest hoisting warning surfaced in the runner log: "vi.mock call is not at the top level of the module."
    - Same hoisting check for `vi.mock('@/hooks/use-toast', ...)`.

    **Per-file commits (one per file is acceptable; alternatively a single commit covering all 7 with a path-listing body is also fine):**

    Recommended messages:
    - `fix(50-09): migrate BulkActionToolbar test to renderWithProviders + drop local i18n mock`
    - `fix(50-09): migrate EscalationDialog test to renderWithProviders`
    - `fix(50-09): migrate FilterPanel test to renderWithProviders + drop local ResizeObserver polyfill`
    - `fix(50-09): migrate ConflictDialog test + add default localChanges fixture (D-10: TEST WRONG)`
    - `fix(50-09): migrate ContributorsList test to renderWithProviders`
    - `fix(50-09): migrate AssignmentDetailsModal test to renderWithProviders`
    - `fix(50-09): hoist ReminderButton vi.mock + import bindings for vi.mocked() resolution`

    After all migrations land, run `pnpm --filter intake-frontend lint frontend/tests/component/{BulkActionToolbar,EscalationDialog,FilterPanel,ConflictDialog,ContributorsList,AssignmentDetailsModal,ReminderButton}.test.tsx` — must exit 0.

    Run `pnpm --filter intake-frontend exec vitest --run frontend/tests/component/BulkActionToolbar.test.tsx frontend/tests/component/EscalationDialog.test.tsx frontend/tests/component/FilterPanel.test.tsx frontend/tests/component/ConflictDialog.test.tsx frontend/tests/component/ContributorsList.test.tsx frontend/tests/component/AssignmentDetailsModal.test.tsx frontend/tests/component/ReminderButton.test.tsx 2>&1 | tail -5` — expect `Test Files: 7 passed (7)` OR per-file partial passes where remaining failures are i18n-text-assertion drift (which Plan 50-10 takes). If a file is still ALL-failing (≥10 fails) after migration, surface — there's a non-mechanical cause this plan missed.

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; for f in tests/component/BulkActionToolbar.test.tsx tests/component/EscalationDialog.test.tsx tests/component/FilterPanel.test.tsx tests/component/ConflictDialog.test.tsx tests/component/ContributorsList.test.tsx tests/component/AssignmentDetailsModal.test.tsx tests/component/ReminderButton.test.tsx; do grep -q "renderWithProviders" "$f" || { echo "FAIL: $f not migrated"; exit 1; }; done &amp;&amp; pnpm exec vitest --run --reporter=default tests/component/BulkActionToolbar.test.tsx tests/component/EscalationDialog.test.tsx tests/component/FilterPanel.test.tsx tests/component/ConflictDialog.test.tsx tests/component/ContributorsList.test.tsx tests/component/AssignmentDetailsModal.test.tsx tests/component/ReminderButton.test.tsx 2&gt;&amp;1 | grep -c "useLanguage must be used within a LanguageProvider" | grep -q "^0$"</automated>
  </verify>
  <done>
    Each of the 7 files imports `renderWithProviders` from `@tests/utils/render` (`grep` verified). Running the 7 files together no longer surfaces `useLanguage must be used within a LanguageProvider` — the LanguageProvider context is supplied by the helper. ReminderButton.test.tsx's `vi.mocked(useReminderAction)` no longer throws `ReferenceError`. Workspace lint exits 0 on the 7 files. Remaining failures (if any) are i18n-text-assertion drift, deferred to Plan 50-10.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                                                                                                                                                                                                   |
| ------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Test runner ↔ global polyfills  | `globalThis.ResizeObserver` and `window.matchMedia` are installed for the runner lifecycle; they must not leak shape changes that mask real production-time polyfill gaps (the production app ships its own polyfills as part of the bundle). |
| Test util ↔ component providers | `renderWithProviders` substitutes the project's LanguageProvider; tests intending to validate provider-absence behavior must opt out via direct `render` from `@testing-library/react`.                                                       |

## STRIDE Threat Register

| Threat ID  | Category                   | Component                                                                                                                                                               | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                     |
| ---------- | -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-50-09-01 | T (Tampering)              | A future contributor replaces the polyfill block's `if (typeof X === 'undefined')` guard with an unconditional install, masking a real-environment polyfill regression. | mitigate    | Plan 50-04's `frontend/docs/test-setup.md` (TEST-04) documents the polyfill contract + guard rationale. The guard pattern is the documented shape; ESLint cannot statically catch its removal — relies on PR review.                                                                |
| T-50-09-02 | I (Information disclosure) | The matchMedia stub returns `matches: false` for every query, which could mask a viewport-dependent regression test by always reporting "desktop".                      | accept      | Tests that need viewport simulation override `window.matchMedia` per-test (the existing `responsive.test.ts` pattern). Default `matches: false` is the safest shape — viewport-aware tests fail loud (assertion mismatch) rather than silent (NPE).                                 |
| T-50-09-03 | E (Elevation of privilege) | Migrating to renderWithProviders could mask a regression where a component should fail in absence of LanguageProvider but now silently works.                           | accept      | The 7 migrated components all transitively call `useLanguage` and SHOULD fail without LanguageProvider. The provider is a hard runtime dependency — migration is correctness-preserving. Tests that intentionally validate provider-absence must use bare `render` (none in scope). |

</threat_model>

<verification>
**Phase-level checks (post-plan):**

- `frontend/tests/setup.ts` defines `globalThis.ResizeObserver` AND `window.matchMedia` (both guarded). The existing jest-dom import (line 1), Supabase env stubs (lines 6-7), react-i18next mock, and MSW lifecycle remain byte-identical.
- Each of the 7 component test files imports `renderWithProviders` (or `renderWithProviders as render`) from `@tests/utils/render`.
- No file in scope still uses local `<I18nextProvider>` or `<QueryClientProvider>` wrappers as the primary render wrapper.
- `ReminderButton.test.tsx` imports `useReminderAction` and `useToast` at module top so `vi.mocked()` resolves.
- The runner output for the 7 in-scope files contains 0 occurrences of `useLanguage must be used within a LanguageProvider` AND 0 occurrences of `ReferenceError: useReminderAction is not defined`.
- `pnpm --filter intake-frontend lint` exits 0 on the 7 modified files.
- The FE runner failing-files count is DOWN from 30 (capture pre/post in the SUMMARY commit message). Expected drop: 7 minus any that remain failing on i18n-text-assertion drift (Plan 50-10's domain).
  </verification>

<success_criteria>

- **TEST-02 progress:** 7 provider-cascade failures cleared (and the ResizeObserver polyfill auto-clears Radix-consuming tests like AfterActionForm). Cumulative phase-exit-0 contract delegated to Plan 50-13.
- **TEST-04 progress:** Polyfill block becomes a canonical pattern Plan 50-04's docs will cite. No production docs touched here.
- **D-01 honored:** No `queued-with-rationale` for any file remaining in default runner. Provider migrations are mechanical; failing tests after migration roll over to Plan 50-10.
- **D-09/D-10 applied for ConflictDialog:** Adding a `localChanges={}` default in test fixtures is a TEST WRONG verdict (the impl correctly throws on `Object.keys(undefined)`).
- **Ceiling discipline honored:** Task 0 verified ≤8 in-scope files.

</success_criteria>

<output>
After completion, create `.planning/phases/50-test-infrastructure-repair/50-09-SUMMARY.md` with:
- Pre/post failing-file counts (30 → expected ≤23 after this plan).
- The polyfill block as authored in setup.ts (paste verbatim for audit reference).
- Per-file migration commit hashes (one per migrated file or one consolidated commit).
- Note that `ConflictDialog.test.tsx` test fixtures received a `localChanges={}` default per D-10 TEST WRONG verdict.
- Handoff to Plan 50-10 (i18n-text-assertion drift in the already-migrated component tests).
- Retain `.planning/phases/50-test-infrastructure-repair/50-09-DISCOVERY.md` as a permanent audit artifact for Plan 50-04 (per PCH-50R-08 — DO NOT delete).
</output>
</content>
