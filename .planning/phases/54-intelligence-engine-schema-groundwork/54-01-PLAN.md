---
phase: 54
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260516000001_phase54_rename_phase45_intelligence_digest.sql
  - supabase/seed/060-dashboard-demo.sql
  - frontend/src/hooks/useDashboardDigest.ts
  - frontend/src/hooks/useIntelligenceDigest.ts
  - frontend/src/hooks/__tests__/useDashboardDigest.test.ts
  - frontend/src/hooks/__tests__/useIntelligenceDigest.test.ts
  - frontend/src/pages/Dashboard/widgets/Digest.tsx
  - frontend/src/pages/Dashboard/widgets/__tests__/Digest.test.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - 'phase-54-base annotated + SSH-signed tag exists locally and `git tag -v phase-54-base` exits 0 with `Good "git" signature`.'
    - 'D-01: Postgres table previously named `intelligence_digest` is now named `dashboard_digest` on staging (`zkrcjzdemdmwhearhfgg`); indexes and RLS policies follow it via `ALTER`.'
    - "D-02: The single frontend caller of the renamed table reads via `useDashboardDigest()` from `frontend/src/hooks/useDashboardDigest.ts` against `.from('dashboard_digest')`."
    - 'D-03: Seed file `supabase/seed/060-dashboard-demo.sql` references the renamed table everywhere (`DELETE FROM dashboard_digest`, `INSERT INTO dashboard_digest`, RAISE NOTICE text); historical Phase-45 migration `20260507210000_phase45_intelligence_digest_seed.sql` is NOT retro-edited.'
    - '`useIntelligenceDigest.ts` and its test do NOT exist on disk after this plan — they are deleted via rename.'
    - "D-15: This plan's schema change is applied to staging (`zkrcjzdemdmwhearhfgg`) via Supabase MCP `apply_migration` — NOT local CLI."
  artifacts:
    - path: 'supabase/migrations/20260516000001_phase54_rename_phase45_intelligence_digest.sql'
      provides: 'ALTER TABLE rename + index renames + policy renames + COMMENT'
      contains: 'ALTER TABLE IF EXISTS public.intelligence_digest RENAME TO dashboard_digest'
    - path: 'frontend/src/hooks/useDashboardDigest.ts'
      provides: 'Dashboard digest read hook against renamed table'
      exports: ['useDashboardDigest', 'DashboardDigestRow']
      contains: ".from('dashboard_digest')"
    - path: 'frontend/src/hooks/__tests__/useDashboardDigest.test.ts'
      provides: 'Vitest spec for renamed hook'
      contains: 'useDashboardDigest'
    - path: 'frontend/src/pages/Dashboard/widgets/Digest.tsx'
      provides: 'Dashboard Digest widget consuming renamed hook'
      contains: 'useDashboardDigest'
    - path: 'frontend/src/pages/Dashboard/widgets/__tests__/Digest.test.tsx'
      provides: 'Widget test mocking renamed hook'
      contains: '@/hooks/useDashboardDigest'
    - path: 'supabase/seed/060-dashboard-demo.sql'
      provides: 'Seed pointing at dashboard_digest'
      contains: 'dashboard_digest'
  key_links:
    - from: 'frontend/src/pages/Dashboard/widgets/Digest.tsx'
      to: 'frontend/src/hooks/useDashboardDigest.ts'
      via: 'import { useDashboardDigest, type DashboardDigestRow }'
      pattern: 'useDashboardDigest'
    - from: 'frontend/src/hooks/useDashboardDigest.ts'
      to: 'public.dashboard_digest (staging table)'
      via: "supabase.from('dashboard_digest')"
      pattern: "from\\('dashboard_digest'\\)"
    - from: 'phase-54-base tag'
      to: 'current HEAD'
      via: 'git tag -a -s phase-54-base'
      pattern: 'phase-54-base'
---

<objective>
Open Phase 54 with the `phase-54-base` annotated + SSH-signed git tag for provenance, then lift Phase-45's `intelligence_digest` table out of the way so the canonical name `intelligence_digest` is free for plan 54-02's new spec-shape table. The DB rename, seed update, frontend hook rename, hook test rename, widget import update, and widget test update all ship in this single wave so `pnpm type-check` and existing Vitest specs stay green between waves.

Purpose: Anchors the phase diff to a verifiable signed tag (per CLAUDE.md tag-signing appendix and Phase-53 BUNDLE-06 precedent), and frees the `intelligence_digest` name atomically so plan 54-02 can create the new table without hitting SQLSTATE `42P07` duplicate_table.

Output:

- `phase-54-base` annotated + SSH-signed tag created locally and pushed to origin.
- New migration `supabase/migrations/20260516000001_phase54_rename_phase45_intelligence_digest.sql` written to disk and applied to staging via Supabase MCP.
- Old hook + its test renamed to `useDashboardDigest.*`; widget + widget test updated; seed updated.
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/54-intelligence-engine-schema-groundwork/54-CONTEXT.md
@.planning/phases/54-intelligence-engine-schema-groundwork/54-RESEARCH.md
@.planning/phases/54-intelligence-engine-schema-groundwork/54-PATTERNS.md
@.planning/phases/54-intelligence-engine-schema-groundwork/54-VALIDATION.md
@CLAUDE.md

# Existing migration we MUST NOT retro-edit (D-03) — read for RLS / policy / index names that need renaming

@supabase/migrations/20260507210000_phase45_intelligence_digest_seed.sql

# Files being renamed/edited — read full current state before touching

@frontend/src/hooks/useIntelligenceDigest.ts
@frontend/src/hooks/**tests**/useIntelligenceDigest.test.ts
@frontend/src/pages/Dashboard/widgets/Digest.tsx
@frontend/src/pages/Dashboard/widgets/**tests**/Digest.test.tsx
@supabase/seed/060-dashboard-demo.sql

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from current useIntelligenceDigest.ts. -->
<!-- Executor should preserve these — only the IDENTIFIER NAMES change (Intelligence -> Dashboard). -->

From frontend/src/hooks/useIntelligenceDigest.ts (current — becomes useDashboardDigest.ts):

```typescript
export interface IntelligenceDigestRow {
  id: string
  headline_en: string
  headline_ar: string | null
  summary_en: string | null
  summary_ar: string | null
  source_publication: string
  occurred_at: string
  dossier_id: string | null
  created_at: string
}

export function useIntelligenceDigest(
  limit: number = 6,
): ReturnType<typeof useQuery<IntelligenceDigestRow[], Error>>
```

Rename map (RESEARCH § Code Examples L676-714):

- `IntelligenceDigestRow` → `DashboardDigestRow`
- `INTELLIGENCE_DIGEST_SELECT` → `DASHBOARD_DIGEST_SELECT`
- `useIntelligenceDigest` → `useDashboardDigest`
- `queryKey: ['dashboard', 'intelligence-digest', limit]` → `['dashboard', 'dashboard-digest', limit]`
- `.from('intelligence_digest')` → `.from('dashboard_digest')`
- `'Failed to fetch intelligence digest:'` → `'Failed to fetch dashboard digest:'`
- Column list stays identical (Phase-45 shape unchanged — only the table name moved).
  </interfaces>
  </context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create + push phase-54-base annotated + SSH-signed tag</name>
  <files>(git ref only — no on-disk files modified)</files>
  <read_first>
    - CLAUDE.md § "Tag signing setup" (lines documenting `~/.gitconfig` + `~/.ssh/allowed_signers` requirement)
    - .planning/phases/53-bundle-tightening-tag-provenance/53-03-PLAN.md (BUNDLE-06 precedent for signed phase-base tags)
    - .planning/phases/54-intelligence-engine-schema-groundwork/54-VALIDATION.md § "Manual-Only Verifications"
  </read_first>
  <what-built>
    Operator must create + push the `phase-54-base` tag. Reason this is a human-action checkpoint (not autonomous): SSH signing requires the operator's local key passphrase + private signing-key access, which Claude cannot perform. CLI is the action; the operator runs it.
  </what-built>
  <how-to-verify>
    Operator runs these exact commands at the repo root:

    1. Confirm signing config is set globally (one-time per machine — see CLAUDE.md):
       `git config --global --get gpg.format`           → must print `ssh`
       `git config --global --get user.signingkey`      → must print path to enrolled `~/.ssh/<key>.pub`
       `git config --global --get gpg.ssh.allowedSignersFile` → must print path to `~/.ssh/allowed_signers`

    2. Create annotated + signed tag at current HEAD:
       `git tag -a -s phase-54-base -m "Phase 54 baseline: Intelligence Engine Schema Groundwork (INTEL-01..05)"`

    3. Verify locally:
       `git cat-file -t phase-54-base`           → must print `tag` (annotated, not lightweight)
       `git tag -v phase-54-base 2>&1`           → must include `Good "git" signature` AND exit 0

    4. Push to origin:
       `git push origin phase-54-base`           → must succeed (annotated tag with signature pushed)

    5. Confirm remote has it:
       `git ls-remote --tags origin phase-54-base` → must list the tag SHA

    Paste the output of step 3 (`git tag -v phase-54-base`) and step 5 in the resume signal.

  </how-to-verify>
  <acceptance_criteria>
    - `git cat-file -t phase-54-base` exits 0 and stdout equals `tag` (annotated form, not `commit` lightweight form).
    - `git tag -v phase-54-base` exits 0 AND stderr/stdout contains the substring `Good "git" signature` (literal — quotes around `git`).
    - `git ls-remote --tags origin phase-54-base` exits 0 and prints one line containing `refs/tags/phase-54-base`.
  </acceptance_criteria>
  <resume-signal>Paste the stdout/stderr of `git tag -v phase-54-base` and `git ls-remote --tags origin phase-54-base`, then type "approved".</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply rename migration to staging via Supabase MCP</name>
  <files>supabase/migrations/20260516000001_phase54_rename_phase45_intelligence_digest.sql</files>
  <read_first>
    - supabase/migrations/20260507210000_phase45_intelligence_digest_seed.sql (full file — note current table name, current 3 index names `idx_intelligence_digest_org_occurred_at`, `idx_intelligence_digest_dossier`, `idx_intelligence_digest_source_publication`, and current 4 policy names `intelligence_digest_select_org`, `intelligence_digest_insert_editor`, `intelligence_digest_update_editor`, `intelligence_digest_delete_admin`).
    - .planning/phases/54-intelligence-engine-schema-groundwork/54-RESEARCH.md § "Pattern 3: `dashboard_digest` rename mechanics (D-01)" L328-373 — verbatim SQL skeleton to write.
    - .planning/phases/54-intelligence-engine-schema-groundwork/54-PATTERNS.md § "supabase/migrations/20260516000001_phase54_rename_phase45_intelligence_digest.sql" L36-72.
    - 54-CONTEXT.md D-01, D-03, D-15.
  </read_first>
  <action>
    (a) Create file `supabase/migrations/20260516000001_phase54_rename_phase45_intelligence_digest.sql` containing the verbatim SQL skeleton from `54-RESEARCH.md` L348-373:
        - `ALTER TABLE IF EXISTS public.intelligence_digest RENAME TO dashboard_digest;`
        - 3 `ALTER INDEX IF EXISTS ... RENAME TO ...` statements (one per existing `idx_intelligence_digest_*` index from `20260507210000_phase45_intelligence_digest_seed.sql` L22-28)
        - 4 `ALTER POLICY ... ON public.dashboard_digest RENAME TO dashboard_digest_*` statements (one per existing policy from the Phase-45 migration L30-67)
        - `COMMENT ON TABLE public.dashboard_digest IS 'Dashboard headline feed (Phase 45). Renamed from intelligence_digest in Phase 54 to free the canonical name for the v7.0 spec-compliant intelligence_digest.';`

    (b) Do NOT retro-edit `supabase/migrations/20260507210000_phase45_intelligence_digest_seed.sql` (Pitfall: D-03 forbids editing historical migrations).

    (c) Apply the migration to staging via the MCP call (D-15):
        `mcp__claude_ai_Supabase__apply_migration(project_id="zkrcjzdemdmwhearhfgg", name="20260516000001_phase54_rename_phase45_intelligence_digest", query=<contents of the .sql file>)`

    (d) Post-apply verification — call `mcp__claude_ai_Supabase__list_tables(schemas=["public"])` and confirm:
        - `dashboard_digest` is in the returned table list
        - `intelligence_digest` is NOT in the returned table list (it was renamed, not duplicated)

    (e) Additional verification — call `mcp__claude_ai_Supabase__execute_sql(project_id="zkrcjzdemdmwhearhfgg", query="SELECT polname FROM pg_policy WHERE polrelid = 'public.dashboard_digest'::regclass ORDER BY polname")` and confirm the 4 policy names returned start with `dashboard_digest_` (not `intelligence_digest_`).

    (f) Update `supabase/seed/060-dashboard-demo.sql` at three sites (verified line numbers per 54-PATTERNS.md L477-479):
        - L104 `DELETE FROM intelligence_digest WHERE id::text LIKE 'b0000010-%';` → `DELETE FROM dashboard_digest WHERE id::text LIKE 'b0000010-%';`
        - L342 `INSERT INTO intelligence_digest (...) VALUES (...)` → `INSERT INTO dashboard_digest (...) VALUES (...)`
        - L367 `RAISE NOTICE 'Seed 060 complete: ... 4 intelligence_digest.';` → `... 4 dashboard_digest.`
        (Note: grep the file before editing — line numbers may have shifted since the pattern map was authored. Replace by content match, not line number.)

    Pitfall reminders:
    - Do NOT drop `intelligence_signal` (singular) literals in `backend/src/`, `frontend/src/`, or `frontend/src/i18n/*.json` — those refer to the existing curated `intelligence_signals` (plural) table and MUST NOT be renamed (Pitfall 4 / 54-RESEARCH.md L456-465).
    - Migration filename is `20260516000001_phase54_rename_phase45_intelligence_digest.sql` exactly — timestamp MUST start with `20260516000001` so plans 02/03 (`...000002`, `...000003`) sort strictly after.

  </action>
  <verify>
    <automated>test -f supabase/migrations/20260516000001_phase54_rename_phase45_intelligence_digest.sql &amp;&amp; grep -q "ALTER TABLE IF EXISTS public.intelligence_digest RENAME TO dashboard_digest" supabase/migrations/20260516000001_phase54_rename_phase45_intelligence_digest.sql &amp;&amp; ! grep -E "(DELETE FROM|INSERT INTO) intelligence_digest" supabase/seed/060-dashboard-demo.sql &amp;&amp; grep -q "dashboard_digest" supabase/seed/060-dashboard-demo.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/20260516000001_phase54_rename_phase45_intelligence_digest.sql` exists.
    - File contains the exact line `ALTER TABLE IF EXISTS public.intelligence_digest RENAME TO dashboard_digest;` (grep -F match).
    - File contains 4 `ALTER POLICY ... RENAME TO dashboard_digest_*` lines and 3 `ALTER INDEX ... RENAME TO idx_dashboard_digest_*` lines.
    - MCP `apply_migration` returned without error.
    - MCP `list_tables(schemas=["public"])` post-apply returns a list that INCLUDES `dashboard_digest` AND DOES NOT include `intelligence_digest`.
    - MCP `execute_sql` query `SELECT polname FROM pg_policy WHERE polrelid = 'public.dashboard_digest'::regclass ORDER BY polname` returns exactly 4 rows, each starting with `dashboard_digest_`.
    - `supabase/seed/060-dashboard-demo.sql` no longer contains the literal strings `DELETE FROM intelligence_digest` or `INSERT INTO intelligence_digest` (`! grep -E "(DELETE FROM|INSERT INTO) intelligence_digest" supabase/seed/060-dashboard-demo.sql` exits 0).
    - `supabase/seed/060-dashboard-demo.sql` contains the literal string `dashboard_digest` at least 3 times (`grep -c dashboard_digest supabase/seed/060-dashboard-demo.sql` returns a number ≥ 3).
    - `supabase/migrations/20260507210000_phase45_intelligence_digest_seed.sql` is unchanged in git diff (D-03 — no retro-edit).
  </acceptance_criteria>
  <done>Rename migration on disk + applied to staging; renamed table verified via MCP `list_tables`; policy names verified via MCP `execute_sql`; seed updated; Phase-45 historical migration untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rename hook + test + update Digest widget + widget test in lockstep</name>
  <files>frontend/src/hooks/useDashboardDigest.ts, frontend/src/hooks/useIntelligenceDigest.ts, frontend/src/hooks/__tests__/useDashboardDigest.test.ts, frontend/src/hooks/__tests__/useIntelligenceDigest.test.ts, frontend/src/pages/Dashboard/widgets/Digest.tsx, frontend/src/pages/Dashboard/widgets/__tests__/Digest.test.tsx</files>
  <read_first>
    - frontend/src/hooks/useIntelligenceDigest.ts (full file)
    - frontend/src/hooks/__tests__/useIntelligenceDigest.test.ts (full file)
    - frontend/src/pages/Dashboard/widgets/Digest.tsx (full file — confirm exact import line + call site)
    - frontend/src/pages/Dashboard/widgets/__tests__/Digest.test.tsx (full file — 11 touch-point sites per 54-PATTERNS.md L465-470)
    - .planning/phases/54-intelligence-engine-schema-groundwork/54-RESEARCH.md § "Frontend hook rename (plan 1)" L674-714 + § "Test rename touch-points (plan 1)" L716-740
    - .planning/phases/54-intelligence-engine-schema-groundwork/54-PATTERNS.md § "frontend/src/hooks/useDashboardDigest.ts" L377-427 + § "Digest.tsx" L448-457 + § "Digest.test.tsx" L461-469
  </read_first>
  <behavior>
    - Test 1: `useDashboardDigest()` calls `supabase.from('dashboard_digest')` (asserted via mock spy in renamed hook test — preserves the existing assertion shape, only the literal `'intelligence_digest'` → `'dashboard_digest'` and identifier names swap).
    - Test 2: `useDashboardDigest()` throws `Error('Failed to fetch dashboard digest: ...')` on Supabase error path (preserves existing error-path assertion with swapped substring).
    - Test 3: `Digest.test.tsx` continues passing after `vi.mock('@/hooks/useDashboardDigest', ...)` swap; `readFileSync('src/hooks/useDashboardDigest.ts', 'utf8')` finds the file and the existing whole-file assertion runs against the new path.
  </behavior>
  <action>
    (a) Create `frontend/src/hooks/useDashboardDigest.ts` by copying `useIntelligenceDigest.ts` verbatim and applying ONLY these symbol/literal swaps (full target source in 54-RESEARCH.md L674-714):
        - `IntelligenceDigestRow` → `DashboardDigestRow` (interface name + every usage in the file)
        - `INTELLIGENCE_DIGEST_SELECT` → `DASHBOARD_DIGEST_SELECT`
        - `useIntelligenceDigest` → `useDashboardDigest` (exported function name)
        - `['dashboard', 'intelligence-digest', limit]` → `['dashboard', 'dashboard-digest', limit]`
        - `.from('intelligence_digest')` → `.from('dashboard_digest')`
        - `'Failed to fetch intelligence digest:'` → `'Failed to fetch dashboard digest:'`
        Column list is unchanged (Phase-45 shape preserved). Do NOT add or remove fields.

    (b) Delete `frontend/src/hooks/useIntelligenceDigest.ts` (use `git mv` if convenient — net effect must be: new file exists, old file does not).

    (c) Create `frontend/src/hooks/__tests__/useDashboardDigest.test.ts` by copying `useIntelligenceDigest.test.ts` verbatim and applying these literal swaps (per 54-PATTERNS.md L435-442):
        - Import: `from '../useIntelligenceDigest'` → `from '../useDashboardDigest'`
        - `import { useIntelligenceDigest }` → `import { useDashboardDigest }`
        - `describe('useIntelligenceDigest', ...)` → `describe('useDashboardDigest', ...)`
        - Test name string `'queries intelligence_digest with publication source fields...'` → `'queries dashboard_digest with publication source fields...'`
        - `renderHook(() => useIntelligenceDigest(), ...)` → `renderHook(() => useDashboardDigest(), ...)` (both call sites: L69 + L88 of original)
        - `expect(fromMock).toHaveBeenCalledWith('intelligence_digest')` → `expect(fromMock).toHaveBeenCalledWith('dashboard_digest')`
        - Error-message regex `/^Failed to fetch intelligence digest: /` → `/^Failed to fetch dashboard digest: /`

    (d) Delete `frontend/src/hooks/__tests__/useIntelligenceDigest.test.ts`.

    (e) Edit `frontend/src/pages/Dashboard/widgets/Digest.tsx` — three sites (per 54-PATTERNS.md L452-455):
        - Import: `import { useIntelligenceDigest, type IntelligenceDigestRow } from '@/hooks/useIntelligenceDigest'` → `import { useDashboardDigest, type DashboardDigestRow } from '@/hooks/useDashboardDigest'`
        - Function signature: `function mapDigestToRow(row: IntelligenceDigestRow, ...)` → `(row: DashboardDigestRow, ...)`
        - Call: `useIntelligenceDigest()` → `useDashboardDigest()`
        All other lines are byte-identical (rendering, state, RTL utilities, GlobeSpinner overlay).

    (f) Edit `frontend/src/pages/Dashboard/widgets/__tests__/Digest.test.tsx` — apply each of the 11 string swaps per 54-PATTERNS.md L465-469:
        - L22 `vi.mock('@/hooks/useIntelligenceDigest', ...)` → `'@/hooks/useDashboardDigest'`
        - L32 `import { useIntelligenceDigest } from '@/hooks/useIntelligenceDigest'` → `useDashboardDigest from '@/hooks/useDashboardDigest'`
        - L61 `Partial<ReturnType<typeof useIntelligenceDigest>>` → `useDashboardDigest`
        - L72, L77, L90, L97, L105, L116, L144 `vi.mocked(useIntelligenceDigest)` → `vi.mocked(useDashboardDigest)` (7 separate sites)
        - L158 `readFileSync('src/hooks/useIntelligenceDigest.ts', 'utf8')` → `readFileSync('src/hooks/useDashboardDigest.ts', 'utf8')`
        Verify by grep that the only post-edit occurrences of `useIntelligenceDigest` in this file are zero. (Line numbers may have shifted — use content-based replacement.)

    Pitfall reminders:
    - Do NOT do a bulk find/replace across the repo — Pitfall 4 (54-RESEARCH.md L456-465). Touch only the 6 files listed in this task and the seed file from Task 2.
    - Do NOT change any column or field in `DashboardDigestRow` — the underlying table shape (renamed in Task 2) is identical to the Phase-45 shape; only the table NAME moved.

  </action>
  <verify>
    <automated>test -f frontend/src/hooks/useDashboardDigest.ts &amp;&amp; ! test -e frontend/src/hooks/useIntelligenceDigest.ts &amp;&amp; test -f frontend/src/hooks/__tests__/useDashboardDigest.test.ts &amp;&amp; ! test -e frontend/src/hooks/__tests__/useIntelligenceDigest.test.ts &amp;&amp; grep -q "useDashboardDigest" frontend/src/pages/Dashboard/widgets/Digest.tsx &amp;&amp; ! grep -q "useIntelligenceDigest" frontend/src/pages/Dashboard/widgets/Digest.tsx &amp;&amp; ! grep -q "useIntelligenceDigest" frontend/src/pages/Dashboard/widgets/__tests__/Digest.test.tsx &amp;&amp; pnpm --filter frontend test src/pages/Dashboard/widgets/__tests__/Digest.test.tsx --run &amp;&amp; pnpm --filter frontend test src/hooks/__tests__/useDashboardDigest.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/src/hooks/useDashboardDigest.ts` exits 0.
    - `test -e frontend/src/hooks/useIntelligenceDigest.ts` exits NON-zero (file removed).
    - `test -f frontend/src/hooks/__tests__/useDashboardDigest.test.ts` exits 0.
    - `test -e frontend/src/hooks/__tests__/useIntelligenceDigest.test.ts` exits NON-zero (test file removed).
    - `grep -c "useDashboardDigest" frontend/src/pages/Dashboard/widgets/Digest.tsx` returns ≥ 3 (import + type + call).
    - `grep -c "useIntelligenceDigest" frontend/src/pages/Dashboard/widgets/Digest.tsx` returns 0.
    - `grep -c "useDashboardDigest" frontend/src/pages/Dashboard/widgets/__tests__/Digest.test.tsx` returns ≥ 10 (mock + import + 7 mocked() + Partial + readFileSync).
    - `grep -c "useIntelligenceDigest" frontend/src/pages/Dashboard/widgets/__tests__/Digest.test.tsx` returns 0.
    - `grep -F "from('dashboard_digest')" frontend/src/hooks/useDashboardDigest.ts` returns 1 line.
    - `grep -F "from('intelligence_digest')" frontend/src/hooks/useDashboardDigest.ts` returns 0 lines.
    - `pnpm --filter frontend test src/pages/Dashboard/widgets/__tests__/Digest.test.tsx --run` exits 0.
    - `pnpm --filter frontend test src/hooks/__tests__/useDashboardDigest.test.ts --run` exits 0.
    - Repo-wide grep returns ZERO matches in app source for `intelligence_signal` having been renamed to `intelligence_event` (Pitfall 4 — confirm `git diff -- backend/src frontend/src/i18n frontend/src/components/entity-links frontend/src/types/preview-layout.types.ts frontend/src/types/multilingual-content.types.ts backend/src/api/entity-search.ts backend/src/services/entity-search.service.ts backend/src/services/link.service.ts` shows no changes).
  </acceptance_criteria>
  <done>Old hook + test files removed; renamed hook + test exist with the literal/identifier swaps applied; widget + widget test consume the renamed hook; both Vitest specs pass; no `intelligence_signal` rename leaked into app code.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                                      |
| ---------------------------- | ------------------------------------------------------------------------------------------------ |
| operator → SSH signing key   | Local-only signing material; only the operator can sign the phase-54-base tag                    |
| Claude → Supabase MCP        | All schema mutation flows via MCP `apply_migration` with project_id `zkrcjzdemdmwhearhfgg`       |
| Supabase RLS → renamed table | Policies follow the table on `ALTER TABLE ... RENAME` (Postgres semantics) — no boundary crossed |

## STRIDE Threat Register

| Threat ID | Category               | Component                             | Disposition | Mitigation Plan                                                                                                                                                                                                                                            |
| --------- | ---------------------- | ------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-54-01-T | Tampering              | phase-54-base tag                     | mitigate    | SSH-signed annotated tag per CLAUDE.md tag-signing appendix; `git tag -v` verifies the signature against `~/.ssh/allowed_signers`                                                                                                                          |
| T-54-01-R | Repudiation            | rename migration application          | mitigate    | Migration file committed to git with timestamp prefix `20260516000001`; MCP `apply_migration` recorded in Supabase migration history under that name                                                                                                       |
| T-54-01-S | Spoofing               | hook rename collateral damage         | mitigate    | Pitfall 4 — explicitly forbid bulk `intelligence_signal` find/replace; restrict edits to the 6 files in Task 3 + the seed file in Task 2; verified by repo-wide `git diff` check in acceptance criteria                                                    |
| T-54-01-D | DoS                    | rename causes downstream read failure | accept      | Hook rename ships in the same wave as the table rename (Pitfall 3); no gap window where staging `intelligence_digest` is gone but the hook still calls it                                                                                                  |
| T-54-01-I | Information disclosure | dashboard_digest data exposure        | mitigate    | RLS policies follow the table via `ALTER TABLE ... RENAME` (Postgres `pg_policy.polrelid` reference); 4 renamed policies (`dashboard_digest_select_org`, `*_insert_editor`, `*_update_editor`, `*_delete_admin`) verified via MCP `execute_sql` post-apply |

</threat_model>

<verification>
After all tasks complete in this plan:

1. `git tag -v phase-54-base` exits 0 with `Good "git" signature`.
2. `git ls-remote --tags origin phase-54-base` lists the tag SHA.
3. MCP `list_tables(schemas=["public"])` includes `dashboard_digest`, excludes `intelligence_digest`.
4. MCP `execute_sql "SELECT polname FROM pg_policy WHERE polrelid = 'public.dashboard_digest'::regclass"` returns 4 rows, all named `dashboard_digest_*`.
5. `pnpm --filter frontend test src/hooks/__tests__/useDashboardDigest.test.ts --run` exits 0.
6. `pnpm --filter frontend test src/pages/Dashboard/widgets/__tests__/Digest.test.tsx --run` exits 0.
7. `! git diff --name-only HEAD -- backend/src frontend/src/i18n frontend/src/types/preview-layout.types.ts frontend/src/types/multilingual-content.types.ts backend/src/services/entity-search.service.ts backend/src/services/link.service.ts backend/src/api/entity-search.ts frontend/src/components/entity-links/` → exits 0 (no Pitfall-4 collateral).
   </verification>

<success_criteria>

- `phase-54-base` signed annotated tag pushed to origin and verifiable via `git tag -v`.
- `intelligence_digest` table renamed to `dashboard_digest` on staging; indexes and policies renamed in lockstep; Phase-45 historical migration untouched.
- Frontend hook + test renamed; widget + widget test updated; seed updated.
- Both Vitest specs green; no Pitfall-4 collateral damage to `intelligence_signals` (plural) references in app code.
  </success_criteria>

<output>
Create `.planning/phases/54-intelligence-engine-schema-groundwork/54-01-SUMMARY.md` when done.
</output>
