---
phase: 54
plan: 03
type: execute
wave: 3
depends_on: ['54-02']
files_modified:
  - supabase/migrations/20260516000003_phase54_intelligence_event_dossiers.sql
  - tests/integration/intelligence-event-dossiers-rls.test.ts
autonomous: true
requirements: [INTEL-03]
must_haves:
  truths:
    - 'D-05: `intelligence_event_dossiers` junction table (NOT `intelligence_signal_dossiers`) exists on staging with surrogate UUID PK and `event_id` + `dossier_id` FKs both `ON DELETE CASCADE`.'
    - 'D-06: `dossier_type TEXT` CHECK constraint enforces exactly the 7 canonical values matching `dossiers_type_check` (`country, organization, forum, engagement, topic, working_group, person`); `elected_official` is rejected.'
    - 'A UNIQUE INDEX on `(event_id, dossier_type, dossier_id)` prevents duplicate links.'
    - 'D-13: RLS is enabled and derives tenancy via EXISTS-on-parent — a user from org A sees only the junction rows whose `event_id` resolves to an org-A event; junction has NO `organization_id` column of its own.'
    - "Inserting `dossier_type='elected_official'` fails (CHECK rejects); deleting the parent `intelligence_event` cascades to remove the junction row."
    - 'Wave-0 integration test `tests/integration/intelligence-event-dossiers-rls.test.ts` exists and passes.'
    - 'D-15: Migration applied to staging (`zkrcjzdemdmwhearhfgg`) via Supabase MCP `apply_migration` — NOT local CLI.'
  artifacts:
    - path: 'supabase/migrations/20260516000003_phase54_intelligence_event_dossiers.sql'
      provides: 'CREATE TABLE intelligence_event_dossiers + indexes + EXISTS-via-parent RLS'
      contains: 'CREATE TABLE IF NOT EXISTS public.intelligence_event_dossiers'
    - path: 'tests/integration/intelligence-event-dossiers-rls.test.ts'
      provides: 'INTEL-03 junction CHECK + tenancy + CASCADE coverage'
      contains: 'intelligence_event_dossiers'
  key_links:
    - from: 'intelligence_event_dossiers.event_id'
      to: 'intelligence_event.id'
      via: 'FK ON DELETE CASCADE'
      pattern: "REFERENCES public.intelligence_event\\(id\\) ON DELETE CASCADE"
    - from: 'intelligence_event_dossiers.dossier_id'
      to: 'dossiers.id'
      via: 'FK ON DELETE CASCADE'
      pattern: "REFERENCES public.dossiers\\(id\\) ON DELETE CASCADE"
    - from: 'intelligence_event_dossiers RLS policies'
      to: 'intelligence_event.organization_id (via EXISTS)'
      via: 'EXISTS subquery on parent'
      pattern: "EXISTS \\(\\s*SELECT 1 FROM public.intelligence_event ie"
---

<objective>
Create the polymorphic junction `intelligence_event_dossiers` that links `intelligence_event` rows to any of the 7 canonical dossier types. Use a surrogate UUID PK with a UNIQUE index on the link triple (matches `work_item_dossiers` precedent), both FKs `ON DELETE CASCADE`, and EXISTS-via-parent RLS so tenancy is single-source-of-truth on `intelligence_event.organization_id` (D-13, no denormalized column on the junction).

Purpose: Lands INTEL-03 — the schema body for many-to-many signal ↔ dossier linking with strict tenancy via the parent event row.

Output:

- Migration `supabase/migrations/20260516000003_phase54_intelligence_event_dossiers.sql` written + applied to staging via Supabase MCP.
- Integration test `tests/integration/intelligence-event-dossiers-rls.test.ts` created and passing.
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/54-intelligence-engine-schema-groundwork/54-CONTEXT.md
@.planning/phases/54-intelligence-engine-schema-groundwork/54-RESEARCH.md
@.planning/phases/54-intelligence-engine-schema-groundwork/54-PATTERNS.md
@.planning/phases/54-intelligence-engine-schema-groundwork/54-VALIDATION.md

# Precedent junction (table shape only — RLS pattern differs)

@supabase/migrations/20260116500001_create_work_item_dossiers.sql

# Helpers — read signatures only

@supabase/migrations/20260113500001_tenant_isolation_layer.sql

# Test template — copy structure for the junction integration test

@tests/integration/polymorphic-document-rls.test.ts

# Plan 54-02 output — junction FKs intelligence_event, which exists after plan 54-02

@.planning/phases/54-intelligence-engine-schema-groundwork/54-02-PLAN.md

<interfaces>
<!-- Junction EXISTS-via-parent RLS pattern — verbatim source: 54-RESEARCH.md § Code Examples L587-670 -->
<!-- New for this codebase (D-13, Pattern 2). NOT in work_item_dossiers (that uses clearance/ownership). -->

```sql
ALTER TABLE public.intelligence_event_dossiers ENABLE ROW LEVEL SECURITY;

DROP POLICY IF EXISTS intelligence_event_dossiers_select ON public.intelligence_event_dossiers;
CREATE POLICY intelligence_event_dossiers_select
  ON public.intelligence_event_dossiers FOR SELECT
  TO authenticated
  USING (
    EXISTS (
      SELECT 1 FROM public.intelligence_event ie
      WHERE ie.id = intelligence_event_dossiers.event_id
        AND tenant_isolation.rls_select_policy(ie.organization_id)
    )
  );

-- INSERT: EXISTS + rls_insert_policy + editor role on parent
-- UPDATE: USING + WITH CHECK both EXISTS + rls_update_policy + editor role on parent
-- DELETE: EXISTS + rls_delete_policy on parent (helper-only — admin baked in)
```

Reference the four `tenant_isolation.rls_*_policy(organization_id)` helpers — same signatures as in plan 54-02, applied via the parent `ie.organization_id`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write + apply intelligence_event_dossiers junction migration via Supabase MCP</name>
  <files>supabase/migrations/20260516000003_phase54_intelligence_event_dossiers.sql</files>
  <read_first>
    - supabase/migrations/20260116500001_create_work_item_dossiers.sql L11-57 (table shape + UNIQUE index pattern; RLS pattern differs — DO NOT copy lines 117-193)
    - 54-RESEARCH.md § "Code Examples — Junction migration (plan 3)" L585-670 (full target SQL — copy and complete)
    - 54-PATTERNS.md § "supabase/migrations/20260516000003_phase54_intelligence_event_dossiers.sql" L200-296 (verbatim junction skeleton + RLS block)
    - 54-CONTEXT.md D-05, D-06, D-13
    - 54-RESEARCH.md § "Pattern 2: Polymorphic junction with EXISTS-based RLS (D-13)" L299-326
    - 54-RESEARCH.md Pitfall 6 (helper-only DELETE — applies here too via parent helper)
  </read_first>
  <action>
    Create `supabase/migrations/20260516000003_phase54_intelligence_event_dossiers.sql` containing, in this exact order:

    (1) Header comment block:
        - Phase 54 / plan 03
        - Source patterns: 20260116500001_create_work_item_dossiers.sql L11-57 (shape); RLS pattern invented per D-13 + 54-RESEARCH.md Pattern 2
        - Requirements: INTEL-03
        - Decisions: D-05 (junction name), D-06 (7-value CHECK), D-13 (EXISTS-via-parent RLS, no own organization_id)

    (2) Table — copy verbatim from 54-PATTERNS.md L208-215. Columns in order: `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`, `event_id UUID NOT NULL REFERENCES public.intelligence_event(id) ON DELETE CASCADE`, `dossier_type TEXT NOT NULL CHECK (dossier_type IN ('country','organization','forum','engagement','topic','working_group','person'))`, `dossier_id UUID NOT NULL REFERENCES public.dossiers(id) ON DELETE CASCADE`, `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`. Wrap in `CREATE TABLE IF NOT EXISTS public.intelligence_event_dossiers (...);`. The 7-value CHECK literal MUST match byte-for-byte the canonical literal in `20260202000001_merge_elected_official_into_person.sql` L194-204 and the same literal in plan 54-02's new `intelligence_digest.dossier_type` CHECK. No `elected_official`.

    (3) Two indexes (54-PATTERNS.md L223-226):
        - UNIQUE INDEX `idx_intelligence_event_dossiers_unique` ON `(event_id, dossier_type, dossier_id)` — prevents duplicate links. NO `WHERE deleted_at IS NULL` partial (junction has no soft-delete column).
        - Secondary INDEX `idx_intelligence_event_dossiers_dossier` ON `(dossier_type, dossier_id)` — reverse lookup for "all events linked to this dossier".
        Both use `CREATE [UNIQUE] INDEX IF NOT EXISTS`.

    (4) `ALTER TABLE public.intelligence_event_dossiers ENABLE ROW LEVEL SECURITY;`

    (5) Four EXISTS-via-parent RLS policies — verbatim from 54-RESEARCH.md L606-666 / 54-PATTERNS.md L232-294:
        - `intelligence_event_dossiers_select` FOR SELECT USING (EXISTS (SELECT 1 FROM public.intelligence_event ie WHERE ie.id = intelligence_event_dossiers.event_id AND tenant_isolation.rls_select_policy(ie.organization_id)))
        - `intelligence_event_dossiers_insert` FOR INSERT WITH CHECK (EXISTS (SELECT 1 FROM public.intelligence_event ie WHERE ie.id = intelligence_event_dossiers.event_id AND tenant_isolation.rls_insert_policy(ie.organization_id) AND public.auth_has_any_role(ARRAY['admin','editor'])))
        - `intelligence_event_dossiers_update` FOR UPDATE — both USING and WITH CHECK mirror the INSERT-style EXISTS body with `rls_update_policy` and the editor-role gate
        - `intelligence_event_dossiers_delete` FOR DELETE USING (EXISTS (SELECT 1 FROM public.intelligence_event ie WHERE ie.id = intelligence_event_dossiers.event_id AND tenant_isolation.rls_delete_policy(ie.organization_id))) — helper-only on DELETE (admin baked into helper)
        Each policy preceded by `DROP POLICY IF EXISTS ...`.

    (6) GRANTs: `GRANT SELECT ON public.intelligence_event_dossiers TO authenticated;` and `GRANT INSERT, UPDATE, DELETE ON public.intelligence_event_dossiers TO authenticated;`

    (7) `COMMENT ON TABLE public.intelligence_event_dossiers IS 'Phase 54: polymorphic many-to-many junction linking intelligence_event rows to any of the 7 canonical dossier types. Tenancy derived from parent intelligence_event.organization_id via EXISTS in RLS (D-13) — junction intentionally has NO organization_id column of its own. ON DELETE CASCADE on both FKs.';`

    (8) Apply to staging via MCP (D-15):
        `mcp__claude_ai_Supabase__apply_migration(project_id="zkrcjzdemdmwhearhfgg", name="20260516000003_phase54_intelligence_event_dossiers", query=<contents of the .sql file>)`

    (9) Post-apply MCP verification:
        - `mcp__claude_ai_Supabase__list_tables(schemas=["public"])` → includes `intelligence_event_dossiers`.
        - `mcp__claude_ai_Supabase__execute_sql(query="SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='intelligence_event_dossiers' ORDER BY ordinal_position")` → returns exactly `id, event_id, dossier_type, dossier_id, created_at` (5 columns; NO `organization_id` — D-13).
        - `mcp__claude_ai_Supabase__execute_sql(query="SELECT polname FROM pg_policy WHERE polrelid = 'public.intelligence_event_dossiers'::regclass ORDER BY polname")` → returns 4 rows.
        - `mcp__claude_ai_Supabase__execute_sql(query="SELECT relrowsecurity FROM pg_class WHERE oid = 'public.intelligence_event_dossiers'::regclass")` → returns `t`.
        - `mcp__claude_ai_Supabase__execute_sql(query="SELECT conname FROM pg_constraint WHERE conrelid = 'public.intelligence_event_dossiers'::regclass AND contype = 'c'")` → at least one CHECK constraint (the `dossier_type` one).
        - `mcp__claude_ai_Supabase__execute_sql(query="SELECT confrelid::regclass::text AS ref_table, confdeltype FROM pg_constraint WHERE conrelid = 'public.intelligence_event_dossiers'::regclass AND contype = 'f' ORDER BY ref_table")` → returns 2 rows: `dossiers` and `intelligence_event`, both with `confdeltype = 'c'` (CASCADE).
        - `mcp__claude_ai_Supabase__get_advisors(project_id="zkrcjzdemdmwhearhfgg", type="security")` → must not surface any new SECURITY/RLS critical findings on this junction.

    Pitfall reminders:
    - DELETE policy is helper-only — Pitfall 6 (54-RESEARCH.md L474-488). Inside the EXISTS subquery, `tenant_isolation.rls_delete_policy(ie.organization_id)` is the only gate. No extra `AND public.auth_has_any_role(...)`.
    - No `organization_id` column on the junction (D-13). Tenancy is parent-derived.
    - 7-value CHECK literal MUST be exact: `('country','organization','forum','engagement','topic','working_group','person')` — no `elected_official`.
    - Idempotent guards on every DDL statement (CREATE TABLE IF NOT EXISTS, CREATE INDEX IF NOT EXISTS, DROP POLICY IF EXISTS).

  </action>
  <verify>
    <automated>test -f supabase/migrations/20260516000003_phase54_intelligence_event_dossiers.sql &amp;&amp; grep -q "CREATE TABLE IF NOT EXISTS public.intelligence_event_dossiers" supabase/migrations/20260516000003_phase54_intelligence_event_dossiers.sql &amp;&amp; grep -q "REFERENCES public.intelligence_event(id) ON DELETE CASCADE" supabase/migrations/20260516000003_phase54_intelligence_event_dossiers.sql &amp;&amp; grep -q "REFERENCES public.dossiers(id) ON DELETE CASCADE" supabase/migrations/20260516000003_phase54_intelligence_event_dossiers.sql &amp;&amp; grep -q "EXISTS (" supabase/migrations/20260516000003_phase54_intelligence_event_dossiers.sql &amp;&amp; grep -q "idx_intelligence_event_dossiers_unique" supabase/migrations/20260516000003_phase54_intelligence_event_dossiers.sql &amp;&amp; ! grep -q "elected_official" supabase/migrations/20260516000003_phase54_intelligence_event_dossiers.sql &amp;&amp; ! grep -E "organization_id\s+UUID" supabase/migrations/20260516000003_phase54_intelligence_event_dossiers.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/20260516000003_phase54_intelligence_event_dossiers.sql` exists.
    - File contains literal `CREATE TABLE IF NOT EXISTS public.intelligence_event_dossiers`.
    - File contains literal `event_id UUID NOT NULL REFERENCES public.intelligence_event(id) ON DELETE CASCADE`.
    - File contains literal `dossier_id UUID NOT NULL REFERENCES public.dossiers(id) ON DELETE CASCADE`.
    - File contains the literal 7-value CHECK `('country','organization','forum','engagement','topic','working_group','person')` (no `elected_official`).
    - File contains literal `idx_intelligence_event_dossiers_unique` UNIQUE INDEX on `(event_id, dossier_type, dossier_id)`.
    - File contains 4 `CREATE POLICY` lines, each preceded by `DROP POLICY IF EXISTS`.
    - File contains literal `EXISTS (` at least 4 times (one per policy).
    - File DELETE policy uses helper-only EXISTS body (probe: lines between `intelligence_event_dossiers_delete` and the next `GRANT` / next `DROP POLICY` MUST NOT contain `auth_has_any_role` — Pitfall 6).
    - File DOES NOT define a column `organization_id` on this junction (`! grep -E "organization_id\s+UUID" supabase/migrations/20260516000003_phase54_intelligence_event_dossiers.sql` exits 0).
    - MCP `apply_migration` succeeded.
    - MCP `list_tables(schemas=["public"])` includes `intelligence_event_dossiers`.
    - MCP `information_schema.columns` probe returns exactly 5 column names: `id`, `event_id`, `dossier_type`, `dossier_id`, `created_at` (NO `organization_id`).
    - MCP `pg_policy` probe returns exactly 4 policy rows.
    - MCP `pg_class.relrowsecurity` returns `t`.
    - MCP `pg_constraint` FK probe returns 2 rows: `dossiers` + `intelligence_event`, both `confdeltype = 'c'` (CASCADE).
    - MCP `get_advisors(type="security")` returns no new critical findings on this junction.
  </acceptance_criteria>
  <done>Junction migration on disk + applied via MCP; 6 MCP probes verify column set (no organization_id), CHECK, RLS enabled, 4 policies, CASCADE on both FKs, no security advisors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Wave-0 RLS integration test for intelligence_event_dossiers junction</name>
  <files>tests/integration/intelligence-event-dossiers-rls.test.ts</files>
  <read_first>
    - tests/integration/polymorphic-document-rls.test.ts (full file — template)
    - tests/integration/intelligence-event-rls.test.ts (created in plan 54-02 Task 2 — reuse the same `beforeAll` org/dossier-discovery scaffold)
    - 54-PATTERNS.md § "tests/integration/intelligence-event-dossiers-rls.test.ts" L363-373
    - 54-VALIDATION.md § "Wave 0 Requirements"
    - 54-RESEARCH.md § "Phase Requirements → Test Map" L779-781 (3 rows for INTEL-03)
  </read_first>
  <behavior>
    - Test 1 (INTEL-03 CHECK rejects elected_official): signed in as user from org A, insert an `intelligence_event` row owned by org A, then `.from('intelligence_event_dossiers').insert({event_id: <eventId>, dossier_type: 'elected_official', dossier_id: <any-valid-uuid>})` returns non-null `error` whose message references the CHECK constraint on `dossier_type`.
    - Test 2 (INTEL-03 CHECK accepts the 7 valid types — at least one): same setup, `.insert({event_id, dossier_type: 'country', dossier_id: <existing-country-dossier>})` succeeds (`error` is null).
    - Test 3 (INTEL-03 tenancy via EXISTS-on-parent): signed in as user from org A inserts event(org=A) + junction row; signed-in as same user, `.from('intelligence_event_dossiers').select('*').eq('event_id', eventId)` returns ≥1 row. Cross-tenant: a separate test fixture with an event belonging to org B should not be visible — model this by `.from('intelligence_event_dossiers').select('*').neq('event_id', <orgA event ids>)` returning 0 rows when no other org's events exist for this signed-in user (the EXISTS subquery filters them out).
    - Test 4 (INTEL-03 CASCADE on event delete): insert event + junction row → record both ids → `.from('intelligence_event').delete().eq('id', eventId)` → `.from('intelligence_event_dossiers').select('id').eq('id', junctionId)` returns 0 rows (CASCADE worked).
  </behavior>
  <action>
    (a) Create `tests/integration/intelligence-event-dossiers-rls.test.ts` modeled on `tests/integration/intelligence-event-rls.test.ts` (built in plan 54-02 Task 2) and `tests/integration/polymorphic-document-rls.test.ts`. The `beforeAll` discovery block (resolving `orgIdA`, `dossierIdA`, optional `orgIdB`) is duplicated from plan 54-02 — that's acceptable per test-isolation norms.

    (b) Implement the 4 `it(...)` blocks above:
        - Test 1 (CHECK rejects `elected_official`): use any UUID for `dossier_id` (the CHECK fires before the FK lookup; if the FK fires first, the test still passes since `elected_official` is also not a valid dossier type by FK lookup — both paths give a non-null error).
        - Test 2 (valid type insert succeeds): use a `dossier_type` and `dossier_id` resolved dynamically from `dossiers` table (`.from('dossiers').select('id, type').eq('organization_id', orgIdA).limit(1).single()` then use those values).
        - Test 3 (tenancy via EXISTS): assert `data.length` ≥ 1 for own-org junction rows, and `data.filter(row => /* not in our inserted ids */).length === 0` for the cross-tenant negative.
        - Test 4 (CASCADE): insert event + insert junction, capture both ids, delete the event, then SELECT the junction by its id — expect 0 rows.

    (c) `afterAll` cleanup: delete remaining events (`.from('intelligence_event').delete().in('id', eventIds)`) — `ON DELETE CASCADE` on `event_id` clears any orphaned junction rows automatically (no separate junction cleanup needed).

    (d) Run the test:
        `pnpm --filter backend test:integration tests/integration/intelligence-event-dossiers-rls.test.ts --run` (or `pnpm vitest run tests/integration/intelligence-event-dossiers-rls.test.ts` if the backend filter doesn't pick up this path — match whichever runner picks up the file from plan 54-02).

    Pitfall reminders:
    - The CHECK on `dossier_type` is independent of the FK on `dossier_id`. The CHECK fires regardless of whether the `dossier_id` value matches a real dossier.
    - Do NOT use service-role keys — use anon + signed-in user so RLS is exercised.
    - For Test 3, if staging has no second organization, the cross-tenant negative test should `it.skipIf(!orgIdB)` with a logged warning. The other 3 tests MUST pass.

  </action>
  <verify>
    <automated>test -f tests/integration/intelligence-event-dossiers-rls.test.ts &amp;&amp; grep -q "intelligence_event_dossiers" tests/integration/intelligence-event-dossiers-rls.test.ts &amp;&amp; grep -q "elected_official" tests/integration/intelligence-event-dossiers-rls.test.ts &amp;&amp; grep -q "CASCADE\|delete().eq.*event" tests/integration/intelligence-event-dossiers-rls.test.ts &amp;&amp; ( pnpm --filter backend test:integration tests/integration/intelligence-event-dossiers-rls.test.ts --run || pnpm vitest run tests/integration/intelligence-event-dossiers-rls.test.ts )</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/integration/intelligence-event-dossiers-rls.test.ts` exists.
    - File contains literal `from('intelligence_event_dossiers')` at least 3 times (`grep -c`).
    - File contains literal `'elected_official'` (Test 1 — CHECK negative case).
    - File contains an INSERT into `intelligence_event_dossiers` with at least one of the 7 valid dossier types (Test 2 — CHECK positive case).
    - File contains a DELETE on `intelligence_event` followed by a SELECT-by-id on `intelligence_event_dossiers` (Test 4 — CASCADE).
    - File contains at least 3 `expect(...).toBeNull()` / `expect(...).not.toBeNull()` / `expect(...length).toBe(0)` assertions covering the 4 behaviors.
    - Vitest invocation exits 0 with ≥3 passes, ≤1 skip (cross-tenant test).
  </acceptance_criteria>
  <done>Junction integration test file exists with 4 `it(...)` blocks covering INTEL-03 CHECK, tenancy via EXISTS-on-parent, and CASCADE; vitest run exits 0 with ≥3 passes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                                                                                              |
| ---------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| anon JWT (signed-in user) → junction RLS | RLS filters reads/writes via `EXISTS (SELECT 1 FROM intelligence_event ie WHERE ie.id = event_id AND tenant_isolation.rls_*_policy(ie.organization_id))` |
| junction → parent event                  | Single source of truth for tenancy; no denormalization (D-13)                                                                                            |
| Parent event CASCADE → junction rows     | `ON DELETE CASCADE` on `event_id` removes orphaned link rows automatically                                                                               |

## STRIDE Threat Register

| Threat ID | Category               | Component                                       | Disposition                  | Mitigation Plan                                                                                                                                                                            |
| --------- | ---------------------- | ----------------------------------------------- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-54-03-I | Information disclosure | cross-tenant SELECT on junction                 | mitigate                     | EXISTS-via-parent policy filters by `tenant_isolation.rls_select_policy(ie.organization_id)`; Test 3 of Task 2 proves visibility scoping                                                   |
| T-54-03-T | Tampering              | invalid dossier_type insert                     | mitigate                     | `CHECK (dossier_type IN (...))` rejects any of `elected_official`, free-form text, or future-added dossier types until that type is explicitly approved; Test 1 of Task 2 proves rejection |
| T-54-03-D | DoS                    | orphaned junction rows after parent delete      | accept (mitigate via design) | `ON DELETE CASCADE` on `event_id` removes them automatically; Test 4 of Task 2 proves cascade behavior                                                                                     |
| T-54-03-D | DoS                    | unindexed reverse lookup `where dossier_id = ?` | mitigate                     | Secondary index `idx_intelligence_event_dossiers_dossier ON (dossier_type, dossier_id)` covers the "all events for this dossier" path                                                      |
| T-54-03-E | Elevation of privilege | non-admin DELETE on junction                    | mitigate                     | DELETE EXISTS body uses `tenant_isolation.rls_delete_policy(ie.organization_id)` which gates on admin role internally (Pitfall 6)                                                          |
| T-54-03-R | Repudiation            | who created the link                            | accept                       | `created_at TIMESTAMPTZ` captures time; actor attribution is deferred to v7.0 since junction has no `created_by` column (deferred per CONTEXT § Deferred — junction stays minimal)         |

</threat_model>

<verification>
After all tasks complete in this plan:

1. MCP `list_tables(schemas=["public"])` includes `intelligence_event_dossiers`.
2. MCP `information_schema.columns` probe returns exactly 5 columns (no `organization_id`).
3. MCP `pg_policy` returns 4 policy rows for the junction.
4. MCP `pg_class.relrowsecurity` returns `t`.
5. MCP `pg_constraint` FK probe shows both FKs as `CASCADE` (`confdeltype = 'c'`).
6. MCP `get_advisors(type="security")` returns no new critical findings.
7. Vitest run of `intelligence-event-dossiers-rls.test.ts` exits 0 with ≥3 passes.
   </verification>

<success_criteria>

- Junction table exists on staging with 5 columns (no `organization_id`), surrogate UUID PK, both FKs CASCADE, 7-value `dossier_type` CHECK.
- 4 EXISTS-via-parent RLS policies enabled.
- CASCADE on event delete proven by integration test.
- `elected_official` insert rejected by CHECK; valid type insert succeeds.
- Wave-0 integration test exists and passes.
  </success_criteria>

<output>
Create `.planning/phases/54-intelligence-engine-schema-groundwork/54-03-SUMMARY.md` when done.
</output>
