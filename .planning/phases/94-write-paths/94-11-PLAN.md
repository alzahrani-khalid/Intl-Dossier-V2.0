---
phase: 94-write-paths
plan: 11
type: execute
wave: 5
depends_on: [94-01, 94-02, 94-03, 94-04, 94-05, 94-06, 94-07, 94-08, 94-09, 94-10]
files_modified:
  - .planning/phases/94-write-paths/94-VERIFICATION.md
autonomous: true
requirements:
  [
    WRITE-01,
    WRITE-02,
    WRITE-03,
    WRITE-04,
    WRITE-05,
    WRITE-06,
    AUDIT-DROP-01,
    AUDIT-ZERO-01,
    ARMA-01,
  ]
truths:
  - "Decisions covered — D-01: all NINE requirements are dispositioned in the verification doc (closed with evidence, or OPEN with a number — never silently absorbed); D-16: the settings population statement is restated as a closing derivation; D-23: GATE-STANDARD C1-C10 incl. C9a/C9b governs the whole-set gate drill — one author, all plans, one pass; a gate whose done state was not constructed is CANNOT CONSTRUCT, never SOUND; D-24: every closing derivation states its POPULATION DEFINITION and what falls outside it; D-25: the full behavioural oracle set (specs + probes) is re-run as the phase's evidence; D-26: every Playwright command asserts spec-file existence FIRST and hardcodes its expected count; D-27: every root-config spec runs --no-deps with inline auth; D-28: the C9b consumer table carries the mock-vs-real column and NON-ORACLES are never folded into the defence count; D-29: no two writers share an output path — this plan writes 94-VERIFICATION.md and MUST NOT claim 94-VERIFICATION-INDEPENDENT.md (the independent gsd-verifier's artifact); D-31: the WRITE-04 reverse-mapping population is restated with what falls outside it"
must_haves:
  truths:
    - 'Every closing derivation in 94-VERIFICATION.md carries: search root, glob/matching rule, and the cases the method CANNOT see'
    - 'The full Phase-94 oracle set runs green with hardcoded counts: settings-save 2, after-action-detail-error 1 (frontend config), 93-report-notfound 1 (--no-deps), and the four probes (report-rls, commitment-readback, after-actions-list, audit-row) exit 0 — re-runs SPACED against the auth rate-limit wall (ORACLECAP-01: back-to-back runs red at the provider limit and that is UNABLE TO MEASURE, not a regression)'
    - "The intended-broken register names every deliberately-open item with its owning phase: overdue rendering + two-signals unification + INSERT gap → Phase 96 (COUNT-04); the mock generate path → Phase 95 (DEAD-09); the global success-toast literal → Phase 98 (COPY-06); the conflict.* inline-English keys → Phase 99; Population-B settings surfaces excluded on assumption A4 (carried, stated); RTL renders verified as key-sets and string inequality only — pixel RTL is the inherited operator park (RULING-P93-06 order 2); commitment_status_history records the COERCED status and is not evidence of user intent (F3); the RLS WITH CHECK silent-zero-row class is the trigger sweep's stated blind spot, not swept"
    - "94-VERIFICATION-INDEPENDENT.md is NOT created by this plan — it is the independent verifier's path (D-29)"
  artifacts:
    - path: '.planning/phases/94-write-paths/94-VERIFICATION.md'
      provides: 'closing derivations with populations, the gate-drill table (C1 both directions), spec+probe outputs, the C9b cross-phase consumer table with mock-vs-real verdicts, and the intended-broken register'
      contains: 'POPULATION'
  key_links:
    - from: '.planning/phases/94-write-paths/94-VERIFICATION.md'
      to: 'scripts/gate-drill.mjs'
      via: 'the mechanical half of the drill; done-state constructions stay authored per gate'
      pattern: 'gate-drill'
---

<objective>
Close the phase the way 93-15 did: re-run every closing derivation with its population stated
(D-24), run the full behavioural oracle set (D-25), drill every gate in the plan set both
directions (D-23/C1), and write the intended-broken register so no later reader infers coverage
that was never claimed.

Output: 94-VERIFICATION.md — derivations, drill table, spec/probe evidence, C9b table, register.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/GATE-STANDARD.md
@.planning/phases/94-write-paths/94-VALIDATION.md  (the instrument constraints — every one has produced a wrong result before)
@.planning/phases/94-write-paths/94-RESEARCH.md  (§8 the C9b triage table to carry; §Validation Architecture the requirement→test map being discharged)

<interfaces>
The closing derivations to run, each recorded with POPULATION + OUTSIDE-statement:
1. WRITE-04 reverse mapping (D-31, RULING-P94-02-approved): where each of the five live statuses
   renders — pending→Todo, in_progress→In-progress, completed→Done, cancelled→not rendered
   (filtered pre-bucketing, deliberate), overdue→Todo via default (stated; handling = Phase 96).
   Re-derive the constraint live (D-03) one final time and cite 94-STAGE-PARITY.md. Outside:
   task and intake reverse mappings.
2. Settings population (D-16): Population A in-criterion (spec evidence), Population B named
   exclusion with A4 reasoning, callback/security/data-privacy outside.
3. AUDIT-ZERO key-diff re-run: BOTH quote styles over supabase/functions (the two-quote-style
   population rule now in the register) → expected 0 broken writers. Outside: variable-assembled
   spread payloads; backend tree (covered separately by 94-06's oracle); link_audit_logs (a
   different table, out of phase).
4. i18n key-set equality: pnpm lint (carries scripts/check-i18n-namespaces.mjs) + the per-plan
   node derivations re-run for common.afterActions, unified-kanban.errors,
   after-actions-page.degraded, report-builder.generate. Outside: namespaces this phase never
   touched.
5. Deploy evidence: 94-DEPLOY-LEDGER.md verdict-last OK count ≥ 27 (counter already
   positive-controlled in 94-10) + after-actions-list-all deploy evidence from 94-07's SUMMARY.
6. D-08 copy sweep over the five repaired surfaces (the named files from plans 01/03/04/07/09):
   zero `toast` calls interpolating `.message`; population = those named files only; outside =
   the rest of the app (COPY-06 and the double-toast question are Phase 98 / deferred).
7. ARMA-01: the tightened spec contains zero `query-error-state` references and passes 1/1.

Instrument constraints (94-VALIDATION.md — binding on every command you write):

- `timeout` DOES NOT EXIST here; never `2>/dev/null`; never read an exit code through a pipe.
- `grep` is a ugrep wrapper honouring .gitignore — sweeps that must see .tickmarkr/ use explicit
  file args or `find … -print0 | xargs -0 grep`; instrument-test every zero against a
  known-present token before believing it.
- The C9b script fails open under zsh — run it under `bash` explicitly, with derived roots (four
  test roots + colocated **tests** dirs); anchor to phase-94-base (exists, signed — verify).
- Playwright: spec paths are FILTERS — existence first, hardcoded counts, --no-deps on every
  root-config command including --list.
- pnpm test via Turbo needs --continue.

C9b cross-phase consumer table: carry research §8's triage verbatim as the starting point and
re-check each verdict against the executed tree — including the NON-ORACLES
(useAfterActionsAll.test mocked; backend contract reports tests non-required;
backend/src colocated tests never run; \_phase52-mid-drag pre-existing red by DATA). The class the
sweep cannot see (shape-coupled tests naming no identifier) is defended by running the shipped
suites and READING the reds against the E2ESTALE-01 inventory, not assuming staleness.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Closing derivations with populations + the intended-broken register</name>
  <files>.planning/phases/94-write-paths/94-VERIFICATION.md</files>
  <read_first>93-15-SUMMARY.md and 93-VERIFICATION.md (the house shape being mirrored); each 94-* SUMMARY produced by this phase</read_first>
  <action>
    Execute derivations 1-7 from the interfaces block and record each as: the command, its
    output, its POPULATION DEFINITION (search root, glob, matching rule), and WHAT FALLS OUTSIDE
    IT. Copy the C9b cross-phase consumer table in with re-checked verdicts (CONFIRMED rows cite
    spec-run results; NON-ORACLE rows keep their mock-vs-real reason — D-28). Then write the
    intended-broken register per the must_haves truth (every deliberately-open item with its
    owning phase — the exact list is the contract). Disposition all NINE requirements (D-01):
    each maps to its evidence or is recorded OPEN with a number. A finding that fails any
    derivation is recorded OPEN — never silently absorbed. NOT-CHECKED beats silence.
  </action>
  <verify>
    <automated>V=.planning/phases/94-write-paths/94-VERIFICATION.md && test -f "$V" && test "$(grep -c 'POPULATION' "$V")" -ge 7 && grep -q 'COUNT-04' "$V" && grep -q 'DEAD-09' "$V" && grep -q 'COPY-06' "$V" && grep -q 'C9b' "$V" && grep -q 'ARMA-01' "$V" && test ! -f .planning/phases/94-write-paths/94-VERIFICATION-INDEPENDENT.md</automated>
  </verify>
  <acceptance_criteria>All 7 derivations recorded with populations and outside-statements (threshold 7 = the enumerated list, C4-derived); the register names every owning phase; all nine requirements dispositioned; the independent verifier's path untouched (D-29).</acceptance_criteria>
  <done>The phase's numbers all say what set they measured, and what stays broken says who owns it.</done>
</task>

<task type="auto">
  <name>Task 2: Full oracle run + the gate drill</name>
  <files></files>
  <read_first>GATE-STANDARD.md §The pass procedure (one author, all gates, one pass; the drill-table row format); scripts/gate-drill.mjs header</read_first>
  <action>
    (1) Specs, existence first, counts hardcoded: frontend config —
    tests/e2e/settings-save.spec.ts (2) and tests/e2e/after-action-detail-error.spec.ts (1);
    root config — tests/e2e/93-report-notfound.spec.ts (1, --no-deps, chromium-en). Space the
    runs (ORACLECAP-01: a rate-limit red is UNABLE TO MEASURE — record it as such and re-run
    after the window, never as a regression).
    (2) Vitest: the five phase suites (AfterActionForm, IntakeForm, commitment-stage-guard,
    WorkBoard, BoardColumn + kanban trio, AfterActionsTable, backend audit-write) — run and
    paste counts.
    (3) Probes: probe-report-rls, probe-commitment-readback, probe-after-actions-list,
    probe-audit-row — all exit 0; paste ledgers.
    (4) Shipped-suite reds: run the pre-existing after-action e2e specs once and READ the reds
    against the E2ESTALE-01 inventory — attribute, never assume.
    (5) Drill: scripts/gate-drill.mjs across the 94 plan set (mechanical half); then author the
    C1 table (plan.gate | C1 red | C1 green construction | C2-C10 exceptions | verdict) in
    94-VERIFICATION.md — constructions in a scratch worktree, `git status --porcelain`
    byte-identical before/after; MCP-only evidence (migration apply, pg_policies) carries
    point-in-time labels. A gate whose done state cannot be constructed is CANNOT CONSTRUCT with
    what it needs.
  </action>
  <verify>
    <automated>test -f tests/e2e/93-report-notfound.spec.ts && OUT=$(pnpm exec playwright test tests/e2e/93-report-notfound.spec.ts --project=chromium-en --no-deps 2>&1) && echo "$OUT" | grep -qE '(^|[^0-9])1 passed' && test -f frontend/tests/e2e/settings-save.spec.ts && test -f frontend/tests/e2e/after-action-detail-error.spec.ts && OUT2=$(cd frontend && pnpm exec playwright test tests/e2e/settings-save.spec.ts tests/e2e/after-action-detail-error.spec.ts --reporter=line 2>&1) && echo "$OUT2" | grep -qE '(^|[^0-9])3 passed' && node scripts/probe-report-rls.mjs && node scripts/probe-commitment-readback.mjs && node scripts/probe-audit-row.mjs && git rev-parse -q --verify refs/tags/phase-94-base >/dev/null && git tag -v phase-94-base >/dev/null 2>&1 && grep -q 'gate-drill' .planning/phases/94-write-paths/94-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>All spec counts match their hardcoded expectations (1 + 3, both spec sets existence-asserted first); all probes green on re-run; the drill table covers every automated gate in the plan set with a verdict and no unconstructed done state folded into SOUND; phase-94-base verifies signed; the tree is byte-identical after constructions.</acceptance_criteria>
  <done>The phase's render-state claims closed by rendering, its live claims by probing, its gates by drilling.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                  |
| --------------------------- | -------------------------------------------- |
| evidence → close-out claims | unstated scopes convert findings into passes |

## STRIDE Threat Register

| Threat ID | Category               | Component           | Disposition | Mitigation Plan                                                                          |
| --------- | ---------------------- | ------------------- | ----------- | ---------------------------------------------------------------------------------------- |
| T-94-20   | Repudiation            | closing derivations | mitigate    | population + outside-statement mandatory per derivation (D-24); NOT-CHECKED over silence |
| T-94-21   | Information Disclosure | probe/spec evidence | mitigate    | probes never echo credentials; pasted outputs contain no secrets                         |
| T-94-SC   | Tampering              | package installs    | accept      | zero installs this phase                                                                 |

</threat_model>

<verification>
- 94-VERIFICATION.md exists with ≥7 POPULATION-stated derivations, the drill table, spec/probe
  outputs, the C9b table with mock-vs-real verdicts, and the intended-broken register.
- The independent verifier's artifact path is untouched.
</verification>

<success_criteria>
The phase closes on evidence whose scope is stated: nine requirements dispositioned, oracles that
rendered and probed, gates drilled both directions, and every deliberately-broken surface named
with its owning phase.
</success_criteria>

<output>
Create `.planning/phases/94-write-paths/94-11-SUMMARY.md` when done.
</output>
