---
phase: 94-write-paths
plan: 09
type: execute
wave: 3
depends_on: [94-05, 94-07]
files_modified:
  - frontend/src/pages/reports/ReportsPage.tsx
  - frontend/src/pages/reports/generate-entry.ts
  - frontend/src/pages/reports/__tests__/generate-entry.test.ts
  - frontend/src/i18n/en/report-builder.json
  - frontend/src/i18n/ar/report-builder.json
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [WRITE-06]
truths:
  - "Decisions covered — D-09: the new generation-unavailable copy lands in BOTH locales in the same commit under key-set equality; D-10: the new t() call uses the COLON form (report-builder:generate.unavailable) — ReportsPage currently uses the bare default namespace, which is exactly the dot-form class; D-25: the terminal-state contract carries a behavioural statement — absent data.url the UI renders an unavailable/error entry, NEVER a fabricated completed entry (asserted by grep gates here and by the closing plan's criterion restatement, and labelled: a full browser oracle for the mock path is deliberately NOT built because the path is being FILED as DEAD-09, not shipped as working)"
must_haves:
  truths:
    - 'PARK-94-06 ruled (a) with the pairing rule: the template→type rename NEVER ships alone — this plan ships shape (i): rename PAIRED with an explicit terminal state (absent data.url → "generation unavailable" entry, never status completed), because converting a visible 400 into an invisible fabricated success is the forbidden shape'
    - "Criterion 5's second half scopes to the REAL surfaces — custom-reports CRUD + scheduled-report creation, both closed by 94-05's probe; the mock generate path is FILED as DEAD-09 (owner Phase 95 — Routes That Don't Render, flagged approve-as-placed), not silently left"
    - 'The generated-reports list can no longer show a completed entry whose url was never returned'
  artifacts:
    - path: 'frontend/src/pages/reports/ReportsPage.tsx'
      provides: 'body key type (matching reports/index.ts:258) + the honest terminal state on the generate mutation'
      contains: 'generate.unavailable'
    - path: '.planning/REQUIREMENTS.md'
      provides: 'DEAD-09: the reports POST mock (fabricated 202 + setTimeout console.log) filed with owner Phase 95 + a status-table row'
      contains: 'DEAD-09'
  key_links:
    - from: 'frontend/src/pages/reports/ReportsPage.tsx'
      to: 'frontend/src/i18n/en/report-builder.json'
      via: 'the terminal state renders t(report-builder:generate.unavailable) — colon form into a namespace registered both locales'
      pattern: 'generate.unavailable'
---

<objective>
Close WRITE-06's generate-surface half in the only honest shape available. The field contract is
real (client sends `template`, `reports/index.ts:258` requires `type` — every generate 400s), but
the handler is a MOCK: 202 + setTimeout console.log, no `data.url` ever returned, while the
client's onSuccess fabricates a 'completed' entry. RULING-P94-04 §PARK-94-06: the rename ships ONLY
paired with an explicit terminal state, and the mock is filed as a tracked requirement.

Output: rename + terminal state + bilingual key + the DEAD-09 filing (this plan is the phase's
third REQUIREMENTS.md writer — wave-serialized after 94-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-write-paths/94-RESEARCH.md  (§6 — the mock-POST hazard, both sides of the contract pinned)
@.tickmarkr/overseer/RULING-P94-04-RESEARCH-PARKS.md  (§PARK-94-06 — the pairing rule, binding)

<interfaces>
Verified by research (re-read at execution):
- `ReportsPage.tsx:186` — invoke('reports', { body: { template: templateId, format, parameters } }).
- `ReportsPage.tsx:195-207` — onSuccess pushes { status: 'completed', url: data.url } where
  data.url is NEVER returned by the fn (its POST is a mock, reports/index.ts:266-285).
- `reports/index.ts:258` — `if (!body.type || !body.format)` → 400. The FUNCTION IS NOT EDITED in
  this plan (the mock's fate is Phase 95's, via DEAD-09) — the client-side rename alone removes
  the 400.
- ReportsPage uses bare useTranslation() (default ns). The new key goes in the report-builder
  namespace (registered BOTH locales, i18n/index.ts:312/:448) addressed with the COLON form —
  which also keeps this plan out of common.json (owned by 94-07 in the prior wave).
- C9b: backend/tests/contract/reports-generate.test.ts and reports-schedule.test.ts route to the
  NON-REQUIRED integration job — NON-ORACLES unless individually shown green pre-phase; record as
  named non-consumers in the SUMMARY.
- Register conventions for the filing: entries are section-prefixed (### DEAD block, after
  DEAD-08); the status table near the file's end takes one row per id; COPY-06 (:124) is the
  formatting precedent for a Phase-94-filed entry with an owner phase and an approve-as-placed
  flag.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename paired with the terminal state + bilingual key</name>
  <files>frontend/src/pages/reports/ReportsPage.tsx, frontend/src/i18n/en/report-builder.json, frontend/src/i18n/ar/report-builder.json</files>
  <read_first>ReportsPage.tsx:180-260 (the mutation, onSuccess, and the generated-reports list rendering); report-builder.json both locales (existing key structure)</read_first>
  <action>
    (1) Keys, both files same edit: a `generate.unavailable` pair (suggested EN "Report generation
    is unavailable" with a short description key if the list entry renders two lines — sentence
    case, no marketing voice; AR aligned with the namespace's existing terminology). Keep the
    report-builder key sets equal.
    (2) `:186` — body key `template` → `type` (the server's name; the server is not edited).
    (3) onSuccess: if data?.url is a non-empty string, keep the completed entry; OTHERWISE push an
    entry whose status renders as unavailable/error using t('report-builder:generate.unavailable')
    — NEVER 'completed', NEVER a fabricated url. Style the state with existing tokens (text-warn
    or the list's existing error affordance — no new chrome, no new variants). If the entry-shape
    union does not admit an error state, extend the local type — this is page-local state, no
    shared types involved.
    (4) The mutation's onError path (if any) keeps or gains translated copy only — no raw
    error.message to the DOM (D-08 ambient).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/pages/reports/__tests__/generate-entry.test.ts && grep -qE "type: *templateId" src/pages/reports/ReportsPage.tsx && test "$(grep -cE "template: *templateId" src/pages/reports/ReportsPage.tsx)" -eq 0 && grep -q "report-builder:generate.unavailable" src/pages/reports/ReportsPage.tsx && test "$(grep -cE "status: *'completed'" src/pages/reports/ReportsPage.tsx)" -eq 0 && node -e "const en=require('./src/i18n/en/report-builder.json'),ar=require('./src/i18n/ar/report-builder.json');process.exit(typeof en.generate?.unavailable==='string'&&typeof ar.generate?.unavailable==='string'&&en.generate.unavailable!==ar.generate.unavailable?0:1)" && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>The body sends type; no template key remains in the invoke body; absent data.url can only produce the translated unavailable entry (fabricated completed impossible by construction); key bilingual; type-check clean.</acceptance_criteria>
  <done>A visible 400 became a visible honest state — not an invisible lie.</done>
</task>

<task type="auto">
  <name>Task 1b: Extract the entry mapping to a pure function and TEST it (RULING-P94-06 B4)</name>
  <files>frontend/src/pages/reports/generate-entry.ts</files>
  <files>frontend/src/pages/reports/__tests__/generate-entry.test.ts</files>
  <action>
    The grep-only form of this gate did not enforce the pairing it exists for: a rename plus a
    stray key reference anywhere in the file would pass it, while `ReportsPage.tsx:201` pushes
    `status: 'completed'` UNCONDITIONALLY today and nothing checked that it becomes conditional.
    That is PARK-94-06's forbidden shape surviving its own guard.

    Extract the onSuccess entry-mapping into a pure, exported function in
    `frontend/src/pages/reports/generate-entry.ts` — input: the function response object; output:
    the list entry. Then assert its behaviour directly in vitest:
      - response `{}` (no `url`, which is what the mock ALWAYS returns) → an entry whose status is
        the unavailable/error state, and NOT `completed`;
      - response `{ url: 'https://…' }` → a `completed` entry carrying that url.
    `ReportsPage.tsx` calls the function instead of building the entry inline.

    A pure function is testable without a browser, a server, or the mock — so this closes the
    hazard with a real oracle rather than with a stricter grep.

  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/pages/reports/__tests__/generate-entry.test.ts && grep -q 'export' src/pages/reports/generate-entry.ts && grep -q 'generate-entry' src/pages/reports/ReportsPage.tsx && test "$(grep -cE "status: *'completed'" src/pages/reports/ReportsPage.tsx)" -eq 0</automated>
  </verify>
  <acceptance_criteria>The vitest run EXITS 0 (its exit code gates — no `| grep passed`), both cases are asserted, `ReportsPage.tsx` no longer contains a literal `status: 'completed'`, and the mapping is reachable from the page. A rename without the terminal state now fails this test, which is the property the grep gate could not express.</acceptance_criteria>
  <done>The rename cannot ship alone: the entry mapping's behaviour is pinned by a test that fails if an absent url still yields a completed entry.</done>
</task>

<task type="auto">
  <name>Task 2: File DEAD-09 — the mock generate path, owner Phase 95 (same commit as Task 1)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md §DEAD (DEAD-01..08) and the COPY-06 entry (:124, the filed-from-Phase-94 formatting precedent); the status table near the end</read_first>
  <action>
    STAGE AND COMMIT WITH TASK 1 (the pairing rule: the rename and the filing are one honest act).
    Add **DEAD-09** at the end of the ### DEAD block: the `reports` edge function's POST handler
    is a MOCK — returns 202 { job_id, status: 'pending' } from a setTimeout that only
    console.logs (reports/index.ts:266-285); no report is ever generated and no url returned;
    Phase 94 renamed the client field (template→type per the server contract at :258) PAIRED with
    an explicit unavailable terminal state per RULING-P94-04 §PARK-94-06, so the surface is
    honest but DEAD — generation needs a real execution path (candidates: the custom-reports fn
    flow, or the Express /report-builder/generate path in misc.repository.ts:125). Owner:
    **Phase 95 — Routes That Don't Render** (its goal: every surface either works or says why it
    can't), flagged **approve-as-placed** (the D-73 pattern). Add the status-table row
    `| DEAD-09 | Phase 95 — Routes That Don't Render | Pending |`. Verify DEAD-09 is genuinely
    the next free id in the section before writing (derive, don't assume). Touch nothing else —
    94-10 owns the next REQUIREMENTS.md edit.
  </action>
  <verify>
    <automated>test "$(grep -c 'DEAD-09' .planning/REQUIREMENTS.md)" -ge 2 && grep -q 'PARK-94-06' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>DEAD-09 exists as an entry AND a status-table row (count ≥2), cites the ruling, names the owner phase and the approve-as-placed flag, and landed in the SAME commit as the rename (checked mechanically).</acceptance_criteria>
  <done>The mock is tracked in the queue, not remembered in prose.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                  |
| --------------------------- | -------------------------------------------- |
| mock fn response → UI state | a 202 with no url must not render as success |

## STRIDE Threat Register

| Threat ID | Category    | Component        | Disposition | Mitigation Plan                                                                                       |
| --------- | ----------- | ---------------- | ----------- | ----------------------------------------------------------------------------------------------------- |
| T-94-16   | Repudiation | generate flow    | mitigate    | fabricated completed entries impossible by construction; the dead path filed as DEAD-09 with an owner |
| T-94-SC   | Tampering   | package installs | accept      | zero installs this phase                                                                              |

</threat_model>

<verification>
- Grep gates: type sent, template gone, unavailable key wired bilingually; type-check.
- DEAD-09 entry + table row in the same commit as the rename.
</verification>

<success_criteria>
WRITE-06's generate surface is honest: the real surfaces (CRUD + schedules, closed in 94-05) work,
the mock path renders its truth, and its repair has a named owner.
</success_criteria>

<output>
Create `.planning/phases/94-write-paths/94-09-SUMMARY.md` when done.
</output>
