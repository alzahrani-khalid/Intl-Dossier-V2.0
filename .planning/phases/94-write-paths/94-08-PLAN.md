---
phase: 94-write-paths
plan: 08
type: execute
wave: 2
depends_on: [94-03]
files_modified:
  - frontend/src/components/kanban/index.ts
  - frontend/src/pages/WorkBoard/BoardColumn.tsx
  - frontend/src/pages/WorkBoard/WorkBoard.tsx
  - frontend/src/pages/WorkBoard/__tests__/BoardColumn.test.tsx
  - scripts/probe-commitment-readback.mjs
  - .planning/phases/94-write-paths/94-STAGE-PARITY.md
autonomous: true
requirements: [WRITE-04]
truths:
  - 'Decisions covered — D-04: the per-card droppable predicate is PROVEN expressible by an oracle, never assumed (the ruling explicitly does not establish it); if it proves inexpressible, (a) alone — the 94-03 mutation-layer reject — is the fallback and the switch note says so; D-32: persistence is proven by WRITE-THEN-READ-BACK against the stored row with a non-past-due fixture, plus a past-due COERCION CONTROL — never by "no error"; D-33: the droppable predicate mirrors the trigger condition via the SHARED guard module from 94-03 (one condition, two enforcement points), the dragged card''s HOME column is NEVER disabled (the closestCenter carve-out — a home drop over a disabled column would retarget, for an overdue card possibly onto Done, a silent write), and the home-drop oracle asserts NO mutation fires; D-34: the trigger-sweep findings are consumed as inputs — the tasks STAGE_TO_STATUS map is a second copy of the DB''s own CASE kept in sync by nothing, so this plan ships the parity oracle (client map vs live CASE) and no oracle asserts a client task-status payload reached the row'
must_haves:
  truths:
    - 'During a commitment drag, the Review column paints NO drop affordance (excluded from collision candidates via useDroppable disabled) — absence of the drop signal IS the affordance; no new CSS, no opacity treatment'
    - "During a past-due commitment drag, columns whose mapped status is pending or in_progress are likewise non-droppable — EXCEPT the card's own current column, which is never disabled (the mandatory carve-out); a home drop fires NO mutation (asserted, not assumed)"
    - 'Two stated traps carried from research: closestCenter RETARGETS a release over a disabled column to the nearest enabled one (tolerated only because no drop signal was shown and the landing is visible); cards inside Review are droppables of their own, so the 94-03 mutation reject remains the load-bearing guarantee'
    - "Switch note (D-33/RULING-P94-03): whether refusal remains the right interaction once overdue renders distinctly is Phase 96's to revisit — carried, not settled"
    - 'The client STAGE_TO_STATUS map and the live sync_task_status_from_workflow_stage CASE agree cell-for-cell — derived live, recorded in 94-STAGE-PARITY.md; a mismatch is a STOP, not a footnote'
    - 'NAMED NON-IMPLEMENTATION (W4, recorded so the tolerance cannot silently worsen): the own-column carve-out closes cancel-by-drop-home, but it does NOT close releasing a past-due card over the DISABLED In-progress column. closestCenter retargets that release to the nearest ENABLED droppable — either Todo (home; a harmless no-op) or DONE, which writes `completed` with only the global success toast. The user aimed for In-progress and may get a completed commitment. This is TOLERATED for this phase because the landing is visible on the board, and it is recorded HERE plus in 94-STAGE-PARITY.md as an expected outcome rather than left implicit. No oracle pins it — that is the honest gap, not a claim of coverage.'
  artifacts:
    - path: 'frontend/src/pages/WorkBoard/BoardColumn.tsx'
      provides: 'the droppable predicate consuming the 94-03 guard module; data-droppable-id selector untouched'
      contains: 'disabled'
    - path: 'scripts/probe-commitment-readback.mjs'
      provides: 'the D-32 write-then-read-back oracle with the coercion control (Wave 0 gap closed)'
      contains: 'read-back'
    - path: '.planning/phases/94-write-paths/94-STAGE-PARITY.md'
      provides: 'the D-34 parity evidence: live CASE pairs vs client map, verdict line PARITY: MATCH'
      contains: 'PARITY'
  key_links:
    - from: 'frontend/src/pages/WorkBoard/BoardColumn.tsx'
      to: 'frontend/src/pages/WorkBoard/commitment-stage-guard.ts'
      via: 'the droppable predicate calls the SAME resolveCommitmentDropDecision the mutation layer uses — no second copy of the condition'
      pattern: 'resolveCommitmentDropDecision'
    - from: 'frontend/src/components/kanban/index.ts'
      to: 'frontend/src/pages/WorkBoard/BoardColumn.tsx'
      via: 'useDndContext re-exported through the barrel — direct @dnd-kit/core imports are ESLint-banned outside components/kanban/*'
      pattern: 'useDndContext'
---

<objective>
Close WRITE-04's affordance half and its live oracles, on top of 94-03's guard module. dnd-kit
6.3.1 makes the per-card droppable predicate expressible (useDroppable accepts disabled;
useDndContext is a public export) — but the oracle must PROVE it, and two measured hazards
(closestCenter retargeting; review-resident cards as droppables) mean the 94-03 mutation reject
stays load-bearing. Also ships the D-32 read-back probe and the D-34 STAGE_TO_STATUS parity oracle.

Output: droppable predicate + barrel export + component oracle + read-back probe + parity record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-write-paths/94-UI-SPEC.md  (§2 Non-droppable affordance — the carve-out is a contract, not a suggestion)
@.planning/phases/94-write-paths/94-RESEARCH.md  (§4 — the dnd-kit type evidence and the two traps)
@.tickmarkr/overseer/P94-TRIGGER-SWEEP.md  (F1 — the parity subject; F3 — commitment_status_history records the COERCED value and is NOT evidence of user intent: no oracle may read it as such)

<interfaces>
Verified by research (re-read at execution):
- `BoardColumn.tsx:138` — `useDroppable({ id: stage })` today. Add `disabled`, computed from the
  active drag: read the active item via `useDndContext()` (add to the `@/components/kanban`
  barrel — direct @dnd-kit/core imports are ESLint-fenced inside components/kanban/*, enforced by
  eslint-ban-dndkit.test.ts) or via the provider's existing activeCardId (KanbanProvider.tsx:130)
  + an item lookup. Predicate: for an active COMMITMENT card, a column is disabled when
  resolveCommitmentDropDecision(activeItem, column.stage) returns { ok: false } AND the column is
  NOT the card's current column (resolveBoardStage(activeItem) — the carve-out). Tasks and intake
  cards: never disabled by this predicate.
- `@dnd-kit/core@6.3.1` useDroppable.d.ts: `disabled?: boolean` re-evaluated per render;
  `useDndContext` exported from dist/index.d.ts; Active carries `data`.
- KanbanCard sortables inside Review remain droppables (useSortable, not disabled) — card-level
  droppable-disable (`useSortable({ disabled: { droppable } })`, sortable@10.0.0) is PERMITTED
  polish, NOT required; the decision to skip it is recorded in the SUMMARY as a named
  non-implementation (the mutation reject covers that path).
- Do NOT rename section.col / .col-head / data-droppable-id — five shipped e2e specs key on them
  (kanban-render, kanban-a11y, tasks-tab-dnd, tasks-tab-keyboard, tasks-tab-a11y). Run
  kanban component tests after the change; _phase52-mid-drag-capture.spec.ts is pre-existing red
  by DATA (P52FIXTURE-01) — do not chase.
- The live CASE (P94-TRIGGER-SWEEP F1): todo→pending, in_progress→in_progress, review→review,
  done→completed (+ completed_at stamp), cancelled→cancelled — re-derive at execution from
  pg_proc, never quote the sweep.
- Read-back probe env: SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY (for fixture setup only) plus a USER JWT minted via signInWithPassword for the READ-BACK itself (W5, RULING-P94-06: reuse 94-05's user-JWT pattern — a service-role read bypasses RLS, so it cannot see the WITH CHECK silent-zero-row class; one read-back through the real user path converts a carried blind spot into a closed one); absent → exit 2 UNABLE TO
  MEASURE (C2). aa_commitments requires namespaced fixture rows; the trigger is UPDATE-only
  (never fires on INSERT), so a past-due fixture INSERTs at the status you give it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The droppable predicate with the own-column carve-out, proven by its oracle</name>
  <files>frontend/src/components/kanban/index.ts, frontend/src/pages/WorkBoard/BoardColumn.tsx, frontend/src/pages/WorkBoard/WorkBoard.tsx, frontend/src/pages/WorkBoard/__tests__/BoardColumn.test.tsx</files>
  <read_first>BoardColumn.tsx in full (:11-14 the selector-contract comment); KanbanProvider.tsx:120-140 and :280-290 (activeCardId + closestCenter); commitment-stage-guard.ts (the 94-03 contract this consumes); BoardColumn.test.tsx</read_first>
  <action>
    (1) Add useDndContext to the @/components/kanban barrel. (2) Wire the predicate per the
    interfaces block — the guard module is the ONLY source of the condition (no second copy;
    D-33's no-drift-by-construction). The carve-out is mandatory: the active card's home column
    is NEVER disabled, because closestCenter retargets a release over a disabled column to the
    nearest ENABLED one — a cancel-by-dropping-home on an overdue card could otherwise land on
    Done and silently write completed. (3) No visual treatment beyond the absence of the drop
    signal: disabled droppables leave collision candidates, so no isOver ring paints — no new
    CSS, no opacity. (4) Extend BoardColumn.test.tsx to PROVE expressibility (D-04's explicit
    demand): render within a DndContext simulating an active commitment drag and assert the
    Review column's droppable is disabled (no isOver affordance attribute/class) while a task
    drag leaves it enabled; assert the past-due predicate disables Todo/In-progress but NOT the
    card's home column; assert a home drop fires NO mutation (the carve-out oracle — absence of
    the call, per D-32's never-absence-of-error rule). If the predicate proves INEXPRESSIBLE at
    any point: STOP the affordance half, keep the 94-03 reject as the sole guarantee, and record
    the switch note (fallback (a) alone) in the SUMMARY — that outcome is a legitimate close of
    D-04, not a failure. (5) Run the three kanban component suites to confirm no prop regression.
  </action>
  <verify>
    <automated>cd frontend && grep -q 'useDndContext' src/components/kanban/index.ts && grep -q 'resolveCommitmentDropDecision' src/pages/WorkBoard/BoardColumn.tsx && grep -q 'data-droppable-id' src/pages/WorkBoard/BoardColumn.tsx && pnpm exec vitest run src/pages/WorkBoard/__tests__/BoardColumn.test.tsx src/components/kanban/__tests__/KanbanBoard.test.tsx src/components/kanban/__tests__/KanbanCard.test.tsx src/components/kanban/__tests__/KanbanProvider.test.tsx src/components/kanban/__tests__/eslint-ban-dndkit.test.ts && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>The predicate consumes the shared guard (grep-proven); the expressibility oracle passes (or the fallback path is taken and recorded); the home-drop no-mutation assertion exists; all kanban suites + the ESLint fence stay green; selector contract untouched; type-check clean.</acceptance_criteria>
  <done>Nothing that cannot succeed looks droppable, and the one drop that must stay reachable (home) provably fires nothing.</done>
</task>

<task type="auto">
  <name>Task 2: The write-then-read-back probe with the coercion control</name>
  <files>scripts/probe-commitment-readback.mjs</files>
  <read_first>scripts/probe-edge-auth.sh (secret hygiene); PARK-P94.md §PARK-94-04 (the transactional proof this probe re-establishes as a persistent instrument)</read_first>
  <action>
    Write scripts/probe-commitment-readback.mjs (node fetch, service-role via PostgREST; env
    absent → exit 2 UNABLE TO MEASURE). Printing every id it acts on:
    (1) PERSISTENCE side: INSERT a namespaced fixture commitment ('p94-probe-readback', due_date =
    today + 30 days, status 'pending'); UPDATE status to 'in_progress'; READ BACK by id — must
    equal 'in_progress' (the trigger's condition is false for a future due date). This is the
    stored-value half of the criterion invariant.
    (2) COERCION CONTROL: INSERT a second fixture with due_date = today - 30 days, status
    'pending' (the trigger never fires on INSERT, so it stays pending); UPDATE status to
    'in_progress'; READ BACK — must equal 'overdue' (the trigger coerced it). This proves the
    condition the client guard mirrors is REAL and still live — if this control ever reads
    'in_progress', the trigger changed and the client guard has silently drifted: exit 1 with a
    DRIFT message (this probe is the standing drift alarm).
    (3) Do NOT read commitment_status_history as evidence of anything (F3: it records the coerced
    value, not user intent).
    (4) CLEANUP: delete both fixtures; re-assert the staging census excludes them. Never touch the
    10 live commitments. Exit 0 only if both sides held and cleanup verified.
  </action>
  <verify>
    <automated>test -f scripts/probe-commitment-readback.mjs && grep -qi 'read-back' scripts/probe-commitment-readback.mjs && node scripts/probe-commitment-readback.mjs</automated>
  </verify>
  <acceptance_criteria>Probe exits 0: future-due write persists verbatim (read-back, not error-absence), past-due control observed coerced (the D-32/D-33 coercion case in the oracle set), fixtures cleaned, live rows untouched, ids printed.</acceptance_criteria>
  <done>The criterion invariant's stored-value clause has a live, re-runnable oracle — and a drift alarm on the mirrored condition.</done>
</task>

<task type="auto">
  <name>Task 3: The STAGE_TO_STATUS parity derivation (D-34/F1)</name>
  <files>.planning/phases/94-write-paths/94-STAGE-PARITY.md</files>
  <read_first>P94-TRIGGER-SWEEP.md §F1 (the CASE text and the duplicate-copy finding); WorkBoard.tsx:78-84 (the client map)</read_first>
  <action>
    Via Supabase MCP execute_sql: SELECT prosrc FROM pg_proc WHERE proname =
    'sync_task_status_from_workflow_stage'; extract the five WHEN pairs. Read the client
    STAGE_TO_STATUS from WorkBoard.tsx at HEAD. Write 94-STAGE-PARITY.md: both tables
    side-by-side, the derivation date, the population definition (the five workflow_stage keys;
    OUTSIDE it: the completed_at side effect, which the client map has no equivalent for — stated,
    per the sweep), and a final verdict line `PARITY: MATCH` or `PARITY: MISMATCH — <cells>`.
    On MISMATCH: STOP and surface to the orchestrator — do not adapt either copy silently (which
    copy is right is a product call; Phase 96 owns the seam's future). Also record the standing
    consequence: no WRITE-04 oracle asserts a client task-status payload reached the row — for
    tasks, status is DB-derived from workflow_stage.
  </action>
  <verify>
    <automated>test -f .planning/phases/94-write-paths/94-STAGE-PARITY.md && grep -q 'PARITY: MATCH' .planning/phases/94-write-paths/94-STAGE-PARITY.md && grep -qi 'population' .planning/phases/94-write-paths/94-STAGE-PARITY.md</automated>
  </verify>
  <acceptance_criteria>The parity record exists with both tables, the derivation command, the population + outside-statement, and the MATCH verdict (a MISMATCH run stops the plan instead of passing this gate — RED for the right reason).</acceptance_criteria>
  <done>The two copies of the task state machine are proven to agree today, in writing, with the seam named for Phase 96.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                        |
| ----------------------------- | -------------------------------------------------- |
| service-role probe → staging  | fixture writes on a live table with a live trigger |
| drag affordance → user intent | a disabled column must not silently reroute a drop |

## STRIDE Threat Register

| Threat ID | Category  | Component              | Disposition | Mitigation Plan                                                                                                                                  |
| --------- | --------- | ---------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-94-14   | Tampering | probe fixtures         | mitigate    | namespaced rows, ids printed, deleted at end, live census re-asserted; history table never read as intent                                        |
| T-94-15   | Tampering | closestCenter retarget | mitigate    | own-column carve-out (home drop = provable no-op); mutation-layer reject (94-03) remains load-bearing for every path the affordance cannot close |
| T-94-SC   | Tampering | package installs       | accept      | zero installs this phase                                                                                                                         |

</threat_model>

<verification>
- Expressibility oracle + kanban suites + ESLint fence green (or the recorded fallback path).
- Read-back probe green both sides with cleanup.
- Parity record MATCH with population statement.
</verification>

<success_criteria>
The affordance no longer invites impossible drops, the home-cancel path provably writes nothing,
persistence is proven by read-back with a live coercion control, and the task-map duplicate is
pinned in evidence for Phase 96.
</success_criteria>

<output>
Create `.planning/phases/94-write-paths/94-08-SUMMARY.md` when done.
</output>
