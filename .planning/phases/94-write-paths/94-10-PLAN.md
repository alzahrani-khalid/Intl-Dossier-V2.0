---
phase: 94-write-paths
plan: 10
type: execute
wave: 4
depends_on: [94-06, 94-09]
files_modified:
  - supabase/functions/activate-account/index.ts
  - supabase/functions/approve-role-change/index.ts
  - supabase/functions/assign-role/index.ts
  - supabase/functions/assignments-manual-override/index.ts
  - supabase/functions/attachments-delete/index.ts
  - supabase/functions/auth-biometric-setup/index.ts
  - supabase/functions/auth-refresh-token/index.ts
  - supabase/functions/auth-step-up-complete/index.ts
  - supabase/functions/auth-step-up-initiate/index.ts
  - supabase/functions/certify-user-access/index.ts
  - supabase/functions/commitments-update-status/index.ts
  - supabase/functions/complete-access-review/index.ts
  - supabase/functions/create-user/index.ts
  - supabase/functions/deactivate-user/index.ts
  - supabase/functions/engagements-positions-attach/index.ts
  - supabase/functions/engagements-positions-detach/index.ts
  - supabase/functions/generate-access-review/index.ts
  - supabase/functions/inactive-users/index.ts
  - supabase/functions/initiate-password-reset/index.ts
  - supabase/functions/notifications-register-device/index.ts
  - supabase/functions/positions-unpublish/index.ts
  - supabase/functions/push-device-register/index.ts
  - supabase/functions/reactivate-user/index.ts
  - supabase/functions/reset-password/index.ts
  - supabase/functions/schedule-access-review/index.ts
  - supabase/functions/setup-mfa/index.ts
  - supabase/functions/verify-mfa-setup/index.ts
  - scripts/probe-audit-row.mjs
  - .planning/phases/94-write-paths/94-DEPLOY-LEDGER.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [AUDIT-ZERO-01, AUDIT-DROP-01]
truths:
  - "Decisions covered — D-18 (RULING-P94-04 §PARK-94-08 ruled (a)): the precondition-grade subset is enumerated BY NAME — assign-role, create-user, deactivate-user, approve-role-change, certify-user-access FAIL the action when the audit write fails (the honest tension is STATED in the criterion: a role grant can fail BECAUSE auditing failed — that is the intended trade); the other 22 log loudly and continue; D-19: every repaired function is REDEPLOYED — the P92 139-deploy precedent; a fixed function not redeployed has not been fixed; D-20: AUDIT-ZERO-01's population was re-derived with BOTH quote styles (38 files / 36 writers / 27 broken — the filed 20-of-32 was single-quote-biased) and its stated blind spots are carried forward: payloads assembled as variables and spread are OUTSIDE the literal-object scan; RPC-mediated writes searched, none found; upsert included, zero found"
must_haves:
  truths:
    - 'All 27 broken edge writers insert through the 94-06 shared helper with the REAL audit_logs column set; zero of the known bad keys (event_type, resource_type, resource_id, target_user_id, metadata, changes, after, details, actor_id) remain in their audit payloads'
    - 'A representative repaired function, exercised against DEPLOYED staging, lands a real audit_logs row — the table goes 0 → >0 for the first time (the AUDIT-ZERO-01 closure evidence; the row stays, it is a genuine audit record)'
    - 'The deploy ledger records one row per deployed function in the shape `| fn | timestamp | OK |` with the verdict cell LAST — the 94-11 counting gate parses exactly that shape, and the counter is positive-controlled against a synthetic row BEFORE first use (C9a: an artifact whose only format check lives downstream is an unverified contract)'
    - "Both audit register corrections land with derivations inline, in the same commit as this plan's repair work (RULING-P94-04 cross-cutting order 1)"
  artifacts:
    - path: '.planning/phases/94-write-paths/94-DEPLOY-LEDGER.md'
      provides: 'the deploy evidence 94-11 counts — verdict-last rows, one per function'
      contains: '| OK |'
    - path: 'scripts/probe-audit-row.mjs'
      provides: "the representative row-landing oracle (Wave 0): exercise a repaired deployed fn, assert audit_logs count delta ≥ 1 and the row's shape"
      contains: 'audit_logs'
  key_links:
    - from: 'supabase/functions/assign-role/index.ts'
      to: 'supabase/functions/_shared/audit.ts'
      via: 'representative precondition-grade site: { ok: false } from writeAuditLog fails the action with a sanitized 500'
      pattern: '_shared/audit'
    - from: '.planning/phases/94-write-paths/94-DEPLOY-LEDGER.md'
      to: '.planning/phases/94-write-paths/94-11-PLAN.md'
      via: 'the closing gate counts verdict-last OK rows — format is a cross-plan contract, positive-controlled here'
      pattern: 'OK'
---

<objective>
Close AUDIT-ZERO-01's fleet half: repair the 27 broken edge audit writers against the 94-06 helper,
grade the five privileged mutations precondition-level (PARK-94-08 (a)), redeploy every one
(D-19), prove a real row lands, and correct both audit register entries where they were filed.

Output: 27 repaired + deployed functions, the deploy ledger, the row-landing probe, two register
corrections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-write-paths/94-RESEARCH.md  (§7 — the 27-name broken list, the bad-key classes, the RLS posture)
@.tickmarkr/overseer/RULING-P94-04-RESEARCH-PARKS.md  (§PARK-94-08 + cross-cutting order 1)

<interfaces>
From research (re-read at execution):
- The 27 broken files are exactly the files_modified list above (research §7's BROKEN row —
  re-verify each still matches `audit_logs` with an insert before editing; a file repaired in the
  meantime is recorded, not re-edited). The CLEAN 9 (intake-tickets-* family, intake-classification,
  auth-verify-step-up, delegate-permissions, revoke-delegation) and the 2 read-only files are NOT
  touched.
- Helper contract (94-06): writeAuditLog(client, entry) → { ok } | { ok: false, error }; entry
  requires user_id (JWT clients may only insert their own uid — RLS) and user_role (NOT NULL).
  Functions using supabaseAdmin pass unconditionally; JWT-scoped clients must pass the caller's
  uid. user_role: use the role the function already resolved for authorization where present;
  otherwise fetch from public.users.
- Precondition-grade subset BY NAME (fails the action on { ok: false }, returning a sanitized
  5xx — no internals in the response body): assign-role, create-user, deactivate-user,
  approve-role-change, certify-user-access. All 22 others: log-and-continue (the helper already
  console.errors loudly).
- Bad-key classes to eliminate (research per-file table): event_type/resource_type/resource_id/
  target_user_id/metadata/changes/after (the 20-file class), details (5 files), actor_id (2),
  changed_by, assignee_id/override_reason/wip_status/capacity_warning (assignments-manual-override)
  — fold the semantic content into old_values/new_values jsonb or the real columns.
- Deploy: `supabase functions deploy <name>` per function (CLI authed, 2.106.0); the P92
  precedent. scripts/probe-edge-auth.sh gives one `fn -> status` line per function for
  deployed-artifact evidence.
- Representative for the row-landing probe: commitments-update-status against a NAMESPACED
  fixture commitment (create → update via the fn with a TEST_USER JWT → delete the fixture; the
  audit row stays — it is a genuine record of a genuine staging action). If that fn's contract
  makes this awkward, choose another BENIGN repaired fn and record the substitution + reason.
  Never exercise assign-role/create-user/deactivate-user against staging for probe purposes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair the 27 writers against the shared helper, grades stated per site</name>
  <files>the 27 supabase/functions/*/index.ts files enumerated in frontmatter</files>
  <read_first>supabase/functions/_shared/audit.ts (the 94-06 contract); supabase/functions/assign-role/index.ts:230-260 (the representative shape mismatch); three more of the 27 before starting, to see the payload variants</read_first>
  <action>
    For each of the 27: replace the inline audit_logs insert with a writeAuditLog call mapping
    the site's semantic payload onto the REAL columns (entity_type = the audited domain,
    entity_id = the affected resource id, action, user_id, user_role, old_values/new_values for
    the former metadata/changes/details content). For the five NAMED precondition sites: on
    { ok: false }, the function returns a sanitized failure (5xx, generic body — the honest
    tension is that the mutation fails because auditing failed; that is the intended trade,
    stated in a one-line comment at each of the five sites). For the other 22: the helper's loud
    console.error is the surfacing; execution continues. Do not otherwise refactor the functions
    (surgical — every changed line traces to the audit write). Work through the list in
    frontmatter order; commit in batches if needed but every file lands.
  </action>
  <verify>
    <automated>MISSING=0; for f in activate-account approve-role-change assign-role assignments-manual-override attachments-delete auth-biometric-setup auth-refresh-token auth-step-up-complete auth-step-up-initiate certify-user-access commitments-update-status complete-access-review create-user deactivate-user engagements-positions-attach engagements-positions-detach generate-access-review inactive-users initiate-password-reset notifications-register-device positions-unpublish push-device-register reactivate-user reset-password schedule-access-review setup-mfa verify-mfa-setup; do test -f "supabase/functions/$f/index.ts" || { echo "NO FILE $f"; exit 1; }; grep -q '_shared/audit' "supabase/functions/$f/index.ts" || { echo "NOT REPAIRED $f"; MISSING=1; }; done; test "$MISSING" -eq 0</automated>
  </verify>
  <acceptance_criteria>All 27 import the shared helper (file existence asserted before each grep — C5); the five precondition sites fail-the-action with sanitized bodies; the 22 continue-sites rely on the helper's loud error; no known bad key remains in any repaired audit payload (re-run of the key-diff derivation is 94-11's closing check; spot-check here).</acceptance_criteria>
  <done>The fleet writes a shape the table can accept, with the grade of every site decided and visible.</done>
</task>

<task type="auto">
  <name>Task 2: The deploy round, the ledger, and the row-landing proof</name>
  <files>.planning/phases/94-write-paths/94-DEPLOY-LEDGER.md, scripts/probe-audit-row.mjs</files>
  <read_first>scripts/probe-edge-auth.sh; GATE-STANDARD.md §C9a (the 92-04 ledger-format instance — the exact failure this task's format rule prevents)</read_first>
  <action>
    (1) Deploy all 27 via supabase CLI, one `supabase functions deploy <name>` each; after each,
    append a ledger row in EXACTLY the shape `| <fn> | <ISO timestamp> | OK |` (verdict cell
    LAST — the 94-11 counting gate greps verdict-last; C9a instance 92-04 is what happens
    otherwise). A failed deploy records `| <fn> | <ts> | FAIL |` and is retried; the plan does
    not close with a FAIL row. BEFORE writing real rows, positive-control the counter: add a
    synthetic `| synthetic-control | 2026-01-01T00:00:00Z | OK |` row, run
    `grep -cE '\| *OK *\|[[:space:]]*$' <ledger>` , confirm it counts 1, then delete the row and
    record the control in the ledger header comment.
    (2) Run scripts/probe-edge-auth.sh over all 27 names and paste the output into the ledger
    (deployed-artifact evidence: every fn responds, none 404).
    (3) Write scripts/probe-audit-row.mjs: service-role count of audit_logs BEFORE; exercise the
    representative (commitments-update-status on a namespaced fixture commitment with a TEST_USER
    JWT, per the interfaces block); count AFTER — delta ≥ 1; fetch the new row and assert its
    columns are exactly real ones (entity_type/entity_id/action/user_id/user_role present);
    delete the fixture commitment (NOT the audit row); print ids. Env absent → exit 2 (C2).
  </action>
  <verify>
    <automated>L=.planning/phases/94-write-paths/94-DEPLOY-LEDGER.md && test -f "$L" && test "$(grep -cE '\| *OK *\|[[:space:]]*$' "$L")" -ge 27 && test "$(grep -cE '\| *FAIL *\|[[:space:]]*$' "$L")" -eq 0 && grep -q 'probe-edge-auth' "$L" && test -f scripts/probe-audit-row.mjs && node scripts/probe-audit-row.mjs</automated>
  </verify>
  <acceptance_criteria>Ledger holds ≥27 verdict-last OK rows and zero FAIL rows (threshold derived from the 27-name enumeration in this plan — max achievable 27 ≥ 27, C4), with the positive-control recorded and the probe output pasted; the row-landing probe exits 0 with audit_logs 0 → ≥1 and a real-shape row.</acceptance_criteria>
  <done>The deployed fleet writes, and the first real audit row in the table's history is on the record.</done>
</task>

<task type="auto">
  <name>Task 3: Correct both audit register entries (same commit as this plan's work)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md §AUDIT-DROP-01 and §AUDIT-ZERO-01 in full; 94-RESEARCH.md §7 (the derivations to inline)</read_first>
  <action>
    STAGE AND COMMIT WITH THIS PLAN's repair work (RULING-P94-04 cross-cutting order 1).
    (1) AUDIT-ZERO-01: replace the "20 of 32" population with the re-derived truth, derivation
    inline: files under supabase/functions matching from('audit_logs') OR from("audit_logs") —
    the filed derivation was single-quote-only, a live instrument bias — giving 38 files,
    36 writers, 27 broken, 9 clean, 2 read-only. The corrected entry STATES the two-quote-style
    population rule, names the backend writer OUTSIDE both filed populations
    (backend/src/services/mou.service.ts:636, key `changes`, no user_role — repaired by 94-06),
    and carries the blind spots forward: variable-assembled spread payloads outside the
    literal-object scan; RPC searched, none; upsert included, zero.
    (2) AUDIT-DROP-01: state that the filed fix ("map resource_type→entity_type,
    details→additional_context") was INCOMPLETE — the insert still fails on tenant_id and
    entity_id (both NOT NULL, no defaults; the only prior successful writers are DB triggers
    taking NEW.tenant_id from the audited row), with the NOT-NULL derivation inline and the A3
    resolution (tenant = COALESCE(profiles.organization_id, users.default_organization_id); no
    sentinel; unresolvable = loud skip). Include the literal tokens `38 files` and
    `default_organization_id` (gates key on them; both verified absent from the file today).
    Touch nothing else — this is the phase's LAST planned REQUIREMENTS.md edit before close.
  </action>
  <verify>
    <automated>grep -q '38 files' .planning/REQUIREMENTS.md && grep -q 'default_organization_id' .planning/REQUIREMENTS.md && git log -1 --name-only --pretty=format: | grep -q 'REQUIREMENTS.md'</automated>
  </verify>
  <acceptance_criteria>Both entries corrected with derivations inline and marker tokens present; landed in the same commit as repair work from this plan.</acceptance_criteria>
  <done>The register's audit story matches the measured world.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                          |
| ----------------------------- | ---------------------------------------------------- |
| edge fn → audit_logs          | JWT clients insert only their own user_id (RLS)      |
| audit failure → user response | precondition sites fail sanitized; no internals leak |

## STRIDE Threat Register

| Threat ID | Category               | Component                   | Disposition | Mitigation Plan                                                                                                                                  |
| --------- | ---------------------- | --------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-94-17   | Repudiation            | 27 edge audit writers       | mitigate    | real column shape via the shared helper; five privileged mutations fail without an audit record (the intended trade, stated)                     |
| T-94-18   | Information Disclosure | precondition failure bodies | mitigate    | sanitized generic 5xx — the audit error text never reaches the client                                                                            |
| T-94-19   | Denial of Service      | precondition grading        | accept      | an audit-table outage blocks the five privileged mutations — accepted per PARK-94-08 (a): a role grant with no audit record is the worse failure |
| T-94-SC   | Tampering              | package installs            | accept      | zero installs this phase                                                                                                                         |

</threat_model>

<verification>
- 27/27 helper imports; ledger ≥27 verdict-last OKs with positive-controlled counter + probe paste.
- Row-landing probe green (0 → ≥1 with real shape).
- Register corrections with marker tokens, same commit.
</verification>

<success_criteria>
AUDIT-ZERO-01 closes on deployed behaviour: the fleet writes a real shape, the first row exists,
failures are loud everywhere and fatal exactly where ruled, and the register tells the measured
story.
</success_criteria>

<output>
Create `.planning/phases/94-write-paths/94-10-SUMMARY.md` when done.
</output>
