---
phase: 94-write-paths
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/audit.ts
  - backend/src/services/auth.service.ts
  - backend/src/services/mou.service.ts
  - backend/tests/unit/audit-write.test.ts
autonomous: true
requirements: [AUDIT-DROP-01, AUDIT-ZERO-01]
truths:
  - "Decisions covered — D-17: BOTH column maps are re-derived against live staging BEFORE any helper is written, and there are TWO helpers, not one — audit_log (singular, backend, tenant_id question) and audit_logs (plural, edge, user_role/RLS question) are different tables in different runtimes and the plan does not assume they reconcile; D-18: the insert error is SURFACED — reaching a log with the failure distinguishable from success; the two backend sites are graded log-loudly-and-continue (stated per site below; the precondition-grade EDGE subset is Plan 94-10's, enumerated by name there)"
must_haves:
  truths:
    - 'The edge helper supabase/functions/_shared/audit.ts exists with the REAL audit_logs column set, takes user_id (RLS: a JWT-scoped client may only insert user_id = auth.uid()) and NOT-NULL user_role, returns { ok: true } | { ok: false, error } and never throws — the contract every 94-10 repair consumes'
    - "logSecurityEvent (auth.service.ts:846-856) inserts the REAL audit_log columns: entity_type 'security', entity_id = the affected resource/subject id, additional_context = details, tenant_id = COALESCE(profiles.organization_id, users.default_organization_id) for the acting user (RULING-P94-04 A3); NO SENTINEL TENANT is invented — unresolvable tenant means the insert is SKIPPED with a loud winston error naming the skip"
    - 'mou.service.ts:636-645 (logStateTransition) writes the real audit_logs (plural) shape — the non-column key is replaced, user_role supplied, the insert error destructured and logged distinguishably; graded log-loudly-and-continue (an MoU state transition does not fail because auditing failed — stated, per D-18)'
    - 'The new test lives under backend/tests/ — backend/src/**/__tests__/ sits outside every vitest include glob and NEVER RUNS (a non-oracle)'
  artifacts:
    - path: 'supabase/functions/_shared/audit.ts'
      provides: 'the edge audit-write contract for the 27 repairs in 94-10'
      exports: ['writeAuditLog']
    - path: 'backend/tests/unit/audit-write.test.ts'
      provides: 'the AUDIT-DROP-01 behavioural oracle: payload keys ⊆ live column set; failure path logs distinguishably; unresolvable tenant skips loudly'
      contains: 'tenant_id'
  key_links:
    - from: 'backend/src/services/auth.service.ts'
      to: 'backend/tests/unit/audit-write.test.ts'
      via: 'the test drives logSecurityEvent with a mocked supabaseAdmin and asserts the exact insert payload keys + the surfaced-failure and skipped-tenant branches'
      pattern: 'logSecurityEvent'
    - from: 'supabase/functions/_shared/audit.ts'
      to: 'supabase/functions/_shared/audit.ts'
      via: 'result-union contract ({ ok } | { ok: false, error }) — callers decide precondition vs continue; the helper never throws'
      pattern: 'ok: false'
---

<objective>
Author the two audit-write foundations. `public.audit_log` (singular, backend Express) has never
recorded a security event — `auth.service.ts:846` inserts two non-columns and swallows the error;
the filed fix is INCOMPLETE (still fails on NOT NULL tenant_id + entity_id). `public.audit_logs`
(plural, edge) holds 0 rows against 36 writers — this plan ships the shared helper; Plan 94-10
repairs and redeploys the 27 broken functions against it. One backend writer sits outside both
filed populations: `mou.service.ts:636` writes audit_logs (plural) from the backend with a
non-column key.

Output: edge helper + two backend repairs + the backend/tests oracle. (Register corrections for
both AUDIT entries ride with 94-10 — REQUIREMENTS.md edits are wave-serialized.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-write-paths/94-RESEARCH.md  (§7 AUDIT — the re-derived column sets, RLS postures, and the mou.service finding)
@.tickmarkr/overseer/RULING-P94-04-RESEARCH-PARKS.md  (A3: derive tenant, never invent one; unresolvable = loud skip)

<interfaces>
Verified by research (RE-DERIVE at execution — D-17 makes this mandatory, not advisory):
- audit_log (singular): id(default), tenant_id uuid NOT NULL(no default), entity_type NOT NULL,
  entity_id uuid NOT NULL, action NOT NULL, user_id NOT NULL, timestamp(default now()),
  old_values, new_values, ip_address, user_agent, session_id, additional_context. 75 rows (all
  trigger-written). Writer is supabaseAdmin (service role) → RLS not a factor.
- audit_logs (plural): id, entity_type NOT NULL, entity_id NOT NULL, action NOT NULL, old_values,
  new_values, user_id NOT NULL, user_role NOT NULL(no default), ip_address, user_agent,
  required_mfa(def false), mfa_verified(def false), mfa_method, correlation_id, session_id,
  created_at(def now()). 0 rows. RLS: INSERT allows service_role unconditionally OR
  user_id = auth.uid() for JWT clients — the helper must take user_id as the caller's uid.
- The broken backend site verbatim: auth.service.ts:846-856 inserts { user_id, action,
  resource_type, details, timestamp } in a try/catch that only logError-s.
- Tenant derivation (A3): the acting user's COALESCE(profiles.organization_id,
  users.default_organization_id). profiles has NO id column — join profiles by user_id
  (profiles.user_id = <uid>; the clearance-RLS lesson). No sentinel tenant. Unresolvable →
  SKIP the insert + winston.error with a distinguishable marker (e.g. 'AUDIT-DROP-01 skip').
- mou.service.ts:636-645 logStateTransition: inserts audit_logs with key `changes` (not a
  column) and no user_role; awaited without destructuring error. mous.ts:419 is a SELECT — leave.
  link.service.ts writes link_audit_logs — a DIFFERENT table, outside this phase (stated).
- backend vitest UNIT config includes tests/unit/**/*.test.ts (required CI) — the new test path
  backend/tests/unit/audit-write.test.ts IS picked up. Colocated backend/src/**/__tests__ never
  runs (its shipped auth.service.test.ts is a named NON-ORACLE).
- No shared audit helper exists in supabase/functions/_shared/ today (16 helpers, none audit).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive both column maps live, then author the edge helper (interface-first)</name>
  <files>supabase/functions/_shared/audit.ts</files>
  <read_first>supabase/functions/_shared/ directory listing + one existing helper for the house Deno style; supabase/functions/assign-role/index.ts:230-260 (the representative broken call the helper replaces in 94-10)</read_first>
  <action>
    FIRST (D-17): via Supabase MCP execute_sql, re-derive BOTH column sets:
    SELECT table_name, column_name, is_nullable, column_default FROM information_schema.columns
    WHERE table_schema='public' AND table_name IN ('audit_log','audit_logs') ORDER BY 1, ordinal_position;
    Paste into the SUMMARY. If either set differs from the interfaces block, adapt to the LIVE set
    and flag the drift.
    Then author `_shared/audit.ts`: export `writeAuditLog(client, entry)` where entry carries
    entity_type, entity_id, action, user_id, user_role (required — NOT NULL, no default),
    old_values?, new_values?, plus the optional real columns (ip_address, user_agent, mfa fields,
    correlation_id, session_id). The helper inserts EXACTLY real column names, destructures
    { error }, and returns { ok: true } | { ok: false, error: <message string> } — NEVER throws
    (result-union law; callers decide precondition vs log-and-continue, which is 94-10's per-site
    call). On failure it also console.error-s a structured line
    (`AUDIT-ZERO-01 write failed: <fn context> <error>`) so a drop is loud even for
    log-and-continue callers. Header comment: the RLS posture (JWT clients may only insert their
    own user_id; service-role passes unconditionally) and the two-table warning (this helper is
    for audit_logs PLURAL only — audit_log singular is the backend's, different columns).
  </action>
  <verify>
    <automated>test -f supabase/functions/_shared/audit.ts && grep -q 'user_role' supabase/functions/_shared/audit.ts && grep -q 'ok: false' supabase/functions/_shared/audit.ts && test "$(grep -c 'resource_type\|event_type\|metadata:' supabase/functions/_shared/audit.ts)" -eq 0</automated>
  </verify>
  <acceptance_criteria>Live column derivation pasted in SUMMARY; helper exports the result-union contract with the real column set and none of the known bad keys; the two-table warning is in the header.</acceptance_criteria>
  <done>The contract 27 repairs will consume exists and matches the live table.</done>
</task>

<task type="auto">
  <name>Task 2: Repair the two backend writers (audit_log singular + the out-of-population audit_logs writer)</name>
  <files>backend/src/services/auth.service.ts, backend/src/services/mou.service.ts</files>
  <read_first>auth.service.ts:830-870; mou.service.ts:620-660; backend/src/utils/logger.ts (the winston surface)</read_first>
  <action>
    (1) logSecurityEvent: map to the REAL audit_log columns — entity_type: 'security',
    entity_id: the affected resource/subject user id (honest per A3), action, user_id (the acting
    user), additional_context: details, plus ip_address/user_agent/session_id where the call site
    has them. Derive tenant_id per A3: query profiles by user_id (profiles.user_id = uid — the
    table has NO id column; binding an outer id yields NULL and blocks everything) with fallback
    users.default_organization_id, via supabaseAdmin. NO sentinel tenant: if neither resolves,
    SKIP the insert and winston.error('AUDIT-DROP-01: audit insert SKIPPED — no tenant resolves
    for user <id>') — a stated visible gap, never fabricated data (A3). Stop swallowing: on
    insert error, winston.error with the PostgREST error message and a distinguishable marker;
    the surrounding auth action CONTINUES (log-loudly-and-continue — grade stated: backend
    security-event logging is observability, not a mutation precondition; the precondition-grade
    EDGE subset is 94-10's).
    (2) logStateTransition (mou.service.ts:636-645): replace the non-column `changes` key with
    new_values (and old_values if the prior state is at hand), supply user_role (fetch the acting
    user's role from public.users, or thread it from the caller if already present), destructure
    { error } and winston.error on failure with a marker; the transition continues (grade stated).
  </action>
  <verify>
    <automated>cd backend && grep -q "entity_type: 'security'" src/services/auth.service.ts && grep -q 'additional_context' src/services/auth.service.ts && grep -q 'tenant_id' src/services/auth.service.ts && grep -q 'default_organization_id' src/services/auth.service.ts && grep -q 'new_values' src/services/mou.service.ts && grep -q 'user_role' src/services/mou.service.ts && pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>Both writers name only real columns; the tenant COALESCE derivation is in code with the loud-skip branch; both insert errors are surfaced distinguishably; backend compiles. The honest tension is documented at the site: a skipped audit insert is logged as an ERROR, never silently absorbed.</acceptance_criteria>
  <done>No backend audit write can silently drop again.</done>
</task>

<task type="auto">
  <name>Task 3: The backend/tests oracle + live shape evidence</name>
  <files>backend/tests/unit/audit-write.test.ts</files>
  <read_first>backend/vitest.config.ts include globs; an existing backend/tests/unit test for the mock idiom</read_first>
  <action>
    Write backend/tests/unit/audit-write.test.ts driving the REAL logSecurityEvent with a mocked
    supabaseAdmin: (a) assert the insert payload's keys are a subset of the real audit_log column
    set (embed the derived list from Task 1's SUMMARY as the test's expected set, with a comment
    dating the derivation); (b) failure branch: insert resolves { error } → winston.error called
    with the distinguishable marker, function does not throw; (c) tenant-unresolvable branch:
    profiles+users lookups return null org → insert NOT called, loud skip logged. Then the LIVE
    half (evidence step, recorded in SUMMARY, not a gate): one service-role insert of a
    namespaced audit_log row using the exact payload shape (action 'p94-probe'), read back by id,
    then DELETE it — proving the shape lands on the live table; paste ids into the SUMMARY.
  </action>
  <verify>
    <automated>cd backend && pnpm exec vitest run tests/unit/audit-write.test.ts 2>&1 | grep -qE '[1-9][0-9]* passed'</automated>
  </verify>
  <acceptance_criteria>≥3 tests pass and the run is not "no test files found" (the passed-count grep guards that); the live shape evidence (insert → read-back → delete, ids pasted) is in the SUMMARY.</acceptance_criteria>
  <done>AUDIT-DROP-01 has a running oracle in a location that actually runs.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                   |
| -------------------------------- | ------------------------------------------------------------- |
| edge JWT client → audit_logs     | RLS permits only user_id = auth.uid() inserts                 |
| backend service role → audit_log | service role bypasses RLS — payload discipline is the control |

## STRIDE Threat Register

| Threat ID | Category    | Component              | Disposition | Mitigation Plan                                                                                           |
| --------- | ----------- | ---------------------- | ----------- | --------------------------------------------------------------------------------------------------------- |
| T-94-10   | Repudiation | security-event logging | mitigate    | inserts stop silently failing; failures logged at error level with markers; skip-branch loud (A3)         |
| T-94-11   | Tampering   | tenant derivation      | mitigate    | tenant derived from the acting user's own org columns, never invented; unresolvable = skip, not fabricate |
| T-94-SC   | Tampering   | package installs       | accept      | zero installs this phase                                                                                  |

</threat_model>

<verification>
- Live column derivations pasted (both tables); helper contract gates; backend test green; live
  shape evidence recorded and cleaned.
- The 27-function repair, the deploy round, the representative row-landing probe, and both
  register corrections are Plan 94-10's (producer→consumer, D-25; REQUIREMENTS.md wave-serialized).
</verification>

<success_criteria>
Both audit sinks have a correct, loud write path authored against the LIVE schema, with the
backend halves repaired and proven; the edge fleet repair consumes this plan's helper.
</success_criteria>

<output>
Create `.planning/phases/94-write-paths/94-06-SUMMARY.md` when done.
</output>
