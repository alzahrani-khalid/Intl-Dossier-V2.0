---
phase: 94-write-paths
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260816500001_p94_report_rls_recursion.sql
  - scripts/probe-report-rls.mjs
  - tests/e2e/93-report-notfound.spec.ts
autonomous: true
requirements: [WRITE-06, ARMA-01]
truths:
  - 'Decisions covered — D-02: ARMA-01 rides INSIDE this WRITE-06 plan as its LAST task, ordered strictly AFTER the 42P17 migration (before the fix, the tightened spec reds for the right reason at the wrong time); D-21: the 42P17 fix is ONE migration file applied through the Supabase MCP, never ad-hoc DDL; D-22: the fix is proven TWO-SIDED — no 42P17 AND the visible row set is not widened, asserted by a probe with two minted identities (a one-sided oracle is what D-22 forbids); D-26: the ARMA spec gate asserts file existence FIRST and hardcodes 1 passed; D-27: the spec keeps its inline auth and runs --no-deps — no dependence on the e2e setup project; D-34: the report_schedules trigger derives next_run_at and an INACTIVE schedule legitimately has it NULL — no oracle here asserts non-null'
must_haves:
  truths:
    - 'After the migration, an authenticated read of custom_reports returns rows (or an empty set) — never 42P17 — and the recursion is broken by construction: report_shares policies no longer reference custom_reports'
    - 'D-22 two-sided: user A sees only own+shared rows, user B sees exactly the row shared with them, user C sees NONE — proven by a probe that mints identities with the service-role key (PARK-94-07 ruled (a)), uses NAMESPACED fixtures, PRINTS what it acted on, and CLEANS UP (both tables are live-empty — leave them so)'
    - 'A scheduled report can be created against a real custom_reports row (criterion 5, the REAL surface unblocked by the fix)'
    - 'tests/e2e/93-report-notfound.spec.ts asserts the 404 arm ALONE — arm (b) (the query-error-state disjunct, lines 74-90 + header lines 17-21/27-28) is DELETED, and the tightened spec passes 1/1'
  artifacts:
    - path: 'supabase/migrations/20260816500001_p94_report_rls_recursion.sql'
      provides: "is_report_owner SECURITY DEFINER helper + report_shares policy rewrite — the phase's only schema-level change"
      contains: 'SECURITY DEFINER'
    - path: 'scripts/probe-report-rls.mjs'
      provides: 'the D-22 two-sided visibility oracle (Wave 0 gap closed)'
      contains: 'is_report_owner'
    - path: 'tests/e2e/93-report-notfound.spec.ts'
      provides: 'the ARMA-01 tightened 404-only spec'
      contains: 'notFoundPage'
  key_links:
    - from: 'scripts/probe-report-rls.mjs'
      to: 'supabase/migrations/20260816500001_p94_report_rls_recursion.sql'
      via: 'probe consumes the deployed policy state the migration creates — producer before consumer within this plan'
      pattern: 'custom_reports'
    - from: 'tests/e2e/93-report-notfound.spec.ts'
      to: 'supabase/migrations/20260816500001_p94_report_rls_recursion.sql'
      via: 'the 404 arm can only fire once the by-id read stops rejecting — task order enforces D-02'
      pattern: '404'
---

<objective>
Close WRITE-06's policy half and ARMA-01. The live recursion (research §6, full policy text):
`custom_reports` SELECT → EXISTS over `report_shares` → its SELECT policy → EXISTS over
`custom_reports` → 42P17. Break ONE direction with a SECURITY DEFINER owner-check helper
(the get_user_clearance_level precedent), prove it two-sided (D-22 — the phase's sharpest security
risk), then delete the report-notfound spec's arm (b) and assert 404 alone (D-02).

The generate-surface half of WRITE-06 (template→type + terminal state) is Plan 94-09's.
Output: one migration + the D-22 probe + the tightened spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-write-paths/94-RESEARCH.md  (§6 WRITE-06 — the exact live policy texts, the cycle, the three candidate fixes; §9 ARMA-01 — the exact lines of arm (b))
@.tickmarkr/overseer/RULING-P94-04-RESEARCH-PARKS.md  (PARK-94-07 → (a): mint two identities; fixtures namespaced and cleaned; the probe prints what it acted on)

<interfaces>
Verified by research (re-derive pg_policies at execution — policy text is volatile):
- The recursive quals, live 2026-08-16: custom_reports SELECT has an EXISTS over report_shares;
  report_shares SELECT/INSERT-WITH-CHECK/DELETE each EXISTS over custom_reports; custom_reports
  UPDATE references report_shares; report_executions SELECT/INSERT reference custom_reports.
- The RECOMMENDED shape (judged against D-22 in research; candidate 1): create
  `public.is_report_owner(p_report_id uuid) RETURNS boolean SECURITY DEFINER STABLE
  SET search_path = ''` with body `SELECT created_by = auth.uid() FROM public.custom_reports
  WHERE id = p_report_id` — the definer reads ONE row's created_by and compares to the caller:
  semantically identical to the current predicate, so the row set is unchanged by construction.
  Replace the EXISTS-over-custom_reports inside report_shares' SELECT, INSERT WITH CHECK, and
  DELETE quals with is_report_owner(report_id). Once report_shares no longer references
  custom_reports, every evaluation path terminates (custom_reports → report_shares → helper →
  done; report_executions → custom_reports → report_shares → helper → done).
- Candidate 2 (delete the owner-EXISTS) is REFUSED here: invariant-dependent and does not fix the
  UPDATE/executions paths. Candidate 3 (broad definer view) is REFUSED by D-22.
- ARMA-01 arm (b), exact (research §9, re-read the file first): header lines 11-28 (specifically
  17-21 and the standing instruction 27-28); line 74 queryErrorState locator; lines 77-82 the
  expect.poll sum; lines 85-90 the arm-recording block. KEEP: line 65 (crypto.randomUUID absent
  id), line 73 (notFoundPage locator), line 93 (builderHeading conjunct), the inline auth
  (lines 36-55, D-27-compliant), RETRY_BACKOFF with an updated comment.
- Machine facts for the gates: no `timeout` binary on this machine; never 2>/dev/null; capture
  exit codes directly, never through a pipe.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The recursion migration, applied via MCP, re-derived after</name>
  <files>supabase/migrations/20260816500001_p94_report_rls_recursion.sql</files>
  <read_first>94-RESEARCH.md §6 (the full live policy texts — the DROP/CREATE statements must reproduce every non-recursive clause verbatim); supabase/migrations/20260614000001_p68_clearance_canonical.sql (the SECURITY DEFINER precedent)</read_first>
  <action>
    FIRST re-derive the live policy texts via Supabase MCP execute_sql (SELECT policyname, cmd,
    qual, with_check FROM pg_policies WHERE tablename IN ('custom_reports','report_shares',
    'report_executions')) — research's copies are evidence, not a substitute (D-17-class law).
    Write ONE migration file: create is_report_owner per the interfaces block; DROP and re-CREATE
    the three report_shares policies with the EXISTS-over-custom_reports replaced by
    is_report_owner(report_id), preserving every other clause byte-faithfully (shared_with =
    auth.uid(), shared_by = auth.uid(), the INSERT WITH CHECK's shared_by-must-be-owner form).
    Do NOT touch custom_reports' or report_executions' policies — with report_shares acyclic they
    terminate. Grant EXECUTE on the helper to authenticated. Apply via Supabase MCP
    apply_migration (D-21 — never execute_sql for DDL). AFTER applying, re-derive pg_policies and
    paste the new report_shares quals into the SUMMARY (point-in-time MCP evidence, D-23-style
    label), confirming zero references to custom_reports remain in report_shares' quals.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260816500001_p94_report_rls_recursion.sql && grep -q 'SECURITY DEFINER' supabase/migrations/20260816500001_p94_report_rls_recursion.sql && grep -q "search_path" supabase/migrations/20260816500001_p94_report_rls_recursion.sql && grep -q 'is_report_owner' supabase/migrations/20260816500001_p94_report_rls_recursion.sql</automated>
  </verify>
  <acceptance_criteria>Migration file exists with the pinned-search_path definer helper; applied to staging via MCP (evidence: apply_migration result + post-apply pg_policies paste in SUMMARY showing report_shares quals free of custom_reports references). The migration is the phase's ONLY schema-level change.</acceptance_criteria>
  <done>The policy graph is acyclic and the change is a tracked migration.</done>
</task>

<task type="auto">
  <name>Task 2: The D-22 two-sided probe + the scheduled-report creation oracle</name>
  <files>scripts/probe-report-rls.mjs</files>
  <read_first>scripts/probe-edge-auth.sh (the house probe hygiene: never echo secrets); 94-RESEARCH.md §6 (report_schedules policies self-contained; F4 next_run_at caveat)</read_first>
  <action>
    Write scripts/probe-report-rls.mjs (node, no new deps — use fetch). Env: SUPABASE_URL +
    SUPABASE_SERVICE_ROLE_KEY (+ anon key for user-JWT calls) read from env/.env.test-style
    sources; if absent, print UNABLE TO MEASURE and exit 2 — a missing credential is not a red
    (GATE-STANDARD C2). Steps, printing every id it acts on:
    (1) Service-role: create THREE namespaced auth users (emails like p94-probe-{a,b,c}@probe.invalid)
    via the admin API with known passwords; sign each in for a user JWT.
    (2) As A (user JWT through PostgREST): INSERT a custom_reports row (namespaced name
    'p94-probe-report') — this exercises the real INSERT policy; then INSERT a report_shares row
    sharing it to B — exercising the rewritten WITH CHECK (is_report_owner path).
    (3) Reads: A SELECTs custom_reports → exactly 1 (their own); B → exactly 1 (the shared one);
    C → exactly 0. ANY 42P17 (code in the response body) → hard fail printing the body. This is
    the two-sided assertion: error-absence alone is what D-22 forbids.
    (4) Criterion-5 surface: as A, INSERT a report_schedules row referencing the report
    (is_active false is fine); assert the row lands and is readable by A. Do NOT assert
    next_run_at is non-null — the BEFORE trigger derives it and NULL is legitimate for an
    inactive schedule (D-34/F4).
    (5) CLEANUP in reverse order: schedule, share, report, then the three auth users. Re-assert
    custom_reports and report_shares row counts are 0 (service-role count) — the tables were
    live-empty and must be left so. Exit 0 only if every assertion held AND cleanup verified.
    Never print any key or JWT.
  </action>
  <verify>
    <automated>test -f scripts/probe-report-rls.mjs && node scripts/probe-report-rls.mjs</automated>
  </verify>
  <acceptance_criteria>Probe exits 0 with the printed ledger of ids: A=1/B=1/C=0 visibility, no 42P17 anywhere, schedule row created (no next_run_at assertion), fixtures cleaned to 0/0. If run before Task 1's migration it must FAIL with 42P17 — record that RED in the SUMMARY if observed (C1 both directions).</acceptance_criteria>
  <done>The fix is proven to remove the error WITHOUT widening the row set, and the real scheduling surface works.</done>
</task>

<task type="auto">
  <name>Task 3: ARMA-01 — delete arm (b), assert the 404 arm alone (strictly after the migration)</name>
  <files>tests/e2e/93-report-notfound.spec.ts</files>
  <read_first>tests/e2e/93-report-notfound.spec.ts in full (96 lines — the arm map in the interfaces block must be re-verified against the file before editing)</read_first>
  <action>
    ORDERED AFTER Tasks 1-2 (D-02 — before the fix this edit reds the spec for the right reason at
    the wrong time). Delete arm (b) exactly: the queryErrorState locator (line 74), the
    expect.poll disjunction (77-82), the arm-recording block (85-90), and the header's arm-(b)
    definition + standing instruction (17-21, 27-28). Replace with a direct
    `await expect(notFoundPage).toBeVisible(...)` (or a poll on notFoundPage.count() alone).
    Update the header comment to state the disjunction was tightened by Phase 94 after the 42P17
    fix. The string `query-error-state` must not appear ANYWHERE in the file afterwards — code or
    comment (the gate's negative grep depends on it; C8 handled by this instruction). KEEP: the
    absent-id randomUUID, the notFoundPage locator, the unconditional builderHeading conjunct, the
    inline auth, and RETRY_BACKOFF with a one-line updated rationale (a lenient budget is harmless
    for a slow 404).
  </action>
  <verify>
    <automated>test -f tests/e2e/93-report-notfound.spec.ts && test "$(grep -c 'query-error-state' tests/e2e/93-report-notfound.spec.ts)" -eq 0 && OUT=$(pnpm exec playwright test tests/e2e/93-report-notfound.spec.ts --project=chromium-en --no-deps 2>&1) && echo "$OUT" | grep -qE '(^|[^0-9])1 passed'</automated>
  </verify>
  <acceptance_criteria>Spec exists (asserted first — paths are filters), contains zero references to the deleted arm, and passes exactly 1/1 under --no-deps (count hardcoded per D-26; the spec keeps its inline auth per D-27). The green is now unfakeable: only a real 404 render passes.</acceptance_criteria>
  <done>The report-builder 404 path has a persisting, single-arm test, and the permanent-false-green trap is dismantled.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                        |
| ---------------------------- | ------------------------------------------------------------------ |
| SECURITY DEFINER helper      | bypasses RLS by construction — scope must stay one-row owner-check |
| service-role probe → staging | admin credentials mint identities and must clean up                |

## STRIDE Threat Register

| Threat ID | Category               | Component              | Disposition | Mitigation Plan                                                                                                                                                               |
| --------- | ---------------------- | ---------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-94-08   | Information Disclosure | is_report_owner helper | mitigate    | STABLE, SET search_path='', reads ONE row's created_by and compares to auth.uid() — no data returned to caller beyond a boolean; D-22 probe asserts the row set did not widen |
| T-94-09   | Elevation of Privilege | probe fixtures         | mitigate    | namespaced probe users deleted at end; both report tables verified back to 0 rows; no secret ever printed                                                                     |
| T-94-SC   | Tampering              | package installs       | accept      | zero installs this phase                                                                                                                                                      |

</threat_model>

<verification>
- Migration file gates + MCP apply evidence + post-apply pg_policies paste (report_shares acyclic).
- Probe exit 0 with A/B/C = 1/1/0, no 42P17, cleanup verified.
- Tightened spec 1 passed under --no-deps, zero arm-(b) residue.
</verification>

<success_criteria>
Criterion 5's report half holds on its REAL surfaces: report reads and scheduled-report creation
work without 42P17, visibility is proven not-widened, and ARMA-01's deletion makes the 404 spec's
green mean what it says.
</success_criteria>

<output>
Create `.planning/phases/94-write-paths/94-05-SUMMARY.md` when done.
</output>
