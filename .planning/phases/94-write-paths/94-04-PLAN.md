---
phase: 94-write-paths
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/settings/SettingsPage.tsx
  - frontend/tests/e2e/settings-save.spec.ts
autonomous: true
requirements: [WRITE-05]
truths:
  - 'Decisions covered — D-14: the users write becomes .update(…).eq(id, user.id) — NOT an upsert with email added, which would "work" while writing an auth-owned column from the client; D-15: the notification bridge (applySettingsTogglesToCategoryPrefs) is unreachable, not broken — it is NOT rewritten, and the acceptance criterion observes the bridge EFFECT (a toggle round-trips through reload), not merely that Save stops erroring; D-16: the "every /settings tab" population is DERIVED and STATED — Population A (the nine SettingsPage sections through the one shared Save) is in-criterion; Population B (five child routes with their own save paths) is a NAMED EXCLUSION with reasoning; D-08: the save-failure toast drops the raw error.message description — settings:saveError renders alone'
must_haves:
  truths:
    - 'Save on /settings succeeds for an existing user row: profile/general fields write via UPDATE (passing the live users_update_self RLS policy), the notification bridge executes, and the values survive a full page reload'
    - 'Population statement (D-16): IN — Population A, the 9 SettingsPage sections whose fields flow through the one shared saveMutation (profile, general, notification toggles, localStorage-persisted fields; appearance applied client-side onSuccess). OUT, named with reasons — Population B child routes (calendar-sync, email-digest, integrations, notifications, webhooks: each has its OWN save path against live RLS-enabled tables and was not part of the filed defect — assumption A4, carried, not silently trusted); /settings/calendar/callback (no save affordance); security (MFA write deliberately absent, comment at :209-210); data-privacy (action buttons, not saved values)'
    - 'The mutation payload does NOT gain an email field — the fix is the verb, not the tuple'
    - 'A failed save renders settings:saveError alone; the raw error goes to console.error'
  artifacts:
    - path: 'frontend/src/pages/settings/SettingsPage.tsx'
      provides: 'the .update().eq() users write + D-08-clean onError'
      contains: ".eq('id'"
    - path: 'frontend/tests/e2e/settings-save.spec.ts'
      provides: 'the Wave 0 reload-persistence oracle (frontend Playwright config, real login)'
      contains: 'reload'
  key_links:
    - from: 'frontend/tests/e2e/settings-save.spec.ts'
      to: 'frontend/src/pages/settings/SettingsPage.tsx'
      via: 'spec drives the real Save, reloads, and asserts both the users field AND a notification toggle persisted (the D-15 bridge-effect observation)'
      pattern: 'settings'
---

<objective>
Close WRITE-05: every `/settings` tab (population stated) saves and survives reload. The one broken
path, verified live: `SettingsPage.tsx:211`'s `.upsert({...})` omits NOT NULL `email`, and PostgREST
upsert is INSERT…ON CONFLICT — the NOT NULL check fires before conflict resolution (23502), so every
Save fails, which also makes the correctly-written notification bridge (steps 2-3, :225-280)
unreachable. The file carries a repair comment for a DIFFERENT, already-fixed defect (:205-209) —
it looks fixed and is not.

Output: the .update() fix + D-08 toast repair + the settings-save reload-persistence spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-write-paths/94-RESEARCH.md  (§5 WRITE-05 — the population tables and the live RLS verification)
@.planning/phases/94-write-paths/94-UI-SPEC.md  (§Intake and settings — copy deltas only; zero new keys)

<interfaces>
Verified by research (re-read at execution):
- `SettingsPage.tsx:211` — the upsert (id, full_name, job_title_en, department, phone, avatar_url,
  language_preference, timezone, updated_at — no email). Live: users.email NOT NULL no default;
  INSERT RLS would also deny the client. The D-14 fix `.update({ …same fields minus id… }).eq('id',
  user.id)` passes the live users_update_self policy (auth.uid() = id, USING + WITH CHECK,
  verified in pg_policies).
- `:225-280` — steps 2-3 (applySettingsTogglesToCategoryPrefs bridge + writeLocalSettings) are
  CORRECT and unreachable. Do not rewrite them (D-15). notification_category_preferences exists
  live, RLS on, onConflict 'user_id,category' matches.
- `:314-320` — onError: `toast.error(t('saveError'), { description: detail ?? undefined })` where
  detail = error.message. Drop the description; raw stays in the existing console.error. The
  component uses `useTranslation('settings')`, so the bare `t('saveError')` already resolves the
  settings namespace — key exists both locales; zero new keys.
- Playwright: the FRONTEND config (frontend/playwright.config.ts, testDir frontend/tests) has a
  globalSetup that logs in with TEST_USER_* — this spec lives there and needs no inline auth and
  no --no-deps caveat (that is the ROOT config's disease; D-27 concerns the root `setup` project).
- Staging hygiene: the spec edits the TEST_USER's own row — write namespaced-but-plausible values
  (e.g. full_name suffixed with a timestamp), assert persistence after reload, then RESTORE the
  original values in the same spec before it ends.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The users write becomes UPDATE; the failure toast stops leaking</name>
  <files>frontend/src/pages/settings/SettingsPage.tsx</files>
  <read_first>SettingsPage.tsx:200-330 in full (the mutationFn's three steps, the misleading repair comment, the onError)</read_first>
  <action>
    (1) `:211` — replace `.upsert({ id, …fields })` with `.update({ …fields })` on
    `.eq('id', user.id)`. Do NOT add email to the payload (pitfall: adding email makes the upsert
    "work" while writing an auth-owned column from the client — refused by D-14's explicit shape).
    Keep the field list otherwise identical; keep steps 2-3 byte-identical (they are correct and
    will now finally run). (2) Update the stale repair comment at :205-210 so the next reader is
    not told the file is fixed for the wrong reason — one sentence naming the 23502/NOT-NULL-email
    upsert defect this commit removes. (3) `:314-320` — remove the raw description
    (`description: detail`); `t('saveError')` renders alone; error stays in console.error.
  </action>
  <verify>
    <automated>cd frontend && test "$(grep -v '^[[:space:]]*//' src/pages/settings/SettingsPage.tsx | grep -cE '\.upsert\(')" -eq 1 && grep -qE "\.eq\('id', *user\.id\)" src/pages/settings/SettingsPage.tsx && test "$(grep -c 'description: detail' src/pages/settings/SettingsPage.tsx)" -eq 0 && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>The users write is an UPDATE scoped to the caller's own row; exactly one non-comment .upsert( remains in the file (the bridge's category-prefs upsert at :245-266, which is correct — threshold derived: 2 sites today, the users one removed leaves 1); the failure toast renders the translated key alone; type-check clean.</acceptance_criteria>
  <done>Save reaches the database and a failed save says so without internals.</done>
</task>

<task type="auto">
  <name>Task 2: The settings-save reload-persistence spec (Wave 0)</name>
  <files>frontend/tests/e2e/settings-save.spec.ts</files>
  <read_first>frontend/playwright.config.ts (globalSetup + baseURL); frontend/tests/e2e/settings-page.spec.ts (the shipped layout spec — a NAMED non-consumer of the save path; do not duplicate its assertions); SettingsPage.tsx:358-415 (the section nav)</read_first>
  <action>
    Create the spec with EXACTLY 2 tests (the gate hardcodes the count, D-26):
    Test 1 "profile save survives reload": open /settings, change full_name to a timestamped
    value, Save, assert the success toast, page.reload(), assert the field shows the new value —
    the reload IS the read-back through the app (never "no error" as success). Restore the
    original value and Save again before the test ends (staging hygiene).
    Test 2 "notification toggle survives reload (the D-15 bridge effect)": flip one notification
    toggle, Save, reload, assert the toggle held — this observes the bridge writing
    notification_category_preferences, the criterion D-15 demands. Restore it after.
    State IN THE SPEC HEADER COMMENT the D-16 population this spec covers (Population A through
    the shared Save) and that Population B child routes are out with the A4 reasoning — the spec
    is the criterion's oracle, so the population statement lives where the oracle lives.
  </action>
  <verify>
    <automated>test -f frontend/tests/e2e/settings-save.spec.ts && cd frontend && OUT=$(pnpm exec playwright test tests/e2e/settings-save.spec.ts --reporter=line 2>&1) && echo "$OUT" | grep -qE '(^|[^0-9])2 passed'</automated>
  </verify>
  <acceptance_criteria>Spec file exists (existence asserted FIRST — paths are filters) and exactly 2 tests pass; both assert post-reload state; both restore staging values; the population statement is in the header.</acceptance_criteria>
  <done>WRITE-05's criterion has a behavioural oracle that a regression would turn red.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                   |
| -------------------- | --------------------------------------------- |
| client → users table | self-row UPDATE under users_update_self RLS   |
| error object → toast | PostgREST 23502 detail must not reach the DOM |

## STRIDE Threat Register

| Threat ID | Category               | Component          | Disposition | Mitigation Plan                                                                     |
| --------- | ---------------------- | ------------------ | ----------- | ----------------------------------------------------------------------------------- |
| T-94-06   | Elevation of Privilege | users write        | mitigate    | .update().eq('id', user.id) under existing RLS; email never written from the client |
| T-94-07   | Information Disclosure | save-failure toast | mitigate    | raw description removed; translated key alone; error to console.error (D-08)        |
| T-94-SC   | Tampering              | package installs   | accept      | zero installs this phase                                                            |

</threat_model>

<verification>
- Grep gates (one non-comment upsert left, .eq self-scope, no raw description) + type-check.
- The 2-test spec green: field persistence AND bridge effect, both post-reload.
- Population statement present in spec header; restated in 94-11's closing derivations.
</verification>

<success_criteria>
Criterion 5's first half holds for the stated population: Save succeeds, the bridge runs, values
survive reload — and what "every tab" excludes is written down, not assumed.
</success_criteria>

<output>
Create `.planning/phases/94-write-paths/94-04-SUMMARY.md` when done.
</output>
