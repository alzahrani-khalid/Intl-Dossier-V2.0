---
phase: 94-write-paths
plan: 07
type: execute
wave: 2
depends_on: [94-02]
files_modified:
  - supabase/functions/after-actions-list-all/index.ts
  - scripts/probe-after-actions-list.mjs
  - frontend/src/routes/_protected/after-actions/$afterActionId.tsx
  - frontend/src/i18n/en/common.json
  - frontend/src/i18n/ar/common.json
  - frontend/src/i18n/en/after-actions-page.json
  - frontend/src/i18n/ar/after-actions-page.json
  - frontend/src/components/after-actions/AfterActionsTable.tsx
  - frontend/src/components/after-actions/__tests__/AfterActionsTable.test.tsx
  - frontend/src/hooks/useAfterAction.ts
  - frontend/src/hooks/__tests__/useAfterActionsAll.test.ts
  - frontend/tests/e2e/after-action-detail-error.spec.ts
  - .planning/REQUIREMENTS.md
  - .planning/phases/94-write-paths/94-CONTEXT.md
autonomous: true
requirements: [WRITE-02]
truths:
  - "Decisions covered — D-12 (as corrected by RULING-P94-04 §PARK-94-05): NO table holds the FK — the fix is a TWO-QUERY REWRITE inside the function (batched .in(id, ids)), not a repoint and not a new FK (the 42P17 migration is the phase's only schema change); D-13: an after-action whose join misses is LISTED in a named degraded state, never hidden — dropping !inner without handling the null join would trade a hidden row for a titleless one, also refused; D-19: the fixed edge function is REDEPLOYED — source-only repair leaves the deployed bundle broken; D-09: every new key lands in EN and AR in the same commit under key-set equality; D-10: repaired t() calls use the COLON form — adding the key while keeping the dot form reproduces the bug silently (that IS the detail defect); D-11: error surfaces reuse Phase 93's QueryErrorState vocabulary, never new error markup; D-08: the publish toast at :109 renders the existing translated key alone — the raw err.message operand is removed"
must_haves:
  truths:
    - '/after-actions lists records: the deployed after-actions-list-all returns 200 with rows composed in code from after_action_records + batched dossiers/engagement_dossiers lookups, emitting engagement: null / dossier: null on a miss'
    - 'A missed join renders the bilingual degraded label (after-actions-page:degraded.engagementMissing) in text-warn at Body size in the engagement cell — the row present, navigable, never hidden; other join-dependent cells render an em dash, never fake values'
    - 'The detail page renders translated copy: common:afterActions.loadError (NEW key, both locales) via the COLON form — the raw key can no longer reach the screen'
    - 'The conflict.* sibling group surviving on inline-English defaults is a NAMED EXCLUSION (AR-04a mask population, Phase 99) — this plan repairs loadError, notFound, notFoundDescription and the two toast sites only'
  artifacts:
    - path: 'supabase/functions/after-actions-list-all/index.ts'
      provides: 'the two-query compose (no PostgREST embeds — there is no FK to embed through)'
      contains: ".in('id'"
    - path: 'scripts/probe-after-actions-list.mjs'
      provides: 'the deployed-list behavioural oracle (Wave 0): authenticated 200 + composed row shape'
      contains: 'after-actions-list-all'
    - path: 'frontend/src/i18n/en/after-actions-page.json'
      provides: 'degraded.engagementMissing (and its ar twin)'
      contains: 'engagementMissing'
    - path: 'frontend/tests/e2e/after-action-detail-error.spec.ts'
      provides: 'the forced-query-error oracle: translated copy renders, the raw key string does not'
      contains: 'loadError'
  key_links:
    - from: 'frontend/src/components/after-actions/AfterActionsTable.tsx'
      to: 'frontend/src/i18n/en/after-actions-page.json'
      via: 'degraded cell renders t(after-actions-page:degraded.engagementMissing) for engagement: null rows'
      pattern: 'degraded.engagementMissing'
    - from: 'frontend/src/hooks/useAfterAction.ts'
      to: 'supabase/functions/after-actions-list-all/index.ts'
      via: 'AfterActionRecordWithJoins is the response contract — keep the shape (title_* from dossiers.name_*, engagement_date from engagement_dossiers.start_date) or change hook + fn + its mocked test in the SAME task'
      pattern: 'AfterActionRecordWithJoins'
---

<objective>
Close WRITE-02: `/after-actions` lists records and the detail page renders translated copy. The
live catalog falsified the filed mechanism: `after_action_records` has NO FK on engagement_id or
dossier_id (five FKs, all → auth.users), so BOTH `!inner` embeds fail PostgREST relationship
resolution (PGRST200) and the fn 500s — and `public.engagements` lacks the very columns the embed
selects. The detail half is the i18n dot-form class: `afterActions.loadError` does not exist in the
default namespace, so the raw key prints.

Output: two-query fn rewrite + redeploy + probe; bilingual keys + colon-form repairs + degraded row
state; register corrections (same commit as the citing work).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-write-paths/94-RESEARCH.md  (§2 WRITE-02 — the catalog proof, the fix shape, the key map)
@.planning/phases/94-write-paths/94-UI-SPEC.md  (§3 Degraded row state + §4 Translated error copy + Copywriting Contract — copy is BINDING)
@.tickmarkr/overseer/RULING-P94-04-RESEARCH-PARKS.md  (§PARK-94-05 — the two-query ruling and the correction order)

<interfaces>
Verified by research (re-read at execution):
- `after-actions-list-all/index.ts:85-108` — the embeds (`engagement:engagements!inner`,
  `dossier:dossiers!inner`) and the 500 path. The rewrite: query after_action_records alone
  (keep existing filters/pagination), collect engagement_id + dossier_id sets, batch-fetch
  `dossiers` by `.in('id', ids)` (titles live on dossiers.name_en/name_ar) and
  `engagement_dossiers` for start_date where present, compose in code; emit
  `engagement: null` / `dossier: null` on a miss (D-13's render contract depends on the null).
- Response contract: `useAfterAction.ts:130-142` AfterActionRecordWithJoins —
  engagement.title_en/title_ar/engagement_date, dossier.name_en/name_ar. Keep this shape
  (source title_* from dossiers.name_*, engagement_date from engagement_dossiers.start_date,
  null when absent) so the frontend contract is stable; if you change it instead, update
  useAfterAction.ts AND useAfterActionsAll.test.ts in the SAME task (C9a).
- The staging truth that shapes the oracle: 1 live after_action_record (905b6a3a…) whose
  engagement_id exists in dossiers but whose engagement_dossiers extension row is MISSING — the
  live list should render it with a null engagement_date, not hide it.
- Detail page `$afterActionId.tsx`: `:65` t('afterActions.loadError') — dot form, key absent from
  common's 228-key afterActions subtree in BOTH locales; also missing: notFound,
  notFoundDescription (rendered at :77-78); the conflict.* group survives on inline-English
  defaults — NAMED EXCLUSION (Phase 99). `:109` — err.message preferred over the EXISTING
  publishFailed key (D-08). The error surface reuses the shipped QueryErrorState /
  the route's existing error region — never new markup (D-11).
- C9b consumers: `useAfterActionsAll.test.ts` — MOCKED (asserts invoke name + body): a NON-ORACLE
  for the server fix; flips only if the hook contract changes. `AfterActionsTable.test.tsx` —
  REAL component: extend with an engagement:null row asserting the degraded label (this is the
  D-13 oracle). `frontend/tests/e2e/after-action-publish.spec.ts` — REAL; the :109 copy change
  could touch its locators: check them in-task and update in the same task if coupled.
- i18n: `common` and `after-actions-page` are both registered for both locales (i18n/index.ts).
  The afterActions subtree is key-set EQUAL today (228/228) — keep it so.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two-query rewrite, redeploy, and the deployed-list probe</name>
  <files>supabase/functions/after-actions-list-all/index.ts, scripts/probe-after-actions-list.mjs</files>
  <read_first>after-actions-list-all/index.ts in full; scripts/probe-edge-auth.sh (credential hygiene: never echo secrets); useAfterAction.ts:120-150</read_first>
  <action>
    Rewrite the fn per the interfaces block: no embeds, two batched lookups, compose in code,
    nulls on miss, keep auth/filters/pagination/error-shape behaviour otherwise. The 500 branch
    keeps returning a sanitized error (never raw error.message to the client if it does not
    already). REDEPLOY via Supabase CLI (`supabase functions deploy after-actions-list-all`) — a
    fixed function that is not redeployed has not been fixed (D-19); paste the deploy output into
    the SUMMARY. Then write scripts/probe-after-actions-list.mjs: mint a TEST_USER JWT from
    .env.test (the probe-edge-auth.sh pattern — env from .env.test, secrets never printed),
    invoke the DEPLOYED fn, assert HTTP 200, a rows array, and that every row carries the
    engagement/dossier keys with object-or-null values (the composed contract); print the row
    count and the null-join count. Exit 2 UNABLE TO MEASURE if credentials are absent (C2).
  </action>
  <verify>
    <automated>test -f scripts/probe-after-actions-list.mjs && test "$(grep -c '!inner' supabase/functions/after-actions-list-all/index.ts)" -eq 0 && grep -q ".in('id'" supabase/functions/after-actions-list-all/index.ts && node scripts/probe-after-actions-list.mjs</automated>
  </verify>
  <acceptance_criteria>Zero !inner embeds remain; the deployed fn answers 200 with the composed shape (probe green against the DEPLOYED artifact, not source); the live 905b6a3a row appears with its missing-extension state represented as null, not hidden; deploy output pasted.</acceptance_criteria>
  <done>The list write path actually reads, and its degraded rows survive to the client.</done>
</task>

<task type="auto">
  <name>Task 2: Bilingual keys + colon-form repairs + the degraded row + oracles</name>
  <files>frontend/src/routes/_protected/after-actions/$afterActionId.tsx, frontend/src/i18n/en/common.json, frontend/src/i18n/ar/common.json, frontend/src/i18n/en/after-actions-page.json, frontend/src/i18n/ar/after-actions-page.json, frontend/src/components/after-actions/AfterActionsTable.tsx, frontend/src/components/after-actions/__tests__/AfterActionsTable.test.tsx, frontend/src/hooks/useAfterAction.ts, frontend/src/hooks/__tests__/useAfterActionsAll.test.ts, frontend/tests/e2e/after-action-detail-error.spec.ts</files>
  <read_first>94-UI-SPEC.md §3-4 + Copywriting Contract (exact EN copy and suggested AR); $afterActionId.tsx:55-120 and :160-185; AfterActionsTable.tsx (the error prop + cell layout); frontend/tests/e2e/after-action-publish.spec.ts locators</read_first>
  <action>
    (1) Keys, BOTH locales in the same edit: common.json afterActions.loadError (UI-SPEC copy),
    afterActions.notFound, afterActions.notFoundDescription; after-actions-page.json
    degraded.engagementMissing ("Engagement details unavailable" / "بيانات المشاركة غير متاحة").
    Keep the afterActions subtree key-set equal.
    (2) $afterActionId.tsx: convert the repaired call sites to COLON form
    (t('common:afterActions.loadError') etc. at :65, :77-78); the error surface reuses the shipped
    QueryErrorState vocabulary (D-11) — no new markup. `:109`: remove the err.message operand —
    t('common:afterActions.publishFailed') renders alone; raw to console.error. Do NOT convert
    untouched resolving dot-form calls (out of scope; the class fix is Phase 99's).
    (3) AfterActionsTable.tsx: rows with engagement === null render degraded.engagementMissing in
    the engagement cell — Body 13px/400, `text-warn` (warn, not danger: incomplete, not failed —
    the P93 D-07 discriminator), em dash for other join-dependent cells, row stays navigable at
    var(--row-h), no icon, no badge, no role="alert" (persistent state, not an interruption).
    Logical properties only. Extend AfterActionsTable.test.tsx with an engagement:null row case
    asserting the label (the D-13 oracle). Update useAfterAction.ts/useAfterActionsAll.test.ts
    ONLY if the response contract changed in Task 1 (C9a same-edit rule).
    (4) New spec frontend/tests/e2e/after-action-detail-error.spec.ts, EXACTLY 1 test: derive the
    detail-fetch function name from useAfterAction.ts, page.route(…, route.abort()) it, open a
    detail URL, assert the translated loadError copy is visible AND the literal string
    'afterActions.loadError' is NOT present in the page (the raw-key symptom), AND no PostgREST/
    Postgres code pattern (PGRST, 42P17, 23502) renders (the P93 copy-rule oracle).
    (5) Check after-action-publish.spec.ts locators against the :109 change; update in this task
    if coupled, else record it as a named non-consumer in the SUMMARY.
  </action>
  <verify>
    <automated>cd frontend && node -e "const en=require('./src/i18n/en/common.json'),ar=require('./src/i18n/ar/common.json');const flat=(o,p=[])=>Object.entries(o||{}).flatMap(([k,v])=>typeof v==='object'&&v!==null?flat(v,[...p,k]):[[...p,k].join('.')]);const ek=new Set(flat(en.afterActions)),ak=new Set(flat(ar.afterActions));const sym=[...ek].filter(k=>!ak.has(k)).concat([...ak].filter(k=>!ek.has(k)));if(sym.length){console.error('afterActions key-set mismatch:',sym);process.exit(1)};const need=['loadError','notFound','notFoundDescription'];process.exit(need.every(k=>typeof en.afterActions[k]==='string'&&typeof ar.afterActions[k]==='string'&&en.afterActions[k]!==ar.afterActions[k])?0:1)" && node -e "const en=require('./src/i18n/en/after-actions-page.json'),ar=require('./src/i18n/ar/after-actions-page.json');process.exit(typeof en.degraded?.engagementMissing==='string'&&typeof ar.degraded?.engagementMissing==='string'&&en.degraded.engagementMissing!==ar.degraded.engagementMissing?0:1)" && grep -q "common:afterActions.loadError" 'src/routes/_protected/after-actions/$afterActionId.tsx' && test "$(grep -cE "err\.message" 'src/routes/_protected/after-actions/$afterActionId.tsx')" -eq 0 && pnpm exec vitest run src/components/after-actions/__tests__/AfterActionsTable.test.tsx && test -f tests/e2e/after-action-detail-error.spec.ts && OUT=$(pnpm exec playwright test tests/e2e/after-action-detail-error.spec.ts --reporter=line 2>&1) && echo "$OUT" | grep -qE '(^|[^0-9])1 passed' && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>Key sets equal with the three new common keys + the degraded key bilingual; colon-form at the repaired sites; zero err.message in the detail route; degraded-row component test green; the 1-test forced-error spec green (existence asserted first, count hardcoded); type-check clean.</acceptance_criteria>
  <done>The detail page can only ever render translated copy, and a half-joined record is visibly incomplete instead of invisible.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the WRITE-02 register prose + the D-12 context note (same commit as the citing work)</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/94-write-paths/94-CONTEXT.md</files>
  <read_first>.planning/REQUIREMENTS.md §WRITE-02 (the falsified parenthetical); 94-CONTEXT.md §D-12; RULING-P94-04 §PARK-94-05 (the correction order)</read_first>
  <action>
    STAGE AND COMMIT WITH THIS PLAN's fn-rewrite work (RULING-P94-04 cross-cutting order 1).
    (1) REQUIREMENTS.md WRITE-02: replace "(PostgREST embed targets the table that holds the FK)"
    with the measured truth, derivation inline: pg_constraint on after_action_records returns
    exactly 5 FKs, ALL REFERENCES auth.users(id) — NO FK on engagement_id or dossier_id, so both
    embeds die at PostgREST relationship resolution (PGRST200) and public.engagements lacks
    title_en/title_ar/engagement_date regardless; the fix is the two-query batched .in('id', ids)
    rewrite (no schema change — the 42P17 migration is the phase's only one). Include the literal
    token `PGRST200` (the gate keys on it; verified absent from the file today).
    (2) 94-CONTEXT.md D-12: APPEND a correction blockquote inside the D-12 bullet (never renumber,
    never delete the original sentence — the house convention is correction notes, and the
    coverage extractor keys on the `**D-12:**` form which must survive): one paragraph stating
    the instruction was written from the requirement's prose, the catalog shows no FK exists, and
    RULING-P94-04 §PARK-94-05 resolved it to the two-query rewrite.
    Touch NOTHING else in either file (94-09/94-10 own later REQUIREMENTS.md edits; this plan is
    wave-serialized after 94-02's).
  </action>
  <verify>
    <automated>grep -q 'PGRST200' .planning/REQUIREMENTS.md && grep -q 'PARK-94-05' .planning/phases/94-write-paths/94-CONTEXT.md && grep -q '\*\*D-12:' .planning/phases/94-write-paths/94-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>Both corrections present with their marker tokens; the D-12 original id form survives (the coverage extractor keys on it); the most recent commit carries the register edit alongside this plan's work.</acceptance_criteria>
  <done>The register and the context no longer instruct an unsatisfiable repoint.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                     |
| ------------------------ | ----------------------------------------------- |
| edge fn → client rows    | composed join must not leak rows RLS would hide |
| query error → detail DOM | raw keys and server messages must not render    |

## STRIDE Threat Register

| Threat ID | Category               | Component                 | Disposition | Mitigation Plan                                                                                                                     |
| --------- | ---------------------- | ------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| T-94-12   | Information Disclosure | after-actions-list-all    | mitigate    | the batched lookups run under the SAME client/auth context as the base query — no service-role widening; 500 branch stays sanitized |
| T-94-13   | Information Disclosure | detail-page toasts/errors | mitigate    | err.message operand removed; colon-form translated keys; copy-rule oracle in the spec (no PGRST/42P17/23502 renders)                |
| T-94-SC   | Tampering              | package installs          | accept      | zero installs this phase                                                                                                            |

</threat_model>

<verification>
- Deployed-fn probe green (200 + composed shape + null-join representation).
- Key-set equality gates + colon-form + zero err.message; degraded-row component test; 1-test
  forced-error spec.
- Register + context corrections landed with marker tokens, same commit as the citing work.
</verification>

<success_criteria>
Criterion 2 holds behaviourally: the list renders rows including the degraded join against the
DEPLOYED function, and the detail page can only render translated copy — with the falsified
diagnosis corrected where it was filed.
</success_criteria>

<output>
Create `.planning/phases/94-write-paths/94-07-SUMMARY.md` when done.
</output>
