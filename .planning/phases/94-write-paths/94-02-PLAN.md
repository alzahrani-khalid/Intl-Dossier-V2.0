---
phase: 94-write-paths
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/intake-form/IntakeForm.tsx
  - frontend/tests/component/IntakeForm.test.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [WRITE-03]
truths:
  - 'Decisions covered — D-23: both gates below were authored against GATE-STANDARD C1-C10 (marker tokens verified absent today, so each gate is RED on the undone tree for its subject); D-25: the schema relaxation carries a behavioural oracle — the REAL Zod schema is executed by the component test with a live non-RFC staging id, not grepped'
must_haves:
  truths:
    - 'The filed WRITE-03 diagnosis is measured FALSE and the register is corrected in the same commit as the fix: the picker writes dossierId (IntakeForm.tsx:82) and the schema reads dossierId (:53) — same field; the real cause is zod 4.3.6 .uuid() (RFC-9562-strict) rejecting 35 of 44 staging dossier ids'
    - 'The schema keeps requiredness: z.string().min(1, …) — relaxation removes the RFC check, never the required check (never .uuid() a dossier id — standing project law, Phase 86)'
    - 'setValue(dossierId, …) passes { shouldValidate: true } at both call sites (:82, :85) so a stale "dossier required" error clears the moment a dossier is picked — "Linked to: OECD" and "At least one dossier is required" can never render together'
    - 'Zero new i18n keys — the existing dossier-context:validation.dossier_required message becomes truthful, not replaced'
  artifacts:
    - path: 'frontend/src/components/intake-form/IntakeForm.tsx'
      provides: 'non-uuid dossierId schema + revalidating setValue calls'
      contains: 'shouldValidate'
    - path: 'frontend/tests/component/IntakeForm.test.tsx'
      provides: 'non-RFC-id acceptance case beside the surviving "zod blocks submit" case'
      contains: 'b0000001-0000-0000-0000-000000000005'
    - path: '.planning/REQUIREMENTS.md'
      provides: 'corrected WRITE-03 entry with the zod derivation inline (RULING-P94-04 cross-cutting order 1)'
      contains: 'RFC-9562'
  key_links:
    - from: 'frontend/tests/component/IntakeForm.test.tsx'
      to: 'frontend/src/components/intake-form/IntakeForm.tsx'
      via: 'the test imports and exercises the REAL schema (selector + API mocked) — the C9b real oracle for this change'
      pattern: 'dossierId'
---

<objective>
Close WRITE-03: `/intake/new` submits. The real mechanism (research §3, proven against the installed
zod): `z.string().uuid()` under Zod 4.3.6 enforces RFC-9562 version/variant bits and rejects 35 of
44 staging dossier ids (OECD `b0000001-0000-0000-0000-000000000005` proven FAIL), rendering
`dossier-context:validation.dossier_required` while the badge renders from separate React state —
the exact audit symptom. The filed field-name-mismatch diagnosis is FALSE and the register prose is
corrected in the same commit (RULING-P94-04 order 1).

Output: schema relaxation + shouldValidate + test extension + register correction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-write-paths/94-RESEARCH.md  (§3 WRITE-03 — the derivation this plan implements and cites)

<interfaces>
Verified by research (re-read at execution):
- `IntakeForm.tsx:53` — `dossierId: z.string().uuid({ message: tDossier('validation.dossier_required') })`.
- `IntakeForm.tsx:82,:85` — `setValue('dossierId', …)` with NO third argument (shouldValidate
  defaults false); the sibling calls for requestType/urgency at :192-195/:333-336 already pass it —
  mirror them, single-line so the gate can see the option.
- Downstream is already correct: `useIntakeApi.ts:60` maps `dossier_id: data.dossierId`;
  `intake-tickets-create/index.ts:13,209` accepts `dossier_id`. Do not touch either.
- `intake_tickets.calculate_ticket_priority` trigger derives `priority` from urgency+sensitivity
  and the form never sends `priority` — benign, DO NOT add a priority field (P94-TRIGGER-SWEEP F2).
- C9b: `frontend/tests/component/IntakeForm.test.tsx` mocks DossierSelector (:13-16) and
  useIntakeApi but exercises the REAL schema — its "zod blocks submit" case (:49) MUST survive
  (empty dossierId still blocks); add a non-RFC acceptance case. `IntakeFormData.dossierId?: string`
  (types/intake.ts:64) needs no change.
- Outside the fix, stated: the dev-only "Fill with Mock Data" flow (:457-478) never sets dossierId
  — correct; attachments honestly disabled (:402-410); the local dossierError state (:75) is a
  second render source for the same message — leave it, it renders only when genuinely empty.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relax the dossierId schema, revalidate on pick, extend the real-schema test</name>
  <files>frontend/src/components/intake-form/IntakeForm.tsx, frontend/tests/component/IntakeForm.test.tsx</files>
  <read_first>IntakeForm.tsx:40-100 and :320-400; IntakeForm.test.tsx in full (the mock boundary and the surviving case)</read_first>
  <behavior>
    - Test (survives, unchanged): empty dossierId blocks submit with the required message.
    - Test (new): a non-RFC staging id (`b0000001-0000-0000-0000-000000000005`) passes the schema and submit proceeds to the (mocked) API.
    - Test (new, if expressible at component level): after a failed submit, selecting a dossier clears the dossierId error without a second submit.
  </behavior>
  <action>
    (1) `:53` → `dossierId: z.string().min(1, { message: tDossier('validation.dossier_required') })`
    — requiredness kept, RFC check removed. NEVER `.uuid()` a dossier id (standing law; 35/44
    staging ids are version-nibble-0 seeds). (2) Add `{ shouldValidate: true }` as the third
    argument to BOTH `setValue('dossierId', …)` calls (:82, :85), single-line, mirroring the
    requestType/urgency precedent in the same file. (3) Extend the test per the behavior block.
    Server-side validation in intake-tickets-create is UNCHANGED (V5: the client relaxation does
    not remove the server's checks).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run tests/component/IntakeForm.test.tsx && test "$(grep -cE "setValue\('dossierId'.*shouldValidate: true" src/components/intake-form/IntakeForm.tsx)" -ge 2 && test "$(grep -cE "dossierId: z\.string\(\)\.uuid" src/components/intake-form/IntakeForm.tsx)" -eq 0 && grep -q 'b0000001-0000-0000-0000-000000000005' tests/component/IntakeForm.test.tsx && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>All IntakeForm tests green including the non-RFC acceptance case AND the surviving blocks-empty case; both setValue sites revalidate; no .uuid() on dossierId; type-check clean.</acceptance_criteria>
  <done>/intake/new submits with any live staging dossier and a stale validation error clears on pick.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the WRITE-03 register prose (same commit as Task 1)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md §WRITE-03 (the falsified sentence); 94-RESEARCH.md §3 (the derivation to inline)</read_first>
  <action>
    STAGE AND COMMIT THIS EDIT TOGETHER WITH TASK 1's CHANGE — RULING-P94-04 cross-cutting order 1:
    every overturned diagnosis's register prose is corrected in the same commit as the work that
    cites it. Rewrite the WRITE-03 entry: the filed "picker writes a field the schema does not
    read" mechanism is falsified — both sides are `dossierId` (IntakeForm.tsx:82 vs :53). The real
    cause: zod 4.3.6's `.uuid()` is RFC-9562-strict and 35 of 44 staging dossier ids are non-RFC
    seed UUIDs (version nibble 0); OECD `b0000001-0000-0000-0000-000000000005` proven FAIL against
    the installed zod while `7c0d830b…` passes. State the fix (min(1) + shouldValidate) and the
    standing law (never .uuid() a dossier id, Phase 86). Include the literal tokens `RFC-9562` and
    `35 of 44` (the gate keys on them; both verified ABSENT from the file today). Do not touch any
    other entry — Plans 94-07/94-09/94-10 own the other register edits and are wave-serialized on
    this file.
  </action>
  <verify>
    <automated>grep -q 'RFC-9562' .planning/REQUIREMENTS.md && grep -q '35 of 44' .planning/REQUIREMENTS.md && git log -1 --name-only --pretty=format: | grep -q 'REQUIREMENTS.md' && git log -1 --name-only --pretty=format: | grep -q 'IntakeForm.tsx'</automated>
  </verify>
  <acceptance_criteria>The corrected entry carries the derivation inline with both marker tokens, and the most recent commit contains BOTH the register edit and the code fix (the same-commit order, checked mechanically).</acceptance_criteria>
  <done>The register no longer asserts a mechanism the catalog falsified.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                        |
| --------------------- | -------------------------------------------------- |
| client form → edge fn | client validation relaxed; server checks unchanged |

## STRIDE Threat Register

| Threat ID | Category  | Component               | Disposition | Mitigation Plan                                                                        |
| --------- | --------- | ----------------------- | ----------- | -------------------------------------------------------------------------------------- |
| T-94-03   | Tampering | intake dossierId schema | mitigate    | min(1) keeps requiredness; intake-tickets-create server-side validation untouched (V5) |
| T-94-SC   | Tampering | package installs        | accept      | zero installs this phase                                                               |

</threat_model>

<verification>
- IntakeForm suite green (surviving case + non-RFC case); type-check clean.
- Register correction present with marker tokens, same commit as the fix.
</verification>

<success_criteria>
Success criterion 3 holds: /intake/new submits for real staging dossiers; the required-dossier
message renders only when no dossier is linked; the register states the true mechanism.
</success_criteria>

<output>
Create `.planning/phases/94-write-paths/94-02-SUMMARY.md` when done.
</output>
