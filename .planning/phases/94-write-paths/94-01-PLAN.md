---
phase: 94-write-paths
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/probe-after-action-publish.mjs
  - frontend/tests/component/after-action-route-wiring.test.tsx
  - frontend/src/components/after-action-form/AfterActionForm.tsx
  - frontend/src/routes/_protected/engagements/$engagementId/after-action.tsx
  - frontend/tests/component/AfterActionForm.test.tsx
autonomous: true
requirements: [WRITE-01]
truths:
  - 'Decisions covered — D-08: the save/publish failure toasts render a specific translated message, never a server-originated error.message (after-action.tsx:103 repaired; raw goes to console.error only); D-10: the repaired t() call uses the COLON form common:afterActions.saveFailed, never the dot form; D-25: the create-mode enablement contract carries a behavioural oracle — new create-mode component tests drive the REAL AfterActionForm with no initialData and observe button state'
must_haves:
  truths:
    - 'In create mode (no initialData), Save draft is DISABLED on an untouched empty form and ENABLES once the user enters content — the fix must NOT enable Save on mount (deleting the guard naively does exactly that)'
    - 'The engagement route passes canPublish + onPublish; canPublish adopts the SERVER role set [staff, supervisor, admin] from after-actions-publish/index.ts:60-71 (RULING-P94-04 A2: the server is the enforcement boundary; a UI narrower than the server is a hidden capability)'
    - 'Publish from create mode is a two-step (create then publish) because after-actions-create hardcodes publication_status: draft (index.ts:134) — the client-sent status spread is decorative'
    - 'No toast in either touched file interpolates err.message — a publish/save failure renders the existing translated key alone'
  artifacts:
    - path: 'frontend/src/components/after-action-form/AfterActionForm.tsx'
      provides: 'create-mode Save enablement derived from content, edit-mode behaviour unchanged'
      contains: 'hasContent'
    - path: 'frontend/src/routes/_protected/engagements/$engagementId/after-action.tsx'
      provides: 'canPublish + onPublish wiring, two-step publish, D-08-clean toasts'
      contains: 'canPublish'
    - path: 'frontend/tests/component/AfterActionForm.test.tsx'
      provides: 'create-mode (no initialData) enablement oracle; shipped initialData={{}} tests keep passing'
      contains: 'create mode'
  key_links:
    - from: 'frontend/src/routes/_protected/engagements/$engagementId/after-action.tsx'
      to: 'frontend/src/hooks/usePublishAfterAction.ts'
      via: 'onPublish two-step: useCreateAfterAction().mutateAsync then usePublishAfterAction().mutateAsync({ afterActionId: created.id, isConfidential })'
      pattern: 'usePublishAfterAction'
    - from: 'frontend/src/components/after-action-form/AfterActionForm.tsx'
      to: 'frontend/tests/component/AfterActionForm.test.tsx'
      via: 'create-mode tests are the C9b consumer update shipped in the SAME task as the guard change'
      pattern: 'initialData'
---

<objective>
Close WRITE-01: an after-action record can be created, saved and published from the engagement UI.
Two defects: (1) `AfterActionForm.tsx:130-134`'s `if (!initialData) return` pins `isDirty` false in
create mode so Save draft is permanently disabled; (2) the engagement route never passes
`canPublish`/`onPublish`, so Publish does not render at all.

Purpose: the first advertised write path that "actually writes".
Output: form enablement fix + route wiring + create-mode component tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-write-paths/94-UI-SPEC.md  (§5 Create-mode Save/Publish enablement — the state table is BINDING)
@.planning/phases/94-write-paths/94-RESEARCH.md  (§1 WRITE-01 — every file:line pin below comes from it; re-read at execution, never retype from memory)

<interfaces>
Verified by research (sha fe380206c; re-read at execution):
- `AfterActionForm.tsx:45-57` — `onSave(data, isDraft)`, `onPublish?(data)`, `canPublish?` (default
  false at :66). Publish renders only when `canPublish && onPublish` (:499), disabled until
  `isFormValid()` (:296-305: ≥1 attendee AND ≥1 of decisions/commitments/risks/follow-ups).
- `AfterActionForm.tsx:130-134` — the isDirty guard. Provenance: bulk cleanup commit `307485dd3`,
  NOT a design decision. `:140-151` already computes a content-derived `hasContent` expression for
  create mode (its own comment at :136-139 says isDirty is "edit-mode").
- Route `after-action.tsx:156-162` passes engagementId, dossierId, availableUsers, onSave,
  onDirtyChange — no canPublish, no onPublish, no initialData (create mode).
- `after-actions-create/index.ts:134` hardcodes `publication_status: 'draft'`.
- `after-actions-publish/index.ts:60-71` allows `['staff','supervisor','admin']`, 403 otherwise.
- Detail-page precedent `$afterActionId.tsx:85` uses `['supervisor','admin']` — do NOT copy it;
  RULING-P94-04 A2 decided the UI adopts the SERVER set, citing :60-71. (The detail page itself is
  Plan 94-07's file — do not touch it here.)
- Hooks exist and handle invalidation: `useCreateAfterAction`, `usePublishAfterAction`
  (`usePublishAfterAction.ts:19-22` invokes `after-actions-publish`). No new data-layer code.
- `after-action.tsx:103` — `toast.error((err instanceof Error ? err.message : null) ?? t('afterActions.saveFailed'))`
  — the in-scope D-08 violation. The fallback key EXISTS in common.json (228-key afterActions
  subtree, key-set equal EN/AR).
- C9b: `frontend/tests/component/AfterActionForm.test.tsx` dodges the bug via `initialData={{}}`
  (:375, :399 and others) — those tests KEEP passing after the fix; this plan ADDS create-mode
  cases, it does not flip shipped ones. `frontend/tests/e2e/after-action-create.spec.ts` et al are
  REAL consumers of the route DOM whose current colour is unmeasured (E2ESTALE ambient) — run for
  evidence, judged at phase close against the known-red inventory, never a hard gate here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create-mode Save enablement + create-mode component tests</name>
  <files>frontend/src/components/after-action-form/AfterActionForm.tsx, frontend/tests/component/AfterActionForm.test.tsx</files>
  <read_first>94-UI-SPEC.md §5 (the four-row state table); AfterActionForm.tsx:120-160 and :480-510 in full; AfterActionForm.test.tsx:360-410 (the shipped initialData={{}} workaround cases)</read_first>
  <behavior>
    - Test (create mode, no initialData prop): untouched empty form → Save draft button disabled.
    - Test (create mode): after user input (e.g. typing a title / adding an attendee), Save draft enabled.
    - Test (create mode, canPublish + onPublish passed): Publish visible but disabled until isFormValid(); enabled once ≥1 attendee and ≥1 of decisions/commitments/risks/follow-ups exist.
    - Shipped initialData={{}} tests continue to pass unchanged.
  </behavior>
  <action>
    Fix the enablement WITHOUT enabling Save on an untouched form: the effect at :130-134 fires
    post-mount on the initial formData, so simply deleting the guard enables Save immediately —
    the UI-SPEC forbids that. Gate the Save button on content-derived dirtiness in create mode:
    reuse the `hasContent` expression the file already computes at :140-151 (e.g. disable when
    `saving || publishing || (initialData ? !isDirty : !hasContent)` — exact wiring is discretion;
    the state table is not). Edit-mode behaviour byte-equivalent. Add test titles containing
    "create mode" so the gate can find them. Keep the shipped tests green — they are the
    regression net, not the target.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run tests/component/AfterActionForm.test.tsx && test "$(grep -ci 'create mode' tests/component/AfterActionForm.test.tsx)" -ge 3 && grep -qi 'untouched' tests/component/AfterActionForm.test.tsx && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>STATED LIMIT of the trap clause (round-3): `grep -qi 'untouched'` binds the trap case's PRESENCE, not its assertion's DIRECTION — a test named for the untouched form that asserted the wrong thing would still pass it. It is a FAIR proxy rather than an arbitrarily-absent string (the word is load-bearing in this plan's own behavior block and in UI-SPEC's state table), and it is 0 today so it can fire — but naming the literal test title would bind direction too, and that is the honest upgrade. All AfterActionForm component tests green including ≥3 new create-mode cases; untouched-empty-form-disabled is asserted (the trap case); type-check clean.</acceptance_criteria>
  <done>Create-mode Save enables on content, proven by tests that drive the real component with no initialData.</done>
</task>

<task type="auto">
  <name>Task 2: Wire canPublish + onPublish on the engagement route; D-08 toast repair</name>
  <files>frontend/src/routes/_protected/engagements/$engagementId/after-action.tsx</files>
  <read_first>after-action.tsx in full (esp. :85 the decorative publication_status spread, :103 the toast, :156-162 the props); supabase/functions/after-actions-publish/index.ts:60-71; frontend/src/hooks/usePublishAfterAction.ts</read_first>
  <action>
    (1) Compute `canPublish = ['staff','supervisor','admin'].includes(user?.role ?? '')` — the
    SERVER's set, with a one-line comment citing after-actions-publish/index.ts:60-71 and
    RULING-P94-04 A2 (a UI narrower than the server is a hidden capability; if staff should be
    excluded, that is a server policy change to file, not a button to narrow).
    (2) `onPublish` is a TWO-STEP: create via useCreateAfterAction().mutateAsync (the fn hardcodes
    draft — the :85 status spread is decorative, leave or remove it, never rely on it), then
    usePublishAfterAction().mutateAsync({ afterActionId: created.id, isConfidential }). While it
    runs both buttons stay disabled via the form's existing publishing state. On failure render
    the EXISTING translated key alone (publish: common:afterActions.publishFailed if the route
    surfaces it; save: common:afterActions.saveFailed) — never success-then-nothing.
    (3) D-08/D-10 repair at :103 (and any sibling toast in this file): drop the err.message
    operand; toast renders t('common:afterActions.saveFailed') (COLON form — the dot form resolves
    only via the aliased default namespace, which is the WRITE-02 bug class); the raw error object
    goes to console.error(err) only. No new i18n keys — both keys exist in both locales.
  </action>
  <verify>
    <automated>cd frontend && grep -q 'canPublish' 'src/routes/_protected/engagements/$engagementId/after-action.tsx' && grep -q 'onPublish' 'src/routes/_protected/engagements/$engagementId/after-action.tsx' && grep -q 'common:afterActions.saveFailed' 'src/routes/_protected/engagements/$engagementId/after-action.tsx' && test "$(grep -cE 'toast\.(error|success)\([^)]*\.message' 'src/routes/_protected/engagements/$engagementId/after-action.tsx')" -eq 0 && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>Route passes canPublish (server set, cited) + onPublish (two-step); no toast call interpolates .message; the repaired t() call is colon-form; type-check clean. Evidence step (not a gate): run frontend/tests/e2e/after-action-create.spec.ts once and record its colour in the SUMMARY — judged against the E2ESTALE known-red inventory at phase close, per 94-VALIDATION.md.</acceptance_criteria>
  <done>Publish is reachable from the engagement UI for exactly the roles the server accepts, and a failed save/publish says so in translated copy.</done>
</task>

<task type="auto">
  <name>Task 3: Behavioural PROBE for criterion 1's "published" clause (RULING-P94-06 B6)</name>
  <files>scripts/probe-after-action-publish.mjs</files>
  <action>
    Criterion 1 says an after-action can be created, saved AND PUBLISHED from the engagement UI.
    Before this task the only verification of the publish half was four greps plus `type-check` —
    nothing that could FAIL if create→publish did not work. The e2e both this plan and
    `94-VALIDATION.md:88` nominated navigates to `/after-action/create`, **a route that does not
    exist in the tree**, so it yields an uninformative red rather than evidence.

    Write `scripts/probe-after-action-publish.mjs` in the shape of the phase's other four probes:
      1. Read `TEST_USER_EMAIL` / `TEST_USER_PASSWORD`. **Absent ⇒ print what is missing and
         `process.exit(2)`** — a distinct code so "could not measure" is never read as a pass
         (GATE-STANDARD C2).
      2. Mint the user JWT via `signInWithPassword` (inline auth — D-27; no dependence on the e2e
         `setup` project, which E2ECRED-01 blocks).
      3. Invoke `after-actions-create`, capture the returned id.
      4. Invoke `after-actions-publish` with that id.
      5. Read the row back and assert `publication_status === 'published'` — the WRITE, observed,
         not the absence of an error.
      6. Delete the namespaced fixture it created, then print every id it acted on.
      7. Exit 0 only if the read-back assertion held.

    The fixture is namespaced (`p94-probe-…`) and removed; the probe prints its own subjects so a
    failure names them.

    **AND a REQUIRED wiring oracle — `frontend/tests/component/after-action-route-wiring.test.tsx`.**
    The probe closes the SERVER half; it never executes the two-step wiring in
    `engagements/$engagementId/after-action.tsx`, which is the only code Task 2 writes. Render the
    route component with `useCreateAfterAction` and `usePublishAfterAction` mocked, press Publish,
    and assert: create is called once; publish is called once **with the id create returned**; on
    create failure publish is NOT called.

    **This test is a REAL oracle, not a mocked non-oracle** (`RULING-P94-07`). C9b's mock-vs-real
    test is decided by the **subject**: here the subject IS the wiring, and the hooks are its
    boundary — exactly the shape `94-03` already calls the real oracle for the no-op guard. An
    earlier draft of this criterion dismissed it as "a mocked NON-ORACLE", which applied two
    standards to one shape.

  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run tests/component/after-action-route-wiring.test.tsx && cd .. && test -f scripts/probe-after-action-publish.mjs && grep -q 'publication_status' scripts/probe-after-action-publish.mjs && grep -q 'process.exit(2)' scripts/probe-after-action-publish.mjs && node scripts/probe-after-action-publish.mjs</automated>
  </verify>
  <acceptance_criteria>The probe exits 0 having created, published and READ BACK `publication_status === 'published'`, then cleaned its fixture. Absent credentials it exits 2, which is UNABLE TO MEASURE and is never counted as a pass. The wiring test is REQUIRED and counts as a real oracle for the wiring half — its subject is the wiring, and mocking the hooks mocks that subject's boundary, not the subject (RULING-P94-07). The probe owns the server half; neither substitutes for the other.</acceptance_criteria>
  <done>Criterion 1's publish half has an oracle that fails when the work is not done — it did not before.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                     |
| --------------------------- | ----------------------------------------------- |
| client role check → publish | client-side canPublish is UX, not authorization |
| error object → toast copy   | server-originated messages must not reach DOM   |

## STRIDE Threat Register

| Threat ID | Category               | Component                   | Disposition | Mitigation Plan                                                                                                    |
| --------- | ---------------------- | --------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------ |
| T-94-01   | Elevation of Privilege | onPublish wiring            | mitigate    | server re-checks role at after-actions-publish/index.ts:60-71 (unchanged); client mirrors, never widens the server |
| T-94-02   | Information Disclosure | save/publish failure toasts | mitigate    | err.message operand removed; translated keys only; raw error to console.error (D-08)                               |
| T-94-SC   | Tampering              | package installs            | accept      | zero installs this phase (research Package Legitimacy Audit: nothing to install)                                   |

</threat_model>

<verification>
- Component suite green with new create-mode cases; type-check clean.
- Grep gates: canPublish/onPublish present; colon-form key present; no toast interpolates .message.
- Evidence (recorded, not gated): after-action-create.spec.ts run result in SUMMARY.
</verification>

<success_criteria>
Success criterion 1 of the phase holds behaviourally: create-mode Save enables on content (never on
mount), Publish renders for the server's role set and performs the two-step create→publish, and
every failure on this surface renders translated copy.
</success_criteria>

<output>
Create `.planning/phases/94-write-paths/94-01-SUMMARY.md` when done.
</output>
