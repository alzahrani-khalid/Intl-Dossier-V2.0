---
phase: 94-write-paths
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/WorkBoard/commitment-stage-guard.ts
  - frontend/src/pages/WorkBoard/__tests__/commitment-stage-guard.test.ts
  - frontend/src/hooks/useUnifiedKanban.ts
  - frontend/src/pages/WorkBoard/WorkBoard.tsx
  - frontend/src/pages/WorkBoard/__tests__/WorkBoard.test.tsx
  - frontend/src/i18n/en/unified-kanban.json
  - frontend/src/i18n/ar/unified-kanban.json
autonomous: true
requirements: [WRITE-04]
truths:
  - 'Decisions covered — D-03: the five-value commitment lifecycle is RE-DERIVED live before the mapping is finalized (never trusted from any document, including this one); D-30: the stage→status defect is ONE cell (review→review) — todo/in_progress/done already write valid statuses and are not remapped; D-04: review rejects at the MUTATION layer (the safety net demanded by criterion 4 regardless of the affordance); D-05: the no-op guard compares resolveBoardStage(item), never workflow_stage (null for every commitment); D-06: aa_commitments columns and enums are never renamed or extended — the mapping lives at the mutation layer; D-07: query-client.ts is NOT edited (COPY-06 is Phase 98); D-08: both reject causes and the generic failure render specific translated messages with role="alert", never error.message; D-09: all four new keys land in EN and AR in the same commit under the key-set-equality gate; D-32: no oracle in this plan asserts persistence by asserting "no error" — the trigger silently overwrites past-due writes; D-33: the guard predicate MIRRORS the trigger condition (due date before today AND the status about to be written is pending/in_progress) — one condition, two enforcement points, no drift by construction; D-31: the reverse-mapping population statement (where all five live statuses render) is carried in this plan and restated at close'
must_haves:
  truths:
    - 'A commitment drag persists exactly when the DB state machine permits it; a drag the trigger would coerce is refused BEFORE the write with the real bilingual reason; the stored value always equals either what was written or what the user was told; never a success signal followed by a snap-back (RULING-P94-03 order 2 — the criterion invariant, verbatim)'
    - 'TWO DISTINCT reject keys, both locales: unified-kanban:errors.commitmentNoReviewStage(+Description) and unified-kanban:errors.commitmentPastDue(+Description) — the past-due copy names the derived state AND the remedy the trigger honours (extend the due date, or complete it); a single generic string is REFUSED'
    - 'A refusal fires NO mutation, NO success toast, and NO global onSuccess/onError handler — the reject is decided before any mutation is enqueued; the mutationFn re-asserts as defence in depth'
    - 'Own-column drop is a no-op: NO mutation, NO toast (D-05 via resolveBoardStage) — asserted as absence of both, not absence of error'
    - "Reverse-mapping population (D-31, RULING-P94-02-approved): pending→Todo, in_progress→In-progress, completed→Done, cancelled→not rendered (filtered at WorkBoard.tsx:203, deliberate), overdue→Todo via the default branch (undesigned, stated; handling is Phase 96's COUNT-04, NOT this plan's work)"
  artifacts:
    - path: 'frontend/src/pages/WorkBoard/commitment-stage-guard.ts'
      provides: 'the single source for commitment stage→status mapping + drop decision (reject reasons no_review_counterpart | past_due_coercion) consumed by the mutation layer now and the droppable predicate in 94-08'
      exports: ['resolveCommitmentDropDecision', 'COMMITMENT_STAGE_TO_STATUS']
    - path: 'frontend/src/i18n/en/unified-kanban.json'
      provides: 'four new errors.* keys (and their ar twins) for the two reject causes'
      contains: 'commitmentPastDue'
    - path: 'frontend/src/pages/WorkBoard/__tests__/commitment-stage-guard.test.ts'
      provides: 'the mapper/guard behavioural oracle including the coercion case (Wave 0 gap closed)'
      contains: 'past_due_coercion'
  key_links:
    - from: 'frontend/src/hooks/useUnifiedKanban.ts'
      to: 'frontend/src/pages/WorkBoard/commitment-stage-guard.ts'
      via: 'the commitment branch calls resolveCommitmentDropDecision before any write'
      pattern: 'resolveCommitmentDropDecision'
    - from: 'frontend/src/pages/WorkBoard/WorkBoard.tsx'
      to: 'frontend/src/pages/WorkBoard/WorkBoard.tsx'
      via: 'no-op guard compares targetStage === resolveBoardStage(item)'
      pattern: 'resolveBoardStage\(item\)'
---

<objective>
Close WRITE-04's mutation-layer half: the commitment drag becomes honest. Three defects in one seam:
(1) the `review` column writes a status the CHECK constraint rejects (the ONE broken cell, D-30);
(2) the `BEFORE UPDATE` trigger `commitment_overdue_check` silently rewrites past-due writes to
`overdue` after a success toast (D-32 — 8 of 10 staging commitments are past-due); (3) the no-op
guard compares `workflow_stage` (null for commitments) so an own-column drop fires a mutation and a
success toast (D-05). Plan 94-08 adds the droppable affordance on top of this module.

Output: shared guard module + guarded mutation layer + fixed no-op guard + four bilingual keys +
unit and board-level oracles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-write-paths/94-UI-SPEC.md  (§1 Kanban reject messages + Copywriting Contract — copy is BINDING, EN and AR tables)
@.planning/phases/94-write-paths/94-RESEARCH.md  (§4 WRITE-04 — every seam pin; §Common Pitfalls 1-2)
@.tickmarkr/overseer/RULING-P94-03-TRIGGER.md  (orders 2-3: the criterion invariant and the two-key rule)

<interfaces>
Verified by research (re-read at execution):
- `useUnifiedKanban.ts:303-327` — `mapToValidIntakeStatus`: the exact pattern to mirror (source-
  specific mapper, explicit unknown-key branch). `:371-384` — the commitment branch writes
  `newStatus` RAW today. `:481-485` — onError toast with hardcoded English title
  ('Failed to update status') + `description: error.message` — the in-scope D-08 violation;
  existing keys `unified-kanban:errors.updateFailed` + `updateFailedDescription` REPLACE it (zero
  new keys for this repair). `:423-468` optimistic update applies the CLIENT's status; `:490`
  onSettled refetches — which is WHY a coerced write snaps back after a success toast.
- `WorkBoard.tsx:67` STAGES; `:78-84` STAGE_TO_STATUS (a TASKS map — do not repurpose for
  commitments); `:92-106` resolveBoardStage (:97-98 is the dead commitment-review branch — remove
  it now that PARK-94-01 is ruled); `:305-311` the no-op guard comparing `item.workflow_stage`.
- Commitment items: `work-item.types.ts:70` — workflow_stage is null for commitments; derive the
  due-date field name from the type at execution (the query layer maps aa_commitments.due_date).
- The trigger condition to MIRROR (never paraphrase): `NEW.due_date < CURRENT_DATE AND NEW.status
  IN ('pending','in_progress')` — client form: the commitment's due date is before today
  (date-only compare) AND the status about to be written is pending or in_progress. The guard
  NEVER writes 'overdue' (DB-owned, StatusDropdown.tsx:83 comment: auto-applied).
- Toast a11y: react-hot-toast defaults to role="status"; the reject toast must render
  role="alert" (RULING-P94-01 order 1 — an ACCEPTANCE CRITERION). The installed react-hot-toast
  supports per-toast `ariaProps: { role: 'alert', 'aria-live': 'assertive' }`; the useToast wrapper
  does not forward ariaProps today — extend it or call toast.error directly; the contract is the
  rendered role="alert" + the translated strings, not the mechanism.
- `unified-kanban` namespace registered both locales (i18n/index.ts:287/:423); its `errors` object
  exists in both. Copy for all four keys: UI-SPEC Copywriting Contract tables (EN + suggested AR;
  align AR terminology with the shipped columns.* glossary before committing).
- C9b: `WorkBoard.test.tsx` mocks `@/components/kanban` AND `useUnifiedKanban`, capturing
  onDragEnd at the boundary — the REAL oracle for the no-op guard and the reject decision; update
  it IN THE SAME TASK (its items currently carry workflow_stage). Do not rename `section.col`,
  `.col-head`, or `data-droppable-id` — five shipped e2e specs key on them.
- Result-shape law (~/.claude/rules/core.md): the guard module returns a discriminated union and
  never throws; the surrounding mutation API keeps its existing exception style — do not mix
  styles inside one API surface.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The commitment-stage guard module + its behavioural oracle (interface-first)</name>
  <files>frontend/src/pages/WorkBoard/commitment-stage-guard.ts, frontend/src/pages/WorkBoard/__tests__/commitment-stage-guard.test.ts</files>
  <read_first>useUnifiedKanban.ts:303-327 (the pattern); WorkBoard.tsx:60-110; work-item.types.ts (the commitment item shape)</read_first>
  <behavior>
    - todo→'pending', in_progress→'in_progress', done→'completed' map for a non-past-due commitment → { ok: true, status }.
    - Any path resolving stage 'review' for a commitment → { ok: false, reason: 'no_review_counterpart' } (including a drop onto a card sitting in Review — the caller passes the resolved stage).
    - A commitment whose due date is before today, target status pending or in_progress → { ok: false, reason: 'past_due_coercion' } (the COERCION CASE — mandatory per RULING-P94-03 order 2).
    - Same past-due commitment, target 'completed' → { ok: true } (Done is the one drag the trigger permits).
    - Boundary: due date exactly today → ok (the trigger fires only when due_date < CURRENT_DATE).
    - The module never returns or writes 'overdue'.
  </behavior>
  <action>
    BEFORE finalizing the map, RE-DERIVE the constraint live (D-03 orders this even against this
    plan's own text): via Supabase MCP execute_sql (read-only):
    SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conrelid='public.aa_commitments'::regclass AND conname='aa_commitments_status_check';
    Paste the result into the task's commit message body and the SUMMARY. Expected five values; if
    it differs, STOP and surface — do not adapt silently.
    Create `commitment-stage-guard.ts`: export COMMITMENT_STAGE_TO_STATUS (todo/in_progress/done
    cells only) and resolveCommitmentDropDecision(item, targetStage) returning the discriminated
    union { ok: true, status } | { ok: false, reason: 'no_review_counterpart' | 'past_due_coercion' }.
    The past-due predicate mirrors the trigger condition per the interfaces block — comment the
    SQL condition verbatim above the predicate with a pointer to check_commitment_overdue() so a
    future editor sees the mirror contract. Never throws (result-union law). Write the unit test
    per the behavior block.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/pages/WorkBoard/__tests__/commitment-stage-guard.test.ts && grep -q 'no_review_counterpart' src/pages/WorkBoard/commitment-stage-guard.ts && grep -q 'past_due_coercion' src/pages/WorkBoard/commitment-stage-guard.ts && test "$(grep -cE "status: *'overdue'" src/pages/WorkBoard/commitment-stage-guard.ts)" -eq 0 && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>Guard test green covering BOTH reject reasons (coercion case included), the Done-permitted case, and the today-boundary; the module exports the two names 94-08 will consume; live re-derivation pasted; type-check clean.</acceptance_criteria>
  <done>One condition, one module — the contract both enforcement points share exists and is proven.</done>
</task>

<task type="auto">
  <name>Task 2: Mutation-layer reject + D-08 onError repair + four bilingual keys</name>
  <files>frontend/src/hooks/useUnifiedKanban.ts, frontend/src/i18n/en/unified-kanban.json, frontend/src/i18n/ar/unified-kanban.json</files>
  <read_first>useUnifiedKanban.ts:290-500 in full; 94-UI-SPEC.md §Copywriting Contract (both key tables — the copy is the contract)</read_first>
  <action>
    (1) Keys first, BOTH files in the same edit: errors.commitmentNoReviewStage,
    errors.commitmentNoReviewStageDescription, errors.commitmentPastDue,
    errors.commitmentPastDueDescription — EN copy verbatim from UI-SPEC; AR from its table with
    terminology aligned to the shipped columns.* glossary (للتنفيذ / قيد التنفيذ / مكتمل).
    Sentence case, no emoji, no exclamation marks.
    (2) The commitment status-update path calls resolveCommitmentDropDecision BEFORE any mutation
    is enqueued. On { ok: false }: render the cause-specific toast (title + description keys,
    COLON form, role="alert" via ariaProps — extend useToast or call toast.error directly) and
    RETURN — no mutation object is created, so no optimistic update, no global onSuccess, no
    global onError, no snap-back. On { ok: true }: write decision.status (never the raw stage).
    (3) Defence in depth: the mutationFn's commitment branch re-asserts the guard and throws its
    existing error style if somehow reached with an invalid target (a throw here is the current
    API's style — do not convert the whole hook to result unions).
    (4) D-08 repair at :481-485: replace the hardcoded English title + error.message description
    with the EXISTING keys unified-kanban:errors.updateFailed + errors.updateFailedDescription
    (both locales already present — zero new keys); raw error goes to console.error only.
  </action>
  <verify>
    <automated>cd frontend && grep -q 'resolveCommitmentDropDecision' src/hooks/useUnifiedKanban.ts && grep -q 'errors.updateFailed' src/hooks/useUnifiedKanban.ts && test "$(grep -cE "description: *error\.message" src/hooks/useUnifiedKanban.ts)" -eq 0 && node -e "const en=require('./src/i18n/en/unified-kanban.json'),ar=require('./src/i18n/ar/unified-kanban.json');const flat=(o,p=[])=>Object.entries(o||{}).flatMap(([k,v])=>typeof v==='object'&&v!==null?flat(v,[...p,k]):[[...p,k].join('.')]);const ek=new Set(flat(en.errors)),ak=new Set(flat(ar.errors));const sym=[...ek].filter(k=>!ak.has(k)).concat([...ak].filter(k=>!ek.has(k)));if(sym.length){console.error('key-set mismatch:',sym);process.exit(1)};const need=['commitmentNoReviewStage','commitmentNoReviewStageDescription','commitmentPastDue','commitmentPastDueDescription'];process.exit(need.every(k=>typeof en.errors[k]==='string'&&typeof ar.errors[k]==='string'&&en.errors[k]!==ar.errors[k])?0:1)" && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>Both reject causes reach the DOM as distinct translated strings with role="alert"; the errors key SETS stay equal EN/AR and all four new keys resolve with differing values; no toast interpolates error.message; a refusal enqueues no mutation (asserted behaviourally in Task 3's board test).</acceptance_criteria>
  <done>The mutation layer refuses exactly what the DB would reject or coerce, in the user's language, before the write.</done>
</task>

<task type="auto">
  <name>Task 3: No-op guard fix + dead-branch removal + board-level oracle</name>
  <files>frontend/src/pages/WorkBoard/WorkBoard.tsx, frontend/src/pages/WorkBoard/__tests__/WorkBoard.test.tsx</files>
  <read_first>WorkBoard.tsx:60-110 and :290-320; WorkBoard.test.tsx in full (the onDragEnd capture boundary)</read_first>
  <action>
    (1) `:305-311` — replace `targetStage === item.workflow_stage` with
    `targetStage === resolveBoardStage(item)` (the function that placed the card). (2) Remove the
    dead commitment-review branch at :97-98 (`item.status === 'review'` can never be true —
    cleanup authorized by the PARK-94-01 ruling landing). (3) Update WorkBoard.test.tsx (its
    fixture items carry workflow_stage — align with the new comparison) and ADD, via the captured
    onDragEnd with mocked mutate: (a) own-column commitment drop → mutate NOT called AND no toast
    fired (absence of both, per D-32 — never absence-of-error); (b) commitment drop resolving
    review → mutate NOT called; (c) past-due commitment drop to in_progress → mutate NOT called.
    (b)/(c) may live at whichever boundary the capture reaches — the assertion is that no write
    fires. (4) Do NOT touch query-client.ts (D-07/COPY-06 — Phase 98); do NOT rename section.col /
    .col-head / data-droppable-id.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/pages/WorkBoard/__tests__/WorkBoard.test.tsx && grep -q 'resolveBoardStage(item)' src/pages/WorkBoard/WorkBoard.tsx && test "$(grep -c 'item\.workflow_stage ===\|=== item\.workflow_stage' src/pages/WorkBoard/WorkBoard.tsx)" -eq 0 && git rev-parse -q --verify refs/tags/phase-94-base >/dev/null && test -z "$(git diff --name-only phase-94-base -- frontend/src/lib/query-client.ts)" && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>Board tests green including the three new absence assertions; the no-op guard reads resolveBoardStage; query-client.ts is byte-identical to phase-94-base (C7-anchored, not HEAD); selector contract untouched.</acceptance_criteria>
  <done>An own-column drop is a recognized no-op and the false success toast on it is gone.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                                          |
| --------------------------- | ------------------------------------------------------------------------------------ |
| drag gesture → DB write     | client guard mirrors the DB state machine; DB CHECK + trigger remain the enforcement |
| supabase error → toast copy | CHECK-violation strings must never reach the DOM                                     |

## STRIDE Threat Register

| Threat ID | Category               | Component               | Disposition | Mitigation Plan                                                                                                    |
| --------- | ---------------------- | ----------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------ |
| T-94-04   | Information Disclosure | kanban onError toast    | mitigate    | error.message removed from DOM (existing updateFailed keys); raw to console.error (D-08, LEAK-ATTACH-01 class)     |
| T-94-05   | Tampering              | commitment status write | mitigate    | guard never writes 'overdue' or 'review'; DB CHECK + trigger unchanged; guard mirrors, never replaces, the DB rule |
| T-94-SC   | Tampering              | package installs        | accept      | zero installs this phase                                                                                           |

</threat_model>

<verification>
- Guard unit test (both rejects + coercion case + boundary), board test (three absence assertions),
  i18n key-set equality + four bilingual keys, no error.message in DOM path, query-client untouched.
- The live read-back persistence probe and the affordance oracle are Plan 94-08's (producer→consumer
  order, D-25); this plan's oracles are the client-side refusal half.
</verification>

<success_criteria>
The criterion invariant (RULING-P94-03 order 2) holds at the mutation layer: permitted drags write
the mapped status; review and would-be-coerced drags are refused before the write with distinct
bilingual role="alert" reasons; own-column drops fire nothing. "No error shown" passes nothing.
</success_criteria>

<output>
Create `.planning/phases/94-write-paths/94-03-SUMMARY.md` when done.
</output>
