---
phase: 63-relationship-graph-route-bidirectional-traversal
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/graph-edge-orientation.ts
  - frontend/src/pages/relationships/__tests__/edge-orientation.test.ts
  - frontend/src/lib/dossier-routes.test.ts
  - frontend/src/components/relationships/GraphVisualization.tsx
  - frontend/src/i18n/en/graph.json
  - frontend/src/i18n/ar/graph.json
autonomous: true
requirements: [GRAPH-02, GRAPH-03]

must_haves:
  truths:
    - 'A pure, unit-tested helper pins the D-04 edge-orientation contract: direction_path "incoming" swaps source/target, missing direction_path degrades to all-outgoing, duplicate (source, target, type) edges dedup'
    - 'getDossierDetailPath is proven for all 8 dossier types + the undefined-type fallback — the GRAPH-03 per-type path contract is regression-guarded'
    - 'Basic-mode graph edges carry an ArrowClosed marker — no undirected lines in any of the 3 viz modes (D-04)'
    - 'graph.json EN values consumed by the page + mini-graph are sentence case (Bureau voice, D-06) and browseDossiers exists in EN + AR for the D-02 link'
  artifacts:
    - path: 'frontend/src/lib/graph-edge-orientation.ts'
      provides: 'buildGraphEdges — the executable spec mirroring the edge-function transform'
      exports: ['buildGraphEdges']
    - path: 'frontend/src/pages/relationships/__tests__/edge-orientation.test.ts'
      provides: 'Wave 0 GRAPH-02/D-04 client-side orientation test'
      min_lines: 40
    - path: 'frontend/src/lib/dossier-routes.test.ts'
      provides: 'Wave 0 GRAPH-03 per-type path test (8 types + fallback)'
      min_lines: 20
  key_links:
    - from: 'frontend/src/pages/relationships/__tests__/edge-orientation.test.ts'
      to: 'frontend/src/lib/graph-edge-orientation.ts'
      via: 'import { buildGraphEdges }'
      pattern: 'buildGraphEdges'
    - from: 'frontend/src/components/relationships/GraphVisualization.tsx'
      to: '@xyflow/react'
      via: 'markerEnd MarkerType.ArrowClosed on every Basic-mode edge'
      pattern: 'MarkerType.ArrowClosed'
---

<objective>
Close the remaining Wave 0 test gaps (edge-orientation contract for GRAPH-02/D-04, per-type path test for GRAPH-03), add the missing directional arrow to Basic mode, and do the value-level i18n sweep (sentence case + the browseDossiers key).

Purpose: pins the two contracts the rest of the phase builds against — the orientation transform that plan 63-04 ports into the edge function, and the per-type navigation the live D-09 walkthrough verifies.
Output: 2 new test files + 1 pure helper, Basic-mode arrows, corrected graph.json EN/AR values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/63-relationship-graph-route-bidirectional-traversal/63-PATTERNS.md

<interfaces>
<!-- Contracts this plan creates (consumed by plan 63-04) and uses. -->

NEW — frontend/src/lib/graph-edge-orientation.ts must export exactly:

```typescript
export interface TraversalRow {
  path: string[]
  relationship_path: string[]
  direction_path?: string[]
}
export interface OrientedEdge {
  source_id: string
  target_id: string
  relationship_type: string
}
export function buildGraphEdges(rows: TraversalRow[]): OrientedEdge[]
```

From frontend/src/lib/dossier-routes.ts (L12-21) — the 8-entry map under test:
DOSSIER_TYPE_TO_ROUTE: country→countries, organization→organizations, person→persons,
engagement→engagements, forum→forums, working_group→working_groups, topic→topics,
elected_official→elected-officials; undefined/unknown type falls back to countries.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Edge-orientation contract — pure helper + Wave 0 test</name>
  <files>frontend/src/lib/graph-edge-orientation.ts, frontend/src/pages/relationships/__tests__/edge-orientation.test.ts</files>
  <read_first>
    - supabase/functions/graph-traversal/index.ts (L146-168 — the edge-building loop being mirrored; the helper is its executable spec)
    - frontend/src/lib/person-display.test.ts (the repo's pure-helper test analog: AAA structure, naming)
    - .planning/phases/63-relationship-graph-route-bidirectional-traversal/63-RESEARCH.md (§Pattern 3 — the exact orientation rule: isIncoming = direction_path?.[i] === 'incoming' swaps source/target)
  </read_first>
  <behavior>
    - Outgoing hop keeps path order: row {path:[A,B], relationship_path:['member_of'], direction_path:['outgoing']} → [{source_id:A, target_id:B, relationship_type:'member_of'}]
    - Incoming hop swaps: direction_path ['incoming'] → [{source_id:B, target_id:A, relationship_type:'member_of'}] — the arrow points AT the focused dossier (D-04 headline case)
    - Mixed multi-hop: path [A,B,C], directions ['outgoing','incoming'] → edges A→B and C→B
    - Missing direction_path (legacy RPC during the deploy window) degrades to all-outgoing — no crash (RESEARCH Pitfall 3 guard)
    - Duplicate (source_id, target_id, relationship_type) across rows is emitted once (dedup also collapses the same physical row surfaced from both traversal directions)
  </behavior>
  <action>
    1. Create `frontend/src/lib/graph-edge-orientation.ts` (kebab-case — lib/** ESLint filename rule) with the exact exports in the interfaces block. Implementation mirrors the edge-fn loop: for each row with path.length > 1 and relationship_path, iterate i over hops; `const isIncoming = row.direction_path?.[i] === 'incoming'`; source = isIncoming ? path[i+1] : path[i]; target = isIncoming ? path[i] : path[i+1]; skip if an identical (source_id, target_id, relationship_type) edge was already emitted. Header comment MUST state: "Executable spec for the edge-building transform in supabase/functions/graph-traversal/index.ts — keep both in lockstep." Explicit return types, immutable patterns (build a new array, no mutation of inputs).
    2. Create `frontend/src/pages/relationships/__tests__/edge-orientation.test.ts` covering the 5 behavior cases in AAA style per the person-display.test.ts analog. Write tests first, watch them fail (no helper yet), then implement the helper to GREEN — atomic commits per the repo TDD convention.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run src/pages/relationships/__tests__/edge-orientation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && pnpm vitest run src/pages/relationships/__tests__/edge-orientation.test.ts` exits 0 with >= 5 passing test cases
    - graph-edge-orientation.ts exports buildGraphEdges, TraversalRow, OrientedEdge with the exact signatures in the interfaces block
    - The helper contains `direction_path?.[i]` (optional-chained guard) and the lockstep header comment naming supabase/functions/graph-traversal/index.ts
    - `cd frontend && pnpm type-check` exits 0
  </acceptance_criteria>
  <done>The D-04 orientation contract is pinned by a passing unit test; plan 63-04 ports this exact logic into the edge function.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GRAPH-03 per-type path test (Wave 0)</name>
  <files>frontend/src/lib/dossier-routes.test.ts</files>
  <read_first>
    - frontend/src/lib/dossier-routes.ts (the module under test — DOSSIER_TYPE_TO_ROUTE L12-21, getDossierDetailPath L45-47, getDossierRouteSegment)
    - frontend/src/lib/person-display.test.ts (sibling test convention — same directory, kebab-case filename)
  </read_first>
  <behavior>
    - getDossierDetailPath('id-1', 'country') → '/dossiers/countries/id-1' — and equivalently for all 8 types: organization→organizations, person→persons, engagement→engagements, forum→forums, working_group→working_groups, topic→topics, elected_official→elected-officials
    - getDossierDetailPath('id-1', undefined) → '/dossiers/countries/id-1' (documented fallback)
    - getDossierDetailPath('id-1', 'not-a-type') → '/dossiers/countries/id-1' (unknown-type fallback)
  </behavior>
  <action>
    Create `frontend/src/lib/dossier-routes.test.ts`. Use a table-driven `it.each` (or 8 explicit cases) over the full type→segment map plus the two fallback cases. This is the GRAPH-03 regression guard — the helper is already correct (R17-02); the test pins it so the full-page graph and mini-graph can never diverge silently. AAA structure, explicit return types in any helper closures.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run src/lib/dossier-routes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && pnpm vitest run src/lib/dossier-routes.test.ts` exits 0
    - The test file contains all 8 segment strings: countries, organizations, persons, engagements, forums, working_groups, topics, elected-officials
    - At least one assertion covers the undefined-type fallback to countries
  </acceptance_criteria>
  <done>Per-type navigation paths are regression-guarded for all 8 dossier types.</done>
</task>

<task type="auto">
  <name>Task 3: Basic-mode arrow marker + graph.json sentence-case sweep + browseDossiers key</name>
  <files>frontend/src/components/relationships/GraphVisualization.tsx, frontend/src/i18n/en/graph.json, frontend/src/i18n/ar/graph.json</files>
  <read_first>
    - frontend/src/components/relationships/GraphVisualization.tsx (L215-232 — the Basic-mode edge map missing markerEnd)
    - frontend/src/components/dossier/MiniRelationshipGraph.tsx (L27 MarkerType import, L358-383 — the markerEnd shape to copy)
    - frontend/src/i18n/en/graph.json + frontend/src/i18n/ar/graph.json (the values being edited — keys stay identical)
    - .planning/phases/63-relationship-graph-route-bidirectional-traversal/63-PATTERNS.md (§graph.json — exact sentence-case target list)
  </read_first>
  <action>
    1. GraphVisualization.tsx (D-04 — Basic mode is the only viz mode with NO arrow): import `MarkerType` from `@xyflow/react` and add to every edge in the Basic-mode `edges` useMemo map: `markerEnd: { type: MarkerType.ArrowClosed, width: 10, height: 10, color: getEdgeColor(edge.relationship_type) }`. No RTL flip, no `.reverse()` — arrows are canvas semantics (RESEARCH anti-pattern). Touch nothing else in the file.
    2. graph.json EN sentence-case sweep (D-06 Bureau voice — values only, keys unchanged): `title` → 'Relationship graph', `maxDegrees` → 'Degrees of separation', `relationshipType` → 'Relationship type', `allTypes` → 'All types', `graphView` → 'Graph view', `listView` → 'List view', `maxDegree` → 'Max degree', `queryTime` → 'Query time', `miniGraph.viewFullGraph` → 'View full graph', plus any other Title Case values among the 24 page-consumed keys (description, noDossier, refresh, basicMode, enhancedMode, advancedMode, nodes, edges, error, degree, degrees, complexity.*). LEAVE deep viz-internal keys (`layout`, `pathFinding`, `timeAnimation`) untouched to limit diff surface (RESEARCH Open Question 1 recommendation).
    3. Add `browseDossiers` to both files for the D-02 link: EN 'Browse dossiers', AR 'تصفح الملفات'. AR values otherwise untouched (verified present and idiomatic).
    4. Both JSON files must parse; run the relationships test directory to confirm nothing keyed off old values.
  </action>
  <verify>
    <automated>cd frontend && node -e "const en=require('./src/i18n/en/graph.json'),ar=require('./src/i18n/ar/graph.json');if(en.browseDossiers!=='Browse dossiers')throw new Error('en browseDossiers');if(typeof ar.browseDossiers!=='string'||ar.browseDossiers.length===0)throw new Error('ar browseDossiers');if(en.title!=='Relationship graph')throw new Error('title not sentence case');console.log('JSON_OK')" && grep -c 'MarkerType.ArrowClosed' src/components/relationships/GraphVisualization.tsx | grep -qv '^0$' && pnpm vitest run src/pages/relationships</automated>
  </verify>
  <acceptance_criteria>
    - Node JSON assertion passes: en.browseDossiers === 'Browse dossiers', ar.browseDossiers is a non-empty string, en.title === 'Relationship graph'
    - en graph.json values for maxDegrees, allTypes, graphView, listView, maxDegree, queryTime, miniGraph.viewFullGraph are sentence case ('Degrees of separation', 'All types', 'Graph view', 'List view', 'Max degree', 'Query time', 'View full graph')
    - GraphVisualization.tsx contains `MarkerType.ArrowClosed` inside the Basic-mode edges useMemo and imports MarkerType from '@xyflow/react'
    - GraphVisualization.tsx contains zero occurrences of `.reverse()`
    - `cd frontend && pnpm vitest run src/pages/relationships` exits 0 and `pnpm type-check` exits 0
  </acceptance_criteria>
  <done>All 3 viz modes render directed arrows; page-facing copy is Bureau sentence case in EN with the D-02 key present in both languages.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                       |
| -------------------------- | ----------------------------------------------------------------- |
| API rows → buildGraphEdges | traversal rows from the edge function are transformed client-side |

## STRIDE Threat Register

| Threat ID | Category          | Component                         | Disposition | Mitigation Plan                                                                                                                                        |
| --------- | ----------------- | --------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-63-08   | Denial of Service | buildGraphEdges on malformed rows | mitigate    | Guard `path.length > 1 && relationship_path` before iterating; optional-chained `direction_path?.[i]`; covered by the missing-direction_path test case |
| T-63-09   | Tampering         | i18n JSON values rendered as text | accept      | i18next interpolation escapes by default; values render as React text nodes; no HTML in the edited values                                              |
| T-63-SC   | Tampering         | npm/pip/cargo installs            | accept      | No package installs in this plan (MarkerType comes from the already-installed @xyflow/react)                                                           |

</threat_model>

<verification>
- VALIDATION.md rows realized: "Edge orientation: direction_path 'incoming' swaps source/target" → Task 1; "handleNodeSelect produces /dossiers/<segment>/<id> for each of the 8 types" → Task 2
- `cd frontend && pnpm vitest run src/pages/relationships src/lib && pnpm type-check` exits 0 at plan end
</verification>

<success_criteria>

- Both remaining Wave 0 test gaps are closed with passing tests
- D-04 has no undirected mode left; D-06 copy voice corrected at the bundle level
- The orientation contract exists as importable, documented code for the 63-04 executor
  </success_criteria>

<output>
Create `.planning/phases/63-relationship-graph-route-bidirectional-traversal/63-03-SUMMARY.md` when done.
</output>
