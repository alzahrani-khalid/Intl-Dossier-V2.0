---
phase: 63-relationship-graph-route-bidirectional-traversal
plan: 05
type: execute
wave: 3
depends_on: [63-01, 63-02, 63-03, 63-04]
files_modified: []
autonomous: true
requirements: [GRAPH-01, GRAPH-02, GRAPH-03]

must_haves:
  truths:
    - 'Staging dossier_relationships contains seeded active rows so the rendered graph holds nodes of all 7 reachable dossier types (country, organization, forum, engagement, topic, working_group, person — no elected_official dossiers exist on staging; EO is a person subtype) with a mix of incoming and outgoing edges relative to the anchor (D-09)'
    - 'Clicking the mini-graph "View full graph" button on a dossier lands on /relationships/graph rendering that dossier graph — the loop is closed live (GRAPH-01, D-08)'
    - 'Clicking each node type in the live graph navigates to the correct /dossiers/<segment>/$id route — all types verified, Phase 62 precedent (GRAPH-03, D-09)'
    - 'An incoming reference renders with the arrow pointing AT the focused dossier on the live page (GRAPH-02, D-04)'
    - 'In Arabic mode the page chrome reads from graph.json AR (title مخطط العلاقات), dir=rtl applies, and node labels render name_ar (D-07); the page holds at 1024px and 1400px (D-06); degree 2→3 and the canonical type filter behave with the seeded volume (D-05)'
  artifacts:
    - path: '.planning/phases/63-relationship-graph-route-bidirectional-traversal/63-05-SUMMARY.md'
      provides: 'Seed inventory + per-type click-through matrix (type, node id, expected route, observed route) + AR/width evidence + suite/size gate results'
  key_links:
    - from: 'MiniRelationshipGraph "View full graph" button'
      to: '/relationships/graph?dossierId=<anchor>'
      via: 'live browser click (agent-browser)'
      pattern: 'relationships/graph'
    - from: 'graph page node click'
      to: '/dossiers/<segment>/$id'
      via: 'getDossierDetailPath (verified live per type)'
      pattern: 'dossiers/'
---

<objective>
Seed staging with relationships covering every reachable dossier type, then run the live all-types verification: entry from the mini-graph, per-type node click-through, incoming-arrow direction, AR/RTL, DoD widths, degree/filter behavior — plus the wave-gate suite and bundle-size checks.

Purpose: D-09 mandates live verification of every dossier type present in the graph; staging has exactly 1 relationship row today, so verification is impossible without seeding (RESEARCH Pitfall 6).
Output: seeded staging graph + recorded verification matrix in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/63-relationship-graph-route-bidirectional-traversal/63-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed staging relationships covering all 7 reachable types (idempotent, canonical vocabulary)</name>
  <files>none (live database operation via Supabase MCP)</files>
  <read_first>
    - .planning/phases/63-relationship-graph-route-bidirectional-traversal/63-01-SUMMARY.md (anchor ids captured during the migration probes)
    - .planning/phases/63-relationship-graph-route-bidirectional-traversal/63-RESEARCH.md (§Live Staging Verification — dossier-type inventory; §Pitfall 6 — seeding constraints; §Anti-Patterns — canonical type strings only)
    - frontend/src/types/relationship.types.ts (L18-36 — the canonical DossierRelationshipType values; dossier_relationships.relationship_type is free TEXT with NO CHECK constraint, so only discipline keeps labels resolving)
  </read_first>
  <action>
    Via Supabase MCP `execute_sql` against staging `zkrcjzdemdmwhearhfgg`:
    1. Inventory: select one dossier id per type with `status != 'deleted'` AND `sensitivity_level <= 2` (test-user visibility, RESEARCH A3) for: country (use Indonesia `b0000001-0000-0000-0000-000000000001` as the ANCHOR), organization, forum, engagement, topic, person. The working_group anchor side already exists (the live `cooperates_with` row, incoming to Indonesia).
    2. Seed with INSERT ... SELECT guarded by `WHERE NOT EXISTS` on (source_dossier_id, target_dossier_id, relationship_type) — idempotent, `status = 'active'`, `effective_to` NULL. Rows (mix of directions relative to the anchor, canonical types only):
       - Indonesia →`member_of`→ <organization> (outgoing)
       - Indonesia →`participates_in`→ <forum> (outgoing)
       - <engagement> →`involves`→ Indonesia (incoming)
       - <topic> →`discusses`→ Indonesia (incoming)
       - <person> →`represents`→ Indonesia (incoming)
       - <organization> →`participates_in`→ <forum> (degree-2 path from the anchor, for the D-05 degree test)
    3. Verify counts: `SELECT relationship_type, COUNT(*) FROM dossier_relationships WHERE status = 'active' GROUP BY 1` — expect >= 7 active rows total; then a user-JWT RPC probe from the anchor at max_degrees 2 must return nodes covering all 7 types (working_group via the pre-existing row).
    4. Record every seeded row (source id, target id, type) in the SUMMARY for future cleanup attribution.
  </action>
  <verify>
    <automated>MISSING — live-DB seeding; the gate is the RPC probe in step 3 returning all 7 dossier_type values, captured as SUMMARY evidence.</automated>
  </verify>
  <acceptance_criteria>
    - All seeded relationship_type values are members of the DossierRelationshipType union (no invented strings — graph.json labels must resolve)
    - Re-running the seed SQL inserts 0 additional rows (idempotency proven by a second execution)
    - User-JWT RPC probe from b0000001-0000-0000-0000-000000000001 (max_degrees 2) returns rows whose dossier_type set = {country, organization, forum, engagement, topic, working_group, person} minus the anchor type, and at least one row has direction_path starting with 'incoming' and one with 'outgoing'
    - Every seeded dossier has sensitivity_level <= 2
  </acceptance_criteria>
  <done>The anchor dossier graph contains all 7 reachable types in both directions, visible to the test user.</done>
</task>

<task type="auto">
  <name>Task 2: Live browser walkthrough — entry, per-type node clicks, arrow direction, controls (D-09, D-05)</name>
  <files>none (live verification via agent-browser)</files>
  <read_first>
    - .claude/skills/agent-browser/SKILL.md if present, else use the CLAUDE.md §Browser Automation workflow (open → snapshot -i → click @ref → re-snapshot)
    - .env.test (TEST_USER_EMAIL / TEST_USER_PASSWORD for login; NEVER echo values)
  </read_first>
  <action>
    1. Ensure the dev server is up: `cd frontend && pnpm dev` in the background if :5173 is not responding (the app proxies to staging Supabase; backend Express is not needed for this page). Login at http://localhost:5173 with the .env.test credentials.
    2. Entry contract (GRAPH-01/D-08): navigate to the anchor dossier detail (`/dossiers/countries/b0000001-0000-0000-0000-000000000001`), find the mini-graph "View full graph" button, click it, and confirm the URL is `/relationships/graph?dossierId=b0000001-...` and the graph page renders (no redirect to /dossiers).
    3. Per-type click-through (GRAPH-03/D-09): with maxDegrees = 2, for EACH node type present (organization, forum, engagement, topic, working_group, person — and country by entering from a non-country node graph or clicking back to the anchor), click the node and assert the resulting URL matches `/dossiers/<segment>/<uuid>` with the correct segment per type, and the detail page renders. Navigate back to the graph between clicks. Record a matrix row per type.
    4. Arrow direction (GRAPH-02/D-04): on the anchor's graph, confirm the working_group→Indonesia edge renders with the arrow INTO Indonesia and the edge label reads as working_group cooperates_with Indonesia (not reversed — RESEARCH Pitfall 2 warning sign).
    5. Controls (D-05): switch degrees 2 → 3 and confirm the graph re-renders without error and the degree-2 organization→forum path appears; select relationship-type filter `cooperates_with` and confirm the graph reduces to that edge; reset to all types.
    6. No-dossier state (D-02): open `/relationships/graph` with no search param — confirm the alert plus the Browse-dossiers link, click it, land on /dossiers.
    7. Consumer blast-radius smoke: open `/stakeholder-influence` and confirm the page renders without an error boundary (it consumes the rewritten RPC).
    8. Capture screenshots/snapshots as evidence at each step.
  </action>
  <verify>
    <automated>MISSING — live visual navigation, manual-only justified (D-09, Phase 62 all-types precedent); evidence = the per-type matrix + screenshots referenced in the SUMMARY.</automated>
  </verify>
  <acceptance_criteria>
    - Mini-graph button click lands on /relationships/graph?dossierId=... rendering the graph (no redirect)
    - The click-through matrix shows every present node type navigating to its correct segment: organizations, forums, engagements, topics, working_groups, persons, countries
    - The incoming edge's arrow points at the focused dossier with a non-reversed label
    - Degree 3 renders the seeded second-degree node; the cooperates_with filter isolates the expected edge; no console error boundary at any step
    - /relationships/graph without dossierId shows the alert + working Browse-dossiers link
    - /stakeholder-influence renders post-rewrite
  </acceptance_criteria>
  <done>Every roadmap success criterion is observed live on staging data, per-type, from the contract entry point.</done>
</task>

<task type="auto">
  <name>Task 3: AR/RTL + width verification and wave gates (suite, type-check, bundle size)</name>
  <files>none (live verification + repo-level gates)</files>
  <read_first>
    - CLAUDE.md (§Definition of Done — UI checklist; §Arabic RTL guidelines — language persists under localStorage id.locale, switch via the topbar ع button, NOT i18nextLng)
  </read_first>
  <action>
    1. AR/RTL (D-07): on the live graph page, click the topbar `ع` button. Assert: document dir=rtl, the page title renders `مخطط العلاقات` (proves the 63-02 namespace fix live — the page previously stayed English in AR), controls/legend/alerts show Arabic strings, node labels render name_ar, and the filter labels resolve from `relationship.*` AR values. Switch back to EN afterward.
    2. Widths (D-06): screenshot the graph page at 1024px and 1400px viewport widths in EN, and at least 1280px in AR — no horizontal overflow, controls usable, no clipped legend.
    3. Wave gates: `cd frontend && pnpm vitest run` (full suite) and `pnpm type-check` — both exit 0. Then `cd frontend && pnpm exec size-limit` and grep the FULL output for every `exceeded` line (failures stack hidden — project memory): zero matches required. If a budget trips, record exact numbers and stop for budget discussion rather than silently bumping .size-limit.json.
    4. Compile the SUMMARY: seed inventory, click-through matrix, AR/width evidence, gate outputs, and the deliberate exclusions (CORS tightening, database.types.ts regen, *DossierDetail disposition → Phase 67 per RESEARCH component-disposition table).
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run && pnpm type-check && pnpm exec size-limit 2>&1 | tee /tmp/size-limit-63.log && ! grep -i 'exceeded' /tmp/size-limit-63.log</automated>
  </verify>
  <acceptance_criteria>
    - Live AR assertions hold: dir=rtl, title مخطط العلاقات, Arabic node labels (screenshots referenced in SUMMARY)
    - 1024px and 1400px screenshots show no overflow or clipped controls
    - `cd frontend && pnpm vitest run` and `pnpm type-check` exit 0
    - size-limit output contains zero `exceeded` lines (case-insensitive, whole output)
  </acceptance_criteria>
  <done>D-06/D-07 verified live, full suite green, bundle budgets intact — the phase is ready for /gsd:verify-work and human UAT.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                |
| ---------------------------------- | ---------------------------------------------------------- |
| seed SQL → staging database        | planner-authored DML executed with elevated MCP privileges |
| test credentials → browser session | .env.test secrets used for live login                      |

## STRIDE Threat Register

| Threat ID | Category               | Component                               | Disposition | Mitigation Plan                                                                                                                                                             |
| --------- | ---------------------- | --------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-63-14   | Tampering              | seed inserts                            | mitigate    | Fixed-value INSERT ... SELECT with WHERE NOT EXISTS idempotency; only canonical relationship_type strings; every seeded row recorded in the SUMMARY for attribution/cleanup |
| T-63-15   | Information Disclosure | credentials leaking into evidence       | mitigate    | Source .env.test as env vars; acceptance criteria forbid token/password values in SUMMARY or screenshots of credential fields                                               |
| T-63-16   | Information Disclosure | seeded nodes exceeding viewer clearance | mitigate    | Seeds constrained to sensitivity_level <= 2 (RESEARCH A3); the DEFINER RPC clearance predicate (verified in 63-01) filters anything above the caller's level                |
| T-63-SC   | Tampering              | npm/pip/cargo installs                  | accept      | No package installs in this plan                                                                                                                                            |

</threat_model>

<verification>
- VALIDATION.md rows realized: "All-types live click-through on staging" (D-09, manual-only justified) → Task 2; per-wave gates (full vitest + size-limit) → Task 3
- Phase gate per VALIDATION.md: full suite green + staging probes bidirectional (63-01/63-04 SUMMARYs) + this click-through complete before /gsd:verify-work
</verification>

<success_criteria>

- All three roadmap success criteria observed live: page renders from a dossier, incoming edge visible with correct arrow, per-type node navigation correct for every type present
- D-05/D-06/D-07/D-09 evidence recorded; no CI-blocking gate (suite, types, bundle) left red
  </success_criteria>

<output>
Create `.planning/phases/63-relationship-graph-route-bidirectional-traversal/63-05-SUMMARY.md` when done (matrix + evidence, no secrets).
</output>
