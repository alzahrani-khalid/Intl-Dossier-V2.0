---
phase: 63-relationship-graph-route-bidirectional-traversal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260612100000_bidirectional_traverse_in_place.sql
autonomous: true
requirements: [GRAPH-02]

must_haves:
  truths:
    - 'POST /rest/v1/rpc/traverse_relationship_graph from the incoming-only anchor (Indonesia, b0000001-0000-0000-0000-000000000001) returns at least 1 row with direction_path containing "incoming" — today it returns [] (live-proven outgoing-only bug, D-03)'
    - 'The same RPC from the outgoing anchor (the working_group source of the existing live row) still returns the target with direction_path ["outgoing"] — no regression for existing callers'
    - 'The recreated function is SECURITY DEFINER with search_path pinned to public and EXECUTE granted to authenticated (an authenticated-JWT probe succeeds)'
    - 'traverse_relationship_graph_bidirectional is untouched — graph-traversal-advanced (L229) and get_graph_statistics (migration L797) still depend on it'
  artifacts:
    - path: 'supabase/migrations/20260612100000_bidirectional_traverse_in_place.sql'
      provides: 'DROP + CREATE of traverse_relationship_graph with legacy input signature + direction_path TEXT[] output column + re-GRANT'
      contains: 'DROP FUNCTION IF EXISTS traverse_relationship_graph'
  key_links:
    - from: 'supabase/migrations/20260612100000_bidirectional_traverse_in_place.sql'
      to: 'staging zkrcjzdemdmwhearhfgg'
      via: 'Supabase MCP apply_migration'
      pattern: 'apply_migration'
    - from: 'traverse_relationship_graph'
      to: 'dossier_relationships'
      via: '4-branch recursive CTE (outgoing+incoming anchors, outgoing+incoming recursion)'
      pattern: 'target_dossier_id = start_dossier_id'
---

<objective>
Redefine `traverse_relationship_graph` in place (D-03) so it walks BOTH directions and emits a `direction_path TEXT[]` column per hop, then apply the migration to live staging. This is the data-layer half of GRAPH-02 — the live RPC is proven outgoing-only (traversal from an incoming-only dossier returns `[]` today).

Purpose: GRAPH-02 / roadmap success criterion 2 — a dossier referenced by another must show that incoming edge.
Output: new migration file on disk + applied to staging, verified by authenticated curl probes in both directions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/63-relationship-graph-route-bidirectional-traversal/63-RESEARCH.md
@.planning/phases/63-relationship-graph-route-bidirectional-traversal/63-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the bidirectional in-place migration (DROP + CREATE + GRANT)</name>
  <files>supabase/migrations/20260612100000_bidirectional_traverse_in_place.sql</files>
  <read_first>
    - supabase/migrations/20251022000008_create_graph_functions.sql (L14-85 — the exact legacy signature, security envelope, and filter clauses to preserve)
    - supabase/migrations/20260111200001_enhanced_graph_traversal.sql (L13-127 — the live-proven 4-branch bidirectional CTE to mirror; incoming anchor L62-72, incoming recursion L94-106)
    - supabase/migrations/20260508110346_phase46_fix_upcoming_events_rpc.sql (L9-11, L139 — the repo's DROP + CREATE + GRANT precedent)
    - .planning/phases/63-relationship-graph-route-bidirectional-traversal/63-RESEARCH.md (§Pattern 2 — the full migration sketch including UNION-vs-UNION-ALL rationale)
  </read_first>
  <action>
    Create the migration implementing RESEARCH.md Pattern 2 exactly:
    1. `DROP FUNCTION IF EXISTS traverse_relationship_graph(UUID, INTEGER, TEXT);` — DROP is mandatory, not hygiene: adding `direction_path` to RETURNS TABLE changes OUT parameters, which CREATE OR REPLACE refuses (PostgreSQL rule, cited in RESEARCH Pitfall 1).
    2. `CREATE FUNCTION traverse_relationship_graph` with input parameter names byte-identical to the legacy version: `start_dossier_id UUID, max_degrees INTEGER DEFAULT 2, relationship_type_filter TEXT DEFAULT NULL` (singular TEXT, NOT TEXT[]). PostgREST dispatches by named args — live callers `graph-traversal` (index.ts L102-107), `stakeholder-influence` (L413), `cqrs-queries` (L449) all pass these exact names.
    3. RETURNS TABLE = the legacy 8 columns (`dossier_id UUID, dossier_type TEXT, name_en TEXT, name_ar TEXT, status TEXT, degree INTEGER, path UUID[], relationship_path TEXT[]`) plus `direction_path TEXT[]` appended ('outgoing' | 'incoming' per hop).
    4. Envelope: `LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public` (pure SQL — the plpgsql in the _bidirectional reference only hoisted clearance into a variable).
    5. Body: 4-branch recursive CTE joined with `UNION` (not the reference's `UNION ALL` — mid-recursion dedup per RESEARCH Pitfall 9): outgoing anchor (`dr.source_dossier_id = start_dossier_id`), incoming anchor (`dr.target_dossier_id = start_dossier_id`), outgoing recursion (`JOIN ... ON dr.source_dossier_id = rg.node_id`), incoming recursion (`JOIN ... ON dr.target_dossier_id = rg.node_id`). Every branch keeps: `dr.status = 'active'`, `(dr.effective_to IS NULL OR dr.effective_to > now())`, `(relationship_type_filter IS NULL OR dr.relationship_type = relationship_type_filter)`, and cycle prevention `NOT (<next_id> = ANY(rg.path))` on recursion branches with `rg.degree < max_degrees`.
    6. Final SELECT: `DISTINCT ON (d.id)` joined to `dossiers d`, preserving `d.status != 'deleted'` and the clearance predicate `d.sensitivity_level <= (SELECT COALESCE(clearance_level, 1) FROM profiles WHERE user_id = auth.uid())`, `ORDER BY d.id, rg.degree`.
    7. `GRANT EXECUTE ON FUNCTION traverse_relationship_graph(UUID, INTEGER, TEXT) TO authenticated;` — DROP discards grants.
    8. Do NOT touch `traverse_relationship_graph_bidirectional` — two live consumers depend on it (do not consolidate or drop, per RESEARCH "Alternatives Considered").
    Use the Write tool, never heredoc.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260612100000_bidirectional_traverse_in_place.sql | grep -c 'DROP FUNCTION IF EXISTS traverse_relationship_graph(UUID, INTEGER, TEXT)' | grep -qv '^0$' && grep -v '^--' supabase/migrations/20260612100000_bidirectional_traverse_in_place.sql | grep -c 'GRANT EXECUTE ON FUNCTION traverse_relationship_graph(UUID, INTEGER, TEXT) TO authenticated' | grep -qv '^0$' && echo STRUCTURE_OK</automated>
  </verify>
  <acceptance_criteria>
    - File contains `DROP FUNCTION IF EXISTS traverse_relationship_graph(UUID, INTEGER, TEXT);` before the CREATE
    - CREATE parameter list is exactly `start_dossier_id UUID, max_degrees INTEGER DEFAULT 2, relationship_type_filter TEXT DEFAULT NULL`
    - RETURNS TABLE contains `direction_path TEXT[]` as the 9th column after `relationship_path TEXT[]`
    - File contains `SECURITY DEFINER` and `SET search_path = public`
    - File contains both `dr.source_dossier_id = start_dossier_id` and `dr.target_dossier_id = start_dossier_id` (the two anchors)
    - File contains the clearance predicate `sensitivity_level <=` with `COALESCE(clearance_level, 1)` and `user_id = auth.uid()`
    - File contains `GRANT EXECUTE ON FUNCTION traverse_relationship_graph(UUID, INTEGER, TEXT) TO authenticated;`
    - File does NOT contain the string `traverse_relationship_graph_bidirectional` (no consolidation, no drop)
  </acceptance_criteria>
  <done>Migration file exists with the exact legacy input signature, 9-column return shape, preserved security envelope, and re-GRANT.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply migration to staging via Supabase MCP and probe both directions live</name>
  <files>none (live database operation)</files>
  <read_first>
    - supabase/migrations/20260612100000_bidirectional_traverse_in_place.sql (the file being applied — confirm Task 1 state is on disk)
    - .planning/phases/63-relationship-graph-route-bidirectional-traversal/63-RESEARCH.md (§Live Staging Verification table + §Code Examples "Live verification probes" — exact probe commands and expected rows)
    - .env.test (TEST_USER_EMAIL / TEST_USER_PASSWORD + staging anon key — source as env vars; NEVER echo values or write them into the SUMMARY)
  </read_first>
  <action>
    1. Apply the migration via Supabase MCP `apply_migration` against staging project `zkrcjzdemdmwhearhfgg` with name `bidirectional_traverse_in_place` and the exact file contents. Do NOT use `supabase db push`. MCP-applied DDL triggers PostgREST schema-cache reload automatically; if the RPC briefly 404s, retry once after ~10s (RESEARCH assumption A2).
    2. Definition assertion via Supabase MCP `execute_sql`: `SELECT prosecdef, proconfig, pg_get_functiondef(oid) FROM pg_proc WHERE proname = 'traverse_relationship_graph';` — expect exactly 1 row, `prosecdef = true`, `proconfig` containing `search_path=public`, and the definition containing `direction_path` and `sensitivity_level <=`.
    3. Capture the live anchor pair via `execute_sql`: `SELECT source_dossier_id, target_dossier_id, relationship_type FROM dossier_relationships WHERE status = 'active';` — record the working_group source id (ends `...404`, "Test Working Group D") and the country target `b0000001-0000-0000-0000-000000000001` (Indonesia).
    4. Obtain a real user JWT via password grant: POST `https://zkrcjzdemdmwhearhfgg.supabase.co/auth/v1/token?grant_type=password` with apikey = staging anon key and the .env.test credentials (a user JWT is required — service-role probes return empty rows because the clearance subquery yields NULL for a non-user).
    5. Incoming probe (the GRAPH-02 gate): POST `/rest/v1/rpc/traverse_relationship_graph` with body `{"start_dossier_id":"b0000001-0000-0000-0000-000000000001","max_degrees":2,"relationship_type_filter":null}` — expect HTTP 200 with at least 1 row whose dossier_id is the working_group source from step 3 and whose `direction_path` is `["incoming"]`. Today this returns `[]` — this probe IS the bug fix proof.
    6. Outgoing regression probe: same RPC with `start_dossier_id` = the working_group source id — expect the Indonesia row with `direction_path` `["outgoing"]`.
    7. Note in the SUMMARY: `database.types.ts` regen deliberately skipped (RESEARCH marks it optional — no typed direct frontend callers of this RPC).
  </action>
  <verify>
    <automated>MISSING — live-DB probes; run the curl probes in steps 5-6 and capture HTTP codes + row JSON (ids only, no tokens) as evidence in the SUMMARY. The frontend mirror contract is unit-tested in plan 63-03 (edge-orientation.test.ts).</automated>
  </verify>
  <acceptance_criteria>
    - Supabase MCP `apply_migration` returns success for project zkrcjzdemdmwhearhfgg
    - `pg_proc` query returns prosecdef = true and proconfig containing `search_path=public` for traverse_relationship_graph
    - `pg_get_functiondef` output contains `direction_path` and `sensitivity_level <=`
    - RPC probe from b0000001-0000-0000-0000-000000000001 (authenticated user JWT) returns HTTP 200 with >= 1 row where direction_path = ["incoming"]
    - RPC probe from the working_group source id returns HTTP 200 with >= 1 row where direction_path = ["outgoing"]
    - `SELECT COUNT(*) FROM pg_proc WHERE proname = 'traverse_relationship_graph_bidirectional'` returns 1 (sibling function untouched)
    - No JWT, password, or anon key value appears in the SUMMARY
  </acceptance_criteria>
  <done>The bidirectional RPC is live on staging, both traversal directions return rows with correct direction_path, and the security envelope is verified in pg_proc.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                             | Description                                                                           |
| ---------------------------------------------------- | ------------------------------------------------------------------------------------- |
| authenticated client → PostgREST RPC                 | untrusted callers invoke a SECURITY DEFINER function that bypasses RLS                |
| SECURITY DEFINER function → dossiers/profiles tables | function runs with definer privileges; clearance filtering is the only access control |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                    | Disposition | Mitigation Plan                                                                                                                                                                                           |
| --------- | ---------------------- | ------------------------------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-63-01   | Information Disclosure | traverse_relationship_graph (clearance bypass via traversal) | mitigate    | Preserve `d.sensitivity_level <= COALESCE(clearance_level,1)` + `d.status != 'deleted'` predicates byte-faithfully in the final SELECT; assert presence via pg_get_functiondef post-apply (Task 2 step 2) |
| T-63-02   | Elevation of Privilege | SECURITY DEFINER search-path hijack                          | mitigate    | `SET search_path = public` retained in CREATE; verified via pg_proc.proconfig post-apply                                                                                                                  |
| T-63-03   | Denial of Service      | recursive CTE resource exhaustion                            | mitigate    | `UNION` (not UNION ALL) dedup, per-path cycle prevention `NOT ... = ANY(path)`, `rg.degree < max_degrees` bound, edge fn caps maxDegrees at 5, STABLE function                                            |
| T-63-04   | Tampering              | GRANT scope wider than legacy                                | mitigate    | Re-GRANT EXECUTE to `authenticated` only (parity with legacy envelope); no PUBLIC or anon grant                                                                                                           |
| T-63-SC   | Tampering              | npm/pip/cargo installs                                       | accept      | No package installs in this phase (RESEARCH Package Legitimacy Audit: none)                                                                                                                               |

</threat_model>

<verification>
- Migration file passes the grep structure gate (Task 1)
- Live staging probes prove incoming + outgoing rows with direction_path (Task 2 — the VALIDATION.md "RPC returns incoming + outgoing" manual row, justified manual: SQL function not unit-testable in jsdom)
</verification>

<success_criteria>

- GRAPH-02 data layer complete: live RPC returns both directions with per-hop direction metadata
- All legacy callers keep working (named-arg signature unchanged; extra column is additive)
- Security envelope (DEFINER + pinned search_path + clearance filter + authenticated-only grant) verified on the live function
  </success_criteria>

<output>
Create `.planning/phases/63-relationship-graph-route-bidirectional-traversal/63-01-SUMMARY.md` when done (probe evidence with ids and HTTP codes only — no tokens).
</output>
