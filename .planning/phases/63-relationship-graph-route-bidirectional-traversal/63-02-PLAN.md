---
phase: 63-relationship-graph-route-bidirectional-traversal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/_protected/relationships/graph.tsx
  - frontend/src/pages/relationships/RelationshipGraphPage.tsx
  - frontend/src/components/dossier/MiniRelationshipGraph.tsx
  - frontend/src/pages/relationships/__tests__/RelationshipGraphPage.test.tsx
autonomous: true
requirements: [GRAPH-01]

must_haves:
  truths:
    - '/relationships/graph renders RelationshipGraphPage — no redirect to /dossiers (D-01: mount, do not retire)'
    - 'Landing without a dossierId search param shows the "No dossier selected" alert PLUS a working link to /dossiers — not a dead end (D-02)'
    - 'The page resolves strings via the graph i18n namespace (useTranslation("graph") + bare keys) so Arabic strings actually render in AR mode (D-07 — page is broken in AR today via the dot-vs-colon trap)'
    - 'The relationship-type filter offers the 18 canonical DossierRelationshipType values with graph:relationship.* labels — the current values (partner, parent_org, participant, signatory) filter to zero rows'
    - 'MiniRelationshipGraph remains the sole entry point (D-08) — its Link is now properly typed (no as-any cast) and no new UI surfaces are added'
  artifacts:
    - path: 'frontend/src/routes/_protected/relationships/graph.tsx'
      provides: 'Route mount with validateSearch for dossierId'
      contains: 'validateSearch'
    - path: 'frontend/src/pages/relationships/__tests__/RelationshipGraphPage.test.tsx'
      provides: 'Wave 0 page test: render, no-dossier link, graph-namespace regression guard'
      min_lines: 60
  key_links:
    - from: 'frontend/src/routes/_protected/relationships/graph.tsx'
      to: 'frontend/src/pages/relationships/RelationshipGraphPage.tsx'
      via: 'component mount'
      pattern: 'RelationshipGraphPage'
    - from: 'frontend/src/pages/relationships/RelationshipGraphPage.tsx'
      to: 'graph i18n namespace'
      via: "useTranslation('graph')"
      pattern: "useTranslation\\('graph'\\)"
    - from: 'frontend/src/pages/relationships/RelationshipGraphPage.tsx'
      to: '/dossiers'
      via: 'Browse-dossiers Link under the no-dossier Alert'
      pattern: 'to="/dossiers"'
---

<objective>
Mount the dormant `/relationships/graph` route (GRAPH-01, D-01) and repair the page: typed `validateSearch` for `dossierId`, the D-02 actionable no-dossier state, the i18n namespace fix (the page falls back to inline English in BOTH languages today), and the canonical relationship-type filter vocabulary. Test-first: the Wave 0 page test is written before the implementation.

Purpose: GRAPH-01 / roadmap success criterion 1 — the mini-graph "View Full Graph" button currently dead-ends in a redirect.
Output: mounted route, repaired page, passing page test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/63-relationship-graph-route-bidirectional-traversal/63-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase by the pattern mapper. -->

From frontend/src/types/relationship.types.ts (L18-36) — the canonical filter vocabulary:

```typescript
export type DossierRelationshipType =
  | 'member_of'
  | 'participates_in'
  | 'cooperates_with'
  | 'bilateral_relation'
  | 'partnership'
  | 'parent_of'
  | 'subsidiary_of'
  | 'related_to'
  | 'represents'
  | 'hosted_by'
  | 'sponsored_by'
  | 'involves'
  | 'discusses'
  | 'participant_in'
  | 'observer_of'
  | 'affiliate_of'
  | 'successor_of'
  | 'predecessor_of'
```

From frontend/src/lib/dossier-routes.ts (L45-47) — GRAPH-03 navigation, already correct, do not change:

```typescript
export function getDossierDetailPath(dossierId: string, type: string | undefined | null): string {
  return `/dossiers/${getDossierRouteSegment(type)}/${dossierId}`
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the Wave 0 page test (RED — fails against the current page)</name>
  <files>frontend/src/pages/relationships/__tests__/RelationshipGraphPage.test.tsx</files>
  <read_first>
    - frontend/src/routes/_protected/dossiers/countries/__tests__/CountriesListPage.test.tsx (the repo's page-test analog: mock-before-import structure, react-i18next mock shape, heavy-child stubs, AAA assertions)
    - frontend/src/pages/relationships/RelationshipGraphPage.tsx (the component under test — identify its imports to stub: 4 viz children, supabase client, router hooks, useQuery)
    - .planning/phases/63-relationship-graph-route-bidirectional-traversal/63-PATTERNS.md (§RelationshipGraphPage.test.tsx — mock list and case adaptations)
  </read_first>
  <behavior>
    - Test 1 (GRAPH-01 / D-02): with no dossierId, the page renders the no-dossier alert AND a link whose href is `/dossiers`
    - Test 2 (GRAPH-01): with a dossierId and a mocked successful query, the page renders the graph chrome (heading + controls) without redirecting
    - Test 3 (D-07 namespace guard): the page requests the `graph` namespace with bare keys — the useTranslation mock records its namespace argument and serves a map keyed `graph:noDossier` → sentinel string; assert the sentinel renders and that useTranslation was called with 'graph' (proves bare-key + namespace-arg form, not the broken `t('graph.x')` dot-form)
  </behavior>
  <action>
    Create `frontend/src/pages/relationships/__tests__/RelationshipGraphPage.test.tsx` copying the CountriesListPage.test.tsx structure. Mocks (all declared BEFORE importing the page): `react-i18next` (useTranslation mock that captures the ns argument and resolves `${ns}:${key}` from a local map, falling back to the inline default), `@tanstack/react-router` (export `useSearch`, `useNavigate`, `Link` as anchor stub, AND `getRouteApi` returning `{ useSearch: searchMock }` so the test survives the Task 3 switch to getRouteApi), `@tanstack/react-query` (useQuery mock like the analog's useCountriesMock), `@/lib/supabase`, and stubs for the 4 heavy children: `GraphVisualization`, `EnhancedGraphVisualization`, `AdvancedGraphVisualization`, `RelationshipNavigator` (mock by their actual import paths as read from the page). Drive `searchMock` per-case: `{}` for the no-dossier case, `{ dossierId: 'a0000000-0000-0000-0000-000000000404' }` for the render case. ESLint: explicit return types on all mock functions, single quotes, no semicolons. Run the test and confirm it FAILS for the expected reasons (no /dossiers link exists yet; page uses default-namespace dot-form keys). Commit the RED state.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run src/pages/relationships/__tests__/RelationshipGraphPage.test.tsx 2>&1 | grep -qE 'failed|FAIL' && echo RED_CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists with the 3 behavior cases and mock-before-import structure
    - `cd frontend && pnpm vitest run src/pages/relationships/__tests__/RelationshipGraphPage.test.tsx` runs (no import/transform crash) and reports failing assertions — RED for the right reason (missing link + dot-form keys), not for a broken mock
    - The router mock exports both `useSearch` and `getRouteApi` so Task 3's implementation change does not require test edits
  </acceptance_criteria>
  <done>Wave 0 page test exists and is RED against the current redirect-era page, pinning the GRAPH-01/D-02/D-07 contract.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the route with validateSearch; type the mini-graph Link</name>
  <files>frontend/src/routes/_protected/relationships/graph.tsx, frontend/src/components/dossier/MiniRelationshipGraph.tsx</files>
  <read_first>
    - frontend/src/routes/_protected/relationships/graph.tsx (current 7-line redirect being replaced)
    - frontend/src/routes/_protected/geographic-visualization.tsx (the repo's validateSearch + page-mount analog — copy its shape)
    - frontend/src/components/dossier/MiniRelationshipGraph.tsx (~L722-728 — the "View Full Graph" Link carrying `search={{ dossierId: dossier.id } as any}`)
  </read_first>
  <action>
    1. Replace the redirect in `graph.tsx`: `createFileRoute('/_protected/relationships/graph')` with `validateSearch: (search: Record<string, unknown>): GraphSearch => ({ dossierId: typeof search.dossierId === 'string' && search.dossierId.length > 0 ? search.dossierId : undefined })` where `interface GraphSearch { dossierId?: string }`, and `component: RelationshipGraphRoute` — a wrapper `function RelationshipGraphRoute(): ReactElement { return <RelationshipGraphPage /> }` importing `RelationshipGraphPage` from `@/pages/relationships/RelationshipGraphPage` (named export — confirm export form when reading the page). Remove the `redirect` import entirely. ESLint requires the explicit `: ReactElement` return type (the analog omits it — do not copy that omission).
    2. In `MiniRelationshipGraph.tsx` (~L724) drop the `as any` cast on the Link's `search` prop — once validateSearch exists the search params are properly typed. Make no other change to this file (D-08: the mini-graph button is the sole entry point; zero new UI surfaces).
    3. `routeTree.gen.ts` already registers `/relationships/graph` and regenerates on build — if `pnpm type-check` complains about Link search types, run `cd frontend && pnpm build` first to regenerate, then re-run type-check.
  </action>
  <verify>
    <automated>grep -c 'validateSearch' frontend/src/routes/_protected/relationships/graph.tsx | grep -qv '^0$' && grep -c 'redirect' frontend/src/routes/_protected/relationships/graph.tsx | grep -q '^0$' && grep -c 'as any' frontend/src/components/dossier/MiniRelationshipGraph.tsx | grep -q '^0$' && echo ROUTE_MOUNTED</automated>
  </verify>
  <acceptance_criteria>
    - graph.tsx contains `validateSearch` and `RelationshipGraphPage`; contains zero occurrences of `redirect`
    - graph.tsx route wrapper has an explicit `: ReactElement` return type
    - MiniRelationshipGraph.tsx contains zero `as any` occurrences
    - `cd frontend && pnpm type-check` exits 0 (after routeTree regen via build if needed)
  </acceptance_criteria>
  <done>The route mounts the page with typed dossierId search validation; the sole entry point's Link is properly typed.</done>
</task>

<task type="auto">
  <name>Task 3: Repair the page — graph namespace, canonical filter, D-02 link, sentence-case defaults (GREEN)</name>
  <files>frontend/src/pages/relationships/RelationshipGraphPage.tsx</files>
  <read_first>
    - frontend/src/pages/relationships/RelationshipGraphPage.tsx (the file being modified — i18n calls throughout, search read ~L112, node-click ~L156-164, no-dossier alert ~L170-184, filter Select ~L228-245)
    - frontend/src/components/dossier/MiniRelationshipGraph.tsx (L495 + L723-728 — the correct `useTranslation('graph')` form and the Button-asChild + Link pattern to copy for D-02)
    - frontend/src/types/relationship.types.ts (L18-36 — the 18-value DossierRelationshipType union to drive the filter)
    - .planning/phases/63-relationship-graph-route-bidirectional-traversal/63-PATTERNS.md (§RelationshipGraphPage — the 24-key list and exact sentence-case targets)
  </read_first>
  <action>
    All changes in RelationshipGraphPage.tsx:
    1. i18n namespace fix (D-07): change `const { t } = useTranslation()` to `const { t } = useTranslation('graph')` and strip the `graph.` prefix from every key — the 24 page keys are: title, description, noDossier, maxDegrees, degree, degrees, relationshipType, allTypes, refresh, complexity.* , basicMode, enhancedMode, advancedMode, nodes, edges, maxDegree, queryTime, error, graphView, listView. All exist in graph.json EN+AR. Never use colon-form `t('graph:x')` mixed with the namespace arg (project trap — bare keys only).
    2. Typed search: replace `(search as any)?.dossierId` (~L112) with `getRouteApi('/_protected/relationships/graph').useSearch()` from `@tanstack/react-router` (module-level `const routeApi = getRouteApi(...)`, then `const { dossierId } = routeApi.useSearch()` in the component). Zero `as any` remains in the file.
    3. Canonical filter (RESEARCH Pitfall 5): replace the stale Select options (partner, parent_org, participant, signatory and camelCase labels) with a module-level `const RELATIONSHIP_TYPES: DossierRelationshipType[] = [...]` listing all 18 canonical values, rendered as options with value = the snake_case type and label = `t(`relationship.${value}`)`. Keep the all-types option labeled `t('allTypes', 'All types')`. Import the type from `@/types/relationship.types`.
    4. D-02 actionable no-dossier state: under the existing Alert (~L170-184), add `<Button variant="outline" asChild><Link to="/dossiers">{t('browseDossiers', 'Browse dossiers')}</Link></Button>` — Link from `@tanstack/react-router`. The `browseDossiers` graph.json key is added by plan 63-03; the inline default keeps EN correct regardless of plan ordering within the wave. No inline dossier picker (deferred).
    5. Sentence-case inline defaults in lockstep with the 63-03 JSON sweep (D-06 Bureau voice): 'Relationship graph' (~L190), 'Degrees of separation' (~L200), 'All types' (~L228), 'Graph view' (~L385), 'List view' (~L389), 'Max degree' (~L338), 'Query time' (~L344). Do NOT change layout, styling, or the handleNodeSelect block (~L156-164 — GRAPH-03, already correct per R17-02; preserve `getDossierDetailPath` exactly).
    6. Run the Task 1 test — GREEN now. Then `pnpm type-check`.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run src/pages/relationships/__tests__/RelationshipGraphPage.test.tsx && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && pnpm vitest run src/pages/relationships/__tests__/RelationshipGraphPage.test.tsx` exits 0 (all 3 cases GREEN)
    - RelationshipGraphPage.tsx contains `useTranslation('graph')` and zero occurrences of `t('graph.` (dot-form eliminated)
    - RelationshipGraphPage.tsx contains zero `as any` occurrences
    - RelationshipGraphPage.tsx contains `member_of` and `cooperates_with` (canonical filter values) and zero occurrences of `parent_org` or `signatory`
    - RelationshipGraphPage.tsx contains a Link with `to="/dossiers"` reachable from the no-dossier branch
    - `grep -c 'getDossierDetailPath' frontend/src/pages/relationships/RelationshipGraphPage.tsx` is unchanged from before the edit (node-click preserved)
    - `cd frontend && pnpm type-check` exits 0
  </acceptance_criteria>
  <done>Page consumes the graph namespace with bare keys, offers the canonical 18-type filter, has an actionable no-dossier state, and the Wave 0 test is GREEN.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                             |
| ------------------------------------ | --------------------------------------------------------------------------------------- |
| URL search params → route            | untrusted `?dossierId=` input crosses into the app via the address bar / external links |
| page → graph-traversal edge function | user JWT forwarded; response rendered into the DOM                                      |

## STRIDE Threat Register

| Threat ID | Category               | Component                           | Disposition | Mitigation Plan                                                                                                                                                                                |
| --------- | ---------------------- | ----------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-63-05   | Tampering              | graph.tsx search params             | mitigate    | `validateSearch` coerces dossierId to a non-empty string or undefined (ASVS V5); downstream the value is only used as a fetch query param and an `enabled` gate — never interpolated into HTML |
| T-63-06   | Information Disclosure | arbitrary dossierId probing via URL | accept      | Access control lives server-side: the edge function 404s on RLS-denied start dossiers and the DEFINER RPC clearance-filters every node (63-01); the client adds no enforcement by design       |
| T-63-07   | Spoofing               | page render of API-provided names   | accept      | Node labels render via React text nodes (auto-escaped); no dangerouslySetInnerHTML in the page or viz components                                                                               |
| T-63-SC   | Tampering              | npm/pip/cargo installs              | accept      | No package installs in this plan                                                                                                                                                               |

</threat_model>

<verification>
- VALIDATION.md rows realized: "Route renders page (no redirect); no-dossier alert has /dossiers link" → Task 1/3 test; "AR mode renders graph.json AR strings (namespace fix regression guard)" → Task 1 case 3 (namespace-form guard; live AR rendering verified in 63-05)
- `cd frontend && pnpm vitest run src/pages/relationships && pnpm type-check` exits 0 at plan end
</verification>

<success_criteria>

- GRAPH-01 satisfied in code: route mounts, no redirect, typed search, actionable no-dossier state
- The dot-vs-colon i18n trap is closed for this page; filter vocabulary matches the canonical union
- Zero new UI surfaces (D-08) and no layout redesign (D-06 scope)
  </success_criteria>

<output>
Create `.planning/phases/63-relationship-graph-route-bidirectional-traversal/63-02-SUMMARY.md` when done.
</output>
