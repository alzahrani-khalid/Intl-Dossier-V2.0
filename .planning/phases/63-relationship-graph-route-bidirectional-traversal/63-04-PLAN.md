---
phase: 63-relationship-graph-route-bidirectional-traversal
plan: 04
type: execute
wave: 2
depends_on: [63-01, 63-03]
files_modified:
  - supabase/functions/graph-traversal/index.ts
autonomous: true
requirements: [GRAPH-02]

must_haves:
  truths:
    - 'The deployed graph-traversal edge function orients every edge by direction_path: an incoming hop swaps source/target so the arrow points AT the focused dossier (D-04 — D-03 alone is NOT enough, the edge fn assumed path order = edge direction)'
    - 'Edge fn from the incoming-only anchor (Indonesia) returns >= 1 edge whose source_id is the working_group dossier and target_id is Indonesia — today it returns 1 node / 0 edges (live-reproduced bug)'
    - 'The auth pattern is preserved verbatim: esm.sh/@supabase/supabase-js@2 + auth.getUser(token) — bare getUser() 401s on valid tokens (project memory, verified live)'
    - 'The transform is guarded with direction_path?.[i] so either deploy order degrades to current behavior instead of crashing (RESEARCH Pitfall 3)'
  artifacts:
    - path: 'supabase/functions/graph-traversal/index.ts'
      provides: 'Direction-aware edge building in lockstep with frontend/src/lib/graph-edge-orientation.ts'
      contains: 'direction_path'
  key_links:
    - from: 'supabase/functions/graph-traversal/index.ts'
      to: 'traverse_relationship_graph RPC'
      via: 'named-arg rpc call (start_dossier_id, max_degrees, relationship_type_filter)'
      pattern: 'start_dossier_id'
    - from: 'supabase/functions/graph-traversal/index.ts'
      to: 'frontend/src/lib/graph-edge-orientation.ts'
      via: 'lockstep comment + identical orientation logic (unit-tested in 63-03)'
      pattern: 'graph-edge-orientation'
---

<objective>
Port the unit-tested orientation transform into the `graph-traversal` edge function and redeploy it to staging — the hidden client change D-03 did not anticipate. Without this, the bidirectional RPC ships but incoming arrows render backwards (D-04 inverted).

Purpose: GRAPH-02 end to end — RPC (63-01) emits direction metadata; this plan makes the only consumer that builds edges actually use it.
Output: updated + redeployed edge function, live e2e probe proving the swapped edge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/63-relationship-graph-route-bidirectional-traversal/63-RESEARCH.md

<interfaces>
<!-- The contract this edge fn must mirror — created and unit-tested by plan 63-03. -->

From frontend/src/lib/graph-edge-orientation.ts (63-03 output):

```typescript
// for each hop i in row.path:
const isIncoming = row.direction_path?.[i] === 'incoming'
const sourceId = isIncoming ? row.path[i + 1] : row.path[i] // true relationship source
const targetId = isIncoming ? row.path[i] : row.path[i + 1] // true relationship target
// existing (source_id, target_id, relationship_type) dedup retained
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Direction-aware edge building in graph-traversal (auth + contract untouched)</name>
  <files>supabase/functions/graph-traversal/index.ts</files>
  <read_first>
    - supabase/functions/graph-traversal/index.ts (the file being modified — auth L5/L39-63, RPC call L101-108, edge-building loop L146-168, row typing)
    - frontend/src/lib/graph-edge-orientation.ts (the unit-tested transform to mirror — created by 63-03; if absent, stop: wave ordering violated)
    - .planning/phases/63-relationship-graph-route-bidirectional-traversal/63-PATTERNS.md (§graph-traversal — the exact replacement block and what must NOT change)
  </read_first>
  <action>
    Surgical change to the edge-building loop only (L146-168):
    1. Inside the per-hop loop add `const isIncoming = row.direction_path?.[i] === 'incoming'` and derive `sourceId`/`targetId` per the interfaces block — optional chaining is the deploy-window guard (missing column → all-outgoing, never a crash).
    2. Keep the existing `edgeExists` dedup on (source_id, target_id, relationship_type) — it now also collapses the same physical row surfaced from both traversal directions (discretion item answered).
    3. Extend the local row typing with `direction_path?: string[]` wherever the RPC row shape is declared (interface or inline cast).
    4. Add a lockstep comment naming `frontend/src/lib/graph-edge-orientation.ts` next to the loop.
    5. Change NOTHING else: the import must stay `https://esm.sh/@supabase/supabase-js@2`, auth must stay `auth.getUser(token)` with the explicit token, the RPC call must keep named args `start_dossier_id`, `max_degrees`, `relationship_type_filter`, the maxDegrees 1-5 clamp and startDossierId presence validation stay, and CORS stays as-is (pre-existing `Access-Control-Allow-Origin: *` — accepted for this phase per RESEARCH CORS note; do not introduce the ALLOWED_ORIGINS pattern here).
  </action>
  <verify>
    <automated>grep -c 'direction_path' supabase/functions/graph-traversal/index.ts | grep -qv '^0$' && grep -c 'getUser(token)' supabase/functions/graph-traversal/index.ts | grep -qv '^0$' && grep -c 'supabase-js@2' supabase/functions/graph-traversal/index.ts | grep -qv '^0$' && grep -c 'start_dossier_id' supabase/functions/graph-traversal/index.ts | grep -qv '^0$' && echo EDGE_FN_OK</automated>
  </verify>
  <acceptance_criteria>
    - index.ts contains `direction_path?.[i]` (optional-chained) inside the edge-building loop
    - index.ts still contains `getUser(token)` and `esm.sh/@supabase/supabase-js@2` (auth pattern unregressed)
    - index.ts still calls the RPC with named args start_dossier_id, max_degrees, relationship_type_filter
    - index.ts contains a comment referencing graph-edge-orientation (lockstep marker)
    - The edgeExists/dedup logic on (source_id, target_id, relationship_type) is still present
    - git diff for this task touches only the edge-building region, row typing, and the comment — no auth/CORS/validation lines changed
  </acceptance_criteria>
  <done>The edge function orients edges by true relationship direction, guarded for the deploy window, with auth and the RPC contract byte-preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Redeploy to staging and prove D-04 end to end</name>
  <files>none (deploy + verification operation)</files>
  <read_first>
    - .planning/phases/63-relationship-graph-route-bidirectional-traversal/63-01-SUMMARY.md (the captured anchor ids + JWT-acquisition recipe; confirms the migration is already live — deploy order: migration FIRST, this deploy second)
    - .env.test (credentials for the password-grant JWT; NEVER echo values)
  </read_first>
  <action>
    1. Pre-deploy gate: confirm the migration is live — POST `/rest/v1/rpc/traverse_relationship_graph` from `b0000001-0000-0000-0000-000000000001` (user JWT) must return rows with `direction_path`. If it returns `[]` or rows without direction_path, STOP — 63-01 did not land; do not deploy.
    2. Deploy `graph-traversal` to staging project `zkrcjzdemdmwhearhfgg` via Supabase MCP `deploy_edge_function` (Supabase CLI `supabase functions deploy graph-traversal --project-ref zkrcjzdemdmwhearhfgg` is the fallback). Confirm the function version/updated timestamp advanced in the deploy response or `supabase functions list`.
    3. E2E probe (the D-04 gate): GET `https://zkrcjzdemdmwhearhfgg.supabase.co/functions/v1/graph-traversal?startDossierId=b0000001-0000-0000-0000-000000000001&maxDegrees=2` with the user JWT + apikey — expect HTTP 200 with >= 2 nodes and >= 1 edge where `source_id` = the working_group dossier id (ends ...404) and `target_id` = b0000001-0000-0000-0000-000000000001. Today this anchor returns 1 node / 0 edges — this probe is the full-stack bug-fix proof.
    4. Outgoing regression probe: same endpoint with startDossierId = the working_group id — the edge must have the SAME orientation (source = working_group, target = Indonesia), proving direction is now semantic, not path-order.
    5. Warm-start note: ignore any `performance_warning` on the first call (cold start, RESEARCH Pitfall 8) — record second-call query_time_ms instead.
    6. Record in the SUMMARY: deploy evidence + both probe results (ids and counts only, no tokens). The stakeholder-influence consumer smoke-check happens in the 63-05 browser walkthrough.
  </action>
  <verify>
    <automated>MISSING — live deploy + probes; capture HTTP codes, node/edge counts, and the oriented edge JSON (ids only) as SUMMARY evidence. The transform itself is covered by cd frontend && pnpm vitest run src/pages/relationships/__tests__/edge-orientation.test.ts (63-03, must still exit 0).</automated>
  </verify>
  <acceptance_criteria>
    - Pre-deploy RPC probe returns rows containing direction_path (migration confirmed live before deploy)
    - Deploy response (MCP or CLI) confirms graph-traversal updated on zkrcjzdemdmwhearhfgg
    - Edge fn probe from b0000001-0000-0000-0000-000000000001 returns HTTP 200 with >= 1 edge where source_id ends with '404' and target_id = 'b0000001-0000-0000-0000-000000000001'
    - Edge fn probe from the working_group anchor returns the identically-oriented edge (source = working_group, target = Indonesia)
    - No JWT, password, or key value appears in the SUMMARY
  </acceptance_criteria>
  <done>GRAPH-02 is proven end to end through the full stack: incoming references are visible and their arrows point at the focused dossier.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                    |
| ------------------------------------ | ------------------------------------------------------------------------------ |
| internet → edge function             | unauthenticated callers reach the function URL; JWT validation is the gate     |
| edge function → SECURITY DEFINER RPC | function forwards the user's JWT so clearance filtering binds to the real user |

## STRIDE Threat Register

| Threat ID | Category               | Component                                    | Disposition | Mitigation Plan                                                                                                                                                                                   |
| --------- | ---------------------- | -------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-63-10   | Spoofing               | graph-traversal auth                         | mitigate    | Preserve `@2` + `auth.getUser(token)` verbatim (ASVS V2); grep-gated in Task 1 acceptance criteria; user-context client (anon key + Authorization header) keeps RLS/clearance bound to the caller |
| T-63-11   | Denial of Service      | maxDegrees abuse                             | mitigate    | Existing 1-5 clamp and startDossierId presence validation explicitly preserved (Task 1 step 5)                                                                                                    |
| T-63-12   | Tampering              | open CORS (`Access-Control-Allow-Origin: *`) | accept      | Pre-existing; GET-only + JWT-required limits exposure; tightening to ALLOWED_ORIGINS is optional hygiene explicitly out of this phase (RESEARCH CORS note)                                        |
| T-63-13   | Information Disclosure | edge fn leaking nodes beyond clearance       | transfer    | Enforcement lives in the DEFINER RPC's clearance predicate (verified in 63-01); the edge fn only transforms rows the DB already authorized                                                        |
| T-63-SC   | Tampering              | npm/pip/cargo installs                       | accept      | No package installs; the only remote import is the pinned-major esm.sh supabase-js@2 already in production use                                                                                    |

</threat_model>

<verification>
- VALIDATION.md row realized: "RPC returns incoming + outgoing" e2e leg — edge fn probes in Task 2 (manual-justified: live staging)
- 63-03 edge-orientation.test.ts still green (lockstep contract)
</verification>

<success_criteria>

- Incoming edges visible through the full stack with correct arrow direction (D-04)
- Deploy ordering respected (migration verified live before function deploy); guard makes any drift non-fatal
- Zero regression to auth, validation, CORS, or the RPC named-arg contract
  </success_criteria>

<output>
Create `.planning/phases/63-relationship-graph-route-bidirectional-traversal/63-04-SUMMARY.md` when done.
</output>
