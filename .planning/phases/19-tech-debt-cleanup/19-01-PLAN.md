---
phase: 19-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx
  - frontend/src/pages/Dashboard/components/__tests__/EngagementStageGroup.test.tsx
  - frontend/src/pages/Dashboard/components/ActionBar.tsx
autonomous: true
requirements:
  - DEBT-01
must_haves:
  truths:
    - 'Clicking an engagement row in OPS-03 navigates via typed TanStack Router params, not a string URL'
    - 'Pressing Enter/Space on a focused engagement row navigates via typed params'
    - 'OPS-07 ActionBar is verified to contain zero string-URL navigate() calls and this is documented'
    - 'pnpm typecheck passes, proving the typed `to`/`params` contract is correct'
  artifacts:
    - path: 'frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx'
      provides: "Typed navigate({ to: '/engagements/$engagementId', params }) at both callsites"
      contains: "to: '/engagements/$engagementId'"
    - path: 'frontend/src/pages/Dashboard/components/__tests__/EngagementStageGroup.test.tsx'
      provides: 'RTL unit tests covering click + keyboard navigation with mocked useNavigate'
      min_lines: 40
    - path: 'frontend/src/pages/Dashboard/components/ActionBar.tsx'
      provides: 'Documenting comment: no string-nav present; intentional WorkCreation palette + Cmd+K dispatch'
  key_links:
    - from: 'EngagementStageGroup.tsx onClick/onKeyDown'
      to: "TanStack Router FileRoutesByPath['/engagements/$engagementId']"
      via: 'navigate({ to, params: { engagementId } })'
      pattern: "to: '/engagements/\\$engagementId'"
---

<objective>
Replace the two string-template `navigate({ to: `/engagements/${id}` })` callsites in
`EngagementStageGroup.tsx` (OPS-03) with TanStack Router typed navigation using
`params: { engagementId }`, and formally verify + document that OPS-07 `ActionBar.tsx`
contains zero string-URL navigation (per D1, tight scope).

Purpose: Closes DEBT-01. Restores compile-time type safety for the only remaining
string-URL router callsites in OPS-03/OPS-07. Surfaces future route renames as TS errors.

Output: Typed navigation at 2 callsites, new Vitest RTL test file, ActionBar documentation
comment, clean `pnpm typecheck`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-tech-debt-cleanup/19-CONTEXT.md
@.planning/phases/19-tech-debt-cleanup/19-RESEARCH.md
@frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx
@frontend/src/pages/Dashboard/components/ActionBar.tsx
@frontend/src/routeTree.gen.ts

<interfaces>
<!-- Typed route target already generated in routeTree.gen.ts lines 110, 1388, 1452, 1834 -->
<!-- FileRoutesByPath entry: '/engagements/$engagementId' with params { engagementId: string } -->

Target pattern (TanStack Router v5 typed navigation):

```tsx
navigate({
  to: '/engagements/$engagementId',
  params: { engagementId: engagement.id },
})
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing RTL tests for EngagementStageGroup navigation</name>
  <files>frontend/src/pages/Dashboard/components/__tests__/EngagementStageGroup.test.tsx</files>
  <behavior>
    - Test 1: Clicking an engagement row calls `navigate` with `{ to: '/engagements/$engagementId', params: { engagementId: <id> } }` (exact object shape, not a string URL)
    - Test 2: Pressing Enter on a focused row triggers the same typed navigate call
    - Test 3: Pressing Space on a focused row triggers the same typed navigate call
    - Test 4: Pressing any other key (e.g. 'a') does NOT call navigate
    - Mock `@tanstack/react-router` `useNavigate` to return a `vi.fn()` spy
  </behavior>
  <action>
    Create Vitest + @testing-library/react test file. Mock `useNavigate` from
    `@tanstack/react-router`. Render `<EngagementStageGroup>` with a minimal fixture
    (one engagement with `id: 'eng-123'`). For each test case, assert the spy was called
    with the EXACT typed object shape (use `expect(spy).toHaveBeenCalledWith({ to: '/engagements/$engagementId', params: { engagementId: 'eng-123' } })`).
    Follow project Prettier: no semis, single quotes, 2-space indent, trailing commas, 100-char width.
    Run the tests — they MUST fail (current code still uses string templates).
  </action>
  <verify>
    <automated>pnpm --filter frontend test EngagementStageGroup -- --run 2>&1 | tail -40</automated>
  </verify>
  <done>Test file exists, all 4 tests run, tests currently FAIL because production code still passes a string URL. RED state captured.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor EngagementStageGroup to typed navigation</name>
  <files>frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx</files>
  <behavior>
    - After change: both callsites use `{ to: '/engagements/$engagementId', params: { engagementId: engagement.id } }`
    - No string templates `/engagements/${...}` remain anywhere in the file
    - `void navigate(...)` prefix is preserved (no floating promises)
    - `pnpm typecheck` passes
    - All 4 tests from Task 1 now pass
  </behavior>
  <action>
    Edit `EngagementStageGroup.tsx`:
    1. Lines 93-95 (onClick handler): replace
       `void navigate({ to: `/engagements/${engagement.id}` })`
       with
       ```
       void navigate({
         to: '/engagements/$engagementId',
         params: { engagementId: engagement.id },
       })
       ```
    2. Lines 96-101 (onKeyDown handler, inside the `if (e.key === 'Enter' || e.key === ' ')` block):
       apply the same replacement.
    Do NOT introduce a `buildDashboardRoute()` helper — only 2 identical callsites in one file
    (per D1 + research recommendation; helper overhead > value).
    Preserve explicit return types on handlers. Preserve Prettier formatting (no semis, single quotes).
  </action>
  <verify>
    <automated>pnpm --filter frontend test EngagementStageGroup -- --run && pnpm typecheck 2>&1 | tail -40</automated>
  </verify>
  <done>Both callsites use typed `to`/`params`. No `/engagements/${` templates remain in the file. All 4 RTL tests GREEN. `pnpm typecheck` exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: Verify + document OPS-07 ActionBar has no string-nav</name>
  <files>frontend/src/pages/Dashboard/components/ActionBar.tsx</files>
  <action>
    1. Read the entire `ActionBar.tsx` file.
    2. Grep inside it for `navigate(` and `` `/ `` (backtick-slash) string templates — assert zero matches.
    3. Confirm that "New Engagement" / "New Request" use `useWorkCreation().openPalette(...)` (modal palette, no router call) and that the Cmd+K button dispatches a synthetic `KeyboardEvent` to the global listener in `CommandPalette.tsx`.
    4. Add a short JSDoc-style comment block ABOVE the component (or above the relevant handlers) documenting:
       ```
       // DEBT-01 / OPS-07 verified 2026-04-08:
       // This ActionBar intentionally contains NO TanStack Router navigate() calls.
       // - New Engagement / New Request open the WorkCreation palette (modal) via useWorkCreation().openPalette(...).
       // - Cmd+K dispatches a synthetic KeyboardEvent consumed by the global listener in CommandPalette.tsx.
       // Do not "modernize" these to navigate({ to, params }) — they are not navigations.
       ```
    5. Do NOT change any handler logic. No behavior changes.
    6. Run `pnpm typecheck` to confirm no regressions from the comment insertion.
  </action>
  <verify>
    <automated>cd frontend && grep -nE "navigate\\(|`/" src/pages/Dashboard/components/ActionBar.tsx; pnpm --filter frontend exec tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>ActionBar.tsx contains the verification comment. grep for `navigate(` and backtick-URL templates returns zero hits. Typecheck clean. OPS-07 portion of DEBT-01 formally closed with "no string-nav present — verified".</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                               |
| -------- | ------------------------------------------------------------------------- |
| (none)   | Pure type-safety refactor in frontend client code. No new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category                   | Component                | Disposition | Mitigation Plan                                                                                                                                                     |
| --------- | -------------------------- | ------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-19-01   | T (Tampering)              | Router navigation target | accept      | Typed `to` literal is compile-time validated against `FileRoutesByPath`; a wrong target becomes a TS error instead of a runtime 404. No user input flows into `to`. |
| T-19-02   | I (Information Disclosure) | engagement.id in URL     | accept      | No change vs current behavior — `engagement.id` already appeared in the string URL. RLS on the destination route controls visibility.                               |

</threat_model>

<verification>
1. `pnpm --filter frontend test EngagementStageGroup -- --run` — all 4 tests GREEN
2. `pnpm typecheck` — exit 0
3. `grep -rn "/engagements/\\${" frontend/src/pages/Dashboard/components/EngagementStageGroup.tsx` — zero hits
4. `grep -n "navigate(" frontend/src/pages/Dashboard/components/ActionBar.tsx` — zero hits
5. Manual smoke (optional): open dashboard, click an engagement row, confirm navigation works
</verification>

<success_criteria>

- DEBT-01 OPS-03: both callsites in `EngagementStageGroup.tsx` use typed `to`/`params`
- DEBT-01 OPS-07: `ActionBar.tsx` documented as containing zero navigate calls, verified by grep
- New RTL test file exists and passes (click + Enter + Space + negative key)
- `pnpm typecheck` clean
- Zero `/engagements/${` string templates anywhere in `EngagementStageGroup.tsx`
- No new dependencies added
- No i18n / RTL / layout changes (refactor only, per D1 + Deferred)
  </success_criteria>

<output>
After completion, create `.planning/phases/19-tech-debt-cleanup/19-01-SUMMARY.md`.

Commit message: `refactor(19-01): typed router nav for OPS-03 (DEBT-01)`
</output>
