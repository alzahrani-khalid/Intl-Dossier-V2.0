---
phase: 19-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - $HOME/.claude/get-shit-done/bin/lib/roadmap.cjs
  - $HOME/.claude/get-shit-done/bin/gsd-tools.cjs
  - $HOME/.claude/get-shit-done/bin/lib/__tests__/roadmap-sync-progress.test.cjs
  - $HOME/.claude/agents/gsd-executor.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - DEBT-02
must_haves:
  truths:
    - '`gsd-tools roadmap sync-progress` regenerates the entire progress table from phase manifest state on each call (D3)'
    - 'The table is delimited by stable `<!-- gsd:progress:start -->` / `<!-- gsd:progress:end -->` markers (D4)'
    - 'Content outside the markers is preserved byte-for-byte'
    - 'If markers are missing on first run, the subcommand bootstraps them at the `## Progress` section'
    - 'If only one marker is present, the subcommand errors out (no silent repair)'
    - '`gsd-executor` calls `sync-progress` after each atomic plan commit (D2)'
    - 'Re-running `sync-progress` with no state changes produces byte-equal output (idempotent)'
    - 'Source of truth is phase manifest files under `.planning/phases/*`, NOT `- [x]` checkboxes in PLAN.md (D3)'
  artifacts:
    - path: '$HOME/.claude/get-shit-done/bin/lib/roadmap.cjs'
      provides: 'PROGRESS_START/PROGRESS_END constants, renderProgressTable(phases), bootstrapMarkers(content, region), cmdRoadmapSyncProgress(cwd, raw)'
      contains: 'cmdRoadmapSyncProgress'
    - path: '$HOME/.claude/get-shit-done/bin/gsd-tools.cjs'
      provides: "`case 'sync-progress'` wired into the roadmap subcommand switch + help text"
      contains: 'sync-progress'
    - path: '$HOME/.claude/get-shit-done/bin/lib/__tests__/roadmap-sync-progress.test.cjs'
      provides: 'Vitest fixtures: bootstrap, replace, idempotency, byte-preservation, single-marker error'
      min_lines: 120
    - path: '$HOME/.claude/agents/gsd-executor.md'
      provides: 'Hook point updated to call `roadmap sync-progress` with no phase argument'
      contains: 'roadmap sync-progress'
    - path: '.planning/ROADMAP.md'
      provides: '`<!-- gsd:progress:start -->` / `<!-- gsd:progress:end -->` markers wrapping the progress table'
      contains: 'gsd:progress:start'
  key_links:
    - from: 'gsd-executor atomic commit step'
      to: 'gsd-tools.cjs roadmap sync-progress'
      via: 'shell invocation after commit returns'
      pattern: 'roadmap sync-progress'
    - from: 'cmdRoadmapSyncProgress'
      to: 'renderProgressTable(collectPhasesFromDisk(cwd))'
      via: 'pure function chain inside withPlanningLock'
      pattern: 'renderProgressTable'
    - from: 'cmdRoadmapSyncProgress'
      to: '.planning/ROADMAP.md between markers'
      via: 'indexOf slice + write-then-rename atomic write'
      pattern: 'PROGRESS_START'
---

<objective>
Add a new `gsd-tools roadmap sync-progress` subcommand that regenerates the `## Progress`
table in `.planning/ROADMAP.md` from phase manifest state on every call, wrap that region
with stable `<!-- gsd:progress:start -->` / `<!-- gsd:progress:end -->` markers, bootstrap
the markers on first run, wire `gsd-executor` to call it after each atomic plan commit,
and ship a Vitest fixture suite that proves bootstrap/replace/idempotency/byte-preservation/
single-marker-error behavior.

Purpose: Closes DEBT-02. Eliminates manual hand-editing of Plans Complete / Status /
Completed columns and removes format-drift risk from per-row regex patching.

Output: New subcommand, executor wiring, markers bootstrapped in live ROADMAP.md, passing
test suite, self-test proving live output matches manifest state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-tech-debt-cleanup/19-CONTEXT.md
@.planning/phases/19-tech-debt-cleanup/19-RESEARCH.md
@$HOME/.claude/get-shit-done/bin/gsd-tools.cjs
@$HOME/.claude/get-shit-done/bin/lib/roadmap.cjs
@$HOME/.claude/agents/gsd-executor.md

<interfaces>
<!-- Existing helpers to REUSE (no hand-rolling — research Don't-Hand-Roll table) -->

From bin/lib/roadmap.cjs (existing):

- `findPhaseInternal(cwd, phaseIdentifier)` — returns phase object with `.plans[]`, `.summaries[]`, `.milestone`
- `cmdRoadmapAnalyze(cwd, raw)` — already walks `.planning/phases/*` and groups by milestone
- `cmdRoadmapUpdatePlanProgress(cwd, phaseArg, raw)` at lines 250-347 — KEEP as backward-compat wrapper; do NOT delete
- `withPlanningLock(cwd, fn)` at line 280 — wraps atomic mutation; re-entrancy TBD in Wave 0
- `planningPaths(cwd).roadmap` — path to `.planning/ROADMAP.md`

From bin/gsd-tools.cjs:

- `roadmap` subcommand switch at lines 630-642
- Help text block at lines 44-47

Status vocabulary (MUST match existing table, per Pitfall 3):

```
STATUS = { complete: 'Complete', in_progress: 'In Progress', planned: 'Planned', not_started: 'Not started' }
```

Target route shape for markers (D4):

```
<!-- gsd:progress:start -->

| Phase                               | Milestone | Plans Complete | Status      | Completed  |
| ----------------------------------- | --------- | -------------- | ----------- | ---------- |
| ...rows from manifest...                                                                     |

<!-- gsd:progress:end -->
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0 (Wave 0): Resolve 3 open questions from research</name>
  <files>.planning/phases/19-tech-debt-cleanup/19-02-WAVE0-NOTES.md</files>
  <action>
    Before writing any production code, resolve the 3 open questions and dump findings
    to a Wave 0 notes file in the plan directory. This file is consumed by Tasks 1-4 below.

    **Open Question 1 — Phase manifest `name` field availability:**
    1. List all manifest files: `ls .planning/phases/*/.manifest.json` (or whatever the actual manifest path is — grep `state begin-phase` in gsd-tools.cjs to confirm).
    2. For each manifest, extract the `name` field (jq or node one-liner).
    3. Diff each `name` against the displayed label in the current `.planning/ROADMAP.md` `## Progress` table (lines ~167-194).
    4. Record: (a) which phases have a `name` field, (b) which match the displayed label exactly, (c) which need a fallback strategy.
    5. Decision: if ≥ 1 manifest is missing `name` OR the labels don't match byte-for-byte, the renderer MUST have a fallback: `manifest.name ?? titleCase(phaseDir.split('-').slice(1).join(' '))`. Record the fallback rule.

    **Open Question 2 — Other callers of `update-plan-progress`:**
    1. Grep `update-plan-progress` across `$HOME/.claude/agents/` and `$HOME/.claude/get-shit-done/workflows/`.
    2. List every file + line number found.
    3. Decision: keep `update-plan-progress` as a THIN WRAPPER that internally calls `cmdRoadmapSyncProgress` (backward-compat, zero breakage for stale callers). Do NOT delete the old command.
    4. Record full list of call sites so Task 3 can update the primary executor path deliberately.

    **Open Question 3 — `withPlanningLock` re-entrancy:**
    1. Locate `withPlanningLock` implementation (likely `bin/lib/security.cjs` or similar).
    2. Read the implementation — determine whether nested calls deadlock, no-op, or succeed.
    3. Decision:
       - If REENTRANT → `sync-progress` may be called from inside any existing locked command safely.
       - If NON-REENTRANT → `cmdRoadmapSyncProgress` must NOT be invoked from inside another `withPlanningLock`; Task 3 must ensure the executor calls it AFTER `commit` returns (sequential), and the backward-compat wrapper in OQ2 must drop the outer lock before delegating.
    4. Record the finding + the required call-ordering constraint.

    Write all three findings + decisions to `19-02-WAVE0-NOTES.md` with headings per question.

  </action>
  <verify>
    <automated>test -f .planning/phases/19-tech-debt-cleanup/19-02-roadmap-sync-progress/19-02-WAVE0-NOTES.md && grep -E "OQ1|OQ2|OQ3" .planning/phases/19-tech-debt-cleanup/19-02-roadmap-sync-progress/19-02-WAVE0-NOTES.md</automated>
  </verify>
  <done>Wave 0 notes file exists with all 3 questions resolved and documented decisions that Tasks 1-4 will cite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Write failing Vitest fixtures for sync-progress</name>
  <files>$HOME/.claude/get-shit-done/bin/lib/__tests__/roadmap-sync-progress.test.cjs</files>
  <behavior>
    - Test 1 (bootstrap): ROADMAP fixture with `## Progress` heading + existing table, NO markers → after `cmdRoadmapSyncProgress`, markers exist wrapping a regenerated table; pre-`## Progress` content and post-table content are byte-identical
    - Test 2 (replace): ROADMAP fixture with markers present and stale content between them → after call, content between markers is the freshly rendered table; content outside markers is byte-identical
    - Test 3 (idempotency): Running `cmdRoadmapSyncProgress` twice back-to-back against the same fixture produces byte-equal output on the second call
    - Test 4 (byte preservation outside): Fixture has a custom comment above and below the markers; after call, hash of pre-marker slice and post-marker slice is unchanged
    - Test 5 (single-marker error): Fixture has only `<!-- gsd:progress:start -->` (no end marker) → call throws/exits with an explicit "manual repair required" error
    - Test 6 (manifest-derived rows): Fixture with 3 fake phase directories (varying plan/summary counts) → rendered rows match expected status values (`Not started`, `Planned`, `In Progress`, `Complete`) per the STATUS vocabulary
    - Test 7 (no checkbox parsing): Fixture includes a PLAN.md with `- [x]` and `- [ ]` lines — the rendered table MUST ignore them and derive state only from disk inventory (plan count vs summary count)
  </behavior>
  <action>
    Create Vitest (CommonJS) test file at `$HOME/.claude/get-shit-done/bin/lib/__tests__/roadmap-sync-progress.test.cjs`.
    Use `tmp` or `fs.mkdtempSync` to create per-test fixture directories with a fake
    `.planning/` tree: `.planning/ROADMAP.md` + `.planning/phases/NN-slug/` containing
    fake PLAN.md and SUMMARY.md files.
    Import `cmdRoadmapSyncProgress` from `../roadmap.cjs` (will not exist yet — tests fail at import).
    For each test, set up fixture → call `cmdRoadmapSyncProgress(fixtureDir, true)` → read ROADMAP.md → assert.
    Use `crypto.createHash('sha256')` for byte-preservation hashing.
    Run tests — they MUST all fail (function doesn't exist yet). RED state.
  </action>
  <verify>
    <automated>cd "$HOME/.claude/get-shit-done" && npx vitest run bin/lib/__tests__/roadmap-sync-progress.test.cjs 2>&1 | tail -40</automated>
  </verify>
  <done>Test file exists with 7 tests. All 7 fail (import error or function undefined). RED state captured. No production code written yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement renderProgressTable + cmdRoadmapSyncProgress in lib/roadmap.cjs</name>
  <files>$HOME/.claude/get-shit-done/bin/lib/roadmap.cjs, $HOME/.claude/get-shit-done/bin/gsd-tools.cjs</files>
  <behavior>
    - After change: all 7 Wave 0 tests from Task 1 pass
    - `cmdRoadmapSyncProgress` reuses `withPlanningLock` per the Wave 0 OQ3 decision
    - Source of truth is disk inventory (plan files vs summary files), NEVER `- [x]` parsing (D3)
    - Renders FULL table from all discovered phases on every call (D3)
    - Handles decimal phase numbers (e.g. 19.1) via existing sort in `findPhaseInternal` (do NOT re-sort lexically)
    - Marker tokens are `<!-- gsd:progress:start -->` / `<!-- gsd:progress:end -->` exported as constants
    - Status vocabulary matches existing table: `Complete`, `In Progress`, `Planned`, `Not started`
    - Display label derives from manifest `name` field with title-case fallback per Wave 0 OQ1 decision
  </behavior>
  <action>
    Edit `$HOME/.claude/get-shit-done/bin/lib/roadmap.cjs`:
    1. Add near top (alongside other constants):
       ```js
       const PROGRESS_START = '<!-- gsd:progress:start -->'
       const PROGRESS_END = '<!-- gsd:progress:end -->'
       const STATUS = {
         complete: 'Complete',
         in_progress: 'In Progress',
         planned: 'Planned',
         not_started: 'Not started',
       }
       ```
    2. Add `function collectPhasesFromDisk(cwd)` that wraps/reuses the existing
       `cmdRoadmapAnalyze` logic (do NOT duplicate directory walking — import or call
       the same helper). Returns an array of `{ phaseNumber, displayLabel, milestone,
       planCount, summaryCount, isComplete, completedDate }` sorted by numeric phase
       (use the existing sort).
    3. Add `function renderProgressTable(phases)` — pure function matching the
       skeleton in research section "DEBT-02 — Subcommand Skeleton" (lines 172-190 of
       RESEARCH.md). Use `padEnd` widths that match the existing table's column widths
       in `.planning/ROADMAP.md`. Use the STATUS constant. `displayLabel` comes from
       manifest `name` with fallback per Wave 0 OQ1.
    4. Add `function bootstrapMarkers(content, region)`:
       - Locate `## Progress` heading via `indexOf('## Progress')`
       - Locate the first `| Phase` table header line after that heading
       - Locate the end of that table block (first blank line after the last `|`-prefixed row)
       - Replace the existing table block with `region` (which already contains the markers)
       - Return new content
       - If `## Progress` heading is missing, throw an explicit error (`ROADMAP.md missing ## Progress section`)
    5. Add `function cmdRoadmapSyncProgress(cwd, raw)`:
       - Use `withPlanningLock(cwd, ...)` per Wave 0 OQ3 decision
       - Read `planningPaths(cwd).roadmap`
       - Compute `phases = collectPhasesFromDisk(cwd)`
       - Compute `table = renderProgressTable(phases)`
       - Build `region = PROGRESS_START + '\n\n' + table + '\n\n' + PROGRESS_END`
       - Find `startIdx = content.indexOf(PROGRESS_START)` and `endIdx = content.indexOf(PROGRESS_END)`
       - If both present → slice + replace (string `indexOf`, NO regex, per Pitfall 1)
       - If both absent → call `bootstrapMarkers(content, region)`
       - If only one present → throw `ROADMAP.md has only one progress marker — manual repair required`
       - Write atomically (use existing atomic-write helper if present; otherwise write to `.tmp` then `fs.renameSync`)
       - Return `{ updated: true }` via the existing `output()` helper
    6. Export `cmdRoadmapSyncProgress`, `renderProgressTable`, `PROGRESS_START`, `PROGRESS_END` from the module.

    Edit `$HOME/.claude/get-shit-done/bin/gsd-tools.cjs`:
    7. In the `roadmap` subcommand switch (lines 630-642), add:
       ```js
       case 'sync-progress':
         return cmdRoadmapSyncProgress(cwd, raw)
       ```
    8. Update the help text block (lines 44-47) to include `roadmap sync-progress` with a one-line description.

    Run the Task 1 test suite — all 7 MUST pass.

  </action>
  <verify>
    <automated>cd "$HOME/.claude/get-shit-done" && npx vitest run bin/lib/__tests__/roadmap-sync-progress.test.cjs 2>&1 | tail -40</automated>
  </verify>
  <done>All 7 fixture tests GREEN. `cmdRoadmapSyncProgress` exported and wired into `gsd-tools.cjs`. `node bin/gsd-tools.cjs roadmap sync-progress --help` (or equivalent) mentions the new command.</done>
</task>

<task type="auto">
  <name>Task 3: Wire gsd-executor + update backward-compat wrapper</name>
  <files>$HOME/.claude/agents/gsd-executor.md, $HOME/.claude/get-shit-done/bin/lib/roadmap.cjs</files>
  <action>
    1. Based on Wave 0 OQ2 findings, update EVERY call site of `roadmap update-plan-progress`
       in `$HOME/.claude/agents/gsd-executor.md` (research confirms lines 468, 483, 529 at minimum)
       to instead invoke:
       ```bash
       node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" roadmap sync-progress
       ```
       Note: no phase argument — `sync-progress` rebuilds the full table per D3.

    2. Update the description text at line 483 and the verification checklist at line 529
       to reflect that the whole table is regenerated (not just the current phase's row).

    3. Update any ADDITIONAL call sites discovered in Wave 0 OQ2 across
       `$HOME/.claude/agents/` and `$HOME/.claude/get-shit-done/workflows/` — apply the
       same replacement.

    4. Keep `cmdRoadmapUpdatePlanProgress` in `lib/roadmap.cjs` as a BACKWARD-COMPAT
       wrapper: its new body simply delegates to `cmdRoadmapSyncProgress(cwd, raw)` after
       logging a one-line deprecation notice. This protects any stale caller not caught
       by the grep. Honor the Wave 0 OQ3 re-entrancy constraint — if non-reentrant, the
       wrapper must NOT nest `withPlanningLock` calls.

    5. Do NOT remove the `update-plan-progress` case from the `roadmap` subcommand switch —
       it still routes to the (now-delegating) wrapper.

  </action>
  <verify>
    <automated>grep -rn "roadmap update-plan-progress\|roadmap sync-progress" "$HOME/.claude/agents/" "$HOME/.claude/get-shit-done/workflows/" 2>&1 | tail -20</automated>
  </verify>
  <done>All primary call sites invoke `sync-progress`. `update-plan-progress` wrapper still exists and delegates. Re-running the Task 2 tests still passes (no regression from wrapper change).</done>
</task>

<task type="auto">
  <name>Task 4: Bootstrap markers in live ROADMAP.md + self-test</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Run the new subcommand against the actual project ROADMAP.md to perform the
    first-run bootstrap, then self-test per CONTEXT downstream notes.

    1. Snapshot current state:
       ```bash
       cp .planning/ROADMAP.md /tmp/roadmap-before.md
       sha256sum /tmp/roadmap-before.md
       ```
    2. First run (bootstrap):
       ```bash
       node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" roadmap sync-progress
       ```
    3. Diff against the snapshot:
       ```bash
       diff /tmp/roadmap-before.md .planning/ROADMAP.md
       ```
       Expected changes: ONLY (a) marker insertion, (b) any required label
       normalization per Wave 0 OQ1 decisions. Content outside `## Progress` MUST be
       byte-identical. If anything outside `## Progress` changed, STOP and investigate.
    4. Idempotency check:
       ```bash
       cp .planning/ROADMAP.md /tmp/roadmap-run1.md
       node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" roadmap sync-progress
       diff /tmp/roadmap-run1.md .planning/ROADMAP.md
       ```
       Expected: ZERO diff.
    5. Byte-preservation check: add a harmless comment ABOVE `## Progress` (e.g.
       `<!-- test-preserve-me -->`), run sync-progress, confirm the comment is still
       there and byte-identical.
    6. Self-test (manifest ↔ table parity): pick 3 phases at random, read their
       `.manifest.json` + count their plan/summary files on disk, and assert the table
       row's `Plans Complete` and `Status` values match. Document the 3 sampled phases
       and their match status in `19-02-SELFTEST.md`.
    7. Remove the test comment from step 5 before committing.

  </action>
  <verify>
    <automated>node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" roadmap sync-progress && cp .planning/ROADMAP.md /tmp/r1.md && node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" roadmap sync-progress && diff /tmp/r1.md .planning/ROADMAP.md && grep -c "gsd:progress:start\|gsd:progress:end" .planning/ROADMAP.md</automated>
  </verify>
  <done>Markers bootstrapped in `.planning/ROADMAP.md`. Re-run diff is empty (idempotent). Byte-preservation verified. Self-test file `19-02-SELFTEST.md` documents manifest↔table parity for 3 sampled phases.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                       |
| ------------------------------------ | ------------------------------------------------- |
| gsd-tools CLI ↔ .planning/ROADMAP.md | Tool-owned region in a human-edited markdown file |

## STRIDE Threat Register

| Threat ID | Category              | Component                                                       | Disposition | Mitigation Plan                                                                                       |
| --------- | --------------------- | --------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------- |
| T-19-03   | T (Tampering)         | Mid-write corruption of ROADMAP.md                              | mitigate    | Reuse `withPlanningLock` + write-then-rename atomic write so a crash never leaves a half-written file |
| T-19-04   | T (Tampering)         | Silent auto-repair of partial markers masking a real corruption | mitigate    | Explicit error on single-marker state; require manual repair (no auto-insert of the missing marker)   |
| T-19-05   | I (Info Disclosure)   | Phase names / counts rendered                                   | accept      | Table content is already public in the repo; no secrets involved                                      |
| T-19-06   | D (Denial of Service) | Lock deadlock via re-entrancy                                   | mitigate    | Wave 0 OQ3 verifies re-entrancy behavior; call ordering enforced in Task 3                            |

</threat_model>

<verification>
1. `npx vitest run bin/lib/__tests__/roadmap-sync-progress.test.cjs` — all 7 tests GREEN
2. `node bin/gsd-tools.cjs roadmap sync-progress` run twice against live ROADMAP.md → second diff is empty (idempotent)
3. Content outside `## Progress` is byte-identical before/after first run (manual diff inspection)
4. `.planning/ROADMAP.md` contains exactly one `<!-- gsd:progress:start -->` and one `<!-- gsd:progress:end -->`
5. `grep -rn "roadmap update-plan-progress" ~/.claude/agents/` returns zero live call sites (only the backward-compat wrapper definition remains in `lib/roadmap.cjs`)
6. Manifest ↔ table parity self-test documented in `19-02-SELFTEST.md` for 3 sampled phases
7. No `- [x]` checkbox parsing anywhere in the new code (grep the diff)
</verification>

<success_criteria>

- `gsd-tools roadmap sync-progress` subcommand exists, documented in `--help`
- Full progress table regenerates from phase manifest state on every call (D3)
- Marker-bounded region with stable tokens (D4); content outside markers byte-preserved
- First-run bootstrap inserts markers at `## Progress` section
- Single-marker state errors out with clear message (no silent repair)
- `gsd-executor` calls `sync-progress` after atomic plan commits (D2)
- `update-plan-progress` retained as backward-compat wrapper delegating to `sync-progress`
- Vitest suite covers: bootstrap, replace, idempotency, byte-preservation, single-marker error, manifest-derived rows, ignores `- [x]` checkboxes
- Live self-test on `.planning/ROADMAP.md` passes manifest↔table parity for 3 sampled phases
- All 3 Wave 0 open questions resolved and documented in `19-02-WAVE0-NOTES.md`
- No new dependencies
  </success_criteria>

<output>
After completion, create `.planning/phases/19-tech-debt-cleanup/19-02-SUMMARY.md`.

Atomic commit plan (one per logical unit):

- `test(19-02): add failing fixtures for roadmap sync-progress` (Task 1)
- `feat(19-02): implement roadmap sync-progress subcommand (DEBT-02)` (Task 2)
- `refactor(19-02): wire gsd-executor to sync-progress + backward-compat wrapper` (Task 3)
- `chore(19-02): bootstrap progress markers in ROADMAP.md + self-test` (Task 4)
  </output>
