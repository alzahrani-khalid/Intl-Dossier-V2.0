---
phase: 41
plan: 10
type: execute
wave: 0
depends_on: []
requirements: [DRAWER-01]
source_gaps: [G1]
files_modified:
  - frontend/src/pages/Dashboard/index.tsx
  - frontend/tests/e2e/dossier-drawer-trigger-recent.spec.ts
  - .planning/phases/41-dossier-drawer/deferred-items.md # conditional: Option B/C only (Option A leaves this file untouched)
autonomous: false
must_haves:
  truths:
    - 'Decision recorded in plan SUMMARY about how to reconcile RecentDossiers regression with Phase 41 trigger spec — either re-mount RecentDossiers in Dashboard/index.tsx OR update the trigger spec to skip when widget absent. User explicit confirmation captured in checkpoint.'
    - 'Default recommendation: re-mount RecentDossiers in Dashboard so the Phase 41 trigger surface for D-01 stays exercised. Per CLAUDE.md "Dossier-Centric Development Patterns" the dossier widget is a load-bearing dossier reference; removing it strips the dashboard of dossier-centric affordances. Phase 38 + Phase 41 both depend on its presence.'
    - 'If user accepts the re-mount, Dashboard/index.tsx imports RecentDossiers + ForumsStrip + Digest from ./widgets and mounts them in a position consistent with the Phase 38 baseline (left-side after WeekAhead/OverdueCommitments OR right-side after MyTasks — preserve the e8f3341a layout intent if possible).'
    - 'If user rejects the re-mount, the trigger spec dossier-drawer-trigger-recent.spec.ts is updated to skip when the widget is not mounted (with explicit `test.skip(!widgetExists, ...)`) and a deferred-items.md entry is created tracking the regression for Phase 42 design review.'
    - 'Spec dossier-drawer-trigger-recent.spec.ts transitions FAIL → PASS (or FAIL → SKIP under the rejection path) with the chosen resolution applied.'
  artifacts:
    - path: 'frontend/src/pages/Dashboard/index.tsx'
      provides: 'Dashboard composition that mounts the dossier-centric widgets'
      contains: 'RecentDossiers'
      conditional: 'Only present if user accepts re-mount in Task 1 checkpoint.'
    - path: '.planning/phases/41-dossier-drawer/deferred-items.md'
      provides: 'Tracking row for the alternate path (RecentDossiers permanently removed)'
      contains: 'RecentDossiers dashboard mount'
      conditional: 'Only present if user rejects re-mount.'
  key_links:
    - from: 'Dashboard/index.tsx mount'
      to: 'RecentDossiers widget (already exists; already wired to openDossier per plan 41-06)'
      via: 'JSX render in DashboardGrid OR a sibling slot'
      pattern: '<RecentDossiers'
---

<objective>
Close VERIFICATION.md G1 (`recent-dossier-trigger` testid not found on `/dashboard`). The widget itself still exists at `frontend/src/pages/Dashboard/widgets/RecentDossiers.tsx` and is fully wired by Plan 41-06 (`onClick={() => openDossier({ id: d.id, type: d.type })}` with `data-testid="recent-dossier-trigger"`). The regression is upstream: the user's pre-existing layout work (commit `e8f3341a` "chore(wip): snapshot in-progress design-system + layout work before phase 41 execution") removed `RecentDossiers`, `ForumsStrip`, and `Digest` from `Dashboard/index.tsx`. The current dashboard mounts only `KpiStrip`, `WeekAhead`, `OverdueCommitments`, `SlaHealth`, `VipVisits`, and `MyTasks`.

Per the gap-closure context, this gap is **WIP-REGRESSION** territory — not a Phase 41 implementation gap. The plan must surface a DECISION POINT to the user rather than silently restoring the layout. CLAUDE.md "Dossier-Centric Development Patterns" guidance ("Everything starts with a Dossier") favors keeping the dossier widget mounted. The Phase 38 baseline (which the user already shipped) included it. Phase 41's trigger spec was written against its presence.

This plan presents both options in a checkpoint, captures the user's decision, applies the chosen path, and updates the spec accordingly. The plan is **non-autonomous** — Task 1 is a `checkpoint:decision` task.

Purpose: Restore (or formally retire) the RecentDossiers trigger surface so the Phase 41 trigger inventory (D-01) is in a known-consistent state.

Output:

- Path A (recommended): Dashboard/index.tsx re-mounts `RecentDossiers` (and optionally `ForumsStrip` + `Digest` if their absence would also degrade the dossier-centric layout). Spec passes as-is.
- Path B (alternate): Spec is updated with a `test.skip` guard plus a `deferred-items.md` row tracking the regression for Phase 42.
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/41-dossier-drawer/41-VERIFICATION.md
@.planning/phases/41-dossier-drawer/41-CONTEXT.md
@.planning/phases/41-dossier-drawer/41-06-SUMMARY.md
@.planning/phases/41-dossier-drawer/deferred-items.md
@CLAUDE.md

@frontend/src/pages/Dashboard/index.tsx
@frontend/src/pages/Dashboard/widgets/RecentDossiers.tsx
@frontend/src/pages/Dashboard/widgets/ForumsStrip.tsx
@frontend/src/pages/Dashboard/widgets/Digest.tsx
@frontend/src/pages/Dashboard/widgets/index.ts
@frontend/tests/e2e/dossier-drawer-trigger-recent.spec.ts

<interfaces>
<!-- Current state of Dashboard/index.tsx (verified) -->
```tsx
import { type ReactElement } from 'react'
import { DashboardGrid } from './widgets/DashboardGrid'
import { DashboardHero } from './components/DashboardHero'
import { KpiStrip, WeekAhead, OverdueCommitments, SlaHealth, VipVisits, MyTasks } from './widgets'
import './widgets/dashboard.css'

export function Dashboard(): ReactElement {
return (
<div className="page dash-root">
<DashboardHero />
<KpiStrip />
<DashboardGrid
left={[<WeekAhead key="wa" />, <OverdueCommitments key="oc" />]}
right={[<SlaHealth key="sh" />, <VipVisits key="vv" />, <MyTasks key="mt" />]}
/>
</div>
)
}

````

<!-- Available but unmounted widgets (still exported from widgets/index.ts) -->
- `RecentDossiers` — wired to openDossier per Plan 41-06; data-testid="recent-dossier-trigger"
- `ForumsStrip` — wired to openDossier per Plan 41-06; data-testid="forum-trigger"
- `Digest` — not a drawer trigger, but part of the original Phase 38 dashboard composition

<!-- The trigger spec (frontend/tests/e2e/dossier-drawer-trigger-recent.spec.ts) -->
```ts
test('clicking a recent dossier row opens drawer with all sections', async ({ page }) => {
  await loginForListPages(page)
  await page.goto('/')
  // ... waits for fonts
  const trigger = page.getByTestId('recent-dossier-trigger').first()
  await trigger.click()
  // ... asserts drawer opens with all 5 sections
})
````

The spec hits `page.goto('/')`, which redirects to `/dashboard`. The `recent-dossier-trigger` testid only renders if `RecentDossiers` is mounted in `Dashboard/index.tsx`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide — re-mount RecentDossiers vs. retire the trigger spec</name>
  <decision>How should Phase 41 reconcile the RecentDossiers trigger surface (D-01) with the user's pre-existing dashboard layout work that removed the widget?</decision>
  <context>
    **Background:**
    - Phase 38 shipped `RecentDossiers` as a dashboard widget with `<a href={`/dossiers/${id}`}>` row navigation.
    - Phase 41 Plan 41-06 (Wave 1) replaced the per-row `<Link>` with `<button onClick={() => openDossier({ id, type })}>` so clicks open the drawer instead of navigating away. The widget gained `data-testid="recent-dossier-trigger"`.
    - Phase 41 Plan 41-07 (Wave 2) shipped a Playwright spec asserting the trigger works.
    - Between Plan 41-06 landing and the smoke run, commit `e8f3341a` (the user's pre-existing WIP layout work) modified `Dashboard/index.tsx` to remove `RecentDossiers`, `ForumsStrip`, and `Digest` from the rendered tree. The widgets are still exported from `widgets/index.ts` and still wired to `openDossier`.
    - Smoke run on 2026-05-02 fails the trigger spec because the testid never appears in the DOM (the widget is not mounted).

    **CLAUDE.md guidance:**
    - "Dossier-Centric Development Patterns" — "Everything starts with a Dossier". Removing the dossier-centric widget from the dashboard contradicts this principle.
    - Phase 38 / Phase 41 architecture both treat RecentDossiers as a load-bearing dashboard surface for dossier discovery.
    - The user's commit message (`chore(wip): snapshot in-progress design-system + layout work before phase 41 execution`) suggests the removal may have been a mid-flight design experiment rather than a deliberate retire.

    **What this plan does NOT do:**
    - It does not silently restore the layout — that would override the user's WIP without consultation.
    - It does not modify other Phase 41 plans (which are immutable per gap-closure protocol).
    - It does not recreate any widget — they all still exist and are wired.

  </context>
  <options>
    <option id="option-a">
      <name>Re-mount RecentDossiers (and optionally ForumsStrip + Digest) in Dashboard/index.tsx</name>
      <pros>
        - Restores Phase 38 baseline + Phase 41 trigger surface in one move.
        - Spec dossier-drawer-trigger-recent.spec.ts passes without modification.
        - Aligns with CLAUDE.md "Dossier-Centric Development Patterns".
        - Zero spec/test infrastructure churn.
        - Adding the widget back is a 2-line edit (import + JSX).
      </pros>
      <cons>
        - Overrides the user's WIP layout decision in commit e8f3341a.
        - The user may have intentionally moved RecentDossiers to a different surface (e.g. a sidebar) that's not yet ready.
        - May briefly conflict with whatever layout work the user has in flight.
      </cons>
    </option>
    <option id="option-b">
      <name>Update the trigger spec to skip when RecentDossiers is not mounted; add deferred-items.md row</name>
      <pros>
        - Honors the user's WIP layout decision.
        - Spec stays in the suite (skipped, not deleted) so re-mounting later auto-restores coverage.
        - Phase 41's other 6 specs still cover D-01 trigger surfaces (calendar, OverdueCommitments dossier-group head, ForumsStrip even though it's also unmounted — wait, ForumsStrip is also missing from Dashboard, so the same issue exists there, just not exercised by a spec).
      </pros>
      <cons>
        - DRAWER-01 trigger spec coverage degrades — the most prominent dossier widget (RecentDossiers) loses its E2E gate.
        - Skipped tests rot. If RecentDossiers comes back without the spec being re-enabled, the regression goes unnoticed.
        - Departs from CLAUDE.md "Dossier-Centric" principle.
        - The downstream ForumsStrip trigger (also wired by Plan 41-06) is currently unmounted too — there's no Wave 2 spec for it (the trigger spec map in 41-07-PLAN only covers RecentDossiers + calendar). So Option B masks an even bigger drift: TWO of the four widget triggers from D-01 (RecentDossiers + ForumsStrip) are no longer mounted.
      </cons>
    </option>
    <option id="option-c">
      <name>Re-mount only what the trigger spec needs (RecentDossiers); leave ForumsStrip + Digest unmounted</name>
      <pros>
        - Minimal change to user's WIP layout (single widget restored).
        - Closes G1 without disturbing other layout decisions.
      </pros>
      <cons>
        - Half-measure. ForumsStrip is also a Phase 41 D-01 trigger surface; leaving it unmounted creates a silent gap that no spec exercises (the gap exists today; this option doesn't fix or surface it).
        - Inconsistent with how the Phase 38 baseline was structured.
      </cons>
    </option>
  </options>
  <resume-signal>
    Reply with: "option-a" / "option-b" / "option-c", optionally with placement guidance for restored widgets (e.g. "option-a, mount RecentDossiers in left column after WeekAhead, ForumsStrip+Digest in right column after MyTasks").
    The default if no response within reasonable time: option-a per CLAUDE.md "Dossier-Centric" guidance, with RecentDossiers + ForumsStrip restored to the Phase 38 baseline positions, Digest left out (it was less load-bearing).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply the chosen resolution</name>
  <files>
    frontend/src/pages/Dashboard/index.tsx
    frontend/tests/e2e/dossier-drawer-trigger-recent.spec.ts
    .planning/phases/41-dossier-drawer/deferred-items.md
  </files>
  <read_first>
    - frontend/src/pages/Dashboard/index.tsx (entire 18-line file — current state)
    - frontend/src/pages/Dashboard/widgets/index.ts (confirms RecentDossiers, ForumsStrip, Digest are still exported)
    - frontend/src/pages/Dashboard/widgets/RecentDossiers.tsx (confirms wiring + testid is intact post-Plan 41-06)
    - frontend/src/pages/Dashboard/widgets/DashboardGrid.tsx (the layout primitive — confirm it accepts an arbitrary number of children per side or has a max slot count)
    - frontend/tests/e2e/dossier-drawer-trigger-recent.spec.ts (the spec to update under Option B)
    - .planning/phases/41-dossier-drawer/deferred-items.md (existing tracking file; under Option B append a new row)
    - The Task 1 checkpoint resume-signal (the user's chosen option)
  </read_first>
  <action>
    Branch on the option chosen in Task 1.

    ---

    **If `option-a` (recommended default):**

    Edit `frontend/src/pages/Dashboard/index.tsx` to import RecentDossiers + ForumsStrip from widgets and re-add them to the layout. Replace lines 4 + 12-15 with:

    ```tsx
    import {
      KpiStrip,
      WeekAhead,
      OverdueCommitments,
      SlaHealth,
      VipVisits,
      MyTasks,
      RecentDossiers,
      ForumsStrip,
    } from './widgets'

    // ...

    <DashboardGrid
      left={[
        <WeekAhead key="wa" />,
        <RecentDossiers key="rd" />,
        <OverdueCommitments key="oc" />,
      ]}
      right={[
        <SlaHealth key="sh" />,
        <ForumsStrip key="fs" />,
        <VipVisits key="vv" />,
        <MyTasks key="mt" />,
      ]}
    />
    ```

    The exact placement (column + position) is per the user's resume-signal guidance, OR the default above. If the user's resume-signal specifies "RecentDossiers + ForumsStrip + Digest", import Digest as well and place it on the right column. Preserve all other widgets at their current positions.

    Verify `DashboardGrid` accepts the new array shape (read its prop types). If it has a max-slot rule (e.g. exactly 2-per-side), restructure as needed and document the choice in the SUMMARY.

    No spec changes. The existing `dossier-drawer-trigger-recent.spec.ts` will pass as-is once RecentDossiers is mounted.

    ---

    **If `option-b`:**

    Edit `frontend/tests/e2e/dossier-drawer-trigger-recent.spec.ts` to skip when the widget is not mounted. Replace the test body's first action lines:

    ```ts
    test('clicking a recent dossier row opens drawer with all sections', async ({ page }) => {
      await loginForListPages(page)
      await page.goto('/')
      await page.evaluate(() => document.fonts.ready)

      // Phase 41 G1 (VERIFICATION.md): RecentDossiers may be unmounted from
      // Dashboard/index.tsx pending Phase 42 layout decisions. Skip rather
      // than fail when the widget is absent. Re-enable by removing the skip
      // when the widget is restored. Tracked in deferred-items.md.
      const triggerExists = await page
        .getByTestId('recent-dossier-trigger')
        .first()
        .count()
      test.skip(
        triggerExists === 0,
        'RecentDossiers widget not mounted in Dashboard — see .planning/phases/41-dossier-drawer/deferred-items.md',
      )

      const trigger = page.getByTestId('recent-dossier-trigger').first()
      // ... existing assertions
    })
    ```

    Then append to `.planning/phases/41-dossier-drawer/deferred-items.md`:

    ```markdown
    ## RecentDossiers dashboard mount — DEFERRED

    **Source:** Plan 41-10 Task 1 user decision (option-b); 41-VERIFICATION.md G1 (WIP-REGRESSION).

    **Reason:** Commit `e8f3341a` removed RecentDossiers, ForumsStrip, and Digest from Dashboard/index.tsx as part of pre-Phase-41 design-system + layout WIP. The widgets still exist and are still wired to `openDossier` (Plan 41-06), but they are not mounted. User chose to defer the re-mount decision to Phase 42 rather than re-introduce them in Phase 41 gap closure.

    **Mitigation:** `dossier-drawer-trigger-recent.spec.ts` skips when the widget is absent (with a clear console message pointing back to this row). Re-enable by removing the `test.skip` guard once RecentDossiers is re-mounted.

    **Track:** Phase 42 (or whichever phase resolves the dashboard layout question). Re-mount restores both the trigger spec coverage and the dossier-centric dashboard pattern from CLAUDE.md.

    ## ForumsStrip dashboard mount — DEFERRED

    Same as above; the ForumsStrip trigger is also unmounted. There is no Phase 41 spec exercising the forum trigger (only RecentDossiers + calendar have specs), so no spec change is required — but the trigger surface is currently dark.
    ```

    ---

    **If `option-c`:**

    Apply only the Option A RecentDossiers re-mount (single import + single JSX entry); leave ForumsStrip and Digest unmounted. Append a SHORT row to `deferred-items.md` for ForumsStrip explaining its trigger is unmounted (consistent with Option B's ForumsStrip row).

    ---

    **Decision references:** D-01 (drawer trigger inventory MUST cover RecentDossiers + OverdueCommitments + ForumsStrip + calendar). VERIFICATION.md G1 (WIP-REGRESSION classification). CLAUDE.md Dossier-Centric Development Patterns.

  </action>
  <verify>
    <automated>OPT=$(cat /tmp/41-10-option 2&gt;/dev/null || echo "option-a"); if [[ "$OPT" == "option-a" ]] || [[ "$OPT" == "option-c" ]]; then grep -q "RecentDossiers" frontend/src/pages/Dashboard/index.tsx; elif [[ "$OPT" == "option-b" ]]; then grep -q "test\.skip" frontend/tests/e2e/dossier-drawer-trigger-recent.spec.ts &amp;&amp; grep -q "RecentDossiers dashboard mount" .planning/phases/41-dossier-drawer/deferred-items.md; fi &amp;&amp; cd frontend &amp;&amp; pnpm typecheck 2&gt;&amp;1 | grep -c "Dashboard/index" | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - The chosen option's artifacts exist (per the option's branch in `<action>`).
    - Under option-a or option-c: `grep -c "RecentDossiers" frontend/src/pages/Dashboard/index.tsx` returns ≥ 2 (one in the import, one in the JSX). The widget appears in either the `left` or `right` array of `DashboardGrid`.
    - Under option-b: `grep -c "test.skip" frontend/tests/e2e/dossier-drawer-trigger-recent.spec.ts` returns ≥ 1 AND a "RecentDossiers dashboard mount — DEFERRED" row exists in deferred-items.md.
    - `pnpm typecheck` reports 0 new errors on Dashboard/index.tsx (or on the spec file under option-b).
    - The change is bounded to the files listed in `<files>` (no other files modified).
    - Plan SUMMARY records which option was chosen and the user's resume-signal text verbatim.
  </acceptance_criteria>
  <done>
    The chosen option is applied. Spec dossier-drawer-trigger-recent.spec.ts either passes (option-a/c) or skips with a clear deferral message (option-b). DRAWER-01 trigger surface is in a known-consistent state.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                                                                      |
| -------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| Dashboard layout decision  | Affects user-visible analyst workstation. Any restoration of widgets must be confirmed by the user, not inferred.                |
| Test skip vs. test enabled | A skipped test rots silently. If chosen (option-b), the deferred-items.md row is the tracking mechanism so re-enable is visible. |

## STRIDE Threat Register

| Threat ID  | Category               | Component                            | Disposition | Mitigation Plan                                                                                                                                                                                                                      |
| ---------- | ---------------------- | ------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-41-10-01 | Tampering              | Dashboard layout override of WIP     | mitigate    | Decision is gated by `checkpoint:decision` task — no silent override of user's WIP layout commit. Default option (option-a) is documented as such, and resume-signal MUST be captured before Task 2 runs.                            |
| T-41-10-02 | Information Disclosure | Skipped test masking real regression | mitigate    | Under option-b, deferred-items.md tracks the regression with explicit Phase 42 follow-up. The skip uses a clear message pointing at that file. Phase 42 inventory check should grep for `test.skip` references to deferred-items.md. |

</threat_model>

<verification>
- Task 1 checkpoint resume-signal recorded in plan SUMMARY (verbatim quote).
- Task 2 applied the chosen option correctly (per `<verify>` block).
- Spec dossier-drawer-trigger-recent.spec.ts state matches the chosen option:
  - option-a/c: spec is unchanged and would PASS on next smoke run.
  - option-b: spec is unchanged in assertion logic but adds a `test.skip` guard at the start, transitioning FAIL → SKIP.
- Dashboard renders without runtime errors after the change (manual verification recommended in plan 41-11 smoke).
- No Phase 41 plans 41-01 through 41-09 modified (gap-closure protocol).
</verification>

<success_criteria>

- DRAWER-01 trigger inventory is in a known-consistent state — either fully restored or formally retired with deferred tracking.
- 1 of 7 VERIFICATION.md gaps (G1) closed.
- User decision captured for the next phase to inherit.
  </success_criteria>

<output>
On completion, create `.planning/phases/41-dossier-drawer/41-10-SUMMARY.md` documenting:
- The chosen option (verbatim resume-signal)
- The exact lines added/changed in the chosen path's files
- Whether ForumsStrip + Digest were also re-mounted (under option-a) or deferred (under option-b/c)
- Any deviations from the plan instructions (e.g. if DashboardGrid had a slot-count constraint that forced a different layout)
- Cross-reference: 41-VERIFICATION.md G1 row that this plan closes
</output>
</content>
</invoke>
