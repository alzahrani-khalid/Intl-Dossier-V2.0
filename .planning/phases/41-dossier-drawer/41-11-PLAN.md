---
phase: 41
plan: 11
type: execute
wave: 1
depends_on: [41-08, 41-09, 41-10]
requirements: [DRAWER-01, DRAWER-02, DRAWER-03]
verifies_gaps: [G1, G2, G3, G4, G5, G6, G7]
files_modified:
  - .planning/phases/41-dossier-drawer/41-VERIFICATION.md
autonomous: false
must_haves:
  truths:
    - 'All 14 dossier-drawer Playwright specs run after plans 41-08, 41-09, 41-10 land — expected outcome: 13/14 pass (visual baselines still deferred to HUMAN-UAT per Phase 40 precedent; the 2 visual tests remain in PENDING-BASELINE state, not failures)'
    - 'For each of the 7 gaps G1-G7 in 41-VERIFICATION.md, an explicit close-out row is added under a new "Smoke Results — gap closure pass (date)" section: gap id | spec | resolution | new status (PASS / SKIP / DEFERRED-WITH-TRACKING)'
    - 'Phase 41 verdict in 41-VERIFICATION.md frontmatter flips from `SMOKE-PARTIAL-GAPS-FOUND` to either `PASS` (all 7 gaps closed clean) or `PASS-WITH-DEVIATION` (some gaps closed via skip+defer rather than fix)'
    - 'playwright_pass / playwright_fail counters in frontmatter are updated to reflect the new run'
    - 'last_updated frontmatter date matches the smoke run date'
    - 'Sign-off Checklist at bottom of VERIFICATION.md ticked for the items now satisfied (all 10 D-13 cases pass on staging, axe-core EN+AR zero serious/critical, etc.)'
  artifacts:
    - path: '.planning/phases/41-dossier-drawer/41-VERIFICATION.md'
      provides: 'Updated phase verdict reflecting gap-closure outcomes'
      contains: 'Smoke Results — gap closure pass'
  key_links:
    - from: '41-08 / 41-09 / 41-10 SUMMARY files'
      to: '41-VERIFICATION.md gap-closure pass rows'
      via: 'Each gap row cites the closing plan id (41-08 G2/G5/G6/G7, 41-09 G3/G4, 41-10 G1)'
      pattern: 'Plan 41-(08|09|10)'
---

<objective>
Final gap-closure plan: re-run the Phase 41 Playwright smoke after plans 41-08, 41-09, 41-10 land, then update `41-VERIFICATION.md` with the new verdict. Includes a human checkpoint for the operator to confirm the live smoke (since this worktree is sandboxed and cannot start the dev server autonomously, per the Phase 40 / Phase 41-07 precedent).

This plan **verifies and records** the close-out of G1..G7 — it does not itself close any gap. The closing plans are 41-08 (G2/G5/G6/G7), 41-09 (G3/G4), and 41-10 (G1); 41-11's role is to run the live smoke after those plans land and update the canonical VERIFICATION.md verdict.

Purpose: Lock in the verdict for Phase 41. Either flip to `PASS` (all 7 gaps closed cleanly) or `PASS-WITH-DEVIATION` (some gaps tracked as deferred); ensure the canonical verification doc reflects reality; unblock Phase 42.

Output: An updated `41-VERIFICATION.md` with:

- Frontmatter `verdict`, `playwright_pass`, `playwright_fail`, `last_updated` reflecting the gap-closure smoke run.
- A new "Smoke Results — gap closure pass (YYYY-MM-DD)" section with a 7-row gap-by-gap close-out table.
- The Sign-off Checklist boxes ticked for items now satisfied.
- Optional `41-11-SUMMARY.md` documenting the smoke run output.
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/41-dossier-drawer/41-VERIFICATION.md
@.planning/phases/41-dossier-drawer/41-08-PLAN.md
@.planning/phases/41-dossier-drawer/41-09-PLAN.md
@.planning/phases/41-dossier-drawer/41-10-PLAN.md
@.planning/phases/41-dossier-drawer/deferred-items.md
@frontend/playwright.config.ts

<interfaces>
<!-- The 14 dossier-drawer specs that compose the smoke surface (from VERIFICATION.md): -->
1. dossier-drawer-trigger-recent.spec.ts (D-13 case 1) — G1
2. dossier-drawer-trigger-calendar.spec.ts (D-13 case 2) — G2
3. dossier-drawer-deeplink.spec.ts (D-13 case 3) — currently PASS
4. dossier-drawer-a11y.spec.ts ESC test (D-13 case 4) — currently PASS
5. dossier-drawer-rtl.spec.ts AR test (D-13 case 5) — G6
6. dossier-drawer-rtl.spec.ts EN test (D-13 case 5 inverse) — currently PASS
7. dossier-drawer-mobile.spec.ts (D-13 case 6) — G5
8. dossier-drawer-a11y.spec.ts Tab test (D-13 case 7) — currently PASS
9. dossier-drawer-cta.spec.ts open-full test (D-13 case 8) — currently PASS
10. dossier-drawer-cta.spec.ts log-engagement test (D-13 case 9) — currently PASS
11. dossier-drawer-cta.spec.ts brief-stub test — currently PASS
12. dossier-drawer-commitment-click.spec.ts (D-13 case 10) — G7
13. dossier-drawer-axe.spec.ts EN — G3
14. dossier-drawer-axe.spec.ts AR — G4

(The 2 visual regression tests in dossier-drawer-visual.spec.ts are NOT counted in this 14 — they are deferred to HUMAN-UAT per VERIFICATION.md "visual baselines deferred" deviation.)

<!-- Verdict mapping rule -->

- ALL 14 pass + visual baselines pending HUMAN-UAT only → verdict `PASS`
- 14 pass with at least one gap closed via skip+defer (e.g. G1 option-b) → verdict `PASS-WITH-DEVIATION`
- 1+ FAILS unrelated to gap closure (regressions caused by 41-08/41-09/41-10) → verdict reverts to `SMOKE-PARTIAL-GAPS-FOUND`, escalate
  </interfaces>
  </context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator runs the gap-closure smoke on a dev machine</name>
  <what-built>
    Cumulative effect of plans 41-08 + 41-09 + 41-10:
    - CalendarEventPill exposes `data-dossier-id` (G2)
    - DossierDrawer forces `box-shadow: none` at ≤768px viewport (G5)
    - loginForListPages('ar') waits for `dir=rtl` to settle (G6)
    - All dossier-drawer specs use `b0000001-0000-0000-0000-000000000004` instead of `seed-country-sa` placeholder (G7)
    - `--sla-bad` token darkened to `oklch(46% 0.18 25)` for WCAG AA (G3 + G4)
    - Dashboard/index.tsx restored RecentDossiers (or trigger spec skipped per user decision in 41-10 Task 1) (G1)
  </what-built>
  <how-to-verify>
    1. Pull the branch with all 3 prerequisite plans (41-08, 41-09, 41-10) merged. Confirm with `git log --oneline -10` that all three plan commits are present.
    2. On a dev machine with `.env.test` populated (`TEST_USER_EMAIL=kazahrani@stats.gov.sa`, `TEST_USER_PASSWORD=itisme` from local Doppler/.env), run `cd frontend && pnpm dev` and confirm the server is up at http://localhost:5173.
    3. Apply the seed: from repo root run `pnpm db:seed` (or apply via Supabase MCP). Verify the China dossier is present:
       ```bash
       psql "$DATABASE_URL" -c "SELECT id, name_en, sensitivity_level FROM dossiers WHERE id = 'b0000001-0000-0000-0000-000000000004';"
       ```
       Expected: 1 row with `name_en='China'`, `sensitivity_level=3`.
    4. Run the full Phase 41 spec suite (excluding the visual baseline tests, which are deferred):
       ```bash
       cd frontend && pnpm playwright test \
         dossier-drawer-trigger-recent \
         dossier-drawer-trigger-calendar \
         dossier-drawer-deeplink \
         dossier-drawer-a11y \
         dossier-drawer-rtl \
         dossier-drawer-mobile \
         dossier-drawer-cta \
         dossier-drawer-commitment-click \
         dossier-drawer-axe \
         --reporter=list
       ```
       Expected outcome: 14/14 PASS (or 13/14 + 1 SKIP if user chose option-b in 41-10).
    5. If any spec FAILS that wasn't in the original G1-G7 set, the gap-closure has introduced a regression. STOP and escalate; do NOT update VERIFICATION.md.
    6. Capture the playwright reporter output to `.planning/phases/41-dossier-drawer/41-11-smoke-output.txt` (gitignored if you prefer; cite key lines in the SUMMARY).
    7. Run `pnpm size` and confirm Total JS still passes the 815 KB ceiling (no per-component drawer entry per RESEARCH §15 / A6 — this stays the same).
    8. Run a final manual axe scan on `/dashboard?dossier=b0000001-0000-0000-0000-000000000004&dossierType=country` in EN AND AR (DevTools axe panel) — confirm zero serious/critical violations on the `.drawer` selector.
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - "approved 14/14" — all 14 specs pass, no regressions, ready to update VERIFICATION.md to PASS
    - "approved 13/14 skip" — 13 pass + 1 skip (G1 option-b deferral) — ready for PASS-WITH-DEVIATION
    - "regression in <spec-name>: <details>" — STOP; the closure plans introduced a new bug
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update 41-VERIFICATION.md with the gap-closure outcome</name>
  <files>
    .planning/phases/41-dossier-drawer/41-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/41-dossier-drawer/41-VERIFICATION.md (entire file — current verdict + smoke results)
    - .planning/phases/41-dossier-drawer/41-08-SUMMARY.md (gap closure outcomes for G2, G5, G6, G7)
    - .planning/phases/41-dossier-drawer/41-09-SUMMARY.md (gap closure outcomes for G3, G4)
    - .planning/phases/41-dossier-drawer/41-10-SUMMARY.md (gap closure outcome for G1)
    - .planning/phases/41-dossier-drawer/deferred-items.md (any new rows added under option-b/c of plan 41-10)
    - The smoke-output capture from Task 1 (the playwright reporter output)
  </read_first>
  <action>
    Edit `41-VERIFICATION.md` in 3 surgical regions:

    ### Region 1: Frontmatter (top of file, lines 1-20)

    Update:
    - `status: gaps_found` → `status: closed` (or `status: closed_with_deviation` under PASS-WITH-DEVIATION)
    - `verdict: SMOKE-PARTIAL-GAPS-FOUND` → `verdict: PASS` (or `PASS-WITH-DEVIATION`)
    - `playwright_pass: 7` → new pass count from Task 1 (expected: 14, or 13 if option-b)
    - `playwright_fail: 7` → new fail count (expected: 0; if 1+ then this task should not run — escalate)
    - `last_updated: 2026-05-02` → today's date when Task 1 was approved
    - Update the per-requirement statuses:
      - `DRAWER-01 status: PARTIAL-INTEGRATION-GAP` → `VERIFIED` (option-a/c) or `VERIFIED-WITH-DEFERRAL` (option-b)
      - `DRAWER-02 status: VERIFIED-CODE` → `VERIFIED-CODE-AND-RUNTIME` (the runtime smoke now passes 100%)
      - `DRAWER-03 status: PARTIAL-FAIL-CONTRAST` → `VERIFIED` (G3+G4 closed by Plan 41-09)

    ### Region 2: Insert a new section "Smoke Results — gap closure pass (YYYY-MM-DD)"

    Insert this new section RIGHT AFTER the existing "Smoke Results — 2026-05-02 (orchestrator-run)" section (do NOT delete the prior section — it remains as historical record).

    Format:

    ```markdown
    ## Smoke Results — gap closure pass (YYYY-MM-DD)

    **Environment:** local Playwright run, dev server `pnpm dev` on port 5173, seed
    `supabase/seed/060-dashboard-demo.sql` applied (China dossier
    `b0000001-0000-0000-0000-000000000004` is the canonical fixture; sensitivity_level=3
    with 3 overdue aa_commitments). Logged in as `TEST_USER_EMAIL`.

    **Cumulative gap-closure result:** N/14 PASS (was 7/14 on 2026-05-02).

    | # | Original gap | Closing plan | Resolution | New status |
    |---|---|---|---|---|
    | G1 | RecentDossiers trigger missing | Plan 41-10 | <option chosen + brief detail> | <PASS / SKIP-WITH-TRACKING> |
    | G2 | calendar [data-dossier-id] missing | Plan 41-08 Task 1 | data-dossier-id added to CalendarEventPill via conditional spread | PASS |
    | G3 | axe contrast EN — --sla-bad token | Plan 41-09 Task 1 | --sla-bad darkened to oklch(46% 0.18 25) at all 4 byte-synced sites | PASS |
    | G4 | axe contrast AR — same token | Plan 41-09 Task 1 | (same — token cascades to AR locale automatically) | PASS |
    | G5 | mobile box-shadow not 'none' | Plan 41-08 Task 2 | inline-style override on DossierDrawer via useSyncExternalStore + matchMedia | PASS |
    | G6 | loginForListPages('ar') dir race | Plan 41-08 Task 3 | helper waits for `dir=rtl` AND `lang=ar` via page.waitForFunction (10s timeout) | PASS |
    | G7 | seed-country-sa placeholder | Plan 41-08 Task 4 | replaced with real UUID b0000001-0000-0000-0000-000000000004 in shared fixture helper; all 9 specs import it | PASS |

    **Visual baselines:** still deferred to HUMAN-UAT per Phase 40 precedent. The 2
    `dossier-drawer-visual.spec.ts` tests remain in PENDING-BASELINE state. Note
    that the new `--sla-bad` token from Plan 41-09 means any operator-generated
    baseline must be regenerated against the new color (subtle red deepening).

    **Total JS budget:** still passes the 815 KB ceiling. No per-component drawer
    entry per RESEARCH §15 / A6.

    **axe-core:** 0 serious/critical violations on `.drawer` in both EN and AR.
    ```

    Replace `<option chosen + brief detail>` with the verbatim resume-signal text from Plan 41-10 Task 1, plus a one-line summary of the resolution.

    Replace `N/14` with the actual pass count.

    ### Region 3: Sign-off Checklist (bottom of file)

    Tick boxes that the gap-closure pass now satisfies:

    - `[x] All 10 D-13 case smokes pass on staging` — only if all 10 D-13 cases now PASS (option-a/c); leave unchecked under option-b
    - `[x] pnpm playwright test dossier-drawer green (functional + a11y)` — only if 14/14
    - `[ ] pnpm playwright test dossier-drawer-visual --update-snapshots produced 2 baselines` — STAYS UNCHECKED (deferred to HUMAN-UAT)
    - `[ ] Baseline PNGs committed under frontend/tests/e2e/dossier-drawer-visual.spec.ts-snapshots/` — STAYS UNCHECKED
    - `[x] axe-core EN + AR runs zero serious/critical` — TICK (Plan 41-09 closes)
    - `[x] Total JS overage logged as deferred (DO NOT raise the limit)` — TICK (already logged; gap closure doesn't change this)
    - `[x] This file's frontmatter verdict flipped to PASS-WITH-DEVIATION` — TICK after Region 1 edit

    Do NOT remove or modify the existing checklist items; only flip the boxes that the gap-closure satisfies.

    **Decision references:** This task realizes the canonical verdict per the gap-closure protocol. Each gap row cites the closing plan, providing forward traceability for Phase 42.

  </action>
  <verify>
    <automated>grep -q "Smoke Results — gap closure pass" .planning/phases/41-dossier-drawer/41-VERIFICATION.md &amp;&amp; grep -q "verdict: PASS" .planning/phases/41-dossier-drawer/41-VERIFICATION.md &amp;&amp; grep -c "Plan 41-08\|Plan 41-09\|Plan 41-10" .planning/phases/41-dossier-drawer/41-VERIFICATION.md | grep -E "^[3-9]|^[0-9][0-9]" &gt; /dev/null &amp;&amp; ! grep -q "verdict: SMOKE-PARTIAL-GAPS-FOUND" .planning/phases/41-dossier-drawer/41-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter `verdict` is `PASS` or `PASS-WITH-DEVIATION` (not `SMOKE-PARTIAL-GAPS-FOUND`).
    - Frontmatter `playwright_pass` and `playwright_fail` reflect the new run (`pass + fail = 14`, `fail = 0` under success path).
    - Frontmatter `last_updated` matches today's date (operator's local date when smoke ran).
    - The new "Smoke Results — gap closure pass" section exists after the original "Smoke Results — 2026-05-02" section (BOTH sections present; the original is preserved as historical record).
    - The new section's gap table has 7 rows, one per G1-G7, each citing the closing plan (41-08 / 41-09 / 41-10).
    - Sign-off Checklist boxes ticked for items now satisfied (axe, playwright pass, verdict flip); visual baseline boxes remain unchecked.
    - The 2 visual regression tests are NOT marked as failures; they remain deferred to HUMAN-UAT.
    - No content in regions 1-3 contradicts the operator's resume-signal from Task 1.
  </acceptance_criteria>
  <done>
    41-VERIFICATION.md reflects the post-gap-closure verdict. The phase is ready for Phase 42 to begin.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                                             |
| --------------------------------- | ------------------------------------------------------------------------------------------------------- |
| Operator smoke run output         | Manually-run by the user; trusted but verifiable (the smoke output is captured to a file).              |
| VERIFICATION.md verdict authority | The verdict is the canonical phase exit signal; updating it without a real smoke would corrupt history. |

## STRIDE Threat Register

| Threat ID  | Category    | Component                      | Disposition | Mitigation Plan                                                                                                                                                                                                    |
| ---------- | ----------- | ------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-41-11-01 | Tampering   | VERIFICATION.md verdict update | mitigate    | Task 2 only runs after Task 1 checkpoint emits an explicit "approved" resume-signal. The verdict cannot flip to PASS without the operator's confirmation that the smoke was run and produced the expected results. |
| T-41-11-02 | Repudiation | Smoke output not captured      | mitigate    | Task 1 instructs the operator to capture the playwright reporter output to a file; Task 2's SUMMARY cites it. Future audits can re-run the same command and compare.                                               |

</threat_model>

<verification>
- 41-VERIFICATION.md frontmatter reflects PASS or PASS-WITH-DEVIATION verdict.
- All 7 gaps G1-G7 have an explicit close-out row citing the responsible plan (this plan verifies/records the close-out — the actual fixes ship in 41-08, 41-09, 41-10).
- The Sign-off Checklist boxes are flipped for satisfied items.
- The original "Smoke Results — 2026-05-02" section is preserved (historical record).
- No prior plan files (41-01..41-10) modified.
- Phase 42 can begin.
</verification>

<success_criteria>

- DRAWER-01: VERIFIED (or VERIFIED-WITH-DEFERRAL under option-b).
- DRAWER-02: VERIFIED-CODE-AND-RUNTIME.
- DRAWER-03: VERIFIED (axe + RTL + mobile all green).
- 7/7 VERIFICATION.md gaps formally addressed (closed cleanly or tracked under deferred-items.md).
- Phase 41 phase verdict committed and unblocks Phase 42.
  </success_criteria>

<output>
On completion, create `.planning/phases/41-dossier-drawer/41-11-SUMMARY.md` documenting:
- Smoke run timestamp + final pass/fail counts (verbatim from playwright reporter output)
- The verbatim Task 1 resume-signal
- Confirmation that all 7 gap rows are present in the new VERIFICATION.md section
- Final verdict + which sign-off boxes were ticked
- Any anomaly observed during the smoke (flakes, retries, timeouts) — note even non-blocking issues for Phase 42 awareness
</output>
</content>
</invoke>
