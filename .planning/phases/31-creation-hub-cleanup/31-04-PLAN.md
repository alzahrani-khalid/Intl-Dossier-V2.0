---
phase: 31-creation-hub-cleanup
plan: 04
type: execute
wave: 4
depends_on: [31-03]
files_modified:
  - frontend/src/components/dossier/DossierCreateWizard.tsx
  - frontend/src/pages/dossiers/DossierCreatePage.tsx
  - frontend/src/components/dossier/wizard-steps/Shared.ts
  - frontend/src/components/dossier/wizard-steps/ClassificationStep.tsx
  - frontend/src/components/dossier/wizard-steps/TypeSelectionStep.tsx
  - frontend/src/i18n/en/dossier.json
  - frontend/src/i18n/ar/dossier.json
autonomous: true
requirements: [UX-03]
tags: [frontend, cleanup, i18n, playwright, e2e]

must_haves:
  truths:
    - 'Legacy `DossierCreateWizard.tsx` and `DossierCreatePage.tsx` no longer exist (D-15)'
    - 'Legacy wizard-steps (`Shared.ts`, `ClassificationStep.tsx`, `TypeSelectionStep.tsx`) no longer exist (D-15)'
    - 'Orphaned i18n keys (`dossier:create.cancel`, `dossier:create.helpTitle`, `dossier:create.helpText`) removed from EN + AR (D-18)'
    - 'Retained i18n keys (`dossier:create.title`, `dossier:create.subtitleSelectType`) remain in EN + AR (reused by CreateDossierHub)'
    - '`DossierTypeSelector` orphan status verified; NOT deleted in this phase (D-17)'
    - '`pnpm --filter frontend typecheck` is clean — no broken imports'
    - '`pnpm --filter frontend build` is green'
    - 'Full Playwright E2E suite passes (hub flow + all 8 per-type wizard flows)'
  artifacts:
    - path: 'frontend/src/components/dossier/DossierCreateWizard.tsx'
      provides: 'DELETED — file must not exist after this plan'
      contains: 'DELETED'
    - path: 'frontend/src/pages/dossiers/DossierCreatePage.tsx'
      provides: 'DELETED'
      contains: 'DELETED'
    - path: 'frontend/src/components/dossier/wizard-steps/Shared.ts'
      provides: 'DELETED'
      contains: 'DELETED'
    - path: 'frontend/src/components/dossier/wizard-steps/ClassificationStep.tsx'
      provides: 'DELETED'
      contains: 'DELETED'
    - path: 'frontend/src/components/dossier/wizard-steps/TypeSelectionStep.tsx'
      provides: 'DELETED'
      contains: 'DELETED'
  key_links:
    - from: 'routes/_protected/dossiers/create.tsx'
      to: 'CreateDossierHub'
      via: 'component: CreateDossierHub (already wired in Plan 31-01)'
      pattern: "component:\\s*CreateDossierHub"
    - from: 'frontend/src/i18n/{en,ar}/dossier.json'
      to: 'hub page'
      via: 'reused keys: create.title, create.subtitleSelectType, create.hubDescription.*'
      pattern: 'hubDescription'
---

<objective>
Remove the legacy monolithic `DossierCreateWizard` and its 4 supporting files (D-15), audit and prune orphaned `dossier:create.*` i18n keys (D-18), verify `DossierTypeSelector` orphan status without deleting it (D-17), and confirm the final suite is green. This is the LAST plan per D-21 — it runs only after the hub, guidance, and reference updates are all in place so no commit ships a broken `/dossiers/create`.

Purpose: Satisfies UX-03 (Old monolithic DossierCreateWizard removed after all 8 type-specific wizards verified).
Output: 5 file deletions + targeted i18n pruning (3 keys in EN, 3 in AR) + grep-driven orphan report + green typecheck/build/E2E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/31-creation-hub-cleanup/31-CONTEXT.md
@.planning/phases/31-creation-hub-cleanup/31-PATTERNS.md
@.planning/phases/31-creation-hub-cleanup/31-01-SUMMARY.md
@.planning/phases/31-creation-hub-cleanup/31-02-SUMMARY.md
@.planning/phases/31-creation-hub-cleanup/31-03-SUMMARY.md
@CLAUDE.md

<!-- Target files to delete — confirm no non-legacy importer remains before deleting -->

@frontend/src/components/dossier/DossierCreateWizard.tsx
@frontend/src/pages/dossiers/DossierCreatePage.tsx
@frontend/src/components/dossier/wizard-steps/Shared.ts
@frontend/src/components/dossier/wizard-steps/ClassificationStep.tsx
@frontend/src/components/dossier/wizard-steps/TypeSelectionStep.tsx

<!-- i18n source of truth -->

@frontend/src/i18n/en/dossier.json
@frontend/src/i18n/ar/dossier.json

<interfaces>
<!-- Orphan-verification grep matrix — executor runs each BEFORE deleting -->

| File to delete          | Grep command                                              |
| ----------------------- | --------------------------------------------------------- |
| DossierCreateWizard.tsx | `grep -rl "DossierCreateWizard" frontend/src tests`       |
| DossierCreatePage.tsx   | `grep -rl "DossierCreatePage" frontend/src tests`         |
| Shared.ts               | `grep -rl "from.*wizard-steps/Shared" frontend/src tests` |
| ClassificationStep.tsx  | `grep -rl "ClassificationStep" frontend/src tests`        |
| TypeSelectionStep.tsx   | `grep -rl "TypeSelectionStep" frontend/src tests`         |

Each grep must return ONLY the file itself + files being deleted in this batch. Any other hit = a live consumer.

i18n key audit (`frontend/src/i18n/{en,ar}/dossier.json`):

| Key                                     | Expected consumer                         | Action     |
| --------------------------------------- | ----------------------------------------- | ---------- |
| `create.title`                          | CreateDossierHub (Plan 31-01)             | **KEEP**   |
| `create.subtitleSelectType`             | CreateDossierHub                          | **KEEP**   |
| `create.hubDescription.*` (added 31-01) | CreateDossierHub                          | **KEEP**   |
| `create.cancel`                         | DossierCreatePage (deleted)               | **DELETE** |
| `create.helpTitle`                      | DossierCreatePage (deleted)               | **DELETE** |
| `create.helpText`                       | DossierCreatePage (deleted)               | **DELETE** |
| Any other `create.*` key                | Grep-audit; if zero external hits, delete |

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify orphan status + delete 5 legacy files + DossierTypeSelector audit + Playwright legacy-spec check</name>
  <files>
    frontend/src/components/dossier/DossierCreateWizard.tsx,
    frontend/src/pages/dossiers/DossierCreatePage.tsx,
    frontend/src/components/dossier/wizard-steps/Shared.ts,
    frontend/src/components/dossier/wizard-steps/ClassificationStep.tsx,
    frontend/src/components/dossier/wizard-steps/TypeSelectionStep.tsx
  </files>
  <read_first>
    - frontend/src/components/dossier/DossierCreateWizard.tsx (note outbound imports — any outside the 5 legacy files means a live consumer)
    - frontend/src/pages/dossiers/DossierCreatePage.tsx
    - frontend/src/components/dossier/wizard-steps/Shared.ts
    - frontend/src/components/dossier/wizard-steps/ClassificationStep.tsx
    - frontend/src/components/dossier/wizard-steps/TypeSelectionStep.tsx
    - frontend/src/routes/_protected/dossiers/create.tsx (confirm Plan 31-01 swap is in place — `component: CreateDossierHub`)
    - frontend/src/components/dossier/DossierTypeSelector.tsx (orphan-audit target per D-17 — do NOT delete)
    - 31-PATTERNS.md §"Plan 31-04 — DELETE"
  </read_first>
  <action>
    **Step 1 — Pre-delete verification (safety gate per D-21).**

    Run each grep. Each must return ONLY the file itself + files being deleted in this batch. Any other hit means a live consumer — STOP and surface to the user.

    ```bash
    grep -rl "DossierCreateWizard"             frontend/src tests
    grep -rl "DossierCreatePage"               frontend/src tests
    grep -rl "wizard-steps/Shared"             frontend/src tests
    grep -rl "ClassificationStep"              frontend/src tests
    grep -rl "TypeSelectionStep"               frontend/src tests
    ```

    Expected per 31-PATTERNS: each grep returns only the legacy files themselves.

    **Step 2 — Delete the 5 files.**

    ```bash
    git rm frontend/src/components/dossier/DossierCreateWizard.tsx
    git rm frontend/src/pages/dossiers/DossierCreatePage.tsx
    git rm frontend/src/components/dossier/wizard-steps/Shared.ts
    git rm frontend/src/components/dossier/wizard-steps/ClassificationStep.tsx
    git rm frontend/src/components/dossier/wizard-steps/TypeSelectionStep.tsx
    ```

    If `frontend/src/components/dossier/wizard-steps/` becomes empty, `git rm -r` the empty directory. If other step files remain, leave the directory alone.

    **Step 3 — Typecheck after delete.**

    ```bash
    pnpm --filter frontend typecheck
    ```

    Must exit 0. If TypeScript reports missing imports, an orphan audit was missed — HALT.

    **Step 4 — `DossierTypeSelector` orphan audit (D-17 — AUDIT ONLY, DO NOT DELETE).**

    ```bash
    grep -rl "DossierTypeSelector" frontend/src tests
    ```

    Expected result per 31-PATTERNS: only `frontend/src/components/dossier/DossierTypeSelector.tsx` itself (its consumer `TypeSelectionStep.tsx` was just deleted).

    Record in `31-04-SUMMARY.md`:
    > "DossierTypeSelector orphan verified post-Phase-31. Safe to delete in a future cleanup phase. Consumers grep: [paste grep output]."

    Do NOT delete `DossierTypeSelector.tsx` in this phase.

    **Step 5 — Playwright legacy-spec check (D-19).**

    Per 31-PATTERNS §"Playwright / E2E audit", zero executable specs reference the old flow. Confirm:

    ```bash
    grep -rln "'/dossiers/create'" tests/ 2>/dev/null || true
    grep -rln "'/dossiers/create'" $(find . -name '*.spec.ts' -not -path '*/node_modules/*') 2>/dev/null || true
    ```

    If zero hits: no action. If hits in actual `.spec.ts` files (not docs): `git rm` them and document in summary.

  </action>
  <verify>
    <automated>test ! -f frontend/src/components/dossier/DossierCreateWizard.tsx &amp;&amp; test ! -f frontend/src/pages/dossiers/DossierCreatePage.tsx &amp;&amp; test ! -f frontend/src/components/dossier/wizard-steps/Shared.ts &amp;&amp; test ! -f frontend/src/components/dossier/wizard-steps/ClassificationStep.tsx &amp;&amp; test ! -f frontend/src/components/dossier/wizard-steps/TypeSelectionStep.tsx &amp;&amp; pnpm --filter frontend typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All 5 legacy files deleted: each `test -f {path}` returns non-zero (file absent)
    - No residual imports: `grep -rl "DossierCreateWizard\|DossierCreatePage\|ClassificationStep\|TypeSelectionStep" frontend/src tests` returns empty
    - `wizard-steps/Shared` import is gone: `grep -rl "wizard-steps/Shared" frontend/src tests` returns empty
    - DossierTypeSelector remains: `test -f frontend/src/components/dossier/DossierTypeSelector.tsx` exits 0
    - DossierTypeSelector orphan audit recorded: `grep -n "DossierTypeSelector orphan verified" .planning/phases/31-creation-hub-cleanup/31-04-SUMMARY.md` matches (runs at end of plan)
    - Route still wired to hub: `grep -n "component: CreateDossierHub" frontend/src/routes/_protected/dossiers/create.tsx` matches
    - `pnpm --filter frontend typecheck` exits 0
  </acceptance_criteria>
  <done>
    Five legacy files removed; typecheck passes; DossierTypeSelector orphan status recorded for future cleanup; no stale Playwright specs remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit-and-prune orphaned dossier:create.* i18n keys (EN + AR)</name>
  <files>
    frontend/src/i18n/en/dossier.json,
    frontend/src/i18n/ar/dossier.json
  </files>
  <read_first>
    - frontend/src/i18n/en/dossier.json (current `create.*` subtree)
    - frontend/src/i18n/ar/dossier.json (mirror)
    - 31-PATTERNS.md §"Plan 31-04 — i18n audit — `dossier:create.*` keys" (the 5-row disposition table)
  </read_first>
  <action>
    **Step 1 — Enumerate current `create.*` keys.**

    List every key under `create.*` in `frontend/src/i18n/en/dossier.json`:
    ```bash
    node -e "const j=require('./frontend/src/i18n/en/dossier.json');const keys=[];(function w(o,p){for(const k in o){const n=p?p+'.'+k:k;if(typeof o[k]==='object')w(o[k],n);else keys.push(n)}})(j.create,'create');console.log(keys.join('\n'))"
    ```

    **Step 2 — For each enumerated key, grep against the rest of the codebase (excluding the 5 deleted files which are already gone).**

    For each key `create.{leaf}` — e.g. `create.cancel`:
    ```bash
    grep -rln "create\.cancel\|'create.cancel'\|\"create.cancel\"\|dossier:create\.cancel" frontend/src
    ```

    Zero hits → DELETE from both EN and AR. Any hit → KEEP.

    **Step 3 — Apply deletions per 31-PATTERNS audit table (confirmed orphans):**

    From `frontend/src/i18n/en/dossier.json`, remove these three keys under `create.*`:
    - `create.cancel`
    - `create.helpTitle`
    - `create.helpText`

    Mirror the same three deletions in `frontend/src/i18n/ar/dossier.json`.

    **Explicitly KEEP** in both EN and AR:
    - `create.title` (reused by CreateDossierHub — Plan 31-01)
    - `create.subtitleSelectType` (reused by CreateDossierHub — Plan 31-01)
    - `create.hubDescription.*` (added by Plan 31-01 — all 8 types)

    **Step 4 — Preserve JSON validity.** After editing, run:
    ```bash
    node -e "JSON.parse(require('fs').readFileSync('frontend/src/i18n/en/dossier.json','utf8'));JSON.parse(require('fs').readFileSync('frontend/src/i18n/ar/dossier.json','utf8'));console.log('both dossier.json files parse ok')"
    ```

    **Step 5 — Discovery-mode extension.** If Step 2's grep sweep surfaces ADDITIONAL orphaned `create.*` keys beyond the three in 31-PATTERNS (e.g. `create.someLegacyField`), delete those too and document in the summary which extras were removed.

    **Step 6 — Final build gate.** Run the full frontend suite:
    ```bash
    pnpm --filter frontend typecheck
    pnpm --filter frontend test -- --run
    pnpm --filter frontend build
    ```

    All three must exit 0.

    **Step 7 — Playwright E2E smoke.** Run the full E2E suite against a local dev server — hub flow + at least one per-type wizard flow:
    ```bash
    pnpm --filter frontend test:e2e 2>/dev/null || pnpm test:e2e
    ```

    (Use whichever script the repo exposes — check `package.json` scripts.) All E2E tests must pass. If a previously-green spec now fails because it depended on a deleted i18n key, surface the failure, restore that key, document as "KEEP after audit" in the summary, and re-run.

  </action>
  <verify>
    <automated>node -e "const en=require('./frontend/src/i18n/en/dossier.json');const ar=require('./frontend/src/i18n/ar/dossier.json');const gone=['cancel','helpTitle','helpText'];for (const k of gone) { if (en.create[k] !== undefined) { console.error('EN still has create.'+k); process.exit(1) } if (ar.create[k] !== undefined) { console.error('AR still has create.'+k); process.exit(1) } }; if (!en.create.title || !en.create.subtitleSelectType || !en.create.hubDescription) { console.error('EN missing retained key'); process.exit(1) }; if (!ar.create.title || !ar.create.subtitleSelectType || !ar.create.hubDescription) { console.error('AR missing retained key'); process.exit(1) }; console.log('i18n audit: orphans removed, retained keys present')" &amp;&amp; pnpm --filter frontend typecheck &amp;&amp; pnpm --filter frontend build</automated>
  </verify>
  <acceptance_criteria>
    - Orphan keys absent from EN: `grep -E "\"(cancel|helpTitle|helpText)\":" frontend/src/i18n/en/dossier.json` returns zero matches WITHIN the `create` subtree (the keys may exist elsewhere under different parents — the Node check above is authoritative)
    - Orphan keys absent from AR: same as above for AR
    - Retained keys present EN: `grep -n "\"subtitleSelectType\":" frontend/src/i18n/en/dossier.json` matches AND `grep -n "\"hubDescription\":" frontend/src/i18n/en/dossier.json` matches
    - Retained keys present AR: same for AR
    - Both JSON files parse: Node `JSON.parse` check exits 0
    - No remaining consumer of deleted keys: `grep -rln "create\.cancel\|create\.helpTitle\|create\.helpText" frontend/src` returns empty
    - `pnpm --filter frontend typecheck` exits 0
    - `pnpm --filter frontend build` exits 0
    - `pnpm --filter frontend test -- --run` exits 0 (no unit test regressions)
    - Playwright E2E exits 0 (full suite: hub + 8 per-type wizard happy paths)
  </acceptance_criteria>
  <done>
    3 orphan keys removed from EN + AR; retained keys verified; JSON valid; typecheck + build + unit tests + E2E all green.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                             |
| ---------------------- | ----------------------------------------------------------------------- |
| Source code repository | Deletions reduce attack surface (fewer code paths, smaller i18n bundle) |

## STRIDE Threat Register

| Threat ID | Category          | Component                              | Disposition | Mitigation Plan                                                                                                                                                                                       |
| --------- | ----------------- | -------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-31-10   | Denial of Service | Broken route after deletion            | mitigate    | Plan ordering (D-21): this plan runs LAST so the route is already swapped to the hub by Plan 31-01 before any legacy file is deleted. Typecheck gate in acceptance_criteria catches missed importers. |
| T-31-11   | Tampering         | Stale i18n key referenced after delete | mitigate    | Pre-delete grep sweep in Task 2 Step 2; build gate in Step 6 catches runtime regressions via E2E.                                                                                                     |
| T-31-12   | Info Disclosure   | Deleted files in git history           | accept      | Files are legacy frontend components with no secrets; git history retention is expected behavior.                                                                                                     |

**Scope note:** Pure removal phase. No new code, no new data flows, no external integrations. Net attack surface DECREASES.
</threat_model>

<verification>
- File deletions verified: five `test ! -f` checks pass
- `grep -rl "DossierCreateWizard"` returns empty
- `pnpm --filter frontend typecheck` exits 0
- `pnpm --filter frontend test -- --run` exits 0
- `pnpm --filter frontend build` exits 0
- Playwright E2E suite exits 0 (hub + 8 per-type happy paths)
- i18n orphan keys absent in EN + AR; retained keys present
- DossierTypeSelector orphan-audit result recorded in summary
- Manual smoke (optional): `pnpm dev`, visit `/dossiers/create` → hub renders; visit `/dossiers/countries/create` → country wizard renders with guidance banner; dismiss banner → reload → banner stays hidden
</verification>

<success_criteria>

- UX-03 fully satisfied — legacy DossierCreateWizard and its 4 supporting files removed (D-15)
- `/dossiers/create` still serves the hub (no regression from Plan 31-01's swap)
- All 8 per-type wizards still reachable and functional (no regression from Plan 31-02's guidance additions)
- All UX-04 call sites still route correctly (no regression from Plan 31-03's repoints)
- i18n bundle shrinks by 3 keys × 2 locales = 6 entries
- `DossierTypeSelector` orphan status documented for a future cleanup pass (D-17)
- Build is green, tests are green, E2E is green
  </success_criteria>

<output>
After completion, create `.planning/phases/31-creation-hub-cleanup/31-04-SUMMARY.md` documenting:
- Files deleted: 5 production files (list with paths)
- Any empty directories removed: yes/no (wizard-steps)
- i18n keys deleted: EN + AR, three keys each
- Additional orphan keys found via grep-audit (if any) and their disposition
- DossierTypeSelector orphan-audit grep output verbatim + "safe to delete in future phase" follow-up note
- Playwright legacy-spec check result: 0 hits confirmed / N specs deleted
- Final gate results: typecheck OK, build OK, unit tests OK, E2E OK
- Commit SHA(s)
</output>
