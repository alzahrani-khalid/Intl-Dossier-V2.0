---
phase: 62-export-pack-contract-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/dossier-export-pack/index.ts
autonomous: true
requirements: [EXPORT-02]

must_haves:
  truths:
    - 'POSTing a valid dossier_id with a valid Bearer token returns 200 with Content-Type text/html and a complete self-contained HTML document'
    - 'D-05: No query in the function references positions.dossier_ids, positions.classification, mous.title_en, mous.status, the legacy commitments table, calendar_entries.start_datetime, or documents.entity_type — every advertised section stays and reads live-schema columns'
    - 'A failed section data query renders a visible localized .section-error block in the pack instead of crashing the export, and the section key appears in the X-Failed-Sections response header'
    - 'D-06: No storage upload, signed URL, base64 encode, or SUPABASE_SERVICE_ROLE_KEY usage remains in the function — the generated HTML is returned directly in the response body'
    - 'D-02: Printing the pack produces A4 pages with each section on its own page and table rows unsplit'
  artifacts:
    - path: 'supabase/functions/dossier-export-pack/index.ts'
      provides: 'Schema-correct HTML briefing pack generator with direct text/html response'
      contains: 'Deno.serve'
  key_links:
    - from: 'supabase/functions/dossier-export-pack/index.ts'
      to: 'position_dossier_links'
      via: 'junction select with positions embed'
      pattern: "from\\('position_dossier_links'\\)"
    - from: 'supabase/functions/dossier-export-pack/index.ts'
      to: 'aa_commitments'
      via: 'direct dossier_id query'
      pattern: "from\\('aa_commitments'\\)"
    - from: 'supabase/functions/dossier-export-pack/index.ts'
      to: 'frontend fetch client'
      via: 'X-Failed-Sections header exposed through Access-Control-Expose-Headers'
      pattern: 'X-Failed-Sections'
---

<objective>
Make `dossier-export-pack` schema-correct and contract-honest: fix all six stale DB reads against the live staging schema (D-05), add per-section failure tracking with in-document error notes (D-08), add print-optimized CSS (D-02), and replace the storage-upload/signed-URL delivery with a direct text/html response body (D-06), while modernizing the runtime to Deno.serve + getCorsHeaders.

Purpose: EXPORT-02 — every export must resolve against live columns so no dossier type 500s, and the function must deliver the advertised HTML directly.
Output: a deploy-ready `supabase/functions/dossier-export-pack/index.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/62-export-pack-contract-deploy/62-CONTEXT.md
@.planning/phases/62-export-pack-contract-deploy/62-RESEARCH.md
@.planning/phases/62-export-pack-contract-deploy/62-PATTERNS.md
</context>

<interfaces>
<!-- Live-verified column contracts (orchestrator-injected from information_schema, 2026-06-11). Use these directly — no schema exploration needed. -->

position linkage (canonical): `position_dossier_links(dossier_id, position_id)` with embed `position:positions(id, title_en, title_ar, status, created_at)` — positions has NO `classification`, NO `dossier_ids`.

mous: `id, title, title_ar, lifecycle_state, signatory_1_dossier_id, signatory_2_dossier_id, deleted_at, created_at` — NO `title_en`, NO `status`, NO `dossier_ids`. Live working filter form (dossier-overview.service.ts L491-501): `.eq('signatory_N_dossier_id', dossierId).is('deleted_at', null)`.

aa_commitments: `id, title, title_ar, status, priority, due_date, owner_user_id, is_deleted, dossier_id, created_at` — NO `title_en`, NO `deadline`, NO `responsible_user_id`. Legacy `commitments` table is EMPTY — never query it.

calendar_entries: `id, title_en, title_ar, entry_type, event_date, event_time, location, is_virtual, status, dossier_id` — NO `start_datetime`, NO `event_type`, NO `location_en/location_ar`.

key_contacts: `dossier_id, name, role, organization, email, phone` — monolingual; NO `name_ar`, NO `title_en/title_ar`, NO `organization_en/organization_ar`.

documents: `title, type, classification, related_entities (jsonb)` — NO `entity_type`, NO `entity_id`, NO `file_name`.

dossier_relationships: has `status` column; soft-delete filter is `.eq('status', 'active')` (do not rely on `deleted_at`).

audit_logs: `entity_id, action, created_at` — all valid, no change.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix all stale DB reads in fetchDossierData and add per-section error tracking</name>
  <files>supabase/functions/dossier-export-pack/index.ts</files>
  <read_first>
    - supabase/functions/dossier-export-pack/index.ts (full read once — fetchDossierData is ~L920-1100; renderers L300-900; handler L1140-1281; extract everything for Tasks 1-3 in this single pass)
    - frontend/src/services/dossier-overview.service.ts L464-560 (fetchDocuments — the live-verified mous signatory query form and position_dossier_links flatten pattern to replicate)
    - frontend/src/types/dossier-export.types.ts (DEFAULT_EXPORT_SECTIONS — the exact section `type` string values; sectionErrors keys MUST use these same strings so the dialog can map them to `sections.*` i18n labels)
    - .planning/phases/62-export-pack-contract-deploy/62-RESEARCH.md (D-05 stale-read audit table — section-by-section fix spec)
  </read_first>
  <action>
    In fetchDossierData, apply every fix from the RESEARCH D-05 audit, per D-05 (all sections stay, all reconciled):

    1. Relationships (~L948-983): replace `.is('deleted_at', null)` with `.eq('status', 'active')` on BOTH the outgoing (source_dossier_id) and incoming (target_dossier_id) queries. Keep the target_dossier/source_dossier embeds as-is.
    2. Positions (~L985-991): delete the `.contains('dossier_ids', [dossierId])` query entirely. Replace with a `position_dossier_links` junction select using embed `position:positions(id, title_en, title_ar, status, created_at)`, `.eq('dossier_id', dossierId)`, `.limit(20)`, then flatten links to positions handling the array-or-object embed shape (mirror dossier-overview.service.ts L480-485). Drop `classification` from the select — the column does not exist.
    3. MoUs (~L993-999): delete the `.contains('dossier_ids', ...)` query. Replace with the two-signatory pattern from dossier-overview.service.ts L491-515: two parallel queries selecting `id, title, title_ar, lifecycle_state, created_at` with `.eq('signatory_1_dossier_id', dossierId)` / `.eq('signatory_2_dossier_id', dossierId)`, both with `.is('deleted_at', null)`, then deduplicate by id via a Set.
    4. Commitments (~L1001-1007 + L1049-1059): delete the two-step work_item_dossiers → commitments fetch entirely. Replace with a single direct query: `from('aa_commitments')` selecting `id, title, title_ar, status, priority, due_date, owner_user_id, created_at`, `.eq('dossier_id', dossierId)`, `.is('is_deleted', false)`, `.order('created_at', { ascending: false })`, `.limit(20)`. Update `stats.commitments_count` to use this result's length (Pitfall 5 in RESEARCH — the old count path would report 0).
    5. Calendar (~L1009-1017): change the select to `id, title_en, title_ar, entry_type, event_date, event_time, location, is_virtual, status`; change `.gte('start_datetime', new Date().toISOString())` to `.gte('event_date', new Date().toISOString().split('T')[0])`; change `.order('start_datetime', ...)` to `.order('event_date', ...)`.
    6. Documents (~L1031-1040 + transform L1090-1097): delete the `documents` table query (`entity_type`/`entity_id` columns do not exist) and the `d.file_name` transform. Replace by deriving the documents list from the positions and mous already fetched in this same function — mirror the fetchDocuments mapping in dossier-overview.service.ts L529-560: each position becomes `{ id, title_en, title_ar, document_type: 'position', status }` and each mou becomes `{ id, title_en: m.title, title_ar: m.title_ar ?? m.title, document_type: 'mou', status: m.lifecycle_state }`. This matches the app's own Documents tab contract (the generic documents table has no direct dossier linkage — verified in the overview service comment at L525-527). Keep the documents section advertised; empty input still renders the existing "No documents found" text.
    7. Timeline/audit_logs (~L1022-1030): no query change — already valid (`entity_id`, `action`, `created_at`).

    D-08 error tracking: declare `const sectionErrors: Record<string, string> = {}` at the top of fetchDossierData. Wrap EACH section fetch (relationships, positions, mous, commitments, events, contacts, timeline) in its own try/catch AND check each result's `.error`; on failure, record `sectionErrors[<sectionKey>] = <error message>` and continue with an empty array — never throw, never abort the export for a single section. Section keys MUST be the exact `type` string values from DEFAULT_EXPORT_SECTIONS in frontend/src/types/dossier-export.types.ts (read it first to confirm; e.g. 'positions', 'mous', 'commitments'). Return `sectionErrors` from fetchDossierData alongside the data so generateHTMLDocument can render per-section error blocks (Task 2) and the handler can emit X-Failed-Sections (Task 3). The derived documents section never sets a sectionErrors key (derivation cannot fail independently).

  </action>
  <verify>
    <automated>grep -v '^\s*//' supabase/functions/dossier-export-pack/index.ts | grep -c "dossier_ids\|start_datetime\|entity_type\|from('commitments')\|work_item_dossiers" | grep -qx 0 && grep -c "from('position_dossier_links')" supabase/functions/dossier-export-pack/index.ts | grep -qv "^0$" && grep -c "from('aa_commitments')" supabase/functions/dossier-export-pack/index.ts | grep -qv "^0$" && grep -c "sectionErrors" supabase/functions/dossier-export-pack/index.ts | grep -qv "^0$" && echo TASK1-GATES-PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' supabase/functions/dossier-export-pack/index.ts | grep -c "dossier_ids"` outputs 0
    - `grep -v '^\s*//' supabase/functions/dossier-export-pack/index.ts | grep -c "from('commitments')"` outputs 0 (aa_commitments only)
    - `grep -v '^\s*//' supabase/functions/dossier-export-pack/index.ts | grep -c "start_datetime"` outputs 0
    - `grep -v '^\s*//' supabase/functions/dossier-export-pack/index.ts | grep -c "entity_type"` outputs 0
    - index.ts contains `from('position_dossier_links')`, `from('aa_commitments')`, `signatory_1_dossier_id`, `signatory_2_dossier_id`, `.eq('status', 'active')`, `event_date`
    - fetchDossierData declares `sectionErrors` and returns it; each of the 7 fetched sections records into it on failure instead of throwing
    - The mous queries use `.is('deleted_at', null)` and aa_commitments uses `.is('is_deleted', false)` (the two tables use DIFFERENT soft-delete columns — do not normalize)
  </acceptance_criteria>
  <done>Every DB read in fetchDossierData targets live columns; a failing section query degrades to an empty array plus a sectionErrors entry instead of a 500.</done>
</task>

<task type="auto">
  <name>Task 2: Fix renderer column reads, add the .section-error block, and add D-02 print CSS</name>
  <files>supabase/functions/dossier-export-pack/index.ts</files>
  <read_first>
    - .planning/phases/62-export-pack-contract-deploy/62-UI-SPEC.md (Surface 2 — exact .section-error CSS, @media print block, failure copy EN/AR, distinctness rule vs .no-data)
    - supabase/functions/dossier-export-pack/index.ts renderers and stylesheet (already read in Task 1 single pass — do not re-read; work from extracted state)
  </read_first>
  <action>
    Renderer column fixes (the data shape changed in Task 1):
    - generatePositionsSection: delete the `pos.classification` conditional block entirely.
    - generateMousSection: `m.title_en` → `m.title`; `m.status` → `m.lifecycle_state` (including the getStatusBadge argument).
    - generateCommitmentsSection: `c.title_en` → `c.title`; `c.deadline` → `c.due_date`. Drop the assignee name read tied to the removed `responsible_user_id` join (omit assignee display or show nothing — do not invent a join).
    - generateEventsSection: `e.start_datetime` → `e.event_date`; `e.event_type` → `e.entry_type`; `e.location_en`/`e.location_ar` → `e.location` (monolingual).
    - generateContactsSection: `c.name_ar` → `c.name`; `c.title_en`/`c.title_ar` → `c.role`; `c.organization_en`/`c.organization_ar` → `c.organization`.
    - Documents transform/renderer: render the derived `{ title_en, title_ar, document_type, status }` entries from Task 1; no `d.file_name` reads remain.

    D-08 error blocks: thread `sectionErrors` from fetchDossierData through generateHTMLDocument into each section renderer (optional `error?: string` parameter). When set, the renderer emits the section's normal `<div class="section">` wrapper and `<h2 class="section-title">`, followed by a `<div class="section-error">` containing one `<p>` with the failure copy — EN `This section could not be generated.` / AR `تعذّر إنشاء هذا القسم.` — selected by the pack's isRTL, matching the existing inline-bilingual renderer pattern. Failed sections still appear in the TOC. The copy is a static string (no DB data interpolation).

    Stylesheet additions (inline `<style>` block) — copy the two CSS blocks from 62-UI-SPEC.md Surface 2 verbatim:
    - `.section-error` rules: `border: 1px solid #d1d5db`, `border-inline-start: 4px solid #b45309`, `background-color: #fffbeb`, `padding: 16px 20px`, break-inside avoidance, `print-color-adjust: exact` (+ -webkit- prefix); `.section-error p`: `color: #92400e`, `font-size: 11pt`, `font-style: normal`, `text-align: start`, `margin: 0`. The block must stay visually distinct from `.no-data` (which stays italic, centered, #9ca3af, borderless).
    - `@media print` rules: `.section { break-before: page; page-break-before: always; }`, `.cover-page` and `.toc` get `break-after: page; page-break-after: always;`, `.data-table tr` gets `break-inside: avoid; page-break-inside: avoid;`, `.data-table thead { display: table-header-group; }`, `.content-card, .contact-card, .section-error` get break-inside avoidance, `.document-footer { position: fixed; }`. Keep the existing `@page { size: A4; margin: 1.5cm }` and `.page-break` rules.

    SECURITY (round-fkn hardening — MUST preserve): do not remove `escapeHtml` from any user-data insertion point; do not introduce any DB-derived value into the `<style>` block (only the existing config-derived `direction`/`fontFamily`/isRTL interpolations are permitted). The new CSS uses only literal values.

  </action>
  <verify>
    <automated>grep -c "section-error" supabase/functions/dossier-export-pack/index.ts | grep -qv "^0$" && grep -c "@media print" supabase/functions/dossier-export-pack/index.ts | grep -qv "^0$" && grep -v '^\s*//' supabase/functions/dossier-export-pack/index.ts | grep -c "title_en ?? \|m\.title_en\|c\.deadline\|e\.event_type\|location_en\|organization_en" | grep -qx 0 && grep -c "escapeHtml" supabase/functions/dossier-export-pack/index.ts | grep -qv "^0$" && echo TASK2-GATES-PASS</automated>
  </verify>
  <acceptance_criteria>
    - index.ts contains `.section-error` CSS with `border-inline-start: 4px solid #b45309` and `print-color-adjust: exact`
    - index.ts contains an `@media print` block with `break-before: page` for `.section` and `table-header-group` for `.data-table thead`
    - `grep -v '^\s*//' supabase/functions/dossier-export-pack/index.ts | grep -c "m\.title_en"` outputs 0; same for `c\.deadline`, `e\.start_datetime`, `e\.event_type`, `c\.name_ar`, `organization_en`, `d\.file_name`
    - The AR failure string `تعذّر إنشاء هذا القسم` and EN `This section could not be generated` both appear in index.ts
    - `escapeHtml` call count in index.ts is unchanged or higher than before the edit (no hardening removed)
    - Each section renderer accepts and handles the error parameter; a section with an error still renders its `section-title` heading
  </acceptance_criteria>
  <done>Renderers read only live columns; failed sections render the amber-barred localized error block; Cmd+P pagination rules are in the inline stylesheet; XSS hardening intact.</done>
</task>

<task type="auto">
  <name>Task 3: Modernize transport — Deno.serve, getCorsHeaders, direct HTML response, dead-code removal</name>
  <files>supabase/functions/dossier-export-pack/index.ts</files>
  <read_first>
    - supabase/functions/_shared/cors.ts (getCorsHeaders + handleCorsPreflightRequest signatures — L63-96)
    - supabase/functions/dossier-activity-timeline/index.ts L1-130 (reference: Deno.serve shape, @2.39.3 import, UUID regex validation returning 400)
    - supabase/functions/dossier-export-pack/index.ts handler tail (already read in Task 1 single pass — auth block L1148-1178, response block L1212-1281)
  </read_first>
  <action>
    1. Imports: delete `import { serve } from 'https://deno.land/std@0.168.0/http/server.ts'`; change the supabase-js import to `https://esm.sh/@supabase/supabase-js@2.39.3`; replace the static `corsHeaders` import with `import { getCorsHeaders, handleCorsPreflightRequest } from '../_shared/cors.ts'`.
    2. Serve: replace `serve(async (req) => {...})` with `Deno.serve(async (req: Request) => {...})`. OPTIONS requests return `handleCorsPreflightRequest(req)`. Every other response spreads `getCorsHeaders(req)` into its headers — no static wildcard remains.
    3. Input validation (V5/ASVS L1): after parsing the body, validate `dossier_id` against the UUID regex used in dossier-activity-timeline (L114-126); on failure return 400 JSON `{ error, code: 'INVALID_REQUEST' }` with getCorsHeaders.
    4. AUTH — preserve exactly (project memory: bare getUser() 401s): keep the createClient with `global: { headers: { Authorization: authHeader } }` and the explicit `supabase.auth.getUser(token)` call at ~L1148-1178. Do not switch to bare getUser().
    5. D-06 direct response: delete the base64/btoa encode block (~L1213-1215), the serviceClient creation + storage upload (~L1224-1244), the createSignedUrl call, and the `SUPABASE_SERVICE_ROLE_KEY` env read (attack-surface reduction — the function must run entirely on the user-scoped anon-key client). Replace the success response with `new Response(html, { status: 200, headers: ... })` where headers = spread of getCorsHeaders(req) plus `Content-Type: text/html; charset=utf-8`, `Content-Disposition: inline; filename="briefing-pack-{dossierSlug}-{timestamp}.html"` (slug = lowercased English dossier name, non-alphanumerics collapsed to '-', max 30 chars; timestamp = ISO with ':' and '.' replaced by '-'), `Access-Control-Expose-Headers: X-Failed-Sections`, and — only when sectionErrors is non-empty — `X-Failed-Sections` set to the comma-joined section keys from Task 1.
    6. Error paths: keep the existing JSON error envelope (success:false, error.code/message_en/message_ar) for 401/400/404/500 responses — the frontend service parses JSON on non-OK — but switch their headers to getCorsHeaders(req).
    7. Best-effort type sanity: if `command -v deno` succeeds, run `deno check supabase/functions/dossier-export-pack/index.ts` and fix reported errors; if deno is absent, rely on the grep gates and the staging deploy in plan 62-03.
  </action>
  <verify>
    <automated>grep -c "Deno.serve" supabase/functions/dossier-export-pack/index.ts | grep -qv "^0$" && grep -c "getCorsHeaders" supabase/functions/dossier-export-pack/index.ts | grep -qv "^0$" && grep -v '^\s*//' supabase/functions/dossier-export-pack/index.ts | grep -c "SUPABASE_SERVICE_ROLE_KEY\|createSignedUrl\|deno.land/std@0.168.0\|btoa(" | grep -qx 0 && grep -c "supabase-js@2.39.3" supabase/functions/dossier-export-pack/index.ts | grep -qv "^0$" && grep -c "getUser(token)" supabase/functions/dossier-export-pack/index.ts | grep -qv "^0$" && grep -c "X-Failed-Sections" supabase/functions/dossier-export-pack/index.ts | grep -qv "^0$" && echo TASK3-GATES-PASS</automated>
  </verify>
  <acceptance_criteria>
    - index.ts contains `Deno.serve(` and does NOT contain `deno.land/std@0.168.0` (comment-filtered grep count 0)
    - index.ts imports `@supabase/supabase-js@2.39.3` and `getCorsHeaders, handleCorsPreflightRequest` from `../_shared/cors.ts`
    - `grep -v '^\s*//' supabase/functions/dossier-export-pack/index.ts | grep -c "SUPABASE_SERVICE_ROLE_KEY"` outputs 0; same for `createSignedUrl` and `btoa(`
    - index.ts contains `supabase.auth.getUser(token)` (auth pattern preserved verbatim)
    - The 200 path returns `text/html; charset=utf-8` with `Content-Disposition` and `Access-Control-Expose-Headers: X-Failed-Sections`; non-200 paths return JSON envelopes with getCorsHeaders(req)
    - index.ts contains a UUID regex test on dossier_id returning a 400 JSON response on mismatch
  </acceptance_criteria>
  <done>The function streams the pack as text/html with the failed-sections header, runs only on the user-scoped client, and matches the project's modern edge-function transport conventions — ready for the 62-03 deploy.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                              |
| ------------------------ | -------------------------------------------------------- |
| client → edge function   | untrusted request body (dossier_id, config) crosses here |
| edge → PostgREST         | user-token-scoped queries; RLS is the row filter         |
| edge HTML → user browser | generated document rendered in a tab carrying app origin |

## STRIDE Threat Register

| Threat ID | Category               | Component                             | Disposition | Mitigation Plan                                                                                                                                                                                 |
| --------- | ---------------------- | ------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-62-01   | Tampering (XSS)        | section renderers / inline stylesheet | mitigate    | escapeHtml preserved on every user-data insertion (Task 2 acceptance gate); no DB-derived values enter the `<style>` block; failure copy is static strings; round-fkn @page hardening untouched |
| T-62-02   | Spoofing               | auth block                            | mitigate    | preserve `@2` + explicit `getUser(token)` pattern verbatim (Task 3 step 4); 401 on missing/invalid token unchanged                                                                              |
| T-62-03   | Information Disclosure | data fetch layer                      | mitigate    | D-06 deletes the SUPABASE_SERVICE_ROLE_KEY client entirely — all queries run on the user-scoped anon-key client under RLS (attack-surface reduction)                                            |
| T-62-04   | Tampering (injection)  | request parsing                       | mitigate    | dossier_id validated as UUID before any query, 400 on mismatch (Task 3 step 3, ASVS V5)                                                                                                         |
| T-62-05   | Information Disclosure | CORS                                  | mitigate    | static wildcard corsHeaders replaced by getCorsHeaders(req) + ALLOWED_ORIGINS allowlist on every response                                                                                       |
| T-62-SC   | Tampering              | package installs                      | accept      | no npm/pip/cargo installs in this phase — Package Legitimacy Gate not triggered (RESEARCH confirms zero new packages)                                                                           |

</threat_model>

<verification>
- All three task grep-gate commands print their PASS sentinel.
- `grep -v '^\s*//' supabase/functions/dossier-export-pack/index.ts | grep -cE "dossier_ids|start_datetime|entity_type|from\('commitments'\)|SUPABASE_SERVICE_ROLE_KEY|createSignedUrl"` outputs 0.
- If deno is installed: `deno check supabase/functions/dossier-export-pack/index.ts` exits 0.
</verification>

<success_criteria>

- Every stale read from the EXPORT-02 requirement text is gone: `positions.classification`/`dossier_ids`, `mous.title_en`/`status`, `documents.entity_type`, legacy `commitments` → all reconciled to `position_dossier_links`, `mous.title`/`lifecycle_state`, derived documents, `aa_commitments`.
- D-02 print CSS, D-06 direct HTML response, and D-08 error notes are all in the file per the UI-SPEC contracts.
- File is deploy-ready for plan 62-03 (no local-only assumptions, no service-role dependency).
  </success_criteria>

<output>
Create `.planning/phases/62-export-pack-contract-deploy/62-01-SUMMARY.md` when done.
</output>
