---
phase: 62-export-pack-contract-deploy
plan: 03
type: execute
wave: 2
depends_on: [62-01, 62-02]
files_modified: []
autonomous: true
requirements: [EXPORT-02]

must_haves:
  truths:
    - 'dossier-export-pack appears in the deployed edge functions list for staging project zkrcjzdemdmwhearhfgg (it was NOT among the 204 deployed functions on 2026-06-11 — deploy is the gap)'
    - 'POSTing one dossier_id of every distinct dossier type on staging (expected 7) returns HTTP 200 with Content-Type text/html — no 404, no 500'
    - 'No smoke response carries a non-empty X-Failed-Sections header (a failed section on an admin token means a remaining schema mismatch, not RLS)'
    - 'The ALLOWED_ORIGINS Supabase secret is confirmed set so deployed-origin browsers get real CORS headers, not ACAO:null'
  artifacts:
    - path: '.planning/phases/62-export-pack-contract-deploy/62-03-SUMMARY.md'
      provides: 'Per-type smoke matrix (type, dossier_id, http_code, content-type, X-Failed-Sections) with tokens redacted'
  key_links:
    - from: 'supabase CLI'
      to: 'staging zkrcjzdemdmwhearhfgg'
      via: 'supabase functions deploy dossier-export-pack --project-ref zkrcjzdemdmwhearhfgg'
      pattern: 'functions deploy dossier-export-pack'
    - from: 'smoke curl'
      to: 'https://zkrcjzdemdmwhearhfgg.supabase.co/functions/v1/dossier-export-pack'
      via: 'POST with Bearer token per dossier type'
      pattern: 'functions/v1/dossier-export-pack'
---

<objective>
Deploy the reconciled `dossier-export-pack` edge function to staging and prove EXPORT-02 end to end: confirm CLI auth, confirm the ALLOWED_ORIGINS CORS secret, deploy, then smoke-export one dossier of every distinct type live on staging — asserting 200 + text/html and zero failed sections for each.

Purpose: EXPORT-02 / roadmap success criterion 2 — the function was never deployed (verified absent from the 204 deployed functions on 2026-06-11); without this plan the phase ships nothing observable.
Output: deployed function + recorded per-type smoke matrix in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/62-export-pack-contract-deploy/62-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify CLI auth + ALLOWED_ORIGINS secret, then deploy dossier-export-pack to staging</name>
  <files>none (deploy operation)</files>
  <read_first>
    - supabase/functions/dossier-export-pack/index.ts (confirm the 62-01 state is on disk before deploying: contains Deno.serve, getCorsHeaders, aa_commitments, position_dossier_links — spot-grep, do not full-read)
    - .planning/phases/62-export-pack-contract-deploy/62-RESEARCH.md §Deploy Flow and §Environment Availability (command, pre-deploy checklist, ALLOWED_ORIGINS note)
  </read_first>
  <action>
    1. Auth check: run `supabase projects list` and confirm project ref `zkrcjzdemdmwhearhfgg` is visible. If the CLI is not authenticated, this is an auth gate — pause with instructions to run `supabase login`, then retry (do not pre-plan around it).
    2. CORS secret check: run `supabase secrets list --project-ref zkrcjzdemdmwhearhfgg` and confirm `ALLOWED_ORIGINS` is present (values are digests — presence is the check; other deployed edge functions already work from app origins, so an existing secret is correct). ONLY if absent, set it: `supabase secrets set ALLOWED_ORIGINS="http://localhost:5173,http://138.197.195.242" --project-ref zkrcjzdemdmwhearhfgg` (local dev origin + droplet origin; never set a wildcard). Never print or log secret values.
    3. Pre-deploy gate: confirm via grep that index.ts contains `Deno.serve`, `getCorsHeaders`, `from('aa_commitments')`, `from('position_dossier_links')` and does NOT contain `SUPABASE_SERVICE_ROLE_KEY` (comment-filtered). If any gate fails, stop — plan 62-01 did not land.
    4. Deploy: `supabase functions deploy dossier-export-pack --project-ref zkrcjzdemdmwhearhfgg` (Supabase CLI 2.102.0 at /opt/homebrew/bin/supabase). The Supabase MCP deploy tool is an acceptable alternative if the CLI session cannot be authenticated non-interactively.
    5. Post-deploy: run `supabase functions list --project-ref zkrcjzdemdmwhearhfgg` and confirm `dossier-export-pack` is listed with a fresh created/updated timestamp.
  </action>
  <verify>
    <automated>supabase functions list --project-ref zkrcjzdemdmwhearhfgg | grep -c "dossier-export-pack" | grep -qv "^0$" && echo DEPLOYED</automated>
  </verify>
  <acceptance_criteria>
    - `supabase functions list --project-ref zkrcjzdemdmwhearhfgg` output contains `dossier-export-pack`
    - `supabase secrets list --project-ref zkrcjzdemdmwhearhfgg` output contains `ALLOWED_ORIGINS`
    - No secret value appears anywhere in command output captured into the SUMMARY
    - An unauthenticated OPTIONS preflight to https://zkrcjzdemdmwhearhfgg.supabase.co/functions/v1/dossier-export-pack returns a 2xx (function reachable, no 404)
  </acceptance_criteria>
  <done>The function is live on staging with CORS governed by the ALLOWED_ORIGINS allowlist.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke-export one dossier of every distinct type on staging (expected 7) and record the matrix</name>
  <files>none (verification operation)</files>
  <read_first>
    - .env.test (TEST_USER_EMAIL / TEST_USER_PASSWORD and any staging keys — source as env vars; NEVER echo values or write them into the SUMMARY)
    - frontend/src/types/dossier-export.types.ts (DossierExportRequest body shape — `{ dossier_id, config }` — and DEFAULT_EXPORT_SECTIONS for a full-sections config)
  </read_first>
  <action>
    1. Obtain an admin session token: `curl -s -X POST "https://zkrcjzdemdmwhearhfgg.supabase.co/auth/v1/token?grant_type=password" -H "apikey: $VITE_SUPABASE_ANON_KEY" -H "Content-Type: application/json" -d "{\"email\":\"$TEST_USER_EMAIL\",\"password\":\"$TEST_USER_PASSWORD\"}"` with credentials sourced from .env.test (anon key from .env.test or frontend env). Store `access_token` in a shell variable only — redact from all logs and the SUMMARY.
    2. Collect one dossier id per type: query staging via the Supabase MCP execute_sql tool (read-only): `SELECT DISTINCT ON (type) type, id FROM dossiers ORDER BY type, created_at DESC;`. Expected 7 distinct types (country, organization, forum, engagement, topic, working_group, person; elected_official may appear as person_subtype rather than a distinct type — smoke every distinct `dossiers.type` value actually returned). If any expected type has zero rows on staging, record it as N/A in the matrix with the SQL evidence rather than failing the task.
    3. For each (type, id): `curl -s -o /tmp/pack-<type>.html -w "%{http_code} %{content_type}\n" -D /tmp/pack-<type>.headers -X POST "https://zkrcjzdemdmwhearhfgg.supabase.co/functions/v1/dossier-export-pack" -H "Authorization: Bearer $TOKEN" -H "apikey: $VITE_SUPABASE_ANON_KEY" -H "Content-Type: application/json" -d "{\"dossier_id\":\"<id>\",\"config\":{\"language\":\"en\",\"sections\":<DEFAULT_EXPORT_SECTIONS-with-all-enabled>,\"includeCoverPage\":true,\"includeTableOfContents\":true,\"includePageNumbers\":true}}"`.
    4. Gate per type: http_code is 200; content_type starts with `text/html`; the body file contains `<title>Briefing Pack` (or the dossier name in the title); the `X-Failed-Sections` header in /tmp/pack-<type>.headers is absent or empty. A 404 means the deploy did not land (return to Task 1); a 500 or any non-empty X-Failed-Sections under the admin token means a remaining stale read — diagnose via `supabase functions logs dossier-export-pack --project-ref zkrcjzdemdmwhearhfgg` (or the MCP get_logs tool), fix the offending query in supabase/functions/dossier-export-pack/index.ts, redeploy, and re-run that type before closing the task.
    5. Record the matrix (type | dossier_id | http_code | content_type | x-failed-sections) in the SUMMARY. Include the bash one-liner used so verify-work can replay it. Delete /tmp/pack-*.html and headers files after recording (they contain dossier data).
  </action>
  <verify>
    <automated>for f in /tmp/pack-smoke-results.txt; do test -s "$f" && ! grep -vE "200 text/html" "$f" | grep -q . && echo SMOKE-ALL-200; done</automated>
  </verify>
  <acceptance_criteria>
    - Every distinct dossier type returned by the staging query has a smoke line reading `200 text/html...` in /tmp/pack-smoke-results.txt (the per-type `-w` outputs appended to one file)
    - Zero lines contain 404 or 500
    - Zero /tmp/pack-<type>.headers files contain a non-empty `X-Failed-Sections` value (`grep -i "x-failed-sections: ." /tmp/pack-*.headers` matches nothing)
    - At least one saved body contains `<title>Briefing Pack` proving real pack HTML, not an error page
    - 62-03-SUMMARY.md contains the full per-type matrix and no token, password, or secret value
  </acceptance_criteria>
  <done>EXPORT-02 proven live: every dossier type on staging exports the advertised HTML pack with zero failed sections under an admin token.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                        |
| ----------------------------- | -------------------------------------------------- |
| local CLI → Supabase platform | deploy + secrets management against staging        |
| smoke script → staging auth   | password-grant token minting with test credentials |

## STRIDE Threat Register

| Threat ID | Category               | Component                         | Disposition | Mitigation Plan                                                                                                                                                                             |
| --------- | ---------------------- | --------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-62-09   | Information Disclosure | test credentials / session tokens | mitigate    | credentials sourced from .env.test env vars only; tokens held in shell vars; SUMMARY matrix excludes all secret material (Task 2 acceptance gate); /tmp pack bodies deleted after recording |
| T-62-10   | Spoofing / DoS surface | CORS on the deployed function     | mitigate    | ALLOWED_ORIGINS presence verified before smoke; never set to wildcard; getCorsHeaders allowlist (from 62-01) governs responses                                                              |
| T-62-11   | Elevation of Privilege | deploy channel                    | mitigate    | deploy only via authenticated supabase CLI/MCP against the named project ref; no service-role key used anywhere in the smoke flow                                                           |
| T-62-SC   | Tampering              | package installs                  | accept      | no packages installed in this plan                                                                                                                                                          |

</threat_model>

<verification>
- `supabase functions list --project-ref zkrcjzdemdmwhearhfgg` lists dossier-export-pack.
- /tmp/pack-smoke-results.txt shows `200 text/html` for every distinct staging dossier type, zero 404/500 lines, zero non-empty X-Failed-Sections headers.
- SUMMARY contains the replayable smoke matrix with secrets redacted.
</verification>

<success_criteria>

- Roadmap success criterion 2 met live on staging: all 7 dossier types export the advertised file — no 404 (deployed), no 500 (schema-correct).
- Roadmap success criterion 3 evidenced: zero X-Failed-Sections under an admin token proves every section query resolved against live columns.
  </success_criteria>

<output>
Create `.planning/phases/62-export-pack-contract-deploy/62-03-SUMMARY.md` when done.
</output>
