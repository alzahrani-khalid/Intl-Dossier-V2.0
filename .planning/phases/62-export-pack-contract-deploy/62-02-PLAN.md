---
phase: 62-export-pack-contract-deploy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/dossier-export.types.ts
  - frontend/src/services/dossier-export.service.ts
  - frontend/src/hooks/useDossierExport.ts
  - frontend/src/components/dossier/ExportDossierDialog.tsx
  - frontend/src/components/dossier/__tests__/ExportDossierDialog.test.tsx
  - frontend/src/i18n/en/dossier-export.json
  - frontend/src/i18n/ar/dossier-export.json
autonomous: true
requirements: [EXPORT-01]

must_haves:
  truths:
    - 'The Export dialog renders no PDF/Word format options and no Bilingual language option — only an HTML info line and EN/AR language cards (D-01, D-03, D-04)'
    - "The language selection defaults to the user's current UI language"
    - 'Clicking Export opens a new tab synchronously (placeholder document) and writes the pack HTML into it on success; when popups are blocked it falls back to an .html blob download with a localized notice (D-07)'
    - 'When the edge reports failed sections, the success state shows a warning banner listing localized section names and the dialog does not auto-close (D-08)'
    - 'All new dialog copy ships in EN and AR with key parity between the two JSON files'
  artifacts:
    - path: 'frontend/src/components/dossier/ExportDossierDialog.tsx'
      provides: 'Honest HTML export dialog with new-tab delivery'
    - path: 'frontend/src/components/dossier/__tests__/ExportDossierDialog.test.tsx'
      provides: 'EXPORT-01 component test (no format picker, EN/AR only, default language)'
      min_lines: 30
    - path: 'frontend/src/services/dossier-export.service.ts'
      provides: 'exportDossier returning { html, failedSections } from text/html response'
  key_links:
    - from: 'frontend/src/components/dossier/ExportDossierDialog.tsx'
      to: 'frontend/src/hooks/useDossierExport.ts'
      via: 'useDossierExport hook'
      pattern: 'useDossierExport'
    - from: 'frontend/src/services/dossier-export.service.ts'
      to: '/functions/v1/dossier-export-pack'
      via: 'authed fetch POST'
      pattern: 'dossier-export-pack'
    - from: 'frontend/src/services/dossier-export.service.ts'
      to: 'X-Failed-Sections response header'
      via: 'response.headers.get'
      pattern: 'X-Failed-Sections'
---

<objective>
Rework the frontend export contract to match the honest-HTML decision: trim types to the real contract (D-01, D-04), rewrite the service for a text/html response with the X-Failed-Sections header, rewrite the hook for the new-tab flow (D-07), rework the dialog (D-03, D-08), update i18n copy in both languages, and add the EXPORT-01 component test.

Purpose: EXPORT-01 — the dialog advertises only what the system actually produces.
Output: reworked dialog, types, service, hook, EN/AR i18n, and a passing component test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/62-export-pack-contract-deploy/62-CONTEXT.md
@.planning/phases/62-export-pack-contract-deploy/62-PATTERNS.md
@.planning/phases/62-export-pack-contract-deploy/62-UI-SPEC.md
</context>

<interfaces>
<!-- Contracts the executor builds against. Extracted at plan time — no exploration needed. -->

New service contract (PATTERNS §dossier-export.service.ts):

```typescript
export interface ExportDossierResult {
  html: string
  failedSections: string[]
}
export async function exportDossier(request: DossierExportRequest): Promise<ExportDossierResult>
// success: response.text() + response.headers.get('X-Failed-Sections')
// non-OK: JSON envelope → throw DossierExportAPIError (class preserved)
```

New hook return contract (PATTERNS §useDossierExport):

```typescript
return { exportDossier, quickExport, progress, isExporting, error, failedSections, reset }
// exportDossier(dossierId, config?) => Promise<{ html: string; failedSections: string[] }>
```

Dialog call sites (verified — DO NOT change their props): DossierShell.tsx L294-300 and DossierDetailLayout.tsx L372-376 pass only `dossierId, dossierName, dossierType, open, onClose`. ExportDossierDialogProps stays source-compatible.

NAME-COLLISION WARNING (verified): `frontend/src/hooks/useDossierOverview.ts` L88 exports a DIFFERENT `useDossierExport` that calls `exportDossierProfile` against the separate `dossier-export` edge function (used by DossierOverview.tsx). It is OUT OF SCOPE — do not modify useDossierOverview.ts, dossier-overview.service.ts, dossier-overview.types.ts, or DossierOverview.tsx.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rework types, service, and hook to the honest-HTML contract</name>
  <files>frontend/src/types/dossier-export.types.ts, frontend/src/services/dossier-export.service.ts, frontend/src/hooks/useDossierExport.ts</files>
  <read_first>
    - frontend/src/types/dossier-export.types.ts (current contracts: DossierExportFormat, ExportLanguage, DossierExportStatus, DossierExportConfig, DossierExportResponse, UseDossierExportReturn, DEFAULT_EXPORT_CONFIG, DEFAULT_EXPORT_SECTIONS)
    - frontend/src/services/dossier-export.service.ts (getAuthHeaders, exportDossier, downloadExportedFile, dossierExportKeys, DossierExportAPIError)
    - frontend/src/hooks/useDossierExport.ts (progress state machine, content_base64 decode block, quickExport)
    - .planning/phases/62-export-pack-contract-deploy/62-PATTERNS.md §types / §service / §hook (target-state code excerpts)
  </read_first>
  <action>
    Types (dossier-export.types.ts), per PATTERNS §types:
    - Remove the `DossierExportFormat` type ('pdf' | 'docx') and the `format` field from `DossierExportConfig` and `DEFAULT_EXPORT_CONFIG` — format is no longer configurable (D-01/D-03).
    - `ExportLanguage` = `'en' | 'ar'` (remove 'both', D-04). `DEFAULT_EXPORT_CONFIG.language` = `'en'` (the dialog overrides with i18n.language).
    - `DossierExportStatus`: remove the `'uploading'` variant.
    - `DossierExportResponse`: remove `download_url`, `file_name`, `file_size`, `page_count`, `expires_at`, `content_base64`, `content_type`; add `failed_sections?: string[]`; keep `success` and the bilingual `error` object.
    - `UseDossierExportReturn`: `exportDossier`/`quickExport` resolve to `{ html: string; failedSections: string[] }`; `quickExport` loses its format parameter; add `failedSections: string[]` to the return shape.
    - Keep `DEFAULT_EXPORT_SECTIONS` and `ExportSectionConfig` unchanged (D-05 — all sections stay; their `type` values are the X-Failed-Sections key contract with the edge).

    Service (dossier-export.service.ts), per PATTERNS §service:
    - `exportDossier`: on OK, `const html = await response.text()` and parse `response.headers.get('X-Failed-Sections')` (comma-split, filter empty) into `failedSections`; return `{ html, failedSections }` typed as the new exported `ExportDossierResult` interface. On non-OK keep the JSON error parse + `DossierExportAPIError` throw exactly as today.
    - Preserve `getAuthHeaders` unchanged (getSession → Bearer + apikey headers).
    - Delete `downloadExportedFile` entirely and the `history` key from `dossierExportKeys` (dead per D-06/D-07; export history deferred).

    Hook (useDossierExport.ts), per PATTERNS §hook:
    - Add `failedSections` state (string[], reset to [] on start and in `reset()`); expose it in the return object.
    - Remove the `downloadExportedFile` import, the `content_base64` atob/byte-array decode block, the `download_url` branch, and the `'uploading'` progress stage. Progress stages become preparing → fetching → generating → ready.
    - `exportDossier` calls the reworked service, stores `failedSections`, builds the `DossierExportResponse` for `onSuccess` as `{ success: true, failed_sections }`, and resolves `{ html, failedSections }` to the caller (the dialog owns the new-tab write — the hook performs NO window/document side effects).
    - `quickExport(dossierId)` drops the format parameter and delegates to `exportDossier` with defaults.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && grep -v '^\s*//' frontend/src/types/dossier-export.types.ts | grep -c "'pdf'\|'docx'\|'both'\|'uploading'\|content_base64\|download_url" | grep -qx 0 && grep -v '^\s*//' frontend/src/services/dossier-export.service.ts | grep -c "downloadExportedFile\|history:" | grep -qx 0 && grep -c "X-Failed-Sections" frontend/src/services/dossier-export.service.ts | grep -qv "^0$" && pnpm --filter intake-frontend build >/dev/null 2>&1 && echo TASK1-PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' frontend/src/types/dossier-export.types.ts | grep -c "'pdf'"` outputs 0; same for `'docx'`, `'both'`, `'uploading'`, `content_base64`, `download_url`
    - dossier-export.service.ts contains `response.text()` and `headers.get('X-Failed-Sections')`; `downloadExportedFile` and `dossierExportKeys.history` are gone
    - useDossierExport.ts return object contains `failedSections` and no longer imports `downloadExportedFile`
    - useDossierOverview.ts, dossier-overview.service.ts, dossier-overview.types.ts, DossierOverview.tsx have zero diff (git diff --name-only must not list them)
    - `pnpm --filter intake-frontend build` exits 0
  </acceptance_criteria>
  <done>Types/service/hook express only the real contract: HTML out, EN/AR in, failed sections surfaced; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Update EN and AR i18n copy for the new contract</name>
  <files>frontend/src/i18n/en/dossier-export.json, frontend/src/i18n/ar/dossier-export.json</files>
  <read_first>
    - frontend/src/i18n/en/dossier-export.json and frontend/src/i18n/ar/dossier-export.json (current key inventory — change surgically, keep all unlisted keys)
    - .planning/phases/62-export-pack-contract-deploy/62-UI-SPEC.md §Copywriting Contract (the EXACT EN and AR strings — this is the approved copy source; where RESEARCH copy differs, UI-SPEC wins)
  </read_first>
  <action>
    Apply the UI-SPEC i18n key diff to BOTH files (key parity mandatory):
    - ADD `format.html` = "HTML briefing pack" / "حزمة إحاطة HTML"; `format.html_info` = "Exports as a print-ready HTML briefing pack. To save as PDF, use your browser's print dialog." / the AR string from the UI-SPEC copy table; `warning.failedSections` = "Some sections could not be generated: {{sections}}" / "تعذّر إنشاء بعض الأقسام: {{sections}}"; `newTab.generating` = "Generating briefing pack…" / "جارٍ إنشاء حزمة الإحاطة…"; `popupBlocked` = "Pop-ups are blocked. The briefing pack was downloaded as an HTML file instead." / "النوافذ المنبثقة محظورة. تم تنزيل حزمة الإحاطة كملف HTML بدلًا من ذلك."
    - CHANGE `success` to the new-tab copy ("Export complete. The briefing pack opened in a new tab." / "اكتمل التصدير. فُتحت حزمة الإحاطة في تبويب جديد."); `progress.ready` drops the exclamation ("Export complete" / "اكتمل التصدير"); `cancel` value becomes "Close" / "إغلاق" (key name retained).
    - REMOVE `format.pdf`, `format.docx`, `language.both`, `progress.uploading` from both files.
    - Keep everything else byte-identical: `title`, `description`, `language.label/en/ar`, `options.*`, `sections.*` (all section names — they feed the failed-sections warning), `advanced.*`, remaining `progress.*`, `error`, `export`, `exporting`, `quickExport*`, `type.*`.
    - Voice rules: sentence case, no exclamation marks, no marketing words, no emoji. The `dossier-export` namespace is already registered in src/i18n/index.ts — do NOT touch the registration.
  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && node -e "const en=require('./frontend/src/i18n/en/dossier-export.json'),ar=require('./frontend/src/i18n/ar/dossier-export.json');const flat=(o,p='')=>Object.entries(o).flatMap(([k,v])=>typeof v==='object'?flat(v,p+k+'.'):[p+k]);const e=flat(en).sort(),a=flat(ar).sort();const miss=e.filter(k=>!a.includes(k)).concat(a.filter(k=>!e.includes(k)));if(miss.length){console.error('PARITY FAIL',miss);process.exit(1)};const dead=['format.pdf','format.docx','language.both','progress.uploading'];if(dead.some(k=>e.includes(k))){console.error('DEAD KEYS PRESENT');process.exit(1)};const need=['format.html','format.html_info','warning.failedSections','newTab.generating','popupBlocked'];if(need.some(k=>!e.includes(k))){console.error('MISSING NEW KEYS');process.exit(1)};console.log('I18N-PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - Both JSON files parse; flattened key sets are identical (parity script prints I18N-PASS)
    - `format.html`, `format.html_info`, `warning.failedSections`, `newTab.generating`, `popupBlocked` exist in both files
    - `format.pdf`, `format.docx`, `language.both`, `progress.uploading` exist in neither file
    - en `cancel` = "Close"; ar `cancel` = "إغلاق"; en `progress.ready` contains no "!"
  </acceptance_criteria>
  <done>Both language files carry the approved copy with full key parity and no dead keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rework ExportDossierDialog (info line, EN/AR only, new-tab flow, failed-sections warning) with component test</name>
  <files>frontend/src/components/dossier/ExportDossierDialog.tsx, frontend/src/components/dossier/__tests__/ExportDossierDialog.test.tsx</files>
  <read_first>
    - frontend/src/components/dossier/ExportDossierDialog.tsx (current structure: imports L1-42, state ~L59, handleExport ~L80-102, state-machine panels ~L136-163, format picker ~L169-205, language group ~L207-239)
    - .planning/phases/62-export-pack-contract-deploy/62-UI-SPEC.md §Surface 1 (dialog structure, states table, RTL + a11y contract) and §Surface 3 (new-tab flow, placeholder, fallback filename)
    - frontend/src/components/dossier/__tests__/DossierShell.test.tsx (test setup conventions: render helpers, i18n/provider mocking in this repo)
    - frontend/src/hooks/useDossierExport.ts (post-Task-1 contract: failedSections in return)
  </read_first>
  <behavior>
    - Test 1: renders no PDF or Word format option (queryByText for the removed `format.pdf`/`format.docx` labels and any radio with value "pdf"/"docx" returns null) — EXPORT-01
    - Test 2: renders exactly two language options, EN and AR, with no Bilingual option — EXPORT-01 / D-04
    - Test 3: renders the HTML info line (`format.html_info` copy present) — D-03
    - Test 4: when i18n.language is 'ar', the AR language radio is selected by default; when 'en', EN is selected — D-04
    - Test 5: when the hook reports failedSections non-empty in the ready state, the warning banner (role="alert") renders with the localized section names — D-08
  </behavior>
  <action>
    Write the failing test file FIRST (mock useDossierExport per the post-Task-1 return contract; follow the repo's existing test conventions from DossierShell.test.tsx), run it red, then rework the dialog per UI-SPEC Surface 1:

    1. Delete the format `<RadioGroup>` (PDF/Word cards), the `format` state, the `format.label` group, and the `DossierExportFormat` import. In its place render the info line: `Info` icon (lucide, `h-4 w-4 text-[var(--ink-mute)] shrink-0 mt-0.5`) + `t('format.html_info')` at `text-sm text-[var(--ink-mute)]`, layout `flex items-start gap-2`, `text-start` — no border, no background, no accent (it is a statement of fact, not a callout).
    2. Trim the language RadioGroup to `['en','ar']`, keeping the existing radio-card selected/unselected styling and the sr-only RadioGroupItem pattern. Initialize state from the current UI language: `useState<ExportLanguage>(i18n.language === 'ar' ? 'ar' : 'en')`.
    3. Keep advanced options (cover page, TOC, all section checkboxes) untouched (D-05). Footer dismiss button keeps the `cancel` key (now reading "Close").
    4. handleExport — D-07 popup-blocker-safe sequence per UI-SPEC Surface 3: (a) call `window.open('', '_blank')` SYNCHRONOUSLY before any await; (b) if a tab opened, immediately write the placeholder document into it — title and single centered body line from `t('newTab.generating')`, background #f7f6f4, text #6b6459, system-ui 14px, `dir` matching the UI language (frontend-authored, allowed literal colors since it is a transient non-app document); (c) await the hook's exportDossier; (d) on success write the pack via document.open/write/close into the tab, or — if window.open returned null — fall back to a Blob (`text/html; charset=utf-8`) anchor download named `briefing-pack-{slug}-{YYYYMMDD}.html` (slug = lowercased hyphenated English dossier name, non-alphanumerics collapsed to '-') with `URL.revokeObjectURL` after click, and set a popupBlocked flag for the success copy; (e) on error call `newTab?.close()` so no orphan placeholder tab survives, then surface the existing error state.
    5. Success-state rules from the UI-SPEC states table: clean success keeps the 1500ms auto-close with the new `success` copy (role="status", aria-live="polite"); failedSections non-empty renders the warning banner below the success banner (`flex items-start gap-3 rounded-[var(--radius-sm)] border border-[var(--warn)] bg-[var(--warn-soft)] p-4`, AlertTriangle `h-5 w-5 text-[var(--warn)] shrink-0`, text `text-sm text-[var(--ink)]`, role="alert") with `t('warning.failedSections', { sections })` where sections = the header keys mapped through the existing `sections.*` i18n labels, comma-joined — and SUPPRESSES auto-close; popup-blocked shows `t('popupBlocked')` and also suppresses auto-close. The new tab opens regardless of failed sections (warning annotates, never pauses).
    6. Constraints: logical properties only (`ms-*`, `ps-*`, `text-start`); all colors via `var(--*)` tokens inside the app dialog; add `Info` and `AlertTriangle` to the lucide imports; remove imports made dead by the format-picker deletion; props interface unchanged (DossierShell/DossierDetailLayout call sites compile untouched).

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && pnpm --filter intake-frontend exec vitest run src/components/dossier/__tests__/ExportDossierDialog.test.tsx && pnpm --filter intake-frontend build >/dev/null 2>&1 && echo TASK3-PASS</automated>
  </verify>
  <acceptance_criteria>
    - `vitest run src/components/dossier/__tests__/ExportDossierDialog.test.tsx` exits 0 with all 5 behavior tests green
    - `grep -v '^\s*//' frontend/src/components/dossier/ExportDossierDialog.tsx | grep -c "'pdf'\|'docx'\|'both'\|DossierExportFormat"` outputs 0
    - ExportDossierDialog.tsx contains `window.open('', '_blank')` positioned before the awaited export call in handleExport
    - ExportDossierDialog.tsx contains `role="alert"` on the warning banner and `var(--warn)` tokens; zero occurrences of `ml-`, `mr-`, `pl-`, `pr-`, `text-left`, `text-right` in the diff
    - `pnpm --filter intake-frontend build` exits 0
  </acceptance_criteria>
  <done>The dialog advertises only the real HTML contract, delivers via popup-safe new tab with honest fallback and failure warnings, and the EXPORT-01 component test locks the contract.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                       |
| -------------------------------------- | ----------------------------------------------------------------- |
| edge response → new tab document.write | edge-generated HTML is written into a same-origin about:blank tab |
| user gesture → window.open             | popup-blocker policy boundary                                     |

## STRIDE Threat Register

| Threat ID | Category               | Component                                       | Disposition | Mitigation Plan                                                                                                                                                                                                           |
| --------- | ---------------------- | ----------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-62-06   | Tampering (XSS)        | document.write of pack HTML into app-origin tab | mitigate    | content originates only from the authed first-party edge over HTTPS; all user data is escapeHtml'd server-side (62-01 T-62-01 gate) and the pack template ships no script tags; no third-party HTML ever enters this path |
| T-62-07   | Information Disclosure | blob URL in popup-blocked fallback              | mitigate    | `URL.revokeObjectURL` called immediately after the anchor click (Task 3 step 4d)                                                                                                                                          |
| T-62-08   | Spoofing               | auth header on export fetch                     | mitigate    | getAuthHeaders (getSession → Bearer + apikey) preserved unchanged; non-OK responses keep the typed DossierExportAPIError path                                                                                             |
| T-62-SC   | Tampering              | package installs                                | accept      | zero new packages this phase — lucide icons and primitives already installed                                                                                                                                              |

</threat_model>

<verification>
- All three task verify commands print their PASS sentinels.
- `git diff --name-only` is limited to the 7 files in files_modified (overview-export namesake files untouched).
- `pnpm --filter intake-frontend build` exits 0 (matches the pre-commit/CI gate).
</verification>

<success_criteria>

- EXPORT-01 satisfied: the dialog advertises HTML only, EN/AR only, with the D-03 info line, locked by a green component test.
- D-07 new-tab flow and D-08 warning behavior implemented exactly per the approved UI-SPEC states table (auto-close suppression rules included).
- Both i18n files have key parity, the approved copy, and no dead keys.
  </success_criteria>

<output>
Create `.planning/phases/62-export-pack-contract-deploy/62-02-SUMMARY.md` when done.
</output>
