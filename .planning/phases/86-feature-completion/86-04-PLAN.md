---
phase: 86-feature-completion
plan: 04
type: execute
wave: 2
depends_on: [86-02]
files_modified:
  - frontend/src/routes/_protected/users/$id.tsx
  - frontend/src/pages/users/UserDetailPage.tsx
  - frontend/src/pages/users/__tests__/UserDetailPage.test.tsx
  - frontend/src/pages/users/UsersListPage.tsx
  - frontend/tests/e2e/user-management.spec.ts
autonomous: true
requirements: [FEAT-03, FEAT-02]

must_haves:
  truths:
    - "Admin can open /users/:id from a clickable row on the users list and view the user's profile (email, username, names, role, status, MFA, last login, department)"
    - "Admin can change a user's role (admin/editor/viewer only); an admin grant surfaces the requires_approval dual-approval response instead of masking it"
    - 'Admin can deactivate/reactivate a user; the status badge flips and orphanedItems/sessionsTerminated are surfaced via existing userDeactivation.* keys'
    - 'A non-admin/anon client cannot read arbitrary public.users rows (IDOR smoke in E2E)'
    - 'The live E2E proves the whole D-10 loop: create → list → detail → role/status, in EN and AR'
  artifacts:
    - path: 'frontend/src/pages/users/UserDetailPage.tsx'
      provides: 'Detail view + role/status admin actions against the hardened fns'
      min_lines: 150
    - path: 'frontend/src/routes/_protected/users/$id.tsx'
      provides: 'Param route mounting UserDetailPage under the admin-gated layout'
    - path: 'frontend/tests/e2e/user-management.spec.ts'
      provides: 'E2E: create → list → detail → role/status + IDOR smoke + AR pass'
  key_links:
    - from: 'frontend/src/routes/_protected/users/$id.tsx'
      to: 'frontend/src/pages/users/UserDetailPage.tsx'
      via: 'Route.useParams → userId prop'
      pattern: 'UserDetailPage'
    - from: 'frontend/src/pages/users/UserDetailPage.tsx'
      to: 'frontend/src/services/user-management-api.ts'
      via: 'assignRole / deactivateUser / reactivateUser'
      pattern: 'assignRole'
    - from: 'frontend/src/pages/users/UserDetailPage.tsx'
      to: 'assign-role dual-approval contract'
      via: 'union-response branch'
      pattern: 'requires_approval'
    - from: 'frontend/src/pages/users/UsersListPage.tsx'
      to: '/users/$id'
      via: 'clickable rows'
      pattern: "users/\\$id"
---

<objective>
FEAT-03 (D-10 detail half): the user detail/management view at /users/:id — profile read via the house direct-select pattern, role change through the verified `assign-role` fn (handling the dual-approval union response), status flip through `deactivate-user`/`reactivate-user`, clickable rows on the users list, and the combined user-management E2E that also live-smokes FEAT-02's create flow plus the IDOR check.

Purpose: closes the detail half of D-10 (ROADMAP success criterion 3) and provides the live proof for criterion 2.
Output: working /users/:id surface; passing user-management.spec.ts incl. security smoke and AR pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-feature-completion/86-RESEARCH.md
@.planning/phases/86-feature-completion/86-PATTERNS.md
@.planning/phases/86-feature-completion/86-VALIDATION.md
@.planning/phases/86-feature-completion/86-02-SUMMARY.md
</context>

<interfaces>
assign-role union response (source of truth: frontend/src/services/user-management-api.ts:39-59 + supabase/functions/assign-role/index.ts:19-33,210):
- Immediate: `{ success, role_changed, new_role, sessions_terminated }`
- Admin grant: `{ success, requires_approval: true, approval_request_id, pending_approvals }` — the change does NOT apply; do NOT build an approvals UI this phase (approve-role-change fn exists for later)

deactivate/reactivate: flip `public.users.is_active` (D-11 — there is no `status` column); responses carry `orphanedItems` / `sessionsTerminated`; i18n keys exist under `userDeactivation.*` (EN+AR).

Detail read pattern (UsersListPage.tsx:86-97): `supabase.from('users').select('id, email, username, full_name, name_en, name_ar, role, is_active, mfa_enabled, last_login_at, department, avatar_url').is('deleted_by', null)` — detail variant adds `.eq('id', userId).single()`, key `['users', 'detail', userId]`. NEVER copy the list page's `.or(...ilike...)` search block (:114-119) — SEC-01 injection class.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: $id route + UserDetailPage profile read + clickable list rows</name>
  <files>frontend/src/routes/_protected/users/$id.tsx, frontend/src/pages/users/UserDetailPage.tsx, frontend/src/pages/users/__tests__/UserDetailPage.test.tsx, frontend/src/pages/users/UsersListPage.tsx</files>
  <read_first>
    - .planning/phases/86-feature-completion/86-PATTERNS.md §11, §13, §16-18 (param-route analog, composite detail pattern, test mocks)
    - frontend/src/routes/_protected/engagements/$engagementId.tsx (:9-25 — param route analog; users version is simpler: no child Outlet)
    - frontend/src/pages/users/UsersListPage.tsx (:86-124 — the direct-select read pattern to adapt; row markup for the click target; the .or( block to NOT copy)
    - frontend/src/lib/format-date.ts (formatDayFirstYear / formatDateTime — Phase 82 Latin-digit policy for last_login_at)
    - frontend/src/i18n/en/user-management.json + ar (userDetail.* keys — exist EN+AR)
  </read_first>
  <behavior>
    - Test 1: given a mocked users select, the page renders email, username, full name, role label (via user-management:roles.*), and an active/inactive status badge
    - Test 2: last_login_at renders through the format-date helpers (day-first, Latin digits), not toLocaleDateString
    - Test 3: query error renders a localized error state (role="alert"), not a blank page
  </behavior>
  <action>
    Create `routes/_protected/users/$id.tsx`: `createFileRoute('/_protected/users/$id')`, component reads `const { id } = Route.useParams()` and renders `<UserDetailPage userId={id} />` — no per-child beforeLoad (the 86-02 layout's requireAdmin gates it), no child Outlet.
    Build `UserDetailPage` read half: `useQuery` with key `['users', 'detail', userId]` and the select from <interfaces> (`.eq('id', userId).is('deleted_by', null).single()`, no count). Render the profile fields with existing `userDetail.*` keys; role shown via `user-management:roles.*` (editor now exists from 86-02); status as an active/inactive badge using semantic tokens (no raw hex, no palette literals); `last_login_at` via `formatDayFirstYear`/`formatDateTime` from `@/lib/format-date`. Loading + error states (error uses role="alert"). Linear layout: surfaces via the ladder, borders `1px solid var(--line)`, radii tokens, logical properties, `text-start`.
    UsersListPage surgical edit ONLY: make rows navigate to `/users/$id` (typed Link/`useNavigate` with `params: { id }`); keep hover row treatment consistent with existing list rows. Do NOT touch the search `.or(` block or the role filter.
    Test file mocks `@/lib/supabase` for the select (PATTERNS §16 t-mock).
  </action>
  <verify>
    <automated>pnpm --dir frontend test -- run src/pages/users/__tests__/UserDetailPage.test.tsx && pnpm --dir frontend type-check</automated>
  </verify>
  <acceptance_criteria>
    - Targeted vitest exits 0 (render, date-format, error-state tests green)
    - `rg -n "\['users', 'detail'" frontend/src/pages/users/UserDetailPage.tsx` matches; `! rg -q "\.or\(" frontend/src/pages/users/UserDetailPage.tsx` (no injection-class search copied)
    - `rg -n 'users/\$id' frontend/src/pages/users/UsersListPage.tsx` matches (rows wired)
    - `pnpm --dir frontend lint --max-warnings 0` and type-check exit 0
  </acceptance_criteria>
  <done>/users/:id renders a real profile read behind the admin gate; list rows navigate to it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Role and status management actions (dual-approval branch surfaced)</name>
  <files>frontend/src/pages/users/UserDetailPage.tsx, frontend/src/pages/users/__tests__/UserDetailPage.test.tsx</files>
  <read_first>
    - frontend/src/services/user-management-api.ts (:39-59 AssignRole union types; :133-161 Deactivate/Reactivate types; the four invoke methods added in 86-02)
    - supabase/functions/assign-role/index.ts (:19-33,210 — dual-approval semantics)
    - frontend/src/i18n/en/user-management.json + ar (roles.roleRequiresApproval — exists EN+AR; userDeactivation.* block)
    - .planning/phases/86-feature-completion/86-PATTERNS.md §13 (the exact branch requirement)
  </read_first>
  <behavior>
    - Test 1: changing role to `editor` (immediate response) invalidates ['users'], shows success incl. sessions_terminated, and the displayed role updates
    - Test 2: changing role to `admin` returning `{requires_approval: true, ...}` shows the `user-management:roles.roleRequiresApproval` toast and the displayed role does NOT change (response never masked)
    - Test 3: deactivate flips the status badge to inactive and surfaces orphanedItems via userDeactivation.* keys; reactivate restores active
    - Test 4: the role picker offers exactly admin/editor/viewer
  </behavior>
  <action>
    Extend `UserDetailPage` with the write half:
    - Role control: Select limited to `['admin','editor','viewer']` + a confirm action; `useMutation` calling `assignRole` from `@/services/user-management-api`. Branch on the union with a `'requires_approval' in result` check: approval branch → toast `t('user-management:roles.roleRequiresApproval')` and STOP (no local role update, no approvals UI this phase — approve-role-change is out of scope, note in SUMMARY); immediate branch → invalidate `['users']` (list + detail prefix) and toast success including `sessions_terminated` count.
    - Status control: deactivate/reactivate buttons (destructive styling via semantic tokens for deactivate; `.btn-ghost`-recipe outline for reactivate) calling `deactivateUser`/`reactivateUser`; on success invalidate `['users']` and surface `orphanedItems`/`sessionsTerminated` with the existing `userDeactivation.*` keys. Add a confirm step before deactivation (house pattern: AlertDialog/confirm from ui primitives).
    - All mutations handle errors with localized toasts; no floating promises (`void` fire-and-forget where needed); explicit return types.
    Extend the unit test with the four behaviors (mock the service module; assert the requires_approval branch is the L1 "UI must not mask" control — T-86-13).
  </action>
  <verify>
    <automated>pnpm --dir frontend test -- run src/pages/users/__tests__/UserDetailPage.test.tsx && pnpm --dir frontend type-check</automated>
  </verify>
  <acceptance_criteria>
    - Targeted vitest exits 0; the requires_approval test proves the response is surfaced (toast) and the role display unchanged
    - `rg -n "requires_approval" frontend/src/pages/users/UserDetailPage.tsx` matches (branch exists)
    - `! rg -q "manager|staff" frontend/src/pages/users/UserDetailPage.tsx` (role set locked to the fn contract)
    - `pnpm --dir frontend lint --max-warnings 0` and type-check exit 0
  </acceptance_criteria>
  <done>Role/status management works against the hardened fns with the dual-approval flow honestly surfaced.</done>
</task>

<task type="auto">
  <name>Task 3: Combined user-management E2E — create → list → detail → role/status + IDOR smoke + AR</name>
  <files>frontend/tests/e2e/user-management.spec.ts</files>
  <read_first>
    - frontend/tests/e2e/global-setup.ts (pre-authenticated storageState; .env.test admin credentials)
    - frontend/src/lib/supabase.ts or frontend env config (anon key/url for the IDOR smoke client)
    - .planning/phases/86-feature-completion/86-RESEARCH.md Pitfalls 7 (inactive users + "all" filter) and A5 (created_at sort assumption)
    - One existing spec under frontend/tests/e2e/ for house locator style
  </read_first>
  <action>
    Write `user-management.spec.ts` covering the full D-10 loop as the admin (pre-authed session):
    1. FEAT-02 live smoke: /users → Create user → fill unique timestamped email/username (e.g. `e2e-<epoch>@example.test` / `e2e_<epoch>`), full name, role `editor`, clearance 2 → submit → success → back on the list with the DEFAULT "all" filter (created users are `is_active: false` — Pitfall 7), assert the new user's row is visible (search by the unique email if row-1-by-created_at is unreliable — assumption A5).
    2. FEAT-03: click the new user's row → /users/:id renders the profile (email/username/role visible). Change role editor→viewer → assert success feedback and updated role. Change role →admin → assert the pending-dual-approval toast appears and the displayed role does NOT become admin. Deactivate → status badge inactive; reactivate → active.
    3. IDOR smoke (T-86-12): using a plain supabase-js client with the anon key and NO session, `select` the created user's row by id — assert zero rows returned (house fact: RLS denial = empty 200, not an error). If a non-admin seed account is available (check `.env.test` / `.planning/seeds`), repeat with its JWT; if none exists, record that variant as a follow-up in the SUMMARY rather than faking it.
    4. AR pass: `/users/create?lng=ar` and the detail page with `?lng=ar` — assert AR strings render (no raw-key leaks) and the layout is `dir="rtl"` with start-aligned labels.
    Deterministic waits only; spec header documents the running-app precondition (dev server or E2E_BASE_URL against staging).
  </action>
  <verify>
    <automated>pnpm --dir frontend exec playwright test tests/e2e/user-management.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec exits 0 against the running app: create 201 → row visible (all filter) → detail read → immediate role change applied → admin grant surfaces requires_approval → deactivate/reactivate flip the badge
    - IDOR assert present and green: anon client read of another user's row returns 0 rows
    - AR pass asserts localized strings + RTL layout on both new pages
    - `pnpm --dir frontend type-check` exits 0
  </acceptance_criteria>
  <done>D-10 proven live end-to-end (both halves), with the L1 access-control smoke and bilingual check.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                  | Description                       |
| --------------------------------------------------------- | --------------------------------- |
| browser → public.users (PostgREST)                        | Detail read of user PII under RLS |
| browser → assign-role/deactivate-user/reactivate-user fns | Privileged role/status writes     |

## STRIDE Threat Register

| Threat ID | Category               | Component                | Disposition | Mitigation Plan                                                                                                                                                                                                                            |
| --------- | ---------------------- | ------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-86-12   | Info disclosure (IDOR) | /users/$id read          | mitigate    | Parent layout requireAdmin (from 86-02) + public.users RLS; Task 3 E2E smoke asserts an anon client cannot read arbitrary rows (zero rows on RLS denial); non-admin-JWT variant run if a seed non-admin exists, else recorded as follow-up |
| T-86-13   | Elevation              | Self-service admin grant | mitigate    | assign-role enforces dual-approval server-side; UI branches on `requires_approval` and SURFACES it (toast, no local role update) — unit test asserts the branch; no approvals UI shortcut built                                            |
| T-86-14   | Tampering              | Detail read query        | mitigate    | `.eq('id', userId)` builder only; the list page's `.or(...ilike...)` interpolation NOT copied into any new code (grep gate Task 1) — that class is SEC-01/Phase 88                                                                         |
| T-86-15   | Session management     | Role change side effects | transfer    | Session termination is fn-owned (assign-role terminates sessions); UI only displays sessions_terminated                                                                                                                                    |
| T-86-SC   | Tampering              | package installs         | n/a         | No new packages this phase                                                                                                                                                                                                                 |

</threat_model>

<verification>
- `pnpm --dir frontend test -- run src/pages/users/__tests__/UserDetailPage.test.tsx` green (incl. requires_approval branch)
- `pnpm --dir frontend exec playwright test tests/e2e/user-management.spec.ts` green (loop + IDOR + AR)
- `pnpm --dir frontend type-check` + `lint --max-warnings 0` exit 0
- `! rg -q "\.or\(" frontend/src/pages/users/UserDetailPage.tsx`
</verification>

<success_criteria>
Success criteria 2 + 3 (ROADMAP Phase 86): admin creates a user and sees it in the list (live-proven here), and admin opens /users/:id from the list to view/manage role, status, and profile — with the dual-approval response surfaced, the IDOR smoke green, and both pages verified in AR. D-10 fully closed.
</success_criteria>

<output>
Create `.planning/phases/86-feature-completion/86-04-SUMMARY.md` when done.
</output>
