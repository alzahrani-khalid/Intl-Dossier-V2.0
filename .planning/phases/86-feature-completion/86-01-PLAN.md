---
phase: 86-feature-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/domains/mous/types.ts
  - frontend/src/domains/mous/keys.ts
  - frontend/src/domains/mous/repositories/mous.repository.ts
  - frontend/src/domains/mous/hooks/useCreateMou.ts
  - frontend/src/domains/mous/index.ts
  - frontend/src/components/mous/CreateMouDialog.tsx
  - frontend/src/components/mous/__tests__/CreateMouDialog.test.tsx
  - frontend/src/pages/MoUs/MousPage.tsx
  - frontend/src/i18n/en/common.json
  - frontend/src/i18n/ar/common.json
  - frontend/tests/e2e/mou-create.spec.ts
autonomous: true
requirements: [FEAT-01]

must_haves:
  truths:
    - 'User can click the Add MoU button on /mous and a working create form opens (no longer disabled)'
    - 'Submitting the form writes a MoU through the deployed `mous` edge fn (type, mou_category, dates, parties, lifecycle_state)'
    - 'The new MoU appears in the MoUs list with non-empty party names (parties jsonb sent on create)'
    - 'Form validation mirrors the edge-fn contract: bilingual titles required, signatories must differ, expiry after effective'
    - 'The dialog renders correctly in AR/RTL (dir handling, Tajawal/var(--font-arabic) on Arabic input, logical properties)'
  artifacts:
    - path: 'frontend/src/components/mous/CreateMouDialog.tsx'
      provides: 'MoU create dialog (RHF + Zod, NewPositionDialog pattern)'
      min_lines: 150
    - path: 'frontend/src/domains/mous/repositories/mous.repository.ts'
      provides: "createMou → apiPost('/mous', payload)"
      exports: ['createMou']
    - path: 'frontend/src/domains/mous/hooks/useCreateMou.ts'
      provides: 'Mutation hook invalidating mouKeys.all'
      exports: ['useCreateMou']
    - path: 'frontend/src/components/mous/__tests__/CreateMouDialog.test.tsx'
      provides: 'Unit coverage incl. parties-derivation regression guard'
    - path: 'frontend/tests/e2e/mou-create.spec.ts'
      provides: 'E2E: Add MoU → submit → row visible; AR check via ?lng=ar'
  key_links:
    - from: 'frontend/src/pages/MoUs/MousPage.tsx'
      to: 'frontend/src/components/mous/CreateMouDialog.tsx'
      via: 'dialog mount + onClick open'
      pattern: 'CreateMouDialog'
    - from: 'frontend/src/components/mous/CreateMouDialog.tsx'
      to: 'frontend/src/domains/mous/hooks/useCreateMou.ts'
      via: 'mutateAsync on submit'
      pattern: 'useCreateMou'
    - from: 'frontend/src/domains/mous/repositories/mous.repository.ts'
      to: 'edge fn `mous` (deployed v10)'
      via: 'apiPost'
      pattern: "apiPost.*'/mous'"
    - from: 'frontend/src/domains/mous/hooks/useCreateMou.ts'
      to: "MousPage list query ['mous', searchTerm, filterState]"
      via: "invalidateQueries prefix ['mous']"
      pattern: 'invalidateQueries'
---

<objective>
FEAT-01: Make MoU creation real. The backend is already live (PR #85: `mous` edge fn v10 on staging, SECURITY DEFINER triggers, profile-resolved org RLS, verified end-to-end). This plan is pure frontend wiring: a `domains/mous` layer, a `CreateMouDialog` mirroring `NewPositionDialog`, enabling the disabled "Add MoU" button at `MousPage.tsx:209`, i18n form keys (EN+AR), a unit test, and an E2E spec.

Purpose: closes C-3 — the first of three honest-disabled data-entry features (ROADMAP Phase 86 success criterion 1).
Output: working MoU create flow; new MoU visible in the list with party names.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-feature-completion/86-RESEARCH.md
@.planning/phases/86-feature-completion/86-PATTERNS.md
@.planning/phases/86-feature-completion/86-VALIDATION.md
</context>

<interfaces>
Edge-fn request contract (source of truth: supabase/functions/mous/index.ts, deployed v10):
- Required: `title` (string, EN — column is `title`, NOT title_en), `title_ar` (string), `type` ∈ bilateral|multilateral|framework|technical (mou_type enum), `mou_category` ∈ data_exchange|capacity_building|strategic|technical
- Optional: `lifecycle_state` ∈ draft|negotiation|pending_approval|signed|active|suspended|expired|terminated (default draft), `description`, `country_id`, `signatory_1_dossier_id`, `signatory_2_dossier_id` (fn 400s if equal), `effective_date`, `expiry_date` (fn 400s unless expiry > effective), `dates` (jsonb), `parties` (jsonb array — view reads `parties->0/1->>'name_en'/'name_ar'`)
- NEVER send: `organization_id`, `tenant_id`, `created_by`, `reference_number` (server-resolved; ref auto-generated MOU-YYYY-NNNN)

DossierPicker contract (components/work-creation/DossierPicker.tsx:45-68):

- `onChange?: (dossierId: string | null, dossier?: DossierOption) => void` where `DossierOption = { id, name_en, name_ar, type, status }`
- `filterByDossierType?: DossierType | DossierType[]`

api-client (lib/api-client.ts): `apiPost<T>(path, body)` at :139; throws `ApiError extends Error` with `(message, status, details)` at :34-38.

Existing inline list key that invalidation must prefix-match: `MousPage.tsx:94` → `queryKey: ['mous', searchTerm, filterState]`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create the domains/mous layer + i18n form keys</name>
  <files>frontend/src/domains/mous/types.ts, frontend/src/domains/mous/keys.ts, frontend/src/domains/mous/repositories/mous.repository.ts, frontend/src/domains/mous/hooks/useCreateMou.ts, frontend/src/domains/mous/index.ts, frontend/src/i18n/en/common.json, frontend/src/i18n/ar/common.json</files>
  <read_first>
    - .planning/phases/86-feature-completion/86-PATTERNS.md §2-5 and §7 (exact analog excerpts)
    - frontend/src/domains/positions/repositories/positions.repository.ts (repository analog)
    - frontend/src/domains/positions/hooks/useCreatePosition.ts (hook analog, explicit-return-type idiom)
    - frontend/src/domains/engagements/keys.ts (key-factory analog)
    - frontend/src/domains/positions/index.ts (barrel analog)
    - frontend/src/lib/api-client.ts:34-38,139 (ApiError + apiPost)
    - frontend/src/i18n/en/common.json:290-311 + frontend/src/i18n/ar/common.json:290 (existing `mous` block incl. `statuses.*`)
  </read_first>
  <action>
    Create the mous domain per the PATTERNS analogs, interface-first:
    1. `types.ts`: `CreateMouPayload` mirroring the edge-fn contract in <interfaces> (title/title_ar required; `type`, `mou_category`, `lifecycle_state` as string-literal unions; optional description, country_id, signatory_1_dossier_id, signatory_2_dossier_id, effective_date, expiry_date, dates, parties as `Array<{ name_en: string; name_ar: string }>`). Also a minimal `Mou` response type (id, reference_number, title, title_ar, type, mou_category, lifecycle_state). Do NOT include organization_id/tenant_id/created_by/reference_number in the payload type.
    2. `keys.ts`: `mouKeys` factory with `all: ['mous'] as const` (+ optional `list()`) — root string MUST stay `'mous'` to prefix-match the inline key at MousPage.tsx:94.
    3. `repositories/mous.repository.ts`: `createMou(payload: CreateMouPayload): Promise<Mou>` returning `apiPost<Mou>('/mous', payload)` — exactly the positions.repository shape (module doc comment, `@/lib/api-client` import).
    4. `hooks/useCreateMou.ts`: `useMutation` calling the repository; `onSuccess` invalidates `{ queryKey: mouKeys.all }`. Explicit return type via the `ReturnType<typeof useMutation<Mou, Error, CreateMouPayload>>` house idiom (ESLint explicit-function-return-type).
    5. `index.ts` barrel: export `useCreateMou`, `mouKeys`, repository namespace, types.
    6. i18n: extend the existing `mous` block in BOTH en/common.json and ar/common.json (same commit, symmetric keys) with a `form` sub-block: field labels (title, titleAr, type, category, lifecycleState, signatory1, signatory2, effectiveDate, expiryDate, description), `errors.titleRequired`, `errors.titleArRequired`, `errors.signatoriesMustDiffer`, `errors.expiryAfterEffective`, `toastSuccess`, `toastError`, dialog title/description/cancel/submit, plus `types.*` (4 mou_type labels) and `categories.*` (4 mou_category labels). Do NOT add lifecycle labels — reuse the existing `mous.statuses.*` sub-block for lifecycle_state options. `common` is the default ns (page already calls `t('mous.addMou')` bare) — no namespace registration change. Sentence case, no emoji, no marketing voice; AR strings are real translations, not transliterations.
    House style: no semicolons, single quotes, explicit return types, no `any`. Filename case: domain `hooks/` camelCase, repository dotted-kebab — matches positions domain exactly.
  </action>
  <verify>
    <automated>pnpm --dir frontend type-check && pnpm --dir frontend lint --max-warnings 0</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/domains/mous/` contains types.ts, keys.ts, repositories/mous.repository.ts, hooks/useCreateMou.ts, index.ts; barrel exports compile
    - `rg -n "all: \['mous'\] as const" frontend/src/domains/mous/keys.ts` matches
    - `rg -n "apiPost<Mou>\('/mous'" frontend/src/domains/mous/repositories/mous.repository.ts` matches
    - `CreateMouPayload` has no organization_id/tenant_id/created_by/reference_number fields: `! rg -q "organization_id|tenant_id|created_by|reference_number" frontend/src/domains/mous/types.ts`
    - `rg -c '"form"' frontend/src/i18n/en/common.json` and the AR file both gain the block; `pnpm --dir frontend lint --max-warnings 0` (runs check-i18n-namespaces) exits 0
    - `pnpm --dir frontend type-check` exits 0
  </acceptance_criteria>
  <done>Domain layer + i18n keys exist, typed, linted, with the invalidation root locked to 'mous'.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CreateMouDialog + unit test + enable the Add MoU button</name>
  <files>frontend/src/components/mous/CreateMouDialog.tsx, frontend/src/components/mous/__tests__/CreateMouDialog.test.tsx, frontend/src/pages/MoUs/MousPage.tsx</files>
  <read_first>
    - .planning/phases/86-feature-completion/86-PATTERNS.md §1 and §6 (dialog scaffold, Zod-with-i18n-keys, Arabic-input treatment, MousPage edit target :207-213)
    - frontend/src/components/positions/NewPositionDialog.tsx (the house create-dialog canon — props, form setup :177-188, submit/error :305-338, footer :598-616)
    - frontend/src/components/positions/__tests__/NewPositionDialog.test.tsx (t-mock :50-63 + mutation-hook mock :115-120)
    - frontend/src/components/work-creation/DossierPicker.tsx:45-68 (DossierOption/props contract)
    - frontend/src/pages/MoUs/MousPage.tsx (:88 useDirection, :89-116 list query, :207-213 disabled button)
  </read_first>
  <behavior>
    - Test 1: dialog renders all fields (title EN, title AR, type select, category select, lifecycle select defaulting draft, two signatory pickers, effective/expiry dates, description)
    - Test 2: empty title_ar surfaces the `mous.form.errors.titleArRequired` message; submit blocked while invalid
    - Test 3: picking the same dossier for both signatories surfaces `mous.form.errors.signatoriesMustDiffer`
    - Test 4: expiry date earlier than effective date surfaces `mous.form.errors.expiryAfterEffective`
    - Test 5 (Pitfall-2 regression guard): successful submit calls mutateAsync with a payload whose `parties` array is derived from the two selected DossierOptions as [{name_en, name_ar}, {name_en, name_ar}]
    - Test 6: mutateAsync rejection keeps the dialog open with input intact and shows the error toast
  </behavior>
  <action>
    Build `CreateMouDialog` mirroring NewPositionDialog: controlled props `{ isOpen: boolean; onClose: () => void; isRTL: boolean }`; `useForm` with `zodResolver` + `mode: 'onTouched'`; Zod schema exactly as drafted in 86-RESEARCH.md §Code Examples (enum pickers for type/mou_category/lifecycle_state; two `.refine()`s: signatories-differ keyed to `signatory_2_dossier_id`, expiry-after-effective keyed to `expiry_date`; message strings are i18n KEYS under `mous.form.errors.*`, bare default-ns form). Fields: title `dir="ltr"`; title_ar `dir="rtl"` + `style={{ fontFamily: 'var(--font-arabic)' }}` (analog :143); Select options for type from `mous.form.types.*`, category from `mous.form.categories.*`, lifecycle from existing `mous.statuses.*`; two single-select `DossierPicker` instances with `filterByDossierType={['country','organization']}` — keep the full `DossierOption` from onChange in local state; native date inputs for effective/expiry; optional description textarea. On submit derive `parties` from the stored DossierOptions ([{name_en, name_ar}] per selected signatory — omit `parties` entirely if none selected) and call `useCreateMou().mutateAsync`; success → `form.reset()`, `onClose()`, `toast.success(t('mous.form.toastSuccess'))`; failure → `toast.error(t('mous.form.toastError'))`, dialog stays open. Labels `className="text-start"`, controls `min-h-11`, logical spacing only (`me-*`/`ms-*`), tokens only (no hex, no palette literals), Dialog/Form/Select/Input/Button from `@/components/ui/*`.
    MousPage surgical edit ONLY: at :207-213 drop `disabled` + `title={t('common.notYetAvailable')}`, add onClick opening local `createOpen` state, and mount `<CreateMouDialog isOpen onClose isRTL />` (page already has `isRTL` from useDirection at :88). Do NOT touch the list query, the `.or(...ilike...)` search block (:101-105 — SEC-01/Phase 88 territory), or restyle the page.
    Test file: colocated `__tests__` (lint-case exempt); use the NewPositionDialog.test t-mock (returns key when unmapped — catches raw-key leaks) and mock `@/domains/mous` hook + `DossierPicker` (render a stub that fires onChange with a full DossierOption).
  </action>
  <verify>
    <automated>pnpm --dir frontend test -- run src/components/mous/__tests__/CreateMouDialog.test.tsx && pnpm --dir frontend type-check</automated>
  </verify>
  <acceptance_criteria>
    - Targeted vitest file exits 0 with all behavior tests green (incl. the parties-derivation payload assert)
    - `rg -n "CreateMouDialog" frontend/src/pages/MoUs/MousPage.tsx` matches (mounted) and `rg -n "disabled title=" frontend/src/pages/MoUs/MousPage.tsx` no longer matches the Add MoU button
    - Threat gates: `! rg -q "\.or\(" frontend/src/components/mous frontend/src/domains/mous` (no filter-string interpolation in new code) and `! rg -q "from\('mous'\)" frontend/src/components/mous frontend/src/domains/mous` (no direct table writes — edge fn only)
    - `pnpm --dir frontend type-check` exits 0; `pnpm --dir frontend lint --max-warnings 0` exits 0
  </acceptance_criteria>
  <done>Add MoU opens a validated, token-compliant dialog that submits through the edge fn path and refreshes the list via ['mous'] invalidation.</done>
</task>

<task type="auto">
  <name>Task 3: E2E spec — create MoU end-to-end + AR/RTL check</name>
  <files>frontend/tests/e2e/mou-create.spec.ts</files>
  <read_first>
    - frontend/tests/e2e/global-setup.ts (storageState auth model — specs start pre-authenticated; tour overlays pre-dismissed)
    - One existing spec under frontend/tests/e2e/ for locator/assertion house style
    - frontend/playwright.config.ts (baseURL: E2E_BASE_URL ?? http://localhost:5173)
    - 86-RESEARCH.md Pitfalls 1 and 2 (profile org 400; parties display)
  </read_first>
  <action>
    Write `mou-create.spec.ts`: (1) goto `/mous`; click the Add MoU button; fill title with a unique timestamped value (e.g. `E2E MoU <epoch>`), title_ar with an Arabic string, pick type `bilateral`, category `data_exchange`, keep lifecycle `draft`, select two different signatory dossiers via the pickers; submit; assert the success toast and that a row containing the unique title appears in the list; assert the row's party cells are non-empty (Pitfall-2 guard against blank `parties`). If the API returns 400 "Caller has no organization on profile" (Pitfall 1), let the test FAIL with that message surfaced — do not retry or mask. (2) AR pass: goto `/mous?lng=ar` (locale detector reads the querystring at first paint), open the dialog, assert the Arabic-title input has `dir="rtl"` and at least one AR label string from `ar/common.json` `mous.form.*` renders (no raw-key leak). Precondition note in the spec header: requires a running app (dev server or `E2E_BASE_URL`) against staging with `.env.test` credentials.
  </action>
  <verify>
    <automated>pnpm --dir frontend exec playwright test tests/e2e/mou-create.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec exits 0 against the running app: new MoU row visible with non-empty party names; AR pass asserts dir="rtl" + localized label
    - No `waitForTimeout`-style flaky waits — deterministic locator waits only
    - `pnpm --dir frontend type-check` exits 0
  </acceptance_criteria>
  <done>FEAT-01 proven live end-to-end in EN and AR: button → form → edge fn → list row with parties.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                    |
| ------------------------------------- | ---------------------------------------------- |
| browser → `mous` edge fn              | Untrusted form input crosses to the write path |
| browser → PostgREST (`mous_frontend`) | List read under RLS                            |

## STRIDE Threat Register

| Threat ID | Category        | Component                     | Disposition | Mitigation Plan                                                                                                                                                                                  |
| --------- | --------------- | ----------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-86-01   | Tampering       | New mous code (dialog/domain) | mitigate    | No PostgREST filter-string interpolation anywhere in new code (grep gate in Task 2); existing MousPage `.or(...ilike.%${term}%)` search left untouched — that class is SEC-01, owned by Phase 88 |
| T-86-02   | Elevation       | MoU write path                | mitigate    | Writes ONLY via `apiPost('/mous')` edge fn (server resolves org/tenant, RLS `mous_org_isolation_insert` enforces); grep gate: no `from('mous')` insert in new dirs                               |
| T-86-03   | Tampering       | Form input → fn               | mitigate    | Zod client schema mirrors fn validators (required bilingual titles, enum sets, sig1≠sig2, expiry>effective); edge fn re-validates server-side — client validation is UX only                     |
| T-86-04   | Info disclosure | `mous` fn wildcard CORS       | accept      | Known debt owned by Phase 90 (CORS-01..03); not widened here                                                                                                                                     |
| T-86-SC   | Tampering       | package installs              | n/a         | No new packages this phase (RESEARCH Package Legitimacy Audit: none)                                                                                                                             |

</threat_model>

<verification>
- `pnpm --dir frontend test -- run src/components/mous/__tests__/CreateMouDialog.test.tsx` green
- `pnpm --dir frontend exec playwright test tests/e2e/mou-create.spec.ts` green (EN + AR passes)
- `pnpm --dir frontend type-check` and `pnpm --dir frontend lint --max-warnings 0` exit 0
- Threat gates: `! rg -q "\.or\(" frontend/src/components/mous frontend/src/domains/mous` and `! rg -q "from\('mous'\)" frontend/src/components/mous frontend/src/domains/mous`
</verification>

<success_criteria>
Success criterion 1 (ROADMAP Phase 86): user creates a MoU from the MoUs page — the Add MoU button opens a working form (type, mou_category, dates, parties, lifecycle_state) that writes `mous`, and the new MoU appears in the list with party names. C-3 closed. AR/RTL verified via the `?lng=ar` E2E pass (final human render sign-off in plan 86-05).
</success_criteria>

<output>
Create `.planning/phases/86-feature-completion/86-01-SUMMARY.md` when done.
</output>
