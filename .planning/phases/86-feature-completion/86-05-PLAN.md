---
phase: 86-feature-completion
plan: 05
type: execute
wave: 3
depends_on: [86-01, 86-02, 86-03, 86-04]
files_modified: []
autonomous: false
requirements: [FEAT-01, FEAT-02, FEAT-03, FEAT-04]

must_haves:
  truths:
    - 'Full frontend gates are green across the whole phase: vitest suite, lint --max-warnings 0, type-check, both new E2E specs, and the FEAT-04 grep gates'
    - 'All new/changed surfaces (MoU dialog, /users/create, /users/:id) are human-verified at 1400 and 1024, dark, in BOTH EN/LTR and AR/RTL (Tajawal, logical properties, token compliance) — success criterion 5'
  artifacts: []
  key_links: []
---

<objective>
Phase gate: run the consolidated verification battery from 86-VALIDATION.md and take the single house-practice human render sign-off for the three new surfaces (Phases 81-85 precedent: one consolidated walk, not per-plan churn).

Purpose: closes ROADMAP Phase 86 success criterion 5 (EN/LTR + AR/RTL correctness on every new/changed surface) and gates the phase before /gsd:verify-work.
Output: green gate log + human approval recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-feature-completion/86-VALIDATION.md
@.planning/phases/86-feature-completion/86-01-SUMMARY.md
@.planning/phases/86-feature-completion/86-02-SUMMARY.md
@.planning/phases/86-feature-completion/86-03-SUMMARY.md
@.planning/phases/86-feature-completion/86-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full phase gate battery</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/86-feature-completion/86-VALIDATION.md (sampling contract + per-req test map)
    - The four wave-1/2 SUMMARYs (any recorded deviations to re-check)
  </read_first>
  <action>
    Run and record, in order: (1) `pnpm --dir frontend test -- run` (full vitest); (2) `pnpm --dir frontend lint --max-warnings 0`; (3) `pnpm --dir frontend type-check`; (4) the FEAT-04 grep gates (`! rg -q "ConsistencyPanel" frontend/src`; `! rg -q "ConsistencyCheck\b" frontend/src`; `! rg -q "consistency_check\b" frontend/src --glob '!**/database.types.ts'`); (5) the security grep gates from plans 01/02/04 (no `.or(` interpolation and no `from('mous')` writes in the new mous/users code); (6) both E2E specs against the running app: `pnpm --dir frontend exec playwright test tests/e2e/mou-create.spec.ts tests/e2e/user-management.spec.ts`. Any red gate: fix-forward within this plan only if the fix is a test/verification defect; product defects go back as gap reports — do not paper over.
  </action>
  <verify>
    <automated>pnpm --dir frontend test -- run && pnpm --dir frontend lint --max-warnings 0 && pnpm --dir frontend type-check && ! rg -q "ConsistencyPanel" frontend/src && pnpm --dir frontend exec playwright test tests/e2e/mou-create.spec.ts tests/e2e/user-management.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All six gate groups exit 0, logged in the SUMMARY with command output excerpts
    - Zero skipped/quarantined tests introduced by this phase
  </acceptance_criteria>
  <done>Phase-wide automated evidence is green and recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Consolidated human render sign-off (criterion 5)</name>
  <files>none (human verification)</files>
  <action>Present the render walk below to the developer and wait for explicit approval. Do not self-approve; this checkpoint is the house-practice consolidated render sign-off (Phases 81-85 precedent) covering ROADMAP success criterion 5.</action>
  <what-built>Three new surfaces on top of the deployed backends: the MoU create dialog on /mous, the user create page at /users/create, and the user detail page at /users/:id — all built to Linear tokens with EN/LTR + AR/RTL support.</what-built>
  <how-to-verify>
    With the dev server running (or against the staging deploy), walk each surface at BOTH 1400px and 1024px, dark mode, in EN then AR (append `?lng=ar`):
    1. /mous → click "Add MoU" → confirm the dialog: token-flat surfaces (no shadows/gradients), 1px var(--line) borders, 6/8/12 radii, labels start-aligned, Arabic title input renders Tajawal RTL; cancel, reopen, create a throwaway MoU, confirm it appears in the list with party names.
    2. /users → "Create user" button → /users/create: form layout correct in LTR and RTL, role picker shows Admin/Editor/Viewer localized, validation messages localized (no raw keys like `user-management:...` visible).
    3. /users → click a row → /users/:id: profile fields, role/status controls, status badge; try a role change to admin and confirm the "pending dual approval" message appears.
    4. Confirm NO leftover consistency-panel UI or strings anywhere on positions pages.
  </how-to-verify>
  <verify>
    <automated>MISSING — human render walk; automated equivalents (E2E ?lng=ar passes) already ran in Task 1</automated>
  </verify>
  <done>Developer typed approval recorded verbatim in the SUMMARY, or the reported issues were fixed and re-verified.</done>
  <resume-signal>Type "approved" to close the phase, or describe the failing surface/state (page, width, locale) for a fix round.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                   |
| -------- | ------------------------------------------------------------- |
| —        | Verification-only plan; no new code paths or trust boundaries |

## STRIDE Threat Register

| Threat ID | Category    | Component        | Disposition | Mitigation Plan                                                                                                                                |
| --------- | ----------- | ---------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| T-86-16   | Repudiation | Phase evidence   | mitigate    | Gate outputs + human approval recorded in the SUMMARY; re-runs the T-86-01/02/12/14 security gates from plans 01/02/04 as the phase-exit check |
| T-86-SC   | Tampering   | package installs | n/a         | No packages installed                                                                                                                          |

</threat_model>

<verification>
- Task 1 battery green; Task 2 human approval recorded verbatim in the SUMMARY.
</verification>

<success_criteria>
Success criterion 5 (ROADMAP Phase 86): all new/changed surfaces work correctly in both EN/LTR and AR/RTL, human-verified at the two analyst-workstation widths. Phase 86 ready for /gsd:verify-work.
</success_criteria>

<output>
Create `.planning/phases/86-feature-completion/86-05-SUMMARY.md` when done.
</output>
