---
phase: 86-feature-completion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/consistency-panel/ConsistencyPanel.tsx
  - frontend/src/components/__tests__/ConsistencyPanel.test.tsx
  - frontend/src/i18n/en/positions.json
  - frontend/src/i18n/ar/positions.json
  - frontend/src/routes/_protected/positions/$id.tsx
  - frontend/src/types/position.ts
  - frontend/src/domains/positions/types/index.ts
  - .planning/decisions/ADR-008-consistency-panel-retirement.md
  - .planning/STATE.md
autonomous: true
requirements: [FEAT-04]

must_haves:
  truths:
    - 'No permanently-dead ConsistencyPanel UI remains: component, test, i18n consistency block (EN+AR), dead types, and the stale mount comment are all gone'
    - 'The retirement decision is RECORDED with rationale and a post-Phase-91 revisit trigger (ADR-008 + STATE.md entry) — not a silent removal'
    - 'Backend assets are RETAINED dormant: positions-consistency-check edge fn, position_consistency_checks table/RPCs/migrations, and the generated database.types.ts schema are untouched'
    - 'Full frontend suite stays green after the deletions (no dangling imports or key references)'
  artifacts:
    - path: '.planning/decisions/ADR-008-consistency-panel-retirement.md'
      provides: 'Auditable decision record closing E-8 (criterion 4)'
      contains: 'LIVE-01'
  key_links:
    - from: '.planning/STATE.md'
      to: '.planning/decisions/ADR-008-consistency-panel-retirement.md'
      via: 'Decisions Made entry'
      pattern: 'ADR-008'
---

<objective>
FEAT-04: formally retire the ConsistencyPanel (the DELETE path — the build-vs-delete call was delegated to research+planner; 86-RESEARCH.md § FEAT-04 gives HIGH-confidence evidence for DELETE: nothing writes `position_embeddings` so the vector leg is dead, the LLM leg is GPU-gated until Phase 91, the remaining keyword heuristic is a proven false-positive generator, the panel is unmounted with total contract drift, and its accept/escalate actions have no backend meaning — reconcile is a 501 stub). Delete the frontend artifacts, retain all backend assets dormant, and record the decision + revisit trigger.

Purpose: closes E-8 (ROADMAP success criterion 4 — "fully deleted (component + i18n keys) with the decision recorded").
Output: zero dangling references in frontend/src; ADR-008 committed; STATE.md decision entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-feature-completion/86-RESEARCH.md
@.planning/phases/86-feature-completion/86-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the ConsistencyPanel surface (component, test, i18n, dead types, stale comment)</name>
  <files>frontend/src/components/consistency-panel/ConsistencyPanel.tsx, frontend/src/components/__tests__/ConsistencyPanel.test.tsx, frontend/src/i18n/en/positions.json, frontend/src/i18n/ar/positions.json, frontend/src/routes/_protected/positions/$id.tsx, frontend/src/types/position.ts, frontend/src/domains/positions/types/index.ts</files>
  <read_first>
    - .planning/phases/86-feature-completion/86-PATTERNS.md "FEAT-04 delete scope" table (items 21-26 — exact verified locations) and the delete-scope verification note
    - frontend/src/domains/positions/types/index.ts (:12 import alias, :22 re-export, :191 consistency_check field)
    - frontend/src/types/position.ts (:104-122 — comment + ConsistencyCheck interface)
    - frontend/src/routes/_protected/positions/$id.tsx (:212-213 stale comment)
    - frontend/src/i18n/en/positions.json (:85-183 consistency block; next top-level key emergencyCorrection at :184) + ar/positions.json (:85)
  </read_first>
  <action>
    Execute the verified delete scope exactly (PATTERNS items 21-26):
    1. Delete the directory `frontend/src/components/consistency-panel/` (contains only ConsistencyPanel.tsx, 382 lines, mounted nowhere).
    2. Delete `frontend/src/components/__tests__/ConsistencyPanel.test.tsx` (17 tests against the old stub contract).
    3. Delete the top-level `"consistency"` block from BOTH `en/positions.json` (:85-183 — the next top-level key `emergencyCorrection` starts at :184) and `ar/positions.json` (block starts :85) in the same commit — keep EN/AR symmetric and the JSON valid (watch the trailing comma on the preceding key).
    4. Delete the stale 2-line comment at `routes/_protected/positions/$id.tsx:212-213` ("Consistency panel hidden until the check query + action handlers are wired") — nothing else in that file.
    5. Delete the `ConsistencyCheck` interface from `types/position.ts` (:105-122 including the `// Consistency check result…` comment at :104).
    6. In `domains/positions/types/index.ts`: remove the :12 import alias `ConsistencyCheck as ConsistencyCheckType`, the :22 `ConsistencyCheck` re-export, and the :191 `consistency_check: ConsistencyCheckType` field — leaving `SubmitPositionResponse` as `{ position: PositionType }` (which matches what positions-submit actually returns; the field was a lie per research).
    RETAIN untouched: `types/database.types.ts` (generated — its `position_consistency_checks` schema is the retained backend), the `positions-consistency-check` and `positions-consistency-reconcile` edge fns, migrations `20250101008` + `20260111100001`, and all DB objects. This is a frontend-only retirement; do not "clean up" backend assets.
  </action>
  <verify>
    <automated>! rg -q "ConsistencyPanel" frontend/src && ! rg -q "ConsistencyCheck\b" frontend/src && ! rg -q "consistency_check\b" frontend/src --glob '!**/database.types.ts' && ! rg -q '"consistency"' frontend/src/i18n/en/positions.json frontend/src/i18n/ar/positions.json && pnpm --dir frontend test -- run && pnpm --dir frontend type-check</automated>
  </verify>
  <acceptance_criteria>
    - `! rg -q "ConsistencyPanel" frontend/src` (zero references in live code)
    - `! rg -q "ConsistencyCheck\b" frontend/src` (dead type fully pruned)
    - `! rg -q "consistency_check\b" frontend/src --glob '!**/database.types.ts'` (only the retained generated schema remains)
    - `! rg -q '"consistency"' frontend/src/i18n/en/positions.json frontend/src/i18n/ar/positions.json` and both files parse (lint's i18n check passes)
    - `rg -q "position_consistency_checks" frontend/src/types/database.types.ts` still matches (retention proof)
    - Full `pnpm --dir frontend test -- run` green; `pnpm --dir frontend type-check` and `pnpm --dir frontend lint --max-warnings 0` exit 0
  </acceptance_criteria>
  <done>Frontend carries zero ConsistencyPanel artifacts; backend assets provably retained; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Record the retirement decision (ADR-008 + STATE.md)</name>
  <files>.planning/decisions/ADR-008-consistency-panel-retirement.md, .planning/STATE.md</files>
  <read_first>
    - .planning/decisions/ADR-007-accept-interim-v7-verification-evidence.md (house ADR format: Status/Date/Context/Decision/Residual Risk)
    - .planning/phases/86-feature-completion/86-RESEARCH.md § "FEAT-04: ConsistencyPanel — Build vs Delete" (the evidence being recorded)
    - .planning/STATE.md "### Decisions Made" section (entry format)
  </read_first>
  <action>
    Write `ADR-008-consistency-panel-retirement.md` following the ADR-007 format. Content requirements:
    - Context: E-8 provenance (L7 honest-disable left the panel unmounted); the build-vs-delete call was explicitly delegated by the user to research+planning; summarize the four pivotal evidence points from 86-RESEARCH.md (dead vector leg — nothing writes `position_embeddings`; LLM leg gated on VLLM_BASE_URL/Phase 91 LIVE-01; the keyword heuristic is a false-positive generator whose score would be misleading UI; total contract drift + `positions-consistency-reconcile` is a 501 stub with accept/escalate semantics undefined).
    - Decision: DELETE the frontend surface (component + test + i18n consistency block + dead types + stale comment). RETAIN dormant: `positions-consistency-check` edge fn (979 lines), `position_consistency_checks` table, `get_latest_consistency_check`/`can_auto_approve_position` RPCs, migrations.
    - Revisit trigger (MANDATORY, verbatim intent): after Phase 91 lands LIVE-01/LIVE-03 (live GPU inference), if position consistency is still wanted, re-scope as "LLM-backed consistency review" WITH a position_embeddings backfill pipeline — never resurrect the keyword heuristic as a numeric score. Reference 86-RESEARCH.md § FEAT-04 as the revisit map.
    Then append one entry to STATE.md "### Decisions Made": `- [Phase 86]: FEAT-04 ConsistencyPanel formally retired (frontend deleted, backend assets retained dormant); revisit after Phase 91 LIVE-01 — see ADR-008`.
    No marketing voice; plain factual prose.
  </action>
  <verify>
    <automated>test -f .planning/decisions/ADR-008-consistency-panel-retirement.md && rg -q "LIVE-01" .planning/decisions/ADR-008-consistency-panel-retirement.md && rg -q "position_embeddings" .planning/decisions/ADR-008-consistency-panel-retirement.md && rg -q "ADR-008" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - ADR-008 exists with Status/Date/Context/Decision sections, names the retained backend assets, and contains the Phase-91/LIVE-01 revisit trigger and the "never resurrect the keyword heuristic" constraint
    - STATE.md "### Decisions Made" contains the Phase 86 entry referencing ADR-008
    - The ADR notes the decision was user-delegated to research+planning (audit trail for criterion 4)
  </acceptance_criteria>
  <done>E-8 closes with an auditable decision record and a concrete revisit trigger — not a silent removal.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                           |
| -------- | --------------------------------------------------------------------- |
| —        | Deletion-only plan; no new input paths or trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category                    | Component                                                                                      | Disposition | Mitigation Plan                                                                                                                                                               |
| --------- | --------------------------- | ---------------------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-86-10   | Repudiation                 | Feature retirement                                                                             | mitigate    | ADR-008 + STATE.md entry make the removal auditable with rationale + revisit trigger (success criterion 4 requires "decision recorded")                                       |
| T-86-11   | Info disclosure / integrity | Retained dormant positions-consistency-check fn (wildcard CORS, supabase-js 2.39.0, unmounted) | accept      | Fn has no UI consumer after this plan; CORS migration is Phase 90 scope; the deletion REMOVES the integrity risk of shipping a misleading keyword-derived "consistency score" |
| T-86-SC   | Tampering                   | package installs                                                                               | n/a         | No new packages; deletions only                                                                                                                                               |

</threat_model>

<verification>
- All four grep gates from Task 1 pass (ConsistencyPanel / ConsistencyCheck / consistency_check / i18n block all zero; database.types.ts retention proof positive)
- Full `pnpm --dir frontend test -- run` + `type-check` + `lint --max-warnings 0` green post-deletion
- ADR-008 exists with the revisit trigger; STATE.md references it
</verification>

<success_criteria>
Success criterion 4 (ROADMAP Phase 86): ConsistencyPanel is fully deleted (component + i18n keys) with the decision recorded. E-8 closed with an auditable ADR and a post-LIVE-01 revisit trigger; backend assets retained dormant.
</success_criteria>

<output>
Create `.planning/phases/86-feature-completion/86-03-SUMMARY.md` when done.
</output>
