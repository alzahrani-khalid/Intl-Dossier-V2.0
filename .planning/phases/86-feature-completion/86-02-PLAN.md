---
phase: 86-feature-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/services/user-management-api.ts
  - frontend/src/i18n/en/user-management.json
  - frontend/src/i18n/ar/user-management.json
  - frontend/src/routes/_protected/users.tsx
  - frontend/src/routes/_protected/users/index.tsx
  - frontend/src/routes/_protected/users/create.tsx
  - frontend/src/pages/users/UserCreatePage.tsx
  - frontend/src/pages/users/__tests__/UserCreatePage.test.tsx
  - frontend/src/pages/users/UsersListPage.tsx
autonomous: true
requirements: [FEAT-02]

must_haves:
  truths:
    - 'The `assign-role` edge fn is verified deployed on staging BEFORE detail-page work depends on it (research risk A2)'
    - '/users, /users/create (and later /users/:id) are all admin-gated by ONE requireAdmin beforeLoad on the converted layout route'
    - 'Admin can open /users/create from a Create user button on the users list'
    - 'The create form mirrors the deployed edge validators (email regex, username ^[a-z0-9_-]{3,50}$, full_name 2-100, role admin/editor/viewer ONLY, clearance int 1-4) and invokes the create-user fn'
    - 'Duplicate email/username 400s surface as field-level errors; requires_approval-class responses are never masked'
    - 'roles.editor localizes in EN and AR (missing key added)'
  artifacts:
    - path: 'frontend/src/routes/_protected/users.tsx'
      provides: 'Layout route: Outlet + beforeLoad requireAdmin'
      contains: 'Outlet'
    - path: 'frontend/src/routes/_protected/users/index.tsx'
      provides: 'Index route mounting the existing users list'
    - path: 'frontend/src/routes/_protected/users/create.tsx'
      provides: 'Create route mounting UserCreatePage'
    - path: 'frontend/src/pages/users/UserCreatePage.tsx'
      provides: 'RHF+Zod create form invoking createUser()'
      min_lines: 120
    - path: 'frontend/src/services/user-management-api.ts'
      provides: 'createUser/assignRole/deactivateUser/reactivateUser invoke methods + clearance field on CreateUserRequest'
      exports: ['createUser', 'assignRole', 'deactivateUser', 'reactivateUser']
    - path: 'frontend/src/pages/users/__tests__/UserCreatePage.test.tsx'
      provides: 'Unit coverage of validator mirroring + submit + duplicate handling'
  key_links:
    - from: 'frontend/src/routes/_protected/users.tsx'
      to: 'frontend/src/lib/auth/require-admin.ts'
      via: 'beforeLoad'
      pattern: 'beforeLoad: requireAdmin'
    - from: 'frontend/src/pages/users/UserCreatePage.tsx'
      to: 'frontend/src/services/user-management-api.ts'
      via: 'createUser() on submit'
      pattern: 'createUser'
    - from: 'frontend/src/services/user-management-api.ts'
      to: 'edge fn create-user v2 (staging)'
      via: 'supabase.functions.invoke'
      pattern: "'create-user'"
    - from: 'frontend/src/pages/users/UsersListPage.tsx'
      to: '/users/create'
      via: 'Create user button/Link'
      pattern: 'users/create'
---

<objective>
FEAT-02 (D-10 create half): stand up the user-management route family and the create flow against the L1-hardened edge functions (create-user v2 / deactivate-user v1 / reactivate-user v1 deployed 2026-06-28; assign-role deployment MEDIUM-confidence — verified first here). Converts flat `users.tsx` to a layout, adds the missing invoke methods + `clearance` type field, builds `/users/create`, and adds the missing `roles.editor` i18n key.

Purpose: closes the create half of D-10 (ROADMAP success criterion 2) and lays the routing/service foundation plan 86-04 (detail page) builds on.
Output: admin-gated `/users` layout family; working user creation; verified assign-role deployment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-feature-completion/86-RESEARCH.md
@.planning/phases/86-feature-completion/86-PATTERNS.md
@.planning/phases/86-feature-completion/86-VALIDATION.md
</context>

<interfaces>
create-user edge-fn validators (source of truth: supabase/functions/create-user/index.ts, deployed v2):
- email regex; username `^[a-z0-9_-]{3,50}$` (lowercase); full_name 2-100 chars; role ∈ ['admin','editor','viewer']; optional `clearance` integer 1-4 (index.ts:29, validated :84-92); `user_type: 'guest'` additionally requires future expires_at + non-empty allowed_resources (this phase hardcodes 'employee' per research Open Question 3)
- Errors: 400 `DUPLICATE_EMAIL` / `DUPLICATE_USERNAME`; rate limit 10/min; requester must be admin per `public.users.role` (403 otherwise)
- Success: 201 `{ user_id, activation_sent }`; created users are `is_active: false` (pending activation — activation email is a console.log stub and there is NO /activate route: pre-existing gap, explicitly OUT of FEAT-02 scope, record in SUMMARY)

Client types already in frontend/src/services/user-management-api.ts:

- `CreateUserRequest/Response` :12-27 — NOTE: `CreateUserRequest` (:12-20) is MISSING `clearance?: number` (PATTERNS research correction) — must be added
- `AssignRoleRequest` + `AssignRoleResponse = AssignRoleImmediateResponse | AssignRoleApprovalResponse` :39-59
- `DeactivateUserRequest/Response` :133-150, `ReactivateUserRequest/Response` :152-161
- Invoke-method analog: `delegatePermissions` :258-277 (copy this shape verbatim per fn)
  </interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify assign-role deployment, add invoke methods + clearance field + roles.editor i18n</name>
  <files>frontend/src/services/user-management-api.ts, frontend/src/i18n/en/user-management.json, frontend/src/i18n/ar/user-management.json</files>
  <read_first>
    - .planning/phases/86-feature-completion/86-PATTERNS.md §14-15 (delegatePermissions analog + i18n edit target)
    - frontend/src/services/user-management-api.ts (whole file — types :12-161, delegatePermissions :258-277)
    - supabase/functions/create-user/index.ts:29,84-92 (clearance contract) and supabase/functions/assign-role/index.ts:19-33,210 (dual-approval response shape)
    - frontend/src/i18n/en/user-management.json:41-46 + ar mirror (roles block — admin/manager/staff/viewer, no editor)
  </read_first>
  <action>
    FIRST — deployment verification (research risk A2 / Pitfall 8, the one deployment risk this phase): run `supabase functions list --project-ref zkrcjzdemdmwhearhfgg` and confirm `assign-role` is present and ACTIVE. If missing or errored, deploy it unchanged via `supabase functions deploy assign-role --project-ref zkrcjzdemdmwhearhfgg` and re-list. Smoke: an unauthenticated POST to the fn URL must return 401 (not 404) — proves routable. Record the found version in the SUMMARY. Do NOT edit the fn source and do NOT migrate its CORS (Phase 90 owns that — Pitfall 9).
    THEN — service methods: in `user-management-api.ts`, add four exported async functions mirroring `delegatePermissions` verbatim: `createUser` → invoke `'create-user'`, `assignRole` → `'assign-role'`, `deactivateUser` → `'deactivate-user'`, `reactivateUser` → `'reactivate-user'`, each typed with the existing request/response interfaces, throwing on error and on null result ("No response from <fn> function"). Add the missing `clearance?: number` field to `CreateUserRequest` (:12-20) — the deployed fn accepts it (integer 1-4).
    THEN — i18n: add `"editor"` to the `roles` block beside `admin` (:42) in BOTH en and ar user-management.json (EN "Editor"; AR "محرر"), same commit, symmetric. Do NOT remove `manager`/`staff` (pre-existing list-filter drift, out of scope — note in SUMMARY only). Namespace `user-management` is already registered in src/i18n/index.ts — no registration change.
  </action>
  <verify>
    <automated>supabase functions list --project-ref zkrcjzdemdmwhearhfgg | grep -i "assign-role" && pnpm --dir frontend type-check && pnpm --dir frontend lint --max-warnings 0</automated>
  </verify>
  <acceptance_criteria>
    - `supabase functions list` output shows assign-role ACTIVE (version recorded in SUMMARY); unauth POST returns 401 not 404
    - `rg -n "clearance\?: number" frontend/src/services/user-management-api.ts` matches inside CreateUserRequest
    - Four new exports resolve: `rg -c "export async function (createUser|assignRole|deactivateUser|reactivateUser)" frontend/src/services/user-management-api.ts` returns 4
    - `rg -n '"editor"' frontend/src/i18n/en/user-management.json frontend/src/i18n/ar/user-management.json` matches in both files
    - type-check and lint exit 0
  </acceptance_criteria>
  <done>assign-role deployment confirmed; all four invoke methods exist typed; roles.editor localizes EN+AR.</done>
</task>

<task type="auto">
  <name>Task 2: Convert users.tsx to a layout route with index child</name>
  <files>frontend/src/routes/_protected/users.tsx, frontend/src/routes/_protected/users/index.tsx</files>
  <read_first>
    - .planning/phases/86-feature-completion/86-PATTERNS.md §8-9 (current users.tsx :1-8, engagements layout analog)
    - frontend/src/routes/_protected/engagements.tsx (:9-13 — the layout shape) and frontend/src/routes/_protected/engagements/index.tsx (:9-14 — the index shape)
    - frontend/src/routes/_protected/users.tsx (current flat route)
    - frontend/src/lib/auth/require-admin.ts (:19-34 — the guard being preserved)
  </read_first>
  <action>
    Convert `routes/_protected/users.tsx` to the engagements layout shape: `component: () => <Outlet />` — but KEEP `beforeLoad: requireAdmin` (TanStack runs parent beforeLoad for every child, so one line gates /users, /users/create, and the later /users/:id — Pitfall 4). Create `routes/_protected/users/index.tsx` with `createFileRoute('/_protected/users/')` mounting `UsersPage` (note: `pages/users/UsersPage.tsx` is a one-line re-export of `UsersListPage` — either import name works). Let `routeTree.gen.ts` regenerate via dev/build — NEVER hand-edit it. No per-child beforeLoad. Authz reads only `public.users.role` — never user_metadata/app_metadata.
  </action>
  <verify>
    <automated>pnpm --dir frontend type-check && pnpm --dir frontend lint --max-warnings 0</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Outlet" frontend/src/routes/_protected/users.tsx` matches AND `rg -n "beforeLoad: requireAdmin" frontend/src/routes/_protected/users.tsx` matches
    - `frontend/src/routes/_protected/users/index.tsx` exists mounting the list page
    - `routeTree.gen.ts` regenerated (not hand-edited): type-check exits 0 with the new route ids
    - No import of user_metadata/app_metadata in touched files: `! rg -q "user_metadata|app_metadata" frontend/src/routes/_protected/users.tsx frontend/src/routes/_protected/users/index.tsx`
  </acceptance_criteria>
  <done>/users still renders the list; the layout now gates the whole family with one requireAdmin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: UserCreatePage + create route + list-page Create user button + unit test</name>
  <files>frontend/src/pages/users/UserCreatePage.tsx, frontend/src/pages/users/__tests__/UserCreatePage.test.tsx, frontend/src/routes/_protected/users/create.tsx, frontend/src/pages/users/UsersListPage.tsx</files>
  <read_first>
    - .planning/phases/86-feature-completion/86-PATTERNS.md §10, §12, §16-18 (route shape, composite form analog, test mock patterns)
    - frontend/src/components/positions/NewPositionDialog.tsx (form-layer analog: Zod-with-i18n-keys, Form primitives, Arabic-input treatment)
    - frontend/src/services/user-management-api.ts (createUser from Task 1; CreateUserRequest incl. new clearance field)
    - supabase/functions/create-user/index.ts (validators the Zod schema must mirror)
    - frontend/src/pages/users/UsersListPage.tsx (header area for the button; do NOT touch its .or( search block :114-119)
    - frontend/src/i18n/en/user-management.json + ar (existing keys to reuse — form labels, userDetail.*, roles.*)
  </read_first>
  <behavior>
    - Test 1: role picker offers EXACTLY admin/editor/viewer (no manager/staff — Pitfall 3)
    - Test 2: uppercase/short username fails client-side against ^[a-z0-9_-]{3,50}$ before any invoke
    - Test 3: clearance outside 1-4 (e.g. 0, 5, non-integer) fails client-side
    - Test 4: valid submit calls createUser with the full payload (email, username, full_name, role, clearance, user_type 'employee') and navigates to /users on success
    - Test 5: a 400 DUPLICATE_EMAIL error maps to a field-level error on email via form.setError (and DUPLICATE_USERNAME to username) — not a generic toast-only failure
  </behavior>
  <action>
    Build `UserCreatePage` (PascalCase, beside UsersListPage): full-page RHF+Zod form using the NewPositionDialog form-layer pattern (zodResolver, mode 'onTouched', `<Form>`/`<FormField>`/`<FormMessage>` primitives, message strings as i18n keys in the `user-management` namespace via `useTranslation('user-management')`). Schema mirrors the deployed validators from <interfaces>: email (regex), username `^[a-z0-9_-]{3,50}$`, full_name 2-100, role `z.enum(['admin','editor','viewer'])`, clearance `z.number().int().min(1).max(4).optional()`. Hardcode `user_type: 'employee'` in the payload (guest creation = documented follow-up, research Open Question 3). Submit via `createUser()` from Task 1 inside a useMutation invalidating `['users']`; on success toast + `useNavigate()` to `/users`; on DUPLICATE_EMAIL/DUPLICATE_USERNAME set field errors; on other failures localized error toast — never mask or swallow the error body. Note on the page (SUMMARY too): created users are inactive pending activation (Pitfall 7).
    Create `routes/_protected/users/create.tsx` per the engagements/index shape with path `'/_protected/users/create'` mounting UserCreatePage.
    UsersListPage surgical edit ONLY: add a "Create user" primary Button (default variant ≙ .btn-primary recipe) in the page header linking to `/users/create` (typed TanStack Link). Do NOT touch the search `.or(` block, the 4-role filter drift, or restyle the page.
    Styling: Linear tokens only, logical properties (`ms-*`/`ps-*`/`text-start`), `min-h-11` controls, sentence case, no emoji. Test file mocks `@/services/user-management-api` and TanStack's useNavigate; t-mock per PATTERNS §16.
  </action>
  <verify>
    <automated>pnpm --dir frontend test -- run src/pages/users/__tests__/UserCreatePage.test.tsx && pnpm --dir frontend type-check</automated>
  </verify>
  <acceptance_criteria>
    - Targeted vitest exits 0 with all five behavior tests green
    - `rg -n "z.enum\(\['admin', ?'editor', ?'viewer'\]\)" frontend/src/pages/users/UserCreatePage.tsx` matches (role set locked)
    - `! rg -q "manager|staff" frontend/src/pages/users/UserCreatePage.tsx` (no drift propagation)
    - `rg -n "users/create" frontend/src/pages/users/UsersListPage.tsx` matches (button wired); the page's `.or(` search block byte-unchanged
    - `pnpm --dir frontend lint --max-warnings 0` and type-check exit 0
  </acceptance_criteria>
  <done>Admin can navigate list → Create user → validated form → create-user fn → back to list. Live E2E smoke lands in plan 86-04's combined spec.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                    |
| ---------------------------------- | -------------------------------------------------------------- |
| browser → user-management edge fns | Untrusted form input crosses to privileged service-role writes |
| browser → /users route family      | Admin-only surface reachable by any authenticated session      |

## STRIDE Threat Register

| Threat ID | Category               | Component                           | Disposition | Mitigation Plan                                                                                                                                                                             |
| --------- | ---------------------- | ----------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-86-05   | Info disclosure (IDOR) | /users, /users/create routes        | mitigate    | `beforeLoad: requireAdmin` on the converted layout gates the whole family (Task 2 grep gate); every edge fn independently 403s non-admins via `public.users.role` — client guard is UX only |
| T-86-06   | Elevation              | create-user role assignment         | mitigate    | Role picker restricted to admin/editor/viewer (Task 3 grep gates); fn validates requester is admin server-side; no client-side auth.admin calls anywhere                                    |
| T-86-07   | Tampering              | Create form input                   | mitigate    | Zod mirrors the deployed validators exactly (email regex, username regex, full_name 2-100, clearance int 1-4); server re-validates — never trust the form                                   |
| T-86-08   | Elevation              | Metadata-based authz                | mitigate    | No reads/writes of user_metadata/app_metadata in any new code (documented privilege-escalation vector, require-admin.ts header); Task 2 grep gate                                           |
| T-86-09   | Info disclosure        | Wildcard CORS on create-user et al. | accept      | Known debt owned by Phase 90; fns not edited here, CORS not widened                                                                                                                         |
| T-86-SC   | Tampering              | package installs                    | n/a         | No new packages this phase                                                                                                                                                                  |

</threat_model>

<verification>
- `supabase functions list --project-ref zkrcjzdemdmwhearhfgg` shows assign-role ACTIVE (recorded)
- `pnpm --dir frontend test -- run src/pages/users/__tests__/UserCreatePage.test.tsx` green
- `pnpm --dir frontend type-check` + `pnpm --dir frontend lint --max-warnings 0` exit 0
- Route family gated: users.tsx contains both `Outlet` and `beforeLoad: requireAdmin`
</verification>

<success_criteria>
Success criterion 2 (ROADMAP Phase 86): admin can create a user at /users/create against the L1-hardened edge functions and sees the new user in the users list (asserted live in plan 86-04's E2E with the default "all" filter — new users are inactive). D-10 create half closed. Foundation (layout + service methods + verified assign-role) ready for plan 86-04.
</success_criteria>

<output>
Create `.planning/phases/86-feature-completion/86-02-SUMMARY.md` when done.
</output>
