---
phase: 87-linear-affordances
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/empty-states/EmptyState.tsx
  - frontend/src/components/empty-states/ListEmptyState.tsx
  - frontend/src/components/empty-states/__tests__/ListEmptyState.test.tsx
  - frontend/src/i18n/en/empty-states.json
  - frontend/src/i18n/ar/empty-states.json
  - frontend/src/components/dossier/dossier-overview/sections/ActivityTimelineSection.tsx
  - frontend/src/components/dossier/dossier-overview/sections/KeyContactsSection.tsx
  - frontend/src/components/dossier/dossier-overview/sections/CalendarEventsSection.tsx
  - frontend/src/components/dossier/dossier-overview/sections/DocumentsSection.tsx
  - frontend/src/i18n/en/dossier-overview.json
  - frontend/src/i18n/ar/dossier-overview.json
autonomous: true
requirements: [AFF-04]
must_haves:
  truths:
    - 'ListEmptyState supports all 9 core-surface entities (topic, working_group, elected_official, work_item added) with Pattern-A anatomy: icon wash + heading + one-sentence body + one .btn-primary CTA'
    - "ListEmptyState has a filtered-empty branch: 'No matching rows' + ghost 'Clear filters' — never the create CTA when filters caused the empty"
    - 'When no onCreate handler is supplied the CTA is absent entirely — never a disabled accent button'
    - "empty-states.json core-set copy matches the UI-SPEC matrix verbatim (sentence case, no 'we', no Title Case)"
    - 'Dossier-overview section empties conform to Pattern B: single glyph + one line, no accent CTA'
  artifacts:
    - path: 'frontend/src/components/empty-states/ListEmptyState.tsx'
      provides: 'Extended EntityType union + filtered/onClearFilters props'
      contains: 'elected_official'
    - path: 'frontend/src/components/empty-states/EmptyState.tsx'
      provides: 'Linear token reskin (surface-raised wash, ink-mute body, btn-primary/btn-ghost actions)'
    - path: 'frontend/src/components/empty-states/__tests__/ListEmptyState.test.tsx'
      provides: 'Per-entity render + filtered-branch + CTA-gating tests'
    - path: 'frontend/src/i18n/en/empty-states.json'
      provides: 'UI-SPEC §F26 copy matrix (list.<entity>.title/description/cta + list.filtered.*)'
  key_links:
    - from: 'frontend/src/components/empty-states/ListEmptyState.tsx'
      to: 'frontend/src/i18n/en/empty-states.json'
      via: 'entityConfig translationKey → list.<entity>.* keys'
      pattern: "list\\."
---

<objective>
Make the F26 empty-state infrastructure real: reskin EmptyState to Linear tokens, extend ListEmptyState's entity union and add the filtered-empty branch, replace the voice-debt copy with the UI-SPEC matrix in both locales, and align the dossier-overview section empties to Pattern B. Wiring into the 9 surfaces happens in Wave 2 (plans 87-06..87-09) — this plan delivers the component + copy so those plans only pass props.

Purpose: AFF-04 — empty states explain the surface and offer a primary action; no bare "no data" text remains.
Output: reskinned EmptyState, extended ListEmptyState + tests, rewritten empty-states.json (EN+AR), Pattern-B-conformant dossier tab sections.

Note (A3 resolution, documented): no create-permission model exists on any dossier list — all create routes are ungated for authenticated users, so CTAs render whenever the surface has a create target and the caller passes onCreate. The component contract enforces the spec rule mechanically: no onCreate → no CTA rendered (never disabled-accent). RLS remains the authoritative write gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-linear-affordances/87-UI-SPEC.md (§Copywriting F26 per-surface matrix + Pattern B lines, §F26 interaction contract)
@.planning/phases/87-linear-affordances/87-PATTERNS.md (assignment 7)
@frontend/src/components/empty-states/EmptyState.tsx
@frontend/src/components/empty-states/ListEmptyState.tsx
</context>

<interfaces>
From ListEmptyState.tsx:21-34 (the union to extend) and :71-88 (the config map):

```typescript
export type EntityType =
  | 'document'
  | 'dossier'
  | 'engagement'
  | 'commitment'
  | 'organization'
  | 'country'
  | 'forum'
  | 'event'
  | 'task'
  | 'person'
  | 'position'
  | 'mou'
  | 'relationship'
  | 'brief'
  | 'generic'
// ADD: 'topic' | 'working_group' | 'elected_official' | 'work_item'

const entityConfig: Record<
  EntityType,
  { icon: LucideIcon; translationKey: string; suggestionContext: SuggestionContext }
>
```

New props this plan adds (consumed by Wave-2 wiring plans):

```typescript
interface ListEmptyStateProps {
  entityType: EntityType
  onCreate?: () => void // absent → NO CTA rendered (never disabled accent)
  filtered?: boolean // true → 'No matching rows' branch
  onClearFilters?: () => void // ghost recovery action for the filtered branch
  // ...existing props preserved
}
```

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EmptyState Linear reskin + ListEmptyState extension + tests</name>
  <files>frontend/src/components/empty-states/EmptyState.tsx, frontend/src/components/empty-states/ListEmptyState.tsx, frontend/src/components/empty-states/__tests__/ListEmptyState.test.tsx</files>
  <read_first>frontend/src/components/empty-states/EmptyState.tsx (structural skeleton :116-137 stays; only tokens/CTA recipes change), frontend/src/components/empty-states/ListEmptyState.tsx (union :21-34, entityConfig :71-88), 87-UI-SPEC.md §F26 interaction contract (Pattern A anatomy row + filtered-empty row + CTA permission row), 87-PATTERNS.md assignment 7 (exact token swap table)</read_first>
  <behavior>
    - render entityType="topic" → heading from empty-states:list.topic.title, body from list.topic.description, no crash (same for working_group, elected_official, work_item)
    - onCreate provided → exactly one button carrying the .btn-primary recipe with list.<entity>.cta label; onCreate absent → zero accent buttons in the DOM (no disabled button either)
    - filtered=true → renders list.filtered.title ('No matching rows') + a ghost 'Clear filters' button wired to onClearFilters; the create CTA is NOT rendered even when onCreate is provided
    - icon renders inside a rounded-full wash element using surface-raised (assert via class)
    - existing consumers (AdvancedSearchPage, DossierListPage use EmptyState directly) keep compiling — EmptyState public prop API unchanged
  </behavior>
  <action>Write ListEmptyState.test.tsx FIRST per the behavior list. EmptyState.tsx reskin (structure unchanged): `bg-muted` → `bg-surface-raised` (icon wash), `text-muted-foreground` → `text-ink-mute` (body) / `text-ink-faint` (icon), `text-foreground` → `text-ink`; primary action renders the `.btn-primary` recipe and secondary/recovery actions `.btn-ghost` (replace the ui/button variant chrome per PATTERNS assignment 7 — keep the existing action-prop API so out-of-scope consumers compile); no shadow, no gradient; radius via var(--radius-lg) where card chrome exists. ListEmptyState.tsx: extend EntityType with 'topic' | 'working_group' | 'elected_official' | 'work_item'; add entityConfig entries (lucide icons: e.g. Tag/BookOpen for topic, Users for working_group, Landmark for elected_official, ListTodo for work_item — pick non-directional icons); add the `filtered`/`onClearFilters`/`onCreate` props per the interfaces block with the filtered branch precedence rule (filtered wins over create). Keep suggestionContext values consistent with existing entries.</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run src/components/empty-states && pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - ListEmptyState.test.tsx exits 0 covering all 5 behavior lines
    - `grep -n "bg-muted\|text-muted-foreground\|text-foreground" frontend/src/components/empty-states/EmptyState.tsx` returns no matches
    - `grep -c "topic\|working_group\|elected_official\|work_item" frontend/src/components/empty-states/ListEmptyState.tsx` >= 8 (union + config entries)
    - Existing TourableEmptyState.test.tsx still exits 0
  </acceptance_criteria>
  <done>ListEmptyState renders all 13+4 entities with Pattern-A anatomy, filtered branch, and mechanical CTA gating; suite green</done>
</task>

<task type="auto">
  <name>Task 2: empty-states.json copy matrix rewrite (EN + AR)</name>
  <files>frontend/src/i18n/en/empty-states.json, frontend/src/i18n/ar/empty-states.json</files>
  <read_first>87-UI-SPEC.md §Copywriting F26 (the 10-row per-surface matrix — copy is LOCKED, transcribe verbatim), frontend/src/i18n/en/empty-states.json (full — the Title Case + 'we' debt to purge on core-set keys)</read_first>
  <action>Add/rewrite `list.<entity>.{title,description,cta}` for the 9 core surfaces using the UI-SPEC matrix EXACTLY: country ("No country dossiers" / "Country dossiers track diplomatic relations, engagements, and commitments per state." / "Add country"), organization, forum, engagement ("Log engagement"), topic, working_group, person, elected_official, work_item ("No work items" / "Work items collect tasks, commitments, and intake requests across dossiers." / "Create work item"). Add `list.filtered.{title,description,clear}` = "No matching rows" / "No rows match the current filters." / "Clear filters". De-"we" and sentence-case every key this pass touches: ":10 We couldn't find anything matching your search criteria." and ":18 We encountered an error while searching." get rewritten without first-person plural; core-set "Add Your First X" / "Get Started" / "Start Your Search" / "No Results Found" patterns are replaced by the matrix copy. AR mirrors every changed/added key with real translations (same sentence-case discipline, Latin digits, no marketing voice). Do NOT touch out-of-scope namespaces or non-core keys beyond the cited voice violations. Keep EN/AR key sets identical.</action>
  <verify>
    <automated>pnpm --dir frontend lint --max-warnings 0 2>&1 | tail -3 && pnpm --dir frontend exec vitest run src/components/empty-states</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "We couldn\|We encountered" frontend/src/i18n/en/empty-states.json` returns no matches
    - `grep -c "No country dossiers\|No organization dossiers\|No matching rows\|Create work item" frontend/src/i18n/en/empty-states.json` == 4
    - `node -e "const en=require('./frontend/src/i18n/en/empty-states.json'),ar=require('./frontend/src/i18n/ar/empty-states.json');const f=(o,p='')=>Object.entries(o).flatMap(([k,v])=>typeof v==='object'?f(v,p+k+'.'):p+k);const a=f(en).sort().join(),b=f(ar).sort().join();process.exit(a===b?0:1)"` exits 0
    - ListEmptyState tests still green against the new keys
  </acceptance_criteria>
  <done>Matrix copy live in both locales, voice debt purged on touched keys, key parity proven</done>
</task>

<task type="auto">
  <name>Task 3: Pattern-B audit of dossier-overview section empties</name>
  <files>frontend/src/components/dossier/dossier-overview/sections/ActivityTimelineSection.tsx, frontend/src/components/dossier/dossier-overview/sections/KeyContactsSection.tsx, frontend/src/components/dossier/dossier-overview/sections/CalendarEventsSection.tsx, frontend/src/components/dossier/dossier-overview/sections/DocumentsSection.tsx, frontend/src/i18n/en/dossier-overview.json, frontend/src/i18n/ar/dossier-overview.json</files>
  <read_first>All four section files (their existing empty branches — ActivityTimelineSection.tsx:254-256, KeyContactsSection.tsx:184-186, CalendarEventsSection.tsx:195, DocumentsSection.tsx:131), 87-UI-SPEC.md Pattern B contract (single glyph 16-20px text-ink-faint + one 13px text-ink-mute line, no accent, no card chrome) + the six Pattern-B one-liner copy examples</read_first>
  <action>Audit each section's empty branch against Pattern B and fix only violations: structure = one non-directional lucide glyph (16-20px, text-ink-faint) + ONE line (13px, text-ink-mute); remove any accent CTA, extra heading tier, or card chrome inside these empties (ghost actions may stay); copy = sentence case, no "we", no Title Case, one-liner style matching the UI-SPEC examples ("No engagements recorded for this dossier." register) — update dossier-overview.json EN+AR keys where the current copy violates voice rules, keeping key names stable so no other consumer breaks. These are settled-empty branches — do not alter each section's loading/error branches. This is an audit-and-align task: where a section already conforms, change nothing (surgical-changes rule).</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run src/components/dossier/dossier-overview 2>/dev/null || pnpm --dir frontend exec vitest run src/components/dossier && pnpm --dir frontend lint --max-warnings 0 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 4 sections' empty branch renders exactly one glyph + one text line (source inspection: no .btn-primary and no accent token inside the empty branch of any of the 4 files)
    - `grep -in "we couldn\|we encountered\|get started" frontend/src/i18n/en/dossier-overview.json` returns no matches
    - EN/AR dossier-overview key parity holds (lint exits 0)
    - Any existing dossier-overview section tests still pass
  </acceptance_criteria>
  <done>All four tab-section empties are Pattern-B conformant; unchanged sections untouched</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary       | Description                                                          |
| -------------- | -------------------------------------------------------------------- |
| props → render | Empty states render only static i18n copy + caller-supplied handlers |

## STRIDE Threat Register

| Threat ID | Category               | Component                | Disposition | Mitigation Plan                                                                                                                                                                                                 |
| --------- | ---------------------- | ------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-87-10   | Elevation of privilege | onCreate CTA             | mitigate    | Component renders NO CTA without an onCreate handler (never disabled-accent); Wave-2 callers pass onCreate only where a create route exists; RLS remains the write gate (A3 resolution documented in objective) |
| T-87-11   | Tampering (XSS)        | title/description render | mitigate    | React JSX escaping only; all copy from static i18n JSON; no dangerouslySetInnerHTML                                                                                                                             |
| T-87-SC   | Tampering              | package installs         | accept      | No package installs this phase                                                                                                                                                                                  |

</threat_model>

<verification>
- `pnpm --dir frontend exec vitest run src/components/empty-states` exits 0
- `pnpm --dir frontend lint` exits 0 (token gates + EN/AR parity)
- `pnpm --dir frontend exec tsc --noEmit` exits 0
</verification>

<success_criteria>

- Wave-2 plans can wire any core surface with `<ListEmptyState entityType=... onCreate=... filtered=... onClearFilters=.../>` and get spec-conformant Pattern A + filtered-empty behavior
- Copy matrix is verbatim UI-SPEC in EN with AR parity
- Pattern-B tab empties conform without structural churn
  </success_criteria>

<output>
Create `.planning/phases/87-linear-affordances/87-04-SUMMARY.md` when done
</output>
