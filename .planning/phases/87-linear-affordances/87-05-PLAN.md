---
phase: 87-linear-affordances
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/_protected/dossiers/persons/index.tsx
  - frontend/src/routes/_protected/dossiers/persons/-PersonsListPage.tsx
  - frontend/src/routes/_protected/dossiers/engagements/index.tsx
  - frontend/src/pages/engagements/EngagementsListPage.tsx
  - frontend/src/routes/_protected/dossiers/elected-officials/index.tsx
  - frontend/src/components/elected-officials/ElectedOfficialListTable.tsx
autonomous: true
requirements: [AFF-01, AFF-02]
must_haves:
  truths:
    - 'Persons list search state lives in URL params (?search=, ?page=) — refresh and share restore the same view'
    - 'Engagements list search + type filter live in URL params — the FilterPill selection survives refresh'
    - 'Elected-officials list filters live in URL params and the page validates them via validateSearch'
    - 'All three surfaces behave identically to before for the user (pure state-relocation — no feature change)'
  artifacts:
    - path: 'frontend/src/routes/_protected/dossiers/persons/index.tsx'
      provides: 'Live validateSearch (currently a dead shell) consumed by the page'
    - path: 'frontend/src/pages/engagements/EngagementsListPage.tsx'
      provides: 'URL-driven search + type filter (local useState removed)'
    - path: 'frontend/src/routes/_protected/dossiers/elected-officials/index.tsx'
      provides: 'validateSearch + URL-driven filters'
  key_links:
    - from: 'frontend/src/routes/_protected/dossiers/persons/-PersonsListPage.tsx'
      to: 'frontend/src/routes/_protected/dossiers/persons/index.tsx'
      via: 'Route.useSearch/useNavigate replacing local useState'
      pattern: 'useSearch|useNavigate'
    - from: 'frontend/src/pages/engagements/EngagementsListPage.tsx'
      to: 'frontend/src/routes/_protected/dossiers/engagements/index.tsx'
      via: 'search params passed from route'
      pattern: 'useSearch|validateSearch'
---

<objective>
Normalize the three bespoke non-kanban surfaces onto the countries URL-state idiom: persons, engagements, and elected-officials lift their local `useState` filter/search state into validated URL search params. This is the enabler slice RESEARCH mandates before F23 peek and F24 popovers can hook in uniformly — plan 87-08 wires the features on top of these seams. Kanban's normalization lives in 87-09 (kept with the rest of the kanban work for cohesion).

Purpose: AFF-01/AFF-02 enabler — peek registration and filter/display persistence both require URL-driven, route-validated state on every core surface.
Output: three surfaces on the `validateSearch` + navigate-reducer idiom, zero user-visible behavior change.

Scope discipline: this plan does NOT add popovers, peek, or empty states — pure state relocation, verified by unchanged behavior. Elected-officials decision (PATTERNS assignment 5 planner call): KEEP the existing PageHeader + ElectedOfficialListTable structure and do NOT migrate onto ListPageShell — a parallel toolbar row lands in 87-08 for the F24 triggers. Shell migration is churn without user-visible gain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-linear-affordances/87-PATTERNS.md (assignment 5 — the countries idiom all three surfaces copy)
@frontend/src/routes/_protected/dossiers/countries/index.tsx
@frontend/src/routes/_protected/dossiers/persons/-PersonsListPage.tsx
@frontend/src/pages/engagements/EngagementsListPage.tsx
</context>

<interfaces>
The idiom to replicate (countries/index.tsx:15-27 + :33-45):

```typescript
interface DossierListSearch {
  page: number
  search?: string
}
export const Route = createFileRoute('/_protected/dossiers/countries/')({
  component: CountriesListRoute,
  validateSearch: (search: Record<string, unknown>): DossierListSearch => ({
    page: Math.max(1, Number(search.page) || 1),
    search:
      typeof search.search === 'string' && search.search.length > 0 ? search.search : undefined,
  }),
})
// writes: void navigate({ search: (prev) => ({ ...prev, search: next || undefined, page: 1 }), replace: true })
```

Reserved parent params never reused: `dossier`, `dossierType`, `commitment`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Persons — activate the dead validateSearch and lift search state</name>
  <files>frontend/src/routes/_protected/dossiers/persons/index.tsx, frontend/src/routes/_protected/dossiers/persons/-PersonsListPage.tsx</files>
  <read_first>frontend/src/routes/_protected/dossiers/persons/index.tsx (the dead validateSearch shell at :11 — activate, don't duplicate), frontend/src/routes/_protected/dossiers/persons/-PersonsListPage.tsx (local useState + useDebouncedValue at :68-70 to replace), frontend/src/routes/_protected/dossiers/countries/index.tsx (the idiom), frontend/src/routes/_protected/dossiers/persons/__tests__/ (existing route tests must stay green)</read_first>
  <action>Wire the route's validateSearch to the countries DossierListSearch shape ({page, search}) and make -PersonsListPage consume `Route.useSearch()` instead of its local useState: the search input writes through a navigate reducer (replace: true, page reset to 1) while KEEPING the existing useDebouncedValue behavior (debounce the URL write, not just the query — one history-visible state, no keystroke spam). usePersons receives the same derived values it does today (search + any offset/limit it already passes) — do not change the data hook or add pagination UI this plan; if the page currently ignores `page`, keep ignoring it in rendering but preserve the param round-trip so 87-08 can use it. Existing route tests in __tests__/ must pass unchanged or with mechanical search-prop updates only.</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run src/routes/_protected/dossiers/persons && pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "useState" frontend/src/routes/_protected/dossiers/persons/-PersonsListPage.tsx` shows no search/filter useState (unrelated UI state may remain)
    - validateSearch in persons/index.tsx validates page (>=1 number) and search (non-empty string) with undefined fallbacks
    - Existing persons route tests exit 0
  </acceptance_criteria>
  <done>Persons search survives refresh via URL; behavior otherwise identical; tests green</done>
</task>

<task type="auto">
  <name>Task 2: Engagements — lift search + type filter to URL</name>
  <files>frontend/src/routes/_protected/dossiers/engagements/index.tsx, frontend/src/pages/engagements/EngagementsListPage.tsx</files>
  <read_first>frontend/src/pages/engagements/EngagementsListPage.tsx (local useState search + filter at :95-99; the FilterPill type filter whose value set defines the `type` whitelist), frontend/src/routes/_protected/dossiers/engagements/index.tsx (route shell gaining validateSearch), frontend/src/routes/_protected/dossiers/countries/index.tsx (idiom)</read_first>
  <action>Add validateSearch to engagements/index.tsx: `{ search?: string; type?: EngagementTypeValue }` where `type` is whitelist-validated against the exact value set the existing FilterPill filter uses (read the component to enumerate — any other value → undefined). EngagementsListPage swaps its local useState for Route.useSearch + navigate reducers (replace: true). The FilterPill row keeps rendering and functioning exactly as today, just URL-backed (87-08 later folds it into the Filter popover). The infinite query (useEngagementsInfinite) receives the same inputs it does today. No `page` param — the list is infinite-scroll/load-more (page semantics do not apply).</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run src/pages/engagements 2>/dev/null; pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "useState" frontend/src/pages/engagements/EngagementsListPage.tsx` shows no search/type-filter useState remaining
    - engagements/index.tsx validateSearch whitelists `type` against the FilterPill value set (source-visible enumeration, not a bare typeof check)
    - Selecting a type pill updates the URL (?type=...) and refresh restores the pill selection (verified by the route/page test if one exists, else by tsc + manual note in summary)
  </acceptance_criteria>
  <done>Engagements search + type filter are URL state; pills behave identically</done>
</task>

<task type="auto">
  <name>Task 3: Elected officials — validateSearch + URL-driven filters</name>
  <files>frontend/src/routes/_protected/dossiers/elected-officials/index.tsx, frontend/src/components/elected-officials/ElectedOfficialListTable.tsx</files>
  <read_first>frontend/src/routes/_protected/dossiers/elected-officials/index.tsx (local useState filters at :61-66; ignored validateSearch), frontend/src/components/elected-officials/ElectedOfficialListTable.tsx (internal filter/pagination state at :334-349 and the empty branch at :162-167 — do NOT touch the empty branch this plan), frontend/src/routes/_protected/dossiers/countries/index.tsx (idiom)</read_first>
  <action>Enumerate the existing EO filter dimensions from the route's local useState (:61-66) and define a whitelist validateSearch for them plus `search` and `page` (the table's client-side currentPage lifts to the URL so peek position math in 87-08 has a stable source). Replace the route-level useState with Route.useSearch + navigate reducers (replace: true, page reset on filter change); thread values into ElectedOfficialListTable via props — the table becomes controlled for filter/page state it previously owned (keep its internal sorting/rendering logic untouched). KEEP the PageHeader structure (no ListPageShell migration — planner decision in objective). Behavior byte-identical for the user.</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run src/components/elected-officials 2>/dev/null; pnpm --dir frontend exec tsc --noEmit && pnpm --dir frontend lint --max-warnings 0 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - elected-officials/index.tsx has a validateSearch whitelisting every filter key it previously held in useState, plus page/search
    - `grep -n "useState" frontend/src/routes/_protected/dossiers/elected-officials/index.tsx` shows no filter useState remaining
    - ElectedOfficialListTable receives filter/page via props (its signature shows the new controlled props)
    - tsc + lint exit 0; any existing EO tests green
  </acceptance_criteria>
  <done>EO filters/pagination are URL state; table controlled; no visual/behavioral change</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary          | Description                                                      |
| ----------------- | ---------------------------------------------------------------- |
| URL → route state | New `search`/`type`/`page`/EO-filter params enter via deep links |

## STRIDE Threat Register

| Threat ID | Category  | Component                       | Disposition | Mitigation Plan                                                                                                                                                                       |
| --------- | --------- | ------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-87-12   | Tampering | three new validateSearch blocks | mitigate    | Whitelist validation per param (typed guards + enumerated value sets; unknown → undefined) — precedent \_protected.tsx:38-44                                                          |
| T-87-13   | Injection | search strings → data hooks     | mitigate    | Hooks unchanged this plan; existing `.or(ilike)` builder + SEARCH_MAX_LEN slice patterns (useCountries.ts:45,55-66) remain the only transport — no string-concatenated SQL introduced |
| T-87-SC   | Tampering | package installs                | accept      | No package installs this phase                                                                                                                                                        |

</threat_model>

<verification>
- `pnpm --dir frontend exec vitest run src/routes/_protected/dossiers/persons src/components/elected-officials src/pages/engagements` exits 0 (existing suites; skip dirs with no tests)
- `pnpm --dir frontend exec tsc --noEmit` exits 0
- `pnpm --dir frontend lint` exits 0
</verification>

<success_criteria>

- All three surfaces read/write filter/search state exclusively through validated URL params
- Zero user-visible behavior change (pure relocation)
- 87-08 can wire peek + popovers against the same idiom the 5 uniform lists use
  </success_criteria>

<output>
Create `.planning/phases/87-linear-affordances/87-05-SUMMARY.md` when done
</output>
