---
phase: 87-linear-affordances
plan: 09
type: execute
wave: 2
depends_on: [87-01, 87-02, 87-04]
files_modified:
  - frontend/src/routes/_protected/kanban.tsx
  - frontend/src/pages/WorkBoard/WorkBoard.tsx
  - frontend/src/pages/WorkBoard/BoardToolbar.tsx
  - frontend/src/pages/WorkBoard/BoardColumn.tsx
  - frontend/src/components/commitments/CommitmentDrawer.tsx
  - frontend/src/i18n/en/unified-kanban.json
  - frontend/src/i18n/ar/unified-kanban.json
autonomous: true
requirements: [AFF-01, AFF-02, AFF-04]
must_haves:
  truths:
    - 'Kanban group-by mode and search live in URL params; the old toggle-pill row is gone — Display popover owns grouping (working options only) and sorting'
    - 'Kanban Filter popover filters by source and priority with client-side live counts from the full dataset'
    - "Clicking a commitment card opens CommitmentDrawer as a peek with an 'n / total' counter and prev/next chevrons over the visible commitment cards; task/intake cards keep their navigation (spec-conformant narrowing)"
    - "An all-empty board shows the rich 'No work items' state with a Create work item CTA (openPalette); filtered-empty shows 'No matching rows' + Clear filters; per-column empties stay one-line Pattern B"
  artifacts:
    - path: 'frontend/src/pages/WorkBoard/BoardToolbar.tsx'
      provides: 'Filter + Display popovers replacing the pill rows'
    - path: 'frontend/src/components/commitments/CommitmentDrawer.tsx'
      provides: 'Peek counter + chevrons via usePeekPaging (param-agnostic reuse)'
    - path: 'frontend/src/routes/_protected/kanban.tsx'
      provides: 'validateSearch for group/search/source/priority'
  key_links:
    - from: 'frontend/src/pages/WorkBoard/WorkBoard.tsx'
      to: 'frontend/src/store/peekStore.ts'
      via: 'register visible commitment ids on commitment-card click'
      pattern: 'usePeekStore|register'
    - from: 'frontend/src/components/commitments/CommitmentDrawer.tsx'
      to: 'frontend/src/hooks/usePeekPaging.ts'
      via: 'usePeekPaging(currentCommitmentId, navigate-to-commitment callback)'
      pattern: 'usePeekPaging'
---

<objective>
Complete the ninth core surface: normalize kanban's local state to URL, replace its toolbar pills with the Filter/Display popovers (folding the group-by toggle per the UI-SPEC — never both), and wire the work-item peek for commitment cards through CommitmentDrawer with the same counter pattern, plus the board's empty states.

Purpose: AFF-01, AFF-02, AFF-04 on surface 9.
Output: kanban fully wired; CommitmentDrawer peek-capable.

Resolved open question (RESEARCH Open Q2, per recommendation): kanban peek scope = COMMITMENT cards only, via the existing CommitmentDrawer (`?commitment=` param, mounted \_protected.tsx:86). Task and intake cards keep their existing navigation — no task/intake drawer infra exists and building one violates "extend, don't invent". This is a documented spec-conformant narrowing of UI-SPEC §F23's work-item row. Bonus fix: commitment cards currently route to the /commitments LIST (WorkBoard.tsx:242-244) — the peek strictly improves this.

Per-surface config: grouping = ONLY 'status' (By dossier/By owner are deliberate D-06 dead stubs — do NOT render them as options, Pitfall 6); sort = the existing unified-kanban sorting options; filters = `source` (task/commitment/intake) + `priority` (low/medium/high/urgent) counted client-side via Array.filter over the full dataset (kanban holds all items client-side — the one surface where client counting is truthful).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-linear-affordances/87-UI-SPEC.md (§F23 work-item row, §F24 Display fold-in, §F26 kanban rows)
@.planning/phases/87-linear-affordances/87-PATTERNS.md (assignment 4 — BoardToolbar aria-pressed semantics)
@frontend/src/pages/WorkBoard/WorkBoard.tsx
@frontend/src/pages/WorkBoard/BoardToolbar.tsx
@frontend/src/hooks/useCommitmentDrawer.ts
@frontend/src/store/peekStore.ts
</context>

<interfaces>
From 87-01: `usePeekPaging(currentId, navigateToId)` is param-agnostic BY DESIGN for this plan — pass a callback that swaps the `?commitment=` param with replace:true (mirror useCommitmentDrawer's open reducer with replace flipped). `usePeekStore.register({ids, type:'commitment', total, pageOffset:0, pageSize: ids.length})` — full client dataset, no fetchPage.
From 87-02: popover components + useListControls (kanban search/group/source/priority params).
From 87-04: `<ListEmptyState entityType="work_item" onCreate={() => openPalette()} filtered onClearFilters/>` — CTA uses the existing work-creation palette (WorkBoard.tsx:260 precedent), available to all users.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Kanban URL normalization (group + search + filters)</name>
  <files>frontend/src/routes/_protected/kanban.tsx, frontend/src/pages/WorkBoard/WorkBoard.tsx</files>
  <read_first>frontend/src/pages/WorkBoard/WorkBoard.tsx (local useState mode+search :114-115, client-side search filter :119-137, card routing :236-251), frontend/src/routes/_protected/kanban.tsx (route shell), frontend/src/routes/_protected/dossiers/countries/index.tsx (validateSearch idiom)</read_first>
  <action>Add validateSearch to kanban.tsx: `{ group?: 'status'; search?: string; source?: WorkSource; priority?: Priority }` — group whitelists exactly 'status' (the only wired columnMode; future modes extend the whitelist when they become real), source whitelists task/commitment/intake, priority whitelists low/medium/high/urgent (work-item glossary — NOT 'critical'). WorkBoard swaps its useState for Route.useSearch + navigate reducers (replace: true). The client-side visibleItems filter chain extends to apply source + priority after search. Zero visual change this task — pills still render (Task 2 replaces them).</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run src/pages/WorkBoard 2>/dev/null; pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "useState" frontend/src/pages/WorkBoard/WorkBoard.tsx` shows no mode/search state remaining (DnD/UI state may remain)
    - kanban.tsx validateSearch whitelists group/source/priority against exact enumerated sets (source-visible literals)
    - Refresh restores mode + search + filters from the URL
  </acceptance_criteria>
  <done>Kanban state is URL state; behavior unchanged</done>
</task>

<task type="auto">
  <name>Task 2: BoardToolbar → Filter + Display popovers (fold-in)</name>
  <files>frontend/src/pages/WorkBoard/BoardToolbar.tsx, frontend/src/pages/WorkBoard/WorkBoard.tsx, frontend/src/i18n/en/unified-kanban.json, frontend/src/i18n/ar/unified-kanban.json</files>
  <read_first>frontend/src/pages/WorkBoard/BoardToolbar.tsx (pill group :44-84 — the aria-pressed semantics migrate into the Display popover's Group-by rows; the aria-disabled 'Coming soon' stubs at :67-84 are NOT carried over), 87-UI-SPEC.md §Copywriting F24 (display.grouping reuses the kanban 'Group by' wording; the standalone toggle FOLDS INTO Display, not duplicated), frontend/src/components/list-controls/DisplayPopover.tsx (config contract)</read_first>
  <action>Replace BoardToolbar's columnMode pill group with the two popover triggers: Filter (source + priority options, live counts computed via Array.filter over the full item list passed as a prop — no Supabase queries, this surface is client-truthful; counts recompute against the OTHER active filter per the facet contract) and Display (grouping = [{id:'status'}] only + the existing sort options from unified-kanban's sorting section + NO properties section — kanban cards have no toggleable columns, section omitted per the <2 rule). Keep the search input and the LtrIsolate overdue chip exactly as-is. FilterChipsRow renders beneath the toolbar with 'Showing N of M' (N = visibleItems.length, M = all items). Remove now-dead pill i18n keys from unified-kanban.json en+ar ONLY if unreferenced after the swap (grep first; keep columnModes.label — Display reuses the wording per UI-SPEC). D-06 stub pills die with the pill row — the Display popover renders no dead options.</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run src/pages/WorkBoard 2>/dev/null; pnpm --dir frontend exec tsc --noEmit && pnpm --dir frontend lint --max-warnings 0 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "filter-pill" frontend/src/pages/WorkBoard/BoardToolbar.tsx` returns no matches (pill row gone)
    - `grep -n "aria-disabled" frontend/src/pages/WorkBoard/BoardToolbar.tsx` returns no matches (no dead stub options carried over)
    - Group-by renders exactly one option ('status'); Filter popover renders source + priority with numeric counts
    - en/ar unified-kanban key parity holds (lint exits 0)
  </acceptance_criteria>
  <done>One grouping control (Display popover), truthful client-side filter counts, chips row live</done>
</task>

<task type="auto">
  <name>Task 3: Commitment-card peek + board empty states</name>
  <files>frontend/src/pages/WorkBoard/WorkBoard.tsx, frontend/src/pages/WorkBoard/BoardColumn.tsx, frontend/src/components/commitments/CommitmentDrawer.tsx</files>
  <read_first>frontend/src/pages/WorkBoard/WorkBoard.tsx (card routing :236-251 — commitment branch currently goes to the /commitments LIST), frontend/src/hooks/useCommitmentDrawer.ts (the ?commitment= param reducer to mirror with replace:true), frontend/src/components/commitments/CommitmentDrawer.tsx (head area for the counter — find its close-button flex, same insertion logic as DrawerHead; identify its i18n namespace for the prev/next/counter keys), frontend/src/components/dossier/DossierDrawer/DrawerHead.tsx (post-87-01 — the counter + chevron implementation to mirror), frontend/src/pages/WorkBoard/BoardColumn.tsx (col-empty :175-177)</read_first>
  <action>WorkBoard commitment-card click: register({ids: the visible commitment cards' ids in board render order (filtered by current search/filters), type:'commitment', total: that count, pageOffset:0, pageSize: count}) then open the commitment drawer via useCommitmentDrawer — task/intake branches untouched. CommitmentDrawer: add the counter + chevrons mirroring DrawerHead's post-87-01 implementation — usePeekPaging(currentCommitmentId, callback that swaps ?commitment= with replace:true), inline dir="ltr" font-mono counter span, two plain btn-ghost chevron buttons with aria-labels + testids, boundary disable, render ONLY when the registry holds the current id (drawer opened from elsewhere → no counter); add the peek.counter/prev/next keys to CommitmentDrawer's own namespace en+ar (mirror dossier-drawer:peek.* values; do not cross-reference namespaces). Empty states: board-level all-empty (zero items unfiltered) renders `<ListEmptyState entityType="work_item" onCreate={() => openPalette()} />` centered in the board area; filtered-to-zero renders the filtered branch with Clear filters wired to the F24 clear-all; per-column 'No items' one-liners stay as Pattern-B (align copy/tokens only if non-conformant). Respect the settled-empty rule (no flash while loading).</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run src/pages/WorkBoard src/components/commitments 2>/dev/null; pnpm --dir frontend exec tsc --noEmit && pnpm --dir frontend lint --max-warnings 0 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - WorkBoard commitment branch no longer navigates to '/commitments' (grep the routing switch — the list-navigate is gone) and instead opens the commitment drawer with a registered peek
    - CommitmentDrawer renders counter + chevrons only when registered (mirrors DrawerHead tests' pattern — add equivalent cases if a CommitmentDrawer suite exists, else cover via a new small test)
    - `grep -n "ListEmptyState" frontend/src/pages/WorkBoard/WorkBoard.tsx` returns >= 1 match (board-level empty)
    - en/ar parity for the new commitment-drawer keys (lint exits 0)
  </acceptance_criteria>
  <done>Commitment cards peek with counter; task/intake unchanged; board empties rich + filtered-aware</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary          | Description                                      |
| ----------------- | ------------------------------------------------ |
| URL → board state | group/search/source/priority params              |
| URL → drawer      | ?commitment= (existing whitelisted layout param) |

## STRIDE Threat Register

| Threat ID | Category               | Component                  | Disposition | Mitigation Plan                                                                                    |
| --------- | ---------------------- | -------------------------- | ----------- | -------------------------------------------------------------------------------------------------- |
| T-87-23   | Tampering              | kanban validateSearch      | mitigate    | Exact-literal whitelists for group/source/priority; unknown → undefined                            |
| T-87-24   | Information disclosure | client-side counts         | accept      | Counts derive from the dataset the caller's RLS already returned — no new data access              |
| T-87-25   | Tampering              | commitment peek navigation | mitigate    | Only writes the already-whitelisted `commitment` param; ids come from the RLS-scoped board dataset |
| T-87-SC   | Tampering              | package installs           | accept      | No package installs this phase                                                                     |

</threat_model>

<verification>
- `pnpm --dir frontend exec vitest run src/pages/WorkBoard src/components/commitments` exits 0 (suites that exist)
- `pnpm --dir frontend exec tsc --noEmit` and `pnpm --dir frontend lint` exit 0
</verification>

<success_criteria>

- Kanban delivers all three affordances with truthful client-side counts and a single grouping control
- Commitment peek strictly improves the previous list-navigate behavior; narrowing documented in the summary
  </success_criteria>

<output>
Create `.planning/phases/87-linear-affordances/87-09-SUMMARY.md` when done
</output>
