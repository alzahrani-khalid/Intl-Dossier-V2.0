---
phase: 87-linear-affordances
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/keyboard-shortcuts/CommandPalette.tsx
  - frontend/src/components/keyboard-shortcuts/__tests__/CommandPalette.audit.test.tsx
  - frontend/src/i18n/en/keyboard-shortcuts.json
  - frontend/src/i18n/ar/keyboard-shortcuts.json
  - frontend/src/pages/MoUs/MousPage.tsx
  - frontend/src/routes/_protected/mous.tsx
autonomous: true
requirements: [AFF-03]
must_haves:
  truths:
    - 'Every command rendered by the ⌘K palette navigates or executes a real action — zero dead/half-dead targets remain'
    - "'Create MoU', 'Create user', 'Create elected official', 'Toggle theme', and 'Switch language' commands exist with EN+AR labels"
    - 'Command labels are sentence case in both the JSON keys AND the inline t() fallbacks'
    - '/mous?action=create opens the CreateMouDialog (the page consumes the param the palette advertises)'
  artifacts:
    - path: 'frontend/src/components/keyboard-shortcuts/__tests__/CommandPalette.audit.test.tsx'
      provides: 'Wave-0 regression lock: table-driven assertions that dead targets stay fixed'
    - path: 'frontend/src/pages/MoUs/MousPage.tsx'
      provides: '?action=create consumption opening CreateMouDialog'
    - path: 'frontend/src/i18n/en/keyboard-shortcuts.json'
      provides: 'Sentence-cased labels + 5 new command keys'
  key_links:
    - from: 'frontend/src/components/keyboard-shortcuts/CommandPalette.tsx'
      to: 'frontend/src/components/work-creation/WorkCreationProvider.tsx'
      via: 'useWorkCreation().openPalette re-target for create-task/intake/commitment (palette mounts inside the provider at __root.tsx:59-63)'
      pattern: 'useWorkCreation|openPalette'
    - from: 'frontend/src/routes/_protected/mous.tsx'
      to: 'frontend/src/pages/MoUs/MousPage.tsx'
      via: 'validateSearch action param → createOpen state'
      pattern: 'action.*create'
---

<objective>
Execute the F25 command-menu audit: fix or remove the 9 verified dead/broken findings, add the 5 high-value commands, sentence-case the entire keyboard-shortcuts copy surface (EN+AR + inline fallbacks), give MousPage the `?action=create` seam, and regression-lock the audit with a table-driven test.

Purpose: AFF-03 — every advertised ⌘K command works, missing high-value commands added, EN+AR correct.
Output: audited CommandPalette, 5 new commands, sentence-cased i18n, MousPage action param, CommandPalette.audit.test.tsx.

Resolved open question (RESEARCH Open Q4, per recommendation): MousPage consumes `?action=create` (the palette's Create MoU command targets it). The other four half-dead create commands are re-targeted at surfaces that already have create UIs — the unified work-creation palette (`useWorkCreation().openPalette`) for task/intake/commitment; `create-position` is REMOVED (verified this session: no positions create route or dialog exists anywhere — positions are created from dossier context only). Every command the menu advertises resolves to a working target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-linear-affordances/87-UI-SPEC.md (§Copywriting F25, §F25 interaction contract)
@.planning/phases/87-linear-affordances/87-RESEARCH.md (§F25 audit findings — the 9-row dead/broken table with file:line evidence)
@frontend/src/components/keyboard-shortcuts/CommandPalette.tsx
@frontend/src/components/work-creation/WorkCreationProvider.tsx
</context>

<interfaces>
Command entry contract (CommandPalette.tsx:617-641 — clone for new commands):

```typescript
{ id: 'create-dossier', label: t('createActions.newDossier', 'Create New Dossier'),
  icon: FolderPlus, action: () => navigateTo('/dossiers/create'),
  shortcut: formatShortcut('d', isMac ? ['meta','shift'] : ['ctrl','shift']), category: 'create' }
```

`navigateTo` (:605-614) navigates then closes the palette. Non-navigation commands must call `closeCommandPalette()` themselves inside `action`.

Seams for the new commands (all verified this session):

- Create MoU → `/mous?action=create` (this plan adds the consumer)
- Create user → `/users/create` — the `/users` layout route already enforces `beforeLoad: requireAdmin` (lib/auth/require-admin.ts reads `public.users.role`, accepts exactly 'admin'). Route through this existing gate; do NOT build a new role hook (A2 resolution: gate-routing is sanctioned; server RLS stays authoritative)
- Create elected official → `/dossiers/elected-officials/create` (route exists; i18n key createActions.newElectedOfficial already exists)
- Toggle theme → `useMode()` from design-system/hooks/useMode.ts; `setMode(mode === 'dark' ? 'light' : 'dark')` (Topbar.tsx:161-165 precedent)
- Switch language → `switchLanguage` exported from i18n/index.ts:586 (persists under id.locale)
- create-task/intake/commitment re-targets → `useWorkCreation().openPalette('task' | ...)` from components/work-creation (CommandPalette renders INSIDE WorkCreationProvider — \_\_root.tsx:56-63 verified)
  </interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix/remove the 9 audit findings + MousPage action=create seam</name>
  <files>frontend/src/components/keyboard-shortcuts/CommandPalette.tsx, frontend/src/pages/MoUs/MousPage.tsx, frontend/src/routes/_protected/mous.tsx</files>
  <read_first>frontend/src/components/keyboard-shortcuts/CommandPalette.tsx (createActions :617-708, quickActions :711-873, routeContexts :217-299, suggested-action resolver :894-931 — read in chunks, 1620 lines), 87-RESEARCH.md §F25 audit table (the authoritative finding list with line numbers), frontend/src/pages/MoUs/MousPage.tsx (createOpen state :89, CreateMouDialog :217), frontend/src/routes/_protected/mous.tsx (route shell gaining validateSearch), frontend/src/components/work-creation/WorkCreationProvider.tsx (openPalette signature + WorkItemType union — verify which types the palette supports before re-targeting)</read_first>
  <action>Apply each audit verdict: (1) `create-task` action → `openPalette('task')` + `closeCommandPalette()` via `useWorkCreation()` (hook call at component top level); (2) `create-intake` → `openPalette('intake')` and (3) `create-commitment` → `openPalette('commitment')` — FIRST verify WorkItemType supports these values in WorkCreationProvider; any unsupported type falls back to removal of that command (never leave a dead target); (4) `create-position` → REMOVE the entry (no create UI exists; do not hide — delete); (5) `cmd-export-dossiers` → REMOVE (?action=export consumed nowhere); (6) `cmd-view-network` → target `/relationships/graph` (the route analyze deep-links already use); (7) `nav-analytics` → target `/analytics` (matching the Alt+A binding at useKeyboardShortcuts.ts:361-369); (8) drop the `shortcut:` fields on create-task/create-intake/create-dossier entries (⌘N/⇧⌘I/⇧⌘D are advertised-but-unbound — no action-shortcut registration exists; dropping the glyph is the honest minimal fix); (9) for each of the 8 dead suggested-action ids in routeContexts (view-relationships, create-event, export-report, view-profile, view-help, view-users, view-monitoring, create-brief): either delete the id or make it resolve in the resolver — final state is every routeContexts id resolves. MousPage seam: add `validateSearch` to routes/_protected/mous.tsx whitelisting `action?: 'create'` (any other value → undefined), and in MousPage read the param, open CreateMouDialog when 'create', and strip the param via navigate replace:true once consumed (so close/reopen behaves). Do not fight cmdk's hardcoded listbox id (Pitfall 3 — pre-authorized).</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run src/components/keyboard-shortcuts && pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "action=create\|action=export" frontend/src/components/keyboard-shortcuts/CommandPalette.tsx` returns ONLY the `/mous?action=create` target (all other ?action= targets gone)
    - `grep -n "create-position\|cmd-export-dossiers" frontend/src/components/keyboard-shortcuts/CommandPalette.tsx` returns no matches
    - `grep -n "'/analytics'" frontend/src/components/keyboard-shortcuts/CommandPalette.tsx` returns >= 1 match and `nav-analytics` no longer targets /dashboard
    - `grep -n "relationships/graph" frontend/src/components/keyboard-shortcuts/CommandPalette.tsx` returns >= 1 match
    - routes/_protected/mous.tsx contains a validateSearch whitelisting action === 'create'
    - Existing CommandPalette.analyze.test.tsx still exits 0
  </acceptance_criteria>
  <done>Zero dead/half-dead/broken command targets; MousPage opens its dialog from the URL param; existing palette tests green</done>
</task>

<task type="auto">
  <name>Task 2: Add the 5 high-value commands + sentence-case pass (EN + AR + fallbacks)</name>
  <files>frontend/src/components/keyboard-shortcuts/CommandPalette.tsx, frontend/src/i18n/en/keyboard-shortcuts.json, frontend/src/i18n/ar/keyboard-shortcuts.json</files>
  <read_first>frontend/src/i18n/en/keyboard-shortcuts.json (full — every Title Case label to fix), frontend/src/design-system/hooks/useMode.ts (toggle-theme seam), frontend/src/i18n/index.ts:586 (switchLanguage export), 87-UI-SPEC.md §Copywriting F25 (group headers, label case, key glyph, additions rows)</read_first>
  <action>Add five entries following the existing entry contract: `create-mou` (icon FileSignature or similar lucide, action navigateTo('/mous?action=create'), category 'create'), `create-elected-official` (navigateTo('/dossiers/elected-officials/create'), reuse existing key createActions.newElectedOfficial), `create-user` (navigateTo('/users/create'), category 'create' — route's requireAdmin gate authorizes; non-admins hit the standing access-denied boundary, and RLS/edge functions remain the security gate), `toggle-theme` (useMode() at top level; action: setMode(mode === 'dark' ? 'light' : 'dark') then closeCommandPalette(); category 'commands'), `switch-language` (action: void switchLanguage(i18n.language === 'ar' ? 'en' : 'ar') then closeCommandPalette(); category 'commands'). New i18n keys in BOTH en+ar keyboard-shortcuts.json (e.g. createActions.newMou "Create MoU", createActions.newUser "Create user", commands.toggleTheme "Toggle theme", commands.switchLanguage "Switch language" — AR: real translations). Then the sentence-case pass: every Title Case label in en/keyboard-shortcuts.json becomes sentence case ("Create New Task" → "Create task", "Go to Dashboard" → "Go to dashboard", "Show Keyboard Shortcuts" → "Show keyboard shortcuts", etc. — sweep the whole file), mirror any needed AR adjustments, AND update every inline `t(key, fallback)` fallback string in CommandPalette.tsx in the SAME pass (stale Title Case fallbacks leak when a key is missing — PATTERNS assignment 6 warning). Proper nouns (MoU, dossier names) keep their casing. No emoji, no exclamation marks.</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run src/components/keyboard-shortcuts && pnpm --dir frontend lint --max-warnings 0 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Create New\|Go to Dashboard\|Show Keyboard" frontend/src/i18n/en/keyboard-shortcuts.json frontend/src/components/keyboard-shortcuts/CommandPalette.tsx` returns no matches
    - `grep -c "newMou\|newUser\|toggleTheme\|switchLanguage" frontend/src/i18n/en/keyboard-shortcuts.json` >= 4 and the same grep on ar/keyboard-shortcuts.json returns the same count
    - CommandPalette.tsx contains ids create-mou, create-user, create-elected-official, toggle-theme, switch-language
    - `pnpm --dir frontend lint` exits 0 (en/ar key parity via check-i18n-namespaces.mjs)
  </acceptance_criteria>
  <done>5 commands live with EN+AR labels; zero Title Case leaks in keys or fallbacks</done>
</task>

<task type="auto">
  <name>Task 3: CommandPalette.audit.test.tsx regression lock</name>
  <files>frontend/src/components/keyboard-shortcuts/__tests__/CommandPalette.audit.test.tsx</files>
  <read_first>frontend/src/components/keyboard-shortcuts/__tests__/CommandPalette.analyze.test.tsx (the table-driven precedent to mirror — mounting/mocking approach), frontend/src/components/keyboard-shortcuts/CommandPalette.tsx (post-Task-2 state)</read_first>
  <action>Write the Wave-0 audit regression test mirroring CommandPalette.analyze.test.tsx's approach. Table-driven assertions locking the audit verdicts: (a) the palette source (assert via rendered commands or an exported command manifest — if the inline arrays resist testing, extract a pure `PALETTE_NAV_TARGETS` const from CommandPalette.tsx as part of this task and assert on it) contains NO navigation target matching `?action=` except `/mous?action=create`; (b) nav-analytics targets '/analytics'; (c) cmd-view-network targets '/relationships/graph'; (d) removed ids (create-position, cmd-export-dossiers) are absent; (e) every suggested-action id in routeContexts resolves through the resolver (iterate the exported/extracted routeContexts table — no silently-dropped ids); (f) the 5 new command ids exist. Keep it maintainable: prefer asserting against extracted pure data over deep DOM queries; do not fight cmdk internals.</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run src/components/keyboard-shortcuts/__tests__/CommandPalette.audit.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - CommandPalette.audit.test.tsx exits 0 with assertions (a)-(f) present and each traceable to an audit finding
    - Full `pnpm --dir frontend exec vitest run src/components/keyboard-shortcuts` exits 0 (analyze + audit + any existing suites)
  </acceptance_criteria>
  <done>Wave-0 gap closed; a future PR reintroducing a dead command target fails this suite</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                        |
| -------------------------------------- | -------------------------------------------------- |
| URL → MousPage                         | New `?action=create` param crosses into page state |
| command visibility → privileged routes | Palette advertises navigation to admin surfaces    |

## STRIDE Threat Register

| Threat ID | Category               | Component               | Disposition | Mitigation Plan                                                                                                                                                                                                             |
| --------- | ---------------------- | ----------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-87-07   | Tampering              | mous.tsx validateSearch | mitigate    | Whitelist: `action` accepts exactly 'create', anything else → undefined                                                                                                                                                     |
| T-87-08   | Elevation of privilege | create-user command     | mitigate    | Command only navigates; `/users` layout `beforeLoad: requireAdmin` (public.users.role, exactly 'admin') authorizes, and admin edge functions enforce server-side — command visibility is UX, not authorization (audit rule) |
| T-87-09   | Elevation of privilege | dead-command laundering | mitigate    | Audit rule enforced by CommandPalette.audit.test.tsx: every advertised target resolves and target pages keep their own gates                                                                                                |
| T-87-SC   | Tampering              | package installs        | accept      | No package installs this phase                                                                                                                                                                                              |

</threat_model>

<verification>
- `pnpm --dir frontend exec vitest run src/components/keyboard-shortcuts` exits 0
- `pnpm --dir frontend lint` exits 0 (i18n parity + voice-adjacent gates)
- `pnpm --dir frontend exec tsc --noEmit` exits 0
- Manual AR-RTL glyph check deferred to the 87-10 consolidated sign-off (VALIDATION.md manual row)
</verification>

<success_criteria>

- Zero commands navigate to consumed-nowhere targets (repo greps in Task 1 acceptance prove it)
- 5 additions live, EN/AR parity enforced by lint
- Audit locked by test — AFF-03's "every advertised command works" is machine-checked
  </success_criteria>

<output>
Create `.planning/phases/87-linear-affordances/87-03-SUMMARY.md` when done
</output>
