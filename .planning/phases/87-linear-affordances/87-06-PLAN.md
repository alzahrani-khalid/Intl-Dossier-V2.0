---
phase: 87-linear-affordances
plan: 06
type: execute
wave: 2
depends_on: [87-01, 87-02, 87-04]
files_modified:
  - frontend/src/components/list-page/DossierTable.tsx
  - frontend/src/routes/_protected/dossiers/countries/index.tsx
  - frontend/src/routes/_protected/dossiers/organizations/index.tsx
  - frontend/src/hooks/useCountries.ts
  - frontend/src/hooks/useOrganizations.ts
autonomous: true
requirements: [AFF-01, AFF-02, AFF-04]
must_haves:
  truths:
    - "Clicking a countries or organizations row opens the peek drawer with a correct 'n / total' counter; chevrons page through the full filtered list including across server pages"
    - 'Countries and organizations toolbars carry Filter and Display popovers; filters show live counts, apply immediately, emit chips, and persist in the URL'
    - 'Display popover toggles hide/show the engagements / last-touch / sensitivity columns and sort by name or last updated in both directions'
    - "Empty countries/organizations lists show the rich Pattern-A state with create CTA; filtered-empty shows 'No matching rows' + ghost Clear filters"
  artifacts:
    - path: 'frontend/src/components/list-page/DossierTable.tsx'
      provides: 'visibleColumns prop driving the grid template (name+glyph always on)'
    - path: 'frontend/src/routes/_protected/dossiers/countries/index.tsx'
      provides: 'Extended validateSearch + peek registration + popovers + ListEmptyState (the template the other uniform surfaces mirror)'
    - path: 'frontend/src/hooks/useCountries.ts'
      provides: 'status/sensitivity filters + orderBy threading into the Supabase query'
  key_links:
    - from: 'frontend/src/routes/_protected/dossiers/countries/index.tsx'
      to: 'frontend/src/store/peekStore.ts'
      via: 'register({ids,type,total,pageOffset,fetchPage,pageSize}) on row click'
      pattern: 'usePeekStore|register'
    - from: 'frontend/src/routes/_protected/dossiers/countries/index.tsx'
      to: 'frontend/src/hooks/useDossierDrawer.ts'
      via: 'openDossier replacing navigate-to-detail on row click'
      pattern: 'openDossier'
    - from: 'frontend/src/routes/_protected/dossiers/countries/index.tsx'
      to: 'frontend/src/components/list-controls/FilterPopover.tsx'
      via: 'toolbar fragment in ListPageShell toolbar slot'
      pattern: 'FilterPopover'
---

<objective>
Wire the full F23 + F24 + F26 affordance set into the two DossierTable surfaces (countries, organizations), including the DossierTable column-visibility capability they need. Countries is deliberately done first and thoroughly — it is the Phase-40 template file; organizations then mirrors it exactly.

Purpose: AFF-01 (peek + paging), AFF-02 (popovers + live counts + URL persistence), AFF-04 (rich/filtered empty states) on surfaces 1-2 of 9.
Output: two fully-wired list surfaces + column-visibility support in DossierTable.

Per-surface config (planner scope resolved): filters = `status` + `sensitivity` (both real columns on the dossiers table — executor confirms enum values from existing dossier type defs before hardcoding option lists); sortFields = `name` (name_en/name_ar per locale) + `updated` (updated_at); properties = `engagements`, `lastTouch`, `sensitivity` (glyph + name never toggleable). Facet counts via anon-client head-counts on `dossiers` filtered by type (useWorkingGroups.ts:84-97 pattern).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-linear-affordances/87-UI-SPEC.md (§F23/§F24/§F26 interaction contracts)
@.planning/phases/87-linear-affordances/87-PATTERNS.md (assignment 5 — countries idiom; assignment 4 — popover overrides)
@frontend/src/routes/_protected/dossiers/countries/index.tsx
@frontend/src/components/list-page/DossierTable.tsx
@frontend/src/components/list-controls/useListControls.ts
@frontend/src/store/peekStore.ts
</context>

<interfaces>
From 87-02 (list-controls contracts): `useListControls(config, search, setSearch)`, `parseListControlsSearch(raw, config)`, `FilterPopover`, `DisplayPopover`, `FilterChipsRow`, `ListControlsConfig`.
From 87-01 (peek contracts): `usePeekStore` with `register({ids, type, total, pageOffset, fetchPage, pageSize})`; drawer reads it — the list only registers and calls `openDossier`.
From 87-04: `<ListEmptyState entityType="country" onCreate={...} filtered={hasActiveFilters} onClearFilters={clearAll}/>`.

fetchPage contract for these surfaces: an async closure over queryClient using `queryClient.fetchQuery` with the SAME query key family + filters the list hook uses, returning that page's ordered ids (executor derives the key from the hook's own useQuery call — never a divergent duplicate query definition).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DossierTable visibleColumns capability</name>
  <files>frontend/src/components/list-page/DossierTable.tsx</files>
  <read_first>frontend/src/components/list-page/DossierTable.tsx (the fixed `auto 1fr auto auto auto` grid at :41 and the 5 column renders: glyph/name/engagements/last-touch/sensitivity), 87-PATTERNS.md No-Analog table (this is net-new capability — Open Q3 resolution)</read_first>
  <action>Add optional prop `visibleColumns?: ReadonlyArray<'engagements' | 'lastTouch' | 'sensitivity'>` (absent = all visible — every existing consumer unaffected). Compute the grid-template-columns string from the visible set (glyph `auto` + name `1fr` always present; one `auto` per visible optional column) and conditionally render each optional cell in BOTH the header and body rows so alignment holds. Keep row heights on var(--row-h) and all existing classes/testids. Export the column id union type for the routes' PropertyDef configs.</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run src/components/list-page && pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - DossierTable renders 5 columns with no prop and 3 columns with visibleColumns={['sensitivity']} (existing or new test proves both)
    - No consumer compile break: tsc exits 0 across the repo
    - grid template is computed, not hard-coded per-branch (single source of truth for column order)
  </acceptance_criteria>
  <done>DossierTable supports column visibility with zero default-behavior change</done>
</task>

<task type="auto">
  <name>Task 2: Countries — full F23/F24/F26 wiring (the template)</name>
  <files>frontend/src/routes/_protected/dossiers/countries/index.tsx, frontend/src/hooks/useCountries.ts</files>
  <read_first>frontend/src/routes/_protected/dossiers/countries/index.tsx (current validateSearch :15-27, onSearchChange :33-45, onRowClick :47-55, empty-hint :117-132), frontend/src/hooks/useCountries.ts (SEARCH_MAX_LEN :45, .or(ilike) :55-66, .order/.range :72, count:'exact'), frontend/src/components/list-controls/useListControls.ts (config + parse contracts from 87-02), frontend/src/store/peekStore.ts (registration contract from 87-01), 87-UI-SPEC.md §F23 trigger/counter rows + §F26 list-page-empty row</read_first>
  <action>useCountries: accept optional `{status?, sensitivity?, orderBy?: 'name' | 'updated', dir?: 'asc' | 'desc'}` — thread as `.eq()` filters and swap the hard-coded `.order('updated_at', {ascending:false})` for the param-driven order (name sorts on name_en for en / name_ar for ar — accept a resolved column name from the caller); keep SEARCH_MAX_LEN + `.or(ilike)` untouched; include the new params in the queryKey. Route: extend DossierListSearch via `parseListControlsSearch` composition (page/search stay as-is); define the surface ListControlsConfig from the objective (status + sensitivity filters with buildCountQuery = anon-client head-counts on dossiers type='country' applying the OTHER active filters; sortFields name/updated; properties engagements/lastTouch/sensitivity mapped to DossierTable visibleColumns). Toolbar slot gets `<ToolbarSearch/> + <FilterPopover/> + <DisplayPopover/>` fragment plus `<FilterChipsRow/>` beneath. Row click: REPLACE navigate-to-detail with peek registration + open — `usePeekStore.getState().register({ids: rows.map(r=>r.id), type:'country', total: pagination.total, pageOffset: (page-1)*20, pageSize: 20, fetchPage})` then `openDossier({id: row.id, type:'country'})`; fetchPage per the interfaces block. Empty state: replace the bare empty-hint div with `<ListEmptyState entityType="country" onCreate={()=>navigate to the countries create route} filtered={hasActiveFilters} onClearFilters={clearAllFilters}/>` — verify the create route path from the routes tree before wiring (dossiers/countries/create). Every filter change resets page 1 with replace:true.</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run src/routes/_protected/dossiers/countries src/hooks 2>/dev/null; pnpm --dir frontend exec vitest run src/components/list-controls && pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "empty-hint" frontend/src/routes/_protected/dossiers/countries/index.tsx` returns no matches
    - `grep -n "openDossier\|usePeekStore" frontend/src/routes/_protected/dossiers/countries/index.tsx` returns >= 2 matches and the detail-route navigate is gone from onRowClick
    - useCountries queryKey includes status/sensitivity/orderBy/dir; `.order(` reads from params not a literal
    - validateSearch round-trips status/sensitivity/sort/dir/cols with whitelist fallbacks (unknown → undefined)
    - tsc + lint exit 0
  </acceptance_criteria>
  <done>Countries list: peek with cross-page counter, both popovers with live counts, chips, URL persistence, rich + filtered empty states</done>
</task>

<task type="auto">
  <name>Task 3: Organizations — mirror the countries template</name>
  <files>frontend/src/routes/_protected/dossiers/organizations/index.tsx, frontend/src/hooks/useOrganizations.ts</files>
  <read_first>frontend/src/routes/_protected/dossiers/countries/index.tsx (post-Task-2 — the finished template to mirror), frontend/src/routes/_protected/dossiers/organizations/index.tsx (current state: URL {page,search} :26, row click :75-84, empty-hint :102-110), frontend/src/hooks/useOrganizations.ts</read_first>
  <action>Apply the identical treatment: useOrganizations gains the same filter/orderBy params; route gains the same config (type:'organization', entityType="organization", head-counts on dossiers type='organization', create route path verified from the routes tree); row click registers + opens peek with type 'organization'; empty-hint replaced by ListEmptyState. Mirror the countries file shape closely — these two files should diff near-identically (the Phase-40 copy-paste symmetry is intentional and aids review).</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run src/routes/_protected/dossiers 2>/dev/null; pnpm --dir frontend exec tsc --noEmit && pnpm --dir frontend lint --max-warnings 0 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "empty-hint" frontend/src/routes/_protected/dossiers/organizations/index.tsx` returns no matches
    - organizations row click calls openDossier (grep >= 1) and no longer navigates to the detail route directly
    - Full unit suite for touched dirs + lint + tsc all exit 0
  </acceptance_criteria>
  <done>Organizations mirrors countries; both DossierTable surfaces fully wired</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                                        |
| ------------------ | ------------------------------------------------------------------ |
| URL → list queries | status/sensitivity/sort/dir/cols params flow into Supabase queries |
| client → PostgREST | facet head-counts with filter values                               |

## STRIDE Threat Register

| Threat ID | Category               | Component                                   | Disposition | Mitigation Plan                                                                                                                                       |
| --------- | ---------------------- | ------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-87-14   | Tampering              | extended validateSearch (2 routes)          | mitigate    | parseListControlsSearch whitelist — unknown values → undefined; sort columns resolved from a fixed map, never raw param interpolation into `.order()` |
| T-87-15   | Injection              | useCountries/useOrganizations filter params | mitigate    | `.eq()` builders with whitelist-validated values; search keeps SEARCH_MAX_LEN + `.or(ilike)` builder (no string SQL)                                  |
| T-87-16   | Information disclosure | facet counts                                | mitigate    | Anon client head-counts → RLS applies; no service-role endpoints                                                                                      |
| T-87-SC   | Tampering              | package installs                            | accept      | No package installs this phase                                                                                                                        |

</threat_model>

<verification>
- `pnpm --dir frontend exec vitest run src/components/list-page src/components/list-controls src/store src/hooks src/components/dossier/DossierDrawer` exits 0
- `pnpm --dir frontend exec tsc --noEmit` and `pnpm --dir frontend lint` exit 0
</verification>

<success_criteria>

- Both DossierTable surfaces deliver all three affordances end-to-end against live data shapes
- Countries file is a clean template for any future surface adoption
  </success_criteria>

<output>
Create `.planning/phases/87-linear-affordances/87-06-SUMMARY.md` when done
</output>
