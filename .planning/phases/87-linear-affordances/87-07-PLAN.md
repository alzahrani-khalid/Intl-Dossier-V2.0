---
phase: 87-linear-affordances
plan: 07
type: execute
wave: 2
depends_on: [87-01, 87-02, 87-04]
files_modified:
  - frontend/src/components/list-page/GenericListPage.tsx
  - frontend/src/routes/_protected/dossiers/forums/index.tsx
  - frontend/src/routes/_protected/dossiers/working_groups/index.tsx
  - frontend/src/routes/_protected/dossiers/topics/index.tsx
  - frontend/src/routes/_protected/dossiers/topics/-TopicsListPage.tsx
  - frontend/src/hooks/useForums.ts
  - frontend/src/hooks/useWorkingGroups.ts
  - frontend/src/hooks/useTopics.ts
autonomous: true
requirements: [AFF-01, AFF-02, AFF-04]
must_haves:
  truths:
    - 'Clicking a forums, topics, or working-groups row opens the peek drawer with a correct counter and cross-page chevron paging'
    - 'All three toolbars carry Filter + Display popovers with live counts, chips, and URL persistence'
    - 'Display properties on these GenericListPage surfaces toggle the secondary line and status chip (Open Q3 resolution)'
    - "Empty lists show rich Pattern-A states; filtered-empty shows 'No matching rows' + ghost Clear filters; the working-groups-empty testid survives"
  artifacts:
    - path: 'frontend/src/components/list-page/GenericListPage.tsx'
      provides: 'showSecondary/showStatus visibility props (default true)'
    - path: 'frontend/src/routes/_protected/dossiers/forums/index.tsx'
      provides: 'Wired GenericListPage surface following the countries template shape'
    - path: 'frontend/src/hooks/useWorkingGroups.ts'
      provides: 'orderBy/filter threading (its head-count block is also the facet-count precedent)'
  key_links:
    - from: 'frontend/src/routes/_protected/dossiers/forums/index.tsx'
      to: 'frontend/src/store/peekStore.ts'
      via: 'register + openDossier on row click'
      pattern: 'usePeekStore|openDossier'
    - from: 'frontend/src/routes/_protected/dossiers/topics/-TopicsListPage.tsx'
      to: 'frontend/src/components/list-controls/DisplayPopover.tsx'
      via: 'config-driven sections (sort omitted if the API repo cannot honor it)'
      pattern: 'DisplayPopover'
---

<objective>
Wire F23 + F24 + F26 into the three GenericListPage surfaces (forums, topics, working groups), adding the secondary-line/status-chip visibility capability GenericListPage needs. Follows the countries template established in 87-06 (both plans run in the same wave against disjoint files; the template shape is defined in 87-06's plan text and PATTERNS assignment 5 — do not wait on its completion to start, but mirror its structure).

Purpose: AFF-01, AFF-02, AFF-04 on surfaces 3, 5, 6 of 9.
Output: three wired surfaces + GenericListPage property-visibility props.

Per-surface config (planner scope resolved): filters = `status` + `sensitivity` on all three (dossiers-table columns; confirm enum values from type defs); sortFields = `name` + `updated` for forums/working-groups (Supabase-direct hooks — trivial `.order()` threading). Topics goes through the API repo (`useTopics` → `useDossiersByType('topic')`): expose sort/filters ONLY if the repo call already accepts them — otherwise OMIT those config sections (config-driven narrowing; never render a dead option, and do NOT extend backend surface this phase). Facet counts for ALL three run as anon-client head-counts on `dossiers` with the matching `type` — this works for topics too regardless of its list transport (dossiers rows are RLS-readable either way). Properties on all three = `secondary` + `status` (exactly 2 → section renders per Open Q3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-linear-affordances/87-UI-SPEC.md (§F23/§F24/§F26 interaction contracts)
@.planning/phases/87-linear-affordances/87-PATTERNS.md (assignment 5)
@frontend/src/components/list-page/GenericListPage.tsx
@frontend/src/hooks/useWorkingGroups.ts
@frontend/src/components/list-controls/useListControls.ts
@frontend/src/store/peekStore.ts
</context>

<interfaces>
From 87-02: `useListControls`, `parseListControlsSearch`, `FilterPopover`, `DisplayPopover`, `FilterChipsRow`, `ListControlsConfig` (properties section auto-omits below 2 entries).
From 87-01: `usePeekStore.register({ids, type, total, pageOffset, fetchPage, pageSize})` + `openDossier` — identical row-click recipe as 87-06.
From 87-04: `<ListEmptyState entityType={'forum' | 'topic' | 'working_group'} .../>` with filtered/onClearFilters.
GenericListPage rows are `{primary, secondary, status, icon}` (GenericListPage.tsx:5-12) — the property toggles map to `secondary` and `status` only.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: GenericListPage secondary/status visibility props</name>
  <files>frontend/src/components/list-page/GenericListPage.tsx</files>
  <read_first>frontend/src/components/list-page/GenericListPage.tsx (row shape :5-12 + render), 87-PATTERNS.md No-Analog table (Open Q3 resolution: secondary-line + status-chip toggles on GenericListPage surfaces)</read_first>
  <action>Add optional `showSecondary?: boolean` and `showStatus?: boolean` props (default true — zero change for existing consumers). When false, skip rendering the secondary line / status chip per row while keeping row height on var(--row-h) (rows do not collapse — density tokens own height). Export nothing new beyond the props; keep the component presentational.</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run src/components/list-page && pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - GenericListPage renders without secondary text when showSecondary={false} and without status chip when showStatus={false} (test or existing-suite extension proves it)
    - Default render byte-identical to before (no snapshot/test churn on consumers)
  </acceptance_criteria>
  <done>GenericListPage supports the two property toggles with safe defaults</done>
</task>

<task type="auto">
  <name>Task 2: Forums + Working groups wiring (Supabase-direct hooks)</name>
  <files>frontend/src/routes/_protected/dossiers/forums/index.tsx, frontend/src/routes/_protected/dossiers/working_groups/index.tsx, frontend/src/hooks/useForums.ts, frontend/src/hooks/useWorkingGroups.ts</files>
  <read_first>frontend/src/routes/_protected/dossiers/forums/index.tsx (URL {page,search} :51, row click :96, empty-hint :118-129), frontend/src/routes/_protected/dossiers/working_groups/index.tsx (:29 search, :89-98 empty with data-testid="working-groups-empty" — PRESERVE the testid, a route test depends on it, Pitfall 7), frontend/src/hooks/useForums.ts (count:'exact' :34, name_en asc order :51), frontend/src/hooks/useWorkingGroups.ts (RPC + separate head-count :77-91), 87-06-PLAN.md Task 2 (the countries template recipe — same steps)</read_first>
  <action>Apply the countries recipe to both surfaces: hooks gain `{status?, sensitivity?, orderBy?, dir?}` threaded via `.eq()` + param-driven `.order()` (working-groups: thread into both the RPC path's ordering if supported and the head-count filters — read the hook to see which side owns ordering; if the RPC cannot sort, sort the returned page client-side ONLY if it is the full result window, else omit the sort section for this surface and note it in the summary); routes extend validateSearch via parseListControlsSearch; toolbar fragments mount ToolbarSearch + FilterPopover + DisplayPopover + FilterChipsRow; row clicks register peek ({type:'forum'} / {type:'working_group'}, total from each hook's existing total, pageOffset=(page-1)*20, fetchPage via queryClient.fetchQuery on the hook's key family) then openDossier; empty-hint blocks replaced by ListEmptyState (entityType forum/working_group, onCreate → each surface's existing create route verified from the routes tree, filtered branch wired to clearAllFilters). Working-groups' ListEmptyState wrapper (or a wrapping div) carries data-testid="working-groups-empty" so the existing route test stays green.</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run src/routes/_protected/dossiers 2>/dev/null; pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "empty-hint" frontend/src/routes/_protected/dossiers/forums/index.tsx frontend/src/routes/_protected/dossiers/working_groups/index.tsx` returns no matches
    - `grep -n "working-groups-empty" frontend/src/routes/_protected/dossiers/working_groups/index.tsx` returns >= 1 match (testid preserved)
    - Both routes call openDossier + usePeekStore register on row click
    - Existing working-groups route test exits 0
  </acceptance_criteria>
  <done>Forums + working groups fully wired; testid contract intact</done>
</task>

<task type="auto">
  <name>Task 3: Topics wiring (API-repo hook, config-driven narrowing)</name>
  <files>frontend/src/routes/_protected/dossiers/topics/index.tsx, frontend/src/routes/_protected/dossiers/topics/-TopicsListPage.tsx, frontend/src/hooks/useTopics.ts</files>
  <read_first>frontend/src/routes/_protected/dossiers/topics/-TopicsListPage.tsx (URL {page,search} :33-35, row click :92-95, empty-hint :112-118), frontend/src/hooks/useTopics.ts AND its delegate frontend/src/domains/dossiers/hooks/useDossier.ts:180-192 (useDossiersByType — determine which filter/sort params the repo call actually accepts BEFORE building the config), 87-06-PLAN.md Task 2 (template recipe)</read_first>
  <action>Same recipe with the narrowing rule from the objective: build the topics ListControlsConfig with ONLY the capabilities the domains repo supports server-side — verified by reading useDossiersByType and its repository. If status/sensitivity filtering is unsupported there, keep the Filter popover but back it with... nothing invented: omit unsupported fields (an empty filters array means the Filter trigger is NOT rendered on topics — acceptable narrowing, documented in the summary); if sort is unsupported, omit sortFields. Facet counts (if filters render) still use anon-client dossiers head-counts. Peek row click always ships (type:'topic', total from the response's total when present — if the response lacks a total, register total = loaded length + set fetchPage undefined and note the degradation, mirroring A4 handling). Properties toggles (secondary/status → GenericListPage props) always ship. Empty-hint replaced by ListEmptyState entityType="topic" with the topics create route. Do NOT extend the backend/API surface this phase.</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run src/routes/_protected/dossiers 2>/dev/null; pnpm --dir frontend exec tsc --noEmit && pnpm --dir frontend lint --max-warnings 0 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "empty-hint" frontend/src/routes/_protected/dossiers/topics/-TopicsListPage.tsx` returns no matches
    - Topics row click opens the peek (openDossier grep >= 1)
    - Zero dead filter/sort options: every rendered option in the topics config maps to a repo-supported param (source-verifiable from the config definition + the repo signature)
    - lint + tsc exit 0
  </acceptance_criteria>
  <done>Topics wired with honestly-narrowed controls; peek + properties + empty states live</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                         |
| ------------------ | --------------------------------------------------- |
| URL → list queries | status/sensitivity/sort/dir/cols params on 3 routes |
| client → PostgREST | facet head-counts                                   |

## STRIDE Threat Register

| Threat ID | Category               | Component                        | Disposition | Mitigation Plan                                                                                          |
| --------- | ---------------------- | -------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------- |
| T-87-17   | Tampering              | 3 extended validateSearch blocks | mitigate    | parseListControlsSearch whitelists; sort columns from a fixed map, never raw interpolation               |
| T-87-18   | Injection              | hook filter params               | mitigate    | `.eq()`/`.or(ilike)` builders only; working-groups RPC params passed as typed RPC args, not concatenated |
| T-87-19   | Information disclosure | facet counts                     | mitigate    | Anon client → RLS; no service-role endpoints                                                             |
| T-87-SC   | Tampering              | package installs                 | accept      | No package installs this phase                                                                           |

</threat_model>

<verification>
- `pnpm --dir frontend exec vitest run src/components/list-page src/routes/_protected/dossiers` exits 0
- `pnpm --dir frontend exec tsc --noEmit` and `pnpm --dir frontend lint` exit 0
</verification>

<success_criteria>

- Three GenericListPage surfaces deliver peek, popovers (honestly scoped), and rich empty states
- working-groups-empty testid contract preserved; no dead options anywhere
  </success_criteria>

<output>
Create `.planning/phases/87-linear-affordances/87-07-SUMMARY.md` when done
</output>
