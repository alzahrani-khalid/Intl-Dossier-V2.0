---
phase: 87-linear-affordances
plan: 10
type: execute
wave: 3
depends_on: [87-03, 87-06, 87-07, 87-08, 87-09]
files_modified:
  - .planning/phases/87-linear-affordances/87-VALIDATION.md
autonomous: false
requirements: [AFF-01, AFF-02, AFF-03, AFF-04]
must_haves:
  truths:
    - "Full unit suite, lint (i18n parity + token gates), tsc, and build are green across the whole phase's changes"
    - 'A human has verified the three manual-only behaviors from 87-VALIDATION.md: peek paging feel in AR-RTL, live counts under RLS, ⌘K glyph LTR isolation in AR'
    - "No core surface renders bare 'no data' text (repo-wide grep proves the empty-hint pattern is gone from core surfaces)"
  artifacts:
    - path: '.planning/phases/87-linear-affordances/87-VALIDATION.md'
      provides: 'Per-requirement verification map stamped green + nyquist_compliant: true'
  key_links:
    - from: '.planning/phases/87-linear-affordances/87-VALIDATION.md'
      to: 'frontend test suites'
      via: 'stamped automated commands'
      pattern: 'vitest run'
---

<objective>
Phase gate: run the full battery over the integrated result of plans 87-01..87-09, sweep the cross-cutting greps, stamp the validation map, and hold the single consolidated human render sign-off covering the manual-only verifications.

Purpose: prove AFF-01..AFF-04 hold together on the integrated tree before /gsd:verify-work.
Output: green battery, stamped 87-VALIDATION.md, human sign-off record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-linear-affordances/87-VALIDATION.md
@.planning/phases/87-linear-affordances/87-UI-SPEC.md (§Checker Notes — pre-baked resolutions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full battery + cross-cutting sweeps</name>
  <files>.planning/phases/87-linear-affordances/87-VALIDATION.md</files>
  <read_first>.planning/phases/87-linear-affordances/87-VALIDATION.md (the per-requirement map to stamp), all 87-0X-SUMMARY.md files in the phase dir (deviations that may shift the sweep expectations)</read_first>
  <action>Run in order and fix any fallout (fixes must be minimal and within phase scope): (1) `pnpm --dir frontend exec vitest run` — full unit, baseline was 1488 green + this phase's additions; (2) `pnpm --dir frontend lint --max-warnings 0` — token/logical-property/i18n-parity gates; (3) `pnpm --dir frontend exec tsc --noEmit`; (4) `pnpm --dir frontend build` (or the workspace build if that is the standing command); (5) targeted Playwright a11y run against the touched surfaces if the harness runs locally (`pnpm --dir frontend test:a11y` targeted spec) — record honestly if environment-blocked. Cross-cutting sweeps: (a) `grep -rn "empty-hint" frontend/src/routes/_protected/dossiers frontend/src/pages/engagements` → 0 matches; (b) `grep -rn "action=create\|action=export" frontend/src/components/keyboard-shortcuts/CommandPalette.tsx` → only /mous?action=create; (c) `grep -rn "framer-motion" frontend/src/components/list-controls` → 0; (d) Wave-0 files all exist and pass: peekStore.test.ts, list-controls/__tests__, CommandPalette.audit.test.tsx. Stamp every row of the 87-VALIDATION.md Per-Task Verification Map with its result, check off the Wave-0 checklist, and set `nyquist_compliant: true` + `wave_0_complete: true` in its frontmatter.</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run 2>&1 | tail -5 && pnpm --dir frontend lint --max-warnings 0 2>&1 | tail -3 && pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Full vitest run exits 0; lint exits 0; tsc exits 0; build exits 0
    - All four sweep greps return the stated results
    - 87-VALIDATION.md frontmatter shows nyquist_compliant: true and every map row stamped ✅ (or ⚠️ with a written reason)
  </acceptance_criteria>
  <done>Integrated tree green end-to-end; validation contract stamped</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Consolidated EN/AR render sign-off (manual-only verifications)</name>
  <action>Present the verification walk below to the user and wait for approval. All automation is already complete (Task 1) — this checkpoint verifies visual/interactive behavior only.</action>
  <what-built>All four Linear affordances integrated across the nine core surfaces: right-peek panel with cross-page counter paging (F23), split Filter/Display popovers with live counts and URL persistence (F24), the audited + extended ⌘K menu (F25), and rich/filtered empty states (F26) — dark mode, EN/LTR + AR/RTL.</what-built>
  <how-to-verify>
    Run the dev server (PORT=5001 backend if needed; frontend :5173), log in with the .env.test admin credentials, viewport 1400px, dark mode. These are the three manual-only rows from 87-VALIDATION.md plus a general pass:

    1. Peek paging (AR-RTL): switch to Arabic (topbar ع). Open /dossiers/countries, click a row — the peek opens from the inline-start edge (RTL mirror), the head shows a mono "n / total" counter rendered LTR with Latin digits. Chevron through at least 3 rows (counter tracks, URL ?dossier= swaps without history spam — browser back closes the peek in one step). Page past row 20 on a >20-row list: next keeps working (neighbor page fetch). "Open full dossier" routes to the detail page.
    2. Live counts under RLS: on /dossiers/organizations (EN), open Filter — each option shows a count; apply one filter and confirm the chip row's "Showing N of M results" matches the rendered rows for your role. Toggle a Display property — the column hides; refresh — filters, sort, and hidden columns all persist from the URL.
    3. ⌘K in AR: open the palette in Arabic. Key glyphs (⌘K etc.) render LTR-isolated inside mirrored rows; group headers and labels are sentence case in both languages. Fire at minimum: Create MoU (dialog opens on /mous), Toggle theme, Switch language, Go to analytics (lands on /analytics), and one Analyze command from a dossier route.
    4. Empty states: visit a list with no data or apply a filter that excludes everything — filtered case shows "No matching rows" + ghost Clear filters (never a create CTA); a truly-empty list shows the rich glyph + heading + one-sentence body + single accent CTA. Kanban: commitment card click opens the commitment peek with a counter; task cards still navigate.
    5. Spot-check at 1024px width and light mode for obvious breakage on one list surface.

  </how-to-verify>
  <resume-signal>Type "approved" to close the phase gate, or describe issues per surface/feature</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                                            |
| -------- | ------------------------------------------------------------------------------------------------------ |
| none new | Gate plan — no production code changes beyond fixing battery fallout within already-modeled boundaries |

## STRIDE Threat Register

| Threat ID | Category    | Component           | Disposition | Mitigation Plan                                                                             |
| --------- | ----------- | ------------------- | ----------- | ------------------------------------------------------------------------------------------- |
| T-87-26   | Repudiation | verification record | mitigate    | 87-VALIDATION.md stamped with reproducible commands; human sign-off recorded in the summary |
| T-87-SC   | Tampering   | package installs    | accept      | No package installs this phase                                                              |

</threat_model>

<verification>
- Battery: full vitest + lint + tsc + build all exit 0
- Human checkpoint approved
</verification>

<success_criteria>

- All 4 ROADMAP success criteria demonstrated on the integrated tree (peek paging, popovers with counts, audited ⌘K, rich empty states) in EN and AR
- Validation contract complete: nyquist_compliant: true, Wave-0 files green, manual rows human-verified
  </success_criteria>

<output>
Create `.planning/phases/87-linear-affordances/87-10-SUMMARY.md` when done
</output>
