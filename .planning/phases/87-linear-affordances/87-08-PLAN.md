---
phase: 87-linear-affordances
plan: 08
type: execute
wave: 2
depends_on: [87-01, 87-02, 87-04, 87-05]
files_modified:
  - frontend/src/routes/_protected/dossiers/persons/-PersonsListPage.tsx
  - frontend/src/routes/_protected/dossiers/persons/index.tsx
  - frontend/src/pages/engagements/EngagementsListPage.tsx
  - frontend/src/routes/_protected/dossiers/engagements/index.tsx
  - frontend/src/hooks/useEngagementsInfinite.ts
  - frontend/src/routes/_protected/dossiers/elected-officials/index.tsx
  - frontend/src/components/elected-officials/ElectedOfficialListTable.tsx
autonomous: true
requirements: [AFF-01, AFF-02, AFF-04]
must_haves:
  truths:
    - 'Clicking a persons, engagements, or elected-officials row opens the peek drawer with a correct counter; chevrons page through loaded rows and extend at edges'
    - "Engagements' peek counter shows an exact total (head-count added to the infinite hook) and 'next' at the loaded edge triggers fetchNextPage"
    - 'All three toolbars carry Filter + Display popovers (honestly scoped to data-layer capabilities) with chips and URL persistence'
    - "All three surfaces show rich Pattern-A empty states with the filtered-empty branch; engagements' FilterPill row is folded into the Filter popover (not duplicated)"
  artifacts:
    - path: 'frontend/src/hooks/useEngagementsInfinite.ts'
      provides: "Exact grand total via parallel count:'exact' head query (A4 resolution)"
    - path: 'frontend/src/pages/engagements/EngagementsListPage.tsx'
      provides: 'Peek registration over the infinite window + popover toolbar'
    - path: 'frontend/src/components/elected-officials/ElectedOfficialListTable.tsx'
      provides: 'Row click → peek + ListEmptyState replacing the bare h3'
  key_links:
    - from: 'frontend/src/pages/engagements/EngagementsListPage.tsx'
      to: 'frontend/src/store/peekStore.ts'
      via: 'register with ids = all loaded infinite pages, fetchPage = fetchNextPage adapter'
      pattern: 'usePeekStore|register'
    - from: 'frontend/src/components/elected-officials/ElectedOfficialListTable.tsx'
      to: 'frontend/src/hooks/useDossierDrawer.ts'
      via: 'openDossier on row click (drawer type support verified/extended)'
      pattern: 'openDossier'
---

<objective>
Wire F23 + F24 + F26 into the three normalized bespoke surfaces (persons, engagements, elected-officials) on the URL seams 87-05 established.

Purpose: AFF-01, AFF-02, AFF-04 on surfaces 4, 7, 8 of 9.
Output: three wired surfaces; useEngagementsInfinite gains an exact total.

Per-surface config (planner scope resolved, honesty rule throughout — render only options the data layer honors):

- Persons: filters/sort ONLY where usePersons' repo already accepts params (read the domains repo first; omit unsupported sections). Peek type 'person'. Empty entity 'person', CTA → persons create route.
- Engagements: filter = `type` (the existing FilterPill value set, now inside the Filter popover — the pill row is REMOVED, its values become popover options; UI-SPEC fold-in rule, same logic as kanban's group-by). Peek type 'engagement'. Empty entity 'engagement', CTA "Log engagement" → engagements create route.
- Elected officials: filters = whatever 87-05 lifted to URL (existing dimensions only); sort only if the domains repo supports it. Peek type: verify `isValidDrawerDossierType` accepts the EO drawer type — if the drawer's type union lacks it, extend the validator + drawer type mapping (EO is one of the 8 dossier types; the drawer is the universal dossier overview) rather than leaving EO out. Empty entity 'elected_official', CTA → EO create route (route exists: dossiers/elected-officials/create).
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-linear-affordances/87-UI-SPEC.md (§F23/§F24/§F26 interaction contracts)
@.planning/phases/87-linear-affordances/87-PATTERNS.md (assignments 1, 5)
@frontend/src/hooks/useEngagementsInfinite.ts
@frontend/src/components/list-controls/useListControls.ts
@frontend/src/store/peekStore.ts
</context>

<interfaces>
From 87-01: `usePeekStore.register({ids, type, total, pageOffset, fetchPage, pageSize})`. For infinite lists: `pageOffset` is always 0 (the window starts at the list head), `ids` = every loaded row in order, and `fetchPage` adapts to `fetchNextPage` (the store's extendWindow appends the new page's ids).
From 87-02: `useListControls` + popover components + `FilterChipsRow`.
From 87-04: `<ListEmptyState entityType={...} onCreate filtered onClearFilters/>`.
From 87-05: all three surfaces already read/write state via validateSearch — extend those interfaces, do not re-lift.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Persons wiring</name>
  <files>frontend/src/routes/_protected/dossiers/persons/-PersonsListPage.tsx, frontend/src/routes/_protected/dossiers/persons/index.tsx</files>
  <read_first>frontend/src/routes/_protected/dossiers/persons/-PersonsListPage.tsx (post-87-05 URL-driven state; PersonsGrid render; bare centered empty text :94-99), the usePersons hook + its domains repo (enumerate supported params BEFORE building the config), frontend/src/types/person.types.ts:423-431 (PersonListResponse.pagination.total), 87-06-PLAN.md Task 2 (template recipe)</read_first>
  <action>Row click → `usePeekStore.getState().register({ids: loaded person ids in render order, type:'person', total: pagination.total, pageOffset: derived from the page/offset the hook uses, pageSize: the hook's limit, fetchPage: queryClient.fetchQuery adapter on the persons key family})` then `openDossier({id, type:'person'})`, replacing the detail navigate. Mount FilterPopover/DisplayPopover in the page toolbar area with a config holding ONLY repo-supported fields (empty filters → no Filter trigger; note narrowings in the summary); facet counts (if filters exist) via anon-client dossiers head-counts type='person'. Replace the bare centered empty text with ListEmptyState entityType="person" (onCreate → persons create route, filtered branch off hasActiveFilters — search text counts as a filter for the filtered-empty distinction: data exists but search excludes all → 'No matching rows' + clear). Verify the settled-empty gate: render the empty state only when the query is not loading (UI-SPEC loading row).</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run src/routes/_protected/dossiers/persons && pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Persons row click calls openDossier + register (greps >= 1 each); detail navigate removed from the row handler
    - `grep -n "ListEmptyState" frontend/src/routes/_protected/dossiers/persons/-PersonsListPage.tsx` returns >= 1 match and the old bare text block is gone
    - Existing persons route tests exit 0
  </acceptance_criteria>
  <done>Persons: peek + honestly-scoped controls + rich/filtered empty states</done>
</task>

<task type="auto">
  <name>Task 2: Engagements wiring + exact total (A4)</name>
  <files>frontend/src/pages/engagements/EngagementsListPage.tsx, frontend/src/routes/_protected/dossiers/engagements/index.tsx, frontend/src/hooks/useEngagementsInfinite.ts</files>
  <read_first>frontend/src/hooks/useEngagementsInfinite.ts (getNextPageParam :43-47; identify the base table + filters it queries), frontend/src/pages/engagements/EngagementsListPage.tsx (post-87-05; the FilterPill row to fold away; EngagementsList emptyState prop), frontend/src/hooks/useWorkingGroups.ts:84-97 (head-count pattern for the total), 87-UI-SPEC.md §F24 (fold-in rule) + §F26 matrix engagements row</read_first>
  <action>useEngagementsInfinite: add a parallel `count:'exact', head:true` query on the same base table applying the same filters (search + type), exposed as `total` from the hook (A4 resolution — exact counter denominator); include filters in both query keys. Page: build the ListControlsConfig with the `type` filter using the former FilterPill values; REMOVE the FilterPill row (fold-in — popover replaces pills, never both); counts per type option via the same head-count builder scoped per value. Row click → register({ids: all loaded rows across pages in order, type:'engagement', total, pageOffset: 0, pageSize: the infinite page size, fetchPage: adapter that calls fetchNextPage and resolves with the new page's ids}) then openDossier({id, type:'engagement'}). Pass `<ListEmptyState entityType="engagement" onCreate={→ engagements create route} filtered onClearFilters/>` through EngagementsList's existing emptyState prop. Display popover: sort section only if the infinite hook has a threadable `.order()`; properties per what EngagementsList rows expose (omit section if < 2 toggles — Open Q3 rule).</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run src/pages/engagements src/hooks 2>/dev/null; pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - useEngagementsInfinite exposes a numeric `total` backed by a head-count query (source-visible `count: 'exact', head: true`)
    - `grep -n "FilterPill" frontend/src/pages/engagements/EngagementsListPage.tsx` returns no matches (pill row folded into the popover)
    - Row click registers the peek and opens the drawer; ListEmptyState wired through the emptyState prop
    - tsc exits 0; touched suites green
  </acceptance_criteria>
  <done>Engagements: exact-total peek over the infinite window, popover-folded type filter, rich empty states</done>
</task>

<task type="auto">
  <name>Task 3: Elected officials wiring</name>
  <files>frontend/src/routes/_protected/dossiers/elected-officials/index.tsx, frontend/src/components/elected-officials/ElectedOfficialListTable.tsx</files>
  <read_first>frontend/src/routes/_protected/dossiers/elected-officials/index.tsx (post-87-05 URL-driven), frontend/src/components/elected-officials/ElectedOfficialListTable.tsx (row rendering, client pagination :334-349, bare h3 empty :162-167, data.total :105), frontend/src/lib/dossier-type-guards.ts + the drawer's isValidDrawerDossierType (verify EO type support before wiring), 87-UI-SPEC.md §F26 matrix elected-officials row</read_first>
  <action>Add the toolbar row (parallel to PageHeader per the 87-05 decision) mounting FilterPopover (existing EO filter dimensions from the 87-05 URL params; facet counts via anon-client dossiers head-counts scoped to the EO type/subtype — read how EO rows are stored: memory says EO = person_subtype, so scope counts accordingly) + DisplayPopover (sort only if the repo supports it; properties per the table's toggleable secondary elements, omit if < 2) + FilterChipsRow. Row click → register({ids: current filtered+sorted page order, type: the EO drawer type, total: data.total, pageOffset: (currentPage-1)*pageSize, pageSize, fetchPage: client-side — the table already holds the full filtered dataset client-side per RESEARCH (client currentPage/totalPages), so register ALL filtered ids with pageOffset 0 and no fetchPage}) then openDossier — FIRST verifying the drawer accepts the EO type; extend isValidDrawerDossierType + the drawer's type mapping if absent (smallest diff that makes EO peek real — never silently skip the surface). Replace the bare `<h3>{t('list.empty')}</h3>` with ListEmptyState entityType="elected_official" (onCreate → /dossiers/elected-officials/create, filtered branch). Verify the settled-empty gate on this non-shell surface (F26 loading row): empty renders only when not loading.</action>
  <verify>
    <automated>pnpm --dir frontend exec vitest run src/components/elected-officials 2>/dev/null; pnpm --dir frontend exec tsc --noEmit && pnpm --dir frontend lint --max-warnings 0 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - EO row click opens the peek drawer (openDossier grep >= 1 in the table or route)
    - `grep -n "list.empty" frontend/src/components/elected-officials/ElectedOfficialListTable.tsx` returns no bare-h3 usage (replaced by ListEmptyState)
    - If the drawer type union was extended, the change is in the validator + mapping only (no drawer restructure)
    - lint + tsc exit 0
  </acceptance_criteria>
  <done>EO: peek over the client-side dataset, popovers on existing dimensions, rich empty states</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                                     |
| ------------------ | --------------------------------------------------------------- |
| URL → list queries | filter params on 3 routes (established in 87-05, extended here) |
| client → PostgREST | engagements total head-count + facet counts                     |

## STRIDE Threat Register

| Threat ID | Category               | Component                      | Disposition | Mitigation Plan                                                                                                                                                    |
| --------- | ---------------------- | ------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-87-20   | Tampering              | extended validateSearch params | mitigate    | Whitelist validation per 87-05 idiom; new display params through parseListControlsSearch                                                                           |
| T-87-21   | Injection              | engagements head-count filters | mitigate    | PostgREST builders only (`.eq()`, `.or(ilike)`); values whitelist-validated upstream                                                                               |
| T-87-22   | Information disclosure | facet counts + totals          | mitigate    | Anon client → RLS applies (note: persons RLS org-isolation means counts reflect the caller's visibility — correct behavior, not a leak); no service-role endpoints |
| T-87-SC   | Tampering              | package installs               | accept      | No package installs this phase                                                                                                                                     |

</threat_model>

<verification>
- `pnpm --dir frontend exec vitest run src/routes/_protected/dossiers/persons src/pages/engagements src/components/elected-officials src/hooks` exits 0 (dirs with suites)
- `pnpm --dir frontend exec tsc --noEmit` and `pnpm --dir frontend lint` exit 0
</verification>

<success_criteria>

- All three bespoke surfaces deliver peek + controls + empty states with honestly-scoped configs
- Engagements counter denominator is exact; EO peeks over its full client-side dataset
  </success_criteria>

<output>
Create `.planning/phases/87-linear-affordances/87-08-SUMMARY.md` when done
</output>
