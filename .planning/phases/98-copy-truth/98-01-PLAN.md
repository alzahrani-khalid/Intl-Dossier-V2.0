---
phase: 98-copy-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/98-copy01-labels.spec.ts # NEW — criterion 1 DOM detector
  - tests/e2e/98-copy02-rawkeys.spec.ts # NEW — criterion 2 console + DOM + census
  - tests/e2e/98-copy03-dashboard.spec.ts # NEW — criterion 3 CDP-forced states
  - tests/e2e/98-copy04-voice.spec.ts # NEW — criterion 4, two graded halves (@values / @case)
  - tests/e2e/98-copy05-dates.spec.ts # NEW — criterion 5 rendered shapes
  - tests/e2e/98-copy06-toast.spec.ts # NEW — criterion 6 real-mutation toast
  - tests/e2e/98-copy07-statscard.spec.ts # NEW — COPY-07 rendered card
  - tests/e2e/98-copy08-eo-popover.spec.ts # NEW — criterion 7 rendered popover
  - .planning/phases/98-copy-truth/98-RED-BASELINE.md # NEW — machine-checkable per-spec RED/NOT CONSTRUCTED table
autonomous: true
requirements: [COPY-01, COPY-02, COPY-03, COPY-04, COPY-05, COPY-06, COPY-07, COPY-08]
success_criterion: 'the eight named oracles exist, each is proven RED at HEAD on its own defect with captured failure text, and no criterion is left without a named oracle (D-08)'
truths:
  - 'Decisions covered — D-07: every criterion closes on a rendered surface or a drilled instrument, never a source grep alone, and every spec header states its locale and role; D-08: seven criteria, seven named oracles — this plan authors all eight spec files so no criterion is oracle-less; D-09: Playwright spec paths are filters, so the gate asserts the existence of all eight files by name and hardcodes the count 8 before any multi-spec invocation; D-06: every detector regex is instrument-tested — shown firing on a known-present defect at HEAD — before its zero is believed; D-18: every command in every gate uses absolute paths.'
must_haves:
  truths:
    - 'Eight tests/e2e/98-*.spec.ts files exist, and running each at HEAD fails ON ITS DEFECT (captured text names the defect, not infra)'
    - 'Each spec header states criterion text, population, stated exclusions, locale (en + ?lng=ar), and role (admin)'
    - 'No spec contains test.skip, no spec references a chromium-ar project, no command wraps in timeout N'
    - 'The copy08 and copy02 specs carry static census assertions (JSON imports) in addition to rendered assertions'
  artifacts:
    - path: 'tests/e2e/98-copy08-eo-popover.spec.ts'
      provides: 'criterion 7 oracle: rendered popover presence assertions + 5-key/both-locale census'
      contains: 'lucide-crown'
    - path: 'tests/e2e/98-copy02-rawkeys.spec.ts'
      provides: 'criterion 2 oracle: DOM dotted-key detector + entityLinks census backstop + UNDRIVEN discipline'
      contains: 'UNDRIVEN'
  key_links:
    - from: 'tests/e2e/98-copy06-toast.spec.ts'
      to: 'frontend/src/lib/query-client.ts'
      via: 'sonner DOM assertion on the default mutations.onSuccess toast'
      pattern: 'data-sonner-toast'
---

<objective>
Author the phase's entire oracle set — eight Playwright specs, one per requirement — and prove
every one RED at HEAD against the defect it exists to detect. A spec that has never failed has
not been shown to detect anything (98-VALIDATION Wave 0). Nothing in this plan repairs anything:
the specs are authored to their POST-repair green assertions, and their RED state at HEAD is the
evidence that each one watches the right surface.

Purpose: pre-commitment 2 (D-07) made mechanical — the closers exist before any repair, so no
later plan can quietly substitute a source grep for a rendered surface.
Output: eight spec files + the recorded RED-at-HEAD run per spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-copy-truth/98-CONTEXT.md (D-01..D-30 — binding)
@.planning/phases/98-copy-truth/98-VALIDATION.md (the oracle contract these specs implement)
@.planning/phases/98-copy-truth/98-UI-SPEC.md §Verification Notes (the rendered assertions each spec must carry)
@tests/e2e/96-calendar-family.spec.ts (the proven header + inline-auth + narrow-CDP shape — copy its discipline verbatim)
@playwright.config.ts (projects: setup, chromium-en, chromium-ar-smoke, chromium-mobile; baseURL :5173)

<interfaces>
Verified on disk 2026-08-18.

- Auth: inline via `tests/e2e/support/pages/LoginPage.ts` + `TEST_USER_EMAIL`/`TEST_USER_PASSWORD`
  from `.env.test`, run `--no-deps` (the `setup` project throws on missing `E2E_*` keys —
  `E2ECRED-01` → P101). Locale flip: `?lng=ar` querystring inside `chromium-en` — NEVER the
  `chromium-ar-smoke` project (it filters to `**/ar-smoke/**` and is not the both-locale mechanism).
- Missing-key console capture is NOT available and the copy02 oracle carries NO console leg
  (dropped, revision 1): `frontend/src/i18n/index.ts` sets `saveMissing: false`, and i18next
  25.10.10 invokes `missingKeyHandler` only under that flag — the handler registered at ~:575
  is dead code at HEAD, so a listener for `Missing translation key:` can never fire in either
  locale (D-06/D-07: a leg that cannot fire is not a closer). No plan in the phase modifies
  `i18n/index.ts`; if a later phase flips the flag, the leg can be revived there.
- Sonner DOM: the single `SonnerToaster` mounts at `App.tsx:51`; toasts render under
  `[data-sonner-toast]`.
- EO popover chrome: trigger is `hidden sm:inline-flex` inside `DossierTypeStatsCard.tsx`
  (desktop viewport REQUIRED); at HEAD the trigger is withheld for EO by
  `{type !== 'elected_official' && (` at line ~168 — the copy08 RED. Lucide icons carry a
  `lucide-<name>` class: assert `svg.lucide-crown` inside the popover header, and the icon chip
  carrying `text-primary` (the country/primary set, D-27) — a Globe or muted popover is a FAIL.
- Stats-card label: EN value stays byte-identical `% of total active dossiers` (98-UI-SPEC C1),
  so the copy07 discriminator is the `?lng=ar` leg (AR value differs; at HEAD the EN literal
  renders under ar → RED).
- COPY-06 oracle mutation — CORRECTED revision 1, verified on disk: PRIMARY =
  `useUnifiedKanbanStatusUpdate` (`frontend/src/hooks/useUnifiedKanban.ts` ~:388) via a kanban
  stage move on a TASK card. Its `useMutation` options carry `onError` (~:547) and `onSettled`
  but NO `onSuccess`, and TanStack shallow-merges `{...defaultOptions.mutations, ...options}`,
  so the missing key lets the global default toast fire. The comment at ~:574 ("no global
  onSuccess") describes the D-33 REFUSAL path for commitment drops — a refusal never creates a
  mutation — so drive a TASK, never a commitment (which also avoids the aa_commitments
  no-`review` constraint). The entity-link mutations are NOT usable: `useEntityLinks.ts` defines
  its OWN `onSuccess` at :123, :181, :219, :369, each of which REPLACES the global default (a
  per-mutation `onSuccess` overrides, never merges). Re-derivation instrument for any
  replacement candidate: grep the WHOLE hook file for `onSuccess` — a windowed scan produced the
  original false negative and is forbidden.
- Sanctioned relative-time surfaces for copy05's exclusion list (D-25 enumeration, graded, fixed
  by plan 98-07): `components/activity-feed/EnhancedActivityFeed.tsx`,
  `pages/Dashboard/components/ActivityFeedItem.tsx`,
  `pages/dossiers/overview-cards/SharedRecentActivityCard.tsx`,
  `components/comments/CommentItem.tsx`, `components/notifications/NotificationItem.tsx`,
  `pages/Dashboard/widgets/RecentDossiers.tsx`. The spec hardcodes these six as the ONLY
  surfaces where a relative-time phrase may render.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: detector specs — copy01, copy02, copy04, copy05 (populations + exclusions in the header)</name>
  <files>tests/e2e/98-copy01-labels.spec.ts, tests/e2e/98-copy02-rawkeys.spec.ts, tests/e2e/98-copy04-voice.spec.ts, tests/e2e/98-copy05-dates.spec.ts</files>
  <read_first>tests/e2e/96-calendar-family.spec.ts (header discipline, inline auth, --no-deps note); .planning/phases/98-copy-truth/98-UI-SPEC.md §Verification Notes; frontend/src/i18n/index.ts (its i18n.init options — note saveMissing: false, the reason copy02 has no console leg); .planning/phases/98-copy-truth/98-RESEARCH.md §C1, §C2, §C4, §C5 (populations, exclusions, controls)</read_first>
  <action>
    Author four detector specs, each with the 96-family header (criterion text verbatim,
    population definition, stated exclusions, locale + role bound, inline-auth note). All run in
    `chromium-en` at desktop viewport, admin (TEST_USER_EMAIL), both locale legs by `?lng=ar`.

    **98-copy01-labels.spec.ts** (criterion 1). Visit signals list, engagements list, my-work
    kanban, waiting-queue, and the /dossiers hub. In asserted regions (scope innerText scans to
    the main content region; exclude elements that legitimately carry underscores — email
    addresses, mono ID chips) assert ZERO matches of the snake token `/\b[a-z]+(?:_[a-z]+)+\b/`
    and the ISO-week token `/\b\d{4}-W\d{2}\b/` in visible text AND in aria-labels on the
    asserted regions (aria copy is copy). POSITIVE LABEL LEG (the underscore-free enum class —
    `email`, `high`, `todo`, `done` — is invisible to both regexes, so absence alone cannot
    close it; NOTE the snake-token zero is aggressive over raw innerText — slugs, filenames
    and data values no plan repairs will match, so narrow the asserted region PER SURFACE to
    the repaired elements' containers and NAME each excluded region in the spec header, a
    narrowing nobody can read being a silent scope cut): assert the routed signals source-type
    chip renders the display label
    `Human entered` under `en` (and the ar value under `?lng=ar`), and a work-item priority
    chip renders `Urgent` — never a bare lowercase enum token. Instrument self-test INSIDE the spec: before the zero
    assertions, evaluate the same regexes against the literal fixtures 'human_entered' and
    'WEEK OF 2026-W27' in-page and require both to fire — a detector never shown firing proves
    nothing (D-06). Stated exclusions in the header: prop-position expressions, free-text data
    values, DB column names (glossary carve-outs).

    **98-copy02-rawkeys.spec.ts** (criterion 2). Two legs — a console-capture leg was
    considered and DROPPED (revision 1): `saveMissing: false` at HEAD means i18next never
    invokes `missingKeyHandler`, and a leg that cannot fire is not a closer (D-06/D-07); no
    plan flips the flag. State the drop in the spec header. (a) DOM detector: drive the intake
    ticket detail page (route `/_protected/intake/tickets.$id` — pick the first ticket from the
    intake queue; if NO ticket exists the leg records UNDRIVEN per D-24, never silently) with
    the entity link manager open, the country wizard region step, and an auth loading state;
    assert visible text contains no dotted token matching known prefixes
    `/(entityLinks|regions|typeGuide|typeDescription|calendar\.recurrence)\.[A-Za-z.]+/`, in
    BOTH locale legs. BOTH-POLARITY SELF-TEST in-spec (overseer requirement on the
    vacuous-oracle class): before the zero assertion, the regex is shown FIRING on a planted
    in-page fixture string `entityLinks.title` (negative control) and shown PASSING on a clean
    fixture string (positive control) — one polarity is not a tested instrument. (b) Census
    backstop (D-24 — backstop, NOT closure): import
    `frontend/src/i18n/en/common.json` and `ar/common.json` directly in the spec; derive the
    full static `entityLinks.*` key set referenced by the 8 source files (hardcode the derived
    list of key paths in the spec as a literal array with its derivation command in a comment;
    the DERIVED set governs — the checker's re-derivation at HEAD found 80 STATIC paths plus 2
    dynamic families (`linkTypes.${…}`, `entityTypes.${…}`), exactly the ruled 82; the census
    asserts the derived STATIC set — `static >= 82` would be permanently red — and the spec
    comment states the 80+2 decomposition) and assert every one resolves to a non-empty string
    in BOTH files; prove the census resolver both ways — a planted bogus path (`zz.nope`)
    reports unresolved while a known-present path resolves.
    (c) RECURRENCE CENSUS (criterion 2's recurrence class — no 98-spec drives
    `RecurrencePatternEditor`, so it closes per D-24 on census plus a NAMED UNDRIVEN line):
    derive the dot-form `calendar.recurrence.*` distinct-path set from the component source via
    D-22's derivation command (37 paths at check HEAD — the derived set governs), import
    `frontend/src/i18n/{en,ar}/calendar.json`, and assert every derived path resolves non-empty
    under the `recurrence` subtree in BOTH locales. Spec header carries the line:
    "RecurrencePatternEditor surface UNDRIVEN — closes on census per D-24; routing verified by
    98-04's conservation gate".
    Surfaces the oracle cannot drive (expected: AI-suggestion accept/reject flows needing the
    AnythingLLM backend; the recurrence editor) are closed by census PLUS a named UNDRIVEN
    scope line in the spec header and the SUMMARY — never silently.

    **98-copy04-voice.spec.ts** (criterion 4) — TWO tagged groups so waves can gate separately.
    Group `@values`: the EO list page add button text equals exactly `Add elected official`
    under `en` and `إضافة مسؤول منتخب` under `?lng=ar`; the calendar deadline chip renders
    `Deadline` (never `Deadline / Due Date`); visible text on the visited surfaces contains no
    `!` (the `validation:password.addSpecial` charset listing is excluded by scoping the scan
    away from password-validation hints — state it). Group `@case`: the sentence-case
    captured-label detector per D-20 — on every surface the 98-spec set visits (enumerate them
    in the spec: /dossiers hub, EO list, intake ticket detail, dashboard, calendar, signals
    list, engagements list, my-work) capture the text of label-class elements (`button`,
    `[role="tab"]`, `h1,h2,h3`, nav link labels, empty-state headings), EXCLUDING `th`
    table-column headers, elements with uppercase text-transform (classification ribbons), and
    mono-labelled elements (the CLAUDE.md carve-outs); keep only captured strings that
    exact-match an EN i18n leaf value (reverse-bundle lookup — data-driven text such as entity
    names is OUT of the population by this rule); flag Title Case (2–6 words, ≥2 non-initial
    words capitalized, not ALL-CAPS). Assert zero flagged. This group is EXPECTED RED until
    plan 98-08 lands; the `@values` group is expected green after 98-06.

    **98-copy05-dates.spec.ts** (criterion 5). On repaired surfaces (dashboard, audit logs if
    reachable, briefing books list, my-work) assert date tokens match
    `/^[A-Z][a-z]{2} \d{2} [A-Z][a-z]{2}/` shapes and times match `/\d{2}:\d{2} GST/`; assert
    NO relative-time phrase `/\b\d+ (minutes?|hours?|days?|months?|years?) ago\b/` renders
    ANYWHERE except on the six sanctioned feed surfaces (hardcode the six file-backed surfaces
    from the interfaces block as an in-spec allowlist of routes/regions); on one sanctioned
    feed under `?lng=ar` assert the relative phrase renders Arabic words (contains `منذ` or an
    Arabic time-unit word) with Latin digits. Header states the D-25 exclusions: `MMMM yyyy`
    calendar month-nav headers OUT; `yyyy-MM-dd` input value plumbing OUT.

    No fenced code blocks are prescribed here — implement using the 96-family idioms. Every
    spec: no `test.skip`, expected-test counts derivable from declaration lines (the 97 §4c
    SKIPPED-is-not-PASSED rule), absolute paths in any shell-out, and no `timeout N` wrapper
    anywhere (RC=127 on this Mac).

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && for f in 98-copy01-labels 98-copy02-rawkeys 98-copy04-voice 98-copy05-dates; do test -f "$R/tests/e2e/$f.spec.ts" || exit 1; done && test "$(command grep -c "test.skip" "$R/tests/e2e/98-copy01-labels.spec.ts" "$R/tests/e2e/98-copy02-rawkeys.spec.ts" "$R/tests/e2e/98-copy04-voice.spec.ts" "$R/tests/e2e/98-copy05-dates.spec.ts" | awk -F: '{s+=$2} END {print s}')" -eq 0 && command grep -q "lng=ar" "$R/tests/e2e/98-copy01-labels.spec.ts" && command grep -q "entityLinks" "$R/tests/e2e/98-copy02-rawkeys.spec.ts" && command grep -q "@case" "$R/tests/e2e/98-copy04-voice.spec.ts" && command grep -q "@values" "$R/tests/e2e/98-copy04-voice.spec.ts" && command grep -q "GST" "$R/tests/e2e/98-copy05-dates.spec.ts" && command grep -q "UNDRIVEN" "$R/tests/e2e/98-copy02-rawkeys.spec.ts" && command grep -q "zz.nope" "$R/tests/e2e/98-copy02-rawkeys.spec.ts" && command grep -q "entityLinks.title" "$R/tests/e2e/98-copy02-rawkeys.spec.ts" && cd "$R" && pnpm exec playwright test tests/e2e/98-copy01-labels.spec.ts tests/e2e/98-copy02-rawkeys.spec.ts tests/e2e/98-copy04-voice.spec.ts tests/e2e/98-copy05-dates.spec.ts --project=chromium-en --no-deps --list >/dev/null</automated>
  </verify>
  <acceptance_criteria>All four files exist (checked one at a time so a missing one is named); zero test.skip across the four (summed count, not per-file); the copy02 spec carries the census token `entityLinks`, the D-24 `UNDRIVEN` discipline token (NO console-capture clause — the leg is dropped, see interfaces), AND both polarity-control tokens pinned by the gate (rev 3): the planted DOM fixture `entityLinks.title` and the census bogus path `zz.nope` — presence is gate-asserted here and the controls EXECUTE on every spec run; copy04 carries both group tags; copy05 carries the `GST` shape; the `--list` run exits 0 proving all four parse and enumerate tests (list mode needs no dev server). RED-at-HEAD is Task 3's business, not this gate's. MAX REACHABLE: every clause is satisfiable by authoring the four files as specified; the `--list` exit is the only environment-dependent clause and depends only on node_modules, which P93–97 used daily.</acceptance_criteria>
  <done>Four detector specs exist, parse, and carry their populations, exclusions, controls, and locale/role bounds in their headers.</done>
</task>

<task type="auto">
  <name>Task 2: state-forcing and render specs — copy03, copy06, copy07, copy08</name>
  <files>tests/e2e/98-copy03-dashboard.spec.ts, tests/e2e/98-copy06-toast.spec.ts, tests/e2e/98-copy07-statscard.spec.ts, tests/e2e/98-copy08-eo-popover.spec.ts</files>
  <read_first>tests/e2e/96-calendar-family.spec.ts (narrow CDP block pattern — block the API call, NEVER the SPA module: the 95-03 lesson); frontend/src/components/dossier/DossierTypeStatsCard.tsx (the guard, the trigger, the Crown mapping at ~:95); frontend/src/components/dossier/DossierTypeGuide.tsx (popover variant ~:186-283 — section anatomy, silent-default guards at :224,241); frontend/src/hooks/useUnifiedKanban.ts ~:380-600 (the PRIMARY oracle mutation: options at ~:388 with onError ~:547 and NO onSuccess; the ~:574 comment scopes to the D-33 refusal path — read it whole before driving anything); .planning/phases/98-copy-truth/98-UI-SPEC.md §Component Contracts C7 (the popover assertions and content shape bounds)</read_first>
  <action>
    **98-copy03-dashboard.spec.ts** (criterion 3, the [V] item). Force the digest widget's empty
    and error states via CDP `Network.setBlockedURLs` scoped to the digest request pattern ONLY
    (derive the endpoint from the widget's hook before writing the pattern; a pattern that
    blocks the SPA module is a spec bug), and the VIP widget's empty state. Assert the rendered
    strings equal the NEW dashboard-widgets values (98-UI-SPEC C3 table) and that
    `/seed|staging|test data/i` matches NOWHERE in the widget region — in BOTH locales.
    Reading-emptiness-is-not-error discipline: the empty state is FORCED, never assumed from
    today's data shape.

    **98-copy06-toast.spec.ts** (criterion 6). Drive ONE real mutation known to lack its own
    onSuccess: PRIMARY a kanban stage move on a TASK card (source === 'task' ONLY — a commitment
    drop can be REFUSED before any mutation exists, D-33, and carries the aa_commitments
    no-`review` hazard; move the task to an adjacent stage and back so the check is idempotent).
    FALLBACK if the kanban board is undrivable: derive another candidate by grepping the WHOLE
    candidate hook for `onSuccess` (zero occurrences required — the entity-link mutations are
    disqualified, see interfaces). Assert `[data-sonner-toast]` visible
    text equals `Changes saved` under `en` and `تم حفظ التغييرات` under `?lng=ar`, and the
    literal `Operation completed successfully` appears NOWHERE in either leg. The HEAD negative
    control (Task 3) must observe the English literal under `?lng=ar` — proving the probe
    watches the right toast.

    **98-copy07-statscard.spec.ts** (COPY-07). On /dossiers at desktop: the EO stats card's
    percentage label — under `en` equals `% of total active dossiers` (EN value stays
    byte-identical per 98-UI-SPEC C1, so the en leg alone cannot discriminate t() from literal);
    under `?lng=ar` equals the new AR value `النسبة من إجمالي الملفات النشطة` and does NOT
    contain `of total active dossiers`. The ar leg is the discriminator and the HEAD RED.

    **98-copy08-eo-popover.spec.ts** (criterion 7). On /dossiers, desktop, both locales: the EO
    stats card's help trigger EXISTS; click it; inside the popover assert (a) header title
    equals the `dossier:type.elected_official` value, (b) `svg.lucide-crown` present in the
    header and NO `svg.lucide-globe` there, (c) the header icon chip carries `text-primary`
    (country/primary set per D-27/WR-07 — a muted chip is a FAIL), (d) description text equals
    the new `typeDescription.elected_official` value byte-for-byte per locale, (e) all FOUR
    sections PRESENT with non-empty bodies — ≥1 example Badge, ≥1 common-link row — presence
    assertions because the silent-default guards make a missing section vanish rather than leak
    (98-RESEARCH pitfall 6), (f) no visible text matches `/typeGuide\.|typeDescription\./`.
    Assert NOTHING about the seven sibling types' section bodies (GUIDE-HOLLOW-01 → P102).
    Static census in the same spec: import `frontend/src/i18n/en/dossier.json` and
    `ar/dossier.json`; assert `typeDescription.elected_official` is a non-empty string and
    `typeGuide.elected_official` holds `whenToUse` (string), `examples` (array length 3–5),
    `commonLinks` (array length 3–4), `notFor` (string) in BOTH files — this replaces a
    separate parity-script clone; state that choice in the header. HEAD negative control: the
    trigger is ABSENT for EO pre-fix.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && for f in 98-copy03-dashboard 98-copy06-toast 98-copy07-statscard 98-copy08-eo-popover; do test -f "$R/tests/e2e/$f.spec.ts" || exit 1; done && command grep -q "setBlockedURLs" "$R/tests/e2e/98-copy03-dashboard.spec.ts" && command grep -q "data-sonner-toast" "$R/tests/e2e/98-copy06-toast.spec.ts" && command grep -q "Operation completed successfully" "$R/tests/e2e/98-copy06-toast.spec.ts" && command grep -q "lucide-crown" "$R/tests/e2e/98-copy08-eo-popover.spec.ts" && command grep -q "text-primary" "$R/tests/e2e/98-copy08-eo-popover.spec.ts" && command grep -q "typeGuide" "$R/tests/e2e/98-copy08-eo-popover.spec.ts" && test "$(command grep -c "test.skip" "$R/tests/e2e/98-copy03-dashboard.spec.ts" "$R/tests/e2e/98-copy06-toast.spec.ts" "$R/tests/e2e/98-copy07-statscard.spec.ts" "$R/tests/e2e/98-copy08-eo-popover.spec.ts" | awk -F: '{s+=$2} END {print s}')" -eq 0 && cd "$R" && pnpm exec playwright test tests/e2e/98-copy03-dashboard.spec.ts tests/e2e/98-copy06-toast.spec.ts tests/e2e/98-copy07-statscard.spec.ts tests/e2e/98-copy08-eo-popover.spec.ts --project=chromium-en --no-deps --list >/dev/null</automated>
  </verify>
  <acceptance_criteria>All four files exist by name; copy03 carries the narrow-CDP token; copy06 carries both the sonner DOM hook and the banned literal it must prove absent; copy08 carries the glyph assertion (`lucide-crown`), the color assertion (`text-primary`), and the raw-key detector token; zero test.skip summed; `--list` exits 0. The copy08 census-in-spec choice (in place of a parity-script clone) is stated in that spec's header. MAX REACHABLE: all clauses satisfiable by authoring; no dev server needed for this gate.</acceptance_criteria>
  <done>All eight phase oracles exist and parse; each names what it closes on, in which locale, as which role.</done>
</task>

<task type="auto">
  <name>Task 3: prove every spec RED at HEAD, attributed — the Nyquist gate</name>
  <files>.planning/phases/98-copy-truth/98-RED-BASELINE.md, tests/e2e/98-copy01-labels.spec.ts</files>
  <read_first>.planning/phases/98-copy-truth/98-VALIDATION.md §Wave 0 Requirements (each spec proven RED via its own negative control before any repair lands); .planning/STATE.md §Current Position (the P97 CANNOT CONSTRUCT discipline for dev-stack-dependent halves)</read_first>
  <action>
    Run the full eight-spec set at HEAD:
    `cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && pnpm exec playwright test tests/e2e/98-copy01-labels.spec.ts tests/e2e/98-copy02-rawkeys.spec.ts tests/e2e/98-copy03-dashboard.spec.ts tests/e2e/98-copy04-voice.spec.ts tests/e2e/98-copy05-dates.spec.ts tests/e2e/98-copy06-toast.spec.ts tests/e2e/98-copy07-statscard.spec.ts tests/e2e/98-copy08-eo-popover.spec.ts --project=chromium-en --no-deps --reporter=json`
    — after first asserting all eight files exist (paths are FILTERS, D-09; the count is
    hardcoded at 8). Expected: EVERY spec fails at HEAD, each on its own defect. For each spec,
    capture and record in the SUMMARY the failure text that ATTRIBUTES the red to the defect —
    copy01 red on a rendered snake token, copy02 red on a rendered `entityLinks.` key, copy03 red on `/seed/i` in the forced empty state, copy04 red on
    `Add Elected Official` Title Case, copy05 red on a `N months ago` phrase, copy06 red on the
    English literal under `?lng=ar`, copy07 red on the EN literal under the ar leg, copy08 red
    on the ABSENT trigger. A red whose text names infra (auth failure, server not started,
    missing seed row) is NOT a control — repair the spec or the environment and re-run; where a
    leg's precondition genuinely cannot be constructed (no intake ticket in staging; the AI
    panel's backend), record the leg NOT CONSTRUCTED with what and why — never report it as a
    proven control. If any spec passes GREEN at HEAD, the spec is defective by definition (the
    defect exists at HEAD): fix the spec, not the claim. Adjust only spec files if edits are
    needed; the file scope of this task is the spec set from Tasks 1–2 plus the baseline record.

    Then write `.planning/phases/98-copy-truth/98-RED-BASELINE.md`: one table row per spec —
    `| 98-copyNN-name | RED <one-line attributing text> |` or
    `| 98-copyNN-name | NOT CONSTRUCTED <what and why> |` — a CLOSED two-term vocabulary; a
    GREEN row is by definition a spec defect and may not be written. This is the
    machine-checkable half of the Nyquist proof; the full captured failure text still lands in
    the SUMMARY.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && N=$(ls "$R/tests/e2e"/98-copy0*.spec.ts | wc -l | tr -d ' ') && test "$N" -eq 8 && test "$(command grep -rc "chromium-ar-smoke" "$R/tests/e2e"/98-copy0*.spec.ts | awk -F: '{s+=$2} END {print s}')" -eq 0 && F="$R/.planning/phases/98-copy-truth/98-RED-BASELINE.md" && test -f "$F" && test "$(command grep -c "^| 98-copy" "$F")" -eq 8 && test "$(command grep -Ec "^\| 98-copy[0-9]+[^|]*\| (RED|NOT CONSTRUCTED)" "$F")" -eq 8 && test "$(command grep -c "| GREEN" "$F" || true)" -eq 0</automated>
  </verify>
  <acceptance_criteria>Exactly 8 spec files exist (derived count equals the hardcoded 8 — a ninth or a missing one is caught); none references the chromium-ar-smoke project (the both-locale mechanism is ?lng=ar, summed zero across all eight). The RED-at-HEAD outcome is now MACHINE-ENFORCED via the baseline record: exactly 8 rows, every row RED or NOT CONSTRUCTED (closed vocabulary, both counts derived in the gate run), zero GREEN rows anywhere — a spec that passed at HEAD cannot be recorded without turning this gate red. The captured per-spec failure text still lands in the SUMMARY with the JSON reporter's counts, under the 97 §4c rule (expected count from declaration lines; SKIP is red); any leg that could not be constructed is listed NOT CONSTRUCTED with what and why — silence about a leg is a gate failure at close (98-09 consolidates the drill). MAX REACHABLE: the file-count and zero-count clauses are reachable today; the run evidence is reachable wherever the P96/P97 spec runs were.</acceptance_criteria>
  <done>Eight oracles, eight recorded REDs (or named NOT CONSTRUCTED legs), zero repairs made — the phase now has closers that provably detect its defects.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                                                    |
| ------------------------------- | ---------------------------------------------------------------------------------------------- |
| spec assertions → phase grading | every later green is read against these specs; a vacuous spec corrupts all downstream evidence |

## STRIDE Threat Register

| Threat ID | Category    | Component                                 | Disposition | Mitigation Plan                                                                        |
| --------- | ----------- | ----------------------------------------- | ----------- | -------------------------------------------------------------------------------------- |
| T-98-01   | Repudiation | a spec that never failed                  | mitigate    | Task 3 records an attributed RED (or NOT CONSTRUCTED) per spec before any repair lands |
| T-98-02   | Tampering   | spec-path filtering drops a spec silently | mitigate    | file existence asserted per name; count hardcoded at 8 (D-09)                          |
| T-98-SC   | Tampering   | package installs                          | accept      | zero installs this phase                                                               |

</threat_model>

<verification>
All three gates green; eight specs on disk; RED-at-HEAD (or named NOT CONSTRUCTED legs) recorded
per spec in the SUMMARY with attributing text. POPULATION: the eight 98-spec files. OUTSIDE IT:
every repair (later waves), the lint-guard extension (98-02), and all sibling-type popover
assertions (GUIDE-HOLLOW-01 → P102).
</verification>

<success_criteria>
Every criterion 1–7 has a named, on-disk, proven-to-fire oracle before any repair exists; the
criterion→oracle map of 98-VALIDATION is implemented file-for-file (COPY-07's oracle is the
dedicated 98-copy07 spec — the VALIDATION table's fold-in option is superseded by its own Wave-0
eight-file requirement, and the reconciliation is stated in the SUMMARY).
</success_criteria>

<output>
Create `.planning/phases/98-copy-truth/98-01-SUMMARY.md` when done. It MUST record: the per-spec
RED evidence table (spec → captured attributing text → locale/role of the observation), every
NOT CONSTRUCTED leg with what and why, the JSON-reporter expected-vs-actual counts per spec, and
the D-24 UNDRIVEN candidates the copy02 spec names.
</output>
