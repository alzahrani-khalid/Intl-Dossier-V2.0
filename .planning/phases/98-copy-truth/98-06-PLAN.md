---
phase: 98-copy-truth
plan: 06
type: execute
wave: 3
depends_on: [98-01, 98-03, 98-04]
files_modified:
  - frontend/src/i18n/en/dashboard-widgets.json # COPY-03 [V] — the 4 seed-instruction strings
  - frontend/src/i18n/ar/dashboard-widgets.json # same commit (D-16)
  - frontend/src/i18n/en/elected-officials.json # D-14/D-30 named instance re-case
  - frontend/src/i18n/ar/elected-officials.json
  - frontend/src/i18n/en/calendar.json # the Deadline / Due Date chip + its exclamation member
  - frontend/src/i18n/ar/calendar.json
  - frontend/src/i18n/en/commitments.json # dueDate display values
  - frontend/src/i18n/ar/commitments.json
  - frontend/src/i18n/en/meeting-minutes.json
  - frontend/src/i18n/ar/meeting-minutes.json
  - frontend/src/i18n/en/working-groups.json
  - frontend/src/i18n/ar/working-groups.json
  - frontend/src/i18n/en/onboarding.json # largest exclamation cluster (10)
  - frontend/src/i18n/ar/onboarding.json
  - frontend/src/i18n/en/actionable-errors.json # remainder of the derived exclamation set, en+ar pairs (A3: declared, since files_modified feeds collision checking)
  - frontend/src/i18n/ar/actionable-errors.json
  - frontend/src/i18n/en/ai-brief.json
  - frontend/src/i18n/ar/ai-brief.json
  - frontend/src/i18n/en/briefing-books.json
  - frontend/src/i18n/ar/briefing-books.json
  - frontend/src/i18n/en/calendar-sync.json
  - frontend/src/i18n/ar/calendar-sync.json
  - frontend/src/i18n/en/comments.json
  - frontend/src/i18n/ar/comments.json
  - frontend/src/i18n/en/contextual-suggestions.json
  - frontend/src/i18n/ar/contextual-suggestions.json
  - frontend/src/i18n/en/dossier-recommendations.json
  - frontend/src/i18n/ar/dossier-recommendations.json
  - frontend/src/i18n/en/engagement-recommendations.json
  - frontend/src/i18n/ar/engagement-recommendations.json
  - frontend/src/i18n/en/export-import.json
  - frontend/src/i18n/ar/export-import.json
  - frontend/src/i18n/en/integrations.json
  - frontend/src/i18n/ar/integrations.json
  - frontend/src/i18n/en/notification-center.json
  - frontend/src/i18n/ar/notification-center.json
  - frontend/src/i18n/en/positions.json
  - frontend/src/i18n/ar/positions.json
  - frontend/src/i18n/en/sample-data.json # + the ~24-file first-person candidate set, derived at execution — ALWAYS excluding common.json, dossier.json, intelligence-signals.json, engagements.json and all TSX, which other plans own
  - frontend/src/i18n/ar/sample-data.json
autonomous: true
requirements: [COPY-03, COPY-04]
success_criterion: 'criterion 3 in full and criterion 4 clauses 2-4 (exclamations, first person, retired terms) plus the named Title Case instance — all on derived populations, closed on forced rendered states and the copy04 @values oracle'
truths:
  - 'Decisions covered — D-14: elected-officials:list.add is the named Title Case instance inside COPY-04 with no register row of its own; D-30: the ratified CTA literal is Add elected official EN with the existing ar value unchanged; D-21: the exclamation population is the DERIVED 31 EN / 30 AR, not the audit 46, with the validation charset listing as the single stated carve-out, and first-person plural is re-derived at execution because the register 8 is unverified; D-04/D-05: every population in this plan is re-derived with stated definitions and exclusions; D-16: every repaired key pair is reviewed in both locales in the same commit.'
must_haves:
  truths:
    - 'The 4 dashboard-widgets strings are rewritten in both locales; /seed|staging|test data/i matches nothing in either locale of that namespace and the forced empty/error digest states render the new copy'
    - 'No "Due Date" display value survives anywhere in en/*.json; the calendar chip renders Deadline'
    - 'elected-officials list.add is exactly Add elected official (EN) with the ar value unchanged'
    - 'Exclamation leaf values total exactly 1 EN / 1 AR (the charset carve-out) after repair'
    - 'Every first-person candidate carries a triage verdict (product-speaker repaired / user-speaker out / domain-term flagged to P99); zero product-speaker members remain'
  artifacts:
    - path: 'frontend/src/i18n/en/dashboard-widgets.json'
      provides: 'truthful empty/error copy'
      contains: 'digest'
    - path: 'frontend/src/i18n/en/elected-officials.json'
      provides: 'the re-cased named instance'
      contains: 'Add elected official'
  key_links:
    - from: 'tests/e2e/98-copy03-dashboard.spec.ts'
      to: 'frontend/src/i18n/en/dashboard-widgets.json'
      via: 'CDP-forced empty/error states rendering the rewritten values'
      pattern: 'digest'
---

<objective>
The voice-value lane: criterion 3's four seed-instruction strings (the phase's one [V] item) and
criterion 4's three FULL clauses — exclamations, first-person plural, retired terminology — plus
the named Title Case instance. Everything here is a JSON string-value edit; no TSX is touched.
The sentence-case CAPTURED-LABEL clause is NOT this plan — it is bounded by D-20 to the oracle's
captured set and closes in 98-08 after every surface has its final copy.

File-scope law for this wave: common.json and dossier.json voice members were already repaired
in their owning lanes (98-04, 98-03); intelligence-signals.json and engagements.json belong to the wave-3
sibling 98-05. If a re-derivation here finds an in-population member inside any of those four
files, it is recorded as a named handoff line in the SUMMARY — never edited here, never dropped.

Purpose: the product stops shouting, stops saying we, stops shipping dev instructions and
retired vocabulary.
Output: rewritten values across the derived set, both locales, with verdict tables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-copy-truth/98-CONTEXT.md (D-14, D-16, D-21, D-30; the voice law in Canonical References)
@.planning/phases/98-copy-truth/98-RESEARCH.md §C3, §C4 (the [V] strings, derivation commands, the product-as-speaker triage rule)
@.planning/phases/98-copy-truth/98-UI-SPEC.md §C3 (prescribed replacement values), §Copywriting Contract

<interfaces>
Verified on disk 2026-08-18.

- COPY-03 targets (dashboard-widgets.json, both locales): `digest.empty.heading`,
  `digest.empty.body`, `digest.error`, `vip.empty.body`. Prescribed replacements (98-UI-SPEC C3
  — adjustable within voice law, but /seed|staging|test data/i must match in NEITHER locale):
  EN `No publications yet` / `New digest publications appear here as they are issued.` /
  `The digest could not load. Try again.` / `No VIP participants to show yet.`; AR
  `لا توجد منشورات بعد` / `تظهر منشورات الموجز الجديدة هنا عند صدورها.` /
  `تعذر تحميل الموجز. أعد المحاولة.` / `لا يوجد مشاركون من كبار الشخصيات لعرضهم بعد.`
- Named instance (D-14/D-30): `elected-officials:list.add` EN `Add Elected Official` →
  `Add elected official`; AR `إضافة مسؤول منتخب` unchanged. The key feeds the list-page header
  button AND the empty-state CTA.
- Retired terms (derive by value-grep, offsets rot): the chip `calendar.json` `"title":
"Deadline / Due Date"` → `Deadline`; `"dueDate": "Due Date"` values in commitments.json (×2),
  meeting-minutes.json, working-groups.json (common.json and dossier.json members already
  repaired upstream — the wave-3 re-derivation must find them GONE, which is the check that the
  upstream lanes did their part). AR pairs `الموعد النهائي` aligned with 98-03's choice. KEY
  names and DB columns untouched (carve-outs).
- Exclamation distribution at last derivation (re-derive; D-21): 31 EN / 30 AR leaf values
  across 17 files — onboarding (10), actionable-errors, ai-brief, engagement-recommendations,
  integrations, notification-center (2 EN / 1 AR — pairs are NOT always symmetric), and singles
  including calendar.json and sample-data.json. Carve-out: `validation:password.addSpecial`
  charset listing keeps its `!` — the post-repair floor is exactly 1/1, a non-zero floor so the
  zero-trap does not arise.
- First-person raw candidates at last derivation: 30 across 24 files (the register's 8 is
  unverified — D-21). Triage rule (98-RESEARCH §C4b): IN when the PRODUCT is the speaker
  (`We found existing contacts…`, `Tell us about…`); OUT when the USER is the speaker
  (`ai-chat:suggestion1` `What are our active commitments?` — a canned user prompt); domain
  terms (`dossier:overview.positions.ourStance` `Our Position` — an organizational noun phrase)
  are flagged to the P99 glossary, not rewritten here.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: COPY-03 — the four seed-instruction strings, both locales, forced-state proven</name>
  <files>frontend/src/i18n/en/dashboard-widgets.json, frontend/src/i18n/ar/dashboard-widgets.json</files>
  <read_first>both dashboard-widgets.json files in full (the 4 keys in context; any sibling the value-sweep should catch); .planning/phases/98-copy-truth/98-UI-SPEC.md §C3 (the prescribed values and the never-leak-internals rule)</read_first>
  <action>
    Replace the four values in BOTH locales with the prescribed replacements (interfaces block).
    Then re-run the sibling sweep (98-RESEARCH §C3): value-scan ALL EN namespaces for
    `\bseed(ed)?\b|\bstaging\b|test data|migration` — the last derivation returned exactly these
    4 (its own hits were the control); post-repair it must return 0 with the instrument re-shown
    firing on a scratch fixture. The sibling sweep is EN-namespaces-only — stated per D-05: the
    English pattern cannot see Arabic values, which is why the gate carries a separate ar
    defect-term leg; ar sibling coverage rides EN/AR key-parity (a rewritten EN key's ar pair is
    rewritten with it, D-16). Stated exclusions: key NAMES, code comments, `.planning/`,
    tests, and `sample-data:firstRun.adminBody` (a product first-run feature, not a seed
    instruction — it is a first-person member for Task 3, not a COPY-03 member).
  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && python3 -c "
import json,re
pat=re.compile(r'\bseed(ed)?\b|\bstaging\b|test data',re.I)
for loc in ('en','ar'):
    d=json.load(open('$R/frontend/src/i18n/%s/dashboard-widgets.json'%loc))
    def leaves(o):
        for v in o.values():
            if isinstance(v,dict): yield from leaves(v)
            elif isinstance(v,str): yield v
    bad=[v for v in leaves(d) if pat.search(v)]
    assert not bad, (loc,bad)
    assert pat.search('Apply the dashboard demo seed'), 'EN control failed to fire'
arpat=re.compile('المزروعة|التجريبية|بيانات الاختبار')
d=json.load(open('$R/frontend/src/i18n/ar/dashboard-widgets.json'))
def leaves(o):
    for v in o.values():
        if isinstance(v,dict): yield from leaves(v)
        elif isinstance(v,str): yield v
badar=[v for v in leaves(d) if arpat.search(v)]
assert not badar, ('ar seed-instruction terms survive', badar)
assert arpat.search('الموجز جاهز للمنشورات المزروعة.'), 'AR control failed to fire'
print('COPY03-OK both locales, both polarities')
" && command grep -q '"heading": "No publications yet"' "$R/frontend/src/i18n/en/dashboard-widgets.json" && command grep -q "لا توجد منشورات بعد" "$R/frontend/src/i18n/ar/dashboard-widgets.json" && cd "$R" && pnpm exec playwright test tests/e2e/98-copy03-dashboard.spec.ts --project=chromium-en --no-deps</automated>
  </verify>
  <acceptance_criteria>The python sweep proves zero seed/staging/test-data values in BOTH locales with BOTH polarities per locale (revision 1, A2 — the English regex is VACUOUS over Arabic values, so the ar leg gets its own defect-term pattern المزروعة/التجريبية/بيانات الاختبار, shown firing on the old ar value inline and returning zero on the repaired file); the new EN heading AND the new AR heading are asserted byte-exactly; the copy03 spec — CDP-forcing the empty and error states — goes GREEN in both legs, which is the [V] item's rendered closure. NOT CONSTRUCTED discipline applies to the spec half. MAX REACHABLE: all clauses reachable by the 8 value edits.</acceptance_criteria>
  <done>No seed or test instruction ships as user copy; proven on forced rendered states.</done>
</task>

<task type="auto">
  <name>Task 2: retired terms everywhere + the named Title Case instance</name>
  <files>frontend/src/i18n/en/elected-officials.json, frontend/src/i18n/ar/elected-officials.json, frontend/src/i18n/en/calendar.json, frontend/src/i18n/ar/calendar.json, frontend/src/i18n/en/commitments.json, frontend/src/i18n/ar/commitments.json, frontend/src/i18n/en/meeting-minutes.json, frontend/src/i18n/ar/meeting-minutes.json, frontend/src/i18n/en/working-groups.json, frontend/src/i18n/ar/working-groups.json</files>
  <read_first>the retired-term derivation output (run FIRST: value-grep for "Due Date" and "Deadline / Due" across frontend/src/i18n/en/*.json — the derived set governs, and the common.json/dossier.json members must already be GONE, which verifies the upstream lanes); en/elected-officials.json ~:5 (the named instance in context)</read_first>
  <action>
    Re-case `elected-officials:list.add` EN to exactly `Add elected official` (D-30 ratified;
    matches the `empty-states:list.*.cta` family register); AR unchanged. Replace the calendar
    chip value `Deadline / Due Date` with `Deadline` and every remaining `"Due Date"` display
    value in the derived set with `Deadline` (EN) / `الموعد النهائي` (AR pair, aligned with
    98-03). Key names stay; DB columns stay (`aa_commitments.due_date`, `tasks.sla_deadline` —
    the carve-outs are correct as they stand and renaming keys forces code edits for zero user
    value). If the derivation finds a `Due Date` value in a file this plan is banned from
    (common.json, dossier.json, intelligence-signals.json, engagements.json), record the handoff line —
    expected outcome: none, because 98-03/98-04 already repaired theirs.
  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && command grep -q '"add": "Add elected official"' "$R/frontend/src/i18n/en/elected-officials.json" && command grep -q '"add": "إضافة مسؤول منتخب"' "$R/frontend/src/i18n/ar/elected-officials.json" && test "$(command grep -l '"Due Date"\|Deadline / Due' "$R/frontend/src/i18n/en/"*.json | wc -l | tr -d ' ')" -eq 0 && command grep -rq '"Due Date"' "$R/.planning/phases/98-copy-truth/98-06-PLAN.md" && cd "$R/frontend" && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>The named instance is byte-exact in both locales (EN re-cased, AR untouched — asserted, not assumed); ZERO en/*.json files still carry a "Due Date" or chip value — a REPO-WIDE-namespace zero, reachable only because the upstream lanes repaired their files first, which makes this clause double as the cross-lane check; the instrument control rides the same chain: the identical pattern must still find its own quotation in THIS plan file, proving the grep instrument reads (D-06 — a wrapper-blinded or mistyped pattern fails the control, not just the zero); type-check green. MAX REACHABLE: the zero is reachable at this wave by construction of the lane ordering.</acceptance_criteria>
  <done>The glossary term is Deadline everywhere a user reads it; the named instance is sentence case.</done>
</task>

<task type="auto">
  <name>Task 3: exclamations and first-person — re-derive, triage, repair, floor-proven</name>
  <files>frontend/src/i18n/{en,ar}/onboarding.json plus the rest of the derived exclamation set in en+ar pairs — at last derivation: actionable-errors.json, ai-brief.json, briefing-books.json, calendar-sync.json, comments.json, contextual-suggestions.json, dossier-recommendations.json, engagement-recommendations.json, export-import.json, integrations.json, notification-center.json, positions.json, sample-data.json (calendar.json rides Task 2; validation.json is the carve-out, untouched) — plus the ~24-file first-person candidate set (interfaces block); ALWAYS excluding the four banned files common.json, dossier.json, intelligence-signals.json, engagements.json (renamed from the non-existent signals.json, rev 3 — the ban guards against 98-05, whose wave-3 file IS intelligence-signals.json; derived fact: it carries 0 exclamations in either locale)</files>
  <read_first>the two derivation outputs (run FIRST: the python leaf-walkers from 98-RESEARCH §C4a/§C4b over frontend/src/i18n/{en,ar}/*.json — exclamation values and first-person candidates); every file the derivations name (the file list in this task's files field names the largest cluster; the derived set governs and extends it, EXCLUDING the four banned files)</read_first>
  <action>
    EXCLAMATIONS (D-21): re-derive; expect ~31 EN / ~30 AR. Repair every member except the
    validation charset listing (the single stated carve-out): drop the `!` or neutrally
    rephrase — `Welcome aboard!` shapes lose the mark, celebration copy calms down, no
    rewording beyond what removing the mark requires. Pairs repair together: where the AR value
    carries the mirrored `!` it is edited in the same commit; where it does not
    (notification-center has 2 EN / 1 AR), the EN-only edit is recorded as such — D-16 governs
    keys, and the pair REVIEW is the rule stated per commit. Post-repair the walker must return
    exactly 1 EN / 1 AR (the carve-out — a non-zero floor, so no zero-trap).

    FIRST-PERSON: re-derive candidates (~30; the register's 8 is unverified — the derived set
    governs, both figures reported per D-04). Apply the product-as-speaker triage rule to EVERY
    candidate and record a verdict table: product-speaker → rewritten without we/our/us (state
    what the product did instead: `We found 3 matches` → `3 matches found`); user-speaker →
    OUT, named; domain term (`Our Position`) → flagged to the P99 glossary, named. Zero
    product-speaker members remain. AR pairs follow the same review rule.

    D-15 DEVIATION, stated with cause (matching 98-07 T2's shape): this task spans ~17
    exclamation files x 2 locales plus the ~24-file first-person candidate set — over the
    ~10-file cap — but every edit is a one-line value change in a single mechanical class
    (~60-110 diff lines total, under the 120-line cap); splitting would manufacture plan
    boundaries inside one derived population for zero quality gain.

    Then run the copy04 oracle's `@values` group — the rendered closure for this task and
    Task 2 (the EO CTA text, the Deadline chip, no `!` on visited surfaces). The `@case` group
    is EXPECTED still red (98-08's business — state it, never chase it here).

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && python3 -c "
import json,glob
for loc,want in (('en',1),('ar',1)):
    n=0; hits=[]
    for f in glob.glob('$R/frontend/src/i18n/%s/*.json'%loc):
        def walk(o,fn):
            global n
            if isinstance(o,dict):
                for v in o.values(): walk(v,fn)
            elif isinstance(o,list):
                for v in o: walk(v,fn)
            elif isinstance(o,str) and '!' in o:
                n+=1; hits.append(fn)
        walk(json.load(open(f)),f.split('/')[-1])
    assert n==want, (loc,n,hits[:10])
print('EXCLAIM-FLOOR-OK 1/1')
" && command grep -q '!' "$R/frontend/src/i18n/en/validation.json" && cd "$R" && pnpm exec playwright test tests/e2e/98-copy04-voice.spec.ts --project=chromium-en --no-deps --grep "@values"</automated>
  </verify>
  <acceptance_criteria>The walker (the SAME instrument that derived 31/30 — its non-zero HEAD reading was its own control) returns exactly 1 EN and 1 AR, printing the offending files on failure; the surviving `!` is proven to be the validation charset carve-out by the paired presence grep — floor and carve-out asserted together so an over-eager sweep that deletes the charset listing goes red too; the copy04 `@values` leg goes GREEN in both legs. The first-person verdict table (candidate → verdict → rewritten value where applicable) lands in the SUMMARY with the derived-vs-register figures both reported; a candidate without a verdict is a population failure. MAX REACHABLE: 1/1 is reachable because the carve-out is the only sanctioned survivor; @values is reachable once Tasks 1-3 and the upstream lanes have landed.</acceptance_criteria>
  <done>The product's voice obeys its own rules on every derived member, with the one carve-out standing and stated.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                                                             |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| error-copy rewrite → users | new error strings must not leak internals; the old digest.error's staging-seed mention is itself the leak being removed |

## STRIDE Threat Register

| Threat ID | Category               | Component                              | Disposition | Mitigation Plan                                                                           |
| --------- | ---------------------- | -------------------------------------- | ----------- | ----------------------------------------------------------------------------------------- | ----------------------------- |
| T-98-12   | Information disclosure | internals in rewritten error copy      | mitigate    | prescribed values carry problem + next step only; the /seed                               | staging/ sweep is in the gate |
| T-98-13   | Repudiation            | triage hiding a product-speaker member | mitigate    | every candidate gets a written verdict; zero-verdict members fail the close consolidation |
| T-98-SC   | Tampering              | package installs                       | accept      | zero installs this phase                                                                  |

</threat_model>

<verification>
All three gates green; copy03 green both legs; copy04 @values green both legs. POPULATION: the
four COPY-03 strings, the derived exclamation/first-person/retired-term sets, the named
instance. OUTSIDE IT: the four banned files (owned by 98-03/98-04/98-05), the sentence-case
captured-label clause (98-08 per D-20), key names, code comments, tests, `.planning/`, the dead
public/locales.
</verification>

<success_criteria>
Criterion 3 closes on forced rendered states; criterion 4's exclamation, first-person, and
retired-term clauses close IN FULL on derived populations; the named instance renders sentence
case in both legs.
</success_criteria>

<output>
Create `.planning/phases/98-copy-truth/98-06-SUMMARY.md` when done. It MUST record: the
post-repair walker outputs (1/1) with the carve-out named; the full first-person verdict table
with derived-vs-register figures; the retired-term derivation showing the upstream lanes'
members already gone; any banned-file handoff lines (expected: none — state it); and both spec
results with locale+role per green.
</output>
