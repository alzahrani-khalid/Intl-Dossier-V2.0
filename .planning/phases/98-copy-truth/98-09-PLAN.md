---
phase: 98-copy-truth
plan: 09
type: execute
wave: 6
depends_on: [98-01, 98-02, 98-03, 98-04, 98-05, 98-06, 98-07, 98-08]
files_modified:
  - .planning/phases/98-copy-truth/98-GATE-DRILL.md # NEW — one row per gate, one author, one pass
  - .planning/phases/98-copy-truth/98-CLOSING-DERIVATION.md # NEW — coverage, populations, bounds, weakest point
  - .planning/REQUIREMENTS.md # the ONLY writer of this file in the phase
autonomous: true
requirements: [COPY-01, COPY-02, COPY-03, COPY-04, COPY-05, COPY-06, COPY-07, COPY-08]
success_criterion: 'all seven criteria — this plan re-derives coverage by command, consolidates the drill record, updates the register with bounds, and names the phase weakest point'
truths:
  - 'Decisions covered — D-01: the phase closes 8 requirements COPY-01..COPY-08, each mapped in plan frontmatter to the criterion it serves and RE-DERIVED from the register by command, with a silent drop of any requirement a REJECT; D-02: the register is the queue and the ROADMAP entry its digest — the mapping is re-derived by command and the match is STATED, never assumed; D-03: out-of-phase surfaces are named, not assumed — the OUT list (GUIDE-HOLLOW-01 → P102, per-mutation success copy set aside, NAV-01 never edited by any plan, Arabic naturalness and pixel RTL → P99, the AR-04a interaction recorded not pre-empted) is restated in the closing record with owners; D-15: task caps carried with each deviation stated in its plan; D-17: no shell-embedded node -e file writes anywhere in the phase; D-19: every gate names cd frontend with the real script names because pnpm --filter frontend exits 0 matching nothing.'
must_haves:
  truths:
    - 'Requirement coverage 8/8 is re-derived from the register by command, not asserted, and the ROADMAP-versus-register match is stated'
    - 'Decision coverage runs green with uncovered [] and a falsification drill lands on disk in both colours'
    - 'Every gate in the phase has a drill row with a verdict; half-constructed is CANNOT CONSTRUCT, never SOUND'
    - 'The criterion→oracle map is restated with each green locale+role bound; every UNDRIVEN, NOT CONSTRUCTED, and handoff line from the SUMMARYs is consolidated in one list'
    - 'The register rows for COPY-01..08 reflect what actually shipped, bounds included; NAV-01 (a Phase 97 row) is untouched'
    - 'The report names the phase weakest point; a uniform pass is itself a finding'
  artifacts:
    - path: '.planning/phases/98-copy-truth/98-CLOSING-DERIVATION.md'
      provides: 'coverage derivations, closing population statements, the consolidated bounds list, the named weakest point'
      contains: 'WEAKEST POINT'
    - path: '.planning/phases/98-copy-truth/98-GATE-DRILL.md'
      provides: 'one row per gate with both directions and a verdict'
      contains: 'CANNOT CONSTRUCT'
  key_links:
    - from: '.planning/REQUIREMENTS.md'
      to: '.planning/phases/98-copy-truth/98-CLOSING-DERIVATION.md'
      via: 'register status rows citing the closing derivation and its bounds'
      pattern: 'Phase 98'
---

<objective>
Close the phase honestly, in the 97-12 shape. Coverage is re-derived, not asserted — the
register and the ROADMAP agree at the repair commit and the derivation still runs and says so
(D-02). The drill record is consolidated by one author holding all eight plans at once. The
report names the weakest green — a uniform pass is a DOWNGRADE in the acceptance's own terms.

Single-writer, by name: this plan is the phase's ONLY writer of `.planning/REQUIREMENTS.md`.
Handoff lines arrive as text in SUMMARYs (98-04's UNDRIVEN surfaces, 98-06's banned-file
handoffs if any, 98-08's KEEP carve-outs) and are written here, so two plans never write one
file. NAV-01's Phase 97 row is NOT edited — its dated note at close-out is the overseer's act,
not any seat's (CONTEXT Phase Boundary).

Purpose: what shipped, what did not, and where the weakest green is.
Output: two records and the register.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-copy-truth/98-CONTEXT.md (the OUT list, the four grading pre-commitments)
@.planning/phases/98-copy-truth/98-VALIDATION.md (the criterion→oracle contract to restate with results)
@.planning/ROADMAP.md §Phase 98 (the seven criteria at HEAD — 2, 4, 5, 7 as amended)
@.planning/REQUIREMENTS.md (COPY definitions at ~:149-183; register rows re-derived by command, last seen 757-764 — the register grows, pinned offsets decay)
@.planning/phases/97-reachability/97-GATE-DRILL.md (the row shape and verdict vocabulary to mirror)

<interfaces>
- Coverage derivation: `command grep -n "^| COPY-0[1-8] " /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0/.planning/REQUIREMENTS.md`
  (8 rows expected, all owned by Phase 98) and the ROADMAP `**Requirements**:` line for Phase 98
  — state the comparison result explicitly.
- Decision coverage: `node scripts/decision-coverage.mjs <phase_dir> <context_path>` — scans
  plan frontmatter truths/must_haves/objective for mechanical `\bD-NN\b`; everything from the
  CONTEXT's Claude's-Discretion heading onward is untracked. The phase defines D-01..D-30.
- Gate drill: `node scripts/gate-drill.mjs <phase_dir> --json --timeout <seconds>` — pass the
  flags (the bare form prints usage). It proves parse + current-tree result ONLY; it cannot
  tell a red-for-the-right-reason from a red-because-absent. The constructed-done-state half
  lives in each plan's SUMMARY; this plan consolidates.
- AUTHORING HAZARD (97-12, proven): the drill script pairs a gate tag written in PROSE with the
  next real closing tag and extracts garbage between. Never write the literal gate tag names in
  either record's commentary — name gates as `98-NN.tN`.
- Prettier re-wraps frontmatter on `.planning/`-only commits; re-verify machine-readable
  frontmatter still PARSES after every hook run (the P95 empty-map incident).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: the consolidated gate-drill record, one author, all gates</name>
  <files>.planning/phases/98-copy-truth/98-GATE-DRILL.md</files>
  <read_first>.planning/phases/97-reachability/97-GATE-DRILL.md (the row shape, the partial-construction rule, the third-direction discipline); scripts/gate-drill.mjs header (flags and limits); every 98-*-SUMMARY.md in the phase directory (the per-plan construction observations this record consolidates)</read_first>
  <action>
    Run the drill over the eight repair plans (exclude this plan's own file from the scratch
    scope, the 97-12 precedent) and keep the JSON. Write one row per gate in the
    GATE-STANDARD shape — `| 98-NN.tN | red observed (how) | green constructed (how) | exceptions | verdict |`
    with verdicts from the CLOSED set `SOUND` / `REPAIRED (what changed)` /
    `CANNOT CONSTRUCT (what and why)`. State the partial-construction rule at the top: a gate
    whose done state was not FULLY constructed is CANNOT CONSTRUCT, never SOUND — this phase's
    expected members are every Playwright-running gate half in an environment without the dev
    stack, and the copy02/copy06 legs gated on staging data presence (an intake ticket to
    drive). For every coarse-aggregate gate (`pnpm type-check`, `pnpm lint`, the build, whole
    spec runs) the row carries CAPTURED text attributing the red to the gate's own subject.
    Sweep the plan set for every oracle/artifact/script name that changed during execution
    (C9a) and paste the counts; record post-close mutations naming the closed gates whose
    subjects they changed — the known member at plan time: 98-08's JSON value edits change
    subjects of 98-06's floor gates (the exclamation walker) and the copy04 @values oracle —
    name each and its state after wave 5.
  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && F="$R/.planning/phases/98-copy-truth/98-GATE-DRILL.md" && test -f "$R/scripts/gate-drill.mjs" && test -f "$F" && D="${TMPDIR:-/tmp}/p98-09-drillscope" && rm -rf "$D" && mkdir -p "$D" && for p in "$R/.planning/phases/98-copy-truth"/98-*-PLAN.md; do case "$p" in *98-09-PLAN.md) ;; *) cp "$p" "$D"/ ;; esac; done && test "$(ls "$D"/98-*-PLAN.md | wc -l | tr -d ' ')" -eq 8 && node "$R/scripts/gate-drill.mjs" "$D" --json --timeout 120 > "$D/drill.json" && test -s "$D/drill.json" && node -e 'const r=require(process.argv[1]).results; if(!r.length) process.exit(1); const bad=r.filter(x=>x.parse!=="PARSE-OK"); if(bad.length){console.error(bad.map(b=>b.id||b.plan));process.exit(1)}; process.stdout.write(String(r.length))' "$D/drill.json" > "$D/n.txt" && GATES=$(cat "$D/n.txt") && test "$GATES" -ge 20 && ROWS=$(command grep -c '^| 98-' "$F") && test "$ROWS" -eq "$GATES" && command grep -q 'CANNOT CONSTRUCT' "$F" && command grep -q 'C9' "$F"</automated>
  </verify>
  <acceptance_criteria>The scratch scope holds exactly 8 plan files (this plan excluded — a ninth appearing or one vanishing breaks the pin); every extracted gate block is PARSE-OK with failures named; the record's row count EQUALS the drilled gate count, both derived in the same run so a forgotten row or an added gate goes red (the plan-time expectation is 21 gates across the eight plans; the floor of 20 guards against an extraction regression without freezing the derived number); the partial-construction vocabulary is present (a phase with a dev-stack dependency and NO CANNOT CONSTRUCT row anywhere would itself be suspicious — if genuinely all constructed, write the one such row as `CANNOT CONSTRUCT: none — all constructed` so the token is a statement, not an accident); the C9 sweep counts are pasted. Gate names appear as 98-NN.tN in commentary, never as literal tags (the extraction hazard). MAX REACHABLE: rows==gates is reachable the moment the record is complete; both sides derive in this gate.</acceptance_criteria>
  <done>Every gate in the phase examined once, by one author, in one pass.</done>
</task>

<task type="auto">
  <name>Task 2: coverage re-derived, populations closed, weakest point named</name>
  <files>.planning/phases/98-copy-truth/98-CLOSING-DERIVATION.md</files>
  <read_first>.planning/REQUIREMENTS.md at the COPY definitions and the re-derived register rows; .planning/ROADMAP.md §Phase 98 at HEAD (the amended criteria 2/4/5/7); every 98-*-SUMMARY.md (every bound, UNDRIVEN line, NOT CONSTRUCTED leg, verdict table, handoff); .planning/phases/98-copy-truth/98-VALIDATION.md (the oracle contract to restate with results)</read_first>
  <action>
    **§1 Requirement coverage, by command.** Derive the eight ids from the register, map each to
    its criterion and closing plan(s), and state the ROADMAP-versus-register comparison result
    explicitly (D-02). A silent drop of any id is a REJECT (D-01).

    **§2 Decision coverage.** Run
    `node scripts/decision-coverage.mjs .planning/phases/98-copy-truth .planning/phases/98-copy-truth/98-CONTEXT.md`
    (absolute paths) and paste the result — exit 0 with `uncovered: []` over D-01..D-30. Then
    the FALSIFICATION DRILL on disk: in a scratch copy remove one D-NN citation from one plan's
    truths, re-run, record the red naming that decision, restore, record the green. A coverage
    gate never seen red is a gate nobody tested.

    **§3 Criterion→oracle closure table.** One row per criterion 1–7: the named oracle, its
    final result, and the LOCALE and ROLE of every green (pre-commitment 2 — a green without
    its bound stated is not a green). Consolidate every UNDRIVEN line (D-24), every NOT
    CONSTRUCTED leg, and the D-20 captured-set bound into the table's exceptions column.

    **§4 Closing population statements.** Restate each derived population (Part A/B enums,
    entityLinks census, exclamations 1/1 floor, first-person verdicts, date classes, captured
    labels) with its definition, final count, and what fell OUTSIDE (D-05). Then the
    phase-level OUT restatement with owners (D-03): GUIDE-HOLLOW-01 and COPY-09 → P102;
    AR-01..04 and the AR-04a mask class and the operator's Arabic-naturalness park → P99;
    per-mutation success copy → set aside by ruling; NAV-01 untouched. List every bound the
    SUMMARYs recorded — the admin-only evidence base, viewport bounds, any data-present
    precondition unmet — in one list. State the SKIPPED-is-not-PASSED rule applied: per spec,
    expected-vs-actual counts from the JSON reporter.

    **§5 WEAKEST POINT.** Name the single weakest green and why — the expected candidates are
    the UNDRIVEN entityLinks surfaces (census-closed, never rendered) and any CANNOT CONSTRUCT
    Playwright half — but derive it from the records, do not pre-write it. If nothing weak is
    found, report THAT with what was looked for.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && F="$R/.planning/phases/98-copy-truth/98-CLOSING-DERIVATION.md" && test -f "$F" && test -f "$R/scripts/decision-coverage.mjs" && OUT=$(node "$R/scripts/decision-coverage.mjs" "$R/.planning/phases/98-copy-truth" "$R/.planning/phases/98-copy-truth/98-CONTEXT.md") && printf '%s' "$OUT" | command grep -q '"uncovered": \[\]' && for id in COPY-01 COPY-02 COPY-03 COPY-04 COPY-05 COPY-06 COPY-07 COPY-08; do command grep -q "$id" "$F" || { echo "MISSING $id"; exit 1; }; done && command grep -q 'WEAKEST POINT' "$F" && command grep -q 'falsification' "$F" && command grep -q 'UNDRIVEN' "$F" && command grep -q 'Phase 99' "$F" && command grep -q 'Phase 102' "$F" && command grep -q 'GUIDE-HOLLOW-01' "$F" && command grep -q 'COPY-09' "$F"</automated>
  </verify>
  <acceptance_criteria>The decision-coverage scanner is RUN by the gate — captured inside the && chain so a non-zero exit stops it — and must report a literally empty uncovered array over every decision its extractor tracks (30/30 since the extractor repair at 87b2d040e — the orchestrator on-disk run reports total 30, covered 30, uncovered []); all eight ids appear, checked one at a time with the missing one named; the WEAKEST POINT heading, the falsification token, the UNDRIVEN consolidation, and the out-of-phase owners (P99, P102, GUIDE-HOLLOW-01, COPY-09) are all pinned. The falsification drill's red output is pasted in §2, not summarised. MAX REACHABLE: uncovered:[] is reachable — verified GREEN at revision time with 30/30 covered; every other clause is reachable by writing the record from the SUMMARYs.</acceptance_criteria>
  <done>The phase can be graded from its own records without re-deriving anything by hand.</done>
</task>

<task type="auto">
  <name>Task 3: the register — one writer, what actually shipped</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md — the COPY-01..08 register rows RE-DERIVED by row-prefix command (pinned offsets decay) and the COPY definitions; .planning/phases/98-copy-truth/98-CLOSING-DERIVATION.md (as just written — the rows cite it); the 98-04 and 98-08 SUMMARYs (UNDRIVEN scope lines and KEEP carve-outs that belong IN the row bounds)</read_first>
  <action>
    Update the eight COPY-01..COPY-08 rows to reflect what shipped, in the P96/P97 bounded
    form: `Complete (BOUNDED — <bound> → <owner>, <record>)` where a bound exists, plain
    `Complete (<record>)` where none does. Known bound classes to carry from the SUMMARYs:
    COPY-02's UNDRIVEN surfaces (census-closed per D-24), COPY-04's D-20 sentence-case bound
    with COPY-09 → P102 named, COPY-05's named month-header exemption (D-25), and the
    phase-wide admin-only evidence bound. Do NOT edit the requirement DEFINITIONS unless
    execution proved one factually wrong — and say so in the SUMMARY if it did. Do NOT touch
    NAV-01 or any other phase's row. COPY-09's row (owner Phase 102) is not this phase's to
    edit; if 98-08's record materially re-sizes it, the note goes in the SUMMARY for the
    overseer, not in the row.

    This file has exactly one writer this phase; if any other plan's SUMMARY reports having
    written it, that is a single-writer violation and belongs in the closing record's bounds
    list.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && F="$R/.planning/REQUIREMENTS.md" && C="$R/.planning/phases/98-copy-truth/98-CLOSING-DERIVATION.md" && test -f "$F" && test -f "$C" && for id in COPY-01 COPY-02 COPY-03 COPY-04 COPY-05 COPY-06 COPY-07 COPY-08; do command grep -q "^| $id | Phase 98 — Copy Truth | " "$F" || { echo "ROW SHAPE $id"; exit 1; }; done && test "$(command grep -c "^| COPY-0[1-8] | Phase 98 — Copy Truth | Pending" "$F" || true)" -eq 0 && command grep -q "^| COPY-09 | Phase 102" "$F" && command grep -q "^| NAV-01 | Phase 97 — Reachability | Complete (BOUNDED" "$F" && W=$(command grep -c '^  - \.planning/REQUIREMENTS\.md' "$R/.planning/phases/98-copy-truth"/98-*-PLAN.md | awk -F: '{s+=$2} END {print s}') && test "$W" -eq 1</automated>
  </verify>
  <acceptance_criteria>All eight rows exist in the fully-qualified column shape and NONE still reads Pending — the zero anchored to the full row prefix so an unrelated Pending elsewhere in the register cannot satisfy or break it, inside the && chain via argument-position substitution with || true (the 97-12 lesson: a bare ; discards every assertion before it); COPY-09 still belongs to Phase 102 and NAV-01's Phase 97 row is byte-untouched in its Complete (BOUNDED…) shape — both asserted, because this phase's most likely register accident is exactly one of those two rows; EXACTLY one plan file in the phase declares this file in its frontmatter list (the anchored `^  - ` form that prose cannot contaminate — summed across files so a second declarer is caught wherever it hides). GATE DRILL: red observed on the undone tree (all eight rows read Pending today); green constructed in a scratch copy by editing the rows. MAX REACHABLE: the writer count is 1 today because only this plan declares the file; the Pending zero is red today and flips exactly at completion.</acceptance_criteria>
  <done>The register says what happened, bounds included, written once.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                   |
| ------------------------------ | ------------------------------------------------------------- |
| plan SUMMARYs → closing record | consolidated claims are attributed to their SUMMARY of origin |
| closing record → register      | the register outlives the phase; a bound omitted here is lost |

## STRIDE Threat Register

| Threat ID | Category    | Component                                           | Disposition | Mitigation Plan                                                                         |
| --------- | ----------- | --------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------- |
| T-98-18   | Repudiation | unattributed coverage claims                        | mitigate    | both scanners RUN by gates; the falsification drill lands in both colours               |
| T-98-19   | Tampering   | a definition or another phase's row edited at close | mitigate    | NAV-01 and COPY-09 asserted intact IN the gate; definition edits require a stated proof |
| T-98-20   | Repudiation | a uniform pass read as strength                     | mitigate    | WEAKEST POINT is mandatory and machine-pinned                                           |
| T-98-SC   | Tampering   | package installs                                    | accept      | zero installs this phase                                                                |

</threat_model>

<verification>
All three gates green; decision coverage uncovered []; drill row count equals derived gate
count; register rows flipped with bounds. POPULATION: the phase's own records, gates, and the
eight register rows. OUTSIDE IT: the product code (owned by 98-02..98-08), NAV-01 and every
other phase's row, ROADMAP checkbox edits (orchestrator close-out), and any independent
verification pass (a separate seat).
</verification>

<success_criteria>
The phase can be accepted or rejected from its own artifacts: coverage derived, every gate
drilled and verdicted, every bound listed, the weakest green named.
</success_criteria>

<output>
Create `.planning/phases/98-copy-truth/98-09-SUMMARY.md` when done. It MUST record: the
coverage derivation output verbatim; the decision-coverage result and both colours of its
falsification drill; the gate-count-to-row-count reconciliation; every handoff line written into
register bounds and its SUMMARY of origin; any single-writer violation found; and the weakest
point in one sentence, verbatim from the closing record so the two cannot drift.
</output>
