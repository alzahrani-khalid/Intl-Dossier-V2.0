---
phase: 98-copy-truth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-date-formatting.mjs # EXTEND — four new checks + burn-down allowlist
  - scripts/date-format-fixtures/ # NEW fixture files under the script's CLI-arg self-test dir (derive exact dir name from the script)
  - frontend/src/lib/format-date.ts # ADD formatRelativeTime — the ONE sanctioned relative-time helper (D-25)
autonomous: true
requirements: [COPY-05]
success_criterion: 'the drilled instrument for criterion 5 exists: the extended guard fails on planted fixtures for every new check, passes at HEAD via an enumerated burn-down allowlist, and the sanctioned relative-time helper lives in the formatter module'
truths:
  - 'Decisions covered — D-25: the one shared localized relative-time helper lands in the formatter module (frontend/src/lib/format-date.ts) before any feed surface routes onto it; D-06: every new check in the extended guard is shown RED on a planted fixture before its green over frontend/src is believed, and the guard already ships the CLI-arg fixture mechanism for exactly this; D-17: no file content is written through a shell-embedded node -e string; D-19: every package command runs as cd frontend && pnpm <script> because pnpm --filter frontend matches zero packages and exits 0.'
must_haves:
  truths:
    - 'check-date-formatting.mjs flags formatDistanceToNow/formatDistance outside lib/format-date.ts, date-fns skeleton literals (PPP/PP/PPpp), 12-hour h:mm a literals, and date-receiver .toLocaleString( — each proven red on a fixture'
    - 'The guard is GREEN at HEAD via an explicit burn-down allowlist enumerating the pre-existing violation sites, so intermediate-wave lint stays green until 98-07 empties the allowlist'
    - 'formatRelativeTime exists in format-date.ts: localized phrase in the session language (Arabic words under ar, Latin digits), em-dash for null/invalid, no ar-SA literal'
  artifacts:
    - path: 'frontend/src/lib/format-date.ts'
      provides: 'the sanctioned relative-time helper'
      contains: 'formatRelativeTime'
    - path: 'scripts/check-date-formatting.mjs'
      provides: 'the extended drilled instrument for criterion 5'
      contains: 'formatDistanceToNow'
  key_links:
    - from: 'scripts/check-date-formatting.mjs'
      to: 'frontend/src/lib/format-date.ts'
      via: 'the formatter module is the single allowed home of relative-time calls'
      pattern: 'format-date'
---

<objective>
Build criterion 5's drilled instrument before any date routing happens. Two deliverables:
(1) extend the existing lint-wired guard `scripts/check-date-formatting.mjs` to the four classes
its regexes currently cannot see, each new check proven red on a planted fixture via the script's
existing CLI-arg self-test mechanism; (2) add the ONE sanctioned localized relative-time helper
to `frontend/src/lib/format-date.ts` so plan 98-07's six feed surfaces have something to route
onto and the guard can ban direct date-fns relative calls everywhere else.

The guard must stay GREEN at HEAD: the ~30 pre-existing violation sites migrate in 98-07
(wave 4), so the extension ships with a burn-down ALLOWLIST enumerating them. An allowlist entry
is a named debt, not a pass — 98-07's gate requires the allowlist EMPTY.

Purpose: a routing sweep with no instrument behind it decays the day it lands; the extended
guard is what keeps criterion 5 true after the phase closes.
Output: extended script + fixtures + the helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-copy-truth/98-CONTEXT.md (D-25, D-06, D-17, D-19)
@.planning/phases/98-copy-truth/98-RESEARCH.md §C5 (the escaping-class table and derivation commands)
@scripts/check-date-formatting.mjs (read IN FULL — its check structure, allowlist mechanism, fixture-dir CLI arg, lint wiring)
@frontend/src/lib/format-date.ts (the four existing exports and the em-dash null convention)
@frontend/src/lib/query-client.ts (the proven i18n-singleton import pattern for non-React modules)

<interfaces>
Verified on disk 2026-08-18.

- `frontend/src/lib/format-date.ts` exports `formatDayFirst` (`Tue 28 Apr`), `formatTime`
  (`14:30 GST`), `formatDayFirstYear`, `formatDateTime`; em-dash `—` for null/invalid; Policy D
  Latin digits both locales.
- The i18n singleton import for non-React modules is proven at
  `frontend/src/lib/query-client.ts:15` — `import i18n from '@/i18n'`.
- date-fns is installed (80 importer files). Its `ar` locale interpolates counts via plain
  String() → Latin digits, satisfying Policy D. The guard bans the literal `'ar-SA'` — use the
  `ar` locale object import, never a locale-tag string.
- `scripts/check-date-formatting.mjs` is wired into `cd frontend && pnpm lint` and is GREEN at
  HEAD (1534 files scanned, 0 sites); it takes a fixture-dir CLI arg as a built-in
  positive-failure self-test.
- Escaping classes derived at HEAD (98-RESEARCH §C5, re-derive before enumerating the
  allowlist): `formatDistanceToNow`/`formatDistance(` in 21 files; skeleton `PPP`/`PP`/`PPpp`
  ≈9 sites; `h:mm a` 4 sites; Date-receiver `.toLocaleString(` 2 confirmed
  (`components/collaboration/ConflictResolutionDialog.tsx` ~:179, `components/commitments/StatusTimeline.tsx` ~:52).
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: formatRelativeTime — the one sanctioned helper, in the formatter module</name>
  <files>frontend/src/lib/format-date.ts</files>
  <read_first>frontend/src/lib/format-date.ts in full (naming, null convention, Policy D comments); frontend/src/lib/query-client.ts lines 1-30 (the i18n singleton import idiom and its in-code rationale comment)</read_first>
  <action>
    Add ONE export to `frontend/src/lib/format-date.ts`:
    `formatRelativeTime(value: string | Date | null | undefined): string`. Behavior: returns
    `—` (the module's existing em-dash convention) for null/undefined/invalid input; otherwise
    returns date-fns `formatDistanceToNow(date, { addSuffix: true, locale })` where `locale` is
    the date-fns `ar` locale object when the current language (read at CALL time from the
    `@/i18n` singleton, `i18n.language`, startsWith 'ar') is Arabic, and the date-fns default
    otherwise. Imports: `formatDistanceToNow` from `date-fns`, `ar` from `date-fns/locale`,
    `i18n` from `@/i18n` — mirror the query-client comment explaining the singleton is used
    because this module lives outside React context. NO `'ar-SA'` string literal anywhere (the
    guard bans it). Explicit return type per ESLint. Latin digits fall out of date-fns `ar`
    plural interpolation (Policy D — do not localize digits). This is the ONLY place in
    `frontend/src` where `formatDistanceToNow` may be called once 98-07 lands; the function's
    doc comment states the D-25 sanction: feed/timeline recency surfaces only.
  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && F="$R/frontend/src/lib/format-date.ts" && test -f "$F" && command grep -q "export function formatRelativeTime" "$F" && command grep -q "formatDistanceToNow" "$F" && test "$(command grep -c "ar-SA" "$F" || true)" -eq 0 && command grep -q "from '@/i18n'" "$F" && cd "$R/frontend" && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>The export exists by exact name `formatRelativeTime`; the file calls `formatDistanceToNow`; zero `ar-SA` literals (count in argument position with `|| true` so grep's no-match exit does not break the chain); the i18n singleton import is present; `pnpm type-check` (the real script name, D-19 — never `typecheck`, never `--filter frontend`) exits 0. MAX REACHABLE: all clauses reachable by the ~15-line addition; type-check is green at HEAD so a red is attributable to this edit.</acceptance_criteria>
  <done>The formatter module owns relative time; feed surfaces have a routing target.</done>
</task>

<task type="auto">
  <name>Task 2: extend the guard — four new checks, each proven red on a fixture, green at HEAD via burn-down allowlist</name>
  <files>scripts/check-date-formatting.mjs, scripts/date-format-fixtures/</files>
  <read_first>scripts/check-date-formatting.mjs in full — BEFORE writing anything: its existing check registry shape, how the fixture-dir CLI arg drives the self-test, how its current allowlist (the calendar data-day exemption) is expressed, and how pnpm lint invokes it (frontend package.json lint script does cd .. first)</read_first>
  <action>
    Extend `scripts/check-date-formatting.mjs` with four checks, following its existing check
    structure (extend, do not fork — the reuse rule of 98-RESEARCH §Don't Hand-Roll):

    1. RELATIVE-TIME: any `formatDistanceToNow` or `formatDistance(` token in
       `frontend/src/**/*.{ts,tsx}` OUTSIDE `frontend/src/lib/format-date.ts` (the module
       exemption is by path, not by allowlist — it is permanent).
    2. SKELETON: date-fns format literals `'PPP'`, `'PP'`, `'PPpp'`, `'PPP p'` (month-first
       renderers with no literal MMM token — the class the existing month-first check cannot
       see).
    3. TWELVE-HOUR: `h:mm a` inside a format literal.
    4. LOCALESTRING-DATE: `.toLocaleString(` where the receiver expression matches a date-ish
       name heuristic `/(date|time|_at|At|created|updated|timestamp)\w*\s*\)?\.toLocaleString\(/`
       — a heuristic, so false positives go in the allowlist with a reason, and the two
       confirmed offender shapes (ConflictResolutionDialog, StatusTimeline) must be caught.

    BURN-DOWN ALLOWLIST: re-derive the current violation population for each new check (the
    §C5 census commands, `command grep`, absolute paths), and enumerate every hit in an
    explicit allowlist structure in the script (file path + check name + the exact row marker
    token `P98-BURNDOWN`, annotated removed-by-98-07). That token appears ONLY in allowlist
    rows — never in comments, header prose, or check names — so 98-07's emptiness check
    (`grep -c P98-BURNDOWN == 0`) cannot false-red on documentation text. State what falls OUTSIDE each check's population in the script
    header: `yyyy-MM-dd` input value plumbing, `MMMM yyyy` calendar month-nav headers (D-25:
    navigation chrome, out), test files, and the existing exemptions. The script must exit 0
    against `frontend/src` at HEAD WITH the allowlist and exit non-zero if any allowlisted
    entry's file no longer contains the pattern (a stale allowlist row is itself a failure —
    self-cleaning), matching how strict burn-down lists behave.

    FIXTURES: under the script's existing fixture-dir mechanism add one fixture file per new
    check, each containing the minimal offending pattern (relative-time call, `'PPP'` literal,
    `h:mm a` literal, `createdAt.toLocaleString(` shape). Running the script against the
    fixture dir must report ALL FOUR new checks red. This is the D-06 drill built into the
    instrument.

    Do not touch the four existing checks. Do not rewire lint — the script is already wired.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && S="$R/scripts/check-date-formatting.mjs" && test -f "$S" && command grep -q "formatDistanceToNow" "$S" && command grep -q "PPpp\|'PPP'" "$S" && command grep -q "h:mm a" "$S" && command grep -q "toLocaleString" "$S" && command grep -q "P98-BURNDOWN" "$S" && test -d "$R/scripts/date-format-fixtures" && cd "$R" && if node "$S" "$R/scripts/date-format-fixtures" >/dev/null; then echo "FIXTURE RUN EXITED 0 — self-test failed to fire" && exit 1; fi && node "$S" >/dev/null && cd "$R/frontend" && pnpm lint</automated>
  </verify>
  <acceptance_criteria>The script names all four new patterns and carries the P98-BURNDOWN row marker (rows only, never prose — the token is 98-07's emptiness anchor); the fixture dir exists and running the script against it exits NON-ZERO (the if/then inversion makes a zero exit the failure — the positive-failure drill is in the gate, not just claimed); running the script bare against the repo exits 0 (allowlist covers HEAD); `cd frontend && pnpm lint` exits 0 end-to-end proving the wiring still carries all guard scripts green. MAX REACHABLE: fixture-red and repo-green are simultaneously reachable precisely because the allowlist enumerates HEAD's sites; if re-derivation finds the population moved from the §C5 figures, the allowlist follows the DERIVED set (D-04 — counts fall out, never quoted).</acceptance_criteria>
  <done>Criterion 5 has a drilled instrument that fails on every class it claims to check and passes at HEAD only through an enumerated, self-cleaning debt list.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                  |
| ------------------------------------ | ---------------------------------------------------------------------------- |
| guard verdict → every later C5 green | a check that cannot fire lets the seven formats regress silently after close |

## STRIDE Threat Register

| Threat ID | Category    | Component                            | Disposition | Mitigation Plan                                                                       |
| --------- | ----------- | ------------------------------------ | ----------- | ------------------------------------------------------------------------------------- |
| T-98-03   | Repudiation | a new check that never fired         | mitigate    | fixture run must exit non-zero IN the gate; a zero exit is an explicit gate failure   |
| T-98-04   | Tampering   | allowlist rows outliving their sites | mitigate    | stale-row detection: an allowlisted pattern absent from its file turns the script red |
| T-98-SC   | Tampering   | package installs                     | accept      | zero installs this phase                                                              |

</threat_model>

<verification>
Both gates green. POPULATION: the guard script, its fixtures, and the one helper export.
OUTSIDE IT: every call-site migration (98-07), the rendered-shape oracle (98-copy05 spec,
98-01), and the built-bundle fillMock oracle (98-07).
</verification>

<success_criteria>
The extended guard is red-able on demand, green at HEAD only via an enumerated burn-down list,
and the sanctioned helper exists in the formatter module with the D-25 sanction stated on it.
</success_criteria>

<output>
Create `.planning/phases/98-copy-truth/98-02-SUMMARY.md` when done. It MUST record: the fixture
run's non-zero exit with its per-check findings; the re-derived per-check populations WITH their
stated exclusions (D-05) and the allowlist row count per check; and the type-check/lint results.
</output>
