---
phase: 98-copy-truth
plan: 08
type: execute
wave: 5
depends_on: [98-01, 98-03, 98-04, 98-05, 98-06, 98-07]
files_modified:
  - .planning/phases/98-copy-truth/98-CAPTURED-LABELS.md # NEW — the captured set, verdicts, and repairs (the D-20 graded record)
  - frontend/src/i18n/en/common.json # representative capture target (nav labels live in common.json's navigation subtree — navigation.json does NOT exist, do not create it); the ACTUAL file set is the reverse-lookup of the captured labels, derived at execution, both locales, any namespace — every earlier lane has closed, so no collision is possible in this wave
  - frontend/src/i18n/ar/common.json
autonomous: true
requirements: [COPY-04]
success_criterion: "criterion 4's sentence-case clause, BOUNDED per D-20: the named instance plus every label the phase's oracle set captures on visited surfaces renders sentence case; the ~4.5k long tail stays COPY-09 → Phase 102, neither absorbed nor silently narrowed"
truths:
  - "Decisions covered — D-20: the sentence-case clause closes on (a) the named instance elected-officials:list.add and (b) every label the phase's oracle set captures on the surfaces it visits — the spec set defines visited, the oracle's captured set defines label; a surface this phase drives may not ship a Title Case label; the ~4.5k-string long tail is COPY-09, owner Phase 102, filed not absorbed and not silently narrowed further; D-04: the captured population is derived by running the oracle, never quoted from the ~4,471/4,562 magnitude figures; D-16: every repaired value's locale pair is reviewed in the same commit."
must_haves:
  truths:
    - 'The copy04 @case oracle ran over the phase-final visited surfaces and its captured set is recorded with a verdict per flagged label'
    - 'Every flagged bundle-sourced Title Case label is repaired to sentence case in both locales, or kept with a named carve-out reason (UPPERCASE ribbon / mono / table-column header / embedded proper noun)'
    - 'The copy04 spec runs green in FULL (@values and @case) in both legs'
    - 'The long tail is not touched: no value outside the captured set + named instance is edited'
  artifacts:
    - path: '.planning/phases/98-copy-truth/98-CAPTURED-LABELS.md'
      provides: 'the graded D-20 record: visited surfaces, captured labels, reverse-lookup sources, verdicts'
      contains: 'VERDICT'
  key_links:
    - from: 'tests/e2e/98-copy04-voice.spec.ts'
      to: '.planning/phases/98-copy-truth/98-CAPTURED-LABELS.md'
      via: 'the @case capture output is the record source — the oracle defines the population'
      pattern: '@case'
---

<objective>
Close criterion 4's bounded sentence-case clause. D-20 fixed the bound: the plan's spec set
defines "visited"; the oracle's captured set defines "label". This plan runs LAST among the
repair waves deliberately — the captured set must reflect the phase's FINAL surfaces (the
entityLinks labels 98-04 authored, the CTA 98-06 re-cased, the popover 98-03 opened), so a
label authored earlier in the phase that shipped Title Case is caught here rather than
grandfathered.

The population mechanism (from the 98-01 spec design): capture text of label-class elements
(buttons, tabs, h1-h3, nav links, empty-state headings) on every visited surface, EXCLUDING the
CLAUDE.md carve-outs (th column headers, uppercase-transformed ribbons, mono labels); keep only
strings that exact-match an EN i18n leaf value (reverse-bundle lookup — data-driven text such
as dossier names is data, not a label); flag Title Case. The flagged set IS the work list.

Purpose: no surface this phase drives ships a Title Case label; the tail stays filed, not
absorbed.
Output: the graded captured-set record + the repairs + a fully green copy04 oracle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-copy-truth/98-CONTEXT.md (D-20; the COPY-09 boundary)
@.planning/REQUIREMENTS.md COPY-09 row (~:183-192 — what stays OUT and why the tail is non-mechanical)
@tests/e2e/98-copy04-voice.spec.ts (the @case group — the population instrument)
@.planning/phases/98-copy-truth/98-RESEARCH.md §C4d (the scope hazard and the ~4.5k magnitude)

<interfaces>
- Visited surfaces (fixed by the 98-spec set — the D-20 "visited" definition, enumerated in the
  copy04 spec at 98-01): /dossiers hub, the EO list page, the intake ticket detail, the
  dashboard, the calendar surface, the signals list, the engagements list, my-work.
- Reverse-lookup source: `frontend/src/i18n/en/*.json` leaf values (129 namespaces). A captured
  string matching no leaf value is data → OUT, recorded as such.
- Repair rule: sentence case per CLAUDE.md — first word capitalized, the rest lowercase except
  embedded proper nouns (GASTAT, country names) which keep their casing and get a named
  carve-out line. AR values have no case; their pair is reviewed and edited only if the EN
  rewording changed meaning (it should not — case-only edits).
- The magnitude figures 4,471/4,562 are BOUNDED AWAY: they size COPY-09, not this plan. Editing
  a value the oracle never captured is scope creep INTO a filed requirement owned by P102.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: run the capture over the final surfaces; write the verdict record</name>
  <files>.planning/phases/98-copy-truth/98-CAPTURED-LABELS.md</files>
  <read_first>tests/e2e/98-copy04-voice.spec.ts (the @case capture mechanics and its exclusion selectors); .planning/REQUIREMENTS.md COPY-09 row (the boundary this record must not cross)</read_first>
  <action>
    Run the copy04 `@case` group against the wave-4-final tree (both legs; the EN leg carries
    the case population — Arabic has no letter case). Collect the flagged set. For every
    flagged label write a row in `98-CAPTURED-LABELS.md`: captured text → surface(s) → matched
    EN leaf key path(s) (a label can have several sources — list all; an unmatched capture is
    recorded DATA-NOT-LABEL) → VERDICT — row shape fixed so 98-08 Task 2's completion gate can count it:
    `| <label> | <surfaces> | <key paths> | REPAIR | <completion, blank until repaired> |` or
    `| <label> | <surfaces> | <key paths> | KEEP — <carve-out> | n/a |` (carve-outs: embedded
    proper noun, legitimate UPPERCASE class the selector exclusions missed, or an acronym). Also record the visited-surface list actually driven and
    any leg that could not be constructed. The record ends with the boundary statement: every
    EN leaf value NOT in this captured set is COPY-09 territory (P102) and was not edited.
  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && F="$R/.planning/phases/98-copy-truth/98-CAPTURED-LABELS.md" && test -f "$F" && command grep -q "VERDICT" "$F" && command grep -q "COPY-09" "$F" && command grep -qi "visited" "$F" && test "$(command grep -c "REPAIR\|KEEP" "$F")" -ge 1</automated>
  </verify>
  <acceptance_criteria>The record exists and carries the verdict vocabulary, the COPY-09 boundary statement, the visited-surface list, and at least one verdict row (zero rows would mean the phase's own surfaces ship no Title Case label — possible but implausible given the pre-phase de-facto Title Case; if genuinely zero, the record says so explicitly and Task 2 collapses to the spec run). The closed verdict vocabulary is REPAIR / KEEP — a flagged label with neither is an incomplete record (the 97 closed-vocabulary lesson: a missing term forces a wrong description). MAX REACHABLE: reachable wherever the spec set runs; the capture is read-only.</acceptance_criteria>
  <done>The D-20 population exists on disk as a verdicted record — the grading surface for the bounded clause.</done>
</task>

<task type="auto">
  <name>Task 2: repair every REPAIR row, both locales; prove the full copy04 oracle green</name>
  <files>frontend/src/i18n/en/common.json, frontend/src/i18n/ar/common.json — representative (the navigation subtree); the ACTUAL set is every file the verdict record names, derived by reverse-lookup at execution</files>
  <read_first>.planning/phases/98-copy-truth/98-CAPTURED-LABELS.md (the work list — its REPAIR rows and their key paths); each source JSON file the rows name</read_first>
  <action>
    For every REPAIR row: edit the EN leaf value to sentence case at its recorded key path(s) —
    ALL source paths for a label that appears in several namespaces, or the rendered surfaces
    diverge; review the AR pair in the same commit (edit only if meaning shifted — case-only
    edits should leave AR untouched, and the record says which). The frontmatter file list
    names the likely largest source (navigation labels); the ACTUAL set is the record's — every
    file it names is in scope for this task and no other file is (editing an uncaptured value
    is scope creep into COPY-09). Where a KEEP verdict exempted a label, no edit. Then run the
    copy04 spec IN FULL — both groups, both legs — and re-run the 98-06 exclamation floor
    walker as a regression control (case edits must not have introduced or removed a `!`).
  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && F="$R/.planning/phases/98-copy-truth/98-CAPTURED-LABELS.md" && test -f "$F" && test "$(command grep -c "| REPAIR |" "$F" || true)" -eq "$(command grep -c "| REPAIR |.*REPAIRED" "$F" || true)" && command grep -q '"add": "Add elected official"' "$R/frontend/src/i18n/en/elected-officials.json" && cd "$R/frontend" && pnpm type-check && cd "$R" && pnpm exec playwright test tests/e2e/98-copy04-voice.spec.ts --project=chromium-en --no-deps</automated>
  </verify>
  <acceptance_criteria>Every REPAIR row in the record carries a REPAIRED completion marker (the two counts are derived from the same file in the same run and must be EQUAL — a repaired-but-unmarked or marked-but-unrepaired row breaks the equality); the named instance still reads sentence case (regression pin from 98-06); type-check green; the copy04 spec passes IN FULL — the @case group green over the final surfaces IS the bounded clause's rendered closure, and @values green proves no voice regression. NOT CONSTRUCTED discipline for the spec half. MAX REACHABLE: the equality clause is reachable because this task writes both the repairs and the markers; @case green is reachable exactly when every captured Title Case label is repaired or KEEP-exempted, which is the clause's definition.</acceptance_criteria>
  <done>No surface this phase drives ships a Title Case label; the tail is P102's by name, untouched.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                                                                                          |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| captured set → grading | the oracle defines the population; a selector that under-captures narrows the clause silently — the record's visited-surface list is the audit trail |

## STRIDE Threat Register

| Threat ID | Category    | Component                          | Disposition | Mitigation Plan                                                                                                                                 |
| --------- | ----------- | ---------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| T-98-16   | Repudiation | silent narrowing of the D-20 bound | mitigate    | the record lists visited surfaces and label classes; KEEP verdicts carry named reasons; the boundary statement pins what was not edited and why |
| T-98-17   | Tampering   | scope creep into COPY-09           | mitigate    | only record-named key paths may be edited; the boundary statement is asserted in the gate                                                       |
| T-98-SC   | Tampering   | package installs                   | accept      | zero installs this phase                                                                                                                        |

</threat_model>

<verification>
Both gates green; copy04 fully green both legs. POPULATION: the captured-label set on the
visited surfaces plus the named instance. OUTSIDE IT: every EN value the oracle never captured
(COPY-09 → P102), AR case (no letter case), data-driven text, and the UPPERCASE/mono/th
carve-outs.
</verification>

<success_criteria>
Criterion 4's final clause closes exactly at the ruled bound: named instance + captured labels,
sentence case, both legs, with the tail filed and provably untouched.
</success_criteria>

<output>
Create `.planning/phases/98-copy-truth/98-08-SUMMARY.md` when done. It MUST record: the captured
count, REPAIR/KEEP split, per-KEEP reasons; the file set actually edited with en+ar commit
evidence; the exclamation-floor regression re-check result; and the full copy04 result with
locale+role per green.
</output>
