---
phase: 98-copy-truth
plan: 03
type: execute
wave: 2
depends_on: [98-01]
files_modified:
  - frontend/src/components/dossier/DossierTypeStatsCard.tsx # guard deletion + label t() routing — ONE owner for both criteria (CONTEXT Specific Ideas)
  - frontend/src/components/dossier/DossierTypeGuide.tsx # Crown + country/primary cases (D-27)
  - frontend/src/i18n/en/dossier.json # 5 EO keys + typeStats label key + Deadline value
  - frontend/src/i18n/ar/dossier.json # same keys, same commit (D-16)
autonomous: true
requirements: [COPY-07, COPY-08, COPY-04]
success_criterion: 'criteria 7 and 1(COPY-07): the EO popover renders header + description + all four sections with Crown on country/primary colors in both locales, the stats-card label localizes, and dossier.json ships no Due Date value'
truths:
  - 'Decisions covered — D-12: the five EO keys and the guard deletion land in the SAME change or not at all, closing on the rendered popover in both locales with no raw key and no empty section; D-13: the hardcoded percentage label is routed through t() with the STRING as the anchor, never the line number; D-27: getTypeIcon gains case elected_official returning Crown and getTypeColors gains case elected_official returning the country/primary set citing WR-07 — a Globe or muted popover green is a FAIL; D-28: EO gets NO new color family (seven semantic families are exhausted; revisit only if a designer allocates an eighth); D-29: NO new register row is invented for the glyph fix — the row mechanism is for work that leaves the phase; D-16: en and ar land in the same commit.'
must_haves:
  truths:
    - 'typeDescription.elected_official and typeGuide.elected_official.{whenToUse,examples,commonLinks,notFor} exist in BOTH locales with the UI-SPEC C7 shape bounds (examples array 3-5, commonLinks array 3-4)'
    - "The type !== 'elected_official' guard and its withhold comment block are deleted from DossierTypeStatsCard.tsx in the SAME commit"
    - 'DossierTypeGuide getTypeIcon/getTypeColors each carry a case for elected_official: Crown, and bg-primary/10 text-primary border-primary/30 with a WR-07 citation comment'
    - 'The percentage label renders through t(dossier:typeStats.percentOfActive); the EN value is byte-identical to the old literal so the en render is pixel-unchanged'
    - 'dossier.json dueDate display VALUE reads Deadline (EN) with its AR pair; the JSON key name and DB columns are untouched'
  artifacts:
    - path: 'frontend/src/i18n/en/dossier.json'
      provides: 'the five EO keys + the typeStats label key'
      contains: 'elected_official'
    - path: 'frontend/src/components/dossier/DossierTypeGuide.tsx'
      provides: 'the ninth-case glyph and color arms'
      contains: 'WR-07'
  key_links:
    - from: 'frontend/src/components/dossier/DossierTypeStatsCard.tsx'
      to: 'frontend/src/i18n/en/dossier.json'
      via: 'the DossierTypeGuide trigger now rendering for EO resolves typeDescription/typeGuide keys'
      pattern: 'DossierTypeGuide'
---

<objective>
The one atomic unit of the phase (D-12): five `dossier:` keys in both locales, the render-guard
deletion, and the two switch arms that keep the newly-rendering ninth case from wearing another
type's glyph (D-27) — one change, one commit. Plus the two small dossier.json residents that
must ride this lane because this plan owns the file: the COPY-07 label key and the criterion-4
`dueDate` display value.

Two criteria, one file, one owner — `DossierTypeStatsCard.tsx` carries both COPY-07 and COPY-08
and is never edited by two tasks in parallel (CONTEXT Specific Ideas).

Purpose: re-open the one surface Phase 97 deliberately closed, correctly, in both locales, with
the identity glyph the register promised.
Output: the rendered EO popover; the localized stats-card label.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-copy-truth/98-CONTEXT.md (D-12, D-13, D-16, D-27, D-28, D-29, D-30)
@.planning/phases/98-copy-truth/98-UI-SPEC.md §Component Contracts C7 (content shape bounds — read before authoring any key)
@.planning/phases/98-copy-truth/98-RESEARCH.md §C7 (re-verified key absence, guard anchor, silent-default anatomy)
@frontend/src/components/dossier/DossierTypeStatsCard.tsx
@frontend/src/components/dossier/DossierTypeGuide.tsx

<interfaces>
Verified on disk 2026-08-18 (line numbers drift; strings are the anchors — D-13).

- The guard: `{type !== 'elected_official' && (` at `DossierTypeStatsCard.tsx` ~:168, with the
  Phase 97 withhold comment block ~:160-167 above it. Both go.
- The card already maps EO to Crown: `case 'elected_official': return <Crown {...iconProps} />`
  at ~:95-96 — the popover must match it.
- `DossierTypeGuide.tsx`: `getTypeIcon` switch ~:60-81 (default returns Globe — the country
  glyph); `getTypeColors` switch ~:92-143 (default returns the muted set; the `country` case
  returns `{ bg: 'bg-primary/10', text: 'text-primary', border: 'border-primary/30' }` — THIS
  is the set EO gets, verbatim). WR-07 basis: `frontend/src/lib/semantic-colors.ts` ~:84 —
  `dossierTypeColors[type] ?? dossierTypeColors.country!` with `elected_official` absent from
  the map, so country/primary IS the canonical EO fallback today.
- Content consumers: `typeGuide.${type}.whenToUse`/`notFor` are read with `t(key, '')` silent
  defaults; `examples`/`commonLinks` via `returnObjects: true` behind `Array.isArray`/`length>0`
  guards (~:224,241) — a missing section VANISHES rather than leaking a key; `examples` renders
  `slice(0, 5)` Badge chips, `commonLinks` renders `slice(0, 4)` rows.
- `dossier.json` (both locales): `type.elected_official` EXISTS (the popover header title
  source); `typeDescription` has 8 keys with NO `elected_official`; `typeGuide` holds only the
  five flat section labels. The label literal `% of total active dossiers` sits in the card TSX
  at ~:228.
- Pre-existing drift recorded, NOT repaired (D-29): the guide's local color switch duplicating
  the canonical map is queued as one line on GUIDE-HOLLOW-01 (P102). The trigger's
  `t('typeGuide.learnMore', 'Learn more…')` inline-default is the P99 AR-04a class — record the
  interaction in the SUMMARY (D-03), do not sweep it.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: the atomic change — five keys ×2 locales + guard deletion + Crown/color arms</name>
  <files>frontend/src/i18n/en/dossier.json, frontend/src/i18n/ar/dossier.json, frontend/src/components/dossier/DossierTypeStatsCard.tsx, frontend/src/components/dossier/DossierTypeGuide.tsx</files>
  <read_first>frontend/src/components/dossier/DossierTypeGuide.tsx in full (both switches, the section anatomy, the silent-default guards); frontend/src/components/dossier/DossierTypeStatsCard.tsx (the guard block and the Crown mapping); .planning/phases/98-copy-truth/98-UI-SPEC.md §C7 content table (shape bounds are binding); frontend/src/i18n/en/dossier.json and ar/dossier.json (typeDescription/typeGuide shapes as they stand)</read_first>
  <action>
    ONE commit, four files, per D-12 + D-27. In `en/dossier.json`: add
    `typeDescription.elected_official` = `Track elected officials — their offices, terms, and engagement history with the organization.`;
    add subtree `typeGuide.elected_official` with `whenToUse` =
    `Create one dossier per elected official whose office, term, or relationship you need to track over time.`,
    `examples` = the 4-item array `Ministers`, `Members of parliament`, `Governors`,
    `Municipal council heads`, `commonLinks` = the 4-item array `Country dossiers`,
    `Engagements`, `Positions`, `Work items`, `notFor` =
    `Not for private-sector contacts or unelected officials — use a person dossier instead.`
    In `ar/dossier.json` the same paths:
    `تتبع المسؤولين المنتخبين — مناصبهم وفترات ولايتهم وسجل تواصلهم مع المنظمة.`;
    `أنشئ ملفاً واحداً لكل مسؤول منتخب تحتاج إلى تتبع منصبه أو فترة ولايته أو علاقته على مدى الزمن.`;
    examples `الوزراء`, `أعضاء البرلمان`, `المحافظون`, `رؤساء المجالس البلدية`; commonLinks
    `ملفات الدول`, `المشاركات`, `المواقف`, `بنود العمل`; notFor
    `ليس لجهات الاتصال من القطاع الخاص أو المسؤولين غير المنتخبين — استخدم ملف شخص بدلاً من ذلك.`
    (Values obey the voice law: sentence case, no exclamation, no first person, within the C7
    length bounds; the operator's Arabic-naturalness park stands over all ar copy as review
    debt, per D-30 — record it, do not block on it.)

    In `DossierTypeStatsCard.tsx`: delete the `type !== 'elected_official' &&` guard AND its
    ~:160-167 withhold comment block, leaving the trigger rendering for all nine cases.

    In `DossierTypeGuide.tsx`: `getTypeIcon` gains `case 'elected_official':` returning
    `<Crown {...iconProps} />` (add `Crown` to the existing lucide-react import); `getTypeColors`
    gains `case 'elected_official':` returning exactly
    `{ bg: 'bg-primary/10', text: 'text-primary', border: 'border-primary/30' }` with a one-line
    comment citing WR-07 (`semantic-colors.ts` falls back to the canonical country entry and
    elected_official is absent from that map — popover == card == canonical fallback). NO other
    change to either switch; NO refactor onto dossierTypeColors (pre-existing drift, P102 per
    D-29); NO new color family (D-28).

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && python3 -c "
import json,sys
for loc in ('en','ar'):
    d=json.load(open('$R/frontend/src/i18n/%s/dossier.json'%loc))
    td=d['typeDescription']['elected_official']; assert isinstance(td,str) and td.strip(), loc+' typeDescription'
    g=d['typeGuide']['elected_official']
    assert isinstance(g['whenToUse'],str) and g['whenToUse'].strip(), loc+' whenToUse'
    assert isinstance(g['examples'],list) and 3<=len(g['examples'])<=5, loc+' examples'
    assert isinstance(g['commonLinks'],list) and 3<=len(g['commonLinks'])<=4, loc+' commonLinks'
    assert isinstance(g['notFor'],str) and g['notFor'].strip(), loc+' notFor'
    for v in [td,g['whenToUse'],g['notFor']]+g['examples']+g['commonLinks']:
        assert '!' not in v, loc+' exclamation'
print('KEYS-OK')
" && C="$R/frontend/src/components/dossier/DossierTypeStatsCard.tsx" && G="$R/frontend/src/components/dossier/DossierTypeGuide.tsx" && test "$(command grep -c "type !== 'elected_official'" "$C" || true)" -eq 0 && command grep -q "DossierTypeGuide" "$C" && test "$(command grep -c "case 'elected_official'" "$G")" -eq 2 && command grep -q "WR-07" "$G" && command grep -q "Crown" "$G" && cd "$R/frontend" && pnpm type-check && cd "$R" && pnpm exec playwright test tests/e2e/98-copy08-eo-popover.spec.ts --project=chromium-en --no-deps</automated>
  </verify>
  <acceptance_criteria>The python key-walk asserts all five paths in BOTH locales with types and array-length bounds AND the no-exclamation voice check, failing with the locale+path name (never a bare assert); the guard count in the card is 0 with the chain-safe `|| true` form while `DossierTypeGuide` remains referenced in the same file (the instrument-control pair: the zero is about the guard, not about a gutted file); the guide carries EXACTLY 2 `case 'elected_official'` arms (icon + colors — 1 means the popover wears Globe or muted, 3 means a stray case), the WR-07 citation, and Crown; type-check green; the 98-copy08 spec — authored RED in 98-01 — runs GREEN, closing criterion 7 on the rendered popover in both legs (the spec's ar leg runs by ?lng=ar; its census half re-proves the keys statically). Dev-stack-dependent half: if the spec run cannot be constructed in this environment, record NOT CONSTRUCTED with what and why — the static clauses still hold the commit and 98-09's drill consolidates. MAX REACHABLE: every static clause reachable by the edits named; the spec clause reachable wherever 98-01's runs were.</acceptance_criteria>
  <done>The EO popover renders complete in both locales with the Crown on country/primary — the atomic unit is indivisible in history (one commit) and proven on the rendered surface.</done>
</task>

<task type="auto">
  <name>Task 2: the stats-card label through t() + the dossier.json Deadline value</name>
  <files>frontend/src/components/dossier/DossierTypeStatsCard.tsx, frontend/src/i18n/en/dossier.json, frontend/src/i18n/ar/dossier.json</files>
  <read_first>frontend/src/components/dossier/DossierTypeStatsCard.tsx (the label literal — search the STRING `% of total active dossiers`, not a line number, per D-13; and the component's existing useTranslation namespace); .planning/phases/98-copy-truth/98-UI-SPEC.md §C1 (EN byte-identical rule, AR bound of ≤2 wrapped lines at 1024px)</read_first>
  <action>
    Route the hardcoded label through i18n: add `typeStats.percentOfActive` to both dossier.json
    locales — EN value BYTE-IDENTICAL `% of total active dossiers` (the en render stays
    pixel-unchanged), AR value `النسبة من إجمالي الملفات النشطة` — and replace the JSX literal
    in `DossierTypeStatsCard.tsx` with the t() call against the component's existing dossier
    namespace binding (colon-form if the component's useTranslation default is not dossier).
    Same element, same classes, no truncation, no new styling.

    Second edit, same files: the criterion-4 retired-term value in dossier.json — the `dueDate`
    display VALUE (EN `Due Date` → `Deadline`; AR pair to the established Arabic for Deadline
    used by the glossary — align with the value calendar.json's chip repair will use in 98-06,
    `الموعد النهائي`). KEY name `dueDate` stays (renaming keys forces code edits for zero user
    value); DB columns are never touched (the CLAUDE.md carve-outs are correct as they stand).
    Derive the site by value-grep for `"Due Date"` in both dossier.json locales rather than
    trusting the audit's `en:962` offset.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && C="$R/frontend/src/components/dossier/DossierTypeStatsCard.tsx" && test "$(command grep -c "% of total active dossiers" "$C" || true)" -eq 0 && command grep -q "percentOfActive" "$C" && command grep -q '"percentOfActive": "% of total active dossiers"' "$R/frontend/src/i18n/en/dossier.json" && command grep -q "percentOfActive" "$R/frontend/src/i18n/ar/dossier.json" && test "$(command grep -c '"Due Date"' "$R/frontend/src/i18n/en/dossier.json" || true)" -eq 0 && command grep -q '"dueDate": "Deadline"' "$R/frontend/src/i18n/en/dossier.json" && cd "$R/frontend" && pnpm type-check && cd "$R" && pnpm exec playwright test tests/e2e/98-copy07-statscard.spec.ts --project=chromium-en --no-deps</automated>
  </verify>
  <acceptance_criteria>The literal is GONE from the TSX (zero-count, chain-safe) while the key token `percentOfActive` is PRESENT in the same file — absence + replacement asserted as a pair so a deleted-but-never-routed label cannot pass; the EN JSON value is byte-identical to the old literal (exact-string grep); the AR key exists; no `"Due Date"` VALUE survives in en/dossier.json while `"dueDate": "Deadline"` does (value changed, key name kept); type-check green; the 98-copy07 spec goes GREEN on its ar-leg discriminator. Same NOT CONSTRUCTED discipline as Task 1 for the spec half. MAX REACHABLE: all clauses reachable by the edits; the byte-identical clause is reachable because the EN value is copied, not rewritten.</acceptance_criteria>
  <done>Both dossier.json residents of criteria 1 and 4 are localized; the file's phase diff is complete in one lane.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                             |
| --------------------------------- | --------------------------------------------------------------------------------------- |
| authored ar copy → Arabic readers | naturalness is a P99/operator judgment; this phase proves resolution, never naturalness |

## STRIDE Threat Register

| Threat ID | Category    | Component                                                                      | Disposition | Mitigation Plan                                                                                  |
| --------- | ----------- | ------------------------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------ |
| T-98-05   | Spoofing    | EO popover wearing the country Globe                                           | mitigate    | D-27 arms + the spec's lucide-crown / text-primary assertions; a Globe green is a FAIL by ruling |
| T-98-06   | Repudiation | keys land without guard (dead bytes) or guard without keys (raw key on screen) | mitigate    | one commit, one task; the spec's presence assertions defeat the silent-default guards            |
| T-98-SC   | Tampering   | package installs                                                               | accept      | zero installs this phase                                                                         |

</threat_model>

<verification>
Both gates green; 98-copy08 and 98-copy07 specs green in both legs (or NOT CONSTRUCTED recorded
for consolidation). POPULATION: the four named files. OUTSIDE IT: the seven sibling guide bodies
(GUIDE-HOLLOW-01 → P102), the learnMore silent default (P99 AR-04a — interaction recorded in the
SUMMARY), NAV-01's register row (NEVER edited by any plan — overseer's close-out act), and the
guide's color-switch refactor (P102 per D-29).
</verification>

<success_criteria>
Criterion 7 closes on the rendered popover in both locales with the ruled glyph; COPY-07 closes
on the rendered card's ar leg; dossier.json carries no retired-term display value.
</success_criteria>

<output>
Create `.planning/phases/98-copy-truth/98-03-SUMMARY.md` when done. It MUST record: the single
atomic commit hash carrying all four Task-1 files; both spec results with locale+role stated per
green; the learnMore AR-04a interaction note (D-03); and the statement that NAV-01's row was not
touched.
</output>
