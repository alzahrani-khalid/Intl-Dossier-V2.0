---
phase: 98-copy-truth
plan: 04
type: execute
wave: 2
depends_on: [98-01]
files_modified:
  - frontend/src/i18n/en/common.json # entityLinks subtree (~82 keys) + toast.savedGeneric + loginSuccess de-exclamation + dueDate value
  - frontend/src/i18n/ar/common.json # same keys, same commits (D-16)
  - frontend/src/lib/query-client.ts # the COPY-06 one-liner
  - frontend/src/components/dossier/wizard/steps/CountryDetailsStep.tsx # regions casing normalize
  - frontend/src/components/dossier/wizard/review/CountryReviewStep.tsx # regions casing normalize
  - frontend/src/components/calendar/RecurrencePatternEditor.tsx # dot-form family → colon-form routing (D-22; count DERIVED at execution per D-04)
  - scripts/i18n-mask-audit.mjs # EXTEND to one-arg-aware (B6 — the raw-key class is one-arg t() calls, invisible to the 2-arg-only script at HEAD)
autonomous: true
requirements: [COPY-02, COPY-06, COPY-04]
success_criterion: 'criteria 2 and 6: every referenced entityLinks key resolves in both locales on driveable surfaces (census as backstop), the recurrence editor renders content not keys, and the default mutation toast is localized on a real mutation in both locales'
truths:
  - 'Decisions covered — D-24: ALL derived entityLinks keys (floor 82 across 8 files) are authored and registered in BOTH locales; the oracle closes on driveable rendered surfaces, the 82/82 census is a backstop not a closure, and any undriveable surface closes on census plus a named UNDRIVEN scope line, never silently; D-22: the calendar.recurrence instance is repaired by ROUTING — the WHOLE dot-form calendar.recurrence family in RecurrencePatternEditor.tsx flips to colon-form against the existing calendar namespace, no key authoring, with the site count DERIVED at execution per D-04 (the ruling quoted 16, an unverified figure; the checker derivation at HEAD found 43 occurrences over 37 distinct paths, all already resolving in en+ar calendar.json — the derived count governs and the delta is stated); D-23: every named criterion-2 instance is a CLASS — the dot-form-vs-namespace-miss population is swept by the EXTENDED one-arg-aware finder (the stock script matches only two-arg t() calls and cannot see the raw-key class it is assigned — extended in this plan, control shown firing) with the remainder bucketed by class, not just the named strings; D-10: i18n is static-bundled in frontend/src/i18n/index.ts, public/locales is dead, and the entityLinks subtree lands in common.json precisely because bare useTranslation() defaults there — zero component edits; D-11: the default mutation success toast is produced via t() inside the callback and renders localized in both locales on a real mutation — generic-but-localized is the accepted end state; D-30: the ratified toast literal is Changes saved EN with the authored ar equivalent.'
must_haves:
  truths:
    - 'Every static entityLinks.* key path referenced by the 6 components + 2 hooks resolves to a non-empty string in en AND ar common.json, plus the enumerable linkTypes/entityTypes dynamic-family values'
    - "toast.success('Operation completed successfully') is GONE from query-client.ts; toast.success(i18n.t('common:toast.savedGeneric')) fires INSIDE the callback"
    - 'RecurrencePatternEditor.tsx contains zero dot-form calendar.recurrence.* lookups, and the colon-form count EQUALS the pre-fix dot-form count derived from the parent git blob — a conservation check, never a quoted number'
    - 'The regions lookup normalizes casing so a capitalized data value (Europe) cannot miss the lowercase bundle keys'
    - 'The extended one-arg-aware i18n-mask-audit finder is shown FIRING on its known-present one-arg control before repairs, reports the entityLinks and calendar:recurrence families resolved after repair, prints its full result (no 8-row truncation), and the remaining population is bucketed by class with a verdict per class'
  artifacts:
    - path: 'frontend/src/i18n/en/common.json'
      provides: 'the entityLinks subtree + toast.savedGeneric'
      contains: 'entityLinks'
    - path: 'frontend/src/lib/query-client.ts'
      provides: 'the localized default success toast'
      contains: 'toast.savedGeneric'
  key_links:
    - from: 'frontend/src/components/entity-links/EntityLinkManager.tsx'
      to: 'frontend/src/i18n/en/common.json'
      via: 'bare useTranslation() defaultNS aliased to common.json resolves entityLinks.* without component edits'
      pattern: 'entityLinks'
---

<objective>
The common.json lane — the phase's highest-contention file gets ONE owner. Four repairs ride it:
the ~82-key `entityLinks` subtree in both locales (D-24 — the whole intake entity-links surface
renders raw keys today), the COPY-06 toast localization (D-11/D-30), the two criterion-4 values
that live in common.json (the `auth.loginSuccess` exclamation and the `dueDate` display value),
and the two routing repairs that share criterion 2's class: the recurrence editor's dot-form
family (D-22 — count derived at execution, never quoted) and the region-casing dynamic-key miss.

Mechanism choice (CONTEXT Claude's Discretion, decided here): the entityLinks subtree merges
into common.json rather than a new registered namespace — the 8 consumer files all call bare
`useTranslation()`, whose defaultNS aliases to common.json (D-10), so the merge needs ZERO
component edits; a new namespace would touch 8 code files for no user-visible gain.

Purpose: no raw key reaches the screen; every successful write announces itself in the session
language.
Output: resolved entity-links surfaces, a localized default toast, a swept dot-form class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-copy-truth/98-CONTEXT.md (D-10, D-11, D-16, D-22, D-23, D-24, D-30)
@.planning/phases/98-copy-truth/98-RESEARCH.md §C2, §C6 (leak mechanisms, the proven onError precedent)
@.planning/phases/98-copy-truth/98-UI-SPEC.md §C2, §C6 (label length bounds; toast copy contract)
@frontend/src/lib/query-client.ts
@frontend/src/components/entity-links/ (the six components)
@frontend/src/hooks/useEntityLinks.ts
@frontend/src/hooks/useAiSuggestions.ts

<interfaces>
Verified on disk 2026-08-18.

- The 8 entityLinks consumer files: `frontend/src/components/entity-links/{AISuggestionPanel,EntityLinkManager,EntitySearchDialog,LinkCard,LinkList,LinkTypeBadge}.tsx`,
  `frontend/src/hooks/useEntityLinks.ts`, `frontend/src/hooks/useAiSuggestions.ts` — all bare
  `useTranslation()`; plus `frontend/src/components/ai/EntityLinkSuggestions.tsx` renders the
  panel. Surface: `frontend/src/routes/_protected/intake/tickets.$id.tsx` (intake ticket
  detail). The sibling `en/entity-linking.json` namespace serves a DIFFERENT component family —
  do not conflate (98-RESEARCH §C2.1).
- The toast precedent, three lines up in the same file: `query-client.ts` `onError` calls
  `toast.error(i18n.t('common:errors.queryFailedInline'))` with `import i18n from '@/i18n'` at
  :15 and an in-code comment explaining the singleton pattern. The defect:
  `toast.success('Operation completed successfully')` in `mutations.onSuccess` (~:71).
- Region miss: `CountryDetailsStep.tsx` ~:99 and `CountryReviewStep.tsx` ~:34 build
  `t('form-wizard:regions.' + region)` while form-wizard.json keys are lowercase — a
  capitalized value misses.
- Recurrence: `frontend/src/components/calendar/RecurrencePatternEditor.tsx` — the dot-form
  `calendar.recurrence.` family against a namespace-less `useTranslation()` (~:176). Checker
  derivation at HEAD `87b2d040e`: **43 occurrences (41 single-quoted + 2 template-literal at
  ~:458, ~:525), 37 distinct key paths** — NOT the ruling's 16; re-derive at execution, the
  derived count governs (D-04). All 37 paths EXIST under the `calendar` namespace in BOTH
  locales (verified by the checker), reachable colon-form only — routing, no key authoring.
  Population = this ONE file (D-22 bounds it); any repo-wide token sweep must exclude the
  git-ignored `frontend/src/.understand-anything/.trash-*` extract artifacts, which duplicate
  components (scoping to the source file avoids the 49-hit repo-wide figure). D-22 at HEAD
  (CENSUS CORRECTED, `1a30f45e2`/`ddb6ec1f1`) now carries the DERIVATION COMMANDS themselves —
  pull the occurrence and distinct-path commands from `98-CONTEXT.md` D-22 and record both
  results in the SUMMARY; never freeze 43 or 37 as new literals.
- The finder: `scripts/i18n-mask-audit.mjs` — CORRECTED revision 1: at HEAD it matches ONLY
  two-arg `t('key', 'default')` calls, so the raw-key class (ONE-arg calls — all 43 recurrence
  sites and 52 of the 80 static entityLinks paths) is structurally invisible to it, and the
  earlier claim that `entityLinks.title` is its known-present control is FALSE
  (`EntityLinkManager.tsx:236` is one-arg and absent from its output). It also truncates output
  to 8 samples. This plan EXTENDS it (Task 3): a one-arg matcher resolved against the call
  site's effective namespace, full-list output, and the one-arg control shown firing.
- common.json criterion-4 residents (re-derive by value-grep): `auth.loginSuccess` carries the
  file's one `!`; one `"dueDate": "Due Date"` display value.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: derive the full entityLinks key census; author the core subtree, both locales</name>
  <files>frontend/src/i18n/en/common.json, frontend/src/i18n/ar/common.json</files>
  <read_first>all six frontend/src/components/entity-links/*.tsx and the two hooks (every t() call — the census source); frontend/src/i18n/en/common.json top-level shape (where the subtree lands and what must not be disturbed); .planning/phases/98-copy-truth/98-UI-SPEC.md §C2 (buttons ≤~24 chars, titles ≤~40, LinkTypeBadge 1–3 words, sentence case)</read_first>
  <action>
    DERIVE the population first (D-04/D-05): extract every static `entityLinks.*` key path
    referenced across the 8 files (command-grep the `t('entityLinks.` and template-literal
    forms; state what falls OUTSIDE — the two dynamic families handled in Task 2, test files,
    the unrelated entity-linking.json namespace). Record the derived list and count in the
    SUMMARY; the established floor is 82 total static paths (D-24) — if the derivation
    disagrees, the DERIVED set governs and the delta is stated.

    Author the CORE portion of the subtree (everything except the `aiSuggestions.*` branch,
    which Task 2 owns — split per D-15's ~120-line cap) into `en/common.json` and
    `ar/common.json` under a top-level `entityLinks` key: short mechanical UI labels per the
    UI-SPEC bounds — sentence case, no exclamation, no first person, no marketing voice; AR
    values are real copy (naturalness is P99 review debt). Both locales in the SAME commit
    (D-16). Key NAMES must byte-match the referenced paths — the consumers are not edited, so a
    misspelled key is an invisible miss until the census gate catches it.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && python3 -c "
import json
for loc in ('en','ar'):
    d=json.load(open('$R/frontend/src/i18n/%s/common.json'%loc))
    el=d.get('entityLinks'); assert isinstance(el,dict) and el, loc+' entityLinks subtree missing'
    def leaves(o):
        for v in o.values():
            if isinstance(v,dict): yield from leaves(v)
            else: yield v
    vals=list(leaves(el)); assert len(vals)>=50, loc+' core subtree too small: %d'%len(vals)
    assert all(isinstance(v,str) and v.strip() for v in vals), loc+' empty value'
print('CORE-OK')
" && python3 -c "
import json
en=json.load(open('$R/frontend/src/i18n/en/common.json'))
ar=json.load(open('$R/frontend/src/i18n/ar/common.json'))
def paths(o,p=''):
    for k,v in o.items():
        if isinstance(v,dict): yield from paths(v,p+k+'.')
        else: yield p+k
e=set(paths(en['entityLinks'])); a=set(paths(ar['entityLinks']))
assert e==a, 'EN/AR parity broken: %s'%(e^a)
print('PARITY-OK')
"</automated>
  </verify>
  <acceptance_criteria>Both locales carry a non-empty entityLinks subtree with ≥50 core leaf values (the floor for the core split; the FULL census closes in Task 2), every leaf a non-empty string, and EN/AR key-path parity EXACT — the symmetric difference is printed on failure so a one-locale drift names its keys. MAX REACHABLE: the ≥50 floor is reachable because the derived core set (82 minus the ~22 aiSuggestions branch) exceeds it; parity is reachable because both files are authored in the same task.</acceptance_criteria>
  <done>The core entity-links vocabulary exists in both locales under the namespace the consumers already resolve.</done>
</task>

<task type="auto">
  <name>Task 2: aiSuggestions branch + dynamic families; census gate closes 82/82; drive the surface</name>
  <files>frontend/src/i18n/en/common.json, frontend/src/i18n/ar/common.json</files>
  <read_first>frontend/src/hooks/useAiSuggestions.ts and frontend/src/components/entity-links/AISuggestionPanel.tsx (the aiSuggestions.* paths); frontend/src/components/entity-links/LinkTypeBadge.tsx (the entityLinks.linkTypes.${…} dynamic family and its value domain); the link-type CHECK constraint knowledge: link_type values are applies_to/related_to/endorsed_by/opposed_by (positions-attach lesson) — derive the actual domains from the types the components consume</read_first>
  <action>
    Author the `entityLinks.aiSuggestions.*` branch (~22 keys) in both locales, plus the two
    dynamic families' full value domains: `entityLinks.linkTypes.<each link_type value>` and
    `entityLinks.entityTypes.<each entity type value>` — DERIVE each domain from the TypeScript
    types/unions the components consume (state the derived domains and what falls outside:
    free-text values cannot be enumerated and are data, not copy). Both locales, same commit.

    Then run the census gate over the COMPLETE derived static-path list from Task 1 and assert
    100% resolution in both locales. Then drive the surface: run the 98-copy02 spec. Where a
    surface leg cannot be driven (expected: AI-suggestion accept/reject needing the AnythingLLM
    backend; a ticketless staging queue), the SUMMARY carries a named UNDRIVEN scope line per
    surface — census + UNDRIVEN line is the D-24 closure for exactly those, never silence.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && python3 -c "
import json,re,glob
root='$R/frontend/src'
files=glob.glob(root+'/components/entity-links/*.tsx')+[root+'/hooks/useEntityLinks.ts',root+'/hooks/useAiSuggestions.ts',root+'/components/ai/EntityLinkSuggestions.tsx']
refs=set()
for f in files:
    src=open(f).read()
    refs|=set(re.findall(r'entityLinks\.([A-Za-z0-9_.$#{}]*[A-Za-z0-9_])',src))
static={r for r in refs if '$' not in r and '{' not in r}
assert len(static)>=60, 'census floor: only %d static refs found — instrument problem?'%len(static)
en=json.load(open('$R/frontend/src/i18n/en/common.json'))['entityLinks']
ar=json.load(open('$R/frontend/src/i18n/ar/common.json'))['entityLinks']
def resolve(d,path):
    cur=d
    for seg in path.split('.'):
        if not isinstance(cur,dict) or seg not in cur: return None
        cur=cur[seg]
    return cur
missing=[(p,loc) for p in sorted(static) for loc,d in (('en',en),('ar',ar)) if not resolve(d,p)]
assert not missing, 'UNRESOLVED: %s'%missing[:20]
print('CENSUS-OK %d static paths resolved in both locales'%len(static))
" && command grep -q '"aiSuggestions"' "$R/frontend/src/i18n/en/common.json" && command grep -q '"aiSuggestions"' "$R/frontend/src/i18n/ar/common.json"</automated>
  </verify>
  <acceptance_criteria>The census instrument RE-DERIVES the referenced set from the 9 source files at gate time (never trusts Task 1's recorded list), floors it at 60 as an instrument sanity check (a lower count means the extraction regex broke, not that the work shrank — D-06), and asserts every static path resolves to a truthy leaf in BOTH locales, printing the first 20 misses by path+locale on failure; both aiSuggestions branches exist. The copy02 spec run MOVED to Task 3's gate (revision 1, B8 — copy02 drives the country wizard region step, whose casing fix lands in Task 3; a gate must not assert a state a later task in the same plan produces). MAX REACHABLE: census reachable once both tasks' authoring is complete; the derived count is expected near the 80-static floor and the SUMMARY states the actual.</acceptance_criteria>
  <done>The whole entityLinks namespace resolves — driveable surfaces proven rendered, the rest closed on census plus named UNDRIVEN lines.</done>
</task>

<task type="auto">
  <name>Task 3: the localized toast + routing repairs + the dot-form class sweep</name>
  <files>frontend/src/lib/query-client.ts, frontend/src/i18n/en/common.json, frontend/src/i18n/ar/common.json, frontend/src/components/dossier/wizard/steps/CountryDetailsStep.tsx, frontend/src/components/dossier/wizard/review/CountryReviewStep.tsx, frontend/src/components/calendar/RecurrencePatternEditor.tsx, scripts/i18n-mask-audit.mjs</files>
  <read_first>frontend/src/lib/query-client.ts lines 55-75 (the onError precedent and the defect); frontend/src/components/calendar/RecurrencePatternEditor.tsx (EVERY dot-form calendar.recurrence site — single-quoted AND the two template-literal forms — plus its useTranslation call; the repair is colon-form at the call sites, leaving the hook untouched unless every t() in the file is calendar-namespaced); scripts/i18n-mask-audit.mjs header (what the finder models)</read_first>
  <action>
    Four small repairs plus the extended finder, one lane (spec runs for copy02 AND copy06 live in THIS task's gate — after every repair they assert exists):

    1. TOAST (COPY-06, D-11/D-30): add `toast.savedGeneric` to both common.json locales — EN
       `Changes saved`, AR `تم حفظ التغييرات` — and replace the hardcoded literal in
       `query-client.ts` `mutations.onSuccess` with `toast.success(i18n.t('common:toast.savedGeneric'))`,
       calling `i18n.t` INSIDE the callback (the onError precedent — resolution at fire time in
       the current language, never at module import). No timing, no logic, no per-mutation copy
       (explicitly out, D-03).
    2. COMMON VOICE VALUES (COPY-04 residents of this file): de-exclaim `auth.loginSuccess` in
       both locales (drop the `!`, keep sentence case); change the `"dueDate"` display VALUE to
       `Deadline` / its AR pair (`الموعد النهائي`), key name unchanged. Derive both sites by
       value-grep, not offsets.
    3. REGIONS (criterion 2 class, dynamic-key casing): in `CountryDetailsStep.tsx` and
       `CountryReviewStep.tsx`, normalize the lookup segment to lowercase before interpolation
       so `Europe` resolves the lowercase `form-wizard:regions.europe` keys. No bundle edit.
    4. RECURRENCE ROUTING (D-22): DERIVE the population first — count `calendar.recurrence.`
       token occurrences in `RecurrencePatternEditor.tsx` (command grep, absolute path; 43
       occurrences / 37 distinct paths at check HEAD) and record the count plus the delta vs
       the ruling's 16 (D-04: derived governs). Then flip EVERY site — the single-quoted
       `t('calendar.recurrence.…')` forms AND the two template-literal forms — to
       `calendar:recurrence.…` colon-form against the registered calendar namespace, in ONE
       commit for this file (the gate's conservation check reads the parent blob). No key
       authoring, no wider hunt (the ruling bounds it to this one file).

    Then the CLASS SWEEP (D-23, instrument extended per B6): EXTEND
    `scripts/i18n-mask-audit.mjs` — add a ONE-ARG matcher (`t('dotted.key')` with no default,
    resolved against the call site's effective namespace exactly as the existing two-arg path
    does), remove the 8-sample truncation so the FULL result prints, keep the existing checks
    untouched, and give it a scan-directory CLI arg (mirror `check-date-formatting.mjs`'s
    existing fixture-dir mechanism) so the polarity controls below are GATE-EXECUTABLE, not
    prose. BOTH-POLARITY control (overseer requirement): before repairs the extended
    finder must FIRE on the known-present one-arg site `EntityLinkManager.tsx:236`
    `t('entityLinks.title')` (negative control), and after repairs it must report that same
    family resolved while still firing on a planted scratch one-arg fixture (positive+negative
    pair on the same instrument). Then run it and bucket the result BY CLASS (the one-arg
    population is ~501 sites / ~436 distinct keys repo-wide at check HEAD — a per-member
    verdict table at that size is noise): per class record repaired-here (this criterion's
    dot-form-vs-namespace misses), already-resolves (the `common.loading` /
    `afterActions.loadError` class — report, don't repair), silent-default mask (P99 AR-04a,
    recorded not swept), or named-with-owner. `CALENDAR.RECURRENCE.TITLE` uppercase is expected
    NOT reproducible at HEAD (98-RESEARCH A3) — report the named instance gone, the class
    covered.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && Q="$R/frontend/src/lib/query-client.ts" && test "$(command grep -c "Operation completed successfully" "$Q" || true)" -eq 0 && command grep -q "toast.success(i18n.t('common:toast.savedGeneric'))" "$Q" && command grep -q '"savedGeneric": "Changes saved"' "$R/frontend/src/i18n/en/common.json" && command grep -q "savedGeneric" "$R/frontend/src/i18n/ar/common.json" && P="$R/frontend/src/components/calendar/RecurrencePatternEditor.tsx" && test "$(command grep -c "calendar\.recurrence\." "$P" || true)" -eq 0 && POST=$(command grep -o "calendar:recurrence\." "$P" | wc -l | tr -d ' ') && test "$POST" -ge 1 && LASTC=$(cd "$R" && git log -n1 --format=%H -- frontend/src/components/calendar/RecurrencePatternEditor.tsx) && PRE=$(cd "$R" && git show "$LASTC^:frontend/src/components/calendar/RecurrencePatternEditor.tsx" | command grep -o "calendar\.recurrence\." | wc -l | tr -d ' ') && test "$PRE" -ge 1 && test "$POST" -eq "$PRE" && command grep -qi "toLowerCase" "$R/frontend/src/components/dossier/wizard/steps/CountryDetailsStep.tsx" && command grep -qi "toLowerCase" "$R/frontend/src/components/dossier/wizard/review/CountryReviewStep.tsx" && command grep -q "ONE_ARG\|one-arg\|oneArg" "$R/scripts/i18n-mask-audit.mjs" && DFIX="${TMPDIR:-/tmp}/p98-04-maskfix" && rm -rf "$DFIX" && mkdir -p "$DFIX/red" "$DFIX/clean" && printf '%s\n' "const a = t('zz.raw.key')" > "$DFIX/red/f.tsx" && printf '%s\n' "const b = 1" > "$DFIX/clean/f.tsx" && node "$R/scripts/i18n-mask-audit.mjs" "$DFIX/red" | command grep -q "zz.raw.key" && test "$(node "$R/scripts/i18n-mask-audit.mjs" "$DFIX/clean" | command grep -c "zz\." || true)" -eq 0 && cd "$R/frontend" && pnpm type-check && cd "$R" && test -f "$R/tests/e2e/98-copy02-rawkeys.spec.ts" && test -f "$R/tests/e2e/98-copy06-toast.spec.ts" && pnpm exec playwright test tests/e2e/98-copy02-rawkeys.spec.ts tests/e2e/98-copy06-toast.spec.ts --project=chromium-en --no-deps</automated>
  </verify>
  <acceptance_criteria>The English literal is GONE from query-client.ts (chain-safe zero) while the exact replacement call is PRESENT (pair assertion); both locale keys exist with the ratified EN literal `Changes saved` asserted byte-exactly; the recurrence file carries ZERO dot-form tokens AND the colon-form count EQUALS the dot-form count in the parent git blob of the last commit touching the file — a CONSERVATION check with both sides derived in the gate run, no quoted literal (the pure dot→colon flip preserves total occurrences; a missed site leaves dot-form non-zero, a stray or dropped site breaks the equality), and BOTH POLARITIES controlled per the overseer's vacuous-oracle requirement — PRE ≥ 1 is the negative control (the same token instrument shown finding the defect in the pre-fix blob) and POST == PRE with dot == 0 is the positive (the instrument passing on the repaired tree). The routing flip must land in ONE commit for this file — a second commit touching it re-bases the parent, and the executor then re-verifies conservation against the true pre-fix blob and states its hash; both wizard files normalize casing; the extended finder carries its one-arg marker AND its polarity controls are EXECUTED BY THE GATE (rev 3 — a control that is only described is not run): the red-fixture run must report the planted one-arg `zz.raw.key` and the clean-fixture run must report nothing zz-shaped, both against scratch dirs planted in the same chain; type-check green; BOTH specs go GREEN in this gate — copy02 (moved here per B8: the regions casing this task fixes is on its driven path; its census and recurrence-census legs are the D-24 closures) and copy06 (the rendered toast in both legs on a real mutation — a TASK kanban stage move, never a commitment — is the criterion-6 closure per D-11, with the HEAD control, the English literal under ?lng=ar, already recorded by 98-01). The mask-audit sweep's full unresolved list + per-member verdicts land in the SUMMARY; an untriaged member is a gate failure at close. MAX REACHABLE: all static clauses reachable by the four edits; the conservation equality is reachable because the flip is 1:1 by construction and both counts derive in the same run.</acceptance_criteria>
  <done>Every write announces itself in the session language; the dot-form class is swept, not just its named instances.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                                                                                                                  |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| authored labels → intake analysts | short mechanical labels replace raw keys on a trust-boundary surface (intake tickets carry user-submitted data); labels are static JSON, no interpolation of user data added |
| default toast → every mutation    | blast radius is app-wide; the change is copy-only and the oracle observes real behaviour                                                                                     |

## STRIDE Threat Register

| Threat ID | Category               | Component                              | Disposition | Mitigation Plan                                                                                                                                                                                                                       |
| --------- | ---------------------- | -------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | -------------------------------- |
| T-98-07   | Repudiation            | census green over a misspelled key     | mitigate    | census re-derives the referenced set from source at gate time; misses print by path+locale                                                                                                                                            |
| T-98-08   | Information disclosure | new copy leaking internals             | mitigate    | voice law bans dev vocabulary; the copy03 oracle greps /seed                                                                                                                                                                          | staging | test data/i on rendered surfaces |
| T-98-09   | Tampering              | i18next escaping disabled for new keys | accept      | escapeValue is FALSE at HEAD (pre-existing, deliberate — checker-verified); no interpolation options are touched and every new key is a static label interpolating no user data, so the exposure is unchanged; do not flip the option |
| T-98-SC   | Tampering              | package installs                       | accept      | zero installs this phase                                                                                                                                                                                                              |

</threat_model>

<verification>
All three gates green; copy02 and copy06 specs green on driveable legs with UNDRIVEN lines named.
POPULATION: the six named files and the entityLinks/aiSuggestions subtrees. OUTSIDE IT: the
entity-linking.json namespace (different family), the silent-default mask class (P99 AR-04a),
per-mutation specific copy (deferred by ruling), and every other common.json subtree.
</verification>

<success_criteria>
Criterion 2 closes on driven surfaces + census backstop + named UNDRIVEN lines; criterion 6
closes on the rendered toast in both locales; common.json's phase diff is complete in one lane.
</success_criteria>

<output>
Create `.planning/phases/98-copy-truth/98-04-SUMMARY.md` when done. It MUST record: the derived
entityLinks census (count, definition, exclusions — D-05) and its delta vs the 82 floor; every
UNDRIVEN surface line; the mask-audit unresolved list with per-member verdicts; the mutation the
copy06 spec drove and both locale observations; and the commit hashes showing en+ar landed
together.
</output>
