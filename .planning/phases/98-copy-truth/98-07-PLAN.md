---
phase: 98-copy-truth
plan: 07
type: execute
wave: 4
depends_on: [98-01, 98-02, 98-05]
files_modified:
  - frontend/src/components/activity-feed/EnhancedActivityFeed.tsx # SANCTIONED feed → formatRelativeTime
  - frontend/src/pages/Dashboard/components/ActivityFeedItem.tsx # SANCTIONED feed
  - frontend/src/pages/dossiers/overview-cards/SharedRecentActivityCard.tsx # SANCTIONED timeline
  - frontend/src/components/comments/CommentItem.tsx # SANCTIONED discussion timeline
  - frontend/src/components/notifications/NotificationItem.tsx # SANCTIONED notification feed
  - frontend/src/pages/Dashboard/widgets/RecentDossiers.tsx # SANCTIONED recency widget
  - frontend/src/components/audit-logs/AuditLogTable.tsx # MIGRATE (date of record)
  - frontend/src/components/briefing-books/BriefingBooksList.tsx # MIGRATE
  - frontend/src/components/duplicate-detection/DuplicateCandidateCard.tsx # MIGRATE
  - frontend/src/components/form-auto-save/AutoSaveIndicator.tsx # MIGRATE
  - frontend/src/components/form-auto-save/FormDraftBanner.tsx # MIGRATE
  - frontend/src/components/intelligence/BilateralOpportunities.tsx # MIGRATE
  - frontend/src/components/intelligence/EconomicDashboard.tsx # MIGRATE
  - frontend/src/components/intelligence/PoliticalAnalysis.tsx # MIGRATE
  - frontend/src/components/intelligence/SecurityAssessment.tsx # MIGRATE
  - frontend/src/components/scenario-sandbox/ScenarioCard.tsx # MIGRATE
  - frontend/src/components/scheduled-reports/ScheduledReportsManager.tsx # MIGRATE
  - frontend/src/components/workflow-automation/WorkflowExecutionsList.tsx # MIGRATE
  - frontend/src/components/workflow-automation/WorkflowRuleCard.tsx # MIGRATE
  - frontend/src/pages/my-work/components/WorkItemCard.tsx # MIGRATE
  - frontend/src/routes/_protected/tags.tsx # MIGRATE
  - frontend/src/components/collaboration/ConflictResolutionDialog.tsx # toLocaleString-Date class
  - frontend/src/components/commitments/StatusTimeline.tsx # toLocaleString-Date class
  - frontend/src/components/intake-form/IntakeForm.tsx # fillMock label inlined in the DEV block (D-26)
  - frontend/src/i18n/en/intake.json # fillMock key DELETED
  - frontend/src/i18n/ar/intake.json # fillMock key DELETED, same commit
  - scripts/check-date-formatting.mjs # burn-down allowlist emptied (plus derived skeleton/12h/Intl.DateTimeFormat sites, resolved at execution)
autonomous: true
requirements: [COPY-05]
success_criterion: 'criterion 5: every date renders Tue 28 Apr and every time 14:30 GST from the one formatter; relative time survives ONLY on the six enumerated feed surfaces through the shared localized helper; the dev-affordance string is absent from the BUILT bundle with a positive control'
truths:
  - 'Decisions covered — D-25: relative time is sanctioned on feed/timeline recency surfaces ONLY, via the one shared localized helper in the formatter module, and THIS PLAN CARRIES THE GRADED ENUMERATION: EnhancedActivityFeed, Dashboard ActivityFeedItem, SharedRecentActivityCard, CommentItem, NotificationItem, and RecentDossiers are the feed surfaces; all other formatDistanceToNow sites migrate to the day-first formatter; MMMM yyyy calendar-grid month headers are navigation chrome OUT of the population; D-26: intake:fillMock is deleted from both locales, the label is inlined in the DEV-gated block, and the oracle greps the BUILT bundle with a positive control; D-15: the fifteen-file migration task exceeds the ~10-file cap with cause stated — it is a single mechanical class repair at ~2 lines per file, well under the 120-line diff cap; D-18: absolute paths in every command.'
must_haves:
  truths:
    - 'Zero formatDistanceToNow/formatDistance call sites exist outside lib/format-date.ts; the six sanctioned surfaces call formatRelativeTime'
    - 'The check-date-formatting burn-down allowlist is EMPTY and the guard is green strict'
    - 'The EMITTED production code (js/css, sourcemaps excluded) contains zero CASE-INSENSITIVE occurrences of the affordance string fill with mock data — banning BOTH the deleted capitalised JSON value and the inlined lowercase dev literal — while the positive-control string is found in the same non-map artifact set'
    - 'A sanctioned feed under ?lng=ar renders an Arabic relative phrase with Latin digits'
  artifacts:
    - path: 'frontend/src/components/notifications/NotificationItem.tsx'
      provides: 'a sanctioned feed routed through the shared helper'
      contains: 'formatRelativeTime'
    - path: 'frontend/src/i18n/en/intake.json'
      provides: 'the intake namespace with the dev-affordance key gone'
      contains: 'actions'
  key_links:
    - from: 'frontend/src/components/activity-feed/EnhancedActivityFeed.tsx'
      to: 'frontend/src/lib/format-date.ts'
      via: 'formatRelativeTime import — the ONE sanctioned relative-time path'
      pattern: 'formatRelativeTime'
---

<objective>
Criterion 5's routing sweep. The population was derived in 98-02 and enumerated into the guard's
burn-down allowlist; this plan empties it. The D-25 enumeration is fixed here and graded:

SANCTIONED (feed/timeline recency surfaces — content is a reverse-chronological stream whose
primary meaning is how recent): `EnhancedActivityFeed`, Dashboard `ActivityFeedItem`,
`SharedRecentActivityCard`, `CommentItem`, `NotificationItem`, `RecentDossiers`. These six route
onto `formatRelativeTime` (localized: Arabic words under ar, Latin digits per Policy D).

MIGRATE (the timestamp identifies WHEN, for the record): the other fifteen derived files —
audit logs, briefing books, duplicate detection, auto-save indicator and draft banner, the four
intelligence dashboards, scenario cards, scheduled reports, both workflow-automation surfaces,
work-item cards, tags — plus the skeleton-pattern (`PPP`/`PP`), 12-hour (`h:mm a`),
Date-receiver `.toLocaleString(`, and `Intl.DateTimeFormat` classes, all onto
`formatDayFirst`/`formatDayFirstYear`/`formatTime`/`formatDateTime`.

Plus D-26: the `fillMock` key leaves BOTH locales, the label inlines into the DEV-gated block,
and the claim closes on the BUILT bundle.

Purpose: one date language, everywhere a user reads time.
Output: an empty burn-down list, a strict-green guard, a bundle proven clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-copy-truth/98-CONTEXT.md (D-15, D-18, D-25, D-26)
@.planning/phases/98-copy-truth/98-RESEARCH.md §C5 (escaping-class table, census commands, the Vite DCE mechanism note and its A2 assumption)
@.planning/phases/98-copy-truth/98-UI-SPEC.md §C5 (dates are BODY text, never mono; no new dir spans; no new CSS)
@frontend/src/lib/format-date.ts (the four exports + formatRelativeTime from 98-02)
@scripts/check-date-formatting.mjs (the extended guard and its burn-down allowlist)

<interfaces>
Verified on disk 2026-08-18.

- The 21-file formatDistanceToNow population (files list in frontmatter) was derived with
  `command grep -rln "formatDistanceToNow\|formatDistance(" frontend/src --include="*.tsx" --include="*.ts"`
  excluding tests. Re-derive at execution; the derived set governs (D-04). What falls OUTSIDE
  (D-05): `lib/format-date.ts` itself (the helper's home — permanent path exemption),
  `__tests__`, and `MMMM yyyy` month-nav headers (D-25).
- toLocaleString-Date confirmed sites: `ConflictResolutionDialog.tsx` ~:179-180,
  `StatusTimeline.tsx` ~:52 → `formatDateTime`.
- fillMock anchors: `en/intake.json:86` and `ar/intake.json:86` `"fillMock"` under `actions`;
  single consumer `IntakeForm.tsx` ~:480 `{t('actions.fillMock')}` inside the
  `{import.meta.env.DEV && (…)}` block (~:461-482). Re-grep `fillMock` repo-wide at execution
  for stray consumers (98-RESEARCH A5).
- Vite statically replaces `import.meta.env.DEV` and eliminates the block in production
  (corroborated in-repo at `FullscreenLoader.tsx` ~:39-41); the bundle grep is the proof either
  way — if the inlined literal survives DCE, the oracle catches it and the fix stays key-deletion
  (the A2 fallback).
- AutoSaveIndicator note: `Saved N minutes ago` becomes `formatTime` (`Saved 14:32 GST`) — a
  form status line is not a feed (the classification rule, stated in the objective, governs).
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: route the six sanctioned feeds onto formatRelativeTime</name>
  <files>frontend/src/components/activity-feed/EnhancedActivityFeed.tsx, frontend/src/pages/Dashboard/components/ActivityFeedItem.tsx, frontend/src/pages/dossiers/overview-cards/SharedRecentActivityCard.tsx, frontend/src/components/comments/CommentItem.tsx, frontend/src/components/notifications/NotificationItem.tsx, frontend/src/pages/Dashboard/widgets/RecentDossiers.tsx, scripts/check-date-formatting.mjs</files>
  <read_first>frontend/src/lib/format-date.ts (formatRelativeTime signature and its D-25 sanction comment); each of the six files' current formatDistanceToNow call shape (locale options they pass today, if any — the helper subsumes them)</read_first>
  <action>
    In each of the six sanctioned files: replace the direct `formatDistanceToNow` import/call
    with `formatRelativeTime` from `@/lib/format-date` — same element, same position, no
    styling change; delete the now-unused date-fns relative imports (and any per-file locale
    plumbing they carried — the helper owns locale now). Remove the six files' rows from the
    guard's burn-down allowlist in the same commit, so the guard itself proves the migration
    (a lingering row would turn the guard red via its stale-row rule; a lingering call site
    would turn it red via the relative-time check).
  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && for f in components/activity-feed/EnhancedActivityFeed.tsx pages/Dashboard/components/ActivityFeedItem.tsx pages/dossiers/overview-cards/SharedRecentActivityCard.tsx components/comments/CommentItem.tsx components/notifications/NotificationItem.tsx pages/Dashboard/widgets/RecentDossiers.tsx; do command grep -q "formatRelativeTime" "$R/frontend/src/$f" || { echo "MISSING helper in $f"; exit 1; }; test "$(command grep -c "formatDistanceToNow" "$R/frontend/src/$f" || true)" -eq 0 || { echo "RAW date-fns call survives in $f"; exit 1; }; done && cd "$R" && node "$R/scripts/check-date-formatting.mjs" >/dev/null && cd "$R/frontend" && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>All six files import the helper AND carry zero direct formatDistanceToNow tokens — checked per file with the failure naming the file; the guard runs green (its stale-row rule makes a forgotten allowlist row red, its relative-time check makes a forgotten call site red — the gate leans on the drilled instrument, not on this chain alone); type-check green. MAX REACHABLE: all clauses reachable by the six mechanical swaps plus six allowlist row removals.</acceptance_criteria>
  <done>Relative time exists only where D-25 sanctions it, through the one localized helper.</done>
</task>

<task type="auto">
  <name>Task 2: migrate the fifteen date-of-record files off relative time</name>
  <files>frontend/src/components/audit-logs/AuditLogTable.tsx, frontend/src/components/briefing-books/BriefingBooksList.tsx, frontend/src/components/duplicate-detection/DuplicateCandidateCard.tsx, frontend/src/components/form-auto-save/AutoSaveIndicator.tsx, frontend/src/components/form-auto-save/FormDraftBanner.tsx, frontend/src/components/intelligence/BilateralOpportunities.tsx, frontend/src/components/intelligence/EconomicDashboard.tsx, frontend/src/components/intelligence/PoliticalAnalysis.tsx, frontend/src/components/intelligence/SecurityAssessment.tsx, frontend/src/components/scenario-sandbox/ScenarioCard.tsx, frontend/src/components/scheduled-reports/ScheduledReportsManager.tsx, frontend/src/components/workflow-automation/WorkflowExecutionsList.tsx, frontend/src/components/workflow-automation/WorkflowRuleCard.tsx, frontend/src/pages/my-work/components/WorkItemCard.tsx, frontend/src/routes/_protected/tags.tsx, scripts/check-date-formatting.mjs</files>
  <read_first>each file's relative-time call in context (what the timestamp MEANS there decides formatDayFirst vs formatDayFirstYear vs formatDateTime vs formatTime — a same-year list date takes formatDayFirst, a cross-year record takes formatDayFirstYear, a log row takes formatDateTime, the auto-save line takes formatTime); frontend/src/lib/format-date.ts (the export set)</read_first>
  <action>
    Replace every relative-time call in the fifteen files with the appropriate format-date
    export (per-file choice recorded in the SUMMARY); delete the unused date-fns imports;
    remove the fifteen burn-down rows in the same commit. D-15 deviation stated with cause:
    fifteen files exceeds the ~10-file cap, but this is one mechanical class repair at ~2 lines
    per file (~30 diff lines, far under the 120-line cap) — splitting it would manufacture a
    plan boundary inside a single derived population for no quality gain. If execution
    re-derivation finds the population moved (a 22nd file, a removed one), the DERIVED set
    governs and the delta is stated (D-04).
  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && HITS=$(command grep -rln "formatDistanceToNow\|formatDistance(" "$R/frontend/src" --include="*.tsx" --include="*.ts" | command grep -v __tests__ | command grep -v "\.test\." | command grep -v "lib/format-date.ts" | wc -l | tr -d ' ') && test "$HITS" -eq 0 && test "$(command grep -c "formatDistanceToNow" "$R/frontend/src/lib/format-date.ts")" -ge 1 && cd "$R" && node "$R/scripts/check-date-formatting.mjs" >/dev/null && cd "$R/frontend" && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>The repo-wide census returns ZERO files calling relative-time APIs outside the formatter module — and the SAME instrument in the SAME chain returns ≥1 on format-date.ts itself, the known-present control that proves the grep read the tree (D-06: an unfiltered-zero without its control is not evidence); the guard runs green; type-check green. MAX REACHABLE: zero is reachable exactly when Tasks 1-2 are complete, because 98-02's derivation enumerated every member and the allowlist's stale-row rule catches drift.</acceptance_criteria>
  <done>Nine-months-ago is gone as a hardcoded English pattern everywhere it was a date of record.</done>
</task>

<task type="auto">
  <name>Task 3: the remaining format classes + fillMock out of the build + the strict-green gate</name>
  <files>frontend/src/components/collaboration/ConflictResolutionDialog.tsx, frontend/src/components/commitments/StatusTimeline.tsx, frontend/src/components/intake-form/IntakeForm.tsx, frontend/src/i18n/en/intake.json, frontend/src/i18n/ar/intake.json, scripts/check-date-formatting.mjs</files>
  <read_first>the 98-02 allowlist (the remaining rows ARE the work list: skeleton PPP/PP sites, h:mm a sites, the two toLocaleString-Date sites, any Intl.DateTimeFormat rows); frontend/src/components/intake-form/IntakeForm.tsx ~:455-485 (the DEV block and the t() call); both intake.json files at the fillMock key</read_first>
  <action>
    Work the allowlist to empty: route each remaining skeleton-pattern (`PPP`/`PP`/`PPpp`) and
    12-hour (`h:mm a`) site onto the matching format-date export; the two Date-receiver
    `.toLocaleString(` sites onto `formatDateTime`; triage each `Intl.DateTimeFormat` row with
    a verdict — month-nav headers are navigation chrome OUT per D-25 (they leave the allowlist
    by becoming a stated permanent exemption in the script header, named, with the D-25
    citation), true date-of-record renders migrate. Every row leaves the allowlist by REPAIR or
    by NAMED EXEMPTION — never by silent deletion.

    fillMock (D-26): delete the `fillMock` key from BOTH intake.json locales; in the DEV-gated
    block replace `{t('actions.fillMock')}` with the inline literal `Fill with mock data`
    (dev-only copy needs no i18n and no Title Case); re-grep `fillMock` repo-wide for stray
    consumers first (A5 — expected: the one).

    Then the build-artifact oracle: `cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0/frontend && pnpm build`,
    then CASE-INSENSITIVELY grep the dist assets EXCLUDING `*.map` for the affordance string
    (expect zero files). The `*.map` exclusion is a POPULATION statement, not a convenience:
    `vite.config.ts` sets `sourcemap: true`, so dist carries ~305 `.map` files whose
    `sourcesContent` embeds original sources verbatim — the current build's IntakeForm map
    already proves the mechanism (it embeds `actions.fillMock`), so once the literal is inlined
    every rebuild embeds it in a `.map` FOREVER and an unexcluded grep is unsatisfiable.
    Sourcemaps are debug artifacts OUTSIDE the render path — a browser fetches them only when
    devtools ask; a string that exists only in a `.map` was never user copy, so the exclusion
    DEFINES criterion 5's population (renderable strings in the executable bundle) rather than
    narrowing it. And the fact that verbatim sources ship at all is not waved away — it is
    QUEUED as `CLIENTSEC-02` (REQUIREMENTS.md:349, owner Phase 100, filed `2aad479bc`); no
    Phase 98 plan touches `vite.config.ts`. The -i pattern must catch the deleted capitalised JSON
    value AND the inlined lowercase literal, or the oracle is blind to whichever survives. Same
    exclusion on the positive control (a `.map` hit could otherwise certify a build whose JS
    chunk never emitted) — both controls
    (`Changes saved` — a literal this phase itself put in the bundle) in the same run. Finish
    with the full criterion-5 rendered oracle and the lint chain.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && test "$(command grep -c "fillMock" "$R/frontend/src/i18n/en/intake.json" || true)" -eq 0 && test "$(command grep -c "fillMock" "$R/frontend/src/i18n/ar/intake.json" || true)" -eq 0 && command grep -q "Fill with mock data" "$R/frontend/src/components/intake-form/IntakeForm.tsx" && command grep -q "import.meta.env.DEV" "$R/frontend/src/components/intake-form/IntakeForm.tsx" && test "$(command grep -c "P98-BURNDOWN" "$R/scripts/check-date-formatting.mjs" || true)" -eq 0 && cd "$R" && node "$R/scripts/check-date-formatting.mjs" >/dev/null && cd "$R/frontend" && pnpm build >/dev/null && NEGCTRL=$(command grep -ci "fill with mock data" "$R/frontend/src/components/intake-form/IntakeForm.tsx") && test "$NEGCTRL" -ge 1 && DEVHIT=$(command grep -ril --exclude='*.map' "fill with mock data" "$R/frontend/dist/assets" | wc -l | tr -d ' ') && test "$DEVHIT" -eq 0 && CTRL=$(command grep -rl --exclude='*.map' "Changes saved" "$R/frontend/dist/assets" | wc -l | tr -d ' ') && test "$CTRL" -ge 1 && pnpm lint && cd "$R" && pnpm exec playwright test tests/e2e/98-copy05-dates.spec.ts --project=chromium-en --no-deps</automated>
  </verify>
  <acceptance_criteria>The key is gone from BOTH locales (per-file zero, chain-safe) while the inline literal AND the DEV gate both survive in IntakeForm (the pair proves the affordance still exists in dev — deletion of the whole block would be scope creep); the burn-down allowlist rows are ZERO so the guard now runs strict; the EMITTED code (js/css, `--exclude='*.map'` on BOTH greps — sourcemaps embed sources verbatim via sourcesContent and sit outside the render path — never shipped UI; the exclusion is stated as a population bound with the queued sibling fact CITED (`CLIENTSEC-02` → Phase 100 owns the sources-ship-at-all disclosure), so it cannot be read as narrowing-to-green, and without it the gate is unsatisfiable-forever the moment the literal is inlined) contains zero CASE-INSENSITIVE hits of the affordance string — the ban set covers both the deleted capitalised JSON value and the lowercase inlined literal, so the clause discriminates whether or not DCE stripped the DEV block — while BOTH polarity controls ride the same run (overseer requirement on the vacuous-oracle class): the NEGATIVE control shows the identical case-insensitive pattern firing ≥1 on the known-present source literal in IntakeForm.tsx — a pattern that cannot find the string where it provably exists is a pattern that never ran — and the POSITIVE control shows `Changes saved` landing ≥1 in the SAME artifacts dir, proving the build produced output and the instrument reads it (a zero without its control could mean the build never ran, D-26's exact reasoning); `pnpm lint` green carries all guard scripts; the 98-copy05 spec — RED at HEAD on the ago-phrase — goes GREEN in both legs including the Arabic relative phrase on a sanctioned feed. NOT CONSTRUCTED discipline for the spec half only; the build oracle is constructible anywhere node runs. MAX REACHABLE: every clause reachable once the allowlist rows are worked to zero; if Vite DCE fails to strip the inline literal (A2), the DEVHIT clause catches it and the fallback is stated in the SUMMARY (label removed rather than inlined).</acceptance_criteria>
  <done>One date formatter, six sanctioned feeds, a strict guard, and a bundle that provably ships no dev affordance string.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                                              |
| ------------------------------ | -------------------------------------------------------------------------------------------------------- |
| source claims → build artifact | criterion 5's dev-affordance clause is a build-output claim; only the bundle grep with control closes it |

## STRIDE Threat Register

| Threat ID | Category    | Component                               | Disposition | Mitigation Plan                                                                           |
| --------- | ----------- | --------------------------------------- | ----------- | ----------------------------------------------------------------------------------------- |
| T-98-14   | Repudiation | bundle-zero over a build that never ran | mitigate    | positive control asserted ≥1 in the same artifacts dir in the same gate run               |
| T-98-15   | Tampering   | allowlist rows deleted without repair   | mitigate    | rows leave by repair or NAMED exemption; the guard's checks re-fire on any surviving site |
| T-98-SC   | Tampering   | package installs                        | accept      | zero installs this phase                                                                  |

</threat_model>

<verification>
All three gates green; guard strict-green; copy05 green both legs. POPULATION: every class the
extended guard checks, the six sanctioned feeds, the fillMock affordance. OUTSIDE IT: `MMMM
yyyy` month-nav headers (named exemption, D-25), `yyyy-MM-dd` input plumbing, number
toLocaleString via toFormatLocale, the EngagementsList week header (98-05 owned it), tests, and
Arabic month/weekday naturalness (AR-02 → P99).
</verification>

<success_criteria>
Criterion 5 closes: rendered day-first/GST shapes on repaired surfaces in both locales, relative
time only on the six graded feed surfaces via the one helper, and the built bundle proven clean
with a live control.
</success_criteria>

<output>
Create `.planning/phases/98-copy-truth/98-07-SUMMARY.md` when done. It MUST record: the
enumeration verdict per file (sanctioned/migrated + which export); every allowlist row's exit
(repaired or named exemption); the Intl.DateTimeFormat triage table; the bundle grep outputs
BOTH directions; the re-derived population deltas vs the frontmatter list; and both spec-leg
results with locale+role.
</output>
