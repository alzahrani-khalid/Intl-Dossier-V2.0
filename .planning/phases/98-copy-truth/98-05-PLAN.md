---
phase: 98-copy-truth
plan: 05
type: execute
wave: 3
depends_on: [98-01, 98-04]
files_modified:
  - frontend/src/components/signals/SignalRow.tsx # named instance human_entered
  - frontend/src/components/list-page/EngagementsList.tsx # WEEK OF 2026-W27 — sole owner of this file in the phase (its date shape included)
  - frontend/src/components/waiting-queue/AssignmentDetailsModal.tsx # {entity.status}
  - frontend/src/components/assignments/KanbanTaskCard.tsx # {assignment.priority}
  - frontend/src/components/dossier/dossier-overview/sections/ActivityTimelineSection.tsx # {activity.status}
  - frontend/src/i18n/en/intelligence-signals.json # sourceType family (REGISTERED namespace — signals.json does NOT exist; creating it would ship an unregistered no-op, D-10)
  - frontend/src/i18n/ar/intelligence-signals.json # same commit (D-16)
  - frontend/src/components/tasks/TaskDetail.tsx # de-snake sweep member. CLOSED derived-scope reservation (rev 3): this plan may additionally edit ONLY (a) MiniRelationshipGraph.tsx, EnhancedGraphVisualization.tsx, and the five intelligence dashboards; (b) surface-namespace JSON pairs for enum families it extends, PROVIDED no wave-3 sibling declares the file (98-06's declared list is the check); (c) common.json/dossier.json for cross-cutting family members only (wave-2-owned, no wave-3 sibling). Any derivation hit OUTSIDE this closed list is RECORDED as a handoff line for 98-09 — never edited here
autonomous: true
requirements: [COPY-01]
success_criterion: 'criterion 1: no database value renders as user copy — the named instances and the two derived populations (enum text-position renders; de-snake hacks) are routed through display labels, closed on rendered surfaces in both locales'
truths:
  - 'Decisions covered — D-04: the criterion-1 populations are RE-DERIVED at execution with the stated Part A/Part B instruments and the counts fall out — no audit figure is quoted as truth; D-05: each derivation names what falls OUTSIDE it (prop-position expressions, test files, values already inside t() with defaultValue which are the P99 mask class, free-text data fields, and DB column names under the CLAUDE.md carve-outs); D-06: both instruments are shown returning non-zero on their known-present controls before any post-repair zero is believed, using command grep because the repo grep wrapper honors .gitignore; D-13 adjacency honored: strings anchor every edit, never line numbers.'
must_haves:
  truths:
    - 'SignalRow renders a localized source-type label, never the raw human_entered value, in both locales'
    - 'The engagements list group header renders a localized prefix and a day-first date — no ISO 2026-W27 token, no silent English default'
    - 'Every true text-position enum render found by Part A is routed through an enum-keyed t() family in the surface namespace (house pattern — no new label registry)'
    - 'Every de-snake .replace site found by Part B has a verdict: routed (enum domain) or data-not-copy (named)'
  artifacts:
    - path: 'frontend/src/i18n/en/intelligence-signals.json'
      provides: 'the sourceType enum-keyed family'
      contains: 'sourceType'
    - path: 'frontend/src/components/list-page/EngagementsList.tsx'
      provides: 'the localized week header'
      contains: 'formatDayFirst'
  key_links:
    - from: 'frontend/src/components/signals/SignalRow.tsx'
      to: 'frontend/src/i18n/en/intelligence-signals.json'
      via: 'enum-keyed t() lookup on source_type through the intelligence-signals-typed t prop'
      pattern: 'sourceType'
---

<objective>
Criterion 1 as two derived populations plus two named instances. Part A: JSX text-position
renders of enum-typed member expressions. Part B: `.replace(/_/g, ' ')` de-snake hacks. The
repair mechanism is fixed by the house pattern (98-RESEARCH §C1): enum-keyed `t()` families in
the surface's own namespace — the enum value is the key segment, no central label map, no
localizing StatusBadge. The week header is the one site where criterion 1 meets criterion 5:
one task treats it as one repair (98-RESEARCH collision table).

Purpose: `in_progress`, `human_entered`, `WEEK OF 2026-W27` and their whole class stop reaching
the screen.
Output: routed enum renders, a localized week header, a verdicted de-snake sweep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-copy-truth/98-CONTEXT.md (D-04, D-05, D-06, D-16)
@.planning/phases/98-copy-truth/98-RESEARCH.md §C1 (mechanism, named instances, both derivation commands with controls)
@.planning/phases/98-copy-truth/98-UI-SPEC.md §C1 (labels render in the SAME element/position/classes; Urgent-never-Critical for work-item priority; intake urgency keeps Critical — distinct enum)
@frontend/src/components/tasks/TaskCard.tsx (the existing-correct enum-keyed t() idiom to mirror)

<interfaces>
Verified on disk 2026-08-18 (strings anchor, lines drift).

- `SignalRow.tsx` ~:84 — `<span className="font-mono">{signal.source_type}</span>` renders
  `human_entered` raw. CORRECTED revision 1: SignalRow has NO `useTranslation` of its own — it
  receives `t: TFunction<'intelligence-signals'>` as a PROP (`SignalRow.tsx:30`). The namespace
  `intelligence-signals` IS registered (`i18n/index.ts:402,538`) and
  `frontend/src/i18n/{en,ar}/intelligence-signals.json` both EXIST. `signals.json` does NOT
  exist and must not be created — an unregistered namespace silently falls back to inline
  English in BOTH locales (D-10), converting a criterion-1 repair into a criterion-2 defect.
- `EngagementsList.tsx` ~:68 `useTranslation(['engagements', 'list-pages'])`; ~:142,145 —
  `t('week.of', { defaultValue: 'Week of' }) + ' ' + group.key` where `group.key` is the ISO
  token from `lib/date/getISOWeek.ts`. `key={group.key}` at ~:141 is a React key prop and
  STAYS. CORRECTED revision 1: `week.of` ALREADY EXISTS in both locales (en `Week of`, ar
  `أسبوع`) — no key registration and no JSON edit is owed. The repair is code-only: drop the
  redundant `defaultValue`, and render the week as `formatDayFirst` of the week's start —
  derive the start from the group's first engagement's date via date-fns `startOfISOWeek`
  (already installed; no new parser). Same change applies to the aria-label.
- Part A members named by research (re-derive; triage prop-position matches OUT):
  `AssignmentDetailsModal.tsx` ~:276 `{entity.status}`, `KanbanTaskCard.tsx` ~:69
  `{assignment.priority}`, `ActivityTimelineSection.tsx` ~:221 `{activity.status}`.
- Part B members named by research (re-derive): `TaskDetail.tsx` ~:261,
  `MiniRelationshipGraph.tsx` ~:266,374, `EnhancedGraphVisualization.tsx` ~:623,808, five
  intelligence dashboards — 16 sites at last derivation.
- Existing enum families: follow whatever namespace each surface's family already lives in
  (`t('priority.…')` in tasks; `workItemStatus`/`workItemSource` in the work-items surface).
  New members join EXISTING families; only a family that does not exist yet is created, in the
  surface's own namespace. Priority labels render Urgent, never Critical (work-item enum);
  intake `urgency` legitimately renders Critical (distinct enum — carve-out).
- File-scope note: no wave-3 sibling edits common.json or dossier.json (98-06 is banned from
  both), so if a cross-cutting family this plan extends already lives in one of them, the edit
  is safe here.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: derive Part A; route the named instances and every true text-position member</name>
  <files>frontend/src/components/signals/SignalRow.tsx, frontend/src/components/waiting-queue/AssignmentDetailsModal.tsx, frontend/src/components/assignments/KanbanTaskCard.tsx, frontend/src/components/dossier/dossier-overview/sections/ActivityTimelineSection.tsx, frontend/src/i18n/en/intelligence-signals.json, frontend/src/i18n/ar/intelligence-signals.json</files>
  <read_first>each of the four TSX files at the anchor strings above (the render context and the file's useTranslation namespace); frontend/src/components/tasks/TaskCard.tsx ~:51-54 (the idiom); the target namespace JSON files for each surface (which families already exist — join them, never fork)</read_first>
  <action>
    Run the Part A finder (98-RESEARCH §C1 Population, the enum-ish member-expression regex over
    `frontend/src/{components,pages,routes}/**/*.tsx` via `command grep`, absolute path) and
    triage: prop-position matches OUT (a prop is not copy until rendered — the component it
    feeds becomes the member), free-text fields OUT (verdict per site), text-position renders
    IN. The instrument control: the finder MUST return the `SignalRow.tsx` source_type line
    before repairs, and MUST still fire on a planted scratch fixture after repairs (D-06 —
    scratch file, never committed).

    Route every IN member through an enum-keyed family: `SignalRow` renders
    `t('sourceType.' + signal.source_type)` through its EXISTING `t` prop (typed
    `TFunction<'intelligence-signals'>` — no new hook), against a new `sourceType` family in
    `intelligence-signals.json` (EN labels: `human_entered` → `Human entered`, plus every other
    value of the source-type enum — derive the domain from the signal TypeScript type; AR pairs
    authored, same commit);
    the other three surfaces join their existing status/priority families (add only MISSING
    members — derive each enum domain from its type; work-item priority renders Urgent, never
    Critical). Same element, same classes, sentence case. Do NOT add a raw-value defaultValue
    (that is the P99 mask class — where an existing family idiom uses one, record the
    interaction per D-03, do not replicate it in new code).

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && test "$(command grep -c "{signal.source_type}" "$R/frontend/src/components/signals/SignalRow.tsx" || true)" -eq 0 && command grep -q "sourceType" "$R/frontend/src/components/signals/SignalRow.tsx" && python3 -c "
import json
for loc in ('en','ar'):
    d=json.load(open('$R/frontend/src/i18n/%s/intelligence-signals.json'%loc))
    st=d.get('sourceType'); assert isinstance(st,dict) and 'human_entered' in st and st['human_entered'].strip(), loc
print('SOURCETYPE-OK')
" && test "$(command grep -c "{entity.status}" "$R/frontend/src/components/waiting-queue/AssignmentDetailsModal.tsx" || true)" -eq 0 && test "$(command grep -c "{assignment.priority}" "$R/frontend/src/components/assignments/KanbanTaskCard.tsx" || true)" -eq 0 && test "$(command grep -c "{activity.status}" "$R/frontend/src/components/dossier/dossier-overview/sections/ActivityTimelineSection.tsx" || true)" -eq 0 && cd "$R/frontend" && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>The four named raw renders are GONE (chain-safe zero-counts, one file at a time so a survivor is named) and SignalRow routes through the sourceType family, which exists in BOTH locales with the human_entered member (python walk failing with the locale name); type-check green. The FULL Part A derivation — count, triage table with per-site verdicts, what fell outside (D-05), and the instrument control both directions — lands in the SUMMARY; four named greens without the derivation table is a criterion-1 population failure, not a pass. MAX REACHABLE: named-instance clauses reachable by the routing edits; the derivation is reachable because the instrument and control are specified.</acceptance_criteria>
  <done>Every text-position enum render found by the stated instrument routes through a display label in both locales.</done>
</task>

<task type="auto">
  <name>Task 2: derive Part B; verdict and route the de-snake hacks</name>
  <files>frontend/src/components/tasks/TaskDetail.tsx plus the rest of the derived Part B set — at last derivation: MiniRelationshipGraph.tsx, EnhancedGraphVisualization.tsx, and the five intelligence dashboards (~9 files; the derived set governs) — and, for enum families extended, their surface namespace JSON pairs where no wave-3 sibling declares the file; any hit outside the frontmatter's closed reservation is recorded as a 98-09 handoff line, never edited</files>
  <read_first>the Part B derivation output (run FIRST: the .replace de-snake command from 98-RESEARCH §C1 over frontend/src, command grep, absolute path); each hit's render context — a de-snaked ENUM routes through t(), a de-snaked free-text DATA value is not copy and gets a named data-not-copy verdict</read_first>
  <action>
    Run the Part B finder (both `.replace(/_/g, ' ')` and `replace('_', ' ')` forms, tests
    excluded — control: the instrument must return the `TaskDetail.tsx` site at HEAD). For each
    hit, verdict: (a) enum domain → delete the de-snake hack and route through the surface's
    enum-keyed family (adding missing members in both locales, same commit); (b) free-text or
    graph node label whose domain is data → data-not-copy, named in the SUMMARY with the field;
    (c) already behind t() → out (P99 mask class if it carries a raw defaultValue — record).
    The last derivation counted 16 sites across ~9 files (TaskDetail, MiniRelationshipGraph ×2,
    EnhancedGraphVisualization ×2, five intelligence dashboards) — the derived set governs
    (D-04). The intelligence dashboards are ALSO wave-4 (98-07) date-migration files; this task
    edits only their de-snake sites, and 98-07 runs after — no same-wave contention by
    construction.

    After repairs: re-run the finder over the repaired file set, require zero enum-domain
    survivors, and prove the instrument still fires on a scratch fixture (D-06).

    D-15 DEVIATION, stated with cause (matching 98-07 T2's shape): the derived Part B set was
    ~9 files at last derivation and may run slightly over with namespace pairs — a single
    mechanical de-snake class at ~2 lines per site, far under the 120-line diff cap; splitting
    would manufacture boundaries inside one derived population.

  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && test "$(command grep -c "replace(/_/g, ' ')" "$R/frontend/src/components/tasks/TaskDetail.tsx" || true)" -eq 0 && D="${TMPDIR:-/tmp}/p98-05-fixture" && rm -rf "$D" && mkdir -p "$D" && printf '%s\n' "const x = status.replace(/_/g, ' ')" > "$D/fixture.tsx" && test "$(command grep -rc "replace(/_/g, ' ')" "$D" | awk -F: '{s+=$2} END {print s}')" -eq 1 && cd "$R/frontend" && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>The named TaskDetail de-snake site is gone (chain-safe zero) AND the same grep instrument is shown returning exactly 1 on a just-planted scratch fixture in the same gate run — the zero and its control ride one chain (D-06); type-check green. The 98-copy01 spec run moved to Task 3's gate (revision 1, B9 — a gate must not assert a state a LATER task in the same plan produces; the week header lands in Task 3). The full Part B verdict table (site → enum-routed / data-not-copy / mask-recorded) lands in the SUMMARY; every enum-domain site must read routed. Spec half under the NOT CONSTRUCTED discipline if the stack is unavailable. MAX REACHABLE: all clauses reachable; the fixture control cannot false-green because it is planted in the same run it is counted.</acceptance_criteria>
  <done>The de-snake class has a verdict per member and zero enum-domain survivors.</done>
</task>

<task type="auto">
  <name>Task 3: the week header — localized prefix, day-first date, no ISO token</name>
  <files>frontend/src/components/list-page/EngagementsList.tsx</files>
  <read_first>frontend/src/components/list-page/EngagementsList.tsx (the grouping structure — where group.key is born and what per-group item dates are available); frontend/src/lib/format-date.ts (formatDayFirst signature); frontend/src/i18n/{en,ar}/engagements.json (confirm week.of resolves in both — read, not edit)</read_first>
  <action>
    `week.of` already exists in both locales (revision 1, A7 — the audit-era claim that it was
    missing is false; asserting its existence discriminates nothing). Code-only repair in
    `EngagementsList.tsx`: drop the `{ defaultValue: 'Week of' }` option (a resolving key needs
    no mask; the silent-default interaction is recorded per D-03), and
    replace the rendered ` + group.key` ISO token (in BOTH the heading text and the aria-label)
    with `formatDayFirst(weekStart)` where `weekStart` is date-fns `startOfISOWeek` of the
    group's first engagement's date (the group already holds its items — no new parser, no
    string arithmetic on the ISO token). `group.key` remains as the React key prop. The
    rendered header shape becomes `Week of Mon 29 Jun` / `أسبوع Mon 29 Jun` — the Latin date
    token inside the Arabic header is Policy D, not a defect.
  </action>
  <verify>
    <automated>R=/Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && F="$R/frontend/src/components/list-page/EngagementsList.tsx" && test "$(command grep -c "defaultValue: 'Week of'" "$F" || true)" -eq 0 && command grep -q "formatDayFirst" "$F" && command grep -q "startOfISOWeek" "$F" && command grep -q "key={group.key}" "$F" && cd "$R/frontend" && pnpm type-check && cd "$R" && pnpm exec playwright test tests/e2e/98-copy01-labels.spec.ts --project=chromium-en --no-deps</automated>
  </verify>
  <acceptance_criteria>The silent English default is gone while the React key prop SURVIVES (the pair proves the edit hit the rendered text, not the list identity); formatDayFirst and startOfISOWeek are both present (the date arrives through the one formatter, not a hand-rolled range); type-check green; the 98-copy01 spec — which asserts zero ISO-week tokens — runs IN THIS GATE and goes green in both legs, closing the rendered half AFTER the repair it asserts exists (revision 1, B9). The week.of-exists clause was REMOVED as non-discriminating (A7 — it passes at HEAD). NOT CONSTRUCTED discipline for the spec half. MAX REACHABLE: all clauses reachable by the described edit.</acceptance_criteria>
  <done>The week header speaks the project's date language in both locales; EngagementsList.tsx's phase diff is complete in this one plan.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                                                                               |
| ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------- |
| enum value domains → authored labels | a label authored for a value outside the real enum domain masks future values; domains are derived from the TypeScript types, not guessed |

## STRIDE Threat Register

| Threat ID | Category    | Component                              | Disposition | Mitigation Plan                                                                                               |
| --------- | ----------- | -------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- |
| T-98-10   | Repudiation | a population green about the wrong set | mitigate    | both instruments carry known-present controls in both directions; exclusions stated per D-05                  |
| T-98-11   | Tampering   | terminology drift (Critical vs Urgent) | mitigate    | the UI-SPEC carve-out is restated in Task 1; work-item priority renders Urgent, intake urgency keeps Critical |
| T-98-SC   | Tampering   | package installs                       | accept      | zero installs this phase                                                                                      |

</threat_model>

<verification>
All three gates green; 98-copy01 green in both legs (or NOT CONSTRUCTED recorded). POPULATION:
Part A text-position enum renders, Part B de-snake sites, the two named instances. OUTSIDE IT:
prop-position expressions, free-text data, values already inside t() (P99 mask class), DB column
names (carve-outs), and every date-format concern except the week header (98-07 owns dates).
</verification>

<success_criteria>
Criterion 1 closes on rendered surfaces in both locales, with both populations derived, both
instruments controlled, and every member carrying a verdict.
</success_criteria>

<output>
Create `.planning/phases/98-copy-truth/98-05-SUMMARY.md` when done. It MUST record: both
derivation outputs with counts, definitions, exclusions, and both-direction instrument controls;
the per-site verdict tables; which namespaces gained which family members (en+ar commit hashes);
and any cross-cutting family member that had to land in common.json or dossier.json.
</output>
