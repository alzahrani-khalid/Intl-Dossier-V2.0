---
phase: 72-agent-platform-runtime-retrieval-reads
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/docker-compose.prod.yml
  - deploy/agent-runtime.env.example
  - deploy/DROPLET_INSTRUCTIONS.md
  - deploy/nginx/nginx.conf
  - deploy/nginx/nginx.prod.conf
autonomous: false
requirements: [INFRA-01, INFRA-02]
user_setup:
  - service: GPU host (vLLM)
    why: 'Serve Gemma 4 12B over the OpenAI-compatible /v1 (INFRA-01)'
    env_vars:
      - name: VLLM_BASE_URL
        source: 'http://vllm:8000 inside the compose network; set on the agent-runtime service'
    dashboard_config:
      - task: 'Confirm the datacenter GPU (16-24GB) and pull google/gemma-4-12B-it; validate FP8/QAT + 8K ctx fit before the phase gate'
        location: 'deploy host with NVIDIA runtime'

must_haves:
  truths:
    - 'vLLM serves Gemma 4 12B over OpenAI-compatible /v1, swappable by config (served-model-name gemma-4-12b)'
    - 'TEI serves bge-m3 (embeddings) and bge-reranker-v2-m3 (rerank) as two local internal-only containers'
    - 'All four new services are internal-only (expose, never ports) on the intl-dossier network, zero egress'
    - 'Implements INFRA-01 (vLLM Gemma 4) and INFRA-02 (TEI embed+rerank) per D-02'
    - "D-01: the agent-runtime docker-compose service carries CORS via the ALLOWED_ORIGINS secret (never '*') and a distinct port, per the workspace-wiring decision"
    - 'nginx reverse-proxies /api/copilot/ to agent-runtime:4100 with SSE-friendly settings (no buffering, long read timeout) in BOTH nginx.conf and nginx.prod.conf, so the CopilotKit runtimeUrl does not 502 in prod'
  artifacts:
    - path: 'deploy/docker-compose.prod.yml'
      provides: 'agent-runtime + vllm + tei-embed + tei-rerank service blocks'
      contains: 'tei-rerank'
    - path: 'deploy/agent-runtime.env.example'
      provides: 'The new env contract (ALLOWED_ORIGINS, VLLM_BASE_URL, TEI_EMBED_URL, TEI_RERANK_URL, MASTRA_PG_URL, SUPABASE_ANON_KEY)'
    - path: 'deploy/nginx/nginx.conf'
      provides: 'upstream agent-runtime + location /api/copilot/ SSE-friendly reverse proxy (HTTP/testing config)'
      contains: 'agent-runtime'
    - path: 'deploy/nginx/nginx.prod.conf'
      provides: 'upstream agent-runtime + location /api/copilot/ SSE-friendly reverse proxy (HTTPS prod config)'
      contains: 'agent-runtime'
  key_links:
    - from: 'deploy/docker-compose.prod.yml agent-runtime service'
      to: 'vllm + tei-embed + tei-rerank'
      via: 'VLLM_BASE_URL / TEI_EMBED_URL / TEI_RERANK_URL on the same intl-dossier network'
      pattern: 'VLLM_BASE_URL'
---

<objective>
Stand up the serving substrate the copilot runs on: add four new services to docker-compose.prod — agent-runtime (the workspace built in 72-05, declared here so the network/env contract lands first), vllm (Gemma 4 12B over OpenAI /v1), tei-embed (bge-m3), tei-rerank (bge-reranker-v2-m3). All internal-only, zero egress, on the intl-dossier network. This is the parallel infra track the ROADMAP says must land before the phase executes.

Purpose: the agent-runtime, the re-embed job, and the read tools all need vLLM + TEI reachable by service name; defining the compose services + env contract first unblocks them. nginx must also reverse-proxy the copilot SSE route (/api/copilot/) to agent-runtime:4100, or the CopilotKit provider's runtimeUrl 502s in prod and the 72-09 INFRA-03 end-to-end smoke fails.
Output: the four service blocks (pre-written in RESEARCH Pattern 1, L253-294) + the env example + the nginx /api/copilot/ SSE proxy (both nginx.conf and nginx.prod.conf) + a deploy-instructions note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-CONTEXT.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-RESEARCH.md

<interfaces>
docker-compose.prod.yml conventions (verified): only nginx has ports:; every internal service uses expose:.
Zero-egress services carry a "# Internal-only ... Zero egress" banner (L180). Existing ports: backend 4000, anythingllm 3001,
langfuse 3000, phoenix 6006/4317, redis 6379. New: agent-runtime 4100, vllm 8000, tei-embed 80, tei-rerank 80.
The full four-service YAML is pre-written in RESEARCH Pattern 1 (L253-294). vLLM command flags:
--served-model-name gemma-4-12b --enable-auto-tool-choice --tool-call-parser gemma4 --max-model-len 8192 --kv-cache-dtype fp8.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add vllm + tei-embed + tei-rerank service blocks (INFRA-01/02)</name>
  <read_first>
    - deploy/docker-compose.prod.yml (the backend service block L64-100 to mirror; the internal-only langfuse L183 / phoenix L216 zero-egress banner + expose convention; the networks block L241)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-RESEARCH.md (Pattern 1 L253-294 the pre-written four-service YAML; Pitfall 6 Gemma GPU-fit flags; Environment Availability table for TEI image tags)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-PATTERNS.md (the docker-compose.prod.yml assignment — internal services use expose never ports; container_name intl-dossier-<svc>)
  </read_first>
  <action>
    Add three internal-only services to deploy/docker-compose.prod.yml, copying the pre-written YAML from RESEARCH Pattern 1. vllm: image vllm/vllm-openai:latest, command with --model google/gemma-4-12B-it --served-model-name gemma-4-12b --enable-auto-tool-choice --tool-call-parser gemma4 --max-model-len 8192 --kv-cache-dtype fp8 --gpu-memory-utilization 0.90, expose 8000, an nvidia GPU device reservation, networks [intl-dossier]. tei-embed: image ghcr.io/huggingface/text-embeddings-inference:latest, command --model-id BAAI/bge-m3, expose 80. tei-rerank: same image, command --model-id BAAI/bge-reranker-v2-m3, expose 80. Mirror the existing zero-egress banner. Each carries container_name intl-dossier-<svc> and joins the intl-dossier network. NO ports: (internal-only). Do NOT flip the backend's AI_USE_ANYTHINGLLM (RESEARCH Runtime State — copilot points at vLLM via config, backend stays on AnythingLLM this phase).
  </action>
  <verify>
    <automated>grep -q "tei-rerank" deploy/docker-compose.prod.yml && grep -q "gemma-4-12b" deploy/docker-compose.prod.yml && grep -q "bge-reranker-v2-m3" deploy/docker-compose.prod.yml</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.prod.yml contains vllm, tei-embed, tei-rerank blocks, each with expose (not ports) and networks [intl-dossier].
    - The vllm command includes --served-model-name gemma-4-12b and --tool-call-parser gemma4 (INFRA-01 swappable config + tool-calling).
    - No ports: key is added to any of the three (internal-only; nginx is the only ports: service).
    - docker compose config parses without error (or the closest non-GPU-host equivalent validation noted in the SUMMARY).
  </acceptance_criteria>
  <done>vLLM + TEI services defined internal-only; the copilot's model + embed + rerank backends are reachable by service name.</done>
</task>

<task type="auto">
  <name>Task 2: Add the agent-runtime service block + env contract</name>
  <read_first>
    - deploy/docker-compose.prod.yml (the backend service block — environment keys, depends_on [redis], expose, networks)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-RESEARCH.md (Pattern 1 the agent-runtime service YAML L254-271; the CORS lock note — ALLOWED_ORIGINS secret; Runtime State Inventory secrets/env-vars row)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-PATTERNS.md (the Dockerfile.prod port 4100 + pnpm@10.29.1 lock; the ALLOWED_ORIGINS secret never '*' lock)
    - deploy/DROPLET_INSTRUCTIONS.md (existing deploy flow to extend with the new env vars)
    - deploy/nginx/nginx.conf AND deploy/nginx/nginx.prod.conf (the existing `upstream backend { server backend:4000; }` + `location /api/` blocks are the analogs to mirror for the new agent-runtime upstream + /api/copilot/ location; nginx is the ONLY ports: service so the copilot SSE route MUST be proxied here)
  </read_first>
  <action>
    Add the agent-runtime service to docker-compose.prod.yml (build context ../agent-runtime, Dockerfile.prod; container_name intl-dossier-agent-runtime; expose 4100; depends_on [redis]; networks [intl-dossier]). Environment: NODE_ENV=production, PORT=4100, SUPABASE_URL, SUPABASE_ANON_KEY (keystone uses anon + caller JWT, NEVER service-role), ALLOWED_ORIGINS (SECRET — CORS origin, never '*'), VLLM_BASE_URL=http://vllm:8000, TEI_EMBED_URL=http://tei-embed:80, TEI_RERANK_URL=http://tei-rerank:80, DATABASE_URL=${MASTRA_PG_URL} (@mastra/pg threads on the same Supabase PG), OTEL_EXPORTER_OTLP_ENDPOINT=http://phoenix:4317. Create deploy/agent-runtime.env.example documenting every new var with a one-line description and the ALLOWED_ORIGINS-must-be-set warning. Append a short section to DROPLET_INSTRUCTIONS.md listing the new env vars + that nginx proxies /api/copilot/ to agent-runtime:4100. The Dockerfile.prod itself + the workspace are authored in 72-05; this task only wires the compose service + env.

    Then add the nginx reverse proxy for the copilot SSE route in BOTH deploy/nginx/nginx.conf AND deploy/nginx/nginx.prod.conf (mirroring the existing `upstream backend` / `location /api/` blocks): (1) add `upstream agent-runtime { server agent-runtime:4100; keepalive 32; }` next to the existing upstream blocks; (2) add a `location /api/copilot/ { ... }` block INSIDE the server block (in nginx.conf the listen-80 server; in nginx.prod.conf the listen-443 server) that `proxy_pass http://agent-runtime/;` (TRAILING SLASH — strips the /api/copilot/ prefix so the CopilotKit runtimeUrl /api/copilot/chat lands on the runtime's /chat route) with SSE-friendly settings so streamed AG-UI/SSE responses are NOT buffered: `proxy_http_version 1.1;`, `proxy_set_header Connection '';` (empty — defeats keep-alive close + the /api/ block's `Connection "upgrade"`), `proxy_buffering off;`, `proxy_read_timeout 3600s;`, plus the usual Host / X-Real-IP / X-Forwarded-For / X-Forwarded-Proto headers. Place the /api/copilot/ location BEFORE the generic /api/ location so the more-specific prefix wins. Use the api rate-limit zone (limit_req zone=api burst=20 nodelay) consistent with /api/.

  </action>
  <verify>
    <automated>grep -q "intl-dossier-agent-runtime" deploy/docker-compose.prod.yml && grep -q "ALLOWED_ORIGINS" deploy/docker-compose.prod.yml && test -f deploy/agent-runtime.env.example && grep -q "upstream agent-runtime" deploy/nginx/nginx.conf && grep -q "location /api/copilot/" deploy/nginx/nginx.conf && grep -q "upstream agent-runtime" deploy/nginx/nginx.prod.conf && grep -q "location /api/copilot/" deploy/nginx/nginx.prod.conf</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.prod.yml agent-runtime service exposes 4100 (NOT ports:), depends_on redis, networks [intl-dossier].
    - The service environment sets ALLOWED_ORIGINS from the secret (no literal '*'), SUPABASE_ANON_KEY (not service-role), and the VLLM/TEI/MASTRA_PG URLs.
    - deploy/agent-runtime.env.example lists ALLOWED_ORIGINS, VLLM_BASE_URL, TEI_EMBED_URL, TEI_RERANK_URL, MASTRA_PG_URL, SUPABASE_ANON_KEY.
    - DROPLET_INSTRUCTIONS.md notes the nginx /api/copilot/ -> agent-runtime:4100 proxy.
    - BOTH deploy/nginx/nginx.conf and deploy/nginx/nginx.prod.conf contain `upstream agent-runtime` (server agent-runtime:4100) AND a `location /api/copilot/` block that proxy_passes to http://agent-runtime/ (trailing slash) with SSE-friendly settings (proxy_buffering off; proxy_read_timeout 3600s; proxy_http_version 1.1; Connection '').
    - The /api/copilot/ location precedes the generic /api/ location (more-specific prefix wins) so the CopilotKit runtimeUrl /api/copilot/chat reaches the runtime's /chat route, not the backend.
  </acceptance_criteria>
  <done>agent-runtime service + env contract defined; the workspace built in 72-05 has a home + CORS/keystone env ready.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                              |
| ---------------------------- | ---------------------------------------------------------------------------------------- |
| nginx -> agent-runtime       | The only externally-proxied path (/api/copilot/ -> /chat); everything else internal-only |
| agent-runtime -> vllm/tei    | Internal compose network; no egress                                                      |
| compose services -> internet | Zero-egress boundary; only nginx has ports:                                              |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                     | Disposition | Mitigation Plan                                                                                                                        |
| ---------- | ---------------------- | --------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| T-72-02-01 | Information Disclosure | agent-runtime CORS                            | mitigate    | ALLOWED_ORIGINS is a secret set from env, never '\*'; unset -> ACAO:null on deployed origins (carried edge-fn lesson)                  |
| T-72-02-02 | Elevation of Privilege | agent-runtime DB creds                        | mitigate    | Service env ships SUPABASE_ANON_KEY only (caller-JWT keystone), never SUPABASE_SERVICE_ROLE_KEY                                        |
| T-72-02-03 | Information Disclosure | vllm/tei egress                               | accept      | Internal-only expose (no ports); zero-egress posture matches langfuse/phoenix; models are local weights, no telemetry                  |
| T-72-SC    | Tampering              | container images (vllm/vllm-openai, ghcr TEI) | mitigate    | Pin to known org images (vLLM Apache-2.0, TEI Apache-2.0 per RESEARCH audit); validate Gemma 4 12B GPU fit before the gate (Pitfall 6) |

</threat_model>

<verification>
- nginx.conf AND nginx.prod.conf carry `upstream agent-runtime` + a `location /api/copilot/` SSE-friendly proxy to http://agent-runtime/ (no buffering, 3600s read).
- docker compose config parses (or non-GPU-host equivalent recorded).
- All four new services use expose, none add ports.
- vllm command carries the gemma4 tool-call parser + served-model-name gemma-4-12b.
- agent-runtime env ships ALLOWED_ORIGINS (secret) + anon key, no service-role.
</verification>

<success_criteria>

- vLLM (Gemma 4 12B) + TEI (bge-m3 + bge-reranker-v2-m3) + agent-runtime defined internal-only on the intl-dossier network.
- The env contract (ALLOWED_ORIGINS secret, VLLM/TEI/MASTRA_PG URLs) is documented.
- INFRA-01 + INFRA-02 service substrate is in place; the smoke proofs (curl /v1/models, /embed, /rerank) are exercised live at the phase gate (72-09).
  </success_criteria>

<output>
Create `.planning/phases/72-agent-platform-runtime-retrieval-reads/72-02-SUMMARY.md` when done. Note whether GPU-host validation of Gemma 4 12B fit was possible or deferred to the gate.
</output>
