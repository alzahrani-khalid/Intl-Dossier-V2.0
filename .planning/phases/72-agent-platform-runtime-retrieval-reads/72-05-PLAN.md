---
phase: 72-agent-platform-runtime-retrieval-reads
plan: 05
type: execute
wave: 4
depends_on: ['72-01', '72-02']
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - turbo.json
  - agent-runtime/package.json
  - agent-runtime/tsconfig.json
  - agent-runtime/Dockerfile.prod
  - agent-runtime/src/index.ts
  - agent-runtime/src/mastra/index.ts
  - agent-runtime/src/mastra/agents/copilot.ts
  - agent-runtime/src/config.ts
  - agent-runtime/src/llm-router.ts
  - agent-runtime/.env.example
autonomous: true
requirements: [INFRA-03, AGENT-01, AGENT-06]
user_setup: []

must_haves:
  truths:
    - 'agent-runtime is the 4th Turborepo workspace (pnpm-workspace.yaml + root package.json + turbo) building dist/**'
    - 'The Mastra server terminates AG-UI over SSE via registerCopilotKit on a distinct port (4100), CORS from ALLOWED_ORIGINS secret'
    - 'setContext forwards the caller JWT + language to runtimeContext using the spike-proven path (#4465)'
    - 'The copilot agent lifts the EN/AR system prompts and selects by language; reads-only tool roster'
    - 'Implements INFRA-03 (own workspace on a distinct port in compose) + the AGENT-01/06 runtime spine'
    - 'D-01: the agent-runtime workspace is created with its own port (4100), turbo + root package.json wiring, and lifts backend/src/ai/{config,llm-router}.ts rather than rewriting'
  artifacts:
    - path: 'agent-runtime/src/mastra/index.ts'
      provides: 'Mastra server: registerCopilotKit, CORS, bundler.externals'
      contains: 'registerCopilotKit'
    - path: 'agent-runtime/src/mastra/agents/copilot.ts'
      provides: 'reads-only copilot agent with EN/AR prompts + model-native tool-calling'
    - path: 'agent-runtime/Dockerfile.prod'
      provides: 'prod image on port 4100, pnpm@10.29.1 pin'
      contains: '4100'
  key_links:
    - from: 'agent-runtime/src/mastra/index.ts'
      to: 'the copilot agent + tools'
      via: 'registerCopilotKit setContext sets authorization + language'
      pattern: 'setContext'
    - from: 'pnpm-workspace.yaml'
      to: 'agent-runtime'
      via: '4th workspace package entry'
      pattern: 'agent-runtime'
---

<objective>
Build the agent-runtime workspace: the 4th Turborepo package (Express + Mastra server + the self-hosted CopilotKit runtime) that terminates AG-UI over SSE on port 4100, forwards the caller JWT via setContext (the spike-proven path), and hosts the reads-only copilot agent. Lift backend/src/ai/{config,llm-router}.ts and the chat-assistant EN/AR system prompts — do not rewrite. The shell/runtime binding follows the spike's shell_decision (72-01 SPIKE-FINDINGS.md): CopilotKit if it passed, the @ag-ui/client headless server contract if assistant-ui landed (the runtime/agent/tools stay either way).

Purpose: this is the spine the tools (72-06) plug into and the drawer (72-08) talks to; INFRA-03 requires it as its own deployable workspace.
Output: the workspace scaffold, the Mastra server + copilot agent, the lifted config, the prod Dockerfile, and the workspace wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-CONTEXT.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-RESEARCH.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-PATTERNS.md
@agent-runtime/spike/SPIKE-FINDINGS.md

<interfaces>
The Mastra server + setContext + bundler.externals is pre-written in RESEARCH Pattern 2 (L302-331).
registerCopilotKit({ path:'/chat', resourceId:'copilot', setContext:(c,rc)=>{ rc.set('authorization', c.req.header('authorization')); rc.set('language', c.req.header('x-language')) } }).
CORS: server.cors.origin = process.env.ALLOWED_ORIGINS.split(','); allowHeaders ['content-type','authorization','x-copilotkit-runtime-client-gql-version'].
bundler.externals MUST include '@copilotkit/runtime' or mastra build 500s.
Re-pin @mastra/core ^1.36 -> the spike-passing 1.43 (or whatever SPIKE-FINDINGS pinned). pnpm@10.29.1 in Dockerfile.prod.
EN/AR prompts: CHAT_SYSTEM_PROMPT / ARABIC_CHAT_SYSTEM_PROMPT (chat-assistant.ts L74-110); selection idiom L472.
Do NOT lift decideToolUsage (L626-773) — replaced by model-native tool-calling (--tool-call-parser gemma4).
config.ts vllm block (L105-123): default the copilot provider to vllm (gemma-4-12b); do NOT flip backend AI_USE_ANYTHINGLLM.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workspace scaffold + wiring + lifted config (INFRA-03)</name>
  <read_first>
    - backend/package.json (lines 2-22 scripts + type:module + lint-from-root; the deps to reuse: bullmq 5.77.1, zod ^4.3.6, @supabase/supabase-js, @mastra/core)
    - backend/tsconfig.json + backend/Dockerfile.prod (lines 33-34, 53-101 multi-stage Alpine + pnpm@10.29.1; change EXPOSE/PORT to 4100)
    - pnpm-workspace.yaml + turbo.json + package.json (the 3-workspace list to extend; workspaces array duplicates in root package.json)
    - backend/src/ai/config.ts + backend/src/ai/llm-router.ts (the files to lift verbatim — vllm/ollama provider blocks)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-PATTERNS.md (the package.json / Dockerfile.prod / config.ts lift assignments + the re-pin flag)
  </read_first>
  <action>
    Add 'agent-runtime' to pnpm-workspace.yaml AND the root package.json workspaces array. Create agent-runtime/package.json (name agent-runtime, type module, scripts dev/build/start/test/lint/type-check mirroring backend; lint runs from root with the shared eslint config; build emits dist/**). Pin @mastra/core to the spike-passing version (1.43 unless SPIKE-FINDINGS says otherwise) + @mastra/pg + @ag-ui/mastra + @copilotkit/runtime + @ai-sdk/openai + zod + @supabase/supabase-js. Create agent-runtime/tsconfig.json extending the root strict config. Create agent-runtime/Dockerfile.prod mirroring backend's multi-stage Alpine build, pnpm@10.29.1 pin, EXPOSE 4100, PORT 4100, healthcheck on :4100/health. Lift backend/src/ai/config.ts -> agent-runtime/src/config.ts and llm-router.ts -> agent-runtime/src/llm-router.ts (default the copilot provider to vllm/gemma-4-12b; do NOT flip the backend's global AI_USE_ANYTHINGLLM). Confirm turbo build/dev/lint/type-check pick up the new package via its scripts (no per-package block needed). Create agent-runtime/.env.example.
  </action>
  <verify>
    <automated>grep -q "agent-runtime" pnpm-workspace.yaml && grep -q "4100" agent-runtime/Dockerfile.prod</automated>
  </verify>
  <acceptance_criteria>
    - pnpm-workspace.yaml AND root package.json workspaces both list agent-runtime.
    - agent-runtime/package.json pins @mastra/core to the spike-passing version (not ^1.36) and declares build/dev/lint/type-check scripts; lint runs from root.
    - agent-runtime/Dockerfile.prod EXPOSE/PORT 4100 + corepack prepare pnpm@10.29.1.
    - pnpm --filter agent-runtime type-check resolves; turbo recognizes the workspace.
  </acceptance_criteria>
  <done>agent-runtime is a real 4th workspace with lifted config + prod image on 4100; INFRA-03 workspace shape met.</done>
</task>

<task type="auto">
  <name>Task 2: Mastra server (registerCopilotKit + SSE + JWT setContext) + bootstrap</name>
  <read_first>
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-RESEARCH.md (Pattern 2 L302-331 the pre-written Mastra server + setContext + bundler.externals; the CORS lock; the air-gap note)
    - agent-runtime/spike/SPIKE-FINDINGS.md (the JWT-delivery path that passed — setContext direct vs server-middleware — and the pinned versions)
    - backend/src/ai/mastra-config.ts (the existing new Mastra({}) + OTel init to mirror for the bootstrap/observability export to phoenix:4317)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-PATTERNS.md (the mastra/index.ts No-Analog row; the docker-compose CORS lock)
  </read_first>
  <action>
    Create agent-runtime/src/mastra/index.ts: new Mastra({ agents:{ copilot }, server:{ cors:{ origin: process.env.ALLOWED_ORIGINS?.split(',') ?? [], allowMethods:['*'], allowHeaders:['content-type','authorization','x-copilotkit-runtime-client-gql-version'] }, apiRoutes:[ registerCopilotKit({ path:'/chat', resourceId:'copilot', setContext }) ] }, bundler:{ externals:['@copilotkit/runtime'] } }). Implement setContext to forward authorization + x-language to runtimeContext USING THE SPIKE-PROVEN PATH (if SPIKE-FINDINGS recorded the server-middleware workaround for #4465, implement that instead of the direct setContext). Create agent-runtime/src/index.ts: the bootstrap (port 4100, OTel export to phoenix:4317 mirroring mastra-config.ts, a /health endpoint). The self-hosted runtime needs NO Copilot Cloud key (air-gap). If SPIKE-FINDINGS landed assistant-ui, expose the @ag-ui/client-compatible AG-UI server route instead of registerCopilotKit (same agent + setContext).
  </action>
  <verify>
    <automated>grep -q "registerCopilotKit" agent-runtime/src/mastra/index.ts && grep -q "externals" agent-runtime/src/mastra/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - mastra/index.ts wires registerCopilotKit (or the AG-UI server route per SPIKE-FINDINGS) on path /chat with setContext forwarding authorization + language.
    - bundler.externals includes @copilotkit/runtime; CORS origin reads ALLOWED_ORIGINS (no literal '*').
    - src/index.ts boots on 4100 with a /health endpoint and exports OTel traces to phoenix:4317.
    - The runtime requires no Copilot Cloud key (air-gap verified in the spike).
  </acceptance_criteria>
  <done>The Mastra server terminates AG-UI/SSE on 4100, forwards the JWT via the proven path, externals set; the spine is live.</done>
</task>

<task type="auto">
  <name>Task 3: The reads-only copilot agent (EN/AR prompts + model-native tool-calling)</name>
  <read_first>
    - backend/src/ai/agents/chat-assistant.ts (lines 74-110 CHAT_SYSTEM_PROMPT + ARABIC_CHAT_SYSTEM_PROMPT to lift; line 472 the language selection idiom; lines 390-448 the tool roster shape — do NOT lift decideToolUsage L626-773)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-PATTERNS.md (the copilot.ts assignment — lift prompts + selection idiom; adapt to reads-only roster)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-UI-SPEC.md (the empty-state body framing "answers only from data you're cleared to see" — the agent's self-description)
  </read_first>
  <action>
    Create agent-runtime/src/mastra/agents/copilot.ts: a Mastra agent using the lifted CHAT_SYSTEM_PROMPT / ARABIC_CHAT_SYSTEM_PROMPT (selected by runtimeContext.get('language')), the vllm/gemma-4-12b model via the lifted config, model-native tool-calling (no heuristic decideToolUsage), and a tool roster reference that 72-06 fills (hybrid_rag_search, read_signals, query_graph, generate_digest-preview, get_dossier, list_dossiers, query_work_items). Adapt the prompt to the READS-ONLY framing: the agent answers only from data the caller is cleared to see, never claims to perform writes, and uses the neutral no-answer copy on empty results (indistinguishable-empty — no clearance/filtered/restricted language). Wire @mastra/pg PostgresStore (DATABASE_URL = MASTRA_PG_URL) for persistent threads (D-08) with resourceId = the authenticated user. The tools themselves are imported from ./tools/* (authored in 72-06); reference them so the agent compiles once 72-06 lands (this plan may stub the imports with a clear TODO if 72-06 hasn't run, flagged in the SUMMARY).
  </action>
  <verify>
    <automated>grep -qi "ARABIC" agent-runtime/src/mastra/agents/copilot.ts && grep -qi "language" agent-runtime/src/mastra/agents/copilot.ts</automated>
  </verify>
  <acceptance_criteria>
    - copilot.ts selects the system prompt by language (EN/AR) and uses the vllm/gemma-4-12b model.
    - The agent uses model-native tool-calling (no decideToolUsage heuristic lifted).
    - @mastra/pg PostgresStore is wired with resourceId = the authenticated user (D-08 persistent threads).
    - The reads-only framing is encoded; no forbidden substring in the prompt; the agent never claims to write.
  </acceptance_criteria>
  <done>The copilot agent exists with bilingual prompts + model-native tools + persistent threads; AGENT-06 language spine + reads-only contract met.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                                          |
| ----------------------- | -------------------------------------------------------------------- |
| browser -> /chat (SSE)  | The caller JWT enters via setContext; the keystone delivery boundary |
| agent -> mastra_threads | resourceId = auth.uid(); owner-only thread persistence               |
| runtime -> internet     | Air-gap; self-hosted, no Cloud key                                   |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                 | Disposition | Mitigation Plan                                                                                                               |
| ---------- | ---------------------- | ----------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------- |
| T-72-05-01 | Elevation of Privilege | setContext JWT delivery (#4465)           | mitigate    | Implement the spike-proven path; setContext (or server-middleware) forwards the caller JWT; tools assert it non-empty (72-06) |
| T-72-05-02 | Information Disclosure | CORS exposing the runtime                 | mitigate    | server.cors.origin from ALLOWED_ORIGINS secret, scoped allowHeaders; never '\*'                                               |
| T-72-05-03 | Information Disclosure | runtime egress / Cloud telemetry          | mitigate    | Self-hosted runtime, no Copilot Cloud key, air-gap proven in the spike                                                        |
| T-72-05-04 | Information Disclosure | agent prompt leaking clearance            | mitigate    | Reads-only prompt uses indistinguishable-empty copy; no clearance/filtered/restricted language                                |
| T-72-SC    | Tampering              | @mastra/\* + @copilotkit/runtime installs | mitigate    | Pin the spike-passing versions; RESEARCH audit cleared the core packages; bundler.externals set                               |

</threat_model>

<verification>
- pnpm --filter agent-runtime build emits dist/**; type-check passes.
- mastra/index.ts: registerCopilotKit + setContext + bundler.externals + CORS from ALLOWED_ORIGINS.
- copilot.ts: EN/AR prompt selection + vllm model + @mastra/pg threads.
- Dockerfile.prod EXPOSE 4100 + pnpm@10.29.1.
</verification>

<success_criteria>

- agent-runtime is the 4th workspace, builds, and boots on 4100 (INFRA-03).
- The Mastra server forwards the JWT via the spike-proven path; air-gap holds.
- The copilot agent is bilingual + reads-only + persistent-threaded (AGENT-06 spine, D-08).
  </success_criteria>

<output>
Create `.planning/phases/72-agent-platform-runtime-retrieval-reads/72-05-SUMMARY.md` when done. Record the pinned @mastra/core version, which JWT-delivery path was used, and which shell (copilotkit/assistant-ui) the runtime route serves.
</output>
