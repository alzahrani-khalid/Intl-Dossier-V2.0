---
phase: 72-agent-platform-runtime-retrieval-reads
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-runtime/spike/README.md
  - agent-runtime/spike/server.ts
  - agent-runtime/spike/client.tsx
  - agent-runtime/spike/SPIKE-FINDINGS.md
  - agent-runtime/vitest.config.ts
  - agent-runtime/src/mastra/tools/__scaffold__/tools.test.ts
  - frontend/src/i18n/en/copilot.json
  - frontend/src/i18n/ar/copilot.json
  - frontend/src/i18n/index.ts
  - frontend/tests/e2e/copilot.spec.ts
  - scripts/verify-rag-chunks-dims.sql
  - scripts/check-copilot-i18n.mjs
autonomous: false
requirements: [AGENT-02, AGENT-06, AGENT-04, AGENT-05]
user_setup: []

must_haves:
  truths:
    - 'The Option-C spike proves the caller JWT reaches tool.execute() (Mastra #4465 gate) OR records the server-middleware workaround that does'
    - 'The spike resolves the production shell: CopilotKit (if RTL+token fidelity AND air-gap both pass) OR the documented assistant-ui headless fallback'
    - 'A throwaway AG-UI SSE loop streams one token-bound tool-call render in both EN and AR with dir=rtl'
    - 'Implements spike-first per D-03; proves JWT-reaches-tools per the AGENT-02 keystone and RTL fidelity per AGENT-06/D-09'
    - 'D-11: the OSS-survey mandate is honored — battle-tested on-prem/permissive libraries (CopilotKit, assistant-ui, react-markdown, @ag-ui/*) are evaluated in the spike before hand-rolling, within the token/RTL/air-gap guardrails'
  artifacts:
    - path: 'agent-runtime/spike/SPIKE-FINDINGS.md'
      provides: 'The two gate verdicts (#4465 JWT path, CopilotKit-vs-fallback) + pinned @mastra/core + @ag-ui/mastra versions that passed'
      contains: 'shell_decision'
    - path: 'agent-runtime/vitest.config.ts'
      provides: 'Test infrastructure for the net-new agent-runtime workspace'
    - path: 'frontend/src/i18n/en/copilot.json'
      provides: 'Copilot namespace EN strings (UI-SPEC copywriting contract)'
    - path: 'frontend/src/i18n/ar/copilot.json'
      provides: 'Copilot namespace AR strings'
    - path: 'scripts/verify-rag-chunks-dims.sql'
      provides: 'AGENT-05 re-embed dimension proof + AGENT-04 prosecdef proof script (used by Wave 3)'
  key_links:
    - from: 'agent-runtime/spike/server.ts'
      to: 'Ollama /v1'
      via: 'ai-sdk openai baseURL pointed at local Ollama'
      pattern: 'baseURL'
    - from: 'frontend/src/i18n/index.ts'
      to: 'frontend/src/i18n/en/copilot.json'
      via: 'static import + resources map registration'
      pattern: 'copilot'
---

<objective>
De-risk the entire phase before any production code is written. Build the throwaway Option-C spike (D-03) that settles the two MEDIUM-confidence open questions the whole stack hinges on: (1) Mastra #4465 — does the caller JWT actually reach tool.execute() through the AG-UI/CopilotKit bridge (the AGENT-02 keystone delivery path); (2) D-09 — can CopilotKit hit RTL + token fidelity AND full air-gap, or does the headless assistant-ui fallback land. Also lay the Wave-0 test scaffold the rest of the phase samples against (agent-runtime vitest, copilot E2E stub, the re-embed dimension-proof SQL) and register the copilot i18n namespace (silent-English-fallback guard).

Purpose: every downstream plan (production runtime, drawer, tools) branches on which shell wins and whether the JWT keystone needs a server-middleware workaround. Resolving this first prevents a mid-phase rewrite.
Output: a throwaway spike under agent-runtime/spike/ (deleted/ignored before merge), SPIKE-FINDINGS.md recording both verdicts + the pinned passing versions, the agent-runtime test infra, the copilot i18n namespace, and the AGENT-05 verification SQL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-CONTEXT.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-RESEARCH.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-UI-SPEC.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-VALIDATION.md

<interfaces>
The keystone idiom the spike must prove the JWT reaches. From chat-assistant.ts:24 (verified):
createUserClient(authHeader) returns a SupabaseClient built from the anon key + the caller Bearer token.
The spike's #1 job: assert runtimeContext.get('authorization') is non-empty inside a tool BEFORE the RPC call.

Pre-written Mastra<->CopilotKit bridge + setContext (RESEARCH Pattern 2, L302-348) — the spike adapts this.
registerCopilotKit with path '/chat', resourceId 'copilot', and setContext forwarding c.req.header('authorization').
bundler.externals must include '@copilotkit/runtime' or `mastra build` 500s.

i18n registry shape (frontend/src/i18n/index.ts, verified): resources has an en branch (L265) and an ar branch (L397);
language persists under localStorage['id.locale'] (NOT i18nextLng).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Throwaway Option-C spike — AG-UI SSE loop + JWT-reaches-tools + RTL render</name>
  <read_first>
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-RESEARCH.md (Pattern 2 L302-348 pre-written bridge; Pattern 3 L350-384 per-request client in a tool; Pitfall 1 #4465 + workaround; Open Questions 1-2)
    - backend/src/ai/agents/chat-assistant.ts (lines 19-28 createUserClient; lines 74-110 EN/AR prompts)
    - backend/src/ai/config.ts (lines 105-123 the vllm/ollama provider blocks — the OpenAI-compatible swap point for the spike model client)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-UI-SPEC.md (CopilotKit theming contract — the copilot-kit token remap the spike tests; Color/Typography token names)
  </read_first>
  <action>
    Build a minimal throwaway slice under agent-runtime/spike/ (this dir is git-ignored or deleted before merge — NOT production). Use the ai-sdk openai provider pointed at a local Ollama /v1 (baseURL from env, identical API to vLLM per D-02), @ag-ui/mastra registerCopilotKit, and a single Mastra agent with ONE Zod-typed read tool (a stub read_signals that builds createUserClient from runtimeContext.get('authorization') and asserts the header is non-empty). Wire setContext to forward the authorization + x-language headers. Set bundler.externals to include '@copilotkit/runtime'.

    GATE 1 (#4465, AGENT-02 keystone): drive a chat turn from the spike client carrying a real Bearer token; the tool MUST observe a non-empty runtimeContext.get('authorization'). If the AG-UI bridge drops it, apply the documented workaround (set context in a Mastra server middleware that runs before the agent, reading c.req.header('authorization')) and record which path worked plus the exact @mastra/core + @ag-ui/mastra versions.

    GATE 2 (D-09, AGENT-06 shell decision): render ONE assistant message + one citation chip through the CopilotKit react-ui shell, re-skinned via copilot-kit CSS vars remapped to IntelDossier tokens (background to --surface, foreground to --ink, primary to --accent, border to --line) with shadow vars neutralized to none, under dir="rtl" with Tajawal. Verify visually at 1024px in AR. If CopilotKit's chrome cannot clear the token/RTL bar (it ships 0 RTL CSS rules + hardcoded shadows — RESEARCH-verified), build the same render via the assistant-ui headless fallback (@assistant-ui/react + @assistant-ui/react-ag-ui, ships /docs/rtl + DirectionProvider) and record that as the landing shell.

    GATE 3 (air-gap): run the self-hosted @copilotkit/runtime with NO Copilot Cloud key; block egress at the network layer (CDP Network.setBlockedURLs over external hosts OR run offline) and confirm a chat turn still completes with zero outbound calls to non-local hosts.

    Do NOT build the production runtime, drawer, or full tool roster here — that is Waves 4-6. This slice exists only to produce the two verdicts.

  </action>
  <verify>
    <automated>test -f agent-runtime/spike/SPIKE-FINDINGS.md && grep -qi "shell_decision" agent-runtime/spike/SPIKE-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - SPIKE-FINDINGS.md records GATE 1 verdict: the literal path (setContext direct vs server-middleware) that delivers a non-empty runtimeContext.get('authorization') to a tool, with the pinned @mastra/core + @ag-ui/mastra versions that passed.
    - SPIKE-FINDINGS.md records GATE 2 verdict as `shell_decision: copilotkit` OR `shell_decision: assistant-ui`, with a screenshot or DOM evidence of the AR render showing dir="rtl" + Tajawal + zero raw hex / zero card shadow on the message/citation surface.
    - SPIKE-FINDINGS.md records GATE 3: a chat turn completes with egress blocked and no outbound request to any non-local host (no Copilot Cloud key set).
    - The spike streams a tool-call render over AG-UI SSE in both an EN and an AR turn (one token-bound tool result each).
  </acceptance_criteria>
  <done>Both spike gates resolved; SPIKE-FINDINGS.md carries shell_decision + pinned versions + the JWT-delivery path; downstream plans can branch on it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave-0 test scaffold — agent-runtime vitest, tool test stubs, copilot E2E stub, re-embed dimension SQL</name>
  <read_first>
    - backend/vitest.config.ts (the per-workspace vitest config analog)
    - backend/src/queues/intelligence-alert.worker.test.ts (a backend integration test shape that hits staging via real createClient, per Phase 71 note)
    - frontend/tests/e2e (existing Playwright spec shape + the CDP Network.setBlockedURLs forced-error idiom)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-VALIDATION.md (Wave 0 Requirements; Per-Requirement Verification Map — the exact automated commands each test must assert)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-RESEARCH.md (Validation Architecture, Phase Requirements Test Map; the AGENT-05 vector_dims query and the AGENT-04 get_function_security proof)
  </read_first>
  <behavior>
    - agent-runtime/vitest.config.ts exists; pnpm --filter agent-runtime test runs (even with zero passing tests yet) and is wired into turbo.
    - tools.test.ts scaffold declares (initially skipped/failing) per-tool assertions: each tool builds a per-request client from runtimeContext.get('authorization'); each tool return serialized does not match /clearance|filtered|restricted/i (indistinguishable-empty).
    - copilot.spec.ts (Playwright) scaffold declares (skipped) the AGENT-01 (drawer + Cmd+K), AGENT-06 (EN+AR dir=rtl), and AGENT-03 forced-error (CDP Network.setBlockedURLs to role="alert" neutral copy) flows.
    - scripts/verify-rag-chunks-dims.sql contains the AGENT-05 proof (count of rows where vector_dims(embedding) <> 1024 must be 0) plus the AGENT-04 prosecdef proof for hybrid_rag_search.
  </behavior>
  <action>
    Create the net-new agent-runtime workspace test infra (vitest.config.ts mirroring backend's). NOTE the workspace package.json is authored in Plan 72-05, so scope this task to the config + test files only; if pnpm --filter agent-runtime cannot resolve before 72-05, place a minimal package.json stub with only name + a test script and let 72-05 complete it (flag in the SUMMARY). Author the failing/skipped test scaffolds named in the behavior block as the Nyquist Wave-0 gaps from VALIDATION.md. Author scripts/verify-rag-chunks-dims.sql with the two DB-schema proofs (reuse the P68 get_function_security helper for the prosecdef check). These tests stay RED until their implementing waves (re-embed Wave 3, tools Wave 5, drawer Wave 6) turn them GREEN — they exist now so the sampling rate has targets.
  </action>
  <verify>
    <automated>grep -q "vector_dims(embedding) <> 1024" scripts/verify-rag-chunks-dims.sql && grep -q "prosecdef" scripts/verify-rag-chunks-dims.sql && test -f agent-runtime/vitest.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - test -f agent-runtime/vitest.config.ts exits 0.
    - grep over agent-runtime/src/mastra/tools/__scaffold__/tools.test.ts for the pattern clearance|filtered|restricted inside a not.toMatch assertion matches at least once.
    - grep -q "vector_dims(embedding) <> 1024" scripts/verify-rag-chunks-dims.sql exits 0 AND grep -q prosecdef scripts/verify-rag-chunks-dims.sql exits 0.
    - frontend/tests/e2e/copilot.spec.ts references setBlockedURLs and asserts role="alert" for the forced-error flow.
  </acceptance_criteria>
  <done>Wave-0 test infra + scaffolds + dimension-proof SQL exist; all VALIDATION.md Wave-0 gaps for test infrastructure have a file.</done>
</task>

<task type="auto">
  <name>Task 3: Register the copilot i18n namespace (EN+AR) — silent-English-fallback guard</name>
  <read_first>
    - frontend/src/i18n/index.ts (lines 5-263 the static imports; the resources map at L264, en branch L265, ar branch L397; lookupLocalStorage 'id.locale' L549)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-UI-SPEC.md (the entire Copywriting Contract table + the icon-only aria-label table — every EN/AR string the namespace must contain)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-PATTERNS.md (Shared Pattern E — the exact static-import + resources-map registration form)
  </read_first>
  <action>
    Create frontend/src/i18n/en/copilot.json and frontend/src/i18n/ar/copilot.json containing every string from the UI-SPEC Copywriting Contract and the icon-only aria-label table (primary CTA, Cmd+K entry, send/stop/copy/close/delete aria-labels, empty-state heading+body for dossier-context AND no-dossier, the neutral no-answer/indistinguishable-empty copy, error heading+body+retry, the Thinking indicator, delete-conversation confirmation). Register both in frontend/src/i18n/index.ts: add the enCopilot + arCopilot static imports next to the existing imports, and add a copilot entry referencing enCopilot in the en branch and arCopilot in the ar branch of the resources map. The strings MUST contain NO forbidden substring (clearance, filtered, restricted) anywhere — the no-answer copy is the approved neutral string per the UI-SPEC indistinguishable-empty hard constraint. Sentence case; no emoji; no marketing voice (CLAUDE.md). Author scripts/check-copilot-i18n.mjs as a tiny node guard that loads both JSON files, fails if any forbidden substring appears, and asserts EN/AR key parity.
  </action>
  <verify>
    <automated>node scripts/check-copilot-i18n.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node scripts/check-copilot-i18n.mjs exits 0 (no forbidden substring; EN/AR key parity holds).
    - frontend/src/i18n/index.ts contains a static import of en/copilot.json and ar/copilot.json AND a copilot entry in both the en and ar branches of the resources map.
    - The frontend i18n-namespace guard (scripts/check-i18n-namespaces.mjs, folded into Lint in P68) passes with the copilot namespace registered.
  </acceptance_criteria>
  <done>copilot namespace registered in the static bundle; AR copy will not silently fall back to English; forbidden-substring guard is green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                                                                        |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| browser -> agent-runtime (spike) | The caller JWT crosses here in the Authorization header; the spike proves it survives the AG-UI bridge to the tool |
| spike tool -> Supabase           | Per-request anon-key client carrying the caller JWT; RLS is the floor                                              |
| @copilotkit/runtime -> network   | Air-gap boundary; zero egress, no Copilot Cloud key (D-09 gate 3)                                                  |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                                              | Disposition | Mitigation Plan                                                                                                                                                                                              |
| ---------- | ---------------------- | -------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-72-01-01 | Elevation of Privilege | spike tool execute()                                                                   | mitigate    | Spike GATE 1 asserts runtimeContext.get('authorization') is non-empty before any RPC; if the bridge drops it the server-middleware workaround is recorded — never fall back to service-role in a tool        |
| T-72-01-02 | Information Disclosure | @copilotkit/runtime egress                                                             | mitigate    | GATE 3 runs the runtime with egress blocked + no Cloud key; confirm zero outbound non-local calls before CopilotKit locks                                                                                    |
| T-72-01-03 | Information Disclosure | copilot i18n strings                                                                   | mitigate    | check-copilot-i18n.mjs asserts no clearance/filtered/restricted substring anywhere in EN/AR copy (indistinguishable-empty)                                                                                   |
| T-72-SC    | Tampering              | npm installs (spike deps: @ag-ui/mastra, @copilotkit/\*, @ai-sdk/openai, assistant-ui) | mitigate    | RESEARCH Package Legitimacy Audit cleared 7/7 core packages [OK]; @ag-ui/mastra is the one Approved-verify-in-spike item — confirm the registerCopilotKit export imports cleanly and pin the passing version |

</threat_model>

<verification>
- Spike server boots; a chat turn round-trips over AG-UI SSE against local Ollama.
- SPIKE-FINDINGS.md carries: shell_decision, the JWT-delivery path, pinned @mastra/core + @ag-ui/mastra versions, the air-gap result.
- pnpm --filter agent-runtime test resolves (config present); scaffolds are RED (expected).
- node scripts/check-copilot-i18n.mjs exits 0; the i18n-namespace guard passes.
- scripts/verify-rag-chunks-dims.sql exists with both the AGENT-05 and AGENT-04 proofs.
</verification>

<success_criteria>

- Spike gates 1-3 resolved and recorded; the production shell is decided (copilotkit OR assistant-ui).
- agent-runtime test infrastructure exists; the Wave-0 test gaps from VALIDATION.md each have a file.
- copilot i18n namespace registered (EN+AR), forbidden-substring-clean, key-parity-clean.
- The throwaway spike dir is flagged for deletion/ignore before merge (it is NOT production code).
  </success_criteria>

<output>
Create `.planning/phases/72-agent-platform-runtime-retrieval-reads/72-01-SUMMARY.md` when done. Record the shell_decision verbatim (downstream plans 72-05/72-08 branch on it), the JWT-delivery path, and the pinned passing versions.
</output>
