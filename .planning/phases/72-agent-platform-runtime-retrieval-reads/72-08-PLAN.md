---
phase: 72-agent-platform-runtime-retrieval-reads
plan: 08
type: execute
wave: 6
depends_on: ['72-01', '72-06']
files_modified:
  - frontend/src/components/copilot/CopilotDrawer.tsx
  - frontend/src/components/copilot/CopilotMessageList.tsx
  - frontend/src/components/copilot/CopilotComposer.tsx
  - frontend/src/components/copilot/CitationCard.tsx
  - frontend/src/components/copilot/ThreadList.tsx
  - frontend/src/components/copilot/copilot-commands.ts
  - frontend/src/components/copilot/copilot-theme.css
  - frontend/src/components/keyboard-shortcuts/CommandPalette.tsx
  - frontend/src/components/layout/Topbar.tsx
  - frontend/src/routes/_protected.tsx
autonomous: false
requirements: [AGENT-01, AGENT-06, AGENT-03]
user_setup: []

must_haves:
  truths:
    - 'A responsive copilot drawer (desktop slide-over mirroring the 720px dossier drawer + mobile BottomSheet) opens from a topbar FAB and Cmd+K'
    - 'The conversational shell is token-bound (copilot-kit vars remapped to IntelDossier tokens) with dir=rtl + Tajawal in Arabic, no raw hex, no card shadows'
    - 'Opening on a dossier passes that dossier as readable context (useCopilotReadable-style); Cmd+K from a dossier pre-fills it'
    - 'Replies render as token-bound bilingual cards (message + citation) with deep-links; no-answer copy is indistinguishable-empty'
    - 'Implements AGENT-01 (drawer + Cmd+K), AGENT-06 (EN/AR + RTL), AGENT-03 (neutral empty); D-04/D-05/D-09 surface'
  artifacts:
    - path: 'frontend/src/components/copilot/CopilotDrawer.tsx'
      provides: 'the responsive drawer/sheet shell mounting the conversational surface'
      contains: 'Sheet'
    - path: 'frontend/src/components/copilot/copilot-theme.css'
      provides: 'the copilot-kit -> IntelDossier token remap + shadow-neutralization + dir=rtl'
      contains: '--copilot-kit'
  key_links:
    - from: 'frontend/src/routes/_protected.tsx'
      to: 'agent-runtime /chat'
      via: 'CopilotKit provider runtimeUrl /api/copilot/chat + Bearer token + x-language headers'
      pattern: 'runtimeUrl'
    - from: 'CommandPalette.tsx'
      to: 'CopilotDrawer'
      via: 'copilot command row pre-filling dossier context'
      pattern: 'copilot'
---

<objective>
Build the responsive copilot surface to the approved UI-SPEC: a single drawer (desktop right slide-over mirroring the 720px dossier drawer; mobile BottomSheet) launched from a topbar FAB and Cmd+K, mounted in the _protected tree, talking to agent-runtime /chat under the caller JWT + language. The conversational shell is the spike's shell_decision (CopilotKit re-skinned via copilot-kit token vars, or assistant-ui headless), token-bound + RTL/Tajawal. Context-aware: opening on a dossier passes it as readable context; Cmd+K from a dossier pre-fills it.

Purpose: this is the user-facing AGENT-01/06 deliverable — where a cleared user actually converses with the copilot.
Output: the drawer + message list + composer + citation card + thread list + the copilot-kit token remap + Cmd+K/FAB entries + the \_protected mount.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-UI-SPEC.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-PATTERNS.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-RESEARCH.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-VALIDATION.md
@agent-runtime/spike/SPIKE-FINDINGS.md

<interfaces>
The CopilotKit provider (RESEARCH Pattern 2 L338-348): <CopilotKit runtimeUrl="/api/copilot/chat" agent="copilot"
headers={{ authorization: `Bearer ${session.access_token}`, 'x-language': i18n.language }}>. Mount under _protected (authenticated-only).
DossierDrawer (DossierDrawer.tsx L89-133): Sheet + SheetContent side="right" className="drawer w-[min(720px,92vw)] max-md:w-screen max-md:border-0 max-md:shadow-none p-0 gap-0";
useSyncExternalStore matchMedia('(max-width: 768px)') (L69-85, jsdom-safe); the role="alert" error branch + Retry min-h-11.
Mobile branch (RelationshipSidebar.tsx L494-510): BottomSheet/BottomSheetContent showHandle, snapPreset="large"; unread dot bg-[var(--accent)].
Cmd+K wiring (CommandPalette.tsx): mirror P71 getAnalyzeCommandActions (import L88, label map L141, suggestedActions L219); extractDossierIdFromPathname (analyze-commands.ts L77-83) reusable verbatim.
_protected mount (L88-108): add <CopilotDrawer/> wrapped in <ErrorBoundary> as a sibling of <DossierDrawer/>.
copilot-kit token remap (UI-SPEC CopilotKit theming contract): background->--surface, foreground->--ink, primary->--accent, border->--line, separators->--line-soft, muted->--ink-mute; shadow vars -> none; radii --radius-sm/--radius/--radius-lg; dir=rtl + Tajawal.
Bundle: frontend entry near 460KB ceiling — DYNAMIC-IMPORT the CopilotDrawer so CopilotKit react-ui weight stays out of the entry chunk.
All copy via the copilot i18n namespace (registered in 72-01). markdown: react-markdown + remark-gfm + rehype-sanitize (XSS gate).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CopilotDrawer shell (desktop Sheet + mobile BottomSheet) + copilot-kit token remap (D-04/D-09)</name>
  <read_first>
    - frontend/src/components/dossier/DossierDrawer/DossierDrawer.tsx (lines 69-133 the Sheet wiring + width token + matchMedia hook + role="alert" error branch + Retry min-h-11)
    - frontend/src/components/dossier/RelationshipSidebar.tsx (lines 29-35 imports; 494-510 the BottomSheet mobile branch; 436 the accent unread dot)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-UI-SPEC.md (Component Inventory: CopilotDrawer/CopilotSheet/States; the CopilotKit theming contract; Color/Typography/Spacing tokens; Visual Hierarchy + RTL reading order)
    - agent-runtime/spike/SPIKE-FINDINGS.md (shell_decision — CopilotKit vs assistant-ui; the proven RTL/token approach)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-PATTERNS.md (the CopilotDrawer + CopilotSheet + the copilot-kit token remap No-Analog row)
  </read_first>
  <action>
    Create frontend/src/components/copilot/CopilotDrawer.tsx mirroring DossierDrawer: a Radix Sheet side="right" className "drawer w-[min(720px,92vw)] max-md:w-screen max-md:border-0 max-md:shadow-none p-0 gap-0", the useSyncExternalStore matchMedia('(max-width: 768px)') hook for the mobile shadow override, the role="alert" error branch (heading + body + Retry min-h-11) using the copilot i18n error copy. On mobile (<=768px) render the same single conversational surface inside a BottomSheet (showHandle, snapPreset large) — one data path, only chrome differs (D-04). Inside, render the conversational shell per shell_decision: if CopilotKit, the CopilotChat slot re-skinned; if assistant-ui, the headless Thread. Create frontend/src/components/copilot/copilot-theme.css: remap every --copilot-kit-* var to an IntelDossier token (background->--surface, foreground->--ink, primary/accent->--accent, border->--line, separators->--line-soft, muted->--ink-mute), neutralize the shadow vars to none on message/citation surfaces, radii from --radius-*; set dir="rtl" handling so Tajawal applies in Arabic. NO raw hex, NO card shadows (drawer --shadow-lg is the only allowed one). DYNAMIC-IMPORT CopilotDrawer at the mount so CopilotKit react-ui stays out of the entry chunk (Bundle Size ceiling).
  </action>
  <verify>
    <automated>grep -q "Sheet" frontend/src/components/copilot/CopilotDrawer.tsx && grep -q "copilot-kit" frontend/src/components/copilot/copilot-theme.css</automated>
  </verify>
  <acceptance_criteria>
    - CopilotDrawer renders a desktop Sheet (drawer w-[min(720px,92vw)]) and a mobile BottomSheet from one data path; the matchMedia hook + role="alert" error branch are present.
    - copilot-theme.css remaps the --copilot-kit-* vars to IntelDossier tokens and neutralizes shadow vars; no raw hex; no card shadow on message/citation surfaces.
    - The drawer is dynamic-imported at the mount (entry chunk does not grow by the full CopilotKit react-ui weight; verify via the bundle/size-limit check).
    - Rendered in Arabic, the drawer container is dir="rtl" and Tajawal applies (logical properties only — ms/me/ps/pe).
  </acceptance_criteria>
  <done>The responsive token-bound drawer/sheet exists; copilot-kit chrome is remapped to tokens + RTL; bundle-safe via dynamic import.</done>
</task>

<task type="auto">
  <name>Task 2: Message list + composer + citation card + thread list (AGENT-06 bilingual, indistinguishable-empty)</name>
  <read_first>
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-UI-SPEC.md (Component Inventory: MessageList, Composer, StreamingIndicator, CitationCard, ThreadList; Copywriting Contract — every EN/AR string + the icon aria-labels; Color accent-reserved list; the indistinguishable-empty hard constraint)
    - frontend/src/pages/word-assistant/WordAssistantPage.tsx (the Message/role/composer scaffold for STRUCTURE only — not visual; left untouched as a component)
    - frontend/src/components/dossier/DossierDrawer/DrawerHead.tsx (the header title + close + chip-row logical order)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-PATTERNS.md (the MessageList/Composer/CitationCard/ThreadList rows + their analogs)
  </read_first>
  <action>
    Create the four token-bound conversational components per the UI-SPEC: CopilotMessageList.tsx (RTL-aware vertical stream, --gap between turns, user+assistant rows both --surface with --ink text and a --t-meta role label, a copy-message icon button per assistant turn with aria-label, markdown via react-markdown + remark-gfm + rehype-sanitize as the XSS gate); CopilotComposer.tsx (textarea --t-body on --surface + 1px solid --line + accent focus ring, Send = .btn-primary, Stop control while streaming, logical ps/pe padding, send/stop aria-labels); CitationCard.tsx (flat --surface card, 1px solid --line, --radius, --pad, NO shadow, source line --t-meta + mono source ID --t-mono-small, deep-links to dossier/signal/graph/record, selected chip --accent — NO rich generative dossier card, that is P73); ThreadList.tsx (rows at --row-h, --line-soft dividers, hover --shadow-sm, title --t-body + meta --t-meta, active/unread dot bg-[var(--accent)], delete-conversation affordance OR archive-only). All copy from the copilot namespace. The empty/no-answer state uses ONLY the neutral indistinguishable-empty copy (no clearance/filtered/restricted). The StreamingIndicator is the accent caret + "Thinking…" (--t-meta). Wire useCopilotReadable-style dossier context (D-05) so the current dossier scopes answers.
  </action>
  <verify>
    <automated>grep -q "rehype-sanitize" frontend/src/components/copilot/CopilotMessageList.tsx && grep -q "btn-primary" frontend/src/components/copilot/CopilotComposer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - MessageList renders markdown through rehype-sanitize (no dangerouslySetInnerHTML of raw model output); user+assistant rows are --surface (no accent-tinted bubbles); each icon control has an aria-label.
    - Composer Send is .btn-primary (accent), the Stop control shows only while streaming, padding uses logical ps/pe.
    - CitationCard is flat --surface + --line, NO shadow, mono source ID, deep-links; selected chip is --accent.
    - The no-answer/empty copy matches the UI-SPEC neutral string; a grep of the rendered copilot tree finds no clearance/filtered/restricted token; the accent token appears only in the reserved spots.
  </acceptance_criteria>
  <done>The bilingual token-bound message/composer/citation/thread components exist; RTL-safe; indistinguishable-empty; XSS-gated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Mount in _protected + Cmd+K/FAB entries + human RTL/visual verify</name>
  <read_first>
    - frontend/src/routes/_protected.tsx (lines 88-108 the provider + drawer-sibling mount point)
    - frontend/src/components/keyboard-shortcuts/CommandPalette.tsx (the P71 getAnalyzeCommandActions wiring at L88/141/219; extractDossierIdFromPathname in analyze-commands.ts L77-83)
    - frontend/src/components/layout/Topbar.tsx (lines 75-110 the .tb-search Cmd+K pill + icon-button idiom for the FAB)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-UI-SPEC.md (the Topbar FAB + Cmd+K entry row; the aria-labels; the kbd hint --font-mono)
  </read_first>
  <action>
    Create frontend/src/components/copilot/copilot-commands.ts mirroring analyze-commands.ts: a pure pathname->command helper returning one command (open the drawer with the dossier id pre-filled as readable context, or empty off a dossier route), reusing extractDossierIdFromPathname verbatim. Wire it into CommandPalette.tsx the P71 way (import + localized-label map + per-route suggestedActions) as a copilot command group. Add the copilot FAB to Topbar.tsx (.btn-ghost icon button, aria-label "Ask the copilot", lucide glyph, opens the drawer) + the Cmd+K copilot row with the --font-mono kbd hint. Mount in _protected.tsx: wrap the CopilotKit provider (runtimeUrl="/api/copilot/chat", agent="copilot", headers Bearer token + x-language) around a dynamic-imported <CopilotDrawer/> as a sibling of <DossierDrawer/>, each in an <ErrorBoundary>. Then PAUSE for human verification.
  </action>
  <verify>
    <human-check>
On staging (or local against staging), in BOTH EN and AR:
1. Click the topbar copilot FAB -> the drawer opens (desktop right slide-over; resize <=768px -> bottom sheet).
2. Open via Cmd+K -> the "Ask the copilot" row appears; launched from a dossier it pre-fills that dossier as context.
3. Switch to Arabic (ع button -> localStorage['id.locale']='ar') -> the drawer is dir="rtl", Tajawal applies, the composer/message rows flip (Send at inline-end), at 1024px AND 1400px.
4. Send a question -> a streamed reply renders as token-bound cards; a no-result query shows the neutral "couldn't find anything to answer that" copy (NO clearance/filtered/restricted anywhere, incl. aria-live).
5. Confirm no raw hex / no card shadow on message/citation surfaces (design bar).
    </human-check>
  </verify>
  <acceptance_criteria>
    - copilot-commands.ts returns the dossier-context-prefilled command; CommandPalette shows the copilot row; the FAB opens the drawer.
    - _protected.tsx mounts the CopilotKit provider (runtimeUrl /api/copilot/chat + Bearer + x-language) around the dynamic-imported CopilotDrawer in an ErrorBoundary.
    - Human confirms: drawer opens from FAB + Cmd+K; AR is dir=rtl + Tajawal + flipped at 1024/1400px; streamed token-bound reply; neutral empty copy; no raw hex / no card shadow.
  </acceptance_criteria>
  <done>The copilot is reachable from the FAB + Cmd+K, mounted authenticated-only, bilingual + RTL-correct, human-verified EN+AR.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                                  |
| ---------------------------- | -------------------------------------------------------------------------------------------- |
| browser -> /api/copilot/chat | The CopilotKit provider sends the caller JWT + language; nginx proxies to agent-runtime:4100 |
| agent markdown -> DOM        | rehype-sanitize gate; agent output is untrusted                                              |
| \_protected route            | Authenticated-only mount; unauthenticated users are redirected                               |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                      | Disposition | Mitigation Plan                                                                                                                    |
| ---------- | ---------------------- | ---------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| T-72-08-01 | Tampering              | XSS via agent-rendered markdown                | mitigate    | react-markdown + rehype-sanitize; no dangerouslySetInnerHTML of raw model output                                                   |
| T-72-08-02 | Information Disclosure | clearance inference in rendered copy           | mitigate    | Indistinguishable-empty copy only; no clearance/filtered/restricted in visible copy OR aria-live; verified by the forced-error E2E |
| T-72-08-03 | Information Disclosure | unauthenticated copilot access                 | mitigate    | Mounted in \_protected (redirects unauthenticated); provider carries the caller JWT                                                |
| T-72-08-04 | Information Disclosure | CopilotKit egress / Cloud key                  | mitigate    | runtimeUrl points at the local agent-runtime; no publicApiKey; air-gap proven in the spike                                         |
| T-72-SC    | Tampering              | @copilotkit/react-\* + react-markdown installs | mitigate    | RESEARCH audit cleared the packages; dynamic-import keeps weight out of the entry chunk (bundle gate)                              |

</threat_model>

<verification>
- pnpm --filter frontend type-check + lint (--max-warnings 0, PascalCase filename rule for components/**) pass.
- Bundle Size Check: entry chunk does not absorb the full CopilotKit react-ui weight (dynamic import).
- copilot.spec.ts (the Wave-0 E2E) GREEN: drawer + Cmd+K (AGENT-01), EN+AR dir=rtl (AGENT-06), forced-error neutral copy (AGENT-03).
- Human checkpoint: EN+AR drawer/FAB/Cmd+K/RTL/empty-copy/design-bar confirmed.
</verification>

<success_criteria>

- A cleared user converses with the copilot from the drawer AND Cmd+K (AGENT-01).
- Replies render as token-bound bilingual cards with correct RTL in Arabic (AGENT-06).
- Reduced/empty results show the neutral indistinguishable-empty copy (AGENT-03).
- D-04 (responsive drawer) + D-05 (context-aware) + D-09 (token-bound shell) delivered.
  </success_criteria>

<output>
Create `.planning/phases/72-agent-platform-runtime-retrieval-reads/72-08-SUMMARY.md` when done. Record the shell used, the bundle delta, and whether thread management shipped as delete or archive-only.
</output>
