---
phase: 72-agent-platform-runtime-retrieval-reads
plan: 04
type: execute
wave: 3
depends_on: ['72-03']
files_modified:
  - backend/src/jobs/reembed-rag-chunks.ts
  - backend/src/jobs/reembed-rag-chunks.test.ts
  - backend/src/jobs/chunk-source-content.ts
autonomous: true
requirements: [AGENT-05, AGENT-04]
user_setup: []

must_haves:
  truths:
    - 'The three 72-03 migrations are applied to staging via Supabase MCP before any verification or RAG smoke-test'
    - 'A one-time re-embed backfill chunks + embeds the corpus into rag_chunks at bge-m3 1024-dim with no pad/truncate'
    - 'Every rag_chunks row passes array_length(embedding,1) = 1024 (the AGENT-05 dimension proof)'
    - 'Implements AGENT-05 (one-time re-embed, no drift) and lands the AGENT-04 store live'
    - "D-06: the v1 re-embed corpus is the core intelligence text (dossiers, signals, briefs, after-action, positions) plus OCR'd documents, chunked into the single bge-m3 1024 store"
  artifacts:
    - path: 'backend/src/jobs/reembed-rag-chunks.ts'
      provides: 'One-shot backfill: chunk + embed 6 source types into rag_chunks with the 1024 write guard'
      contains: '1024'
  key_links:
    - from: 'backend/src/jobs/reembed-rag-chunks.ts'
      to: 'rag_chunks'
      via: 'upsert with onConflict source_type,source_id,chunk_index'
      pattern: 'rag_chunks'
---

<objective>
Apply the 72-03 migrations to staging via the Supabase MCP (the mandatory [BLOCKING] step — build/typecheck pass without it, creating a false-positive verification state), then run the one-time re-embed backfill that fills rag_chunks at bge-m3 1024-dim. The corpus is tiny (~23 live rows: dossiers 12 / positions 2 / aa_commitments 9; signals/events/briefs/documents 0) so this is a one-shot, no batching/throughput planning.

Purpose: rag_chunks is empty until backfilled; the RAG tool and the AGENT-05 dimension proof both need rows. The apply gates every downstream live proof.
Output: the migrations live on staging, the re-embed job, and a green array_length=1024 check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-RESEARCH.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-PATTERNS.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-VALIDATION.md

<interfaces>
The 1024 write guard + storeEmbedding shape (embeddings-service.ts:544-565): embed, assert embedding.length === 1024
(throw on mismatch, NEVER pad/truncate), upsert into the store with onConflict. Re-point .from('ai_embeddings') -> 'rag_chunks'
and onConflict 'owner_type,owner_id,model' -> 'source_type,source_id,chunk_index'.
Embedder: Xenova/bge-m3 ONNX (embeddings-service.ts:96, config default 1024) OR TEI bge-m3.
BullMQ enqueue: hyphen jobIds only (intelligence-alert.worker.ts L65 — BullMQ 5.x forbids ':').
service-role is CORRECT here (background/no-user backfill — the D-10 cron carve-out, document app-layer authz).
Migrations are applied via Supabase MCP apply_migration to staging zkrcjzdemdmwhearhfgg (CLAUDE.md), NOT supabase db push.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Apply the three 72-03 migrations to staging via Supabase MCP</name>
  <read_first>
    - supabase/migrations/20260618_phase72_rag_chunks.sql (the authored migration to apply)
    - supabase/migrations/20260618_phase72_hybrid_rag_search.sql
    - supabase/migrations/20260618_phase72_mastra_threads_rls.sql
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-VALIDATION.md (the AGENT-04 prosecdef proof + the schema-proof expectations)
  </read_first>
  <action>
    Apply the three migrations to staging zkrcjzdemdmwhearhfgg using the Supabase MCP apply_migration (NOT a local supabase db push — types come from generated config, not the live DB, so build/typecheck are green even unapplied; this is the false-positive state the apply closes). Order: rag_chunks first, then hybrid_rag_search (depends on the table), then mastra_threads_rls (guarded for library-managed tables; safe to apply now or re-apply after the runtime boots). After applying, run the schema proofs via MCP execute_sql: SELECT prosecdef FROM pg_proc WHERE proname='hybrid_rag_search' must be false; confirm rag_chunks exists with the HNSW + 2 GIN indexes and the RLS policy uses user_id = auth.uid(). Confirm the live dossiers.sensitivity_level column type here (INTEGER 1/2/3 — LIVE-VERIFIED 2026-06-18; the old CREATE TABLE's TEXT was superseded) so Task 2's mapping reads it directly. This task is non-interactive (autonomous) — the MCP apply needs no human input.
  </action>
  <verify>
    <automated>grep -q "halfvec(1024)" supabase/migrations/20260618_phase72_rag_chunks.sql</automated>
  </verify>
  <acceptance_criteria>
    - The three migrations are applied on staging (MCP apply_migration succeeded for each).
    - MCP execute_sql confirms prosecdef=false for hybrid_rag_search (AGENT-04).
    - MCP execute_sql confirms rag_chunks exists with the clearance RLS policy in the user_id = auth.uid() form.
    - The live dossiers.sensitivity_level type is recorded in the SUMMARY for Task 2's mapping.
  </acceptance_criteria>
  <done>The chunks store + hybrid RPC + thread RLS are live on staging; the schema proofs pass; downstream live proofs are unblocked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: One-time re-embed backfill into rag_chunks (AGENT-05)</name>
  <read_first>
    - backend/src/ai/embeddings-service.ts (lines 96, 538-565 the storeEmbedding 1024 guard + Xenova/bge-m3 embedder)
    - backend/src/queues/intelligence-alert.worker.ts (lines 65-70 the BullMQ hyphen-jobId enqueue idiom)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-PATTERNS.md (the reembed-rag-chunks assignment — analog + the 1024 guard re-point; the tiny-corpus one-shot note)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-RESEARCH.md (Re-embed Job section; the per-source sensitivity resolution; A2 corpus sizing)
  </read_first>
  <action>
    Create backend/src/jobs/reembed-rag-chunks.ts: a one-shot backfill (a script or a single BullMQ job with a hyphen jobId — never ':'). For each of the 6 source types present (dossiers, positions, aa_commitments now; signals/briefs/after_actions/documents when seeded), pull the text fields, chunk them (extract the chunking into chunk-source-content.ts — a small helper; chunking strategy is planner discretion, e.g. ~512-token windows), embed each chunk via the Xenova/bge-m3 ONNX embedder (or TEI bge-m3), assert embedding.length === 1024 (throw on mismatch — NEVER pad/truncate, the AGENT-05 guarantee), and upsert into rag_chunks with onConflict (source_type, source_id, chunk_index). Resolve sensitivity_level per source EXPLICITLY using the mapping confirmed in Task 1 (dossier: use dossiers.sensitivity_level INTEGER 1/2/3 directly; signal/position/brief/document from the owning dossier) — never default. Use service-role for this background job (the D-10 cron carve-out; document the explicit app-layer authz). The store is empty for signals/briefs/documents on staging today — the job processes whatever exists and is idempotent so the 72-09 seed step can re-run it after seeding.
  </action>
  <verify>
    <automated>grep -q "rag_chunks" backend/src/jobs/reembed-rag-chunks.ts && grep -q "1024" backend/src/jobs/reembed-rag-chunks.ts</automated>
  </verify>
  <acceptance_criteria>
    - reembed-rag-chunks.ts upserts into rag_chunks with onConflict source_type,source_id,chunk_index and throws if any embedding length is not 1024.
    - Any BullMQ jobId uses hyphens, never a colon.
    - sensitivity_level is resolved per source table (no DEFAULT reaching a row); the mapping matches Task 1's confirmed dossiers type.
    - After running against staging, scripts/verify-rag-chunks-dims.sql returns 0 rows failing the dimension check (run via MCP execute_sql).
  </acceptance_criteria>
  <done>rag_chunks backfilled at native 1024-dim with no drift; per-source sensitivity correct; AGENT-05 dimension proof green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                             |
| -------------------------- | --------------------------------------------------------------------------------------- |
| re-embed job -> rag_chunks | Service-role write (the cron/no-user carve-out); the only non-JWT write path this phase |
| MCP apply -> staging DB    | Schema change boundary; applied by the executor via MCP, not the CI pipeline            |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                     | Disposition | Mitigation Plan                                                                                                                     |
| ---------- | ---------------------- | --------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| T-72-04-01 | Tampering              | embedding dimension drift                     | mitigate    | The 1024 write guard throws on any mismatch (never pad/truncate); AGENT-05 proof asserts 0 failing rows                             |
| T-72-04-02 | Elevation of Privilege | re-embed service-role write                   | accept      | Background/no-user backfill is the documented D-10 cron carve-out; explicit app-layer authz noted; no caller path uses service-role |
| T-72-04-03 | Information Disclosure | mis-resolved sensitivity over-exposing chunks | mitigate    | Per-source sensitivity resolved from the owning dossier explicitly; never defaulted; verified by the L1/L3 proof in 72-09           |
| T-72-SC    | Tampering              | re-embed deps (BullMQ already pinned)         | mitigate    | No new package install; reuses existing embeddings-service + BullMQ 5.77.1 (hyphen jobIds)                                          |

</threat_model>

<verification>
- The three migrations are live on staging (MCP apply succeeded).
- prosecdef=false for hybrid_rag_search; rag_chunks RLS uses user_id = auth.uid().
- reembed-rag-chunks.ts has the 1024 guard + rag_chunks upsert + hyphen jobId.
- scripts/verify-rag-chunks-dims.sql returns 0 failing rows after the backfill.
</verification>

<success_criteria>

- [BLOCKING] migrations applied via Supabase MCP before any verification.
- rag_chunks backfilled at 1024-dim, per-source sensitivity correct, idempotent.
- AGENT-05 (no dimension drift) + AGENT-04 (INVOKER store live) proven on staging.
  </success_criteria>

<output>
Create `.planning/phases/72-agent-platform-runtime-retrieval-reads/72-04-SUMMARY.md` when done. Record the confirmed dossiers.sensitivity_level type, the row counts embedded per source, and the dimension-proof result.
</output>
