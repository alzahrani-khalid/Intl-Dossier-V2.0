---
phase: 72-agent-platform-runtime-retrieval-reads
plan: 03
type: execute
wave: 2
depends_on: ['72-01']
files_modified:
  - supabase/migrations/20260618_phase72_rag_chunks.sql
  - supabase/migrations/20260618_phase72_hybrid_rag_search.sql
  - supabase/migrations/20260618_phase72_mastra_threads_rls.sql
autonomous: true
requirements: [AGENT-04, AGENT-05]
user_setup: []

must_haves:
  truths:
    - 'A single rag_chunks store exists: halfvec(1024) HNSW + EN/AR tsvector GIN + denormalized sensitivity_level, RLS via profiles.user_id = auth.uid()'
    - 'hybrid_rag_search is SECURITY INVOKER, fuses dense+sparse by RRF k=60, and RLS runs before the candidates are returned'
    - 'mastra_threads/mastra_messages carry owner-only RLS (user owns own threads only)'
    - 'A clearance-sync trigger keeps rag_chunks.sensitivity_level resolved per-source-table, never defaulted'
    - 'Implements AGENT-04 (single INVOKER+RLS chunks store, hybrid retrieval) and the AGENT-05 store shape; D-08 thread RLS'
    - 'D-06: rag_chunks is the single store for the re-embed corpus (dossiers/signals/briefs/after-action/positions/documents); graph relationships stay in query_graph, not RAG'
  artifacts:
    - path: 'supabase/migrations/20260618_phase72_rag_chunks.sql'
      provides: 'rag_chunks table + HNSW/GIN indexes + clearance RLS + per-source sensitivity-sync trigger'
      contains: 'halfvec(1024)'
    - path: 'supabase/migrations/20260618_phase72_hybrid_rag_search.sql'
      provides: 'hybrid_rag_search SECURITY INVOKER RPC (RRF k=60)'
      contains: 'SECURITY INVOKER'
    - path: 'supabase/migrations/20260618_phase72_mastra_threads_rls.sql'
      provides: 'owner-only RLS on the @mastra/pg-managed thread/message tables'
  key_links:
    - from: 'rag_chunks RLS policy'
      to: 'profiles.clearance_level'
      via: 'sensitivity_level <= (SELECT clearance_level FROM profiles WHERE user_id = auth.uid())'
      pattern: 'user_id = auth.uid'
    - from: 'hybrid_rag_search'
      to: 'rag_chunks'
      via: 'dense HNSW + sparse tsvector FULL OUTER JOIN under caller JWT RLS'
      pattern: 'rrf'
---

<objective>
Author the three migrations that are the data backbone of the copilot's retrieval + memory: (1) the NEW rag_chunks store (halfvec(1024), dual EN/AR tsvector, denormalized sensitivity, HNSW + GIN, clearance RLS) with a per-source-table sensitivity-sync trigger; (2) the hybrid_rag_search SECURITY INVOKER RPC (RRF k=60 over dense + sparse, RLS-before-return); (3) owner-only RLS on the @mastra/pg thread/message tables. The full SQL for (1) and (2) is pre-written in RESEARCH Code Examples — adapt it, do not invent. These are AUTHORED here and APPLIED via Supabase MCP in 72-04 (this plan does not apply).

Purpose: rag_chunks + hybrid_rag_search are the only net-new DB pieces (the three read RPCs already exist); the chunks store is what the re-embed fills and the RAG tool queries.
Output: three migration files committed (not yet applied); build/typecheck pass without the apply (types come from generated config, not the live DB).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-CONTEXT.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-RESEARCH.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-PATTERNS.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-VALIDATION.md

<interfaces>
The full rag_chunks CREATE TABLE + indexes + RLS is pre-written in RESEARCH Code Examples L486-516 (halfvec(1024),
content_tsv_en/content_tsv_ar GENERATED, HNSW halfvec_cosine_ops, GIN on both tsvectors, RLS sensitivity_level <= profiles.user_id clearance).
The full hybrid_rag_search RPC is pre-written in RESEARCH Code Examples L524-565 (LANGUAGE sql, SECURITY INVOKER, RRF k=60,
FULL OUTER JOIN dense/sparse, websearch_to_tsquery EN/AR switch, GRANT to authenticated).
The P68 get_function_security helper (20260614000002 L53-64) is the AGENT-04 prosecdef proof.

CLEARANCE-SOURCE ASYMMETRY (live-verified): only dossiers + intelligence_event carry their own sensitivity_level.
dossiers.sensitivity_level is INTEGER 1/2/3 (LIVE-VERIFIED on staging 2026-06-18: values 1/2/3; 20250930002 L17 declared TEXT but a later migration altered it to integer — the P68 spec-error landmine) — USE DIRECTLY, no text->int CASE.
intelligence_event.sensitivity_level is INTEGER (use directly). intelligence_signals/positions/briefs/aa_commitments/documents
have NONE -> resolve from the OWNING dossier (for signals, the linked dossier/event). NEVER default sensitivity (low over-exposes; NULL deny-alls).

CORRECT RLS form to MIRROR (P69 20260614_phase69_signals_extend.sql L60-65): WHERE user_id = auth.uid().
WRONG analog — do NOT mirror (20251022000009 L102/L119/L131 uses WHERE id = auth.uid() -> deny-all on profiles which has no id).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rag_chunks table + HNSW/GIN indexes + clearance RLS + per-source sensitivity-sync trigger</name>
  <read_first>
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-RESEARCH.md (Code Examples L486-516 the pre-written rag_chunks DDL; the live probe RESULTS L679-687 — pgvector 0.8.0, profiles.user_id, clearance-source asymmetry)
    - supabase/migrations/20260614_phase69_signals_extend.sql (lines 58-66 the CORRECT user_id clearance RLS form to mirror)
    - supabase/migrations/20250930002_create_dossiers_table.sql (lines 37-49 — the GENERATED tsvector + GIN pattern ONLY; its L17 declares sensitivity_level TEXT but the LIVE column is INTEGER 1/2/3 — do NOT trust the TEXT declaration)
    - supabase/migrations/20250129006_create_ai_tables.sql (the ai_embeddings pgvector shape — for reference; do NOT reuse it as the store)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-PATTERNS.md (Shared Pattern C the INVOKER+user_id form + the deny-all landmine; the rag_chunks assignment + clearance-source asymmetry note)
  </read_first>
  <action>
    Create supabase/migrations/20260618_phase72_rag_chunks.sql adapting the pre-written DDL from RESEARCH Code Examples L486-516: CREATE TABLE public.rag_chunks with id, source_type (CHECK in dossier/signal/brief/after_action/position/document), source_id, parent_dossier_id, chunk_index, content, content_lang, sensitivity_level INTEGER NOT NULL CHECK 1-4 (DENORMALIZED), embedding halfvec(1024) NOT NULL, content_tsv_en + content_tsv_ar GENERATED tsvector STORED, organization_id, created_at, UNIQUE(source_type, source_id, chunk_index). Indexes: HNSW (embedding halfvec_cosine_ops) m=16 ef_construction=64; GIN on content_tsv_en; GIN on content_tsv_ar; btree (organization_id, sensitivity_level). RLS: ENABLE ROW LEVEL SECURITY + a SELECT policy for authenticated USING (sensitivity_level <= (SELECT clearance_level FROM public.profiles WHERE user_id = auth.uid())) — user_id, NEVER id.

    Add a sensitivity-sync trigger function (or a resolve helper the re-embed calls) that maps each source's clearance EXPLICITLY: dossier -> use dossiers.sensitivity_level directly (it is INTEGER 1/2/3, LIVE-VERIFIED — do NOT text-CASE it); intelligence_event -> use its INTEGER sensitivity_level directly; signal -> resolve from the linked dossier/intelligence_event sensitivity; brief/after_action/position/document -> resolve from the owning dossier. NEVER default (low over-exposes; NULL deny-alls). The live dossiers.sensitivity_level is INTEGER 1/2/3 (the CREATE TABLE's TEXT declaration was superseded — confirmed via live probe 2026-06-18); write the mapping to read the integer directly. Keep the trigger SECURITY DEFINER only if it must read parent rows the caller can't (document the app-layer rationale, the D-10 cron carve-out); the RLS on rag_chunks itself stays the enforcement floor.

  </action>
  <verify>
    <automated>grep -q "halfvec(1024)" supabase/migrations/20260618_phase72_rag_chunks.sql && grep -q "user_id = auth.uid" supabase/migrations/20260618_phase72_rag_chunks.sql</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "halfvec(1024)" on the rag_chunks migration exits 0.
    - grep -q "user_id = auth.uid" exits 0 AND grep for "id = auth.uid" without the user_ prefix returns nothing (no deny-all form).
    - The migration creates an HNSW index using halfvec_cosine_ops and two GIN indexes (content_tsv_en, content_tsv_ar).
    - The sensitivity-sync logic uses dossiers.sensitivity_level (INTEGER 1/2/3) directly and resolves signal/brief/position/document sensitivity from the owning dossier — no text->int CASE, no DEFAULT sensitivity on the column path that reaches rows.
  </acceptance_criteria>
  <done>rag_chunks store + indexes + clearance RLS + per-source sensitivity-sync authored; the deny-all landmine is avoided (user_id form).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: hybrid_rag_search SECURITY INVOKER RPC (RRF k=60, RLS-before-return)</name>
  <read_first>
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-RESEARCH.md (Code Examples L524-565 the pre-written hybrid_rag_search RPC; the tool-layer iterative-scan note L564; Pitfall 4 overfilter/iterative-scan; Pitfall 5 indistinguishable-empty via JSON keys)
    - supabase/migrations/20260614000002_p68_search_invoker_rpc.sql (lines 16-38 the closest INVOKER vector-RPC analog with WHERE user_id = auth.uid(); lines 53-64 the get_function_security helper)
    - supabase/migrations/20260617_phase71_query_graph.sql (the INVOKER + REVOKE/GRANT + indistinguishable-empty structure to mirror)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-PATTERNS.md (the hybrid_rag_search assignment; Shared Pattern C; Shared Pattern D indistinguishable-empty)
  </read_first>
  <action>
    Create supabase/migrations/20260618_phase72_hybrid_rag_search.sql adapting the pre-written RPC from RESEARCH Code Examples L524-565. Signature: hybrid_rag_search(p_query_embedding halfvec(1024), p_query_text TEXT, p_lang TEXT DEFAULT 'en', p_limit INT DEFAULT 50) RETURNS TABLE(chunk_id, source_type, source_id, content, sensitivity_level, rrf_score). LANGUAGE sql, SECURITY INVOKER (NEVER DEFINER), STABLE. Body: a dense CTE (ROW_NUMBER over embedding <=> p_query_embedding, LIMIT p_limit*2) and a sparse CTE (ts_rank_cd over content_tsv_ar|content_tsv_en via websearch_to_tsquery with the arabic|english config, LIMIT p_limit*2), FULL OUTER JOIN on id, RRF score = 1.0/(60+dense.rank) + 1.0/(60+sparse.rank), ORDER BY rrf_score DESC LIMIT p_limit. RLS on rag_chunks applies inside both CTEs under the caller JWT (RLS-before-return — the reranker in the tool layer only ever sees RLS-passing candidates). REVOKE EXECUTE FROM PUBLIC, anon; GRANT EXECUTE TO authenticated. The RETURNS columns carry NO clearance/filtered/restricted field (indistinguishable-empty — Pitfall 5); the result is content rows only, "no data" and "above clearance" return the same empty shape. Ensure get_function_security('hybrid_rag_search') will return prosecdef=false.
  </action>
  <verify>
    <automated>grep -q "SECURITY INVOKER" supabase/migrations/20260618_phase72_hybrid_rag_search.sql && grep -qi "websearch_to_tsquery" supabase/migrations/20260618_phase72_hybrid_rag_search.sql</automated>
  </verify>
  <acceptance_criteria>
    - The RPC declares SECURITY INVOKER (never DEFINER) and GRANTs EXECUTE only to authenticated (REVOKE from PUBLIC, anon).
    - The body uses a dense + sparse CTE fused by RRF with the constant 60 (k=60), FULL OUTER JOIN on id.
    - The RETURNS TABLE column list contains no token matching clearance|filtered|restricted.
    - p_lang switches content_tsv_ar/content_tsv_en and the arabic/english tsquery config.
  </acceptance_criteria>
  <done>hybrid_rag_search authored INVOKER + RRF k=60 + RLS-before-return + indistinguishable-empty; AGENT-04 RPC contract met.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Owner-only RLS on the @mastra/pg thread/message tables (D-08)</name>
  <read_first>
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-RESEARCH.md (Don't-Hand-Roll thread persistence row; D-08; the No-Analog note on RLS-on-a-library-managed-table)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-PATTERNS.md (the No Analog Found row for @mastra/pg tables — apply Shared Pattern C owner-only SELECT with user_id = auth.uid())
    - supabase/migrations/20260614_phase69_signals_extend.sql (the RLS policy form to mirror)
  </read_first>
  <action>
    Create supabase/migrations/20260618_phase72_mastra_threads_rls.sql. @mastra/pg PostgresStore auto-creates mastra_threads + mastra_messages (no hand-written CREATE TABLE here) — so this migration is idempotent and guards on table existence (e.g. a DO block or IF EXISTS) since the tables are created at runtime by the library on first connect. ENABLE ROW LEVEL SECURITY on both and add owner-only policies: the thread's resourceId (the Mastra resource = the user id, set to auth.uid() by the runtime) must equal the caller. Map the owner column to the caller via auth.uid() (the agent sets resourceId from the authenticated user). Mirror the P69 policy form (TO authenticated, USING owner = auth.uid()). Document that the tables may not exist until the runtime first boots — the apply (72-04) re-runs this after the runtime has provisioned them, OR the policy creation is guarded so it no-ops cleanly if absent and is re-applied. Cross-tenant thread read is the threat this closes.
  </action>
  <verify>
    <automated>grep -qi "mastra_threads" supabase/migrations/20260618_phase72_mastra_threads_rls.sql && grep -qi "row level security" supabase/migrations/20260618_phase72_mastra_threads_rls.sql</automated>
  </verify>
  <acceptance_criteria>
    - The migration ENABLEs RLS on mastra_threads and mastra_messages and adds owner-only SELECT (and write) policies keyed to auth.uid().
    - The migration is guarded for the library-managed tables not yet existing (IF EXISTS / DO block) so it is safe to apply before or after the runtime first provisions them.
    - No policy uses the broken id = auth.uid() form on profiles; thread ownership keys off the Mastra resourceId = caller.
  </acceptance_criteria>
  <done>mastra_threads/mastra_messages owner-only RLS authored; D-08 user-private threads enforced; cross-tenant read closed.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                     |
| ------------------------------- | --------------------------------------------------------------- |
| caller JWT -> rag_chunks        | RLS gate; sensitivity_level <= caller clearance, enforced in-DB |
| caller JWT -> hybrid_rag_search | INVOKER RPC; the only retrieval path; RLS inside both CTEs      |
| caller JWT -> mastra_threads    | Owner-only RLS; a user reads only their own threads             |

## STRIDE Threat Register

| Threat ID  | Category               | Component                         | Disposition | Mitigation Plan                                                                                                                             |
| ---------- | ---------------------- | --------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| T-72-03-01 | Elevation of Privilege | rag_chunks RLS deny-all landmine  | mitigate    | RLS uses profiles.user_id = auth.uid() (never id); mirrors P69/P71; verified by the L1/L3 impersonation proof in 72-09                      |
| T-72-03-02 | Information Disclosure | denormalized sensitivity drift    | mitigate    | Per-source sensitivity-sync resolves clearance from the owning dossier/event explicitly; never defaulted (low over-exposes, NULL deny-alls) |
| T-72-03-03 | Elevation of Privilege | rerank bypassing clearance        | mitigate    | hybrid_rag_search is SECURITY INVOKER; RLS runs inside both CTEs so the tool-layer reranker only sees RLS-passing rows                      |
| T-72-03-04 | Information Disclosure | clearance inference via JSON keys | mitigate    | RETURNS columns carry no clearance/filtered/restricted field; same empty shape for no-data vs above-clearance                               |
| T-72-03-05 | Information Disclosure | cross-tenant thread read          | mitigate    | mastra_threads/mastra_messages owner-only RLS keyed to auth.uid()                                                                           |

</threat_model>

<verification>
- The three migration files exist and parse as SQL (psql --dry-run or the closest static check).
- rag_chunks: halfvec(1024) + HNSW halfvec_cosine_ops + 2 GIN + clearance RLS with user_id = auth.uid().
- hybrid_rag_search: SECURITY INVOKER + RRF k=60 + websearch_to_tsquery + no forbidden field in RETURNS.
- mastra RLS: ENABLE RLS + owner-only policies, guarded for library-managed tables.
- Build + typecheck pass WITHOUT applying (types come from generated config) — the apply is 72-04.
</verification>

<success_criteria>

- rag_chunks store authored (single store, INVOKER-queryable, RLS-gated, per-source sensitivity-synced).
- hybrid_rag_search authored (AGENT-04 hybrid + INVOKER + RLS-before-rerank).
- mastra thread/message owner-only RLS authored (D-08).
- The deny-all landmine (profiles.id) is provably avoided; indistinguishable-empty holds in the RPC contract.
  </success_criteria>

<output>
Create `.planning/phases/72-agent-platform-runtime-retrieval-reads/72-03-SUMMARY.md` when done. Record the confirmed live dossiers.sensitivity_level column type and the final per-source sensitivity mapping. Flag for 72-04 that these are AUTHORED-not-applied.
</output>
