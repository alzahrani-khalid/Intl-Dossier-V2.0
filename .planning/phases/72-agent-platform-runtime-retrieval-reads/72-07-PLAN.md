---
phase: 72-agent-platform-runtime-retrieval-reads
plan: 07
type: execute
wave: 4
depends_on: []
files_modified:
  - backend/src/ai/agents/brief-generator.ts
  - backend/src/ai/agents/intake-linker.ts
  - backend/src/ai/agents/brief-generator.test.ts
  - backend/src/ai/agents/intake-linker.test.ts
autonomous: true
requirements: [AGENT-03]
user_setup: []

must_haves:
  truths:
    - "User-triggered paths in brief-generator.ts run under the caller's JWT (createUserClient), not supabaseAdmin"
    - "User-triggered paths in intake-linker.ts run under the caller's JWT, not supabaseAdmin"
    - 'Genuinely background/cron paths that keep service-role carry a documented explicit app-layer authz justification'
    - 'Implements D-10 (fold + audit the two background agents) closing the folded P68 supabaseAdmin follow-up; reinforces AGENT-03'
  artifacts:
    - path: 'backend/src/ai/agents/brief-generator.ts'
      provides: 'JWT-scoped reads on user-triggered paths; documented carve-outs on cron paths'
      contains: 'createUserClient'
    - path: 'backend/src/ai/agents/intake-linker.ts'
      provides: 'JWT-scoped reads on user-triggered paths; documented carve-outs on cron paths'
  key_links:
    - from: 'brief-generator.ts user-triggered reads'
      to: 'Supabase under caller JWT'
      via: 'createUserClient(authHeader) replacing supabaseAdmin'
      pattern: 'createUserClient'
---

<objective>
Close the folded P68 follow-up (D-10): audit and retire supabaseAdmin from the two background agents while the JWT-scoped-client idiom is fresh. brief-generator.ts has 3 supabaseAdmin call sites (L413, L444, L472); intake-linker.ts has 6 (L194, L243, L264, L392, L422, L452). For each: if the path is user-triggered, swap to createUserClient(authHeader) so RLS applies; if genuinely background/cron, keep service-role but document the explicit app-layer authz (the keystone carve-out).

Purpose: these two agents are the last interactive-adjacent service-role readers; retiring them on user paths completes the keystone coverage the copilot depends on.
Output: both agents triaged + repointed/documented, with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-CONTEXT.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-PATTERNS.md

<interfaces>
The keystone idiom (chat-assistant.ts:24): createUserClient(authHeader) = createClient(SUPABASE_URL, SUPABASE_ANON_KEY,
{ global:{ headers:{ Authorization: authHeader } } }). This is exactly how P68 retired chat-assistant.ts — lift the JWT version, not the old one.

supabaseAdmin call sites to triage (PATTERNS.md verified):

- brief-generator.ts: import L15; call sites L413, L444, L472.
- intake-linker.ts: import L14; call sites L194, L243, L264, L392, L422, L452 (L452 writes ai_entity_link_proposals from a queue — likely the cron carve-out).
  For user-triggered paths: thread the caller JWT in + swap to createUserClient. For genuine background/cron: keep service-role + document explicit app-layer authz.
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retire supabaseAdmin from brief-generator.ts user-triggered paths</name>
  <read_first>
    - backend/src/ai/agents/brief-generator.ts (line 15 the supabaseAdmin import; lines 413, 444, 472 the call sites; the surrounding functions to classify user-triggered vs background)
    - backend/src/ai/agents/chat-assistant.ts (lines 19-28 createUserClient — the JWT version to lift; how P68 threaded the authHeader through)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-PATTERNS.md (the brief-generator.ts D-10 assignment — the call-site triage table)
  </read_first>
  <action>
    Classify each of the 3 brief-generator.ts supabaseAdmin call sites (L413, L444, L472) as user-triggered or genuinely background. Brief generation is user-initiated (a user asks for a brief), so these reads are almost certainly user-triggered: add createUserClient(authHeader) (thread the caller's Authorization through the call chain, exactly as P68 did for chat-assistant) and swap supabaseAdmin -> the per-request client so RLS (sensitivity_level <= clearance) applies. If any site is genuinely a background/scheduled brief (no user in the loop), keep supabaseAdmin but add a comment documenting the explicit app-layer authz (the keystone carve-out). Update the import accordingly. Add/extend brief-generator.test.ts asserting the user-triggered reads use the caller-scoped client (RLS-respecting), mirroring the P68 chat-assistant retirement test shape.
  </action>
  <verify>
    <automated>grep -q "createUserClient" backend/src/ai/agents/brief-generator.ts</automated>
  </verify>
  <acceptance_criteria>
    - brief-generator.ts user-triggered reads use createUserClient(authHeader); supabaseAdmin remains only on documented background paths (if any), each with an explicit app-layer-authz comment.
    - The caller Authorization is threaded through the brief-generation call chain (no read silently runs unscoped).
    - brief-generator.test.ts asserts the user-triggered path is caller-scoped (RLS-respecting).
    - pnpm --filter backend type-check + the brief-generator test pass.
  </acceptance_criteria>
  <done>brief-generator.ts user paths run under the caller JWT; cron carve-outs documented; the D-10 audit half is done.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retire/document supabaseAdmin in intake-linker.ts</name>
  <read_first>
    - backend/src/ai/agents/intake-linker.ts (line 14 the import; lines 194, 243, 264, 392, 422, 452 the call sites — L452 writes ai_entity_link_proposals from a queue, likely the cron carve-out)
    - backend/src/ai/agents/chat-assistant.ts (lines 19-28 createUserClient)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-PATTERNS.md (the intake-linker.ts D-10 assignment — the call-site triage table + the cron carve-out note)
  </read_first>
  <action>
    Classify each of the 6 intake-linker.ts supabaseAdmin call sites (L194, L243, L264, L392, L422, L452). intake-linker runs in two modes: user-triggered linking (a user links an intake ticket) and background queue processing (writing ai_entity_link_proposals, L452, from a worker — no user in the loop). For user-triggered sites: swap to createUserClient(authHeader) (thread the caller JWT) so RLS applies. For genuine background/queue sites (e.g. L452): keep service-role and document the explicit app-layer authz (the keystone carve-out — the queue has no caller JWT). Update the import. Add/extend intake-linker.test.ts asserting the user-triggered reads are caller-scoped and the background write path is the documented carve-out.
  </action>
  <verify>
    <automated>grep -q "createUserClient" backend/src/ai/agents/intake-linker.ts</automated>
  </verify>
  <acceptance_criteria>
    - intake-linker.ts user-triggered sites use createUserClient(authHeader); background/queue sites (e.g. L452 ai_entity_link_proposals) keep service-role WITH a documented app-layer-authz comment.
    - The caller Authorization is threaded through the user-triggered linking path.
    - intake-linker.test.ts asserts the user path is caller-scoped and the background path is the documented carve-out.
    - pnpm --filter backend type-check + the intake-linker test pass.
  </acceptance_criteria>
  <done>intake-linker.ts user paths run under the caller JWT; the queue write is a documented carve-out; the folded P68 supabaseAdmin todo is closed.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                               |
| -------------------------------------- | --------------------------------------------------------- |
| user brief/linking request -> Supabase | Now caller-JWT-scoped; RLS enforces clearance             |
| background queue -> Supabase           | Documented service-role carve-out (no caller in the loop) |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                   | Disposition | Mitigation Plan                                                                                    |
| ---------- | ---------------------- | ------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------- |
| T-72-07-01 | Elevation of Privilege | brief-generator user-path service-role read | mitigate    | Swap to createUserClient(authHeader); RLS applies; closes an above-clearance read path             |
| T-72-07-02 | Elevation of Privilege | intake-linker user-path service-role read   | mitigate    | Swap user-triggered sites to caller JWT; RLS applies                                               |
| T-72-07-03 | Elevation of Privilege | intake-linker background queue write        | accept      | No caller JWT in a queue worker; documented explicit app-layer authz (the keystone cron carve-out) |
| T-72-SC    | Tampering              | no new installs                             | mitigate    | Pure repoint to an existing idiom; no package change                                               |

</threat_model>

<verification>
- brief-generator.ts + intake-linker.ts user paths use createUserClient; cron sites documented.
- The caller Authorization is threaded through both user-triggered call chains.
- Both agent tests pass; backend type-check passes.
</verification>

<success_criteria>

- D-10 fully audited: both background agents triaged, user paths JWT-scoped, cron carve-outs documented.
- The folded P68 supabaseAdmin follow-up is closed.
- AGENT-03 reinforced — no interactive-adjacent path reads above the caller's clearance.
  </success_criteria>

<output>
Create `.planning/phases/72-agent-platform-runtime-retrieval-reads/72-07-SUMMARY.md` when done. Record the per-call-site classification (user-triggered vs cron carve-out) for all 9 sites.
</output>
