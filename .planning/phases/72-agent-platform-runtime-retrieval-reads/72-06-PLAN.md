---
phase: 72-agent-platform-runtime-retrieval-reads
plan: 06
type: execute
wave: 5
depends_on: ['72-04', '72-05']
files_modified:
  - agent-runtime/src/mastra/tools/_supabase.ts
  - agent-runtime/src/mastra/tools/hybrid-rag-search.ts
  - agent-runtime/src/mastra/tools/read-signals.ts
  - agent-runtime/src/mastra/tools/query-graph.ts
  - agent-runtime/src/mastra/tools/generate-digest.ts
  - agent-runtime/src/mastra/tools/dossier-lookups.ts
  - agent-runtime/src/mastra/tools/index.ts
  - agent-runtime/src/mastra/tools/tools.test.ts
autonomous: true
requirements: [AGENT-02, AGENT-03, AGENT-04]
user_setup: []

must_haves:
  truths:
    - "Every read tool builds a per-request Supabase client from runtimeContext.get('authorization') (the JWT keystone), never service-role"
    - 'hybrid_rag_search tool sets iterative-scan GUCs, calls the INVOKER RPC, then reranks via TEI (RLS-before-rerank)'
    - 'read_signals/query_graph/generate_digest-preview wrap the existing P69/P70/P71 RPCs; dossier/work-item lookups read directly'
    - 'generate_digest exposes only the preview path; publish_digest is never called (P73 write)'
    - 'Every tool return is indistinguishable-empty: same empty shape for no-data and above-clearance, no forbidden substring'
    - 'Implements AGENT-02 (answers from gated data under caller JWT), AGENT-03 (reduced results), AGENT-04 (hybrid RAG)'
  artifacts:
    - path: 'agent-runtime/src/mastra/tools/_supabase.ts'
      provides: 'createUserClient(authorization) — the keystone idiom for every tool'
      contains: 'authorization'
    - path: 'agent-runtime/src/mastra/tools/hybrid-rag-search.ts'
      provides: 'the RAG tool: iterative-scan + INVOKER RPC + TEI rerank'
      contains: 'hybrid_rag_search'
  key_links:
    - from: 'every tool'
      to: 'Supabase under caller JWT'
      via: "createUserClient(runtimeContext.get('authorization'))"
      pattern: 'runtimeContext'
    - from: 'hybrid-rag-search.ts'
      to: 'TEI reranker'
      via: 'rerank top candidates AFTER the RLS-gated RPC returns'
      pattern: 'rerank'
---

<objective>
Author the reads-only tool roster (D-07): the keystone _supabase.ts helper + 6 narrow Zod-typed least-privilege tools. Three wrap the already-built INVOKER RPCs (read_signals P69, query_graph P71, generate_digest-preview P70), one is the new hybrid_rag_search (iterative-scan + RPC + TEI rerank), and dossier-lookups reads dossiers/work-items directly. Every tool reads the caller JWT from runtimeContext (the #4465-proven path) and returns indistinguishable-empty on denial. No tool ever gets the service-role key; none calls publish_digest.

Purpose: these tools are how the agent reads gated data under the caller's clearance — the AGENT-02/03 keystone in practice.
Output: 6 tools + the helper + the index + the tool tests (turning the Wave-0 scaffold GREEN).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-CONTEXT.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-RESEARCH.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-PATTERNS.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-VALIDATION.md

<interfaces>
createUserClient (RESEARCH Pattern 3 L355-362): createClient(SUPABASE_URL, SUPABASE_ANON_KEY, { global:{ headers:{ Authorization: authorization } }, auth:{ persistSession:false, autoRefreshToken:false } }).
Tool execute reads runtimeContext.get('authorization') and MUST assert it non-empty before the RPC (the #4465 gate).

Live RPC signatures the tools wrap (verified):
read_signals(p_dossier_id, p_status, p_since, p_limit DEFAULT 50) -> TABLE (P69).
query_graph(p_query_type, p_entity_id, p_entity_id_2, p_window_days DEFAULT 90) -> JSONB (P71);
query_type whitelist: forum_membership, shared_committees, engagement_chain, shortest_path.
generate_digest(p_dossier_id, p_period DEFAULT 'daily') -> JSONB (P70); PREVIEW only — expose p_period in {daily,weekly,monthly}; NEVER publish_digest.
hybrid_rag_search(p_query_embedding halfvec(1024), p_query_text, p_lang DEFAULT 'en', p_limit DEFAULT 50) -> TABLE (72-03).
Before the RAG RPC: SET LOCAL hnsw.iterative_scan='relaxed_order'; SET LOCAL hnsw.max_scan_tuples=20000.

dossier-lookups columns (chat-assistant.ts getDossier L250-263 / listDossiers L352-384 / queryCommitments L277-317 -> aa_commitments):
getDossier select id,name_en,name_ar,type,description_en,description_ar,status,sensitivity_level,tags,metadata; eq is_active true; single; return {dossier:null} on error.
NEVER pass SUPABASE_SERVICE_ROLE_KEY to a tool. Never a generic execute_sql.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _supabase.ts keystone helper + the three RPC-wrapping tools (read_signals, query_graph, generate_digest-preview)</name>
  <read_first>
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-RESEARCH.md (Pattern 3 L350-384 the createUserClient + tool shape; Code Examples L568-576 the three RPC contracts; Anti-Patterns — never service-role in a tool)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-PATTERNS.md (Shared Pattern A keystone; Shared Pattern B edge-fn wrap; the _supabase.ts + per-tool assignments + the live RPC signatures comment block)
    - backend/src/ai/agents/chat-assistant.ts (lines 19-28 createUserClient to mirror)
    - supabase/functions/analytic-graph/index.ts (the JWT-wrap-an-INVOKER-RPC call shape + the query_type whitelist L18)
  </read_first>
  <action>
    Create agent-runtime/src/mastra/tools/_supabase.ts exporting createUserClient(authorization) per RESEARCH Pattern 3 (anon key + caller Authorization header, persistSession false). Create three Mastra createTool tools, each with a Zod inputSchema matching the live RPC signature, each reading auth = runtimeContext.get('authorization'), asserting it non-empty (the #4465 gate — return indistinguishable-empty if somehow absent rather than falling back to service-role), then calling sb.rpc(...) on createUserClient(auth):
    - read-signals.ts: read_signals(p_dossier_id?, p_status?, p_since?, p_limit max 100 default 50); return { signals: data ?? [] }.
    - query-graph.ts: query_graph(p_query_type enum of the 4 whitelist values, p_entity_id, p_entity_id_2?, p_window_days default 90); return data ?? the empty JSONB shape.
    - generate-digest.ts: generate_digest(p_dossier_id from context, p_period enum {daily,weekly,monthly}); PREVIEW ONLY — never call publish_digest. Return data ?? empty.
    Every return is indistinguishable-empty (no clearance/filtered/restricted field; same empty shape for no-data vs above-clearance).
  </action>
  <verify>
    <automated>grep -q "runtimeContext" agent-runtime/src/mastra/tools/_supabase.ts || grep -q "Authorization" agent-runtime/src/mastra/tools/_supabase.ts</automated>
  </verify>
  <acceptance_criteria>
    - _supabase.ts createUserClient uses SUPABASE_ANON_KEY + the caller Authorization header, persistSession false; it does NOT reference SUPABASE_SERVICE_ROLE_KEY.
    - Each of the three tools reads runtimeContext.get('authorization'), asserts non-empty, and calls the correct RPC with a Zod-validated input.
    - query-graph p_query_type is a Zod enum limited to the 4 whitelist values; generate-digest p_period is a Zod enum {daily,weekly,monthly} and never calls publish_digest.
    - Each tool return serialized does not match /clearance|filtered|restricted/i.
  </acceptance_criteria>
  <done>The keystone helper + 3 RPC-wrapping tools exist; reads run under the caller JWT; preview-only digest; indistinguishable-empty holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: hybrid-rag-search tool (iterative-scan + INVOKER RPC + TEI rerank) + dossier-lookups</name>
  <read_first>
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-RESEARCH.md (Code Examples L518-565 the hybrid_rag_search RPC + the iterative-scan SET LOCAL note L564; Architecture diagram TEI rerank; Pitfall 4 overfilter; Pitfall 5 indistinguishable-empty)
    - backend/src/ai/embeddings-service.ts (the bge-m3 embedder — to embed p_query_text into halfvec(1024) before the RPC; or call TEI_EMBED_URL)
    - backend/src/ai/agents/chat-assistant.ts (lines 245-275 getDossier, 277-317 queryCommitments->aa_commitments, 352-384 listDossiers — the dossier-lookups bodies to mirror verbatim)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-PATTERNS.md (the hybrid-rag-search.ts + dossier-lookups.ts assignments)
  </read_first>
  <action>
    Create agent-runtime/src/mastra/tools/hybrid-rag-search.ts: a tool that (1) embeds the query text to halfvec(1024) via TEI_EMBED_URL (or the local bge-m3), (2) on createUserClient(auth) sets the iterative-scan GUCs (SET LOCAL hnsw.iterative_scan='relaxed_order'; SET LOCAL hnsw.max_scan_tuples=20000) so the RLS post-filter doesn't collapse recall, (3) calls hybrid_rag_search(p_query_embedding, p_query_text, p_lang from language context, p_limit ~50), (4) reranks the RLS-passing candidates via TEI_RERANK_URL (bge-reranker-v2-m3) and keeps ~8-10 (RLS-BEFORE-rerank — the reranker only ever sees RLS-passing rows). If TEI rerank is unavailable, degrade gracefully to the RRF order (the RPC still returns ranked candidates). Return { results: [...] } with no clearance/filtered/restricted field. Create dossier-lookups.ts mirroring chat-assistant getDossier/listDossiers/queryCommitments (aa_commitments) verbatim, each via createUserClient(auth), each returning the empty shape on error (indistinguishable-empty). Export all 6 from tools/index.ts and wire them into the copilot agent (72-05 referenced them). Turn the Wave-0 tools.test.ts scaffold GREEN (move it from __scaffold__ to tools/ and implement the per-tool JWT-scoping + indistinguishable-empty assertions).
  </action>
  <verify>
    <automated>grep -q "hybrid_rag_search" agent-runtime/src/mastra/tools/hybrid-rag-search.ts && grep -qi "rerank" agent-runtime/src/mastra/tools/hybrid-rag-search.ts</automated>
  </verify>
  <acceptance_criteria>
    - hybrid-rag-search.ts sets the iterative-scan GUCs before the RPC, calls hybrid_rag_search under the caller JWT, and reranks via TEI AFTER the RPC returns (RLS-before-rerank).
    - The tool degrades to RRF order if TEI rerank is down (still returns candidates).
    - dossier-lookups mirrors the three chat-assistant reads (dossiers + aa_commitments), each via createUserClient, each indistinguishable-empty on error.
    - tools/index.ts exports all 6; tools.test.ts asserts each tool builds a per-request client from runtimeContext and each return does not match /clearance|filtered|restricted/i; pnpm --filter agent-runtime test passes.
  </acceptance_criteria>
  <done>The RAG tool + dossier/work-item lookups complete the reads-only roster; RLS-before-rerank; the Wave-0 tool tests are GREEN.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                                           |
| ------------------------ | --------------------------------------------------------------------- |
| tool execute -> Supabase | Per-request anon-key client carrying the caller JWT; RLS is the floor |
| tool -> TEI rerank       | The reranker only sees RLS-passing candidates (rerank-after-RLS)      |
| tool inputs -> RPC       | Zod-validated, bounded; no raw SQL, no execute_sql                    |

## STRIDE Threat Register

| Threat ID  | Category               | Component                           | Disposition | Mitigation Plan                                                                                                                  |
| ---------- | ---------------------- | ----------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------- |
| T-72-06-01 | Elevation of Privilege | service-role leak via a tool        | mitigate    | No tool imports SUPABASE_SERVICE_ROLE_KEY; createUserClient is anon + caller JWT only; asserted in tools.test.ts                 |
| T-72-06-02 | Elevation of Privilege | prompt-injection escalating reads   | mitigate    | RLS floor — a hijacked agent still only sees caller-clearance rows; narrow Zod tools, no execute_sql; verified by L1/L3 in 72-09 |
| T-72-06-03 | Elevation of Privilege | rerank bypassing clearance          | mitigate    | iterative-scan + hybrid_rag_search RLS returns only passing candidates; TEI rerank runs AFTER, in the tool layer                 |
| T-72-06-04 | Information Disclosure | clearance inference via tool output | mitigate    | Every tool return indistinguishable-empty; no clearance/filtered/restricted field; same empty shape no-data vs denied            |
| T-72-06-05 | Tampering              | injected SQL via query text         | mitigate    | websearch_to_tsquery in the RPC (no raw SQL); Zod-bounded inputs; enum-restricted query_type/period                              |

</threat_model>

<verification>
- pnpm --filter agent-runtime test passes (tool tests GREEN).
- _supabase.ts: anon key + caller header, no service-role.
- hybrid-rag-search: iterative-scan GUCs + RPC + TEI rerank-after-RLS.
- generate-digest never references publish_digest; query-graph enum-limited to the 4 whitelist values.
- Every tool return asserted not to match /clearance|filtered|restricted/i.
</verification>

<success_criteria>

- 6 narrow Zod-typed reads-only tools + the keystone helper, all under the caller JWT.
- hybrid RAG: dense+sparse RRF (RPC) + TEI rerank, RLS-before-rerank (AGENT-04).
- preview-only digest; no service-role anywhere; indistinguishable-empty everywhere.
- AGENT-02 (gated reads under JWT) + AGENT-03 (reduced results) proven by the tool tests.
  </success_criteria>

<output>
Create `.planning/phases/72-agent-platform-runtime-retrieval-reads/72-06-SUMMARY.md` when done. Record the rerank top-k chosen, the iterative-scan tuning, and confirm publish_digest is never reachable.
</output>
