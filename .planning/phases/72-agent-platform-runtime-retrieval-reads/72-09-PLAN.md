---
phase: 72-agent-platform-runtime-retrieval-reads
plan: 09
type: execute
wave: 7
depends_on: ['72-02', '72-04', '72-05', '72-06', '72-07', '72-08']
files_modified:
  - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-UAT.md
  - supabase/seeds/72-copilot-uat-seed.sql
autonomous: false
requirements:
  [AGENT-01, AGENT-02, AGENT-03, AGENT-04, AGENT-05, AGENT-06, INFRA-01, INFRA-02, INFRA-03]
user_setup: []

must_haves:
  truths:
    - 'The 5 milestone phase-gate proofs pass live on staging, EN+AR (seed -> observe -> restore)'
    - 'A low-clearance (L1) account provably gets strictly reduced copilot results vs L3, zero above-clearance, indistinguishable-empty'
    - 'vLLM /v1, TEI embed+rerank, and agent-runtime on 4100 are reachable and process a chat turn end-to-end'
    - 'array_length(embedding,1)=1024 for all rag_chunks rows; hybrid_rag_search is SECURITY INVOKER'
    - 'Closes AGENT-01..06 + INFRA-01..03 with live evidence'
  artifacts:
    - path: '.planning/phases/72-agent-platform-runtime-retrieval-reads/72-UAT.md'
      provides: 'the 5 live proofs + per-requirement pass/fail, EN+AR, with evidence'
      contains: 'Clearance-reduction proof'
    - path: 'supabase/seeds/72-copilot-uat-seed.sql'
      provides: 'seed signals/events across >=2 clearance levels for the AGENT-03/05 proofs (restored after)'
  key_links:
    - from: 'L1 vs L3 authenticated impersonation'
      to: 'the copilot (RAG + tools)'
      via: 'identical query from two clearance accounts; L1 result subset of L3'
      pattern: 'impersonation'
---

<objective>
Run the milestone verification bar live on staging: the 5 phase-gate proofs (clearance-reduction, EN+AR RTL render, array_length=1024, SECURITY INVOKER+RLS, end-to-end smoke), EN+AR, seed -> observe -> restore. Signals/events/briefs/documents are 0 rows on staging, so the AGENT-03 (reduced results) and AGENT-05 (dimension) proofs need a seed of signals/events across >=2 clearance levels first (then re-run the re-embed, then restore). RLS denial returns empty 200s, so force errors via CDP Network.setBlockedURLs and assert role="alert"/reduced-counts, never HTTP status.

Purpose: this is the phase gate — the evidence that the copilot reads gated data under the JWT keystone and cannot leak above-clearance content, in both languages.
Output: 72-UAT.md with all 5 proofs + per-requirement pass/fail; the seed file (and confirmation it was restored).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-VALIDATION.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-RESEARCH.md
@.planning/phases/72-agent-platform-runtime-retrieval-reads/72-UI-SPEC.md

<interfaces>
Clearance dist on staging: L1=344, L2=23, L3=6. Use authenticated impersonation (service-role MCP BYPASSES RLS — P69 landmine);
the L1/L3 proof must run under real authenticated sessions.
Forced-error: CDP Network.setBlockedURLs the RAG/tool call; wait through TanStack 3x retry; assert role="alert"/neutral copy via DOM (screenshot can wedge).
Language: switch AR via the ع button -> localStorage['id.locale']='ar'. Test at 1024px AND 1400px (analyst widths).
The 5 proofs (VALIDATION.md Manual-Only + Phase-gate live proofs):
1. Clearance-reduction (AGENT-03): L1 strictly reduced vs L3, zero above-clearance, no forbidden substring anywhere (copy/aria-live/JSON).
2. EN+AR RTL render (AGENT-06): dir=rtl + Tajawal, message/composer/citation flipped, 1024 & 1400.
3. array_length=1024 (AGENT-05): scripts/verify-rag-chunks-dims.sql -> 0 failing rows (via MCP).
4. SECURITY INVOKER+RLS (AGENT-04): get_function_security('hybrid_rag_search') prosecdef=false; RLS uses user_id=auth.uid().
5. End-to-end smoke (INFRA-03): agent-runtime on 4100 processes a chat turn (drawer + Cmd+K) under the caller JWT.
INFRA-01/02 smokes: curl vllm /v1/models -> gemma-4-12b; curl TEI /embed -> 1024-dim; curl TEI /rerank -> scores.
Test creds: $TEST_USER_EMAIL/$TEST_USER_PASSWORD (.env.test); admin kazahrani@stats.gov.sa. Local app :5173 -> staging zkrcjzdemdmwhearhfgg.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed signals/events across >=2 clearance levels for the AGENT-03/05 proofs</name>
  <read_first>
    - supabase/migrations/20260614_phase69_signals_extend.sql (the intelligence_event/signal columns + sensitivity_level INTEGER + the read_signals RPC — what to seed)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-RESEARCH.md (the probe RESULTS — signals/events/briefs/documents 0 rows; the seed-dependency note; clearance-source asymmetry)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-VALIDATION.md (the Seed dependency note; the Manual-Only proofs)
    - backend/src/jobs/reembed-rag-chunks.ts (the idempotent re-embed to re-run after seeding)
  </read_first>
  <action>
    Author supabase/seeds/72-copilot-uat-seed.sql: insert a small set of intelligence_event/signal rows (and link them to existing dossiers) spanning at least sensitivity_level 1 and sensitivity_level 3 (ideally include an L4-reserved row), plus enough dossier/position text already present (12/2/9) to make the RAG and read_signals tools return non-trivial results. Tie each seeded signal to a dossier so the per-source sensitivity resolves correctly. Apply the seed via Supabase MCP, then re-run the idempotent reembed-rag-chunks job so the seeded rows land in rag_chunks at 1024-dim. Record the exact rows seeded (ids) so they can be restored (deleted) after the proofs. This is the seed half of seed -> observe -> restore.
  </action>
  <verify>
    <automated>grep -qi "intelligence_event" supabase/seeds/72-copilot-uat-seed.sql && grep -qi "sensitivity_level" supabase/seeds/72-copilot-uat-seed.sql</automated>
  </verify>
  <acceptance_criteria>
    - The seed inserts signals/events across at least 2 clearance levels (1 and 3), each linked to a dossier.
    - After applying the seed + re-running the re-embed, scripts/verify-rag-chunks-dims.sql returns 0 failing rows (via MCP).
    - The seeded row ids are recorded for restore.
  </acceptance_criteria>
  <done>Staging has multi-clearance signals/events + RAG rows so the AGENT-03/05 proofs are exercisable; seed ids recorded for restore.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the 5 live phase-gate proofs (EN+AR) + INFRA smokes, then restore</name>
  <read_first>
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-VALIDATION.md (the full Per-Requirement Verification Map + the Manual-Only proofs + Phase-gate live proofs)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-RESEARCH.md (Phase-gate live proofs; the CDP forced-error reality — empty 200s)
    - scripts/verify-rag-chunks-dims.sql (the AGENT-04/05 DB proofs)
    - .planning/phases/72-agent-platform-runtime-retrieval-reads/72-UI-SPEC.md (the indistinguishable-empty copy + RTL reading order to verify visually)
  </read_first>
  <action>
    Execute the 5 phase-gate proofs live on staging and record each (with evidence) in 72-UAT.md:
    1. INFRA smokes: curl vllm /v1/models returns gemma-4-12b; curl TEI_EMBED /embed returns a 1024-dim vector; curl TEI_RERANK /rerank returns scores; docker compose ps shows agent-runtime healthy on 4100.
    2. AGENT-04/05 DB proofs (via MCP execute_sql): get_function_security('hybrid_rag_search') prosecdef=false; scripts/verify-rag-chunks-dims.sql -> 0 failing rows; rag_chunks RLS uses user_id=auth.uid().
    3. Clearance-reduction (AGENT-03): with two REAL authenticated sessions (L1 and L3 — NOT service-role), ask the copilot the same question that hits the seeded above-clearance signals; assert the L1 result set is a strict subset of L3, zero above-clearance content, and the FULL serialized payload (visible copy + aria-live + any JSON) does not match /clearance|filtered|restricted/i. Also run the CDP Network.setBlockedURLs forced-error on the RAG/tool call and assert role="alert"/neutral no-answer copy via DOM.
    4. EN+AR RTL render (AGENT-06): in both languages at 1024px and 1400px, confirm dir=rtl + Tajawal + message/composer/citation flipped + token-bound (no raw hex, no card shadow).
    5. End-to-end smoke (INFRA-03/AGENT-01/02): from the drawer AND Cmd+K, a cleared user sends a question and gets a streamed token-bound reply from gated data under their JWT.
    Then RESTORE: delete the seeded rows (and any rag_chunks rows they produced) so staging returns to its pre-UAT state. Write 72-UAT.md with per-requirement pass/fail + evidence. PAUSE for human confirmation of the visual/RTL/clearance proofs.
  </action>
  <verify>
    <human-check>
Confirm on staging, EN+AR:
1. INFRA: vllm /v1/models = gemma-4-12b; TEI embed = 1024-dim; TEI rerank = scores; agent-runtime healthy on 4100.
2. DB: hybrid_rag_search prosecdef=false; 0 rag_chunks rows fail the 1024 check; RLS = user_id=auth.uid().
3. Clearance: L1 result strictly reduced vs L3; zero above-clearance; NO clearance/filtered/restricted token anywhere (copy/aria-live/JSON); forced-error shows the neutral role="alert" copy.
4. RTL: AR drawer dir=rtl + Tajawal + flipped at 1024 & 1400; no raw hex / no card shadow.
5. E2E: drawer + Cmd+K each return a streamed token-bound reply under the caller JWT.
6. Seed rows restored (staging clean).
    </human-check>
  </verify>
  <acceptance_criteria>
    - 72-UAT.md records all 5 proofs PASS with evidence, EN+AR, plus the INFRA-01/02 smokes.
    - The clearance-reduction proof used authenticated L1/L3 sessions (not service-role) and shows L1 subset of L3 + no forbidden substring anywhere.
    - The DB proofs (prosecdef=false; 0 failing dimension rows; user_id RLS) pass via MCP.
    - The seeded rows are restored (staging returned to pre-UAT state); human confirms the visual/RTL/clearance proofs.
  </acceptance_criteria>
  <done>All 5 milestone proofs pass live EN+AR; staging restored; AGENT-01..06 + INFRA-01..03 closed with evidence.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                      |
| -------------------------- | ---------------------------------------------------------------- |
| L1 session -> copilot      | The clearance-reduction proof boundary; RLS must strictly reduce |
| seed -> staging -> restore | Temporary test data; must be removed after the proofs            |
| forced-error -> UI         | CDP-blocked call; the UI must show neutral copy, not a leak      |

## STRIDE Threat Register

| Threat ID  | Category               | Component                              | Disposition | Mitigation Plan                                                                                                                                              |
| ---------- | ---------------------- | -------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-72-09-01 | Information Disclosure | above-clearance leak to L1             | mitigate    | The proof IS the mitigation check — L1 strictly reduced vs L3, zero above-clearance, indistinguishable-empty; authenticated impersonation (not service-role) |
| T-72-09-02 | Information Disclosure | clearance inference under forced-error | mitigate    | CDP-blocked RAG call must render the neutral role="alert" copy; assert no forbidden substring in the empty/error state                                       |
| T-72-09-03 | Tampering              | leftover UAT seed data                 | mitigate    | Record seeded ids; restore (delete) after the proofs; confirm staging clean                                                                                  |
| T-72-SC    | Tampering              | n/a (no installs)                      | accept      | Verification-only plan; no package changes                                                                                                                   |

</threat_model>

<verification>
- 72-UAT.md: 5 proofs PASS + INFRA-01/02 smokes + per-requirement pass/fail, EN+AR.
- Clearance proof under authenticated L1/L3; L1 subset of L3; no forbidden substring anywhere.
- DB proofs via MCP: prosecdef=false; 0 failing dimension rows; user_id RLS.
- Seed restored; staging clean.
</verification>

<success_criteria>

- The 5 milestone phase-gate proofs pass live EN+AR (the verification bar).
- AGENT-01..06 + INFRA-01..03 closed with live evidence.
- The copilot provably cannot leak above-clearance content (the keystone, proven).
- Staging returned to its pre-UAT state.
  </success_criteria>

<output>
Create `.planning/phases/72-agent-platform-runtime-retrieval-reads/72-09-SUMMARY.md` when done. Link 72-UAT.md, record the L1/L3 node/result counts compared, and confirm the seed restore.
</output>
