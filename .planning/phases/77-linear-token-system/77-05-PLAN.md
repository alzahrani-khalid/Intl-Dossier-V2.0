---
phase: 77-linear-token-system
plan: 05
type: execute
wave: 4
depends_on: [77-04]
files_modified:
  - frontend/src/components/tweaks/TweaksDrawer.tsx
  - frontend/src/components/layout/Topbar.tsx
  - frontend/src/components/settings/sections/AppearanceSettingsSection.tsx
  - frontend/src/i18n/en/common.json
  - frontend/src/i18n/ar/common.json
  - frontend/src/i18n/en/settings.json
  - frontend/src/i18n/ar/settings.json
  - frontend/src/components/layout/Topbar.test.tsx (or colocated test path)
  - frontend/src/components/tweaks/TweaksDrawer.test.tsx (or colocated test path)
  - frontend/tests (label-parity test)
autonomous: true
requirements: [TOKEN-04]
must_haves:
  truths:
    - 'No user-facing surface offers a design-direction choice: the 4-direction switcher is gone from TweaksDrawer, Topbar, and AppearanceSettingsSection'
    - 'No user-facing surface offers an accent-hue control (hue slider/presets retired with the switcher — accent is verbatim-literal since 77-04)'
    - 'Theme (light/dark) and density controls REMAIN fully functional on all three surfaces'
    - 'No orphaned direction/hue i18n keys remain in EN or AR bundles; label-parity stays green'
  artifacts:
    - path: 'frontend/src/components/layout/Topbar.tsx'
      provides: 'Topbar without the DIRECTIONS segmented radio'
    - path: 'frontend/src/components/tweaks/TweaksDrawer.tsx'
      provides: 'Tweaks drawer without direction section and hue presets'
    - path: 'frontend/src/components/settings/sections/AppearanceSettingsSection.tsx'
      provides: 'Appearance settings without direction radio group and hue slider'
  key_links:
    - from: 'i18n common.json/settings.json (en+ar)'
      to: 'removed UI controls'
      via: 'direction/hue key removal in the same commit as the components consuming them'
      pattern: 'direction'
---

<objective>
Retire the user-facing direction switcher and the hue control (TOKEN-04 UI half). Since 77-04, every direction input coerces to linear and the accent is a verbatim literal — these controls are already inert; this plan removes them and their i18n strings. The Direction TYPE stays 4+1-wide until 77-07 (so this plan compiles independently and can run in parallel with 77-06).

Purpose: Linear is the only selectable visual direction (REQUIREMENTS TOKEN-04); dead controls that no-op are worse than no controls.
Output: three cleaned surfaces + pruned EN/AR bundles + reworked component tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-linear-token-system/77-RESEARCH.md  (§Blast radius rows for TweaksDrawer/Topbar/AppearanceSettingsSection; §i18n paragraph)
@frontend/src/components/tweaks/TweaksDrawer.tsx
@frontend/src/components/layout/Topbar.tsx
@frontend/src/components/settings/sections/AppearanceSettingsSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the direction switcher + hue controls from the three surfaces and rework their tests</name>
  <files>frontend/src/components/tweaks/TweaksDrawer.tsx, frontend/src/components/layout/Topbar.tsx, frontend/src/components/settings/sections/AppearanceSettingsSection.tsx, plus their colocated/associated test files (locate via Glob: **/Topbar.test.tsx, **/TweaksDrawer.test.tsx, and any AppearanceSettingsSection test)</files>
  <read_first>frontend/src/components/tweaks/TweaksDrawer.tsx (DIRECTIONS array + direction buttons + hue presets), frontend/src/components/layout/Topbar.tsx (DIRECTIONS array line ~48, segmented radio lines ~141-167), frontend/src/components/settings/sections/AppearanceSettingsSection.tsx (direction radio group + hue slider), each surface's test file, 77-RESEARCH.md §Blast radius</read_first>
  <action>Remove, per TOKEN-04 and the planner's Q1 adoption (hue retired with the switcher): in Topbar.tsx — the DIRECTIONS constant and the direction segmented-radio block, plus now-unused imports (useDesignDirection or equivalent) and any aria-labels tied to it; in TweaksDrawer.tsx — the direction section (DIRECTIONS array + buttons) AND the hue preset/slider controls, keeping the theme (light/dark) and density controls intact and functional; in AppearanceSettingsSection.tsx — the direction radio group AND the hue slider, keeping theme + density controls. Do not remove the useHue hook or DesignProvider hue state (77-07 owns plumbing); simply stop rendering controls that call them. Follow the surgical-changes rule: remove imports/variables that YOUR removals orphan, touch nothing else. Rework each surface's tests: delete direction-switcher/hue interaction cases, keep+verify theme and density interaction cases still pass; where a test asserted the switcher's presence, replace with an assertion that no element with the retired control's role/testid renders.</action>
  <verify>
    <automated>pnpm -C frontend exec vitest run --dir src/components 2>/dev/null || pnpm -C frontend exec vitest run src/components/layout src/components/tweaks src/components/settings</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "DIRECTIONS" frontend/src/components/layout/Topbar.tsx frontend/src/components/tweaks/TweaksDrawer.tsx frontend/src/components/settings/sections/AppearanceSettingsSection.tsx` → 0 matches.
    - `grep -rn "hue" frontend/src/components/tweaks/TweaksDrawer.tsx frontend/src/components/settings/sections/AppearanceSettingsSection.tsx` → 0 matches (case-insensitive check too).
    - Theme + density controls still render and their tests pass.
    - `pnpm -C frontend type-check` green.
  </acceptance_criteria>
  <done>Three surfaces expose theme/density only; tests reworked; no orphaned imports.</done>
</task>

<task type="auto">
  <name>Task 2: Prune direction/hue i18n keys (EN+AR) and keep label-parity green</name>
  <files>frontend/src/i18n/en/common.json, frontend/src/i18n/ar/common.json, frontend/src/i18n/en/settings.json, frontend/src/i18n/ar/settings.json, the label-parity test (locate via Grep: label-parity)</files>
  <read_first>frontend/src/i18n/en/common.json around lines 1205 and 1275 (shell.direction.* / tweaks direction+hue entries — VERIFY which "direction" keys are DESIGN-direction vs unrelated reading-direction keys, e.g. the "Reading direction" label at ~1244 must STAY), frontend/src/i18n/en/settings.json around line 113 (appearance.direction.*), the AR counterparts, the label-parity test file, 77-RESEARCH.md §Pitfall 3 (grep-replace corrupts unrelated domains)</read_first>
  <action>Remove ONLY the i18n keys consumed by the controls deleted in Task 1: the design-direction label/name keys (shell/tweaks direction entries naming Bureau/Chancery/Situation/Ministerial) and the hue control keys, from BOTH en and ar bundles symmetrically (common.json + settings.json). Do NOT touch unrelated keys that merely contain the word "direction" (reading direction, sort direction, calendar-sync/citations text-direction — Pitfall 3): identify design-direction keys by cross-referencing the exact t() keys removed from the three components in Task 1. Update the label-parity test if it enumerates the removed key groups explicitly.</action>
  <verify>
    <automated>pnpm -C frontend exec vitest run tests/unit/i18n 2>/dev/null || pnpm -C frontend exec vitest run --testNamePattern="parity"; node scripts/check-i18n-namespaces.mjs</automated>
  </verify>
  <acceptance_criteria>
    - label-parity test green (EN/AR key sets still mirror each other).
    - `grep -in "bureau\|chancery\|situation\|ministerial" frontend/src/i18n/en/common.json frontend/src/i18n/ar/common.json frontend/src/i18n/en/settings.json frontend/src/i18n/ar/settings.json` → 0 matches (retired direction NAMES gone; engagement-domain 'ministerial' strings live in other namespace files and are untouched).
    - "Reading direction" (and other non-design direction keys) still present.
    - Full `pnpm run lint` (from frontend/) green — includes check-i18n-namespaces.
  </acceptance_criteria>
  <done>EN/AR bundles carry no retired-direction or hue strings; parity intact.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                   |
| ---------- | ----------------------------------------------------------------------------- |
| (none new) | UI-surface and i18n string removal only; no input handling or storage changes |

## STRIDE Threat Register

| Threat ID  | Category           | Component                                                                        | Disposition | Mitigation Plan                                                                                       |
| ---------- | ------------------ | -------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------- |
| T-77-05-01 | Spoofing/Tampering | none — removed controls wrote whitelisted localStorage keys via existing setters | accept      | Removal shrinks the input surface; coercion from 77-04 already neutralizes stale writes from old tabs |
| T-77-05-SC | Tampering          | package installs                                                                 | accept      | No package-manager installs in this plan; slopcheck N/A                                               |

</threat_model>

<verification>
- Component + i18n vitest suites green; type-check green; full lint green.
- Manual smoke: TweaksDrawer and Settings→Appearance show theme + density only; topbar has no direction segmented control.
</verification>

<success_criteria>

- TOKEN-04 UI half complete: Linear is the only visual direction anywhere a user can look or click; theme/density customization preserved.
  </success_criteria>

<output>
Create `.planning/phases/77-linear-token-system/77-05-SUMMARY.md` when done.
</output>
