---
phase: 77-linear-token-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tests/e2e/activity-page-visual.spec.ts
  - frontend/tests/e2e/after-actions-page-visual.spec.ts
  - frontend/tests/e2e/briefs-page-visual.spec.ts
  - frontend/tests/e2e/calendar-visual.spec.ts
  - frontend/tests/e2e/dashboard-visual.spec.ts
  - frontend/tests/e2e/dashboard-widgets-visual.spec.ts
  - frontend/tests/e2e/dossier-drawer-visual.spec.ts
  - frontend/tests/e2e/kanban-visual.spec.ts
  - frontend/tests/e2e/list-pages-visual.spec.ts
  - frontend/tests/e2e/settings-page-visual.spec.ts
  - frontend/tests/e2e/tasks-page-visual.spec.ts
  - frontend/tests/e2e/tasks-tab-visual.spec.ts
  - frontend/tests/e2e/*-snapshots/ (regenerated PNGs)
  - .planning/phases/77-linear-token-system/77-BASELINE-VALIDATION.md
autonomous: false
requirements: [VERIFY-01, TOKEN-01]
must_haves:
  truths:
    - 'A committed pre-swap Playwright visual baseline (all 12 live *-visual.spec.ts specs, PNGs recaptured on the darwin capture machine) exists in git history BEFORE any Linear literal lands in directions.ts'
    - 'Every one of the 12 visual specs pins id.theme and locale explicitly (localStorage seed / ?lng= querystring), so the Phase-77 default-theme flip (light→dark) cannot silently change what Phase 80 re-compares'
    - 'Replaying the 12 specs locally WITHOUT --update-snapshots passes (baseline is reproducible, not laundered)'
    - 'A human has reviewed the captured PNGs and the coverage note before the baseline commit'
  artifacts:
    - path: '.planning/phases/77-linear-token-system/77-BASELINE-VALIDATION.md'
      provides: 'Capture log: spec list, PNG counts, pin-state audit results, explicit coverage statement (no silent expansion)'
    - path: 'frontend/tests/e2e/list-pages-visual.spec.ts'
      provides: 'Pin-state-audited spec (representative of all 12)'
  key_links:
    - from: 'frontend/playwright.config.ts'
      to: 'pnpm dev'
      via: 'webServer auto-start when E2E_BASE_URL is unset (config line ~137)'
      pattern: 'webServer'
    - from: 'visual specs'
      to: 'localStorage id.theme / id.locale'
      via: 'addInitScript / ?lng= querystring pinning'
      pattern: "id\\.theme"
---

<objective>
Capture the VERIFY-01 pre-swap visual baseline that GATES the entire token swap. This is the mandated gating step from ROADMAP Phase 77: before any literal in `frontend/src/design-system/tokens/directions.ts` changes, the current (Bureau) rendering of all currently-baselined surfaces is captured, human-reviewed, and committed. Re-comparison is Phase 80's job — do NOT design or run any re-compare here.

Purpose: no baseline laundering — Phase 80 must be able to diff Linear against a trustworthy pre-Linear state.
Output: recaptured `-chromium-darwin` PNGs for the 12 live visual specs, pin-state fixes in the specs, and a human-signed capture log.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-linear-token-system/77-RESEARCH.md  (§Pattern 5: Visual baseline gate, §Pitfall 6)
@.planning/phases/77-linear-token-system/77-CONTEXT.md
@.planning/phases/46-*/46-01-SUMMARY.md
@.planning/phases/46-*/46-04-SUMMARY.md
</context>

<important_facts>

- The `Visual Regression (Phase 46)` CI job runs against a deployed URL with stale secrets and is red on main (issue #31 class). Do NOT gate on CI going green. Capture/replay is local (this Mac; snapshots are `-chromium-darwin`).
- Current live baseline: 12 specs in `frontend/tests/e2e/*-visual.spec.ts`, 51 committed PNGs. `frontend/tests/visual/theme-visual.spec.ts` references dead `gastat`/`blue-sky` themes — stale legacy, NOT part of the live baseline; leave it untouched and note it in the log.
- The 8 direction×mode `focused-primitive` PNGs belong to `frontend/tests/e2e/qa-sweep-focus-outline.spec.ts`, NOT to the 12 visual specs. Do NOT rewrite that spec here — it is rewritten linear-only in plan 77-07. Its existing committed PNGs stay as-is and count as pre-swap state.
- After this phase the app default flips light→dark. Any spec that relies on the app DEFAULT theme would diff apples-to-oranges in Phase 80 (Pitfall 6) — hence the pin-state audit.
  </important_facts>

<tasks>

<task type="auto">
  <name>Task 1: Pin-state audit — every visual spec pins theme and locale explicitly</name>
  <files>frontend/tests/e2e/activity-page-visual.spec.ts, frontend/tests/e2e/after-actions-page-visual.spec.ts, frontend/tests/e2e/briefs-page-visual.spec.ts, frontend/tests/e2e/calendar-visual.spec.ts, frontend/tests/e2e/dashboard-visual.spec.ts, frontend/tests/e2e/dashboard-widgets-visual.spec.ts, frontend/tests/e2e/dossier-drawer-visual.spec.ts, frontend/tests/e2e/kanban-visual.spec.ts, frontend/tests/e2e/list-pages-visual.spec.ts, frontend/tests/e2e/settings-page-visual.spec.ts, frontend/tests/e2e/tasks-page-visual.spec.ts, frontend/tests/e2e/tasks-tab-visual.spec.ts</files>
  <read_first>frontend/playwright.config.ts (webServer + snapshot config), each of the 12 spec files, .planning/phases/77-linear-token-system/77-RESEARCH.md §Pattern 5 step 6 and §Pitfall 6</read_first>
  <action>Audit each of the 12 visual specs for explicit state pinning. A spec is "pinned" when (a) its theme is set deterministically — via `page.addInitScript` seeding `localStorage.setItem('id.theme', 'light')` (or 'dark' where the spec intends dark) before navigation, or an equivalent existing mechanism — and (b) its locale is set deterministically — via the `?lng=en` / `?lng=ar` querystring (the i18n detector reads querystring first) or `localStorage.setItem('id.locale', …)`. For every spec (or spec variant) currently relying on the app default for either dimension, add the explicit pin matching what the spec renders TODAY (default today = light + en), so the captured PNGs stay byte-representative of current state. Do not change viewport sizes, routes, or shot lists — this task changes pinning only. Do not pin `id.dir` (post-swap coercion makes it meaningless). Record per-spec audit results (already-pinned vs fixed) for the Task 2 log.</action>
  <verify>
    <automated>grep -l "id.theme\|lng=" frontend/tests/e2e/activity-page-visual.spec.ts frontend/tests/e2e/after-actions-page-visual.spec.ts frontend/tests/e2e/briefs-page-visual.spec.ts frontend/tests/e2e/calendar-visual.spec.ts frontend/tests/e2e/dashboard-visual.spec.ts frontend/tests/e2e/dashboard-widgets-visual.spec.ts frontend/tests/e2e/dossier-drawer-visual.spec.ts frontend/tests/e2e/kanban-visual.spec.ts frontend/tests/e2e/list-pages-visual.spec.ts frontend/tests/e2e/settings-page-visual.spec.ts frontend/tests/e2e/tasks-page-visual.spec.ts frontend/tests/e2e/tasks-tab-visual.spec.ts | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - All 12 spec files match the grep (each contains an explicit `id.theme` seed or `lng=` pin); the grep above prints 12.
    - `pnpm -C frontend type-check` passes (specs still compile).
    - No spec's route list, viewport matrix, or screenshot names changed — `git diff --stat` shows only pinning-related additions.
  </acceptance_criteria>
  <done>All 12 visual specs deterministically pin theme + locale; diff contains pinning changes only.</done>
</task>

<task type="auto">
  <name>Task 2: Seed refresh + local capture + replay proof + capture log</name>
  <files>frontend/tests/e2e/*-snapshots/ (regenerated PNGs), .planning/phases/77-linear-token-system/77-BASELINE-VALIDATION.md</files>
  <read_first>.planning/phases/46-*/46-01-SUMMARY.md and 46-04-SUMMARY.md (seed + readiness precedent), .planning/phases/77-linear-token-system/77-RESEARCH.md §Pattern 5</read_first>
  <action>Follow the Phase 46 capture protocol. (1) Refresh the deterministic staging seed rows via the Supabase MCP (project zkrcjzdemdmwhearhfgg): update the `b0000002-*` engagement_dossiers rows' dates to today-relative values so dashboard widgets render non-empty (mirror the exact UPDATE shape from the 46-01 SUMMARY; idempotent, touch only the b0000002-* fixture rows). (2) Capture locally: ensure E2E_BASE_URL is unset so playwright.config.ts auto-starts the dev server, then run the 12 visual specs with --update-snapshots on this Mac. (3) Replay the same 12 specs WITHOUT --update-snapshots — must pass, proving the baseline is reproducible. (4) Write .planning/phases/77-linear-token-system/77-BASELINE-VALIDATION.md: the per-spec pin-state audit table from Task 1, the list of regenerated PNGs with counts, the replay result, and an EXPLICIT coverage statement: baseline = the existing 12-spec matrix as-is (EN+AR where specs already do both; light-dominant); dark-mode coverage exists only via the qa-sweep focused-primitive shots (left untouched, rewritten in 77-07); NO coverage expansion was performed (per VERIFY-01: expansion is called out, not assumed); theme-visual.spec.ts is stale legacy and excluded. Do NOT commit yet — commit happens after the Task 3 human checkpoint.</action>
  <verify>
    <automated>pnpm -C frontend exec playwright test tests/e2e/activity-page-visual.spec.ts tests/e2e/after-actions-page-visual.spec.ts tests/e2e/briefs-page-visual.spec.ts tests/e2e/calendar-visual.spec.ts tests/e2e/dashboard-visual.spec.ts tests/e2e/dashboard-widgets-visual.spec.ts tests/e2e/dossier-drawer-visual.spec.ts tests/e2e/kanban-visual.spec.ts tests/e2e/list-pages-visual.spec.ts tests/e2e/settings-page-visual.spec.ts tests/e2e/tasks-page-visual.spec.ts tests/e2e/tasks-tab-visual.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The replay run (no --update-snapshots) exits 0 on this machine.
    - 77-BASELINE-VALIDATION.md exists and contains: pin-state audit table, PNG inventory, replay proof line, and the explicit no-expansion coverage statement.
    - Regenerated PNGs are all `-chromium-darwin` named, staged but consistent with spec output.
  </acceptance_criteria>
  <done>Baseline captured and reproducible locally; log written; awaiting human review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human review of the baseline PNGs, then commit the gate</name>
  <files>frontend/tests/e2e/ (baseline commit), .planning/phases/77-linear-token-system/77-BASELINE-VALIDATION.md</files>
  <action>Present the capture log and PNG inventory for human review; on approval, commit the pinned specs + regenerated PNGs + 77-BASELINE-VALIDATION.md as a single baseline commit. Do not proceed to any plan that touches directions.ts until this commit exists.</action>
  <what-built>Recaptured pre-swap visual baseline (12 specs, pinned state) + capture log. This commit is the VERIFY-01 gate — no directions.ts literal change may land before it.</what-built>
  <how-to-verify>
    1. Open .planning/phases/77-linear-token-system/77-BASELINE-VALIDATION.md and confirm the coverage statement matches expectations (no silent expansion; theme-visual excluded; qa-sweep PNGs untouched).
    2. Spot-review regenerated PNGs under frontend/tests/e2e/*-snapshots/ (dashboard, list pages, one AR shot) — widgets non-empty, no obviously broken render.
    3. Reply "approved" to let the executor commit the baseline (specs + PNGs + log) as a single commit.
  </how-to-verify>
  <resume-signal>Type "approved" to commit the baseline, or describe issues to re-capture.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                              |
| ------------------------ | -------------------------------------------------------- |
| dev machine → staging DB | Supabase MCP seed refresh writes to staging fixture rows |
| repo → CI                | Committed PNGs become the Phase-80 comparison oracle     |

## STRIDE Threat Register

| Threat ID  | Category    | Component                       | Disposition | Mitigation Plan                                                                                                                                  |
| ---------- | ----------- | ------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-77-01-01 | Tampering   | staging seed refresh            | mitigate    | Touch only the deterministic `b0000002-*` fixture rows with idempotent date UPDATEs mirroring the 46-01 precedent; no schema changes, no deletes |
| T-77-01-02 | Repudiation | baseline integrity (laundering) | mitigate    | Baseline commit precedes any directions.ts change (wave ordering) + human checkpoint reviews PNGs before commit                                  |
| T-77-01-SC | Tampering   | package installs                | accept      | No package-manager installs in this plan; slopcheck N/A                                                                                          |

</threat_model>

<verification>
- Replay of all 12 visual specs passes locally without --update-snapshots.
- `git log` shows the baseline commit BEFORE any commit touching frontend/src/design-system/tokens/directions.ts in this phase.
</verification>

<success_criteria>

- VERIFY-01 pre-swap gate satisfied: pinned specs + recaptured PNGs + human-reviewed log committed.
- Downstream plans (77-03+) are unblocked.
  </success_criteria>

<output>
Create `.planning/phases/77-linear-token-system/77-01-SUMMARY.md` when done.
</output>
