---
phase: 77-linear-token-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-bootstrap-parity.mjs
  - tools/bootstrap-fixtures/bad-bootstrap.js
  - frontend/package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [FOUC-01, TOKEN-02]
must_haves:
  truths:
    - 'Running node scripts/check-bootstrap-parity.mjs exits 0 on the current repo (guard proven against live Bureau values BEFORE any Linear literal moves)'
    - 'Running the guard against tools/bootstrap-fixtures/bad-bootstrap.js exits non-zero and names the diverging variable (byte-match, not type-check)'
    - 'pnpm lint fails when bootstrap.js palette/font/density literals diverge from directions.ts/densities.ts'
    - 'The ci.yml Lint job runs the guard plus a positive-failure fixture step (check-duplicate-rtl precedent shape)'
  artifacts:
    - path: 'scripts/check-bootstrap-parity.mjs'
      provides: 'Dependency-free byte-match guard (node:fs/vm/path/url only, native TS data import)'
      min_lines: 60
    - path: 'tools/bootstrap-fixtures/bad-bootstrap.js'
      provides: 'Positive-failure fixture with exactly one diverged palette literal'
  key_links:
    - from: 'scripts/check-bootstrap-parity.mjs'
      to: 'frontend/src/design-system/tokens/directions.ts'
      via: 'native import() of the data module (Node >=22.18 type stripping)'
      pattern: "directions\\.ts"
    - from: 'frontend/package.json lint script'
      to: 'scripts/check-bootstrap-parity.mjs'
      via: 'appended node invocation in the lint chain'
      pattern: 'check-bootstrap-parity'
    - from: '.github/workflows/ci.yml'
      to: 'scripts/check-bootstrap-parity.mjs'
      via: 'Lint job step + negated positive-failure step'
      pattern: 'check-bootstrap-parity'
---

<objective>
Land the FOUC-01 byte-match CI guard BEFORE any token literal moves (ROADMAP hard-sequencing: "the bootstrap.js/directions.ts byte-match CI guard is added at the START of Phase 77"). The guard executes the real `frontend/public/bootstrap.js` in a `node:vm` sandbox for every direction × mode × density combination exported by the token data modules and asserts the painted CSS custom-property values are string-identical to the exported literals. Proving it green against today's Bureau values (and red against a deliberately-diverged fixture) validates the mechanism before the swap depends on it.

Purpose: replace the doc-enforced byte-match discipline (which has already failed — live drift exists in index.css :root) with a build-breaking gate.
Output: guard script + fixture + `pnpm lint` chain entry + ci.yml Lint-job wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-linear-token-system/77-RESEARCH.md  (§Pattern 2: FOUC byte-match CI guard — the prescriptive design; §Standard Stack Node capability check)
@scripts/check-duplicate-rtl.mjs  (the precedent to mirror)
@frontend/public/bootstrap.js
@frontend/src/design-system/tokens/directions.ts
@frontend/src/design-system/tokens/densities.ts
@frontend/tests/bootstrap/migrator.test.ts  (the DOM/localStorage stub surface to replicate without jsdom)
</context>

<interfaces>
<!-- Probe-verified facts from 77-RESEARCH.md the executor must not re-derive: -->
- Native TS import WORKS for the two data modules on Node 22.22/22.23 (their only imports are `import type`, which erase): `await import(pathToFileURL(resolve(repoRoot,'frontend/src/design-system/tokens/directions.ts')).href)` → `{ PALETTES, FONTS }`; same for `densities.ts` → `{ DENSITIES }`.
- Do NOT import `buildTokens.ts` — its extensionless `'./densities'` specifier fails native resolution (probe-verified).
- bootstrap.js is ES5, wrapped in try/catch, reads localStorage keys `id.dir/id.theme/id.hue/id.density/id.classif/id.locale`, and writes via `document.documentElement.style.setProperty`. It also calls `classList.toggle/add/remove`, `setAttribute`, sets `lang`, `dir`, and `dataset.classification` — the vm context must stub ALL of these plus `parseInt`/`isNaN`, or the try/catch silently swallows a ReferenceError and the guard sees zero vars.
- P-table key names differ from the TS palette (`rSm/r/rLg` vs `radius.sm/base/lg`) — compare PAINTED values against EXPORTED values, never file bytes/ASTs (research anti-pattern).
- frontend/package.json lint script already runs `cd ..` first (repo-root cwd), so `node scripts/check-bootstrap-parity.mjs` resolves the same way `check-duplicate-rtl.mjs` does.
- ci.yml Lint job precedent (lines ~76-82): a run step + a `shell: bash` step with `set -e` and a negated invocation against the fixture.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write the parity guard script + positive-failure fixture</name>
  <files>scripts/check-bootstrap-parity.mjs, tools/bootstrap-fixtures/bad-bootstrap.js</files>
  <read_first>scripts/check-duplicate-rtl.mjs (arg handling + exit discipline precedent), frontend/public/bootstrap.js (full file — the exact stub surface), frontend/tests/bootstrap/migrator.test.ts (stub shapes), .planning/phases/77-linear-token-system/77-RESEARCH.md §Pattern 2 and §Code Examples</read_first>
  <action>Create scripts/check-bootstrap-parity.mjs using ONLY node:fs, node:vm, node:path, node:url. Truth side: native-import PALETTES + FONTS from frontend/src/design-system/tokens/directions.ts and DENSITIES from frontend/src/design-system/tokens/densities.ts (resolve repoRoot from the script's own location so it runs from any cwd). Bootstrap side: read the bootstrap file from the optional CLI arg (resolved via path.resolve against repoRoot, read-only) defaulting to frontend/public/bootstrap.js; for EACH direction key of PALETTES × mode in [light,dark] × density key of DENSITIES, build a FRESH vm context containing: a localStorage stub seeded with id.dir/id.theme/id.density for the combination (getItem returns seeded values or null; setItem/removeItem record into an ops array and succeed), a document stub whose documentElement exposes style.setProperty collecting into a Map, classList with toggle/add/remove no-ops, setAttribute no-op, dataset object, and settable lang/dir properties, plus parseInt and isNaN; then new vm.Script(source).runInContext(ctx). Compare, with strict string equality (this IS the byte-match), the collected values for: --bg, --surface, --surface-raised, --ink, --ink-mute, --ink-faint, --line, --line-soft, --sidebar-bg, --sidebar-ink against the palette fields; --radius-sm/--radius/--radius-lg against palette.radius.sm/base/lg; --font-display/--font-body/--font-mono against FONTS[direction]; --pad/--pad-inline/--pad-block/--gap/--row-h against the DENSITIES entry (note: bootstrap paints --pad and --pad-inline from the density pad value and --pad-block from padBlock — mirror the live mapping, reading densities.ts field names first). On any mismatch or missing var: print direction/mode/density, the variable, expected vs actual, and exit 1; on success print a one-line summary with combination count and exit 0. Structure the comparison var-list as a data table at the top of the script so plan 77-04 can extend it (new tier/accent/semantic vars + coercion probes) without restructuring. Then create tools/bootstrap-fixtures/bad-bootstrap.js: an exact copy of the current frontend/public/bootstrap.js with EXACTLY one palette literal changed (bureau.light.bg '#f7f6f4' → '#f7f6f5') and a header comment stating it is a deliberately-diverged positive-failure fixture, never loaded by the app.</action>
  <verify>
    <automated>node scripts/check-bootstrap-parity.mjs && ! node scripts/check-bootstrap-parity.mjs tools/bootstrap-fixtures/bad-bootstrap.js</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-bootstrap-parity.mjs` exits 0 against the live repo and prints a summary naming the number of combinations checked (currently 4 directions × 2 modes × 3 densities = 24).
    - `node scripts/check-bootstrap-parity.mjs tools/bootstrap-fixtures/bad-bootstrap.js` exits 1 and its output names `--bg` and the bureau/light combination.
    - The script imports no packages beyond node: builtins (grep for `from '` shows only node: specifiers).
  </acceptance_criteria>
  <done>Guard proves parity on the live pair and detects a single-byte divergence via the fixture.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the guard into pnpm lint and the CI Lint job</name>
  <files>frontend/package.json, .github/workflows/ci.yml</files>
  <read_first>frontend/package.json line 17 (lint script), .github/workflows/ci.yml lines 60-90 (Lint job + check-duplicate-rtl positive-failure precedent)</read_first>
  <action>Append `&& node scripts/check-bootstrap-parity.mjs` to the frontend/package.json `lint` script (after the existing check-duplicate-rtl invocation — the script already cd's to repo root). In .github/workflows/ci.yml Lint job, after the duplicate-rtl steps, add two steps mirroring the existing shape exactly: a step named "Check bootstrap token parity" running `node scripts/check-bootstrap-parity.mjs`, and a step named "Assert bootstrap parity check fails on bad fixture (positive-failure)" with `shell: bash` and a `set -e` + `! node scripts/check-bootstrap-parity.mjs tools/bootstrap-fixtures/bad-bootstrap.js` body.</action>
  <verify>
    <automated>cd frontend && pnpm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm run lint` (from frontend/) passes and its output includes the parity guard's summary line.
    - `grep -c "check-bootstrap-parity" .github/workflows/ci.yml` prints 2 (run step + positive-failure step).
    - `grep -c "check-bootstrap-parity" frontend/package.json` prints 1.
  </acceptance_criteria>
  <done>Guard is a hard local + CI gate; a future bootstrap/directions divergence fails the build.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                           |
| ----------------------------- | ------------------------------------- |
| CLI arg → filesystem          | Guard accepts a fixture path argument |
| vm sandbox → bootstrap source | Guard executes repo JS in-process     |

## STRIDE Threat Register

| Threat ID  | Category  | Component                        | Disposition | Mitigation Plan                                                                                                         |
| ---------- | --------- | -------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------- |
| T-77-02-01 | Tampering | fixture path CLI arg             | mitigate    | path.resolve against repoRoot, read-only fs.readFileSync, no subprocess; mirrors check-duplicate-rtl handling           |
| T-77-02-02 | Elevation | vm execution of bootstrap source | mitigate    | Sandbox context contains only hand-built stubs — no require, no process, no network objects exposed                     |
| T-77-02-03 | DoS       | guard flakiness blocking CI      | mitigate    | Pure deterministic computation, no network/timing; fixture proves both polarities so silent-pass regressions are caught |
| T-77-02-SC | Tampering | package installs                 | accept      | No package-manager installs in this plan; slopcheck N/A                                                                 |

</threat_model>

<verification>
- Both guard polarities verified locally (exit 0 live, exit 1 fixture).
- `pnpm run lint` green with the guard in the chain.
</verification>

<success_criteria>

- FOUC-01 mechanism live and proven BEFORE any Linear literal lands (ROADMAP sequencing satisfied).
- TOKEN-02 enforcement path exists; plan 77-04 extends the same script rather than building a second gate.
  </success_criteria>

<output>
Create `.planning/phases/77-linear-token-system/77-02-SUMMARY.md` when done.
</output>
