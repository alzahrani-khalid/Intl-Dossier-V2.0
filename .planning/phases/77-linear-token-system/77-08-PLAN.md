---
phase: 77-linear-token-system
plan: 08
type: execute
wave: 6
depends_on: [77-06, 77-07]
files_modified:
  - CLAUDE.md
  - frontend/CLAUDE.md
  - frontend/src/design-system/CLAUDE.md
  - frontend/DESIGN.md
  - frontend/design-system/inteldossier_handoff_design/README.md
  - frontend/src/design-system/hooks/useLocale.ts
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - 'No doc declares Bureau canonical: root /CLAUDE.md, frontend/CLAUDE.md, and frontend/src/design-system/CLAUDE.md describe Linear (dark-canonical, radii 6/8/12, surface-1..4 ladder, single direction, THREE literal copies + CI guard)'
    - 'frontend/DESIGN.md is the Linear spec — the authoritative value tables (dark verbatim + derived light + TOKEN-03 palettes), the hairline-strong mapping decision, and the recipe rules'
    - 'inteldossier_handoff_design/ is visibly superseded (banner) and no CLAUDE.md required-reading instruction points new work at it'
    - 'The Phase-76 doc drift is fixed: frontend/CLAUDE.md provider tree no longer names RTLWrapper (MD-01), and the useLocale.ts doc comment matches the delegated implementation (LO-02)'
  artifacts:
    - path: 'frontend/DESIGN.md'
      provides: 'The Linear design spec (replaces the Bureau spec)'
      min_lines: 80
    - path: 'frontend/src/design-system/CLAUDE.md'
      provides: 'Linear-single-direction engine doc: three-copy invariant + check-bootstrap-parity guard + coercion'
  key_links:
    - from: 'CLAUDE.md (root) Visual Design Source of Truth section'
      to: 'frontend/DESIGN.md'
      via: 'repointed required-reading order (handoff dir demoted to historical reference)'
      pattern: "DESIGN\\.md"
---

<objective>
Migrate the design source-of-truth off Bureau (DOC-01): rewrite the three CLAUDE.md design sections and DESIGN.md to the Linear reality shipped by plans 77-03..07, mark the IntelDossier handoff prototype superseded, and clear the Phase-76 carryover doc drift (MD-01 RTLWrapper provider-tree prose, LO-02 useLocale comment).

Purpose: every future UI task reads these files first — stale Bureau guidance would actively fight the new system (raw-radii rules, "Bureau is the default", handoff-component required reading).
Output: Linear-authoritative docs; the prototype demoted to historical reference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-linear-token-system/77-RESEARCH.md  (§DOC-01 scope — exact files + current state + action table)
@.planning/phases/77-linear-token-system/77-03-SUMMARY.md  (derived light set, TOKEN-03 values + ratios, hairline-mapping deviation, unmapped verbatim extras)
@.planning/phases/77-linear-token-system/77-06-SUMMARY.md  (recipe decisions + carve-out table)
@.planning/phases/77-linear-token-system/77-07-SUMMARY.md  (collapsed end-state)
</context>

<planner_decisions>

- **Handoff directory fate (CONTEXT: "retire or repoint"):** KEEP the directory as a historical reference with a supersession banner — do NOT delete it. Rationale: deleting touches eslint.config.mjs ignores and destroys the provenance record for near-zero gain; a banner achieves the DOC-01 goal (future work follows Linear). Add a prominent "SUPERSEDED (2026-07) — the design source of truth is now frontend/DESIGN.md (Linear)" banner at the top of its README.md and repoint every required-reading instruction in the CLAUDE.md files to DESIGN.md.
  </planner_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the three CLAUDE.md design sections + fix MD-01/LO-02 drift</name>
  <files>CLAUDE.md, frontend/CLAUDE.md, frontend/src/design-system/CLAUDE.md, frontend/src/design-system/hooks/useLocale.ts</files>
  <read_first>CLAUDE.md "Visual Design Source of Truth" + "Design rules — non-negotiable" + "Definition of Done — UI checklist" + "Component Library Strategy" sections, frontend/CLAUDE.md (provider-tree paragraph ~line 21 + "Design tokens" section), frontend/src/design-system/CLAUDE.md (full — it is the engine doc), frontend/src/design-system/hooks/useLocale.ts (the stale setLocale doc comment), frontend/src/App.tsx (the ACTUAL current provider chain — verify before writing prose), 77-RESEARCH.md §DOC-01 scope</read_first>
  <action>Root CLAUDE.md: rewrite the Visual Design Source of Truth section — the canonical spec is now frontend/DESIGN.md (Linear); demote inteldossier_handoff_design/ to "historical reference (superseded)"; update the required-reading order to DESIGN.md → src/design-system/CLAUDE.md → closest existing component; update the non-negotiable rules that changed: default is Linear DARK-canonical (drop "Default direction: Bureau… ignore Chancery/Situation/Ministerial"), radii from --radius-sm/--radius/--radius-lg now 6/8/12, surface ladder surface-1..4 (cards on surface, popovers surface-3, drawers surface-4), hairline borders var(--line) with var(--line-strong) for emphasis, no card shadows (unchanged rule, restate against Linear); keep all voice/emoji/date-format rules verbatim (unchanged); update the Definition of Done checklist items that referenced Bureau radii. frontend/CLAUDE.md: fix the provider-tree paragraph — replace the stale `LanguageProvider → RTLWrapper → AppRouter` chain with the ACTUAL current chain read from App.tsx (Phase 76 replaced RTLWrapper with the DirectionProvider bridge; also update `initialDirection="bureau"` prose to linear/dark) — this closes MD-01; Linear-ize the "Design tokens" section wording (token names unchanged; add the new surface-3/4, line-strong, accent-hover, status-1..6 utilities). frontend/src/design-system/CLAUDE.md: rewrite — "Linear is the only direction" (replacing the Bureau-default section), document the coercion invariant (any stored id.dir ≠ linear coerces, both layers), document ALL THREE literal copies (directions.ts ↔ bootstrap.js ↔ index.css :root) and that scripts/check-bootstrap-parity.mjs enforces them in lint+CI, update the file inventory (no directionDefaults/useHue if 77-07 deleted them), and state dark is the default mode. useLocale.ts: fix the doc comment to describe the actual delegated implementation (LO-02). Follow each file's existing tone and structure — these are working docs, not marketing.</action>
  <verify>
    <automated>! grep -rn "Bureau is the default\|initialDirection=\"bureau\"\|RTLWrapper" CLAUDE.md frontend/CLAUDE.md frontend/src/design-system/CLAUDE.md && grep -c "linear" frontend/src/design-system/CLAUDE.md | awk '$1>=3'</automated>
  </verify>
  <acceptance_criteria>
    - Zero matches for "Bureau is the default", `initialDirection="bureau"`, or "RTLWrapper" across the three CLAUDE.md files.
    - frontend/CLAUDE.md provider-tree prose matches the live App.tsx chain (manually cross-checked against the file).
    - useLocale.ts comment no longer claims setLocale calls i18n.changeLanguage directly.
    - The three-copy invariant + guard script are named in frontend/src/design-system/CLAUDE.md.
  </acceptance_criteria>
  <done>Every CLAUDE.md layer teaches Linear; Phase-76 drift (MD-01/LO-02) closed.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite DESIGN.md as the Linear spec + supersede the handoff prototype</name>
  <files>frontend/DESIGN.md, frontend/design-system/inteldossier_handoff_design/README.md</files>
  <read_first>frontend/DESIGN.md (current Bureau spec — mirror its structural conventions), .planning/phases/77-linear-token-system/77-03-SUMMARY.md (the shipped values + ratios + mapping decisions), .planning/phases/77-linear-token-system/77-06-SUMMARY.md (recipe + carve-out decisions), frontend/src/design-system/tokens/directions.ts (the authoritative literals — transcribe, do not invent)</read_first>
  <action>Rewrite frontend/DESIGN.md as the Linear specification, sourcing every value from the SHIPPED directions.ts literals (never from memory): dark-canonical principle + the light-derivation discipline; full token tables for dark and light (core surfaces/ink/lines incl. surface-3/4, ink-tertiary, line-strong; accent family incl. accent-hover; semantic danger/warn/ok/info + softs; the six status-tag pairs with their measured AA ratios from the 77-03 SUMMARY); the hairline mapping decision (verbatim #34343a = --line-strong, --line-soft is derived-softer — the documented deviation from the raw reference naming); the reserved unmapped verbatim extras (hairline-tertiary, semantic-overlay, brand-secure); type stack ('Inter Variable' / 'JetBrains Mono Variable' naming with the registered-family rationale, weights, Tajawal RTL cascade preserved); radius ladder 6/8/12 with the fuller reference ladder noted; recipe rules (hairline borders, no card shadows, surface ladder assignments, button/input/card recipes as shipped in 77-06); and the engine contract summary (three copies + parity guard + coercion + dark default). Add the supersession banner at the top of frontend/design-system/inteldossier_handoff_design/README.md: a short block stating the prototype is SUPERSEDED as of v8.0 Phase 77, kept for historical reference only, with the pointer to frontend/DESIGN.md — change nothing else in that directory.</action>
  <verify>
    <automated>grep -q "#5e6ad2" frontend/DESIGN.md && grep -q "Inter Variable" frontend/DESIGN.md && grep -qi "superseded" frontend/design-system/inteldossier_handoff_design/README.md && ! grep -qi "bureau" frontend/DESIGN.md</automated>
  </verify>
  <acceptance_criteria>
    - DESIGN.md contains the Linear accent (#5e6ad2), the registered font names, dark AND light token tables, the status-tag palette with ratios, and zero Bureau references (except an optional one-line "replaces the Bureau spec" historical note — if kept, adjust the grep expectation consciously and record why).
    - Handoff README.md opens with the supersession banner; `git diff --stat` for that directory shows README.md only.
    - Every hex value in DESIGN.md matches directions.ts (spot-check at least 10 values against the file).
  </acceptance_criteria>
  <done>DESIGN.md is the authoritative Linear spec; the prototype is visibly historical.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                             |
| ---------- | ----------------------------------------------------------------------- |
| (none new) | Documentation-only changes; no code, storage, or input-handling surface |

## STRIDE Threat Register

| Threat ID  | Category    | Component                                              | Disposition | Mitigation Plan                                                                                                                        |
| ---------- | ----------- | ------------------------------------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| T-77-08-01 | Repudiation | docs diverging from shipped values (a new drift class) | mitigate    | Task 2 mandates transcription from directions.ts + spot-check acceptance criterion; the parity guard keeps the code-side copies honest |
| T-77-08-SC | Tampering   | package installs                                       | accept      | No package-manager installs in this plan; slopcheck N/A                                                                                |

</threat_model>

<verification>
- All greps in both tasks pass; `pnpm run lint` still green (docs are outside lint scope but the commit hook builds).
- DOC-01 grep from the phase validation map: `grep -rn "Bureau is the default" CLAUDE.md frontend/` → 0.
</verification>

<success_criteria>

- DOC-01 complete: source-of-truth migrated to Linear across all five doc targets; MD-01 + LO-02 Phase-76 carryovers closed.
  </success_criteria>

<output>
Create `.planning/phases/77-linear-token-system/77-08-SUMMARY.md` when done.
</output>
