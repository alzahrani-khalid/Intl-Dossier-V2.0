---
phase: 77-linear-token-system
plan: 04
type: execute
wave: 3
depends_on: [77-03]
files_modified:
  - frontend/public/bootstrap.js
  - frontend/src/design-system/DesignProvider.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/styles/list-pages.css
  - frontend/src/pages/Dashboard/widgets/dashboard.css
  - frontend/src/components/calendar/calendar.css
  - scripts/check-bootstrap-parity.mjs
  - frontend/tests/bootstrap/coercion.test.ts
  - frontend/tests/e2e/font-registration.spec.ts
  - frontend/tests/unit/design-system/fouc-bootstrap.test.ts
  - frontend/tests/unit/design-system/DesignProvider.test.tsx
  - frontend/tests/unit/design-system/applyTokens.test.ts
  - frontend/tests/unit/design-system/handoff-css-contract.test.ts
  - frontend/tests/bootstrap/migrator.test.ts
autonomous: true
requirements: [TOKEN-04, TOKEN-02, FOUC-01, TOKEN-05, TOKEN-01]
must_haves:
  truths:
    - 'A user whose localStorage holds id.dir=bureau/chancery/situation/ministerial (i.e., EVERY existing user) first-paints the full Linear token set — bootstrap AND DesignProvider both coerce, in the same commit, with write-back (Pitfall 1 closed)'
    - "Unset id.theme first-paints DARK (user decision, dark-canonical); an explicitly persisted id.theme='light' is preserved"
    - 'The index.css :root fallback block holds Linear-dark literals (third copy re-synced; live drift from Pitfall 2 eliminated) and the parity guard now enforces it'
    - 'The parity guard fails the build if a retired id.dir stops coercing to linear (regression probe) or if any of the three literal copies diverge'
    - "'Inter Variable' and 'JetBrains Mono Variable' are the resolved Latin families (document.fonts.check probe) and the Tajawal cascade still wins under dir=rtl"
    - 'The 113 .dir-* CSS references resolve for the linear direction (.dir-bureau renamed to .dir-linear; chancery/situation/ministerial rule blocks deleted) — list pages and dashboard keep their styling at the flip'
  artifacts:
    - path: 'frontend/tests/bootstrap/coercion.test.ts'
      provides: 'vm-harness coercion regression: each retired dir, unset dir, unset theme→dark, persisted theme kept, blocked storage'
    - path: 'frontend/tests/e2e/font-registration.spec.ts'
      provides: 'document.fonts.check probes for Inter Variable / JetBrains Mono Variable / Tajawal-under-rtl'
    - path: 'scripts/check-bootstrap-parity.mjs'
      provides: 'Guard v2: extended var list + coercion probes + :root literal check'
  key_links:
    - from: 'frontend/public/bootstrap.js'
      to: 'localStorage id.dir'
      via: 'ES5 coercion with try-guarded write-back before palette lookup'
      pattern: "id\\.dir"
    - from: 'frontend/src/design-system/DesignProvider.tsx'
      to: 'localStorage id.dir'
      via: "constant 'linear' direction init + WR-10 one-time write-back effect (no writes in lazy initializers)"
      pattern: 'LS_DIR'
    - from: 'frontend/src/App.tsx'
      to: 'DesignProvider'
      via: 'initialDirection linear + initialMode dark + fallbackDirection linear'
      pattern: 'initialDirection'
    - from: 'frontend/src/index.css :root'
      to: 'PALETTES.linear.dark'
      via: 'guard v2 regex-extracted literal comparison'
      pattern: ':root'
---

<objective>
Activate Linear — the single highest-risk change of the phase (CONTEXT: "the id.dir legacy-coercion is the single highest-risk change; it must land in bootstrap.js AND DesignProvider together or first paint loses all tokens"). One atomic flip commit changes: bootstrap coercion + dark default + linear paint (including the new tier/accent/semantic/status vars), DesignProvider coercion + dark default, App.tsx defaults, the index.css :root third copy, and the .dir-* CSS class rename — then the guard is extended to make every one of those invariants build-breaking forever.

Purpose: every existing user holds a retired id.dir; the old `P.bureau.light` fallback vanishes with the swap. Coercion in both layers + write-back is the load-bearing migration (TOKEN-04).
Output: app boots Linear dark-canonical for everyone; guard v2 + coercion tests + font probe lock it in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-linear-token-system/77-RESEARCH.md  (§Pattern 3: the coercion design — copy it; §Pattern 2 steps 4/6; §Pitfalls 1, 2, 6, 7, 8; §Blast radius CSS table)
@.planning/phases/77-linear-token-system/77-CONTEXT.md  (default id.theme = dark, USER DECISION)
@frontend/public/bootstrap.js
@frontend/src/design-system/DesignProvider.tsx
@frontend/src/App.tsx
</context>

<atomicity_note>
Task 1 touches 7 production files in ONE commit. This exceeds normal task sizing deliberately: (a) the three-copy invariant requires directions-side values, bootstrap, and the :root block to move together (bootstrap+:root here; values landed in 77-03); (b) coercion must land in bootstrap AND DesignProvider in the same change (CONTEXT lock); (c) the moment coercion makes `dir-linear` the emitted class, every `.dir-bureau`-scoped CSS rule (76 in list-pages.css, 30 in dashboard.css, 6 in index.css, 1 in calendar.css) goes dead — so the CSS rename must ride the same commit or list pages visually break at the flip (Pitfall 8). Splitting any of these into separate commits produces a broken intermediate state on the branch.
</atomicity_note>

<interfaces>
Current bootstrap contract (read the file — 118 lines, ES5 invariant in header):
  line 9: `var d = localStorage.getItem('id.dir') || 'bureau';`
  line 10: `var m = localStorage.getItem('id.theme') || 'light';`
  line 58: `var p = (P[d] && P[d][m]) || P.bureau.light;`  ← the vanishing fallback (Pitfall 1)
  lines 70-81: accent + semantic vars painted from hue-math / mode ternaries — replace with linear palette-literal reads
  lines 56-57: `.dir-*` class emission (`r.classList.add('dir-' + d)`)

Coercion shape to implement (RESEARCH §Pattern 3, ES5-safe):
bootstrap: if d !== 'linear' → d = 'linear' + try { localStorage.setItem('id.dir','linear') } catch; m default 'dark' + whitelist to light|dark; palette lookup `(P.linear && P.linear[m]) || P.linear.dark`.
DesignProvider (lines 161-169 today): direction lazy init becomes constant 'linear'; NO localStorage writes in initializers (WR-10 — StrictMode double-invokes); add a one-time useEffect write-back: if safeGetItem(LS_DIR) !== 'linear' → safeSetItem(LS_DIR,'linear'). Mode lazy init already prefers the stored value (preserves explicit light users); flip the `initialMode` default parameter (line ~153) 'light' → 'dark'. Storage-event listener (line ~325): id.dir events only accepted when newValue === 'linear'.

App.tsx: initialDirection="linear", initialMode="dark", fallbackDirection="linear" (union widened in 77-03). Leave initialHue as-is (inert; removed in 77-07).

index.css: @theme block at line 43; :root literal block at line 125 (currently Bureau-light and DRIFTED — --ink-faint #9a9082, --accent-fg oklch(99% 0.01 32)); RTL Tajawal cascade at ~434-460 lists 'Inter' (unregistered name) in fallback stacks; .dir-bureau appears in the cascade display list (~line 459) and in a `html[dir='rtl'] .dir-bureau .btn-*` specificity rule (~line 492).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The flip commit — coercion + dark default + linear paint + :root + CSS dir-class rename (atomic)</name>
  <files>frontend/public/bootstrap.js, frontend/src/design-system/DesignProvider.tsx, frontend/src/App.tsx, frontend/src/index.css, frontend/src/styles/list-pages.css, frontend/src/pages/Dashboard/widgets/dashboard.css, frontend/src/components/calendar/calendar.css, frontend/tests/unit/design-system/fouc-bootstrap.test.ts, frontend/tests/unit/design-system/DesignProvider.test.tsx, frontend/tests/unit/design-system/applyTokens.test.ts, frontend/tests/unit/design-system/handoff-css-contract.test.ts, frontend/tests/bootstrap/migrator.test.ts</files>
  <read_first>frontend/public/bootstrap.js (full), frontend/src/design-system/DesignProvider.tsx (esp. lines 33-53 guards, 92-200 initializers + WR-10 comments, 250-260 setters, 320-335 storage listener), frontend/src/App.tsx, frontend/src/index.css lines 36-130 (@theme + :root) and 425-500 (RTL cascade + .dir rules), 77-RESEARCH.md §Pattern 3 + §Pitfall 8, frontend/src/design-system/tokens/directions.ts (the linear values landed by 77-03)</read_first>
  <action>Apply the flip atomically, per D-locked CONTEXT decisions. bootstrap.js (ES5 ONLY — no arrows/const/template literals, Pitfall 7): implement the id.dir coercion + write-back and the 'dark' theme default + whitelist per the interfaces block; change the palette lookup to the P.linear-internal fallback; stop reading id.hue for paint (keep the var h parse removed or unused-safe — prefer removing the id.hue read entirely); rewrite the accent/semantic paint lines (70-81) to read the linear palette-literal groups (accent.base/hover/fg/ink/soft, semantic.*, sla.*) and ADD setProperty lines for --surface-3, --surface-4, --ink-tertiary, --line-strong, --accent-hover, and the twelve --status-N/--status-N-soft vars from the P.linear nested groups; update the --shadow-* static lines so card-level shadow tokens align with Linear (no card shadows; keep the drawer shadow); dir-class emission stays `'dir-' + d` (coerced d = 'linear' → emits dir-linear). DesignProvider.tsx: constant-'linear' direction init + WR-10 write-back effect + initialMode default 'dark' + storage-listener guard per the interfaces block (do NOT remove hue state — 77-07). App.tsx: the three prop changes per the interfaces block. index.css: rewrite the :root block (line 125) to the Linear DARK literals byte-consistent with PALETTES.linear.dark (this is the new default first-frame fallback for blocked-localStorage, and it retires the drifted --ink-faint/--accent-fg values), including the new tier/accent/semantic/status vars; extend the @theme block with --color-surface-3, --color-surface-4, --color-ink-tertiary, --color-line-strong, --color-accent-hover, and --color-status-1..6 (+soft) mappings to their runtime vars; replace the unregistered 'Inter' family name with 'Inter Variable' (and any 'JetBrains Mono' with 'JetBrains Mono Variable') in the RTL cascade fallback stacks and anywhere else index.css names them, keeping 'Tajawal' first under html[dir='rtl']. CSS rename (strategy (a), planner decision per RESEARCH Q3): in index.css, list-pages.css, dashboard.css, calendar.css — rename every `.dir-bureau` selector to `.dir-linear` and DELETE the `.dir-chancery`/`.dir-situation`/`.dir-ministerial` rule blocks; preserve the `html[dir='rtl'] .dir-linear .btn-*` !important Tajawal rules exactly (they win deliberate specificity battles). Update the enumerated test files in the same commit so vitest stays green: fouc-bootstrap.test.ts / migrator.test.ts (default expectations: linear + dark), DesignProvider.test.tsx (direction always linear, mode default dark, stored-light preserved), applyTokens.test.ts and handoff-css-contract.test.ts (retired-direction assertions → linear equivalents). Change no OTHER behavior: density, classification, locale handling in bootstrap stay byte-identical.</action>
  <verify>
    <automated>pnpm -C frontend exec vitest run tests/bootstrap tests/unit/design-system && pnpm -C frontend type-check && grep -rn "dir-chancery\|dir-situation\|dir-ministerial\|dir-bureau" frontend/src frontend/public | grep -v node_modules | wc -l | grep -qx 0 && echo CSS-CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - vitest bootstrap + design-system suites green with updated expectations.
    - Zero `.dir-bureau|chancery|situation|ministerial` references remain under frontend/src and frontend/public (bootstrap's classList.remove line may keep legacy names ONLY if still needed for stale-DOM cleanup — if kept, add it to the grep exclusion consciously and record why; default: remove them and emit dir-linear only).
    - `grep -n "id.hue" frontend/public/bootstrap.js` → 0 matches (bootstrap no longer reads the hue key).
    - index.css :root contains '#010102' and no longer contains '#9a9082' (the drifted value).
    - App.tsx contains initialDirection="linear", initialMode="dark", fallbackDirection="linear".
  </acceptance_criteria>
  <done>App boots Linear-dark for every legacy profile; three copies aligned; list-page/dashboard CSS alive under .dir-linear.</done>
</task>

<task type="auto">
  <name>Task 2: Guard v2 (extended vars + coercion probes + :root check) and the coercion vm-harness test</name>
  <files>scripts/check-bootstrap-parity.mjs, tools/bootstrap-fixtures/bad-bootstrap.js, frontend/tests/bootstrap/coercion.test.ts</files>
  <read_first>scripts/check-bootstrap-parity.mjs (the var-table structure from 77-02), frontend/tests/bootstrap/migrator.test.ts (vm+JSDOM harness pattern), 77-RESEARCH.md §Pattern 2 steps 3/4/6</read_first>
  <action>Extend the guard: (a) add the new painted vars to the comparison table — --surface-3, --surface-4, --ink-tertiary, --line-strong, --accent, --accent-hover, --accent-fg, --accent-ink, --accent-soft, --danger(-soft), --warn(-soft), --ok(-soft), --info(-soft), the six --sla-* vars, and --status-1..6(+soft) — compared against the PALETTES nested-group leaf strings (only for directions whose palette carries the groups, so the guard stays generic); (b) coercion probes: four extra vm runs seeding id.dir='bureau'/'chancery'/'situation'/'ministerial' (theme unset) asserting the painted --bg equals PALETTES.linear.dark.bg AND that the localStorage stub recorded setItem('id.dir','linear'); plus one run with everything unset asserting linear-dark paint (default-theme probe); (c) :root check: regex-extract the first `:root { ... }` block from frontend/src/index.css and compare its values for the core+extended var names against PALETTES.linear.dark / FONTS.linear — ENFORCED, not advisory (planner decision: the live drift found by research justifies it). Refresh tools/bootstrap-fixtures/bad-bootstrap.js as a copy of the NEW bootstrap.js with exactly one diverged literal (linear dark bg '#010102' → '#010103') so the positive-failure step keeps proving the live code path. Create frontend/tests/bootstrap/coercion.test.ts reusing the migrator.test.ts vm+JSDOM harness with cases: each of the four retired id.dir values → linear palette painted + id.dir rewritten to 'linear'; unset id.dir → linear; unset id.theme → dark values; persisted id.theme='light' → linear LIGHT values painted (explicit choice preserved); localStorage.setItem throwing (read-only storage) → coercion still paints linear without throwing.</action>
  <verify>
    <automated>node scripts/check-bootstrap-parity.mjs && ! node scripts/check-bootstrap-parity.mjs tools/bootstrap-fixtures/bad-bootstrap.js && pnpm -C frontend exec vitest run tests/bootstrap/coercion.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Guard v2 exits 0 live and 1 on the refreshed fixture; its output shows the coercion probes and the :root check ran.
    - coercion.test.ts covers all 8 enumerated cases and passes.
    - Full `pnpm run lint` (from frontend/) green — guard v2 is in the chain from 77-02.
  </acceptance_criteria>
  <done>Every flip invariant (byte-match ×3 copies, coercion, dark default) is build-breaking.</done>
</task>

<task type="auto">
  <name>Task 3: Font-registration e2e probe (TOKEN-05 honest verification)</name>
  <files>frontend/tests/e2e/font-registration.spec.ts</files>
  <read_first>77-RESEARCH.md §Pitfall 4, frontend/src/fonts.ts, one existing small spec in frontend/tests/e2e/ for project Playwright conventions</read_first>
  <action>Create a small Playwright spec (no snapshots) that loads the app root and asserts via page.evaluate: document.fonts.check("16px 'Inter Variable'") === true and document.fonts.check("16px 'JetBrains Mono Variable'") === true (await document.fonts.ready first); that getComputedStyle(document.body).fontFamily includes 'Inter Variable' in LTR; and — navigating with ?lng=ar — that document.documentElement.dir === 'rtl', document.fonts.check("16px Tajawal") === true, and the computed body font-family starts with Tajawal. This is the honest TOKEN-05 verification: 'Inter' was never a registered family (today's UI silently renders system-ui), so the probe must target the Variable names.</action>
  <verify>
    <automated>pnpm -C frontend exec playwright test tests/e2e/font-registration.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec passes locally (dev-server auto-start) with all font checks true in both LTR and RTL navigations.
  </acceptance_criteria>
  <done>Latin stack provably renders Inter Variable / JetBrains Mono Variable; Tajawal cascade provably intact under RTL.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                          |
| --------------------------------------- | -------------------------------------------------------------------- |
| localStorage → bootstrap/DesignProvider | Untrusted id.dir/id.theme drive first-paint and steady-state theming |
| CI guard CLI arg → filesystem           | Fixture path argument (unchanged from 77-02)                         |

## STRIDE Threat Register

| Threat ID  | Category  | Component                                           | Disposition | Mitigation Plan                                                                                                                        |
| ---------- | --------- | --------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| T-77-04-01 | Tampering | stored id.dir / id.theme injection                  | mitigate    | Coercion IS a whitelist: any value ≠ 'linear' → 'linear'; theme whitelisted to light                                                   | dark; values never reach the DOM except as static-table lookups via setProperty (ASVS V5) |
| T-77-04-02 | DoS       | first-paint token loss for legacy users (Pitfall 1) | mitigate    | Dual-layer coercion in one commit + guard v2 retired-dir probes make regression build-breaking + coercion.test.ts blocked-storage case |
| T-77-04-03 | Tampering | :root third-copy drift re-emerging                  | mitigate    | Guard v2 :root literal check enforced in lint + CI                                                                                     |
| T-77-04-SC | Tampering | package installs                                    | accept      | No package-manager installs in this plan; slopcheck N/A                                                                                |

</threat_model>

<verification>
- Full vitest (bootstrap + design-system) green; guard v2 both polarities; font spec green; `pnpm run lint` green; `pnpm -C frontend type-check` green.
- Manual smoke on the dev server: fresh profile → dark Linear first paint with no flash; profile seeded id.dir='bureau' → identical result.
</verification>

<success_criteria>

- TOKEN-04 coercion + explicit dark default live in both layers; TOKEN-02/FOUC-01 enforcement covers all three copies + coercion; TOKEN-05 fonts provably registered; Linear is what renders.
  </success_criteria>

<output>
Create `.planning/phases/77-linear-token-system/77-04-SUMMARY.md` when done.
</output>
