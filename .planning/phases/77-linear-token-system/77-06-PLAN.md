---
phase: 77-linear-token-system
plan: 06
type: execute
wave: 4
depends_on: [77-04]
files_modified:
  - frontend/src/index.css
  - frontend/src/components/ui/file-upload.tsx
  - frontend/src/components/ui/enhanced-progress.tsx
  - frontend/src/components/ui/sidebar-collapsible.tsx
  - frontend/src/components/ui/timeline.tsx
  - frontend/src/components/ui/pull-to-refresh-indicator.tsx
  - frontend/src/components/ui/form-wizard.tsx
autonomous: true
requirements: [TOKEN-06]
must_haves:
  truths:
    - 'components/ui primitives follow the Linear recipes: hairline 1px var(--line) borders, NO drop shadows on cards/buttons/inputs, radius exclusively from --radius-sm/--radius/--radius-lg, surface-1..4 ladder applied (cards on surface, popovers/menus on surface-3, drawers/modals on surface-4)'
    - 'Each of the ~74 carve-out color-literal lines has an explicit per-item keep/migrate/skip decision recorded (live-chrome files migrated to semantic tokens; chart/map data palettes kept; Phase-79 dead files skipped with annotation)'
    - 'Design Token Check (ESLint) stays green — no NEW raw hex or palette literals introduced anywhere'
  artifacts:
    - path: 'frontend/src/index.css'
      provides: 'Linear-recipe .btn-primary/.btn-ghost/.card/.id-input styles (hairline borders, no card shadows, surface ladder)'
    - path: 'frontend/src/components/ui/file-upload.tsx'
      provides: 'Semantic-token status colors (largest carve-out migration, 15 literal lines)'
  key_links:
    - from: 'frontend/src/components/ui/* migrated files'
      to: '@theme semantic utilities (text-danger, bg-ok-soft, --status-*)'
      via: 'Tailwind utility replacement of palette literals'
      pattern: 'text-danger|bg-danger|status-'
---

<objective>
Re-skin the `components/ui/*` primitive layer to Linear's recipes and resolve the ESLint carve-out (TOKEN-06). The token values already flow (77-03/77-04) — this plan fixes what tokens cannot: shadow usage, border weights, surface-ladder assignments in the recipe CSS, and the ~74 pre-existing color literals that the Design Token Check never sees (the eslint.config.mjs carve-out for components/ui).

Purpose: color re-skin is automatic via tokens; recipe metrics and hard-coded literals are the remaining Bureau residue.
Output: Linear-faithful primitives + a per-item carve-out decision table in the SUMMARY (CONTEXT mandates the decisions be made against the actual code during execution and recorded).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-linear-token-system/77-RESEARCH.md  (§TOKEN-06 carve-out inventory — per-file table with Phase-75 verdicts + framework buckets; §Primitive anatomy)
@.planning/research/STACK.md  (Linear recipe direction: hairline borders, surface ladder, radius 6/8/12)
@frontend/src/index.css
@frontend/eslint.config.mjs  (carve-out block at ~lines 236-241)
</context>

<important_facts>

- Research counted EXACTLY 74 Tailwind-palette-literal lines across 12 files + 20 raw-hex occurrences in 4 files. RE-COUNT at execution (parallel work can move numbers) with: grep over components/ui for palette-literal classes and hex.
- Phase-75 verdicts: file-upload(15)/enhanced-progress(9)/sidebar-collapsible(8)/timeline(7)/pull-to-refresh-indicator(7)/form-wizard(3) are LIVE → migrate. chart.tsx(5 hex)/world-map.tsx(5 hex) are LIVE data-viz palettes → keep-as-is (deferred chart-token phase, Phase-51 D-03 rationale). expandable-card(8)/floating-dock(6)/placeholders-and-vanish-input(5+1)/background-boxes(3+9)/animated-tooltip(2)/moving-border(1) are DEAD, deleted by Phase 79 → skip with one-line annotation each (migrating them is wasted work).
- These verdicts are the researched STARTING POSITION, not the decision — CONTEXT requires the final call per item to be made against the actual code and recorded in the SUMMARY.
- ui/button.tsx re-exports heroui-button.tsx whose variants map to .btn-primary/.btn-ghost CSS classes; ui/input.tsx uses .id-input — the recipes live in index.css, so most of the re-skin is CSS-side.
- --shadow-card is already 'none' for linear (77-03) and --line-strong exists (77-03); bg-surface-3 / bg-surface-4 utilities exist via @theme (77-04).
  </important_facts>

<tasks>

<task type="auto">
  <name>Task 1: Linear recipe pass over the primitive CSS (borders, shadows, radius, surface ladder)</name>
  <files>frontend/src/index.css</files>
  <read_first>frontend/src/index.css (.btn-primary, .btn-ghost, .card, .id-input, drawer/popover/menu surface rules, plus any box-shadow declarations), 77-RESEARCH.md §Primitive anatomy, .planning/research/STACK.md §1</read_first>
  <action>Apply the Linear recipe to the primitive/recipe classes in index.css: every card-class rule uses border 1px solid var(--line) and NO box-shadow (remove/neutralize any remaining card-level box-shadow declarations; the drawer shadow var(--shadow-drawer)/--shadow-lg usage for drawers and hovered list rows stays — that is the standing design rule); button recipes (.btn-primary/.btn-ghost) get hairline borders where Linear uses them (ghost: 1px var(--line); primary: none or accent-matched), accent-hover state uses var(--accent-hover) (new token from 77-04) instead of any opacity/filter hack; inputs (.id-input) get 1px var(--line) borders with var(--line-strong) or the focus-ring on focus per the existing --focus-ring token; verify no rule hard-codes px corner radii — everything through var(--radius-sm/--radius/--radius-lg) (Bureau's 8/12/16 became Linear's 6/8/12 automatically via tokens; this task only fixes hard-coded stragglers); apply the surface ladder where index.css assigns surfaces: cards/panels on var(--surface), popover/menu/dropdown content on var(--surface-3), drawer/modal/sheet content on var(--surface-4) (use the vars directly in recipe CSS; component-level Tailwind utilities are out of scope here). Make NO layout/spacing changes — color/border/shadow/radius/surface only. The Manual-Only VALIDATION row (Linear re-skin visual fidelity) is satisfied at phase verify-work — note any judgment calls in the SUMMARY for that review.</action>
  <verify>
    <automated>cd frontend && pnpm run lint && pnpm type-check && pnpm exec vitest run tests/unit/design-system/handoff-css-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "box-shadow" frontend/src/index.css` shows no card-class shadow (only drawer/hover-row/focus-ring occurrences).
    - No new hard-coded px radius on recipe classes: radius declarations on .btn-*/.card/.id-input resolve to var(--radius*) tokens.
    - Popover/menu recipe rules reference var(--surface-3); drawer/modal rules reference var(--surface-4).
    - Lint + type-check + handoff-css-contract test green.
  </acceptance_criteria>
  <done>Primitive recipes are Linear-faithful: hairline borders, flat cards, token radii, surface ladder.</done>
</task>

<task type="auto">
  <name>Task 2: Carve-out sweep — explicit keep/migrate/skip per literal, live files migrated to semantic tokens</name>
  <files>frontend/src/components/ui/file-upload.tsx, frontend/src/components/ui/enhanced-progress.tsx, frontend/src/components/ui/sidebar-collapsible.tsx, frontend/src/components/ui/timeline.tsx, frontend/src/components/ui/pull-to-refresh-indicator.tsx, frontend/src/components/ui/form-wizard.tsx</files>
  <read_first>77-RESEARCH.md §TOKEN-06 carve-out inventory (the 12-file table), frontend/eslint.config.mjs carve-out block (~236-241), each of the 6 migrate-bucket files in full, frontend/src/index.css @theme block (available semantic utilities incl. --color-status-*)</read_first>
  <action>First RE-COUNT the carve-out: grep components/ui for Tailwind palette literals (pattern like (text|bg|border|ring|from|to|via)-(red|green|blue|amber|yellow|orange|purple|violet|indigo|sky|cyan|teal|emerald|lime|rose|pink|fuchsia|slate|gray|zinc|neutral|stone)-[0-9]+) and raw hex; reconcile against the research table and note any drift. Then migrate the 6 LIVE files: replace each palette literal with the semantically-equivalent token utility — success/complete states → text-ok/bg-ok-soft (or --status-* where the usage is a status tag), errors → text-danger/bg-danger-soft, warnings → text-warn equivalents, informational → text-info equivalents, neutral grays → ink/ink-mute/ink-faint/line utilities — choosing per-usage against the actual rendered intent, not mechanically. For chart.tsx and world-map.tsx: KEEP as-is (data-visualization palettes; deferred chart-token phase) — no edit. For the 7 Phase-79-dead files (expandable-card, floating-dock, placeholders-and-vanish-input, background-boxes, animated-tooltip, moving-border, and any other dead-verdict file in the re-count): SKIP — no edit, one-line annotation each in the decision table pointing at Phase 79 deletion. Produce the per-item decision table (file, literal line count, decision, replacement mapping or skip rationale) for the SUMMARY — this table is a TOKEN-06 deliverable, not optional. Do not remove or narrow the eslint.config.mjs carve-out block in this plan (chart/map/dead files still need it; revisit post-Phase-79).</action>
  <verify>
    <automated>cd frontend && pnpm run lint && pnpm exec vitest run --dir tests/unit 2>/dev/null || pnpm exec vitest run tests/unit</automated>
  </verify>
  <acceptance_criteria>
    - The 6 migrated files contain zero Tailwind palette literals (re-run the re-count grep scoped to those 6 files → 0 matches).
    - chart.tsx / world-map.tsx byte-unchanged (git diff empty for both).
    - Decision table drafted covering EVERY file from the re-count (12+ rows), ready for the SUMMARY.
    - Full lint green (Design Token Check unchanged), vitest unit suite green.
  </acceptance_criteria>
  <done>Every carve-out literal has an explicit recorded decision; live chrome uses semantic tokens.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                 |
| ---------- | --------------------------------------------------------------------------- |
| (none new) | CSS + class-string refactor; no input handling, storage, or network changes |

## STRIDE Threat Register

| Threat ID  | Category  | Component                                      | Disposition | Mitigation Plan                                                                                                                                   |
| ---------- | --------- | ---------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-77-06-01 | Tampering | visual-regression risk masquerading as styling | mitigate    | Recipe changes verified by handoff-css-contract test + phase-level human visual review (Manual-Only VALIDATION row); no layout properties touched |
| T-77-06-SC | Tampering | package installs                               | accept      | No package-manager installs in this plan; slopcheck N/A                                                                                           |

</threat_model>

<verification>
- Full lint (incl. Design Token Check + parity guard) green; vitest unit green; type-check green.
- The recorded decision table covers 100% of the re-counted carve-out lines.
</verification>

<success_criteria>

- TOKEN-06 complete: primitives follow Linear recipes; every carve-out literal has an explicit keep/migrate/skip decision recorded in the SUMMARY.
  </success_criteria>

<output>
Create `.planning/phases/77-linear-token-system/77-06-SUMMARY.md` when done — MUST embed the per-item carve-out decision table.
</output>
