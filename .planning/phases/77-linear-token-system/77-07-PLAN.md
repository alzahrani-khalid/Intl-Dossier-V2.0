---
phase: 77-linear-token-system
plan: 07
type: execute
wave: 5
depends_on: [77-05, 77-06]
files_modified:
  - frontend/src/design-system/tokens/types.ts
  - frontend/src/design-system/tokens/directions.ts
  - frontend/src/design-system/tokens/buildTokens.ts
  - frontend/public/bootstrap.js
  - frontend/src/fonts.ts
  - frontend/src/design-system/DesignProvider.tsx
  - frontend/src/design-system/hooks/useHue.ts (delete)
  - frontend/src/design-system/hooks/index.ts
  - frontend/src/design-system/hooks/useDesignTokens.ts
  - frontend/src/design-system/directionDefaults.ts
  - frontend/src/components/theme-error-boundary/ThemeErrorBoundary.tsx
  - frontend/src/types/settings.types.ts
  - frontend/src/pages/settings/SettingsPage.tsx
  - frontend/src/services/preference-sync.ts
  - frontend/src/components/layout/ClassificationBar.tsx
  - frontend/src/components/layout/AppShell.tsx (comments)
  - frontend/src/pages/Dashboard/widgets/WidgetCard.tsx (comments)
  - frontend/src/pages/Dashboard/widgets/WidgetHeader.tsx (comments)
  - frontend/src/utils/storage/preference-storage.ts (comments)
  - frontend/tests/unit/design-system/buildTokens.test.ts
  - frontend/tests/unit/design-system/contrast.test.ts (imports only, if needed)
  - frontend/src/design-system/directionDefaults.test.ts (or its actual location; may be deleted with the module)
  - frontend/src/components/layout/ClassificationBar.test.tsx (or colocated path)
  - frontend/tests (AppShell tests if they reference directions)
  - frontend/tests/e2e/qa-sweep-focus-outline.spec.ts
  - frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/ (8 deleted, 2 added)
autonomous: true
requirements: [TOKEN-04, TOKEN-01, TOKEN-05]
must_haves:
  truths:
    - "Direction type is exactly 'linear'; PALETTES/FONTS hold exactly one direction; bootstrap P/F tables hold exactly one direction — the retired data is deleted, not dormant"
    - 'The hue axis is fully retired: no id.hue read anywhere, useHue deleted, BuildInput has no hue field, the coercion write-back removes a stale id.hue key'
    - 'fonts.ts self-hosts only Inter Variable + JetBrains Mono Variable + Tajawal (retired per-direction fonts dropped; bundle shrinks; Bundle Size Check still green)'
    - "grep finds zero retired-direction identifiers in frontend/src outside the engagement domain ('ministerial' as an engagement level is a DIFFERENT enum and stays untouched)"
    - 'The direction-matrix visual spec is linear-only (dark+light, 2 shots) with the 8 obsolete direction PNGs deleted and the replacement documented'
  artifacts:
    - path: 'frontend/src/design-system/tokens/types.ts'
      provides: "Direction = 'linear'; extended palette fields now required; BuildInput without hue"
    - path: 'frontend/src/fonts.ts'
      provides: '3-family font pipeline (inter, jetbrains-mono variable + tajawal 400/500/700)'
    - path: 'frontend/tests/e2e/qa-sweep-focus-outline.spec.ts'
      provides: 'linear × {light,dark} focused-primitive matrix'
  key_links:
    - from: 'frontend/src/design-system/DesignProvider.tsx'
      to: 'localStorage id.hue'
      via: 'removeItem in the one-time write-back effect'
      pattern: "id\\.hue"
    - from: 'scripts/check-bootstrap-parity.mjs'
      to: 'collapsed single-direction tables'
      via: 'data-driven iteration (guard needs no edit; combination count drops to 6)'
      pattern: 'PALETTES'
---

<objective>
Collapse the engine to Linear-only — delete the four retired directions from the type system, both literal tables, the font pipeline, and every consumer; retire the hue plumbing end-to-end; rewrite the direction-matrix visual spec linear-only. After 77-04/77-05 nothing reaches the retired code paths; this plan deletes them so they cannot resurrect.

Purpose: TOKEN-04's "the 4-direction switcher is gone from tokens/types.ts" clause + TOKEN-01's single-direction end-state; dormant palettes invite drift and confuse the byte-match story.
Output: single-direction engine, hue-free API, 3-family font pipeline, zero retired identifiers outside the engagement domain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-linear-token-system/77-RESEARCH.md  (§Blast radius — the enumerated 19-file table IS this plan's checklist; §Pitfall 3 — engagement.types.ts FALSE POSITIVE; §Runtime State Inventory — preference-sync is ORPHANED, code-edit only, no data migration)
@frontend/src/design-system/tokens/types.ts
@frontend/src/design-system/DesignProvider.tsx
</context>

<critical_guardrails>

- **NEVER grep-replace direction names repo-wide** (Pitfall 3): `engagement.types.ts` defines 'ministerial' as an ENGAGEMENT LEVEL (lines ~62, 490) with AR translations and downstream consumers — untouchable. Work strictly from the enumerated file list.
- `usePreferenceSync` has ZERO importers (research-verified orphan) — update the `preference-sync.ts:75` fallback literal ('chancery' → 'linear') and nothing else server-side; NO data migration (the `user_preferences.theme` rows are never read by live code). Optionally note the orphan as a future-cleanup candidate in the SUMMARY; removing it is OUT OF SCOPE.
- The parity guard is data-driven over PALETTES keys — it needs NO edit for the collapse; its combination count simply drops (30 → 6 + probes). If the fixture (tools/bootstrap-fixtures/bad-bootstrap.js) no longer diverges after the collapse, refresh it from the new bootstrap.js with one diverged linear literal.
  </critical_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Engine collapse — types, data tables, buildTokens, fonts</name>
  <files>frontend/src/design-system/tokens/types.ts, frontend/src/design-system/tokens/directions.ts, frontend/public/bootstrap.js, frontend/src/design-system/tokens/buildTokens.ts, frontend/src/fonts.ts, frontend/tests/unit/design-system/buildTokens.test.ts, tools/bootstrap-fixtures/bad-bootstrap.js (refresh if stale)</files>
  <read_first>frontend/src/design-system/tokens/types.ts, frontend/src/design-system/tokens/directions.ts, frontend/public/bootstrap.js, frontend/src/design-system/tokens/buildTokens.ts, frontend/src/fonts.ts, frontend/tests/unit/design-system/buildTokens.test.ts, 77-RESEARCH.md §Token engine anatomy item 7</read_first>
  <action>types.ts: collapse Direction to the single literal 'linear'; promote the 77-03 optional extended fields (surface3, surface4, inkTertiary, lineStrong, accent, semantic, sla, status) to REQUIRED; remove the Hue type and drop hue from BuildInput. directions.ts: delete the chancery/situation/ministerial/bureau PALETTES and FONTS entries and rewrite the stale header comment (it still narrates the 4-direction handoff port) to describe the Linear single-direction engine + the three-copy invariant. bootstrap.js: delete the four retired P and F entries (ES5 discipline holds; the coercion logic from 77-04 STAYS — it is permanent legacy-profile protection); simplify the classList handling to remove/add only dir-linear if legacy class names were still being removed. buildTokens.ts: remove the hue parameter and ALL legacy hue-math/mode-branch fallbacks — every family now reads palette literals unconditionally; '--shadow-card' is unconditionally 'none'; keep the direction parameter and PALETTES[direction] lookup (API stability; single key). fonts.ts: remove the fraunces, public-sans, space-grotesk variable imports and all six ibm-plex classic imports; keep inter + jetbrains-mono variable and tajawal 400/500/700; update the header comment. buildTokens.test.ts: rewrite — drop legacy-direction cases, keep/extend the linear dark+light cases and the hue-independence case becomes obsolete (no hue input); assert the full var set. Update contrast.test.ts imports only if the type changes require it. Run `pnpm exec size-limit` in frontend/ to confirm the font removals keep Bundle Size Check green (it should shrink).</action>
  <verify>
    <automated>node scripts/check-bootstrap-parity.mjs && ! node scripts/check-bootstrap-parity.mjs tools/bootstrap-fixtures/bad-bootstrap.js && pnpm -C frontend exec vitest run tests/unit/design-system tests/bootstrap && pnpm -C frontend type-check</automated>
  </verify>
  <acceptance_criteria>
    - Guard reports 1 direction × 2 modes × 3 densities = 6 combinations + coercion probes, exit 0; fixture polarity still exits 1.
    - `grep -c "chancery\|situation\|ministerial\|bureau" frontend/src/design-system/tokens/directions.ts frontend/public/bootstrap.js` → 0 per file (coercion compares `!== 'linear'`, so no retired names needed).
    - `grep -n "hue" frontend/src/design-system/tokens/buildTokens.ts` → 0 matches.
    - `grep -c "fontsource" frontend/src/fonts.ts` → exactly 5 import lines (2 variable + 3 tajawal weights).
    - `cd frontend && pnpm exec size-limit` passes.
  </acceptance_criteria>
  <done>Single-direction, hue-free engine; both literal tables minimal; fonts pruned; suites green.</done>
</task>

<task type="auto">
  <name>Task 2: Consumer collapse — provider, hooks, defaults, enums, fallbacks, comments</name>
  <files>frontend/src/design-system/DesignProvider.tsx, frontend/src/design-system/hooks/useHue.ts, frontend/src/design-system/hooks/index.ts, frontend/src/design-system/hooks/useDesignTokens.ts, frontend/src/design-system/directionDefaults.ts, frontend/src/components/theme-error-boundary/ThemeErrorBoundary.tsx, frontend/src/types/settings.types.ts, frontend/src/pages/settings/SettingsPage.tsx, frontend/src/services/preference-sync.ts, frontend/src/components/layout/ClassificationBar.tsx, frontend/src/components/layout/AppShell.tsx, frontend/src/pages/Dashboard/widgets/WidgetCard.tsx, frontend/src/pages/Dashboard/widgets/WidgetHeader.tsx, frontend/src/utils/storage/preference-storage.ts, plus affected tests (DesignProvider.test.tsx, directionDefaults test, ClassificationBar test, AppShell tests)</files>
  <read_first>frontend/src/design-system/DesignProvider.tsx (hue state, LS keys, write-back effect from 77-04), frontend/src/design-system/hooks/index.ts + useHue.ts + useDesignTokens.ts, frontend/src/design-system/directionDefaults.ts, 77-RESEARCH.md §Blast radius rows for each file in this task, frontend/src/components/layout/ClassificationBar.tsx lines 90-130</read_first>
  <action>DesignProvider.tsx: remove the hue state, its localStorage persistence, and its context surface; extend the 77-04 one-time write-back effect with safeRemoveItem('id.hue') (stale-key cleanup per the Q1 decision); collapse isDirection to value === 'linear'; drop the App-facing initialHue prop; simplify setDirection to a no-op-or-removed API consistent with how consumers use it (check callers first — after 77-05 the setters should be call-site-free; remove dead context fields your changes orphan). Delete hooks/useHue.ts and its export from hooks/index.ts; update useDesignTokens.ts if it passes hue into buildTokens. directionDefaults.ts: delete the module and its test IF its only consumers were the removed switcher surfaces (grep importers first); otherwise collapse to the single linear entry. ThemeErrorBoundary.tsx: union → 'linear' (or import the collapsed Direction type) and default fallbackDirection 'linear'. settings.types.ts (~lines 83, 196) and SettingsPage.tsx (~line 54): the direction z.enum collapses to z.literal('linear') (or the field is dropped if nothing reads it — grep first; prefer the minimal edit) and the 'chancery' defaults become 'linear'. preference-sync.ts:75: fallback 'chancery' → 'linear' (orphaned module — this one literal only). ClassificationBar.tsx: replace the switch(direction) banner-variant logic with the single linear rendering, keeping the current bureau/ministerial .cls-chip branch's BEHAVIOR as the linear variant (research's action class). Comment-only touch-ups: AppShell.tsx, WidgetCard.tsx, WidgetHeader.tsx (they reference .dir-bureau CSS), preference-storage.ts. Update the affected test files to the collapsed reality.</action>
  <verify>
    <automated>pnpm -C frontend type-check && pnpm -C frontend exec vitest run tests/unit && cd frontend && pnpm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "useHue\|id\\.hue" frontend/src` → only the removeItem cleanup line in DesignProvider (and nothing else).
    - `grep -rn "'chancery'\|'situation'" frontend/src` → 0 matches; `grep -rn "'bureau'" frontend/src` → 0 matches; `grep -rn "'ministerial'" frontend/src` → matches ONLY in the engagement domain (engagement.types.ts + its consumers/i18n) — record the residual list in the SUMMARY as verified-domain hits.
    - Type-check, full unit suite, and full lint green.
  </acceptance_criteria>
  <done>No consumer knows the retired directions or the hue axis existed; engagement domain untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Direction-matrix visual spec → linear-only + reference purge gate</name>
  <files>frontend/tests/e2e/qa-sweep-focus-outline.spec.ts, frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/ (delete 8 direction PNGs, add linear-light + linear-dark)</files>
  <read_first>frontend/tests/e2e/qa-sweep-focus-outline.spec.ts (what the focused-primitive matrix actually asserts — research A4: if it guards something beyond direction coverage, preserve that intent), 77-RESEARCH.md §Runtime State Inventory build-artifacts row</read_first>
  <action>Rewrite the focused-primitive direction×mode matrix in qa-sweep-focus-outline.spec.ts to linear × {light, dark} (2 shots), pinning id.theme per shot (Pitfall-6 discipline) and preserving any non-direction assertions the spec carries (focus-outline checks etc.). Delete the 8 obsolete direction-named PNGs and capture the 2 linear replacements locally (dev-server auto-start, --update-snapshots for this spec only, then replay without the flag). Document in the SUMMARY: the 8 pre-swap PNGs remain in git history under the 77-01 baseline commit lineage — Phase 80's re-compare treats the direction-matrix surface as structurally replaced (intended change), not a silent baseline swap.</action>
  <verify>
    <automated>pnpm -C frontend exec playwright test tests/e2e/qa-sweep-focus-outline.spec.ts && ls frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/ | grep -c "linear" | grep -qx 2 && ! ls frontend/tests/e2e/qa-sweep-focus-outline.spec.ts-snapshots/ | grep -q "bureau\|chancery\|situation\|ministerial"</automated>
  </verify>
  <acceptance_criteria>
    - Spec passes locally without --update-snapshots; snapshot dir holds exactly the 2 linear PNGs for the focused-primitive matrix (plus any unrelated shots the spec already owned).
    - No direction-named snapshot files remain.
  </acceptance_criteria>
  <done>Visual matrix reflects the single-direction reality; replacement documented for Phase 80.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                    |
| ----------------------------- | ---------------------------------------------- |
| localStorage → DesignProvider | id.hue key removal touches stored client state |

## STRIDE Threat Register

| Threat ID  | Category  | Component                                                  | Disposition | Mitigation Plan                                                                                                    |
| ---------- | --------- | ---------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------ |
| T-77-07-01 | Tampering | stale id.hue / retired id.dir values from old tabs         | mitigate    | Coercion (77-04) is permanent; write-back + removeItem are try-guarded; storage-event listener whitelists 'linear' |
| T-77-07-02 | DoS       | accidental engagement-domain corruption via name collision | mitigate    | Enumerated-file work only + the Task-2 grep gate explicitly whitelists engagement-domain 'ministerial' hits        |
| T-77-07-SC | Tampering | package installs                                           | accept      | No package-manager installs (only removals of unused font imports); slopcheck N/A                                  |

</threat_model>

<verification>
- Guard both polarities; full vitest + type-check + lint green; size-limit green; playwright focused-primitive spec green.
- Reference-purge greps pass exactly as specified in Task 2.
</verification>

<success_criteria>

- TOKEN-04 fully closed (switcher gone from types.ts through UI); TOKEN-01 single-direction end-state; TOKEN-05 font pipeline pruned to the Linear+Tajawal set.
  </success_criteria>

<output>
Create `.planning/phases/77-linear-token-system/77-07-SUMMARY.md` when done. Record: deleted modules, the engagement-domain residual-hit list, the focused-primitive baseline replacement note.
</output>
