---
phase: 58-tier-c-design-token-suppression-full-clear
plan: 01
type: execute
wave: 1
surface: forms
depends_on: ['00']
files_modified:
  - frontend/src/components/advanced-search/AdaptiveFilters.tsx
  - frontend/src/components/advanced-search/DateRangeFilter.tsx
  - frontend/src/components/advanced-search/EnhancedSearchInput.tsx
  - frontend/src/components/duplicate-comparison/DuplicateComparison.tsx
  - frontend/src/components/duplicate-detection/DuplicateCandidateCard.tsx
  - frontend/src/components/duplicate-detection/DuplicateCandidatesList.tsx
  - frontend/src/components/empty-states/FilterPresetsSection.tsx
  - frontend/src/components/empty-states/IntakeRoleEmptyState.tsx
  - frontend/src/components/empty-states/IntelligentSearchSuggestions.tsx
  - frontend/src/components/empty-states/NotificationPreviewTimeline.tsx
  - frontend/src/components/form-auto-save/AutoSaveIndicator.tsx
  - frontend/src/components/form-auto-save/FormProgressIndicator.tsx
  - frontend/src/components/forms/FormCheckboxAceternity.tsx
  - frontend/src/components/forms/FormInput.tsx
  - frontend/src/components/forms/FormSelect.tsx
  - frontend/src/components/forms/SearchableSelect.tsx
  - frontend/src/components/validation/validation-badge.tsx
  - frontend/tests/unit/FormInput.test.tsx
autonomous: true
requirements: [TOKEN-01, TOKEN-02]
requirements_addressed: [TOKEN-01, TOKEN-02]
tags: [tier-c, design-tokens, forms, suppression-clear, retroactive]
retroactive: true
provenance:
  initial_commit: '144bf880'
  initial_executor: 'external (Gemini)'
  closure_commit: 'TBD (this plan + Wave 1 fix commit)'
  closure_executor: 'gsd-orchestrator (claude-opus-4-7)'

must_haves:
  truths:
    - 'All 17 `Phase 51 Tier-C` lint suppressions across the 17 Wave-1 source files are removed (D-58-01)'
    - 'Every replaced Tailwind color literal resolves to a real `@theme`-mapped token defined in frontend/src/index.css (verified against the @theme block at lines 46-98)'
    - 'No new `no-restricted-syntax` suppressions are introduced anywhere in the Wave-1 file set'
    - 'pnpm lint exits with 0 errors / 0 warnings under --max-warnings 0'
    - 'pnpm type-check exits with 0 TypeScript diagnostics'
    - 'pnpm vitest run keeps the project at 1,241 passing tests (no regressions; FormInput.test.tsx assertions updated to match new semantic tokens)'
    - 'No emoji symbols remain in user-visible JSX text in DuplicateComparison.tsx (CLAUDE.md no-emoji rule)'
    - 'Notification category palette in NotificationPreviewTimeline.tsx preserves visual distinction between mentions/deadlines and calendar/workflow via opacity steps on shared token families'
    - 'Logical properties (ms-*, pe-*, start-*, end-*) preserved — RTL behavior unchanged from pre-Phase-58 baseline'
  artifacts:
    - path: 'frontend/src/components/forms/FormInput.tsx'
      provides: 'Token-bound input primitive — labels (text-ink), errors (text-danger), borders (border-danger/border-line), help (text-ink-mute), icon (text-ink-faint), focus ring (ring-primary)'
      contains: 'border-danger'
    - path: 'frontend/src/components/forms/FormSelect.tsx'
      provides: 'Same token vocabulary as FormInput, plus chevron (text-ink-faint)'
      contains: 'border-line'
    - path: 'frontend/src/components/empty-states/NotificationPreviewTimeline.tsx'
      provides: '8-category palette using info/success/warning/danger/accent + ink-mute, with /20 opacity step for mentions/workflow to retain distinction from deadlines/calendar'
      contains: 'bg-warning/20'
    - path: 'frontend/src/components/duplicate-comparison/DuplicateComparison.tsx'
      provides: 'Token-bound duplicate-merge surface; lucide icons (AlertTriangle, CheckCircle2) replace ⚠️/✓ emoji'
      contains: 'CheckCircle2'
    - path: 'frontend/tests/unit/FormInput.test.tsx'
      provides: 'Assertions against semantic tokens (border-danger, border-line) instead of literal Tailwind colors'
      contains: 'border-danger'
  key_links:
    - from: 'frontend/src/components/forms/FormInput.tsx'
      to: 'frontend/src/index.css#L46-L98'
      via: '@theme block maps every token used in FormInput (text-ink, text-danger, border-line, etc.) to a CSS custom property'
      pattern: '--color-ink: var(--ink)'
    - from: 'frontend/src/components/empty-states/NotificationPreviewTimeline.tsx'
      to: 'frontend/src/components/empty-states/NotificationPreviewTimeline.tsx#L66-L78'
      via: 'categoryColors map with inline rationale comment explaining the /20 opacity step rule'
      pattern: 'Opacity steps differentiate categories that share a token family'

deviations:
  - id: 'D-58-01-01'
    rule: 'CLAUDE.md: No raw hex. No Tailwind color literals like text-blue-500.'
    instance: 'frontend/src/components/duplicate-comparison/DuplicateComparison.tsx — modal backdrop uses `bg-black/50`'
    justification: 'Modal scrim is the conventional pattern across the codebase (alert-dialog.tsx uses bg-black/80; bottom-sheet.tsx uses bg-black/20; ExpandableDossierCard uses bg-black/40). The design system does not currently expose an --overlay token. Migrating this inline modal to the HeroUI/Radix Dialog primitive (which owns its own scrim) is the correct long-term fix but is out of Phase 58 scope (suppression clearance, not refactor).'
    follow_up: 'Future phase: migrate DuplicateComparison merge dialog to HeroUI Modal so the scrim is handled by the primitive, OR add --color-overlay token to the design system and update the bg-black/N call sites project-wide.'
  - id: 'D-58-01-02'
    rule: 'Visual distinction across all 8 notification categories'
    instance: 'NotificationPreviewTimeline.tsx — only 5 status colors (info/success/warning/danger + accent) and 1 neutral (ink-mute) for 8 categories'
    justification: 'Mapping mentions→warning/20 vs deadlines→warning/10, and workflow→success/20 vs calendar→success/10, preserves a visible distinction via opacity step. The 8 category icons (AtSign, Clock, GitBranch, Calendar, etc.) carry primary semantic load; color reinforces grouping.'
    follow_up: 'If the product team wants stronger category differentiation, expand the design system palette (e.g., add --color-teal or --color-purple tokens) in a dedicated token-expansion phase.'

verification:
  commands:
    - 'pnpm lint --max-warnings 0'
    - 'pnpm type-check'
    - 'pnpm vitest run'
  evidence_required:
    - 'lint exit code 0, zero errors, zero warnings'
    - 'type-check exit code 0'
    - 'vitest output shows 1,241 passing tests (and 25 todo) — no regressions'
    - '0 grep hits for `Phase 51 Tier-C` in any of the 17 Wave-1 source files'
    - '0 grep hits for `bg-black bg-opacity-` (deprecated v3 syntax) in any Wave-1 file'
    - '0 grep hits for `text-red-500`, `text-blue-500`, etc. raw Tailwind palette literals in any Wave-1 file (`bg-black/N` overlay scrim is permitted per D-58-01-01)'
---

<objective>
Clear all 17 Wave-1 (forms surface) `Phase 51 Tier-C` lint suppressions by rewriting Tailwind color literals to design-system tokens. Wave 1 covers form primitives (FormInput, FormSelect, FormCheckboxAceternity, SearchableSelect), form-auto-save indicators, advanced-search filters, duplicate-detection cards, the duplicate-comparison merge dialog, validation badges, and empty-state surfaces that share the form-component vocabulary.

This plan is **retroactive**. The initial token swap was executed externally (commit 144bf880, author: Gemini) without a corresponding GSD plan or summary. This document closes that gap and folds in three correctness fixes that the initial execution missed.

## Token mapping rules

Every replacement must resolve to a token defined in `frontend/src/index.css` @theme block (lines 46–98):

| Old literal pattern                           | New token                                                         | Notes                          |
| --------------------------------------------- | ----------------------------------------------------------------- | ------------------------------ |
| `text-gray-700 dark:text-gray-300` (labels)   | `text-ink`                                                        | Primary text                   |
| `text-gray-600 dark:text-gray-400` (help)     | `text-ink-mute`                                                   | Secondary text                 |
| `text-gray-400` / `text-gray-500` (faint)     | `text-ink-faint`                                                  | Tertiary/icon text             |
| `border-gray-300 dark:border-gray-600`        | `border-line`                                                     | Default border                 |
| `border-red-500`                              | `border-danger`                                                   | Error border                   |
| `text-red-500` / `text-red-600`               | `text-danger`                                                     | Error text + required-asterisk |
| `text-green-600`                              | `text-success`                                                    | Success states                 |
| `bg-red-50 dark:bg-red-900/20`                | `bg-danger/10`                                                    | Status banner background       |
| `border-red-200 dark:border-red-800`          | `border-danger/30`                                                | Status banner border           |
| `bg-blue-50 dark:bg-blue-900/20`              | `bg-primary/10`                                                   | Selected / info background     |
| `border-blue-500`                             | `border-primary`                                                  | Selected outline               |
| `bg-blue-600 hover:bg-blue-700`               | `bg-primary hover:bg-primary/90` (+ `text-primary-foreground`)    | Primary button                 |
| `dark:bg-gray-700 dark:text-white` (field bg) | `bg-background text-ink` (light) / `bg-surface text-ink` (raised) | Field background               |
| `hover:bg-gray-100 dark:hover:bg-gray-800`    | `hover:bg-muted`                                                  | Hover surface                  |
| `focus:ring-primary-500`                      | `focus:ring-primary`                                              | Focus ring                     |

## Notification category palette (NotificationPreviewTimeline.tsx)

8 categories must remain visually distinguishable. With only 5 status colors + accent + ink-mute available, opacity steps differentiate categories sharing a token family:

| Category    | Token assignment             | Distinguished from             |
| ----------- | ---------------------------- | ------------------------------ |
| assignments | `text-info bg-info/10`       | unique                         |
| intake      | `text-accent bg-accent/10`   | unique (brand)                 |
| calendar    | `text-success bg-success/10` | (paired with workflow at /20)  |
| signals     | `text-danger bg-danger/10`   | unique                         |
| mentions    | `text-warning bg-warning/20` | denser warning vs deadlines/10 |
| deadlines   | `text-warning bg-warning/10` | base warning vs mentions/20    |
| system      | `text-ink-mute bg-muted`     | unique (neutral)               |
| workflow    | `text-success bg-success/20` | denser success vs calendar/10  |

## Fixes folded into this plan (versus the bare 144bf880 execution)

1. **`text-accent bg-primary/10` → `text-accent bg-accent/10`** in NotificationPreviewTimeline (`intake` category): the prior mapping used both `accent` and `primary` token names interchangeably even though they resolve to the same CSS variable. Normalize to one name for readability.
2. **mentions/deadlines color collapse** restored via the /20 opacity step.
3. **calendar/workflow color collapse** restored via the /20 opacity step.
4. **Emoji `⚠️` and `✓` in DuplicateComparison.tsx** replaced with `AlertTriangle` and `CheckCircle2` lucide-react icons per CLAUDE.md no-emoji-in-copy rule.
5. **`bg-black bg-opacity-50` (deprecated Tailwind v3 syntax)** modernized to `bg-black/50` matching the established overlay convention used by `alert-dialog.tsx`, `bottom-sheet.tsx`, `ExpandableDossierCard.tsx`, etc. The literal is documented as deviation D-58-01-01 with a follow-up to migrate to a primitive Dialog.
   </objective>

<tasks>

## Task 1 — Token swap, 17 source files + 1 test (initial, COMPLETE)

Executed externally as commit 144bf880. Rewrites every Tailwind color literal in the 17 Wave-1 source files to the corresponding design-system token per the mapping table above. Test file updated to assert against semantic tokens.

**Status:** complete (provenance: 144bf880)

## Task 2 — NotificationPreviewTimeline category palette correction

In `frontend/src/components/empty-states/NotificationPreviewTimeline.tsx`, replace the `categoryColors` map to use opacity steps on shared token families per the palette table above. Add inline rationale comment explaining the /20 opacity rule.

**Status:** complete (this plan's closure commit)

## Task 3 — DuplicateComparison icon + overlay fixes

In `frontend/src/components/duplicate-comparison/DuplicateComparison.tsx`:

- Add `import { AlertTriangle, CheckCircle2 } from 'lucide-react'`.
- Replace `<div className="mb-4 text-6xl">✓</div>` with `<CheckCircle2 className="mx-auto mb-4 h-12 w-12 text-success" aria-hidden="true" />`.
- Replace `<span className="text-xl text-danger">⚠️</span>` with `<AlertTriangle className="h-5 w-5 text-danger" aria-hidden="true" />`.
- Modernize `bg-black bg-opacity-50` to `bg-black/50` (matches existing overlay convention).

**Status:** complete (this plan's closure commit)

## Task 4 — Verification

Run `pnpm lint`, `pnpm type-check`, and `pnpm vitest run`. All must exit clean. Confirm no `Phase 51 Tier-C` suppressions remain in any Wave-1 file via grep.

**Status:** complete — lint 0/0, type-check 0 diagnostics, 1241 tests passing, 0 suppressions remaining.

</tasks>
