---
phase: 60-schema-type-truth-restoration
plan: 06
type: execute
wave: 3
depends_on: [60-05]
files_modified:
  - scripts/check-edge-fn-schema-refs.mjs
  - scripts/edge-fn-schema-refs-allowlist.json
  - tools/edge-fn-fixtures/bad-schema-ref/index.ts
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [P1]
must_haves:
  truths:
    - "A Node script parses every supabase/functions/**/index.ts for .from('X') / .rpc('Y') literals and asserts each X is a Tables/Views key and each Y is a Functions key in frontend/src/types/database.types.ts"
    - 'The script exits non-zero when an unknown reference appears and exits 0 against the real (post-60-05) tree, modulo a documented allowlist'
    - 'The check runs in CI as a step inside the existing lint job — it does NOT add a new required status context on protected main'
  artifacts:
    - path: 'scripts/check-edge-fn-schema-refs.mjs'
      provides: 'Edge-fn schema-reference smoke test (regex parse + types membership assertion)'
      contains: 'database.types.ts'
    - path: 'scripts/edge-fn-schema-refs-allowlist.json'
      provides: 'Documented allowlist of legitimately-dynamic or known-out-of-scope names'
      contains: '{'
  key_links:
    - from: '.github/workflows/ci.yml'
      to: 'scripts/check-edge-fn-schema-refs.mjs'
      via: 'a step inside the existing lint job'
      pattern: 'check-edge-fn-schema-refs'
---

<objective>
Add a CI smoke test that prevents the schema/type drift this phase just closed from silently recurring: every table/view an edge function reads via `.from('X')` and every RPC it calls via `.rpc('Y')` must exist in the generated `database.types.ts` (regenerated in 60-05). Wire it into the existing CI lint job (not a new required context) so it gates PRs without changing branch-protection required checks.

Purpose: Standing guard against the drift class [P1 CI smoke test].
Output: A dependency-free Node script + documented allowlist, wired into CI, proven to fail on a planted bad reference and pass on the real tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-schema-type-truth-restoration/60-CONTEXT.md
@.planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md
@.planning/phases/60-schema-type-truth-restoration/60-PATTERNS.md
@.planning/phases/60-schema-type-truth-restoration/60-05-SUMMARY.md
@.github/workflows/ci.yml

<process_rules>
LOCKED process rules (CONTEXT.md "Process rules"):

- This plan runs AFTER 60-05 regen — the script asserts against the regenerated types, so it must not run before regen lands (else legitimate post-phase names would still be missing).
- After every commit run `pnpm --filter intake-frontend build` directly.
- `pnpm exec`, never `npx`. NO `timeout` command on this Mac.
- Commit convention: `feat(60-06): ...` / `ci(60-06): ...`, no AI attribution.
  </process_rules>

<interfaces>
<!-- Verified facts for building the smoke test (RESEARCH "CI smoke test" + PATTERNS "CI smoke test"). -->
- 301-302 edge functions under supabase/functions/*/index.ts. Real literal usage examples:
    .from('escalation_events'), .from('organizational_units'), .from('staff_profiles'),
    .from('position_delegations'), .from('word_assistant_logs'), .from('positions'), .from('profiles')
  Note: .from('profiles') is a likely alias/view — exactly the kind of entry the allowlist documents if it is not a Tables/Views key.
- Verified regex (PATTERNS): /\.(from|rpc)\(\s*['"]([a-zA-Z_]\w*)['"]/g — group 1 = from|rpc, group 2 = the name. 344 distinct .from() names live.
- Generated types structure: frontend/src/types/database.types.ts has export type Database = { public: { Tables: { <name>: {...} }, Views: { <name>: {...} }, Functions: { <name>: {...} } } }. Top-level keys appear as 6-space-indented `      <name>: {`. RESEARCH + PATTERNS: slice the file between the Tables: { / Views: { / Functions: { markers and their matching close, then test for ^      <name>: { (dependency-free, sufficient — no ts-morph).
- Root lint entry: package.json "lint": "node scripts/lint.mjs" (the lint CI job runs pnpm run lint). scripts/ already holds .mjs tools.
- Analog script: frontend/scripts/assert-size-limit-matches.mjs — Node ESM, fs-walk + regex + process.exit(1); mirror its recursive walkFiles + fail-collect+exit + script-relative path resolution.
- CI: .github/workflows/ci.yml `lint` job (lines ~44-64) runs on every PR to main/develop. Add the smoke test as a STEP there (after "Run linting") — do NOT add a new top-level job (avoids a new required-context on protected main per RESEARCH CI-wiring guidance). Positive-failure idiom already in repo: design-token-check (lines ~85-90) and i18next-factory-check (lines ~111-116) use `! pnpm exec ...` bash negation against a fixture.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the edge-fn schema-reference smoke-test script + allowlist</name>
  <files>scripts/check-edge-fn-schema-refs.mjs, scripts/edge-fn-schema-refs-allowlist.json</files>
  <read_first>
    - frontend/src/types/database.types.ts (open the public: { Tables / Views / Functions } blocks to confirm the exact shape the parser must read — note the 6-space-indented `<name>: {` key format)
    - frontend/scripts/assert-size-limit-matches.mjs (the structural analog — copy its recursive fs walk, fail-collect + process.exit(1), and script-relative path resolution)
    - scripts/lint.mjs (mirror the repo's .mjs script conventions: shebang/exit-code style, file reads, reporting)
    - supabase/functions/escalations-report/index.ts (a real edge fn to validate the .from/.rpc literal regex against)
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md ("CI smoke test" section — the exact assertion: X in Tables|Views, Y in Functions; allowlist for dynamic names)
    - .planning/phases/60-schema-type-truth-restoration/60-PATTERNS.md ("CI smoke test" section — the verified regex + the block-slice membership approach)
  </read_first>
  <action>
    Create scripts/check-edge-fn-schema-refs.mjs (Node ESM, zero runtime deps — only node: builtins). Behavior:
    (1) Read frontend/src/types/database.types.ts as text. Extract the set of valid table/view names (top-level keys under public.Tables and public.Views) and the set of valid function names (keys under public.Functions). Use the block-slice approach: locate the Tables: { / Views: { / Functions: { markers within the public schema object, slice to the matching close, and collect the first-level `      <identifier>: {` keys inside each (6-space indent — NOT nested column names).
    (2) Recursively glob supabase/functions/**/index.ts (and *.ts helpers in those dirs if a fn splits files). For each, regex-match with /\.(from|rpc)\(\s*['"]([a-zA-Z_]\w*)['"]/g (single AND double quotes). Collect each reference with its file path, kind (from|rpc), and the literal.
    (3) For each from literal, assert the name is in the valid tables-or-views set. For each rpc literal, assert the name is in the valid functions set. Skip any name present in the allowlist. Ignore dynamic .from(`...`) / .from(varName) references (template-literal/variable — cannot be statically checked; count them and print a summary note, do NOT fail on them).
    (4) On any unknown reference, print a clear report (file path, literal, kind table/view vs function) and process.exit(1). On all-clear, print a one-line success summary (N files scanned, M from/rpc literals checked, K dynamic skipped) and process.exit(0).
    Create scripts/edge-fn-schema-refs-allowlist.json: a JSON object { "tables": [], "functions": [] }; each entry documented (object form like { "name": "profiles", "reason": "view alias / out-of-scope — backlog ref" }) so future agents know why each is exempt. Start EMPTY; add only what the real-tree run (Task 2) forces, each with a reason.
    Read the types path and functions glob root from constants at the top of the script so it is easy to retarget (Task 2 points it at a fixture dir for the positive-failure test).
  </action>
  <verify>
    <automated>node scripts/check-edge-fn-schema-refs.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/check-edge-fn-schema-refs.mjs exists, is Node ESM, and imports no runtime dependencies (only node: builtins such as node:fs / node:path).
    - Running node scripts/check-edge-fn-schema-refs.mjs prints a summary line with counts of files scanned and literals checked.
    - The script reads valid names from frontend/src/types/database.types.ts (grep "database.types.ts" in the script returns >= 1).
    - The script matches BOTH single- and double-quoted .from/.rpc literals (the regex covers ['"]).
    - scripts/edge-fn-schema-refs-allowlist.json exists and is valid JSON with tables and functions arrays; every entry (if any) carries a documented reason.
    - pnpm --filter intake-frontend build exits 0.
  </acceptance_criteria>
  <done>Script + allowlist committed (feat(60-06): edge-fn schema-reference CI smoke test + allowlist); build green.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the script fails on a planted bad reference and passes on the real tree; finalize allowlist</name>
  <files>scripts/check-edge-fn-schema-refs.mjs, scripts/edge-fn-schema-refs-allowlist.json, tools/edge-fn-fixtures/bad-schema-ref/index.ts</files>
  <read_first>
    - scripts/check-edge-fn-schema-refs.mjs (the script from Task 1)
    - scripts/edge-fn-schema-refs-allowlist.json (the allowlist to finalize)
    - .github/workflows/ci.yml (the existing positive-failure jobs design-token-check / i18next-factory-check — mirror their isolated-fixture + bash-negation idiom)
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md ("Validation Architecture": "Smoke test works" row — seeded fake .rpc('nonexistent_fn') makes the script exit non-zero; real tree exits 0; allowlist should be EMPTY or document each entry)
  </read_first>
  <action>
    Positive-failure proof: create a fixture dir OUTSIDE supabase/functions (e.g. tools/edge-fn-fixtures/bad-schema-ref/index.ts) containing `await supabase.rpc('nonexistent_fn_xyz').select()` and `await supabase.from('nonexistent_table_xyz')`. Run the script pointed at that fixture dir (via the retargetable glob-root constant or a CLI arg) and confirm it exits NON-ZERO and names both planted references. This fixture lives OUTSIDE the real scanned tree so it never breaks normal CI; the positive-failure CI step (Task 3) invokes it explicitly with bash negation, mirroring the repo's design-token-check pattern.
    Real-tree proof: run node scripts/check-edge-fn-schema-refs.mjs against the actual supabase/functions tree (post-60-05 regenerated types) and confirm exit 0. If it reports any unknown reference, decide per-name: if the name is a legitimate live object missing from types → that is a real drift to escalate (note it; should NOT happen after 60-05 unless out of scope) ; if it is a dynamic/alias/known-out-of-scope name (e.g. 'profiles' if it is a view alias not in Views) → add it to the allowlist WITH a documented reason. Re-run until exit 0. The allowlist should end EMPTY or with every entry carrying a backlog/reason note (RESEARCH expectation).
    Record in the SUMMARY: the planted-failure output, the final real-tree exit-0 output, and the final allowlist contents with reasons.
  </action>
  <verify>
    <automated>node scripts/check-edge-fn-schema-refs.mjs; echo "real-tree exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - A planted .rpc('nonexistent_fn_xyz') / .from('nonexistent_table_xyz') reference (in the isolated fixture dir) makes the script exit NON-ZERO and the output names both planted references (captured in SUMMARY).
    - The fixture lives OUTSIDE supabase/functions; node scripts/check-edge-fn-schema-refs.mjs against the real committed tree exits 0.
    - Final scripts/edge-fn-schema-refs-allowlist.json is valid JSON; every entry (if any) has a reason; the SUMMARY lists them.
  </acceptance_criteria>
  <done>Positive-failure and real-tree-pass both demonstrated and recorded; allowlist finalized.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the smoke test + its positive-failure check into the existing CI lint job (no new required context)</name>
  <files>package.json, .github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the lint job, lines ~44-64 — add steps after "Run linting"; the design-token-check lines ~85-90 + i18next-factory-check lines ~111-116 for the `! ...` positive-failure idiom; do NOT create a new top-level job)
    - package.json (the scripts block — add a script alias so CI and local both call the same entry; mirror existing style: double-quoted keys/values, no trailing semicolons per repo prettier config)
    - scripts/check-edge-fn-schema-refs.mjs + the fixture dir from Task 2
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md ("CI smoke test": wire into an existing lint/typecheck job to avoid new required-context churn on protected main)
  </read_first>
  <action>
    (A) Add a package.json script: "check:edge-fn-schema": "node scripts/check-edge-fn-schema-refs.mjs" in the root package.json scripts block.
    (B) In .github/workflows/ci.yml, inside the EXISTING lint job (the one that already runs pnpm run lint), add a step AFTER "Run linting":
        - name: Check edge-fn schema references
          run: pnpm run check:edge-fn-schema
      AND a positive-failure step that asserts the script fails on the planted fixture, mirroring design-token-check:
        - name: Assert edge-fn schema check fails on bad fixture (positive-failure)
          shell: bash
          run: |
            set -e
            ! node scripts/check-edge-fn-schema-refs.mjs <fixture-glob-root or CLI arg pointing at tools/edge-fn-fixtures/bad-schema-ref>
      Do NOT add a new top-level job and do NOT register a new branch-protection context — folding into the existing lint job means the existing required Lint context now also enforces the smoke test. Keep YAML indentation consistent with the surrounding steps.
    Confirm both steps sit inside the lint job's steps list (not a sibling job) and that pnpm run check:edge-fn-schema executes locally with exit 0, and the negated fixture invocation exits non-zero (so the `!` step passes).
  </action>
  <verify>
    <automated>pnpm run check:edge-fn-schema; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - package.json scripts block contains "check:edge-fn-schema": "node scripts/check-edge-fn-schema-refs.mjs" (grep "check:edge-fn-schema" package.json returns >= 1).
    - .github/workflows/ci.yml contains a step "Check edge-fn schema references" running pnpm run check:edge-fn-schema, nested under the existing lint job's steps (grep "check:edge-fn-schema" ci.yml returns >= 1).
    - ci.yml also contains the positive-failure step using `! node scripts/check-edge-fn-schema-refs.mjs <fixture>` (bash negation against the isolated bad fixture).
    - No NEW top-level job key was added under jobs: (the new content is steps inside the existing lint job).
    - pnpm run check:edge-fn-schema exits 0 locally against the real tree.
    - pnpm --filter intake-frontend build exits 0.
  </acceptance_criteria>
  <done>Smoke test + positive-failure step wired into the lint job; local pnpm run check:edge-fn-schema exits 0; committed (ci(60-06): wire edge-fn schema-reference check into lint job); build green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                                                          |
| ------------------------- | ------------------------------------------------------------------------------------ |
| CI workflow → repo source | The smoke test reads repo source files only; no external/untrusted input, no secrets |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                  | Disposition | Mitigation Plan                                                                                                                                                                               |
| ---------- | ---------------------- | ---------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-60-06-T  | Tampering              | allowlist used to silence real drift                       | mitigate    | Every allowlist entry must carry a documented reason; RESEARCH sets the expectation that the list is EMPTY or fully justified. Real-missing-object references are escalated, not allowlisted. |
| T-60-06-D  | Denial of Service      | a committed bad fixture permanently reds CI                | mitigate    | The bad fixture lives OUTSIDE supabase/functions and is only invoked by the dedicated negated positive-failure step; the normal check scans only the real tree (exits 0).                     |
| T-60-06-I  | Information Disclosure | CI script leaks secrets / executes untrusted input         | mitigate    | Script uses only node: builtins, reads committed source files, takes no network/env input, and runs no shell from file contents (planning-context CI security note).                          |
| T-60-06-S  | Spoofing               | new required CI context blocks protected main unexpectedly | mitigate    | Folded into the existing lint job — no new branch-protection context is created (RESEARCH guidance), avoiding churn on protected main.                                                        |
| T-60-06-SC | Tampering              | npm/pip/cargo installs                                     | accept      | Script uses only Node builtins; no package installs.                                                                                                                                          |

</threat_model>

<verification>
- node scripts/check-edge-fn-schema-refs.mjs exits 0 on the real tree; exits non-zero on the planted bad fixture.
- check:edge-fn-schema script wired into package.json and into the existing ci.yml lint job (no new job/required-context) + a positive-failure step.
- pnpm --filter intake-frontend build green.
</verification>

<success_criteria>

- The drift class (edge-fn references an object absent from generated types) is caught in CI going forward.
- The check rides the existing required Lint context — no new protected-main required status.
  </success_criteria>

<output>
Create `.planning/phases/60-schema-type-truth-restoration/60-06-SUMMARY.md` when done, recording the planted-failure output, real-tree exit-0 output, and final allowlist contents with reasons.
</output>
