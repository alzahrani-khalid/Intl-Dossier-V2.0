---
phase: 60-schema-type-truth-restoration
plan: 05
type: execute
wave: 2
depends_on: [60-01, 60-02, 60-03, 60-04]
files_modified:
  - frontend/src/types/database.types.ts
  - backend/src/types/database.types.ts
autonomous: true
requirements: [P1]
must_haves:
  truths:
    - 'Regenerated database.types.ts (frontend + backend, byte-identical) contains the SLA dashboard RPCs, event_details view, and the 3 newly-created tables that plans 60-01..04 brought live'
    - 'The dead doubled-path backend/backend/src/types/database.types.ts is removed (confirmed unreferenced)'
    - 'Both frontend and backend type-check AND build pass after the type swap'
  artifacts:
    - path: 'frontend/src/types/database.types.ts'
      provides: 'Regenerated types matching live staging after 60-01..04'
      contains: 'get_sla_dashboard_overview'
    - path: 'backend/src/types/database.types.ts'
      provides: 'Byte-identical copy of the regenerated frontend types (Phase 54 precedent)'
      contains: 'get_sla_dashboard_overview'
  key_links:
    - from: 'frontend/src/types/database.types.ts'
      to: 'live staging catalog'
      via: 'MCP generate_typescript_types (project zkrcjzdemdmwhearhfgg)'
      pattern: 'event_details'
    - from: 'backend/src/types/database.types.ts'
      to: 'frontend/src/types/database.types.ts'
      via: 'byte-identical copy (cmp silent)'
      pattern: 'Database'
---

<objective>
With all schema objects now live (plans 60-01..04 brought the unified work stack, SLA dashboard RPCs, event_details view, and the three ungenerated tables live), make the compile-time truth agree with live: regenerate the TypeScript types from staging, write them byte-identical to the two real copies, delete the confirmed-dead doubled-path copy, and prove both workspaces still type-check and build.

This is the SEQUENCING PIVOT of the phase: types regeneration MUST happen AFTER every live-schema mutation (60-01..04), otherwise the regenerated types would not reflect this phase's schema fixes and verification would be a false positive.

Purpose: Close the compile-time-truth gap [P1 type regeneration] so generated types == live staging across both real copies.
Output: Regenerated types in two locations byte-identical; dead doubled-path artifact removed; both workspaces green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-schema-type-truth-restoration/60-CONTEXT.md
@.planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md
@.planning/phases/60-schema-type-truth-restoration/60-PATTERNS.md
@.planning/phases/60-schema-type-truth-restoration/60-01-SUMMARY.md
@.planning/phases/60-schema-type-truth-restoration/60-02-SUMMARY.md
@.planning/phases/60-schema-type-truth-restoration/60-03-SUMMARY.md
@.planning/phases/60-schema-type-truth-restoration/60-04-SUMMARY.md

<process_rules>
LOCKED process rules (CONTEXT.md "Process rules"):

- Types regenerated via Supabase MCP `generate_typescript_types` (project_id `zkrcjzdemdmwhearhfgg`). Phase 54 precedent: the two real copies must be byte-identical.
- `as`-casts mean drift compiles silently — run BOTH build AND type-check on frontend + backend after the type swap.
- After every commit run `pnpm --filter intake-frontend build` directly.
- `pnpm exec`, never `npx`. NO `timeout` command on this Mac.
- Commit convention: `chore(60-05): ...` / `fix(60-05): ...`, no AI attribution. Do NOT re-fix bucket-A items.
  </process_rules>

<interfaces>
<!-- Verified facts (RESEARCH "Types regeneration" + PATTERNS "Type regeneration") -->
- The two LIVE copies (frontend/src/types/database.types.ts, backend/src/types/database.types.ts) are CURRENTLY byte-identical (39567 lines each). Keep them byte-identical after regen (cmp must be silent).
- DEAD doubled-path copy: backend/backend/src/types/database.types.ts is 1037 lines (vs 39567), has a leaked Supabase-CLI log header, references stale calendar_events, and NOTHING imports it (zero backend/backend references outside node_modules/worktrees). Phase 54 commit ef88cc31 wrote only TWO copies. DELETE it; do not regenerate into it.
- Frontend type-check script: pnpm --filter intake-frontend type-check (tsc --noEmit). Backend: pnpm --filter intake-backend type-check (tsc --noEmit).
- Newly-live objects (from 60-01..04) that MUST now appear in regenerated types: get_sla_dashboard_overview (+ the other 3 SLA dashboard RPCs), event_details view, pending_role_approvals, position_delegations, word_assistant_logs. (unified_work_items + its RPCs were already typed; confirm they remain.)
- File structure: top-level export type Database = { ... public: { Tables: {...} Views: {...} Functions: {...} Enums: {...} CompositeTypes: {...} } }. Do NOT hand-edit; write the raw MCP output.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate types from staging (twice for determinism), write byte-identical to both copies, remove dead doubled-path (BLOCKING regen-after-mutations)</name>
  <read_first>
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md ("Types regeneration" section + matrix "Generated-types stale entries today")
    - .planning/phases/60-schema-type-truth-restoration/60-PATTERNS.md ("Type regeneration" section — byte-identical copy + determinism-check pattern + the DEAD doubled-path warning)
    - backend/tsconfig.json (confirm rootDir ./src + include src/**/* — proves backend/backend is outside the build)
    - .planning/phases/60-schema-type-truth-restoration/60-01-SUMMARY.md, 60-02-SUMMARY.md, 60-03-SUMMARY.md, 60-04-SUMMARY.md (confirm ALL upstream objects are live before regenerating — this is the gate that makes regen reflect the phase's fixes)
  </read_first>
  <action>
    PRECONDITION: Confirm from the four upstream SUMMARYs (and, if any doubt, a quick MCP execute_sql existence probe) that the SLA dashboard RPCs, event_details view, and the three tables are all live. Do NOT regenerate before this is true (RESEARCH critical ordering — regen before mutations is a false positive).
    Generate TypeScript types from staging via Supabase MCP generate_typescript_types (project_id zkrcjzdemdmwhearhfgg). Run it TWICE and confirm the two outputs are identical (determinism check) before committing. Write the EXACT output to BOTH frontend/src/types/database.types.ts AND backend/src/types/database.types.ts — byte-identical (Phase 54 precedent). Do not hand-edit the generated text.
    Then handle the doubled-path artifact: confirm backend/backend/src/types/database.types.ts is dead by grepping for "backend/backend" across backend/src and frontend/src (expect no import) and confirming backend/tsconfig.json rootDir ./src + include src/**/* (the doubled path is outside it). Given it is unreferenced and stale, DELETE backend/backend/src/types/database.types.ts; if the backend/backend/src/types, backend/backend/src, and backend/backend dirs become empty, remove those empty dirs too. Record the dead-path evidence (grep output + tsconfig facts) in the SUMMARY.
    Verify the regenerated types contain the newly-live objects: grep for get_sla_dashboard_overview, get_sla_compliance_by_assignee, event_details, pending_role_approvals, position_delegations, word_assistant_logs (each must appear). Commit chore(60-05): regenerate database.types from staging + remove dead doubled-path copy, run pnpm --filter intake-frontend build.
  </action>
  <verify>
    <automated>cmp -s frontend/src/types/database.types.ts backend/src/types/database.types.ts && grep -c "get_sla_dashboard_overview" frontend/src/types/database.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - cmp -s frontend/src/types/database.types.ts backend/src/types/database.types.ts is silent and exits 0 (byte-identical).
    - grep -c "get_sla_dashboard_overview" frontend/src/types/database.types.ts returns >= 1.
    - Each of event_details, pending_role_approvals, position_delegations, word_assistant_logs appears in frontend/src/types/database.types.ts (grep >= 1 each).
    - unified_work_items and get_unified_work_items still appear (grep >= 1 each) — regen did not drop the already-typed objects.
    - backend/backend/src/types/database.types.ts no longer exists (test ! -f returns true).
    - Two consecutive generate_typescript_types runs produced identical output (recorded in SUMMARY).
    - pnpm --filter intake-frontend build exits 0.
  </acceptance_criteria>
  <done>Types regenerated (deterministic) to both copies byte-identical; dead path deleted; newly-live objects present; frontend builds.</done>
</task>

<task type="auto">
  <name>Task 2: Type-check + build both workspaces; fix any type fallout surgically (BLOCKING dual gate)</name>
  <read_first>
    - frontend/src/types/database.types.ts (the regenerated file from Task 1)
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md ("Types regeneration": run build AND tsc because as-casts let drift compile silently)
    - backend/package.json + frontend/package.json (confirm the type-check script names: type-check = tsc --noEmit in each)
  </read_first>
  <action>
    [BLOCKING] Validate the regenerated types did not break either workspace. Because as-casts mean drift compiles silently, run BOTH type-check and build:
    - pnpm --filter intake-frontend type-check — must exit 0.
    - pnpm --filter intake-backend type-check — must exit 0.
    - pnpm --filter intake-frontend build — must exit 0 (built).
    If the regenerated types surface compile errors (e.g. a renamed/removed column the app referenced via the old types, or a now-present table changing an inferred shape), fix the affected call sites MINIMALLY (surgical — touch only what the type change broke; match existing style). Re-run type-check and re-build after each fix. Record each fixed call site in the SUMMARY with its file path; if no call-site fixes were needed, state that explicitly. If a fix touches a file beyond the two type files, note it in the SUMMARY for traceability (the executor may add it to files_modified in the SUMMARY).
    Commit fix(60-05): type-check fallout from regenerated types (or, if no fallout, fold into the Task 1 commit and note "no fallout" in the SUMMARY).
  </action>
  <verify>
    <automated>pnpm --filter intake-frontend type-check && pnpm --filter intake-backend type-check</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --filter intake-frontend type-check exits 0.
    - pnpm --filter intake-backend type-check exits 0.
    - pnpm --filter intake-frontend build exits 0 (built).
    - Any call-site fixes are surgical and listed in the SUMMARY with file paths; if none were needed, that is stated explicitly.
  </acceptance_criteria>
  <done>Both workspaces type-check clean; frontend builds; fallout (if any) recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                            |
| ----------------------------- | ---------------------------------------------------------------------- |
| generated types → app compile | regenerated types define the compile-time contract for both workspaces |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                        | Disposition | Mitigation Plan                                                                                                                                                                                                          |
| ---------- | ---------------------- | ------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-60-05-T  | Tampering              | regenerated types hide a real drift via as-casts | mitigate    | Run BOTH build AND tsc type-check on frontend + backend; determinism check (generate twice, identical) ensures the types reflect live, not a transient. Regen is gated on all upstream SUMMARYs confirming objects live. |
| T-60-05-R  | Repudiation            | divergent type copies drift again silently       | mitigate    | cmp -s gate asserts the two real copies are byte-identical; the dead doubled-path copy is removed so it cannot be mistaken for a third source of truth.                                                                  |
| T-60-05-I  | Information Disclosure | generated types are no security surface          | accept      | Generated TypeScript types contain no secrets and grant no access; they are compile-time only (planning-context security note: regenerated types have no security surface).                                              |
| T-60-05-SC | Tampering              | npm/pip/cargo installs                           | accept      | No package installs in this plan (MCP regen + type-check/build only).                                                                                                                                                    |

</threat_model>

<verification>
- cmp confirms frontend/backend types byte-identical and contain all newly-live objects (SLA RPCs, event_details, 3 tables) plus the already-typed unified stack.
- backend/backend dead copy removed.
- Both workspaces type-check; frontend builds.
</verification>

<success_criteria>

- Generated types agree with live staging across both real copies; dead doubled-path gone.
- Regen happened strictly after all live-schema mutations (no false-positive verification).
  </success_criteria>

<output>
Create `.planning/phases/60-schema-type-truth-restoration/60-05-SUMMARY.md` when done, recording the determinism check, dead-path evidence, type-check/build results, and any call-site fixes.
</output>
