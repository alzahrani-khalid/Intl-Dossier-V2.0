---
phase: 60-schema-type-truth-restoration
plan: 05
type: execute
wave: 3
depends_on: [60-04]
files_modified:
  - scripts/check-edge-fn-schema-refs.mjs
  - scripts/edge-fn-schema-refs-allowlist.json
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [P1]
must_haves:
  truths:
    - "A Node script parses every supabase/functions/**/index.ts for .from('X') / .rpc('Y') literals and asserts each X is a Tables/Views key and each Y is a Functions key in frontend/src/types/database.types.ts"
    - "The script exits non-zero when an unknown reference appears and exits 0 against the real (post-60-04) tree, modulo a documented allowlist"
    - "The check runs in CI as a step inside the existing lint job — it does NOT add a new required status context on protected main"
  artifacts:
    - path: "scripts/check-edge-fn-schema-refs.mjs"
      provides: "Edge-fn schema-reference smoke test (regex/AST parse + types membership assertion)"
      contains: "database.types.ts"
    - path: "scripts/edge-fn-schema-refs-allowlist.json"
      provides: "Documented allowlist of legitimately-dynamic or known-out-of-scope names"
      contains: "{"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-edge-fn-schema-refs.mjs"
      via: "a step inside the existing lint job"
      pattern: "check-edge-fn-schema-refs"
---

<objective>
Add a CI smoke test that prevents the schema/type drift this phase just closed from silently recurring: every table/view an edge function reads via `.from('X')` and every RPC it calls via `.rpc('Y')` must exist in the generated `database.types.ts`. Wire it into the existing CI lint job (not a new required context) so it gates PRs without changing branch-protection required checks.

Purpose: Standing guard against the drift class [P1 CI smoke test].
Output: A dependency-free Node script + documented allowlist, wired into CI, proven to fail on a planted bad reference and pass on the real tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-schema-type-truth-restoration/60-CONTEXT.md
@.planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md
@.planning/phases/60-schema-type-truth-restoration/60-04-SUMMARY.md
@.github/workflows/ci.yml

<interfaces>
<!-- Verified facts for building the smoke test. -->
- 302 edge functions under supabase/functions/*/index.ts. Literal usage examples (real):
    .from('escalation_events'), .from('organizational_units'), .from('staff_profiles'),
    .from('position_delegations'), .from('word_assistant_logs'), .from('positions'), .from('profiles')
  Note: .from('profiles') is a likely alias/view — exactly the kind of entry the allowlist documents if it is not a Tables/Views key.
- Generated types structure: frontend/src/types/database.types.ts has a Database type with public: { Tables: { <name>: {...} }, Views: { <name>: {...} }, Functions: { <name>: {...} } }. RESEARCH recommends regex on `<name>: {` inside the Tables:/Views:/Functions: blocks (dependency-free, sufficient).
- Root lint entry: package.json "lint": "node scripts/lint.mjs" (the lint CI job runs `pnpm run lint`). scripts/ already holds .mjs tools (lint.mjs, fix-rls.mjs).
- CI: .github/workflows/ci.yml `lint` job (lines ~44-64) runs on every PR to main/develop. Add the smoke test as a STEP there (after "Run linting") — do NOT add a new top-level job (avoids a new required-context on protected main per RESEARCH CI-wiring guidance).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the edge-fn schema-reference smoke-test script + allowlist</name>
  <read_first>
    - frontend/src/types/database.types.ts (open the public: { Tables / Views / Functions } blocks to confirm the exact shape the parser must read — note how key names are formatted)
    - scripts/lint.mjs (mirror the repo's .mjs script conventions: shebang/exit-code style, how it reads files, how it reports)
    - supabase/functions/escalations-report/index.ts (a real edge fn to validate the .from/.rpc literal regex against)
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md ("CI smoke test" section — the exact assertion: X ∈ Tables|Views, Y ∈ Functions; allowlist for dynamic names)
  </read_first>
  <action>
    Create scripts/check-edge-fn-schema-refs.mjs (Node ESM, zero runtime deps). Behavior:
    (1) Read frontend/src/types/database.types.ts as text. Extract the set of valid table/view names (keys under public.Tables and public.Views) and the set of valid function names (keys under public.Functions). Use a robust regex approach: locate the `Tables:`, `Views:`, and `Functions:` block boundaries within the public schema object and collect the top-level `<identifier>: {` keys inside each. (Parse the Database['public']['Tables'] etc. region; collect the first-level keys only, not nested column names.)
    (2) Recursively glob supabase/functions/**/index.ts (and *.ts helpers in those dirs if the existing code splits files). For each, regex-match `.from('<name>')` / `.from("<name>")` and `.rpc('<name>')` / `.rpc("<name>")` (single AND double quotes). Collect each reference with its file path + the literal.
    (3) For each .from literal, assert the name is in the valid tables-or-views set. For each .rpc literal, assert the name is in the valid functions set. Skip any name present in the allowlist (see below). Ignore template-literal/variable .from(`...`) / .from(varName) references (dynamic — cannot be statically checked; count them and print a summary note, do not fail on them).
    (4) On any unknown reference, print a clear report (file path, literal, kind table/view vs function) and process.exit(1). On all-clear, print a one-line success summary (N files scanned, M from/rpc literals checked, K dynamic skipped) and process.exit(0).
    Create scripts/edge-fn-schema-refs-allowlist.json: a JSON object with two arrays { "tables": [], "functions": [] }; each entry must be documented (use an object form like { "name": "profiles", "reason": "view alias / out-of-scope — see backlog ref" } OR a parallel comment file) so future agents know why each entry is exempt. Seed it ONLY with names that are genuinely valid-but-unparseable or explicitly out-of-P1-scope after running the script once against the real tree (Task 2 finalizes this); start empty and add only what the real-tree run forces, each with a reason.
    The script must read the types path and functions glob from constants at the top so it is easy to retarget.
  </action>
  <verify>
    <automated>node scripts/check-edge-fn-schema-refs.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/check-edge-fn-schema-refs.mjs exists, is Node ESM, and imports no runtime dependencies (only node: builtins like node:fs / node:path).
    - Running node scripts/check-edge-fn-schema-refs.mjs prints a summary line with counts of files scanned and literals checked.
    - The script reads valid names from frontend/src/types/database.types.ts (grep 'database.types.ts' in the script returns ≥1).
    - The script matches BOTH single- and double-quoted .from/.rpc literals (the regex covers ['"]).
    - scripts/edge-fn-schema-refs-allowlist.json exists and is valid JSON with tables and functions arrays; every entry carries a documented reason.
    - Commit message feat(60-05): edge-fn schema-reference CI smoke test + allowlist.
  </acceptance_criteria>
  <done>Script + allowlist committed; build shows ✓ built.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the script fails on a planted bad reference and passes on the real tree; finalize allowlist</name>
  <read_first>
    - scripts/check-edge-fn-schema-refs.mjs (the script from Task 1)
    - scripts/edge-fn-schema-refs-allowlist.json (the allowlist to finalize)
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md ("Validation Architecture": "Smoke test works" row — seeded fake .rpc('nonexistent_fn') makes the script exit non-zero; real tree exits 0; allowlist should be EMPTY or document each entry)
  </read_first>
  <action>
    Positive-failure proof: create a temporary scratch fixture (e.g. tools/edge-fn-fixtures/bad-schema-ref/index.ts under supabase/functions OR point the script at a temp fixture dir) containing a line `await supabase.rpc('nonexistent_fn_xyz').select()` and `await supabase.from('nonexistent_table_xyz')`. Run the script and confirm it exits NON-ZERO and names both planted references. Then REMOVE the scratch fixture (it must NOT be committed into the scanned tree, or it will permanently fail CI) — OR, preferred, add a dedicated positive-failure invocation that points the script at a fixtures dir outside supabase/functions and asserts non-zero exit (mirroring the repo's existing positive-failure CI pattern in ci.yml for design-token/vi.mock fixtures). Choose the approach that leaves the committed real tree passing.
    Real-tree proof: run node scripts/check-edge-fn-schema-refs.mjs against the actual supabase/functions tree (post-60-04 regenerated types) and confirm exit 0. If it reports any unknown reference, decide per-name: if the name is a legitimate live object missing from types → that is a real drift to escalate (note it; should NOT happen after 60-04 unless out of scope); if it is a dynamic/alias/known-out-of-scope name (e.g. 'profiles' if it is a view alias not in Views) → add it to the allowlist WITH a documented reason. Re-run until exit 0. The allowlist should end EMPTY or with every entry carrying a backlog/reason note (RESEARCH expectation).
    Record in the SUMMARY: the planted-failure output, the final real-tree exit-0 output, and the final allowlist contents with reasons.
  </action>
  <verify>
    <automated>node scripts/check-edge-fn-schema-refs.mjs; echo "real-tree exit=$?"  # expect exit 0</automated>
  </verify>
  <acceptance_criteria>
    - A planted .rpc('nonexistent_fn_xyz') / .from('nonexistent_table_xyz') reference makes the script exit NON-ZERO and the output names both planted references (captured in SUMMARY).
    - After removing/isolating the planted fixture, node scripts/check-edge-fn-schema-refs.mjs against the real committed tree exits 0.
    - No scratch/bad fixture remains inside the scanned supabase/functions tree (the committed real tree passes).
    - Final scripts/edge-fn-schema-refs-allowlist.json is valid JSON; every entry (if any) has a reason; the SUMMARY lists them.
  </acceptance_criteria>
  <done>Positive-failure and real-tree-pass both demonstrated and recorded; allowlist finalized.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the smoke test into the existing CI lint job (no new required context)</name>
  <read_first>
    - .github/workflows/ci.yml (the lint job, lines ~44-64 — add a step after "Run linting"; do NOT create a new top-level job)
    - package.json (the scripts block — add a script alias so CI and local both call the same entry)
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md ("CI smoke test": wire into an existing lint/typecheck job to avoid new required-context churn on protected main)
  </read_first>
  <action>
    (A) Add a package.json script: "check:edge-fn-schema": "node scripts/check-edge-fn-schema-refs.mjs" in the root package.json scripts block (mirror existing script style: no trailing semicolons, double-quoted keys/values per repo prettier config).
    (B) In .github/workflows/ci.yml, inside the EXISTING `lint` job (the one that already runs `pnpm run lint`), add a new step AFTER the "Run linting" step:
        - name: Check edge-fn schema references
          run: pnpm run check:edge-fn-schema
      Do NOT add a new top-level job and do NOT register a new branch-protection context — folding it into the existing lint job means the existing required `Lint` context now also enforces the smoke test (same pattern the phase intends). Keep YAML indentation consistent with the surrounding steps.
    Confirm the step is inside the lint job's steps list (not a sibling job) and that the run command resolves (pnpm run check:edge-fn-schema executes locally).
  </action>
  <verify>
    <automated>pnpm run check:edge-fn-schema && grep -c 'check:edge-fn-schema' .github/workflows/ci.yml package.json</automated>
  </verify>
  <acceptance_criteria>
    - package.json scripts block contains "check:edge-fn-schema": "node scripts/check-edge-fn-schema-refs.mjs" (grep 'check:edge-fn-schema' package.json returns ≥1).
    - .github/workflows/ci.yml contains a step "Check edge-fn schema references" running pnpm run check:edge-fn-schema, nested under the existing lint job's steps (grep 'check:edge-fn-schema' ci.yml returns ≥1).
    - No NEW top-level job key was added to ci.yml (the job count under `jobs:` is unchanged except for steps inside `lint`); confirm by checking the step sits between the lint job's "Run linting" step and the next job boundary.
    - pnpm run check:edge-fn-schema exits 0 locally against the real tree.
    - Commit message ci(60-05): wire edge-fn schema-reference check into lint job.
  </acceptance_criteria>
  <done>Smoke test wired into the lint job; local pnpm run check:edge-fn-schema exits 0; build shows ✓ built.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow → repo source | The smoke test reads source files; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-05-01 | Tampering | allowlist used to silence real drift | mitigate | Every allowlist entry must carry a documented reason; RESEARCH sets the expectation that the list is EMPTY or fully justified. Real-missing-object references are escalated, not allowlisted. |
| T-60-05-02 | Denial of Service | a committed bad fixture permanently red-CI | mitigate | Task 2 requires the scratch fixture be removed/isolated so the committed real tree exits 0; positive-failure proof uses an isolated fixture path mirroring the existing ci.yml positive-failure pattern. |
| T-60-05-03 | Spoofing | new required CI context blocks protected-main unexpectedly | mitigate | Folded into the existing lint job — no new branch-protection context is created (RESEARCH guidance), avoiding churn on protected main. |
| T-60-05-SC | Tampering | npm/pip/cargo installs | accept | Script uses only Node builtins; no package installs. |
</threat_model>

<verification>
- node scripts/check-edge-fn-schema-refs.mjs exits 0 on the real tree; exits non-zero on a planted bad reference.
- check:edge-fn-schema script wired into package.json and into the existing ci.yml lint job (no new job/required-context).
- pnpm --filter intake-frontend build shows ✓ built.
</verification>

<success_criteria>
- The drift class (edge-fn references an object absent from generated types) is caught in CI going forward.
- The check rides the existing required Lint context — no new protected-main required status.
</success_criteria>

<output>
Create `.planning/phases/60-schema-type-truth-restoration/60-05-SUMMARY.md` when done, recording the planted-failure output, real-tree exit-0 output, and final allowlist contents with reasons.
</output>
