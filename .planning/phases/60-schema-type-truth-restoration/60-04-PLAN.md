---
phase: 60-schema-type-truth-restoration
plan: 04
type: execute
wave: 2
depends_on: [60-01, 60-02, 60-03]
files_modified:
  - frontend/src/types/database.types.ts
  - backend/src/types/database.types.ts
  - supabase/migrations/009_data_library.sql
  - supabase/functions/escalations-report/index.ts
autonomous: true
requirements: [P1]
must_haves:
  truths:
    - "Regenerated database.types.ts (frontend + backend, byte-identical) contains the SLA RPCs, event_details view, and the 3 newly-created tables that plans 60-01..03 brought live"
    - "The dead doubled-path backend/backend/src/types/database.types.ts is removed (confirmed unreferenced) and 009_data_library.sql carries a superseded header"
    - "escalations-report edge function references only live columns and is redeployed so the fix is active"
    - "Both frontend and backend type-check and build pass after the type swap"
  artifacts:
    - path: "frontend/src/types/database.types.ts"
      provides: "Regenerated types matching live staging after 60-01..03"
      contains: "get_sla_dashboard_overview"
    - path: "supabase/functions/escalations-report/index.ts"
      provides: "Edge fn fixed to reference real columns (unit via staff_profiles.unit_id, organizational_units.name_en/name_ar, users.full_name)"
      contains: "name_en"
  key_links:
    - from: "frontend/src/types/database.types.ts"
      to: "live staging catalog"
      via: "MCP generate_typescript_types"
      pattern: "event_details"
    - from: "supabase/functions/escalations-report/index.ts"
      to: "public.organizational_units / public.users / public.staff_profiles"
      via: "corrected select column lists + unit-via-staff_profiles.unit_id join"
      pattern: "name_en"
---

<objective>
With all schema objects now live (plans 60-01..03), regenerate the TypeScript types from staging and write them byte-identical to the two real copies; delete the confirmed-dead doubled-path copy; mark the dead `009_data_library.sql` superseded; and fix the `escalations-report` edge function's three absent-column references, redeploying so the fix is active.

Purpose: Make the compile-time truth (generated types) agree with live, and close the last absent-column bug [tq #5].
Output: Regenerated types in two locations, dead artifact removed, superseded marker, fixed+redeployed edge fn.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-schema-type-truth-restoration/60-CONTEXT.md
@.planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md
@.planning/phases/60-schema-type-truth-restoration/60-01-SUMMARY.md
@.planning/phases/60-schema-type-truth-restoration/60-02-SUMMARY.md
@.planning/phases/60-schema-type-truth-restoration/60-03-SUMMARY.md
@supabase/functions/escalations-report/index.ts

<interfaces>
<!-- Verified live facts for the escalations-report fix (RESEARCH + source read). -->
escalations-report current broken refs (supabase/functions/escalations-report/index.ts):
  - line ~121: assignments embedded select includes organizational_unit_id — assignments has NO organizational_unit_id (has assignee_id; unit lives on staff_profiles.unit_id).
  - line ~130/204: filter/read assignments.organizational_unit_id — must route unit via the assignee's staff_profiles.unit_id.
  - line ~213-214: .from('organizational_units').select('id, name') — organizational_units has name_en, name_ar (NO name).
  - line ~240-241: .from('staff_profiles').select('user_id, full_name, current_assignment_count') — staff_profiles has NO full_name (names on users.full_name via staff_profiles.user_id → users.id).

Dead-path facts:
  - backend/backend/src/types/database.types.ts is 33KB (stale), NOT in backend/tsconfig.json (rootDir ./src, include src/**/*), and grep finds ZERO imports → dead doubled-path artifact → delete.
  - The two LIVE copies (frontend/src/types/database.types.ts, backend/src/types/database.types.ts) are byte-identical at 1.3MB.

Supersession: 009_create_data_library_items.sql matches live; 009_data_library.sql (title_en/file_url/category schema) is NOT live → mark superseded via header comment (lowest blast radius, per RESEARCH open decision 2).

Deploy: edge-fn fixes are inert until redeploy — supabase functions deploy escalations-report --project-ref zkrcjzdemdmwhearhfgg (handoff rule 4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate types from staging, write byte-identical to both copies, remove dead doubled-path</name>
  <read_first>
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md ("Types regeneration" section + matrix "Generated-types stale entries today")
    - backend/tsconfig.json (confirm rootDir ./src + include src/**/* — proves backend/backend is outside the build)
    - .planning/phases/60-schema-type-truth-restoration/60-01-SUMMARY.md, 60-02-SUMMARY.md, 60-03-SUMMARY.md (confirm all upstream objects are live before regenerating)
  </read_first>
  <action>
    Generate TypeScript types from staging via Supabase MCP generate_typescript_types (project_id zkrcjzdemdmwhearhfgg). Write the EXACT output to BOTH frontend/src/types/database.types.ts AND backend/src/types/database.types.ts — they must be byte-identical (Phase 54 precedent). Run generate_typescript_types TWICE and confirm the two outputs are identical (determinism check) before committing.
    Then handle the doubled-path artifact: confirm backend/backend/src/types/database.types.ts is dead by running grep for "backend/backend" across backend/src and frontend/src (expect no import) and confirming backend/tsconfig.json rootDir is ./src with include src/**/* (the doubled path is outside it). Given it is unreferenced and stale (33KB vs 1.3MB), DELETE backend/backend/src/types/database.types.ts and, if the backend/backend/src/types, backend/backend/src, and backend/backend dirs become empty, remove those empty dirs too. Record the dead-path evidence in the SUMMARY.
    Verify the regenerated types contain the newly-live objects: grep for get_sla_dashboard_overview, get_sla_compliance_by_assignee, event_details, pending_role_approvals, position_delegations, word_assistant_logs.
  </action>
  <verify>
    <automated>diff -q frontend/src/types/database.types.ts backend/src/types/database.types.ts && grep -c 'get_sla_dashboard_overview' frontend/src/types/database.types.ts && grep -c 'event_details\|position_delegations\|word_assistant_logs' frontend/src/types/database.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - diff -q frontend/src/types/database.types.ts backend/src/types/database.types.ts reports identical (exit 0, no output).
    - grep -c 'get_sla_dashboard_overview' frontend/src/types/database.types.ts returns ≥1.
    - Each of event_details, pending_role_approvals, position_delegations, word_assistant_logs appears in frontend/src/types/database.types.ts (grep ≥1 each).
    - test ! -f backend/backend/src/types/database.types.ts (the dead doubled-path file is removed).
    - Two consecutive generate_typescript_types runs produced identical output (recorded in SUMMARY).
    - Commit message chore(60-04): regenerate database.types from staging + remove dead doubled-path copy.
  </acceptance_criteria>
  <done>Types regenerated to both copies byte-identical; dead path deleted; build (pnpm --filter intake-frontend build) shows ✓ built.</done>
</task>

<task type="auto">
  <name>Task 2: Mark 009_data_library.sql superseded and fix escalations-report edge fn columns</name>
  <read_first>
    - supabase/functions/escalations-report/index.ts (read the full query-building section lines ~105-270 — the three broken spots and how unitId/assigneeId flow)
    - supabase/migrations/009_data_library.sql (the dead migration — add a header comment; do NOT alter its body)
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md (escalations-report paragraph + open decision 3: this is an EDGE FUNCTION fix, redeploy required)
    - frontend/src/types/database.types.ts (regenerated in Task 1 — confirm the real column names organizational_units.name_en/name_ar, users.full_name, staff_profiles.unit_id before editing the edge fn)
  </read_first>
  <action>
    (A) Supersede the dead migration: prepend a header comment to supabase/migrations/009_data_library.sql reading: SUPERSEDED (data-library #1): this title_en/file_url/category schema was NEVER applied to staging. The LIVE data_library_items schema is created by 009_create_data_library_items.sql. Do NOT apply this file or "fix" the drift backwards. Kept for git history only. Do NOT modify the SQL body or delete the file.
    (B) Fix supabase/functions/escalations-report/index.ts to reference only live columns, preserving the existing JSON response envelope/field names (assignee_name, unit_name, byUnit, byAssignee) so the frontend contract is unchanged:
      - In the assignments!inner embedded select (line ~117-122) REMOVE organizational_unit_id (not a real column). Keep id, work_item_type, assignee_id, sla_status.
      - Replace the .eq('assignments.organizational_unit_id', unitId) filter (line ~130): first resolve the user_ids in that unit via .from('staff_profiles').select('user_id').eq('unit_id', unitId), then filter escalations by assignments.assignee_id in that set (use .in on assignee_id, or post-filter the fetched rows). Keep the byUnit aggregation output shape.
      - For the byUnit aggregation (line ~204): build a staff_profiles map keyed by user_id → unit_id (single fetch of staff_profiles for all assignee_ids), then derive each escalation's unit from its assignee_id; replace esc.assignments?.organizational_unit_id reads with that lookup.
      - Fix the org-unit name fetch (line ~213-214): .from('organizational_units').select('id, name_en, name_ar') and build unitMap from name_en (default; org-unit naming has no locale param here) instead of the nonexistent name.
      - Fix the assignee name fetch (line ~240-241): staff_profiles has no full_name. Fetch staff_profiles (user_id, unit_id, current_assignment_count) AND fetch names from users (.from('users').select('id, full_name').in('id', assigneeIds)); set assignee_name from the users map (users.full_name), keep current_assignment_count from staff_profiles.
      Add no columns beyond those verified live. Update the local TypeScript interface(s) in the file (e.g. the organizational_unit_id field on the assignments shape at line ~63) so the edge fn's own types match.
  </action>
  <verify>
    <automated>test "$(grep -c 'organizational_unit_id' supabase/functions/escalations-report/index.ts)" -eq 0 && test "$(grep -c "'id, name')" supabase/functions/escalations-report/index.ts)" -eq 0 && grep -c 'SUPERSEDED' supabase/migrations/009_data_library.sql</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'organizational_unit_id' supabase/functions/escalations-report/index.ts returns 0.
    - grep -c "select('id, name')" supabase/functions/escalations-report/index.ts returns 0 (the org-unit name select no longer uses the nonexistent name column).
    - grep -c 'name_en' supabase/functions/escalations-report/index.ts returns ≥1 (org-unit name resolved via name_en).
    - The assignee-name path queries users for full_name (grep "from('users')" and "full_name" present in the assignee section).
    - grep -c 'SUPERSEDED' supabase/migrations/009_data_library.sql returns 1; the file's CREATE TABLE body is unchanged (grep 'CREATE TABLE IF NOT EXISTS public.data_library_items' still 1).
    - Commit message fix(60-04): escalations-report real columns + supersede dead 009_data_library.
  </acceptance_criteria>
  <done>009 superseded marker added; edge fn references only live columns; build shows ✓ built. Redeploy happens in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Redeploy escalations-report; type-check both workspaces; verify type fallout fixed</name>
  <read_first>
    - supabase/functions/escalations-report/index.ts (the fixed file from Task 2 — the deploy ships this)
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md ("Types regeneration": run build AND tsc because as-casts let drift compile; open decision 3 redeploy)
  </read_first>
  <action>
    Redeploy the fixed edge function: run supabase functions deploy escalations-report --project-ref zkrcjzdemdmwhearhfgg (the fix is inert until redeployed — handoff rule 4). Confirm the deploy reports success.
    Then validate the type swap did not break either workspace (as-casts mean drift compiles silently, so run both build AND type-check):
    - pnpm --filter intake-frontend type-check  (or the repo's frontend type-check script) — must exit 0.
    - pnpm --filter intake-backend type-check — must exit 0.
    - pnpm --filter intake-frontend build — must show ✓ built.
    If the regenerated types surface compile errors (e.g. a renamed/removed column the app referenced via the old types), fix the affected call sites minimally (surgical — touch only what the type change broke), re-run type-check, and re-build. Record each fixed call site in the SUMMARY.
    Optionally invoke the redeployed escalations-report edge fn against staging with a minimal payload to confirm it returns 200 (no 42703), and record the result.
  </action>
  <verify>
    <automated>pnpm --filter intake-frontend type-check && pnpm --filter intake-backend type-check</automated>
  </verify>
  <acceptance_criteria>
    - supabase functions deploy escalations-report --project-ref zkrcjzdemdmwhearhfgg completed with a success message (recorded in SUMMARY).
    - pnpm --filter intake-frontend type-check exits 0.
    - pnpm --filter intake-backend type-check exits 0.
    - pnpm --filter intake-frontend build shows ✓ built.
    - Any call-site fixes from the type swap are surgical and listed in the SUMMARY; if none were needed, that is stated explicitly.
  </acceptance_criteria>
  <done>Edge fn redeployed; both workspaces type-check clean; frontend builds; results in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edge function → staging DB | escalations-report queries org-units/users/staff_profiles |
| generated types → app compile | regenerated types define the compile-time contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-04-01 | Information Disclosure | escalations-report exposes assignee names | accept | users.full_name for assignees is already surfaced across the app; the report runs for authorized escalation-report viewers. No new data exposed vs the prior (broken) intent. |
| T-60-04-02 | Tampering | regenerated types hide a real drift via as-casts | mitigate | Run BOTH build and tsc type-check on frontend + backend; determinism check (generate twice, identical) ensures the types reflect live, not a transient. |
| T-60-04-03 | Repudiation | edge fn fix not actually deployed (inert) | mitigate | [BLOCKING] redeploy step + optional live invoke returning 200 confirms the active function references real columns. |
| T-60-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (type regen + edge-fn edit only). |
</threat_model>

<verification>
- diff confirms frontend/backend types byte-identical and contain all newly-live objects.
- backend/backend dead copy removed; 009_data_library.sql carries SUPERSEDED header (body unchanged).
- escalations-report has zero organizational_unit_id refs and no select('id, name'); redeployed; both workspaces type-check; frontend builds.
</verification>

<success_criteria>
- Generated types agree with live staging across both real copies; dead doubled-path gone.
- escalations-report references only live columns and is active on staging.
</success_criteria>

<output>
Create `.planning/phases/60-schema-type-truth-restoration/60-04-SUMMARY.md` when done, recording the determinism check, dead-path evidence, deploy success line, type-check results, and any call-site fixes.
</output>
