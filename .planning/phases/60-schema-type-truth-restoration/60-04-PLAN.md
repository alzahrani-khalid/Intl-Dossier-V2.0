---
phase: 60-schema-type-truth-restoration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260610000004_create_pending_role_approvals.sql
  - supabase/migrations/20260610000005_create_delegation_and_word_assistant_tables.sql
autonomous: true
requirements: [P1]
must_haves:
  truths:
    - 'pending_role_approvals table exists live with approval_status enum + indexes + owner-scoped RLS, and NO auth.users-mutating trigger and NO reference to the missing user_sessions table'
    - 'position_delegations and word_assistant_logs tables exist live with the exact columns their edge functions write, plus owner-scoped RLS'
    - 'All three tables report relrowsecurity = true on staging after apply'
  artifacts:
    - path: 'supabase/migrations/20260610000004_create_pending_role_approvals.sql'
      provides: 'approval_status enum + pending_role_approvals table + indexes + owner-scoped RLS (no auth.users trigger)'
      contains: 'CREATE TABLE'
    - path: 'supabase/migrations/20260610000005_create_delegation_and_word_assistant_tables.sql'
      provides: 'position_delegations + word_assistant_logs tables with owner-scoped RLS'
      contains: 'position_delegations'
  key_links:
    - from: 'supabase/functions/positions-delegate/index.ts'
      to: 'public.position_delegations'
      via: 'insert position_id delegator_id delegate_id reason expires_at status'
      pattern: 'position_delegations'
    - from: 'supabase/functions/word-assistant/index.ts'
      to: 'public.word_assistant_logs'
      via: 'insert user_id action input_text output_text session_id'
      pattern: 'word_assistant_logs'
---

<objective>
Create the three tables that exist in NO repo migration and NOT live, so their edge functions stop falling back to a 42P01 error path and so the objects appear in regenerated types:
1. `pending_role_approvals` — a committed migration exists (`20251011214943`) but depends on a missing `approval_status` enum, a missing `user_sessions` table, and an `apply_admin_role_approval` trigger that mutates `auth.users.role`. Author a corrected migration: create the enum, the table, indexes, safe triggers, and owner-scoped RLS, OMITTING the auth.users-mutating trigger (Phase 61 scope).
2. `position_delegations` — author new from the `positions-delegate` edge-fn write shape.
3. `word_assistant_logs` — author new from the `word-assistant` edge-fn write shape.

Purpose: Close every "missing table" drift for approvals [approvals #6] and word-assistant [wa #4] so they appear in regenerated types in plan 60-05.
Output: Two dated migrations applied to staging + committed (one for pending_role_approvals, one for the two simpler tables).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-schema-type-truth-restoration/60-CONTEXT.md
@.planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md
@.planning/phases/60-schema-type-truth-restoration/60-PATTERNS.md
@supabase/migrations/20251011214943_create_pending_role_approvals.sql
@supabase/functions/positions-delegate/index.ts
@supabase/functions/word-assistant/index.ts

<process_rules>
LOCKED process rules (CONTEXT.md "Process rules"):

- Migrations applied via Supabase MCP `apply_migration` ONLY (project_id `zkrcjzdemdmwhearhfgg`). NEVER `supabase db push`, NEVER raw psql. ALSO commit the identical SQL.
- VERIFY every dependency (enum, FK target table) against staging SQL before acting.
- After every commit run `pnpm --filter intake-frontend build` directly.
- `pnpm exec`, never `npx`. NO `timeout` command on this Mac.
- Commit convention: `feat(60-04): ...`, no AI attribution. Do NOT re-fix bucket-A items (68552968..33671c41).
  </process_rules>

<interfaces>
<!-- VERIFIED live facts (RESEARCH "Key dependency facts") + edge-fn shapes. -->
pending_role_approvals: user_role enum EXISTS live; approval_status enum MISSING (create it: pending|approved|rejected|expired);
  user_sessions table MISSING; the apply_admin_role_approval trigger UPDATEs auth.users.role AND deletes from user_sessions — OMIT this trigger entirely (Phase 61). Keep the table, indexes, and the update_approvals_updated_at trigger + expire_pending_approvals helper.

position_delegations shape (supabase/functions/positions-delegate/index.ts lines 159-189): id, position_id (->positions),
delegator_id (auth user), delegate_id, reason text null, expires_at timestamptz null, status text default active, created_at.
Queried by .eq(position_id).eq(delegate_id).eq(status, active).

word_assistant_logs shape (supabase/functions/word-assistant/index.ts line ~256): id, user_id, action text,
input_text text, output_text text, session_id text/uuid, created_at timestamptz default now().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author pending_role_approvals migration (enum + table + indexes + RLS, NO auth.users trigger)</name>
  <read_first>
    - supabase/migrations/20251011214943_create_pending_role_approvals.sql (the committed original — copy table + indexes + safe triggers; the apply_admin_role_approval trigger and its function MUST be omitted)
    - supabase/migrations/20260604120000_calendar_entries_select_rls.sql (the owner-scoped RLS pattern: DROP POLICY IF EXISTS then CREATE POLICY USING col = auth.uid() OR ...)
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md ("Key dependency facts" pending_role_approvals paragraph + Migration strategy item 4)
    - .planning/phases/60-schema-type-truth-restoration/60-PATTERNS.md ("Table-authoring migrations" section — per-table guidance for pending_role_approvals)
  </read_first>
  <action>
    Create supabase/migrations/20260610000004_create_pending_role_approvals.sql (BEGIN; ... COMMIT;) containing:
    (1) approval_status enum, guarded: DO block testing pg_type for typname=approval_status before CREATE TYPE approval_status AS ENUM (pending, approved, rejected, expired) so re-apply is safe.
    (2) CREATE TABLE IF NOT EXISTS pending_role_approvals (...) copied verbatim from the original (all columns + both CHECK constraints chk_approval_different_approvers and chk_approval_not_self) — it references user_role (live) and approval_status (created above).
    (3) The three indexes idx_approvals_user, idx_approvals_status, idx_approvals_expires_at as CREATE INDEX IF NOT EXISTS.
    (4) The update_approvals_updated_at() function + trigger_update_approvals_updated_at BEFORE UPDATE trigger (safe — no auth.users mutation).
    (5) The expire_pending_approvals() helper (safe — only updates this table).
    OMIT ENTIRELY: the apply_admin_role_approval() function and trigger_apply_admin_role trigger (they UPDATE auth.users.role and DELETE FROM user_sessions which does not exist — Phase 61).
    (6) ALTER TABLE pending_role_approvals ENABLE ROW LEVEL SECURITY; + owner-scoped policies: a SELECT policy for rows where user_id = auth.uid() OR requested_by = auth.uid() OR first_approver_id = auth.uid() OR second_approver_id = auth.uid() (the parties to the approval — confirm the exact column names against the original migration before writing), and an INSERT policy WITH CHECK (requested_by = auth.uid()). Do NOT add broad admin policies (admin gating is Phase 61).
    Verify the enum is not already present before authoring the guard (MCP execute_sql: SELECT 1 FROM pg_type WHERE typname=approval_status). Header comment: pending_role_approvals (approvals #6): enum + table + indexes + owner-scoped RLS; apply_admin_role_approval trigger OMITTED (mutates auth.users, depends on missing user_sessions — Phase 61); applied 2026-06-10.
    Commit feat(60-04): pending_role_approvals table (no auth.users trigger), run pnpm --filter intake-frontend build.
  </action>
  <verify>
    <automated>grep -c "apply_admin_role_approval" supabase/migrations/20260610000004_create_pending_role_approvals.sql</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20260610000004_create_pending_role_approvals.sql exists.
    - grep -c apply_admin_role_approval returns 0 (the auth.users-mutating function/trigger omitted).
    - grep -c user_sessions returns 0 (no reference to the missing table).
    - grep -c "UPDATE auth.users" returns 0.
    - approval_status enum is created guarded (grep finds approval_status; the CREATE TYPE is inside a DO/IF NOT EXISTS guard).
    - grep -c "ENABLE ROW LEVEL SECURITY" returns 1 and at least one CREATE POLICY with auth.uid() is present.
    - pnpm --filter intake-frontend build exits 0.
  </acceptance_criteria>
  <done>pending_role_approvals migration committed; no omitted-by-design objects present; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Author position_delegations + word_assistant_logs migration, then apply BOTH migrations via MCP and probe (BLOCKING apply)</name>
  <read_first>
    - supabase/functions/positions-delegate/index.ts (lines 159-189 — the exact insert shape for position_delegations)
    - supabase/functions/word-assistant/index.ts (line ~256 — the exact insert shape for word_assistant_logs)
    - supabase/migrations/20260604120000_calendar_entries_select_rls.sql (owner-scoped RLS pattern)
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md ("Key dependency facts" position_delegations + word_assistant_logs + Migration strategy item 5; "Validation Architecture" 3-tables row)
    - supabase/migrations/20260610000004_create_pending_role_approvals.sql (the file authored in Task 1 — needed to apply it in this task)
  </read_first>
  <action>
    First author supabase/migrations/20260610000005_create_delegation_and_word_assistant_tables.sql (BEGIN; ... COMMIT;):
    - CREATE TABLE IF NOT EXISTS public.position_delegations with: id uuid PK default gen_random_uuid(), position_id uuid NOT NULL REFERENCES positions(id) ON DELETE CASCADE, delegator_id uuid NOT NULL REFERENCES auth.users(id), delegate_id uuid NOT NULL REFERENCES auth.users(id), reason text, expires_at timestamptz, status text NOT NULL DEFAULT active, created_at timestamptz NOT NULL DEFAULT now(). Add CREATE INDEX IF NOT EXISTS on (position_id, delegate_id, status). Before writing the positions FK, VERIFY positions table + id column exist live via MCP execute_sql (SELECT 1 FROM information_schema.columns WHERE table_schema=public AND table_name=positions AND column_name=id) — if positions is absent live, drop the FK and keep position_id as a plain uuid (note the decision in SUMMARY).
    - CREATE TABLE IF NOT EXISTS public.word_assistant_logs with: id uuid PK default gen_random_uuid(), user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE, action text NOT NULL, input_text text, output_text text, session_id text, created_at timestamptz NOT NULL DEFAULT now(). Add CREATE INDEX IF NOT EXISTS on (user_id, created_at).
    - ENABLE ROW LEVEL SECURITY on both + owner-scoped policies: position_delegations SELECT where delegator_id = auth.uid() OR delegate_id = auth.uid(), INSERT WITH CHECK delegator_id = auth.uid(); word_assistant_logs SELECT and INSERT where user_id = auth.uid().
    - Header comment: position_delegations + word_assistant_logs (approvals #6 / wa #4): tables matching edge-fn write shapes + owner-scoped RLS; applied 2026-06-10.
    Commit feat(60-04): position_delegations + word_assistant_logs tables, run pnpm --filter intake-frontend build.

    THEN [BLOCKING] apply BOTH of this plan's migrations via Supabase MCP apply_migration (project_id zkrcjzdemdmwhearhfgg), in order: 20260610000004 (name create_pending_role_approvals), then 20260610000005 (name create_delegation_and_word_assistant_tables) — each query = the exact committed SQL. Probe with MCP execute_sql:
    (a) SELECT table_name FROM information_schema.tables WHERE table_schema=public AND table_name IN (pending_role_approvals, position_delegations, word_assistant_logs) ORDER BY table_name — expect 3 rows.
    (b) SELECT 1 FROM pg_type WHERE typname=approval_status — 1 row.
    (c) SELECT relname, relrowsecurity FROM pg_class WHERE relname IN (pending_role_approvals, position_delegations, word_assistant_logs) — all relrowsecurity = true.
    (d) SELECT count(*) FROM pg_policies WHERE schemaname=public AND tablename IN (pending_role_approvals, position_delegations, word_assistant_logs) — >= 5 policies total (the owner-scoped SELECT/INSERT policies).
    If any apply errors, capture the PG error, verify the offending object live, correct the SQL, re-commit, re-apply.

  </action>
  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS public.position_delegations" supabase/migrations/20260610000005_create_delegation_and_word_assistant_tables.sql</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20260610000005_create_delegation_and_word_assistant_tables.sql exists with both CREATE TABLE statements (grep finds public.position_delegations AND public.word_assistant_logs) and both tables RLS-enabled with auth.uid() policies.
    - position_delegations columns match the edge-fn insert: position_id, delegator_id, delegate_id, reason, expires_at, status (grep confirms all 6).
    - word_assistant_logs columns match: user_id, action, input_text, output_text, session_id (grep confirms all 5).
    - Both migrations applied via MCP without error.
    - Probe (a) returns exactly 3 rows (pending_role_approvals, position_delegations, word_assistant_logs).
    - Probe (b) returns 1 row (approval_status enum exists).
    - Probe (c): all three tables report relrowsecurity = true.
    - Probe (d): >= 5 RLS policies across the three tables.
    - pnpm --filter intake-frontend build exits 0.
  </acceptance_criteria>
  <done>Second migration committed; both migrations applied; probes (a)-(d) pass; outputs + any FK-omission decision in SUMMARY; build green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                               |
| ------------------------------------------ | --------------------------------------------------------- |
| migration SQL → live staging DB            | New tables created on the operational staging DB via MCP  |
| authenticated user → new RLS-scoped tables | Owner-scoped reads/writes on approvals, delegations, logs |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                  | Disposition | Mitigation Plan                                                                                                                                                                               |
| ---------- | ---------------------- | ---------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-60-04-E  | Elevation of Privilege | pending_role_approvals trigger mutating auth.users.role    | mitigate    | The apply_admin_role_approval trigger is OMITTED entirely (grep gate asserts 0). Role-apply mechanics deferred to Phase 61's role-source unification; no path in this plan writes auth.users. |
| T-60-04-S  | Spoofing               | forged delegation/approval rows attributed to another user | mitigate    | INSERT policies use WITH CHECK (delegator_id / requested_by = auth.uid()); SELECT policies scope to the parties only. No broad authenticated-all policy.                                      |
| T-60-04-I  | Information Disclosure | word_assistant_logs leak other users' prompt/output text   | mitigate    | RLS SELECT/INSERT scoped to user_id = auth.uid(); no cross-user read. Probe (c)/(d) confirm RLS enabled + policies present.                                                                   |
| T-60-04-SC | Tampering              | npm/pip/cargo installs                                     | accept      | No package installs in this plan (SQL-only migrations).                                                                                                                                       |

</threat_model>

<verification>
- pending_role_approvals migration has zero apply_admin_role_approval / user_sessions / UPDATE auth.users references; RLS enabled.
- Both migration files exist with the asserted CREATE statements + owner-scoped RLS.
- MCP probes (a)-(d) confirm 3 tables + approval_status enum live, all RLS-enabled with >= 5 policies.
- pnpm --filter intake-frontend build green after each commit.
</verification>

<success_criteria>

- pending_role_approvals, position_delegations, word_assistant_logs all live on staging (ready for type regen in 60-05).
- No omitted-by-design object (auth.users trigger) is present; all three tables are RLS-protected owner-scoped.
  </success_criteria>

<output>
Create `.planning/phases/60-schema-type-truth-restoration/60-04-SUMMARY.md` when done, recording probes (a)-(d) verbatim and any FK-omission decision.
</output>
