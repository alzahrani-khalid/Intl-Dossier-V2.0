---
phase: 60-schema-type-truth-restoration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260610000002_enhanced_sla_monitoring_corrected.sql
autonomous: true
requirements: [P1]
must_haves:
  truths:
    - "The 4 SLA dashboard RPCs plus capture_sla_daily_snapshot exist live on staging after applying the corrected migration"
    - "The corrected migration references real columns only — assignee/at-risk RPCs resolve names via users.full_name / users.name_ar, never staff_profiles.full_name"
    - "All 4 SLA dashboard RPCs are callable without 42703 (undefined column) or 42883 (undefined function) errors"
  artifacts:
    - path: "supabase/migrations/20260610000002_enhanced_sla_monitoring_corrected.sql"
      provides: "Corrected copy of 20260111600001 with staff_profiles.full_name → users.full_name via LEFT JOIN users"
      contains: "LEFT JOIN users u ON u.id = sp.user_id"
  key_links:
    - from: "supabase/migrations/20260610000002_enhanced_sla_monitoring_corrected.sql"
      to: "public.users (full_name, name_ar)"
      via: "LEFT JOIN users u ON u.id = sp.user_id replacing sp.full_name / sp.full_name_ar"
      pattern: "u\\.full_name"
---

<objective>
Apply the SLA monitoring migration that was committed (`20260111600001_enhanced_sla_monitoring.sql`) but NEVER applied — its 4 dashboard RPCs and `capture_sla_daily_snapshot` are absent from live staging and from generated types (RESEARCH matrix). The original migration is unappliable as-is: it joins `staff_profiles.full_name` / `staff_profiles.full_name_ar`, columns that do not exist live (staff_profiles cols: id, user_id, unit_id, role, …). This plan produces a CORRECTED copy that resolves names via `users` and applies it.

Purpose: Bring the SLA dashboard RPCs live so they appear in regenerated types [sla #2] and fix the absent-column bug [sla #4].
Output: `supabase/migrations/20260610000002_enhanced_sla_monitoring_corrected.sql`, applied to staging and verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-schema-type-truth-restoration/60-CONTEXT.md
@.planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md
@supabase/migrations/20260111600001_enhanced_sla_monitoring.sql

<interfaces>
<!-- Verified live column facts (RESEARCH "Key dependency facts" + matrix). -->
- staff_profiles columns: id, user_id, unit_id, role, … (NO full_name, NO full_name_ar)
- users.full_name ✅ exists; users.name_ar ✅ exists; users.full_name_ar ❌ does NOT exist
- The broken references in 20260111600001 are at lines ~190-193 + GROUP BY ~213 (v_at_risk... view), ~382-383 + GROUP BY ~407 (a function), ~442 + GROUP BY ~450 (another function). All use `LEFT JOIN staff_profiles sp ON sp.user_id = t.assigned_to` then read `sp.full_name` / `sp.full_name_ar`.
- SLA migration deps all live: sla_events, sla_policies, intake_tickets.priority/.submitted_at/.assigned_to, sla_escalations (CREATE TABLE IF NOT EXISTS — harmless), sla_compliance_snapshots (created by the migration).
- The 5 functions: get_sla_dashboard_overview, get_sla_compliance_by_type, get_sla_compliance_by_assignee, get_sla_at_risk_items, capture_sla_daily_snapshot.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy migration and rewrite all staff_profiles.full_name references to users columns</name>
  <read_first>
    - supabase/migrations/20260111600001_enhanced_sla_monitoring.sql (the FULL original — read all of it; the three broken spots are at lines ~190-213, ~382-407, ~442-450 but the whole file must be copied verbatim except those edits)
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md ("Migration strategy" item 2 — exact replacement recipe)
  </read_first>
  <action>
    Create supabase/migrations/20260610000002_enhanced_sla_monitoring_corrected.sql as a byte-for-byte copy of 20260111600001_enhanced_sla_monitoring.sql, then apply EXACTLY these three corrections (and ONLY these — do not otherwise restructure the migration):
    (1) Each spot already has `LEFT JOIN staff_profiles sp ON sp.user_id = t.assigned_to`. Immediately after that join, add `LEFT JOIN users u ON u.id = sp.user_id`.
    (2) Replace every `sp.full_name` with `u.full_name`.
    (3) Replace every `sp.full_name_ar` with `u.name_ar` (NOT u.full_name_ar — that column does not exist live).
    (4) Update each affected GROUP BY clause to match: where the GROUP BY listed `sp.full_name, sp.full_name_ar` change to `u.full_name, u.name_ar`; where it listed `sp.id, sp.full_name, sp.full_name_ar, sp.unit_id` change to `sp.id, u.full_name, u.name_ar, sp.unit_id` (keep sp.id and sp.unit_id — those columns DO exist).
    Leave `CREATE TABLE IF NOT EXISTS sla_escalations` intact (already live, IF NOT EXISTS is harmless). Add a header comment: `-- Corrected reapply of 20260111600001 (sla #2, sla #4): staff_profiles.full_name/.full_name_ar do not exist live; resolve names via users.full_name / users.name_ar. The original was never applied. Applied 2026-06-10.`
    Verify after editing that NO occurrence of `sp.full_name` remains anywhere in the new file.
  </action>
  <verify>
    <automated>test "$(grep -c 'sp\.full_name' supabase/migrations/20260610000002_enhanced_sla_monitoring_corrected.sql)" -eq 0 && grep -c 'LEFT JOIN users u ON u.id = sp.user_id' supabase/migrations/20260610000002_enhanced_sla_monitoring_corrected.sql  # expect 3</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20260610000002_enhanced_sla_monitoring_corrected.sql exists.
    - `grep -c 'sp\.full_name' <file>` returns 0 (no surviving staff_profiles.full_name or .full_name_ar references).
    - `grep -c 'LEFT JOIN users u ON u.id = sp.user_id' <file>` returns 3 (one per corrected spot).
    - `grep -c 'u\.name_ar' <file>` returns at least 2 (the Arabic-name spots; NOT u.full_name_ar).
    - `grep -c 'full_name_ar' <file>` returns 0 (the nonexistent users.full_name_ar column is never referenced).
    - The five function names get_sla_dashboard_overview, get_sla_compliance_by_type, get_sla_compliance_by_assignee, get_sla_at_risk_items, capture_sla_daily_snapshot all still appear in the file (CREATE FUNCTION grep count ≥ 5).
  </acceptance_criteria>
  <done>Corrected migration committed (`fix(60-02): corrected SLA monitoring migration — users.full_name not staff_profiles`); build shows `✓ built`.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply corrected SLA migration via MCP and smoke-call the 4 RPCs</name>
  <read_first>
    - supabase/migrations/20260610000002_enhanced_sla_monitoring_corrected.sql (the file from Task 1 — read it to copy the exact SQL into apply_migration)
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md ("Validation Architecture": SLA RPCs live row)
  </read_first>
  <action>
    Apply the corrected migration to staging via Supabase MCP apply_migration (project_id zkrcjzdemdmwhearhfgg, name `enhanced_sla_monitoring_corrected`, query = the exact committed SQL — NOT supabase db push, NOT psql). Then probe with MCP execute_sql:
    (1) `SELECT proname FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace WHERE n.nspname='public' AND proname IN ('get_sla_dashboard_overview','get_sla_compliance_by_type','get_sla_compliance_by_assignee','get_sla_at_risk_items','capture_sla_daily_snapshot') ORDER BY proname;` — expect 5 rows.
    (2) Smoke-call each of the 4 dashboard RPCs with their captured signatures (e.g. `SELECT * FROM get_sla_dashboard_overview() LIMIT 1;`, `SELECT * FROM get_sla_compliance_by_assignee() LIMIT 1;` — adjust args to the real signature). Each must return rows or empty WITHOUT 42703/42883.
    If apply errors with a missing-dependency or column error, capture the exact PG error, correct the SQL (verify the offending column against live first via execute_sql on information_schema.columns), re-commit, and re-apply. Do not declare done until probe (1) returns 5 and all 4 dashboard RPCs smoke-call cleanly.
  </action>
  <verify>
    <automated>MISSING — apply + probe via MCP. Proof = probe (1) returns 5 rows and 4 dashboard RPC smoke-calls return without error, recorded verbatim in SUMMARY.</automated>
  </verify>
  <acceptance_criteria>
    - MCP apply_migration completed without error; migration in staging history.
    - Probe (1) returns exactly 5 rows (the 5 named functions).
    - Each of get_sla_dashboard_overview, get_sla_compliance_by_type, get_sla_compliance_by_assignee, get_sla_at_risk_items executed via SELECT and returned rows/empty with NO 42703 (undefined column) and NO 42883 (undefined function).
  </acceptance_criteria>
  <done>Corrected SLA migration applied to staging; 5 functions present; 4 dashboard RPCs callable; probe outputs in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → live staging DB | Corrected DDL applied to operational DB via MCP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-02-01 | Information Disclosure | SLA assignee RPCs expose user names | accept | Names (users.full_name) are already exposed throughout the app for assignees; RPCs operate over intake_tickets the user can already see. GRANTs follow the original migration (authenticated). |
| T-60-02-02 | Tampering | wrong column edit corrupts RPC logic | mitigate | Only the three documented join/column swaps are applied; grep gate asserts zero surviving sp.full_name and exactly 3 added users joins; smoke-call after apply proves the RPCs still execute. |
| T-60-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (SQL-only migration). |
</threat_model>

<verification>
- `grep -c 'sp\.full_name' <file>` = 0; `grep -c 'LEFT JOIN users u ON u.id = sp.user_id' <file>` = 3.
- MCP probe confirms 5 SLA functions live; 4 dashboard RPCs smoke-call cleanly.
- `pnpm --filter intake-frontend build` shows `✓ built` after the commit.
</verification>

<success_criteria>
- SLA dashboard RPCs are live on staging (ready to appear in regenerated types in plan 60-04).
- No SLA RPC references a nonexistent column.
</success_criteria>

<output>
Create `.planning/phases/60-schema-type-truth-restoration/60-02-SUMMARY.md` when done, recording the probe outputs verbatim.
</output>
