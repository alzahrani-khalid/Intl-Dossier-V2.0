---
phase: 60-schema-type-truth-restoration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260610000002_enhanced_sla_monitoring_corrected.sql
  - supabase/functions/escalations-report/index.ts
autonomous: true
requirements: [P1]
must_haves:
  truths:
    - 'The 4 SLA dashboard RPCs plus capture_sla_daily_snapshot exist live on staging after applying the corrected migration'
    - 'The corrected SLA migration references real columns only — assignee/at-risk RPCs resolve names via users.full_name / users.name_ar, never staff_profiles.full_name'
    - 'escalations-report edge function references only live columns (unit via staff_profiles.unit_id, organizational_units.name_en/name_ar, users.full_name) and is redeployed so the fix is active'
  artifacts:
    - path: 'supabase/migrations/20260610000002_enhanced_sla_monitoring_corrected.sql'
      provides: 'Corrected copy of 20260111600001 with staff_profiles.full_name -> users.full_name via LEFT JOIN users'
      contains: 'LEFT JOIN users u ON u.id = sp.user_id'
    - path: 'supabase/functions/escalations-report/index.ts'
      provides: 'Edge fn fixed to reference real columns (unit via staff_profiles.unit_id, organizational_units.name_en/name_ar, users.full_name)'
      contains: 'name_en'
  key_links:
    - from: 'supabase/migrations/20260610000002_enhanced_sla_monitoring_corrected.sql'
      to: 'public.users (full_name, name_ar)'
      via: 'LEFT JOIN users u ON u.id = sp.user_id replacing sp.full_name / sp.full_name_ar'
      pattern: "u\\.full_name"
    - from: 'supabase/functions/escalations-report/index.ts'
      to: 'public.organizational_units / public.users / public.staff_profiles'
      via: 'corrected select column lists + unit-via-staff_profiles.unit_id'
      pattern: 'name_en'
---

<objective>
Fix the two column-reference defects that share the same root cause (RPC/edge-fn SQL referencing columns absent from live staging):
1. Apply the SLA monitoring migration that was committed (`20260111600001_enhanced_sla_monitoring.sql`) but NEVER applied — its 4 dashboard RPCs + `capture_sla_daily_snapshot` are absent from live staging and from generated types. The original is unappliable as-is: it joins `staff_profiles.full_name` / `staff_profiles.full_name_ar`, columns that do not exist live (staff_profiles cols: id, user_id, unit_id, role). Produce a CORRECTED copy resolving names via `users` and apply it.
2. Fix the `escalations-report` edge function's three absent-column references (`assignments.organizational_unit_id`, `organizational_units.name`, `staff_profiles.full_name`) and redeploy so the fix is active.

Purpose: Bring the SLA dashboard RPCs live so they appear in regenerated types [sla #2], fix the SLA absent-column bug [sla #4], and fix the escalations-report absent-column bug [tq #5].
Output: Corrected SLA migration applied + committed; escalations-report edge fn fixed + redeployed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-schema-type-truth-restoration/60-CONTEXT.md
@.planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md
@.planning/phases/60-schema-type-truth-restoration/60-PATTERNS.md
@supabase/migrations/20260111600001_enhanced_sla_monitoring.sql
@supabase/functions/escalations-report/index.ts

<process_rules>
LOCKED process rules (CONTEXT.md "Process rules"):

- Migrations applied via Supabase MCP `apply_migration` ONLY (project_id `zkrcjzdemdmwhearhfgg`). NEVER `supabase db push`, NEVER raw psql. ALSO commit the identical SQL.
- Edge-function fixes are INERT until redeployed — redeploy escalations-report via Supabase CLI/MCP (handoff rule 4). SQL-only application does nothing for an edge fn.
- VERIFY every DB/column claim against staging SQL before acting.
- After every commit run `pnpm --filter intake-frontend build` directly.
- `pnpm exec`, never `npx`. NO `timeout` command on this Mac.
- Commit convention: `fix(60-02): ...`, no AI attribution. Do NOT re-fix bucket-A items (commits 68552968..33671c41).
  </process_rules>

<interfaces>
<!-- Verified live column facts (RESEARCH "Key dependency facts" + matrix + source read). -->
SLA migration:
- staff_profiles columns: id, user_id, unit_id, role (NO full_name, NO full_name_ar).
- users.full_name EXISTS; users.name_ar EXISTS; users.full_name_ar does NOT exist.
- Broken refs in 20260111600001: view ~lines 190-213, function ~382-407, function ~442-450. Each uses LEFT JOIN staff_profiles sp ON sp.user_id = t.assigned_to then reads sp.full_name / sp.full_name_ar.
- SLA migration deps all live: sla_events, sla_policies, intake_tickets.priority/.submitted_at/.assigned_to, sla_escalations (CREATE TABLE IF NOT EXISTS — harmless), sla_compliance_snapshots (created by the migration).
- The 5 functions: get_sla_dashboard_overview, get_sla_compliance_by_type, get_sla_compliance_by_assignee, get_sla_at_risk_items, capture_sla_daily_snapshot.

escalations-report (supabase/functions/escalations-report/index.ts) broken refs — VERIFIED at these lines:

- line 63: local TS interface field organizational_unit_id (not a real DB column).
- lines 117-122 + 121: assignments embedded select includes organizational_unit_id — assignments has assignee_id; unit lives on staff_profiles.unit_id.
- line 130 + 204: filter/read assignments.organizational_unit_id — must route unit via the assignee's staff_profiles.unit_id.
- lines 213-214: .from('organizational_units').select('id, name') — organizational_units has name_en, name_ar (NO name).
- lines 240-241: .from('staff_profiles').select('user_id, full_name, current_assignment_count') — staff_profiles has NO full_name (names on users.full_name via staff_profiles.user_id -> users.id).
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Copy SLA migration, rewrite staff_profiles.full_name -> users columns, apply via MCP, smoke-call 4 RPCs (BLOCKING apply)</name>
  <files>supabase/migrations/20260610000002_enhanced_sla_monitoring_corrected.sql</files>
  <read_first>
    - supabase/migrations/20260111600001_enhanced_sla_monitoring.sql (the FULL original — read all of it; the three broken spots are at lines ~190-213, ~382-407, ~442-450 but the whole file must be copied verbatim except those edits)
    - supabase/migrations/20260531120000_fix_get_team_workload_email_text_cast.sql (the corrective-header comment style for the top of the new file)
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md ("Migration strategy" item 2 — exact replacement recipe; "Validation Architecture": SLA RPCs live row)
    - .planning/phases/60-schema-type-truth-restoration/60-PATTERNS.md ("Corrective RPC migrations" section — the signature corrective pattern)
  </read_first>
  <action>
    Create supabase/migrations/20260610000002_enhanced_sla_monitoring_corrected.sql as a byte-for-byte copy of 20260111600001_enhanced_sla_monitoring.sql, then apply EXACTLY these corrections (and ONLY these — do not otherwise restructure):
    (1) Each spot already has LEFT JOIN staff_profiles sp ON sp.user_id = t.assigned_to. Immediately after that join, add LEFT JOIN users u ON u.id = sp.user_id.
    (2) Replace every sp.full_name with u.full_name.
    (3) Replace every sp.full_name_ar with u.name_ar (NOT u.full_name_ar — that column does not exist live).
    (4) Update each affected GROUP BY: where it listed sp.full_name, sp.full_name_ar change to u.full_name, u.name_ar; where it listed sp.id, sp.full_name, sp.full_name_ar, sp.unit_id change to sp.id, u.full_name, u.name_ar, sp.unit_id (keep sp.id and sp.unit_id — those columns exist).
    Leave CREATE TABLE IF NOT EXISTS sla_escalations intact (already live, IF NOT EXISTS is harmless). Prepend a corrective `--` header: staff_profiles has no full_name/full_name_ar column (cols: id, user_id, unit_id, role); 20260111600001 referenced sp.full_name -> 42703 undefined_column and was never applied; Fix: LEFT JOIN users u ON u.id = sp.user_id and select u.full_name / u.name_ar; all other logic preserved byte-for-byte (sla #2, sla #4); applied 2026-06-10.
    Verify after editing that NO occurrence of sp.full_name remains. Commit `fix(60-02): corrected SLA monitoring migration (users.full_name not staff_profiles)`, run pnpm --filter intake-frontend build.

    THEN [BLOCKING] apply via MCP apply_migration (project_id zkrcjzdemdmwhearhfgg, name enhanced_sla_monitoring_corrected, query = the exact committed SQL). Probe with MCP execute_sql:
    (a) SELECT proname FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace WHERE n.nspname='public' AND proname IN ('get_sla_dashboard_overview','get_sla_compliance_by_type','get_sla_compliance_by_assignee','get_sla_at_risk_items','capture_sla_daily_snapshot') ORDER BY proname — expect 5 rows.
    (b) Smoke-call each of the 4 dashboard RPCs with their captured signatures, e.g. SELECT * FROM get_sla_dashboard_overview() LIMIT 1, SELECT * FROM get_sla_compliance_by_assignee() LIMIT 1 — each returns rows/empty WITHOUT 42703/42883.
    If apply errors with a missing-dependency or column error, capture the exact PG error, verify the offending column against live (information_schema.columns), correct the SQL, re-commit, re-apply.

  </action>
  <verify>
    <automated>test "$(grep -c 'sp\.full_name' supabase/migrations/20260610000002_enhanced_sla_monitoring_corrected.sql)" -eq 0 && test "$(grep -c 'LEFT JOIN users u ON u.id = sp.user_id' supabase/migrations/20260610000002_enhanced_sla_monitoring_corrected.sql)" -eq 3 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20260610000002_enhanced_sla_monitoring_corrected.sql exists.
    - grep -c 'sp\.full_name' on the file returns 0 (no surviving staff_profiles.full_name or .full_name_ar references).
    - grep -c 'LEFT JOIN users u ON u.id = sp.user_id' returns 3 (one per corrected spot).
    - grep -c 'u\.name_ar' returns at least 2; grep -c 'full_name_ar' returns 0 (the nonexistent users.full_name_ar column is never referenced).
    - The five function names all still appear (CREATE FUNCTION grep count >= 5).
    - MCP apply_migration completed without error; probe (a) returns exactly 5 rows.
    - Each of the 4 dashboard RPCs smoke-called via SELECT returned rows/empty with NO 42703 and NO 42883.
    - pnpm --filter intake-frontend build exits 0.
  </acceptance_criteria>
  <done>Corrected SLA migration committed + applied; 5 functions present live; 4 dashboard RPCs callable; probe outputs in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Fix escalations-report edge fn to real columns, then redeploy (BLOCKING deploy)</name>
  <files>supabase/functions/escalations-report/index.ts</files>
  <read_first>
    - supabase/functions/escalations-report/index.ts (read the full query-building section lines ~55-270 — the three broken spots, the local interface at line 63, and how unitId/assigneeId flow through byUnit/byAssignee)
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md (escalations-report paragraph line 33 + open decision 3: this is an EDGE FUNCTION fix, redeploy required)
    - .planning/phases/60-schema-type-truth-restoration/60-PATTERNS.md ("Edge-function schema-ref fix" section — the three confirmed-broken references + the CRITICAL redeploy note)
  </read_first>
  <action>
    Fix supabase/functions/escalations-report/index.ts to reference only live columns, preserving the existing JSON response envelope/field names (assignee_name, unit_name, byUnit, byAssignee) so the frontend contract is unchanged. Verify each real column against the live schema (information_schema.columns) before editing:
    - In the assignments!inner embedded select (lines ~117-122) REMOVE organizational_unit_id (not a real column). Keep id, work_item_type, assignee_id, sla_status.
    - Replace the .eq('assignments.organizational_unit_id', unitId) filter (line ~130): first resolve user_ids in that unit via .from('staff_profiles').select('user_id').eq('unit_id', unitId), then filter escalations by assignee_id in that set (.in on assignee_id, or post-filter the fetched rows). Keep the byUnit aggregation output shape.
    - For the byUnit aggregation (line ~204): build a staff_profiles map keyed by user_id -> unit_id (single fetch of staff_profiles for all assignee_ids), then derive each escalation's unit from its assignee_id; replace esc.assignments?.organizational_unit_id reads with that lookup.
    - Fix the org-unit name fetch (lines ~213-214): .from('organizational_units').select('id, name_en, name_ar') and build unitMap from name_en (org-unit naming has no locale param here) instead of the nonexistent name.
    - Fix the assignee name fetch (lines ~240-241): staff_profiles has no full_name. Fetch staff_profiles (user_id, unit_id, current_assignment_count) AND fetch names from users via .from('users').select('id, full_name').in('id', assigneeIds); set assignee_name from the users map (users.full_name), keep current_assignment_count from staff_profiles.
    - Update the local TS interface(s) (e.g. the organizational_unit_id field on the assignments shape at line 63) so the edge fn's own types match the corrected queries. Add no columns beyond those verified live.
    Commit `fix(60-02): escalations-report references real columns (unit via staff_profiles.unit_id, name_en, users.full_name)`, run pnpm --filter intake-frontend build.

    THEN [BLOCKING] redeploy: supabase functions deploy escalations-report --project-ref zkrcjzdemdmwhearhfgg (the fix is inert until redeployed). Confirm the deploy reports success. Optionally invoke the redeployed fn against staging with a minimal payload and confirm it returns 200 (no 42703); record the result.

  </action>
  <verify>
    <automated>test "$(grep -c 'organizational_unit_id' supabase/functions/escalations-report/index.ts)" -eq 0 && test "$(grep -c "select('id, name')" supabase/functions/escalations-report/index.ts)" -eq 0 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'organizational_unit_id' supabase/functions/escalations-report/index.ts returns 0 (removed from select, filter, reads, and local interface).
    - grep -c "select('id, name')" supabase/functions/escalations-report/index.ts returns 0 (org-unit name select no longer uses the nonexistent name column).
    - grep -c 'name_en' supabase/functions/escalations-report/index.ts returns >= 1 (org-unit name resolved via name_en).
    - The assignee-name path queries users for full_name (grep "from('users')" present AND "full_name" present in the assignee section).
    - supabase functions deploy escalations-report --project-ref zkrcjzdemdmwhearhfgg completed with a success message (recorded in SUMMARY).
    - pnpm --filter intake-frontend build exits 0.
  </acceptance_criteria>
  <done>escalations-report references only live columns and is redeployed active on staging; deploy success line + optional 200 invoke recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                                 |
| ------------------------------- | --------------------------------------------------------------------------- |
| migration SQL → live staging DB | Corrected SLA DDL applied to the operational staging DB via MCP             |
| edge function → staging DB      | escalations-report queries org-units / users / staff_profiles / assignments |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                    | Disposition | Mitigation Plan                                                                                                                                                                                                                                                            |
| ---------- | ---------------------- | -------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-60-02-I  | Information Disclosure | SLA + escalations RPCs expose assignee names | accept      | users.full_name for assignees is already surfaced across the app; both surfaces operate over intake_tickets/escalations the requesting user can already see. GRANTs follow the original SLA migration (authenticated); the edge fn auth is unchanged. No new data exposed. |
| T-60-02-T  | Tampering              | wrong column edit corrupts RPC/edge-fn logic | mitigate    | Only the documented join/column swaps are applied; grep gates assert zero surviving sp.full_name (SQL) and zero organizational_unit_id (edge fn); SLA smoke-call after apply proves the RPCs still execute; optional 200 invoke proves the edge fn runs.                   |
| T-60-02-E  | Elevation of Privilege | SLA functions SECURITY DEFINER               | accept      | The corrected migration preserves the original migration's SECURITY DEFINER + search_path declarations byte-for-byte; only column references change. No privilege or search_path widening.                                                                                 |
| T-60-02-R  | Repudiation            | edge-fn fix not actually deployed (inert)    | mitigate    | [BLOCKING] redeploy step + optional live 200 invoke confirms the active function references real columns; deploy success line recorded in SUMMARY.                                                                                                                         |
| T-60-02-SC | Tampering              | npm/pip/cargo installs                       | accept      | No package installs in this plan (SQL migration + edge-fn TypeScript edit + deploy).                                                                                                                                                                                       |

</threat_model>

<verification>
- grep -c 'sp\.full_name' on the SLA migration = 0; grep -c 'LEFT JOIN users u ON u.id = sp.user_id' = 3.
- MCP probe confirms 5 SLA functions live; 4 dashboard RPCs smoke-call cleanly.
- escalations-report has zero organizational_unit_id refs and no select('id, name'); redeployed; pnpm --filter intake-frontend build green.
</verification>

<success_criteria>

- SLA dashboard RPCs live on staging (ready to appear in regenerated types in plan 60-05) with no nonexistent-column references.
- escalations-report references only live columns and is active on staging.
  </success_criteria>

<output>
Create `.planning/phases/60-schema-type-truth-restoration/60-02-SUMMARY.md` when done, recording the SLA probe outputs and the edge-fn deploy success line verbatim.
</output>
