---
phase: 60-schema-type-truth-restoration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260610000003_create_event_details_view.sql
  - supabase/migrations/20260610000004_create_pending_role_approvals.sql
  - supabase/migrations/20260610000005_create_delegation_and_word_assistant_tables.sql
autonomous: true
requirements: [P1]
must_haves:
  truths:
    - "event_details view exists live with the columns EventsPage reads (title_en/ar, start_datetime, venue_*, status, max_participants, organizer_name_en/ar, organizer_id, country_name_en/ar, country_id), granted to authenticated only"
    - "pending_role_approvals table exists live with approval_status enum + indexes + owner-scoped RLS, and NO auth.users-mutating trigger"
    - "position_delegations and word_assistant_logs tables exist live with the exact columns their edge functions write, plus owner-scoped RLS"
  artifacts:
    - path: "supabase/migrations/20260610000003_create_event_details_view.sql"
      provides: "event_details view in 035 shape + organizer/country columns sourced from event_attendees→dossiers"
      contains: "CREATE OR REPLACE VIEW public.event_details"
    - path: "supabase/migrations/20260610000004_create_pending_role_approvals.sql"
      provides: "approval_status enum + pending_role_approvals table + indexes + RLS (no auth.users trigger)"
      contains: "CREATE TABLE"
    - path: "supabase/migrations/20260610000005_create_delegation_and_word_assistant_tables.sql"
      provides: "position_delegations + word_assistant_logs tables with owner-scoped RLS"
      contains: "position_delegations"
  key_links:
    - from: "supabase/functions/positions-delegate/index.ts"
      to: "public.position_delegations"
      via: "insert { position_id, delegator_id, delegate_id, reason, expires_at, status }"
      pattern: "position_delegations"
    - from: "supabase/functions/word-assistant/index.ts"
      to: "public.word_assistant_logs"
      via: "insert { user_id, action, input_text, output_text, session_id }"
      pattern: "word_assistant_logs"
---

<objective>
Create three migrations that bring four missing schema objects live: the `event_details` view (absent live; the committed 035/037 are unappliable as-is), the `pending_role_approvals` table (committed migration depends on missing enum + table + an auth.users-mutating trigger), and two never-migrated tables `position_delegations` and `word_assistant_logs` (their edge functions write to them via a 42P01 fallback). All authored against VERIFIED live column facts.

Purpose: Close every "missing table/view" drift for events [events #4], approvals [approvals #6], and word-assistant [wa #4] so they appear in regenerated types in plan 60-04.
Output: Three dated migrations applied to staging and probed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-schema-type-truth-restoration/60-CONTEXT.md
@.planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md
@supabase/migrations/035_create_event_details_view.sql
@supabase/migrations/037_update_event_details_add_org_country.sql
@supabase/migrations/20251011214943_create_pending_role_approvals.sql
@frontend/src/pages/events/EventsPage.tsx

<interfaces>
<!-- VERIFIED live facts (RESEARCH matrix + "Key dependency facts") + edge-fn shapes. -->
EventsPage.tsx does `.from('event_details').select('*')` (line 265) and RENDERS:
  title_en/title_ar, start_datetime, venue_en/venue_ar, location_en/location_ar, is_virtual, virtual_link,
  max_participants, status, organizer_name_en/organizer_name_ar, organizer_id, country_name_en/country_name_ar, country_id.
035 view shape (appliable as-is) covers everything EXCEPT organizer_*/country_*. 037 is UNAPPLIABLE:
  live `organizations` (id, org_code, org_type, …) and `countries` (id, iso_code_2, iso_code_3, …) are dossier-EXTENSION tables — names live on `dossiers` (name_en/name_ar). `countries` has iso_code_2 (NOT `code`).
  event_attendees columns: event_id, entity_id, type, role, created_at (live ✅).
events columns: id, title, type, start_time, end_time, location, virtual_link, status (live ✅).

pending_role_approvals: user_role enum ✅ live; approval_status enum ❌ MISSING (create it: pending|approved|rejected|expired);
  user_sessions table ❌ MISSING; the apply_admin_role_approval trigger UPDATEs auth.users.role AND deletes from user_sessions — OMIT this trigger entirely (Phase 61). Keep the table, indexes, and the update_approvals_updated_at trigger.

position_delegations shape (supabase/functions/positions-delegate/index.ts lines 159-189): id, position_id (→positions),
  delegator_id (auth user), delegate_id, reason text null, expires_at timestamptz null, status text default 'active', created_at.
  Queried by .eq('position_id').eq('delegate_id').eq('status','active').

word_assistant_logs shape (supabase/functions/word-assistant/index.ts line 256): id, user_id, action text,
  input_text text, output_text text, session_id text/uuid, created_at timestamptz default now().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author event_details view migration (035 shape + organizer/country via dossiers)</name>
  <read_first>
    - supabase/migrations/035_create_event_details_view.sql (the appliable base shape — copy its column list)
    - supabase/migrations/037_update_event_details_add_org_country.sql (the UNAPPLIABLE attempt — shows the intended organizer/country columns and the event_attendees LATERAL join pattern; its organizations.name_en / countries.code references are WRONG vs live)
    - frontend/src/pages/events/EventsPage.tsx (lines 160-205 + 230-272 — confirm exactly which view columns are read so the view contract matches)
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md ("Key dependency facts" event_details paragraph + open decision 1)
  </read_first>
  <action>
    Create supabase/migrations/20260610000003_create_event_details_view.sql (BEGIN; ... COMMIT;). Start from the 035 column list verbatim (id, title→title_en/title_ar, type::text, start_time→start_datetime, end_time→end_datetime, location→location_en/location_ar/venue_en/venue_ar, is_virtual, virtual_link, NULL::int max_participants, status with in_progress→ongoing CASE). Then add the organizer/country columns EventsPage reads, sourced correctly for live schema:
    - organizer: LEFT JOIN LATERAL over event_attendees ea (type='organization') joining `dossiers d` (NOT organizations) for names: `d.id AS organizer_id, d.name_en AS organizer_name_en, d.name_ar AS organizer_name_ar`. Join path: `JOIN dossiers d ON d.id = ea.entity_id` (event_attendees.entity_id references the dossier id for org attendees). Order by host-first then created_at, LIMIT 1.
    - country: LEFT JOIN LATERAL over event_attendees ea (type='country') joining `dossiers d` for names + `countries c ON c.id = d.id` for the code: `d.id AS country_id, d.name_en AS country_name_en, d.name_ar AS country_name_ar, c.iso_code_2 AS country_code` (use iso_code_2 — `countries.code` does NOT exist). Order host-first then created_at, LIMIT 1.
    Before finalizing the join paths, VERIFY with MCP execute_sql against staging that event_attendees.entity_id joins to dossiers.id for org/country attendee rows (run `SELECT type, entity_id FROM event_attendees LIMIT 5;` and `SELECT id FROM dossiers WHERE id = '<sampled entity_id>';`). If the live data shows entity_id references the extension table id instead, adjust the join — but names always come from dossiers, never from the extension tables.
    GRANT SELECT ON public.event_details TO authenticated ONLY — do NOT grant to anon (035 granted anon; drop it per security requirement since EventsPage is behind _protected). Header comment: `-- event_details view (events #4): 035 shape + organizer/country from event_attendees→dossiers (037 was unappliable: organizations/countries are extension tables). authenticated-only grant. Applied 2026-06-10.`
  </action>
  <verify>
    <automated>grep -c 'organizer_name_en\|country_name_en\|GRANT SELECT ON public.event_details TO authenticated' supabase/migrations/20260610000003_create_event_details_view.sql  # expect ≥3</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20260610000003_create_event_details_view.sql exists with a single CREATE OR REPLACE VIEW public.event_details.
    - View selects organizer_id, organizer_name_en, organizer_name_ar, country_id, country_name_en, country_name_ar, country_code (grep confirms all 7).
    - Names come from `dossiers` (grep `d.name_en AS organizer_name_en` and `d.name_en AS country_name_en` present); `country_code` uses `iso_code_2` (grep `iso_code_2` present, grep `c\.code` count 0).
    - `grep -c 'GRANT SELECT ON public.event_details TO anon' <file>` returns 0 (no anon grant).
    - `grep -c 'GRANT SELECT ON public.event_details TO authenticated' <file>` returns 1.
  </acceptance_criteria>
  <done>event_details migration committed (`feat(60-03): event_details view migration`); build shows `✓ built`.</done>
</task>

<task type="auto">
  <name>Task 2: Author pending_role_approvals migration (enum + table + indexes + RLS, NO auth.users trigger)</name>
  <read_first>
    - supabase/migrations/20251011214943_create_pending_role_approvals.sql (the committed original — copy table + indexes; the apply_admin_role_approval trigger and its function MUST be omitted)
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md ("Key dependency facts" pending_role_approvals paragraph + Migration strategy item 4)
  </read_first>
  <action>
    Create supabase/migrations/20260610000004_create_pending_role_approvals.sql (BEGIN; ... COMMIT;) containing:
    (1) `CREATE TYPE approval_status AS ENUM ('pending','approved','rejected','expired');` guarded by a DO block / `IF NOT EXISTS` pattern so re-apply is safe (e.g. `DO $$ BEGIN IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname='approval_status') THEN CREATE TYPE approval_status AS ENUM (...); END IF; END $$;`).
    (2) The `CREATE TABLE IF NOT EXISTS pending_role_approvals (...)` block copied verbatim from the original (all columns, both CHECK constraints chk_approval_different_approvers and chk_approval_not_self) — it references `user_role` (live ✅) and `approval_status` (created above).
    (3) The three indexes idx_approvals_user, idx_approvals_status, idx_approvals_expires_at as `CREATE INDEX IF NOT EXISTS`.
    (4) The `update_approvals_updated_at()` function + `trigger_update_approvals_updated_at` BEFORE UPDATE trigger (these are safe — no auth.users mutation).
    (5) The `expire_pending_approvals()` helper function (safe — only updates this table).
    OMIT ENTIRELY: the `apply_admin_role_approval()` function and `trigger_apply_admin_role` trigger (they UPDATE auth.users.role and DELETE FROM user_sessions which does not exist — deferred to Phase 61).
    (6) Enable RLS and add owner-scoped policies: `ALTER TABLE pending_role_approvals ENABLE ROW LEVEL SECURITY;` + a SELECT policy allowing rows where `user_id = auth.uid() OR requested_by = auth.uid() OR first_approver_id = auth.uid() OR second_approver_id = auth.uid()` (the parties to the approval) and an INSERT policy `WITH CHECK (requested_by = auth.uid())`. Do NOT add broad admin policies here (admin gating is Phase 61).
    Header comment: `-- pending_role_approvals (approvals #6): enum + table + indexes + owner-scoped RLS. apply_admin_role_approval trigger OMITTED (mutates auth.users, depends on missing user_sessions — Phase 61). Applied 2026-06-10.`
  </action>
  <verify>
    <automated>test "$(grep -c 'apply_admin_role_approval\|trigger_apply_admin_role\|user_sessions' supabase/migrations/20260610000004_create_pending_role_approvals.sql)" -eq 0 && grep -c 'ENABLE ROW LEVEL SECURITY' supabase/migrations/20260610000004_create_pending_role_approvals.sql  # expect 1</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20260610000004_create_pending_role_approvals.sql exists.
    - `grep -c 'apply_admin_role_approval' <file>` returns 0 (the auth.users-mutating function omitted).
    - `grep -c 'user_sessions' <file>` returns 0 (no reference to the missing table).
    - `grep -c 'UPDATE auth.users' <file>` returns 0.
    - approval_status enum is created guarded (grep `approval_status` present; creation wrapped so re-apply is safe).
    - `grep -c 'ENABLE ROW LEVEL SECURITY' <file>` returns 1 and at least one CREATE POLICY with `auth.uid()` is present.
  </acceptance_criteria>
  <done>pending_role_approvals migration committed (`feat(60-03): pending_role_approvals table migration`); build shows `✓ built`.</done>
</task>

<task type="auto">
  <name>Task 3: Author position_delegations + word_assistant_logs migration, then [BLOCKING] apply all three migrations via MCP and probe</name>
  <read_first>
    - supabase/functions/positions-delegate/index.ts (lines 159-189 — the exact insert shape for position_delegations)
    - supabase/functions/word-assistant/index.ts (line ~256 — the exact insert shape for word_assistant_logs)
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md ("Key dependency facts" position_delegations + word_assistant_logs + Migration strategy item 5; "Validation Architecture" 3-tables row)
    - supabase/migrations/20260610000003_create_event_details_view.sql and supabase/migrations/20260610000004_create_pending_role_approvals.sql (the two files authored in Tasks 1-2 — needed to apply them)
  </read_first>
  <action>
    First author supabase/migrations/20260610000005_create_delegation_and_word_assistant_tables.sql (BEGIN; ... COMMIT;):
    - `CREATE TABLE IF NOT EXISTS public.position_delegations` with: id uuid PK default gen_random_uuid(), position_id uuid NOT NULL REFERENCES positions(id) ON DELETE CASCADE, delegator_id uuid NOT NULL REFERENCES auth.users(id), delegate_id uuid NOT NULL REFERENCES auth.users(id), reason text, expires_at timestamptz, status text NOT NULL DEFAULT 'active', created_at timestamptz NOT NULL DEFAULT now(). Add index on (position_id, delegate_id, status). Before writing the positions FK, VERIFY positions table + id column exist live via MCP execute_sql (`SELECT 1 FROM information_schema.columns WHERE table_schema='public' AND table_name='positions' AND column_name='id';`) — if positions is absent live, drop the FK and keep position_id as a plain uuid (note the decision in SUMMARY).
    - `CREATE TABLE IF NOT EXISTS public.word_assistant_logs` with: id uuid PK default gen_random_uuid(), user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE, action text NOT NULL, input_text text, output_text text, session_id text, created_at timestamptz NOT NULL DEFAULT now(). Add index on (user_id, created_at).
    - Enable RLS on both with owner-scoped policies: position_delegations SELECT where `delegator_id = auth.uid() OR delegate_id = auth.uid()`, INSERT WITH CHECK `delegator_id = auth.uid()`; word_assistant_logs SELECT/INSERT where `user_id = auth.uid()`.
    - Header comment: `-- position_delegations + word_assistant_logs (approvals #6 / wa #4): tables matching edge-fn write shapes + owner-scoped RLS. Applied 2026-06-10.`
    Commit the file.
    THEN apply all THREE of this plan's migrations via Supabase MCP apply_migration (project_id zkrcjzdemdmwhearhfgg), in order: 20260610000003 (name `create_event_details_view`), 20260610000004 (name `create_pending_role_approvals`), 20260610000005 (name `create_delegation_and_word_assistant_tables`) — each query = the exact committed SQL. After each apply, probe with MCP execute_sql:
    (a) `SELECT 1 FROM pg_views WHERE schemaname='public' AND viewname='event_details';` — 1 row; then `SELECT organizer_name_en, country_code FROM event_details LIMIT 1;` — executes without 42703.
    (b) `SELECT table_name FROM information_schema.tables WHERE table_schema='public' AND table_name IN ('pending_role_approvals','position_delegations','word_assistant_logs') ORDER BY table_name;` — 3 rows.
    (c) `SELECT 1 FROM pg_type WHERE typname='approval_status';` — 1 row.
    (d) `SELECT relrowsecurity FROM pg_class WHERE relname IN ('pending_role_approvals','position_delegations','word_assistant_logs');` — all true.
    If any apply errors, capture the PG error, verify the offending object against live, correct the SQL, re-commit, re-apply.
  </action>
  <verify>
    <automated>grep -c 'CREATE TABLE IF NOT EXISTS public.position_delegations\|CREATE TABLE IF NOT EXISTS public.word_assistant_logs' supabase/migrations/20260610000005_create_delegation_and_word_assistant_tables.sql  # expect 2</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20260610000005_create_delegation_and_word_assistant_tables.sql exists with both CREATE TABLE statements (grep count 2) and both tables RLS-enabled with auth.uid() policies.
    - position_delegations columns match the edge-fn insert: position_id, delegator_id, delegate_id, reason, expires_at, status (grep confirms all 6).
    - word_assistant_logs columns match: user_id, action, input_text, output_text, session_id (grep confirms all 5).
    - All three migrations applied via MCP without error.
    - Probe (a): event_details view exists AND `SELECT organizer_name_en, country_code FROM event_details LIMIT 1` runs without 42703.
    - Probe (b): returns exactly 3 rows (pending_role_approvals, position_delegations, word_assistant_logs).
    - Probe (c): approval_status enum exists (1 row).
    - Probe (d): all three tables report relrowsecurity = true.
  </acceptance_criteria>
  <done>Third migration committed (`feat(60-03): position_delegations + word_assistant_logs tables`); all three migrations applied; probes (a)-(d) pass; outputs in SUMMARY; build shows `✓ built`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → live staging DB | New view + tables created on operational DB via MCP |
| authenticated user → new RLS-scoped tables | Owner-scoped reads/writes on approvals, delegations, logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-03-01 | Elevation of Privilege | pending_role_approvals trigger mutating auth.users.role | mitigate | The apply_admin_role_approval trigger is OMITTED entirely (verified by grep gate = 0). Role-apply mechanics deferred to Phase 61's role-source unification. |
| T-60-03-02 | Information Disclosure | event_details view exposed to anon | mitigate | GRANT to authenticated ONLY; anon grant from old 035 is dropped (grep gate asserts no anon grant). EventsPage is behind _protected. |
| T-60-03-03 | Tampering | spoofed delegation/approval rows | mitigate | INSERT policies use WITH CHECK (delegator_id/requested_by = auth.uid()); SELECT policies scope to parties only. No broad authenticated-all policy. |
| T-60-03-04 | Information Disclosure | word_assistant_logs leak other users' text | mitigate | RLS SELECT/INSERT scoped to user_id = auth.uid(); no cross-user read. |
| T-60-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (SQL-only migrations). |
</threat_model>

<verification>
- All three migration files exist with the asserted CREATE statements and RLS policies.
- MCP probes (a)-(d) confirm event_details view (with organizer/country cols) + 3 tables + approval_status enum live, all RLS-enabled.
- `pnpm --filter intake-frontend build` shows `✓ built` after each commit.
</verification>

<success_criteria>
- event_details, pending_role_approvals, position_delegations, word_assistant_logs all live on staging (ready for type regen in 60-04).
- No omitted-by-design object (auth.users trigger) is present; no anon grant on event_details.
</success_criteria>

<output>
Create `.planning/phases/60-schema-type-truth-restoration/60-03-SUMMARY.md` when done, recording probes (a)-(d) verbatim and any FK-omission decisions.
</output>
