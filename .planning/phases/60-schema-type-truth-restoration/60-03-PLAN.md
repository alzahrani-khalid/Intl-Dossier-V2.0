---
phase: 60-schema-type-truth-restoration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260610000003_create_event_details_view.sql
  - supabase/migrations/009_data_library.sql
autonomous: true
requirements: [P1]
must_haves:
  truths:
    - 'event_details view exists live with the columns EventsPage reads (title_en/ar, start_datetime, venue_*, location_*, status, max_participants, organizer_name_en/ar, organizer_id, country_name_en/ar, country_id, country_code), granted to authenticated only'
    - 'Selecting the organizer/country columns from event_details on staging runs without 42703 (undefined column)'
    - '009_data_library.sql carries a SUPERSEDED header marking it never-applied, with its CREATE TABLE body unchanged and the live data_library_items table untouched'
  artifacts:
    - path: 'supabase/migrations/20260610000003_create_event_details_view.sql'
      provides: 'event_details view in 035 shape + organizer/country columns sourced from event_attendees -> dossiers'
      contains: 'CREATE OR REPLACE VIEW public.event_details'
    - path: 'supabase/migrations/009_data_library.sql'
      provides: 'SUPERSEDED header marking the dead never-applied data-library schema'
      contains: 'SUPERSEDED'
  key_links:
    - from: 'frontend/src/pages/events/EventsPage.tsx'
      to: 'public.event_details'
      via: 'from event_details select star rendering organizer_name and country_name'
      pattern: 'event_details'
---

<objective>
Bring the `event_details` view live (absent live; the committed 035/037 migrations are unappliable as-is) so EventsPage loads against a real view, and mark the dead `009_data_library.sql` superseded so future agents do not "fix" the data-library drift backwards.

The view must match the contract EventsPage already consumes: it does `.from('event_details').select('*')` (line 265) and renders organizer + country names that the appliable 035 shape lacks. The 037 attempt is unappliable because it joins `organizations.name_en` / `countries.code` — columns that do NOT exist on the dossier-extension tables (names live on `dossiers`; `countries` has `iso_code_2`, not `code`).

Purpose: Close the "missing view" drift for events [events #4] and lock down the dual-009 data-library drift [data-library #1].
Output: event_details view migration applied + committed; 009_data_library.sql superseded marker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-schema-type-truth-restoration/60-CONTEXT.md
@.planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md
@.planning/phases/60-schema-type-truth-restoration/60-PATTERNS.md
@supabase/migrations/035_create_event_details_view.sql
@supabase/migrations/037_update_event_details_add_org_country.sql
@frontend/src/pages/events/EventsPage.tsx

<process_rules>
LOCKED process rules (CONTEXT.md "Process rules"):

- Migrations applied via Supabase MCP `apply_migration` ONLY (project_id `zkrcjzdemdmwhearhfgg`). NEVER `supabase db push`, NEVER raw psql. ALSO commit the identical SQL.
- VERIFY every join path / column claim against staging SQL before acting.
- After every commit run `pnpm --filter intake-frontend build` directly.
- `pnpm exec`, never `npx`. NO `timeout` command on this Mac.
- Commit convention: `feat(60-03): ...` / `chore(60-03): ...`, no AI attribution.
  </process_rules>

<interfaces>
<!-- VERIFIED live facts (RESEARCH matrix + "Key dependency facts") + source read. -->
EventsPage.tsx does from event_details select star (line 265). The query uses start_datetime (line 267), type (276), title_en/title_ar (272). The list view RENDERS (lines 75, 104, 141-175, 230-251):
  title_en/title_ar, location_en/location_ar, venue_en/venue_ar, is_virtual, virtual_link, max_participants, status,
  organizer_name_en/organizer_name_ar, organizer_id, country_name_en/country_name_ar, country_id.
035 view shape (appliable as-is) covers events.* mapped columns EXCEPT organizer_*/country_*.
037 is UNAPPLIABLE: live organizations (id, org_code, org_type) and countries (id, iso_code_2, iso_code_3) are dossier-EXTENSION tables — names live on dossiers (name_en/name_ar). countries has iso_code_2 (NOT code).
event_attendees columns: event_id, entity_id, type, role, created_at (live).
events columns: id, title, type, start_time, end_time, location, virtual_link, status (live). 035 maps title->title_en, start_time->start_datetime etc. The executor MUST confirm the LIVE events columns via information_schema before finalizing the mapping (the page reads title_en/ar, venue_en/ar, location_en/ar — verify whether events stores bilingual columns live or whether 035's single-column mapping feeds title_en/ar).

data-library: 009_create_data_library_items.sql matches live (title_en/title_ar bilingual). 009_data_library.sql (title_en/file_url/category schema) is NOT live -> mark superseded via header comment (lowest blast radius, RESEARCH open decision 2). Do NOT touch the live data_library_items table.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author event_details view migration, apply via MCP, probe organizer/country columns (BLOCKING apply)</name>
  <read_first>
    - supabase/migrations/035_create_event_details_view.sql (the appliable base shape — copy its column list + the BEGIN/COMMIT + GRANT template)
    - supabase/migrations/037_update_event_details_add_org_country.sql (the UNAPPLIABLE attempt — shows the intended organizer/country columns + the event_attendees LATERAL join pattern; its organizations.name_en / countries.code references are WRONG vs live)
    - frontend/src/pages/events/EventsPage.tsx (lines 141-175 + 230-272 — confirm exactly which view columns are read so the view contract matches)
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md ("Key dependency facts" event_details paragraph + open decision 1)
    - .planning/phases/60-schema-type-truth-restoration/60-PATTERNS.md ("View migrations" section — full view migration template + dossier-join vs NULL-placeholder note)
  </read_first>
  <action>
    First, VERIFY the live events columns with MCP execute_sql: SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='events' ORDER BY column_name. Map every column EventsPage reads to a real source. Then create supabase/migrations/20260610000003_create_event_details_view.sql (BEGIN; ... COMMIT;) starting from the 035 column list and adding the organizer/country columns EventsPage reads, sourced correctly for live schema:
    - Base columns per 035 (adjusted to the verified live events columns): id, title->title_en/title_ar, type::text, start_time->start_datetime, end_time->end_datetime, location->location_en/location_ar, venue_en/venue_ar, is_virtual, virtual_link, max_participants, status with the in_progress->ongoing CASE. If a bilingual column EventsPage reads (e.g. venue_ar) has no live source column, emit it as NULL::text AS venue_ar (NULL placeholder) and note it in the SUMMARY — never invent a join to a nonexistent column.
    - organizer: LEFT JOIN LATERAL over event_attendees ea (type=organization) joining dossiers d (NOT organizations) for names: d.id AS organizer_id, d.name_en AS organizer_name_en, d.name_ar AS organizer_name_ar. Order role/host-first then created_at, LIMIT 1.
    - country: LEFT JOIN LATERAL over event_attendees ea (type=country) joining dossiers d for names + countries c ON c.id = d.id for the code: d.id AS country_id, d.name_en AS country_name_en, d.name_ar AS country_name_ar, c.iso_code_2 AS country_code (use iso_code_2 — countries.code does NOT exist). Order, LIMIT 1.
    Before finalizing the join paths, VERIFY with MCP execute_sql that event_attendees.entity_id joins to dossiers.id for org/country attendee rows (SELECT type, entity_id FROM event_attendees LIMIT 5, then SELECT id FROM dossiers WHERE id = sampled-entity_id). If live data shows entity_id references the extension-table id, adjust the join — but names ALWAYS come from dossiers, never from the extension tables.
    GRANT SELECT ON public.event_details TO authenticated ONLY — do NOT grant to anon (035 granted anon; drop it per security requirement, EventsPage is behind _protected). Header comment: event_details view (events #4): 035 shape + organizer/country from event_attendees->dossiers (037 was unappliable: organizations/countries are extension tables, countries.code does not exist); authenticated-only grant; applied 2026-06-10.
    Commit feat(60-03): event_details view (035 shape + organizer/country via dossiers), run pnpm --filter intake-frontend build.

    THEN [BLOCKING] apply via MCP apply_migration (project_id zkrcjzdemdmwhearhfgg, name create_event_details_view, query = exact committed SQL). Probe with MCP execute_sql:
    (a) SELECT 1 FROM pg_views WHERE schemaname='public' AND viewname='event_details' — 1 row.
    (b) SELECT organizer_name_en, organizer_id, country_name_en, country_id, country_code, title_en, start_datetime, status FROM event_details LIMIT 1 — executes WITHOUT 42703 (asserts every EventsPage-read column resolves).
    If apply errors, capture the PG error, verify the offending column live, correct the SQL, re-commit, re-apply.

  </action>
  <verify>
    <automated>grep -c "CREATE OR REPLACE VIEW public.event_details" supabase/migrations/20260610000003_create_event_details_view.sql</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20260610000003_create_event_details_view.sql exists with exactly one CREATE OR REPLACE VIEW public.event_details.
    - View selects organizer_id, organizer_name_en, organizer_name_ar, country_id, country_name_en, country_name_ar, country_code (grep confirms all 7 aliases present).
    - Names come from dossiers (grep finds d.name_en AS organizer_name_en and d.name_en AS country_name_en); country_code uses iso_code_2 (grep finds iso_code_2; grep for c.code returns 0).
    - grep for GRANT SELECT ON public.event_details TO anon returns 0 (no anon grant); grep for GRANT SELECT ON public.event_details TO authenticated returns 1.
    - MCP apply_migration completed without error; probe (a) returns 1 row; probe (b) executes WITHOUT 42703.
    - Any NULL-placeholder columns (no live source) are listed in the SUMMARY.
    - pnpm --filter intake-frontend build exits 0.
  </acceptance_criteria>
  <done>event_details view migration committed + applied; organizer/country columns resolve live; probes (a)-(b) pass; NULL placeholders (if any) recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Mark 009_data_library.sql superseded (header comment only)</name>
  <read_first>
    - supabase/migrations/009_data_library.sql (the dead migration — its title_en/file_url/category CREATE TABLE; add a header comment ONLY, do NOT alter the body)
    - supabase/migrations/009_create_data_library_items.sql (the LIVE schema — confirm it is the bilingual title_en/title_ar shape matching live)
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md (data_library_items matrix row + open decision 2: header comment is lowest blast radius)
    - .planning/phases/60-schema-type-truth-restoration/60-PATTERNS.md ("Superseding a dead migration" section — the first-line header style to mirror)
  </read_first>
  <action>
    Prepend a header comment to supabase/migrations/009_data_library.sql reading: SUPERSEDED (data-library #1): this title_en/file_url/category schema was NEVER applied to staging. The LIVE data_library_items schema is created by 009_create_data_library_items.sql (bilingual title_en/title_ar). Do NOT apply this file or fix the drift backwards. Kept for git history only. Marked 2026-06-10.
    Do NOT modify the SQL body, do NOT delete the file, do NOT touch the live data_library_items table. This is a comment-only edit. Commit chore(60-03): mark dead 009_data_library.sql superseded, run pnpm --filter intake-frontend build.
  </action>
  <verify>
    <automated>grep -c "SUPERSEDED" supabase/migrations/009_data_library.sql</automated>
  </verify>
  <acceptance_criteria>
    - grep -c SUPERSEDED supabase/migrations/009_data_library.sql returns >= 1.
    - The file's CREATE TABLE body is unchanged: grep -c "CREATE TABLE IF NOT EXISTS public.data_library_items" still returns 1 and the title_en/file_url/category column list is byte-unchanged from before.
    - The file is NOT deleted and the live data_library_items table was NOT altered (no MCP mutation in this task).
    - pnpm --filter intake-frontend build exits 0.
  </acceptance_criteria>
  <done>009_data_library.sql carries the SUPERSEDED header; body and live table untouched; committed.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                      |
| ------------------------------- | ---------------------------------------------------------------- |
| migration SQL → live staging DB | event_details view created on the operational staging DB via MCP |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                                  | Disposition | Mitigation Plan                                                                                                                                                                                                                |
| ---------- | ---------------------- | -------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-60-03-I  | Information Disclosure | event_details view exposed to anon                                         | mitigate    | GRANT to authenticated ONLY; the anon grant from old 035 is dropped (grep gate asserts no anon grant). EventsPage is behind \_protected.                                                                                       |
| T-60-03-T  | Tampering              | view selects a nonexistent/wrong-table column and silently breaks the page | mitigate    | Live events columns + event_attendees->dossiers join path verified via execute_sql BEFORE finalizing; probe (b) selects every EventsPage-read column and must not 42703; NULL placeholders are explicit, never invented joins. |
| T-60-03-R  | Repudiation            | future agent fixes the dead 009 drift backwards                            | mitigate    | 009_data_library.sql SUPERSEDED header documents never-applied status + points to the live-creating migration; body unchanged so git history is intact.                                                                        |
| T-60-03-SC | Tampering              | npm/pip/cargo installs                                                     | accept      | No package installs in this plan (SQL migration + comment edit only).                                                                                                                                                          |

</threat_model>

<verification>
- event_details view exists live (probe a) and resolves every EventsPage-read column (probe b, no 42703); authenticated-only grant.
- 009_data_library.sql carries SUPERSEDED header with body unchanged; live table untouched.
- pnpm --filter intake-frontend build green after each commit.
</verification>

<success_criteria>

- event_details is live on staging with the organizer/country contract EventsPage consumes (ready for type regen in 60-05).
- The dual-009 data-library drift is locked: the dead file is marked, the live schema is authoritative.
  </success_criteria>

<output>
Create `.planning/phases/60-schema-type-truth-restoration/60-03-SUMMARY.md` when done, recording probes (a)-(b) verbatim and any NULL-placeholder column decisions.
</output>
