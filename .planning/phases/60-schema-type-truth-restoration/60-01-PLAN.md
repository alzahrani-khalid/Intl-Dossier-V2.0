---
phase: 60-schema-type-truth-restoration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260610000001_capture_unified_work_stack.sql
autonomous: true
requirements: [P1]
must_haves:
  truths:
    - 'The unified work stack (unified_work_items view + user_work_summary view + user_productivity_metrics matview + 3 RPCs) exists as a committed forward migration that reproduces the live staging objects'
    - 'Applying the capture migration to staging is an idempotent no-op (objects already live) and records the migration in staging migration history'
    - 'After apply, all 3 RPCs and both views/matview return rows-or-empty cleanly (no 42883 / 42P01 / 42703)'
  artifacts:
    - path: 'supabase/migrations/20260610000001_capture_unified_work_stack.sql'
      provides: 'Forward migration recreating unified_work_items, user_work_summary, user_productivity_metrics (+ indexes/refresh), get_unified_work_items, get_user_work_summary, get_user_productivity_metrics'
      contains: 'CREATE OR REPLACE VIEW public.unified_work_items'
      min_lines: 40
  key_links:
    - from: 'supabase/migrations/20260610000001_capture_unified_work_stack.sql'
      to: 'live staging catalog (pg_proc / pg_views / pg_matviews)'
      via: 'definitions captured via pg_get_viewdef / pg_get_functiondef / pg_matviews.definition'
      pattern: 'get_unified_work_items'
---

<objective>
Capture the live unified-work-item stack from staging into a single committed forward migration so the repo reproduces what the my-work UI already reads. Today `unified_work_items` (view), `user_work_summary` (view), `user_productivity_metrics` (matview), and the three RPCs `get_unified_work_items` / `get_user_work_summary` / `get_user_productivity_metrics` exist live and are typed in generated types, but NO repo migration creates them (RESEARCH matrix rows 1-6). This plan dumps the live definitions, commits them as one dated migration, and applies it via MCP (idempotent against live).

Purpose: Close the "live-only object" drift for the my-work surface [my-work #3] so the schema is reproducible from migrations.
Output: `supabase/migrations/20260610000001_capture_unified_work_stack.sql`, applied to staging and verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-schema-type-truth-restoration/60-CONTEXT.md
@.planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md
@.planning/phases/60-schema-type-truth-restoration/60-PATTERNS.md

<process_rules>
LOCKED process rules (CONTEXT.md "Process rules"):

- Migrations applied via Supabase MCP `apply_migration` ONLY (project_id `zkrcjzdemdmwhearhfgg`). NEVER `supabase db push`, NEVER raw psql. ALSO commit the identical SQL to `supabase/migrations/`.
- VERIFY every DB claim against staging SQL (MCP `execute_sql`) before acting — round-1 WG claim was a false positive.
- After every commit run `pnpm --filter intake-frontend build` directly (the pre-commit hook does NOT block on failure).
- `pnpm exec`, never `npx`. There is NO `timeout` command on this Mac — never wrap commands in `timeout`.
- Commit convention: `feat(60-01): ...`, no AI attribution.
  </process_rules>

<interfaces>
<!-- Live object names the capture must recreate (verified live, RESEARCH matrix). -->
- Views: public.unified_work_items, public.user_work_summary
- Materialized view: public.user_productivity_metrics (+ any unique index + refresh function)
- Functions: public.get_unified_work_items, public.get_user_work_summary, public.get_user_productivity_metrics
- All three RPCs are present in current frontend/src/types/database.types.ts Functions block (confirms live signatures).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dump live unified-work-stack definitions via MCP execute_sql</name>
  <files>supabase/migrations/20260610000001_capture_unified_work_stack.sql (captured here; assembled in Task 2)</files>
  <read_first>
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md (matrix rows 1-6 + "Migration strategy" item 1 — the exact capture recipe)
    - .planning/phases/60-schema-type-truth-restoration/60-PATTERNS.md ("View migrations" section — capture-from-live notes: CREATE OR REPLACE VIEW / CREATE MATERIALIZED VIEW IF NOT EXISTS / CREATE OR REPLACE FUNCTION + include matview unique index)
    - frontend/src/types/database.types.ts (locate the get_unified_work_items / get_user_work_summary / get_user_productivity_metrics Functions entries to confirm return shapes and argument signatures before capturing)
  </read_first>
  <action>
    Using Supabase MCP execute_sql against project_id zkrcjzdemdmwhearhfgg, capture the canonical DDL for every object in the stack. Run, in order, and save the raw output text for the migration:
    (1) pg_get_viewdef('public.user_work_summary'::regclass, true) — dependent view, capture first.
    (2) pg_get_viewdef('public.unified_work_items'::regclass, true).
    (3) SELECT definition FROM pg_matviews WHERE schemaname='public' AND matviewname='user_productivity_metrics'.
    (4) SELECT indexdef FROM pg_indexes WHERE schemaname='public' AND tablename='user_productivity_metrics' — capture ALL index rows (a UNIQUE index is required for CONCURRENT refresh).
    (5) For each RPC: SELECT p.proname, pg_get_functiondef(p.oid) FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace WHERE n.nspname='public' AND p.proname IN ('get_unified_work_items','get_user_work_summary','get_user_productivity_metrics').
    (6) Probe for a refresh helper: SELECT proname FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace WHERE n.nspname='public' AND proname ILIKE '%refresh%productivity%' — if one exists, capture its functiondef too.
    Do NOT hand-write any definition; the live DDL is the source of truth. Resolve ordering by dependency (user_work_summary and user_productivity_metrics are dependencies of the RPCs per RESEARCH, so they must be created before the functions that read them). Record each function's exact argument signature from the dumped CREATE for the smoke-call in Task 2.
  </action>
  <verify>
    <automated>MISSING — capture is interactive via MCP; proof is captured in Task 2 (file exists with all object names + live probes pass). Record each execute_sql output verbatim in the SUMMARY.</automated>
  </verify>
  <acceptance_criteria>
    - execute_sql returned a non-empty definition string for unified_work_items, user_work_summary, user_productivity_metrics, and all three RPCs (6 objects minimum).
    - The pg_indexes probe for user_productivity_metrics returned at least one index row; any UNIQUE index is captured for inclusion.
    - No object returned 42P01 "relation does not exist" — all six are confirmed live.
    - Each RPC argument signature is recorded for the Task 2 smoke-call.
  </acceptance_criteria>
  <done>All six object definitions plus matview indexes captured as raw DDL text, ready to assemble into the migration file.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble, commit, and apply the capture migration; probe live (BLOCKING apply)</name>
  <files>supabase/migrations/20260610000001_capture_unified_work_stack.sql</files>
  <read_first>
    - supabase/migrations/035_create_event_details_view.sql (the whole-file BEGIN/COMMIT + COMMENT ON + GRANT template to mirror)
    - supabase/migrations/20260531120000_fix_get_team_workload_email_text_cast.sql (the corrective multi-line `--` header style for the top of the new file)
    - .planning/phases/60-schema-type-truth-restoration/60-RESEARCH.md ("Migration strategy" item 1 + "Validation Architecture": object-existence + RPC smoke-call assertions)
  </read_first>
  <action>
    Create supabase/migrations/20260610000001_capture_unified_work_stack.sql wrapping the captured DDL in one transaction (BEGIN; ... COMMIT;). Open with a multi-line `--` header stating: these 6 objects exist live + in generated types but had NO creating migration (live-only drift, my-work #3); this migration captures the live definitions so the repo is reproducible; idempotent no-op against live; applied 2026-06-10. Use idempotent forms: CREATE OR REPLACE VIEW public.user_work_summary, then CREATE OR REPLACE VIEW public.unified_work_items, then CREATE MATERIALIZED VIEW IF NOT EXISTS public.user_productivity_metrics, then each captured index as CREATE UNIQUE INDEX IF NOT EXISTS / CREATE INDEX IF NOT EXISTS, then each CREATE OR REPLACE FUNCTION public.<name>(...) body VERBATIM from the dump (these already carry SECURITY DEFINER + SET search_path — keep those lines, do NOT strip them), then the refresh helper if found. Order: dependent view → main view → matview → indexes → functions → refresh fn. Preserve any GRANT the dumped function bodies carry; if the dump did not include grants, add GRANT SELECT ON public.unified_work_items TO authenticated, GRANT SELECT ON public.user_work_summary TO authenticated, and GRANT EXECUTE on the three functions to authenticated (authenticated-only, no anon).

    Commit `feat(60-01): capture unified work stack as forward migration from live staging`, then run pnpm --filter intake-frontend build.

    THEN [BLOCKING] apply via MCP apply_migration (project_id zkrcjzdemdmwhearhfgg, name capture_unified_work_stack, query = the exact committed SQL). Because objects already exist live, expect a no-op that records migration history. Probe with MCP execute_sql:
    (1) SELECT proname FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace WHERE n.nspname='public' AND proname IN ('get_unified_work_items','get_user_work_summary','get_user_productivity_metrics') ORDER BY proname — expect 3 rows.
    (2) SELECT viewname FROM pg_views WHERE schemaname='public' AND viewname IN ('unified_work_items','user_work_summary') — expect 2 rows.
    (3) SELECT matviewname FROM pg_matviews WHERE schemaname='public' AND matviewname='user_productivity_metrics' — expect 1 row.
    (4) Smoke-call each RPC with its captured signature (NULL-safe args), e.g. SELECT * FROM get_user_work_summary(NULL) LIMIT 1 — must return rows/empty WITHOUT 42883/42703.
    If apply_migration errors (e.g. a captured grant references a role that differs live), reconcile the SQL, re-commit, and re-apply — do not leave the file and live state divergent.

  </action>
  <verify>
    <automated>grep -vE '^[[:space:]]*--' supabase/migrations/20260610000001_capture_unified_work_stack.sql | grep -cE 'CREATE OR REPLACE FUNCTION public\.get_(unified_work_items|user_work_summary|user_productivity_metrics)'</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20260610000001_capture_unified_work_stack.sql exists with a single BEGIN and COMMIT pair.
    - grep -c 'CREATE OR REPLACE VIEW public.unified_work_items' on the file returns 1.
    - grep -c 'CREATE MATERIALIZED VIEW IF NOT EXISTS public.user_productivity_metrics' on the file returns 1.
    - The non-comment grep gate above returns 3 (all three CREATE OR REPLACE FUNCTION public.get_* present).
    - Each captured RPC body still contains a SET search_path line (definer functions are not stripped of their search_path pin).
    - MCP apply_migration completed without error and the migration appears in staging migration history.
    - Probe (1) returns exactly 3 rows; probe (2) returns exactly 2 rows; probe (3) returns exactly 1 row.
    - At least one RPC smoke-call returned rows/empty WITHOUT 42883 or 42703.
    - pnpm --filter intake-frontend build exits 0 (shows built).
  </acceptance_criteria>
  <done>Migration committed, applied to staging (idempotent), all 6 objects confirmed live-callable; probe outputs recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                             |
| ------------------------------- | ----------------------------------------------------------------------- |
| migration SQL → live staging DB | Author-supplied DDL applied to the operational staging database via MCP |

## STRIDE Threat Register

| Threat ID  | Category               | Component                             | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                         |
| ---------- | ---------------------- | ------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-60-01-E  | Elevation of Privilege | get\_\* RPCs are SECURITY DEFINER     | mitigate    | Capture-from-live preserves the EXISTING definer + SET search_path lines verbatim from pg_get_functiondef; this plan does NOT alter privilege or search_path — it reproduces what already runs live. Acceptance criteria assert each body still contains SET search_path before commit. |
| T-60-01-I  | Information Disclosure | view/RPC GRANTs                       | accept      | Objects are reproduced with the same authenticated-only grants already live; no widening (these objects already serve the my-work UI). No anon grant added.                                                                                                                             |
| T-60-01-T  | Tampering              | migration body diverges from live def | mitigate    | Body is the verbatim pg*get*\* dump (no hand-edits); apply is idempotent CREATE OR REPLACE, so committed SQL == live def by construction. Probe-after-apply confirms all objects still present.                                                                                         |
| T-60-01-SC | Tampering              | npm/pip/cargo installs                | accept      | No package installs in this plan (SQL + MCP only).                                                                                                                                                                                                                                      |

</threat_model>

<verification>
- supabase/migrations/20260610000001_capture_unified_work_stack.sql exists and contains all 6 object definitions.
- MCP probes confirm 3 RPCs, 2 views, 1 matview live after apply; at least one RPC smoke-call clean.
- pnpm --filter intake-frontend build shows built after the commit.
</verification>

<success_criteria>

- Unified work stack is reproducible from a committed migration applied to staging.
- The migration is recorded in staging migration history and is an idempotent no-op.
  </success_criteria>

<output>
Create `.planning/phases/60-schema-type-truth-restoration/60-01-SUMMARY.md` when done, recording the four probe outputs verbatim.
</output>
