---
phase: 78-heroui-v3-api-audit-bump
plan: 04
type: execute
wave: 4
depends_on: [78-01, 78-02, 78-03]
files_modified: []
autonomous: true
requirements: [HEROUI-01]

must_haves:
  truths:
    - 'Production build succeeds on 3.2.1'
    - 'ALL size-limit budgets are green (Bundle Size Check is a REQUIRED CI gate)'
    - 'Dashboard + AppShell drawer + TweaksDrawer render in EN with zero console errors'
    - 'The same surfaces render in AR (html[dir=rtl], Tajawal computed on visible text) with zero console errors'
    - 'The one expected visual delta — Drawer bodies gaining thin scrollbars — is pre-recorded as intentional and NOT flagged as a regression'
  artifacts:
    - path: 'frontend/dist/index.html'
      provides: 'Successful 3.2.1 production build output'
  key_links:
    - from: 'frontend/.size-limit.json budgets'
      to: 'vite chunking (vite.config.ts manualChunks)'
      via: 'react-aria externalization moves weight from heroui-vendor to the vendor catch-all; only Total JS sees it'
      pattern: 'heroui-vendor'
---

<objective>
Final gate for HEROUI-01: prove the bumped tree builds, holds every REQUIRED bundle budget, and the live HeroUI runtime surface (the drawers — the only high-traffic component whose runtime code materially changed) renders in EN and AR with zero console errors. Lightweight smoke only — the formal full-route visual re-compare is Phase 80's job.

Purpose: ROADMAP success criterion 3 ("all routes using HeroUI components render without regression in both EN and AR") at smoke depth, plus the Bundle Size Check that has previously failed PRs with hidden stacked breaches.

Output: green build + size run, recorded EN/AR smoke evidence in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-heroui-v3-api-audit-bump/78-RESEARCH.md
@.planning/phases/78-heroui-v3-api-audit-bump/78-VALIDATION.md
@frontend/.size-limit.json

Environment facts:

- Run from the MAIN checkout (deps installed; worktrees share no `frontend/node_modules`).
- Dev server: `pnpm --dir frontend dev` on :5173 (talks to the staging Supabase directly). macOS AirPlay owns port 5000 — if the backend proxy is needed, it runs on PORT=5001, but login + dashboard reads work against staging without it.
- Login creds: `$TEST_USER_EMAIL` / `$TEST_USER_PASSWORD` from `.env.test` (never hardcode them in any artifact).
- browser-harness CANNOT attach to the user's Chrome on this Mac — launch a DEDICATED headless Chrome with remote debugging on :9222 and point the harness at it via `BU_CDP_URL` (established pattern). React inputs need native-setter value injection.
- Language flip: the i18n detector reads the querystring FIRST, so `?lng=ar` flips to Arabic at first paint; persisted key is localStorage `id.locale` (NOT `i18nextLng`).
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Production build + size-limit REQUIRED gate on 3.2.1</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/78-heroui-v3-api-audit-bump/78-RESEARCH.md — §Bundle Size (budgets, expected chunk-movement mechanism, hidden-stacked-failures warning)
    - frontend/.size-limit.json (the budget table: Initial JS 476 KB, heroui-vendor 9 KB, Total JS 2.78 MB, etc.)
  </read_first>
  <action>
    Run `pnpm --dir frontend build` — must exit 0 with `frontend/dist/index.html` produced. Then run `pnpm --dir frontend size` — every budget must pass.

    Pre-recorded EXPECTED chunk movement (RESEARCH §Bundle Size, assumption A2): 3.2.1 stopped vendoring react-aria, so those imports now resolve to the external `react-aria`/`react-aria-components` packages, which match NO named manualChunks rule and fall into the `vendor` catch-all — `heroui-vendor` shrinks or holds at/below its 9 KB gzip budget, and only `Total JS` (2.78 MB) absorbs the shift. Record the actual per-budget numbers in the SUMMARY (they become Phase 80's baseline).

    If size-limit fails: grep the FULL output for EVERY `exceeded` line before concluding anything — failures stack hidden (a single visible breach can mask others). A breach caused purely by the react-aria chunk movement in a NON-heroui budget is a real finding: record the exact numbers and surface it as a blocker in the SUMMARY (budget edits are a deliberate decision, not an auto-fix); do not silently bump `.size-limit.json`.

    Also run `pnpm --dir frontend lint` once — it includes `check-bootstrap-parity.mjs` (the three-copy Linear token byte-match) and the i18n/RTL guards; this phase touches none of those inputs, so it must stay green (CONTEXT.md integration point).

    Rollback reference if the build itself breaks on 3.2.1: `git revert <78-01 bump sha> && pnpm install && pnpm --dir frontend type-check && pnpm --dir frontend build` (RESEARCH §Rollback — never partial).

  </action>
  <verify>
    <automated>pnpm --dir frontend build && pnpm --dir frontend size && pnpm --dir frontend lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir frontend build` exits 0 and `frontend/dist/index.html` exists
    - `pnpm --dir frontend size` exits 0 (ALL budgets green, none skipped)
    - Actual heroui-vendor and Total JS gzip numbers recorded in the SUMMARY alongside their budgets
    - `pnpm --dir frontend lint` exits 0 (bootstrap-parity guard green)
  </acceptance_criteria>
  <done>Build green, every REQUIRED bundle budget green, chunk movement recorded against the predicted mechanism.</done>
</task>

<task type="auto">
  <name>Task 2: EN + AR drawer/route render smoke with zero console errors</name>
  <files>(none — session procedure; evidence recorded in SUMMARY)</files>
  <read_first>
    - .planning/phases/78-heroui-v3-api-audit-bump/78-RESEARCH.md — §Regression-Sweep Tooling (browser-smoke row + "Smoke surface" paragraph), §CSS Delta (the drawer scrollbar rule)
    - .planning/phases/78-heroui-v3-api-audit-bump/78-VALIDATION.md — "Manual-Only Verifications" row (the exact procedure this task automates)
  </read_first>
  <action>
    Automate the VALIDATION.md smoke procedure via browser-harness against a dedicated headless Chrome (:9222 / `BU_CDP_URL` pattern — the harness cannot attach to the user's Chrome):

    1. Start the frontend dev server (`pnpm --dir frontend dev`, :5173) if not already running; launch the dedicated headless Chrome.
    2. EN pass: navigate to `http://localhost:5173`, log in with `$TEST_USER_EMAIL`/`$TEST_USER_PASSWORD` from `.env.test`, load the dashboard, open the AppShell drawer and the TweaksDrawer (the ONLY high-traffic HeroUI surfaces whose runtime materially changed: react-aria externalized + RAC 1.17→1.18 + the drawer.css scrollbar rule). Capture the console log — assert ZERO console errors (warnings from pre-existing tooling are recorded but not blocking). Screenshot each open drawer.
    3. AR pass: reload with `http://localhost:5173/?lng=ar` (querystring detector flips language at first paint). Assert `document.documentElement.dir === 'rtl'`, assert the computed `font-family` on a visible primary button or body text includes `Tajawal`, re-open both drawers, capture console — again zero errors. Screenshot each open drawer.
    4. PRE-RECORDED EXPECTED DELTA (do NOT flag as regression, do NOT add app CSS to undo it): Drawer bodies now show thin scrollbars — `@heroui/styles` 3.2.1 adds the `scrollbar` utility to the drawer-body rule (`@apply min-h-0 flex-1 scrollbar`; thumb = color-mix foreground 15%). This is the ONE intentional upstream visual change in the range. If design later objects, the supported seam is the `--scrollbar-*` theme vars, not override CSS (RESEARCH §Don't Hand-Roll) — note only, no action this phase.
    5. Record in the SUMMARY: per-pass console-error count (must be 0/0), the dir/font assertions, screenshot paths, and the scrollbar-delta acknowledgment. Buttons/cards/skeletons need no dedicated pass — type-identical and fully re-skinned by app recipes (RESEARCH §Smoke surface).

    Fallback if the harness/headless-Chrome path is unavailable in this session: write the same assertions as a one-off Playwright spec under `frontend/tests/` using the existing `playwright.config.ts` (baseURL :5173, pre-authenticated storageState), run it, and delete or keep per its usefulness — do NOT reuse the stale `theme-visual.spec.ts` (references pre-Linear themes; RESEARCH marks it do-not-reuse).

    No production code changes are expected from this task. If the smoke surfaces a REAL regression (console error tracing to HeroUI, drawer failing to open/dismiss, RTL breakage): stop, record the evidence, and surface as a blocker in the SUMMARY with the rollback reference (`git revert <78-01 bump sha> && pnpm install`) — the fix/rollback decision escalates rather than being improvised here.

  </action>
  <verify>
    <automated>test -f frontend/dist/index.html && pnpm --dir frontend test run ConcurrentDrawers</automated>
    <human-check>SUMMARY smoke record shows: EN console errors = 0, AR console errors = 0, dir=rtl confirmed, Tajawal confirmed, both drawers opened in both languages, scrollbar delta acknowledged as expected</human-check>
  </verify>
  <acceptance_criteria>
    - EN pass recorded: dashboard + AppShell drawer + TweaksDrawer opened, console-error count = 0
    - AR pass recorded: `?lng=ar` first-paint flip, `html[dir=rtl]` asserted true, computed font includes Tajawal, both drawers opened, console-error count = 0
    - Screenshots captured for both drawers in both languages (paths recorded in SUMMARY)
    - The drawer thin-scrollbar delta is explicitly acknowledged as the expected upstream change — zero app CSS added to counter it
    - `pnpm --dir frontend test run ConcurrentDrawers` exits 0 (re-run binds the jsdom oracle to the final tree)
  </acceptance_criteria>
  <done>EN + AR smoke green with evidence; the phase's "renders without regression" criterion is proven at smoke depth, with the formal full-route re-compare deferred to Phase 80 by design.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary       | Description                                                                                                                         |
| -------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| (none crossed) | Verification-only plan: build, size gate, lint, and a read-only browser smoke against the dev server with existing test credentials |

## STRIDE Threat Register

| Threat ID | Category               | Component           | Disposition                | Mitigation Plan                                                                                                                                                                                                               |
| --------- | ---------------------- | ------------------- | -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-78-01   | Tampering              | dependency versions | accept (out of scope here) | No dependency change in this plan; supply-chain mitigation landed in plan 78-01. The build + size run additionally bounds the blast radius of any unexpected injected code by pinning observed bundle weights against budgets |
| T-78-I    | Information Disclosure | smoke credentials   | mitigate                   | Login uses `$TEST_USER_EMAIL`/`$TEST_USER_PASSWORD` from the git-ignored `.env.test`; no credential is echoed into 78-04 artifacts, screenshots of auth forms, or the SUMMARY                                                 |

**No HIGH-severity app-logic threats:** this plan changes no application code — it verifies build output and rendered behavior.
</threat_model>

<verification>
- `pnpm --dir frontend build` exit 0; `pnpm --dir frontend size` exit 0 (all budgets); `pnpm --dir frontend lint` exit 0
- EN + AR smoke: 0 console errors each, dir=rtl + Tajawal confirmed in AR, drawers functional
- Scrollbar delta recorded as expected, not chased
</verification>

<success_criteria>
ROADMAP success criteria 1-3 all provable at phase close: lockstep 3.2.1 (78-01), Phase 75 confirmation holds (78-03), and routes using HeroUI render without regression in EN and AR (this plan) — with the REQUIRED Bundle Size Check green before any PR.
</success_criteria>

<output>
Create `.planning/phases/78-heroui-v3-api-audit-bump/78-04-SUMMARY.md` when done. Record: per-budget size numbers (Phase 80 baseline), EN/AR smoke evidence (console counts, assertions, screenshot paths), and the scrollbar-delta acknowledgment.
</output>
