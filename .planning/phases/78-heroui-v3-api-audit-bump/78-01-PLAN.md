---
phase: 78-heroui-v3-api-audit-bump
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/package.json, pnpm-lock.yaml]
autonomous: true
requirements: [HEROUI-01]

must_haves:
  truths:
    - 'pnpm-lock.yaml resolves @heroui/react AND @heroui/styles to exactly 3.2.1 with zero 3.0.5 remnants'
    - 'The frontend tree type-checks (exit 0) against the 3.2.1 typings'
    - 'Two real HeroUI Drawers mount with focus containment on the externalized react-aria runtime (ConcurrentDrawers vitest passes)'
    - 'The bump is a single atomic commit touching only frontend/package.json and pnpm-lock.yaml (independently revertible)'
  artifacts:
    - path: 'frontend/package.json'
      provides: 'Exact coupled pins'
      contains: '"@heroui/react": "3.2.1"'
    - path: 'pnpm-lock.yaml'
      provides: 'Single-version resolution for both packages'
      contains: '@heroui/styles@3.2.1'
  key_links:
    - from: 'frontend/src/index.css'
      to: '@heroui/styles subpaths (base, themes/default, utilities, variants, components/drawer.css)'
      via: '@import lines 28-33 resolving through the DIRECT @heroui/styles dependency'
      pattern: '@heroui/styles'
---

<objective>
Bump `@heroui/react` and `@heroui/styles` from 3.0.5 to 3.2.1 **in lockstep** as one atomic, independently-revertible commit, and prove the tree compiles and the highest-traffic HeroUI runtime surface (Drawer, now on externalized react-aria) still mounts.

Purpose: HEROUI-01. The two packages are version-coupled (`@heroui/react@3.2.1` declares `@heroui/styles: 3.2.1` as an exact regular dependency); a mismatched pair is the phase's primary failure mode — two styles copies install and `frontend/src/index.css` serves 3.0.5 CSS to 3.2.1 components. Bonus: `@heroui-pro/react@1.0.0-beta.6` peer-requires `@heroui/react >=3.2.0`, currently violated at 3.0.5 — this bump resolves the peer-range violation.

Output: `frontend/package.json` + root `pnpm-lock.yaml` on 3.2.1, committed atomically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-heroui-v3-api-audit-bump/78-RESEARCH.md
@.planning/phases/78-heroui-v3-api-audit-bump/78-VALIDATION.md
@.planning/phases/78-heroui-v3-api-audit-bump/78-CONTEXT.md

Verified environment facts (2026-07-03):

- The pnpm lockfile is the MONOREPO ROOT `pnpm-lock.yaml` — there is NO `frontend/pnpm-lock.yaml`. All lockfile greps run against the root file.
- `frontend/package.json` lines 44-45 currently pin `"@heroui/react": "3.0.5"` and `"@heroui/styles": "3.0.5"` (exact pins, no caret). `"@heroui-pro/react": "1.0.0-beta.6"` on line 43 is NOT touched.
- `grep -cE "@heroui/(react|styles)@3\.0\.5" pnpm-lock.yaml` currently returns 5 — the post-bump target is 0.
- Node v22+, pnpm 10.29.1 pinned; use `pnpm`, never `npx` (broken on this Mac). `frontend/node_modules` is installed in the MAIN checkout — run all type-check/test/build commands from an environment with deps installed (GSD worktrees share no `frontend/node_modules`; Pitfall 5 in RESEARCH.md).
- Pre-commit hook builds every commit and lint-staged may touch staged files; commit with an explicit pathspec (`git commit -- frontend/package.json pnpm-lock.yaml`) so nothing else is swept in.
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Bump both HeroUI pins to 3.2.1 and verify lockstep lockfile resolution</name>
  <files>frontend/package.json, pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/78-heroui-v3-api-audit-bump/78-RESEARCH.md — sections "Standard Stack", "Version Coupling & Lockfile Verification", "Package Legitimacy Audit", "Rollback"
    - frontend/package.json (lines 40-50 — the three @heroui* pins)
  </read_first>
  <action>
    Sanity-gate the target first: run `npm view @heroui/react version`. Expected `3.2.1` (the audited version, verified against the npm registry and heroui-inc GitHub releases on 2026-07-03 in RESEARCH.md). If a NEWER version has since published, still bump to exactly 3.2.1 — that is the audited target — and note the newer release in the SUMMARY as a follow-up; do NOT bump past the audit.

    Edit `frontend/package.json`: change `"@heroui/react": "3.0.5"` to `"@heroui/react": "3.2.1"` and `"@heroui/styles": "3.0.5"` to `"@heroui/styles": "3.2.1"` (both exact pins, no caret/tilde — the packages are version-coupled per RESEARCH §Version Coupling). Do NOT touch `"@heroui-pro/react": "1.0.0-beta.6"` or any other dependency. Do NOT edit `pnpm-lock.yaml` by hand.

    Run `pnpm install` from the repo root to regenerate the root `pnpm-lock.yaml` and node_modules. Expected transitive changes (documented, NOT anomalies): `react-aria-components` exact-pin moves 1.17.0 → 1.18.0, NEW direct dep `react-aria@3.49.0` appears under `@heroui/react` (3.2.1 stopped vendoring react-aria — upstream PR #6653), plus `@react-aria/*` patch bumps. Peer deps (react >=19, tailwindcss >=4) are already satisfied — no other package.json changes are needed. If pnpm reports any peer warning OTHER than pre-existing ones, record it verbatim in the SUMMARY.

    Then verify lockstep resolution per RESEARCH §Version Coupling:
    1. `grep -E "@heroui/(react|styles)@3\.0\.5" pnpm-lock.yaml` must print NOTHING (exit 1) — zero 3.0.5 remnants.
    2. `grep -cE "@heroui/(react|styles)@3\.2\.1" pnpm-lock.yaml` must be >= 2.
    3. `pnpm --dir frontend list @heroui/react @heroui/styles` must show both at 3.2.1 (and only 3.2.1 — a dual-version listing means the coupled bump failed).

    Rollback if this task fails BEFORE committing (RESEARCH §Rollback): `git restore frontend/package.json pnpm-lock.yaml && pnpm install` — safe here only because those two files carry nothing but the bump. NEVER roll back one package alone; lockstep applies in both directions.

  </action>
  <verify>
    <automated>grep -E "@heroui/(react|styles)@3\.0\.5" pnpm-lock.yaml; test $? -eq 1 && pnpm --dir frontend list @heroui/react @heroui/styles</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` contains `"@heroui/react": "3.2.1"` and `"@heroui/styles": "3.2.1"` as exact pins; `@heroui-pro/react` line unchanged
    - `grep -E "@heroui/(react|styles)@3\.0\.5" pnpm-lock.yaml` produces no output and exits 1
    - `grep -cE "@heroui/(react|styles)@3\.2\.1" pnpm-lock.yaml` prints a count >= 2
    - `pnpm --dir frontend list @heroui/react @heroui/styles` shows exactly one version for each: 3.2.1
    - `git status` shows only `frontend/package.json` and `pnpm-lock.yaml` modified by this task
  </acceptance_criteria>
  <done>Both packages pinned and resolved to 3.2.1 with zero 3.0.5 remnant anywhere in the lockfile.</done>
</task>

<task type="auto">
  <name>Task 2: Prove compile + Drawer runtime on 3.2.1, then land the atomic bump commit</name>
  <files>frontend/package.json, pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/78-heroui-v3-api-audit-bump/78-RESEARCH.md — sections "Regression-Sweep Tooling" (ConcurrentDrawers row), "Rollback", "Common Pitfalls" (Pitfall 1: exit-0 is necessary-not-sufficient)
    - .planning/phases/78-heroui-v3-api-audit-bump/78-VALIDATION.md — Per-Task Verification Map rows for plan 01
  </read_first>
  <action>
    Run the two plan-01 oracles from VALIDATION.md, in order, from the main checkout (deps installed):

    1. `pnpm --dir frontend type-check` — must exit 0. RESEARCH verified every project import except Checkbox/Switch is byte-identical at the type level between 3.0.5 and 3.2.1, and the old Checkbox/Switch markup in `heroui-forms.tsx` still COMPILES under 3.2.1 (children are ReactNode), so exit 0 is expected WITHOUT touching any source file. A non-zero exit tracing to a HeroUI call site is a real 3.2.1 regression: stop, record the tsc output verbatim, and roll back per §Rollback rather than patching source in this plan (source changes belong to plan 78-02).
    2. `pnpm --dir frontend test run ConcurrentDrawers` — must pass. This vitest mounts two real HeroUI Drawers in jsdom and asserts no-throw + Tab focus containment + >=2 dialog FocusScopes; it directly exercises the react-aria externalization path (the ONLY runtime-relevant packaging change in 3.2.1, RESEARCH assumption A1). A failure here is a real Drawer regression → roll back and record.

    Both green: commit the bump as a SINGLE atomic commit touching ONLY the two files, using an explicit pathspec so no other staged work is swept in (shared-index hazard): stage and commit `frontend/package.json pnpm-lock.yaml` with message `chore(78-01): bump @heroui/react + @heroui/styles 3.0.5 -> 3.2.1 in lockstep`. Note: do NOT keep the toggles migration (plan 78-02) in this commit — RESEARCH §Rollback requires the bump and the migration to be independently revertible.

    Rollback AFTER this commit lands (for later plans to reference): `git revert <bump-sha>` (restores both files) then `pnpm install`, then `pnpm --dir frontend type-check && pnpm --dir frontend build` to confirm the restored 3.0.5 baseline.

  </action>
  <verify>
    <automated>pnpm --dir frontend type-check && pnpm --dir frontend test run ConcurrentDrawers</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir frontend type-check` exits 0
    - `pnpm --dir frontend test run ConcurrentDrawers` exits 0 (all ConcurrentDrawers assertions pass)
    - `git show --stat HEAD` for the bump commit lists exactly two files: frontend/package.json and pnpm-lock.yaml
    - Commit message references the lockstep 3.0.5 -> 3.2.1 bump
  </acceptance_criteria>
  <done>Tree compiles and Drawers mount on 3.2.1; atomic bump commit landed and independently revertible.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                      |
| --------------------------- | ---------------------------------------------------------------- |
| npm registry → node_modules | Third-party package code enters the build at `pnpm install` time |

## STRIDE Threat Register

| Threat ID | Category  | Component                                  | Disposition       | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                                         |
| --------- | --------- | ------------------------------------------ | ----------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-78-01   | Tampering | `@heroui/react` + `@heroui/styles` install | mitigate          | Exact-pin BOTH packages to the audited 3.2.1 (RESEARCH §Package Legitimacy Audit: both Approved — official heroui-inc org, npm registry + GitHub releases cross-checked 2026-07-03). Verify the lockfile resolves ONLY 3.2.1 (Task 1 greps). Confirm transitive changes match the documented expected set (react-aria externalization, RAC 1.18.0) — any OTHER unexpected transitive change is recorded and investigated before commit. |
| T-78-SC   | Tampering | pnpm supply chain                          | accept (residual) | No NEW package names are introduced — a version bump of two existing, audited packages. RESEARCH audit table has zero [ASSUMED]/[SUS]/[SLOP] entries, so no blocking human legitimacy checkpoint is required.                                                                                                                                                                                                                           |

**No HIGH-severity app-logic threats:** this plan changes no auth, input-handling, or data-path code — it touches exactly two dependency-manifest files.
</threat_model>

<verification>
- Lockfile lockstep: zero `@3.0.5` matches, >=2 `@3.2.1` matches for `@heroui/(react|styles)` in root `pnpm-lock.yaml`
- `pnpm --dir frontend type-check` exit 0
- `pnpm --dir frontend test run ConcurrentDrawers` exit 0
- Atomic commit contains only frontend/package.json + pnpm-lock.yaml
</verification>

<success_criteria>
Both packages resolve to exactly 3.2.1 in lockstep, the tree compiles, the Drawer runtime oracle passes, and the bump is one revertible commit. (Necessary-not-sufficient: the toggles behavioral oracle and the protocol re-run land in plans 78-02/78-03.)
</success_criteria>

<output>
Create `.planning/phases/78-heroui-v3-api-audit-bump/78-01-SUMMARY.md` when done. Record: npm view sanity output, lockfile grep results, any peer warnings, transitive delta observed vs expected, bump commit sha (needed by later plans for the rollback reference).
</output>
