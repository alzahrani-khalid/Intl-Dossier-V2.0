---
phase: 78-heroui-v3-api-audit-bump
plan: 02
type: execute
wave: 2
depends_on: [78-01]
files_modified:
  - frontend/src/components/ui/heroui-forms.test.tsx
  - frontend/src/components/ui/heroui-forms.tsx
  - frontend/src/components/ui/heroui-chip.tsx
autonomous: true
requirements: [HEROUI-02]

must_haves:
  truths:
    - 'Clicking the visible label of HeroUIFormCheckbox toggles its selection under 3.2.1 (onChange fires, state flips)'
    - 'Clicking the visible label of HeroUIFormSwitch toggles its selection under 3.2.1'
    - 'heroui-forms.tsx renders the v3.2.0 *.Content composition for both toggles (Checkbox.Content, Switch.Content)'
    - 'heroui-chip.tsx docstring truthfully describes its cva + Radix Slot implementation (no false HeroUI-primitive claim)'
    - 'The migration commit is separate from the 78-01 bump commit (independently revertible)'
  artifacts:
    - path: 'frontend/src/components/ui/heroui-forms.test.tsx'
      provides: 'The behavioral oracle tsc cannot provide: label-click toggles + Content DOM presence (VALIDATION.md Wave 0 gap)'
      min_lines: 30
    - path: 'frontend/src/components/ui/heroui-forms.tsx'
      provides: 'Migrated toggle wrappers'
      contains: 'HeroUICheckbox.Content'
    - path: 'frontend/src/components/ui/heroui-chip.tsx'
      provides: 'Corrected docstring'
  key_links:
    - from: 'frontend/src/components/ui/heroui-forms.test.tsx'
      to: 'frontend/src/components/ui/heroui-forms.tsx'
      via: "import from the wrapper module — NEVER from '@heroui/react' directly (keeps protocol import-site count at 8)"
      pattern: "from ['\"]\\./heroui-forms['\"]"
    - from: 'HeroUICheckbox.Content'
      to: 'HeroUICheckbox.Control + label text'
      via: 'Content wraps Control+Indicator+label; Description is a SIBLING of Content'
      pattern: "HeroUICheckbox\\.Content"
---

<objective>
Handle the ONE breaking change in the 3.0.5→3.2.1 range: v3.2.0 refactored Checkbox/Switch/Radio to explicit `*.Content` composition. Migrate `HeroUIFormCheckbox` and `HeroUIFormSwitch` in `frontend/src/components/ui/heroui-forms.tsx` to the new anatomy, TDD-first with the Wave-0 vitest oracle from 78-VALIDATION.md, plus the Phase 75-flagged `heroui-chip.tsx` docstring fix.

Purpose: HEROUI-02 (regression-sweep clause). Under 3.2.1 the OLD markup still compiles and renders (Control self-wraps), but the root stops being a `<label>`: the visible Label loses click-to-toggle association and the accessible name moves — a behavioral/a11y regression that tsc and the Phase 75 grep protocol CANNOT see. `heroui-forms.tsx` has ZERO consumers today, but the module exists "for new forms" per its docstring — leaving it silently broken hands the next consumer a broken pattern. This is NOT a v2 flat-prop straggler and does NOT re-open a migration narrative (CONTEXT.md locked decision).

Output: migrated wrappers + committed behavioral test + honest chip docstring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-heroui-v3-api-audit-bump/78-RESEARCH.md
@.planning/phases/78-heroui-v3-api-audit-bump/78-VALIDATION.md
@frontend/src/components/ui/heroui-forms.tsx
@frontend/src/components/layout/ConcurrentDrawers.test.tsx

<interfaces>
<!-- Current heroui-forms.tsx toggle markup (the code being migrated). Extracted 2026-07-03. -->

HeroUIFormCheckbox (lines 190-209) — OLD composition:

- `<HeroUICheckbox name isSelected defaultSelected onChange isRequired isDisabled isIndeterminate className={cn('flex items-start gap-3', 'min-h-11 sm:min-h-10', className)}>` containing `<HeroUICheckbox.Control className="mt-0.5"><HeroUICheckbox.Indicator /></HeroUICheckbox.Control>` + sibling `<div className="flex flex-col"><HeroUILabel>{label}</HeroUILabel>{description && <HeroUIDescription className="text-xs">…</HeroUIDescription>}</div>`

HeroUIFormSwitch (lines 244-260) — OLD composition:

- `<HeroUISwitch name isSelected defaultSelected onChange isDisabled className={cn('flex items-center justify-between gap-3', 'min-h-11 sm:min-h-10', className)}>` containing `<div className="flex flex-col"><HeroUILabel>{label}</HeroUILabel>{description && …}</div>` + `<HeroUISwitch.Control><HeroUISwitch.Thumb /></HeroUISwitch.Control>`

Imports are aliased: `Checkbox as HeroUICheckbox`, `Switch as HeroUISwitch`, plus `Label/Description/FieldError` etc. Props interfaces `HeroUICheckboxFieldProps` / `HeroUISwitchFieldProps` are exported and must NOT change.

3.2.1 shipped parts (verified against dist, RESEARCH §The One Breaking Change):

- Checkbox: `Root` (now a Field wrapper, NOT the label element), `Content` (the clickable label — extends CheckboxButtonPrimitive), `Control`, `Indicator`
- Switch: same pattern; `Switch.Thumb` still exists; additive `Switch.Icon`
- Naming trap: upstream PR #6614's body says `*.Button`; the SHIPPED part is `*.Content`. No `.Button` part exists.
- Canonical anatomy: Content wraps Control(+Indicator/Thumb) + plain label text (NO nested Label component inside Content); Description/FieldError are SIBLINGS of Content under the root.
  </interfaces>

Test infra facts (verified 2026-07-03): vitest 4.x + @testing-library/react 16.3.2 + @testing-library/user-event 14.6.1 installed; `ConcurrentDrawers.test.tsx` is the proven pattern for rendering real HeroUI components under jsdom. Filename case for `src/components/ui/**` is kebab-case (ESLint-enforced), so `heroui-forms.test.tsx` is correct. Run tests from the main checkout (worktrees share no `frontend/node_modules`).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing behavioral oracle for the migrated toggles (RED)</name>
  <files>frontend/src/components/ui/heroui-forms.test.tsx</files>
  <read_first>
    - frontend/src/components/ui/heroui-forms.tsx (current toggle markup + exported props)
    - frontend/src/components/layout/ConcurrentDrawers.test.tsx (the proven HeroUI-in-jsdom vitest pattern: setup, rendering, queries)
    - .planning/phases/78-heroui-v3-api-audit-bump/78-VALIDATION.md — "Wave 0 Requirements"
    - .planning/phases/78-heroui-v3-api-audit-bump/78-RESEARCH.md — §The One Breaking Change
  </read_first>
  <behavior>
    - Test 1 (Checkbox label-click): render `HeroUIFormCheckbox` with `name="t"`, `label="Accept terms"`, and an `onChange` vi.fn spy; `userEvent.click` on the "Accept terms" text; assert the spy was called with `true` and the rendered checkbox reflects selected state
    - Test 2 (Switch label-click): mirror Test 1 for `HeroUIFormSwitch` with `label="Enable alerts"`
    - Test 3 (Content composition present, Checkbox): assert the clickable label element (react-aria renders `*.Content` as a `<label>`) CONTAINS BOTH the label text and the control — i.e. the text "Accept terms" and the checkbox control share a common `<label>` ancestor
    - Test 4 (Description sibling): render with a `description` prop; assert the description text renders and is NOT inside the clickable label element (it is a sibling of Content under the root)
  </behavior>
  <action>
    Create `frontend/src/components/ui/heroui-forms.test.tsx` implementing the behaviors above with @testing-library/react + @testing-library/user-event, mirroring the jsdom setup used by ConcurrentDrawers.test.tsx.

    CRITICAL constraint (protocol integrity, RESEARCH §Phase 75 Re-run Protocol): the test file MUST import `HeroUIFormCheckbox` / `HeroUIFormSwitch` from `./heroui-forms` (the wrapper module) and MUST NOT contain any import from `'@heroui/react'`. A direct `@heroui/react` import would grow the Phase 75 import-site count from 8 to 9 and corrupt plan 78-03's diff.

    Run `pnpm --dir frontend test run heroui-forms` and confirm the label-click tests FAIL against the current (unmigrated) markup on 3.2.1 — this is the expected RED proving the regression is real (under 3.2.1 the root is no longer the label, so clicking the label text no longer toggles). If Tests 1-2 unexpectedly PASS on the old markup, stop and re-diagnose against RESEARCH §The One Breaking Change before migrating — do not proceed on a vacuous oracle.

    Commit the failing test with message `test(78-02): add failing toggles Content-composition oracle for heroui-forms` (commit only this file via pathspec). Note: the pre-commit hook builds but does not run vitest, so committing a RED test is safe.

  </action>
  <verify>
    <automated>pnpm --dir frontend test run heroui-forms 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/ui/heroui-forms.test.tsx` exists and contains zero occurrences of `from '@heroui/react'` (grep confirms)
    - Test file imports the wrappers from `./heroui-forms`
    - `pnpm --dir frontend test run heroui-forms` runs the new file and the label-click tests FAIL against the unmigrated markup (recorded RED output)
    - Test committed as its own commit
  </acceptance_criteria>
  <done>Behavioral oracle exists, demonstrably RED against the old composition on 3.2.1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate HeroUIFormCheckbox + HeroUIFormSwitch to the *.Content composition (GREEN)</name>
  <files>frontend/src/components/ui/heroui-forms.tsx</files>
  <read_first>
    - frontend/src/components/ui/heroui-forms.tsx (lines 178-262 — the two toggle wrappers)
    - .planning/phases/78-heroui-v3-api-audit-bump/78-RESEARCH.md — §The One Breaking Change (canonical anatomy + "Recommended handling")
  </read_first>
  <action>
    Rework ONLY the two toggle wrappers' JSX (props interfaces, exports, and every other component in the file stay untouched):

    HeroUIFormCheckbox: root `<HeroUICheckbox>` keeps all props (name, isSelected, defaultSelected, onChange, isRequired, isDisabled, isIndeterminate) but its className drops the layout classes — keep `min-h-11 sm:min-h-10` + caller `className` on the root. Inside the root render `<HeroUICheckbox.Content className="flex items-start gap-3">` wrapping `<HeroUICheckbox.Control className="mt-0.5"><HeroUICheckbox.Indicator /></HeroUICheckbox.Control>` followed by the `{label}` text directly (plain text inside Content — per the canonical anatomy, NO nested HeroUILabel inside Content). Render `{description && <HeroUIDescription className="text-xs">{description}</HeroUIDescription>}` as a SIBLING of Content (still inside the root).

    HeroUIFormSwitch: mirror — root `<HeroUISwitch>` keeps props + `min-h-11 sm:min-h-10` + caller className; `<HeroUISwitch.Content className="flex items-center justify-between gap-3">` wraps the `{label}` text FIRST then `<HeroUISwitch.Control><HeroUISwitch.Thumb /></HeroUISwitch.Control>` (the `justify-between` layout moves onto Content per RESEARCH); Description remains a sibling of Content.

    Constraints: only Tailwind utility/layout classes already present may move between elements — introduce NO new colors, NO raw hex, NO physical (`ml-*`/`pl-*`) properties (this file already uses only gap/flex/min-h utilities, which are direction-safe). If the removed `HeroUILabel` usage inside the toggles leaves the import otherwise unused, keep it — `HeroUILabel` is still used by the TextField/TextArea wrappers and is re-exported at the bottom of the file.

    Run `pnpm --dir frontend test run heroui-forms` → all tests GREEN. Run `pnpm --dir frontend type-check` → exit 0. Then commit ONLY heroui-forms.tsx with message `feat(78-02): migrate toggle wrappers to v3.2 *.Content composition` — this commit must stay SEPARATE from the 78-01 bump commit (RESEARCH §Rollback: independent revertability).

  </action>
  <verify>
    <automated>pnpm --dir frontend test run heroui-forms && pnpm --dir frontend type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "HeroUICheckbox.Content" frontend/src/components/ui/heroui-forms.tsx` >= 1 and `grep -c "HeroUISwitch.Content" frontend/src/components/ui/heroui-forms.tsx` >= 1
    - `pnpm --dir frontend test run heroui-forms` exits 0 (all 4 behaviors pass)
    - `pnpm --dir frontend type-check` exits 0
    - Exported interfaces `HeroUICheckboxFieldProps` / `HeroUISwitchFieldProps` unchanged (public API preserved)
    - Migration is its own commit, separate from the 78-01 bump commit
  </acceptance_criteria>
  <done>Both toggle wrappers on the shipped 3.2.1 anatomy; label-click behavior proven by the vitest oracle.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the stale heroui-chip.tsx docstring (Phase 75-flagged cleanup)</name>
  <files>frontend/src/components/ui/heroui-chip.tsx</files>
  <read_first>
    - frontend/src/components/ui/heroui-chip.tsx (lines 1-10 — the false docstring)
    - .planning/phases/75-ui-component-migration-audit/75-AUDIT-heroui-confirmation.md — "Nuance 2" section (the flag + evidence)
  </read_first>
  <action>
    Rewrite the file's header docstring (currently claims "Real @heroui/react Chip primitive") to truthfully describe the implementation: a cva + @radix-ui/react-slot Badge replacement that preserves the shadcn-style `badgeVariants` cva API and `asChild` branch — it contains NO `@heroui/react` import. Keep the Phase 33-05 provenance line. Comment-only change: zero runtime/JSX/export edits. No emoji, no marketing voice.

    Known side effect to record in the SUMMARY (RESEARCH False-Positive Guard 3): this drops the LOOSE text-match count `grep -rln "@heroui" --include="*.tsx" --include="*.ts" frontend/src` from 12 to 11. That grep is NOT the protocol grep — plan 78-03 uses the import-specific grep, whose 8-file result is unaffected. Do not "fix" any grep to compensate.

    Commit only this file: `docs(78-02): correct heroui-chip.tsx docstring — cva + Radix Slot, not a HeroUI primitive`.

  </action>
  <verify>
    <automated>grep -c "Real @heroui/react Chip primitive" frontend/src/components/ui/heroui-chip.tsx; test $? -eq 1 && pnpm --dir frontend type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Real @heroui/react Chip primitive" frontend/src/components/ui/heroui-chip.tsx` produces no output (exit 1)
    - New docstring mentions cva and @radix-ui/react-slot and states there is no @heroui/react import
    - `git diff` for this commit shows ONLY comment lines changed in heroui-chip.tsx (no code lines)
    - `pnpm --dir frontend type-check` exits 0
  </acceptance_criteria>
  <done>Docstring is honest; no behavior change; loose-grep count change pre-recorded as expected.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary       | Description                                                                                                                                 |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| (none crossed) | This plan edits one unconsumed wrapper module, one test file, and one docstring — no untrusted input path, no auth, no data path is touched |

## STRIDE Threat Register

| Threat ID | Category  | Component           | Disposition                | Mitigation Plan                                                                                                         |
| --------- | --------- | ------------------- | -------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| T-78-01   | Tampering | dependency versions | accept (out of scope here) | No dependency change in this plan — supply-chain mitigation lives in plan 78-01 (lockstep pins + lockfile verification) |
| T-78-SC   | Tampering | package installs    | accept                     | Zero package-manager installs in this plan                                                                              |

**No HIGH-severity app-logic threats:** markup-only migration of a zero-consumer module plus a comment fix. The migration IMPROVES a11y (restores label→control click association and accessible-name placement via the shipped `*.Content` part, which is react-aria's CheckboxButton/SwitchButton — per RESEARCH §Don't Hand-Roll, no custom label-click handlers).
</threat_model>

<verification>
- `pnpm --dir frontend test run heroui-forms` exit 0 (label-click + Content composition oracle)
- `pnpm --dir frontend type-check` exit 0
- heroui-forms.tsx contains `HeroUICheckbox.Content` and `HeroUISwitch.Content`
- heroui-forms.test.tsx contains no `@heroui/react` import
- heroui-chip.tsx docstring corrected, comment-only diff
</verification>

<success_criteria>
The one real breaking change of the range is closed with a behavioral test that would have caught it (the oracle tsc cannot provide), the chip docstring is honest, and all changes landed in commits separate from the bump for independent revertability.
</success_criteria>

<output>
Create `.planning/phases/78-heroui-v3-api-audit-bump/78-02-SUMMARY.md` when done. Record: the RED output from Task 1, GREEN confirmation, the loose-grep 12→11 side effect, and the note that command #4 of the Phase 75 protocol will now show additional `Checkbox.Content`/`Switch.Content` lines (expected delta for plan 78-03).
</output>
