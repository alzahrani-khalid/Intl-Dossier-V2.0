---
phase: 78-heroui-v3-api-audit-bump
plan: 03
type: execute
wave: 3
depends_on: [78-01, 78-02]
files_modified:
  - .planning/phases/78-heroui-v3-api-audit-bump/78-PROTOCOL-RERUN.md
autonomous: true
requirements: [HEROUI-02]

must_haves:
  truths:
    - 'All 4 Phase 75 protocol commands re-run VERBATIM post-bump with raw output captured and dated'
    - 'Command 1 returns the same 8-file import list (Import-site count: 8); heroui-forms.test.tsx does NOT appear'
    - 'Command 2 returns 0 removed-name import hits (exit 1)'
    - 'Command 3 (type-check) exits 0'
    - 'Command 4 shows compound dot-notation in all three wrappers; the ONLY delta vs the Phase 75 record is the expected Checkbox.Content/Switch.Content lines from plan 78-02, recorded as an explained delta'
    - 'Zero residual flat-prop stragglers (or any that surfaced is converted per the criterion-3 path)'
  artifacts:
    - path: '.planning/phases/78-heroui-v3-api-audit-bump/78-PROTOCOL-RERUN.md'
      provides: 'Evidence-paired re-run record: command + raw output + date + interpretation + diff verdict per command'
      min_lines: 60
  key_links:
    - from: '.planning/phases/78-heroui-v3-api-audit-bump/78-PROTOCOL-RERUN.md'
      to: '.planning/phases/75-ui-component-migration-audit/75-AUDIT-heroui-confirmation.md'
      via: 'per-command diff against the recorded expected outputs'
      pattern: '75-AUDIT-heroui-confirmation'
---

<objective>
Re-run the Phase 75 "Phase 78 re-run protocol" — the 4 recorded commands — VERBATIM against the 3.2.1 tree, diff each raw output against the outputs recorded in `75-AUDIT-heroui-confirmation.md`, and write the evidence-paired re-run artifact. A delta is the only thing worth acting on.

Purpose: HEROUI-02 (confirmation clause). CONTEXT.md locks the expectation: SAME output — 8 import sites, 0 removed-name imports, type-check exit 0, compound dot-notation present. Convert a straggler ONLY if one surfaces (expected: none). Do NOT re-open a v2→v3 migration narrative.

Output: `.planning/phases/78-heroui-v3-api-audit-bump/78-PROTOCOL-RERUN.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-ui-component-migration-audit/75-AUDIT-heroui-confirmation.md
@.planning/phases/78-heroui-v3-api-audit-bump/78-RESEARCH.md
@.planning/phases/78-heroui-v3-api-audit-bump/78-02-SUMMARY.md

Environment: run all commands from the REPO ROOT of the main checkout (`frontend/node_modules` installed — GSD worktrees share none; RESEARCH Pitfall 5).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the 4 protocol commands verbatim and capture raw output</name>
  <files>.planning/phases/78-heroui-v3-api-audit-bump/78-PROTOCOL-RERUN.md</files>
  <read_first>
    - .planning/phases/75-ui-component-migration-audit/75-AUDIT-heroui-confirmation.md — "Phase 78 re-run protocol" table + AUDIT-02/AUDIT-03 raw-output records (the diff baseline)
    - .planning/phases/78-heroui-v3-api-audit-bump/78-RESEARCH.md — §Phase 75 Re-run Protocol ("Critical caveat"), §False-Positive Guards
  </read_first>
  <action>
    Execute exactly these 4 commands (copied verbatim from the Phase 75 artifact — do NOT "improve" them; Pitfall 4), capturing full raw output + exit code for each:

    1. `grep -rlnE "from ['\"]@heroui/react['\"]" --include='*.tsx' --include='*.ts' frontend/src` — expected: the SAME 8-file list recorded in Phase 75 (heroui-skeleton, heroui-forms, heroui-modal, heroui-button, heroui-card, AppShell, ConcurrentDrawers.test, TweaksDrawer). NEVER substitute the loose `@heroui` text match (over-counts; and plan 78-02's chip docstring fix moved that loose count 12→11 — irrelevant to THIS grep).
    2. `grep -rnE "import\s*\{[^}]*\b(Navbar|Snippet|User|Spacer|Image|Code|Autocomplete|DateInput|Ripple)\b[^}]*\}\s*from\s*['\"]@heroui/react['\"]" --include='*.tsx' --include='*.ts' frontend/src` — expected: no output, exit 1.
    3. `pnpm --dir frontend type-check` — expected: exit 0.
    4. `grep -rn "Modal\.\|Checkbox\.\|Switch\.\|Card\." frontend/src/components/ui/heroui-modal.tsx frontend/src/components/ui/heroui-forms.tsx frontend/src/components/ui/heroui-card.tsx` — expected: compound dot-notation in all three wrappers, PLUS new `HeroUICheckbox.Content` / `HeroUISwitch.Content` lines from the 78-02 migration.

    Write the raw outputs into a new `.planning/phases/78-heroui-v3-api-audit-bump/78-PROTOCOL-RERUN.md` using the Phase 75 evidence style (RESEARCH Pattern 2): per command — the exact command, its raw output, the run date, exit code, and a one-line interpretation. Include the machine-readable summary lines mirroring Phase 75: `Import-site count: N`, `Removed-name import hits: N (exit X)`, `Type-check: exit N`.

    Special check on command 1: if `heroui-forms.test.tsx` appears in the list, plan 78-02's constraint was violated (the test must import from `./heroui-forms`, never `@heroui/react`) — fix the test's import in place, re-run command 1, and record the incident; the final recorded count must be 8.

  </action>
  <verify>
    <automated>grep -rlE "from ['\"]@heroui/react['\"]" --include='*.tsx' --include='*.ts' frontend/src | grep -vc node_modules | grep -qx 8 && pnpm --dir frontend type-check</automated>
  </verify>
  <acceptance_criteria>
    - 78-PROTOCOL-RERUN.md exists containing all 4 commands with raw output, exit codes, and dates
    - Command 1 output = exactly 8 files, matching the Phase 75 list; `Import-site count: 8` recorded
    - Command 2 = zero hits, exit 1; `Removed-name import hits: 0 (exit 1)` recorded
    - Command 3 = exit 0; `Type-check: exit 0` recorded
    - Command 4 raw output captured including the new `.Content` lines
  </acceptance_criteria>
  <done>All 4 protocol outputs captured verbatim in the re-run artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Diff against the Phase 75 records, adjudicate deltas, close HEROUI-02</name>
  <files>.planning/phases/78-heroui-v3-api-audit-bump/78-PROTOCOL-RERUN.md</files>
  <read_first>
    - .planning/phases/75-ui-component-migration-audit/75-AUDIT-heroui-confirmation.md — Nuance 1, Nuance 2, AUDIT-03 nuance (the false-positive guards travel with the diff)
    - .planning/phases/78-heroui-v3-api-audit-bump/78-RESEARCH.md — §False-Positive Guards (all 5), §Phase 75 Re-run Protocol "Critical caveat"
  </read_first>
  <action>
    Add a "Diff vs Phase 75 records" section to 78-PROTOCOL-RERUN.md adjudicating each command:

    EXPECTED DELTAS (record + explain, do NOT treat as failures):
    - Command 4 gains `HeroUICheckbox.Content` / `HeroUISwitch.Content` lines and loses the old sibling-`<div>` label lines — the plan 78-02 migration to the v3.2.0 anatomy. Cite 78-02-SUMMARY.md.
    - Everything else must byte-match the Phase 75 records (same 8 files, exit 1, exit 0).

    FALSE-POSITIVE GUARDS — bake these into the adjudication so nothing is mis-flagged:
    1. Flat-named `Drawer*` exports in TweaksDrawer.tsx ARE v3 (Nuance 1 — the export line is identical in the 3.2.1 dist). Do NOT flag.
    2. `Autocomplete` STILL EXISTS as a 3.2.1 export — command 2's claim stays import-evidence ("0 imports of the 9 names"), never a removal claim. Do NOT flag its existence.
    3. `heroui-chip/switch/tabs.tsx` are lookalikes with no `@heroui/react` import (Nuance 2) — never counted.
    4. `CheckboxRenderProps` deprecation (aliased, not removed) in 3.2.1 typings is not a straggler.
    5. Additive 3.2.x exports (`Switch.Icon`, Calendar/Table expansions) are not regressions.

    Also record the protocol's known blind spot explicitly (RESEARCH Critical caveat): these 4 commands CANNOT detect the v3.2.0 toggles composition change — tsc exits 0 either way — which is why the behavioral oracle in `heroui-forms.test.tsx` (plan 78-02) exists. State that HEROUI-02's real oracle is `pnpm --dir frontend test run heroui-forms`, and run it once here to bind the two: it must exit 0.

    UNEXPECTED DELTA handling (contingency — expected: none): a genuine new flat-prop straggler or removed-name import hit gets the ROADMAP criterion-3 path — convert to native HTML per the official incremental-migration docs, or to the v3 `ComboBox` / `DateField` primitive for `Autocomplete` / `DateInput` respectively — as a code change committed in this plan, then re-run the affected command and record before/after. Any OTHER unexplained delta (e.g. type-check failure): stop, record verbatim, and surface as a blocker in the SUMMARY rather than improvising — rollback of the bump is `git revert <78-01 bump sha> && pnpm install` (RESEARCH §Rollback).

    Conclude the artifact with a verdict table mirroring the Phase 75 findings-summary style, then commit it: `docs(78-03): protocol re-run — Phase 75 records hold on 3.2.1 (HEROUI-02)`.

  </action>
  <verify>
    <automated>pnpm --dir frontend test run heroui-forms && grep -q "Import-site count: 8" .planning/phases/78-heroui-v3-api-audit-bump/78-PROTOCOL-RERUN.md && grep -qi "diff vs phase 75" .planning/phases/78-heroui-v3-api-audit-bump/78-PROTOCOL-RERUN.md</automated>
  </verify>
  <acceptance_criteria>
    - 78-PROTOCOL-RERUN.md contains a per-command diff verdict against 75-AUDIT-heroui-confirmation.md
    - The command-4 `.Content` delta is recorded as EXPECTED with a citation to 78-02, not flagged as a failure
    - All 5 false-positive guards are cited in the adjudication (no false flag on TweaksDrawer, Autocomplete, lookalike files, CheckboxRenderProps, additive exports)
    - The toggles blind-spot note is present and `pnpm --dir frontend test run heroui-forms` exit 0 is recorded as HEROUI-02's behavioral oracle
    - Straggler count recorded: 0 (or the conversion commit + re-run evidence if one surfaced)
    - Artifact committed
  </acceptance_criteria>
  <done>Phase 75 confirmation holds on 3.2.1 with every delta explained; HEROUI-02 closed with evidence.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary       | Description                                                                                                                     |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| (none crossed) | Read-only verification plan — greps + type-check + a docs artifact; no runtime code, auth, input-handling, or data path changes |

## STRIDE Threat Register

| Threat ID | Category  | Component           | Disposition                | Mitigation Plan                                                                                                                                                                                                                                        |
| --------- | --------- | ------------------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-78-01   | Tampering | dependency versions | accept (out of scope here) | No dependency change in this plan; supply-chain mitigation landed in plan 78-01. This plan's protocol re-run additionally confirms no unexpected API surface appeared post-install (repudiation-resistant evidence trail: command + raw output + date) |
| T-78-SC   | Tampering | package installs    | accept                     | Zero package-manager installs in this plan                                                                                                                                                                                                             |

**No HIGH-severity app-logic threats:** verification-only plan (the sole contingent code change — a straggler conversion — replaces markup with native HTML or an audited v3 primitive, introducing no new input paths).
</threat_model>

<verification>
- All 4 protocol commands re-run verbatim; outputs match Phase 75 records modulo the explained `.Content` delta
- `pnpm --dir frontend test run heroui-forms` exit 0 (binds the blind spot to its oracle)
- 78-PROTOCOL-RERUN.md committed with evidence-paired records + diff verdicts
</verification>

<success_criteria>
ROADMAP success criterion 2 proven: the Phase 75 confirmation holds — zero flat-prop call sites remain (or any straggler converted with evidence) — via a re-derivable, evidence-paired artifact that a future phase can diff against.
</success_criteria>

<output>
Create `.planning/phases/78-heroui-v3-api-audit-bump/78-03-SUMMARY.md` when done. Record: per-command match/delta verdicts, straggler count (expected 0), and the heroui-forms oracle result.
</output>
