---
phase: 16-email-push-channels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/queues/notification.processor.ts
  - backend/src/services/email-template.service.ts
  - backend/src/__tests__/email-notifications.test.ts
autonomous: true
requirements: [NOTIF-03]
must_haves:
  truths:
    - 'When a notification is processed and email_enabled is true for that category, a row is inserted into email_queue'
    - "Email subject and body are rendered in the user's preferred language (ar or en)"
    - 'Email templates include bilingual unsubscribe footer linking to /settings?section=notifications'
  artifacts:
    - path: 'backend/src/services/email-template.service.ts'
      provides: 'Bilingual email template rendering for alert notifications'
      exports: ['renderAlertEmailTemplate', 'mapNotificationTypeToTemplate']
    - path: 'backend/src/queues/notification.processor.ts'
      provides: 'Extended processor with email channel dispatch'
      contains: 'email_queue'
    - path: 'backend/src/__tests__/email-notifications.test.ts'
      provides: 'Tests for email notification dispatch'
      contains: 'email_enabled'
  key_links:
    - from: 'backend/src/queues/notification.processor.ts'
      to: 'email_queue table'
      via: "supabase.from('email_queue').insert()"
      pattern: 'email_queue.*insert'
    - from: 'backend/src/services/email-template.service.ts'
      to: 'notification.processor.ts'
      via: 'renderAlertEmailTemplate import'
      pattern: 'import.*renderAlertEmailTemplate'
---

<objective>
Wire the email notification channel into the existing BullMQ notification processor. When a notification fires and the user has email_enabled for that category, insert a row into the email_queue table with bilingual subject/body. The existing Supabase Edge Function (email-send) handles Resend delivery.

Purpose: Fulfills NOTIF-03 -- users receive email for critical notifications (overdue deadlines, new assignments).
Output: Extended notification processor + bilingual email template service + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-email-push-channels/16-CONTEXT.md
@.planning/phases/16-email-push-channels/16-RESEARCH.md
@.planning/phases/16-email-push-channels/16-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/src/queues/notification.queue.ts:

```typescript
export interface NotificationJobData {
  // contains userId, type, category, title, message, actionUrl, priority
}
export const notificationQueue = new Queue<NotificationJobData>('notifications', { ... })
```

From backend/src/queues/notification.processor.ts:

```typescript
export async function processNotificationJob(job: Job<NotificationJobData>): Promise<void>
```

From backend/src/services/notification.service.ts:

```typescript
export async function sendInAppNotification(...)
export async function enqueueNotification(...)
export class NotificationService { ... }
```

Email queue table columns (from 20260110700001_email_integration.sql):

- id, to_email, subject, body_html, body_text, template_type, template_data, language, user_id, priority, status, created_at
- template_type enum includes: digest_daily, digest_weekly, notification_alert, welcome, password_reset
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create bilingual email template service</name>
  <files>backend/src/services/email-template.service.ts, backend/src/__tests__/email-notifications.test.ts</files>
  <read_first>
    - backend/src/queues/notification.processor.ts
    - backend/src/queues/notification.queue.ts
    - backend/src/services/notification.service.ts
    - supabase/migrations/20260110700001_email_integration.sql
    - frontend/src/i18n/ar/email.json
    - frontend/src/i18n/ar/notification-center.json
    - .planning/phases/16-email-push-channels/16-UI-SPEC.md
  </read_first>
  <behavior>
    - renderAlertEmailTemplate('en', { title: 'New Assignment', message: 'You were assigned...', actionUrl: '/work-items/123' }) returns { subject: '[Action Required] New Assignment', bodyHtml: string containing 'View in Intl Dossier', bodyText: string }
    - renderAlertEmailTemplate('ar', { title: 'مهمة جديدة', message: '...', actionUrl: '/work-items/123' }) returns { subject: '[اجراء مطلوب] مهمة جديدة', bodyHtml: string containing dir="rtl" and 'عرض في الملف الدولي', bodyText: string }
    - All HTML templates use inline styles only (no external CSS), table-based layout, max-width 600px
    - Arabic templates have dir="rtl" on html, body, and td elements
    - Unsubscribe footer links to /settings?section=notifications
    - mapNotificationTypeToTemplate maps assignment/deadline types to 'notification_alert' template_type enum value
  </behavior>
  <action>
    Create `backend/src/services/email-template.service.ts` with these exports:

    1. `renderAlertEmailTemplate(language: 'ar' | 'en', data: { title: string, message: string, actionUrl: string }): { subject: string, bodyHtml: string, bodyText: string }`
       - English subject: `[Action Required] ${data.title}`
       - Arabic subject: `[اجراء مطلوب] ${data.title}`
       - HTML body uses table-based layout, inline CSS, 600px max-width
       - Colors from UI-SPEC: bg #f4f4f5, content #ffffff, heading text #18181b, body text #3f3f46, CTA button bg #166534, CTA text #ffffff, footer text #71717a, border #e4e4e7
       - CTA button text: EN="View in Intl Dossier", AR="عرض في الملف الدولي"
       - Unsubscribe footer: EN="Unsubscribe from these emails", AR="الغاء الاشتراك من هذه الرسائل"
       - Footer reason: EN="You received this because {category} email notifications are enabled in your preferences.", AR="تلقيت هذه الرسالة لان اشعارات {category} مفعلة في اعداداتك."
       - Arabic template: `dir="rtl"` on `<html>`, `<body>`, all `<td>` elements; `lang="ar"`
       - English template: `dir="ltr"` on `<html>`, `<body>`; `lang="en"`
       - bodyText: plain-text version with same content, no HTML tags

    2. `mapNotificationTypeToTemplate(type: string): string` -- returns 'notification_alert' for assignment/deadline/lifecycle types

    Create test file `backend/src/__tests__/email-notifications.test.ts`:
    - Test renderAlertEmailTemplate for both 'en' and 'ar' languages
    - Assert subject prefixes match exactly
    - Assert bodyHtml contains dir="rtl" for Arabic
    - Assert bodyHtml contains unsubscribe link with /settings?section=notifications
    - Assert bodyHtml contains CTA button text in correct language
    - Assert mapNotificationTypeToTemplate returns correct enum values

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/Coding\ Space/Intl-Dossier-V2.0/backend && pnpm vitest run src/__tests__/email-notifications.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/src/services/email-template.service.ts` exists and contains `export function renderAlertEmailTemplate`
    - File `backend/src/services/email-template.service.ts` contains `export function mapNotificationTypeToTemplate`
    - File `backend/src/services/email-template.service.ts` contains `dir="rtl"` for Arabic template
    - File `backend/src/services/email-template.service.ts` contains `#166534` (CTA button color)
    - File `backend/src/services/email-template.service.ts` contains `/settings?section=notifications` (unsubscribe link)
    - File `backend/src/__tests__/email-notifications.test.ts` exists and contains `renderAlertEmailTemplate`
    - Test file passes with 0 failures
  </acceptance_criteria>
  <done>Email template service renders bilingual alert emails with correct RTL, colors, CTA, and unsubscribe footer. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Extend notification processor with email channel dispatch</name>
  <files>backend/src/queues/notification.processor.ts</files>
  <read_first>
    - backend/src/queues/notification.processor.ts
    - backend/src/queues/notification.queue.ts
    - backend/src/services/email-template.service.ts
    - supabase/migrations/20260110700001_email_integration.sql
    - supabase/migrations/20260111100001_notification_center.sql
  </read_first>
  <action>
    Extend `processNotificationJob()` in `backend/src/queues/notification.processor.ts` to add email dispatch AFTER the existing in-app notification creation. Per D-01 and D-02:

    1. After in-app notification is created, query `notification_category_preferences` for the user+category:
       ```typescript
       const { data: emailPref } = await supabase
         .from('notification_category_preferences')
         .select('email_enabled')
         .eq('user_id', job.data.userId)
         .eq('category', job.data.category)
         .single()
       ```

    2. If `emailPref?.email_enabled !== false` (default to enabled):
       - Get user email and language preference from auth.users and user_preferences
       - Call `renderAlertEmailTemplate(language, { title: job.data.title, message: job.data.message, actionUrl: job.data.actionUrl })`
       - Insert into email_queue:
         ```typescript
         await supabase.from('email_queue').insert({
           to_email: userEmail,
           subject,
           body_html: bodyHtml,
           body_text: bodyText,
           template_type: mapNotificationTypeToTemplate(job.data.type),
           template_data: { title: job.data.title, message: job.data.message, actionUrl: job.data.actionUrl },
           language,
           user_id: job.data.userId,
           priority: job.data.priority === 'urgent' ? 1 : job.data.priority === 'high' ? 2 : 3,
         })
         ```

    3. Add a helper function `getUserEmailAndLanguage(userId: string): Promise<{ email: string, language: 'ar' | 'en' }>` that queries auth.users for email and user_preferences for language (default 'en').

    4. Wrap email dispatch in try/catch -- log errors with `logError()` but do NOT throw (email failure should not block in-app notification). Per anti-pattern: email insertion is fire-and-forget.

    5. Import `renderAlertEmailTemplate` and `mapNotificationTypeToTemplate` from `../services/email-template.service`

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/Coding\ Space/Intl-Dossier-V2.0/backend && pnpm vitest run src/__tests__/email-notifications.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `notification.processor.ts` contains `import { renderAlertEmailTemplate, mapNotificationTypeToTemplate } from '../services/email-template.service'`
    - `notification.processor.ts` contains `email_queue`
    - `notification.processor.ts` contains `email_enabled`
    - `notification.processor.ts` contains `getUserEmailAndLanguage`
    - `notification.processor.ts` contains a try/catch around email dispatch code
    - `notification.processor.ts` does NOT contain `import.*resend` (per D-01)
  </acceptance_criteria>
  <done>Notification processor inserts into email_queue when email_enabled is true, with bilingual content from template service. Email failures are logged but do not block in-app delivery.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                            |
| ---------------------------- | ------------------------------------------------------ |
| Processor -> email_queue     | Internal: BullMQ processor inserts into Supabase table |
| email_queue -> Edge Function | Internal: Edge Function reads and sends via Resend     |

## STRIDE Threat Register

| Threat ID | Category               | Component                 | Disposition | Mitigation Plan                                                                                                                         |
| --------- | ---------------------- | ------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| T-16-01   | Tampering              | email-template.service.ts | mitigate    | Sanitize template variables (title, message) -- escape HTML entities before insertion into template to prevent HTML injection in emails |
| T-16-02   | Information Disclosure | getUserEmailAndLanguage   | mitigate    | Query only needed fields (email, language), do not log email addresses                                                                  |
| T-16-03   | Denial of Service      | email dispatch            | accept      | Rate limiting inherited from BullMQ queue concurrency settings; email_queue table has existing rate controls                            |

</threat_model>

<verification>
- `pnpm vitest run src/__tests__/email-notifications.test.ts` passes
- `notification.processor.ts` contains email_queue insertion logic
- No `resend` import in backend dependencies
</verification>

<success_criteria>
When a notification is processed and the user has email_enabled for that category, a row appears in email_queue with bilingual subject/body matching the user's language preference. Email failures do not block in-app notifications.
</success_criteria>

<output>
After completion, create `.planning/phases/16-email-push-channels/16-01-SUMMARY.md`
</output>
