---
phase: 40-list-pages
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/components/list-page/ListPageShell.tsx
  - frontend/src/components/list-page/ListPageShell.skeleton.tsx
  - frontend/src/components/list-page/GenericListPage.tsx
  - frontend/src/components/list-page/DossierTable.tsx
  - frontend/src/components/list-page/PersonsGrid.tsx
  - frontend/src/components/list-page/EngagementsList.tsx
  - frontend/src/components/list-page/FilterPill.tsx
  - frontend/src/components/list-page/ToolbarSearch.tsx
  - frontend/src/components/list-page/sensitivity.ts
  - frontend/src/components/list-page/index.ts
  - frontend/src/hooks/useCountries.ts
  - frontend/src/hooks/useOrganizations.ts
  - frontend/src/hooks/useEngagementsInfinite.ts
  - frontend/src/styles/list-pages.css
  - frontend/src/index.css
  - frontend/.size-limit.json
  - frontend/src/components/list-page/__tests__/ListPageShell.test.tsx
  - frontend/src/components/list-page/__tests__/GenericListPage.test.tsx
  - frontend/src/components/list-page/__tests__/DossierTable.test.tsx
  - frontend/src/components/list-page/__tests__/PersonsGrid.test.tsx
  - frontend/src/components/list-page/__tests__/EngagementsList.test.tsx
  - frontend/src/hooks/__tests__/useCountries.test.ts
  - frontend/src/hooks/__tests__/useOrganizations.test.ts
  - frontend/src/hooks/__tests__/useEngagementsInfinite.test.ts
autonomous: true
requirements: [LIST-01, LIST-02, LIST-03, LIST-04]
must_haves:
  truths:
    - 'ListPageShell renders title, optional subtitle, optional toolbar, and switches between skeleton/empty/children based on isLoading/isEmpty'
    - 'GenericListPage renders rows with grid template `auto 1fr auto auto`, padding 14px, ≥44px row height, status chip slot, RTL chevron via .icon-flip'
    - 'DossierTable renders header (Name/Engagements/LastTouch/Sensitivity) + DossierGlyph + EN/AR names + numeric sensitivity → chip class via SENSITIVITY_CHIP map; mobile (<768px) collapses to card list'
    - 'PersonsGrid renders 1/2/3-column responsive grid with 44px circular avatar (bg-accent-soft / text-accent-ink), VIP chip when is_vip true, role + organization meta'
    - 'EngagementsList renders search input + 4 filter pills (.btn / .btn.btn-primary on active) + week-list + load-more row with GlobeSpinner during isFetchingNextPage'
    - 'useCountries / useOrganizations Supabase adapter return shape `{ data, pagination }` mirrors useForums.ts:25-97'
    - 'useEngagementsInfinite uses useInfiniteQuery with offset getNextPageParam returning undefined when items.length < limit'
    - 'Handoff CSS rules .spinner-row, .pill.active, .dossier-row, .icon-flip, .week-list, .forum-row, .tbl, .chip* available globally'
    - 'Total JS size-limit budget bumped from 800 KB to 815 KB'
  artifacts:
    - path: 'frontend/src/components/list-page/index.ts'
      provides: 'Barrel re-exports for all primitives + skeletons + sensitivity helper'
      exports:
        [
          'ListPageShell',
          'GenericListPage',
          'DossierTable',
          'PersonsGrid',
          'EngagementsList',
          'FilterPill',
          'ToolbarSearch',
          'SENSITIVITY_CHIP',
        ]
    - path: 'frontend/src/hooks/useCountries.ts'
      provides: "Supabase adapter for type='country' dossiers"
      exports: ['useCountries']
    - path: 'frontend/src/hooks/useOrganizations.ts'
      provides: "Supabase adapter for type='organization' dossiers"
      exports: ['useOrganizations']
    - path: 'frontend/src/hooks/useEngagementsInfinite.ts'
      provides: 'useInfiniteQuery wrapper around engagementsRepo.getEngagements'
      exports: ['useEngagementsInfinite']
    - path: 'frontend/src/styles/list-pages.css'
      provides: 'Handoff CSS port: .spinner-row, .pill, .pill.active, .dossier-row, .icon-flip, .tbl, .chip*, .week-list, .forum-row'
      contains: '.icon-flip'
    - path: 'frontend/.size-limit.json'
      provides: 'Budget gate updated to 815 KB'
      contains: '815 KB'
  key_links:
    - from: 'frontend/src/index.css'
      to: 'frontend/src/styles/list-pages.css'
      via: '@import url(./styles/list-pages.css)'
      pattern: "@import.*list-pages\\.css"
    - from: 'DossierTable'
      to: '@/components/signature-visuals (DossierGlyph)'
      via: 'ES import'
      pattern: "from '@/components/signature-visuals'"
    - from: 'EngagementsList load-more'
      to: '@/components/signature-visuals (GlobeSpinner)'
      via: 'ES import'
      pattern: "GlobeSpinner.*from '@/components/signature-visuals'"
    - from: 'useEngagementsInfinite'
      to: '@/domains/engagements (engagementsRepo)'
      via: 'ES import'
      pattern: "engagementsRepo.*from '@/domains/engagements'"
---

<objective>
Scaffold all Phase 40 primitives (ListPageShell + GenericListPage + DossierTable + PersonsGrid + EngagementsList + FilterPill + ToolbarSearch + skeletons), 3 adapter hooks (useCountries, useOrganizations, useEngagementsInfinite), port the handoff CSS rules required by all bodies, bump the size-limit budget, and ship 8 vitest fixtures (5 component + 3 hook). Wave 1 (the 7 page plans) consume these directly.

Purpose: Eliminate scavenger-hunt risk in Wave 1 — page bodies become mechanical compositions of these primitives.
Output: Complete `frontend/src/components/list-page/` directory + 3 new hooks + handoff CSS port + size-limit bump + 8 test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/40-list-pages/40-CONTEXT.md
@.planning/phases/40-list-pages/40-RESEARCH.md
@.planning/phases/40-list-pages/40-PATTERNS.md
@.planning/phases/40-list-pages/40-VALIDATION.md
@frontend/src/hooks/useForums.ts
@frontend/src/components/ui/table.tsx
@frontend/src/components/signature-visuals/index.ts
@frontend/src/hooks/useDirection.ts
@frontend/src/hooks/useDebouncedValue.ts
@frontend/src/lib/i18n/toArDigits.ts

<interfaces>
<!-- ALL contracts the Wave 1 page bodies will import. Execute Wave 0 against these signatures verbatim. -->

// frontend/src/components/list-page/index.ts (barrel)
export { ListPageShell } from './ListPageShell'
export { GenericListPage, type GenericListPageItem } from './GenericListPage'
export { DossierTable, type DossierTableRow } from './DossierTable'
export { PersonsGrid, type PersonCard } from './PersonsGrid'
export { EngagementsList } from './EngagementsList'
export { FilterPill } from './FilterPill'
export { ToolbarSearch } from './ToolbarSearch'
export {
DossierTableSkeleton,
GenericListSkeleton,
PersonsGridSkeleton,
EngagementsListSkeleton,
} from './ListPageShell.skeleton'
export { SENSITIVITY_CHIP, sensitivityChipClass, sensitivityLabelKey } from './sensitivity'

// ListPageShell props
type ListPageShellProps = {
title: React.ReactNode
subtitle?: React.ReactNode
action?: React.ReactNode
toolbar?: React.ReactNode
isLoading?: boolean
isEmpty?: boolean
emptyState?: React.ReactNode
skeleton?: React.ReactNode
children: React.ReactNode
className?: string
}

// GenericListPage props
type GenericListPageItem = {
id: string
glyphType: 'forum' | 'topic' | 'working_group'
nameEn: string
nameAr?: string
meta?: React.ReactNode
status?: { label: string; tone: 'accent' | 'warn' | 'danger' | 'ok' | 'info' }
href: string
}

// DossierTable props
type DossierTableRow = {
id: string
type: 'country' | 'organization'
name_en: string
name_ar: string
flag_iso?: string
engagement_count: number
updated_at: string
sensitivity_level: 1 | 2 | 3 | 4
}

// PersonsGrid props
type PersonCard = {
id: string
name_en: string
name_ar?: string
role?: string
organization?: string
is_vip?: boolean
flag_iso?: string
}

// Sensitivity LOCK (RESEARCH §"Sensitivity chip + last-touch column mapping")
const SENSITIVITY_CHIP: Record<1 | 2 | 3 | 4, { tone: string; key: string }> = {
1: { tone: 'chip-info', key: 'sensitivity.public' }, // numeric 1 → public → chip-info per RESEARCH note 1
2: { tone: 'chip-default', key: 'sensitivity.internal' }, // 2 → internal → chip default (no variant)
3: { tone: 'chip-warn', key: 'sensitivity.confidential' }, // 3 → confidential → chip-warn
4: { tone: 'chip-danger', key: 'sensitivity.restricted' }, // 4 → restricted → chip-danger
}

// useCountries / useOrganizations return shape (mirrors useForums.ts)
{
data: Dossier[],
pagination: { page: number, limit: number, total: number | null, totalPages: number }
}

// useEngagementsInfinite return shape (TanStack Query InfiniteQueryResult)
{
data: { pages: Array<{ items: Engagement[] }>, pageParams: number[] },
hasNextPage: boolean,
isFetchingNextPage: boolean,
fetchNextPage: () => Promise<void>,
...
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build all 7 list-page primitives + skeletons + sensitivity helper + barrel</name>
  <files>frontend/src/components/list-page/ListPageShell.tsx, frontend/src/components/list-page/ListPageShell.skeleton.tsx, frontend/src/components/list-page/GenericListPage.tsx, frontend/src/components/list-page/DossierTable.tsx, frontend/src/components/list-page/PersonsGrid.tsx, frontend/src/components/list-page/EngagementsList.tsx, frontend/src/components/list-page/FilterPill.tsx, frontend/src/components/list-page/ToolbarSearch.tsx, frontend/src/components/list-page/sensitivity.ts, frontend/src/components/list-page/index.ts, frontend/src/components/list-page/__tests__/ListPageShell.test.tsx, frontend/src/components/list-page/__tests__/GenericListPage.test.tsx, frontend/src/components/list-page/__tests__/DossierTable.test.tsx, frontend/src/components/list-page/__tests__/PersonsGrid.test.tsx, frontend/src/components/list-page/__tests__/EngagementsList.test.tsx</files>
  <read_first>
    - .planning/phases/40-list-pages/40-PATTERNS.md (analog code excerpts for every primitive — lines 41-426)
    - .planning/phases/40-list-pages/40-RESEARCH.md §"Code Examples" (Examples 1-5 — verbatim port targets) + §"Filter pill active styling anatomy"
    - frontend/src/components/ui/table.tsx (DossierTable composes Table/TableHeader/TableBody/TableRow/TableHead/TableCell/TableContainer)
    - frontend/src/components/signature-visuals/index.ts (DossierGlyph + GlobeSpinner exports)
    - frontend/src/hooks/useDebouncedValue.ts (EngagementsList consumes — verify signature `useDebouncedValue(value, ms)` returns the debounced value)
    - frontend/src/hooks/useDirection.ts (returns `{ isRTL: boolean }`)
    - frontend/src/lib/i18n/toArDigits.ts (DossierTable + EngagementsList consume for AR digit rendering)
  </read_first>
  <behavior>
    - ListPageShell: renders `<div className="page space-y-6">` with `.page-head` (title + optional subtitle + optional action), optional toolbar slot, then conditionally renders skeleton (when isLoading), emptyState (when isEmpty && !isLoading), or children
    - GenericListPage: renders `<ul className="forum-list border border-line rounded-md overflow-hidden">` mapping items to `<li className="forum-row grid items-center min-h-11" style={{ gridTemplateColumns: 'auto 1fr auto auto', padding: '14px', borderBottom: '1px solid var(--line-soft)' }}>` with DossierGlyph + Link + meta + chip + ChevronRight; chevron has `className="icon-flip h-4 w-4"`
    - DossierTable: composes existing `Table/TableHeader/TableBody/TableRow/TableHead/TableCell/TableContainer` from `@/components/ui/table` inside `<TableContainer className="hidden md:block rounded-lg border border-line">`; renders Name (DossierGlyph + EN name + AR name with `dir="rtl"`) / Engagements (toArDigits when isRTL) / Last updated / Sensitivity chip via SENSITIVITY_CHIP map / Chevron with `className="icon-flip h-4 w-4 text-ink-mute"`. Renders parallel `<div className="md:hidden space-y-2">` of card-stacked rows for mobile.
    - PersonsGrid: `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">` mapping items to `<Link>` cards with 44px circular initial avatar + name + DossierGlyph + conditional VIP chip + role + organization
    - EngagementsList: search input (250ms debounce via useDebouncedValue) + 4 FilterPill components + week-list grouped by ISO week + LoadMoreRow that shows GlobeSpinner + bilingual loading text when isFetchingNextPage, button "Load more engagements" otherwise
    - FilterPill: `<button type="button" aria-pressed={active} className="btn min-h-11 min-w-11 ${active ? 'btn-primary' : ''}">{label}{count != null && <span>{count}</span>}</button>`
    - ToolbarSearch: `<div className="tb-search relative"><input className="min-h-11 ps-3 pe-3 border border-line rounded-md w-full sm:w-72" type="search" /></div>` with onChange forwarded
    - sensitivity.ts: exports SENSITIVITY_CHIP map + sensitivityChipClass(level) + sensitivityLabelKey(level) helpers
    - All 5 component vitest tests (RTL render assertions): each renders the component with synthetic props, asserts core anatomy (header text, row count, chip class, ≥44px touch via min-h-11 class presence, RTL chevron `icon-flip` class on chevron)
  </behavior>
  <action>
RED → GREEN cycle for each primitive. Write tests FIRST (vitest + @testing-library/react), then ship the implementation.

**Implementation specifics — NO improvisation:**

1. **`ListPageShell.tsx`**: Verbatim from PATTERNS.md lines 56-97. Explicit `JSX.Element` return type. Use `cn` from `@/lib/utils`. Slot order is: head → toolbar → (skeleton OR emptyState OR children). Use `isLoading === true` and `isEmpty === true` strict comparisons (project ESLint rule strict-boolean-expressions).

2. **`ListPageShell.skeleton.tsx`**: Export 4 named skeletons:
   - `DossierTableSkeleton` (rows: number = 8) — array of `<div className="h-11 animate-pulse bg-line-soft rounded-md" />`
   - `PersonsGridSkeleton` (rows: number = 6) — `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">` of card-shaped pulse divs
   - `GenericListSkeleton` (rows: number = 6) — list of row-shaped pulse divs (h-14)
   - `EngagementsListSkeleton` — 1 week header pulse div + 3 row pulse divs
   - All use existing tokens: `bg-line-soft` or `bg-surface-raised` (verify Phase 33 tokens; fall back to `bg-zinc-100 dark:bg-zinc-800` if neither resolves).

3. **`sensitivity.ts`**: Export the `SENSITIVITY_CHIP` const (Record<1|2|3|4, { tone, key }>) per the LOCKED map in `<interfaces>`. Export `sensitivityChipClass(level: 1|2|3|4): string` and `sensitivityLabelKey(level: 1|2|3|4): string`. Defensive default: levels outside 1-4 map to `chip-default` + `sensitivity.public` (warn via console.warn — pattern per Phase 39 D-13).

4. **`GenericListPage.tsx`**: Verbatim from PATTERNS.md lines 110-154 with explicit return type `JSX.Element`. Use `Link` from `@tanstack/react-router`. Use `DossierGlyph` from `@/components/signature-visuals`. Add `<ChevronRight className="icon-flip h-4 w-4 text-ink-mute" />` from `lucide-react` after status chip.

5. **`DossierTable.tsx`**: Verbatim from PATTERNS.md lines 169-262. Imports: `Table, TableBody, TableCell, TableHead, TableHeader, TableRow, TableContainer` from `@/components/ui/table`; `DossierGlyph` from `@/components/signature-visuals`; `Link` from `@tanstack/react-router`; `ChevronRight` from `lucide-react`; `toArDigits` from `@/lib/i18n/toArDigits`; SENSITIVITY_CHIP from `./sensitivity`. Format `updated_at` via `new Intl.DateTimeFormat(isRTL ? 'ar-SA' : 'en-US', { month: 'short', day: 'numeric', year: 'numeric' }).format(new Date(r.updated_at))`. Wrap entire result in `<TableContainer className="hidden md:block ...">` and add `<div className="md:hidden space-y-2">` parallel block listing each row as a stacked card (DossierGlyph + names + chips inline).

6. **`PersonsGrid.tsx`**: Verbatim from PATTERNS.md lines 287-328. `initial(name)` helper extracts uppercased first character.

7. **`EngagementsList.tsx`**: Use signature shown in PATTERNS.md lines 351-426. Group rows by ISO week (compute via `getISOWeek(date) = ((d.getTime() - new Date(d.getFullYear(),0,1).getTime())/86400000 + new Date(d.getFullYear(),0,1).getDay()+1) / 7 | 0` — implement inline). Render `<div className="week-list">` with each week as `<section><header className="week-of">{t('engagements:week.of', { date: formattedWeekStart })}</header>{rows}</section>`. Apply client-side filter per D-07: `all`/`confirmed` (status==='confirmed')/`travel` (kind==='travel')/`pending` (status==='pending'). Search filter via debounced `q` against `title.toLowerCase().includes(q.toLowerCase()) || (counterpart ?? '').toLowerCase().includes(q.toLowerCase())`. LoadMoreRow renders `<div className="spinner-row flex items-center justify-center gap-3 py-4 border-t border-line-soft">` containing `<GlobeSpinner size={16} />` + bilingual `<span>{t('list-pages:loadMore.loading')}</span>` when isFetchingNextPage; otherwise render `<button className="spinner-row btn min-h-11 w-full text-center border-t border-line-soft" onClick={() => void query.fetchNextPage()}>{t('list-pages:loadMore.button')}</button>`.

8. **`FilterPill.tsx`**: `<button type="button" aria-pressed={active} className={"btn min-h-11 min-w-11 " + (active ? "btn-primary" : "")} onClick={onClick}>{label}{count != null ? <span className="ms-2 text-xs opacity-70">{count}</span> : null}</button>`.

9. **`ToolbarSearch.tsx`**: `<div className="tb-search relative"><input type="search" className="min-h-11 ps-3 pe-3 border border-line rounded-md w-full sm:w-72" placeholder={placeholder} value={value} onChange={(e) => onChange(e.target.value)} /></div>`.

10. **`index.ts`**: Barrel re-exports per `<interfaces>` block above. Use named exports only.

**Vitest tests** (5 files in `__tests__/`): Each test file imports the primitive, renders it via `@testing-library/react`, and asserts:

- ListPageShell.test: title renders, toolbar slot renders when provided, skeleton renders when isLoading=true, emptyState renders when isEmpty=true && !isLoading, children render otherwise
- GenericListPage.test: 3 items → 3 `<li>` elements, each with `min-h-11` class, status chip class matches tone, `.icon-flip` chevron present, RTL prop swaps name to nameAr when present
- DossierTable.test: header has 5 columns (Name/Engagements/Last updated/Sensitivity/blank), level=4 row chip has `chip-danger`, level=1 row chip has `chip-info`, RTL render shows AR name in `[dir="rtl"]` cell, mobile breakpoint section (`md:hidden`) present
- PersonsGrid.test: 3 items → 3 cards, avatar has `w-11 h-11 rounded-full bg-accent-soft`, VIP chip renders ONLY when `is_vip === true`, role + organization meta present
- EngagementsList.test: 4 filter pills render with `aria-pressed`, search input min-h-11, mock `useEngagementsInfinite` returning 2 pages → all rows visible, click `Confirmed` pill → only confirmed rows visible, click load-more button calls `fetchNextPage`, GlobeSpinner visible when isFetchingNextPage=true

Mock `@/hooks/useEngagementsInfinite` for the EngagementsList test. Mock `@/hooks/useDebouncedValue` to pass-through (synchronous).

NO `any`. Explicit return types on every function. No `ml/mr/pl/pr/text-left/text-right/left-/right-` — only logical properties.
</action>
<verify>
<automated>cd frontend && pnpm vitest run src/components/list-page/**tests**/ --reporter=dot</automated>
</verify>
<acceptance_criteria> - All 10 primitive files exist at the exact paths in `files_modified` - `frontend/src/components/list-page/index.ts` exports all 8 primitives + 4 skeletons + 3 sensitivity helpers - `pnpm vitest run src/components/list-page/__tests__/` exits 0 with 5 files passing - `grep -rn "ml-\|mr-\|pl-\|pr-\|text-left\|text-right\|rounded-l-\|rounded-r-" frontend/src/components/list-page/` returns 0 matches - `grep -rn ": any\|as any\|<any>" frontend/src/components/list-page/` returns 0 matches - `grep -n "scaleX(-1)\|icon-flip" frontend/src/components/list-page/DossierTable.tsx` returns at least 1 match - `grep -n "GlobeSpinner" frontend/src/components/list-page/EngagementsList.tsx` returns at least 1 match - `grep -n "SENSITIVITY_CHIP" frontend/src/components/list-page/sensitivity.ts` returns at least 1 match - `grep -n "min-h-11" frontend/src/components/list-page/FilterPill.tsx` returns at least 1 match
</acceptance_criteria>
<done>All primitives + skeletons + barrel ship; 5 vitest files green; zero physical RTL classes; zero `any`; touch targets enforced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build 3 adapter hooks (useCountries / useOrganizations / useEngagementsInfinite) + tests</name>
  <files>frontend/src/hooks/useCountries.ts, frontend/src/hooks/useOrganizations.ts, frontend/src/hooks/useEngagementsInfinite.ts, frontend/src/hooks/__tests__/useCountries.test.ts, frontend/src/hooks/__tests__/useOrganizations.test.ts, frontend/src/hooks/__tests__/useEngagementsInfinite.test.ts</files>
  <read_first>
    - frontend/src/hooks/useForums.ts (analog: lines 25-97 — verbatim port pattern with type swap)
    - frontend/src/domains/engagements/index.ts (locate engagementsRepo export)
    - frontend/src/domains/engagements/repositories/engagements.repository.ts (verify getEngagements signature: `{ page, limit, sort_by, sort_order, search? }` returns `{ items?: Engagement[], total?: number }` per RESEARCH §"Engagements pagination column")
    - .planning/phases/40-list-pages/40-PATTERNS.md §"useCountries.ts" + §"useEngagementsInfinite.ts" (lines 432-535)
    - frontend/src/hooks/__tests__/ (existing test patterns for TanStack Query hooks — locate analog if present)
  </read_first>
  <behavior>
    - useCountries({ search?, status?, page=1, limit=20 }): TanStack useQuery with key `['countries', filters]`, queries Supabase `from('dossiers').select('*', { count: 'exact' }).eq('type', 'country').neq('status', 'deleted')`, applies search via `.or('name_en.ilike.%${search}%,name_ar.ilike.%${search}%')`, optional status filter, orders by `updated_at desc`, ranges via `(offset, offset+limit-1)`. Returns `{ data, pagination: { page, limit, total, totalPages } }`. staleTime 5 min.
    - useOrganizations: identical pattern with `eq('type', 'organization')`.
    - useEngagementsInfinite({ search?, limit=20 }): useInfiniteQuery key `['engagements-infinite', params]`, initialPageParam 1, queryFn calls `engagementsRepo.getEngagements({ page: pageParam, limit, sort_by: 'start_date', sort_order: 'desc', search })`, getNextPageParam returns `items.length < limit ? undefined : allPages.length + 1`, staleTime 30s.
    - Tests: each hook test mocks `@/lib/supabase` (or `engagementsRepo` for infinite), wraps in QueryClientProvider, asserts queryKey shape, asserts pagination math (page=2, limit=20 → range(20, 39)), asserts getNextPageParam returns undefined when items.length < limit and increments otherwise.
  </behavior>
  <action>
RED → GREEN per hook.

**`useCountries.ts`**: Verbatim from PATTERNS.md lines 444-484. Imports:

```ts
import { useQuery } from '@tanstack/react-query'
import { supabase } from '@/lib/supabase'
```

Explicit return type. Strict boolean expressions. Throw `new Error(error.message)` on Supabase error.

**`useOrganizations.ts`**: Identical to useCountries.ts with two changes: `COUNTRIES_QUERY_KEY` → `ORGANIZATIONS_QUERY_KEY = 'organizations'`, `eq('type', 'country')` → `eq('type', 'organization')`.

**`useEngagementsInfinite.ts`**: Verbatim from PATTERNS.md lines 508-533. Imports:

```ts
import { useInfiniteQuery } from '@tanstack/react-query'
import { engagementsRepo } from '@/domains/engagements'
```

If `engagementsRepo` is not directly exported from `@/domains/engagements`, fall back to deep import `@/domains/engagements/repositories/engagements.repository` (verify path exists per the analog file referenced in `read_first`).

**Tests** (3 files in `frontend/src/hooks/__tests__/`):

1. **useCountries.test.ts**: Mock `@/lib/supabase` to return a chained mock that resolves to `{ data: [{id:'a',type:'country'}], error: null, count: 1 }`. Wrap render in `QueryClientProvider`. Call `useCountries({ page: 2, limit: 20 })`. Assert: `mockRange` called with (20, 39); query key starts with `'countries'`; result `pagination.totalPages` matches `Math.ceil(count/limit)`. Add a second test where `error: { message: 'boom' }` is returned — assert query throws.

2. **useOrganizations.test.ts**: Same shape with `'organization'` swap. Asserts query key starts with `'organizations'` and `eq('type', 'organization')` is called.

3. **useEngagementsInfinite.test.ts**: Mock `engagementsRepo.getEngagements` to return `{ items: Array(20).fill({id:'e'}) }` for page 1 and `{ items: Array(5).fill({id:'e'}) }` for page 2. Render the hook. Assert: getNextPageParam after page 1 returns 2 (because items.length === limit); after page 2 returns undefined (because 5 < 20). Verify queryFn was called with `{ page: 1, limit: 20, sort_by: 'start_date', sort_order: 'desc', search: undefined }`.
   </action>
   <verify>
   <automated>cd frontend && pnpm vitest run src/hooks/**tests**/useCountries.test.ts src/hooks/**tests**/useOrganizations.test.ts src/hooks/**tests**/useEngagementsInfinite.test.ts --reporter=dot</automated>
   </verify>
   <acceptance_criteria> - All 3 hook source files exist and export the named hook - All 3 test files pass (`pnpm vitest run` exits 0) - `grep -n "from '@/lib/supabase'" frontend/src/hooks/useCountries.ts` returns 1 match - `grep -n "from '@/lib/supabase'" frontend/src/hooks/useOrganizations.ts` returns 1 match - `grep -n "useInfiniteQuery\|engagementsRepo" frontend/src/hooks/useEngagementsInfinite.ts` returns at least 2 matches - `grep -n "eq('type', 'country')" frontend/src/hooks/useCountries.ts` returns 1 match - `grep -n "eq('type', 'organization')" frontend/src/hooks/useOrganizations.ts` returns 1 match - `grep -n "getNextPageParam" frontend/src/hooks/useEngagementsInfinite.ts` returns 1 match - `grep -rn ": any\|as any\|<any>" frontend/src/hooks/useCountries.ts frontend/src/hooks/useOrganizations.ts frontend/src/hooks/useEngagementsInfinite.ts` returns 0 matches
   </acceptance_criteria>
   <done>3 adapter hooks ship with full Supabase / useInfiniteQuery semantics; 3 vitest files green; pagination math verified; type strictness enforced.</done>
   </task>

<task type="auto">
  <name>Task 3: Port handoff CSS rules + bump size-limit budget</name>
  <files>frontend/src/styles/list-pages.css, frontend/src/index.css, frontend/.size-limit.json</files>
  <read_first>
    - /tmp/inteldossier-handoff/inteldossier/project/src/app.css (port targets — see line ranges below)
    - frontend/src/index.css (locate appropriate `@import` insertion point, near other component CSS imports)
    - frontend/.size-limit.json (current `Total JS` budget = 800 KB, line bump → 815 KB)
    - .planning/phases/40-list-pages/40-RESEARCH.md §"Architecture-specific Resolutions" §1 (handoff CSS rule line ranges)
  </read_first>
  <action>
**Step A — Create `frontend/src/styles/list-pages.css`:**

Port the following rules verbatim from `/tmp/inteldossier-handoff/inteldossier/project/src/app.css`. Each rule must be copied byte-identical from the listed line range; substitute `--ink`/`--surface`/`--line`/`--line-soft`/`--accent`/`--accent-soft`/`--accent-ink`/`--ink-mute`/`--ink-faint`/`--radius`/`--radius-sm`/`--pad`/`--gap`/`--font-display`/`--font-body` with the existing Phase 33 token names already wired in `frontend/src/index.css` (verify each token resolves; if a token name has changed since handoff, prefer the Phase 33 name).

Required rules:

1. `.tb-search`, `.tb-search input`, `.tb-search input::placeholder` — handoff lines 115-117
2. `.btn`, `.btn.btn-primary` (filter pill base + active) — handoff lines 222-225 (use `--ink` for primary background per RESEARCH §6 LOCK; ignore the `dir-situation`/`dir-chancery` overrides at handoff lines 226,228)
3. `.card` — handoff line 233 (border + radius + padding)
4. `.chip`, `.chip-accent`, `.chip-danger`, `.chip-warn`, `.chip-ok`, `.chip-info`, `.chip-default` — handoff lines 245-250 (font-size 11, padding 2px 8px, border-radius 99px). Add `.chip-default` variant if missing in handoff (use `background: var(--surface-raised); color: var(--ink-mute); border: 1px solid var(--line)`).
5. `table.tbl`, `.tbl th`, `.tbl td`, `.tbl tr:last-child td`, `.tbl tr:hover td` — handoff lines 260-265
6. `.week-list`, `.week-row`, `.week-date`, `.week-day`, `.week-dd`, `.week-time`, `.week-body`, `.week-title`, `.week-meta`, `.week-meta .sep`, `.week-right` — handoff lines 309-322
7. `.forums-list` (alias also as `.forum-list`), `.forum-row`, `.forum-row:last-child`, `.forum-name` — handoff lines 435-440
8. `.empty-hint`, `.empty-hint .sub` — handoff lines 457-458
9. `html[dir="rtl"] .icon-flip { transform: scaleX(-1); }` — handoff line 667 (CRITICAL — drives all chevron mirroring)
10. `.page`, `.page-head`, `.page-title`, `.page-sub` — handoff lines 201-208
11. `.spinner-row { display: flex; align-items: center; gap: 12px; padding: 16px; min-height: 44px; }` — port the rule (locate exact lines via grep on handoff source; if not found verbatim, use this canonical definition derived from RESEARCH usage at handoff `pages.jsx:90-111`)
12. `.dossier-row { display: grid; grid-template-columns: auto 1fr auto; align-items: center; gap: 12px; padding: 12px; border-bottom: 1px solid var(--line-soft); min-height: 44px; }` — port (handoff source uses inline styles in `DossierListPage`; canonicalize for reuse).

If any handoff rule already exists in `frontend/src/index.css` from Phase 33/36/37 (run `grep -n "spinner-row\|\.pill\|dossier-row\|icon-flip\|\.tbl\|\.chip\|\.week-list\|\.forum-row" frontend/src/index.css`), DO NOT re-add it in `list-pages.css` — note the duplicate-skip in the file's leading comment.

**Step B — Wire the import in `frontend/src/index.css`:**

Add `@import url('./styles/list-pages.css');` near the top of `frontend/src/index.css`, AFTER any Phase 33 token import and AFTER any HeroUI `@import` lines but BEFORE app-level layered rules. Use the Edit tool — DO NOT recreate `index.css`.

**Step C — Bump size-limit budget:**

Use the Edit tool on `frontend/.size-limit.json` to change ONLY the `Total JS` entry's `"limit": "800 KB"` → `"limit": "815 KB"`. All other entries unchanged.
</action>
<verify>
<automated>test -f frontend/src/styles/list-pages.css && grep -q 'icon-flip' frontend/src/styles/list-pages.css && grep -q 'spinner-row' frontend/src/styles/list-pages.css && grep -q "list-pages.css" frontend/src/index.css && grep -q '"815 KB"' frontend/.size-limit.json && echo "OK"</automated>
</verify>
<acceptance_criteria> - `frontend/src/styles/list-pages.css` exists and contains rules: `.tb-search`, `.btn.btn-primary`, `.chip`, `.tbl`, `.week-list`, `.forum-row`, `.icon-flip`, `.spinner-row`, `.page`, `.page-head` - `grep -n "@import.*list-pages" frontend/src/index.css` returns 1 match - `frontend/.size-limit.json` `Total JS` budget reads `815 KB` - `grep -c '"800 KB"' frontend/.size-limit.json` returns 0 (no leftover 800 KB entry) - `grep -n "scaleX(-1)" frontend/src/styles/list-pages.css` returns at least 1 match - `cd frontend && pnpm dev --no-open & sleep 5 && curl -sf http://localhost:5173/src/styles/list-pages.css >/dev/null; kill %1` (optional sanity check — skip if dev server unavailable in agent sandbox)
</acceptance_criteria>
<done>Handoff CSS port lives in `list-pages.css`, imported by `index.css`, size-limit bumped to 815 KB, all required selectors present.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                                                                                                                       |
| ------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Browser ↔ Supabase                   | useCountries / useOrganizations issue authenticated PostgREST calls; visibility enforced by row-level security policies on `dossiers`.                                            |
| Browser ↔ Backend REST               | useEngagementsInfinite issues authenticated REST to `/engagements`; backend re-enforces RLS via service-role-bypassed SQL with explicit user-scoped filters (per Phase 38 audit). |
| User input → Supabase `.or()` filter | Search string interpolated into PostgREST filter expression.                                                                                                                      |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                     | Disposition | Mitigation Plan                                                                                                                                                                                                                                                   |
| ---------- | ---------------------- | ------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-40-02-01 | Tampering              | useCountries / useOrganizations search filter                 | mitigate    | Search string passed as PostgREST `.or('name_en.ilike.%${search}%,...')` is parameterized by Supabase JS client; client escapes special PostgREST operator characters. Add hard length cap `search.slice(0, 200)` in adapter to prevent expensive ILIKE patterns. |
| T-40-02-02 | Information disclosure | sensitivity_level chip rendering                              | mitigate    | Chip is display-only; row visibility enforced by Supabase RLS already filters out rows the user cannot read. Chip cannot reveal data the user couldn't otherwise see.                                                                                             |
| T-40-02-03 | Information disclosure | EngagementsList client-side filter on `kind === 'travel'`     | accept      | All engagements returned by RLS-filtered backend are already visible to user; client-side filter is UX-only.                                                                                                                                                      |
| T-40-02-04 | Spoofing               | Click-target navigation `/engagements/$engagementId/overview` | accept      | Engagement detail route enforces RLS independently; navigating to a forbidden engagement renders a 404/forbidden surface.                                                                                                                                         |
| T-40-02-05 | Tampering              | Inline event handlers (onClick) on rows / chevrons            | mitigate    | All event handlers consume row id via JSX prop closure (not URL parsing); React's default JSX value escaping protects rendered text content. No raw HTML insertion APIs anywhere in Phase 40.                                                                     |

</threat_model>

<verification>
- All 8 vitest files green (`cd frontend && pnpm vitest run src/components/list-page/__tests__/ src/hooks/__tests__/useCountries.test.ts src/hooks/__tests__/useOrganizations.test.ts src/hooks/__tests__/useEngagementsInfinite.test.ts`)
- `frontend/src/components/list-page/index.ts` barrel resolves all named exports
- `frontend/src/styles/list-pages.css` imported by `index.css`
- size-limit Total JS = 815 KB
- Zero physical RTL classes in `list-page/`
- Zero `any` in new code
</verification>

<success_criteria>

- 10 primitive files + 3 hook files + 8 test files + CSS port + size-limit bump complete
- All Wave 1 page plans can import from `@/components/list-page` + `@/hooks/{useCountries,useOrganizations,useEngagementsInfinite}` without further plumbing
- Handoff CSS classes available globally
- Wave 1 unblocked
  </success_criteria>

<output>
After completion, create `.planning/phases/40-list-pages/40-02-SUMMARY.md`. Note any handoff CSS rule line-range adjustments and whether `engagementsRepo` deep-import fallback was needed.
</output>
