---
phase: 40-list-pages
plan: 07
type: execute
wave: 1
depends_on: [40-01, 40-02]
files_modified:
  - frontend/src/routes/_protected/dossiers/topics/index.tsx
  - frontend/src/routes/_protected/dossiers/topics/__tests__/TopicsListPage.test.tsx
autonomous: true
requirements: [LIST-03]
must_haves:
  truths:
    - "/dossiers/topics renders <GenericListPage> with glyphType='topic'"
    - 'Status chip mapping: active→ok, archived→info, draft→warn'
    - "Loading: GenericListSkeleton; empty: 'No topics yet'"
    - 'Row click → /dossiers/topics/$id'
  artifacts:
    - path: 'frontend/src/routes/_protected/dossiers/topics/index.tsx'
      provides: 'Replaced body — ListPageShell + GenericListPage + useTopics'
      exports: ['Route']
  key_links:
    - from: 'TopicsListPage'
      to: 'useTopics'
      via: 'ES import'
      pattern: "useTopics.*from '@/hooks/useTopics'"
---

<objective>
Replace `frontend/src/routes/_protected/dossiers/topics/index.tsx` body. Mirror plan 06 (Forums) with `useTopics` + topic-specific status mapping. Per LIST-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-list-pages/40-CONTEXT.md
@.planning/phases/40-list-pages/40-RESEARCH.md
@.planning/phases/40-list-pages/40-PATTERNS.md
@.planning/phases/40-list-pages/40-06-SUMMARY.md
@frontend/src/routes/_protected/dossiers/forums/index.tsx
@frontend/src/hooks/useTopics.ts

<interfaces>
import { ListPageShell, GenericListPage, GenericListSkeleton, ToolbarSearch, type GenericListPageItem } from '@/components/list-page'
import { useTopics } from '@/hooks/useTopics'

useTopics return shape: verify in domain (RESEARCH §9 noted 'verify in domain'). Implementation MUST handle both `Topic[]` and `{ items: Topic[] }` and `{ data: Topic[] }` shapes via runtime check.

Status tone map (topics):

- active → ok
- archived → info
- draft → warn
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace topics/index.tsx body + render-assertion test</name>
  <files>frontend/src/routes/_protected/dossiers/topics/index.tsx, frontend/src/routes/_protected/dossiers/topics/__tests__/TopicsListPage.test.tsx</files>
  <read_first>
    - frontend/src/routes/_protected/dossiers/forums/index.tsx (analog from plan 06 — IDENTICAL with `forums`→`topics`/`forum`→`topic` swap)
    - frontend/src/routes/_protected/dossiers/topics/index.tsx (current body — to be replaced)
    - frontend/src/hooks/useTopics.ts (verify return shape — re-export from @/domains/topics per RESEARCH §9)
    - .planning/phases/40-list-pages/40-PATTERNS.md §"GenericListPage.tsx"
  </read_first>
  <behavior>
    - Mirror plan 06 (Forums) structure
    - Use `useTopics` instead of `useForums`
    - Translation namespace `useTranslation(['topics', 'list-pages'])`
    - Status tone map per `<interfaces>` (active/archived/draft)
    - `glyphType: 'topic' as const`
    - Href `/dossiers/topics/${id}`
    - Handle `useTopics` return-shape variants (Topic[] OR `{ items }` OR `{ data }`) via runtime check
  </behavior>
  <action>
**Step A — Create `frontend/src/routes/_protected/dossiers/topics/index.tsx`:**

Use the Write tool. Adapt plan 06's forums body with these substitutions:

- Route path → `/_protected/dossiers/topics/`
- `useTranslation(['forums', 'list-pages'])` → `useTranslation(['topics', 'list-pages'])`
- `useForums` → `useTopics`
- `glyphType: 'forum'` → `glyphType: 'topic'`
- `href: '/dossiers/forums/...'` → `'/dossiers/topics/...'`
- `FORUM_STATUS_TONE` map → `TOPIC_STATUS_TONE = { active: 'ok', archived: 'info', draft: 'warn' }` (default fallback `'info'`)
- All `t('forums:...')` keys → `t('topics:...')`
- Function name → `TopicsListPage`

**Critical — handle useTopics return shape variants:**
Per RESEARCH §9, useTopics is a re-export from `@/domains/topics`; the actual return shape may be `Topic[]` directly OR `{ items }` OR `{ data }`. Use the same runtime list-extraction pattern from plan 05's PersonsListPage:

```ts
const raw = query.data as unknown
let list: Array<Record<string, unknown>> = []
if (Array.isArray(raw)) list = raw as Array<Record<string, unknown>>
else if (
  raw != null &&
  typeof raw === 'object' &&
  'items' in raw &&
  Array.isArray((raw as { items?: unknown[] }).items)
)
  list = (raw as { items: Array<Record<string, unknown>> }).items
else if (
  raw != null &&
  typeof raw === 'object' &&
  'data' in raw &&
  Array.isArray((raw as { data?: unknown[] }).data)
)
  list = (raw as { data: Array<Record<string, unknown>> }).data
```

NO `any`. Logical properties only.

**Step B — Test** at `__tests__/TopicsListPage.test.tsx`:

Mock `@/hooks/useTopics` returning `{ data: [{id:'t1',name_en:'Climate Policy',name_ar:'سياسة المناخ',status:'active',updated_at:'2026-04-15T00:00:00Z'},{id:'t2',name_en:'Trade Reform',name_ar:'إصلاح التجارة',status:'draft',updated_at:'2026-03-01T00:00:00Z'}], isLoading: false }`. Mock `useDirection` `{isRTL:false}`.

Assert:

- `Topics` title visible
- 2 row `<li>` elements
- `Climate Policy` row contains chip class `chip-ok`
- `Trade Reform` row contains chip class `chip-warn`
- Chevron has class `icon-flip`
  </action>
  <verify>
  <automated>cd frontend && pnpm vitest run src/routes/\_protected/dossiers/topics/**tests**/TopicsListPage.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria> - File exists with `createFileRoute('/_protected/dossiers/topics/')` - `grep -n "GenericListPage\|useTopics\|ListPageShell" frontend/src/routes/_protected/dossiers/topics/index.tsx` returns at least 3 matches - `grep -rn "ml-\|mr-\|pl-\|pr-\|text-left\|text-right\|rounded-l-\|rounded-r-" frontend/src/routes/_protected/dossiers/topics/index.tsx` returns 0 matches - `grep -rn ": any\|as any\|<any>" frontend/src/routes/_protected/dossiers/topics/index.tsx` returns 0 matches - Vitest passes
  </acceptance_criteria>
  <done>Topics page renders via GenericListPage; LIST-03 covered for topics.</done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description        |
| ------------------------------ | ------------------ |
| Browser → useTopics → Supabase | RLS-enforced read. |

## STRIDE Threat Register

| Threat ID  | Category  | Component                        | Disposition | Mitigation Plan            |
| ---------- | --------- | -------------------------------- | ----------- | -------------------------- |
| T-40-07-01 | Tampering | URL search param                 | mitigate    | validateSearch coerces.    |
| T-40-07-02 | Spoofing  | Row click → /dossiers/topics/$id | accept      | Detail route enforces RLS. |

</threat_model>

<verification>
- Vitest passes; topic status tone mapping verified
- Zero physical RTL classes; no `any`
</verification>

<success_criteria>

- LIST-03 covered for topics
  </success_criteria>

<output>
After completion, create `.planning/phases/40-list-pages/40-07-SUMMARY.md`. Note actual useTopics return shape encountered.
</output>
