---
phase: 40-list-pages
plan: 08
type: execute
wave: 1
depends_on: [40-01, 40-02]
files_modified:
  - frontend/src/routes/_protected/dossiers/working_groups/index.tsx
  - frontend/src/routes/_protected/dossiers/working_groups/__tests__/WorkingGroupsListPage.test.tsx
autonomous: true
requirements: [LIST-03]
must_haves:
  truths:
    - "/dossiers/working_groups renders <GenericListPage> with glyphType='working_group'"
    - 'Status chip mapping: active→ok, completed→info, on_hold→warn'
    - "Loading: GenericListSkeleton; empty: 'No working groups yet'"
    - 'Row click → /dossiers/working-groups/$id (note hyphenated route segment per dossier-routes.ts)'
  artifacts:
    - path: 'frontend/src/routes/_protected/dossiers/working_groups/index.tsx'
      provides: 'Replaced body — ListPageShell + GenericListPage + useWorkingGroups'
      exports: ['Route']
  key_links:
    - from: 'WorkingGroupsListPage'
      to: 'useWorkingGroups'
      via: 'ES import'
      pattern: "useWorkingGroups.*from '@/hooks/useWorkingGroups'"
---

<objective>
Replace `frontend/src/routes/_protected/dossiers/working_groups/index.tsx` body. Mirror plan 06 with `useWorkingGroups`. Per LIST-03.

Note: route directory is `working_groups/` (underscore) but click-target route segment is `working-groups` (hyphen) per `getDossierRouteSegment('working_group')`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-list-pages/40-CONTEXT.md
@.planning/phases/40-list-pages/40-RESEARCH.md
@.planning/phases/40-list-pages/40-PATTERNS.md
@.planning/phases/40-list-pages/40-06-SUMMARY.md
@frontend/src/routes/_protected/dossiers/forums/index.tsx
@frontend/src/hooks/useWorkingGroups.ts
@frontend/src/lib/dossier-routes.ts

<interfaces>
import { ListPageShell, GenericListPage, GenericListSkeleton, ToolbarSearch, type GenericListPageItem } from '@/components/list-page'
import { useWorkingGroups } from '@/hooks/useWorkingGroups'
import { getDossierRouteSegment } from '@/lib/dossier-routes'

useWorkingGroups({ search?, page=1, limit=20 }) → { data: { data: WorkingGroup[], pagination }, isLoading } (mirrors useForums per RESEARCH §9)

Status tone map (working groups):

- active → ok
- completed → info
- on_hold → warn
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace working_groups/index.tsx body + render-assertion test</name>
  <files>frontend/src/routes/_protected/dossiers/working_groups/index.tsx, frontend/src/routes/_protected/dossiers/working_groups/__tests__/WorkingGroupsListPage.test.tsx</files>
  <read_first>
    - frontend/src/routes/_protected/dossiers/forums/index.tsx (analog from plan 06)
    - frontend/src/routes/_protected/dossiers/working_groups/index.tsx (current body — to be replaced)
    - frontend/src/hooks/useWorkingGroups.ts (verify return shape — Supabase pattern per RESEARCH §9)
    - frontend/src/lib/dossier-routes.ts (verify `getDossierRouteSegment('working_group')` returns `'working-groups'` — hyphenated)
    - .planning/phases/40-list-pages/40-PATTERNS.md §"GenericListPage.tsx"
  </read_first>
  <behavior>
    - Mirror plan 06 (Forums) structure
    - Use `useWorkingGroups`
    - Translation namespace `useTranslation(['working-groups', 'list-pages'])` (note hyphenated namespace name matches locale file `working-groups.json`)
    - Status tone map: active→ok, completed→info, on_hold→warn
    - `glyphType: 'working_group' as const`
    - Href: `/dossiers/${getDossierRouteSegment('working_group')}/${id}` (resolves to `/dossiers/working-groups/${id}`)
    - Route path stays `/_protected/dossiers/working_groups/` (filesystem)
  </behavior>
  <action>
**Step A — Create `frontend/src/routes/_protected/dossiers/working_groups/index.tsx`:**

Use the Write tool. Adapt plan 06's forums body with these substitutions:

- Route path → `createFileRoute('/_protected/dossiers/working_groups/')`
- `useTranslation(['forums', 'list-pages'])` → `useTranslation(['working-groups', 'list-pages'])`
- `useForums` → `useWorkingGroups`
- `glyphType: 'forum'` → `glyphType: 'working_group'`
- `FORUM_STATUS_TONE` → `WG_STATUS_TONE = { active: 'ok', completed: 'info', on_hold: 'warn' }` (default `'info'`)
- All `t('forums:...')` → `t('working-groups:...')`
- `href: '/dossiers/forums/...'` → `` `/dossiers/${getDossierRouteSegment('working_group')}/${id}` ``
- Function name → `WorkingGroupsListPage`

Import `getDossierRouteSegment` from `@/lib/dossier-routes`.

NO `any`. Logical properties only. Strict boolean expressions.

**Step B — Test** at `__tests__/WorkingGroupsListPage.test.tsx`:

Mock `@/hooks/useWorkingGroups` returning `{ data: { data: [{id:'wg1',name_en:'AI Ethics WG',name_ar:'فريق أخلاقيات الذكاء الاصطناعي',status:'active',updated_at:'2026-04-15T00:00:00Z'},{id:'wg2',name_en:'Trade Sanctions WG',name_ar:'فريق العقوبات التجارية',status:'on_hold',updated_at:'2026-03-01T00:00:00Z'}], pagination:{page:1,limit:20,total:2,totalPages:1} }, isLoading: false }`. Mock `useDirection` `{isRTL:false}`.

Assert:

- `Working Groups` title visible
- 2 row `<li>` elements
- `AI Ethics WG` row contains chip class `chip-ok`
- `Trade Sanctions WG` row contains chip class `chip-warn`
- Chevron has class `icon-flip`
- Each row has `min-h-11`
  </action>
  <verify>
  <automated>cd frontend && pnpm vitest run src/routes/\_protected/dossiers/working_groups/**tests**/WorkingGroupsListPage.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria> - File exists with `createFileRoute('/_protected/dossiers/working_groups/')` - `grep -n "GenericListPage\|useWorkingGroups\|ListPageShell" frontend/src/routes/_protected/dossiers/working_groups/index.tsx` returns at least 3 matches - `grep -n "getDossierRouteSegment\|working-groups" frontend/src/routes/_protected/dossiers/working_groups/index.tsx` returns at least 1 match - `grep -rn "ml-\|mr-\|pl-\|pr-\|text-left\|text-right\|rounded-l-\|rounded-r-" frontend/src/routes/_protected/dossiers/working_groups/index.tsx` returns 0 matches - `grep -rn ": any\|as any\|<any>" frontend/src/routes/_protected/dossiers/working_groups/index.tsx` returns 0 matches - Vitest passes
  </acceptance_criteria>
  <done>Working Groups page renders via GenericListPage; LIST-03 covered for WG.</done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description        |
| ------------------------------------- | ------------------ |
| Browser → useWorkingGroups → Supabase | RLS-enforced read. |

## STRIDE Threat Register

| Threat ID  | Category  | Component                                | Disposition | Mitigation Plan            |
| ---------- | --------- | ---------------------------------------- | ----------- | -------------------------- |
| T-40-08-01 | Tampering | URL search param                         | mitigate    | validateSearch coerces.    |
| T-40-08-02 | Spoofing  | Row click → /dossiers/working-groups/$id | accept      | Detail route enforces RLS. |

</threat_model>

<verification>
- Vitest passes
- Zero physical RTL classes; no `any`
- Click target uses hyphenated route segment via getDossierRouteSegment
</verification>

<success_criteria>

- LIST-03 covered for working groups
  </success_criteria>

<output>
After completion, create `.planning/phases/40-list-pages/40-08-SUMMARY.md`.
</output>
