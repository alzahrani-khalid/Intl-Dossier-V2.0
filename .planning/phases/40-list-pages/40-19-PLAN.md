---
plan_id: 40-19
phase: 40
phase_name: list-pages
mode: gap_closure
gaps_addressed: [G1, G2, G3, G4, G5, G6, G7, G8]
requirements_addressed: []
depends_on: [40-12, 40-13, 40-14, 40-15, 40-16, 40-17, 40-18]
wave: 4
files_modified:
  - .planning/phases/40-list-pages/VERIFICATION.md
  - .planning/phases/40-list-pages/40-HUMAN-UAT.md
  - .planning/STATE.md
autonomous: false
estimated_loc: 30
must_haves:
  truths:
    - "All 6 E2E spec files (render, rtl, engagements, a11y, touch-targets, visual) green on chromium-darwin"
    - "All 14 visual baselines stable across 3 consecutive replays (0 pixel drift)"
    - "axe-core: 0 violations on all 7 pages × LTR + AR"
    - "VERIFICATION.md status flips from PASS-WITH-DEVIATION to PASS"
    - "HUMAN-UAT.md items 1-10 all marked passed"
  artifacts:
    - path: ".planning/phases/40-list-pages/VERIFICATION.md"
      provides: "Updated verdict block with status: PASS"
      contains: "status: PASS"
    - path: ".planning/phases/40-list-pages/40-HUMAN-UAT.md"
      provides: "Updated UAT items with passed: 10"
      contains: "passed: 10"
  key_links:
    - from: ".planning/STATE.md"
      to: ".planning/phases/40-list-pages/VERIFICATION.md"
      via: "phase verdict update"
      pattern: "Phase 40.*PASS"
---

<objective>
Final gate flip — re-run all 6 E2E spec files end-to-end, capture stable baselines, and flip the Phase 40 verdict from `PASS-WITH-DEVIATION` to `PASS`. Includes a human checkpoint for the operator to confirm the gate before STATE.md and ROADMAP.md are updated.

Purpose: Close all 8 gaps (G1–G8); convert the deviation note to PASS; mark Phase 40 complete.
Output: VERIFICATION.md, 40-HUMAN-UAT.md, STATE.md updated; phase closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/40-list-pages/VERIFICATION.md
@.planning/phases/40-list-pages/40-HUMAN-UAT.md
@.planning/STATE.md
@.planning/phases/40-list-pages/40-CONTEXT-GAPS.md
</context>

<tasks>

<task id="t1" type="auto">
  <name>Task 1: Full E2E re-run on chromium-darwin</name>
  <files>(verification only)</files>
  <read_first>
    - frontend/playwright.config.ts (confirm chromium-darwin project still defined)
  </read_first>
  <action>
    With the dev server running (`pnpm dev` in a separate terminal), execute the full Phase 40 E2E suite:

    ```
    pnpm playwright test \
      list-pages-render \
      list-pages-rtl \
      list-pages-engagements \
      list-pages-a11y \
      list-pages-touch-targets \
      list-pages-visual \
      --project=chromium-darwin --reporter=line
    ```

    Expected: ALL specs pass. If any fails, this task BLOCKS — do not proceed to verdict flip. Patch the failing area (route to the appropriate gap-closure plan) and re-run from this task.

    Capture the full results to `/tmp/40-19-final-run.txt` for the SUMMARY:
    `pnpm playwright test list-pages-* --project=chromium-darwin --reporter=line > /tmp/40-19-final-run.txt 2>&1`
  </action>
  <verify>
    <automated>pnpm playwright test list-pages-render list-pages-rtl list-pages-engagements list-pages-a11y list-pages-touch-targets list-pages-visual --project=chromium-darwin --reporter=line</automated>
  </verify>
  <done>All 6 spec files green; result captured.</done>
</task>

<task id="t2" type="auto">
  <name>Task 2: Triple-replay visual suite for stability proof</name>
  <files>(verification only)</files>
  <read_first>
    - .planning/phases/40-list-pages/40-CONTEXT-GAPS.md §G7 verification
  </read_first>
  <action>
    Replay only the visual suite three consecutive times to prove determinism:

    ```
    pnpm playwright test list-pages-visual --project=chromium-darwin --reporter=line
    pnpm playwright test list-pages-visual --project=chromium-darwin --reporter=line
    pnpm playwright test list-pages-visual --project=chromium-darwin --reporter=line
    ```

    All three runs must show 14/14 passing with 0 pixel-diff failures. Record results in 40-19-SUMMARY.md as a 3-line table.

    If ANY replay shows drift, the determinism stack from 40-17 is incomplete — file the residual cause in the SUMMARY and reopen as a follow-up.
  </action>
  <verify>
    <automated>pnpm playwright test list-pages-visual --project=chromium-darwin --reporter=line</automated>
  </verify>
  <done>Three consecutive visual replays clean.</done>
</task>

<task id="t3" type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Operator approves gate flip</name>
  <what-built>
    - 6/6 E2E spec files green on chromium-darwin
    - 14/14 visual baselines stable across 3 replays
    - axe-core: 0 violations on all 7 × 2 locales
    - All 8 gaps (G1–G8) closed
  </what-built>
  <how-to-verify>
    1. Review the t1 output (`/tmp/40-19-final-run.txt`) — confirm 0 failures.
    2. Review the t2 triple-replay results — confirm 14/14 × 3 stable.
    3. Spot-check the actual pages with `pnpm dev`:
       - `/dossiers/countries` — RTL chevron flips correctly (open AR locale)
       - `/dossiers/working_groups` — ≥5 rows visible
       - `/dossiers/engagements` — 4 filter pills (meeting/call/travel/event), row click navigates
       - Any page — `<main>` landmark visible in DevTools Accessibility tree
    4. Confirm the verdict can flip to PASS.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with VERIFICATION.md verdict flip; otherwise list remaining concerns.</resume-signal>
</task>

<task id="t4" type="auto">
  <name>Task 4: Flip VERIFICATION.md verdict to PASS</name>
  <files>.planning/phases/40-list-pages/VERIFICATION.md</files>
  <read_first>
    - .planning/phases/40-list-pages/VERIFICATION.md (full)
  </read_first>
  <action>
    In `.planning/phases/40-list-pages/VERIFICATION.md`:

    1. Locate the frontmatter or top-level verdict block (likely `status: PASS-WITH-DEVIATION` or `verdict: PASS-WITH-DEVIATION`).

    2. Change to `status: PASS` (or `verdict: PASS`).

    3. Append a new section at the bottom:

       ```markdown
       ## Gap Closure — 2026-04-26+

       Eight gaps (G1–G8) opened by the 2026-04-26 live E2E run were closed via gap-closure plans 40-12 through 40-19:

       | Gap | Plan | Resolution |
       |-----|------|------------|
       | G1  | 40-14 | min-w-0 audit on ListPageShell + DossierTable + EngagementsList + PersonsGrid |
       | G2  | 40-15 | <main> landmark, <html lang> sync, chip contrast tokens |
       | G3  | 40-13 | scaleX(-1) primitive swap on row chevron |
       | G4  | 40-18 | Spec aligned to type taxonomy (meeting/call/travel/event) |
       | G5  | 40-16 | Engagement row as single button + loosened route regex |
       | G6  | 40-12 | Seed migration for ≥5 working_group dossiers |
       | G7  | 40-13 + 40-17 | data-loading marker + clock.install + transition suppression |
       | G8  | 40-18 | Spec reconciliation pass against per-plan SUMMARY deviations |

       **Final E2E run:** All 6 spec files green on chromium-darwin; 14/14 visual baselines stable across 3 replays; axe-core 0 violations on 7 × 2 locales.

       **Verdict flipped:** PASS-WITH-DEVIATION → PASS on 2026-04-26.
       ```

    4. Save the file.
  </action>
  <verify>
    <automated>grep -E "^(status|verdict): PASS$" .planning/phases/40-list-pages/VERIFICATION.md</automated>
  </verify>
  <done>VERIFICATION.md status is PASS; gap-closure section appended.</done>
</task>

<task id="t5" type="auto">
  <name>Task 5: Update 40-HUMAN-UAT.md to passed: 10</name>
  <files>.planning/phases/40-list-pages/40-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/40-list-pages/40-HUMAN-UAT.md (full)
  </read_first>
  <action>
    In `.planning/phases/40-list-pages/40-HUMAN-UAT.md`:

    1. Update frontmatter: `status: partial` → `status: complete`.
    2. For each of the 10 UAT items (sections 1–10), change `result: [pending]` or `result: PARTIAL` → `result: passed`.
    3. Update the Summary block:
       ```
       total: 10
       passed: 10
       issues: 0
       pending: 0
       skipped: 0
       blocked: 0
       partial: 0
       ```
    4. In the Gaps section, prefix each gap heading with `~~CLOSED~~`. Append a final note: "All gaps G1–G8 closed via plans 40-12 through 40-19. See VERIFICATION.md gap-closure section."
  </action>
  <verify>
    <automated>grep -E "passed: 10" .planning/phases/40-list-pages/40-HUMAN-UAT.md &amp;&amp; grep -E "status: complete" .planning/phases/40-list-pages/40-HUMAN-UAT.md</automated>
  </verify>
  <done>UAT marks 10/10 passed; status complete.</done>
</task>

<task id="t6" type="auto">
  <name>Task 6: Update STATE.md + ROADMAP.md Phase 40 entry</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/STATE.md (full)
    - .planning/ROADMAP.md (Phase 40 section)
  </read_first>
  <action>
    1. In `.planning/STATE.md`, find the Phase 40 entry. Update:
       - Status: `PASS-WITH-DEVIATION` → `PASS`
       - Date: append "Closed 2026-04-26 via gap-closure plans 40-12..40-19"

    2. In `.planning/ROADMAP.md`, find the Phase 40 entry. Update the plan checklist to mark 40-12 through 40-19 as `[x]` complete (assuming the executor will tick them as each plan finishes — this task only updates the roadmap if not auto-ticked).

    3. Commit message template (do NOT commit in this task — let the user run final commit):
       ```
       state(40): close Phase 40 PASS via gap-closure plans 40-12..40-19

       All 8 gaps (G1–G8) from the 2026-04-26 live E2E run resolved.
       6/6 E2E spec files green; 14/14 visual baselines stable; axe clean.
       ```
  </action>
  <verify>
    <automated>grep -E "Phase 40.*PASS\b" .planning/STATE.md</automated>
  </verify>
  <done>STATE.md + ROADMAP.md updated; phase 40 marked PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — documentation update) | Pure docs change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-19-01 | n/a | n/a | accept | Documentation only |
</threat_model>

<verification>
- 6/6 E2E spec files green
- 14/14 visual baselines stable × 3 replays
- VERIFICATION.md status: PASS
- UAT 10/10 passed
- STATE.md + ROADMAP.md reflect Phase 40 PASS
</verification>

<success_criteria>
- All 8 gaps closed
- Phase 40 verdict PASS
- Phase officially complete
</success_criteria>

<output>
After completion, create `.planning/phases/40-list-pages/40-19-SUMMARY.md` with:
- Final E2E run summary (specs × pass count)
- 3-replay visual stability table
- Per-gap closure attribution table (G1–G8 → plan)
- Note that ROADMAP.md / STATE.md were updated
</output>
