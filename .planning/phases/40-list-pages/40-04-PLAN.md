---
phase: 40-list-pages
plan: 04
type: execute
wave: 1
depends_on: [40-01, 40-02a, 40-02b, 40-02c]
files_modified:
  - frontend/src/routes/_protected/dossiers/organizations/index.tsx
  - frontend/src/routes/_protected/dossiers/organizations/__tests__/OrganizationsListPage.test.tsx
autonomous: true
requirements: [LIST-01]
must_haves:
  truths:
    - "/dossiers/organizations renders DossierTable with type='organization' rows"
    - 'Sensitivity chip + chevron with .icon-flip per LIST-01'
    - 'Mobile <768px collapses to card list'
    - "Loading state renders DossierTableSkeleton; empty state renders 'No organizations yet'"
    - 'Row click navigates to /dossiers/organizations/$id'
  artifacts:
    - path: 'frontend/src/routes/_protected/dossiers/organizations/index.tsx'
      provides: 'Replaced body — ListPageShell + DossierTable + useOrganizations'
      exports: ['Route']
  key_links:
    - from: 'OrganizationsListPage'
      to: 'useOrganizations'
      via: 'ES import'
      pattern: "useOrganizations.*from '@/hooks/useOrganizations'"
---

<objective>
Replace `frontend/src/routes/_protected/dossiers/organizations/index.tsx` body with ListPageShell + DossierTable + useOrganizations. Per LIST-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-list-pages/40-CONTEXT.md
@.planning/phases/40-list-pages/40-RESEARCH.md
@.planning/phases/40-list-pages/40-PATTERNS.md
@.planning/phases/40-list-pages/40-03-SUMMARY.md
@frontend/src/routes/_protected/dossiers/countries/index.tsx

<interfaces>
import { ListPageShell, DossierTable, DossierTableSkeleton, ToolbarSearch, type DossierTableRow } from '@/components/list-page'
import { useOrganizations } from '@/hooks/useOrganizations'
useOrganizations({ search?, page=1, limit=20 }) → { data: { data: Dossier[], pagination }, isLoading, isError }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace organizations/index.tsx body + render-assertion test</name>
  <files>frontend/src/routes/_protected/dossiers/organizations/index.tsx, frontend/src/routes/_protected/dossiers/organizations/__tests__/OrganizationsListPage.test.tsx</files>
  <read_first>
    - frontend/src/routes/_protected/dossiers/countries/index.tsx (analog from plan 03 — IDENTICAL shape with `country`→`organization` swap)
    - frontend/src/routes/_protected/dossiers/organizations/index.tsx (current body — to be replaced)
    - frontend/src/hooks/useOrganizations.ts (Wave 0 hook)
    - .planning/phases/40-list-pages/40-PATTERNS.md §"frontend/src/routes/_protected/dossiers/{...}/index.tsx"
  </read_first>
  <behavior>
    - Mirror plan 03's CountriesListPage structure exactly
    - Use `useOrganizations` instead of `useCountries`
    - Use namespace tuple `useTranslation(['organizations', 'list-pages'])`
    - Map dossier rows to DossierTableRow with `type: 'organization' as const`
    - Click navigation handled inside DossierTable: `/dossiers/organizations/$id`
    - DossierGlyph receives `type='organization'` (no flag_iso for organizations — `flag_iso` undefined)
  </behavior>
  <action>
**Step A — Create `frontend/src/routes/_protected/dossiers/organizations/index.tsx` (full rewrite):**

Use the Write tool. Identical to `countries/index.tsx` (plan 03) with these substitutions:

- File path → `organizations/`
- Route path → `/_protected/dossiers/organizations/`
- `useTranslation(['countries', 'list-pages'])` → `useTranslation(['organizations', 'list-pages'])`
- `useCountries` → `useOrganizations`
- `t('countries:title')` → `t('organizations:title')` (and `subtitle`, `empty.title`, `empty.description`)
- `type: 'country' as const` → `type: 'organization' as const`
- `iso_code` mapping: organizations have no ISO codes → set `flag_iso: undefined` (DossierGlyph falls back to type-based glyph per Phase 37)
- Function name: `CountriesListPage` → `OrganizationsListPage`

NO `any`. Strict boolean expressions. No physical RTL classes.

**Step B — Render-assertion test** at `__tests__/OrganizationsListPage.test.tsx`:

Mirror plan 03's countries test:

- Mock `@/hooks/useOrganizations` returning `{ data: { data: [{id:'b',name_en:'WHO',name_ar:'منظمة الصحة العالمية',engagement_count:17,updated_at:'2026-04-10T00:00:00Z',sensitivity_level:2}], pagination:{...} }, isLoading: false }`
- Mock `@/hooks/useDirection` returning `{ isRTL: false }`
- Render via QueryClientProvider + MemoryRouter
- Assert: `Organizations` title visible, `WHO` row visible, `17` count visible, sensitivity chip class `chip-default` (level=2)
- Re-render with empty data → assert `No organizations yet` empty hint
  </action>
  <verify>
  <automated>cd frontend && pnpm vitest run src/routes/\_protected/dossiers/organizations/**tests**/OrganizationsListPage.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria> - File exists with `createFileRoute('/_protected/dossiers/organizations/')` - `grep -n "useOrganizations\|DossierTable\|ListPageShell" frontend/src/routes/_protected/dossiers/organizations/index.tsx` returns at least 3 matches - `grep -rn "ml-\|mr-\|pl-\|pr-\|text-left\|text-right\|rounded-l-\|rounded-r-" frontend/src/routes/_protected/dossiers/organizations/index.tsx` returns 0 matches - `grep -rn ": any\|as any\|<any>" frontend/src/routes/_protected/dossiers/organizations/index.tsx` returns 0 matches - Vitest test passes
  </acceptance_criteria>
  <done>Organizations page renders via ListPageShell + DossierTable + useOrganizations; render + empty-state assertions green.</done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                     |
| ----------------------------------------- | --------------------------------------------------------------- |
| Browser → Supabase (via useOrganizations) | RLS-enforced read of `dossiers` rows where type='organization'. |

## STRIDE Threat Register

| Threat ID  | Category               | Component                 | Disposition | Mitigation Plan                                                                                      |
| ---------- | ---------------------- | ------------------------- | ----------- | ---------------------------------------------------------------------------------------------------- |
| T-40-04-01 | Tampering              | URL search param `search` | mitigate    | validateSearch coerces; Wave 0 useOrganizations applies length cap + Supabase parameterized `.or()`. |
| T-40-04-02 | Information disclosure | sensitivity_level chip    | mitigate    | RLS-protected; chip display-only.                                                                    |

</threat_model>

<verification>
- Vitest assertions green
- Zero physical RTL classes
- No `any`
</verification>

<success_criteria>

- Organizations page parity with Countries shape
- LIST-01 covered for organizations
  </success_criteria>

<output>
After completion, create `.planning/phases/40-list-pages/40-04-SUMMARY.md`.
</output>
