---
phase: 40-list-pages
plan: 11
type: execute
wave: 2
depends_on: [40-10]
files_modified:
  - .planning/phases/40-list-pages/VERIFICATION.md
autonomous: false
requirements: [LIST-01, LIST-02, LIST-03, LIST-04]
must_haves:
  truths:
    - 'Human has compared all 7 pages × LTR (and AR for visual sanity) against handoff reference PNGs'
    - 'Persons cards judged visually consistent with dashboard.png card aesthetic (D-04)'
    - 'Forums/Topics/Working-Groups rows judged visually identical (D-05)'
    - '14 visual baselines committed to repo as approved'
    - 'VERIFICATION.md records 4/4 LIST requirements PASS or PASS-WITH-DEVIATION with explicit deviation list'
  artifacts:
    - path: '.planning/phases/40-list-pages/VERIFICATION.md'
      provides: 'Final phase verification report — LIST-01..04 verdict + deviations + baseline approval'
      contains: 'LIST-01'
  key_links:
    - from: 'Wave 2 plan 10 visual baselines'
      to: 'VERIFICATION.md sign-off'
      via: 'Human PNG-parity comparison'
      pattern: 'frontend/tests/e2e/list-pages-visual.spec.ts-snapshots'
---

<objective>
Single human checkpoint that closes Phase 40: visually compare all 7 list pages (LTR + AR sanity check) against handoff reference PNGs, approve the 14 captured visual baselines, and write the phase VERIFICATION.md verdict.

Per VALIDATION.md §"Manual-Only Verifications": handoff PNG parity is qualitative judgment beyond pixel diff; first-run baseline must be human-approved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-list-pages/40-CONTEXT.md
@.planning/phases/40-list-pages/40-VALIDATION.md
@.planning/phases/40-list-pages/40-10-SUMMARY.md
@.planning/phases/38-dashboard-verbatim/VERIFICATION.md
@.planning/phases/39-kanban-calendar/VERIFICATION.md
@.planning/REQUIREMENTS.md

<interfaces>
Reference PNGs (handoff source of truth):
- /tmp/inteldossier-handoff/inteldossier/project/reference/countries.png    (LIST-01)
- /tmp/inteldossier-handoff/inteldossier/project/reference/organizations.png (LIST-01)
- /tmp/inteldossier-handoff/inteldossier/project/reference/forums.png       (LIST-03 — Topics + WG parity)
- /tmp/inteldossier-handoff/inteldossier/project/reference/engagements.png  (LIST-04)
- /tmp/inteldossier-handoff/inteldossier/project/reference/dashboard.png    (LIST-02 persons card aesthetic only)

Captured baselines after plan 10 runs with --update-snapshots:
frontend/tests/e2e/list-pages-visual.spec.ts-snapshots/{countries,organizations,persons,forums,topics,working-groups,engagements}-{en,ar}-chromium-en.png
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human PNG-parity approval + baseline capture</name>
  <what-built>
After Wave 0 + Wave 1 + Wave 2 plan 10 completed:
- 7 list pages render at /dossiers/countries, /dossiers/organizations, /persons, /dossiers/forums, /dossiers/topics, /dossiers/working-groups, /engagements
- Wave 2 plan 10 captured (or is ready to capture) 14 visual baselines via `pnpm exec playwright test tests/e2e/list-pages-visual.spec.ts --update-snapshots`
- ESLint logical-properties enforcement active
- All vitest + Playwright functional specs green
  </what-built>
  <files>.planning/phases/40-list-pages/VERIFICATION.md, frontend/tests/e2e/list-pages-visual.spec.ts-snapshots/</files>
  <action>
Run through the visual parity protocol in the `<how-to-verify>` block below. After approval, capture / commit the 14 visual baselines and write `.planning/phases/40-list-pages/VERIFICATION.md` per the template at the bottom of this task. The action is the human protocol itself — Claude executes step 1 (start dev server), step 2 (capture baselines), and step 10 (write VERIFICATION.md draft) on user instruction during the resume.
  </action>
  <verify>
    <automated>test -f .planning/phases/40-list-pages/VERIFICATION.md && grep -q "LIST-01\|LIST-02\|LIST-03\|LIST-04" .planning/phases/40-list-pages/VERIFICATION.md && grep -q "Verdict:" .planning/phases/40-list-pages/VERIFICATION.md && ls frontend/tests/e2e/list-pages-visual.spec.ts-snapshots/ 2>/dev/null | wc -l | awk '{exit ($1>=14?0:1)}' && echo OK</automated>
  </verify>
  <done>VERIFICATION.md written with verdict and 4 LIST requirements graded; 14 visual baselines committed under `frontend/tests/e2e/list-pages-visual.spec.ts-snapshots/`; user has typed `approved` (or `deviation: ...`) confirming PNG-parity sign-off.</done>
  <how-to-verify>
1. **Start dev server:** `cd frontend && pnpm dev` — wait for `http://localhost:5173`.

2. **Capture baselines** (if not already captured by plan 10):
   `cd frontend && pnpm exec playwright test tests/e2e/list-pages-visual.spec.ts --update-snapshots --project=chromium-en`

3. **Visual parity comparison — LIST-01 (Countries + Organizations):**
   - Open browser tab A: `http://localhost:5173/dossiers/countries` (LTR)
   - Open image viewer with `/tmp/inteldossier-handoff/inteldossier/project/reference/countries.png`
   - Compare side-by-side at 1280px width:
     - DossierGlyph rendering matches handoff
     - EN/AR name stacking matches
     - Engagement count column position matches
     - Last-touch date format matches
     - Sensitivity chip color/shape matches (chip-warn for level=3, chip-danger for level=4)
     - Chevron right edge alignment matches
   - Repeat for `/dossiers/organizations` against `organizations.png`

4. **Visual parity comparison — LIST-02 (Persons):**
   - Open `http://localhost:5173/persons` (LTR) at 1280px
   - Open `/tmp/inteldossier-handoff/inteldossier/project/reference/dashboard.png` for card aesthetic reference
   - Compare:
     - Card border-radius matches dashboard cards
     - Card padding matches
     - Card shadow (subtle, border-only acceptable per RESEARCH §"Persons card tokens")
     - 44px circular avatar with bg-accent-soft / text-accent-ink colors
     - VIP chip uses chip-accent class (accent color)
     - Role + organization line spacing

5. **Visual parity comparison — LIST-03 (Forums / Topics / Working Groups):**
   - Open `http://localhost:5173/dossiers/forums` (LTR)
   - Open `/tmp/inteldossier-handoff/inteldossier/project/reference/forums.png`
   - Compare row anatomy: glyph + name + meta + status chip + chevron — same row height (~64px), same padding, same border-bottom color
   - Open `/dossiers/topics` and `/dossiers/working-groups` and confirm visual parity with `forums.png` (same shape, different data — D-05)

6. **Visual parity comparison — LIST-04 (Engagements):**
   - Open `http://localhost:5173/engagements` (LTR) at 1280px
   - Open `/tmp/inteldossier-handoff/inteldossier/project/reference/engagements.png`
   - Compare:
     - Toolbar: search input width + 4 filter pills inline
     - Filter pill active state: dark `--ink` background + light text (NOT accent — per RESEARCH §6 LOCK)
     - Week-list: "Week of {{date}}" header rendering
     - Each row: title + counterpart + glyph + status chip
     - Load-more row at bottom (if pagination active)
     - Click load-more → GlobeSpinner appears + "Loading more engagements…" text visible

7. **AR sanity check (visual only):**
   - Switch language to Arabic via `?lng=ar` on each page (or i18next switcher)
   - Verify chevrons mirror (point left visually) — `.icon-flip` rule active
   - Verify Tajawal font renders Arabic text (not fallback)
   - Verify chips/meta text flows right-to-left (filter pills appear right-to-left in Engagements toolbar)
   - Verify no text clipping at edges; logical properties (ms/me/ps/pe) hold

8. **Touch target sanity check at 320px:**
   - Resize browser to 320px width
   - On `/dossiers/countries`: rows should collapse to card stack
   - On `/persons`: grid should be 1-column
   - On `/engagements`: pills should wrap; all interactive elements ≥44×44

9. **Approve baselines:** if all 7 pages × LTR pass parity (and AR sanity-checks pass) → run `git add frontend/tests/e2e/list-pages-visual.spec.ts-snapshots/` to commit the 14 baselines.

10. **Write VERIFICATION.md:**

Create `.planning/phases/40-list-pages/VERIFICATION.md` with this structure (mirror Phase 38/39 VERIFICATION format):

```markdown
# Phase 40 list-pages — VERIFICATION

**Date:** {today}
**Verdict:** PASS | PASS-WITH-DEVIATION | FAIL

## Requirements

| ID      | Description                                                                                               | Status | Evidence                                                                                                                                    |
| ------- | --------------------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------- |
| LIST-01 | Countries + Organizations tables (DossierGlyph + EN/AR + count + last-touch + sensitivity + chevron flip) | PASS   | Plan 03 vitest green; visual baseline countries-en + organizations-en human-approved; chevron .icon-flip verified in list-pages-rtl.spec.ts |
| LIST-02 | Persons grid (44px avatar + name + glyph + VIP chip + role + org)                                         | PASS   | Plan 05 vitest green; visual baseline persons-en human-approved; VIP chip conditional verified                                              |
| LIST-03 | Forums + Topics + Working-Groups rows via shared GenericListPage                                          | PASS   | Plans 06/07/08 vitest green; visual baselines forums-en + topics-en + working-groups-en human-approved                                      |
| LIST-04 | Engagements (search + 4 pills + week-list + load-more with GlobeSpinner + bilingual text)                 | PASS   | Plan 09 vitest green; engagements load-more E2E green; visual baseline engagements-en human-approved                                        |

## Test totals

- Vitest: {N}/{N} green ({M} files)
- Playwright functional: {N}/{N} green
- Playwright a11y: 14/14 zero violations (or note exceptions)
- Playwright visual: 14/14 baselines human-approved
- size-limit: 815 KB Total JS budget green

## Deviations

(List any deviations encountered, e.g., useTopics return shape required runtime-extraction fallback; engagement count column missing → defaulted to 0; etc.)

## Phase 41 unblocked: yes
```

  </how-to-verify>
  <resume-signal>
Type:
- `approved` — all 7 pages match handoff parity, baselines committed, VERIFICATION.md written
- `deviation: <description>` — describe any deviations to record before approval (still proceed to PASS-WITH-DEVIATION verdict)
- `reject: <description>` — describe blocking issues; phase returns to gap-closure planning
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                          |
| ------------------------------- | ---------------------------------------------------- |
| Human reviewer ↔ rendered pages | Manual visual judgment; no automated trust transfer. |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                      | Disposition | Mitigation Plan                                                                                                  |
| ---------- | ---------------------- | ---------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------- |
| T-40-11-01 | Information disclosure | Reference PNGs in `/tmp/inteldossier-handoff/` | accept      | Handoff PNGs are design source-of-truth; no production user data.                                                |
| T-40-11-02 | Tampering              | Visual baselines committed to repo             | mitigate    | Baselines committed only after explicit human approval; CI gates re-run against committed baselines on every PR. |

</threat_model>

<verification>
- All 7 pages × LTR judged visually consistent with handoff reference PNGs
- AR sanity (chevron flip, Tajawal, RTL flow) confirmed
- 14 baselines committed
- VERIFICATION.md written with explicit verdict + deviation list
</verification>

<success_criteria>

- LIST-01..04 each marked PASS or PASS-WITH-DEVIATION in VERIFICATION.md
- Phase 40 closed; Phase 41 (dossier-drawer) unblocked
- STATE.md ready for /gsd-close-phase update
  </success_criteria>

<output>
After completion, create `.planning/phases/40-list-pages/40-11-SUMMARY.md` summarizing the human checkpoint outcome and link to VERIFICATION.md.
</output>
