---
phase: 79-aceternity-removal
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components.json
  - frontend/.aceternity/
  - frontend/README.md
  - frontend/src/components/ui/timeline.tsx
  - frontend/src/components/timeline/UnifiedVerticalTimeline.tsx
  - frontend/src/components/timeline/EnhancedVerticalTimelineCard.tsx
  - frontend/src/components/dossier/ExpandableDossierCard.tsx
  - frontend/src/routes/_protected/dossiers/index.tsx
autonomous: true
requirements: [ACET-02]
must_haves:
  truths:
    - "Implements the locked user decision (CONTEXT.md 'Aceternity scope', 2026-07-03) registry + fully-gone item: the @aceternity-pro registry entry and all repo residue are purged so the 79-04 grep proof is unconditional"
    - 'frontend/components.json contains no registries key and no aceternity string, and remains valid JSON with all other keys (including rtl: true) intact'
    - 'The git-tracked frontend/.aceternity/ docs directory is removed'
    - "frontend/README.md no longer lists Aceternity (lines ~52 and ~291); the 4 'Aceternity-inspired' comments in live files are reworded; the dead frontend/src/components/ui/timeline.tsx (0 importers) is deleted"
    - 'The inverted no-restricted-imports ban in root eslint.config.mjs is NOT weakened or modified in any way'
  artifacts:
    - path: 'frontend/components.json'
      provides: 'shadcn-CLI config without the registries block'
  key_links:
    - from: 'root eslint.config.mjs'
      to: 'no-restricted-imports Aceternity/Kibo ban (lines ~122-162)'
      via: 'untouched — anti-regression control stays green'
      pattern: 'no-restricted-imports'
---

<objective>
Remove the `@aceternity-pro` registry entry from `frontend/components.json` (ACET-02's letter) and purge the repo-wide Aceternity residue RESEARCH found (docs dir, README claims, 4 live-file comments, one dead component file with Aceternity demo copy) — so the phase's "fully gone" grep proof in 79-04 holds unconditionally, with zero exclusions.

Purpose: ACET-02 plus the fully-gone gray zone. Without the residue purge, the final proof needs grep exclusions and the phase title is dishonest.
Output: `components.json` without `registries`; `.aceternity/` gone; README + 4 comments cleaned; dead `ui/timeline.tsx` deleted; ESLint ban untouched and green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/79-aceternity-removal/79-RESEARCH.md

Key sections of 79-RESEARCH.md for this plan: "Registry Removal + Fully-Gone Proof" (the exact `components.json` edit, the ESLint ban location, the residue table with all five locations and suggested actions, and the leave-alone list: `.archive/**`, `frontend/.archive/**`, generated graph caches).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the @aceternity-pro registries block from components.json</name>
  <files>frontend/components.json</files>
  <read_first>
    - frontend/components.json — the full file (the `registries` object at lines ~20–22 contains EXACTLY ONE entry: `"@aceternity-pro": "https://pro.aceternity.com/registry/{name}.json"`; everything else, including `"rtl": true`, must survive)
    - .planning/phases/79-aceternity-removal/79-RESEARCH.md — § "Registry Removal + Fully-Gone Proof" (the exact edit)
  </read_first>
  <action>
    Edit `frontend/components.json`: delete the entire `registries` object (the key and its single `@aceternity-pro` entry), fixing the trailing/leading comma so the file stays valid JSON. Do NOT touch any other key (`rtl`, `style`, `tailwind`, `aliases`, etc.). This is the literal ACET-02 requirement per the locked user decision (CONTEXT.md success criterion 3).
  </action>
  <verify>
    <automated>node -e "const c=JSON.parse(require('fs').readFileSync('frontend/components.json','utf8')); if('registries' in c) throw new Error('registries key still present'); if(c.rtl!==true) throw new Error('rtl key lost'); console.log('components.json OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -in "aceternity\|registries" frontend/components.json` produces no output
    - `node -e "JSON.parse(require('fs').readFileSync('frontend/components.json','utf8'))"` exits 0 (valid JSON)
    - `"rtl": true` and all non-registries keys still present (verify command above exits 0)
  </acceptance_criteria>
  <done>`components.json` has no `registries` key, parses as valid JSON, all other config intact.</done>
</task>

<task type="auto">
  <name>Task 2: Purge doc/comment residue — .aceternity/ dir, README, 4 live-file comments, dead ui/timeline.tsx</name>
  <files>frontend/.aceternity/, frontend/README.md, frontend/src/components/ui/timeline.tsx, frontend/src/components/timeline/UnifiedVerticalTimeline.tsx, frontend/src/components/timeline/EnhancedVerticalTimelineCard.tsx, frontend/src/components/dossier/ExpandableDossierCard.tsx, frontend/src/routes/_protected/dossiers/index.tsx</files>
  <read_first>
    - .planning/phases/79-aceternity-removal/79-RESEARCH.md — the residue table in § "Registry Removal + Fully-Gone Proof" (locations, what each is, suggested action per row)
    - frontend/README.md — lines ~52 (tree comment "shadcn/ui + Aceternity UI components") and ~291 ("**Aceternity UI** (primary)") — find by grep, not line number
    - frontend/src/components/ui/timeline.tsx — the dead file (confirm the Aceternity demo copy at ~line 35 and that it exports nothing anyone imports)
    - The 4 comment sites (grep "Aceternity" in each): frontend/src/components/timeline/UnifiedVerticalTimeline.tsx (~line 9), frontend/src/components/timeline/EnhancedVerticalTimelineCard.tsx (~line 5), frontend/src/components/dossier/ExpandableDossierCard.tsx (~line 3), frontend/src/routes/_protected/dossiers/index.tsx (~line 6)
  </read_first>
  <action>
    Purge per the RESEARCH residue table (locked scope includes all four items):

    1. `git rm -r frontend/.aceternity/` — 10 git-tracked legacy migration docs; zero code risk, directly contradicts "fully gone".
    2. Edit `frontend/README.md`: reword the tree comment at ~line 52 to reflect reality (e.g. "HeroUI v3 + token-bound primitives") and REPLACE the ~line 291 "**Aceternity UI** (primary): https://ui.aceternity.com" component-library entry — it actively contradicts the CLAUDE.md ban; describe the real cascade (HeroUI v3 primary → Radix → build-yourself, per frontend/CLAUDE.md). Touch ONLY the Aceternity-referencing lines (surgical change).
    3. Reword the 4 "Aceternity-inspired" / "Aceternity UI inspired" doc comments in the live files listed above to describe the behavior instead (e.g. "Expandable-card interaction pattern"). One-line comment edits ONLY — zero code changes in these 4 files.
    4. **Importer gate, then delete** `frontend/src/components/ui/timeline.tsx`: first run `grep -rln "components/ui/timeline\b" --include="*.tsx" --include="*.ts" frontend/src tests | grep -v "ui/timeline.tsx"` — expected 0 (re-verified 2026-07-03). If 0, `git rm` it (dead file containing Aceternity demo marketing copy). Any match = STOP and report instead of deleting.
    5. **Leave alone** (explicitly out of scope, per RESEARCH): `.archive/**`, `frontend/.archive/**` (archived history), `frontend/src/.understand-anything/` and `graphify-out/` (git-ignored/generated caches), and the root `eslint.config.mjs` ban block (lines ~122–162 — do NOT edit; it is the anti-regression control).

  </action>
  <verify>
    <automated>pnpm --dir frontend exec tsc --noEmit && ! grep -rqin "aceternity" frontend/README.md frontend/src/components/timeline/UnifiedVerticalTimeline.tsx frontend/src/components/timeline/EnhancedVerticalTimelineCard.tsx frontend/src/components/dossier/ExpandableDossierCard.tsx frontend/src/routes/_protected/dossiers/index.tsx && test ! -e frontend/.aceternity && test ! -e frontend/src/components/ui/timeline.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e frontend/.aceternity` exits 0 (directory removed from working tree; `git ls-files frontend/.aceternity` produces no output)
    - `grep -in "aceternity" frontend/README.md` produces no output
    - `grep -in "aceternity" frontend/src/components/timeline/UnifiedVerticalTimeline.tsx frontend/src/components/timeline/EnhancedVerticalTimelineCard.tsx frontend/src/components/dossier/ExpandableDossierCard.tsx frontend/src/routes/_protected/dossiers/index.tsx` produces no output
    - `test ! -e frontend/src/components/ui/timeline.tsx` exits 0, and the pre-deletion importer grep returned 0
    - `git diff --stat` shows NO change to `eslint.config.mjs`
    - `pnpm --dir frontend exec tsc --noEmit` exits 0 (the 4 comment edits and the dead-file deletion broke nothing)
  </acceptance_criteria>
  <done>All residue rows from the RESEARCH table actioned; ESLint ban and archive/generated paths untouched; typecheck green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                                                                                                                    |
| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Build/tooling config | `components.json` is consumed by the shadcn CLI at scaffold time only — not at runtime; removing the registry URL removes a scaffold-time supply-chain ingress |

## STRIDE Threat Register

Per 79-RESEARCH.md § Security Domain: **no high-severity NEW threats are introduced by this plan.** Removing the `@aceternity-pro` registry URL strictly reduces supply-chain surface (no future `shadcn add` can pull from that registry).

| Threat ID | Category  | Component                                                | Disposition | Mitigation Plan                                                                                                                                     |
| --------- | --------- | -------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-79-S3   | Elevation | Aceternity re-introduction (registry scaffold or import) | mitigate    | Registry entry deleted (this plan) + inverted `no-restricted-imports` ban left byte-untouched + 79-04 grep battery makes re-introduction CI-visible |
| T-79-SC   | Tampering | Package installs                                         | n/a         | Zero packages installed this phase (RESEARCH § Package Legitimacy Audit)                                                                            |

</threat_model>

<verification>
- `components.json`: no `registries`/`aceternity`, valid JSON, `rtl: true` retained
- `.aceternity/` and `ui/timeline.tsx` gone; README + 4 comments clean
- `eslint.config.mjs` unmodified; `tsc --noEmit` exits 0
</verification>

<success_criteria>

- ACET-02's letter satisfied (registry entry removed) and its spirit prepared (residue purged so 79-04's case-insensitive repo grep can demand literally zero matches)
- The anti-regression control (ESLint ban) untouched
  </success_criteria>

<output>
Create `.planning/phases/79-aceternity-removal/79-03-SUMMARY.md` when done.
</output>
