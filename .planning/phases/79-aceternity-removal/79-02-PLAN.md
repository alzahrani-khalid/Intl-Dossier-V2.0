---
phase: 79-aceternity-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/forms/FormInputAceternity.tsx
  - frontend/src/components/forms/FormTextareaAceternity.tsx
  - frontend/src/components/forms/FormSelectAceternity.tsx
  - frontend/src/components/forms/FormCheckboxAceternity.tsx
  - frontend/src/components/forms/FormRadioAceternity.tsx
  - frontend/src/components/forms/FormFieldWithValidation.tsx
  - frontend/src/components/forms/SmartInput.tsx
  - frontend/src/components/forms/index.ts
  - frontend/src/hooks/useFieldValidation.ts
autonomous: true
requirements: [ACET-01]
must_haves:
  truths:
    - "Implements the locked user decision (CONTEXT.md 'Aceternity scope', 2026-07-03): the 7 dead components are DELETED, not rebuilt — this is the delete-7 half of delete-7/rebuild-1"
    - 'The 7 dead Aceternity component files no longer exist on disk and no barrel export references them'
    - 'The newly-orphaned frontend/src/hooks/useFieldValidation.ts is deleted; lib/validation-rules.ts and components/forms/ValidationIndicator.tsx are RETAINED (they have other live consumers / are pre-existing and out of scope)'
    - 'The shared smart-input i18n namespace (en + ar JSON files and their registration in src/i18n/index.ts) is byte-untouched — the LIVE SearchableSelect consumes its select.* keys'
    - 'tsc --noEmit, whole-repo lint --max-warnings 0, and the full vitest suite all pass post-deletion (proof no hidden importer existed)'
  artifacts:
    - path: 'frontend/src/components/forms/index.ts'
      provides: 'Barrel with the 7 dead export blocks pruned; SearchableSelect/SelectOption/OptionGroup exports retained'
  key_links:
    - from: 'frontend/src/components/forms/SearchableSelect.tsx'
      to: 'frontend/src/i18n/en/smart-input.json'
      via: "t('smart-input:select.*') keys — namespace must survive SmartInput deletion"
      pattern: 'smart-input:select'
    - from: 'frontend/src/i18n/index.ts'
      to: 'smart-input namespace registration'
      via: 'enSmartInput/arSmartInput static imports (untouched)'
      pattern: 'SmartInput'
---

<objective>
Delete the 7 dead Aceternity form components (0 external call sites each — proven in the Phase 75 audit and re-verified fresh 2026-07-03), prune their barrel exports, and delete the one hook these deletions newly orphan — without touching the live SearchableSelect/UserPicker path or the shared `smart-input` i18n namespace.

Purpose: The delete-7 half of ACET-01 under the locked user decision. Dead code the phase exists to eliminate.
Output: 7 component files + 1 orphaned hook removed; `forms/index.ts` pruned; typecheck/lint/full-suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/79-aceternity-removal/79-RESEARCH.md

Key sections of 79-RESEARCH.md for this plan: "Deletion Safety for the 7" (per-component 0-call-site table + the liveness grep loop), "Orphan-dependency map" (delete `useFieldValidation`, KEEP `validation-rules.ts` and `ValidationIndicator.tsx`), Pitfall 7 (the shared `smart-input` namespace hazard).
Deletion warrant: @.planning/phases/75-ui-component-migration-audit/75-AUDIT-aceternity-contracts.md § "Liveness evidence".
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm liveness = 0, then delete the 7 dead components and prune the barrel</name>
  <files>frontend/src/components/forms/FormInputAceternity.tsx, frontend/src/components/forms/FormTextareaAceternity.tsx, frontend/src/components/forms/FormSelectAceternity.tsx, frontend/src/components/forms/FormCheckboxAceternity.tsx, frontend/src/components/forms/FormRadioAceternity.tsx, frontend/src/components/forms/FormFieldWithValidation.tsx, frontend/src/components/forms/SmartInput.tsx, frontend/src/components/forms/index.ts</files>
  <read_first>
    - frontend/src/components/forms/index.ts — the barrel: identify every export block referencing the 7 names below before editing (RESEARCH places them at lines ~17–22, 24–28, 56–62, but prune by NAME, not line number)
    - .planning/phases/79-aceternity-removal/79-RESEARCH.md — "Deletion Safety for the 7" table + the evidence grep loop (run it yourself before deleting)
    - .planning/phases/75-ui-component-migration-audit/75-AUDIT-aceternity-contracts.md — § "Liveness evidence" (the warrant this task executes)
  </read_first>
  <action>
    Per the locked user decision (CONTEXT.md "Aceternity scope" — delete 7 dead, rebuild 1 live):

    1. **Re-run the liveness proof BEFORE deleting** (deletion gate): for each of `FormInputAceternity`, `FormTextareaAceternity`, `FormSelectAceternity`, `FormCheckboxAceternity`, `FormRadioAceternity`, `FormFieldWithValidation`, `SmartInput`, run the RESEARCH evidence loop — `grep -rl "\b<Name>\b" --include="*.tsx" --include="*.ts" frontend/src tests | grep -v "frontend/src/components/forms/"`. Expected: 0 files each, EXCEPT `SmartInput` which matches `frontend/src/i18n/index.ts` only via the `enSmartInput`/`arSmartInput` import identifiers (the namespace registration — a benign symbol collision, NOT a component import). Any other match = STOP and investigate before deleting.
    2. `git rm` the 7 files listed in `<files>` (all in `frontend/src/components/forms/`).
    3. Edit `frontend/src/components/forms/index.ts`: remove every export line/block referencing the 7 deleted modules AND the `// Aceternity-styled components` section comment (RESEARCH scopes the prune to `index.ts` lines 17–22 — line 17 IS that comment; it survives the name-greps below, and leaving it would force 79-04 T2's strict `grep -rni "aceternity"` proof to fix-forward this plan's leftover). KEEP the `SearchableSelect` export block intact (component + `SelectOption` + `OptionGroup` types — the prop surface is NOT being trimmed, per 79-RESEARCH Open Question 2 recommendation "smallest verifiable diff"). Keep every other non-Aceternity export untouched (surgical change — do not reorder or reformat unrelated lines).
    4. **i18n hazard (RESEARCH Pitfall 7) — explicitly forbidden:** do NOT delete or modify `frontend/src/i18n/en/smart-input.json`, `frontend/src/i18n/ar/smart-input.json`, or their registration in `frontend/src/i18n/index.ts`. The namespace is SHARED — the live SearchableSelect uses its `select.*` keys; removing it silently renders raw keys in both languages. Do not prune the SmartInput-specific keys either (scope creep).
    5. Do NOT touch `frontend/src/components/forms/ValidationIndicator.tsx` (pre-existing dead-ish code, NOT in the user's delete list — mention as pre-existing dead code in the SUMMARY, per Karpathy surgical-change rule) and do NOT touch `frontend/src/lib/validation-rules.ts` (has live consumers: `types/actionable-error.types.ts`, `hooks/useActionableErrors.ts`, `ValidationIndicator.tsx`).

  </action>
  <verify>
    <automated>pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `ls frontend/src/components/forms/ | grep -iE "Aceternity|^SmartInput|^FormFieldWithValidation"` produces no output (all 7 files gone)
    - `grep -E "FormInputAceternity|FormTextareaAceternity|FormSelectAceternity|FormCheckboxAceternity|FormRadioAceternity|FormFieldWithValidation|SmartInput" frontend/src/components/forms/index.ts` produces no output; `grep -q "SearchableSelect" frontend/src/components/forms/index.ts` exits 0 (live export retained)
    - `grep -ni "aceternity" frontend/src/components/forms/index.ts` returns 0 lines (the `// Aceternity-styled components` comment is gone along with the export blocks)
    - `git diff --stat` shows ZERO changes to `frontend/src/i18n/en/smart-input.json`, `frontend/src/i18n/ar/smart-input.json`, and `frontend/src/i18n/index.ts`
    - `frontend/src/components/forms/ValidationIndicator.tsx` and `frontend/src/lib/validation-rules.ts` still exist unchanged
    - `pnpm --dir frontend exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>7 dead components deleted, barrel pruned by name, i18n namespace and out-of-scope files untouched, typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the newly-orphaned useFieldValidation hook and prove deletion safety repo-wide</name>
  <files>frontend/src/hooks/useFieldValidation.ts</files>
  <read_first>
    - frontend/src/hooks/useFieldValidation.ts — the file being deleted
    - .planning/phases/79-aceternity-removal/79-RESEARCH.md — "Orphan-dependency map" (sole consumer was FormFieldWithValidation, deleted in Task 1; the hook has no tests)
  </read_first>
  <action>
    1. **Orphan gate before deleting:** run `grep -rln "useFieldValidation" --include="*.tsx" --include="*.ts" frontend/src tests | grep -v "hooks/useFieldValidation"` — expected 0 matches now that Task 1 deleted `FormFieldWithValidation.tsx` (its sole consumer, verified in RESEARCH). Any match = STOP and investigate.
    2. `git rm frontend/src/hooks/useFieldValidation.ts` (orphan created by this phase's own change — Karpathy rule: remove orphans YOUR change creates).
    3. Run the full deletion-safety battery (per-wave sampling from 79-VALIDATION.md): `pnpm --dir frontend exec tsc --noEmit`, `pnpm --dir frontend lint --max-warnings 0` (whole repo — CI-equivalent; also proves the inverted `no-restricted-imports` ban in root `eslint.config.mjs` stays green, untouched), and `pnpm --dir frontend vitest run` (full suite — proves no hidden runtime importer; includes `CreateTaskCtas.test.tsx` which guards the untouched UserPicker module path).
    4. **Parallel-wave note (NOT a dependency):** the full-suite run may pick up `src/components/forms/__tests__/SearchableSelect.a11y.test.tsx` while parallel Wave-1 plan 79-01 is still authoring/triaging it in the shared tree. This task's gate verifies that THIS plan's deletions broke nothing in the existing suite — 79-01 owns the green-in-isolation guarantee for that new a11y file. If the ONLY failures are confined to that in-flight file, they are not this plan's regression: re-run the full suite after 79-01 lands (or exclude that single file from this run) instead of touching 79-01's file from here.
  </action>
  <verify>
    <automated>pnpm --dir frontend exec tsc --noEmit && pnpm --dir frontend lint --max-warnings 0 && pnpm --dir frontend vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/hooks/useFieldValidation.ts` does not exist (`test ! -e frontend/src/hooks/useFieldValidation.ts` exits 0)
    - `grep -rln "useFieldValidation" --include="*.tsx" --include="*.ts" frontend/src tests` produces no output
    - `pnpm --dir frontend exec tsc --noEmit` exits 0
    - `pnpm --dir frontend lint --max-warnings 0` exits 0 (root `eslint.config.mjs` `no-restricted-imports` block NOT modified — `git diff --stat` shows no change to `eslint.config.mjs`)
    - `pnpm --dir frontend vitest run` exits 0 — tolerating only in-flight failures confined to 79-01's `SearchableSelect.a11y.test.tsx` (that file's green-in-isolation is 79-01's acceptance, not this plan's; re-run after 79-01 lands if hit)
  </acceptance_criteria>
  <done>Orphaned hook removed; typecheck + whole-repo lint + full unit suite green — no external importer existed.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                                                                        |
| -------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| None new | Pure dead-code deletion; no runtime, API, or DB surface touched (RESEARCH verified 0 aceternity refs in supabase/ and backend/src) |

## STRIDE Threat Register

Per 79-RESEARCH.md § Security Domain: this phase touches ASVS V5 only. **No high-severity NEW threats are introduced by this plan** — deleting unreferenced components strictly shrinks the attack surface.

| Threat ID | Category  | Component                                                 | Disposition                         | Mitigation Plan                                                                                                                                           |
| --------- | --------- | --------------------------------------------------------- | ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-79-S3   | Elevation | Dormant Aceternity import path re-introduced later        | mitigate                            | Inverted `no-restricted-imports` ban (root eslint.config.mjs, UNTOUCHED by this plan) + the 79-04 fully-gone grep battery keep re-introduction CI-visible |
| T-79-S2   | Tampering | `UserPicker.handleSearch` PostgREST `.or()` interpolation | accept (pre-existing, out of scope) | Facade FROZEN by locked decision — this plan does not touch UserPicker; flagged for future hardening; RLS bounds visibility                               |
| T-79-SC   | Tampering | Package installs                                          | n/a                                 | Zero packages installed this phase (RESEARCH § Package Legitimacy Audit)                                                                                  |

</threat_model>

<verification>
- All 7 files + the orphaned hook absent; barrel pruned by name with SearchableSelect exports retained
- `smart-input` namespace files and registration byte-identical (git diff clean on those paths)
- `tsc --noEmit` + `lint --max-warnings 0` + full `vitest run` all exit 0
</verification>

<success_criteria>

- The delete-7 half of ACET-01 complete: dead components fully removed with liveness re-proven at deletion time
- Zero collateral: live SearchableSelect/UserPicker path, shared i18n namespace, validation-rules/ValidationIndicator, and the ESLint ban all untouched
  </success_criteria>

<output>
Create `.planning/phases/79-aceternity-removal/79-02-SUMMARY.md` when done. Note `ValidationIndicator.tsx` as pre-existing dead code left in place (out of scope).
</output>
