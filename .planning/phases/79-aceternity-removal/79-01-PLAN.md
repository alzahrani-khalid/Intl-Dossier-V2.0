---
phase: 79-aceternity-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/forms/__tests__/SearchableSelect.a11y.test.tsx
autonomous: true
requirements: [ACET-01]
must_haves:
  truths:
    - "Implements the locked user decision (CONTEXT.md 'Aceternity scope', 2026-07-03): this is the Wave 0 test contract for the 1 live component being rebuilt (delete-7/rebuild-1 scope)"
    - 'An automated a11y test file exercises the CURRENT SearchableSelect against the Phase 75 contract: C1 role=alert error announcement in EN and AR, C2a keyboard focus order, C2b 12-attribute ARIA + jest-axe'
    - 'The T-79-01 nested-interactive question is answered from axe evidence against the current component, not assumption, and the verdict is recorded for plan 79-04'
    - 'Attribute, alert, and keyboard tests pass against the UNMODIFIED component — proving the tests capture the existing contract before any rebuild touches it'
  artifacts:
    - path: 'frontend/src/components/forms/__tests__/SearchableSelect.a11y.test.tsx'
      provides: 'Phase 75 contract regression tests (C1/C2a/C2b) run against real cmdk + Radix Popover'
      min_lines: 80
      contains: 'toHaveNoViolations'
  key_links:
    - from: 'frontend/src/components/forms/__tests__/SearchableSelect.a11y.test.tsx'
      to: 'frontend/src/components/forms/SearchableSelect.tsx'
      via: 'direct component import (real cmdk/Popover — ui/command and ui/popover NOT mocked)'
      pattern: "from '\\.\\./SearchableSelect'"
    - from: 'frontend/src/components/forms/__tests__/SearchableSelect.a11y.test.tsx'
      to: 'jest-axe'
      via: 'axe(container) + toHaveNoViolations matcher'
      pattern: 'jest-axe'
---

<objective>
Create the Wave 0 accessibility contract test for `SearchableSelect` — written against the CURRENT component BEFORE the rebuild — so plan 79-04 can prove the Phase 75 ARIA/validation contract is *preserved*, not merely *present*.

Purpose: Nyquist Wave 0 requirement from 79-VALIDATION.md. Without this baseline, "preserved" is unverifiable — the rebuild could silently regress the contract and still show a green new test.
Output: `frontend/src/components/forms/__tests__/SearchableSelect.a11y.test.tsx` covering criteria C1 (role="alert" EN+AR), C2a (keyboard focus order), C2b (12-attribute ARIA + zero serious/critical axe violations), plus a recorded verdict on the T-79-01 nested-interactive hazard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/79-aceternity-removal/79-VALIDATION.md
@.planning/phases/79-aceternity-removal/79-RESEARCH.md

Key sections of 79-RESEARCH.md for this plan: "Code Examples" (the test-file skeleton — start from it verbatim), "The ARIA/Validation Contract Checklist" (the 12-attribute table with line numbers), Pitfalls 2 (global react-i18next mock returns raw keys), 3 (jsdom polyfills for real cmdk/Popover), 4 (MSW error-on-unhandled — avoided by testing SearchableSelect directly with fixture options, no UserPicker render).
</context>

<interfaces>
<!-- The contract under test — extracted from 79-RESEARCH.md, line-verified 2026-07-03 against frontend/src/components/forms/SearchableSelect.tsx -->

Trigger Button (attrs 1–8): `role="combobox"` (line 434), `aria-controls={listboxId}` (435, equals CommandList id at line 489), `aria-expanded={open}` (436), `aria-haspopup="listbox"` (437), `aria-labelledby` → `{selectId}-label` when `label` set (438), `aria-describedby` → space-joined `[errorId, helpId]` filtered to active (439), `aria-invalid={!!error}` (440), `aria-required={required}` (441).

Attrs 9–12: clear affordance `<span role="button">` (450) + `aria-label={t('smart-input:select.clear')}` (451) — rendered only when a value is selected; required-marker `aria-label={t('common:validation.required')}` (420); error `<p id={errorId} role="alert">` (564) — the live region.

cmdk-supplied (not literal in file): `role="listbox"` on `CommandList id={listboxId}`, `role="option"`/`aria-selected`, `aria-activedescendant` on the search input.

Keyboard contract: click/Enter/Space opens → focus moves to search input (0 ms-timeout effect, lines 287–293) → ArrowUp/ArrowDown move active option → Enter selects (fires `onChange`, single-select closes) → Escape closes and returns focus to trigger (Radix Popover).

Relevant props (all current): `options: SelectOption[]`, `value?: string | string[]`, `onChange?: (value: string | string[] | null) => void`, `label?: string`, `error?: string` (RESOLVED string, not FieldError), `required?: boolean`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write the SearchableSelect a11y contract test file (C1 + C2a + C2b)</name>
  <files>frontend/src/components/forms/__tests__/SearchableSelect.a11y.test.tsx</files>
  <read_first>
    - .planning/phases/79-aceternity-removal/79-RESEARCH.md — "Code Examples" § New test file skeleton (copy as the starting point) and "The ARIA/Validation Contract Checklist" (the 12-attribute table)
    - frontend/src/components/forms/SearchableSelect.tsx — the component under test (confirm current prop names and the id scheme `{selectId}-label` / errorId / listboxId)
    - frontend/tests/setup.ts — what is already polyfilled (ResizeObserver, matchMedia) vs missing (scrollIntoView, pointer capture), and the global `vi.mock('react-i18next')` behavior
    - frontend/src/components/layout/AppShell.a11y.test.tsx — jest-axe matrix exemplar
    - frontend/src/components/work-creation/__tests__/DossierPicker.test.tsx — the `@/hooks/useDirection` mock pattern (do NOT copy its ui/command mock — this test must use the REAL cmdk)
  </read_first>
  <action>
    Create `frontend/src/components/forms/__tests__/SearchableSelect.a11y.test.tsx` (the `__tests__` directory does not exist yet — create it; `__tests__/**` is exempt from the PascalCase filename rule). Start from the RESEARCH skeleton and extend it to the full contract. Wave 0 of the locked user decision (CONTEXT.md "Aceternity scope"). Requirements:

    1. jsdom polyfills in `beforeAll` (RESEARCH Pitfall 3): `window.HTMLElement.prototype.scrollIntoView = vi.fn()`, plus `hasPointerCapture`/`releasePointerCapture` stubs. Do NOT mock `@/components/ui/command` or `@/components/ui/popover` — the real listbox semantics ARE the contract under test.
    2. Direction matrix: mock ONLY `@/hooks/useDirection` with a mutable `directionMock.isRTL` (DossierPicker pattern from read_first).
    3. Fixture: inline options array (2 users, with `description` emails). No UserPicker render, no supabase — avoids MSW `onUnhandledRequest: 'error'` (Pitfall 4).
    4. Test group A — trigger contract attrs 1–8 (closed state, rendered with `label="Assignee"`, `required`, `error="Required"`): `getByRole('combobox')` resolves to the trigger; assert `aria-haspopup="listbox"`, `aria-expanded="false"`, `aria-invalid="true"`, `aria-required="true"`; `aria-describedby` contains the error id; `aria-labelledby` is set; `aria-controls` is truthy. Then open (click) and assert `aria-expanded="true"` and that `getByRole('listbox')` (cmdk CommandList) has `id` equal to the trigger's `aria-controls` value.
    5. Test group B — attrs 9–12 (render with `value="u1"` so the clear affordance appears): assert the clear affordance is queryable by its accessible name — NOTE (Pitfall 2): the global react-i18next mock returns RAW KEYS for unmapped keys, so its aria-label resolves to the literal string `smart-input:select.clear`; assert on that raw key. Assert the required marker's aria-label is the raw key `common:validation.required`. Assert `getByRole('alert')` exists when `error` is set AND its `id` is contained in the trigger's `aria-describedby` (the errorId ↔ describedby link).
    6. Test group C — C1 bilingual `role="alert"`: `it.each([['en', 'This field is required', false], ['ar', 'هذا الحقل مطلوب', true]])` — set `directionMock.isRTL`, wrap render in `<div dir={rtl ? 'rtl' : 'ltr'}>`, pass the message as the `error` prop (it is a caller-RESOLVED string — this bypasses the mocked `t()` entirely, per Pitfall 2), assert `getByRole('alert')` has that exact text content.
    7. Test group D — C2a keyboard flow with `userEvent.setup()`: click trigger → `aria-expanded="true"` + listbox present → the search input receives focus (the 0 ms-timeout effect — flush with `waitFor`/`findBy`, do not assert synchronously) → `{ArrowDown}{Enter}` → `onChange` called with `'u1'` → reopen → `{Escape}` → popover closed AND trigger has focus. Use keyboard APIs (`{Enter}`, `{Escape}`, `{ArrowDown}`), not raw fireEvent.
    8. Test group E — C2b axe: run `axe(container)` + `toHaveNoViolations` on (a) closed state rendered with `label`, `value="u1"` (selected → clear affordance present), `error`, `required`; and (b) the open state (render, click trigger, then axe). Write these as STRICT assertions (no violation filtering).

    Style: repo conventions — no semicolons, single quotes, explicit return types not required inside test callbacks (test files), no `any`.

  </action>
  <verify>
    <automated>pnpm --dir frontend vitest run src/components/forms/__tests__/SearchableSelect.a11y.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/forms/__tests__/SearchableSelect.a11y.test.tsx` exists, imports `SearchableSelect` from `../SearchableSelect` (the real component), and contains NO `vi.mock` of `@/components/ui/command` or `@/components/ui/popover` (`! grep -qE "vi\.mock\(['\"]@/components/ui/(command|popover)" frontend/src/components/forms/__tests__/SearchableSelect.a11y.test.tsx` exits 0)
    - The file contains `toHaveNoViolations`, `role="alert"` assertions for both the EN and the AR literal `هذا الحقل مطلوب`, and a `{Escape}` keyboard step (grep-verifiable)
    - `pnpm --dir frontend vitest run src/components/forms/__tests__/SearchableSelect.a11y.test.tsx` executes the file with test groups A–D (attribute/alert/keyboard) PASSING against the current unmodified component (group E axe outcome is triaged in Task 2)
  </acceptance_criteria>
  <done>Test file committed; groups A–D green against the current component; real cmdk/Popover rendered (no primitive mocks).</done>
</task>

<task type="auto">
  <name>Task 2: Triage the axe run — resolve T-79-01 (nested-interactive) from evidence</name>
  <files>frontend/src/components/forms/__tests__/SearchableSelect.a11y.test.tsx</files>
  <read_first>
    - .planning/phases/79-aceternity-removal/79-RESEARCH.md — Pitfall 1 (the nested clear-affordance hazard and its two defined outcomes)
    - The vitest output of Task 1's run (the axe test result for the selected-value state)
  </read_first>
  <action>
    Run the full test file and triage test group E (axe) against the CURRENT component — this answers research assumption A1 empirically:

    - **If axe passes clean:** no file change. Record verdict "T-79-01: NOT flagged — clear affordance preserved verbatim in 79-04" in this plan's SUMMARY.
    - **If axe fails ONLY with `nested-interactive`** (on the selected-value render, from the `<span role="button">` inside the trigger `<Button>`): convert ONLY the failing axe test(s) to `it.skip` with the exact comment `// T-79-01: nested-interactive flagged on current clear affordance — 79-04 restructures it as a focusable sibling and MUST unskip this test`. Do NOT weaken the assertion itself (no violation filtering, no rule disabling). Record verdict "T-79-01: FLAGGED — 79-04 must restructure the clear affordance" in the SUMMARY.
    - **If axe fails with any OTHER serious/critical rule:** stop and investigate — the test setup is likely wrong (missing polyfill, mocked primitive, missing label association in the fixture render). Fix the SETUP per Pitfalls 2–4, never the assertion's meaning. If a genuine second pre-existing violation is confirmed, apply the same skip-with-comment protocol and record it for 79-04.

    Keyboard fallback (research assumption A3, MEDIUM): if — after the polyfills and `waitFor` flushing — the Escape-returns-focus-to-trigger step proves undrivable in jsdom, isolate ONLY that assertion into its own test, apply the same `it.skip` + comment protocol referencing RESEARCH A3's Playwright fallback, and record it in the SUMMARY as a deviation for 79-04 to resolve (real-browser Playwright smoke per 79-VALIDATION § Manual-Only). The open→focus→ArrowDown→Enter-select assertions must remain green in vitest regardless.

  </action>
  <verify>
    <automated>pnpm --dir frontend vitest run src/components/forms/__tests__/SearchableSelect.a11y.test.tsx && pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir frontend vitest run src/components/forms/__tests__/SearchableSelect.a11y.test.tsx` exits 0 (all non-skipped tests green — suite is ordering-safe for the parallel Wave 1 plans)
    - Any `it.skip` in the file carries a `T-79-01` (or `A3`) comment naming 79-04 as the unskip owner (`grep -A1 "it.skip" frontend/src/components/forms/__tests__/SearchableSelect.a11y.test.tsx` shows the comment context, or zero skips exist)
    - `pnpm --dir frontend exec tsc --noEmit` exits 0
    - The plan SUMMARY records the explicit T-79-01 verdict (FLAGGED or NOT flagged) — 79-04's rebuild task branches on it
  </acceptance_criteria>
  <done>Axe verdict recorded from evidence; test suite green (with at most documented, owner-assigned skips); tsc clean.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary       | Description                                                                              |
| -------------- | ---------------------------------------------------------------------------------------- |
| Test tier only | This plan adds test code; no production surface, no untrusted input crosses anything new |

## STRIDE Threat Register

Per 79-RESEARCH.md § Security Domain: this phase touches ASVS V5 (Input Validation) only. **No high-severity NEW threats are introduced by this plan** — it is test-only code exercising an existing component with inline fixtures.

| Threat ID | Category  | Component                                                               | Disposition                         | Mitigation Plan                                                                                                                                                                                                                      |
| --------- | --------- | ----------------------------------------------------------------------- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-79-S1   | Tampering | SearchableSelect option rendering (XSS via option labels)               | mitigate                            | Contract test renders options as React text nodes; 79-04 asserts no `dangerouslySetInnerHTML` is introduced                                                                                                                          |
| T-79-S2   | Tampering | `UserPicker.handleSearch` PostgREST `.or()` filter-string interpolation | accept (pre-existing, out of scope) | Facade is FROZEN by the locked user decision — do NOT modify this phase; flagged for a future hardening pass (`.ilike()` builder calls or sanitize `,().`); RLS still bounds visibility. This plan does not render UserPicker at all |
| T-79-SC   | Tampering | Package installs                                                        | n/a                                 | Zero packages installed this phase (RESEARCH § Package Legitimacy Audit: no installs; all test deps already in repo)                                                                                                                 |

</threat_model>

<verification>
- `pnpm --dir frontend vitest run src/components/forms/__tests__/SearchableSelect.a11y.test.tsx` exits 0
- `pnpm --dir frontend exec tsc --noEmit` exits 0
- File renders real cmdk + Radix Popover (no `ui/command`/`ui/popover` mocks present)
</verification>

<success_criteria>

- Wave 0 test file exists and captures the Phase 75 contract against the CURRENT component (groups A–D green pre-rebuild)
- T-79-01 verdict recorded from an actual axe run, with the skip-and-owner protocol applied if flagged
- Suite is green in isolation so parallel Wave 1 plans running full vitest are unaffected
  </success_criteria>

<output>
Create `.planning/phases/79-aceternity-removal/79-01-SUMMARY.md` when done. MUST include the T-79-01 verdict line (FLAGGED / NOT flagged) and any A3 keyboard deviation — plan 79-04 reads it.
</output>
