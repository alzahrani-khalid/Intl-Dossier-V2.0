---
phase: 79-aceternity-removal
plan: 04
type: execute
wave: 2
depends_on: [79-01, 79-02, 79-03]
files_modified:
  - frontend/src/components/forms/SearchableSelect.tsx
  - frontend/src/components/forms/__tests__/SearchableSelect.a11y.test.tsx
autonomous: true
requirements: [ACET-01, ACET-02]
must_haves:
  truths:
    - "Implements the locked user decision (CONTEXT.md 'Aceternity scope', 2026-07-03): the 1 live component is REBUILT in place on the retained HeroUI v3 Button + Radix Popover + cmdk internals — the rebuild-1 half of delete-7/rebuild-1; HeroUI ComboBox migration explicitly rejected"
    - 'SearchableSelect has no variant prop, no aceternity string, no motion/react import, and no hard-coded rgba shadow literals — Linear tokens + logical properties throughout'
    - 'The rebuilt SearchableSelect announces validation errors via role=alert in EN and AR, and the full 12-attribute ARIA + keyboard focus contract holds — the Wave 0 a11y test file is fully green with ZERO T-79-01 it.skip remaining (a documented A3 jsdom-limitation skip, only if 79-01 recorded it, is the sole permitted exception)'
    - 'The UserPicker facade file, public API, and module path are byte-untouched; CreateTaskCtas.test.tsx passes'
    - 'The fully-gone grep battery returns 0 repo-wide, whole-repo lint --max-warnings 0 passes (inverted import ban green), and the size-limit bundle budgets do not regress'
  artifacts:
    - path: 'frontend/src/components/forms/SearchableSelect.tsx'
      provides: 'Rebuilt live combobox — HeroUI v3 Button trigger + Radix Popover + cmdk, variant/motion/shadow-free'
      min_lines: 300
      contains: 'role="combobox"'
    - path: 'frontend/src/components/forms/__tests__/SearchableSelect.a11y.test.tsx'
      provides: 'Contract proof, fully unskipped and green post-rebuild'
      contains: 'toHaveNoViolations'
  key_links:
    - from: 'frontend/src/components/forms/UserPicker.tsx'
      to: 'frontend/src/components/forms/SearchableSelect.tsx'
      via: 'unchanged same-directory import — the facade seam'
      pattern: "from '\\./SearchableSelect'"
    - from: 'SearchableSelect trigger Button'
      to: 'cmdk CommandList'
      via: 'aria-controls={listboxId} ↔ CommandList id={listboxId}'
      pattern: 'aria-controls'
    - from: 'SearchableSelect error <p role=alert>'
      to: 'trigger aria-describedby'
      via: 'errorId membership in the space-joined describedby list'
      pattern: 'role="alert"'
    - from: 'frontend/src/components/forms/SearchableSelect.tsx'
      to: 'frontend/src/i18n/en/smart-input.json'
      via: "t('smart-input:select.*') keys — namespace untouched"
      pattern: 'smart-input:select'
---

<objective>
Rebuild the one LIVE Aceternity-marked component — `SearchableSelect` — as a surgical in-place edit that strips the `variant`/`aceternity` styling path, the rgba shadow literals, and all `motion/react` usage while preserving the full Phase 75 ARIA/keyboard/validation contract (proven by the Wave 0 test staying green), then run the phase's fully-gone proof and REQUIRED gates.

Purpose: The rebuild-1 half of ACET-01 plus the final ACET-02 fully-gone proof. The Wave 0 baseline from 79-01 makes "preserved" verifiable.
Output: Variant-free, motion-free `SearchableSelect.tsx` on the retained HeroUI v3 Button + Radix Popover + cmdk internals; a11y test fully green with zero skips; grep battery at 0; lint + full suite + size-limit green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/79-aceternity-removal/79-RESEARCH.md
@.planning/phases/79-aceternity-removal/79-01-SUMMARY.md

Key sections of 79-RESEARCH.md for this plan: "The Rebuild: Recommendation" (the in-place restyle checklist — the task spec), "The ARIA/Validation Contract Checklist" (the 12 attributes + keyboard contract), "The UserPicker Facade Contract" (the frozen API), Pitfall 1 (nested-interactive resolution path), Pitfall 5 (heroui-vendor 9 KB budget — why NOT to import HeroUI ComboBox), "Code Examples" § Fully-gone verification + § Bundle gate.
Contract source of truth: @.planning/phases/75-ui-component-migration-audit/75-AUDIT-aceternity-contracts.md § SearchableSelect + § UserPicker facade contract.
</context>

<interfaces>
<!-- The frozen facade API — UserPicker.tsx lines 13–22, byte-verified 2026-07-03. This plan must NOT change UserPicker.tsx at all; shown so the executor knows which SearchableSelect props the facade binds. -->

```ts
export interface UserPickerProps {
  value?: string // single user id
  onChange?: (userId: string | null) => void // consumers rely on `?? ''` coalescing
  label?: string
  placeholder?: string
  error?: string // resolved string, NOT FieldError
  required?: boolean
  disabled?: boolean
  className?: string
}
```

SearchableSelect props UserPicker passes through (the minimum surface the rebuild must keep working): `options`, `value`, `onChange`, `onSearchChange`, `label`, `placeholder`, `searchPlaceholder`, `error`, `required`, `disabled`, `loading`, `className`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild SearchableSelect in place — strip variant/motion/shadows, preserve the contract, resolve T-79-01</name>
  <files>frontend/src/components/forms/SearchableSelect.tsx, frontend/src/components/forms/__tests__/SearchableSelect.a11y.test.tsx</files>
  <read_first>
    - frontend/src/components/forms/SearchableSelect.tsx — the FULL current source (the edit target; locate: `variant` prop ~line 92, `aceternityTriggerClasses` ~392–398, `triggerClasses` ternary ~400, `motion/react` import ~line 9, the 12 ARIA attributes per the checklist, the focus effect ~287–293)
    - .planning/phases/79-aceternity-removal/79-01-SUMMARY.md — the recorded T-79-01 verdict (FLAGGED or NOT flagged) and any A3 keyboard deviation; this task branches on it
    - .planning/phases/79-aceternity-removal/79-RESEARCH.md — "The Rebuild: Recommendation" (the restyle checklist, followed verbatim) + "The ARIA/Validation Contract Checklist"
    - .planning/phases/75-ui-component-migration-audit/75-AUDIT-aceternity-contracts.md — § SearchableSelect contract (what is contractual vs animation-only) + § UserPicker facade contract
    - frontend/src/components/forms/__tests__/SearchableSelect.a11y.test.tsx — the Wave 0 contract test (must stay green; unskip any T-79-01/A3 skips here)
    - frontend/src/components/forms/UserPicker.tsx — READ ONLY to confirm the pass-through prop surface; this file must NOT be modified
  </read_first>
  <action>
    Surgical in-place edit of `SearchableSelect.tsx` per the locked user decision (CONTEXT.md "Aceternity scope") and the RESEARCH restyle checklist. **Primitive decision is LOCKED (RESEARCH "The Rebuild", HIGH confidence): KEEP the cmdk + Radix Popover internals and the HeroUI v3 Button trigger. Do NOT import `@heroui/react` ComboBox or any new `@heroui` module** — the RAC input-combobox cannot preserve the captured button-trigger focus contract, and the `heroui-vendor` chunk budget is 9 KB gzip (REQUIRED Bundle Size Check; Pitfall 5). ACET-01's "HeroUI v3/Radix primitives" is satisfied by the existing trigger Button + Radix Popover.

    Remove:
    1. The `variant?: 'default' | 'aceternity'` prop and every `variant`/`'aceternity'` reference; delete `aceternityTriggerClasses` (the hard-coded rgba box-shadow literals — Linear design-rule violations) and the `triggerClasses` ternary, keeping `triggerBaseClasses` as the single trigger class source.
    2. The `import { m, AnimatePresence } from 'motion/react'` line and all usage: `m.label` → `label`, the two `m.p` (help, error) → plain `<p>` with the SAME `id`, `role`, and classNames (element insertion still fires the `role="alert"` announcement — the motion was decoration, non-contractual per the 75 audit), and drop the `AnimatePresence` wrappers. KEEP the chevron rotation — it is a CSS `transition-transform`, not motion/react.

    Preserve verbatim (the contract — every item is asserted by the Wave 0 test):
    3. All 12 ARIA attributes per the checklist table (combobox trigger attrs 1–8, clear-affordance aria-label, required-marker aria-label, `role="alert"` error with `id={errorId}` ∈ `aria-describedby`); the cmdk-supplied listbox/option/activedescendant semantics; `Command shouldFilter={false}` + `filterOptions` + `onSearchChange`; the open→focus-search-input effect; Radix Escape-returns-focus; single-select close-and-clear-query behavior.
    4. RTL/Linear compliance: logical properties only (`ms-2`, `me-2`, `text-start` — already compliant, must not regress), chevron `isRTL && 'rotate-180'`, `--radix-popover-trigger-width`, `align="start"`, all colors via existing token-mapped utilities (no raw hex, no Tailwind palette literals), radii token-driven.
    5. The `t('smart-input:select.*')` key usage (namespace untouched by 79-02 — do not rename keys).
    6. **Prop surface NOT trimmed** (discretion resolved per RESEARCH Open Question 2 recommendation: smallest verifiable diff): `multiple`, `creatable`, `groupBy`, `renderOption`, `renderValue`, `maxDisplayed` all stay. `forwardRef<HTMLButtonElement>` stays.

    T-79-01 branch (from the 79-01 SUMMARY verdict):
    7. **If FLAGGED (nested-interactive):** restructure the clear affordance from the `<span role="button">` nested inside the trigger `<Button>` to a real keyboard-operable `<button type="button">` SIBLING of the trigger (same visual position via the wrapping flex row), keeping `aria-label={t('smart-input:select.clear')}` and the clear-on-activate behavior. Then remove the `it.skip` from the a11y test (the T-79-01-commented test) — it must pass strict. Do not weaken any test query.
    8. **If NOT flagged:** preserve the clear affordance structure verbatim; no test edits needed.
    9. If 79-01 recorded an A3 keyboard deviation (Escape-focus-return skipped in jsdom): attempt to make it green post-rebuild; if still undrivable in jsdom, leave the documented skip, and note in the SUMMARY that the optional non-gating Playwright smoke (79-VALIDATION § Manual-Only) covers it.

    Forbidden: modifying `UserPicker.tsx` (file, API, module path — the frozen facade; its 4 consumers must not change), any `@heroui` import addition, `dangerouslySetInnerHTML`, physical-direction classes (`ml-*`/`pl-*`/`text-left`).

  </action>
  <verify>
    <automated>pnpm --dir frontend vitest run src/components/forms/__tests__/SearchableSelect.a11y.test.tsx && pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -in "aceternity" frontend/src/components/forms/SearchableSelect.tsx` produces no output (no variant prop, no aceternityTriggerClasses)
    - `grep -n "motion/react" frontend/src/components/forms/SearchableSelect.tsx` produces no output; `grep -n "rgba(" frontend/src/components/forms/SearchableSelect.tsx` produces no output
    - `grep -n "dangerouslySetInnerHTML" frontend/src/components/forms/SearchableSelect.tsx` produces no output (T-79-S1 control)
    - `pnpm --dir frontend vitest run src/components/forms/__tests__/SearchableSelect.a11y.test.tsx` exits 0 AND no T-79-01 skip survives: `grep -B1 -A1 "it.skip" frontend/src/components/forms/__tests__/SearchableSelect.a11y.test.tsx | grep -c "T-79-01"` reports 0 (an explicitly-labeled A3 jsdom-limitation skip is the ONLY `it.skip` permitted to remain, and only if 79-01 recorded it — this plan owns the T-79-01 unskip)
    - `git diff --stat` shows ZERO change to `frontend/src/components/forms/UserPicker.tsx`
    - `grep -c "aria-" frontend/src/components/forms/SearchableSelect.tsx` is >= 10 (the explicit ARIA contract survives — 12 occurrences expected)
    - `pnpm --dir frontend exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>SearchableSelect is variant-free, motion-free, shadow-literal-free on the retained HeroUI Button + Radix + cmdk internals; the Wave 0 contract test is fully green (T-79-01 resolved per evidence); UserPicker byte-untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Fully-gone proof + phase gates (grep battery, lint, full suite, facade guard, bundle)</name>
  <files></files>
  <read_first>
    - .planning/phases/79-aceternity-removal/79-RESEARCH.md — "Code Examples" § Fully-gone verification (single script) + § Bundle gate + the grep battery in "Registry Removal + Fully-Gone Proof"
    - frontend/.size-limit.json — the budgets being gated (note heroui-vendor 9 KB)
  </read_first>
  <action>
    Run the phase's final proof from the repo root (all Wave 1 plans + Task 1 are merged at this point):

    1. **Grep battery (each must produce zero matches):** (a) `grep -rn "variant=[\"']aceternity" frontend/src tests --include="*.tsx" --include="*.ts"`; (b) `grep -rn "Aceternity" frontend/src tests --include="*.tsx" --include="*.ts"`; (c) `grep -n "aceternity" frontend/components.json`; (d) `grep -rn "from ['\"]aceternity\|from ['\"]@aceternity" frontend/src tests --include="*.tsx" --include="*.ts"`; (e) `ls frontend/src/components/forms/ | grep -iE "Aceternity|^SmartInput|^FormFieldWithValidation"`. Plus the STRICT unconditional proof enabled by 79-03's purge: `grep -rni "aceternity" frontend/src --include="*.tsx" --include="*.ts"` → zero matches, no exclusions.
    2. **Lint gate (import ban):** `pnpm --dir frontend lint --max-warnings 0` — whole repo, CI-equivalent; proves the untouched inverted `no-restricted-imports` ban stays green.
    3. **Full suite + facade guard:** `pnpm --dir frontend vitest run` (full suite), and confirm `src/pages/engagements/workspace/__tests__/CreateTaskCtas.test.tsx` passed within it (its `vi.doMock('@/components/forms/UserPicker')` guards the frozen module path).
    4. **Bundle gate (REQUIRED CI check):** `cd frontend && pnpm build && pnpm exec size-limit` — inspect the output for EVERY `exceeded` line, not just the exit code (failures stack hidden). Expected direction: reduced (7 components + motion usage deleted); any regression = investigate before finishing. Record the size deltas in the SUMMARY.
    5. **Graph refresh (project CLAUDE.md, non-gating):** run `graphify update .` to refresh the knowledge graph after the code changes.
    6. If ANY battery line or gate fails: fix forward within this plan's scope (missed residue → remove it) — do NOT weaken a grep, skip a budget, or touch `eslint.config.mjs`.

  </action>
  <verify>
    <automated>! grep -rqin "aceternity" frontend/src --include="*.tsx" --include="*.ts" && ! grep -qin "aceternity" frontend/components.json && pnpm --dir frontend lint --max-warnings 0 && pnpm --dir frontend vitest run && pnpm --dir frontend build && pnpm --dir frontend exec size-limit</automated>
  </verify>
  <acceptance_criteria>
    - All 5 battery greps + the strict case-insensitive `grep -rni "aceternity" frontend/src --include="*.tsx" --include="*.ts"` produce zero matches
    - `pnpm --dir frontend lint --max-warnings 0` exits 0
    - `pnpm --dir frontend vitest run` exits 0, including `CreateTaskCtas.test.tsx`
    - `pnpm --dir frontend build && pnpm --dir frontend exec size-limit` exits 0 with zero `exceeded` lines in the output; deltas recorded in the SUMMARY
    - `git diff --stat` across the phase shows no change to `eslint.config.mjs`, `frontend/src/components/forms/UserPicker.tsx`, or `frontend/src/i18n/{en,ar}/smart-input.json`
  </acceptance_criteria>
  <done>Aceternity is provably fully gone (unconditional grep proof), the import ban and all REQUIRED gates are green, and the bundle shrank or held.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                                             |
| ----------------------------- | ------------------------------------------------------------------------------------------------------- |
| client → Supabase PostgREST   | UserPicker's `users`-table reads cross it — FROZEN this phase (facade untouched); RLS bounds visibility |
| user input → SearchableSelect | Search query + option labels rendered client-side                                                       |

## STRIDE Threat Register

Per 79-RESEARCH.md § Security Domain (ASVS V5 only): **no high-severity NEW threats are introduced by this phase** — the rebuild removes styling/motion code without adding input paths, HTML injection sinks, or new dependencies; deletion + registry removal strictly shrink the surface.

| Threat ID | Category  | Component                                                                                                                                                         | Disposition                           | Mitigation Plan                                                                                                                                                                                    |
| --------- | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-79-S1   | Tampering | XSS via user-supplied `full_name`/`email` in options                                                                                                              | mitigate                              | React text-node escaping preserved — Task 1 acceptance asserts zero `dangerouslySetInnerHTML` in the rebuilt file                                                                                  |
| T-79-S2   | Tampering | Pre-existing PostgREST filter-string interpolation in `UserPicker.handleSearch` (`.or('full_name.ilike.%${query}%…')`) — injects PostgREST filter syntax, not SQL | accept (out of scope / facade frozen) | Do NOT modify this phase (locked facade). FLAG for future hardening: `.ilike()` builder calls or sanitize `,().` from the query. RLS still bounds visibility. Record the flag in the phase SUMMARY |
| T-79-S3   | Elevation | Dormant Aceternity import/registry path re-introduction                                                                                                           | mitigate                              | Inverted `no-restricted-imports` ban untouched + this plan's fully-gone grep battery as the anti-regression proof                                                                                  |
| T-79-SC   | Tampering | Package installs                                                                                                                                                  | n/a                                   | Zero packages installed this phase (RESEARCH § Package Legitimacy Audit: no installs)                                                                                                              |

</threat_model>

<verification>
- Wave 0 a11y test fully green post-rebuild (zero T-79-01 skips) — contract *preserved*, not merely present
- Strict repo-wide case-insensitive aceternity grep at 0; battery at 0; lint `--max-warnings 0` green
- Full vitest suite + CreateTaskCtas facade guard green; build + size-limit green with no `exceeded` lines
- UserPicker.tsx, eslint.config.mjs, smart-input i18n files byte-untouched across the phase
</verification>

<success_criteria>

- ACET-01 rebuild half complete: SearchableSelect on HeroUI v3 Button + Radix Popover + cmdk with the Phase 75 contract proven preserved (role="alert" EN+AR, keyboard order, 12-attribute ARIA, axe clean)
- ACET-02 complete and proven: registry gone, ban green, zero aceternity references repo-wide with no grep exclusions
- Bundle Size Check direction confirmed (shrunk or held) — the REQUIRED CI gate will pass
  </success_criteria>

<output>
Create `.planning/phases/79-aceternity-removal/79-04-SUMMARY.md` when done. MUST include: the T-79-01 resolution taken (verbatim-preserved vs sibling-restructured), size-limit deltas, and the T-79-S2 future-hardening flag for `UserPicker.handleSearch`.
</output>
