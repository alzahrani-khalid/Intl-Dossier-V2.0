---
phase: 42-remaining-pages
plan: 11
type: execute
wave: 2
depends_on:
  - 42-05
  - 42-06
  - 42-07
  - 42-08
  - 42-09
  - 42-10
files_modified:
  - frontend/tests/e2e/page-42-axe.spec.ts
  - frontend/tests/e2e/touch-targets-42.spec.ts
  - .planning/phases/42-remaining-pages/42-VERIFICATION.md
autonomous: false
requirements:
  - PAGE-01
  - PAGE-02
  - PAGE-03
  - PAGE-04
  - PAGE-05
tags:
  - phase-42
  - axe-core
  - a11y
  - touch-targets
  - verification
  - wave-2
must_haves:
  truths:
    - 'axe-core WCAG AA gate passes for all 5 pages × LTR + AR (10 cases) — zero serious or critical violations'
    - 'Touch-target gate passes for all 5 pages — every interactive element ≥44×44 in both dimensions'
    - 'Both specs un-skipped (test.skip → test) and run in CI'
    - 'Phase 42 VERIFICATION.md authored with verdict, requirement-by-requirement traceability, and any documented deviations'
  artifacts:
    - path: frontend/tests/e2e/page-42-axe.spec.ts
      provides: 'Activated axe-core gate (10 cases)'
    - path: frontend/tests/e2e/touch-targets-42.spec.ts
      provides: 'Activated touch-target gate (5 cases)'
    - path: .planning/phases/42-remaining-pages/42-VERIFICATION.md
      provides: 'Phase 42 verdict (PASS / PASS-WITH-DEVIATION / PARTIAL) with PAGE-01..05 traceability'
  key_links:
    - from: frontend/tests/e2e/page-42-axe.spec.ts
      to: .axe-core
      via: "AxeBuilder({ page }).include('.page').withTags(['wcag2a','wcag2aa','wcag21a','wcag21aa']).analyze()"
      pattern: 'AxeBuilder'
---

<objective>
Phase 42 final gates. Activates the axe-core WCAG AA spec and the touch-targets spec authored in Wave 0 plan 04. Runs the full Phase 42 Playwright suite, runs vitest unit tests, and authors `42-VERIFICATION.md` with verdict + requirement-level traceability.

This plan contains a `checkpoint:human-verify` gate at the end — the human reviewer signs off the VERIFICATION verdict before Phase 42 is closed.

Output: 2 un-skipped spec files, all gates green (or deviations documented), 1 new VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-remaining-pages/42-CONTEXT.md
@.planning/phases/42-remaining-pages/42-VALIDATION.md
@.planning/phases/42-remaining-pages/42-RESEARCH.md
@.planning/phases/40-list-pages/40-VERIFICATION.md
@.planning/phases/41-dossier-drawer/41-VERIFICATION.md
@.planning/phases/42-remaining-pages/42-04-SUMMARY.md
@.planning/phases/42-remaining-pages/42-05-SUMMARY.md
@.planning/phases/42-remaining-pages/42-06-SUMMARY.md
@.planning/phases/42-remaining-pages/42-07-SUMMARY.md
@.planning/phases/42-remaining-pages/42-08-SUMMARY.md
@.planning/phases/42-remaining-pages/42-09-SUMMARY.md
@.planning/phases/42-remaining-pages/42-10-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Un-skip axe + touch-targets specs and run gates</name>
  <files>frontend/tests/e2e/page-42-axe.spec.ts, frontend/tests/e2e/touch-targets-42.spec.ts</files>
  <read_first>
    - frontend/tests/e2e/page-42-axe.spec.ts (Wave 0 stub — un-skip all 10 cases)
    - frontend/tests/e2e/touch-targets-42.spec.ts (Wave 0 stub — un-skip all 5 cases)
    - frontend/tests/e2e/dossier-drawer-axe.spec.ts (Phase 41 precedent — same scoping pattern)
    - .planning/phases/42-remaining-pages/42-PATTERNS.md §"Pattern F" + §"Pattern G"
  </read_first>
  <action>

**Part A — Un-skip axe spec.** Open `frontend/tests/e2e/page-42-axe.spec.ts` and replace `test.skip(...)` with `test(...)` for ALL 10 cases (5 pages × LTR + AR).

**Part B — Un-skip touch-targets spec.** Open `frontend/tests/e2e/touch-targets-42.spec.ts` and replace `test.skip(...)` with `test(...)` for all 5 cases.

**Part C — Run the gates:**

```bash
cd frontend && pnpm playwright test \
  page-42-axe.spec.ts \
  touch-targets-42.spec.ts \
  --workers=1
```

If axe spec FAILS with `serious` or `critical` violations:

1. Capture the violation list per page+locale.
2. Categorize: color-contrast / aria-label / focus / keyboard / etc.
3. If the violation is on ported handoff CSS (color-contrast against tokens): determine whether the project Bureau-light token is the cause; surface to Wave 1 plan-owner for a token-tweak gap-closure plan.
4. If the violation is on missing aria-label or wrong heading order: fix inline in the relevant Wave 1 plan's component file (small surgical fix; un-skip stays).
5. Re-run the spec.

If touch-targets spec FAILS:

1. Identify which selector / page reports a sub-44px element.
2. Add `minHeight: 44, minWidth: 44` inline style to the component (or wrap in a 44×44 hit-area div).
3. Re-run.

If gates can't reach green within reasonable surgical effort, surface deviations and proceed to Task 2 (VERIFICATION.md will record them as PASS-WITH-DEVIATION).

**Part D — Run the full Phase 42 suite:**

```bash
pnpm --filter frontend test --run --reporter=verbose && \
pnpm --filter frontend playwright test --grep "phase-42|briefs-page|after-actions-page|tasks-page|activity-page|settings-page" --workers=1
```

Expected: vitest all green, Playwright all green (or deviations from previous step documented).
</action>
<verify>
<automated>! grep -q "test.skip" frontend/tests/e2e/page-42-axe.spec.ts && ! grep -q "test.skip" frontend/tests/e2e/touch-targets-42.spec.ts && cd frontend && pnpm playwright test page-42-axe.spec touch-targets-42.spec --workers=1 2>&1 | tail -10</automated>
</verify>
<done>Both specs un-skipped; gates either fully PASS or document deviations for VERIFICATION verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Author 42-VERIFICATION.md</name>
  <files>.planning/phases/42-remaining-pages/42-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/40-list-pages/40-VERIFICATION.md (Phase 40 verdict template)
    - .planning/phases/41-dossier-drawer/41-VERIFICATION.md (Phase 41 verdict template)
    - All 12 SUMMARY files in .planning/phases/42-remaining-pages/ (00 through 10)
    - Task 1 output (gate results + any deviations)
  </read_first>
  <action>
Author `.planning/phases/42-remaining-pages/42-VERIFICATION.md` following Phase 40/41 verdict template:

```markdown
---
phase: 42-remaining-pages
verdict: PASS | PASS-WITH-DEVIATION | PARTIAL
verified: 2026-05-XX
---

# Phase 42 Verification Verdict

## Summary

[1-2 sentence outcome — e.g., "5/5 PAGE requirements VERIFIED via 11 visual baselines + 10 axe cases + 5 touch-target cases + 30+ vitest unit cases. Phase 42 PASS with N documented deviations."]

## Requirement Traceability

| Requirement | Description                                                                                            | Plan  | Test                                                                                                                                                           | Status      |
| ----------- | ------------------------------------------------------------------------------------------------------ | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| PAGE-01     | Briefs card grid (auto-fill, minmax(320px, 1fr)) + status chip + page count + serif title + author/due | 42-05 | briefs-page-visual.spec.ts (LTR + AR), briefs-page.spec.ts (3 cases), BriefsPage.test.tsx (5 cases)                                                            | ✅ VERIFIED |
| PAGE-02     | After-actions table — engagement / date / dossier chip / decisions count / commitments count           | 42-06 | after-actions-page-visual.spec.ts (LTR + AR), after-actions-page.spec.ts (2 cases), AfterActionsTable.test.tsx (7 cases), useAfterActionsAll.test.ts (5 cases) | ✅ VERIFIED |
| PAGE-03     | Tasks card list — checkbox + glyph + title+subtitle + priority chip + due column                       | 42-07 | tasks-page-visual.spec.ts (LTR + AR), tasks-page.spec.ts (3 cases), MyTasksPage.test.tsx (6 cases)                                                             | ✅ VERIFIED |
| PAGE-04     | Activity timeline — `time · icon · who action what in where` with `where` in accent-ink                | 42-08 | activity-page-visual.spec.ts (LTR + AR), activity-page.spec.ts (2 cases), ActivityList.test.tsx (6 cases)                                                      | ✅ VERIFIED |
| PAGE-05     | Settings 240+1fr + 9-section nav (R-02) + Appearance design controls (D-11) + mobile pill row          | 42-09 | settings-page-visual.spec.ts (LTR + AR + mobile), settings-page.spec.ts (2 cases), SettingsLayout.test.tsx (6 cases)                                           | ✅ VERIFIED |

## Wave-by-wave outcomes

| Wave | Plans        | Status          | Notes                                                              |
| ---- | ------------ | --------------- | ------------------------------------------------------------------ |
| 0    | 42-00..42-04 | [PASS / status] | Icon + Edge Function + i18n + CSS port + fixtures                  |
| 1    | 42-05..42-09 | [PASS / status] | 5 page reskins, parallel; vitest 30+/30+                           |
| 2    | 42-10..42-11 | [PASS / status] | 11 visual baselines, axe-core 10/10, touch-targets 5/5, size-limit |

## Resolutions honored

- R-01: `<Icon/>` set shipped in Wave 0 plan 00 (14 glyphs)
- R-02: 9 settings sections in nav (NOT 7)
- R-03: density triad rename `spacious → dense` with migration shim in `DesignProvider.tsx`
- R-04: After-actions Date column = `engagement.engagement_date`
- R-05: Activity row click conditional on `metadata.navigation_url`

## Deviations (if any)

[List any documented deviations — for example, axe color-contrast violations on a specific token, deferred baseline regen, etc. Or "None — clean PASS."]

## Test totals

- Vitest: [N] tests across [M] files
- Playwright Phase 42: [N] specs ([M] visual, [P] functional, [Q] axe, [R] touch-targets)
- Bundle: [actual KB] / 815 KB ceiling

## Verdict

[verdict] — [1-2 sentence summary]. [Any next-action recommendations for Phase 43.]
```

Fill in actual numbers from the SUMMARY files of plans 00-10 and Task 1 outcomes.
</action>
<verify>
<automated>test -f .planning/phases/42-remaining-pages/42-VERIFICATION.md && grep -c "PAGE-01" .planning/phases/42-remaining-pages/42-VERIFICATION.md | grep -v "^0$" && grep -c "PAGE-05" .planning/phases/42-remaining-pages/42-VERIFICATION.md | grep -v "^0$" && grep -E "verdict: (PASS|PASS-WITH-DEVIATION|PARTIAL)" .planning/phases/42-remaining-pages/42-VERIFICATION.md</automated>
</verify>
<done>VERIFICATION.md exists with traceability table, all 5 PAGE-XX requirements mapped to plans + test files + status.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human approves Phase 42 verdict</name>
  <what-built>
    All 12 plans executed:
    - Wave 0 (5 infra plans): Icon, Edge Function, i18n, CSS port, fixtures
    - Wave 1 (5 page reskins): Briefs, After-actions, Tasks, Activity, Settings
    - Wave 2 (2 gate plans): visual baselines, axe + touch-targets + VERIFICATION
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/42-remaining-pages/42-VERIFICATION.md`.
    2. Confirm verdict matches actual outcome (PASS vs PASS-WITH-DEVIATION).
    3. Smoke-test the 5 pages in dev:
       - `cd frontend && pnpm dev`
       - Visit `/briefs`, `/after-actions`, `/tasks`, `/activity`, `/settings`
       - Toggle EN ↔ AR via the Tweaks Drawer or directly via `localStorage.setItem('id.locale','ar')`
       - Confirm: card grid / table / list / timeline / 2-col layout match the handoff
       - Confirm AR locale: chevrons flip, accent bar on inset-inline-start (or border-block-end on mobile)
    4. **Approval criteria:** verdict is accurate; no surprise regressions on adjacent pages (Dashboard, list pages, dossier drawer); all 5 PAGE-XX requirements VERIFIED in the table.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 42, or describe any failing acceptance items + which plan to revisit.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                  |
| -------- | ---------------------------------------------------------------------------- |
| (none)   | Verification step runs against already-vetted artifacts; no new code surface |

## STRIDE Threat Register

| Threat ID    | Category        | Component    | Disposition | Mitigation Plan                                                                                       |
| ------------ | --------------- | ------------ | ----------- | ----------------------------------------------------------------------------------------------------- |
| T-42-11-NONE | none-applicable | verification | accept      | Verification step does not introduce new attack surface; aggregates prior plans' threat dispositions. |

</threat_model>

<verification>
- `! grep -q "test.skip" frontend/tests/e2e/page-42-axe.spec.ts`
- `! grep -q "test.skip" frontend/tests/e2e/touch-targets-42.spec.ts`
- axe + touch-targets gates PASS (or deviations documented)
- `.planning/phases/42-remaining-pages/42-VERIFICATION.md` exists with verdict + 5 PAGE-XX rows
- Human checkpoint signed off
</verification>

<success_criteria>

- All Phase 42 gates green or documented deviations
- VERIFICATION.md authored with requirement traceability
- Human signoff received
- Phase 42 closed; Phase 43 (rtl-a11y-responsive-sweep) unblocked
  </success_criteria>

<output>
After completion, create `.planning/phases/42-remaining-pages/42-11-SUMMARY.md` with:
- Final test counts (vitest + Playwright)
- Verdict (PASS / PASS-WITH-DEVIATION / PARTIAL)
- Any deferred-to-Phase-43 items
- Human approval signal
</output>

## Truths — CONTEXT.md decisions addressed by this plan

This plan implements the following locked decisions from `42-CONTEXT.md`. Citation here satisfies the decision-coverage gate; the concrete implementation lives in the tasks above.

- **D-16**: Test gates per Phase 38/40/41 precedent (3 waves: infra → reskin → gates with visual baselines + axe + size-limit) — implemented per the locked decision (resolutions R-01..R-05 honored where applicable).
- **D-18**: Touch targets ≥44×44px for checkboxes, CTAs, table rows, card click targets, settings nav pills — implemented per the locked decision (resolutions R-01..R-05 honored where applicable).
