---
phase: 42-remaining-pages
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/i18n/en/briefs-page.json
  - frontend/src/i18n/ar/briefs-page.json
  - frontend/src/i18n/en/after-actions-page.json
  - frontend/src/i18n/ar/after-actions-page.json
  - frontend/src/i18n/en/tasks-page.json
  - frontend/src/i18n/ar/tasks-page.json
  - frontend/src/i18n/en/activity-feed.json
  - frontend/src/i18n/ar/activity-feed.json
  - frontend/src/i18n/en/settings.json
  - frontend/src/i18n/ar/settings.json
  - frontend/src/i18n/index.ts
  - frontend/tests/unit/i18n/phase-42-i18n-parity.test.ts
autonomous: true
requirements:
  - PAGE-01
  - PAGE-02
  - PAGE-03
  - PAGE-04
  - PAGE-05
tags:
  - phase-42
  - i18n
  - bilingual
  - wave-0
must_haves:
  truths:
    - 'Three NEW namespaces exist as JSON file pairs (EN+AR): briefs-page, after-actions-page, tasks-page'
    - 'Two EXISTING namespaces extended with new keys (EN+AR): activity-feed (events.* templates), settings (nav rename + appearance.* labels)'
    - 'All new namespaces registered in frontend/src/i18n/index.ts via static imports'
    - 'Every EN key has an AR sibling at the same dot-path (parity verified by automated test)'
    - 'Full top-level parity drift-guard covers ALL 5 namespaces at the root (including settings.errors.* and any future top-level blocks) — not just the named sub-blocks (events.*, nav.*, appearance.*)'
    - "No marketing voice (no 'Discover', 'Easily', 'Unleash', exclamation marks); sentence case for titles and buttons"
  artifacts:
    - path: frontend/src/i18n/en/briefs-page.json
      provides: 'Briefs page EN copy (title, cta.newBrief, card.pages, card.byAuthor, status.{ready,awaiting,review,draft}, empty.heading, error.list)'
    - path: frontend/src/i18n/ar/briefs-page.json
      provides: 'Briefs page AR copy (parity)'
    - path: frontend/src/i18n/en/after-actions-page.json
      provides: 'After-actions EN copy (title, columns.{engagement,date,dossier,decisions,commitments}, empty.heading, error.list)'
    - path: frontend/src/i18n/ar/after-actions-page.json
      provides: 'After-actions AR copy'
    - path: frontend/src/i18n/en/tasks-page.json
      provides: 'Tasks EN copy (title, subtitle, cta.newTask, tabs.{assigned,contributed}, priority.{low,medium,high,urgent}, empty.heading, error.list, error.update)'
    - path: frontend/src/i18n/ar/tasks-page.json
      provides: 'Tasks AR copy'
  key_links:
    - from: frontend/src/i18n/index.ts
      to: frontend/src/i18n/en/briefs-page.json
      via: 'static import + resources registration'
      pattern: 'briefs-page'
---

<objective>
Ship the i18n infrastructure for all 5 Wave 1 page reskins. Creates 3 NEW namespace pairs (briefs-page, after-actions-page, tasks-page) and extends 2 EXISTING namespaces (activity-feed, settings) with bilingual EN+AR keys. Registers all new namespaces in `frontend/src/i18n/index.ts`. Adds an automated parity drift-guard test that fails when any EN key lacks its AR sibling — including a full top-level pass over every namespace so newly added or shipped sub-blocks (e.g., `settings.errors.*`) cannot drift silently.

Purpose: Wave 1 plans 05–09 reference `t('briefs-page.title')` etc. — those keys must resolve at runtime, not return the literal key string. Per RESEARCH Pitfall 9, the canonical i18n source is `frontend/src/i18n/{en,ar}/` (NOT `frontend/public/locales/`). All Activity templates per D-14 + RESEARCH §"Activity event_type / action_type enum" must be authored.

Output: 10 JSON files (5 namespaces × EN+AR), 1 modified `index.ts`, 1 new parity test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-remaining-pages/42-CONTEXT.md
@.planning/phases/42-remaining-pages/42-RESEARCH.md
@.planning/phases/42-remaining-pages/42-UI-SPEC.md
@frontend/src/i18n/index.ts
@frontend/src/i18n/en/activity-feed.json
@frontend/src/i18n/en/settings.json
@frontend/src/i18n/en/list-pages.json

<interfaces>
<!-- Existing static-import pattern. Extend at the same depth/style. -->

From frontend/src/i18n/index.ts (current pattern, lines 4-30):

```ts
import enListPages from './en/list-pages.json'
import arListPages from './ar/list-pages.json'
// ... later in resources block:
en: { ..., 'list-pages': enListPages, ... }
ar: { ..., 'list-pages': arListPages, ... }
```

From RESEARCH §i18n Strategy:

- briefs-page: NEW
- after-actions-page: NEW
- tasks-page: NEW
- activity-feed: EXTEND with `events.{create,update,comment,upload,approval,status_change,_default}` (per D-14 reduced set)
- settings: EXTEND with `nav.accessAndSecurity` rename + appearance.\* labels for Phase 33/34 design controls

From RESEARCH §"Activity event_type / action_type enum" — handoff icon mapping (informational; only the keys EXIST in i18n, the icon mapping is in code):

- 6 primary keys: approval, create, comment, upload, update, status_change
- 1 fallback key: \_default
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Author 3 NEW namespace pairs + extend 2 EXISTING namespaces</name>
  <files>frontend/src/i18n/en/briefs-page.json, frontend/src/i18n/ar/briefs-page.json, frontend/src/i18n/en/after-actions-page.json, frontend/src/i18n/ar/after-actions-page.json, frontend/src/i18n/en/tasks-page.json, frontend/src/i18n/ar/tasks-page.json, frontend/src/i18n/en/activity-feed.json, frontend/src/i18n/ar/activity-feed.json, frontend/src/i18n/en/settings.json, frontend/src/i18n/ar/settings.json</files>
  <read_first>
    - frontend/src/i18n/en/list-pages.json (analog NEW namespace shape — exact JSON formatting + key style)
    - frontend/src/i18n/en/activity-feed.json (full file — Read first, append events.* without rewriting existing keys)
    - frontend/src/i18n/en/settings.json (full file — Read first, append nav.* + appearance.* without rewriting)
    - frontend/src/i18n/ar/activity-feed.json (full file — must mirror new EN keys at the same paths)
    - frontend/src/i18n/ar/settings.json (full file — must mirror new EN keys)
    - .planning/phases/42-remaining-pages/42-RESEARCH.md §"i18n Strategy" (canonical key list per namespace)
    - .planning/phases/42-remaining-pages/42-UI-SPEC.md §"Copywriting Contract" (locked CTA labels, empty/error copy)
  </read_first>
  <action>

**Part A — Create `frontend/src/i18n/en/briefs-page.json`:**

```json
{
  "title": "Briefs",
  "subtitle": "Captured intelligence summaries and AI-generated briefs.",
  "cta": {
    "newBrief": "New brief"
  },
  "card": {
    "pages": "{{count}} pp",
    "byAuthor": "By {{name}}",
    "approxLengthHint": "Approximate length"
  },
  "status": {
    "ready": "Ready",
    "awaiting": "Awaiting",
    "review": "Review",
    "draft": "Draft"
  },
  "empty": {
    "heading": "No briefs yet"
  },
  "error": {
    "list": "Could not load briefs. Try refreshing the page.",
    "generation": "Brief generation failed. Try again or contact support."
  }
}
```

**Part B — Create `frontend/src/i18n/ar/briefs-page.json`:** Mirror Part A with these exact AR strings:

```json
{
  "title": "الملخصات",
  "subtitle": "ملخصات الاستخبارات الموثقة وملخصات تم إنشاؤها بالذكاء الاصطناعي.",
  "cta": {
    "newBrief": "ملخص جديد"
  },
  "card": {
    "pages": "{{count}} صفحة",
    "byAuthor": "بواسطة {{name}}",
    "approxLengthHint": "طول تقريبي"
  },
  "status": {
    "ready": "جاهز",
    "awaiting": "قيد الانتظار",
    "review": "قيد المراجعة",
    "draft": "مسودة"
  },
  "empty": {
    "heading": "لا توجد ملخصات بعد"
  },
  "error": {
    "list": "تعذر تحميل الملخصات. حاول تحديث الصفحة.",
    "generation": "فشل إنشاء الملخص. حاول مرة أخرى أو تواصل مع الدعم."
  }
}
```

**Part C — Create `frontend/src/i18n/en/after-actions-page.json`:**

```json
{
  "title": "After-actions",
  "subtitle": "Captured decisions, commitments, and notes from past engagements.",
  "columns": {
    "engagement": "ENGAGEMENT",
    "date": "DATE",
    "dossier": "DOSSIER",
    "decisions": "DECISIONS",
    "commitments": "COMMITMENTS"
  },
  "empty": {
    "heading": "No after-action records yet"
  },
  "error": {
    "list": "Could not load after-action records. Try refreshing the page."
  }
}
```

**Part D — Create `frontend/src/i18n/ar/after-actions-page.json`:**

```json
{
  "title": "ما بعد الإجراء",
  "subtitle": "القرارات والالتزامات والملاحظات الموثقة من المشاركات السابقة.",
  "columns": {
    "engagement": "المشاركة",
    "date": "التاريخ",
    "dossier": "الملف",
    "decisions": "القرارات",
    "commitments": "الالتزامات"
  },
  "empty": {
    "heading": "لا توجد سجلات بعد"
  },
  "error": {
    "list": "تعذر تحميل سجلات ما بعد الإجراء. حاول تحديث الصفحة."
  }
}
```

**Part E — Create `frontend/src/i18n/en/tasks-page.json`:**

```json
{
  "title": "My desk",
  "subtitle": "Tasks assigned to you and tasks you contribute to.",
  "cta": {
    "newTask": "New task"
  },
  "tabs": {
    "assigned": "Assigned",
    "contributed": "Contributed"
  },
  "priority": {
    "low": "Low",
    "medium": "Medium",
    "high": "High",
    "urgent": "Urgent"
  },
  "due": {
    "today": "Today",
    "overdue": "{{count}}d overdue"
  },
  "empty": {
    "heading": "No tasks on your desk"
  },
  "error": {
    "list": "Could not load tasks. Try refreshing the page.",
    "update": "Could not update task. Changes were reverted."
  }
}
```

**Part F — Create `frontend/src/i18n/ar/tasks-page.json`:**

```json
{
  "title": "مكتبي",
  "subtitle": "المهام المسندة إليك والمهام التي تساهم فيها.",
  "cta": {
    "newTask": "مهمة جديدة"
  },
  "tabs": {
    "assigned": "مسند إليّ",
    "contributed": "مساهمات"
  },
  "priority": {
    "low": "منخفضة",
    "medium": "متوسطة",
    "high": "عالية",
    "urgent": "عاجلة"
  },
  "due": {
    "today": "اليوم",
    "overdue": "متأخرة {{count}} يوم"
  },
  "empty": {
    "heading": "لا توجد مهام على مكتبك"
  },
  "error": {
    "list": "تعذر تحميل المهام. حاول تحديث الصفحة.",
    "update": "تعذر تحديث المهمة. تم التراجع عن التغييرات."
  }
}
```

**Part G — Extend `frontend/src/i18n/en/activity-feed.json` (do NOT rewrite the file — Read first, then add an `events` block):**

Open `frontend/src/i18n/en/activity-feed.json` and add (or merge into existing top-level) these keys:

```json
{
  "events": {
    "approval": "approved <entity>{{entity}}</entity> in <where>{{where}}</where>",
    "rejection": "rejected <entity>{{entity}}</entity> in <where>{{where}}</where>",
    "create": "created <entity>{{entity}}</entity> in <where>{{where}}</where>",
    "update": "updated <entity>{{entity}}</entity> in <where>{{where}}</where>",
    "delete": "deleted <entity>{{entity}}</entity> in <where>{{where}}</where>",
    "comment": "commented on <entity>{{entity}}</entity> in <where>{{where}}</where>",
    "mention": "mentioned <entity>{{entity}}</entity> in <where>{{where}}</where>",
    "upload": "uploaded <entity>{{entity}}</entity> in <where>{{where}}</where>",
    "download": "downloaded <entity>{{entity}}</entity> in <where>{{where}}</where>",
    "view": "viewed <entity>{{entity}}</entity> in <where>{{where}}</where>",
    "share": "shared <entity>{{entity}}</entity> in <where>{{where}}</where>",
    "assign": "assigned <entity>{{entity}}</entity> in <where>{{where}}</where>",
    "status_change": "changed status on <entity>{{entity}}</entity> in <where>{{where}}</where>",
    "archive": "archived <entity>{{entity}}</entity> in <where>{{where}}</where>",
    "restore": "restored <entity>{{entity}}</entity> in <where>{{where}}</where>",
    "_default": "acted on <entity>{{entity}}</entity> in <where>{{where}}</where>"
  },
  "tabs": {
    "all": "All",
    "following": "Following"
  },
  "empty": {
    "all": "No activity yet",
    "following": "Follow entities to see their activity"
  },
  "errorList": "Could not load activity. Try refreshing the page."
}
```

Preserve all existing keys in the file. If a top-level `events` key already exists, merge the new sub-keys into it (do not overwrite siblings).

**Part H — Extend `frontend/src/i18n/ar/activity-feed.json`** with mirrored AR strings (`<entity>` and `<where>` markers stay as-is — they are i18next Trans component placeholders, locale-independent):

```json
{
  "events": {
    "approval": "وافق على <entity>{{entity}}</entity> في <where>{{where}}</where>",
    "rejection": "رفض <entity>{{entity}}</entity> في <where>{{where}}</where>",
    "create": "أنشأ <entity>{{entity}}</entity> في <where>{{where}}</where>",
    "update": "حدّث <entity>{{entity}}</entity> في <where>{{where}}</where>",
    "delete": "حذف <entity>{{entity}}</entity> في <where>{{where}}</where>",
    "comment": "علّق على <entity>{{entity}}</entity> في <where>{{where}}</where>",
    "mention": "أشار إلى <entity>{{entity}}</entity> في <where>{{where}}</where>",
    "upload": "رفع <entity>{{entity}}</entity> في <where>{{where}}</where>",
    "download": "نزّل <entity>{{entity}}</entity> من <where>{{where}}</where>",
    "view": "اطلع على <entity>{{entity}}</entity> في <where>{{where}}</where>",
    "share": "شارك <entity>{{entity}}</entity> في <where>{{where}}</where>",
    "assign": "أسند <entity>{{entity}}</entity> في <where>{{where}}</where>",
    "status_change": "غيّر حالة <entity>{{entity}}</entity> في <where>{{where}}</where>",
    "archive": "أرشف <entity>{{entity}}</entity> في <where>{{where}}</where>",
    "restore": "استعاد <entity>{{entity}}</entity> في <where>{{where}}</where>",
    "_default": "تفاعل مع <entity>{{entity}}</entity> في <where>{{where}}</where>"
  },
  "tabs": {
    "all": "الكل",
    "following": "متابَع"
  },
  "empty": {
    "all": "لا يوجد نشاط بعد",
    "following": "تابع الكيانات لمشاهدة نشاطها"
  },
  "errorList": "تعذر تحميل النشاط. حاول تحديث الصفحة."
}
```

**Part I — Extend `frontend/src/i18n/en/settings.json`** with the nav rename + appearance design control labels. READ THE FILE FIRST, then add/merge:

```json
{
  "nav": {
    "profile": "Profile",
    "general": "General",
    "appearance": "Appearance",
    "notifications": "Notifications",
    "accessAndSecurity": "Access & Security",
    "accessibility": "Accessibility",
    "dataPrivacy": "Data & Privacy",
    "emailDigest": "Email Digest",
    "integrations": "Integrations"
  },
  "appearance": {
    "direction": {
      "label": "Design direction",
      "help": "Choose the visual character.",
      "bureau": "Bureau",
      "chancery": "Chancery",
      "situation": "Situation Room",
      "ministerial": "Ministerial"
    },
    "mode": {
      "label": "Mode",
      "light": "Light",
      "dark": "Dark"
    },
    "hue": {
      "label": "Accent hue",
      "help": "0–360°"
    },
    "density": {
      "label": "Density",
      "comfortable": "Comfortable",
      "compact": "Compact",
      "dense": "Dense"
    }
  },
  "errors": {
    "save": "Could not save changes. Review the highlighted fields and try again."
  }
}
```

If existing `nav.security` key exists in the file, leave it (do NOT delete) — Wave 1 plan 09 will reference `nav.accessAndSecurity` in the new SettingsLayout; the legacy `nav.security` key remains for any existing callers and can be cleaned up in Phase 43.

**Part J — Extend `frontend/src/i18n/ar/settings.json`** with mirrored AR:

```json
{
  "nav": {
    "profile": "الملف الشخصي",
    "general": "عام",
    "appearance": "المظهر",
    "notifications": "الإشعارات",
    "accessAndSecurity": "الوصول والأمان",
    "accessibility": "إمكانية الوصول",
    "dataPrivacy": "البيانات والخصوصية",
    "emailDigest": "ملخص البريد",
    "integrations": "التكاملات"
  },
  "appearance": {
    "direction": {
      "label": "اتجاه التصميم",
      "help": "اختر الطابع البصري.",
      "bureau": "Bureau",
      "chancery": "Chancery",
      "situation": "Situation Room",
      "ministerial": "Ministerial"
    },
    "mode": {
      "label": "النمط",
      "light": "فاتح",
      "dark": "داكن"
    },
    "hue": {
      "label": "درجة اللون",
      "help": "0–360°"
    },
    "density": {
      "label": "الكثافة",
      "comfortable": "مريح",
      "compact": "مدمج",
      "dense": "كثيف"
    }
  },
  "errors": {
    "save": "تعذر حفظ التغييرات. راجع الحقول المحددة وحاول مرة أخرى."
  }
}
```

Constraints (CLAUDE.md voice rules):

- No marketing voice ("Discover", "Easily", "Unleash" banned)
- No exclamation marks
- No emoji in user-visible copy
- Sentence case for titles and CTAs (NOT title case)
- UPPERCASE only for table column headers (per UI-SPEC) and classification ribbons
- Direction names ("Bureau", "Chancery", "Situation Room", "Ministerial") stay as proper nouns in both locales
  </action>
  <verify>
  <automated>node -e "for (const f of ['en/briefs-page','ar/briefs-page','en/after-actions-page','ar/after-actions-page','en/tasks-page','ar/tasks-page']) { JSON.parse(require('fs').readFileSync('frontend/src/i18n/'+f+'.json','utf8')); } console.log('OK')"</automated>
  </verify>
  <done>All 10 JSON files exist and parse as valid JSON; new top-level namespace files have at least the documented keys; activity-feed and settings preserve their pre-existing keys.</done>
  </task>

<task type="auto">
  <name>Task 2: Register new namespaces in frontend/src/i18n/index.ts</name>
  <files>frontend/src/i18n/index.ts</files>
  <read_first>
    - frontend/src/i18n/index.ts (full file — locate the import block at lines 4-80 and the `resources` registration block; append, do NOT rewrite)
  </read_first>
  <action>
Open `frontend/src/i18n/index.ts`. Locate the existing import block (around lines 4-80). After the last `import ... from './ar/...json'` line, append:

```ts
import enBriefsPage from './en/briefs-page.json'
import arBriefsPage from './ar/briefs-page.json'
import enAfterActionsPage from './en/after-actions-page.json'
import arAfterActionsPage from './ar/after-actions-page.json'
import enTasksPage from './en/tasks-page.json'
import arTasksPage from './ar/tasks-page.json'
```

Locate the `resources` object (further down — search for `en: {` and `ar: {`). Inside the `en: { ... }` block, add the three new entries (and ensure the existing `'activity-feed'` and `settings` namespaces are present — they should already be):

```ts
en: {
  // ... existing entries ...
  'briefs-page': enBriefsPage,
  'after-actions-page': enAfterActionsPage,
  'tasks-page': enTasksPage,
}
```

Inside the `ar: { ... }` block, mirror the three new entries:

```ts
ar: {
  // ... existing entries ...
  'briefs-page': arBriefsPage,
  'after-actions-page': arAfterActionsPage,
  'tasks-page': arTasksPage,
}
```

Do NOT remove or reorder any existing imports or registrations. Do NOT change the file's overall structure.

If `i18n.options.ns` (whitelist of namespace names) is explicitly set somewhere in the file, also append `'briefs-page'`, `'after-actions-page'`, `'tasks-page'` to that array.
</action>
<verify>
<automated>grep -c "briefs-page" frontend/src/i18n/index.ts | grep -v "^0$" && grep -c "after-actions-page" frontend/src/i18n/index.ts | grep -v "^0$" && grep -c "tasks-page" frontend/src/i18n/index.ts | grep -v "^0$" && pnpm --filter frontend tsc --noEmit 2>&1 | grep -c "i18n/index.ts" | grep -E "^0$"</automated>
</verify>
<done>All 3 new namespaces imported and registered in both `en` and `ar` resource blocks; zero new TS errors on `i18n/index.ts`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author EN/AR parity drift-guard test</name>
  <files>frontend/tests/unit/i18n/phase-42-i18n-parity.test.ts</files>
  <read_first>
    - frontend/src/i18n/en/briefs-page.json (just authored — extract key paths)
    - frontend/src/i18n/ar/briefs-page.json (mirrors EN — should match key-for-key)
    - frontend/tests/unit/ (look for existing parity-style drift tests; if any, model the new test on them)
  </read_first>
  <behavior>
    - Test 1: Every key in `en/briefs-page.json` has a sibling at the same dot-path in `ar/briefs-page.json` (and vice versa)
    - Test 2: Same parity check for `en/after-actions-page.json` ↔ `ar/after-actions-page.json`
    - Test 3: Same parity check for `en/tasks-page.json` ↔ `ar/tasks-page.json`
    - Test 4: For `events.*` block in `activity-feed.json`, every EN event key has an AR sibling
    - Test 5: For `nav.*` and `appearance.*` blocks in `settings.json`, every EN key has an AR sibling
    - Test 6 (full top-level drift-guard): For ALL 5 namespaces (briefs-page, after-actions-page, tasks-page, activity-page/activity-feed, settings-page/settings), assert FULL parity at the ROOT (no namespace path filter). This catches any future top-level block (e.g. `settings.errors.*`) that drifts because Tests 1–5 only inspect named sub-blocks. Implementation: helper `function assertFullParity(en, ar, ns) { const enKeys = flattenKeys(en).sort(); const arKeys = flattenKeys(ar).sort(); expect(arKeys, ns + ' AR keys').toEqual(enKeys); }` and call it once per namespace pair.
  </behavior>
  <action>
Create `frontend/tests/unit/i18n/phase-42-i18n-parity.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import enBriefs from '@/i18n/en/briefs-page.json'
import arBriefs from '@/i18n/ar/briefs-page.json'
import enAA from '@/i18n/en/after-actions-page.json'
import arAA from '@/i18n/ar/after-actions-page.json'
import enTasks from '@/i18n/en/tasks-page.json'
import arTasks from '@/i18n/ar/tasks-page.json'
import enActivity from '@/i18n/en/activity-feed.json'
import arActivity from '@/i18n/ar/activity-feed.json'
import enSettings from '@/i18n/en/settings.json'
import arSettings from '@/i18n/ar/settings.json'

function flattenKeys(obj: Record<string, unknown>, prefix = ''): string[] {
  const out: string[] = []
  for (const [k, v] of Object.entries(obj)) {
    const path = prefix ? `${prefix}.${k}` : k
    if (v && typeof v === 'object' && !Array.isArray(v)) {
      out.push(...flattenKeys(v as Record<string, unknown>, path))
    } else {
      out.push(path)
    }
  }
  return out
}

function assertParity(en: Record<string, unknown>, ar: Record<string, unknown>, ns: string): void {
  const enKeys = new Set(flattenKeys(en))
  const arKeys = new Set(flattenKeys(ar))
  const missingInAr = [...enKeys].filter((k) => !arKeys.has(k))
  const extraInAr = [...arKeys].filter((k) => !enKeys.has(k))
  expect(missingInAr, `${ns}: keys missing in AR`).toEqual([])
  expect(extraInAr, `${ns}: keys missing in EN`).toEqual([])
}

// Full top-level drift-guard helper. Compares ALL keys at the ROOT of the namespace,
// catching new sibling blocks (e.g. `settings.errors.*`, future `tasks-page.toast.*`)
// that the targeted sub-block tests above (Tests 4–5) intentionally skip.
function assertFullParity(
  en: Record<string, unknown>,
  ar: Record<string, unknown>,
  ns: string,
): void {
  const enKeys = flattenKeys(en).sort()
  const arKeys = flattenKeys(ar).sort()
  expect(arKeys, `${ns} AR keys`).toEqual(enKeys)
}

describe('Phase 42 i18n parity', () => {
  it('briefs-page EN ↔ AR parity', () => assertParity(enBriefs, arBriefs, 'briefs-page'))
  it('after-actions-page EN ↔ AR parity', () => assertParity(enAA, arAA, 'after-actions-page'))
  it('tasks-page EN ↔ AR parity', () => assertParity(enTasks, arTasks, 'tasks-page'))
  it('activity-feed events.* EN ↔ AR parity', () => {
    const enEvents = (enActivity as { events?: Record<string, unknown> }).events ?? {}
    const arEvents = (arActivity as { events?: Record<string, unknown> }).events ?? {}
    assertParity(enEvents, arEvents, 'activity-feed.events')
  })
  it('settings nav.* + appearance.* EN ↔ AR parity', () => {
    const enNav = (enSettings as { nav?: Record<string, unknown> }).nav ?? {}
    const arNav = (arSettings as { nav?: Record<string, unknown> }).nav ?? {}
    const enApp = (enSettings as { appearance?: Record<string, unknown> }).appearance ?? {}
    const arApp = (arSettings as { appearance?: Record<string, unknown> }).appearance ?? {}
    assertParity(enNav, arNav, 'settings.nav')
    assertParity(enApp, arApp, 'settings.appearance')
  })
  it('Test 6: Full top-level parity drift-guard for all 5 namespaces', () => {
    // Loads each namespace pair (en + ar) and calls assertFullParity at the ROOT
    // of each — no namespace path filter. Any future top-level block (e.g.
    // settings.errors.*, tasks-page.toast.*, activity-feed.banner.*) that drifts
    // between EN and AR will fail this check.
    assertFullParity(
      enBriefs as Record<string, unknown>,
      arBriefs as Record<string, unknown>,
      'briefs-page',
    )
    assertFullParity(
      enAA as Record<string, unknown>,
      arAA as Record<string, unknown>,
      'after-actions-page',
    )
    assertFullParity(
      enTasks as Record<string, unknown>,
      arTasks as Record<string, unknown>,
      'tasks-page',
    )
    assertFullParity(
      enActivity as Record<string, unknown>,
      arActivity as Record<string, unknown>,
      'activity-feed (activity-page)',
    )
    assertFullParity(
      enSettings as Record<string, unknown>,
      arSettings as Record<string, unknown>,
      'settings (settings-page)',
    )
  })
})
```

Acceptance criteria for this file:

- `grep "assertFullParity\|Full top-level parity" tests/i18n/phase-42-i18n-parity.test.ts` returns ≥ 1 match (verifies the drift-guard helper + Test 6 exist). The test file path is `frontend/tests/unit/i18n/phase-42-i18n-parity.test.ts`; if the verify script is invoked from a different cwd, adjust the path accordingly.
  </action>
  <verify>
  <automated>grep -c "assertFullParity\|Full top-level parity" frontend/tests/unit/i18n/phase-42-i18n-parity.test.ts | grep -v "^0$" && pnpm --filter frontend test --run --reporter=verbose phase-42-i18n-parity</automated>
  </verify>
  <done>6/6 parity tests PASS; sub-block tests + full top-level drift-guard both green; any future drift between EN and AR for these namespaces (including new sibling blocks like `settings.errors.*`) fails the suite.</done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                        |
| -------- | ---------------------------------------------------------------------------------- |
| (none)   | i18n JSON files are static build-time artifacts; no runtime untrusted input parsed |

## STRIDE Threat Register

| Threat ID         | Category               | Component                   | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                 |
| ----------------- | ---------------------- | --------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-42-02-XSS-1     | Tampering              | i18next Trans interpolation | accept      | i18next + React's default escaping prevents HTML injection through `{{entity}}` and `{{where}}` interpolations. The `<entity>` / `<where>` markers are parsed by the i18next Trans component, which maps them to React components — not raw `dangerouslySetInnerHTML`. No mitigation beyond not using `dangerouslySetInnerHTML` anywhere downstream (verified in Wave 1 plans). |
| T-42-02-CONTENT-1 | Information Disclosure | Bilingual copy fidelity     | accept      | All copy authored from CONTEXT.md and UI-SPEC source-of-truth strings; no PII embedded. AR translations follow the same noun/verb structure as EN.                                                                                                                                                                                                                              |

</threat_model>

<verification>
- 10 JSON files exist and parse as valid JSON
- `grep -c '"events"' frontend/src/i18n/en/activity-feed.json` ≥ 1
- `grep -c '"appearance"' frontend/src/i18n/en/settings.json` ≥ 1
- `grep -c "briefs-page" frontend/src/i18n/index.ts` ≥ 4 (2 imports + 2 registrations)
- `grep -c "assertFullParity\|Full top-level parity" frontend/tests/unit/i18n/phase-42-i18n-parity.test.ts` ≥ 1
- `pnpm --filter frontend test --run --reporter=verbose phase-42-i18n-parity` → 6/6 PASS
- No raw hex, no emoji, no marketing voice (manual scan during code review)
</verification>

<success_criteria>

- 3 NEW namespace pairs authored with locked CTA + empty/error copy from UI-SPEC
- 2 EXISTING namespaces extended with new keys (preserving prior content)
- index.ts registrations + parity drift-guard suite green
- Full top-level drift-guard (Test 6) covers ALL 5 namespaces — no silent EN/AR drift on shipped or future sibling blocks (e.g., `settings.errors.*`)
- Wave 1 plans 05–09 can call `useTranslation('briefs-page')` etc. and `t('briefs-page.title')` resolves to "Briefs" (EN) / "الملخصات" (AR)
  </success_criteria>

<output>
After completion, create `.planning/phases/42-remaining-pages/42-02-SUMMARY.md` documenting:
- 10 JSON file paths + line counts
- Total new keys added per namespace
- Parity test results (6/6 PASS expected; including Test 6 full top-level drift-guard)
- Any deviation from CONTEXT.md / UI-SPEC voice rules
</output>

## Truths — CONTEXT.md decisions addressed by this plan

This plan implements the following locked decisions from `42-CONTEXT.md`. Citation here satisfies the decision-coverage gate; the concrete implementation lives in the tasks above.

- **D-14**: Activity row composition: bilingual i18n templates per action_type with who/action/what/where slots and the handoff icon map — implemented per the locked decision (resolutions R-01..R-05 honored where applicable).
