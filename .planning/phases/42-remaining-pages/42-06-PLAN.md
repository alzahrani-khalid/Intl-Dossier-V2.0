---
phase: 42-remaining-pages
plan: 06
type: execute
wave: 1
depends_on:
  - 42-00
  - 42-01
  - 42-02
  - 42-03
  - 42-04
files_modified:
  - frontend/src/routes/_protected/after-actions/index.tsx
  - frontend/src/components/after-actions/AfterActionsTable.tsx
  - frontend/src/components/after-actions/__tests__/AfterActionsTable.test.tsx
  - frontend/tests/e2e/after-actions-page.spec.ts
autonomous: true
requirements:
  - PAGE-02
tags:
  - phase-42
  - after-actions
  - page-reskin
  - wave-1
must_haves:
  truths:
    - "User sees a `<table class='tbl'>` on /after-actions with 6 columns: Engagement / Date / Dossier chip / Decisions / Commitments / chevron"
    - 'Engagement title rendered with `font-weight: 500`; Date rendered in mono `var(--font-mono)` `var(--ink-mute)` using `engagement.engagement_date` per R-04'
    - 'Dossier chip uses `var(--accent-soft)` background + `var(--accent-ink)` text'
    - 'Decisions and commitments counts use `.length` of the joined arrays (id-only payloads from Edge Function), rendered in mono and end-aligned'
    - "Row chevron uses `<Icon name='chevron-right' className='icon-flip' />` so it flips via `scaleX(-1)` in RTL"
    - 'Whole row is keyboard-focusable (`tabIndex=0`); Enter activates; click navigates to `/after-actions/$id`'
    - 'Hook used: `useAfterActionsAll()` from `@/hooks/useAfterAction` (NOT the per-dossier `useAfterActions`)'
    - "Empty state: 'No after-action records yet' / 'لا توجد سجلات بعد'"
    - 'Section root emits `data-loading` attribute'
  artifacts:
    - path: frontend/src/routes/_protected/after-actions/index.tsx
      provides: 'Reskinned After-actions list route → renders <AfterActionsTable/>'
    - path: frontend/src/components/after-actions/AfterActionsTable.tsx
      provides: 'New presentation component for the handoff `.tbl` rendering'
      min_lines: 100
    - path: frontend/src/components/after-actions/__tests__/AfterActionsTable.test.tsx
      provides: 'Vitest unit suite (column count + row click + chip + chevron-flip + empty state)'
  key_links:
    - from: frontend/src/routes/_protected/after-actions/index.tsx
      to: frontend/src/hooks/useAfterAction.ts
      via: 'useAfterActionsAll() invocation'
      pattern: 'useAfterActionsAll'
    - from: frontend/src/components/after-actions/AfterActionsTable.tsx
      to: frontend/src/components/signature-visuals/Icon.tsx
      via: "<Icon name='chevron-right' className='icon-flip' />"
      pattern: 'icon-flip'
---

<objective>
Reskin the After-actions list route (`/after-actions`) to the IntelDossier handoff `.tbl` table per UI-SPEC §"PAGE-02 After-actions". Wires the new `useAfterActionsAll` hook (from Wave 0 plan 01) into a verbatim `.tbl` 6-column anatomy.

Strips:

- Existing stub-CTA layout (currently a placeholder route)

Adds:

- `<PageHeader/>` "After-actions" — no CTA per D-04
- New `<AfterActionsTable/>` presentation component with 6 columns
- Row click → existing `/after-actions/$afterActionId` detail route (preserved)
- Empty state heading
- Row-shape skeleton during load

Output: 1 modified route, 1 new component, 1 new vitest suite, 1 un-skipped Playwright functional spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-remaining-pages/42-CONTEXT.md
@.planning/phases/42-remaining-pages/42-RESEARCH.md
@.planning/phases/42-remaining-pages/42-PATTERNS.md
@.planning/phases/42-remaining-pages/42-UI-SPEC.md
@frontend/src/routes/_protected/after-actions/index.tsx
@frontend/src/components/list-page/ListPageShell.tsx
@frontend/src/hooks/useAfterAction.ts
@frontend/design-system/inteldossier_handoff_design/src/pages.jsx
@.planning/phases/42-remaining-pages/42-00-SUMMARY.md
@.planning/phases/42-remaining-pages/42-01-SUMMARY.md
@.planning/phases/42-remaining-pages/42-02-SUMMARY.md
@.planning/phases/42-remaining-pages/42-03-SUMMARY.md
@.planning/phases/42-remaining-pages/42-04-SUMMARY.md

<interfaces>
After Wave 0 plan 01 lands:
```ts
export function useAfterActionsAll(options?: {
  status?: 'draft' | 'published' | 'edit_requested' | 'edit_approved'
  limit?: number
  offset?: number
}): UseQueryResult<{ data: AfterActionRecordWithJoins[]; total: number }, Error>

export interface AfterActionRecordWithJoins extends AfterActionRecord {
engagement?: { id: string; title_en: string; title_ar: string; engagement_date: string }
dossier?: { id: string; name_en: string; name_ar: string }
}

````

After Wave 0 plan 00:
```ts
import { Icon } from '@/components/signature-visuals'
// supports name='chevron-right'
````

After Wave 0 plan 03 (CSS):

- `.tbl`, `.icon-flip`, `.chip`, `.page` all available

Verbatim handoff anatomy (PATTERNS.md §after-actions):

```tsx
<table className="tbl">
  <thead><tr>
    <th className="text-start">{t('columns.engagement')}</th>
    <th className="text-start">{t('columns.date')}</th>
    <th className="text-start">{t('columns.dossier')}</th>
    <th className="text-end">{t('columns.decisions')}</th>
    <th className="text-end">{t('columns.commitments')}</th>
    <th aria-label="" />
  </tr></thead>
  <tbody>
    {rows.map(r => (
      <tr key={r.id} tabIndex={0} role="button" onClick={...} onKeyDown={...}>
        <td style={{ fontWeight: 500 }}>{locale==='ar' ? r.engagement?.title_ar : r.engagement?.title_en}</td>
        <td style={{ fontFamily: 'var(--font-mono)', color: 'var(--ink-mute)' }} dir="ltr">{formatDayFirst(r.engagement?.engagement_date)}</td>
        <td><span className="chip" style={{ background: 'var(--accent-soft)', color: 'var(--accent-ink)' }}>{locale==='ar' ? r.dossier?.name_ar : r.dossier?.name_en}</span></td>
        <td className="text-end" style={{ fontFamily: 'var(--font-mono)' }}>{toArDigits(r.decisions?.length ?? 0, locale)}</td>
        <td className="text-end" style={{ fontFamily: 'var(--font-mono)' }}>{toArDigits(r.commitments?.length ?? 0, locale)}</td>
        <td><Icon name="chevron-right" size={16} className="icon-flip" /></td>
      </tr>
    ))}
  </tbody>
</table>
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author AfterActionsTable presentation component</name>
  <files>frontend/src/components/after-actions/AfterActionsTable.tsx, frontend/src/components/after-actions/__tests__/AfterActionsTable.test.tsx</files>
  <read_first>
    - frontend/src/hooks/useAfterAction.ts (post-Plan-01: confirm `useAfterActionsAll` signature + `AfterActionRecordWithJoins` shape)
    - frontend/src/components/list-page/ListPageShell.tsx (analog for `data-loading` ready marker)
    - frontend/design-system/inteldossier_handoff_design/src/pages.jsx (lines 331-365 — `.tbl` anatomy)
    - frontend/src/lib/i18n/toArDigits.ts (signature)
    - .planning/phases/42-remaining-pages/42-PATTERNS.md §"after-actions/index.tsx — REWRITE"
    - .planning/phases/42-remaining-pages/42-UI-SPEC.md §"PAGE-02 After-actions"
  </read_first>
  <behavior>
    - Test 1: Renders a `<table class="tbl">` with exactly 6 `<th>` cells (engagement, date, dossier, decisions, commitments, blank)
    - Test 2: Each row has `tabIndex={0}` and `role="button"` so keyboard navigation works
    - Test 3: Engagement column displays `engagement.title_en` in EN locale and `engagement.title_ar` in AR locale
    - Test 4: Date column displays a day-first format `Tue 28 Apr` from `engagement.engagement_date` (R-04) in mono font with `dir="ltr"`
    - Test 5: Dossier chip has `var(--accent-soft)` background; decisions/commitments counts come from `.length` of arrays
    - Test 6: Last cell renders `<Icon name="chevron-right" className="icon-flip" />` — verify `data-testid="icon-chevron-right"` and `class*="icon-flip"`
    - Test 7: Empty state heading "No after-action records yet" renders when `rows.length === 0`
  </behavior>
  <action>
Create `frontend/src/components/after-actions/AfterActionsTable.tsx`:

```tsx
import type { ReactNode } from 'react'
import { useNavigate } from '@tanstack/react-router'
import { useTranslation } from 'react-i18next'
import { Icon } from '@/components/signature-visuals'
import { toArDigits } from '@/lib/i18n/toArDigits'
import type { AfterActionRecordWithJoins } from '@/hooks/useAfterAction'

interface AfterActionsTableProps {
  rows: AfterActionRecordWithJoins[]
  isLoading: boolean
  error: Error | null
}

export function AfterActionsTable({
  rows,
  isLoading,
  error,
}: AfterActionsTableProps): React.JSX.Element {
  const { t, i18n } = useTranslation('after-actions-page')
  const locale = i18n.language as 'en' | 'ar'
  const isRTL = locale === 'ar'
  const navigate = useNavigate()

  function formatDayFirst(date: string | null | undefined): string {
    if (!date) return '—'
    const d = new Date(date)
    return d.toLocaleDateString('en-GB', { weekday: 'short', day: '2-digit', month: 'short' })
  }

  function navigateToDetail(id: string): void {
    void navigate({ to: '/after-actions/$afterActionId', params: { afterActionId: id } })
  }

  if (error) {
    return (
      <div className="card" role="alert">
        <Icon name="alert" size={16} style={{ color: 'var(--danger)' }} aria-hidden />
        <span className="ms-2">{t('error.list')}</span>
      </div>
    )
  }

  if (isLoading) {
    return (
      <div className="card" data-testid="after-actions-skeleton">
        {Array.from({ length: 8 }).map((_, i) => (
          <div
            key={i}
            className="h-[var(--row-h)] w-full animate-pulse rounded-[var(--radius-sm)] bg-[var(--line-soft)] mb-2"
          />
        ))}
      </div>
    )
  }

  if (rows.length === 0) {
    return (
      <div className="text-center py-12 text-[var(--ink-mute)]">
        <h2 className="text-lg" style={{ fontFamily: 'var(--font-display)' }}>
          {t('empty.heading')}
        </h2>
      </div>
    )
  }

  return (
    <div className="card">
      <table className="tbl" role="table">
        <thead>
          <tr>
            <th className="text-start">{t('columns.engagement')}</th>
            <th className="text-start">{t('columns.date')}</th>
            <th className="text-start">{t('columns.dossier')}</th>
            <th className="text-end">{t('columns.decisions')}</th>
            <th className="text-end">{t('columns.commitments')}</th>
            <th aria-label="" />
          </tr>
        </thead>
        <tbody>
          {rows.map((r) => (
            <tr
              key={r.id}
              data-testid="after-action-row"
              role="button"
              tabIndex={0}
              style={{ cursor: 'pointer', minHeight: 44 }}
              onClick={() => navigateToDetail(r.id)}
              onKeyDown={(e) => {
                if (e.key === 'Enter' || e.key === ' ') navigateToDetail(r.id)
              }}
            >
              <td style={{ fontWeight: 500 }}>
                {locale === 'ar'
                  ? (r.engagement?.title_ar ?? r.engagement?.title_en ?? '—')
                  : (r.engagement?.title_en ?? r.engagement?.title_ar ?? '—')}
              </td>
              <td style={{ fontFamily: 'var(--font-mono)', color: 'var(--ink-mute)' }} dir="ltr">
                {toArDigits(formatDayFirst(r.engagement?.engagement_date), locale)}
              </td>
              <td>
                <span
                  className="chip"
                  style={{ background: 'var(--accent-soft)', color: 'var(--accent-ink)' }}
                >
                  {locale === 'ar' ? (r.dossier?.name_ar ?? '—') : (r.dossier?.name_en ?? '—')}
                </span>
              </td>
              <td className="text-end" style={{ fontFamily: 'var(--font-mono)' }}>
                {toArDigits(r.decisions?.length ?? 0, locale)}
              </td>
              <td className="text-end" style={{ fontFamily: 'var(--font-mono)' }}>
                {toArDigits(r.commitments?.length ?? 0, locale)}
              </td>
              <td>
                <Icon name="chevron-right" size={16} className="icon-flip" aria-hidden />
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  )
}
```

Then create `frontend/src/components/after-actions/__tests__/AfterActionsTable.test.tsx` with 7 tests per the `<behavior>` block. Use the i18n mock pattern from PATTERNS §I. Mock `@tanstack/react-router` `useNavigate` to return a vi.fn() and assert it's called with `{ to: '/after-actions/$afterActionId', params: { afterActionId: 'r1' } }` on row click.

CRITICAL constraints:

- Logical properties only (`text-start`, `text-end`, `ms-*`)
- All colors via `var(--*)` tokens
- 44×44 minimum row height (`minHeight: 44`)
- `dir="ltr"` on the date span ensures the day-first format stays LTR even inside an AR-RTL container (TYPO-04 / Phase 35)
- `role="button"` on rows + keyboard handler (a11y for non-button click target)
  </action>
  <verify>
  <automated>pnpm --filter frontend test --run --reporter=verbose AfterActionsTable && grep -c "icon-flip" frontend/src/components/after-actions/AfterActionsTable.tsx | grep -v "^0$" && grep -c "engagement_date" frontend/src/components/after-actions/AfterActionsTable.tsx | grep -v "^0$" && grep -c "useAfterActionsAll\|useAfterActions" frontend/src/components/after-actions/AfterActionsTable.tsx | grep -E "^0$"</automated>
  </verify>
  <done>Table component + 7/7 vitest tests PASS; component does NOT call the hook directly (caller passes rows in via props — keeps it presentational); chevron uses `icon-flip` class.</done>
  </task>

<task type="auto">
  <name>Task 2: Rewrite /after-actions route + un-skip Playwright spec</name>
  <files>frontend/src/routes/_protected/after-actions/index.tsx, frontend/tests/e2e/after-actions-page.spec.ts</files>
  <read_first>
    - frontend/src/routes/_protected/after-actions/index.tsx (existing stub)
    - frontend/src/components/after-actions/AfterActionsTable.tsx (Task 1 output — props interface)
    - frontend/src/components/layout/PageHeader.tsx (PageHeaderProps)
    - frontend/tests/e2e/after-actions-page.spec.ts (Wave 0 stub — un-skip)
  </read_first>
  <action>
Rewrite `frontend/src/routes/_protected/after-actions/index.tsx`:

```tsx
import { createFileRoute } from '@tanstack/react-router'
import { useTranslation } from 'react-i18next'
import { useAfterActionsAll } from '@/hooks/useAfterAction'
import { PageHeader } from '@/components/layout/PageHeader'
import { AfterActionsTable } from '@/components/after-actions/AfterActionsTable'

export const Route = createFileRoute('/_protected/after-actions/')({
  component: AfterActionsListPage,
})

function AfterActionsListPage(): React.JSX.Element {
  const { t, i18n } = useTranslation('after-actions-page')
  const isRTL = i18n.language === 'ar'
  const { data, isLoading, error } = useAfterActionsAll()
  const rows = data?.data ?? []

  return (
    <section
      role="region"
      aria-label={t('title')}
      dir={isRTL ? 'rtl' : 'ltr'}
      data-loading={isLoading ? 'true' : 'false'}
      className="page flex min-w-0 flex-col gap-[var(--gap)]"
    >
      <PageHeader title={t('title')} subtitle={t('subtitle')} />
      <AfterActionsTable rows={rows} isLoading={isLoading} error={error ?? null} />
    </section>
  )
}
```

Preserve any existing route options (auth, prefetch) by reading the existing file first.

Then un-skip `frontend/tests/e2e/after-actions-page.spec.ts` — change `test.skip(...)` to `test(...)` for both cases (`renders the .tbl with 6 columns`, `navigation: row click → /after-actions/$id`).

The corresponding visual spec (`after-actions-page-visual.spec.ts`) STAYS test.skip — Wave 2 plan 10 generates baselines.
</action>
<verify>
<automated>grep -c "useAfterActionsAll" frontend/src/routes/\_protected/after-actions/index.tsx | grep -v "^0$" && grep -c "AfterActionsTable" frontend/src/routes/_protected/after-actions/index.tsx | grep -v "^0$" && grep -c "data-loading" frontend/src/routes/\_protected/after-actions/index.tsx | grep -v "^0$" && grep -c "test.skip" frontend/tests/e2e/after-actions-page.spec.ts | grep -E "^0$"</automated>
</verify>
<done>Route renders the new table component; functional E2E un-skipped (2 active cases); ready for Wave 2 visual generation.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                              |
| ------------------------- | -------------------------------------------------------- |
| Edge Function → Browser   | Joined records crossing the wire — RLS enforced upstream |
| Bilingual title rendering | Untrusted user-supplied titles                           |

## STRIDE Threat Register

| Threat ID       | Category               | Component                                     | Disposition         | Mitigation Plan                                                                                                                        |
| --------------- | ---------------------- | --------------------------------------------- | ------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| T-42-06-XSS-1   | Tampering              | Engagement title + dossier name interpolation | mitigate            | React's default escaping handles `{r.engagement?.title_en}` etc. No `dangerouslySetInnerHTML` (`! grep -q "dangerouslySetInnerHTML"`). |
| T-42-06-AUTHZ-1 | Information Disclosure | Cross-dossier read                            | mitigate (upstream) | RLS gate enforced at the Edge Function level (T-42-01-AA-1 in plan 01); the page only renders what the hook returns.                   |
| T-42-06-INFO-1  | Information Disclosure | Bilingual fallback                            | accept              | `?? '—'` fallbacks render an em-dash when a name is missing — no info leak.                                                            |

</threat_model>

<verification>
- `grep -c "<table className=\"tbl\">" frontend/src/components/after-actions/AfterActionsTable.tsx` → 1
- `grep -c "thead" frontend/src/components/after-actions/AfterActionsTable.tsx` → 1 (6 columns)
- `grep -c "icon-flip" frontend/src/components/after-actions/AfterActionsTable.tsx` → ≥ 1
- `grep "useAfterActionsAll" frontend/src/routes/_protected/after-actions/index.tsx` → 1 match
- `pnpm --filter frontend test --run --reporter=verbose AfterActionsTable` → 7/7 PASS
- `! grep -q "test.skip" frontend/tests/e2e/after-actions-page.spec.ts`
</verification>

<success_criteria>

- After-actions table renders 6 columns per UI-SPEC; rows clickable + keyboard-focusable
- Chevron flips in RTL via `.icon-flip` class
- Hook integration uses `useAfterActionsAll` (NOT per-dossier); R-04 date column from `engagement.engagement_date`
- Empty state + skeleton + error state all bilingual
- Vitest 7/7 PASS; functional E2E un-skipped
  </success_criteria>

<output>
After completion, create `.planning/phases/42-remaining-pages/42-06-SUMMARY.md` with file deltas, test counts, and any deviations from PATTERNS.
</output>

## Truths — CONTEXT.md decisions addressed by this plan

This plan implements the following locked decisions from `42-CONTEXT.md`. Citation here satisfies the decision-coverage gate; the concrete implementation lives in the tasks above.

- **D-17**: Logical properties only (ms-/me-/ps-/pe-/start-/end-/inset-inline-_/border-inline-_/border-block-\*) — CLAUDE.md mandate — implemented per the locked decision (resolutions R-01..R-05 honored where applicable).
- **D-20**: Bilingual digit rendering — page counts, decisions/commitments counts, dates' numeric components rendered via toArDigits — implemented per the locked decision (resolutions R-01..R-05 honored where applicable).
