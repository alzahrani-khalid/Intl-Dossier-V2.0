---
phase: 42-remaining-pages
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/index.css
  - frontend/src/design-system/DesignProvider.tsx
  - frontend/src/design-system/types.ts
  - frontend/src/design-system/__tests__/density-migration.test.ts
autonomous: true
requirements:
  - PAGE-03
  - PAGE-04
  - PAGE-05
tags:
  - phase-42
  - css-port
  - density-migration
  - wave-0
must_haves:
  truths:
    - 'Handoff CSS classes .tasks-list/.task-row/.task-box/.task-title/.task-due/.act-list/.act-row/.act-t/.act-who/.act-what/.act-where/.settings-nav/.card-head/.card-title/.card-sub render their canonical handoff styles when applied to JSX in Wave 1'
    - 'All ported CSS uses logical properties only (inset-inline-start, border-block-end) — no left/right physical properties'
    - 'Density triad rename: spacious → dense at the type level (Density union), localStorage value level (id.density), and migration shim level (one-time read+rewrite)'
    - "Existing localStorage 'spacious' values are migrated to 'dense' on first read after deploy"
  artifacts:
    - path: frontend/src/index.css
      provides: 'Handoff CSS port for Phase 42 page selectors'
      contains: '.tasks-list'
    - path: frontend/src/design-system/DesignProvider.tsx
      provides: 'Density migration shim (one-time spacious → dense rewrite)'
      contains: 'spacious'
    - path: frontend/src/design-system/__tests__/density-migration.test.ts
      provides: 'Vitest suite asserting one-time migration behavior'
  key_links:
    - from: frontend/src/index.css
      to: frontend/design-system/inteldossier_handoff_design/src/app.css
      via: 'verbatim copy of .tasks-list / .act-list / .settings-nav blocks'
      pattern: '.tasks-list'
---

<objective>
Two consolidated infra deltas in one plan:

1. **Handoff CSS port (Pitfall 6 mitigation):** verbatim copy of the 5-page-specific selectors from `frontend/design-system/inteldossier_handoff_design/src/app.css` into `frontend/src/index.css`. Phase 40 already ported `.btn / .chip / .tbl / .week-list / .forum-row / .icon-flip / .pill / .page*` per the comment in `index.css:11-12`; this plan adds `.tasks-list / .task-row / .act-list / .act-row / .settings-nav / .card-head / .card-sub`.

2. **Density triad rename (R-03):** rename `spacious → dense` at the type and storage levels, and add a one-time migration shim in `DesignProvider.tsx` that maps any existing `id.density='spacious'` localStorage value to `'dense'` on first read. Visible labels become `Comfortable / Compact / Dense`.

Purpose: Wave 1 plans 05–09 reference these CSS classes (`.tasks-list`, `.act-list`, `.settings-nav`) and the renamed density values directly. Without this plan, page reskin JSX renders unstyled and Settings → Appearance shows a stale "Spacious" label.

Output: 1 modified `index.css` (append-only), 1 modified `DesignProvider.tsx` (migration shim), 1 modified types file (Density union), 1 new vitest suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-remaining-pages/42-CONTEXT.md
@.planning/phases/42-remaining-pages/42-RESEARCH.md
@.planning/phases/42-remaining-pages/42-PATTERNS.md
@frontend/src/index.css
@frontend/design-system/inteldossier_handoff_design/src/app.css
@frontend/src/design-system/DesignProvider.tsx

<interfaces>
<!-- Existing index.css comment block establishing the precedent for appended handoff CSS port. -->

From frontend/src/index.css lines 10-15 (Phase 40 precedent):

```
/* Phase 40 list-pages handoff CSS (Plan 02c) — port of app.css rules
   for list-page primitives (.btn, .chip, .tbl, .week-list, .forum-row,
   .icon-flip, .spinner-row, .pill, .dossier-row, .page*). Imported as
   a side-effect from main.tsx instead of @import here, because Tailwind
   v4 + flag-icons expansion places non-@import statements before this
   point in the bundle, violating the CSS @import-must-be-first rule. */
```

From frontend/design-system/inteldossier_handoff_design/src/app.css — selectors to port (line ranges per RESEARCH Pitfall 6 and PATTERNS):

- `.settings-nav` + `.settings-nav.active` + `.settings-nav.active::before` — lines 73-100
- `.card-head` / `.card-title` / `.card-sub` — lines around 236-241
- `.tasks-list` / `.task-row` / `.task-box` / `.task-title` / `.task-due` (+ `.task-box.done`, `.task-box:hover`, `.task-due.today`, `.task-due.high`) — lines 341-350
- `.act-list` / `.act-row` / `.act-t` / `.act-who` / `.act-what` / `.act-where` — lines 444-450

From frontend/src/design-system/types.ts (current — Density union per RESEARCH §"Settings sections inventory"):

```ts
export type Density = 'comfortable' | 'compact' | 'spacious' // R-03 renames spacious → dense
```

From frontend/src/design-system/DesignProvider.tsx lines 33-71 (existing pattern):

```ts
const LS_DENSITY = 'id.density'
const isDensity = (value: unknown): value is Density =>
  value === 'comfortable' || value === 'compact' || value === 'spacious'
const safeGetItem = (key: string): string | null => {
  /* ... */
}
const safeSetItem = (key: string, value: string): void => {
  /* ... */
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append handoff CSS port to frontend/src/index.css</name>
  <files>frontend/src/index.css</files>
  <read_first>
    - frontend/src/index.css (full file — append at the END after the existing Phase 40 port block; do NOT modify earlier blocks)
    - frontend/design-system/inteldossier_handoff_design/src/app.css (line ranges 73-100, 236-241, 341-350, 444-450 — copy verbatim)
    - .planning/phases/42-remaining-pages/42-PATTERNS.md §"No Analog Found" (mitigation note: handoff source-of-truth is the analog)
  </read_first>
  <action>
**APPEND** the following CSS block to the END of `frontend/src/index.css` (after all existing content). Open `frontend/design-system/inteldossier_handoff_design/src/app.css`, locate each line range below, and copy the rule verbatim. Do NOT improvise selectors, dimensions, or values.

Wrap the new block in a comment header so future readers know its provenance:

```css
/* ============================================================
   Phase 42 remaining-pages handoff CSS port (Plan 42-03) —
   Verbatim copy of app.css selectors for the 5 reskinned pages.
   Source: frontend/design-system/inteldossier_handoff_design/src/app.css
   - .settings-nav* (lines 73-100)
   - .card-head / .card-title / .card-sub (lines 236-241)
   - .tasks-list / .task-row / .task-box / .task-title / .task-due (lines 341-350)
   - .act-list / .act-row / .act-t / .act-who / .act-what / .act-where (lines 444-450)
   - Settings mobile pill-row (≤768px) per D-12
   ============================================================ */

/* --- .settings-nav (handoff app.css:73-100) --- */
.settings-nav {
  position: relative;
  display: flex;
  align-items: center;
  gap: 10px;
  padding-block: 10px;
  padding-inline: 12px;
  border: 0;
  background: transparent;
  cursor: pointer;
  font: inherit;
  color: var(--ink-mute);
  border-radius: var(--radius-sm);
  width: 100%;
  text-align: start;
}
.settings-nav:hover {
  background: var(--line-soft);
}
.settings-nav.active {
  background: var(--accent-soft);
  color: var(--accent-ink);
}
.settings-nav.active::before {
  content: '';
  position: absolute;
  inset-inline-start: 0;
  inset-block: 8px;
  width: 2px;
  background: var(--accent);
  border-radius: 2px;
}

/* --- .card-head / .card-title / .card-sub (handoff app.css:236-241) --- */
.card-head {
  margin-block-end: 12px;
}
.card-title {
  font-family: var(--font-display);
  font-size: 16px;
  font-weight: 600;
  line-height: 1.35;
  color: var(--ink);
  letter-spacing: -0.005em;
}
.card-sub {
  font-size: 11.5px;
  color: var(--ink-mute);
  line-height: 1.4;
  margin-block-start: 2px;
}

/* --- .tasks-list / .task-row / .task-box / .task-title / .task-due (handoff app.css:341-350) --- */
.tasks-list {
  list-style: none;
  margin: 0;
  padding: 0;
}
.task-row {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 8px 6px;
  border-bottom: 1px solid var(--line-soft);
  font-size: 13px;
}
.task-row:last-child {
  border-bottom: 0;
}
.task-box {
  width: 14px;
  height: 14px;
  border: 1.5px solid var(--ink-faint);
  border-radius: 3px;
  flex-shrink: 0;
  cursor: pointer;
  transition: all 0.12s;
  display: flex;
  align-items: center;
  justify-content: center;
  background: transparent;
  padding: 0;
}
.task-box:hover {
  border-color: var(--accent);
}
.task-box.done {
  background: var(--ok);
  border-color: var(--ok);
}
.task-title {
  flex: 1;
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.task-due {
  font-size: 11px;
  font-family: var(--font-mono);
  color: var(--ink-mute);
  min-width: 60px;
  text-align: end;
}
.task-due.today,
.task-due.high {
  color: var(--danger);
  font-weight: 600;
}

/* --- .act-list / .act-row / .act-t / .act-who / .act-what / .act-where (handoff app.css:444-450) --- */
.act-list {
  list-style: none;
  margin: 0;
  padding: 0;
}
.act-row {
  display: grid;
  grid-template-columns: 60px 24px 1fr;
  align-items: baseline;
  gap: 10px;
  padding-block: 8px;
  border-bottom: 1px solid var(--line-soft);
  font-size: 13px;
}
.act-row:last-child {
  border-bottom: 0;
}
.act-t {
  font-family: var(--font-mono);
  font-size: 10.5px;
  color: var(--ink-faint);
}
.act-who {
  font-weight: 500;
}
.act-what {
  color: var(--ink-mute);
}
.act-where {
  color: var(--accent-ink);
}

/* --- Settings mobile pill row (≤768px) per D-12 --- */
@media (max-width: 768px) {
  .settings-layout {
    grid-template-columns: 1fr !important;
  }
  .settings-nav-card {
    display: flex;
    overflow-x: auto;
    gap: 8px;
    padding: 8px;
  }
  .settings-nav-card .settings-nav {
    white-space: nowrap;
    flex: 0 0 auto;
    width: auto;
  }
  .settings-nav.active::before {
    display: none;
  }
  .settings-nav.active {
    border-block-end: 2px solid var(--accent-ink);
  }
}
```

CRITICAL constraints:

- All values copied verbatim from app.css line ranges noted in the header comment
- Logical properties only (`inset-inline-start`, `border-block-end`, `padding-inline`, `margin-block-end`, `text-align: end / start`) — NO `left/right/margin-left/margin-right/text-align: right/left`
- All colors via `var(--*)` tokens — NO raw hex
- `.task-box` includes the 44×44 padded outer hit area NOT in the visual 14×14 box — outer hit is enforced via `min-width:44px; min-height:44px` inline style on the button per UI-SPEC (NOT in this CSS file — handled in Wave 1 plan 07)
- Do NOT modify or delete any pre-existing CSS in `index.css`
  </action>
  <verify>
  <automated>grep -c "\.tasks-list" frontend/src/index.css | grep -v "^0$" && grep -c "\.act-list" frontend/src/index.css | grep -v "^0$" && grep -c "\.settings-nav" frontend/src/index.css | grep -v "^0$" && grep -c "\.card-head" frontend/src/index.css | grep -v "^0$" && ! grep -E "(margin-left|margin-right|text-align: \*(left|right))" frontend/src/index.css | grep -v "^/\*"</automated>
  </verify>
  <done>All 4 selector groups present; mobile pill-row media query present; no physical-property violations on the new block (logical properties only). `pnpm --filter frontend build` (or dev server probe) does not error.</done>
  </task>

<task type="auto" tdd="true">
  <name>Task 2: Density triad rename + migration shim in DesignProvider</name>
  <files>frontend/src/design-system/DesignProvider.tsx, frontend/src/design-system/types.ts, frontend/src/design-system/__tests__/density-migration.test.ts</files>
  <read_first>
    - frontend/src/design-system/types.ts (full file — locate Density union; rename ONLY the value, do NOT change the export name)
    - frontend/src/design-system/DesignProvider.tsx (full file — locate `LS_DENSITY`, `isDensity`, `safeGetItem`, `safeSetItem` around line 33-71)
    - frontend/src/design-system/buildTokens.ts (search for Density usage; update token map keys if `spacious` is referenced)
    - frontend/src/design-system/densities.ts (likely contains the `spacious` token map — rename to `dense`)
    - .planning/phases/42-remaining-pages/42-CONTEXT.md §Resolutions R-03 (locked spec)
    - .planning/phases/42-remaining-pages/42-PATTERNS.md §"DesignProvider.tsx — EXTEND with migration shim"
  </read_first>
  <behavior>
    - Test 1: Reading `id.density='spacious'` from localStorage triggers a one-time rewrite to `'dense'` (`localStorage.setItem` called with `('id.density','dense')`)
    - Test 2: After migration, the DesignProvider exposes `density === 'dense'`
    - Test 3: Reading `id.density='comfortable'` does NOT trigger any rewrite (no `setItem` call)
    - Test 4: Reading `id.density='dense'` does NOT trigger any rewrite (idempotent)
    - Test 5: Reading missing key returns the default density (Comfortable per Phase 33/34 contract) without error
  </behavior>
  <action>
**Part A — Rename Density union:**

In `frontend/src/design-system/types.ts`, change:

```ts
export type Density = 'comfortable' | 'compact' | 'spacious'
```

to:

```ts
export type Density = 'comfortable' | 'compact' | 'dense'
```

Search the codebase for `'spacious'` literal and rename to `'dense'` in:

- `frontend/src/design-system/densities.ts` (token-map keys)
- `frontend/src/design-system/buildTokens.ts` (any references)
- Any other design-system file in `frontend/src/design-system/` that hard-codes the literal

Use a search to confirm: `grep -rn "spacious" frontend/src/design-system/` should return ZERO matches after the rename, EXCEPT inside `DesignProvider.tsx` where the migration shim retains a reference for the rewrite logic.

**Part B — Migration shim in DesignProvider.tsx:**

Locate the existing `safeGetItem(LS_DENSITY)` call and the `isDensity` helper around line 33-71. ADD a new helper function `readDensityWithMigration` that wraps the existing read:

```ts
const LS_DENSITY = 'id.density'
const isDensity = (value: unknown): value is Density =>
  value === 'comfortable' || value === 'compact' || value === 'dense'

function readDensityWithMigration(): Density | null {
  const raw = safeGetItem(LS_DENSITY)
  if (raw === 'spacious') {
    safeSetItem(LS_DENSITY, 'dense')
    return 'dense'
  }
  return isDensity(raw) ? raw : null
}
```

Replace the existing `safeGetItem(LS_DENSITY)` call site (where the lazy initializer reads density) with `readDensityWithMigration()`. Verify by reading the file before and after — the only call site should now be wrapped.

CRITICAL:

- Do NOT remove the `'spacious'` string from `DesignProvider.tsx` — the migration shim depends on detecting it
- The migration MUST be idempotent: subsequent reads of an already-migrated `'dense'` key MUST NOT call `safeSetItem` again (that would be wasteful but not incorrect; the test asserts no extra writes)
- Do NOT add any UI to surface the migration to users — it is silent

**Part C — Vitest suite:**

Create `frontend/src/design-system/__tests__/density-migration.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'

const STORAGE_KEY = 'id.density'

function makeStorageMock(initial: Record<string, string>) {
  const store = { ...initial }
  return {
    getItem: vi.fn((k: string) => (k in store ? store[k] : null)),
    setItem: vi.fn((k: string, v: string) => {
      store[k] = v
    }),
    removeItem: vi.fn((k: string) => {
      delete store[k]
    }),
    clear: vi.fn(() => {
      for (const k of Object.keys(store)) delete store[k]
    }),
    key: vi.fn(),
    length: 0,
  } as unknown as Storage
}

describe('Density migration shim (R-03)', () => {
  beforeEach(() => {
    vi.resetModules()
  })

  it('rewrites legacy "spacious" → "dense" on first read', async () => {
    const mock = makeStorageMock({ [STORAGE_KEY]: 'spacious' })
    vi.stubGlobal('localStorage', mock)
    const mod = await import('../DesignProvider')
    // Trigger initialization by importing — DesignProvider's module-scope has the read.
    // If the read is inside the component, render it instead.
    expect(mock.setItem).toHaveBeenCalledWith(STORAGE_KEY, 'dense')
  })

  it('leaves "comfortable" untouched', async () => {
    const mock = makeStorageMock({ [STORAGE_KEY]: 'comfortable' })
    vi.stubGlobal('localStorage', mock)
    await import('../DesignProvider')
    expect(mock.setItem).not.toHaveBeenCalledWith(STORAGE_KEY, expect.anything())
  })

  it('leaves "dense" untouched (idempotent)', async () => {
    const mock = makeStorageMock({ [STORAGE_KEY]: 'dense' })
    vi.stubGlobal('localStorage', mock)
    await import('../DesignProvider')
    expect(mock.setItem).not.toHaveBeenCalledWith(STORAGE_KEY, expect.anything())
  })

  it('returns null when key is missing (defaults handled by provider)', async () => {
    const mock = makeStorageMock({})
    vi.stubGlobal('localStorage', mock)
    await import('../DesignProvider')
    expect(mock.setItem).not.toHaveBeenCalledWith(STORAGE_KEY, expect.anything())
  })
})
```

NOTE: If `readDensityWithMigration` is invoked only inside the React component's lazy initializer (not at module scope), adapt the test to render the `DesignProvider` component and inspect the localStorage mock. Use `@testing-library/react` `render` with a child consumer. Verify by reading DesignProvider.tsx first.
</action>
<verify>
<automated>grep -c "'dense'" frontend/src/design-system/types.ts | grep -v "^0$" && grep -c "readDensityWithMigration" frontend/src/design-system/DesignProvider.tsx | grep -v "^0$" && pnpm --filter frontend test --run --reporter=verbose density-migration && pnpm --filter frontend tsc --noEmit 2>&1 | grep -c "design-system" | grep -E "^0$"</automated>
</verify>
<done>Density union renamed; migration shim added; vitest 4/4 PASS; zero new TS errors on design-system files; `grep -rn "spacious" frontend/src/design-system/` returns ONLY references inside DesignProvider.tsx (the migration shim).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                               |
| ---------------------- | ------------------------------------------------------------------------- |
| Browser → localStorage | User-controllable storage values read by DesignProvider on initialization |

## STRIDE Threat Register

| Threat ID     | Category  | Component                  | Disposition | Mitigation Plan                                                                                                                                                                                                                                |
| ------------- | --------- | -------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-42-03-T-1   | Tampering | Density localStorage value | mitigate    | `isDensity` type guard validates the read value matches one of the three union members (`comfortable`/`compact`/`dense`); `'spacious'` triggers the migration; any other value falls through to the default. No string is interpreted as code. |
| T-42-03-XSS-1 | Tampering | Static CSS port            | accept      | All values copied verbatim from a vetted handoff source-of-truth file (`inteldossier_handoff_design/src/app.css`); no user input parsed; no inline JS or `expression()`.                                                                       |

</threat_model>

<verification>
- `grep -c "\.tasks-list" frontend/src/index.css` ≥ 1
- `grep -c "\.act-list" frontend/src/index.css` ≥ 1
- `grep -c "\.settings-nav" frontend/src/index.css` ≥ 1
- `grep -c "@media (max-width: 768px)" frontend/src/index.css` ≥ 1 (mobile pill row)
- `grep "'dense'" frontend/src/design-system/types.ts` matches
- `grep "readDensityWithMigration" frontend/src/design-system/DesignProvider.tsx` matches
- `pnpm --filter frontend test --run --reporter=verbose density-migration` → 4/4 PASS
- `pnpm --filter frontend tsc --noEmit 2>&1 | grep -c "design-system"` → 0
</verification>

<success_criteria>

- index.css contains the 4 selector groups (settings-nav, card-head, tasks-list, act-list) + mobile pill-row media query, all logical-properties only
- Density union rename complete; migration shim live; vitest 4/4 PASS
- Wave 1 plans 07–09 can use `<ul className="tasks-list">`, `<ul className="act-list">`, `<button className="settings-nav active">` and the styles render
- Settings → Appearance section in Wave 1 plan 09 will display `Comfortable / Compact / Dense` (NOT "Spacious")
  </success_criteria>

<output>
After completion, create `.planning/phases/42-remaining-pages/42-03-SUMMARY.md` documenting:
- index.css line count delta (before vs after the append)
- The exact rename surface in design-system files
- Migration test results (4/4 PASS expected)
- Any deviations from handoff CSS line ranges (should be zero)
</output>

## Truths — CONTEXT.md decisions addressed by this plan

This plan implements the following locked decisions from `42-CONTEXT.md`. Citation here satisfies the decision-coverage gate; the concrete implementation lives in the tasks above.

- **D-17**: Logical properties only (ms-/me-/ps-/pe-/start-/end-/inset-inline-_/border-inline-_/border-block-\*) — CLAUDE.md mandate — implemented per the locked decision (resolutions R-01..R-05 honored where applicable).
