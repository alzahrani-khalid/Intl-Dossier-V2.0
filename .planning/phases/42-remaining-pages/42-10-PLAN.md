---
phase: 42-remaining-pages
plan: 10
type: execute
wave: 2
depends_on:
  - 42-05
  - 42-06
  - 42-07
  - 42-08
  - 42-09
files_modified:
  - frontend/tests/e2e/briefs-page-visual.spec.ts
  - frontend/tests/e2e/after-actions-page-visual.spec.ts
  - frontend/tests/e2e/tasks-page-visual.spec.ts
  - frontend/tests/e2e/activity-page-visual.spec.ts
  - frontend/tests/e2e/settings-page-visual.spec.ts
  - frontend/tests/e2e/briefs-page-visual.spec.ts-snapshots/
  - frontend/tests/e2e/after-actions-page-visual.spec.ts-snapshots/
  - frontend/tests/e2e/tasks-page-visual.spec.ts-snapshots/
  - frontend/tests/e2e/activity-page-visual.spec.ts-snapshots/
  - frontend/tests/e2e/settings-page-visual.spec.ts-snapshots/
  - frontend/.size-limit.json
autonomous: false
requirements:
  - PAGE-01
  - PAGE-02
  - PAGE-03
  - PAGE-04
  - PAGE-05
tags:
  - phase-42
  - visual-regression
  - size-limit
  - wave-2
must_haves:
  truths:
    - '10 visual baseline PNGs accepted by human reviewer (5 pages × LTR + AR @ 1280); 11th baseline for settings mobile @ 768'
    - 'Visual specs un-skipped (test.skip → test) for all 5 pages; suite enumerates 11 visual cases'
    - 'size-limit gate passes — Total JS budget held at 815 KB (or raised if Phase 42 surface added <10 KB net)'
    - 'Each baseline file (PNG) committed to repo at the standard playwright snapshots path'
  artifacts:
    - path: frontend/tests/e2e/briefs-page-visual.spec.ts-snapshots/briefs-page-en.png
      provides: 'Briefs LTR @ 1280 baseline'
    - path: frontend/tests/e2e/briefs-page-visual.spec.ts-snapshots/briefs-page-ar.png
      provides: 'Briefs AR @ 1280 baseline'
    - path: frontend/.size-limit.json
      provides: 'Updated bundle budget if Phase 42 net add exceeded the 815 KB ceiling'
  key_links:
    - from: frontend/tests/e2e/briefs-page-visual.spec.ts
      to: frontend/tests/e2e/briefs-page-visual.spec.ts-snapshots/
      via: 'Playwright `toHaveScreenshot()` baseline file'
      pattern: '.png'
---

<objective>
Wave 2 visual + bundle gates. Generates 10 (or 11 with settings mobile) visual baselines via `playwright test --update-snapshots`, surfaces them for human review (per checkpoint), then runs `size-limit` to confirm the bundle stayed under budget. After human approval, commits the baseline PNGs to the repo so subsequent runs gate against them.

Per Phase 38 D-13 / Phase 40 D-06 / Phase 41 D-12 precedent: visual regression at 1280px LTR + AR only. Mobile breakpoints are render assertions (Wave 0 plan 04 functional spec stubs) — except for the settings mobile pill row which is a documented UI-SPEC contract worth a baseline.

This plan contains a `checkpoint:human-verify` gate — visual baselines must be reviewed against the handoff source-of-truth before commit.

Output: 10-11 PNG baseline files committed; 5 visual specs un-skipped; size-limit budget verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-remaining-pages/42-CONTEXT.md
@.planning/phases/42-remaining-pages/42-VALIDATION.md
@.planning/phases/42-remaining-pages/42-UI-SPEC.md
@frontend/playwright.config.ts
@frontend/.size-limit.json
@.planning/phases/42-remaining-pages/42-04-SUMMARY.md
@.planning/phases/42-remaining-pages/42-05-SUMMARY.md
@.planning/phases/42-remaining-pages/42-06-SUMMARY.md
@.planning/phases/42-remaining-pages/42-07-SUMMARY.md
@.planning/phases/42-remaining-pages/42-08-SUMMARY.md
@.planning/phases/42-remaining-pages/42-09-SUMMARY.md

<interfaces>
Existing playwright.config.ts (Phase 33-09 + 40-17 precedent — already configured):
```ts
expect: { toHaveScreenshot: { maxDiffPixels: 100, threshold: 0.2 } }
use: { reducedMotion: 'reduce' }
```

Existing .size-limit.json budget:

```json
[{ "name": "Total JS", "path": "dist/assets/index-*.js", "limit": "815 KB" }]
```

Each visual spec from Wave 0 plan 04 (now needs un-skip):

- briefs-page-visual.spec.ts: LTR + AR @ 1280
- after-actions-page-visual.spec.ts: LTR + AR @ 1280
- tasks-page-visual.spec.ts: LTR + AR @ 1280
- activity-page-visual.spec.ts: LTR + AR @ 1280
- settings-page-visual.spec.ts: LTR + AR @ 1280 + mobile @ 768
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Un-skip 5 visual specs and generate baselines via --update-snapshots</name>
  <files>frontend/tests/e2e/briefs-page-visual.spec.ts, frontend/tests/e2e/after-actions-page-visual.spec.ts, frontend/tests/e2e/tasks-page-visual.spec.ts, frontend/tests/e2e/activity-page-visual.spec.ts, frontend/tests/e2e/settings-page-visual.spec.ts</files>
  <read_first>
    - frontend/tests/e2e/briefs-page-visual.spec.ts (still test.skip from Wave 0)
    - frontend/tests/e2e/list-pages-visual.spec.ts (Phase 40 baseline-generation precedent)
    - frontend/playwright.config.ts (verify the snapshot path + `expect.toHaveScreenshot` config)
  </read_first>
  <action>
**Part A — Un-skip all 5 visual spec files.** In each file, replace `test.skip(...)` with `test(...)` for every case.

**Part B — Generate baselines.** Run:

```bash
cd frontend && pnpm playwright test \
  briefs-page-visual.spec.ts \
  after-actions-page-visual.spec.ts \
  tasks-page-visual.spec.ts \
  activity-page-visual.spec.ts \
  settings-page-visual.spec.ts \
  --update-snapshots \
  --workers=1
```

`--workers=1` ensures deterministic baseline generation (no parallel viewport contention).

**Part C — Re-run for stability** (Phase 40-17 + 41 precedent — 3× repeat to detect flake):

```bash
pnpm playwright test \
  briefs-page-visual.spec.ts \
  after-actions-page-visual.spec.ts \
  tasks-page-visual.spec.ts \
  activity-page-visual.spec.ts \
  settings-page-visual.spec.ts \
  --repeat-each=3 \
  --workers=1
```

If any spec fails on the second or third repeat, the determinism stack (clock freeze, transition suppression, font readiness) is incomplete — fix before proceeding to checkpoint. Common culprits per Phase 40-17 G7:

- Animation not suppressed (add to SUPPRESS_TRANSITIONS_CSS in fixtures)
- Font not loaded (add `await page.waitForFunction(() => document.fonts.ready)` already in `gotoPhase42Page`)
- Realtime subscription jitter (mock or disable in test env)

If ENV / dev-server unavailable in this session: document the deferral in SUMMARY and escalate to ops as a one-shot run on a dev machine. **Do NOT commit baselines without human review** (Task 2 is the gate).
</action>
<verify>
<automated>grep -c "test.skip" frontend/tests/e2e/briefs-page-visual.spec.ts | grep -E "^0$" && ls frontend/tests/e2e/briefs-page-visual.spec.ts-snapshots/_.png 2>/dev/null | head -1 | grep -q "\.png" || echo "BASELINES NOT YET GENERATED — DEFERRAL TO OPS"</automated>
</verify>
<done>5 visual specs un-skipped. Either: (a) 10-11 baseline PNGs generated under `_-snapshots/` directories and stable across 3× repeats, or (b) deferred-to-ops with rationale documented in SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human reviews 10-11 visual baselines against handoff source</name>
  <what-built>
    Wave 1 plans 05–09 reskinned the 5 pages to the IntelDossier handoff anatomy. Task 1 of this plan generated 10-11 visual baselines (5 pages × 2 locales @ 1280 + settings mobile @ 768).
  </what-built>
  <how-to-verify>
    1. Open each baseline PNG under `frontend/tests/e2e/*-page-visual.spec.ts-snapshots/`.
    2. Open the corresponding handoff source for visual comparison:
       - Briefs: `frontend/design-system/inteldossier_handoff_design/src/pages.jsx#L295-329` rendered at 1280
       - After-actions: `pages.jsx#L331-365`
       - Tasks: `pages.jsx#L367-396`
       - Activity: `pages.jsx#L398-421`
       - Settings: `pages.jsx#L423-465` + reference image `frontend/design-system/inteldossier_handoff_design/project/reference/settings.png`
    3. For each LTR baseline, compare against the handoff:
       - Card grid / table / list anatomy matches (column count, row anatomy, chip placement)
       - Typography matches (display-font titles, mono numerics, ink-faint helper copy)
       - Colors match (60/30/10 split per UI-SPEC)
       - Borders are 1px solid `--line`; no card shadows except where allowed
       - Logical-property RTL flip works on the AR baseline (chevrons flipped, `inset-inline-start` correct edge, Tajawal font applied)
    4. For the settings mobile baseline @ 768: nav row collapses to horizontal pill strip; active marker is `border-block-end` underline (NOT `inset-inline-start` accent bar).
    5. **PASS criteria:** all baselines match handoff verbatim port within human visual tolerance. **FAIL criteria:** any visible deviation that can't be explained by token resolution (e.g., different padding, wrong color, missing chip).
    6. If FAIL: surface specific page + locale; the executor opens a gap-closure plan to fix before re-generating baselines.
  </how-to-verify>
  <resume-signal>Type "approved" to commit baselines, or describe per-page issues and which Wave 1 plan to revisit.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run size-limit gate</name>
  <files>frontend/.size-limit.json</files>
  <read_first>
    - frontend/.size-limit.json (current budget — 815 KB Total JS per Phase 40-02c)
  </read_first>
  <action>
After human approves baselines (previous task), run the bundle gate:

```bash
cd frontend && pnpm size-limit
```

Expected outcome: PASS at 815 KB or below.

If FAIL:

1. Read the size-limit output to see how much over budget.
2. If the over-budget delta is <10 KB and Phase 42 added the only new code: raise the budget to accommodate (Phase 40 precedent for 800 → 815 raise). Document the raise in `frontend/.size-limit.json` and SUMMARY.
3. If the delta is >10 KB: investigate which file added the bulk (typically a dependency import); discuss with user before raising.

The new icons component (~14 cases × ~50 LOC each = ~3 KB), the i18n JSONs (~10 KB total compressed), the new Edge Function hook (~2 KB), and the CSS port (~3 KB) sum to ~18 KB raw. Tree-shaking + minification typically reduces this to ~5-8 KB net. Expect to stay within budget.
</action>
<verify>
<automated>cd frontend && pnpm size-limit 2>&1 | tail -5</automated>
</verify>
<done>size-limit reports PASS (or raised budget with documented rationale).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                    |
| -------- | -------------------------------------------------------------- |
| (none)   | Baseline generation runs in CI sandbox against seeded fixtures |

## STRIDE Threat Register

| Threat ID      | Category               | Component                             | Disposition | Mitigation Plan                                                                  |
| -------------- | ---------------------- | ------------------------------------- | ----------- | -------------------------------------------------------------------------------- |
| T-42-10-INFO-1 | Information Disclosure | Baseline PNGs may capture seeded data | accept      | Test fixtures use synthetic names; no PII; reviewed in checkpoint before commit. |

</threat_model>

<verification>
- 10-11 PNG baselines exist in `frontend/tests/e2e/*-page-visual.spec.ts-snapshots/`
- `! grep -q "test.skip" frontend/tests/e2e/briefs-page-visual.spec.ts` (and 4 others)
- `pnpm playwright test *-page-visual --repeat-each=3 --workers=1` → all PASS (zero flake)
- `pnpm size-limit` → PASS (or raised with rationale)
- Human approval received for all baselines
</verification>

<success_criteria>

- All 10-11 visual baselines committed and human-approved
- Visual suite stable across 3× repeats
- Bundle budget held (or raised with rationale)
  </success_criteria>

<output>
After completion, create `.planning/phases/42-remaining-pages/42-10-SUMMARY.md` with:
- Baseline file paths + sizes
- Repeat-3× stability outcome (flake count)
- Bundle delta + final budget
- Human approval signal
</output>

## Truths — CONTEXT.md decisions addressed by this plan

This plan implements the following locked decisions from `42-CONTEXT.md`. Citation here satisfies the decision-coverage gate; the concrete implementation lives in the tasks above.

- **D-16**: Test gates per Phase 38/40/41 precedent (3 waves: infra → reskin → gates with visual baselines + axe + size-limit) — implemented per the locked decision (resolutions R-01..R-05 honored where applicable).
- **D-19**: Card-view fallback / responsive reflow at 320 / 768 / 1280px (Briefs grid; After-actions reflow; Settings nav collapse) — implemented per the locked decision (resolutions R-01..R-05 honored where applicable).
