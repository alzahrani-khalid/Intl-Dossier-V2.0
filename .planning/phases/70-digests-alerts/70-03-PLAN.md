---
phase: 70-digests-alerts
plan: 03
type: execute
wave: 3
depends_on:
  - 70-02
files_modified: []
autonomous: false
requirements:
  - DIGEST-01
  - DIGEST-02
  - DIGEST-03
  - DIGEST-04
  - ALERT-01
  - ALERT-02

must_haves:
  truths:
    - '[BLOCKING] Migration 20260615_phase70_digests_alerts.sql is LIVE on staging zkrcjzdemdmwhearhfgg'
    - 'intelligence_digest_subscriptions table exists on staging'
    - 'intelligence_alert_rules table exists on staging'
    - 'intelligence_digest_items table exists on staging'
    - 'intelligence_email_queue table exists on staging'
    - 'generate_digest RPC returns JSONB on direct invocation (staging DB)'
    - 'publish_digest RPC accepts and commits a digest row (staging DB)'
    - 'read_digests RPC returns empty list for a new authenticated user (staging DB)'
    - 'notify_intelligence_alert trigger is active on intelligence_event (staging DB)'
    - 'profiles.user_id RLS landmine is NOT present in any P70 policy (verified by impersonation test)'
    - 'intelligence_digest dossier_type CHECK now includes elected_official (D-03)'
    - 'D-13 personal ownership RLS is live: subscriber sees only their own subscriptions/alert-rules'

  artifacts:
    - path: 'supabase/migrations/20260615_phase70_digests_alerts.sql'
      provides: 'committed migration (read-only at this step — already authored in 70-02)'

  key_links:
    - from: 'staging DB intelligence_digest_subscriptions'
      to: 'generate_digest RPC caller JWT'
      via: 'subscriber_id = auth.uid() RLS enforced on staging'
    - from: 'staging DB intelligence_event AFTER INSERT trigger'
      to: "pg_notify('intelligence_alert')"
      via: 'notify_intelligence_alert SECURITY DEFINER trigger live on staging'
---

<objective>
[BLOCKING] Apply the P70 migration to live staging (zkrcjzdemdmwhearhfgg) via Supabase MCP, then run impersonation UAT to confirm RLS correctness. This plan has no autonomous executor tasks — it is a human+orchestrator checkpoint. The orchestrator applies the migration; the human (or orchestrator via MCP) runs verification queries.

Purpose: Without a live apply, build/typecheck/tsc all pass green (types come from config, not live DB). RLS correctness can only be proven against the applied schema via impersonation reads. This is the mandatory gate before any implementation consumes the new tables.
Output: All 5 new tables confirmed live; all 4 RPCs executable; trigger active; RLS impersonation test passes (user reads own subscriptions, cannot read others').
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/70-digests-alerts/70-CONTEXT.md
@.planning/phases/70-digests-alerts/70-RESEARCH.md
@.planning/phases/70-digests-alerts/70-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Orchestrator applies P70 migration to staging via Supabase MCP</name>
  <read_first>
    supabase/migrations/20260615_phase70_digests_alerts.sql — the migration to apply (authored in Plan 02).
    CLAUDE.md §"Deployment Configuration" — staging project ID: zkrcjzdemdmwhearhfgg.
    .planning/phases/70-digests-alerts/70-RESEARCH.md §RF-2 — expected schema after apply.
  </read_first>
  <what-built>
    The orchestrator calls mcp__supabase__apply_migration with:
      project_id: "zkrcjzdemdmwhearhfgg"
      migration SQL: contents of supabase/migrations/20260615_phase70_digests_alerts.sql

    After apply, the orchestrator runs these verification SQL queries via mcp__supabase__execute_sql:
      Query 1 — Table existence:
        SELECT table_name FROM information_schema.tables
        WHERE table_schema = 'public'
        AND table_name IN ('intelligence_digest_subscriptions','intelligence_alert_rules','intelligence_digest_items','intelligence_email_queue')
        ORDER BY table_name;
        Expected: 4 rows.

      Query 2 — dossier_type CHECK includes elected_official:
        SELECT constraint_name, check_clause
        FROM information_schema.check_constraints
        WHERE constraint_name = 'intelligence_digest_subscriptions_dossier_type_check';
        Expected: clause contains 'elected_official'.

      Query 3 — RPC existence:
        SELECT routine_name, security_type FROM information_schema.routines
        WHERE routine_schema = 'public'
        AND routine_name IN ('generate_digest','publish_digest','generate_digest_content','read_digests')
        ORDER BY routine_name;
        Expected: 4 rows, all INVOKER.

      Query 4 — Trigger existence:
        SELECT trigger_name, event_object_table, action_timing
        FROM information_schema.triggers
        WHERE trigger_name = 'intelligence_event_alert_notify';
        Expected: 1 row, AFTER, intelligence_event.

      Query 5 — RLS landmine check (profiles.user_id):
        SELECT polname, pg_get_expr(polqual, polrelid) as policy_expr
        FROM pg_policy
        WHERE polrelid IN (
          'intelligence_digest_subscriptions'::regclass,
          'intelligence_alert_rules'::regclass,
          'intelligence_digest'::regclass
        );
        Expected: no expression contains "profiles.id = auth.uid()". ALL clearance subqueries use "profiles.user_id = auth.uid()".

      Query 6 — Impersonation RLS test:
        -- Run as authenticated user with a known user ID (staging test user):
        SELECT count(*) FROM intelligence_digest_subscriptions;
        Expected: 0 rows (empty on fresh staging, not an RLS block — service-role confirms table has 0 rows total).
        If service-role count = 0 AND authenticated count = 0: RLS is correct (not blocking, just no data).
        If service-role count > 0 AND authenticated count = 0 AND subscriber_id != auth.uid(): RLS correctly denies.

  </what-built>
  <how-to-verify>
    After the orchestrator runs all 6 queries:
    1. Confirm 4 new tables exist (Query 1 returns 4 rows).
    2. Confirm elected_official in dossier_type CHECK (Query 2 contains it).
    3. Confirm 4 RPCs exist as SECURITY INVOKER (Query 3 returns 4 rows, all INVOKER).
    4. Confirm trigger is AFTER INSERT on intelligence_event (Query 4 returns 1 row).
    5. Confirm no profiles.id landmine in any policy (Query 5 review).
    6. Confirm basic RLS does not break (Query 6 exits without 500 error).
  </how-to-verify>
  <resume-signal>Type "migration applied and verified" after all 6 queries return expected results. If any query fails, describe the failure and the orchestrator will apply a fixup migration before proceeding.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                                      |
| ------------------------------ | ------------------------------------------------------------------------------------------------ |
| Supabase MCP → live staging DB | DDL changes applied to production-equivalent environment; irreversible without a fixup migration |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                 | Disposition | Mitigation Plan                                                                                                                                                           |
| --------- | ---------------------- | --------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-70-08   | Tampering              | Migration apply — schema change on live staging           | mitigate    | Blocking human checkpoint; orchestrator applies via MCP only after 70-02 migration is committed and reviewed; fixup migration path available if verification queries fail |
| T-70-09   | Elevation of Privilege | profiles.user_id RLS landmine in live policies            | mitigate    | Query 5 explicitly checks all 3 tables' policies for "profiles.id = auth.uid()" pattern; plan DOES NOT proceed if any match found                                         |
| T-70-10   | Information Disclosure | generate_digest_content accessible by authenticated users | mitigate    | Query 3 confirms security_type = INVOKER for all 4 RPCs; GRANT for generate_digest_content is service_role only (verified by pg_proc.proconfig if needed)                 |

</threat_model>

<verification>
All verification is embedded in the task above (6 SQL queries). The plan is complete only when all 6 queries return expected results and the human signals "migration applied and verified".
</verification>

<success_criteria>
Plan complete when:

- migration applied to staging zkrcjzdemdmwhearhfgg (MCP apply_migration confirmed)
- 4 new tables confirmed live
- 4 RPCs confirmed SECURITY INVOKER
- trigger confirmed AFTER INSERT on intelligence_event
- No profiles.id landmine in any live policy
- Human has signalled "migration applied and verified"
  </success_criteria>

<output>
Create `.planning/phases/70-digests-alerts/70-03-SUMMARY.md` when done
</output>
