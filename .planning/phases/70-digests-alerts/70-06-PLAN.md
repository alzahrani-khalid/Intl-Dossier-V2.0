---
phase: 70-digests-alerts
plan: 06
type: execute
wave: 5
depends_on:
  - 70-04
  - 70-05
files_modified:
  - frontend/src/i18n/en/intelligence-digests.json
  - frontend/src/i18n/ar/intelligence-digests.json
  - frontend/src/i18n/en/intelligence-alerts.json
  - frontend/src/i18n/ar/intelligence-alerts.json
  - frontend/src/i18n/index.ts
  - frontend/src/domains/signals/hooks/useDigests.ts
  - frontend/src/domains/signals/hooks/useAlertRules.ts
  - frontend/src/components/intelligence/DigestsTab.tsx
  - frontend/src/components/intelligence/DigestCard.tsx
  - frontend/src/components/intelligence/DigestReader.tsx
  - frontend/src/components/intelligence/DigestSubscribeDrawer.tsx
  - frontend/src/components/intelligence/AlertsTab.tsx
  - frontend/src/components/intelligence/AlertRuleRow.tsx
  - frontend/src/components/intelligence/AlertRuleForm.tsx
  - frontend/src/pages/intelligence/IntelligencePage.tsx
autonomous: true
requirements:
  - DIGEST-01
  - DIGEST-03
  - ALERT-01
  - ALERT-03

must_haves:
  truths:
    - 'intelligence-digests and intelligence-alerts namespaces are registered in src/i18n/index.ts for both en and ar (P68 CI guard — D-11)'
    - 'DigestsTab renders published digest list with indistinguishable-empty state — never mentions clearance (P68 D-09)'
    - 'DigestCard shows rollup count header in --t-kpi-value 36px, dossier glyph, frequency chip, period string (UI-SPEC Surface 1 Digests)'
    - 'DigestReader renders 4 collapsible sections (Signals, Engagements, Commitments, Status changes) (UI-SPEC Surface 2)'
    - 'AlertsTab renders alert-rule rows with active/inactive toggle, severity filter chip, channel icons, delete icon-button (UI-SPEC Surface 1 Alerts)'
    - "AlertRuleForm drawer is 480px end-edge; channels 'in_app' always enabled and non-uncheckabl; D-10 disclaimer shown for webhook channel (UI-SPEC Surface 4)"
    - 'DigestSubscribeDrawer shows frequency radio chips (Daily/Weekly/Monthly) and subscribe/unsubscribe flow (UI-SPEC Surface 3)'
    - 'IntelligencePage.tsx has 4 tabs in order: Reports | Signals | Digests | Alerts (D-11)'
    - 'All colors use var(--*) tokens — no raw hex, no text-blue-500 (CLAUDE.md design rules)'
    - 'All layout uses logical properties: ms-*/me-*/ps-*/pe-*/text-start (CLAUDE.md RTL rules)'
    - 'Row heights use var(--row-h) on digest card list and alert-rule list rows (UI-SPEC)'
    - 'D-12 entry from dossier page — DigestSubscribeDrawer has dossierId/dossierType/dossierName props for per-dossier subscription'
    - "D-13 personal ownership — useDigests and useAlertRules fetch only the authenticated user's data via INVOKER RPCs / owner-scoped API calls"

  artifacts:
    - path: 'frontend/src/i18n/index.ts'
      provides: 'digests and alerts namespaces registered for en + ar'
      contains: 'intelligence-digests'
    - path: 'frontend/src/components/intelligence/DigestsTab.tsx'
      provides: 'Digests hub tab with useDigests hook, DigestCard list, empty states'
    - path: 'frontend/src/components/intelligence/AlertsTab.tsx'
      provides: 'Alerts hub tab with useAlertRules hook, AlertRuleRow list, AlertRuleForm drawer'
    - path: 'frontend/src/pages/intelligence/IntelligencePage.tsx'
      provides: '4-tab intelligence hub (Reports | Signals | Digests | Alerts)'
    - path: 'frontend/src/domains/signals/hooks/useDigests.ts'
      provides: 'TanStack Query hook for read_digests INVOKER RPC'
    - path: 'frontend/src/domains/signals/hooks/useAlertRules.ts'
      provides: 'TanStack Query hook + mutations for intelligence_alert_rules CRUD'

  key_links:
    - from: 'DigestsTab'
      to: 'read_digests INVOKER RPC'
      via: "useDigests() → supabase.rpc('read_digests', { p_dossier_id })"
    - from: 'AlertRuleForm'
      to: 'POST /api/intelligence-alerts/rules'
      via: "useCreateAlertRule mutation → supabase.from('intelligence_alert_rules').insert()"
    - from: 'IntelligencePage.tsx'
      to: 'DigestsTab + AlertsTab'
      via: "activeTab === 'digests' | activeTab === 'alerts' conditional render"
---

<objective>
Build all frontend surfaces for Phase 70: the Digests + Alerts tabs in the intelligence hub, supporting components (DigestCard, DigestReader, DigestSubscribeDrawer, AlertRuleRow, AlertRuleForm), TanStack Query hooks, and i18n registration. Extend IntelligencePage with the two new tabs.

Purpose: The analyst-facing layer that makes digests and alerts actionable.
Output: 15 new/modified frontend files; intelligence hub has 4 working tabs; DIGEST-03 and ALERT-01 visible in browser.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/70-digests-alerts/70-CONTEXT.md
@.planning/phases/70-digests-alerts/70-UI-SPEC.md
@.planning/phases/70-digests-alerts/70-PATTERNS.md

<interfaces>
<!-- Extracted from codebase analogs — executor reads these directly -->

From frontend/src/pages/intelligence/IntelligencePage.tsx (the file being extended):
const [activeTab, setActiveTab] = useState<'reports' | 'signals'>('reports')
-- EXTEND to: useState<'reports' | 'signals' | 'digests' | 'alerts'>('reports')
const { isRTL } = useDirection()

  <div className="container mx-auto py-6" dir={isRTL ? 'rtl' : 'ltr'}>

From frontend/src/components/signals/SignalsQueue.tsx (structural analog for DigestsTab + AlertsTab):
interface SignalsQueueProps { dossierId?: string }
export function SignalsQueue({ dossierId }: SignalsQueueProps): React.ReactElement
-- DigestsTab({ dossierId?: string }), AlertsTab({ dossierId?: string }) follow same shape

From frontend/src/domains/signals/hooks/useSignals.ts (exact hook template for useDigests + useAlertRules):
return useQuery({
queryKey: signalKeys.list(filters),
queryFn: async (): Promise<Signal[]> => {
const { data, error } = await supabase.rpc('read_signals', { ... })
if (error) throw error
return (data as Signal[]) ?? []
},
staleTime: 60_000,
})

From frontend/src/components/signals/CaptureSignalForm.tsx (form pattern for AlertRuleForm):
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import { z } from 'zod'
import { useMutation, useQueryClient } from '@tanstack/react-query'

From frontend/src/i18n/index.ts (P69 registration pattern):
import enIntelligenceSignals from './en/intelligence-signals.json'
import arIntelligenceSignals from './ar/intelligence-signals.json'
// In resources: en: { ..., 'intelligence-signals': enIntelligenceSignals },
// ar: { ..., 'intelligence-signals': arIntelligenceSignals }
-- MIRROR this pattern for 'intelligence-digests' and 'intelligence-alerts'

From frontend/src/i18n/en/intelligence-signals.json (JSON shape template):
{
"tab": { "label": "Signals" },
"queue": { "emptyState": { "heading": "..." }, "emptyDossier": "..." },
"action": { ... }
}
-- MIRROR this structure for digests + alerts (use UI-SPEC copywriting for values)

Design token rules (CLAUDE.md — non-negotiable):

- All colors: var(--\*) tokens only (bg-bg, text-ink, border-line, bg-accent, etc.)
- Borders: 1px solid var(--line); no card shadows (hover row: var(--shadow-sm) only)
- Row heights: var(--row-h)
- Logical Tailwind only: ms-_, me-_, ps-_, pe-_, text-start, text-end, rounded-s-_, rounded-e-_
- No raw hex, no text-blue-500, no ml-_, mr-_, pl-_, pr-_
- No emoji in copy; sentence case; no marketing voice

UI-SPEC severity badge colors (semantic data — NOT decorative accent):
high/urgent: var(--danger) text on var(--danger-soft) bg
medium: var(--warn) text on var(--warn-soft) bg
low: var(--ok) text on var(--ok-soft) bg
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n registration + TanStack Query hooks</name>
  <files>
    frontend/src/i18n/en/intelligence-digests.json,
    frontend/src/i18n/ar/intelligence-digests.json,
    frontend/src/i18n/en/intelligence-alerts.json,
    frontend/src/i18n/ar/intelligence-alerts.json,
    frontend/src/i18n/index.ts,
    frontend/src/domains/signals/hooks/useDigests.ts,
    frontend/src/domains/signals/hooks/useAlertRules.ts
  </files>
  <read_first>
    frontend/src/i18n/index.ts — read lines 245-400 to find the P69 intelligence-signals registration; mirror pattern exactly for intelligence-digests and intelligence-alerts.
    frontend/src/i18n/en/intelligence-signals.json — read in full for JSON shape; mirrors for digests and alerts.
    frontend/src/i18n/ar/intelligence-signals.json — read in full for Arabic shape.
    frontend/src/domains/signals/hooks/useSignals.ts — read in full (39 lines) for useQuery hook template.
    .planning/phases/70-digests-alerts/70-UI-SPEC.md §"New i18n Namespaces" — namespace names: 'intelligence-digests' and 'intelligence-alerts' (note: UI-SPEC says 'digests' and 'alerts' but the PATTERNS.md uses 'intelligence-digests' — use 'intelligence-digests' and 'intelligence-alerts' to match the import pattern from PATTERNS.md).
    .planning/phases/70-digests-alerts/70-UI-SPEC.md §"Copywriting Contract" — exact EN copy for all labels and empty states.
  </read_first>
  <action>
    1. Create 4 JSON translation files:
      en/intelligence-digests.json — structure: { "tab": { "label": "Digests" }, "header": "Digests", "empty": { "noSubscriptions": { "heading": "No digest subscriptions yet", "body": "Subscribe to a dossier to receive a recurring rollup of its signals and activity." }, "noDraftPublished": { "heading": "No digests published yet", "body": "Your first digest will appear here after the next scheduled delivery, or generate one now." } }, "action": { "subscribe": "Subscribe to digest", "unsubscribe": "Unsubscribe", "generate": "Generate digest now", "generateNow": "Generate now", "editFrequency": "Edit frequency", "publish": "Publish digest", "discard": "Discard" }, "frequency": { "daily": "Daily", "weekly": "Weekly", "monthly": "Monthly" }, "chip": { "daily": "DAILY", "weekly": "WEEKLY", "monthly": "MONTHLY" }, "preview": { "banner": "Preview — not published", "published": "Digest published {{date}} at {{time}}" }, "error": { "load": "Couldn't load digests. Check your connection and retry.", "generate": "Couldn't generate digest. Check your connection and retry.", "publish": "Couldn't publish digest. Try again." }, "confirm": { "unsubscribe": { "heading": "Unsubscribe from this digest?", "body": "You'll stop receiving the recurring rollup for this dossier.", "confirm": "Unsubscribe", "cancel": "Keep subscription" } }, "section": { "signals": "Signals", "engagements": "Engagements", "commitments": "Commitments", "statusChanges": "Status changes" }, "subscriptions": "Your subscriptions" }
      ar/intelligence-digests.json — Arabic translations for all keys above.
      en/intelligence-alerts.json — structure: { "tab": { "label": "Alerts" }, "header": "Alerts", "empty": { "heading": "No alert rules", "body": "Add an alert to be notified when a new signal appears on a dossier you track." }, "action": { "add": "Add alert", "edit": "Edit alert rule", "delete": "Delete alert rule", "save": "Save", "cancel": "Cancel" }, "form": { "dossier": "Dossier", "condition": "New signal on this dossier", "severityFilter": "Severity filter", "severityHelper": "Only alert for signals at these severities. Leave unselected to alert on all signals.", "channels": "Channels", "active": "Alert active", "channelDesc": { "inApp": "In-app notification", "smtp": "Delivered to your account email via on-prem SMTP.", "webhook": "POST to configured endpoint. Deep-link only." } }, "chip": { "newSignal": "NEW SIGNAL", "high": "HIGH", "urgent": "URGENT" }, "toggle": { "active": "Active", "inactive": "Inactive" }, "error": { "load": "Couldn't load alert rules. Check your connection and retry.", "save": "Couldn't save alert rule. Try again." }, "confirm": { "delete": { "heading": "Delete this alert rule?", "body": "You'll no longer be notified of matching signals on this dossier.", "confirm": "Delete", "cancel": "Keep alert" } } }
      ar/intelligence-alerts.json — Arabic translations.

    2. Register both namespaces in frontend/src/i18n/index.ts — add 4 import statements and 4 resources entries (en + ar for each namespace). Mirror EXACTLY the P69 intelligence-signals pattern.

    3. useDigests.ts — TanStack Query hook calling read_digests INVOKER RPC:
      queryKey: ['intelligence-digests', filters]
      queryFn: supabase.rpc('read_digests', { p_dossier_id: filters.dossierId ?? null, p_limit: filters.limit ?? 50 })
      staleTime: 60_000
      Export useDigests(filters: DigestFilters = {}): UseQueryResult<Digest[]>
      DigestFilters: { dossierId?: string; limit?: number }
      Digest type: { id: string; dossier_id: string; dossier_type: string; frequency: string; period: string; summary: string; generated_at: string; published_at: string; clearance_level_at_generation: number }

    4. useAlertRules.ts — read hook + useMutation:
      useAlertRules(dossierId?: string): reads intelligence_alert_rules table directly via supabase client (owner-scoped via RLS; no service-role).
      useCreateAlertRule: useMutation that INSERTs to intelligence_alert_rules; invalidates ['intelligence-alert-rules'].
      useUpdateAlertRule: useMutation for PATCH.
      useDeleteAlertRule: useMutation for DELETE by id.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && pnpm --filter intake-frontend typecheck 2>&1 | tail -10</automated>
  </verify>
  <done>
    4 JSON translation files exist (en/ar for both namespaces).
    i18n/index.ts has 'intelligence-digests' and 'intelligence-alerts' registered in both en and ar resources.
    useDigests.ts and useAlertRules.ts typecheck cleanly.
    pnpm --filter intake-frontend build exits 0 (no compile error from new namespace imports).
  </done>
</task>

<task type="auto">
  <name>Task 2: Digest components (DigestsTab, DigestCard, DigestReader, DigestSubscribeDrawer) + IntelligencePage Digests wiring</name>
  <files>
    frontend/src/components/intelligence/DigestsTab.tsx,
    frontend/src/components/intelligence/DigestCard.tsx,
    frontend/src/components/intelligence/DigestReader.tsx,
    frontend/src/components/intelligence/DigestSubscribeDrawer.tsx,
    frontend/src/pages/intelligence/IntelligencePage.tsx
  </files>
  <read_first>
    frontend/src/pages/intelligence/IntelligencePage.tsx — read lines 140-420 in full for the current 2-tab structure; understand how to extend to 4 tabs without breaking existing Signals + Reports tabs.
    frontend/src/components/signals/SignalsQueue.tsx — read lines 1-80 for component shell, imports, empty state pattern, useDirection() usage. This is the EXACT structural template for DigestsTab.
    frontend/src/components/signals/SignalRow.tsx — read in full for row anatomy (severity badge, date formatting, design token usage). Analog for DigestCard.
    frontend/src/components/signals/EscalateSignalDialog.tsx — read in full for confirm-before-action dialog pattern. Analog for DigestReader publish confirmation.
    frontend/src/components/notifications/NotificationPanel.tsx — read lines 1-80 for Popover/drawer + tabs pattern. Analog for DigestSubscribeDrawer.
    .planning/phases/70-digests-alerts/70-UI-SPEC.md — read Surfaces 1-3, Copywriting Contract, and Definition of Done. Binding contract for digest component anatomy.
    frontend/src/lib/format-locale.ts — check if toFormatLocale function exists (for number formatting; MEMORY: never use Intl.NumberFormat('ar') directly — produces Latin digits in Chrome).
    frontend/src/lib/date-format.ts or similar — find formatDayFirst() utility (used throughout; UI-SPEC requires it for all dates).
    .planning/phases/70-digests-alerts/70-PATTERNS.md §DigestsTab, DigestCard, GenerateDigestButton — code excerpts for imports and component shells.
  </read_first>
  <action>
    Create all digest components in frontend/src/components/intelligence/ (PascalCase, per ESLint filename convention for components/).

    DigestsTab.tsx:
      Props: { dossierId?: string }. Import useDigests, DigestCard, DigestSubscribeDrawer, GenerateDigestButton. Use useTranslation('intelligence-digests') and useDirection(). Empty states from UI-SPEC (no mention of clearance). List sorted newest-first (already from RPC). Renders a compact "Your subscriptions" strip at top (collapsible, shows active subscriptions for this dossier). Renders published digests below. Loading: skeleton rows using var(--row-h). Error state: inline error with retry (UI-SPEC error copy).

    DigestCard.tsx:
      Props: { digest: Digest; onUnsubscribe: () => void; onGenerateNow: () => void }. Token-only styling: border-line on card, no shadow at rest, shadow-sm on hover (120ms ease-out via transition). Card anatomy from UI-SPEC Surface 1: Header row (DossierGlyph + dossier name 16px/600 + frequency chip UPPERCASE label + period string 12px ink-mute). Rollup count strip: 3 KPI cells in --t-kpi-value 36px (inline style), separated by 1px var(--line) vertical dividers. Section list: up to 5 signal titles with severity badges (danger-soft/warn-soft/ok-soft). Footer: generated timestamp (formatDayFirst()) + Unsubscribe ghost button end-aligned.

    DigestReader.tsx:
      Props: { digestId: string }. Fetch digest details from supabase (read_digests filtered by id or existing digest row). Anatomy from UI-SPEC Surface 2: back link, title, rollup header (3 KPI cells), 4 collapsible details sections. Empty section = heading + count 0, no body copy (indistinguishable-empty). Dates via formatDayFirst(). Mono formatting for T-relative values.

    DigestSubscribeDrawer.tsx:
      Props: { dossierId: string; dossierType: string; dossierName: string; isOpen: boolean; onClose: () => void }. Anatomy from UI-SPEC Surface 3a. Unsubscribed state: frequency radio chips (Daily/Weekly/Monthly) + Subscribe accent button + Cancel ghost. Subscribed state: frequency chip + Unsubscribe ghost link + confirmation dialog on click (UI-SPEC destructive confirmation copy).

    IntelligencePage.tsx (EXTEND for Digests — Alerts wired in Task 3):
      Change state type to 'reports' | 'signals' | 'digests' | 'alerts'.
      Add "Digests" tab button in the tab strip after Signals.
      Add tab content block: activeTab === 'digests' → DigestsTab.
      Import DigestsTab.
      Use useTranslation('intelligence-digests') for Digests tab label.
      Do NOT change any existing tab behavior (Reports, Signals untouched).

    Design token rules (enforce on every component):
      - All colors via var(--*) or @theme-mapped utilities: bg-bg, bg-surface, text-ink, text-ink-mute, text-ink-faint, border-line, bg-accent, bg-line-soft
      - No raw hex, no text-blue-500, no bg-red-500
      - Severity badges: bg-[var(--danger-soft)] text-[var(--danger)] / bg-[var(--warn-soft)] text-[var(--warn)] / bg-[var(--ok-soft)] text-[var(--ok)]
      - Logical Tailwind only: ms-*, me-*, ps-*, pe-*, text-start, text-end, rounded-s-*, rounded-e-*
      - Row heights: style={{ minHeight: 'var(--row-h)' }} on list rows
      - Drawer: inset-inline-end: 0 (not right: 0); shadow via var(--shadow-lg)
      - No .reverse() on arrays (RTL handled by dir=rtl)

  </action>
  <verify>
    <automated>pnpm --filter intake-frontend typecheck 2>&1 | tail -10</automated>
  </verify>
  <done>
    DigestsTab.tsx, DigestCard.tsx, DigestReader.tsx, DigestSubscribeDrawer.tsx exist in frontend/src/components/intelligence/ (PascalCase).
    IntelligencePage.tsx has "Digests" tab wired (Reports | Signals | Digests at minimum).
    pnpm --filter intake-frontend typecheck exits 0.
    grep -r "clearance\|filtered" frontend/src/components/intelligence/DigestsTab.tsx frontend/src/components/intelligence/DigestCard.tsx returns 0 matches.
  </done>
</task>

<task type="auto">
  <name>Task 3: Alerts components (AlertsTab, AlertRuleRow, AlertRuleForm) + IntelligencePage Alerts tab</name>
  <files>
    frontend/src/components/intelligence/AlertsTab.tsx,
    frontend/src/components/intelligence/AlertRuleRow.tsx,
    frontend/src/components/intelligence/AlertRuleForm.tsx,
    frontend/src/pages/intelligence/IntelligencePage.tsx
  </files>
  <read_first>
    frontend/src/components/intelligence/DigestsTab.tsx — read in full (just created in Task 2); structural template for AlertsTab.
    frontend/src/components/signals/SignalRow.tsx — read in full for row anatomy with severity badges. Analog for AlertRuleRow.
    frontend/src/components/signals/CaptureSignalForm.tsx — read lines 1-80 for React Hook Form + Zod + useMutation drawer pattern. Analog for AlertRuleForm.
    frontend/src/pages/intelligence/IntelligencePage.tsx — read in full (modified by Task 2); extend to add the Alerts tab.
    .planning/phases/70-digests-alerts/70-UI-SPEC.md — read Surfaces 1 (Alerts), 4 (AlertRuleForm), Copywriting Contract. Binding contract for alert component anatomy.
    .planning/phases/70-digests-alerts/70-PATTERNS.md §AlertRuleForm — code excerpt for the alertRuleSchema and form shell.
  </read_first>
  <action>
    Create all alerts components in frontend/src/components/intelligence/ (PascalCase).

    AlertsTab.tsx:
      Props: { dossierId?: string }. Import useAlertRules, AlertRuleRow, AlertRuleForm. Same structural pattern as DigestsTab. "Add alert" primary CTA in header. Empty state from UI-SPEC. List of AlertRuleRow, each var(--row-h). AlertRuleForm opens as a 480px end-edge drawer on "Add alert" click.

    AlertRuleRow.tsx:
      Props: { rule: AlertRule; onEdit: (rule) => void; onToggle: (id, active) => void; onDelete: (id) => void }. Row height var(--row-h). Anatomy from UI-SPEC Surface 1 Alerts: DossierGlyph (20px) + rule name/auto-label start-aligned; condition chip "New signal" (info-soft bg); severity filter chip (danger-soft); channel icons (bell/email/webhook glyphs, ink-mute); HeroUI Toggle (accent when active); delete icon-button with aria-label "Delete alert rule" (EN) / "حذف قاعدة التنبيه" (AR). Hover: row bg fades to line-soft at 120ms.

    AlertRuleForm.tsx:
      480px end-edge drawer. Props: { initialValues?: Partial<AlertRuleFormData>; onSave: (values) => void; onDelete?: (id) => void; onClose: () => void }. React Hook Form + Zod schema (alertRuleSchema from PATTERNS.md). 5 form fields (UI-SPEC Surface 4): Dossier (DossierTypeSelector + DossierSelector), Condition (read-only chip), Severity filter (toggle group High/Urgent), Channels (checkbox: in_app always enabled + not uncheckable; email; webhook with D-10 disclaimer "POST to configured endpoint. Deep-link only."), Active toggle. Footer: primary "Add alert" / "Save" button + Cancel ghost. Edit mode: destructive "Delete alert rule" link start-aligned, triggers inline alert dialog before delete (UI-SPEC destructive confirmation).

    IntelligencePage.tsx (EXTEND to add Alerts tab — picks up where Task 2 left off):
      Add "Alerts" tab button in the tab strip after Digests. Final tab strip: Reports | Signals | Digests | Alerts.
      Add tab content block: activeTab === 'alerts' → AlertsTab.
      Import AlertsTab.
      Use useTranslation('intelligence-alerts') for Alerts tab label.
      Do NOT change any existing tab behavior (Reports, Signals, Digests untouched).

    Design token rules (enforce on every component):
      - All colors via var(--*) or @theme-mapped utilities
      - No raw hex, no text-blue-500, no bg-red-500
      - Severity badges: bg-[var(--danger-soft)] text-[var(--danger)] / bg-[var(--warn-soft)] text-[var(--warn)]
      - Logical Tailwind only: ms-*, me-*, ps-*, pe-*, text-start, text-end, rounded-s-*, rounded-e-*
      - Row heights: style={{ minHeight: 'var(--row-h)' }} on list rows
      - Drawer: inset-inline-end: 0 (not right: 0); shadow via var(--shadow-lg)

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && pnpm --filter intake-frontend typecheck 2>&1 | tail -10 && pnpm --filter intake-frontend build 2>&1 | tail -5</automated>
  </verify>
  <done>
    AlertsTab.tsx, AlertRuleRow.tsx, AlertRuleForm.tsx exist in frontend/src/components/intelligence/ (PascalCase).
    IntelligencePage.tsx has all 4 tabs: Reports | Signals | Digests | Alerts.
    pnpm --filter intake-frontend typecheck exits 0.
    pnpm --filter intake-frontend build exits 0.
    grep -r "ml-\|mr-\|pl-\|pr-" frontend/src/components/intelligence/ returns 0 matches (no directional Tailwind classes).
  </done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                        | Description                                                                         |
| ----------------------------------------------- | ----------------------------------------------------------------------------------- |
| Frontend → read_digests INVOKER RPC             | Clearance gating enforced server-side; frontend never reads above-clearance content |
| AlertRuleForm → intelligence_alert_rules INSERT | Owner RLS; organization_id must come from authenticated user's profile              |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                              | Disposition | Mitigation Plan                                                                                                                            |
| --------- | ---------------------- | ---------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| T-70-20   | Information Disclosure | DigestCard/DigestReader showing clearance-filtered status to user      | mitigate    | Empty sections show generic copy + 0 count only (UI-SPEC: "No body. No filtered by clearance text.") — enforced in component action        |
| T-70-21   | Elevation of Privilege | AlertRuleForm: user can submit a webhook channel to leak alert content | mitigate    | D-10 disclaimer shown in webhook channel description; webhook adapter enforces contract server-side regardless of what the form sends      |
| T-70-22   | Information Disclosure | i18n namespace not registered — Arabic falls back to English keys      | mitigate    | i18n registration in index.ts is mandatory first step (Task 1); CI Lint guard (P68 REMED-06) catches unregistered namespaces at build time |

</threat_model>

<verification>
After all 3 tasks:
  pnpm --filter intake-frontend typecheck && pnpm --filter intake-frontend build
  → both exit 0

grep -rn "text-blue-500\|text-red-500\|text-green-500\|#[0-9a-fA-F]" frontend/src/components/intelligence/
→ 0 matches (design token compliance)

grep -rn "\" ml-\|\" mr-\|\" pl-\|\" pr-" frontend/src/components/intelligence/
→ 0 matches (logical Tailwind only)

grep -c "intelligence-digests\|intelligence-alerts" frontend/src/i18n/index.ts
→ >= 4 (2 namespace keys registered in both en and ar)

grep -c "clearance\|filtered" frontend/src/components/intelligence/DigestsTab.tsx frontend/src/components/intelligence/DigestCard.tsx
→ 0 matches (no clearance-disclosure copy in UI)
</verification>

<success_criteria>
Plan complete when:

- 7 components exist in frontend/src/components/intelligence/ (PascalCase, ESLint-clean)
- 2 hooks exist in frontend/src/domains/signals/hooks/ (useDigests.ts, useAlertRules.ts)
- 4 i18n JSON files exist, both namespaces registered in index.ts
- IntelligencePage.tsx has 4 tabs in order: Reports | Signals | Digests | Alerts
- typecheck and build both exit 0
- No raw hex, no directional Tailwind, no clearance disclosure in copy (greps clean)
  </success_criteria>

<output>
Create `.planning/phases/70-digests-alerts/70-06-SUMMARY.md` when done
</output>
