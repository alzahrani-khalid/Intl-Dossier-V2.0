---
phase: 70-digests-alerts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/intelligence/subscriptions.test.ts
  - backend/tests/intelligence/digest-cron.integration.test.ts
  - backend/tests/intelligence/generate-digest.integration.test.ts
  - backend/tests/intelligence/alert-rules.test.ts
  - backend/tests/intelligence/alert-fanout.integration.test.ts
  - backend/tests/intelligence/channel-adapter.test.ts
  - backend/tests/intelligence/webhook-payload-contract.test.ts
  - e2e/intelligence-digests.spec.ts
autonomous: false
requirements:
  - DIGEST-01
  - DIGEST-02
  - DIGEST-03
  - DIGEST-04
  - ALERT-01
  - ALERT-02
  - ALERT-03
  - ALERT-04

must_haves:
  truths:
    - 'Wave 0 test scaffold exists: 7 backend stubs + 1 E2E stub covering all 8 REQ-IDs'
    - 'nodemailer and @types/nodemailer are installed in backend (per D-08 on-prem SMTP requirement)'
    - 'pg (node-postgres) is confirmed absent from backend/ (not direct or transitive) and MUST be installed in this wave for pg LISTEN in the alert worker (per D-05 / research resolution)'
    - 'pnpm --filter intake-backend test -- --run tests/intelligence/ exits 0 with all tests pending/passing'
    - 'D-01 digest content plane coverage is represented by digest-cron.integration.test.ts stub'
    - 'D-05 alert trigger is represented by alert-fanout.integration.test.ts stub'
    - 'D-08 channel adapter isolation is represented by channel-adapter.test.ts stub'
    - 'D-10 webhook payload contract is asserted in webhook-payload-contract.test.ts stub'

  artifacts:
    - path: 'backend/tests/intelligence/subscriptions.test.ts'
      provides: 'DIGEST-01 unit test stub'
    - path: 'backend/tests/intelligence/digest-cron.integration.test.ts'
      provides: 'DIGEST-02 integration test stub'
    - path: 'backend/tests/intelligence/generate-digest.integration.test.ts'
      provides: 'DIGEST-04 integration test stub'
    - path: 'backend/tests/intelligence/alert-rules.test.ts'
      provides: 'ALERT-01 unit test stub'
    - path: 'backend/tests/intelligence/alert-fanout.integration.test.ts'
      provides: 'ALERT-02 integration test stub'
    - path: 'backend/tests/intelligence/channel-adapter.test.ts'
      provides: 'ALERT-03 unit test stub'
    - path: 'backend/tests/intelligence/webhook-payload-contract.test.ts'
      provides: 'ALERT-04 unit test stub'
    - path: 'e2e/intelligence-digests.spec.ts'
      provides: 'DIGEST-03 Playwright E2E stub'

  key_links:
    - from: 'backend/tests/intelligence/'
      to: '70-VALIDATION.md Wave 0 Requirements'
      via: 'all 8 REQ-ID test files mapped'
    - from: 'nodemailer'
      to: 'backend/src/adapters/intelligence/smtp-adapter.ts'
      via: 'npm install + package.json entry'
---

<objective>
Wave 0: Establish the test scaffold and install missing dependencies before any implementation begins. This plan creates all 8 test stub files from the VALIDATION.md Wave 0 checklist, installs nodemailer for the on-prem SMTP adapter (RF-1), and installs pg (confirmed absent from backend) for LISTEN/NOTIFY alert triggering (RF-5). Subsequent plans implement against these stubs.

Purpose: Nyquist gate — no 3 consecutive tasks without a runnable test; all REQ-IDs have a test file before any implementation.
Output: 8 test stub files, updated backend/package.json with nodemailer, confirmed pg dep, human-verified package legitimacy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-digests-alerts/70-CONTEXT.md
@.planning/phases/70-digests-alerts/70-RESEARCH.md
@.planning/phases/70-digests-alerts/70-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify nodemailer + pg package legitimacy before install</name>
  <read_first>
    .planning/phases/70-digests-alerts/70-RESEARCH.md §"Package Legitimacy Audit" — both packages are [ASSUMED]; researcher could not run slopcheck. Must verify manually before install.
  </read_first>
  <what-built>
    Before installing nodemailer or pg, verify their legitimacy on the npm registry. The researcher tagged both as [ASSUMED] — slopcheck was unavailable at research time.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/nodemailer — confirm: author = andris9/Nodemailer Team, first published > 10 years ago, weekly downloads > 10M. Check for any security advisories.
    2. Open https://www.npmjs.com/package/@types/nodemailer — confirm it is the DefinitelyTyped package, weekly downloads > 5M, no security advisories.
    3. Open https://www.npmjs.com/package/pg — confirm it is the node-postgres package by brianc, first published ~12 years ago, weekly downloads > 10M. Check for advisories.
    4. Open https://www.npmjs.com/package/@types/pg — confirm DefinitelyTyped, no advisories.
    All four should appear as well-known, long-lived packages with no active CVEs.
  </how-to-verify>
  <resume-signal>Type "packages verified" after confirming all 4 packages are legitimate on npmjs.com, or describe any concerns found.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install nodemailer + pg backend deps and create test stubs</name>
  <read_first>
    backend/package.json — to understand current dep format and confirm nodemailer/pg are absent.
    backend/vitest.config.ts — to confirm test file glob pattern (ensure tests/intelligence/ is picked up).
    backend/tests/ — scan for an existing test file to use as structural template (e.g. any .test.ts in tests/).
    .planning/phases/70-digests-alerts/70-VALIDATION.md §"Wave 0 Requirements" — the 8 test file names.
    .planning/phases/70-digests-alerts/70-RESEARCH.md §"Validation Architecture" — per-requirement test command map.
    frontend/e2e/ or tests/ — find an existing Playwright spec to use as structural template for e2e/intelligence-digests.spec.ts.
  </read_first>
  <action>
    Step 1 — Install missing backend dependencies using pnpm (NOT npx):
      pnpm --filter intake-backend add nodemailer
      pnpm --filter intake-backend add -D @types/nodemailer
      pnpm --filter intake-backend add pg
      pnpm --filter intake-backend add -D @types/pg
    After install, confirm package.json entries exist for all four packages.

    Step 2 — Create backend/tests/intelligence/ directory if it doesn't exist.

    Step 3 — Create 7 backend test stub files. Each must:
      - Import vitest (describe/it/expect).
      - Have ONE describe block per REQ-ID it covers.
      - Have at least one it.todo() call or a pending test (no empty describe — vitest won't run with 0 tests).
      - Have a comment at the top: "// Wave 0 stub — implementation fills tests in later waves".
      - Cover exactly the REQ-ID in its filename:
          subscriptions.test.ts → DIGEST-01
          digest-cron.integration.test.ts → DIGEST-02
          generate-digest.integration.test.ts → DIGEST-04
          alert-rules.test.ts → ALERT-01
          alert-fanout.integration.test.ts → ALERT-02
          channel-adapter.test.ts → ALERT-03
          webhook-payload-contract.test.ts → ALERT-04

    Step 4 — Create e2e/intelligence-digests.spec.ts Playwright stub:
      - Import { test, expect } from @playwright/test.
      - One test.todo() for "DIGEST-03: Digests tab renders published digests".
      - Header comment: "// Wave 0 stub for DIGEST-03 — implemented after frontend ships in wave 4+5".

    Step 5 — Confirm pg availability:
      Run: node -e "require('pg'); console.log('pg ok')" in backend directory. If it throws, the install from Step 1 handles it. Document the result.

    Critical: Do NOT use npx for anything. Use pnpm exclusively.
    Critical: Test files must be created in backend/tests/intelligence/ (not src/), matching the vitest config glob.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && pnpm --filter intake-backend test -- --run tests/intelligence/ 2>&1 | tail -20</automated>
  </verify>
  <done>
    pnpm --filter intake-backend test -- --run tests/intelligence/ exits 0 (all stubs pending/passing).
    package.json contains "nodemailer" and "pg" in dependencies, "@types/nodemailer" and "@types/pg" in devDependencies.
    8 test stub files exist: 7 in backend/tests/intelligence/, 1 in e2e/.
    pnpm --filter intake-backend typecheck exits 0 (no new type errors from stub files).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                         |
| ---------------------------- | --------------------------------------------------- |
| npm registry → local install | External packages enter the backend dependency tree |

## STRIDE Threat Register

| Threat ID | Category  | Component                                                  | Disposition | Mitigation Plan                                                                                                 |
| --------- | --------- | ---------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------- |
| T-70-01   | Tampering | npm install (nodemailer, @types/nodemailer, pg, @types/pg) | mitigate    | checkpoint:human-verify before install; human confirms npmjs.com legitimacy for all 4 packages (both [ASSUMED]) |
| T-70-SC   | Tampering | npm/pip/cargo installs                                     | mitigate    | Task 1 blocking human checkpoint verifies all [ASSUMED] packages on npmjs.com before Task 2 installs them       |

</threat_model>

<verification>
Run after all tasks complete:
  pnpm --filter intake-backend test -- --run tests/intelligence/
  → should print 7+ test suite files, all pending or passing, exit 0

ls backend/tests/intelligence/
→ should list 7 .test.ts files

node -e "require('nodemailer');require('pg');console.log('deps ok')" from backend directory (use pnpm exec node)
→ should print "deps ok"
</verification>

<success_criteria>
Wave 0 complete when:

- 8 test stubs exist (7 backend + 1 E2E) covering all 8 REQ-IDs
- nodemailer, @types/nodemailer, pg, @types/pg all in backend/package.json
- pnpm --filter intake-backend test -- --run tests/intelligence/ exits 0
- typecheck passes
  </success_criteria>

<output>
Create `.planning/phases/70-digests-alerts/70-01-SUMMARY.md` when done
</output>
