---
phase: 70-digests-alerts
plan: 07
type: execute
wave: 6
depends_on:
  - 70-05
  - 70-06
files_modified:
  - frontend/src/components/intelligence/GenerateDigestButton.tsx
  - frontend/src/routes/_protected/dossiers/countries/$id/digests.tsx
  - frontend/src/routes/_protected/dossiers/organizations/$id/digests.tsx
  - frontend/src/routes/_protected/dossiers/forums/$id/digests.tsx
  - frontend/src/routes/_protected/dossiers/topics/$id/digests.tsx
  - frontend/src/routes/_protected/dossiers/working_groups/$id/digests.tsx
  - frontend/src/routes/_protected/dossiers/persons/$id/digests.tsx
  - frontend/src/routes/_protected/dossiers/elected-officials/$id/digests.tsx
  - frontend/src/routes/_protected/engagements/$engagementId/digests.tsx
  - e2e/intelligence-digests.spec.ts
  - backend/src/api/intelligence.ts
  - backend/src/index.ts
autonomous: false
requirements:
  - DIGEST-03
  - DIGEST-04
  - ALERT-02
  - ALERT-03
  - ALERT-04

must_haves:
  truths:
    - 'GenerateDigestButton.tsx implements D-14 HITL: generate → preview card → confirm → publish_digest RPC; never mentions clearance in UI copy'
    - '8 dossier digests.tsx route files follow the exact same TanStack Router lazy pattern as the P69 signals.tsx routes — DigestsTab with dossierId passed (D-12)'
    - 'intelligence-digest.ts and intelligence-alerts.ts routers are mounted under /api/intelligence/ in backend/src/api/intelligence.ts (D-11)'
    - 'registerIntelligenceDigestScheduler() and startAlertListener() are called inside startServer() in backend/src/index.ts — after registerDigestScheduler() (D-02)'
    - 'e2e/intelligence-digests.spec.ts has real Playwright assertions for DIGEST-03: subscribe → Digests tab shows subscription → generated digest card renders (DIGEST-03)'
    - 'UAT confirmed: two clearance-level accounts subscribe to same dossier; lower-clearance digest contains fewer signals (DIGEST-02 live verification)'
    - 'UAT confirmed: seeded intelligence_event triggers alert fan-out to all 3 channels; webhook POST body contains only deepLink + genericLabel (D-10 live verification)'
    - 'UAT confirmed: generate_digest RPC preview rendered in browser; publish_digest commits the row; Digests tab shows new digest (D-14 live verification)'

  artifacts:
    - path: 'frontend/src/components/intelligence/GenerateDigestButton.tsx'
      provides: 'HITL generate→preview→publish flow (D-14 affordance)'
      exports: ['GenerateDigestButton']
    - path: 'frontend/src/routes/_protected/dossiers/countries/$id/digests.tsx'
      provides: 'Country Digests tab (D-12 entry point)'
    - path: 'frontend/src/routes/_protected/engagements/$engagementId/digests.tsx'
      provides: 'Engagement Digests tab (D-12 entry point)'
    - path: 'backend/src/api/intelligence.ts'
      provides: 'intelligence-digest and intelligence-alerts routes mounted under /api/intelligence'
      contains: 'intelligence-digest'
    - path: 'backend/src/index.ts'
      provides: 'registerIntelligenceDigestScheduler + startAlertListener called in startServer'
      contains: 'registerIntelligenceDigestScheduler'
    - path: 'e2e/intelligence-digests.spec.ts'
      provides: 'Playwright E2E for DIGEST-03 subscribe + digest card flow'

  key_links:
    - from: 'GenerateDigestButton'
      to: "supabase.rpc('generate_digest', { p_dossier_id, p_period })"
      via: 'generateMutation.mutateAsync() → setPreview(data)'
    - from: 'GenerateDigestButton'
      to: "supabase.rpc('publish_digest', { ... })"
      via: "publishMutation.mutateAsync(preview) → queryClient.invalidateQueries(['intelligence-digests'])"
    - from: 'backend/src/index.ts startServer()'
      to: 'registerIntelligenceDigestScheduler() + startAlertListener()'
      via: 'sequential calls after registerDigestScheduler() completes'
    - from: 'backend/src/api/intelligence.ts'
      to: 'intelligence-digest.ts router'
      via: "intelligenceRouter.use('/digest', intelligenceDigestRouter)"
---

<objective>
Complete Phase 70 wiring: ship GenerateDigestButton (D-14 HITL affordance), add 8 dossier-page digests route files (D-12 entry points), mount the two new backend routers and startup hooks, fill the E2E stub with real assertions, and run live UAT to verify clearance differentiation, alert fan-out, webhook contract, and the generate-digest HITL flow.

Purpose: Plans 01–06 built every layer; this plan connects them end-to-end and proves them live on staging.
Output: 10 new/modified files; Phase 70 success criteria 1–5 verified on staging zkrcjzdemdmwhearhfgg.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/70-digests-alerts/70-CONTEXT.md
@.planning/phases/70-digests-alerts/70-UI-SPEC.md
@.planning/phases/70-digests-alerts/70-PATTERNS.md

<interfaces>
<!-- Key contracts for the executor — extracted from codebase -->

From frontend/src/routes/\_protected/dossiers/countries/$id/signals.tsx (EXACT pattern for all 8 digests.tsx routes):

```typescript
import { createFileRoute } from '@tanstack/react-router'
import type { ReactElement } from 'react'
import { lazy, Suspense } from 'react'
import { TabSkeleton } from '@/components/workspace/TabSkeleton'

const DossierSignalsTab = lazy(() =>
  import('@/components/dossier/tabs/DossierSignalsTab').then((m) => ({
    default: m.DossierSignalsTab,
  })),
)

export const Route = createFileRoute('/_protected/dossiers/countries/$id/signals')({
  component: CountrySignalsRoute,
})

function CountrySignalsRoute(): ReactElement {
  const { id } = Route.useParams()
  return (
    <Suspense fallback={<TabSkeleton type="list" />}>
      <DossierSignalsTab dossierId={id} />
    </Suspense>
  )
}
```

Mirror this EXACTLY for each digests.tsx route. Change:

- Import path: '@/components/intelligence/DigestsTab' (named export DigestsTab)
- Route path: '/\_protected/dossiers/{type}/$id/digests'
- Component name: e.g. CountryDigestsRoute
- useParams key: id (or engagementId for the engagements route)

From frontend/src/routes/\_protected/engagements/$engagementId/signals.tsx (engagement variant):
  Route.useParams() returns { engagementId }
  createFileRoute('/_protected/engagements/$engagementId/signals')
→ mirror as createFileRoute('/\_protected/engagements/$engagementId/digests')

From frontend/src/components/intelligence/GenerateDigestButton.tsx pattern (70-PATTERNS.md §GenerateDigestButton):

```typescript
import { useState } from 'react'
import { useMutation, useQueryClient } from '@tanstack/react-query'
import { supabase } from '@/lib/supabase'
// Uses INVOKER RPC — caller JWT enforces clearance automatically

interface Props {
  dossierId: string
  period: 'daily' | 'weekly' | 'monthly'
}

interface DigestPreview {
  signals: SignalSummary[]
  engagements: unknown[]
  commitments: unknown[]
  status_changes: unknown[]
  clearance_level: number
}

export function GenerateDigestButton({ dossierId, period }: Props): React.ReactElement {
  const [preview, setPreview] = useState<DigestPreview | null>(null)
  const queryClient = useQueryClient()

  const generateMutation = useMutation({
    mutationFn: async () => {
      const { data, error } = await supabase.rpc('generate_digest', {
        p_dossier_id: dossierId,
        p_period: period,
      })
      if (error) throw error
      return data as DigestPreview
    },
    onSuccess: (data) => setPreview(data),
  })

  const publishMutation = useMutation({
    mutationFn: async (preview: DigestPreview) => {
      const { data, error } = await supabase.rpc('publish_digest', {
        p_dossier_id: dossierId,
        p_period: period,
        p_summary: renderSummaryText(preview),
        p_clearance_level_at_generation: preview.clearance_level,
      })
      if (error) throw error
      return data
    },
    onSuccess: () => {
      setPreview(null)
      void queryClient.invalidateQueries({ queryKey: ['intelligence-digests'] })
    },
  })
  // Renders: "Generate digest now" button → preview modal → "Publish" / "Discard" buttons
}
```

From backend/src/api/intelligence.ts (current file to EXTEND):
import { Router } from 'express'
import { IntelligenceService } from '../services/intelligence.service'
// intelligenceRouter.use('/digest', intelligenceDigestRouter)
// intelligenceRouter.use('/alerts', intelligenceAlertsRouter)
Mount the two new sub-routers AFTER existing routes.

From backend/src/index.ts (startup sequence — EXTEND lines 130–162):
After: await registerDigestScheduler()
Add:
await registerIntelligenceDigestScheduler()
await startAlertListener()
Import both from their respective files at top of index.ts.

From e2e/intelligence-digests.spec.ts (Wave 0 stub — FILL with real assertions):
This is an existing stub file. Read it first, then replace the pending stubs with
real Playwright test steps targeting the staging URL from process.env.E2E_BASE_URL.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GenerateDigestButton + 8 dossier digests route files + E2E fill + backend wiring</name>
  <files>
    frontend/src/components/intelligence/GenerateDigestButton.tsx,
    frontend/src/routes/_protected/dossiers/countries/$id/digests.tsx,
    frontend/src/routes/_protected/dossiers/organizations/$id/digests.tsx,
    frontend/src/routes/_protected/dossiers/forums/$id/digests.tsx,
    frontend/src/routes/_protected/dossiers/topics/$id/digests.tsx,
    frontend/src/routes/_protected/dossiers/working_groups/$id/digests.tsx,
    frontend/src/routes/_protected/dossiers/persons/$id/digests.tsx,
    frontend/src/routes/_protected/dossiers/elected-officials/$id/digests.tsx,
    frontend/src/routes/_protected/engagements/$engagementId/digests.tsx,
    e2e/intelligence-digests.spec.ts,
    backend/src/api/intelligence.ts,
    backend/src/index.ts
  </files>
  <read_first>
    frontend/src/routes/_protected/dossiers/countries/$id/signals.tsx — read in full (29 lines); this is the EXACT template to mirror for all 8 digests.tsx route files.
    frontend/src/routes/_protected/engagements/$engagementId/signals.tsx — read in full; this is the template for the engagement digests route (uses engagementId param, not id).
    frontend/src/components/signals/EscalateSignalDialog.tsx — read lines 1–80 for the confirm-before-action dialog pattern (preview modal anatomy for GenerateDigestButton).
    e2e/intelligence-digests.spec.ts — read in full; replace the Wave 0 pending stubs with real Playwright assertions.
    backend/src/api/intelligence.ts — read in full; identify where to append the two new router.use() mounts.
    backend/src/index.ts — read lines 1–20 (imports) and lines 130–162 (startServer body); understand where to add the new imports and startup calls.
    .planning/phases/70-digests-alerts/70-PATTERNS.md §GenerateDigestButton — full code excerpt (mirrors EscalateSignalDialog pattern).
    .planning/phases/70-digests-alerts/70-VALIDATION.md §UAT Scenarios — scenarios 3, 4, 5 describe the live tests this plan's UAT checkpoint will run.
    .planning/phases/70-digests-alerts/70-UI-SPEC.md §Surface 5 (Generate Digest HITL) — button label, preview card anatomy, confirm/discard copy.
  </read_first>
  <action>
    1. frontend/src/components/intelligence/GenerateDigestButton.tsx:
      Props: { dossierId: string; period: 'daily' | 'weekly' | 'monthly' }.
      State: preview: DigestPreview | null (null = not yet generated; non-null = preview visible).
      generateMutation: calls supabase.rpc('generate_digest', { p_dossier_id: dossierId, p_period: period }); on success sets preview.
      publishMutation: calls supabase.rpc('publish_digest', { p_dossier_id: dossierId, p_period: period, p_summary: renderSummaryText(preview), p_clearance_level_at_generation: preview.clearance_level }); on success clears preview + invalidates ['intelligence-digests'] query.
      renderSummaryText(preview): returns a short EN string like "Weekly digest: N signals, M engagements, K commitments."
      Rendering:
        When preview is null: render single button using .btn-primary (from prototype app.css) labeled with t('action.generateNow') from 'intelligence-digests' namespace.
        When preview is non-null: render a preview card (bg-surface border border-line rounded-[var(--radius)]) with:
          - Banner strip: t('preview.banner') in mono uppercase — bg-[var(--warn-soft)] text-[var(--warn)] — "PREVIEW — NOT PUBLISHED"
          - KPI row: 3 cells (signals, engagements, commitments count) in --t-kpi-value 36px
          - Footer: "Publish digest" .btn-primary button + "Discard" .btn-ghost button (end-aligned)
          - No mention of clearance level in any copy.
        publishMutation.isPending: disable both buttons, show loading spinner inline.
        generateMutation.isError: show inline error in var(--danger) text using t('error.generate').
        publishMutation.isError: show inline error using t('error.publish').
      Design tokens (non-negotiable): all colors via var(--*), border-line, no raw hex, logical Tailwind only (ps-*, ms-* etc.), no emoji in copy.

    2. Eight dossier digests.tsx route files — create all 8 using the signals.tsx template pattern EXACTLY:
      a. frontend/src/routes/_protected/dossiers/countries/$id/digests.tsx
         Route: '/_protected/dossiers/countries/$id/digests'
         Component: CountryDigestsRoute, params: { id }
      b. frontend/src/routes/_protected/dossiers/organizations/$id/digests.tsx
         Route: '/_protected/dossiers/organizations/$id/digests'
         Component: OrganizationDigestsRoute, params: { id }
      c. frontend/src/routes/_protected/dossiers/forums/$id/digests.tsx
         Route: '/_protected/dossiers/forums/$id/digests'
         Component: ForumDigestsRoute, params: { id }
      d. frontend/src/routes/_protected/dossiers/topics/$id/digests.tsx
         Route: '/_protected/dossiers/topics/$id/digests'
         Component: TopicDigestsRoute, params: { id }
      e. frontend/src/routes/_protected/dossiers/working_groups/$id/digests.tsx
         Route: '/_protected/dossiers/working_groups/$id/digests'
         Component: WorkingGroupDigestsRoute, params: { id }
      f. frontend/src/routes/_protected/dossiers/persons/$id/digests.tsx
         Route: '/_protected/dossiers/persons/$id/digests'
         Component: PersonDigestsRoute, params: { id }
      g. frontend/src/routes/_protected/dossiers/elected-officials/$id/digests.tsx
         Route: '/_protected/dossiers/elected-officials/$id/digests'
         Component: ElectedOfficialDigestsRoute, params: { id }
      h. frontend/src/routes/_protected/engagements/$engagementId/digests.tsx
         Route: '/_protected/engagements/$engagementId/digests'
         Component: EngagementDigestsRoute, params: { engagementId } — pass as dossierId to DigestsTab

      Each file is identical in structure to the signals.tsx analog except:
        - lazy import is `import('@/components/intelligence/DigestsTab').then((m) => ({ default: m.DigestsTab }))`
        - Component name matches the route (e.g. CountryDigestsRoute)
        - dossierId prop passed to DigestsTab is the route param

    3. e2e/intelligence-digests.spec.ts — read the existing Wave 0 stub, then fill with real Playwright assertions:
      Test 1 — Subscribe to digest (DIGEST-01 + DIGEST-03):
        Navigate to a country dossier detail page on staging.
        Find and click the "Subscribe to digest" button (the DigestSubscribeDrawer trigger).
        Select "Weekly" frequency radio chip.
        Click "Subscribe".
        Assert: drawer closes (not visible), navigate to /intelligence Digests tab, assert DigestCard for the test dossier is visible (role=listitem, contains the dossier name text).
      Test 2 — Digests tab renders in IntelligencePage (DIGEST-03):
        Navigate to /intelligence.
        Click the "Digests" tab.
        Assert: DigestsTab section is visible (tab panel, role=tabpanel).
        Assert: no raw text "clearance" appears on page (indistinguishable-empty enforcement).
      Test 3 — Alert tab renders (ALERT-01):
        Click the "Alerts" tab.
        Assert: "Add alert" button is visible.
      Use process.env.E2E_BASE_URL ?? 'http://localhost:5173' as base URL.
      Use stored auth state (page.context().storageState()) per existing E2E test pattern.

    4. backend/src/api/intelligence.ts — EXTEND (do not touch existing routes):
      Add two import lines at the top:
        import intelligenceDigestRouter from './intelligence-digest'
        import intelligenceAlertsRouter from './intelligence-alerts'
      Add two router.use() calls AFTER all existing routes, before `export default router`:
        intelligenceRouter.use('/digest', intelligenceDigestRouter)
        intelligenceRouter.use('/alerts', intelligenceAlertsRouter)
      This exposes /api/intelligence/digest/* and /api/intelligence/alerts/* endpoints.

    5. backend/src/index.ts — EXTEND startServer() function:
      Add two import lines at the top of the file:
        import { registerIntelligenceDigestScheduler } from './queues/intelligence-digest.scheduler'
        import { startAlertListener } from './queues/intelligence-alert.worker'
      Inside startServer(), immediately after `await registerDigestScheduler()` (line ~149), add:
        // Register intelligence digest scheduler (daily/weekly/monthly per-subscriber cron)
        await registerIntelligenceDigestScheduler()

        // Start intelligence alert listener (pg LISTEN + BullMQ alert worker)
        await startAlertListener()
      The call order matters: registerDigestScheduler must complete before registerIntelligenceDigestScheduler
      (both share the notificationQueue; the existing queue worker must be initialized first).

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && pnpm --filter intake-frontend typecheck 2>&1 | tail -10 && pnpm --filter intake-backend typecheck 2>&1 | tail -10</automated>
  </verify>
  <done>
    GenerateDigestButton.tsx exists in frontend/src/components/intelligence/ with generate + publish mutations.
    8 dossier digests.tsx route files exist (PascalCase component names, correct route paths).
    e2e/intelligence-digests.spec.ts has real Playwright test bodies (no pending stubs).
    backend/src/api/intelligence.ts has `/digest` and `/alerts` sub-router mounts.
    backend/src/index.ts imports and calls registerIntelligenceDigestScheduler() + startAlertListener() in startServer().
    pnpm --filter intake-frontend typecheck exits 0.
    pnpm --filter intake-backend typecheck exits 0.
    pnpm --filter intake-backend build exits 0 (routes resolve; no circular import).
    grep -c "registerIntelligenceDigestScheduler\|startAlertListener" backend/src/index.ts returns 4 (2 imports + 2 calls).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Live UAT — clearance differentiation, alert fan-out, webhook contract, generate-digest HITL</name>
  <read_first>
    .planning/phases/70-digests-alerts/70-VALIDATION.md §Manual UAT Scenarios — scenarios 1–5 are the test protocol.
    .planning/phases/70-digests-alerts/70-CONTEXT.md §"Carried forward" — indistinguishable-empty mandate (D-09), webhook payload hard contract (D-10).
    CLAUDE.md §"Test Credentials for Browser/Chrome MCP" — staging test accounts.
  </read_first>
  <what-built>
    Plans 01–07 have been executed. The full Phase 70 stack is live on staging zkrcjzdemdmwhearhfgg:
    - Database migration applied (70-03): 4 new tables, 4 RPCs, 1 trigger
    - Backend: intelligence digest scheduler + alert listener running
    - Backend: /api/intelligence/digest and /api/intelligence/alerts routes mounted
    - Frontend: IntelligencePage has 4 tabs (Reports | Signals | Digests | Alerts)
    - Frontend: 8 dossier types have per-dossier Digests tab (D-12 entry points)
    - Frontend: GenerateDigestButton on DigestsTab (D-14 HITL)

    The orchestrator now runs these UAT verification steps:

    UAT-1: Clearance differentiation (DIGEST-02 + Success Criterion 4)
      Log in as User A (clearance_level=1) and User B (clearance_level=3) on staging.
      Both users subscribe to the same country dossier with "Weekly" frequency.
      Seed two intelligence_event rows into that dossier via mcp__supabase__execute_sql:
        Row 1: sensitivity_level=1 (accessible to both users)
        Row 2: sensitivity_level=3 (accessible only to User B)
      Trigger digest generation manually: call generate_digest RPC for both users.
      Verify: User A's digest preview shows only the sensitivity-1 event.
      Verify: User B's digest preview shows both events.
      Restore: delete the two seeded intelligence_event rows.

    UAT-2: Alert fan-out across 3 channels (ALERT-02 + ALERT-03 + Success Criterion 2)
      As User C (clearance_level >= 1): create an alert rule on the same dossier
        channels: ['in_app', 'smtp', 'webhook'].
      Seed one intelligence_event (sensitivity=1, severity='high') on that dossier.
      Wait up to 30 seconds for the pg_notify → backend listener → BullMQ → adapter chain.
      Verify in_app: select * from notifications where user_id=User_C.id order by created_at desc limit 1 — confirm 1 row with action_url pointing to the dossier.
      Verify SMTP: select * from intelligence_email_queue where recipient_email=User_C.email order by created_at desc limit 1 — confirm 1 row (NOT email_queue — confirms on-prem path, RF-1).
      Verify webhook: if a test webhook endpoint is configured at INTELLIGENCE_WEBHOOK_URL, inspect the POST body: confirm it contains ONLY text (= genericLabel) and potentialAction[0].targets[0].uri (= deepLink). Confirm body contains NO signal title, NO dossier name, NO severity field.
      Coalescing guard: seed a second intelligence_event within 5 minutes. Confirm BullMQ dedup (same jobId 'alert:{event_id}:check') — only 1 SMTP row, not 2.

    UAT-3: Webhook payload D-10 hard contract (ALERT-04 + Success Criterion 3)
      This step is embedded in UAT-2 webhook verification above. Document the actual outbound
      POST body captured (or confirmed via intelligence_email_queue if webhook URL is not
      configured in staging). Confirm the body has no field matching signal title or dossier name.

    UAT-4: generate_digest HITL preview → confirm → publish (DIGEST-04 + Success Criterion 5)
      As User B (clearance_level=3): navigate to the /intelligence Digests tab.
      Click "Generate digest now" on the subscribed dossier.
      Confirm: preview card renders with KPI counts (signals, engagements, commitments).
      Confirm: "PREVIEW — NOT PUBLISHED" banner is visible.
      Click "Publish digest".
      Confirm: preview card disappears.
      Confirm: a new DigestCard appears in the Digests tab with published_at timestamp.
      Verify DB: select published_at from intelligence_digest where subscriber_id=User_B.id order by created_at desc limit 1 — confirm NOT NULL.

    UAT-5: Indistinguishable empty on clearance denial (D-09 + P68 indistinguishable-empty)
      As User A (clearance_level=1): view the Digests tab after UAT-4.
      Confirm: User A's digest card shows only the sensitivity-1 content (fewer KPI counts than User B's).
      Confirm: the word "clearance" or "filtered" does not appear anywhere on the page.
      Use CDP Network.setBlockedURLs(['*supabase*/rest*']) to force a network error state,
      then assert role="alert" renders the error copy (t('error.load')) — which also must not
      mention clearance. Unblock after assertion.

  </what-built>
  <how-to-verify>
    After all 5 UAT scenarios:
    1. UAT-1 passes: User A and User B digest previews contain different signal counts.
    2. UAT-2 passes: notifications row + intelligence_email_queue row exist for User C; NO email_queue row.
    3. UAT-3 passes: webhook body contains only text + potentialAction (or N/A if no staging webhook configured — document that webhook adapter was tested in unit test webhook-payload-contract.test.ts instead).
    4. UAT-4 passes: intelligence_digest row has published_at NOT NULL; Digests tab shows new DigestCard.
    5. UAT-5 passes: "clearance" or "filtered" never appears in page content; forced-error state shows generic error copy.
  </how-to-verify>
  <resume-signal>Type "UAT complete" with a one-line result for each UAT scenario (pass/N-A/fail + notes). If any scenario fails, describe the failure; the orchestrator will open a gap-closure plan before marking Phase 70 complete.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                           |
| ---------------------------------------- | --------------------------------------------------------------------- |
| Frontend → generate_digest INVOKER RPC   | Caller JWT enforces clearance; no supabaseAdmin on this path          |
| Backend startServer → startAlertListener | pg LISTEN connected only after queue worker is initialized            |
| Dossier digests.tsx routes → DigestsTab  | dossierId from route params is UUID-typed; not user-supplied freeform |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                               | Disposition | Mitigation Plan                                                                                                                                            |
| --------- | ---------------------- | --------------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-70-23   | Information Disclosure | GenerateDigestButton: preview renders above-clearance content                           | mitigate    | generate_digest is SECURITY INVOKER — caller's JWT clearance limits the preview JSONB returned; component never receives above-clearance content to render |
| T-70-24   | Tampering              | intelligence.ts router mounting: intelligenceDigestRouter loaded before auth middleware | mitigate    | Sub-routers inherit parent router's middleware; existing intelligenceRouter already has authenticateToken applied; sub-routes are protected automatically  |
| T-70-25   | Elevation of Privilege | startAlertListener called before notificationQueue worker initialized                   | mitigate    | startAlertListener is called after registerDigestScheduler() which initializes notificationQueue; startup order enforced in action text                    |
| T-70-26   | Information Disclosure | UAT-2: webhook POST body logged in backend                                              | mitigate    | smtpAdapter and webhookAdapter do not log request body or payload; only log INFO on send success (no content)                                              |
| T-70-SC   | Tampering              | npm/pip/cargo installs                                                                  | mitigate    | No new package installs in this plan; all packages installed in Plan 01                                                                                    |

</threat_model>

<verification>
After Task 1:
  pnpm --filter intake-frontend typecheck
  → exit 0

pnpm --filter intake-backend typecheck && pnpm --filter intake-backend build
→ both exit 0

ls frontend/src/routes/\_protected/dossiers/{countries,organizations,forums,topics,working_groups,persons,elected-officials}/$id/digests.tsx frontend/src/routes/_protected/engagements/$engagementId/digests.tsx
→ 8 files exist

grep -c "registerIntelligenceDigestScheduler\|startAlertListener" backend/src/index.ts
→ 4 (2 import lines + 2 call lines)

grep -c "intelligence-digest\|intelligence-alerts" backend/src/api/intelligence.ts
→ >= 2 (two router.use() lines)

grep -c "GenerateDigestButton" frontend/src/components/intelligence/GenerateDigestButton.tsx
→ >= 1

grep -v "^#" e2e/intelligence-digests.spec.ts | grep -c "expect\|assert\|toBe\|toBeVisible"
→ >= 3 (real assertions exist, not just stubs)

After Task 2 (UAT checkpoint):
Human signals "UAT complete" with per-scenario results.
All 5 scenarios pass (or webhook N/A is documented and unit test cited).
</verification>

<success_criteria>
Plan complete when:

- GenerateDigestButton.tsx exists with generate + publish mutations (D-14)
- 8 dossier digests route files exist (all 8 dossier types + engagements, D-12)
- e2e/intelligence-digests.spec.ts has real Playwright assertions (DIGEST-03)
- backend/src/api/intelligence.ts mounts /digest and /alerts sub-routers
- backend/src/index.ts registers intelligence digest scheduler + alert listener in startup
- typecheck and build exit 0 (frontend + backend)
- UAT complete: clearance differentiation confirmed live (UAT-1), alert fan-out confirmed (UAT-2), HITL publish confirmed (UAT-4), no clearance disclosure in UI (UAT-5)
  </success_criteria>

<output>
Create `.planning/phases/70-digests-alerts/70-07-SUMMARY.md` when done
</output>
