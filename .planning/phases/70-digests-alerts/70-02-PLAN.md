---
phase: 70-digests-alerts
plan: 02
type: execute
wave: 2
depends_on:
  - 70-01
files_modified:
  - supabase/migrations/20260615_phase70_digests_alerts.sql
autonomous: true
requirements:
  - DIGEST-01
  - DIGEST-02
  - DIGEST-03
  - DIGEST-04
  - ALERT-01
  - ALERT-02
  - ALERT-03
  - ALERT-04

must_haves:
  truths:
    - 'migration file 20260615_phase70_digests_alerts.sql exists and is syntactically valid SQL'
    - 'intelligence_digest dossier_type CHECK is extended to cover all 8 types including elected_official (per D-03)'
    - 'intelligence_digest gains subscriber_id, frequency, published_at, clearance_level_at_generation, period columns (per D-02/D-04)'
    - 'intelligence_digest_subscriptions table is created with owner-scoped RLS (subscriber_id = auth.uid()) (per D-01/D-03/D-13)'
    - 'intelligence_alert_rules table is created with owner-scoped RLS (owner_id = auth.uid()) (per D-05/D-06/D-13)'
    - 'intelligence_digest_items child table is created for structured rollup records (per D-04)'
    - 'intelligence_email_queue table is created — isolated from email_queue (per D-08 on-prem SMTP, RF-1)'
    - 'generate_digest SECURITY INVOKER RPC is defined — returns JSONB preview clearance-gated under caller JWT (per D-14)'
    - 'publish_digest SECURITY INVOKER RPC is defined — commits approved preview as published intelligence_digest row (per D-14)'
    - 'generate_digest_content SECURITY INVOKER RPC is defined — takes explicit p_clearance_level arg for cron path (per RF-3)'
    - "read_digests SECURITY INVOKER RPC is defined — returns caller's published digests (per DIGEST-03)"
    - 'notify_intelligence_alert trigger function is SECURITY DEFINER and calls pg_notify with event_id/sensitivity_level/severity ONLY — never title or content (per D-05)'
    - 'All clearance RLS uses profiles.user_id = auth.uid() — NEVER profiles.id (landmine #1)'
    - 'UNIQUE constraint (subscriber_id, dossier_id, frequency, period) on intelligence_digest prevents duplicate cron rows (per RF-4)'

  artifacts:
    - path: 'supabase/migrations/20260615_phase70_digests_alerts.sql'
      provides: 'Complete P70 schema extension: 4 new tables, 4 new RPCs, 1 trigger, convergent RLS'
      contains: 'intelligence_digest_subscriptions'

  key_links:
    - from: 'generate_digest RPC'
      to: 'intelligence_event + intelligence_event_dossiers'
      via: 'SECURITY INVOKER — RLS enforces sensitivity_level <= caller clearance_level'
    - from: 'notify_intelligence_alert trigger'
      to: 'intelligence_event AFTER INSERT'
      via: "pg_notify('intelligence_alert', payload WITHOUT title/content)"
    - from: 'intelligence_digest.subscriber_id'
      to: 'auth.users.id'
      via: 'FK ON DELETE CASCADE — deprovisioning handled automatically'
---

<objective>
Author the complete Phase 70 database migration: extend intelligence_digest, create 3 new tables (subscriptions, alert_rules, digest_items, email_queue), add 4 SECURITY INVOKER RPCs (generate_digest, publish_digest, generate_digest_content, read_digests), add the AFTER INSERT trigger for alert notifications, and converge intelligence_digest RLS onto the clearance-gated pattern.

Purpose: The migration is the single source of truth for all P70 data contracts. Plans 03–05 implement against it. The live apply (Plan 03) requires this file to be committed first.
Output: supabase/migrations/20260615_phase70_digests_alerts.sql committed to main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/70-digests-alerts/70-CONTEXT.md
@.planning/phases/70-digests-alerts/70-RESEARCH.md
@.planning/phases/70-digests-alerts/70-PATTERNS.md

<interfaces>
<!-- Key existing tables and their columns — executor uses these directly -->

From supabase/migrations/20260516000002 (intelligence_digest, CURRENT live schema):
TABLE intelligence_digest (
id UUID PRIMARY KEY,
organization_id UUID NOT NULL REFERENCES organizations(id),
dossier_type TEXT NOT NULL CHECK (dossier_type IN ('country','organization','forum','engagement','topic','working_group','person')),
-- ^^ MISSING 'elected_official' — this migration fixes it
dossier_id UUID NOT NULL REFERENCES dossiers(id),
period_start TIMESTAMPTZ NOT NULL,
period_end TIMESTAMPTZ NOT NULL,
summary TEXT NOT NULL,
generated_by UUID REFERENCES auth.users(id),
generated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
)

From supabase/migrations/20260614_phase69_signals_extend.sql (P69 clearance RLS pattern):
-- CORRECT clearance subquery form:
sensitivity_level <= (SELECT clearance_level FROM profiles WHERE user_id = auth.uid())
-- NEVER: WHERE id = auth.uid() — profiles has NO id column

From supabase/migrations/20260614_phase69_signals_extend.sql (SECURITY INVOKER RPC shape):
CREATE OR REPLACE FUNCTION public.read_signals(p_dossier_id UUID DEFAULT NULL, ...)
RETURNS TABLE (id UUID, title TEXT, source_type TEXT, ...)
LANGUAGE SQL SECURITY INVOKER STABLE AS $$
SELECT ie.id, ie.title::text, ie.source_type::TEXT, ...
FROM public.intelligence_event ie
WHERE ...
ORDER BY ie.occurred_at DESC LIMIT p_limit;

$$
;
GRANT EXECUTE ON FUNCTION public.read_signals TO authenticated;

From RESEARCH.md RF-5 (trigger DDL pattern):
CREATE OR REPLACE FUNCTION public.notify_intelligence_alert()
RETURNS TRIGGER LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS
$$

BEGIN
PERFORM pg_notify('intelligence_alert',
json_build_object(
'event_id', NEW.id,
'organization_id', NEW.organization_id,
'sensitivity_level', NEW.sensitivity_level,
'severity', NEW.severity,
'occurred_at', NEW.occurred_at
-- NEVER: NEW.title, NEW.content
)::text);
RETURN NEW;
END;

$$
;

From RESEARCH.md RF-6 (generate_digest RPC shape — JSONB return, plpgsql, reads profiles.clearance_level):
CREATE OR REPLACE FUNCTION public.generate_digest(p_dossier_id UUID, p_period TEXT DEFAULT 'daily')
RETURNS JSONB LANGUAGE plpgsql SECURITY INVOKER AS
$$

DECLARE v_clearance INTEGER; v_since TIMESTAMPTZ; v_until TIMESTAMPTZ; v_result JSONB;
BEGIN
SELECT clearance_level INTO v_clearance FROM profiles WHERE user_id = auth.uid();
-- time window computed from p_period
-- content aggregated across signals + engagements + commitments + status changes
-- all filtered by sensitivity_level <= v_clearance (INVOKER RLS does this automatically)
RETURN v_result;
END;

$$
;
GRANT EXECUTE ON FUNCTION public.generate_digest TO authenticated;

From RESEARCH.md RF-2 (intelligence_digest_subscriptions schema):
TABLE intelligence_digest_subscriptions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  organization_id UUID NOT NULL REFERENCES organizations(id),
  subscriber_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  dossier_type TEXT NOT NULL CHECK (...all 8 types...),
  dossier_id UUID NOT NULL REFERENCES dossiers(id) ON DELETE CASCADE,
  frequency TEXT NOT NULL DEFAULT 'daily' CHECK (frequency IN ('daily','weekly','monthly')),
  is_active BOOLEAN NOT NULL DEFAULT true,
  created_at / updated_at TIMESTAMPTZ,
  UNIQUE (subscriber_id, dossier_id, frequency)
)
RLS: subscriber_id = auth.uid() AND tenant_isolation.rls_select_policy(organization_id)

From RESEARCH.md RF-2 (intelligence_alert_rules schema):
TABLE intelligence_alert_rules (
  id UUID PRIMARY KEY,
  organization_id UUID NOT NULL,
  owner_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  dossier_type TEXT NOT NULL CHECK (...all 8 types...),
  dossier_id UUID NOT NULL REFERENCES dossiers(id) ON DELETE CASCADE,
  condition_type TEXT NOT NULL DEFAULT 'new_signal' CHECK (condition_type IN ('new_signal')),
  condition_config JSONB NOT NULL DEFAULT '{}',  -- { "severity_filter": ["high","urgent"] }
  channels TEXT[] NOT NULL DEFAULT ARRAY['in_app'],
  is_active BOOLEAN NOT NULL DEFAULT true,
  last_fired_at TIMESTAMPTZ,
  created_at / updated_at TIMESTAMPTZ
)
RLS: owner_id = auth.uid() AND tenant_isolation.rls_select_policy(organization_id)
</interfaces>
</context>

<tasks>

<task type="auto">
<name>Task 1: Write P70 database migration SQL</name>
<files>supabase/migrations/20260615_phase70_digests_alerts.sql</files>
<read_first>
  supabase/migrations/20260614_phase69_signals_extend.sql — read in full; this is the exact migration template to mirror (header pattern, RLS pattern with profiles.user_id, SECURITY INVOKER RPC shape, GRANT pattern).
  supabase/migrations/20260516000002_phase54_intelligence_event_and_digest.sql — read lines 1-60; get the intelligence_digest CREATE TABLE DDL and its existing RLS policy names to DROP before replacing.
  supabase/migrations/20260516000003_phase54_intelligence_event_dossiers.sql — read lines 1-40; get the polymorphic junction pattern (the subscription/alert tables mirror this for dossier_type CHECK).
  .planning/phases/70-digests-alerts/70-RESEARCH.md §RF-2 — full DDL blocks for all 4 new tables.
  .planning/phases/70-digests-alerts/70-RESEARCH.md §RF-3 — generate_digest_content RPC with explicit p_clearance_level arg (the cron path — different from the interactive generate_digest RPC).
  .planning/phases/70-digests-alerts/70-RESEARCH.md §RF-5 — trigger DDL.
  .planning/phases/70-digests-alerts/70-RESEARCH.md §RF-6 — generate_digest and publish_digest RPC signatures.
  .planning/phases/70-digests-alerts/70-PATTERNS.md §migration — full DDL and anti-pattern block list (especially: never `WHERE id = auth.uid()` on profiles).
</read_first>
<action>
  Author supabase/migrations/20260615_phase70_digests_alerts.sql with the following sections in order:

    Section 1 — Header comment block: phase 70 decisions covered (D-01..D-14), applied-via annotation, CRITICAL profiles.user_id note.

    Section 2 — Fix intelligence_digest dossier_type CHECK: DROP existing constraint, ADD new CHECK including 'elected_official' (all 8 types).

    Section 3 — Extend intelligence_digest: ADD COLUMN IF NOT EXISTS subscriber_id UUID REFERENCES auth.users(id) ON DELETE CASCADE; frequency TEXT CHECK ('daily','weekly','monthly') DEFAULT 'daily'; published_at TIMESTAMPTZ; clearance_level_at_generation INTEGER; period TEXT. Add UNIQUE constraint (subscriber_id, dossier_id, frequency, period) — this is the idempotency guard for the cron (RF-4).

    Section 4 — Create intelligence_digest_subscriptions table (per RF-2 DDL). Enable RLS. Four policies: SELECT/INSERT/UPDATE/DELETE — all owner-scoped with subscriber_id = auth.uid() AND tenant_isolation.rls_select_policy (SELECT/INSERT only).

    Section 5 — Create intelligence_alert_rules table (per RF-2 DDL). Enable RLS. Four owner-scoped policies with owner_id = auth.uid().

    Section 6 — Create intelligence_digest_items child table: id, digest_id FK ON DELETE CASCADE, item_type CHECK ('signal','engagement','commitment','status_change'), item_id UUID, item_data JSONB, sensitivity_level INTEGER CHECK (1-4). Index on (digest_id, sensitivity_level). No RLS — access gated via parent digest_id FK. GRANT SELECT TO authenticated.

    Section 7 — Create intelligence_email_queue table: id UUID PK, recipient_id UUID, recipient_email TEXT, subject TEXT, body_html TEXT, body_text TEXT, deep_link TEXT, queued_at TIMESTAMPTZ DEFAULT NOW(), sent_at TIMESTAMPTZ, retry_count INTEGER DEFAULT 0. Enable RLS. Service-role only (no authenticated INSERT policy — cron writes this). GRANT SELECT, UPDATE ON intelligence_email_queue TO service_role (already implicit).

    Section 8 — Converge intelligence_digest RLS: DROP existing policies by name (read them from the P54 migration). CREATE new policies: SELECT (subscriber_id = auth.uid() OR subscriber_id IS NULL; AND published_at IS NOT NULL). No authenticated INSERT policy — cron uses service-role. Note above the policy: "-- Service-role cron path uses supabaseAdmin; no authenticated INSERT policy needed."

    Section 9 — notify_intelligence_alert trigger function (SECURITY DEFINER): exactly the DDL from RF-5 in RESEARCH.md. Payload MUST contain ONLY: event_id, organization_id, sensitivity_level, severity, occurred_at. Never title, never content.

    Section 10 — Create trigger: intelligence_event_alert_notify AFTER INSERT ON intelligence_event FOR EACH ROW EXECUTE FUNCTION notify_intelligence_alert().

    Section 11 — generate_digest SECURITY INVOKER RPC (per RF-6 signature): reads profiles.clearance_level WHERE user_id = auth.uid(); computes time window from p_period; queries 4 content planes (signals via intelligence_event+intelligence_event_dossiers, engagements via engagement_dossiers+dossiers, commitments via aa_commitments joined through work_item_dossiers, status changes via dossiers.updated_at); all planes filter by sensitivity_level <= v_clearance (INVOKER RLS also enforces this). Returns JSONB. GRANT EXECUTE TO authenticated.

    Section 12 — publish_digest SECURITY INVOKER RPC: INSERT into intelligence_digest with published_at = NOW(). Handles the INSERT UNIQUE violation gracefully (use ON CONFLICT DO NOTHING, return existing id on conflict). GRANT EXECUTE TO authenticated.

    Section 13 — generate_digest_content SECURITY INVOKER RPC: takes p_dossier_id UUID, p_since TIMESTAMPTZ, p_until TIMESTAMPTZ, p_clearance_level INTEGER (explicit arg — no auth.uid() lookup, caller is service-role cron). Queries same 4 planes as generate_digest but filters by sensitivity_level <= p_clearance_level directly. Returns JSONB. GRANT EXECUTE TO service_role (cron path only — NOT authenticated; this RPC is called by backend service-role, not frontend).

    Section 14 — read_digests SECURITY INVOKER RPC: SELECT from intelligence_digest WHERE subscriber_id = auth.uid() AND published_at IS NOT NULL. Optional p_dossier_id filter. Returns TABLE (id UUID, dossier_id UUID, dossier_type TEXT, frequency TEXT, period TEXT, summary TEXT, generated_at TIMESTAMPTZ, published_at TIMESTAMPTZ, clearance_level_at_generation INTEGER). GRANT EXECUTE TO authenticated.

    CRITICAL RULES (must appear in DDL):
  - Every profiles lookup: WHERE user_id = auth.uid() — NEVER WHERE id = auth.uid()
  - All new RPCs are SECURITY INVOKER except notify_intelligence_alert (SECURITY DEFINER — required to call pg_notify)
  - intelligence_email_queue is isolated from email_queue (different table name; no shared policies)
  - pg_notify payload in trigger: event_id, organization_id, sensitivity_level, severity, occurred_at ONLY
  - RETURNS TABLE column types: cast enums to TEXT explicitly (e.g. item_type::TEXT) to avoid 42804 errors
</action>
<verify>
  <automated>grep -c "intelligence_digest_subscriptions\|intelligence_alert_rules\|generate_digest\|publish_digest\|notify_intelligence_alert\|intelligence_email_queue\|intelligence_digest_items\|read_digests" /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0/supabase/migrations/20260615_phase70_digests_alerts.sql</automated>
</verify>
<done>
  Migration file exists at supabase/migrations/20260615_phase70_digests_alerts.sql.
  grep output >= 8 (all key identifiers present).
  The file contains SECURITY INVOKER on generate_digest, publish_digest, generate_digest_content, read_digests.
  The file contains SECURITY DEFINER on notify_intelligence_alert only.
  No occurrence of "WHERE id = auth.uid()" on profiles table (grep -c "FROM profiles WHERE id" should return 0).
  The trigger payload does not include "title" or "content" fields (grep -c "NEW.title\|NEW.content" in trigger function should return 0).
  File is committed to git.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend cron (service-role) → intelligence_digest | Service-role writes digest rows; per-subscriber clearance must be app-layer enforced |
| intelligence_event INSERT → pg_notify → backend | Trigger payload must carry no classified content |
| Caller JWT → INVOKER RPCs | RLS enforces clearance; no service-role bypass in interactive path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-02 | Information Disclosure | generate_digest_content RPC — explicit clearance arg | mitigate | RPC takes p_clearance_level INTEGER from service-role caller; content filtered by sensitivity_level <= p_clearance_level; empty = indistinguishable (no "filtered" label) |
| T-70-03 | Information Disclosure | notify_intelligence_alert trigger payload | mitigate | DDL constraint: payload contains ONLY event_id/org/sensitivity_level/severity/occurred_at — never title/content; enforced by DDL review before migration apply |
| T-70-04 | Elevation of Privilege | profiles.user_id vs profiles.id in RLS | mitigate | All DDL uses WHERE user_id = auth.uid(); verified by grep before migration apply; impersonation UAT after apply |
| T-70-05 | Denial of Service | Duplicate cron rows (concurrent workers) | mitigate | UNIQUE (subscriber_id, dossier_id, frequency, period) on intelligence_digest; worker catches 23505 as expected duplicate |
| T-70-06 | Information Disclosure | intelligence_email_queue cross-contamination with email_queue | mitigate | Separate table (intelligence_email_queue); no shared policies; email_queue path untouched |
| T-70-07 | Elevation of Privilege | generate_digest_content called by authenticated user | mitigate | GRANT EXECUTE ON generate_digest_content TO service_role ONLY (not authenticated); frontend calls generate_digest instead |
</threat_model>

<verification>
After task completes:
grep -c "SECURITY INVOKER" supabase/migrations/20260615_phase70_digests_alerts.sql
→ should be >= 4 (generate_digest, publish_digest, generate_digest_content, read_digests)

  grep -c "SECURITY DEFINER" supabase/migrations/20260615_phase70_digests_alerts.sql
→ should be 1 (notify_intelligence_alert trigger function only)

  grep -c "FROM profiles WHERE id" supabase/migrations/20260615_phase70_digests_alerts.sql
→ must be 0 (the profiles landmine — user_id, never id)

  grep -c "NEW.title\|NEW.content" supabase/migrations/20260615_phase70_digests_alerts.sql
→ must be 0 (no classified content in trigger payload)

  git log --oneline -1
→ migration file committed
</verification>

<success_criteria>
Migration is complete when:
- File exists at supabase/migrations/20260615_phase70_digests_alerts.sql and is committed
- Contains all 14 decisions' DDL requirements (4 tables, 4 RPCs, 1 trigger, convergent RLS)
- Zero occurrences of "FROM profiles WHERE id = auth.uid()" (landmine check)
- Zero occurrences of classified fields in trigger payload
- All interactive RPCs are SECURITY INVOKER; only trigger function is SECURITY DEFINER
- File passes SQL syntax check (supabase db lint if available, or manual review)
</success_criteria>

<output>
Create `.planning/phases/70-digests-alerts/70-02-SUMMARY.md` when done
</output>
$$
