---
phase: 70-digests-alerts
plan: 05
type: execute
wave: 4
depends_on:
  - 70-03
files_modified:
  - backend/src/services/intelligence-digest.service.ts
  - backend/src/services/alerts.service.ts
  - backend/src/queues/intelligence-digest.scheduler.ts
  - backend/src/api/intelligence-digest.ts
  - backend/src/api/intelligence-alerts.ts
  - backend/tests/intelligence/subscriptions.test.ts
  - backend/tests/intelligence/digest-cron.integration.test.ts
  - backend/tests/intelligence/generate-digest.integration.test.ts
  - backend/tests/intelligence/alert-rules.test.ts
autonomous: true
requirements:
  - DIGEST-01
  - DIGEST-02
  - DIGEST-04
  - ALERT-01
  - ALERT-02

must_haves:
  truths:
    - 'intelligence-digest.service.ts generates per-subscriber clearance-gated digest via generate_digest_content RPC (explicit p_clearance_level arg, RF-3, D-02)'
    - 'intelligence-digest.scheduler.ts registers daily/weekly/monthly BullMQ repeatable jobs (D-02) with distinct names from personal-notification digest jobs (RF-4)'
    - 'Monthly cadence uses day-of-month check inside a 24h-interval BullMQ job (RF-4)'
    - 'alerts.service.ts REPLACES the in-memory Map stub entirely — now wraps intelligence_alert_rules table via supabaseAdmin (D-06)'
    - 'intelligence-digest.ts Express router exposes GET /subscriptions + POST /subscribe + DELETE /unsubscribe (DIGEST-01)'
    - 'intelligence-alerts.ts Express router exposes GET /rules + POST /rules + PUT /rules/:id + DELETE /rules/:id (ALERT-01)'
    - "digest-cron reads profiles.clearance_level with .eq('user_id', sub.subscriber_id) — NEVER .eq('id', ...) (landmine #1)"
    - 'subscriber deprovisioning edge case: if profile == null, cron skips subscriber (RF-4)'
    - 'UNIQUE constraint violation (23505) on intelligence_digest insert is caught and logged at INFO not ERROR (RF-4)'
    - 'D-13 personal ownership: all API routes check userId from authenticateToken middleware; no cross-user reads'
    - 'generate_digest_content returns JSONB with no "filtered" or "clearance"-reason key — the cron payload and intelligence_digest.summary contain zero filtering-reason metadata (indistinguishable-empty, D-09)'
    - '/generate and /publish routes construct the Supabase client from req.headers.authorization (caller JWT), NOT supabaseAdmin/service-role — INVOKER RLS applies clearance automatically (W3 assertion)'

  artifacts:
    - path: 'backend/src/services/intelligence-digest.service.ts'
      provides: 'generateDigestForSubscriber() function using generate_digest_content RPC'
      exports: ['generateDigestForSubscriber', 'publishDigestRow']
    - path: 'backend/src/services/alerts.service.ts'
      provides: 'DB-backed alert rule CRUD (replaces in-memory stub)'
      exports: ['listAlertRules', 'createAlertRule', 'updateAlertRule', 'deleteAlertRule']
    - path: 'backend/src/queues/intelligence-digest.scheduler.ts'
      provides: 'registerIntelligenceDigestScheduler() with daily/weekly/monthly cron'
      exports: ['registerIntelligenceDigestScheduler', 'processIntelligenceDigestJob']
    - path: 'backend/src/api/intelligence-digest.ts'
      provides: 'Express router for digest subscription CRUD'
    - path: 'backend/src/api/intelligence-alerts.ts'
      provides: 'Express router for alert rule CRUD'

  key_links:
    - from: 'backend/src/queues/intelligence-digest.scheduler.ts'
      to: 'generate_digest_content RPC'
      via: "supabase.rpc('generate_digest_content', { p_dossier_id, p_since, p_until, p_clearance_level })"
    - from: 'backend/src/queues/intelligence-digest.scheduler.ts'
      to: 'backend/src/adapters/intelligence/ChannelAdapter.ts'
      via: 'dispatch to in-app + smtp + webhook adapters after each digest row is written'
    - from: 'backend/src/api/intelligence-alerts.ts'
      to: 'backend/src/services/alerts.service.ts'
      via: 'listAlertRules(userId), createAlertRule(input), deleteAlertRule(id, userId)'
---

<objective>
Build the backend digest pipeline (cron scheduler + service layer) and replace the in-memory alerts stub with the DB-backed service. Add Express API routes for subscription and alert-rule CRUD. Fill 4 more Wave 0 test stubs with real assertions.

Purpose: Plan 04 built the delivery layer; this plan builds the generation+scheduling layer and the CRUD surface that feeds it.
Output: 5 new/replaced backend files + 4 test stubs filled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/70-digests-alerts/70-CONTEXT.md
@.planning/phases/70-digests-alerts/70-RESEARCH.md
@.planning/phases/70-digests-alerts/70-PATTERNS.md

<interfaces>
<!-- Key contracts from analogs the executor uses directly -->

From backend/src/queues/digest-scheduler.ts (exact structural mirror):

- supabase service-role client init: createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY)
- upsertJobScheduler idempotency pattern (lines 331-347)
- per-subscriber try/catch + continue error handling (lines 176-179)
- reads from intelligence_digest_subscriptions (new table), NOT email_notification_preferences

From backend/src/services/notification.service.ts (CRUD pattern for alerts.service.ts):

- supabaseAdmin.from('table').select('\*').eq('owner_id', userId)
- supabase error check: if (error !== null) throw error

From backend/src/api/notifications.ts (Express router pattern for intelligence-digest.ts / intelligence-alerts.ts):
import { Router } from 'express'
const router = Router()
const userId = (req as any).user?.id
if (!userId) { res.status(401).json({ error: 'Unauthorized' }); return }
try { ... } catch (err) { res.status(500).json({ error: 'Internal server error' }) }

From backend/src/services/digest-template.service.ts (reuse for renderWeeklyDigestTemplate):
export function renderDailyDigestTemplate(lang: string, dateRange: DateRange, content: DigestContent): { subject: string; bodyHtml: string; bodyText: string }
export function renderWeeklyDigestTemplate(lang: string, dateRange: DateRange, content: DigestContent): { subject: string; bodyHtml: string; bodyText: string }
-- add renderMonthlyDigestTemplate following the same pattern

CRITICAL: profiles.clearance_level lookup in digest cron:
supabase.from('profiles').select('clearance_level').eq('user_id', sub.subscriber_id).single()
-- NEVER: .eq('id', sub.subscriber_id) — profiles has NO id column
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: intelligence-digest.service.ts + intelligence-digest.scheduler.ts + subscriptions.test.ts + digest-cron.integration.test.ts</name>
  <files>
    backend/src/services/intelligence-digest.service.ts,
    backend/src/queues/intelligence-digest.scheduler.ts,
    backend/tests/intelligence/subscriptions.test.ts,
    backend/tests/intelligence/digest-cron.integration.test.ts
  </files>
  <behavior>
    subscriptions.test.ts (DIGEST-01):
    - POST /subscribe with valid dossier_id + frequency → creates row in intelligence_digest_subscriptions (is_active=true)
    - DELETE /unsubscribe with subscriber_id = auth.uid() → sets is_active=false (soft delete)
    - GET /subscriptions → returns only the authenticated user's subscriptions
    - Cross-user read: user A cannot read user B's subscriptions (owner RLS)

    digest-cron.integration.test.ts (DIGEST-02):
    - processIntelligenceDailyDigests() with 2 subscribers at clearance 1 and 3 on same dossier with 2 signals (sensitivity 1 and 3):
        subscriber A (clearance 1) digest contains only the sensitivity-1 signal
        subscriber B (clearance 3) digest contains both signals
    - If subscriber profile does not exist (deprovisioned): subscriber is skipped, no error thrown
    - UNIQUE constraint violation on intelligence_digest insert is caught and logged INFO, not ERROR (duplicate cron)
    - intelligence_email_queue (NOT email_queue) receives the SMTP row when smtp channel is active

  </behavior>
  <read_first>
    backend/src/queues/digest-scheduler.ts — read lines 1-200 in full; this is the exact structural template for intelligence-digest.scheduler.ts.
    backend/src/services/digest-template.service.ts — read first 50 lines for render function signatures; will reuse these.
    backend/src/services/email-template.service.ts — read first 30 lines for bilingual template pattern.
    .planning/phases/70-digests-alerts/70-PATTERNS.md §intelligence-digest.scheduler.ts and §intelligence-digest.service.ts — full per-subscriber cron body pattern + RPC call pattern.
    .planning/phases/70-digests-alerts/70-RESEARCH.md §RF-3 — explicit clearance-arg mechanism (p_clearance_level INTEGER).
    .planning/phases/70-digests-alerts/70-RESEARCH.md §RF-4 — monthly cadence implementation, subscriber deprovisioning edge case, concurrent delivery prevention.
  </read_first>
  <action>
    1. intelligence-digest.service.ts:
      Export generateDigestForSubscriber(sub: SubscriptionRow, since: Date, until: Date, clearanceLevel: number): calls supabase.rpc('generate_digest_content', { p_dossier_id: sub.dossier_id, p_since: since.toISOString(), p_until: until.toISOString(), p_clearance_level: clearanceLevel }). On RPC error: throws (caller catches). Returns the JSONB content.
      Export publishDigestRow(orgId, dossierType, dossierId, periodKey, frequency, summary, subscriberId, clearanceLevel): INSERTs into intelligence_digest. Catches 23505 UNIQUE violation → logInfo('Duplicate digest skipped', ...) + return without throw. Sets published_at = new Date().toISOString() (scheduled cron publishes immediately on write).
      Export dispatchDigestToChannels(subscriber, channels, payload): calls matching adapters from intelligence/ adapter directory.

    2. intelligence-digest.scheduler.ts (mirrors digest-scheduler.ts structure):
      Import: supabase service-role createClient, notificationQueue, renderDailyDigestTemplate/renderWeeklyDigestTemplate from digest-template.service, logInfo/logError, the channel adapters.
      Export registerIntelligenceDigestScheduler(): three upsertJobScheduler calls with names 'intelligence-digest-daily', 'intelligence-digest-weekly', 'intelligence-digest-monthly' (distinct from 'daily-digest-processor'/'weekly-digest-processor'). Each scheduler id is idempotent.
      Export processIntelligenceDigestJob(jobName): dispatcher calling processIntelligenceDailyDigests / processIntelligenceWeeklyDigests / processIntelligenceMonthlyDigests.
      processIntelligenceDailyDigests(): fetches active intelligence_digest_subscriptions where frequency='daily'. For each subscriber:
        - reads profile clearance: supabase.from('profiles').select('clearance_level').eq('user_id', sub.subscriber_id).single() — NEVER .eq('id', ...)
        - if profile == null: logInfo('Subscriber deprovisioned, skipping') + continue
        - calls generateDigestForSubscriber(sub, since24h, now, clearanceLevel)
        - calls publishDigestRow(...)
        - dispatches to subscriber's configured channels via dispatchDigestToChannels
        - per-subscriber try/catch: catches errors, logError + continue (do NOT re-throw per-subscriber)
      processIntelligenceMonthlyDigests(): mirrors weekly but checks day-of-month via new Date().getUTCDate() against sub.condition_config?.day_of_month (default to 1 if not set; cap at 28).

    3. subscriptions.test.ts: use vi.mock for supabaseAdmin. Test subscribe (INSERT), unsubscribe (UPDATE is_active=false), list-own subscriptions (SELECT with eq('subscriber_id', userId)). Mock returns correct row shapes.

    4. digest-cron.integration.test.ts: mock supabase RPC generate_digest_content to return different JSONB depending on p_clearance_level (level-1 subscriber gets 1 signal, level-3 gets 2). Mock intelligence_digest INSERT to track calls. Assert the two subscriber paths produce distinct content. Assert deprovisioned subscriber (profile=null) is skipped without error.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && pnpm --filter intake-backend test -- --run tests/intelligence/subscriptions.test.ts tests/intelligence/digest-cron.integration.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>
    Both test files pass green.
    subscriptions.test.ts: subscribe, unsubscribe, list-own all pass.
    digest-cron.integration.test.ts: clearance differentiation test passes (level-1 vs level-3 subscribers get different content).
    Deprovisioning skip test passes.
    processIntelligenceDigestJob is exported and importable by notification.queue.ts.
    typecheck exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: alerts.service.ts replacement + API routes + alert-rules.test.ts + generate-digest.integration.test.ts</name>
  <files>
    backend/src/services/alerts.service.ts,
    backend/src/api/intelligence-digest.ts,
    backend/src/api/intelligence-alerts.ts,
    backend/tests/intelligence/alert-rules.test.ts,
    backend/tests/intelligence/generate-digest.integration.test.ts
  </files>
  <behavior>
    alert-rules.test.ts (ALERT-01):
    - createAlertRule({ owner_id, dossier_id, dossier_type, condition_type:'new_signal', channels:['in_app'] }) → inserts into intelligence_alert_rules, returns row with is_active=true
    - listAlertRules(ownerId) → returns only rules where owner_id = ownerId (no cross-user)
    - updateAlertRule(id, ownerId, { is_active: false }) → updates and returns updated row
    - deleteAlertRule(id, ownerId) → deletes by id + owner_id (owner-scoped delete)

    generate-digest.integration.test.ts (DIGEST-04):
    - GET /api/intelligence-digest/generate?dossier_id=X&period=weekly → calls generate_digest RPC → returns 200 with preview JSONB
    - POST /api/intelligence-digest/publish with { dossier_id, period, summary, clearance_level_at_generation } → calls publish_digest RPC → returns 201 with { id }
    - Unauthenticated request → 401
    - Caller-JWT contract: spy on createClient calls in the /generate and /publish route handlers; assert the Supabase client is constructed from req.headers.authorization (not the service-role key); assert supabaseAdmin.rpc is NOT called on these routes (W3)

  </behavior>
  <read_first>
    backend/src/services/alerts.service.ts — read the CURRENT in-memory Map stub in full; understand what methods exist so the replacement exports the same interface (preserving callers).
    backend/src/api/notifications.ts — read in full for Express Router + auth guard + error handler pattern.
    backend/src/config/supabase.ts — for supabaseAdmin import.
    backend/src/middleware/ — grep for authenticateToken middleware name and import path.
    .planning/phases/70-digests-alerts/70-PATTERNS.md §alerts.service.ts (REPLACE NOT EXTEND) — replacement code pattern.
    .planning/phases/70-digests-alerts/70-PATTERNS.md §intelligence-digest.ts controller — auth guard + error handler pattern.
  </read_first>
  <action>
    1. alerts.service.ts — REPLACE entirely. Delete the in-memory Map + AlertsService class. Write new module-level functions (no class):
      listAlertRules(ownerId: string): supabaseAdmin.from('intelligence_alert_rules').select('*').eq('owner_id', ownerId).eq('is_active', true).order('created_at', { ascending: false }).
      createAlertRule(input: CreateAlertRuleInput): supabaseAdmin.from('intelligence_alert_rules').insert(input).select().single().
      updateAlertRule(id: string, ownerId: string, patch: Partial<AlertRule>): supabaseAdmin.from('intelligence_alert_rules').update({ ...patch, updated_at: new Date().toISOString() }).eq('id', id).eq('owner_id', ownerId).select().single().
      deleteAlertRule(id: string, ownerId: string): supabaseAdmin.from('intelligence_alert_rules').delete().eq('id', id).eq('owner_id', ownerId).
      Add CreateAlertRuleInput and AlertRule TypeScript interfaces at the top of the file.
      Add a type guard: condition_type must be 'new_signal'. channels items must be 'in_app' | 'smtp' | 'webhook'.

    2. intelligence-digest.ts — Express Router:
      GET /subscriptions → listSubscriptions(userId) → supabaseAdmin.from('intelligence_digest_subscriptions').select('*').eq('subscriber_id', userId).eq('is_active', true)
      POST /subscribe → body: { dossier_id, dossier_type, frequency }. Validate: dossier_type is one of 8 valid types (Zod or manual check); frequency is 'daily'|'weekly'|'monthly'. Upsert with ON CONFLICT DO UPDATE (update is_active=true, updated_at). Return 201.
      DELETE /subscribe/:dossierId — sets is_active=false for subscriber_id=userId AND dossier_id=param. Return 200.
      GET /generate?dossier_id=X&period=P → calls supabase (caller-JWT client, NOT supabaseAdmin) .rpc('generate_digest', { p_dossier_id, p_period }). Return 200 with preview JSONB. IMPORTANT: use the user's JWT client here (not supabaseAdmin) so INVOKER RLS applies clearance.
      POST /publish → body: { dossier_id, period, summary, clearance_level_at_generation }. Calls supabase (caller-JWT).rpc('publish_digest', { ... }). Return 201 with { id }.
      Auth guard on all routes: userId from (req as any).user?.id; 401 if missing.

    3. intelligence-alerts.ts — Express Router:
      GET /rules → listAlertRules(userId)
      POST /rules → validate body (Zod: dossier_id UUID, dossier_type enum[8], condition_type='new_signal', channels array min 1). createAlertRule({ owner_id: userId, organization_id: orgId, ...body }). Return 201.
      PUT /rules/:id → updateAlertRule(id, userId, body).
      DELETE /rules/:id → deleteAlertRule(id, userId). Return 204.
      Auth guard on all routes.

    4. alert-rules.test.ts — fill Wave 0 stub with assertions from behavior block. Mock supabaseAdmin. Test all 4 CRUD operations; assert owner_id is always the caller's userId.

    5. generate-digest.integration.test.ts — fill Wave 0 stub. Mock supabase RPC calls (generate_digest and publish_digest). Test: authenticated GET /generate returns 200 with preview JSONB. Unauthenticated returns 401. POST /publish returns 201 with id.

    Note: the caller-JWT Supabase client for the /generate and /publish routes must be constructed from the request's auth header (not supabaseAdmin). Pattern: createClient(SUPABASE_URL, SUPABASE_ANON_KEY, { global: { headers: { Authorization: req.headers.authorization } } }). This ensures the RPC runs under the caller's identity and INVOKER RLS applies.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 && pnpm --filter intake-backend test -- --run tests/intelligence/alert-rules.test.ts tests/intelligence/generate-digest.integration.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>
    alert-rules.test.ts passes green (CRUD + owner-scoped reads tested).
    generate-digest.integration.test.ts passes green (generate preview + publish + 401 unauthenticated).
    alerts.service.ts has NO in-memory Map (grep -c 'new Map' should return 0).
    intelligence-digest.ts and intelligence-alerts.ts typecheck without errors.
    All 4 backend test files in tests/intelligence/ still green after this plan.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                              | Description                                                                  |
| ----------------------------------------------------- | ---------------------------------------------------------------------------- |
| HTTP request → Express API routes                     | User-supplied dossier_id, period, channels must be validated before DB write |
| Service-role cron → intelligence_digest_subscriptions | Cron reads all subscriptions; must not cross-tenant                          |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                  | Disposition | Mitigation Plan                                                                                                                     |
| --------- | ---------------------- | -------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| T-70-16   | Elevation of Privilege | /subscribe: user can set organization_id to a different tenant             | mitigate    | API derives organization_id from the authenticated user's profile (default_organization_id), never from request body                |
| T-70-17   | Information Disclosure | Cron reads profiles.clearance_level via service-role — could log it        | mitigate    | clearance_level is never logged; only used for the p_clearance_level arg to generate_digest_content RPC                             |
| T-70-18   | Elevation of Privilege | generate /digest route using supabaseAdmin instead of caller JWT           | mitigate    | /generate and /publish routes use caller-JWT client; INVOKER RLS enforces clearance automatically; action doc explicitly notes this |
| T-70-19   | Tampering              | In-memory alerts.service.ts stub called by other modules after replacement | mitigate    | Replace entire file; verify no other module imports from alerts.service.ts except intelligence-alerts.ts (grep before commit)       |

</threat_model>

<verification>
After both tasks complete:
  pnpm --filter intake-backend test -- --run tests/intelligence/
  → all 7 backend test files pass green

grep -c "new Map" backend/src/services/alerts.service.ts
→ must be 0 (in-memory stub fully replaced)

grep -c "email_queue" backend/src/queues/intelligence-digest.scheduler.ts
→ must be 0 (cron never writes to email_queue)

grep -c "filtered\|clearance.\*reason\|filtering_reason" backend/src/queues/intelligence-digest.scheduler.ts backend/src/services/intelligence-digest.service.ts
→ must be 0 (no filtering-reason metadata in cron/payload builders, D-09)

grep -c "profiles.user_id\|eq('user_id'" backend/src/queues/intelligence-digest.scheduler.ts
→ must be >= 1 (clearance lookup uses correct column)

pnpm --filter intake-backend typecheck
→ exit 0
</verification>

<success_criteria>
Plan complete when:

- intelligence-digest.service.ts and intelligence-digest.scheduler.ts exist with daily/weekly/monthly cron
- alerts.service.ts fully replaces in-memory stub (no Map, no AlertsService class)
- intelligence-digest.ts and intelligence-alerts.ts Express routers registered
- 4 test stubs filled and passing green (subscriptions, digest-cron, alert-rules, generate-digest)
- Total 7 backend intelligence tests pass (3 from Plan 04 + 4 from this plan)
- typecheck exits 0
- Cron never writes to email_queue (grep clean)
  </success_criteria>

<output>
Create `.planning/phases/70-digests-alerts/70-05-SUMMARY.md` when done
</output>
