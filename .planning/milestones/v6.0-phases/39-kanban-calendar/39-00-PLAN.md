---
phase: 39-kanban-calendar
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/pages/WorkBoard/index.ts
  - frontend/src/lib/i18n/toArDigits.ts
  - frontend/src/lib/i18n/__tests__/toArDigits.test.ts
  - frontend/public/locales/en/unified-kanban.json
  - frontend/public/locales/ar/unified-kanban.json
  - frontend/public/locales/en/calendar.json
  - frontend/public/locales/ar/calendar.json
  - frontend/tests/e2e/kanban-render.spec.ts
  - frontend/tests/e2e/kanban-rtl.spec.ts
  - frontend/tests/e2e/kanban-visual.spec.ts
  - frontend/tests/e2e/kanban-search.spec.ts
  - frontend/tests/e2e/kanban-filters.spec.ts
  - frontend/tests/e2e/kanban-responsive.spec.ts
  - frontend/tests/e2e/kanban-a11y.spec.ts
  - frontend/tests/e2e/kanban-dnd.spec.ts
  - frontend/tests/e2e/calendar-render.spec.ts
  - frontend/tests/e2e/calendar-rtl.spec.ts
  - frontend/tests/e2e/calendar-visual.spec.ts
  - frontend/tests/e2e/calendar-mobile.spec.ts
  - frontend/tests/e2e/calendar-a11y.spec.ts
  - scripts/check-deleted-components.sh
autonomous: true
requirements: [BOARD-01, BOARD-02, BOARD-03]
must_haves:
  truths:
    - 'WorkBoard folder scaffold exists with empty barrel'
    - 'toArDigits utility ported from handoff with passing unit tests'
    - 'All Phase 39 i18n keys (EN+AR) present in unified-kanban.json + calendar.json'
    - '12 Playwright spec stubs exist (skipped) so Wave 1 plans can fill them in-place'
    - 'check-deleted-components.sh contains commented-out Phase 39 block ready for 39-09 activation'
  artifacts:
    - path: frontend/src/pages/WorkBoard/index.ts
      provides: 'barrel re-export point for WorkBoard, KCard, BoardColumn, BoardToolbar'
    - path: frontend/src/lib/i18n/toArDigits.ts
      provides: 'Arabic-Indic digit transformer used by kcard count, calendar day, overdue chip'
    - path: frontend/public/locales/en/unified-kanban.json
      contains: 'filters.byStatus, filters.byDossier, filters.byOwner, filters.comingSoon, overdueChip, actions.newItem'
    - path: frontend/public/locales/ar/unified-kanban.json
      contains: 'بالحالة, بالملف, بالمسؤول, قريبًا, {{count}} متأخر, عنصر جديد'
  key_links:
    - from: frontend/src/lib/i18n/toArDigits.ts
      to: frontend/src/lib/i18n/__tests__/toArDigits.test.ts
      via: vitest
      pattern: 'toArDigits'
---

<objective>
Wave 0 infrastructure for Phase 39 kanban-calendar reskin: scaffold the new WorkBoard folder, port the `toArDigits` Arabic-Indic digit utility from the handoff, extend `unified-kanban` and `calendar` i18n namespaces with all keys Wave 1 needs, and create skipped Playwright spec stubs (12) so each Wave 1 plan can drop assertions into a known file. Activates the `check-deleted-components.sh` Phase 39 block in commented form (39-09 uncomments).

Purpose: Give every Wave 1 plan a stable target — folders, utilities, locale keys, and test files exist so widget plans can run in parallel without race conditions.

Output: Folder scaffold, working `toArDigits()`, complete locale extensions, 12 skipped E2E specs, CI gate scaffold (commented).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/39-kanban-calendar/39-CONTEXT.md
@.planning/phases/39-kanban-calendar/39-RESEARCH.md
@.planning/phases/39-kanban-calendar/39-PATTERNS.md
@.planning/phases/38-dashboard-verbatim/38-00-PLAN.md
@.planning/phases/38-dashboard-verbatim/38-00-SUMMARY.md

<interfaces>
From `/tmp/inteldossier-handoff/inteldossier/project/src/data.jsx#L8`:
```js
const AR_DIGITS = ['٠','١','٢','٣','٤','٥','٦','٧','٨','٩']
const toArDigits = (s, lang) => lang === 'ar' ? String(s).replace(/[0-9]/g, d => AR_DIGITS[d]) : String(s)
```

From PATTERNS.md SP-5 — i18n hook usage everywhere downstream:

```ts
const { t, i18n } = useTranslation('unified-kanban') // or 'calendar'
const lang = i18n.language
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold WorkBoard folder + barrel + port toArDigits utility</name>
  <files>frontend/src/pages/WorkBoard/index.ts, frontend/src/lib/i18n/toArDigits.ts, frontend/src/lib/i18n/__tests__/toArDigits.test.ts</files>
  <read_first>
    - .planning/phases/39-kanban-calendar/39-PATTERNS.md (lines 64-77 for barrel shape; lines 366-383 for toArDigits skeleton)
    - frontend/src/pages/Dashboard/widgets/index.ts (Phase 38 barrel analog — verbatim shape, just fewer exports)
    - /tmp/inteldossier-handoff/inteldossier/project/src/data.jsx (lines 1-20 — AR_DIGITS source)
  </read_first>
  <action>
    Create `frontend/src/pages/WorkBoard/index.ts` exactly:

    ```ts
    export { WorkBoard } from './WorkBoard'
    export { KCard } from './KCard'
    export { BoardColumn } from './BoardColumn'
    export { BoardToolbar } from './BoardToolbar'
    ```

    The four imported files do NOT exist yet — Wave 1 creates them. To avoid TypeScript errors before Wave 1 ships, ALSO create empty placeholder files at:
    - `frontend/src/pages/WorkBoard/WorkBoard.tsx` (export `export const WorkBoard = (): null => null` with `// PLACEHOLDER: replaced in 39-04`)
    - `frontend/src/pages/WorkBoard/KCard.tsx` (export `export const KCard = (): null => null` with `// PLACEHOLDER: replaced in 39-01`)
    - `frontend/src/pages/WorkBoard/BoardColumn.tsx` (export `export const BoardColumn = (): null => null` with `// PLACEHOLDER: replaced in 39-02`)
    - `frontend/src/pages/WorkBoard/BoardToolbar.tsx` (export `export const BoardToolbar = (): null => null` with `// PLACEHOLDER: replaced in 39-03`)

    Then create `frontend/src/lib/i18n/toArDigits.ts`:

    ```ts
    const AR_DIGITS = ['٠', '١', '٢', '٣', '٤', '٥', '٦', '٧', '٨', '٩'] as const

    export function toArDigits(input: string | number, lang: string): string {
      const s = String(input)
      if (lang !== 'ar') return s
      return s.replace(/[0-9]/g, (d) => AR_DIGITS[Number(d)])
    }
    ```

    Then create `frontend/src/lib/i18n/__tests__/toArDigits.test.ts` covering:
    - `toArDigits('123', 'en')` → `'123'`
    - `toArDigits('123', 'ar')` → `'١٢٣'`
    - `toArDigits(0, 'ar')` → `'٠'`
    - `toArDigits('25 Apr', 'ar')` → `'٢٥ Apr'` (only digits transformed, letters preserved)
    - `toArDigits('Overdue 62d', 'ar')` → `'Overdue ٦٢d'`
    - never compose with `toLocaleString('ar')` — Landmine from RESEARCH.md.

    Use `describe('toArDigits', ...)`, `it(...)`, `expect(...).toBe(...)` (vitest).

  </action>
  <verify>
    <automated>pnpm --filter frontend test --run src/lib/i18n/__tests__/toArDigits.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/pages/WorkBoard/index.ts` contains the four exact exports listed above
    - File `frontend/src/lib/i18n/toArDigits.ts` contains string `'٠','١','٢','٣','٤','٥','٦','٧','٨','٩'`
    - `pnpm --filter frontend test --run src/lib/i18n/__tests__/toArDigits.test.ts` exits 0 with at least 5 passing tests
    - `grep -c "PLACEHOLDER" frontend/src/pages/WorkBoard/*.tsx` returns 4
    - `pnpm --filter frontend typecheck` exits 0 (placeholders prevent broken barrel)
  </acceptance_criteria>
  <done>WorkBoard barrel + 4 placeholder components + toArDigits utility + 5+ passing unit tests committed.</done>
</task>

<task type="auto">
  <name>Task 2: Extend unified-kanban + calendar i18n namespaces (EN + AR)</name>
  <files>frontend/public/locales/en/unified-kanban.json, frontend/public/locales/ar/unified-kanban.json, frontend/public/locales/en/calendar.json, frontend/public/locales/ar/calendar.json</files>
  <read_first>
    - .planning/phases/39-kanban-calendar/39-PATTERNS.md (lines 432-465 — full key list with Arabic mirrors)
    - frontend/public/locales/en/unified-kanban.json (read existing keys to avoid collision)
    - frontend/public/locales/ar/unified-kanban.json (verify Arabic structure)
    - frontend/public/locales/en/calendar.json (existing weekday.* keys must remain)
  </read_first>
  <action>
    Append to `frontend/public/locales/en/unified-kanban.json` (merge with existing top-level keys; do NOT overwrite unrelated keys):

    ```json
    {
      "filters": {
        "byStatus": "By status",
        "byDossier": "By dossier",
        "byOwner": "By owner",
        "comingSoon": "Coming soon",
        "search": "Search work items…"
      },
      "overdueChip": "{{count}} overdue",
      "actions": {
        "newItem": "New item",
        "listView": "List view",
        "addToColumn": "Add to {{column}}"
      }
    }
    ```

    Append to `frontend/public/locales/ar/unified-kanban.json` (merge):

    ```json
    {
      "filters": {
        "byStatus": "بالحالة",
        "byDossier": "بالملف",
        "byOwner": "بالمسؤول",
        "comingSoon": "قريبًا",
        "search": "بحث في عناصر العمل…"
      },
      "overdueChip": "{{count}} متأخر",
      "actions": {
        "newItem": "عنصر جديد",
        "listView": "عرض القائمة",
        "addToColumn": "إضافة إلى {{column}}"
      }
    }
    ```

    Append to `frontend/public/locales/en/calendar.json` (merge — preserve any existing `weekday.*` block):

    ```json
    {
      "weeklist": {
        "previousWeek": "Previous week",
        "nextWeek": "Next week",
        "today": "Today"
      },
      "actions": { "newEvent": "New event" }
    }
    ```

    Append to `frontend/public/locales/ar/calendar.json` (merge):

    ```json
    {
      "weeklist": {
        "previousWeek": "الأسبوع السابق",
        "nextWeek": "الأسبوع التالي",
        "today": "اليوم"
      },
      "actions": { "newEvent": "فعالية جديدة" }
    }
    ```

    Use a JSON-aware merge (read original, parse, deep-merge, write with 2-space indent + trailing newline). Do NOT use `cat`/`echo` heredocs. Do NOT delete or rename any pre-existing key.

  </action>
  <verify>
    <automated>node -e "['en','ar'].forEach(l => { const k=require('./frontend/public/locales/'+l+'/unified-kanban.json'); if(!k.filters||!k.filters.byStatus||!k.overdueChip||!k.actions||!k.actions.newItem) {process.exit(1)} const c=require('./frontend/public/locales/'+l+'/calendar.json'); if(!c.weeklist||!c.weeklist.today||!c.actions||!c.actions.newEvent) {process.exit(1)} }); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "byStatus" frontend/public/locales/en/unified-kanban.json` returns at least 1
    - `grep -c "بالحالة" frontend/public/locales/ar/unified-kanban.json` returns at least 1
    - `grep -c "{{count}} overdue" frontend/public/locales/en/unified-kanban.json` returns at least 1
    - `grep -c "{{count}} متأخر" frontend/public/locales/ar/unified-kanban.json` returns at least 1
    - `grep -c "previousWeek" frontend/public/locales/en/calendar.json` returns at least 1
    - `grep -c "الأسبوع السابق" frontend/public/locales/ar/calendar.json` returns at least 1
    - `node -e 'JSON.parse(require("fs").readFileSync("./frontend/public/locales/ar/unified-kanban.json"))'` exits 0 (valid JSON)
    - All four locale files keep existing `weekday.*` and any pre-existing keys (run `git diff --stat` to confirm minimal additions)
  </acceptance_criteria>
  <done>All 4 locale files contain the full Phase 39 key set; valid JSON; existing keys preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Create 12 Playwright spec stubs + commented Phase 39 CI gate block</name>
  <files>frontend/tests/e2e/kanban-render.spec.ts, frontend/tests/e2e/kanban-rtl.spec.ts, frontend/tests/e2e/kanban-visual.spec.ts, frontend/tests/e2e/kanban-search.spec.ts, frontend/tests/e2e/kanban-filters.spec.ts, frontend/tests/e2e/kanban-responsive.spec.ts, frontend/tests/e2e/kanban-a11y.spec.ts, frontend/tests/e2e/kanban-dnd.spec.ts, frontend/tests/e2e/calendar-render.spec.ts, frontend/tests/e2e/calendar-rtl.spec.ts, frontend/tests/e2e/calendar-visual.spec.ts, frontend/tests/e2e/calendar-mobile.spec.ts, frontend/tests/e2e/calendar-a11y.spec.ts, scripts/check-deleted-components.sh</files>
  <read_first>
    - frontend/tests/e2e/dashboard.spec.ts (Phase 38 analog for spec shape: imports, login helper, page.goto)
    - frontend/tests/e2e/dashboard-visual.spec.ts (visual-regression analog — matrix shape)
    - frontend/tests/e2e/dashboard-a11y.spec.ts (axe analog)
    - scripts/check-deleted-components.sh (existing Phase 34 + 36 blocks — append with same shape)
    - .planning/phases/39-kanban-calendar/39-PATTERNS.md (lines 468-484 — spec → analog mapping)
  </read_first>
  <action>
    For EACH of the 12 specs in `<files>` (excluding `kanban-dnd.spec.ts` which gets a slightly different stub), create a skipped placeholder:

    ```ts
    import { test } from '@playwright/test'

    test.describe.skip('Phase 39: <descriptive-name>', () => {
      test.skip('placeholder — assertions added by Wave 1 + Wave 2 plans', async () => {
        // Plan 39-XX activates this — see .planning/phases/39-kanban-calendar/<plan>.md
      })
    })
    ```

    Replace `<descriptive-name>` and `<plan>` per spec:
    - `kanban-render.spec.ts` → "Kanban render" / `39-04`
    - `kanban-rtl.spec.ts` → "Kanban RTL" / `39-04`
    - `kanban-visual.spec.ts` → "Kanban visual regression" / `39-09`
    - `kanban-search.spec.ts` → "Kanban client-side search" / `39-03`
    - `kanban-filters.spec.ts` → "Kanban filter pill aria-disabled" / `39-03`
    - `kanban-responsive.spec.ts` → "Kanban responsive 320/768/1280" / `39-07`
    - `kanban-a11y.spec.ts` → "Kanban axe LTR + RTL" / `39-09`
    - `kanban-dnd.spec.ts` → "Kanban drag-between-columns" / `39-04` — **DO NOT overwrite if `frontend/tests/e2e/kanban-drag-drop.spec.ts` already covers it; instead, write the new file as a skipped stub that delegates: `// SUPERSEDES kanban-drag-drop.spec.ts in 39-09 cleanup`**
    - `calendar-render.spec.ts` → "Calendar grid render" / `39-05`
    - `calendar-rtl.spec.ts` → "Calendar RTL Arabic dow + Indic digits" / `39-05`
    - `calendar-visual.spec.ts` → "Calendar visual regression" / `39-09`
    - `calendar-mobile.spec.ts` → "Calendar week-list <640px" / `39-07`
    - `calendar-a11y.spec.ts` → "Calendar axe LTR + RTL" / `39-09`

    Then APPEND to `scripts/check-deleted-components.sh` (do NOT replace existing arrays — concat). Append the entire Phase 39 block COMMENTED OUT with `# 39-09 ACTIVATES:` prefix on every line so the script still passes today:

    ```bash
    # ============================================================================
    # 39-09 ACTIVATES: Phase 39 kanban-calendar deletions (D-04)
    # ============================================================================
    # PHASE_39_PATTERNS=(
    #   "from.*components/unified-kanban/UnifiedKanbanBoard"
    #   "from.*components/unified-kanban/EnhancedKanbanBoard"
    #   "from.*components/unified-kanban/UnifiedKanbanCard"
    #   "from.*components/unified-kanban/UnifiedKanbanColumn"
    #   "from.*components/unified-kanban/UnifiedKanbanHeader"
    #   "from.*components/unified-kanban/utils/swimlane-utils"
    #   "from.*components/unified-kanban/utils/wip-limits"
    #   "from.*components/ui/kanban"
    # )
    # PHASE_39_DELETED_FILES=(
    #   "frontend/src/components/unified-kanban/UnifiedKanbanBoard.tsx"
    #   "frontend/src/components/unified-kanban/EnhancedKanbanBoard.tsx"
    #   "frontend/src/components/unified-kanban/UnifiedKanbanCard.tsx"
    #   "frontend/src/components/unified-kanban/UnifiedKanbanColumn.tsx"
    #   "frontend/src/components/unified-kanban/UnifiedKanbanHeader.tsx"
    #   "frontend/src/components/unified-kanban/utils/swimlane-utils.ts"
    #   "frontend/src/components/unified-kanban/utils/wip-limits.ts"
    #   "frontend/src/components/ui/kanban.tsx"
    #   "frontend/src/routes/_protected/my-work/board.tsx"
    # )

  </action>
  <verify>
    <automated>ls frontend/tests/e2e/kanban-*.spec.ts frontend/tests/e2e/calendar-*.spec.ts | wc -l | grep -E "^\s*1[3-9]\s*$|^\s*[2-9][0-9]\s*$" && grep -c "39-09 ACTIVATES" scripts/check-deleted-components.sh</automated>
  </verify>
  <acceptance_criteria>
    - 13 new spec files created (8 kanban + 5 calendar), each containing `test.describe.skip(`
    - `grep -l "PLACEHOLDER" frontend/tests/e2e/kanban-render.spec.ts frontend/tests/e2e/calendar-render.spec.ts` returns both files OR both contain `test.describe.skip`
    - `pnpm --filter frontend exec playwright test tests/e2e/kanban-render.spec.ts --list` exits 0 (parses without error)
    - `bash scripts/check-deleted-components.sh` exits 0 (Phase 39 block is commented, does not yet enforce)
    - `grep -c "PHASE_39_DELETED_FILES" scripts/check-deleted-components.sh` returns at least 1 (commented block present)
    - `grep -c "my-work/board.tsx" scripts/check-deleted-components.sh` returns at least 1 (legacy redirect target queued)
  </acceptance_criteria>
  <done>13 skipped E2E spec stubs exist; CI gate has commented Phase 39 block ready for 39-09 activation; current CI still green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                                     | Description                                               |
| ---------------------------------------------------------------------------- | --------------------------------------------------------- |
| n/a — Wave 0 is pure scaffolding (utility port + JSON merge + skipped specs) | No new endpoints, no auth surface, no rendered user input |

## STRIDE Threat Register

| Threat ID             | Category | Component         | Disposition | Mitigation Plan                                                                                                        |
| --------------------- | -------- | ----------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------- |
| T-39-00-INFO-LEAK     | I        | toArDigits        | accept      | Pure transform, no I/O, no logging                                                                                     |
| T-39-00-MERGE-CORRUPT | T        | locale JSON merge | mitigate    | Use JSON.parse + deep-merge + JSON.stringify (no string concat); verify parse round-trip in Task 2 acceptance criteria |

</threat_model>

<verification>
- `pnpm --filter frontend test --run src/lib/i18n/__tests__/toArDigits.test.ts` passes
- `pnpm --filter frontend typecheck` exits 0
- All locale JSON files parse cleanly (`node -e 'JSON.parse(...)'`)
- `pnpm --filter frontend exec playwright test --list` exits 0
- `bash scripts/check-deleted-components.sh` exits 0 (CI gate still green pre-Wave 2)
</verification>

<success_criteria>
WorkBoard folder, toArDigits utility, locale extensions, and 13 skipped E2E specs exist. Wave 1 plans (39-01..39-08) can now run in parallel because their target files all exist.
</success_criteria>

<output>
After completion, create `.planning/phases/39-kanban-calendar/39-00-SUMMARY.md`.
</output>
