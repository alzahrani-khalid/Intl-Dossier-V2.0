---
plan_id: 40-18
phase: 40
phase_name: list-pages
mode: gap_closure
gaps_addressed: [G4, G8]
requirements_addressed: []
depends_on: [40-12, 40-13, 40-14, 40-15, 40-16]
wave: 3
files_modified:
  - frontend/tests/e2e/list-pages-render.spec.ts
  - frontend/tests/e2e/list-pages-rtl.spec.ts
  - frontend/tests/e2e/list-pages-engagements.spec.ts
  - frontend/tests/e2e/list-pages-a11y.spec.ts
  - frontend/tests/e2e/list-pages-touch-targets.spec.ts
autonomous: true
estimated_loc: 80
must_haves:
  truths:
    - 'Engagements filter spec asserts pill labels meeting/call/travel/event (not Confirmed/Pending/Travel)'
    - "Each filter pill click toggles aria-pressed: clicked pill = 'true', others = 'false'"
    - 'Spec assertions reference EngagementListItem field names that exist in shipped types (name_en, name_ar, host_country_en, host_country_ar, engagement_type, engagement_status, participant_count)'
    - 'Working_groups spec uses underscored route /dossiers/working_groups'
    - 'VIP chip spec checks importance_level >= 4 (not is_vip)'
    - "Topics spec works against useDossiersByType('topic') shape (not a custom useTopics fixture)"
  artifacts:
    - path: 'frontend/tests/e2e/list-pages-engagements.spec.ts'
      provides: 'Filter pill assertions aligned with type-taxonomy'
      contains: 'meeting'
  key_links:
    - from: 'frontend/tests/e2e/list-pages-*.spec.ts'
      to: 'Phase 40 SUMMARY deviation notes'
      via: 'spec assertion reconciliation'
      pattern: 'name_en|engagement_type|importance_level'
---

<objective>
Spec reconciliation pass: walk each Phase 40 SUMMARY's "Deviations" section and update spec assertions to match shipped TypeScript exports. Closes G4 (engagement filter taxonomy alignment, spec-only) and G8 (specs assume idealized props/taxonomies).

Five known deviations to reconcile (per CONTEXT-GAPS §G8):

1. **Engagement field names** (40-02b + 40-09 SUMMARY): `name_en/name_ar/host_country_en/host_country_ar/engagement_type/engagement_status/participant_count`
2. **GenericListPageItem.statusChipClass** value mapping (40-06/07/08 SUMMARY)
3. **Working_groups underscored route** (40-08 SUMMARY): `working_groups/` not `working-groups/`
4. **`useTopics` shim** (40-07 SUMMARY): wraps `useDossiersByType('topic')`
5. **VIP chip from `importance_level >= 4`** (40-05 SUMMARY) — no `is_vip` field

Plus G4: replace `Confirmed/Pending/Travel` filter labels with `meeting/call/travel/event` taxonomy.

Purpose: Closes G4 (2 specs) + G8 (multiple specs).
Output: Five spec files reconciled with shipped types; `tsc --noEmit` on the spec directory passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/40-list-pages/40-CONTEXT-GAPS.md
@.planning/phases/40-list-pages/40-02b-SUMMARY.md
@.planning/phases/40-list-pages/40-05-SUMMARY.md
@.planning/phases/40-list-pages/40-06-SUMMARY.md
@.planning/phases/40-list-pages/40-07-SUMMARY.md
@.planning/phases/40-list-pages/40-08-SUMMARY.md
@.planning/phases/40-list-pages/40-09-SUMMARY.md
</context>

<tasks>

<task id="t1" type="auto">
  <name>Task 1: Reconcile engagements filter pill taxonomy + aria-pressed (G4)</name>
  <files>frontend/tests/e2e/list-pages-engagements.spec.ts</files>
  <read_first>
    - frontend/tests/e2e/list-pages-engagements.spec.ts (full)
    - frontend/src/components/list-page/EngagementsList.tsx (filter pills section)
    - .planning/phases/40-list-pages/40-09-SUMMARY.md (toEngagementRow mapper)
    - .planning/phases/40-list-pages/40-CONTEXT-GAPS.md §G4
  </read_first>
  <action>
    In `frontend/tests/e2e/list-pages-engagements.spec.ts`, replace every occurrence of pill label assertions `Confirmed`, `Pending`, `Travel` with the shipped engagement-type taxonomy `meeting`, `call`, `travel`, `event`.

    Specifically:

    1. Find every `getByRole('button', { name: 'Confirmed' })` and replace with `getByRole('button', { name: /meeting/i })`.
    2. Replace `getByRole('button', { name: 'Pending' })` with `getByRole('button', { name: /call/i })`.
    3. Keep any `Travel` reference but change to lowercase: `getByRole('button', { name: /travel/i })`.
    4. Add a 4th pill assertion: `getByRole('button', { name: /event/i })`.

    Each click MUST assert `aria-pressed='true'` on the clicked pill AND `aria-pressed='false'` on the other three. Add a deterministic harness:

    ```ts
    test('engagement filter pills toggle aria-pressed correctly', async ({ page }) =&gt; {
      await page.goto('/dossiers/engagements')
      await page.waitForSelector('[data-loading="false"]')

      const pills = ['meeting', 'call', 'travel', 'event'] as const

      for (const target of pills) {
        await page.getByRole('button', { name: new RegExp(`^${target}$`, 'i') }).click()
        for (const p of pills) {
          const pill = page.getByRole('button', { name: new RegExp(`^${p}$`, 'i') })
          await expect(pill).toHaveAttribute('aria-pressed', p === target ? 'true' : 'false')
        }
      }
    })
    ```

    Replace any prior filter test with this harness. Re-run: `pnpm playwright test list-pages-engagements --project=chromium-darwin`.

    If `EngagementsList.tsx` doesn't already render `aria-pressed` on the pill buttons, ALSO patch `frontend/src/components/list-page/FilterPill.tsx` (or the inline pill JSX) so each pill button carries `aria-pressed={isActive ? 'true' : 'false'}`. This is the only IMPL change in this plan — the rest is spec-only.

  </action>
  <verify>
    <automated>pnpm playwright test list-pages-engagements --project=chromium-darwin --grep "filter pills" --reporter=line</automated>
  </verify>
  <done>4 pills with type-taxonomy labels, aria-pressed toggles correctly; filter test passes.</done>
</task>

<task id="t2" type="auto">
  <name>Task 2: Reconcile EngagementListItem field names across all spec files</name>
  <files>frontend/tests/e2e/list-pages-render.spec.ts, frontend/tests/e2e/list-pages-rtl.spec.ts, frontend/tests/e2e/list-pages-engagements.spec.ts</files>
  <read_first>
    - .planning/phases/40-list-pages/40-02b-SUMMARY.md
    - .planning/phases/40-list-pages/40-09-SUMMARY.md (toEngagementRow mapper)
  </read_first>
  <action>
    Walk every spec file under `frontend/tests/e2e/list-pages-*.spec.ts` and replace IDEALIZED field references with SHIPPED field references:

    | Idealized (replace) | Shipped (use) |
    |----------------------|---------------|
    | `engagement.name` | `engagement.name_en` (LTR) / `engagement.name_ar` (AR) |
    | `engagement.title` | `engagement.name_en` |
    | `engagement.host` | `engagement.host_country_en` / `engagement.host_country_ar` |
    | `engagement.type` | `engagement.engagement_type` |
    | `engagement.status` | `engagement.engagement_status` |
    | `engagement.participants` | `engagement.participant_count` |

    For each spec file in the files_modified list, run:
    `grep -nE "engagement\.(name|title|host|type|status|participants)\b" <file>`
    For every match, rewrite using the shipped field. If the spec accesses these via DOM (`page.getByText(engagement.name)`), the rewrite is the JS variable name — the rendered text doesn't change.

    If any test fixture file (`frontend/tests/e2e/fixtures/*.ts`) defines an Engagement shape, update it to match `EngagementListItem` exactly per 40-02b SUMMARY. Run `pnpm --filter frontend exec tsc --noEmit -p tsconfig.test.json` (or the equivalent test tsconfig) to verify type-correctness.

  </action>
  <verify>
    <automated>pnpm --filter frontend exec tsc --noEmit -p tsconfig.json 2&gt;&amp;1 | head -20</automated>
  </verify>
  <done>All spec files use shipped EngagementListItem field names; tsc --noEmit on the test project passes.</done>
</task>

<task id="t3" type="auto">
  <name>Task 3: Reconcile working_groups underscored route + status mapping</name>
  <files>frontend/tests/e2e/list-pages-render.spec.ts, frontend/tests/e2e/list-pages-rtl.spec.ts, frontend/tests/e2e/list-pages-a11y.spec.ts, frontend/tests/e2e/list-pages-touch-targets.spec.ts</files>
  <read_first>
    - .planning/phases/40-list-pages/40-08-SUMMARY.md (status mapping: active→ok, completed→info, planned→warn, cancelled→danger; underscored route)
  </read_first>
  <action>
    Across all spec files in this task's files_modified list:

    1. Replace any URL `/dossiers/working-groups` (hyphen) with `/dossiers/working_groups` (underscore). Search:
       `grep -rnE "working-groups" frontend/tests/e2e/`
       Every match → replace with underscored form.

    2. For specs that assert chip class names on working_groups status, align with 40-08 mapping:
       - `active` → `chip-ok` (or `bg-ok-soft text-ok-ink`)
       - `completed` → `chip-info`
       - `planned` → `chip-warn`
       - `cancelled` → `chip-danger`

    3. Spec must reference `data-status` attribute on the chip (or the actual class names rendered) — NOT a hypothetical `data-chip` testid.

  </action>
  <verify>
    <automated>! grep -rE "working-groups" frontend/tests/e2e/ ; echo "exit=$?"</automated>
  </verify>
  <done>No hyphenated `working-groups` references in spec dir; status chip assertions match 40-08 mapping.</done>
</task>

<task id="t4" type="auto">
  <name>Task 4: Reconcile VIP chip derivation (importance_level &gt;= 4) + useTopics shim</name>
  <files>frontend/tests/e2e/list-pages-render.spec.ts, frontend/tests/e2e/list-pages-a11y.spec.ts</files>
  <read_first>
    - .planning/phases/40-list-pages/40-05-SUMMARY.md (VIP chip from importance_level &gt;= 4)
    - .planning/phases/40-list-pages/40-07-SUMMARY.md (useTopics shim)
  </read_first>
  <action>
    1. **VIP chip:** Search `grep -rnE "is_vip|isVip" frontend/tests/e2e/list-pages-*.spec.ts`. For every match, the assertion that "VIP chip is present when X" must use `importance_level >= 4` as the condition. If a fixture sets `is_vip: true`, change to `importance_level: 5`. If a spec asserts the chip's existence, the trigger condition in the test setup must use the importance_level threshold.

    2. **useTopics shim:** If any spec mocks `useTopics` directly (rare — most use Supabase fixtures), replace with mock of `useDossiersByType('topic')` per 40-07 SUMMARY. Search: `grep -rnE "useTopics" frontend/tests/e2e/`. Each match → adjust to the actual hook used.

    3. **GenericListPageItem.statusChipClass:** Per 40-06/07 SUMMARY, the value is the actual className string (e.g., `'bg-ok-soft text-ok-ink border-ok-soft'`), not a logical name. If specs assert `getByTestId('status-chip').toHaveAttribute('data-chip', 'ok')`, change to `.toHaveClass(/bg-ok-soft/)`.

  </action>
  <verify>
    <automated>! grep -rE "is_vip|isVip" frontend/tests/e2e/list-pages-*.spec.ts ; echo "exit=$?"</automated>
  </verify>
  <done>VIP chip assertions use `importance_level >= 4` trigger; useTopics references aligned with shipped hook; statusChipClass assertions use actual className strings.</done>
</task>

<task id="t5" type="auto">
  <name>Task 5: Type-check + final spec re-run</name>
  <files>(verification only)</files>
  <read_first>
    - frontend/tsconfig.json
  </read_first>
  <action>
    1. Type-check the spec directory: `pnpm --filter frontend exec tsc --noEmit`. Expected: 0 errors.

    2. Run the entire list-pages spec suite (excluding visual which is owned by 40-17):
       ```
       pnpm playwright test \
         list-pages-render \
         list-pages-rtl \
         list-pages-engagements \
         list-pages-a11y \
         list-pages-touch-targets \
         --project=chromium-darwin --reporter=line
       ```
       Expected: 0 failures across all 5 spec files.

    3. If any failure remains, classify:
       - Real bug → file finding in 40-18-SUMMARY.md and reopen as 40-19a follow-up.
       - Lingering deviation → patch in this plan and re-run.

  </action>
  <verify>
    <automated>pnpm --filter frontend exec tsc --noEmit</automated>
  </verify>
  <done>tsc --noEmit clean; 5/5 spec files green excluding visual (handled in 40-17).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description              |
| -------- | ------------------------ |
| (none)   | Spec reconciliation only |

## STRIDE Threat Register

| Threat ID  | Category | Component | Disposition | Mitigation Plan   |
| ---------- | -------- | --------- | ----------- | ----------------- |
| T-40-18-01 | n/a      | n/a       | accept      | Test-only changes |

</threat_model>

<verification>
- tsc --noEmit clean
- 5 spec files green
- No hyphenated `working-groups`, no `is_vip`, no idealized field names remain
</verification>

<success_criteria>

- G4 + G8 closed
- All Phase 40 SUMMARY deviations reconciled
  </success_criteria>

<output>
After completion, create `.planning/phases/40-list-pages/40-18-SUMMARY.md`
</output>
