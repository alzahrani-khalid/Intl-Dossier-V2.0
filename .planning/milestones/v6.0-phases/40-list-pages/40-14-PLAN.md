---
plan_id: 40-14
phase: 40
phase_name: list-pages
mode: gap_closure
gaps_addressed: [G1]
requirements_addressed: []
depends_on: [40-13]
wave: 1
files_modified:
  - frontend/src/components/list-page/ListPageShell.tsx
  - frontend/src/components/list-page/DossierTable.tsx
  - frontend/src/components/list-page/EngagementsList.tsx
  - frontend/src/components/list-page/PersonsGrid.tsx
autonomous: true
estimated_loc: 25
must_haves:
  truths:
    - 'At 320×720, 768×1024, 1280×800 the document.scrollWidth equals clientWidth on all 7 list pages × LTR + AR (84 cases)'
    - 'No flex/grid descendant of ListPageShell lacks min-w-0 where its content can exceed its container'
  artifacts:
    - path: 'frontend/src/components/list-page/ListPageShell.tsx'
      provides: 'Outer wrapper with min-w-0 + overflow-x-hidden on the main content area'
      contains: 'min-w-0'
    - path: 'frontend/src/components/list-page/DossierTable.tsx'
      provides: 'Table row containers with min-w-0 on long-text cells'
      contains: 'min-w-0'
  key_links:
    - from: 'frontend/tests/e2e/list-pages-render.spec.ts'
      to: 'frontend/src/components/list-page/ListPageShell.tsx'
      via: 'viewport overflow assertion'
      pattern: 'scrollWidth.*clientWidth'
---

<objective>
Audit `ListPageShell.tsx`, `DossierTable.tsx`, `EngagementsList.tsx`, `PersonsGrid.tsx` for missing `min-w-0` on flex/grid children whose content can exceed the container width. Apply `min-w-0` to the offending wrapper(s); add `overflow-x-hidden` to the outermost shell content area as a belt-and-braces guard. Closes G1 (12 specs failing on mobile/tablet viewports).

Purpose: Long names ("Test Working Group A — Climate Coordination" or AR equivalents) overflow the row container at <1280px, pushing `scrollWidth > clientWidth`. CSS spec: a flex/grid item's default `min-width: auto` lets it grow past `min-content`; `min-w-0` (`min-width: 0`) restores normal shrink behavior.
Output: Four files patched; horizontal overflow eliminated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/40-list-pages/40-CONTEXT-GAPS.md
@.planning/phases/40-list-pages/40-HUMAN-UAT.md
@CLAUDE.md
</context>

<tasks>

<task id="t1" type="auto">
  <name>Task 1: Audit + patch ListPageShell outer wrapper</name>
  <files>frontend/src/components/list-page/ListPageShell.tsx</files>
  <read_first>
    - frontend/src/components/list-page/ListPageShell.tsx (full file — currently ~60 lines)
    - .planning/phases/40-list-pages/40-CONTEXT-GAPS.md §G1
  </read_first>
  <action>
    In `frontend/src/components/list-page/ListPageShell.tsx`:

    1. Locate the outermost root `<div>` (the one that now carries `data-loading` from Plan 40-13). Add `min-w-0` to its className. Final className must contain BOTH `min-w-0` and the existing classes verbatim.

    2. Locate the inner content area where `{children}` renders (typically a `<main>` or `<div>` wrapping the table/list). Add BOTH `min-w-0` AND `overflow-x-hidden`. Final className must contain `min-w-0 overflow-x-hidden` plus existing classes.

    3. If the shell uses any flex container (`flex` or `flex-col` or `flex-row`), every direct child rendering variable-width content (toolbar, table, pagination) must have `min-w-0` on its className.

    Example transformation (illustrative — adapt to actual structure):

    BEFORE:
    ```tsx
    return (
      &lt;div data-loading={isLoading ? 'true' : 'false'} className="flex flex-col gap-4 px-4 sm:px-6 lg:px-8"&gt;
        &lt;header className="..."&gt;...&lt;/header&gt;
        &lt;main className="flex-1"&gt;{children}&lt;/main&gt;
      &lt;/div&gt;
    )
    ```

    AFTER:
    ```tsx
    return (
      &lt;div data-loading={isLoading ? 'true' : 'false'} className="flex flex-col gap-4 px-4 sm:px-6 lg:px-8 min-w-0"&gt;
        &lt;header className="... min-w-0"&gt;...&lt;/header&gt;
        &lt;main className="flex-1 min-w-0 overflow-x-hidden"&gt;{children}&lt;/main&gt;
      &lt;/div&gt;
    )
    ```

    Do NOT introduce `overflow-x-auto` — gap-closure decision per CONTEXT-GAPS is to clip, not scroll horizontally.
    Do NOT use `ml-*`/`mr-*`/`pl-*`/`pr-*` — project rule: logical properties only.

  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const f = fs.readFileSync('frontend/src/components/list-page/ListPageShell.tsx', 'utf8'); const ok = f.includes('min-w-0') &amp;&amp; f.includes('overflow-x-hidden') &amp;&amp; !f.match(/\\b(ml|mr|pl|pr)-/); process.exit(ok ? 0 : 1);"</automated>
  </verify>
  <done>ListPageShell root + content area both contain `min-w-0`; content area contains `overflow-x-hidden`; no physical-direction utilities introduced.</done>
</task>

<task id="t2" type="auto">
  <name>Task 2: Audit + patch DossierTable, EngagementsList, PersonsGrid for min-w-0 on truncating cells</name>
  <files>frontend/src/components/list-page/DossierTable.tsx, frontend/src/components/list-page/EngagementsList.tsx, frontend/src/components/list-page/PersonsGrid.tsx</files>
  <read_first>
    - frontend/src/components/list-page/DossierTable.tsx (full)
    - frontend/src/components/list-page/EngagementsList.tsx (full)
    - frontend/src/components/list-page/PersonsGrid.tsx (full)
    - .planning/phases/40-list-pages/40-CONTEXT-GAPS.md §G1
  </read_first>
  <action>
    For each of the three files, walk the JSX top-down and apply this rule:

    **Rule:** Any element that is BOTH (a) a direct child of a flex container AND (b) wraps content that could overflow (text labels, name strings, meta lines) MUST have `min-w-0` on its className. Additionally, any descendant that uses `truncate` (line-clamp single line) MUST also have `min-w-0` on its parent flex item — `truncate` is a no-op without `min-w-0`.

    Specifically check:

    **DossierTable.tsx:**
    - The row `<button>` or `<Link>` wrapper of each row → if `flex` or `inline-flex`, add `min-w-0` to the cell containing the dossier name (`name_en`/`name_ar`).
    - Any `<span className="truncate">` → ensure parent has `min-w-0`.
    - The chevron column should stay fixed-width (`shrink-0`) — do NOT add `min-w-0` to fixed-width sidebar elements.

    **EngagementsList.tsx:**
    - The row container (`<button>` per row) → ensure the title column has `min-w-0`.
    - The host_country meta line → if it sits in a flex row beside a chip/badge, give the text wrapper `min-w-0`.

    **PersonsGrid.tsx:**
    - The grid card content → ensure the text column (name + role · organization) has `min-w-0` so the avatar (44×44 fixed) doesn't push text overflow.

    For every change, ALSO add `truncate` (or keep existing) on the actual text element so long names ellipsize cleanly instead of wrapping mid-word.

    Hard prohibition: do NOT add `overflow-hidden` on grid cards — Aceternity card variants rely on overflow for shadow rendering. Use `min-w-0` + `truncate` on text spans only.

  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const files = ['frontend/src/components/list-page/DossierTable.tsx','frontend/src/components/list-page/EngagementsList.tsx','frontend/src/components/list-page/PersonsGrid.tsx']; let ok = true; for (const fp of files) { const f = fs.readFileSync(fp,'utf8'); if (!f.includes('min-w-0')) { console.error(fp, 'missing min-w-0'); ok = false; } if (f.match(/\\b(ml|mr|pl|pr)-\\d/)) { console.error(fp, 'physical direction utility introduced'); ok = false; } } process.exit(ok ? 0 : 1);"</automated>
  </verify>
  <done>All three files contain at least one `min-w-0`; no `ml-*`/`mr-*`/`pl-*`/`pr-*` introduced; truncating spans correctly parented.</done>
</task>

<task id="t3" type="auto">
  <name>Task 3: Smoke-verify via Playwright render spec at three viewports</name>
  <files>frontend/tests/e2e/list-pages-render.spec.ts (READ ONLY — do not modify)</files>
  <read_first>
    - frontend/tests/e2e/list-pages-render.spec.ts (full)
  </read_first>
  <action>
    Run the existing render spec to confirm no horizontal overflow at 320×720, 768×1024, 1280×800:

    `pnpm playwright test list-pages-render --project=chromium-darwin`

    Expected: 0 failures across all 7 pages × LTR + AR. If any page still overflows, re-audit that page's specific component for missing `min-w-0` on a wrapper not caught above. Common culprits: a `<div className="flex"><span>{longName}</span></div>` where `<span>` lacks `min-w-0`.

    Do NOT modify the spec file in this plan — that's Plan 40-18's reconciliation pass. If a spec assertion is genuinely incorrect (e.g., wrong viewport), defer to 40-18 and document in the SUMMARY.

  </action>
  <verify>
    <automated>pnpm playwright test list-pages-render --project=chromium-darwin --reporter=line</automated>
  </verify>
  <done>list-pages-render spec passes on all viewports × pages × locales (84 cases).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description               |
| ---------------------------- | ------------------------- |
| (none — pure CSS layout fix) | No trust boundary crossed |

## STRIDE Threat Register

| Threat ID  | Category | Component | Disposition | Mitigation Plan                              |
| ---------- | -------- | --------- | ----------- | -------------------------------------------- |
| T-40-14-01 | n/a      | n/a       | accept      | Pure presentation change, no security impact |

</threat_model>

<verification>
- `pnpm playwright test list-pages-render` — 0 failures
- ESLint logical-properties rule still green (no `ml-*`/`mr-*`/`pl-*`/`pr-*` introduced)
- Vitest list-page suite still 66/66 green
</verification>

<success_criteria>

- 12 G1 spec failures resolved
- Mobile + tablet viewports clean across all 7 list pages
  </success_criteria>

<output>
After completion, create `.planning/phases/40-list-pages/40-14-SUMMARY.md`
</output>
