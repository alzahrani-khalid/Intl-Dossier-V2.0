---
phase: 40-list-pages
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/public/locales/en/list-pages.json
  - frontend/public/locales/ar/list-pages.json
  - frontend/public/locales/en/countries.json
  - frontend/public/locales/ar/countries.json
  - frontend/public/locales/en/organizations.json
  - frontend/public/locales/ar/organizations.json
  - frontend/public/locales/en/persons.json
  - frontend/public/locales/ar/persons.json
  - frontend/public/locales/en/forums.json
  - frontend/public/locales/ar/forums.json
  - frontend/public/locales/en/topics.json
  - frontend/public/locales/ar/topics.json
  - frontend/public/locales/en/working-groups.json
  - frontend/public/locales/ar/working-groups.json
  - frontend/public/locales/en/engagements.json
  - frontend/public/locales/ar/engagements.json
autonomous: true
requirements: [LIST-01, LIST-02, LIST-03, LIST-04]
must_haves:
  truths:
    - 'All 7 entity namespaces resolve in EN at /dossiers/{type} pages'
    - 'All 7 entity namespaces resolve in AR (Tajawal renders) at /dossiers/{type} pages'
    - 'Shared list-pages namespace provides search placeholder, load-more button, and 4 filter pill labels'
    - 'Sensitivity chip labels (public/internal/restricted/confidential) translate in both locales — mapping: 1→public, 2→internal, 3→restricted, 4→confidential per RESEARCH lock'
    - 'Engagements load-more bilingual loading text key (loadMore.loading) exists in both locales'
  artifacts:
    - path: 'frontend/public/locales/en/list-pages.json'
      provides: 'Shared chrome strings (search, loadMore, pill labels)'
      contains: 'search.placeholder'
    - path: 'frontend/public/locales/ar/list-pages.json'
      provides: 'Arabic parity'
      contains: 'loadMore.loading'
    - path: 'frontend/public/locales/en/countries.json'
      provides: 'Countries page strings + sensitivity chip labels + table column labels'
      contains: 'table.sensitivity'
    - path: 'frontend/public/locales/ar/engagements.json'
      provides: 'Engagements AR strings (load-more, week-of, filter pills)'
      contains: 'loadMore.loading'
  key_links:
    - from: 'ListPageShell consumers (Wave 1 page bodies)'
      to: 'list-pages namespace'
      via: "useTranslation(['{entity}', 'list-pages'])"
      pattern: "useTranslation\\(\\[.*'list-pages'\\]\\)"
---

<objective>
Create all 16 locale namespace JSON files (8 namespaces × EN + AR) consumed by every Phase 40 page. Research confirms NONE of these namespaces currently exist in `frontend/public/locales/{en,ar}/` — without them, every Wave 1 page renders raw i18n keys (Pitfall 3 in RESEARCH.md).

Purpose: Unblock all 7 Wave 1 page plans (each `useTranslation(['{entity}', 'list-pages'])` call needs both namespaces present).
Output: 16 JSON files in `frontend/public/locales/{en,ar}/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/40-list-pages/40-CONTEXT.md
@.planning/phases/40-list-pages/40-RESEARCH.md
@.planning/phases/40-list-pages/40-PATTERNS.md

<interfaces>
<!-- Analog locale shape: frontend/public/locales/en/dossier.json -->
<!-- Sensitivity chip mapping LOCKED in RESEARCH §"Sensitivity chip + last-touch column mapping" -->

Sensitivity numeric → label key map (consumed by DossierTable in Wave 0 plan 02):

- 1 → "sensitivity.public" (chip-info)
- 2 → "sensitivity.internal" (chip-default)
- 3 → "sensitivity.restricted" (chip-warn) ← LOCK per RESEARCH §"Sensitivity chip + last-touch column mapping"
- 4 → "sensitivity.confidential" (chip-danger) ← LOCK per RESEARCH §"Sensitivity chip + last-touch column mapping"

Filter pill keys (engagements only): pill.all, pill.confirmed, pill.travel, pill.pending

Shared list-pages namespace shape (RESEARCH §Recommended Project Structure):
{
"search": { "placeholder": "Search…" / "بحث…" },
"loadMore": { "button": "Load more" / "تحميل المزيد", "loading": "Loading more engagements…" / "جارٍ تحميل المزيد من الارتباطات…" },
"pill": { "all": "All", "confirmed": "Confirmed", "travel": "Travel", "pending": "Pending" },
"shownCount": "shown" / "معروض",
"empty": { "title": "Nothing here yet", "description": "Records will appear once created." }
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write shared list-pages namespace (EN + AR)</name>
  <files>frontend/public/locales/en/list-pages.json, frontend/public/locales/ar/list-pages.json</files>
  <read_first>
    - frontend/public/locales/en/dossier.json (analog shape)
    - .planning/phases/40-list-pages/40-RESEARCH.md §"Recommended Project Structure" + §"Filter pill active styling anatomy"
    - .planning/phases/40-list-pages/40-PATTERNS.md §"Shared `list-pages.json`"
  </read_first>
  <action>
Create `frontend/public/locales/en/list-pages.json` with the exact JSON object below (no trailing comma, single nested level for chrome strings):

```json
{
  "search": { "placeholder": "Search…" },
  "loadMore": { "button": "Load more", "loading": "Loading more engagements…" },
  "pill": { "all": "All", "confirmed": "Confirmed", "travel": "Travel", "pending": "Pending" },
  "shownCount": "shown",
  "empty": { "title": "Nothing here yet", "description": "Records will appear once created." }
}
```

Create `frontend/public/locales/ar/list-pages.json` with Arabic parity:

```json
{
  "search": { "placeholder": "بحث…" },
  "loadMore": { "button": "تحميل المزيد", "loading": "جارٍ تحميل المزيد من الارتباطات…" },
  "pill": { "all": "الكل", "confirmed": "مؤكد", "travel": "سفر", "pending": "قيد الانتظار" },
  "shownCount": "معروض",
  "empty": { "title": "لا توجد سجلات بعد", "description": "ستظهر السجلات بمجرد إنشائها." }
}
```

Use the Write tool, never heredoc. Pretty-print with 2-space indent (project convention — match `dossier.json`).
</action>
<verify>
<automated>test -f frontend/public/locales/en/list-pages.json && test -f frontend/public/locales/ar/list-pages.json && node -e "JSON.parse(require('fs').readFileSync('frontend/public/locales/en/list-pages.json'));JSON.parse(require('fs').readFileSync('frontend/public/locales/ar/list-pages.json'));console.log('OK')"</automated>
</verify>
<acceptance_criteria> - Both files exist and parse as valid JSON - `frontend/public/locales/en/list-pages.json` contains key `loadMore.loading` with value `"Loading more engagements…"` - `frontend/public/locales/ar/list-pages.json` contains key `loadMore.loading` with value `"جارٍ تحميل المزيد من الارتباطات…"` - `grep -c '"pill"' frontend/public/locales/en/list-pages.json` returns 1 - `grep -c '"pill"' frontend/public/locales/ar/list-pages.json` returns 1
</acceptance_criteria>
<done>Shared list-pages namespace exists in EN and AR with chrome, pill, loadMore, search, shownCount, and empty keys parity-matched.</done>
</task>

<task type="auto">
  <name>Task 2: Write 7 entity namespaces × EN + AR (14 files)</name>
  <files>frontend/public/locales/en/countries.json, frontend/public/locales/ar/countries.json, frontend/public/locales/en/organizations.json, frontend/public/locales/ar/organizations.json, frontend/public/locales/en/persons.json, frontend/public/locales/ar/persons.json, frontend/public/locales/en/forums.json, frontend/public/locales/ar/forums.json, frontend/public/locales/en/topics.json, frontend/public/locales/ar/topics.json, frontend/public/locales/en/working-groups.json, frontend/public/locales/ar/working-groups.json, frontend/public/locales/en/engagements.json, frontend/public/locales/ar/engagements.json</files>
  <read_first>
    - frontend/public/locales/en/dossier.json (analog shape — pull `title`, `subtitle`, `table`, `empty`, `loading`, `chip` patterns)
    - .planning/phases/40-list-pages/40-RESEARCH.md §"Sensitivity chip + last-touch column mapping" (sensitivity labels)
    - .planning/phases/40-list-pages/40-PATTERNS.md §"frontend/public/locales/{en,ar}/*.json" (shape template)
  </read_first>
  <action>
Create 14 files. Each entity namespace MUST contain: `title`, `subtitle`, `empty.title`, `empty.description`, `loading`, `chip` (entity-specific status labels), and where applicable `table` (Countries/Organizations only) or `card` (Persons only) or `row` (Forums/Topics/Working-Groups) or `filter` + `week` (Engagements only).

**EN/AR pairs to write (use Write tool for each file):**

1. countries.json — title "Countries"/"الدول", subtitle "All country dossiers"/"جميع ملفات الدول", `table: { name: "Country"/"الدولة", engagements: "Engagements"/"الارتباطات", lastTouch: "Last updated"/"آخر تحديث", sensitivity: "Sensitivity"/"الحساسية" }`, `sensitivity: { public: "Public"/"عام", internal: "Internal"/"داخلي", confidential: "Confidential"/"سري", restricted: "Restricted"/"مقيد" }`, `empty: { title: "No countries yet"/"لا توجد دول بعد", description: "Country dossiers will appear here."/"ستظهر ملفات الدول هنا." }`, `loading: "Loading countries…"/"جارٍ تحميل الدول…"`

2. organizations.json — same shape with title "Organizations"/"المنظمات", subtitle "International bodies and agencies"/"الهيئات والوكالات الدولية", table.name "Organization"/"المنظمة", empty.title "No organizations yet"/"لا توجد منظمات بعد", same sensitivity keys, loading "Loading organizations…"/"جارٍ تحميل المنظمات…"

3. persons.json — title "Persons"/"الأشخاص", subtitle "VIPs and key contacts"/"الشخصيات وجهات الاتصال الرئيسية", `card: { role: "Role"/"المنصب", organization: "Organization"/"المنظمة" }`, `chip: { vip: "VIP"/"شخصية مهمة" }`, empty.title "No persons yet"/"لا يوجد أشخاص بعد", empty.description "VIP profiles will appear here."/"ستظهر ملفات الشخصيات هنا.", loading "Loading persons…"/"جارٍ تحميل الأشخاص…"

4. forums.json — title "Forums"/"المنتديات", subtitle "Multi-party conferences and summits"/"المؤتمرات والقمم متعددة الأطراف", `row: { meta: "Meta"/"البيانات الوصفية" }`, `status: { active: "Active"/"نشط", completed: "Completed"/"مكتمل", planned: "Planned"/"مخطط له", cancelled: "Cancelled"/"ملغى" }`, empty.title "No forums yet"/"لا توجد منتديات بعد", loading "Loading forums…"/"جارٍ تحميل المنتديات…"

5. topics.json — title "Topics"/"الموضوعات", subtitle "Policy areas and strategic initiatives"/"مجالات السياسات والمبادرات الاستراتيجية", `status: { active: "Active"/"نشط", archived: "Archived"/"مؤرشف", draft: "Draft"/"مسودة" }`, empty.title "No topics yet"/"لا توجد موضوعات بعد", loading "Loading topics…"/"جارٍ تحميل الموضوعات…"

6. working-groups.json — title "Working Groups"/"مجموعات العمل", subtitle "Committees and task forces"/"اللجان وفرق العمل", `status: { active: "Active"/"نشط", completed: "Completed"/"مكتمل", on_hold: "On hold"/"معلق" }`, empty.title "No working groups yet"/"لا توجد مجموعات عمل بعد", loading "Loading working groups…"/"جارٍ تحميل مجموعات العمل…"

7. engagements.json — title "Engagements"/"الارتباطات", subtitle "Meetings, consultations, and visits"/"الاجتماعات والمشاورات والزيارات", `search: { placeholder: "Search engagements…"/"ابحث في الارتباطات…" }`, `filter: { all: "All"/"الكل", confirmed: "Confirmed"/"مؤكد", travel: "Travel"/"سفر", pending: "Pending"/"قيد الانتظار" }`, `week: { of: "Week of {{date}}"/"أسبوع {{date}}" }`, `loadMore: { button: "Load more engagements"/"تحميل المزيد من الارتباطات", loading: "Loading more engagements…"/"جارٍ تحميل المزيد من الارتباطات…" }`, `status: { confirmed: "Confirmed"/"مؤكد", pending: "Pending"/"قيد الانتظار", completed: "Completed"/"مكتمل", cancelled: "Cancelled"/"ملغى" }`, empty.title "No engagements yet"/"لا توجد ارتباطات بعد", loading "Loading engagements…"/"جارٍ تحميل الارتباطات…"

Use the Write tool for each file. 2-space indent. No trailing commas. Use semantic Unicode (proper Arabic punctuation `…` not `...`).
</action>
<verify>
<automated>node -e "const f=['countries','organizations','persons','forums','topics','working-groups','engagements'];for(const n of f){for(const l of ['en','ar']){const p='frontend/public/locales/'+l+'/'+n+'.json';const j=JSON.parse(require('fs').readFileSync(p));if(!j.title||!j.empty||!j.loading)throw new Error(p+' missing required keys');}}console.log('14/14 OK');"</automated>
</verify>
<acceptance_criteria> - All 14 files exist and parse as valid JSON - Every entity namespace contains keys `title`, `empty.title`, `empty.description`, `loading` - `frontend/public/locales/en/countries.json` contains `table.sensitivity` AND `sensitivity.confidential` - `frontend/public/locales/en/persons.json` contains `chip.vip` - `frontend/public/locales/en/engagements.json` contains `filter.travel` AND `week.of` AND `loadMore.loading` - `frontend/public/locales/ar/engagements.json` contains `filter.travel` value `سفر` - `frontend/public/locales/en/forums.json` contains `status.active`
</acceptance_criteria>
<done>14 entity namespace files exist, all parse, every required key present, AR parity verified.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                               |
| ----------------------------------------- | ----------------------------------------------------------------------------------------- |
| Server (i18next loader) → Browser (React) | Locale JSON fetched over HTTPS at runtime; treated as trusted static asset (same-origin). |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                    | Disposition | Mitigation Plan                                                                                                                                                                       |
| ---------- | ---------------------- | ------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-40-01-01 | Tampering              | locale JSON files                                            | accept      | Static assets served from same origin behind authenticated route shell; no user-writable surface.                                                                                     |
| T-40-01-02 | Information disclosure | sensitivity labels (public/internal/confidential/restricted) | mitigate    | Labels are display-only; actual row visibility enforced by Supabase RLS on `dossiers` table (verified in Phase 33 RLS audit). Chip never exposes data the user couldn't already read. |
| T-40-01-03 | Information disclosure | i18n interpolation `{{date}}` in engagements `week.of`       | mitigate    | i18next escapes interpolated values by default; consumers use safe React JSX rendering only — no raw HTML insertion APIs anywhere in Phase 40.                                        |

</threat_model>

<verification>
- All 16 JSON files parse cleanly
- All 7 Wave 1 page plans can `useTranslation(['{entity}', 'list-pages'])` without raw-key fallback
- AR strings render with proper Arabic glyphs (no question marks / mojibake)
</verification>

<success_criteria>

- 16 files created at the exact paths in `files_modified`
- Required key paths exist in every file
- AR parity confirmed for every EN key
- Wave 1 unblocked
  </success_criteria>

<output>
After completion, create `.planning/phases/40-list-pages/40-01-SUMMARY.md`.
</output>
