---
phase: 40-list-pages
plan: 20
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260503120000_seed_persons_test_data.sql
autonomous: true
gap_closure: true
requirements: [LIST-02]
tags: [seed-data, persons, gap-G9, supabase-staging]

must_haves:
  truths:
    - '/dossiers/persons renders ≥10 person cards (D-04 anatomy: 44×44 circular avatar, EN+AR name, role · organization, VIP chip)'
    - '≥2 person dossiers have importance_level >= 4 so the VIP chip variant is visually verifiable'
    - 'Bilingual EN+AR coverage on every seeded person row'
    - 'Re-running the migration leaves the row count unchanged (idempotent via ON CONFLICT (id) DO NOTHING)'
  artifacts:
    - path: 'supabase/migrations/20260503120000_seed_persons_test_data.sql'
      provides: '10 person dossier rows + 10 persons extension rows'
      contains: 'INSERT INTO dossiers'
    - path: '(staging Supabase) public.dossiers'
      provides: "≥10 rows WHERE type='person' AND is_seed_data=true"
    - path: '(staging Supabase) public.persons'
      provides: '≥10 extension rows joined by id with importance_level distribution {1,2,3,4,5}'
  key_links:
    - from: 'supabase/migrations/20260503120000_seed_persons_test_data.sql'
      to: 'staging Supabase project zkrcjzdemdmwhearhfgg'
      via: 'mcp__claude_ai_Supabase__apply_migration'
      pattern: 'apply_migration name=seed_persons_test_data'
    - from: '/dossiers/persons (PersonsListPage)'
      to: '/persons API → search_persons_advanced RPC'
      via: 'INNER JOIN persons p ON d.id = p.id'
      pattern: 'p.importance_level >= 4 → is_vip badge'
---

<objective>
Phase 40 Gap-Closure G9: seed ≥10 bilingual person dossier fixtures into staging Supabase so the `/dossiers/persons` D-04 card aesthetic (44px avatar + name + role · org + VIP chip) is visually verifiable. The existing `populate_diplomatic_seed` RPC is short-circuited by `v_already=true` because other tables already have `is_seed_data=true` rows.

Purpose: unblock Test 4 (Persons list page — visual parity, currently `result: blocked, blocked_by: data-state` in 40-HUMAN-UAT.md).

Output: a single timestamped `supabase/migrations/*.sql` file applied to staging via Supabase MCP. Acceptance verifiable with one `SELECT count(*)` query.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-list-pages/40-HUMAN-UAT.md
@.planning/phases/40-list-pages/40-12-SUMMARY.md
@CLAUDE.md
@supabase/migrations/20260426120000_seed_working_groups_test_data.sql
@supabase/migrations/20251022000002_create_extension_tables.sql
@supabase/migrations/20260110000003_persons_entity_management.sql
@frontend/src/routes/_protected/dossiers/persons/-PersonsListPage.tsx

<interfaces>
<!-- Persons read path consumed by /dossiers/persons -->

dossiers (canonical row):
id UUID, type TEXT CHECK ∈ {country, organization, forum, person, engagement, topic, working_group},
name_en TEXT NOT NULL, name_ar TEXT,
description_en TEXT, description_ar TEXT,
status TEXT CHECK ∈ {active, inactive, archived, deleted},
sensitivity_level INT 1..4,
tags TEXT[],
is_seed_data BOOLEAN,
created_at TIMESTAMPTZ, updated_at TIMESTAMPTZ

persons (extension; PK references dossiers.id ON DELETE CASCADE):
id UUID PRIMARY KEY,
title_en TEXT, title_ar TEXT,
organization_id UUID REFERENCES organizations(id),
nationality_country_id UUID REFERENCES countries(id),
email TEXT, phone TEXT,
biography_en TEXT, biography_ar TEXT, photo_url TEXT,
importance_level INTEGER DEFAULT 1 CHECK (BETWEEN 1 AND 5),
-- 1=Regular, 2=Important, 3=Key, 4=VIP, 5=Critical
linkedin_url TEXT, twitter_url TEXT,
expertise_areas TEXT[], languages TEXT[], notes TEXT

PersonsListPage VIP rule (frontend/src/routes/\_protected/dossiers/persons/-PersonsListPage.tsx:54-56):
is_vip = (raw.is_vip === true) || (typeof raw.importance_level === 'number' && raw.importance_level >= 4)

Required `is_seed_data` flag: persons table does NOT have an `is_seed_data` column (verified in 20251022000002 + 20260110000003). The flag lives on dossiers only — that is the row used for cleanup targeting.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the persons seed migration</name>
  <files>supabase/migrations/20260503120000_seed_persons_test_data.sql</files>

<read_first> - supabase/migrations/20260426120000_seed_working_groups_test_data.sql (analog/template — comment header, INSERT shape, ON CONFLICT pattern, stable-UUID range, bilingual fixtures) - supabase/migrations/20251022000002_create_extension_tables.sql (lines 119-136: persons table definition) - supabase/migrations/20260110000003_persons_entity_management.sql (lines 13-19: importance_level column + comment legend) - frontend/src/routes/\_protected/dossiers/persons/-PersonsListPage.tsx (lines 36-58: card mapping + VIP rule) - .planning/phases/40-list-pages/40-12-SUMMARY.md (deviation notes — dossiers.status CHECK ∈ {active,inactive,archived,deleted}; dossiers has NO `last_touch`; persons has NO `is_seed_data` — apply same patterns here) - CLAUDE.md (project rules — bilingual EN+AR mandatory; Tajawal-friendly Arabic strings; do NOT include real PII)
</read_first>

  <action>
    Create the file `supabase/migrations/20260503120000_seed_persons_test_data.sql` with the following exact structure (modeled on `20260426120000_seed_working_groups_test_data.sql`):

    1. Header comment block explaining purpose (G9 closure), schema notes, and the importance_level → VIP mapping.

    2. Step 1: `INSERT INTO dossiers (...)` for **10 rows** with `type='person'`, `is_seed_data=true`, stable UUIDs in range `a0000000-0000-0000-0000-0000000005{01..10}`. All names are clearly fictional ("Test Person" prefix in EN, "شخص اختباري" prefix in AR). Use ONLY `dossiers.status` values from the CHECK set {active, inactive, archived, deleted}; default all 10 to `'active'` so they all surface (the page filters out `archived`).

       Distribute `sensitivity_level` 1..3 across rows for realism but keep most at 1 (low) so RLS doesn't hide them under default analyst clearance.

       Spread `updated_at` across `NOW() - INTERVAL '1 day'` to `NOW() - INTERVAL '120 days'` for visual rhythm.

       Exact 10 rows (use these literal name pairs):

       | UUID suffix | name_en | name_ar | importance_level (target) | role intent |
       |---|---|---|---|---|
       | 0501 | Test Person A — Senior Diplomat | شخص اختباري أ — دبلوماسي رفيع | 5 | Critical (VIP) |
       | 0502 | Test Person B — Trade Envoy | شخص اختباري ب — مبعوث تجاري | 4 | VIP |
       | 0503 | Test Person C — Policy Advisor | شخص اختباري ج — مستشار سياسات | 3 | Key |
       | 0504 | Test Person D — Regional Coordinator | شخص اختباري د — منسق إقليمي | 3 | Key |
       | 0505 | Test Person E — Technical Attaché | شخص اختباري هـ — ملحق تقني | 2 | Important |
       | 0506 | Test Person F — Cultural Liaison | شخص اختباري و — منسق ثقافي | 2 | Important |
       | 0507 | Test Person G — Economics Analyst | شخص اختباري ز — محلل اقتصادي | 1 | Regular |
       | 0508 | Test Person H — Climate Specialist | شخص اختباري ح — أخصائي مناخ | 1 | Regular |
       | 0509 | Test Person I — Health Coordinator | شخص اختباري ط — منسق صحي | 1 | Regular |
       | 0510 | Test Person J — Digital Affairs Officer | شخص اختباري ي — مسؤول الشؤون الرقمية | 1 | Regular |

       Wrap rows in a single multi-row VALUES list, terminate with `ON CONFLICT (id) DO NOTHING;`. Set every row to `is_seed_data=true` and `tags = ARRAY['test', 'person']`.

       Provide bilingual `description_en` / `description_ar` (one-sentence "Test fixture for Phase 40 persons list page (G9 gap-closure)." in both languages — Arabic equivalent: "بيانات اختبارية للمرحلة 40 - صفحة الأشخاص (سد الثغرة G9).").

    3. Step 2: `INSERT INTO persons (id, title_en, title_ar, importance_level, email, ...)` for the same 10 UUIDs (no `is_seed_data` column on persons — verified in schema).

       Pair each UUID with the importance_level from the table above and a bilingual `title_en` / `title_ar` matching the role intent (e.g., for 0501: `title_en='Senior Diplomat'`, `title_ar='دبلوماسي رفيع'`).

       Email pattern: `testperson{a..j}@example.test` (do NOT use real domains — these are clearly fake fixtures). Phone: leave NULL. organization_id and nationality_country_id: leave NULL (avoid coupling to other seed data).

       Wrap in a single multi-row VALUES + `ON CONFLICT (id) DO NOTHING;`.

    4. Trailing comment block: state the verification SQL `SELECT count(*) FROM dossiers WHERE type='person' AND is_seed_data=true` should return ≥10.

    Do NOT introduce any new columns, helper functions, or RLS policies. Do NOT modify other migrations. Do NOT use real names of public figures, real email domains, or real phone numbers — these are clearly fictional fixtures (G9 threat-model requirement).

  </action>

  <verify>
    <automated>test -f supabase/migrations/20260503120000_seed_persons_test_data.sql &amp;&amp; grep -c "^  ('a0000000-0000-0000-0000-0000000005" supabase/migrations/20260503120000_seed_persons_test_data.sql | grep -E '^(20|2[1-9])$'</automated>
  </verify>

<acceptance_criteria> - File `supabase/migrations/20260503120000_seed_persons_test_data.sql` EXISTS - File contains the literal string `INSERT INTO dossiers` (case-sensitive) - File contains the literal string `INSERT INTO persons` (case-sensitive) - File contains the literal string `ON CONFLICT (id) DO NOTHING` AT LEAST 2 times (one per INSERT block) — verify: `grep -c "ON CONFLICT (id) DO NOTHING" supabase/migrations/20260503120000_seed_persons_test_data.sql` returns `2` - File contains the literal string `is_seed_data` AT LEAST once on the dossiers INSERT (verify: `grep -c "is_seed_data" supabase/migrations/20260503120000_seed_persons_test_data.sql` returns ≥ `1`) - File contains exactly 10 dossier UUIDs starting `'a0000000-0000-0000-0000-0000000005` — verify: `grep -c "^  ('a0000000-0000-0000-0000-0000000005" supabase/migrations/20260503120000_seed_persons_test_data.sql` returns `20` (10 rows × 2 INSERTs = 20 occurrences) - File contains the literal string `importance_level` at least 10 times (one per persons row) — verify: `grep -c importance_level supabase/migrations/20260503120000_seed_persons_test_data.sql` returns ≥ `10` - File contains AT LEAST 2 lines with `importance_level=4` OR `importance_level=5` literal in the persons INSERT — verify: `grep -cE "importance_level=(4|5)|, (4|5),.*-- VIP" supabase/migrations/20260503120000_seed_persons_test_data.sql` returns ≥ `2` (use whichever syntax matches the chosen INSERT shape; literal `, 5,` and `, 4,` after a `'persons'` INSERT line counts) - File contains Arabic Unicode characters in name_ar values — verify: `grep -cE 'شخص اختباري' supabase/migrations/20260503120000_seed_persons_test_data.sql` returns ≥ `10` - File does NOT contain any string matching `gmail\.com|outlook\.com|hotmail\.com|yahoo\.com` (no real email domains) — verify: `grep -cE 'gmail\.com|outlook\.com|hotmail\.com|yahoo\.com' supabase/migrations/20260503120000_seed_persons_test_data.sql` returns `0`
</acceptance_criteria>

<done>The migration file exists locally, contains 10 idempotent person dossiers + 10 persons extension rows with bilingual fixtures, and the VIP-tier subset (importance_level ≥ 4) is at least 2.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply the migration to staging Supabase via MCP</name>
  <files>(no local file changes; staging Supabase project zkrcjzdemdmwhearhfgg)</files>

<read_first> - The migration file written in Task 1: supabase/migrations/20260503120000_seed_persons_test_data.sql - .planning/phases/40-list-pages/40-12-SUMMARY.md (analog: same MCP apply pattern; project_id zkrcjzdemdmwhearhfgg) - CLAUDE.md (Deployment Configuration section — confirms project_id zkrcjzdemdmwhearhfgg, region eu-west-2)
</read_first>

  <action>
    Apply the migration to staging Supabase using the MCP tool `mcp__claude_ai_Supabase__apply_migration`:

    - `project_id`: `zkrcjzdemdmwhearhfgg`
    - `name`: `seed_persons_test_data`
    - `query`: the full SQL contents of `supabase/migrations/20260503120000_seed_persons_test_data.sql` (read the file then pass its body)

    After the call succeeds (`success: true`), run a verification query via `mcp__claude_ai_Supabase__execute_sql`:

    - `project_id`: `zkrcjzdemdmwhearhfgg`
    - `query`: `SELECT count(*)::int AS person_count, count(*) FILTER (WHERE p.importance_level >= 4)::int AS vip_count FROM dossiers d INNER JOIN persons p ON p.id = d.id WHERE d.type = 'person' AND d.is_seed_data = true;`

    Capture the result. Both `person_count >= 10` AND `vip_count >= 2` MUST hold. If either fails, the seed is incomplete — diagnose and re-apply (idempotency via ON CONFLICT means re-applying with corrections is safe).

    If `apply_migration` returns an error mentioning a CHECK constraint violation, RE-READ the migration file, locate the offending column/value, and amend the file FIRST (never weaken a CHECK constraint to make seed data fit — fix the seed values to match the constraint, exactly as plan 40-12 did).

  </action>

  <verify>
    <automated>echo "Verification SQL must be executed via mcp__claude_ai_Supabase__execute_sql AFTER apply_migration. Expected: person_count >= 10 AND vip_count >= 2."</automated>
  </verify>

<acceptance_criteria> - MCP `apply_migration` call returns `{ success: true }` with name `seed_persons_test_data` - MCP `execute_sql` SELECT returns `person_count >= 10` (record exact value in summary) - MCP `execute_sql` SELECT returns `vip_count >= 2` (record exact value in summary) - Re-running `apply_migration` with the same SQL leaves `person_count` unchanged (idempotency probe; record before/after counts in the summary)
</acceptance_criteria>

<done>Staging Supabase shows ≥10 person dossiers (with ≥2 VIP-tier) and the migration is idempotent. The Phase 40 `/dossiers/persons` page can now render the D-04 card aesthetic against real data.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                    |
| ---------------------------------- | ------------------------------------------------------------------------------ |
| local migration → staging Postgres | SQL applied via Supabase MCP runs as the service role; bypasses RLS            |
| seed fixtures → analyst UI         | Fictional names render in production-grade UI; must be unambiguously test data |

## STRIDE Threat Register

| Threat ID  | Category                   | Component                          | Disposition | Mitigation Plan                                                                                                                                                                                                                                         |
| ---------- | -------------------------- | ---------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ----------- | ---------------------- |
| T-40-G9-01 | I (Information disclosure) | seed migration SQL                 | mitigate    | All fixture names start with the literal "Test Person" / "شخص اختباري". Emails use the `.test` reserved TLD (RFC 2606). No real PII (real names, real emails, real phone numbers, real organization assignments). Acceptance criterion greps `gmail.com | outlook.com | hotmail.com | yahoo.com` to enforce. |
| T-40-G9-02 | T (Tampering)              | accidental promotion to production | mitigate    | Every dossiers row carries `is_seed_data=true` so the standard cleanup script can target and purge before any prod cutover. Acceptance criterion greps `is_seed_data` ≥ 1.                                                                              |
| T-40-G9-03 | E (Elevation of privilege) | persons.importance_level ≥ 4 (VIP) | accept      | importance_level is purely a UI presentation flag (drives the VIP chip), not an authorization input. Search RPC returns it as a sort key, not a permission gate. No mitigation required beyond the existing CHECK BETWEEN 1 AND 5.                      |
| T-40-G9-04 | D (Denial of service)      | duplicate seed re-runs             | mitigate    | `ON CONFLICT (id) DO NOTHING` on both INSERTs. Acceptance criterion: idempotency probe (re-apply leaves count unchanged).                                                                                                                               |

</threat_model>

<verification>
Single end-to-end check after Task 2 lands:

```sql
SELECT count(*)::int AS person_count,
       count(*) FILTER (WHERE p.importance_level >= 4)::int AS vip_count
FROM dossiers d
INNER JOIN persons p ON p.id = d.id
WHERE d.type = 'person'
  AND d.is_seed_data = true;
```

Expected: `person_count >= 10`, `vip_count >= 2`.

Then a smoke check via the front-end (manual, recorded in summary): visit `/dossiers/persons` and confirm at least 10 cards render with at least 2 visibly bearing the VIP chip. Live render verification of the full D-04 anatomy (44px avatar, role · org meta) is part of the post-AUTH-FIX HUMAN-UAT sweep, not this plan.
</verification>

<success_criteria>

- Migration file exists with all acceptance grep checks passing
- MCP apply_migration returns success
- Staging count of seeded person dossiers is ≥ 10
- Staging count of VIP-tier (importance_level ≥ 4) seeded persons is ≥ 2
- Re-applying the migration leaves counts unchanged (idempotent)
  </success_criteria>

<output>
After completion, create `.planning/phases/40-list-pages/40-20-SUMMARY.md` with:
- The exact file path created
- The MCP `apply_migration` request name and `success` field
- The MCP `execute_sql` SELECT result (person_count, vip_count) BEFORE and AFTER re-application probe
- Any deviations from this plan (e.g., adjusted importance_level distribution to satisfy a CHECK constraint discovered at apply-time)
- Confirmation that closes Phase 40 gap G9 and addresses requirement LIST-02
</output>
