---
phase: 40-list-pages
plan: 21
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/_protected/dossiers/forums/index.tsx
  - frontend/src/i18n/en/forums.json
  - frontend/src/i18n/ar/forums.json
autonomous: true
gap_closure: true
requirements: [LIST-03]
tags: [i18n, forums, gap-G10, bilingual]

must_haves:
  truths:
    - "/dossiers/forums page heading reads 'Forums' (EN) / 'المنتديات' (AR), not the column-header literal 'Title' / 'العنوان'"
    - "/dossiers/forums subtitle reads its bilingual full sentence, not the literal key 'subtitle'"
    - "Status chips on forum rows render localized labels: 'Active' (EN) / 'نشط' (AR), not the raw English key 'active'"
  artifacts:
    - path: 'frontend/src/routes/_protected/dossiers/forums/index.tsx'
      provides: 'ForumsListPage with corrected i18n keys'
      contains: "t('forums:pageTitle')"
    - path: 'frontend/src/i18n/en/forums.json'
      provides: 'EN namespace with `status` map covering chip variants used by FORUM_STATUS_CHIP'
    - path: 'frontend/src/i18n/ar/forums.json'
      provides: 'AR namespace with mirrored `status` map'
  key_links:
    - from: 'ForumsListPage component'
      to: "i18n bundled namespace 'forums'"
      via: "useTranslation(['forums', 'list-pages'])"
      pattern: "t('forums:pageTitle') resolves to forums.pageTitle"
    - from: 'FORUM_STATUS_CHIP keys (active/completed/planned/cancelled)'
      to: 'forums.status.* translations'
      via: 't(`forums:status.${status}`, { defaultValue: status })'
      pattern: 'every key in FORUM_STATUS_CHIP must exist in both locales'
---

<objective>
Phase 40 Gap-Closure G10: fix three i18n bugs on `/dossiers/forums` so user-visible text reflects the proper bilingual page heading, subtitle, and status chip labels — not column-header literals or raw enum keys.

Purpose: close `40-HUMAN-UAT.md` Test 5 (currently `result: issue, severity: major`) without re-implementing any other forums functionality. Row anatomy, RTL mirror, Tajawal application, chevron flip, and date formatting all already PASS — only the i18n string resolution is broken.

Output: a single route-file edit + two i18n JSON edits. Acceptance is grep-verifiable on the bundled JSON files and the route source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-list-pages/40-HUMAN-UAT.md
@CLAUDE.md
@frontend/src/routes/_protected/dossiers/forums/index.tsx
@frontend/src/i18n/en/forums.json
@frontend/src/i18n/ar/forums.json

<interfaces>
<!-- Existing forums namespace shape (bundled, not the runtime /public/locales copy) -->

forums.json (current, EN):

- "pageTitle": "Forums" ← EXISTS, currently UNUSED by route
- "pageSubtitle": "International forums, conferences, and multilateral meetings" ← EXISTS, currently UNUSED
- "title": "Title" ← column-header label, currently MISUSED as page heading
- "statuses": { active, inactive, archived, scheduled, ongoing, completed, cancelled } ← exists, but route looks for `status.*` (singular)

ForumsListPage call sites (frontend/src/routes/\_protected/dossiers/forums/index.tsx):

- line 114: `title={t('forums:title')}` → returns "Title" (BUG)
- line 115: `subtitle={t('forums:subtitle')}` → returns "subtitle" (BUG: key absent)
- line 82: `t(`forums:status.${status}`, { defaultValue: status })` → returns raw `status` value because `forums.status` is absent (the existing key is `forums.statuses`, plural)

FORUM_STATUS_CHIP map (line 33-38):

- active → chip-ok
- completed → chip-info
- planned → chip-accent
- cancelled → chip-danger
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the route to call the correct i18n keys</name>
  <files>frontend/src/routes/_protected/dossiers/forums/index.tsx</files>

<read_first> - frontend/src/routes/\_protected/dossiers/forums/index.tsx (full file — 138 lines) - frontend/src/i18n/en/forums.json (confirm `pageTitle` and `pageSubtitle` exist) - frontend/src/i18n/ar/forums.json (confirm same in AR) - .planning/phases/40-list-pages/40-HUMAN-UAT.md (G10 truths a/b/c)
</read_first>

  <action>
    Make exactly TWO line edits to `frontend/src/routes/_protected/dossiers/forums/index.tsx`:

    1. Replace line 114 from:
       ```
       title={t('forums:title')}
       ```
       to:
       ```
       title={t('forums:pageTitle')}
       ```

    2. Replace line 115 from:
       ```
       subtitle={t('forums:subtitle')}
       ```
       to:
       ```
       subtitle={t('forums:pageSubtitle')}
       ```

    Do NOT touch line 82 (the `t(`forums:status.${status}`, ...)` call) — Task 2 fixes that by adding the missing `status` keys to the i18n namespace, so the existing `defaultValue: status` fallback becomes redundant once Task 2 lands.

    Do NOT modify FORUM_STATUS_CHIP, the imports, the component shape, the navigate callback, or any layout. Two-line edit only.

  </action>

  <verify>
    <automated>cd frontend &amp;&amp; grep -c "t('forums:pageTitle')" src/routes/_protected/dossiers/forums/index.tsx | grep -q '^1$' &amp;&amp; grep -c "t('forums:pageSubtitle')" src/routes/_protected/dossiers/forums/index.tsx | grep -q '^1$' &amp;&amp; grep -cE "t\('forums:title'\)|t\('forums:subtitle'\)" src/routes/_protected/dossiers/forums/index.tsx | grep -q '^0$'</automated>
  </verify>

<acceptance_criteria> - `frontend/src/routes/_protected/dossiers/forums/index.tsx` contains the literal `t('forums:pageTitle')` exactly once — verify: `grep -c "t('forums:pageTitle')" frontend/src/routes/_protected/dossiers/forums/index.tsx` returns `1` - File contains `t('forums:pageSubtitle')` exactly once — verify: `grep -c "t('forums:pageSubtitle')" frontend/src/routes/_protected/dossiers/forums/index.tsx` returns `1` - File NO LONGER contains `t('forums:title')` (the bug call) — verify: `grep -c "t('forums:title')" frontend/src/routes/_protected/dossiers/forums/index.tsx` returns `0` - File NO LONGER contains `t('forums:subtitle')` (the bug call) — verify: `grep -c "t('forums:subtitle')" frontend/src/routes/_protected/dossiers/forums/index.tsx` returns `0` - `pnpm --filter frontend exec tsc --noEmit` passes (no new type errors introduced) — verify: command exit code 0
</acceptance_criteria>

<done>The route calls `forums:pageTitle` and `forums:pageSubtitle` keys that already exist in both locales, so the page heading and subtitle render their correct bilingual values.</done>
</task>

<task type="auto">
  <name>Task 2: Add status chip translations to both i18n namespaces</name>
  <files>frontend/src/i18n/en/forums.json, frontend/src/i18n/ar/forums.json</files>

<read_first> - frontend/src/i18n/en/forums.json (full 86 lines — note the existing `statuses` plural map) - frontend/src/i18n/ar/forums.json (full 86 lines — mirror structure) - frontend/src/routes/\_protected/dossiers/forums/index.tsx (FORUM_STATUS_CHIP at lines 33-38: active, completed, planned, cancelled — these are the four keys the route consumes) - .planning/phases/40-list-pages/40-HUMAN-UAT.md (G10 truth c)
</read_first>

  <action>
    Add a NEW top-level `status` key (singular, distinct from the existing `statuses` plural) to BOTH locale files. The route calls `t(`forums:status.${status}`, { defaultValue: status })` so the namespace path is `forums.status.<value>`.

    Insert the block AFTER the existing `"statuses": { ... }` block (preserve `statuses` — it is consumed elsewhere in the codebase; do NOT delete or rename it).

    Edit `frontend/src/i18n/en/forums.json` — add this exact block right after the closing `}` of the `statuses` object (insert a comma after the existing `}` of `statuses`, then the new block):

    ```json
      "status": {
        "active": "Active",
        "inactive": "Inactive",
        "completed": "Completed",
        "planned": "Planned",
        "cancelled": "Cancelled",
        "scheduled": "Scheduled",
        "ongoing": "Ongoing",
        "archived": "Archived",
        "on_hold": "On Hold"
      },
    ```

    Edit `frontend/src/i18n/ar/forums.json` — add the same shape with Arabic values:

    ```json
      "status": {
        "active": "نشط",
        "inactive": "غير نشط",
        "completed": "مكتمل",
        "planned": "مجدول",
        "cancelled": "ملغى",
        "scheduled": "مقرر",
        "ongoing": "جاري",
        "archived": "مؤرشف",
        "on_hold": "قيد الانتظار"
      },
    ```

    The four MUST-HAVE keys are `active`, `completed`, `planned`, `cancelled` (matching FORUM_STATUS_CHIP). The five extras (`inactive`, `scheduled`, `ongoing`, `archived`, `on_hold`) cover the broader status enum used elsewhere in the page (the `defaultValue: status` fallback would otherwise leak raw English).

    Validate the resulting JSON parses (no trailing-comma errors, no missing comma after `statuses`). Use a tool like `node -e "JSON.parse(require('fs').readFileSync('frontend/src/i18n/en/forums.json','utf8'))"` to confirm.

    Do NOT modify any other key. Do NOT remove `statuses`. Do NOT change `pageTitle` or `pageSubtitle`.

  </action>

  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('frontend/src/i18n/en/forums.json','utf8'))" &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('frontend/src/i18n/ar/forums.json','utf8'))" &amp;&amp; node -e "const en=require('./frontend/src/i18n/en/forums.json'); const ar=require('./frontend/src/i18n/ar/forums.json'); for (const k of ['active','completed','planned','cancelled']) { if (!en.status?.[k] || !ar.status?.[k]) { console.error('MISSING', k); process.exit(1); } } console.log('OK');"</automated>
  </verify>

<acceptance_criteria> - `frontend/src/i18n/en/forums.json` parses as valid JSON — verify: `node -e "JSON.parse(require('fs').readFileSync('frontend/src/i18n/en/forums.json','utf8'))"` exits 0 - `frontend/src/i18n/ar/forums.json` parses as valid JSON — verify: `node -e "JSON.parse(require('fs').readFileSync('frontend/src/i18n/ar/forums.json','utf8'))"` exits 0 - EN namespace resolves `status.active` to `"Active"` — verify: `node -e "console.log(require('./frontend/src/i18n/en/forums.json').status.active)"` prints `Active` - EN namespace resolves `status.completed`, `status.planned`, `status.cancelled` to non-empty strings — verify per-key node -e check - AR namespace resolves `status.active` to `"نشط"` — verify: `node -e "console.log(require('./frontend/src/i18n/ar/forums.json').status.active)"` prints `نشط` - AR namespace resolves `status.completed`, `status.planned`, `status.cancelled` to non-empty Arabic strings (each contains at least one Arabic Unicode character) - The existing `statuses` plural key is STILL present in both files — verify: `node -e "if (!require('./frontend/src/i18n/en/forums.json').statuses) process.exit(1)"` exits 0; same for AR - The existing `pageTitle` and `pageSubtitle` keys are unchanged — verify: `node -e "const en=require('./frontend/src/i18n/en/forums.json'); if (en.pageTitle!=='Forums' || !en.pageSubtitle) process.exit(1)"` exits 0
</acceptance_criteria>

<done>Both locales expose a `status.{active,completed,planned,cancelled,...}` map. The existing `t(`forums:status.${status}`, { defaultValue: status })` call now resolves to localized chip text in both EN and AR.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                          |
| -------------------------------- | ---------------------------------------------------- |
| i18n JSON → React render         | static keys + interpolation; no user-controlled HTML |
| route source → bundled namespace | build-time import; no runtime fetch                  |

## STRIDE Threat Register

| Threat ID   | Category                                   | Component                 | Disposition | Mitigation Plan                                                                                                                                                                                                                                                            |
| ----------- | ------------------------------------------ | ------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-40-G10-01 | T (Tampering) / I (Information disclosure) | i18n string interpolation | accept      | The fix uses static literal keys (`pageTitle`, `pageSubtitle`, `status.<enum>`) and a server-validated `status` enum value. No user-controlled string is interpolated into XSS-vulnerable contexts; React escapes by default. No new `dangerouslySetInnerHTML` introduced. |
| T-40-G10-02 | D (Denial of service)                      | malformed JSON crash      | mitigate    | Acceptance criterion runs `JSON.parse` on both files; build will also fail noisily if a trailing comma slips in.                                                                                                                                                           |

</threat_model>

<verification>
After both tasks land:

1. Build check: `pnpm --filter frontend exec tsc --noEmit` exits 0 (no new type errors).
2. JSON parse check: `node -e "JSON.parse(require('fs').readFileSync('frontend/src/i18n/en/forums.json','utf8')); JSON.parse(require('fs').readFileSync('frontend/src/i18n/ar/forums.json','utf8'))"` exits 0.
3. Smoke render check (recorded in summary, not gated): a dev-server screenshot of `/dossiers/forums` in EN shows page heading "Forums" and subtitle "International forums, conferences, and multilateral meetings"; same page in AR shows "المنتديات" and the Arabic subtitle. Status chip text reads "Active" / "نشط" instead of "active". Live verification of the chip-class variants (chip-ok, chip-info, chip-accent, chip-danger) requires forum rows with the corresponding status values — that is a separate seed concern outside G10 scope.
   </verification>

<success_criteria>

- Route file calls `forums:pageTitle` and `forums:pageSubtitle` (greps confirm)
- Route file no longer calls `forums:title` or `forums:subtitle` (greps return 0)
- Both forums.json locale files have a top-level `status` map covering at least `{active, completed, planned, cancelled}`
- Both locale files still have their original `statuses`, `pageTitle`, `pageSubtitle` keys intact
- `pnpm --filter frontend exec tsc --noEmit` passes
  </success_criteria>

<output>
After completion, create `.planning/phases/40-list-pages/40-21-SUMMARY.md` with:
- Diff summary of the route file (before/after for the two replaced lines)
- Diff summary of each i18n file (the new `status` block added after `statuses`)
- Output of the JSON-parse verification commands
- Confirmation that closes Phase 40 gap G10 and addresses requirement LIST-03
- Note that the chip-variant visual coverage (chip-info, chip-accent, chip-danger) still depends on having forum rows with non-active statuses; that is out of G10 scope and remains a future seed concern.
</output>
