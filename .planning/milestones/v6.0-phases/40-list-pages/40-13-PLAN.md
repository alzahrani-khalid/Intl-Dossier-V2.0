---
plan_id: 40-13
phase: 40
phase_name: list-pages
mode: gap_closure
gaps_addressed: [G3, G7]
requirements_addressed: []
depends_on: []
wave: 0
files_modified:
  - frontend/src/components/list-page/DossierTable.tsx
  - frontend/src/components/list-page/ListPageShell.tsx
  - frontend/tailwind.config.ts
autonomous: true
estimated_loc: 30
must_haves:
  truths:
    - 'Computed transform on row chevron is matrix(-1, 0, 0, 1, 0, 0) on RTL runs (scaleX(-1))'
    - "ListPageShell root exposes data-loading='true' while loading, 'false' when ready"
  artifacts:
    - path: 'frontend/src/components/list-page/DossierTable.tsx'
      provides: 'Chevron with inline scaleX(-1) transform on RTL'
      contains: 'scaleX(-1)'
    - path: 'frontend/src/components/list-page/ListPageShell.tsx'
      provides: 'data-loading attribute on shell root'
      contains: 'data-loading'
  key_links:
    - from: 'frontend/src/components/list-page/DossierTable.tsx'
      to: 'frontend/tests/e2e/list-pages-rtl.spec.ts'
      via: "computed-style assertion on [data-testid='row-chevron']"
      pattern: "matrix\\(-1, 0, 0, 1"
    - from: 'frontend/src/components/list-page/ListPageShell.tsx'
      to: 'frontend/tests/e2e/list-pages-visual.spec.ts (Plan 40-17)'
      via: 'page.waitForSelector(''[data-loading="false"]'')'
      pattern: 'data-loading'
---

<objective>
Two primitive-level fixes that unblock downstream waves:
1. **G3:** Replace Tailwind `rotate-180` with inline `scaleX(-1)` transform on row chevrons so the computed `transform` matches the spec assertion `matrix(-1, 0, 0, 1, 0, 0)` and the handoff CSS convention.
2. **G7-marker:** Add a `data-loading` attribute on the `ListPageShell` root that flips `'true' → 'false'` when `isLoading` settles, so visual specs can deterministically wait for the ready state.

Purpose: G3 unblocks 7 RTL specs; G7-marker unblocks the visual baseline determinism stack landing in 40-17.
Output: Two source files modified; primitives ready for downstream consumers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/40-list-pages/40-CONTEXT-GAPS.md
@.planning/phases/40-list-pages/40-HUMAN-UAT.md
@CLAUDE.md
</context>

<tasks>

<task id="t1" type="auto">
  <name>Task 1: Replace rotate-180 with scaleX(-1) on row chevron + add data-testid</name>
  <files>frontend/src/components/list-page/DossierTable.tsx</files>
  <read_first>
    - frontend/src/components/list-page/DossierTable.tsx (line 107: `<ChevronRight className={...}>`)
    - .planning/phases/40-list-pages/40-CONTEXT-GAPS.md §G3
    - CLAUDE.md §"Arabic RTL Support Guidelines"
  </read_first>
  <action>
    In `frontend/src/components/list-page/DossierTable.tsx`, locate the row chevron at line ~107:

    BEFORE:
    ```tsx
    &lt;ChevronRight className={`size-4 text-muted-foreground md:hidden ${isRTL ? 'rotate-180' : ''}`} aria-hidden="true" /&gt;
    ```

    AFTER:
    ```tsx
    &lt;ChevronRight
      data-testid="row-chevron"
      className="size-4 text-muted-foreground md:hidden"
      style={isRTL ? { transform: 'scaleX(-1)' } : undefined}
      aria-hidden="true"
    /&gt;
    ```

    Rationale:
    - `data-testid="row-chevron"` is what the spec selector targets (per CONTEXT-GAPS G3 verification line).
    - Inline `style={{ transform: 'scaleX(-1)' }}` produces computed `matrix(-1, 0, 0, 1, 0, 0)` (cross-checked: `scaleX(-1)` = `matrix(-1, 0, 0, 1, 0, 0)`).
    - Conditional via `style={isRTL ? {...} : undefined}` keeps LTR runs untouched.

    If any OTHER component in `frontend/src/components/list-page/` (e.g., `EngagementsList.tsx`, `PersonsGrid.tsx`, `GenericListPage.tsx`) renders a chevron with `rotate-180` for RTL flipping, apply the same pattern there. Search command:
    `grep -rn "rotate-180" frontend/src/components/list-page/`
    For every match in this directory tree, swap to the same inline `scaleX(-1)` pattern with `data-testid="row-chevron"`.

    Do NOT modify `rotate-180` usages outside `frontend/src/components/list-page/` — out of scope.

  </action>
  <verify>
    <automated>grep -q "scaleX(-1)" frontend/src/components/list-page/DossierTable.tsx &amp;&amp; grep -q "data-testid=\"row-chevron\"" frontend/src/components/list-page/DossierTable.tsx &amp;&amp; ! grep -rq "rotate-180" frontend/src/components/list-page/ ; echo "exit=$?"</automated>
  </verify>
  <done>DossierTable + any other list-page chevron uses inline `scaleX(-1)` with `data-testid="row-chevron"`; no `rotate-180` remains in `frontend/src/components/list-page/`.</done>
</task>

<task id="t2" type="auto">
  <name>Task 2: Add data-loading attribute on ListPageShell root</name>
  <files>frontend/src/components/list-page/ListPageShell.tsx</files>
  <read_first>
    - frontend/src/components/list-page/ListPageShell.tsx (`isLoading?: boolean` prop already exists at line 8, 26)
    - .planning/phases/40-list-pages/40-CONTEXT-GAPS.md §G7 step 2
  </read_first>
  <action>
    In `frontend/src/components/list-page/ListPageShell.tsx`, locate the outermost root element returned by the component (the wrapper around `{isLoading ? skeleton : children}` at line ~51).

    Add a `data-loading` attribute that reflects `isLoading`:

    Add this attribute to the OUTERMOST wrapper element (the one that exists in BOTH the loading and ready states — typically a `<div>` or `<main>` at the top of the JSX tree):

    ```tsx
    &lt;div
      data-loading={isLoading ? 'true' : 'false'}
      className="..." {/* keep existing className verbatim */}
    &gt;
      {/* existing children */}
    &lt;/div&gt;
    ```

    If the current shell renders `{isLoading ? <Skeleton /> : <>{children}</>}` as a fragment with no shared wrapper, ADD a wrapper `<div data-loading={...}>` around the conditional. The wrapper must NOT change layout — use `className="contents"` only if no other styling is currently on a parent; otherwise use a plain `<div>` with `display:contents`-equivalent or carry the existing wrapper's classes.

    If a wrapper already exists, add the attribute to it directly without changing className.

    Important: This attribute must remain `'true'` until the data has loaded AND children have rendered. Since `isLoading` already settles to `false` only when `useDossiersByType` resolves, the existing prop is sufficient. No new state needed.

  </action>
  <verify>
    <automated>grep -q "data-loading" frontend/src/components/list-page/ListPageShell.tsx &amp;&amp; grep -q "isLoading ? 'true' : 'false'" frontend/src/components/list-page/ListPageShell.tsx ; echo "exit=$?"</automated>
  </verify>
  <done>ListPageShell root carries `data-loading={isLoading ? 'true' : 'false'}`; visible in DOM via `document.querySelector('[data-loading]')`.</done>
</task>

<task id="t3" type="auto">
  <name>Task 3: Verify primitives via unit/manual smoke</name>
  <files>frontend/src/components/list-page/__tests__</files>
  <read_first>
    - frontend/src/components/list-page/__tests__ (existing unit tests if any)
    - frontend/src/components/list-page/DossierTable.tsx
    - frontend/src/components/list-page/ListPageShell.tsx
  </read_first>
  <action>
    Run vitest scoped to the list-page component tests to confirm no regressions from t1+t2:

    `pnpm --filter frontend vitest run src/components/list-page --reporter=verbose`

    Expected: all existing tests pass (66/66 from Phase 40-11 baseline). If any test asserts the previous `rotate-180` className, update its assertion to check `style.transform === 'scaleX(-1)'` instead — but ONLY in the `__tests__` folder under `frontend/src/components/list-page/`, NOT in E2E specs (those land in 40-17/40-18).

  </action>
  <verify>
    <automated>pnpm --filter frontend vitest run src/components/list-page --reporter=verbose</automated>
  </verify>
  <done>All list-page unit tests green; no rotate-180 references remain in the component tree.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description               |
| -------------------------------------- | ------------------------- |
| (none — internal CSS primitive change) | No trust boundary crossed |

## STRIDE Threat Register

| Threat ID  | Category | Component | Disposition | Mitigation Plan                              |
| ---------- | -------- | --------- | ----------- | -------------------------------------------- |
| T-40-13-01 | n/a      | n/a       | accept      | Pure presentation change, no security impact |

</threat_model>

<verification>
- `grep -rn "rotate-180" frontend/src/components/list-page/` returns nothing
- `grep -rn "scaleX(-1)" frontend/src/components/list-page/` returns ≥1 match
- ListPageShell exposes `data-loading` attribute
- Vitest list-page suite green
</verification>

<success_criteria>

- G3 spec assertion `matrix(-1, 0, 0, 1, 0, 0)` will match (verified in Wave 4 E2E re-run)
- G7-marker enables Plan 40-17's `waitForSelector('[data-loading="false"]')` strategy
  </success_criteria>

<output>
After completion, create `.planning/phases/40-list-pages/40-13-SUMMARY.md`
</output>
