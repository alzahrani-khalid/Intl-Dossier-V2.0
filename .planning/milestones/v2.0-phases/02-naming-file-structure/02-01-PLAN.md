---
phase: 02-naming-file-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/use-ai-suggestions.ts
  - frontend/src/hooks/use-bulk-selection.ts
  - frontend/src/hooks/use-compliance.ts
  - frontend/src/hooks/use-contributors.ts
  - frontend/src/hooks/use-delegation.ts
  - frontend/src/hooks/use-entity-links.ts
  - frontend/src/hooks/use-entity-search.ts
  - frontend/src/hooks/use-language.ts
  - frontend/src/hooks/use-mobile.tsx
  - frontend/src/hooks/use-offline-state.ts
  - frontend/src/hooks/use-optimistic-locking.ts
  - frontend/src/hooks/use-outside-click.tsx
  - frontend/src/hooks/use-queue-filters.ts
  - frontend/src/hooks/use-responsive.ts
  - frontend/src/hooks/use-tasks.ts
  - frontend/src/hooks/use-theme.ts
  - frontend/src/hooks/use-toast.ts
  - frontend/src/hooks/use-waiting-queue-actions.ts
  - frontend/src/components/ActivityFeed/
  - frontend/src/components/Calendar/
  - frontend/src/components/Dashboard/
  - frontend/src/components/Dossier/
  - frontend/src/components/Editor/
  - frontend/src/components/Forms/
  - frontend/src/components/KeyboardShortcuts/
  - frontend/src/components/Layout/
  - frontend/src/components/Legislation/
  - frontend/src/components/Notifications/
  - frontend/src/components/Search/
  - frontend/src/components/Table/
autonomous: true
requirements: [ARCH-01]

must_haves:
  truths:
    - 'All 18 kebab-case hook files in frontend/src/hooks/ are renamed to camelCase'
    - 'All 12 PascalCase component directories in frontend/src/components/ are renamed to kebab-case'
    - 'All import paths referencing renamed hooks and directories are updated'
    - 'pnpm build succeeds with zero errors after all renames'
  artifacts:
    - path: 'frontend/src/hooks/useToast.ts'
      provides: 'Renamed hook file (most imported, 25 refs)'
    - path: 'frontend/src/hooks/useTasks.ts'
      provides: 'Renamed hook file (6 refs)'
    - path: 'frontend/src/hooks/useTheme.ts'
      provides: 'Renamed hook file (6 refs)'
    - path: 'frontend/src/hooks/useResponsive.ts'
      provides: 'Renamed hook file (5 refs)'
    - path: 'frontend/src/components/dossier/'
      provides: 'Renamed directory (largest, 58 files, 20 refs)'
    - path: 'frontend/src/components/forms/'
      provides: 'Renamed directory (22 files, 11 refs)'
  key_links:
    - from: 'all consumers of use-toast.ts'
      to: 'frontend/src/hooks/useToast.ts'
      via: 'updated import path'
      pattern: "from '@/hooks/useToast'"
    - from: 'all consumers of Dossier/'
      to: 'frontend/src/components/dossier/'
      via: 'updated import paths'
      pattern: "from '@/components/dossier/"
---

<objective>
Rename all 18 kebab-case hook files to camelCase and all 12 PascalCase component directories to kebab-case, updating every import path referencing these files.

Purpose: Establishes consistent naming conventions per layer (D-05, D-06 for hooks; D-01 for component dirs) as the foundation for ARCH-01. Hooks and directory renames are independent and can be done in sequence within one plan.

Output: 30 renamed files/directories with all ~115 import path references updated, verified by `pnpm build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-naming-file-structure/02-CONTEXT.md
@.planning/phases/02-naming-file-structure/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename 18 kebab-case hook files to camelCase and update all import paths</name>
  <files>
    frontend/src/hooks/use-ai-suggestions.ts -> frontend/src/hooks/useAiSuggestions.ts
    frontend/src/hooks/use-bulk-selection.ts -> frontend/src/hooks/useBulkSelection.ts
    frontend/src/hooks/use-compliance.ts -> frontend/src/hooks/useCompliance.ts
    frontend/src/hooks/use-contributors.ts -> frontend/src/hooks/useContributors.ts
    frontend/src/hooks/use-delegation.ts -> frontend/src/hooks/useDelegation.ts
    frontend/src/hooks/use-entity-links.ts -> frontend/src/hooks/useEntityLinks.ts
    frontend/src/hooks/use-entity-search.ts -> frontend/src/hooks/useEntitySearch.ts
    frontend/src/hooks/use-language.ts -> frontend/src/hooks/useLanguage.ts
    frontend/src/hooks/use-mobile.tsx -> frontend/src/hooks/useMobile.tsx
    frontend/src/hooks/use-offline-state.ts -> frontend/src/hooks/useOfflineState.ts
    frontend/src/hooks/use-optimistic-locking.ts -> frontend/src/hooks/useOptimisticLocking.ts
    frontend/src/hooks/use-outside-click.tsx -> frontend/src/hooks/useOutsideClick.tsx
    frontend/src/hooks/use-queue-filters.ts -> frontend/src/hooks/useQueueFilters.ts
    frontend/src/hooks/use-responsive.ts -> frontend/src/hooks/useResponsive.ts
    frontend/src/hooks/use-tasks.ts -> frontend/src/hooks/useTasks.ts
    frontend/src/hooks/use-theme.ts -> frontend/src/hooks/useTheme.ts
    frontend/src/hooks/use-toast.ts -> frontend/src/hooks/useToast.ts
    frontend/src/hooks/use-waiting-queue-actions.ts -> frontend/src/hooks/useWaitingQueueActions.ts
    + all files importing from these hooks (~70 import references)
  </files>
  <read_first>
    frontend/src/hooks/use-toast.ts
    frontend/src/hooks/use-tasks.ts
    frontend/src/hooks/use-theme.ts
    frontend/src/hooks/use-responsive.ts
  </read_first>
  <action>
    Per D-05, D-06, D-09: Rename each kebab-case hook file to camelCase using `git mv`. Since macOS filesystem is case-insensitive, simple kebab-to-camelCase renames do NOT need the two-step temp rename (that is only for case-only changes like `Foo` -> `foo`; `use-toast` -> `useToast` changes more than case).

    For each of the 18 hook files:
    1. Run `git mv frontend/src/hooks/use-xxx.ts frontend/src/hooks/useXxx.ts`
    2. Find all files importing from the old path using grep: `grep -rl "from '@/hooks/use-xxx'" frontend/src/` and `grep -rl "from '@/hooks/use-xxx'" tests/`
    3. Update each import path from `@/hooks/use-xxx` to `@/hooks/useXxx`
    4. Also check for relative imports: `grep -rl "from './use-xxx'" frontend/src/hooks/` and update those too

    Complete rename table (old -> new):
    - use-ai-suggestions.ts -> useAiSuggestions.ts (2 import refs)
    - use-bulk-selection.ts -> useBulkSelection.ts (1 import ref)
    - use-compliance.ts -> useCompliance.ts (1 import ref)
    - use-contributors.ts -> useContributors.ts (2 import refs)
    - use-delegation.ts -> useDelegation.ts (4 import refs)
    - use-entity-links.ts -> useEntityLinks.ts (3 import refs)
    - use-entity-search.ts -> useEntitySearch.ts (1 import ref)
    - use-language.ts -> useLanguage.ts (3 import refs)
    - use-mobile.tsx -> useMobile.tsx (3 import refs)
    - use-offline-state.ts -> useOfflineState.ts (0 import refs -- may be dead code, rename anyway)
    - use-optimistic-locking.ts -> useOptimisticLocking.ts (1 import ref)
    - use-outside-click.tsx -> useOutsideClick.tsx (3 import refs)
    - use-queue-filters.ts -> useQueueFilters.ts (2 import refs)
    - use-responsive.ts -> useResponsive.ts (5 import refs)
    - use-tasks.ts -> useTasks.ts (6 import refs)
    - use-theme.ts -> useTheme.ts (6 import refs)
    - use-toast.ts -> useToast.ts (25 import refs -- highest, do this one carefully)
    - use-waiting-queue-actions.ts -> useWaitingQueueActions.ts (3 import refs)

    IMPORTANT: Do NOT rename the function/export names inside the files -- only rename the FILES and update IMPORT PATHS (per D-10: naming only, no refactoring).

    After all 18 renames and import updates, run `pnpm build` to verify zero broken imports.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && ls frontend/src/hooks/use-*.ts frontend/src/hooks/use-*.tsx 2>/dev/null | wc -l | grep -q '^0$' && echo "PASS: No kebab-case hooks remain" || echo "FAIL: Kebab-case hooks still exist"</automated>
  </verify>
  <acceptance_criteria>
    - `ls frontend/src/hooks/use-*.ts frontend/src/hooks/use-*.tsx 2>/dev/null` returns empty (zero kebab-case hook files)
    - `ls frontend/src/hooks/useToast.ts` exists
    - `ls frontend/src/hooks/useTasks.ts` exists
    - `ls frontend/src/hooks/useTheme.ts` exists
    - `grep -r "from '@/hooks/use-" frontend/src/ | wc -l` returns 0 (no old import paths remain)
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>All 18 hook files renamed to camelCase, all ~70 import references updated, pnpm build passes</done>
</task>

<task type="auto">
  <name>Task 2: Rename 12 PascalCase component directories to kebab-case and update all import paths</name>
  <files>
    frontend/src/components/ActivityFeed/ -> frontend/src/components/activity-feed/
    frontend/src/components/Calendar/ -> frontend/src/components/calendar/
    frontend/src/components/Dashboard/ -> frontend/src/components/dashboard/
    frontend/src/components/Dossier/ -> frontend/src/components/dossier/
    frontend/src/components/Editor/ -> frontend/src/components/editor/
    frontend/src/components/Forms/ -> frontend/src/components/forms/
    frontend/src/components/KeyboardShortcuts/ -> frontend/src/components/keyboard-shortcuts/
    frontend/src/components/Layout/ -> frontend/src/components/layout/
    frontend/src/components/Legislation/ -> frontend/src/components/legislation/
    frontend/src/components/Notifications/ -> frontend/src/components/notifications/
    frontend/src/components/Search/ -> frontend/src/components/search/
    frontend/src/components/Table/ -> frontend/src/components/table/
    + all files importing from these directories (~45 import references)
  </files>
  <read_first>
    frontend/src/components/Dossier/
    frontend/src/components/Forms/
    frontend/src/components/Layout/
  </read_first>
  <action>
    Per D-01, D-04, D-09: Rename each PascalCase component directory to kebab-case using `git mv`. Component FILES inside directories stay PascalCase (D-04) -- only the DIRECTORY name changes.

    CRITICAL macOS caveat: For single-word directories where only case changes (e.g., `Calendar` -> `calendar`), macOS APFS is case-insensitive, so `git mv Calendar calendar` silently fails. Use the two-step rename:
    ```
    git mv frontend/src/components/Calendar frontend/src/components/calendar-tmp
    git mv frontend/src/components/calendar-tmp frontend/src/components/calendar
    ```

    Multi-word PascalCase-to-kebab-case renames (e.g., `ActivityFeed` -> `activity-feed`) do NOT need the two-step because the names are genuinely different.

    Complete rename table with strategy:
    - ActivityFeed/ -> activity-feed/ (direct git mv, 1 import ref)
    - Calendar/ -> calendar/ (TWO-STEP via calendar-tmp, 3 import refs)
    - Dashboard/ -> dashboard/ (TWO-STEP via dashboard-tmp, 0 import refs)
    - Dossier/ -> dossier/ (TWO-STEP via dossier-tmp, 20 import refs -- LARGEST, do carefully)
    - Editor/ -> editor/ (TWO-STEP via editor-tmp, 0 import refs)
    - Forms/ -> forms/ (TWO-STEP via forms-tmp, 11 import refs)
    - KeyboardShortcuts/ -> keyboard-shortcuts/ (direct git mv, 2 import refs)
    - Layout/ -> layout/ (TWO-STEP via layout-tmp, 1 import ref)
    - Legislation/ -> legislation/ (TWO-STEP via legislation-tmp, 0 import refs)
    - Notifications/ -> notifications/ (TWO-STEP via notifications-tmp, 2 import refs)
    - Search/ -> search/ (TWO-STEP via search-tmp, 1 import ref)
    - Table/ -> table/ (TWO-STEP via table-tmp, 4 import refs)

    For each directory rename:
    1. Execute the `git mv` (two-step for case-only changes)
    2. Find all files importing from the old directory path: `grep -rl "from '@/components/OldDir" frontend/src/` and `grep -rl "from '@/components/OldDir" tests/`
    3. Update each import path from `@/components/OldDir/` to `@/components/new-dir/`
    4. Also check route files: `grep -rl "from '@/components/OldDir" frontend/src/routes/`

    Start with the largest directory (Dossier/ with 58 files and 20 import refs) to surface any issues early.

    After all 12 directory renames and import updates:
    1. Run `pnpm build` to verify zero broken imports
    2. Run `pnpm dev` briefly to regenerate `routeTree.gen.ts` if it changed, then stop the dev server
    3. Commit the regenerated `routeTree.gen.ts` if modified

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && ls -d frontend/src/components/[A-Z]*/ 2>/dev/null | grep -v '__' | wc -l | grep -q '^0$' && echo "PASS: No PascalCase dirs remain" || echo "FAIL: PascalCase dirs still exist"</automated>
  </verify>
  <acceptance_criteria>
    - `ls -d frontend/src/components/[A-Z]*/ 2>/dev/null | grep -v '__'` returns empty (zero PascalCase directories, excluding __tests__)
    - `ls -d frontend/src/components/dossier/` exists and contains 58 files
    - `ls -d frontend/src/components/forms/` exists and contains 22 files
    - `ls -d frontend/src/components/calendar/` exists
    - `grep -r "from '@/components/Dossier/" frontend/src/ | wc -l` returns 0
    - `grep -r "from '@/components/Forms/" frontend/src/ | wc -l` returns 0
    - `grep -r "from '@/components/Calendar/" frontend/src/ | wc -l` returns 0
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>All 12 component directories renamed to kebab-case, all ~45 import references updated, routeTree.gen.ts regenerated if needed, pnpm build passes</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `ls frontend/src/hooks/use-*.ts frontend/src/hooks/use-*.tsx 2>/dev/null` returns nothing
2. `ls -d frontend/src/components/[A-Z]*/ 2>/dev/null | grep -v '__'` returns nothing
3. `grep -r "from '@/hooks/use-" frontend/src/ | wc -l` returns 0
4. `grep -r "from '@/components/[A-Z]" frontend/src/ | grep -v "__tests__" | grep -v "components/ui/" | wc -l` returns 0
5. `pnpm build` exits 0
</verification>

<success_criteria>

- Zero kebab-case hook files remain in frontend/src/hooks/
- Zero PascalCase component directories remain in frontend/src/components/ (excluding **tests**)
- All import paths updated to use new names
- pnpm build passes with zero errors
- git blame preserved for all renamed files (used git mv)
  </success_criteria>

<output>
After completion, create `.planning/phases/02-naming-file-structure/02-01-SUMMARY.md`
</output>
