---
phase: 02-naming-file-structure
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - backend/src/services/AuthService.ts
  - backend/src/services/BriefService.ts
  - backend/src/services/CommitmentService.ts
  - backend/src/services/ContactService.ts
  - backend/src/services/CountryService.ts
  - backend/src/services/DocumentService.ts
  - backend/src/services/EventService.ts
  - backend/src/services/IntelligenceService.ts
  - backend/src/services/MoUService.ts
  - backend/src/services/OrganizationService.ts
  - backend/src/services/PermissionDelegationService.ts
  - backend/src/services/PositionConsistencyService.ts
  - backend/src/services/RelationshipHealthService.ts
  - backend/src/services/SignatureOrchestrator.ts
  - backend/src/services/SignatureService.ts
  - backend/src/services/VoiceService.ts
  - backend/src/models/Country.ts
  - backend/src/models/PermissionDelegation.ts
  - backend/src/models/PositionConsistency.ts
  - backend/src/models/SignatureRequest.ts
  - backend/src/models/User.ts
  - backend/src/services/__tests__/AuthService.test.ts
  - eslint.config.mjs
autonomous: true
requirements: [ARCH-01]

must_haves:
  truths:
    - 'All 16 PascalCase backend service files are renamed to kebab-case with .service.ts suffix'
    - 'All 5 PascalCase backend model files are renamed to kebab-case with .model.ts suffix'
    - 'Backend test files follow the renamed subject naming'
    - 'ESLint filename rules enforce conventions and pass on all directories'
    - 'pnpm build succeeds and pnpm lint passes with zero errors'
  artifacts:
    - path: 'backend/src/services/auth.service.ts'
      provides: 'Renamed backend service (was AuthService.ts)'
    - path: 'backend/src/services/country.service.ts'
      provides: 'Renamed backend service (was CountryService.ts)'
    - path: 'backend/src/models/user.model.ts'
      provides: 'Renamed backend model (was User.ts)'
    - path: 'backend/src/models/country.model.ts'
      provides: 'Renamed backend model (was Country.ts)'
    - path: 'eslint.config.mjs'
      provides: 'ESLint filename enforcement rules via eslint-plugin-check-file'
  key_links:
    - from: 'backend/src/api/*.ts (controllers)'
      to: 'backend/src/services/*.service.ts'
      via: 'updated import paths'
      pattern: "from '../services/auth.service'"
    - from: 'backend/src/services/*.service.ts'
      to: 'backend/src/models/*.model.ts'
      via: 'updated import paths'
      pattern: "from '../models/user.model'"
    - from: 'eslint.config.mjs'
      to: 'eslint-plugin-check-file'
      via: 'plugin import and rule configuration'
      pattern: "import checkFile from 'eslint-plugin-check-file'"
---

<objective>
Rename all PascalCase backend service and model files to kebab-case, then add ESLint filename enforcement rules to prevent future naming violations across the entire monorepo.

Purpose: Completes the backend naming consistency (ARCH-01) and adds automated enforcement (D-11, D-12) so conventions cannot regress. Runs in Wave 2 parallel with Plan 02 since backend files are independent of frontend standalone component moves.

Output: 21 renamed backend files with updated imports, plus ESLint rules enforcing naming conventions across all layers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-naming-file-structure/02-CONTEXT.md
@.planning/phases/02-naming-file-structure/02-RESEARCH.md
@.planning/phases/02-naming-file-structure/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename 16 PascalCase backend service files and 5 model files to kebab-case, update all imports</name>
  <files>
    backend/src/services/AuthService.ts -> backend/src/services/auth.service.ts
    backend/src/services/BriefService.ts -> backend/src/services/brief.service.ts
    backend/src/services/CommitmentService.ts -> backend/src/services/commitment.service.ts
    backend/src/services/ContactService.ts -> backend/src/services/contact.service.ts
    backend/src/services/CountryService.ts -> backend/src/services/country.service.ts
    backend/src/services/DocumentService.ts -> backend/src/services/document.service.ts
    backend/src/services/EventService.ts -> backend/src/services/event.service.ts
    backend/src/services/IntelligenceService.ts -> backend/src/services/intelligence.service.ts
    backend/src/services/MoUService.ts -> backend/src/services/mou.service.ts
    backend/src/services/OrganizationService.ts -> backend/src/services/organization.service.ts
    backend/src/services/PermissionDelegationService.ts -> backend/src/services/permission-delegation.service.ts
    backend/src/services/PositionConsistencyService.ts -> backend/src/services/position-consistency.service.ts
    backend/src/services/RelationshipHealthService.ts -> backend/src/services/relationship-health.service.ts
    backend/src/services/SignatureOrchestrator.ts -> backend/src/services/signature-orchestrator.ts
    backend/src/services/SignatureService.ts -> backend/src/services/signature.service.ts
    backend/src/services/VoiceService.ts -> backend/src/services/voice.service.ts
    backend/src/services/__tests__/AuthService.test.ts -> backend/src/services/__tests__/auth.service.test.ts
    backend/src/models/Country.ts -> backend/src/models/country.model.ts
    backend/src/models/PermissionDelegation.ts -> backend/src/models/permission-delegation.model.ts
    backend/src/models/PositionConsistency.ts -> backend/src/models/position-consistency.model.ts
    backend/src/models/SignatureRequest.ts -> backend/src/models/signature-request.model.ts
    backend/src/models/User.ts -> backend/src/models/user.model.ts
    + all files importing from these (~35 import references)
  </files>
  <read_first>
    backend/src/services/AuthService.ts
    backend/src/services/CountryService.ts
    backend/src/models/User.ts
    backend/src/models/Country.ts
  </read_first>
  <action>
    Per D-09, D-10: Rename each PascalCase backend file to kebab-case using `git mv`. Do NOT rename the class/function names inside the files -- only file names and import paths.

    Note on macOS: Since these renames change more than just case (adding `.service.ts` suffix, splitting PascalCase to kebab-case), they do NOT need the two-step temp rename.

    **Services (16 renames):**
    For each service file:
    1. `git mv backend/src/services/AuthService.ts backend/src/services/auth.service.ts`
    2. Find imports: `grep -rl "from.*AuthService" backend/src/` and `grep -rl "from.*AuthService" tests/`
    3. Update import paths: `../services/AuthService` -> `../services/auth.service` (or `./AuthService` -> `./auth.service` for relative)
    4. Note: `SignatureOrchestrator.ts` -> `signature-orchestrator.ts` (no `.service.ts` suffix -- it is an orchestrator, not a service)

    **Test files:**
    - `backend/src/services/__tests__/AuthService.test.ts` -> `backend/src/services/__tests__/auth.service.test.ts`
    - Check for any other test files referencing renamed services

    **Models (5 renames):**
    For each model file:
    1. `git mv backend/src/models/Country.ts backend/src/models/country.model.ts`
    2. Find imports: `grep -rl "from.*models/Country" backend/src/`
    3. Update import paths: `../models/Country` -> `../models/country.model`

    After all renames and import updates, run `pnpm build` to verify zero broken imports.

    IMPORTANT per D-10: Do NOT rename class names (e.g., `class AuthService` stays as `class AuthService`), do NOT rename function names, do NOT rename exports. Only file paths and import paths change.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && ls backend/src/services/[A-Z]*.ts 2>/dev/null | wc -l | grep -q '^0$' && ls backend/src/models/[A-Z]*.ts 2>/dev/null | wc -l | grep -q '^0$' && echo "PASS: No PascalCase backend files" || echo "FAIL: PascalCase files remain"</automated>
  </verify>
  <acceptance_criteria>
    - `ls backend/src/services/[A-Z]*.ts 2>/dev/null` returns empty (zero PascalCase service files)
    - `ls backend/src/models/[A-Z]*.ts 2>/dev/null` returns empty (zero PascalCase model files)
    - `ls backend/src/services/auth.service.ts` exists
    - `ls backend/src/services/country.service.ts` exists
    - `ls backend/src/models/user.model.ts` exists
    - `ls backend/src/models/country.model.ts` exists
    - `ls backend/src/services/__tests__/auth.service.test.ts` exists
    - `grep -r "from.*services/[A-Z]" backend/src/ | wc -l` returns 0 (no old PascalCase import paths)
    - `grep -r "from.*models/[A-Z]" backend/src/ | wc -l` returns 0
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>All 16 service files and 5 model files renamed to kebab-case with proper suffixes, all ~35 import references updated, pnpm build passes</done>
</task>

<task type="auto">
  <name>Task 2: Install eslint-plugin-check-file and add filename enforcement rules to ESLint config</name>
  <files>
    eslint.config.mjs
    package.json
    pnpm-lock.yaml
  </files>
  <read_first>
    eslint.config.mjs
    package.json
  </read_first>
  <action>
    Per D-11, D-12: Install `eslint-plugin-check-file` and configure filename/folder naming rules in the existing ESLint 9 flat config.

    Step 1: Install the plugin
    ```
    pnpm add -D eslint-plugin-check-file
    ```

    Step 2: Add to `eslint.config.mjs`. Add the import at the top:
    ```javascript
    import checkFile from 'eslint-plugin-check-file'
    ```

    Then add these config blocks (AFTER the existing configs, BEFORE the ignores block):

    ```javascript
    // === Filename & Folder Naming Convention Enforcement (Phase 2: ARCH-01) ===

    // Frontend component directories: kebab-case dirs, PascalCase files
    {
      files: ['frontend/src/components/**/*.{ts,tsx}'],
      ignores: ['frontend/src/components/ui/**', 'frontend/src/components/__tests__/**'],
      plugins: { 'check-file': checkFile },
      rules: {
        'check-file/folder-naming-convention': ['error', {
          'frontend/src/components/**/': 'KEBAB_CASE',
        }],
        'check-file/filename-naming-convention': ['error', {
          'frontend/src/components/**/*.{tsx,ts}': 'PASCAL_CASE',
        }, { ignoreMiddleExtensions: true }],
      },
    },

    // Frontend UI primitives: kebab-case files (shadcn convention)
    {
      files: ['frontend/src/components/ui/**/*.{ts,tsx}'],
      plugins: { 'check-file': checkFile },
      rules: {
        'check-file/filename-naming-convention': ['error', {
          'frontend/src/components/ui/**/*.{ts,tsx}': 'KEBAB_CASE',
        }, { ignoreMiddleExtensions: true }],
      },
    },

    // Frontend hooks: camelCase files
    {
      files: ['frontend/src/hooks/**/*.{ts,tsx}'],
      plugins: { 'check-file': checkFile },
      rules: {
        'check-file/filename-naming-convention': ['error', {
          'frontend/src/hooks/**/*.{ts,tsx}': 'CAMEL_CASE',
        }, { ignoreMiddleExtensions: true }],
      },
    },

    // Frontend types: kebab-case files
    {
      files: ['frontend/src/types/**/*.ts'],
      plugins: { 'check-file': checkFile },
      rules: {
        'check-file/filename-naming-convention': ['error', {
          'frontend/src/types/**/*.ts': 'KEBAB_CASE',
        }, { ignoreMiddleExtensions: true }],
      },
    },

    // Frontend lib: kebab-case files
    {
      files: ['frontend/src/lib/**/*.ts'],
      plugins: { 'check-file': checkFile },
      rules: {
        'check-file/filename-naming-convention': ['error', {
          'frontend/src/lib/**/*.ts': 'KEBAB_CASE',
        }, { ignoreMiddleExtensions: true }],
      },
    },

    // Backend services: kebab-case files and folders
    {
      files: ['backend/src/services/**/*.ts'],
      ignores: ['backend/src/services/__tests__/**'],
      plugins: { 'check-file': checkFile },
      rules: {
        'check-file/filename-naming-convention': ['error', {
          'backend/src/services/**/*.ts': 'KEBAB_CASE',
        }, { ignoreMiddleExtensions: true }],
        'check-file/folder-naming-convention': ['error', {
          'backend/src/services/**/': 'KEBAB_CASE',
        }],
      },
    },

    // Backend models: kebab-case files
    {
      files: ['backend/src/models/**/*.ts'],
      plugins: { 'check-file': checkFile },
      rules: {
        'check-file/filename-naming-convention': ['error', {
          'backend/src/models/**/*.ts': 'KEBAB_CASE',
        }, { ignoreMiddleExtensions: true }],
      },
    },

    // Backend API routes: kebab-case files
    {
      files: ['backend/src/api/**/*.ts'],
      plugins: { 'check-file': checkFile },
      rules: {
        'check-file/filename-naming-convention': ['error', {
          'backend/src/api/**/*.ts': 'KEBAB_CASE',
        }, { ignoreMiddleExtensions: true }],
      },
    },

    // Backend middleware: kebab-case files
    {
      files: ['backend/src/middleware/**/*.ts'],
      plugins: { 'check-file': checkFile },
      rules: {
        'check-file/filename-naming-convention': ['error', {
          'backend/src/middleware/**/*.ts': 'KEBAB_CASE',
        }, { ignoreMiddleExtensions: true }],
      },
    },
    ```

    Key configuration notes:
    - `ignoreMiddleExtensions: true` handles `.service.ts`, `.model.ts`, `.test.ts`, `.types.ts`, `.d.ts` patterns (treats `auth.service.ts` as `auth` for KEBAB_CASE check)
    - `__tests__/` directories are excluded from folder naming rules (double-underscore is standard)
    - `frontend/src/components/ui/` has its own rule (kebab-case, per D-07)
    - Route files in `frontend/src/routes/` are NOT included (TanStack Router has its own conventions)

    Step 3: Run ESLint to verify all rules pass:
    ```
    pnpm lint
    ```

    If any violations appear, they indicate files that were missed during the rename phase. Fix them before proceeding.

    Step 4: Verify pre-commit hooks still work by making a small change and committing:
    ```
    # The existing lint-staged config in package.json will pick up the new rules automatically
    # since they are in the same eslint.config.mjs that lint-staged already runs
    ```

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && pnpm lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "eslint-plugin-check-file" package.json` returns a match (plugin installed)
    - `grep "check-file" eslint.config.mjs` returns matches (plugin configured)
    - `grep "folder-naming-convention" eslint.config.mjs` returns matches (folder rules added)
    - `grep "filename-naming-convention" eslint.config.mjs` returns matches (filename rules added)
    - `grep "ignoreMiddleExtensions" eslint.config.mjs` returns matches (extension handling configured)
    - `pnpm lint` exits 0 with zero errors
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>eslint-plugin-check-file installed, filename and folder naming rules configured for all layers (components, hooks, types, lib, ui, services, models, api, middleware), pnpm lint passes with zero violations, pre-commit hooks enforce new rules</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `ls backend/src/services/[A-Z]*.ts 2>/dev/null` returns nothing
2. `ls backend/src/models/[A-Z]*.ts 2>/dev/null` returns nothing
3. `grep "eslint-plugin-check-file" package.json` shows installed
4. `pnpm lint` exits 0
5. `pnpm build` exits 0
6. `pnpm knip` exits 0

Full phase verification (all 3 plans complete): 7. Zero kebab-case hooks in frontend/src/hooks/ 8. Zero PascalCase dirs in frontend/src/components/ (excluding **tests**) 9. Zero standalone .tsx at frontend/src/components/ root 10. Zero PascalCase service/model files in backend/src/ 11. ESLint enforces all conventions automatically
</verification>

<success_criteria>

- All backend service files use kebab-case with .service.ts suffix
- All backend model files use kebab-case with .model.ts suffix
- ESLint filename rules installed and configured for all layers
- pnpm lint passes with zero errors (all naming conventions enforced)
- pnpm build passes
- Pre-commit hooks automatically enforce naming on every future commit
  </success_criteria>

<output>
After completion, create `.planning/phases/02-naming-file-structure/02-03-SUMMARY.md`
</output>
