---
phase: 05-responsive-design
plan: 05
type: execute
wave: 3
depends_on: [05-01, 05-03, 05-04]
files_modified:
  - frontend/src/routes/_protected/ (forms, settings, profile, remaining routes)
  - frontend/src/components/ui/heroui-modal.tsx
  - frontend/src/components/ui/heroui-forms.tsx
autonomous: true
requirements: [RESP-01, RESP-02, RESP-05]

must_haves:
  truths:
    - 'All forms render correctly on mobile with stacked fields and sticky save button'
    - 'All modals use AdaptiveDialog (bottom sheet on mobile, centered dialog on desktop)'
    - 'Settings and profile pages render correctly at all 5 breakpoints'
    - 'ALL remaining route pages have responsive layouts and touch targets'
    - 'Every interactive element across the entire app meets 44x44px touch target'
  artifacts:
    - path: 'frontend/src/routes/_protected/'
      provides: 'All remaining responsive routes'
      contains: 'min-h-11'
    - path: 'frontend/src/components/ui/heroui-modal.tsx'
      provides: 'Modal with AdaptiveDialog integration guidance'
  key_links:
    - from: 'form pages'
      to: 'frontend/src/components/ui/adaptive-dialog.tsx'
      via: 'AdaptiveDialog for modal forms'
      pattern: 'AdaptiveDialog'
---

<objective>
Complete the responsive pass: forms with sticky save buttons, modals as bottom sheets on mobile, settings/profile, and ALL remaining route pages.

Purpose: Per D-05, fix ALL pages. This plan covers D-06 priorities 3-5 (forms, settings, remaining) and ensures RESP-05 (forms/modals on mobile). After this plan, every page in the app is responsive.
Output: 100% of routes responsive, forms mobile-friendly, modals adaptive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-responsive-design/05-CONTEXT.md
@.planning/phases/05-responsive-design/05-RESEARCH.md
@.planning/phases/05-responsive-design/05-UI-SPEC.md
@.planning/phases/05-responsive-design/05-01-SUMMARY.md
@.planning/phases/05-responsive-design/05-03-SUMMARY.md
@.planning/phases/05-responsive-design/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Forms and modals mobile pass (D-07, D-08, RESP-05)</name>
  <files>
    frontend/src/routes/_protected/ (all form/create/edit routes),
    frontend/src/components/ (form components, modal usage sites)
  </files>
  <read_first>
    frontend/src/components/ui/adaptive-dialog.tsx (from Plan 01),
    frontend/src/components/ui/heroui-modal.tsx,
    frontend/src/components/ui/heroui-forms.tsx,
    frontend/src/components/ui/bottom-sheet.tsx,
    frontend/src/hooks/useResponsive.ts
  </read_first>
  <action>
Per D-07, D-08, and RESP-05: Forms and modals must be fully usable on mobile.

1. Find ALL modal usage sites across the codebase:

   ```bash
   grep -rn "heroui-modal\|HeroUIModal\|Modal\b" src/ --include="*.tsx" -l
   ```

2. For each modal usage site, evaluate:
   - If the modal contains a form or significant content (> 3 lines): Replace with `AdaptiveDialog` from Plan 01 (which renders BottomSheet on mobile, Modal on desktop)
   - If the modal is a simple confirm/alert (1-2 lines): Keep as regular modal (small modals are fine on mobile)
   - Import: `import { AdaptiveDialog } from '@/components/ui/adaptive-dialog'`
   - Replace `<HeroUIModal>` with `<AdaptiveDialog snapPreset="medium">` (or "large" for long forms)

3. For ALL form pages (create/edit dossiers, create work items, settings forms, etc.):
   Per D-08: Single scrollable layout with all fields stacked vertically and a sticky save button.
   a. Form container: `w-full max-w-2xl mx-auto px-4 sm:px-6`
   b. Form fields: `flex flex-col gap-4` (stacked vertically at all breakpoints)
   c. Each field label: `text-sm font-medium text-start mb-1`
   d. Each input: `w-full min-h-11` (full-width, 44px touch target)
   e. Side-by-side fields on desktop only: `grid grid-cols-1 sm:grid-cols-2 gap-4`
   f. Sticky save button on mobile:

   ```tsx
   <div className="sticky bottom-0 bg-background/95 backdrop-blur-sm border-t border-border p-4 -mx-4 sm:static sm:bg-transparent sm:border-0 sm:p-0 sm:mx-0 sm:mt-6">
     <Button type="submit" className="w-full sm:w-auto min-h-11">
       {t('save')}
     </Button>
   </div>
   ```

   g. No stepped wizards or accordions per D-08.

4. Checkboxes and radio buttons:
   - Expand hit area: wrap in a label with `min-h-11 flex items-center gap-3 cursor-pointer`
   - Gap between checkboxes: `gap-2` minimum

5. Date pickers, selects, dropdowns:
   - Ensure they open correctly on mobile (not clipped by viewport)
   - Touch targets: `min-h-11`

6. All CSS logical properties. useDirection() for RTL.
   </action>
   <verify>
   <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0/frontend && grep -rn "AdaptiveDialog" src/ --include="_.tsx" -l | head -10; grep -rn "sticky bottom-0" src/ --include="_.tsx" | head -5; pnpm build 2>&1 | tail -3</automated>
   </verify>
   <acceptance_criteria> - Multiple files import and use `AdaptiveDialog` (modal-to-bottom-sheet conversion) - Form pages contain `sticky bottom-0` for sticky save button on mobile - Form inputs contain `min-h-11` touch targets - Form fields use `flex flex-col gap-4` stacked layout - No `ml-*`, `mr-*`, `pl-*`, `pr-*`, `text-left`, `text-right` in modified files - `pnpm build` succeeds
   </acceptance_criteria>
   <done>All modals use AdaptiveDialog (bottom sheet on mobile), all forms have stacked layouts with sticky save buttons on mobile</done>
   </task>

<task type="auto">
  <name>Task 2: Settings, profile, and ALL remaining routes responsive pass</name>
  <files>
    frontend/src/routes/_protected/ (settings, profile, admin, and ALL remaining routes not covered by Plans 03-04)
  </files>
  <read_first>
    frontend/src/routes/_protected/ (list ALL subdirectories to identify uncovered routes),
    frontend/src/hooks/useResponsive.ts,
    frontend/src/hooks/useDirection.ts
  </read_first>
  <action>
Per D-05: Fix ALL pages. Per D-06 priorities 4-5: Settings, profile, and all remaining.

1. Enumerate ALL route files not yet covered by Plans 03 (dashboard, kanban) and 04 (dossiers):

   ```bash
   find src/routes/_protected -name "*.tsx" -not -path "*/dashboard/*" -not -path "*/kanban/*" -not -path "*/dossiers/*"
   ```

   This typically includes: settings, profile, admin, reports, notifications, work-items, intake, after-action, briefings, calendar, network-graph, search, help, onboarding, modern-nav-demo, etc.

2. For EACH remaining route file, apply responsive patterns:
   a. Page container: `container mx-auto px-4 sm:px-6 lg:px-8`
   b. Page title: `text-lg sm:text-xl lg:text-2xl font-semibold text-start`
   c. Content grids: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`
   d. All buttons: `min-h-11 min-w-11`
   e. All form inputs: `min-h-11 w-full`
   f. Toolbars/action bars: `flex flex-col sm:flex-row gap-2 sm:gap-4`

3. Settings pages specifically:
   - Settings navigation (sidebar or tabs): horizontal scroll on mobile with `overflow-x-auto`
   - Settings sections: stack vertically on mobile, `grid-cols-1 lg:grid-cols-2` on desktop
   - Toggle switches: `min-h-11` touch target with label

4. Profile page:
   - Avatar + info: stack vertically on mobile (`flex flex-col sm:flex-row gap-4`)
   - Edit profile form: stacked fields with sticky save (same pattern as Task 1)

5. Calendar pages:
   - Mobile: switch to list/agenda view (hide month grid if too cramped)
   - Use `useResponsive()` to detect mobile and conditionally render list view

6. Network graph pages:
   - Already wrapped in LtrIsolate from Phase 4
   - Ensure container is `w-full h-[50vh] sm:h-[60vh] lg:h-[70vh]` (responsive height)
   - Controls/toolbar: `min-h-11` touch targets

7. Remove demo route if appropriate:
   - `/modern-nav-demo` route can be removed since NavigationShell is now production (per Plan 01)
   - Or keep it — Claude's discretion per D-02

8. All CSS logical properties. useDirection() for RTL.

9. Final sweep: Run a comprehensive grep to verify NO interactive element across the entire `src/routes/` directory is missing `min-h-11`:
   ```bash
   grep -rn "<button\|<Button\|<a " src/routes/ --include="*.tsx" | grep -v "min-h-11\|min-h-\[44\|min-h-12" | head -20
   ```
   Fix any remaining gaps.
   </action>
   <verify>
   <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0/frontend && echo "=== Remaining routes with responsive classes ===" && grep -rn "px-4 sm:px-6\|grid-cols-1\|min-h-11" src/routes/\_protected/ --include="_.tsx" -l | wc -l && echo "=== Build check ===" && pnpm build 2>&1 | tail -3</automated>
   </verify>
   <acceptance_criteria> - ALL route files under `src/routes/_protected/` contain responsive container padding or are wrapped by a responsive parent - Settings pages contain `overflow-x-auto` for tab/nav scrolling on mobile - Profile page uses stacked layout on mobile - All buttons across all routes contain `min-h-11` or equivalent 44px touch target - No `ml-_`, `mr-_`, `pl-_`, `pr-\*`, `text-left`, `text-right`in any modified file
-`pnpm build` succeeds with exit code 0
   </acceptance_criteria>
   <done>100% of route pages are responsive at all 5 breakpoints with proper touch targets, completing the comprehensive responsive pass per D-05</done>
   </task>

</tasks>

<verification>
- `cd frontend && pnpm build` passes
- All modals converted to AdaptiveDialog where appropriate
- All forms have stacked layouts and sticky save buttons on mobile
- All remaining routes have responsive containers and touch targets
- Zero physical CSS properties in any modified route file
</verification>

<success_criteria>

- Every modal with forms uses AdaptiveDialog (bottom sheet on mobile per D-07)
- Every form has stacked fields and sticky save button on mobile per D-08
- Settings, profile, and ALL remaining pages are responsive per D-05
- RESP-05 fully satisfied: forms and modals usable on mobile
- RESP-01 and RESP-02 fully satisfied across ALL pages
- Build passes with zero errors
  </success_criteria>

<output>
After completion, create `.planning/phases/05-responsive-design/05-05-SUMMARY.md`
</output>
