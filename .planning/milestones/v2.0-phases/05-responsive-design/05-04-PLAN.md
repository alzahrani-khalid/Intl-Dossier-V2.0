---
phase: 05-responsive-design
plan: 04
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - frontend/src/routes/_protected/dossiers/
  - frontend/src/components/Dossier/
  - frontend/src/components/table/
autonomous: true
requirements: [RESP-01, RESP-02]

must_haves:
  truths:
    - 'Dossier list pages render correctly at all 5 breakpoints'
    - 'Dossier detail pages render correctly at all 5 breakpoints'
    - 'All interactive elements on dossier pages meet 44x44px touch targets'
    - 'Dossier list tables use card-view on mobile'
    - 'Dossier detail tab/section layouts adapt to mobile'
  artifacts:
    - path: 'frontend/src/routes/_protected/dossiers/'
      provides: 'Responsive dossier routes'
      contains: 'grid-cols-1'
    - path: 'frontend/src/components/Dossier/'
      provides: 'Responsive dossier components'
      contains: 'min-h-11'
  key_links:
    - from: 'dossier list pages'
      to: 'frontend/src/components/table/AdvancedDataTable.tsx'
      via: 'enableViewToggle prop'
      pattern: 'enableViewToggle'
---

<objective>
Make all dossier list and detail pages fully responsive at all 5 breakpoints with proper touch targets.

Purpose: Dossier list + detail pages are the core workflow (D-06 priority 2). These cover all 8 dossier types (country, organization, forum, engagement, topic, working_group, person, elected_official).
Output: All dossier routes fully usable on mobile, tablet, and desktop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-responsive-design/05-CONTEXT.md
@.planning/phases/05-responsive-design/05-RESEARCH.md
@.planning/phases/05-responsive-design/05-UI-SPEC.md
@.planning/phases/05-responsive-design/05-01-SUMMARY.md
@.planning/phases/05-responsive-design/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dossier list pages responsive pass (all 8 dossier types)</name>
  <files>
    frontend/src/routes/_protected/dossiers/ (list routes for all types),
    frontend/src/components/Dossier/ (list components like UniversalDossierCard)
  </files>
  <read_first>
    frontend/src/routes/_protected/dossiers/ (list directory to find all route files),
    frontend/src/components/Dossier/UniversalDossierCard.tsx,
    frontend/src/components/table/AdvancedDataTable.tsx (from Plan 02, card-view API),
    frontend/src/hooks/useResponsive.ts
  </read_first>
  <action>
Per D-06 priority 2 and RESP-01/RESP-02: All dossier list pages (countries, organizations, forums, engagements, topics, working groups, persons, elected officials).

1. Find all dossier list route files. Common patterns: `index.tsx` or `route.tsx` in each dossier type directory under `_protected/dossiers/`.

2. For each list page:
   a. Page container: `container mx-auto px-4 sm:px-6 lg:px-8`
   b. Page title: `text-lg sm:text-xl lg:text-2xl font-semibold text-start`
   c. If the page uses AdvancedDataTable or SelectableDataTable, add `enableViewToggle={true}` and set appropriate `mobileCardColumns` and `cardTitleColumn` for each dossier type:
   - Countries: `mobileCardColumns={['name', 'region', 'status']}`, `cardTitleColumn="name"`
   - Organizations: `mobileCardColumns={['name', 'type', 'status']}`, `cardTitleColumn="name"`
   - (Similar for all 8 types — use the most informative 3-4 columns)
     d. Filter/search toolbar: Stack vertically on mobile (`flex flex-col sm:flex-row gap-2 sm:gap-4`)
     e. "Create new" / action buttons: `min-h-11 min-w-11` touch targets, full-width on mobile (`w-full sm:w-auto`)

3. UniversalDossierCard responsive improvements:
   - Card: `p-3 sm:p-4` responsive padding
   - Card title: `text-sm sm:text-base font-semibold text-start`
   - Card metadata (type badge, date, etc.): `text-xs` on all sizes, wrap on mobile
   - Action buttons: `min-h-11 min-w-11`
   - Card grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3 sm:gap-4`

4. All CSS logical properties. useDirection() for RTL. No physical properties.
   </action>
   <verify>
   <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0/frontend && grep -rn "enableViewToggle\|grid-cols-1\|min-h-11" src/routes/\_protected/dossiers/ 2>/dev/null | head -10; pnpm build 2>&1 | tail -3</automated>
   </verify>
   <acceptance_criteria> - Dossier list pages pass `enableViewToggle={true}` to their table components - Dossier list pages contain responsive container padding `px-4 sm:px-6 lg:px-8` - Filter toolbars use `flex flex-col sm:flex-row` for mobile stacking - Action buttons contain `min-h-11` - No physical CSS properties in modified files - `pnpm build` succeeds
   </acceptance_criteria>
   <done>All 8 dossier type list pages are responsive with card-view tables on mobile, stacked filters, and proper touch targets</done>
   </task>

<task type="auto">
  <name>Task 2: Dossier detail pages responsive pass (all 8 dossier types)</name>
  <files>
    frontend/src/routes/_protected/dossiers/ (detail routes like $id.tsx),
    frontend/src/components/Dossier/ (detail components)
  </files>
  <read_first>
    frontend/src/routes/_protected/dossiers/ (find detail route files, typically $id.tsx or $countryId.tsx etc.),
    frontend/src/components/Dossier/ (detail view components),
    frontend/src/hooks/useResponsive.ts
  </read_first>
  <action>
Per D-06 priority 2 and RESP-01/RESP-02: All dossier detail pages.

1. Find all dossier detail route files (pattern: `$id.tsx`, `$countryId.tsx`, etc.).

2. For each detail page:
   a. Page container: `container mx-auto px-4 sm:px-6 lg:px-8`
   b. Page title / dossier name: `text-lg sm:text-xl lg:text-2xl font-semibold text-start`
   c. Header with back button + title + actions:
   - Mobile: Stack vertically, back button full-width or top-start
   - Desktop: Horizontal flex with `flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3`
     d. Tab navigation (if tabs are used for sections):
   - Mobile: Horizontal scroll with `overflow-x-auto` and `-webkit-overflow-scrolling: touch`
   - Each tab: `min-h-11 px-3 sm:px-4 whitespace-nowrap`
   - Hide less-used tabs on mobile behind a "More" menu if >5 tabs
     e. Detail sections / info grids:
   - `grid grid-cols-1 sm:grid-cols-2 gap-4`
   - Labels above values on mobile (not side-by-side)
     f. Related items lists (relationships, documents, etc.):
   - Use card layout on mobile, table on desktop
   - Each card: `min-h-12 p-3`

3. Sidebar panels (if detail pages have a sidebar for metadata):
   - Mobile: Collapse sidebar below main content (`flex flex-col lg:flex-row`)
   - Desktop: Side-by-side (`lg:flex-row lg:gap-6`)

4. Action buttons (edit, delete, share): `min-h-11 min-w-11`
5. All CSS logical properties. useDirection() for RTL.
   </action>
   <verify>
   <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0/frontend && find src/routes/\_protected/dossiers -name '_$_' -o -name '_Id_' 2>/dev/null | head -10; grep -rn "flex-col.\*sm:flex-row\|grid-cols-1\|min-h-11" src/routes/\_protected/dossiers/ 2>/dev/null | head -10; pnpm build 2>&1 | tail -3</automated>
   </verify>
   <acceptance_criteria> - Detail pages use responsive container `px-4 sm:px-6 lg:px-8` - Detail pages use `flex flex-col sm:flex-row` or `flex flex-col lg:flex-row` for header/sidebar layouts - Tab navigation has `overflow-x-auto` for mobile horizontal scroll - Action buttons have `min-h-11` touch targets - Info grids use `grid-cols-1 sm:grid-cols-2` for mobile stacking - No physical CSS properties in modified files - `pnpm build` succeeds
   </acceptance_criteria>
   <done>All 8 dossier type detail pages are responsive with stacked layouts on mobile, scrollable tabs, and proper touch targets</done>
   </task>

</tasks>

<verification>
- `cd frontend && pnpm build` passes
- All dossier list pages pass `enableViewToggle` to tables
- All detail pages have responsive layouts and touch targets
- No physical CSS properties in any modified files
</verification>

<success_criteria>

- All 8 dossier list pages show card-view tables on mobile, standard tables on desktop
- All 8 dossier detail pages stack sections vertically on mobile, use side-by-side layouts on desktop
- Filter toolbars, tab navigations, and action buttons are touch-friendly
- Build passes with zero errors
  </success_criteria>

<output>
After completion, create `.planning/phases/05-responsive-design/05-04-SUMMARY.md`
</output>
