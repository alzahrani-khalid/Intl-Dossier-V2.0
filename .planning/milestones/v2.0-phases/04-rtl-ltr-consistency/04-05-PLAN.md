---
phase: 04-rtl-ltr-consistency
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/chart.tsx
  - frontend/src/routes/_protected/tags.tsx
  - frontend/src/routes/_protected/admin/data-retention.tsx
  - frontend/src/routes/_protected/admin/ai-usage.tsx
  - frontend/src/routes/_protected/stakeholder-influence.tsx
  - frontend/src/pages/analytics/AnalyticsDashboardPage.tsx
  - frontend/src/pages/audit-logs/AuditLogsPage.tsx
  - frontend/src/pages/activity/ActivityPage.tsx
  - frontend/src/components/audit-logs/AuditLogStatistics.tsx
  - frontend/src/components/report-builder/VisualizationSelector.tsx
  - frontend/src/components/intelligence/EconomicDashboard.tsx
  - frontend/src/components/analytics/AnalyticsPreviewOverlay.tsx
  - frontend/src/components/availability-polling/AvailabilityPollResults.tsx
  - frontend/src/components/tags/TagAnalytics.tsx
  - frontend/src/components/stakeholder-influence/InfluenceReport.tsx
  - frontend/src/components/dashboard-widgets/ChartWidget.tsx
  - frontend/src/components/dashboard-widgets/WidgetLibrary.tsx
  - frontend/src/components/dashboard-widgets/QuickActionsWidget.tsx
autonomous: true
gap_closure: true
requirements:
  - RTL-03
  - RTL-04

must_haves:
  truths:
    - 'Every file that renders a Recharts chart component has its outermost chart container wrapped in LtrIsolate'
    - 'XAxis components in wrapped charts use reversed={isRTL} for proper RTL axis direction'
  artifacts:
    - path: 'frontend/src/components/ui/chart.tsx'
      provides: 'Base chart wrapper with LtrIsolate'
      contains: 'LtrIsolate'
    - path: 'frontend/src/routes/_protected/tags.tsx'
      provides: 'Tags page with RTL-safe charts'
      contains: 'LtrIsolate'
    - path: 'frontend/src/components/intelligence/EconomicDashboard.tsx'
      provides: 'Economic dashboard with RTL-safe charts'
      contains: 'LtrIsolate'
  key_links:
    - from: 'all 18 modified files'
      to: 'frontend/src/components/ui/ltr-isolate.tsx'
      via: 'import { LtrIsolate }'
      pattern: 'LtrIsolate'
---

<objective>
Wrap remaining 18 Recharts-rendering files in LtrIsolate to prevent RTL chart rendering bugs.

Purpose: Only 9 of ~27 chart-rendering files received LtrIsolate wrapping in 04-03. The remaining files will display mirrored axes and misaligned tooltips in Arabic mode.
Output: All Recharts chart containers wrapped in LtrIsolate with reversed XAxis when in RTL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rtl-ltr-consistency/04-VERIFICATION.md
@.planning/phases/04-rtl-ltr-consistency/04-03-SUMMARY.md
</context>

<interfaces>
<!-- Key imports the executor needs -->

From frontend/src/components/ui/ltr-isolate.tsx:

```typescript
export function LtrIsolate({
  children,
  className,
}: {
  children: ReactNode
  className?: string
}): ReactElement
```

From frontend/src/hooks/useDirection.ts:

```typescript
export function useDirection(): { direction: 'ltr' | 'rtl'; isRTL: boolean }
```

Wrapping pattern (established in 04-03):

```tsx
import { LtrIsolate } from '@/components/ui/ltr-isolate'
import { useDirection } from '@/hooks/useDirection'

const { isRTL } = useDirection()

<LtrIsolate>
  <ResponsiveContainer width="100%" height={300}>
    <BarChart data={data}>
      <XAxis dataKey="name" reversed={isRTL} />
      {/* ... rest of chart */}
    </BarChart>
  </ResponsiveContainer>
</LtrIsolate>
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wrap chart.tsx base wrapper and route-level chart files in LtrIsolate</name>
  <files>
    frontend/src/components/ui/chart.tsx
    frontend/src/routes/_protected/tags.tsx
    frontend/src/routes/_protected/admin/data-retention.tsx
    frontend/src/routes/_protected/admin/ai-usage.tsx
    frontend/src/routes/_protected/stakeholder-influence.tsx
    frontend/src/pages/analytics/AnalyticsDashboardPage.tsx
    frontend/src/pages/audit-logs/AuditLogsPage.tsx
    frontend/src/pages/activity/ActivityPage.tsx
  </files>
  <read_first>
    - frontend/src/components/ui/chart.tsx (base chart wrapper -- may already wrap ResponsiveContainer; if ChartContainer already uses LtrIsolate, skip this file)
    - frontend/src/components/ui/ltr-isolate.tsx (LtrIsolate component for import path)
    - frontend/src/hooks/useDirection.ts (useDirection hook for import path)
    - frontend/src/routes/_protected/tags.tsx
    - frontend/src/routes/_protected/admin/data-retention.tsx
    - frontend/src/routes/_protected/admin/ai-usage.tsx
    - frontend/src/routes/_protected/stakeholder-influence.tsx
    - frontend/src/pages/analytics/AnalyticsDashboardPage.tsx
    - frontend/src/pages/audit-logs/AuditLogsPage.tsx
    - frontend/src/pages/activity/ActivityPage.tsx
  </read_first>
  <action>
For each file, apply this mechanical transformation:

1. Add imports (if not already present):

   ```typescript
   import { LtrIsolate } from '@/components/ui/ltr-isolate'
   import { useDirection } from '@/hooks/useDirection'
   ```

2. Inside the component function, add (if not already present):

   ```typescript
   const { isRTL } = useDirection()
   ```

3. Find the outermost Recharts container (typically `<ResponsiveContainer>` or the chart root like `<BarChart>`, `<LineChart>`, `<PieChart>`, `<AreaChart>`, `<RadarChart>`) and wrap it:

   ```tsx
   <LtrIsolate>
     <ResponsiveContainer ...>
       {/* existing chart code */}
     </ResponsiveContainer>
   </LtrIsolate>
   ```

4. For any `<XAxis>` inside the chart, add `reversed={isRTL}`:
   ```tsx
   <XAxis dataKey="name" reversed={isRTL} />
   ```

SPECIAL CASE for chart.tsx: If this file exports a `ChartContainer` component that wraps `ResponsiveContainer`, add LtrIsolate INSIDE ChartContainer so ALL consumers automatically get LTR isolation. This is the highest-leverage fix -- it may cover some of the other files automatically. Check if chart.tsx already does this (04-03 SUMMARY mentions "ChartContainer auto-wraps ResponsiveContainer in LtrIsolate"). If yes, skip chart.tsx and check whether files using ChartContainer are already covered.

For files that only REFERENCE chart component names in strings/configs (not JSX rendering), skip them -- no wrapping needed.
</action>
<verify>
<automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && for f in frontend/src/components/ui/chart.tsx frontend/src/routes/\_protected/tags.tsx frontend/src/routes/\_protected/admin/data-retention.tsx frontend/src/routes/\_protected/admin/ai-usage.tsx frontend/src/routes/\_protected/stakeholder-influence.tsx frontend/src/pages/analytics/AnalyticsDashboardPage.tsx frontend/src/pages/audit-logs/AuditLogsPage.tsx frontend/src/pages/activity/ActivityPage.tsx; do echo "=== $f ==="; grep -c "LtrIsolate\|useDirection" "$f" 2>/dev/null || echo "SKIP (no chart rendering)"; done</automated>
</verify>
<acceptance_criteria> - Each file that renders a Recharts chart JSX element contains "LtrIsolate" (import + usage) - Each file that renders a Recharts chart JSX element contains "useDirection" (import + call) - Each XAxis element has reversed={isRTL} prop - Files that only reference chart names in strings/configs are skipped (no false wrapping) - No TypeScript compilation errors introduced
</acceptance_criteria>
<done>All 8 files (or subset that actually render charts) have LtrIsolate wrapping and reversed XAxis</done>
</task>

<task type="auto">
  <name>Task 2: Wrap remaining component-level chart files in LtrIsolate</name>
  <files>
    frontend/src/components/audit-logs/AuditLogStatistics.tsx
    frontend/src/components/report-builder/VisualizationSelector.tsx
    frontend/src/components/intelligence/EconomicDashboard.tsx
    frontend/src/components/analytics/AnalyticsPreviewOverlay.tsx
    frontend/src/components/availability-polling/AvailabilityPollResults.tsx
    frontend/src/components/tags/TagAnalytics.tsx
    frontend/src/components/stakeholder-influence/InfluenceReport.tsx
    frontend/src/components/dashboard-widgets/ChartWidget.tsx
    frontend/src/components/dashboard-widgets/WidgetLibrary.tsx
    frontend/src/components/dashboard-widgets/QuickActionsWidget.tsx
  </files>
  <read_first>
    - frontend/src/components/audit-logs/AuditLogStatistics.tsx
    - frontend/src/components/report-builder/VisualizationSelector.tsx
    - frontend/src/components/intelligence/EconomicDashboard.tsx
    - frontend/src/components/analytics/AnalyticsPreviewOverlay.tsx
    - frontend/src/components/availability-polling/AvailabilityPollResults.tsx
    - frontend/src/components/tags/TagAnalytics.tsx
    - frontend/src/components/stakeholder-influence/InfluenceReport.tsx
    - frontend/src/components/dashboard-widgets/ChartWidget.tsx
    - frontend/src/components/dashboard-widgets/WidgetLibrary.tsx
    - frontend/src/components/dashboard-widgets/QuickActionsWidget.tsx
  </read_first>
  <action>
Apply the same mechanical transformation as Task 1 to each file:

1. Add imports:

   ```typescript
   import { LtrIsolate } from '@/components/ui/ltr-isolate'
   import { useDirection } from '@/hooks/useDirection'
   ```

2. Add `const { isRTL } = useDirection()` inside the component function.

3. Wrap the outermost Recharts container in `<LtrIsolate>...</LtrIsolate>`.

4. Add `reversed={isRTL}` to any `<XAxis>` element.

IMPORTANT CHECKS before wrapping each file:

- If the file uses `ChartContainer` from `@/components/ui/chart` and Task 1 already added LtrIsolate inside ChartContainer, then the file is ALREADY covered. Skip it -- do NOT double-wrap.
- If the file only references chart component names in strings, arrays, or config objects (not rendering JSX), skip it.
- If the file renders charts inside a component that is NOT a React function component (e.g., a render prop or callback), add useDirection at the nearest function component scope and pass isRTL down.

For files with MULTIPLE charts (e.g., EconomicDashboard may have several), wrap EACH chart's ResponsiveContainer individually, or wrap a parent container that holds all charts.
</action>
<verify>
<automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && for f in frontend/src/components/audit-logs/AuditLogStatistics.tsx frontend/src/components/report-builder/VisualizationSelector.tsx frontend/src/components/intelligence/EconomicDashboard.tsx frontend/src/components/analytics/AnalyticsPreviewOverlay.tsx frontend/src/components/availability-polling/AvailabilityPollResults.tsx frontend/src/components/tags/TagAnalytics.tsx frontend/src/components/stakeholder-influence/InfluenceReport.tsx frontend/src/components/dashboard-widgets/ChartWidget.tsx frontend/src/components/dashboard-widgets/WidgetLibrary.tsx frontend/src/components/dashboard-widgets/QuickActionsWidget.tsx; do echo "=== $f ==="; grep -c "LtrIsolate\|useDirection" "$f" 2>/dev/null || echo "SKIP"; done</automated>
</verify>
<acceptance_criteria> - Each file that renders Recharts chart JSX contains "LtrIsolate" (import + usage) OR uses ChartContainer which already wraps in LtrIsolate - Each file that renders Recharts chart JSX contains "useDirection" OR inherits isRTL from ChartContainer - Each XAxis element has reversed={isRTL} prop - No double-wrapping of LtrIsolate (if ChartContainer already wraps, component files should NOT add another) - pnpm typecheck passes without new errors - grep -r "ResponsiveContainer\|BarChart\|LineChart\|PieChart\|AreaChart" frontend/src/ --include="\*.tsx" -l | xargs grep -L "LtrIsolate\|ChartContainer" returns 0 chart-rendering files (only config/navigation files)
</acceptance_criteria>
<done>All 10 component files have LtrIsolate coverage (directly or via ChartContainer), with reversed XAxis where applicable</done>
</task>

</tasks>

<verification>
1. `cd frontend && grep -r "ResponsiveContainer\|BarChart\|LineChart\|PieChart\|AreaChart\|RadarChart" src/ --include="*.tsx" -l | xargs grep -L "LtrIsolate\|ChartContainer" | grep -v navigation | grep -v IconRail | grep -v Sidebar | grep -v CommandPalette | wc -l` returns 0
2. `pnpm typecheck` passes
3. No double LtrIsolate wrapping in any file
</verification>

<success_criteria>

- Every file that renders a Recharts chart has LtrIsolate coverage (direct or via ChartContainer)
- XAxis elements use reversed={isRTL} for proper RTL axis direction
- No TypeScript errors introduced
- No double-wrapping of LtrIsolate
  </success_criteria>

<output>
After completion, create `.planning/phases/04-rtl-ltr-consistency/04-05-SUMMARY.md`
</output>
