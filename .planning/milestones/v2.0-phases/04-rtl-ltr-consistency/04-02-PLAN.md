---
phase: 04-rtl-ltr-consistency
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - 'frontend/src/**/*.tsx (469+ files — bulk codemod)'
  - frontend/src/components/ui/heroui-modal.tsx
  - frontend/src/components/ui/heroui-forms.tsx
autonomous: true
requirements: [RTL-01, RTL-02, RTL-05]

must_haves:
  truths:
    - "Zero per-component dir={isRTL ? 'rtl' : 'ltr'} attributes remain in codebase (except RTLWrapper root and intentional LTR isolation)"
    - "All isRTL = i18n.language === 'ar' patterns replaced with useDirection() hook"
    - 'Zero physical CSS properties (ml-*, mr-*, pl-*, pr-*, text-left, text-right) remain in frontend TSX/CSS files'
    - 'Icon rotation and animation logic using isRTL still functions correctly via useDirection()'
  artifacts:
    - path: 'frontend/src/components/ui/heroui-modal.tsx'
      provides: 'Physical CSS replaced with logical'
      contains: 'ms-|me-|ps-|pe-|text-start|text-end'
    - path: 'frontend/src/components/ui/heroui-forms.tsx'
      provides: 'Physical CSS replaced with logical'
      contains: 'ms-|me-|ps-|pe-|text-start|text-end'
  key_links:
    - from: '469+ component files'
      to: 'frontend/src/hooks/useDirection.ts'
      via: "import { useDirection } from '@/hooks/useDirection'"
      pattern: 'useDirection.*from.*@/hooks/useDirection'
    - from: '469+ component files'
      to: 'document.documentElement.dir'
      via: 'RTLWrapper/LanguageProvider sets dir at root'
      pattern: 'No per-component dir= attributes'
---

<objective>
Bulk-migrate all 469+ files to remove per-component `dir=` attributes and replace scattered `isRTL` patterns with `useDirection()` hook. Also fix the 2 files with physical CSS properties.

Purpose: Achieve RTL-01 (single DirectionProvider), RTL-02 (zero physical CSS), and RTL-05 (reusable patterns). This is the largest plan by file count but is highly mechanical — regex/codemod-driven.

Output: Clean codebase with zero per-component dir attributes, zero physical CSS, and all direction logic flowing through useDirection().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rtl-ltr-consistency/04-CONTEXT.md
@.planning/phases/04-rtl-ltr-consistency/04-RESEARCH.md
@.planning/phases/04-rtl-ltr-consistency/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From frontend/src/hooks/useDirection.ts:
```typescript
export function useDirection(): { direction: 'ltr' | 'rtl'; isRTL: boolean }
```

From frontend/src/components/ui/ltr-isolate.tsx:

```typescript
export function LtrIsolate({ children, className }: LtrIsolateProps): ReactElement
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bulk-remove per-component dir= attributes and replace isRTL patterns with useDirection()</name>
  <files>frontend/src/**/*.tsx (469+ files)</files>
  <read_first>
    - frontend/src/hooks/useDirection.ts
    - frontend/src/components/rtl-wrapper/RTLWrapper.tsx
    - frontend/src/components/language-provider/language-provider.tsx
  </read_first>
  <action>
Execute a multi-step codemod across all 469+ frontend TSX files. This is a bulk mechanical operation.

**Step 1: Remove dir={isRTL ? 'rtl' : 'ltr'} attributes (787 occurrences in 469 files)**

Use regex find-and-replace across all `frontend/src/**/*.tsx` files to remove these patterns:

- `dir={isRTL ? 'rtl' : 'ltr'}` — most common (remove entirely)
- `dir={isRTL ? "rtl" : "ltr"}` — double-quote variant (remove entirely)
- `dir={direction}` — remove EXCEPT in `RTLWrapper.tsx` line 21 (that is the root provider)

DO NOT remove:

- `dir="ltr"` on intentional LTR isolation containers (these are needed per D-05/D-06)
- `dir="rtl"` on intentional RTL override containers
- `dir="auto"` on mixed-direction text spans
- `dir={direction}` in `RTLWrapper.tsx` (line 21 — this is the single root provider)

**Step 2: Replace `const isRTL = i18n.language === 'ar'` with `useDirection()` hook**

For each file that has `const isRTL = i18n.language === 'ar'` (or `i18n.dir === 'rtl'` variants):

a. If `isRTL` is STILL USED after Step 1 (for icon rotation like `isRTL ? 'rotate-180' : ''`, animation direction like `isRTL ? 20 : -20`, or conditional logic):

- Replace `const isRTL = i18n.language === 'ar'` with `const { isRTL } = useDirection()`
- Add `import { useDirection } from '@/hooks/useDirection'` if not already present
- Remove `const { i18n } = useTranslation()` ONLY if `i18n` is no longer used elsewhere in the file (it may still be used for `i18n.changeLanguage()` or `t()`)
- Remove `import { useTranslation } from 'react-i18next'` ONLY if neither `t` nor `i18n` are used elsewhere

b. If `isRTL` is NO LONGER USED after Step 1 (the only usage was in `dir=` which was removed):

- Remove the entire `const isRTL = i18n.language === 'ar'` line
- Remove the `useTranslation` import ONLY if `t` and `i18n` are no longer used
- Do NOT add `useDirection` import (not needed if isRTL was only for dir=)

**Step 3: Verify no broken imports**

After the bulk operation, run `pnpm --filter frontend typecheck` to catch any broken imports or missing references. Fix any TypeScript errors.

**CRITICAL EXCEPTIONS (per Pitfall 5 from RESEARCH.md):**

- Files using `isRTL` for `rotate-180` (125 files) MUST keep the isRTL variable via useDirection()
- Files using `isRTL` for animation direction (framer-motion `x: isRTL ? 20 : -20`) MUST keep isRTL via useDirection()
- Files using `isRTL` for conditional CSS classes MUST keep isRTL via useDirection()
  </action>
  <verify>
  <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && echo "dir={isRTL count:" && rg "dir=\{isRTL" frontend/src/ --glob '_.tsx' 2>/dev/null | wc -l && echo "i18n.language===ar count:" && rg "i18n\.language._===._'ar'" frontend/src/ --glob '_.tsx' 2>/dev/null | wc -l && echo "Expected: both 0"</automated>
  </verify>
  <acceptance_criteria> - `rg "dir=\{isRTL" frontend/src/ --glob '*.tsx'` returns 0 matches - `rg "dir=\{direction" frontend/src/ --glob '*.tsx'` returns exactly 1 match (RTLWrapper.tsx) - `rg "i18n\.language.*===.*'ar'" frontend/src/ --glob '*.tsx'` returns 0 matches - `rg "useDirection" frontend/src/ --glob '*.tsx'` returns matches in files that still use isRTL for rotation/animation - `pnpm --filter frontend typecheck` passes (or only has pre-existing errors unrelated to RTL changes)
  </acceptance_criteria>
  <done>Zero per-component dir= ternary attributes remain. All isRTL patterns use useDirection() hook or are removed entirely. TypeScript compiles without new errors.</done>
  </task>

<task type="auto">
  <name>Task 2: Replace physical CSS properties with logical equivalents in 2 remaining files</name>
  <files>frontend/src/components/ui/heroui-modal.tsx, frontend/src/components/ui/heroui-forms.tsx</files>
  <read_first>
    - frontend/src/components/ui/heroui-modal.tsx
    - frontend/src/components/ui/heroui-forms.tsx
  </read_first>
  <action>
Replace ALL physical CSS properties in these 2 files using this exact mapping (per D-07):

| Find         | Replace With |
| ------------ | ------------ |
| `ml-`        | `ms-`        |
| `mr-`        | `me-`        |
| `pl-`        | `ps-`        |
| `pr-`        | `pe-`        |
| `text-left`  | `text-start` |
| `text-right` | `text-end`   |
| `left-`      | `start-`     |
| `right-`     | `end-`       |
| `rounded-l-` | `rounded-s-` |
| `rounded-r-` | `rounded-e-` |

For each file:

1. Read current contents
2. Apply all replacements — match whole Tailwind class tokens only (e.g., `ml-4` becomes `ms-4`, not `xml-4`)
3. Write updated file
4. Verify no physical properties remain with `rg '\b(ml-|mr-|pl-|pr-|text-left|text-right)\b' {file}`

IMPORTANT: Match at word boundaries. The regex pattern should be `\bml-` not just `ml-` to avoid matching things like `html-` or `xml-`.
</action>
<verify>
<automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && rg '\b(ml-|mr-|pl-|pr-|text-left|text-right|rounded-l-|rounded-r-)\b' frontend/src/components/ui/heroui-modal.tsx frontend/src/components/ui/heroui-forms.tsx 2>/dev/null | wc -l && echo "Expected: 0"</automated>
</verify>
<acceptance_criteria> - `rg '\b(ml-|mr-|pl-|pr-)\b' frontend/src/components/ui/heroui-modal.tsx` returns 0 matches - `rg '\b(ml-|mr-|pl-|pr-)\b' frontend/src/components/ui/heroui-forms.tsx` returns 0 matches - `rg '\b(text-left|text-right)\b' frontend/src/components/ui/heroui-modal.tsx frontend/src/components/ui/heroui-forms.tsx` returns 0 matches - heroui-modal.tsx contains `ms-` or `me-` or `ps-` or `pe-` where physical properties were - heroui-forms.tsx contains `ms-` or `me-` or `ps-` or `pe-` where physical properties were - `pnpm --filter frontend build` succeeds
</acceptance_criteria>
<done>Zero physical CSS properties in frontend TSX/CSS files. ESLint rtl-friendly rule reports zero violations.</done>
</task>

</tasks>

<verification>
- `rg "dir=\{isRTL" frontend/src/ --glob '*.tsx' | wc -l` returns 0
- `rg "i18n\.language.*===.*'ar'" frontend/src/ --glob '*.tsx' | wc -l` returns 0
- `rg '\b(ml-|mr-|pl-|pr-|text-left|text-right)\b' frontend/src/ --glob '*.tsx' --glob '*.css' | wc -l` returns 0
- `pnpm --filter frontend typecheck` passes
- `pnpm --filter frontend build` succeeds
</verification>

<success_criteria>

- Zero per-component dir= ternary attributes (RTL-01 achieved)
- Zero physical CSS properties in all frontend files (RTL-02 achieved)
- All components using direction logic import from useDirection() (RTL-05 achieved)
- Build and typecheck pass cleanly
  </success_criteria>

<output>
After completion, create `.planning/phases/04-rtl-ltr-consistency/04-02-SUMMARY.md`
</output>
