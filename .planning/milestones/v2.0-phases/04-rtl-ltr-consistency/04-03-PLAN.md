---
phase: 04-rtl-ltr-consistency
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - frontend/src/components/relationships/AdvancedGraphVisualization.tsx
  - frontend/src/components/relationships/RelationshipGraphPanel.tsx
  - frontend/src/components/network-graph/*.tsx
  - frontend/src/components/charts/*.tsx
  - frontend/src/components/analytics/*.tsx
  - frontend/src/components/kanban/*.tsx
  - frontend/src/components/unified-kanban/*.tsx
  - frontend/src/components/data-table/*.tsx
autonomous: false
requirements: [RTL-03, RTL-04]

must_haves:
  truths:
    - 'React Flow graph containers wrapped in LtrIsolate — nodes render without position bugs in RTL'
    - 'Recharts chart containers wrapped in LtrIsolate — XAxis reversed when isRTL, tooltips correct'
    - 'DnD-kit kanban boards tested in RTL — drag coordinates work correctly (RTL modifier applied if needed)'
    - 'TanStack Table headers use text-start not text-left — table direction handled by document dir'
    - 'All 4 theme x direction combos (light+LTR, light+RTL, dark+LTR, dark+RTL) produce no visual bugs'
  artifacts:
    - path: 'frontend/src/components/relationships/AdvancedGraphVisualization.tsx'
      provides: 'React Flow wrapped in LtrIsolate'
      contains: 'LtrIsolate'
    - path: 'frontend/src/components/kanban/*.tsx'
      provides: 'DnD-kit tested/wrapped for RTL'
  key_links:
    - from: 'React Flow graph components'
      to: 'frontend/src/components/ui/ltr-isolate.tsx'
      via: 'LtrIsolate wrapper around ReactFlow'
      pattern: 'LtrIsolate.*ReactFlow'
    - from: 'Recharts chart components'
      to: 'frontend/src/components/ui/ltr-isolate.tsx'
      via: 'LtrIsolate wrapper around ResponsiveContainer'
      pattern: 'LtrIsolate.*ResponsiveContainer'
---

<objective>
Wrap third-party visualization libraries (React Flow, Recharts, DnD-kit, TanStack Table) in RTL-safe containers and validate all 4 theme x direction combinations.

Purpose: Achieve RTL-04 (third-party components correct in RTL) and RTL-03 (theme switching without visual bugs). React Flow and Recharts have known RTL bugs — the standard workaround is `dir="ltr"` isolation. DnD-kit needs empirical testing. TanStack Table is headless and just needs CSS fixes.

Output: All third-party components render correctly in RTL, verified across light/dark x AR/EN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rtl-ltr-consistency/04-CONTEXT.md
@.planning/phases/04-rtl-ltr-consistency/04-RESEARCH.md
@.planning/phases/04-rtl-ltr-consistency/04-01-SUMMARY.md
@.planning/phases/04-rtl-ltr-consistency/04-02-SUMMARY.md

<interfaces>
From frontend/src/components/ui/ltr-isolate.tsx:
```typescript
export function LtrIsolate({ children, className }: LtrIsolateProps): ReactElement
```

From frontend/src/hooks/useDirection.ts:

```typescript
export function useDirection(): { direction: 'ltr' | 'rtl'; isRTL: boolean }
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap React Flow and Recharts in LtrIsolate, configure DnD-kit and TanStack Table for RTL</name>
  <files>
    frontend/src/components/relationships/AdvancedGraphVisualization.tsx,
    frontend/src/components/relationships/RelationshipGraphPanel.tsx,
    frontend/src/components/network-graph/*.tsx,
    frontend/src/components/charts/*.tsx,
    frontend/src/components/analytics/*.tsx,
    frontend/src/components/kanban/*.tsx,
    frontend/src/components/unified-kanban/*.tsx,
    frontend/src/components/data-table/*.tsx
  </files>
  <read_first>
    - frontend/src/components/ui/ltr-isolate.tsx
    - frontend/src/hooks/useDirection.ts
    - frontend/src/components/relationships/AdvancedGraphVisualization.tsx
  </read_first>
  <action>
**React Flow (11 files) — per D-09, research: known broken in RTL:**

Find all files that render `<ReactFlow` or `<ReactFlowProvider` components. For each file:

1. Import `LtrIsolate` from `@/components/ui/ltr-isolate`
2. Wrap the `<ReactFlow>` component (or its immediate container div) in `<LtrIsolate className="h-full w-full">` (preserve existing height/width classes)
3. DO NOT wrap the entire page — only the ReactFlow canvas container
4. Node label content that displays Arabic text should use `dir="auto"` on the label span

Primary targets (from research):

- `frontend/src/components/relationships/AdvancedGraphVisualization.tsx` (main graph)
- `frontend/src/components/relationships/RelationshipGraphPanel.tsx`
- Any files in `frontend/src/components/network-graph/`

Search command: `rg -l 'ReactFlow|@xyflow' frontend/src/ --glob '*.tsx'`

**Recharts (10 files) — per D-09, research: limited RTL support:**

Find all files that render Recharts components (`<BarChart`, `<LineChart`, `<PieChart`, `<AreaChart`, `<ResponsiveContainer`). For each file:

1. Import `LtrIsolate` from `@/components/ui/ltr-isolate`
2. Import `useDirection` from `@/hooks/useDirection` (if `isRTL` is needed for XAxis)
3. Wrap `<ResponsiveContainer>` (or the outermost chart container) in `<LtrIsolate>`
4. Add `reversed={isRTL}` prop to any `<XAxis>` component to flip axis order in RTL
5. For custom tooltip content (`<Tooltip content={<CustomTooltip />}`), add `dir="auto"` to the tooltip wrapper div so Arabic text renders correctly inside the LTR chart

Search command: `rg -l 'BarChart|LineChart|PieChart|AreaChart|ResponsiveContainer' frontend/src/ --glob '*.tsx'`

**DnD-kit (kanban boards) — per D-09, research: unknown RTL behavior:**

Find all files using `@dnd-kit/core` or `@dnd-kit/sortable`. For each file:

1. First, test if drag-and-drop works correctly in RTL by running the dev server and switching to Arabic
2. If drag coordinates are inverted (items snap to wrong positions), add an RTL modifier:

```typescript
import { useDirection } from '@/hooks/useDirection'

const { isRTL } = useDirection()
const rtlModifier: Modifier = ({ transform }) => ({
  ...transform,
  x: isRTL ? -transform.x : transform.x,
})
// Add to DndContext: <DndContext modifiers={isRTL ? [rtlModifier] : []}>
```

3. If drag works correctly in RTL (browser handles coordinate flipping), no modifier needed — just document the finding in the SUMMARY

Search command: `rg -l '@dnd-kit' frontend/src/ --glob '*.tsx'`

**TanStack Table (5 files) — per research: works via CSS, headless:**

Find all files using `@tanstack/react-table`. These do NOT need LtrIsolate wrapping. Instead:

1. Ensure table header cells use `text-start` not `text-left` (should already be fixed by Plan 02 physical CSS migration)
2. Verify table renders correctly by checking `th` and `td` elements have no hardcoded `text-left` or `text-right` classes
3. No wrapper component needed — document-level `dir` handles table layout

Search command: `rg -l '@tanstack/react-table|useReactTable' frontend/src/ --glob '*.tsx'`
</action>
<verify>
<automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && echo "React Flow files with LtrIsolate:" && rg -l "ReactFlow" frontend/src/ --glob '_.tsx' 2>/dev/null | xargs rg -l "LtrIsolate" 2>/dev/null | wc -l && echo "Recharts files with LtrIsolate:" && rg -l "ResponsiveContainer" frontend/src/ --glob '_.tsx' 2>/dev/null | xargs rg -l "LtrIsolate" 2>/dev/null | wc -l && echo "Build check:" && pnpm --filter frontend typecheck 2>&1 | tail -3</automated>
</verify>
<acceptance_criteria> - Every file containing `<ReactFlow` also contains `LtrIsolate` import and wrapping - Every file containing `<ResponsiveContainer` also contains `LtrIsolate` import and wrapping - Recharts files with `<XAxis` contain `reversed={isRTL}` prop - DnD-kit files either have RTL modifier applied OR documented as working without it - TanStack Table files contain zero `text-left` or `text-right` classes - `pnpm --filter frontend typecheck` passes
</acceptance_criteria>
<done>All third-party visualization libraries wrapped or configured for RTL. React Flow and Recharts isolated in LtrIsolate. DnD-kit tested and fixed if needed. TanStack Table verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of 4 theme x direction combinations</name>
  <files>N/A — verification only, no files modified</files>
  <read_first>
    - frontend/src/components/ui/ltr-isolate.tsx
    - frontend/src/hooks/useDirection.ts
  </read_first>
  <action>
Run `pnpm dev` to start the dev server. Then present the user with the visual verification checklist below.

Complete RTL/LTR migration across entire frontend has been built:

- useDirection() hook replaces all scattered isRTL patterns
- Zero per-component dir= attributes (single DirectionProvider at root)
- Zero physical CSS properties (all logical: ms-_, me-_, ps-_, pe-_, text-start, text-end)
- React Flow, Recharts wrapped in LtrIsolate
- DnD-kit kanban and TanStack Table verified for RTL
- ESLint rtl-friendly plugin prevents future regressions

User must test all 4 combinations (per D-11):

**Combo 1: English + Light mode**

1. Set language to English, theme to Light
2. Navigate: Dashboard -> any Country dossier -> Relationships tab (React Flow graph) -> Analytics page (Recharts) -> Kanban board
3. Expected: Standard LTR layout, graphs/charts render normally, drag-and-drop works

**Combo 2: Arabic + Light mode**

1. Switch language to Arabic (keep Light mode)
2. Navigate same pages as above
3. Expected: RTL layout, text right-aligned, navigation flipped, graphs/charts still render (inside LTR containers), Arabic labels visible, Kanban drag works

**Combo 3: English + Dark mode**

1. Switch to English + Dark mode
2. Navigate same pages
3. Expected: LTR layout with dark theme, no color/layout interaction bugs

**Combo 4: Arabic + Dark mode**

1. Switch to Arabic + Dark mode
2. Navigate same pages
3. Expected: RTL + dark theme, all elements properly positioned, no overlapping, no layout shifts

**Specific checks:**

- No elements overlapping or misaligned in any combo
- Directional icons (arrows, chevrons) point correctly in RTL
- Charts display data correctly (hover tooltips match visual position)
- Tables have correct text alignment per direction
- Modals and forms render correctly in both directions
- No content jumping/shifting when switching language
  </action>
  <verify>
  <automated>echo "Checkpoint: requires human visual verification of 4 theme x direction combos"</automated>
  </verify>
  <acceptance_criteria> - User has tested all 4 combos (light+LTR, light+RTL, dark+LTR, dark+RTL) - User confirms no visual bugs or layout shifts - User types "approved" to proceed
  </acceptance_criteria>
  <done>Human approved all 4 theme x direction combinations as visually correct with no layout bugs.</done>
  </task>

</tasks>

<verification>
- All React Flow files have LtrIsolate wrapping
- All Recharts files have LtrIsolate wrapping and XAxis reversed for RTL
- DnD-kit tested in RTL and working (with or without modifier)
- TanStack Table renders correctly in RTL via document dir
- Human verified all 4 theme x direction combos
- `pnpm --filter frontend build` succeeds
</verification>

<success_criteria>

- Third-party components render correctly in RTL mode (RTL-04)
- All 4 theme x direction combos produce no visual bugs (RTL-03)
- Human approval of visual verification checkpoint
  </success_criteria>

<output>
After completion, create `.planning/phases/04-rtl-ltr-consistency/04-03-SUMMARY.md`
</output>
