---
phase: 04-rtl-ltr-consistency
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - eslint.config.mjs
autonomous: true
gap_closure: true
requirements:
  - RTL-02
  - RTL-05

must_haves:
  truths:
    - 'eslint-plugin-rtl-friendly is imported and registered in eslint.config.mjs'
    - 'Running eslint on frontend files catches physical CSS properties via rtl-friendly/no-physical-properties rule'
  artifacts:
    - path: 'eslint.config.mjs'
      provides: 'RTL-friendly ESLint plugin registration'
      contains: 'rtl-friendly'
  key_links:
    - from: 'eslint.config.mjs'
      to: 'eslint-plugin-rtl-friendly'
      via: 'import + plugins + rules config block'
      pattern: "import rtlFriendly from 'eslint-plugin-rtl-friendly'"
---

<objective>
Wire eslint-plugin-rtl-friendly into eslint.config.mjs so the RTL enforcement layer actually fires.

Purpose: The plugin was installed as a devDependency in 04-01 but never imported or registered in the ESLint config. Physical CSS regressions are currently uncaught at lint time.
Output: Updated eslint.config.mjs with rtl-friendly plugin active on frontend files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rtl-ltr-consistency/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register eslint-plugin-rtl-friendly in eslint.config.mjs</name>
  <files>eslint.config.mjs</files>
  <read_first>
    - eslint.config.mjs (current ESLint flat config - already has no-restricted-syntax RTL rules at lines 99-151)
  </read_first>
  <action>
Add the rtl-friendly plugin import and config block to eslint.config.mjs:

1. Add import at top of file (after line 6, before line 7):

   ```
   import rtlFriendly from 'eslint-plugin-rtl-friendly'
   ```

2. Add a NEW config block targeting frontend files. Insert it AFTER the existing frontend override block (after line 152, before the UI library exception block). The block should be:
   ```javascript
   // ── RTL-friendly plugin: catch physical CSS in Tailwind classes ────
   {
     files: ['frontend/**/*.{ts,tsx}'],
     ignores: ['frontend/**/components/ui/**/*.{ts,tsx}'],
     plugins: {
       'rtl-friendly': rtlFriendly,
     },
     rules: {
       'rtl-friendly/no-physical-properties': 'warn',
     },
   },
   ```

IMPORTANT NOTES:

- Set the rule to 'warn' (not 'error') because the existing no-restricted-syntax rules already cover physical CSS at error level. The rtl-friendly plugin adds a second layer with auto-fix capability.
- Exclude `frontend/**/components/ui/**` to match the existing UI library exception at lines 154-161 that already turns off no-restricted-syntax for shadcn wrappers.
- Do NOT remove or modify the existing no-restricted-syntax RTL rules (lines 99-151) -- they complement the plugin.
  </action>
  <verify>
  <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && grep -c "rtl-friendly" eslint.config.mjs</automated>
  </verify>
  <acceptance_criteria> - eslint.config.mjs line 7 (or nearby) contains: import rtlFriendly from 'eslint-plugin-rtl-friendly' - eslint.config.mjs contains a config block with: plugins: { 'rtl-friendly': rtlFriendly } - eslint.config.mjs contains: 'rtl-friendly/no-physical-properties': 'warn' - eslint.config.mjs contains ignores pattern: 'frontend/**/components/ui/**/\*.{ts,tsx}' - grep -c "rtl-friendly" eslint.config.mjs returns 4 or more - Existing no-restricted-syntax RTL rules (lines 99-151) are unchanged - pnpm lint runs without config parse errors (exit 0 or only pre-existing warnings)
  </acceptance_criteria>
  <done>eslint-plugin-rtl-friendly is imported, registered as a plugin, and its no-physical-properties rule is active on all frontend files except UI wrappers</done>
  </task>

</tasks>

<verification>
1. `grep -c "rtl-friendly" eslint.config.mjs` returns >= 4 (import + plugin key + rule + ignores context)
2. `cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && pnpm lint 2>&1 | head -5` completes without "Failed to load plugin" errors
</verification>

<success_criteria>

- eslint-plugin-rtl-friendly is imported and registered in eslint.config.mjs
- The rule rtl-friendly/no-physical-properties fires on frontend files (warn level)
- UI wrapper files (components/ui/) are excluded from the rule
- ESLint config parses without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/04-rtl-ltr-consistency/04-04-SUMMARY.md`
</output>
