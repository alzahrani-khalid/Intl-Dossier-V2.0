---
phase: 06-architecture-consolidation
plan: 05
type: execute
wave: 3
depends_on: ['06-02', '06-03', '06-04']
files_modified:
  - backend/src/services/tasks.service.ts
  - backend/src/services/event.service.ts
  - backend/src/services/country.service.ts
  - backend/src/services/signature.service.ts
  - backend/src/services/brief.service.ts
  - backend/src/services/link.service.ts
  - .planning/codebase/ARCHITECTURE.md
autonomous: true
requirements:
  - ARCH-03
  - ARCH-04

must_haves:
  truths:
    - 'No duplicate service files remain in backend/src/services/'
    - 'All API routes that imported merged services now import from the primary service'
    - 'Architecture docs reflect the actual flat services structure, not aspirational ports/adapters'
    - 'Backend tests still pass after service merges'
  artifacts:
    - path: 'backend/src/services/tasks.service.ts'
      provides: 'Merged tasks service including task-creation logic'
      contains: 'task-creation functions merged in'
    - path: 'backend/src/services/event.service.ts'
      provides: 'Merged events service including conflict detection'
    - path: 'backend/src/services/country.service.ts'
      provides: 'Merged countries service including search'
    - path: 'backend/src/services/signature.service.ts'
      provides: 'Merged signature service including orchestrator'
    - path: 'backend/src/services/brief.service.ts'
      provides: 'Merged brief service including context'
    - path: 'backend/src/services/link.service.ts'
      provides: 'Merged link service including audit and migration'
    - path: '.planning/codebase/ARCHITECTURE.md'
      provides: 'Updated architecture docs matching actual structure'
  key_links:
    - from: 'backend/src/api/*.ts'
      to: 'backend/src/services/*.ts'
      via: 'import from merged service files'
      pattern: 'import.*from.*services/'
---

<objective>
Merge 6 backend duplicate service pairs and update architecture documentation to reflect the actual codebase structure.

Purpose: Eliminates service duplication (ARCH-03) and ensures architecture docs match reality (D-08). This runs after all frontend migrations are complete.
Output: 6 service files deleted (merged into primaries), all API route imports updated, architecture docs accurate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-architecture-consolidation/06-CONTEXT.md
@.planning/phases/06-architecture-consolidation/06-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge 6 backend duplicate service pairs into primary services</name>
  <files>
    backend/src/services/tasks.service.ts,
    backend/src/services/task-creation.service.ts,
    backend/src/services/event.service.ts,
    backend/src/services/event-conflicts.ts,
    backend/src/services/country.service.ts,
    backend/src/services/countries-search.ts,
    backend/src/services/signature.service.ts,
    backend/src/services/signature-orchestrator.ts,
    backend/src/services/brief.service.ts,
    backend/src/services/brief-context.service.ts,
    backend/src/services/link.service.ts,
    backend/src/services/link-audit.service.ts,
    backend/src/services/link-migration.service.ts
  </files>
  <read_first>
    backend/src/services/tasks.service.ts,
    backend/src/services/task-creation.service.ts,
    backend/src/services/event.service.ts,
    backend/src/services/event-conflicts.ts,
    backend/src/services/country.service.ts,
    backend/src/services/countries-search.ts,
    backend/src/services/signature.service.ts,
    backend/src/services/signature-orchestrator.ts,
    backend/src/services/brief.service.ts,
    backend/src/services/brief-context.service.ts,
    backend/src/services/link.service.ts,
    backend/src/services/link-audit.service.ts,
    backend/src/services/link-migration.service.ts
  </read_first>
  <action>
    For each of 6 merge pairs, follow this process (per D-07):

    **Merge 1: task-creation.service.ts -> tasks.service.ts**
    1. Read both files completely
    2. Copy all exported functions from `task-creation.service.ts` into `tasks.service.ts`
    3. Copy any imports needed by the moved functions
    4. Run `grep -rn "task-creation.service" backend/src/` to find all importers
    5. Update each importer to import from `tasks.service` instead
    6. Delete `task-creation.service.ts`

    **Merge 2: event-conflicts.ts -> event.service.ts**
    1. Same process: merge exports, update importers (`grep -rn "event-conflicts" backend/src/`), delete
    2. Note: source file may not have `.service.ts` suffix — handle exact filename

    **Merge 3: countries-search.ts -> country.service.ts**
    1. Same process: merge, update (`grep -rn "countries-search" backend/src/`), delete

    **Merge 4: signature-orchestrator.ts -> signature.service.ts**
    1. Same process: merge, update (`grep -rn "signature-orchestrator" backend/src/`), delete

    **Merge 5: brief-context.service.ts -> brief.service.ts**
    1. Same process: merge, update (`grep -rn "brief-context" backend/src/`), delete

    **Merge 6: link-audit.service.ts + link-migration.service.ts -> link.service.ts**
    1. Two files merge into one primary. Same process for each.
    2. Update importers for both (`grep -rn "link-audit\|link-migration" backend/src/`), delete both

    After all merges:
    - Run `pnpm --filter backend tsc --noEmit` to verify no type errors
    - Run `pnpm --filter backend test -- --run` to verify tests pass
    - Verify deleted files no longer exist

    **Per D-16 (discretion on utility extraction):** During merges, if you notice duplicated pagination/filtering utility code across the services being merged, extract into `backend/src/utils/` as a separate helper. Only do this if the duplication is obvious (same function copy-pasted). Do NOT over-engineer.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && pnpm --filter backend tsc --noEmit 2>&1 | tail -5 && echo "---Deleted files check---" && ls backend/src/services/task-creation.service.ts backend/src/services/event-conflicts.ts backend/src/services/countries-search.ts backend/src/services/signature-orchestrator.ts backend/src/services/brief-context.service.ts backend/src/services/link-audit.service.ts backend/src/services/link-migration.service.ts 2>&1 | grep -c "No such file"</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/services/task-creation.service.ts does NOT exist
    - backend/src/services/event-conflicts.ts does NOT exist
    - backend/src/services/countries-search.ts does NOT exist
    - backend/src/services/signature-orchestrator.ts does NOT exist
    - backend/src/services/brief-context.service.ts does NOT exist
    - backend/src/services/link-audit.service.ts does NOT exist
    - backend/src/services/link-migration.service.ts does NOT exist
    - `grep -rn "task-creation.service\|event-conflicts\|countries-search\|signature-orchestrator\|brief-context.service\|link-audit.service\|link-migration.service" backend/src/api/` returns zero results (all imports updated)
    - `pnpm --filter backend tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>7 duplicate service files deleted, all exports merged into 6 primary services, all API route imports updated, TypeScript clean</done>
</task>

<task type="auto">
  <name>Task 2: Update architecture documentation to reflect actual structure</name>
  <files>
    .planning/codebase/ARCHITECTURE.md
  </files>
  <read_first>
    .planning/codebase/ARCHITECTURE.md,
    .planning/phases/06-architecture-consolidation/06-CONTEXT.md
  </read_first>
  <action>
    Update `.planning/codebase/ARCHITECTURE.md` (per D-06 and D-08) to reflect the ACTUAL codebase structure after Phase 6 consolidation:

    **Changes required:**

    1. **Backend services section:** Update to describe flat `backend/src/services/` structure (per D-06). Remove any references to ports/adapters directories. List the ~38 remaining service files (after 7 deletions from 44 original). Note that search services remain separate (4 files for distinct search strategies).

    2. **Frontend domains section:** Add new section describing the domain repository pattern:
       - `frontend/src/domains/{feature}/` structure (repositories/, hooks/, types/, index.ts)
       - Shared apiClient at `frontend/src/lib/api-client.ts`
       - Re-export pattern at `frontend/src/hooks/` for backward compatibility
       - List all created domain directories (dossiers, positions, engagements, calendar, work-items, relationships, documents, persons, topics, ai, search, intake, audit, analytics, briefings, tags, import, misc)

    3. **Data flow section:** Update to show: Route -> Hook -> Repository -> apiClient -> API endpoint. Remove any mention of direct fetch() in hooks.

    4. **Key layers table:** Update to include the domains layer between Routes and Components.

    5. **Remove aspirational content:** Any references to patterns not actually implemented (like hexagonal architecture, port interfaces, adapter factories) should be removed or noted as "not implemented."

    Keep the document concise and factual. It should describe what IS, not what SHOULD BE.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && grep -c "domains/" .planning/codebase/ARCHITECTURE.md && grep -c "api-client" .planning/codebase/ARCHITECTURE.md && grep -c "ports/adapters\|port interface\|adapter factory" .planning/codebase/ARCHITECTURE.md || echo "0 aspirational refs"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/codebase/ARCHITECTURE.md contains `frontend/src/domains/` (at least 3 occurrences)
    - .planning/codebase/ARCHITECTURE.md contains `api-client` (at least 1 occurrence)
    - .planning/codebase/ARCHITECTURE.md contains `backend/src/services/` describing flat structure
    - .planning/codebase/ARCHITECTURE.md does NOT contain `ports/adapters` as a directory path (D-06: no ports/adapters dirs)
    - .planning/codebase/ARCHITECTURE.md contains the word `repository` or `repositories` (documenting the new pattern)
    - .planning/codebase/ARCHITECTURE.md describes the data flow: Hook -> Repository -> apiClient
  </acceptance_criteria>
  <done>Architecture docs accurately describe the actual flat backend services, frontend domain repository pattern, and apiClient-based data flow</done>
</task>

</tasks>

<verification>
- `pnpm --filter backend tsc --noEmit` passes
- `pnpm --filter backend test -- --run` passes
- 7 duplicate service files deleted
- Architecture docs updated with domain pattern, flat services, no aspirational content
- `grep -rn "task-creation.service\|event-conflicts\|countries-search\|signature-orchestrator\|brief-context\|link-audit\|link-migration" backend/src/` returns only git-related or comment references
</verification>

<success_criteria>

- 6 service merge pairs completed (7 files deleted)
- All API route imports updated to primary services
- Backend TypeScript compilation and tests pass
- Architecture docs reflect actual structure post-consolidation
- ARCH-03 fully satisfied (no duplicate services)
- D-08 satisfied (docs match reality)
  </success_criteria>

<output>
After completion, create `.planning/phases/06-architecture-consolidation/06-05-SUMMARY.md`
</output>
