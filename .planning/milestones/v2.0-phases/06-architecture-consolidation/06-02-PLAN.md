---
phase: 06-architecture-consolidation
plan: 02
type: execute
wave: 2
depends_on: ['06-01']
files_modified:
  - frontend/src/domains/positions/repositories/positions.repository.ts
  - frontend/src/domains/positions/hooks/usePositions.ts
  - frontend/src/domains/positions/hooks/usePosition.ts
  - frontend/src/domains/positions/hooks/useCreatePosition.ts
  - frontend/src/domains/positions/hooks/useUpdatePosition.ts
  - frontend/src/domains/positions/hooks/useSubmitPosition.ts
  - frontend/src/domains/positions/hooks/usePositionSuggestions.ts
  - frontend/src/domains/positions/hooks/usePositionAnalytics.ts
  - frontend/src/domains/positions/hooks/usePositionDossierLinks.ts
  - frontend/src/domains/positions/hooks/useCreatePositionDossierLink.ts
  - frontend/src/domains/positions/hooks/useDeletePositionDossierLink.ts
  - frontend/src/domains/positions/types/index.ts
  - frontend/src/domains/positions/index.ts
  - frontend/src/domains/engagements/repositories/engagements.repository.ts
  - frontend/src/domains/engagements/hooks/useEngagements.ts
  - frontend/src/domains/engagements/hooks/useEngagementKanban.ts
  - frontend/src/domains/engagements/hooks/useEngagementBriefs.ts
  - frontend/src/domains/engagements/hooks/useEngagementRecommendations.ts
  - frontend/src/domains/engagements/types/index.ts
  - frontend/src/domains/engagements/index.ts
  - frontend/src/domains/calendar/repositories/calendar.repository.ts
  - frontend/src/domains/calendar/hooks/useCalendarEvents.ts
  - frontend/src/domains/calendar/hooks/useCreateCalendarEvent.ts
  - frontend/src/domains/calendar/hooks/useUpdateCalendarEvent.ts
  - frontend/src/domains/calendar/hooks/useCalendarConflicts.ts
  - frontend/src/domains/calendar/hooks/useRecurringEvents.ts
  - frontend/src/domains/calendar/types/index.ts
  - frontend/src/domains/calendar/index.ts
autonomous: true
requirements:
  - ARCH-02
  - ARCH-04

must_haves:
  truths:
    - 'Positions domain has repository + 10 hooks migrated from raw fetch()'
    - 'Engagements domain has repository + 4 hooks migrated from raw fetch()'
    - 'Calendar domain has repository + 5 hooks migrated from raw fetch()'
    - 'All 19 hooks route through domain repositories, not direct fetch()'
    - 'Old hook import paths still work via re-exports'
  artifacts:
    - path: 'frontend/src/domains/positions/repositories/positions.repository.ts'
      provides: 'Plain function exports for all position API operations'
      exports: ['getPositions', 'getPosition', 'createPosition', 'updatePosition', 'submitPosition']
    - path: 'frontend/src/domains/engagements/repositories/engagements.repository.ts'
      provides: 'Plain function exports for all engagement API operations'
      exports: ['getEngagements', 'getEngagementKanban', 'getEngagementBriefs']
    - path: 'frontend/src/domains/calendar/repositories/calendar.repository.ts'
      provides: 'Plain function exports for all calendar API operations'
      exports: ['getCalendarEvents', 'createCalendarEvent', 'updateCalendarEvent']
  key_links:
    - from: 'frontend/src/domains/positions/hooks/*.ts'
      to: 'frontend/src/domains/positions/repositories/positions.repository.ts'
      via: 'import from ../repositories/'
      pattern: "positionsRepo\\."
    - from: 'frontend/src/domains/engagements/hooks/*.ts'
      to: 'frontend/src/domains/engagements/repositories/engagements.repository.ts'
      via: 'import from ../repositories/'
      pattern: "engagementsRepo\\."
    - from: 'frontend/src/domains/calendar/hooks/*.ts'
      to: 'frontend/src/domains/calendar/repositories/calendar.repository.ts'
      via: 'import from ../repositories/'
      pattern: "calendarRepo\\."
---

<objective>
Migrate positions (10 hooks), engagements (4 hooks), and calendar (5 hooks) domains to the repository pattern established in Plan 01.

Purpose: These are the three largest remaining core domains. Migrating them eliminates 19 raw fetch() hooks.
Output: Three complete domain directories with repos, hooks, types, barrels, and backward-compat re-exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-architecture-consolidation/06-CONTEXT.md
@.planning/phases/06-architecture-consolidation/06-RESEARCH.md
@.planning/phases/06-architecture-consolidation/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate positions domain (10 hooks) to repository pattern</name>
  <files>
    frontend/src/domains/positions/repositories/positions.repository.ts,
    frontend/src/domains/positions/hooks/usePositions.ts,
    frontend/src/domains/positions/hooks/usePosition.ts,
    frontend/src/domains/positions/hooks/useCreatePosition.ts,
    frontend/src/domains/positions/hooks/useUpdatePosition.ts,
    frontend/src/domains/positions/hooks/useSubmitPosition.ts,
    frontend/src/domains/positions/hooks/usePositionSuggestions.ts,
    frontend/src/domains/positions/hooks/usePositionAnalytics.ts,
    frontend/src/domains/positions/hooks/usePositionDossierLinks.ts,
    frontend/src/domains/positions/hooks/useCreatePositionDossierLink.ts,
    frontend/src/domains/positions/hooks/useDeletePositionDossierLink.ts,
    frontend/src/domains/positions/types/index.ts,
    frontend/src/domains/positions/index.ts
  </files>
  <read_first>
    frontend/src/hooks/usePositions.ts,
    frontend/src/hooks/usePosition.ts,
    frontend/src/hooks/useCreatePosition.ts,
    frontend/src/hooks/useUpdatePosition.ts,
    frontend/src/hooks/useSubmitPosition.ts,
    frontend/src/hooks/usePositionSuggestions.ts,
    frontend/src/hooks/usePositionAnalytics.ts,
    frontend/src/hooks/usePositionDossierLinks.ts,
    frontend/src/hooks/useCreatePositionDossierLink.ts,
    frontend/src/hooks/useDeletePositionDossierLink.ts,
    frontend/src/lib/api-client.ts,
    frontend/src/domains/dossiers/repositories/dossiers.repository.ts,
    frontend/src/domains/dossiers/index.ts
  </read_first>
  <action>
    Follow the exact same pattern established in Plan 01 for the dossiers domain. For each of the 10 position hooks:

    **Step 1 — Types:** Read all 10 source hooks. Extract type definitions into `frontend/src/domains/positions/types/index.ts`. Include position-specific interfaces for filters, create/update params, analytics responses, dossier link params.

    **Step 2 — Repository (`positions.repository.ts`):**
    - Import `{ apiGet, apiPost, apiPut, apiPatch, apiDelete }` from `@/lib/api-client`
    - Extract fetch calls from each hook into plain exported functions (per D-02):
      - `getPositions(filters)` — list positions with filters
      - `getPosition(id)` — single position detail
      - `createPosition(data)` — create new position
      - `updatePosition(id, data)` — update existing position
      - `submitPosition(id)` — submit position for review
      - `getPositionSuggestions(params)` — AI-powered suggestions
      - `getPositionAnalytics(id)` — analytics data
      - `getPositionDossierLinks(positionId)` — linked dossiers
      - `createPositionDossierLink(positionId, dossierId)` — create link
      - `deletePositionDossierLink(linkId)` — delete link
    - Preserve exact URL paths and query param serialization from source hooks
    - All functions must have explicit return types

    **Step 3 — Hooks:** Create 10 hook files in `domains/positions/hooks/`, each:
    - Imports from `../repositories/positions.repository`
    - Preserves exact hook signature (per D-12)
    - Preserves query key structure
    - Zero `fetch(`, zero `getAuthHeaders(`, zero `API_BASE_URL`

    **Step 4 — Barrel:** `frontend/src/domains/positions/index.ts` re-exports all hooks + repo.

    **Step 5 — Re-exports:** Replace each of the 10 original files in `frontend/src/hooks/` with `export { ... } from '@/domains/positions'`.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && grep -c "fetch(" frontend/src/domains/positions/hooks/*.ts 2>/dev/null | grep -v ":0" || echo "No raw fetch in position hooks - PASS"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/domains/positions/repositories/positions.repository.ts exists and contains `import { apiGet` from `@/lib/api-client`
    - frontend/src/domains/positions/repositories/positions.repository.ts contains `export function getPositions`
    - frontend/src/domains/positions/hooks/usePositions.ts exists and does NOT contain `fetch(`
    - frontend/src/domains/positions/index.ts exists with re-exports for all 10 hooks
    - frontend/src/hooks/usePositions.ts contains `from '@/domains/positions'`
    - `grep -c "fetch(" frontend/src/domains/positions/hooks/*.ts` shows 0 for all files
  </acceptance_criteria>
  <done>All 10 position hooks migrated to domain repository pattern with zero raw fetch() and backward-compat re-exports</done>
</task>

<task type="auto">
  <name>Task 2: Migrate engagements (4 hooks) and calendar (5 hooks) domains to repository pattern</name>
  <files>
    frontend/src/domains/engagements/repositories/engagements.repository.ts,
    frontend/src/domains/engagements/hooks/useEngagements.ts,
    frontend/src/domains/engagements/hooks/useEngagementKanban.ts,
    frontend/src/domains/engagements/hooks/useEngagementBriefs.ts,
    frontend/src/domains/engagements/hooks/useEngagementRecommendations.ts,
    frontend/src/domains/engagements/types/index.ts,
    frontend/src/domains/engagements/index.ts,
    frontend/src/domains/calendar/repositories/calendar.repository.ts,
    frontend/src/domains/calendar/hooks/useCalendarEvents.ts,
    frontend/src/domains/calendar/hooks/useCreateCalendarEvent.ts,
    frontend/src/domains/calendar/hooks/useUpdateCalendarEvent.ts,
    frontend/src/domains/calendar/hooks/useCalendarConflicts.ts,
    frontend/src/domains/calendar/hooks/useRecurringEvents.ts,
    frontend/src/domains/calendar/types/index.ts,
    frontend/src/domains/calendar/index.ts
  </files>
  <read_first>
    frontend/src/hooks/useEngagements.ts,
    frontend/src/hooks/useEngagementKanban.ts,
    frontend/src/hooks/useEngagementBriefs.ts,
    frontend/src/hooks/useEngagementRecommendations.ts,
    frontend/src/hooks/useCalendarEvents.ts,
    frontend/src/hooks/useCreateCalendarEvent.ts,
    frontend/src/hooks/useUpdateCalendarEvent.ts,
    frontend/src/hooks/useCalendarConflicts.ts,
    frontend/src/hooks/useRecurringEvents.ts,
    frontend/src/lib/api-client.ts,
    frontend/src/domains/positions/repositories/positions.repository.ts
  </read_first>
  <action>
    Apply the same domain migration pattern for two domains:

    **Engagements domain (4 hooks):**
    1. Types: Extract from source hooks into `domains/engagements/types/index.ts`
    2. Repository (`engagements.repository.ts`): Plain function exports — `getEngagements(filters)`, `getEngagementKanban(params)`, `getEngagementBriefs(id)`, `getEngagementRecommendations(id)`. Import from `@/lib/api-client`.
    3. Hooks: 4 files in `domains/engagements/hooks/`, each importing from repo, preserving signatures and query keys
    4. Barrel: `domains/engagements/index.ts`
    5. Re-exports: Replace 4 files in `frontend/src/hooks/` with re-exports from `@/domains/engagements`

    **Calendar domain (5 hooks):**
    1. Types: Extract from source hooks into `domains/calendar/types/index.ts`
    2. Repository (`calendar.repository.ts`): Plain function exports — `getCalendarEvents(params)`, `createCalendarEvent(data)`, `updateCalendarEvent(id, data)`, `getCalendarConflicts(params)`, `getRecurringEvents(params)`. Import from `@/lib/api-client`.
    3. Hooks: 5 files in `domains/calendar/hooks/`, each importing from repo
    4. Barrel: `domains/calendar/index.ts`
    5. Re-exports: Replace 5 files in `frontend/src/hooks/` with re-exports from `@/domains/calendar`

    After both domains, run `pnpm --filter frontend tsc --noEmit` to verify no type errors.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && pnpm --filter frontend tsc --noEmit 2>&1 | tail -5 && grep -c "fetch(" frontend/src/domains/engagements/hooks/*.ts frontend/src/domains/calendar/hooks/*.ts 2>/dev/null | grep -v ":0" || echo "No raw fetch in engagement/calendar hooks - PASS"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/domains/engagements/repositories/engagements.repository.ts exists and contains `import { apiGet` from `@/lib/api-client`
    - frontend/src/domains/engagements/hooks/useEngagements.ts exists and does NOT contain `fetch(`
    - frontend/src/domains/engagements/index.ts exists with re-exports
    - frontend/src/domains/calendar/repositories/calendar.repository.ts exists and contains `import { apiGet` from `@/lib/api-client`
    - frontend/src/domains/calendar/hooks/useCalendarEvents.ts exists and does NOT contain `fetch(`
    - frontend/src/domains/calendar/index.ts exists with re-exports
    - frontend/src/hooks/useEngagements.ts contains `from '@/domains/engagements'`
    - frontend/src/hooks/useCalendarEvents.ts contains `from '@/domains/calendar'`
    - `pnpm --filter frontend tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>All 9 engagement + calendar hooks migrated to domain repos, zero raw fetch(), backward-compat re-exports, TypeScript clean</done>
</task>

</tasks>

<verification>
- `pnpm --filter frontend tsc --noEmit` passes
- `grep -rn "fetch(" frontend/src/domains/positions/hooks/ frontend/src/domains/engagements/hooks/ frontend/src/domains/calendar/hooks/` returns nothing
- All 19 re-export files in `frontend/src/hooks/` point to their respective `@/domains/*` paths
- Domain directories exist: positions/, engagements/, calendar/ each with repositories/, hooks/, types/, index.ts
</verification>

<success_criteria>

- 3 complete domain directories (positions, engagements, calendar)
- 19 hooks migrated with zero raw fetch()
- Backward-compat re-exports at old paths
- TypeScript compilation clean
  </success_criteria>

<output>
After completion, create `.planning/phases/06-architecture-consolidation/06-02-SUMMARY.md`
</output>
