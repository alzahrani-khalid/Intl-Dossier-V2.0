---
phase: 23-missing-verifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/17-seed-data-first-run/17-VERIFICATION.md
autonomous: true
requirements: [SEED-01, SEED-02, SEED-03]

must_haves:
  truths:
    - 'Phase 17 has a VERIFICATION.md confirming SEED-01 is satisfied with grep/CLI evidence'
    - 'Phase 17 has a VERIFICATION.md confirming SEED-02 is satisfied with grep/CLI evidence'
    - 'Phase 17 has a VERIFICATION.md confirming SEED-03 is satisfied with grep/CLI evidence'
  artifacts:
    - path: '.planning/phases/17-seed-data-first-run/17-VERIFICATION.md'
      provides: 'Formal verification of SEED-01, SEED-02, SEED-03'
      contains: 'SEED-01'
  key_links:
    - from: '17-VERIFICATION.md'
      to: '17-02-SUMMARY.md, 17-03-SUMMARY.md, 17-04-SUMMARY.md, 17-05-SUMMARY.md'
      via: 'Evidence references'
      pattern: 'SEED-0[123]'
---

<objective>
Create formal VERIFICATION.md for Phase 17 (Seed Data & First Run) confirming SEED-01, SEED-02, SEED-03 are satisfied with code + runtime evidence.

Purpose: Close the 3 partial requirement gaps identified in v4.0-MILESTONE-AUDIT.md where Phase 17 code is complete per SUMMARYs but lacks formal verification.
Output: `.planning/phases/17-seed-data-first-run/17-VERIFICATION.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v4.0-MILESTONE-AUDIT.md
@.planning/phases/17-seed-data-first-run/17-01-SUMMARY.md
@.planning/phases/17-seed-data-first-run/17-02-SUMMARY.md
@.planning/phases/17-seed-data-first-run/17-03-SUMMARY.md
@.planning/phases/17-seed-data-first-run/17-04-SUMMARY.md
@.planning/phases/17-seed-data-first-run/17-05-SUMMARY.md
@.planning/phases/18-e2e-test-suite/18-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 17 VERIFICATION.md with code + runtime evidence</name>
  <files>.planning/phases/17-seed-data-first-run/17-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/18-e2e-test-suite/18-VERIFICATION.md (format reference)
    - .planning/phases/17-seed-data-first-run/17-01-SUMMARY.md (is_seed_data columns)
    - .planning/phases/17-seed-data-first-run/17-02-SUMMARY.md (populate_diplomatic_seed RPC)
    - .planning/phases/17-seed-data-first-run/17-03-SUMMARY.md (check_first_run RPC)
    - .planning/phases/17-seed-data-first-run/17-04-SUMMARY.md (FirstRunModal component)
    - .planning/phases/17-seed-data-first-run/17-05-SUMMARY.md (dashboard wiring + useFirstRunCheck)
    - supabase/migrations/20260407000002_populate_diplomatic_seed.sql (seed RPC source)
    - supabase/migrations/20260407000003_check_first_run.sql (first-run RPC source)
    - supabase/migrations/20260407000001_add_is_seed_data_columns.sql (tagging migration)
    - frontend/src/components/FirstRun/FirstRunModal.tsx (first-run UI)
    - frontend/src/hooks/useFirstRunCheck.ts (first-run hook)
    - frontend/src/routes/_protected/dashboard.tsx (modal mount point)
    - frontend/src/i18n/en/sample-data.json (EN i18n keys)
    - frontend/src/i18n/ar/sample-data.json (AR i18n keys)
  </read_first>
  <action>
Create `.planning/phases/17-seed-data-first-run/17-VERIFICATION.md` following the exact format of `18-VERIFICATION.md`. The verification must use code + runtime evidence per D-01 and D-03.

**Frontmatter** (YAML):

- phase: 17-seed-data-first-run
- verified: current date ISO
- status: passed (or human_needed if any runtime check cannot be automated)
- score: 3/3 (for SEED-01, SEED-02, SEED-03)

**Verification approach for each requirement:**

**SEED-01** ("Seed script creates realistic diplomatic scenario with 5-10 countries, organizations, forums, and engagements at various lifecycle stages with realistic names and relationships"):

1. grep the migration file `20260407000002_populate_diplomatic_seed.sql` for entity counts: confirm 10 countries, 10 organizations, 10 forums, 10 engagements exist in the fixture arrays
2. grep for bilingual names (name_en, name_ar patterns) confirming realistic diplomatic names (GASTAT, GCC-Stat, UNSD, etc.)
3. grep for lifecycle stage references (engagement_status or lifecycle_stage values) confirming various stages
4. Record grep output as evidence

**SEED-02** ("Seed data includes dossier relationships across tiers and work items in different states"):

1. grep `20260407000002_populate_diplomatic_seed.sql` for `work_item_dossiers` inserts confirming cross-tier relationships
2. grep for `inheritance_source` values confirming multiple relationship types (direct, engagement, etc.)
3. grep for task generation loop confirming different status/priority/workflow_stage values via enum cycling
4. Count task generation (should be 50 tasks with full enum coverage)

**SEED-03** ("First-run experience detects empty database and offers to populate seed data"):

1. grep `20260407000003_check_first_run.sql` for `check_first_run` function confirming it returns `is_empty` and `can_seed`
2. grep `frontend/src/hooks/useFirstRunCheck.ts` for the RPC call and state mapping
3. grep `frontend/src/components/FirstRun/FirstRunModal.tsx` for the populate button and dialog
4. grep `frontend/src/routes/_protected/dashboard.tsx` for FirstRunModal mount
5. grep `frontend/src/i18n/en/sample-data.json` for `firstRun` key namespace
6. grep `frontend/src/i18n/ar/sample-data.json` for `firstRun` key namespace (bilingual)
7. Run `pnpm test useFirstRunCheck --run` and `pnpm test FirstRunModal --run` to confirm tests pass (runtime evidence per D-01)

**Required Artifacts table:** List all key files created in Phase 17 with verification status.

**Key Link Verification table:** Show connections between:

- populate_diplomatic_seed RPC -> FirstRunModal (via supabase.rpc call)
- check_first_run RPC -> useFirstRunCheck hook (via supabase.rpc call)
- useFirstRunCheck -> dashboard.tsx (via hook import)
- FirstRunModal -> dashboard.tsx (via component mount)
- i18n keys -> FirstRunModal (via useTranslation)

**Requirements Coverage table:** Map SEED-01, SEED-02, SEED-03 to specific files and evidence.

**If any verification reveals a gap (per D-02):** Fix it inline and document the fix in the VERIFICATION.md under a "Fixes Applied" section with the commit hash.
</action>
<verify>
<automated>grep -c "SEED-01" .planning/phases/17-seed-data-first-run/17-VERIFICATION.md && grep -c "SEED-02" .planning/phases/17-seed-data-first-run/17-VERIFICATION.md && grep -c "SEED-03" .planning/phases/17-seed-data-first-run/17-VERIFICATION.md && grep -c "VERIFIED" .planning/phases/17-seed-data-first-run/17-VERIFICATION.md</automated>
</verify>
<acceptance_criteria> - File `.planning/phases/17-seed-data-first-run/17-VERIFICATION.md` exists - File contains "SEED-01" at least 2 times (requirement ID + coverage table) - File contains "SEED-02" at least 2 times - File contains "SEED-03" at least 2 times - File contains "VERIFIED" at least 3 times (one per requirement) - File contains YAML frontmatter with `phase: 17-seed-data-first-run` - File contains "Observable Truths" section with pass/fail per ROADMAP success criteria - File contains "Required Artifacts" table listing key Phase 17 files - File contains "Key Link Verification" table - File contains "Requirements Coverage" table - File contains grep output or CLI output as evidence (not just prose claims) - If tests were run, their pass/fail output is included
</acceptance_criteria>
<done>Phase 17 VERIFICATION.md exists with evidence-backed verification of all 3 SEED requirements matching the depth of 18-VERIFICATION.md</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No trust boundaries — this plan creates documentation artifacts only, no production code changes.

## STRIDE Threat Register

| Threat ID | Category                   | Component       | Disposition | Mitigation Plan                                                                                      |
| --------- | -------------------------- | --------------- | ----------- | ---------------------------------------------------------------------------------------------------- |
| T-23-01   | I (Information Disclosure) | VERIFICATION.md | accept      | Verification files are in .planning/ which is gitignored from production builds; no secrets included |

</threat_model>

<verification>
- 17-VERIFICATION.md exists and contains evidence for SEED-01, SEED-02, SEED-03
- Each requirement has grep/CLI evidence, not just prose claims
- Format matches 18-VERIFICATION.md reference pattern
</verification>

<success_criteria>
Phase 17 has a formal VERIFICATION.md that an auditor can read to confirm SEED-01, SEED-02, SEED-03 are satisfied with concrete evidence (grep output, test results, file existence checks).
</success_criteria>

<output>
After completion, create `.planning/phases/23-missing-verifications/23-01-SUMMARY.md`
</output>
