---
phase: 58-tier-c-design-token-suppression-full-clear
plan: 02
type: execute
wave: 2
surface: tables
depends_on: ['00', '01']
files_modified:
  - frontend/src/components/assignments/EscalationDashboard.tsx
  - frontend/src/components/audit-logs/AuditLogFilters.tsx
  - frontend/src/components/audit-logs/AuditLogStatistics.tsx
  - frontend/src/components/audit-logs/AuditLogTable.tsx
  - frontend/src/components/bulk-actions/BulkActionProgressIndicator.tsx
  - frontend/src/components/bulk-actions/BulkActionsToolbar.tsx
  - frontend/src/components/bulk-actions/SelectableDataTable.tsx
  - frontend/src/components/bulk-actions/UndoToast.tsx
  - frontend/src/components/elected-officials/ElectedOfficialListTable.tsx
  - frontend/src/components/entity-comparison/EntityComparisonSelector.tsx
  - frontend/src/components/entity-comparison/EntityComparisonTable.tsx
  - frontend/src/components/risk-list/RiskList.tsx
  - frontend/src/components/triage-panel/TriagePanel.tsx
  - frontend/src/components/version-history-viewer/VersionHistoryViewer.tsx
  - frontend/src/components/working-groups/WGMemberSuggestions.tsx
autonomous: true
requirements: [TOKEN-01, TOKEN-02]
requirements_addressed: [TOKEN-01, TOKEN-02]
tags: [tier-c, design-tokens, tables, suppression-clear]

scope:
  total_files: 15
  total_annotations: 137
  heaviest_file: 'frontend/src/components/triage-panel/TriagePanel.tsx (53 annotations)'
  files_with_dark_variants: 2 # EntityComparisonTable, WGMemberSuggestions
  files_with_emoji: 1 # TriagePanel.tsx (⚠️)

must_haves:
  truths:
    - 'All 137 `Phase 51 Tier-C` lint suppressions across the 15 Wave-2 source files are removed'
    - 'Every replaced Tailwind color literal resolves to a real `@theme`-mapped token defined in frontend/src/index.css'
    - 'No new `no-restricted-syntax` suppressions introduced anywhere in the Wave-2 file set'
    - 'pnpm lint exits with 0 errors / 0 warnings under --max-warnings 0'
    - 'pnpm type-check exits with 0 TypeScript diagnostics'
    - 'pnpm vitest run keeps the project at 1,241+ passing tests (zero regressions in any existing test)'
    - 'No emoji symbols remain in user-visible JSX in TriagePanel.tsx (⚠️ replaced with AlertTriangle lucide icon, per Wave-1 precedent)'
    - 'Confidence-tier semantics in TriagePanel.tsx remain distinguishable: high (success) / medium (warning) / low (danger) / unknown (ink-faint)'
    - 'Logical properties (ms-*, pe-*, start-*, end-*) preserved — RTL behavior unchanged from pre-Phase-58 baseline'
    - 'Row-height tokens (--row-h) remain wherever tables previously used them; no hard-coded h-N substitutions'
  artifacts:
    - path: 'frontend/src/components/triage-panel/TriagePanel.tsx'
      provides: 'Token-bound AI triage surface — confidence tiers (success/warning/danger/ink-faint), AlertTriangle icon replaces ⚠️, status banners use bg-warning/10 + border-warning/30'
      contains: 'AlertTriangle'
    - path: 'frontend/src/components/audit-logs/AuditLogTable.tsx'
      provides: 'Token-bound audit table — row borders use border-line, hover uses bg-muted, status cells use semantic tokens (success/warning/danger)'
      contains: 'border-line'
    - path: 'frontend/src/components/bulk-actions/SelectableDataTable.tsx'
      provides: 'Token-bound selectable table primitive — selected row state uses bg-primary/10, checkbox accent uses accent-foreground'
      contains: 'bg-primary/10'
    - path: 'frontend/src/components/entity-comparison/EntityComparisonTable.tsx'
      provides: 'Token-bound entity comparison — dark-variant chains removed (this is one of two Wave-2 files with dark: variants), diff highlight cells use bg-warning/10'
      contains: 'bg-warning/10'

  key_links:
    - from: 'frontend/src/components/triage-panel/TriagePanel.tsx'
      to: 'frontend/src/index.css#L46-L98'
      via: '@theme block maps text-success / text-warning / text-danger / text-ink-faint used by getConfidenceColor()'
      pattern: '--color-success: var(--ok)'

deviations:
  - id: 'D-58-02-01'
    rule: 'Map orange-600 (low-confidence) to text-warning, preserving the medium-vs-low distinction'
    instance: 'TriagePanel.tsx getConfidenceColor() — three confidence tiers (high/medium/low) collapse onto two tokens (warning/danger) if low→warning'
    decision: 'low (<0.6) → text-danger (skeptical-of-AI semantics: a 50% confidence triage suggestion should warn the analyst); medium (0.6-0.8) → text-warning; high (0.8+) → text-success; unknown → text-ink-faint'
    rationale: 'Keeps the three-tier visual distinction. Low-confidence AI output is the case where the analyst should override the suggestion — danger color reinforces "do not trust blindly".'
  - id: 'D-58-02-02'
    rule: 'CLAUDE.md: No emoji in user-visible copy.'
    instance: 'TriagePanel.tsx line 142 — ⚠️ in AI-unavailable banner'
    decision: 'Replace with AlertTriangle lucide-react import (matches Wave-1 DuplicateComparison precedent).'

token_mapping_rules:
  # Same vocabulary as Wave 1; new patterns specific to tables:
  text_color_swaps:
    - 'text-gray-500 / text-gray-400': 'text-ink-faint'
    - 'text-gray-600 dark:text-gray-400': 'text-ink-mute'
    - 'text-gray-700 dark:text-gray-300': 'text-ink'
    - 'text-gray-900 dark:text-white': 'text-ink'
    - 'text-green-600': 'text-success'
    - 'text-yellow-600 / text-yellow-700 dark:text-yellow-300/400': 'text-warning'
    - 'text-orange-600': 'text-danger (Wave-2 only: low-confidence semantics — see D-58-02-01)'
    - 'text-red-500 / text-red-600 dark:text-red-400': 'text-danger'
    - 'text-blue-500 / text-blue-600 dark:text-blue-400': 'text-info OR text-accent (context-dependent — accent for links/CTAs; info for informational icons)'
    - 'text-yellow-800 dark:text-yellow-300': 'text-warning'

  border_swaps:
    - 'border-gray-200 / border-gray-300 dark:border-gray-600/700': 'border-line'
    - 'border-red-200 dark:border-red-800': 'border-danger/30'
    - 'border-yellow-200 dark:border-yellow-800': 'border-warning/30'
    - 'border-green-200 dark:border-green-800': 'border-success/30'
    - 'border-blue-200 dark:border-blue-800': 'border-info/30'
    - 'border-blue-500 / border-blue-600': 'border-primary'

  background_swaps:
    - 'bg-white dark:bg-gray-800': 'bg-surface'
    - 'bg-gray-50 dark:bg-gray-900': 'bg-muted'
    - 'bg-gray-100 dark:bg-gray-800 (hover/active)': 'bg-muted'
    - 'bg-red-50 dark:bg-red-900/20': 'bg-danger/10'
    - 'bg-yellow-50 dark:bg-yellow-900/20': 'bg-warning/10'
    - 'bg-green-50 dark:bg-green-900/20': 'bg-success/10'
    - 'bg-blue-50 dark:bg-blue-900/20': 'bg-info/10'
    - 'bg-blue-100 (selected row)': 'bg-primary/10'

  emoji_swaps:
    - '⚠️ → <AlertTriangle className="h-5 w-5 text-warning" aria-hidden="true" />'
    - '✓  → <CheckCircle2 className="..." aria-hidden="true" />'
    - 'Add icon imports from lucide-react where first used; do not duplicate if already imported'

  forbidden_introductions:
    - 'No raw hex codes (#RRGGBB)'
    - 'No Tailwind palette literals (text-red-500, bg-blue-100, etc.) except bg-black/N for modal scrim (D-58-01-01 precedent)'
    - 'No new eslint-disable lines (the goal is removal)'
    - 'No textAlign:"right" — use logical text-end if alignment needed'

verification:
  commands:
    - 'pnpm lint --max-warnings 0'
    - 'pnpm type-check'
    - 'pnpm vitest run'
  evidence_required:
    - 'lint exit code 0, zero errors, zero warnings'
    - 'type-check exit code 0'
    - 'vitest output shows 1,241+ passing tests — zero regressions'
    - '0 grep hits for `Phase 51 Tier-C` in any of the 15 Wave-2 source files'
    - '0 grep hits for `dark:` color variants in the 15 source files (the @theme tokens handle theme switching via CSS custom properties)'
    - '0 grep hits for raw Tailwind palette colors (text-{red,blue,green,yellow,gray}-N, bg-{red,blue,green,yellow,gray}-N, border-{red,blue,green,yellow,gray}-N) in any Wave-2 file. bg-black/N is allowed only as documented per D-58-01-01.'
---

<objective>
Clear all 137 Wave-2 (tables surface) `Phase 51 Tier-C` lint suppressions across 15 files by rewriting Tailwind color literals to design-system tokens, applying the same vocabulary established in Wave 1 plus three table-specific patterns (selected-row, row-hover, status-cell). All work happens on branch `phase-58/wave-2-tables`, forked from `main` (origin/HEAD), not from `phase-58/wave-1-forms` — Wave 2 should be independently reviewable.

## File inventory (15 files, 137 annotations)

| File                                                       | Annotations | Dark variants | Multi-literal lines | Notes                                                                                   |
| ---------------------------------------------------------- | :---------: | :-----------: | :-----------------: | --------------------------------------------------------------------------------------- |
| components/assignments/EscalationDashboard.tsx             |      3      |      no       |         no          | Status indicators                                                                       |
| components/audit-logs/AuditLogFilters.tsx                  |      3      |      no       |         no          | Filter chips                                                                            |
| components/audit-logs/AuditLogStatistics.tsx               |      4      |      no       |         yes         | Stat cards                                                                              |
| components/audit-logs/AuditLogTable.tsx                    |     10      |      no       |         yes         | Row hover, status cells, severity                                                       |
| components/bulk-actions/BulkActionProgressIndicator.tsx    |     13      |      no       |         yes         | Progress bar, status states                                                             |
| components/bulk-actions/BulkActionsToolbar.tsx             |      8      |      no       |         yes         | Action buttons, selection counter                                                       |
| components/bulk-actions/SelectableDataTable.tsx            |      3      |      no       |         yes         | Selected-row background, checkbox accent                                                |
| components/bulk-actions/UndoToast.tsx                      |      6      |      no       |         yes         | Toast (semantic: info or success)                                                       |
| components/elected-officials/ElectedOfficialListTable.tsx  |      2      |      no       |         yes         | Term-status badges                                                                      |
| components/entity-comparison/EntityComparisonSelector.tsx  |      2      |      no       |         yes         | Selection chips                                                                         |
| components/entity-comparison/EntityComparisonTable.tsx     |     13      |    **yes**    |         yes         | One of two files with dark: chains. Diff-highlight cells use bg-warning/10              |
| components/risk-list/RiskList.tsx                          |      4      |      no       |         no          | Risk severity badges                                                                    |
| components/triage-panel/TriagePanel.tsx                    |     53      |      no       |         yes         | **HEAVIEST.** Confidence tiers (D-58-02-01), AI-unavailable banner with ⚠️ (D-58-02-02) |
| components/version-history-viewer/VersionHistoryViewer.tsx |      7      |      no       |         no          | Version-diff highlights                                                                 |
| components/working-groups/WGMemberSuggestions.tsx          |      6      |    **yes**    |         no          | Other dark: chain file                                                                  |

## Execution order (sequential, single executor agent)

The wave is small enough (137 annotations across 15 files, no cross-file dependencies) to execute in a single gsd-executor pass without sub-tasking. The order below proceeds from lowest-risk (small files, no dark variants) to highest-risk (TriagePanel.tsx with 53 annotations + emoji + tri-color confidence semantics).

1. EscalationDashboard.tsx (3)
2. AuditLogFilters.tsx (3)
3. SelectableDataTable.tsx (3) — sets the selected-row pattern (bg-primary/10)
4. ElectedOfficialListTable.tsx (2)
5. EntityComparisonSelector.tsx (2)
6. AuditLogStatistics.tsx (4)
7. RiskList.tsx (4)
8. UndoToast.tsx (6)
9. VersionHistoryViewer.tsx (7)
10. AuditLogTable.tsx (10)
11. BulkActionsToolbar.tsx (8)
12. BulkActionProgressIndicator.tsx (13)
13. WGMemberSuggestions.tsx (6) — dark variants present
14. EntityComparisonTable.tsx (13) — dark variants present
15. TriagePanel.tsx (53) — confidence tiers + emoji (D-58-02-01, D-58-02-02)

## Key decisions encoded ahead of execution

**D-58-02-01 (confidence-tier colors):** The executor must map `getConfidenceColor()` in TriagePanel.tsx as: high (≥0.8) → `text-success`, medium (≥0.6) → `text-warning`, low (<0.6) → `text-danger`, unknown → `text-ink-faint`. Do NOT collapse low and medium to the same token; the visual distinction is functional, not decorative.

**D-58-02-02 (emoji replacement):** Add `import { AlertTriangle } from 'lucide-react'` to TriagePanel.tsx if not already present, and replace the `⚠️` span with the icon component. Match the JSX shape used in Wave-1 DuplicateComparison.tsx: `<AlertTriangle className="h-5 w-5 text-warning" aria-hidden="true" />` (note: text-warning here because the surrounding banner is yellow/warning, not red/danger).

**Status banner pattern (carry-over from Wave 1):** `border border-{tone}/30 bg-{tone}/10 text-{tone}` for warning/success/info/danger banners. No dark: variants needed — tokens handle theme switching.

**Row-hover pattern:** `hover:bg-muted` (matches Wave 1 advanced-search files).

**Selected-row pattern:** `bg-primary/10` for the selected state on table rows, `border-primary` for the selected outline if rows have one.
</objective>

<tasks>

## Task 1 — Branch and prep

Create `phase-58/wave-2-tables` from `origin/main`. Do not branch from `phase-58/wave-1-forms` — Wave 2 should land as its own PR.

```bash
git fetch origin main
git checkout -b phase-58/wave-2-tables origin/main
```

## Task 2 — Execute 15 file rewrites in the order listed above

For each file: remove every `Phase 51 Tier-C` suppression and rewrite its associated literal to a design-system token per the mapping rules. Each file gets its own atomic commit:

```
chore(58/wave-2): clear Tier-C suppressions in <file basename> (<N> annotations)
```

For TriagePanel.tsx, the commit body must explicitly enumerate:

- The confidence-tier mapping (D-58-02-01)
- The emoji-to-lucide replacement (D-58-02-02)

## Task 3 — Verification

After all 15 commits, run from the project root:

```bash
pnpm lint        # must exit 0, zero warnings
pnpm type-check  # must exit 0
pnpm vitest run  # must show 1,241+ passing, zero regressions
```

If any check fails, do not proceed to the SUMMARY commit; report the failure and stop.

## Task 4 — Final grep validation

```bash
# Must return 0 hits:
grep -rn "Phase 51 Tier-C" \
  frontend/src/components/assignments/EscalationDashboard.tsx \
  frontend/src/components/audit-logs/ \
  frontend/src/components/bulk-actions/ \
  frontend/src/components/elected-officials/ElectedOfficialListTable.tsx \
  frontend/src/components/entity-comparison/ \
  frontend/src/components/risk-list/RiskList.tsx \
  frontend/src/components/triage-panel/TriagePanel.tsx \
  frontend/src/components/version-history-viewer/VersionHistoryViewer.tsx \
  frontend/src/components/working-groups/WGMemberSuggestions.tsx
```

## Task 5 — Write SUMMARY.md

`58-02-SUMMARY.md` documenting all 15 file commits, verification evidence, and any deviations recorded during execution. Use 58-01-SUMMARY.md as the template.

</tasks>
