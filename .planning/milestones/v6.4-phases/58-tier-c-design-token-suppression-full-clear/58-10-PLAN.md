---
phase: 58-tier-c-design-token-suppression-full-clear
plan: 10
type: execute
wave: 7
surface: closure
depends_on: ['00', '01', '02', '03', '04', '05', '06', '07', '08', '09']
files_modified:
  - eslint.config.mjs
autonomous: false # lint behavior change requires verification; if pnpm lint fails after waiver removal, the wave is incomplete and a prior wave missed a suppression — manual recovery required
requirements: [TOKEN-01, TOKEN-02]
requirements_addressed: [TOKEN-01, TOKEN-02]
tags: [tier-c, design-tokens, closure, suppression-clear]

scope:
  total_files: 1
  total_annotations: 0 # this plan deletes the waiver token; it does not clear suppressions in source
  heaviest_file: 'eslint.config.mjs (1 file, 3 edits across lines 10–28, 217, 292–297)'
  files_with_dark_variants: 0
  files_with_blue_purple_collision: 0
  files_with_multi_literal_line: 0
  files_with_raw_hex: 0
  files_with_emoji: 0

must_haves:
  truths:
    - 'The designTokenSyntaxRestrictions constant block at eslint.config.mjs:10–28 is deleted entirely'
    - 'The `...designTokenSyntaxRestrictions` spread at eslint.config.mjs:217 is removed'
    - 'The bad-design-token.tsx fixture block at eslint.config.mjs:292–297 is updated to INLINE the 3 selectors directly (option (a) from RESEARCH section 8), keeping the fixture functional and the CI positive-failure assertion intact pending Phase 59 POLISH-04'
    - 'Zero grep hits for `gsd-design-token-tier-c-allow` across frontend/src/'
    - 'Zero grep hits for `Phase 51 Tier-C` across frontend/src/'
    - 'Zero grep hits for `designTokenSyntaxRestrictions`, `tier-c`, or `Tier-C` in eslint.config.mjs'
    - 'pnpm lint --max-warnings 0 exits 0 workspace-wide WITHOUT the waiver constant present (this is the critical canary: if any prior wave missed a suppression, lint fails here)'
    - 'pnpm type-check exits 0 workspace-wide'
    - 'pnpm vitest run keeps the project at 1,241+ passing tests'
    - 'Signed phase-58-base tag pushed per BUNDLE-06 convention (git tag -v phase-58-base prints "Good \"git\" signature")'
  artifacts:
    - path: 'eslint.config.mjs'
      provides: 'Single source of frontend/src lint rules; Tier-C waiver removed; D-05 selectors at error severity preserved for the frontend file glob'
      contains: 'no-restricted-syntax'

verification_commands:
  - 'grep -rn "gsd-design-token-tier-c-allow" frontend/src/  # SUCCESS CRITERION 1: expect zero matches'
  - 'grep -n "designTokenSyntaxRestrictions\|tier-c\|Tier-C" eslint.config.mjs  # SUCCESS CRITERION 2: expect zero matches'
  - 'pnpm lint --max-warnings 0  # SUCCESS CRITERION 3: expect exit 0 workspace-wide WITHOUT the waiver'
  - 'pnpm type-check  # expect exit 0'
  - 'pnpm vitest run  # expect 1,241+ passing'
  - 'git tag phase-58-base && git tag -v phase-58-base  # expect Good "git" signature per CLAUDE.md tag-signing setup'
---

# Wave 7 — Closure (eslint.config.mjs Tier-C Waiver Removal)

Wave 7 deletes the `designTokenSyntaxRestrictions` constant from `eslint.config.mjs`, removes its spread reference at line 217, and inlines the 3 selectors directly into the `bad-design-token.tsx` fixture block at lines 292–297. After Wave 7, the workspace-wide `pnpm lint` MUST exit 0 without the waiver token present — this is the canary that proves Waves 1–6 cleared every Tier-C suppression. If lint fails, a prior wave missed a file; the executor reverts Wave 7 and patches the failing prior wave's SUMMARY before retrying.

**This plan is NOT autonomous.** Lint behavior change requires verification; if `pnpm lint` fails after waiver removal, the wave is incomplete and a prior wave missed a suppression. Manual recovery (revert Wave 7 commit; patch prior wave; re-run) is required. Set `autonomous: false` so the orchestrator pauses for the operator after Task 3.

## Inputs read

Every executor task MUST read the following before touching code:

- `eslint.config.mjs` — full file (focus on lines 1–28, 200–230, 280–310) to understand current selector layout
- `.planning/phases/58-tier-c-design-token-suppression-full-clear/58-01-PLAN.md` through `58-09-PLAN.md` — confirm each prior wave's `requirements-completed: [TOKEN-01, TOKEN-02]` (via reading the corresponding SUMMARY files when available)
- `.planning/phases/58-tier-c-design-token-suppression-full-clear/58-RESEARCH.md` — Section 8 (Wave 7 line-precise removal plan) + Section 11 (verification command sequence) + OQ-58-RES-05 (Wave 7 vs Phase 59 POLISH-04 sequencing)
- `CLAUDE.md` "Tag signing setup" section — for the `phase-58-base` SSH-signed tag at end of wave
- `.planning/REQUIREMENTS.md` — TOKEN-01, TOKEN-02 definitions (Wave 7 closes both)

## Wave 7 scope summary

| Edit | File              | Line(s) | Action                                                                                |
| ---: | ----------------- | ------- | ------------------------------------------------------------------------------------- |
|    1 | eslint.config.mjs | 10–28   | DELETE the entire `const designTokenSyntaxRestrictions = [ ... ]` block (3 selectors) |
|    2 | eslint.config.mjs | 217     | REMOVE the `...designTokenSyntaxRestrictions,` line inside the frontend override      |
|    3 | eslint.config.mjs | 292–297 | INLINE the 3 selectors directly into the `bad-design-token.tsx` fixture block         |

**The 3 selectors to inline at edit 3 (verbatim from current eslint.config.mjs:10–28):**

1. **Selector 1 (raw hex):** `Literal[value=/#[0-9a-fA-F]{3,8}\b/]` with message: `Raw hex colors are not allowed in frontend/src. Use a design token (var(--accent), var(--ink), var(--line), ...) or a token-mapped Tailwind utility (text-accent, text-ink, border-line). See frontend/src/index.css @theme block and CLAUDE.md Design rules - non-negotiable.`
2. **Selector 2 (Tailwind palette in Literal):** `Literal[value=/(?:^|\s)(?:[a-z-]+:)*(text|bg|border|ring|fill|stroke|from|to|via|outline|divide|placeholder|caret|decoration|shadow)-(red|blue|green|yellow|purple|pink|indigo|cyan|teal|emerald|amber|rose|orange|sky|slate|gray|zinc|neutral|stone|fuchsia|violet|lime)-\d{2,3}\b/]` with message: `Tailwind palette literals (text-blue-*, bg-red-*, border-amber-*, ...) are not allowed in frontend/src. Use a token-mapped utility (text-accent, text-danger, text-success, text-warning, text-info, text-ink, bg-surface, border-line) or the semantic-colors.ts map. See frontend/src/index.css @theme block.`
3. **Selector 3 (Tailwind palette in TemplateElement):** `TemplateElement[value.raw=/(?:^|\s)(?:[a-z-]+:)*(text|bg|border|ring|fill|stroke|from|to|via|outline|divide|placeholder|caret|decoration|shadow)-(red|blue|green|yellow|purple|pink|indigo|cyan|teal|emerald|amber|rose|orange|sky|slate|gray|zinc|neutral|stone|fuchsia|violet|lime)-\d{2,3}\b/]` with message: `Tailwind palette literals are not allowed in frontend/src, including inside template literals. Use a token-mapped utility (text-accent, text-danger, text-success, text-warning, text-info, text-ink, bg-surface, border-line) or the semantic-colors.ts map.`

After edit 3, the `bad-design-token.tsx` fixture block must read approximately:

```
files: ['tools/eslint-fixtures/bad-design-token.tsx'],
rules: {
  'no-restricted-syntax': ['warn', { selector: '...', message: '...' }, { selector: '...', message: '...' }, { selector: '...', message: '...' }],
},
```

(Three inline selector objects in array form, no shared constant.)

## Tasks

<task type="auto">
  <name>Task 1: Branch and prep + prior-wave completeness audit</name>
  <files>n/a (branching + read-only audit)</files>
  <read_first>
    - .planning/phases/58-tier-c-design-token-suppression-full-clear/58-01-SUMMARY.md through 58-09-SUMMARY.md (each prior wave SUMMARY)
    - .planning/STATE.md (confirm Phase 58 progress — all prior waves complete)
    - eslint.config.mjs (full file, current state)
  </read_first>
  <action>Create branch `phase-58/wave-7-closure` from `origin/main`. Verify (a) every prior wave SUMMARY records `requirements-completed: [TOKEN-01, TOKEN-02]`; (b) `git log origin/main --grep="chore(58/wave-"` shows ≥9 commits (one per prior wave merge); (c) `grep -rn "gsd-design-token-tier-c-allow\|Phase 51 Tier-C" frontend/src/` returns 0 matches workspace-wide BEFORE editing eslint.config.mjs (this is the pre-condition for safely removing the waiver). If any of these fails, STOP — the failing wave(s) must be patched and merged before Wave 7 can proceed.</action>
  <acceptance_criteria>
    - `git branch --show-current` returns `phase-58/wave-7-closure`
    - All 9 prior wave SUMMARYs (00 manifest, 01–09 execution) record `requirements-completed: [TOKEN-01, TOKEN-02]`
    - `grep -rn "gsd-design-token-tier-c-allow\|Phase 51 Tier-C" frontend/src/` returns 0 matches BEFORE editing eslint.config.mjs
    - Each prior wave's merge commit found in `git log origin/main` history
  </acceptance_criteria>
  <done>Branch exists; pre-condition verified.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the 3 eslint.config.mjs edits (single atomic commit)</name>
  <files>eslint.config.mjs</files>
  <read_first>
    - eslint.config.mjs (lines 1–30, 200–230, 280–310)
    - .planning/phases/58-tier-c-design-token-suppression-full-clear/58-RESEARCH.md (section 8 line-precise removal plan)
  </read_first>
  <action>Apply the 3 edits in this order in a single commit: (1) DELETE the entire `const designTokenSyntaxRestrictions = [ ... ]` block at lines 10–28 (the 3-selector constant); (2) REMOVE the single line `...designTokenSyntaxRestrictions,` at line 217 inside the frontend `no-restricted-syntax` rule's spread (the spread was at the end of the rule's selector array — preserve all other selectors in that array, including the border-l/r RTL selectors at lines 209–215); (3) INLINE the 3 selectors directly into the `bad-design-token.tsx` fixture block at lines 292–297. The fixture's `no-restricted-syntax` rule must become `['warn', { selector: '...', message: '...' }, { selector: '...', message: '...' }, { selector: '...', message: '...' }]` — copy the 3 selectors VERBATIM from the old constant (do NOT alter the regexes; this is what makes Phase 59 POLISH-04 work as a positive-failure CI assertion). Commit message: `chore(58/wave-7): remove Tier-C design-token waiver from eslint.config.mjs`. Commit body enumerates the 3 edits and references SUCCESS CRITERION 2 from ROADMAP.md.</action>
  <acceptance_criteria>
    - 1 atomic commit lands on `phase-58/wave-7-closure`
    - `grep -n "designTokenSyntaxRestrictions" eslint.config.mjs` returns 0 matches
    - `grep -n "tier-c\|Tier-C" eslint.config.mjs` returns 0 matches
    - The frontend `no-restricted-syntax` rule still contains the border-l/r RTL selectors (lines 209–215 in the pre-edit file)
    - The `bad-design-token.tsx` fixture block contains 3 inline selectors with matching messages
  </acceptance_criteria>
  <done>eslint.config.mjs waiver removed; fixture self-contained.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Wave 7 has deleted the Tier-C waiver from eslint.config.mjs. The next step is the canary: `pnpm lint` MUST exit 0 workspace-wide without the waiver. If lint fails, a prior wave missed a Tier-C suppression and Wave 7 cannot proceed.</what-built>
  <how-to-verify>
    From the project root, run in this exact order and report the outcome of each:

    1. `grep -rn "gsd-design-token-tier-c-allow" frontend/src/` — MUST return zero matches (SUCCESS CRITERION 1 from ROADMAP.md). If non-zero, STOP and identify the file; that file's prior wave is incomplete.
    2. `grep -n "designTokenSyntaxRestrictions\|tier-c\|Tier-C" eslint.config.mjs` — MUST return zero matches (SUCCESS CRITERION 2). If non-zero, the constant or its references were not fully removed; revisit Task 2.
    3. `pnpm lint --max-warnings 0` — MUST exit 0 workspace-wide (SUCCESS CRITERION 3). This is the canary.
    4. `pnpm type-check` — MUST exit 0.
    5. `pnpm vitest run` — MUST show 1,241+ passing, zero regressions.

    Expected outcome: all 5 checks pass.

    If check 3 (`pnpm lint`) fails:
    - Identify the failing file(s) from lint output.
    - For each failing file, determine which prior wave (3, 4, 5, 6A, 6B, 6C, or 6D) should have cleared it (cross-reference 58-WAVE-MANIFEST.md).
    - File an issue against the relevant prior wave's SUMMARY and `git revert` the Wave-7 commit (NOT the prior-wave commits).
    - The failing prior wave must be patched and re-merged BEFORE Wave 7 is retried.
    - DO NOT restore the Tier-C waiver to make lint pass — that defeats TOKEN-02.

  </how-to-verify>
  <resume-signal>Type "approved — lint clean" if all 5 checks pass and Wave 7 can complete. Type "revert" if `pnpm lint` failed and Wave 7 must be reverted; include the failing file path so the orchestrator can route the patch back to the relevant prior wave.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Tag phase-58-base + push</name>
  <files>n/a (git tag operations only)</files>
  <read_first>
    - CLAUDE.md "Tag signing setup" section
  </read_first>
  <action>After the human-verify checkpoint confirms lint is clean: (a) confirm `git tag -v phase-49-base` exits 0 (verifies the local SSH signing setup is functional per BUNDLE-06 convention from Phase 53); (b) `git tag phase-58-base` to create the signed tag (configured via `git config --global gpg.format ssh` per CLAUDE.md); (c) `git tag -v phase-58-base` to verify the signature locally — MUST print `Good "git" signature` and exit 0; (d) `git push origin phase-58-base` to publish the tag.</action>
  <acceptance_criteria>
    - `git tag -v phase-58-base` exits 0 with `Good "git" signature`
    - `git push origin phase-58-base` succeeds
    - Tag visible at https://github.com/<owner>/Intl-Dossier-V2.0/releases/tag/phase-58-base
  </acceptance_criteria>
  <done>phase-58-base signed tag published.</done>
</task>

<task type="auto">
  <name>Task 5: Write 58-10-SUMMARY.md + close Phase 58</name>
  <files>.planning/phases/58-tier-c-design-token-suppression-full-clear/58-10-SUMMARY.md</files>
  <read_first>
    - .planning/phases/58-tier-c-design-token-suppression-full-clear/58-02-SUMMARY.md (template + cadence)
    - .planning/STATE.md (Phase 58 progress block — update at end)
  </read_first>
  <action>Author `58-10-SUMMARY.md` using `58-02-SUMMARY.md` as the template. Document the 3 eslint.config.mjs edits + the human-verify canary outcome + the phase-58-base signing/push outcome. Record any `D-58-07-NN` deviations (e.g., if Phase 59 POLISH-04 shipped first and changed the fixture shape — then D-58-07-01 would document the fallback per OQ-58-RES-05 option (b)). Update `.planning/ROADMAP.md` Phase 58 entry to status COMPLETE; update `.planning/STATE.md` Phase 58 row from `Not started` to `Complete` with the date; update `.planning/REQUIREMENTS.md` TOKEN-01 and TOKEN-02 entries from `[ ]` to `[x]` with the closure citation. Commit the SUMMARY + roadmap/state/requirements updates as `chore(58/wave-7): close Phase 58 — Tier-C waiver removed, TOKEN-01 + TOKEN-02 verified`.</action>
  <acceptance_criteria>
    - 58-10-SUMMARY.md exists with the cadence of 58-02-SUMMARY.md
    - The 3-edit list documented + canary outcome + tag-signing outcome
    - `requirements-completed: [TOKEN-01, TOKEN-02]` present
    - ROADMAP.md Phase 58 status updated to COMPLETE
    - STATE.md Phase 58 row updated to Complete with date
    - REQUIREMENTS.md TOKEN-01 + TOKEN-02 marked `[x]` with closure citation
  </acceptance_criteria>
  <done>Phase 58 closed. Milestone v6.4 progresses from 2/14 to 14/14 requirements complete (assuming Phase 57 + Phase 59 ship around the same time).</done>
</task>

## Per-file overrides

eslint.config.mjs is the sole modified file. Per RESEARCH section 8, the 3 edits are line-precise (10–28, 217, 292–297). No manifest override applies because eslint.config.mjs is NOT in the 58-WAVE-MANIFEST.md (the manifest is a `frontend/src` source-file mapping; lint config lives outside it).

## Open questions

- **OQ-58-RES-05** (Wave 7 vs Phase 59 POLISH-04 sequencing) — **RESOLVED**: this plan picks option (a) inline the 3 selectors directly into bad-design-token.tsx, per RESEARCH section 8 recommendation. If Phase 59 POLISH-04 lands first and reshapes the fixture's CI assertion, document the rework as D-58-07-01 in the SUMMARY (it will be a cosmetic shape change at most — the 3 selectors remain).

## Deviations expected

- `D-58-07-01` (only if Phase 59 POLISH-04 reshapes the fixture before Wave 7 ships) — adapt to POLISH-04's fixture shape rather than option (a) inline.
- `D-58-07-EXTRA-NN` — none expected (Wave 7 is a 1-file mechanical edit).

## Threat model

**Risk class:** Lint-rule behavior change. The waiver removal could:

1. Mask a missed suppression in a prior wave (canary `pnpm lint` catches this immediately).
2. Affect the bad-design-token.tsx fixture's positive-failure CI assertion (mitigated by inlining the 3 selectors verbatim — fixture shape preserved).
3. Affect any other ESLint rule downstream of the constant (none — the constant is only referenced at lines 217 and 295 per `grep -n designTokenSyntaxRestrictions eslint.config.mjs`).
   **Threat surface:** None added. No auth, no network, no user-input, no PII handling, no IPC, no file I/O changes — same as prior waves.
   **Mitigations:**
4. Task 1 pre-condition audit: `grep -rn "Phase 51 Tier-C" frontend/src/` MUST return 0 before Task 2.
5. Human-verify checkpoint at Task 3 (blocking): the operator runs all 5 success-criteria checks and approves only when lint clean.
6. Revert procedure documented: if `pnpm lint` fails, `git revert` the Wave-7 commit; patch the failing prior wave; retry.
7. Signed `phase-58-base` tag at Task 4: provides a verifiable cryptographic checkpoint for the phase closure (per BUNDLE-06 / Phase 53 convention).
   **Severity:** Tier-A (refactor + verification gate). Block on: medium (canary failure indicates a prior wave is incomplete; operator intervention required).

## Branch and commit cadence

Branch: `phase-58/wave-7-closure` (forked from `origin/main` after all 6 prior waves have merged).

Commit pattern: 2 commits total — (1) `chore(58/wave-7): remove Tier-C design-token waiver from eslint.config.mjs` (Task 2); (2) `chore(58/wave-7): close Phase 58 — Tier-C waiver removed, TOKEN-01 + TOKEN-02 verified` (Task 5, includes SUMMARY + ROADMAP/STATE/REQUIREMENTS updates).

Signed tag: `phase-58-base` (Task 4) per BUNDLE-06 convention from Phase 53.

## Rollback procedure

If `pnpm lint --max-warnings 0` fails at Task 3 (human-verify checkpoint):

1. Identify the file(s) reported by lint as failing.
2. For each failing file, cross-reference `58-WAVE-MANIFEST.md` to determine which prior wave (3, 4, 5, 6A, 6B, 6C, 6D) was responsible.
3. Run `git revert <wave-7-commit-sha>` on the local branch — this re-adds the waiver, restoring lint to clean.
4. Push the revert: `git push origin phase-58/wave-7-closure`.
5. File an issue against the responsible prior wave's SUMMARY: "Tier-C suppression in `<file>` was missed during wave clearance; patch required before Wave 7 retry."
6. Wait for the prior wave to be patched and merged.
7. Re-fetch: `git fetch origin main` and rebase `phase-58/wave-7-closure` onto the patched main.
8. Re-apply Task 2 edits (the waiver removal); retry Task 3.

**Do NOT restore the Tier-C waiver as a permanent fix** — that defeats TOKEN-02. The waiver must come out cleanly.
