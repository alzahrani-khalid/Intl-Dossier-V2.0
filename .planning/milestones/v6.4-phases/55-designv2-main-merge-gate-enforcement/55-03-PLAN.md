---
phase: 55-designv2-main-merge-gate-enforcement
plan: 03
type: execute
wave: 3
depends_on:
  - 55-02
files_modified:
  - .planning/phases/55-designv2-main-merge-gate-enforcement/protection-before.json
  - .planning/phases/55-designv2-main-merge-gate-enforcement/protection-after.json
  - .planning/phases/55-designv2-main-merge-gate-enforcement/55-03-SUMMARY.md
autonomous: true
requirements:
  - MERGE-02
must_haves:
  truths:
    - 'Branch protection on `main` requires exactly 8 contexts: the original 6 (type-check, Security Scan, Lint, Bundle Size Check (size-limit), Tests (frontend), Tests (backend)) PLUS the 2 new (Design Token Check, react-i18next Factory Check)'
    - 'All security-critical fields (enforce_admins=true, allow_force_pushes=false, allow_deletions=false, block_creations=false) are PRESERVED — not silently dropped by partial PUT'
    - '`protection-before.json` and `protection-after.json` are committed to the phase folder as audit trail (D-16)'
    - 'The PUT call returns 200/201 and a subsequent GET confirms the 8-element contexts array'
    - 'A belt-and-braces verification PR (or live observation of an in-flight PR) confirms the 2 new contexts show as required and report status'
  artifacts:
    - path: '.planning/phases/55-designv2-main-merge-gate-enforcement/protection-before.json'
      provides: 'Snapshot of branch protection BEFORE the update (audit trail per D-16)'
      contains: 'required_status_checks.contexts (6 items)'
    - path: '.planning/phases/55-designv2-main-merge-gate-enforcement/protection-after.json'
      provides: 'PUT-shaped body sent to GitHub (audit trail per D-16; serves as rollback template)'
      contains: 'required_status_checks.contexts (8 items)'
    - path: '.planning/phases/55-designv2-main-merge-gate-enforcement/55-03-SUMMARY.md'
      provides: 'Plan 03 closure record + diff summary + rollback procedure'
  key_links:
    - from: 'protection-after.json `required_status_checks.contexts[]`'
      to: 'ci.yml `jobs.<id>.name:` values from Plan 02'
      via: 'exact string match (Pitfall 3)'
      pattern: 'Design Token Check|react-i18next Factory Check'
    - from: 'protection-after.json'
      to: 'github.com server-side protection state'
      via: 'gh api -X PUT .../branches/main/protection --input'
      pattern: 'gh api -X PUT repos/alzahrani-khalid/Intl-Dossier-V2.0/branches/main/protection'
---

<objective>
Update `main` branch protection to require the 2 new contexts added in Plan 02, bringing the required-context count from 6 to 8. Per D-16, Claude runs `gh api -X PUT`. Per D-13, the new contexts are `Design Token Check` and `react-i18next Factory Check`. Per RESEARCH Pitfall 1+2, the PUT must be a round-trip from the live GET to preserve `enforce_admins`, `allow_force_pushes`, `allow_deletions`, and other booleans (the PUT replaces, does NOT merge).

This plan addresses MERGE-02 (protection half). It MUST follow Plan 02 (Pitfall 4: adding required contexts before jobs exist on main bricks all PRs).

Purpose: Move the gate from "6 contexts required" to "8 contexts required" without corroding `enforce_admins`, force-push protection, or any other invariant.

Output: 8-context branch protection on main; `protection-before.json` + `protection-after.json` committed as audit trail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/55-designv2-main-merge-gate-enforcement/55-CONTEXT.md
@.planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md
@.planning/phases/55-designv2-main-merge-gate-enforcement/55-02-SUMMARY.md

<interfaces>
<!-- GitHub branch protection PUT contract (Pitfall 1+2): -->
<!--   - PUT replaces entire body; partial body silently drops fields -->
<!--   - GET response wraps booleans: { "enforce_admins": { "enabled": true } } -->
<!--   - PUT request expects raw booleans: "enforce_admins": true -->
<!--   - Required top-level fields: required_status_checks, enforce_admins, -->
<!--                                required_pull_request_reviews, restrictions -->
<!--   - Current protection (verified empirically in RESEARCH): -->
<!--       enforce_admins=true, allow_force_pushes=false, allow_deletions=false, -->
<!--       block_creations=false, required_conversation_resolution=false, -->
<!--       required_linear_history.enabled=false, lock_branch.enabled=false, -->
<!--       allow_fork_syncing.enabled=true, required_signatures.enabled=false -->
<!--   - Required PR reviews are NULL on current protection; must send `null` in PUT -->
<!--   - Restrictions are NULL on current protection; must send `null` in PUT -->

<!-- The jq round-trip transform (verified in RESEARCH Code Examples §"Update branch protection"): -->
<!--   {required_status_checks: {strict, contexts: (existing + 2 new)}, -->
<!--    enforce_admins: .enforce_admins.enabled, -->
<!--    required_pull_request_reviews: null, restrictions: null, -->
<!--    required_linear_history: .required_linear_history.enabled, -->
<!--    allow_force_pushes: .allow_force_pushes.enabled, -->
<!--    allow_deletions: .allow_deletions.enabled, -->
<!--    block_creations: .block_creations.enabled, -->
<!--    required_conversation_resolution: .required_conversation_resolution.enabled, -->
<!--    lock_branch: .lock_branch.enabled, -->
<!--    allow_fork_syncing: .allow_fork_syncing.enabled} -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot current protection and run pre-flight safety checks</name>
  <files>
    .planning/phases/55-designv2-main-merge-gate-enforcement/protection-before.json
  </files>
  <read_first>
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Pitfall 1: GitHub branch protection PUT is replace-not-merge"
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Code Examples → Update branch protection (plan 3 — D-16)"
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-02-SUMMARY.md (to confirm job names landed correctly)
  </read_first>
  <action>
    GET current protection and snapshot it to `protection-before.json`. Run `gh api repos/alzahrani-khalid/Intl-Dossier-V2.0/branches/main/protection > .planning/phases/55-designv2-main-merge-gate-enforcement/protection-before.json`. Then assert three invariants on the snapshot before transforming: (a) currently 6 contexts (`length == 6`), (b) `enforce_admins.enabled == true`, (c) `allow_force_pushes.enabled == false`, `allow_deletions.enabled == false`. If any invariant fails, STOP — surface to user; do NOT proceed with PUT. Per Pitfall 4 belt-and-braces, also confirm the 2 new contexts are NOT YET present (`Design Token Check`, `react-i18next Factory Check` should NOT appear in the `contexts[]` array of the snapshot). Also verify both new jobs DO exist in the most-recent main CI run (already proven by Plan 02 but re-verify defensively).
  </action>
  <verify>
    <automated>mkdir -p .planning/phases/55-designv2-main-merge-gate-enforcement && gh api repos/alzahrani-khalid/Intl-Dossier-V2.0/branches/main/protection > .planning/phases/55-designv2-main-merge-gate-enforcement/protection-before.json && jq -e '.required_status_checks.contexts | length == 6' .planning/phases/55-designv2-main-merge-gate-enforcement/protection-before.json && jq -e '.enforce_admins.enabled == true and .allow_force_pushes.enabled == false and .allow_deletions.enabled == false' .planning/phases/55-designv2-main-merge-gate-enforcement/protection-before.json && jq -e '.required_status_checks.contexts | (index("Design Token Check") == null and index("react-i18next Factory Check") == null)' .planning/phases/55-designv2-main-merge-gate-enforcement/protection-before.json && RUN_ID=$(gh api 'repos/alzahrani-khalid/Intl-Dossier-V2.0/actions/workflows/ci.yml/runs?branch=main&per_page=1' --jq '.workflow_runs[0].id') && gh api "repos/alzahrani-khalid/Intl-Dossier-V2.0/actions/runs/$RUN_ID/jobs" --jq '[.jobs[].name] | (index("Design Token Check") != null and index("react-i18next Factory Check") != null)' | grep -qx 'true' && echo "PREFLIGHT INVARIANTS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `protection-before.json` exists
    - Current contexts count is 6 (not 7 or 8 — defensive)
    - `enforce_admins.enabled == true`
    - `allow_force_pushes.enabled == false` and `allow_deletions.enabled == false`
    - Neither new context name is in current contexts[]
    - Both new job names DO appear in the latest main CI run (precondition for Pitfall 4 satisfied)
  </acceptance_criteria>
  <done>Snapshot captured; invariants confirmed; safe to transform.</done>
</task>

<task type="auto">
  <name>Task 2: Build protection-after.json via jq round-trip; assert security invariants preserved</name>
  <files>
    .planning/phases/55-designv2-main-merge-gate-enforcement/protection-after.json
  </files>
  <read_first>
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Pitfall 1: GitHub branch protection PUT is replace-not-merge"
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Pitfall 2: GET-response vs PUT-request JSON shape mismatch"
    - .planning/phases/55-designv2-main-merge-gate-enforcement/protection-before.json
  </read_first>
  <action>
    Run the jq transform from RESEARCH Code Examples §"Update branch protection (plan 3 — D-16)" verbatim — it converts GET shape (`{enabled: bool}` wrappers) to PUT shape (raw booleans), and appends the 2 new contexts. Write result to `protection-after.json`. Then assert ALL security invariants are preserved BEFORE the PUT (T-55-01 mitigation): `enforce_admins == true`, `allow_force_pushes == false`, `allow_deletions == false`, `block_creations == false` (these are raw booleans in the PUT shape). Also assert `required_status_checks.contexts | length == 8` and the 2 new context strings appear verbatim. If ANY assertion fails, STOP — do NOT send the PUT.
  </action>
  <verify>
    <automated>jq '{required_status_checks: {strict: .required_status_checks.strict, contexts: (.required_status_checks.contexts + ["Design Token Check", "react-i18next Factory Check"])}, enforce_admins: .enforce_admins.enabled, required_pull_request_reviews: null, restrictions: null, required_linear_history: .required_linear_history.enabled, allow_force_pushes: .allow_force_pushes.enabled, allow_deletions: .allow_deletions.enabled, block_creations: .block_creations.enabled, required_conversation_resolution: .required_conversation_resolution.enabled, lock_branch: .lock_branch.enabled, allow_fork_syncing: .allow_fork_syncing.enabled}' .planning/phases/55-designv2-main-merge-gate-enforcement/protection-before.json > .planning/phases/55-designv2-main-merge-gate-enforcement/protection-after.json && jq -e '.enforce_admins == true and .allow_force_pushes == false and .allow_deletions == false and .block_creations == false' .planning/phases/55-designv2-main-merge-gate-enforcement/protection-after.json && jq -e '.required_status_checks.contexts | length == 8 and (index("Design Token Check") != null) and (index("react-i18next Factory Check") != null) and (index("type-check") != null) and (index("Security Scan") != null) and (index("Lint") != null) and (index("Bundle Size Check (size-limit)") != null) and (index("Tests (frontend)") != null) and (index("Tests (backend)") != null)' .planning/phases/55-designv2-main-merge-gate-enforcement/protection-after.json && echo "PUT BODY READY + INVARIANTS PRESERVED"</automated>
  </verify>
  <acceptance_criteria>
    - `protection-after.json` exists
    - Contains raw boolean `enforce_admins == true`, `allow_force_pushes == false`, `allow_deletions == false`, `block_creations == false`
    - `required_status_checks.contexts` has length 8 and contains all 6 original + 2 new strings verbatim
    - `required_pull_request_reviews: null` and `restrictions: null` (per Pitfall 1 — required fields)
  </acceptance_criteria>
  <done>PUT body ready and security invariants verified pre-flight.</done>
</task>

<task type="auto">
  <name>Task 3: PUT the protection update and verify server-side state</name>
  <files>
    (server-side mutation; no local file writes — but the PUT response is logged)
  </files>
  <read_first>
    - .planning/phases/55-designv2-main-merge-gate-enforcement/55-RESEARCH.md §"Code Examples → Update branch protection (plan 3 — D-16)" → §"Apply" + §"Verify"
    - .planning/phases/55-designv2-main-merge-gate-enforcement/protection-after.json
  </read_first>
  <action>
    Per D-16, send the PUT: `gh api -X PUT repos/alzahrani-khalid/Intl-Dossier-V2.0/branches/main/protection --input .planning/phases/55-designv2-main-merge-gate-enforcement/protection-after.json`. Capture the response stdout/stderr. Then GET the live protection again and assert: (a) 8 required contexts exactly; (b) the 2 new context names present; (c) `enforce_admins.enabled == true`; (d) `allow_force_pushes.enabled == false`; (e) `allow_deletions.enabled == false`. If ANY post-PUT invariant fails (in particular if `enforce_admins` dropped to false), IMMEDIATELY rollback: `gh api -X PUT ... --input protection-before.json` (after converting it from GET shape to PUT shape using the same jq, but with contexts unchanged) — this is the rollback path. Surface to user.
  </action>
  <verify>
    <automated>gh api -X PUT repos/alzahrani-khalid/Intl-Dossier-V2.0/branches/main/protection --input .planning/phases/55-designv2-main-merge-gate-enforcement/protection-after.json > /dev/null && gh api repos/alzahrani-khalid/Intl-Dossier-V2.0/branches/main/protection > /tmp/protection-verify.json && jq -e '.required_status_checks.contexts | length == 8 and (index("Design Token Check") != null) and (index("react-i18next Factory Check") != null)' /tmp/protection-verify.json && jq -e '.enforce_admins.enabled == true and .allow_force_pushes.enabled == false and .allow_deletions.enabled == false' /tmp/protection-verify.json && echo "PROTECTION UPDATED + SECURITY INVARIANTS INTACT"</automated>
  </verify>
  <acceptance_criteria>
    - PUT returns success (no error from `gh api -X PUT`)
    - Live GET returns `required_status_checks.contexts | length == 8`
    - Both new context strings present in live contexts[]
    - `enforce_admins.enabled == true` server-side (T-55-01 mitigated)
    - `allow_force_pushes.enabled == false` and `allow_deletions.enabled == false` server-side
  </acceptance_criteria>
  <done>Branch protection updated to 8 required contexts; security invariants verified intact.</done>
</task>

<task type="auto">
  <name>Task 4: Commit audit artifacts and write Plan 03 SUMMARY</name>
  <files>
    .planning/phases/55-designv2-main-merge-gate-enforcement/55-03-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/55-designv2-main-merge-gate-enforcement/protection-before.json
    - .planning/phases/55-designv2-main-merge-gate-enforcement/protection-after.json
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Create `55-03-SUMMARY.md`. Sections required: (a) Outcome (PASS); (b) Diff summary: 6 → 8 contexts, list the 2 added; (c) Security invariants preserved: list each (enforce_admins=true, allow_force_pushes=false, allow_deletions=false, block_creations=false) with the values from before/after JSON; (d) D-13 reversal note (this completes the reversal of v6.3 D-09's fold-into-Lint); (e) Rollback procedure — concrete command to restore the 6-context state using `protection-before.json` after converting it to PUT shape (write out the jq transform); (f) Plan 04 unblocked. Commit `protection-before.json`, `protection-after.json`, and `55-03-SUMMARY.md` together with message `docs(55): record branch protection update (6→8 contexts) [Plan 03] [MERGE-02]`.
  </action>
  <verify>
    <automated>test -f .planning/phases/55-designv2-main-merge-gate-enforcement/55-03-SUMMARY.md && grep -q '6.*8\|6 → 8\|6 -> 8' .planning/phases/55-designv2-main-merge-gate-enforcement/55-03-SUMMARY.md && grep -q 'enforce_admins' .planning/phases/55-designv2-main-merge-gate-enforcement/55-03-SUMMARY.md && grep -q -i 'rollback\|restore' .planning/phases/55-designv2-main-merge-gate-enforcement/55-03-SUMMARY.md && grep -q 'D-13\|D-09\|fold' .planning/phases/55-designv2-main-merge-gate-enforcement/55-03-SUMMARY.md && git add .planning/phases/55-designv2-main-merge-gate-enforcement/protection-before.json .planning/phases/55-designv2-main-merge-gate-enforcement/protection-after.json .planning/phases/55-designv2-main-merge-gate-enforcement/55-03-SUMMARY.md && git diff --cached --name-only | grep -q '55-03-SUMMARY.md' && echo "SUMMARY + AUDIT ARTIFACTS STAGED"</automated>
  </verify>
  <acceptance_criteria>
    - `55-03-SUMMARY.md` exists with the 6→8 diff stated
    - Lists each security invariant (enforce_admins, allow_force_pushes, allow_deletions) with before/after values
    - Includes concrete rollback command
    - References D-13 reversing D-09 fold
    - Both protection JSONs staged for commit
  </acceptance_criteria>
  <done>Plan 03 closure recorded; audit trail durable; Plan 04 unblocked.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                                                                                                     |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------- |
| local → GitHub API       | `gh api -X PUT` mutates server-side branch protection; requires `admin:repo_hook` / `repo` scope.                               |
| GET response → PUT body  | JSON shape mismatch (Pitfall 2); `jq` transform crosses this boundary.                                                          |
| user → server protection | All clients (including admins) bound by `enforce_admins=true` so the protection itself is the enforcement layer (the boundary). |

## STRIDE Threat Register

| Threat ID | Category               | Component                       | Disposition | Mitigation Plan                                                                                                                                                                                                     |
| --------- | ---------------------- | ------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-55-01   | Tampering              | branch protection PUT roundtrip | mitigate    | Task 1 snapshots BEFORE; Task 2 builds PUT body via verified jq transform; Task 2 asserts security booleans BEFORE PUT; Task 3 asserts them server-side AFTER PUT. Rollback path documented if any assertion fails. |
| T-55-PB   | Tampering              | PUT replaces partial body       | mitigate    | The jq transform explicitly carries forward every relevant field from the GET (per Pitfall 1). Task 2 invariants confirm none silently dropped to default-permissive values.                                        |
| T-55-CN   | Tampering              | required-context name typo      | mitigate    | Task 2 asserts both new context strings appear verbatim AND all 6 original strings still present. Plan 02 already wrote `name:` values matching these strings.                                                      |
| T-55-PD   | Elevation of Privilege | admin bypasses protection       | mitigate    | `enforce_admins == true` re-verified post-PUT. No admin bypass merges are part of this plan.                                                                                                                        |
| T-55-RB   | Repudiation            | rollback procedure missing      | mitigate    | Task 4 SUMMARY includes a concrete rollback command; `protection-before.json` committed so the rollback is one jq + one PUT.                                                                                        |
| T-55-SC   | Tampering              | npm/pip/cargo installs          | accept      | No package installs in this plan.                                                                                                                                                                                   |

</threat_model>

<verification>
Plan 03 closes when:
1. Live `gh api .../branches/main/protection` shows `required_status_checks.contexts | length == 8`.
2. Live `enforce_admins.enabled == true`, `allow_force_pushes.enabled == false`, `allow_deletions.enabled == false`.
3. `protection-before.json` + `protection-after.json` + `55-03-SUMMARY.md` committed.
4. SUMMARY includes the rollback command.
</verification>

<success_criteria>
ROADMAP Phase 55 criterion #4 (the 4 v6.3-introduced gate contexts appear as required on `main` branch protection) FULLY SATISFIED, and exceeded — D-13 raised the count from the original 6 to 8 with explicit failure-attribution clarity.
</success_criteria>

<output>
Create `.planning/phases/55-designv2-main-merge-gate-enforcement/55-03-SUMMARY.md` with diff summary, security invariants check, and rollback procedure. Commit alongside `protection-before.json` and `protection-after.json`.
</output>
