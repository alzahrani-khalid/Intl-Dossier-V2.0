---
phase: 59-cosmetic-ci-gap-closure
plan: 03
type: execute
wave: 2
depends_on: ['59-01', '59-02']
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [POLISH-01, POLISH-02, POLISH-03, POLISH-04]
user_setup: []

must_haves:
  truths:
    - 'A single PR carries all four POLISH items (not split docs-vs-CI)'
    - 'All 8 required main contexts pass on the PR before merge; no admin bypass, no force-push to main, no --no-verify'
    - 'After merge, an SSH-signed annotated phase-59-base tag is pushed to origin'
    - 'git tag -v phase-59-base exits 0 with Good "git" signature'
    - 'ROADMAP traceability + Phase-59 checkboxes and STATE reflect Phase 59 shipped'
  artifacts:
    - path: '.planning/phases/59-cosmetic-ci-gap-closure/59-03-SUMMARY.md'
      provides: 'Closure record: PR URL, merge SHA, phase-59-base tag verification'
      contains: 'phase-59-base'
  key_links:
    - from: 'phase-59-base annotated SSH-signed tag'
      to: 'merge commit on main'
      via: 'git tag -s phase-59-base on the post-merge main HEAD'
      pattern: 'phase-59-base'
---

<objective>
Ship Phase 59 as a single PR carrying all four POLISH items (D-10), pass the 8 required
main contexts, merge without bypass, then close with an SSH-signed annotated phase-59-base
tag pushed to origin (D-11). Finalize ROADMAP/STATE.

Purpose: One cohesive delivery for four small items; close on the same signed-tag convention
the phase itself audited (POLISH-01 is about tag provenance, so phase-59-base must be signed
and verifiable).

Output: A merged PR, a verified phase-59-base tag on origin, and updated ROADMAP/STATE.

Non-autonomous: PR merge into protected main and the signed-tag push are operations that
require a green CI gate and human-confirmed merge; the executor must not bypass branch
protection or admin gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-cosmetic-ci-gap-closure/59-CONTEXT.md
@.planning/phases/59-cosmetic-ci-gap-closure/59-01-SUMMARY.md
@.planning/phases/59-cosmetic-ci-gap-closure/59-02-SUMMARY.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Authoritative facts the executor needs — do not re-derive by exploration. -->

CLAUDE.md §"Tag signing setup" — phase-59-base must be an annotated SSH-signed tag;
`git tag -v phase-59-base` MUST exit 0 with `Good "git" signature`. Use the same
user-local signing config already verified in Plan 02.

main branch protection (Phase 55 D-13): 8 required contexts including `Design Token Check`
and `react-i18next Factory Check`. enforce_admins=true. NO admin bypass, NO force-push to
main, NO `--no-verify` on any commit/merge/tag (D-12 / repo convention).

Branch base: branch off the current `main` HEAD (already past phase-58-base). Suggested
branch name (Claude's discretion, follow v6.4 convention): `phase-59/cosmetic-ci-gap-closure`.

Commit granularity (Claude's discretion): prior phases use per-item or grouped docs commits;
follow the shape seen in `git log phase-58-base..HEAD`. Keep commits scoped to the four
POLISH items; do NOT commit the neutralized flip-test fixtures (they were reverted in Plan 01).

phase-59-base tag body shape (from CONTEXT §specifics):
"Phase 59 — Cosmetic + CI gap closure. POLISH-01..04 satisfied: Phase 53 SUMMARY/VERIFICATION
wording refreshed (origin tags verified), TweaksDrawer comment drift removed, 51-VALIDATION
frontmatter passed, bad-design-token + bad-vi-mock positive-failure CI assertions verified
(lint-based, Phase 55 jobs) and ROADMAP wording reconciled."

ROADMAP/STATE finalization (prose + tracking edits):

- ROADMAP.md: mark POLISH-01..04 checkboxes complete in the §"Cosmetic + CI gap closure"
  requirements list; update the traceability table rows POLISH-01..04 from Pending →
  ✓ Complete (date, plan ref). Update Phase 59 "Plans: TBD" to the actual plan count (3).
- STATE.md: advance Current Position to Phase 59 complete; update progress counts and
  Carryover Tech Debt Status (the four POLISH lines now closed).
- These are content/tracking edits. Where a registered gsd-sdk handler exists for a tracking
  mutation (e.g. progress table), prefer it over raw edits per repo anti-pattern #15; for the
  Phase-59 free-text criterion/plan-count prose, a direct edit is fine. Do NOT re-touch the
  POLISH-04 criterion-4 wording already reconciled in Plan 01.

POLISH-01 BLOCKED handoff: if Plan 02 reported POLISH-01 BLOCKED (user declined force-push of a
divergent origin tag), do NOT mark POLISH-01 complete in ROADMAP/traceability and do NOT
close the phase as fully shipped — surface that POLISH-01 remains blocked and ship the other
three under the single PR, noting POLISH-01 status in the PR body and SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize ROADMAP + STATE tracking for Phase 59</name>
  <read_first>
    - .planning/phases/59-cosmetic-ci-gap-closure/59-01-SUMMARY.md and 59-02-SUMMARY.md — confirm which POLISH items closed vs blocked
    - .planning/ROADMAP.md §"Cosmetic + CI gap closure" + §"Traceability" + §"Phase 59" — checkboxes, table rows, plan count
    - .planning/STATE.md §"Current Position" + §"Carryover Tech Debt Status" — position + carryover lines to close
  </read_first>
  <action>
    In ROADMAP.md: tick POLISH-01, POLISH-02, POLISH-03, POLISH-04 checkboxes in the
    §"Cosmetic + CI gap closure" list (lines 38-41) — but only tick POLISH-01 if Plan 02
    reported it CLOSED (not BLOCKED). In §"Traceability", change the POLISH-01..04 rows
    (lines 74-77) from `Pending` to `✓ Complete (2026-05-24, Phase 59)` with a brief plan ref;
    again hold POLISH-01 at Pending/Blocked if Plan 02 was BLOCKED. Update §"Phase 59"
    `Plans: TBD` to `Plans: 3 plans`. Do NOT re-edit the criterion-4 prose (already done in
    Plan 01). In STATE.md: set Current Position to Phase 59 complete (or "Phase 59 — 3/3 plans,
    POLISH-01 blocked" if applicable), bump progress counts, and update the four Carryover Tech
    Debt Status lines (82-85) to reflect closure.
  </action>
  <acceptance_criteria>
    - ROADMAP.md §"Cosmetic + CI gap closure" shows POLISH-02/03/04 as ticked `[x]` (and POLISH-01 ticked iff Plan 02 CLOSED)
    - ROADMAP.md traceability rows POLISH-02/03/04 read "✓ Complete" (POLISH-01 matches Plan 02 outcome)
    - ROADMAP.md §"Phase 59" no longer reads "Plans: TBD"
    - STATE.md Current Position references Phase 59
    - git diff confined to .planning/ROADMAP.md and .planning/STATE.md
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Plans.*3 plans" .planning/ROADMAP.md && grep -qi "phase 59" .planning/STATE.md && git diff --name-only | grep -qE "ROADMAP.md|STATE.md"</automated>
  </verify>
  <done>ROADMAP checkboxes/traceability/plan-count and STATE position/carryover reflect Phase 59's actual outcome (respecting any POLISH-01 BLOCKED status).</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Single-PR merge into protected main (8 contexts green, no bypass)</name>
  <action>Open ONE PR carrying all four POLISH items, push the phase branch, and wait for human-confirmed merge after all 8 required contexts pass. No admin bypass, no force-push to main, no --no-verify. Verify the diff excludes neutralized fixtures and ci.yml changes before merge.</action>
  <what-built>
    All Phase 59 edits across Plans 01-03 are committed on the phase branch
    (`phase-59/cosmetic-ci-gap-closure`, branched off current main HEAD). The executor opened a
    SINGLE PR (D-10) carrying all four POLISH items and pushed the branch. This checkpoint
    governs the merge into protected `main`, which requires all 8 required contexts green and
    a human-confirmed merge — no admin bypass, no force-push, no `--no-verify`.
  </what-built>
  <how-to-verify>
    1. Open the PR (URL provided by the executor). Confirm the diff is exactly: TweaksDrawer
       comment removed; 51-VALIDATION status: passed; ROADMAP + REQUIREMENTS POLISH-04 wording
       reconciled to lint/ESLint; 53-03-SUMMARY PASS + 53-VERIFICATION BUNDLE-06 verified
       (present iff POLISH-01 CLOSED); ROADMAP/STATE tracking. No neutralized fixtures, no
       ci.yml changes.
    2. Confirm all 8 required CI contexts are green on the PR — including `Design Token Check`
       and `react-i18next Factory Check` (these prove the fixtures still fail as expected,
       since those jobs were unchanged).
    3. Approve the merge (squash or merge per repo convention). The executor will NOT bypass
       branch protection or use admin override.
  </how-to-verify>
  <resume-signal>Type "approved — merged, merge SHA <sha>" once the PR is merged into main with all 8 contexts green, or describe blocking issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Cut + push + verify the SSH-signed phase-59-base tag</name>
  <read_first>
    - CLAUDE.md §"Tag signing setup" — `git tag -s` / `git tag -v` contract and allowed_signers
    - .planning/phases/59-cosmetic-ci-gap-closure/59-CONTEXT.md §specifics — the phase-59-base tag body shape
  </read_first>
  <action>
    PRECONDITION: the PR is merged into main (Task 2 checkpoint approved). Fetch origin and
    check out the post-merge main HEAD. Create an annotated SSH-signed tag with
    `git tag -s phase-59-base <merge-sha>` using the tag body from CONTEXT §specifics (Phase 59
    — Cosmetic + CI gap closure; POLISH-01..04 satisfied summary). Verify locally with
    `git tag -v phase-59-base` (must exit 0 with `Good "git" signature`). Push the tag with
    `git push origin phase-59-base` (non-destructive new ref; NO --force, NO --no-verify).
    Re-verify against origin: `git ls-remote --tags origin phase-59-base` resolves to the same
    object, and `git tag -v phase-59-base` still passes. If Plan 02 reported POLISH-01 BLOCKED,
    note in the tag body / SUMMARY that POLISH-01 remains open and the other three shipped.
  </action>
  <acceptance_criteria>
    - git tag -v phase-59-base exits 0 and output contains 'Good "git" signature'
    - git ls-remote --tags origin phase-59-base returns a ref (tag pushed to origin)
    - The tag is annotated (git cat-file -t phase-59-base returns 'tag', not 'commit')
    - No --force or --no-verify flag used on the tag push
  </acceptance_criteria>
  <verify>
    <automated>git tag -v phase-59-base 2>&1 | grep -q 'Good "git" signature' && git ls-remote --tags origin phase-59-base | grep -q phase-59-base && [ "$(git cat-file -t phase-59-base)" = "tag" ]</automated>
  </verify>
  <done>phase-59-base is an annotated SSH-signed tag on origin that verifies clean via git tag -v.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                                                        |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| phase branch → protected main | Merge crosses into protected `main` (8 required contexts, enforce_admins=true). Bypass would weaken the live gate. |
| local repo → origin (shared)  | The phase-59-base tag becomes shared provenance; it must be signed and verifiable.                                 |

## STRIDE Threat Register

| Threat ID | Category               | Component                              | Disposition | Mitigation Plan                                                                                                                         |
| --------- | ---------------------- | -------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| T-59-07   | Elevation of Privilege | main branch protection                 | mitigate    | Merge gated behind a blocking-human checkpoint requiring all 8 contexts green; no admin bypass, no force-push, no `--no-verify` (D-12). |
| T-59-08   | Spoofing / Repudiation | phase-59-base tag authenticity         | mitigate    | Tag is SSH-signed and verified with `git tag -v` (exit 0 + `Good "git" signature`) before and after push (D-11).                        |
| T-59-09   | Tampering              | flip-test fixtures leaking into the PR | mitigate    | Fixtures reverted in Plan 01; PR-review checkpoint step explicitly checks the diff excludes neutralized fixtures and ci.yml changes.    |
| T-59-SC   | Tampering              | npm/pip/cargo installs                 | accept      | No package installs in this plan.                                                                                                       |

</threat_model>

<verification>
- Single PR carries all four POLISH items; 8 required contexts green at merge.
- phase-59-base annotated SSH-signed tag on origin verifies clean.
- ROADMAP/STATE reflect Phase 59 outcome (respecting any POLISH-01 BLOCKED status).
</verification>

<success_criteria>

- D-10 satisfied: one PR for all four POLISH items.
- D-11 satisfied: phase-59-base signed tag pushed and `git tag -v` exits 0 with Good signature.
- D-12 satisfied: no --no-verify, no admin bypass, no force-push to main.
- Phase 59 closed in tracking (or POLISH-01 explicitly carried as blocked if user declined the force-push).
  </success_criteria>

<output>
Create `.planning/phases/59-cosmetic-ci-gap-closure/59-03-SUMMARY.md` when done.
Record the PR URL, the merge SHA, the 8-context green confirmation, and the phase-59-base
`git tag -v` result. If POLISH-01 shipped BLOCKED, state that clearly.
</output>
</output>
