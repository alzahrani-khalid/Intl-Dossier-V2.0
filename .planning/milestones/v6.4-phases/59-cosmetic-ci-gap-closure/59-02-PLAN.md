---
phase: 59-cosmetic-ci-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/53-bundle-tightening-tag-provenance/53-03-SUMMARY.md
  - .planning/phases/53-bundle-tightening-tag-provenance/53-VERIFICATION.md
autonomous: false
requirements: [POLISH-01]
user_setup: []

must_haves:
  truths:
    - 'Each origin tag phase-47/48/49-base resolves to the SAME commit SHA as its local annotated tag'
    - 'git tag -v for all three phase-4{7,8,9}-base exits 0 with Good "git" signature'
    - '53-03-SUMMARY.md status/verdict read PASS (only after the three tags verify)'
    - '53-VERIFICATION.md records BUNDLE-06 as verified (not verified-local-only)'
    - 'If any origin tag is missing or divergent, the wording is NOT flipped and POLISH-01 is reported BLOCKED until the user resolves the push'
  artifacts:
    - path: '.planning/phases/53-bundle-tightening-tag-provenance/53-03-SUMMARY.md'
      provides: 'Phase 53 Plan 03 summary with PASS verdict once tags verified'
      contains: 'verdict: PASS'
    - path: '.planning/phases/53-bundle-tightening-tag-provenance/53-VERIFICATION.md'
      provides: 'BUNDLE-06 verified record'
      contains: 'verified'
    - path: '.planning/phases/59-cosmetic-ci-gap-closure/59-02-SUMMARY.md'
      provides: 'Origin-tag verification evidence (SHAs + signature results)'
      contains: 'phase-47-base'
  key_links:
    - from: 'git ls-remote --tags origin phase-47-base'
      to: 'git rev-parse phase-47-base^{commit}'
      via: 'commit-SHA equality after dereferencing the remote tag object'
      pattern: 'phase-4[789]-base'
---

<objective>
Refresh the Phase 53 deferral paperwork (POLISH-01) — but ONLY after proving the three
SSH-signed origin tags (phase-47-base, phase-48-base, phase-49-base) match their local
annotated tags by commit SHA and verify their signatures. The original D-26 deferral existed
because those tags were created locally but not confirmed on origin; this plan closes that
evidence gap, then flips PASS-WITH-DEFERRAL → PASS and verified-local-only → verified.

Purpose: Never mark BUNDLE-06 verified on a false premise. The wording flip is gated behind
hard, observable tag-provenance evidence.

Output: Two refreshed Phase 53 docs (gated), plus a SUMMARY capturing the per-tag SHA + signature
verification evidence. This plan is non-autonomous because the D-07 force-push contingency
requires explicit user confirmation if origin holds a divergent tag object.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-cosmetic-ci-gap-closure/59-CONTEXT.md
@.planning/phases/53-bundle-tightening-tag-provenance/53-03-SUMMARY.md
@.planning/phases/53-bundle-tightening-tag-provenance/53-VERIFICATION.md

<interfaces>
<!-- Authoritative facts the executor needs — do not re-derive by exploration. -->

CLAUDE.md §"Tag signing setup" — `git tag -v <name>` MUST exit 0 and print `Good "git" signature`.
SSH signing config is user-local (~/.gitconfig, ~/.ssh/allowed_signers); already configured
per machine. If `git tag -v` fails because allowed_signers is unconfigured (not a real
signature problem), surface that as a setup blocker — do NOT flip wording.

POLISH-01 edit targets (gated):
53-03-SUMMARY.md frontmatter — `status: PASS-WITH-DEFERRAL` (line 4) and
`verdict: PASS-WITH-DEFERRAL` (line 5) → both become `PASS`. The D-26 deviation block
(lines 8+) describes the force-push deferral; leave the historical narrative intact unless
it directly contradicts the new PASS verdict — if it does, update only the status-bearing
phrase, not the history.
53-VERIFICATION.md — BUNDLE-06 `status: verified-local-only` (line 12) → `verified`;
the BUNDLE-06 row in the results table (line 98) `✓ verified-local-only ... D-26 force-push
    deferred` → `✓ verified` with the deferral note updated to reflect origin tags confirmed;
the deferral sentence at line 25 ("remote state needs three push commands") updated to note
remote state confirmed.

Origin-tag verification recipe (D-06 — MUST pass for all three before any wording edit):
Step 0 (robustness — DO THIS FIRST): the origin tag objects MUST be in the local object
store before any `^{commit}` dereference, or the gate produces a FALSE mismatch. Run
`git fetch --tags origin refs/tags/phase-47-base refs/tags/phase-48-base refs/tags/phase-49-base`
(or `git fetch --tags origin`) up front. Then, per tag:
For T in phase-47-base phase-48-base phase-49-base:
REMOTE_commit = the peeled commit SHA. PREFER parsing the `<T>^{}` peeled line that
`git ls-remote --tags origin <T>` emits directly (that line IS the commit SHA — no local
dereference needed). Only fall back to `git rev-parse <remote-object>^{commit}` after the
fetch in Step 0 has guaranteed the object is local.
LOCAL_commit = `git rev-parse <T>^{commit}`.
Require REMOTE_commit == LOCAL_commit.
Require `git tag -v <T>` exit 0 with `Good "git" signature`.

D-07 contingency (force-push is the only branch that makes this plan non-autonomous):

- Tag ABSENT on origin → push the re-issued annotated tag with `git push origin <T>`
  (non-destructive fast-forward of a new ref; allowed).
- Tag PRESENT on origin but pointing at a DIFFERENT / divergent object (e.g. an old
  lightweight tag) → the push needs `git push --force origin <T>`. This is shared-state
  destructive. STOP. Do NOT run it. Surface a checkpoint:human-action describing the
  divergence and the exact force-push command, and wait for explicit user approval.
  If the user declines, leave 53-VERIFICATION BUNDLE-06 as verified-local-only, leave
  53-03-SUMMARY as PASS-WITH-DEFERRAL, and report POLISH-01 as BLOCKED (not closed).
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Verify origin tags match local + signatures valid (the D-06 gate)</name>
  <read_first>
    - CLAUDE.md §"Tag signing setup" — the `git tag -v` contract and allowed_signers requirement
    - .planning/phases/53-bundle-tightening-tag-provenance/53-03-SUMMARY.md — the D-26 deferral provenance (what was deferred and why)
  </read_first>
  <action>
    FIRST, make the gate robust regardless of prior fetch state: run
    `git fetch --tags origin refs/tags/phase-47-base refs/tags/phase-48-base refs/tags/phase-49-base`
    (fall back to `git fetch --tags origin` if the explicit refspec form is rejected) so every
    origin tag object is in the local object store before any dereference. Without this, a tag
    object absent from the local store makes `git rev-parse <object>^{commit}` fail and would
    wrongly route to the D-07 force-push checkpoint.
    Then, for each of phase-47-base, phase-48-base, phase-49-base, gather and record three facts:
    (1) the LOCAL commit SHA via `git rev-parse <T>^{commit}`;
    (2) the ORIGIN commit SHA — run `git ls-remote --tags origin <T>` and PREFER the peeled
        `<T>^{}` line it emits (that line is already the commit SHA, so no local dereference is
        needed and the gate is robust even if the tag object were not fetched); only if no
        peeled line is present, fall back to `git rev-parse <object>^{commit}` (safe now that
        Step 0's fetch guarantees the object is local);
    (3) the signature result via `git tag -v <T>` — capture exit code and whether the output
        contains `Good "git" signature`.
    Build a 3-row evidence table (tag | local SHA | origin SHA | SHAs match? | signature good?).
    The GATE PASSES only if all three rows show matching SHAs AND a good signature.
    Write this evidence table into 59-02-SUMMARY.md. If any row fails the SHA match, hand off
    to Task 3 (do NOT proceed to Task 2). If `git tag -v` fails purely because allowed_signers
    is not configured on this machine, stop and surface a setup blocker rather than treating it
    as a divergent tag.
  </action>
  <acceptance_criteria>
    - git fetch --tags origin (refs phase-4{7,8,9}-base) completed before any dereference (no false mismatch from a missing local object)
    - git rev-parse phase-47-base^{commit}, phase-48-base^{commit}, phase-49-base^{commit} each return a 40-char SHA
    - git ls-remote --tags origin phase-47-base (and 48, 49) each return a ref whose peeled `^{}` commit SHA equals the corresponding local SHA
    - git tag -v phase-47-base exits 0 and stdout/stderr contains 'Good "git" signature' (and likewise for 48, 49)
    - 59-02-SUMMARY.md contains a 3-row evidence table with local SHA, origin SHA, match result, and signature result for each tag
  </acceptance_criteria>
  <verify>
    <automated>git fetch --tags origin refs/tags/phase-47-base refs/tags/phase-48-base refs/tags/phase-49-base 2>/dev/null || git fetch --tags origin; for T in phase-47-base phase-48-base phase-49-base; do L=$(git rev-parse "$T^{commit}"); R=$(git ls-remote --tags origin "$T" | awk '/\^\{\}$/{print $1}'); [ -n "$R" ] || R=$(git ls-remote --tags origin "$T" | awk 'END{print $1}'); RC=$(git rev-parse "$R^{commit}" 2>/dev/null || echo "$R"); [ "$L" = "$RC" ] || { echo "SHA MISMATCH $T: local=$L origin=$RC"; exit 1; }; git tag -v "$T" 2>&1 | grep -q 'Good "git" signature' || { echo "BAD SIG $T"; exit 1; }; done; echo "ALL THREE TAGS VERIFIED"</automated>
  </verify>
  <done>All three origin tags confirmed to match local commit SHAs and carry valid SSH signatures; evidence table recorded. Gate is open for Task 2 — OR a mismatch was found and Task 3 handles it.</done>
</task>

<task type="auto">
  <name>Task 2: GATED — flip Phase 53 wording PASS-WITH-DEFERRAL → PASS and verified-local-only → verified</name>
  <read_first>
    - .planning/phases/53-bundle-tightening-tag-provenance/53-03-SUMMARY.md (frontmatter lines 1-15) — status/verdict to flip and the D-26 deviation block
    - .planning/phases/53-bundle-tightening-tag-provenance/53-VERIFICATION.md (lines 11-25 and the BUNDLE-06 table row at line 98)
    - .planning/phases/59-cosmetic-ci-gap-closure/59-02-SUMMARY.md — the Task 1 evidence table (the precondition for this edit)
  </read_first>
  <action>
    PRECONDITION: only run this task if Task 1's gate PASSED for all three tags. In
    53-03-SUMMARY.md change `status: PASS-WITH-DEFERRAL` (line 4) → `status: PASS` and
    `verdict: PASS-WITH-DEFERRAL` (line 5) → `verdict: PASS`. In 53-VERIFICATION.md change the
    BUNDLE-06 `status: verified-local-only` (line 12) → `status: verified`; update the BUNDLE-06
    results-table row (line 98) from `✓ verified-local-only ... D-26 force-push deferred` to
    `✓ verified` with the note updated to "origin tags confirmed (SHA + signature) Phase 59";
    update the deferral sentence at line 25 so it no longer says remote needs push commands —
    instead note remote state confirmed in Phase 59 POLISH-01. Do NOT rewrite the historical
    D-26 narrative beyond what is needed to make the verdict self-consistent. Make no edits to
    any other file.
  </action>
  <acceptance_criteria>
    - grep -c "PASS-WITH-DEFERRAL" .planning/phases/53-bundle-tightening-tag-provenance/53-03-SUMMARY.md returns 0 in the frontmatter status/verdict lines; grep -c "verdict: PASS" returns 1 and "status: PASS" returns 1
    - grep -c "verified-local-only" .planning/phases/53-bundle-tightening-tag-provenance/53-VERIFICATION.md returns 0
    - grep -c "verified" applied to the BUNDLE-06 status line of 53-VERIFICATION.md returns 1 (status: verified)
    - git diff touches ONLY 53-03-SUMMARY.md and 53-VERIFICATION.md
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "verified-local-only" .planning/phases/53-bundle-tightening-tag-provenance/53-VERIFICATION.md && grep -q "^status: PASS$" .planning/phases/53-bundle-tightening-tag-provenance/53-03-SUMMARY.md && grep -q "^verdict: PASS$" .planning/phases/53-bundle-tightening-tag-provenance/53-03-SUMMARY.md</automated>
  </verify>
  <done>Phase 53 SUMMARY reads PASS and VERIFICATION records BUNDLE-06 verified — flipped only because Task 1's gate passed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: D-07 force-push contingency — gated origin-tag reconciliation</name>
  <action>Only engages if Task 1.'s gate did NOT pass for all three tags. Present the divergence (absent vs divergent origin object), the exact push/force-push command, and wait for explicit user approval before any push. Never force-push automatically. If declined, report POLISH-01 BLOCKED and leave the Phase 53 wording unflipped.</action>
  <what-built>
    Task 1 ran the origin-tag verification. This checkpoint exists ONLY for the D-07
    divergence contingency: if Task 1 found an origin tag that is ABSENT or points at a
    DIVERGENT object, a force-push may be required to reconcile it before the wording can
    be flipped. A force-push to a shared tag is destructive and must never run automatically.
  </what-built>
  <how-to-verify>
    If Task 1 reported ALL THREE TAGS VERIFIED, this checkpoint is a no-op — approve to skip
    (no push needed; Task 2 already flipped the wording). Otherwise the executor will present:
    1. Which tag(s) are absent vs divergent on origin, with the local SHA and the origin object.
    2. For an ABSENT tag: the non-destructive `git push origin <T>` it intends to run (safe).
    3. For a DIVERGENT tag: the exact `git push --force origin <T>` command — DO NOT let it run
       without your explicit "approved: force-push <T>".
    If you decline the force-push, the executor will leave BUNDLE-06 as verified-local-only,
    leave 53-03-SUMMARY as PASS-WITH-DEFERRAL, and report POLISH-01 as BLOCKED (not closed),
    with the divergence recorded in 59-02-SUMMARY.md.
  </how-to-verify>
  <resume-signal>Type "approved: no push needed", "approved: push <T>" (absent tag), "approved: force-push <T>" (divergent tag, explicit), or "declined — leave BUNDLE-06 verified-local-only"</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                                                                                            |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| local repo → origin (shared) | Tag refs on origin are shared state. Force-pushing a tag rewrites provenance other clones depend on. This is the only real boundary this plan touches. |

## STRIDE Threat Register

| Threat ID | Category                | Component                                      | Disposition | Mitigation Plan                                                                                                                                                                |
| --------- | ----------------------- | ---------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-59-04   | Tampering / Repudiation | origin tag refs phase-47/48/49-base            | mitigate    | Force-push is gated behind a blocking-human checkpoint (D-07); never runs automatically. Absent-tag push is non-destructive and allowed.                                       |
| T-59-05   | Integrity (false PASS)  | 53-03-SUMMARY.md / 53-VERIFICATION.md verdicts | mitigate    | Wording flip is hard-gated on Task 1's SHA-match + valid-signature evidence (D-06). If the gate fails, the verdict stays PASS-WITH-DEFERRAL and POLISH-01 is reported BLOCKED. |
| T-59-06   | Spoofing                | SSH tag signatures                             | mitigate    | `git tag -v` against the user-local allowed_signers must print `Good "git" signature`; a missing allowed_signers config is surfaced as a setup blocker, not silently passed.   |
| T-59-SC   | Tampering               | npm/pip/cargo installs                         | accept      | No package installs in this plan.                                                                                                                                              |

</threat_model>

<verification>
- POLISH-01 CLOSED path: all three origin tags match local SHAs + valid signatures → wording flipped to PASS / verified.
- POLISH-01 BLOCKED path: a tag is absent/divergent and the user declines force-push → wording NOT flipped; reported BLOCKED with divergence recorded.
- git diff confined to the two Phase 53 docs (plus the new 59-02-SUMMARY.md).
</verification>

<success_criteria>

- ROADMAP Success Criterion 1 satisfied (53-03-SUMMARY reads PASS; 53-VERIFICATION BUNDLE-06 verified) — IFF the gate passed.
- No force-push to a divergent origin tag without explicit user approval.
- Verification evidence (per-tag SHAs + signature results) captured in 59-02-SUMMARY.md.
  </success_criteria>

<output>
Create `.planning/phases/59-cosmetic-ci-gap-closure/59-02-SUMMARY.md` when done.
Record the 3-row origin-tag evidence table (local SHA / origin SHA / match / signature) and
the resolution path taken (CLOSED via verified gate, or BLOCKED with divergence + user decision).
</output>
</output>
