---
phase: 56-rls-closure-last-typed-shim-retirement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/domains/misc/hooks/useStakeholderTimeline.ts
  - frontend/src/hooks/useStakeholderTimeline.ts
  - frontend/src/components/stakeholder-timeline/StakeholderInteractionTimeline.tsx
  - frontend/src/domains/misc/hooks/useStakeholderTimeline.test.ts
  - frontend/src/components/stakeholder-timeline/StakeholderInteractionTimeline.test.tsx
autonomous: true
requirements:
  - TYPE-05
tags:
  - typescript
  - react-query
  - shim-retirement
  - test-coverage
must_haves:
  truths:
    - '`useStakeholderInteractionMutations` declares an explicit `UseStakeholderInteractionMutationsReturn` return type at source (no `any`, no `Promise.resolve({success:true})`)'
    - 'Consumer `StakeholderInteractionTimeline.tsx` destructures the hook with no `as unknown as` cast and no local shim interface'
    - 'The deprecated re-export file `frontend/src/hooks/useStakeholderTimeline.ts` is deleted; the consumer imports from `@/domains/misc` directly'
    - "Calling `createInteraction(...)` or `createAnnotation(...)` at runtime throws `Error('useStakeholderInteractionMutations: not implemented — wire to real backend in future stakeholder-interactions feature phase')`"
    - '`pnpm type-check` exits 0 across both workspaces'
    - "`grep -rn 'useStakeholderInteractionMutations' frontend/src | grep ' as '` returns empty"
    - 'Annotated SSH-signed `phase-56-base` tag exists on the phase-completion commit and `git tag -v phase-56-base` exits 0'
  artifacts:
    - path: 'frontend/src/domains/misc/hooks/useStakeholderTimeline.ts'
      provides: 'Retyped `useStakeholderInteractionMutations` returning `UseStakeholderInteractionMutationsReturn` with `createInteraction`, `isCreating`, `createAnnotation`, `isAnnotating`'
      exports: ['useStakeholderInteractionMutations', 'UseStakeholderInteractionMutationsReturn']
    - path: 'frontend/src/domains/misc/hooks/useStakeholderTimeline.test.ts'
      provides: 'Vitest unit tests for the retyped hook (typed return surface, mutationFn throws not-implemented, no cast inside body)'
      min_lines: 60
    - path: 'frontend/src/components/stakeholder-timeline/StakeholderInteractionTimeline.test.tsx'
      provides: 'Vitest consumer-destructure assertion: the four destructured hook members are typed/present (D-56-11 testing-precedent narrative)'
      min_lines: 30
    - path: 'frontend/src/components/stakeholder-timeline/StakeholderInteractionTimeline.tsx'
      provides: 'Consumer with no `as unknown as` cast, no `StakeholderInteractionMutationsShim` interface, import from `@/domains/misc`'
      contains: "from '@/domains/misc'"
  key_links:
    - from: 'frontend/src/components/stakeholder-timeline/StakeholderInteractionTimeline.tsx'
      to: 'frontend/src/domains/misc/hooks/useStakeholderTimeline.ts'
      via: "import { useStakeholderInteractionMutations } from '@/domains/misc'"
      pattern: 'useStakeholderInteractionMutations'
    - from: 'useStakeholderInteractionMutations hook body'
      to: 'useMutation<StakeholderTimelineEvent, Error, CreateInteractionRequest> and useMutation<TimelineAnnotation, Error, CreateAnnotationRequest>'
      via: '@tanstack/react-query generics'
      pattern: 'useMutation'
---

<objective>
Close TYPE-05 — the last v6.2-era typed-shim holdover:
1. Retype `useStakeholderInteractionMutations` at source so its return shape is `UseStakeholderInteractionMutationsReturn = { createInteraction, isCreating, createAnnotation, isAnnotating }`, with mutationFn signatures explicitly generic over the domain types from `@/types/stakeholder-interaction.types` (D-56-05, D-56-06, D-56-07, D-56-10).
2. Replace each stub `Promise.resolve({success:true})` body with a typed `throw new Error('useStakeholderInteractionMutations: not implemented — wire to real backend in future stakeholder-interactions feature phase')` (D-56-09).
3. Delete the deprecated re-export file `frontend/src/hooks/useStakeholderTimeline.ts` and update the sole consumer to import from `@/domains/misc` (D-56-08).
4. Remove the consumer's `as unknown as StakeholderInteractionMutationsShim` cast and the local shim interface (D-56-08).
5. Add Vitest unit coverage for the hook AND a co-located consumer-destructure assertion test per D-56-11 (the latter creates `StakeholderInteractionTimeline.test.tsx` since it does not exist on disk; the file is co-located beside the consumer per D-56-11's testing-precedent narrative).
6. Phase-wrap: after BOTH plans (56-01, 56-02) verify (sequentially per D-56-15), issue annotated SSH-signed `phase-56-base` tag, push to origin, verify signature (D-56-16).

Purpose: closes the final v6.2 TYPE-04 lineage carryover; "typed-at-source over consumer cast" becomes invariant across the codebase; `pnpm type-check` stays green workspace-wide.

Output: retyped hook, deleted shim re-export, simplified consumer, new hook test file, new consumer-destructure test file, signed `phase-56-base` tag pushed to origin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-rls-closure-last-typed-shim-retirement/56-CONTEXT.md
@frontend/src/domains/misc/hooks/useStakeholderTimeline.ts
@frontend/src/hooks/useStakeholderTimeline.ts
@frontend/src/components/stakeholder-timeline/StakeholderInteractionTimeline.tsx
@frontend/src/types/stakeholder-interaction.types.ts
@frontend/src/domains/misc/index.ts
@frontend/src/hooks/__tests__/useAfterActionsAll.test.ts

<interfaces>
<!-- Domain types that drive the retyped hook (from frontend/src/types/stakeholder-interaction.types.ts): -->

From frontend/src/types/stakeholder-interaction.types.ts:

- `StakeholderTimelineEvent` (L148) — the "created event" return shape for createInteraction.
- `TimelineAnnotation` (L79) — the "created annotation" return shape for createAnnotation.
- `CreateInteractionRequest` (L246) — input variable type for createInteraction.
- `CreateAnnotationRequest` (L278) — input variable type for createAnnotation.

Target hook return interface (NEW, co-located in useStakeholderTimeline.ts per D-56-07):

- `UseStakeholderInteractionMutationsReturn`:
  - `createInteraction: UseMutateAsyncFunction<StakeholderTimelineEvent, Error, CreateInteractionRequest>` (or the simpler `(req: CreateInteractionRequest) => Promise<StakeholderTimelineEvent>` typed projection — see Task 1 action).
  - `isCreating: boolean`
  - `createAnnotation: UseMutateAsyncFunction<TimelineAnnotation, Error, CreateAnnotationRequest>` (same projection).
  - `isAnnotating: boolean`

useMutation generics shape (already used in useAddTimelineEvent L74-83 of the same file):

- `useMutation<TData, TError, TVariables>({ mutationFn: (vars: TVariables) => Promise<TData>, onSuccess })`
- Output exposes `.mutateAsync` (Promise-returning) and `.isPending` (boolean — TanStack Query v5 name).

Consumer destructure (current — to become cast-free, ref: StakeholderInteractionTimeline.tsx L277-278):

- `const { createInteraction, isCreating, createAnnotation, isAnnotating } = useStakeholderInteractionMutations()`
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retype useStakeholderInteractionMutations at source with co-located return interface</name>
  <files>frontend/src/domains/misc/hooks/useStakeholderTimeline.ts</files>
  <read_first>
    - frontend/src/domains/misc/hooks/useStakeholderTimeline.ts (full file — preserve `useStakeholderTimeline`, `useAddTimelineEvent`, `useTimelineCategories`, `timelineKeys`, `StakeholderTimelineState`, `getAvailableInteractionTypes`, `StakeholderTimelineStats` verbatim; only mutate the `useStakeholderInteractionMutations` export and add the new interface)
    - frontend/src/types/stakeholder-interaction.types.ts (StakeholderTimelineEvent L148, TimelineAnnotation L79, CreateInteractionRequest L246, CreateAnnotationRequest L278)
    - .planning/phases/56-rls-closure-last-typed-shim-retirement/56-CONTEXT.md §decisions D-56-05, D-56-06, D-56-07, D-56-09, D-56-10
  </read_first>
  <behavior>
    - The hook's destructured return shape matches `UseStakeholderInteractionMutationsReturn` (compile-time enforced via explicit function return type annotation).
    - `createInteraction` is a Promise-returning function typed as `(req: CreateInteractionRequest) => Promise<StakeholderTimelineEvent>` (via `useMutation.mutateAsync`).
    - `createAnnotation` is a Promise-returning function typed as `(req: CreateAnnotationRequest) => Promise<TimelineAnnotation>`.
    - `isCreating` and `isAnnotating` are `boolean` (derived from `useMutation.isPending`).
    - The `addInteraction`, `updateInteraction`, `deleteInteraction` legacy members are GONE from the return object (per D-56-05; no consumers).
    - When `createInteraction(...)` or `createAnnotation(...)` is invoked, the underlying `mutationFn` throws `Error('useStakeholderInteractionMutations: not implemented — wire to real backend in future stakeholder-interactions feature phase')` (per D-56-09 — typed stub, error message per CONTEXT §specifics).
    - No `any`, no `unknown` (except in onSuccess's `void queryClient.invalidateQueries(...)` invocation which already uses no cast), no `as` cast inside the hook body.
    - `UseStakeholderInteractionMutationsReturn` is exported alongside `StakeholderTimelineState` (same file, mirroring the existing co-location precedent).
  </behavior>
  <action>
    Edit frontend/src/domains/misc/hooks/useStakeholderTimeline.ts:

    1. Add to the top-of-file `import type { ... } from '@/types/stakeholder-interaction.types'` block these additional named types (alongside the existing `StakeholderTimelineEvent` and `StakeholderTimelineFilters`):
       - `TimelineAnnotation`
       - `CreateInteractionRequest`
       - `CreateAnnotationRequest`

    2. ABOVE the existing `useStakeholderInteractionMutations` declaration (currently L95-116), add an exported interface declaration immediately AFTER `StakeholderTimelineState` (which ends ~L44):

       Identifier: `UseStakeholderInteractionMutationsReturn`
       Members (exactly these four, in this order):
       - `createInteraction: (req: CreateInteractionRequest) => Promise<StakeholderTimelineEvent>`
       - `isCreating: boolean`
       - `createAnnotation: (req: CreateAnnotationRequest) => Promise<TimelineAnnotation>`
       - `isAnnotating: boolean`

       JSDoc comment above the interface: "Return contract for `useStakeholderInteractionMutations` (D-56-07). Co-located here to match the `StakeholderTimelineState` precedent."

    3. REPLACE the entire body of `useStakeholderInteractionMutations` (L95-116) with a new implementation:
       - Annotate the function with the explicit return type `: UseStakeholderInteractionMutationsReturn`.
       - Inside: declare two `useMutation` instances (no destructure of the legacy add/update/delete trio):
         - `createInteractionMutation = useMutation<StakeholderTimelineEvent, Error, CreateInteractionRequest>({ mutationFn: (_req) => { throw new Error('useStakeholderInteractionMutations: not implemented — wire to real backend in future stakeholder-interactions feature phase') }, onSuccess: () => { void queryClient.invalidateQueries({ queryKey: timelineKeys.all }) } })`
         - `createAnnotationMutation = useMutation<TimelineAnnotation, Error, CreateAnnotationRequest>({ mutationFn: (_req) => { throw new Error('useStakeholderInteractionMutations: not implemented — wire to real backend in future stakeholder-interactions feature phase') }, onSuccess: () => { void queryClient.invalidateQueries({ queryKey: timelineKeys.all }) } })`
       - Return object:
         - `createInteraction: createInteractionMutation.mutateAsync`
         - `isCreating: createInteractionMutation.isPending`
         - `createAnnotation: createAnnotationMutation.mutateAsync`
         - `isAnnotating: createAnnotationMutation.isPending`

    4. UPDATE the leading block comment at L93. Replace `/* Stub exports – removed during refactoring, still imported by components */` with:
       `/* Typed stub mutations (D-56-05..D-56-10). Return shape: UseStakeholderInteractionMutationsReturn. Bodies throw 'not implemented' until the real stakeholder-interactions backend lands; consumers compile cleanly against the typed contract. */`

    5. Constraints — DO NOT:
       - Import the consumer's `StakeholderInteractionMutationsShim` interface (D-56 anti-pattern: consumer types must not leak into hook source).
       - Add `any` anywhere in the new code (`@typescript-eslint/no-explicit-any` is at error).
       - Change `useStakeholderTimeline`, `useAddTimelineEvent`, `useTimelineCategories`, `timelineKeys`, `StakeholderTimelineState`, `getAvailableInteractionTypes`, or `StakeholderTimelineStats` — they must remain byte-identical.
       - Wire `mutationFn` to `addTimelineEvent` or any other real backend call (explicit scope-out — "Hard scope boundary" in CONTEXT §domain).
       - Use `mutate` instead of `mutateAsync` — the consumer awaits the promise (`await createInteraction(...)` at L298 of the consumer), so `mutateAsync` is required.
       - Reintroduce the `success: true` placeholder shape.

    6. After saving, run `pnpm exec tsc --noEmit -p frontend` (or `pnpm --filter frontend type-check`) and confirm zero new errors. Do NOT proceed to Task 2 if this fails.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 &amp;&amp; grep -c "UseStakeholderInteractionMutationsReturn" frontend/src/domains/misc/hooks/useStakeholderTimeline.ts | awk '{ if ($1 &gt;= 3) exit 0; else exit 1 }' &amp;&amp; ! grep "Promise.resolve.*success: true" frontend/src/domains/misc/hooks/useStakeholderTimeline.ts &amp;&amp; grep -c "throw new Error.*not implemented.*wire to real backend" frontend/src/domains/misc/hooks/useStakeholderTimeline.ts | awk '{ if ($1 == 2) exit 0; else exit 1 }' &amp;&amp; ! grep -E ":\s*any\b" frontend/src/domains/misc/hooks/useStakeholderTimeline.ts &amp;&amp; pnpm --filter frontend type-check</automated>
  </verify>
  <done>
    Interface `UseStakeholderInteractionMutationsReturn` declared + exported + used as the explicit return annotation (≥ 3 occurrences). Both `mutationFn` bodies throw with the exact "not implemented — wire to real backend in future stakeholder-interactions feature phase" message (2 occurrences). Zero `Promise.resolve({success: true})` and zero `: any` type-annotations remain in the file (the `:\s*any\b` regex matches only TS type annotations and avoids false positives on the word "any" in comments). `pnpm --filter frontend type-check` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete deprecated re-export shim file and update consumer import path</name>
  <files>
    frontend/src/hooks/useStakeholderTimeline.ts (DELETE)
    frontend/src/components/stakeholder-timeline/StakeholderInteractionTimeline.tsx
  </files>
  <read_first>
    - frontend/src/hooks/useStakeholderTimeline.ts (the 12-line deprecated re-export — confirm it is a pure passthrough with no additional logic before deletion)
    - frontend/src/components/stakeholder-timeline/StakeholderInteractionTimeline.tsx L42-46 (the import block to rewrite), L60-70 (the local shim interface to delete), L275-278 (the cast site to remove)
    - frontend/src/domains/misc/index.ts (confirms `useStakeholderInteractionMutations`, `useStakeholderTimeline`, `getAvailableInteractionTypes` are all exported from the barrel — no new exports needed)
    - .planning/phases/56-rls-closure-last-typed-shim-retirement/56-CONTEXT.md §decisions D-56-08
  </read_first>
  <action>
    Step A — Delete the deprecated re-export file:
    1. Verify the file is a 12-line pure passthrough re-export (no consumers other than `StakeholderInteractionTimeline.tsx` per the CONTEXT grep — already confirmed in planner notes).
    2. Run a final guard grep to confirm nothing else imports from `@/hooks/useStakeholderTimeline`: `grep -rn "from ['\\\"]@/hooks/useStakeholderTimeline['\\\"]" frontend/src` — must return only `StakeholderInteractionTimeline.tsx`. If anything else appears, STOP and surface the unexpected consumer.
    3. Delete the file `frontend/src/hooks/useStakeholderTimeline.ts`. (Use the editing tool's file-delete or remove operation. Do NOT leave a tombstone re-export.)

    Step B — Update the consumer's import path AND remove the local shim interface AND remove the cast:
    Edit frontend/src/components/stakeholder-timeline/StakeholderInteractionTimeline.tsx:

    1. REWRITE the import block at L42-46. Replace the entire `import { useStakeholderTimeline, useStakeholderInteractionMutations, getAvailableInteractionTypes } from '@/hooks/useStakeholderTimeline'` with the same three names imported from `'@/domains/misc'`. No other lines in the import region change.

    2. DELETE the block at L60-70 (the JSDoc-style comment "Stub useStakeholderInteractionMutations is internally still a stub returning…" AND the `interface StakeholderInteractionMutationsShim { … }` declaration). Remove both the comment and the interface — they exist only to support the cast that disappears in step 3.

    3. REPLACE the destructure at L275-278:
       Current text:
       `  // Stub useStakeholderInteractionMutations takes 0 args; the (stakeholderType,`
       `  // stakeholderId) intent is preserved as void above for clarity.`
       `  const { createInteraction, isCreating, createAnnotation, isAnnotating } =`
       `    useStakeholderInteractionMutations() as unknown as StakeholderInteractionMutationsShim`

       New text (no cast, no shim interface reference):
       `  const { createInteraction, isCreating, createAnnotation, isAnnotating } =`
       `    useStakeholderInteractionMutations()`

       Drop the two-line `// Stub useStakeholderInteractionMutations …` comment entirely — it's stale documentation that no longer matches the typed-at-source contract.

    4. Constraints — DO NOT:
       - Touch any other line in `StakeholderInteractionTimeline.tsx` (no refactor of unrelated handlers, no comment cleanup, no import re-sort).
       - Add a new shim or wrapper anywhere else in the component tree.
       - Reorder imports — keep `useStakeholderTimeline`, `useStakeholderInteractionMutations`, `getAvailableInteractionTypes` in the same triple even after re-pointing the source.
       - Modify `frontend/src/domains/misc/index.ts` — the barrel already exports these three names.

    5. After save, run `pnpm --filter frontend type-check` and confirm zero errors. The destructured `createInteraction`/`createAnnotation` now type-flow from `UseStakeholderInteractionMutationsReturn` (defined in Task 1), and the existing `await createInteraction({...})` call at L298 will type-check directly against `(req: CreateInteractionRequest) => Promise<StakeholderTimelineEvent>`.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 &amp;&amp; ! test -e frontend/src/hooks/useStakeholderTimeline.ts &amp;&amp; ! grep -rn "from ['\"]@/hooks/useStakeholderTimeline['\"]" frontend/src &amp;&amp; grep -c "from '@/domains/misc'" frontend/src/components/stakeholder-timeline/StakeholderInteractionTimeline.tsx | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; ! grep "StakeholderInteractionMutationsShim" frontend/src/components/stakeholder-timeline/StakeholderInteractionTimeline.tsx &amp;&amp; ! grep "as unknown as" frontend/src/components/stakeholder-timeline/StakeholderInteractionTimeline.tsx &amp;&amp; pnpm --filter frontend type-check</automated>
  </verify>
  <done>
    Deprecated re-export file deleted; no `from '@/hooks/useStakeholderTimeline'` import remains in the codebase; consumer imports from `@/domains/misc`; `StakeholderInteractionMutationsShim` interface and `as unknown as` cast both gone; `pnpm --filter frontend type-check` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Vitest unit tests — hook surface + consumer-destructure assertion</name>
  <files>
    frontend/src/domains/misc/hooks/useStakeholderTimeline.test.ts
    frontend/src/components/stakeholder-timeline/StakeholderInteractionTimeline.test.tsx
  </files>
  <read_first>
    - frontend/src/domains/misc/hooks/useStakeholderTimeline.ts (the retyped hook from Task 1)
    - frontend/src/components/stakeholder-timeline/StakeholderInteractionTimeline.tsx (the cast-free consumer from Task 2 — confirm the destructure at L275-276 in its post-Task-2 form for Test F to mirror)
    - frontend/src/hooks/__tests__/useAfterActionsAll.test.ts (pattern reference: `createWrapper` with QueryClientProvider, `renderHook` from `@testing-library/react`, `vi.mock` setup, mutation invocation via `result.current.<mutateAsync-name>()` inside `await waitFor`)
    - frontend/tests/setup.ts (confirms `vi.mock('react-i18next', ...)` factory is global — no need to re-mock per file)
    - .planning/phases/56-rls-closure-last-typed-shim-retirement/56-CONTEXT.md §decisions D-56-11 (file location + test discipline; CONTEXT explicitly mandates "light addition to existing `StakeholderInteractionTimeline.test.tsx` (consumer destructure assertion)". As of revision time, this file does NOT exist on disk — Task 3 CREATES it co-located beside the consumer, still honoring the testing-precedent narrative)
    - frontend/src/types/stakeholder-interaction.types.ts (CreateInteractionRequest L246, CreateAnnotationRequest L278 — input shapes for the test fixtures)
  </read_first>
  <behavior>
    Hook surface tests (in `frontend/src/domains/misc/hooks/useStakeholderTimeline.test.ts`):
    - Test A (`returns the typed mutation surface`): `renderHook(() => useStakeholderInteractionMutations(), { wrapper })`; assert `result.current` has exactly 4 keys: `createInteraction`, `isCreating`, `createAnnotation`, `isAnnotating`. Assert `typeof result.current.createInteraction === 'function'`, `typeof result.current.createAnnotation === 'function'`, `result.current.isCreating === false` (initial), `result.current.isAnnotating === false` (initial). Assert NO `addInteraction`/`updateInteraction`/`deleteInteraction` keys exist.
    - Test B (`createInteraction throws not-implemented`): invoke `result.current.createInteraction(fixtureCreateInteractionRequest)` and assert it rejects with `Error` whose message contains `'not implemented'` AND `'wire to real backend'`. Use `expect(...).rejects.toThrow(/not implemented.*wire to real backend/)`.
    - Test C (`createAnnotation throws not-implemented`): same shape as Test B but for `createAnnotation(fixtureCreateAnnotationRequest)`.
    - Test D (`isCreating reflects mutation pending state`): trigger `createInteraction(...)` without awaiting; in a `waitFor` block, assert `result.current.isCreating === true` transitions. Note — because the mutationFn throws synchronously, isPending may flicker briefly; if non-deterministic, this test is OPTIONAL — see Action note.
    - Test E (`no cast in hook body — source assertion`): read the hook source file via `fs/promises` and assert it contains zero `as unknown as` or `:\s*any\b` (TypeScript type-annotation form) or `Promise.resolve({ success: true })` tokens. Belt-and-braces guard against future regression.

    Consumer-destructure tests (in `frontend/src/components/stakeholder-timeline/StakeholderInteractionTimeline.test.tsx`):
    - Test F (`useStakeholderInteractionMutations destructures cleanly without cast`): per CONTEXT §decisions D-56-11 explicit mandate ("light addition to existing `StakeholderInteractionTimeline.test.tsx` (consumer destructure assertion)"). Since the file does not exist on disk at revision time, this task CREATES it. The test (a) imports `useStakeholderInteractionMutations` from `'@/domains/misc'` (same source as the consumer post-Task-2), (b) renders the hook via `renderHook` with the same `QueryClientProvider` wrapper, and (c) asserts the four destructured members exist and have the expected runtime types: `createInteraction` and `createAnnotation` are `function`, `isCreating` and `isAnnotating` are `boolean`. The test additionally reads `StakeholderInteractionTimeline.tsx` source via `fs/promises` and asserts the source contains the cast-free destructure pattern `useStakeholderInteractionMutations()` (no trailing `as unknown as`) — this catches a future regression that re-introduces the cast at the consumer site.

    - The test files use the existing `renderHook` + `QueryClientProvider` wrapper pattern from `useAfterActionsAll.test.ts`.
    - Hook test file is co-located in `frontend/src/domains/misc/hooks/`; consumer test file is co-located in `frontend/src/components/stakeholder-timeline/` (mirrors the source location precedent — D-56-11 grants planner discretion; co-location chosen for read-locality).

  </behavior>
  <action>
    Sub-task 3a — Create the hook surface test file:

    Create frontend/src/domains/misc/hooks/useStakeholderTimeline.test.ts with the following structure (no fenced code in this `<action>` — the actual file body is implemented per these specs):

    1. Header JSDoc:
       - Title: `Phase 56 / TYPE-05 — useStakeholderInteractionMutations typed-stub contract test`
       - Bullets: (a) returns the 4-key UseStakeholderInteractionMutationsReturn surface; (b) createInteraction throws 'not implemented'; (c) createAnnotation throws 'not implemented'; (d) source contains no `as unknown as` / `: any` / Promise.resolve success placeholder.

    2. Imports:
       - `import { describe, it, expect, beforeEach } from 'vitest'`
       - `import { renderHook, waitFor } from '@testing-library/react'`
       - `import { QueryClient, QueryClientProvider } from '@tanstack/react-query'`
       - `import React from 'react'`
       - `import { readFile } from 'node:fs/promises'`
       - `import { fileURLToPath } from 'node:url'`
       - `import { dirname, join } from 'node:path'`
       - `import { useStakeholderInteractionMutations } from './useStakeholderTimeline'`
       - `import type { CreateInteractionRequest, CreateAnnotationRequest } from '@/types/stakeholder-interaction.types'`

    3. `createWrapper()` helper — IDENTICAL shape to `useAfterActionsAll.test.ts` L30-37: builds a `QueryClient` with `{ defaultOptions: { queries: { retry: false, gcTime: 0 }, mutations: { retry: false } } }` and returns `{ wrapper }`. (Note the addition of `mutations.retry: false` to prevent TanStack Query from retrying the throwing mutation.)

    4. Fixture builders:
       - `function makeCreateInteractionRequest(): CreateInteractionRequest` returning a minimal valid shape: `{ stakeholder_type: 'person', stakeholder_id: 'stk-test', interaction_type: 'meeting', title_en: 'Test interaction' }` (other fields optional — typed-check at compile time).
       - `function makeCreateAnnotationRequest(): CreateAnnotationRequest` returning: `{ event_type: 'interaction', event_id: 'evt-test', annotation_type: 'note', content_en: 'Test annotation' }`.

    5. `describe('useStakeholderInteractionMutations — Phase 56 / TYPE-05', () => { ... })` block with 4 (or 5 if D test is feasible) `it()` blocks per behaviors A-E above.

       Specifically for Test E (source assertion): use `import.meta.url` to derive __dirname, then `await readFile(join(__dirname, 'useStakeholderTimeline.ts'), 'utf-8')`. Assert via `.not.toMatch(/as unknown as/)`, `.not.toMatch(/Promise\.resolve\(\{\s*success:\s*true\s*\}\)/)`, AND `.not.toMatch(/:\s*any\b/)` (the latter loosely matches `: any` type-annotations without also banning the substring `any` inside identifiers like `companyName` or in JSDoc comments).

    6. Skip Test D (isCreating transitions) if it proves flaky on first run — D-56-11 only mandates test discipline, not exhaustive transition assertions. Keep Tests A, B, C, E mandatory.

    7. Constraints — DO NOT:
       - Mock `@tanstack/react-query` — use the real `useMutation` driven by an isolated QueryClient.
       - Mock the hook itself.
       - Use snapshot tests (brittle; assert specific keys/values instead).
       - Touch the global `vi.mock('react-i18next', …)` factory from `frontend/tests/setup.ts` — this hook doesn't use i18n.

    Sub-task 3b — Create the consumer-destructure test file (D-56-11 explicit mandate):

    Create frontend/src/components/stakeholder-timeline/StakeholderInteractionTimeline.test.tsx with the following structure:

    1. Header JSDoc:
       - Title: `Phase 56 / TYPE-05 — StakeholderInteractionTimeline consumer-destructure test`
       - Bullet: (a) `useStakeholderInteractionMutations()` destructures into `{ createInteraction, isCreating, createAnnotation, isAnnotating }` cleanly without `as unknown as` cast; runtime types of the four members match the typed contract.
       - Reference: D-56-11 — "light addition to existing `StakeholderInteractionTimeline.test.tsx` (consumer destructure assertion)". File did not exist at revision time; created here per the testing-precedent narrative.

    2. Imports:
       - `import { describe, it, expect } from 'vitest'`
       - `import { renderHook } from '@testing-library/react'`
       - `import { QueryClient, QueryClientProvider } from '@tanstack/react-query'`
       - `import React from 'react'`
       - `import { readFile } from 'node:fs/promises'`
       - `import { fileURLToPath } from 'node:url'`
       - `import { dirname, join } from 'node:path'`
       - `import { useStakeholderInteractionMutations } from '@/domains/misc'`

    3. `createWrapper()` helper — same shape as Sub-task 3a (QueryClient with retry disabled on both queries and mutations).

    4. `describe('StakeholderInteractionTimeline consumer destructure — Phase 56 / TYPE-05', () => { ... })` block with two `it()` blocks:

       - Test F1 (`useStakeholderInteractionMutations destructures cleanly without cast`):
         - Render the hook via `renderHook(() => useStakeholderInteractionMutations(), { wrapper })`.
         - Destructure: `const { createInteraction, isCreating, createAnnotation, isAnnotating } = result.current`.
         - Assert each destructured member exists and has the expected runtime type:
           - `expect(typeof createInteraction).toBe('function')`
           - `expect(typeof createAnnotation).toBe('function')`
           - `expect(typeof isCreating).toBe('boolean')`
           - `expect(typeof isAnnotating).toBe('boolean')`
         - Assert initial state: `expect(isCreating).toBe(false)` and `expect(isAnnotating).toBe(false)`.

       - Test F2 (`consumer source contains no cast`):
         - Resolve the absolute path of `StakeholderInteractionTimeline.tsx` using `import.meta.url` + `fileURLToPath` + `dirname` + `join('.', 'StakeholderInteractionTimeline.tsx')`.
         - Read the file via `readFile(..., 'utf-8')`.
         - Assert via `.not.toMatch(/as unknown as/)` AND `.not.toMatch(/StakeholderInteractionMutationsShim/)`.
         - Assert via `.toMatch(/useStakeholderInteractionMutations\(\)/)` to confirm the call site exists in its bare (no-cast) form.

    5. Constraints — DO NOT:
       - Render the full `StakeholderInteractionTimeline` component (it pulls in i18n, routing, dialog dependencies, and many child hooks — out of scope for a destructure-shape assertion). The `renderHook` approach validates the same contract the consumer relies on.
       - Mock `useStakeholderInteractionMutations` — call the real export from `@/domains/misc` so the test fails if Task 1 regresses.
       - Add `as` casts inside the test file itself (the file ought to model the cast-free pattern it asserts).
       - Mock `@tanstack/react-query` — use the real `useMutation` per the wrapper.

    6. After save, run BOTH new test files in isolation:
       - `pnpm --filter frontend exec vitest run src/domains/misc/hooks/useStakeholderTimeline.test.ts` — must exit 0.
       - `pnpm --filter frontend exec vitest run src/components/stakeholder-timeline/StakeholderInteractionTimeline.test.tsx` — must exit 0.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 &amp;&amp; test -f frontend/src/domains/misc/hooks/useStakeholderTimeline.test.ts &amp;&amp; test -f frontend/src/components/stakeholder-timeline/StakeholderInteractionTimeline.test.tsx &amp;&amp; pnpm --filter frontend exec vitest run src/domains/misc/hooks/useStakeholderTimeline.test.ts --reporter=verbose 2>&amp;1 | tee /tmp/56-02-hook-test.log &amp;&amp; grep -c "✓\|passed" /tmp/56-02-hook-test.log | awk '{ if ($1 &gt;= 4) exit 0; else exit 1 }' &amp;&amp; pnpm --filter frontend exec vitest run src/components/stakeholder-timeline/StakeholderInteractionTimeline.test.tsx --reporter=verbose 2>&amp;1 | tee /tmp/56-02-consumer-test.log &amp;&amp; grep -c "✓\|passed" /tmp/56-02-consumer-test.log | awk '{ if ($1 &gt;= 2) exit 0; else exit 1 }'</automated>
  </verify>
  <done>
    Hook test file `useStakeholderTimeline.test.ts` exists with ≥ 4 PASS lines (Tests A, B, C, E mandatory; D optional); zero failures. Consumer test file `StakeholderInteractionTimeline.test.tsx` exists with ≥ 2 PASS lines (Tests F1 + F2); zero failures. Source-assertion guards in both files catch future regressions of `as unknown as` / `: any` / `Promise.resolve({success:true})` in hook source and `as unknown as` / `StakeholderInteractionMutationsShim` in consumer source.
  </done>
</task>

<task type="auto">
  <name>Task 4: Workspace-wide type-check + cast-elimination grep gate</name>
  <files>(verification only — no edits)</files>
  <read_first>
    - frontend/src/components/stakeholder-timeline/StakeholderInteractionTimeline.tsx (verify cast/shim absence in final state)
    - frontend/src/domains/misc/hooks/useStakeholderTimeline.ts (verify typed source state)
  </read_first>
  <action>
    Sequentially run the gate commands (D-56-15: this plan's verification runs AFTER Plan 56-01 verification completes):

    1. Workspace-wide type-check: `pnpm type-check` from repo root — must exit 0. (Runs both `--filter frontend type-check` and `--filter backend type-check` per the root turbo task.)

    2. No-cast grep at consumer site: `grep -r "as unknown as.*Shim" frontend/src/components/stakeholder-timeline` — must return empty.

    3. No-cast grep across all callsites — bash-precedence-safe form (the naive `! grep ... | grep ' as '` parses as `(! grep ...) | grep ' as '`; the `!` only negates the first grep and the second never runs):
       `! { grep -rn "useStakeholderInteractionMutations" frontend/src | grep -q " as "; }` — must exit 0 (zero `as` callsites). The `-q` quiets output and `{ ... }` brace-group keeps `!` applied to the WHOLE pipeline.

    4. No deprecated re-export survives: `test ! -e frontend/src/hooks/useStakeholderTimeline.ts && ! grep -rn "from ['\"]@/hooks/useStakeholderTimeline['\"]" frontend/src` — must exit 0.

    5. Stub-shape regression guards (mirrors Test E inside the hook test, but at the gate layer):
       - `grep "Promise.resolve.*success: true" frontend/src/domains/misc/hooks/useStakeholderTimeline.ts` — must return empty.
       - `grep -E ":\s*any\b" frontend/src/domains/misc/hooks/useStakeholderTimeline.ts` — must return empty.

    6. Hook test + consumer test still pass (re-run as smoke checks):
       - `pnpm --filter frontend exec vitest run src/domains/misc/hooks/useStakeholderTimeline.test.ts` — must exit 0.
       - `pnpm --filter frontend exec vitest run src/components/stakeholder-timeline/StakeholderInteractionTimeline.test.tsx` — must exit 0.

    7. Plan 56-01 verification confirmation (sequential check, D-56-15): confirm that `tests/security/rls-audit.test.ts` exits 0 on the new tier — `pnpm exec vitest run tests/security/rls-audit.test.ts --exclude='.claude/worktrees/**'` from repo root must exit 0. If Plan 56-01 hasn't landed yet, STOP and signal the orchestrator that sequential ordering was violated.

    If any of steps 1-7 fail, do NOT proceed to Task 5 (the tag). Investigate and re-run Tasks 1-3 as needed.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 &amp;&amp; pnpm type-check &amp;&amp; ! grep -rn "as unknown as.*Shim" frontend/src/components/stakeholder-timeline &amp;&amp; ! { grep -rn "useStakeholderInteractionMutations" frontend/src | grep -q " as "; } &amp;&amp; test ! -e frontend/src/hooks/useStakeholderTimeline.ts &amp;&amp; ! grep -rn "from ['\"]@/hooks/useStakeholderTimeline['\"]" frontend/src &amp;&amp; ! grep "Promise.resolve.*success: true" frontend/src/domains/misc/hooks/useStakeholderTimeline.ts</automated>
  </verify>
  <done>
    `pnpm type-check` exits 0 across both workspaces; consumer has zero `as unknown as.*Shim` matches; no `useStakeholderInteractionMutations` callsite uses an `as` cast (bash-precedence-safe brace-grouped pipeline catches the regression that the un-bracketed form silently bypassed); deprecated re-export file is gone with zero `from '@/hooks/useStakeholderTimeline'` references in src; hook source has no `Promise.resolve({success:true})` placeholder.
  </done>
</task>

<task type="auto">
  <name>Task 5: Phase wrap-up — annotated SSH-signed phase-56-base tag, push, verify</name>
  <files>(no file changes — produces a git tag + remote ref)</files>
  <read_first>
    - .planning/phases/56-rls-closure-last-typed-shim-retirement/56-CONTEXT.md §decisions D-56-16
    - CLAUDE.md §"Tag signing setup" (confirms `gpg.format=ssh`, `user.signingkey`, `gpg.ssh.allowedSignersFile` are configured user-local)
    - prior precedent: `git tag -v phase-55-base` (must exit 0 and print `Good "git" signature`) as the working anchor
  </read_first>
  <action>
    This task runs ONLY after BOTH Plan 56-01 and Plan 56-02 are fully verified (D-56-15 sequential ordering + Task 4 above passed).

    1. Confirm both plans' verification suites are GREEN:
       - `pnpm exec vitest run tests/security/rls-audit.test.ts --exclude='.claude/worktrees/**'` exits 0.
       - `pnpm type-check` exits 0.
       - `pnpm --filter frontend exec vitest run src/domains/misc/hooks/useStakeholderTimeline.test.ts` exits 0.
       - `pnpm --filter frontend exec vitest run src/components/stakeholder-timeline/StakeholderInteractionTimeline.test.tsx` exits 0.

    2. Identify the merge/phase-completion commit SHA. This is HEAD of the branch where both plans landed (per D-56-14 inline-on-main, this is the latest commit on `main` after both plans' atomic commits have merged). Capture as variable `ANCHOR_SHA`.

    3. Pre-tag guards (matches CLAUDE.md "Tag signing setup" §"verify"):
       - `git config gpg.format` must equal `ssh`.
       - `git config user.signingkey` must point to an existing `~/.ssh/*.pub` file.
       - `git config gpg.ssh.allowedSignersFile` must point to a readable file.
       - `git tag -v phase-55-base` must exit 0 — anchor sanity check before creating phase-56-base. If this fails, STOP and surface the signing-config drift (do NOT attempt to fix git config in this task; CLAUDE.md states config is user-local).

    4. Create the tag with annotated + SSH-signed body:
       `git tag -as phase-56-base ${ANCHOR_SHA} -m "Phase 56: RLS Closure & Last Typed-Shim Retirement" -m "" -m "Closed:" -m "- RLS-01: tests/security/rls-audit.test.ts exits 0 on countries via globalReferenceTables tier (D-56-01..D-56-04)" -m "- TYPE-05: useStakeholderInteractionMutations typed at source; consumer cast + local shim removed; deprecated re-export deleted (D-56-05..D-56-11)" -m "" -m "Anchor: ${ANCHOR_SHA}"`

       Note: `git tag -as` is the documented invocation (`-a` annotated, `-s` sign). On systems with `tag.gpgSign=true` set globally, `-a` alone would sign; use `-as` explicitly for portability.

    5. Push the tag to origin: `git push origin phase-56-base`.

    6. Verify signature locally: `git tag -v phase-56-base` MUST exit 0 AND print `Good "git" signature`. If it fails, surface the error — do NOT delete and re-tag without operator approval.

    7. Verify the tag landed on origin: `git ls-remote --tags origin phase-56-base` returns a non-empty line whose SHA matches `${ANCHOR_SHA}` (or the tag object SHA — annotated tags have their own object SHA).

    8. Verify all prior phase-base tags are still reachable from `main` (regression guard mirroring Phase 55 D-13 UAT):
       `for tag in phase-47-base phase-48-base phase-49-base phase-54-base phase-55-base phase-56-base; do git tag -v $tag 2>&1 | grep -q 'Good \"git\" signature' || echo "REGRESSION: $tag verification failed"; done` — must produce zero "REGRESSION" lines.

    Constraints — DO NOT:
    - Use `--no-verify` or `--no-gpg-sign` (CLAUDE.md tag-signing rule).
    - Force-push or delete-then-recreate the tag (CLAUDE.md Git Safety Protocol).
    - Tag any commit other than the phase-completion HEAD on `main`.
    - Skip the `git tag -v phase-55-base` pre-flight check.

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 &amp;&amp; git tag -v phase-56-base 2>&amp;1 | grep -q 'Good \"git\" signature' &amp;&amp; git ls-remote --tags origin phase-56-base | grep -q "refs/tags/phase-56-base"</automated>
  </verify>
  <done>
    `phase-56-base` annotated SSH-signed tag exists locally and on `origin`; `git tag -v phase-56-base` exits 0 with `Good "git" signature`; all 6 phase-base tags (`phase-47-base` through `phase-56-base`) verify clean. Anchor SHA captured in the SUMMARY.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                                                                                                                                                   |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Consumer component → hook          | `StakeholderInteractionTimeline.tsx` calls `useStakeholderInteractionMutations()` and destructures the four members. Type contract enforced at compile time via `UseStakeholderInteractionMutationsReturn`.   |
| Hook → backend                     | Currently STUBBED — `mutationFn` throws synchronously. Real backend wiring is out of scope (Hard scope boundary, CONTEXT §domain).                                                                            |
| Runtime call site → error boundary | When a user clicks "Save interaction" / "Annotate", the existing component error boundary (already present in the component tree) catches the thrown `Error` and surfaces a toast/message. No silent swallow. |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                          | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                  |
| ---------- | ---------------------- | -------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-56-02-01 | Tampering              | Typed stub body could be confused for working code | mitigate    | Stub throws `Error('useStakeholderInteractionMutations: not implemented — wire to real backend in future stakeholder-interactions feature phase')` — error message is unambiguous, logged in production traces by Sentry. Consumer's React error boundary surfaces the failure (does not silently swallow). Test C/Test C\* in hook test asserts the throw at compile + runtime. |
| T-56-02-02 | Information Disclosure | Error message leaks phase metadata                 | accept      | Per CONTEXT §"Claude's Discretion": message avoids phase numbers (which drift). Final wording mentions "future stakeholder-interactions feature phase" — descriptive, not numeric. Acceptable signal-to-noise for ops triage.                                                                                                                                                    |
| T-56-02-03 | Repudiation            | Tag tampering                                      | mitigate    | `phase-56-base` is annotated + SSH-signed against the user's enrolled signing key per CLAUDE.md tag-signing setup. `git tag -v phase-56-base` provides cryptographic non-repudiation. Pre-flight check (`git tag -v phase-55-base`) validates signing-config integrity before issuing the new tag.                                                                               |
| T-56-02-04 | Denial of Service      | Repeated mutation calls would throw repeatedly     | accept      | TanStack Query's default retry behavior is disabled in the test (`mutations.retry: false`) — production behavior follows the app-wide QueryClient default. A user clicking "Save" against the unimplemented hook gets one error per click, surfaced to the existing error boundary. No retry storm.                                                                              |
| T-56-02-SC | Tampering              | Supply chain (npm/pip/cargo installs)              | n/a         | This plan adds zero new package dependencies. The vitest test files import only already-installed packages (`vitest`, `@testing-library/react`, `@tanstack/react-query`, `react`, node:fs/promises, node:url, node:path). No install tasks; legitimacy gate not triggered.                                                                                                       |

</threat_model>

<verification>
SEQUENTIAL VERIFICATION — RUNS AFTER PLAN 56-01 VERIFICATION (D-56-15).

Commands (executor runs from repo root, in order):

1. Pre-check: confirm Plan 56-01 has landed and verified.
   - `pnpm exec vitest run tests/security/rls-audit.test.ts --exclude='.claude/worktrees/**'` exits 0.
   - If this fails, STOP — Plan 56-02 verification depends on Plan 56-01 being green per D-56-15.

2. Workspace type-check: `pnpm type-check` exits 0 (both frontend + backend workspaces).

3. Cast elimination at consumer (bash-precedence-safe form — the naive `! grep ... | grep ' as '` parses as `(! grep) | grep` and the second filter never gates the result):
   - `grep -r "as unknown as.*Shim" frontend/src/components/stakeholder-timeline` returns empty.
   - `! { grep -rn "useStakeholderInteractionMutations" frontend/src | grep -q " as "; }` exits 0 (brace-group keeps `!` applied to the WHOLE pipeline; `-q` quiets the inner grep).
   - `grep -rn "StakeholderInteractionMutationsShim" frontend/src` returns empty (interface fully deleted).

4. Shim file deletion:
   - `test ! -e frontend/src/hooks/useStakeholderTimeline.ts` exits 0.
   - `grep -rn "from ['\"]@/hooks/useStakeholderTimeline['\"]" frontend/src` returns empty.

5. Hook source shape:
   - `grep "Promise.resolve.*success: true" frontend/src/domains/misc/hooks/useStakeholderTimeline.ts` returns empty.
   - `grep -E ":\s*any\b" frontend/src/domains/misc/hooks/useStakeholderTimeline.ts` returns empty.
   - `grep -c "UseStakeholderInteractionMutationsReturn" frontend/src/domains/misc/hooks/useStakeholderTimeline.ts` >= 3 (interface declaration + return annotation + interface export usage).

6. Hook + consumer destructure tests pass:
   - `pnpm --filter frontend exec vitest run src/domains/misc/hooks/useStakeholderTimeline.test.ts` exits 0 with ≥ 4 PASS lines (Tests A, B, C, E mandatory).
   - `pnpm --filter frontend exec vitest run src/components/stakeholder-timeline/StakeholderInteractionTimeline.test.tsx` exits 0 with ≥ 2 PASS lines (Tests F1, F2 mandatory).

7. Phase tag exists + verifies:
   - `git tag -v phase-56-base` exits 0 with `Good "git" signature`.
   - `git ls-remote --tags origin phase-56-base` returns the tag.
   - Regression check: `for tag in phase-47-base phase-48-base phase-49-base phase-54-base phase-55-base phase-56-base; do git tag -v $tag 2>&1 | grep -q 'Good "git" signature' || echo "REGRESSION: $tag"; done` produces zero output.

All 7 must succeed before phase-56 marks VERIFIED.
</verification>

<success_criteria>

- ROADMAP success criterion #2: `grep -r "useStakeholderInteractionMutations" frontend/src` shows zero `as` casts or typed-shim wrappers at consumer sites. ✅ via Tasks 2 + 4 (gate step 3, bash-precedence-safe brace-grouped pipeline).
- ROADMAP success criterion #3: `useStakeholderInteractionMutations` declares an explicit, non-`any`, non-`Promise.resolve({success: true})` return type that consumers consume directly. ✅ via Task 1 (`UseStakeholderInteractionMutationsReturn` interface, typed `mutateAsync`, throwing mutationFn).
- ROADMAP success criterion #4: `pnpm type-check` exits 0 across both workspaces with the shim removed. ✅ via Task 4 (gate step 1).
- D-56-08: Deprecated re-export file deleted; consumer imports from `@/domains/misc` directly. ✅ via Task 2.
- D-56-11: Vitest unit tests for hook + consumer destructure. ✅ via Task 3 (hook tests in `useStakeholderTimeline.test.ts` + consumer-destructure tests F1/F2 in `StakeholderInteractionTimeline.test.tsx` — the latter file CREATED per CONTEXT mandate since it did not exist on disk).
- D-56-16: Annotated SSH-signed `phase-56-base` tag on phase-completion commit, pushed to origin, `git tag -v` exits 0. ✅ via Task 5.
  </success_criteria>

<output>
After all 5 tasks complete and verification passes, create `.planning/phases/56-rls-closure-last-typed-shim-retirement/56-02-SUMMARY.md` using the standard SUMMARY template. Cite D-56-05..D-56-11 + D-56-15..D-56-16 in the "Decisions Honored" section; cite the deleted file path, the new test file paths (both hook test and consumer-destructure test), and the tag SHA + signature verification output in the "Artifacts" section; capture the workspace `pnpm type-check` exit-code + cast-grep empty results (with the bash-precedence-safe form) in the "Verification Evidence" section.
</output>
</output>
