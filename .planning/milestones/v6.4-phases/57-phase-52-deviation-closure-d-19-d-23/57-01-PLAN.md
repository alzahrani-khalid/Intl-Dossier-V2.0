---
phase: 57-phase-52-deviation-closure-d-19-d-23
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/adr/0001-mobile-dnd-scope-out.md
  - docs/adr/README.md
  - .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-ADR-D-19-mobile-dnd-scope-out.md
  - frontend/tests/e2e/tasks-tab-dnd.spec.ts
  - frontend/tests/e2e/tasks-tab-keyboard.spec.ts
autonomous: true
requirements: [DEVIATE-01]
requirements_addressed: [DEVIATE-01]
tags: [adr, mobile-dnd, scope-out, e2e]

must_haves:
  truths:
    - 'Top-level ADR docs/adr/0001-mobile-dnd-scope-out.md exists with status ACCEPTED documenting the mobile DnD scope-out rule (D-57-01, D-57-02)'
    - 'Phase artifact ADR .planning/phases/57-.../57-ADR-D-19-mobile-dnd-scope-out.md exists capturing the deviation-closure trail (D-57-02, D-57-05)'
    - 'tasks-tab-dnd.spec.ts contains a fifth it()/test() that asserts the mobile (<lg, 768×1024) branch renders a <select> "Move to" picker AND a programmatic select change updates workflow_stage (D-57-04)'
    - 'Existing test.skip() entries at 768×1024 in tasks-tab-dnd.spec.ts and tasks-tab-keyboard.spec.ts remain skips with crisp rationale wording citing D-19 scope-out (D-57-03)'
    - 'No production code change in TasksTab consumer or shared KanbanProvider primitive (D-57-03)'
  artifacts:
    - path: 'docs/adr/0001-mobile-dnd-scope-out.md'
      provides: 'Architectural rule: mobile DnD is scope-out; reopen requires UX research showing analyst mobile-DnD demand'
      contains: 'Status: Accepted'
    - path: 'docs/adr/README.md'
      provides: 'ADR directory purpose + numbering convention (created if absent)'
      contains: 'numbering'
    - path: '.planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-ADR-D-19-mobile-dnd-scope-out.md'
      provides: 'Phase-scoped deviation-closure trail for D-19'
      contains: 'D-19'
    - path: 'frontend/tests/e2e/tasks-tab-dnd.spec.ts'
      provides: 'New mobile-branch assertion: <select> Move to picker renders + programmatic change persists'
      contains: 'select'
  key_links:
    - from: 'docs/adr/0001-mobile-dnd-scope-out.md'
      to: 'frontend/src/components/kanban/KanbanProvider.tsx L99-101'
      via: 'cited line reference proving TouchSensor is wired for future consumers'
      pattern: 'KanbanProvider.tsx:99-101'
    - from: '.planning/phases/57-.../57-ADR-D-19-mobile-dnd-scope-out.md'
      to: '.planning/phases/52-heroui-v3-kanban-migration/52-VERIFICATION.md'
      via: 'cross-link to original D-19 deviation row'
      pattern: 'D-19-MOBILE-TOUCH-DND-SCOPE-OUT'
---

<objective>
Close D-19 (mobile touch DnD scope-out) via documentation + a single new e2e assertion that proves the mobile <select> "Move to" picker is the canonical mobile branch — no TouchSensor exercise on TasksTab mobile, no un-skipping of existing 768×1024 cells.

Purpose: Phase 52 deviation row D-19-MOBILE-TOUCH-DND-SCOPE-OUT carried PASS-WITH-DEVIATION because there was no positive evidence that the mobile branch works. This plan replaces the bare test.skip() with an ADR explaining the policy + an assertion exercising the mobile <select> path so the surface is verified, not merely skipped.

Output:

- One top-level ADR at docs/adr/0001-mobile-dnd-scope-out.md (creates docs/adr/ folder + README if absent)
- One phase-artifact ADR at .planning/phases/57-.../57-ADR-D-19-mobile-dnd-scope-out.md
- One new e2e test (fifth it() in tasks-tab-dnd.spec.ts) covering the mobile <select> picker path
- Crisper rationale strings on the existing test.skip() entries citing the ADR

**Execution context:** Inline on `main` with atomic per-task commits (D-57-20); verification sequencing across Phase 57 plans is D-19 → D-22 → D-21 → D-23 (D-57-21) — this plan's `wave` + `depends_on` frontmatter enforces that order.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-CONTEXT.md
@.planning/phases/52-heroui-v3-kanban-migration/52-VERIFICATION.md
@CLAUDE.md

<interfaces>
<!-- Key files the executor needs. Extract from codebase. -->

From frontend/src/components/kanban/KanbanProvider.tsx (L98-105):

```typescript
const mouseSensor = useSensor(MouseSensor, { activationConstraint: { distance: 8 } })
const touchSensor = useSensor(TouchSensor, {
  activationConstraint: { delay: 200, tolerance: 5 },
})
const keyboardSensor = useSensor(KeyboardSensor, {
  coordinateGetter: sortableKeyboardCoordinates,
})
const sensors = useSensors(mouseSensor, touchSensor, keyboardSensor)
```

From frontend/tests/e2e/tasks-tab-dnd.spec.ts (L19-50):

```typescript
const SEEDED_ENGAGEMENT_ID =
  process.env.PHASE_52_FIXTURE_ENGAGEMENT_ID ?? '00000000-0000-0052-0000-000000000001'

test.describe('Phase 52: Tasks tab Kanban drag-and-drop parity', () => {
  for (const { dir, viewport } of matrix) {
    test(`tasks tab drag persists in ${dir} @ ${viewport.width}x${viewport.height}`, async ({ page }) => {
      if (viewport.width < 1024) {
        test.skip(true, 'Pointer DnD is desktop-primary (≥1024); mobile uses the "Move to" select')
      }
      ...
```

From frontend/tests/e2e/tasks-tab-keyboard.spec.ts (L26-28):

```typescript
if (viewport.width < 1024) {
  test.skip(true, 'Keyboard DnD targets desktop only; mobile uses the "Move to" select')
}
```

</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the two ADRs (top-level + phase artifact)</name>
  <files>docs/adr/0001-mobile-dnd-scope-out.md, docs/adr/README.md, .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-ADR-D-19-mobile-dnd-scope-out.md</files>
  <read_first>
    - .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-CONTEXT.md (D-57-01, D-57-02, D-57-05 specifics)
    - .planning/phases/52-heroui-v3-kanban-migration/52-VERIFICATION.md (deviations_acknowledged D-19-MOBILE-TOUCH-DND-SCOPE-OUT row for verbatim quoting)
    - frontend/src/components/kanban/KanbanProvider.tsx (cite TouchSensor activation constraint at L99-101 — `{ delay: 200, tolerance: 5 }`)
    - CLAUDE.md (Responsive Design section — desktop-primary analyst workstation 1280–1400px target; quote verbatim in ADR rationale)
  </read_first>
  <action>
    Create docs/adr/README.md if the docs/adr/ folder does not exist. README content: brief explanation of ADR directory purpose, numbering convention (NNNN-kebab-case-slug.md, four-digit zero-padded; start at 0001), and one-line links to Michael Nygard's ADR template. Then create docs/adr/0001-mobile-dnd-scope-out.md with sections: `# 0001 — Mobile DnD scope-out for TasksTab` / `## Status` (Accepted, 2026-05-18) / `## Context` (cites CLAUDE.md "desktop-primary analyst workstation" + Phase 52 D-19 deviation row from 52-VERIFICATION.md) / `## Decision` (TasksTab keeps the existing <select> "Move to" picker on `<lg` breakpoint; shared KanbanProvider TouchSensor at frontend/src/components/kanban/KanbanProvider.tsx:99-101 with `{ delay: 200, tolerance: 5 }` stays available for future consumers but is not exercised by TasksTab on mobile) / `## Consequences` (Phase 52 D-19 deviation closed; reopen clause: requires explicit UX research showing analyst mobile-DnD demand) / `## References` (link to phase ADR + Phase 52 VERIFICATION row + CLAUDE.md Responsive Design section). Then create .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-ADR-D-19-mobile-dnd-scope-out.md with the same body PLUS a `## Deviation Closure Trail` section linking to docs/adr/0001-... and quoting the 52-VERIFICATION.md D-19 row verbatim. Filename rationale: ADR 0001 is the first slot per D-57-02 (planner picks; folder absent so we start the numbering). No emoji in copy. Reference D-57-01, D-57-02, D-57-05 in the phase artifact's metadata block.
  </action>
  <verify>
    <automated>test -f docs/adr/0001-mobile-dnd-scope-out.md && test -f docs/adr/README.md && test -f .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-ADR-D-19-mobile-dnd-scope-out.md && grep -q "Status" docs/adr/0001-mobile-dnd-scope-out.md && grep -q "Accepted" docs/adr/0001-mobile-dnd-scope-out.md && grep -q "KanbanProvider.tsx:99-101" docs/adr/0001-mobile-dnd-scope-out.md && grep -q "D-19-MOBILE-TOUCH-DND-SCOPE-OUT" .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-ADR-D-19-mobile-dnd-scope-out.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/adr/ directory exists with README.md explaining numbering convention
    - docs/adr/0001-mobile-dnd-scope-out.md has Status section reading "Accepted"
    - docs/adr/0001-mobile-dnd-scope-out.md cites KanbanProvider.tsx:99-101 verbatim
    - docs/adr/0001-mobile-dnd-scope-out.md quotes the CLAUDE.md desktop-primary rule
    - .planning/phases/57-.../57-ADR-D-19-mobile-dnd-scope-out.md links to the top-level ADR
    - .planning/phases/57-.../57-ADR-D-19-mobile-dnd-scope-out.md quotes the 52-VERIFICATION.md D-19 row verbatim under "Deviation Closure Trail"
    - No emoji anywhere in the ADRs (`grep -P "[\x{1F300}-\x{1FAFF}]" docs/adr/0001-mobile-dnd-scope-out.md` returns nothing)
    - No marketing voice (no "easily", "unleash", "discover", exclamation marks)
  </acceptance_criteria>
  <done>Both ADR files exist; deviation closure trail cross-link in place; CLAUDE.md desktop-primary rule quoted verbatim; KanbanProvider TouchSensor citation present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add mobile-branch assertion to tasks-tab-dnd.spec.ts + tighten skip-rationale strings</name>
  <files>frontend/tests/e2e/tasks-tab-dnd.spec.ts, frontend/tests/e2e/tasks-tab-keyboard.spec.ts</files>
  <read_first>
    - frontend/tests/e2e/tasks-tab-dnd.spec.ts (current structure — matrix loop + single it() per row; the new assertion goes AFTER the existing test.describe block at end of file)
    - frontend/tests/e2e/tasks-tab-keyboard.spec.ts (current skip-rationale string at L27)
    - frontend/tests/e2e/tasks-tab-visual.spec.ts (precedent for 768×1024 viewport + engagement workspace navigation to Tasks tab)
    - .planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-CONTEXT.md (D-57-03, D-57-04 — pick fifth it() in tasks-tab-dnd.spec.ts; do NOT create a new component test file)
    - docs/adr/0001-mobile-dnd-scope-out.md (just written; reference its filename in the updated skip-rationale strings)
  </read_first>
  <behavior>
    - New test in tasks-tab-dnd.spec.ts: "tasks tab mobile <select> Move to picker updates workflow_stage" — at viewport 768×1024, dir 'ltr'. Navigates to /engagements/<SEEDED_ENGAGEMENT_ID>, clicks the Tasks tab, locates the first row whose visible workflow_stage select shows the "todo" option selected, programmatically changes the select to "in_progress" via `page.locator('select[name*="workflow_stage"]').first().selectOption('in_progress')` (or equivalent role-based selector), waits ~800ms for mutation flight, reloads, asserts the same row's select now shows "in_progress". If no rows match (fixture not seeded), test.skip with rationale citing fixture absence.
    - Existing skip strings at tasks-tab-dnd.spec.ts L34 and tasks-tab-keyboard.spec.ts L27 are tightened to read exactly: `'Mobile DnD scope-out per docs/adr/0001-mobile-dnd-scope-out.md — TasksTab mobile branch uses the <select> Move to picker; touch DnD is not exercised on TasksTab (KanbanProvider TouchSensor remains wired for future consumers)'`
  </behavior>
  <action>
    Edit frontend/tests/e2e/tasks-tab-dnd.spec.ts: (1) Replace the existing skip rationale at L34 (`'Pointer DnD is desktop-primary (≥1024); mobile uses the "Move to" select'`) with the verbatim string from <behavior>. (2) After the existing test.describe block closes at L98, append a new top-level test.describe block named `'Phase 57: Tasks tab mobile <select> Move to picker (D-19 scope-out closure)'` containing exactly one test(). The test sets viewport to 768×1024, navigates to `/engagements/${SEEDED_ENGAGEMENT_ID}` (reuse the existing `SEEDED_ENGAGEMENT_ID` constant; do not redeclare), waits for the Tasks tab to be visible and clicks it, locates the mobile <select> elements (selector: `select` filtered by `name` or `aria-label` containing `workflow_stage` — verify against tasks-tab-visual.spec.ts pattern at runtime; if the TasksTab mobile branch uses a different control type, adjust selector but keep semantic intent of "the workflow_stage picker for a todo row"). Locate the first row whose current value is `todo`, change it to `in_progress` via Playwright `.selectOption('in_progress')`, wait 800ms, `page.reload()`, `waitForLoadState('networkidle')`, click Tasks tab again if needed, and assert the same row's select value is now `in_progress`. If no `todo`-valued select exists after the Tasks tab loads, `test.skip(true, 'No todo-stage rows present in fixture engagement to exercise the mobile <select> picker')`. Edit frontend/tests/e2e/tasks-tab-keyboard.spec.ts: replace the existing skip rationale at L27 (`'Keyboard DnD targets desktop only; mobile uses the "Move to" select'`) with the verbatim string from <behavior>. NO production code change in TasksTab consumer or KanbanProvider primitive per D-57-03. Per D-19 the existing 768×1024 skips MUST remain skips — do NOT un-skip them.
  </action>
  <verify>
    <automated>grep -c "Phase 57: Tasks tab mobile" frontend/tests/e2e/tasks-tab-dnd.spec.ts | grep -q "^1$" && grep -q "docs/adr/0001-mobile-dnd-scope-out.md" frontend/tests/e2e/tasks-tab-dnd.spec.ts && grep -q "docs/adr/0001-mobile-dnd-scope-out.md" frontend/tests/e2e/tasks-tab-keyboard.spec.ts && grep -cE "test\.skip\(true, '" frontend/tests/e2e/tasks-tab-dnd.spec.ts | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && grep -cE "test\.skip\(true, '" frontend/tests/e2e/tasks-tab-keyboard.spec.ts | awk '{ if ($1 >= 1) exit 0; else exit 1 }'</automated>
    <human-check>Operator confirms `pnpm --filter ./frontend exec playwright test tests/e2e/tasks-tab-dnd.spec.ts --list` enumerates exactly 5 tests (4 original + 1 new D-19 scope-out closure test); when the dev server + staging fixture is available, the new test passes (or skips with the "No todo-stage rows" rationale, which is acceptable).</human-check>
  </verify>
  <acceptance_criteria>
    - frontend/tests/e2e/tasks-tab-dnd.spec.ts contains exactly one new top-level test.describe block titled 'Phase 57: Tasks tab mobile <select> Move to picker (D-19 scope-out closure)'
    - The new test references SEEDED_ENGAGEMENT_ID (existing constant), navigates to /engagements/${SEEDED_ENGAGEMENT_ID}, and exercises selectOption('in_progress')
    - frontend/tests/e2e/tasks-tab-dnd.spec.ts L34 skip-rationale text contains 'docs/adr/0001-mobile-dnd-scope-out.md'
    - frontend/tests/e2e/tasks-tab-keyboard.spec.ts L27 skip-rationale text contains 'docs/adr/0001-mobile-dnd-scope-out.md'
    - No new spec file is created; no TasksTab consumer or KanbanProvider edit
    - `pnpm --filter ./frontend exec playwright test tests/e2e/tasks-tab-dnd.spec.ts --list` enumerates 5 tests
  </acceptance_criteria>
  <done>Fifth test added to tasks-tab-dnd.spec.ts exercising the mobile <select> path. Skip rationales in both spec files updated to cite the ADR. Spec enumeration passes (`playwright --list` shows 5 tests in tasks-tab-dnd.spec.ts).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                                                          |
| ------------------ | ------------------------------------------------------------------------------------ |
| documentation-only | ADRs cross trust boundary as docs, not executable code; no runtime exposure          |
| test code          | new e2e test runs against staging Supabase via env vars; no new auth flow introduced |

## STRIDE Threat Register

| Threat ID  | Category               | Component                           | Disposition | Mitigation Plan                                                                                                              |
| ---------- | ---------------------- | ----------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------- |
| T-57-01-01 | Information Disclosure | ADR public copy                     | accept      | ADRs cite only public engineering decisions; no credentials, no PII; reviewed by author before commit                        |
| T-57-01-02 | Tampering              | tasks-tab-dnd.spec.ts new assertion | mitigate    | spec uses existing `SEEDED_ENGAGEMENT_ID` env-var chain (fallback to non-secret canonical UUID); no new secret-handling path |
| T-57-01-03 | Repudiation            | ADR provenance                      | mitigate    | Phase 57 close issues SSH-signed `phase-57-base` tag per D-57-22; ADRs land in tracked git history                           |

</threat_model>

<verification>
- Both ADR files exist with correct frontmatter and verbatim citations
- tasks-tab-dnd.spec.ts now enumerates 5 tests via `playwright --list`
- tasks-tab-keyboard.spec.ts skip rationale references the ADR
- No production code modified outside test files and docs
- `pnpm --filter ./frontend lint frontend/tests/e2e/tasks-tab-dnd.spec.ts frontend/tests/e2e/tasks-tab-keyboard.spec.ts` exits 0
</verification>

<success_criteria>

- DEVIATE-01 ROADMAP success criterion #1 met: D-19 closed via explicit scope-out ADR + mobile <select> picker assertion exists
- 52-VERIFICATION.md D-19-MOBILE-TOUCH-DND-SCOPE-OUT row can be flipped from `passed_with_deviation` to `passed` (cross-phase update happens at phase close, not in this plan)
- Existing 768×1024 test.skip() entries in tasks-tab-dnd.spec.ts and tasks-tab-keyboard.spec.ts remain skips with refined rationale
- No code regression: TasksTab consumer + shared KanbanProvider unmodified
  </success_criteria>

<output>
Create `.planning/phases/57-phase-52-deviation-closure-d-19-d-23/57-01-SUMMARY.md` when done. Include: ADR filenames + numbers chosen, exact text of the new test name, file:line of the updated skip-rationale strings, verification command outputs (playwright --list count, grep checks).
</output>
