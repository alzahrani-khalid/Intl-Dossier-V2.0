# Milestone v6.5: Escalated Backlog Hardening

**Status:** ✅ SHIPPED 2026-06-11
**Phases:** 60-61
**Total Plans:** 6 (Phase 60) + 1 quick-task delivery (Phase 61)
**Source:** `reports/escalated-backlog-master-2026-06-10.md` (no standalone REQUIREMENTS.md — requirements lived in the backlog master)

## Overview

Closed the two highest-leverage items from the escalated backlog master: restored schema/type truth between the repo and the live staging database, and closed the four known security holes. Phase 60 regenerated `database.types.ts` byte-identical across both workspaces, committed all live-only SQL as forward migrations (unified work stack, SLA dashboard RPCs, event_details view, three ungenerated tables), and wired a CI smoke test so every edge-fn `.from`/`.rpc` reference must exist in generated types. Phase 61 (delivered by adversarial security quick task 260610-fkn, merged via PR #54) bound activity_stream inserts to the authenticated actor, escaped every briefing-books HTML sink, deleted the token-leaking ExportDialog, and unified admin role gating on `public.users.role` with fail-closed `requireAdmin` guards — UAT-verified live.

In parallel (tracked as quick task 260608-c9b, also merged via PR #54): a 17-round dossier-workflow inspection loop across all 8 dossier types fixed the positions-attach 4-bug stack, the shared documents embed, the backend auth dead-column 401, persons-list RLS, and ~30 navigation/i18n items; its bucket-B residue was escalated to `.planning/dossier-workflow-backlog-phases-2026-06-11.md` (seeds v6.6).

## Phases

### Phase 60: Schema & Type Truth Restoration ✅ COMPLETE 2026-06-10

**Goal:** The repo's generated types, committed migrations, and edge-function SQL all agree with the live staging database — every RPC/table an edge function references exists in generated types, and the missing canonical SQL is committed as forward migrations.
**Depends on:** Nothing (foundation phase; unblocks P2-P8 backlog phases)
**Plans:** 6/6 complete (VERIFICATION passed 14/14)
**Requirements:** Backlog P1 (reports/escalated-backlog-master-2026-06-10.md)

Plans:

**Wave 1**

- [x] 60-01-PLAN.md (Wave 1) — Capture unified work stack (view + RPCs) from live into a forward migration [my-work #3]
- [x] 60-02-PLAN.md (Wave 1) — Corrected SLA monitoring migration (users.full_name, not staff_profiles) + 4 dashboard RPCs + fix/redeploy escalations-report absent columns [sla #2/#4, tq #5]
- [x] 60-03-PLAN.md (Wave 1) — event_details view (035 shape + organizer/country via dossiers) + supersede dead 009_data_library [events #4, data-library #1]
- [x] 60-04-PLAN.md (Wave 1) — pending_role_approvals + position_delegations + word_assistant_logs tables (owner-scoped RLS, no auth.users trigger) [approvals #6, wa #4]

**Wave 2** _(blocked on Wave 1 completion)_

- [x] 60-05-PLAN.md (Wave 2) — Regenerate database.types byte-identical (frontend+backend) + remove dead doubled-path copy + type-check both workspaces [P1 type regen]

**Wave 3** _(blocked on Wave 2 completion)_

- [x] 60-06-PLAN.md (Wave 3) — CI smoke test: every edge-fn .from/.rpc reference must exist in generated types (wired into existing lint job)

Scope:

- Regenerate `frontend/src/types/database.types.ts` from staging (project `zkrcjzdemdmwhearhfgg`) via Supabase MCP `generate_typescript_types`
- Commit missing canonical SQL as forward migrations: unified work view/RPCs (`unified_work_items`, `get_unified_work_items`, `get_user_work_summary`, `get_user_productivity_metrics`) [my-work #3], 4 SLA dashboard RPCs (`get_sla_dashboard_overview` etc.) [sla #2], `event_details` view [events #4]
- Reconcile dual `009_create_data_library_items.sql` vs `009_data_library.sql` (query staging FIRST to decide which schema is live) [data-library #1]
- Generate-or-migrate ungenerated tables: `pending_role_approvals`, `position_delegations`, `word_assistant_logs` [approvals #6, wa #4]
- Fix RPC SQL referencing absent columns: `staff_profiles.full_name` (SLA assignee/at-risk) [sla #4], escalations-report columns (`assignments.organizational_unit_id`, `organizational_units.name`) [tq #5]
- Add CI smoke test: every RPC/table an edge fn references must exist in generated types

### Phase 61: Security Pass ✅ COMPLETE 2026-06-11 (delivered by quick task 260610-fkn, merged via PR #54)

**Goal:** The four known security holes are closed: activity_stream inserts are bound to the authenticated actor with visibility scope enforced, briefing-books HTML output is sanitized, the test-token ExportDialog is gone, and admin-surface role gating reads one canonical role source.
**Depends on:** Phase 60 (regenerated types)
**Plans:** Delivered outside plan files by quick task 260610-fkn (adversarial security pass); verified 2026-06-11
**Requirements:** Backlog P2 (reports/escalated-backlog-master-2026-06-10.md)

Scope (all four verified closed):

- [x] `activity_stream` RLS: `activity_stream_insert_own_actor` WITH CHECK `actor_id = auth.uid()`; SELECT scoped to public/actor/target — live-verified via pg_policies; migration `20260610120000_activity_stream_actor_binding_and_role_backfill.sql` [activity #1]
- [x] `briefing-books` HTML generation escaped via `escapeHtml` on every sink, incl. @page header/footer CSS sinks (commits 21f67834, e3e51688) [bb #9]
- [x] `ExportDialog.tsx` (hard-coded `Bearer test-auth-token`) deleted with its orphaned test; remaining token refs are NODE_ENV-guarded contract mocks (backend/src/api/contract/\*) [exp #6]
- [x] Role source unified on `public.users.role`: `requireAdmin` beforeLoad on all 9 admin routes (fail-closed), authStore metadata fallback removed, audit-logs-viewer edge fn admin-only — UAT-verified live (admin sees all 9; DB role-flipped viewer blocked on all 9) [settings #4/#5, audit #4]

---

## Milestone Summary

**Decimal Phases:** None.

**Key decisions:**

- Phase 61 was satisfied by an out-of-band adversarial security quick task (260610-fkn) instead of planned phase execution — verified retroactively against all four scope items (live pg_policies + repo evidence) before close.
- `requireAdmin.ts` → `require-admin.ts` rename (lib/\*\* kebab-case lint rule) landed during PR #54 CI hardening.
- Bundle budgets raised once for legitimate growth: entry 450→460 KB, Total JS 2.5→2.55 MB (frontend/.size-limit.json).

**Known deferred items at close:** 34 quick-task directories lack completion markers (work itself shipped; see STATE.md Deferred Items). No v6.5 MILESTONE-AUDIT.md was produced; Phase 60 has VERIFICATION passed 14/14 and Phase 61 was hand-verified 2026-06-11.

**Tech debt carried forward:** dossier-workflow B-backlog (6 phases) → v6.6; droplet backend still needs the round-11 `backend/src/middleware/auth.ts` deploy.
