# Milestone v6.3: Carryover Sweep & v7.0 Prep

**Status:** ✅ SHIPPED 2026-05-17
**Phases:** 50-54
**Total Plans:** 28
**Audit:** `.planning/milestones/v6.3-MILESTONE-AUDIT.md` (status: passed; 20/20 requirements satisfied; re-audit confirmed 2026-05-16 after quick-task 260516-s3j paperwork closure)

## Overview

Closed v6.2 carryover debt, hardened test and design-token gates, executed the HeroUI v3 Kanban migration, tightened the React vendor budget, re-issued v6.2 phase-base tags as annotated + SSH-signed, and laid the v7.0 Intelligence Engine schema groundwork. Schema-only — no API, no UI — to keep the milestone a clean runway before feature work.

Coverage: 20/20 v6.3 requirements satisfied (TEST-01..04, DESIGN-01..04, KANBAN-01..04, BUNDLE-05..07, INTEL-01..05).

## Phases

### Phase 50: Test Infrastructure Repair

**Goal:** Module-evaluation succeeds for all frontend test consumers; 4 wizard tests pass green; vitest setup gaps documented so subsequent phases can rely on green test infra.
**Depends on:** Nothing (entry phase — unblocks downstream verification)
**Plans:** 10/10 plans complete
**Requirements:** TEST-01, TEST-02, TEST-03, TEST-04

Plans:

- [x] 50-01: FE cascade unblock + i18n factory + 11 dead-import deletions (Wave 1)
- [x] 50-03: BE global mocks + integration runner split + per-file triage (Wave 1)
- [x] 50-04: 50-TEST-AUDIT.md + frontend/docs/test-setup.md + backend/docs/test-setup.md (Wave 5)
- [x] 50-05: CI jobs + D-13 branch protection + D-15 ESLint rule (Wave 6; PR #11 CLOSED, branch protection re-verified)
- [x] 50-09: Cross-cutting polyfills + provider migration (Wave 2)
- [x] 50-10: i18n-text drift batched fix (Wave 3)
- [x] 50-11: a11y/perf outliers (Wave 3)
- [x] 50-12: design-system + route harness (Wave 3)
- [x] 50-13a: phase-exit-0 original 6-file scope; useCountryAutoFill + 5 residual files (Wave 4)
- [x] 50-13b: 7-file tests/component/ i18n cluster + downstream 50-04/05 depends_on rewire (Wave 4)

### Phase 51: Design-Token Compliance Gate

**Goal:** Raw hex colors and Tailwind color literals can no longer enter `frontend/src/`; all existing violations cleared; PR-blocking CI gate registered on `main`.
**Depends on:** Phase 50 (green test infra so lint-driven fixes don't mask test regressions)
**Plans:** 4/4 plans complete
**Requirements:** DESIGN-01, DESIGN-02, DESIGN-03, DESIGN-04

Plans:

- [x] 51-01: ESLint D-05 selectors (hex + palette + TemplateElement) + D-03 Tier-B carve-out + phase-51-base tag + regression fixture (Wave 1; severity ships at `warn`)
- [x] 51-02: Tier-A swap WorldMapVisualization.tsx + PositionEditor.tsx with visual-parity verification (Wave 2)
- [x] 51-03: Tier-A mechanical sweep ~80-120 files + 51-DESIGN-AUDIT.md scaffold (Wave 2)
- [x] 51-04: Tier-C disables + audit population + severity flip `warn`→`error` + smoke PR (mergeStateStatus=BLOCKED) + 51-SUMMARY.md (Wave 3)

### Phase 52: HeroUI v3 Kanban Migration

**Goal:** Both Kanban surfaces (TasksTab + EngagementKanbanDialog) run on HeroUI v3 + `@dnd-kit/core` with full behavior parity; `kibo-ui/kanban` is gone from the codebase and banned from re-introduction; visual baselines regenerated and Playwright specs green.
**Depends on:** Phase 50 (test infra), Phase 51 (so the migrated kanban code is born design-token-compliant)
**Plans:** 5/5 plans complete
**Requirements:** KANBAN-01, KANBAN-02, KANBAN-03, KANBAN-04

Plans:

- [x] 52-01: Wave-0 test scaffolds (vitest stubs + 8 Playwright spec skeletons + ESLint regression fixture) (Wave 1)
- [x] 52-02: Build new shared `frontend/src/components/kanban/*` primitive (5 components + barrel; native DragOverlay; WorkBoard sensor stack) (Wave 2)
- [x] 52-03: Consumer migration (TasksTab + EngagementKanbanDialog import swap; STAGE_COLORS deletion; KanbanTaskCard Tier-C absorption) (Wave 3)
- [x] 52-04: kibo-ui directory deletion + tunnel-rat removal + ESLint widen + check-deleted-components update + Phase 51 audit row closeout (Wave 4)
- [x] 52-05: 4 visual baselines (EN+AR × 1280+768) + axe-core a11y + 12-spec regression anchor + human-verify checkpoint + 52-SUMMARY.md (Wave 5; PASS-WITH-DEVIATION — D-19..D-23 documented)

### Phase 53: Bundle Tightening + Tag Provenance

**Goal:** React vendor budget tightened to D-03 min rule; v6.2 phase-base tags upgraded to annotated + signed for `git tag -v` provenance; CLAUDE.md Node note matches `package.json` engines.
**Depends on:** Phase 50 (test infra), Phase 52 (Kanban migration may shift vendor chunk composition; tighten after migration is locked)
**Plans:** 3/3 plans complete
**Requirements:** BUNDLE-05, BUNDLE-06, BUNDLE-07

Plans:

- [x] 53-01: BUNDLE-05 — fresh react-vendor measurement, lower `.size-limit.json` ceiling to D-03 value (~285 KB), replace React vendor row in `bundle-budget.md`
- [x] 53-02: BUNDLE-07 — align both CLAUDE.md Node references to `Node.js 22.13.0+` to match `package.json` engines
- [x] 53-03: BUNDLE-06 — configure global SSH signing, re-issue `phase-47/48/49-base` as annotated + signed, force-push to origin, add `Tag signing setup` appendix to CLAUDE.md (PASS — origin SHAs match local annotated+signed objects post-audit verification)

### Phase 54: Intelligence Engine Schema Groundwork

**Goal:** v7.0 Intelligence Engine has its data layer ready — `intelligence_event` + `intelligence_digest` + polymorphic dossier linking + source enum + regenerated TS types — applied to staging via Supabase MCP. Schema only; no API, no UI.
**Depends on:** Nothing (groundwork for v7.0; can run in parallel with Phases 51–53. Soft-depends on Phase 50 only for green test infra during type regen.)
**Plans:** 4/4 plans complete
**Requirements:** INTEL-01, INTEL-02, INTEL-03, INTEL-04, INTEL-05

Plans:

- [x] 54-01: Phase-54-base signed tag + rename `intelligence_digest` → `dashboard_digest` + lockstep frontend hook/test/widget rename (Wave 1)
- [x] 54-02: `signal_source_type` enum + `intelligence_event` table + new `intelligence_digest` table + 4-policy RLS + Wave-0 RLS integration test (Wave 2)
- [x] 54-03: `intelligence_event_dossiers` polymorphic junction with EXISTS-via-parent RLS + CASCADE + 7-value `dossier_type` CHECK + Wave-0 junction integration test (Wave 3)
- [x] 54-04: REQUIREMENTS.md + ROADMAP.md text patches (post-Phase-54 wording) + TS regen via MCP to both workspaces + dual `pnpm type-check` + `rls-audit.test.ts` sensitiveTables extension (Wave 4)

**Cross-cutting constraints:**

- D-15: Migration applied to staging (`zkrcjzdemdmwhearhfgg`) via Supabase MCP `apply_migration` — NOT local CLI.

## v6.3 Coverage Validation

**Total v6.3 requirements:** 20 unique REQ-IDs across 5 categories
**Mapped:** 20/20 ✓
**Orphans:** 0
**Duplicates:** 0

| Category | REQ-IDs                                          | Phase    |
| -------- | ------------------------------------------------ | -------- |
| TEST     | TEST-01, TEST-02, TEST-03, TEST-04               | Phase 50 |
| DESIGN   | DESIGN-01, DESIGN-02, DESIGN-03, DESIGN-04       | Phase 51 |
| KANBAN   | KANBAN-01, KANBAN-02, KANBAN-03, KANBAN-04       | Phase 52 |
| BUNDLE   | BUNDLE-05, BUNDLE-06, BUNDLE-07                  | Phase 53 |
| INTEL    | INTEL-01, INTEL-02, INTEL-03, INTEL-04, INTEL-05 | Phase 54 |

## v6.3 Dependency Graph

```
Phase 50 (TEST infra) — entry, no deps
   ├──> Phase 51 (DESIGN gate)
   │       └──> Phase 52 (KANBAN migration — born compliant)
   │               └──> Phase 53 (BUNDLE tighten — after Kanban shifts)
   └──> Phase 54 (INTEL schema — parallel-safe; soft-dep on green tests for type regen)
```

Phase 54 ran independently of Phases 51–53 once Phase 50 was green.

## Milestone Summary

**Key Decisions:**

- ESLint D-05 selectors at `error` severity workspace-wide ban raw hex (`#[0-9a-fA-F]{3,8}`) and Tailwind palette literals (`text-blue-*`, `bg-red-*`, etc.) in `frontend/src/`; token-mapped utilities (`text-bg`, `text-ink`, `text-accent`) permitted
- Tier-A / Tier-C split for DESIGN-03: 50 named-anchor files swapped to tokens; 271 Tier-C files / 2336 AST nodes suppressed with per-Literal `eslint-disable-next-line` annotations (cleanup waves inherited to v6.4)
- D-09 fold into existing Phase 48 `Lint` required context (no new branch-protection PUT) — smoke PR #12 captured `Lint=FAILURE` + `mergeStateStatus=BLOCKED`
- KANBAN-02 satisfied-by-deletion per D-20: `EngagementKanbanDialog.tsx` + `EngagementDossierPage.tsx` deleted as dead code; route `/dossiers/engagements/$id` now redirect-only to the workspace TasksTab (which exercises the shared `@dnd-kit/core` primitive)
- Shared `frontend/src/components/kanban/*` primitive built on `@dnd-kit/core` with pointer + keyboard sensors, native `DragOverlay`, token-bound styling — consumed by both surfaces before kibo-ui deletion
- `tunnel-rat` removed from `frontend/package.json` + lockfile; `@/components/kibo-ui/*` banned via `no-restricted-imports`; `check-deleted-components.sh` CI gate asserts absence
- `vi.mock("react-i18next")` factory in `frontend/tests/setup.ts:46-58` uses `vi.importActual` + spread so `initReactI18next` and other real exports pass through unchanged (TEST-01)
- `intelligence_digest` (Phase-45) renamed to `dashboard_digest` to free the canonical name for the v7.0 Intelligence Engine surface (54-01) with lockstep frontend hook/test/widget rename
- `signal_source_type` enum (`publication`, `feed`, `human_entered`, `ai_generated`) applied to `intelligence_event.source_type` as the canonical source taxonomy
- `intelligence_event_dossiers` polymorphic junction enforces a 7-value `dossier_type` CHECK matching the canonical types (`country`, `organization`, `forum`, `engagement`, `topic`, `working_group`, `person`)
- Migrations applied via Supabase MCP `apply_migration` to staging (`zkrcjzdemdmwhearhfgg`) — local CLI path explicitly disallowed per D-15
- React vendor ceiling lowered 349 → 285 KB gz in `.size-limit.json` per D-03 min rule (measured 279.42 kB + ~5 kB slack documented in `frontend/docs/bundle-budget.md`)
- `phase-47-base` / `phase-48-base` / `phase-49-base` re-issued annotated + SSH-signed; `git tag -v` exits 0 locally with `Good "git" signature` for all three; origin force-push effectively resolved (origin SHAs match local annotated+signed objects)
- CLAUDE.md Node engine note aligned to `Node.js 22.13.0+` at both call sites (L84 + L483) to match `package.json` engines.node `">=22.13.0"`
- `eslint-ban.test.ts` vitest timeout raised 20s → 60s to accommodate cold pnpm spawn budget (closed by quick-task 260516-s3j)

**Issues Resolved:**

- 4 wizard tests fixed (TEST-01..02): module-eval no longer crashes on `vi.mock("react-i18next")` missing `initReactI18next`
- `WorldMapVisualization.tsx:193` raw hex `#3B82F6` → token; `PositionEditor.tsx` `text-blue-600` / `text-red-{600,800}` literals → tokens (DESIGN-03 Tier-A)
- `pnpm lint` 0 + `pnpm type-check` 0 with new design-token rules at `error` severity workspace-wide (DESIGN-04)
- HeroUI v3 Kanban migration complete: TasksTab on shared `@dnd-kit/core` primitive; `kibo-ui` directory + `tunnel-rat` dependency purged (KANBAN-01..04)
- React vendor 349 → 285 KB gz ceiling tightening (BUNDLE-05)
- Phase-base tags re-issued annotated + SSH-signed (BUNDLE-06)
- CLAUDE.md Node note aligned to `22.13.0+` (BUNDLE-07)
- Intelligence Engine schema groundwork complete: `intelligence_event`, `intelligence_digest`, `intelligence_event_dossiers`, `signal_source_type` enum, regenerated TS types byte-identical across workspaces, dual `pnpm type-check` exit 0 (INTEL-01..05)
- All 11 stale `Pending` rows in REQUIREMENTS.md traceability table refreshed to `Verified` (closed by quick-task 260516-s3j)
- TEST-02 palette-literal drift on `SLAIndicator` / `TaskCard` closed by quick-task 260516-rcm
- Phase 50 + Phase 54 VALIDATION.md frontmatter refreshed (`nyquist_compliant: true`, `status: passed`) — closed by quick-task 260516-s3j

**Issues Deferred (v6.4 carryover):**

- DesignV2 → main merge sequence
- `53-03-SUMMARY.md` verdict `PASS-WITH-DEFERRAL` → `PASS` refresh (cosmetic; origin SHAs already match)
- `53-VERIFICATION.md` BUNDLE-06 `verified-local-only` → `verified` refresh (cosmetic)
- Tier-C design-token cleanup waves (271 files / 2336 AST nodes / TBD-design-token-tier-c-cleanup-wave-N)
- Tier-C `follow_up_phase` placeholders → concrete ROADMAP entries
- Multi-Literal-on-one-line delta unreported (D-12 clause bookkeeping)
- D-22 visual baseline RTL flip (`?lng=ar` URL gate or post-load render wait) for `tasks-tab-visual.spec.ts`
- D-23 live tasks-tab Playwright run (host operator with seeded staging fixture per `52-FIXTURE.md`)
- Phase 39 `kanban-*.spec.ts` regression follow-up (out-of-scope deferral from Phase 52 D-21)
- `TweaksDrawer.test.tsx:6-8` stale comment about TEST-01 mock factory (documentation drift, no functional break)
- `51-VALIDATION.md` `status: draft` → `passed` frontmatter polish
- bad-design-token.tsx + bad-vi-mock.ts positive-failure CI assertion gap
- D-54-04-RLS-AUDIT-PRE-EXISTING-FAIL: pre-existing `countries` row in `sensitiveTables` (Phase 03/04 vintage)
- INTEL-01..05 API endpoints + ingestion pipeline + alerting + AI correlation (v7.0 feature work)

**Technical Debt Incurred:**

- 271 Tier-C design-token suppressions (per-Literal `eslint-disable-next-line`) — cleanup waves staged for v6.4
- 5 Phase 52 PASS-WITH-DEVIATION deviations (D-19..D-23) catalogued in 52-SUMMARY.md
- Phase 51 `status: draft` VALIDATION.md frontmatter (nyquist_compliant:true is correct)

---

_For current project status, see .planning/ROADMAP.md_
