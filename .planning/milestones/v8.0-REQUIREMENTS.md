# Requirements Archive: Intl-Dossier v8.0 Linear Design System Migration

> **ARCHIVED 2026-07-04** — milestone SHIPPED. All 24 requirements complete (24/24). This is the frozen v8.0 requirements record; the active `.planning/REQUIREMENTS.md` is recreated fresh at the next milestone. Audit: [v8.0-MILESTONE-AUDIT.md](v8.0-MILESTONE-AUDIT.md) (status: passed).

**Defined:** 2026-07-01 · **Shipped:** 2026-07-04
**Core Value:** Unified intelligence management for diplomatic operations — every relationship, commitment, and signal tracked in one secure, bilingual platform.

## v8.0 Requirements — all complete

Replace the IntelDossier prototype design system with a Linear-derived design language, on properly-configured shadcn/ui RTL infrastructure and HeroUI v3 (already installed at 3.0.5, bumped to 3.2.1), with Aceternity fully removed — while preserving Arabic RTL correctness throughout. A 2026-07-02 pre-execution review verified the tree was **already on the v3 compound-component API**, **none** of the "v3-removed" components were imported, and the Latin fonts were **already** Inter + JetBrains Mono — so AUDIT-02/03 and HEROUI-02 were confirmations, not migrations, and Phase 78 was a version bump + regression sweep. The genuine work was the Linear palette re-skin, the 8-component Aceternity rebuild (1 live + 7 dead), and RTL direction-source consolidation.

### UI Component Audit (Phase 75)

- [x] **AUDIT-01**: Every hand-rolled UI surface in `frontend/src/components/**` classified as replace-with-shadcn-primitive, keep-custom (domain-specific), or replace-with-shadcn-block. — **Outcome:** full classification artifact (rulebook header + per-directory + per-file tiers), coverage reconciled; domain-sensitive surfaces (clearance/RTL/flags/dossier-type) defaulted to keep-custom.
- [x] **AUDIT-02**: HeroUI usage confirmed already on the v3 compound-component API; any residual flat-prop call site listed (expected: none). — **Outcome:** confirmed compound-API across Card/Checkbox/Switch/Modal wrappers; zero flat-prop call sites; a Phase 78 re-run protocol was recorded.
- [x] **AUDIT-03**: Confirm none of the HeroUI v3-removed components (Navbar, Snippet, User, Spacer, Image, Code, Autocomplete, DateInput) are imported. — **Outcome:** 0 imports confirmed by review; no replacement plan needed.
- [x] **AUDIT-04**: The 8 Aceternity-styled form components inventoried with their RHF/Zod/ARIA/keyboard-focus behavioral contracts captured before rebuild. — **Outcome:** contracts captured + UserPicker facade; the 7/8-dead-code finding fed the Phase 79 rescope.

### RTL Infrastructure Bridge (Phase 76)

- [x] **RTLB-01**: layout/portal `dir` has exactly one owner (derived from `i18n.language`/`useDirection()`), consolidating the 4 scattered setters and bridged into both `<html>` and Radix's direction context. The 68 per-field `dir="rtl"` inputs on Arabic-only (`_ar`) fields are explicitly retained. — **Outcome:** single owner `ui/direction.tsx` + DirectionProvider Radix bridge; 4 legacy setters demoted; RTLWrapper deleted.
- [x] **RTLB-02**: Portal-based components (Popover/Tooltip/Dropdown/Sheet/dossier drawer) animate from the correct edge in both EN and AR. — **Outcome:** same-frame flip verified by `direction-portals` e2e.

### shadcn RTL Logical Properties (Phase 76)

- [x] **SRTL-01**: `pnpm dlx shadcn@latest migrate rtl` runs once against `components/ui/**`, is committed, and is never blindly re-run. — **Outcome:** one-shot reviewed isolated commit; `components.json` `rtl: true`.
- [x] **SRTL-02**: Calendar, Pagination, and Sidebar (CLI-exempt components) manually verified RTL-correct. — **Outcome:** AR verification record + human sign-off (approved 2026-07-02).
- [x] **SRTL-03**: A CI check prevents duplicate `rtl:*` utility classes from re-appearing. — **Outcome:** `check-duplicate-rtl.mjs` guard + fixture wired into lint-chain + CI positive-failure job.

### Linear Token System (Phase 77)

- [x] **TOKEN-01**: Dark (canonical) and light token sets derived from the Linear reference values in `.planning/research/STACK.md` and wired into `directions.ts`/`buildTokens.ts`/`applyTokens.ts`. — **Outcome:** transcribed verbatim; Design Token Check green (zero raw hex / Tailwind literals).
- [x] **TOKEN-02**: `bootstrap.js` byte-matches the new `directions.ts` literals, enforced by a CI guard. — **Outcome:** three-copy byte-match guard v2 (`directions.ts` ↔ `bootstrap.js` ↔ `index.css :root`) with coercion probes.
- [x] **TOKEN-03**: Form-error/warning colors + a 6-value status-tag palette derived in Linear's dark-surface luminance band (WCAG AA). — **Outcome:** palette gap-filled with measured AA ratios.
- [x] **TOKEN-04**: The 4-direction switcher retired from all sites (`tokens/types.ts`, `TweaksDrawer`, `Topbar`, `AppearanceSettingsSection`); Linear the sole direction; legacy persisted `id.dir` coerced to `linear` in `bootstrap.js` + `DesignProvider`; default `id.theme` explicitly dark-canonical. — **Outcome:** dual-layer coercion; hue axis also retired; `useHue`/`directionDefaults` deleted; `Direction` type → `'linear'`.
- [x] **TOKEN-05**: Latin stack is Inter (500/600/700) + JetBrains Mono; **Tajawal Arabic cascade preserved** for `dir="rtl"`. — **Outcome:** 3-family font system; no proprietary Linear fonts added.
- [x] **TOKEN-06**: `components/ui/*` primitives re-skinned per Linear's button/card/input recipes (no drop shadows, hairline borders, `surface-1..4` ladder); the `components/ui` ESLint color-literal carve-out gets an explicit keep-or-migrate decision. — **Outcome:** re-skin shipped in 77-GAPFIX; carve-out decision recorded.
- [x] **FOUC-01**: A CI script fails the build if `bootstrap.js` and `directions.ts` literals diverge. — **Outcome:** `check-bootstrap-parity.mjs` (byte-match, not type-check) added at the start of Phase 77.
- [x] **DOC-01**: Design source-of-truth migrated off Bureau — root `/CLAUDE.md` + `frontend/CLAUDE.md` design sections updated to Linear, `frontend/DESIGN.md` rewritten as the Linear spec, `inteldossier_handoff_design/` superseded. — **Outcome:** 3× CLAUDE.md rewritten + 321-line Linear DESIGN.md + supersession banner.

### HeroUI v3 API Audit + Bump (Phase 78)

- [x] **HEROUI-01**: `@heroui/react`/`@heroui/styles` bump 3.0.5 → 3.2.1. — **Outcome:** atomic lockstep bump; size-limit REQUIRED gate + EN/AR drawer render smoke green.
- [x] **HEROUI-02**: Any residual flat-prop HeroUI call site from AUDIT-02 converted to the compound pattern. — **Outcome:** closed on confirmation (tree already compound-API); heroui-forms toggles migrated to the v3.2 `*.Content` composition with a behavioral vitest oracle.

### Aceternity Removal (Phase 79)

- [x] **ACET-01**: The 8 Aceternity-based form components rebuilt on HeroUI v3/Radix primitives, preserving RHF validation, ARIA, and keyboard-focus. — **Outcome:** audit-verified reality was 1 live (SearchableSelect, rebuilt in place preserving the Phase 75 contract) + 7 dead (deleted); role="alert" EN+AR, keyboard order, and 12-attr ARIA + axe verified.
- [x] **ACET-02**: The `@aceternity-pro` registry entry removed from `components.json`. — **Outcome:** registry removed + repo residue purged (`.aceternity/` docs, README, comments, dead `ui/timeline.tsx`); inverted `no-restricted-imports` ban stays green.

### Full-Route Visual + A11y Verification (Phase 80)

- [x] **VERIFY-01**: All Playwright-baselined surfaces (EN+AR × dark+light) re-captured before the token phase and re-compared after; any route expansion called out. — **Outcome:** 43 baselines re-compared via a HUMAN blocking diff-triage (approved 2026-07-03): 39 intended-Linear + 4 within-tolerance + 0 regressions; Linear baselines recaptured (43/43 byte-stable), Bureau lineage recoverable at 14191cb85.
- [x] **VERIFY-02**: axe-core sweep passes across all 4 axes with no new violations vs a RECORDED pre-migration baseline. — **Outcome:** pre-token baseline recorded (set A @14191cb85, B ⊆ A); the 4 NEW Linear-light color-contrast scans fixed at the shared `.chip` recipe; a11y gate green (0 axe violations at CI parity).

### Bootstrap CI Guard + Smoke Suite (Phase 80 / FOUC-02)

- [x] **FOUC-02**: Portal-animation and Calendar/Pagination/Sidebar RTL smoke tests run in CI. — **Outcome:** `rtl-component-smokes.spec.ts` (Popover/Pagination/Sidebar, DOM/computed-style only) + `direction-portals.spec.ts` + clock-frozen `calendar-rtl.spec.ts` wired as the green-from-birth `test-rtl-smokes` job (advisory; branch-protection promotion deferred to repo-admin).

## Traceability

Every v8.0 requirement mapped to exactly one phase. Coverage: **24/24 Complete**.

| Requirement | Phase    | Status   |
| ----------- | -------- | -------- |
| AUDIT-01    | Phase 75 | Complete |
| AUDIT-02    | Phase 75 | Complete |
| AUDIT-03    | Phase 75 | Complete |
| AUDIT-04    | Phase 75 | Complete |
| RTLB-01     | Phase 76 | Complete |
| RTLB-02     | Phase 76 | Complete |
| SRTL-01     | Phase 76 | Complete |
| SRTL-02     | Phase 76 | Complete |
| SRTL-03     | Phase 76 | Complete |
| TOKEN-01    | Phase 77 | Complete |
| TOKEN-02    | Phase 77 | Complete |
| TOKEN-03    | Phase 77 | Complete |
| TOKEN-04    | Phase 77 | Complete |
| TOKEN-05    | Phase 77 | Complete |
| TOKEN-06    | Phase 77 | Complete |
| FOUC-01     | Phase 77 | Complete |
| DOC-01      | Phase 77 | Complete |
| HEROUI-01   | Phase 78 | Complete |
| HEROUI-02   | Phase 78 | Complete |
| ACET-01     | Phase 79 | Complete |
| ACET-02     | Phase 79 | Complete |
| VERIFY-01   | Phase 80 | Complete |
| VERIFY-02   | Phase 80 | Complete |
| FOUC-02     | Phase 80 | Complete |

**Note:** VERIFY-01 spanned two moments — the pre-token baseline capture (gating step of Phase 77) and the post-migration re-comparison (Phase 80, where it closed).

## Deferred (not in v8.0)

- **DESIGNOPS-01**: Figma/design-token sync tooling
- **DESIGNOPS-02**: Storybook-based component-level visual diffing (v6.1 deferral ADR still stands)

## Out of Scope (as shipped)

| Feature                                    | Reason                                                                 |
| ------------------------------------------ | ---------------------------------------------------------------------- |
| Reintroducing a multi-direction switcher   | Full replacement decision — Linear is the only direction going forward |
| Net-new HeroUI v2 dependency               | Already on v3; no reason to add v2 coexistence tooling                 |
| Feed ingestion, GPU/TEI stack, GAP-2/GAP-3 | Carried forward from v7.0, unrelated to design system work             |
| Mobile native app                          | Standing exclusion (cancelled)                                         |
