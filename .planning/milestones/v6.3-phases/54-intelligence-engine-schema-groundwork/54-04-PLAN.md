---
phase: 54
plan: 04
type: execute
wave: 4
depends_on: ['54-01', '54-02', '54-03']
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - backend/src/types/database.types.ts
  - frontend/src/types/database.types.ts
  - tests/security/rls-audit.test.ts
autonomous: true
requirements: [INTEL-01, INTEL-02, INTEL-03, INTEL-04, INTEL-05]
must_haves:
  truths:
    - 'D-04, D-07, D-08: REQUIREMENTS.md INTEL-01..05 use post-Phase-54 wording — `intelligence_event` (not `intelligence_signal`), `organization_id` (not `tenant_id` per D-08 semantic alias), 7 dossier types (not 8 — `elected_official` dropped per D-07).'
    - 'D-07: ROADMAP.md Phase 54 § success-criterion #3 reads `7` dossier types (not `8`); criterion #1 and #4 reference `intelligence_event` (not `intelligence_signal`).'
    - 'D-14: `backend/src/types/database.types.ts` and `frontend/src/types/database.types.ts` are byte-identical, regenerated from staging `zkrcjzdemdmwhearhfgg` via Supabase MCP `generate_typescript_types`, and contain the symbols `intelligence_event`, `intelligence_event_dossiers`, new-shape `intelligence_digest`, `dashboard_digest`, and `signal_source_type`. Type files are committed in the same wave as the regen — NOT a follow-up plan.'
    - 'D-14: `pnpm --filter backend type-check` AND `pnpm --filter frontend type-check` both exit 0 on the regenerated types.'
    - '`tests/security/rls-audit.test.ts` sensitiveTables list includes `intelligence_event`, `intelligence_digest`, AND `dashboard_digest`; the existing audit passes for all three.'
  artifacts:
    - path: 'backend/src/types/database.types.ts'
      provides: 'Regenerated Supabase TS types for backend workspace'
      contains: 'intelligence_event'
    - path: 'frontend/src/types/database.types.ts'
      provides: 'Regenerated Supabase TS types for frontend workspace (byte-identical to backend)'
      contains: 'intelligence_event'
    - path: '.planning/REQUIREMENTS.md'
      provides: 'INTEL-01..05 wording aligned to on-disk schema'
      contains: 'intelligence_event'
    - path: '.planning/ROADMAP.md'
      provides: 'Phase 54 success criteria aligned to on-disk schema (7 dossier types, intelligence_event)'
      contains: 'intelligence_event'
    - path: 'tests/security/rls-audit.test.ts'
      provides: 'sensitiveTables list extended with Phase-54 additions'
      contains: 'intelligence_event'
  key_links:
    - from: 'backend/src/types/database.types.ts'
      to: 'frontend/src/types/database.types.ts'
      via: 'byte-identical (cmp exits 0)'
      pattern: 'cmp'
    - from: 'tests/security/rls-audit.test.ts'
      to: 'public.intelligence_event / intelligence_digest / dashboard_digest'
      via: 'sensitiveTables array entry'
      pattern: "'intelligence_event'|'intelligence_digest'|'dashboard_digest'"
---

<objective>
Close Phase 54 by aligning spec text to the on-disk schema and regenerating Supabase TS types into both workspaces so `pnpm type-check` exits 0 everywhere. Patch REQUIREMENTS.md INTEL-01..05 (rename to `intelligence_event`, drop `elected_official`, swap `tenant_id` → `organization_id`); patch ROADMAP.md Phase 54 § success criteria (7 not 8; `intelligence_event` not `intelligence_signal`); regenerate `database.types.ts` via Supabase MCP and write to both workspaces; extend the existing `tests/security/rls-audit.test.ts` sensitiveTables list with the three new table names.

Purpose: Lands INTEL-05 (TS regen + dual type-check) and re-asserts INTEL-01..04 via the patched spec text. Closes the documentation-vs-on-disk drift created when the spec was written before the Phase-45 collision was discovered.

Output:

- `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` text patches.
- `backend/src/types/database.types.ts` + `frontend/src/types/database.types.ts` regenerated and byte-identical.
- `tests/security/rls-audit.test.ts` sensitiveTables extended.
- Both workspaces' `tsc --noEmit` exit 0.
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/54-intelligence-engine-schema-groundwork/54-CONTEXT.md
@.planning/phases/54-intelligence-engine-schema-groundwork/54-RESEARCH.md
@.planning/phases/54-intelligence-engine-schema-groundwork/54-PATTERNS.md
@.planning/phases/54-intelligence-engine-schema-groundwork/54-VALIDATION.md

# Files being patched — read current state before touching

@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@tests/security/rls-audit.test.ts

# Outputs from the three preceding plans — provide the symbols that must appear in regenerated types

@.planning/phases/54-intelligence-engine-schema-groundwork/54-01-PLAN.md
@.planning/phases/54-intelligence-engine-schema-groundwork/54-02-PLAN.md
@.planning/phases/54-intelligence-engine-schema-groundwork/54-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch REQUIREMENTS.md INTEL-01..05 + ROADMAP.md Phase 54 § text</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md full file (83 lines — read all)
    - .planning/ROADMAP.md L255-269 (Phase 54 § including the 5 success-criterion bullets)
    - 54-CONTEXT.md D-04, D-07, D-08 (the three decisions driving these patches)
    - 54-RESEARCH.md § "Phase Requirements" L51-63 (post-Phase-54 wording — copy into INTEL-01..05)
    - 54-PATTERNS.md L536-557 (REQUIREMENTS.md + ROADMAP.md text-patch summary)
  </read_first>
  <action>
    (a) Patch `.planning/REQUIREMENTS.md`. Edit ONLY the INTEL section (currently L36-42 in the file as read; reconfirm offsets via Read before editing). Apply these exact transformations:

        - L38 (INTEL-01): `intelligence_signal` → `intelligence_event`; `tenant_id` → `organization_id`. New line per the post-Phase-54 wording in 54-RESEARCH.md L57 (copy verbatim).
        - L39 (INTEL-02): `tenant_id` → `organization_id`. Wording per 54-RESEARCH.md L58.
        - L40 (INTEL-03): `intelligence_signal_dossiers` → `intelligence_event_dossiers`; `8 existing dossier types` → `7 canonical dossier types`. Wording per 54-RESEARCH.md L59. Drop `elected_official` if listed inline.
        - L41 (INTEL-04): `intelligence_signal.source_type` → `intelligence_event.source_type` (enum NAME `signal_source_type` stays per D-10). Wording per 54-RESEARCH.md L60.
        - L42 (INTEL-05): leave intact unless wording references stale table names — confirm via the source line.

        Do NOT touch any other section (no edits to KANBAN, DESIGN, TEST, BUNDLE blocks).

    (b) Patch `.planning/ROADMAP.md` Phase 54 § (around L255-269 — reconfirm offsets via Read before editing; line numbers may have drifted since planning). Apply these exact transformations:

        - Success-criterion #1 (L263): `intelligence_signal` → `intelligence_event`; `tenant_id` → `organization_id`. Match 54-RESEARCH.md L57 wording.
        - Success-criterion #3 (L265-266): `intelligence_signal_dossiers` → `intelligence_event_dossiers`; `8 existing dossier types (...)` → `7 canonical dossier types (country, organization, forum, engagement, topic, working_group, person)`. Drop `elected_official`.
        - Success-criterion #4 (L267): `intelligence_signal.source_type` → `intelligence_event.source_type`. Keep enum name `signal_source_type`.
        - The "Phase 54: Intelligence Engine Schema Groundwork" summary line in the v6.3 section (L137): `intelligence_signal` → `intelligence_event`.
        - The Phase 54 § header description (L258): `intelligence_signal` → `intelligence_event`.

        Do NOT touch any other phase section (no edits to Phase 50/51/52/53 entries beyond what is required by the rules above).

    Pitfall reminders:
    - Pitfall 4 — DO NOT touch any literal `intelligence_signal` or `intelligence_signals` outside these two files. Specifically: no edits to `backend/src/`, `frontend/src/`, `frontend/src/i18n/`. Confirm with `git diff` before commit.
    - The substring `intelligence_signals` (plural) MAY still appear in REQUIREMENTS / ROADMAP — those references to the existing curated table are correct and must be preserved.

  </action>
  <verify>
    <automated>! grep -E "intelligence_signal\b" .planning/REQUIREMENTS.md | grep -v "intelligence_signals" &amp;&amp; ! grep -E "intelligence_signal\b" .planning/ROADMAP.md | grep -v "intelligence_signals" &amp;&amp; ! grep "elected_official" .planning/REQUIREMENTS.md &amp;&amp; ! grep "8 existing dossier types" .planning/ROADMAP.md &amp;&amp; grep -q "intelligence_event" .planning/REQUIREMENTS.md &amp;&amp; grep -q "intelligence_event" .planning/ROADMAP.md &amp;&amp; grep -q "intelligence_event_dossiers" .planning/REQUIREMENTS.md &amp;&amp; grep -q "intelligence_event_dossiers" .planning/ROADMAP.md &amp;&amp; grep -q "organization_id" .planning/REQUIREMENTS.md &amp;&amp; grep -q "7 canonical dossier types\|7 dossier types" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "intelligence_signal\b" .planning/REQUIREMENTS.md | grep -v "intelligence_signals"` returns 0 lines (no remaining singular `intelligence_signal` references that aren't part of the plural curated-table name).
    - `grep -E "intelligence_signal\b" .planning/ROADMAP.md | grep -v "intelligence_signals"` returns 0 lines.
    - `grep "elected_official" .planning/REQUIREMENTS.md` returns 0 lines.
    - `grep "8 existing dossier types" .planning/ROADMAP.md` returns 0 lines.
    - `grep -c "intelligence_event" .planning/REQUIREMENTS.md` returns ≥ 3 (one per INTEL-01, INTEL-03, INTEL-04 ref).
    - `grep -c "intelligence_event" .planning/ROADMAP.md` returns ≥ 3 (Phase 54 success criteria + description).
    - `grep -c "intelligence_event_dossiers" .planning/REQUIREMENTS.md` returns ≥ 1.
    - `grep -c "intelligence_event_dossiers" .planning/ROADMAP.md` returns ≥ 1.
    - `grep -c "organization_id" .planning/REQUIREMENTS.md` returns ≥ 2 (INTEL-01 + INTEL-02).
    - `grep -E "7 canonical dossier types|7 dossier types" .planning/ROADMAP.md` returns ≥ 1 (Phase 54 success-criterion #3).
    - `git diff --name-only HEAD -- backend/src frontend/src/i18n frontend/src/types/preview-layout.types.ts frontend/src/types/multilingual-content.types.ts frontend/src/components/entity-links/ backend/src/api/entity-search.ts backend/src/services/entity-search.service.ts backend/src/services/link.service.ts` returns no lines (Pitfall 4 — no collateral edits in app code).
  </acceptance_criteria>
  <done>REQUIREMENTS.md INTEL section + ROADMAP.md Phase 54 § match on-disk schema (post-rename, 7 dossier types, organization_id); no collateral edits to app source.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate database.types.ts via Supabase MCP + write to both workspaces + dual type-check</name>
  <files>backend/src/types/database.types.ts, frontend/src/types/database.types.ts</files>
  <read_first>
    - 54-RESEARCH.md § "Pattern 4: TypeScript regeneration workflow (D-14)" L375-395
    - 54-PATTERNS.md § "backend/src/types/database.types.ts + frontend/src/types/database.types.ts" L517-533
    - 54-CONTEXT.md D-14 (regen flow, dual workspace, type-check 0)
    - backend/package.json L16 (`"type-check": "tsc --noEmit"`)
    - frontend/package.json L19 (`"type-check": "tsc --noEmit"`)
  </read_first>
  <action>
    (a) Confirm plans 54-01, 54-02, 54-03 have all been applied to staging — `mcp__claude_ai_Supabase__list_tables(schemas=["public"])` MUST return a list containing all of `dashboard_digest`, `intelligence_event`, `intelligence_digest`, `intelligence_event_dossiers`. If any are missing, stop and surface the gap (regenerating against a partial schema produces stale types — Pitfall 2).

    (b) Call `mcp__claude_ai_Supabase__generate_typescript_types(project_id="zkrcjzdemdmwhearhfgg")` to obtain the regenerated TypeScript module as a single string.

    (c) Write the returned string verbatim to BOTH:
        - `backend/src/types/database.types.ts`
        - `frontend/src/types/database.types.ts`

        Both files MUST be byte-identical (D-14 / Pitfall 5).

    (d) Sanity-grep — all 5 strings must be found in `frontend/src/types/database.types.ts`:
        - `intelligence_event:`
        - `intelligence_event_dossiers:`
        - `intelligence_digest:`
        - `dashboard_digest:`
        - `signal_source_type`

    (e) Confirm byte-identity: `cmp backend/src/types/database.types.ts frontend/src/types/database.types.ts` MUST exit 0.

    (f) Run both type-checks (D-14 — both MUST exit 0 in this same wave):
        `pnpm --filter backend type-check` → exit 0
        `pnpm --filter frontend type-check` → exit 0

    (g) If either type-check fails, the most likely cause (per Pitfall 2) is the rename in plan 54-01 did not fully propagate (e.g., a residual `.from('intelligence_digest')` call somewhere using the OLD Phase-45 shape, but type-check now sees the NEW shape). Diagnose by reading the tsc error output; if it points to a known callsite from plan 54-01, that's a plan-54-01 regression that must be fixed there. Do not bandaid.

    Pitfall reminders:
    - Both files MUST be byte-identical. There is no automated drift check in CI (Pitfall 5 / 54-RESEARCH.md L386, L467-472). Use `cmp` after every write to confirm.
    - The MCP output is the source of truth. Do NOT hand-edit the file (Don't Hand-Roll, 54-RESEARCH.md L414).
    - Regen runs ONCE, after all three migrations are applied. Do NOT regen mid-phase (Pitfall 2 / L444-447).

  </action>
  <verify>
    <automated>test -f backend/src/types/database.types.ts &amp;&amp; test -f frontend/src/types/database.types.ts &amp;&amp; cmp backend/src/types/database.types.ts frontend/src/types/database.types.ts &amp;&amp; grep -q "intelligence_event:" frontend/src/types/database.types.ts &amp;&amp; grep -q "intelligence_event_dossiers:" frontend/src/types/database.types.ts &amp;&amp; grep -q "intelligence_digest:" frontend/src/types/database.types.ts &amp;&amp; grep -q "dashboard_digest:" frontend/src/types/database.types.ts &amp;&amp; grep -q "signal_source_type" frontend/src/types/database.types.ts &amp;&amp; pnpm --filter backend type-check &amp;&amp; pnpm --filter frontend type-check</automated>
  </verify>
  <acceptance_criteria>
    - Pre-regen MCP `list_tables(schemas=["public"])` returns a list containing ALL of: `dashboard_digest`, `intelligence_event`, `intelligence_digest`, `intelligence_event_dossiers`.
    - Both `backend/src/types/database.types.ts` and `frontend/src/types/database.types.ts` exist.
    - `cmp backend/src/types/database.types.ts frontend/src/types/database.types.ts` exits 0 (byte-identical).
    - `grep -q "intelligence_event:" frontend/src/types/database.types.ts` exits 0.
    - `grep -q "intelligence_event_dossiers:" frontend/src/types/database.types.ts` exits 0.
    - `grep -q "intelligence_digest:" frontend/src/types/database.types.ts` exits 0.
    - `grep -q "dashboard_digest:" frontend/src/types/database.types.ts` exits 0.
    - `grep -q "signal_source_type" frontend/src/types/database.types.ts` exits 0.
    - `pnpm --filter backend type-check` exits 0.
    - `pnpm --filter frontend type-check` exits 0.
  </acceptance_criteria>
  <done>Types regenerated from staging, written byte-identically to both workspaces, all 5 sanity greps pass, both type-checks exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: Extend tests/security/rls-audit.test.ts sensitiveTables with Phase-54 additions + run audit</name>
  <files>tests/security/rls-audit.test.ts</files>
  <read_first>
    - tests/security/rls-audit.test.ts full file (~110 lines — read all). Look specifically at the `sensitiveTables` array around L66-79 and the `hasOrgPolicy` assertion around L99-109.
    - 54-PATTERNS.md § "tests/security/rls-audit.test.ts" L485-513 (extension recipe + note on junction)
    - 54-VALIDATION.md (full file — for sampling-rate context)
  </read_first>
  <action>
    (a) Open `tests/security/rls-audit.test.ts`. Locate the `sensitiveTables` array (anchor: the existing entries `'intelligence_signals'`, `'commitments'`, `'tasks'`, etc. per 54-PATTERNS.md L491-503).

    (b) Add three new entries at the end of the array, preserving existing trailing commas + formatting:
        - `'intelligence_event'`           (NEW per D-04)
        - `'intelligence_digest'`          (NEW spec shape per INTEL-02)
        - `'dashboard_digest'`             (RENAMED from intelligence_digest per D-01)

        Add inline comments per 54-PATTERNS.md L504-507 (`// Phase 54 additions:` above the new entries).

    (c) Do NOT add `intelligence_event_dossiers` to `sensitiveTables`. The junction has no `organization_id` of its own (D-13) — the existing `get_tables_without_rls()` RPC auto-catches it for the RLS-enabled assertion (54-PATTERNS.md L513).

    (d) Run the audit:
        `pnpm --filter backend test tests/security/rls-audit.test.ts --run` (or `pnpm vitest run tests/security/rls-audit.test.ts` if the backend filter doesn't pick up that path — match whichever runner resolves the file).

    (e) The audit MUST exit 0 — the three new tables already have `organization_id`-scoped RLS from plans 54-01 (rename preserved policies) and 54-02 (new tables created with the 4-policy template). The junction (not in sensitiveTables, but caught by the separate `get_tables_without_rls` check) MUST also pass because plan 54-03 enabled RLS.

    Pitfall reminders:
    - Do NOT modify the `hasOrgPolicy` assertion or any other test logic. Pure array extension.
    - Do NOT add `intelligence_event_dossiers` to `sensitiveTables` (D-13 — different shape; junction is exempt from the `organization_id` policy check).

  </action>
  <verify>
    <automated>grep -q "'intelligence_event'" tests/security/rls-audit.test.ts &amp;&amp; grep -q "'intelligence_digest'" tests/security/rls-audit.test.ts &amp;&amp; grep -q "'dashboard_digest'" tests/security/rls-audit.test.ts &amp;&amp; ! grep -q "'intelligence_event_dossiers'" tests/security/rls-audit.test.ts &amp;&amp; ( pnpm --filter backend test tests/security/rls-audit.test.ts --run || pnpm vitest run tests/security/rls-audit.test.ts )</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "'intelligence_event'" tests/security/rls-audit.test.ts` exits 0 (literal added).
    - `grep -F "'intelligence_digest'" tests/security/rls-audit.test.ts` exits 0.
    - `grep -F "'dashboard_digest'" tests/security/rls-audit.test.ts` exits 0.
    - `grep -F "'intelligence_event_dossiers'" tests/security/rls-audit.test.ts` returns NOT found (D-13 — junction NOT in sensitiveTables).
    - The `sensitiveTables` array is the only block changed (`git diff tests/security/rls-audit.test.ts` shows additions ONLY inside the array literal — no edits to `describe`, `it`, `expect`, or assertion helpers).
    - Vitest invocation (`pnpm --filter backend test tests/security/rls-audit.test.ts --run` or `pnpm vitest run tests/security/rls-audit.test.ts`) exits 0.
  </acceptance_criteria>
  <done>Three Phase-54 table names added to sensitiveTables; junction intentionally excluded; rls-audit test exits 0.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                        | Description                                                                                         |
| ----------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| MCP generate_typescript_types → workspace types | Generator output is source of truth for TS types in both workspaces                                 |
| REQUIREMENTS/ROADMAP text → future readers      | Spec wording becomes the canonical reference for downstream phases                                  |
| rls-audit test → all Phase-54 tables            | Existing CI gate (when audit runs in CI) catches any regression that disables RLS on the new tables |

## STRIDE Threat Register

| Threat ID | Category               | Component                                | Disposition | Mitigation Plan                                                                                                                                              |
| --------- | ---------------------- | ---------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-54-04-T | Tampering              | hand-edited types                        | mitigate    | MCP is the only writer; verified by `cmp` byte-identity + grep for all 5 expected symbols + dual `tsc --noEmit` exit 0                                       |
| T-54-04-I | Information disclosure | RLS regression on new tables             | mitigate    | rls-audit test now covers `intelligence_event`, `intelligence_digest`, `dashboard_digest`; junction caught by separate `get_tables_without_rls` audit (D-13) |
| T-54-04-R | Repudiation            | "spec says X, code does Y" drift         | mitigate    | REQUIREMENTS + ROADMAP patched to match on-disk schema (D-04, D-07, D-08); future phases reference the corrected wording                                     |
| T-54-04-D | DoS                    | partial type regen (one workspace stale) | mitigate    | `cmp` check inside Task 2 acceptance criteria; both workspaces' type-check MUST exit 0 in the same wave (D-14 / Pitfall 5)                                   |

</threat_model>

<verification>
After all tasks complete in this plan:

1. `grep -E "intelligence_signal\b" .planning/REQUIREMENTS.md .planning/ROADMAP.md | grep -v "intelligence_signals"` returns 0 lines.
2. `grep "elected_official" .planning/REQUIREMENTS.md` returns 0 lines.
3. `cmp backend/src/types/database.types.ts frontend/src/types/database.types.ts` exits 0.
4. All 5 grep symbols (`intelligence_event:`, `intelligence_event_dossiers:`, `intelligence_digest:`, `dashboard_digest:`, `signal_source_type`) appear in both type files.
5. `pnpm --filter backend type-check` exits 0.
6. `pnpm --filter frontend type-check` exits 0.
7. `pnpm --filter backend test tests/security/rls-audit.test.ts --run` (or `pnpm vitest run tests/security/rls-audit.test.ts`) exits 0.
8. `git diff` shows zero edits to `backend/src/`, `frontend/src/i18n/`, `frontend/src/components/entity-links/`, `frontend/src/types/preview-layout.types.ts`, `frontend/src/types/multilingual-content.types.ts`, `backend/src/api/entity-search.ts`, `backend/src/services/entity-search.service.ts`, `backend/src/services/link.service.ts` (Pitfall 4 — no collateral).
   </verification>

<success_criteria>

- REQUIREMENTS.md + ROADMAP.md text matches on-disk schema (`intelligence_event`, `organization_id`, 7 dossier types).
- `database.types.ts` regenerated in both workspaces, byte-identical, contains all Phase-54 schema symbols.
- Both `pnpm type-check` invocations exit 0.
- `rls-audit.test.ts` extended with three new table names and passes.
- Zero collateral edits to `intelligence_signal` (singular) references in app code (Pitfall 4 contained).
  </success_criteria>

<output>
Create `.planning/phases/54-intelligence-engine-schema-groundwork/54-04-SUMMARY.md` when done.
</output>
