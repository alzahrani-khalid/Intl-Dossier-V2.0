---
phase: 52-heroui-v3-kanban-migration
plan: 04
type: execute
wave: 4
depends_on:
  - 52-03
files_modified:
  - eslint.config.mjs
  - frontend/package.json
  - frontend/pnpm-lock.yaml
  - scripts/check-deleted-components.sh
  - .planning/phases/51-design-token-compliance-gate/51-DESIGN-AUDIT.md
files_deleted:
  - frontend/src/components/kibo-ui/kanban/index.tsx
autonomous: true
requirements:
  - KANBAN-03
tags:
  - kanban
  - cleanup
  - eslint
  - dependency-removal

must_haves:
  truths:
    - frontend/src/components/kibo-ui/kanban/ directory does not exist
    - tunnel-rat is absent from frontend/package.json (and pnpm-lock.yaml)
    - eslint.config.mjs no-restricted-imports.patterns.group includes '@/components/kibo-ui/*'
    - eslint.config.mjs no longer carries the Phase 48 D-02 narrowing comment block (lines 122-132 in the pre-Plan-04 file)
    - eslint.config.mjs no longer carries the `frontend/src/components/**/index.tsx` carve-out at the pre-Plan-04 line 332
    - The eslint regression fixture at tools/eslint-fixtures/bad-kibo-ui-import.tsx now triggers a lint violation when linted in isolation
    - scripts/check-deleted-components.sh asserts the absence of frontend/src/components/kibo-ui/kanban/
    - 51-DESIGN-AUDIT.md TasksTab row is updated to reflect Phase 52 resolution
    - pnpm --filter frontend lint and pnpm --filter frontend type-check exit 0 workspace-wide
  artifacts:
    - path: eslint.config.mjs
      provides: Widened no-restricted-imports ban; carve-out removed
      contains: "'@/components/kibo-ui/*'"
    - path: scripts/check-deleted-components.sh
      provides: CI assertion that kibo-ui kanban dir is gone
      contains: 'kibo-ui/kanban'
    - path: .planning/phases/51-design-token-compliance-gate/51-DESIGN-AUDIT.md
      provides: TasksTab row updated to "Resolved during Phase 52 migration" subsection
      contains: 'Resolved during Phase 52'
  key_links:
    - from: eslint.config.mjs
      to: tools/eslint-fixtures/bad-kibo-ui-import.tsx
      via: no-restricted-imports.patterns rule now fires on the fixture
      pattern: '@/components/kibo-ui'
    - from: scripts/check-deleted-components.sh
      to: frontend/src/components/kibo-ui/kanban
      via: file-existence assertion (CI gate)
      pattern: 'kibo-ui/kanban'
---

<objective>
Execute CONTEXT D-18 cleanup phase: delete the kibo-ui kanban module, widen the ESLint ban to prevent re-introduction, drop the Phase 48 D-02 narrowing comment block + index.tsx carve-out (no longer needed), remove the `tunnel-rat` npm dependency (D-19), wire the CI deletion check, and close out the Phase 51 audit row (D-20). This plan is "make the migration permanent."

Purpose: After Plan 03 made the kibo-ui module dead code (zero consumers), this plan turns deletion into a one-way street — ESLint blocks re-introduction, CI asserts the dir is gone, the npm dep is removed from the lockfile, and the audit-trail row reflects closure. The ESLint regression fixture from Plan 01 starts firing as designed.

Output: Five files modified, one directory deleted, one npm dep stripped. No production-code changes outside the deletion target. Plan 05 inherits a clean slate for visual baseline regeneration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-heroui-v3-kanban-migration/52-CONTEXT.md
@.planning/phases/52-heroui-v3-kanban-migration/52-PATTERNS.md
@.planning/phases/52-heroui-v3-kanban-migration/52-RESEARCH.md
@.planning/phases/52-heroui-v3-kanban-migration/52-VALIDATION.md
@eslint.config.mjs
@frontend/package.json
@scripts/check-deleted-components.sh
@.planning/phases/51-design-token-compliance-gate/51-DESIGN-AUDIT.md
@tools/eslint-fixtures/bad-kibo-ui-import.tsx

<interfaces>
<!-- ESLint widening — current state of patterns.group at eslint.config.mjs:148-163 -->
```js
patterns: [
  {
    group: [
      'aceternity-ui/*',
      '@aceternity/*',
      'kibo-ui/*',
      '@kibo-ui/*',
      '@/components/ui/3d-card',
      '@/components/ui/bento-grid',
      '@/components/ui/floating-navbar',
      '@/components/ui/link-preview',
    ],
    message: '...',
  },
],
```

<!-- Target state (D-18 step 5 widening): insert '@/components/kibo-ui/*' after '@kibo-ui/*' -->

<!-- Lines to DELETE — Phase 48 D-02 narrowing comment block at lines 122-132 (current file) -->
<!-- The block explains why the LOCAL alias kibo-ui ban was deferred to Phase 52. Phase 52 closes the deferral; the comment is obsolete. -->

<!-- Line 332 carve-out (current file) -->

'frontend/src/components/\*\*/index.tsx', // 48-02 scope-expansion: index.tsx re-exports follow same pattern as index.ts (catches kibo-ui/kanban/index.tsx)

<!-- This carve-out existed SPECIFICALLY for kibo-ui/kanban/index.tsx; after deletion it has no consumer and can be removed. -->

<!-- Verify no other index.tsx file under frontend/src/components/ depends on the carve-out (Pitfall 7): -->
<!-- Run: find frontend/src/components -name "index.tsx" -not -path "*/kibo-ui/*" -->
<!-- Expected: empty (no other consumer). If non-empty, the carve-out must be narrowed instead of fully deleted. -->
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete kibo-ui kanban directory + remove tunnel-rat dependency</name>
  <files>
    frontend/src/components/kibo-ui/kanban/index.tsx
    frontend/package.json
    frontend/pnpm-lock.yaml
  </files>
  <read_first>
    - frontend/src/components/kibo-ui/kanban/index.tsx (about to delete — verify no consumers via grep first)
    - frontend/package.json line 115 (`"tunnel-rat": "^0.1.2"`)
    - .planning/phases/52-heroui-v3-kanban-migration/52-CONTEXT.md D-18 step 4 + D-19
    - .planning/phases/52-heroui-v3-kanban-migration/52-RESEARCH.md §"State of the Art" + Assumption A2 (tunnel-rat replacement validated)
  </read_first>
  <action>
    Step 1 — pre-deletion sanity check:
      `grep -rln "@/components/kibo-ui" frontend/src/ frontend/tests/ 2>/dev/null | grep -v "frontend/src/components/kibo-ui/"` — must return 0 results. (The grep excludes the kibo-ui directory itself; any other match means a consumer was missed in Plan 03 and the deletion would break the build.) If results return, STOP and report the leftover consumer; do NOT proceed.

      Also verify `tools/eslint-fixtures/bad-kibo-ui-import.tsx` is OUTSIDE `frontend/src/` (the fixture deliberately imports from the doomed path but is not part of the frontend lint sweep until after the ban widens — confirm via `ls tools/eslint-fixtures/bad-kibo-ui-import.tsx`). The fixture's banned import resolving to a non-existent module after deletion is FINE — ESLint's `no-restricted-imports` runs at static-analysis time and fires before module resolution.

    Step 2 — delete the kibo-ui kanban directory:
      `rm -rf frontend/src/components/kibo-ui/kanban/`
      The parent `frontend/src/components/kibo-ui/` directory may now be empty — check with `ls frontend/src/components/kibo-ui/`. If empty, remove it too: `rmdir frontend/src/components/kibo-ui/`. If it contains other vendored kibo-ui modules, leave them alone (Phase 52 scope is `kanban` only).

    Step 3 — remove tunnel-rat dependency:
      `cd frontend && pnpm remove tunnel-rat` (do NOT use `pnpm remove tunnel-rat --filter frontend` from repo root — pnpm-lock.yaml at the frontend workspace level updates more cleanly with cwd inside the workspace).
      Verify: `grep -c "tunnel-rat" frontend/package.json` returns 0. `grep -c "tunnel-rat" frontend/pnpm-lock.yaml` returns 0 (or only stale comments inside the lockfile header — execute `pnpm install` once more if lockfile drift suspected).

    Step 4 — smoke build:
      `pnpm --filter frontend type-check 2>&1 | tail -10` — must exit 0.
      `pnpm --filter frontend build 2>&1 | tail -10` — must exit 0. Bundle composition shift is expected (one external dep removed); Phase 53 owns the bundle ceiling recalibration.

    If any step fails, restore via `git restore frontend/package.json frontend/pnpm-lock.yaml && git restore --staged frontend/src/components/kibo-ui/kanban/ && git checkout -- frontend/src/components/kibo-ui/kanban/` and investigate. The kibo-ui module is still in git history — recovery is one command.

  </action>
  <verify>
    <automated>test ! -d frontend/src/components/kibo-ui/kanban &amp;&amp; ! grep -q "tunnel-rat" frontend/package.json &amp;&amp; pnpm --filter frontend type-check 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    Directory `frontend/src/components/kibo-ui/kanban/` does not exist. `tunnel-rat` is gone from `frontend/package.json` and `frontend/pnpm-lock.yaml`. Workspace type-check and build still exit 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Widen ESLint no-restricted-imports + drop the Phase 48 D-02 carve-out and comment block</name>
  <files>
    eslint.config.mjs
  </files>
  <read_first>
    - eslint.config.mjs lines 115-200 (current `no-restricted-imports` block AND surrounding context)
    - eslint.config.mjs line ~332 (the `frontend/src/components/**/index.tsx` carve-out — pre-Plan-04 line number; may shift after Task 1's frontend/package.json edit)
    - .planning/phases/52-heroui-v3-kanban-migration/52-PATTERNS.md §"eslint.config.mjs (modified)" (lines 808-878)
    - .planning/phases/52-heroui-v3-kanban-migration/52-CONTEXT.md D-18 step 5
    - .planning/phases/52-heroui-v3-kanban-migration/52-RESEARCH.md §"ESLint `no-restricted-imports.patterns` widening (D-18 step 5)" + Pitfall 7
  </read_first>
  <action>
    Three surgical edits to eslint.config.mjs.

    Edit 1 — widen `patterns.group` to ban the local kibo-ui path:
      Locate the patterns array (currently at lines 148-163). Insert `'@/components/kibo-ui/*'` as a NEW entry, ordered alphabetically/semantically between `'@kibo-ui/*'` and `'@/components/ui/3d-card'`. Final array shape:
      ```js
      group: [
        'aceternity-ui/*',
        '@aceternity/*',
        'kibo-ui/*',
        '@kibo-ui/*',
        '@/components/kibo-ui/*',  // Phase 52 D-18 — local kibo-ui ban (was deferred by Phase 48 D-02)
        '@/components/ui/3d-card',
        '@/components/ui/bento-grid',
        '@/components/ui/floating-navbar',
        '@/components/ui/link-preview',
      ],
      ```

    Edit 2 — replace the 48-02 narrowing comment block (lines 122-132 in the pre-Plan-04 file):
      Delete the entire multi-line comment block from "48-02 narrowing: minimatch in `patterns.group`..." down through "...tracked as a deferred follow-up item in the SUMMARY." (≈11 lines of comments).
      Replace with two lines:
      ```js
      // CLAUDE.md primitive cascade (HeroUI v3 → Radix → custom).
      // Aceternity and Kibo UI are banned without explicit user request.
      ```
      Preserve the existing 2-line comment header above the deleted block (lines 122-123) — those state the rule and are still accurate.

    Edit 3 — Pitfall 7 verification BEFORE removing the index.tsx carve-out:
      Run `find frontend/src/components -name "index.tsx" -not -path "*/kibo-ui/*" 2>/dev/null`. Expected: empty output. If results return, the carve-out is still load-bearing for those files and can NOT be removed; instead, narrow it to specifically the surviving path(s) and document the narrowing in eslint.config.mjs as an inline comment + 52-04-SUMMARY.md.
      If the find returns empty (expected per Plan 02 confirmation that no other components/**/index.tsx exists), DELETE the carve-out line entirely. The line is currently at ~line 332 of the pre-Plan-04 file (verify exact line via `grep -n "48-02 scope-expansion" eslint.config.mjs`). Delete the entire array entry (just the one line — `'frontend/src/components/**/index.tsx', // 48-02 scope-expansion: ...`).

    Edit 4 — workspace lint gate:
      `pnpm --filter frontend lint 2>&1 | tail -20` — must exit 0. The new pattern ban must NOT fire on any file in `frontend/src/` (Plan 03 ensured no consumer imports remain). If it fires on a `frontend/src/` file, that file slipped through Plan 03's grep audit — fix the import path there before declaring Task 2 done.

    Edit 5 — fixture-fire verification:
      `pnpm --filter frontend lint --no-cache tools/eslint-fixtures/bad-kibo-ui-import.tsx 2>&1` — should exit NON-ZERO. The fixture's deliberate `@/components/kibo-ui/kanban` import now triggers the widened ban. If exit code is 0, the widening didn't take effect — re-check Edit 1.

    Edit 6 — companion Vitest fixture test (created in Plan 01):
      `cd frontend && pnpm exec vitest run src/components/kanban/__tests__/eslint-ban.test.ts` — must turn GREEN (the test was RED in Wave 0; widening now makes the lint shell-out exit non-zero, which the test asserts).

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/CodingSpace/Intl-Dossier-V2.0 &amp;&amp; grep -q "'@/components/kibo-ui/\*'" eslint.config.mjs &amp;&amp; ! grep -q "48-02 scope-expansion" eslint.config.mjs &amp;&amp; pnpm --filter frontend lint --no-cache tools/eslint-fixtures/bad-kibo-ui-import.tsx 2>&amp;1 | tail -5; echo "exit=$?"</automated>
  </verify>
  <done>
    eslint.config.mjs widens the ban to include `@/components/kibo-ui/*`. The 48-02 narrowing comment block is gone (replaced with a 2-line rule reminder). The index.tsx carve-out is either gone (Pitfall 7 cleared) or narrowed (Pitfall 7 hit, with documented narrowing). Workspace `pnpm lint` exits 0. The bad-kibo-ui-import fixture file produces a lint violation when linted directly. The Plan-01 Vitest fixture-test turns GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire CI deletion-check + close Phase 51 audit row</name>
  <files>
    scripts/check-deleted-components.sh
    .planning/phases/51-design-token-compliance-gate/51-DESIGN-AUDIT.md
  </files>
  <read_first>
    - scripts/check-deleted-components.sh (current 17 lines of kanban/kibo-ui-related checks)
    - .planning/phases/51-design-token-compliance-gate/51-DESIGN-AUDIT.md (entire file or at minimum the TasksTab row section)
    - .planning/phases/52-heroui-v3-kanban-migration/52-CONTEXT.md D-20
  </read_first>
  <action>
    Edit 1 — append CI deletion-check to scripts/check-deleted-components.sh:
      Read the existing script's pattern (it likely uses `if [ -d "frontend/src/path" ]; then echo "fail"; fi`-style assertions per Phase 39 precedent). Append a new check at the bottom (BEFORE any final `exit 0` if present):
      ```bash
      # Phase 52 KANBAN-03: kibo-ui kanban directory must remain deleted post-Plan-04
      if [ -d "frontend/src/components/kibo-ui/kanban" ]; then
        echo "FAIL: frontend/src/components/kibo-ui/kanban/ re-introduced (banned per Phase 52 D-18)"
        exit 1
      fi
      ```
      Run the script: `bash scripts/check-deleted-components.sh; echo "exit=$?"` — must exit 0 (the directory is gone after Task 1).

    Edit 2 — close Phase 51 audit row (D-20):
      Open `.planning/phases/51-design-token-compliance-gate/51-DESIGN-AUDIT.md`. Find the row(s) referring to `TasksTab.tsx` (likely 5 row entries for the 5 Tier-C STAGE_COLORS disables) AND, if Plan 03 Task 3 absorbed KanbanTaskCard's 4 disables, the rows for `KanbanTaskCard.tsx` (4 row entries for SLA color literals).

      For TasksTab rows: either strike through (using Markdown `~~ ~~`) OR move them under a new H2 section `## Resolved during Phase 52 migration` at the bottom of the file. Append the date and commit-hash placeholder (executor fills in actual commit SHA from `git log -1 --format=%H` after Plan 03 commit lands):
      ```markdown
      ## Resolved during Phase 52 migration

      Five Tier-C `eslint-disable-next-line no-restricted-syntax` directives in `frontend/src/pages/engagements/workspace/TasksTab.tsx` (formerly lines 35, 37, 39, 41, 43 — STAGE_COLORS palette literals) were resolved by deletion-of-need per Phase 52 D-05. The `STAGE_COLORS` map was dropped entirely; the new shared kanban primitive (`frontend/src/components/kanban/KanbanBoard.tsx`) ships flat surfaces (`bg-muted/30 border-muted`) across all stages with a `border-danger/30` ring on the cancelled column (D-07). Resolved at commit <PHASE-52-PLAN-03-SHA>.

      [If Plan 03 Task 3 succeeded — only include this paragraph if KanbanTaskCard.tsx was absorbed:]
      Four Tier-C `eslint-disable-next-line no-restricted-syntax` directives in `frontend/src/components/assignments/KanbanTaskCard.tsx` (formerly lines 25, 27, 29, 31 — SLA-state color literals) were resolved by token swap per Phase 52 D-13 narrow-blast-radius clause. Replaced `text-red-600/bg-red-50/...` palette literals with semantic SLA-state tokens (`text-danger/bg-danger-soft/text-warn/bg-warn-soft/text-ok/bg-ok-soft`). KanbanTaskCard's only consumers are TasksTab and EngagementKanbanDialog (verified leaf-level via grep); no rippling. Resolved at commit <PHASE-52-PLAN-03-SHA>.
      ```

      Use the second paragraph ONLY if Plan 03 Task 3 succeeded (4 → 0 disables). If Plan 03 Task 3 was deferred (4 → 4 disables remain), do NOT include the second paragraph; instead add: `KanbanTaskCard.tsx Tier-C disables deferred to a future "kanban polish" phase — rippling evidence captured in 52-03-SUMMARY.md.`

    Edit 3 — final verification:
      `bash scripts/check-deleted-components.sh 2>&1 | tail -5; echo "exit=$?"` — exit 0.
      `grep -c "Resolved during Phase 52" .planning/phases/51-design-token-compliance-gate/51-DESIGN-AUDIT.md` — returns ≥ 1.

  </action>
  <verify>
    <automated>bash scripts/check-deleted-components.sh 2>&amp;1 | tail -5 &amp;&amp; grep -c "Resolved during Phase 52" .planning/phases/51-design-token-compliance-gate/51-DESIGN-AUDIT.md</automated>
  </verify>
  <done>
    scripts/check-deleted-components.sh asserts the absence of `frontend/src/components/kibo-ui/kanban` and exits 0. 51-DESIGN-AUDIT.md has a "Resolved during Phase 52 migration" section documenting the TasksTab Tier-C closure (and, conditionally, the KanbanTaskCard closure if Plan 03 Task 3 succeeded).
  </done>
</task>

<task type="auto">
  <name>Task 4: Workspace gate — full lint/type-check/build + smoke-PR-equivalent verification</name>
  <files>
    eslint.config.mjs
    frontend/package.json
    scripts/check-deleted-components.sh
  </files>
  <read_first>
    - .planning/phases/52-heroui-v3-kanban-migration/52-CONTEXT.md D-18 (full sequence)
    - .planning/phases/52-heroui-v3-kanban-migration/52-VALIDATION.md rows 52-XX-10 through 52-XX-15
  </read_first>
  <action>
    No file mutations — final verification gate before Plan 05:

    1. `test ! -d frontend/src/components/kibo-ui/kanban` — exits 0.
    2. `! grep -q '"kibo-ui"' frontend/package.json` — exits 0 (no kibo-ui npm dep ever existed in this codebase per CONTEXT line 187, so this is a defensive check; the actual cleanup is `tunnel-rat` removal).
    3. `! grep -q "tunnel-rat" frontend/package.json` — exits 0.
    4. `pnpm --filter frontend lint 2>&1 | tail -3` — exits 0.
    5. `pnpm --filter frontend type-check 2>&1 | tail -3` — exits 0.
    6. `pnpm --filter frontend build 2>&1 | tail -3` — exits 0.
    7. `bash scripts/check-deleted-components.sh; echo "ci-gate=$?"` — exits 0.
    8. `pnpm --filter frontend lint --no-cache tools/eslint-fixtures/bad-kibo-ui-import.tsx 2>&1 | grep -c "no-restricted-imports"` — returns ≥ 1 (the fixture fires the widened ban; absolute proof the ESLint rule is wired). NOTE: this command is RUN from the repo root; the fixture path is relative to repo root, but lint config is workspace-aware. If `pnpm --filter frontend lint` refuses to lint files outside `frontend/`, fall back to `cd tools && pnpm eslint --config ../eslint.config.mjs bad-kibo-ui-import.tsx 2>&1` and document the invocation in 52-04-SUMMARY.md.
    9. Bonus regression anchor: `pnpm --filter frontend exec playwright test --list tests/e2e/kanban-*.spec.ts 2>&1 | wc -l` — must enumerate the 12 existing kanban-*.spec.ts files unchanged (D-17 regression anchor).

    If any check fails, identify the offending file, surgically revert the offending edit, re-run.

  </action>
  <verify>
    <automated>test ! -d frontend/src/components/kibo-ui/kanban &amp;&amp; ! grep -q "tunnel-rat" frontend/package.json &amp;&amp; pnpm --filter frontend lint 2>&amp;1 | tail -3 &amp;&amp; pnpm --filter frontend type-check 2>&amp;1 | tail -3 &amp;&amp; bash scripts/check-deleted-components.sh; echo "ci-gate=$?"</automated>
  </verify>
  <done>
    All eight verification checks pass. The Phase 52 cleanup is sealed: kibo-ui dir deleted, tunnel-rat removed, ESLint ban widened, CI deletion check live, Phase 51 audit row closed. Plan 05 (visual baselines + regression anchor sweep) is unblocked.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                      | Description                                                                                                                    |
| --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| Filesystem (deletion) ↔ git history           | `rm -rf` is recoverable via `git restore` only if the deletion was staged or committed; pre-deletion sanity check is mandatory |
| Build pipeline ↔ pnpm-lock.yaml               | `pnpm remove` mutates the lockfile; must re-verify lockfile state with `pnpm install`                                          |
| ESLint config edits ↔ live ban enforcement    | If patterns array typo'd, the ban silently fails; fixture-fire test (Edit 5) is the only assertion that the rule is alive      |
| CI assertion script ↔ developer machine state | check-deleted-components.sh runs in both environments; must be deterministic                                                   |

## STRIDE Threat Register

| Threat ID | Category               | Component                                             | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --------- | ---------------------- | ----------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-52-13   | Tampering              | kibo-ui directory re-introduction (future regression) | mitigate    | Three independent defenses: (1) ESLint `no-restricted-imports.patterns` block at static-analysis time; (2) `scripts/check-deleted-components.sh` CI assertion; (3) `tools/eslint-fixtures/bad-kibo-ui-import.tsx` regression fixture wired by Plan 01 + Plan 02 Vitest assertion shell-out. Anchors V14 Configuration from `<security_threat_model>` (ESLint config edits cannot silently disable the ban without breaking the fixture test) |
| T-52-14   | Tampering              | Phase 48 D-02 carve-out removal (Pitfall 7)           | mitigate    | Task 2 Edit 3 runs `find frontend/src/components -name "index.tsx" -not -path "*/kibo-ui/*"` BEFORE deleting; if non-empty, narrows the carve-out instead of removing it entirely                                                                                                                                                                                                                                                            |
| T-52-15   | Repudiation            | Phase 51 audit trail loss                             | mitigate    | Task 3 Edit 2 preserves the original Tier-C row context by moving to "Resolved during Phase 52 migration" subsection rather than deleting; commit-SHA placeholder anchors the resolution chronologically                                                                                                                                                                                                                                     |
| T-52-16   | Information Disclosure | pnpm-lock.yaml drift after tunnel-rat removal         | accept      | Standard `pnpm remove` flow regenerates lockfile; verified by `grep -c "tunnel-rat" pnpm-lock.yaml` returning 0 post-removal                                                                                                                                                                                                                                                                                                                 |
| T-52-SC   | Tampering              | npm/pnpm operations                                   | mitigate    | Single `pnpm remove tunnel-rat` invocation; no new packages added. Existing Phase 48/49 lockfile-discipline patterns followed                                                                                                                                                                                                                                                                                                                |

</threat_model>

<verification>
After all four tasks complete:

1. `test ! -d frontend/src/components/kibo-ui/kanban` — exits 0.
2. `! grep -q "tunnel-rat" frontend/package.json` AND `! grep -q "tunnel-rat" frontend/pnpm-lock.yaml` — both exit 0.
3. `grep -q "'@/components/kibo-ui/\*'" eslint.config.mjs` — exits 0.
4. `! grep -q "48-02 narrowing" eslint.config.mjs` — exits 0 (the obsolete comment block is gone).
5. `! grep -q "48-02 scope-expansion" eslint.config.mjs` — exits 0 (the carve-out is gone) OR remaining narrowed carve-out is documented inline (Pitfall 7 fallback).
6. `bash scripts/check-deleted-components.sh; echo exit=$?` returns `exit=0`.
7. `pnpm --filter frontend lint` exits 0 workspace-wide.
8. `pnpm --filter frontend type-check` exits 0 workspace-wide.
9. `pnpm --filter frontend build` exits 0 workspace-wide.
10. `grep -c "Resolved during Phase 52" .planning/phases/51-design-token-compliance-gate/51-DESIGN-AUDIT.md` returns ≥ 1.
11. Bad-kibo-ui-import fixture fires: `pnpm --filter frontend lint --no-cache tools/eslint-fixtures/bad-kibo-ui-import.tsx 2>&1 | grep -c "no-restricted-imports"` returns ≥ 1 (OR the fallback `pnpm eslint --config eslint.config.mjs` invocation if filter-aware lint refuses out-of-workspace paths).
12. Plan-01 Vitest assertion turns GREEN: `cd frontend && pnpm exec vitest run src/components/kanban/__tests__/eslint-ban.test.ts` — passes.
    </verification>

<success_criteria>

- kibo-ui kanban directory is deleted.
- tunnel-rat is removed from package.json and pnpm-lock.yaml.
- eslint.config.mjs bans `@/components/kibo-ui/*` and the Phase 48 D-02 narrowing comment + index.tsx carve-out are gone (or narrowed per Pitfall 7).
- scripts/check-deleted-components.sh asserts the deletion and exits 0.
- 51-DESIGN-AUDIT.md TasksTab row is closed out under "Resolved during Phase 52 migration"; KanbanTaskCard row closed too if Plan 03 Task 3 succeeded.
- All workspace gates green: lint, type-check, build.
- ESLint regression fixture (Plan 01 Task 3) now fires; Plan-01 companion Vitest test turns GREEN.
  </success_criteria>

<output>
Create `.planning/phases/52-heroui-v3-kanban-migration/52-04-SUMMARY.md` documenting:
- Deletion confirmation with `git log -1 --stat` summary
- tunnel-rat removal evidence (before/after package.json line + lockfile diff summary)
- eslint.config.mjs diff summary (patterns array delta + carve-out removal/narrowing)
- check-deleted-components.sh patch summary
- 51-DESIGN-AUDIT.md closure paragraph (with commit SHA filled in)
- Workspace-gate exit codes (lint, type-check, build, ci-gate)
- Fixture-fire evidence (the lint violation message from bad-kibo-ui-import.tsx)
- Plan-01 Vitest fixture-test transition RED → GREEN
- Pitfall 7 outcome (carve-out fully removed vs narrowed) with rationale
</output>
