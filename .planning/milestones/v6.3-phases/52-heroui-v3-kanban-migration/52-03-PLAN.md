---
phase: 52-heroui-v3-kanban-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 52-02
files_modified:
  - frontend/src/pages/engagements/workspace/TasksTab.tsx
  - frontend/src/components/assignments/EngagementKanbanDialog.tsx
  - frontend/src/components/assignments/KanbanTaskCard.tsx
autonomous: true
requirements:
  - KANBAN-01
  - KANBAN-02
tags:
  - kanban
  - consumer-migration
  - design-tokens
  - tier-c-resolution

must_haves:
  truths:
    - TasksTab.tsx imports kanban primitives from @/components/kanban (NOT @/components/kibo-ui/kanban)
    - EngagementKanbanDialog.tsx imports kanban primitives from @/components/kanban
    - STAGE_COLORS map is deleted from TasksTab.tsx; all 5 Tier-C eslint-disable-next-line directives are removed (D-05)
    - Mobile MobileStageSection uses bg-muted/30 border-muted; cancelled stage uses border-danger/30 (D-08)
    - Both consumers pass isCancelled={column.id === 'cancelled'} to KanbanBoard (D-07)
    - hover:shadow-md is stripped from both consumers' KanbanCard className; replaced with hover:bg-surface-raised hover:border-line-soft (D-09/D-10)
    - KanbanTaskCard.tsx 4 Tier-C disables resolved by token swap (text-danger/bg-danger-soft for overdue; text-warn/bg-warn-soft for urgent and warning; text-ok/bg-ok-soft for normal)
    - Consumer-level validStages.includes(newStage) invariant remains intact verbatim in both files (V5 security gate preserved)
    - pnpm --filter frontend lint exits 0; pnpm --filter frontend type-check exits 0
  artifacts:
    - path: frontend/src/pages/engagements/workspace/TasksTab.tsx
      provides: TasksTab using new kanban primitive; STAGE_COLORS deleted; mobile section tokenized
      contains: "from '@/components/kanban'"
    - path: frontend/src/components/assignments/EngagementKanbanDialog.tsx
      provides: Dialog using new kanban primitive; isCancelled wired
      contains: "from '@/components/kanban'"
    - path: frontend/src/components/assignments/KanbanTaskCard.tsx
      provides: Tier-C absorbed; SLA colors via semantic tokens
      contains: 'text-danger'
  key_links:
    - from: frontend/src/pages/engagements/workspace/TasksTab.tsx
      to: frontend/src/components/kanban/index.ts
      via: Plan 02 barrel export
      pattern: 'KanbanProvider.*KanbanBoard.*KanbanHeader.*KanbanCards.*KanbanCard'
    - from: frontend/src/components/assignments/EngagementKanbanDialog.tsx
      to: frontend/src/components/kanban/index.ts
      via: Plan 02 barrel export
      pattern: "from '@/components/kanban'"
    - from: frontend/src/components/assignments/KanbanTaskCard.tsx
      to: frontend/src/index.css @theme block
      via: semantic SLA-state tokens (text-danger, text-warn, text-ok)
      pattern: '(text|bg)-(danger|warn|ok)'
---

<objective>
Swap both kibo-ui consumers onto the new primitive built in Plan 02, drop the STAGE_COLORS bespoke palette (resolving 5 Tier-C eslint-disable directives by deletion), and absorb KanbanTaskCard's 4 Tier-C disables into the same atomic migration window (per CONTEXT D-13 narrow-blast-radius clause — verified leaf-level, no WorkBoard consumer). The kibo-ui directory is NOT deleted in this plan; that is Plan 04's job. The shape of this plan is "make both consumers compile + render against the new primitive while keeping kibo-ui still on disk as a deletable safety net."

Purpose: Bridge the new primitive (Plan 02) and the cleanup phase (Plan 04). After this plan, `pnpm build && pnpm lint && pnpm type-check` must exit 0 with the new primitive as the sole live consumer of `@/components/kibo-ui/kanban` being… zero (both consumers now point at the new path). The kibo-ui module is dead code, ready for Plan 04's deletion + ESLint ban widening.

Output: Three modified files. Zero new files. Zero deletions. Phase 51 D-12 zero-net-new-disable posture preserved — and 9 existing Tier-C disables eliminated (5 in TasksTab + 4 in KanbanTaskCard).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-heroui-v3-kanban-migration/52-CONTEXT.md
@.planning/phases/52-heroui-v3-kanban-migration/52-PATTERNS.md
@.planning/phases/52-heroui-v3-kanban-migration/52-RESEARCH.md
@.planning/phases/52-heroui-v3-kanban-migration/52-UI-SPEC.md
@.planning/phases/52-heroui-v3-kanban-migration/52-VALIDATION.md
@frontend/src/pages/engagements/workspace/TasksTab.tsx
@frontend/src/components/assignments/EngagementKanbanDialog.tsx
@frontend/src/components/assignments/KanbanTaskCard.tsx
@frontend/src/components/kanban/index.ts
@frontend/src/index.css

<interfaces>
<!-- New primitive's barrel exports — consumers import these names verbatim (no behavior change). -->
```typescript
import {
  KanbanProvider,
  KanbanBoard,
  KanbanHeader,
  KanbanCards,
  KanbanCard,
  type DragEndEvent,
} from '@/components/kanban'
```

<!-- KanbanBoard's new isCancelled prop (D-07 from Plan 02 Task 2): -->

```typescript
<KanbanBoard
  id={column.id}
  isCancelled={column.id === 'cancelled'}
  className="min-w-[280px]"  // layout-only; color comes from primitive defaults
>
```

<!-- Token map for KanbanTaskCard SLA colors — verified against frontend/src/index.css @theme block: -->

```
text-red-600 bg-red-50            → text-danger bg-danger-soft
text-orange-600 bg-orange-50      → text-warn bg-warn-soft     (urgent — borrow warn for hotter shade)
text-yellow-600 bg-yellow-50      → text-warning bg-warning-soft  (warning state — use warning if token exists; fallback to text-warn bg-warn-soft if not)
text-emerald-600 bg-emerald-50    → text-ok bg-ok-soft
```

EXECUTOR VERIFICATION REQUIRED: confirm exact token names available via `grep -E "(--color-warn|--color-warning|--color-danger|--color-ok)" frontend/src/index.css`. If `--color-warning` is not exported as a Tailwind utility, use `text-warn bg-warn-soft` for both urgent and warning states (consolidates to 3-state palette instead of 4-state; documented deviation).
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate TasksTab.tsx — import swap + STAGE_COLORS deletion + mobile section + KanbanBoard isCancelled + KanbanCard className strip</name>
  <files>
    frontend/src/pages/engagements/workspace/TasksTab.tsx
  </files>
  <read_first>
    - frontend/src/pages/engagements/workspace/TasksTab.tsx (entire file, 342 lines — must preserve all behavior)
    - frontend/src/components/kanban/index.ts (Plan 02 barrel — confirm exports match expectations)
    - .planning/phases/52-heroui-v3-kanban-migration/52-PATTERNS.md §"TasksTab.tsx (modified)" (lines 579-654) — exact diff prescription with line numbers
    - .planning/phases/52-heroui-v3-kanban-migration/52-CONTEXT.md D-05, D-07, D-08, D-09, D-10
    - .planning/phases/52-heroui-v3-kanban-migration/52-UI-SPEC.md §"Color" (especially "Stage-specific colors: none" + cancelled cue rules)
  </read_first>
  <action>
    Apply six surgical edits to TasksTab.tsx. After each edit, verify the file still passes `pnpm --filter frontend type-check` before moving to the next:

    Edit 1 (import path swap, lines 19-25):
      Replace `from '@/components/kibo-ui/kanban'` with `from '@/components/kanban'`. Component import list is identical (KanbanProvider, KanbanBoard, KanbanHeader, KanbanCards, KanbanCard). Type imports (`DragEndEvent`) follow the same swap if present.

    Edit 2 (STAGE_COLORS deletion, lines 34-45):
      Delete the entire `const STAGE_COLORS: Record<WorkflowStage, string> = { ... }` block AND its 5 `// eslint-disable-next-line no-restricted-syntax -- Phase 51 Tier-C: ...` comments. This block currently spans 12 lines (34-45 inclusive); after deletion, downstream line numbers shift up by 12.

    Edit 3 (desktop KanbanBoard wiring, around line 199-203 in original):
      Find the `<KanbanBoard ...>` element. Replace `className={`bg-muted/30 border-muted min-w-[280px]`}` with:
        - Pass `isCancelled={column.id === 'cancelled'}` as a NEW prop (D-07 wiring).
        - Drop the `bg-muted/30 border-muted` from className (the primitive now ships these defaults per D-06).
        - Keep ONLY layout-related className: `className="min-w-[280px]"`.

    Edit 4 (desktop KanbanCard className strip, around line 221 in original):
      Find the `<KanbanCard ...>` render. Replace `className="bg-background hover:shadow-md transition-shadow border-border"` with `className="bg-background hover:bg-surface-raised hover:border-line-soft transition-all"`. D-09 strips shadow; D-10 substitutes surface-raised + line-soft.

    Edit 5 (MobileStageSection prop update, around lines 261-342):
      Locate the `MobileStageSection` function. Inside its return, find the outer `<div className={`rounded-lg border ${STAGE_COLORS[stage]} overflow-hidden`}>`. Since STAGE_COLORS is deleted, this line MUST change. Replace with `<div className={cn('rounded-lg border bg-muted/30 border-muted overflow-hidden', stage === 'cancelled' && 'border-danger/30')}>` — D-08 token compliance + D-07 cancelled cue mirrored on mobile.
      Verify the existing import `import { cn } from '@/lib/utils'` is present; if not, add it.

    Edit 6 (no-op verification — preserve V5 security gate):
      Find the `onKanbanDragEnd` callback (around lines 100-116). Verify the line `const validStages: WorkflowStage[] = ['todo', 'in_progress', 'review', 'done', 'cancelled']` and `if (validStages.includes(newStage) && item.column !== newStage)` are PRESENT AND UNCHANGED. This is the V5 input-validation gate from `<security_threat_model>` — must remain intact byte-for-byte. Do NOT refactor this block.

    After all six edits:
      - Run `pnpm --filter frontend type-check 2>&1 | grep "TasksTab" | head` — expect zero errors.
      - Run `pnpm --filter frontend lint --no-cache frontend/src/pages/engagements/workspace/TasksTab.tsx` — expect zero violations (including zero eslint-disable lines — the 5 Tier-C disables are GONE).
      - Verify `grep -c "STAGE_COLORS" frontend/src/pages/engagements/workspace/TasksTab.tsx` returns 0 (no lingering references).
      - Verify `grep -c "@/components/kibo-ui" frontend/src/pages/engagements/workspace/TasksTab.tsx` returns 0.
      - Verify `grep -c "validStages.includes" frontend/src/pages/engagements/workspace/TasksTab.tsx` returns 1 (V5 gate intact).
      - Verify `grep -c "eslint-disable" frontend/src/pages/engagements/workspace/TasksTab.tsx` returns 0 (all 5 Tier-C disables eliminated).

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm --filter frontend type-check 2>&amp;1 | grep -c "TasksTab" || echo "0 errors" &amp;&amp; pnpm --filter frontend lint --no-cache src/pages/engagements/workspace/TasksTab.tsx 2>&amp;1 | grep -cE "(error|warn)" || echo "lint clean"</automated>
  </verify>
  <done>
    TasksTab.tsx compiles cleanly, lints cleanly, and contains zero references to STAGE_COLORS, zero `@/components/kibo-ui` imports, zero `eslint-disable` directives, and exactly one `validStages.includes` invocation. The V5 input-validation gate is preserved byte-for-byte.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate EngagementKanbanDialog.tsx — import swap + KanbanBoard isCancelled + KanbanCard className strip</name>
  <files>
    frontend/src/components/assignments/EngagementKanbanDialog.tsx
  </files>
  <read_first>
    - frontend/src/components/assignments/EngagementKanbanDialog.tsx (entire file, 206 lines)
    - frontend/src/components/kanban/index.ts (Plan 02 barrel)
    - .planning/phases/52-heroui-v3-kanban-migration/52-PATTERNS.md §"EngagementKanbanDialog.tsx (modified)" (lines 657-703) — exact diff prescription
    - .planning/phases/52-heroui-v3-kanban-migration/52-CONTEXT.md D-07, D-09, D-10
    - .planning/phases/52-heroui-v3-kanban-migration/52-RESEARCH.md Open Question 4 (dialog always-renders cancelled column — do NOT change this)
  </read_first>
  <action>
    Apply three surgical edits to EngagementKanbanDialog.tsx:

    Edit 1 (import path swap, lines 15-21):
      Replace `from '@/components/kibo-ui/kanban'` with `from '@/components/kanban'`. Component list is identical to TasksTab.

    Edit 2 (KanbanBoard isCancelled wiring, around line 168):
      Find `<KanbanBoard key={column.id} id={column.id} className="bg-muted/30 border-muted">`. Replace with:
      ```
      <KanbanBoard
        key={column.id}
        id={column.id}
        isCancelled={column.id === 'cancelled'}
      />
      ```
      Drop the explicit `bg-muted/30 border-muted` className — primitive ships defaults per D-06.

    Edit 3 (KanbanCard className strip, around line 187):
      Find the `<KanbanCard ...>` element with `className="bg-background hover:shadow-md transition-shadow border-border"`. Replace with `className="bg-background hover:bg-surface-raised hover:border-line-soft transition-all"`.

    No-op verification (V5 security gate, around lines 102-119):
      Confirm `const validStages: WorkflowStage[] = ['todo', 'in_progress', 'review', 'done', 'cancelled']` and `if (validStages.includes(newStage) && item.column !== newStage)` remain UNCHANGED.

    Per Open Question 4: do NOT change the dialog's "always render cancelled column" posture. The hardcoded `kanbanColumns` array (lines 52-61) renders all 5 columns regardless of cancelled-emptiness — preserve this behavior parity even though TasksTab uses a conditional render. Different surface, different behavior; that asymmetry is intentional.

    Verify after edits:
      - `pnpm --filter frontend type-check 2>&1 | grep "EngagementKanbanDialog" | head` — zero errors.
      - `pnpm --filter frontend lint --no-cache frontend/src/components/assignments/EngagementKanbanDialog.tsx` — zero violations.
      - `grep -c "@/components/kibo-ui" frontend/src/components/assignments/EngagementKanbanDialog.tsx` returns 0.
      - `grep -c "hover:shadow-md" frontend/src/components/assignments/EngagementKanbanDialog.tsx` returns 0.
      - `grep -c "validStages.includes" frontend/src/components/assignments/EngagementKanbanDialog.tsx` returns 1.

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm --filter frontend type-check 2>&amp;1 | grep -c "EngagementKanbanDialog" || echo "0 errors" &amp;&amp; grep -c "@/components/kibo-ui\|hover:shadow-md" frontend/src/components/assignments/EngagementKanbanDialog.tsx</automated>
  </verify>
  <done>
    EngagementKanbanDialog.tsx compiles, lints, and has zero kibo-ui imports + zero hover:shadow utilities + one preserved validStages.includes invocation. Always-render-cancelled behavior is preserved (line 52-61 unchanged).
  </done>
</task>

<task type="auto">
  <name>Task 3: Absorb KanbanTaskCard.tsx 4 Tier-C disables via semantic SLA-state token swap</name>
  <files>
    frontend/src/components/assignments/KanbanTaskCard.tsx
  </files>
  <read_first>
    - frontend/src/components/assignments/KanbanTaskCard.tsx (entire file, 94 lines)
    - frontend/src/index.css §@theme block (lines 43-118) — verify exact token names available (text-danger, bg-danger-soft, text-warn, bg-warn-soft, text-ok, bg-ok-soft, and any -warning variant)
    - .planning/phases/52-heroui-v3-kanban-migration/52-CONTEXT.md D-13 narrow-blast-radius clause
    - .planning/phases/52-heroui-v3-kanban-migration/52-UI-SPEC.md §"Out of contract" first row (the contingent absorption hint)
    - .planning/phases/52-heroui-v3-kanban-migration/52-RESEARCH.md Open Question 1 + Assumption A5
    - .planning/phases/51-design-token-compliance-gate/51-DESIGN-AUDIT.md §"KanbanTaskCard" (audit rows for the 4 disables)
  </read_first>
  <action>
    KanbanTaskCard.tsx is consumed ONLY by TasksTab (line ~228) and EngagementKanbanDialog (line ~187) — verified by `grep -rln "KanbanTaskCard" frontend/src/` during research. No WorkBoard consumer. The Tier-C absorption is contained to a single leaf-level file, satisfying CONTEXT D-13's "if cleanly containable" clause.

    Step 1 — verify token availability:
      Run `grep -E "color-(danger|warn|warning|ok)" frontend/src/index.css | head -20`. Document which of these utility tokens exist:
        - `text-danger` / `bg-danger-soft` (Phase 51 D-11 likely shipped this)
        - `text-warn` / `bg-warn-soft`
        - `text-warning` / `bg-warning-soft` (may NOT exist as a separate utility)
        - `text-ok` / `bg-ok-soft`
      If `text-warning` is not a Tailwind utility, use `text-warn bg-warn-soft` for BOTH `urgent` AND `warning` SLA states (3-state palette: overdue=danger, urgent+warning=warn, normal=ok). Document this consolidation in the file header comment AND in 52-03-SUMMARY.md as a deviation from the 4-state spec.

    Step 2 — replace lines 24-32:
      The current `const slaColors: Record<string, string>` map has 4 entries wrapped in `// eslint-disable-next-line no-restricted-syntax` directives. Replace the entire block with:
      ```typescript
      const slaColors: Record<string, string> = {
        overdue: 'text-danger bg-danger-soft',
        urgent: 'text-warn bg-warn-soft',
        warning: 'text-warning bg-warning-soft',  // or 'text-warn bg-warn-soft' if no -warning utility
        normal: 'text-ok bg-ok-soft',
      }
      ```
      No `eslint-disable` directives — the new strings resolve to design tokens and pass `no-restricted-syntax` cleanly.

    Step 3 — verify no other Tier-C disables in the file:
      `grep -n "eslint-disable" frontend/src/components/assignments/KanbanTaskCard.tsx` must return 0 lines after the edit.

    Step 4 — visual regression awareness:
      Color semantics shift slightly: previous `text-red-600 bg-red-50` (Tailwind palette) → `text-danger bg-danger-soft` (Bureau terracotta family). The visual baseline regeneration in Plan 05 will capture this drift — that is expected and is the entire point of the migration. Document the expected drift in the file header comment for human reviewers of Plan 05's baseline approval step.

    Verify after edit:
      - `pnpm --filter frontend type-check 2>&1 | grep "KanbanTaskCard"` — zero errors.
      - `pnpm --filter frontend lint --no-cache frontend/src/components/assignments/KanbanTaskCard.tsx` — zero violations.
      - `grep -c "eslint-disable" frontend/src/components/assignments/KanbanTaskCard.tsx` returns 0.
      - `grep -c "text-red-600\|bg-red-50\|text-orange-600\|bg-orange-50\|text-yellow-600\|bg-yellow-50\|text-emerald-600\|bg-emerald-50" frontend/src/components/assignments/KanbanTaskCard.tsx` returns 0 (all 8 Tier-C literals eliminated).

    If at any point during this task the change ripples beyond KanbanTaskCard.tsx (e.g., a test snapshot file needs updating, or another component imports `slaColors`), STOP, revert the file, and DEFER this Tier-C absorption to a future "kanban polish" phase. Document the deferral in 52-03-SUMMARY.md. The CONTEXT D-13 "if cleanly containable" clause is a hard gate.

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm --filter frontend lint --no-cache src/components/assignments/KanbanTaskCard.tsx 2>&amp;1 | tail -10 &amp;&amp; grep -c "eslint-disable\|text-red-600\|bg-red-50" src/components/assignments/KanbanTaskCard.tsx</automated>
  </verify>
  <done>
    Either (a) KanbanTaskCard.tsx has 4 Tier-C disables replaced by semantic-token classes, zero eslint-disable directives remain, and the file lints/type-checks clean; OR (b) the absorption was determined to ripple beyond the file and was deferred — in which case the file remains UNCHANGED from its pre-Plan-03 state and 52-03-SUMMARY.md documents the deferral with the rippling evidence. Either outcome satisfies the success criterion.
  </done>
</task>

<task type="auto">
  <name>Task 4: Workspace gate — full smoke build + lint + type-check (D-18 step 3 dry-run)</name>
  <files>
    frontend/src/pages/engagements/workspace/TasksTab.tsx
    frontend/src/components/assignments/EngagementKanbanDialog.tsx
    frontend/src/components/assignments/KanbanTaskCard.tsx
  </files>
  <read_first>
    - .planning/phases/52-heroui-v3-kanban-migration/52-CONTEXT.md D-18 step 3 (smoke build/lint/type-check requirement before deletion)
    - .planning/phases/52-heroui-v3-kanban-migration/52-VALIDATION.md "Sampling Rate" + "Per-Task Verification Map" rows 52-XX-14, 52-XX-15
  </read_first>
  <action>
    No file mutations — this task is a strict verification gate that gates Plan 04 (which performs the actual kibo-ui deletion):

    1. `pnpm --filter frontend lint 2>&1 | tail -20` — must exit 0. No new lint violations anywhere.
    2. `pnpm --filter frontend type-check 2>&1 | tail -20` — must exit 0. No new TS errors anywhere.
    3. `pnpm --filter frontend build 2>&1 | tail -20` — must exit 0. Vite build succeeds; bundle written to dist/.
    4. `grep -rln "@/components/kibo-ui" frontend/src/` — must return exactly ONE line: the kibo-ui module's own self-references inside `frontend/src/components/kibo-ui/kanban/index.tsx`. NO consumer files should match. If TasksTab.tsx or EngagementKanbanDialog.tsx appear in this grep, Task 1 or Task 2 left a stray import — fix before declaring done.
    5. `grep -rln "@/components/kanban" frontend/src/` — must return exactly THREE files: TasksTab.tsx, EngagementKanbanDialog.tsx, and the new kanban module's `index.ts`.
    6. Sanity check on Tier-C disable reduction across the phase scope:
       `grep -rn "eslint-disable-next-line no-restricted-syntax" frontend/src/pages/engagements/workspace/TasksTab.tsx frontend/src/components/assignments/EngagementKanbanDialog.tsx frontend/src/components/assignments/KanbanTaskCard.tsx` — must return either 0 (if Task 3 absorbed KanbanTaskCard's disables) or 4 (if Task 3 deferred them, keeping the file unchanged). Anything between 1 and 3, or anything ≥ 5, is a regression — investigate.

    If any of 1-3 fail, identify the offending file, revert to a checkpoint state (last green commit), and re-apply the relevant edit smaller. Phase 51's smoke-PR pattern (D-18) is the precedent: the migration must produce a green smoke before deletion lands.

    If grep checks 4-6 fail, the relevant prior task missed a code path — investigate and surgically fix.

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm --filter frontend lint 2>&amp;1 | tail -5 &amp;&amp; pnpm --filter frontend type-check 2>&amp;1 | tail -5 &amp;&amp; grep -rln "@/components/kibo-ui" frontend/src/ | wc -l</automated>
  </verify>
  <done>
    Workspace lint, type-check, and build all exit 0. Exactly one self-reference in `frontend/src/components/kibo-ui/` matches the kibo-ui import grep. Both consumer files point at `@/components/kanban`. Phase 52 Wave 3 is sealed and Plan 04 (deletion + ESLint widening) is unblocked.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                                                                                                            |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Consumer → workflow_stage mutation | The `onDragEnd → validStages.includes(newStage) → handleDragEnd → updateStageMutation` chain must remain unbroken; any consumer-level whitelist removal is a V5 breach |
| Consumer → new primitive           | Import swap must NOT alter the runtime behavior; behavior parity is the contract                                                                                       |
| KanbanTaskCard → SLA color tokens  | Token swap MUST resolve via Tailwind v4 @theme block; broken token names will render as transparent (silent regression)                                                |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                     | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                             |
| --------- | ---------------------- | ------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-52-09   | Tampering              | onKanbanDragEnd whitelist                                     | mitigate    | Task 1 + Task 2 explicitly require `grep -c "validStages.includes" === 1` verification post-edit. The V5 input-validation gate is preserved byte-for-byte. Anchors `<security_threat_model>` V5 invariant                                                                                                   |
| T-52-10   | Tampering              | Tier-C disable removal                                        | mitigate    | Task 1 reduces TasksTab eslint-disable count from 5 to 0 by deletion-of-need (D-05); Task 3 reduces KanbanTaskCard from 4 to 0 (or defers cleanly per D-13 narrow-blast-radius gate). Final state: zero net-new disables, fewer total disables — strictly better posture. Anchors Phase 51 D-12 inheritance |
| T-52-11   | Tampering              | SLA color token swap                                          | mitigate    | Task 3 verifies token availability via `grep` against `frontend/src/index.css @theme` BEFORE writing the swap. Visual baselines in Plan 05 capture any unintended drift. Token-utility resolution validated by `pnpm lint` exit-0                                                                           |
| T-52-12   | Information Disclosure | Always-render-cancelled dialog vs conditional-render TasksTab | accept      | Asymmetry is INTENTIONAL behavior parity (Open Question 4 in 52-RESEARCH). Unifying is out of phase scope per "Out of contract" §UI-SPEC                                                                                                                                                                    |
| T-52-SC   | Tampering              | npm installs                                                  | mitigate    | Zero new packages. Plan 03 mutates three existing files only                                                                                                                                                                                                                                                |

</threat_model>

<verification>
After all four tasks complete:

1. `pnpm --filter frontend lint` exits 0.
2. `pnpm --filter frontend type-check` exits 0.
3. `pnpm --filter frontend build` exits 0.
4. `grep -rln "@/components/kibo-ui" frontend/src/` returns at most lines inside `frontend/src/components/kibo-ui/` itself (kibo-ui module's internal references — count is fine; zero consumer files outside that dir).
5. `grep -rln "@/components/kanban" frontend/src/` returns the new module + TasksTab + EngagementKanbanDialog (exactly three matches).
6. `grep -c "STAGE_COLORS" frontend/src/pages/engagements/workspace/TasksTab.tsx` returns 0.
7. `grep -c "validStages.includes" frontend/src/pages/engagements/workspace/TasksTab.tsx frontend/src/components/assignments/EngagementKanbanDialog.tsx` returns 2 (both V5 gates intact).
8. `grep -rn "eslint-disable" frontend/src/pages/engagements/workspace/TasksTab.tsx frontend/src/components/assignments/EngagementKanbanDialog.tsx` returns 0 in both files.
9. Tier-C disable accounting: starting count was 5 (TasksTab) + 4 (KanbanTaskCard) = 9. Final count is either 0 (full absorption) or 4 (KanbanTaskCard deferred). NEVER anywhere between.
10. Existing 12 `kanban-*.spec.ts` files remain green: `pnpm --filter frontend exec playwright test --list tests/e2e/kanban-*` enumerates expected count (regression anchor unchanged).
    </verification>

<success_criteria>

- Three files modified; zero new files; zero deletions; zero new npm packages.
- All four Wave-0 vitest files (from Plan 01) still GREEN (Plan 02 already proved this; Plan 03 must NOT regress).
- Workspace lint + type-check + build all exit 0.
- Tier-C disable count in phase scope drops from 9 → 0 (full absorption) or 9 → 4 (KanbanTaskCard deferred); NEVER between.
- V5 security invariant preserved verbatim in both consumer files (`validStages.includes(newStage)` whitelist intact).
- Both consumers import from `@/components/kanban` (new primitive); kibo-ui module is dead code ready for Plan 04 deletion.
  </success_criteria>

<output>
Create `.planning/phases/52-heroui-v3-kanban-migration/52-03-SUMMARY.md` documenting:
- Three modified file paths with line-count deltas
- Tier-C disable accounting (before/after for each file)
- Whether KanbanTaskCard absorption succeeded (4 → 0) or was deferred (4 → 4) with rippling evidence
- V5 security gate verification (grep count = 2 across both consumer files)
- Smoke-build exit codes for lint, type-check, build (must all be 0)
- Confirmation that kibo-ui module is dead code (no consumers) and Plan 04 deletion is unblocked
- Any deviation from prescribed edits with rationale
</output>
