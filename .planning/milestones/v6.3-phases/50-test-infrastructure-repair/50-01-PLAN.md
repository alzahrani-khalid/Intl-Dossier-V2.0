---
phase: 50-test-infrastructure-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/vitest.config.ts
  - frontend/tests/setup.ts
autonomous: true
requirements:
  - TEST-01
  - TEST-02
user_setup: []

must_haves:
  truths:
    - '`pnpm --filter intake-frontend test --run` no longer collects Playwright `.spec.ts` files under any path.'
    - "`frontend/tests/setup.ts` `vi.mock('react-i18next')` factory uses `await vi.importActual<typeof import('react-i18next')>('react-i18next')` and spreads it, so `initReactI18next`, `I18nextProvider`, `withTranslation`, and `Translation` are real exports inside any test consumer."
    - 'The 4 module-eval-blocked wizard tests (`CreateWizardShell` × 2 + `SharedBasicInfoStep` × 1 + `useCreateDossierWizard` × 1) load and execute their `describe` bodies without `TypeError: No "initReactI18next" export is defined on the "react-i18next" mock`.'
    - '11 known dead-import test files (legacy theme-provider, deleted DossierCard/LanguageToggle, deleted `src/i18n/config`) no longer exist on disk; their absence does not regress any green test.'
    - 'After this plan lands, `pnpm --filter intake-frontend test --run 2>&1 | tail -5` reports ≤10 remaining failing test files (per RESEARCH G1+G2+G3 cascades: 175 + 20 + 11 = 206 of 218 cleared; residual ~12 = class-S env (4) + class-R product/test regressions (~5-8) addressed by Plan 50-02).'
  artifacts:
    - path: 'frontend/vitest.config.ts'
      provides: 'Test runner config that picks up ONLY `.test.{ts,tsx}` files and excludes Playwright `.spec.*`'
      contains: "include: ['**/*.test.{ts,tsx}']"
    - path: 'frontend/tests/setup.ts'
      provides: 'Global `react-i18next` mock using `vi.importActual` spread + override pattern (D-05)'
      contains: 'await vi.importActual'
  key_links:
    - from: 'frontend/tests/setup.ts'
      to: 'frontend/src/i18n/index.ts:447'
      via: '`i18n.use(initReactI18next).init(...)` resolves at module-eval because the spread surfaces the real `initReactI18next` symbol'
      pattern: 'initReactI18next'
    - from: 'frontend/vitest.config.ts'
      to: 'tests/e2e/**/*.spec.ts (Playwright)'
      via: 'Vitest no longer collects Playwright specs because `include` is narrowed to `.test.{ts,tsx}` and `exclude` lists `**/*.spec.*`'
      pattern: "include.*\\*\\*/\\*\\.test\\.\\{ts,tsx\\}"
---

<objective>
Clear the two biggest frontend failure cascades in a single low-risk plan:

1. **G1 — Playwright misglobbed (175 files).** Add `include` + `exclude` to `frontend/vitest.config.ts` so vitest stops collecting `tests/e2e/**/*.spec.ts` (Playwright) and `tests/visual/**/*.spec.ts`. This is a 2-line config edit per D-01 / RESEARCH §Failure Inventory G1.

2. **G2 — `initReactI18next` module-eval cascade (20 files, including the 4 wizard tests counted by TEST-02).** Rewrite the `vi.mock('react-i18next')` factory at `frontend/tests/setup.ts:6` per D-05: use `await vi.importActual<typeof import('react-i18next')>('react-i18next')`, spread the real module, override ONLY `useTranslation` and `Trans`. Preserve the 60-key `translations` map verbatim per D-06.

3. **G3 — Dead-import cleanup (11 files).** Delete 11 frontend test files that import deleted modules (legacy theme-provider, deleted DossierCard / LanguageToggle, deleted `src/i18n/config`). These were left orphaned by Phase 33/34/36 migrations; deleting them restores module-eval to green.

Purpose: Each fix is one tiny diff but unblocks ~200 files. This plan is the highest-leverage work in the phase — it MUST land first so 50-02 can triage the small residual set without being drowned in 218-file noise.

Output:

- `frontend/vitest.config.ts` with `include` + `exclude` for vitest-only test collection
- `frontend/tests/setup.ts` with D-05 factory (spread of real module + overrides)
- 11 dead test files deleted from `frontend/tests/integration/` and `frontend/src/**/__tests__/` (exact paths discovered in Task 3)
- After landing: TEST-01 is SATISFIED at the FE-factory dimension; TEST-02 4 wizard tests pass green
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-test-infrastructure-repair/50-CONTEXT.md
@.planning/phases/50-test-infrastructure-repair/50-RESEARCH.md
@.planning/phases/50-test-infrastructure-repair/50-PATTERNS.md
@.planning/phases/50-test-infrastructure-repair/50-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase verbatim. -->
<!-- DO NOT re-explore the codebase — use these directly. -->

From `frontend/tests/setup.ts` (CURRENT broken shape — lines 1-93):

- Imports: `beforeAll, afterEach, afterAll, vi` from 'vitest'; `cleanup` from '@testing-library/react'; `server` from './mocks/server'
- Lines 6-81: `vi.mock('react-i18next', () => ({ useTranslation: ..., Trans: ... }))` — SYNC factory, misses `initReactI18next`
- Lines 11-72: the 60-key `translations` map MUST be preserved verbatim (D-06)
- Lines 83-93: `beforeAll(() => server.listen({ onUnhandledRequest: 'error' }))`, `afterEach(() => { server.resetHandlers(); cleanup(); })`, `afterAll(() => server.close())` — KEEP unchanged
- Style: semicolons + double quotes — DO NOT reformat (existing local style)

From `frontend/vitest.config.ts` (CURRENT — 31 lines):

- Top-level `defineConfig({ plugins: [react()], test: {...}, resolve: { alias: { '@', '@tests' } } })`
- Inside `test:` block: `globals: true`, `environment: 'jsdom'`, `setupFiles: ['./tests/setup.ts']`, `css: true`, `coverage: { provider: 'v8', reporter: [...], exclude: [...] }`
- NO `include`, NO `exclude` for test collection (the bug)
- Coverage `exclude` already lists `tests/` — do not touch

From `frontend/tests/component/CommitmentList.test.tsx:451-462` (verbatim in-repo PRECEDENT for the spread pattern):

- `vi.mock('react-i18next', async () => { const actual = await vi.importActual('react-i18next'); return { ...actual, useTranslation: () => ({ t: (key: string) => key, i18n: { language: 'ar' } }) } })`

From `frontend/src/components/signature-visuals/__tests__/GlobeLoader.reducedMotion.test.tsx:18-22` (verbatim TYPED-spread precedent):

- `vi.mock('../ensureWorld', async () => { const d3Real = await vi.importActual<typeof import('d3-geo')>('d3-geo'); ... })`

From `frontend/src/i18n/index.ts:447` (the consumer that breaks today):

- `i18n.use(initReactI18next).init({ ... })` — line 447 of the real i18n bootstrap. With the broken mock, `initReactI18next === undefined` so `i18n.use(undefined)` throws at module-eval.

From `frontend/src/components/dossier/wizard/__tests__/CreateWizardShell.test.tsx` (the 4 wizard tests counted in TEST-02 — all blocked by the same root cause):

- `CreateWizardShell.test.tsx > CreateWizardShell > should be importable as a named export`
- `CreateWizardShell.test.tsx > CreateWizardShell > should accept dossierType and steps props`
- `frontend/src/components/dossier/wizard/__tests__/SharedBasicInfoStep.test.tsx > SharedBasicInfoStep > should be importable as a named export`
- `frontend/src/components/dossier/wizard/hooks/__tests__/useCreateDossierWizard.test.ts > useCreateDossierWizard > should be importable`
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow vitest collection — add include + exclude to frontend/vitest.config.ts (G1 cascade)</name>
  <files>frontend/vitest.config.ts</files>
  <read_first>
    - frontend/vitest.config.ts (full file — 31 lines; current state has no `include`/`exclude`)
    - backend/vitest.config.ts:29-30 (sibling pattern — `include: ['tests/**/*.test.ts']`, `exclude: ['node_modules/', 'dist/']`)
    - .planning/phases/50-test-infrastructure-repair/50-RESEARCH.md §Failure Inventory G1 + §Integration Runner Choice "Existing config quirks"
    - .planning/phases/50-test-infrastructure-repair/50-PATTERNS.md §`frontend/vitest.config.ts`
  </read_first>
  <action>
    Inside the existing `test: { ... }` block in `frontend/vitest.config.ts`, add two new properties immediately after `css: true,` (before `coverage:`):
    - `include: ['**/*.test.{ts,tsx}']` — restricts vitest collection to `.test.{ts,tsx}` files only (per RESEARCH G1 — drops 175 Playwright misglobbed `.spec.ts` files and 1 e2e `.spec.ts` with `@faker-js/faker` import).
    - `exclude: ['node_modules/', 'dist/', 'build/', '**/*.spec.*']` — defensive belt-and-suspenders (covers any `.spec.ts` that slipped past the include narrowing).

    Preserve verbatim the rest of the file: `plugins: [react()]`, `globals: true`, `environment: 'jsdom'`, `setupFiles: ['./tests/setup.ts']`, `css: true`, the entire `coverage: { ... }` block (including its own `exclude` list — do not consolidate the two excludes), and the `resolve.alias` block (`@`, `@tests`).

    Do NOT add coverage thresholds (CONTEXT line 124's claim about FE thresholds is incorrect per RESEARCH §Coverage Threshold Impact — adding thresholds is deferred). Do NOT touch `setupFiles`. Match the existing local style (semicolons + double quotes — confirmed by Read of the file).

    Rationale: One config edit clears 175 Playwright + 1 faker = 176 of the 218 failures in a single tiny diff per RESEARCH §Recommended Plan Slicing.

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -c "include:.*\\*\\*/\\*\\.test\\.\\{ts,tsx\\}" vitest.config.ts | grep -q "^1$" &amp;&amp; grep -c "exclude:.*spec" vitest.config.ts | grep -q "^1$" &amp;&amp; pnpm exec vitest --run --reporter=verbose 2>&amp;1 | tail -5 | grep -E "Test Files.*failed" | awk '{print $4}' | tr -d '|' | awk '$1 &lt; 50 { print "PASS: failures dropped below 50"; exit 0 } { print "FAIL: still " $1 " failures"; exit 1 }'</automated>
  </verify>
  <done>
    `frontend/vitest.config.ts` has both `include: ['**/*.test.{ts,tsx}']` and `exclude` listing `**/*.spec.*` inside the `test:` block. `pnpm --filter intake-frontend test --run` no longer collects any Playwright `.spec.ts` file. Total FE failing test files drop from 218 → ≤50 (175 Playwright + 1 faker cleared; module-eval cascade still open until Task 2).
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite frontend/tests/setup.ts react-i18next factory per D-05 (G2 cascade)</name>
  <files>frontend/tests/setup.ts</files>
  <read_first>
    - frontend/tests/setup.ts (full file — current 93-line broken shape with sync factory)
    - frontend/tests/component/CommitmentList.test.tsx:451-462 (verbatim in-repo PRECEDENT — `vi.mock('react-i18next', async () => { const actual = await vi.importActual('react-i18next'); return { ...actual, useTranslation: ... } })`)
    - frontend/src/components/signature-visuals/__tests__/GlobeLoader.reducedMotion.test.tsx:18-22 (verbatim TYPED-spread precedent — `await vi.importActual&lt;typeof import('d3-geo')&gt;('d3-geo')`)
    - frontend/src/i18n/index.ts:447 (the consumer — `i18n.use(initReactI18next).init(...)`)
    - .planning/phases/50-test-infrastructure-repair/50-RESEARCH.md §Mock Factory Specs (frontend, lines 213-254) for the full target shape
    - .planning/phases/50-test-infrastructure-repair/50-PATTERNS.md §`frontend/tests/setup.ts`
    - .planning/phases/50-test-infrastructure-repair/50-CONTEXT.md D-05, D-06, D-11 (preserve mixed assertion-text contract)
  </read_first>
  <action>
    Replace the `vi.mock('react-i18next', () => ({ ... }))` block (current lines 6-81, 76 lines total ending at `}));`) with the D-05 async-factory form. Required target shape:

    - Factory signature: `async () =&gt; { ... }` (NOT sync `() =&gt; ({ ... })`). Sync factories cannot `await vi.importActual`.
    - First statement inside the body: `const actual = await vi.importActual<typeof import('react-i18next')>('react-i18next')` — use the TYPED-generic form per the GlobeLoader precedent (gives editor autocomplete and type safety).
    - Return an object literal that begins with `...actual,` (spreads every real export of `react-i18next` — `initReactI18next`, `I18nextProvider`, `withTranslation`, `Translation`, and 12+ others — per D-05).
    - After the spread, override ONLY two exports:
      - `useTranslation: () =&gt; ({ t: (key, params) =&gt; ..., i18n: { language: 'en' } })` where `t` consults the existing 60-key `translations: Record&lt;string, string&gt;` map and returns `translations[key] ?? key` per D-06 fallback.
      - `Trans: ({ children }: any) =&gt; children` (children-passthrough — preserves the current stub semantics per D-05 final paragraph).
    - The 60-key `translations` map (current lines 11-72 of setup.ts — keys `'afterActions.decisions.title'` through `'common.selectDate'`) MUST be copied VERBATIM into the new `t`. No key additions, no value rewording, no key removals. D-06 forbids the mass-rewrite.
    - The `??` operator (nullish coalescing) replaces the current `||` per D-06's exact wording in RESEARCH §Mock Factory Specs (`translations[key] ?? key`). Both behave identically for non-empty strings; `??` is the documented contract going forward.

    Preserve verbatim lines 1-3 (imports: `beforeAll, afterEach, afterAll, vi` from 'vitest'; `cleanup` from '@testing-library/react'; `server` from './mocks/server') and lines 83-93 (MSW `beforeAll`/`afterEach`/`afterAll` lifecycle + cleanup). DO NOT change the import order, DO NOT touch the MSW lifecycle, DO NOT remove the cleanup call.

    Do NOT reformat existing semicolon/quote style — file is locally semicolon + double-quote formatted (Prettier root config says `semi: false` but this file predates that). Leave the local style; only diff the factory block per PATTERNS §Gotchas.

    Result after this task: `initReactI18next` is a real callable inside any test consumer, so `frontend/src/i18n/index.ts:447` evaluates without throwing, which clears all 20 class-M failures including the 4 wizard tests named in TEST-02.

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -c "await vi.importActual&lt;typeof import('react-i18next')&gt;" tests/setup.ts | grep -q "^1$" &amp;&amp; grep -c "\\.\\.\\.actual" tests/setup.ts | grep -q "^1$" &amp;&amp; grep -c "afterActions.decisions.title" tests/setup.ts | grep -q "^1$" &amp;&amp; pnpm exec vitest --run src/components/dossier/wizard/__tests__/CreateWizardShell.test.tsx src/components/dossier/wizard/__tests__/SharedBasicInfoStep.test.tsx src/components/dossier/wizard/hooks/__tests__/useCreateDossierWizard.test.ts 2>&amp;1 | tail -3 | grep -E "Test Files.*passed" | grep -v "failed"</automated>
  </verify>
  <done>
    `frontend/tests/setup.ts` factory is `async () => { const actual = await vi.importActual<typeof import('react-i18next')>('react-i18next'); return { ...actual, useTranslation, Trans } }`. All 3 wizard test files (`CreateWizardShell.test.tsx`, `SharedBasicInfoStep.test.tsx`, `useCreateDossierWizard.test.ts` = 4 total `it`-level cases per TEST-02) pass green. `grep "initReactI18next" frontend/tests/setup.ts` returns 0 (the spread surfaces it implicitly — no literal occurrence). `grep -c "afterActions.decisions.title" frontend/tests/setup.ts` returns 1 (translation map preserved verbatim per D-06).
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete 11 dead-import frontend test files (G3 cascade)</name>
  <files>(11 paths discovered at execution time — list in commit message)</files>
  <read_first>
    - .planning/phases/50-test-infrastructure-repair/50-RESEARCH.md §Failure Inventory class-D (11 files, error signature "Failed to resolve import \"../../src/components/theme-provider/theme-provider\"" etc.)
    - .planning/phases/50-test-infrastructure-repair/50-PATTERNS.md §`frontend/tests/setup.ts` (does NOT list the 11 paths — they must be discovered)
    - .planning/STATE.md §Phase 33/34/36 summaries (theme-provider deleted in Phase 33-07; LanguageToggle deleted in Phase 34-08; useTheme/MainLayout/AppSidebar/SiteHeader/MobileBottomTabBar deleted in Phase 36-05)
    - .planning/phases/50-test-infrastructure-repair/50-CONTEXT.md D-01 (full-green; deletion of orphans is in scope)
  </read_first>
  <action>
    Discover and delete every frontend test file whose `import` statement targets a module that no longer exists on disk. The discovery procedure is:

    1. Run `pnpm --filter intake-frontend test --run 2>&amp;1 | grep -B1 "Failed to resolve import" | grep -oE "frontend/(tests|src)/[^ ]+\\.(test|spec)\\.tsx?"` to enumerate the broken-import test files. RESEARCH §Failure Inventory class-D names the count (11) and the diagnostic signature. Expected hits include:
       - `frontend/tests/integration/test_language_switch.test.tsx` (imports `src/i18n/config` — deleted in Phase 34-08)
       - `frontend/tests/integration/*theme*` test files (import deleted `theme-provider/theme-provider`)
       - `frontend/tests/unit/components.test.tsx` (imports deleted `DossierCard` / `LanguageToggle`)
       - other Phase 33/34/36 orphans

    2. For each discovered file, verify the import target is genuinely deleted (NOT renamed): `git log --diff-filter=D --name-only --pretty=format: -- &lt;target-path&gt; | head -3` — if the file was deleted, the test is class-D and is safe to delete. If the file was renamed, the test should be updated to the new path instead of deleted (rare — note in commit message if encountered).

    3. Delete each confirmed-orphan test file. Use `git rm` so deletion is staged. Do NOT delete any test file whose import target still exists or was merely moved within the workspace.

    4. After deletion, re-run `pnpm --filter intake-frontend test --run 2>&amp;1 | grep -c "Failed to resolve import"` — expect 0.

    5. List every deleted path in the commit message body so reviewers can audit.

    Do NOT delete `frontend/tests/integration/` as a directory; only delete individual orphan files. Surviving files in `tests/integration/` (if any) stay in place — Plan 50-04's audit decides their long-term disposition.

    Do NOT delete files in `tests/e2e/` or `tests/visual/` — those are Playwright `.spec.ts` files already excluded by Task 1's vitest config and may be in scope for Phase 52 KANBAN-04 (D-12).

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm exec vitest --run --reporter=verbose 2>&amp;1 | grep -c "Failed to resolve import" | grep -q "^0$" &amp;&amp; pnpm exec vitest --run --reporter=verbose 2>&amp;1 | tail -5 | grep -E "Test Files.*failed" | awk '{ for(i=1;i&lt;=NF;i++) if ($i ~ /^[0-9]+$/) { print $i; exit } }' | awk '$1 &lt; 15 { print "PASS: residual " $1 " failures (≤15 expected)"; exit 0 } { print "FAIL: " $1 " failures, expected ≤15"; exit 1 }'</automated>
  </verify>
  <done>
    All discovered dead-import test files (~11 per RESEARCH count, exact list captured in commit message) are removed via `git rm`. `grep "Failed to resolve import"` in the post-Plan vitest output returns 0. Total residual FE failing files ≤15 (the class-S env failures + class-R product/test regressions left for Plan 50-02 — exact count documented in commit message and rolled into the audit by Plan 50-04).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                                                                                                                            |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Test runner ↔ filesystem                   | Vitest reads test files only from globs matching `**/*.test.{ts,tsx}` after this plan — narrower attack surface than the pre-plan default that included `**/*.spec.*`. |
| Mock factory ↔ real `react-i18next` module | `vi.importActual` runs the real SDK import at module-eval — same trust boundary as production code.                                                                    |

## STRIDE Threat Register

| Threat ID  | Category                   | Component                                                                                                                                            | Disposition | Mitigation Plan                                                                                                                                                                                                                                               |
| ---------- | -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-50-01-01 | I (Information disclosure) | `vi.importActual('react-i18next')` could surface unintended exports if the package adds a future export that contains a sentinel/secret.             | accept      | `react-i18next` is an OSS i18n SDK with public exports only. No credentials. The spread pattern is safer than list-every-export because new exports get covered automatically.                                                                                |
| T-50-01-02 | T (Tampering)              | A test file disguised as `*.test.ts` could exploit the new `include` glob to run with the global i18n mock and inject behavior into `frontend/src/`. | mitigate    | Vitest tests cannot mutate `frontend/src/` at runtime (jsdom sandbox); existing `repo-policy` CI job (`.github/workflows/ci.yml:17-41`) blocks committed `.env`/build artifacts. The new `include` narrows, not widens, collection.                           |
| T-50-01-03 | E (Elevation of privilege) | Deletion of 11 test files (Task 3) could mask a live test that was previously catching a regression.                                                 | mitigate    | D-09/D-10 requires `git log --diff-filter=D` verification that each test's import target is genuinely deleted (not renamed). Commit message lists every deleted path so reviewers audit. Plan 50-04's `50-TEST-AUDIT.md` records dispositions with rationale. |

</threat_model>

<verification>
**Phase-level checks (post-plan):**

- `frontend/vitest.config.ts` has both `include: ['**/*.test.{ts,tsx}']` AND `exclude: ['node_modules/', 'dist/', 'build/', '**/*.spec.*']` inside the `test:` block — verified by grep.
- `frontend/tests/setup.ts` contains `await vi.importActual<typeof import('react-i18next')>('react-i18next')` and `...actual,` — verified by grep.
- `frontend/tests/setup.ts` retains the 60-key translation map (`afterActions.decisions.title` through `common.selectDate`) verbatim — verified by line count `wc -l frontend/tests/setup.ts` falls within ±5 of original 93.
- The 4 wizard tests (TEST-02 satisfied at this plan): `pnpm --filter intake-frontend test --run src/components/dossier/wizard/__tests__ src/components/dossier/wizard/hooks/__tests__` exits 0.
- Residual FE failure count: ≤15 (class-S env + class-R product/test regressions only — Plan 50-02 finishes the job).
- No regression in currently-green tests: `pnpm --filter intake-frontend test --run` passing-count is ≥ pre-plan passing-count (121 + the 4 wizard tests + 20 module-eval cascade green = ≥145).
- `pnpm --filter intake-frontend lint` exits 0 (no new lint violations introduced by setup.ts rewrite).
  </verification>

<success_criteria>

- **TEST-01 (FE dimension):** `frontend/tests/setup.ts` `vi.mock('react-i18next')` factory uses `vi.importActual` spread → SATISFIED.
- **TEST-02 (4 wizard tests):** All four module-eval-blocked tests pass green when run in isolation → SATISFIED.
- **G1 cascade closed:** 175 Playwright misglobbed files cleared from vitest collection.
- **G2 cascade closed:** 20 module-eval-blocked files clear (4 wizard + 16 transitive consumers).
- **G3 cascade closed:** 11 dead-import files deleted via `git rm`.
- **Residual count:** `pnpm --filter intake-frontend test --run` reports ≤15 failing test files (handed to Plan 50-02).
- **No regressions:** Pre-plan passing tests still pass.
  </success_criteria>

<output>
After completion, create `.planning/phases/50-test-infrastructure-repair/50-01-SUMMARY.md` with:
- Pre-plan vs post-plan failing-file counts (218 → expected ≤15)
- The 11 deleted test file paths (exact list)
- Confirmation that the 4 wizard tests in TEST-02 pass green
- Any deviations from the 175 / 20 / 11 cascade counts (with rationale)
- Handoff note to Plan 50-02 listing the residual failing files Plan 50-02 must triage
</output>
</content>
</invoke>
