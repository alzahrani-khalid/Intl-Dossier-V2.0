---
phase: 50-test-infrastructure-repair
plan: 12
type: execute
wave: 3
depends_on:
  - 50-01
  - 50-09
  - 50-10
files_modified:
  - frontend/tests/unit/design-system/buildTokens.test.ts
  - frontend/tests/unit/design-system/fouc-bootstrap.test.ts
  - frontend/tests/unit/components/ui/heroui-wrappers.test.tsx
  - frontend/tests/unit/hooks/responsive.test.ts
  - frontend/src/pages/Dashboard/widgets/__tests__/KpiStrip.test.tsx
  - frontend/src/routes/_protected/dossiers/forums/__tests__/ForumsListPage.test.tsx
  - frontend/tests/unit/monitoring.dashboard.test.tsx
  - frontend/public/bootstrap.js
  - frontend/src/design-system/tokens/buildTokens.ts
  - frontend/src/components/ui/heroui-button.tsx
  - frontend/src/pages/Dashboard/widgets/KpiStrip.tsx
  - frontend/src/hooks/useResponsive.ts
  - frontend/src/pages/monitoring/Dashboard.tsx
autonomous: true
requirements:
  - TEST-02
user_setup: []

must_haves:
  truths:
    - "AT TASK START, the executor re-runs `pnpm --filter intake-frontend exec vitest --run --reporter=default 2>&1 | tail -8` and counts failing files. If MORE than 8 of THIS PLAN'S files (`files_modified`) are failing, STOP — phase scope was misjudged."
    - 'Each of the 7 test files passes `pnpm --filter intake-frontend exec vitest --run <file>` exit 0 after this plan lands. Root causes are diagnosed per file using D-10 archaeology (`git log -p`) and fixed on the wronger side (test contract OR impl).'
    - '`buildTokens.test.ts` SC-3 `--sla-bad` assertion is reconciled: either the test value `oklch(54% 0.2 25)` is updated to match the current `buildTokens` output `oklch(46% 0.18 25)` (IMPL DRIFTED + sla-bad-hue-locked deviation per `[v6.0/35-04]` STATE entry), OR `buildTokens.ts` is restored to emit the test-expected value (TEST WRONG; verify via git archaeology that the canonical token engine output is `54%/0.2` vs `46%/0.18`).'
    - '`fouc-bootstrap.test.ts` 9 palette-scrape failures are reconciled per the CLAUDE.md byte-match contract: either the test scrape regex is updated to match the current `frontend/public/bootstrap.js` literal shape, OR `bootstrap.js` is resynced to match `frontend/src/design-system/tokens/directions.ts` canonical form. NO `.skip` on sub-tests. The drift-guard intent is preserved.'
    - '`heroui-wrappers.test.tsx` 2 failures (semantic-token assertion for `destructive` variant) are reconciled per the Phase 33-05 HeroUI v3 rewrite — assertion updated to match the post-33-05 token shape, OR wrapper restored to the assertion-expected token shape.'
    - '`responsive.test.ts` 15 failures (viewport detection) close after Plan 50-09 installed the matchMedia polyfill. If still failing, the per-test override of `window.matchMedia` per viewport (`mobile`/`tablet`/`desktop`) needs preservation alongside the global polyfill.'
    - '`KpiStrip.test.tsx`, `ForumsListPage.test.tsx`, `monitoring.dashboard.test.tsx` per-test failures reconciled per D-10 archaeology — these are TanStack Router harness + dashboard widget drift inherited from the Phase 38/40 baselines.'
    - 'No file in scope exits this plan in `queued-with-rationale` state per D-01.'
    - "PCH-50R-01 honored: Plan 50-12 does NOT edit `frontend/tests/setup.ts`. Translation keys needed by ForumsListPage.test.tsx and monitoring.dashboard.test.tsx are added in Plan 50-10's batched setup.ts edit. This plan READS setup.ts only."
    - 'Per-file impl-edit disposition gates fire ONLY when the corresponding D-09 disposition was selected by the executor. The verify block reads `50-12-DISCOVERY.md` to determine which dispositions were chosen and runs the matching grep gates conditionally (PCH-50R-06).'
  artifacts:
    - path: 'frontend/tests/unit/design-system/buildTokens.test.ts'
      provides: 'buildTokens unit test aligned with current token engine output OR token engine restored to test-expected output'
      contains: 'sla-bad'
    - path: 'frontend/tests/unit/design-system/fouc-bootstrap.test.ts'
      provides: 'fouc-bootstrap drift guard with regex aligned to current bootstrap.js palette shape OR bootstrap.js resynced'
      contains: 'chancery'
  key_links:
    - from: 'frontend/tests/unit/design-system/fouc-bootstrap.test.ts'
      to: 'frontend/public/bootstrap.js'
      via: 'Test regex extracts direction.theme palette objects from bootstrap.js literal; either the regex updates to match bootstrap.js OR bootstrap.js resyncs to match the regex (canonical = tokens/directions.ts per CLAUDE.md)'
      pattern: 'chancery|situation|ministerial'
    - from: 'frontend/tests/unit/hooks/responsive.test.ts'
      to: 'frontend/tests/setup.ts matchMedia polyfill (Plan 50-09 owned; Plan 50-10 may extend translations only)'
      via: "Default matchMedia returns {matches: false, ...}; per-test override sets matches per viewport breakpoint to satisfy useResponsive's mobile/tablet/desktop detection. The test file (Plan 50-12 owned) is edited; setup.ts (Plan 50-10 owned) is read-only."
      pattern: 'matchMedia'
    - from: 'Plan 50-12 ForumsListPage.test.tsx + monitoring.dashboard.test.tsx'
      to: 'Plan 50-10 setup.ts batched edit (translations map keys: forums:pageTitle, forums:pageSubtitle, monitoring.headings.*)'
      via: 'Plan 50-12 read_first confirms the keys are present in setup.ts (added by 50-10 Task 1). If absent at task start, Plan 50-12 halts and escalates (50-10 should land first in Wave 3 ordering).'
      pattern: 'forums:pageTitle|monitoring\\.headings'
---

<objective>
Close 7 files spanning three independent root-cause classes that are best triaged together because they all involve product-source-or-test reconciliation against a known phase baseline (33/34/35/36/38/40):

1. **Design-system unit drift (3 files):**
   - `tests/unit/design-system/buildTokens.test.ts`: 1 of 99 fails — SC-3 `--sla-bad` hue-locked test expects `oklch(54% 0.2 25)` but `buildTokens` emits `oklch(46% 0.18 25)`. Per the v6.0/35-04 STATE entry, `sla-bad` was reclassified as hue-locked at 25 — verify whether the test was authored before the lock or after.
   - `tests/unit/design-system/fouc-bootstrap.test.ts`: 9 of 10 fail — scrape regex doesn't match current `frontend/public/bootstrap.js` palette shape for 3 directions × 2 themes (chancery / situation / ministerial × light / dark). Plan 50-07 archived had the same fix.
   - `tests/unit/components/ui/heroui-wrappers.test.tsx`: 2 of 11 fail — Phase 33-05 HeroUI v3 rewrite drift on semantic-token assertions for `destructive` variant.

2. **Hook + route harness drift (2 files):**
   - `tests/unit/hooks/responsive.test.ts`: 15 of 15 fail — viewport detection always returns `desktop`. Plan 50-09 polyfill installs matchMedia stub returning `matches: false`; the test's per-test override of matchMedia needs to coexist with the global polyfill.
   - `tests/unit/monitoring.dashboard.test.tsx`: 1 of 1 fails — `renders headings` assertion. Likely text-assertion or harness drift.

3. **Dashboard widget + list page drift (2 files):**
   - `src/pages/Dashboard/widgets/__tests__/KpiStrip.test.tsx`: 2 of 5 fail — `expected ['12+2', '8−4', '3+2', '5+1'] to deeply equal ['12', '8', '3', '5']`. The widget concatenates absolute + delta values in its rendered text; the test fixture expects bare values. Likely Phase 38 DASH-04 deviation.
   - `src/routes/_protected/dossiers/forums/__tests__/ForumsListPage.test.tsx`: 4 of 4 fail — `renders Forums title`. Migrated to renderWithProviders in 50-06 but still failing. Likely i18n key (`forums:pageTitle`) vs assertion text (`'Forums'`).

Plan 50-12 OWNS 7 test-file modifications PLUS up to 6 product-source-impl files when D-09 IMPL DRIFTED dispositions are selected (bootstrap.js, buildTokens.ts, heroui-button.tsx, KpiStrip.tsx, useResponsive.ts, monitoring/Dashboard.tsx). The product-source files are listed in `files_modified` so file-ownership conflicts cannot arise. Wave 3 parallel safety is preserved: setup.ts ownership is delegated to Plan 50-10 (PCH-50R-01).

Runs in parallel with Plan 50-11. Wave-ordered AFTER Plan 50-10 (intra-Wave-3 dependency).

Purpose: Close the design-system + route harness + dashboard widget cluster so Plan 50-13 inherits only ≤6 files (per-file impl drift like useCountryAutoFill).

Output:

- 7 test files passing green individually.
- Up to 6 product-source files modified per D-09 disposition (each listed in `files_modified` for ownership clarity).
- Per-file commits documenting D-10 verdict + impl edits when needed (D-09 permits product-source changes per case).
- Any byte-match-contract violations (fouc-bootstrap) restored per CLAUDE.md.
- `frontend/tests/setup.ts` UNCHANGED (Plan 50-10 owns it in Wave 3 per PCH-50R-01).
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-test-infrastructure-repair/50-CONTEXT.md
@.planning/phases/50-test-infrastructure-repair/50-RESEARCH.md
@.planning/phases/50-test-infrastructure-repair/50-PATTERNS.md
@.planning/phases/50-test-infrastructure-repair/50-07-PLAN.archived.md
@.planning/phases/50-test-infrastructure-repair/50-09-SUMMARY.md
@.planning/phases/50-test-infrastructure-repair/50-10-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Per-cluster diagnosis. -->

From `/tmp/phase50-06-task4-full-after-mock-fix.log`:

- `buildTokens.test.ts:212`: `expect(light['--sla-bad']).toBe('oklch(54% 0.2 25)')` — actual `'oklch(46% 0.18 25)'`. Single failure inside the SC-3 hue-locked block.
- `fouc-bootstrap.test.ts:49`: `throw new Error("Failed to scrape ${dir}.${mode} palette from bootstrap.js")` — 9 sub-tests fail via this throw (chancery/light, chancery/dark, situation/light, situation/dark, ministerial/light, ministerial/dark, bureau/light + 2 more — likely fonts or `bureau.dark` per the 10-of-10-minus-1 shape).
- `heroui-wrappers.test.tsx`: 2 fail. Looking at the failure line: `destructive variant uses only semantic tokens — no literal color utilities`. The test asserts that the rendered button for the `destructive` variant contains only token-based classes; if the wrapper's destructive variant uses a Tailwind color literal (e.g., `bg-red-500`), the assertion fires.
- `responsive.test.ts:53`: `expect(result.current.viewport).toBe('mobile')` — actual `'desktop'`. matchMedia override pattern.
- `KpiStrip.test.tsx:53`: `expect(...).toEqual(['12', '8', '3', '5'])` — actual `['12+2', '8−4', '3+2', '5+1']`. The widget renders delta values concatenated.
- `ForumsListPage.test.tsx`: 4 tests fail with title-not-found. The page renders `t('forums:pageTitle')` — global mock returns the key.
- `monitoring.dashboard.test.tsx`: `renders headings` test fails. Likely same i18n-key-vs-English-string pattern as 50-10's cluster.

From `.planning/STATE.md` `[v6.0/35-04]` entry (sla-bad hue-locked context):

> "Two legacy weight references (`--text-weight`, `--display-weight`) folded into existing Tailwind-scale `--font-normal` (400) / `--font-semibold` (600) — same rendered weight, different token name."

And earlier:

> "[v6.0/33-09] fix(33-09): tighten SC-1 + SC-4 assertions against actual token behavior (SC-1: `--accent` is hue-driven not direction-driven, `--surface-raised` collapses to pure white in both light palettes — removed from delta check. SC-4: set `dir='rtl'` directly on the probe element instead of on `<html>`..."

The pattern is clear: the test engine evolved AFTER the test assertions were authored, and the assertions are TEST WRONG per D-10. Verify with `git log -p frontend/src/design-system/tokens/buildTokens.ts | grep -A4 "sla-bad"` at task time.

From CLAUDE.md (the byte-match contract):

> "the FOUC bootstrap that paints first-frame tokens is at `frontend/public/bootstrap.js` — its palette/font literals must byte-match `tokens/directions.ts`."

This means: if `bootstrap.js` literals diverge from `tokens/directions.ts`, the test SHOULD fire. The remediation is to resync `bootstrap.js` (NOT to weaken the test). However, if the test's regex shape is too brittle for a valid `bootstrap.js` change, update the regex.

From Plan 50-07 archived Task 3 (the fouc-bootstrap disposition tree — preserved verbatim):

- Case I — bootstrap.js drifted from directions.ts → update bootstrap.js (D-09 root-cause). Touches `frontend/public/bootstrap.js`.
- Case II — regex too strict for a valid bootstrap.js shape → update regex. Touches the test file only.
- Case III — both drifted → update both to match directions.ts canonical form. Touches `frontend/public/bootstrap.js` AND the test file.

From `frontend/src/hooks/useResponsive.ts` (the consumer):

- Reads `window.matchMedia('(max-width: <breakpoint>px)').matches` for each viewport breakpoint.
- Returns 'mobile' if narrowest matches, etc.
- Plan 50-12 may need to touch `useResponsive.ts` only if D-09 returns IMPL DRIFTED (e.g., the hook's media-query string contract changed since the test was authored). Default disposition is TEST WRONG → fix per-test matchMedia spy.

From `frontend/tests/unit/hooks/responsive.test.ts` (the test):

- Per `pnpm exec vitest --run` log: `it('should detect mobile viewport correctly', ...)` likely overrides `window.matchMedia` per-test to make a specific breakpoint match. The global polyfill from 50-09 sets `matches: false` for all — the test MUST override it before the hook reads. Verify per-test override pattern is present; if not, add `vi.spyOn(window, 'matchMedia').mockReturnValue({matches: <breakpoint-specific>, ...})` in each `it` block.

From `frontend/src/pages/Dashboard/widgets/KpiStrip.tsx` (the impl):

- Renders 4 KPI cards. Each card's number is the cumulative value PLUS a delta indicator (e.g., `12+2` means "12 total, +2 since last period"). This is Phase 38 DASH-04 behavior.
- The test fixture mocks `useDashboardStats()` to return base values (`[12, 8, 3, 5]`) and asserts the rendered text matches those bare values. Test was authored before the delta-rendering shipped — TEST WRONG per D-10.
- Fix: update the test to assert against the full rendered text including deltas, OR mock `useDashboardStats` to return deltas of 0 so the rendered string is just `'12'`. Default disposition is TEST WRONG (test-file edit only). Plan 50-12 may touch `KpiStrip.tsx` only if D-09 returns IMPL DRIFTED (e.g., the delta-rendering itself is the bug — unlikely per the v6.0 STATE notes).

From `frontend/tests/setup.ts` translations map (POST-50-10):

- Plan 50-10 OWNS the setup.ts batched edit and will have added `forums:pageTitle`, `forums:pageSubtitle`, and `monitoring.headings.*` keys.
- Plan 50-12 READ-ONLY usage: confirm the keys exist via grep in Task 0 discovery. DO NOT edit setup.ts (PCH-50R-01).

**D-09 IMPL-EDIT DISPOSITIONS (PCH-50R-06 conditional grep gates):**

Each design-system / hook / widget failure has TWO possible dispositions:

- **TEST WRONG (default):** edit only the test file.
- **IMPL DRIFTED:** also edit the matching product-source file.

The executor must record the chosen disposition per file in `50-12-DISCOVERY.md` (Task 0). The verify block conditionally fires grep gates based on which dispositions were chosen:

| Test file                           | IMPL-edit candidate (if D-09 IMPL DRIFTED chosen)                                              | Conditional grep gate (Task verify)                                                                                                         |
| ----------------------------------- | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| `buildTokens.test.ts`               | `frontend/src/design-system/tokens/buildTokens.ts` (sla-bad formula)                           | `grep -c "sla-bad" frontend/src/design-system/tokens/buildTokens.ts >= 1` AND the changed line emits the test-expected oklch value          |
| `fouc-bootstrap.test.ts` (Case I)   | `frontend/public/bootstrap.js` (resync to directions.ts)                                       | `grep -c "chancery" frontend/public/bootstrap.js >= 2` AND the chancery palette object byte-matches `tokens/directions.ts`                  |
| `fouc-bootstrap.test.ts` (Case III) | both `bootstrap.js` AND test (regex update)                                                    | both gates above + the test file's regex changed                                                                                            |
| `heroui-wrappers.test.tsx`          | `frontend/src/components/ui/heroui-button.tsx` (destructive variant class list)                | `grep -c "destructive" frontend/src/components/ui/heroui-button.tsx >= 1` AND no `bg-red-500` / Tailwind color literal in the changed range |
| `responsive.test.ts`                | `frontend/src/hooks/useResponsive.ts` (rare; only if media-query contract changed)             | `grep -c "matchMedia" frontend/src/hooks/useResponsive.ts >= 1`                                                                             |
| `KpiStrip.test.tsx`                 | `frontend/src/pages/Dashboard/widgets/KpiStrip.tsx` (rare; only if delta-rendering is the bug) | `grep -c "delta" frontend/src/pages/Dashboard/widgets/KpiStrip.tsx >= 1` AND test expectation aligns                                        |
| `monitoring.dashboard.test.tsx`     | `frontend/src/pages/monitoring/Dashboard.tsx` (rare)                                           | `grep -c "heading" frontend/src/pages/monitoring/Dashboard.tsx >= 1`                                                                        |

The Task 1 and Task 2 verify blocks read `50-12-DISCOVERY.md` for each file's chosen disposition and conditionally run the corresponding grep. If `50-12-DISCOVERY.md` is absent or a file's disposition is "TEST WRONG only", the impl-grep gate is skipped for that file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Discovery — verify in-scope files still fail; capture per-file signatures + record D-09 dispositions</name>
  <files>.planning/phases/50-test-infrastructure-repair/50-12-DISCOVERY.md</files>
  <read_first>
    - .planning/phases/50-test-infrastructure-repair/50-09-SUMMARY.md (the matchMedia polyfill may have already auto-closed responsive.test.ts; verify)
    - .planning/phases/50-test-infrastructure-repair/50-10-SUMMARY.md (confirm setup.ts now contains forums:pageTitle, forums:pageSubtitle, monitoring.headings.* per PCH-50R-01)
    - frontend/tests/setup.ts (POST-50-10 — grep for the keys; halt and escalate if absent)
  </read_first>
  <action>
    Discovery + ceiling check + D-09 disposition recording.

    1. Run `pnpm --filter intake-frontend exec vitest --run --reporter=default 2>&1 | tee /tmp/phase50-12-discovery.log`.

    2. For each of the 7 test files in scope, capture pass/fail count + first failure signature.

    3. **PCH-50R-01 gate:** `grep -E "forums:pageTitle|monitoring.headings" frontend/tests/setup.ts` — must return matches. If empty, Plan 50-10 has not yet landed Task 1; HALT and escalate to operator (Wave-3 ordering violation).

    4. **Ceiling check:** if MORE than 8 of THIS PLAN'S files are failing, halt. Scope owns 7 test files + up to 6 conditional impl files — file-modify count below 13 is OK; failing-file count must be ≤8. If a file auto-closed via 50-09 or 50-10, document and shrink scope.

    5. **Record D-09 dispositions per file:** For EACH of the 7 test files, read the impl AND the test, then declare in DISCOVERY.md whether the disposition is `TEST WRONG` (test-only edit) or `IMPL DRIFTED` (also requires product-source edit). The declaration drives the Task 1/2 verify block's conditional grep gates (PCH-50R-06).

       Format in DISCOVERY.md:
       ```
       ## D-09 Dispositions
       - buildTokens.test.ts: <TEST WRONG | IMPL DRIFTED> — <rationale>
       - fouc-bootstrap.test.ts: <Case I | Case II | Case III> — <rationale>
       - heroui-wrappers.test.tsx: <TEST WRONG | IMPL DRIFTED> — <rationale>
       - responsive.test.ts: <TEST WRONG | IMPL DRIFTED> — <rationale>
       - KpiStrip.test.tsx: <TEST WRONG | IMPL DRIFTED> — <rationale>
       - ForumsListPage.test.tsx: <TEST WRONG | IMPL DRIFTED> — <rationale> (translations-map already covered by 50-10)
       - monitoring.dashboard.test.tsx: <TEST WRONG | IMPL DRIFTED> — <rationale> (translations-map already covered by 50-10)
       ```

    6. Write `50-12-DISCOVERY.md` enumerating per-file state + the disposition block.

    7. Commit `chore(50-12): record discovery snapshot + D-09 dispositions for ≤8 ceiling check`.

  </action>
  <verify>
    <automated>test -f /tmp/phase50-12-discovery.log &amp;&amp; test -f .planning/phases/50-test-infrastructure-repair/50-12-DISCOVERY.md &amp;&amp; grep -cE "forums:pageTitle|monitoring.headings" frontend/tests/setup.ts | awk '$1 &gt;= 1 { exit 0 } { exit 1 }' &amp;&amp; grep -q "## D-09 Dispositions" .planning/phases/50-test-infrastructure-repair/50-12-DISCOVERY.md</automated>
  </verify>
  <done>
    DISCOVERY.md records 7-file state. PCH-50R-01 verified (Plan 50-10's setup.ts keys present). Per-file D-09 dispositions recorded (TEST WRONG vs IMPL DRIFTED).
  </done>
</task>

<task type="auto">
  <name>Task 1: Design-system cluster — buildTokens + fouc-bootstrap + heroui-wrappers (3 test files; up to 3 impl files conditional on D-09)</name>
  <files>frontend/tests/unit/design-system/buildTokens.test.ts, frontend/tests/unit/design-system/fouc-bootstrap.test.ts, frontend/tests/unit/components/ui/heroui-wrappers.test.tsx, frontend/public/bootstrap.js, frontend/src/design-system/tokens/buildTokens.ts, frontend/src/components/ui/heroui-button.tsx</files>
  <read_first>
    - .planning/phases/50-test-infrastructure-repair/50-12-DISCOVERY.md (the D-09 disposition block — drives the conditional impl edits)
    - frontend/tests/unit/design-system/buildTokens.test.ts (full file — focus on SC-3 hue-locked block, line 212 region)
    - frontend/src/design-system/tokens/buildTokens.ts (impl — the function the test exercises; identify the canonical `--sla-bad` formula)
    - `git log --oneline -- frontend/src/design-system/tokens/buildTokens.ts` (impl history — confirm whether sla-bad output value changed; if a Phase 35-04 commit changed it, the test is TEST WRONG)
    - frontend/tests/unit/design-system/fouc-bootstrap.test.ts (full file — identify the scrape regex; 9 failing assertions)
    - frontend/public/bootstrap.js (current shape — compare to test's expected regex)
    - frontend/src/design-system/tokens/directions.ts (canonical source of truth — confirm 4-direction palette shape)
    - frontend/tests/unit/components/ui/heroui-wrappers.test.tsx (full file — identify the 2 failing tests for `destructive` variant)
    - frontend/src/components/ui/heroui-button.tsx (impl — confirm post-33-05 token usage for `destructive`)
    - CLAUDE.md §Visual Design Source of Truth (FOUC bootstrap palette/font literals MUST byte-match `tokens/directions.ts`)
    - .planning/phases/50-test-infrastructure-repair/50-07-PLAN.archived.md Task 3 (fouc-bootstrap disposition tree)
    - .planning/phases/50-test-infrastructure-repair/50-CONTEXT.md D-09, D-10
  </read_first>
  <action>
    Three-test-file design-system cluster. Each gets its own commit. Impl files (`bootstrap.js`, `buildTokens.ts`, `heroui-button.tsx`) are touched ONLY when the corresponding D-09 disposition (recorded in DISCOVERY.md) selects IMPL DRIFTED / Case I / Case III.

    **A) `buildTokens.test.ts` — single SC-3 sla-bad mismatch:**

    1. Read the SC-3 hue-locked block. Around line 212: `expect(light['--sla-bad']).toBe('oklch(54% 0.2 25)')`.

    2. Run `git log -p --follow frontend/src/design-system/tokens/buildTokens.ts | grep -A6 "sla-bad"` — identify when the output formula was set. The current emit is `oklch(46% 0.18 25)`. Per the v6.0/35-04 STATE entry (re-read at task time), if a Phase 35-04 commit changed the formula, the test is TEST WRONG.

    3. Disposition per DISCOVERY.md:
       - **TEST WRONG:** Update the test line to match (`oklch(46% 0.18 25)`). Also update the dark-mode assertion at line 213 if it shows the same drift. `frontend/src/design-system/tokens/buildTokens.ts` is NOT touched.
       - **IMPL DRIFTED:** Restore `buildTokens.ts` to emit the test-expected `oklch(54% 0.2 25)` (require explicit STATE.md evidence the current `46%/0.18` is the bug). The test file is NOT touched.

    Commit: `fix(50-12): buildTokens test — update sla-bad assertion to match current hue-locked formula (D-10: TEST WRONG per Phase 35-04)` OR `fix(50-12): buildTokens.ts — restore sla-bad formula to oklch(54% 0.2 25) (D-09: IMPL DRIFTED)`.

    **B) `fouc-bootstrap.test.ts` — 9 palette-scrape failures:**

    Apply the Plan 50-07 archived disposition tree verbatim. The disposition (Case I / II / III) was selected in Task 0 DISCOVERY.md.

    1. Read the test's scrape regex.

    2. Read `frontend/public/bootstrap.js` and identify the current palette literal shape for chancery / situation / ministerial / bureau.

    3. Cross-reference with `frontend/src/design-system/tokens/directions.ts`.

    4. Disposition per DISCOVERY.md:
       - **Case I** — bootstrap.js drifted from directions.ts → update `frontend/public/bootstrap.js` to match `directions.ts` (D-09 root-cause; preserves the byte-match contract per CLAUDE.md). Test file UNCHANGED.
       - **Case II** — regex too strict for a valid bootstrap.js → update only the test's regex. `bootstrap.js` UNCHANGED.
       - **Case III** — both drifted from directions.ts → update both to canonical form.

    5. Verify all 9 sub-tests pass after the fix. NO `.skip` on sub-tests. The drift-guard intent is preserved.

    Commit: `fix(50-12): fouc-bootstrap test — <Case I/II/III chosen> (PF-2; CLAUDE.md byte-match contract preserved)`.

    **C) `heroui-wrappers.test.tsx` — 2 destructive-variant assertions:**

    1. Read the failing test cases (`destructive variant uses only semantic tokens — no literal color utilities`).

    2. Read `frontend/src/components/ui/heroui-button.tsx` (or the relevant wrapper) and check the `destructive` variant's class list.

    3. Disposition per DISCOVERY.md:
       - **TEST WRONG:** Update the test's expected class list to match the current wrapper class list. `heroui-button.tsx` NOT touched.
       - **IMPL DRIFTED:** If the wrapper uses a Tailwind color literal (e.g., `bg-red-500 text-white` in the destructive variant) → that's a real DESIGN-01/02 violation per Phase 51. Restore the wrapper to use semantic tokens. Test file NOT touched.

    Commit: `fix(50-12): heroui-wrappers test — align destructive variant assertion with post-33-05 wrapper (D-10: TEST WRONG)` OR `fix(50-12): heroui-button.tsx — restore destructive variant to semantic tokens (D-09: IMPL DRIFTED)`.

    After all three test files land, run `pnpm --filter intake-frontend exec vitest --run tests/unit/design-system/ tests/unit/components/ui/heroui-wrappers.test.tsx 2>&1 | tail -5` — expect 0 failed.

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm exec vitest --run --reporter=default tests/unit/design-system/buildTokens.test.ts tests/unit/design-system/fouc-bootstrap.test.ts tests/unit/components/ui/heroui-wrappers.test.tsx 2&gt;&amp;1 | tail -5 | grep -E "Test Files" | awk '{ for(i=1;i&lt;=NF;i++) if ($i ~ /failed/) { print "FAIL: still failing"; exit 1 } print "PASS"; exit 0 }' &amp;&amp; { [ ! -f ../.planning/phases/50-test-infrastructure-repair/50-12-DISCOVERY.md ] || { ! grep -q "fouc-bootstrap.*Case I\|fouc-bootstrap.*Case III" ../.planning/phases/50-test-infrastructure-repair/50-12-DISCOVERY.md || { grep -c "chancery" public/bootstrap.js | awk '$1 &gt;= 2 { exit 0 } { exit 1 }' &amp;&amp; diff &lt;(grep -oE "chancery[^,}]*" public/bootstrap.js | sort -u) &lt;(grep -oE "chancery[^,}]*" src/design-system/tokens/directions.ts | sort -u) &gt; /dev/null; }; }; } &amp;&amp; { [ ! -f ../.planning/phases/50-test-infrastructure-repair/50-12-DISCOVERY.md ] || { ! grep -q "buildTokens.*IMPL DRIFTED" ../.planning/phases/50-test-infrastructure-repair/50-12-DISCOVERY.md || grep -c "sla-bad" src/design-system/tokens/buildTokens.ts | awk '$1 &gt;= 1 { exit 0 } { exit 1 }'; }; } &amp;&amp; { [ ! -f ../.planning/phases/50-test-infrastructure-repair/50-12-DISCOVERY.md ] || { ! grep -q "heroui-wrappers.*IMPL DRIFTED" ../.planning/phases/50-test-infrastructure-repair/50-12-DISCOVERY.md || { grep -c "destructive" src/components/ui/heroui-button.tsx | awk '$1 &gt;= 1 { exit 0 } { exit 1 }' &amp;&amp; ! grep -E "bg-red-[0-9]+" src/components/ui/heroui-button.tsx; }; }; }</automated>
  </verify>
  <done>
    All 3 design-system unit test files pass green. CLAUDE.md byte-match contract for `bootstrap.js` ↔ `directions.ts` preserved (verified by diff when Case I / III was chosen). NO `.skip` introduced. Per-file D-09 dispositions matched by impl-edit grep gates per PCH-50R-06.
  </done>
</task>

<task type="auto">
  <name>Task 2: Hook + route + dashboard cluster — responsive + KpiStrip + ForumsListPage + monitoring.dashboard (4 test files; up to 3 impl files conditional on D-09)</name>
  <files>frontend/tests/unit/hooks/responsive.test.ts, frontend/src/pages/Dashboard/widgets/__tests__/KpiStrip.test.tsx, frontend/src/routes/_protected/dossiers/forums/__tests__/ForumsListPage.test.tsx, frontend/tests/unit/monitoring.dashboard.test.tsx, frontend/src/hooks/useResponsive.ts, frontend/src/pages/Dashboard/widgets/KpiStrip.tsx, frontend/src/pages/monitoring/Dashboard.tsx</files>
  <read_first>
    - .planning/phases/50-test-infrastructure-repair/50-12-DISCOVERY.md (D-09 dispositions per file)
    - frontend/tests/unit/hooks/responsive.test.ts (full file — identify per-test matchMedia override pattern; should coexist with Plan 50-09 global polyfill)
    - frontend/src/hooks/useResponsive.ts (impl — confirm matchMedia query strings)
    - frontend/src/pages/Dashboard/widgets/KpiStrip.tsx (impl — confirm delta-rendering behavior)
    - frontend/src/pages/Dashboard/widgets/__tests__/KpiStrip.test.tsx (full file — identify the test's mock of useDashboardStats and the failing assertion at line 53)
    - frontend/src/routes/_protected/dossiers/forums/ForumsListPage.tsx (impl — confirm `t('forums:pageTitle')` use)
    - frontend/src/routes/_protected/dossiers/forums/__tests__/ForumsListPage.test.tsx (full file — identify what text the assertions expect)
    - frontend/tests/setup.ts (POST-50-10 — confirm forums:pageTitle / monitoring.headings.* keys exist; READ ONLY per PCH-50R-01)
    - frontend/public/locales/en/forums.json (canonical English values — already used by Plan 50-10 batched edit)
    - frontend/tests/unit/monitoring.dashboard.test.tsx (full file — identify the single `renders headings` failure)
    - frontend/src/pages/monitoring/Dashboard.tsx (impl)
    - .planning/phases/50-test-infrastructure-repair/50-CONTEXT.md D-09, D-10, D-11
    - .planning/phases/50-test-infrastructure-repair/50-09-SUMMARY.md (the matchMedia polyfill default shape — `matches: false`)
  </read_first>
  <action>
    Four-test-file fix. Independent dispositions; one commit per file. Impl files (`useResponsive.ts`, `KpiStrip.tsx`, `monitoring/Dashboard.tsx`) touched only when D-09 disposition recorded in DISCOVERY.md is IMPL DRIFTED.

    `frontend/tests/setup.ts` is NOT edited in this task (Plan 50-10 owns it; the required translation keys have already been added per PCH-50R-01).

    **A) `responsive.test.ts` — 15 failures (viewport detection):**

    1. Read the file. Identify each `it()` block — there should be one per viewport (mobile / tablet / desktop). Each block likely sets `window.innerWidth` and expects `useResponsive` to report the right viewport.

    2. The Plan 50-09 polyfill installs a default `window.matchMedia` returning `matches: false`. The test's per-test override (if any) must run BEFORE the hook's `useEffect` fires.

    3. Disposition per DISCOVERY.md:
       - **TEST WRONG (default):** Add `vi.spyOn(window, 'matchMedia').mockImplementation((query) => ({ matches: query.includes('(max-width: 767px)'), media: query, ...stubs }))` in each `it` block (matching specific media queries useResponsive reads). `useResponsive.ts` NOT touched.
       - **IMPL DRIFTED (rare):** If the hook's media-query contract changed (e.g., breakpoint constants shifted) without test update → update `useResponsive.ts` to restore the contract. The test stays as-is.

    Commit: `fix(50-12): responsive.test.ts — per-test matchMedia spy override per viewport (works alongside 50-09 global polyfill)`.

    **B) `KpiStrip.test.tsx` — 2 failures (delta-rendering drift):**

    1. Read the failing test at line 53. The assertion expects `['12', '8', '3', '5']` — bare values without deltas.

    2. Read `KpiStrip.tsx` impl. Confirm whether the rendered output is `<span>12</span><span>+2</span>` (composite) or `<span>12+2</span>` (concatenated). The error says actual is `['12+2', '8−4', '3+2', '5+1']` — concatenated.

    3. Disposition per DISCOVERY.md:
       - **TEST WRONG (default):** Update either the assertion (`['12+2', '8−4', '3+2', '5+1']`) OR the mock fixture (set deltas to 0 so rendered string is `'12'` not `'12+0'`). Preferred fix per Karpathy §3: update the fixture deltas to 0. `KpiStrip.tsx` NOT touched.
       - **IMPL DRIFTED (rare):** Only if the delta-rendering itself is the bug — restore `KpiStrip.tsx` to the bare-value rendering.

    Second failing test (`wraps all 4 numerals in <LtrIsolate>`) — read and apply D-10 separately. If LtrIsolate was renamed or replaced (Phase 36-04 introduced direction-aware components), update the test's selector.

    Commit: `fix(50-12): KpiStrip test — neutralize deltas in fixture + update LtrIsolate selector (D-10: TEST WRONG per Phase 38 DASH-04)`.

    **C) `ForumsListPage.test.tsx` — 4 failures (i18n-text-assertion drift):**

    1. The test asserts `expect(screen.getByText('Forums')).toBeInTheDocument()`. The impl renders `t('forums:pageTitle')`. The global mock returns the key.

    2. **PCH-50R-01:** The translations-map fix is OWNED by Plan 50-10. Verify at task start: `grep -c "forums:pageTitle" frontend/tests/setup.ts` must return ≥1. If 0, Plan 50-10 has not yet landed — halt and escalate.

    3. With setup.ts already extended by 50-10, the assertion `screen.getByText('Forums')` should now pass (the mock returns `'Forums'` instead of the literal key). Re-run the test; if it passes, no test-file edit needed. If it still fails, the remaining diagnosis is:
       - The 3 other failing tests (`renders one row per forum`, `maps active → chip-ok`, `maps cancelled → chip-danger`) are likely impl-markup or chip-class drift. Apply D-10 per case.
       - If the test asserts on the colon-form key but setup.ts has the dot-form (or vice versa), align the test file to match the impl's actual `t()` call form (Plan 50-10 used impl-derived form). DO NOT edit setup.ts.

    Commit: `fix(50-12): ForumsListPage test — reconcile chip-class assertions (translations covered by 50-10 batched edit)`.

    **D) `monitoring.dashboard.test.tsx` — 1 failure (renders headings):**

    1. Read the failing test in full. Likely a single `it('renders headings')` asserting text via getByRole('heading').

    2. The impl is `frontend/src/pages/monitoring/Dashboard.tsx`. Read it.

    3. **PCH-50R-01:** The translations-map fix for the heading keys is owned by Plan 50-10. Verify the relevant `monitoring.headings.*` keys are present in setup.ts. If 0, halt.

    4. With setup.ts extended, the `renders headings` assertion should pass. If it still fails:
       - **TEST WRONG:** Update the test's assertion (e.g., the test might assert on a heading the impl no longer renders).
       - **IMPL DRIFTED:** Restore `monitoring/Dashboard.tsx` to render the heading the test expects.

    Commit: `fix(50-12): monitoring.dashboard test — align renders-headings assertion (D-10: TEST WRONG; translations covered by 50-10)`.

    After all four files land, run `pnpm --filter intake-frontend exec vitest --run tests/unit/hooks/responsive.test.ts src/pages/Dashboard/widgets/__tests__/KpiStrip.test.tsx src/routes/_protected/dossiers/forums/__tests__/ForumsListPage.test.tsx tests/unit/monitoring.dashboard.test.tsx 2>&1 | tail -5` — expect 0 failed.

    Lint check: `pnpm --filter intake-frontend lint` on the four modified test files — must exit 0.

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm exec vitest --run --reporter=default tests/unit/hooks/responsive.test.ts src/pages/Dashboard/widgets/__tests__/KpiStrip.test.tsx src/routes/_protected/dossiers/forums/__tests__/ForumsListPage.test.tsx tests/unit/monitoring.dashboard.test.tsx 2&gt;&amp;1 | tail -5 | grep -E "Test Files" | awk '{ for(i=1;i&lt;=NF;i++) if ($i ~ /failed/) { print "FAIL: still failing"; exit 1 } print "PASS"; exit 0 }' &amp;&amp; { [ ! -f ../.planning/phases/50-test-infrastructure-repair/50-12-DISCOVERY.md ] || { ! grep -q "responsive.*IMPL DRIFTED" ../.planning/phases/50-test-infrastructure-repair/50-12-DISCOVERY.md || grep -c "matchMedia" src/hooks/useResponsive.ts | awk '$1 &gt;= 1 { exit 0 } { exit 1 }'; }; } &amp;&amp; { [ ! -f ../.planning/phases/50-test-infrastructure-repair/50-12-DISCOVERY.md ] || { ! grep -q "KpiStrip.*IMPL DRIFTED" ../.planning/phases/50-test-infrastructure-repair/50-12-DISCOVERY.md || grep -c "delta" src/pages/Dashboard/widgets/KpiStrip.tsx | awk '$1 &gt;= 1 { exit 0 } { exit 1 }'; }; } &amp;&amp; { [ ! -f ../.planning/phases/50-test-infrastructure-repair/50-12-DISCOVERY.md ] || { ! grep -q "monitoring.dashboard.*IMPL DRIFTED" ../.planning/phases/50-test-infrastructure-repair/50-12-DISCOVERY.md || grep -ci "heading" src/pages/monitoring/Dashboard.tsx | awk '$1 &gt;= 1 { exit 0 } { exit 1 }'; }; } &amp;&amp; ! git diff --name-only HEAD~10 -- tests/setup.ts | grep -q "tests/setup.ts" ; true</automated>
  </verify>
  <done>
    All 4 test files exit vitest green individually AND together. Per-file commits document D-10 verdict + impl/test edits. PCH-50R-01: this plan did NOT edit `frontend/tests/setup.ts` (Plan 50-10 owns it). PCH-50R-06: conditional impl-grep gates fired only for IMPL DRIFTED dispositions.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                         | Description                                                                                                                                                                                                   |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Test assertion ↔ design-token output             | Token-engine tests guard the public API of buildTokens; weakening them lets palette regressions ship.                                                                                                         |
| bootstrap.js ↔ directions.ts byte-match contract | CLAUDE.md mandates byte-match; both .skip-ing sub-tests and weakening regex undermine the FOUC paint contract.                                                                                                |
| matchMedia spy ↔ global polyfill                 | A spy that doesn't restore correctly leaks state to the next test file (pool: 'forks' isolates between files, but within a file the spy must be cleaned by vitest's auto-restore or an explicit `afterEach`). |
| Wave-3 setup.ts ownership ↔ Plan 50-10           | Plan 50-10 owns setup.ts in Wave 3. Plan 50-12 must NOT edit it. Cross-write would cause parallel-merge conflict (PCH-50R-01).                                                                                |

## STRIDE Threat Register

| Threat ID  | Category                   | Component                                                                                                                                          | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                    |
| ---------- | -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-50-12-01 | T (Tampering)              | Weakening the fouc-bootstrap regex masks first-frame palette regressions.                                                                          | mitigate    | Task 1B explicitly forbids `.skip` and regex permissiveness beyond what bootstrap.js needs. The drift-guard intent is documented in the commit body; Plan 50-04 audit cross-checks. When Case I or III is chosen, the verify block grep-diffs `chancery` (and similar) palette strings between `bootstrap.js` and `directions.ts` to confirm byte-match.           |
| T-50-12-02 | I (Information disclosure) | Updating buildTokens test assertion to match output without confirming the output is canonical lets an impl bug slip into the test's pinned value. | mitigate    | Task 1A requires `git log -p` archaeology to confirm whether the formula change was intentional. If unclear, commit body records the uncertainty and Plan 50-04 audit re-verifies.                                                                                                                                                                                 |
| T-50-12-03 | E (Elevation of privilege) | matchMedia spy override might overshadow real-environment polyfills if the spy isn't restored.                                                     | accept      | Vitest auto-restores `vi.spyOn` mocks between tests within a file (per RESEARCH §Common Pitfalls Pitfall 3 — `clear` for call history, vitest's `restoreMocks: true` config option handles `spyOn` restoration). Verify `restoreMocks: true` is set in `frontend/vitest.config.ts`; if not, add explicit `afterEach(() => vi.restoreAllMocks())` to the test file. |
| T-50-12-04 | T (Tampering)              | A spurious impl-edit in 50-12 (e.g., editing KpiStrip.tsx when TEST WRONG was correct) could ship behaviour change as a "test fix".                | mitigate    | PCH-50R-06: Task 0 records D-09 disposition per file. Tasks 1/2 verify blocks conditionally grep the impl file ONLY when the disposition is IMPL DRIFTED. Plan 50-04 audit cross-checks each impl diff against the recorded disposition.                                                                                                                           |
| T-50-12-05 | I (Information disclosure) | Plan 50-12 racing Plan 50-10 on setup.ts would produce a merge conflict masking either cohort's keys.                                              | mitigate    | PCH-50R-01: Plan 50-12 depends_on includes 50-10; intra-Wave-3 ordering ensures 50-10 lands first. Task 0 verifies the 50-12 cohort keys are present in setup.ts before proceeding.                                                                                                                                                                                |

</threat_model>

<verification>
**Phase-level checks (post-plan):**

- All 7 in-scope test files exit `pnpm --filter intake-frontend exec vitest --run <file>` green.
- No `.skip` directives added to fouc-bootstrap.test.ts.
- bootstrap.js ↔ directions.ts byte-match preserved (either bootstrap.js updated to match directions.ts when Case I / III chosen, OR the test's regex updated to a shape that still enforces palette extraction when Case II chosen).
- `pnpm --filter intake-frontend lint` exits 0 on modified files.
- FE runner failing-files count drops by ≥7 (the 7 files in scope close).
- `frontend/tests/setup.ts` UNCHANGED by this plan's commits (verified by `git log --name-only` filter on Plan 50-12's commit range — should not include `frontend/tests/setup.ts`).
- For every IMPL DRIFTED disposition recorded in DISCOVERY.md, the corresponding product-source file appears in `git log --name-only` for this plan's commits AND passes the impl-grep gate.
  </verification>

<success_criteria>

- **TEST-02 progress:** 7 design-system + route + dashboard test files closed.
- **CLAUDE.md byte-match contract preserved:** fouc-bootstrap drift-guard remains active.
- **D-10 archaeology applied per file:** commit body documents verdict.
- **PCH-50R-01 honored:** setup.ts not edited by this plan.
- **PCH-50R-06 honored:** impl-edit grep gates fire conditionally per recorded D-09 disposition.

</success_criteria>

<output>
After completion, create `.planning/phases/50-test-infrastructure-repair/50-12-SUMMARY.md` with:
- Per-file disposition table: `file | verdict | impl edits (if any) | commit hash | git-archaeology`.
- fouc-bootstrap Case I/II/III chosen + rationale.
- Cross-reference: which 50-10 translations-map keys this plan's tests consumed.
- Pre/post failing-file counts.
- Retain `.planning/phases/50-test-infrastructure-repair/50-12-DISCOVERY.md` as a permanent audit artifact for Plan 50-04 (per PCH-50R-08 — DO NOT delete).
</output>
</content>
