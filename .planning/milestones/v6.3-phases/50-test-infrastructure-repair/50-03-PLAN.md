---
phase: 50-test-infrastructure-repair
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/setup.ts
  - backend/vitest.config.ts
  - backend/vitest.integration.config.ts
  - backend/package.json
autonomous: true
requirements:
  - TEST-01
  - TEST-02
user_setup: []

must_haves:
  truths:
    - '`pnpm --filter intake-backend test --run` exits 0 — the default backend test runner makes ZERO outbound network calls (no Supabase, no Redis, no LLM, no local Express).'
    - '`backend/tests/setup.ts` declares global `vi.mock(...)` factories for `@/config/supabase`, `ioredis`, `bullmq`, `@anthropic-ai/sdk`, `openai`, and `@xenova/transformers` (D-07 + D-08).'
    - '`backend/vitest.config.ts` `include` narrows to the unit / services / security / root-level test directories; the four real-service directories (`tests/contract/**`, `tests/contracts/**`, `tests/integration/**`, `tests/performance/**`) are excluded from the default runner.'
    - '`backend/vitest.integration.config.ts` exists, extends the base via `mergeConfig`, includes ONLY the four integration directories, and uses `testTimeout: 60000` to accommodate real-service latency.'
    - '`backend/package.json` has a new `test:integration` script invoking `vitest --config ./vitest.integration.config.ts --run`. The existing `test` script (unit-only) and `test:coverage` remain unchanged.'
    - 'Per-test Supabase refinement uses `vi.mocked(supabaseAdmin.from).mockReturnValue(...)` / `vi.mocked(supabaseAdmin.rpc).mockResolvedValue(...)` — the chainable mock surface supports `.select/.eq/.in/.gt/.lt/.gte/.lte/.order/.limit/.range/.single/.maybeSingle/.then` and the terminal mutators `.insert/.update/.upsert/.delete`.'
    - 'Per D-01, every residual BE default-runner failing test is either fixed-green, split-to-integration, or deleted-dead; ZERO files exit this plan in `queued-with-rationale` state for the BE default runner.'
  artifacts:
    - path: 'backend/tests/setup.ts'
      provides: 'Global mocks for every external dep the unit suite touches (D-07/D-08)'
      contains: "vi.mock('@/config/supabase'"
    - path: 'backend/vitest.config.ts'
      provides: 'Default test runner config narrowed to unit / services / security / root-level test paths'
      contains: 'tests/unit/'
    - path: 'backend/vitest.integration.config.ts'
      provides: 'Opt-in integration runner that includes the four real-service directories with 60s timeout (D-03)'
      contains: 'mergeConfig'
    - path: 'backend/package.json'
      provides: '`test:integration` script (D-03)'
      contains: 'test:integration'
  key_links:
    - from: 'backend/tests/setup.ts'
      to: 'every test file in `tests/unit`, `tests/services`, `tests/security`, root-level `tests/*.test.ts`'
      via: 'Vitest hoists `vi.mock(...)` from setupFiles before any test imports; the global mocks intercept `import` resolution'
      pattern: "vi.mock\\('@/config/supabase'"
    - from: 'backend/vitest.integration.config.ts'
      to: 'backend/vitest.config.ts'
      via: '`mergeConfig(baseConfig, defineConfig({ test: { include: [...integration paths...], testTimeout: 60000 } }))` reuses every base setting (aliases, pool, env, setupFiles) and only overrides `include`/`exclude`/`testTimeout`'
      pattern: 'mergeConfig'
---

<objective>
Drive the backend default test runner from 208 failures to 0 (D-02 phase-exit criterion) via the D-03 split + D-07/D-08 global mocks. Parallel to Plan 50-01 (FE) per RESEARCH §Wave parallelism (no file overlap between FE and BE workspaces).

Per RESEARCH §Failure Inventory and §Backend Class Distribution, the 208 BE failures break down as:

- **~202 integration-only** (class-I-Supabase 64 + class-I-Express 171 — overlap because some are double-counted with class-E): tests that hit `127.0.0.1:54321` (local Supabase) or `127.0.0.1:5001` (local Express dev server). **These move behind the new `pnpm test:integration` runner per D-03 — they do NOT run in the default suite.**
- **~104 env-missing** (`supabaseKey is required` / `supabaseUrl is required`): cleared by the D-07 global Supabase mock (no env needed once mocked).
- **~25 null-deref on supabase mock** (`Cannot read properties of undefined (reading 'from')`): also cleared by D-07.
- **~24 helper-missing** (`createTestServer is not a function`): per-file triage in Task 4.
- **~31 module-not-found** + **~17 api-shape drift**: per-file triage in Task 4.

After the split, the default `pnpm test` runs ONLY `tests/unit/**`, `tests/services/**`, `tests/security/**`, and the 4 root-level test files (`deadline-checker`, `digest-scheduler`, `email-notifications`, `notification-queue`, `push-notifications` — note: 5 root-level files per verified `find` output, RESEARCH listed 4; planner reconciles in Task 2). That's ~26 BE test files vs 219 today — a much smaller surface where the D-07/D-08 global mocks plus targeted helper fixes drive to green.

Purpose: BE green is the parallel half of TEST-01 / TEST-02. Unblocks Plan 50-04 (audit needs both workspaces green to record final exit codes) and Plan 50-05 (`Tests (backend)` CI gate cannot register on red).

Output:

- `backend/tests/setup.ts` rewritten with D-07/D-08 global mocks (~150 lines vs current 46)
- `backend/vitest.config.ts` modified: narrowed `include` + expanded `exclude` (4 lines diff)
- `backend/vitest.integration.config.ts` created (~25 lines, `mergeConfig` of base)
- `backend/package.json` extended with `test:integration` script (1 new line)
- Per-file fixes for the ~24 + 31 + 17 = ~72 surviving failures in the default suite (helper missing, module-not-found, api-shape drift) — exact count discovered in Task 4
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-test-infrastructure-repair/50-CONTEXT.md
@.planning/phases/50-test-infrastructure-repair/50-RESEARCH.md
@.planning/phases/50-test-infrastructure-repair/50-PATTERNS.md
@.planning/phases/50-test-infrastructure-repair/50-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. -->

From `backend/tests/setup.ts` (CURRENT — 46 lines, NO global mocks):

- Imports: `beforeAll, afterAll, afterEach` from 'vitest'; `config` from 'dotenv'; `path`
- Lines 5-6: `config({ path: path.resolve(__dirname, '../../.env') })` then `config({ path: path.resolve(__dirname, '../.env.test') })` — env loading order matters; preserve
- Line 8: `process.env.NODE_ENV = 'test'`
- Lines 18-33: `beforeAll(async () => { ... })` warns if `SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY / SUPABASE_ANON_KEY` are missing — after D-07 global mocks land, env-missing is no longer fatal but the warning can stay for integration runner observability
- Backend is pure ESM (`"type": "module"` per `backend/package.json:4`)

From `backend/vitest.config.ts` (CURRENT — 40 lines):

- `root: __dirname`
- `test.globals: true`, `environment: 'node'`, `pool: 'forks'` (perfect for global mocks per Pitfall 7), `setupFiles: [path.resolve(__dirname, './tests/setup.ts')]` (absolute path)
- `test.coverage`: provider v8, thresholds `lines: 80, functions: 80, branches: 75, statements: 80` (PRESERVE — D-13 gate is "test exits 0", coverage is separately invoked)
- `test.include: ['tests/**/*.test.ts']` — currently picks up EVERY directory
- `test.exclude: ['node_modules/', 'dist/']` — too narrow
- `test.testTimeout: 30000, hookTimeout: 30000`
- `resolve.alias: { '@': './src', '@tests': './tests' }`

From `backend/package.json:11-12` (CURRENT scripts):

- `"test": "vitest --config ./vitest.config.ts"` (watch mode by default — CI passes `--run`)
- `"test:coverage": "vitest --coverage --config ./vitest.config.ts"`
- No `--run` baked in; pattern matches frontend `package.json:12` (`"test": "vitest"`)

From `backend/tests/` directory structure (verified by `ls`):

- `tests/contract/` — 129 files (integration: real Supabase/Express)
- `tests/contracts/` — 11 files (integration; also contains a dead `vitest.config.ts` that should be flagged in audit but not consumed)
- `tests/integration/` — 53 files (integration)
- `tests/performance/` — 9 files (integration)
- `tests/security/` — 1 file (unit)
- `tests/services/` — 1 file (unit)
- `tests/unit/` — 10 files (unit)
- Root-level `tests/*.test.ts` — verified by `find -maxdepth 1`: 5 files = `deadline-checker.test.ts`, `digest-scheduler.test.ts`, `email-notifications.test.ts`, `notification-queue.test.ts`, `push-notifications.test.ts` (RESEARCH listed 4; the verified `find` returned 5)

From RESEARCH §Mock Factory Specs (BE):

| Module                 | Production importer                                                             | Mock shape                                                                                                     |
| ---------------------- | ------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| `@/config/supabase`    | `backend/src/config/supabase.ts:15` exports `supabaseAdmin`, `supabaseAnon`     | Chainable `from()` builder + `rpc` + `auth` + `storage`                                                        |
| `ioredis`              | `backend/src/config/redis.ts:1` `import Redis from 'ioredis'` (default)         | Return `{ default: Redis, Redis }` — Map-backed stub for `get/set/del/exists/expire/incr/on/quit/disconnect`   |
| `bullmq`               | `backend/src/queues/notification.queue.ts:1` named `Queue, Worker, QueueEvents` | Three named exports as `vi.fn().mockImplementation(...)` returning `{ add, on, close, getJobCounts }`          |
| `@anthropic-ai/sdk`    | `backend/src/ai/llm-router.ts` + 3 agents                                       | `default` class with `messages.create.mockResolvedValue({ content: [{ type: 'text', text: '...' }] })`         |
| `openai`               | `backend/src/ai/embeddings-service.ts` + `backend/src/ai/config.ts`             | `default` class with `embeddings.create.mockResolvedValue({ data: [{ embedding: new Array(1536).fill(0) }] })` |
| `@xenova/transformers` | `backend/src/ai/embeddings-service.ts` (pipeline)                               | `pipeline()` → `async (text) => ({ data: new Float32Array(1536) })`                                            |

Full canonical recipe verbatim in `.planning/phases/50-test-infrastructure-repair/50-RESEARCH.md` §Mock Factory Specs (lines 274-414). Copy as-is.

From RESEARCH §Path resolution gotchas:

- `backend/vitest.config.ts:36` declares `resolve.alias.@: ./src` — so `vi.mock('@/config/supabase', ...)` resolves cleanly. No `import.meta.resolve` needed.
- `pool: 'forks'` already in base config — each test file fresh process; in-memory Map state isolates between files automatically (no cross-file leak).
- `ioredis` is CJS exposed as `default` + named `Redis` — mock MUST return both shapes.
- `bullmq` is ESM with named exports only — mock returns named bindings, no `default`.

From RESEARCH §Integration Runner Choice Option B (RECOMMENDED):

```typescript
// backend/vitest.integration.config.ts (new file)
import { defineConfig, mergeConfig } from 'vitest/config'
import baseConfig from './vitest.config'
export default mergeConfig(
  baseConfig,
  defineConfig({
    test: {
      include: [
        'tests/contract/**/*.test.ts',
        'tests/contracts/**/*.test.ts',
        'tests/integration/**/*.test.ts',
        'tests/performance/**/*.test.ts',
      ],
      testTimeout: 60000,
      hookTimeout: 60000,
    },
  }),
)
```

From RESEARCH §Pitfall 4 / §`backend/vitest.integration.config.ts` Gotchas: `mergeConfig` may inherit `setupFiles` (the default suite's setup.ts with global mocks) — for the integration runner this is COUNTER-INTUITIVE because integration tests want REAL services. Planner option in Task 3: either (a) leave inherited setupFiles + add `vi.unmock(...)` calls inside individual integration tests that need real services, OR (b) create a second `tests/setup.integration.ts` that explicitly does NOT re-import the global mocks and wire it via `setupFiles: ['./tests/setup.integration.ts']` in the integration config. **Choose (a)** as the cheapest viable per Karpathy: don't add a second file until provably needed. Document this choice in Plan 50-04 docs (`backend/docs/test-setup.md`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite backend/tests/setup.ts with global mocks (D-07 + D-08)</name>
  <files>backend/tests/setup.ts</files>
  <read_first>
    - backend/tests/setup.ts (full file — current 46 lines, no global mocks)
    - .planning/phases/50-test-infrastructure-repair/50-RESEARCH.md §Mock Factory Specs (BE) — lines 274-414 (full canonical recipe)
    - .planning/phases/50-test-infrastructure-repair/50-PATTERNS.md §`backend/tests/setup.ts` (D-07/D-08 paths + ESM/CJS gotchas)
    - .planning/phases/50-test-infrastructure-repair/50-CONTEXT.md D-07, D-08
    - backend/src/config/supabase.ts (verify the exports `supabaseAdmin` + `supabaseAnon` and the export shape)
    - backend/src/config/redis.ts (verify `import Redis from 'ioredis'` default-import pattern)
    - backend/src/queues/notification.queue.ts (verify `Queue, Worker` named imports from bullmq)
    - backend/src/ai/llm-router.ts (verify the Anthropic import pattern + how `messages.create` is consumed downstream)
    - backend/src/ai/embeddings-service.ts (verify the OpenAI + `@xenova/transformers` import patterns)
    - frontend/tests/setup.ts (POST-Plan-50-01 shape — the FE precedent for `vi.importActual + spread`. Plan 50-01 lands first per wave structure, but this task does NOT depend on 50-01 because the BE work is parallel; treat the FE setup.ts as a reference shape only.)
  </read_first>
  <action>
    Rewrite `backend/tests/setup.ts` to extend its current 46-line shape with global `vi.mock(...)` factories for every external dependency the unit suite touches. The canonical recipe is RESEARCH §Mock Factory Specs (BE) lines 274-414 — copy that recipe verbatim as the starting point, adjusting only what local verification (Task 1's `read_first` files) reveals.

    Required structure of the new `backend/tests/setup.ts`:

    1. Preserve verbatim lines 1-16 of current file: imports (`beforeAll, afterAll, afterEach, vi` from 'vitest'; `config` from 'dotenv'; `path`); the two `config({ path: ... })` calls in order (`.env` first, then `.env.test`); `process.env.NODE_ENV = 'test'`. Note: `vi` is NOT in the current imports — add it to the vitest import list since the new mocks use it.

    2. Add `vi.mock('@/config/supabase', () => ({ ... }))` returning `{ supabaseAdmin: {...}, supabaseAnon: {...} }` where the `supabaseAdmin.from(...)` returns a chainable object exposing every method the production code calls. Per RESEARCH lines 286-326, the chain MUST support: `.select`, `.insert`, `.update`, `.upsert`, `.delete`, `.eq`, `.neq`, `.in`, `.gt`, `.lt`, `.gte`, `.lte`, `.order`, `.limit`, `.range`, `.single`, `.maybeSingle`, and a `.then` so the chain is await-able directly. Terminal default is `{ data: null, error: null }`. The chain helper function `makeChain` returns a NEW chain on every call so per-test refinement can target individual tables (see Pitfall 2 — `vi.mocked(supabaseAdmin.from).mockImplementation((table) => customChain)` is the supported override pattern).

    3. Add `vi.mock('ioredis', () => ({ default: Redis, Redis }))` where `Redis` is a `vi.fn().mockImplementation(...)` returning an instance with a Map-backed `store` and `expiry` and methods `get / set / del / exists / expire / incr / on / quit / disconnect`. Async-return on the terminal methods (`get/set/del/exists/expire/incr/quit`) per Pitfall 6 (`vi.fn(async () => ...)`).

    4. Add `vi.mock('bullmq', () => ({ Queue: vi.fn().mockImplementation(...), Worker: vi.fn().mockImplementation(...), QueueEvents: vi.fn().mockImplementation(...) }))`. Each implementation returns `{ add: vi.fn(async () => ({ id: 'mock-job-id' })), on: vi.fn(), close: vi.fn(async () => undefined), getJobCounts: vi.fn(async () => ({ waiting: 0, active: 0, completed: 0, failed: 0 })) }`. No `default` export — bullmq is named-export-only.

    5. Add `vi.mock('@anthropic-ai/sdk', () => ({ default: vi.fn().mockImplementation(() => ({ messages: { create: vi.fn(async () => ({ id: 'msg_mock', type: 'message', role: 'assistant', content: [{ type: 'text', text: '{"mock": true}' }], stop_reason: 'end_turn', usage: { input_tokens: 0, output_tokens: 0 } })) } })) }))`. Shape matches the Anthropic Messages API response per RESEARCH §Mock Factory Specs (BE).

    6. Add `vi.mock('openai', () => ({ default: vi.fn().mockImplementation(() => ({ embeddings: { create: vi.fn(async () => ({ data: [{ embedding: new Array(1536).fill(0) }] })) }, chat: { completions: { create: vi.fn(async () => ({ choices: [{ message: { content: '{"mock":true}' } }] })) } } })) }))`. Per Assumption A4 in RESEARCH: if the import is unused, the mock is harmless.

    7. Add `vi.mock('@xenova/transformers', () => ({ pipeline: vi.fn(async () => async (_text: string) => ({ data: new Float32Array(1536) })) }))`. The pipeline factory returns an async function that returns a Float32Array-wrapped result, mirroring the production embedding API.

    8. Preserve the existing `beforeAll` warning (lines 18-33 of current file) but soften the message to: "Missing env vars; default test runner uses global mocks. If running test:integration, populate `.env.test` for real-service tests." Reword to drop the "Some tests may fail" alarm since the global mocks now cover the default suite.

    9. Add `afterEach(async () => { vi.clearAllMocks() })` to reset per-test override state between tests. Do NOT use `vi.resetAllMocks()` per Pitfall 3 (resetAllMocks wipes implementation, clearAllMocks wipes only call history).

    Style: backend uses `"semi": false`, `"singleQuote": true` per CLAUDE.md root Prettier config. Match this style (current file already uses `'` for strings).

    The mock import paths MUST resolve correctly:
    - `'@/config/supabase'` resolves via `backend/vitest.config.ts:36` alias `@: ./src`.
    - `'ioredis'`, `'bullmq'`, `'@anthropic-ai/sdk'`, `'openai'`, `'@xenova/transformers'` resolve via `node_modules`.
    Verify by running `pnpm --filter intake-backend test --run tests/unit 2>&amp;1 | head -50` after the rewrite — no `Cannot find module` errors should appear.

  </action>
  <verify>
    <automated>cd backend &amp;&amp; grep -c "vi.mock('@/config/supabase'" tests/setup.ts | grep -q "^1$" &amp;&amp; grep -c "vi.mock('ioredis'" tests/setup.ts | grep -q "^1$" &amp;&amp; grep -c "vi.mock('bullmq'" tests/setup.ts | grep -q "^1$" &amp;&amp; grep -c "vi.mock('@anthropic-ai/sdk'" tests/setup.ts | grep -q "^1$" &amp;&amp; grep -c "vi.mock('openai'" tests/setup.ts | grep -q "^1$" &amp;&amp; grep -c "vi.mock('@xenova/transformers'" tests/setup.ts | grep -q "^1$" &amp;&amp; grep -c "makeChain\\|maybeSingle" tests/setup.ts | awk '$1 &gt;= 1 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>
    `backend/tests/setup.ts` has 6 top-level `vi.mock(...)` calls covering supabase / ioredis / bullmq / anthropic / openai / xenova. The Supabase chain mock supports `.maybeSingle` and the full filter set. The `dotenv` env-loading at the top is preserved. The `afterEach(vi.clearAllMocks())` is present. No `Cannot find module` errors in `pnpm --filter intake-backend test --run tests/unit 2>&amp;1 | head -50`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Narrow backend/vitest.config.ts include + expand exclude (D-03)</name>
  <files>backend/vitest.config.ts</files>
  <read_first>
    - backend/vitest.config.ts (full file — current 40 lines with `include: ['tests/**/*.test.ts']`)
    - .planning/phases/50-test-infrastructure-repair/50-RESEARCH.md §Integration Runner Choice (Option B `include` narrowing diff, lines 488-498)
    - .planning/phases/50-test-infrastructure-repair/50-PATTERNS.md §`backend/vitest.integration.config.ts` Companion edit section
    - The actual list of root-level `*.test.ts` files in `backend/tests/` (verified by `find backend/tests -maxdepth 1 -name "*.test.ts"`): 5 files = `deadline-checker.test.ts`, `digest-scheduler.test.ts`, `email-notifications.test.ts`, `notification-queue.test.ts`, `push-notifications.test.ts` (RESEARCH listed 4 — reconcile to the verified 5).
  </read_first>
  <action>
    Two-edit diff on `backend/vitest.config.ts`:

    1. Replace `include: ['tests/**/*.test.ts']` (line 29) with a four-entry array literal containing, in order: `'tests/unit/**/*.test.ts'`, `'tests/services/**/*.test.ts'`, `'tests/security/**/*.test.ts'`, and `'tests/{deadline-checker,digest-scheduler,email-notifications,notification-queue,push-notifications}.test.ts'`. The fourth entry uses vitest micromatch brace-expansion to pick up all 5 verified root-level test files (RESEARCH listed 4 missing `notification-queue`; correct count is 5 per `find` output).

    2. Replace `exclude: ['node_modules/', 'dist/']` (line 30) with a six-entry array literal containing, in order: `'node_modules/'`, `'dist/'`, `'tests/contract/**'`, `'tests/contracts/**'`, `'tests/integration/**'`, `'tests/performance/**'`. These four directory excludes are belt-and-suspenders matching the integration runner's `include`; without the explicit excludes, edge-case path collisions could re-include integration tests via Vite's path resolution.

    Preserve verbatim everything else: `root: __dirname`, the entire `test` block's other properties (`globals`, `environment: 'node'`, `pool: 'forks'`, `setupFiles` absolute path, the entire `coverage` block with thresholds, `testTimeout: 30000`, `hookTimeout: 30000`), and the `resolve.alias` block.

    Do NOT change coverage thresholds — RESEARCH §Coverage Threshold Impact explicitly says backend thresholds (80/80/75/80) are preserved; coverage runs against unit + integration combined later (a separate concern post-Phase-50).

    The `tests/contracts/vitest.config.ts` dead file (40 lines, not consumed by any current script) should NOT be touched here; flag it in Plan 50-04's audit as `disposition: deleted-dead` or `queued-out-of-scope` — Plan 50-03 does not own audit decisions.

  </action>
  <verify>
    <automated>cd backend &amp;&amp; grep -c "tests/unit/\\*\\*/\\*\\.test\\.ts" vitest.config.ts | grep -q "^1$" &amp;&amp; grep -c "tests/contract/\\*\\*" vitest.config.ts | grep -q "^1$" &amp;&amp; grep -c "tests/integration/\\*\\*" vitest.config.ts | grep -q "^1$" &amp;&amp; pnpm exec vitest --run --reporter=verbose 2>&amp;1 | grep -E "Test Files" | head -1 | awk '{ for(i=1;i&lt;=NF;i++) { if ($i+0 &gt; 0 &amp;&amp; $i+0 &lt; 60) { total=$i; break } } } END { if (total &gt; 0 &amp;&amp; total &lt; 60) { print "PASS: " total " files in default suite (≤60 expected)"; exit 0 } print "FAIL: file count out of range"; exit 1 }'</automated>
  </verify>
  <done>
    `backend/vitest.config.ts` `include` covers exactly 4 path globs (unit, services, security, root-level brace-expanded list of 5 files). `exclude` lists `tests/contract/**`, `tests/contracts/**`, `tests/integration/**`, `tests/performance/**`. Running `pnpm --filter intake-backend test --run` collects ~26 BE test files (down from 219). Brace expansion is supported by vitest's micromatch glob engine — verified by file count in the test output.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create backend/vitest.integration.config.ts + extend backend/package.json (D-03)</name>
  <files>backend/vitest.integration.config.ts, backend/package.json</files>
  <read_first>
    - backend/vitest.config.ts (post-Task-2 state — the BASE config that `mergeConfig` extends)
    - backend/package.json (full file — current scripts at lines 6-21)
    - .planning/phases/50-test-infrastructure-repair/50-RESEARCH.md §Integration Runner Choice Option B (lines 463-509 — full target shape)
    - .planning/phases/50-test-infrastructure-repair/50-PATTERNS.md §`backend/vitest.integration.config.ts`
  </read_first>
  <action>
    Two creates / extends:

    **A) Create `backend/vitest.integration.config.ts` (new file, ~25 lines):**

    Body must:
    - Import `defineConfig` and `mergeConfig` from `'vitest/config'`.
    - Import the base config as `baseConfig` from `'./vitest.config'` (default import).
    - Export default `mergeConfig(baseConfig, defineConfig({ test: { include: [...integration paths...], exclude: ['node_modules/', 'dist/'], testTimeout: 60000, hookTimeout: 60000 } }))`.
    - `include` is exactly the four directory globs: `'tests/contract/**/*.test.ts'`, `'tests/contracts/**/*.test.ts'`, `'tests/integration/**/*.test.ts'`, `'tests/performance/**/*.test.ts'`.
    - `testTimeout: 60000` is 2× base — RESEARCH §Mock Factory Specs notes real-service latency needs the longer ceiling.
    - Do NOT add a separate `tests/setup.integration.ts` — per the §`backend/vitest.integration.config.ts` Gotchas in PATTERNS, choose option (a): inherit `setupFiles` from base, let individual integration tests `vi.unmock(...)` whatever they need. Defer the second-setup-file split until evidence proves it's needed.
    - `mergeConfig` deep-merges; the integration `include` REPLACES base's `include` (the narrowed unit-suite list), giving the integration runner the four integration directories instead.

    Style: backend Prettier root config = `semi: false` + `singleQuote: true`. Match.

    **B) Extend `backend/package.json` scripts block:**

    Insert a new JSON key BETWEEN the existing `"test": "vitest --config ./vitest.config.ts"` line and the `"test:coverage"` line. The new key is named `"test:integration"` and its value is the command string `"vitest --config ./vitest.integration.config.ts --run"`. Include a trailing comma (the line is not last in the scripts block). The explicit `--run` flag — integration is one-shot per VALIDATION row "No watch-mode flags in CI commands". The base `test` script intentionally does NOT have `--run` (consumers add it via `pnpm test --run` for one-shot mode); only `test:integration` bakes it in because the integration runner has no dev-loop value.

    Do NOT touch `dev`, `build`, `start`, `test`, `test:coverage`, `lint`, `format`, `type-check`, `type-check:summary`, `supabase:start`, `supabase:stop`, `migrate`, or `seed` scripts. Do NOT touch root `package.json` — Turbo's `turbo run test` picks up scripts named in `turbo.json` pipeline; check `turbo.json` separately. If `turbo.json` has a `test:integration` pipeline entry, the new script will be available via `pnpm test:integration` at repo root automatically; if not, that's intentional (advisory runner is workspace-scoped).

  </action>
  <verify>
    <automated>cd backend &amp;&amp; test -f vitest.integration.config.ts &amp;&amp; grep -c "mergeConfig" vitest.integration.config.ts | grep -q "^1$" &amp;&amp; grep -c "testTimeout: 60000" vitest.integration.config.ts | grep -q "^1$" &amp;&amp; grep -c "tests/contract/\\*\\*/\\*\\.test\\.ts" vitest.integration.config.ts | grep -q "^1$" &amp;&amp; grep -c "test:integration" package.json | grep -q "^1$" &amp;&amp; pnpm exec vitest --config ./vitest.integration.config.ts --run --reporter=verbose 2>&amp;1 | head -3 | grep -qE "RUN.*v[0-9]"</automated>
  </verify>
  <done>
    `backend/vitest.integration.config.ts` exists, exports `mergeConfig(baseConfig, ...)` with the four integration glob paths and `testTimeout: 60000`. `backend/package.json` has the new `test:integration` script with `--run` flag. Running `pnpm --filter intake-backend test:integration` (which WILL fail tests because no real services are running) invokes the integration runner cleanly — no config errors. `mergeConfig` correctly inherits `pool: 'forks'`, `setupFiles`, and aliases from base.
  </done>
</task>

<task type="auto">
  <name>Task 4: Triage and fix the residual ~26 default-suite failures (helper-missing, module-not-found, api-shape drift)</name>
  <files>(per-file paths discovered at execution time; primary scope = backend/tests/unit/**, backend/tests/services/**, backend/tests/security/**, and 5 root-level test files)</files>
  <read_first>
    - The exact post-Task-1+2+3 failing-file list from `pnpm --filter intake-backend test --run 2>&amp;1 | tail -50` (run at task start)
    - .planning/phases/50-test-infrastructure-repair/50-RESEARCH.md §Backend Class Distribution (lines 138-148: class-H helper missing, class-M module-not-found, class-A api-shape drift)
    - .planning/phases/50-test-infrastructure-repair/50-CONTEXT.md D-09, D-10 (root-cause fixes allowed; test name = contract spec)
    - backend/tests/unit/ files (read each that fails to understand the failure)
    - backend/tests/services/ files (one file per RESEARCH §Backend test-folder structure)
    - backend/tests/security/ files (one file)
    - The 5 root-level `tests/*.test.ts` files (`deadline-checker`, `digest-scheduler`, `email-notifications`, `notification-queue`, `push-notifications`)
    - For each failing test, also read the impl it tests (e.g., `backend/src/queues/notification.queue.ts` for `notification-queue.test.ts`)
  </read_first>
  <action>
    Apply D-10 per-file archaeology to drive the default backend suite to green. The procedure mirrors Plan 50-02 Task 2 but targets the backend. Per D-01 (BE exit-0 mirrors FE exit-0 for the phase), every residual failing BE default-runner test MUST be either (a) fixed green, or (b) moved to the integration runner via the Plan 50-03 split (`backend/vitest.integration.config.ts` include). **`queued-with-rationale` is NOT acceptable for the BE default runner at phase exit.**

    1. Run `pnpm --filter intake-backend test --run 2>&amp;1 | tee /tmp/be-failures.log` to capture the current failure set after Tasks 1-3 land.

    2. For each failing test file:
       a. Read the failure signature from the log (look for `Error:`, `AssertionError:`, `Cannot find`, etc.).
       b. `git log --oneline -- &lt;test-file&gt;` to see test history.
       c. `git log --oneline -- &lt;impl-file&gt;` to see impl history.
       d. Classify by RESEARCH §Backend Class Distribution AND assign disposition (fixed-green OR moved-to-integration OR deleted-dead — never queued):
          - **class-H (helper missing — `createTestServer is not a function`):** Either restore the test helper at `backend/tests/helpers/test-server.ts` (export a function that returns a mocked Express app) → fixed-green; OR delete tests that depend on it AND record `disposition: deleted-dead` rationale for Plan 50-04 audit. Check `backend/tests/helpers/` directory contents first; the helper may exist but with a different name.
          - **class-M (module-not-found):** If the imported module was deleted in a prior phase (Phase 47 deletion-first), the test is dead-import → delete-dead. If the module was renamed, update the import → fixed-green.
          - **class-A (api-shape drift — e.g., `apiClient.put is not a function`):** Verify the impl exports what the test expects. If impl drifted, either update the test to match the new API (D-10 contract reversal — record rationale in commit message and audit) → fixed-green, OR re-export the missing method → fixed-green.
          - **Genuinely integration-only (test cannot pass without a real Redis / Supabase / external service even with D-07/D-08 mocks):** Move the file to `backend/tests/integration/` (or one of the four integration directories already in `backend/vitest.integration.config.ts` include), then re-verify it is picked up by the integration runner and excluded from the default runner → split-to-integration.
       e. Apply fix per classification. Each fix gets its own commit `fix(50-03): &lt;file&gt; — &lt;diagnosis verdict&gt;` or `split(50-03): &lt;file&gt; → integration` for disposition-(b) moves.

    3. The 5 root-level test files (`deadline-checker`, `digest-scheduler`, `email-notifications`, `notification-queue`, `push-notifications`) may be unit OR integration depending on whether they hit real services. Read each test to determine; if a test hits a real Redis/Supabase/external API even after D-07/D-08 mocks intercept, it MUST move to one of the integration directories (file move + integration-config glob already covers `tests/integration/**`). Do NOT use `it.skipIf(!process.env.REAL_INTEGRATION)` as an escape hatch — `skipIf` leaves the test in the default runner where it silently no-ops in CI, which is functionally the same as `queued-with-rationale` and violates the D-01 contract. Either the test passes in the default runner with mocks OR it physically lives under the integration runner.

    4. If a queue/scheduler test fails because the bullmq mock doesn't model some specific behavior (e.g., per-test job queueing semantics), extend the per-test override using `vi.mocked(Queue).mockImplementation(...)` inside that test file's `beforeEach` — do NOT extend the global mock in `setup.ts` (per Karpathy: minimal global, scope per-test). The test must end fixed-green in the default runner; if extending the per-test override is not viable, the file goes to integration (disposition-b), not to a queued state.

    5. After all triage: re-run `pnpm --filter intake-backend test --run` and confirm exit code 0.

    6. Record in `50-03-SUMMARY.md`:
       - Per-file classification (class-H / class-M / class-A) AND per-file disposition (fixed-green / split-to-integration / deleted-dead)
       - Zero `queued-with-rationale` rows for the BE default runner (per D-01)
       - Total commit count
       - Final exit code + timestamp for the audit handoff
       - List of any `split-to-integration` files for Plan 50-04 audit to absorb

  </action>
  <verify>
    <automated>cd backend &amp;&amp; pnpm exec vitest --run 2>&amp;1 | tail -8 | grep -E "Test Files.*passed" | grep -vE "[1-9]+[0-9]* failed" &amp;&amp; pnpm exec vitest --run 2>&amp;1 | tail -8 | grep -E "Tests.*passed" | grep -vE "[1-9]+[0-9]* failed"</automated>
  </verify>
  <done>
    `pnpm --filter intake-backend test --run` exits 0 (D-01 BE side non-negotiable). Default suite runs ~26 files (down from 219). Per-file commits exist for each fixed/split test. ZERO `queued-with-rationale` rows for the BE default runner — every residual file is either fixed-green, split-to-integration, or deleted-dead. Any `split-to-integration` files are listed in `50-03-SUMMARY.md` for Plan 50-04 audit to absorb. `pnpm --filter intake-backend test:integration` still runs (fails because no real services running locally, but config + runner work as designed). `pnpm --filter intake-backend lint` exits 0.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                            | Description                                                                                                                                                                  |
| --------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Test runner ↔ external services                     | Default `pnpm test` makes ZERO outbound calls post-this-plan — every external dep is mocked via D-07/D-08. Integration runner is opt-in, sandboxed behind a separate config. |
| Mocked Supabase client ↔ production `supabaseAdmin` | The mock returns `{ data: null, error: null }` by default; production code that interprets `null` as "row not found" continues to work.                                      |
| `ioredis` mock ↔ production Redis                   | The mock is in-memory, per-process; no network.                                                                                                                              |
| `bullmq` mock ↔ production queue                    | The mock returns a fake job id (`'mock-job-id'`) and never enqueues work.                                                                                                    |

## STRIDE Threat Register

| Threat ID  | Category                   | Component                                                                                                                                                                            | Disposition | Mitigation Plan                                                                                                                                                                                                                              |
| ---------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-50-03-01 | I (Information disclosure) | A test in the default suite leaks real credentials via an unmocked code path (e.g., a production module imports a secret-laden config directly rather than via `@/config/supabase`). | mitigate    | D-07/D-08 global mocks default to safe-empty responses; the audit (Plan 50-04) records every test's disposition so this surface is enumerable. Integration runner is opt-in; default suite has zero network calls.                           |
| T-50-03-02 | T (Tampering)              | An integration-only test could leak into the default suite via glob mis-classification (e.g., a contract test mistakenly placed in `tests/unit/`).                                   | mitigate    | Task 2's `exclude` list explicitly bans the four integration directories from the base config. Plan 50-05's `Tests (backend)` CI gate will fail if such a test runs and hits real services.                                                  |
| T-50-03-03 | S (Spoofing)               | A mocked Supabase response could be confused with a real response, masking auth-bypass bugs.                                                                                         | accept      | Per CONTEXT D-03, tests requiring real auth move to integration runner. Unit tests asserting on auth behavior must explicitly set up their mocks; the global default of `null` data forces the test author to be deliberate.                 |
| T-50-03-04 | E (Elevation of privilege) | `vi.mock('@/config/supabase')` could be bypassed by a test that imports `supabaseAdmin` via a different path (e.g., relative path `../../config/supabase`).                          | mitigate    | Backend `tsconfig.json` enforces `@/*` aliases as the canonical import; `eslint.config.mjs` could add `no-restricted-imports` for the relative path if regression occurs. Out of Phase 50 scope; record as `tech_debt` in audit if observed. |

</threat_model>

<verification>
**Phase-level checks (post-plan):**

- `pnpm --filter intake-backend test --run` exits 0 (full BE green per VALIDATION row "TEST-02 extension: Full BE green").
- `backend/tests/setup.ts` has 6 top-level `vi.mock(...)` calls (supabase/ioredis/bullmq/anthropic/openai/xenova).
- `backend/vitest.config.ts` `include` lists exactly the 4 unit-suite paths; `exclude` lists exactly the 4 integration directories.
- `backend/vitest.integration.config.ts` exists with `mergeConfig`-based extension.
- `backend/package.json` scripts block has exactly one `test:integration` line.
- `pnpm --filter intake-backend test:integration` invokes the integration runner without config errors (the integration tests themselves fail because no real services — that's expected; CI marks them advisory per D-13).
- `pnpm --filter intake-backend type-check` exits 0 (no TS regressions in setup.ts).
- `pnpm --filter intake-backend lint` exits 0.
- `pnpm --filter intake-backend test --run --coverage` runs; coverage thresholds may FAIL because the default suite is narrow now — that's not a Phase 50 gate (D-13 gate is "test exits 0", not coverage hits 80). Plan 50-04 documents the coverage-vs-suite-split trade-off in `backend/docs/test-setup.md`.
  </verification>

<success_criteria>

- **TEST-01 (BE dimension):** `backend/tests/setup.ts` declares global mocks for every external dep the unit suite touches → SATISFIED.
- **TEST-02 (BE extension):** `pnpm --filter intake-backend test --run` exits 0 → SATISFIED. Per D-01, BE exit-0 is non-negotiable; no `queued-with-rationale` carve-outs in the default runner.
- **D-03 (integration split):** New `backend/vitest.integration.config.ts` + `test:integration` script wire an opt-in runner for the 202 real-service tests; default suite makes zero outbound calls.
- **No regression:** Currently-passing 11 BE tests (per RESEARCH §Probed live counts) remain green.
- **Audit handoff:** Per-file dispositions ready for Plan 50-04's `50-TEST-AUDIT.md` (every BE test file at phase start has a row).
  </success_criteria>

<output>
After completion, create `.planning/phases/50-test-infrastructure-repair/50-03-SUMMARY.md` with:
- Pre-plan vs post-plan failing-file counts (208 → 0 expected)
- The chosen treatment for the 5 root-level test files (unit / moved-to-integration / each rationale)
- Per-file disposition table (or pointer to source-of-truth for the audit)
- Final BE test runner output (`Test Files: N passed (N)`, exit code 0)
- Confirmation that integration runner config exists and invokes cleanly (tests fail because no services, but config works)
- The dead `backend/tests/contracts/vitest.config.ts` file flagged for Plan 50-04 audit (do not delete here)
- Confirmation that root `vitest.config.ts` (legacy stray, not consumed by any script) is flagged for audit
</output>
</content>
</invoke>
