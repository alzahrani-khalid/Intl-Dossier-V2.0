---
phase: 50-test-infrastructure-repair
plan: 08
type: execute
wave: 3
depends_on:
  - 50-01
  - 50-06
  - 50-07
files_modified:
  - frontend/src/components/dossier/wizard/hooks/useCountryAutoFill.ts
  - frontend/src/components/dossier/wizard/hooks/__tests__/useCountryAutoFill.test.ts
  - frontend/src/pages/Dashboard/widgets/__tests__/KpiStrip.test.tsx
  - frontend/tests/unit/components/ui/heroui-wrappers.test.tsx
  - frontend/tests/unit/design-system/buildTokens.test.ts
  - frontend/tests/unit/analytics.cluster.test.tsx
  - frontend/tests/unit/hooks/responsive.test.ts
  - frontend/tests/accessibility/waiting-queue-a11y.test.tsx
  - frontend/tests/component/BulkActionToolbar.test.tsx
  - frontend/tests/component/EscalationDialog.test.tsx
  - frontend/tests/component/ConflictDialog.test.tsx
  - frontend/tests/unit/FormInput.test.tsx
autonomous: true
requirements:
  - TEST-02
user_setup: []

must_haves:
  truths:
    - 'AT TASK START, the executor re-runs `pnpm --filter intake-frontend test --run --reporter=default` and counts failing FE files. If >12, STOP and surface to operator — phase scope was misjudged again (mirroring the original Plan 50-02 ceiling discipline).'
    - 'Every residual failing FE file in the default runner is dispositioned per D-10 archaeology: (a) fixed-green, (b) split-to-integration, or (c) deleted-dead. `queued-with-rationale` is NOT acceptable per D-01 non-negotiable.'
    - '`useCountryAutoFill` test fixture uses REST Countries v3.1 contract (`cca2`, `cca3`, `region`, `capital`) and the impl has 3 new `match.cca2 != null` / `match.cca3 != null` / `match.region != null` runtime null-guards (D-09 product hardening + D-10 test contract preservation). This is the must-fix hot-list item from the operator gap brief.'
    - 'After this plan lands, `pnpm --filter intake-frontend test --run` exits 0 — the cumulative phase-exit-0 contract across 50-06 + 50-07 + 50-08 is satisfied. D-01 non-negotiable.'
    - 'Each per-file fix has its own commit (`fix(50-08): <file> — <verdict>` or `split(50-08): <file> → integration` or `delete(50-08): <file> — <rationale>`) so reviewers can audit per-file granularity per D-10.'
  artifacts:
    - path: 'frontend/src/components/dossier/wizard/hooks/useCountryAutoFill.ts'
      provides: 'Hook with `match.cca2 != null` / `match.cca3 != null` / `match.region != null` runtime null-guards (D-09 product hardening; mirror existing line-67 `match.capital != null` idiom)'
      contains: 'match.cca2 != null'
    - path: 'frontend/src/components/dossier/wizard/hooks/__tests__/useCountryAutoFill.test.ts'
      provides: "Test using REST Countries v3.1 fixture shape with `cca2: 'SA'`, `cca3: 'SAU'`, `region: 'Asia'`, `capital: ['Riyadh']` + assertion `region: 'asia'` post-REGION_MAP"
      contains: "cca2: 'SA'"
  key_links:
    - from: 'frontend/src/components/dossier/wizard/hooks/__tests__/useCountryAutoFill.test.ts'
      to: 'frontend/src/components/dossier/wizard/hooks/useCountryAutoFill.ts'
      via: 'Test mocks `useQuery` with REST Countries v3.1 shape the impl reads — fields `cca2`/`cca3`/`region`/`capital` (NOT `code`/`code3`)'
      pattern: "cca2.*'SA'"
    - from: 'Final FE test runner output'
      to: 'D-01 phase-exit contract'
      via: '`pnpm --filter intake-frontend test --run` exits 0 → FE green proof across 50-06 + 50-07 + 50-08'
      pattern: 'Test Files.*passed.*0 failed'
---

<objective>
Finish the cumulative phase-exit-0 contract for the frontend default runner across the new Wave 2/3 plans. After Plan 50-06 closes the cross-cutting setup gaps (CC-1/CC-2/CC-3/CC-4) and Plan 50-07 resolves the single-class outliers (CC-5/CC-6/PF-2), the residual is per-file product/test drift requiring D-10 archaeology per file.

Per the operator gap brief at `.continue-here.md`, the hot-list for this plan is 12 files (must-fix candidates). The ≤12-file ceiling is the planner's discipline budget — if discovery at task start reveals >12 still-failing files after 50-06+50-07 land, the executor MUST STOP and escalate (do NOT silently expand scope; this mirrors the original Plan 50-02 ceiling discipline that the operator gap brief explicitly preserves).

The hot-list (per `.continue-here.md` §Bucketing Strategy → 50-08):

1. `useCountryAutoFill.test.ts` + `useCountryAutoFill.ts` (test fix + 3 impl null-guards) — the canonical "TEST IS WRONG, IMPL ALSO NEEDS HARDENING" case from the archived Plan 50-02 (preserve that work verbatim).
2. `KpiStrip.test.tsx` — dashboard widget assertion drift (Phase 38 baseline).
3. `heroui-wrappers.test.tsx` — HeroUI v3 wrapper drift (Phase 33-05 baseline).
4. `buildTokens.test.ts` — token builder unit drift.
5. `analytics.cluster.test.tsx` — `vi.spyOn() can only spy on a function. Received undefined.` (module under spy no longer exports the symbol).
6. `responsive.test.ts` — same spyOn-undefined class.
7. `waiting-queue-a11y.test.tsx` — a11y assertion drift.
8. `BulkActionToolbar.test.tsx` — likely auto-closes via Plan 50-06 jest-dom matchers (CC-1); verify before triage.
9. `EscalationDialog.test.tsx` — same CC-1 suspicion; verify after 50-06.
10. `ConflictDialog.test.tsx` — `Cannot convert undefined or null to object`; missing-prop diagnosis.
11. `FormInput.test.tsx` — per-file drift.

Plus discovery rule: re-run the FE runner at task start; any file not in the hot list that is still failing after 50-06+50-07 land joins the per-file triage. The aggregate file count (hot list + newly-discovered) MUST NOT exceed 12.

This plan depends on Plan 50-06 AND Plan 50-07 because the residual set is only knowable AFTER 50-06 auto-clears the cross-cutting failures (CC-1 in particular should close BulkActionToolbar, EscalationDialog, and possibly more — the operator brief flags this expectation; the executor verifies).

**D-01 is non-negotiable here.** This is the plan that achieves `pnpm --filter intake-frontend test --run` exit code 0. If a file cannot be fixed-green or split-to-integration within the plan budget, STOP and escalate — the alternative is to silently widen scope and re-create the original Plan 50-02 ceiling-overshoot situation.

Purpose: Close the phase-exit-0 contract per D-01. After this plan lands, the FE default runner is green, which is the precondition for Plan 50-04 (audit + docs) and Plan 50-05 (CI gate + ESLint rule) to run safely.

Output:

- `frontend/src/components/dossier/wizard/hooks/useCountryAutoFill.ts` with 3 added `!= null` guards.
- `frontend/src/components/dossier/wizard/hooks/__tests__/useCountryAutoFill.test.ts` with REST Countries v3.1 fixture + corrected REGION_MAP assertion.
- Each other per-file residual either fixed-green, split-to-integration, or deleted-dead with per-file commit and rationale.
- Cumulative result: `pnpm --filter intake-frontend test --run` exits 0.
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-test-infrastructure-repair/50-CONTEXT.md
@.planning/phases/50-test-infrastructure-repair/50-RESEARCH.md
@.planning/phases/50-test-infrastructure-repair/50-PATTERNS.md
@.planning/phases/50-test-infrastructure-repair/50-VALIDATION.md
@.planning/phases/50-test-infrastructure-repair/.continue-here.md
@.planning/phases/50-test-infrastructure-repair/50-01-SUMMARY.md
@.planning/phases/50-test-infrastructure-repair/50-06-SUMMARY.md
@.planning/phases/50-test-infrastructure-repair/50-07-SUMMARY.md
@.planning/phases/50-test-infrastructure-repair/50-02-PLAN.archived.md
@CLAUDE.md

<interfaces>
<!-- Per-file diagnosis prep. Most detail for the must-fix useCountryAutoFill pair (the canonical case preserved from archived 50-02). -->

From `frontend/src/components/dossier/wizard/hooks/useCountryAutoFill.ts` (CURRENT impl — 72 lines; archived Plan 50-02 spec preserved verbatim):

- `RestCountryResult` interface (lines 6-11): `cca2: string`, `cca3: string`, `region: string`, `capital?: string[]` — non-optional except `capital`.
- `REGION_MAP` (lines 14-21): `{ Asia: 'asia', Africa: 'africa', Europe: 'europe', Americas: 'americas', Oceania: 'oceania', Antarctic: 'antarctic' }`.
- `fetchCountryReference` (lines 23-33): fetches `https://restcountries.com/v3.1/name/${name}?fields=cca2,cca3,region,capital`, returns first array element.
- `useCountryAutoFill` hook (lines 39-71): uses `useQuery`; in the `useEffect` reads `match.cca2`, `match.cca3`, `match.region` (mapped via REGION_MAP), `match.capital[0]`.
- Lines 55-66: 4 fill-when-empty blocks. Lines 55, 58, 61 use only `match.cca2 !== ''` / `match.cca3 !== ''` / `match.region !== ''` (empty-string guard — `undefined` passes through). Line 67 ALREADY uses `match.capital != null` — copy this idiom.

From `frontend/src/components/dossier/wizard/hooks/__tests__/useCountryAutoFill.test.ts` (CURRENT test — 110 lines):

- Lines 60-66 (first `it` block fixture — WRONG): `const mockMatch = { code: 'SA', code3: 'SAU', name_en: ..., name_ar: ..., region: 'Asia' }`.
- Lines 83-89 (second `it` block fixture — WRONG, same shape).
- Line 77: `expect(form.setValue).toHaveBeenCalledWith('iso_code_2', 'SA')` — CURRENTLY fails with "Received: undefined" because impl reads `match.cca2`, mock provides `code`.
- Line 78: `expect(form.setValue).toHaveBeenCalledWith('iso_code_3', 'SAU')` — same.
- Line 79: `expect(form.setValue).toHaveBeenCalledWith('region', 'Asia')` — WRONG ASSERTION; impl maps `'Asia'` → `'asia'` via REGION_MAP, so correct assertion is `'asia'`.
- Line 106: `expect(form.setValue).not.toHaveBeenCalledWith('region', 'Asia')` — must update to `'asia'`.
- vi.mock('@tanstack/react-query', ...) line 7-9: keep verbatim; the `vi.mocked(useQuery).mockReturnValue(...)` per-test override pattern (lines 68, 91) is correct, don't touch.

From RESEARCH (per-file diagnostic notes):

- `KpiStrip.test.tsx`: 2 failures, Dashboard widget assertion drift (Phase 38 baseline). The Phase 38 PASS-WITH-DEVIATION noted multiple Rule-3 API adaptations including `Donut value+variants[3]` — update test assertions to match current widget API.
- `heroui-wrappers.test.tsx`: 2 failures, HeroUI v3 wrapper drift from Phase 33-05 (`feat(33-05): rewrite heroui-button to use real @heroui/react Button primitive`). Update assertions to match the current rewrite.
- `buildTokens.test.ts`: token builder unit drift. Read impl + test, D-10 verdict per case.
- `analytics.cluster.test.tsx`: `vi.spyOn() can only spy on a function. Received undefined.` — module under spy no longer exports the symbol; verify import path + module shape.
- `responsive.test.ts`: same spyOn-undefined class.
- `waiting-queue-a11y.test.tsx`: a11y assertion drift per-file triage.
- `BulkActionToolbar.test.tsx`: jest-dom matcher gap suspected (CC-1, closed by Plan 50-06) + possible drift. Verify CC-1 alone closes it; if not, D-10 triage.
- `EscalationDialog.test.tsx`: Same suspicion (CC-1, closed by Plan 50-06). Verify after re-running runner at task start.
- `ConflictDialog.test.tsx`: `Cannot convert undefined or null to object` — test setup omits a required prop; D-10 per case.
- `FormInput.test.tsx`: per-file drift; D-10 triage.

From `.continue-here.md` §Bucketing Strategy → 50-08:

- "Discovery rule: re-run the FE runner at task start; any file not in the hot-list that's still failing after 50-06+50-07 land joins the per-file triage. Upper bound 12; if discovery exceeds 12, **STOP and escalate** — phase scope was misjudged again."

From CONTEXT D-10:

- "For each test failing on an assertion mismatch, do `git log -p` on both the asserting line AND the impl under test to determine which side regressed, then fix that side. The test name is treated as the contract spec; deviate only with an explicit rationale row in `50-TEST-AUDIT.md`."

From CONTEXT D-11:

- "Mixed assertion-text contract: Some tests assert on i18n keys (`'wizard.country.title'`); others assert on English strings (`'Decisions'`). Both remain valid; the `t() => key` fallback (D-06) supports both. Convert a test from English-string to key-based ONLY when already editing that test file for another reason. No bulk-rewrite-for-consistency sweep."

From CONTEXT D-01 (as amended by operator gap brief):

- "Phase 50 ends when `pnpm --filter frontend test` exits 0."
- **Per the operator gap brief amendment**: `queued-with-rationale` is NO LONGER acceptable for the default FE runner. Only `fixed-green`, `split-to-integration`, or `deleted-dead`.

The `split-to-integration` mechanism (preserved from archived Plan 50-02):

- Move the file out of the default FE runner glob by adding the file (or its directory) to `frontend/vitest.config.ts` `exclude`. The post-Plan-50-01 exclude is `['node_modules/', 'dist/', 'build/', '**/*.spec.*']` — append the file pattern (e.g., `frontend/tests/<path>`).
- Add a coordinated handoff entry to this plan's SUMMARY so Plan 50-04 (audit) and Plan 50-05 (CI gate) can absorb the split.
- Plan 50-04 records the file with `disposition: split-to-integration`.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Discovery + ceiling-check — re-run FE runner, enumerate residual, halt if >12 files</name>
  <files>.planning/phases/50-test-infrastructure-repair/50-08-DISCOVERY.md</files>
  <read_first>
    - .planning/phases/50-test-infrastructure-repair/50-06-SUMMARY.md (Plan 50-06 handoff list — files migrated to renderWithProviders; the expected ≤15 post-state)
    - .planning/phases/50-test-infrastructure-repair/50-07-SUMMARY.md (Plan 50-07 handoff — CC-5/CC-6/PF-2 disposition list)
    - .planning/phases/50-test-infrastructure-repair/.continue-here.md §Bucketing Strategy → 50-08 ("Upper bound 12; if discovery exceeds 12, STOP and escalate — phase scope was misjudged again")
  </read_first>
  <action>
    Discovery + halt-check task. Creates a single throwaway artifact recording the residual file set.

    1. Run `pnpm --filter intake-frontend test --run --reporter=default 2>&amp;1 | tee /tmp/phase50-08-discovery.log`.

    2. From the output, extract the list of failing test files. Vitest's default reporter prints per-file results; failing files are flagged with `FAIL` or `Test Files: N failed`. Extract via:
       ```
       grep -E "^.?FAIL\\s+" /tmp/phase50-08-discovery.log | awk '{print $2}' | sort -u
       ```
       OR (more reliable for vitest):
       ```
       grep -E "Test Files.*failed" /tmp/phase50-08-discovery.log | tail -1
       ```
       and a per-file enumeration via the reporter's "Failed Tests" summary section.

    3. Count the distinct failing test file paths.

    4. **Ceiling check:** If count > 12, STOP. Write a halt message to the operator stating:
       - "Discovery at Plan 50-08 Task 0 shows N failing files (> 12 ceiling)."
       - The full list of failing files.
       - "Phase scope was misjudged. Surface to operator for: (a) ratify scope expansion, (b) split 50-08 into 50-08a/08b, or (c) accept N-12 files as `split-to-integration` and absorb via vitest.config.ts exclude."
       - DO NOT proceed to Task 1+. The remaining tasks are blocked pending operator response.

    5. If count ≤ 12, write the residual file list (with each file's last-failure-line signature for D-10 archaeology lookup) into `.planning/phases/50-test-infrastructure-repair/50-08-DISCOVERY.md` (a temporary throwaway artifact). Format:
       ```
       # Plan 50-08 Discovery (task 0 snapshot)
       Captured: <ISO 8601 timestamp>
       Total failing files: N
       Files:
       - <path>: <one-line failure signature from runner output>
       - <path>: ...
       ```
       The discovery file is the input to Task 1+ per-file triage. It is deleted at SUMMARY-write time.

    6. Commit the discovery file with message `chore(50-08): record post-50-06/07 residual for ≤12 ceiling check`. The commit serves as audit evidence.

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm exec vitest --run --reporter=default 2>&amp;1 | tee /tmp/phase50-08-discovery.log | tail -5 | grep -E "Test Files" | awk '{ for(i=1;i&lt;=NF;i++) if ($i ~ /failed/) { prev=$(i-1); gsub(/[^0-9]/, "", prev); if (prev+0 &lt;= 12) { print "PASS: residual " prev " ≤12 ceiling"; exit 0 } else { print "HALT: " prev " &gt; 12 ceiling — escalate to operator"; exit 1 } } print "PASS: no failed-count token (likely 0 failures)"; exit 0 }' &amp;&amp; test -f .planning/phases/50-test-infrastructure-repair/50-08-DISCOVERY.md</automated>
  </verify>
  <done>
    Either (a) the runner reports ≤12 failing files AND `50-08-DISCOVERY.md` exists with the enumerated list, OR (b) the runner reports >12 failing files AND this task halts WITHOUT advancing to Task 1 (operator escalation surfaced). Path (a) advances; path (b) blocks Plan 50-08 pending operator decision.
  </done>
</task>

<task type="auto">
  <name>Task 1: Fix useCountryAutoFill regression — test mock shape + impl null-guards (D-09 + D-10, must-fix from gap brief)</name>
  <files>frontend/src/components/dossier/wizard/hooks/useCountryAutoFill.ts, frontend/src/components/dossier/wizard/hooks/__tests__/useCountryAutoFill.test.ts</files>
  <read_first>
    - frontend/src/components/dossier/wizard/hooks/useCountryAutoFill.ts (full file — current 72-line impl reading `match.cca2`, `match.cca3`, `match.region`, `match.capital`)
    - frontend/src/components/dossier/wizard/hooks/__tests__/useCountryAutoFill.test.ts (full file — current 110-line test with wrong `code`/`code3` mock and wrong `'Asia'` assertion)
    - .planning/phases/50-test-infrastructure-repair/50-02-PLAN.archived.md §Task 1 (the canonical spec for this fix — preserved verbatim from archived plan; do NOT deviate)
    - .planning/phases/50-test-infrastructure-repair/50-RESEARCH.md §Wizard Regression Triage (git archaeology: TEST IS WRONG since commit 7943ad20 on 2026-04-15)
    - .planning/phases/50-test-infrastructure-repair/50-PATTERNS.md §`useCountryAutoFill.ts` and §`useCountryAutoFill.test.ts`
    - .planning/phases/50-test-infrastructure-repair/50-CONTEXT.md D-09 (root-cause fixes allowed), D-10 (test name = contract spec)
  </read_first>
  <action>
    Two-file fix per D-10 archaeology verdict in RESEARCH. This task PRESERVES the spec from archived Plan 50-02 Task 1 verbatim per the operator gap brief.

    **A) Fix the test (`useCountryAutoFill.test.ts`) — primary fix per "TEST IS WRONG" verdict:**

    - In the first `it('auto-fills empty form fields when reference match found', ...)` block (around lines 59-80), change the `mockMatch` literal at lines 60-66 from `{ code: 'SA', code3: 'SAU', name_en: ..., name_ar: ..., region: 'Asia' }` to the REST Countries v3.1 contract `{ cca2: 'SA', cca3: 'SAU', region: 'Asia', capital: ['Riyadh'] }`. Drop `name_en` / `name_ar` — they are not on the `RestCountryResult` interface (lines 6-11 of impl) and never were read.
    - Change the line-79 assertion from `expect(form.setValue).toHaveBeenCalledWith('region', 'Asia')` to `expect(form.setValue).toHaveBeenCalledWith('region', 'asia')` — the impl maps `'Asia'` → `'asia'` via `REGION_MAP` at line 62.
    - Add a new assertion immediately after line 79: `expect(form.setValue).toHaveBeenCalledWith('capital_en', 'Riyadh')` — the impl line 68 fills `capital_en` from `match.capital[0]`, so the test should cover that contract now that the fixture provides it.
    - In the second `it('does not overwrite user-entered values', ...)` block (around lines 82-109), apply the SAME mockMatch shape fix at lines 83-89.
    - Update line 106 `expect(form.setValue).not.toHaveBeenCalledWith('region', 'Asia')` to `expect(form.setValue).not.toHaveBeenCalledWith('region', 'asia')` (must match the post-REGION_MAP value).
    - Do NOT touch the `vi.mock('@tanstack/react-query', ...)` block at lines 7-9 or the `vi.mocked(useQuery).mockReturnValue(...)` per-test overrides at lines 68 and 91 — those are the correct vitest patterns.

    **B) Harden the impl (`useCountryAutoFill.ts`) — D-09 product hardening:**

    - At line 55, extend the guard `if (current.iso_code_2 === '' && match.cca2 !== '')` to `if (current.iso_code_2 === '' && match.cca2 != null && match.cca2 !== '')` — the `!= null` matches both `null` and `undefined`.
    - At line 58, same treatment for `iso_code_3` / `match.cca3`: add `match.cca3 != null && ` before `match.cca3 !== ''`.
    - At line 61, same treatment for `region` / `match.region`: add `match.region != null && ` before `match.region !== ''`.
    - Do NOT touch the `match.capital != null` guard at line 67 — it is the in-file precedent and already correct.
    - Do NOT widen the TypeScript interface `RestCountryResult` (lines 6-11) to make fields optional — that would cascade through callers. The null-guard is a runtime defense only, per PATTERNS §useCountryAutoFill.ts Gotchas.

    Run the test BEFORE and AFTER each diff. After A+B both land, both `it` blocks pass green.

    Commit message: `fix(50-08): useCountryAutoFill — REST Countries v3.1 fixture + 3 null-guards (D-09 + D-10)`. Body lists the 5 test-file edits and 3 impl-file edits.

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -c "cca2: 'SA'" src/components/dossier/wizard/hooks/__tests__/useCountryAutoFill.test.ts | awk '$1 &gt;= 1 { exit 0 } { exit 1 }' &amp;&amp; grep -c "match.cca2 != null" src/components/dossier/wizard/hooks/useCountryAutoFill.ts | grep -q "^1$" &amp;&amp; grep -c "match.cca3 != null" src/components/dossier/wizard/hooks/useCountryAutoFill.ts | grep -q "^1$" &amp;&amp; grep -c "match.region != null" src/components/dossier/wizard/hooks/useCountryAutoFill.ts | grep -q "^1$" &amp;&amp; pnpm exec vitest --run src/components/dossier/wizard/hooks/__tests__/useCountryAutoFill.test.ts 2>&amp;1 | tail -5 | grep -E "Test Files.*passed" | grep -vE "[1-9]+[0-9]* failed"</automated>
  </verify>
  <done>
    Both `it` blocks in `useCountryAutoFill.test.ts` pass green. `grep "cca2: 'SA'" useCountryAutoFill.test.ts` returns ≥1 (both fixtures fixed). `grep "match.cca2 != null"` / `cca3 != null` / `region != null` each return 1 in `useCountryAutoFill.ts`. The `RestCountryResult` interface at lines 6-11 is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Per-file triage of remaining residual files (D-10 archaeology each); achieve phase-exit-0</name>
  <files>(Every residual failing FE file from Task 0's `50-08-DISCOVERY.md` MINUS the useCountryAutoFill pair handled in Task 1. Hot-list illustrated below; actual set is Task 0's output.)
- frontend/src/pages/Dashboard/widgets/__tests__/KpiStrip.test.tsx
- frontend/tests/unit/components/ui/heroui-wrappers.test.tsx
- frontend/tests/unit/design-system/buildTokens.test.ts
- frontend/tests/unit/analytics.cluster.test.tsx
- frontend/tests/unit/hooks/responsive.test.ts
- frontend/tests/accessibility/waiting-queue-a11y.test.tsx
- frontend/tests/component/BulkActionToolbar.test.tsx
- frontend/tests/component/EscalationDialog.test.tsx
- frontend/tests/component/ConflictDialog.test.tsx
- frontend/tests/unit/FormInput.test.tsx</files>
  <read_first>
    - .planning/phases/50-test-infrastructure-repair/50-08-DISCOVERY.md (Task 0 output — authoritative residual list)
    - .planning/phases/50-test-infrastructure-repair/50-06-SUMMARY.md (which files migrated to renderWithProviders — those files may have additional drift beyond CC-3)
    - .planning/phases/50-test-infrastructure-repair/50-07-SUMMARY.md (CC-5/CC-6/PF-2 dispositions — for cross-reference)
    - .planning/phases/50-test-infrastructure-repair/50-CONTEXT.md D-01, D-09, D-10, D-11 (the four governing decisions for per-file fixes)
    - .planning/phases/50-test-infrastructure-repair/.continue-here.md §Bucketing Strategy → 50-08 (the per-file diagnoses + hot-list)
    - For EACH file in the residual list, read: the test file itself (full file — identify failing assertions); the impl file under test (for D-10 archaeology — was the test wrong or did the impl drift?); the relevant Phase XX-YY summary in `.planning/phases/*/XX-YY-SUMMARY.md` if the test references a specific phase's baseline (KpiStrip → Phase 38; heroui-wrappers → Phase 33-05; etc.)
    - `git log -p <test-file>` and `git log -p <impl-file>` for archaeology per D-10
  </read_first>
  <action>
    Per-file triage. For EACH file in the residual list from Task 0, apply the D-10 procedure.

    **D-10 Per-File Procedure:**

    1. `pnpm --filter intake-frontend test --run <file> 2>&amp;1 | tail -30` — capture failure signature.
    2. `git log --oneline -- <file>` — test commit history.
    3. `git log --oneline -- <impl-file>` — impl commit history.
    4. Determine the verdict:
       - **TEST WRONG:** Test authored after impl with assertions that don't match impl behavior → fix test.
       - **IMPL DRIFTED:** Impl recently changed in a way that broke the test → fix impl OR update test (test name = contract spec; deviation requires explicit rationale in commit body).
       - **INFRASTRUCTURE-DEPENDENT:** Test cannot pass without real services → first attempt rescue (add MSW handler, stub env, wrap in providers); if not viable, split-to-integration via `frontend/vitest.config.ts` `exclude` addition + Plan 50-04 audit handoff.
       - **DEAD:** Test imports deleted modules or asserts behavior no longer in codebase → delete with rationale.

    5. Apply the fix.
    6. Run `pnpm --filter intake-frontend test --run <file>` — must exit 0 OR file must be removed from default-runner glob (split disposition).
    7. Commit per-file. Message format:
       - `fix(50-08): <file> — TEST WRONG (<short verdict>)` for test fixes.
       - `fix(50-08): <file> — IMPL DRIFTED (<short verdict>)` for impl fixes.
       - `split(50-08): <file> → integration (<infra required>)` for split disposition.
       - `delete(50-08): <file> — <rationale>` for dead-file deletion.
       Each commit body includes the `git log` evidence supporting the verdict.

    **Per-file starting hypotheses** (apply from `.continue-here.md`; verify before fixing):

    - `KpiStrip.test.tsx` (2 failures): Phase 38 baseline drift. Read current `KpiStrip.tsx` impl and align test assertions to current component API (text content, prop names, ARIA labels). Phase 38 PASS-WITH-DEVIATION noted `Donut value+variants[3]` adaptation — verify the test isn't asserting against the pre-deviation Donut shape.
    - `heroui-wrappers.test.tsx` (2 failures): Phase 33-05 HeroUI v3 rewrite drift (`feat(33-05): rewrite heroui-button to use real @heroui/react Button primitive`). Read current `frontend/src/components/ui/heroui-button.tsx` (and sibling -card, -chip, -skeleton) and update wrapper assertions to match the post-33-05 shape.
    - `buildTokens.test.ts`: Token builder unit drift. Read `frontend/src/design-system/tokens/buildTokens.ts` impl + test. D-10 verdict per case.
    - `analytics.cluster.test.tsx`: `vi.spyOn() can only spy on a function. Received undefined.` Verify the module path under spy — if a renamed module no longer exports the spied symbol, update the import path; if the symbol was removed entirely, the spied behavior may be obsolete (delete the spy and possibly the test if the behavior is gone).
    - `responsive.test.ts`: Same spyOn-undefined class. (Note: this file was touched in Plan 50-01's `93f56b2b` commit per `git show --stat` — confirm whether the prior touch addressed only part of the drift.)
    - `waiting-queue-a11y.test.tsx`: a11y assertion drift; read current impl + test + check whether axe rules changed in `@axe-core/react` upgrade.
    - `BulkActionToolbar.test.tsx`: First check whether Plan 50-06's CC-1 (jest-dom matchers) closed it. Re-run `pnpm exec vitest --run tests/component/BulkActionToolbar.test.tsx`. If green, the file is no longer in this plan's residual — note in commit-less SUMMARY entry. If still failing, triage per D-10.
    - `EscalationDialog.test.tsx`: Same CC-1 closure suspicion; verify first.
    - `ConflictDialog.test.tsx`: `Cannot convert undefined or null to object` — test setup omits a required prop. Read test render call sites + impl prop types; add the missing prop with a sensible default.
    - `FormInput.test.tsx`: Per-file drift; D-10 triage.

    **Important: do NOT bulk-rewrite per D-11.** When fixing a test for one reason (e.g., updating an assertion to match post-Phase-38 widget), do NOT also convert English-string assertions to i18n-key assertions unless the file is already being touched. Stay minimal.

    **CRITICAL — phase-exit-0 contract per D-01:**

    After ALL per-file dispositions are applied, run `pnpm --filter intake-frontend test --run` and confirm exit code 0. If any file remains failing AND is not split-to-integration, STOP and escalate to operator — DO NOT proceed to SUMMARY-write task. The phase-exit-0 contract is the entire point of Plans 50-06 + 50-07 + 50-08 together.

  </action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm exec vitest --run --reporter=default 2>&amp;1 | tail -5 | grep -E "Test Files" | awk '{ failed=0; for(i=1;i&lt;=NF;i++) if ($i ~ /failed/) { prev=$(i-1); gsub(/[^0-9]/, "", prev); failed=prev+0 } if (failed == 0) { print "PASS: phase-exit-0 contract satisfied (0 failing files)"; exit 0 } else { print "FAIL: " failed " files still failing — D-01 not satisfied"; exit 1 } }' &amp;&amp; pnpm exec vitest --run --reporter=default 2>&amp;1 | tail -3 | grep -qE "Tests.*passed" &amp;&amp; pnpm --filter intake-frontend lint</automated>
  </verify>
  <done>
    `pnpm --filter intake-frontend test --run` exits 0. Every residual file from Task 0's discovery list has a recorded disposition (fixed-green / split-to-integration / deleted-dead) with per-file commit. Workspace lint exits 0. The phase-exit-0 contract per D-01 is satisfied across Plans 50-06 + 50-07 + 50-08.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                                                                                                                                                   |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Test runner ↔ phase-exit-0 contract     | The FE default runner's exit code is the single source of truth for D-01. A false-positive green (e.g., file silently excluded without disposition record) would invalidate Plans 50-04 and 50-05 downstream. |
| Per-file disposition ↔ Plan 50-04 audit | Each per-file commit's verdict feeds the `50-TEST-AUDIT.md` body rows; falsifying a verdict would let a regression hide in the `disposition: fixed-green` column.                                             |
| Wizard hook fixture ↔ product hook      | `useCountryAutoFill` mocks `useQuery` with attacker-controlled data; the impl null-guards prevent malformed REST Countries responses from writing `undefined` to the form (D-09 hardening).                   |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                                                                                                         | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                        |
| ---------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-50-08-01 | T (Tampering)          | A per-file split-to-integration disposition could silently exclude a real regression check from the default runner.                               | mitigate    | Task 2's split disposition requires (a) explicit `frontend/vitest.config.ts` `exclude` addition (visible in the diff) and (b) handoff entry in this plan's SUMMARY for Plan 50-04 audit + Plan 50-05 integration runner reference. The two-channel record prevents the file from being "lost" outside the audit trail. |
| T-50-08-02 | D (DoS via test hangs) | Per-file triage at scale could exhaust the executor context window if >12 files actually need investigation.                                      | mitigate    | Task 0 ceiling check enforces the upper bound. If discovery exceeds 12, the executor halts BEFORE consuming budget on triage — escalation surfaces phase-scope misjudgment for operator decision.                                                                                                                      |
| T-50-08-03 | T (Tampering)          | `useCountryAutoFill` impl could be widened too aggressively (e.g., making `RestCountryResult` fields optional in the TypeScript interface).       | mitigate    | Task 1 part B explicitly forbids widening the `RestCountryResult` interface — the null-guard is runtime-only. Cascading interface changes would propagate through callers; preventing that is the disposition.                                                                                                         |
| T-50-08-04 | R (Repudiation)        | A per-file fix could be committed with a wrong verdict in the message (e.g., "TEST WRONG" when actually IMPL DRIFTED), polluting the audit trail. | accept      | Plan 50-04's audit author cross-checks each per-file commit against the actual diff at audit time. If a verdict is wrong, the audit row corrects it; commit messages are advisory, not authoritative.                                                                                                                  |

</threat_model>

<verification>
**Phase-level checks (post-plan):**

- `pnpm --filter intake-frontend test --run` exits 0 (D-01 non-negotiable, satisfied across 50-06 + 50-07 + 50-08).
- `frontend/src/components/dossier/wizard/hooks/useCountryAutoFill.ts` contains `match.cca2 != null`, `match.cca3 != null`, `match.region != null` (3 new null-guard occurrences).
- `frontend/src/components/dossier/wizard/hooks/__tests__/useCountryAutoFill.test.ts` contains `cca2: 'SA'` and `cca3: 'SAU'` in both `it` block fixtures.
- Per-file commits exist for each fixed/split/deleted residual (`git log --oneline 50-08` shows ≥1 per disposition).
- `pnpm --filter intake-frontend lint` exits 0 (no new violations introduced).
- Files moved to integration runner (if any) are listed in `50-08-SUMMARY.md` AND excluded from `frontend/vitest.config.ts`.
- Zero FE default-runner files in `queued-with-rationale` state.
- The 4 wizard tests from Plan 50-01 still pass green (no regression).
- Plan 50-06's renderWithProviders migration still works (the 9 migrated files continue to pass).

</verification>

<success_criteria>

- **TEST-02 fully SATISFIED:** `pnpm --filter intake-frontend test --run` exits 0; the cumulative phase-exit-0 contract across 50-06 + 50-07 + 50-08 is satisfied.
- **D-01 satisfied:** FE exit-0 achieved with no `queued-with-rationale` carve-outs in the default runner.
- **D-09 hardening:** `useCountryAutoFill.ts` has 3 new `!= null` guards (mirror of the existing `match.capital != null` idiom at line 67).
- **D-10 archaeology applied:** Each per-file disposition has a per-file commit with `git log` evidence in the body.
- **No regressions in Plan 50-01/06/07 gains:** Plan 50-01's 4 wizard tests + cascade-cleared tests stay green; Plan 50-06's renderWithProviders-migrated 9 files stay green; Plan 50-07's outlier dispositions stay closed.
- **Ceiling discipline honored:** Task 0 verified ≤12 files in the residual; if it would have exceeded 12, the executor escalated rather than silently expanding scope (the original Plan 50-02 mistake the gap brief explicitly preserves discipline against).
- **Audit / integration handoff:** Any `split-to-integration` files listed in `50-08-SUMMARY.md` ready for Plan 50-04 (audit) and Plan 50-05 (CI gate) to absorb.

</success_criteria>

<output>
After completion, create `.planning/phases/50-test-infrastructure-repair/50-08-SUMMARY.md` with:
- Task 0 discovery snapshot: count of residual files at task start + the enumerated list with failure signatures.
- Per-file disposition table for Task 2: `file | verdict (TEST WRONG / IMPL DRIFTED / split / delete) | commit hash | git-archaeology evidence`.
- Final FE test runner output: `Test Files: N passed (N) | Tests: M passed (M)`, exit code 0 confirmation.
- Note that the absence of any `queued-with-rationale` row for the FE default runner satisfies the D-01 amendment per the operator gap brief.
- Cross-plan handoff: any `split-to-integration` files listed for Plan 50-04 (audit) and Plan 50-05 (CI gate's `Tests (integration)` job) to absorb.
- Delete the throwaway `50-08-DISCOVERY.md` (no longer needed once the SUMMARY records the residual list).
- Phase-50-exit attestation for the FE workspace: TEST-02 satisfied at full-runner level; the BE workspace was already green per `50-03-SUMMARY.md`; Plans 50-04 (audit + docs) and 50-05 (CI gate + ESLint rule) are unblocked.
</output>
