# Milestone v6.2: Type-Check, Lint & Bundle Reset

**Status:** ✅ SHIPPED 2026-05-12
**Phases:** 47-49
**Total Plans:** 17
**Audit:** `.planning/milestones/v6.2-MILESTONE-AUDIT.md` (status: passed; re-audit confirmed 2026-05-13 after paperwork closure)

## Overview

Restored code-quality gates and bundle budget on `main` before v7.0 Intelligence Engine work. Drove frontend (1580) + backend (498) TS errors to zero with no suppression escape hatches; cleared frontend (52 err + 671 warn) + backend (3 err + 1 warn) lint to zero; aligned `no-restricted-imports` with the CLAUDE.md primitive cascade (HeroUI v3 → Radix → custom) and purged Aceternity references from the ESLint config; lowered `.size-limit.json` Initial-route ceiling 517 → 450 KB and static-prim 64 → 12 KB while splitting heroui/sentry/dnd sub-vendor chunks; restored type-check, Lint, and `Bundle Size Check (size-limit)` as PR-blocking branch-protection contexts on `main`. Coverage: 12/12 v6.2 requirements satisfied (TYPE-01..04, LINT-06..09, BUNDLE-01..04).

## Phases

### Phase 47: Type-Check Zero

**Goal:** Frontend and backend `pnpm type-check` exit 0 on a clean clone, with type-check restored as a PR-blocking CI gate so a single regression cannot reach `main`.
**Depends on:** Nothing inside v6.2 (first phase); operates directly on the v6.1 baseline.
**Plans:** 11/11 plans complete
**Requirements:** TYPE-01, TYPE-02, TYPE-03, TYPE-04

Plans:

- [x] 47-01: Frontend type-check baseline + EXCEPTIONS ledger seed + `phase-47-base` tag (TYPE-01 frontend half, TYPE-04 frontend half)
- [x] 47-02: Backend type-check 498 → 0 via ParsedQs narrowing, TS7030 return-path fixes, allowlisted Supabase codegen (TYPE-02, TYPE-04 backend half)
- [x] 47-03: Split `Lint` job into separate `Lint` + `type-check` jobs; branch-protection PUT; smoke-PR proof; TYPE-04 reconciliation (TYPE-03)
- [x] 47-04: `src/types/*` cluster — 60 hand-authored `*.types.ts` files cleaned (frontend 1564 → 1191)
- [x] 47-05: `src/components/{tasks,kanban,entity-links}/*` cross-workspace fence (frontend 1191 → 1184)
- [x] 47-06: `src/components/**` remainder + typed-shim pattern + 6 orphan deletes (frontend 1184 → 649)
- [x] 47-07: `src/hooks/**` cluster via reusable Python brace-tracker (frontend 649 → 496)
- [x] 47-08: `src/domains/**` typed-at-source + -33 consumer cascade (frontend 496 → 310)
- [x] 47-09: `src/pages/**` + `src/routes/**` + 8 stub hooks parametrized (frontend 310 → 134)
- [x] 47-10: `src/{services,lib,utils,store,contexts,design-system}/**` — 102 dead exports across 39 files (frontend 134 → 15)
- [x] 47-11: Residual 15 + 19 of 20 shims retired by typing underlying hooks at source; TYPE-04 ledger sites byte-immutable (TYPE-01 closed end-to-end)

### Phase 48: Lint & Config Alignment

**Goal:** Frontend and backend `pnpm lint` exit 0, the ESLint config matches the CLAUDE.md primitive cascade with zero Aceternity references, and lint is restored as a PR-blocking CI gate.
**Depends on:** Phase 47 (typed baseline avoids re-doing edits and prevents lint changes from masking type errors).
**Plans:** 3/3 plans complete
**Requirements:** LINT-06, LINT-07, LINT-08, LINT-09

Plans:

- [x] 48-01: Delete `frontend/eslint.config.js` shadow; invert `no-restricted-imports` to ban Aceternity + Kibo UI per CLAUDE.md cascade; delete 3 orphan Aceternity wrappers (`3d-card.tsx`, `bento-grid.tsx`, `floating-navbar.tsx`); add 5 filename-convention carve-outs; pin workspace lint scripts to root config with `--max-warnings 0`; `phase-48-base` tag (LINT-08)
- [x] 48-02: 12 `require()` → `vi.importActual`; 4 physical Tailwind → logical (`rounded-bl/br` → `rounded-es/ee`); 2 RTL test-fixture refactors; 9 stale `eslint-disable` deletions + 1 unused import; backend empty-interface → type alias; Winston `logInfo` replaces `console.log`; filename/folder carve-outs expanded (32 + 28 + 3 violations cleared); `paths` narrowing on `no-restricted-imports` (LINT-06, LINT-07)
- [x] 48-03: `Lint` added to main branch-protection `required_status_checks.contexts`; two D-16 smoke PRs verify `mergeStateStatus=BLOCKED`; phase-wide D-17 audit returns 0 net-new `eslint-disable`; STATE.md Phase 47 follow-up #1 resolved by analogy (LINT-09)

### Phase 49: Bundle Budget Reset

**Goal:** `frontend/.size-limit.json` reflects a real, defensible budget; the initial route loads under it; `size-limit` is restored as a PR-blocking CI gate.
**Depends on:** Phase 48 (typed and linted baseline so every `React.lazy()` and vendor-chunk change is enforced by the same gates that catch regressions).
**Plans:** 3/3 plans complete
**Requirements:** BUNDLE-01, BUNDLE-02, BUNDLE-03, BUNDLE-04

Plans:

- [x] 49-01: `phase-49-base` diff anchor; `ANALYZE=true` audit → `49-BUNDLE-AUDIT.md` (top-20 chunks + lazy candidates); re-baselined `.size-limit.json` ceilings per D-01..D-03; scaffolded `frontend/docs/bundle-budget.md`. D-02 escalation FILED (Total JS 2.45 MB, not 1.8 MB). manualChunks `id.includes("react")` substring mis-classification surfaced as Plan 02 prerequisite (BUNDLE-01, BUNDLE-04)
- [x] 49-02: Extended manualChunks with heroui/sentry/dnd-vendor branches (D-07); strict `===1` entries in `assert-size-limit-matches.mjs`; 3 audit-identified ≥30 KB gz non-initial components converted to React.lazy / dynamic-import (`PositionEditor`, `WorldMapVisualization`, `useExportData/exceljs`) with D-13 Suspense fallbacks; TanStack vendor ceiling 51 → 63 KB (D-03 honest re-baseline post ordering fix) (BUNDLE-02, BUNDLE-04)
- [x] 49-03: `Bundle Size Check (size-limit)` added to main branch protection (verbatim casing); `enforce_admins=true` preserved. Two D-12 smoke PRs proved `BLOCKED`: #9 (initial-JS overflow via 100 KB high-entropy literal) and #10 (sub-vendor d3-geospatial overflow via `world-atlas/countries-50m.json`); both closed `--delete-branch`. D-14 phase-wide audit returned 0 net-new suppressions + 0 unauthorized ceiling raises (BUNDLE-03)

## Milestone Summary

**Key Decisions:**

- Phase-base git tags (`phase-47-base`, `phase-48-base`, `phase-49-base`) as suppression-diff anchors — replaces unreliable `git merge-base main HEAD` for net-new-suppression audits
- `@ts-nocheck` on auto-generated Supabase types (`database.types.ts`, `contact-directory.types.ts`) as documented EXCEPTIONS-ledger entries — D-11 alternative to `tsconfig` exclude
- Typed-at-source migration retires 19 of 20 typed shims by tightening underlying domain hook return types instead of casting at the destructure boundary
- Single canonical root `eslint.config.mjs`; `frontend/eslint.config.js` shadow deleted; workspace scripts pinned to root with `--max-warnings 0`
- `no-restricted-imports` inverted: bans Aceternity/Kibo UI per CLAUDE.md primitive cascade (HeroUI v3 → Radix → custom); rule messages no longer recommend a banned library
- D-02 honest Total-JS ceiling locked at 2.45 MB (not 1.8 MB) with documented paper trail; per-chunk ceilings are the binding budget
- manualChunks ordering: scoped-package rules placed BEFORE `id.includes("react")` substring rule to prevent @heroui/@dnd-kit/@radix-ui mis-classification into react-vendor
- Sub-vendor decomposition (heroui-vendor / sentry-vendor / dnd-vendor); `assert-size-limit-matches.mjs` strict-`===1` entries lock the chunk identity
- Branch protection PUT mechanics (read-then-merge-then-write) preserved across all three CI-gate plans; `enforce_admins=true` retained
- size-limit's native fail-on-exceed IS the BUNDLE-03 enforcement (no custom delta calculator)

**Issues Resolved:**

- Frontend type-check 1580 → 0 errors (deletion / real-fix only — zero `@ts-ignore` / `@ts-expect-error` added)
- Backend type-check 498 → 0 errors (ParsedQs narrowing + TS7030 return-path discipline + Supabase codegen allowlist)
- Frontend lint 723 problems (52 err + 671 warn) → 0
- Backend lint 4 problems (3 err + 1 warn) → 0
- Aceternity references purged from `frontend/eslint.config.js`; `no-restricted-imports` aligned with primitive cascade
- Bundle budget reset: Initial-route ceiling 517 → 450 KB; static-prim 64 → 12 KB; sub-vendor chunks split with documented ceilings
- type-check, Lint, and `Bundle Size Check (size-limit)` restored as PR-blocking branch-protection contexts on `main`
- 19 of 20 typed shims retired via typed-at-source domain hook returns

**Issues Deferred:**

- `useStakeholderInteractionMutations` retained as the lone shim (1 of 20); underlying hook still returns `Promise.resolve({ success: true })` until real implementation lands
- `TasksTab.tsx` + `EngagementKanbanDialog.tsx` still import `@/components/kibo-ui/kanban`; lint rule narrowed (paths vs patterns split) to avoid false-positive — HeroUI v3 Kanban + @dnd-kit refactor deferred until HeroUI v3 stabilizes
- React vendor ceiling 349 KB vs measured 279.91 KB — Plan 02 intentionally left loose; tighten to ~285 KB per D-03 min rule as a micro-task
- 4 wizard tests fail at module-evaluation time — pre-existing `vi.mock("react-i18next")` factory omits `initReactI18next` in `frontend/tests/setup.ts:6`; NOT introduced by Phase 48
- Visual deviation closeouts for Phase 38/39/41 PASS-WITH-DEVIATION items — separate concern, not blocking v7.0

**Technical Debt Incurred:**

- CLAUDE.md Node note reads `Node.js 20.19.0+` while `package.json` engines moved to `22.13.0+` — chore commit
- Pre-existing design-rule violations preserved (out of Phase 49 scope): `WorldMapVisualization.tsx:193` raw hex `#3B82F6`; `PositionEditor.tsx:211/237` `text-blue-600 underline`; `PositionEditor.tsx:412/531` `text-red-800` / `text-red-600`. Queue for design-compliance sweep
- `phase-47-base` / `phase-48-base` / `phase-49-base` exist as lightweight tags rather than annotated/signed — `git tag -v` errors with "non-tag object". Recommend re-tagging with `-a -s` in next milestone for provenance
- Recommend `presize-limit` hook (`rm -rf dist`) before measuring — local `dist/` hygiene caused a confusing false-overflow on initial Phase 49 verification
- Tag SHA notation inconsistency — Plan 01 SUMMARY used commit SHA for `phase-49-base`; Plans 02/03 used tag-object SHA. Both dereference to same commit; cosmetic
- Wave-3 executor commit `4fd65d60` edited orchestrator-owned STATE/ROADMAP/REQUIREMENTS fields; values are correct, documented in 48-03-SUMMARY.md Deviation #4

---

_For current project status, see `.planning/ROADMAP.md`._
