# Milestone v6.4: Stabilization & Carryover Sweep

**Status:** ✅ SHIPPED 2026-05-27
**Phases:** 55-59
**Total Plans:** 20
**Audit:** `.planning/milestones/v6.4-MILESTONE-AUDIT.md` (status: tech_debt — functionally complete; 14/14 requirements satisfied; integration 14/14 wired with 0 blockers; 6 documentation/process-debt items, 0 functional blockers)

## Overview

Closed the v6.3 carryover backlog and merged the long-lived DesignV2 branch onto `main` so v7.0 Intelligence Engine feature work starts on stable, fully-gated ground. The milestone landed DesignV2 with every v6.3 quality gate intact, upgraded `main` branch protection from 6 → 8 required CI contexts (proven live with a planted-violation smoke PR), resolved the pre-existing `countries` RLS audit fail, retired the last v6.2-era typed shim, closed all five Phase 52 PASS-WITH-DEVIATION carryovers, fully cleared the 271-suppression / 2336-node Tier-C design-token backlog, and reconciled the remaining cosmetic + CI paperwork — including positive-failure CI fixtures that break the build if the design-token / vi.mock guards ever stop firing.

Coverage: 14/14 v6.4 requirements satisfied (MERGE-01..02, RLS-01, TYPE-05, DEVIATE-01..04, TOKEN-01..02, POLISH-01..04). No orphans.

## Phases

### Phase 55: DesignV2 → Main Merge & Gate Enforcement

**Goal:** DesignV2 lands on `main` and all v6.3 quality gates (type-check, Lint, Bundle Size Check, design-token D-05, react-i18next factory) enforce on every `main` PR context.
**Depends on:** Nothing (first phase of v6.4; blocker for 56-59)
**Plans:** 4/4 complete (sequential waves 1→4 per D-15 clean causality chain)
**Requirements:** MERGE-01, MERGE-02

Plans:

- [x] 55-01: Pre-merge verification + `--no-ff` merge (commit `3f763ddc`, PR #13) + signed `phase-55-base` tag; remote DesignV2 deleted (Wave 1)
- [x] 55-02: 2 new CI jobs (`Design Token Check` + `react-i18next Factory Check`) landed on main via PR #15 (`9e4471e3`); both green on post-merge HEAD (Wave 2)
- [x] 55-03: `main` branch protection round-tripped 6 → 8 required contexts via `gh api -X PUT`; all security invariants preserved (`enforce_admins=true`, `allow_force_pushes=false`, `allow_deletions=false`, `block_creations=false`, `strict=true`); protection-before/after JSON committed per D-16 (Wave 3)
- [x] 55-04: Smoke PR #18 with 4 planted violations → `mergeStateStatus=BLOCKED` (GraphQL, uppercase) with `Lint`+`type-check` at FAILURE; evidence committed via PR #19 (`ec9caffb`) before smoke PR closed `--delete-branch` per D-12 (Wave 4)

**D-13 upgrade:** reversed the v6.3 D-09 fold-into-Lint — 8 explicit required contexts (original 6 + `Design Token Check` + `react-i18next Factory Check`) for failure-attribution clarity.

### Phase 56: RLS Closure & Last Typed-Shim Retirement

**Goal:** `rls-audit.test.ts` passes for `countries` without an acknowledged-fail entry, and the final v6.2-era typed shim is removed in favour of source-typed return shapes.
**Depends on:** Phase 55
**Plans:** 2/2 complete (2026-05-18)
**Requirements:** RLS-01, TYPE-05

Plans:

- [x] 56-01: D-54-04 closed — `countries` moved from `sensitiveTables` to a new `globalReferenceTables` tier (authenticated-read + role-gated writes); staging policy reconciliation migration applied; `rls-audit.test.ts` green
- [x] 56-02: `UseStakeholderInteractionMutationsReturn` exported from the source hook; deprecated consumer shim deleted; focused Vitest coverage added; `phase-56-base` SSH-signed

### Phase 57: Phase 52 Deviation Closure (D-19..D-23)

**Goal:** All five Phase 52 PASS-WITH-DEVIATION carryovers (D-19..D-23, excluding the already-closed D-20) reach a verified resolution recorded against the original deviation rows.
**Depends on:** Phase 55
**Plans:** 4/4 complete (2026-05-19)
**Requirements:** DEVIATE-01, DEVIATE-02, DEVIATE-03, DEVIATE-04

Plans:

- [x] 57-01 (DEVIATE-01 / D-19): ADR `docs/adr/0001-mobile-dnd-scope-out.md` scopes mobile touch DnD out + mobile `<select>` "Move to" read-only fallback in `TasksTab.tsx`; 768×1024 desktop-DnD skips retained
- [x] 57-02 (DEVIATE-02 / D-21): shared `@/components/kanban` primitive owns `@dnd-kit/core`; ESLint ban + `eslint-ban-dndkit.test.ts` regression test + positive-failure fixture; specs scoped to `.workboard-page`
- [x] 57-03 (DEVIATE-03 / D-22): kanban + tasks-tab LTR/RTL baselines md5-distinct at both 768 and 1280; `?lng` URL-param fix + 3-layer ESLint guard against the `addInitScript(i18nextLng)` anti-pattern
- [x] 57-04 (DEVIATE-04 / D-23): live tasks-tab Playwright run on seeded staging engagement — 17 enumerated / 8 passed / 9 expected-skip / 0 fail; artifact `57-LIVE-RUN-SUMMARY.md` + log + 4 screenshots committed

### Phase 58: Tier-C Design-Token Suppression Full Clear

**Goal:** Every `// gsd-design-token-tier-c-allow` suppression in `frontend/src/` is replaced with a real token reference, and the waiver token is removed from `eslint.config.mjs` so `pnpm lint` stays at 0 without it.
**Depends on:** Phase 55
**Plans:** 7/7 complete (wave 0 + waves 1-6; 2026-05-22; merge `aed43b97`; `phase-58-base` signed)
**Requirements:** TOKEN-01, TOKEN-02

Outcomes:

- [x] TOKEN-01: `grep -r "gsd-design-token-tier-c-allow" frontend/src` → 0 matches (all 271 suppressions / 2336 AST nodes cleared, wave-staged by surface: forms, tables, charts, drawers, dossier rail)
- [x] TOKEN-02: waiver token removed from `eslint.config.mjs`; `pnpm exec eslint -c eslint.config.mjs --max-warnings 0 'frontend/src/**/*.{ts,tsx}'` exits 0 with D-05 selectors at `error`

_Note: numbered PLANs 58-07..58-10 are superseded stubs; the real work is the 7 wave plans 58-00..58-06._

### Phase 59: Cosmetic + CI Gap Closure

**Goal:** Stale v6.3 paperwork is refreshed, documentation drift is corrected, and the bad-design-token + bad-vi-mock fixtures fail CI loudly when their expected lint/test errors stop firing.
**Depends on:** Phase 55
**Plans:** 3/3 complete (PR #27 merged `d3e7f8e`; `phase-59-base` SSH-signed on merge SHA)
**Requirements:** POLISH-01, POLISH-02, POLISH-03, POLISH-04

Plans:

- [x] 59-01 (POLISH-02/03/04): TweaksDrawer stale comment removed; `51-VALIDATION.md` frontmatter `status: passed`; positive-failure ESLint fixtures flip-tested + criterion reconciliation
- [x] 59-02 (POLISH-01): Phase 53 `PASS`/`verified` flip gated behind origin-tag object + peeled-commit + SSH-signature verification
- [x] 59-03: closure — PR #27 merged (`d3e7f8e`), 8 required contexts green, SSH-signed `phase-59-base` tag on merge SHA

## Milestone Summary

**Key Decisions:**

- Reverse the v6.3 D-09 fold: 8 explicit required `main` contexts over 6 — failure attribution per gate beats a single composite `Lint` context (Phase 55 D-13)
- `countries` belongs in a `globalReferenceTables` RLS tier (authenticated-read + role-gated writes), not `sensitiveTables` — resolves the Phase 03/04-vintage pre-existing fail without weakening the audit (Phase 56)
- Mobile touch DnD scoped out via ADR + a read-only `<select>` fallback rather than wiring a touch sensor — keeps the shared primitive simple; desktop DnD is the supported path (Phase 57 D-19)
- Tier-C cleared as mechanical swaps to existing tokens, wave-staged by surface for independent reviewability/revertability — not a token-system extension (Phase 58)
- Positive-failure CI fixtures (`bad-design-token.tsx`, `bad-vi-mock.ts`) live outside every `pnpm lint` glob so they can't break the main `Lint` context, yet their dedicated jobs fail if the guards stop firing (Phase 59 POLISH-04)

**Issues Resolved:**

- DesignV2 → main merge sequence (MERGE-01/02) — DesignV2 landed; 8-context enforcement proven live
- D-54-04 RLS pre-existing fail (RLS-01) and last typed shim (TYPE-05)
- All 5 Phase 52 deviations D-19..D-23 (DEVIATE-01..04)
- Full 271-suppression Tier-C design-token backlog (TOKEN-01/02)
- Phase 53 cosmetic wording, TweaksDrawer comment drift, 51-VALIDATION frontmatter, bad-fixture CI assertions (POLISH-01..04)

**Issues Deferred (documentation/process tech debt — 0 functional blockers):**

- Missing VERIFICATION.md for Phases 55-58 (satisfaction established via audit ground-truth + integration checker running the actual gates)
- Nyquist VALIDATION paperwork lags implementation: 1 compliant (55), 1 draft (58), 3 missing (56/57/59)
- `phase-59-base` SSH-signed and confirmed on origin (`29efc676` matches local; the audit's "not on origin" note was stale — verified at close)
- Hygiene: `57-LIVE-RUN-SUMMARY.md` non-standard filename; Phase 58 superseded stub PLANs 58-07..10
- RLS-01 test env-gated on `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` — confirm the `Tests (backend)` CI job injects them

---

_For current project status, see .planning/ROADMAP.md_
