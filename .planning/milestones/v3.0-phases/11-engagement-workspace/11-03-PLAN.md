---
phase: 11-engagement-workspace
plan: 03
type: execute
wave: 2
depends_on: ['11-01']
files_modified:
  - frontend/src/pages/engagements/workspace/OverviewTab.tsx
  - frontend/src/pages/engagements/workspace/ContextTab.tsx
autonomous: true
requirements:
  - WORK-04
  - WORK-05
must_haves:
  truths:
    - "Overview tab shows engagement summary, participants, key decisions, and what's next action card"
    - 'Context tab shows linked dossiers organized by tier with Link Dossier action'
    - 'Context tab shows AI-generated recommendations from useEngagementRecommendations'
  artifacts:
    - path: 'frontend/src/pages/engagements/workspace/OverviewTab.tsx'
      provides: 'Summary dashboard with metrics, participants, recent activity, quick actions'
      min_lines: 80
    - path: 'frontend/src/pages/engagements/workspace/ContextTab.tsx'
      provides: 'Intelligence/prep sheet with linked dossiers, AI recommendations, talking points'
      min_lines: 60
  key_links:
    - from: 'frontend/src/pages/engagements/workspace/OverviewTab.tsx'
      to: 'frontend/src/domains/engagements/hooks/useEngagements.ts'
      via: 'useEngagement for summary data, useEngagementParticipants for participants'
      pattern: 'useEngagement|useEngagementParticipants'
    - from: 'frontend/src/pages/engagements/workspace/ContextTab.tsx'
      to: 'frontend/src/domains/engagements/hooks/useEngagementRecommendations.ts'
      via: 'useEngagementRecommendations for AI suggestions'
      pattern: 'useEngagementRecommendations'
---

<objective>
Build the Overview and Context tab content components for the engagement workspace.

Purpose: The Overview tab (D-01) is the landing view showing engagement health at a glance. The Context tab (D-02) is the intelligence/prep sheet for understanding the engagement's diplomatic context. Both are primary workspace tabs users interact with frequently.

Output: OverviewTab.tsx (summary dashboard) and ContextTab.tsx (intelligence sheet) — fully functional tab content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-engagement-workspace/11-CONTEXT.md
@.planning/phases/11-engagement-workspace/11-RESEARCH.md
@.planning/phases/11-engagement-workspace/11-UI-SPEC.md
@.planning/phases/11-engagement-workspace/11-01-SUMMARY.md

<interfaces>
<!-- Key hooks and components the executor needs -->

From frontend/src/domains/engagements/hooks/useEngagements.ts:

```typescript
export function useEngagement(engagementId: string): UseQueryResult<EngagementDetailResponse>
export function useEngagementParticipants(engagementId: string): UseQueryResult<Participant[]>
export function useEngagementAgenda(engagementId: string): UseQueryResult<AgendaItem[]>
```

From frontend/src/domains/engagements/hooks/useEngagementKanban.ts:

```typescript
export function useEngagementKanban(engagementId: string): {
  columns: KanbanColumns | undefined
  stats: {
    todo: number
    in_progress: number
    review: number
    done: number
    total: number
    completionPercent: number
  }
  // ...
}
```

From frontend/src/domains/engagements/hooks/useEngagementRecommendations.ts:

```typescript
export function useEngagementRecommendations(params: {
  target_dossier_id: string
}): UseQueryResult<Recommendation[]>
```

From frontend/src/domains/engagements/hooks/useLifecycle.ts:

```typescript
export function useLifecycleHistory(engagementId: string): UseQueryResult<LifecycleTransition[]>
```

From frontend/src/components/Dossier/DossierContextBadge.tsx:

```typescript
export function DossierContextBadge({
  dossier,
  inheritanceSource,
}: DossierContextBadgeProps): ReactElement
```

From frontend/src/types/lifecycle.types.ts:

```typescript
export type LifecycleStage =
  | 'intake'
  | 'preparation'
  | 'briefing'
  | 'execution'
  | 'follow_up'
  | 'closed'
export const LIFECYCLE_STAGE_LABELS: Record<LifecycleStage, { en: string; ar: string }>
```

Route param access in tab components:

```typescript
import { useParams } from '@tanstack/react-router'
const { engagementId } = useParams({ from: '/_protected/engagements/$engagementId' })
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build OverviewTab — summary dashboard with metrics, participants, activity, quick actions</name>
  <files>
    frontend/src/pages/engagements/workspace/OverviewTab.tsx
  </files>
  <read_first>
    frontend/src/pages/engagements/workspace/OverviewTab.tsx
    frontend/src/pages/engagements/EngagementDetailPage.tsx
    frontend/src/domains/engagements/hooks/useEngagements.ts
    frontend/src/domains/engagements/hooks/useEngagementKanban.ts
    frontend/src/domains/engagements/hooks/useLifecycle.ts
    frontend/src/components/ui/card.tsx
    frontend/src/components/ui/button.tsx
    frontend/src/hooks/useDirection.ts
    frontend/src/i18n/en/workspace.json
  </read_first>
  <action>
    Replace the stub in `frontend/src/pages/engagements/workspace/OverviewTab.tsx` with the full summary dashboard (per D-01, L-02 in UI-SPEC):

    **1. Data fetching:**
    - Get `engagementId` from `useParams({ from: '/_protected/engagements/$engagementId' })`
    - `useEngagement(engagementId)` — engagement details (name, type, start_date, end_date, lifecycle_stage, description)
    - `useEngagementParticipants(engagementId)` — participants list
    - `useEngagementKanban(engagementId)` — task stats (completionPercent, total, done, in_progress)
    - `useLifecycleHistory(engagementId)` — to compute days in current stage
    - `useTranslation('workspace')` for labels

    **2. Metrics row (top of tab):**
    Layout: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4`
    4 metric cards, each using Card component (`@/components/ui/card`):

    - **Current Stage card:** Icon (lifecycle icon), label `t('overview.currentStage')`, value = `LIFECYCLE_STAGE_LABELS[engagement.lifecycle_stage]` in current language. Use `text-xl font-semibold` for value, `text-sm text-muted-foreground` for label.
    - **Days in Stage card:** Icon (clock), label `t('overview.daysInStage')`, value = computed from most recent transition date in `lifecycleHistory`. Calculate: `Math.floor((Date.now() - new Date(lastTransition.transitioned_at).getTime()) / 86400000)`. `text-xl font-semibold` for value.
    - **Task Progress card:** Icon (check-circle), label `t('overview.taskProgress')`, value = `${stats.completionPercent}%` with a small progress indicator. Show `${stats.done}/${stats.total}` below in muted text.
    - **Deadline card:** Icon (calendar), label `t('overview.deadline')`, value = formatted `engagement.end_date` or "No deadline" if null.

    Each card: `p-4 rounded-lg border bg-card`, min touch target `min-h-11`.

    **3. Two-column section below metrics:**
    Layout: `grid grid-cols-1 md:grid-cols-2 gap-6 mt-6`

    **Participants column:**
    - Heading: `t('overview.participants')` in `text-xl font-semibold`
    - List of participants from `useEngagementParticipants`. Each item: avatar placeholder (initials circle), name, role badge.
    - If empty: "No participants added yet" in muted text.
    - Layout: `space-y-3`

    **Recent Activity column:**
    - Heading: `t('overview.recentActivity')` in `text-xl font-semibold`
    - Show last 5 lifecycle transitions from `useLifecycleHistory` as simple activity items: "[user_name] transitioned to [stage] — [date]"
    - If empty: "No activity yet" in muted text.
    - Layout: `space-y-3`

    **4. Quick Actions row:**
    Layout: `flex flex-wrap gap-2 mt-6`
    Buttons using Button component (`@/components/ui/button`):
    - "Advance Stage" button (`variant="default"`) — navigates to lifecycle bar or triggers transition
    - "Log After-Action" button (`variant="outline"`) — navigates to `/engagements/${engagementId}/after-action`
    - "Create Task" button (`variant="outline"`) — placeholder action (will integrate with Tasks tab)
    All buttons: `min-h-11 min-w-11` touch targets.

    **5. RTL and responsive:**
    - Use `useDirection()` for `dir` attribute on container
    - All logical properties: `ms-*`, `me-*`, `ps-*`, `pe-*`
    - No `ml-*`, `mr-*`, `text-left`, `text-right`
    - Mobile: single column, cards stack vertically
    - Padding: `p-4 sm:p-6 lg:p-8` on container

    **6. Explicit return type** `ReactElement` on the default export function.
    **7. Use `export default function OverviewTab()`** (required for React.lazy).

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && pnpm --filter frontend exec tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `OverviewTab.tsx` contains `useEngagement` import
    - `OverviewTab.tsx` contains `useEngagementParticipants` import
    - `OverviewTab.tsx` contains `useEngagementKanban` import
    - `OverviewTab.tsx` contains `useLifecycleHistory` import
    - `OverviewTab.tsx` contains `useParams` import from `@tanstack/react-router`
    - `OverviewTab.tsx` contains `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` (metrics row layout)
    - `OverviewTab.tsx` contains `grid grid-cols-1 md:grid-cols-2` (two-column section)
    - `OverviewTab.tsx` contains `export default function OverviewTab`
    - `OverviewTab.tsx` contains `useTranslation('workspace')` or `useTranslation` with workspace namespace
    - `OverviewTab.tsx` contains `/after-action` string (quick action link)
    - No `ml-*`, `mr-*`, `text-left`, `text-right` in the file
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Overview tab renders a summary dashboard with 4 metric cards (stage, days in stage, task progress, deadline), participants list, recent activity feed, and quick action buttons. Mobile-first responsive layout. RTL-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Build ContextTab — linked dossiers, AI recommendations, talking points</name>
  <files>
    frontend/src/pages/engagements/workspace/ContextTab.tsx
  </files>
  <read_first>
    frontend/src/pages/engagements/workspace/ContextTab.tsx
    frontend/src/domains/engagements/hooks/useEngagementRecommendations.ts
    frontend/src/components/Dossier/DossierContextBadge.tsx
    frontend/src/hooks/useDossier.ts
    frontend/src/domains/engagements/hooks/useEngagements.ts
    frontend/src/components/ui/button.tsx
    frontend/src/components/ui/card.tsx
    frontend/src/i18n/en/workspace.json
  </read_first>
  <action>
    Replace the stub in `frontend/src/pages/engagements/workspace/ContextTab.tsx` with the intelligence/prep sheet (per D-02, L-03 in UI-SPEC):

    **1. Data fetching:**
    - Get `engagementId` from `useParams({ from: '/_protected/engagements/$engagementId' })`
    - `useEngagement(engagementId)` — for linked dossier IDs and engagement metadata
    - `useEngagementRecommendations({ target_dossier_id: engagementId })` — AI recommendations
    - `useTranslation('workspace')` for labels

    **2. Linked Dossiers section (top):**
    - Heading: "Linked Dossiers" in `text-xl font-semibold`
    - Group linked dossiers by tier: Anchors (countries, organizations), Activities (engagements, forums), Threads (topics, working groups), Contacts (persons, elected officials)
    - For each tier group:
      - Tier subheading in `text-sm font-semibold text-muted-foreground uppercase tracking-wider`
      - Dossier badges in `flex flex-wrap gap-2` using `DossierContextBadge` component
    - `[+ Link Dossier]` button at bottom of section (`variant="outline"`, `size="sm"`) — placeholder action for now
    - Empty state: show `t('empty.context.heading')` and `t('empty.context.body')` text with `t('empty.context.action')` button
    - Layout: `space-y-4`

    **3. AI Recommendations section (middle):**
    - Heading: "AI Recommendations" in `text-xl font-semibold`
    - Map over recommendations array. Each recommendation as a Card:
      - Title: recommendation title in `text-base font-semibold`
      - Body: recommendation description in `text-sm text-muted-foreground`
      - Type badge (relationship, action, insight) in small colored badge
      - Priority indicator if high priority
    - If no recommendations: "No AI recommendations available" in muted text
    - Layout: `space-y-3`

    **4. Talking Points section (bottom):**
    - Heading: "Talking Points" in `text-xl font-semibold`
    - Display engagement description/notes as talking points content
    - If engagement has `talking_points` field, render it. Otherwise show the engagement `description` as context.
    - Placeholder text area or readonly display — actual editing is not in scope for this phase
    - Layout: `space-y-2`

    **5. Overall layout:**
    - Container: `space-y-6 p-4 sm:p-6 lg:p-8`
    - Each section separated by visual gap (space-y-6)
    - RTL: `useDirection()` for `dir`, logical properties only
    - Mobile: single column, sections stack naturally
    - No `ml-*`, `mr-*`, `text-left`, `text-right`

    **6. Explicit return type** `ReactElement` on the default export function.
    **7. Use `export default function ContextTab()`** (required for React.lazy).

  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0 && pnpm --filter frontend exec tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `ContextTab.tsx` contains `useEngagementRecommendations` import
    - `ContextTab.tsx` contains `DossierContextBadge` import
    - `ContextTab.tsx` contains `useParams` import from `@tanstack/react-router`
    - `ContextTab.tsx` contains `export default function ContextTab`
    - `ContextTab.tsx` contains `useTranslation` call
    - `ContextTab.tsx` contains `Link Dossier` or `linkDossier` string (action button)
    - `ContextTab.tsx` contains tier grouping labels: `Anchors` or `anchors` or tier classification logic
    - `ContextTab.tsx` contains `space-y-6` (section separation)
    - No `ml-*`, `mr-*`, `text-left`, `text-right` in the file
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Context tab renders linked dossiers grouped by tier with DossierContextBadge components, AI recommendations from useEngagementRecommendations as cards, and talking points section. Empty states for each section. Mobile-first, RTL-safe layout.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd frontend && pnpm exec tsc --noEmit`
2. Navigate to `/engagements/:id/overview` — summary dashboard renders with metrics, participants, activity
3. Navigate to `/engagements/:id/context` — linked dossiers and AI recommendations render
4. Both tabs responsive on mobile (single column) and desktop (multi-column for overview)
5. RTL: text alignment and layout direction correct in Arabic
</verification>

<success_criteria>

- Overview tab shows 4 metric cards, participants, recent activity, and quick action buttons
- Context tab shows linked dossiers by tier, AI recommendations, and talking points
- Both tabs fetch data via existing hooks (no new API calls)
- Both tabs have proper empty states
- TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/11-engagement-workspace/11-03-SUMMARY.md`
</output>
