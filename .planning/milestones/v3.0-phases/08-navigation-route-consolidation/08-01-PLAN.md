---
phase: 08-navigation-route-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/navigation-config.ts
  - frontend/src/components/layout/nav-main.tsx
  - frontend/src/components/layout/AppSidebar.tsx
  - frontend/src/components/layout/nav-user.tsx
autonomous: true
requirements: [NAV-01, NAV-02]
must_haves:
  truths:
    - 'User sees sidebar with 3 groups: Operations, Dossiers, Administration'
    - 'Operations group contains Dashboard, Engagements, After-Actions, Tasks, Calendar, Briefs, Briefing Books, and secondary items (Analytics, Activity, Advanced Search, Availability Polling)'
    - 'Dossiers group contains 7 dossier types plus Engagements cross-link'
    - 'Administration group (admin-only) contains AI Settings, System Settings, Field Permissions, Audit Logs, Data Retention, Approvals'
    - 'Badge counts appear on Tasks, Approvals, and Engagements items only'
    - 'User avatar and name appear in sidebar header area'
    - 'All 8 dossier types are navigable under /dossiers/{type}/'
  artifacts:
    - path: 'frontend/src/components/layout/navigation-config.ts'
      provides: '3-group navigation config with NavigationGroup type'
      exports: ['createNavigationGroups', 'NavigationGroup', 'NavigationItem']
    - path: 'frontend/src/components/layout/nav-main.tsx'
      provides: 'Group-aware nav renderer with collapsible groups and secondary items'
      contains: 'NavigationGroup'
    - path: 'frontend/src/components/layout/AppSidebar.tsx'
      provides: 'Refactored sidebar using 3-group config with user avatar in header'
      contains: 'createNavigationGroups'
    - path: 'frontend/src/components/layout/nav-user.tsx'
      provides: 'User menu relocated to sidebar header area'
  key_links:
    - from: 'frontend/src/components/layout/AppSidebar.tsx'
      to: 'frontend/src/components/layout/navigation-config.ts'
      via: 'createNavigationGroups import'
      pattern: 'createNavigationGroups'
    - from: 'frontend/src/components/layout/nav-main.tsx'
      to: 'frontend/src/components/layout/navigation-config.ts'
      via: 'NavigationGroup type import'
      pattern: 'NavigationGroup'
    - from: 'frontend/src/components/layout/AppSidebar.tsx'
      to: 'frontend/src/hooks/useWorkQueueCounts'
      via: 'badge count data'
      pattern: 'useWorkQueueCounts'
---

<objective>
Restructure the app sidebar from the current 6-category flat layout to a hub-based 3-group sidebar (Operations, Dossiers, Administration) per user decisions D-01 through D-05. This is the foundational plan that establishes the navigation config consumed by subsequent plans (mobile tab bar, command palette).

Purpose: Replace the fragmented 6-section navigation with a clean mental model matching how diplomatic staff actually work — operations, dossiers, and administration.
Output: Refactored sidebar with 3 collapsible groups, badge counts on key items, user avatar in header area, and all dossier types linked under /dossiers/{type}/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-navigation-route-consolidation/08-CONTEXT.md
@.planning/phases/08-navigation-route-consolidation/08-RESEARCH.md

@frontend/src/components/layout/navigation-config.ts
@frontend/src/components/layout/AppSidebar.tsx
@frontend/src/components/layout/nav-main.tsx
@frontend/src/components/layout/nav-user.tsx
@frontend/src/components/layout/SiteHeader.tsx
@frontend/src/components/layout/MainLayout.tsx
@frontend/src/components/ui/sidebar.tsx
@frontend/src/hooks/useWorkQueueCounts.ts
@frontend/src/hooks/useDirection.ts
@frontend/src/hooks/useResponsive.ts
@frontend/src/lib/dossier-routes.ts

<interfaces>
<!-- Key types the executor needs from existing code -->

From frontend/src/components/ui/sidebar.tsx:

- SidebarGroup, SidebarGroupLabel, SidebarMenu, SidebarMenuButton, SidebarMenuItem
- SidebarMenuBadge, SidebarMenuSubButton, SidebarMenuSubItem
- SidebarHeader, SidebarContent, SidebarFooter, SidebarRail, SidebarSeparator
- Sidebar (root), SidebarInset, SidebarProvider, useSidebar

From frontend/src/components/layout/navigation-config.ts (CURRENT - to be replaced):

```typescript
export interface NavigationItem {
  id: string
  label: string
  path: string
  icon: LucideIcon
  badgeCount?: number
  adminOnly?: boolean
}
export interface NavigationSection {
  id: string
  label: string
  items: NavigationItem[]
}
export const createNavigationSections = (counts: { intake: number; waiting: number }, isAdmin: boolean): NavigationSection[]
```

From frontend/src/hooks/useWorkQueueCounts.ts:

```typescript
// Returns { data: { intake: number; waiting: number } }
export function useWorkQueueCounts(): UseQueryResult
```

From frontend/src/components/layout/nav-user.tsx:

- NavUser component currently rendered in SidebarFooter
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure navigation-config.ts from 6 categories to 3 groups</name>
  <files>frontend/src/components/layout/navigation-config.ts</files>
  <action>
Replace the entire `navigation-config.ts` with a new 3-group structure per D-01, D-02, D-03, D-04.

1. **Define new types** (replace existing `NavigationItem` and `NavigationSection`):

```typescript
export interface NavigationItem {
  id: string
  label: string
  path: string
  icon: LucideIcon
  badgeCount?: number
  secondary?: boolean // NEW: for standalone pages per D-02
}

export interface NavigationGroup {
  id: 'operations' | 'dossiers' | 'administration'
  label: string
  icon: LucideIcon
  items: NavigationItem[]
  collapsible?: boolean
  defaultOpen?: boolean
}
```

2. **Create `createNavigationGroups` function** (replaces `createNavigationSections`):

- Signature: `(counts: { tasks: number; approvals: number; engagements: number }, isAdmin: boolean) => NavigationGroup[]`
- Three groups per D-01:
  - **Operations** (`defaultOpen: true`): Dashboard, Engagements (with badge per D-04), After-Actions (own item per discretion — distinct workflow step), Tasks/Work Items (with badge per D-04), Calendar, Briefs, Briefing Books. Secondary items (per D-02): Analytics, Activity, Advanced Search, Availability Polling.
  - **Dossiers** (`defaultOpen: true`): Countries, Organizations, Persons, Forums, Topics, Working Groups, Engagements (cross-link per D-03). All paths under `/dossiers/{type}/`. Use 7 types (elected_official handled in Phase 12). No badges on dossier links per D-04.
  - **Administration** (`collapsible: true`, `defaultOpen: false`, admin-only): AI Settings (`/admin/ai-settings`), System Settings (`/admin/system`), Field Permissions (`/admin/field-permissions`), Audit Logs (`/audit-logs`), Data Retention (`/admin/data-retention`), Approvals (with badge per D-04).

3. **Keep backward-compat export** — export a thin `createNavigationSections` wrapper that maps groups to sections for any legacy consumers (avoids breaking imports during transition). Can be removed after Phase 8 is verified.

4. **Remove `bottomNavigationItems`** — Settings and Help will be handled differently (sidebar footer).

5. **Icons**: Use lucide-react icons. Import only what's needed. Operations group icon: `LayoutDashboard`. Dossiers: `FolderOpen`. Administration: `Settings`. Individual items use the icons from the research document's pattern (Briefcase for engagements, ClipboardList for after-actions, CheckSquare for tasks, CalendarDays for calendar, ScrollText for briefs, BookOpen for briefing-books, Globe for countries, Building2 for orgs, Users for persons, MessageSquare for forums, Tag for topics, UsersRound for working-groups, Sparkles for AI settings, Wrench for system, Shield for field-permissions, History for audit-logs, Database for data-retention, CheckSquare for approvals, TrendingUp for analytics, Activity for activity, Search for advanced-search, Clock for availability-polling).

6. **i18n keys**: Use `navigation.` prefix for all labels (e.g., `navigation.operations`, `navigation.dashboard`). These keys already exist in the common namespace.

Code style: no semicolons, single quotes, trailing commas, explicit return types, no `any`.
</action>
<verify>
<automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
</verify>
<done> - `createNavigationGroups` exported and returns 3 groups (2 for non-admin, 3 for admin) - `NavigationGroup` and `NavigationItem` types exported - Operations has 11 items (7 primary + 4 secondary), Dossiers has 7 items + engagements cross-link, Administration has 6 items - Badge counts on Tasks, Approvals, Engagements only (per D-04) - Engagements appears in both Operations and Dossiers (per D-03) - Secondary items flagged with `secondary: true` (per D-02) - TypeScript compiles without errors
</done>
</task>

<task type="auto">
  <name>Task 2: Refactor NavMain, AppSidebar, and NavUser for 3-group layout with user avatar in header</name>
  <files>frontend/src/components/layout/nav-main.tsx, frontend/src/components/layout/AppSidebar.tsx, frontend/src/components/layout/nav-user.tsx</files>
  <action>
**nav-main.tsx** — Update to render `NavigationGroup[]` instead of `NavigationSection[]`:

1. Change props from `sections: NavigationSection[]` to `groups: NavigationGroup[]`.
2. For each group, render a `SidebarGroup` with `SidebarGroupLabel`.
3. Groups with `collapsible: true` get wrapped in `Collapsible` + `CollapsibleTrigger`/`CollapsibleContent` (already done for admin — generalize this pattern to work with `group.collapsible` flag).
4. Use `group.defaultOpen` for `Collapsible defaultOpen` prop. Per discretion: Operations and Dossiers expanded, Administration collapsed.
5. Render `secondary` items with smaller/muted styling — use `SidebarMenuSubItem` + `SidebarMenuSubButton` for secondary items, `SidebarMenuButton` for primary items. Add a thin separator before secondary items within the Operations group.
6. Badge counts rendered via `SidebarMenuBadge` (existing pattern — already works).
7. Active state: Compare `pathname` with `item.path`. For dossiers, also check `pathname.startsWith(item.path)` to highlight parent when on detail pages.
8. RTL: Use `useDirection()` for chevron rotation direction on collapsible triggers. Existing pattern already handles this — keep it.
9. Import `NavigationGroup` type from `./navigation-config`.

**AppSidebar.tsx** — Update to use new config and move user to header:

1. Replace `createNavigationSections` import with `createNavigationGroups`.
2. Update `useMemo` to call `createNavigationGroups` with `{ tasks: workCounts?.tasks ?? 0, approvals: workCounts?.approvals ?? 0, engagements: workCounts?.engagements ?? 0 }` and `isAdmin`. Note: `useWorkQueueCounts` may need its return shape checked — if it only returns `intake`/`waiting`, pass those as `tasks`/`approvals` respectively and `0` for engagements until the hook is extended.
3. Pass `groups` (not `sections`) to `NavMain`.
4. **Move user avatar to header area per D-05**: In `SidebarHeader`, after the brand/logo `SidebarMenuItem`, add a small user avatar + name row. Import user data from `useAuth()`. Show user avatar (first letter fallback) + display name. This replaces the footer user menu position.
5. Remove `NavUser` from `SidebarFooter`. Keep `ThemeSelector` and `LanguageToggle` in footer.
6. Remove `SidebarSearch` and `QuickNavigationMenu` from sidebar content — these are being replaced by enhanced Cmd+K (Plan 04). Keep the sidebar clean with just groups.
7. Keep Settings and Help as footer items (below theme/language controls).

**nav-user.tsx** — Adapt for header placement:

1. Refactor `NavUser` to be a compact header-friendly component showing avatar (small circle with user initials or image) + truncated name.
2. On click/hover, show a dropdown with: Profile, Settings, Theme toggle, Language toggle, Sign Out.
3. Use `DropdownMenu` from existing UI components.
4. Ensure RTL: logical properties only (ms-_, me-_, text-start).
5. In icon-collapsed sidebar state (`group-data-[collapsible=icon]`), show only the avatar circle.

Code style: no semicolons, single quotes, trailing commas, explicit return types, no `any`.
</action>
<verify>
<automated>cd /Users/khalidalzahrani/Library/CloudStorage/OneDrive-Personal/coding/Intl-DossierV2.0/frontend && npx tsc --noEmit --pretty 2>&1 | head -30 && pnpm build 2>&1 | tail -5</automated>
</verify>
<done> - Sidebar renders 3 groups (Operations, Dossiers, Administration for admins; 2 groups for non-admins) - Operations and Dossiers default expanded, Administration collapsed - Secondary items (Analytics, Activity, Advanced Search, Availability Polling) render with muted/sub-item styling - Badge counts visible on Tasks, Approvals, Engagements - User avatar + name visible in sidebar header area (per D-05) - Engagements cross-linked in both Operations and Dossiers (per D-03) - All dossier types link to /dossiers/{type}/ paths - RTL layout works correctly (chevrons, alignment) - Build succeeds without errors
</done>
</task>

</tasks>

<verification>
1. `cd frontend && pnpm build` succeeds
2. `cd frontend && npx tsc --noEmit` passes
3. Manual check: Sidebar shows 3 groups in correct order
4. Manual check: Badge counts appear on Tasks, Approvals, Engagements
5. Manual check: User avatar visible in header area
6. Manual check: RTL layout correct (switch to Arabic)
7. Manual check: All 7 dossier type links navigate to /dossiers/{type}/
</verification>

<success_criteria>

- Sidebar displays 3 groups: Operations, Dossiers, Administration
- All items from D-01 are present in correct groups
- Badge counts on Tasks, Approvals, Engagements only
- User avatar in sidebar header area
- Engagements cross-linked in Operations + Dossiers
- Secondary items visually distinguished
- Build and TypeScript compilation pass
- RTL and LTR both render correctly
  </success_criteria>

<output>
After completion, create `.planning/phases/08-navigation-route-consolidation/08-01-SUMMARY.md`
</output>
