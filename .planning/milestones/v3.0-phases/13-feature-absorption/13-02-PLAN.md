---
phase: 13-feature-absorption
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/keyboard-shortcuts/CommandPalette.tsx
  - frontend/src/components/keyboard-shortcuts/KeyboardShortcutProvider.tsx
  - frontend/src/domains/dossiers/hooks/useQuickSwitcherSearch.ts
  - frontend/src/i18n/locales/en/quick-switcher.json
  - frontend/src/i18n/locales/ar/quick-switcher.json
autonomous: true
requirements:
  - ABSORB-03

must_haves:
  truths:
    - "User presses Cmd+K and sees 5 most recent dossiers + 5 most-used commands before typing anything"
    - "User types a query and sees results grouped as: Recent -> Entities (sub-grouped by dossier type) -> Commands -> Search Results"
    - "Cmd+K searches across all 8 dossier entity types, recent items, commands/actions, and global full-text search"
    - "Cmd+K completely replaces the standalone advanced search page"
  artifacts:
    - path: "frontend/src/components/keyboard-shortcuts/CommandPalette.tsx"
      provides: "Enhanced command palette with empty state, grouped entity results, full-text search"
      exports: ["CommandPalette"]
  key_links:
    - from: "frontend/src/components/keyboard-shortcuts/CommandPalette.tsx"
      to: "frontend/src/domains/dossiers/hooks/useQuickSwitcherSearch.ts"
      via: "useQuickSwitcherSearch for entity search data"
      pattern: "useQuickSwitcherSearch"
    - from: "frontend/src/components/keyboard-shortcuts/CommandPalette.tsx"
      to: "localStorage"
      via: "command usage frequency tracking for most-used commands"
      pattern: "localStorage.*commandUsage"
---

<objective>
Enhance the existing Cmd+K CommandPalette into a full-featured global quick switcher that replaces the standalone advanced search page entirely. Per D-01, search covers ALL four categories: all 8 dossier entity types, recent items, commands/actions, and global full-text search. Per D-02, results are organized in grouped categories: Recent -> Entities (sub-grouped by dossier type) -> Commands -> Search Results. Per D-03, empty state shows 5 most recent dossiers + 5 most-used commands. Per D-04, this completely replaces the standalone search page.

Purpose: Users navigate and search the entire application from a single Cmd+K entry point, eliminating the need for a separate search page.
Output: Enhanced CommandPalette.tsx with empty state, grouped entity results, full-text search results group, and command usage tracking.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-feature-absorption/13-CONTEXT.md
@.planning/phases/13-feature-absorption/13-RESEARCH.md
@.planning/phases/13-feature-absorption/13-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/components/keyboard-shortcuts/CommandPalette.tsx:
```typescript
// Existing command palette (cmdk-based, ~400+ lines)
// Already has: grouped results (Recents, Dossiers, Pages, Work Items, Commands)
// Already has: cache-first hybrid search via useQuickSwitcherSearch
// Already has: mobile full-screen mode
// Already has: quickActions array defining commands
// Needs: empty state enhancement (D-03), entity sub-grouping by type (D-02),
//        full-text search results group, command usage frequency tracking
export function CommandPalette({ className }: CommandPaletteProps): React.ReactElement | null
```

From frontend/src/components/ui/command.tsx:
```typescript
// cmdk UI primitive wrapper -- CommandDialog, CommandInput, CommandList,
// CommandEmpty, CommandGroup, CommandItem, CommandSeparator, CommandShortcut
```

From frontend/src/domains/dossiers/hooks/useQuickSwitcherSearch.ts:
```typescript
// Provides dossier + work item search with 200ms debounce
// cache-first hybrid strategy already implemented
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance CommandPalette empty state and grouped entity results</name>
  <files>
    frontend/src/components/keyboard-shortcuts/CommandPalette.tsx
    frontend/src/domains/dossiers/hooks/useQuickSwitcherSearch.ts
    frontend/src/i18n/locales/en/quick-switcher.json
    frontend/src/i18n/locales/ar/quick-switcher.json
  </files>
  <read_first>
    frontend/src/components/keyboard-shortcuts/CommandPalette.tsx
    frontend/src/components/keyboard-shortcuts/KeyboardShortcutProvider.tsx
    frontend/src/components/ui/command.tsx
    frontend/src/domains/dossiers/hooks/useQuickSwitcherSearch.ts
    frontend/src/hooks/useRecentNavigation.ts
    frontend/src/i18n/locales/en/quick-switcher.json
    frontend/src/i18n/locales/ar/quick-switcher.json
    frontend/src/pages/search/DossierSearchPage.tsx
  </read_first>
  <action>
    **Enhance `CommandPalette.tsx`** -- this is an EXTENSION of the existing component, not a rewrite. Preserve all existing functionality.

    **1. Empty state (D-03):** When search query is empty (trimmed length < 2, matching existing threshold):
    - Show "Quick Access" / "وصول سريع" heading
    - Show `<CommandGroup heading={t('groups.recent')}>` with 5 most recent dossiers from `useRecentNavigation` (or equivalent existing hook). Each item: Clock icon (me-2 h-4 w-4), entity title, onSelect navigates to entity path.
    - Show `<CommandSeparator />`
    - Show `<CommandGroup heading={t('groups.commands')}>` with 5 most-used commands. Track usage in localStorage key `'cmdkUsageFrequency'` as JSON object `{ [commandId: string]: number }`. Increment count each time a command is executed via onSelect. Sort commands by usage count descending. Fallback to first 5 from existing `quickActions` array when no usage data exists. Each item: command icon (me-2 h-4 w-4), command label, optional shortcut via CommandShortcut.
    - Empty state replaces current results display. When user starts typing, results replace empty state.

    **2. Grouped entity results (D-02):** When search query has results:
    - Group order: Recent -> Entities (sub-grouped by dossier type) -> Commands -> Search Results
    - Entity sub-groups: Create separate `<CommandGroup>` for each dossier type that has results. Heading is the dossier type name (translated). Types: Countries, Organizations, Forums, Engagements, Topics, Working Groups, Persons, Elected Officials.
    - Only render CommandGroup if it has items (per Research pitfall 3: `{items.length > 0 && <CommandGroup>...}`).
    - Limit: 5 results per entity type sub-group, 5 commands, 10 search results (per UI-SPEC interaction contracts).
    - Use `<CommandSeparator />` between major groups (Recent, Entities block, Commands, Search Results).

    **3. Full-text search results group:** After entity results and commands, show a "Search Results" group with full-text matches from the existing search hook. If the existing `useQuickSwitcherSearch` does not support full-text search across all content, extend it to include a backend search call that returns results from all entity types. The group heading is "Search Results" / "نتائج البحث".

    **4. Command usage tracking:** Create helper functions inside CommandPalette or as a separate utility:
    - `getCommandUsageCounts(): Record<string, number>` -- reads from localStorage `'cmdkUsageFrequency'`
    - `incrementCommandUsage(commandId: string): void` -- increments count in localStorage
    - `getMostUsedCommands(commands: QuickAction[], limit: number): QuickAction[]` -- sorts by usage, returns top N
    - Call `incrementCommandUsage` in existing command onSelect handlers.

    **5. "No results" state:** When search returns nothing: show "No results found. Try a different search." / "لم يتم العثور على نتائج. جرب بحثا مختلفا."

    **6. Keyboard hint:** Add subtle hint text below the input: "Navigate with arrow keys, Enter to select, Esc to close" / "تنقل بمفاتيح الاسهم، Enter للاختيار، Esc للاغلاق" -- styled with text-xs text-muted-foreground.

    **Update i18n** (EN quick-switcher.json):
    ```json
    {
      "placeholder": "Search dossiers, commands...",
      "quickAccess": "Quick Access",
      "groups": {
        "recent": "Recent",
        "commands": "Commands",
        "searchResults": "Search Results",
        "countries": "Countries",
        "organizations": "Organizations",
        "forums": "Forums",
        "engagements": "Engagements",
        "topics": "Topics",
        "workingGroups": "Working Groups",
        "persons": "Persons",
        "electedOfficials": "Elected Officials"
      },
      "noResults": "No results found. Try a different search.",
      "keyboardHint": "Navigate with arrow keys, Enter to select, Esc to close"
    }
    ```

    **Update i18n** (AR quick-switcher.json):
    ```json
    {
      "placeholder": "ابحث في الملفات، الاوامر...",
      "quickAccess": "وصول سريع",
      "groups": {
        "recent": "الاخيرة",
        "commands": "الاوامر",
        "searchResults": "نتائج البحث",
        "countries": "الدول",
        "organizations": "المنظمات",
        "forums": "المنتديات",
        "engagements": "الارتباطات",
        "topics": "المواضيع",
        "workingGroups": "مجموعات العمل",
        "persons": "الاشخاص",
        "electedOfficials": "المسؤولون المنتخبون"
      },
      "noResults": "لم يتم العثور على نتائج. جرب بحثا مختلفا.",
      "keyboardHint": "تنقل بمفاتيح الاسهم، Enter للاختيار، Esc للاغلاق"
    }
    ```

    **Cmd+K group heading styling** per UI-SPEC: text-xs (11px), font-semibold (600), uppercase, tracking-wider (0.05em). This may already be styled by cmdk/command.tsx -- verify and adjust if needed.

    **RTL compliance:** All spacing uses logical properties (me-2, ms-2, ps-*, pe-*). No physical left/right. Use `text-start` for alignment.
  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/Coding\ Space/Intl-Dossier-V2.0 && pnpm exec tsc --noEmit --project frontend/tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CommandPalette.tsx contains `cmdkUsageFrequency` or equivalent localStorage key for command tracking
    - CommandPalette.tsx contains `getCommandUsageCounts` or `getMostUsedCommands` function/logic
    - CommandPalette.tsx contains at least 8 dossier type group references (countries, organizations, forums, etc.)
    - CommandPalette.tsx contains `CommandSeparator` between major groups
    - CommandPalette.tsx conditionally renders groups only when items exist (pattern: `.length > 0 &&` or similar)
    - CommandPalette.tsx contains `slice(0, 5)` for recent items limit and commands limit
    - quick-switcher.json (EN) contains `"quickAccess": "Quick Access"`
    - quick-switcher.json (AR) contains `"quickAccess": "وصول سريع"`
    - quick-switcher.json (EN) contains `"noResults": "No results found. Try a different search."`
    - quick-switcher.json (AR) contains `"noResults": "لم يتم العثور على نتائج. جرب بحثا مختلفا."`
    - No `ml-`, `mr-`, `pl-`, `pr-`, `text-left`, `text-right` in modified sections
    - CommandPalette.tsx contains `keyboardHint` or equivalent hint text
  </acceptance_criteria>
  <done>Cmd+K shows empty state with 5 recent dossiers + 5 most-used commands before typing. Typing shows grouped results: Recent -> Entities (sub-grouped by 8 dossier types) -> Commands -> Search Results. Command usage tracked in localStorage. No results state and keyboard hint displayed. Full bilingual support.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes: `cd frontend && pnpm exec tsc --noEmit`
- CommandPalette renders empty state when opened without typing
- Typing produces grouped results with entity sub-groups by dossier type
- Command usage frequency persists in localStorage across sessions
- No physical CSS properties in modified code
- i18n complete for both EN and AR
</verification>

<success_criteria>
- Cmd+K empty state shows 5 recent dossiers + 5 most-used commands immediately on open
- Search results grouped: Recent -> Entities (8 type sub-groups) -> Commands -> Search Results
- Result limits enforced: 5 per entity type, 5 commands, 10 search results
- No results and keyboard hint states render correctly
- RTL compliant, bilingual
</success_criteria>

<output>
After completion, create `.planning/phases/13-feature-absorption/13-02-SUMMARY.md`
</output>
