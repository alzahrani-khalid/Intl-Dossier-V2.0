---
phase: quick-260605-u2z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/i18n/en/admin.json
  - frontend/src/i18n/ar/admin.json
  - frontend/src/i18n/en/ai-admin.json
  - frontend/src/i18n/ar/ai-admin.json
  - frontend/src/i18n/en/ai-brief.json
  - frontend/src/i18n/ar/ai-brief.json
  - frontend/src/i18n/en/ai-chat.json
  - frontend/src/i18n/ar/ai-chat.json
  - frontend/src/i18n/en/dossier-context.json
  - frontend/src/i18n/ar/dossier-context.json
  - frontend/src/i18n/en/entity-linking.json
  - frontend/src/i18n/ar/entity-linking.json
  - frontend/src/i18n/en/graph.json
  - frontend/src/i18n/ar/graph.json
  - frontend/src/i18n/en/preview-layouts.json
  - frontend/src/i18n/ar/preview-layouts.json
  - frontend/src/i18n/index.ts
autonomous: true
requirements:
  - QUICK-260605-u2z
must_haves:
  truths:
    - "useTranslation('dossier-context') in DossierActivityTimeline.tsx resolves the Arabic timeline heading (not the inline English default) when language is ar"
    - 'All 11 previously-unregistered namespaces resolve their Arabic strings at runtime in both en and ar'
    - 'Existing 100+ registered namespaces still resolve unchanged'
  artifacts:
    - path: 'frontend/src/i18n/en/dossier-context.json'
      provides: 'English dossier-context strings copied from public/locales'
    - path: 'frontend/src/i18n/ar/dossier-context.json'
      provides: 'Arabic dossier-context strings copied from public/locales'
    - path: 'frontend/src/i18n/index.ts'
      provides: 'Static import + resources registration for all 11 namespaces'
      contains: "'dossier-context'"
  key_links:
    - from: 'frontend/src/i18n/index.ts'
      to: 'frontend/src/i18n/{en,ar}/dossier-context.json'
      via: 'static import + resources.{en,ar} entry'
      pattern: "'dossier-context':"
---

<objective>
Register 11 referenced-but-unregistered i18n namespaces in the frontend's static `resources` object so their Arabic strings resolve at runtime instead of silently falling back to inline English defaults in BOTH languages.

Purpose: `frontend/src/i18n/index.ts` bundles every namespace STATICALLY into a `resources` object (no i18next-http-backend, so `public/locales/**` JSON is dead at runtime). Any namespace referenced via `useTranslation('X')` but absent from `resources` falls back to the English default string even when the user is in Arabic. This is what made the dossier activity-timeline heading stay English in Arabic mode.

Output: 8 copied JSON files (Group A) plus import + resource-key edits in `index.ts` covering all 11 namespaces (Group A + Group B).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/i18n/index.ts

<interfaces>
<!-- The exact existing pattern to mirror (from the tail of index.ts). -->
<!-- Imports at top of file: -->
import enTasksPage from './en/tasks-page.json'
import arTasksPage from './ar/tasks-page.json'

<!-- Inside resources.en: -->

'tasks-page': enTasksPage,

<!-- Inside resources.ar: -->

'tasks-page': arTasksPage,

<!-- Import identifiers are camelCase derived from the namespace; resource KEYS are the exact hyphenated namespace strings. -->
</interfaces>

Namespace inventory (verified against disk):

GROUP A — JSON exists only in `frontend/public/locales/{en,ar}/<ns>.json`; MISSING from `frontend/src/i18n/{en,ar}/`. Must be copied into src first, then imported + registered:
| namespace | import id (en / ar) |
| --- | --- |
| admin | enAdmin / arAdmin |
| ai-admin | enAiAdmin / arAiAdmin |
| ai-brief | enAiBrief / arAiBrief |
| ai-chat | enAiChat / arAiChat |
| dossier-context | enDossierContext / arDossierContext |
| entity-linking | enEntityLinking / arEntityLinking |
| graph | enGraph / arGraph |
| preview-layouts | enPreviewLayouts / arPreviewLayouts |

GROUP B — JSON ALREADY present in `frontend/src/i18n/{en,ar}/<ns>.json`. Add import + resource entry only (no copy):
| namespace | import id (en / ar) |
| --- | --- |
| geographic-visualization | enGeographicVisualization / arGeographicVisualization |
| push-notifications | enPushNotifications / arPushNotifications |
| retention-policies | enRetentionPolicies / arRetentionPolicies |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy the 8 Group A JSON files from public/locales into src/i18n</name>
  <files>
    frontend/src/i18n/en/admin.json, frontend/src/i18n/ar/admin.json,
    frontend/src/i18n/en/ai-admin.json, frontend/src/i18n/ar/ai-admin.json,
    frontend/src/i18n/en/ai-brief.json, frontend/src/i18n/ar/ai-brief.json,
    frontend/src/i18n/en/ai-chat.json, frontend/src/i18n/ar/ai-chat.json,
    frontend/src/i18n/en/dossier-context.json, frontend/src/i18n/ar/dossier-context.json,
    frontend/src/i18n/en/entity-linking.json, frontend/src/i18n/ar/entity-linking.json,
    frontend/src/i18n/en/graph.json, frontend/src/i18n/ar/graph.json,
    frontend/src/i18n/en/preview-layouts.json, frontend/src/i18n/ar/preview-layouts.json
  </files>
  <action>
    Copy each of the 8 Group A namespaces from `frontend/public/locales/{lang}/<ns>.json` to `frontend/src/i18n/{lang}/<ns>.json` for both `en` and `ar` (16 files total). Use a byte-for-byte copy (e.g. `cp`) — do NOT edit, reformat, or alter the CONTENTS of any translation file. Leave the `public/locales/` originals untouched (out of scope). The Group B namespaces (geographic-visualization, push-notifications, retention-policies) are NOT copied — their src/i18n files already exist and are handled in Task 2.
  </action>
  <verify>
    <automated>for ns in admin ai-admin ai-brief ai-chat dossier-context entity-linking graph preview-layouts; do for lang in en ar; do test -f "frontend/src/i18n/$lang/$ns.json" || { echo "MISSING $lang/$ns.json"; exit 1; }; done; done && echo "all 16 present"</automated>
  </verify>
  <done>All 16 Group A JSON files exist under frontend/src/i18n/{en,ar}/ and are byte-identical to their public/locales counterparts.</done>
</task>

<task type="auto">
  <name>Task 2: Register all 11 namespaces in index.ts (imports + resources.en + resources.ar)</name>
  <files>frontend/src/i18n/index.ts</files>
  <action>
    Edit `frontend/src/i18n/index.ts` to register all 11 namespaces (8 Group A + 3 Group B), mirroring the existing static-import + resources pattern exactly. Follow the prettier config in CLAUDE.md: single quotes, NO semicolons, 2-space indent, trailing comma `all`.

    1. Imports: After the last existing import block (the `briefs-page` / `after-actions-page` / `tasks-page` imports near line 219), add `import en<Id> from './en/<ns>.json'` and `import ar<Id> from './ar/<ns>.json'` for each of the 11 namespaces, using the camelCase import identifiers from the context table (e.g. `enAiBrief`/`arAiBrief`, `enDossierContext`/`arDossierContext`, `enGeographicVisualization`/`arGeographicVisualization`).

    2. resources.en: Inside the `en` object (before its closing brace near line 335), add one entry per namespace keyed by the EXACT hyphenated namespace string mapped to the en import id — e.g. `'dossier-context': enDossierContext,`, `'ai-brief': enAiBrief,`, `admin: enAdmin,`, `graph: enGraph,`, `'geographic-visualization': enGeographicVisualization,`. Note `admin` and `graph` are single words so their keys need no quotes (match existing unquoted-key style like `dashboard:` / `compliance:`); all hyphenated keys must be quoted.

    3. resources.ar: Inside the `ar` object (before its closing brace near line 445), add the identical 11 keys mapped to the `ar` import ids.

    Do not modify any other file, any component, or any existing namespace registration. Do not touch the i18n `.init()` config.

  </action>
  <verify>
    <automated>cd frontend && for ns in admin ai-admin ai-brief ai-chat dossier-context entity-linking graph preview-layouts geographic-visualization push-notifications retention-policies; do c=$(grep -c "'\?$ns'\?:" src/i18n/index.ts); test "$c" -ge 2 || { echo "FAIL $ns count=$c"; exit 1; }; done && pnpm typecheck</automated>
  </verify>
  <done>`pnpm --filter frontend typecheck` passes; each of the 11 namespace keys appears in both resources.en and resources.ar (≥2 occurrences each); useTranslation('dossier-context') now resolves Arabic strings.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm typecheck` exits 0 (resolveJsonModule already enabled — proven by the existing 100+ JSON imports).
- All 11 namespaces appear in both `resources.en` and `resources.ar`.
- The 8 Group A namespaces now have JSON under `frontend/src/i18n/{en,ar}/`.
- No file other than `index.ts` and the 16 copied Group A JSON files is modified; `public/locales/` is untouched.
</verification>

<success_criteria>

- 11 previously-unregistered namespaces resolve their Arabic strings at runtime in `ar`, English in `en`.
- The dossier activity-timeline heading (`useTranslation('dossier-context')` in DossierActivityTimeline.tsx) renders Arabic in Arabic mode.
- Existing registered namespaces are unchanged; typecheck is green.
  </success_criteria>

<output>
Create `.planning/quick/260605-u2z-register-11-unregistered-i18n-namespaces/260605-u2z-SUMMARY.md` when done.
</output>
