---
quick_id: 260529-1s8
title: Fix confirmed data-entry bugs (forms, validation, write paths)
status: in_progress
created: 2026-05-29
mode: quick
source: 6-agent data-entry audit + live-DB verification (staging zkrcjzdemdmwhearhfgg)
---

# Quick Task 260529-1s8 — Data-entry bug fixes

Fix the confirmed, DB-verified data-entry defects. Scope = bugs only. Security
hardening (#9), `dossiers-update` rewrite (#4), and DB-schema migrations are the
NEXT checkpoint and are explicitly out of scope here.

Grouped into atomic commits. Each frontend commit must pass `pnpm build`
(pre-commit hook builds every commit).

## Task 1 — Broken dossier-type submit paths

- **Files:** `supabase/functions/dossiers-create/index.ts`,
  `frontend/src/components/dossier/wizard/config/forum.config.ts`,
  `.../schemas/forum.schema.ts`, `.../steps/ForumDetailsStep.tsx`,
  `.../review/ForumReviewStep.tsx`, `.../defaults/index.ts`,
  `.../config/working-group.config.ts`, `.../schemas/working-group.schema.ts`,
  `.../steps/WorkingGroupDetailsStep.tsx`, plus the 2 forum wizard tests.
- **Action:**
  1. `extensionTableMap.engagement`: `'engagements'` → `'engagement_dossiers'`
     (verified: engagement_dossiers CHECK enums + start_date/end_date NOT NULL
     match the wizard exactly; engagements is a separate legacy table). Add
     start_date/end_date to `TYPES_WITH_REQUIRED_EXTENSION.engagement` for a
     clean 400 instead of a 500 rollback.
  2. Add `abbreviation` to `DossierCreateRequest` + the dossiers insert
     (column exists, CHECK ≤20). Validate length ≤20.
  3. Working group: config key `status` → `wg_status`; align schema + step enum
     to DB CHECK `{active, suspended, disbanded}`.
  4. Forum: remove orphaned `forum_type` (no DB column; DB work deferred) from
     schema/config/UI/review/defaults + its 2 tests. Flag for re-add with a
     `forums.forum_type` column at the DB checkpoint.
- **Verify:** typecheck + build pass; engagement/forum/working-group create no
  longer rollback (manual reasoning against verified schema; live create test
  after deploy).
- **Done:** edge fn changed + redeployed; wizard configs/schemas/UI consistent.

## Task 2 — Intake submit

- **Files:** `frontend/src/components/work-creation/forms/IntakeQuickForm.tsx`,
  `frontend/src/domains/intake/hooks/useIntakeApi.ts` (+ create hook).
- **Action:** route IntakeQuickForm through `useCreateTicket()` (fixes 404 +
  camelCase payload); map snake_case `ticket_number` → `ticketNumber` in the
  create response so the success dialog shows the number.
- **Verify:** typecheck + build; IntakeQuickForm posts to `intake-tickets-create`
  with snake_case; success surfaces ticket number.

## Task 3 — Validation-message i18n leaks

- **Files:** `frontend/src/components/ui/form.tsx`,
  `.../tasks/TaskEditDialog.tsx`, `.../contacts/InteractionNoteForm.tsx`,
  `.../legislation/LegislationForm.tsx`, `.../commitments/CommitmentForm.tsx`,
  `.../type-specific-fields/TypeSpecificFields.tsx`, locale JSONs (en+ar).
- **Action:** translate `error.message` in `FormMessage` via
  `t(body, { defaultValue: body })`; repoint TaskEditDialog keys to existing
  `tasks.*` keys + add any missing en+ar; replace hardcoded English zod messages
  with i18n keys; fix CommitmentForm namespace; translate the 2 hardcoded
  `<option>` labels.
- **Verify:** typecheck + build; no raw `validation.*` keys render; Arabic shows
  Arabic.

## Task 4 — DB-required fields optional in forms

- **Files:** `.../intake-form/IntakeForm.tsx`,
  `.../dossier/wizard/schemas/topic.schema.ts`, `.../schemas/country.schema.ts`,
  `.../schemas/organization.schema.ts`, `.../wizard/SharedBasicInfoStep.tsx`.
- **Action:** make intake `title_ar`/`description_ar` required (NOT NULL);
  `theme_category` (topic), `iso_code_2/3` (country), `org_type` (org) required
  client-side; add string↔array transform for `tags`.
- **Verify:** typecheck + build; forms block before submit instead of bouncing
  off NOT-NULL / guard errors.

## Task 5 — Silent-failure write handlers

- **Files:** `frontend/src/hooks/useForums.ts`,
  `frontend/src/services/preference-sync.ts` (usePreferenceSync),
  `frontend/src/components/notifications/PushOptInBanner.tsx`.
- **Action:** useForums — throw + roll back the parent dossier on extension
  failure (mirror useWorkingGroups); usePreferenceSync — surface remote upsert
  failure; PushOptInBanner — targeted `update` keyed on user_id, not a partial
  upsert that clobbers theme/language.
- **Verify:** typecheck + build; no swallowed write errors; no row clobber.

## Task 6 — Glossary carve-out doc

- **Files:** `CLAUDE.md` (Work Management Terminology section).
- **Action:** add concise carve-out: intake `status` lifecycle, commitment
  `owner_*`/`due_date`, task `sla_deadline` legitimately differ from the work
  glossary and match the DB — do NOT rename.
- **Verify:** doc reads clearly; no code impact.

## NOT in scope (next checkpoint)

Security/mass-assignment whitelists + authz (#9), delete dead countries+orgs
paths, `dossiers-update` rewrite (#4), `aa_commitments.dossier_id` FK + other
migrations, backend validation gaps, duplicate person-creation retirement.
Do NOT change IntakeForm urgency `critical` (urgency_level enum, verified).
