---
phase: quick-260616-l4e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/dossier/DossierTabNav.tsx
  - frontend/src/i18n/en/dossier-shell.json
  - frontend/src/i18n/ar/dossier-shell.json
  - frontend/src/pages/intelligence/IntelligencePage.tsx
  - frontend/src/components/intelligence/GenerateDigestButton.tsx
  - frontend/src/i18n/en/intelligence-digests.json
  - frontend/src/i18n/ar/intelligence-digests.json
  - frontend/src/components/intelligence/DigestsTab.tsx
  - frontend/src/components/intelligence/DigestSubscribeDrawer.tsx
  - frontend/src/components/intelligence/DigestCard.tsx
  - frontend/src/components/intelligence/DigestReader.tsx
  - frontend/src/components/intelligence/AlertsTab.tsx
  - frontend/src/components/intelligence/AlertRuleForm.tsx
  - frontend/src/components/intelligence/AlertRuleRow.tsx
  - frontend/src/i18n/en/intelligence-alerts.json
  - frontend/src/i18n/ar/intelligence-alerts.json
  - frontend/src/components/ui/dialog.tsx
autonomous: true
requirements: [DIGEST-01, DIGEST-02, ALERT-01]

must_haves:
  truths:
    - "A 'Digests' tab is reachable from every dossier shell (all 8 types), closing D-12 entry-point contract"
    - 'The intelligence hub tab strip uses the accent-underline .tab pattern, not filled Button toggles'
    - 'Generate digest opens a 720px end-edge drawer with a Daily/Weekly/Monthly period picker that does not render off-screen'
    - 'All P70 component text uses --t-body/--t-meta tokens, not raw text-sm/text-xs'
    - 'DigestReader footer offers an Unsubscribe ghost link; the dialog close button has a bilingual accessible name'
    - 'Build and typecheck stay green; EN+AR keys present for every new label'
  artifacts:
    - path: 'frontend/src/components/dossier/DossierTabNav.tsx'
      provides: 'digests entry in BASE_DOSSIER_TABS'
      contains: "labelKey: 'tabs.digests'"
    - path: 'frontend/src/components/intelligence/GenerateDigestButton.tsx'
      provides: '720px HITL drawer with period picker'
      contains: "translate: 'none'"
    - path: 'frontend/src/i18n/ar/dossier-shell.json'
      provides: 'Arabic Digests tab label'
      contains: 'الملخصات'
  key_links:
    - from: 'frontend/src/components/dossier/DossierTabNav.tsx'
      to: 'per-dossier /digests route'
      via: "BASE_DOSSIER_TABS path 'digests'"
      pattern: "path: 'digests'"
    - from: 'frontend/src/pages/intelligence/IntelligencePage.tsx'
      to: 'frontend/src/styles/list-pages.css .tab'
      via: 'className tab / tab active'
      pattern: "className=.*\\btab\\b"
---

<objective>
Close the remaining 8 open findings from `70-UI-REVIEW.md` (overall 16/24) for Phase 70
(Digests & Alerts). The two runtime bugs (off-screen drawers, Arabic Tajawal on `.btn-*`)
were already fixed in quick 260616-hv6 and are OUT OF SCOPE. This plan delivers: the
per-dossier Digests tab (D-12 blocker), the hub tab-strip migration to the design-system
`.tab` pattern, the full HITL 720px drawer rebuild (locked decision), a design-token /
spacing / color sweep across the 8 P70 components, and the smaller spec/a11y gaps
(DigestReader unsubscribe link, specific edit label, bilingual dialog close label).

Purpose: bring Phase 70 UI into full compliance with `70-UI-SPEC.md` and CLAUDE.md design rules.
Output: 8 modified components + 6 modified i18n JSON files, build/typecheck green.

ALL changes obey CLAUDE.md: design tokens only (no raw hex, no Tailwind color literals like
`text-blue-500`); logical properties only (`ps/pe/ms/me`, `text-start`); no card drop-shadows;
buttons use `.btn-primary`/`.btn-ghost` (the existing `Button` variants); row heights
`var(--row-h)`; no emoji; no marketing voice; sentence case. Every new i18n key is added in
BOTH `en/` and `ar/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/70-digests-alerts/70-UI-SPEC.md
@.planning/phases/70-digests-alerts/70-UI-REVIEW.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — no exploration required. -->

From frontend/src/components/dossier/DossierTabNav.tsx (existing pattern):

- `interface DossierTabConfig { key: string; labelKey: string; path: string }`
- `BASE_DOSSIER_TABS: DossierTabConfig[]` — last entry is `{ key: 'signals', labelKey: 'tabs.signals', path: 'signals' }`
- Labels resolve via `useTranslation('dossier-shell')` → `t(tab.labelKey)` (namespace `dossier-shell`, keys under `tabs.*`)

From frontend/src/domains/signals/hooks/useDigests.ts:

- `export type DigestFrequency = 'daily' | 'weekly' | 'monthly'`
- `export interface Digest { dossier_id: string; frequency: DigestFrequency | string; ... }`
- `export function useUnsubscribeFromDigest()` → `.mutate({ dossierId })`
- `export const digestKeys` with `digestKeys.all`

From frontend/src/styles/list-pages.css (DO NOT EDIT — reference only):

- `.tabs { display:flex; gap:24px; border-bottom:1px solid var(--line); background:var(--surface) }`
- `.tab { padding:10px 0; font-size:13.5px; font-weight:500; color:var(--ink-mute); background:transparent; border:0; border-bottom:2px solid transparent; cursor:pointer; margin-bottom:-1px; white-space:nowrap }`
- `.tab:hover { color:var(--ink) }`
- `.tab.active, .tab[aria-selected='true'] { color:var(--accent); border-bottom-color:var(--accent); font-weight:600 }`

From frontend/src/components/intelligence/DigestSubscribeDrawer.tsx (CORRECT drawer pattern to mirror — both transform AND translate 'none'):

- `<DialogContent style={{ width:'min(100vw, 480px)', height:'100dvh', insetInlineEnd:0, insetInlineStart:'auto', insetBlockStart:0, insetBlockEnd:0, transform:'none', translate:'none', boxShadow:'var(--shadow-lg)' }} dir={isRTL?'rtl':'ltr'}>`
- `className="max-w-none rounded-none ps-4 pe-4 sm:rounded-none sm:ps-6 sm:pe-6"`

From frontend/src/i18n common namespace (already bilingual, no new key needed):

- `common.close` → EN "Close" / AR "إغلاق"; resolve with `useTranslation('common')` + `t('common.close', { defaultValue: 'Close' })`
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Per-dossier Digests tab + dossier-shell i18n (closes D-12 blocker)</name>
  <files>frontend/src/components/dossier/DossierTabNav.tsx, frontend/src/i18n/en/dossier-shell.json, frontend/src/i18n/ar/dossier-shell.json</files>
  <action>
In `DossierTabNav.tsx`, add `{ key: 'digests', labelKey: 'tabs.digests', path: 'digests' }`
to the `BASE_DOSSIER_TABS` array immediately AFTER the `signals` entry (line 41). This makes
the already-existing per-dossier `/digests` child routes (present for all 8 types, which
pass `dossierId` to `<DigestsTab dossierId=...>`) reachable from the dossier shell —
satisfying CONTEXT D-12 ("entry point = from the dossier"). Do NOT change any other tab,
the `audit` comment, or the component logic.

Add the `digests` label under the `tabs` object in BOTH i18n files:

- `en/dossier-shell.json` `tabs`: `"digests": "Digests"` (add after the `"signals"` line, keep
  trailing comma valid — place it before `"ariaLabel"`).
- `ar/dossier-shell.json` `tabs`: `"digests": "الملخصات"` (same position).

Do NOT add a hub dossier-picker. The hub `DigestsTab` (no `dossierId`) stays a read-only
management view per D-12; subscribe works only in the per-dossier route where `dossierId`
is supplied — that is correct and intentional.
</action>
<verify>
<automated>cd frontend && grep -q "labelKey: 'tabs.digests'" src/components/dossier/DossierTabNav.tsx && node -e "const e=require('./src/i18n/en/dossier-shell.json'),a=require('./src/i18n/ar/dossier-shell.json'); if(e.tabs.digests!=='Digests'||a.tabs.digests!=='الملخصات'){process.exit(1)}" && echo OK</automated>
</verify>
<done>BASE_DOSSIER_TABS contains a `digests` entry; `tabs.digests` resolves to "Digests" (EN) and "الملخصات" (AR); both JSON files parse.</done>
</task>

<task type="auto">
  <name>Task 2: Hub tab strip → design-system .tab accent-underline pattern</name>
  <files>frontend/src/pages/intelligence/IntelligencePage.tsx</files>
  <action>
Replace the 4 hub tab `<Button variant={activeTab===...?'default':'outline'} size="sm">`
toggles (lines ~407-436, Reports / Signals / Digests / Alerts) with the design-system
`.tab` pattern from `list-pages.css` (the same CSS `DossierTabNav` uses — DO NOT edit the CSS).

Wrap the four tabs in a `<nav role="tablist" className="tabs mb-4" dir={isRTL?'rtl':'ltr'}>`
containing four stateful `<button type="button">` elements (these call `setActiveTab`, they
are NOT router links — keep them as buttons). Each button:

- `className` = `cn('tab outline-none focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[var(--accent)]', activeTab===KEY && 'active')`
  (import `cn` from `@/lib/utils` if not already imported).
- `role="tab"`, `aria-selected={activeTab===KEY}`, `onClick={() => setActiveTab(KEY)}`.
- Keep the existing label calls verbatim: Reports = `t('intelligence.tabs.reports', { defaultValue: 'Reports' })`,
  Signals = `tSignals('tab.label')`, Digests = `tDigests('tab.label')`, Alerts = `tAlerts('tab.label')`.
- Preserve the existing EN/LTR order Reports | Signals | Digests | Alerts (RTL mirrors via `dir`).

Replace the old wrapper `<div className="flex gap-2 mb-4">` entirely. Do NOT touch the
pre-existing Reports-section dashboard cards/KPIs below (the `text-muted-foreground`,
`bg-accent/10`, `text-2xl` markup) — those are pre-P70 and explicitly out of scope.
</action>
<verify>
<automated>cd frontend && grep -qE 'role="tablist"' src/pages/intelligence/IntelligencePage.tsx && grep -c "variant={activeTab" src/pages/intelligence/IntelligencePage.tsx | grep -qx 0 && pnpm exec tsc --noEmit --pretty false -p tsconfig.json 2>&1 | grep -i "IntelligencePage" | grep -v '^#' | grep -c error | grep -qx 0 && echo OK</automated>
</verify>
<done>The 4 hub tabs render as `role="tab"` buttons inside a `.tabs` tablist; zero `variant={activeTab` toggles remain; IntelligencePage has no new tsc errors; active tab shows the accent underline (.active), not an accent background fill.</done>
</task>

<task type="auto">
  <name>Task 3: HITL "Generate digest" — full 720px drawer rebuild with period picker</name>
  <files>frontend/src/components/intelligence/GenerateDigestButton.tsx, frontend/src/i18n/en/intelligence-digests.json, frontend/src/i18n/ar/intelligence-digests.json</files>
  <action>
Rebuild `GenerateDigestButton.tsx` to match `70-UI-SPEC.md` Surface 5 as a 720px end-edge
DRAWER, replacing the current inline 520px card. Keep the component's props unchanged
(`dossierId: string`, `period: DigestFrequency`) so the 3 existing call sites in
`DigestsTab.tsx` still work — `period` becomes the picker's INITIAL value, not a fixed value.

Structure:

- Trigger: a `Button size="sm"` labeled `t('action.generateNow')` that sets `open` state true
  (opens the drawer). Keep its loading spinner behavior for the generate mutation.
- Drawer: build on the SAME corrected `<DialogContent>` pattern as `DigestSubscribeDrawer`/`AlertRuleForm`.
  Import `Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter` from `@/components/ui/dialog`.
  Inline `style` MUST include BOTH `transform: 'none'` AND `translate: 'none'` plus:
  `width: 'min(100vw, 720px)'`, `height: '100dvh'`, `insetInlineEnd: 0`, `insetInlineStart: 'auto'`,
  `insetBlockStart: 0`, `insetBlockEnd: 0`, `boxShadow: 'var(--shadow-lg)'`. Set `dir={isRTL?'rtl':'ltr'}`
  and `className="max-w-none rounded-none ps-4 pe-4 sm:rounded-none sm:ps-6 sm:pe-6"`.
  This is REQUIRED so the drawer does NOT reintroduce the off-screen bug fixed in 260616-hv6.
- Step 1 — Period picker: a `role="radiogroup"` of three radio chips (Daily / Weekly / Monthly),
  mirroring the chip markup already in `DigestSubscribeDrawer` (selected = `border-[var(--accent)]
bg-accent-soft text-accent-ink`, unselected = `border-line bg-surface text-ink-mute hover:bg-line-soft`,
  chip text class `[font-size:var(--t-body)]`). Local `pickedPeriod` state defaults to the `period`
  prop. Labels via `t('frequency.daily'|'weekly'|'monthly')` (keys already exist).
- Step 2 — Preview: a "Generate preview" action runs the EXISTING `generate_digest` RPC mutation,
  now passing `p_period: pickedPeriod`. Render the rollup-count strip (Signals / Engagements /
  Commitments — preserve `countPreviewItems` and the 3-cell `--t-kpi-value` grid markup from the
  current file). Keep the `t('preview.banner')` warning banner.
- Footer: Discard (ghost, resets preview + closes) + Publish (`publish_digest` with
  `p_period: pickedPeriod`, preserving `renderSummaryText`, `p_clearance_level_at_generation`, and
  the `digestKeys.all` invalidation `onSuccess`). Keep both error banners (`error.generate`,
  `error.publish`) with `role="alert"`.

Preserve all existing mutation logic, `useQueryClient`, and `digestKeys` invalidation. Use
`--t-body`/`--t-meta` tokens (no `text-sm`/`text-xs`) and `pt-1 pb-1` (not `pt-0.5`) for any
chip/badge padding in the new markup.

i18n — add period-picker chrome labels under a new `picker` object in BOTH JSON files (the
Daily/Weekly/Monthly chip labels REUSE the existing `frequency.*` keys — do not duplicate):

- `en/intelligence-digests.json`: add `"picker": { "step1": "Choose a period", "generatePreview": "Generate preview" }`
- `ar/intelligence-digests.json`: add `"picker": { "step1": "اختر فترة", "generatePreview": "إنشاء معاينة" }`
  (Add the `picker` object as a new top-level key; keep JSON valid.)
  </action>
  <verify>
  <automated>cd frontend && grep -q "translate: 'none'" src/components/intelligence/GenerateDigestButton.tsx && grep -q "min(100vw, 720px)" src/components/intelligence/GenerateDigestButton.tsx && grep -q "role=\"radiogroup\"" src/components/intelligence/GenerateDigestButton.tsx && grep -v '^#' src/components/intelligence/GenerateDigestButton.tsx | grep -cE 'text-sm|text-xs|pt-0\.5|pb-0\.5' | grep -qx 0 && node -e "const e=require('./src/i18n/en/intelligence-digests.json'),a=require('./src/i18n/ar/intelligence-digests.json'); if(!e.picker||!a.picker){process.exit(1)}" && echo OK</automated>
  </verify>
  <done>GenerateDigestButton renders a 720px end-edge drawer with both `transform`+`translate` 'none', a Daily/Weekly/Monthly radiogroup defaulting to the `period` prop, preview rollup, and Discard/Publish footer; generate/publish use `pickedPeriod`; no `text-sm`/`text-xs`/`pt-0.5`/`pb-0.5` in the file; `picker` keys present EN+AR; the 3 DigestsTab call sites still compile.</done>
  </task>

<task type="auto">
  <name>Task 4: Design-token / spacing / color sweep across the 8 P70 components</name>
  <files>frontend/src/components/intelligence/DigestsTab.tsx, frontend/src/components/intelligence/DigestSubscribeDrawer.tsx, frontend/src/components/intelligence/AlertRuleForm.tsx, frontend/src/components/intelligence/AlertsTab.tsx, frontend/src/components/intelligence/DigestReader.tsx, frontend/src/components/intelligence/AlertRuleRow.tsx, frontend/src/components/intelligence/DigestCard.tsx</files>
  <action>
Across these 7 files (GenerateDigestButton is handled in Task 3), apply the typography and
spacing/color normalization. Surgical token swaps only — do not restructure layout.

TYPOGRAPHY (replace raw Tailwind size classes with token expressions):

- Every `text-sm` → `[font-size:var(--t-body)]`
- Every `text-xs` → `[font-size:var(--t-meta)]`
  Apply ONLY to font-size classes. Do NOT touch `font-mono`, `font-medium`, `font-semibold`,
  `text-ink`, `text-ink-mute`, `text-danger`, or already-tokenized `[font-size:var(--t-*)]`.
  When a class string is e.g. `"... font-mono text-xs uppercase text-accent-ink"`, replace only
  the `text-xs` segment → `[font-size:var(--t-meta)]`, keeping `font-mono uppercase text-accent-ink`.

SPACING (badge padding minimum 4px):

- Every `pt-0.5` → `pt-1` and every `pb-0.5` → `pb-1` (the 2px badge padding flagged in the
  review). Known sites: DigestCard 145/179, DigestsTab 100, AlertRuleRow 101/106,
  DigestSubscribeDrawer 110/111, DigestReader 89/104. Grep each file and convert all occurrences.

COLOR (consistent wrapped-var token references):

- `DigestReader.tsx` line 104: severity badge `text-warning` → `text-[var(--warn)]`.
- `DigestCard.tsx` `SEVERITY_CLASSES` (lines 21-26): normalize all four to wrapped-var utilities —
  `urgent` and `high` → `'bg-[var(--danger-soft)] text-[var(--danger)]'`,
  `medium` → `'bg-[var(--warn-soft)] text-[var(--warn)]'`,
  `low` → `'bg-[var(--ok-soft)] text-[var(--ok)]'`.
  (Replace the `text-danger`/`text-warning` utility aliases with the `text-[var(--*)]` form so all
  four are consistent token references.)

Do not introduce any new colors, do not change `bg-accent-soft`/`text-accent-ink`/`bg-line-soft`
(those are valid mapped utilities), and keep all logical properties intact.
</action>
<verify>
<automated>cd frontend && for f in DigestsTab DigestSubscribeDrawer AlertRuleForm AlertsTab DigestReader AlertRuleRow DigestCard; do grep -v '^#' "src/components/intelligence/$f.tsx" | grep -cE '\btext-sm\b|\btext-xs\b|\bpt-0\.5\b|\bpb-0\.5\b|text-warning' | grep -qx 0 || { echo "FAIL $f"; exit 1; }; done && grep -q "bg-\[var(--ok-soft)\] text-\[var(--ok)\]" src/components/intelligence/DigestCard.tsx && pnpm exec tsc --noEmit --pretty false -p tsconfig.json 2>&1 | grep -E "components/intelligence/(DigestsTab|DigestSubscribeDrawer|AlertRuleForm|AlertsTab|DigestReader|AlertRuleRow|DigestCard)" | grep -v '^#' | grep -c error | grep -qx 0 && echo OK</automated>
</verify>
<done>None of the 7 files contain `text-sm`, `text-xs`, `pt-0.5`, `pb-0.5`, or `text-warning`; DigestCard SEVERITY_CLASSES use consistent `text-[var(--*)]` wrapped-var form for all four severities; no new tsc errors in these files.</done>
</task>

<task type="auto">
  <name>Task 5: DigestReader unsubscribe link + specific alert edit label + bilingual dialog close</name>
  <files>frontend/src/components/intelligence/DigestReader.tsx, frontend/src/i18n/en/intelligence-alerts.json, frontend/src/i18n/ar/intelligence-alerts.json, frontend/src/components/ui/dialog.tsx</files>
  <action>
THREE small spec/a11y gaps.

(a) DigestReader unsubscribe link — in `DigestReader.tsx`, import `useUnsubscribeFromDigest`
from `@/domains/signals/hooks/useDigests` (add to the existing `useDigests` import line). In the
footer (lines 121-128), add a ghost-link `Button` after the published-timestamp `<span>`,
end-aligned with `className="ms-auto text-ink-mute"`, labeled `t('action.unsubscribe')` (key
already exists in intelligence-digests.json). Its `onClick` calls
`unsubscribe.mutate({ dossierId: digest.dossier_id })`. Keep the footer's existing
`[font-size:var(--t-meta)] text-ink-faint` classes on the wrapper.

(b) Specific edit-mode label — in `intelligence-alerts.json`, change `action.save` from the
generic `"Save"` to a specific edit label in BOTH files:

- `en/intelligence-alerts.json` `action.save`: `"Save changes"`
- `ar/intelligence-alerts.json` `action.save`: `"حفظ التغييرات"`
  (`AlertRuleForm.tsx` line 364 already renders `t('action.save')` in edit mode — no component
  change needed.)

(c) Bilingual dialog close — in `dialog.tsx`, give the `<DialogPrimitive.Close>` a localized
accessible name. Add `import { useTranslation } from 'react-i18next'` and inside the
`DialogContent` forwardRef body call `const { t } = useTranslation('common')`. Set BOTH the
button `aria-label={t('common.close', { defaultValue: 'Close' })}` on the
`<DialogPrimitive.Close>` element AND replace the sr-only text
`<span className="sr-only">Close</span>` with `<span className="sr-only">{t('common.close', { defaultValue: 'Close' })}</span>`.
The `common.close` key already exists (EN "Close" / AR "إغلاق") — no new i18n key needed.
This is the ONLY change to dialog.tsx — do NOT alter positioning/centering classes or any
other behavior, so the 100+ other consumers are unaffected.
</action>
<verify>
<automated>cd frontend && grep -q "useUnsubscribeFromDigest" src/components/intelligence/DigestReader.tsx && grep -q "dossierId: digest.dossier_id" src/components/intelligence/DigestReader.tsx && node -e "const e=require('./src/i18n/en/intelligence-alerts.json'),a=require('./src/i18n/ar/intelligence-alerts.json'); if(e.action.save!=='Save changes'||a.action.save!=='حفظ التغييرات'){process.exit(1)}" && grep -q "useTranslation('common')" src/components/ui/dialog.tsx && grep -q "aria-label={t('common.close'" src/components/ui/dialog.tsx && pnpm exec tsc --noEmit --pretty false -p tsconfig.json 2>&1 | grep -E "components/intelligence/DigestReader|components/ui/dialog" | grep -v '^#' | grep -c error | grep -qx 0 && echo OK</automated>
</verify>
<done>DigestReader footer has an end-aligned Unsubscribe ghost link wired to `unsubscribe.mutate`; `action.save` reads "Save changes"/"حفظ التغييرات"; dialog close button has a bilingual `aria-label` and sr-only text from `common.close`; no new tsc errors in DigestReader or dialog.</done>
</task>

</tasks>

<verification>
Full repository typecheck (the authoritative gate — confirms no regressions across the
100+ dialog consumers and the 3 GenerateDigestButton call sites):

```bash
cd frontend && pnpm exec tsc --noEmit --pretty false -p tsconfig.json 2>&1 | grep -v '^#' | grep -c error
```

Expected: `0`.

i18n parity (every touched JSON parses and the new keys exist EN+AR):

```bash
cd frontend && node -e "
const f=['en/dossier-shell','ar/dossier-shell','en/intelligence-digests','ar/intelligence-digests','en/intelligence-alerts','ar/intelligence-alerts'];
for (const n of f) require('./src/i18n/'+n+'.json');
const ed=require('./src/i18n/en/dossier-shell.json'), ad=require('./src/i18n/ar/dossier-shell.json');
const edg=require('./src/i18n/en/intelligence-digests.json'), adg=require('./src/i18n/ar/intelligence-digests.json');
const ea=require('./src/i18n/en/intelligence-alerts.json'), aa=require('./src/i18n/ar/intelligence-alerts.json');
if(!ed.tabs.digests||!ad.tabs.digests||!edg.picker||!adg.picker) throw new Error('missing keys');
if(ea.action.save!=='Save changes'||aa.action.save!=='حفظ التغييرات') throw new Error('save label');
console.log('i18n OK');
"
```

Design-rule grep gates (no raw color literals / no banned font-size or 2px padding in P70 components):

```bash
cd frontend && for f in DigestsTab DigestCard DigestReader DigestSubscribeDrawer GenerateDigestButton AlertsTab AlertRuleRow AlertRuleForm; do
  grep -v '^#' "src/components/intelligence/$f.tsx" | grep -nE '\btext-sm\b|\btext-xs\b|\bpt-0\.5\b|\bpb-0\.5\b|text-warning|#[0-9a-fA-F]{3,6}|text-(blue|red|green|yellow)-[0-9]' && { echo "FAIL $f"; exit 1; } || true
done; echo "design gates clean"
```

Browser re-verification (1024px, 1400px, RTL) is the ORCHESTRATOR's job against the running
Vite dev server on :5173 — the executor does NOT run a browser.
</verification>

<success_criteria>

- DossierTabNav exposes a Digests tab on all 8 dossier types (D-12 closed); `tabs.digests` EN+AR present.
- Intelligence hub tab strip uses the `.tab`/`.tab.active` accent-underline pattern; zero filled Button toggles remain.
- GenerateDigestButton is a 720px end-edge drawer with a Daily/Weekly/Monthly period picker and both `transform:'none'`+`translate:'none'` (no off-screen regression); generate/publish use the picked period; mutation logic + `digestKeys.all` invalidation preserved.
- All 8 P70 components use `--t-body`/`--t-meta` tokens (no `text-sm`/`text-xs`), `pt-1`/`pb-1` badge padding (no 2px), and consistent `text-[var(--*)]` severity colors (no `text-warning` alias).
- DigestReader footer has an end-aligned Unsubscribe ghost link; alert edit label is "Save changes"/"حفظ التغييرات"; dialog close button has a bilingual accessible name.
- `pnpm exec tsc --noEmit` reports 0 errors; all 6 i18n JSON files parse with the new keys EN+AR.
  </success_criteria>

<output>
Create `.planning/quick/260616-l4e-fix-remaining-phase-70-ui-review-finding/260616-l4e-SUMMARY.md` when done.
</output>
