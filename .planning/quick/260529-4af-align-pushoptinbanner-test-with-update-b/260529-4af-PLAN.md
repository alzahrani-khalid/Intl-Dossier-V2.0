---
quick_id: 260529-4af
title: Align PushOptInBanner test with update-based dismissal; surface missing ALLOWED_ORIGINS
status: in_progress
created: 2026-05-29
mode: quick
source: PR 32 CI triage — "Tests (frontend)" required check red (1 failed / 1238 passed)
---

# Quick Task 260529-4af — Align PushOptInBanner test with update-based dismissal

Follow-up to 260529-1s8 (PR 32). The 1s8 silent-write fix changed
`PushOptInBanner.persistDismissal` from a clobbering `.upsert(...)` to a targeted
`.update({ push_prompt_dismissed_at }).eq('user_id', user.id)` — but the unit
test was not updated alongside it, so it still mocks `upsert` and asserts
`mockUpsert` was called. Result: `mockUpsert` is called 0 times →
`Tests (frontend)` (a required merge check) fails. The component is correct;
the **test** is stale.

Scope = test alignment only. Do NOT touch the component. Also fold in two
already-edited working-tree files the user approved (cors.ts warn-once + a
dossiers-create reformat) so PR 32 carries them.

Each commit must pass `pnpm build` (pre-commit hook builds every commit).

## Task 1 — Realign the PushOptInBanner dismissal test to update().eq()

- **Files:** `frontend/src/__tests__/PushOptInBanner.test.tsx`
- **Action:**
  1. In the `@/lib/supabase` mock, split the chained `.eq` so the fetch path and
     the dismiss path don't share one mock: keep `select → mockSelectEq →
maybeSingle` for the fetch, and add `update → mockUpdateEq` where
     `mockUpdateEq` resolves `{ error: null }`. `from('user_preferences')` now
     returns `{ select, update }` (drop the dead `upsert`).
  2. Rewrite the "persists dismissal when dismiss button is clicked" assertion:
     `expect(mockUpdate).toHaveBeenCalledWith(objectContaining({ push_prompt_dismissed_at: any(String) }))`
     and `expect(mockUpdateEq).toHaveBeenCalledWith('user_id', 'test-user-id')`.
     (user_id moves from the upsert payload to the `.eq` filter — matches the
     component.)
- **Verify:** `cd frontend && pnpm vitest --run src/__tests__/PushOptInBanner.test.tsx`
  → all tests pass (was 1 failing).
- **Done:** The single introduced, required-check failure on PR 32 is cleared.

## Folded into the same code commit (user-approved working-tree edits)

- `supabase/functions/_shared/cors.ts` — warn-once when `ALLOWED_ORIGINS` is
  unset in a non-development environment (deployed origins would otherwise be
  silently rejected with `Access-Control-Allow-Origin: null`), plus prettier
  reformat. Do not revert/reformat further.
- `supabase/functions/dossiers-create/index.ts` — prettier reformat only, no
  logic change. Do not revert/reformat further.

## Out of scope

- The flaky `Tests (backend)` failure (`queue-processing` debounce 99 vs 100ms)
  is unrelated to PR 32 and clears on CI re-run via `gh pr update-branch`.
- E2E / Visual / Accessibility / RTL / integration checks are non-required and
  pre-existing (issue #31).
- Orchestrator (not this task) handles push, `gh pr update-branch 32`, and merge.
