---
phase: 260607-h9z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Dashboard/widgets/OverdueCommitments.tsx
  - frontend/src/pages/Dashboard/widgets/__tests__/OverdueCommitments.test.tsx
  - frontend/src/hooks/useCountries.ts
  - frontend/src/hooks/__tests__/useCountries.test.ts
  - frontend/src/components/list-page/DossierTable.tsx
  - frontend/src/components/list-page/__tests__/DossierTable.test.tsx
  - frontend/src/styles/list-pages.css
  - backend/src/queues/deadline-scheduler.ts
  - backend/src/queues/digest-scheduler.ts
  - backend/src/queues/notification.queue.ts
  - backend/tests/deadline-checker.test.ts
autonomous: true
requirements:
  - 'FIND-1: Dashboard overdue-commitment items are clickable'
  - 'FIND-2: Country list engagement_count computed (latent always-zero bug)'
  - 'FIND-3: DossierTable row a11y semantics (button-in-list, not role=listitem on button)'
  - 'FIND-4: BullMQ idempotent repeatable registration + surfaced deadline-check errors'

must_haves:
  truths:
    - 'Each overdue-commitment row in the dashboard widget is a real button that opens the commitment detail drawer'
    - 'useCountries returns engagement_count per country row, computed from engagement_dossiers.host_country_id'
    - 'Each DossierTable row is exposed as an interactive button inside a semantic list item (no role conflict)'
    - 'Re-running deadline/digest registration does not create duplicate repeatable jobs'
    - 'A failed deadline-check query is surfaced (not silently swallowed)'
  artifacts:
    - path: 'frontend/src/pages/Dashboard/widgets/OverdueCommitments.tsx'
      provides: 'Clickable commitment rows via useCommitmentDrawer'
    - path: 'frontend/src/hooks/useCountries.ts'
      provides: 'engagement_count aggregation merge'
    - path: 'frontend/src/components/list-page/DossierTable.tsx'
      provides: 'ul/li/button a11y structure'
    - path: 'backend/src/queues/deadline-scheduler.ts'
      provides: 'Idempotent registration + surfaced query errors'
    - path: 'backend/src/queues/notification.queue.ts'
      provides: 'Explicit worker lock options'
  key_links:
    - from: 'OverdueCommitments.tsx'
      to: 'useCommitmentDrawer'
      via: 'openCommitment(c.id)'
      pattern: 'openCommitment'
    - from: 'useCountries.ts'
      to: 'engagement_dossiers'
      via: "second .in('host_country_id', ids) query"
      pattern: 'engagement_dossiers'
    - from: 'deadline-scheduler.ts / digest-scheduler.ts'
      to: 'notificationQueue'
      via: 'upsertJobScheduler'
      pattern: 'upsertJobScheduler'
---

<objective>
Fix four verified code defects surfaced by the Country-Dossier workflow inspection.
Evidence is already gathered and confirmed against live staging — do NOT re-investigate.
Each task maps to exactly one atomic commit.

Purpose: Remove three frontend interaction/data/a11y defects and one backend
reliability defect that were confirmed by a 6-agent verification pass + live DB checks.
Output: 4 commits across the files listed in frontmatter, each independently verified.

Out of scope (verified NOT code defects): Saudi-Arabia empty dossier (data-seeding),
Vite HMR stale-cache on the already-deleted flags/ dir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — no exploration required. -->

useCommitmentDrawer (frontend/src/hooks/useCommitmentDrawer.ts) — already wired:
openCommitment(id: string): void // toggles the `commitment` search param
The global <CommitmentDrawer /> is mounted in frontend/src/routes/\_protected.tsx:101,
which wraps the dashboard, so openCommitment works from the OverdueCommitments widget.
Canonical precedent: frontend/src/components/dossier/DossierDrawer/OpenCommitmentsSection.tsx
(useCommitmentDrawer().openCommitment(it.id) inside a <button type="button">).

GroupedCommitmentItem (frontend/src/hooks/usePersonalCommitments.ts):
{ id: string; title: string; daysOverdue: number; severity: 'red'|'amber'|'yellow'; ownerInitials: string }
`id` IS the commitment id (use it for openCommitment).

CountryDossier (frontend/src/hooks/useCountries.ts) currently:
interface CountryDossier extends Dossier { iso_code?: string }
The hook already does a second `.in('id', ids)` query against `countries` to merge iso_code.
Mirror that exact pattern for the engagement_dossiers count.

engagement_dossiers.host_country_id confirmed in frontend/src/types/database.types.ts:10306
(FK engagement_dossiers_host_country_id_fkey → dossiers.id). This is the schema-canonical
country↔engagement link.

DossierTableRow / DossierTable (frontend/src/components/list-page/DossierTable.tsx):
Row is already a native <button type="button"> but ALSO carries role="listitem"
(the conflict) and no aria-label. Container is <div role="list">.

.dossier-row CSS (frontend/src/styles/list-pages.css:930-948): each row is its own
`display:grid`; `.dossier-row { border-bottom: 1px }` draws dividers and
`.dossier-row:last-child { border-bottom: 0 }` drops the trailing divider. This
last-child rule must be re-scoped when rows are wrapped in <li> (see Task 3).

BullMQ 5.77.1 — use queue.upsertJobScheduler(schedulerId, repeatOpts, { name, data })
for idempotent repeatable registration (supersedes add(name,data,{repeat,jobId})).
The worker branches on job.name, so the job-template `name` MUST stay
'check-deadlines' / 'process-daily-digests' / 'process-weekly-digests'.
Worker accepts lockDuration and maxStalledCount options.

notificationWorker (backend/src/queues/notification.queue.ts:31-48): Worker currently
passes only { connection, concurrency: 5 }.
</interfaces>

@frontend/src/pages/Dashboard/widgets/RecentDossiers.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make dashboard overdue-commitment items clickable (FIND-1)</name>
  <files>
    frontend/src/pages/Dashboard/widgets/OverdueCommitments.tsx
    frontend/src/pages/Dashboard/widgets/__tests__/OverdueCommitments.test.tsx
  </files>
  <behavior>
    - Each commitment row renders as a native <button type="button"> reachable by role/name.
    - Clicking a commitment row calls useCommitmentDrawer().openCommitment(c.id) with that item's id.
    - Clicking the expand/collapse toggle still calls neither openCommitment nor openDossier.
    - The existing dossier-group-head button behavior (openDossier) is unchanged.
  </behavior>
  <action>
    In OverdueCommitments.tsx, the inner commitment list currently renders each item
    (lines ~155-179) as a static <li className="overdue-row overdue-item ...">. Convert
    each item so the clickable element is a real <button type="button">: keep the outer
    <li key={c.id}> as the list item, and move the existing row content (severity dot,
    title span, LtrIsolate days, owner-initials span, ArrowUpRight icon) inside a
    <button>. The button MUST keep the `overdue-row overdue-item` classNames (tests query
    `.overdue-row`), add `w-full text-start`, mirror RecentDossiers hover styling
    (`hover:bg-surface/60`), and add a focus-visible ring using tokens consistent with
    DossierTable (`focus:outline-none focus-visible:outline-2 focus-visible:outline-offset-[-2px]
    focus-visible:outline-[var(--accent)]`). Set `aria-label={c.title}` (mirrors
    RecentDossiers `aria-label={name}`). Wire `onClick` to a new handler that calls
    `openCommitment(c.id)`.

    Add the hook: import `useCommitmentDrawer` from `@/hooks/useCommitmentDrawer` and
    destructure `const { openCommitment } = useCommitmentDrawer()` alongside the existing
    `useDossierDrawer()` call. This opens the commitment detail drawer in place via the
    `commitment` search param read by the globally-mounted <CommitmentDrawer /> — the same
    pattern OpenCommitmentsSection.tsx already uses. Do NOT navigate to /commitments.

    Keep design tokens only (no raw hex, no text-blue-*), keep RTL logical properties
    (text-start, gap, icon-flip already present), and keep explicit return types. The
    ArrowUpRight icon already signals interactivity — leave it.

    Update the test: add a `useCommitmentDrawer` mock exposing an `openCommitment` spy
    (mirror the existing `useDossierDrawer` mock block), and add a test asserting that
    clicking a commitment row button (e.g. the red item, by role 'button' name 'Red item')
    calls openCommitment with that item's id ('c5'). Verify the existing expand test still
    asserts neither drawer opens on the toggle click (extend it to also assert the
    openCommitment spy was not called).

  </action>
  <verify>
    <automated>pnpm --filter frontend test -- --run src/pages/Dashboard/widgets/__tests__/OverdueCommitments.test.tsx</automated>
  </verify>
  <done>Each overdue-commitment item is a keyboard-reachable button whose click opens the commitment drawer via openCommitment(c.id); widget test suite green; lint + type-check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compute country engagement_count in useCountries (FIND-2)</name>
  <files>
    frontend/src/hooks/useCountries.ts
    frontend/src/hooks/__tests__/useCountries.test.ts
  </files>
  <behavior>
    - useCountries returns each row with engagement_count: number (0 when no engagements).
    - engagement_count for a country = number of engagement_dossiers rows whose host_country_id equals that country's id.
    - iso_code merge behavior is unchanged.
    - Supabase error on the primary dossiers query still propagates as before.
  </behavior>
  <action>
    In useCountries.ts, after the existing iso-merge block (the second query against
    `countries` via `.in('id', ids)`), add a parallel aggregation against
    `engagement_dossiers`. For the same `ids`, run
    `supabase.from('engagement_dossiers').select('host_country_id').in('host_country_id', ids)`,
    then tally into a `const countById: Record<string, number> = {}` by incrementing per
    non-null `host_country_id`. PostgREST has no simple GROUP BY here, so count in JS — this
    mirrors the existing iso-merge (a second `.in()` query + JS reduce). Guard `ids.length > 0`.

    Update the merge map so each row becomes
    `{ ...d, iso_code: isoById[d.id], engagement_count: countById[d.id] ?? 0 }`.

    Add `engagement_count?: number` to the `CountryDossier` interface (next to `iso_code?`).
    Keep explicit return types and no floating promises. Note that
    frontend/src/routes/_protected/dossiers/countries/index.tsx:95 already reads
    `d.engagement_count` — this populates it. This is the schema-canonical link
    (engagement_dossiers.host_country_id); it reads 0 today because host_country_id is NULL
    on all rows in staging, but it removes the latent always-zero bug. Do NOT touch the
    route file.

    Update the test: the mocked `inMock` is shared by both extension lookups. Change it from
    `mockResolvedValue` to a `mockImplementation((column: string) => column === 'host_country_id'
    ? Promise.resolve(engagementResolved) : Promise.resolve(countriesResolved))` so the iso
    query (`.in('id', …)`) and the count query (`.in('host_country_id', …)`) resolve
    independently and order-independently. Add a third `engagementResolved` parameter to
    `buildBuilder` (default `{ data: [] }`). Add a test asserting that when
    engagement_dossiers returns three rows for id 'c1', the merged row has
    `engagement_count === 3`, and assert `fromMock` was called with 'engagement_dossiers' and
    `inMock` with ('host_country_id', ['c1']). Existing iso/pagination/error tests must stay green.

  </action>
  <verify>
    <automated>pnpm --filter frontend test -- --run src/hooks/__tests__/useCountries.test.ts</automated>
  </verify>
  <done>useCountries merges engagement_count from engagement_dossiers.host_country_id onto each CountryDossier; interface updated; test asserts the merge + the new query; lint + type-check clean. Commit body notes organizations/index.tsx has the same latent pattern (host_organization_id) — out of scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix DossierTable row a11y semantics (FIND-3)</name>
  <files>
    frontend/src/components/list-page/DossierTable.tsx
    frontend/src/components/list-page/__tests__/DossierTable.test.tsx
    frontend/src/styles/list-pages.css
  </files>
  <behavior>
    - The table exposes one list (role="list") containing one listitem per row.
    - Each row is an interactive button reachable by role 'button' AND accessible name (dossier display name).
    - Pressing Enter on a focused row button fires onRowClick.
    - Row dividers (border between rows, none after the last) are visually preserved.
  </behavior>
  <action>
    In DossierTable.tsx, fix the role conflict where the row <button> also carries
    role="listitem" (the explicit listitem role suppresses the button's interactive role in
    the a11y tree). Restructure: keep the outer wrapper as a plain
    `<div className="card overflow-hidden p-0">` (remove role="list" and aria-label from this
    div). Keep the existing desktop header `<div className="dossier-row label hidden md:grid">`
    as the first child. Then render the rows inside a new
    `<ul role="list" aria-label={...} className="dossier-row-list">` (move the existing
    table.aria label onto the <ul>). Each row becomes
    `<li key={row.id}>` containing the existing `<button type="button">` — REMOVE
    `role="listitem"` from the button (the <li> now provides listitem semantics) and ADD
    `aria-label={displayName}`. Keep the button's grid classes, `dossier-row`, the
    focus-visible token outline, hover, DossierGlyph, all spans, and the mobile ChevronRight
    exactly as-is.

    In list-pages.css, the divider depends on `.dossier-row:last-child { border-bottom: 0 }`
    (lines ~946-948), which breaks once each row is the only/last child of its <li> (every
    row would match and lose its border). Replace that single rule so the trailing-divider
    removal keys off the list item instead, and add list reset styling:
      - add `.dossier-row-list { list-style: none; margin: 0; padding: 0; }`
      - replace `.dossier-row:last-child { border-bottom: 0; }` with
        `.dossier-row-list > li:last-child .dossier-row { border-bottom: 0; }`
    The base `.dossier-row { border-bottom: 1px }` still draws dividers between all rows; the
    header div keeps its bottom border (it is not inside the <ul>). Tokens only; no new colors.

    Update the test: add an assertion that the row is reachable via
    `screen.getByRole('button', { name: 'Saudi Arabia' })`; assert `screen.getByRole('list')`
    exists and `within(list).getAllByRole('listitem')` has the expected length; and add a
    keyboard test using `@testing-library/user-event` — focus the row button and press
    `{Enter}`, asserting onRowClick fired with the row (the precedent
    WeekListMobile.test.tsx uses getByRole('list')/getAllByRole('listitem'); FirstRunModal.test.tsx
    imports userEvent). Keep the existing tests green.

  </action>
  <verify>
    <automated>pnpm --filter frontend test -- --run src/components/list-page/__tests__/DossierTable.test.tsx</automated>
  </verify>
  <done>DossierTable renders a ul[role=list] > li > button[aria-label] structure with the button role intact; row dividers preserved; tests assert role/name reachability + Enter activation; lint + type-check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Idempotent BullMQ registration + surfaced deadline-check errors (FIND-4)</name>
  <files>
    backend/src/queues/deadline-scheduler.ts
    backend/src/queues/digest-scheduler.ts
    backend/src/queues/notification.queue.ts
    backend/tests/deadline-checker.test.ts
  </files>
  <behavior>
    - registerDeadlineChecker / registerDigestScheduler are idempotent: re-running them does not create duplicate repeatable jobs.
    - processDeadlineCheck surfaces a query error (rejects) instead of silently logging and continuing.
    - The notification worker declares explicit lock options.
    - Existing happy-path deadline tests still pass.
  </behavior>
  <action>
    Root cause: `tsx watch` reloads re-run registerDeadlineChecker()/registerDigestScheduler()
    (backend/src/index.ts ~140-143), adding duplicate repeatable jobs across new queue
    instances → lock contention. Apply the BullMQ 5.77 idempotent pattern (KISS, no
    Redis-flag complexity).

    deadline-scheduler.ts — registerDeadlineChecker: replace the
    `notificationQueue.add('check-deadlines', {} as never, { repeat: { every: ... }, jobId: 'deadline-checker' })`
    call with `notificationQueue.upsertJobScheduler('deadline-checker', { every: 15 * 60 * 1000 }, { name: 'check-deadlines', data: {} })`.
    upsertJobScheduler is idempotent by scheduler id (replaces the prior iteration rather than
    duplicating). The job-template name MUST stay 'check-deadlines' so the worker branch matches.

    digest-scheduler.ts — registerDigestScheduler: same swap for both schedulers:
    `upsertJobScheduler('daily-digest-processor', { every: 60 * 60 * 1000 }, { name: 'process-daily-digests', data: {} })`
    and `upsertJobScheduler('weekly-digest-processor', { every: 24 * 60 * 60 * 1000 }, { name: 'process-weekly-digests', data: {} })`.
    Keep names 'process-daily-digests' / 'process-weekly-digests' (worker branches on them).

    deadline-scheduler.ts — processDeadlineCheck: stop swallowing the query errors. Currently
    `approachError` / `overdueError` are logged then execution continues with null data. Change
    so that on either query error it logs with context AND throws (e.g.
    `throw new Error('Deadline check: approaching-tasks query failed: ' + approachError.message)`),
    and change the outer `catch (err) { logError('Deadline check failed', err) }` to re-throw
    after logging so BullMQ marks the job failed (the worker already has a `.on('failed')`
    handler + 3 retries) — failures must be visible. Keep the per-task `add` try/catch loops
    as-is (one bad task should not fail the whole batch).

    notification.queue.ts — add explicit Worker lock options to the notificationWorker
    constructor options object (alongside `connection` and `concurrency: 5`):
    `lockDuration: 60_000` and `maxStalledCount: 2`. The deadline/digest jobs scan many rows,
    so a longer lock avoids false stall detection under reload churn.

    All functions keep explicit return types (@typescript-eslint/explicit-function-return-type)
    and no floating promises.

    Update the test (backend/tests/deadline-checker.test.ts): add a regression test that when
    the first mocked query result is `{ data: null, error: { message: 'boom' } }`,
    `await expect(processDeadlineCheck()).rejects.toThrow()` (the mock thenable already resolves
    the whole `_resolve` object, so adding an `error` key flows into the destructure). Existing
    tests (which inject only `data`) must stay green because `error` is undefined on the
    happy path. The test mock of notification.queue only needs `add`; registration is not
    exercised by these tests, so the upsertJobScheduler swap needs no test-mock change.

  </action>
  <verify>
    <automated>pnpm --filter backend test -- --run tests/deadline-checker.test.ts && pnpm --filter backend type-check</automated>
  </verify>
  <done>Registration uses idempotent upsertJobScheduler (no duplicate repeatable jobs on reload); processDeadlineCheck rejects on query error; worker declares lockDuration + maxStalledCount; new error-surfacing test green and existing tests pass; type-check clean.</done>
</task>

</tasks>

<verification>
Run once after all four commits (whole-repo gates, since CI Lint is repo-wide --max-warnings 0):

- `pnpm --filter frontend lint`
- `pnpm --filter frontend type-check`
- `pnpm --filter frontend test -- --run src/pages/Dashboard/widgets/__tests__/OverdueCommitments.test.tsx src/hooks/__tests__/useCountries.test.ts src/components/list-page/__tests__/DossierTable.test.tsx`
- `pnpm --filter backend lint`
- `pnpm --filter backend type-check`
- `pnpm --filter backend test -- --run tests/deadline-checker.test.ts`

Filename-case lint guard: all touched files keep their existing PascalCase
(components/**, widgets), camelCase (hooks/**), and kebab paths — no renames introduced.
</verification>

<success_criteria>

- Dashboard overdue-commitment rows open the commitment detail drawer on click (FIND-1).
- useCountries returns a computed engagement_count per country from engagement_dossiers.host_country_id (FIND-2).
- DossierTable rows are interactive buttons inside an accessible list (role/name reachable, dividers preserved) (FIND-3).
- Repeatable job registration is idempotent and deadline-check query errors are surfaced (FIND-4).
- All listed lint / type-check / test gates pass; four atomic commits, one per finding.
  </success_criteria>

<output>
Create `.planning/quick/260607-h9z-fix-country-dossier-inspection-findings-/260607-h9z-SUMMARY.md` when done.
</output>
