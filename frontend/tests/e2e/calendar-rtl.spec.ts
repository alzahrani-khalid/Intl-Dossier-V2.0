import { test } from '@playwright/test'

test.describe.skip('Phase 39: Calendar RTL Arabic dow + Indic digits', () => {
  test.skip('placeholder — assertions added by Wave 1 + Wave 2 plans', async () => {
    // Plan 39-05 activates this — see .planning/phases/39-kanban-calendar/39-05-PLAN.md
  })
})
