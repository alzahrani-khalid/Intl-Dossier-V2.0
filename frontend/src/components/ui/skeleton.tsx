/**
 * Skeleton — re-exports from heroui-skeleton.tsx
 *
 * Maintains the existing named exports so all call sites (DossierLoadingSkeletons,
 * WaitingQueue, etc.) keep working. Preset compositions (SkeletonCard,
 * SkeletonText, SkeletonTable, SkeletonAvatar, SkeletonButton) now live in
 * heroui-skeleton.tsx alongside the base primitive.
 */

export {
  HeroUISkeleton as Skeleton,
  SkeletonCard,
  SkeletonText,
  SkeletonTable,
  SkeletonAvatar,
  SkeletonButton,
} from './heroui-skeleton'
