/**
 * Badge — re-exports from heroui-chip.tsx
 *
 * Maintains shadcn-compatible named exports so all existing consumers
 * (import { Badge } from "@/components/ui/badge") keep working.
 */

export { HeroUIChip as Badge, badgeVariants } from './heroui-chip'
export type { BadgeProps } from './heroui-chip'
