/**
 * Tabs re-export — HeroUI v3 styled tabs
 *
 * Drop-in replacement: same Radix/shadcn API (Tabs, TabsList, TabsTrigger, TabsContent)
 * with HeroUI v3 visual design (rounded pill indicator, smooth transitions, muted bg).
 *
 * Internal implementation: ./heroui-tabs.tsx
 */

export {
  HeroUITabs as Tabs,
  HeroUITabsList as TabsList,
  HeroUITabsTrigger as TabsTrigger,
  HeroUITabsContent as TabsContent,
} from './heroui-tabs'
