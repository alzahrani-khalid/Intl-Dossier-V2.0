/**
 * DrawerCtaRow — Wave 0 stub. Plan 41-02 fills with Log engagement / Brief /
 * Follow / Open full dossier buttons.
 */
import type * as React from 'react'

export interface DrawerCtaRowProps {
  dossierId: string
  dossierType: string
}

export function DrawerCtaRow(_props: DrawerCtaRowProps): React.JSX.Element {
  return <div className="flex gap-2" />
}
