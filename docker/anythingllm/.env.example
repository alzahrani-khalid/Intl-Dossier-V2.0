# AnythingLLM Environment Variables
# Feature: 029-dynamic-country-intelligence
# Copy this file to .env and fill in your actual values

# AnythingLLM API Key (generate in UI after first launch)
# Used by Supabase Edge Functions to authenticate with AnythingLLM
ANYTHINGLLM_API_KEY=your-anythingllm-api-key-here

# JWT Secret for authentication (generate a random 32+ character string)
# Used for session management in AnythingLLM multi-user mode
JWT_SECRET=your-random-jwt-secret-here

# OpenAI API Key (for embeddings and LLM inference)
# Get from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Generate a strong JWT secret: openssl rand -hex 32
# 3. Add your OpenAI API key
# 4. Deploy AnythingLLM: docker-compose up -d
# 5. Access UI at http://localhost:3001 and create admin account
# 6. Generate API key in Settings → API Keys
# 7. Update ANYTHINGLLM_API_KEY in .env with generated key
# 8. Restart container: docker-compose restart
# 9. Set same secrets in Supabase Edge Functions:
#    - ANYTHINGLLM_URL=http://host.docker.internal:3001
#    - ANYTHINGLLM_API_KEY=(same as above)
#    - OPENAI_API_KEY=(same as above)
